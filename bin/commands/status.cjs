'use strict';

// Phase 204 Plan 01 Task 3: `markos status` command stub.
// Business logic ships in Plan 204-09 (markos status).

async function main(_opts) {
  process.stderr.write('markos status: not yet implemented (Plan 204-09 will ship)\n');
  process.exit(5);
}

module.exports = { main };
