'use strict';

// Phase 204 Plan 01 Task 3: `markos eval` command stub.
// Business logic ships in Plan 204-07 (markos eval).

async function main(_opts) {
  process.stderr.write('markos eval: not yet implemented (Plan 204-07 will ship)\n');
  process.exit(5);
}

module.exports = { main };
