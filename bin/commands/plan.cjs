'use strict';

// Phase 204 Plan 01 Task 3: `markos plan` command stub.
// Business logic ships in Plan 204-06 (markos plan).

async function main(_opts) {
  process.stderr.write('markos plan: not yet implemented (Plan 204-06 will ship)\n');
  process.exit(5);
}

module.exports = { main };
