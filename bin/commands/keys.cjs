'use strict';

// Phase 204 Plan 01 Task 3: `markos keys` command stub.
// Business logic (create/list/revoke API keys) ships in Plan 204-03.

async function main(_opts) {
  process.stderr.write('markos keys: not yet implemented (Plan 204-03 will ship)\n');
  process.exit(5);
}

module.exports = { main };
