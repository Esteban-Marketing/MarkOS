'use strict';

// Phase 204 Plan 01 Task 3: `markos login` command stub.
// Business logic (OAuth 2.0 device flow per RFC 8628) ships in Plan 204-02.

async function main(_opts) {
  process.stderr.write('markos login: not yet implemented (Plan 204-02 will ship)\n');
  process.exit(5);
}

module.exports = { main };
