'use strict';

// Phase 204 Plan 01 Task 3: `markos doctor` command stub.
// Business logic (filesystem + runtime + doctrine freshness checks) ships in Plan 204-11.

async function main(_opts) {
  process.stderr.write('markos doctor: not yet implemented (Plan 204-11 will ship)\n');
  process.exit(5);
}

module.exports = { main };
