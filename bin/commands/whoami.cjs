'use strict';

// Phase 204 Plan 01 Task 3: `markos whoami` command stub.
// Business logic ships in Plan 204-04 (markos whoami).

async function main(_opts) {
  process.stderr.write('markos whoami: not yet implemented (Plan 204-04 will ship)\n');
  process.exit(5);
}

module.exports = { main };
