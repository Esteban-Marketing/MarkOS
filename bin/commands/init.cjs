'use strict';

// Phase 204 Plan 01 Task 3: `markos init` command stub.
// Business logic ships in Plan 204-05 (markos init).

async function main(_opts) {
  process.stderr.write('markos init: not yet implemented (Plan 204-05 will ship)\n');
  process.exit(5);
}

module.exports = { main };
