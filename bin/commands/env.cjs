'use strict';

// Phase 204 Plan 01 Task 3: `markos env` command stub.
// Business logic (env pull/push/diff/merge) ships in Plan 204-08.

async function main(_opts) {
  process.stderr.write('markos env: not yet implemented (Plan 204-08 will ship)\n');
  process.exit(5);
}

module.exports = { main };
