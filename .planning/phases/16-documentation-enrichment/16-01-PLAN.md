---
id: "16-01-documentation-enrichment"
wave: 1
depends_on: ["15-01-strategic-enrichment"]
discipline: marketing_operations
tracking_required: false
requires_human_approval: false
autonomous: true
objective: "Implement executable documentation hardening for agent reliability across backend context, prompts, and protocol-lore navigation."
must_haves:
  - `@llm_context` blocks added to critical backend modules
  - 7 specialized prompts updated with failure/context sections
  - Protocol-lore deep links added and valid
  - Gold-standard behavior catalog created
---

# Plan 16-01: Documentation Enrichment

## Wave 1 — Backend Context Hardening

<task id="1" name="Add llm_context blocks to orchestrator">
  <read_first>
    - onboarding/backend/agents/orchestrator.cjs
    - .planning/phases/16-documentation-enrichment/16-CONTEXT.md
  </read_first>
  <action>
    Add concise `/** @llm_context ... */` blocks above major execution sections and exported orchestration behavior in `orchestrator.cjs`.
    
    Required block coverage:
    - Retry strategy intent and non-retryable failure boundaries.
    - Neuro-auditor optional path and fallback behavior.
    - Draft persistence expectations and failure tolerance when Supabase + Upstash Vector is unavailable.
  </action>
  <acceptance_criteria>
    - At least 3 `@llm_context` blocks exist in `orchestrator.cjs`.
    - Each block includes both intent and failure boundary language.
    - Existing runtime behavior is unchanged.
  </acceptance_criteria>
</task>

<task id="2" name="Add llm_context blocks to llm-adapter">
  <read_first>
    - onboarding/backend/agents/llm-adapter.cjs
    - .planning/phases/16-documentation-enrichment/16-CONTEXT.md
  </read_first>
  <action>
    Add concise `/** @llm_context ... */` blocks in `llm-adapter.cjs` for provider selection and fallback semantics.

    Required block coverage:
    - Provider priority resolution and why order matters.
    - Fatal vs recoverable adapter errors.
    - Expected shape of success/failure return payloads.
  </action>
  <acceptance_criteria>
    - At least 3 `@llm_context` blocks exist in `llm-adapter.cjs`.
    - Blocks document provider priority, boundaries, and output contract.
    - No functional code path changes.
  </acceptance_criteria>
</task>

## Wave 2 — Prompt Precision

<task id="3" name="Add failure/context sections to 7 specialized prompts">
  <read_first>
    - .agent/prompts/brand_enforcer_qa.md
    - .agent/prompts/cro_landing_page_builder.md
    - .agent/prompts/email_lifecycle_strategist.md
    - .agent/prompts/paid_media_creator.md
    - .agent/prompts/seo_content_architect.md
    - .agent/prompts/social_community_manager.md
    - .agent/prompts/telemetry_synthesizer.md
  </read_first>
  <action>
    For each file above, append or update two sections using consistent structure:
    - `## FAILURE MODE AWARENESS`
    - `## CONTEXT RELAY`

    Section rules:
    - Failure section must name at least 3 concrete failure patterns specific to that role.
    - Context section must point to authoritative protocol and template files to consult first.
    - Avoid generic phrasing; include role-specific constraints.
  </action>
  <acceptance_criteria>
    - All 7 prompt files contain both sections exactly once.
    - Each failure section has at least 3 concrete failure bullets.
    - Each context section includes references to `.protocol-lore/` and relevant MIR/MSP sources.
  </acceptance_criteria>
</task>

## Wave 3 — Lore Deep-Linking + Gold Standard

<task id="4" name="Deep-link protocol-lore index documents">
  <read_first>
    - .protocol-lore/INDEX.md
    - .protocol-lore/CODEBASE-MAP.md
  </read_first>
  <action>
    Add implementation deep links from architecture/lore summaries to concrete source files.

    Minimum link targets:
    - `onboarding/backend/agents/orchestrator.cjs`
    - `onboarding/backend/agents/llm-adapter.cjs`
    - `onboarding/backend/server.cjs`
    - `onboarding/backend/vector-store-client.cjs`
  </action>
  <acceptance_criteria>
    - Both lore files include direct implementation links.
    - No broken references after markdown validation.
    - Existing lore structure remains readable and concise.
  </acceptance_criteria>
</task>

<task id="5" name="Create gold-standard interaction catalog">
  <read_first>
    - .agent/prompts/
    - .planning/phases/16-documentation-enrichment/16-CONTEXT.md
  </read_first>
  <action>
    Create `.agent/prompts/examples/GOLD-STANDARD.md` with 4 high-quality examples:
    - 1 strategy/planning interaction
    - 1 content-generation interaction
    - 1 analytics/telemetry interaction
    - 1 verification/audit interaction

    Each example must include: user input, expected grounded reasoning pattern, and output format expectations.
  </action>
  <acceptance_criteria>
    - File exists with 4 clearly labeled examples.
    - Examples are concrete and role-specific, not generic placeholders.
    - Prompt files reference this catalog where relevant.
  </acceptance_criteria>
</task>

## Completion Gate

- [x] `@llm_context` blocks present in both critical backend modules.
- [x] All 7 specialized prompts include failure/context sections.
- [x] Protocol-lore deep links added and valid.
- [x] Gold-standard behavior catalog created.
- [x] `VERIFICATION.md` checks can be executed without ambiguity.

