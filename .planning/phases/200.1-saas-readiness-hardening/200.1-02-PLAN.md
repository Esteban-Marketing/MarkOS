---
phase: 200.1
phase_name: saas-readiness-hardening
plan: 02
plan_id: 200.1-02
subsystem: webhooks-migrations
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/rollback/70_markos_webhook_subscriptions.down.sql
autonomous: true
requirements:
  - WHK-01
  - QA-13
closes_review_concerns:
  - M3
must_haves:
  truths:
    - "supabase/migrations/rollback/70_markos_webhook_subscriptions.down.sql exists"
    - "Rollback drops markos_webhook_subscriptions and markos_webhook_deliveries with `if exists` guards"
    - "Rollback drops the RLS policies that migration 70 created (named exactly as migration 70 named them) before dropping tables"
    - "Rollback uses the same idempotent shape as Phase 201's rollback files (e.g. supabase/migrations/rollback/81_markos_orgs.down.sql)"
    - "Rollback can be executed against a freshly applied migration 70 and leaves zero leftover objects (verified via dry-run psql against a fresh test database)"
    - "M3 closed: gate-13 (idempotent migrations + rollback) PASSes for migration 70 because supabase/migrations/rollback/70_markos_webhook_subscriptions.down.sql exists with substantive drops"
  artifacts:
    - path: "supabase/migrations/rollback/70_markos_webhook_subscriptions.down.sql"
      provides: "Rollback script for migration 70"
      contains: "drop table if exists markos_webhook_subscriptions"
      min_lines: 25
  key_links:
    - from: "supabase/migrations/rollback/70_markos_webhook_subscriptions.down.sql"
      to: "supabase/migrations/70_markos_webhook_subscriptions.sql"
      via: "matches forward migration's CREATE shape (every CREATE TABLE/POLICY/INDEX has a matching DROP)"
      pattern: "drop\\s+(table|policy|index|trigger|function)\\s+if\\s+exists"
---

<objective>
Ship `supabase/migrations/rollback/70_markos_webhook_subscriptions.down.sql` mirroring the forward migration 70 in reverse. Closes review concern **M3** ("Migration rollback missing — quality-baseline gate 13 violation").

Purpose: Phase 200-03 shipped migration 70 creating `markos_webhook_subscriptions` and `markos_webhook_deliveries` (with their RLS policies + indexes), but never authored a corresponding rollback script under `supabase/migrations/rollback/`. The 15-gate baseline mandates one per forward migration. Phase 201 has already shipped rollbacks for migrations 81-87, so the pattern + path are well-established. This plan re-creates the missing 70.down.sql so gate 13 PASSes for Phase 200's webhook substrate.

Output: One file: `supabase/migrations/rollback/70_markos_webhook_subscriptions.down.sql` with idempotent `drop ... if exists` statements covering every object migration 70 created, in reverse-dependency order.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/200.1-saas-readiness-hardening/200.1-CONTEXT.md
@.planning/phases/200-saas-readiness-wave-0/QUALITY-BASELINE.md
@./CLAUDE.md
@supabase/migrations/70_markos_webhook_subscriptions.sql
@supabase/migrations/rollback/81_markos_orgs.down.sql

<interfaces>
The forward migration `supabase/migrations/70_markos_webhook_subscriptions.sql` creates (per Phase 200-03 SUMMARY):
- `create table markos_webhook_subscriptions` with columns id, tenant_id, url, secret, events[], created_at, updated_at, status
- `create table markos_webhook_deliveries` with columns id, subscription_id, event_id, attempt_count, last_attempt_at, status, payload jsonb
- RLS policies on both tables (membership-bound — tenant_id matches `markos_org_memberships.tenant_id` for the requesting `auth.uid()`)
- Indexes: at minimum `idx_markos_webhook_deliveries_subscription_id`, `idx_markos_webhook_subscriptions_tenant_id`

Rollback shape (Phase 201 reference — `supabase/migrations/rollback/81_markos_orgs.down.sql`):
```sql
-- Phase 201-01 rollback for migration 81_markos_orgs.sql
-- Idempotent: safe to re-run.

-- 1. Drop policies (must precede table drop because they reference the table).
drop policy if exists markos_orgs_select on markos_orgs;
drop policy if exists markos_orgs_insert on markos_orgs;
-- ...
-- 2. Drop indexes (CASCADE not needed; table drop will clean leftovers).
drop index if exists idx_markos_orgs_owner_user_id;
-- 3. Drop tables in reverse dependency order.
drop table if exists markos_org_memberships;
drop table if exists markos_orgs;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author supabase/migrations/rollback/70_markos_webhook_subscriptions.down.sql</name>
  <files>supabase/migrations/rollback/70_markos_webhook_subscriptions.down.sql</files>
  <read_first>
    - supabase/migrations/70_markos_webhook_subscriptions.sql (extract every CREATE statement — every one needs a matching DROP in this rollback)
    - supabase/migrations/rollback/81_markos_orgs.down.sql (style + ordering reference)
    - supabase/migrations/rollback/82_markos_audit_log_hash_chain.down.sql (style reference if exists)
    - .planning/phases/200.1-saas-readiness-hardening/200.1-CONTEXT.md (D-208)
  </read_first>
  <action>
    Read `supabase/migrations/70_markos_webhook_subscriptions.sql` to enumerate exactly which objects it creates. For each CREATE in the forward migration, emit a DROP IF EXISTS in the rollback in reverse dependency order:

    1. Top of file:
       ```sql
       -- Phase 200.1 D-208 rollback for supabase/migrations/70_markos_webhook_subscriptions.sql
       -- Idempotent: every drop guarded by `if exists`. Safe to re-run.
       -- Ships under Phase 200.1 (the rollback Phase 200 originally omitted).
       --
       -- Order: policies → triggers → indexes → tables (reverse dependency).
       ```

    2. **Drop policies first** (every policy named in migration 70). Format:
       `drop policy if exists <policy_name> on <table>;`
       Names to look for in the forward migration: anything matching `create policy <name> on markos_webhook_subscriptions` and `... on markos_webhook_deliveries`.

    3. **Drop triggers** (if any — e.g. `updated_at` triggers).

    4. **Drop indexes** explicitly (defense in depth even though `drop table` cleans them).

    5. **Drop tables** in reverse dependency order: `markos_webhook_deliveries` first (it FKs to subscriptions), then `markos_webhook_subscriptions`.
       ```sql
       drop table if exists markos_webhook_deliveries;
       drop table if exists markos_webhook_subscriptions;
       ```

    6. **Drop functions** if migration 70 created any (e.g. `tg_markos_webhook_subscriptions_updated_at()` if it has an updated_at trigger).

    Final file should be ≥25 lines. Use lower-case identifiers consistent with the forward migration. End-of-file blank line.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const path='supabase/migrations/rollback/70_markos_webhook_subscriptions.down.sql'; const s=fs.readFileSync(path,'utf8'); const checks={'drop_subscriptions': /drop\s+table\s+if\s+exists\s+markos_webhook_subscriptions/i.test(s), 'drop_deliveries': /drop\s+table\s+if\s+exists\s+markos_webhook_deliveries/i.test(s), 'has_policy_drop': /drop\s+policy\s+if\s+exists/i.test(s), 'header': s.includes('Phase 200.1 D-208 rollback'), 'min_lines': s.split('\n').length >= 25}; const failed=Object.entries(checks).filter(([k,v])=>!v); if(failed.length){console.error('Failed checks:',failed); process.exit(1)} else {console.log('all checks pass')}"</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/rollback/70_markos_webhook_subscriptions.down.sql` exists.
    - File contains `drop table if exists markos_webhook_subscriptions` (case-insensitive).
    - File contains `drop table if exists markos_webhook_deliveries` (case-insensitive).
    - File contains at least one `drop policy if exists ...` line.
    - File contains the literal string `Phase 200.1 D-208 rollback`.
    - File is ≥25 lines.
    - The reverse order is correct: `markos_webhook_deliveries` drop appears BEFORE `markos_webhook_subscriptions` drop in the file (deliveries references subscriptions via FK).
    - The set of `drop policy if exists <name>` matches the set of `create policy <name>` in the forward migration `supabase/migrations/70_markos_webhook_subscriptions.sql` (run `grep -c "create policy" supabase/migrations/70_markos_webhook_subscriptions.sql` and `grep -c "drop policy if exists" supabase/migrations/rollback/70_markos_webhook_subscriptions.down.sql` and assert they are equal).
  </acceptance_criteria>
  <done>
    Rollback file landed; gate-13 evidence available for D-211 retroactive verification.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator→psql | Only SREs with service-role + `psql` access can apply or reverse migrations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-200.1-02-01 | Tampering | rollback file | mitigate | Rollback uses `if exists` guards so partial-state databases don't error mid-rollback; idempotent shape matches Phase 201 rollback convention |
| T-200.1-02-02 | Denial of Service | rollback execution | accept | Dropping production webhook tables would break tenants; rollback is documented as STAGING-ONLY drill — production never invokes this without explicit operator decision |
| T-200.1-02-03 | Repudiation | rollback audit | accept | psql sessions logged at DB layer; no application-level audit row — DB-admin scope outside business audit chain |
</threat_model>

<verification>
- `ls supabase/migrations/rollback/70_markos_webhook_subscriptions.down.sql` succeeds.
- `grep -c "drop table if exists" supabase/migrations/rollback/70_markos_webhook_subscriptions.down.sql` ≥ 2.
- `grep -c "drop policy if exists" supabase/migrations/rollback/70_markos_webhook_subscriptions.down.sql` ≥ 1.
- `grep -n "Phase 200.1 D-208 rollback" supabase/migrations/rollback/70_markos_webhook_subscriptions.down.sql` returns ≥ 1 match.
</verification>

<success_criteria>
- Rollback exists and is substantive (≥25 lines, every forward-migration object covered).
- Forward + rollback now form a complete, idempotent pair satisfying gate-13.
- M3 closed.
</success_criteria>

<output>
After completion, create `.planning/phases/200.1-saas-readiness-hardening/200.1-02-SUMMARY.md` documenting: file path, drops emitted, M3 closure evidence, parity with Phase 201 rollback style.
</output>
