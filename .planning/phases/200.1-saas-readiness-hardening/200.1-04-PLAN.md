---
phase: 200.1
phase_name: saas-readiness-hardening
plan: 04
plan_id: 200.1-04
subsystem: threat-models
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/200.1-saas-readiness-hardening/threat-models/mcp-stride.md
  - .planning/phases/200.1-saas-readiness-hardening/threat-models/webhooks-stride.md
  - .planning/phases/200.1-saas-readiness-hardening/threat-models/marketplace-stride.md
  - .planning/phases/200-saas-readiness-wave-0/DISCUSS.md
  - test/threat-models/stride-references.test.js
autonomous: true
requirements:
  - QA-11
  - MCP-01
  - WHK-01
closes_review_concerns:
  - H6
must_haves:
  truths:
    - "Three STRIDE threat-model artifacts exist under .planning/phases/200.1-saas-readiness-hardening/threat-models/: mcp-stride.md, webhooks-stride.md, marketplace-stride.md"
    - "Each artifact contains all 6 STRIDE categories: Spoofing, Tampering, Repudiation, Information Disclosure, Denial of Service, Elevation of Privilege"
    - "Each artifact lists at least 3 threats per category for that domain (≥18 threats per file)"
    - "Each threat row identifies: threat-id (T-200.1-{mcp|webhooks|marketplace}-NN), threat description, mitigation (cross-referencing 200.1-01..200.1-09 plans by plan_id where applicable), residual risk (low/medium/high), verifier (file path, test command, or audit-query SQL)"
    - "Phase 200 DISCUSS.md gains an addendum block (does NOT rewrite existing content) titled '## Threat models (added by Phase 200.1)' that links to the 3 STRIDE files"
    - "test/threat-models/stride-references.test.js parses each STRIDE markdown's Verifier column, asserts each referenced file path exists on disk (skip-and-warn for paths owned by deferred phases; fail for paths in this phase's own files_modified), asserts each referenced 200.1 plan ID resolves to an existing PLAN.md file; exits 0 when all references resolve OR are explicitly tagged `(deferred)` (BLOCKER #3 fix)"
    - "node --test test/threat-models/stride-references.test.js exits 0 after Wave 2 ships (when all 200.1-XX plans referenced by the STRIDE files have shipped their evidence)"
    - "H6 closed: gate-11 (threat model per new domain) PASSes for the MCP, webhooks, and marketplace surfaces ahead of D-211 verification"
  artifacts:
    - path: ".planning/phases/200.1-saas-readiness-hardening/threat-models/mcp-stride.md"
      provides: "STRIDE threat model for /api/mcp/* surface (session + tools)"
      contains: "Elevation of Privilege"
      min_lines: 120
    - path: ".planning/phases/200.1-saas-readiness-hardening/threat-models/webhooks-stride.md"
      provides: "STRIDE threat model for /api/webhooks/* surface (subscribe/dispatch/rotation)"
      contains: "Information Disclosure"
      min_lines: 120
    - path: ".planning/phases/200.1-saas-readiness-hardening/threat-models/marketplace-stride.md"
      provides: "STRIDE threat model for marketplace surface (.claude-plugin/marketplace.json + landing + demo sandbox)"
      contains: "Denial of Service"
      min_lines: 100
    - path: ".planning/phases/200-saas-readiness-wave-0/DISCUSS.md"
      provides: "Addendum linking to STRIDE files (existing content preserved)"
      contains: "Phase 200.1"
    - path: "test/threat-models/stride-references.test.js"
      provides: "Programmatic validator: parses Verifier columns from each STRIDE markdown, confirms file/plan ID references resolve on disk (BLOCKER #3 fix)"
      contains: "stride-references"
      min_lines: 150
  key_links:
    - from: ".planning/phases/200-saas-readiness-wave-0/DISCUSS.md"
      to: ".planning/phases/200.1-saas-readiness-hardening/threat-models/"
      via: "markdown links to all 3 STRIDE files"
      pattern: "threat-models/(mcp|webhooks|marketplace)-stride\\.md"
    - from: ".planning/phases/200.1-saas-readiness-hardening/threat-models/webhooks-stride.md"
      to: ".planning/phases/200.1-saas-readiness-hardening/200.1-01-PLAN.md"
      via: "mitigation cross-reference for SSRF/DNS-pin threats"
      pattern: "200\\.1-01"
    - from: ".planning/phases/200.1-saas-readiness-hardening/threat-models/mcp-stride.md"
      to: ".planning/phases/200.1-saas-readiness-hardening/200.1-07-PLAN.md"
      via: "mitigation cross-reference for MCP auth + cost-meter + kill-switch threats"
      pattern: "200\\.1-07"
    - from: "test/threat-models/stride-references.test.js"
      to: ".planning/phases/200.1-saas-readiness-hardening/threat-models/*.md"
      via: "regex parses Verifier column file/plan refs and asserts existence on disk"
      pattern: "stride-references"
---

<objective>
Author three STRIDE threat-model artifacts (`mcp-stride.md`, `webhooks-stride.md`, `marketplace-stride.md`) under `.planning/phases/200.1-saas-readiness-hardening/threat-models/`, append an addendum to `.planning/phases/200-saas-readiness-wave-0/DISCUSS.md` linking to them, AND ship a programmatic cross-reference validator at `test/threat-models/stride-references.test.js` that asserts every referenced file/plan-ID actually exists on disk (BLOCKER #3 fix). Closes review concern **H6**: "Threat models absent — gate-11 violation. Three new domains (MCP, webhooks, marketplace surface) ship with no formal threat model."

Purpose: QUALITY-BASELINE.md gate 11 mandates a STRIDE threat model in DISCUSS.md per new domain, before implementation. Phase 200's DISCUSS.md only carries a 5-row Risks table — that is not STRIDE. Phase 200.1 retroactively closes the gap. Each model targets one of the three new domains Phase 200 introduced (MCP server, webhook engine, Claude marketplace surface), enumerates threats per STRIDE category, cross-references the 200.1 plans that mitigate them, and records residual risk + verifier. The DISCUSS.md addendum surfaces the threat models from Phase 200's own navigation so future readers find them where gate-11 expects them. The new cross-reference validator (Task 5) closes BLOCKER #3 by enforcing that every "Verifier" column entry — both file paths AND plan IDs — actually resolves; without this, the STRIDE markdown can rot silently as referenced files get renamed or deleted.

Output: 3 markdown files (≥120 lines each for MCP + webhooks; ≥100 lines for marketplace) + 1 DISCUSS.md addendum block + 1 cross-reference validation test.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/200.1-saas-readiness-hardening/200.1-CONTEXT.md
@.planning/phases/200.1-saas-readiness-hardening/200.1-REVIEWS.md
@.planning/phases/200-saas-readiness-wave-0/DISCUSS.md
@.planning/phases/200-saas-readiness-wave-0/QUALITY-BASELINE.md
@./CLAUDE.md

<interfaces>
STRIDE format (Microsoft) for each threat domain:

```markdown
# STRIDE Threat Model — <Domain>

**Phase:** 200.1-saas-readiness-hardening
**Domain scope:** <surfaces, files>
**Trust boundaries:** <list>
**Authored:** 2026-04-27
**Last reviewed:** 2026-04-27

## Trust boundaries

| Boundary | Description |
|----------|-------------|
| ... | ... |

## Threat register

### Spoofing (3+ threats)

| Threat ID | Threat | Mitigation | Residual risk | Verifier |
|-----------|--------|-----------|---------------|----------|
| T-200.1-{domain}-S-01 | <description> | <plan_id reference + concrete control> | low/medium/high | <test path or audit query> |
| ... |

### Tampering (3+ threats)
...

### Repudiation (3+ threats)
...

### Information Disclosure (3+ threats)
...

### Denial of Service (3+ threats)
...

### Elevation of Privilege (3+ threats)
...

## Residual risk acceptance

| Risk | Severity | Owner | Acceptance rationale | Re-review trigger |
|------|----------|-------|----------------------|-------------------|
| ... | ... | ... | ... | ... |
```

Threat ID convention: `T-200.1-{mcp|webhooks|marketplace}-{S|T|R|I|D|E}-NN` where NN is two-digit sequential within the category.

Cross-reference validator surface (Task 5):
- Regex for file paths: `\b(test|lib|api|scripts|supabase)/[\S]+\.(test\.)?(js|cjs|ts|sql|md)\b`
- Regex for plan IDs: `\b200\.1-(0[1-9]|10|11)\b`
- Each captured reference is checked against `fs.existsSync` (file paths) or against `.planning/phases/*/200.1-XX-*-PLAN.md` glob (plan IDs).
- Rows tagged `(deferred)` in the same Verifier cell are skipped (a row may legitimately reference a future-phase artifact).
- Failure = test fails with a per-row diagnostic listing the unresolved reference.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author webhooks-stride.md (depends on 200.1-01 + 200.1-05 + 200.1-06 mitigation references)</name>
  <files>.planning/phases/200.1-saas-readiness-hardening/threat-models/webhooks-stride.md</files>
  <read_first>
    - .planning/phases/200.1-saas-readiness-hardening/200.1-CONTEXT.md (D-201, D-202, D-203 mitigation specs)
    - .planning/phases/200.1-saas-readiness-hardening/200.1-REVIEWS.md (H1, H2, H3 — translate review findings into threat rows)
    - lib/markos/webhooks/engine.cjs (subscribe/unsubscribe/list surface)
    - lib/markos/webhooks/delivery.cjs (delivery worker surface)
    - lib/markos/webhooks/signing.cjs (HMAC signing scheme)
    - api/webhooks/subscribe.js, api/webhooks/test-fire.js, api/webhooks/list.js (API surface)
    - supabase/migrations/70_markos_webhook_subscriptions.sql (data model)
  </read_first>
  <action>
    Author `webhooks-stride.md` with the following structure (≥120 lines):

    **Header block:**
    - Phase: 200.1-saas-readiness-hardening
    - Domain scope: `api/webhooks/*` (subscribe, unsubscribe, list, test-fire) + `lib/markos/webhooks/{engine,signing,delivery,url-validator,secret-vault}.{cjs,ts}` + tables `markos_webhook_subscriptions`, `markos_webhook_deliveries`, `markos_webhook_delivery_nonces` (200.1-05) + Supabase Vault entries for secrets (200.1-06)
    - Trust boundaries: tenant→subscribe, engine→queue, delivery worker→public internet, delivery→3xx redirect target, vault→delivery worker
    - Authored: 2026-04-27

    **Trust boundaries table:** 5 rows (one per boundary above) + description.

    **Threat register — 6 categories × 3+ threats each (≥18 rows total):**

    Spoofing (3+):
    - T-200.1-webhooks-S-01: Tenant impersonation at subscribe (forged auth) → mitigation: `requireHostedSupabaseAuth` middleware on `api/webhooks/subscribe.js` (Phase 200-03) + tenant_id derived from session, never request body → residual: low → verifier: `grep -n "requireHostedSupabaseAuth\\|tenant_id" api/webhooks/subscribe.js`
    - T-200.1-webhooks-S-02: Receiver impersonation (DNS spoofing of subscriber URL) → mitigation: 200.1-01 url-validator.cjs DNS-pin (resolve once, connect by IP, Host header preserved for SNI/cert validation) → residual: low → verifier: `node --test test/webhooks/url-validator.test.js` covers DNS-rebind cases
    - T-200.1-webhooks-S-03: HMAC signature forgery (signing-key compromise) → mitigation: 200.1-06 secret-vault stores secret encrypted, rotation endpoint allows revocation; key length 32 bytes random → residual: low → verifier: `lib/markos/webhooks/secret-vault.cjs` + `api/webhooks/rotate-secret.js`

    Tampering (3+):
    - T-200.1-webhooks-T-01: Payload tampering in transit → mitigation: HMAC-SHA256 over canonical body + headers (Phase 200-03 signing.cjs) → residual: low → verifier: `test/webhooks/signing.test.js`
    - T-200.1-webhooks-T-02: Webhook URL field tampering at storage layer → mitigation: RLS membership policy (Phase 201) ensures only org-admin members can mutate the row → residual: low → verifier: `grep -n "create policy" supabase/migrations/70_markos_webhook_subscriptions.sql`
    - T-200.1-webhooks-T-03: Replay attack on captured delivery → mitigation: 200.1-05 freshness-window 300s + nonce-store (markos_webhook_delivery_nonces) → residual: low → verifier: `node --test test/webhooks/replay-protection.test.js`

    Repudiation (3+):
    - T-200.1-webhooks-R-01: Tenant denies subscribing → mitigation: audit row at subscribe-time (`source_domain='webhooks' action='subscription.created'`) emitted inline (Phase 201.1 D-101 pattern) → residual: low → verifier: `grep -n "subscription.created" lib/markos/webhooks/engine.cjs`
    - T-200.1-webhooks-R-02: Operator denies rotating a secret → mitigation: 200.1-06 rotation endpoint emits audit `action='secret.rotated'` with actor_id from session → residual: low → verifier: `api/webhooks/rotate-secret.js`
    - T-200.1-webhooks-R-03: Receiver denies receiving a webhook → mitigation: HMAC signature in `X-Markos-Signature` + delivery row in `markos_webhook_deliveries` with attempt_count + last_attempt_at + status_code → residual: low → verifier: `markos_webhook_deliveries` table schema

    Information Disclosure (3+):
    - T-200.1-webhooks-I-01: SSRF — exfiltrate cloud metadata via attacker-controlled subscriber URL → mitigation: 200.1-01 url-validator.cjs deny-list rejects 169.254.169.254 + RFC1918 + IPv6 link-local → residual: low → verifier: `node --test test/webhooks/url-validator.test.js`
    - T-200.1-webhooks-I-02: Secret leak via plaintext DB column → mitigation: 200.1-06 migration 70.2 drops `secret` column, replaces with `secret_vault_ref`; secret only in Supabase Vault → residual: low → verifier: `grep -c "secret_vault_ref" supabase/migrations/70.2_markos_webhook_secret_vault.sql` ≥1
    - T-200.1-webhooks-I-03: Secret leak via log statements → mitigation: log-drain (`lib/markos/webhooks/log-drain.cjs`) does NOT log payload bodies or secrets; redaction enforced by content-allow-list → residual: medium → verifier: `grep -n "secret\\b" lib/markos/webhooks/log-drain.cjs` returns no log-emit code paths

    Denial of Service (3+):
    - T-200.1-webhooks-D-01: Delivery loop on slow receiver → mitigation: per-subscription rate-limit (Phase 203-07) + circuit breaker (Phase 203-08) → residual: low → verifier: `lib/markos/webhooks/breaker.cjs` exists
    - T-200.1-webhooks-D-02: Cost amplification via test-fire abuse → mitigation: per-tenant test-fire rate-limit (200-03 baseline) + 200.1-08 OTEL cost telemetry → residual: medium → verifier: `grep -n "rate-limit" api/webhooks/test-fire.js`
    - T-200.1-webhooks-D-03: Redirect chain DoS → mitigation: 200.1-01 url-validator MAX_REDIRECTS=3 hard cap → residual: low → verifier: `grep -n "DEFAULT_MAX_REDIRECTS" lib/markos/webhooks/url-validator.cjs`

    Elevation of Privilege (3+):
    - T-200.1-webhooks-E-01: Cross-tenant subscription read via missing RLS → mitigation: RLS membership policy on both tables → residual: low → verifier: `grep -c "create policy" supabase/migrations/70_markos_webhook_subscriptions.sql`
    - T-200.1-webhooks-E-02: Subscribe to tenant's webhook with stolen secret to forge signed events → mitigation: 200.1-06 rotation endpoint allows revocation; 200.1-05 nonce-store prevents replay even within freshness window → residual: low
    - T-200.1-webhooks-E-03: test-fire used to inject `consent.revoked` event → mitigation: M8 deferred — see 200.1-CONTEXT.md `<deferred>` for documented rationale; receiver-side documentation contract to treat `test=true` payloads non-authoritatively → residual: medium (operator-acceptance) → verifier: documented in 200-VERIFICATION.md (deferred)

    **Residual risk acceptance table:**
    | Risk | Severity | Owner | Acceptance rationale | Re-review trigger |
    | T-200.1-webhooks-I-03 log redaction medium | medium | platform | log-drain explicit allow-list audited 2026-04-27 | New log statement added to webhooks/* lib code |
    | T-200.1-webhooks-D-02 test-fire amplification | medium | platform | rate-limit on test-fire enforced; cost meter post-D-207 surfaces abuse | OTEL alarm on test-fire RPM spike |
    | T-200.1-webhooks-E-03 test-fire injection | medium | platform | M8 deferred to follow-up; receiver contract documents non-authoritative | Receiver complaints OR follow-up phase opens |
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('.planning/phases/200.1-saas-readiness-hardening/threat-models/webhooks-stride.md','utf8'); const checks={'spoofing': /## Spoofing|### Spoofing/.test(s), 'tampering': /## Tampering|### Tampering/.test(s), 'repudiation': /## Repudiation|### Repudiation/.test(s), 'info_disclosure': /## Information Disclosure|### Information Disclosure/.test(s), 'dos': /## Denial of Service|### Denial of Service/.test(s), 'eop': /## Elevation of Privilege|### Elevation of Privilege/.test(s), 'min_threats': (s.match(/T-200\.1-webhooks-/g)||[]).length>=18, 'min_lines': s.split('\n').length>=120, 'plan_xref': s.includes('200.1-01')&&s.includes('200.1-05')&&s.includes('200.1-06')}; const failed=Object.entries(checks).filter(([k,v])=>!v); if(failed.length){console.error('Failed:',failed); process.exit(1)} else console.log('webhooks-stride OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/200.1-saas-readiness-hardening/threat-models/webhooks-stride.md` exists.
    - All 6 STRIDE category headings present.
    - At least 18 distinct `T-200.1-webhooks-` IDs.
    - At least one mitigation row references each of: `200.1-01`, `200.1-05`, `200.1-06`.
    - File ≥120 lines.
  </acceptance_criteria>
  <done>
    Webhooks STRIDE landed; ≥18 threats catalogued; cross-references to mitigation plans complete.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author mcp-stride.md (depends on 200.1-07 + 200.1-08 mitigation references)</name>
  <files>.planning/phases/200.1-saas-readiness-hardening/threat-models/mcp-stride.md</files>
  <read_first>
    - .planning/phases/200.1-saas-readiness-hardening/200.1-CONTEXT.md (D-204, D-207, D-209 mitigation specs)
    - .planning/phases/200.1-saas-readiness-hardening/200.1-REVIEWS.md (H4, H5, M4 — translate review findings into threat rows)
    - lib/markos/mcp/server.ts + lib/markos/mcp/server.cjs
    - api/mcp/session.js + api/mcp/tools/[toolName].js
    - lib/markos/mcp/tools/index.cjs (10 tool surface)
    - lib/markos/mcp/cost-meter.cjs (existing surface from Phase 202)
    - lib/markos/mcp/oauth.cjs (existing OAuth surface — D-204 layers per-tenant API-key bearer in addition to existing OAuth)
  </read_first>
  <action>
    Author `mcp-stride.md` (≥120 lines, same structure as Task 1).

    Header block: Domain scope = `api/mcp/*` (session + tools/[toolName]) + `lib/markos/mcp/*` (server + 10 tool adapters) + `markos_mcp_api_keys` (200.1-07) + `markos_mcp_cost_events` (200.1-07) + `markos_tenant_billing_holds` (kill-switch surface).

    Trust boundaries: Claude Desktop → MCP session, tool invocation → LLM via AI Gateway, MCP tool → tenant data plane (CRM/audit/etc), session SSE → cost meter event sink, kill-switch → tool middleware.

    18+ threats. Spotlight rows:

    Spoofing:
    - T-200.1-mcp-S-01: Anonymous client claiming a tenant_id → mitigation: 200.1-07 per-tenant API-key bearer (`mks_<32b-base32>`); sha256-hashed in `markos_mcp_api_keys`; middleware rejects on missing/invalid → residual: low → verifier: `test/mcp/auth-bearer.test.js`
    - T-200.1-mcp-S-02: Forged Authorization header (timing attack on hash compare) → mitigation: constant-time compare via `crypto.timingSafeEqual` on the hashed input → residual: low → verifier: `grep -n "timingSafeEqual" lib/markos/mcp/auth-bearer.cjs`
    - T-200.1-mcp-S-03: Replay of revoked key → mitigation: middleware re-reads `revoked_at` per request (5-min cache OK; documented decision) → residual: low → verifier: `lib/markos/mcp/auth-bearer.cjs` revoked check

    Tampering:
    - T-200.1-mcp-T-01: Tool input mutation between SSE and worker → mitigation: zod validation at `api/mcp/tools/[toolName].js` entry; canonical request envelope → residual: low
    - T-200.1-mcp-T-02: Cost-meter row tampering → mitigation: append-only table, RLS membership policy, service-role-only mutation → residual: low
    - T-200.1-mcp-T-03: Kill-switch bypass via direct tool invocation → mitigation: middleware enforces `markos_tenant_billing_holds.exceeded_at IS NULL` BEFORE tool dispatch → residual: low

    Repudiation:
    - T-200.1-mcp-R-01: Tenant denies invoking a billed tool → mitigation: audit row per tool invocation (`source_domain='mcp' action='tool.invoked' tool_name=...`) emitted inline (D-101 pattern) → residual: low
    - T-200.1-mcp-R-02: Tenant denies API-key minting → mitigation: audit row at key issue (`action='api_key.created'`) → residual: low
    - T-200.1-mcp-R-03: Operator denies kill-switch trigger → mitigation: audit row when middleware returns 402 (`action='mcp.kill_switch_triggered'`) → residual: low

    Information Disclosure:
    - T-200.1-mcp-I-01: Cross-tenant tool result leak → mitigation: every tool reads tenant_id from validated bearer; RLS on every downstream table → residual: low
    - T-200.1-mcp-I-02: API-key plaintext on disk → mitigation: only sha256 hash stored; raw key returned ONCE at creation → residual: low → verifier: `grep -n "key_hash" supabase/migrations/71.1_markos_mcp_api_keys.sql`
    - T-200.1-mcp-I-03: SSE response containing other tenants' data → mitigation: SSE channel scoped per `mcp_session_id` which is bound to one tenant_id at issue → residual: low

    Denial of Service:
    - T-200.1-mcp-D-01: Open-relay LLM cost vector → mitigation: 200.1-07 per-tenant rate-limit 100 req/min + cost-meter + kill-switch → residual: low → verifier: `node --test test/mcp/rate-limit.test.js`
    - T-200.1-mcp-D-02: Long-running tool pinning Fluid Compute instance → mitigation: tool timeout 60s; SSE heartbeat 15s; instance reuse safe → residual: medium → verifier: documented in 200-VERIFICATION.md (L1 deferred)
    - T-200.1-mcp-D-03: Cost amplification via prompt injection inside tool inputs → mitigation: existing `lib/markos/mcp/injection-denylist.cjs` + 200.1-09 eval-as-test catches prompt-inj corpus → residual: medium → verifier: `lib/markos/evals/mcp/draft_message.eval.js` includes prompt-inj fixtures

    Elevation of Privilege:
    - T-200.1-mcp-E-01: API key with global tool access used cross-tenant → mitigation: `scopes` column on `markos_mcp_api_keys`; middleware enforces per-tool scope match → residual: low
    - T-200.1-mcp-E-02: Demo token used to access prod tools → mitigation: 200.1-10 demo session uses separate token type (`demo_session_token` not `mks_*`) and restricted tool subset → residual: low
    - T-200.1-mcp-E-03: Bearer-key admin escape (no auth on key-mint endpoint) → mitigation: key-mint requires existing `requireHostedSupabaseAuth` session AND tenant-admin role → residual: low

    Cross-references: include `200.1-07`, `200.1-09`, `200.1-10`, `200.1-08` (OTEL).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('.planning/phases/200.1-saas-readiness-hardening/threat-models/mcp-stride.md','utf8'); const checks={'spoofing': /## Spoofing|### Spoofing/.test(s), 'tampering': /## Tampering|### Tampering/.test(s), 'repudiation': /## Repudiation|### Repudiation/.test(s), 'info_disclosure': /## Information Disclosure|### Information Disclosure/.test(s), 'dos': /## Denial of Service|### Denial of Service/.test(s), 'eop': /## Elevation of Privilege|### Elevation of Privilege/.test(s), 'min_threats': (s.match(/T-200\.1-mcp-/g)||[]).length>=18, 'min_lines': s.split('\n').length>=120, 'plan_xref': s.includes('200.1-07')&&s.includes('200.1-09')&&s.includes('200.1-10')}; const failed=Object.entries(checks).filter(([k,v])=>!v); if(failed.length){console.error('Failed:',failed); process.exit(1)} else console.log('mcp-stride OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/200.1-saas-readiness-hardening/threat-models/mcp-stride.md` exists.
    - All 6 STRIDE category headings present.
    - At least 18 `T-200.1-mcp-` IDs.
    - File contains references to `200.1-07`, `200.1-09`, `200.1-10`.
    - File ≥120 lines.
  </acceptance_criteria>
  <done>
    MCP STRIDE landed; covers auth + cost + kill-switch + injection threats.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author marketplace-stride.md (covers .claude-plugin/marketplace.json + landing + demo sandbox)</name>
  <files>.planning/phases/200.1-saas-readiness-hardening/threat-models/marketplace-stride.md</files>
  <read_first>
    - .planning/phases/200.1-saas-readiness-hardening/200.1-CONTEXT.md (D-205 demo sandbox + L2 marketplace.json data exposure)
    - .claude-plugin/marketplace.json (Phase 200-06 manifest)
    - app/(marketing)/integrations/claude/page.tsx (Phase 200-08 landing)
    - app/(marketing)/integrations/claude/demo/page.tsx + api/route.ts (demo sandbox surface)
    - lib/markos/auth/botid.cjs (Phase 201 BotID wrapper — demo gate per D-205)
  </read_first>
  <action>
    Author `marketplace-stride.md` (≥100 lines, same structure as Task 1+2).

    Header: Domain scope = `.claude-plugin/marketplace.json` + `app/(marketing)/integrations/claude/page.tsx` (landing) + `app/(marketing)/integrations/claude/demo/{page.tsx,api/route.ts}` (sandbox) + 200.1-10 `demo_session_token` issuer.

    Trust boundaries: anonymous web visitor → demo sandbox, demo sandbox → MCP server (proxied), marketplace.json → Claude Marketplace platform, marketplace listing → MarkOS landing CTA.

    18+ threats. Highlights:

    Spoofing:
    - T-200.1-marketplace-S-01: Marketplace listing impersonation (someone clones manifest with our brand) → mitigation: marketplace.json submitted only via Anthropic-verified org account; brand registered → residual: medium (external Anthropic-policy dependency) → verifier: documented in 200-VERIFICATION.md L2 audit
    - T-200.1-marketplace-S-02: Demo token forgery → mitigation: 200.1-10 JWT signed with server-only secret; payload includes `aud='demo-sandbox'` claim → residual: low
    - T-200.1-marketplace-S-03: BotID bypass via stolen token → mitigation: 200.1-10 `lib/markos/auth/botid.cjs` fail-closed verifies BotID token at issue, NOT just at request time; expired BotID → 401 → residual: low

    Tampering:
    - T-200.1-marketplace-T-01: marketplace.json mutated post-publish → mitigation: Anthropic platform-side checksum + manifest-signature → residual: low
    - T-200.1-marketplace-T-02: Demo cost-cap bypass via client-side token edit → mitigation: cost-cap enforced server-side in token-validation middleware; client-edited tokens fail signature check → residual: low
    - T-200.1-marketplace-T-03: Landing page brand voice tampering → mitigation: Phase 200-08 voice classifier + Canon pipeline + git review → residual: low

    Repudiation:
    - T-200.1-marketplace-R-01: Demo abuser denies their session → mitigation: BotID token + IP + user-agent in `markos_audit_log` (`source_domain='marketing' action='demo.session_started'`) → residual: low
    - T-200.1-marketplace-R-02: Demo cost-cap event repudiation → mitigation: audit row when 402 returned → residual: low
    - T-200.1-marketplace-R-03: Marketplace install repudiation → mitigation: Phase 202 install-tracking pixel + audit row → residual: low

    Information Disclosure:
    - T-200.1-marketplace-I-01: marketplace.json contains staging URLs / internal hosts → mitigation: 200.1-09 (D-211 verifier) audits marketplace.json fields against allow-list of public hosts → residual: low → verifier: documented L2 check in 200-VERIFICATION.md
    - T-200.1-marketplace-I-02: Demo response leaks tenant data → mitigation: 200.1-10 demo session has NO tenant_id (synthetic sandbox tenant); restricted tools `[draft_message, audit_claim]` (read/propose-only) NEVER touch real tenant data → residual: low
    - T-200.1-marketplace-I-03: Robots.txt / `llms.txt` exposes draft routes → mitigation: 200-VERIFICATION.md L5 audit (deferred but documented) → residual: medium

    Denial of Service:
    - T-200.1-marketplace-D-01: LLM-cost DoS via residential proxies → mitigation: 200.1-10 BotID gate (botid.cjs fail-closed) + per-token total-cost cap $0.50 + 15-min TTL → residual: low → verifier: `test/marketing/demo-sandbox-abuse.test.js`
    - T-200.1-marketplace-D-02: Tool subset abuse (high-cost tool exposed) → mitigation: 200.1-10 demo restricted to `[draft_message, audit_claim]` only (matches Decision 9 tier — read/propose-only) → residual: low
    - T-200.1-marketplace-D-03: Demo spam to drive support load → mitigation: per-IP rate-limit + BotID gate combine to filter automated abuse → residual: low

    Elevation of Privilege:
    - T-200.1-marketplace-E-01: Demo token reused as production MCP token → mitigation: 200.1-10 demo token type ≠ `mks_*` bearer; MCP middleware rejects unknown token types → residual: low
    - T-200.1-marketplace-E-02: Demo token grants `schedule_post` access → mitigation: 200.1-10 token issuer hard-codes restricted scope; UI disables disallowed tool buttons → residual: low
    - T-200.1-marketplace-E-03: Marketplace install grants tenant-admin role → mitigation: install does NOT auto-create tenant; user follows separate signup flow under Phase 201 → residual: low
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('.planning/phases/200.1-saas-readiness-hardening/threat-models/marketplace-stride.md','utf8'); const checks={'spoofing': /## Spoofing|### Spoofing/.test(s), 'tampering': /## Tampering|### Tampering/.test(s), 'repudiation': /## Repudiation|### Repudiation/.test(s), 'info_disclosure': /## Information Disclosure|### Information Disclosure/.test(s), 'dos': /## Denial of Service|### Denial of Service/.test(s), 'eop': /## Elevation of Privilege|### Elevation of Privilege/.test(s), 'min_threats': (s.match(/T-200\.1-marketplace-/g)||[]).length>=18, 'min_lines': s.split('\n').length>=100, 'plan_xref': s.includes('200.1-10')}; const failed=Object.entries(checks).filter(([k,v])=>!v); if(failed.length){console.error('Failed:',failed); process.exit(1)} else console.log('marketplace-stride OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/200.1-saas-readiness-hardening/threat-models/marketplace-stride.md` exists.
    - All 6 STRIDE category headings present.
    - At least 18 `T-200.1-marketplace-` IDs.
    - File contains reference to `200.1-10` (demo sandbox plan).
    - File ≥100 lines.
  </acceptance_criteria>
  <done>
    Marketplace STRIDE landed; demo sandbox abuse vectors enumerated.
  </done>
</task>

<task type="auto">
  <name>Task 4: Append addendum to Phase 200 DISCUSS.md linking to all 3 STRIDE files</name>
  <files>.planning/phases/200-saas-readiness-wave-0/DISCUSS.md</files>
  <read_first>
    - .planning/phases/200-saas-readiness-wave-0/DISCUSS.md (existing content — DO NOT REWRITE; only append addendum)
    - The 3 STRIDE files just authored
  </read_first>
  <action>
    Append (not rewrite) to the END of `.planning/phases/200-saas-readiness-wave-0/DISCUSS.md`:

    ```markdown

    ---

    ## Threat models (added by Phase 200.1)

    > Added 2026-04-27 by Phase 200.1 plan 04 to close gate-11 ("threat model per new domain") for Phase 200's MCP, webhooks, and marketplace surfaces. The original 5-row Risks table above remains as initial scoping; the STRIDE artifacts below are the formal models gate-11 requires.

    Three STRIDE threat models cover the new domains Phase 200 introduced:

    - **MCP server** — `../200.1-saas-readiness-hardening/threat-models/mcp-stride.md` (≥18 threats covering session auth, cost meter, kill-switch, prompt-injection)
    - **Webhook engine** — `../200.1-saas-readiness-hardening/threat-models/webhooks-stride.md` (≥18 threats covering SSRF, replay, secret leak, redirect chains)
    - **Marketplace + demo sandbox** — `../200.1-saas-readiness-hardening/threat-models/marketplace-stride.md` (≥18 threats covering listing impersonation, demo cost DoS, token forgery)

    Each model maps threats → mitigation plans (cross-referencing 200.1-01 through 200.1-10) → residual risk → verifier (test command, file path, or audit query). Residual risks accepted by operator are recorded in each artifact's "Residual risk acceptance" table. Cross-reference integrity is enforced programmatically by `test/threat-models/stride-references.test.js`.

    Originally surfaced by review concern **H6** in `200-REVIEWS.md` and `200.1-REVIEWS.md`.
    ```

    Critically: do NOT modify the existing DISCUSS.md content above this addendum — it is the historical Phase 200 record and downstream phases reference it as-is.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('.planning/phases/200-saas-readiness-wave-0/DISCUSS.md','utf8'); const checks={'has_addendum_heading': s.includes('## Threat models (added by Phase 200.1)'), 'has_mcp_link': s.includes('mcp-stride.md'), 'has_webhooks_link': s.includes('webhooks-stride.md'), 'has_marketplace_link': s.includes('marketplace-stride.md'), 'has_h6_ref': s.includes('H6'), 'preserves_existing': s.length > 5000}; const failed=Object.entries(checks).filter(([k,v])=>!v); if(failed.length){console.error('Failed:',failed); process.exit(1)} else console.log('addendum OK, file length',s.length)"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/200-saas-readiness-wave-0/DISCUSS.md` contains the literal heading `## Threat models (added by Phase 200.1)`.
    - File contains markdown links to all 3 STRIDE files (paths: `../200.1-saas-readiness-hardening/threat-models/{mcp,webhooks,marketplace}-stride.md`).
    - File length ≥5000 chars (existing content preserved — this is a sanity check that the existing DISCUSS.md content is intact).
    - `git diff .planning/phases/200-saas-readiness-wave-0/DISCUSS.md` shows the addendum block APPENDED at the end, not interspersed with existing content.
  </acceptance_criteria>
  <done>
    Phase 200 DISCUSS.md now navigates to the STRIDE artifacts; gate-11 closure path is discoverable from Phase 200 entry.
  </done>
</task>

<task type="auto">
  <name>Task 5: Author test/threat-models/stride-references.test.js — programmatic cross-reference validator (BLOCKER #3 fix)</name>
  <files>test/threat-models/stride-references.test.js</files>
  <read_first>
    - .planning/phases/200.1-saas-readiness-hardening/threat-models/mcp-stride.md (Task 2 output — Verifier column shape)
    - .planning/phases/200.1-saas-readiness-hardening/threat-models/webhooks-stride.md (Task 1 output)
    - .planning/phases/200.1-saas-readiness-hardening/threat-models/marketplace-stride.md (Task 3 output)
    - .planning/phases/200.1-saas-readiness-hardening/ (list .planning/phases/*/ to enumerate existing PLAN files for plan-ID resolution)
  </read_first>
  <behavior>
    The test parses each STRIDE markdown's threat-register tables, extracts everything in the "Verifier" column, then asserts:

    1. **File-path references resolve.** For each match of regex `\b(test|lib|api|scripts|supabase)/[\S]+\.(test\.)?(js|cjs|ts|sql|md)\b`:
       - If the path is in this phase's own `files_modified` (Plan 200.1-01..11): MUST exist on disk → test fails if missing.
       - If the path is in a deferred-phase scope (e.g. references documented L1-L5 / M2-M8 deferrals): skip-and-warn → test logs but doesn't fail.
       - Default: existence required.

    2. **Plan-ID references resolve.** For each match of regex `\b200\.1-(0[1-9]|10|11)\b`:
       - Glob `.planning/phases/200.1-saas-readiness-hardening/200.1-XX-*-PLAN.md` for the matching plan number.
       - At least one match required → test fails if no PLAN.md found.

    3. **Deferred-tag escape hatch.** A row whose Verifier cell contains the literal `(deferred)` is allowed to reference paths that don't yet exist (the escape hatch covers references to artifacts owned by future phases).

    4. **Per-row diagnostic on failure.** Each failed reference produces an output line: `<file>:<line> :: row T-200.1-XX-YY-NN :: unresolved reference: <ref>`.

    The test exits 0 only when EVERY non-deferred reference resolves.
  </behavior>
  <action>
    Implement `test/threat-models/stride-references.test.js`:

    ```javascript
    'use strict';
    // Phase 200.1 BLOCKER #3 fix: STRIDE cross-reference integrity validator.
    // Parses Verifier column of each STRIDE markdown; asserts every file-path
    // and plan-ID reference resolves on disk. Skips rows tagged (deferred).

    const fs = require('node:fs');
    const path = require('node:path');
    const test = require('node:test');
    const assert = require('node:assert/strict');

    const REPO_ROOT = process.cwd();
    const STRIDE_DIR = path.join(REPO_ROOT, '.planning/phases/200.1-saas-readiness-hardening/threat-models');
    const STRIDE_FILES = ['mcp-stride.md', 'webhooks-stride.md', 'marketplace-stride.md'];

    const FILE_REF_RE = /\b(test|lib|api|scripts|supabase)\/[\S]+?\.(?:test\.)?(?:js|cjs|ts|sql|md)\b/g;
    const PLAN_ID_RE = /\b200\.1-(0[1-9]|10|11)\b/g;
    const THREAT_ID_RE = /T-200\.1-(?:mcp|webhooks|marketplace)-(?:S|T|R|I|D|E)-\d{2}/;
    const DEFERRED_TAG_RE = /\(deferred\)/i;

    function planIdResolves(planNum) {
      const phaseDir = path.join(REPO_ROOT, '.planning/phases/200.1-saas-readiness-hardening');
      try {
        const files = fs.readdirSync(phaseDir);
        // Match files like `200.1-01-PLAN.md`, `200.1-10-PLAN.md`.
        const re = new RegExp('^200\\.1-' + planNum + '(?:-[\\w-]+)?-PLAN\\.md$');
        return files.some(f => re.test(f) || f.startsWith('200.1-' + planNum + '-') && f.endsWith('-PLAN.md'));
      } catch {
        return false;
      }
    }

    function parseTable(md) {
      // Extract pipe-style table rows that contain a T-200.1-... threat ID.
      const lines = md.split('\n');
      const rows = [];
      for (let i = 0; i < lines.length; i++) {
        const line = lines[i];
        if (!THREAT_ID_RE.test(line)) continue;
        // Skip header separator rows (---).
        if (/^\|\s*-+/.test(line)) continue;
        rows.push({ lineNo: i + 1, raw: line });
      }
      return rows;
    }

    function extractVerifierCell(rowRaw) {
      // Pipe-table row: | Threat ID | Threat | Mitigation | Residual risk | Verifier |
      // Verifier is the last non-empty pipe-segment.
      const parts = rowRaw.split('|').map(s => s.trim()).filter(Boolean);
      if (parts.length < 5) return rowRaw; // fall back to whole row
      return parts[parts.length - 1];
    }

    for (const fname of STRIDE_FILES) {
      const fpath = path.join(STRIDE_DIR, fname);

      test(`stride-references :: ${fname} :: file references resolve`, () => {
        if (!fs.existsSync(fpath)) {
          // STRIDE file itself missing — that's a different failure (covered by Tasks 1-3).
          // This test only runs once Tasks 1-3 ship; gate the assertion.
          throw new Error(`STRIDE file missing: ${fpath} — Tasks 1-3 must ship first`);
        }
        const md = fs.readFileSync(fpath, 'utf8');
        const rows = parseTable(md);
        const failures = [];

        for (const row of rows) {
          const verifierCell = extractVerifierCell(row.raw);
          if (DEFERRED_TAG_RE.test(verifierCell)) continue; // explicit (deferred) escape hatch

          // File-path references in the Verifier cell.
          const fileRefs = (verifierCell.match(FILE_REF_RE) || []);
          for (const ref of fileRefs) {
            const refPath = path.join(REPO_ROOT, ref);
            if (!fs.existsSync(refPath)) {
              failures.push(`${fname}:${row.lineNo} :: unresolved file: ${ref}`);
            }
          }
        }

        if (failures.length) {
          assert.fail('Unresolved file references in ' + fname + ':\n  ' + failures.join('\n  '));
        }
      });

      test(`stride-references :: ${fname} :: plan-ID references resolve`, () => {
        if (!fs.existsSync(fpath)) throw new Error(`STRIDE file missing: ${fpath}`);
        const md = fs.readFileSync(fpath, 'utf8');
        const rows = parseTable(md);
        const failures = [];

        for (const row of rows) {
          const verifierCell = extractVerifierCell(row.raw);
          if (DEFERRED_TAG_RE.test(verifierCell)) continue;

          // Plan-ID references in either Verifier or Mitigation column — scan whole row.
          const planRefs = (row.raw.match(PLAN_ID_RE) || []);
          for (const m of planRefs) {
            const planNum = m.replace('200.1-', '');
            if (!planIdResolves(planNum)) {
              failures.push(`${fname}:${row.lineNo} :: unresolved plan ID: ${m}`);
            }
          }
        }

        if (failures.length) {
          assert.fail('Unresolved plan-ID references in ' + fname + ':\n  ' + failures.join('\n  '));
        }
      });
    }

    test('stride-references :: every required STRIDE file is present', () => {
      for (const f of STRIDE_FILES) {
        const p = path.join(STRIDE_DIR, f);
        assert.ok(fs.existsSync(p), `Missing STRIDE file: ${p}`);
      }
    });
    ```

    Notes for the executor:
    - This test is INTENTIONALLY written to be run AFTER Wave 2 ships — many of the file references it validates are produced by Plans 200.1-05..10. Plan 200.1-11 (verifier plan) invokes this test as part of its programmatic gate enforcement; that's how the cross-reference integrity flows into the final closeout.
    - Adding rows to the STRIDE markdown that reference (deferred) artifacts MUST tag the Verifier cell with the literal `(deferred)` string for the test to skip them. This is the documented escape hatch.
  </action>
  <verify>
    <automated>node --test test/threat-models/stride-references.test.js</automated>
  </verify>
  <acceptance_criteria>
    - File `test/threat-models/stride-references.test.js` exists.
    - File contains literal strings: `STRIDE_FILES`, `FILE_REF_RE`, `PLAN_ID_RE`, `(deferred)`, `parseTable`, `extractVerifierCell`, `planIdResolves`.
    - File runs at least 7 distinct `test(` blocks (3 file-ref tests + 3 plan-ID tests + 1 file-existence test).
    - When invoked AFTER Wave 2 ships, `node --test test/threat-models/stride-references.test.js` exits 0.
    - When invoked BEFORE Wave 2 ships, the test fails with explicit per-row diagnostics listing the unresolved references — proving the validator catches drift.
    - Plan 200.1-11's verifier (`scripts/verify/verify-phase-200.cjs`) MUST invoke this test as part of its gate-11 enforcement (cross-plan dependency: Plan 11 should add this test path to its check list).
  </acceptance_criteria>
  <done>
    STRIDE cross-reference integrity validator landed; rows referencing nonexistent artifacts fail loudly with per-row diagnostics; (deferred) escape hatch documented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner→threat-model author | Threat model itself is documentation; trust boundary is doc review (not code). |
| stride-references test→file system | Read-only; resolves Verifier-column references against repo state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-200.1-04-01 | Repudiation | threat-model accuracy | accept | This plan ships the threat models themselves; their correctness is a review-time artifact |
| T-200.1-04-02 | Information Disclosure | mitigation cross-references | mitigate | Every mitigation row points to a concrete plan_id, file path, or test — `test/threat-models/stride-references.test.js` (BLOCKER #3 fix) PROGRAMMATICALLY enforces that every link resolves |
| T-200.1-04-03 | Tampering | STRIDE row drift | mitigate | If a downstream phase renames or deletes a referenced file, stride-references test fails with per-row diagnostic on next CI run |
</threat_model>

<verification>
- 3 STRIDE files exist under `.planning/phases/200.1-saas-readiness-hardening/threat-models/`.
- DISCUSS.md addendum present.
- Each STRIDE file has 6 category headings + ≥18 distinct threat-IDs.
- `test/threat-models/stride-references.test.js` exists.
- `node --test test/threat-models/stride-references.test.js` exits 0 after Wave 2 ships (all referenced files + plan IDs resolve).
- All cross-references to plan IDs (200.1-01, 200.1-05, 200.1-06, 200.1-07, 200.1-09, 200.1-10) resolve to plan files (D-211 verifier confirms via the new stride-references test).
</verification>

<success_criteria>
- gate-11 evidence available for D-211 verifier on three new domains.
- Phase 200 DISCUSS.md surfaces threat models so future readers find them.
- Programmatic cross-reference validator catches future drift in STRIDE markdown.
- H6 closed.
</success_criteria>

<output>
After completion, create `.planning/phases/200.1-saas-readiness-hardening/200.1-04-SUMMARY.md` documenting: 3 STRIDE files authored, threat counts per category, DISCUSS.md addendum integration, stride-references validator design + escape hatch behavior, H6 closure evidence.
</output>
