---
phase: 200.1
plan: "03"
subsystem: onboarding-ci
completed: 2026-04-29
---

# Phase 200.1 Plan 03 Summary

Plan `200.1-03` added an explicit parity gate for the preset mirror trees: `bin/lib/presets/*.json` and `.agent/markos/templates/presets/*.json`. The new script compares filename sets and canonicalized JSON content, the new test suite covers pass/fail drift scenarios plus a real-shape mutation regression, and GitHub Actions now runs the gate whenever either preset tree changes.

## What changed

- Added `scripts/ci/check-preset-parity.cjs` with `--bin-dir`, `--agent-dir`, and `--strict`.
- Added `test/onboarding/preset-parity.test.js` with `spawnSync` coverage plus an in-process fallback for sandboxed environments where child-node spawning is blocked.
- Added `.github/workflows/preset-parity.yml`.

## Verification

- `node scripts/ci/check-preset-parity.cjs`
- `node --test --test-isolation=none test/onboarding/preset-parity.test.js`

## Outcome

Review concern `M9` is closed for the preset mirror contract. Drift is now CI-visible, and the test harness proves the script catches a single-byte mutation on a copied real production preset tree instead of only synthetic toy fixtures.
