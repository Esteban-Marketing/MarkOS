---
phase: 203
plan: 10
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05, 06, 07, 08]
files_modified:
  - api/public/webhooks/status.js
  - app/(markos)/status/webhooks/page.tsx
  - app/(markos)/status/webhooks/page.module.css
  - lib/markos/webhooks/log-drain.cjs
  - lib/markos/webhooks/log-drain.ts
  - lib/markos/webhooks/sentry.cjs
  - lib/markos/webhooks/sentry.ts
  - api/webhooks/queues/deliver.js
  - lib/markos/webhooks/delivery.cjs
  - lib/markos/webhooks/delivery.ts
  - contracts/F-99-webhook-status-v1.yaml
  - public/llms.txt
  - docs/webhooks.md
  - docs/webhooks/rotation.md
  - docs/webhooks/dlq.md
  - docs/webhooks/status.md
  - docs/llms/phase-203-webhooks.md
  - scripts/load/webhooks-smoke.mjs
  - test/webhooks/status-page.test.js
  - test/webhooks/observability.test.js
  - test/webhooks/ui-s3-a11y.test.js
  - test/webhooks/public-status.test.js
autonomous: true
requirements: [WHK-01, QA-06, QA-07, QA-08, QA-09, QA-15]

must_haves:
  truths:
    - "GET /api/public/webhooks/status is public (no auth), 60s cache (`Cache-Control: public, max-age=60, s-maxage=60`), returns { total_24h, success_rate, avg_latency_ms, dlq_count, last_updated }"
    - "Surface 3 /status/webhooks is standalone (NO workspace shell), matches invite/[token] layout posture, renders 4 hero numbers + status line"
    - "lib/markos/webhooks/log-drain.cjs copies 202-05 emitLogLine shape verbatim with domain='webhook' tag; fields: req_id, sub_id, delivery_id, event_type, delivery_attempt, duration_ms, status, error_code"
    - "lib/markos/webhooks/sentry.cjs copies 202-05 captureToolError pattern verbatim with domain='webhook' Sentry tag + triple-safety (env gate + lazy import + try/catch)"
    - "api/webhooks/queues/deliver.js (updated from Plan 203-01) + lib/markos/webhooks/delivery.cjs emit log lines on every outcome + captureToolError on throws"
    - "F-99 contract declares /api/public/webhooks/status GET + references Vercel/Stripe status-page lineage"
    - "scripts/load/webhooks-smoke.mjs copies 202-10 mcp-smoke.mjs shape; 60-concurrent test-fire x 60s; gates p95 ≤ 500ms (higher than MCP's 300ms because webhook delivery includes subscriber round-trip) + err_rate ≤ 1%; dry-run when MARKOS_WEBHOOK_SMOKE_BASE_URL unset"
    - "public/llms.txt gets `## Phase 203 — Webhooks` section with 5 doc links"
    - "4 docs pages + 1 llms-friendly overview shipped (webhooks.md + 3 subpages + llms/phase-203-webhooks.md)"
    - "contracts/openapi.json regenerated with F-99 merged; openapi-build.test.js green with 203-phase path count (10+ new paths total across Waves 2-5)"
  artifacts:
    - path: "api/public/webhooks/status.js"
      provides: "public status endpoint"
    - path: "app/(markos)/status/webhooks/page.tsx"
      provides: "Surface 3 public page"
    - path: "lib/markos/webhooks/log-drain.cjs"
      provides: "emitLogLine with domain='webhook'"
    - path: "lib/markos/webhooks/sentry.cjs"
      provides: "captureToolError (env-gated + lazy)"
    - path: "contracts/F-99-webhook-status-v1.yaml"
      provides: "status page contract"
    - path: "scripts/load/webhooks-smoke.mjs"
      provides: "QA-07 load harness"
    - path: "docs/webhooks.md"
      provides: "subscriber integration guide"
    - path: "docs/webhooks/rotation.md"
      provides: "rotation lifecycle doc"
    - path: "docs/webhooks/dlq.md"
      provides: "DLQ semantics for subscribers"
    - path: "docs/webhooks/status.md"
      provides: "public status page intro"
    - path: "docs/llms/phase-203-webhooks.md"
      provides: "LLM-friendly overview"
  key_links:
    - from: "lib/markos/webhooks/delivery.cjs"
      to: "lib/markos/webhooks/log-drain.cjs emitLogLine"
      via: "finally-block emit on every outcome"
      pattern: "emitLogLine"
    - from: "lib/markos/webhooks/delivery.cjs"
      to: "lib/markos/webhooks/sentry.cjs captureToolError"
      via: "catch-block capture on uncaught exceptions"
      pattern: "captureToolError"
    - from: "api/webhooks/queues/deliver.js"
      to: "log-drain + sentry"
      via: "safe-require swap (Plan 203-01 used () => {} fallbacks; Plan 203-10 provides real modules)"
      pattern: "require.*log-drain|require.*sentry"
---

<objective>
Ship Wave 5's **final layer**: Surface 3 (public status page) + Sentry + log-drain observability primitives + QA-07 load smoke harness + 5 documentation pages + llms.txt update + OpenAPI regen. **QA-06 (Playwright E2E)** and **QA-08 (LLM eval-as-test)** are acknowledged as phase-infra deferrals per 202-10 precedent — documented in deferred-items.md at this plan's close (no webhook LLM surfaces; Playwright gating deferred pending cross-phase testing infrastructure).

Four concerns, all bundled into one plan because they're all the "polish + externalize" layer:
1. **Public status page** (S3 + its API) — standalone page, no auth, 60s cache, same 4 hero numbers as S1.
2. **Observability** — log-drain + Sentry wiring (both copies of 202-05 shapes with webhook domain tag); wires in delivery.cjs finally-block + catch + in the Vercel Queues consumer.
3. **QA-07 load smoke** — mirror 202-10 `scripts/load/mcp-smoke.mjs`; target `/api/webhooks/test-fire`; gates p95 + error rate.
4. **Docs + llms.txt** — 5 docs pages + Phase 203 entry in llms.txt; final OpenAPI regen.

This is the "phase close" plan — after it ships, Phase 203 is ready for `/gsd-verify-work 203`.

Output: 1 public endpoint + 1 public surface page + log-drain + sentry libs + smoke script + F-99 contract + 5 docs + llms.txt update + OpenAPI regen + 4 test suites.
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/203-webhook-subscription-engine-ga/203-CONTEXT.md
@.planning/phases/203-webhook-subscription-engine-ga/203-UI-SPEC.md
@.planning/phases/203-webhook-subscription-engine-ga/203-RESEARCH.md
@.planning/phases/203-webhook-subscription-engine-ga/203-01-PLAN.md
@.planning/phases/203-webhook-subscription-engine-ga/203-09-PLAN.md
@.planning/phases/202-mcp-server-ga-claude-marketplace/202-10-SUMMARY.md
@lib/markos/mcp/log-drain.cjs
@lib/markos/mcp/sentry.cjs

<interfaces>
<!-- From lib/markos/mcp/log-drain.cjs (Plan 202-05) — MIRROR verbatim -->
```javascript
function emitLogLine({ domain, req_id, session_id, tenant_id, tool_id, duration_ms, status, cost_cents, error_code, timestamp }) {
  // Emits JSON.stringify to console.log with null coercion + D-30 enumerated shape
}
module.exports = { emitLogLine };
```

<!-- From lib/markos/mcp/sentry.cjs (Plan 202-05) — MIRROR verbatim -->
```javascript
let _sentryInst = null;
function captureToolError(err, context, deps = {}) {
  if (!process.env.SENTRY_DSN) return;
  try {
    const Sentry = deps.sentry || (_sentryInst || (_sentryInst = require('@sentry/nextjs')));
    try { Sentry.captureException(err, { tags: { domain: 'mcp', ... }, extra: context }); } catch {}
  } catch {}
}
function _internalResetForTests() { _sentryInst = null; }
module.exports = { captureToolError, setupSentryContext, _internalResetForTests };
```

<!-- From scripts/load/mcp-smoke.mjs (202-10) — MIRROR -->
k6-equivalent in pure Node; POST /api/mcp/session 60 concurrent for 60s; measure p95 latency + error rate; dry-run when MARKOS_MCP_BEARER unset.

<!-- From UI-SPEC Surface 3 locked copy -->
h1: "Webhook delivery status"
subheading: "Live metrics for the MarkOS webhook platform. Updated every 60 seconds."
hero labels: (mirror S1)
status-line variants: "All systems operational." / "Some deliveries are being retried." / "Elevated failure rate."
footer: "Subscriber? Learn how to configure webhooks." → /docs/webhooks

<!-- From public/llms.txt existing entries (Phase 201 + Phase 202) -->
Sections like `## Phase 201 — Tenancy` and `## Phase 202 — MCP Server GA` already exist; append Phase 203 section.

<!-- Cache header for /api/public/webhooks/status -->
Cache-Control: public, max-age=60, s-maxage=60, stale-while-revalidate=60
Access-Control-Allow-Origin: * (RESEARCH §Threat Patterns "Over-permissive CORS on /api/public/webhooks/status is accept — no sensitive data in aggregate")

<!-- D-30 log-drain field list for webhook domain -->
{ domain: 'webhook', timestamp, req_id, tenant_id, sub_id, delivery_id, event_type, delivery_attempt, duration_ms, status, error_code, cost_cents: null }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: log-drain + Sentry libs + delivery.cjs + queues/deliver.js observability wiring + observability suite</name>
  <files>lib/markos/webhooks/log-drain.cjs, lib/markos/webhooks/log-drain.ts, lib/markos/webhooks/sentry.cjs, lib/markos/webhooks/sentry.ts, lib/markos/webhooks/delivery.cjs, lib/markos/webhooks/delivery.ts, api/webhooks/queues/deliver.js, test/webhooks/observability.test.js</files>
  <read_first>
    - lib/markos/mcp/log-drain.cjs (MIRROR verbatim with domain='webhook')
    - lib/markos/mcp/sentry.cjs (MIRROR verbatim with domain='webhook')
    - api/webhooks/queues/deliver.js (Plan 203-01 has safe-require fallbacks for log-drain + sentry — replace with real imports here)
    - lib/markos/webhooks/delivery.cjs (wire emitLogLine in finally + captureToolError in catch)
    - test/mcp/observability.test.js (202-05 test shape — MIRROR)
    - .planning/phases/203-webhook-subscription-engine-ga/203-CONTEXT.md (Claude's Discretion — "Internal telemetry schema for Sentry — match 202-05 log-drain shape")
  </read_first>
  <behavior>
    - Test 1a: `emitLogLine({ domain: 'webhook', req_id, delivery_id, sub_id, status, duration_ms })` prints a JSON line with all D-30 enumerated fields; absent optional fields coerce to null.
    - Test 1b: emitLogLine missing required `domain` throws or logs an error (validation).
    - Test 1c: emitLogLine output is single-line JSON (newline-delimited safe for log-drain).
    - Test 1d: `captureToolError(err, { req_id, delivery_id })` with `SENTRY_DSN` unset → returns silently (no throw).
    - Test 1e: With DSN set + mock Sentry dep → `Sentry.captureException` called with `{ tags: { domain: 'webhook' }, extra: context }`.
    - Test 1f: captureToolError with mock Sentry that throws → does NOT propagate (triple-safety).
    - Test 1g: delivery.cjs finally block calls emitLogLine with status='delivered' | 'failed' | 'retrying' | 'breaker_open' | 'rate_limited' per outcome.
    - Test 1h: delivery.cjs catch block calls captureToolError on uncaught exception.
    - Test 1i: api/webhooks/queues/deliver.js (post-wiring) invokes real emitLogLine (not the safe-require stub from Plan 203-01).
    - Test 1j: Log line shape grep: `grep -c "domain: 'webhook'" lib/markos/webhooks/log-drain.cjs` ≥ 1.
  </action>
  <action>
    1. **Create `lib/markos/webhooks/log-drain.cjs`** — COPY `lib/markos/mcp/log-drain.cjs` verbatim + change the `domain` default / enforced value to `'webhook'`:
       - `'use strict';`
       - Export `emitLogLine({ domain, timestamp, req_id, tenant_id, sub_id, delivery_id, event_type, delivery_attempt, duration_ms, status, error_code, cost_cents })`:
         - Coerce undefined → null for every enumerated field.
         - Require `domain === 'webhook'` (throw if not `'webhook'` or `'mcp'` — allow either for shared usage).
         - `console.log(JSON.stringify({ timestamp: timestamp || new Date().toISOString(), domain, req_id: req_id || null, tenant_id: tenant_id || null, sub_id: sub_id || null, delivery_id: delivery_id || null, event_type: event_type || null, delivery_attempt: delivery_attempt ?? null, duration_ms: duration_ms ?? null, status: status || null, error_code: error_code || null, cost_cents: cost_cents ?? null }));`
       - `module.exports = { emitLogLine };`

    2. **Mirror to `lib/markos/webhooks/log-drain.ts`**.

    3. **Create `lib/markos/webhooks/sentry.cjs`** — COPY `lib/markos/mcp/sentry.cjs` verbatim + change `domain: 'mcp'` tag to `domain: 'webhook'`:
       - Triple-safety:
         - Env gate: early-return if `!process.env.SENTRY_DSN`.
         - Lazy import try/catch: require `@sentry/nextjs` inside try; bail silently on ImportError (CI without @sentry/nextjs should not crash).
         - captureException try/catch: wrap the actual call; silent on throw.
       - Export `captureToolError(err, context, deps = {})` — mirror 202-05 exactly.
       - Export `setupSentryContext(tenant_id)` — mirror 202-05.
       - Export `_internalResetForTests()` — mirror 202-05.
       - `module.exports = { captureToolError, setupSentryContext, _internalResetForTests };`

    4. **Mirror to `lib/markos/webhooks/sentry.ts`**.

    5. **Edit `lib/markos/webhooks/delivery.cjs`**:
       - Import `const { emitLogLine } = require('./log-drain.cjs');` + `const { captureToolError } = require('./sentry.cjs');`.
       - Wrap the main processDelivery body in try/catch/finally:
         - try: existing dispatch logic.
         - catch (err): `captureToolError(err, { req_id, delivery_id, sub_id });` → rethrow for queue retry.
         - finally: `emitLogLine({ domain: 'webhook', req_id, delivery_id, sub_id, tenant_id, event_type, delivery_attempt: attempt, duration_ms: Date.now() - started, status: result?.status || 'unknown', error_code: err?.code || null, tenant_id: sub?.tenant_id });`.

    6. **Mirror to `lib/markos/webhooks/delivery.ts`**.

    7. **Update `api/webhooks/queues/deliver.js`** (from Plan 203-01):
       - Replace safe-require `() => {}` fallbacks with real requires:
         - `const { emitLogLine } = require('../../../lib/markos/webhooks/log-drain.cjs');`
         - `const { captureToolError } = require('../../../lib/markos/webhooks/sentry.cjs');`
       - Consumer's own finally block already wires emitLogLine per Plan 203-01 spec; retain that shape (no change needed beyond swap of require).

    8. **Write `test/webhooks/observability.test.js`** — mirror `test/mcp/observability.test.js`:
       - D-30 shape (1a).
       - Missing domain (1b).
       - Single-line JSON (1c).
       - Sentry graceful-degrade when DSN unset (1d).
       - Mock Sentry called correctly (1e).
       - Triple-safety on Sentry throw (1f).
       - delivery.cjs emits log line on every outcome (1g).
       - delivery.cjs catch → captureToolError (1h).
       - queues/deliver.js uses real modules (1i).
       - grep assertion (1j).
  </action>
  <verify>
    <automated>node --test test/webhooks/observability.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/webhooks/observability.test.js` returns 0 (10 behaviors green).
    - `grep -c "domain: 'webhook'" lib/markos/webhooks/log-drain.cjs lib/markos/webhooks/sentry.cjs` ≥ 2.
    - `grep -c "emitLogLine" lib/markos/webhooks/delivery.cjs` ≥ 1 (finally block).
    - `grep -c "captureToolError" lib/markos/webhooks/delivery.cjs` ≥ 1 (catch block).
    - `grep -c "process.env.SENTRY_DSN" lib/markos/webhooks/sentry.cjs` ≥ 1 (env gate).
    - `grep -cE "require.*log-drain" api/webhooks/queues/deliver.js` ≥ 1.
    - `grep -cE "require.*sentry" api/webhooks/queues/deliver.js` ≥ 1.
    - Dual-export: `ls lib/markos/webhooks/log-drain.{cjs,ts} lib/markos/webhooks/sentry.{cjs,ts}` = 4.
    - Regression: `node --test test/webhooks/*.test.js` all green (Plan 203-01's safe-require stubs replaced with real modules without behavior change).
  </acceptance_criteria>
  <done>Log-drain + Sentry wired at delivery.cjs + queues/deliver.js with 202-05 shapes; domain='webhook' tag everywhere; 10 behaviors green; safe-require fallbacks from Plan 203-01 replaced with real modules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Public status endpoint + Surface 3 + F-99 contract + status-page tests</name>
  <files>api/public/webhooks/status.js, app/(markos)/status/webhooks/page.tsx, app/(markos)/status/webhooks/page.module.css, contracts/F-99-webhook-status-v1.yaml, test/webhooks/status-page.test.js, test/webhooks/public-status.test.js, test/webhooks/ui-s3-a11y.test.js</files>
  <read_first>
    - app/(markos)/invite/[token]/page.tsx + page.module.css (standalone-layout reference for Surface 3)
    - lib/markos/webhooks/metrics.cjs (Plan 203-09 — aggregateFleetMetrics; extend with tenant_id=null/global aggregation)
    - .planning/phases/203-webhook-subscription-engine-ga/203-UI-SPEC.md Surface 3 (complete spec)
    - contracts/F-95-mcp-cost-budget-v1.yaml (YAML shape for F-99)
    - app/(markos)/settings/webhooks/page.module.css (from Plan 203-09 — heroGrid/heroCard/heroNumber/heroLabel rules to duplicate per co-location rule)
  </read_first>
  <behavior>
    - Test 2a: GET /api/public/webhooks/status without any auth → 200 `{ total_24h, success_rate, avg_latency_ms, dlq_count, last_updated }`.
    - Test 2b: Response sets `Cache-Control: public, max-age=60` + `Access-Control-Allow-Origin: *`.
    - Test 2c: Aggregation is PLATFORM-wide (all tenants); no tenant_id exposed in response.
    - Test 2d: Surface 3 page.tsx contains locked copy: h1 "Webhook delivery status", subheading "Live metrics for the MarkOS webhook platform. Updated every 60 seconds.", 4 hero labels, last-updated text, 3 status-line variants ("All systems operational." / "Some deliveries are being retried." / "Elevated failure rate."), footer "Subscriber? Learn how to configure webhooks." + `/docs/webhooks` href.
    - Test 2e: Surface 3 a11y: `<main>`, `aria-labelledby="status-heading"`, `<time dateTime=`, `role="status"` + `aria-live="polite"` (status line).
    - Test 2f: Surface 3 CSS: standalone layout (no workspace-shell classes), `max-width: 720px` (inner card), `border-radius: 28px` (hero card), hero tokens (#0d9488, #0f766e, #9a3412 for DLQ-alert, #fef3c7/#78350f for warn status line, #fef2f2/#991b1b for error status line).
    - Test 2g: F-99 YAML parses; declares `/api/public/webhooks/status` GET + 60s cache + no auth requirement.
  </behavior>
  <action>
    1. **Create `api/public/webhooks/status.js`**:
       - `'use strict';`
       - `const { aggregateFleetMetrics } = require('../../../lib/markos/webhooks/metrics.cjs');`
       - Handler:
         - Method gate: GET only (405 else).
         - NO header auth (public endpoint).
         - Set `Cache-Control: public, max-age=60, s-maxage=60, stale-while-revalidate=60`.
         - Set `Access-Control-Allow-Origin: *` (explicit CORS allow — RESEARCH §Threat Patterns accept disposition).
         - Construct admin Supabase client via service-role.
         - Aggregate platform-wide: `aggregateFleetMetrics(supabase, null)` — extend aggregateFleetMetrics to skip tenant_id filter when passed null. Alternative: query the view with no tenant filter.
         - Return `{ total_24h, success_rate, avg_latency_ms, dlq_count, last_updated: now ISO }`.
         - Wrap in try/catch; 500 `{ error: 'status_unavailable' }` on error + emitLogLine with status='error'.

    2. **Create `app/(markos)/status/webhooks/page.tsx`** (Surface 3 standalone):
       - `'use client';`
       - Mirror structure from `app/(markos)/invite/[token]/page.tsx` (centered card, no shell).
       - `useState` for `status`, `loading`, `lastUpdated`.
       - `useEffect` on mount + `setInterval(60_000)` fetching `/api/public/webhooks/status`.
       - Render: `<main><section aria-labelledby="status-heading" className={styles.contentCard}>` with:
         - `<h1 id="status-heading" className={styles.heading}>Webhook delivery status</h1>`
         - Subheading
         - Hero grid (4 cards — reuse `.heroGrid`, `.heroCard`, `.heroNumber`, `.heroLabel` rules co-located in this page.module.css per UI-SPEC §Component Reuse Map).
         - Last-updated `<time dateTime={lastUpdated}>Last updated {relative}</time>`
         - Status line `<p className={styles.statusLine} role="status" aria-live="polite">`: conditional copy per:
           - success_rate >= 99.9 AND dlq_count === 0 → "All systems operational."
           - success_rate >= 95.0 → "Some deliveries are being retried."
           - else → "Elevated failure rate."
         - Footer link: `<a href="/docs/webhooks" className={styles.footerLink}>Subscriber? Learn how to configure webhooks.</a>`
       - Tiny file — no tabs, no dialogs, no mutations.

    3. **Create `app/(markos)/status/webhooks/page.module.css`**:
       - Copy-rename from `app/(markos)/invite/[token]/page.module.css` base (`.page`, `.contentCard`, `.heading`, `.subheading`).
       - Duplicate from Plan 203-09 S1 page.module.css: `.heroGrid`, `.heroCard`, `.heroNumber`, `.heroLabel` (UI-SPEC explicit: "duplicate, do not cross-import" — co-location rule).
       - Add `.statusLine`: font-size 16px, font-weight 600, color #0f172a. Add `.statusDot`: 10px round dot preceding text.
       - Add data-variant states: `[data-status="operational"]` (`.statusDot` bg `#0d9488`); `[data-status="retrying"]` (`#d97706`); `[data-status="elevated"]` (`#dc2626`).
       - Add `.footerLink`: 14px 600 #0d9488 underlined.
       - Page wrapper: `min-height: 100vh; padding: 48px 24px; display: flex; justify-content: center; align-items: flex-start;` inner card `max-width: 720px`.
       - Responsive: `@media (max-width: 640px) { padding: 24px 16px; }`.
       - `@media (prefers-reduced-motion: reduce)` disables transitions.

    4. **Create `contracts/F-99-webhook-status-v1.yaml`**:
       - flow_id: F-99, title: Public Webhook Status, version: v1.
       - paths: single item `/api/public/webhooks/status` GET → 200 schema + 500 error.
       - description: "Public, unauthenticated. 60s cache. Platform-wide aggregates (no tenant_id exposed)."
       - references: Stripe/Vercel status-page lineage, UI-SPEC §Surface 3.

    5. **Regenerate OpenAPI**: `node scripts/openapi/build-openapi.cjs` (merges F-99).

    6. **Write 3 test suites**:
       - `test/webhooks/public-status.test.js`: covers 2a-2c (handler behavior) + 2g (F-99 YAML shape).
       - `test/webhooks/status-page.test.js`: covers 2d (page.tsx copy greps) + 2e (a11y markers).
       - `test/webhooks/ui-s3-a11y.test.js`: covers 2f (CSS token assertions + standalone layout + hero duplication).
  </action>
  <verify>
    <automated>node --test test/webhooks/public-status.test.js test/webhooks/status-page.test.js test/webhooks/ui-s3-a11y.test.js test/openapi/openapi-build.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/webhooks/public-status.test.js test/webhooks/status-page.test.js test/webhooks/ui-s3-a11y.test.js test/openapi/openapi-build.test.js` returns 0.
    - `grep -c "Webhook delivery status" app/(markos)/status/webhooks/page.tsx` ≥ 1.
    - `grep -c "Live metrics for the MarkOS webhook platform." app/(markos)/status/webhooks/page.tsx` ≥ 1.
    - `grep -c "All systems operational." app/(markos)/status/webhooks/page.tsx` ≥ 1.
    - `grep -c "Elevated failure rate." app/(markos)/status/webhooks/page.tsx` ≥ 1.
    - `grep -c "Subscriber? Learn how to configure webhooks." app/(markos)/status/webhooks/page.tsx` ≥ 1.
    - `grep -c "<main>" app/(markos)/status/webhooks/page.tsx` ≥ 1.
    - `grep -c "aria-labelledby=\"status-heading\"" app/(markos)/status/webhooks/page.tsx` ≥ 1.
    - `grep -c "<time dateTime=" app/(markos)/status/webhooks/page.tsx` ≥ 1.
    - `grep -c "max-width: 720px" app/(markos)/status/webhooks/page.module.css` ≥ 1.
    - `grep -c "Cache-Control" api/public/webhooks/status.js` ≥ 1 (60s cache).
    - `grep -c "Access-Control-Allow-Origin" api/public/webhooks/status.js` ≥ 1.
    - `grep -c "F-99" contracts/F-99-webhook-status-v1.yaml` ≥ 1.
    - `grep -c "/api/public/webhooks/status" contracts/F-99-webhook-status-v1.yaml` ≥ 1.
  </acceptance_criteria>
  <done>Surface 3 standalone page + public endpoint ship with 60s cache + no-auth + all UI-SPEC locked copy + CSS tokens; F-99 contract declares path; openapi regen clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Load smoke + 5 docs + llms.txt update + final OpenAPI regen + phase observability/smoke tests</name>
  <files>scripts/load/webhooks-smoke.mjs, docs/webhooks.md, docs/webhooks/rotation.md, docs/webhooks/dlq.md, docs/webhooks/status.md, docs/llms/phase-203-webhooks.md, public/llms.txt</files>
  <read_first>
    - scripts/load/mcp-smoke.mjs (202-10 — MIRROR the 60-concurrent × 60s harness; adjust target + p95 threshold)
    - public/llms.txt (existing Phase 201 + 202 sections — append 203 after)
    - docs/mcp-tools.md + docs/oauth.md (202-10 — doc tone reference)
    - .planning/phases/202-mcp-server-ga-claude-marketplace/202-10-SUMMARY.md (docs shipping pattern)
    - .planning/phases/203-webhook-subscription-engine-ga/203-RESEARCH.md §Documentation Plan + §Pattern 3 subscriber verify snippet (copy into docs/webhooks.md)
  </read_first>
  <behavior>
    - Test 3a: `scripts/load/webhooks-smoke.mjs` exists; contains `60` (concurrency), `60000` (ms duration), `500` (p95 ms threshold for webhook — higher than MCP's 300ms because includes subscriber RTT), `0.01` (error rate threshold).
    - Test 3b: Smoke script falls through to dry-run when `MARKOS_WEBHOOK_SMOKE_BASE_URL` env unset (logs "[dry-run] webhook smoke skipped" and exits 0).
    - Test 3c: `docs/webhooks.md` exists + contains code snippets for Node.js + Python verification + discussion of dual-sig (x-markos-signature-v1 + v2).
    - Test 3d: `docs/webhooks/rotation.md` exists + describes 30-day grace + T-7/T-1/T-0 + rollback window + post-grace purge.
    - Test 3e: `docs/webhooks/dlq.md` exists + describes 24-attempt cap + 7-day TTL + x-markos-replayed-from + x-markos-attempt headers.
    - Test 3f: `docs/webhooks/status.md` exists + describes /status/webhooks page + computation.
    - Test 3g: `docs/llms/phase-203-webhooks.md` exists + ≥ 5 wiki-style links to the 4 docs + SUMMARYs.
    - Test 3h: `public/llms.txt` contains `## Phase 203 — Webhooks` section with at least 5 link entries.
    - Test 3i: `contracts/openapi.json` regenerated with all Phase-203 F-NN contracts merged: F-72 extended, F-73 extended, F-96 (5 paths), F-97 (3 paths), F-98 (2 paths), F-99 (1 path), F-100 (breaker schema — no net-new paths).
    - Test 3j: `test/openapi/openapi-build.test.js` contains new assertions for Phase-203 path coverage (≥ 12 new paths across F-96/F-97/F-98/F-99).
  </behavior>
  <action>
    1. **Create `scripts/load/webhooks-smoke.mjs`** — copy `scripts/load/mcp-smoke.mjs` structure + adjust:
       - Top-level: early return with `[dry-run] ...` if `!process.env.MARKOS_WEBHOOK_SMOKE_BASE_URL`.
       - Target URL: `${MARKOS_WEBHOOK_SMOKE_BASE_URL}/api/webhooks/test-fire`.
       - 60 concurrent workers × 60s duration.
       - p95 threshold: 500ms (includes subscriber RTT; 202-10's 300ms was for MCP internal).
       - Error rate threshold: 0.01 (1%).
       - Emit JSON summary at exit.
       - Use `node:worker_threads` or plain `Promise.all` with fetch (same as 202-10).

    2. **Create `docs/webhooks.md`** — Subscriber integration guide:
       - Intro: what MarkOS webhooks are, events published, HMAC signing, 24-attempt retry, DLQ, rotation.
       - Verify snippet (Node.js):
         ```javascript
         const crypto = require('node:crypto');
         function verify(secret, body, signature, timestamp) {
           const now = Math.floor(Date.now() / 1000);
           if (Math.abs(now - Number(timestamp)) > 300) return false;  // skew window
           const expected = 'sha256=' + crypto.createHmac('sha256', secret).update(`${timestamp}.${body}`).digest('hex');
           return crypto.timingSafeEqual(Buffer.from(signature), Buffer.from(expected));
         }
         const v1 = req.headers['x-markos-signature-v1'];
         const v2 = req.headers['x-markos-signature-v2']; // only during rotation grace
         const ts = req.headers['x-markos-timestamp'];
         const body = rawBody.toString('utf8');
         if (verify(SECRET, body, v1, ts) || (v2 && verify(NEW_SECRET, body, v2, ts))) {
           // accept
         }
         ```
       - Python + Go snippet stubs (shorter).
       - Responding: 2xx = ack; 5xx/timeout = MarkOS retries; 4xx = MarkOS treats as ack (no retry).
       - Headers reference (x-markos-signature-v1, x-markos-signature-v2, x-markos-timestamp, x-markos-replayed-from, x-markos-attempt, x-markos-event).

    3. **Create `docs/webhooks/rotation.md`**:
       - When to rotate (admin decision, SOC 2 best practice).
       - How 30-day grace works: both V1 + V2 headers sent during grace.
       - T-7 / T-1 / T-0 email notifications + dashboard banner.
       - Rollback within grace.
       - Post-grace: old secret is unrecoverable (D-12).
       - Stripe pattern reference.

    4. **Create `docs/webhooks/dlq.md`**:
       - What puts a delivery in DLQ (24 failed attempts).
       - 7-day replay window (D-08).
       - Admin replay from dashboard (D-05 single + batch).
       - Replay headers: x-markos-replayed-from + x-markos-attempt.
       - DLQ is NOT auto-retry (D-07).

    5. **Create `docs/webhooks/status.md`**:
       - /status/webhooks public page.
       - What the 4 numbers mean.
       - 60s cache.
       - Platform-wide (not per-tenant).

    6. **Create `docs/llms/phase-203-webhooks.md`** — LLM-friendly overview:
       - 1-paragraph summary of Phase 203.
       - Links to the 4 docs + 10 SUMMARYs + D-01..D-16 decisions + F-72..F-100 contracts + locked quality gates.
       - Format mirrors `docs/llms/phase-201-tenancy.md` + `docs/llms/phase-202-mcp.md`.

    7. **Update `public/llms.txt`** — append `## Phase 203 — Webhooks` section after the existing Phase 202 section. Include 5+ entries (`- [Webhooks integration guide](/docs/webhooks)`, rotation, dlq, status, llms overview).

    8. **Regenerate OpenAPI**: `node scripts/openapi/build-openapi.cjs` — final merge across all 203 F-NN contracts.

    9. **Extend `test/openapi/openapi-build.test.js`** with 4 new path-coverage assertions specific to Phase 203 — assert presence of:
       - `/api/tenant/webhooks/fleet-metrics` (F-96).
       - `/api/tenant/webhooks/subscriptions/{sub_id}/rotate` (F-97).
       - `/api/tenant/webhooks/subscriptions/{sub_id}/dlq/replay` (F-98).
       - `/api/public/webhooks/status` (F-99).
  </action>
  <verify>
    <automated>node --test test/openapi/openapi-build.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/openapi/openapi-build.test.js` returns 0 with the 4 new Phase-203 path assertions.
    - `node scripts/load/webhooks-smoke.mjs` exits 0 in dry-run mode (no MARKOS_WEBHOOK_SMOKE_BASE_URL set).
    - `grep -c "MARKOS_WEBHOOK_SMOKE_BASE_URL" scripts/load/webhooks-smoke.mjs` ≥ 1.
    - `grep -cE "500|0\\.01" scripts/load/webhooks-smoke.mjs` ≥ 2 (p95 + error-rate thresholds).
    - `ls docs/webhooks.md docs/webhooks/rotation.md docs/webhooks/dlq.md docs/webhooks/status.md docs/llms/phase-203-webhooks.md` = 5 found.
    - `grep -c "x-markos-signature-v1" docs/webhooks.md` ≥ 1 (dual-sig discussion).
    - `grep -c "30-day grace" docs/webhooks/rotation.md` ≥ 1.
    - `grep -c "7-day" docs/webhooks/dlq.md` ≥ 1.
    - `grep -c "## Phase 203" public/llms.txt` ≥ 1.
    - Full regression: `node --test test/webhooks/*.test.js test/openapi/openapi-build.test.js test/audit/hash-chain.test.js` all green.
    - contracts/openapi.json has all 203 F-NN contracts merged (check via: `grep -cE "/api/tenant/webhooks|/api/public/webhooks" contracts/openapi.json` ≥ 11 operations).
  </acceptance_criteria>
  <done>QA-07 load smoke + 5 docs + llms.txt update + final OpenAPI merge; Phase 203 ready for /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public status endpoint | No auth — data MUST be aggregate-only |
| load smoke → production | Dry-run fallback mandatory |
| docs → external readers | Code snippets must not leak secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-203-10-01 | Information Disclosure | status endpoint leaks per-tenant data | mitigate | Aggregation is platform-wide only (tenant_id filter omitted / = null); response shape documented in F-99; test 2c asserts no tenant_id in body |
| T-203-10-02 | Denial of Service | status endpoint receives burst traffic | mitigate | 60s cache header + CDN-ttl (`s-maxage=60`); `stale-while-revalidate=60` softens origin hits |
| T-203-10-03 | Repudiation | observability logs lost | accept | 202-05 triple-safety pattern (env gate + lazy import + try/catch) — captureToolError never propagates; log-drain emits to console (Vercel captures); pattern proven via 202-05 observability.test.js 49/49 green |
| T-203-10-04 | Information Disclosure | docs publish subscriber secret example | mitigate | docs/webhooks.md uses placeholder `SECRET` / `NEW_SECRET` — never real hex; review during phase verify |
| T-203-10-05 | Denial of Service | load smoke against production | mitigate | Smoke script requires MARKOS_WEBHOOK_SMOKE_BASE_URL (opt-in only); no default URL; CI-safe |
| T-203-10-06 | Tampering | docs examples misrepresent behavior | accept | Docs are reference; binding contract is F-72..F-100 OpenAPI; docs updated when contracts change |
</threat_model>

<verification>
1. `node --test test/webhooks/observability.test.js test/webhooks/public-status.test.js test/webhooks/status-page.test.js test/webhooks/ui-s3-a11y.test.js test/openapi/openapi-build.test.js` — all green.
2. `node scripts/load/webhooks-smoke.mjs` — dry-run green.
3. 5 docs + llms.txt update file-presence.
4. Final OpenAPI has all 203 F-NN contracts merged; path count assertion green.
5. Full phase regression: `node --test test/webhooks/*.test.js test/audit/hash-chain.test.js` — all green.
</verification>

<success_criteria>
- QA-07 load smoke harness ships with dry-run fallback.
- QA-09 observability (log-drain + Sentry) wired at every outcome path + catch block.
- QA-15 docs ship 5 pages + llms.txt entry.
- Surface 3 public status page standalone (no shell) matches UI-SPEC.
- F-99 contract declares public path.
- Final OpenAPI merged with all 203 F-NN contracts.
- Phase 203 closes ready for /gsd-verify-work.
</success_criteria>

<output>
After completion, create `.planning/phases/203-webhook-subscription-engine-ga/203-10-SUMMARY.md` per `get-shit-done/templates/summary.md`.
</output>
