---
phase: 203
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/markos/webhooks/store.cjs
  - lib/markos/webhooks/store.ts
  - lib/markos/webhooks/store-supabase.cjs
  - lib/markos/webhooks/store-supabase.ts
  - lib/markos/webhooks/store-vercel-queue.cjs
  - lib/markos/webhooks/store-vercel-queue.ts
  - api/webhooks/queues/deliver.js
  - vercel.ts
  - package.json
  - package-lock.json
  - test/webhooks/store-supabase.test.js
  - test/webhooks/adapter-supabase.test.js
  - test/webhooks/adapter-queues.test.js
  - test/webhooks/vercel-queue.test.js
autonomous: true
requirements: [WHK-01, QA-05, QA-13]
user_setup:
  - service: vercel
    why: "Vercel Queues (beta) topic provisioning for markos-webhook-delivery"
    env_vars:
      - name: WEBHOOK_STORE_MODE
        source: "Vercel Project → Environment Variables (default 'supabase'; set to 'memory' only for local-only runs)"
    dashboard_config:
      - task: "Provision queue topic 'markos-webhook-delivery' (queue/v2beta)"
        location: "Vercel Dashboard → Storage → Queues (may auto-provision on first push)"

must_haves:
  truths:
    - "Every caller of getWebhookStores() receives Supabase-backed subscriptions + deliveries by default and Vercel Queues push for queue.push"
    - "WEBHOOK_STORE_MODE=memory restores the 200-03 in-memory adapters verbatim so existing test suites keep passing"
    - "api/webhooks/queues/deliver.js registered as a queue/v2beta consumer in vercel.ts and processes delivery messages by delegating to processDelivery"
    - "Consumer visibilityTimeoutSeconds is 120 and retry returns { acknowledge: true } after 24 attempts (matches engine.cjs 24-attempt cap)"
    - "@vercel/queue 0.1.6 present in package.json dependencies"
  artifacts:
    - path: "lib/markos/webhooks/store-supabase.cjs"
      provides: "createSupabaseSubscriptionsStore + createSupabaseDeliveriesStore (tenant-first filter)"
    - path: "lib/markos/webhooks/store-supabase.ts"
      provides: "TS dual-export of same"
    - path: "lib/markos/webhooks/store-vercel-queue.cjs"
      provides: "createVercelQueueClient({ topic }) → { push(delivery_id) }"
    - path: "lib/markos/webhooks/store-vercel-queue.ts"
      provides: "TS dual-export of same"
    - path: "lib/markos/webhooks/store.cjs"
      provides: "getWebhookStores(deps?) with WEBHOOK_STORE_MODE switch"
    - path: "api/webhooks/queues/deliver.js"
      provides: "Vercel Queues push consumer invoking processDelivery"
    - path: "vercel.ts"
      provides: "experimentalTriggers entry for queue/v2beta topic markos-webhook-delivery"
  key_links:
    - from: "lib/markos/webhooks/store.cjs"
      to: "lib/markos/webhooks/store-supabase.cjs"
      via: "require() with service-role client (persistSession:false)"
      pattern: "createSupabase(Subscriptions|Deliveries)Store"
    - from: "api/webhooks/queues/deliver.js"
      to: "@vercel/queue handleCallback"
      via: "require('@vercel/queue').handleCallback"
      pattern: "handleCallback\\("
    - from: "api/webhooks/queues/deliver.js"
      to: "lib/markos/webhooks/delivery.cjs processDelivery"
      via: "require('../../../lib/markos/webhooks/delivery.cjs')"
      pattern: "processDelivery"
---

<objective>
Wave 1 foundation: Swap the `lib/markos/webhooks/store.cjs` in-memory singletons for Supabase-backed subscription + delivery adapters AND a `@vercel/queue` push-mode delivery consumer, preserving the existing adapter interface verbatim so callers in `api/webhooks/{subscribe,unsubscribe,list,test-fire}.js` require zero edits. Install `@vercel/queue` 0.1.6 (net-new dep for this phase) and register the `api/webhooks/queues/deliver.js` consumer with a `queue/v2beta` trigger in `vercel.ts`. This plan is the hard-dependency gate for every other 203 plan — per **D-16**, no GA feature work (DLQ, rotation, dashboard, rate-limit, breaker, status page) may proceed until durable state lands.

Purpose: Process-local store singletons silently lose data on Vercel Fluid Compute instance turnover (RESEARCH §Pitfall 1); every downstream plan assumes cross-instance durable state. This plan removes that blocker.
Output: `store.cjs` + Supabase + Vercel-Queues adapters + consumer handler + vercel.ts registration + 4 test suites.
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/203-webhook-subscription-engine-ga/203-CONTEXT.md
@.planning/phases/203-webhook-subscription-engine-ga/203-RESEARCH.md
@.planning/phases/203-webhook-subscription-engine-ga/203-VALIDATION.md
@.planning/phases/200-saas-readiness-wave-0/200-03-webhooks-SUMMARY.md
@.planning/phases/202-mcp-server-ga-claude-marketplace/202-04-SUMMARY.md

<interfaces>
<!-- Extracted from repo — executor uses these verbatim; no codebase exploration needed. -->

From `lib/markos/webhooks/store.cjs` (CURRENT, to be rewritten):
```javascript
function getWebhookStores() → { subscriptions, deliveries, queue }
function _resetWebhookStoresForTests() → void
```

From `lib/markos/webhooks/engine.cjs` (EXISTING — do NOT edit shape):
```javascript
function createInMemoryStore() → {
  insert(row), findById(tenant_id, id), listByTenant(tenant_id),
  updateActive(tenant_id, id, active)
}
```

From `lib/markos/webhooks/delivery.cjs` (EXISTING):
```javascript
function createInMemoryDeliveryStore() → {
  insert(row), findById(id), update(id, patch), listByTenant(tenant_id, opts?)
}
function createInMemoryQueue() → { push(delivery_id), drain() }
async function processDelivery(deliveries, subscriptions, delivery_id) → { delivered: boolean, status }
```

Supabase table columns (from migration 70):
- `markos_webhook_subscriptions`: id, tenant_id, url, events[], secret, active, created_at, updated_at
- `markos_webhook_deliveries`: id, tenant_id, subscription_id, event_type, body, status, attempt, created_at, updated_at

From `lib/markos/mcp/rate-limit.cjs` line 1-23 (Upstash memoization pattern to mirror):
```javascript
let _limiters = null;
function getLimiters(override) { if (_limiters && !override) return _limiters; /* Redis.fromEnv() */ }
```

From `lib/markos/mcp/sentry.cjs` (reference for Supabase service-role client posture — RESEARCH §Pitfall 6):
```javascript
createClient(SUPABASE_URL, SERVICE_ROLE_KEY, { auth: { persistSession: false }, db: { schema: 'public' } })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Supabase adapters (subscriptions + deliveries) + store.cjs rewrite</name>
  <files>lib/markos/webhooks/store-supabase.cjs, lib/markos/webhooks/store-supabase.ts, lib/markos/webhooks/store.cjs, lib/markos/webhooks/store.ts, test/webhooks/store-supabase.test.js, test/webhooks/adapter-supabase.test.js</files>
  <read_first>
    - lib/markos/webhooks/store.cjs (current implementation to rewrite)
    - lib/markos/webhooks/engine.cjs (createInMemoryStore interface — must be preserved verbatim)
    - lib/markos/webhooks/delivery.cjs (createInMemoryDeliveryStore + createInMemoryQueue interfaces)
    - .planning/phases/203-webhook-subscription-engine-ga/203-RESEARCH.md (§Pattern 1 Store Adapter Swap + §Code Examples Supabase subscriptions adapter + §Pitfall 6 service-role client)
    - .planning/phases/203-webhook-subscription-engine-ga/203-CONTEXT.md (D-16: swap in Wave 1)
    - lib/markos/mcp/sessions.cjs (dual-export .cjs/.ts pattern reference)
    - supabase/migrations/70_markos_webhook_subscriptions.sql (column names for adapter SELECT/UPDATE)
  </read_first>
  <behavior>
    - Test 1a: `createSupabaseSubscriptionsStore(client).insert({tenant_id, url, ...})` calls `client.from('markos_webhook_subscriptions').insert(row).select().single()` and returns data row.
    - Test 1b: `updateActive(tenant_id, id, false)` chain is `.update(...).eq('tenant_id', tenant_id).eq('id', id).select().maybeSingle()` (tenant_id FIRST — cross-tenant guard).
    - Test 1c: `listByTenant(tenant_id)` returns only rows where `tenant_id` matches AND `active=true`.
    - Test 1d: `findById(tenant_id, id)` returns null when no row matches (maybeSingle path).
    - Test 1e: `createSupabaseDeliveriesStore(client).insert(row)` chains `.insert().select().single()`.
    - Test 1f: `deliveries.update(id, patch)` merges `updated_at: new Date().toISOString()` into patch.
    - Test 1g: `deliveries.listByTenant(tenant_id, { status: 'failed' })` applies `.eq('status','failed')` filter.
    - Test 1h: `getWebhookStores({ mode: 'memory' })` returns in-memory stores verbatim.
    - Test 1i: `getWebhookStores()` with no args AND `WEBHOOK_STORE_MODE=supabase` (env) returns Supabase-backed subscriptions + deliveries + Vercel Queues client (queue piece covered in Task 2 suite but resolution works here).
    - Test 1j: `getWebhookStores({ mode: 'memory' })` returns the same instances across calls (singleton preserved for in-memory mode — existing 200-03 test invariant).
    - Test 1k: `_resetWebhookStoresForTests()` clears cached adapter instances.
  </behavior>
  <action>
    1. **Install `@vercel/queue` 0.1.6** (net-new dep): run `npm install @vercel/queue@0.1.6 --save --ignore-scripts` (use `--ignore-scripts` to avoid post-install hooks; parallel-wave posture). Verify `package.json` has `"@vercel/queue": "^0.1.6"` in dependencies.

    2. **Create `lib/markos/webhooks/store-supabase.cjs`** (dual-export target):
       - `'use strict';`
       - Export `createSupabaseSubscriptionsStore(client)` returning an object with 4 methods: `insert(row)`, `updateActive(tenant_id, id, active)`, `listByTenant(tenant_id)`, `findById(tenant_id, id)`. Shape EXACTLY as in 203-RESEARCH.md §Code Examples lines 631-673 (tenant_id is the FIRST filter on updateActive + findById — cross-tenant guard).
       - Export `createSupabaseDeliveriesStore(client)` with 4 methods: `insert(row)`, `findById(id)`, `update(id, patch)` (merges `updated_at: new Date().toISOString()`), `listByTenant(tenant_id, { status?, since?, limit = 100 } = {})`. Shape EXACTLY as in 203-RESEARCH.md §Code Examples lines 675-719. Order descending on `created_at`, `.limit(limit)`.
       - Every adapter method throws `new Error(\`store-supabase.<method>: \${error.message}\`)` on Supabase error (matches 202 pattern).
       - `module.exports = { createSupabaseSubscriptionsStore, createSupabaseDeliveriesStore };`

    3. **Create `lib/markos/webhooks/store-supabase.ts`** as a dual-export mirror: same implementation but with TS annotations `(client: any)` return types (match lib/markos/webhooks/engine.ts shape). CJS + TS parity mandatory per CLAUDE.md.

    4. **Rewrite `lib/markos/webhooks/store.cjs`** (replacing current 23-LOC in-memory singleton):
       - Keep exports `getWebhookStores(deps?)` + `_resetWebhookStoresForTests`.
       - Resolve mode via `deps.mode || process.env.WEBHOOK_STORE_MODE || 'supabase'`.
       - When `mode === 'memory'`: preserve existing singleton behavior (return cached `createInMemoryStore` + `createInMemoryDeliveryStore` + `createInMemoryQueue` instances); memoize `_subscriptionsMemo`, `_deliveriesMemo`, `_queueMemo` module-scope variables.
       - When `mode === 'supabase'`: lazy-construct `supabase = deps.supabase || createClient(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, { auth: { persistSession: false }, db: { schema: 'public' } })` (RESEARCH §Pitfall 6 — service-role bypasses RLS only when instantiated this way); call `createSupabaseSubscriptionsStore(supabase)` + `createSupabaseDeliveriesStore(supabase)`; resolve queue via `createVercelQueueClient({ topic: 'markos-webhook-delivery' })` (Task 2 provides this).
       - Memoize Supabase-mode stores at module scope too: `_supaSubs`, `_supaDels`, `_supaQueue` cleared by `_resetWebhookStoresForTests()`.
       - Export test-seam: `_resetWebhookStoresForTests()` must clear ALL 6 module-scope caches (both memory + supabase).

    5. **Rewrite `lib/markos/webhooks/store.ts`** as dual-export mirror (TS types; same resolution logic).

    6. **Write `test/webhooks/store-supabase.test.js`** + **`test/webhooks/adapter-supabase.test.js`**:
       - Use `node:test` (`const { test, describe, beforeEach } = require('node:test')`) + `const assert = require('node:assert/strict')` — matches 200-03 + 202 suites.
       - Build a mock Supabase client via a tiny builder: `function mockSupabase(rows)` returning an object with chained `.from().insert().select().single()` / `.eq().eq().select().maybeSingle()` / `.update().eq().eq().select().maybeSingle()` — return predictable data/error pairs. Do NOT hit real Supabase.
       - Cover behavior 1a-1g above.
       - `adapter-supabase.test.js` covers the `getWebhookStores` resolution — behavior 1h-1k: mode switch works, memory mode singleton preserved, supabase mode injects client via `deps.supabase`, `_resetWebhookStoresForTests` clears caches.

    NO dependencies on Task 2 — this task alone does NOT require the Vercel Queues consumer to exist (it uses `deps.queue` injection or an undefined queue in test scope).
  </action>
  <verify>
    <automated>node --test test/webhooks/store-supabase.test.js test/webhooks/adapter-supabase.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/webhooks/store-supabase.test.js test/webhooks/adapter-supabase.test.js` returns 0 (all green).
    - `grep -c "eq('tenant_id', tenant_id)" lib/markos/webhooks/store-supabase.cjs` ≥ 2 (tenant-first filter on updateActive + findById).
    - `grep -c "persistSession: false" lib/markos/webhooks/store.cjs` = 1 (service-role client pattern per RESEARCH §Pitfall 6).
    - `grep -c "WEBHOOK_STORE_MODE" lib/markos/webhooks/store.cjs` ≥ 1 (env switch).
    - `node -e "const p=require('./package.json'); if(!p.dependencies['@vercel/queue']) process.exit(1); console.log(p.dependencies['@vercel/queue'])"` prints `^0.1.6` (or equivalent).
    - `grep -c "createSupabaseSubscriptionsStore" lib/markos/webhooks/store.cjs lib/markos/webhooks/store-supabase.cjs` ≥ 2 (defined + consumed).
    - `node --test test/webhooks/engine.test.js test/webhooks/delivery.test.js test/webhooks/api-endpoints.test.js` returns 0 (regression: existing 200-03 suites with `mode: 'memory'` default preserved).
    - Both `.cjs` and `.ts` versions of each adapter exist (`ls lib/markos/webhooks/store-supabase.cjs lib/markos/webhooks/store-supabase.ts lib/markos/webhooks/store.cjs lib/markos/webhooks/store.ts` = 4 found).
  </acceptance_criteria>
  <done>Supabase subscriptions + deliveries adapters pass 1a–1g; mode switch passes 1h–1k; regression on 200-03 suites preserved with WEBHOOK_STORE_MODE=memory default in tests; @vercel/queue 0.1.6 installed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vercel Queues client adapter + push consumer + vercel.ts trigger + integration suite</name>
  <files>lib/markos/webhooks/store-vercel-queue.cjs, lib/markos/webhooks/store-vercel-queue.ts, api/webhooks/queues/deliver.js, vercel.ts, test/webhooks/adapter-queues.test.js, test/webhooks/vercel-queue.test.js</files>
  <read_first>
    - lib/markos/webhooks/store.cjs (post-Task-1 rewrite — for the queue adapter import target)
    - lib/markos/webhooks/delivery.cjs (processDelivery signature — consumer imports it)
    - lib/markos/webhooks/engine.cjs (24-attempt cap — retry callback upper bound)
    - .planning/phases/203-webhook-subscription-engine-ga/203-RESEARCH.md (§Pattern 2 Vercel Queues Push-Mode Consumer lines 273-337 verbatim; §Pitfall 5 v2beta trigger-rename risk)
    - vercel.ts (5 existing cron entries — must be preserved; add queue trigger additively)
    - api/mcp/session/cleanup.js (cron wrapper pattern — MARKOS_WEBHOOK_CRON_SECRET header pattern mirrors MARKOS_MCP_CRON_SECRET)
    - lib/markos/mcp/sentry.cjs (captureToolError shape — webhook variant added in Plan 203-10)
  </read_first>
  <behavior>
    - Test 2a: `createVercelQueueClient({ topic: 'markos-webhook-delivery' })` returns an object with `push(delivery_id)` method.
    - Test 2b: `push('del_abc123')` calls `@vercel/queue.send('markos-webhook-delivery', { delivery_id: 'del_abc123' })` via dep-injected `send` function.
    - Test 2c: `push()` rejects with a typed error when `delivery_id` is falsy (no silent ack on missing id).
    - Test 2d: `push('del_x')` optionally passes an `idempotencyKey` when `deps.idempotencyKey` is provided (Task 2 doesn't exercise replay idempotency — that's Plan 203-04 — but the adapter accepts the param).
    - Test 2e: consumer `handleCallback` invokes `processDelivery(stores.deliveries, stores.subscriptions, message.delivery_id)` and returns its result on success.
    - Test 2f: consumer `retry` callback returns `{ acknowledge: true }` when `metadata.deliveryCount > 24` (24-attempt cap matches engine.cjs existing behavior).
    - Test 2g: consumer `retry` callback returns `{ afterSeconds: delay }` otherwise with `delay = Math.min(86400, 5 * Math.pow(2, Math.min(metadata.deliveryCount, 15)))` (mirrors engine.cjs `computeBackoffSeconds` shape).
    - Test 2h: `visibilityTimeoutSeconds` in the consumer options equals `120`.
    - Test 2i: `vercel.ts` contains a queue trigger entry for `api/webhooks/queues/deliver.js` with `"type": "queue/v2beta"`, `"topic": "markos-webhook-delivery"`, and the 5 existing cron entries are all still present (audit/drain, lifecycle/purge, mcp-cron, session-cleanup, mcp-kpi-digest).
  </behavior>
  <action>
    1. **Create `lib/markos/webhooks/store-vercel-queue.cjs`**:
       - `'use strict';`
       - Export `createVercelQueueClient({ topic, deps = {} })` returning `{ push }`:
         - Resolve `sendFn = deps.send || require('@vercel/queue').send` lazily at first push (avoid test-time import).
         - `push(delivery_id, options = {})`: if `!delivery_id` throw `new Error('store-vercel-queue.push: delivery_id required')`; call `sendFn(topic, { delivery_id }, options.idempotencyKey ? { idempotencyKey: options.idempotencyKey } : undefined)`.
       - `module.exports = { createVercelQueueClient };`

    2. **Create `lib/markos/webhooks/store-vercel-queue.ts`** as dual-export mirror.

    3. **Wire queue adapter into store.cjs** (completing Task 1's stub): ensure `store.cjs` Supabase-mode path imports `createVercelQueueClient` from `./store-vercel-queue.cjs` and calls it with `{ topic: 'markos-webhook-delivery' }`. If already done in Task 1, no-op; otherwise add the import + call.

    4. **Create `api/webhooks/queues/deliver.js`** (Vercel Queues push consumer):
       - `'use strict';`
       - Copy the structure from 203-RESEARCH.md §Pattern 2 lines 281-322 verbatim BUT wire real imports:
         - `const { handleCallback } = require('@vercel/queue');`
         - `const { processDelivery } = require('../../../lib/markos/webhooks/delivery.cjs');`
         - `const { getWebhookStores } = require('../../../lib/markos/webhooks/store.cjs');`
         - Safe-require `emitLogLine` from `../../../lib/markos/webhooks/log-drain.cjs` with try/catch fallback `() => {}` (log-drain module ships in Plan 203-10; this plan's consumer must not crash on absence).
         - Safe-require `captureToolError` from `../../../lib/markos/webhooks/sentry.cjs` with try/catch fallback `() => {}` (same reason).
       - Export `module.exports = handleCallback(asyncHandler, { visibilityTimeoutSeconds: 120, retry })`.
       - `asyncHandler(message, metadata)`:
         - Destructure `{ delivery_id }` from `message`; if `!delivery_id` throw `new Error('missing_delivery_id')` (queue will retry).
         - `const { deliveries, subscriptions } = getWebhookStores();`
         - `const started = Date.now(); let result;`
         - In try: `result = await processDelivery(deliveries, subscriptions, delivery_id); return result;`
         - In catch: call `captureToolError(err, { req_id: metadata.messageId, delivery_id })`; rethrow.
         - In finally: call `emitLogLine({ domain: 'webhook', req_id: metadata.messageId, delivery_id, delivery_count: metadata.deliveryCount, duration_ms: Date.now() - started, status: result && result.delivered ? 'delivered' : (result && result.status) || 'unknown' })`.
       - `retry(err, metadata)`:
         - If `metadata.deliveryCount > 24` return `{ acknowledge: true }` (DLQ-as-Supabase-row semantics per RESEARCH §Pattern 2 note lines 338-339).
         - Else return `{ afterSeconds: Math.min(86400, 5 * Math.pow(2, Math.min(metadata.deliveryCount, 15))) }`.

    5. **Update `vercel.ts`** (additive edit — DO NOT remove or reorder existing 5 cron entries):
       - Read current `vercel.ts` first. Append a `functions['api/webhooks/queues/deliver.js']` entry with:
         ```ts
         'api/webhooks/queues/deliver.js': {
           experimentalTriggers: [
             { type: 'queue/v2beta', topic: 'markos-webhook-delivery', retryAfterSeconds: 60 }
           ]
         }
         ```
       - Preserve all 5 existing `crons` entries (audit-drain, lifecycle-purge-cron, cleanup-unverified-signups, mcp-session-cleanup, mcp-kpi-digest). The queue entry is additive under `functions`, not under `crons`.
       - Add an inline comment: `// Phase 203 Plan 01: Vercel Queues push consumer (queue/v2beta — rename expected when Vercel exits beta; see deferred-items.md)` (RESEARCH §Pitfall 5 mitigation).

    6. **Write `test/webhooks/adapter-queues.test.js`** + **`test/webhooks/vercel-queue.test.js`**:
       - `adapter-queues.test.js`: behavior 2a-2d. Inject `deps.send = (topic, payload, opts) => { capturedCall = { topic, payload, opts }; return Promise.resolve(); }` and assert the topic/payload shape.
       - `vercel-queue.test.js`: behavior 2e-2i. For 2e-2h, import the consumer module and exercise the internal handler + retry functions by dep-injection (export them via module.exports.__internals for tests, or use a test harness that stubs `handleCallback` to return its args). For 2i use `const vc = require('../../vercel.ts')` — if TS runtime loading fails, fall back to a regex grep over the file contents for the 3 required literals (`queue/v2beta`, `markos-webhook-delivery`, and the 5 existing cron paths).
  </action>
  <verify>
    <automated>node --test test/webhooks/adapter-queues.test.js test/webhooks/vercel-queue.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/webhooks/adapter-queues.test.js test/webhooks/vercel-queue.test.js` returns 0.
    - `grep -c "queue/v2beta" vercel.ts` ≥ 1.
    - `grep -c "markos-webhook-delivery" vercel.ts api/webhooks/queues/deliver.js lib/markos/webhooks/store.cjs` ≥ 3 (trigger + consumer + client).
    - `grep -c "handleCallback" api/webhooks/queues/deliver.js` ≥ 1.
    - `grep -c "visibilityTimeoutSeconds: 120" api/webhooks/queues/deliver.js` = 1.
    - `grep -cE "deliveryCount > 24" api/webhooks/queues/deliver.js` = 1 (retry-cap match with engine.cjs 24-attempt rule).
    - All 5 pre-existing cron paths (`/api/cron/audit-drain`, `/api/cron/lifecycle-purge`, `/api/cron/cleanup-unverified-signups`, `/api/mcp/session/cleanup`, `/api/cron/mcp-kpi-digest`) still match in `vercel.ts` (use grep: `grep -cE "audit-drain|lifecycle-purge|cleanup-unverified-signups|mcp/session/cleanup|mcp-kpi-digest" vercel.ts` ≥ 5).
    - Both `.cjs` and `.ts` adapters exist (`ls lib/markos/webhooks/store-vercel-queue.cjs lib/markos/webhooks/store-vercel-queue.ts` = 2 found).
    - Full Wave-1 suite: `node --test test/webhooks/*.test.js` returns 0.
  </acceptance_criteria>
  <done>Vercel Queues adapter + consumer + v2beta trigger registered without disturbing existing 5 crons; 24-attempt cap enforced in retry callback; log-drain + Sentry wiring uses safe-require shims pending Plan 203-10.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant-admin → subscribe handler | Untrusted URL flows into subscription row |
| @vercel/queue → consumer handler | Message payload shape assumed `{ delivery_id }` — must be validated |
| consumer handler → Supabase | Service-role client bypasses RLS — every query MUST include tenant_id filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-203-01-01 | Tampering | consumer handler | mitigate | Consumer asserts `message.delivery_id` truthy; throws → queue retries; after 24 → acknowledge (no silent drop of malformed messages without audit trail — processDelivery writes audit) |
| T-203-01-02 | Information Disclosure | Supabase service-role client | mitigate | `persistSession:false` + never instantiated with user JWT (RESEARCH §Pitfall 6); every adapter method uses `.eq('tenant_id', tenant_id)` FIRST (RESEARCH §Code Examples) |
| T-203-01-03 | Denial of Service | queue consumer | mitigate | `visibilityTimeoutSeconds: 120` bounds slow-subscriber hang; 24-attempt cap via retry callback prevents infinite redelivery |
| T-203-01-04 | Repudiation | queue adapter | accept | Vercel Queues is beta (v2beta); trigger-rename risk documented in deferred-items.md (RESEARCH §Pitfall 5). Smoke test in Plan 203-10 will alert on regression. |
| T-203-01-05 | Elevation of Privilege | store.cjs mode switch | mitigate | `WEBHOOK_STORE_MODE=memory` intended for local-only; production ALWAYS uses supabase mode (default). Test suites isolate via explicit `deps.mode: 'memory'`. |
</threat_model>

<verification>
1. Wave-1 targeted: `node --test test/webhooks/store-supabase.test.js test/webhooks/adapter-supabase.test.js test/webhooks/adapter-queues.test.js test/webhooks/vercel-queue.test.js` — all green.
2. Regression on 200-03: `node --test test/webhooks/signing.test.js test/webhooks/engine.test.js test/webhooks/delivery.test.js test/webhooks/api-endpoints.test.js` — all green (48 tests preserved via WEBHOOK_STORE_MODE=memory default in test context).
3. `npm ls @vercel/queue` shows 0.1.6 installed.
4. vercel.ts parses (TS compile check not required; syntactic integrity via regex grep of the 5 pre-existing cron entries).
</verification>

<success_criteria>
- Plan 203-01 files all present (`ls` check on files_modified list: 14 files).
- 2 new test suites + 2 existing-file extensions all green.
- `WEBHOOK_STORE_MODE=memory` regression preserves all 48 200-03 tests.
- @vercel/queue 0.1.6 in package.json.
- vercel.ts has queue/v2beta trigger + 5 existing crons intact.
- No production data loss path remains (RESEARCH §Pitfall 1 mitigated).
</success_criteria>

<output>
After completion, create `.planning/phases/203-webhook-subscription-engine-ga/203-01-SUMMARY.md` per `get-shit-done/templates/summary.md`.
</output>
