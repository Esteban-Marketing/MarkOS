---
phase: 203
plan: 09
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05, 06, 07, 08]
files_modified:
  - api/tenant/webhooks/fleet-metrics.js
  - api/tenant/webhooks/subscriptions/list.js
  - api/tenant/webhooks/subscriptions/[sub_id]/index.js
  - api/tenant/webhooks/subscriptions/[sub_id]/update.js
  - api/tenant/webhooks/subscriptions/[sub_id]/delete.js
  - lib/markos/webhooks/metrics.cjs
  - lib/markos/webhooks/metrics.ts
  - app/(markos)/settings/webhooks/page.tsx
  - app/(markos)/settings/webhooks/page.module.css
  - app/(markos)/settings/webhooks/[sub_id]/page.tsx
  - app/(markos)/settings/webhooks/[sub_id]/page.module.css
  - contracts/F-96-webhook-dashboard-v1.yaml
  - test/webhooks/settings-api.test.js
  - test/webhooks/api-tenant.test.js
  - test/webhooks/settings-ui-a11y.test.js
  - test/webhooks/ui-s1-a11y.test.js
  - test/webhooks/ui-s2-a11y.test.js
autonomous: true
requirements: [WHK-01, QA-01, QA-02, QA-03, QA-14]

must_haves:
  truths:
    - "GET /api/tenant/webhooks/fleet-metrics returns { total_24h, success_rate, avg_latency_ms, dlq_count, window_start, window_end } scoped by tenant_id (reads markos_webhook_fleet_metrics_v1 view + countDLQ)"
    - "GET /api/tenant/webhooks/subscriptions returns tenant-scoped array with rate_limit + breaker_state decoration per sub"
    - "GET /api/tenant/webhooks/subscriptions/{sub_id} returns subscription detail + last-N deliveries + DLQ counter + RateLimitState + BreakerState (F-100 schema)"
    - "POST /update accepts { url, events, rps_override } — re-runs SSRF guard + rps_override ceiling check; rejects 400 on violation"
    - "POST /delete cancels in-flight deliveries (UPDATE pending rows to status='cancelled' + dlq_reason='subscription_deleted') + soft-deletes subscription (active=false); preserves audit rows per UI-SPEC"
    - "Surface 1 page.tsx hero renders 4 numbers (24h deliveries, success %, avg latency, DLQ count) with locked copy AND subscriptions table with URL/Events/Status/Last delivery/Success rate/Actions columns AND Create subscription dialog"
    - "Surface 2 [sub_id]/page.tsx renders breadcrumb + 3-tab bar (Deliveries|DLQ|Settings) + inline row expand on delivery rows + DLQ batch-select replay + Settings with rotate/rollback/delete sections"
    - "Every CSS class traces to a Phase 201 or 202 ancestor OR is one of the 3 documented 203-new conventions (1040px max-width, 16px nested radius, dark mono code block) per UI-SPEC §Design-Token Alignment Table"
    - "WCAG 2.2 AA: min-height 44px on every interactive, outline 2px #0d9488 on :focus-visible, role=meter on success-rate bars, role=tablist/tab/tabpanel on S2 tabs, native <dialog> for all confirms, role=alert only on errors (not rotation banner)"
    - "F-96 contract declares 5 /api/tenant/webhooks/* paths + 403 + 404 + 400 envelopes"
  artifacts:
    - path: "api/tenant/webhooks/fleet-metrics.js"
      provides: "hero banner data source"
    - path: "api/tenant/webhooks/subscriptions/list.js"
      provides: "subscriptions list for S1 table"
    - path: "api/tenant/webhooks/subscriptions/[sub_id]/index.js"
      provides: "GET detail for S2"
    - path: "api/tenant/webhooks/subscriptions/[sub_id]/update.js"
      provides: "POST Settings tab save"
    - path: "api/tenant/webhooks/subscriptions/[sub_id]/delete.js"
      provides: "POST Danger zone delete"
    - path: "lib/markos/webhooks/metrics.cjs"
      provides: "aggregateFleetMetrics + perSubMetrics"
    - path: "app/(markos)/settings/webhooks/page.tsx"
      provides: "Surface 1"
    - path: "app/(markos)/settings/webhooks/[sub_id]/page.tsx"
      provides: "Surface 2"
    - path: "contracts/F-96-webhook-dashboard-v1.yaml"
      provides: "5 dashboard endpoint declarations"
  key_links:
    - from: "app/(markos)/settings/webhooks/page.tsx"
      to: "/api/tenant/webhooks/fleet-metrics + /api/tenant/webhooks/subscriptions"
      via: "parallel fetch on mount + 30s setInterval refresh (mirrors 202-09 cadence)"
      pattern: "fetch.*fleet-metrics|fetch.*subscriptions"
    - from: "app/(markos)/settings/webhooks/[sub_id]/page.tsx"
      to: "5 plan-203 endpoints (detail GET + rotate POST + rollback POST + replay POST + delete POST + update POST)"
      via: "tab-driven fetch + action button handlers"
      pattern: "fetch\\('/api/tenant/webhooks"
---

<objective>
Ship Wave 5's **tenant-admin dashboard** (Surfaces 1 + 2 per UI-SPEC) and the **5 new tenant APIs** that back them. This plan mirrors Plan 202-09 (`/settings/mcp` index) verbatim for shape, copy rules, and a11y contract — the 203 difference is the data domain (webhooks) and the additional complexity of S2's 3-tab detail page.

**5 new APIs** (in addition to the 4 already shipped in Waves 2–4 — replay single/batch, rotate, rollback, rotations/active):
1. `GET /api/tenant/webhooks/fleet-metrics` — S1 hero data
2. `GET /api/tenant/webhooks/subscriptions` — S1 subscriptions table
3. `GET /api/tenant/webhooks/subscriptions/[sub_id]` — S2 detail with embedded RateLimitState + BreakerState
4. `POST /api/tenant/webhooks/subscriptions/[sub_id]/update` — S2 Settings save
5. `POST /api/tenant/webhooks/subscriptions/[sub_id]/delete` — S2 Danger zone

**2 surfaces**:
- **S1 `/settings/webhooks`** — hero 4-number banner + subscriptions table + Create dialog
- **S2 `/settings/webhooks/[sub_id]`** — breadcrumb + 3-tab (Deliveries inline-expand / DLQ batch-replay / Settings rotate-rollback-delete)

**F-96 contract** declares the 5 new paths.

Purpose: Every locked decision D-01 through D-16 needs a visible surface; the admin dashboard is where the phase becomes operationally usable. This plan is the ONLY plan producing the dashboard UI (by design — UI + UX is one concern concentrated in one plan per UI-SPEC §Component Reuse Map).

Output: 5 API handlers + metrics library + 2 surface pages + CSS modules + F-96 contract + 3 test suites (API + 2 UI a11y).
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/203-webhook-subscription-engine-ga/203-CONTEXT.md
@.planning/phases/203-webhook-subscription-engine-ga/203-UI-SPEC.md
@.planning/phases/203-webhook-subscription-engine-ga/203-RESEARCH.md
@.planning/phases/203-webhook-subscription-engine-ga/203-01-PLAN.md
@.planning/phases/203-webhook-subscription-engine-ga/203-03-PLAN.md
@.planning/phases/203-webhook-subscription-engine-ga/203-04-PLAN.md
@.planning/phases/203-webhook-subscription-engine-ga/203-05-PLAN.md
@.planning/phases/203-webhook-subscription-engine-ga/203-07-PLAN.md
@.planning/phases/203-webhook-subscription-engine-ga/203-08-PLAN.md
@.planning/phases/202-mcp-server-ga-claude-marketplace/202-09-SUMMARY.md
@app/(markos)/settings/mcp/page.tsx
@app/(markos)/settings/mcp/page.module.css

<interfaces>
<!-- 5 library functions consumed by this plan's handlers -->
- listDLQ, countDLQ (Plan 203-03)
- replaySingle, replayBatch, BATCH_CAP (Plan 203-04)
- startRotation, rollbackRotation, listActiveRotations (Plan 203-05)
- resolvePerSubRps, PLAN_TIER_RPS (Plan 203-07)
- getBreakerState (Plan 203-08)

<!-- View from migration 72 -->
markos_webhook_fleet_metrics_v1 — aggregates bucket-hour data for last 48h by tenant_id

<!-- 202-09 handler pattern (mirror for 203-09) -->
api/tenant/mcp/usage.js: method-gate → headers(401) → supabase→lib → writeJson({...})
api/tenant/mcp/sessions/revoke.js: +SELECT-before-mutate cross_tenant_forbidden pattern
api/tenant/mcp/cost-breakdown.js: audit_log aggregation via .eq filters

<!-- 202-09 UI posture (mirror for 203-09) -->
'use client' + useState + useEffect parallel fetch on mount + setInterval(30_000)
Native <dialog> with dialogRef + showModal() for destructive confirm
role="status" aria-live="polite" toast region
@media (prefers-reduced-motion: reduce) disables transitions

<!-- LOCKED COPY STRINGS from UI-SPEC §Copywriting Contract (grep targets) -->
S1 h1: "Webhooks"
S1 subheading: "Deliver event callbacks to subscribers. HMAC-signed payloads with automatic retry, DLQ, and signing-secret rotation."
S1 CTA: "Create subscription"
S1 hero labels: "24h deliveries", "Success rate", "Avg latency (p50)", "Dead-letter queue"
S1 subs h2: "Subscriptions"
S1 caption: "Active webhook subscriptions"
S1 status chips: "Healthy", "Half-open", "Tripped"
S1 actions: "View", "Test fire", busy "Firing…"
S1 empty: "No webhook subscriptions yet. Create one to start receiving event callbacks."
S1 create dialog heading: "Create webhook subscription"
S1 toasts: "Subscription created.", "Test fired — check the Deliveries tab.", "Could not fire test. Try again."
S1 errors: "A subscription with this URL already exists.", "Private IPs are not allowed as subscriber endpoints.", "HTTPS required."

S2 breadcrumb: "Webhooks"
S2 tabs: "Deliveries", "DLQ", "Settings"
S2 expand: "Request", "Response", "Error"
S2 replay row: "Replay this delivery"
S2 copy curl: "Copy cURL" → "Copied."
S2 DLQ intro: "7-day replay window. Entries older than 7 days are purged automatically."
S2 DLQ actions: "Select all", "Replay ({N})", "Replay", "Delete"
S2 Settings: "Endpoint URL", "HTTPS required. Private IPs are rejected.", "Events subscribed", "Override (rps)", "Cannot exceed your plan tier ceiling.", "Current signature header", "Rotate secret" busy "Rotating…", "Rotate signing secret?", "A new secret will be generated now. Both old and new secrets will sign every webhook for 30 days.", "x-markos-signature-v1", "x-markos-signature-v2", "Rollback to old secret", "Rollback rotation?", "Delete subscription", "Delete this subscription?", "This cannot be undone.", "Save changes" busy "Saving…", "Settings saved."
S2 toasts: "Rotation started. Old secret retires on", "Rolled back. Old secret restored.", "Subscription deleted."

<!-- LOCKED TOKENS (grep targets in .module.css) -->
#0d9488 (accent), #0f766e (dark teal), #0f172a (body text), #9a3412 (destructive), #fca5a5 (dest border), #fef2f2 (dest bg), #fef3c7 (warn bg), #78350f (warn text), #d97706 (warn border), #991b1b (error text), #dc2626 (error border), #e6fffb (chip bg), #e2e8f0 (divider), #f1f5f9 (row sep), #cbd5e1 (input border), #334155 (label), #475569 (helper), #64748b (mono helper)
border-radius: 28px (top-level card), 16px (nested), 12px (button), 999px (chip)
outline: 2px solid #0d9488 (focus-visible)
min-height: 44px (tap target), 48px (primary CTA)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 5 tenant API handlers + metrics library + F-96 contract + api-tenant test suite</name>
  <files>api/tenant/webhooks/fleet-metrics.js, api/tenant/webhooks/subscriptions/list.js, api/tenant/webhooks/subscriptions/[sub_id]/index.js, api/tenant/webhooks/subscriptions/[sub_id]/update.js, api/tenant/webhooks/subscriptions/[sub_id]/delete.js, lib/markos/webhooks/metrics.cjs, lib/markos/webhooks/metrics.ts, contracts/F-96-webhook-dashboard-v1.yaml, test/webhooks/settings-api.test.js, test/webhooks/api-tenant.test.js</files>
  <read_first>
    - api/tenant/mcp/usage.js + api/tenant/mcp/cost-breakdown.js + api/tenant/mcp/sessions/list.js + api/tenant/mcp/sessions/revoke.js (202-09 handler pattern — MIRROR verbatim)
    - lib/markos/webhooks/dlq.cjs (Plan 203-03 — listDLQ, countDLQ)
    - lib/markos/webhooks/rate-limit.cjs (Plan 203-07 — resolvePerSubRps, PLAN_TIER_RPS)
    - lib/markos/webhooks/breaker.cjs (Plan 203-08 — getBreakerState)
    - lib/markos/webhooks/ssrf-guard.cjs (Plan 203-02 — assertUrlIsPublic for update)
    - supabase/migrations/72_markos_webhook_dlq_and_rotation.sql (fleet_metrics_v1 view)
    - .planning/phases/203-webhook-subscription-engine-ga/203-RESEARCH.md §Open Questions #3 (view vs rollup table decision — use view for Wave 5; upgrade later if p95 > 150ms)
    - contracts/F-95-mcp-cost-budget-v1.yaml (YAML shape for F-96)
    - lib/markos/crm/api.cjs writeJson helper
  </read_first>
  <behavior>
    - Test 1a: `GET /api/tenant/webhooks/fleet-metrics` without header auth → 401.
    - Test 1b: With valid headers → 200 `{ total_24h, success_rate, avg_latency_ms, dlq_count, window_start, window_end, tenant_id }`.
    - Test 1c: fleet-metrics tenant-scopes: cross-tenant deliveries do NOT contribute.
    - Test 1d: `GET /subscriptions` with valid headers → 200 `{ subscriptions: [{ id, url, events, status_chip, rate_limit, breaker_state, last_delivery_at, success_rate, active }, ...] }`; each row decorated with breaker_state from Plan 203-08 + rate_limit from Plan 203-07.
    - Test 1e: `GET /subscriptions/{sub_id}` tenant-scoped → 200 with full detail including `deliveries[]` (last 100 from markos_webhook_deliveries for this sub) + `dlq_count` + `rate_limit: { plan_tier, ceiling_rps, effective_rps, override_rps }` + `breaker_state: { state, trips, probe_at }` + `rotation: null | { grace_ends_at, stage }`.
    - Test 1f: GET /subscriptions/{sub_id} cross-tenant → 403 `{ error: 'cross_tenant_forbidden' }`.
    - Test 1g: `POST /update` with new url + events + rps_override → 200 `{ ok: true, subscription }` + SSRF guard ran + rps_override validated against plan ceiling.
    - Test 1h: POST /update with `rps_override > plan ceiling` → 400 `{ error: 'rps_override_exceeds_plan', ceiling }`.
    - Test 1i: POST /update with private-IP URL → 400 `{ error: 'private_ip' }`.
    - Test 1j: `POST /delete` with cross-tenant subscription → 403.
    - Test 1k: POST /delete happy path → 200 `{ ok: true }`. In-flight deliveries (status IN ('pending','retrying')) get UPDATEd to status='cancelled' + dlq_reason='subscription_deleted'. Subscription row gets active=false. Audit row emitted.
    - Test 1l: F-96 YAML has 5 paths + 403 + 404 + 400 envelopes + references to F-72 + F-100.
  </behavior>
  <action>
    1. **Create `lib/markos/webhooks/metrics.cjs`**:
       - `'use strict';`
       - `async function aggregateFleetMetrics(client, tenant_id, now = new Date())`:
         - Query `markos_webhook_fleet_metrics_v1` view: `.select('*').eq('tenant_id', tenant_id).gte('bucket', new Date(now.getTime() - 24*3600*1000).toISOString())`.
         - Sum total, delivered, failed, retrying, avg avg_latency_ms across buckets.
         - Separately fetch `dlq_count` via `countDLQ(client, { tenant_id })` (Plan 203-03).
         - Compute `success_rate = delivered / total` (percentage with 1 decimal); handle `total=0` → `success_rate=100.0` (no deliveries = healthy).
         - Return `{ total_24h, success_rate, avg_latency_ms, dlq_count, window_start: 24h ago ISO, window_end: now ISO }`.
       - `async function perSubMetrics(client, tenant_id, sub_id, now)`:
         - Query deliveries aggregated by sub: `select status, count(*), avg(extract(epoch from (updated_at - created_at)))` group by status.
         - Return `{ total_24h, success_rate, avg_latency_ms, last_delivery_at }`.
       - `module.exports = { aggregateFleetMetrics, perSubMetrics };`

    2. **Mirror to `lib/markos/webhooks/metrics.ts`**.

    3. **Create 5 API handlers** — each follows 202-09 pattern:

       **`api/tenant/webhooks/fleet-metrics.js`** (GET):
       - Method gate: GET only, 405 else.
       - Header auth: x-markos-user-id + x-markos-tenant-id; 401 if missing.
       - Delegate to `aggregateFleetMetrics(supabase, tenant_id)` + return JSON.

       **`api/tenant/webhooks/subscriptions/list.js`** (GET):
       - Header auth.
       - `const subs = await supabase.from('markos_webhook_subscriptions').select('*').eq('tenant_id', tenant_id);`
       - For each sub, decorate with: `rate_limit` via `resolvePerSubRps + PLAN_TIER_RPS`, `breaker_state` via `getBreakerState(redis, sub.id)`, `last_delivery_at` + `success_rate` via metrics helper.
       - Return `{ subscriptions: [decoratedRows] }`.

       **`api/tenant/webhooks/subscriptions/[sub_id]/index.js`** (GET):
       - Cross-tenant guard: SELECT + tenant_id check → 403 on mismatch.
       - Fetch sub + last-100 deliveries (`.limit(100).order('created_at', { ascending: false })`) + dlq_count via countDLQ + breaker_state + rate_limit + rotation (listActiveRotations filtered to this sub).
       - Return combined object.

       **`api/tenant/webhooks/subscriptions/[sub_id]/update.js`** (POST):
       - Header auth + cross-tenant guard.
       - Body parse: `{ url?, events?, rps_override? }`.
       - If url changed: run `assertUrlIsPublic(url)` — map errors to 400.
       - If rps_override provided: validate against plan ceiling; map to 400 on violation.
       - UPDATE subscription row (tenant_id + id filter).
       - Audit: source_domain='webhooks', action='subscription.updated', payload = updated fields.
       - Return `{ ok: true, subscription: updated }`.

       **`api/tenant/webhooks/subscriptions/[sub_id]/delete.js`** (POST):
       - Header auth + cross-tenant guard.
       - Transaction (2 updates):
         - `UPDATE markos_webhook_deliveries SET status='cancelled', dlq_reason='subscription_deleted', updated_at=now() WHERE subscription_id=X AND status IN ('pending','retrying')`
         - `UPDATE markos_webhook_subscriptions SET active=false, updated_at=now() WHERE id=X AND tenant_id=Y`
       - Audit: action='subscription.deleted'.
       - Return `{ ok: true }`.

    4. **Create `contracts/F-96-webhook-dashboard-v1.yaml`** — mirror F-95 YAML shape:
       - paths: 5 new paths (fleet-metrics GET, subscriptions GET, {sub_id} GET, {sub_id}/update POST, {sub_id}/delete POST).
       - errors: cross_tenant_forbidden (403), not_found (404), private_ip (400), rps_override_exceeds_plan (400), https_required (400).
       - references: D-01..D-16, F-72 (subscription), F-73 (delivery), F-100 (rate_limit + breaker shape).

    5. **Regenerate OpenAPI**: `node scripts/openapi/build-openapi.cjs`.

    6. **Write `test/webhooks/settings-api.test.js`** + **`test/webhooks/api-tenant.test.js`**:
       - `settings-api.test.js`: covers 1a-1c (fleet-metrics) + 1d (subs list).
       - `api-tenant.test.js`: covers 1e-1l (sub detail + update + delete + F-96 YAML).
       - Pattern per handler: mock Supabase client + mock Redis + invoke handler with fake req/res; assert status + body.
  </action>
  <verify>
    <automated>node --test test/webhooks/settings-api.test.js test/webhooks/api-tenant.test.js test/openapi/openapi-build.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/webhooks/settings-api.test.js test/webhooks/api-tenant.test.js test/openapi/openapi-build.test.js` returns 0.
    - `grep -c "x-markos-tenant-id" api/tenant/webhooks/` (recursive)  ≥ 5 (every handler checks the header).
    - `grep -c "cross_tenant_forbidden" api/tenant/webhooks/subscriptions/\\[sub_id\\]/{index.js,update.js,delete.js}` ≥ 3.
    - `grep -c "assertUrlIsPublic" api/tenant/webhooks/subscriptions/\\[sub_id\\]/update.js` ≥ 1 (SSRF guard on update).
    - `grep -c "rps_override_exceeds_plan" api/tenant/webhooks/subscriptions/\\[sub_id\\]/update.js` ≥ 1.
    - `grep -c "F-96" contracts/F-96-webhook-dashboard-v1.yaml` ≥ 1.
    - `grep -cE "/api/tenant/webhooks/(fleet-metrics|subscriptions|subscriptions/\\{sub_id\\}(/update|/delete)?)" contracts/F-96-webhook-dashboard-v1.yaml` ≥ 5 (all 5 paths).
    - `grep -c "aggregateFleetMetrics" lib/markos/webhooks/metrics.cjs` ≥ 1.
    - `grep -c "getBreakerState" api/tenant/webhooks/subscriptions/list.js api/tenant/webhooks/subscriptions/\\[sub_id\\]/index.js` ≥ 2 (decoration on both list + detail).
    - Full Wave-5 API regression: `node --test test/webhooks/*.test.js` all green.
  </acceptance_criteria>
  <done>5 tenant APIs follow 202-09 tenant-scope guard pattern; metrics library aggregates fleet + per-sub; F-96 declares all 5 paths + error envelopes; SSRF guard re-applied on update; cross-tenant guards on every mutator.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface 1 + Surface 2 pages + CSS modules + a11y grep suites</name>
  <files>app/(markos)/settings/webhooks/page.tsx, app/(markos)/settings/webhooks/page.module.css, app/(markos)/settings/webhooks/[sub_id]/page.tsx, app/(markos)/settings/webhooks/[sub_id]/page.module.css, test/webhooks/ui-s1-a11y.test.js, test/webhooks/ui-s2-a11y.test.js, test/webhooks/settings-ui-a11y.test.js</files>
  <read_first>
    - app/(markos)/settings/mcp/page.tsx + app/(markos)/settings/mcp/page.module.css (MIRROR base)
    - .planning/phases/203-webhook-subscription-engine-ga/203-UI-SPEC.md FULL (Surface 1 + Surface 2 component inventory + locked copy + token table + a11y contract)
    - app/(markos)/settings/members/page.module.css (seatBar → success-rate mini-bar reuse)
    - app/(markos)/settings/danger/page.module.css (purgeBanner → graceStatusPanel; dangerCard pattern)
    - app/(markos)/settings/sessions/page.module.css (sessionsTable → subscriptionsTable / deliveriesTable)
    - app/(markos)/invite/[token]/page.module.css (acceptButton → primaryFilledButton)
    - .planning/phases/202-mcp-server-ga-claude-marketplace/202-09-SUMMARY.md §Verification (grep-based a11y test pattern — MIRROR)
    - test/mcp/mcp-settings-ui-a11y.test.js (test file shape)
  </read_first>
  <behavior>
    - Test 2a (S1): page.tsx contains h1 "Webhooks" + subheading substring "HMAC-signed payloads with automatic retry" + CTA "Create subscription" + 4 hero labels + table caption "Active webhook subscriptions" + 3 chip states ("Healthy", "Half-open", "Tripped") + empty state + 3 loading strings.
    - Test 2b (S1 a11y): `aria-labelledby="webhooks-hero-heading"`, `aria-labelledby="webhooks-subs-heading"`, `role="meter"` (success-rate bar), `role="status"` + `aria-live="polite"` (toast), `<caption>`, `scope="col"`, `<dialog>` (create).
    - Test 2c (S1 CSS): `#0d9488` ≥ 3, `#0f766e` ≥ 1, `#fef3c7` ≥ 1, `#fef2f2` ≥ 1, `border-radius: 28px` (top card) ≥ 1, `border-radius: 12px` (button) ≥ 1, `outline: 2px solid #0d9488` ≥ 1, `min-height: 44px` ≥ 3, `prefers-reduced-motion` ≥ 1.
    - Test 2d (S2): page.tsx contains all locked S2 copy strings (breadcrumb "Webhooks", tabs "Deliveries"/"DLQ"/"Settings", "Request"/"Response"/"Error", "Replay this delivery", "Copy cURL"/"Copied.", "7-day replay window. Entries older than 7 days are purged automatically.", "Select all", "Rotate secret", "Rotate signing secret?", "A new secret will be generated now. Both old and new secrets will sign every webhook for 30 days.", "x-markos-signature-v1" AND "x-markos-signature-v2", "Rollback to old secret", "Rollback rotation?", "Delete subscription", "Delete this subscription?", "This cannot be undone.", "Save changes", "Settings saved.", "No dead-letter entries. All deliveries are succeeding or still retrying.", "No deliveries yet." substring).
    - Test 2e (S2 a11y): `role="tablist"`, `role="tab"`, `role="tabpanel"`, `aria-selected`, `aria-controls`, `aria-expanded` (row expand button), `<caption>`, `scope="col"`, `<dialog>` present (rotate + rollback + delete + replay dialogs), `<pre>` AND `<code>` (payload code blocks), `role="status"` AND `aria-live="polite"`, `role="alert"` (form errors).
    - Test 2f (S2 CSS): contains `#fef3c7` + `#78350f` (rotation grace panel), `#fef2f2` + `#9a3412` (danger dashed card), `#0f172a` + `#e2e8f0` used together (dark code block — 203-new convention #3), `border-radius: 28px` (top card), `border-radius: 16px` (nested panel — 203-new convention #2), `max-width: 1040px` (subscriptions + sub detail cards — 203-new convention #1).
  </action>
  <action>
    1. **Create `app/(markos)/settings/webhooks/page.tsx`** (Surface 1) — mirror `app/(markos)/settings/mcp/page.tsx` structure verbatim:
       - `'use client';` directive at top.
       - `useState` for: `heroMetrics` (4 numbers), `subscriptions[]`, `loadingMetrics`, `loadingSubs`, `createDialogOpen`, `testFireBusy[]`, `toastMsg`.
       - `useEffect` on mount: parallel fetch `/api/tenant/webhooks/fleet-metrics` + `/api/tenant/webhooks/subscriptions`. Store results.
       - `useEffect` `setInterval(30_000)` for re-fetch (UI-SPEC 30s cadence).
       - Render (in order):
         1. `<section aria-labelledby="webhooks-hero-heading">` — hero card with 4 `.heroCard` children (24h deliveries, Success rate, Avg latency, DLQ count) + `<h1 id="webhooks-hero-heading" className={styles.heading}>Webhooks</h1>` + subheading + Create CTA top-right.
         2. `<section aria-labelledby="webhooks-subs-heading">` — subscriptions table card with `<h2 id="webhooks-subs-heading">Subscriptions</h2>` + `<table>` with caption + columns per UI-SPEC.
         3. Create `<dialog ref={createDialogRef}>` with form for URL / Events / RPS override.
         4. Toast region `<div className={styles.toast} role="status" aria-live="polite">`.
       - For the success-rate mini-bar: `<div role="meter" aria-valuenow={Math.round(rate*10)} aria-valuemin={0} aria-valuemax={1000} aria-label="Delivery success rate">...`.
       - For status chip: `<span className={styles.statusChip} data-state={chipState} aria-label={\`Circuit breaker: \${chipState}\`}>{chipLabel}</span>` where chipLabel is "Healthy" | "Half-open" | "Tripped".
       - Click URL cell → `router.push(\`/settings/webhooks/\${sub.id}\`)` (use Next.js `useRouter`).
       - Test fire button → `POST /api/webhooks/test-fire` (existing 200-03 endpoint); toast on success/failure.

    2. **Create `app/(markos)/settings/webhooks/page.module.css`** — copy-paste-and-rename from `app/(markos)/settings/mcp/page.module.css`:
       - Keep rules: `.page`, `.contentCard`, `.heading`, `.subheading`, `.cardHeaderRow`, `.refreshButton` (rename to `.testFireButton`), `.sessionsTable` (rename to `.subscriptionsTable`), `.revokeButton` (rename/keep), `.toast`, `.emptyState`, `.dialog` family, `.cancelButton`, `.deleteFilledButton`, `.atCapBanner` (rename to `.pausedBanner` OR remove — S1 doesn't show this; keep rule for S2 grace panel compatibility).
       - Add 203-new classes per UI-SPEC §Component Inventory S1: `.heroGrid`, `.heroCard`, `.heroNumber`, `.heroLabel`, `.urlCell`, `.eventChipGroup`, `.eventChip`, `.overflowChip`, `.statusChip` (3 data-state variants), `.successRateCell` (with `.successBarTrack` + `.successBarFill`), `.actionsCell`, `.primaryFilledButton`.
       - Max-width: override top-level `.contentCard { max-width: 1040px }` (203-new convention #1).
       - Ensure `@media (prefers-reduced-motion: reduce)` block disables every transition.

    3. **Create `app/(markos)/settings/webhooks/[sub_id]/page.tsx`** (Surface 2):
       - `'use client';`
       - `useState`: `activeTab` (default from `?tab=` query or `'deliveries'`), `sub`, `deliveries`, `expandedDeliveryId`, `dlqEntries`, `selectedDlqIds` (Set), `settingsForm`, `rotation` (active rotation state), `busyAction`, `toastMsg`.
       - `useEffect` on mount: fetch subscription detail via `GET /api/tenant/webhooks/subscriptions/{sub_id}`.
       - `useEffect` on activeTab change: fetch tab-specific data (deliveries / dlq).
       - Render:
         1. Breadcrumb + heading card (h1 = sub.url mono truncated; subheading per UI-SPEC; chip row).
         2. Tab bar `<nav role="tablist" aria-label="Subscription detail sections">` with 3 `<button role="tab" aria-selected={...} aria-controls={panelId}>` — keyboard arrow nav.
         3. Tab panels — only active renders (Next.js shallow route `?tab=` drives).
         4. **Deliveries panel**: filter row + `<table>` with expand chevron column + inline `<tr>` expand region with `<pre><code>` payload blocks + row-level replay button + Copy cURL link.
         5. **DLQ panel**: intro line + batch-action bar (Select all + "Replay ({N})" primary filled) + retention counter + `<table>` with checkboxes.
         6. **Settings panel**: Endpoint section (URL input + events multi-checkbox) + Rate limit section (plan tier display + override input) + Signing secret section (`.secretPanel` with masked header preview + Rotate button OR grace panel during rotation + Rollback button inside grace) + Danger zone (`.dangerCard` + Delete button).
         7. All dialogs as native `<dialog>` with `dialogRef` + `showModal()`.
         8. Toast region.
       - Rotation panel conditional render: if `rotation.state === 'active'` AND `rotation.grace_ends_at > now` → show `.graceStatusPanel` with T-{N} days countdown + `.rollbackButton`; else show `.rotateButton`.

    4. **Create `app/(markos)/settings/webhooks/[sub_id]/page.module.css`** — copy-rename from 202 mcp + 201 danger + 201 sessions:
       - All rules from S1 CSS module above (re-included to keep co-location rule; DO NOT cross-import).
       - Add S2-specific: `.tabBar`, `.tabCountBadge`, `.deliveriesTable`, `.expandChevron`, `.expandRow`, `.codeBlock` (dark `#0f172a` bg + `#e2e8f0` text — 203-new convention #3), `.copyCurlLink`, `.checkboxCell`, `.batchActionBar`, `.replaySelectedButton`, `.singleReplayButton`, `.retentionCounter`, `.secretPanel` (border-radius 16px — 203-new convention #2), `.secretHeader`, `.rotateButton`, `.graceStatusPanel`, `.rollbackButton`, `.dangerCard`, `.deleteSubButton`, `.saveChangesButton`, `.filterRow`, `.filterChipGroup`.

    5. **Add sidebar entry** — update whichever file renders the `/settings` sidebar nav (inspect `app/(markos)/settings/` for a `layout.tsx` or dedicated nav component). Add "Webhooks" → `/settings/webhooks` between "MCP" and "Billing" per UI-SPEC §Sidebar entry.

    6. **Write `test/webhooks/ui-s1-a11y.test.js`** + **`test/webhooks/ui-s2-a11y.test.js`** + **`test/webhooks/settings-ui-a11y.test.js`**:
       - Mirror `test/mcp/mcp-settings-ui-a11y.test.js` pattern — read source with `fs.readFileSync` + assert string contents.
       - ui-s1-a11y: all S1 locked copy + a11y markers + CSS tokens (behaviors 2a-2c).
       - ui-s2-a11y: all S2 locked copy + a11y markers + CSS tokens (behaviors 2d-2f).
       - settings-ui-a11y: consolidation suite asserting sidebar has "Webhooks" link + the 2 page files both exist + CSS tokens match UI-SPEC §Design-Token Alignment Table.
       - Use 5+ grep assertions per test; match each UI-SPEC locked string exactly.
  </action>
  <verify>
    <automated>node --test test/webhooks/ui-s1-a11y.test.js test/webhooks/ui-s2-a11y.test.js test/webhooks/settings-ui-a11y.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/webhooks/ui-s1-a11y.test.js test/webhooks/ui-s2-a11y.test.js test/webhooks/settings-ui-a11y.test.js` returns 0.
    - `grep -c "Webhooks" app/(markos)/settings/webhooks/page.tsx` ≥ 1 (h1).
    - `grep -c "HMAC-signed payloads with automatic retry" app/(markos)/settings/webhooks/page.tsx` ≥ 1.
    - `grep -c "Create subscription" app/(markos)/settings/webhooks/page.tsx` ≥ 1.
    - `grep -cE "24h deliveries|Success rate|Avg latency \\(p50\\)|Dead-letter queue" app/(markos)/settings/webhooks/page.tsx` ≥ 4 (all hero labels).
    - `grep -cE "Healthy|Half-open|Tripped" app/(markos)/settings/webhooks/page.tsx` ≥ 3 (all chip states).
    - `grep -c "aria-labelledby=\"webhooks-hero-heading\"" app/(markos)/settings/webhooks/page.tsx` ≥ 1.
    - `grep -c "role=\"meter\"" app/(markos)/settings/webhooks/page.tsx` ≥ 1.
    - `grep -c "role=\"status\"" app/(markos)/settings/webhooks/page.tsx` ≥ 1.
    - `grep -cE "Deliveries|DLQ|Settings" app/(markos)/settings/webhooks/\\[sub_id\\]/page.tsx` ≥ 3.
    - `grep -c "role=\"tablist\"" app/(markos)/settings/webhooks/\\[sub_id\\]/page.tsx` ≥ 1.
    - `grep -c "Rotate signing secret?" app/(markos)/settings/webhooks/\\[sub_id\\]/page.tsx` ≥ 1.
    - `grep -c "x-markos-signature-v1" app/(markos)/settings/webhooks/\\[sub_id\\]/page.tsx` ≥ 1.
    - `grep -c "x-markos-signature-v2" app/(markos)/settings/webhooks/\\[sub_id\\]/page.tsx` ≥ 1.
    - `grep -c "This cannot be undone." app/(markos)/settings/webhooks/\\[sub_id\\]/page.tsx` ≥ 1.
    - `grep -c "#0d9488" app/(markos)/settings/webhooks/page.module.css` ≥ 3.
    - `grep -c "border-radius: 28px" app/(markos)/settings/webhooks/page.module.css` ≥ 1.
    - `grep -c "max-width: 1040px" app/(markos)/settings/webhooks/page.module.css` ≥ 1 (203-new convention #1).
    - `grep -c "border-radius: 16px" app/(markos)/settings/webhooks/\\[sub_id\\]/page.module.css` ≥ 1 (203-new convention #2 for nested secret panel).
    - `grep -cE "#0f172a.*#e2e8f0|#e2e8f0.*#0f172a" app/(markos)/settings/webhooks/\\[sub_id\\]/page.module.css` ≥ 1 (203-new convention #3 dark code block — OR two separate lines containing each: `grep -c "#0f172a" ...` ≥ 1 AND `grep -c "#e2e8f0" ...` ≥ 1).
    - `grep -c "outline: 2px solid #0d9488" app/(markos)/settings/webhooks/page.module.css` ≥ 1 (focus ring).
    - `grep -c "min-height: 44px" app/(markos)/settings/webhooks/page.module.css` ≥ 3.
    - `grep -c "prefers-reduced-motion" app/(markos)/settings/webhooks/page.module.css` ≥ 1.
    - Sidebar has "Webhooks" link (assertion in settings-ui-a11y.test.js based on grep of whichever file renders the sidebar nav).
  </acceptance_criteria>
  <done>Surface 1 + Surface 2 shipped with every UI-SPEC locked copy string + all token assertions green; 3 203-new conventions (1040px, 16px nested, dark code block) each documented + grep-verifiable; all 8 downstream plan APIs wired into the UI; sidebar entry added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant-admin → 5 new APIs | Headers x-markos-user-id + x-markos-tenant-id from Phase 201 middleware |
| S1+S2 DOM → URL → fetch chain | Client-driven mutation paths must re-hit server-side guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-203-09-01 | Elevation of Privilege | cross-tenant sub read/mutation via forged sub_id | mitigate | Every handler SELECTs subscription.tenant_id + compares to header tenant_id → 403 cross_tenant_forbidden; matches 202-09 pattern |
| T-203-09-02 | Information Disclosure | subscriptions list echoes secret column | mitigate | All handlers enumerate explicit return fields excluding secret + secret_v2; UI masks signature preview to last 8 chars |
| T-203-09-03 | Tampering | update bypasses SSRF guard (URL mutation) | mitigate | update.js re-runs assertUrlIsPublic on URL change; tests 1i + 1g cover |
| T-203-09-04 | Tampering | update raises rps_override above plan | mitigate | update.js re-runs resolvePerSubRps; 400 on violation (test 1h) |
| T-203-09-05 | Denial of Service | delete storm cancels in-flight | accept | Delete is admin-gated + tenant-scoped; cancelled deliveries stop but do not regenerate; no amplification |
| T-203-09-06 | Repudiation | subscription update/delete without audit | mitigate | Both endpoints emit enqueueAuditStaging rows (source_domain='webhooks', action='subscription.updated' / '.deleted') |
| T-203-09-07 | Information Disclosure | UI leaks signature via copy-curl | mitigate | Copy-cURL link copies a SANITIZED curl (signature NOT included per UI-SPEC Surface 2 action note) |
</threat_model>

<verification>
1. `node --test test/webhooks/settings-api.test.js test/webhooks/api-tenant.test.js test/webhooks/ui-s1-a11y.test.js test/webhooks/ui-s2-a11y.test.js test/webhooks/settings-ui-a11y.test.js test/openapi/openapi-build.test.js` — all green.
2. Full regression: `node --test test/webhooks/*.test.js test/audit/hash-chain.test.js` — all green.
3. F-96 merged into OpenAPI.
4. Manual (phase verifier): load /settings/webhooks in browser + verify 4 hero numbers + table + Create dialog + S2 tabs.
</verification>

<success_criteria>
- 5 new tenant APIs follow 202-09 pattern + cross-tenant guards.
- Surfaces 1 + 2 ship complete with UI-SPEC locked copy + tokens + a11y contract.
- All 3 203-new CSS conventions documented + grep-verifiable.
- Sidebar has "Webhooks" link alongside "MCP".
- F-96 declares 5 new paths in OpenAPI.
- Every CSS class traces to a 201 or 202 ancestor (or one of 3 documented new conventions) per UI-SPEC §Component Reuse Map.
- WCAG 2.2 AA maintained (44px targets + 2px focus ring + role=meter/tab/tabpanel/status/alert).
</success_criteria>

<output>
After completion, create `.planning/phases/203-webhook-subscription-engine-ga/203-09-SUMMARY.md` per `get-shit-done/templates/summary.md`.
</output>
