---
phase: 203
plan: 04
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - lib/markos/webhooks/signing.cjs
  - lib/markos/webhooks/signing.ts
  - lib/markos/webhooks/replay.cjs
  - lib/markos/webhooks/replay.ts
  - api/tenant/webhooks/subscriptions/[sub_id]/deliveries/[delivery_id]/replay.js
  - api/tenant/webhooks/subscriptions/[sub_id]/dlq/replay.js
  - contracts/F-73-webhook-delivery-v1.yaml
  - contracts/F-98-webhook-dlq-v1.yaml
  - test/webhooks/replay.test.js
  - test/webhooks/replay-idempotency.test.js
  - test/webhooks/signing.test.js
autonomous: true
requirements: [WHK-01, QA-01, QA-02, QA-05]

must_haves:
  truths:
    - "replaySingle(client, { tenant_id, subscription_id, delivery_id, actor_id }) inserts a NEW markos_webhook_deliveries row with replayed_from = original_id and enqueues it via queue.push"
    - "replayBatch(client, { tenant_id, subscription_id, delivery_ids, actor_id }) uses a server-generated batch_id and Vercel Queues idempotencyKey='replay-{delivery_id}-{5-min-bucket}' to prevent double-dispatch (RESEARCH §Pitfall 7)"
    - "Replay uses CURRENT signing secret + CURRENT timestamp via signPayload(sub.secret, body, Date.now) — original ts/sig NEVER preserved (D-06; prevents RESEARCH §Pitfall 3)"
    - "Replay adds outbound headers x-markos-replayed-from: {original_ts} AND x-markos-attempt: {n} (D-06)"
    - "POST /api/tenant/webhooks/subscriptions/{id}/deliveries/{delivery_id}/replay — single replay; tenant-scope + subscription-scope guard before accepting"
    - "POST /api/tenant/webhooks/subscriptions/{id}/dlq/replay — batch replay; body { delivery_ids: string[] }; rejects 400 when delivery_ids.length > 100 (batch cap)"
    - "Both endpoints emit audit rows (source_domain='webhooks', action='delivery.replay_single' or 'delivery.replay_batch') with payload { batch_id?, delivery_ids }"
    - "signPayloadDualSign(v1Secret, v2Secret, body) returns headers with x-markos-signature-v1 AND x-markos-signature-v2 (when v2Secret provided) — foundation reused by Plan 203-05 rotation"
    - "F-73 contract adds delivery columns replayed_from + dlq_at; F-98 contract declares the 2 replay paths and 409 already_replayed envelope"
  artifacts:
    - path: "lib/markos/webhooks/signing.cjs"
      provides: "signPayload (existing) + signPayloadDualSign (new)"
    - path: "lib/markos/webhooks/replay.cjs"
      provides: "replaySingle + replayBatch"
    - path: "api/tenant/webhooks/subscriptions/[sub_id]/deliveries/[delivery_id]/replay.js"
      provides: "POST handler with tenant-scope + Bearer-aware header-auth"
    - path: "api/tenant/webhooks/subscriptions/[sub_id]/dlq/replay.js"
      provides: "POST batch replay with idempotency"
    - path: "contracts/F-98-webhook-dlq-v1.yaml"
      provides: "OpenAPI declaration of replay endpoints + 409 envelope"
    - path: "contracts/F-73-webhook-delivery-v1.yaml"
      provides: "extended with replayed_from + dlq_at columns"
  key_links:
    - from: "api/tenant/webhooks/subscriptions/[sub_id]/deliveries/[delivery_id]/replay.js"
      to: "lib/markos/webhooks/replay.cjs replaySingle"
      via: "require + delegate after tenant-guard"
      pattern: "replaySingle"
    - from: "lib/markos/webhooks/replay.cjs"
      to: "lib/markos/webhooks/signing.cjs signPayload"
      via: "fresh HMAC at replay time"
      pattern: "signPayload\\("
    - from: "lib/markos/webhooks/replay.cjs"
      to: "store.queue.push"
      via: "idempotencyKey option for batch"
      pattern: "idempotencyKey"
---

<objective>
Ship Wave-2's DLQ **replay** path + the **signing foundation for Plan 203-05** (dual-sign). Two endpoints, one library, one contract + one extension.

1. `lib/markos/webhooks/signing.cjs` gains `signPayloadDualSign(v1Secret, v2Secret, body)` returning `{ headers: { X-Markos-Signature-V1, X-Markos-Signature-V2?, X-Markos-Timestamp } }` — when `v2Secret` is null (no active rotation), only V1 is signed. Plan 203-05 uses this at dispatch time; this plan just ships the signing primitive + unit tests.

2. `lib/markos/webhooks/replay.cjs` implements `replaySingle` + `replayBatch` per **D-06** (re-signs with fresh HMAC + CURRENT timestamp, adds `x-markos-replayed-from` + `x-markos-attempt` headers) — prevents the replay-attack oracle in RESEARCH §Pitfall 3. `replayBatch` uses Vercel Queues `idempotencyKey` keyed by 5-minute buckets to prevent double-dispatch (RESEARCH §Pitfall 7).

3. Two tenant-admin endpoints: `/api/tenant/webhooks/subscriptions/{id}/deliveries/{delivery_id}/replay` (single) + `/api/tenant/webhooks/subscriptions/{id}/dlq/replay` (batch). Both tenant-scope-guarded like `api/tenant/mcp/sessions/revoke.js` (202-09 pattern).

4. Contract updates: F-73 extended with delivery columns + headers; F-98 declares both replay endpoints + 409 already_replayed envelope.

Purpose: GA requires admin-triggered replay of failed deliveries (D-05 UI checkboxes); without this plan, the S2 DLQ tab has no handler wired.
Output: signing dual-sign fn + replay library + 2 tenant endpoints + F-98 contract + F-73 extension + 3 test suites.
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/203-webhook-subscription-engine-ga/203-CONTEXT.md
@.planning/phases/203-webhook-subscription-engine-ga/203-RESEARCH.md
@.planning/phases/203-webhook-subscription-engine-ga/203-UI-SPEC.md
@.planning/phases/203-webhook-subscription-engine-ga/203-01-PLAN.md
@.planning/phases/203-webhook-subscription-engine-ga/203-02-PLAN.md
@.planning/phases/203-webhook-subscription-engine-ga/203-03-PLAN.md
@.planning/phases/202-mcp-server-ga-claude-marketplace/202-09-SUMMARY.md

<interfaces>
<!-- Executor uses these verbatim. -->

From `lib/markos/webhooks/signing.cjs` (existing, 200-03):
```javascript
function signPayload(secret, body, nowFn = Date.now) {
  const timestamp = Math.floor(nowFn() / 1000);
  const sig = createHmac('sha256', secret).update(`${timestamp}.${body}`).digest('hex');
  return { signature: `sha256=${sig}`, timestamp };
}
```

NEW function shape (from 203-RESEARCH.md §Pattern 3 lines 349-365):
```javascript
function signPayloadDualSign(v1Secret, v2Secret, body, now = Date.now) {
  const { signature: sig1, timestamp } = signPayload(v1Secret, body, now);
  if (!v2Secret) return { headers: { 'X-Markos-Signature-V1': sig1, 'X-Markos-Timestamp': String(timestamp) } };
  const sig2 = `sha256=${createHmac('sha256', v2Secret).update(`${timestamp}.${body}`).digest('hex')}`;
  return { headers: { 'X-Markos-Signature-V1': sig1, 'X-Markos-Signature-V2': sig2, 'X-Markos-Timestamp': String(timestamp) } };
}
```

From Plan 203-01 store.cjs `{ subscriptions, deliveries, queue }`:
- `queue.push(delivery_id, options?)` accepts `{ idempotencyKey }` option → Vercel Queues dedups

From UI-SPEC locked copy (Surface 2 DLQ panel):
- Row-level replay dialog heading: `"Replay this delivery?"`
- Batch dialog: `"Replay {N} deliveries?"`
- Toast on success: `"Replay queued."` / `"Replay queued for {N} deliveries."`

From `api/tenant/mcp/sessions/revoke.js` (202-09 — mirror pattern verbatim for tenant-scope guard):
```javascript
// 1. method gate (POST only)
// 2. require x-markos-user-id + x-markos-tenant-id headers → 401 if missing
// 3. parse body
// 4. SELECT resource.tenant_id; if !== header tenant → 403 cross_tenant_forbidden
// 5. delegate to lib
// 6. writeJson response
```

From `lib/markos/crm/api.cjs` writeJson helper (used by 202-09 handlers).

From `contracts/F-95-mcp-cost-budget-v1.yaml` — YAML shape to mirror for F-98 (top-level summary + paths + errors + references).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: signPayloadDualSign + replay library + signing regression</name>
  <files>lib/markos/webhooks/signing.cjs, lib/markos/webhooks/signing.ts, lib/markos/webhooks/replay.cjs, lib/markos/webhooks/replay.ts, test/webhooks/signing.test.js, test/webhooks/replay.test.js</files>
  <read_first>
    - lib/markos/webhooks/signing.cjs (existing signPayload — extend, do not rewrite)
    - lib/markos/webhooks/engine.cjs (subscription row shape — secret column)
    - lib/markos/webhooks/delivery.cjs (attempt counter increment)
    - test/webhooks/signing.test.js (existing 8 tests — extend, do not replace)
    - .planning/phases/203-webhook-subscription-engine-ga/203-RESEARCH.md §Pattern 3 lines 340-380 + §Pitfall 3 + §Pitfall 7
    - .planning/phases/203-webhook-subscription-engine-ga/203-CONTEXT.md D-05, D-06, D-07
    - .planning/phases/203-webhook-subscription-engine-ga/203-UI-SPEC.md Surface 2 copy (replay dialog strings)
    - lib/markos/webhooks/dlq.cjs (Plan 203-03 listDLQ — replay.cjs uses it to fetch the DLQ row before creating a replay row)
    - lib/markos/webhooks/store.cjs (getWebhookStores for deliveries + queue)
  </read_first>
  <behavior>
    - Test 1a (signing extension): `signPayloadDualSign('secret1', null, 'body', () => 1700000000000)` returns `{ headers: { 'X-Markos-Signature-V1': 'sha256=...', 'X-Markos-Timestamp': '1700000000' } }` — NO V2 header when v2Secret null.
    - Test 1b: `signPayloadDualSign('secret1', 'secret2', 'body', () => 1700000000000)` returns headers with BOTH V1 AND V2 signatures AND shared Timestamp. V1 signature differs from V2.
    - Test 1c: V1 signature matches `signPayload('secret1', 'body', fixedNow).signature` verbatim (backward compatible).
    - Test 1d: Existing 8 signing.test.js tests still pass (regression).
    - Test 1e (replay): `replaySingle(client, queue, { tenant_id, subscription_id, delivery_id, actor_id, deps })` fetches the original delivery, confirms `status='failed'` AND `tenant_id` matches, inserts a NEW row with `replayed_from = original_id`, `status='pending'`, `attempt = 0`, `body` copied from original, and calls `queue.push(new_delivery.id)`.
    - Test 1f: replaySingle rejects with `Error('cross_tenant_forbidden')` when `original.tenant_id !== tenant_id`.
    - Test 1g: replaySingle rejects with `Error('not_failed')` when original `status !== 'failed'` (D-07: replay only from DLQ; preserves "terminal after 24" semantics).
    - Test 1h: replaySingle emits an audit row via `enqueueAuditStaging({ source_domain: 'webhooks', action: 'delivery.replay_single', tenant_id, actor_id, target_id: original_id, payload: { new_delivery_id, subscription_id } })`.
    - Test 1i (batch): `replayBatch(client, queue, { tenant_id, subscription_id, delivery_ids, actor_id, deps })` iterates delivery_ids, creates replay rows for each, and calls `queue.push(new_delivery.id, { idempotencyKey: \`replay-\${original_id}-\${Math.floor(now()/300000)}\` })`.
    - Test 1j: replayBatch returns `{ batch_id, count, replayed: [{ original_id, new_id }, ...] }` where `batch_id = \`batch_\${randomUUID()}\``.
    - Test 1k: replayBatch with delivery_ids.length > 100 throws `Error('batch_too_large')` (batch cap).
    - Test 1l: replayBatch with duplicate delivery_ids in one call deduplicates (each original_id replayed exactly once); the idempotencyKey's 5-minute bucket also prevents double-enqueue.
    - Test 1m: The replay row's `body` is fed through `signPayload(sub.secret, body, Date.now)` at dispatch — not in the insert; the DELIVERY.cjs worker re-signs per-attempt. Test asserts that `replaySingle` does NOT store a pre-signed blob; the delivery row stores raw `body` only, matching 200-03 convention. Signing happens at delivery.cjs dispatch via `signPayloadDualSign(secret_v1, secret_v2_or_null, body)` (Plan 203-05 wires this; Plan 203-04 confirms current `signPayload` works via signing.test.js regression).
    - Test 1n: The replay row has `attempt = 0` (fresh attempt counter), created_at = now, and headers `x-markos-replayed-from: {original_created_at}` + `x-markos-attempt: {n}` baked into the row's outbound header plan (delivery.cjs dispatch surface). Since attempt=0 is the 0th attempt of the replay, `x-markos-attempt: 1` will go on the wire first (matching 200-03 attempt counter).
  </behavior>
  <action>
    1. **Extend `lib/markos/webhooks/signing.cjs`** — add `signPayloadDualSign` function per §Pattern 3 lines 349-365 verbatim; update `module.exports` to add `signPayloadDualSign`. Existing `signPayload` untouched.

    2. **Mirror to `lib/markos/webhooks/signing.ts`** (dual-export parity).

    3. **Create `lib/markos/webhooks/replay.cjs`**:
       - `'use strict';`
       - `const { randomUUID } = require('node:crypto');`
       - `const { enqueueAuditStaging } = require('../audit/writer.cjs');`
       - `const BATCH_CAP = 100;`
       - `const IDEMPOTENCY_BUCKET_MS = 300_000;` (5 minutes)
       - `async function replaySingle(client, queue, { tenant_id, subscription_id, delivery_id, actor_id, deps = {} }) { ... }`:
         - Validate required params.
         - Fetch original delivery: `const { data: orig } = await client.from('markos_webhook_deliveries').select('*').eq('id', delivery_id).eq('tenant_id', tenant_id).maybeSingle();`
         - If !orig → throw `Error('not_found')`.
         - If orig.subscription_id !== subscription_id → throw `Error('cross_subscription')`.
         - If orig.tenant_id !== tenant_id → throw `Error('cross_tenant_forbidden')` (defense-in-depth; eq already filters, this is belt-and-suspenders).
         - If orig.status !== 'failed' → throw `Error('not_failed')` (D-07).
         - Insert new row: `{ id: \`del_\${randomUUID()}\`, tenant_id, subscription_id, event_type: orig.event_type, body: orig.body, status: 'pending', attempt: 0, replayed_from: orig.id, created_at: new Date().toISOString(), updated_at: new Date().toISOString() }`. `await client.from('markos_webhook_deliveries').insert(newRow).select().single()`.
         - `await queue.push(newRow.id);`
         - `await (deps.enqueueAuditStaging || enqueueAuditStaging)({ source_domain: 'webhooks', action: 'delivery.replay_single', tenant_id, actor_id, target_id: orig.id, payload: { new_delivery_id: newRow.id, subscription_id } }).catch(() => {});`
         - Return `{ original_id: orig.id, new_id: newRow.id }`.
       - `async function replayBatch(client, queue, { tenant_id, subscription_id, delivery_ids, actor_id, deps = {} })`:
         - Validate: throw `Error('batch_too_large')` if `delivery_ids.length > BATCH_CAP`.
         - Dedup: `const uniq = [...new Set(delivery_ids)]`.
         - `const batch_id = \`batch_\${randomUUID()}\`;`
         - `const results = [];`
         - For each `delivery_id` in uniq: call a shared helper that mirrors replaySingle body BUT calls `queue.push(newRow.id, { idempotencyKey: \`replay-\${orig.id}-\${Math.floor(Date.now() / IDEMPOTENCY_BUCKET_MS)}\` })`. Skip (do NOT throw) if status !== 'failed' or cross-tenant mismatch — collect skipped ids into `results.skipped`.
         - Emit single batch audit: `await enqueueAuditStaging({ source_domain: 'webhooks', action: 'delivery.replay_batch', tenant_id, actor_id, target_id: subscription_id, payload: { batch_id, delivery_ids: uniq, replayed_count: results.length, skipped_count: skipped.length } });`
         - Return `{ batch_id, count: results.length, replayed: results, skipped }`.
       - `module.exports = { replaySingle, replayBatch, BATCH_CAP, IDEMPOTENCY_BUCKET_MS };`

    4. **Mirror to `lib/markos/webhooks/replay.ts`** (dual-export).

    5. **Extend `test/webhooks/signing.test.js`** — add 3 new test cases (1a, 1b, 1c) while keeping all 8 existing tests green.

    6. **Write `test/webhooks/replay.test.js`** with 9 behaviors (1e-1n).
       - Use mock Supabase client + a mock queue `{ push: async (id, options) => captured.push({ id, options }) }`.
       - Inject mock `enqueueAuditStaging` via `deps.enqueueAuditStaging`.
  </action>
  <verify>
    <automated>node --test test/webhooks/signing.test.js test/webhooks/replay.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/webhooks/signing.test.js test/webhooks/replay.test.js` returns 0 (11 + 11 = 22 tests green including 8 regression).
    - `grep -c "signPayloadDualSign" lib/markos/webhooks/signing.cjs` ≥ 2 (defined + exported).
    - `grep -cE "X-Markos-Signature-V[12]" lib/markos/webhooks/signing.cjs` ≥ 2 (V1 always, V2 conditional).
    - `grep -c "BATCH_CAP = 100" lib/markos/webhooks/replay.cjs` = 1.
    - `grep -c "idempotencyKey" lib/markos/webhooks/replay.cjs` ≥ 1 (RESEARCH §Pitfall 7 mitigation).
    - `grep -cE "delivery\\.replay_(single|batch)" lib/markos/webhooks/replay.cjs` ≥ 2 (audit action names).
    - `grep -cE "(cross_tenant_forbidden|not_failed|batch_too_large|not_found)" lib/markos/webhooks/replay.cjs` ≥ 4 (all 4 typed error codes).
    - Dual-export: `ls lib/markos/webhooks/signing.{cjs,ts} lib/markos/webhooks/replay.{cjs,ts}` = 4 found.
  </acceptance_criteria>
  <done>Dual-sign foundation ready for Plan 203-05; replay library enforces D-06 (fresh HMAC + current ts), D-07 (replay only from failed), RESEARCH §Pitfall 7 (idempotencyKey on batch); 22 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Two tenant-admin replay endpoints + F-98 contract + F-73 extension + idempotency suite</name>
  <files>api/tenant/webhooks/subscriptions/[sub_id]/deliveries/[delivery_id]/replay.js, api/tenant/webhooks/subscriptions/[sub_id]/dlq/replay.js, contracts/F-98-webhook-dlq-v1.yaml, contracts/F-73-webhook-delivery-v1.yaml, test/webhooks/replay-idempotency.test.js</files>
  <read_first>
    - api/tenant/mcp/sessions/revoke.js (202-09 — tenant-scope guard pattern; MIRROR verbatim)
    - api/tenant/mcp/sessions/list.js (202-09 — method gate + header auth)
    - contracts/F-95-mcp-cost-budget-v1.yaml (YAML shape for F-98)
    - contracts/F-73-webhook-delivery-v1.yaml (existing 200-03 contract — extend)
    - lib/markos/webhooks/replay.cjs (Plan 203-04 Task 1 shape)
    - lib/markos/webhooks/store.cjs (getWebhookStores for queue access)
    - .planning/phases/203-webhook-subscription-engine-ga/203-UI-SPEC.md Surface 2 DLQ panel copy (api response shapes must align with UI toast strings)
    - .planning/phases/202-mcp-server-ga-claude-marketplace/202-09-SUMMARY.md key-decisions (401 vs 403 vs 404 semantics)
  </read_first>
  <behavior>
    - Test 2a: POST single replay without `x-markos-user-id` → 401 `{ error: 'unauthorized' }`.
    - Test 2b: POST single replay with valid headers + cross-tenant subscription_id → 403 `{ error: 'cross_tenant_forbidden' }`.
    - Test 2c: POST single replay with valid headers + valid delivery (status='failed') → 200 `{ ok: true, original_id, new_id }`.
    - Test 2d: POST single replay with delivery.status='delivered' → 409 `{ error: 'not_failed' }` (D-07).
    - Test 2e: POST batch replay with body `{ delivery_ids: [] }` → 400 `{ error: 'empty_batch' }`.
    - Test 2f: POST batch replay with delivery_ids.length > 100 → 400 `{ error: 'batch_too_large' }`.
    - Test 2g: POST batch replay happy path → 200 `{ ok: true, batch_id, count, replayed, skipped }`.
    - Test 2h: Batch replay called twice within 5 minutes with same delivery_ids produces the same `idempotencyKey` for each queue.push (asserts Vercel Queues would dedupe at the queue layer).
    - Test 2i: F-98 YAML parses and has 2 paths declared (`/api/tenant/webhooks/subscriptions/{sub_id}/deliveries/{delivery_id}/replay`, `/api/tenant/webhooks/subscriptions/{sub_id}/dlq/replay`) + 409 error envelope.
    - Test 2j: F-73 YAML has `replayed_from` + `dlq_at` added to delivery schema.
    - Test 2k: `test/openapi/openapi-build.test.js` still green (F-98 glob picked up by `scripts/openapi/build-openapi.cjs`).
  </behavior>
  <action>
    1. **Create `api/tenant/webhooks/subscriptions/[sub_id]/deliveries/[delivery_id]/replay.js`** — mirror `api/tenant/mcp/sessions/revoke.js` structure:
       - Method gate: POST only; 405 else.
       - Headers: require `x-markos-user-id` + `x-markos-tenant-id`; 401 if missing.
       - Path params: `const { sub_id, delivery_id } = req.query;` (Next.js dynamic route — params delivered via query).
       - `const { getWebhookStores } = require(...)` + admin Supabase client via dep-injected path (or direct createClient with service-role).
       - SELECT subscription to verify `tenant_id` match: `SELECT tenant_id FROM markos_webhook_subscriptions WHERE id = sub_id` → if row.tenant_id !== header tenant → 403 `{ error: 'cross_tenant_forbidden' }`.
       - Try: `const result = await replaySingle(supabase, stores.queue, { tenant_id, subscription_id: sub_id, delivery_id, actor_id: user_id });`.
       - Catch specific error codes and map: `not_found` → 404, `cross_tenant_forbidden` → 403, `not_failed` → 409, `cross_subscription` → 400. Generic catch → 500 `{ error: 'replay_failed' }`.
       - writeJson(res, 200, { ok: true, ...result }).

    2. **Create `api/tenant/webhooks/subscriptions/[sub_id]/dlq/replay.js`**:
       - Method gate + header auth same as above.
       - Path params: `{ sub_id }`.
       - Body parse: `{ delivery_ids }`. If `!Array.isArray(delivery_ids) || delivery_ids.length === 0` → 400 `{ error: 'empty_batch' }`.
       - Subscription tenant guard (SELECT subscription + cross-tenant check).
       - Try replayBatch delegate.
       - Catch `batch_too_large` → 400; generic → 500.
       - 200 `{ ok: true, batch_id, count, replayed, skipped }`.

    3. **Create `contracts/F-98-webhook-dlq-v1.yaml`** — mirror F-95 YAML shape:
       - `flow_id: F-98`, `title: Webhook DLQ Replay`, `version: v1`
       - `paths:` block with 2 path items as documented above; each with method: post, requestBody schemas, 200/400/403/404/409/500 responses.
       - `errors:` block describing `not_failed` (409) + `cross_tenant_forbidden` (403) + `batch_too_large` (400) + `empty_batch` (400) envelopes.
       - `references:` block citing F-73 (delivery model), 203-04-PLAN.md, RESEARCH §Pitfall 7 (idempotency).

    4. **Extend `contracts/F-73-webhook-delivery-v1.yaml`** — add under the `delivery` schema:
       - `replayed_from: { type: string, nullable: true, description: 'Original delivery_id if this row is a replay' }`
       - `dlq_at: { type: string, format: date-time, nullable: true, description: 'Set when delivery transitions to status=failed after 24-attempt cap' }`
       - `final_attempt: { type: integer, nullable: true }`
       - `dlq_reason: { type: string, nullable: true }`
       - Also document the outbound headers `x-markos-signature-v1`, `x-markos-signature-v2`, `x-markos-replayed-from`, `x-markos-attempt` in the `outbound_headers` block of the contract.

    5. **Regenerate OpenAPI**: run `node scripts/openapi/build-openapi.cjs` to merge F-98 + F-73 updates into `contracts/openapi.json` + `contracts/openapi.yaml`. Validate via existing `test/openapi/openapi-build.test.js`.

    6. **Write `test/webhooks/replay-idempotency.test.js`**:
       - Covers behaviors 2a-2h via direct handler invocation (dep-injection pattern same as Plan 203-03 Task 2).
       - For 2h (idempotencyKey stability): call batch replay twice within a 5-minute window using the same time-injection; assert captured `queue.push` calls carry identical `idempotencyKey` strings for each original delivery_id across both batch invocations.
       - 2i-2j: parse the F-98 + F-73 YAML with `js-yaml` and assert path/schema presence.
  </action>
  <verify>
    <automated>node --test test/webhooks/replay-idempotency.test.js test/openapi/openapi-build.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/webhooks/replay-idempotency.test.js test/openapi/openapi-build.test.js` returns 0.
    - `grep -c "cross_tenant_forbidden" api/tenant/webhooks/subscriptions/\\[sub_id\\]/deliveries/\\[delivery_id\\]/replay.js` ≥ 1.
    - `grep -c "cross_tenant_forbidden" api/tenant/webhooks/subscriptions/\\[sub_id\\]/dlq/replay.js` ≥ 1.
    - `grep -c "batch_too_large" api/tenant/webhooks/subscriptions/\\[sub_id\\]/dlq/replay.js` ≥ 1.
    - `grep -c "F-98" contracts/F-98-webhook-dlq-v1.yaml` ≥ 1.
    - `grep -cE "/api/tenant/webhooks/subscriptions/\\{sub_id\\}/(deliveries/\\{delivery_id\\}/replay|dlq/replay)" contracts/F-98-webhook-dlq-v1.yaml` ≥ 2 (both paths declared).
    - `grep -c "replayed_from" contracts/F-73-webhook-delivery-v1.yaml` ≥ 1.
    - `grep -c "dlq_at" contracts/F-73-webhook-delivery-v1.yaml` ≥ 1.
    - `grep -cE "x-markos-replayed-from|x-markos-attempt" contracts/F-73-webhook-delivery-v1.yaml` ≥ 2.
    - `contracts/openapi.json` regenerated and test/openapi/openapi-build.test.js green with F-98 paths present (at least 2 new paths registered).
    - Full Wave-2: `node --test test/webhooks/*.test.js` all green.
  </acceptance_criteria>
  <done>2 tenant-admin endpoints gated by 202-09 auth pattern; batch idempotency via Vercel Queues key-bucket; F-98 contract declares 4 error envelopes; F-73 extended with 4 new delivery fields + outbound headers; OpenAPI regenerated without drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant-admin → replay endpoints | Untrusted admin action flows into queue enqueue |
| replay library → Vercel Queues | Duplicate replay requests could fan-out |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-203-04-01 | Tampering | replay path preserving original signature | mitigate | D-06 enforces fresh HMAC + current ts at replay; signing.cjs does NOT expose a "reuse-old-ts" option; test 1m asserts body is stored raw, signed at dispatch |
| T-203-04-02 | Elevation of Privilege | cross-tenant replay | mitigate | Handler SELECTs subscription.tenant_id BEFORE delegating; replaySingle re-checks original.tenant_id (defense-in-depth per 202-09 pattern); tests 2b + 1f cover |
| T-203-04-03 | Denial of Service | batch replay 10_000 ids | mitigate | BATCH_CAP = 100; handler returns 400 batch_too_large; test 2f + 1k cover |
| T-203-04-04 | Tampering | double-dispatch via rapid-click | mitigate | Vercel Queues idempotencyKey keyed on (original_id, 5-min bucket); RESEARCH §Pitfall 7; test 2h asserts key stability |
| T-203-04-05 | Repudiation | replay without audit trail | mitigate | replaySingle + replayBatch both emit markos_audit_log rows with source_domain='webhooks'; actor_id captured from x-markos-user-id header |
| T-203-04-06 | Information Disclosure | body content leak in F-98 OpenAPI | accept | Body is opaque event payload owned by tenant; OpenAPI references its shape by ref to F-73 delivery schema; no secrets inline |
</threat_model>

<verification>
1. `node --test test/webhooks/signing.test.js test/webhooks/replay.test.js test/webhooks/replay-idempotency.test.js test/openapi/openapi-build.test.js` — all green.
2. Regression: Wave-1 + Wave-2 full pass: `node --test test/webhooks/*.test.js` all green.
3. OpenAPI merged spec includes F-98 paths + F-73 extensions.
</verification>

<success_criteria>
- D-06 replay semantics shipped (fresh ts + current HMAC + replayed-from + attempt headers).
- D-07 enforced (replay only from status='failed'; no auto-retry).
- RESEARCH §Pitfall 7 mitigated (batch idempotencyKey).
- 2 tenant-admin endpoints follow 202-09 cross-tenant guard pattern.
- F-73 + F-98 contracts merge cleanly into OpenAPI; openapi-build tests pass.
- signPayloadDualSign ready for Plan 203-05 dispatch wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/203-webhook-subscription-engine-ga/203-04-SUMMARY.md` per `get-shit-done/templates/summary.md`.
</output>
