---
phase: 108-industry-overlay-packs
plan: 07
type: execute
wave: 3
depends_on: [108-01, 108-02, 108-03, 108-04, 108-05, 108-06]
files_modified: []
autonomous: false
requirements: [LIB-02, LIB-04]

must_haves:
  truths:
    - "All 25 tests pass: Suite 106 (14) + Suite 108 (11) — zero failures"
    - "npm test (full suite) exits green"
    - "All 4 tone docs have correct D-04 overlay_for values confirmed by grep"
    - "Each of the 4 verticals has exactly 5 PROMPTS.md files with 4 standalone prompts each"
    - "No PROMPTS.md references external base family files (prompts are standalone per D-07)"
    - "All 4 .industry.json manifests have type: overlay and non-empty overlayFor arrays"
  artifacts:
    - path: "lib/markos/packs/industries/travel.industry.json"
      provides: "Travel overlay manifest"
      contains: '"type": "overlay"'
    - path: "lib/markos/packs/industries/it.industry.json"
      provides: "IT overlay manifest"
      contains: '"type": "overlay"'
    - path: "lib/markos/packs/industries/marketing-services.industry.json"
      provides: "Marketing Services overlay manifest"
      contains: '"type": "overlay"'
    - path: "lib/markos/packs/industries/professional-services.industry.json"
      provides: "Professional Services overlay manifest"
      contains: '"type": "overlay"'
  key_links:
    - from: "test/pack-loader.test.js Suite 108"
      to: "lib/markos/packs/industries/*.industry.json"
      via: "resolvePackSelection() — fs.existsSync + INDUSTRY_ALIAS_MAP"
      pattern: "108\\.(0[1-9]|10|11)"
    - from: ".agent/markos/literacy/Shared/TPL-SHARED-overlay-industry-*.md"
      to: "lib/markos/packs/industries/*.industry.json"
      via: "assets.overlayDoc field"
      pattern: "overlayDoc.*TPL-SHARED-overlay-industry"
---

<objective>
Final verification of all Phase 108 deliverables before the phase is marked complete.

Purpose: Confirm all 4 industry overlay packs are correctly constructed — tests pass, overlay_for values match D-04 locked decisions, PROMPTS.md files are standalone per D-07, and manifests are schema-compliant. This plan has no file modifications; it is a verification checkpoint only.

Output: Confirmed green test suite and a human-reviewed content spot-check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/108-industry-overlay-packs/108-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and verify 25 tests pass</name>
  <files></files>
  <read_first>
    - test/pack-loader.test.js — review Suite 108 block to understand what each assertion tests before running
  </read_first>
  <action>
    Run the pack-loader test file:
    ```
    node --test test/pack-loader.test.js
    ```

    Expected outcome:
    - Suite 106 (14 tests): all pass — getFamilyRegistry and resolvePackSelection base family behaviors unchanged
    - Suite 108 (11 tests): all pass — resolvePackSelection returns type:'overlay' for all 4 overlay slugs; getFamilyRegistry still returns 7

    If any test fails:
    1. Read the failing test assertion (which file/slug it expects to exist)
    2. Check whether the corresponding .industry.json file exists in lib/markos/packs/industries/
    3. If the file is missing: re-run 108-06 plan tasks to create it
    4. If the file exists but the test still fails: read the exact assertion; confirm the file slug in INDUSTRY_ALIAS_MAP matches the slug in the .industry.json filename
    5. Do NOT modify pack-loader.cjs unless a genuine loader bug is identified and confirmed against test failure output

    After pack-loader tests pass, run the full npm test suite:
    ```
    npm test
    ```

    Full suite must exit 0 (zero failures). If a previously-passing test regresses, diagnose before proceeding — do not mark the phase complete with a failing test.
  </action>
  <verify>
    node --test test/pack-loader.test.js 2>&1 | Select-String "pass|fail|error" | Tail -5
    # Must show 25 pass, 0 fail
    npm test
    # Must exit 0
  </verify>
  <acceptance_criteria>
    - `node --test test/pack-loader.test.js` exits 0 with 25 tests passing, 0 failing
    - `npm test` exits 0 (full suite green)
    - No previously-passing test has regressed
  </acceptance_criteria>
  <done>
    Full test suite green. 25 tests confirmed (14 Suite 106 + 11 Suite 108). npm test exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Spot-check tone doc overlay_for values against D-04</name>
  <files></files>
  <read_first>
    - .planning/phases/108-industry-overlay-packs/108-CONTEXT.md — D-04 locked overlay_for values for all 4 slugs
  </read_first>
  <action>
    Run grep checks for all 4 tone docs to verify D-04 overlay_for values are correct:

    ```
    grep "overlay_for" .agent/markos/literacy/Shared/TPL-SHARED-overlay-industry-travel.md
    grep "overlay_for" .agent/markos/literacy/Shared/TPL-SHARED-overlay-industry-it.md
    grep "overlay_for" .agent/markos/literacy/Shared/TPL-SHARED-overlay-industry-marketing-services.md
    grep "overlay_for" .agent/markos/literacy/Shared/TPL-SHARED-overlay-industry-professional-services.md
    ```

    Expected values per D-04:
    - travel: `overlay_for: ["b2c", "b2b"]`
    - it: `overlay_for: ["b2b", "saas", "services"]`
    - marketing-services: `overlay_for: ["agency", "b2b"]`
    - professional-services: `overlay_for: ["services", "b2b"]`

    Also confirm each tone doc has the correct `industry` key (not `business_model`):
    ```
    grep "^industry:" .agent/markos/literacy/Shared/TPL-SHARED-overlay-industry-travel.md
    grep "^industry:" .agent/markos/literacy/Shared/TPL-SHARED-overlay-industry-it.md
    grep "^industry:" .agent/markos/literacy/Shared/TPL-SHARED-overlay-industry-marketing-services.md
    grep "^industry:" .agent/markos/literacy/Shared/TPL-SHARED-overlay-industry-professional-services.md
    ```

    Expected: `industry: ["travel"]`, `industry: ["it"]`, `industry: ["marketing-services"]`, `industry: ["professional-services"]`
    None should return `business_model`.

    If any overlay_for value does not match D-04: fix the tone doc directly — do not create a new plan. D-04 values are locked and non-negotiable.
  </action>
  <verify>
    grep "overlay_for" .agent/markos/literacy/Shared/TPL-SHARED-overlay-industry-travel.md
    # Must return: overlay_for: ["b2c", "b2b"]
    grep "overlay_for" .agent/markos/literacy/Shared/TPL-SHARED-overlay-industry-professional-services.md
    # Must return: overlay_for: ["services", "b2b"]
  </verify>
  <acceptance_criteria>
    - travel tone doc: `overlay_for: ["b2c", "b2b"]`
    - it tone doc: `overlay_for: ["b2b", "saas", "services"]`
    - marketing-services tone doc: `overlay_for: ["agency", "b2b"]`
    - professional-services tone doc: `overlay_for: ["services", "b2b"]`
    - All 4 tone docs use `industry:` key (not `business_model:`)
    - D-06 thin delta constraint present in professional-services tone doc: `grep -i "thin delta" .agent/markos/literacy/Shared/TPL-SHARED-overlay-industry-professional-services.md` returns 1+ match
  </acceptance_criteria>
  <done>
    All 4 tone docs confirmed with correct D-04 overlay_for values and industry key. Professional Services tone doc contains D-06 thin delta language.
  </done>
</task>

<task type="auto">
  <name>Task 3: Spot-check PROMPTS.md standalone integrity per D-07</name>
  <files></files>
  <action>
    Verify the 20 PROMPTS.md files (4 verticals × 5 disciplines) exist and contain standalone prompts.

    1. Confirm file counts:
    ```
    (Get-ChildItem "onboarding/templates/SKELETONS/industries/travel" -Recurse -Filter "PROMPTS.md").Count
    (Get-ChildItem "onboarding/templates/SKELETONS/industries/it" -Recurse -Filter "PROMPTS.md").Count
    (Get-ChildItem "onboarding/templates/SKELETONS/industries/marketing-services" -Recurse -Filter "PROMPTS.md").Count
    (Get-ChildItem "onboarding/templates/SKELETONS/industries/professional-services" -Recurse -Filter "PROMPTS.md").Count
    ```
    Each must return 5.

    2. Spot-check H1 format for one PROMPTS.md per vertical (verify discipline display name):
    ```
    Select-String "^# " "onboarding/templates/SKELETONS/industries/travel/Paid_Media/PROMPTS.md"
    # Must match: # Travel & Hospitality — Paid Media Industry Overlay Prompts

    Select-String "^# " "onboarding/templates/SKELETONS/industries/it/Content_SEO/PROMPTS.md"
    # Must match: # IT & Technology — Content & SEO Industry Overlay Prompts

    Select-String "^# " "onboarding/templates/SKELETONS/industries/marketing-services/Lifecycle_Email/PROMPTS.md"
    # Must match: # Marketing Services — Lifecycle & Email Industry Overlay Prompts

    Select-String "^# " "onboarding/templates/SKELETONS/industries/professional-services/Social/PROMPTS.md"
    # Must match: # Professional Services — Social Industry Overlay Prompts
    ```

    3. Check prompt count in one PROMPTS.md per vertical (each must have exactly 4 `### N.` headings):
    ```
    (Select-String "^### \d+\." "onboarding/templates/SKELETONS/industries/travel/Social/PROMPTS.md").Count
    # Must return 4

    (Select-String "^### \d+\." "onboarding/templates/SKELETONS/industries/professional-services/Landing_Pages/PROMPTS.md").Count
    # Must return 4
    ```

    4. Check no cross-references to base family files exist in any overlay PROMPTS.md:
    ```
    Get-ChildItem "onboarding/templates/SKELETONS/industries" -Recurse -Filter "PROMPTS.md" |
      Select-String "see base|refer to base|base pack|TPL-SHARED-business-model" |
      Select-Object -ExpandProperty Path
    ```
    Must return no results (empty). D-07 requires all prompts to be fully self-contained.

    If any check fails, fix the affected PROMPTS.md file directly before marking this task complete.
  </action>
  <verify>
    Get-ChildItem "onboarding/templates/SKELETONS/industries" -Recurse -Filter "PROMPTS.md" | Measure-Object | Select-Object -ExpandProperty Count
    # Must return 20
  </verify>
  <acceptance_criteria>
    - 20 PROMPTS.md files total across all 4 vertical skeleton directories (5 per vertical)
    - Each spot-checked H1 matches the "{Vertical} — {Discipline} Industry Overlay Prompts" format exactly
    - Each spot-checked PROMPTS.md has exactly 4 `### N.` headings
    - `grep -ri "see base\|refer to base\|base pack\|TPL-SHARED-business-model" "onboarding/templates/SKELETONS/industries/"` returns no results
  </acceptance_criteria>
  <done>
    20 PROMPTS.md files confirmed. H1 format correct per vertical/discipline. 4 standalone prompts per file. No cross-references to base family files (D-07 standalone constraint verified).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 108 Industry Overlay Packs — complete:
    - lib/markos/packs/industries/ directory with 4 .industry.json manifests (travel, it, marketing-services, professional-services)
    - 4 tone overlay docs in .agent/markos/literacy/Shared/
    - 20 PROMPTS.md skeleton files across 4 vertical directories in onboarding/templates/SKELETONS/industries/
    - Suite 108 tests (11 tests) appended to test/pack-loader.test.js — all passing
    - Full test suite: 25 tests pass, 0 fail
  </what-built>
  <how-to-verify>
    1. Run `node --test test/pack-loader.test.js` — confirm 25 tests pass
    2. Open one PROMPTS.md from any vertical/discipline — confirm it reads as a standalone prompt set without needing base pack context
    3. Optionally: open one .industry.json manifest and confirm it has type: "overlay" and a correct overlayFor array
    4. Optionally: spot-check that professional-services tone doc body mentions "thin delta" (D-06 documented)
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 108, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 108 complete when:
1. `node --test test/pack-loader.test.js` → 25 pass, 0 fail
2. `npm test` → exits 0
3. All 4 tone docs have correct D-04 overlay_for values
4. All 20 PROMPTS.md files exist (5 per vertical × 4 verticals)
5. No PROMPTS.md cross-references base family pack files
6. Human approves via checkpoint
</verification>

<success_criteria>
Phase 108 fully verified. 25 tests pass. 4 overlay manifests in industries/ directory. 20 standalone PROMPTS.md files across 4 verticals. D-04 overlay_for values confirmed. D-06 thin delta constraint documented in professional-services tone doc. D-07 standalone prompts confirmed (no base family cross-references).
</success_criteria>

<output>
After completion, create `.planning/phases/108-industry-overlay-packs/108-07-SUMMARY.md`
</output>
