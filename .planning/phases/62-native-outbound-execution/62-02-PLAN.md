---
phase: 62-native-outbound-execution
plan: 02
type: execute
wave: 2
depends_on:
  - 62-01
  - 60-pipeline-engine-and-multi-view-workspace
  - 61-sales-and-success-execution-workspace
files_modified:
  - contracts/F-62-outbound-templates-v1.yaml
  - contracts/F-62-outbound-sequences-v1.yaml
  - supabase/migrations/62_crm_outbound_execution.sql
  - lib/markos/outbound/scheduler.ts
  - api/crm/outbound/templates.js
  - api/crm/outbound/sequences.js
  - api/crm/outbound/bulk-send.js
  - app/(markos)/crm/outbound/page.tsx
  - components/crm/outbound/outbound-workspace.tsx
  - components/crm/outbound/outbound-composer.tsx
  - components/crm/outbound/outbound-consent-gate.tsx
  - test/crm-outbound/crm-outbound-workspace.test.js
  - test/crm-outbound/crm-sequence-approval.test.js
  - test/crm-outbound/crm-bulk-send-guardrails.test.js
autonomous: true
requirements:
  - CRM-05
  - CRM-06

must_haves:
  truths:
    - "Phase 62 Wave 2 turns the outbound foundation into a CRM-native operator workspace rather than a detached messaging tool or route-local compose drawer."
    - "Templates, basic sequences, scheduled sends, and bulk-like execution remain explainable and approval-aware, with low-risk one-off actions staying faster than governed high-risk flows."
    - "The workspace reuses the existing execution-shell grammar and CRM record context instead of inventing a second inbox or campaign-only product surface."
  artifacts:
    - path: "supabase/migrations/62_crm_outbound_execution.sql"
      provides: "Tenant-safe execution schema for templates, sequence definitions, sequence steps, scheduled queue entries, and bulk-send lineage."
      exports: []
    - path: "contracts/F-62-outbound-templates-v1.yaml"
      provides: "Contract for reusable templates, channel constraints, variable substitution shape, approval state, and brand-context integration."
      exports: []
    - path: "contracts/F-62-outbound-sequences-v1.yaml"
      provides: "Contract for first-pass sequence definitions, step timing, approval requirements, and bulk or scheduled execution semantics."
      exports: []
    - path: "lib/markos/outbound/scheduler.ts"
      provides: "Shared scheduler and queue helpers for due-send selection, approval-aware execution, and retry-safe sequencing."
      exports:
        - "selectDueOutboundWork"
        - "buildSequenceExecutionPlan"
    - path: "api/crm/outbound/templates.js"
      provides: "Tenant-safe template read and write API seam for outbound workspace surfaces."
      exports: []
    - path: "api/crm/outbound/sequences.js"
      provides: "Tenant-safe sequence definition and lifecycle API seam with approval-aware transitions."
      exports: []
    - path: "api/crm/outbound/bulk-send.js"
      provides: "Governed bulk-send and scheduled-send API seam with eligibility checks, approval paths, and execution lineage."
      exports: []
    - path: "app/(markos)/crm/outbound/page.tsx"
      provides: "CRM-native outbound execution workspace using queue, compose or thread, and evidence regions."
      exports: []
    - path: "components/crm/outbound/outbound-composer.tsx"
      provides: "Shared operator composer with channel picker, template support, scheduling controls, and approval-aware warnings."
      exports:
        - "OutboundComposer"
  key_links:
    - from: "app/(markos)/crm/outbound/page.tsx"
      to: "app/(markos)/operations/tasks/page.tsx"
      via: "outbound execution reuses the existing three-region execution-shell grammar rather than inventing a detached messaging product shell"
      pattern: "queue|evidence|task|panel|drawer"
    - from: "api/crm/outbound/sequences.js"
      to: "lib/markos/outbound/consent.ts"
      via: "sequence launches and high-risk steps inherit channel-specific eligibility and approval escalation behavior"
      pattern: "evaluateOutboundEligibility|approval|required|sequence"
    - from: "lib/markos/outbound/scheduler.ts"
      to: "api/crm/outbound/send.js"
      via: "scheduled and sequence-driven execution reuses the same governed send seam instead of bypassing one-off policy checks"
      pattern: "send|approval|provider|channel|tenant"
---

<objective>
Create the CRM-native outbound execution workspace and orchestration layer for Phase 62.

Purpose: satisfy the practical operator surface of CRM-05 and CRM-06 by turning the outbound foundation into reusable templates, basic sequences, scheduling, bulk-safe execution, and a shared outbound workspace inside the CRM shell.
Output: template and sequence contracts, scheduled and bulk execution schema, approval-aware APIs, a CRM-native outbound workspace, and tests covering workspace behavior, sequence governance, and bulk-send guardrails.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/62-native-outbound-execution/62-CONTEXT.md
@.planning/phases/62-native-outbound-execution/62-RESEARCH.md
@.planning/phases/62-native-outbound-execution/62-01-PLAN.md
@app/(markos)/operations/tasks/page.tsx
@lib/markos/outbound/consent.ts
@api/crm/outbound/send.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 62-02-01: Create failing workspace, sequence-governance, and bulk-send guardrail tests</name>
  <read_first>
    - .planning/phases/62-native-outbound-execution/62-CONTEXT.md
    - .planning/phases/62-native-outbound-execution/62-RESEARCH.md
    - app/(markos)/operations/tasks/page.tsx
    - test/crm-execution/crm-execution-workspace.test.js
  </read_first>
  <files>test/crm-outbound/crm-outbound-workspace.test.js, test/crm-outbound/crm-sequence-approval.test.js, test/crm-outbound/crm-bulk-send-guardrails.test.js</files>
  <acceptance_criteria>
    - Workspace tests prove queue, compose or detail, and evidence regions stay inside the CRM shell and expose channel, template, and approval context clearly.
    - Sequence tests prove governed launches, approval-aware high-risk steps, and scheduled execution semantics are explicit rather than implicit background behavior.
    - Bulk-send tests prove eligibility filtering, approval routing, and per-message lineage stay visible and fail closed on ambiguous policy.
  </acceptance_criteria>
  <action>
Write the failing regression boundary first. The tests must force a CRM-native execution workspace and governed orchestration behavior rather than allowing Phase 62 to drift into a template library with hidden bulk dispatch.
  </action>
  <verify>
    <automated>node --test test/crm-outbound/crm-outbound-workspace.test.js test/crm-outbound/crm-sequence-approval.test.js test/crm-outbound/crm-bulk-send-guardrails.test.js</automated>
  </verify>
  <done>
Wave 2 begins with explicit failing evidence for workspace structure, sequence governance, and bulk-send guardrails.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 62-02-02: Implement template, sequence, scheduling, and bulk execution schema plus API contracts</name>
  <read_first>
    - .planning/phases/62-native-outbound-execution/62-RESEARCH.md
    - lib/markos/outbound/consent.ts
    - api/crm/outbound/send.js
  </read_first>
  <files>supabase/migrations/62_crm_outbound_execution.sql, contracts/F-62-outbound-templates-v1.yaml, contracts/F-62-outbound-sequences-v1.yaml, lib/markos/outbound/scheduler.ts, api/crm/outbound/templates.js, api/crm/outbound/sequences.js, api/crm/outbound/bulk-send.js, test/crm-outbound/crm-sequence-approval.test.js, test/crm-outbound/crm-bulk-send-guardrails.test.js</files>
  <acceptance_criteria>
    - The schema defines reusable templates, first-pass sequence definitions, scheduled queue entries, and per-message lineage for bulk-like execution.
    - Contracts make approval thresholds, step timing, template variables, and scheduled-send semantics explicit.
    - Sequence and bulk execution reuse governed eligibility and send helpers rather than creating a second orchestration path.
  </acceptance_criteria>
  <action>
Add the execution layer as a narrow, governed extension of Wave 1. Keep the first pass practical: reusable templates, basic step cadences, scheduled sends, and bulk-safe execution with clear audit and approval boundaries.
  </action>
  <verify>
    <automated>node --test test/crm-outbound/crm-sequence-approval.test.js test/crm-outbound/crm-bulk-send-guardrails.test.js --test-name-pattern="sequence|template|schedule|bulk|approval|eligibility|lineage"</automated>
  </verify>
  <done>
The repo now has explicit contracts and schema for reusable outbound execution patterns beyond one-off sends.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 62-02-03: Build the CRM-native outbound workspace on the existing execution-shell pattern</name>
  <read_first>
    - app/(markos)/operations/tasks/page.tsx
    - .planning/phases/62-native-outbound-execution/62-01-PLAN.md
    - .planning/phases/62-native-outbound-execution/62-RESEARCH.md
  </read_first>
  <files>app/(markos)/crm/outbound/page.tsx, components/crm/outbound/outbound-workspace.tsx, components/crm/outbound/outbound-composer.tsx, components/crm/outbound/outbound-consent-gate.tsx, test/crm-outbound/crm-outbound-workspace.test.js</files>
  <acceptance_criteria>
    - The workspace uses a queue or worklist region, an active compose or sequence pane, and an evidence rail for consent, approval, and delivery context.
    - Composer surfaces support channel selection, template application, scheduling, and governed warnings without hiding policy state.
    - The workspace remains visibly CRM-native and record-aware rather than presenting a parallel messaging product shell.
  </acceptance_criteria>
  <action>
Implement the operator-facing outbound workspace using the established execution grammar. Preserve the existing MarkOS shell, keep evidence visible, and make high-risk flows obviously approval-aware instead of burying them in backend-only logic.
  </action>
  <verify>
    <automated>node --test test/crm-outbound/crm-outbound-workspace.test.js --test-name-pattern="workspace|composer|consent|evidence|schedule|template|record"</automated>
  </verify>
  <done>
Wave 2 ends with a CRM-native outbound workspace ready for live conversations, inbound writeback, and telemetry closure.
  </done>
</task>

</tasks>

<verification>
Run the workspace, sequence-governance, and bulk-send guardrail suites after each task; Wave 2 is incomplete until templates, sequences, scheduling, and compose surfaces are explicit, governed, and CRM-native.
</verification>

<success_criteria>
Phase 62 has a practical outbound workspace where operators can compose, template, schedule, and govern one-off or basic sequence execution without leaving the CRM shell.
</success_criteria>

<output>
After completion, create `.planning/phases/62-native-outbound-execution/62-02-SUMMARY.md`
</output>
