---
phase: 62-native-outbound-execution
plan: 01
type: execute
wave: 1
depends_on:
  - 58-crm-canonical-schema-and-identity-graph
  - 59-behavioral-tracking-and-identity-stitching
  - 60-pipeline-engine-and-multi-view-workspace
  - 61-sales-and-success-execution-workspace
  - 51-multi-tenant-foundation-and-authorization
files_modified:
  - contracts/F-62-outbound-send-v1.yaml
  - contracts/F-62-outbound-consent-v1.yaml
  - supabase/migrations/62_crm_outbound_foundation.sql
  - lib/markos/outbound/providers/base-adapter.ts
  - lib/markos/outbound/providers/resend-adapter.ts
  - lib/markos/outbound/providers/twilio-adapter.ts
  - lib/markos/outbound/consent.ts
  - lib/markos/rbac/policies.ts
  - api/crm/outbound/send.js
  - test/crm-outbound/crm-outbound-foundation.test.js
  - test/crm-outbound/crm-consent-eligibility.test.js
  - test/tenant-auth/crm-outbound-tenant-isolation.test.js
autonomous: true
requirements:
  - CRM-05
  - CRM-06

must_haves:
  truths:
    - "Phase 62 starts by creating an explicit outbound schema, provider-adapter layer, and channel-specific consent gates instead of hiding real send behavior behind UI-only actions or generic webhook glue."
    - "Every outbound send path is tenant-scoped, IAM-gated, and CRM-native from the start, with provider references and audit lineage preserved rather than treated as detached messaging metadata."
    - "Email, SMS, and WhatsApp all exist as real first-pass execution channels in Wave 1, even though richer template and sequence depth can expand later in the phase."
  artifacts:
    - path: "supabase/migrations/62_crm_outbound_foundation.sql"
      provides: "Tenant-safe outbound foundation schema for sends, consent state, provider references, scheduling metadata, and approval-aware execution lifecycle fields."
      exports: []
    - path: "contracts/F-62-outbound-send-v1.yaml"
      provides: "Contract for one-off send requests, provider response normalization, channel selection, approval-aware exceptions, and CRM writeback expectations."
      exports: []
    - path: "contracts/F-62-outbound-consent-v1.yaml"
      provides: "Contract for per-channel consent, eligibility, opt-out behavior, fail-closed ambiguity handling, and approval escalation triggers."
      exports: []
    - path: "lib/markos/outbound/providers/base-adapter.ts"
      provides: "Shared MarkOS outbound provider interface covering send, status normalization, capability metadata, and webhook event normalization."
      exports:
        - "sendOutboundMessage"
        - "normalizeOutboundProviderEvent"
        - "getOutboundChannelCapabilities"
    - path: "lib/markos/outbound/providers/resend-adapter.ts"
      provides: "Resend-backed email adapter for one-off send execution and provider event normalization."
      exports:
        - "createResendAdapter"
    - path: "lib/markos/outbound/providers/twilio-adapter.ts"
      provides: "Twilio-backed SMS and WhatsApp adapter for send execution and delivery-event normalization."
      exports:
        - "createTwilioAdapter"
    - path: "lib/markos/outbound/consent.ts"
      provides: "Shared consent and eligibility helper for per-channel policy checks, ambiguity handling, and opt-out state writes."
      exports:
        - "evaluateOutboundEligibility"
        - "recordOutboundOptOut"
    - path: "api/crm/outbound/send.js"
      provides: "Tenant-safe one-off send API that enforces channel policy, permission checks, provider dispatch, and CRM activity writeback."
      exports: []
  key_links:
    - from: "api/crm/outbound/send.js"
      to: "lib/markos/crm/api.cjs"
      via: "outbound send execution inherits tenant context, mutation gates, and append-only CRM activity behavior"
      pattern: "requireCrmTenantContext|assertCrmMutationAllowed|appendCrmActivity"
    - from: "supabase/migrations/62_crm_outbound_foundation.sql"
      to: "supabase/migrations/58_crm_activity_and_identity.sql"
      via: "outbound records preserve provider refs and consent lineage while writing normalized outcomes back into the existing outbound_event ledger family"
      pattern: "crm_activity_ledger|outbound_event|tenant_id|source_event_ref"
    - from: "lib/markos/outbound/consent.ts"
      to: "lib/markos/rbac/policies.ts"
      via: "channel-specific approval escalation and send permissions remain governed through the existing IAM bridge rather than ad hoc UI checks"
      pattern: "ACTION_POLICY|execute_task|approve_task|publish_campaign"
---

<objective>
Create the outbound foundation for Phase 62.

Purpose: satisfy the execution and governance base of CRM-05 and CRM-06 by introducing real channel adapters, explicit outbound persistence, and channel-specific eligibility gates before building richer template, sequence, and conversation surfaces.
Output: outbound schema and contracts, Resend and Twilio-backed adapters, per-channel consent helpers, tenant-safe send APIs, IAM extensions, and tests covering send normalization, eligibility, and tenant isolation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/62-native-outbound-execution/62-CONTEXT.md
@.planning/phases/62-native-outbound-execution/62-RESEARCH.md
@package.json
@lib/markos/crm/api.cjs
@lib/markos/crm/timeline.ts
@lib/markos/plugins/brand-context.js
@lib/markos/rbac/policies.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 62-01-01: Create failing outbound foundation, eligibility, and tenant-isolation tests</name>
  <read_first>
    - .planning/phases/62-native-outbound-execution/62-CONTEXT.md
    - .planning/phases/62-native-outbound-execution/62-RESEARCH.md
    - test/crm-api/crm-api-contracts.test.js
    - test/tenant-auth/crm-tenant-isolation.test.js
  </read_first>
  <files>test/crm-outbound/crm-outbound-foundation.test.js, test/crm-outbound/crm-consent-eligibility.test.js, test/tenant-auth/crm-outbound-tenant-isolation.test.js</files>
  <acceptance_criteria>
    - Tests encode real email, SMS, and WhatsApp send attempts, provider response normalization, consent ambiguity, and approval-escalation behavior as deterministic requirements.
    - Negative-path coverage proves cross-tenant send access, missing consent, and ungoverned high-risk execution fail closed.
    - The test boundary makes it impossible to satisfy Phase 62 with mock-only provider shells or a single generic consent flag.
  </acceptance_criteria>
  <action>
Write the failing regression boundary first. The tests must force a real outbound foundation with per-channel policy, provider normalization, and tenant-safe execution rather than allowing the phase to collapse into decorative compose UI.
  </action>
  <verify>
    <automated>node --test test/crm-outbound/crm-outbound-foundation.test.js test/crm-outbound/crm-consent-eligibility.test.js test/tenant-auth/crm-outbound-tenant-isolation.test.js</automated>
  </verify>
  <done>
Wave 1 begins with explicit failing evidence for real channel execution, consent safety, and tenant isolation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 62-01-02: Implement outbound schema, provider adapters, and channel-specific consent contracts</name>
  <read_first>
    - package.json
    - lib/markos/crm/api.cjs
    - lib/markos/plugins/brand-context.js
    - .planning/phases/62-native-outbound-execution/62-RESEARCH.md
  </read_first>
  <files>supabase/migrations/62_crm_outbound_foundation.sql, contracts/F-62-outbound-send-v1.yaml, contracts/F-62-outbound-consent-v1.yaml, lib/markos/outbound/providers/base-adapter.ts, lib/markos/outbound/providers/resend-adapter.ts, lib/markos/outbound/providers/twilio-adapter.ts, lib/markos/outbound/consent.ts, test/crm-outbound/crm-outbound-foundation.test.js, test/crm-outbound/crm-consent-eligibility.test.js</files>
  <acceptance_criteria>
    - The schema defines tenant-scoped outbound send records, scheduling placeholders, consent or eligibility state, provider refs, and approval lifecycle fields.
    - Contracts make one-off send semantics, provider normalization shape, and per-channel eligibility rules explicit.
    - All three first-pass channels are real through adapter implementations, with email allowed to carry richer metadata without demoting SMS or WhatsApp to stubs.
  </acceptance_criteria>
  <action>
Add the outbound foundation as an additive extension of the existing CRM model. Keep provider-specific translation inside adapters, keep consent channel-aware, and preserve white-label payload shaping through the existing brand-context seam.
  </action>
  <verify>
    <automated>node --test test/crm-outbound/crm-outbound-foundation.test.js test/crm-outbound/crm-consent-eligibility.test.js --test-name-pattern="send|provider|email|sms|whatsapp|consent|eligibility|opt-out"</automated>
  </verify>
  <done>
The repo now has a governed outbound foundation with real provider paths and explicit consent posture.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 62-01-03: Add tenant-safe one-off send APIs and outbound IAM policy extensions with audit hooks</name>
  <read_first>
    - lib/markos/crm/api.cjs
    - lib/markos/rbac/policies.ts
    - api/crm/activities.js
  </read_first>
  <files>api/crm/outbound/send.js, lib/markos/rbac/policies.ts, lib/markos/crm/api.cjs, test/crm-outbound/crm-outbound-foundation.test.js, test/tenant-auth/crm-outbound-tenant-isolation.test.js</files>
  <acceptance_criteria>
    - One-off send APIs enforce tenant context, outbound-specific action policies, channel eligibility, and approval escalation consistently with existing CRM handlers.
    - Every send request writes audit-safe CRM activity with provider refs and channel metadata through the canonical ledger.
    - API responses are deterministic enough for later workspace surfaces to consume without hidden client-side send logic.
  </acceptance_criteria>
  <action>
Implement the first outbound execution seam on top of the shared CRM boundary. Do not introduce a second auth path or a detached messaging API model that bypasses canonical CRM activity logging.
  </action>
  <verify>
    <automated>node --test test/crm-outbound/crm-outbound-foundation.test.js test/tenant-auth/crm-outbound-tenant-isolation.test.js --test-name-pattern="api|send|tenant|forbidden|approval|audit|outbound"</automated>
  </verify>
  <done>
Wave 1 ends with governed one-off send execution ready for richer workspace layers.
  </done>
</task>

</tasks>

<verification>
Run the outbound foundation, eligibility, and tenant-isolation suites after each task; Wave 1 is incomplete until real provider execution, channel-safe eligibility, and CRM-native audit behavior are explicit and test-backed.
</verification>

<success_criteria>
Phase 62 has a concrete outbound foundation where email, SMS, and WhatsApp are real execution channels, consent is channel-specific, and one-off sends write governed evidence into canonical CRM history.
</success_criteria>

<output>
After completion, create `.planning/phases/62-native-outbound-execution/62-01-SUMMARY.md`
</output>
