---
phase: 62-native-outbound-execution
plan: 03
type: execute
wave: 3
depends_on:
  - 62-01
  - 62-02
  - 59-behavioral-tracking-and-identity-stitching
files_modified:
  - contracts/F-62-outbound-conversations-v1.yaml
  - api/webhooks/resend-events.js
  - api/webhooks/twilio-events.js
  - lib/markos/outbound/events.ts
  - lib/markos/outbound/conversations.ts
  - lib/markos/telemetry/events.ts
  - app/(markos)/crm/outbound/conversations/page.tsx
  - components/crm/outbound/conversation-viewer.tsx
  - test/crm-outbound/crm-conversation-writeback.test.js
  - test/crm-outbound/crm-provider-webhook-normalization.test.js
  - test/crm-outbound/crm-outbound-telemetry.test.js
  - test/crm-outbound/crm-assisted-draft-boundary.test.js
autonomous: true
requirements:
  - CRM-05
  - CRM-06

must_haves:
  truths:
    - "Phase 62 closes by making outbound state visible as living CRM history through conversation threads, inbound handling, and provider-event writeback rather than leaving delivery or reply truth outside MarkOS."
    - "Webhook ingestion and conversation handling normalize provider-specific events into one CRM-native outbound_event model with durable sequence-step and provider-ref lineage."
    - "Lightweight assisted drafting stays operator-in-the-loop and clearly bounded so Phase 62 does not drift into the autonomous AI workflows owned by Phase 63."
  artifacts:
    - path: "contracts/F-62-outbound-conversations-v1.yaml"
      provides: "Contract for conversation threads, inbound normalization, reply attribution, opt-out outcomes, and CRM-visible thread state."
      exports: []
    - path: "api/webhooks/resend-events.js"
      provides: "Webhook normalization seam for email delivery, bounce, open or click where available, and inbound reply evidence."
      exports: []
    - path: "api/webhooks/twilio-events.js"
      provides: "Webhook normalization seam for SMS and WhatsApp deliveries, inbound replies, and opt-out outcomes."
      exports: []
    - path: "lib/markos/outbound/events.ts"
      provides: "Shared normalization helpers that convert provider events into CRM-native outbound and conversation events."
      exports:
        - "normalizeOutboundEventForLedger"
        - "buildConversationStateUpdate"
    - path: "lib/markos/outbound/conversations.ts"
      provides: "Conversation-thread helpers for read models, reply attribution, unresolved-state transitions, and record-aware thread assembly."
      exports:
        - "buildOutboundConversation"
        - "appendInboundConversationEvent"
    - path: "lib/markos/telemetry/events.ts"
      provides: "Expanded telemetry vocabulary for outbound compose, send, block, approval, reply, and opt-out instrumentation with payload sanitization preserved."
      exports:
        - "buildEvent"
    - path: "app/(markos)/crm/outbound/conversations/page.tsx"
      provides: "Conversation workspace route for active outbound threads, delivery evidence, and reply handling inside the CRM shell."
      exports: []
    - path: "components/crm/outbound/conversation-viewer.tsx"
      provides: "Thread viewer for inbound and outbound history, delivery markers, opt-out state, and operator follow-up actions."
      exports:
        - "ConversationViewer"
  key_links:
    - from: "api/webhooks/resend-events.js"
      to: "lib/markos/crm/timeline.ts"
      via: "email delivery and reply events normalize into outbound_event history visible through the canonical CRM timeline builder"
      pattern: "outbound_event|buildCrmTimeline|activity_family|occurred_at"
    - from: "api/webhooks/twilio-events.js"
      to: "lib/markos/outbound/conversations.ts"
      via: "SMS and WhatsApp inbound or delivery events update conversation threads and unresolved-state surfaces from one normalized handler path"
      pattern: "reply|conversation|thread|status|opt-out"
    - from: "lib/markos/telemetry/events.ts"
      to: "components/crm/outbound/outbound-composer.tsx"
      via: "compose, block, approval, and assist-draft instrumentation stays product-telemetry-safe while CRM persistence retains operational truth"
      pattern: "sanitizePayload|outbound|compose|approval|reply"
---

<objective>
Close Phase 62 with conversation handling, webhook normalization, outbound telemetry, and bounded draft-assist behavior.

Purpose: satisfy the durable-history and operator-workflow closeout of CRM-05 and CRM-06 by making replies, deliveries, failures, opt-outs, and conversation threads first-class CRM-visible outcomes while keeping AI assistance strictly non-autonomous.
Output: conversation contracts, provider webhook routes, normalized outbound event helpers, conversation workspace surfaces, outbound telemetry expansion, and tests covering writeback, webhook normalization, telemetry, and draft-assist boundaries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/62-native-outbound-execution/62-CONTEXT.md
@.planning/phases/62-native-outbound-execution/62-RESEARCH.md
@.planning/phases/62-native-outbound-execution/62-02-PLAN.md
@lib/markos/crm/timeline.ts
@lib/markos/telemetry/events.ts
@api/crm/outbound/send.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 62-03-01: Create failing conversation, webhook-normalization, telemetry, and draft-boundary tests</name>
  <read_first>
    - .planning/phases/62-native-outbound-execution/62-CONTEXT.md
    - .planning/phases/62-native-outbound-execution/62-RESEARCH.md
    - lib/markos/crm/timeline.ts
    - lib/markos/telemetry/events.ts
  </read_first>
  <files>test/crm-outbound/crm-conversation-writeback.test.js, test/crm-outbound/crm-provider-webhook-normalization.test.js, test/crm-outbound/crm-outbound-telemetry.test.js, test/crm-outbound/crm-assisted-draft-boundary.test.js</files>
  <acceptance_criteria>
    - Conversation tests prove replies, deliveries, failures, and opt-outs land back in CRM-visible history and thread state.
    - Webhook tests prove Resend and Twilio events normalize into one outbound_event and conversation model with provider refs preserved.
    - Telemetry and draft-boundary tests prove outbound instrumentation is explicit and sanitized while AI assistance never initiates autonomous execution.
  </acceptance_criteria>
  <action>
Write the failing regression boundary first. The tests must make it impossible to close Phase 62 with provider webhooks that update only raw tables, with no CRM-visible conversation truth or with AI assistance leaking into autonomous send behavior.
  </action>
  <verify>
    <automated>node --test test/crm-outbound/crm-conversation-writeback.test.js test/crm-outbound/crm-provider-webhook-normalization.test.js test/crm-outbound/crm-outbound-telemetry.test.js test/crm-outbound/crm-assisted-draft-boundary.test.js</automated>
  </verify>
  <done>
Wave 3 begins with explicit failing evidence for conversation truth, webhook normalization, outbound telemetry, and phase-boundary discipline.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 62-03-02: Implement provider webhook normalization and CRM-native conversation writeback</name>
  <read_first>
    - lib/markos/crm/timeline.ts
    - lib/markos/crm/api.cjs
    - lib/markos/outbound/providers/resend-adapter.ts
    - lib/markos/outbound/providers/twilio-adapter.ts
  </read_first>
  <files>contracts/F-62-outbound-conversations-v1.yaml, api/webhooks/resend-events.js, api/webhooks/twilio-events.js, lib/markos/outbound/events.ts, lib/markos/outbound/conversations.ts, test/crm-outbound/crm-conversation-writeback.test.js, test/crm-outbound/crm-provider-webhook-normalization.test.js</files>
  <acceptance_criteria>
    - Resend and Twilio event handlers normalize provider-specific events into durable CRM writeback and conversation state transitions.
    - Replies, opt-outs, and delivery or failure markers remain tied to record context, channel, provider ref, and sequence-step lineage when present.
    - Conversation helpers expose one coherent record-aware thread model for the workspace to render.
  </acceptance_criteria>
  <action>
Implement inbound and delivery normalization as CRM-owned behavior. Provider webhooks should supply evidence, but the canonical truth must remain normalized outbound_event history plus conversation-thread state inside MarkOS.
  </action>
  <verify>
    <automated>node --test test/crm-outbound/crm-conversation-writeback.test.js test/crm-outbound/crm-provider-webhook-normalization.test.js --test-name-pattern="conversation|reply|delivery|bounce|opt-out|sequence|provider|timeline"</automated>
  </verify>
  <done>
The repo now has real conversation and provider-event return paths that land back in CRM truth.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 62-03-03: Add conversation workspace surfaces, outbound telemetry, and bounded assist-draft behavior</name>
  <read_first>
    - app/(markos)/crm/outbound/page.tsx
    - lib/markos/telemetry/events.ts
    - .planning/phases/62-native-outbound-execution/62-CONTEXT.md
  </read_first>
  <files>lib/markos/telemetry/events.ts, app/(markos)/crm/outbound/conversations/page.tsx, components/crm/outbound/conversation-viewer.tsx, test/crm-outbound/crm-outbound-telemetry.test.js, test/crm-outbound/crm-assisted-draft-boundary.test.js</files>
  <acceptance_criteria>
    - Conversation surfaces expose inbound or outbound thread continuity, delivery evidence, and follow-up context inside the CRM shell.
    - Outbound product telemetry covers compose, send, block, approval, reply, and opt-out flows with sanitization preserved.
    - Draft assistance remains explicit, optional, and operator-triggered, with no autonomous planning or send execution path added.
  </acceptance_criteria>
  <action>
Finish the phase by making conversation and telemetry behavior visible in the operator experience while preserving the hard boundary against Phase 63 agentic workflows.
  </action>
  <verify>
    <automated>node --test test/crm-outbound/crm-outbound-telemetry.test.js test/crm-outbound/crm-assisted-draft-boundary.test.js --test-name-pattern="telemetry|compose|approval|reply|assist|draft|autonomous|boundary"</automated>
  </verify>
  <done>
Wave 3 ends with CRM-visible conversation handling, outbound instrumentation, and a clean stop line before Phase 63.
  </done>
</task>

</tasks>

<verification>
Run the conversation, webhook-normalization, telemetry, and draft-boundary suites after each task; Wave 3 is incomplete until outbound outcomes are CRM-visible, product telemetry is explicit, and the phase boundary against autonomous AI remains intact.
</verification>

<success_criteria>
Phase 62 has a complete first-pass outbound loop where operators can send across three real channels, manage conversations inside the CRM, and trust that delivery, reply, opt-out, and approval evidence are visible without drifting into Phase 63 autonomy.
</success_criteria>

<output>
After completion, create `.planning/phases/62-native-outbound-execution/62-03-SUMMARY.md`
</output>
