---
phase: 224
plan: "06"
type: execute
wave: 4
depends_on: ["224-01", "224-02", "224-03", "224-04", "224-05"]
files_modified:
  # Migrations 128 + 132 (per D-56) — runbooks + cron state + runbook execute trigger (D-66 / RH9 part 2)
  - supabase/migrations/128_launch_runbooks.sql
  - supabase/migrations/132_launch_cron_state_and_execute_trigger.sql
  # Runbook executor + step handlers + rollback + idempotency
  - lib/markos/launches/runbook/executor.ts
  - lib/markos/launches/runbook/step-handlers.ts
  - lib/markos/launches/runbook/rollback.ts
  - lib/markos/launches/runbook/idempotency.ts
  - lib/markos/launches/runbook/index.ts
  # Cron handlers
  - lib/markos/launches/cron/surface-health-audit.ts
  - lib/markos/launches/cron/gate-evaluation-poll.ts
  - lib/markos/launches/cron/outcome-computation.ts
  - lib/markos/launches/cron/bounce-spike-alert.ts
  - lib/markos/launches/cron/index.ts
  - lib/markos/launches/index.ts
  # API endpoints (legacy *.js per D-42 / RH4)
  - api/v1/launches/runbooks.js
  - api/v1/launches/runbooks-arm.js
  - api/v1/launches/runbooks-execute.js
  - api/v1/launches/runbooks-rollback.js
  # Cron entry points (legacy *.js)
  - api/crons/launches-surface-health-audit.js
  - api/crons/launches-gate-evaluation-poll.js
  - api/crons/launches-outcome-computation.js
  - api/crons/launches-bounce-spike-alert.js
  - vercel.json
  # Contracts
  - contracts/F-142-launch-runbook-v1.yaml
  - contracts/flow-registry.json
  - contracts/openapi.json
  # Tests (Node --test, *.test.js per D-61)
  - test/launches/schema/launch-runbook.test.js
  - test/launches/rls/launch-runbooks.rls.test.js
  - test/launches/triggers/runbook-execute-required.test.js
  - test/launches/runbook/executor-happy-path.test.js
  - test/launches/runbook/executor-rollback.test.js
  - test/launches/runbook/executor-idempotency.test.js
  - test/launches/runbook/agentrun-upstream-gate.test.js
  - test/launches/runbook/rollback-partial.test.js
  - test/launches/cron/surface-health-audit.test.js
  - test/launches/cron/gate-evaluation-poll.test.js
  - test/launches/cron/outcome-computation-cron.test.js
  - test/launches/cron/bounce-spike.test.js
  - test/launches/api/runbooks-api.test.js
  - .planning/phases/224-conversion-launch-workspace/224-VALIDATION.md
autonomous: true
requirements:
  - LCH-01
  - LCH-02
  - LCH-04
  - LCH-05
  - CNV-04
  - QA-01
  - QA-02
must_haves:
  truths:
    - "launch_runbooks (migration 128) completes the 13-table P224 SOR; tenant-scoped, RLS-enforced (D-12, D-51)"
    - "Migration 132 ships D-66 / RH9 launch_runbook_execute_required trigger: BEFORE UPDATE on launch_runbooks REJECTS state transition to 'executing' if any launch_gates row for the same launch_id has status='blocking'. Mirrors D-65 enforcement-boundary doctrine — service-role + alternative-API write paths cannot bypass."
    - "Runbook executor runs steps sequentially honoring depends_on DAG; each step idempotent via step.idempotency_key (D-35); retry returning same key skips re-execution"
    - "Reverse-runbook rollback executes rollback_steps in REVERSE order (D-36); reversible:true steps reversed; reversible:false steps emit operator task + continue (Pitfall 6 — never abort entire rollback)"
    - "Forward-execution of unavailable step kinds (post_social, custom) emits operator task via markos_tasks insert + advances current_step + CONTINUES with rest of runbook (mirrors non-reversible rollback pattern from D-36); runbook only fails if a critical step kind (publish_surface, dispatch_email_campaign, send_messaging, notify_team) errors"
    - "Runbook execution wrapped in AgentRun (P207) — assertUpstreamReady(['P207']) at executor entry per D-37 + D-60 + RH6. Hard-fail with UpstreamPhaseNotLandedError if AgentRun substrate absent. NO bridge stub (per D-59 / RH6 — runbook IS launch execution; soft-skip breaks audit chain D-49)."
    - "ISR cache invalidation on rollback: when a rollback step archives a conversion_page surface, invalidatePageCache called SYNCHRONOUSLY before the step is marked complete (Pitfall 3 — same rule as Plan 03 publish, applied to rollback)"
    - "All step kinds (publish_surface, dispatch_email_campaign, send_messaging, post_social, notify_team, custom) have step-handler implementation; missing handler → operator task + continue (NOT abort)"
    - "Migration 132 ships cron state tables (cron_run_log + bounce_alert_baselines + runbook_step_log) AND vercel.json gains 4 cron entries: surface_health_audit (daily 4 UTC), gate_evaluation_poll (every 15 min), outcome_computation (daily 6 UTC), bounce_spike_alert (every 1h) per D-48..D-50"
    - "F-142 launch-runbook-v1 contract ships in THIS plan only (was deferred from Plan 01). F-145 launch-evaluate-gates + F-146 launch-execute-runbook MCP contracts are deferred to Plan 07 Task 1 (which owns MCP contracts per its frontmatter); Plan 06 does NOT modify F-145/F-146."
    - "All approval-aware mutations use buildApprovalPackage from lib/markos/crm/agent-actions.ts:68 (D-58); approval entry types: runbook_arm, runbook_execute, rollback (D-20, D-46)"
    - "Rollback path emits markos_audit_log row per step (event_type='runbook_rollback_step') with reversible flag + operator_task_id when reversible:false; final state='rolled_back' with partial_rollback_blockers field listing non-reversible steps"
    - "Surface health audit cron scans published ConversionPages for stale pricing_context_id + stale evidence_pack_id → operator task + email"
    - "Bounce/spike alert cron detects conversion rate drop >2σ from 7-day baseline AND BotID abuse spike >10× baseline → tenant-admin alert via Approval Inbox"
    - "API endpoints + cron handlers ship as legacy api/v1/launches/runbooks*.js + api/crons/launches-*.js (D-42 / RH4); auth wrapper requireHostedSupabaseAuth (D-42)"
  artifacts:
    - path: supabase/migrations/128_launch_runbooks.sql
      provides: launch_runbooks table + RLS + indexes (launch_id, state, agentrun_id); composite UNIQUE on (runbook_id, idempotency_key) for step-replay protection
    - path: supabase/migrations/132_launch_cron_state_and_execute_trigger.sql
      provides: cron_run_log + bounce_alert_baselines + runbook_step_log tables; D-66 / RH9 launch_runbook_execute_required BEFORE UPDATE trigger
    - path: lib/markos/launches/runbook/executor.ts
      provides: executeRunbook({runbook_id, tenant_id}) — assertUpstreamReady(['P207']); sequential step execution; AgentRun wrapper; per-step audit; resume from current_step
    - path: lib/markos/launches/runbook/step-handlers.ts
      provides: 6 step kind handlers (publish_surface, dispatch_email_campaign, send_messaging, post_social, notify_team, custom); each idempotent
    - path: lib/markos/launches/runbook/rollback.ts
      provides: rollbackRunbook({runbook_id, tenant_id}) — reverse-order rollback; non-reversible → operator task + continue (Pitfall 6); ISR invalidation for archived pages
    - path: lib/markos/launches/runbook/idempotency.ts
      provides: checkStepIdempotency(runbook_id, step_id, key) — prevents re-execution within retry window
    - path: lib/markos/launches/cron/surface-health-audit.ts
      provides: scanPublishedSurfacesForStaleness() — checks pricing_context_id + evidence_pack_id; creates operator tasks
    - path: lib/markos/launches/cron/gate-evaluation-poll.ts
      provides: pollInflightLaunches() — every 15 min runs evaluateAllLaunchGates for launches in-flight
    - path: lib/markos/launches/cron/outcome-computation.ts
      provides: triggerOutcomeComputation() — daily; identifies launches at T+7/T+14/T+30 and calls computeLaunchOutcome
    - path: lib/markos/launches/cron/bounce-spike-alert.ts
      provides: detectBounceAndSpikes() — 24h conversion rate vs 7-day baseline; tenant-admin alert
    - path: api/v1/launches/runbooks*.js (4 files)
      provides: full runbook CRUD + 3 lifecycle endpoints (arm, execute, rollback) all approval-aware (D-58)
    - path: vercel.json
      provides: 4 new cron entries with cadence per D-48..D-50
    - path: contracts/F-142-launch-runbook-v1.yaml
      provides: F-142 active contract — runbook CRUD + arm + execute + rollback paths
  key_links:
    - from: lib/markos/launches/runbook/executor.ts
      to: lib/markos/conversion/preflight/upstream-gate.ts
      via: assertUpstreamReady(['P207']) at executor entry; throws UpstreamPhaseNotLandedError if AgentRun absent. NO bridge stub (D-59 / RH6).
    - from: lib/markos/launches/runbook/step-handlers.ts
      to: lib/markos/launches/runbook/step-handlers.ts
      via: dispatcher routes step.kind → handler implementation
    - from: lib/markos/launches/runbook/rollback.ts
      to: lib/markos/conversion/render/cache-tags.ts
      via: rollback handler for publish_surface kind calls invalidatePageCache for conversion_page surfaces (Pitfall 3)
    - from: supabase/migrations/132_launch_cron_state_and_execute_trigger.sql
      to: launch_runbooks.state transitions
      via: D-66 / RH9 trigger rejects 'executing' transition if blocking gates exist
---

<objective>
Ship the launch execution + rollback + cron substrate: launch_runbooks SOR (slot 128), cron state + D-66 / RH9 runbook execute DB trigger (slot 132), step executor with hard-fail AgentRun gating (D-37 + D-60 + RH6), reverse-runbook rollback (Pitfall 6), 4 legacy `*.js` API endpoints, 4 legacy `*.js` cron handlers + vercel.json entries (D-48..D-50), F-142 contract only (F-145 + F-146 deferred to Plan 07 Task 1 closeout).

Purpose: satisfy LCH-01..02 + LCH-04..05 (governed runbook + rollback + post-launch monitoring), CNV-04, QA-01/QA-02. Resolves RH6 (AgentRun hard-fail), RH9 part 2 (runbook execute DB trigger), RH4 / D-42 (legacy *.js API + cron).

Output: 2 migrations + 5 runbook lib files + 5 cron lib files + 4 API endpoints + 4 cron handlers + vercel.json + 3 contracts + 13 test files. Plan 06 runs in Wave 4.
</objective>

<context>
@.planning/phases/224-conversion-launch-workspace/224-CONTEXT.md
@.planning/phases/224-conversion-launch-workspace/224-RESEARCH.md
@.planning/phases/224-conversion-launch-workspace/224-VALIDATION.md
@.planning/phases/224-conversion-launch-workspace/224-REVIEWS.md
@.planning/phases/224-conversion-launch-workspace/224-01-PLAN.md
@.planning/phases/224-conversion-launch-workspace/224-02-PLAN.md
@.planning/phases/224-conversion-launch-workspace/224-03-PLAN.md
@.planning/phases/224-conversion-launch-workspace/224-04-PLAN.md
@.planning/phases/224-conversion-launch-workspace/224-05-PLAN.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@lib/markos/crm/agent-actions.ts
@onboarding/backend/runtime-context.cjs
@lib/markos/conversion/preflight/upstream-gate.ts
@lib/markos/conversion/render/cache-tags.ts
@vercel.json
</context>

<interfaces>
From `lib/markos/launches/contracts/runbook.ts` (Plan 01):
```ts
export interface LaunchRunbook {
  runbook_id: string;
  tenant_id: string;
  launch_id: string;
  steps: RunbookStep[];
  rollback_steps: RunbookStep[];
  state: 'draft' | 'armed' | 'executing' | 'executed' | 'rolling_back' | 'rolled_back' | 'failed';
  current_step: number;
  agentrun_id?: string;
  // ...
}
export interface RunbookStep {
  step_id: string;
  name: string;
  kind: 'publish_surface' | 'dispatch_email_campaign' | 'send_messaging' | 'post_social' | 'notify_team' | 'custom';
  target_ref: string;
  depends_on: string[];
  idempotency_key: string;
  reversible?: boolean;
}
```

From `lib/markos/conversion/preflight/upstream-gate.ts` (Plan 01):
```ts
export function assertUpstreamReady(required: readonly string[]): void;
```

From `lib/markos/launches/gates/evaluate-all.ts` (Plan 04):
```ts
export async function evaluateAllLaunchGates(input, supabase): Promise<{gates, can_publish}>;
```

From `lib/markos/launches/outcomes/compute.ts` (Plan 04):
```ts
export async function computeLaunchOutcome(input, supabase): Promise<LaunchOutcome>;
```

From `lib/markos/conversion/render/cache-tags.ts` (Plan 03):
```ts
export async function invalidatePageCache(tenant_id: string, page_id: string): Promise<void>;
```

From P207 AgentRun (upstream-owned, hard-fail per D-60):
- `markos_agent_runs` table (provided by P207)
- `createAgentRun(input)` → returns agentrun_id
- `updateAgentRun(agentrun_id, state)`
- `cancelAgentRun(agentrun_id)`

From P208 Approval Inbox + P105 buildApprovalPackage (D-58 canonical).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrations 128 + 132 — launch_runbooks table + cron state + D-66 / RH9 runbook execute trigger</name>
  <files>supabase/migrations/128_launch_runbooks.sql, supabase/migrations/132_launch_cron_state_and_execute_trigger.sql, test/launches/schema/launch-runbook.test.js, test/launches/rls/launch-runbooks.rls.test.js, test/launches/triggers/runbook-execute-required.test.js</files>
  <behavior>
    - Test 1: launch_runbooks schema — state enforces 7 valid values; current_step >=0; RLS enforced.
    - Test 2: composite UNIQUE on (runbook_id, idempotency_key) prevents step-replay.
    - Test 3: cron_run_log + bounce_alert_baselines + runbook_step_log tables exist with RLS.
    - Test 4: D-66 / RH9 trigger: UPDATE launch_runbooks SET state='executing' SHOULD FAIL when launch_gates has any blocking row for same launch_id.
    - Test 5: Same UPDATE SUCCEEDS when no blocking gates exist.
  </behavior>
  <read_first>
    - 224-CONTEXT.md D-12, D-15, D-16, D-37, D-51, D-66
    - 224-REVIEWS.md RH9 (DB-trigger model)
    - 224-RESEARCH.md §Schema Sketches lines 516-541 (launch_runbooks DDL)
    - supabase/migrations/127_launch_gates.sql (Plan 04 — referenced by trigger)
    - supabase/migrations/133_launch_readiness_required_trigger.sql (Plan 04 — D-65 mirror trigger pattern)
  </read_first>
  <action>
    All test files `.test.js` with `node:test` + `node:assert/strict`.

    1. **Write failing tests FIRST**.

    2. **Migration 128 (`128_launch_runbooks.sql`):**
       ```sql
       CREATE TABLE IF NOT EXISTS launch_runbooks (
         runbook_id        UUID PRIMARY KEY DEFAULT gen_random_uuid(),
         tenant_id         UUID NOT NULL REFERENCES markos_tenants(id),
         launch_id         UUID NOT NULL REFERENCES launch_briefs(launch_id),
         steps             JSONB NOT NULL DEFAULT '[]',
         rollback_steps    JSONB NOT NULL DEFAULT '[]',
         owner_user_id     UUID,
         state             TEXT NOT NULL DEFAULT 'draft' CHECK (state IN (
           'draft','armed','executing','executed','rolling_back','rolled_back','failed'
         )),
         current_step      INTEGER NOT NULL DEFAULT 0 CHECK (current_step >= 0),
         started_at        TIMESTAMPTZ,
         completed_at      TIMESTAMPTZ,
         agentrun_id       UUID,
         partial_rollback_blockers JSONB NOT NULL DEFAULT '[]',
         created_at        TIMESTAMPTZ NOT NULL DEFAULT NOW(),
         updated_at        TIMESTAMPTZ NOT NULL DEFAULT NOW()
       );
       ALTER TABLE launch_runbooks ENABLE ROW LEVEL SECURITY;
       CREATE POLICY launch_runbooks_tenant_isolation ON launch_runbooks
         USING (tenant_id = current_setting('app.active_tenant_id', true)::UUID);
       CREATE INDEX idx_launch_runbooks_launch_state ON launch_runbooks (launch_id, state);
       CREATE INDEX idx_launch_runbooks_agentrun ON launch_runbooks (agentrun_id) WHERE agentrun_id IS NOT NULL;
       ```

    3. **Migration 132 (`132_launch_cron_state_and_execute_trigger.sql`):**
       ```sql
       -- Cron state tables
       CREATE TABLE IF NOT EXISTS cron_run_log (
         run_id        UUID PRIMARY KEY DEFAULT gen_random_uuid(),
         tenant_id     UUID,  -- nullable for global cron
         cron_name     TEXT NOT NULL,
         started_at    TIMESTAMPTZ NOT NULL DEFAULT NOW(),
         completed_at  TIMESTAMPTZ,
         status        TEXT NOT NULL DEFAULT 'running' CHECK (status IN ('running','succeeded','failed')),
         error_message TEXT,
         result_payload JSONB
       );
       CREATE TABLE IF NOT EXISTS bounce_alert_baselines (
         tenant_id           UUID NOT NULL REFERENCES markos_tenants(id),
         metric_kind         TEXT NOT NULL CHECK (metric_kind IN ('conversion_rate','botid_block_rate')),
         baseline_value      NUMERIC(20,6) NOT NULL,
         baseline_window_days INT NOT NULL DEFAULT 7,
         computed_at         TIMESTAMPTZ NOT NULL DEFAULT NOW(),
         PRIMARY KEY (tenant_id, metric_kind)
       );
       ALTER TABLE bounce_alert_baselines ENABLE ROW LEVEL SECURITY;
       CREATE POLICY bounce_alert_baselines_tenant_isolation ON bounce_alert_baselines
         USING (tenant_id = current_setting('app.active_tenant_id', true)::UUID);
       CREATE TABLE IF NOT EXISTS runbook_step_log (
         log_id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
         tenant_id       UUID NOT NULL REFERENCES markos_tenants(id),
         runbook_id      UUID NOT NULL REFERENCES launch_runbooks(runbook_id),
         step_id         TEXT NOT NULL,
         direction       TEXT NOT NULL CHECK (direction IN ('forward','rollback')),
         status          TEXT NOT NULL CHECK (status IN ('started','completed','failed','skipped')),
         operator_task_id UUID,
         occurred_at     TIMESTAMPTZ NOT NULL DEFAULT NOW(),
         CONSTRAINT runbook_step_log_idempotent UNIQUE (runbook_id, step_id, direction, status)
       );
       ALTER TABLE runbook_step_log ENABLE ROW LEVEL SECURITY;
       CREATE POLICY runbook_step_log_tenant_isolation ON runbook_step_log
         USING (tenant_id = current_setting('app.active_tenant_id', true)::UUID);

       -- D-66 / RH9 runbook execute trigger
       CREATE OR REPLACE FUNCTION launch_runbook_execute_required()
       RETURNS TRIGGER
       LANGUAGE plpgsql
       AS $$
       DECLARE
         v_blocking_count int;
       BEGIN
         IF NEW.state = 'executing' AND OLD.state <> 'executing' THEN
           SELECT COUNT(*) INTO v_blocking_count
           FROM launch_gates
           WHERE launch_id = NEW.launch_id
             AND status = 'blocking';
           IF v_blocking_count > 0 THEN
             RAISE EXCEPTION 'launch_runbook_execute_required: cannot transition runbook % to executing when % blocking gate(s) exist for launch %',
               NEW.runbook_id, v_blocking_count, NEW.launch_id
               USING ERRCODE = '23P01';
           END IF;
         END IF;
         RETURN NEW;
       END;
       $$;

       CREATE TRIGGER launch_runbook_execute_check
         BEFORE UPDATE ON launch_runbooks
         FOR EACH ROW EXECUTE FUNCTION launch_runbook_execute_required();
       ```

    4. Run schema + RLS + trigger tests GREEN. Commits: `test(224-06): add failing runbook schema + RLS + execute trigger tests` then `feat(224-06): migrations 128 (launch_runbooks) + 132 (cron state + D-66 execute trigger)`.
  </action>
  <acceptance_criteria>
    - 2 migration files exist at slots 128, 132
    - 128 contains `CREATE TABLE IF NOT EXISTS launch_runbooks` with 7 state values + RLS
    - 132 contains `CREATE TRIGGER launch_runbook_execute_check BEFORE UPDATE ON launch_runbooks` invoking `launch_runbook_execute_required()`; cron state tables also defined
    - 3 test files green: schema, RLS, trigger
    - Trigger blocks `UPDATE launch_runbooks SET state='executing'` when blocking gate exists
  </acceptance_criteria>
  <verify>
    <automated>node --test test/launches/schema/launch-runbook.test.js test/launches/rls/launch-runbooks.rls.test.js test/launches/triggers/runbook-execute-required.test.js</automated>
  </verify>
  <done>2 migrations land slots 128/132; D-66 / RH9 runbook execute trigger blocks app-bypass; 3 test files green; ready for executor in Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Runbook executor (assertUpstreamReady AgentRun) + step handlers + reverse rollback (Pitfall 6) + idempotency</name>
  <files>lib/markos/launches/runbook/executor.ts, lib/markos/launches/runbook/step-handlers.ts, lib/markos/launches/runbook/rollback.ts, lib/markos/launches/runbook/idempotency.ts, lib/markos/launches/runbook/index.ts, lib/markos/launches/index.ts, test/launches/runbook/executor-happy-path.test.js, test/launches/runbook/executor-rollback.test.js, test/launches/runbook/executor-idempotency.test.js, test/launches/runbook/agentrun-upstream-gate.test.js, test/launches/runbook/rollback-partial.test.js</files>
  <behavior>
    - Test 1: executeRunbook calls assertUpstreamReady(['P207']) at entry; throws UpstreamPhaseNotLandedError if AgentRun absent. NO bridge stub.
    - Test 2: happy path — sequential step execution honoring depends_on DAG; AgentRun row created; runbook.agentrun_id populated; per-step audit row written.
    - Test 3: rollback executes rollback_steps in REVERSE order; reversible:false steps emit operator task + CONTINUE (Pitfall 6).
    - Test 4: idempotency — retrying same step_id with same idempotency_key skips re-execution.
    - Test 5: rollback partial — final state='rolled_back' with partial_rollback_blockers field listing non-reversible steps.
    - Test 6: ISR cache invalidation on rollback — when rollback step archives a conversion_page surface, invalidatePageCache called BEFORE step marked complete.
  </behavior>
  <read_first>
    - 224-CONTEXT.md D-35, D-36, D-37, D-49, D-60
    - 224-REVIEWS.md RH6 (AgentRun bridge stub → assertUpstreamReady)
    - lib/markos/launches/contracts/runbook.ts (Plan 01)
    - lib/markos/conversion/preflight/upstream-gate.ts (Plan 01)
    - lib/markos/conversion/render/cache-tags.ts (Plan 03 — invalidatePageCache)
    - lib/markos/launches/gates/evaluate-all.ts (Plan 04)
  </read_first>
  <action>
    All test files `.test.js`.

    1. **`lib/markos/launches/runbook/idempotency.ts`** — step replay protection:
       ```ts
       export async function checkStepIdempotency(input: {
         runbook_id: string;
         step_id: string;
         idempotency_key: string;
         supabase: SupabaseClient;
       }): Promise<{ already_executed: boolean }> {
         const { count } = await input.supabase
           .from('runbook_step_log')
           .select('*', { count: 'exact', head: true })
           .eq('runbook_id', input.runbook_id)
           .eq('step_id', input.step_id)
           .eq('direction', 'forward')
           .eq('status', 'completed');
         return { already_executed: (count ?? 0) > 0 };
       }
       ```

    2. **`lib/markos/launches/runbook/step-handlers.ts`** — 6 step handlers + dispatcher:
       ```ts
       export async function dispatchStep(step: RunbookStep, ctx): Promise<{ status: 'completed' | 'operator_task'; operator_task_id?: string }> {
         switch (step.kind) {
           case 'publish_surface':
             return await handlePublishSurface(step, ctx);
           case 'dispatch_email_campaign':
             return await handleDispatchEmailCampaign(step, ctx);
           case 'send_messaging':
             return await handleSendMessaging(step, ctx);
           case 'notify_team':
             return await handleNotifyTeam(step, ctx);
           case 'post_social':
           case 'custom': {
             // Forward-execution unavailable kinds → operator task + continue (mirrors non-reversible rollback)
             const taskId = await createOperatorTask({
               tenant_id: ctx.tenant_id,
               kind: `runbook_step_${step.kind}_pending`,
               payload: { step },
             });
             return { status: 'operator_task', operator_task_id: taskId };
           }
           default:
             throw new Error(`unknown_step_kind: ${step.kind}`);
         }
       }
       ```

    3. **`lib/markos/launches/runbook/executor.ts`** — D-37 + D-60 + RH6 hard-fail:
       ```ts
       import { assertUpstreamReady } from '@/lib/markos/conversion/preflight';
       import { dispatchStep } from './step-handlers';
       import { checkStepIdempotency } from './idempotency';
       import { writeAuditRow } from '@/lib/markos/audit';

       export async function executeRunbook(input: { runbook_id: string; tenant_id: string }, supabase: SupabaseClient): Promise<{ state: string }> {
         // D-37 + D-60 + RH6 — hard-fail if AgentRun (P207) absent. NO bridge stub.
         assertUpstreamReady(['P207']);

         const { createAgentRun, updateAgentRun } = await import('@/lib/markos/agentruns');

         const { data: runbook } = await supabase.from('launch_runbooks').select('*').eq('runbook_id', input.runbook_id).single();
         if (!runbook) throw new Error('runbook_not_found');

         // Create AgentRun
         const agentrun_id = await createAgentRun({
           tenant_id: input.tenant_id,
           run_type: 'launch_execution',
           input: { runbook_id: input.runbook_id, launch_id: runbook.launch_id },
         });
         await supabase.from('launch_runbooks').update({ agentrun_id, state: 'executing', started_at: new Date().toISOString() }).eq('runbook_id', input.runbook_id);

         try {
           for (let i = runbook.current_step; i < runbook.steps.length; i++) {
             const step = runbook.steps[i];
             const { already_executed } = await checkStepIdempotency({ runbook_id: input.runbook_id, step_id: step.step_id, idempotency_key: step.idempotency_key, supabase });
             if (already_executed) continue;

             // depends_on — verify all earlier deps completed
             const depsOk = await verifyDependencies(step.depends_on, input.runbook_id, supabase);
             if (!depsOk) throw new Error(`dependency_unsatisfied: ${step.step_id}`);

             await supabase.from('runbook_step_log').insert({ tenant_id: input.tenant_id, runbook_id: input.runbook_id, step_id: step.step_id, direction: 'forward', status: 'started' });

             const result = await dispatchStep(step, { tenant_id: input.tenant_id, supabase });

             await supabase.from('runbook_step_log').insert({ tenant_id: input.tenant_id, runbook_id: input.runbook_id, step_id: step.step_id, direction: 'forward', status: 'completed', operator_task_id: result.operator_task_id ?? null });
             await writeAuditRow({ tenant_id: input.tenant_id, event_type: 'runbook_step_completed', payload: { ...result, runbook_id: input.runbook_id, step_id: step.step_id } });
             await supabase.from('launch_runbooks').update({ current_step: i + 1 }).eq('runbook_id', input.runbook_id);
           }

           await supabase.from('launch_runbooks').update({ state: 'executed', completed_at: new Date().toISOString() }).eq('runbook_id', input.runbook_id);
           await updateAgentRun(agentrun_id, { state: 'completed' });
           return { state: 'executed' };
         } catch (err) {
           await supabase.from('launch_runbooks').update({ state: 'failed' }).eq('runbook_id', input.runbook_id);
           await updateAgentRun(agentrun_id, { state: 'failed', error: err.message });
           throw err;
         }
       }
       ```

    4. **`lib/markos/launches/runbook/rollback.ts`** — D-36 + Pitfall 6 + ISR invalidation:
       ```ts
       import { invalidatePageCache } from '@/lib/markos/conversion/render/cache-tags';

       export async function rollbackRunbook(input: { runbook_id: string; tenant_id: string }, supabase: SupabaseClient): Promise<{ state: string; partial_blockers: string[] }> {
         const { data: runbook } = await supabase.from('launch_runbooks').select('*').eq('runbook_id', input.runbook_id).single();
         await supabase.from('launch_runbooks').update({ state: 'rolling_back' }).eq('runbook_id', input.runbook_id);

         const partialBlockers: string[] = [];
         // Reverse order
         const rollbackSteps = [...runbook.rollback_steps].reverse();
         for (const step of rollbackSteps) {
           if (step.reversible === false) {
             // Pitfall 6 — emit operator task + continue
             const taskId = await createOperatorTask({ tenant_id: input.tenant_id, kind: `rollback_non_reversible_${step.kind}`, payload: { step } });
             partialBlockers.push(`${step.step_id}:${step.kind}:operator_task=${taskId}`);
             await supabase.from('runbook_step_log').insert({ tenant_id: input.tenant_id, runbook_id: input.runbook_id, step_id: step.step_id, direction: 'rollback', status: 'skipped', operator_task_id: taskId });
             continue;
           }

           // Execute reverse step
           if (step.kind === 'archive_surface' && step.target_kind === 'conversion_page') {
             // Pitfall 3 — synchronous ISR invalidation BEFORE marking complete
             await invalidatePageCache(input.tenant_id, step.target_ref);
           }
           await dispatchStep(step, { tenant_id: input.tenant_id, supabase });

           await supabase.from('runbook_step_log').insert({ tenant_id: input.tenant_id, runbook_id: input.runbook_id, step_id: step.step_id, direction: 'rollback', status: 'completed' });
           await writeAuditRow({ tenant_id: input.tenant_id, event_type: 'runbook_rollback_step', payload: { runbook_id: input.runbook_id, step_id: step.step_id, reversible: step.reversible !== false } });
         }

         await supabase.from('launch_runbooks').update({ state: 'rolled_back', partial_rollback_blockers: partialBlockers }).eq('runbook_id', input.runbook_id);
         return { state: 'rolled_back', partial_blockers: partialBlockers };
       }
       ```

    5. Run all tests GREEN. Commits: `test(224-06): add failing executor + rollback + idempotency tests` then `feat(224-06): runbook executor with assertUpstreamReady AgentRun + reverse rollback (Pitfall 6) + ISR invalidation on archive`.
  </action>
  <acceptance_criteria>
    - executor.ts calls `assertUpstreamReady(['P207'])` at function entry; NO `bridge stub` / `if exists` / `fallback` / `workflow.agentrun_v2_available` strings
    - rollback.ts executes rollback_steps in reverse; reversible:false → operator task + continue (Pitfall 6)
    - ISR invalidation called synchronously before step marked complete (Pitfall 3)
    - 5 test files green via `node --test test/launches/runbook/`
    - Architecture-lock + helper-presence re-run green
  </acceptance_criteria>
  <verify>
    <automated>node --test test/launches/runbook/ && node --test test/conversion/preflight/architecture-lock.test.js && grep -c "bridge stub\|stub if absent\|workflow.agentrun_v2_available\|A6 fallback" lib/markos/launches/runbook/ || echo "zero (expected)"</automated>
  </verify>
  <done>Executor + step handlers + reverse rollback + idempotency ship; AgentRun hard-fail per RH6; 5 test files green; ready for cron + API in Task 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 4 cron handlers + 4 legacy *.js API endpoints + vercel.json + F-142 contract (F-145/F-146 deferred to Plan 07)</name>
  <files>lib/markos/launches/cron/surface-health-audit.ts, lib/markos/launches/cron/gate-evaluation-poll.ts, lib/markos/launches/cron/outcome-computation.ts, lib/markos/launches/cron/bounce-spike-alert.ts, lib/markos/launches/cron/index.ts, api/v1/launches/runbooks.js, api/v1/launches/runbooks-arm.js, api/v1/launches/runbooks-execute.js, api/v1/launches/runbooks-rollback.js, api/crons/launches-surface-health-audit.js, api/crons/launches-gate-evaluation-poll.js, api/crons/launches-outcome-computation.js, api/crons/launches-bounce-spike-alert.js, vercel.json, contracts/F-142-launch-runbook-v1.yaml, contracts/flow-registry.json, contracts/openapi.json, test/launches/cron/surface-health-audit.test.js, test/launches/cron/gate-evaluation-poll.test.js, test/launches/cron/outcome-computation-cron.test.js, test/launches/cron/bounce-spike.test.js, test/launches/api/runbooks-api.test.js, .planning/phases/224-conversion-launch-workspace/224-VALIDATION.md</files>
  <behavior>
    - Test 1: surface-health-audit cron flags pages with stale pricing_context_id + creates operator task.
    - Test 2: gate-evaluation-poll cron runs every 15 min; calls evaluateAllLaunchGates for in-flight launches.
    - Test 3: outcome-computation cron triggers computeLaunchOutcome at T+7/T+14/T+30 (verifies D-71 hard-fail propagates).
    - Test 4: bounce-spike-alert detects 2σ deviation from baseline.
    - Test 5: API endpoints ship as legacy *.js (NOT route.ts); use requireHostedSupabaseAuth + buildApprovalPackage.
    - Test 6: vercel.json contains 4 cron entries with correct schedules.
  </behavior>
  <read_first>
    - 224-CONTEXT.md D-39, D-42, D-48, D-49, D-50, D-58
    - 224-REVIEWS.md RH4 (legacy *.js)
    - lib/markos/launches/runbook/* (Task 2)
    - lib/markos/launches/gates/evaluate-all.ts (Plan 04)
    - lib/markos/launches/outcomes/compute.ts (Plan 04)
    - lib/markos/crm/agent-actions.ts (buildApprovalPackage)
    - existing vercel.json (preserve existing cron entries)
  </read_first>
  <action>
    1. **4 cron handler libs** under `lib/markos/launches/cron/`:
       - `surface-health-audit.ts` exports `scanPublishedSurfacesForStaleness({tenant_id?, supabase})`. Iterates published conversion_pages; checks pricing_context_id (P205) + evidence_pack_id (P209) freshness; creates operator task per stale page.
       - `gate-evaluation-poll.ts` exports `pollInflightLaunches({supabase})`. Iterates launch_briefs in 'ready' or 'live' status; calls `evaluateAllLaunchGates` for each.
       - `outcome-computation.ts` exports `triggerOutcomeComputation({supabase})`. For each launched launch, identify which T+7/T+14/T+30 cycle is due; call `computeLaunchOutcome`. (Note: `computeLaunchOutcome` itself calls assertUpstreamReady(['P222','P223']) per Plan 04 — D-71 / RM5; cron handler propagates the error.)
       - `bounce-spike-alert.ts` exports `detectBounceAndSpikes({tenant_id?, supabase})`. Compares 24h conversion rate against `bounce_alert_baselines.baseline_value`; if >2σ drop OR BotID block rate >10× baseline → tenant-admin alert via Approval Inbox.

    2. **4 cron entry points** at `api/crons/launches-*.js` (legacy `*.js`):
       ```js
       const { scanPublishedSurfacesForStaleness } = require('../../lib/markos/launches/cron');
       module.exports = async (req, res) => {
         // Vercel cron auth header check
         if (req.headers.authorization !== `Bearer ${process.env.CRON_SECRET}`) {
           res.statusCode = 401; res.end(); return;
         }
         try {
           const result = await scanPublishedSurfacesForStaleness({ supabase: getServiceRoleClient() });
           res.statusCode = 200; res.end(JSON.stringify(result));
         } catch (err) {
           res.statusCode = 500; res.end(JSON.stringify({ error: err.message }));
         }
       };
       ```

    3. **Update `vercel.json`** — additive merge (NEVER overwrite). Use the explicit JSON-merge approach below; do NOT pass a literal object that replaces the top-level `crons` array. Existing P22x cron entries (CDP/CRM360/channels) MUST be preserved.

       Run this Node merge script (or write equivalent code in the executor):
       ```js
       const fs = require('fs');
       const cfg = JSON.parse(fs.readFileSync('vercel.json', 'utf8'));
       cfg.crons = cfg.crons || [];
       const newEntries = [
         { path: '/api/crons/launches-surface-health-audit', schedule: '0 4 * * *' },
         { path: '/api/crons/launches-gate-evaluation-poll', schedule: '*/15 * * * *' },
         { path: '/api/crons/launches-outcome-computation', schedule: '0 6 * * *' },
         { path: '/api/crons/launches-bounce-spike-alert', schedule: '0 * * * *' },
       ];
       for (const entry of newEntries) {
         if (!cfg.crons.some(c => c.path === entry.path)) cfg.crons.push(entry);
       }
       fs.writeFileSync('vercel.json', JSON.stringify(cfg, null, 2) + '
');
       ```

       Acceptance verification (executable):
       ```bash
       # 1) New launches crons present
       node -e "const c=JSON.parse(require('fs').readFileSync('vercel.json')); if(!c.crons.some(x=>x.path.startsWith('/api/v1/launches/cron') || x.path.startsWith('/api/crons/launches-'))) process.exit(1);"
       # 2) Existing P22x crons preserved (verify any pre-existing cdp/crm/channels schedule is still present — adapt to repo state)
       node -e "const c=JSON.parse(require('fs').readFileSync('vercel.json')); console.log('cron count:', c.crons.length);"
       ```

    4. **4 legacy `*.js` runbook API endpoints** at `api/v1/launches/`:
       - `runbooks.js` — GET/POST CRUD
       - `runbooks-arm.js` — POST `/v1/launches/{id}/runbooks/{runbook_id}/arm` — sets state='armed' + buildApprovalPackage
       - `runbooks-execute.js` — POST `/v1/launches/{id}/runbooks/{runbook_id}/execute` — calls executeRunbook (which DB trigger D-66 will block if any blocking gate exists). Approval-aware.
       - `runbooks-rollback.js` — POST `/v1/launches/{id}/runbooks/{runbook_id}/rollback` — calls rollbackRunbook. Approval-aware.

       Example for `runbooks-execute.js`:
       ```js
       const { requireHostedSupabaseAuth } = require('../../../onboarding/backend/runtime-context.cjs');
       const { buildApprovalPackage } = require('../../../lib/markos/crm/agent-actions');
       const { executeRunbook } = require('../../../lib/markos/launches/runbook');

       module.exports = async (req, res) => {
         if (req.method !== 'POST') { res.statusCode = 405; res.end(); return; }
         try {
           const auth = await requireHostedSupabaseAuth({ req, runtimeContext: getRuntimeContext(), operation: 'runbook.execute' });
           const { runbook_id } = req.body;

           // High-risk approval-aware mutation (D-20 + D-58)
           const pkg = buildApprovalPackage({
             tenant_id: auth.tenant_id, actor_id: auth.user_id, action_type: 'runbook_execute', payload: { runbook_id },
           });

           const result = await executeRunbook({ runbook_id, tenant_id: auth.tenant_id }, getSupabaseClient());
           res.statusCode = 200; res.end(JSON.stringify({ ...result, approval_ref: pkg.ref }));
         } catch (err) {
           // D-66 trigger violation → 422
           if (err.message?.includes('launch_runbook_execute_required')) {
             res.statusCode = 422; res.end(JSON.stringify({ error: 'blocking_gate_present', message: err.message }));
             return;
           }
           // assertUpstreamReady throw → 503
           if (err.constructor?.name === 'UpstreamPhaseNotLandedError') {
             res.statusCode = 503; res.end(JSON.stringify({ error: 'upstream_not_landed', phase: err.phase }));
             return;
           }
           res.statusCode = err.statusCode || 500;
           res.end(JSON.stringify({ error: err.message }));
         }
       };
       ```

    5. **F-142 contract only** — add to `contracts/`:
       - F-142-launch-runbook-v1.yaml: full CRUD + arm + execute + rollback paths; status='active'
       - **F-145 + F-146 are deferred to Plan 07 Task 1 closeout** (per Plan 07 frontmatter ownership). Plan 06 does NOT create or modify F-145/F-146.

    6. **Update flow-registry.json + run `npm run openapi:build`** → contracts/openapi.json regenerated.

    7. **Update `lib/markos/launches/index.ts`** barrel to include cron + runbook.

    8. **Update 224-VALIDATION.md** rows 224-06-01..04 → ✅.

    9. Run all tests + verify legacy *.js convention. Commits: `test(224-06): add failing cron + API tests` then `feat(224-06): 4 cron handlers + 4 legacy *.js runbook API endpoints + vercel.json + F-142 contract`.
  </action>
  <acceptance_criteria>
    - 4 cron lib + 4 cron entry points + 4 API endpoints all `*.js` (NOT `route.ts`)
    - vercel.json has 4 new cron entries with correct schedules; existing entries preserved (verified by: `node -e "const c=JSON.parse(require('fs').readFileSync('vercel.json')); if(!c.crons.some(x=>x.path.startsWith('/api/crons/launches-'))) process.exit(1);"` exits 0 AND total cron count >= prior baseline)
    - All API endpoints use `requireHostedSupabaseAuth` + `buildApprovalPackage` (D-58); D-66 trigger violation surfaces as 422; UpstreamPhaseNotLandedError surfaces as 503
    - F-142 in flow-registry.json + contracts/openapi.json (F-145 + F-146 owned by Plan 07)
    - 5 test files green
    - Architecture-lock test re-runs green (no `route.ts`, no `createApprovalPackage`, no bridge stub)
  </acceptance_criteria>
  <verify>
    <automated>node --test test/launches/cron/ test/launches/api/runbooks-api.test.js && find api/v1/launches/runbooks*.ts api/crons/launches-*.ts -type f 2>/dev/null | wc -l | xargs -I{} test {} -eq 0 && node --test test/conversion/preflight/architecture-lock.test.js</automated>
  </verify>
  <done>4 cron handlers + 4 legacy *.js runbook endpoints + vercel.json + F-142 contract; F-145/F-146 deferred to Plan 07; 224-VALIDATION rows green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app→DB-trigger | D-66 / RH9 runbook execute trigger enforces blocking-gate-blocks-execute at DB level |
| executor→AgentRun | assertUpstreamReady gates executor entry; NO bridge stub |
| cron→tenant | Cron handlers run as service role; per-tenant audit + Approval Inbox alerts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-224-06-01 | Elevation of Privilege | service-role bypass of gate evaluator on execute | mitigate | D-66 / RH9 BEFORE UPDATE trigger fires regardless of role |
| T-224-06-02 | Repudiation | runbook execution without AgentRun audit | mitigate | D-37 + D-60 + RH6 — assertUpstreamReady enforces P207; throws if absent |
| T-224-06-03 | Tampering | step replay | mitigate | runbook_step_log idempotent UNIQUE + checkStepIdempotency |
| T-224-06-04 | Denial of Service | non-reversible step aborts entire rollback | mitigate | Pitfall 6 — emit operator task + continue; partial_rollback_blockers field documents state |
| T-224-06-05 | Information Disclosure | cron leaks cross-tenant data | mitigate | tenant_id parameter required; service-role enforced via cron auth header |
| T-224-06-06 | Tampering | unauthorized cron trigger | mitigate | Bearer CRON_SECRET header check on each cron entry point |
</threat_model>

<verification>
- 13 test files green via `node --test test/launches/runbook/ test/launches/cron/ test/launches/api/runbooks-api.test.js test/launches/triggers/runbook-execute-required.test.js`
- D-66 / RH9 trigger blocks app-bypass (test asserts UPDATE fails)
- D-71 / RM5 outcome-computation cron propagates UpstreamPhaseNotLandedError when P222/P223 absent
- Architecture-lock + helper-presence re-run green
- F-142 status='active' visible in `contracts/openapi.json` (F-145/F-146 owned by Plan 07)
</verification>

<success_criteria>
- 2 migrations land slots 128/132 (per D-56)
- D-66 runbook execute DB trigger enforces blocking-gate semantics regardless of session role
- Runbook executor hard-fails on missing P207 (D-37 + D-60 + RH6) — NO bridge stub
- Reverse rollback honors Pitfall 6 (non-reversible → operator task + continue)
- ISR invalidation synchronous on archive_surface rollback (Pitfall 3)
- 4 cron handlers + 4 legacy `*.js` cron entry points + vercel.json schedules
- 4 legacy `*.js` API endpoints (D-42 / RH4) using `buildApprovalPackage` (D-58)
- F-142 contract registered (F-145 + F-146 deferred to Plan 07 ownership)
- 224-VALIDATION rows 224-06-01..04 green
</success_criteria>

<output>
After completion, create `.planning/phases/224-conversion-launch-workspace/224-06-SUMMARY.md` with:
- Tasks completed (1-3) + commit refs
- Migrations 128/132 contents
- D-66 trigger SQL
- Runbook execution + rollback posture
- 4 cron schedules
- 4 legacy API endpoints
- F-IDs registered
</output>
