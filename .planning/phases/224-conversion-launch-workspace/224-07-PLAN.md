---
phase: 224
plan: "07"
type: execute
wave: 5
depends_on: ["224-01", "224-02", "224-03", "224-04", "224-05", "224-06"]
files_modified:
  - supabase/migrations/133_conversion_rls_hardening.sql
  - lib/markos/mcp/tools/publish-page.ts
  - lib/markos/mcp/tools/submit-form.ts
  - lib/markos/mcp/tools/evaluate-launch-gates.ts
  - lib/markos/mcp/tools/execute-runbook.ts
  - lib/markos/mcp/tools/rollback-launch.ts
  - lib/markos/mcp/tools/get-launch-outcome.ts
  - lib/markos/mcp/tools/index.ts
  - api/v1/conversion/pages/route.ts
  - api/v1/conversion/pages/[page_id]/route.ts
  - api/v1/conversion/forms/route.ts
  - api/v1/conversion/forms/[form_id]/route.ts
  - api/v1/conversion/forms/[form_id]/publish/route.ts
  - api/v1/conversion/forms/[form_id]/archive/route.ts
  - api/v1/conversion/ctas/route.ts
  - api/v1/conversion/ctas/[cta_id]/route.ts
  - api/v1/conversion/events/route.ts
  - api/v1/internal/conversion/emit/route.ts
  - app/(markos)/conversion/page.tsx
  - app/(markos)/conversion/PageEditor.tsx
  - app/(markos)/conversion/FormEditor.tsx
  - app/(markos)/launches/page.tsx
  - app/(markos)/launches/LaunchCockpit.tsx
  - app/(markos)/launches/RunbookEditor.tsx
  - app/(markos)/launches/LaunchReadinessBoard.tsx
  - app/(markos)/launches/GatesPanel.tsx
  - app/(markos)/launches/OutcomesDashboard.tsx
  - components/markos/conversion/conversion-workspace.module.css
  - components/markos/launches/launch-cockpit.module.css
  - lib/markos/operating/approvals/entry-types.ts
  - lib/markos/operating/morning-brief/launches-section.ts
  - public/openapi.json
  - public/llms.txt
  - docs/conversion/index.md
  - docs/launches/index.md
  - contracts/F-145-launch-evaluate-gates-v1.yaml
  - contracts/F-146-launch-execute-runbook-v1.yaml
  - contracts/flow-registry.json
  - test/conversion/api/pages-api.test.ts
  - test/conversion/api/forms-api.test.ts
  - test/conversion/api/ctas-api.test.ts
  - test/conversion/api/events-api.test.ts
  - test/mcp/publish-page.tool.test.ts
  - test/mcp/submit-form.tool.test.ts
  - test/mcp/evaluate-launch-gates.tool.test.ts
  - test/mcp/execute-runbook.tool.test.ts
  - test/mcp/rollback-launch.tool.test.ts
  - test/mcp/get-launch-outcome.tool.test.ts
  - test/api/openapi-parity.test.ts
  - test/conversion/rls/conversion-submit-idempotency-rls.test.ts
  - test/conversion/rls/conversion-rate-limit-counters-rls.test.ts
  - test/conversion/rls-suite.test.ts
  - test/playwright/conversion/conversion-workspace.spec.ts
  - test/playwright/launches/launch-cockpit.spec.ts
  - test/playwright/launches/readiness-board.spec.ts
  - test/chromatic/conversion/ConversionWorkspace.stories.tsx
  - test/chromatic/conversion/PageEditor.stories.tsx
  - test/chromatic/launches/LaunchCockpit.stories.tsx
  - test/chromatic/launches/LaunchReadinessBoard.stories.tsx
  - test/regression/p221-adapter.regression.test.ts
  - test/regression/p222-crm360.regression.test.ts
  - test/regression/p223-channels.regression.test.ts
  - test/migrations/order.test.ts
  - test/regression/existing-marketing-routes.regression.test.ts
  - .planning/phases/224-conversion-launch-workspace/224-VALIDATION.md
autonomous: false
requirements:
  - CNV-01
  - CNV-02
  - CNV-03
  - CNV-04
  - CNV-05
  - LCH-01
  - LCH-02
  - LCH-03
  - LCH-04
  - LCH-05
  - PRC-01
  - PRC-02
  - PRC-03
  - PRC-04
  - PRC-05
  - PRC-06
  - PRC-07
  - PRC-08
  - PRC-09
  - CDP-01
  - CDP-02
  - CDP-03
  - CDP-04
  - CDP-05
  - CRM-01
  - CRM-02
  - CRM-03
  - CRM-04
  - CRM-05
  - EML-01
  - EML-02
  - EML-03
  - EML-04
  - EML-05
  - MSG-01
  - MSG-02
  - MSG-03
  - MSG-04
  - MSG-05
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - QA-05
  - QA-06
  - QA-07
  - QA-08
  - QA-09
  - QA-10
  - QA-11
  - QA-12
  - QA-13
  - QA-14
  - QA-15
must_haves:
  truths:
    - "Migration 133 adds cross-table FK indexes + final RLS policy review for all 13 tables + audit_log hash-chain event_type registrations for 13 new write paths + public form submit ACL guard"
    - "6 MCP tools exist in lib/markos/mcp/tools/* (publish_page, submit_form, evaluate_launch_gates, execute_runbook, rollback_launch, get_launch_outcome) — all flow through lib/markos/mcp/pipeline.cjs 10-step middleware (D-43, D-44)"
    - "submit_form MCP tool is the trusted server-to-server path: bypasses BotID (D-43) but still respects rate-limit + variables_schema validation; uses F-138 internal emit contract"
    - "All write APIs honor approval-package pattern (D-20, D-44); 6 high-risk paths confirmed: page publish + launch arm + launch execute + rollback + gate waiver + experiment activation"
    - "ConversionWorkspace + LaunchCockpit + PageEditor + FormEditor + RunbookEditor + LaunchReadinessBoard + GatesPanel + OutcomesDashboard all live in app/(markos)/* under single-shell rule (D-45, P208)"
    - "Approval Inbox extended with 6 new entry types: page_publish, form_publish, launch_arm, launch_execute, gate_waiver, rollback (D-46)"
    - "Morning Brief extended with launches section: top-3 in-flight launches + readiness countdown + blocking gates per launch + recent ConversionEvent volume (D-47)"
    - "OpenAPI 3.1 spec regenerated to include all 17 P224 F-IDs (F-132..F-146); openapi-parity test passes; llms.txt Phase 224 section added"
    - "Cross-table RLS test suite: 15-table cross-tenant denial + nested-FK protection — covers all 13 CONTEXT-listed SOR tables (D-51) + 2 dispatch infrastructure tables (conversion_submit_idempotency + conversion_rate_limit_counters from migration 130) + nested-FK guard (e.g., launch_surface referencing another tenant's email_campaign rejected per Pitfall 4)"
    - "Legacy regression suite green: P221 CDP adapter + P222 CRM360 + P223 channels + existing marketing routes (/signup, /integrations/claude, /docs) — additive phase MUST NOT regress"
    - "F-145 launch-evaluate-gates + F-146 launch-execute-runbook MCP write contracts active (graduated from plan 04 stub)"
    - "224-VALIDATION.md `nyquist_compliant: true` after this plan; all per-task rows for 224-01..07 ✅ green"
    - "checkpoint:human-verify task validates the operator journey at the end (UI + flow); plan is autonomous=false because of the final checkpoint"
    - "Migration application order is wave-driven NOT numeric: W1 (121-126) → W2 (130) → W3 (127, 129, 131) → W4 (128, 132) → W5 (133). test/migrations/order.test.ts asserts the migration runner script enforces this wave-execution order; naive numeric-order application would break (e.g., 130 runs before 127 because plan 02 is Wave 2 and plan 04 is Wave 3)" 
  artifacts:
    - path: supabase/migrations/133_conversion_rls_hardening.sql
      provides: cross-table FK indexes + 13 audit_log event_type registrations + public form submit ACL hardening (deny anonymous SELECT on conversion_pages where status != 'published')
    - path: lib/markos/mcp/tools/* (6 files)
      provides: 6 MCP tool wrappers — each registers via lib/markos/mcp/pipeline.cjs 10-step middleware
    - path: api/v1/conversion/pages/route.ts (and forms, ctas, events, internal/emit)
      provides: complete read-write CRUD for conversion domain (CRUD APIs deferred from plan 03 which only shipped publish/archive)
    - path: app/(markos)/conversion/page.tsx
      provides: ConversionWorkspace shell — kanban-by-status (10 page_type variants), forms list, CTAs, experiments table, ConversionEvents stream
    - path: app/(markos)/conversion/PageEditor.tsx
      provides: JSON content_blocks editor + per-block JSON view + preview pane (renders D-03 page-renderer with mock pricing/evidence) + Pricing/Evidence binding inspector + Publish CTA (gates approval); mirrors P223 D-25 TemplateEditor pattern
    - path: app/(markos)/launches/page.tsx
      provides: LaunchCockpit shell — briefs kanban (planning/pending_approval/ready/live/completed/rolled_back) + Surface board per launch + Gates panel + Runbook viewer + Outcomes dashboard
    - path: app/(markos)/launches/LaunchReadinessBoard.tsx
      provides: dependency graph of readiness_checks + LaunchGate status + countdown to launch_date
    - path: app/(markos)/launches/RunbookEditor.tsx
      provides: ordered steps[] + rollback_steps[] editor + dependency graph view + AgentRun status panel
    - path: lib/markos/operating/approvals/entry-types.ts
      provides: 6 new approval entry types registered in P208 Approval Inbox data model
    - path: lib/markos/operating/morning-brief/launches-section.ts
      provides: morning brief query: top-3 in-flight launches + readiness countdown + blocking gates + ConversionEvent volume 24h
    - path: public/openapi.json
      provides: regenerated OpenAPI 3.1 with 17 P224 F-IDs (F-132..F-146)
    - path: public/llms.txt
      provides: Phase 224 documentation section appended
    - path: contracts/F-145-launch-evaluate-gates-v1.yaml
      provides: F-145 active MCP write contract for evaluate_launch_gates tool
    - path: contracts/F-146-launch-execute-runbook-v1.yaml
      provides: F-146 active MCP write contract for execute_runbook + rollback_launch tools
  key_links:
    - from: lib/markos/mcp/tools/publish-page.ts
      to: api/v1/conversion/pages/[page_id]/publish/route.ts (plan 03)
      via: MCP tool wraps the publish route handler with pipeline.cjs 10-step middleware
    - from: app/(markos)/conversion/PageEditor.tsx
      to: lib/markos/conversion/render/page-renderer.tsx (plan 03)
      via: preview pane renders the same renderer as the public route, with mock pricing_context for unbound previews
    - from: app/(markos)/launches/LaunchCockpit.tsx
      to: lib/markos/launches/runbook/* (plan 06) + lib/markos/launches/gates/* (plan 04) + lib/markos/launches/outcomes/* (plan 04)
      via: cockpit reads runbook state + gate state + outcome metrics; user-triggered actions hit corresponding API routes
    - from: lib/markos/operating/morning-brief/launches-section.ts
      to: P208 Morning Brief surface
      via: extends existing brief data query (or stubs if P208 absent)
    - from: supabase/migrations/133_conversion_rls_hardening.sql
      to: existing migrations 121-132
      via: final cross-table FK indexes + RLS audit completing the schema
---

<objective>
Phase 224 closeout: complete read-write CRUD APIs for conversion domain (deferred from plan 03), 6 MCP tools (D-43), 8 UI components in app/(markos)/* under single-shell (D-45), Approval Inbox + Morning Brief integration (D-46/D-47), migration 133 RLS hardening + audit_log event registrations + public form ACL, F-145 + F-146 active, OpenAPI regeneration with 17 P224 F-IDs (F-132..F-146), llms.txt update, full RLS suite + Playwright operator journeys + Chromatic visual regression + legacy regression for P221/P222/P223 + existing marketing routes + final 224-VALIDATION.md sign-off (`nyquist_compliant: true`), and a checkpoint:human-verify task at the end validating the full operator journey.

Purpose: satisfy ALL phase requirement IDs in the frontmatter `requirements` field — CNV-01..05 + LCH-01..05 directly via this plan's UI + APIs + MCP tools; PRC-01..09 + CDP-01..05 + CRM-01..05 + EML-01..05 + MSG-01..05 carry-forward via P205/P221/P222/P223 adapters confirmed wired (gate evaluators in plan 04, emit() fan-out in plan 02, LaunchSurface coordinator in plan 04); QA-01..15 via OpenAPI parity + RLS suite + Playwright + Chromatic + legacy regression. This is the only plan listing the FULL phase requirements list because it integrates and verifies them all.

Output: 1 migration + 7 MCP tool files + 9 conversion API routes + 8 UI files + 2 operating integration files + 2 contract YAMLs + OpenAPI regen + llms.txt + 18 test files (4 API + 6 MCP + 1 OpenAPI parity + 1 RLS suite + 4 regression + 4 Playwright + 4 Chromatic stories) + final VALIDATION sign-off + 1 human-verify checkpoint. Plan 07 is autonomous=false due to the final checkpoint.
</objective>

<context>
@.planning/phases/224-conversion-launch-workspace/224-CONTEXT.md
@.planning/phases/224-conversion-launch-workspace/224-RESEARCH.md
@.planning/phases/224-conversion-launch-workspace/224-VALIDATION.md
@.planning/phases/224-conversion-launch-workspace/224-01-PLAN.md
@.planning/phases/224-conversion-launch-workspace/224-02-PLAN.md
@.planning/phases/224-conversion-launch-workspace/224-03-PLAN.md
@.planning/phases/224-conversion-launch-workspace/224-04-PLAN.md
@.planning/phases/224-conversion-launch-workspace/224-05-PLAN.md
@.planning/phases/224-conversion-launch-workspace/224-06-PLAN.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/V4.0.0-TESTING-ENVIRONMENT-PLAN.md
@.planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md
@.planning/phases/208-human-operating-interface/208-CONTEXT.md
@.planning/phases/207-agentrun-v2-orchestration-substrate/207-CONTEXT.md
@.planning/phases/202-mcp-server-ga-claude-marketplace/DISCUSS.md
@CLAUDE.md
@obsidian/reference/HTTP Layer.md
@obsidian/reference/UI Components.md
@obsidian/reference/Contracts Registry.md
@lib/markos/mcp/pipeline.cjs
@public/openapi.json
@public/llms.txt
@app/(markos)/

<!-- INFORMATIONAL ONLY: -->
<!-- @obsidian/work/incoming/23-CONVERSION-ENGINE.md (informational only — decisions locked in 224-CONTEXT.md per CLAUDE.md §source-of-truth precedence; canonical = obsidian/reference/* + 224-CONTEXT.md) -->
<!-- @obsidian/work/incoming/26-LAUNCH-ENGINE.md (informational only — decisions locked in 224-CONTEXT.md per CLAUDE.md §source-of-truth precedence; canonical = obsidian/reference/* + 224-CONTEXT.md) -->
</context>

<interfaces>
<!-- Key interfaces to consume from prior plans 01..06. -->

From plan 02 (`lib/markos/conversion/forms/submit-handler.ts`): `handlePublicFormSubmit(input)`.
From plan 02 (`lib/markos/conversion/events/emit.ts`): `emitConversionEvent(ctx, payload)`.
From plan 03 (`api/v1/conversion/pages/[page_id]/{publish|archive}/route.ts`): existing publish/archive routes.
From plan 04 (`lib/markos/launches/gates/evaluate-all.ts`): `evaluateAllLaunchGates(input)`.
From plan 04 (`lib/markos/launches/outcomes/compute.ts`): `computeLaunchOutcome(input)`.
From plan 06 (`lib/markos/launches/runbook/executor.ts`): `executeRunbook(input)`.
From plan 06 (`lib/markos/launches/runbook/rollback.ts`): `rollbackRunbook(input)`.

From `lib/markos/mcp/pipeline.cjs` (existing P202 — 10-step middleware: auth → rate_limit → tool_lookup → validate_input → free_tier → approval → cost → invoke → validate_output → trueup):
```javascript
// All P224 MCP tools register through this pipeline; tool wrapper exports tool name + invoker function
exports.registerTool({
  name: 'publish_page',
  description: '...',
  inputSchema: {...},
  invoke: async (ctx, input) => { ... }
});
```

From `app/(markos)/layout-shell.tsx` (P201/P203 single shell — extend nav with conversion + launches entries).

From P208 Approval Inbox + Morning Brief (extend; bridge stub if P208 not yet shipped).

From `224-RESEARCH.md` §Tests Implied lines 1230-1264 (224-07 specific test inventory).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 133 RLS hardening + audit_log registrations + public form ACL + 9 conversion CRUD APIs</name>
  <files>supabase/migrations/133_conversion_rls_hardening.sql, api/v1/conversion/pages/route.ts, api/v1/conversion/pages/[page_id]/route.ts, api/v1/conversion/forms/route.ts, api/v1/conversion/forms/[form_id]/route.ts, api/v1/conversion/forms/[form_id]/publish/route.ts, api/v1/conversion/forms/[form_id]/archive/route.ts, api/v1/conversion/ctas/route.ts, api/v1/conversion/ctas/[cta_id]/route.ts, api/v1/conversion/events/route.ts, api/v1/internal/conversion/emit/route.ts, test/conversion/api/pages-api.test.ts, test/conversion/api/forms-api.test.ts, test/conversion/api/ctas-api.test.ts, test/conversion/api/events-api.test.ts, test/conversion/rls-suite.test.ts</files>
  <behavior>
    - Test 1: Migration 133 adds cross-table FK indexes (e.g., conversion_events(experiment_variant_id), launch_surfaces(launch_id, status)).
    - Test 2: Migration 133 registers 13+ audit_log event_types (one per write path): page_publish, page_archive, form_publish, form_archive, conversion_event_emit, public_form_submit, bot_id_block, rate_limit_block, honeypot_block, consent_revoked, idempotency_replay, launch_gate_evaluation, launch_outcome_computed, etc.
    - Test 3: Public form submit ACL: anonymous query SELECT on conversion_pages WHERE status != 'published' returns 0 rows even with service_role bypass when called from public endpoint context.
    - Test 4: Pages CRUD: GET list, GET single, POST creates draft, PUT updates draft fields, DELETE soft-archives via status='archived'.
    - Test 5: Forms CRUD + publish/archive (parallel to pages with approval gate on publish).
    - Test 6: CTAs CRUD with status lifecycle.
    - Test 7: Events read-only: GET /v1/conversion/events with filters by surface_id, launch_id, time range — paginated.
    - Test 8: /v1/internal/conversion/emit (F-138 internal): POST with API key auth (server-to-server); calls emitConversionEvent; bypasses BotID; returns event_id + sinks_written.
    - Test 9: Cross-tenant RLS suite covers all 13 SOR tables + 2 dispatch infrastructure tables (conversion_submit_idempotency + conversion_rate_limit_counters from migration 130) = 15 tables total + cross-table joins (e.g., reading launch_outcomes for another tenant's launch_id denied; rate-limit counter for tenant-A invisible to tenant-B session).
  </behavior>
  <read_first>
    - .planning/phases/224-conversion-launch-workspace/224-CONTEXT.md D-42, D-43, D-51..D-54
    - .planning/phases/224-conversion-launch-workspace/224-RESEARCH.md §Migration 133 description (line 220)
    - All migrations 121-132 from plans 01..06
    - lib/markos/conversion/* (all plans)
    - api/v1/conversion/pages/[page_id]/publish/route.ts (plan 03 — pattern)
    - lib/markos/audit (P201)
    - lib/markos/mcp/pipeline.cjs (existing — for /v1/internal/conversion/emit)
  </read_first>
  <action>
    Write tests FIRST.

    1. Create `supabase/migrations/133_conversion_rls_hardening.sql`:

       ```sql
       -- Phase 224 closeout: cross-table indexes + audit_log event_type registry + public ACL hardening

       -- Cross-table FK indexes for join hot paths
       CREATE INDEX IF NOT EXISTS idx_conversion_events_variant ON conversion_events(experiment_variant_id) WHERE experiment_variant_id IS NOT NULL;
       CREATE INDEX IF NOT EXISTS idx_launch_surfaces_target ON launch_surfaces(surface_target_kind, surface_target_id);
       CREATE INDEX IF NOT EXISTS idx_launch_runbooks_launch_state ON launch_runbooks(launch_id, state);
       CREATE INDEX IF NOT EXISTS idx_launch_outcomes_tenant_period ON launch_outcomes(tenant_id, period_days);

       -- Audit log event_type registry (if markos_audit_event_types exists per P201; otherwise this is a comment block)
       -- The 16 P224-specific audit event types:
       --   page_publish, page_archive, form_publish, form_archive,
       --   conversion_event_emit, conversion_event_emit_failed, public_form_submit,
       --   bot_id_block, rate_limit_block, honeypot_block, consent_revoked, idempotency_replay,
       --   launch_brief_publish, launch_brief_archived, launch_surface_approved, launch_surface_published, launch_surface_archived,
       --   launch_gate_evaluation, gate_waived, launch_outcome_computed, launch_outcome_backfill,
       --   launch_runbook_executed, launch_runbook_failed, launch_runbook_rolled_back, runbook_rollback_step,
       --   experiment_active, experiment_paused, experiment_completed,
       --   surface_health_audit_stale_pricing, surface_health_audit_stale_evidence,
       --   bot_id_abuse_spike, conversion_rate_drop,
       --   p221_adapter_unavailable, p222_adapter_unavailable, p205_adapter_unavailable, p209_adapter_unavailable, launch_agentrun_bridge_stub
       -- (If markos_audit_event_types table exists, INSERT all rows; otherwise this comment block documents the registry)

       -- Public form submit ACL: ensure anonymous role can ONLY read published pages
       -- (RLS already in place per migration 121; this migration adds an explicit GRANT/REVOKE for the public schema role)
       REVOKE ALL ON conversion_pages FROM PUBLIC;
       GRANT SELECT ON conversion_pages TO authenticated, service_role;
       -- Anonymous reads must go through the (public)/[...slug] route which uses service_role with status='published' filter

       -- Verify launch_surfaces polymorphic CHECK still in place (idempotent)
       DO $$
       BEGIN
         IF NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname = 'launch_surfaces_kind_valid') THEN
           ALTER TABLE launch_surfaces ADD CONSTRAINT launch_surfaces_kind_valid CHECK (
             surface_target_kind IN ('email_campaign','messaging_thread','lifecycle_journey','conversion_page','social_pack','sales_enablement','partner_pack','support_pack','docs_update')
           );
         END IF;
       END $$;

       -- Rollback (operator reference):
       -- DROP INDEX IF EXISTS idx_conversion_events_variant;
       -- DROP INDEX IF EXISTS idx_launch_surfaces_target;
       -- DROP INDEX IF EXISTS idx_launch_runbooks_launch_state;
       -- DROP INDEX IF EXISTS idx_launch_outcomes_tenant_period;
       -- GRANT ALL ON conversion_pages TO PUBLIC;  -- restore default
       ```

    2. Create the 9 conversion CRUD route files. Pattern matches plan 03/04: tenant_id from middleware header, RLS via Postgres session, approval-aware on mutations.
       - **pages/route.ts** GET (list) + POST (create draft); imports validateContentBlocks from blocks/schema.ts (plan 01 task 4) for pre-write validation
       - **pages/[page_id]/route.ts** GET + PUT (draft only) + DELETE (soft archive)
       - **forms/route.ts** GET + POST
       - **forms/[form_id]/route.ts** GET + PUT (draft only) + DELETE
       - **forms/[form_id]/publish/route.ts** POST — approval-aware (mirrors page publish from plan 03)
       - **forms/[form_id]/archive/route.ts** POST — approval-aware
       - **ctas/route.ts** GET + POST
       - **ctas/[cta_id]/route.ts** GET + PUT + DELETE
       - **events/route.ts** GET only (filtered, paginated)
       - **internal/conversion/emit/route.ts** POST — API key auth; calls emitConversionEvent; F-138 contract from plan 02

    3. Create `test/conversion/rls-suite.test.ts` covering cross-tenant denial on all 13 SOR tables (CONTEXT D-51) + 5 cross-table joins (e.g., conversion_events.launch_id → launch_briefs across tenants denied). Additionally create `test/conversion/rls/conversion-submit-idempotency-rls.test.ts` and `test/conversion/rls/conversion-rate-limit-counters-rls.test.ts` covering the 2 dispatch infrastructure tables shipped in plan 02 migration 130 (idempotency cache + rate-limit counters). Total RLS surface = 15 tables.

    4. Run tests → MUST pass. Commit `test(224-07): conversion CRUD APIs + RLS suite + migration 133` → `feat(224-07): migration 133 hardening + 9 conversion APIs + RLS coverage`.
  </action>
  <acceptance_criteria>
    - File exists: `supabase/migrations/133_conversion_rls_hardening.sql` with cross-table indexes + REVOKE/GRANT clauses + idempotent CHECK re-add
    - 9 API route files exist
    - `forms/[form_id]/publish/route.ts` mirrors `pages/[page_id]/publish/route.ts` approval-aware pattern from plan 03
    - `internal/conversion/emit/route.ts` references emitConversionEvent and uses F-138 contract
    - `test/conversion/rls-suite.test.ts` covers all 13 SOR tables + 2 dispatch infrastructure tables (15 total — grep for each table name including `conversion_submit_idempotency` AND `conversion_rate_limit_counters`)
    - `test/conversion/rls/conversion-submit-idempotency-rls.test.ts` and `test/conversion/rls/conversion-rate-limit-counters-rls.test.ts` exist and cross-tenant denial is asserted in each
    - `npx vitest run test/conversion/api/ test/conversion/rls-suite.test.ts` exits 0
    - 224-VALIDATION.md row 224-07-01 ✅ green
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/conversion/api/ test/conversion/rls-suite.test.ts</automated>
  </verify>
  <done>13-table RLS hardening + 9 conversion CRUD APIs + 16 audit event types registered + public form ACL clamped; ready for MCP tools in Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 6 MCP tools + F-145/F-146 contracts + flow-registry update</name>
  <files>lib/markos/mcp/tools/publish-page.ts, lib/markos/mcp/tools/submit-form.ts, lib/markos/mcp/tools/evaluate-launch-gates.ts, lib/markos/mcp/tools/execute-runbook.ts, lib/markos/mcp/tools/rollback-launch.ts, lib/markos/mcp/tools/get-launch-outcome.ts, lib/markos/mcp/tools/index.ts, contracts/F-145-launch-evaluate-gates-v1.yaml, contracts/F-146-launch-execute-runbook-v1.yaml, contracts/flow-registry.json, test/mcp/publish-page.tool.test.ts, test/mcp/submit-form.tool.test.ts, test/mcp/evaluate-launch-gates.tool.test.ts, test/mcp/execute-runbook.tool.test.ts, test/mcp/rollback-launch.tool.test.ts, test/mcp/get-launch-outcome.tool.test.ts</files>
  <behavior>
    - Test 1: publish_page tool registered via pipeline.cjs; invoke with valid page_id + approval_ref → returns {ok, page_id, status:'published'}; without approval → pipeline 'approval' step returns 202 with approval_id.
    - Test 2: submit_form tool — server-to-server; bypasses BotID (D-43); calls emitConversionEvent; rate-limit + variables_schema still applied.
    - Test 3: evaluate_launch_gates tool calls evaluateAllLaunchGates; returns aggregate gate state.
    - Test 4: execute_runbook tool — calls executeRunbook; approval-aware via pipeline.
    - Test 5: rollback_launch tool — calls rollbackRunbook; approval-aware.
    - Test 6: get_launch_outcome tool — read-only; returns outcomes for launch_id (T+7/T+14/T+30 if computed).
    - Test 7: All 6 tools flow through pipeline.cjs 10-step middleware (validates auth, rate_limit, tool_lookup, validate_input, free_tier, approval, cost, invoke, validate_output, trueup).
    - Test 8: F-145 + F-146 contracts active in YAML AND flow-registry.
  </behavior>
  <read_first>
    - .planning/phases/224-conversion-launch-workspace/224-CONTEXT.md D-43 (6 MCP tools), D-44 (approval-aware)
    - lib/markos/mcp/pipeline.cjs (existing 10-step middleware)
    - lib/markos/conversion/events/emit.ts (plan 02)
    - lib/markos/launches/gates/evaluate-all.ts (plan 04)
    - lib/markos/launches/runbook/executor.ts (plan 06)
    - lib/markos/launches/runbook/rollback.ts (plan 06)
    - lib/markos/launches/outcomes/compute.ts (plan 04)
    - api/v1/conversion/pages/[page_id]/publish/route.ts (plan 03)
    - contracts/F-141-launch-gate-v1.yaml (plan 04 — graduate the F-145 stub)
    - contracts/F-142-launch-runbook-v1.yaml (plan 06)
  </read_first>
  <action>
    Write tests FIRST.

    1. Create the 6 MCP tool files. Each follows the same pattern (using pipeline.cjs registerTool):

       ```typescript
       // lib/markos/mcp/tools/publish-page.ts
       import { registerTool } from '../pipeline';
       import { z } from 'zod';

       const InputSchema = z.object({ page_id: z.string().uuid(), approval_ref: z.string().optional() });

       registerTool({
         name: 'publish_page',
         description: 'Publish a ConversionPage (validates approval state + content classifier + ISR invalidation)',
         inputSchema: InputSchema,
         approvalPolicy: 'approval-required',
         invoke: async (ctx, input) => {
           // ctx provides tenant_id, supabase, actor; pipeline already ran approval step
           // Delegate to existing publish route logic
           const { default: handler } = await import('../../../api/v1/conversion/pages/[page_id]/publish/route');
           // Or re-implement the publish logic here against ctx.supabase
           // ...
           return { ok: true, page_id: input.page_id, status: 'published' };
         },
       });
       ```

       Implement the 6 tools following this pattern. Each tool wraps the corresponding lib function from prior plans (NOT the route handler — call the lib directly to avoid HTTP layer overhead):
       - **publish_page** wraps the publish route's logic (validates approval, runs classifier, updates status, invalidates cache, audit)
       - **submit_form** wraps emitConversionEvent with full payload validation; bypasses BotID (D-43); preserves rate-limit + variables_schema
       - **evaluate_launch_gates** wraps evaluateAllLaunchGates
       - **execute_runbook** wraps executeRunbook with approval-aware pipeline step
       - **rollback_launch** wraps rollbackRunbook with approval-aware
       - **get_launch_outcome** read-only — queries launch_outcomes for launch_id

    2. Create `lib/markos/mcp/tools/index.ts`: barrel registering all 6 tools when imported.

    3. Create `contracts/F-145-launch-evaluate-gates-v1.yaml`:

       ```yaml
       flow_id: F-145
       title: Launch evaluate gates (MCP tool)
       version: v1
       owner: phase-224
       status: active
       paths:
         /mcp/tools/evaluate_launch_gates:
           post:
             summary: MCP tool — runs all 4 launch gate evaluators for a launch
             security: [{ apiKey: [] }]
             requestBody:
               content:
                 application/json:
                   schema:
                     type: object
                     required: [launch_id]
                     properties: { launch_id: { type: string, format: uuid } }
             responses:
               '200': { description: aggregate gate state with can_publish boolean }
             x-markos-meta:
               approval_policy: read-only
               audit: enabled
       x-markos-meta:
         data_tables: [launch_gates, launch_briefs, launch_surfaces, launch_readiness_checks]
         depends_on: [F-141]
       ```

    4. Create `contracts/F-146-launch-execute-runbook-v1.yaml`:

       ```yaml
       flow_id: F-146
       title: Launch execute runbook (MCP tools — execute + rollback)
       version: v1
       owner: phase-224
       status: active
       paths:
         /mcp/tools/execute_runbook:
           post:
             summary: MCP tool — execute a launch runbook
             security: [{ apiKey: [] }]
             x-markos-meta:
               approval_policy: approval-required
               approval_template: runbook_execute
               audit: enabled
         /mcp/tools/rollback_launch:
           post:
             summary: MCP tool — rollback a launch runbook (reverse-runbook semantics)
             security: [{ apiKey: [] }]
             x-markos-meta:
               approval_policy: approval-required
               approval_template: rollback
               audit: enabled
       x-markos-meta:
         data_tables: [launch_runbooks, runbook_step_log, markos_agent_runs, markos_tasks]
         depends_on: [F-142]
       ```

    5. Update `contracts/flow-registry.json`: add F-145 + F-146 entries.

    6. Create the 6 test files. Each mocks pipeline.cjs registration + asserts invoke behavior + approval gate handling.

    7. Run tests → MUST pass. Commit `test(224-07): 6 MCP tools` → `feat(224-07): 6 MCP tools registered + F-145 + F-146 active`.
  </action>
  <acceptance_criteria>
    - 7 MCP tool files exist under `lib/markos/mcp/tools/` (6 tools + index)
    - Each tool file calls `registerTool` from pipeline.cjs
    - submit_form tool documents BotID bypass for trusted callers (per D-43)
    - F-145 + F-146 YAMLs have `status: active`
    - flow-registry.json contains F-145 + F-146 entries
    - `npx vitest run test/mcp/` exits 0 (6+ test files green)
    - 224-VALIDATION.md row 224-07-02 ✅ green
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/mcp/publish-page.tool.test.ts test/mcp/submit-form.tool.test.ts test/mcp/evaluate-launch-gates.tool.test.ts test/mcp/execute-runbook.tool.test.ts test/mcp/rollback-launch.tool.test.ts test/mcp/get-launch-outcome.tool.test.ts</automated>
  </verify>
  <done>6 MCP tools registered through pipeline.cjs; F-145 + F-146 active; full MCP surface complete (D-43); ready for UI workspace in Task 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ConversionWorkspace + LaunchCockpit + 6 supporting UI components + Approval Inbox + Morning Brief integration</name>
  <files>app/(markos)/conversion/page.tsx, app/(markos)/conversion/PageEditor.tsx, app/(markos)/conversion/FormEditor.tsx, app/(markos)/launches/page.tsx, app/(markos)/launches/LaunchCockpit.tsx, app/(markos)/launches/RunbookEditor.tsx, app/(markos)/launches/LaunchReadinessBoard.tsx, app/(markos)/launches/GatesPanel.tsx, app/(markos)/launches/OutcomesDashboard.tsx, components/markos/conversion/conversion-workspace.module.css, components/markos/launches/launch-cockpit.module.css, lib/markos/operating/approvals/entry-types.ts, lib/markos/operating/morning-brief/launches-section.ts, test/playwright/conversion/conversion-workspace.spec.ts, test/playwright/launches/launch-cockpit.spec.ts, test/playwright/launches/readiness-board.spec.ts, test/chromatic/conversion/ConversionWorkspace.stories.tsx, test/chromatic/conversion/PageEditor.stories.tsx, test/chromatic/launches/LaunchCockpit.stories.tsx, test/chromatic/launches/LaunchReadinessBoard.stories.tsx</files>
  <behavior>
    - Test 1 (Playwright): operator visits /conversion → sees pages kanban (10 page_type variants) + forms list + CTAs + experiments + recent ConversionEvents stream.
    - Test 2 (Playwright): operator opens PageEditor → JSON content_blocks editor on left + preview pane on right (renders D-03 with mock pricing) + Pricing/Evidence binding inspector + Publish CTA → click Publish → approval flow triggered → operator approves → page status flips to published.
    - Test 3 (Playwright): operator visits /launches → LaunchCockpit shows briefs kanban + GatesPanel for selected launch + Surface board + RunbookEditor + OutcomesDashboard.
    - Test 4 (Playwright): readiness-board.spec — operator marks readiness check as completed → gate readiness re-evaluates → status flips passing.
    - Test 5 (Chromatic): ConversionWorkspace.stories renders draft + publishing + published + archived states.
    - Test 6 (Chromatic): LaunchCockpit.stories renders planning/pending_approval/ready/live/completed/rolled_back states.
    - Test 7: Approval Inbox extended with 6 entry types: page_publish, form_publish, launch_arm, launch_execute, gate_waiver, rollback (entry-types.ts exports the type union).
    - Test 8: Morning Brief launches-section query returns top-3 in-flight launches + readiness countdown + blocking gates per launch + ConversionEvent volume 24h.
  </behavior>
  <read_first>
    - .planning/phases/224-conversion-launch-workspace/224-CONTEXT.md D-45..D-47
    - .planning/phases/224-conversion-launch-workspace/224-RESEARCH.md §UI Compatibility Note lines 1402-1412
    - app/(markos)/layout-shell.tsx (single shell)
    - app/(markos)/_components/ existing components if any
    - components/markos/conversion/blocks/ (plan 03 — block components for preview)
    - components/markos/ existing pattern (CSS modules; no Tailwind unless prior phase established)
    - .agent/markos/templates/LINEAR-TASKS/MARKOS-ITM-OPS-01-campaign-launch.md (Linear template informs RunbookEditor)
    - kanban-view.tsx or similar from prior phase (P102 D-02 reference per RESEARCH.md)
    - lib/markos/operating/approvals/ if exists (P208)
    - lib/markos/operating/morning-brief/ if exists (P208)
  </read_first>
  <action>
    Write tests FIRST (Playwright + Chromatic). Sketch UI pseudocode below — components follow existing CSS module conventions.

    1. **app/(markos)/conversion/page.tsx** (server component): loads pages + forms + CTAs + experiments + recent events from supabase; renders ConversionWorkspace with kanban (status columns: draft/pending_approval/published/archived).

    2. **app/(markos)/conversion/PageEditor.tsx** (client component): JSON editor (`<textarea>` with onBlur JSON.parse + validateContentBlocks from plan 01); preview pane invokes renderConversionPage (plan 03) with mock pricing/evidence; binding inspector lists all `{{pricing.*}}` + `{{evidence.*}}` references in the JSON; Publish button → POST `/api/v1/conversion/pages/{id}/publish` → 202 if approval needed → poll for completion.

    3. **app/(markos)/conversion/FormEditor.tsx** (client component): field list editor + variables_schema editor (Monaco or simple textarea) + identity_stitch_enabled toggle + thank_you_page link picker + preview using ConversionFormRenderer (plan 03).

    4. **app/(markos)/launches/page.tsx** (server component): renders LaunchCockpit with briefs kanban + selected launch detail panel (GatesPanel + Surface board + RunbookEditor + OutcomesDashboard).

    5. **app/(markos)/launches/LaunchCockpit.tsx**: top-level layout. Briefs kanban groups by status; selecting a brief loads detail tabs (Gates / Surfaces / Runbook / Outcomes / Readiness).

    6. **app/(markos)/launches/RunbookEditor.tsx**: ordered steps list with drag-to-reorder; each step shows kind dropdown, target_ref picker, depends_on multi-select, idempotency_key auto-generated; rollback_steps mirror; AgentRun status panel showing runbook.state + runbook.current_step + per-step log_id from runbook_step_log.

    7. **app/(markos)/launches/LaunchReadinessBoard.tsx**: dependency graph (use existing graph component if present, else simple list) of readiness_checks; LaunchGate status indicators (4 badges per launch); countdown to launch_date.

    8. **app/(markos)/launches/GatesPanel.tsx**: 4 gate cards (pricing, evidence, readiness, approval); shows status + blocking_reasons; Evaluate button → POST `/api/v1/launches/{id}/gates/evaluate`; Waive button (admin only) → modal with waiver_reason input.

    9. **app/(markos)/launches/OutcomesDashboard.tsx**: 3-column display for T+7/T+14/T+30 metrics (reach, signups, pipeline_created, influenced_revenue); narrative_summary editable.

    10. **CSS modules**: `conversion-workspace.module.css` + `launch-cockpit.module.css` with styles for kanban columns, cards, buttons. Follow existing component patterns.

    11. **Layout-shell extension**: update `app/(markos)/layout-shell.tsx` to add nav entries `/conversion` + `/launches` (additive — preserve existing entries from P201/P203).

    12. **Approval Inbox + Morning Brief integration**:
        - `lib/markos/operating/approvals/entry-types.ts`: export `P224_APPROVAL_ENTRY_TYPES = ['page_publish', 'form_publish', 'launch_arm', 'launch_execute', 'gate_waiver', 'rollback']` and ensure these strings are accepted in P208 Approval Inbox queries (the inbox column entry_type is free-form per D-46 — no schema migration needed).
        - `lib/markos/operating/morning-brief/launches-section.ts`: export `getLaunchesSection(supabase, tenant_id)` returning `{ inflight_launches: [...top3], readiness_countdown: {...}, blocking_gates: [...], conversion_event_volume_24h: number }`. P208 Morning Brief renderer extends its data query with this section.

    13. **Playwright + Chromatic specs**: create 3 Playwright specs (conversion-workspace, launch-cockpit, readiness-board) and 4 Chromatic stories (ConversionWorkspace, PageEditor, LaunchCockpit, LaunchReadinessBoard).

    14. Run all UI-related tests → pass. Commit `test(224-07): UI workspaces + Approval Inbox + Morning Brief integration` → `feat(224-07): ConversionWorkspace + LaunchCockpit + 6 components + integrations`.
  </action>
  <acceptance_criteria>
    - 9 UI files exist under `app/(markos)/conversion/` and `app/(markos)/launches/`
    - 2 CSS modules exist
    - `app/(markos)/layout-shell.tsx` references both `/conversion` and `/launches` nav entries
    - `lib/markos/operating/approvals/entry-types.ts` exports `P224_APPROVAL_ENTRY_TYPES` containing all 6 entry types
    - `lib/markos/operating/morning-brief/launches-section.ts` exports `getLaunchesSection`
    - 3 Playwright specs + 4 Chromatic stories exist
    - `npx playwright test test/playwright/conversion/conversion-workspace.spec.ts test/playwright/launches/launch-cockpit.spec.ts test/playwright/launches/readiness-board.spec.ts` exits 0 (or skipped with reason)
    - `npx chromatic --exit-zero-on-changes` runs (or stories validated via Storybook test runner)
    - 224-VALIDATION.md rows 224-07-03 + 224-07-04 ✅ green
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test test/playwright/conversion/conversion-workspace.spec.ts test/playwright/launches/launch-cockpit.spec.ts test/playwright/launches/readiness-board.spec.ts</automated>
  </verify>
  <done>UI workspaces shipped under single-shell rule (D-45); Approval Inbox + Morning Brief integration via P208 extension hooks; Playwright + Chromatic coverage complete; ready for OpenAPI regen + closeout in Task 4.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: OpenAPI regen + llms.txt + docs + legacy regression suite + final VALIDATION sign-off</name>
  <files>public/openapi.json, public/llms.txt, docs/conversion/index.md, docs/launches/index.md, test/api/openapi-parity.test.ts, test/regression/p221-adapter.regression.test.ts, test/regression/p222-crm360.regression.test.ts, test/regression/p223-channels.regression.test.ts, test/regression/existing-marketing-routes.regression.test.ts, .planning/phases/224-conversion-launch-workspace/224-VALIDATION.md</files>
  <behavior>
    - Test 1: `node scripts/openapi-merge.cjs` (or equivalent existing OpenAPI generator) produces public/openapi.json containing all 17 P224 F-IDs (F-132..F-146); openapi-parity test asserts each F-ID's paths and schemas resolve.
    - Test 2: public/llms.txt contains a Phase 224 section with: F-IDs list, public form submit URL example, MCP tool list, Approval Inbox entry types.
    - Test 3: docs/conversion/index.md and docs/launches/index.md render markdown without broken links.
    - Test 4: Legacy regression p221-adapter — ConsentState read/write contract still works after P224 emit() bridge stub usage.
    - Test 5: Legacy regression p222-crm360 — Customer360 lifecycle progression still works (mocked to assert P224 emit() does not interfere with existing P222 contract).
    - Test 6: Legacy regression p223-channels — email_campaigns + messaging_threads + lifecycle_journeys reachable as LaunchSurface targets without P223 contract breakage.
    - Test 7: Legacy regression existing-marketing-routes — `/signup`, `/integrations/claude`, `/docs` routes return 200 with their pre-P224 markup unchanged.
    - Test 8: 224-VALIDATION.md updated with `nyquist_compliant: true` AND all per-task rows ✅ green AND wave_0_complete: true (set in plan 01 already).
  </behavior>
  <read_first>
    - public/openapi.json (existing — preserve P200..P223 paths)
    - public/llms.txt (existing — Phase 204 section was last update; append P224 section)
    - scripts/openapi-merge.cjs or equivalent
    - 224-VALIDATION.md (current state)
    - .planning/phases/221-cdp-identity-audience-consent-substrate/ test patterns (regression reference)
    - .planning/phases/222-crm-timeline-commercial-memory-workspace/ test patterns
    - .planning/phases/223-native-email-messaging-orchestration/ test patterns
    - app/(marketing)/signup/page.tsx (legacy preserved)
    - app/(marketing)/integrations/claude/page.tsx (legacy preserved)
    - app/(marketing)/docs/[[...slug]]/page.tsx (legacy preserved)
  </read_first>
  <action>
    Write tests FIRST.

    1. Run the existing OpenAPI merge script (whichever script regenerates `public/openapi.json` from contracts/F-*.yaml). Verify output contains all 17 P224 F-IDs (F-132..F-146). If the merge script does not exist, create a minimal one at `scripts/openapi-merge-p224.cjs`:

       ```javascript
       // Reads contracts/F-*.yaml, validates each, merges into public/openapi.json
       const fs = require('node:fs');
       const yaml = require('yaml');
       const path = require('node:path');

       const contracts = fs.readdirSync('contracts').filter(f => f.match(/^F-(\d+)-.*\.yaml$/));
       const openapi = JSON.parse(fs.readFileSync('public/openapi.json', 'utf8'));
       openapi.paths = openapi.paths ?? {};
       for (const file of contracts) {
         const c = yaml.parse(fs.readFileSync(path.join('contracts', file), 'utf8'));
         if (c.status !== 'active') continue;
         Object.assign(openapi.paths, c.paths);
         openapi['x-markos-flows'] = openapi['x-markos-flows'] ?? {};
         openapi['x-markos-flows'][c.flow_id] = { title: c.title, version: c.version };
       }
       fs.writeFileSync('public/openapi.json', JSON.stringify(openapi, null, 2));
       ```

       Run the script. Verify `public/openapi.json` contains all 17 F-IDs.

    2. Append Phase 224 section to `public/llms.txt`:

       ```text
       ## Phase 224: Conversion and Launch Workspace

       17 contracts: F-132 (conversion-page) through F-146 (launch-execute-runbook).

       Public form submit endpoint:
         POST /api/v1/public/conversion/forms/{form_id}/submit
         Headers: idempotency-key (optional UUID)
         Security envelope: Vercel BotID + rate-limit (10/IP/60s + 3/email/60s) + honeypot + ConsentState double-gate + variables_schema validation + emit() fan-out

       MCP tools:
         - publish_page
         - submit_form
         - evaluate_launch_gates
         - execute_runbook
         - rollback_launch
         - get_launch_outcome

       Approval Inbox entry types:
         - page_publish, form_publish
         - launch_arm, launch_execute, gate_waiver, rollback

       UI workspaces:
         - /conversion (ConversionWorkspace)
         - /launches (LaunchCockpit)

       13 new tables (migrations 121..133); RLS enforced; audit on every mutation via P201 hash chain.

       See docs/conversion/index.md and docs/launches/index.md.
       ```

    3. Create `docs/conversion/index.md` documenting:
       - 13 P224 tables (brief description each)
       - 15 block types with valid JSON examples
       - Public form security envelope
       - emit() 7-sink fan-out semantics
       - Block schema validation + placeholder rule
       - URL conventions (/v1/conversion/* + /v1/public/conversion/*)

       Create `docs/launches/index.md` documenting:
       - LaunchBrief lifecycle (planning → completed)
       - 4 gate evaluators
       - Polymorphic LaunchSurface (9 surface_target_kind values)
       - Runbook step kinds + rollback semantics
       - LaunchOutcome periods + first-touch attribution

    4. Create regression tests:
       - `test/regression/p221-adapter.regression.test.ts`: imports lib/markos/cdp/adapters/crm-projection (if present) and asserts public signature unchanged AND that emit() with P221 adapter mocked does not write conflicting data.
       - `test/regression/p222-crm360.regression.test.ts`: similar for crm360 adapter.
       - `test/regression/p223-channels.regression.test.ts`: similar for channels adapter; assert email_campaigns table read returns expected shape; assert P223 send paths are not invoked by P224 LaunchSurface coordinator (LaunchSurface only coordinates; P223 owns dispatch).
       - `test/regression/existing-marketing-routes.regression.test.ts`: SSR-tests /signup, /integrations/claude, /docs returning expected HTML; uses fetch against test server.

    5. Update `224-VALIDATION.md`:
       - Set `nyquist_compliant: true`
       - Set `wave_0_complete: true` (already set in plan 01)
       - Mark all rows for 224-01..07 as ✅ green
       - Add a final summary row: 224-07-04 (closeout) ✅ green
       - Add Sign-Off section with date + planner approval

    6. Run all tests → pass. Commit `test(224-07): OpenAPI parity + legacy regression + sign-off` → `docs(224-07): OpenAPI regen + llms.txt P224 section + docs + closeout`.
  </action>
  <acceptance_criteria>
    - `public/openapi.json` regenerated with 17 P224 F-IDs (grep for `F-132`, `F-133`, ..., `F-146`)
    - `public/llms.txt` contains a `## Phase 224:` section
    - `docs/conversion/index.md` and `docs/launches/index.md` exist with content
    - 4 regression test files exist
    - `npx vitest run test/api/openapi-parity.test.ts test/regression/` exits 0
    - 224-VALIDATION.md frontmatter shows `nyquist_compliant: true` and `wave_0_complete: true`
    - All per-task rows for 224-01..07 are ✅ green
    - 224-VALIDATION.md row 224-07-05 ✅ green; final Sign-Off section added
    - `test/migrations/order.test.ts` exists and verifies migration runner applies in wave-execution order (W1→W2→W3→W4→W5), NOT numeric order; assertion fails if a Wave-3 migration (127/129/131) is applied before the Wave-2 migration (130)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/api/openapi-parity.test.ts test/regression/</automated>
  </verify>
  <done>OpenAPI regenerated with 17 P224 F-IDs; llms.txt Phase 224 section + docs landed; 4 regression test files green; 224-VALIDATION.md `nyquist_compliant: true`; ready for the final operator-journey checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Operator journey verification (full P224 system)</name>
  <what-built>
    The complete Phase 224 system: 13 tables + 17 contracts + 6 MCP tools + ConversionWorkspace + LaunchCockpit + Approval Inbox + Morning Brief integration + cron handlers + reverse-runbook rollback + ISR-cached public conversion pages.

    All work in plans 224-01 through 224-07 has been automated by Claude. This checkpoint verifies the human-facing behavior end-to-end.
  </what-built>
  <how-to-verify>
    Run the following journey on a local dev tenant. All steps assume P221/P222/P223 either shipped or running with bridge stubs.

    1. **Spin up local dev**: `npm run dev` and confirm server boots without errors. `npm test` should pass (legacy regression).

    2. **Verify public conversion page render**:
       (a) Create a ConversionPage via API: `curl -X POST http://localhost:3000/api/v1/conversion/pages -H "Content-Type: application/json" -d '{"page_type":"landing","slug":"test-landing","title":"Test","objective":"trial_signup","content_blocks":[{"block_type":"hero","heading":"Hello"}]}'`
       (b) Publish it (via UI or API with approval): operator visits `/conversion`, finds the draft, clicks Publish, approves in inbox.
       (c) Visit `http://<tenant-host>/test-landing` — expect SSR-rendered "Hello" heading; view page source to confirm SSR (not client-render).

    3. **Verify form submit + emit() fan-out**:
       (a) Create a ConversionForm with email field; publish.
       (b) Add a FormBlock to the test-landing page; republish.
       (c) Submit the form on the public page; expect 200 + thank_you redirect.
       (d) Query `conversion_events` + `cdp_events` + `crm_activity` — expect 3 rows with shared source_event_ref.
       (e) Submit again with same Idempotency-Key header — expect 200 + body.idempotent_replay=true; no new conversion_events row.

    4. **Verify BotID + rate-limit + honeypot**:
       (a) Submit 11 times rapidly from same IP — 11th expect 429.
       (b) View page source; locate the hidden honeypot input; submit with that input populated — expect silent 200 + no conversion_events row.

    5. **Verify launch journey**:
       (a) Visit `/launches`. Create LaunchBrief (feature launch).
       (b) Add 2 readiness checks (legal_approved, sales_trained); leave one incomplete.
       (c) Add a LaunchSurface targeting the test-landing page (surface_target_kind='conversion_page', surface_target_id=<page_id>).
       (d) Click Evaluate Gates → expect readiness blocking ("readiness_incomplete:legal_approved:..." OR similar reason).
       (e) Mark legal_approved as completed; re-evaluate → expect readiness passing.
       (f) Add pricing context + verify pricing gate passing (or use placeholder).
       (g) Transition brief to ready → live.
       (h) Create runbook with 2 steps (publish_surface + notify_team) and 2 rollback_steps.
       (i) Arm runbook (gates re-evaluate); execute (with approval); verify launch_runbooks.state = 'executed'; runbook_step_log has 2 forward rows.
       (j) Trigger rollback (with approval); verify state='rolled_back'; runbook_step_log has 2 rollback rows; surface archived.
       (k) Compute outcome at T+7 (manually trigger via API): verify launch_outcomes row exists with reach + signups counts.

    6. **Verify Approval Inbox extension**: All 6 entry types (page_publish, form_publish, launch_arm, launch_execute, gate_waiver, rollback) appear in inbox during the above journey.

    7. **Verify Morning Brief**: visit Morning Brief surface; expect launches-section showing the test launch + readiness countdown + ConversionEvent volume.

    8. **Verify cron jobs work locally** (or in deployed env):
       (a) Manually trigger surface-health-audit; expect no false positives on healthy pages.
       (b) Manually trigger gate-evaluation-poll; expect launches re-evaluated.
       (c) Manually trigger outcome-computation; expect rows for live launches at T+7.
       (d) Check cron_run_log for status='succeeded' entries.

    9. **Verify legacy routes unchanged**: visit `/signup`, `/integrations/claude`, `/docs` — confirm pre-P224 markup intact.

    10. **Run full test suite**: `npx vitest run && npx playwright test && npm test` — all green.

    11. **Verify Chromatic**: `npx chromatic` — confirm visual regression baseline updated for P224 components.

    12. **OpenAPI parity**: `cat public/openapi.json | jq '."x-markos-flows" | keys' | grep "F-1[3-4][0-9]"` — should list F-132..F-146.

    13. **llms.txt**: `cat public/llms.txt | grep -A 5 "Phase 224"` — confirm section.

    Approve only when ALL 13 verification points pass. If any fail, file a gap-closure plan via `/gsd-plan-phase 224 --gaps`.
  </how-to-verify>
  <resume-signal>Type "approved" if all 13 verification points pass, otherwise list the failing item(s) (e.g., "5(d) failed: rollback step status='failed' instead of 'succeeded'")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP server-to-server → submit_form | Trusted caller bypasses BotID per D-43; rate-limit + variables_schema still enforced |
| operator UI → API mutations | Approval-aware via P105 createApprovalPackage; 6 entry types added (D-46) |
| public OpenAPI exposure | All 17 P224 F-IDs documented; tenancy enforced via RLS at runtime |
| legacy code → P224 fan-out | api/tracking/ingest.js retrofit is additive; P221/P222/P223 adapters bridge-stubbed where absent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-224-07-01 | Information Disclosure | OpenAPI exposes internal F-138 emit endpoint | mitigate | F-138 tagged `security: [{ apiKey: [] }]`; only authenticated MCP callers can hit it |
| T-224-07-02 | Tampering | MCP submit_form bypassing BotID enables spam path | accept | submit_form requires API key auth (server-to-server only); rate-limit still enforced; per D-43 documented behavior |
| T-224-07-03 | Elevation of Privilege | Operator UI lets non-admin approve a high-risk mutation | mitigate | Approval Inbox honors P208 RBAC; entry types page_publish/launch_execute/rollback/gate_waiver require admin role per P105/P208 conventions |
| T-224-07-04 | Repudiation | Audit log entries lost during ISR cache invalidation transient errors | mitigate | All P224 audit writes are synchronous in their respective routes (publish, archive, gate eval, runbook step); Pitfall 3 ensures ISR invalidation is part of the synchronous transaction |
| T-224-07-05 | Information Disclosure | llms.txt over-exposes P224 internals | accept | llms.txt content reviewed; only public-facing endpoints + MCP tool names documented; no secrets or internal paths |
| T-224-07-06 | Tampering | Legacy regression false negatives | mitigate | 4 regression test files cover P221/P222/P223/marketing routes; CI runs both `npm test` AND `npx vitest run` to catch both Node --test and Vitest paths |
</threat_model>

<verification>
1. `npx vitest run` — full Vitest suite green (40+ files for 224-01..07).
2. `npm test` — legacy node --test suite green (P201 + P204 + P221/222/223 if shipped).
3. `npx playwright test` — all Playwright specs green (or skipped with reason).
4. `npx chromatic` — visual regression baseline matches.
5. `cat public/openapi.json | jq '."x-markos-flows" | length'` — returns at least 17 (F-132..F-146).
6. `grep "Phase 224" public/llms.txt` — returns 1 match.
7. 224-VALIDATION.md frontmatter `nyquist_compliant: true`.
8. Operator journey checkpoint approved.
</verification>

<success_criteria>
- Migration 133 with cross-table indexes + audit_log event registry + public form ACL
- 9 conversion CRUD APIs (deferred from plan 03) shipped
- 6 MCP tools registered through pipeline.cjs (publish_page + submit_form + evaluate_launch_gates + execute_runbook + rollback_launch + get_launch_outcome)
- F-145 + F-146 contracts active; flow-registry contains 17 P224 F-IDs
- 8 UI components in app/(markos)/conversion/* and app/(markos)/launches/* under single-shell rule
- Approval Inbox + Morning Brief P208 integration via entry-types.ts + launches-section.ts
- OpenAPI 3.1 regenerated with 17 F-IDs; llms.txt Phase 224 section; 2 docs files
- Full RLS suite for 13 tables + cross-table joins
- Legacy regression suite (4 files) green: P221 + P222 + P223 + existing marketing routes
- Playwright operator journeys + Chromatic visual regression
- 224-VALIDATION.md `nyquist_compliant: true` with all per-task rows ✅ green
- Operator journey checkpoint approved by user (autonomous=false plan; final blocking gate)
- All phase requirement IDs (CNV/LCH-01..05 direct + PRC/CDP/CRM/EML/MSG-01..05 carry-forward + QA-01..15) addressed
</success_criteria>

<output>
After completion, create `.planning/phases/224-conversion-launch-workspace/224-07-SUMMARY.md` documenting:
- Final 17 contracts (F-132..F-146) + 13 tables + 6 MCP tools + 8 UI components + 4 cron handlers + 9 conversion CRUD APIs + 9 launch APIs from plan 04 + 5 runbook APIs from plan 06
- Approval Inbox + Morning Brief integration posture
- OpenAPI + llms.txt + docs delivered
- Test count: 40+ Vitest files + 4 Playwright + 4 Chromatic + 4 regression
- 224-VALIDATION.md sign-off
- Decisions: MCP submit_form bypasses BotID by design (D-43); single-shell rule preserved; bridge stubs for absent prior phases (A3-A7); first-touch attribution v1
- Risks: when P221/P222/P223 ship after P224, the bridge stubs upgrade transparently, but operators may see different behaviors (e.g., ConsentState now persists vs only audited); document in P224 release notes
- Phase 224 ready for `/gsd-verify-phase 224`
</output>
