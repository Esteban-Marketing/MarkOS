---
phase: 224
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - test/fixtures/conversion/page.ts
  - test/fixtures/conversion/form.ts
  - test/fixtures/conversion/cta.ts
  - test/fixtures/conversion/event.ts
  - test/fixtures/conversion/experiment.ts
  - test/fixtures/conversion/blocks.ts
  - test/fixtures/launches/brief.ts
  - test/fixtures/launches/surface.ts
  - test/fixtures/launches/gate.ts
  - test/fixtures/launches/runbook.ts
  - test/fixtures/launches/outcome.ts
  - test/fixtures/launches/readiness.ts
  - test/fixtures/conversion/index.ts
  - test/fixtures/launches/index.ts
  - test/fixtures/smoke.test.ts
  - supabase/migrations/121_conversion_pages.sql
  - supabase/migrations/122_conversion_forms.sql
  - supabase/migrations/123_conversion_events.sql
  - supabase/migrations/124_conversion_experiments.sql
  - supabase/migrations/125_launch_briefs.sql
  - supabase/migrations/126_launch_surfaces.sql
  - lib/markos/conversion/index.ts
  - lib/markos/conversion/contracts/page.ts
  - lib/markos/conversion/contracts/form.ts
  - lib/markos/conversion/contracts/cta.ts
  - lib/markos/conversion/contracts/event.ts
  - lib/markos/conversion/contracts/experiment.ts
  - lib/markos/conversion/blocks/schema.ts
  - lib/markos/launches/index.ts
  - lib/markos/launches/contracts/brief.ts
  - lib/markos/launches/contracts/surface.ts
  - lib/markos/launches/contracts/gate.ts
  - lib/markos/launches/contracts/runbook.ts
  - lib/markos/launches/contracts/outcome.ts
  - lib/markos/launches/contracts/readiness.ts
  - contracts/F-132-conversion-page-v1.yaml
  - contracts/F-133-conversion-form-v1.yaml
  - contracts/F-134-conversion-cta-v1.yaml
  - contracts/F-135-conversion-event-v1.yaml
  - contracts/F-136-conversion-experiment-v1.yaml
  - contracts/F-139-launch-brief-v1.yaml
  - contracts/F-140-launch-surface-v1.yaml
  - contracts/F-141-launch-gate-v1.yaml
  - contracts/F-143-launch-outcome-v1.yaml
  - contracts/F-144-launch-readiness-check-v1.yaml
  - contracts/flow-registry.json
  - test/conversion/schema/conversion-page.test.ts
  - test/conversion/schema/conversion-form.test.ts
  - test/conversion/schema/conversion-cta.test.ts
  - test/conversion/schema/conversion-event.test.ts
  - test/conversion/schema/conversion-experiment.test.ts
  - test/conversion/schema/experiment-variants.test.ts
  - test/conversion/schema/experiment-assignments.test.ts
  - test/conversion/blocks/schema-validators.test.ts
  - test/launches/schema/launch-brief.test.ts
  - test/launches/schema/launch-surface.test.ts
  - test/launches/schema/launch-readiness-check.test.ts
  - test/conversion/rls/conversion-pages.rls.test.ts
  - test/conversion/rls/conversion-forms.rls.test.ts
  - test/conversion/rls/conversion-ctas.rls.test.ts
  - test/conversion/rls/conversion-events.rls.test.ts
  - test/conversion/rls/conversion-experiments.rls.test.ts
  - test/conversion/rls/experiment-variants.rls.test.ts
  - test/conversion/rls/experiment-assignments.rls.test.ts
  - test/launches/rls/launch-briefs.rls.test.ts
  - test/launches/rls/launch-surfaces.rls.test.ts
  - test/launches/rls/launch-readiness-checks.rls.test.ts
  - .planning/phases/224-conversion-launch-workspace/224-VALIDATION.md
autonomous: true
requirements:
  - CNV-01
  - CNV-04
  - CNV-05
  - LCH-01
  - LCH-03
  - QA-01
  - QA-02
must_haves:
  truths:
    - "Wave 0 baseline verified: F-131 is highest existing F-ID and migration 120 is highest existing migration before P224 starts (A1/A2)"
    - "13 new tables (10 in this plan; launch_gates, launch_runbooks, launch_outcomes deferred to plans 04/06) are tenant-scoped, RLS-enforced systems of record per D-01, D-05, D-09, D-12, D-13, D-21, D-22, D-23, D-51"
    - "Block schema validators in lib/markos/conversion/blocks/schema.ts cover all 15 D-02 block types (hero, form, cta, testimonial, pricing, faq, footer, content, image, video, comparison, signup_widget, social_proof, evidence_block, custom_html) with admin-only enforcement on custom_html"
    - "ConversionPage.slug is UNIQUE per tenant (D-01); content_blocks is JSONB array; status state machine enforced via CHECK"
    - "conversion_events.source_event_ref is UUID NOT NULL — shared with cdp_events row in plan 02 fan-out (D-09, D-10)"
    - "launch_surfaces enforces polymorphic CHECK constraint on surface_target_kind (9 values per D-14): email_campaign, messaging_thread, lifecycle_journey, conversion_page, social_pack, sales_enablement, partner_pack, support_pack, docs_update"
    - "experiment_assignments has UNIQUE INDEX on (experiment_id, identity_ref) enforcing sticky assignment per identity (D-23)"
    - "F-132..F-136, F-139..F-141, F-143, F-144 contracts (10) registered in flow-registry.json with read-only stubs; write paths complete in later plans"
    - "test/fixtures/conversion/* and test/fixtures/launches/* fixture factories ship with all 13 fixture files so 224-02..07 import without re-defining shapes"
    - "Module trees lib/markos/conversion/* and lib/markos/launches/* exist with contracts subfolders; renderer/forms/events/gates/runbook/surfaces/outcomes subfolders are stubbed empty (filled in later plans)"
    - "All P100-P105 + P201 + P204 tests stay green after this plan (additive phase, legacy regression)"
  artifacts:
    - path: supabase/migrations/121_conversion_pages.sql
      provides: conversion_pages table + RLS + indexes (tenant_id+slug UNIQUE, tenant_id+status, audience_id partial)
    - path: supabase/migrations/122_conversion_forms.sql
      provides: conversion_forms + conversion_ctas tables + RLS + status indexes
    - path: supabase/migrations/123_conversion_events.sql
      provides: conversion_events table + RLS + indexes (surface_id+occurred_at, launch_id partial, source_event_ref, identity_ref)
    - path: supabase/migrations/124_conversion_experiments.sql
      provides: conversion_experiments + experiment_variants + experiment_assignments tables + RLS + UNIQUE(experiment_id, identity_ref)
    - path: supabase/migrations/125_launch_briefs.sql
      provides: launch_briefs + launch_readiness_checks tables + RLS + indexes (tenant_id+status, tenant_id+launch_date)
    - path: supabase/migrations/126_launch_surfaces.sql
      provides: launch_surfaces table + polymorphic CHECK (9 values) + RLS + indexes (launch_id+status, surface_target_kind+surface_target_id)
    - path: lib/markos/conversion/blocks/schema.ts
      provides: BlockSchemas record (15 Zod validators) + validateBlock() + BlockType union
    - path: lib/markos/conversion/contracts/page.ts
      provides: ConversionPage TypeScript type matching D-01 columns + status state machine type
    - path: lib/markos/conversion/contracts/form.ts
      provides: ConversionForm TypeScript type matching D-05 columns + field types per D-06
    - path: lib/markos/conversion/contracts/event.ts
      provides: ConversionEvent + ConversionEventPayload types matching D-09 + emit() input/output shapes (filled in plan 02)
    - path: lib/markos/conversion/contracts/experiment.ts
      provides: ConversionExperiment + ExperimentVariant + ExperimentAssignment types per D-21..D-23
    - path: lib/markos/launches/contracts/brief.ts
      provides: LaunchBrief type per D-12 + status state machine type
    - path: lib/markos/launches/contracts/surface.ts
      provides: LaunchSurface type per D-12 + SurfaceTargetKind union (9 values per D-14)
    - path: contracts/F-132-conversion-page-v1.yaml
      provides: F-132 read-write CRUD contract for ConversionPage + publish/archive transitions (publish gate field referenced)
    - path: contracts/F-133-conversion-form-v1.yaml
      provides: F-133 read-write CRUD contract for ConversionForm
    - path: contracts/F-134-conversion-cta-v1.yaml
      provides: F-134 read-write CRUD contract for ConversionCTA
    - path: contracts/F-135-conversion-event-v1.yaml
      provides: F-135 read-only contract for ConversionEvent stream queries
    - path: contracts/F-136-conversion-experiment-v1.yaml
      provides: F-136 read-write contract for ConversionExperiment + ExperimentVariant
    - path: contracts/F-139-launch-brief-v1.yaml
      provides: F-139 read-write contract for LaunchBrief
    - path: contracts/F-140-launch-surface-v1.yaml
      provides: F-140 read-write contract for LaunchSurface (polymorphic ref)
    - path: contracts/F-141-launch-gate-v1.yaml
      provides: F-141 read-write contract stub for LaunchGate (full evaluator in plan 04)
    - path: contracts/F-143-launch-outcome-v1.yaml
      provides: F-143 read-only contract stub for LaunchOutcome (compute in plan 04)
    - path: contracts/F-144-launch-readiness-check-v1.yaml
      provides: F-144 read-write contract for LaunchReadinessCheck
    - path: test/fixtures/conversion/page.ts
      provides: Page fixture factory with 10 page_type variants × 4 status variants
    - path: test/fixtures/conversion/blocks.ts
      provides: 15 block type fixtures (valid + invalid payloads each)
    - path: .planning/phases/224-conversion-launch-workspace/224-VALIDATION.md
      provides: Per-task verification rows populated for 224-01 (10 schema tests + 10 RLS tests + block validators + fixture factories)
  key_links:
    - from: lib/markos/conversion/blocks/schema.ts
      to: lib/markos/conversion/contracts/page.ts
      via: BlockType union exported from schema.ts; ConversionPage.content_blocks typed as Block[]
    - from: supabase/migrations/121_conversion_pages.sql
      to: supabase/migrations/100_crm_schema_identity_graph_hardening.sql
      via: tenant_id FK references markos_tenants(id) — same tenant model as P100 baseline
    - from: supabase/migrations/126_launch_surfaces.sql
      to: D-14 polymorphic CHECK constraint
      via: CHECK (surface_target_kind IN ('email_campaign','messaging_thread','lifecycle_journey','conversion_page','social_pack','sales_enablement','partner_pack','support_pack','docs_update'))
    - from: contracts/F-132-conversion-page-v1.yaml
      to: contracts/flow-registry.json
      via: F-132..F-136 + F-139..F-141 + F-143 + F-144 (10 entries) registered alongside F-105 baseline
    - from: test/fixtures/conversion/index.ts
      to: test/fixtures/launches/index.ts
      via: Both fixture indexes re-export their factories for downstream plans 02..07 to import as `from '../../fixtures/conversion'`
---

<objective>
Establish the Phase 224 schema foundation: 10 new Supabase tables across migrations 121-126 with RLS, the 15-block Zod validator library at `lib/markos/conversion/blocks/schema.ts`, TypeScript contract types for both lib/markos/conversion/* and lib/markos/launches/*, 10 F-ID contract YAML stubs (F-132..F-136 conversion + F-139..F-141, F-143, F-144 launch), test fixture factories under test/fixtures/conversion/ and test/fixtures/launches/, and per-table schema + RLS test coverage. Wave 0 first task verifies F-131 + migration 120 baseline (A1/A2/A12 from RESEARCH.md Assumption Log) and confirms Next.js 13.5+ in package.json (A11/A16).

Purpose: satisfy CNV-01 (native conversion surfaces — schema substrate), CNV-04 (RLS + contract-backed), CNV-05 (no parallel SOR — ExperimentSet native, LaunchSurface polymorphic), LCH-01 (governed launch object model), LCH-03 (launch_briefs shape with audience/proof/pricing/metrics), QA-01 (10 new F-IDs registered in flow-registry.json), QA-02 (RLS on 10 of 13 new tables; remaining 3 land in plans 04/06).

Output: 6 migrations + 17 library files (10 contract types + 1 block schema + 6 module index/stub files) + 10 contract YAMLs + 12 fixture files + 23 test files + flow-registry update + populated 224-VALIDATION.md per-task rows. After this plan, Wave 2 plans (224-02 fan-out, 224-03 renderer) can `import { conversionPageFixture, blockSchemaValidate } from '../../fixtures/conversion'` and run schema-level assertions without scaffolding.
</objective>

<context>
@.planning/phases/224-conversion-launch-workspace/224-CONTEXT.md
@.planning/phases/224-conversion-launch-workspace/224-RESEARCH.md
@.planning/phases/224-conversion-launch-workspace/224-VALIDATION.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/V4.0.0-TESTING-ENVIRONMENT-PLAN.md
@.planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md
@.planning/phases/221-cdp-identity-audience-consent-substrate/221-CONTEXT.md
@.planning/phases/222-crm-timeline-commercial-memory-workspace/222-CONTEXT.md
@.planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md
@.planning/phases/223-native-email-messaging-orchestration/223-01-PLAN.md
@CLAUDE.md
@obsidian/reference/Contracts Registry.md
@obsidian/reference/Database Schema.md
@obsidian/reference/HTTP Layer.md
@obsidian/reference/UI Components.md
@obsidian/reference/CRM Domain.md
@supabase/migrations/100_crm_schema_identity_graph_hardening.sql
@supabase/migrations/82_markos_audit_log_hash_chain.sql
@contracts/F-105-cli-whoami-status-v1.yaml
@contracts/flow-registry.json
@package.json

<!-- INFORMATIONAL ONLY (not canonical until distilled into obsidian/reference/*): -->
<!-- @obsidian/work/incoming/19-EMAIL-ENGINE.md (informational only — decisions locked in 224-CONTEXT.md per CLAUDE.md §source-of-truth precedence; canonical = obsidian/reference/* + 224-CONTEXT.md); relevant scope: LaunchSurface email_campaign target only -->
<!-- @obsidian/work/incoming/23-CONVERSION-ENGINE.md (informational only — decisions locked in 224-CONTEXT.md per CLAUDE.md §source-of-truth precedence; canonical = obsidian/reference/* + 224-CONTEXT.md) -->
<!-- @obsidian/work/incoming/26-LAUNCH-ENGINE.md (informational only — decisions locked in 224-CONTEXT.md per CLAUDE.md §source-of-truth precedence; canonical = obsidian/reference/* + 224-CONTEXT.md) -->
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase + research. -->

From `supabase/migrations/82_markos_audit_log_hash_chain.sql` (P201 audit pattern reused for D-49):
```sql
-- markos_audit_log columns: tenant_id, event_type, actor_id, payload_hash, prev_hash, current_hash, occurred_at
-- All P224 mutations append rows with event_type starting 'conversion_' or 'launch_'.
```

From `supabase/migrations/100_crm_schema_identity_graph_hardening.sql` (tenant_id FK pattern):
```sql
-- All P224 tables use: tenant_id UUID NOT NULL REFERENCES markos_tenants(id)
-- RLS policy: USING (tenant_id = current_setting('app.active_tenant_id', true)::UUID)
```

From `contracts/F-105-cli-whoami-status-v1.yaml` (F-ID YAML shape — contracts include flow_id, paths, methods, request/response schemas, x-markos-meta block):
```yaml
flow_id: F-105
title: CLI whoami + status
version: v1
paths:
  /v1/cli/whoami:
    get: { ... }
x-markos-meta:
  approval_policy: read-only
  audit: enabled
```

From `lib/markos/cdp/contracts.cjs` (P221 contract pattern — TypeScript types alongside CommonJS export):
```javascript
// Contract types live in lib/markos/<domain>/contracts/* (TypeScript) AND
// re-exported from lib/markos/<domain>/index.ts as the public API surface.
```

From `lib/markos/channels/index.ts` (P223 module pattern carry-forward):
```typescript
export * from './programs/email-campaign';
export * from './programs/messaging-thread';
// ... lib/markos/conversion/index.ts mirrors this pattern
```

From `224-RESEARCH.md` §Schema Sketches (lines 230-589) — full DDL for all 13 tables. Lines 234-267 (conversion_pages), 271-302 (conversion_forms), 306-325 (conversion_ctas), 329-353 (conversion_events), 357-380 (conversion_experiments), 384-398 (experiment_variants), 402-417 (experiment_assignments), 421-448 (launch_briefs), 450-485 (launch_surfaces), 487-514 (launch_gates — DEFER to plan 04), 516-541 (launch_runbooks — DEFER to plan 06), 543-562 (launch_outcomes — DEFER to plan 04), 564-589 (launch_readiness_checks).

From `224-RESEARCH.md` §Pattern 2 (lines 720-744) — 15 block validators using Zod with per-block schema, BlockSchemas record, validateBlock() helper.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 baseline + fixture factories + module skeletons (no migrations yet)</name>
  <read_first>
    - .planning/phases/224-conversion-launch-workspace/224-RESEARCH.md §Assumptions Log lines 1431-1452 (A1/A2/A12 baseline; A11/A16 Next.js; A17 xxhash)
    - .planning/phases/224-conversion-launch-workspace/224-VALIDATION.md §Wave 0 Requirements (lines 54-75)
    - .planning/phases/224-conversion-launch-workspace/224-RESEARCH.md §Fixture Catalog (lines 1306-1323)
    - package.json (current dependencies — confirm Next.js version, vitest, playwright, zod presence)
    - contracts/flow-registry.json (current — confirm last F-ID is F-105 in this branch state)
    - supabase/migrations/ directory (current highest numbered migration)
    - .planning/phases/223-native-email-messaging-orchestration/223-01-PLAN.md Task 1 (fixture factory pattern reference)
    - test/fixtures/cdp/ if exists (P221 fixture pattern), test/fixtures/channels/ if exists (P223 fixture pattern)
  </read_first>
  <action>
    Execute the following concrete steps in order. Do NOT create migrations or contract YAMLs in this task — that lands in Tasks 2-3.

    1. **Baseline verification (A1/A2/A12).** Run these commands and record results in commit message:
       ```bash
       ls contracts/ | grep -E "^F-[0-9]+" | sort -V | tail -5
       ls supabase/migrations/ | grep -E "^[0-9]+" | sort -V | tail -5
       ```
       Expected per RESEARCH.md A1/A2: highest F-ID = F-131, highest migration = 120. ACTUAL state at planning time: highest F-ID = F-105, highest migration = 100 (because P221/P222/P223 plans have not yet been EXECUTED — they exist only as PLAN.md files). This is acceptable: P224 plans were prepared on the assumption P221/P222/P223 ship first per ROADMAP. If executing 224 BEFORE 221-223, this task MUST halt and emit a checkpoint requesting clarification — but for in-sequence execution this verification simply confirms the registered F-IDs/migrations from prior phases match the expected gap (F-106..F-131 reserved across P221/P222/P223; migrations 101..120 reserved). Document the actual highest F-ID and migration in the commit message; if the gap is correct, proceed. If new contracts/migrations have been added in the F-132+ or 121+ ranges, halt and renumber per planner direction.

    2. **Next.js + dependency verification (A11/A16/A17).** Read `package.json`. Confirm:
       - `next` version >= 13.5 (cacheTag/updateTag API). If older, log warning + plan revalidatePath fallback (do NOT block; document in fallback notes).
       - `zod` is installed (block validators). If absent, `npm install zod@^3.22.0`.
       - `xxhash-wasm` for sticky-hash assignment (A17). Install: `npm install xxhash-wasm@^1.0.2`. Fallback note: SHA-256 truncated via Node.js built-in `crypto` module (no dep) is the documented backup; this task installs xxhash-wasm so plan 05 can use the faster hash by default.
       - `vitest`, `@vitest/coverage-v8`, `playwright`, `@playwright/test` already installed from P204/P221 baseline. If missing, install at the same versions used by P204 (vitest@^1.6.0, playwright@^1.45.0).

    3. **Create fixture factory directories and files.** All factories return plain objects matching the DDL columns from RESEARCH.md §Schema Sketches (lines 230-589). Each file exports named factories that take an options object so callers can override fields.

       Create `test/fixtures/conversion/` with:
       - `page.ts` — exports `conversionPageFixture({ page_type, status, ...overrides })` covering 10 page_type variants (landing, signup, demo, pricing, launch, webinar, offer, thank_you, content_download, upgrade) × 4 status variants (draft, pending_approval, published, archived). Defaults: `page_type='landing', status='draft', slug='test-landing-${randomSuffix}', tenant_id=TEST_TENANT_ID, content_blocks=[], seo_meta={}, locale='en'`. Also export `conversionPageWithBlocksFixture()` returning a page with valid hero+form+pricing+evidence_block blocks.
       - `form.ts` — exports `conversionFormFixture({ objective, identity_stitch_enabled, ...overrides })` covering 8 objectives (contact_capture, demo_request, trial_signup, content_download, waitlist, webinar_registration, purchase, upgrade). Defaults: `objective='contact_capture', identity_stitch_enabled=true, fields=[{name:'email',type:'email',required:true}], variables_schema={type:'object',properties:{email:{type:'string',format:'email'}}}, submit_action='create_lead', status='draft'`.
       - `cta.ts` — exports `conversionCtaFixture({ type, ...overrides })` covering 6 types (button, banner, sticky_bar, exit_intent, inline, modal). Defaults: `type='button', name='Test CTA', target_url='/signup', status='draft'`.
       - `event.ts` — exports `conversionEventFixture({ surface_kind, objective, ...overrides })`. Defaults: `surface_kind='form', objective='trial_signup', identity_ref='anon-test-id', source_event_ref=crypto.randomUUID(), occurred_at=new Date().toISOString()`. Also export `conversionEventWithLaunchFixture()` and `conversionEventWithExperimentFixture()`.
       - `experiment.ts` — exports `conversionExperimentFixture({ status, traffic_split, ...overrides })` and `experimentVariantFixture()` and `experimentAssignmentFixture()`. Defaults for experiment: `status='draft', target_surface_kind='page', traffic_split={control:50, treatment:50}`. For variant: `name='control', traffic_weight=50, is_control=true, content_overrides={}`. For assignment: `identity_ref='anon-test', sticky=true`.
       - `blocks.ts` — exports `validBlockFixture(blockType)` AND `invalidBlockFixture(blockType)` for each of the 15 block types (hero, form, cta, testimonial, pricing, faq, footer, content, image, video, comparison, signup_widget, social_proof, evidence_block, custom_html). Valid fixtures match the Zod schemas defined in Task 4. Invalid fixtures violate one constraint each (e.g., hero with heading > 200 chars; pricing with non-string pricing_binding; custom_html missing admin_approved flag).
       - `index.ts` — re-exports all factories: `export * from './page'; export * from './form'; export * from './cta'; export * from './event'; export * from './experiment'; export * from './blocks';`

       Create `test/fixtures/launches/` with:
       - `brief.ts` — exports `launchBriefFixture({ launch_type, status, ...overrides })` covering 7 launch_type variants (feature, pricing, integration, campaign, event, beta, market_entry) × 6 status (planning, pending_approval, ready, live, completed, rolled_back). Defaults: `launch_type='feature', status='planning', name='Test Launch', objective='Test launch objective', target_audiences=[], internal_readiness_checks=[]`.
       - `surface.ts` — exports `launchSurfaceFixture({ surface_type, status, surface_target_kind, ...overrides })` covering 8 surface_type variants × 9 surface_target_kind variants × 5 status (draft, blocked, approved, published, archived). Defaults: `surface_type='landing_page', status='draft', surface_target_kind='conversion_page', surface_target_id=crypto.randomUUID(), blocking_reasons=[]`. Add a comment block at the top of `surface.ts` explicitly disambiguating the two enums (frequently confused — D-12 vs D-14):
         ```ts
         // surface_type ∈ doc 26 8 values: landing_page, email_campaign, messaging_flow, social_pack, sales_enablement, partner_pack, support_pack, docs_update
         // surface_target_kind ∈ D-14 9 values: email_campaign, messaging_thread, lifecycle_journey, conversion_page, social_pack, sales_enablement, partner_pack, support_pack, docs_update
         // The fixture exposes BOTH fields; do NOT conflate them.
         ```
         The fixture must export both fields on every returned object.
       - `gate.ts` — exports `launchGateFixture({ gate_kind, status, ...overrides })` covering 5 gate_kind (pricing, evidence, readiness, approval, custom) × 4 status (pending, passing, blocking, waived). Defaults: `gate_kind='pricing', status='pending', blocking_reasons=[], evidence_refs=[]`.
       - `runbook.ts` — exports `launchRunbookFixture({ state, steps, ...overrides })` covering single-step + multi-step (3 steps) + reversible/non-reversible mix. Defaults: `state='draft', steps=[{step_id:'s1', name:'Publish landing page', kind:'publish_surface', target_ref:'<uuid>', depends_on:[], idempotency_key:'s1-key', expected_duration_seconds:30}], rollback_steps=[{step_id:'rs1', kind:'archive_surface', target_ref:'<uuid>', reversible:true}], current_step:0`.
       - `outcome.ts` — exports `launchOutcomeFixture({ period_days, ...overrides })` covering T+7/T+14/T+30. Defaults: `period_days=7, reach=0, signups=0, pipeline_created=0, influenced_revenue=0, activation_lift=null`.
       - `readiness.ts` — exports `launchReadinessCheckFixture({ check_kind, status, ...overrides })` covering 6 check_kind variants × 4 status. Defaults: `check_kind='legal_approved', status='pending', owner_user_id=TEST_USER_ID`.
       - `index.ts` — re-exports all.

       Each fixture file imports `crypto.randomUUID()` from `node:crypto` for tenant_id/id generation. TEST_TENANT_ID is a constant: `'00000000-0000-0000-0000-000000000aaa'` per P221 fixture convention.

    4. **Create empty module skeletons** so later plans can co-locate code:
       - `lib/markos/conversion/index.ts` — empty re-export hub: `// barrel — populated by plans 02..07`
       - `lib/markos/conversion/contracts/` (empty dir; populated in Task 3)
       - `lib/markos/conversion/blocks/` (empty dir; populated in Task 4)
       - `lib/markos/conversion/render/` — `// renderer lands in plan 03` placeholder
       - `lib/markos/conversion/forms/` — `// FormRenderer lands in plan 03` placeholder
       - `lib/markos/conversion/events/` — `// emit() lands in plan 02` placeholder
       - `lib/markos/conversion/gates/` — `// content classifier lands in plan 03` placeholder
       - `lib/markos/launches/index.ts` — empty barrel
       - `lib/markos/launches/contracts/` (populated in Task 3)
       - `lib/markos/launches/gates/` — `// evaluators land in plan 04` placeholder
       - `lib/markos/launches/runbook/` — `// executor lands in plan 06` placeholder
       - `lib/markos/launches/surfaces/` — `// coordinator lands in plan 04` placeholder
       - `lib/markos/launches/outcomes/` — `// compute lands in plan 04` placeholder

       Each placeholder is a `.gitkeep` file or a `README.md` of <10 lines stating which plan populates it. Do NOT create actual logic.

    4b. **Create `test/fixtures/smoke.test.ts`** — vitest smoke test that imports every fixture factory from both `test/fixtures/conversion` and `test/fixtures/launches` barrels and asserts each returns valid output. Required assertions (~15 total — one per fixture file):
       - `expect(conversionPageFixture()).toMatchObject({ page_id: expect.any(String), tenant_id: expect.any(String) })`
       - `expect(conversionPageWithBlocksFixture()).toMatchObject({ content_blocks: expect.any(Array) })`
       - `expect(conversionFormFixture()).toMatchObject({ form_id: expect.any(String), objective: expect.any(String) })`
       - `expect(conversionCtaFixture()).toMatchObject({ cta_id: expect.any(String), type: expect.any(String) })`
       - `expect(conversionEventFixture()).toMatchObject({ event_id: expect.any(String), source_event_ref: expect.any(String) })`
       - `expect(conversionEventWithLaunchFixture()).toMatchObject({ launch_id: expect.any(String) })`
       - `expect(conversionEventWithExperimentFixture()).toMatchObject({ experiment_variant_id: expect.any(String) })`
       - `expect(conversionExperimentFixture()).toMatchObject({ experiment_id: expect.any(String), traffic_split: expect.any(Object) })`
       - `expect(experimentVariantFixture()).toMatchObject({ variant_id: expect.any(String) })`
       - `expect(experimentAssignmentFixture()).toMatchObject({ identity_ref: expect.any(String), sticky: true })`
       - `expect(validBlockFixture('hero')).toMatchObject({ block_type: 'hero' })` AND `expect(invalidBlockFixture('hero')).toBeDefined()`
       - `expect(launchBriefFixture()).toMatchObject({ launch_id: expect.any(String), launch_type: expect.any(String) })`
       - `expect(launchSurfaceFixture()).toMatchObject({ surface_id: expect.any(String), surface_target_kind: expect.any(String) })`
       - `expect(launchGateFixture()).toMatchObject({ gate_id: expect.any(String), gate_kind: expect.any(String) })`
       - `expect(launchRunbookFixture()).toMatchObject({ runbook_id: expect.any(String), steps: expect.any(Array) })`
       - `expect(launchOutcomeFixture()).toMatchObject({ launch_id: expect.any(String), period_days: expect.any(Number) })`
       - `expect(launchReadinessCheckFixture()).toMatchObject({ check_id: expect.any(String), check_kind: expect.any(String) })`

       The smoke test is the Wave 0 acceptance bar — it proves fixtures load without import errors AND each factory returns minimally-valid output. Run → MUST exit 0 with ≥15 passing assertions.

    5. **Update `.planning/phases/224-conversion-launch-workspace/224-VALIDATION.md`**: append the per-task verification rows for THIS plan's tasks. Use the existing template format (Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status). Add rows: 224-01-01 (Wave 0 + fixtures, infra), 224-01-02 (migrations 121-126, integration), 224-01-03 (contracts + library types, contract), 224-01-04 (block validators, unit), 224-01-05 (RLS suite, negative). Set `wave_0_complete: true` in the frontmatter once Task 1 commits cleanly.

    Do NOT modify `lib/markos/cdp/`, `lib/markos/crm360/`, or `lib/markos/channels/` directories. Do NOT touch existing test files outside `test/conversion/` and `test/launches/` (which are created in later tasks).
  </action>
  <acceptance_criteria>
    - `package.json` dependencies includes `"zod"` and `"xxhash-wasm"` (or these are documented as already-present)
    - `test/fixtures/conversion/` directory exists with 7 files: `page.ts`, `form.ts`, `cta.ts`, `event.ts`, `experiment.ts`, `blocks.ts`, `index.ts`
    - `test/fixtures/launches/` directory exists with 7 files: `brief.ts`, `surface.ts`, `gate.ts`, `runbook.ts`, `outcome.ts`, `readiness.ts`, `index.ts`
    - `test/fixtures/conversion/page.ts` contains string literal `export function conversionPageFixture` and references all 10 page_type values: `landing`, `signup`, `demo`, `pricing`, `launch`, `webinar`, `offer`, `thank_you`, `content_download`, `upgrade`
    - `test/fixtures/conversion/blocks.ts` contains string `validBlockFixture` AND `invalidBlockFixture` AND references all 15 block types in a single file (grep for each: `'hero'`, `'form'`, `'cta'`, `'testimonial'`, `'pricing'`, `'faq'`, `'footer'`, `'content'`, `'image'`, `'video'`, `'comparison'`, `'signup_widget'`, `'social_proof'`, `'evidence_block'`, `'custom_html'`)
    - `test/fixtures/launches/surface.ts` contains all 9 surface_target_kind values: `email_campaign`, `messaging_thread`, `lifecycle_journey`, `conversion_page`, `social_pack`, `sales_enablement`, `partner_pack`, `support_pack`, `docs_update`
    - `test/fixtures/launches/surface.ts` contains both `surface_type` AND `surface_target_kind` field assignments (grep returns ≥1 match for each); top-of-file comment block disambiguates the two enums per D-12 (8 surface_type values) vs D-14 (9 surface_target_kind values)
    - Module skeletons exist: `lib/markos/conversion/{index.ts,contracts,blocks,render,forms,events,gates}` and `lib/markos/launches/{index.ts,contracts,gates,runbook,surfaces,outcomes}`
    - `test/fixtures/smoke.test.ts` exists; `npx vitest run test/fixtures/smoke.test.ts` exits 0 with ≥15 passing assertions
    - 224-VALIDATION.md has 5 new rows (224-01-01 through 224-01-05) and frontmatter `wave_0_complete: true`
    - `npx vitest --version` returns >= 1.x and `npx playwright --version` returns >= 1.45
    - Commit message documents baseline state (highest existing F-ID + migration number) and the gap reservation
  </acceptance_criteria>
  <verify>
    <automated>npx vitest --version && npx vitest run test/fixtures/smoke.test.ts && node -e "require('./test/fixtures/conversion'); require('./test/fixtures/launches'); console.log('fixtures-ok')" && grep -l "validBlockFixture" test/fixtures/conversion/blocks.ts && grep -l "lifecycle_journey" test/fixtures/launches/surface.ts</automated>
  </verify>
  <done>Wave 0 baseline verified, dependencies installed, all 14 fixture files exist with correct enum coverage (10 page types, 15 block types, 9 surface_target_kinds, 7 launch_types, 6 readiness check kinds), module skeletons in place, 224-VALIDATION.md per-task rows for plan 01 added, ready for migrations in Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrations 121-126 (10 of 13 tables) + RLS test suite</name>
  <files>supabase/migrations/121_conversion_pages.sql, supabase/migrations/122_conversion_forms.sql, supabase/migrations/123_conversion_events.sql, supabase/migrations/124_conversion_experiments.sql, supabase/migrations/125_launch_briefs.sql, supabase/migrations/126_launch_surfaces.sql, test/conversion/schema/conversion-page.test.ts, test/conversion/schema/conversion-form.test.ts, test/conversion/schema/conversion-cta.test.ts, test/conversion/schema/conversion-event.test.ts, test/conversion/schema/conversion-experiment.test.ts, test/conversion/schema/experiment-variants.test.ts, test/conversion/schema/experiment-assignments.test.ts, test/launches/schema/launch-brief.test.ts, test/launches/schema/launch-surface.test.ts, test/launches/schema/launch-readiness-check.test.ts, test/conversion/rls/conversion-pages.rls.test.ts, test/conversion/rls/conversion-forms.rls.test.ts, test/conversion/rls/conversion-ctas.rls.test.ts, test/conversion/rls/conversion-events.rls.test.ts, test/conversion/rls/conversion-experiments.rls.test.ts, test/conversion/rls/experiment-variants.rls.test.ts, test/conversion/rls/experiment-assignments.rls.test.ts, test/launches/rls/launch-briefs.rls.test.ts, test/launches/rls/launch-surfaces.rls.test.ts, test/launches/rls/launch-readiness-checks.rls.test.ts</files>
  <behavior>
    - Test 1: `conversion_pages.page_type` rejects unknown enum (e.g., 'invalid_type'); accepts all 10 valid (landing, signup, demo, pricing, launch, webinar, offer, thank_you, content_download, upgrade).
    - Test 2: `conversion_pages` UNIQUE (tenant_id, slug) — duplicate insert in same tenant raises error; same slug across tenants is allowed.
    - Test 3: `conversion_pages.status` rejects 'invalid_status'; accepts {draft, pending_approval, published, archived}.
    - Test 4: `conversion_forms.objective` enforces 8 valid values; `submit_action` enforces 4 valid values; `identity_stitch_enabled` defaults TRUE.
    - Test 5: `conversion_ctas.type` enforces 6 valid values (button, banner, sticky_bar, exit_intent, inline, modal).
    - Test 6: `conversion_events.surface_kind` enforces {page, form, cta}; `source_event_ref` is UUID NOT NULL.
    - Test 7: `conversion_experiments.status` enforces {draft, active, paused, completed}; `traffic_split` JSONB defaults `{}`.
    - Test 8: `experiment_variants.traffic_weight` enforces 0..100 inclusive; `is_control` defaults FALSE.
    - Test 9: `experiment_assignments` UNIQUE (experiment_id, identity_ref) — duplicate raises error (sticky-assignment guarantee per D-23).
    - Test 10: `launch_briefs.launch_type` enforces 7 valid values; `target_audiences` defaults `'{}'::uuid[]`; `internal_readiness_checks` defaults `'{}'::uuid[]`.
    - Test 11: `launch_surfaces` polymorphic CHECK rejects unknown surface_target_kind (e.g., 'unknown_kind'); accepts all 9: email_campaign, messaging_thread, lifecycle_journey, conversion_page, social_pack, sales_enablement, partner_pack, support_pack, docs_update.
    - Test 12: `launch_surfaces.status` rejects unknown; accepts {draft, blocked, approved, published, archived}.
    - Test 13: `launch_readiness_checks.check_kind` enforces 6 valid (legal_approved, support_ready, sales_trained, docs_published, partner_briefed, custom).
    - Test 14: All 10 RLS tests: `SET app.active_tenant_id = '<tenant-A>'` then SELECT returns only tenant-A rows even when tenant-B rows exist; cross-tenant SELECT denied.
  </behavior>
  <read_first>
    - .planning/phases/224-conversion-launch-workspace/224-RESEARCH.md §Schema Sketches lines 230-485, 564-589 (DDL for the 10 tables this task creates — conversion_pages, conversion_forms, conversion_ctas, conversion_events, conversion_experiments, experiment_variants, experiment_assignments, launch_briefs, launch_surfaces, launch_readiness_checks)
    - .planning/phases/224-conversion-launch-workspace/224-CONTEXT.md D-01, D-02, D-05, D-09, D-12, D-13, D-14, D-21..D-23, D-51 (locked schema decisions)
    - supabase/migrations/100_crm_schema_identity_graph_hardening.sql (FK pattern: `REFERENCES markos_tenants(id)` and RLS policy syntax)
    - supabase/migrations/82_markos_audit_log_hash_chain.sql (RLS pattern: `USING (tenant_id = current_setting('app.active_tenant_id', true)::UUID)`)
    - test/fixtures/conversion/page.ts (created in Task 1 — used by tests for valid payloads)
    - test/fixtures/launches/surface.ts (created in Task 1)
    - test/tenant-auth/ if exists (existing RLS test helper pattern from P201)
    - .planning/phases/223-native-email-messaging-orchestration/223-01-PLAN.md Task 2 (migration TDD pattern reference)
  </read_first>
  <action>
    Write tests FIRST (TDD RED → migration GREEN).

    **Migration application order (wave-driven, NOT numeric):** P224 migrations are numbered 121-133 across 7 plans executing in waves 1-5. Because plans run in parallel within a wave (e.g., plan 02 migration 130 in Wave 2 runs BEFORE plan 04 migration 127/129 in Wave 3), the migration runner MUST apply migrations in **wave-execution order**, not numeric order:
    - Wave 1 (plan 01): 121, 122, 123, 124, 125, 126
    - Wave 2 (plan 02): 130 (numerically higher than later-wave migrations — this is intentional; plan 02 ships dispatch infrastructure ahead of gates)
    - Wave 3 (plan 04 + plan 05 parallel): 127, 129 (plan 04), 131 (plan 05) — apply 127 → 129 → 131 in this order; cross-plan ordering does not matter as long as all are applied before Wave 4
    - Wave 4 (plan 06): 128, 132
    - Wave 5 (plan 07): 133

    The migration runner script honors this by reading `wave` frontmatter from each plan and applying migrations in wave-execution order. Plan 07 includes a verification test (`test/migrations/order.test.ts`) that asserts the runner script enforces wave-execution-order over numeric-order. This documentation rule supersedes any naive `ls supabase/migrations/ | sort -V` ordering and is the ONLY correct execution order.

    1. **Create schema test files** under `test/conversion/schema/` and `test/launches/schema/`. Each test file uses Vitest, imports the corresponding fixture from `test/fixtures/conversion` or `test/fixtures/launches`, and asserts CHECK constraint enforcement + UNIQUE enforcement using a Postgres test harness. The harness is the same one used by P101/P201 RLS tests — find it via `grep -l "current_setting('app.active_tenant_id'" test/` to locate the existing helper. If no helper exists, use a thin wrapper around `pg` library that:
       - Opens a transaction
       - SETs `app.active_tenant_id` to the test tenant UUID
       - Runs the INSERT statement
       - Asserts on resulting rows or expected error code (`23514` for CHECK violation, `23505` for UNIQUE violation, `22P02` for invalid enum input)
       - Rolls back transaction at end of test

       For each schema test (10 schema files), test the behaviors listed in `<behavior>` above. Run all tests → MUST fail (no migration applied yet).

    2. **Create RLS test files** under `test/conversion/rls/` and `test/launches/rls/`. Each RLS test:
       - Inserts row with tenant_id = TENANT_A (via `SET app.active_tenant_id = TENANT_A`)
       - Switches to tenant_id = TENANT_B (via `SET app.active_tenant_id = TENANT_B`)
       - Issues SELECT — asserts row count is 0 (RLS denies cross-tenant read)
       - Switches back to TENANT_A — asserts SELECT returns the inserted row
       - Tests INSERT denial: row with tenant_id = TENANT_B inserted while session is TENANT_A → policy violation OR row not visible
       - Run all → MUST fail.

    3. **Write `supabase/migrations/121_conversion_pages.sql`** using exact DDL from RESEARCH.md lines 230-267:
       ```sql
       CREATE TABLE IF NOT EXISTS conversion_pages (
         page_id           UUID PRIMARY KEY DEFAULT gen_random_uuid(),
         tenant_id         UUID NOT NULL REFERENCES markos_tenants(id),
         page_type         TEXT NOT NULL CHECK (page_type IN (
           'landing','signup','demo','pricing','launch','webinar',
           'offer','thank_you','content_download','upgrade'
         )),
         slug              TEXT NOT NULL,
         title             TEXT NOT NULL,
         status            TEXT NOT NULL DEFAULT 'draft' CHECK (status IN (
           'draft','pending_approval','published','archived'
         )),
         objective         TEXT NOT NULL,
         content_blocks    JSONB NOT NULL DEFAULT '[]',
         seo_meta          JSONB NOT NULL DEFAULT '{}',
         locale            TEXT NOT NULL DEFAULT 'en',
         parent_page_id    UUID REFERENCES conversion_pages(page_id),
         audience_id       UUID,
         experiment_set_id UUID,
         pricing_context_id UUID,
         evidence_pack_id   UUID,
         version           INTEGER NOT NULL DEFAULT 1,
         approved_by       UUID,
         approved_at       TIMESTAMPTZ,
         published_at      TIMESTAMPTZ,
         archived_at       TIMESTAMPTZ,
         created_at        TIMESTAMPTZ NOT NULL DEFAULT NOW(),
         updated_at        TIMESTAMPTZ NOT NULL DEFAULT NOW(),
         CONSTRAINT conversion_pages_slug_tenant_unique UNIQUE (tenant_id, slug)
       );
       ALTER TABLE conversion_pages ENABLE ROW LEVEL SECURITY;
       CREATE POLICY conversion_pages_tenant_isolation ON conversion_pages
         USING (tenant_id = current_setting('app.active_tenant_id', true)::UUID);
       CREATE INDEX IF NOT EXISTS idx_conversion_pages_tenant_status ON conversion_pages (tenant_id, status);
       CREATE INDEX IF NOT EXISTS idx_conversion_pages_audience ON conversion_pages (audience_id) WHERE audience_id IS NOT NULL;
       CREATE INDEX IF NOT EXISTS idx_conversion_pages_published ON conversion_pages (tenant_id, published_at) WHERE status = 'published';

       -- Rollback (operator reference):
       -- DROP POLICY IF EXISTS conversion_pages_tenant_isolation ON conversion_pages;
       -- DROP TABLE IF EXISTS conversion_pages CASCADE;
       ```

    4. **Write `supabase/migrations/122_conversion_forms.sql`** for both `conversion_forms` and `conversion_ctas` (DDL from RESEARCH.md lines 271-325). Both tables: tenant_id FK + RLS + status indexes. `conversion_forms` includes `thank_you_page_id UUID REFERENCES conversion_pages(page_id)` (FK to migration 121's table — ensure migration ordering).

    5. **Write `supabase/migrations/123_conversion_events.sql`** for `conversion_events` (DDL from RESEARCH.md lines 329-353). Indexes: `(tenant_id, surface_id, occurred_at DESC)`, `(launch_id) WHERE launch_id IS NOT NULL`, `(source_event_ref)`, `(tenant_id, identity_ref)`. NOTE: `experiment_variant_id UUID REFERENCES experiment_variants(variant_id)` and `launch_id UUID REFERENCES launch_briefs(launch_id)` — both reference tables created in migrations 124 and 125 in this same plan; ensure migration application is ordered 121→122→123→124→125→126 OR change `123` to use deferred FK constraints. Recommended: number FIXED so 124+125 land BEFORE 123. RENUMBER if needed: 121 conversion_pages, 122 conversion_experiments+variants+assignments, 123 launch_briefs+readiness, 124 conversion_forms+ctas, 125 conversion_events, 126 launch_surfaces. Verify migration runner applies in numeric order. CHOOSE the ordering that produces clean FK resolution and document in commit. The simplest fix: defer `conversion_events.experiment_variant_id` and `conversion_events.launch_id` FKs to a separate ALTER TABLE in `126` after the parent tables exist; create the columns as plain UUID without FK in 123, then add `ALTER TABLE conversion_events ADD CONSTRAINT ... FOREIGN KEY ...` in 126.

    6. **Write `supabase/migrations/124_conversion_experiments.sql`** for `conversion_experiments` + `experiment_variants` + `experiment_assignments` (DDL from RESEARCH.md lines 357-417). Critical: `CREATE UNIQUE INDEX ON experiment_assignments (experiment_id, identity_ref)` enforces sticky assignment.

    7. **Write `supabase/migrations/125_launch_briefs.sql`** for `launch_briefs` + `launch_readiness_checks` (DDL from RESEARCH.md lines 421-448 + 564-589). `target_audiences UUID[] NOT NULL DEFAULT '{}'`. `internal_readiness_checks UUID[] NOT NULL DEFAULT '{}'`.

    8. **Write `supabase/migrations/126_launch_surfaces.sql`** for `launch_surfaces` (DDL from RESEARCH.md lines 450-485) WITH polymorphic CHECK. Also include the deferred FKs from migration 123 (`ALTER TABLE conversion_events ADD CONSTRAINT conversion_events_variant_fk FOREIGN KEY (experiment_variant_id) REFERENCES experiment_variants(variant_id)` and `ALTER TABLE conversion_events ADD CONSTRAINT conversion_events_launch_fk FOREIGN KEY (launch_id) REFERENCES launch_briefs(launch_id)`).

       The polymorphic CHECK constraint:
       ```sql
       CONSTRAINT launch_surfaces_kind_valid CHECK (
         surface_target_kind IN (
           'email_campaign','messaging_thread','lifecycle_journey',
           'conversion_page','social_pack','sales_enablement',
           'partner_pack','support_pack','docs_update'
         )
       )
       ```

    9. Run all schema + RLS tests → MUST pass (GREEN). 10 schema tests + 10 RLS tests = 20 test files.

    10. Commit sequence: `test(224-01): add failing schema + RLS tests for 10 tables` then `feat(224-01): migrations 121-126 with RLS, polymorphic CHECK, sticky-assignment unique index`.

    Do NOT add `launch_gates`, `launch_runbooks`, `launch_outcomes` — those land in plans 04 (gates+outcomes) and 06 (runbooks). Do NOT modify any prior phase migrations. The `experiment_set_id`, `audience_id`, `pricing_context_id`, `evidence_pack_id` columns are deliberately UUID without FK constraint in this plan — those FKs cannot resolve until P205/P209/P221 ship; downstream phases add FKs as their tables come online.
  </action>
  <acceptance_criteria>
    - 6 migration files exist: `supabase/migrations/121_conversion_pages.sql`, `122_conversion_forms.sql`, `123_conversion_events.sql`, `124_conversion_experiments.sql`, `125_launch_briefs.sql`, `126_launch_surfaces.sql`
    - `121_conversion_pages.sql` contains string `CREATE TABLE IF NOT EXISTS conversion_pages` AND `ENABLE ROW LEVEL SECURITY` AND `CONSTRAINT conversion_pages_slug_tenant_unique UNIQUE (tenant_id, slug)` AND all 10 page_type literals in CHECK clause
    - `124_conversion_experiments.sql` contains `CREATE UNIQUE INDEX` referencing `(experiment_id, identity_ref)`
    - `126_launch_surfaces.sql` contains all 9 surface_target_kind literals AND `CONSTRAINT launch_surfaces_kind_valid`
    - 10 schema test files exist under `test/conversion/schema/` (7) and `test/launches/schema/` (3)
    - 10 RLS test files exist under `test/conversion/rls/` (7) and `test/launches/rls/` (3)
    - `npx vitest run test/conversion/schema/ test/launches/schema/ test/conversion/rls/ test/launches/rls/` exits 0 (all 20 test files green)
    - 224-VALIDATION.md row 224-01-02 (migration+schema) and 224-01-05 (RLS suite) status flips to ✅ green
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/conversion/schema/ test/launches/schema/ test/conversion/rls/ test/launches/rls/</automated>
  </verify>
  <done>10 of 13 P224 tables exist with RLS, polymorphic CHECK on launch_surfaces (9 values), UNIQUE sticky-assignment index on experiment_assignments, deferred FKs from conversion_events resolved in migration 126; 20 test files green; ready for contracts + library types in Task 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 10 F-ID contracts + TypeScript contract types + flow-registry update</name>
  <files>contracts/F-132-conversion-page-v1.yaml, contracts/F-133-conversion-form-v1.yaml, contracts/F-134-conversion-cta-v1.yaml, contracts/F-135-conversion-event-v1.yaml, contracts/F-136-conversion-experiment-v1.yaml, contracts/F-139-launch-brief-v1.yaml, contracts/F-140-launch-surface-v1.yaml, contracts/F-141-launch-gate-v1.yaml, contracts/F-143-launch-outcome-v1.yaml, contracts/F-144-launch-readiness-check-v1.yaml, contracts/flow-registry.json, lib/markos/conversion/contracts/page.ts, lib/markos/conversion/contracts/form.ts, lib/markos/conversion/contracts/cta.ts, lib/markos/conversion/contracts/event.ts, lib/markos/conversion/contracts/experiment.ts, lib/markos/conversion/contracts/index.ts, lib/markos/launches/contracts/brief.ts, lib/markos/launches/contracts/surface.ts, lib/markos/launches/contracts/gate.ts, lib/markos/launches/contracts/runbook.ts, lib/markos/launches/contracts/outcome.ts, lib/markos/launches/contracts/readiness.ts, lib/markos/launches/contracts/index.ts, lib/markos/conversion/index.ts, lib/markos/launches/index.ts, test/conversion/contracts/contract-parity.test.ts, test/launches/contracts/contract-parity.test.ts</files>
  <behavior>
    - Test 1: `validate-flow-contracts.cjs` accepts F-132..F-136, F-139..F-141, F-143, F-144 — 10 new entries — without errors.
    - Test 2: `flow-registry.json` contains all 10 new flow_id entries with correct title, version, file path; F-105 (P204 last) and F-131 (P223 last per Wave 0 baseline gap) preserved unchanged.
    - Test 3: TypeScript types `ConversionPage`, `ConversionForm`, `ConversionCta`, `ConversionEvent`, `ConversionExperiment`, `ExperimentVariant`, `ExperimentAssignment` are exported from `lib/markos/conversion/contracts/index.ts` and match DDL column names (e.g., `ConversionPage.page_id`, `.tenant_id`, `.page_type` all 10 values, `.status` 4 values, `.content_blocks: Block[]`).
    - Test 4: TypeScript types `LaunchBrief`, `LaunchSurface`, `LaunchGate`, `LaunchRunbook`, `LaunchOutcome`, `LaunchReadinessCheck`, `SurfaceTargetKind` (union of 9 values) exported from `lib/markos/launches/contracts/index.ts`.
    - Test 5: `lib/markos/conversion/index.ts` re-exports `* from './contracts'` and (placeholder for blocks) `* from './blocks/schema'`; importing from outside `lib/markos/conversion` works via `import { ConversionPage } from '@/lib/markos/conversion'`.
  </behavior>
  <read_first>
    - contracts/F-105-cli-whoami-status-v1.yaml (existing F-ID YAML shape: flow_id, title, version, paths, x-markos-meta, request/response schemas)
    - contracts/flow-registry.json (current registry shape — confirm field structure: flow_id, title, version, file, status)
    - .planning/phases/224-conversion-launch-workspace/224-RESEARCH.md §F-ID and Migration Allocation lines 178-220 (F-132..F-146 mapping with names, flow_type, slice)
    - .planning/phases/224-conversion-launch-workspace/224-CONTEXT.md D-01, D-05, D-09, D-12, D-21, D-22, D-23 (column names for type definitions)
    - bin/validate-flow-contracts.cjs if exists (validation rules for F-ID YAML)
    - obsidian/reference/Contracts Registry.md (canonical naming + version conventions)
    - obsidian/reference/HTTP Layer.md (`/v1/conversion/*` and `/v1/launches/*` URL conventions)
  </read_first>
  <action>
    1. **Generate the 10 F-ID YAML contracts.** Each contract follows the F-105 shape. Read F-105 to extract exact structure. For this plan, contracts are READ-FIRST stubs: list paths and request/response schemas, but mark write-side endpoints as `status: stub` in `x-markos-meta`. The complete write-side specs land in plan 07.

       Template (apply per-flow with appropriate substitutions):
       ```yaml
       flow_id: F-132
       title: Conversion Page CRUD
       version: v1
       owner: phase-224
       status: draft  # graduates to active in plan 07
       paths:
         /v1/conversion/pages:
           get:
             summary: List conversion pages for tenant
             responses:
               '200': { description: list of ConversionPage }
           post:
             summary: Create conversion page (status=draft)
             x-markos-meta:
               approval_policy: approval-aware
               audit: enabled
             responses:
               '201': { description: created }
         /v1/conversion/pages/{page_id}:
           get: { ... }
           put:
             x-markos-meta:
               approval_policy: approval-aware
               audit: enabled
           delete: { ... }
         /v1/conversion/pages/{page_id}/publish:
           post:
             x-markos-meta:
               approval_policy: approval-required
               approval_template: page_publish
               audit: enabled
       x-markos-meta:
         tenancy: enforced-via-rls
         data_tables: [conversion_pages]
         depends_on: [F-105]
       ```

       Per F-ID specifics (use the table from RESEARCH.md):
       - **F-132 conversion-page-v1**: paths `/v1/conversion/pages` + `/v1/conversion/pages/{id}` + `/v1/conversion/pages/{id}/{publish|archive}`. Approval-required on publish. data_tables: [conversion_pages].
       - **F-133 conversion-form-v1**: paths `/v1/conversion/forms` + `/v1/conversion/forms/{id}` + `/v1/conversion/forms/{id}/publish`. Approval-required on publish. data_tables: [conversion_forms]. Note: public submit endpoint is F-137 (plan 02), NOT in F-133.
       - **F-134 conversion-cta-v1**: paths `/v1/conversion/ctas` + `/v1/conversion/ctas/{id}`. Approval-aware. data_tables: [conversion_ctas].
       - **F-135 conversion-event-v1**: read-only `/v1/conversion/events`. data_tables: [conversion_events]. NO write paths in this F-ID — emission is internal via emit() (F-138 in plan 02).
       - **F-136 conversion-experiment-v1**: paths `/v1/conversion/experiments` + `/{id}` + `/variants`. Approval-aware on activation. data_tables: [conversion_experiments, experiment_variants, experiment_assignments].
       - **F-139 launch-brief-v1**: paths `/v1/launches/briefs` + `/{id}` + `/{id}/transition`. Approval-aware on status transitions. data_tables: [launch_briefs].
       - **F-140 launch-surface-v1**: paths `/v1/launches/surfaces` + `/{id}` + `/{id}/transition`. Approval-aware. data_tables: [launch_surfaces]. Note: surface_target_kind validates against the 9-value enum.
       - **F-141 launch-gate-v1**: paths `/v1/launches/gates` + `/{id}` + `/{id}/waive`. Approval-required on waive (admin-only). data_tables: [launch_gates]. Stub — full evaluator endpoint `/v1/launches/{id}/gates/evaluate` lands in plan 04 (which graduates F-141 status from draft → active and adds F-145 evaluate-tool MCP contract).
       - **F-143 launch-outcome-v1**: read-only `/v1/launches/{id}/outcomes`. data_tables: [launch_outcomes]. Stub — outcome compute trigger lives in plan 04.
       - **F-144 launch-readiness-check-v1**: paths `/v1/launches/readiness-checks` + `/{id}/transition`. Approval-aware. data_tables: [launch_readiness_checks].

       (Note: F-137 public-form-submit, F-138 conversion-fan-out-emit, F-142 launch-runbook-v1, F-145 launch-evaluate-gates, F-146 launch-execute-runbook are NOT created in this plan — they land in plans 02, 04, 06 respectively when their behavior is implemented.)

    2. **Update `contracts/flow-registry.json`**: append 10 new entries preserving existing F-100..F-105 entries (and any P221/P222/P223 entries if those phases shipped first). For each:
       ```json
       {
         "flow_id": "F-132",
         "title": "Conversion Page CRUD",
         "version": "v1",
         "file": "F-132-conversion-page-v1.yaml",
         "status": "draft"
       }
       ```

    3. **Create TypeScript contract types** under `lib/markos/conversion/contracts/`:

       `page.ts`:
       ```typescript
       export type PageType = 'landing' | 'signup' | 'demo' | 'pricing' | 'launch' | 'webinar' | 'offer' | 'thank_you' | 'content_download' | 'upgrade';
       export type PageStatus = 'draft' | 'pending_approval' | 'published' | 'archived';
       export interface ConversionPage {
         page_id: string;
         tenant_id: string;
         page_type: PageType;
         slug: string;
         title: string;
         status: PageStatus;
         objective: string;
         content_blocks: unknown[]; // typed in blocks/schema.ts via BlockType union
         seo_meta: Record<string, unknown>;
         locale: string;
         parent_page_id: string | null;
         audience_id: string | null;
         experiment_set_id: string | null;
         pricing_context_id: string | null;
         evidence_pack_id: string | null;
         version: number;
         approved_by: string | null;
         approved_at: string | null;
         published_at: string | null;
         archived_at: string | null;
         created_at: string;
         updated_at: string;
       }
       ```

       `form.ts`: `ConversionForm` interface matching D-05 columns; `FormObjective` union of 8 values; `FormFieldType` union of 11 values per D-06; `SubmitAction` union of 4 values.

       `cta.ts`: `ConversionCta` interface; `CtaType` union of 6 values.

       `event.ts`: `ConversionEvent` interface matching D-09; `SurfaceKind` union {'page','form','cta'}; `ConversionEventPayload` (input shape for emit() — same fields minus event_id, created_at). Reserve `EmitResult` interface stub for plan 02 fan-out.

       `experiment.ts`: `ConversionExperiment`, `ExperimentVariant`, `ExperimentAssignment` interfaces; `ExperimentStatus` union {'draft','active','paused','completed'}.

       `index.ts`: barrel re-exporting all types.

    4. **Create TypeScript contract types** under `lib/markos/launches/contracts/`:

       `brief.ts`: `LaunchBrief` interface; `LaunchType` union (7 values); `BriefStatus` union (6 values).

       `surface.ts`: `LaunchSurface` interface; `SurfaceType` union (9 values per D-14); `SurfaceTargetKind` union (9 values matching CHECK constraint); `SurfaceStatus` union (5 values per D-15).

       `gate.ts`: `LaunchGate` interface; `GateKind` union {'pricing','evidence','readiness','approval','custom'}; `GateStatus` union {'pending','passing','blocking','waived'}; reserve `GateEvaluationResult` interface stub for plan 04.

       `runbook.ts`: `LaunchRunbook` interface; `RunbookState` union (7 values per D-35); `RunbookStep` interface with kind union {'publish_surface','dispatch_email_campaign','send_messaging','post_social','notify_team','custom'} and `reversible: boolean`. Stub — full executor in plan 06.

       `outcome.ts`: `LaunchOutcome` interface matching D-39/D-40; `OutcomePeriod` union {7,14,30}.

       `readiness.ts`: `LaunchReadinessCheck` interface; `CheckKind` union (6 values); `CheckStatus` union (4 values).

       `index.ts`: barrel.

    5. **Update `lib/markos/conversion/index.ts`**: `export * from './contracts';` (blocks/schema export added in Task 4).

    6. **Update `lib/markos/launches/index.ts`**: `export * from './contracts';`.

    7. **Create contract parity tests**:
       - `test/conversion/contracts/contract-parity.test.ts`: imports from `@/lib/markos/conversion`, asserts each type has the documented fields by writing a sample object literal with `satisfies ConversionPage` (TypeScript compile-time assertion). Asserts all 10 PageType values via `const all: PageType[] = ['landing','signup','demo','pricing','launch','webinar','offer','thank_you','content_download','upgrade']`.
       - `test/launches/contracts/contract-parity.test.ts`: similar; explicitly asserts SurfaceTargetKind contains all 9 values matching the migration 126 CHECK constraint.
       - Also runs `node bin/validate-flow-contracts.cjs F-132 F-133 F-134 F-135 F-136 F-139 F-140 F-141 F-143 F-144` (or equivalent) and asserts exit 0.

    8. Run tests: `npx vitest run test/conversion/contracts/ test/launches/contracts/` → MUST pass.

    9. Commit sequence: `test(224-01): add contract parity tests for F-132..F-144` then `feat(224-01): F-132..F-144 contracts + TypeScript types`.

    Do NOT create `lib/markos/conversion/blocks/schema.ts` here — Task 4. Do NOT create executor/evaluator code — those are plans 04 and 06.
  </action>
  <acceptance_criteria>
    - 10 F-ID YAML files exist: `F-132-conversion-page-v1.yaml`, `F-133-conversion-form-v1.yaml`, `F-134-conversion-cta-v1.yaml`, `F-135-conversion-event-v1.yaml`, `F-136-conversion-experiment-v1.yaml`, `F-139-launch-brief-v1.yaml`, `F-140-launch-surface-v1.yaml`, `F-141-launch-gate-v1.yaml`, `F-143-launch-outcome-v1.yaml`, `F-144-launch-readiness-check-v1.yaml`
    - `contracts/flow-registry.json` contains 10 new entries (grep for `F-132`, `F-133`, `F-134`, `F-135`, `F-136`, `F-139`, `F-140`, `F-141`, `F-143`, `F-144`)
    - 11 TypeScript contract files exist: `lib/markos/conversion/contracts/{page,form,cta,event,experiment,index}.ts` (6) and `lib/markos/launches/contracts/{brief,surface,gate,runbook,outcome,readiness,index}.ts` (7)
    - `lib/markos/conversion/contracts/page.ts` contains literal string `'thank_you'` AND `'content_download'` AND `'upgrade'` (proves all 10 PageType values)
    - `lib/markos/launches/contracts/surface.ts` contains all 9 SurfaceTargetKind literals: `email_campaign`, `messaging_thread`, `lifecycle_journey`, `conversion_page`, `social_pack`, `sales_enablement`, `partner_pack`, `support_pack`, `docs_update`
    - `lib/markos/conversion/index.ts` exports from `./contracts` (and `./blocks/schema` is wired in Task 4)
    - `npx vitest run test/conversion/contracts/ test/launches/contracts/` exits 0
    - `node bin/validate-flow-contracts.cjs` (or equivalent) exits 0 if it exists; if not, document this in commit
    - 224-VALIDATION.md row 224-01-03 (contracts) status flips to ✅ green
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/conversion/contracts/ test/launches/contracts/ && grep -l "F-132" contracts/flow-registry.json && grep -l "lifecycle_journey" lib/markos/launches/contracts/surface.ts</automated>
  </verify>
  <done>10 F-ID contracts (F-132..F-136 + F-139..F-141 + F-143 + F-144) exist as draft stubs; 11 TypeScript type files exported from `@/lib/markos/conversion` and `@/lib/markos/launches`; flow-registry updated; contract parity tests green; downstream plans can `import { ConversionPage, LaunchSurface }` directly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Block schema validators (15 types) + Zod library</name>
  <files>lib/markos/conversion/blocks/schema.ts, lib/markos/conversion/blocks/index.ts, lib/markos/conversion/index.ts, test/conversion/blocks/schema-validators.test.ts</files>
  <behavior>
    - Test 1: `validateBlock({block_type: 'hero', heading: 'Hi'})` returns `{success: true, data: {...}}`.
    - Test 2: `validateBlock({block_type: 'hero', heading: 'x'.repeat(201)})` returns `{success: false}` (heading max 200 per D-02 inferred constraint).
    - Test 3: `validateBlock({block_type: 'invalid_type'})` returns `{success: false}` with `error.message` containing 'unknown block_type'.
    - Test 4: All 15 block types accept their valid fixture from `test/fixtures/conversion/blocks.ts`.
    - Test 5: All 15 block types reject their invalid fixture.
    - Test 6: `BlockSchemas` is a `Record<BlockType, z.ZodSchema>` with exactly 15 keys.
    - Test 7: `custom_html` block requires `admin_approved: true` field; `admin_approved: false` or missing → reject.
    - Test 8: `pricing_binding` and `evidence_binding` fields, when present, must be strings matching pattern `/^\{\{(pricing|evidence|MARKOS_PRICING_ENGINE_PENDING|MARKOS_EVIDENCE_PENDING)\..*\}\}$|^\{\{MARKOS_(PRICING_ENGINE|EVIDENCE)_PENDING\}\}$/` — runtime resolution happens in plan 03 renderer; validation here ensures shape.
    - Test 9: `BlockType` union exports correctly from `lib/markos/conversion/blocks/index.ts`.
  </behavior>
  <read_first>
    - .planning/phases/224-conversion-launch-workspace/224-CONTEXT.md D-02 (15 block types listed); D-19 (placeholder rule); D-54 (no raw user input in pricing/evidence bindings)
    - .planning/phases/224-conversion-launch-workspace/224-RESEARCH.md §Pattern 2 lines 720-744 (HeroBlockSchema example + BlockSchemas record + validateBlock helper)
    - .planning/phases/224-conversion-launch-workspace/224-RESEARCH.md §Code Examples lines 1535-1541 (block_type usage in content_blocks)
    - test/fixtures/conversion/blocks.ts (created in Task 1 — fixtures must match the schemas; cross-validate)
    - lib/markos/conversion/contracts/page.ts (Task 3) — `content_blocks: unknown[]` typed via BlockType once schema lands
    - obsidian/brain/Pricing Engine Canon.md (placeholder rule for {{MARKOS_PRICING_ENGINE_PENDING}})
  </read_first>
  <action>
    Write tests FIRST.

    1. Create `test/conversion/blocks/schema-validators.test.ts`. For each of 15 block types, the test imports `validBlockFixture(blockType)` and `invalidBlockFixture(blockType)` from `test/fixtures/conversion/blocks` and asserts `validateBlock(valid).success === true` and `validateBlock(invalid).success === false`. Also test the 9 behaviors above (unknown type, hero heading length, custom_html admin_approved, pricing_binding pattern, BlockSchemas key count). Run → MUST fail (no schema yet).

    2. Create `lib/markos/conversion/blocks/schema.ts` with all 15 Zod schemas. Use shared sub-schemas where possible:

       ```typescript
       import { z } from 'zod';

       // Shared constraints
       const PRICING_BINDING_RE = /^(\{\{pricing\.[^}]+\}\}|\{\{MARKOS_PRICING_ENGINE_PENDING\}\})$/;
       const EVIDENCE_BINDING_RE = /^(\{\{evidence\.[^}]+\}\}|\{\{MARKOS_EVIDENCE_PENDING\}\})$/;
       const PricingBinding = z.string().regex(PRICING_BINDING_RE).optional();
       const EvidenceBinding = z.string().regex(EVIDENCE_BINDING_RE).optional();
       const SafeText = z.string().max(2000);

       // 1. Hero
       const HeroBlockSchema = z.object({
         block_type: z.literal('hero'),
         heading: z.string().max(200),
         subheading: z.string().max(500).optional(),
         cta_label: z.string().max(80).optional(),
         cta_href: z.string().url().or(z.string().startsWith('/')).optional(),
         background_image_url: z.string().url().optional(),
         pricing_binding: PricingBinding,
         evidence_binding: EvidenceBinding,
       }).strict();

       // 2. Form
       const FormBlockSchema = z.object({
         block_type: z.literal('form'),
         form_id: z.string().uuid(),
         layout: z.enum(['inline', 'modal', 'embedded']).optional(),
       }).strict();

       // 3. CTA
       const CtaBlockSchema = z.object({
         block_type: z.literal('cta'),
         cta_id: z.string().uuid().optional(),
         label: z.string().max(80),
         href: z.string().or(z.string().startsWith('/')),
         style: z.enum(['primary', 'secondary', 'tertiary']).optional(),
         pricing_binding: PricingBinding,
       }).strict();

       // 4. Testimonial
       const TestimonialBlockSchema = z.object({
         block_type: z.literal('testimonial'),
         quote: SafeText,
         attribution: z.string().max(200),
         company: z.string().max(200).optional(),
         avatar_url: z.string().url().optional(),
       }).strict();

       // 5. Pricing
       const PricingBlockSchema = z.object({
         block_type: z.literal('pricing'),
         pricing_binding: PricingBinding,
         show_comparison: z.boolean().optional(),
         tiers: z.array(z.object({ name: z.string(), price_binding: PricingBinding })).optional(),
       }).strict();

       // 6. FAQ
       const FaqBlockSchema = z.object({
         block_type: z.literal('faq'),
         items: z.array(z.object({ question: z.string().max(500), answer: SafeText })),
       }).strict();

       // 7. Footer
       const FooterBlockSchema = z.object({
         block_type: z.literal('footer'),
         columns: z.array(z.object({ heading: z.string(), links: z.array(z.object({ label: z.string(), href: z.string() })) })),
       }).strict();

       // 8. Content (markdown)
       const ContentBlockSchema = z.object({
         block_type: z.literal('content'),
         markdown: SafeText,
         alignment: z.enum(['left', 'center', 'right']).optional(),
         pricing_binding: PricingBinding,
         evidence_binding: EvidenceBinding,
       }).strict();

       // 9. Image
       const ImageBlockSchema = z.object({
         block_type: z.literal('image'),
         src: z.string().url(),
         alt: z.string().max(500),
         caption: z.string().max(500).optional(),
       }).strict();

       // 10. Video
       const VideoBlockSchema = z.object({
         block_type: z.literal('video'),
         provider: z.enum(['youtube', 'vimeo', 'loom', 'custom']),
         url: z.string().url(),
         poster: z.string().url().optional(),
       }).strict();

       // 11. Comparison
       const ComparisonBlockSchema = z.object({
         block_type: z.literal('comparison'),
         columns: z.array(z.object({ heading: z.string(), items: z.array(SafeText) })),
         pricing_binding: PricingBinding,
       }).strict();

       // 12. Signup widget
       const SignupWidgetBlockSchema = z.object({
         block_type: z.literal('signup_widget'),
         form_id: z.string().uuid().optional(),
         consent_capture_block_id: z.string().uuid().optional(),
       }).strict();

       // 13. Social proof
       const SocialProofBlockSchema = z.object({
         block_type: z.literal('social_proof'),
         logos: z.array(z.object({ alt: z.string(), src: z.string().url() })).optional(),
         metric: z.object({
           label: z.string().max(200),
           value_binding: EvidenceBinding,
         }).optional(),
       }).strict();

       // 14. Evidence block (D-19 evidence)
       const EvidenceBlockSchema = z.object({
         block_type: z.literal('evidence_block'),
         evidence_binding: EvidenceBinding.unwrap(), // required, not optional
         display: z.enum(['stat', 'quote', 'card']).optional(),
       }).strict();

       // 15. Custom HTML (admin-only)
       const CustomHtmlBlockSchema = z.object({
         block_type: z.literal('custom_html'),
         html: z.string(),
         admin_approved: z.literal(true), // hard gate: must be true
       }).strict();

       export type BlockType = 'hero' | 'form' | 'cta' | 'testimonial' | 'pricing' | 'faq' | 'footer' | 'content' | 'image' | 'video' | 'comparison' | 'signup_widget' | 'social_proof' | 'evidence_block' | 'custom_html';

       export const BlockSchemas: Record<BlockType, z.ZodSchema> = {
         hero: HeroBlockSchema,
         form: FormBlockSchema,
         cta: CtaBlockSchema,
         testimonial: TestimonialBlockSchema,
         pricing: PricingBlockSchema,
         faq: FaqBlockSchema,
         footer: FooterBlockSchema,
         content: ContentBlockSchema,
         image: ImageBlockSchema,
         video: VideoBlockSchema,
         comparison: ComparisonBlockSchema,
         signup_widget: SignupWidgetBlockSchema,
         social_proof: SocialProofBlockSchema,
         evidence_block: EvidenceBlockSchema,
         custom_html: CustomHtmlBlockSchema,
       };

       export type Block = z.infer<typeof HeroBlockSchema> | z.infer<typeof FormBlockSchema> | /* etc — discriminated union via block_type */ ...;

       export function validateBlock(block: unknown): { success: true; data: Block } | { success: false; error: string } {
         if (typeof block !== 'object' || block === null) {
           return { success: false, error: 'block must be object' };
         }
         const type = (block as { block_type?: string }).block_type;
         if (!type || !(type in BlockSchemas)) {
           return { success: false, error: `unknown block_type: ${type}` };
         }
         const schema = BlockSchemas[type as BlockType];
         const result = schema.safeParse(block);
         if (!result.success) return { success: false, error: result.error.message };
         return { success: true, data: result.data as Block };
       }

       export function validateContentBlocks(blocks: unknown[]): { success: true; blocks: Block[] } | { success: false; errors: string[] } {
         const errors: string[] = [];
         const validated: Block[] = [];
         for (let i = 0; i < blocks.length; i++) {
           const r = validateBlock(blocks[i]);
           if (!r.success) errors.push(`block[${i}]: ${r.error}`);
           else validated.push(r.data);
         }
         return errors.length > 0 ? { success: false, errors } : { success: true, blocks: validated };
       }
       ```

    3. Create `lib/markos/conversion/blocks/index.ts`: barrel re-exporting `BlockType`, `BlockSchemas`, `validateBlock`, `validateContentBlocks`.

    4. Update `lib/markos/conversion/index.ts` to add `export * from './blocks';`.

    5. Run `npx vitest run test/conversion/blocks/schema-validators.test.ts` → MUST pass (GREEN).

    6. Verify Task 1's `test/fixtures/conversion/blocks.ts` valid + invalid fixtures align with these schemas. If any fixture mismatches, EDIT the fixture (NOT the schema) to align — fixtures are tests' input; schemas are the SOR. Re-run vitest after fixture corrections.

    7. Commit sequence: `test(224-01): add 15-block validator tests` then `feat(224-01): block schema validators with Zod`.

    Do NOT add render-time logic (renderer is plan 03). Do NOT resolve `{{pricing.*}}` bindings at validation time — validators only check shape; runtime resolution lives in the renderer per D-19.
  </action>
  <acceptance_criteria>
    - File exists: `lib/markos/conversion/blocks/schema.ts`
    - File contains all 15 block schema constants: `HeroBlockSchema`, `FormBlockSchema`, `CtaBlockSchema`, `TestimonialBlockSchema`, `PricingBlockSchema`, `FaqBlockSchema`, `FooterBlockSchema`, `ContentBlockSchema`, `ImageBlockSchema`, `VideoBlockSchema`, `ComparisonBlockSchema`, `SignupWidgetBlockSchema`, `SocialProofBlockSchema`, `EvidenceBlockSchema`, `CustomHtmlBlockSchema`
    - File contains `BlockSchemas` record with all 15 keys; `validateBlock` and `validateContentBlocks` exported
    - File contains regex `MARKOS_PRICING_ENGINE_PENDING` (placeholder rule from D-19)
    - `lib/markos/conversion/blocks/index.ts` exists and re-exports from `./schema`
    - `lib/markos/conversion/index.ts` includes `export * from './blocks'`
    - `npx vitest run test/conversion/blocks/schema-validators.test.ts` exits 0 (all 15 valid + 15 invalid + 9 edge cases pass)
    - 224-VALIDATION.md row 224-01-04 (block validators) status flips to ✅ green
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/conversion/blocks/schema-validators.test.ts</automated>
  </verify>
  <done>15 Zod block validators exist with admin-only custom_html gate, placeholder pattern enforcement on pricing/evidence bindings, 30+ tests green; block validation is the only write-time defense for content_blocks integrity (D-02 + D-19); ready for renderer to consume in plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → API write | Operator-authored content_blocks JSONB crosses into Postgres; Zod validators are the only write-time gate (custom_html admin escalation) |
| API → Postgres | Tenant context via `app.active_tenant_id` session var; RLS denies cross-tenant on all 10 new tables |
| internal → conversion_events table | Plan 02 fan-out is the only legitimate writer; this plan creates the table but does not gate inserts (RLS only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-224-01-01 | Information Disclosure | conversion_pages cross-tenant read | mitigate | RLS policy `conversion_pages_tenant_isolation` USING tenant_id = current_setting('app.active_tenant_id'); Task 2 RLS test asserts cross-tenant SELECT returns empty |
| T-224-01-02 | Tampering | content_blocks JSON injection at write (block_type spoofing) | mitigate | Task 4 BlockSchemas record + validateBlock() rejects unknown block_type; runtime renderer (plan 03) re-validates per Pitfall 1 in RESEARCH.md |
| T-224-01-03 | Elevation of Privilege | custom_html block bypassing admin approval | mitigate | Task 4 CustomHtmlBlockSchema requires `admin_approved: z.literal(true)` — non-admin operator cannot satisfy via UI flow (admin_approved set server-side only — wired in plan 07 API layer) |
| T-224-01-04 | Tampering | Polymorphic surface_target_id pointing at another tenant's email_campaign | mitigate | Task 2 migration 126 CHECK enforces enum value; cross-tenant FK orphan prevented in plan 04 repository read-after-write check (Pitfall 4 in RESEARCH.md) |
| T-224-01-05 | Spoofing | Duplicate slug across tenants confusing routing | accept | UNIQUE constraint is per-tenant by design (D-01); routing always carries tenant context via Host header (plan 03 + P201 BYOD pattern) — same slug across tenants is a feature |
| T-224-01-06 | Tampering | experiment_assignments.identity_ref re-assignment to force variant | mitigate | Task 2 UNIQUE INDEX (experiment_id, identity_ref) — INSERT on duplicate raises 23505; sticky-hash determinism prevents identity_ref crafting (plan 05 hash function) |
| T-224-01-07 | Tampering | block validator regex bypass (e.g., `pricing_binding` containing `<script>`) | accept | Validators check shape (string + regex pattern) but rendered output is sanitized in renderer (plan 03 RSC pattern); custom_html DOMPurify pass at render time |
</threat_model>

<verification>
1. `npx vitest run test/conversion/ test/launches/` — all schema, RLS, contract, block validator tests green (40+ tests).
2. `npx vitest --version` returns >= 1.x; `npx playwright --version` returns >= 1.45.
3. `grep -l "F-132" contracts/flow-registry.json` returns the file path.
4. `grep -c "lifecycle_journey" supabase/migrations/126_launch_surfaces.sql` returns >= 1.
5. `node -e "const c = require('./lib/markos/conversion'); console.log(Object.keys(c).filter(k => /^[A-Z]/.test(k)).length)"` returns >= 7 (exported types).
6. Legacy regression: `npm test` (node --test) → all P100-P105 + P201 + P204 tests green (additive phase, zero regression tolerance).
7. 224-VALIDATION.md frontmatter shows `wave_0_complete: true` and 5 task rows for plan 01 (224-01-01 through 224-01-05) all ✅ green.
</verification>

<success_criteria>
- Wave 0 baseline verified (highest F-ID + migration recorded in commit; gap reservation confirmed for F-106..F-131 + migrations 101..120 from prior phases)
- 6 migrations applied creating 10 of 13 P224 tables with RLS enforced and polymorphic CHECK on launch_surfaces (9 enum values)
- 10 F-ID contract YAMLs exist as draft stubs and registered in flow-registry.json
- 11 TypeScript contract files export the public type surface for both modules
- 15 Zod block validators with admin-only custom_html and placeholder-pattern bindings
- 14 fixture factory files cover every enum × status combination needed by plans 02..07
- 23 test files green (10 schema + 10 RLS + 1 block validator + 2 contract parity = 23)
- Module skeletons in place for downstream plans (lib/markos/conversion/{render,forms,events,gates} + lib/markos/launches/{gates,runbook,surfaces,outcomes})
- Legacy regression suite green (P100-P105 + P201 + P204)
- 224-VALIDATION.md per-task rows for 224-01 populated and `wave_0_complete: true`
</success_criteria>

<output>
After completion, create `.planning/phases/224-conversion-launch-workspace/224-01-SUMMARY.md` documenting:
- Baseline state at Wave 0 (actual F-IDs + migrations highest before plan started)
- 13 tables planned (10 created here + 3 deferred to plans 04/06: launch_gates, launch_runbooks, launch_outcomes)
- 10 F-IDs registered (F-132..F-136 + F-139..F-141 + F-143 + F-144); F-137, F-138, F-142, F-145, F-146 deferred to behavior plans
- 15 block validators with placeholder-pattern enforcement
- Files affected (~50 files) — count each: 6 migrations + 10 contracts + 11 TS types + 1 schema + 14 fixtures + 23 tests + barrels
- Test count green
- Decisions taken (e.g., FK deferral pattern for conversion_events.experiment_variant_id and .launch_id; xxhash-wasm chosen over SHA-256 default; UNIQUE constraint placement on experiment_assignments)
- Risks for downstream plans (e.g., if P221 doesn't ship before 224 execution, audience_id FK on conversion_pages stays unresolvable; plan 02 may need ConsentState bridge stub)
</output>
