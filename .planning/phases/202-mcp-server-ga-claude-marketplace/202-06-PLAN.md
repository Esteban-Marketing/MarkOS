---
phase: 202-mcp-server-ga-claude-marketplace
plan: 06
type: execute
wave: 3
depends_on:
  - 202-01
  - 202-04
  - 202-05
files_modified:
  - lib/markos/mcp/tools/marketing/plan-campaign.cjs
  - lib/markos/mcp/tools/marketing/research-audience.cjs
  - lib/markos/mcp/tools/marketing/generate-brief.cjs
  - lib/markos/mcp/tools/marketing/audit-claim.cjs
  - lib/markos/mcp/tools/literacy/list-pain-points.cjs
  - lib/markos/mcp/tools/execution/rank-execution-queue.cjs
  - lib/markos/mcp/tools/execution/schedule-post.cjs
  - lib/markos/mcp/tools/literacy/explain-literacy.cjs
  - lib/markos/mcp/tools/index.cjs
  - contracts/F-71-mcp-session-v2.yaml
  - test/mcp/tools/plan-campaign.test.js
  - test/mcp/tools/research-audience.test.js
  - test/mcp/tools/generate-brief.test.js
  - test/mcp/tools/audit-claim.test.js
  - test/mcp/tools/list-pain-points.test.js
  - test/mcp/tools/rank-execution-queue.test.js
  - test/mcp/tools/schedule-post.test.js
  - test/mcp/tools/explain-literacy.test.js
autonomous: true
requirements:
  - MCP-01
  - QA-01
  - QA-02
  - QA-04
  - QA-08
tdd: true

must_haves:
  truths:
    - "lib/markos/mcp/tools/marketing/plan-campaign.cjs exports { name:'plan_campaign', latency_tier:'llm', mutating:false, cost_model from cost-table, inputSchema, outputSchema, handler({ args, session, req_id, supabase }) } — handler calls lib/markos/crm/copilot.ts (or equivalent LLM planner) with { objective, audience, budget }, returns { content: [{ type:'text', text: JSON of { plan_id, objective, audience, channels:[{channel, blocks:[...]}], created_at } }], _usage: { input_tokens, output_tokens } } — schema-valid per D-13, tenant_id filter at query layer per D-15"
    - "lib/markos/mcp/tools/marketing/research-audience.cjs wires research-audience to pack-loader.cjs + literacy reader (non-LLM, simple tier) returning { content:[{type:'text', text: JSON of { segment, pains:[], archetypes:[], generated_at } }] }"
    - "lib/markos/mcp/tools/marketing/generate-brief.cjs wires generate-brief to bin/lib/brief-parser.cjs + LLM completion (llm tier), returns { content:[{type:'text', text: JSON of brief fields { channel, audience, pain, promise, brand, derived_from } }], _usage }"
    - "lib/markos/mcp/tools/marketing/audit-claim.cjs wires audit-claim to canon evidence lookup + Haiku LLM classifier (llm tier), returns { content:[{type:'text', text: JSON of { claim, supported: boolean, confidence: number, evidence: [{source, quote, score}] } }], _usage }"
    - "lib/markos/mcp/tools/literacy/list-pain-points.cjs wires list-pain-points to pack-loader.cjs pain-point taxonomy (simple tier), returns { content:[{type:'text', text: JSON of { category, items:[{id, name, description, score}] } }] }"
    - "lib/markos/mcp/tools/execution/rank-execution-queue.cjs wires rank-execution-queue to lib/markos/crm/execution.ts (simple tier), returns { content:[{type:'text', text: JSON of { tenant_id, ranked:[{id, rank, score, reason}], generated_at } }] }"
    - "lib/markos/mcp/tools/execution/schedule-post.cjs wires schedule-post to lib/markos/crm/outbound/* channel-queue write (simple tier, MUTATING=true, preview returns { tool:'schedule_post', channel, scheduled_at, content_preview }), returns { content:[{type:'text', text: JSON of { post_id, channel, scheduled_at, status:'queued' } }] } on committed run"
    - "lib/markos/mcp/tools/literacy/explain-literacy.cjs wires explain-literacy to literacy reader + pack archetypes (simple tier), returns { content:[{type:'text', text: JSON of { node_id, archetype, description, examples:[], canonical_slug } }] }"
    - "lib/markos/mcp/tools/index.cjs REPLACES the Phase 200 hard-coded stubs for these 8 tools with requires of the live handlers from this plan; every TOOL_DEFINITIONS entry now carries: name, description, inputSchema (JSON Schema), outputSchema (JSON Schema), latency_tier, mutating, cost_model, handler, preview (for mutating tools only) — the 2 retained Phase 200 tools (draft_message, run_neuro_audit) are preserved with added latency_tier/mutating/cost_model fields"
    - "contracts/F-71-mcp-session-v2.yaml supersedes F-71-v1 — adds session persistence + cost metering fields to the existing session contract + documents all 10 initially-live tools (2 Phase 200 retained + 8 wave-0 wired) with inputSchema + outputSchema refs; Plan 202-07 extends with 20 net-new tools via F-90..F-93"
    - "8 node:test suites (one per tool) assert: (1) handler returns schema-valid output against its outputSchema, (2) tenant_id filter present in every read query (D-15), (3) schedule-post is marked mutating:true + exposes preview(args), (4) LLM tools populate _usage with integer token counts, (5) audit emission via pipeline is implicit (pipeline tested in Plan 202-04) — tools just return the content envelope"
  artifacts:
    - path: "lib/markos/mcp/tools/marketing/plan-campaign.cjs"
      provides: "Live plan_campaign handler (D-04 hero demo)"
      exports: ["descriptor"]
    - path: "lib/markos/mcp/tools/marketing/research-audience.cjs"
      provides: "Live research_audience handler"
      exports: ["descriptor"]
    - path: "lib/markos/mcp/tools/marketing/generate-brief.cjs"
      provides: "Live generate_brief handler"
      exports: ["descriptor"]
    - path: "lib/markos/mcp/tools/marketing/audit-claim.cjs"
      provides: "Live audit_claim handler"
      exports: ["descriptor"]
    - path: "lib/markos/mcp/tools/literacy/list-pain-points.cjs"
      provides: "Live list_pain_points handler"
      exports: ["descriptor"]
    - path: "lib/markos/mcp/tools/execution/rank-execution-queue.cjs"
      provides: "Live rank_execution_queue handler"
      exports: ["descriptor"]
    - path: "lib/markos/mcp/tools/execution/schedule-post.cjs"
      provides: "Live schedule_post handler (mutating + preview)"
      exports: ["descriptor"]
    - path: "lib/markos/mcp/tools/literacy/explain-literacy.cjs"
      provides: "Live explain_literacy handler"
      exports: ["descriptor"]
    - path: "lib/markos/mcp/tools/index.cjs"
      provides: "TOOL_DEFINITIONS with 10 live entries (2 retained + 8 wired) + descriptor-aware listTools + invokeTool"
      contains: "require('./marketing/plan-campaign.cjs')"
    - path: "contracts/F-71-mcp-session-v2.yaml"
      provides: "Updated OAuth + session + 10-live-tools contract (D-02 marketplace pitch)"
      contains: "plan_campaign"
    - path: "test/mcp/tools/plan-campaign.test.js"
      provides: "plan_campaign schema + handler suite"
      contains: "Suite 202-06"
    - path: "test/mcp/tools/research-audience.test.js"
      provides: "research_audience handler suite"
      contains: "Suite 202-06"
    - path: "test/mcp/tools/generate-brief.test.js"
      provides: "generate_brief handler suite"
      contains: "Suite 202-06"
    - path: "test/mcp/tools/audit-claim.test.js"
      provides: "audit_claim handler suite"
      contains: "Suite 202-06"
    - path: "test/mcp/tools/list-pain-points.test.js"
      provides: "list_pain_points handler suite"
      contains: "Suite 202-06"
    - path: "test/mcp/tools/rank-execution-queue.test.js"
      provides: "rank_execution_queue handler suite"
      contains: "Suite 202-06"
    - path: "test/mcp/tools/schedule-post.test.js"
      provides: "schedule_post mutating + preview suite"
      contains: "Suite 202-06"
    - path: "test/mcp/tools/explain-literacy.test.js"
      provides: "explain_literacy handler suite"
      contains: "Suite 202-06"
  key_links:
    - from: "lib/markos/mcp/tools/marketing/plan-campaign.cjs"
      to: "lib/markos/mcp/cost-table.cjs"
      via: "cost_model imported via COST_TABLE['plan_campaign']"
      pattern: "COST_TABLE"
    - from: "lib/markos/mcp/tools/execution/schedule-post.cjs"
      to: "lib/markos/mcp/pipeline.cjs"
      via: "mutating=true triggers approval-token round-trip in Plan 202-04 pipeline"
      pattern: "mutating"
    - from: "lib/markos/mcp/tools/literacy/list-pain-points.cjs"
      to: "lib/markos/packs/pack-loader.cjs"
      via: "pack-loader for taxonomy + archetype reads"
      pattern: "pack-loader"
    - from: "lib/markos/mcp/tools/index.cjs"
      to: "all 8 new tool files"
      via: "require + push into TOOL_DEFINITIONS array"
      pattern: "require\\('./marketing/|require\\('./literacy/|require\\('./execution/"
---

<objective>
Wire the 8 Phase 200 wave-0 stubs (`plan_campaign`, `research_audience`, `generate_brief`,
`audit_claim`, `list_pain_points`, `rank_execution_queue`, `schedule_post`,
`explain_literacy`) to live backends. Each tool now has: a JSON Schema input + output
contract (for Plan 202-04 AJV validation), a `latency_tier` (simple or llm), a `mutating`
flag (true only for `schedule_post`), a `cost_model` pointer into Plan 202-03's COST_TABLE,
a concrete handler that calls the existing MarkOS library (copilot, pack-loader, brief
parser, crm execution, outbound channel queue, literacy reader), and a `preview(args)`
function where mutating. Plan 202-04's pipeline picks these up via `buildToolRegistryFromDefinitions`.

Purpose: D-02 marketplace pitch "30 tools, all live, zero stubs" — 8/30 graduations
delivered by this plan. QA-01 (contract-first — F-71 bumped to v2), QA-02 (AJV input +
output validated via Plan 202-04 ajv.cjs registry), QA-08 (LLM-backed tools plan_campaign
/ audit_claim / generate_brief get eval fixtures in Plan 202-10 — placeholders declared
here).

Output:
- 8 tool handler files under `lib/markos/mcp/tools/{marketing,literacy,execution}/`
- Updated `lib/markos/mcp/tools/index.cjs` with 10 live descriptors
- F-71-v2 contract supersedes F-71-v1
- 8 per-tool test suites
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/202-mcp-server-ga-claude-marketplace/202-CONTEXT.md
@.planning/phases/202-mcp-server-ga-claude-marketplace/202-RESEARCH.md
@.planning/phases/202-mcp-server-ga-claude-marketplace/202-01-PLAN.md
@.planning/phases/202-mcp-server-ga-claude-marketplace/202-03-PLAN.md
@.planning/phases/202-mcp-server-ga-claude-marketplace/202-04-PLAN.md
@.planning/phases/202-mcp-server-ga-claude-marketplace/202-05-PLAN.md
@lib/markos/mcp/tools/index.cjs
@contracts/F-71-mcp-session-v1.yaml
@lib/markos/packs/pack-loader.cjs
@bin/lib/generate-runner.cjs
@bin/lib/brief-parser.cjs
</context>

<interfaces>
<!-- From Plan 202-03: -->
```javascript
const COST_TABLE: Record<string, { base_cents: number; model: string | null; avg_tokens?: { in: number; out: number } }>;
```

<!-- From Plan 202-04: -->
```javascript
// Pipeline pulls handlers from toolRegistry[tool_name] where each entry is:
interface ToolDescriptor {
  name: string;
  description: string;
  latency_tier: 'simple' | 'llm' | 'long';
  mutating: boolean;
  cost_model: { base_cents: number; model: string | null };
  inputSchema: object;     // JSON Schema
  outputSchema: object;    // JSON Schema
  handler: (ctx: { args, session, req_id, supabase, redis, _meta }) => Promise<{ content: any[]; _usage?: { input_tokens: number; output_tokens: number } }>;
  preview?: (args: any) => any; // only when mutating
}
```

<!-- Existing MarkOS libraries handlers should call (don't re-invent): -->
```javascript
// bin/lib/generate-runner.cjs
async function runDraft({ channel, audience, pain, promise, brand }): Promise<DraftResult>;
function auditDraft({ text }, brief): AuditResult;

// bin/lib/brief-parser.cjs
async function parseBrief(prompt: string): Promise<{ channel, audience, pain, promise, brand, derived_from }>;

// lib/markos/packs/pack-loader.cjs
async function loadPackForTenant(supabase, tenant_id): Promise<{ pains: PainPoint[]; archetypes: Archetype[]; canon: CanonDoc[] }>;

// lib/markos/crm/execution.ts
async function rankExecutionQueue(supabase, tenant_id): Promise<{ ranked: Array<{ id, score, reason }> }>;

// lib/markos/crm/outbound/channel-queue.cjs  (or equivalent)
async function scheduleChannelPost(supabase, { tenant_id, channel, content, scheduled_at }): Promise<{ post_id, status }>;
```
(Executor: if any of these signatures differ from actual repo state, adapt — the descriptor contract is what's fixed.)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 4 marketing/audit handlers (plan-campaign, research-audience, generate-brief, audit-claim) + 4 test suites</name>
  <files>
    lib/markos/mcp/tools/marketing/plan-campaign.cjs,
    lib/markos/mcp/tools/marketing/research-audience.cjs,
    lib/markos/mcp/tools/marketing/generate-brief.cjs,
    lib/markos/mcp/tools/marketing/audit-claim.cjs,
    test/mcp/tools/plan-campaign.test.js,
    test/mcp/tools/research-audience.test.js,
    test/mcp/tools/generate-brief.test.js,
    test/mcp/tools/audit-claim.test.js
  </files>
  <read_first>
    - lib/markos/mcp/tools/index.cjs (existing stubs — shape handlers return)
    - bin/lib/generate-runner.cjs (runDraft + auditDraft signatures)
    - bin/lib/brief-parser.cjs (parseBrief signature; if doesn't exist, create minimal wrapper)
    - lib/markos/packs/pack-loader.cjs (loadPackForTenant shape)
    - lib/markos/mcp/cost-table.cjs (Plan 202-03 — COST_TABLE entries for each tool)
    - .planning/phases/202-mcp-server-ga-claude-marketplace/202-RESEARCH.md §"Tool Inventory (30 tools)" Wave-0 stubs section (handler wire targets)
    - .planning/phases/202-mcp-server-ga-claude-marketplace/202-CONTEXT.md D-02, D-04, D-13, D-15
  </read_first>
  <behavior>
    - Each handler exports `{ descriptor }` where descriptor follows the ToolDescriptor interface above
    - `plan-campaign.cjs`: latency_tier='llm', mutating=false, input `{ objective:string(1..), audience:string(1..), budget?:string }`, output `{ plan_id:string, objective:string, audience:string, channels:Array<{ channel:string, blocks:Array<object> }>, created_at:string(format=date-time) }` — handler calls `buildCampaignPlan` helper (new internal fn in this file) that composes pack-loader data + LLM planner; `_usage` populated from LLM SDK response
    - `research-audience.cjs`: latency_tier='simple', mutating=false, input `{ segment:string(1..) }`, output `{ segment:string, pains:array of string, archetypes:array of string, generated_at:string }` — handler calls pack-loader for tenant's pains + archetypes; reads tenant_id FROM session (D-15 tenant scope)
    - `generate-brief.cjs`: latency_tier='llm', mutating=false, input `{ prompt:string(1..) }`, output `{ channel:string, audience:string, pain:string, promise:string, brand:string, derived_from:string }` — handler calls brief-parser + LLM completion; returns structured brief
    - `audit-claim.cjs`: latency_tier='llm', mutating=false, input `{ claim:string(1..), model?:string }`, output `{ claim:string, supported:boolean, confidence:number, evidence:Array<{source:string, quote:string, score:number}> }` — handler calls canon evidence lookup + Haiku classifier
    - Every handler reads tenant_id from `session.tenant_id` (D-15); every read query filters on tenant_id
    - Descriptor `cost_model` pulled from `require('../../cost-table.cjs').COST_TABLE[toolName]`
    - 4 test suites >= 12 tests total covering: descriptor shape present, outputSchema validates a real handler return, tenant_id filter applied, _usage populated for LLM handlers (mock LLM client injected)
  </behavior>
  <action>
**Step 1. Create `lib/markos/mcp/tools/marketing/plan-campaign.cjs`:**

```javascript
'use strict';

// Tool: plan_campaign (D-04 hero demo).
// Wires to pack-loader for audience/literacy context + LLM planner.
// latency_tier = llm per D-18; mutating = false (plan is a read/generate, not a write).

const { randomUUID } = require('node:crypto');
const { COST_TABLE } = require('../../cost-table.cjs');

const inputSchema = {
  type: 'object',
  required: ['objective', 'audience'],
  additionalProperties: false,
  properties: {
    objective: { type: 'string', minLength: 1, maxLength: 1000 },
    audience:  { type: 'string', minLength: 1, maxLength: 500 },
    budget:    { type: 'string', maxLength: 200 },
  },
};

const outputSchema = {
  type: 'object',
  required: ['content', '_usage'],
  additionalProperties: true,
  properties: {
    content: {
      type: 'array',
      items: {
        type: 'object',
        required: ['type', 'text'],
        properties: { type: { type: 'string', enum: ['text'] }, text: { type: 'string' } },
      },
    },
    _usage: {
      type: 'object',
      required: ['input_tokens', 'output_tokens'],
      properties: {
        input_tokens: { type: 'integer', minimum: 0 },
        output_tokens: { type: 'integer', minimum: 0 },
      },
    },
  },
};

async function invokeLlmPlanner(session, args, deps) {
  const llm = deps.llm || await loadLLM();
  const sys = `You are a marketing campaign planner for tenant ${session.tenant_id}. Produce a structured campaign plan grounded in brand canon.`;
  const user = `Objective: ${args.objective}\nAudience: ${args.audience}\nBudget: ${args.budget || 'n/a'}\nReturn JSON with channels: [{channel, blocks:[...]}].`;

  const resp = await llm.messages.create({
    model: COST_TABLE.plan_campaign.model,
    max_tokens: 1500,
    system: sys,
    messages: [{ role: 'user', content: user }],
  });

  const text = (resp.content && resp.content[0] && resp.content[0].text) || '{}';
  let parsed;
  try { parsed = JSON.parse(text); } catch { parsed = { channels: [] }; }
  return {
    parsed,
    input_tokens: resp.usage?.input_tokens || 0,
    output_tokens: resp.usage?.output_tokens || 0,
  };
}

async function loadLLM() {
  try {
    const Anthropic = require('@anthropic-ai/sdk');
    return new Anthropic.default({ apiKey: process.env.ANTHROPIC_API_KEY });
  } catch {
    return null;
  }
}

async function handler(ctx) {
  const { args, session, supabase } = ctx;
  const deps = ctx.deps || {};

  const { parsed, input_tokens, output_tokens } = await invokeLlmPlanner(session, args, deps);

  const plan = {
    plan_id: `plan-${randomUUID()}`,
    objective: args.objective,
    audience: args.audience,
    channels: Array.isArray(parsed.channels) ? parsed.channels : [],
    created_at: new Date().toISOString(),
    tenant_id: session.tenant_id, // D-15 tenant scope
  };

  return {
    content: [{ type: 'text', text: JSON.stringify(plan, null, 2) }],
    _usage: { input_tokens, output_tokens },
  };
}

const descriptor = {
  name: 'plan_campaign',
  description: 'Produce a campaign plan outline grounded in brand canon from an objective + audience + optional budget.',
  latency_tier: 'llm',
  mutating: false,
  cost_model: COST_TABLE.plan_campaign,
  inputSchema,
  outputSchema,
  handler,
};

module.exports = { descriptor, inputSchema, outputSchema, handler };
```

**Step 2. Create `lib/markos/mcp/tools/marketing/research-audience.cjs`:**

```javascript
'use strict';

// Tool: research_audience. Simple-tier, non-LLM. Reads pack-loader for tenant pains + archetypes.

const { COST_TABLE } = require('../../cost-table.cjs');

const inputSchema = {
  type: 'object',
  required: ['segment'],
  additionalProperties: false,
  properties: { segment: { type: 'string', minLength: 1, maxLength: 200 } },
};

const outputSchema = {
  type: 'object',
  required: ['content'],
  additionalProperties: true,
  properties: {
    content: {
      type: 'array',
      items: {
        type: 'object',
        required: ['type', 'text'],
        properties: { type: { enum: ['text'] }, text: { type: 'string' } },
      },
    },
  },
};

async function loadPack(supabase, tenant_id, deps) {
  if (deps && deps.loadPack) return deps.loadPack(supabase, tenant_id);
  try {
    const { loadPackForTenant } = require('../../../packs/pack-loader.cjs');
    return loadPackForTenant(supabase, tenant_id);
  } catch {
    return { pains: [], archetypes: [], canon: [] };
  }
}

async function handler(ctx) {
  const { args, session, supabase } = ctx;
  const pack = await loadPack(supabase, session.tenant_id, ctx.deps);

  const pains = (pack.pains || []).filter(p => !args.segment || (p.segment || '').toLowerCase() === args.segment.toLowerCase()).map(p => p.name || p);
  const archetypes = (pack.archetypes || []).map(a => a.slug || a.name || String(a));

  return {
    content: [{
      type: 'text',
      text: JSON.stringify({
        segment: args.segment,
        tenant_id: session.tenant_id,
        pains, archetypes,
        generated_at: new Date().toISOString(),
      }, null, 2),
    }],
  };
}

const descriptor = {
  name: 'research_audience',
  description: 'Return an audience research snapshot (pains + archetypes) for a segment key, tenant-scoped.',
  latency_tier: 'simple',
  mutating: false,
  cost_model: COST_TABLE.research_audience,
  inputSchema,
  outputSchema,
  handler,
};

module.exports = { descriptor, inputSchema, outputSchema, handler };
```

**Step 3. Create `lib/markos/mcp/tools/marketing/generate-brief.cjs`:**

```javascript
'use strict';

const { COST_TABLE } = require('../../cost-table.cjs');

const inputSchema = {
  type: 'object', required: ['prompt'], additionalProperties: false,
  properties: { prompt: { type: 'string', minLength: 1, maxLength: 5000 } },
};

const outputSchema = {
  type: 'object', required: ['content', '_usage'], additionalProperties: true,
  properties: {
    content: { type: 'array', items: { type: 'object', required: ['type', 'text'], properties: { type: { enum: ['text'] }, text: { type: 'string' } } } },
    _usage: { type: 'object', required: ['input_tokens', 'output_tokens'], properties: { input_tokens: { type: 'integer', minimum: 0 }, output_tokens: { type: 'integer', minimum: 0 } } },
  },
};

async function invokeLLM(prompt, deps) {
  const llm = deps.llm;
  if (!llm) return { text: JSON.stringify({ channel: 'email', audience: 'derived', pain: prompt.slice(0, 80), promise: 'derived', brand: 'markos', derived_from: 'llm-fallback' }), input_tokens: 0, output_tokens: 0 };
  const resp = await llm.messages.create({
    model: COST_TABLE.generate_brief.model,
    max_tokens: 800,
    system: 'You extract marketing briefs from freeform prompts. Return strict JSON { channel, audience, pain, promise, brand, derived_from }.',
    messages: [{ role: 'user', content: prompt }],
  });
  return {
    text: (resp.content && resp.content[0] && resp.content[0].text) || '{}',
    input_tokens: resp.usage?.input_tokens || 0,
    output_tokens: resp.usage?.output_tokens || 0,
  };
}

async function handler(ctx) {
  const { args, session } = ctx;
  const deps = ctx.deps || {};
  const { text, input_tokens, output_tokens } = await invokeLLM(args.prompt, deps);
  let parsed;
  try { parsed = JSON.parse(text); } catch { parsed = { channel: 'email', audience: 'unknown', pain: args.prompt.slice(0, 80), promise: 'unknown', brand: 'markos', derived_from: 'parse_failed' }; }
  parsed.tenant_id = session.tenant_id;
  return {
    content: [{ type: 'text', text: JSON.stringify(parsed, null, 2) }],
    _usage: { input_tokens, output_tokens },
  };
}

const descriptor = {
  name: 'generate_brief',
  description: 'Generate a structured marketing brief (channel, audience, pain, promise, brand) from a freeform prompt.',
  latency_tier: 'llm',
  mutating: false,
  cost_model: COST_TABLE.generate_brief,
  inputSchema, outputSchema, handler,
};

module.exports = { descriptor, inputSchema, outputSchema, handler };
```

**Step 4. Create `lib/markos/mcp/tools/marketing/audit-claim.cjs`:**

```javascript
'use strict';

const { COST_TABLE } = require('../../cost-table.cjs');

const inputSchema = {
  type: 'object', required: ['claim'], additionalProperties: false,
  properties: {
    claim: { type: 'string', minLength: 1, maxLength: 2000 },
    model: { type: 'string', maxLength: 200 },
  },
};

const outputSchema = {
  type: 'object', required: ['content', '_usage'], additionalProperties: true,
  properties: {
    content: { type: 'array', items: { type: 'object', required: ['type', 'text'], properties: { type: { enum: ['text'] }, text: { type: 'string' } } } },
    _usage: { type: 'object', required: ['input_tokens', 'output_tokens'], properties: { input_tokens: { type: 'integer', minimum: 0 }, output_tokens: { type: 'integer', minimum: 0 } } },
  },
};

async function loadCanon(supabase, tenant_id, deps) {
  if (deps && deps.loadCanon) return deps.loadCanon(supabase, tenant_id);
  try {
    const { loadPackForTenant } = require('../../../packs/pack-loader.cjs');
    const pack = await loadPackForTenant(supabase, tenant_id);
    return pack.canon || [];
  } catch { return []; }
}

async function runClassifier(claim, canon, deps) {
  const llm = deps.llm;
  if (!llm) return { supported: false, confidence: 0.0, evidence: [], input_tokens: 0, output_tokens: 0 };
  const sys = 'You are a claim auditor. Given a claim and canon evidence, return strict JSON { supported: boolean, confidence: number (0..1), evidence: [{ source, quote, score }] }.';
  const user = `Claim: ${claim}\nCanon:\n${(canon || []).slice(0, 5).map((c, i) => `[${i+1}] ${c.title || c.source || ''}: ${(c.text || c.content || '').slice(0, 500)}`).join('\n')}`;
  const resp = await llm.messages.create({
    model: COST_TABLE.audit_claim.model,
    max_tokens: 600,
    system: sys,
    messages: [{ role: 'user', content: user }],
  });
  let parsed;
  try { parsed = JSON.parse((resp.content && resp.content[0] && resp.content[0].text) || '{}'); }
  catch { parsed = { supported: false, confidence: 0.0, evidence: [] }; }
  return { ...parsed, input_tokens: resp.usage?.input_tokens || 0, output_tokens: resp.usage?.output_tokens || 0 };
}

async function handler(ctx) {
  const { args, session, supabase } = ctx;
  const deps = ctx.deps || {};
  const canon = await loadCanon(supabase, session.tenant_id, deps);
  const result = await runClassifier(args.claim, canon, deps);
  return {
    content: [{ type: 'text', text: JSON.stringify({
      claim: args.claim,
      tenant_id: session.tenant_id,
      supported: result.supported === true,
      confidence: Number(result.confidence) || 0,
      evidence: Array.isArray(result.evidence) ? result.evidence : [],
    }, null, 2) }],
    _usage: { input_tokens: result.input_tokens || 0, output_tokens: result.output_tokens || 0 },
  };
}

const descriptor = {
  name: 'audit_claim',
  description: 'Verify whether a marketing claim is supported by canon evidence. Returns { supported, confidence, evidence }.',
  latency_tier: 'llm',
  mutating: false,
  cost_model: COST_TABLE.audit_claim,
  inputSchema, outputSchema, handler,
};

module.exports = { descriptor, inputSchema, outputSchema, handler };
```

**Step 5. Create per-tool tests. `test/mcp/tools/plan-campaign.test.js`:**

```javascript
'use strict';
const test = require('node:test');
const assert = require('node:assert/strict');
const { descriptor, outputSchema } = require('../../../lib/markos/mcp/tools/marketing/plan-campaign.cjs');
const { compileToolSchemas, getToolValidator } = require('../../../lib/markos/mcp/ajv.cjs');

function fakeLLM(text) {
  return {
    messages: {
      async create({ max_tokens }) {
        return { content: [{ type: 'text', text }], usage: { input_tokens: 100, output_tokens: 200 } };
      },
    },
  };
}

test('Suite 202-06: plan_campaign descriptor has required fields (name, latency_tier=llm, mutating=false)', () => {
  assert.equal(descriptor.name, 'plan_campaign');
  assert.equal(descriptor.latency_tier, 'llm');
  assert.equal(descriptor.mutating, false);
  assert.ok(descriptor.cost_model);
  assert.ok(descriptor.inputSchema);
  assert.ok(descriptor.outputSchema);
  assert.equal(typeof descriptor.handler, 'function');
});

test('Suite 202-06: plan_campaign handler returns schema-valid output', async () => {
  compileToolSchemas({ plan_campaign: { input: descriptor.inputSchema, output: descriptor.outputSchema } });
  const v = getToolValidator('plan_campaign');

  const llmText = JSON.stringify({ channels: [{ channel: 'email', blocks: [{ subject: 'Hello', body: 'world' }] }] });
  const r = await descriptor.handler({
    args: { objective: 'launch', audience: 'founders', budget: '$1k' },
    session: { tenant_id: 't1' },
    supabase: null,
    deps: { llm: fakeLLM(llmText) },
    req_id: 'r',
  });
  assert.equal(v.validateOutput(r), true, JSON.stringify(v.validateOutput.errors));
  assert.ok(r._usage.input_tokens > 0);
  assert.ok(r._usage.output_tokens > 0);
});

test('Suite 202-06: plan_campaign output embeds tenant_id (D-15 tenant scope)', async () => {
  const r = await descriptor.handler({
    args: { objective: 'launch', audience: 'founders' },
    session: { tenant_id: 't1' },
    supabase: null,
    deps: { llm: fakeLLM('{"channels":[]}') },
  });
  const parsed = JSON.parse(r.content[0].text);
  assert.equal(parsed.tenant_id, 't1');
});
```

`test/mcp/tools/research-audience.test.js`:

```javascript
'use strict';
const test = require('node:test');
const assert = require('node:assert/strict');
const { descriptor } = require('../../../lib/markos/mcp/tools/marketing/research-audience.cjs');
const { compileToolSchemas, getToolValidator } = require('../../../lib/markos/mcp/ajv.cjs');

test('Suite 202-06: research_audience descriptor (latency_tier=simple, mutating=false)', () => {
  assert.equal(descriptor.name, 'research_audience');
  assert.equal(descriptor.latency_tier, 'simple');
  assert.equal(descriptor.mutating, false);
});

test('Suite 202-06: research_audience returns schema-valid output', async () => {
  compileToolSchemas({ research_audience: { input: descriptor.inputSchema, output: descriptor.outputSchema } });
  const v = getToolValidator('research_audience');
  const r = await descriptor.handler({
    args: { segment: 'founders' },
    session: { tenant_id: 't1' },
    deps: { loadPack: async () => ({ pains: [{ name: 'slow onboarding', segment: 'founders' }], archetypes: [{ slug: 'builder' }] }) },
  });
  assert.equal(v.validateOutput(r), true);
  const parsed = JSON.parse(r.content[0].text);
  assert.equal(parsed.segment, 'founders');
  assert.equal(parsed.tenant_id, 't1');
  assert.deepEqual(parsed.archetypes, ['builder']);
});

test('Suite 202-06: research_audience degrades gracefully when pack-loader throws', async () => {
  const r = await descriptor.handler({
    args: { segment: 'x' },
    session: { tenant_id: 't1' },
    deps: { loadPack: async () => { throw new Error('pack missing'); } },
  });
  // Should not throw; returns empty lists
  assert.ok(r.content[0].text);
});
```

`test/mcp/tools/generate-brief.test.js`:

```javascript
'use strict';
const test = require('node:test');
const assert = require('node:assert/strict');
const { descriptor } = require('../../../lib/markos/mcp/tools/marketing/generate-brief.cjs');

function fakeLLM(text) {
  return { messages: { async create() { return { content: [{ text }], usage: { input_tokens: 50, output_tokens: 120 } }; } } };
}

test('Suite 202-06: generate_brief descriptor (llm tier, non-mutating)', () => {
  assert.equal(descriptor.latency_tier, 'llm');
  assert.equal(descriptor.mutating, false);
});

test('Suite 202-06: generate_brief returns parsed JSON brief with _usage', async () => {
  const r = await descriptor.handler({
    args: { prompt: 'email campaign for early adopters' },
    session: { tenant_id: 't1' },
    deps: { llm: fakeLLM(JSON.stringify({ channel: 'email', audience: 'early adopters', pain: 'adoption', promise: 'fast setup', brand: 'markos', derived_from: 'prompt' })) },
  });
  const parsed = JSON.parse(r.content[0].text);
  assert.equal(parsed.channel, 'email');
  assert.equal(parsed.tenant_id, 't1');
  assert.equal(r._usage.input_tokens, 50);
});

test('Suite 202-06: generate_brief falls back gracefully on unparseable LLM response', async () => {
  const r = await descriptor.handler({
    args: { prompt: 'test' },
    session: { tenant_id: 't1' },
    deps: { llm: fakeLLM('not json') },
  });
  const parsed = JSON.parse(r.content[0].text);
  assert.equal(parsed.derived_from, 'parse_failed');
});
```

`test/mcp/tools/audit-claim.test.js`:

```javascript
'use strict';
const test = require('node:test');
const assert = require('node:assert/strict');
const { descriptor } = require('../../../lib/markos/mcp/tools/marketing/audit-claim.cjs');

function fakeLLM(text) {
  return { messages: { async create() { return { content: [{ text }], usage: { input_tokens: 300, output_tokens: 100 } }; } } };
}

test('Suite 202-06: audit_claim descriptor (llm tier, non-mutating, Haiku model)', () => {
  assert.equal(descriptor.name, 'audit_claim');
  assert.equal(descriptor.latency_tier, 'llm');
  assert.ok(/haiku/i.test(descriptor.cost_model.model || ''));
});

test('Suite 202-06: audit_claim returns { supported, confidence, evidence } wrapped in content', async () => {
  const r = await descriptor.handler({
    args: { claim: 'We have the fastest onboarding' },
    session: { tenant_id: 't1' },
    deps: {
      loadCanon: async () => [{ title: 'Benchmarks', text: 'onboarding in 90s' }],
      llm: fakeLLM(JSON.stringify({ supported: true, confidence: 0.8, evidence: [{ source: 'Benchmarks', quote: 'onboarding in 90s', score: 0.9 }] })),
    },
  });
  const parsed = JSON.parse(r.content[0].text);
  assert.equal(parsed.supported, true);
  assert.equal(parsed.confidence, 0.8);
  assert.equal(parsed.evidence.length, 1);
  assert.equal(parsed.tenant_id, 't1');
});
```
  </action>
  <verify>
    <automated>node --test test/mcp/tools/plan-campaign.test.js test/mcp/tools/research-audience.test.js test/mcp/tools/generate-brief.test.js test/mcp/tools/audit-claim.test.js</automated>
  </verify>
  <acceptance_criteria>
    - All 4 marketing handler files exist under `lib/markos/mcp/tools/marketing/`
    - `grep "latency_tier: 'llm'" lib/markos/mcp/tools/marketing/plan-campaign.cjs lib/markos/mcp/tools/marketing/generate-brief.cjs lib/markos/mcp/tools/marketing/audit-claim.cjs` returns 3 matches
    - `grep "latency_tier: 'simple'" lib/markos/mcp/tools/marketing/research-audience.cjs` returns 1 match
    - `grep "mutating: false" lib/markos/mcp/tools/marketing/*.cjs` returns >= 4 matches
    - `grep "session.tenant_id" lib/markos/mcp/tools/marketing/*.cjs` returns >= 4 matches (D-15 tenant scope)
    - `grep "COST_TABLE" lib/markos/mcp/tools/marketing/*.cjs` returns >= 4 matches
    - `grep "input_tokens" lib/markos/mcp/tools/marketing/plan-campaign.cjs lib/markos/mcp/tools/marketing/generate-brief.cjs lib/markos/mcp/tools/marketing/audit-claim.cjs` returns >= 3 matches (LLM _usage)
    - `node --test test/mcp/tools/plan-campaign.test.js test/mcp/tools/research-audience.test.js test/mcp/tools/generate-brief.test.js test/mcp/tools/audit-claim.test.js` exits 0 with >= 11 tests total
  </acceptance_criteria>
  <done>4 marketing handlers + 4 test suites committed and green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 4 literacy/execution handlers (list-pain-points, rank-execution-queue, schedule-post, explain-literacy) + 4 test suites</name>
  <files>
    lib/markos/mcp/tools/literacy/list-pain-points.cjs,
    lib/markos/mcp/tools/execution/rank-execution-queue.cjs,
    lib/markos/mcp/tools/execution/schedule-post.cjs,
    lib/markos/mcp/tools/literacy/explain-literacy.cjs,
    test/mcp/tools/list-pain-points.test.js,
    test/mcp/tools/rank-execution-queue.test.js,
    test/mcp/tools/schedule-post.test.js,
    test/mcp/tools/explain-literacy.test.js
  </files>
  <read_first>
    - Task 1 output (descriptor shape + tests pattern to mirror)
    - lib/markos/packs/pack-loader.cjs
    - lib/markos/crm/execution.ts (or .cjs; if missing, handler shells out to pack pain taxonomy)
    - .planning/phases/202-mcp-server-ga-claude-marketplace/202-CONTEXT.md D-03 (mutating → approval round-trip), D-16
    - .planning/phases/202-mcp-server-ga-claude-marketplace/202-UI-SPEC.md (preview shape informed by UI but not blocking here)
  </read_first>
  <behavior>
    - `list-pain-points.cjs` simple/non-mutating; input `{ category?:string }`; output `{ content:[{ type:'text', text: JSON of { tenant_id, category, items:[{ id, name, description, score }] } }] }`
    - `rank-execution-queue.cjs` simple/non-mutating; input `{ limit?:integer(1..100) }`; output `{ content:[{ type:'text', text: JSON of { tenant_id, ranked:[{ id, rank, score, reason }], generated_at } }] }` — calls rankExecutionQueue(supabase, session.tenant_id) or a deps-injected substitute
    - `schedule-post.cjs` simple/**mutating=true**; preview(args) returns `{ tool:'schedule_post', channel, scheduled_at, content_preview: content.slice(0,200) }`; input `{ channel:enum[email,x,linkedin,sms], content:string(1..5000), scheduled_at:string(format=date-time), approval_token?:string }`; output `{ content:[{ type:'text', text: JSON of { post_id, channel, scheduled_at, status:'queued', tenant_id } }] }`
    - `schedule-post.cjs` handler ONLY runs when `args.approval_token` present (pipeline enforces round-trip; handler doesn't re-check — pipeline already consumed the token)
    - `explain-literacy.cjs` simple/non-mutating; input `{ node_id:string | archetype:string }` (oneOf); output `{ content:[{ type:'text', text: JSON of { node_id/archetype, description, examples:[], canonical_slug, tenant_id } }] }`
    - 4 test suites >= 12 tests total; schedule-post test asserts `mutating: true` + `preview` function present + preview return shape
  </behavior>
  <action>
**Step 1. Create `lib/markos/mcp/tools/literacy/list-pain-points.cjs`:**

```javascript
'use strict';

const { COST_TABLE } = require('../../cost-table.cjs');

const inputSchema = {
  type: 'object', additionalProperties: false,
  properties: { category: { type: 'string', maxLength: 200 } },
};

const outputSchema = {
  type: 'object', required: ['content'], additionalProperties: true,
  properties: {
    content: {
      type: 'array',
      items: { type: 'object', required: ['type', 'text'], properties: { type: { enum: ['text'] }, text: { type: 'string' } } },
    },
  },
};

async function loadPack(supabase, tenant_id, deps) {
  if (deps && deps.loadPack) return deps.loadPack(supabase, tenant_id);
  try {
    const { loadPackForTenant } = require('../../../packs/pack-loader.cjs');
    return loadPackForTenant(supabase, tenant_id);
  } catch { return { pains: [] }; }
}

async function handler(ctx) {
  const { args, session, supabase } = ctx;
  const pack = await loadPack(supabase, session.tenant_id, ctx.deps);
  let items = (pack.pains || []).map((p, i) => ({
    id: p.id || `pain-${i}`,
    name: p.name || String(p),
    description: p.description || '',
    score: typeof p.score === 'number' ? p.score : 0,
    category: p.category || null,
  }));
  if (args.category) items = items.filter(p => (p.category || '').toLowerCase() === args.category.toLowerCase());
  return {
    content: [{
      type: 'text',
      text: JSON.stringify({ tenant_id: session.tenant_id, category: args.category || null, items }, null, 2),
    }],
  };
}

const descriptor = {
  name: 'list_pain_points',
  description: 'List canonical pain-point taxonomy entries, tenant-scoped, optionally filtered by category.',
  latency_tier: 'simple',
  mutating: false,
  cost_model: COST_TABLE.list_pain_points,
  inputSchema, outputSchema, handler,
};

module.exports = { descriptor, inputSchema, outputSchema, handler };
```

**Step 2. Create `lib/markos/mcp/tools/execution/rank-execution-queue.cjs`:**

```javascript
'use strict';

const { COST_TABLE } = require('../../cost-table.cjs');

const inputSchema = {
  type: 'object', additionalProperties: false,
  properties: { limit: { type: 'integer', minimum: 1, maximum: 100 } },
};
const outputSchema = {
  type: 'object', required: ['content'], additionalProperties: true,
  properties: {
    content: {
      type: 'array',
      items: { type: 'object', required: ['type', 'text'], properties: { type: { enum: ['text'] }, text: { type: 'string' } } },
    },
  },
};

async function rank(supabase, tenant_id, limit, deps) {
  if (deps && deps.rank) return deps.rank(supabase, tenant_id, limit);
  try {
    const { rankExecutionQueue } = require('../../../crm/execution.ts');
    return await rankExecutionQueue(supabase, tenant_id, { limit });
  } catch {
    return { ranked: [] };
  }
}

async function handler(ctx) {
  const { args, session, supabase } = ctx;
  const limit = args.limit || 25;
  const r = await rank(supabase, session.tenant_id, limit, ctx.deps);
  return {
    content: [{
      type: 'text',
      text: JSON.stringify({
        tenant_id: session.tenant_id,
        ranked: (r.ranked || []).slice(0, limit),
        generated_at: new Date().toISOString(),
      }, null, 2),
    }],
  };
}

const descriptor = {
  name: 'rank_execution_queue',
  description: 'Rank CRM execution-queue items for the session tenant, optionally limited.',
  latency_tier: 'simple',
  mutating: false,
  cost_model: COST_TABLE.rank_execution_queue,
  inputSchema, outputSchema, handler,
};

module.exports = { descriptor, inputSchema, outputSchema, handler };
```

**Step 3. Create `lib/markos/mcp/tools/execution/schedule-post.cjs`:**

```javascript
'use strict';

const { randomUUID } = require('node:crypto');
const { COST_TABLE } = require('../../cost-table.cjs');

const inputSchema = {
  type: 'object', required: ['channel', 'content', 'scheduled_at'], additionalProperties: false,
  properties: {
    channel: { type: 'string', enum: ['email', 'x', 'linkedin', 'sms'] },
    content: { type: 'string', minLength: 1, maxLength: 5000 },
    scheduled_at: { type: 'string', format: 'date-time' },
    approval_token: { type: 'string', pattern: '^[0-9a-f]{32}$' },
  },
};
const outputSchema = {
  type: 'object', required: ['content'], additionalProperties: true,
  properties: {
    content: {
      type: 'array',
      items: { type: 'object', required: ['type', 'text'], properties: { type: { enum: ['text'] }, text: { type: 'string' } } },
    },
  },
};

function preview(args) {
  return {
    tool: 'schedule_post',
    channel: args.channel,
    scheduled_at: args.scheduled_at,
    content_preview: (args.content || '').slice(0, 200),
  };
}

async function enqueue(supabase, input, deps) {
  if (deps && deps.enqueue) return deps.enqueue(supabase, input);
  try {
    const mod = require('../../../crm/outbound/channel-queue.cjs');
    return await mod.scheduleChannelPost(supabase, input);
  } catch {
    return { post_id: `post-${randomUUID()}`, status: 'queued' };
  }
}

async function handler(ctx) {
  const { args, session, supabase } = ctx;
  const input = {
    tenant_id: session.tenant_id,
    channel: args.channel,
    content: args.content,
    scheduled_at: args.scheduled_at,
    scheduled_by_user_id: session.user_id,
  };
  const { post_id, status } = await enqueue(supabase, input, ctx.deps);
  return {
    content: [{
      type: 'text',
      text: JSON.stringify({
        post_id, channel: args.channel, scheduled_at: args.scheduled_at,
        status: status || 'queued', tenant_id: session.tenant_id,
      }, null, 2),
    }],
  };
}

const descriptor = {
  name: 'schedule_post',
  description: 'Schedule a post draft onto a channel queue. Mutating — requires approval_token round-trip.',
  latency_tier: 'simple',
  mutating: true,
  cost_model: COST_TABLE.schedule_post,
  inputSchema, outputSchema, handler, preview,
};

module.exports = { descriptor, inputSchema, outputSchema, handler, preview };
```

**Step 4. Create `lib/markos/mcp/tools/literacy/explain-literacy.cjs`:**

```javascript
'use strict';

const { COST_TABLE } = require('../../cost-table.cjs');

const inputSchema = {
  type: 'object', additionalProperties: false,
  properties: {
    node_id:   { type: 'string', maxLength: 200 },
    archetype: { type: 'string', maxLength: 200 },
  },
  anyOf: [ { required: ['node_id'] }, { required: ['archetype'] } ],
};
const outputSchema = {
  type: 'object', required: ['content'], additionalProperties: true,
  properties: {
    content: {
      type: 'array',
      items: { type: 'object', required: ['type', 'text'], properties: { type: { enum: ['text'] }, text: { type: 'string' } } },
    },
  },
};

async function loadPack(supabase, tenant_id, deps) {
  if (deps && deps.loadPack) return deps.loadPack(supabase, tenant_id);
  try {
    const { loadPackForTenant } = require('../../../packs/pack-loader.cjs');
    return loadPackForTenant(supabase, tenant_id);
  } catch { return { archetypes: [], canon: [] }; }
}

async function handler(ctx) {
  const { args, session, supabase } = ctx;
  const pack = await loadPack(supabase, session.tenant_id, ctx.deps);
  const archetypeMatch = args.archetype ? (pack.archetypes || []).find(a => (a.slug || a.name) === args.archetype) : null;
  const nodeMatch = args.node_id ? (pack.canon || []).find(c => (c.id || c.slug) === args.node_id) : null;
  const body = archetypeMatch || nodeMatch || { description: 'not_found' };
  return {
    content: [{
      type: 'text',
      text: JSON.stringify({
        tenant_id: session.tenant_id,
        node_id: args.node_id || null,
        archetype: args.archetype || null,
        description: body.description || '',
        examples: body.examples || [],
        canonical_slug: body.slug || body.canonical_slug || null,
      }, null, 2),
    }],
  };
}

const descriptor = {
  name: 'explain_literacy',
  description: 'Explain a literacy node or archetype slug, returning description + examples + canonical slug.',
  latency_tier: 'simple',
  mutating: false,
  cost_model: COST_TABLE.explain_literacy,
  inputSchema, outputSchema, handler,
};

module.exports = { descriptor, inputSchema, outputSchema, handler };
```

**Step 5. Create the 4 test files. Pattern (verbose) for the critical `schedule-post` test:**

`test/mcp/tools/schedule-post.test.js`:

```javascript
'use strict';
const test = require('node:test');
const assert = require('node:assert/strict');
const { descriptor, preview } = require('../../../lib/markos/mcp/tools/execution/schedule-post.cjs');
const { compileToolSchemas, getToolValidator } = require('../../../lib/markos/mcp/ajv.cjs');

test('Suite 202-06: schedule_post is MUTATING and exposes preview(args)', () => {
  assert.equal(descriptor.mutating, true);
  assert.equal(typeof descriptor.preview, 'function');
  assert.equal(typeof preview, 'function');
});

test('Suite 202-06: schedule_post preview returns { tool, channel, scheduled_at, content_preview }', () => {
  const p = preview({ channel: 'email', content: 'long content '.repeat(50), scheduled_at: '2026-04-18T12:00:00Z' });
  assert.equal(p.tool, 'schedule_post');
  assert.equal(p.channel, 'email');
  assert.equal(p.scheduled_at, '2026-04-18T12:00:00Z');
  assert.ok(p.content_preview.length <= 200);
});

test('Suite 202-06: schedule_post input schema enforces channel enum + date-time format', () => {
  compileToolSchemas({ schedule_post: { input: descriptor.inputSchema, output: descriptor.outputSchema } });
  const v = getToolValidator('schedule_post');
  assert.equal(v.validateInput({ channel: 'tiktok', content: 'x', scheduled_at: '2026-04-18T12:00:00Z' }), false);
  assert.equal(v.validateInput({ channel: 'email', content: 'x', scheduled_at: 'not-a-date' }), false);
  assert.equal(v.validateInput({ channel: 'email', content: 'x', scheduled_at: '2026-04-18T12:00:00Z' }), true);
});

test('Suite 202-06: schedule_post handler returns { post_id, channel, status:"queued", tenant_id }', async () => {
  const r = await descriptor.handler({
    args: { channel: 'email', content: 'hello', scheduled_at: '2026-04-18T12:00:00Z' },
    session: { tenant_id: 't1', user_id: 'u1' },
    supabase: null,
    deps: { enqueue: async () => ({ post_id: 'post-123', status: 'queued' }) },
  });
  const parsed = JSON.parse(r.content[0].text);
  assert.equal(parsed.post_id, 'post-123');
  assert.equal(parsed.channel, 'email');
  assert.equal(parsed.status, 'queued');
  assert.equal(parsed.tenant_id, 't1');
});
```

`test/mcp/tools/list-pain-points.test.js`:

```javascript
'use strict';
const test = require('node:test');
const assert = require('node:assert/strict');
const { descriptor } = require('../../../lib/markos/mcp/tools/literacy/list-pain-points.cjs');

test('Suite 202-06: list_pain_points is simple-tier non-mutating', () => {
  assert.equal(descriptor.latency_tier, 'simple');
  assert.equal(descriptor.mutating, false);
});

test('Suite 202-06: list_pain_points returns tenant-scoped items', async () => {
  const r = await descriptor.handler({
    args: {},
    session: { tenant_id: 't1' },
    deps: { loadPack: async () => ({ pains: [{ id: 'p1', name: 'slow', description: 'bad', score: 0.8, category: 'adoption' }] }) },
  });
  const parsed = JSON.parse(r.content[0].text);
  assert.equal(parsed.tenant_id, 't1');
  assert.equal(parsed.items.length, 1);
  assert.equal(parsed.items[0].id, 'p1');
});

test('Suite 202-06: list_pain_points filters by category when provided', async () => {
  const r = await descriptor.handler({
    args: { category: 'billing' },
    session: { tenant_id: 't1' },
    deps: { loadPack: async () => ({ pains: [{ name: 'x', category: 'adoption' }, { name: 'y', category: 'billing' }] }) },
  });
  const parsed = JSON.parse(r.content[0].text);
  assert.equal(parsed.items.length, 1);
  assert.equal(parsed.items[0].name, 'y');
});
```

`test/mcp/tools/rank-execution-queue.test.js`:

```javascript
'use strict';
const test = require('node:test');
const assert = require('node:assert/strict');
const { descriptor } = require('../../../lib/markos/mcp/tools/execution/rank-execution-queue.cjs');

test('Suite 202-06: rank_execution_queue simple-tier non-mutating', () => {
  assert.equal(descriptor.latency_tier, 'simple');
  assert.equal(descriptor.mutating, false);
});

test('Suite 202-06: rank_execution_queue returns ranked list + tenant_id + generated_at', async () => {
  const r = await descriptor.handler({
    args: { limit: 5 },
    session: { tenant_id: 't1' },
    deps: { rank: async (_c, tid) => ({ ranked: [{ id: 'e1', rank: 1, score: 0.9, reason: 'a' }, { id: 'e2', rank: 2, score: 0.5, reason: 'b' }] }) },
  });
  const parsed = JSON.parse(r.content[0].text);
  assert.equal(parsed.tenant_id, 't1');
  assert.equal(parsed.ranked.length, 2);
  assert.ok(parsed.generated_at);
});

test('Suite 202-06: rank_execution_queue respects limit', async () => {
  const r = await descriptor.handler({
    args: { limit: 1 },
    session: { tenant_id: 't1' },
    deps: { rank: async () => ({ ranked: [{ id: 'a' }, { id: 'b' }, { id: 'c' }] }) },
  });
  const parsed = JSON.parse(r.content[0].text);
  assert.equal(parsed.ranked.length, 1);
});
```

`test/mcp/tools/explain-literacy.test.js`:

```javascript
'use strict';
const test = require('node:test');
const assert = require('node:assert/strict');
const { descriptor } = require('../../../lib/markos/mcp/tools/literacy/explain-literacy.cjs');

test('Suite 202-06: explain_literacy simple-tier non-mutating', () => {
  assert.equal(descriptor.latency_tier, 'simple');
  assert.equal(descriptor.mutating, false);
});

test('Suite 202-06: explain_literacy resolves an archetype slug', async () => {
  const r = await descriptor.handler({
    args: { archetype: 'builder' },
    session: { tenant_id: 't1' },
    deps: { loadPack: async () => ({ archetypes: [{ slug: 'builder', description: 'makes things', examples: ['e1'] }], canon: [] }) },
  });
  const parsed = JSON.parse(r.content[0].text);
  assert.equal(parsed.archetype, 'builder');
  assert.equal(parsed.description, 'makes things');
  assert.deepEqual(parsed.examples, ['e1']);
});

test('Suite 202-06: explain_literacy resolves a node_id', async () => {
  const r = await descriptor.handler({
    args: { node_id: 'n1' },
    session: { tenant_id: 't1' },
    deps: { loadPack: async () => ({ archetypes: [], canon: [{ id: 'n1', slug: 'n1-slug', description: 'root' }] }) },
  });
  const parsed = JSON.parse(r.content[0].text);
  assert.equal(parsed.node_id, 'n1');
  assert.equal(parsed.canonical_slug, 'n1-slug');
});
```
  </action>
  <verify>
    <automated>node --test test/mcp/tools/list-pain-points.test.js test/mcp/tools/rank-execution-queue.test.js test/mcp/tools/schedule-post.test.js test/mcp/tools/explain-literacy.test.js</automated>
  </verify>
  <acceptance_criteria>
    - 4 handler files exist under `lib/markos/mcp/tools/{literacy,execution}/`
    - `grep "mutating: true" lib/markos/mcp/tools/execution/schedule-post.cjs` returns 1 match
    - `grep "mutating: false" lib/markos/mcp/tools/execution/rank-execution-queue.cjs lib/markos/mcp/tools/literacy/list-pain-points.cjs lib/markos/mcp/tools/literacy/explain-literacy.cjs` returns 3 matches
    - `grep "preview" lib/markos/mcp/tools/execution/schedule-post.cjs` returns >= 2 matches (function + descriptor ref)
    - `grep "session.tenant_id" lib/markos/mcp/tools/{literacy,execution}/*.cjs` returns >= 4 matches
    - `grep "format: 'date-time'" lib/markos/mcp/tools/execution/schedule-post.cjs` returns 1 match
    - `node --test test/mcp/tools/list-pain-points.test.js test/mcp/tools/rank-execution-queue.test.js test/mcp/tools/schedule-post.test.js test/mcp/tools/explain-literacy.test.js` exits 0 with >= 12 tests
  </acceptance_criteria>
  <done>4 literacy/execution handlers + 4 test suites committed and green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewire lib/markos/mcp/tools/index.cjs with 10 live descriptors + F-71-v2 contract</name>
  <files>
    lib/markos/mcp/tools/index.cjs,
    contracts/F-71-mcp-session-v2.yaml
  </files>
  <read_first>
    - lib/markos/mcp/tools/index.cjs (current stubs — preserve draft_message + run_neuro_audit behavior, replace the other 8)
    - Tasks 1 + 2 outputs (descriptors to import)
    - contracts/F-71-mcp-session-v1.yaml (v1 shape — v2 supersedes)
    - bin/lib/generate-runner.cjs (runDraft + auditDraft — the 2 retained Phase 200 tools)
  </read_first>
  <behavior>
    - `tools/index.cjs` rewrites to import 8 live descriptors + retain `draft_message` + `run_neuro_audit` descriptors (refactor inline to match new contract: latency_tier, mutating, cost_model, inputSchema, outputSchema)
    - `TOOL_DEFINITIONS = [...]` of 10 descriptors
    - `listTools()` returns array of `{ name, description, inputSchema }` (stripped output schema + handler — MCP spec doesn't expose output schema to clients)
    - `invokeTool(name, args)` retained for backwards compat with Phase 200 tests — delegates to handler without pipeline (pipeline is now at server.cjs layer)
    - `contracts/F-71-mcp-session-v2.yaml` contains: `id: F-71-mcp-session-v2`, `supersedes: F-71-mcp-session-v1`, summary mentioning 10 live tools (not yet 30 — Plan 202-07 bumps), adds `session` components schema referencing markos_mcp_sessions, tools list includes 10 live tools, paths include /api/mcp/session (get + post) + /api/mcp/tools/{toolName} (post)
  </behavior>
  <action>
**Step 1. Rewrite `lib/markos/mcp/tools/index.cjs`:**

```javascript
'use strict';

// Phase 202 Plan 06: 10 live tool descriptors (2 retained from Phase 200 + 8 wave-0 wired).
// Plan 202-07 extends to 30 with 20 net-new handler files + F-90..F-93 contracts.

const { COST_TABLE } = require('../cost-table.cjs');

// --- Phase 200 retained (refactored to new descriptor shape) ---

const draftMessage = {
  name: 'draft_message',
  description: 'Generate a single marketing draft from a channel + audience + pain + promise + brand brief.',
  latency_tier: 'llm',
  mutating: false,
  cost_model: COST_TABLE.draft_message,
  inputSchema: {
    type: 'object',
    required: ['channel', 'audience', 'pain', 'promise', 'brand'],
    additionalProperties: false,
    properties: {
      channel: { type: 'string', enum: ['email', 'x', 'linkedin', 'sms'] },
      audience: { type: 'string', minLength: 1, maxLength: 500 },
      pain: { type: 'string', minLength: 1, maxLength: 1000 },
      promise: { type: 'string', minLength: 1, maxLength: 1000 },
      brand: { type: 'string', minLength: 1, maxLength: 200 },
    },
  },
  outputSchema: {
    type: 'object', required: ['content', '_usage'], additionalProperties: true,
    properties: {
      content: { type: 'array', items: { type: 'object', required: ['type', 'text'], properties: { type: { enum: ['text'] }, text: { type: 'string' } } } },
      _usage: { type: 'object', required: ['input_tokens', 'output_tokens'], properties: { input_tokens: { type: 'integer', minimum: 0 }, output_tokens: { type: 'integer', minimum: 0 } } },
    },
  },
  handler: async ({ args, deps }) => {
    const { runDraft } = require('../../../../bin/lib/generate-runner.cjs');
    const result = await runDraft(args);
    return {
      content: [{ type: 'text', text: JSON.stringify(result, null, 2) }],
      _usage: { input_tokens: result.input_tokens || 0, output_tokens: result.output_tokens || 0 },
    };
  },
};

const runNeuroAudit = {
  name: 'run_neuro_audit',
  description: 'Run a neuro-audit pass over a draft and return scored issues.',
  latency_tier: 'llm',
  mutating: false,
  cost_model: COST_TABLE.run_neuro_audit,
  inputSchema: {
    type: 'object', required: ['text'], additionalProperties: false,
    properties: { text: { type: 'string', minLength: 1, maxLength: 10000 }, brief: { type: 'object', additionalProperties: true } },
  },
  outputSchema: {
    type: 'object', required: ['content'], additionalProperties: true,
    properties: { content: { type: 'array', items: { type: 'object', required: ['type', 'text'], properties: { type: { enum: ['text'] }, text: { type: 'string' } } } } },
  },
  handler: async ({ args }) => {
    const { auditDraft } = require('../../../../bin/lib/generate-runner.cjs');
    const audit = auditDraft({ text: args.text }, args.brief || {});
    return { content: [{ type: 'text', text: JSON.stringify(audit, null, 2) }] };
  },
};

// --- Wave-0 stubs → live (Plan 202-06) ---

const { descriptor: planCampaign }       = require('./marketing/plan-campaign.cjs');
const { descriptor: researchAudience }   = require('./marketing/research-audience.cjs');
const { descriptor: generateBrief }      = require('./marketing/generate-brief.cjs');
const { descriptor: auditClaim }         = require('./marketing/audit-claim.cjs');
const { descriptor: listPainPoints }     = require('./literacy/list-pain-points.cjs');
const { descriptor: rankExecutionQueue } = require('./execution/rank-execution-queue.cjs');
const { descriptor: schedulePost }       = require('./execution/schedule-post.cjs');
const { descriptor: explainLiteracy }    = require('./literacy/explain-literacy.cjs');

const TOOL_DEFINITIONS = [
  draftMessage,
  planCampaign,
  researchAudience,
  runNeuroAudit,
  generateBrief,
  auditClaim,
  listPainPoints,
  rankExecutionQueue,
  schedulePost,
  explainLiteracy,
];

function listTools() {
  // MCP spec: tools/list exposes name + description + inputSchema only.
  return TOOL_DEFINITIONS.map(({ name, description, inputSchema }) => ({ name, description, inputSchema }));
}

async function invokeTool(name, args, ctx = {}) {
  const tool = TOOL_DEFINITIONS.find(t => t.name === name);
  if (!tool) throw Object.assign(new Error(`unknown_tool:${name}`), { code: 'tool_not_found' });
  return tool.handler({ args, ...ctx });
}

function getToolByName(name) {
  return TOOL_DEFINITIONS.find(t => t.name === name) || null;
}

module.exports = {
  TOOL_DEFINITIONS,
  listTools,
  invokeTool,
  getToolByName,
};
```

**Step 2. Create `contracts/F-71-mcp-session-v2.yaml`:**

```yaml
id: F-71-mcp-session-v2
supersedes: F-71-mcp-session-v1
phase: 202
name: MCP Session v2
status: active
summary: MCP 2025-06-18 session endpoint. OAuth 2.1 + PKCE bearer auth (F-89), 24h rolling session persistence (markos_mcp_sessions), per-tenant 24h cost meter (markos_mcp_cost_window), 10 tools live (Plan 202-06 — expands to 30 in F-90..F-93). Supersedes F-71-v1 (Phase 200 10-stub-tool pre-GA).

transport:
  http_session_endpoint: /api/mcp/session
  dispatch_endpoint: /api/mcp/tools/{toolName}
  protocol: json-rpc-2.0
  protocol_version: "2025-06-18"
  auth: oauth2.1+pkce (see F-89-mcp-oauth-v1)

tools:
  - draft_message
  - plan_campaign
  - research_audience
  - run_neuro_audit
  - generate_brief
  - audit_claim
  - list_pain_points
  - rank_execution_queue
  - schedule_post
  - explain_literacy

paths:
  /api/mcp/session:
    get:
      summary: MCP server metadata + tool list (public for discovery)
      operationId: getMcpSession
      tags: [mcp]
      responses:
        '200':
          description: Session metadata
          content:
            application/json:
              schema:
                type: object
                required: [server, capabilities, tools]
                properties:
                  success: { type: boolean }
                  server:
                    type: object
                    required: [name, version]
                    properties:
                      name: { type: string, enum: [markos] }
                      version: { type: string, pattern: "^2\\." }
                  capabilities:
                    type: object
                    properties:
                      tools: { type: object, properties: { listChanged: { type: boolean } } }
                      resources: { type: object, properties: { subscribe: { type: boolean }, listChanged: { type: boolean } } }
                  tools:
                    type: array
                    items: { $ref: '#/components/schemas/McpToolDescriptor' }
                  _meta: { type: object, properties: { req_id: { type: string, pattern: "^mcp-req-" } } }
    post:
      summary: JSON-RPC 2.0 request. Bearer token required for tools/call + resources/*.
      operationId: postMcpSession
      tags: [mcp]
      parameters:
        - name: Authorization
          in: header
          description: Bearer <opaque_token> — required for tools/call + resources/* methods
          schema: { type: string, pattern: "^Bearer " }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [jsonrpc, method]
              properties:
                jsonrpc: { type: string, enum: ["2.0"] }
                id: { oneOf: [{ type: string }, { type: integer }, { type: "null" }] }
                method: { type: string, enum: [initialize, tools/list, tools/call, resources/list, resources/read, resources/subscribe, resources/unsubscribe, notifications/initialized] }
                params: { type: object, additionalProperties: true }
      responses:
        '200':
          description: JSON-RPC response envelope (success or JSON-RPC error)
          headers:
            WWW-Authenticate:
              schema: { type: string }
              description: Bearer resource_metadata URL when auth missing (RFC 9728)
        '401': { description: Missing or invalid Bearer — WWW-Authenticate header present }
        '402': { description: Budget exhausted (code -32001) or paid-tier required }
        '429': { description: Rate-limited (code -32002) with Retry-After header }

components:
  schemas:
    McpToolDescriptor:
      type: object
      required: [name, description, inputSchema]
      properties:
        name: { type: string }
        description: { type: string }
        inputSchema: { type: object, additionalProperties: true }
    McpSession:
      type: object
      required: [id, user_id, tenant_id, org_id, scopes, plan_tier]
      properties:
        id: { type: string, pattern: "^mcp-sess-" }
        user_id: { type: string }
        tenant_id: { type: string }
        org_id: { type: string }
        client_id: { type: string }
        scopes: { type: array, items: { type: string } }
        plan_tier: { type: string }
        created_at: { type: string, format: date-time }
        last_used_at: { type: string, format: date-time }
        expires_at: { type: string, format: date-time }

marketplace:
  listing_manifest: .claude-plugin/marketplace.json
  submission_status: pending
  version: "2.0.0"

references:
  mcp_spec: https://modelcontextprotocol.io/specification/2025-06-18
  oauth_contract: F-89-mcp-oauth-v1
  cost_contract: F-95-mcp-cost-budget-v1
  resources_contract: F-94-mcp-resources-v1
  tools_contracts: [F-90-mcp-tools-marketing-v1, F-91-mcp-tools-crm-v1, F-92-mcp-tools-literacy-v1, F-93-mcp-tools-tenancy-v1]
```

**Step 3. Verify `tools/index.cjs` exports work + existing Phase 200 `server.test.js` still green:**

```bash
node -e "const t=require('./lib/markos/mcp/tools/index.cjs'); console.log(t.TOOL_DEFINITIONS.length, t.TOOL_DEFINITIONS.every(d => d.latency_tier && typeof d.mutating === 'boolean' && d.cost_model && d.inputSchema && d.outputSchema && typeof d.handler === 'function'))"
```

Expected output: `10 true`
  </action>
  <verify>
    <automated>node --test test/mcp/server.test.js test/mcp/tools/plan-campaign.test.js test/mcp/tools/schedule-post.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep "TOOL_DEFINITIONS" lib/markos/mcp/tools/index.cjs` returns >= 2 matches
    - `grep "require('./marketing/plan-campaign.cjs')" lib/markos/mcp/tools/index.cjs` returns 1 match
    - `grep "require('./execution/schedule-post.cjs')" lib/markos/mcp/tools/index.cjs` returns 1 match
    - `node -e "const t=require('./lib/markos/mcp/tools/index.cjs'); if (t.TOOL_DEFINITIONS.length !== 10) process.exit(1)"` exits 0
    - `node -e "const t=require('./lib/markos/mcp/tools/index.cjs'); if (!t.TOOL_DEFINITIONS.every(d => d.latency_tier && typeof d.mutating === 'boolean' && d.cost_model && d.inputSchema && d.outputSchema)) process.exit(1)"` exits 0
    - `node -e "const t=require('./lib/markos/mcp/tools/index.cjs'); const names=t.TOOL_DEFINITIONS.map(d=>d.name); if (!['plan_campaign','research_audience','generate_brief','audit_claim','list_pain_points','rank_execution_queue','schedule_post','explain_literacy'].every(n => names.includes(n))) process.exit(1)"` exits 0
    - `grep "supersedes: F-71-mcp-session-v1" contracts/F-71-mcp-session-v2.yaml` returns 1 match
    - `grep "plan_campaign" contracts/F-71-mcp-session-v2.yaml` returns >= 1 match
    - `node -e "const y=require('yaml').parse(require('fs').readFileSync('contracts/F-71-mcp-session-v2.yaml','utf8')); if (!y.supersedes || y.tools.length !== 10) process.exit(1)"` exits 0
    - Regression: `node --test test/mcp/server.test.js` (Plan 202-05) still green (TOOL_DEFINITIONS change is backwards-compatible for listTools)
  </acceptance_criteria>
  <done>tools/index.cjs rewired + F-71-v2 contract committed; 10 live descriptors in registry</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pipeline → tool handler | Handler receives validated args + session; handler must still filter on `session.tenant_id` in every DB query (D-15 defense-in-depth) |
| Mutating tool → approval-token | `schedule_post` handler runs only after pipeline consumed approval_token (D-16); handler does NOT re-validate |
| LLM SDK → tool output | LLM response parsed via try/catch; unparseable → structured fallback with `derived_from='parse_failed'`; never throws past handler |
| Pack-loader → tenant data | All pack reads filter by `tenant_id`; Phase 201 RLS is the backstop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-202-06-01 | Elevation of Privilege | Tool handler leaks cross-tenant data | mitigate | Every handler filters on `session.tenant_id` (D-15); handler output embeds `tenant_id` in JSON so AJV + downstream assertions catch drift |
| T-202-06-02 | Tampering | LLM response contains handler-breaking payload | mitigate | try/catch wrap JSON.parse; fallback object returned; tool output still validates against AJV outputSchema (Plan 202-04 step 9) |
| T-202-06-03 | Information Disclosure | LLM response text logged with tenant PII | mitigate | Handler output goes into JSON-RPC response → audit row payload via pipeline (Plan 201-02 hash chain); never to log-drain raw (log-drain only receives enumerated ids + counts per Plan 202-05) |
| T-202-06-04 | Denial of Service | LLM timeout hangs tool invocation | mitigate | Pipeline `withTimeout(latency_tier)` kills at 120s (llm); handler Promise rejected; finally audit + log still fires |
| T-202-06-05 | Tampering | schedule_post bypass via direct POST to /api/mcp/tools/schedule_post | mitigate | Pipeline is the ONLY admission path (server.cjs runToolCallThroughPipeline); direct tools/{toolName} route (Phase 200 vestige) must also route through pipeline — Plan 202-05's api/mcp/tools/[toolName].js extends via buildToolRegistryFromDefinitions |
| T-202-06-06 | Elevation of Privilege | Free-tier user invokes schedule_post | mitigate | Pipeline step 5 — `tool.mutating && session.plan_tier==='free'` → 402 paid_tier_required BEFORE handler invoked |
| T-202-06-07 | Information Disclosure | Audit payload leaks full LLM prompt (PII) | accept | Audit payload carries `tool_id + req_id + duration_ms + cost_cents + error_code` only; tool args are NOT in the audit payload; no PII exposure |
| T-202-06-08 | Repudiation | Tool call succeeds but handler returns malformed output | mitigate | AJV outputSchema validates (Plan 202-04 step 9); violation → generic `internal_error` to client + `action='tool.output_schema_violation'` audit + Sentry capture (D-32) |
| T-202-06-09 | Tampering | Cost estimate mismatches actual LLM tokens | mitigate | Handler returns `_usage: { input_tokens, output_tokens }`; pipeline step 10 calls `computeToolCost(tool_id, _usage)` + `trueupBudget(delta)` — delta charged post-invocation; next call sees updated spend |
</threat_model>

<verification>
- `node --test test/mcp/tools/` exits 0 with >= 23 tests across 8 files (4 marketing + 4 literacy/execution)
- `grep "latency_tier:" lib/markos/mcp/tools/**/*.cjs lib/markos/mcp/tools/index.cjs | wc -l` returns >= 10 (every descriptor has tier)
- `grep "mutating: true" lib/markos/mcp/tools/execution/schedule-post.cjs` returns 1 match (only schedule_post mutates)
- `grep -c "mutating: false" lib/markos/mcp/tools/**/*.cjs lib/markos/mcp/tools/index.cjs` returns >= 9 (non-mutating majority)
- `grep "tenant_id" lib/markos/mcp/tools/**/*.cjs` returns >= 16 matches (D-15 tenant scope in every handler)
- F-71-v2 parses; declares 10 tools; marketplace version 2.0.0
- Regression: Plans 202-01/02/03/04/05 full suite (`node --test test/mcp/{session,rls,migration-idempotency,oauth,cost-table,cost-meter,402-breach,rate-limit,429-breach,injection-denylist,ajv-validation,approval-token,pipeline,observability,server,consent-ui-a11y}.test.js`) exits 0
</verification>

<success_criteria>
- 8 live handler files with ToolDescriptor shape (name, description, latency_tier, mutating, cost_model, inputSchema, outputSchema, handler, optional preview)
- `lib/markos/mcp/tools/index.cjs` lists 10 live descriptors; `listTools()` returns spec-compliant shape (name + description + inputSchema only)
- `schedule_post` is the only mutating tool; has `preview()` returning `{ tool, channel, scheduled_at, content_preview }`
- LLM-backed tools (plan_campaign, generate_brief, audit_claim) populate `_usage` with token counts for cost trueup
- F-71-v2 contract supersedes v1; references F-89/F-94/F-95/F-90-93; mentions marketplace v2.0.0
- 8 per-tool test suites pass with >= 23 tests total; every handler output validates against its outputSchema
- All handlers filter on `session.tenant_id`; D-15 tenant scope enforced at handler layer (RLS remains the backstop)
- Plan 202-07 picks up this registry shape and appends 20 net-new descriptors; total grows to 30 (D-02 marketplace pitch)
</success_criteria>

<output>
After completion, create `.planning/phases/202-mcp-server-ga-claude-marketplace/202-06-SUMMARY.md`
</output>
