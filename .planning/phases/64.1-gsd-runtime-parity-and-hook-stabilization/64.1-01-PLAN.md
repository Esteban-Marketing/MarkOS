---
phase: 64.1-gsd-runtime-parity-and-hook-stabilization
plan: 01
type: execute
wave: 1
depends_on:
  - 64-attribution-reporting-and-verification-closure
files_modified:
  - .claude/settings.json
autonomous: true
requirements:
  - GSD-ALIGN-01
  - GSD-ALIGN-03

must_haves:
  truths:
    - "The active .claude runtime boots without calling missing shell hooks, so Phase 64.1 starts from a valid hook contract instead of stale config."
    - "All bound .claude hooks resolve to existing JS files in .claude/hooks, and optional enforcement stays unchanged unless a real runtime gap is proven."
    - "Phase 64.1 repairs the runtime bootstrap without absorbing hook-policy expansion, manifest refresh, or 64.2 instruction-target work."
  artifacts:
    - path: ".claude/settings.json"
      provides: "The active Claude-local hook binding layer with only supported JS commands and no dead shell-hook references."
      exports: []
    - path: ".claude/hooks/gsd-check-update.js"
      provides: "Session-start version and stale-hook detection used by the repaired runtime contract."
      exports: []
    - path: ".claude/hooks/gsd-context-monitor.js"
      provides: "Post-tool context monitoring used by the active runtime after hook cleanup."
      exports: []
    - path: ".claude/hooks/gsd-prompt-guard.js"
      provides: "Write-time prompt guard preserved as part of the JS-only runtime hook set."
      exports: []
    - path: ".claude/hooks/gsd-read-guard.js"
      provides: "Existing advisory read guard preserved without shell-hook replacement work."
      exports: []
    - path: ".claude/hooks/gsd-statusline.js"
      provides: "Statusline bridge that remains part of the active JS-only runtime contract."
      exports: []
  key_links:
    - from: ".claude/settings.json"
      to: ".claude/hooks/gsd-check-update.js"
      via: "SessionStart stays bound to an existing JS hook instead of the deleted shell-hook path"
      pattern: "SessionStart|gsd-check-update\\.js"
    - from: ".claude/settings.json"
      to: ".claude/hooks/gsd-context-monitor.js"
      via: "post-tool monitoring continues through the existing JS hook matcher rather than a replacement shell interceptor"
      pattern: "PostToolUse|gsd-context-monitor\\.js"
    - from: ".claude/settings.json"
      to: ".claude/hooks/gsd-prompt-guard.js"
      via: "write-time protection remains on the repo-backed JS guard while optional workflow enforcement stays unbound"
      pattern: "PreToolUse|gsd-prompt-guard\\.js"
---

<objective>
Repair the active .claude hook bootstrap so the runtime uses only supported JS hooks.

Purpose: satisfy GSD-ALIGN-01 first and create a stable base for runtime parity work without pulling 64.2 or 64.3 scope forward.
Output: cleaned .claude/settings.json with only existing JS hook bindings, preserved active hook coverage, and explicit non-activation of optional hooks such as gsd-workflow-guard.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/64.1-gsd-runtime-parity-and-hook-stabilization/64.1-CONTEXT.md
@.planning/phases/64.1-gsd-runtime-parity-and-hook-stabilization/64.1-RESEARCH.md
@CLAUDE.md
@.github/copilot-instructions.md
@.claude/settings.json
@.agent/settings.json
@.claude/hooks/gsd-check-update.js
@.claude/hooks/gsd-context-monitor.js
@.claude/hooks/gsd-prompt-guard.js
@.claude/hooks/gsd-read-guard.js
@.claude/hooks/gsd-statusline.js
@.claude/hooks/gsd-workflow-guard.js
</context>

<tasks>

<task type="auto">
  <name>Task 64.1-01-01: Remove dead shell-hook bindings from the active Claude runtime contract</name>
  <files>.claude/settings.json</files>
  <action>
Update .claude/settings.json per D-01 and D-02 so the live runtime contains no bash or .sh hook commands. Remove the dead gsd-session-state.sh, gsd-phase-boundary.sh, and gsd-validate-commit.sh references instead of recreating them. Keep the file JS-only, preserve the existing active event structure that still maps to repo-backed hooks, and do not touch manifest or template surfaces in this plan.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const text=fs.readFileSync('.claude/settings.json','utf8'); if(/\\.sh/.test(text)||/bash\\s+\\.claude\\/hooks/.test(text)) throw new Error('shell-hook reference still present');"</automated>
  </verify>
  <done>
The active Claude-local runtime no longer references missing shell hooks and the bootstrap contract is JS-only.
  </done>
</task>

<task type="auto">
  <name>Task 64.1-01-02: Normalize the remaining JS hook bindings without expanding hook policy</name>
  <files>.claude/settings.json</files>
  <action>
Use .agent/settings.json and the actual .claude/hooks inventory as the cleanup precedent. Keep only commands that resolve to existing JS hooks, preserve the current active guards and statusline bridge, and leave gsd-workflow-guard.js and any other optional hooks unbound per D-09 and D-10. Do not add a new hook unless implementation proves a real runtime gap after cleanup; if no verified gap exists, finish with settings cleanup only.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const data=JSON.parse(fs.readFileSync('.claude/settings.json','utf8')); const commands=[]; for (const group of Object.values(data.hooks||{})) { for (const entry of group) { for (const hook of entry.hooks||[]) commands.push(hook.command); } } if (data.statusLine&&data.statusLine.command) commands.push(data.statusLine.command); const missing=[]; for (const command of commands) { const match=command&&command.match(/node\\s+(.claude\\/hooks\\/[^\\s]+)/); if (match && !fs.existsSync(match[1])) missing.push(match[1]); if (/bash\\s+\\.claude\\/hooks/.test(command||'')) throw new Error('unexpected bash hook command: '+command); } if (missing.length) throw new Error('missing hook files: '+missing.join(', ')); if (commands.some(command => (command||'').includes('gsd-workflow-guard.js'))) throw new Error('optional workflow guard unexpectedly bound');"</automated>
  </verify>
  <done>
All bound hooks resolve to existing JS files, the active runtime policy has not widened, and 64.1 remains inside the stabilization boundary.
  </done>
</task>

</tasks>

<verification>
Re-run the settings validation after each edit. Plan 01 is incomplete until .claude/settings.json is JS-only, references only existing hooks, and leaves optional policy hooks unbound.
</verification>

<success_criteria>
The active .claude runtime can start without broken hook commands, with a coherent JS-only binding layer that matches the repo-backed hook inventory and stays inside the 64.1 stabilization boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/64.1-gsd-runtime-parity-and-hook-stabilization/64.1-01-SUMMARY.md`
</output>
