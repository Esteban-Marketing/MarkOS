# Phase 64.1: GSD Runtime Parity and Hook Stabilization - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-05
**Phase:** 64.1-gsd-runtime-parity-and-hook-stabilization
**Areas discussed:** Hook strategy, Runtime parity scope, Command guidance syntax, Optional hook activation

---

## Hook Strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Remove dead shell-hook references and stay JS-only | Clean `.claude/settings.json`, keep the existing Node hooks, add a new JS hook only if a real runtime gap is proven | ✓ |
| Replace each missing shell hook with a new JS equivalent | Preserve the same conceptual hook slots, but implement them in Node instead of shell | |
| Recreate the missing shell scripts | Keep the current config shape and restore `.sh` hooks | |

**User's choice:** Remove dead shell-hook references and stay JS-only
**Notes:** The user locked the phase around stabilization rather than reviving shell-based behavior.

---

## Runtime Parity Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Sync only behavior-affecting runtime deltas | Patch execution logic and operator-guidance drift, but leave wording-only and instruction-surface differences for later | |
| Sync the full runtime subtree now | Aim for broad parity across `.claude/get-shit-done/bin/**` in this phase | ✓ |
| Minimal hook-only fix | Touch only `.claude/settings.json` and avoid any `.claude/get-shit-done/**` sync in 64.1 | |

**User's choice:** Sync the full runtime subtree now
**Notes:** Follow-up boundary clarification locked this to `.claude/get-shit-done/bin/**` plus user-facing workflow/help command guidance, while explicitly excluding template or instruction-target rewrites.

---

## Runtime Scope Boundary

| Option | Description | Selected |
|--------|-------------|----------|
| Full `.claude/get-shit-done/bin/**` plus user-facing workflow/help command guidance, but not template or instruction-target rewrites | Aggressive runtime parity, while still deferring `CLAUDE.md` versus `copilot-instructions.md` alignment to 64.2 | ✓ |
| Everything under `.claude/get-shit-done/**` | Include templates and generated instruction targets in 64.1 too | |
| Only `.claude/get-shit-done/bin/**` | Keep workflow/help markdown out of 64.1 and limit changes to executable runtime files | |

**User's choice:** Full `.claude/get-shit-done/bin/**` plus user-facing workflow/help command guidance, but not template or instruction-target rewrites
**Notes:** This is the cutoff that keeps 64.1 aggressive without absorbing 64.2.

---

## Command Guidance Syntax

| Option | Description | Selected |
|--------|-------------|----------|
| Standardize repo guidance on `/gsd:...` | Use the VS Code / Copilot command form in user-facing guidance where 64.1 touches runtime behavior | ✓ |
| Keep `/gsd-...` everywhere upstream uses it | Prefer upstream GSD wording even if this repo often uses colon commands interactively | |
| Allow mixed syntax for now | Only fix obviously broken guidance and defer command-style consistency | |

**User's choice:** Standardize repo guidance on `/gsd:...`
**Notes:** The repo-local runtime guidance should match how this workspace actually invokes GSD commands.

---

## Optional Hook Activation

| Option | Description | Selected |
|--------|-------------|----------|
| Leave optional hooks unbound unless a verified runtime need exists | Keep 64.1 focused on broken bindings and parity, not new enforcement behavior | ✓ |
| Enable `gsd-workflow-guard.js` now | Expand the active runtime contract in 64.1 | |
| Review each optional hook and decide case by case in 64.1 | Broaden the phase slightly to include selective hook activation | |

**User's choice:** Leave optional hooks unbound unless a verified runtime need exists
**Notes:** Optional hook activation is explicitly deferred unless implementation proves a concrete need.

---

## the agent's Discretion

- Exact diffing and patch sequencing for `.github/get-shit-done/bin/**` versus `.claude/get-shit-done/bin/**`.
- Which touched workflow/help files require repo-local `/gsd:...` guidance updates once the runtime-impacting set is identified.
- Whether a new JS hook is truly required after cleanup reveals an actual runtime gap.

## Deferred Ideas

- Aligning templates, generated instruction targets, and broader `.claude` versus `.github` instruction surfaces belongs in Phase 64.2.
- Manifest refresh and end-to-end aligned-runtime verification belong in Phase 64.3.
