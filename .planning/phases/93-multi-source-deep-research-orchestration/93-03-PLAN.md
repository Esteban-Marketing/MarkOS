---
phase: 93-multi-source-deep-research-orchestration
plan: "03"
type: execute
wave: 3
depends_on:
  - "93-01"
  - "93-02"
files_modified:
  - onboarding/backend/research/evidence-merge-ranker.cjs
  - onboarding/backend/research/contradiction-reporter.cjs
  - onboarding/backend/research/context-pack-assembler.cjs
  - onboarding/backend/research/multi-source-research-orchestrator.cjs
  - test/phase-93/evidence-lineage-contract.test.js
  - test/phase-93/cross-surface-envelope.test.js
autonomous: true
requirements:
  - DRT-02
  - DRT-03
  - DRT-08
  - DRT-09
  - DRT-15
  - DRT-16
must_haves:
  truths:
    - "the final orchestration output merges literacy, MIR, MSP, company overrides, and optional external evidence into one ranked context pack"
    - "contradictions between approved internal truth and fresh external evidence are surfaced explicitly instead of hidden"
    - "operators and downstream clients receive a short summary, route trace, provider attempts, warnings, and evidence metadata in one portable envelope"
    - "the orchestrator degrades gracefully and remains output-only with no artifact mutation or approval bypass"
  artifacts:
    - path: onboarding/backend/research/evidence-merge-ranker.cjs
      provides: ranked dedupe and authority-aware merge logic for internal and external evidence
    - path: onboarding/backend/research/contradiction-reporter.cjs
      provides: first-class contradiction objects with internal claim, external challenge, freshness, confidence, and recommendation
    - path: onboarding/backend/research/context-pack-assembler.cjs
      provides: the structured context_pack and short_summary output for cross-surface use
    - path: onboarding/backend/research/multi-source-research-orchestrator.cjs
      provides: the transport-independent entrypoint that executes the route policy, calls adapters, and returns the final evidence pack
    - path: test/phase-93/evidence-lineage-contract.test.js
      provides: proof that merged findings still carry lineage, freshness, confidence, and implication metadata
  key_links:
    - from: onboarding/backend/research/multi-source-research-orchestrator.cjs
      to: onboarding/backend/research/adaptive-research-policy.cjs
      via: the orchestrator must honor the stage plan and deep-research gate instead of inventing its own route order
    - from: onboarding/backend/research/multi-source-research-orchestrator.cjs
      to: onboarding/backend/research/providers/openai-research-adapter.cjs
      via: the deep path is invoked only when the route explicitly marks it required
    - from: onboarding/backend/research/contradiction-reporter.cjs
      to: onboarding/backend/research/context-pack-assembler.cjs
      via: contradiction objects are embedded directly into the final context pack and surfaced in the short summary warnings
---

<objective>
Assemble the actual multi-source evidence pipeline for Phase 93 so internal retrieval, Tavily, Firecrawl, and optional deep research produce one ranked, auditable context pack with explicit contradiction reporting.

Purpose: satisfy the core orchestration goal of the phase while staying strictly inside scope: output-only research synthesis with route traces and degraded fallback, not artifact writes or approval bypass.
Output: a transport-independent orchestrator, merge and ranking logic, contradiction reporting, and the final context-pack assembly layer.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-multi-source-deep-research-orchestration/93-CONTEXT.md
@.planning/phases/93-multi-source-deep-research-orchestration/93-RESEARCH.md
@.planning/phases/93-multi-source-deep-research-orchestration/93-01-PLAN.md
@.planning/phases/93-multi-source-deep-research-orchestration/93-02-PLAN.md
@.planning/phases/91-filter-taxonomy-and-provider-contract/91-02-PLAN.md
@.planning/phases/92-markos-company-knowledge-mcp-surface/92-02-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@onboarding/backend/vector-store-client.cjs
@onboarding/backend/agents/orchestrator.cjs
@lib/markos/llm/adapter.ts

<interfaces>
Required route and adapter contracts from prior plans:
- buildAdaptiveResearchRoute(request) -> ordered stages with used, skipped, degraded, or blocked status
- each provider adapter returns { evidence: [], attempts: [], warnings: [] }
- the final response envelope must expose short_summary, context_pack, warnings, route_trace, and provider_attempts

Current reusable rank and dedupe signal from onboarding/backend/agents/orchestrator.cjs:
- normalizeHit and dedupeByPriority already model non-destructive rank and dedupe behavior
- Phase 93 should reuse this spirit but extend it for authority class, freshness, and contradiction visibility
</interfaces>
</context>

<threat_model>
- T-93-09 (silent contradiction masking): merge logic could hide differences between approved internal truth and fresher external claims. Mitigation: emit first-class contradiction objects per D-05 and D-06 and surface them in warnings and route trace summaries.
- T-93-10 (summary without provenance): the short summary could strip lineage or evidence context and violate DRT-03. Mitigation: every claim in the assembled pack keeps source lineage, freshness, confidence, and implication metadata, with the summary pointing back to underlying evidence.
- T-93-11 (approval bypass or auto-write): the orchestrator could drift into direct artifact mutation. Mitigation: restrict output to short_summary, context_pack, warnings, route_trace, and provider_attempts only; no writer integration or approval bypass is allowed.
- T-93-12 (audit blind spots): skipped or degraded providers might disappear from the final result. Mitigation: keep a complete provider_attempts ledger and route_trace even when the final evidence set is internal-only or partially degraded.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create failing merge and envelope tests for lineage preservation, contradiction objects, and cross-surface portability</name>
  <files>test/phase-93/evidence-lineage-contract.test.js, test/phase-93/cross-surface-envelope.test.js</files>
  <behavior>
    - merged findings preserve lineage, freshness, confidence, implication, and citation data after dedupe and ranking
    - conflicts between approved internal and external evidence produce explicit contradiction objects rather than silent overrides
    - the final envelope remains portable for MCP, API, CLI, and editor use and includes short_summary plus structured context_pack
  </behavior>
  <action>Write the tests first using fixture evidence from internal, Tavily, Firecrawl, and OpenAI-style results. Assert that DRT-02 and DRT-03 survive the merge, that D-05 and D-06 produce visible contradiction objects, and that D-07 yields a structured context pack plus short summary without artifact write behavior.</action>
  <verify>
    <automated>node --test test/phase-93/evidence-lineage-contract.test.js test/phase-93/cross-surface-envelope.test.js</automated>
  </verify>
  <done>The final output contract is locked in executable tests before the merge and orchestration code is written.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement authority-aware merge, ranking, and contradiction reporting</name>
  <files>onboarding/backend/research/evidence-merge-ranker.cjs, onboarding/backend/research/contradiction-reporter.cjs, onboarding/backend/research/context-pack-assembler.cjs</files>
  <behavior>
    - approved internal evidence ranks highest by default but external freshness can still trigger review-worthy contradictions
    - duplicate evidence from multiple providers is collapsed without losing citations or provider lineage
    - the context pack returns claims, evidence items, contradictions, warnings, filters, route trace, and provider attempts in one structured object
  </behavior>
  <action>Implement the evidence composer so it merges literacy base, current MIR state, current MSP state, company-specific overrides, and optional external evidence into one ranked context pack per DRT-02. Use authority-first plus freshness-aware ranking rather than simple recency. Add a contradiction-reporter that outputs side-by-side internal and external claims, confidence or freshness comparisons, and an operator recommendation per D-05 and D-06. Keep the short summary concise but grounded in the structured pack per D-07.</action>
  <verify>
    <automated>node --test test/phase-93/evidence-lineage-contract.test.js test/phase-93/cross-surface-envelope.test.js test/phase-93/provider-normalization.test.js</automated>
  </verify>
  <done>The repo can merge and rank multi-source evidence into one auditable pack without hiding conflicts or losing provenance.</done>
</task>

<task type="auto">
  <name>Task 3: Implement the transport-independent orchestrator entrypoint and run the full phase regression gate</name>
  <files>onboarding/backend/research/multi-source-research-orchestrator.cjs</files>
  <action>Create a single entrypoint such as buildResearchPack(request, runtimeContext) that executes the adaptive route from 93-01, invokes the normalized adapters from 93-02, and assembles the final short_summary plus context_pack output. The function must always emit route_trace and provider_attempts, preserve degraded warnings, and remain read-only for DRT-08, DRT-09, and DRT-16. Do not add direct artifact writes, approval bypass, or always-on provider fan-out. Finish by running the complete phase-93 suite plus the relevant fallback and retrieval regressions, then the repository-wide test gate.</action>
  <verify>
    <automated>node --test test/phase-93/*.test.js test/llm-adapter/fallback-chain.test.js test/phase-84/retrieval-envelope.test.js test/phase-86/retrieval-filter.test.js test/phase-88/tenant-isolation-matrix.test.js && npm test</automated>
  </verify>
  <done>The multi-source research orchestrator is transport-independent, auditable, contradiction-aware, and safely output-only.</done>
</task>

</tasks>

<verification>
- node --test test/phase-93/*.test.js test/llm-adapter/fallback-chain.test.js test/phase-84/retrieval-envelope.test.js test/phase-86/retrieval-filter.test.js test/phase-88/tenant-isolation-matrix.test.js
- npm test
</verification>

<success_criteria>
- One transport-independent entrypoint returns a ranked context pack and short summary from internal and optional external evidence.
- Contradictions are explicit, review-friendly, and never silently resolved.
- Route traces, provider attempts, warnings, and degraded states remain visible and auditable.
- The implementation stays strictly within scope: no artifact writes, no approval bypass, and no replacement of the existing retrieval system.
</success_criteria>

<output>
After completion, create .planning/phases/93-multi-source-deep-research-orchestration/93-03-SUMMARY.md.
</output>
