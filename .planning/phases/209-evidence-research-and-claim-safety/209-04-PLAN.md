---
phase: 209
plan: "04"
type: execute
wave: 4
depends_on:
  - "209-01"
  - "209-02"
  - "209-03"
f_ids: []
migration_slots: []
integrates_with:
  - "COMP-01 from P206 (public-claim and override posture must remain governance-aware)"
  - "TASK-01..05 from P208 (evidence exposure and blocked claims must land in the actual approval surface)"
translation_gate_for: []
files_modified:
  - .planning/evidence/approval-claim-blocking-policy.md
  - lib/markos/evidence/approval-evidence.ts
  - lib/markos/evidence/claim-blocking.ts
  - app/(markos)/operations/approvals/evidence-summary.tsx
  - app/(markos)/operations/approvals/page.stories.tsx
  - test/evidence/phase-209/domain-4/approval-blocking.test.js
  - test/evidence/phase-209/domain-4/evidence-summary-render.test.js
autonomous: true
ui_scope: UI
ui_spec_block: "209-UI-SPEC.md §Surface A — Approval evidence summary; §Acceptance Criteria — E-1..E-22, X-1..X-10, B-6"
requirements:
  - EVD-02
  - EVD-05
  - QA-04
  - QA-11
  - QA-12
must_haves:
  truths:
    - "Approval evidence is explicit enough for a human to understand claim safety quickly."
    - "Unsupported claims block customer-facing actions by default."
    - "Overrides have explicit reason, actor, and lineage (audit-log row with run_id + actor_id + mutation_class + timestamp)."
    - "Sub-component renders 14+ states verbatim per UI-SPEC §Surface A copy register."
    - "Token-only authoring: zero inline hex, mint-as-text via --color-primary-text, .c-notice mandatory for all state banners (D-08, D-09, D-09b)."
    - "D-13 .c-card--feature PROHIBITED in this sub-component; D-14 vanilla <table> only; D-15 sub-component co-located until reuse on ≥2 surfaces."
    - "Mobile-critical inheritance from 208-04 Approval Inbox: 44px touch target via --h-control-touch on (pointer: coarse)."
  artifacts:
    - path: .planning/evidence/approval-claim-blocking-policy.md
      provides: canonical approval evidence snapshot and blocked-action policy (UI-SPEC B-6 source-of-truth)
  key_links:
    - from: app/(markos)/operations/tasks/evidence-panel.tsx
      to: app/(markos)/operations/approvals/evidence-summary.tsx
      via: approval evidence reuses existing immutable evidence presentation pattern (UI-SPEC E-22, Phase 46 D-09/D-10/D-11)
      pattern: "Evidence is immutable"
    - from: lib/markos/evidence/evidence-map.ts
      to: app/(markos)/operations/approvals/evidence-summary.tsx
      via: sub-component imports 24-field EvidenceMap contract + 7 claim_type literals + 3 inference_label literals (UI-SPEC E-4, E-7, E-8, E-9)
      pattern: "EvidenceMap"
    - from: lib/markos/evidence/claim-blocking.ts
      to: app/(markos)/operations/approvals/evidence-summary.tsx
      via: override commit emits audit event linked to RunApiEnvelope.run_id (UI-SPEC E-20; commitOverride(snapshot, reason, run_id) -> AuditEvent)
      pattern: "commitOverride"
---

<objective>
Expose evidence and claim risk inside approvals so unsupported claims cannot slip through and overrides stay explainable. This plan owns the only UI surface in Phase 209 — the evidence-summary.tsx sub-component mounted inside Phase 208 Approval Inbox detail card.
</objective>

<context>
@.planning/phases/209-evidence-research-and-claim-safety/209-CONTEXT.md
@.planning/phases/209-evidence-research-and-claim-safety/209-RESEARCH.md
@.planning/phases/209-evidence-research-and-claim-safety/209-VALIDATION.md
@.planning/phases/209-evidence-research-and-claim-safety/209-UI-SPEC.md
</context>

<ui_spec_fold>
This plan is the **ONLY UI plan** in the hybrid scope. It receives all 22 surface ACs (E-1..E-22), all 10 cross-cutting carry-forward ACs (X-1..X-10), and backend doctrine AC B-6 from `209-UI-SPEC.md`.

**Surface ACs folded (E-* — 22 total):**
- E-1 file existence; E-2 token-only no inline hex; E-3 `<h3>` heading "Evidence"
- E-4 ApprovalEvidenceSnapshot 11 fields verbatim; E-5 claim-blocking literals; E-6 5 blocked-action families
- E-7 5 freshness states; E-8 3 inference labels; E-9 7 claim types; E-10 6 source-quality bands as `.c-badge`
- E-11 `.c-input` override_reason capture with `.c-field` + required-validation copy; E-12 `.c-button--destructive` "Continue without evidence" / "Request second approver"
- E-13 bracketed glyphs `[ok]`, `[warn]`, `[err]`, `[info]`, `[block]`; E-14 vanilla `<table>` (no `.c-table`, no `.c-card--feature`)
- E-15 `.c-notice c-notice--{success|warning|error|info}` for every state banner; E-16 `.c-chip-protocol` only chip primitive for IDs
- E-17 redaction notice when `redaction_policy != 'none'`; E-18 mobile-critical at `max-width: 640px`
- E-19 banned-lexicon zero matches; E-20 override commit audit-event with run_id linkage
- E-21 6 Storybook state stories (EvidenceFreshSupported, EvidenceStale, EvidenceExpired, EvidenceContradictory, EvidenceKnownGap, EvidenceUnsupportedOverride)
- E-22 migration anchor: immutable-evidence callout reused from `app/(markos)/operations/tasks/evidence-panel.tsx`

**Cross-cutting carry-forward (X-* — 10 total):**
- X-1 D-08 token-only enforcement; X-2 D-09 mint-as-text via `--color-primary-text`; X-3 D-09b `.c-notice` mandatory
- X-4 D-13 `.c-card--feature` zero usage; X-5 D-14 vanilla `<table>` only; X-6 D-15 sub-component not extracted to `components/markos/operator/`
- X-7 touch-target ≥44px via `--h-control-touch`; X-8 banned-lexicon + no exclamation points; X-9 no emoji (bracketed glyphs only); X-10 `prefers-reduced-motion` honored

**Backend AC (B-6):**
- B-6 `.planning/evidence/approval-claim-blocking-policy.md` contains all 11 ApprovalEvidenceSnapshot field names + 5 blocked-action families verbatim

**Inheritance citations (load-bearing):**
- 206-UI-SPEC §Downstream: mutation-class doctrine (`external.send | billing.charge | connector.mutate | price.change | public.claim | data.export`); `default_approval_mode`; `evidence_required` flag; `autonomy_ceiling`
- 207-UI-SPEC §Downstream: `RunApiEnvelope.run_id` for `EvidenceMap.created_by_run_id` linkage; audit event emission per 207-04
- 208-UI-SPEC §Surface D — Approval Inbox: PARENT surface; this sub-component REPLACES `<PlaceholderBanner variant="future_phase_209">`; preserves parent's Approve/Decline/Snooze CTAs; mobile_priority: critical inherited
- 209-01..209-03, 209-05, 209-06: contract upstream — 24 EvidenceMap fields, 7 claim types, 3 inference labels, 6 source-quality bands, 5 freshness states, ApprovalEvidenceSnapshot 11 fields all sourced verbatim

**Forward translation gates:**
- `<PlaceholderBanner variant="future_phase_210">` for `claim_type == 'competitive'` connector-derived evidence
- `<PlaceholderBanner variant="future_phase_212">` for `inference_label == 'inferred'` learning-fixture evidence
</ui_spec_fold>

<tasks>
<task id="209-04-01">
  <name>ApprovalEvidenceSnapshot contract + blocked-action policy + claim-blocking module (UI-SPEC B-6, E-4, E-5, E-6, E-20)</name>
  <files>.planning/evidence/approval-claim-blocking-policy.md, lib/markos/evidence/approval-evidence.ts, lib/markos/evidence/claim-blocking.ts, test/evidence/phase-209/domain-4/approval-blocking.test.js</files>
  <read_first>
    - .planning/phases/209-evidence-research-and-claim-safety/209-RESEARCH.md section "Domain 4 - Approval evidence exposure and claim blocking"
    - .planning/phases/209-evidence-research-and-claim-safety/209-UI-SPEC.md §Inheritance Bindings — From 209-04 approval evidence + claim blocking; §Acceptance Criteria — B-6, E-4, E-5, E-6, E-20
    - .planning/phases/206-soc2-type1-foundation/206-UI-SPEC.md §Downstream — mutation_class doctrine
    - .planning/phases/207-agentrun-v2-orchestration-substrate/207-UI-SPEC.md §Downstream — RunApiEnvelope and ApprovalHandoffRecord
    - .planning/phases/208-human-operating-interface/208-04-PLAN.md (parent Approval Inbox)
    - lib/markos/evidence/evidence-map.ts (created in 209-01-01; consumes 24-field contract)
  </read_first>
  <action>
    Step 1: Create `.planning/evidence/approval-claim-blocking-policy.md` with the exact `ApprovalEvidenceSnapshot` fields verbatim: `snapshot_id`, `approval_id`, `evidence_refs`, `source_quality_summary`, `ttl_summary`, `assumption_list`, `unsupported_claims`, `inference_labels`, `override_reason`, `actor_id`, `created_at` (11 fields total). Document each field with type and consumer notes.
    Step 2: In the same policy doc and in `lib/markos/evidence/claim-blocking.ts`, define the exact 5 blocked-action family literals verbatim: `publish`, `send`, `social`, `pricing`, `support`. Bind each family to the corresponding 206 mutation-class root: `publish` → `public.claim`, `send` → `external.send`, `social` → `external.send`, `pricing` → `price.change`, `support` → `external.send`.
    Step 3: In `lib/markos/evidence/claim-blocking.ts`, declare and export `commitOverride(snapshot: ApprovalEvidenceSnapshot, reason: string, run_id: string): AuditEvent` (UI-SPEC E-20). The function MUST emit an audit event with fields: `event_type: 'evidence_override_committed'`, `run_id`, `actor_id` (from snapshot), `mutation_class` (from approval), `override_reason: reason`, `created_at: new Date().toISOString()`.
    Step 4: In `lib/markos/evidence/approval-evidence.ts`, declare `ApprovalEvidenceSnapshot` TypeScript interface with all 11 fields exactly. Include the 5 blocked-action family literal type union: `BlockedActionFamily = 'publish' | 'send' | 'social' | 'pricing' | 'support'`.
    Step 5: Make policy require override lineage for any unsupported-claim exception. Override path requires reason ≥10 chars; `mutation_class` triggering `default_approval_mode == 'dual_approval'` MUST route through second-approver flow (per 206 doctrine).
    Step 6: Add tests covering blocked, editable, and override-required approval states. Test file MUST contain the exact strings `support`, `commitOverride`, and `mutation_class`.
  </action>
  <acceptance_criteria>
    - **UI-SPEC B-6:** `grep -c "snapshot_id\|approval_id\|evidence_refs\|source_quality_summary\|ttl_summary\|assumption_list\|unsupported_claims\|inference_labels\|override_reason\|actor_id\|created_at\|publish\|send\|social\|pricing\|support" .planning/evidence/approval-claim-blocking-policy.md` >= 16
    - **UI-SPEC E-4:** `grep -c "snapshot_id\|approval_id\|evidence_refs\|source_quality_summary\|ttl_summary\|assumption_list\|unsupported_claims\|inference_labels\|override_reason\|actor_id\|created_at" lib/markos/evidence/approval-evidence.ts` >= 11
    - **UI-SPEC E-5:** `grep -c "unsupported_claims\|override_reason" lib/markos/evidence/claim-blocking.ts` >= 2
    - **UI-SPEC E-6:** `grep -c "publish\|send\|social\|pricing\|support" lib/markos/evidence/claim-blocking.ts` >= 5
    - **UI-SPEC E-20:** `grep -c "commitOverride\|run_id\|created_by_run_id" lib/markos/evidence/claim-blocking.ts` >= 2
    - `test/evidence/phase-209/domain-4/approval-blocking.test.js` contains the exact string `support`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/evidence/phase-209/domain-4/approval-blocking.test.js</automated>
  </verify>
  <done>Approval evidence contract is explicit, claim-blocking module emits audit-traceable override events, and 5 blocked-action families bind to 206 mutation-class doctrine. UI-SPEC B-6, E-4, E-5, E-6, E-20 locked at the contract level.</done>
</task>

<task id="209-04-02">
  <name>evidence-summary.tsx sub-component render + 14+ states + override CTA + Storybook (UI-SPEC E-1..E-3, E-7..E-19, E-21..E-22, X-1..X-10, QA-04 coverage gate)</name>
  <files>app/(markos)/operations/approvals/evidence-summary.tsx, app/(markos)/operations/approvals/page.stories.tsx, test/evidence/phase-209/domain-4/evidence-summary-render.test.js</files>
  <read_first>
    - .planning/phases/209-evidence-research-and-claim-safety/209-UI-SPEC.md §Surface A — Approval evidence summary (full section, lines 384-602); §Acceptance Criteria — E-1..E-22 + X-1..X-10; §Copywriting Contract; §Destructive Actions; §Storybook Coverage; §DESIGN.md Compliance Assertions
    - DESIGN.md (canon v1.1.0 — token table, two-typeface rule, 8px grid, focus rings, no-emoji, banned lexicon)
    - .planning/phases/206-soc2-type1-foundation/206-UI-SPEC.md §Downstream — mutation_class + default_approval_mode + evidence_required + autonomy_ceiling
    - .planning/phases/207-agentrun-v2-orchestration-substrate/207-UI-SPEC.md §Downstream — RunApiEnvelope + ApprovalHandoffRecord
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §Surface D — Approval Inbox parent + future_phase_209 placeholder REPLACEMENT contract
    - app/(markos)/operations/tasks/evidence-panel.tsx (migration anchor; Phase 46 D-09/D-10/D-11 immutable-evidence pattern)
    - lib/markos/evidence/evidence-map.ts (24 fields, 7 claim_type literals, 3 inference_label literals)
    - lib/markos/evidence/approval-evidence.ts (created in 209-04-01)
    - lib/markos/evidence/claim-blocking.ts (created in 209-04-01; commitOverride export)
    - styles/components.css (primitive class definitions: .c-card, .c-notice, .c-button--destructive, .c-input, .c-field, .c-chip-protocol, .c-badge)
  </read_first>
  <action>
    Step 1: Create `app/(markos)/operations/approvals/evidence-summary.tsx` as a sub-component (NOT a page route) that mounts inside Phase 208 Approval Inbox detail card. Heading is `<h3>` with text "Evidence" (E-3). The sub-component does NOT introduce its own CSS module — composes parent's `page.module.css` + `styles/components.css` primitives only.
    Step 2: Render all 14+ enumerated states with verbatim copy from UI-SPEC §Copywriting Contract:
      - `not_required`: `<.c-notice c-notice--info>` "[info] No evidence required for this approval."
      - `loading`: `<.c-notice c-notice--info>` "[info] Loading evidence..."
      - `no_evidence_required_and_not_present`: `<.c-notice c-notice--info>` "[info] No evidence on file (none required)."
      - `no_evidence_but_required`: `<.c-notice c-notice--error>` "[err] No evidence on file. Approval is blocked."
      - `fresh_and_supported`: `<.c-notice c-notice--success>` "[ok] Evidence supports every claim. Fresh and within TTL."
      - `stale`: `<.c-notice c-notice--warning>` "[warn] Stale evidence — refresh recommended before approval."
      - `expired`: `<.c-notice c-notice--error>` "[err] Expired — refresh required before approval."
      - `contradictory`: `<.c-notice c-notice--error>` "[err] Contradictory evidence found. Resolve before approval."
      - `known_gap`: `<.c-notice c-notice--warning>` "[warn] Known gap on record. Review before approval."
      - `inferred`: `<.c-notice c-notice--warning>` "[warn] Inference label: inferred. Operator confirmation required."
      - `unsupported_with_override`: `<.c-notice c-notice--warning>` "[warn] Unsupported claim. Override path available."
      - `unsupported_blocked`: `<.c-notice c-notice--error>` "[block] Unsupported claim. Approval blocked by autonomy ceiling."
      - `override_committed`: `<.c-notice c-notice--info>` "[info] Override committed by {actor_id} — {override_reason}"
      - `redacted`: `<.c-notice c-notice--info>` "[info] Some fields are redacted by tenant policy."
      - `error`: `<.c-notice c-notice--error>` "[err] Could not load evidence. Reload the page to retry."
    Step 3: Render all 7 claim types verbatim (E-9): `marketing_copy`, `pricing`, `competitive`, `public_proof`, `sales_enablement`, `support_guidance`, `saas_metric`. For `claim_type == 'pricing'`, render `{{MARKOS_PRICING_ENGINE_PENDING}}` placeholder verbatim until `pricing_engine_context.schema_version` is set (per 205 inheritance).
    Step 4: Render all 3 inference labels verbatim (E-8): `none`, `inferred`, `unsupported`. Render all 5 freshness states verbatim (E-7): `fresh`, `stale`, `expired`, `contradictory`, `gap_known`.
    Step 5: Render 6 source-quality score bands as `.c-badge` rows (E-10): "First-party", "Official", "Analyst", "Public web", "Operator note", "Unsupported" mapped to scores `1.00`, `0.85`, `0.65`, `0.40`, `0.20`, `0.00`.
    Step 6: Override CTA implementation (E-11, E-12):
      - `.c-input` for `override_reason` with `.c-field` + `.c-field__label` "Override reason (required)" + `.c-field__error` "[err] An override reason is required."
      - `.c-button--destructive` text "Continue without evidence" (single-approval mode) OR "Request second approver" (when `default_approval_mode == 'dual_approval'` per 206 doctrine)
      - On submit: validate reason >= 10 chars, then call `commitOverride(snapshot, reason, run_id)` (imported from 209-04-01)
      - When `autonomy_ceiling` reached: render `[block]` glyph + `.c-notice c-notice--error` "Approval blocked by autonomy ceiling. Operator action required."; disable override CTA.
    Step 7: Forward translation gates:
      - When `claim_type == 'competitive'` AND no connector substrate: render `<PlaceholderBanner variant="future_phase_210">` "[info] Connector-derived evidence available in Phase 210."
      - When `inference_label == 'inferred'` AND no learning fixture: render `<PlaceholderBanner variant="future_phase_212">` "[info] Learning-fixture evidence ledger available in Phase 212."
    Step 8: Render `.c-chip-protocol` for ID display (E-16): `evidence_id`, `created_by_run_id`, `approval_id`, `known_gap_id`, `actor_id`. Use mint-as-text via `--color-primary-text` only (D-09).
    Step 9: When `redaction_policy != 'none'` (E-17): render `.c-notice c-notice--info` "[info] Some fields are redacted by tenant policy." above sources table; show `[redacted]` placeholder for redacted source fields.
    Step 10: Migration anchor (E-22): include immutable-evidence callout `[info] Evidence is immutable — historical records cannot be edited.` at least once. Pattern reused verbatim from `app/(markos)/operations/tasks/evidence-panel.tsx` (Phase 46 D-09/D-10/D-11).
    Step 11: List rendering (E-14): use vanilla `<table>` semantic ONLY; ZERO `.c-table` references; ZERO `.c-card--feature` references.
    Step 12: Token compliance (E-2, X-1, X-2, X-3): zero inline hex literals; every color via `var(--color-*)`; every spacing via `var(--space-*)`; every typography via DESIGN.md `typography.*`. Mint never as fill — only via `--color-primary-text` (D-09). Every state banner uses `.c-notice c-notice--{state}` (D-09b); zero local `.banner`/`.alert`/`.warning` classes.
    Step 13: Mobile-critical (E-18, X-7): at `max-width: 640px`, sources table and known-gaps table collapse to vertical KV stacks; override CTA `.c-button--destructive` and `.c-input` inherit `--h-control-touch` (44px) on `(pointer: coarse)`.
    Step 14: Banned-lexicon and emoji enforcement (E-19, X-8, X-9): zero matches for `synergy|leverage|empower|unlock|transform|revolutionize|supercharge|holistic|seamless|cutting-edge|innovative|game-changer|next-generation|world-class|best-in-class|reimagine|disrupt`; no exclamation points in product copy; no emoji (UTF-8 emoji range `[\xF0-\xF7][\x80-\xBF]{3}`); bracketed glyphs only.
    Step 15: D-15 enforcement (X-6): sub-component remains co-located in `app/(markos)/operations/approvals/`; NOT extracted to `components/markos/operator/` (extraction allowed only after reuse on ≥2 surfaces is proven).
    Step 16: Storybook (E-21): extend `app/(markos)/operations/approvals/page.stories.tsx` with 6 named state stories: `EvidenceFreshSupported`, `EvidenceStale`, `EvidenceExpired`, `EvidenceContradictory`, `EvidenceKnownGap`, `EvidenceUnsupportedOverride`. Each story renders the parent's existing approval-detail scaffold with the sub-component mounted.
    Step 17: QA-04 coverage gate: `app/(markos)/operations/approvals/evidence-summary.tsx` and `lib/markos/evidence/approval-evidence.ts` and `lib/markos/evidence/claim-blocking.ts` MUST achieve 100% statement coverage (approval-package paths). Render-test file `test/evidence/phase-209/domain-4/evidence-summary-render.test.js` exercises every state branch.
  </action>
  <acceptance_criteria>
    - **UI-SPEC E-1:** `test -f app/(markos)/operations/approvals/evidence-summary.tsx`
    - **UI-SPEC E-2 + X-1:** `! grep -nE "#[0-9a-fA-F]{3,8}" app/(markos)/operations/approvals/evidence-summary.tsx` returns 0; no `.module.css` co-located with sub-component
    - **UI-SPEC E-3:** `grep -c "<h3" app/(markos)/operations/approvals/evidence-summary.tsx` >= 1 AND `grep -c "Evidence" app/(markos)/operations/approvals/evidence-summary.tsx` >= 1
    - **UI-SPEC E-7:** `grep -c "fresh\|stale\|expired\|contradictory\|gap_known" app/(markos)/operations/approvals/evidence-summary.tsx` >= 5
    - **UI-SPEC E-8:** `grep -c "none\|inferred\|unsupported" app/(markos)/operations/approvals/evidence-summary.tsx` >= 3
    - **UI-SPEC E-9:** `grep -c "marketing_copy\|pricing\|competitive\|public_proof\|sales_enablement\|support_guidance\|saas_metric" app/(markos)/operations/approvals/evidence-summary.tsx` >= 7
    - **UI-SPEC E-10:** `grep -c "First-party\|Official\|Analyst\|Public web\|Operator note\|Unsupported" app/(markos)/operations/approvals/evidence-summary.tsx` >= 6
    - **UI-SPEC E-11:** `grep -c "c-input\|c-field\|override_reason\|override reason is required" app/(markos)/operations/approvals/evidence-summary.tsx` >= 4
    - **UI-SPEC E-12:** `grep -c "c-button--destructive\|Continue without evidence\|Request second approver" app/(markos)/operations/approvals/evidence-summary.tsx` >= 2
    - **UI-SPEC E-13:** `grep -c "\[ok\]\|\[warn\]\|\[err\]\|\[info\]\|\[block\]" app/(markos)/operations/approvals/evidence-summary.tsx` >= 5
    - **UI-SPEC E-14 + X-4 + X-5:** `grep -c "c-table\|c-card--feature" app/(markos)/operations/approvals/evidence-summary.tsx` returns 0; `grep -c "<table" app/(markos)/operations/approvals/evidence-summary.tsx` >= 1
    - **UI-SPEC E-15 + X-3:** `grep -c "c-notice c-notice--success\|c-notice c-notice--warning\|c-notice c-notice--error\|c-notice c-notice--info" app/(markos)/operations/approvals/evidence-summary.tsx` >= 4; `! grep -nE "className=\"(banner|alert|warning|evidence-state)" app/(markos)/operations/approvals/evidence-summary.tsx` returns 0
    - **UI-SPEC E-16:** `grep -c "c-chip-protocol" app/(markos)/operations/approvals/evidence-summary.tsx` >= 4
    - **UI-SPEC E-17:** `grep -c "redaction_policy\|redacted by tenant policy\|\[redacted\]" app/(markos)/operations/approvals/evidence-summary.tsx` >= 2
    - **UI-SPEC E-19 + X-8:** `! grep -nE "synergy|leverage|empower|unlock|transform|revolutionize|supercharge|holistic|seamless|cutting-edge|innovative|game-changer|next-generation|world-class|best-in-class|reimagine|disrupt" app/(markos)/operations/approvals/evidence-summary.tsx` returns 0
    - **UI-SPEC E-21:** `grep -c "EvidenceFreshSupported\|EvidenceStale\|EvidenceExpired\|EvidenceContradictory\|EvidenceKnownGap\|EvidenceUnsupportedOverride" app/(markos)/operations/approvals/page.stories.tsx` >= 6
    - **UI-SPEC E-22:** `grep -c "Evidence is immutable\|cannot be edited\|immutable" app/(markos)/operations/approvals/evidence-summary.tsx` >= 1
    - **UI-SPEC X-2:** `! grep -nE "background:[[:space:]]+var\(--color-primary\)[^-]" app/(markos)/operations/approvals/evidence-summary.tsx` returns 0
    - **UI-SPEC X-6:** `! find components/markos/operator -name "evidence-summary*" 2>/dev/null` returns nothing
    - **UI-SPEC X-9:** `! grep -nE "[\xF0-\xF7][\x80-\xBF]{3}" app/(markos)/operations/approvals/evidence-summary.tsx` returns 0
    - **Forward gate (P210):** `grep -c "future_phase_210\|PlaceholderBanner" app/(markos)/operations/approvals/evidence-summary.tsx` >= 1
    - **Forward gate (P212):** `grep -c "future_phase_212" app/(markos)/operations/approvals/evidence-summary.tsx` >= 1
    - **Pricing placeholder:** `grep -c "MARKOS_PRICING_ENGINE_PENDING" app/(markos)/operations/approvals/evidence-summary.tsx` >= 1
    - **QA-04 coverage:** Statement coverage on `app/(markos)/operations/approvals/evidence-summary.tsx`, `lib/markos/evidence/approval-evidence.ts`, `lib/markos/evidence/claim-blocking.ts` reaches 100% (approval-package paths)
    - `test/evidence/phase-209/domain-4/evidence-summary-render.test.js` exits 0 with all state branches exercised
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/evidence/phase-209/domain-4/</automated>
    <manual>
      - **UI-SPEC E-18 + X-7:** Storybook viewport check at iPhone 14 Pro emulation — sources table collapses, override CTA `.c-button--destructive` and `.c-input` measure ≥44px
      - **UI-SPEC X-10:** OS-level `prefers-reduced-motion: reduce` — sub-component itself has no motion; parent's modal fade respects preference
      - axe-core accessibility scan returns 0 critical/serious violations
      - Chromatic baseline approved for all 6 new state stories
    </manual>
  </verify>
  <done>evidence-summary.tsx sub-component is a fully token-compliant, accessibility-conforming, mobile-critical surface that exposes evidence + claim risk inside Phase 208 Approval Inbox. UI-SPEC E-1..E-3, E-7..E-19, E-21..E-22, and all X-1..X-10 carry-forward decisions are locked at the render level. Phase 208's `<PlaceholderBanner variant="future_phase_209">` is now replaced. QA-04 coverage gate passes on approval-package paths.</done>
</task>
</tasks>

<verification>
- npm test -- test/evidence/phase-209/domain-4/
- node --test test/evidence/phase-209/domain-4/evidence-summary-render.test.js
- npx storybook build (verify 6 evidence-summary state stories present)
- Chromatic baselines for E-21 stories
</verification>

<success_criteria>
- Approval evidence snapshots are explicit and readable (UI-SPEC E-3..E-10)
- Unsupported claims block customer-facing actions by default (UI-SPEC E-6, E-12, B-6)
- Overrides remain governed and auditable (UI-SPEC E-11, E-12, E-20; reason ≥10 chars + dual-approval modal route + audit-event with run_id)
- Sub-component is token-compliant (X-1, X-2, X-3, X-4, X-5)
- Mobile-critical inheritance from 208-04 honored (E-18, X-7; 44px touch via `--h-control-touch`)
- Migration anchor preserved (E-22; immutable-evidence pattern from evidence-panel.tsx)
- Forward translation gates declared for P210 + P212
- Pricing placeholder rule honored verbatim
- QA-04 coverage floor (≥80% statement / 100% on approval-package paths) achieved
</success_criteria>
