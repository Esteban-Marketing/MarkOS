---
id: "30-03-nextjs-auth-boundary"
wave: 3
depends_on: ["30-01-markosdb-contracts"]
discipline: marketing_operations
requirements: ["MDB-03"]
files_modified:
  - onboarding/backend/runtime-context.cjs
  - onboarding/backend/handlers.cjs
  - api/config.js
  - api/status.js
  - TECH-MAP.md
tracking_required: true
requires_human_approval: true
autonomous: false
objective: "Expose authenticated cloud-canonical access paths using Next.js + Supabase auth while preserving local compatibility fallback."
---

# Plan 30-03: Next.js Auth and Runtime Integration

## Tasks

<task id="1" name="Define authenticated endpoint boundaries">
  <read_first>
    - onboarding/backend/runtime-context.cjs
    - onboarding/backend/handlers.cjs
  </read_first>
  <action>
    Specify which operations require authenticated cloud mode and which can remain local compatibility flows.
  </action>
  <acceptance_criteria>
    - Access matrix is explicit for write/read operations
    - Unauthorized access returns clear denial responses
  </acceptance_criteria>
</task>

<task id="2" name="Implement Supabase auth/RLS-aware handlers">
  <read_first>
    - api/config.js
    - api/status.js
  </read_first>
  <action>
    Integrate auth checks and route handlers that enforce project-scoped access control for MarkOSDB operations.
  </action>
  <acceptance_criteria>
    - Authenticated requests succeed with scoped data
    - Unauthenticated requests are rejected
  </acceptance_criteria>
</task>

<task id="3" name="Document integration contracts">
  <read_first>
    - TECH-MAP.md
  </read_first>
  <action>
    Document cloud and local fallback endpoint behavior, payload contracts, and auth expectations.
  </action>
  <acceptance_criteria>
    - TECH-MAP reflects final endpoint and auth contract
  </acceptance_criteria>
</task>

## Verification

```bash
node --test test/onboarding-server.test.js
node --test test/protocol.test.js
```
