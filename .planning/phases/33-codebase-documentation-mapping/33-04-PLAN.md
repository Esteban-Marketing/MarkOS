---
id: 33-04-protocol-doc-sync-verification
phase: 33-codebase-documentation-mapping
plan: 04
type: execute
wave: 4
depends_on:
  - 33-01-codebase-map-bootstrap
  - 33-02-runtime-route-entrypoint-map
  - 33-03-folder-file-inventory-map
files_modified:
  - README.md
  - TECH-MAP.md
  - .protocol-lore/CODEBASE-MAP.md
  - .planning/codebase/README.md
  - test/protocol.test.js
  - .planning/phases/33-codebase-documentation-mapping/33-VERIFICATION.md
autonomous: true
requirements:
  - DOC-04
  - DOC-05
user_setup: []
must_haves:
  truths:
    - Human-facing docs and protocol-facing docs summarize and deep-link to the canonical map instead of defining conflicting inventory truth.
    - Documentation freshness is enforceable through an explicit verification artifact and, if lightweight enough, protocol tests.
    - The documentation system remains maintainable after Phase 33 rather than requiring another remap immediately.
  artifacts:
    - `README.md`, `TECH-MAP.md`, and `.protocol-lore/CODEBASE-MAP.md` point to `.planning/codebase/` as the canonical map.
    - `.planning/phases/33-codebase-documentation-mapping/33-VERIFICATION.md` defines manual and automated drift checks.
    - `test/protocol.test.js` optionally asserts required documentation paths or wording if that adds durable signal without brittleness.
  key_links:
    - Summary docs -> `.planning/codebase/README.md` via canonical map delegation.
    - `test/protocol.test.js` -> documentation surfaces via required-path or wording assertions.
---

<objective>
Synchronize summary docs to the canonical map and add verification guardrails.

Purpose: prevent the new documentation layer from drifting into another disconnected artifact set.
Output: linked summary docs plus a verification contract.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/map-codebase.md
</execution_context>

<context>
@.planning/codebase/README.md
@.planning/codebase/ROUTES.md
@.planning/codebase/FOLDERS.md
@.planning/codebase/FILES.md
@README.md
@TECH-MAP.md
@.protocol-lore/CODEBASE-MAP.md
@test/protocol.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delegate protocol and human docs to the canonical map</name>
  <read_first>
    - README.md
    - TECH-MAP.md
    - .protocol-lore/CODEBASE-MAP.md
    - .planning/codebase/README.md
  </read_first>
  <files>README.md, TECH-MAP.md, .protocol-lore/CODEBASE-MAP.md</files>
  <action>Update the summary docs so they explain the documentation stack, link to the canonical `.planning/codebase/` docs, and keep only concise orientation content that is appropriate for their audience. Remove or compress duplicate topology detail where the canonical map already covers it.</action>
  <acceptance_criteria>
    - All three docs visibly reference `.planning/codebase/` as canonical.
    - Summary docs keep their audience-specific role without restating the entire file inventory.
    - Route and topology truth is not duplicated in two incompatible places.
  </acceptance_criteria>
  <done>The documentation stack has a clear source-of-truth hierarchy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add verification and drift checks</name>
  <read_first>
    - test/protocol.test.js
    - .planning/codebase/README.md
  </read_first>
  <files>.planning/phases/33-codebase-documentation-mapping/33-VERIFICATION.md, test/protocol.test.js</files>
  <action>Create `33-VERIFICATION.md` with manual checks for route parity, folder/file parity, and canonical-link parity. If lightweight enough, add protocol tests that assert required documentation files exist and that key summary docs mention the canonical map. Avoid brittle line-perfect assertions.</action>
  <verify>
    <automated>node --test test/protocol.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Verification covers route inventory drift, folder/file inventory drift, and canonical-link drift.
    - Automated checks, if added, are durable and low-maintenance.
    - The verification artifact names the update triggers for future maintainers.
  </acceptance_criteria>
  <done>Documentation freshness is enforceable after the mapping pass ships.</done>
</task>

</tasks>

<verification>
Run `node --test test/protocol.test.js` if protocol drift checks are added, then perform manual parity checks between summary docs and `.planning/codebase/`.
</verification>

<success_criteria>
- Summary docs point to a single canonical map.
- Verification rules exist and are practical to run.
- Future route or topology changes have a clear documentation update path.
</success_criteria>

<output>
After completion, create `.planning/phases/33-codebase-documentation-mapping/33-04-SUMMARY.md`.
</output>
