---
id: 33-02-runtime-route-entrypoint-map
phase: 33-codebase-documentation-mapping
plan: 02
type: execute
wave: 2
depends_on:
  - 33-01-codebase-map-bootstrap
files_modified:
  - .planning/codebase/ROUTES.md
  - .planning/codebase/ENTRYPOINTS.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/codebase/ARCHITECTURE.md
autonomous: true
requirements:
  - DOC-02
  - DOC-05
user_setup: []
must_haves:
  truths:
    - Every active HTTP route is mapped to a concrete handler and side effect surface.
    - Hosted wrappers and local onboarding routes are documented together so operators can compare runtime paths.
    - CLI binaries and protocol commands are treated as runtime entrypoints, not secondary implementation details.
  artifacts:
    - `.planning/codebase/ROUTES.md` inventories local server routes, hosted `api/` wrappers, request/response contracts, and write/read side effects.
    - `.planning/codebase/ENTRYPOINTS.md` inventories `bin/*.cjs`, `.agent/get-shit-done/bin/*.cjs`, `.agent/markos/bin/*.cjs`, and other executable starting points.
    - Supporting updates in `INTEGRATIONS.md` and `ARCHITECTURE.md` reflect route and entrypoint reality.
  key_links:
    - `onboarding/backend/server.cjs` -> `.planning/codebase/ROUTES.md` via the literal route inventory.
    - `api/*.js` -> `.planning/codebase/ROUTES.md` via hosted runtime wrappers.
    - `bin/*.cjs` and `.agent/*/bin/*.cjs` -> `.planning/codebase/ENTRYPOINTS.md` via operator entrypoint inventory.
---

<objective>
Document runtime surfaces route by route and entrypoint by entrypoint.

Purpose: give planners and maintainers a single place to answer what can be called, where it starts, what file owns it, and what side effects it has.
Output: canonical route and entrypoint inventories.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/map-codebase.md
</execution_context>

<context>
@.planning/codebase/README.md
@.planning/phases/33-codebase-documentation-mapping/33-RESEARCH.md
@onboarding/backend/server.cjs
@onboarding/backend/handlers.cjs
@api/
@bin/
@.agent/get-shit-done/bin/
@.agent/markos/bin/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the canonical route inventory</name>
  <read_first>
    - onboarding/backend/server.cjs
    - onboarding/backend/handlers.cjs
    - .planning/phases/33-codebase-documentation-mapping/33-RESEARCH.md
  </read_first>
  <files>.planning/codebase/ROUTES.md, .planning/codebase/ARCHITECTURE.md</files>
  <action>Create `.planning/codebase/ROUTES.md` with one row per route covering method, path, handler, implementation files, input contract, output contract, primary side effects, runtime mode constraints, and relevant tests. Include local onboarding routes and hosted wrapper routes from `api/` in the same canonical inventory.</action>
  <acceptance_criteria>
    - Every route wired in `onboarding/backend/server.cjs` appears in the inventory.
    - Hosted `api/` routes are documented or explicitly linked to their shared backend logic.
    - Each route includes at least one implementation file reference and one side-effect summary.
  </acceptance_criteria>
  <done>The HTTP runtime surface is documented end to end.</done>
</task>

<task type="auto">
  <name>Task 2: Create the executable entrypoint inventory</name>
  <read_first>
    - bin/
    - .agent/get-shit-done/bin/
    - .agent/markos/bin/
  </read_first>
  <files>.planning/codebase/ENTRYPOINTS.md, .planning/codebase/INTEGRATIONS.md</files>
  <action>Create `.planning/codebase/ENTRYPOINTS.md` covering CLI binaries, protocol tools, and any other executable surfaces. For each entrypoint, document invocation shape, owning file, downstream modules, required env/setup, and operator use case. Update `INTEGRATIONS.md` where entrypoints introduce external-service dependencies.</action>
  <acceptance_criteria>
    - Root `bin/*.cjs` files are included.
    - `.agent/get-shit-done/bin/` and `.agent/markos/bin/` entrypoints are included.
    - Required setup or secret dependencies are called out for operationally sensitive entrypoints.
  </acceptance_criteria>
  <done>Operator entrypoints are mapped alongside HTTP routes.</done>
</task>

</tasks>

<verification>
Cross-check `ROUTES.md` against `onboarding/backend/server.cjs` and `ENTRYPOINTS.md` against the executable file inventory.
</verification>

<success_criteria>
- HTTP routes and executable entrypoints are documented from invocation to implementation.
- Hosted/local distinctions and side effects are explicit.
- Planners can locate the owning file for any route or command quickly.
</success_criteria>

<output>
After completion, create `.planning/phases/33-codebase-documentation-mapping/33-02-SUMMARY.md`.
</output>
