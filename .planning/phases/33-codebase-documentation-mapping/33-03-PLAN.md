---
id: 33-03-folder-file-inventory-map
phase: 33-codebase-documentation-mapping
plan: 03
type: execute
wave: 3
depends_on:
  - 33-01-codebase-map-bootstrap
  - 33-02-runtime-route-entrypoint-map
files_modified:
  - .planning/codebase/FOLDERS.md
  - .planning/codebase/FILES.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/CONVENTIONS.md
  - .planning/codebase/TESTING.md
autonomous: true
requirements:
  - DOC-03
  - DOC-05
user_setup: []
must_haves:
  truths:
    - Maintainers can answer where a capability lives by following folder and file inventories instead of scanning the tree manually.
    - The inventory covers not only runtime code but also protocol, planning, research, and testing surfaces that shape execution.
    - Large folders are summarized at the right level without losing file ownership information.
  artifacts:
    - `.planning/codebase/FOLDERS.md` documents every maintained directory, its purpose, key files, and update triggers.
    - `.planning/codebase/FILES.md` documents individual root and nested files with role, entrypoints/exports, and cross-links to related docs.
    - `STRUCTURE.md`, `CONVENTIONS.md`, and `TESTING.md` are updated with folder/file placement guidance discovered during the inventory.
  key_links:
    - Workspace directory tree -> `.planning/codebase/FOLDERS.md` via complete maintained-surface coverage.
    - Root docs/config plus runtime modules -> `.planning/codebase/FILES.md` via file-level inventory.
    - `test/` -> `.planning/codebase/TESTING.md` via suite ownership and coverage mapping.
---

<objective>
Document the repository folder by folder and file by file.

Purpose: convert the raw workspace tree into a maintainer-grade inventory with enough detail to guide future planning, execution, and review.
Output: canonical folder and file inventories, with supporting structure/testing guidance.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/map-codebase.md
</execution_context>

<context>
@.planning/codebase/COVERAGE-MATRIX.md
@.planning/phases/33-codebase-documentation-mapping/33-RESEARCH.md
@api/
@bin/
@onboarding/
@.agent/
@.planning/
@RESEARCH/
@scripts/
@test/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the folder inventory</name>
  <read_first>
    - .planning/codebase/COVERAGE-MATRIX.md
    - .planning/phases/33-codebase-documentation-mapping/33-RESEARCH.md
  </read_first>
  <files>.planning/codebase/FOLDERS.md, .planning/codebase/STRUCTURE.md</files>
  <action>Create `.planning/codebase/FOLDERS.md` with one section per maintained directory. For each directory, document purpose, notable subdirectories, key files, runtime relevance, and what changes should trigger a documentation refresh. Update `STRUCTURE.md` so it answers where to place new runtime code, tests, docs, planning artifacts, and protocol assets.</action>
  <acceptance_criteria>
    - All maintained top-level directories are covered.
    - High-churn nested directories such as `onboarding/backend/agents`, `onboarding/backend/parsers`, `.agent/get-shit-done`, and `.agent/markos` are broken out explicitly.
    - Each section includes at least one documentation refresh trigger.
  </acceptance_criteria>
  <done>The repository directory topology is documented in a usable way.</done>
</task>

<task type="auto">
  <name>Task 2: Create the file inventory</name>
  <read_first>
    - root workspace files
    - maintained directories listed in `.planning/codebase/COVERAGE-MATRIX.md`
  </read_first>
  <files>.planning/codebase/FILES.md, .planning/codebase/CONVENTIONS.md, .planning/codebase/TESTING.md</files>
  <action>Create `.planning/codebase/FILES.md` with file-level tables or sections covering root files and maintained implementation files. For each file, document purpose, notable exports or owned behavior, integration links, and related tests or docs where applicable. Update `CONVENTIONS.md` and `TESTING.md` with patterns discovered during the inventory so the map also tells maintainers how new files should fit the existing style.</action>
  <acceptance_criteria>
    - Root files are covered alongside nested implementation files.
    - Test ownership or related verification references are included where practical.
    - The file inventory is organized enough to navigate without reading the raw tree.
  </acceptance_criteria>
  <done>Maintainers can navigate the repo at file granularity through the canonical map.</done>
</task>

</tasks>

<verification>
Cross-check `FOLDERS.md` and `FILES.md` against the workspace tree and coverage matrix for omissions.
</verification>

<success_criteria>
- Folder and file inventories cover all maintained surfaces in scope.
- Structure, conventions, and testing guidance are sharpened by real inventory findings.
- Future agents can answer “where does this belong?” and “what owns this?” from the map alone.
</success_criteria>

<output>
After completion, create `.planning/phases/33-codebase-documentation-mapping/33-03-SUMMARY.md`.
</output>
