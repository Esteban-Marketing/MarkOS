# Phase 64.2: GSD Surface and Instruction Alignment - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-06
**Phase:** 64.2-gsd-surface-and-instruction-alignment
**Areas discussed:** Instruction policy, Ownership boundary, Alignment depth, Generator behavior

---

## Instruction Policy

| Option | Description | Selected |
|--------|-------------|----------|
| Dual-root support | Keep root `CLAUDE.md` live and add/manage root `copilot-instructions.md` from the same policy | ✓ |
| CLAUDE-only | Keep only root `CLAUDE.md` and do not add project-root `copilot-instructions.md` | |
| Copilot-only | Migrate to only root `copilot-instructions.md` and retire root `CLAUDE.md` | |

**User's choice:** Dual-root support
**Notes:** The user locked 64.2 around explicit dual-root instruction support rather than a one-way migration.

---

## Ownership Boundary

| Option | Description | Selected |
|--------|-------------|----------|
| `.github` canonical, `.claude` localized | Shared framework stays canonical in `.github`; `.claude` remains the localized runtime and dispatch layer | ✓ |
| Full convergence into `.github` | Start collapsing `.claude` paths and dispatch toward `.github` now | |
| Keep current split only | Document the split but avoid meaningful ownership alignment work | |

**User's choice:** `.github` canonical, `.claude` localized
**Notes:** The phase should align behavior and targets without collapsing localized path dispatch.

---

## Alignment Depth

| Option | Description | Selected |
|--------|-------------|----------|
| Policy alignment only | Update root instruction targets, generators, templates, workflows, and agent wording; leave manifests and end-to-end proof for 64.3 | ✓ |
| Include requirement registry patch | Do the policy alignment and also patch missing `GSD-ALIGN-04/05` entries in `.planning/REQUIREMENTS.md` | |
| Full closure now | Also refresh manifests, broad generated artifacts, and verification in 64.2 | |

**User's choice:** Policy alignment only
**Notes:** The requirements gap can be handled opportunistically if trivial, but it is not the center of the phase and does not justify pulling 64.3 work forward.

---

## Generator Behavior

| Option | Description | Selected |
|--------|-------------|----------|
| One shared policy, two outputs | Align generators/templates so they deliberately emit both root `CLAUDE.md` and root `copilot-instructions.md` | ✓ |
| Separate parallel generators | Keep separate flows for each root file and manually keep them aligned | |
| Root `copilot-instructions.md` only | Generate only the root `copilot-instructions.md` and treat `CLAUDE.md` as static/manual | |

**User's choice:** One shared policy, two outputs
**Notes:** The repo should encode deliberate dual-root generation instead of relying on manual sync.

---

## the agent's Discretion

- Exact generator/template implementation shape that keeps the two root instruction files aligned without creating a new custom migration layer.
- Which workflow and agent wording changes are strictly required to encode the dual-root policy while preserving localized `.claude` behavior.
- Whether to patch the missing 64.2 requirement IDs as a narrow documentation cleanup during implementation.

## Deferred Ideas

- Manifest refresh and verification closure belong in Phase 64.3.
- Any broader runtime migration that repoints `.claude/skills/**` away from localized `.claude/get-shit-done/**` paths is outside 64.2.
