---
phase: 64.2-gsd-surface-and-instruction-alignment
plan: 01
type: execute
wave: 1
depends_on:
  - 64.1-gsd-runtime-parity-and-hook-stabilization
files_modified:
  - .planning/REQUIREMENTS.md
  - bin/install.cjs
  - .github/get-shit-done/bin/lib/root-instruction-policy.cjs
  - .claude/get-shit-done/bin/lib/root-instruction-policy.cjs
  - .github/get-shit-done/bin/lib/profile-output.cjs
  - .claude/get-shit-done/bin/lib/profile-output.cjs
  - .github/get-shit-done/templates/claude-md.md
  - .github/get-shit-done/templates/copilot-instructions.md
  - .claude/get-shit-done/templates/claude-md.md
  - .claude/get-shit-done/templates/copilot-instructions.md
  - copilot-instructions.md
autonomous: true
requirements:
  - GSD-ALIGN-04
  - GSD-ALIGN-05

must_haves:
  truths:
    - "The repo intentionally supports two root project-instruction files: CLAUDE.md for the localized Claude runtime and copilot-instructions.md for the shared .github GSD surface."
    - "One shared managed policy determines the two root outputs, so 64.2 does not rely on manual parallel editing to keep instruction files aligned."
    - "Root copilot-instructions.md exists as a project artifact without replacing CLAUDE.md or treating .github/copilot-instructions.md as the project contract."
    - "Both the claude-md and copilot-instructions template surfaces in .github and .claude reflect the same dual-root policy, so profile or scaffold generation cannot silently reintroduce single-root drift."
    - "The narrow GSD-ALIGN-04 and GSD-ALIGN-05 registry gap is closed in .planning/REQUIREMENTS.md without inventing broader 64.3 scope."
  artifacts:
    - path: ".github/get-shit-done/bin/lib/root-instruction-policy.cjs"
      provides: "Canonical shared dual-root output policy for project instruction generation."
      exports:
        - "getProjectInstructionTargets"
    - path: ".claude/get-shit-done/bin/lib/root-instruction-policy.cjs"
      provides: "Localized wrapper or mirror that keeps .claude generation aligned to the shared dual-root policy without repointing runtime dispatch."
      exports:
        - "getProjectInstructionTargets"
    - path: ".github/get-shit-done/bin/lib/profile-output.cjs"
      provides: "Shared generator behavior wired to the shared policy instead of a single hardcoded root output."
      exports:
        - "cmdGenerateClaudeMd"
        - "cmdGenerateClaudeProfile"
    - path: ".claude/get-shit-done/bin/lib/profile-output.cjs"
      provides: "Localized generator behavior wired to the same dual-root policy while preserving the Claude-local contract."
      exports:
        - "cmdGenerateClaudeMd"
        - "cmdGenerateClaudeProfile"
    - path: "copilot-instructions.md"
      provides: "Project-root Copilot or shared-framework instruction contract distinct from .github/copilot-instructions.md."
      exports: []
    - path: ".github/get-shit-done/templates/copilot-instructions.md"
      provides: "Canonical shared instruction template aligned to the same dual-root managed policy as claude-md generation."
      exports: []
    - path: ".claude/get-shit-done/templates/copilot-instructions.md"
      provides: "Localized template surface aligned to the same dual-root managed policy without changing localized runtime ownership."
      exports: []
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirement registry entries for GSD-ALIGN-04 and GSD-ALIGN-05 copied from the locked 64.2 roadmap and research scope without expanding it."
      exports: []
  key_links:
    - from: ".github/get-shit-done/bin/lib/root-instruction-policy.cjs"
      to: ".github/get-shit-done/bin/lib/profile-output.cjs"
      via: "Shared generator target selection uses one explicit policy module instead of single-file hardcoding"
      pattern: "getProjectInstructionTargets|copilot-instructions\\.md|CLAUDE\\.md"
    - from: ".claude/get-shit-done/bin/lib/root-instruction-policy.cjs"
      to: ".claude/get-shit-done/bin/lib/profile-output.cjs"
      via: "Localized generator behavior consumes the dual-root policy while preserving the active CLAUDE.md contract"
      pattern: "getProjectInstructionTargets|CLAUDE\\.md"
    - from: "copilot-instructions.md"
      to: ".github/agents/gsd-planner.agent.md"
      via: "Shared GSD agents can load the real project-root instruction contract rather than skipping compliance because the file is missing"
      pattern: "copilot-instructions\\.md"
    - from: "bin/install.cjs"
      to: "copilot-instructions.md"
      via: "Installer treats the new root project instruction artifact as first-class without demoting CLAUDE.md"
      pattern: "copilot-instructions\\.md|CLAUDE\\.md"
---

<objective>
Establish the dual-root instruction policy and root artifacts that all later 64.2 wording work depends on.

Purpose: satisfy D-01 through D-03 and D-09 through D-12 first so the repo has one explicit policy for two intentional root outputs before workflow and agent wording are updated.
Output: shared dual-root policy modules, wired profile-output generators, a managed root copilot-instructions.md artifact, installer awareness of the project-root Copilot contract, and a narrow GSD-ALIGN-04 or GSD-ALIGN-05 requirements registry sync.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/64.2-gsd-surface-and-instruction-alignment/64.2-CONTEXT.md
@.planning/phases/64.2-gsd-surface-and-instruction-alignment/64.2-RESEARCH.md
@CLAUDE.md
@.github/copilot-instructions.md
@.claude/settings.json
@bin/install.cjs
@.github/get-shit-done/bin/lib/profile-output.cjs
@.claude/get-shit-done/bin/lib/profile-output.cjs
@.github/get-shit-done/templates/claude-md.md
@.github/get-shit-done/templates/copilot-instructions.md
@.claude/get-shit-done/templates/claude-md.md
@.claude/get-shit-done/templates/copilot-instructions.md
</context>

<tasks>

<task type="auto">
  <name>Task 64.2-01-01: Introduce one explicit dual-root output policy and wire both generation surfaces to it</name>
  <files>.github/get-shit-done/bin/lib/root-instruction-policy.cjs, .claude/get-shit-done/bin/lib/root-instruction-policy.cjs, .github/get-shit-done/bin/lib/profile-output.cjs, .claude/get-shit-done/bin/lib/profile-output.cjs</files>
  <action>
Create one canonical shared policy module under .github/get-shit-done/bin/lib that defines the deliberate project-root instruction targets per D-01, D-02, D-03, D-10, and D-11: keep root CLAUDE.md live for the localized Claude runtime and add or manage root copilot-instructions.md for the shared .github GSD surface. Add the minimal localized .claude policy bridge or mirror needed to preserve .claude path locality per D-05 and D-06, then update both profile-output.cjs implementations and both claude-md/copilot-instructions template surfaces to consume that policy rather than hardcoding a single output filename or single-root story. Do not repurpose .github/copilot-instructions.md as the project artifact, do not repoint .claude skills or runtime dispatch into .github, and do not add manifest-refresh or end-to-end proof work reserved for 64.3.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const shared=require('./.github/get-shit-done/bin/lib/root-instruction-policy.cjs'); const local=require('./.claude/get-shit-done/bin/lib/root-instruction-policy.cjs'); const sharedTargets=shared.getProjectInstructionTargets({cwd:process.cwd(),global:false}).map((entry)=>entry.path||entry.targetPath||entry.outputPath||entry.file); const localTargets=local.getProjectInstructionTargets({cwd:process.cwd(),global:false}).map((entry)=>entry.path||entry.targetPath||entry.outputPath||entry.file); for (const label of ['CLAUDE.md','copilot-instructions.md']) { if (!sharedTargets.some((value)=>String(value).endsWith(label))) throw new Error('shared policy missing '+label); if (!localTargets.some((value)=>String(value).endsWith(label))) throw new Error('local policy missing '+label); } for (const file of ['.github/get-shit-done/templates/claude-md.md','.github/get-shit-done/templates/copilot-instructions.md','.claude/get-shit-done/templates/claude-md.md','.claude/get-shit-done/templates/copilot-instructions.md']) { const text=fs.readFileSync(file,'utf8'); if (!/CLAUDE\.md|copilot-instructions\.md/.test(text)) throw new Error(file+' missing dual-root instruction language'); }"</automated>
  </verify>
  <done>
The shared and localized generation code no longer encode conflicting single-file project instruction policies and both deliberately target the same two root artifacts.
  </done>
</task>

<task type="auto">
  <name>Task 64.2-01-02: Materialize the root project Copilot contract and close the narrow requirements registry gap</name>
  <files>.planning/REQUIREMENTS.md, bin/install.cjs, .github/get-shit-done/templates/claude-md.md, .claude/get-shit-done/templates/claude-md.md, copilot-instructions.md</files>
  <action>
Update the instruction templates so the dual-root policy is explicit at the template level per D-07 and D-10 across both claude-md and copilot-instructions template surfaces, create or refresh the project-root copilot-instructions.md artifact so it is a real managed project contract rather than a missing file per D-03 and D-12, and extend install.cjs only as far as needed to acknowledge the new root artifact without demoting CLAUDE.md. Patch .planning/REQUIREMENTS.md with concise GSD-ALIGN-04 and GSD-ALIGN-05 entries copied from the locked roadmap or research wording per D-09; keep this as a narrow documentation cleanup and do not invent broader requirement text.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); if (!fs.existsSync('copilot-instructions.md')) throw new Error('missing root copilot-instructions.md'); const root=fs.readFileSync('copilot-instructions.md','utf8'); if (/\.github\/copilot-instructions\.md/.test(root)) throw new Error('root project file incorrectly framed as hidden global artifact'); const install=fs.readFileSync('bin/install.cjs','utf8'); if (!install.includes('copilot-instructions.md')) throw new Error('installer does not acknowledge root copilot-instructions.md'); const req=fs.readFileSync('.planning/REQUIREMENTS.md','utf8'); for (const id of ['GSD-ALIGN-04','GSD-ALIGN-05']) { if (!req.includes(id)) throw new Error('requirements registry missing '+id); } for (const file of ['.github/get-shit-done/templates/copilot-instructions.md','.claude/get-shit-done/templates/copilot-instructions.md']) { if (!fs.readFileSync(file,'utf8').includes('copilot-instructions.md')) throw new Error(file+' missing copilot template contract'); }"</automated>
  </verify>
  <done>
Root copilot-instructions.md exists as a managed project artifact, installer behavior recognizes it as a root instruction target, and the 64.2 requirement IDs exist in the registry without expanding the phase scope.
  </done>
</task>

</tasks>

<verification>
Plan 01 is incomplete until both generation trees resolve the same two project-root outputs, root copilot-instructions.md exists as a project artifact rather than a hidden substitute, and the only REQUIREMENTS.md change is the missing GSD-ALIGN-04 or GSD-ALIGN-05 registry patch.
</verification>

<success_criteria>
64.2 has a real policy foundation: one deliberate dual-root output model, a root copilot-instructions.md file on disk, CLAUDE.md still live for the localized runtime, and no ambiguity about whether .github/copilot-instructions.md satisfies the project contract.
</success_criteria>

<output>
After completion, create `.planning/phases/64.2-gsd-surface-and-instruction-alignment/64.2-01-SUMMARY.md`
</output>
