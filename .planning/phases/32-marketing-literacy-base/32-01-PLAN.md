---
id: 32-01-literacy-storage-contracts
phase: 32-marketing-literacy-base
plan: 01
type: execute
wave: 1
depends_on:
  - 30-02-local-cloud-ingestion
  - 31-03-security-retention-guards
files_modified:
  - .planning/phases/32-marketing-literacy-base/32-LITERACY-SUPABASE.sql
  - onboarding/backend/vector-store-client.cjs
  - onboarding/backend/runtime-context.cjs
  - test/vector-store-client.test.js
autonomous: true
requirements:
  - MLB-01
  - MLB-02
user_setup: []
must_haves:
  truths:
    - Literacy chunks have a canonical relational contract in Supabase with admin-only write semantics aligned to service-role usage.
    - Standards retrieval uses a dedicated namespace pattern markos-standards-{discipline} that is tenant-agnostic and does not mix with per-slug namespaces.
    - Agents can query canonical literacy chunks by discipline and business_model using stable filters without changing existing getContext behavior.
  artifacts:
    - .planning/phases/32-marketing-literacy-base/32-LITERACY-SUPABASE.sql defines markos_literacy_chunks schema, RLS enablement, and canonical/superseded status semantics.
    - onboarding/backend/vector-store-client.cjs exports buildStandardsNamespaceName, getLiteracyContext, and upsertLiteracyChunk.
    - test/vector-store-client.test.js asserts standards namespace naming, filter composition, and dual-write upsert results.
  key_links:
    - vector-store-client.cjs -> Upstash namespace markos-standards-{discipline} via buildStandardsNamespaceName and namespace.query calls.
    - vector-store-client.cjs -> Supabase markos_literacy_chunks via upsertLiteracyChunk relational upsert and onConflict chunk_id.
    - runtime-context.cjs -> vector-store-client.cjs via secret/operation gating for literacy admin writes.
---

<objective>
Define and implement the storage contract for the marketing literacy base.

Purpose: establish a stable, testable contract before ingestion and orchestrator wiring so downstream tasks do not invent schema or namespace behavior ad hoc.
Output: Supabase SQL contract plus vector-store literacy retrieval/upsert functions with contract tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/32-marketing-literacy-base/32-RESEARCH.md
@onboarding/backend/vector-store-client.cjs
@onboarding/backend/runtime-context.cjs
@test/vector-store-client.test.js

<interfaces>
From onboarding/backend/vector-store-client.cjs:
```javascript
async function getContext(slug, section, query = 'summary', nResults = 1)
async function upsertVectorDocument(namespaceName, id, content, metadata = {})
module.exports = {
  getContext,
  storeDraft,
  upsertMarkosdbArtifact,
}
```

From onboarding/backend/runtime-context.cjs:
```javascript
function validateRequiredSecrets({ runtimeMode, operation, env = process.env })
function createRuntimeContext(env = process.env)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Supabase literacy migration contract artifact</name>
  <read_first>
    - .planning/phases/32-marketing-literacy-base/32-RESEARCH.md
    - onboarding/backend/runtime-context.cjs
  </read_first>
  <files>.planning/phases/32-marketing-literacy-base/32-LITERACY-SUPABASE.sql, onboarding/backend/runtime-context.cjs</files>
  <behavior>
    - markos_literacy_chunks schema supports metadata fields required for retrieval filtering and lifecycle state tracking.
    - RLS is enabled and table access model remains admin-first through service-role operations.
    - runtime secret policy declares literacy ingestion/admin operations explicitly.
  </behavior>
  <action>Create .planning/phases/32-marketing-literacy-base/32-LITERACY-SUPABASE.sql with CREATE TABLE markos_literacy_chunks, primary key chunk_id, required metadata columns from research, updated_at defaults, and RLS enablement notes consistent with existing service-role bypass assumptions. Add operation keys literacy_ingest_write and literacy_admin_write to runtime-context.cjs REQUIRED_SECRET_MATRIX and map them to SUPABASE_SERVICE_ROLE_KEY + UPSTASH_VECTOR_REST_TOKEN requirements so later handlers and CLIs can enforce a single policy surface.</action>
  <verify>
    <automated>node --test test/vector-store-client.test.js</automated>
  </verify>
  <acceptance_criteria>
    - SQL contract file exists with table schema covering chunk identity, retrieval metadata, lifecycle status, and checksum columns.
    - runtime-context.cjs exports operation entries for literacy_ingest_write and literacy_admin_write.
    - No existing operation keys are removed or renamed.
  </acceptance_criteria>
  <done>Storage and security contracts are explicit and ready for code integration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement literacy retrieval and upsert primitives in vector store client</name>
  <read_first>
    - onboarding/backend/vector-store-client.cjs
    - test/vector-store-client.test.js
    - .planning/phases/32-marketing-literacy-base/32-RESEARCH.md
  </read_first>
  <files>onboarding/backend/vector-store-client.cjs, test/vector-store-client.test.js</files>
  <behavior>
    - getLiteracyContext returns ordered text + metadata + score records from markos-standards namespaces with default status=canonical filtering.
    - upsertLiteracyChunk writes to both Upstash and Supabase and reports partial success deterministically.
    - standards retrieval does not alter existing slug-based getContext behavior.
  </behavior>
  <action>Add STANDARDS_NAMESPACE_PREFIX constant and buildStandardsNamespaceName(discipline). Implement getLiteracyContext(discipline, query, filters = {}, topK = 5) using namespace.query with includeData/includeMetadata, filter composition for business_model CONTAINS and optional funnel_stage/content_type, and canonical status default. Implement upsertLiteracyChunk(chunk) to upsert vector data and relational metadata into markos_literacy_chunks with onConflict chunk_id and checksum persistence. Extend vector-store-client tests to assert namespace naming, filter string generation, empty result handling, and dual-write success/error surfaces.</action>
  <verify>
    <automated>node --test test/vector-store-client.test.js</automated>
  </verify>
  <acceptance_criteria>
    - module exports include buildStandardsNamespaceName, getLiteracyContext, and upsertLiteracyChunk.
    - getContext existing tests remain green with no behavior regression.
    - New tests fail if canonical filter or namespace naming drifts.
  </acceptance_criteria>
  <done>Core literacy storage/retrieval primitives are implemented and contract-tested.</done>
</task>

</tasks>

<verification>
Run node --test test/vector-store-client.test.js and confirm both existing compatibility tests and new literacy contract tests pass.
</verification>

<success_criteria>
- Literacy table contract exists as a tracked SQL artifact for this phase.
- Vector store client can both upsert literacy chunks and retrieve canonical standards context by discipline.
- New capabilities are covered by deterministic vector-store tests.
</success_criteria>

<output>
After completion, create .planning/phases/32-marketing-literacy-base/32-01-SUMMARY.md
</output>
