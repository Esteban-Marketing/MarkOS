---
id: 32-04-literacy-tests-docs-sprint1
phase: 32-marketing-literacy-base
plan: 04
type: execute
wave: 4
depends_on:
  - 32-01-literacy-storage-contracts
  - 32-02-literacy-ingestion-admin-cli
  - 32-03-literacy-orchestrator-runtime
files_modified:
  - README.md
  - TECH-MAP.md
  - .planning/phases/32-marketing-literacy-base/32-OPERATIONS.md
  - test/vector-store-client.test.js
  - test/onboarding-server.test.js
  - test/protocol.test.js
autonomous: true
requirements:
  - MLB-06
user_setup: []
must_haves:
  truths:
    - Literacy infrastructure changes are protected by automated tests covering storage contract, ingestion lifecycle, and runtime retrieval endpoints.
    - Operators have a single runbook for ingest/query/deprecate workflows and rollback expectations.
    - Protocol documentation stays aligned with implemented endpoint and CLI contracts.
  artifacts:
    - .planning/phases/32-marketing-literacy-base/32-OPERATIONS.md documents ingestion cadence, command usage, and rollback troubleshooting.
    - README.md and TECH-MAP.md include literacy architecture, namespace rules, and admin endpoint references.
    - protocol, vector-store, and onboarding test suites assert literacy contract wording and route behavior.
  key_links:
    - protocol.test.js -> README.md/TECH-MAP.md/32-OPERATIONS.md via contract wording and command examples.
    - onboarding-server.test.js -> handlers/server literacy routes via response and guard checks.
    - vector-store-client.test.js -> upsert/query primitives via namespace/filter assertions.
---

<objective>
Lock phase 32 with durable tests and operator-facing documentation.

Purpose: prevent contract drift after implementation and provide repeatable operational guidance for literacy ingestion and retrieval.
Output: docs/runbook updates and cross-suite tests that assert the literacy architecture end to end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-marketing-literacy-base/32-RESEARCH.md
@.planning/phases/32-marketing-literacy-base/32-01-PLAN.md
@.planning/phases/32-marketing-literacy-base/32-02-PLAN.md
@.planning/phases/32-marketing-literacy-base/32-03-PLAN.md
@README.md
@TECH-MAP.md
@test/protocol.test.js
@test/vector-store-client.test.js
@test/onboarding-server.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand automated literacy contract coverage across test suites</name>
  <read_first>
    - test/vector-store-client.test.js
    - test/onboarding-server.test.js
    - test/protocol.test.js
    - .planning/phases/32-marketing-literacy-base/32-RESEARCH.md
  </read_first>
  <files>test/vector-store-client.test.js, test/onboarding-server.test.js, test/protocol.test.js</files>
  <behavior>
    - test suite catches namespace/filter contract regressions for standards retrieval.
    - endpoint tests catch auth gating or payload regressions for literacy admin routes.
    - protocol tests catch documentation drift on literacy commands and lifecycle semantics.
  </behavior>
  <action>Add targeted tests to vector-store-client and onboarding-server suites for canonical status filtering, business_model CONTAINS filter generation, query diagnostics payloads, and denied-access behavior on /admin/literacy/* routes. Extend protocol tests to assert docs mention markos-standards-{discipline}, supersede-not-delete lifecycle, and the ingest/query/deprecate CLI command surfaces. Keep test runtime practical by using module mocks and avoiding network calls.</action>
  <verify>
    <automated>node --test test/vector-store-client.test.js; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/onboarding-server.test.js; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/protocol.test.js; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail when namespace prefix, status filtering, or endpoint guards drift.
    - No external services are required for the new test cases.
    - Existing non-literacy tests remain passing.
  </acceptance_criteria>
  <done>Literacy contracts are guarded by stable automated tests.
</done>
</task>

<task type="auto">
  <name>Task 2: Publish literacy architecture and operator runbook docs</name>
  <read_first>
    - README.md
    - TECH-MAP.md
    - .planning/phases/32-marketing-literacy-base/32-RESEARCH.md
  </read_first>
  <files>README.md, TECH-MAP.md, .planning/phases/32-marketing-literacy-base/32-OPERATIONS.md</files>
  <action>Update README.md and TECH-MAP.md with a concise literacy architecture section: single Upstash index + markos-standards namespaces, Supabase markos_literacy_chunks purpose, admin endpoint list, and CLI lifecycle (ingest, query, ttl-report, deprecate). Create .planning/phases/32-marketing-literacy-base/32-OPERATIONS.md as the operator runbook containing: required env vars, safe ingest sequence, supersede rollback procedure, and troubleshooting matrix for provider unconfigured/degraded states. Keep wording aligned with Phase 31 secret/redaction policy and avoid introducing public client access claims.</action>
  <verify>
    <automated>node --test test/protocol.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Docs describe literacy as admin-managed and standards-global, not tenant-local.
    - Runbook includes concrete commands for ingest, query, and deprecate operations.
    - Protocol tests assert key wording and command examples.
  </acceptance_criteria>
  <done>Operators and maintainers have a tested documentation baseline for literacy operations.</done>
</task>

</tasks>

<verification>
Run node --test test/vector-store-client.test.js; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/onboarding-server.test.js; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/protocol.test.js; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }.
</verification>

<success_criteria>
- Literacy implementation is protected by tests across storage, runtime, and protocol-doc layers.
- README, TECH-MAP, and phase operations runbook describe the same literacy contract.
- Operators can execute ingestion and maintenance workflows from docs without guesswork.
</success_criteria>

<output>
After completion, create .planning/phases/32-marketing-literacy-base/32-04-SUMMARY.md
</output>
