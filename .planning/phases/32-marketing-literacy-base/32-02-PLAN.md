---
id: 32-02-literacy-ingestion-admin-cli
phase: 32-marketing-literacy-base
plan: 02
type: execute
wave: 2
depends_on:
  - 32-01-literacy-storage-contracts
files_modified:
  - onboarding/backend/literacy-chunker.cjs
  - bin/ingest-literacy.cjs
  - bin/literacy-admin.cjs
  - test/literacy-ingest.test.js
autonomous: true
requirements:
  - MLB-03
  - MLB-04
user_setup: []
must_haves:
  truths:
    - Admin operators can ingest literacy markdown files into canonical standards namespaces using a deterministic CLI workflow.
    - Ingestion preserves history by superseding prior chunk versions rather than destructive deletion.
    - Admins can query, deprecate, and inspect TTL/version state through a dedicated literacy admin CLI.
  artifacts:
    - onboarding/backend/literacy-chunker.cjs parses fenced-yaml metadata and emits stable chunk ids per section strategy.
    - bin/ingest-literacy.cjs supports file/folder ingest, checksum dedup, supersede-to-canonical transitions, and summary output.
    - bin/literacy-admin.cjs supports query, ttl-report, deprecate, and conflict-note update paths.
    - test/literacy-ingest.test.js verifies frontmatter parsing, chunk boundaries, and supersede semantics.
  key_links:
    - ingest-literacy.cjs -> literacy-chunker.cjs via parsed metadata + section chunk payload generation.
    - ingest-literacy.cjs -> vector-store-client.cjs via upsertLiteracyChunk and standards namespace naming.
    - literacy-admin.cjs -> Supabase markos_literacy_chunks via status/ttl/deprecation updates keyed by chunk_id or doc_id.
---

<objective>
Deliver ingestion and administration CLIs for the literacy knowledge base.

Purpose: operationalize the storage contract from Plan 32-01 so standards data can be loaded, maintained, and superseded by operators without manual database edits.
Output: chunker utility, ingestion CLI, admin CLI, and focused ingestion tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-marketing-literacy-base/32-RESEARCH.md
@.planning/phases/32-marketing-literacy-base/32-01-PLAN.md
@onboarding/backend/vector-store-client.cjs
@bin/install.cjs
@bin/update.cjs

<interfaces>
From onboarding/backend/vector-store-client.cjs:
```javascript
function buildStandardsNamespaceName(discipline)
async function upsertLiteracyChunk(chunk)
```

From bin patterns in this repo:
```javascript
#!/usr/bin/env node
'use strict';
// parse process.argv manually; exit(1) on invalid input
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement literacy chunker with fenced-yaml parsing contract</name>
  <read_first>
    - .planning/phases/32-marketing-literacy-base/32-RESEARCH.md
    - onboarding/backend/vector-store-client.cjs
  </read_first>
  <files>onboarding/backend/literacy-chunker.cjs, test/literacy-ingest.test.js</files>
  <behavior>
    - Parser extracts metadata from a yaml code fence and supports array fields such as business_model and retrieval_keywords.
    - Chunk boundaries follow heading strategy: definition, evidence base, each core tactic, benchmarks, counter-indicators, and vocabulary terms.
    - Chunk ids are deterministic and stable across repeated ingestion when source sections are unchanged.
  </behavior>
  <action>Create onboarding/backend/literacy-chunker.cjs exporting parseLiteracyFrontmatter(markdown), chunkLiteracyFile(markdown, metadata), and createLiteracyChunkId(docId, sectionSlug, index). Use zero-dependency parsing with robust regex for fenced yaml and heading sections; skip RELATED FILES and LAST VALIDATED as retrieval-low sections. Add test/literacy-ingest.test.js fixtures validating non-standard fenced frontmatter extraction, correct chunk count for representative markdown, and stable ids across repeated runs.</action>
  <verify>
    <automated>node --test test/literacy-ingest.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Chunker functions are exported and consumed without requiring external libraries.
    - Tests cover frontmatter arrays, tactic split behavior, and vocabulary term splitting.
    - Chunk ids remain byte-stable for identical source content.
  </acceptance_criteria>
  <done>Deterministic chunk generation is available for CLI ingestion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build ingestion CLI with supersede-and-canonical lifecycle</name>
  <read_first>
    - onboarding/backend/literacy-chunker.cjs
    - onboarding/backend/vector-store-client.cjs
    - .planning/phases/32-marketing-literacy-base/32-RESEARCH.md
  </read_first>
  <files>bin/ingest-literacy.cjs, test/literacy-ingest.test.js</files>
  <behavior>
    - ingest command accepts a single file or directory and reports processed/skipped/error counts.
    - re-ingesting an updated doc marks previous chunks superseded and writes new canonical chunks.
    - checksum dedup skips unchanged docs without rewriting vector data.
  </behavior>
  <action>Create bin/ingest-literacy.cjs (raw process.argv parser) with flags --path, --discipline, --dry-run, --limit, and --verbose. For each file, parse frontmatter + chunks, compute checksums, detect unchanged docs, and if changed: mark existing doc_id chunks status=superseded in Supabase/Upstash metadata, then upsert new canonical chunks via upsertLiteracyChunk. Emit machine-readable summary JSON at completion for CI verification. Extend literacy-ingest tests with supersede behavior and dedup/no-op assertions.</action>
  <verify>
    <automated>node --test test/literacy-ingest.test.js</automated>
  </verify>
  <acceptance_criteria>
    - CLI exits non-zero for invalid flags or missing path.
    - Dry-run mode reports planned mutations without writing.
    - Re-ingest transitions old chunks to superseded and inserts canonical replacements.
  </acceptance_criteria>
  <done>Operators can ingest and re-ingest literacy docs with explicit lifecycle behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add literacy admin CLI for query/ttl/deprecate workflows</name>
  <read_first>
    - bin/ingest-literacy.cjs
    - onboarding/backend/vector-store-client.cjs
    - .planning/phases/32-marketing-literacy-base/32-RESEARCH.md
  </read_first>
  <files>bin/literacy-admin.cjs, test/literacy-ingest.test.js</files>
  <behavior>
    - query subcommand returns topK standards matches with metadata and score.
    - ttl-report identifies chunks approaching expiry by ttl_days and last_validated windows.
    - deprecate and conflict-note updates preserve records while transitioning status.
  </behavior>
  <action>Create bin/literacy-admin.cjs with subcommands query, ttl-report, deprecate, and conflict-note. query must call getLiteracyContext and print structured results. ttl-report must query Supabase markos_literacy_chunks for soon-expiring canonical records. deprecate must update status to superseded (not delete). conflict-note must append or set conflict_note for a target chunk/doc. Extend literacy-ingest tests (or add sectioned cases) to verify command parsing and non-destructive deprecation behavior.</action>
  <verify>
    <automated>node --test test/literacy-ingest.test.js</automated>
  </verify>
  <acceptance_criteria>
    - All four subcommands are discoverable via --help output.
    - No command performs destructive namespace/table deletes.
    - CLI output includes deterministic exit codes suitable for automation.
  </acceptance_criteria>
  <done>Admin operations for retrieval diagnostics and lifecycle management are scriptable.
</done>
</task>

</tasks>

<verification>
Run node --test test/literacy-ingest.test.js and confirm chunking, ingestion lifecycle, and admin command behavior are all covered.
</verification>

<success_criteria>
- Literacy markdown can be ingested from CLI with deterministic chunking.
- Re-ingestion preserves historical records through superseded status instead of deletion.
- Admin CLI supports query, TTL reporting, and controlled deprecation/conflict annotation.
</success_criteria>

<output>
After completion, create .planning/phases/32-marketing-literacy-base/32-02-SUMMARY.md
</output>
