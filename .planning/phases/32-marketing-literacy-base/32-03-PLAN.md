---
id: 32-03-literacy-orchestrator-runtime
phase: 32-marketing-literacy-base
plan: 03
type: execute
wave: 3
depends_on:
  - 32-01-literacy-storage-contracts
  - 32-02-literacy-ingestion-admin-cli
  - 31-03-security-retention-guards
files_modified:
  - onboarding/backend/agents/orchestrator.cjs
  - onboarding/backend/handlers.cjs
  - onboarding/backend/server.cjs
  - onboarding/backend/runtime-context.cjs
  - test/onboarding-server.test.js
autonomous: true
requirements:
  - MLB-05
user_setup: []
must_haves:
  truths:
    - Draft generation can merge client context with standards literacy context during orchestration.
    - Admin-only literacy routes expose health and targeted query access without leaking data to public clients.
    - Runtime secret gating and redaction remain consistent with Phase 31 security policy when literacy routes are added.
  artifacts:
    - onboarding/backend/agents/orchestrator.cjs builds dual-layer context using getContext plus getLiteracyContext.
    - onboarding/backend/handlers.cjs implements /admin/literacy/health and /admin/literacy/query handlers with guarded access.
    - onboarding/backend/server.cjs wires literacy admin routes with no regressions to existing endpoints.
    - test/onboarding-server.test.js covers endpoint auth gating and response contracts.
  key_links:
    - orchestrator.cjs -> vector-store-client.cjs via concurrent getContext/getLiteracyContext composition.
    - server.cjs -> handlers.cjs via POST /admin/literacy/query and GET /admin/literacy/health route wiring.
    - handlers.cjs -> runtime-context.cjs via literacy_admin_write secret validation and redactSensitive output handling.
---

<objective>
Integrate literacy retrieval into runtime orchestration and admin API surfaces.

Purpose: make the new literacy base usable by agents and operators, not just stored, while preserving existing hosted/local security boundaries.
Output: orchestrator dual-layer retrieval plus guarded admin endpoints and route tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-marketing-literacy-base/32-RESEARCH.md
@.planning/phases/32-marketing-literacy-base/32-01-PLAN.md
@.planning/phases/32-marketing-literacy-base/32-02-PLAN.md
@onboarding/backend/agents/orchestrator.cjs
@onboarding/backend/handlers.cjs
@onboarding/backend/server.cjs
@onboarding/backend/runtime-context.cjs
@test/onboarding-server.test.js

<interfaces>
From onboarding/backend/agents/orchestrator.cjs:
```javascript
async function orchestrate(seed, slug)
```

From onboarding/backend/handlers.cjs:
```javascript
async function handleStatus(req, res)
async function handleConfig(req, res)
module.exports = { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dual-layer literacy retrieval to orchestrator context assembly</name>
  <read_first>
    - onboarding/backend/agents/orchestrator.cjs
    - onboarding/backend/vector-store-client.cjs
    - .planning/phases/32-marketing-literacy-base/32-RESEARCH.md
  </read_first>
  <files>onboarding/backend/agents/orchestrator.cjs, test/onboarding-server.test.js</files>
  <behavior>
    - orchestrator retrieves client context and standards context in parallel for supported disciplines.
    - missing literacy matches degrade to empty standards context without failing draft generation.
    - standards context includes metadata provenance for auditability in generated output traces.
  </behavior>
  <action>Refactor orchestrator context assembly to call getContext(slug, section, query, n) and getLiteracyContext(discipline, query, filters, topK) via Promise.all. Map business_model filter from seed.company.business_model and preserve existing retry/fallback behavior. Add deterministic logging/telemetry fields for literacy_context_hits and literacy_namespace while redacting sensitive values. Extend onboarding-server tests to assert orchestration still succeeds when literacy provider is unconfigured and includes standards context when mocked hits are returned.</action>
  <verify>
    <automated>node --test test/onboarding-server.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Orchestrator can run with and without literacy context.
    - Existing draft generation flow and response shape remain compatible.
    - Tests fail if literacy failures incorrectly hard-fail submit orchestration.
  </acceptance_criteria>
  <done>Agent generation consumes literacy standards without destabilizing existing submit flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add guarded literacy admin endpoints and route wiring</name>
  <read_first>
    - onboarding/backend/handlers.cjs
    - onboarding/backend/server.cjs
    - onboarding/backend/runtime-context.cjs
    - test/onboarding-server.test.js
  </read_first>
  <files>onboarding/backend/handlers.cjs, onboarding/backend/server.cjs, onboarding/backend/runtime-context.cjs, test/onboarding-server.test.js</files>
  <behavior>
    - GET /admin/literacy/health reports provider readiness plus literacy table availability.
    - POST /admin/literacy/query accepts discipline/query/filters/topK and returns structured matches.
    - both routes reject missing required secrets before querying providers.
  </behavior>
  <action>Implement handlers handleLiteracyHealth and handleLiteracyQuery in handlers.cjs, add routing in server.cjs for GET /admin/literacy/health and POST /admin/literacy/query, and enforce validateRequiredSecrets with literacy_admin_write operation. health output must include upstash/supabase configured flags and counts when available; query output must include namespace, applied filters, and scored matches. Use redactSensitive on logged payload fragments and keep CORS and JSON helper behavior aligned with existing endpoints. Extend onboarding-server tests for authorized and denied path coverage.</action>
  <verify>
    <automated>node --test test/onboarding-server.test.js</automated>
  </verify>
  <acceptance_criteria>
    - New endpoints are unreachable without configured admin secrets.
    - Response payloads are deterministic and include query diagnostics.
    - Existing /status and /config behavior is unchanged.
  </acceptance_criteria>
  <done>Literacy runtime surfaces are operational and safely gated.
</done>
</task>

</tasks>

<verification>
Run node --test test/onboarding-server.test.js and confirm submit flow, existing rollout guards, and new literacy admin routes all pass in one suite.
</verification>

<success_criteria>
- Orchestrator can augment generation with standards literacy context.
- Admin endpoints provide secure operational visibility and diagnostics for literacy retrieval.
- Security and redaction policies remain consistent with prior rollout hardening.
</success_criteria>

<output>
After completion, create .planning/phases/32-marketing-literacy-base/32-03-SUMMARY.md
</output>
