---
phase: 89-runtime-governance-wiring-and-closure-emission
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/phase-89/runtime-governance-telemetry-wiring.test.js
  - test/phase-89/runtime-governance-closeout-verification.test.js
  - test/phase-89/helpers/supabase-audit-store-mock.cjs
  - onboarding/backend/handlers.cjs
autonomous: true
requirements:
  - GOVV-02
  - GOVV-03
must_haves:
  truths:
    - "live agent and operator role-view paths emit schema-valid governance telemetry with required fields"
    - "runtime closeout flow invokes hardened verification before closure emission"
    - "governance wiring fails closed when required telemetry or verification evidence is invalid"
  artifacts:
    - path: test/phase-89/runtime-governance-telemetry-wiring.test.js
      provides: runtime telemetry call-site and schema enforcement contract tests
    - path: test/phase-89/runtime-governance-closeout-verification.test.js
      provides: closeout hardened verification fail-closed integration tests
    - path: onboarding/backend/handlers.cjs
      provides: live role-view telemetry wiring and closeout verification integration
  key_links:
    - from: onboarding/backend/handlers.cjs
      to: onboarding/backend/agents/telemetry.cjs
      via: role-view handlers call captureGovernanceEvent with normalized governance payloads
    - from: onboarding/backend/handlers.cjs
      to: onboarding/backend/vault/hardened-verification.cjs
      via: closeout path blocks emission when verifyHighRiskExecution reports anomalies
---

<objective>
Implement runtime governance wiring for GOVV-02 and GOVV-03 by moving telemetry and hardened verification from test-only posture into live role-view and closeout execution paths.

Purpose: close critical audit findings where governance primitives existed but were not runtime-consumed.
Output: Wave 0 tests plus live handler wiring that enforces secure fail-closed behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/89-runtime-governance-wiring-and-closure-emission/89-CONTEXT.md
@.planning/phases/89-runtime-governance-wiring-and-closure-emission/89-RESEARCH.md
@.planning/phases/89-runtime-governance-wiring-and-closure-emission/89-VALIDATION.md
@onboarding/backend/handlers.cjs
@onboarding/backend/agents/telemetry.cjs
@onboarding/backend/vault/telemetry-schema.cjs
@onboarding/backend/vault/hardened-verification.cjs

<interfaces>
From onboarding/backend/agents/telemetry.cjs:
- captureGovernanceEvent(eventName, properties) -> normalized governance payload

From onboarding/backend/vault/telemetry-schema.cjs:
- normalizeGovernanceTelemetryEvent(input) throws on missing required fields

From onboarding/backend/vault/hardened-verification.cjs:
- verifyHighRiskExecution(input) -> { verified, anomaly_flags, expected_evidence_ref, observed_evidence_ref, reasoning_trace }
</interfaces>
</context>

<threat_model>
- T-89-01 (GOVV-02): telemetry bypass risk where live role-view requests complete without governance evidence. Mitigation: mandatory runtime `captureGovernanceEvent` call-sites in agent/operator handlers with schema validation and explicit failure handling.
- T-89-02 (GOVV-03): non-regression drift risk where closure proceeds without hardened verification. Mitigation: pre-emission `verifyHighRiskExecution` gate in runtime closeout path with fail-closed denial when anomaly flags are present.
- Secure behavior baseline: preserve additive existing routes only (D-01, D-10), system-owned governance trigger semantics (D-02), and strict closure blocking when governance requirements are not met (D-11).
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 governance runtime test scaffolding</name>
  <files>test/phase-89/runtime-governance-telemetry-wiring.test.js, test/phase-89/runtime-governance-closeout-verification.test.js, test/phase-89/helpers/supabase-audit-store-mock.cjs</files>
  <behavior>
    - telemetry wiring test proves live role-view paths invoke governance telemetry with required fields (vault_artifact_id via artifact_id, retrieval_mode, outcome_status, evidence refs, anomaly_flags).
    - closeout verification test proves hardened verification runs in live closeout flow and blocks on missing evidence/anomalies.
    - helper fixture provides deterministic Supabase append/getAll behavior for runtime integration tests.
  </behavior>
  <action>Create failing Wave 0 tests and helper fixture from 89-VALIDATION requirements before runtime implementation (per D-07, D-08, D-09).</action>
  <verify>
    <automated>node --test --test-name-pattern "smoke" test/phase-89/runtime-governance-telemetry-wiring.test.js test/phase-89/runtime-governance-closeout-verification.test.js</automated>
  </verify>
  <done>Phase 89 Wave 0 test contracts exist for GOVV-02 and GOVV-03 with deterministic fixture support.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire governance telemetry capture into live role-view runtime handlers</name>
  <files>onboarding/backend/handlers.cjs</files>
  <behavior>
    - operator role-view action flow emits governance telemetry event after lineage append using normalized payload fields.
    - agent role-view retrieval flow emits governance telemetry event for reason/apply/iterate executions.
    - invalid telemetry payload paths fail closed for governance wiring readiness checks and return machine-readable error envelopes.
  </behavior>
  <action>Integrate `captureGovernanceEvent` call-sites in existing role-view handlers (no new public route per D-01, D-10), include required schema fields, and enforce deterministic handling for missing/invalid telemetry fields per D-09.</action>
  <verify>
    <automated>node --test --test-name-pattern "smoke" test/phase-89/runtime-governance-telemetry-wiring.test.js</automated>
  </verify>
  <done>Live role-view runtime paths emit schema-valid governance telemetry and fail closed on invalid governance payloads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce hardened verification in closeout runtime path</name>
  <files>onboarding/backend/handlers.cjs</files>
  <behavior>
    - closeout path invokes `verifyHighRiskExecution` before closure emission.
    - anomaly flags or missing evidence refs block closeout with deterministic governance error response.
    - successful verification emits governance telemetry containing verification outcome and anomaly state.
  </behavior>
  <action>Wire closeout preconditions to `verifyHighRiskExecution` and block progression when verification fails (per D-11 fail-closed posture), while preserving system-actor trigger ownership after verification gates pass (D-02).</action>
  <verify>
    <automated>node --test --test-name-pattern "smoke" test/phase-89/runtime-governance-closeout-verification.test.js</automated>
  </verify>
  <done>Runtime closeout verification is live, auditable, and fail-closed for GOVV-03.</done>
</task>

</tasks>

<verification>
- `node --test test/phase-89/runtime-governance-telemetry-wiring.test.js`
- `node --test test/phase-89/runtime-governance-closeout-verification.test.js`
- `node --test test/phase-88/*.test.js` (non-regression guard)
</verification>

<success_criteria>
- GOVV-02: Live role-view paths emit normalized governance telemetry with required fields.
- GOVV-03: Hardened verification is invoked and blocks closeout on anomaly/missing evidence.
- All Wave 0 tests for this plan are present and executable.
</success_criteria>

<output>
After completion, create `.planning/phases/89-runtime-governance-wiring-and-closure-emission/89-01-SUMMARY.md`.
</output>
