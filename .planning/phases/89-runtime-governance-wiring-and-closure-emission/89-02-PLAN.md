---
phase: 89-runtime-governance-wiring-and-closure-emission
plan: "02"
type: execute
wave: 2
depends_on: ["89-01"]
files_modified:
  - test/phase-89/runtime-closure-emission-persistence.test.js
  - onboarding/backend/handlers.cjs
  - onboarding/backend/brand-governance/governance-artifact-writer.cjs
  - onboarding/backend/vault/audit-store.cjs
autonomous: true
requirements:
  - GOVV-05
must_haves:
  truths:
    - "closeout runtime emits milestone closure bundle after verification gates pass under system-actor ownership"
    - "successful closeout returns deterministic closure references including bundle_hash and durable locator"
    - "closure evidence persists with dual-write semantics (disk artifact and queryable audit-store record)"
    - "durable-write failure blocks closure (fail-closed) when governance persistence is required"
  artifacts:
    - path: test/phase-89/runtime-closure-emission-persistence.test.js
      provides: runtime closure emission and persistence contract tests
    - path: onboarding/backend/handlers.cjs
      provides: runtime closeout orchestration for bundle emission and response reference contract
    - path: onboarding/backend/brand-governance/governance-artifact-writer.cjs
      provides: deterministic closure bundle persistence helper and hash/locator envelope
    - path: onboarding/backend/vault/audit-store.cjs
      provides: durable audit-store guardrails for fail-closed closure writes
  key_links:
    - from: onboarding/backend/handlers.cjs
      to: onboarding/backend/brand-governance/governance-artifact-writer.cjs
      via: post-gate runtime closeout emits closure bundle and deterministic references
    - from: onboarding/backend/handlers.cjs
      to: onboarding/backend/vault/audit-store.cjs
      via: closure reference record append enforces durable dual-write path
---

<objective>
Implement GOVV-05 runtime closure emission by wiring deterministic milestone closure bundle generation and dual-write persistence into the live closeout path.

Purpose: eliminate test-only closure behavior and make closeout auditable and queryable in production paths.
Output: Wave 0 persistence test, runtime closure emission wiring, deterministic response references, and fail-closed durable write policy.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/89-runtime-governance-wiring-and-closure-emission/89-CONTEXT.md
@.planning/phases/89-runtime-governance-wiring-and-closure-emission/89-RESEARCH.md
@.planning/phases/89-runtime-governance-wiring-and-closure-emission/89-VALIDATION.md
@.planning/v3.5.0-MILESTONE-AUDIT.md
@onboarding/backend/handlers.cjs
@onboarding/backend/brand-governance/governance-artifact-writer.cjs
@onboarding/backend/vault/audit-store.cjs

<interfaces>
From onboarding/backend/brand-governance/governance-artifact-writer.cjs:
- writeMilestoneClosureBundle({ phase, sections }) -> { passed, bundle_hash, written_at, sections }

From onboarding/backend/vault/audit-store.cjs:
- append(entry) -> stored audit record
- createAuditStore(options) -> Supabase or in-memory store (must enforce fail-closed policy in closeout path)
</interfaces>
</context>

<threat_model>
- T-89-03 (GOVV-05): closure evidence tampering/loss risk when closeout success is returned without durable references. Mitigation: deterministic bundle hash + locator returned in response and persisted through dual-write (disk + audit-store/Supabase path).
- Persistence downgrade threat: silent fallback to in-memory store can produce unverifiable closure. Mitigation: closeout path requires durable persistence mode and fails closed if Supabase-backed write cannot be guaranteed.
- Trigger abuse threat: user-triggered closure emission bypassing governance sequence. Mitigation: keep additive existing flow and enforce system-actor emission only after gates pass (D-01, D-02, D-11).
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 closure emission persistence test contract</name>
  <files>test/phase-89/runtime-closure-emission-persistence.test.js</files>
  <behavior>
    - closeout success includes deterministic `bundle_hash` and durable locator/path fields.
    - closure emission writes bundle artifact to disk and appends queryable audit-store record.
    - closeout fails closed when durable persistence is unavailable or append/write fails.
  </behavior>
  <action>Create failing GOVV-05 integration tests mapped to 89-VALIDATION task `89-02-01` and threat `T-89-03` before implementation.</action>
  <verify>
    <automated>node --test --test-name-pattern "smoke" test/phase-89/runtime-closure-emission-persistence.test.js</automated>
  </verify>
  <done>Wave 0 persistence contract exists and captures deterministic reference plus fail-closed expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire runtime closeout closure-bundle emission and deterministic references</name>
  <files>onboarding/backend/handlers.cjs, onboarding/backend/brand-governance/governance-artifact-writer.cjs</files>
  <behavior>
    - on successful closure gates and hardened verification, system actor emits milestone closure bundle from existing runtime flow.
    - closure bundle includes deterministic hash and disk locator suitable for audit retrieval.
    - runtime response includes machine-readable closure reference envelope (hash + locator + timestamp).
  </behavior>
  <action>Add additive closeout emission wiring in existing handlers (no new public route per D-01), extend writer with deterministic persistence envelope where needed, and ensure trigger remains system-owned post-verification (D-02, D-03, D-05).</action>
  <verify>
    <automated>node --test --test-name-pattern "smoke" test/phase-89/runtime-closure-emission-persistence.test.js</automated>
  </verify>
  <done>Runtime closeout emits deterministic closure bundle references from live path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce dual-write durable persistence policy for closure records</name>
  <files>onboarding/backend/handlers.cjs, onboarding/backend/vault/audit-store.cjs</files>
  <behavior>
    - closeout persists closure reference record to audit-store/Supabase path with deterministic lookup fields.
    - closure operation enforces fail-closed behavior when durable persistence preconditions are not met.
    - persisted records remain queryable for milestone forensics and audit cross-checks.
  </behavior>
  <action>Implement closure-specific durable persistence guard in audit-store integration and handler orchestration so closure cannot succeed on non-durable fallback paths (per D-04, D-06, D-11).</action>
  <verify>
    <automated>node --test --test-name-pattern "smoke" test/phase-89/runtime-closure-emission-persistence.test.js</automated>
  </verify>
  <done>Dual-write closure persistence is enforced and auditable with fail-closed runtime behavior.</done>
</task>

</tasks>

<verification>
- `node --test test/phase-89/runtime-closure-emission-persistence.test.js`
- `node --test test/phase-89/*.test.js`
- `node --test test/phase-88/*.test.js` (governance non-regression)
</verification>

<success_criteria>
- GOVV-05 closure emission is runtime-wired (not test-only).
- Closeout responses include deterministic closure references (`bundle_hash` + locator).
- Closure persistence is dual-write and fail-closed when durable path cannot be guaranteed.
</success_criteria>

<output>
After completion, create `.planning/phases/89-runtime-governance-wiring-and-closure-emission/89-02-SUMMARY.md`.
</output>
