---
id: "28-03-gitignore-protection"
wave: 1
depends_on: ["28-02-node-runtime-contract"]
discipline: marketing_operations
requirements: ["P0-03"]
files_modified:
  - bin/install.cjs
  - test/install.test.js
tracking_required: true
requires_human_approval: false
autonomous: true
objective: "Ensure install flow automatically applies private local-data gitignore protections in an idempotent way."
---

# Plan 28-03: Gitignore Protection Injection

## Tasks

<task id="1" name="Implement idempotent gitignore append block">
  <read_first>
    - bin/install.cjs
  </read_first>
  <action>
    Append MarkOS private-data ignore block when missing and avoid duplicate insertions on repeated install runs.
  </action>
  <acceptance_criteria>
    - New projects receive required ignore lines
    - Re-running install does not duplicate entries
    - Console output confirms action only when changes occur
  </acceptance_criteria>
</task>

<task id="2" name="Cover fresh + existing gitignore scenarios">
  <read_first>
    - test/install.test.js
  </read_first>
  <action>
    Add installer tests for empty, existing-without-block, and existing-with-block states.
  </action>
  <acceptance_criteria>
    - Tests assert presence of all required ignore lines
    - Tests assert idempotent behavior
  </acceptance_criteria>
</task>

## Verification

```bash
node --test test/install.test.js
```
