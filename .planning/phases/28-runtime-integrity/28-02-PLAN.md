---
id: "28-02-node-runtime-contract"
wave: 1
depends_on: ["28-01-approve-path-resolver"]
discipline: marketing_operations
requirements: ["P0-02"]
files_modified:
  - package.json
  - bin/install.cjs
  - bin/update.cjs
  - README.md
  - test/install.test.js
tracking_required: true
requires_human_approval: false
autonomous: true
objective: "Align runtime contract to Node 20.16+ and fail fast with clear installation guidance."
---

# Plan 28-02: Node Runtime Contract

## Tasks

<task id="1" name="Set canonical Node engine floor">
  <read_first>
    - package.json
    - README.md
  </read_first>
  <action>
    Update documented and enforced Node prerequisite to `>=20.16.0` across package metadata and setup docs.
  </action>
  <acceptance_criteria>
    - `package.json` engines field matches `>=20.16.0`
    - README prerequisite section matches runtime contract
  </acceptance_criteria>
</task>

<task id="2" name="Add install/update runtime guards">
  <read_first>
    - bin/install.cjs
    - bin/update.cjs
  </read_first>
  <action>
    Add early version guard checks that exit with actionable error messaging for unsupported Node versions.
  </action>
  <acceptance_criteria>
    - Unsupported versions fail before install/update mutations
    - Output includes upgrade guidance and minimum version
  </acceptance_criteria>
</task>

<task id="3" name="Test runtime guard logic">
  <read_first>
    - test/install.test.js
  </read_first>
  <action>
    Add unit tests for below-threshold rejection and supported-version pass-through behavior.
  </action>
  <acceptance_criteria>
    - Guard tests reliably assert both failure and success paths
  </acceptance_criteria>
</task>

## Verification

```bash
node --test test/install.test.js
```
