---
id: "28-01-approve-path-resolver"
wave: 1
depends_on: []
discipline: marketing_operations
requirements: ["P0-01"]
files_modified:
  - onboarding/backend/handlers.cjs
  - onboarding/backend/runtime-context.cjs
  - test/write-mir.test.js
  - test/onboarding-server.test.js
tracking_required: true
requires_human_approval: false
autonomous: true
objective: "Harden approve write-path resolution so valid payloads persist MIR outputs without runtime crashes in local mode and return explicit guards in hosted mode."
---

# Plan 28-01: Approve Path Resolver Hardening

## Tasks

<task id="1" name="Unify MIR output path resolver contract">
  <read_first>
    - onboarding/backend/handlers.cjs
    - onboarding/backend/runtime-context.cjs
  </read_first>
  <action>
    Ensure approve flow uses one canonical resolver path and safe fallback to `.markos-local/MIR` when `mir_output_path` is absent.
  </action>
  <acceptance_criteria>
    - Path resolver is defined and reachable from approve flow
    - No runtime reference errors on valid approve requests
    - Fallback path remains compatibility-safe
  </acceptance_criteria>
</task>

<task id="2" name="Preserve hosted runtime write guard behavior">
  <read_first>
    - onboarding/backend/runtime-context.cjs
    - onboarding/backend/handlers.cjs
  </read_first>
  <action>
    Keep explicit hosted-mode no-local-write response semantics (`LOCAL_PERSISTENCE_UNAVAILABLE`) while guaranteeing stable responses.
  </action>
  <acceptance_criteria>
    - Hosted mode returns explicit guard outcome
    - Local mode remains functional for writes
  </acceptance_criteria>
</task>

<task id="3" name="Add targeted approve write-path tests">
  <read_first>
    - test/write-mir.test.js
    - test/onboarding-server.test.js
  </read_first>
  <action>
    Add tests validating successful file writes and crash-free approve responses under expected payloads.
  </action>
  <acceptance_criteria>
    - Test fails before fix, passes after fix
    - Assertions include output path behavior
  </acceptance_criteria>
</task>

## Verification

```bash
node --test test/write-mir.test.js
node --test test/onboarding-server.test.js
```
