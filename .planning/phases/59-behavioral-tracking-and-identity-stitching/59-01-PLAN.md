---
phase: 59-behavioral-tracking-and-identity-stitching
plan: 01
type: execute
wave: 1
depends_on:
  - 58-crm-canonical-schema-and-identity-graph
  - 51-multi-tenant-foundation-and-authorization
  - 57-observability-and-incident-closure
files_modified:
  - api/tracking/ingest.js
  - onboarding/onboarding.js
  - onboarding/index.html
  - onboarding/backend/agents/telemetry.cjs
  - onboarding/backend/runtime-context.cjs
  - lib/markos/telemetry/events.ts
  - test/tracking/tracking-proxy-ingest.test.js
  - test/tracking/tracking-browser-contract.test.js
  - test/tenant-auth/tracking-tenant-guard.test.js
autonomous: true
requirements:
  - TRK-01
  - TRK-03

must_haves:
  truths:
    - "Phase 59 moves CRM-feeding browser telemetry behind a MarkOS-owned first-party ingestion path instead of leaving onboarding or later surfaces pointed directly at hosted PostHog."
    - "Browser and authenticated event capture are normalized into MarkOS-owned CRM-readable activity semantics rather than treated as raw PostHog truth."
    - "Telemetry ingestion inherits tenant, privacy, redaction, and fail-closed runtime posture from the existing runtime-context seams."
  artifacts:
    - path: "api/tracking/ingest.js"
      provides: "Tenant-safe first-party event ingestion boundary for CRM-feeding browser and app events with normalization hooks and explicit failure semantics."
      exports: []
    - path: "onboarding/onboarding.js"
      provides: "Migrated browser tracking bootstrap that targets the MarkOS-owned ingestion endpoint rather than direct hosted PostHog capture."
      exports: []
    - path: "onboarding/backend/agents/telemetry.cjs"
      provides: "Server-side telemetry capture seam extended for proxy and normalized tracking flows without bypassing redaction or provider attempt logic."
      exports:
        - "capture"
        - "captureExecutionCheckpoint"
    - path: "lib/markos/telemetry/events.ts"
      provides: "Telemetry envelope and payload-sanitization contract reused by the first-party tracking path."
      exports:
        - "sanitizePayload"
        - "buildEvent"
  key_links:
    - from: "api/tracking/ingest.js"
      to: "onboarding/backend/runtime-context.cjs"
      via: "tenant-safe auth, telemetry preference, and redaction enforcement"
      pattern: "telemetry_write|redactSensitive|requireHostedSupabaseAuth"
    - from: "onboarding/onboarding.js"
      to: "api/tracking/ingest.js"
      via: "browser capture is redirected to the MarkOS-controlled ingestion path"
      pattern: "posthog.init|api_host|capture"
    - from: "api/tracking/ingest.js"
      to: "lib/markos/crm/timeline.ts"
      via: "normalized event families remain compatible with the MarkOS-owned timeline contract"
      pattern: "web_activity|campaign_touch|source_event_ref"
---

<objective>
Establish the first-party proxy ingestion and browser migration foundation for Phase 59.

Purpose: satisfy TRK-01 and the capture portion of TRK-03 by moving CRM-feeding telemetry behind a MarkOS-owned path with tenant-safe policy and normalized activity semantics.
Output: first-party ingestion route, onboarding/browser migration, runtime redaction and auth integration, and failing-to-passing tests for proxy capture and tenant-guard behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/59-behavioral-tracking-and-identity-stitching/59-CONTEXT.md
@.planning/phases/59-behavioral-tracking-and-identity-stitching/59-RESEARCH.md
@.planning/phases/58-crm-canonical-schema-and-identity-graph/58-02-PLAN.md
@onboarding/onboarding.js
@onboarding/backend/agents/telemetry.cjs
@onboarding/backend/runtime-context.cjs
@lib/markos/telemetry/events.ts
@api/status.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 59-01-01: Create failing proxy-ingestion and tenant-guard test seams</name>
  <read_first>
    - .planning/phases/59-behavioral-tracking-and-identity-stitching/59-CONTEXT.md
    - .planning/phases/59-behavioral-tracking-and-identity-stitching/59-RESEARCH.md
    - test/tenant-auth/tenant-context-propagation.test.js
  </read_first>
  <files>test/tracking/tracking-proxy-ingest.test.js, test/tracking/tracking-browser-contract.test.js, test/tenant-auth/tracking-tenant-guard.test.js</files>
  <acceptance_criteria>
    - Tests define the required MarkOS-owned ingestion endpoint and reject direct hosted-PostHog-only browser behavior as the end state.
    - Negative-path coverage proves missing tenant or disallowed protected-surface context fails closed where required.
    - Browser contract tests name page-view, key interaction, and form lifecycle capture requirements explicitly.
  </acceptance_criteria>
  <action>
Create the failing Phase 59 Wave 1 tests before touching implementation. Reuse the strongest tenant-auth and telemetry contract patterns already present in the repo, but encode the Phase 59 boundaries directly: first-party proxying, CRM-feeding event normalization, privacy-aware capture, and no fallback to vendor-shaped truth.
  </action>
  <verify>
    <automated>node --test test/tracking/tracking-proxy-ingest.test.js test/tracking/tracking-browser-contract.test.js test/tenant-auth/tracking-tenant-guard.test.js</automated>
  </verify>
  <done>
Wave 1 starts from an explicit failing regression boundary for proxy capture and runtime policy enforcement.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 59-01-02: Implement the first-party tracking ingestion route and runtime enforcement</name>
  <read_first>
    - onboarding/backend/runtime-context.cjs
    - onboarding/backend/agents/telemetry.cjs
    - api/status.js
  </read_first>
  <files>api/tracking/ingest.js, onboarding/backend/agents/telemetry.cjs, onboarding/backend/runtime-context.cjs, lib/markos/telemetry/events.ts, test/tracking/tracking-proxy-ingest.test.js, test/tenant-auth/tracking-tenant-guard.test.js</files>
  <acceptance_criteria>
    - A MarkOS-owned ingestion boundary exists for CRM-feeding first-party tracking traffic.
    - Payloads are sanitized through existing redaction rules before telemetry or CRM normalization logic observes them.
    - Protected-surface tracking requests inherit the existing fail-closed tenant and auth posture.
    - The implementation does not weaken current telemetry preference handling or runtime-mode checks.
  </acceptance_criteria>
  <action>
Add the first-party ingestion handler as an additive boundary that can accept browser and app tracking events, sanitize them, and prepare them for CRM normalization. Keep the route narrowly scoped to Phase 59's CRM-feeding telemetry path and avoid inventing a generic event bus.
  </action>
  <verify>
    <automated>node --test test/tracking/tracking-proxy-ingest.test.js test/tenant-auth/tracking-tenant-guard.test.js --test-name-pattern="proxy|ingest|tenant|deny|redact|telemetry"</automated>
  </verify>
  <done>
The repo now has a first-party ingestion seam that can replace direct hosted capture and preserve existing runtime safeguards.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 59-01-03: Migrate onboarding/browser capture to the MarkOS-owned path with explicit event coverage</name>
  <read_first>
    - onboarding/onboarding.js
    - onboarding/index.html
    - .planning/phases/59-behavioral-tracking-and-identity-stitching/59-RESEARCH.md
  </read_first>
  <files>onboarding/onboarding.js, onboarding/index.html, test/tracking/tracking-browser-contract.test.js</files>
  <acceptance_criteria>
    - Browser bootstrap no longer treats direct hosted PostHog capture as the target end-state for CRM-feeding surfaces.
    - Page views, key interactions, and form-start/form-submit signals remain explicitly captured through the new path.
    - Privacy notice behavior and telemetry preference semantics remain intact.
  </acceptance_criteria>
  <action>
Update onboarding browser tracking to point at the new MarkOS-owned path and make the supported event coverage explicit. Keep the scope limited to the Phase 59 capture migration without introducing UI churn or expanding into redirect, attribution, or identity stitching logic that belongs in later waves.
  </action>
  <verify>
    <automated>node --test test/tracking/tracking-browser-contract.test.js --test-name-pattern="onboarding|browser|page|interaction|form|proxy"</automated>
  </verify>
  <done>
Wave 1 ends with a real first-party browser capture path and explicit coverage for the core CRM-feeding interaction events.
  </done>
</task>

</tasks>

<verification>
Run the proxy-ingestion, browser contract, and tenant-guard suites after each task; Wave 1 is incomplete until direct hosted capture is replaced by a MarkOS-owned path with preserved privacy and auth posture.
</verification>

<success_criteria>
Phase 59 has a first-party ingestion foundation that captures CRM-feeding browser and app events through a MarkOS-controlled path with explicit redaction, auth, and normalized event coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/59-behavioral-tracking-and-identity-stitching/59-01-SUMMARY.md`
</output>
