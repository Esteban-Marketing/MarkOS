---
phase: 213
plan: "03"
type: execute
wave: 3
depends_on:
  - "213-01"
  - "213-02"
f_ids: []
migration_slots: []
integrates_with:
  - "PRC-01..09 and BILL-02 from P205 (P205 owns the PricingRecommendation path; P213 audits public release posture)"
  - "COMP-01 from P206 (public pricing language still has a compliance and evidence-freshness boundary)"
  - "EVD-01..06 from P209 (pricing claims are public claims and need evidence linkage)"
translation_gate_for: []
files_modified:
  - .planning/tenant-zero/pricing/public-pricing-audit.md
  - .planning/tenant-zero/pricing/public-pricing-release-gate.json
  - .planning/tenant-zero/pricing/recommendation-linkage.md
  - scripts/tenant-zero/audit-public-pricing.mjs
  - scripts/tenant-zero/assert-pricing-linkage.mjs
  - test/tenant-zero/phase-213/domain-3/pricing-placeholder-regression.test.js
  - test/tenant-zero/phase-213/domain-3/recommendation-linkage.test.js
  - .planning/phases/213-tenant0-dogfood-compliance-validation/213-VALIDATION.md
autonomous: true
requirements:
  - T0-03
  - QA-09
  - QA-10
  - QA-11
must_haves:
  truths:
    - "No public pricing-sensitive surface may bypass an approved PricingRecommendation or the exact sentinel `{{MARKOS_PRICING_ENGINE_PENDING}}`."
    - "Every audited pricing surface is classified as `placeholder_only`, `release_ready`, or `blocked`."
    - "Phase 205 Tenant 0 pricing dogfood output is consumed as input; P213 does not create a parallel pricing system."
    - "Historical assumptions remain labeled historical instead of being promoted to live public pricing."
    - "Every `blocked` classification names exactly one of the four canonical `release_reason` literals: `pricing_engine_not_yet_activated`, `recommendation_approval_missing`, `evidence_freshness_expired`, or `compliance_posture_insufficient` (per 213-UI-SPEC.md §Public Pricing Release-Gate Copy Contract)."
    - "The five audited public surfaces are SCAN TARGETS only — they are never modified by 213-03 (per 213-UI-SPEC.md §Scope Verification disambiguation note)."
  artifacts:
    - path: .planning/tenant-zero/pricing/public-pricing-audit.md
      provides: per-surface audit result and rationale for keeping placeholder, releasing, or blocking (consumed read-only by future public-pricing release-gate report viewer per 213-UI-SPEC.md §Downstream UI Inheritance Map)
    - path: .planning/tenant-zero/pricing/public-pricing-release-gate.json
      provides: machine-readable gate with recommendation linkage, approval linkage, and freshness status (consumed read-only by future P205 follow-up admin or future enterprise admin per 213-UI-SPEC.md §Downstream UI Inheritance Map; the four `release_reason` literals are the read-only blocking-reason copy contract for future surfaces)
    - path: scripts/tenant-zero/audit-public-pricing.mjs
      provides: deterministic scan of public pricing-sensitive surfaces (the five audit-target paths are scan targets ONLY, never modified)
  key_links:
    - from: .planning/phases/205-pricing-engine-foundation-billing-readiness/205-08-PLAN.md
      to: .planning/tenant-zero/pricing/public-pricing-release-gate.json
      via: P205's activated recommendation is the only acceptable upstream pricing input for release-ready classification
      pattern: "PricingRecommendation"
    - from: lib/markos/cli/runs.cjs
      to: scripts/tenant-zero/assert-pricing-linkage.mjs
      via: run payloads already carry the placeholder sentinel and should align with surface-level release posture
      pattern: "PRICING_PLACEHOLDER_SENTINEL"
---

<ui_spec_fold>
  <ui_scope>NO_UI</ui_scope>
  <ui_spec_block>
    213-UI-SPEC.md §Public Pricing Release-Gate Copy Contract — load-bearing forward contract for every future UI surface that consumes the public pricing release-gate. Authors:
    - **Sentinel-discipline rules:** the verbatim sentinel `{{MARKOS_PRICING_ENGINE_PENDING}}` must appear on every `placeholder_only` surface; never localized, never paraphrased, never abbreviated, never replaced with marketing aliases like "TBD pricing", "Coming soon", "Contact sales". Removal requires `release_ready` classification + linked `recommendation_id` + `approval_ref` AS A HARD GATE enforced by `scripts/tenant-zero/assert-pricing-linkage.mjs`.
    - **Four canonical `release_reason` literals** (verbatim, never extended without simultaneously updating `public-pricing-audit.md` AND 213-UI-SPEC.md):
      1. `pricing_engine_not_yet_activated` — UI display copy: `[block] Pricing Engine not yet activated — surface remains sentinel-protected`; recipe: `.c-notice c-notice--warning`
      2. `recommendation_approval_missing` — UI display copy: `[block] PricingRecommendation approval missing — release gate cannot pass`; recipe: `.c-notice c-notice--error`
      3. `evidence_freshness_expired` — UI display copy: `[block] Pricing evidence freshness expired — re-audit required before release`; recipe: `.c-notice c-notice--warning`
      4. `compliance_posture_insufficient` — UI display copy: `[block] Phase 206 compliance posture insufficient for this claim — block until SOC 2 Type 1 verified`; recipe: `.c-notice c-notice--error`
    - **Cross-binding:** 205 PricingRecommendation as `recommendation_id` source-of-truth; 206 SOC 2 Type 1 evidence pack as `compliance_posture_insufficient` source-of-truth; 209 `EvidenceMap` freshness as `evidence_freshness_expired` source-of-truth; 213-03 `public-pricing-release-gate.json` as the per-surface gate-state-of-truth.
    213-UI-SPEC.md §Downstream UI Inheritance Map row "Public-pricing release-gate report viewer" — future P205 follow-up admin or future enterprise admin reads this gate; renders three `classification` literals (`placeholder_only`, `release_ready`, `blocked`) verbatim as `.c-badge--{warning,success,error}`. The five audited public surfaces render as a vanilla `<table>` (D-14 recipe). `release_ready` rows compose `.c-chip-protocol` (mint-as-text per D-09) for `recommendation_id` deep-links. `blocked` rows compose `.c-notice c-notice--error` with the named `release_reason` copy.
  </ui_spec_block>
  <cross_cutting_carry_forward>
    - **Sentinel-discipline (`{{MARKOS_PRICING_ENGINE_PENDING}}`):** Plan 03 is the central audit signal of this sentinel; the literal sentinel string must appear verbatim in `public-pricing-audit.md`, in `public-pricing-release-gate.json` as the `placeholder_only` classification's expected sentinel value, and in `test/tenant-zero/phase-213/domain-3/pricing-placeholder-regression.test.js`. The sentinel never renders into a UI surface in this phase (it is rendered VERBATIM by future surfaces in `placeholder_only` row detail per 213-UI-SPEC.md §Downstream UI Inheritance Map; never paraphrased, never localized).
    - **Four canonical `release_reason` literals verbatim** (per 213-UI-SPEC.md §Public Pricing Release-Gate Copy Contract): every `blocked` row in `public-pricing-release-gate.json` MUST set `release_reason` to exactly one of `pricing_engine_not_yet_activated`, `recommendation_approval_missing`, `evidence_freshness_expired`, or `compliance_posture_insufficient`. New literals cannot be silently introduced — adding one requires simultaneous updates to 213-UI-SPEC.md §Public Pricing Release-Gate Copy Contract AND `public-pricing-audit.md`. The four literals must be present verbatim in `audit-public-pricing.mjs` source.
    - **Five audit-target paths as SCAN TARGETS ONLY** (per 213-UI-SPEC.md §Scope Verification disambiguation note): `app/(marketing)/integrations/claude/page.tsx`, `app/(marketing)/docs/[[...slug]]/page.tsx`, `app/docs/llms-full.txt/route.ts`, `public/llms.txt`, `docs/pricing/public-tier-placeholder.md`. These paths appear in `<read_first>` and as scan-target string literals embedded in `scripts/tenant-zero/audit-public-pricing.mjs`. They are NEVER in `files_modified`. Surface-content edits to those public surfaces are deferred to P205 follow-up (when an approved PricingRecommendation lands) or P217+ marketing-site phases.
    - **Pricing-classification vocabulary verbatim** (per 213-UI-SPEC.md §Copywriting Contract): the three literals `placeholder_only`, `release_ready`, `blocked` MUST be used verbatim. No synonyms like `pending`, `approved`, `rejected`.
    - **D-08 (token-only) + D-09 (mint-as-text):** Future viewers map `placeholder_only`/`release_ready`/`blocked` to `.c-badge--{warning,success,error}` and `recommendation_id` to `.c-chip-protocol` (mint-as-text). 213-03 emits gate JSON only; rendering is downstream.
    - **D-14 (no `.c-table` primitive):** Future public-pricing release-gate report viewer uses vanilla `<table>` semantic + token-only recipe per 213-UI-SPEC.md §Downstream UI Inheritance Map.
  </cross_cutting_carry_forward>
  <downstream_ui_consumers_read_only>
    - Future P205 follow-up admin (when approved PricingRecommendation lands and surfaces flip from `placeholder_only` to `release_ready`) — reads `public-pricing-release-gate.json`. NOT modified by 213-03.
    - Future enterprise admin or P217+ marketing-site phase — reads `public-pricing-audit.md` + `recommendation-linkage.md` for governance dashboards. NOT modified by 213-03.
    - Future case-study editor (P217+) — reads `public-pricing-release-gate.json` to enforce pricing-claim publish gate inheritance. NOT modified by 213-03.
    - **Critical:** Plan 03 emits gate JSON + audit doctrine + CI scripts only. The five audited public surfaces are SCAN TARGETS, NEVER modified. Any future addition of a `release_reason` literal requires simultaneous updates to 213-UI-SPEC.md §Public Pricing Release-Gate Copy Contract AND `public-pricing-audit.md` per the explicit forward-contract rule.
  </downstream_ui_consumers_read_only>
</ui_spec_fold>

<objective>
Audit and govern MarkOS's own pricing posture so every public pricing-sensitive surface is either sentinel-protected, explicitly release-ready from Phase 205 output, or blocked with a named reason.
</objective>

<context>
@.planning/phases/213-tenant0-dogfood-compliance-validation/213-RESEARCH.md
@.planning/phases/205-pricing-engine-foundation-billing-readiness/205-08-PLAN.md
@.planning/phases/213-tenant0-dogfood-compliance-validation/213-VALIDATION.md
@.planning/phases/213-tenant0-dogfood-compliance-validation/213-UI-SPEC.md
@.planning/phases/206-soc2-type1-foundation/206-UI-SPEC.md
@.planning/phases/209-evidence-research-and-claim-safety/209-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>
<task id="213-03-01">
  <name>Audit public pricing posture and classify every public surface</name>
  <files>.planning/tenant-zero/pricing/public-pricing-audit.md, .planning/tenant-zero/pricing/public-pricing-release-gate.json, .planning/tenant-zero/pricing/recommendation-linkage.md, scripts/tenant-zero/audit-public-pricing.mjs, scripts/tenant-zero/assert-pricing-linkage.mjs, test/tenant-zero/phase-213/domain-3/pricing-placeholder-regression.test.js, test/tenant-zero/phase-213/domain-3/recommendation-linkage.test.js</files>
  <read_first>
    - .planning/phases/213-tenant0-dogfood-compliance-validation/213-RESEARCH.md section "Domain 3 - Pricing Engine posture and public pricing gate"
    - .planning/phases/213-tenant0-dogfood-compliance-validation/213-UI-SPEC.md §Public Pricing Release-Gate Copy Contract (sentinel-discipline + 4 release_reason literals + cross-binding) + §Downstream UI Inheritance Map row "Public-pricing release-gate report viewer"
    - .planning/phases/205-pricing-engine-foundation-billing-readiness/205-08-PLAN.md
    - .planning/phases/206-soc2-type1-foundation/206-UI-SPEC.md (compliance-posture source-of-truth for `compliance_posture_insufficient` literal)
    - .planning/phases/209-evidence-research-and-claim-safety/209-UI-SPEC.md (evidence-freshness source-of-truth for `evidence_freshness_expired` literal)
    - **Audit-target paths (SCAN TARGETS ONLY — never modified):** docs/pricing/public-tier-placeholder.md, public/llms.txt, app/(marketing)/integrations/claude/page.tsx, app/(marketing)/docs/[[...slug]]/page.tsx, app/docs/llms-full.txt/route.ts
  </read_first>
  <action>
    Step 1: Create `.planning/tenant-zero/pricing/public-pricing-audit.md` and classify each audited surface as exactly one of `placeholder_only`, `release_ready`, or `blocked` (verbatim literals per 213-UI-SPEC.md §Copywriting Contract pricing-classification vocabulary). Doctrine prose contains zero CLAUDE.md banned-lexicon terms.
    Step 2: Create `.planning/tenant-zero/pricing/public-pricing-release-gate.json` with per-surface keys `surface`, `classification`, `recommendation_id`, `approval_ref`, `evidence_fresh`, and `release_reason`. Every `blocked` row's `release_reason` field MUST set exactly one of the four canonical literals verbatim: `pricing_engine_not_yet_activated`, `recommendation_approval_missing`, `evidence_freshness_expired`, or `compliance_posture_insufficient` (per 213-UI-SPEC.md §Public Pricing Release-Gate Copy Contract blocking-reason copy contract). Every `placeholder_only` row MUST contain the verbatim sentinel `{{MARKOS_PRICING_ENGINE_PENDING}}` as the expected sentinel value.
    Step 3: Implement `scripts/tenant-zero/audit-public-pricing.mjs` so it scans these five EXACT audit-target paths verbatim (SCAN TARGETS ONLY — never written to): `docs/pricing/public-tier-placeholder.md`, `public/llms.txt`, `app/(marketing)/docs/[[...slug]]/page.tsx`, `app/(marketing)/integrations/claude/page.tsx`, and `app/docs/llms-full.txt/route.ts`. The script emits gate JSON envelopes; CLI output uses bracketed-glyph severity prefixes (`[ok]`, `[warn]`, `[err]`, `[block]`, `[info]`) per DESIGN.md "CLI output" rule, never emoji. The script source MUST contain all four `release_reason` literals (`pricing_engine_not_yet_activated`, `recommendation_approval_missing`, `evidence_freshness_expired`, `compliance_posture_insufficient`) verbatim.
    Step 4: Implement `scripts/tenant-zero/assert-pricing-linkage.mjs` and the domain-3 tests so removal of `{{MARKOS_PRICING_ENGINE_PENDING}}` from any audited public surface fails the CI run unless the gate JSON marks that surface `release_ready` with non-empty `recommendation_id` AND `approval_ref` AND `evidence_fresh: true` (the hard-gate sentinel-discipline rule per 213-UI-SPEC.md §Public Pricing Release-Gate Copy Contract).
  </action>
  <acceptance_criteria>
    - `.planning/tenant-zero/pricing/public-pricing-audit.md` contains the exact classifications `placeholder_only`, `release_ready`, and `blocked` verbatim
    - `.planning/tenant-zero/pricing/public-pricing-release-gate.json` contains the exact keys `"surface"`, `"classification"`, `"recommendation_id"`, `"approval_ref"`, `"evidence_fresh"`, and `"release_reason"`
    - `scripts/tenant-zero/audit-public-pricing.mjs` contains the exact path strings `docs/pricing/public-tier-placeholder.md`, `public/llms.txt`, `app/(marketing)/integrations/claude/page.tsx`, `app/(marketing)/docs/[[...slug]]/page.tsx`, AND `app/docs/llms-full.txt/route.ts` verbatim (all five SCAN TARGET paths)
    - `scripts/tenant-zero/audit-public-pricing.mjs` contains the four canonical `release_reason` literals verbatim: `pricing_engine_not_yet_activated`, `recommendation_approval_missing`, `evidence_freshness_expired`, `compliance_posture_insufficient` (per 213-UI-SPEC.md §Public Pricing Release-Gate Copy Contract)
    - `test/tenant-zero/phase-213/domain-3/pricing-placeholder-regression.test.js` contains the exact sentinel `{{MARKOS_PRICING_ENGINE_PENDING}}`
    - `files_modified` block contains zero entries from the five audit-target paths (SCAN TARGETS, never modified by 213-03 per 213-UI-SPEC.md §Scope Verification)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/tenant-zero/phase-213/domain-3/</automated>
  </verify>
  <done>Pricing-sensitive public surfaces are governed by a real release gate instead of drifting between placeholder doctrine and ad hoc copy changes.</done>
</task>
</tasks>

<verification>
- npm test -- test/tenant-zero/phase-213/domain-3/
</verification>

<success_criteria>
- Every public pricing-sensitive surface has a gate classification
- The exact placeholder sentinel remains wherever release-ready linkage does not exist
- Recommendation, approval, and evidence freshness are recorded for release-ready surfaces
- Historical pricing assumptions are not mistaken for live public posture
- Every `blocked` row names exactly one of the four canonical `release_reason` literals verbatim per 213-UI-SPEC.md §Public Pricing Release-Gate Copy Contract
- The five audit-target paths are SCAN TARGETS ONLY — never modified by 213-03
</success_criteria>
</content>
</invoke>