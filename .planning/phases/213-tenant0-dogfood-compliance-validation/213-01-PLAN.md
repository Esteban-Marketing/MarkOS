---
phase: 213
plan: "01"
type: execute
wave: 1
depends_on: []
f_ids: []
migration_slots: []
integrates_with:
  - "PRC-01..09 and BILL-02 from P205 (Tenant 0 pricing dogfood and placeholder doctrine are upstream inputs, not re-owned here)"
  - "COMP-01 from P206 (public compliance wording must consume the SOC2 evidence posture)"
  - "RUN-01..08 from P207, TASK-01..05 from P208, EVD-01..06 from P209, CONN-01..06 from P210, LOOP-01..08 from P211, and LRN-01..05 from P212 (P213 is the integration and closeout gate)"
translation_gate_for: []
files_modified:
  - .planning/phases/213-tenant0-dogfood-compliance-validation/213-VALIDATION.md
  - .planning/tenant-zero/213-upstream-readiness.md
  - .planning/tenant-zero/workspace-profile.json
  - .planning/tenant-zero/connector-inventory.json
  - .planning/tenant-zero/data-source-policy.md
  - .planning/tenant-zero/public-proof-allowlist.md
  - scripts/tenant-zero/check-upstream-readiness.mjs
  - scripts/tenant-zero/check-architecture-lock.mjs
  - scripts/tenant-zero/assert-tenant-workspace-ready.mjs
  - test/tenant-zero/phase-213/preflight/upstream-readiness.test.js
  - test/tenant-zero/phase-213/preflight/architecture-lock.test.js
  - test/tenant-zero/phase-213/preflight/workspace-baseline.test.js
  - test/tenant-zero/phase-213/preflight/public-private-policy.test.js
autonomous: true
requirements:
  - T0-01
  - T0-02
  - QA-01
  - QA-02
  - QA-03
  - QA-04
must_haves:
  truths:
    - "Wave 0.5 preflight hard-fails when any required P205-P212 closeout input is missing, stale, or still only stubbed."
    - "Tenant 0 workspace artifacts define `tenant_slug: tenant-zero`, `org_slug: markos-internal`, `business_model: saas`, named owner/approver/operator roles, connector state, and public/private data boundaries."
    - "Missing or degraded connectors never create fake proof; they produce `dispatch_status: ready_to_publish` or a named recovery blocker."
    - "Validation strategy is updated with pending rows for every Plan 01 task before execution continues."
    - "Architecture-lock CI rejects the three forbidden strings `synthetic proof`, `fake case study`, `mock customer logo` verbatim across all Phase 213 surfaces, and `.planning/tenant-zero/*.md` doctrine prose contains zero CLAUDE.md banned-lexicon terms."
  artifacts:
    - path: .planning/tenant-zero/213-upstream-readiness.md
      provides: hard and soft readiness checklist for P205-P212 plus blocker ownership
    - path: .planning/tenant-zero/workspace-profile.json
      provides: canonical Tenant 0 org, tenant, roles, and evidence posture used by later plans (consumed read-only by future Tenant 0 readiness dashboards per 213-UI-SPEC.md §Downstream UI Inheritance Map)
    - path: .planning/tenant-zero/connector-inventory.json
      provides: per-connector health envelope (consumed read-only by future connector inventory + data-source policy viewer per 213-UI-SPEC.md §Downstream UI Inheritance Map)
    - path: scripts/tenant-zero/check-upstream-readiness.mjs
      provides: deterministic closeout preflight for Pricing Engine, compliance, AgentRun, tasks, evidence, connectors, loop, and learning
  key_links:
    - from: onboarding/onboarding-seed.schema.json
      to: .planning/tenant-zero/workspace-profile.json
      via: `company.business_model` and brand inputs seed the canonical Tenant 0 workspace profile
      pattern: "business_model"
    - from: bin/lib/cli/doctor-checks.cjs
      to: scripts/tenant-zero/check-architecture-lock.mjs
      via: placeholder doctrine becomes a closeout assertion instead of an ad hoc manual check
      pattern: "pricing_placeholder_policy"
---

<ui_spec_fold>
  <ui_scope>NO_UI</ui_scope>
  <ui_spec_block>
    213-UI-SPEC.md §parent_doctrine_chain (frontmatter) — establishes 206/207/208/209/210/211/212 as the seven-parent inheritance chain that this plan's workspace + readiness substrate consumes via foreign-key references rather than re-owning.
    213-UI-SPEC.md §Downstream UI Inheritance Map row "Tenant 0 readiness dashboard (single-pane health)" — future P217+ readiness dashboard reads `workspace-profile.json` (`org_slug`, `tenant_slug`, `business_model`, `roles`, `pricing_mode`, `connector_policy`) verbatim; binds `<PlaceholderBanner variant="future_phase_214_saas_suite">` if any P214 readiness gate is `yellow`/`red`.
    213-UI-SPEC.md §Downstream UI Inheritance Map row "Connector inventory + data-source policy viewer" — future enterprise admin or P210 connector recovery extension reads `connector-inventory.json` per-connector envelope (`connector_slug`, `status`, `required_for_loop`, `fallback_action`, `last_verified_at`) verbatim; the three `status` literals (`healthy`, `degraded`, `unavailable`) map to `.c-badge--{success,warning,error}` per D-08 token-only and bracketed-glyph pairing per DESIGN.md "Color blindness" rule.
  </ui_spec_block>
  <cross_cutting_carry_forward>
    - **D-08 (token-only):** Plan 01 surfaces ZERO inline hex literals in any doctrine markdown or JSON envelope; the `connector-inventory.json` `status` field uses the three string literals (`healthy`, `degraded`, `unavailable`) only — no embedded color values, no embedded class names. Future surfaces map literals to tokens at the rendering layer.
    - **D-09 (mint-as-text):** `workspace-profile.json` `tenant_slug` and `org_slug` will eventually surface as `.c-chip-protocol` (mint-as-text) deep-links in future readiness dashboards; Plan 01 emits the slugs as plain JSON strings only.
    - **D-09b (`.c-notice` mandatory):** Plan 01 doctrine prose authors no `.banner`/`.alert`/`.warning`/`.noticeBar` content; CI script CLI output uses bracketed-glyph severity prefixes (`[ok]`, `[warn]`, `[err]`, `[block]`, `[info]`) per DESIGN.md "CLI output" rule. Future readiness-dashboard `c-notice c-notice--{info,warning,success,error}` mapping deferred to the consuming phase.
    - **D-14 (no `.c-table` primitive):** `213-upstream-readiness.md` is a GitHub-flavored Markdown table (one row per phase 205-212), parsed by CI scripts and rendered by Markdown viewers, never as a React table. Future phase that browses upstream readiness uses vanilla `<table>` + token-only recipe per D-14.
    - **Architecture-lock forbidden strings** (per 213-UI-SPEC.md §Copywriting Contract): `synthetic proof`, `fake case study`, `mock customer logo` — rejected by `scripts/tenant-zero/check-architecture-lock.mjs` across all 213 surfaces (doctrine markdown + CI scripts + test fixtures + JSON envelopes).
    - **Banned-lexicon zero-match** (per CLAUDE.md "Banned lexicon" + 213-UI-SPEC.md §Copywriting Contract): `synergy`, `leverage`, `empower`, `unlock`, `transform`, `revolutionize`, `supercharge`, `holistic`, `seamless`, `cutting-edge`, `innovative`, `game-changer`, `next-generation`, `world-class`, `best-in-class`, `reimagine`, `disrupt`, `just` (as softener), and exclamation points are forbidden in `.planning/tenant-zero/*.md` doctrine prose. Plan 01 owns the first wave of doctrine docs (`213-upstream-readiness.md`, `data-source-policy.md`, `public-proof-allowlist.md`).
  </cross_cutting_carry_forward>
  <downstream_ui_consumers_read_only>
    - Future P217+ Tenant 0 readiness dashboard (single-pane health) — reads `workspace-profile.json` + `connector-inventory.json` + `data-source-policy.md` + `public-proof-allowlist.md` + `213-upstream-readiness.md`. NOT modified by 213-01.
    - Future P210 connector recovery extension — reads `connector-inventory.json` `status` field for degraded-connector recovery routing. NOT modified by 213-01.
    - Future enterprise admin connector inventory + data-source policy viewer — reads `connector-inventory.json` + `data-source-policy.md` three sections (`## Public Evidence`, `## Private Internal`, `## Never Exported`); never-exported sources hard-block on export CTA. NOT modified by 213-01.
    - **Critical:** Plan 01 emits doctrine + JSON contracts only. The downstream UI consumers above will be authored in their own phases with their own UI-SPEC.md citations back to 213-UI-SPEC.md §Downstream UI Inheritance Map. Plan 01 does NOT modify any P207, P208, P209, P210, P211, or P212 file; existing operator cockpit + connector substrate are CONSUMED in production by 213-02, NOT modified by 213-01.
  </downstream_ui_consumers_read_only>
</ui_spec_fold>

<objective>
Establish the closeout foundation for Tenant 0: an upstream readiness gate across Phases 205-212, an architecture lock against fake-proof and nonstandard test surfaces, and a single canonical Tenant 0 workspace profile with connector and data-source policy.
</objective>

<context>
@.planning/phases/213-tenant0-dogfood-compliance-validation/213-CONTEXT.md
@.planning/phases/213-tenant0-dogfood-compliance-validation/213-RESEARCH.md
@.planning/phases/213-tenant0-dogfood-compliance-validation/213-REVIEWS.md
@.planning/phases/213-tenant0-dogfood-compliance-validation/213-VALIDATION.md
@.planning/phases/213-tenant0-dogfood-compliance-validation/213-UI-SPEC.md
@.planning/phases/206-soc2-type1-foundation/206-UI-SPEC.md
@.planning/phases/207-agentrun-v2-orchestration-substrate/207-UI-SPEC.md
@.planning/phases/208-human-operating-interface/208-UI-SPEC.md
@.planning/phases/209-evidence-research-and-claim-safety/209-UI-SPEC.md
@.planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md
@.planning/phases/211-content-social-revenue-loop/211-UI-SPEC.md
@.planning/phases/212-learning-literacy-evolution/212-UI-SPEC.md
@DESIGN.md
@CLAUDE.md
</context>

<tasks>
<task id="213-01-00">
  <name>Wave 0.5 upstream preflight, architecture lock, and validation baseline</name>
  <files>.planning/tenant-zero/213-upstream-readiness.md, scripts/tenant-zero/check-upstream-readiness.mjs, scripts/tenant-zero/check-architecture-lock.mjs, test/tenant-zero/phase-213/preflight/upstream-readiness.test.js, test/tenant-zero/phase-213/preflight/architecture-lock.test.js, .planning/phases/213-tenant0-dogfood-compliance-validation/213-VALIDATION.md</files>
  <read_first>
    - .planning/phases/213-tenant0-dogfood-compliance-validation/213-RESEARCH.md section "Domain 0 - Upstream readiness and architecture lock"
    - .planning/phases/213-tenant0-dogfood-compliance-validation/213-REVIEWS.md sections HIGH and MEDIUM concerns
    - .planning/phases/213-tenant0-dogfood-compliance-validation/213-UI-SPEC.md sections §parent_doctrine_chain + §Copywriting Contract (architecture-lock forbidden strings) + §DESIGN.md Compliance Assertions (architecture-lock + banned-lexicon rows)
    - .planning/phases/205-pricing-engine-foundation-billing-readiness/205-08-PLAN.md
    - bin/lib/cli/doctor-checks.cjs
    - lib/markos/cli/runs.cjs
  </read_first>
  <action>
    Step 1: Create `.planning/tenant-zero/213-upstream-readiness.md` with one row per phase `205` through `212` and columns `phase`, `required_input`, `evidence_path`, `status`, `blocker_owner`, and `notes`. Doctrine prose must use engineering-grade short sentences and zero CLAUDE.md banned-lexicon terms (no `synergy`, `leverage`, `empower`, `unlock`, `transform`, `revolutionize`, `supercharge`, `holistic`, `seamless`, `cutting-edge`, `innovative`, `game-changer`, `next-generation`, `world-class`, `best-in-class`, `reimagine`, `disrupt`, `just`-as-softener; no exclamation points; no emoji).
    Step 2: Implement `scripts/tenant-zero/check-upstream-readiness.mjs` so it hard-fails when a required closeout path is missing and soft-warns when an artifact is present but stale. CLI output must use bracketed-glyph severity prefixes (`[ok]`, `[warn]`, `[err]`, `[block]`, `[info]`) per DESIGN.md "CLI output" rule, never emoji.
    Step 3: Implement `scripts/tenant-zero/check-architecture-lock.mjs` so it rejects the four GSD-architecture test-substrate strings `vitest`, `playwright`, `.test.ts`, `route.ts` AND the three Phase 213 forbidden-proof strings `synthetic proof`, `fake case study`, `mock customer logo` verbatim. The script also enforces banned-lexicon zero-match in `.planning/tenant-zero/*.md` against the CLAUDE.md banned-lexicon list (per 213-UI-SPEC.md §Copywriting Contract). All seven literal strings must be present verbatim in the script source.
    Step 4: Add preflight tests and update `213-VALIDATION.md` so Tasks `213-01-00` and `213-01-01` exist in the verification map before later plans begin.
  </action>
  <acceptance_criteria>
    - `.planning/tenant-zero/213-upstream-readiness.md` contains one row each for Phases `205`, `206`, `207`, `208`, `209`, `210`, `211`, and `212`
    - `scripts/tenant-zero/check-upstream-readiness.mjs` contains the exact phase keys `205`, `206`, `207`, `208`, `209`, `210`, `211`, and `212`
    - `scripts/tenant-zero/check-architecture-lock.mjs` contains the exact forbidden strings `synthetic proof`, `fake case study`, and `mock customer logo` verbatim (per 213-UI-SPEC.md §Copywriting Contract architecture-lock rule)
    - `scripts/tenant-zero/check-architecture-lock.mjs` enforces banned-lexicon zero-match across `.planning/tenant-zero/*.md` files (no `synergy`, `leverage`, `empower`, `unlock`, `transform`, `revolutionize`, `supercharge`, `holistic`, `seamless`, `cutting-edge`, `innovative`, `game-changer`, `next-generation`, `world-class`, `best-in-class`, `reimagine`, `disrupt`, `just`-as-softener; no exclamation points; no emoji)
    - `213-VALIDATION.md` contains rows `213-01-00` and `213-01-01`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/tenant-zero/phase-213/preflight/</automated>
  </verify>
  <done>Phase 213 now fails closed on missing upstream substrate instead of silently producing a misleading readiness result.</done>
</task>

<task id="213-01-01">
  <name>Tenant 0 workspace profile, connector inventory, and data policy</name>
  <files>.planning/tenant-zero/workspace-profile.json, .planning/tenant-zero/connector-inventory.json, .planning/tenant-zero/data-source-policy.md, .planning/tenant-zero/public-proof-allowlist.md, scripts/tenant-zero/assert-tenant-workspace-ready.mjs, test/tenant-zero/phase-213/preflight/workspace-baseline.test.js, test/tenant-zero/phase-213/preflight/public-private-policy.test.js</files>
  <read_first>
    - .planning/phases/213-tenant0-dogfood-compliance-validation/213-RESEARCH.md section "Domain 1 - Tenant 0 workspace and governed input pack"
    - .planning/phases/213-tenant0-dogfood-compliance-validation/213-UI-SPEC.md §Downstream UI Inheritance Map rows "Tenant 0 readiness dashboard" + "Connector inventory + data-source policy viewer"
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md (connector substrate — three `status` literals `healthy`, `degraded`, `unavailable` inherited verbatim)
    - onboarding/onboarding-seed.schema.json
    - onboarding/onboarding.js
  </read_first>
  <action>
    Step 1: Create `.planning/tenant-zero/workspace-profile.json` with `org_slug: "markos-internal"`, `tenant_slug: "tenant-zero"`, `business_model: "saas"`, `roles: ["owner", "approver", "operator"]`, `pricing_mode: "approved_recommendation_or_MARKOS_PRICING_ENGINE_PENDING"`, and `connector_policy: "publish_if_healthy_else_ready_to_publish"`. These exact field names are the read-only contract the future P217+ readiness dashboard binds to per 213-UI-SPEC.md §Downstream UI Inheritance Map.
    Step 2: Create `.planning/tenant-zero/connector-inventory.json` with per-connector fields `connector_slug`, `status`, `required_for_loop`, `fallback_action`, and `last_verified_at`. The three `status` literals `healthy`, `degraded`, `unavailable` inherit verbatim from 210-UI-SPEC.md and map (in future viewers) to `.c-badge--{success,warning,error}` per D-08 token-only.
    Step 3: Create `.planning/tenant-zero/data-source-policy.md` with the exact sections `## Public Evidence`, `## Private Internal`, and `## Never Exported`. These three section headings are the read-only contract the future enterprise admin viewer binds to (Never-Exported sources hard-block any export CTA per 213-UI-SPEC.md §Downstream UI Inheritance Map). Doctrine prose contains zero banned-lexicon terms.
    Step 4: Create `.planning/tenant-zero/public-proof-allowlist.md` and `scripts/tenant-zero/assert-tenant-workspace-ready.mjs` so missing connector health or unlabeled data sources fail the workspace check. The script enforces the exact `connector_policy` literal `publish_if_healthy_else_ready_to_publish` per 210-UI-SPEC.md degraded → `ready_to_publish` rule.
  </action>
  <acceptance_criteria>
    - `.planning/tenant-zero/workspace-profile.json` contains `"tenant_slug": "tenant-zero"`, `"org_slug": "markos-internal"`, and `"business_model": "saas"`
    - `.planning/tenant-zero/workspace-profile.json` contains the exact array `"roles": ["owner", "approver", "operator"]`
    - `.planning/tenant-zero/connector-inventory.json` contains the per-connector fields `"connector_slug"`, `"status"`, `"required_for_loop"`, `"fallback_action"`, and `"last_verified_at"` (read-only contract for future connector inventory viewer per 213-UI-SPEC.md §Downstream UI Inheritance Map)
    - `.planning/tenant-zero/data-source-policy.md` contains the exact headings `## Public Evidence`, `## Private Internal`, and `## Never Exported`
    - `.planning/tenant-zero/data-source-policy.md` doctrine prose contains zero CLAUDE.md banned-lexicon terms (architecture-lock CI gate per 213-01-00)
    - `scripts/tenant-zero/assert-tenant-workspace-ready.mjs` contains the exact string `publish_if_healthy_else_ready_to_publish`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/tenant-zero/phase-213/preflight/</automated>
  </verify>
  <done>Tenant 0 has a single authoritative workspace definition and a documented rule that degraded connectors create blockers rather than fake proof.</done>
</task>
</tasks>

<verification>
- npm test -- test/tenant-zero/phase-213/preflight/
</verification>

<success_criteria>
- Upstream readiness artifacts exist for P205-P212
- Fake-proof and nonstandard validation surfaces are blocked by architecture lock
- Tenant 0 workspace profile, connector inventory, and data policy exist with the exact canonical fields
- Validation strategy is seeded before later plans execute
- Architecture-lock CI rejects the three Phase 213 forbidden-proof strings + the four GSD-architecture test-substrate strings + zero-match banned-lexicon across `.planning/tenant-zero/*.md`
- Workspace + connector + data-source contracts are consumed read-only by future Tenant 0 readiness dashboards (213-UI-SPEC.md §Downstream UI Inheritance Map) without modification of any P207-P212 file
</success_criteria>
</content>
</invoke>