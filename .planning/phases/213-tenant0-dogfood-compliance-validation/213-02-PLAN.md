---
phase: 213
plan: "02"
type: execute
wave: 2
depends_on:
  - "213-01"
f_ids: []
migration_slots: []
integrates_with:
  - "RUN-01..08 from P207 (the loop must produce real AgentRun traces and side-effect posture)"
  - "TASK-01..05 from P208 (task and approval routing must be real, not narrated)"
  - "EVD-01..06 from P209 (every public claim in the loop needs evidence linkage)"
  - "CONN-01..06 from P210 (dispatch must respect real connector health)"
  - "LOOP-01..08 from P211 and LRN-01..05 from P212 (the loop must end in measurable outcome and learning handoff)"
translation_gate_for: []
files_modified:
  - .planning/tenant-zero/loop-01-brief.md
  - .planning/tenant-zero/loop-01-claim-map.json
  - .planning/tenant-zero/loop-01-approval-log.md
  - .planning/tenant-zero/loop-01-dispatch-log.json
  - .planning/tenant-zero/loop-01-outcome.md
  - .planning/tenant-zero/loop-01-learning-handoff.json
  - scripts/tenant-zero/run-first-loop.mjs
  - scripts/tenant-zero/verify-first-loop.mjs
  - test/tenant-zero/phase-213/domain-2/loop-artifact-chain.test.js
  - test/tenant-zero/phase-213/domain-2/dispatch-honesty.test.js
  - test/tenant-zero/phase-213/domain-2/learning-handoff.test.js
  - .planning/phases/213-tenant0-dogfood-compliance-validation/213-VALIDATION.md
autonomous: true
requirements:
  - T0-01
  - T0-02
  - QA-05
  - QA-06
  - QA-07
  - QA-08
must_haves:
  truths:
    - "The first loop targets one already shipped capability and records a full artifact chain from brief to learning handoff."
    - "Every claim in the loop has `claim_status: evidence_backed` and an approval reference before dispatch."
    - "Dispatch ends in `published` or `ready_to_publish`; degraded connector health becomes a named blocker instead of fake publication."
    - "A learning handoff is written for TenantOverlay or literacy follow-up rather than being left implicit."
    - "Tenant 0 dogfood loop runs on the 211 loop substrate without modifying any P207, P208, P209, P210, P211, or P212 file — the dogfood proof requires those surfaces work for a real tenant under real conditions."
    - "The 6-artifact loop-01 chain dissolves both upstream `future_phase_213_tenant0` placeholder gates (211 + 212) per 213-UI-SPEC.md §Translation Gate Dissolution and Opening."
  artifacts:
    - path: .planning/tenant-zero/loop-01-claim-map.json
      provides: claim-by-claim evidence and approval linkage for the first public-proof loop (consumed read-only by future Tenant 0 dogfood runlog timeline per 213-UI-SPEC.md §Downstream UI Inheritance Map; lineage via 207 RunApiEnvelope `agent_run_id` foreign-key per parent_doctrine_chain)
    - path: .planning/tenant-zero/loop-01-dispatch-log.json
      provides: honest connector outcome with `published` or `ready_to_publish` status (consumed read-only by future runlog timeline; degraded → `ready_to_publish` blocker per 210-UI-SPEC.md connector substrate inheritance, NEVER fake publication)
    - path: .planning/tenant-zero/loop-01-learning-handoff.json
      provides: downstream learning payload for P212-style literacy and overlay updates (binding handshake into 212 substrate; `learning_handoff_status: recorded` dissolves the 212 `future_phase_213_tenant0` gate per 213-UI-SPEC.md §Translation Gate Dissolution and Opening)
  key_links:
    - from: lib/markos/governance/evidence-pack.ts
      to: .planning/tenant-zero/loop-01-claim-map.json
      via: the loop must ground every public claim in reusable evidence-source vocabulary
      pattern: "evidence_source"
    - from: lib/markos/crm/reporting.ts
      to: .planning/tenant-zero/loop-01-outcome.md
      via: outcome and readiness narrative should use the same readiness/reporting posture as existing CRM summaries
      pattern: "buildReadinessReport"
---

<ui_spec_fold>
  <ui_scope>NO_UI</ui_scope>
  <ui_spec_block>
    213-UI-SPEC.md §Downstream UI Inheritance Map row "Tenant 0 dogfood runlog timeline" — future P217+ Tenant 0 health dashboard renders the 7-stage loop as a directed timeline (brief → claim-map → approval-log → dispatch-log → outcome → learning-handoff). Each stage renders a `.c-card` panel with the relevant artifact's status badge. The five canonical loop statuses (`approval_status: approved`, `claim_status: evidence_backed`, `dispatch_status: published | ready_to_publish`, `measurement_window_days: 7`, `learning_handoff_status: recorded`) are the read-only contract authored by 213-02 and consumed verbatim by the timeline. Cross-binding: 207 `RunApiEnvelope` lineage via `agent_run_id` on each loop record, 208 `ApprovalHandoffRecord` via approval-log entries, 211 loop substrate as the upstream contract origin, 212 learning-substrate as the downstream consumer of `learning_handoff_status: recorded`.
    213-UI-SPEC.md §Translation Gate Dissolution and Opening — Plan 02 dissolves TWO upstream `future_phase_213_tenant0` placeholder gates: (1) 211-UI-SPEC §Translation Gates Opened (Tenant 0 loop-readiness panel) dissolves once 213-02 produces the full 6-artifact chain with `claim_status: evidence_backed` + `approval_status: approved` + `dispatch_status: published | ready_to_publish` + `measurement_window_days: 7` + `learning_handoff_status: recorded`; (2) 212-UI-SPEC §Translation Gates Opened (Tenant 0 learning-readiness) dissolves once 213-02 produces `loop-01-learning-handoff.json` with `learning_handoff_status: recorded`.
  </ui_spec_block>
  <cross_cutting_carry_forward>
    - **Inheritance from 207 RunApiEnvelope:** Each of the 6 loop-01 artifacts must include or link to an `agent_run_id` so the future runlog timeline can deep-link back to the originating AgentRun trace. Tenant 0 dogfood runs are real `AgentRun` records produced by 207 substrate; 213-02 does NOT modify 207 surfaces.
    - **Inheritance from 208 cockpit consumer:** The loop runs THROUGH the existing P208 operator cockpit (Approval Inbox handles loop-01 approval, Task Board handles brief edits, Recovery Center handles connector degradation, Weekly Narrative reads loop outcomes, Morning Brief reads loop briefs). The cockpit is CONSUMED in production by the dogfood loop; 213-02 does NOT modify any P208 file. Modifying P208 in this phase would invalidate the dogfood proof.
    - **Inheritance from 209 evidence:** Every claim in `loop-01-claim-map.json` MUST have `evidence_ref` linkage; `claim_status: evidence_backed` is the binding gate per 209-UI-SPEC.md `EvidenceMap` substrate. Future readiness dashboards reuse `<EvidenceSummary />` as a read-only consumer.
    - **Inheritance from 210 connector substrate:** Dispatch respects the three `status` literals (`healthy`, `degraded`, `unavailable`) from `connector-inventory.json` (213-01) verbatim. Degraded → `dispatch_status: ready_to_publish` blocker per 210-UI-SPEC.md, NEVER fake publication.
    - **Inheritance from 211 loop substrate (PARENT):** The 6 loop-01 artifacts map 1:1 to the 211 envelope: brief = `ContentBrief`; claim-map = `MarketingArtifact` + `ArtifactAudit`; approval-log = `ApprovalHandoffRecord`; dispatch-log = `DispatchAttempt`; outcome = `RevenueFeedbackLink`; learning-handoff = `MeasurementHandoff`. Tenant 0 dogfood loop IS one real `MarketingStrategyRun → MeasurementHandoff` chain per 211 contract.
    - **Inheritance from 212 learning substrate (PARENT):** Loop-01 ends in `learning_handoff_status: recorded` (binding handshake into 212 substrate); future Tenant 0 learning surfaces (P217+ growth-experiment registries, P225 analytics narrative) read this handoff directly.
    - **Banned-lexicon zero-match** (per CLAUDE.md + 213-UI-SPEC.md §Copywriting Contract): `loop-01-brief.md` + `loop-01-approval-log.md` + `loop-01-outcome.md` doctrine prose contains zero banned-lexicon terms. Loop-status vocabulary (`approval_status: approved`, `claim_status: evidence_backed`, `dispatch_status: published | ready_to_publish`, `learning_handoff_status: recorded`) MUST be used verbatim — no synonyms like `live`, `shipped`, `pending`, `not_yet_published`, `partial`.
  </cross_cutting_carry_forward>
  <downstream_ui_consumers_read_only>
    - Future P217+ Tenant 0 dogfood runlog timeline — reads all 6 loop-01 artifacts; renders 7-stage directed timeline with status header notice. NOT modified by 213-02.
    - Future P217+ Tenant 0 readiness dashboard (single-pane health) — reads `loop-01-learning-handoff.json` `learning_handoff_status` to confirm closeout completion. NOT modified by 213-02.
    - Future case-study editor with public-proof boundary enforcement (P217+) — reads `loop-01-claim-map.json` per-claim `evidence_ref` linkage as the source-of-truth for case-study claims. NOT modified by 213-02.
    - **Critical:** Plan 02 emits loop-01 artifacts only. The dogfood loop EXECUTES through already-shipped P207-P212 substrate (operator cockpit consumed in production, NOT modified). The 6-artifact chain dissolves the 211 + 212 `future_phase_213_tenant0` placeholders downstream; new translation gates for P214-P217 SaaS Suite Activation are opened by 213-05.
  </downstream_ui_consumers_read_only>
</ui_spec_fold>

<objective>
Run one real MarkOS-on-MarkOS operating loop on an already shipped capability, with evidence, approval, honest dispatch, measurement, and learning artifacts that can be audited later in the closeout.
</objective>

<context>
@.planning/phases/213-tenant0-dogfood-compliance-validation/213-RESEARCH.md
@.planning/phases/213-tenant0-dogfood-compliance-validation/213-VALIDATION.md
@.planning/phases/213-tenant0-dogfood-compliance-validation/213-UI-SPEC.md
@.planning/phases/207-agentrun-v2-orchestration-substrate/207-UI-SPEC.md
@.planning/phases/208-human-operating-interface/208-UI-SPEC.md
@.planning/phases/209-evidence-research-and-claim-safety/209-UI-SPEC.md
@.planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md
@.planning/phases/211-content-social-revenue-loop/211-UI-SPEC.md
@.planning/phases/212-learning-literacy-evolution/212-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>
<task id="213-02-01">
  <name>Execute and document the first real Tenant 0 operating loop</name>
  <files>.planning/tenant-zero/loop-01-brief.md, .planning/tenant-zero/loop-01-claim-map.json, .planning/tenant-zero/loop-01-approval-log.md, .planning/tenant-zero/loop-01-dispatch-log.json, .planning/tenant-zero/loop-01-outcome.md, .planning/tenant-zero/loop-01-learning-handoff.json, scripts/tenant-zero/run-first-loop.mjs, scripts/tenant-zero/verify-first-loop.mjs, test/tenant-zero/phase-213/domain-2/loop-artifact-chain.test.js, test/tenant-zero/phase-213/domain-2/dispatch-honesty.test.js, test/tenant-zero/phase-213/domain-2/learning-handoff.test.js</files>
  <read_first>
    - .planning/phases/213-tenant0-dogfood-compliance-validation/213-RESEARCH.md section "Domain 2 - First real operating loop"
    - .planning/phases/213-tenant0-dogfood-compliance-validation/213-UI-SPEC.md §Downstream UI Inheritance Map row "Tenant 0 dogfood runlog timeline" + §Translation Gate Dissolution and Opening (211 + 212 dissolution rules)
    - .planning/phases/207-agentrun-v2-orchestration-substrate/207-UI-SPEC.md (RunApiEnvelope + AgentRunEventType + ApprovalHandoffRecord)
    - .planning/phases/211-content-social-revenue-loop/211-UI-SPEC.md (loop substrate; 6-artifact mapping to 211 envelope)
    - .planning/phases/212-learning-literacy-evolution/212-UI-SPEC.md (learning substrate; `learning_handoff_status: recorded` handshake)
    - .planning/tenant-zero/workspace-profile.json
    - .planning/tenant-zero/connector-inventory.json
    - lib/markos/governance/evidence-pack.ts
    - lib/markos/crm/reporting.ts
  </read_first>
  <action>
    Step 1: Create `.planning/tenant-zero/loop-01-brief.md` with `asset_slug: tenant-zero-loop-01-markos-proof-asset`, `asset_type: public_proof_content`, `surface: docs_or_distribution`, and `measurement_window_days: 7`. Brief prose contains zero CLAUDE.md banned-lexicon terms per 213-UI-SPEC.md §Copywriting Contract (architecture-lock CI gate per 213-01-00).
    Step 2: Create `.planning/tenant-zero/loop-01-claim-map.json` so each claim row contains `claim_id`, `statement`, `evidence_ref`, `approval_required`, `public_status`, and `claim_status: evidence_backed`. Each claim's `evidence_ref` must link to a 209 `EvidenceMap` record per 209-UI-SPEC.md inheritance.
    Step 3: Implement `scripts/tenant-zero/run-first-loop.mjs` so it records the approval log, dispatch log, outcome, and learning handoff using the canonical statuses `approved`, `published`, `ready_to_publish`, and `recorded` verbatim (no synonyms). The script consumes the existing P208 operator cockpit + P207 RunApiEnvelope + P209 EvidenceMap + P210 connector substrate + P211 loop substrate IN PRODUCTION — it does NOT modify any P207-P212 surface. Connector degradation produces `dispatch_status: ready_to_publish` per 210-UI-SPEC.md inheritance, NEVER fake publication.
    Step 4: Implement `scripts/tenant-zero/verify-first-loop.mjs` and the domain-2 tests so the loop fails when evidence, approval, dispatch honesty, or learning handoff is missing. The verify script enforces the 5 canonical loop statuses verbatim and the 6-artifact chain mapping to 211 envelope (brief → ContentBrief; claim-map → MarketingArtifact + ArtifactAudit; approval-log → ApprovalHandoffRecord; dispatch-log → DispatchAttempt; outcome → RevenueFeedbackLink; learning-handoff → MeasurementHandoff).
  </action>
  <acceptance_criteria>
    - `.planning/tenant-zero/loop-01-brief.md` contains `asset_slug: tenant-zero-loop-01-markos-proof-asset` and `measurement_window_days: 7`
    - `.planning/tenant-zero/loop-01-claim-map.json` contains the exact keys `"claim_id"`, `"evidence_ref"`, `"approval_required"`, and `"claim_status": "evidence_backed"`
    - `.planning/tenant-zero/loop-01-dispatch-log.json` contains `"dispatch_status": "published"` or `"dispatch_status": "ready_to_publish"` (per 210-UI-SPEC.md connector substrate inheritance — degraded → `ready_to_publish`, NEVER fake publication)
    - `.planning/tenant-zero/loop-01-learning-handoff.json` contains `"learning_handoff_status": "recorded"` (binding handshake into 212 substrate per 213-UI-SPEC.md §Translation Gate Dissolution rule)
    - All 6 loop-01 artifacts include or link to an `agent_run_id` per 207-UI-SPEC.md `RunApiEnvelope` lineage
    - `loop-01-brief.md` + `loop-01-approval-log.md` + `loop-01-outcome.md` doctrine prose contains zero CLAUDE.md banned-lexicon terms
    - `files_modified` block contains zero P207, P208, P209, P210, P211, or P212 source files (dogfood proof integrity — operator cockpit + connector substrate + evidence substrate + loop substrate are CONSUMED in production, NOT modified)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/tenant-zero/phase-213/domain-2/</automated>
  </verify>
  <done>The first loop is real enough to audit later and honest enough to block on degraded connector or evidence posture instead of narrating success.</done>
</task>
</tasks>

<verification>
- npm test -- test/tenant-zero/phase-213/domain-2/
</verification>

<success_criteria>
- A full first-loop artifact chain exists from brief through learning handoff
- Every public claim is evidence-backed and approved before dispatch
- Dispatch honesty is explicit and connector-state-aware
- The loop creates measurable output and a learning handoff instead of stopping at publication
- The 6-artifact chain dissolves the 211 + 212 `future_phase_213_tenant0` translation gates per 213-UI-SPEC.md §Translation Gate Dissolution and Opening
- No P207, P208, P209, P210, P211, or P212 file is modified — dogfood proof integrity preserved
</success_criteria>
</content>
</invoke>