---
phase: 213
plan: "04"
type: execute
wave: 4
depends_on:
  - "213-01"
  - "213-02"
  - "213-03"
f_ids: []
migration_slots: []
integrates_with:
  - "COMP-01 from P206 (compliance language must reuse the real SOC2 posture)"
  - "EVD-01..06 from P209 (public proof cannot exist without evidence linkage)"
translation_gate_for: []
files_modified:
  - .planning/tenant-zero/public-proof-policy.md
  - .planning/tenant-zero/case-study-readiness-checklist.md
  - .planning/tenant-zero/compliance-language-boundary.md
  - .planning/tenant-zero/public-claim-audit-summary.md
  - scripts/tenant-zero/audit-public-claims.mjs
  - test/tenant-zero/phase-213/domain-4/public-claim-audit.test.js
  - test/tenant-zero/phase-213/domain-4/compliance-language-boundary.test.js
  - .planning/phases/213-tenant0-dogfood-compliance-validation/213-VALIDATION.md
autonomous: true
requirements:
  - T0-04
  - QA-12
  - QA-13
must_haves:
  truths:
    - "Every public claim is classified as `implemented_verified`, `roadmap_only`, or `internal_only`."
    - "Every case-study or proof claim has evidence linkage, approval linkage, freshness, and privacy class."
    - "Compliance and enterprise-readiness wording distinguishes designed controls, preparation, verified Type I posture, and future Type II work."
    - "Planned agents, growth modules, and future SaaS Suite work are never described as already shipped."
    - "The 9-row banned-phrases table from 213-UI-SPEC.md §Public Proof Boundary Copy Contract is enforced verbatim by `scripts/tenant-zero/audit-public-claims.mjs` as an author-time gate; no banned phrase may pass the audit unless replaced with the allowed-phrasing template."
    - "The allowed-phrasing template `MarkOS Tenant 0 ran <verb-phrase> with <evidence_ref linkage> in <time window>` is the constrained shape for every Tenant 0 case-study claim."
    - "Every case-study publish action is mutation-class `public.claim` (DUAL with `external.send` for external distribution) per 206-UI-SPEC.md mutation-class doctrine."
  artifacts:
    - path: .planning/tenant-zero/public-proof-policy.md
      provides: canonical rules for public-proof eligibility and claim classes (consumed read-only by future case-study editor with public-proof boundary enforcement per 213-UI-SPEC.md §Downstream UI Inheritance Map)
    - path: .planning/tenant-zero/compliance-language-boundary.md
      provides: precise wording boundary for enterprise and SOC2-related claims (the four status literals `designed_control`, `type1_in_preparation`, `type1_verified`, `type2_future` are the read-only compliance-vocabulary contract for future surfaces)
    - path: .planning/tenant-zero/public-claim-audit-summary.md
      provides: surface-by-surface audit summary for public claims and proof posture (consumed read-only by future public-claim audit summary browser per 213-UI-SPEC.md §Downstream UI Inheritance Map; per-claim 7-field envelope `claim_id`, `surface`, `evidence_ref`, `approval_ref`, `fresh_until`, `privacy_class`, `status`)
  key_links:
    - from: lib/markos/governance/evidence-pack.ts
      to: .planning/tenant-zero/public-proof-policy.md
      via: public proof eligibility must be tied to existing evidence-pack vocabulary
      pattern: "evidence_pack_id"
    - from: .planning/phases/206-soc2-type1-foundation/206-RESEARCH.md
      to: .planning/tenant-zero/compliance-language-boundary.md
      via: public compliance wording must consume the real Phase 206 posture rather than aspirational copy
      pattern: "SOC2"
---

<ui_spec_fold>
  <ui_scope>NO_UI</ui_scope>
  <ui_spec_block>
    213-UI-SPEC.md §Public Proof Boundary Copy Contract — load-bearing forward contract for every future UI surface that publishes a Tenant 0 claim. Authors:
    - **9-row banned-phrases table** (verbatim, case-insensitive substring match — author-time gate enforced by `audit-public-claims.mjs`):
      1. `SOC 2 certified`, `SOC 2 compliant`, `SOC 2 verified` (before P206 verified cert exists) → allowed alternatives: `Designed for SOC 2 Type 1 controls`, `SOC 2 Type 1 in preparation`, `SOC 2 Type 1 verified` (post-P206 cert)
      2. `enterprise-grade`, `enterprise-ready` (without evidence linkage) → allowed: `Tenant 0 readiness gate green per 213-05 final-readiness-summary` (with `evidence_ref` linkage)
      3. `10× faster than competitor X` (without evidence linkage), `the only AI marketing OS that...` (without evidence linkage) → allowed: `In MarkOS Tenant 0, X loop ran with Y evidence in Z time` (with `evidence_ref` linkage)
      4. `revolutionize`, `transform`, `unlock`, `empower`, `synergy`, `leverage`, `seamless`, `cutting-edge`, `innovative`, `game-changer`, `next-generation`, `world-class`, `best-in-class`, `reimagine`, `disrupt`, `holistic`, `supercharge` (CLAUDE.md banned-lexicon list) → allowed: concrete, verifiable language describing what shipped
      5. `just` (as softener — "just one click", "just press start") → allowed: remove the softener; describe the action concretely
      6. Any exclamation point in product surface copy → allowed: periods only
      7. Emoji in any product surface copy, CLI output, error message, or technical doc → allowed: bracketed glyphs (`[ok]`, `[warn]`, `[err]`, `[block]`, `[info]`)
      8. `we strive to`, `best-effort`, `we believe`, `we think` (hedging without evidence) → allowed: concrete claim with `evidence_ref` linkage
      9. `production-grade` (without evidence linkage) → allowed: `Used in production by MarkOS Tenant 0` (with `evidence_ref` linkage)
    - **Allowed-phrasing template** (constrained shape for any Tenant 0 case-study claim): `MarkOS Tenant 0 ran <verb-phrase describing actual loop> with <evidence_ref linkage> in <time window>.`
    - **Author-time enforcement** (future case-study editor obligation per 213-UI-SPEC.md):
      1. Substring-match every banned phrase from the 9-row table (case-insensitive)
      2. Render inline `.c-notice c-notice--error` per offending phrase with display copy `[err] Banned phrase detected: "<phrase>" — see allowed alternatives in 213-UI-SPEC.md §Public Proof Boundary Copy Contract`
      3. Disable `.c-button--primary` "Publish" until every banned-phrase notice is dismissed (operator must EDIT the offending phrase, not override)
      4. Log every dismissal AND every correction to a future compliance-language audit log surface
    213-UI-SPEC.md §Downstream UI Inheritance Map row "Case-study editor with public-proof boundary enforcement" — future P217+ marketing-site editor or future content-authoring phase reads `public-proof-policy.md` + `case-study-readiness-checklist.md` + `compliance-language-boundary.md` + 213-UI-SPEC.md §Public Proof Boundary Copy Contract. Mutation-class binding per 206-UI-SPEC.md: `public.claim` (DUAL with `external.send` if the case study targets external email/social distribution); approval mode `dual_approval` minimum (operator + brand officer per 206 mutation-class doctrine).
    213-UI-SPEC.md §Downstream UI Inheritance Map row "Public-claim audit summary browser" — future P217+ marketing-site editor pre-publish gate or future enterprise admin reads `public-claim-audit-summary.md`. Per-claim 7-field row (`claim_id`, `surface`, `evidence_ref`, `approval_ref`, `fresh_until`, `privacy_class`, `status`); the three claim-class statuses (`implemented_verified`, `roadmap_only`, `internal_only`) render as `.c-badge--{success,warning,info}`. D-14 vanilla `<table>` recipe required.
    213-UI-SPEC.md §Downstream UI Inheritance Map row "Compliance-language audit log" — future compliance-monitoring phase (post-P206 SOC 2 Type II) reads `compliance-language-boundary.md` + `public-claim-audit-summary.md`. The four compliance-status literals (`designed_control`, `type1_in_preparation`, `type1_verified`, `type2_future`) render as filter chips.
  </ui_spec_block>
  <cross_cutting_carry_forward>
    - **Banned-lexicon zero-match** + **9-row banned-phrases table verbatim enforcement** (per 213-UI-SPEC.md §Public Proof Boundary Copy Contract): `audit-public-claims.mjs` MUST contain all 9 banned-phrase categories verbatim and reject on author-time substring match (case-insensitive). The CLAUDE.md banned-lexicon list in row 4 is enforced verbatim across all 19 listed words plus `just` (softener) per row 5 plus exclamation points per row 6 plus emoji per row 7. Banned-lexicon zero-match also applies to `.planning/tenant-zero/*.md` doctrine prose (architecture-lock CI gate per 213-01-00).
    - **Allowed-phrasing template enforcement** (per 213-UI-SPEC.md §Public Proof Boundary Copy Contract): `audit-public-claims.mjs` enforces the constrained shape `MarkOS Tenant 0 ran <verb-phrase> with <evidence_ref linkage> in <time window>` as the only acceptable phrasing for Tenant 0 case-study claims. Claims that fail the banned-phrases gate AND lack the allowed-phrasing template fail the audit.
    - **Three claim-class literals verbatim** (per 213-UI-SPEC.md §Copywriting Contract claim-class vocabulary): `implemented_verified`, `roadmap_only`, `internal_only`. No synonyms (`live`, `shipped`, `planned`, `coming_soon`).
    - **Four compliance-status literals verbatim** (per 213-UI-SPEC.md §Copywriting Contract compliance-language vocabulary): `designed_control`, `type1_in_preparation`, `type1_verified`, `type2_future`. No synonyms (`compliant`, `certified`, `audited` without status qualifier).
    - **Mutation-class binding** (per 206-UI-SPEC.md inheritance, surfaced in 213-UI-SPEC.md §Destructive Actions): every case-study publish action is mutation-class `public.claim` (DUAL with `external.send` if the case study targets external email/social distribution); approval mode `dual_approval` minimum. Override path inherits 209-04 doctrine: banned-phrases on case-study publish are NOT override-able (`override_path == 'denied'`).
    - **D-08 (token-only) + D-09 (mint-as-text) + D-13 (`.c-card--feature` reserved) + D-14 (no `.c-table` primitive):** Future case-study editor maps claim-class literals to `.c-badge--{success,warning,info}`; uses `.c-card` default never `.c-card--feature`; uses vanilla `<table>` for audit summary browser; `privacy_class` chips compose `.c-chip-protocol` (mint-as-text). 213-04 emits doctrine + audit JSON only; rendering is downstream.
  </cross_cutting_carry_forward>
  <downstream_ui_consumers_read_only>
    - Future P217+ marketing-site case-study editor with public-proof boundary enforcement — reads `public-proof-policy.md` + `case-study-readiness-checklist.md` + `compliance-language-boundary.md` + 213-UI-SPEC.md §Public Proof Boundary Copy Contract; enforces banned-phrases at author-time; mutation-class `public.claim` DUAL with `external.send`. NOT modified by 213-04.
    - Future P217+ public-claim audit summary browser — reads `public-claim-audit-summary.md` per-claim 7-field envelope; renders 3 claim-class statuses as `.c-badge--{success,warning,info}`. NOT modified by 213-04.
    - Future compliance-monitoring phase (post-P206 SOC 2 Type II) compliance-language audit log — reads `compliance-language-boundary.md` four status literals as filter chips; logs every author-time banned-phrase violation. NOT modified by 213-04.
    - **Critical:** Plan 04 emits public-proof + compliance-language doctrine + audit script only. The 9-row banned-phrases table + allowed-phrasing template + 3 claim-class literals + 4 compliance-status literals are the load-bearing forward contracts for every future case-study editor and audit log browser. Adding a new banned phrase or a new compliance-status literal requires simultaneous updates to 213-UI-SPEC.md §Public Proof Boundary Copy Contract AND `compliance-language-boundary.md` per the explicit forward-contract rule.
  </downstream_ui_consumers_read_only>
</ui_spec_fold>

<objective>
Define the public-proof, case-study, and compliance-language boundary so MarkOS can talk about Tenant 0 results honestly without overstating shipped capability or compliance posture.
</objective>

<context>
@.planning/phases/213-tenant0-dogfood-compliance-validation/213-RESEARCH.md
@.planning/phases/213-tenant0-dogfood-compliance-validation/213-VALIDATION.md
@.planning/phases/213-tenant0-dogfood-compliance-validation/213-UI-SPEC.md
@.planning/phases/206-soc2-type1-foundation/206-UI-SPEC.md
@.planning/phases/209-evidence-research-and-claim-safety/209-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>
<task id="213-04-01">
  <name>Create the public proof and compliance-language boundary</name>
  <files>.planning/tenant-zero/public-proof-policy.md, .planning/tenant-zero/case-study-readiness-checklist.md, .planning/tenant-zero/compliance-language-boundary.md, .planning/tenant-zero/public-claim-audit-summary.md, scripts/tenant-zero/audit-public-claims.mjs, test/tenant-zero/phase-213/domain-4/public-claim-audit.test.js, test/tenant-zero/phase-213/domain-4/compliance-language-boundary.test.js</files>
  <read_first>
    - .planning/phases/213-tenant0-dogfood-compliance-validation/213-RESEARCH.md section "Domain 4 - Public proof, case-study, and compliance language boundary"
    - .planning/phases/213-tenant0-dogfood-compliance-validation/213-UI-SPEC.md §Public Proof Boundary Copy Contract (9-row banned-phrases table verbatim + allowed-phrasing template + author-time enforcement) + §Downstream UI Inheritance Map rows "Case-study editor with public-proof boundary enforcement" + "Public-claim audit summary browser" + "Compliance-language audit log"
    - .planning/phases/206-soc2-type1-foundation/206-UI-SPEC.md (mutation-class `public.claim` DUAL with `external.send` doctrine origin)
    - .planning/phases/209-evidence-research-and-claim-safety/209-UI-SPEC.md (`<EvidenceSummary />` consumer pattern)
    - .planning/tenant-zero/loop-01-claim-map.json
    - .planning/tenant-zero/pricing/public-pricing-audit.md
    - lib/markos/governance/evidence-pack.ts
  </read_first>
  <action>
    Step 1: Create `.planning/tenant-zero/public-proof-policy.md` with the exact claim classes `implemented_verified`, `roadmap_only`, and `internal_only` verbatim (per 213-UI-SPEC.md §Copywriting Contract claim-class vocabulary). Doctrine prose contains zero CLAUDE.md banned-lexicon terms.
    Step 2: Create `.planning/tenant-zero/case-study-readiness-checklist.md` so every proof row requires `claim_id`, `surface`, `evidence_ref`, `approval_ref`, `fresh_until`, `privacy_class`, and `status` (the 7-field envelope read-only contract for future case-study editor + audit summary browser per 213-UI-SPEC.md §Downstream UI Inheritance Map).
    Step 3: Create `.planning/tenant-zero/compliance-language-boundary.md` with the exact status terms `designed_control`, `type1_in_preparation`, `type1_verified`, and `type2_future` verbatim (per 213-UI-SPEC.md §Copywriting Contract compliance-language vocabulary). No synonyms; new literals cannot be silently introduced.
    Step 4: Implement `scripts/tenant-zero/audit-public-claims.mjs` and the domain-4 tests so unsupported, stale, privacy-unsafe, or roadmap-only claims fail the audit. The script MUST contain all 9 banned-phrase categories from 213-UI-SPEC.md §Public Proof Boundary Copy Contract verbatim (case-insensitive substring match): (1) `SOC 2 certified` / `SOC 2 compliant` / `SOC 2 verified`; (2) `enterprise-grade` / `enterprise-ready` (without evidence linkage); (3) comparative claims like `10× faster than competitor X` (without evidence linkage); (4) the 17-word CLAUDE.md banned-lexicon list (`revolutionize`, `transform`, `unlock`, `empower`, `synergy`, `leverage`, `seamless`, `cutting-edge`, `innovative`, `game-changer`, `next-generation`, `world-class`, `best-in-class`, `reimagine`, `disrupt`, `holistic`, `supercharge`); (5) `just` (as softener); (6) exclamation points in product copy; (7) emoji in any product surface; (8) hedging without evidence (`we strive to`, `best-effort`, `we believe`, `we think`); (9) `production-grade` (without evidence linkage). The script ALSO enforces the allowed-phrasing template `MarkOS Tenant 0 ran <verb-phrase> with <evidence_ref linkage> in <time window>` as the constrained shape for Tenant 0 case-study claims.
  </action>
  <acceptance_criteria>
    - `.planning/tenant-zero/public-proof-policy.md` contains the exact strings `implemented_verified`, `roadmap_only`, and `internal_only` verbatim
    - `.planning/tenant-zero/case-study-readiness-checklist.md` contains the exact field names `claim_id`, `surface`, `evidence_ref`, `approval_ref`, `fresh_until`, `privacy_class`, and `status` (7-field envelope per 213-UI-SPEC.md §Downstream UI Inheritance Map)
    - `.planning/tenant-zero/compliance-language-boundary.md` contains the exact status terms `designed_control`, `type1_in_preparation`, `type1_verified`, and `type2_future` verbatim
    - `.planning/tenant-zero/public-claim-audit-summary.md` contains at least one row with a `surface` and `status`
    - `scripts/tenant-zero/audit-public-claims.mjs` contains all 9 banned-phrase categories from 213-UI-SPEC.md §Public Proof Boundary Copy Contract verbatim, including the 17-word CLAUDE.md banned-lexicon list (`synergy`, `leverage`, `empower`, `unlock`, `transform`, `revolutionize`, `supercharge`, `holistic`, `seamless`, `cutting-edge`, `innovative`, `game-changer`, `next-generation`, `world-class`, `best-in-class`, `reimagine`, `disrupt`)
    - `scripts/tenant-zero/audit-public-claims.mjs` enforces the allowed-phrasing template literal `MarkOS Tenant 0 ran` (as a substring marker for the constrained shape) per 213-UI-SPEC.md §Public Proof Boundary Copy Contract
    - `.planning/tenant-zero/*.md` doctrine prose authored by 213-04 contains zero CLAUDE.md banned-lexicon terms (architecture-lock CI gate per 213-01-00)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/tenant-zero/phase-213/domain-4/</automated>
  </verify>
  <done>Tenant 0 proof language is now governed by explicit claim classes, evidence freshness, privacy posture, and compliance wording boundaries.</done>
</task>
</tasks>

<verification>
- npm test -- test/tenant-zero/phase-213/domain-4/
</verification>

<success_criteria>
- Public proof policy exists with explicit claim classes
- Case-study readiness requires evidence, approval, freshness, and privacy metadata
- Compliance language uses precise and bounded status terms
- Public-claim audit catches roadmap-overreach and stale or unsupported proof
- The 9-row banned-phrases table from 213-UI-SPEC.md §Public Proof Boundary Copy Contract is enforced verbatim by `audit-public-claims.mjs` as an author-time gate
- The allowed-phrasing template `MarkOS Tenant 0 ran <verb-phrase> with <evidence_ref linkage> in <time window>` is enforced as the constrained shape
- Mutation-class `public.claim` DUAL with `external.send` doctrine inheritance from 206-UI-SPEC.md is documented for downstream case-study editor consumers
</success_criteria>
</content>
</invoke>