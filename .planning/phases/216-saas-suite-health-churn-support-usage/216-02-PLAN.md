---
phase: 216
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
f_ids:
  - F-260
  - F-261
migration_slots:
  - 113
integrates_with:
  - "CONN-01..06 from P210 (connector_installs FK + connector adapter substrate)"
  - "SG-02 from P218 (PLG event taxonomy planned-only — P218 ActivationDefinition + PQLScore consume product_usage_events.event_category)"
  - "SAS-09 from P217 (P217 surfaces PLG signals via /v1/saas/usage)"
translation_gate_for: []
files_modified:
  - test/fixtures/saas-216/index.js
  - test/fixtures/saas-216/product-usage-event.js
  - test/fixtures/saas-216/product-usage-connector.js
  - scripts/preconditions/216-02-check-upstream.cjs
  - supabase/migrations/113_saas_product_usage_events.sql
  - lib/markos/saas/usage/contracts.ts
  - lib/markos/saas/usage/contracts.cjs
  - lib/markos/saas/usage/event-ingest.ts
  - lib/markos/saas/usage/event-ingest.cjs
  - lib/markos/saas/usage/event-taxonomy.ts
  - lib/markos/saas/usage/connector-adapter.ts
  - lib/markos/saas/usage/health-rollup.ts
  - lib/markos/saas/usage/index.cjs
  - lib/markos/mcp/tools/usage.cjs
  - lib/markos/mcp/tools/index.cjs
  - api/v1/saas/usage.js
  - api/cron/saas-usage-health-rollup.js
  - contracts/F-260-saas-product-usage-event-v1.yaml
  - contracts/F-261-saas-product-usage-connector-v1.yaml
  - contracts/flow-registry.json
  - test/saas-216/domain-2/usage-ingest-connector-auth.test.js
  - test/saas-216/domain-2/usage-ingest-tenant-trigger.test.js
  - test/saas-216/domain-2/usage-event-taxonomy.test.js
  - test/saas-216/domain-2/usage-health-rollup.test.js
  - test/saas-216/domain-2/usage-retention-trigger.test.js
  - test/saas-216/rls/product-usage-events-cross-tenant-denial.test.js
  - .planning/phases/216-saas-suite-health-churn-support-usage/216-VALIDATION.md
autonomous: true
requirements:
  - SAS-07
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - QA-05
  - QA-06
  - QA-07
  - QA-08
must_haves:
  truths:
    - "Plan 02 depends on Plan 01 — assertUpstreamReady from lib/markos/saas/preflight (Plan 01 ships) is invoked before any Plan 02 task; data_retention_classes table + pii_classification ENUM (Plan 01 migration 112) are referenced as FK by retention_class column"
    - "Migration 113 ships product_usage_events + product_usage_connectors tables — RLS-enabled tenant_id-scoped per markos_orgs FK; product_usage_events columns: event_id PK, tenant_id, subscription_id (nullable), customer_id (nullable), connector_id NOT NULL FK connector_installs (P210), external_event_id, event_name, event_category CHECK ('activation','adoption','depth','stickiness','expansion','plg_readiness','churn_signal','other'), user_id_external (hashed/opaque — not PII-raw), account_id_external, properties jsonb (PII-scrubbed), occurred_at, retention_class NOT NULL FK data_retention_classes, retention_until NOT NULL, ingested_at, ingest_source CHECK ('webhook','api','csv_import','connector_sync')"
    - "product_usage_connectors columns: connector_id PK, tenant_id, install_id NOT NULL FK connector_installs (P210), source_type CHECK ('generic_webhook','posthog','segment','amplitude','mixpanel','heap','rudderstack','csv_import'), event_mapping jsonb (maps source event names → P216 event_category taxonomy), is_active boolean DEFAULT true, last_synced_at, created_at — RLS-enabled tenant_id-scoped"
    - "DB-trigger compliance — `PRODUCT_USAGE_INGEST_REQUIRES_TENANT_AUTH` BEFORE INSERT on product_usage_events raises EXCEPTION when tenant_id IS NULL OR connector_id IS NULL OR retention_class IS NULL (verbatim exception name per RESEARCH §Domain 2 DB-Trigger Compliance; defense-in-depth pair with app-layer event-ingest validation)"
    - "Generic-first connector decision (DISCUSS Decisions Locked) — P216 ships generic HTTP webhook ingest endpoint POST /v1/saas/usage/ingest; PostHog/Segment/Amplitude/Mixpanel-specific adapters deferred to v4.2.0; the generic connector uses connector_installs (P210 substrate) with auth via x-connector-auth header; source_type='generic_webhook' is the v1 default"
    - "PLG event taxonomy (7 categories) defined per RESEARCH §Domain 2 Event Taxonomy table — activation/adoption/depth/stickiness/expansion/plg_readiness/churn_signal — exposed via lib/markos/saas/usage/event-taxonomy.ts as EVENT_CATEGORIES const + getDownstreamConsumer(category) returning ['P218.ActivationDefinition'|'P218.PQLScore'|'P218.PQLDepth'|'P216.HealthScore.usage'|'P219.AccountExpansion'|'P218.PLGReadiness'|'P216.HealthScore.usage,P216.Plan04.ChurnPlaybooks']"
    - "2 contracts ship — F-260 (saas-product-usage-event-v1) covering POST /v1/saas/usage/ingest + GET /v1/saas/usage/summary; F-261 (saas-product-usage-connector-v1) covering connector adapter contract + event_mapping JSONB schema; NO collision with F-001..F-259 verified by `flow-registry.json` regen"
    - "MCP tools registered in `lib/markos/mcp/tools/usage.cjs` (CommonJS — NOT .ts) and added to `lib/markos/mcp/tools/index.cjs` registry: get_product_usage_summary (per-subscription summary: activation %, stickiness ratio, PLG readiness)"
    - "Cron `api/cron/saas-usage-health-rollup.js` — daily rollup feeding saas_health_scores usage dimension; uses x-markos-cron-secret header matching MARKOS_SAAS_CRON_SECRET; aggregates last 30d events per (tenant_id, subscription_id) producing usage_dimension_score JSONB written to next saas_health_scores INSERT raw_facts.usage"
    - "Cross-tenant denial test asserts INSERT under tenant A then SELECT under tenant B returns 0 rows for product_usage_events + product_usage_connectors (proves RLS not bypassable)"
    - "Idempotency — external_event_id UNIQUE per (tenant_id, connector_id) prevents duplicate event ingest; INSERT ON CONFLICT DO NOTHING pattern documented; retry-safe ingest verified by usage-ingest-tenant-trigger.test.js"
    - "Privacy — user_id_external is hashed/opaque per pseudonymous classification (Plan 01 ENUM); properties JSONB is PII-scrubbed before INSERT (event-ingest.ts validates against PII detection regex); retention_class='product_event' (365d/hard_delete) seeded in Plan 01 data_retention_classes"
    - "SG-02 documented in frontmatter as `integrates_with` (NOT `requirements`) — P218 OWNS SG-02 PLG capability per REQUIREMENTS.md ownership; P216 reserves the event_category taxonomy that P218 ActivationDefinition + PQLScore will consume; Plan 06 growth_signal_map documents the cross-phase column contract"
  artifacts:
    - path: scripts/preconditions/216-02-check-upstream.cjs
      provides: CLI preflight checker — invokes assertUpstreamReady from lib/markos/saas/preflight (Plan 01); additionally hard-checks data_retention_classes table + pii_classification ENUM exist (Plan 01 dependency); exit 1 on hard miss
    - path: supabase/migrations/113_saas_product_usage_events.sql
      provides: combined migration — product_usage_events + product_usage_connectors + PRODUCT_USAGE_INGEST_REQUIRES_TENANT_AUTH DB-trigger + retention_class trigger (delegated to Plan 05 SUPPORT_DATA_REQUIRES_RETENTION_POLICY) + RLS policies + tenant_id+subscription_id+occurred_at indexes + event_category index + connector_id index + UNIQUE(tenant_id, connector_id, external_event_id) for idempotency
    - path: lib/markos/saas/usage/contracts.ts
      provides: zod schemas + enum constants — EVENT_CATEGORIES = ['activation','adoption','depth','stickiness','expansion','plg_readiness','churn_signal','other'] as const; INGEST_SOURCES = ['webhook','api','csv_import','connector_sync'] as const; CONNECTOR_SOURCE_TYPES = ['generic_webhook','posthog','segment','amplitude','mixpanel','heap','rudderstack','csv_import'] as const; ProductUsageEventSchema + ProductUsageConnectorSchema zod
      exports: [EVENT_CATEGORIES, INGEST_SOURCES, CONNECTOR_SOURCE_TYPES, ProductUsageEventSchema, ProductUsageConnectorSchema]
    - path: lib/markos/saas/usage/event-ingest.ts
      provides: ingestEvent({ tenantId, connectorId, eventName, eventCategory, occurredAt, properties, userIdExternal, accountIdExternal }, supabase) — validates against zod; PII-scrubs properties; INSERTs into product_usage_events with retention_class='product_event' + retention_until=now()+365d; returns { event_id, deduplicated }
      exports: [ingestEvent, scrubPII, computeRetentionUntil]
    - path: lib/markos/saas/usage/event-taxonomy.ts
      provides: EVENT_TAXONOMY map (7 categories → downstream consumer phase + signal name) per RESEARCH §Domain 2 Event Taxonomy; getDownstreamConsumer(category) returns string[]
      exports: [EVENT_TAXONOMY, getDownstreamConsumer]
    - path: lib/markos/saas/usage/connector-adapter.ts
      provides: connector adapter pattern — adaptGenericWebhook({ payload, mapping }) returns normalized event shape; future PostHog/Segment-specific adapters extend this contract
      exports: [adaptGenericWebhook, NormalizedEventShape]
    - path: lib/markos/saas/usage/health-rollup.ts
      provides: rollupUsageForHealth({ tenantId, subscriptionId, windowDays = 30 }, supabase) — aggregates events by category for last N days; returns { activation_pct, stickiness_ratio, plg_readiness_score, churn_signal_count, dau_wau_ratio } feeding saas_health_scores raw_facts.usage
      exports: [rollupUsageForHealth]
    - path: lib/markos/saas/usage/index.cjs
      provides: CommonJS twin re-exporting full surface
    - path: lib/markos/mcp/tools/usage.cjs
      provides: MCP tool descriptor get_product_usage_summary; registered into lib/markos/mcp/tools/index.cjs
    - path: api/v1/saas/usage.js
      provides: POST /v1/saas/usage/ingest (generic webhook + API ingest) + GET /v1/saas/usage/summary (per-subscription summary); legacy api/*.js handler — uses requireHostedSupabaseAuth for /summary; uses x-connector-auth for /ingest
    - path: api/cron/saas-usage-health-rollup.js
      provides: daily cron — for each active SaaS subscription calls rollupUsageForHealth; result feeds next saas_health_scores INSERT raw_facts.usage; emits AgentRun kind='saas-usage-health-rollup'
    - path: contracts/F-260-saas-product-usage-event-v1.yaml
      provides: POST /v1/saas/usage/ingest + GET /v1/saas/usage/summary schemas; mirror lib/markos/saas/usage/contracts.ts zod
    - path: contracts/F-261-saas-product-usage-connector-v1.yaml
      provides: connector adapter contract + event_mapping JSONB schema documentation
    - path: contracts/flow-registry.json
      provides: 2 entries appended for F-260 + F-261; NO overlap with F-001..F-259
    - path: test/saas-216/domain-2/usage-ingest-connector-auth.test.js
      provides: connector_id required — INSERT without connector_id → expect EXCEPTION 'PRODUCT_USAGE_INGEST_REQUIRES_TENANT_AUTH'; without tenant_id → same; without retention_class → same; valid → succeeds
    - path: test/saas-216/domain-2/usage-event-taxonomy.test.js
      provides: EVENT_CATEGORIES coverage — 7 categories all map to downstream consumer; getDownstreamConsumer('activation')→['P218.ActivationDefinition'], etc.
    - path: test/saas-216/domain-2/usage-health-rollup.test.js
      provides: rollupUsageForHealth unit tests — 30d window, multi-event aggregation, empty window returns zero scores
    - path: test/saas-216/domain-2/usage-retention-trigger.test.js
      provides: retention_class FK validates against data_retention_classes; INSERT with retention_class='unknown' → expect FK violation; with retention_class='product_event' → succeeds
    - path: test/saas-216/rls/product-usage-events-cross-tenant-denial.test.js
      provides: cross-tenant RLS denial — INSERT under tenant A; SELECT under tenant B returns 0 rows; same for product_usage_connectors
  key_links:
    - from: lib/markos/saas/usage/event-ingest.ts
      to: supabase/migrations/113_saas_product_usage_events.sql
      via: app-layer ingestEvent INSERT; DB-trigger trg_product_usage_tenant_auth asserts tenant_id + connector_id + retention_class non-null (defense-in-depth pair)
      pattern: "PRODUCT_USAGE_INGEST_REQUIRES_TENANT_AUTH"
    - from: lib/markos/saas/usage/event-ingest.ts
      to: lib/markos/saas/preflight (Plan 01 surface)
      via: imports assertUpstreamReady from index.cjs; hard-checks P210 connector_installs and Plan 01 data_retention_classes before first INSERT
      pattern: "assertUpstreamReady"
    - from: api/cron/saas-usage-health-rollup.js
      to: supabase saas_health_scores (Plan 01 table)
      via: cron rollup output writes to next saas_health_scores INSERT raw_facts.usage JSONB
      pattern: "raw_facts.usage"
    - from: lib/markos/saas/usage/health-rollup.ts
      to: lib/markos/saas/usage/event-taxonomy.ts
      via: rollupUsageForHealth uses EVENT_TAXONOMY to filter by category before aggregating
      pattern: "EVENT_TAXONOMY"
    - from: contracts/F-260-saas-product-usage-event-v1.yaml
      to: lib/markos/saas/usage/contracts.ts
      via: ProductUsageEventSchema parity-tested against F-260 YAML
      pattern: "ProductUsageEventSchema"
---

<objective>
Ship Wave 2 product usage event ingestion + PLG event taxonomy + connector adapter + health rollup, satisfying SAS-07 usage-dimension axis (RESEARCH §Domain 2). Plan 02 builds on Plan 01 foundation — uses lib/markos/saas/preflight assertUpstreamReady; FK-references Plan 01 data_retention_classes lookup table; rollup output feeds Plan 01 saas_health_scores raw_facts.usage. Generic-first connector strategy locked in DISCUSS Decisions Locked; PostHog/Segment/Amplitude/Mixpanel-specific adapters deferred to v4.2.0. PLG event taxonomy reserves event_category values that P218 ActivationDefinition + PQLScore + UpgradeTrigger will consume — documented as integrates_with: SG-02 from P218 (NOT requirements).

Purpose: satisfy SAS-07 (product usage axis of health score; rollup feeds raw_facts.usage with activation_pct + stickiness_ratio + plg_readiness_score + churn_signal_count + dau_wau_ratio per RESEARCH dimension model) + QA-01..08 (architecture-lock + helper-presence + RLS isolation + F-ID parity + threat model + assertUpstreamReady + retention enforcement + cross-tenant denial). The PRODUCT_USAGE_INGEST_REQUIRES_TENANT_AUTH DB-trigger enforces the CONTEXT.md non-negotiable "No product usage dashboard that does not create tasks, alerts, recommendations, or learning" by ensuring every event row has tenant_id + connector_id + retention_class — preventing orphaned/anonymous events that bypass downstream alert generation.

Output: 1 preflight CLI + 1 SOR migration (113 — product_usage_events + product_usage_connectors with FK to Plan 01 data_retention_classes + P210 connector_installs) + 7 usage lib files + 1 MCP tool + 1 API handler + 1 cron + 2 contract YAMLs + flow-registry update + 5 domain-2 test files + 1 RLS test + populated VALIDATION.md.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/216-saas-suite-health-churn-support-usage/216-CONTEXT.md
@.planning/phases/216-saas-suite-health-churn-support-usage/216-RESEARCH.md
@.planning/phases/216-saas-suite-health-churn-support-usage/216-REVIEWS.md
@.planning/phases/216-saas-suite-health-churn-support-usage/216-VALIDATION.md
@.planning/phases/216-saas-suite-health-churn-support-usage/216-01-PLAN.md
@.planning/phases/218-saas-growth-profile-plg-inapp-experimentation/218-RESEARCH.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@lib/markos/saas/preflight/index.cjs
@lib/markos/mcp/tools/index.cjs
@api/cron/webhooks-dlq-purge.js
@supabase/migrations/112_saas_health_explainability.sql

<interfaces>
<!-- Plan 01 ships these surfaces; Plan 02 imports them. -->

From lib/markos/saas/preflight/index.cjs (Plan 01):
```javascript
const { assertUpstreamReady, REQUIRED_UPSTREAM, SOFT_UPSTREAM } = require('./upstream-gate');
const { scanForbiddenPatterns, assertHelpersPresent } = require('./architecture-lock');
const { UpstreamPhaseNotLandedError, RetentionPolicyViolationError } = require('./errors');
```

From supabase/migrations/112_saas_health_explainability.sql (Plan 01):
```sql
-- pii_classification ENUM exists; data_retention_classes lookup table exists
-- retention_class='product_event' seeded with retention_days=365, deletion_method='hard_delete'
```

PLG Event Taxonomy (RESEARCH §Domain 2):
| Category | Downstream Consumer |
|----------|--------------------|
| activation | P218 ActivationDefinition milestones |
| adoption | P218 PQLScore |
| depth | P218 PQLScore depth factor |
| stickiness | P216 HealthScore usage dimension |
| expansion | P218 UpgradeTrigger + P219 AccountExpansionProgram |
| plg_readiness | P218 PLGReadinessScore |
| churn_signal | P216 HealthScore + P216 Plan 04 ChurnPlaybooks |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 113 (product_usage_events + product_usage_connectors) + DB-trigger PRODUCT_USAGE_INGEST_REQUIRES_TENANT_AUTH + RLS + indexes + idempotency UNIQUE</name>
  <files>supabase/migrations/113_saas_product_usage_events.sql, scripts/preconditions/216-02-check-upstream.cjs, test/fixtures/saas-216/product-usage-event.js, test/fixtures/saas-216/product-usage-connector.js, test/fixtures/saas-216/index.js</files>
  <read_first>
    - .planning/phases/216-saas-suite-health-churn-support-usage/216-RESEARCH.md §Domain 2 §product_usage_events Table Schema §product_usage_connectors §DB-Trigger Compliance §Plan 02 API and MCP Surface
    - .planning/phases/216-saas-suite-health-churn-support-usage/216-01-PLAN.md (Plan 01 ships data_retention_classes lookup + pii_classification ENUM)
    - supabase/migrations/112_saas_health_explainability.sql (data_retention_classes table reference)
  </read_first>
  <action>
    Step 1: Create supabase/migrations/113_saas_product_usage_events.sql:
      - CREATE TABLE product_usage_events (full schema per RESEARCH §Domain 2 — event_id PK, tenant_id FK markos_orgs, subscription_id, customer_id, connector_id NOT NULL FK connector_installs, external_event_id, event_name, event_category CHECK 8 values, user_id_external, account_id_external, properties jsonb, occurred_at, retention_class NOT NULL FK data_retention_classes, retention_until NOT NULL, ingested_at, ingest_source CHECK 4 values)
      - UNIQUE(tenant_id, connector_id, external_event_id) for idempotency (NULLS NOT DISTINCT)
      - ALTER TABLE product_usage_events ENABLE ROW LEVEL SECURITY + CREATE POLICY pue_tenant_isolation
      - CREATE INDEX pue_tenant_subscription_idx ON product_usage_events(tenant_id, subscription_id, occurred_at DESC)
      - CREATE INDEX pue_event_category_idx ON product_usage_events(tenant_id, event_category, occurred_at DESC)
      - CREATE INDEX pue_connector_idx ON product_usage_events(connector_id, ingested_at DESC)
      - CREATE TABLE product_usage_connectors (full schema per RESEARCH §Domain 2 — connector_id PK, tenant_id, install_id NOT NULL FK connector_installs, source_type CHECK 8 values, event_mapping jsonb, is_active boolean DEFAULT true, last_synced_at, created_at)
      - ALTER TABLE product_usage_connectors ENABLE ROW LEVEL SECURITY + CREATE POLICY puc_tenant_isolation
      - CREATE OR REPLACE FUNCTION enforce_product_usage_tenant_auth() — verbatim per RESEARCH §Domain 2 (RAISE EXCEPTION 'PRODUCT_USAGE_INGEST_REQUIRES_TENANT_AUTH' on tenant_id IS NULL OR connector_id IS NULL OR retention_class IS NULL)
      - CREATE TRIGGER trg_product_usage_tenant_auth BEFORE INSERT ON product_usage_events FOR EACH ROW EXECUTE FUNCTION enforce_product_usage_tenant_auth()

    Step 2: Create scripts/preconditions/216-02-check-upstream.cjs — invokes assertUpstreamReady from lib/markos/saas/preflight (Plan 01); additionally checks data_retention_classes table exists + pii_classification ENUM exists; exit 1 on miss.

    Step 3: Append to test/fixtures/saas-216/index.js (Plan 01 created barrel) re-exporting productUsageEventFactory + productUsageConnectorFactory; create fixture files with factories returning complete row shapes including retention_class='product_event' default.
  </action>
  <verify>
    <automated>node scripts/preconditions/216-02-check-upstream.cjs</automated>
  </verify>
  <done>Migration 113 applies cleanly; product_usage_events + product_usage_connectors tables exist with RLS + DB-trigger + idempotency UNIQUE; preflight CLI passes; fixture factories exported.</done>
</task>

<task type="auto">
  <name>Task 2: Lib surface (contracts + event-ingest + event-taxonomy + connector-adapter + health-rollup) + MCP tool + API handler + cron + contracts F-260 + F-261</name>
  <files>lib/markos/saas/usage/contracts.ts, lib/markos/saas/usage/contracts.cjs, lib/markos/saas/usage/event-ingest.ts, lib/markos/saas/usage/event-ingest.cjs, lib/markos/saas/usage/event-taxonomy.ts, lib/markos/saas/usage/connector-adapter.ts, lib/markos/saas/usage/health-rollup.ts, lib/markos/saas/usage/index.cjs, lib/markos/mcp/tools/usage.cjs, lib/markos/mcp/tools/index.cjs, api/v1/saas/usage.js, api/cron/saas-usage-health-rollup.js, contracts/F-260-saas-product-usage-event-v1.yaml, contracts/F-261-saas-product-usage-connector-v1.yaml, contracts/flow-registry.json</files>
  <read_first>
    - .planning/phases/216-saas-suite-health-churn-support-usage/216-RESEARCH.md §Domain 2 §PLG Event Taxonomy table §Plan 02 API and MCP Surface
    - lib/markos/mcp/tools/index.cjs (registry pattern; ADD usage.cjs entry)
    - api/cron/webhooks-dlq-purge.js (cron handler pattern; x-markos-cron-secret check)
    - lib/markos/saas/preflight/index.cjs (Plan 01 — import assertUpstreamReady)
  </read_first>
  <action>
    Step 1: Create lib/markos/saas/usage/contracts.ts exporting EVENT_CATEGORIES + INGEST_SOURCES + CONNECTOR_SOURCE_TYPES const arrays + ProductUsageEventSchema + ProductUsageConnectorSchema zod.

    Step 2: Create lib/markos/saas/usage/event-taxonomy.ts exporting EVENT_TAXONOMY map per RESEARCH §Domain 2 (7 categories → downstream consumer) + getDownstreamConsumer.

    Step 3: Create lib/markos/saas/usage/event-ingest.ts exporting ingestEvent — validates against zod, PII-scrubs properties (regex-based detection), INSERTs row with retention_class='product_event' + retention_until=now()+365d, returns {event_id, deduplicated} (idempotent via ON CONFLICT DO NOTHING on UNIQUE(tenant, connector, external_event)).

    Step 4: Create lib/markos/saas/usage/connector-adapter.ts exporting adaptGenericWebhook + NormalizedEventShape (event_mapping JSONB applied to source payload).

    Step 5: Create lib/markos/saas/usage/health-rollup.ts exporting rollupUsageForHealth (30d window aggregation; returns 5 health-relevant scores).

    Step 6: Create CommonJS twins + lib/markos/saas/usage/index.cjs barrel.

    Step 7: Create lib/markos/mcp/tools/usage.cjs with get_product_usage_summary descriptor; register in lib/markos/mcp/tools/index.cjs (require + push).

    Step 8: Create api/v1/saas/usage.js — POST /ingest (x-connector-auth) + GET /summary (requireHostedSupabaseAuth).

    Step 9: Create api/cron/saas-usage-health-rollup.js — x-markos-cron-secret; iterates active subscriptions; feeds saas_health_scores raw_facts.usage on next refresh; AgentRun emit.

    Step 10: Create contracts/F-260-saas-product-usage-event-v1.yaml + F-261-saas-product-usage-connector-v1.yaml; append 2 entries to flow-registry.json.
  </action>
  <verify>
    <automated>npm test -- test/saas-216/domain-2/usage-event-taxonomy.test.js test/saas-216/domain-2/usage-health-rollup.test.js</automated>
  </verify>
  <done>7 usage lib files + MCP tool + API handler + cron exist; F-260+F-261 in flow-registry; usage-event-taxonomy + usage-health-rollup tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Domain-2 test files + RLS cross-tenant denial + 216-VALIDATION.md update</name>
  <files>test/saas-216/domain-2/usage-ingest-connector-auth.test.js, test/saas-216/domain-2/usage-ingest-tenant-trigger.test.js, test/saas-216/domain-2/usage-event-taxonomy.test.js, test/saas-216/domain-2/usage-health-rollup.test.js, test/saas-216/domain-2/usage-retention-trigger.test.js, test/saas-216/rls/product-usage-events-cross-tenant-denial.test.js, .planning/phases/216-saas-suite-health-churn-support-usage/216-VALIDATION.md</files>
  <read_first>
    - .planning/phases/216-saas-suite-health-churn-support-usage/216-RESEARCH.md §Validation Architecture (test map for SAS-07 usage axis)
    - .planning/phases/216-saas-suite-health-churn-support-usage/216-01-PLAN.md (Plan 01 architecture-lock test pattern reference)
  </read_first>
  <action>
    Step 1: Create test/saas-216/domain-2/usage-ingest-connector-auth.test.js — Postgres-backed: INSERT product_usage_events without connector_id → expect EXCEPTION 'PRODUCT_USAGE_INGEST_REQUIRES_TENANT_AUTH'; without tenant_id → same; without retention_class → same; valid → succeeds.

    Step 2: Create usage-ingest-tenant-trigger.test.js — same as above but specifically for tenant_id null path; idempotency check (duplicate external_event_id returns deduplicated=true).

    Step 3: Create usage-event-taxonomy.test.js — EVENT_CATEGORIES = 8 entries; getDownstreamConsumer covers all 7 active categories per RESEARCH §Domain 2 table.

    Step 4: Create usage-health-rollup.test.js — fixture-based: insert 50 events across 30d window; rollupUsageForHealth returns expected 5 scores; empty window returns zeros.

    Step 5: Create usage-retention-trigger.test.js — FK constraint: INSERT with retention_class='unknown' fails with FK violation; INSERT with 'product_event' (Plan 01 seeded) succeeds.

    Step 6: Create test/saas-216/rls/product-usage-events-cross-tenant-denial.test.js — insert under A, select under B returns 0 rows for both product_usage_events and product_usage_connectors.

    Step 7: Append Plan 02 row to 216-VALIDATION.md per-task verification map.
  </action>
  <verify>
    <automated>npm test -- test/saas-216/domain-2/ test/saas-216/rls/product-usage-events-cross-tenant-denial.test.js</automated>
  </verify>
  <done>5 domain-2 test files + 1 RLS test green; 216-VALIDATION.md per-task map updated for Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| connector→/v1/saas/usage/ingest | x-connector-auth header verifies connector_install ownership (P210 substrate) |
| client→/v1/saas/usage/summary | requireHostedSupabaseAuth tenant-scoped read |
| cron→saas-usage-health-rollup | x-markos-cron-secret service-role rollup |
| product event payload→properties JSONB | PII-scrubbing regex (defense-in-depth before retention class enforcement) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-216-07 | S | /v1/saas/usage/ingest | mitigate | x-connector-auth + connector_id validation against connector_installs (P210); reject if mismatch |
| T-216-08 | T | product_usage_events INSERT path | mitigate | PRODUCT_USAGE_INGEST_REQUIRES_TENANT_AUTH DB-trigger blocks events without tenant_id+connector_id+retention_class |
| T-216-09 | I | properties JSONB PII leak | mitigate | scrubPII regex applied before INSERT; pseudonymous retention class; user_id_external is hashed |
| T-216-10 | I | cross-tenant event SELECT | mitigate | RLS policy pue_tenant_isolation; cross-tenant denial test in CI |
| T-216-11 | D | event ingest flood | accept | UNIQUE(tenant, connector, external_event) prevents duplicates; rate-limiting at LB layer |
| T-216-12 | E | connector_install spoofing | mitigate | x-connector-auth header is per-connector_install secret (P210 substrate); not bypassable via tenant API key |
</threat_model>

<verification>
- node scripts/preconditions/216-02-check-upstream.cjs (exits 0 when Plan 01 + P210 ready)
- npm test -- test/saas-216/domain-2/
- npm test -- test/saas-216/rls/product-usage-events-cross-tenant-denial.test.js
- supabase migration up to slot 113 applies cleanly
- flow-registry.json contains F-260 + F-261 with no overlap
</verification>

<success_criteria>
- product_usage_events + product_usage_connectors tables exist with RLS + PRODUCT_USAGE_INGEST_REQUIRES_TENANT_AUTH trigger + idempotency UNIQUE
- ingestEvent validates + PII-scrubs + INSERTs with retention_class='product_event'
- 7 PLG event categories defined and mapped to downstream consumers (P218 SG-02)
- saas-usage-health-rollup cron feeds saas_health_scores raw_facts.usage
- F-260 + F-261 contracts in flow-registry
- 5 domain-2 tests + 1 RLS test green
- 216-VALIDATION.md updated for Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/216-saas-suite-health-churn-support-usage/216-02-SUMMARY.md` documenting: artifacts created, DB-trigger exception name, PLG event taxonomy locked, downstream P218/P219 column contract.
</output>
