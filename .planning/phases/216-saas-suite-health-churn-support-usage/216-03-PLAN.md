---
phase: 216
plan: "03"
type: execute
wave: 2
depends_on: ["01"]
f_ids:
  - F-262
  - F-263
migration_slots:
  - 114
integrates_with:
  - "CONN-01..06 from P210 (connector_installs FK + generic ticket import substrate)"
  - "EVD-01..06 from P209 (kb_grounding_evidence uuid[] reference evidence_map_records)"
  - "TASK-01..05 from P208 (approval_id FK to agent_approval_packages)"
  - "SAS-09 from P217 (P217 surfaces support intel via /v1/saas/support)"
translation_gate_for: []
files_modified:
  - test/fixtures/saas-216/index.js
  - test/fixtures/saas-216/support-ticket.js
  - test/fixtures/saas-216/support-kb-grounding.js
  - scripts/preconditions/216-03-check-upstream.cjs
  - supabase/migrations/114_saas_support_tickets.sql
  - lib/markos/saas/support/contracts.ts
  - lib/markos/saas/support/contracts.cjs
  - lib/markos/saas/support/ticket-import.ts
  - lib/markos/saas/support/ticket-import.cjs
  - lib/markos/saas/support/classifier.ts
  - lib/markos/saas/support/kb-grounding.ts
  - lib/markos/saas/support/response-approval.ts
  - lib/markos/saas/support/index.cjs
  - lib/markos/mcp/tools/support.cjs
  - lib/markos/mcp/tools/index.cjs
  - api/v1/saas/support.js
  - contracts/F-262-saas-support-ticket-v1.yaml
  - contracts/F-263-saas-support-kb-grounding-v1.yaml
  - contracts/flow-registry.json
  - test/saas-216/domain-3/support-ticket-shape.test.js
  - test/saas-216/domain-3/support-retention-trigger.test.js
  - test/saas-216/domain-3/support-response-trigger.test.js
  - test/saas-216/domain-3/kb-grounding-threshold.test.js
  - test/saas-216/domain-3/support-classifier.test.js
  - test/saas-216/rls/support-tickets-cross-tenant-denial.test.js
  - .planning/phases/216-saas-suite-health-churn-support-usage/216-VALIDATION.md
autonomous: true
requirements:
  - SAS-08
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - QA-05
  - QA-06
  - QA-07
  - QA-08
  - QA-09
must_haves:
  truths:
    - "Plan 03 depends on Plan 01 — assertUpstreamReady from lib/markos/saas/preflight (Plan 01) is invoked before any Plan 03 task; data_retention_classes table + pii_classification ENUM (Plan 01 migration 112) referenced as FK by retention_class column with retention_class='support_text' (730d/anonymize/sensitive) seeded in Plan 01"
    - "Migration 114 ships saas_support_tickets + support_kb_groundings tables — RLS-enabled tenant_id-scoped per markos_orgs FK; saas_support_tickets columns per RESEARCH §Domain 3 schema: ticket_id PK, tenant_id, customer_id, subscription_id, ticket_number, channel CHECK ('email','chat','portal','social','phone','api','import'), status CHECK 6 values, priority CHECK 4 values, subject NOT NULL, description (PII-redactable), category CHECK 7 values, tags text[], sentiment CHECK 4 values, intent CHECK 7 values, churn_signal boolean DEFAULT false, churn_signal_reason, topic_tags text[], urgency_score numeric(3,2), suggested_response, suggested_kb_articles text[], kb_grounding_evidence uuid[], response_grounding_confidence numeric(3,2), approval_id (FK agent_approval_packages P208), response_dispatched_at, response_dispatch_method CHECK ('approved','safe_auto_response',null), assigned_to, assigned_at, first_response_sla_hours DEFAULT 8, resolution_sla_hours DEFAULT 24, first_response_at, resolved_at, sla_breached boolean DEFAULT false, csat_requested_at, csat_score CHECK 1-5, csat_comment, health_score_at_open, mrr_at_open, retention_class NOT NULL FK data_retention_classes, retention_until NOT NULL, connector_id FK connector_installs, external_ticket_id, created_at, updated_at"
    - "support_kb_groundings table — grounding_id PK, tenant_id, ticket_id NOT NULL FK saas_support_tickets ON DELETE CASCADE, kb_source_type CHECK ('literacy_chunk','knowledge_article','previous_resolution','external_url'), kb_source_id NOT NULL, kb_source_snippet, relevance_score numeric(3,2), created_at — RLS-enabled tenant_id-scoped"
    - "DB-trigger compliance — `SUPPORT_RESPONSE_REQUIRES_CS_APPROVAL_OR_SAFE_AUTO_FLAG` BEFORE UPDATE on saas_support_tickets raises EXCEPTION when response_dispatched_at is being SET (NEW.response_dispatched_at IS NOT NULL AND OLD.response_dispatched_at IS NULL) AND approval_id IS NULL AND response_dispatch_method IS DISTINCT FROM 'safe_auto_response' (verbatim per RESEARCH §Domain 3 — STRICTEST of 5 compliance triggers; also rejects double-set: approval_id AND safe_auto_response together is invalid)"
    - "Generic-first ticket import (DISCUSS Decisions Locked) — P216 ships POST /v1/saas/support/import (CSV/JSON batch) + POST /v1/saas/support/webhook/:connector_id (live webhook) per Q-3 RESOLVED 2026-04-26; Intercom/Zendesk/HelpScout/HubSpot adapters deferred to v4.2.0; webhook auth via x-connector-auth header (P210 substrate)"
    - "Classifier (lib/markos/saas/support/classifier.ts) produces sentiment + intent + churn_signal + topic_tags + urgency_score from ticket subject+description; classifier output is INPUT to ticket row, not external service call (rule-based v1; ML deferred per DISCUSS); churn_signal=true when intent IN ('cancel','complaint') OR urgency_score > 0.8"
    - "KB grounding (lib/markos/saas/support/kb-grounding.ts) — searches markos_literacy_chunks (P212 substrate; SOFT — graceful degrade if missing) + previous resolutions; returns top-N kb_source_snippets with relevance_score; response_grounding_confidence < threshold (default 0.7 — DISCUSS Claude's Discretion) blocks auto-draft display per CONTEXT.md non-negotiable"
    - "Approval gate (lib/markos/saas/support/response-approval.ts) — calls buildApprovalPackage from lib/markos/crm/agent-actions.ts:68 with kind='saas_support_response_dispatch' before response_dispatched_at can be set; only 2 paths to dispatch: (a) approval_id NOT NULL OR (b) tenant has saas_suite_activations.tenant_safe_auto_response_enabled=true AND response_dispatch_method='safe_auto_response'; tenant_safe_auto_response_enabled flag set ONLY by operator (not AI agent) per RESEARCH §Manual-Only Verifications"
    - "tenant_safe_auto_response_enabled flag — per Plan 03 [ASSUMED A6] check P214 saas_suite_activations for column; if missing, ALTER TABLE ADD COLUMN in migration 114 (ALTER ONLY — does NOT redefine P214 table); document assumption resolution in Plan 03 SUMMARY"
    - "2 contracts ship — F-262 (saas-support-ticket-v1) covering POST /v1/saas/support/import + GET /v1/saas/support (paginated by status/priority/churn_signal) + GET /v1/saas/support/:ticket_id (full payload) + POST /v1/saas/support/:ticket_id/approve-response; F-263 (saas-support-kb-grounding-v1) covering KB grounding contract + relevance_score threshold; NO collision with F-001..F-261"
    - "MCP tools registered in `lib/markos/mcp/tools/support.cjs` (CommonJS) + lib/markos/mcp/tools/index.cjs registry: get_support_patterns (topic clustering, sentiment trends, SLA breach rate) + draft_support_response (returns AI-drafted response with KB grounding — NOT dispatched; surfaces approval requirement)"
    - "Cross-tenant denial test asserts INSERT under tenant A then SELECT under tenant B returns 0 rows for saas_support_tickets + support_kb_groundings"
    - "EVD-01..06 + TASK-01..05 + CONN-01..06 + SAS-09 documented in frontmatter as `integrates_with` (NOT `requirements`) — REVIEWS.md ownership flips per REQUIREMENTS.md (P209 owns EVD; P208 owns TASK; P210 owns CONN; P217 owns SAS-09)"
  artifacts:
    - path: scripts/preconditions/216-03-check-upstream.cjs
      provides: invokes assertUpstreamReady from lib/markos/saas/preflight (Plan 01); additionally hard-checks data_retention_classes + agent_approval_packages (P208) + connector_installs (P210); soft-checks markos_literacy_chunks (P212 — KB grounding degrades to previous-resolution-only if missing); exit 1 on hard miss
    - path: supabase/migrations/114_saas_support_tickets.sql
      provides: combined migration — saas_support_tickets + support_kb_groundings + SUPPORT_RESPONSE_REQUIRES_CS_APPROVAL_OR_SAFE_AUTO_FLAG DB-trigger + RLS policies + indexes (tenant_id+status+created_at, tenant_id+churn_signal+created_at, tenant_id+customer_id+created_at) + ALTER TABLE saas_suite_activations ADD COLUMN tenant_safe_auto_response_enabled boolean DEFAULT false (if column missing — A6 resolution); FK to data_retention_classes (Plan 01) + connector_installs (P210) + agent_approval_packages (P208)
    - path: lib/markos/saas/support/contracts.ts
      provides: zod schemas + enum constants — TICKET_CHANNELS (7) + TICKET_STATUSES (6) + TICKET_PRIORITIES (4) + TICKET_CATEGORIES (7) + TICKET_SENTIMENTS (4) + TICKET_INTENTS (7) + RESPONSE_DISPATCH_METHODS ('approved','safe_auto_response') + KB_SOURCE_TYPES (4); SaaSSupportTicketSchema + SupportKbGroundingSchema zod
      exports: [TICKET_CHANNELS, TICKET_STATUSES, TICKET_PRIORITIES, TICKET_CATEGORIES, TICKET_SENTIMENTS, TICKET_INTENTS, RESPONSE_DISPATCH_METHODS, KB_SOURCE_TYPES, SaaSSupportTicketSchema, SupportKbGroundingSchema]
    - path: lib/markos/saas/support/ticket-import.ts
      provides: importTicket({ tenantId, connectorId, ticketShape }, supabase) — validates against zod; calls classifier; INSERTs row with retention_class='support_text' + retention_until=now()+730d; returns {ticket_id, classification}
      exports: [importTicket, importBatch]
    - path: lib/markos/saas/support/classifier.ts
      provides: classifyTicket({ subject, description, category }) returns {sentiment, intent, churn_signal, topic_tags, urgency_score}; rule-based v1 (regex + keyword); ML deferred to v4.2.0; churn_signal logic per RESEARCH
      exports: [classifyTicket, CHURN_SIGNAL_THRESHOLDS]
    - path: lib/markos/saas/support/kb-grounding.ts
      provides: groundResponse({ tenantId, ticketId, query }, supabase) — searches markos_literacy_chunks (SOFT) + saas_support_tickets resolved=true (previous resolutions); returns top-N with relevance_score; computes response_grounding_confidence
      exports: [groundResponse, KB_CONFIDENCE_THRESHOLD]
    - path: lib/markos/saas/support/response-approval.ts
      provides: requestResponseApproval({ tenantId, ticketId, suggestedResponse, kbGrounding }) — calls buildApprovalPackage from lib/markos/crm/agent-actions; checks tenant_safe_auto_response_enabled flag; returns {approval_id, dispatch_path: 'approval'|'safe_auto'}; dispatchResponse({ ticketId }, supabase) — UPDATEs response_dispatched_at + response_dispatch_method; DB-trigger validates approval gate
      exports: [requestResponseApproval, dispatchResponse, isSafeAutoEnabled]
    - path: lib/markos/saas/support/index.cjs
      provides: CommonJS twin re-exporting full surface
    - path: lib/markos/mcp/tools/support.cjs
      provides: 2 MCP tool descriptors get_support_patterns + draft_support_response; registered into lib/markos/mcp/tools/index.cjs
    - path: api/v1/saas/support.js
      provides: POST /import (x-connector-auth or requireHostedSupabaseAuth for batch upload) + POST /webhook/:connector_id (x-connector-auth for live ingest) + GET / (paginated) + GET /:ticket_id (full) + POST /:ticket_id/approve-response (approval gate handler)
    - path: contracts/F-262-saas-support-ticket-v1.yaml
      provides: 4-route contract (import + list + detail + approve-response); SaaSSupportTicketSchema parity
    - path: contracts/F-263-saas-support-kb-grounding-v1.yaml
      provides: KB grounding contract + relevance_score threshold + kb_source_snippet shape
    - path: contracts/flow-registry.json
      provides: 2 entries appended for F-262 + F-263
    - path: test/saas-216/domain-3/support-response-trigger.test.js
      provides: Postgres-backed test — UPDATE saas_support_tickets SET response_dispatched_at=now() with approval_id IS NULL AND response_dispatch_method != 'safe_auto_response' → expect EXCEPTION 'SUPPORT_RESPONSE_REQUIRES_CS_APPROVAL_OR_SAFE_AUTO_FLAG'; with approval_id NOT NULL → succeeds; with response_dispatch_method='safe_auto_response' AND approval_id IS NULL AND tenant flag enabled → succeeds; with both approval_id AND safe_auto_response → expect EXCEPTION
    - path: test/saas-216/domain-3/support-retention-trigger.test.js
      provides: INSERT saas_support_tickets without retention_class → expect FK violation OR Plan 05 trigger; with retention_class='support_text' → succeeds
    - path: test/saas-216/domain-3/kb-grounding-threshold.test.js
      provides: groundResponse unit test — confidence < 0.7 returns blocked=true; ≥ 0.7 returns blocked=false; missing markos_literacy_chunks gracefully degrades
    - path: test/saas-216/domain-3/support-classifier.test.js
      provides: classifyTicket unit tests covering 4 sentiments + 7 intents + churn_signal logic + urgency_score thresholds
    - path: test/saas-216/rls/support-tickets-cross-tenant-denial.test.js
      provides: cross-tenant RLS denial — INSERT under A; SELECT under B returns 0 rows
  key_links:
    - from: lib/markos/saas/support/response-approval.ts
      to: lib/markos/crm/agent-actions.ts (buildApprovalPackage)
      via: requestResponseApproval calls buildApprovalPackage({kind:'saas_support_response_dispatch', ...}) — defense-in-depth pair with DB-trigger
      pattern: "buildApprovalPackage"
    - from: lib/markos/saas/support/response-approval.ts
      to: supabase/migrations/114_saas_support_tickets.sql
      via: dispatchResponse UPDATE on saas_support_tickets fires trg_support_response_approval; if approval_id missing AND not safe_auto_response, RAISE EXCEPTION
      pattern: "SUPPORT_RESPONSE_REQUIRES_CS_APPROVAL_OR_SAFE_AUTO_FLAG"
    - from: lib/markos/saas/support/kb-grounding.ts
      to: supabase markos_literacy_chunks (P212 SOFT) + saas_support_tickets (resolved tickets)
      via: SELECT from markos_literacy_chunks first; fallback to previous resolutions if P212 missing
      pattern: "kb_source_type"
    - from: lib/markos/saas/support/ticket-import.ts
      to: lib/markos/saas/support/classifier.ts
      via: importTicket calls classifyTicket on subject+description; classification result stamped onto INSERT
      pattern: "classifyTicket"
    - from: contracts/F-262-saas-support-ticket-v1.yaml
      to: lib/markos/saas/support/contracts.ts
      via: SaaSSupportTicketSchema parity-tested against F-262 YAML
      pattern: "SaaSSupportTicketSchema"
---

<objective>
Ship Wave 2 SaaS support ticket intelligence — generic ticket import (CSV/JSON + webhook), classifier (sentiment/intent/churn_signal/urgency/topic_tags), KB grounding (literacy chunks + previous resolutions), and the STRICTEST DB-trigger compliance gate (SUPPORT_RESPONSE_REQUIRES_CS_APPROVAL_OR_SAFE_AUTO_FLAG). Plan 03 satisfies SAS-08 (per RESEARCH §Domain 3 — SaaS support intelligence ingests tickets, classifies risk/topic/SLA/sentiment, grounds suggested responses in knowledge, and requires CS approval before customer-facing replies unless safe auto-response is configured). The CONTEXT.md non-negotiable "No customer-facing support response without CS review unless safe auto-response is explicitly configured" is enforced via 2-layer defense: app-layer requestResponseApproval calls buildApprovalPackage from lib/markos/crm/agent-actions.ts:68; DB-layer trg_support_response_approval blocks any UPDATE that sets response_dispatched_at without either approval_id OR safe_auto_response dispatch method.

Generic-first ticket import locked in DISCUSS Decisions Locked + Q-3 RESOLVED 2026-04-26; Intercom/Zendesk/HelpScout/HubSpot adapters deferred to v4.2.0. Two ingest paths: POST /v1/saas/support/import (CSV/JSON batch) and POST /v1/saas/support/webhook/:connector_id (live webhook with x-connector-auth from P210 substrate). KB grounding minimum confidence default 0.7 (DISCUSS Claude's Discretion; CS-team-reviewable per RESEARCH §Manual-Only Verifications).

Plan 03 reuses lib/markos/saas/preflight (Plan 01) — assertUpstreamReady hard-checks P208 + P210 + Plan 01 data_retention_classes; soft-checks P212 markos_literacy_chunks (KB grounding degrades to previous-resolution-only if missing). EVD-01..06 + TASK-01..05 + CONN-01..06 + SAS-09 documented as integrates_with per REVIEWS.md ownership flips.

Output: 1 preflight CLI + 1 SOR migration (114 — saas_support_tickets + support_kb_groundings + 1 ALTER TABLE on P214 for tenant_safe_auto_response_enabled flag) + 7 support lib files + 1 MCP tool (2 descriptors) + 1 API handler (4 routes) + 2 contract YAMLs + flow-registry update + 5 domain-3 test files + 1 RLS test + populated VALIDATION.md.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/216-saas-suite-health-churn-support-usage/216-CONTEXT.md
@.planning/phases/216-saas-suite-health-churn-support-usage/216-RESEARCH.md
@.planning/phases/216-saas-suite-health-churn-support-usage/216-REVIEWS.md
@.planning/phases/216-saas-suite-health-churn-support-usage/216-VALIDATION.md
@.planning/phases/216-saas-suite-health-churn-support-usage/216-01-PLAN.md
@.planning/phases/216-saas-suite-health-churn-support-usage/DISCUSS.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@obsidian/brain/SaaS Suite Canon.md
@obsidian/reference/MarkOS v2 Operating Loop Spec.md
@lib/markos/crm/agent-actions.ts
@lib/markos/saas/preflight/index.cjs
@lib/markos/mcp/tools/index.cjs
@supabase/migrations/112_saas_health_explainability.sql

<interfaces>
<!-- Plan 01 ships: assertUpstreamReady, data_retention_classes lookup, pii_classification ENUM. -->

From lib/markos/crm/agent-actions.ts:68:
```typescript
function buildApprovalPackage(input: BuildApprovalPackageInput): ApprovalPackage
// Plan 03 calls with kind='saas_support_response_dispatch'
```

From lib/markos/saas/preflight/index.cjs (Plan 01):
```javascript
const { assertUpstreamReady, REQUIRED_UPSTREAM, SOFT_UPSTREAM } = require('./upstream-gate');
```

Ticket category taxonomy (RESEARCH §Domain 3): billing | technical | feature_request | onboarding | account | cancellation | other
Intent taxonomy: get_help | report_bug | request_feature | complaint | cancel | upgrade | billing_dispute
Sentiment: positive | negative | neutral | frustrated
Channel: email | chat | portal | social | phone | api | import
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 114 (saas_support_tickets + support_kb_groundings + SUPPORT_RESPONSE_REQUIRES_CS_APPROVAL_OR_SAFE_AUTO_FLAG trigger + ALTER P214 for safe_auto flag)</name>
  <files>supabase/migrations/114_saas_support_tickets.sql, scripts/preconditions/216-03-check-upstream.cjs, test/fixtures/saas-216/support-ticket.js, test/fixtures/saas-216/support-kb-grounding.js, test/fixtures/saas-216/index.js</files>
  <read_first>
    - .planning/phases/216-saas-suite-health-churn-support-usage/216-RESEARCH.md §Domain 3 §saas_support_tickets Table Schema §support_kb_groundings §DB-Trigger Compliance §Plan 03 API and MCP Surface
    - .planning/phases/216-saas-suite-health-churn-support-usage/216-RESEARCH.md §Assumptions Log A6 (tenant_safe_auto_response_enabled flag — verify P214 column; ALTER if missing)
    - .planning/phases/216-saas-suite-health-churn-support-usage/216-01-PLAN.md (data_retention_classes seed includes 'support_text')
    - supabase/migrations/112_saas_health_explainability.sql (data_retention_classes table reference)
  </read_first>
  <action>
    Step 1: Create supabase/migrations/114_saas_support_tickets.sql:
      - Pre-flight DO-block: IF NOT EXISTS column tenant_safe_auto_response_enabled in saas_suite_activations THEN ALTER TABLE saas_suite_activations ADD COLUMN tenant_safe_auto_response_enabled boolean DEFAULT false NOT NULL — A6 resolution
      - CREATE TABLE saas_support_tickets (full schema per RESEARCH §Domain 3 — 35+ columns including all enums, retention_class FK data_retention_classes NOT NULL, retention_until NOT NULL, approval_id FK agent_approval_packages, connector_id FK connector_installs)
      - ALTER TABLE saas_support_tickets ENABLE ROW LEVEL SECURITY + CREATE POLICY sst_tenant_isolation
      - CREATE INDEX sst_tenant_status_idx + sst_churn_signal_idx + sst_customer_idx
      - CREATE TABLE support_kb_groundings (grounding_id PK, tenant_id, ticket_id NOT NULL FK ON DELETE CASCADE, kb_source_type CHECK 4, kb_source_id NOT NULL, kb_source_snippet, relevance_score numeric(3,2), created_at)
      - ALTER TABLE support_kb_groundings ENABLE RLS + CREATE POLICY skg_tenant_isolation
      - CREATE OR REPLACE FUNCTION enforce_support_response_approval() — verbatim per RESEARCH §Domain 3 (RAISE EXCEPTION 'SUPPORT_RESPONSE_REQUIRES_CS_APPROVAL_OR_SAFE_AUTO_FLAG' when response_dispatched_at is being SET AND (approval_id IS NULL AND response_dispatch_method IS DISTINCT FROM 'safe_auto_response') OR (approval_id IS NOT NULL AND response_dispatch_method = 'safe_auto_response'))
      - CREATE TRIGGER trg_support_response_approval BEFORE UPDATE ON saas_support_tickets FOR EACH ROW EXECUTE FUNCTION enforce_support_response_approval()

    Step 2: Create scripts/preconditions/216-03-check-upstream.cjs — invokes assertUpstreamReady; additionally hard-checks data_retention_classes + agent_approval_packages + connector_installs; soft-checks markos_literacy_chunks; exit 1 on hard miss.

    Step 3: Append to test/fixtures/saas-216/index.js re-exporting supportTicketFactory + supportKbGroundingFactory; create fixture files with factories returning valid row shapes including retention_class='support_text' default + sample classifier output.
  </action>
  <verify>
    <automated>node scripts/preconditions/216-03-check-upstream.cjs</automated>
  </verify>
  <done>Migration 114 applies; saas_support_tickets + support_kb_groundings exist with RLS + DB-trigger; tenant_safe_auto_response_enabled flag added to saas_suite_activations (or pre-existed); preflight CLI passes; fixture factories exported.</done>
</task>

<task type="auto">
  <name>Task 2: Lib surface (contracts + ticket-import + classifier + kb-grounding + response-approval) + MCP tools + API handler + contracts F-262 + F-263</name>
  <files>lib/markos/saas/support/contracts.ts, lib/markos/saas/support/contracts.cjs, lib/markos/saas/support/ticket-import.ts, lib/markos/saas/support/ticket-import.cjs, lib/markos/saas/support/classifier.ts, lib/markos/saas/support/kb-grounding.ts, lib/markos/saas/support/response-approval.ts, lib/markos/saas/support/index.cjs, lib/markos/mcp/tools/support.cjs, lib/markos/mcp/tools/index.cjs, api/v1/saas/support.js, contracts/F-262-saas-support-ticket-v1.yaml, contracts/F-263-saas-support-kb-grounding-v1.yaml, contracts/flow-registry.json</files>
  <read_first>
    - .planning/phases/216-saas-suite-health-churn-support-usage/216-RESEARCH.md §Domain 3 §Plan 03 API and MCP Surface
    - lib/markos/crm/agent-actions.ts (buildApprovalPackage signature)
    - lib/markos/saas/preflight/index.cjs (Plan 01 surface)
    - lib/markos/mcp/tools/index.cjs (registry pattern)
  </read_first>
  <action>
    Step 1: Create lib/markos/saas/support/contracts.ts exporting all 7 enum constants + SaaSSupportTicketSchema + SupportKbGroundingSchema zod.

    Step 2: Create lib/markos/saas/support/classifier.ts exporting classifyTicket — rule-based v1: keyword regex for sentiment (frustrated/negative if matches /angry|frustrated|terrible/, etc.); intent inferred from category + keywords (cancel if /cancel|terminat|unsubscrib/); churn_signal=true when intent IN ('cancel','complaint') OR urgency_score > 0.8; urgency_score from priority + recency.

    Step 3: Create lib/markos/saas/support/kb-grounding.ts exporting groundResponse — searches markos_literacy_chunks first (SOFT), falls back to previous resolved tickets; returns top-3 with relevance_score; computes response_grounding_confidence = avg(top-3 relevance); KB_CONFIDENCE_THRESHOLD = 0.7.

    Step 4: Create lib/markos/saas/support/response-approval.ts exporting requestResponseApproval (calls buildApprovalPackage with kind='saas_support_response_dispatch'; checks tenant_safe_auto_response_enabled flag; returns dispatch_path) + dispatchResponse (UPDATE setting response_dispatched_at + dispatch_method; DB-trigger validates) + isSafeAutoEnabled.

    Step 5: Create lib/markos/saas/support/ticket-import.ts exporting importTicket + importBatch — validates against zod, calls classifyTicket, INSERTs with retention_class='support_text' + retention_until=now()+730d.

    Step 6: Create CommonJS twins + lib/markos/saas/support/index.cjs barrel.

    Step 7: Create lib/markos/mcp/tools/support.cjs with get_support_patterns + draft_support_response descriptors; register in lib/markos/mcp/tools/index.cjs.

    Step 8: Create api/v1/saas/support.js — 5 routes: POST /import + POST /webhook/:connector_id + GET / + GET /:ticket_id + POST /:ticket_id/approve-response.

    Step 9: Create contracts/F-262 + F-263 YAML; append 2 entries to flow-registry.json.
  </action>
  <verify>
    <automated>npm test -- test/saas-216/domain-3/support-classifier.test.js test/saas-216/domain-3/kb-grounding-threshold.test.js</automated>
  </verify>
  <done>7 support lib files + 2 MCP tools + 5-route API handler exist; F-262+F-263 in flow-registry; classifier + KB grounding tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Domain-3 test files (5 tests) + RLS denial + 216-VALIDATION.md update</name>
  <files>test/saas-216/domain-3/support-ticket-shape.test.js, test/saas-216/domain-3/support-retention-trigger.test.js, test/saas-216/domain-3/support-response-trigger.test.js, test/saas-216/domain-3/kb-grounding-threshold.test.js, test/saas-216/domain-3/support-classifier.test.js, test/saas-216/rls/support-tickets-cross-tenant-denial.test.js, .planning/phases/216-saas-suite-health-churn-support-usage/216-VALIDATION.md</files>
  <read_first>
    - .planning/phases/216-saas-suite-health-churn-support-usage/216-RESEARCH.md §Validation Architecture (test map for SAS-08)
  </read_first>
  <action>
    Step 1: Create support-ticket-shape.test.js — zod parity assertion against F-262 YAML; covers all 35+ columns; retention_class FK constraint test.

    Step 2: Create support-retention-trigger.test.js — INSERT without retention_class fails with FK violation; with retention_class='support_text' (Plan 01 seed) succeeds.

    Step 3: Create support-response-trigger.test.js — UPDATE saas_support_tickets SET response_dispatched_at=now() with approval_id IS NULL AND response_dispatch_method != 'safe_auto_response' → expect EXCEPTION 'SUPPORT_RESPONSE_REQUIRES_CS_APPROVAL_OR_SAFE_AUTO_FLAG'; with approval_id NOT NULL → succeeds; with response_dispatch_method='safe_auto_response' AND approval_id IS NULL → succeeds; with both approval_id AND safe_auto_response → expect EXCEPTION.

    Step 4: Create kb-grounding-threshold.test.js — fixture: 3 KB sources with relevance_scores [0.9, 0.8, 0.7] → confidence = 0.8 → blocked=false; with [0.5, 0.4, 0.3] → confidence = 0.4 → blocked=true; markos_literacy_chunks missing → graceful degrade to previous-resolution lookup.

    Step 5: Create support-classifier.test.js — sentiment detection: 'this is terrible' → frustrated; 'thank you' → positive; intent detection: 'I want to cancel' → cancel; churn_signal logic: intent='cancel' → churn_signal=true; urgency_score=0.85 → churn_signal=true regardless of intent.

    Step 6: Create test/saas-216/rls/support-tickets-cross-tenant-denial.test.js — INSERT under A; SELECT under B returns 0 rows for both tables.

    Step 7: Append Plan 03 row to 216-VALIDATION.md per-task verification map.
  </action>
  <verify>
    <automated>npm test -- test/saas-216/domain-3/ test/saas-216/rls/support-tickets-cross-tenant-denial.test.js</automated>
  </verify>
  <done>5 domain-3 tests + 1 RLS test green; 216-VALIDATION.md updated for Plan 03; all 4 EXCEPTION paths verified for SUPPORT_RESPONSE_REQUIRES_CS_APPROVAL_OR_SAFE_AUTO_FLAG trigger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| connector→/v1/saas/support/webhook | x-connector-auth header verifies connector_install ownership (P210) |
| client→/v1/saas/support/* | requireHostedSupabaseAuth tenant-scoped read |
| operator→tenant_safe_auto_response_enabled flag | ONLY operator sets (not AI agent); P214 flag mutation gated separately |
| AI agent→suggested_response | NEVER auto-dispatched; approval gate required unless safe_auto_response + flag |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-216-13 | S | /v1/saas/support/webhook | mitigate | x-connector-auth + connector_id validation against connector_installs |
| T-216-14 | T | response_dispatched_at UPDATE path | mitigate | SUPPORT_RESPONSE_REQUIRES_CS_APPROVAL_OR_SAFE_AUTO_FLAG DB-trigger blocks unauthorized dispatch |
| T-216-15 | R | response_dispatch_method='safe_auto_response' tampering | mitigate | tenant_safe_auto_response_enabled flag set ONLY by operator (audit logged); AI agent cannot toggle |
| T-216-16 | I | description PII leak | mitigate | retention_class='support_text' (730d/anonymize); review_required_at_days=700 flags for review |
| T-216-17 | I | cross-tenant ticket SELECT | mitigate | RLS policy sst_tenant_isolation; cross-tenant denial test in CI |
| T-216-18 | D | KB grounding flood | accept | groundResponse limits top-N=3; relevance threshold prevents low-quality auto-drafts |
| T-216-19 | E | safe_auto_response without flag | mitigate | DB-trigger checks dispatch_method+approval combo; flag column controlled by operator only |
</threat_model>

<verification>
- node scripts/preconditions/216-03-check-upstream.cjs (exits 0 when Plan 01 + P208 + P210 ready)
- npm test -- test/saas-216/domain-3/
- npm test -- test/saas-216/rls/support-tickets-cross-tenant-denial.test.js
- supabase migration up to slot 114 applies cleanly
- flow-registry.json contains F-262 + F-263 with no overlap
- ALTER TABLE saas_suite_activations adds tenant_safe_auto_response_enabled (idempotent)
</verification>

<success_criteria>
- saas_support_tickets + support_kb_groundings tables exist with RLS + SUPPORT_RESPONSE_REQUIRES_CS_APPROVAL_OR_SAFE_AUTO_FLAG trigger
- importTicket validates + classifies + INSERTs with retention_class='support_text'
- requestResponseApproval calls buildApprovalPackage; dispatchResponse blocked by trigger without approval
- groundResponse confidence threshold blocks auto-draft below 0.7
- F-262 + F-263 contracts in flow-registry
- 5 domain-3 tests + 1 RLS test green (all 4 EXCEPTION paths covered)
- 216-VALIDATION.md updated for Plan 03
</success_criteria>

<output>
After completion, create `.planning/phases/216-saas-suite-health-churn-support-usage/216-03-SUMMARY.md` documenting: artifacts created, DB-trigger exception name (STRICTEST of 5), A6 resolution (tenant_safe_auto_response_enabled flag location), generic-first import strategy, KB grounding threshold default.
</output>
