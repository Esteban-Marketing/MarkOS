---
phase: 216
plan: "05"
type: execute
wave: 3
depends_on: ["01", "02", "03", "04"]
f_ids:
  - F-266
  - F-267
migration_slots:
  - 116
integrates_with:
  - "COMP-01 from P206 (compliance/privacy substrate; redaction patterns)"
  - "Downstream INTEGRATES_BY: P217-P228 ALL (every PII-containing table FK references data_retention_classes seeded here)"
translation_gate_for: []
files_modified:
  - test/fixtures/saas-216/index.js
  - test/fixtures/saas-216/retention-policy.js
  - test/fixtures/saas-216/pii-redaction.js
  - scripts/preconditions/216-05-check-upstream.cjs
  - supabase/migrations/116_saas_privacy_retention.sql
  - lib/markos/saas/privacy/contracts.ts
  - lib/markos/saas/privacy/contracts.cjs
  - lib/markos/saas/privacy/retention-policy.ts
  - lib/markos/saas/privacy/retention-policy.cjs
  - lib/markos/saas/privacy/pii-redaction.ts
  - lib/markos/saas/privacy/learning-handoff.ts
  - lib/markos/saas/privacy/audit-views.ts
  - lib/markos/saas/privacy/index.cjs
  - api/cron/saas-privacy-retention-sweep.js
  - contracts/F-266-saas-data-retention-classes-v1.yaml
  - contracts/F-267-saas-pii-redaction-v1.yaml
  - contracts/flow-registry.json
  - test/saas-216/domain-5/retention-policy-trigger.test.js
  - test/saas-216/domain-5/retention-classes-seed.test.js
  - test/saas-216/domain-5/pii-redaction.test.js
  - test/saas-216/domain-5/learning-handoff-anonymization.test.js
  - test/saas-216/domain-5/retention-sweep.test.js
  - .planning/phases/216-saas-suite-health-churn-support-usage/216-VALIDATION.md
autonomous: true
requirements:
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - QA-05
  - QA-06
  - QA-07
  - QA-08
  - QA-09
  - QA-10
  - QA-11
must_haves:
  truths:
    - "Plan 05 depends on Plan 01 (data_retention_classes table + pii_classification ENUM CREATED in slot 112) + Plan 02 + Plan 03 + Plan 04 (PII-containing tables referencing retention_class FK); Q-4 RESOLVED 2026-04-26 — Plan 05 SEEDS additional rows + adds audit views + adds retention sweep cron + adds PII redaction utilities, but TABLE+ENUM creation already happened in Plan 01 migration 112"
    - "Migration 116 ships ADDITIONAL data_retention_classes rows (cross-phase classes for P217-P228 anticipation per RESEARCH §Domain 5 Downstream Consumer Matrix) + retention sweep audit views + retention enforcement on Plan 02/03/04 tables — does NOT recreate data_retention_classes table or pii_classification ENUM (those exist in slot 112 from Plan 01)"
    - "Additional retention class seeds (Plan 05 INSERTs) — `mrr_aggregate` (P217: no_pii / null retention), `pql_score` (P218: pseudonymous / 730d), `abm_account` (P219: personal / 1825d), `community_profile` (P220: pseudonymous / 1095d), `cdp_identity_graph_node` (P221: highly_sensitive / 2555d), `crm_personal` (P222: personal / 1825d), `email_send_record` (P223: personal / 1095d), `attribution_fact` (P225: pseudonymous / 1825d), `sales_artifact` (P226: personal / 1825d), `affiliate_record` (P227: personal / 1825d) — 10 forward-looking rows per RESEARCH §Domain 5 Downstream Consumer Matrix"
    - "DB-trigger compliance — `SUPPORT_DATA_REQUIRES_RETENTION_POLICY` (Plan 05 adds for any table that has retention_class column) — applied to saas_support_tickets (Plan 03) + product_usage_events (Plan 02) + saas_churn_interventions (Plan 04) — verbatim per RESEARCH §Domain 5; raises EXCEPTION when retention_class IS NULL OR retention_until IS NULL on INSERT"
    - "Retention sweep cron `api/cron/saas-privacy-retention-sweep.js` — daily; for each row in saas_support_tickets / product_usage_events / saas_churn_interventions where retention_until < now(): if deletion_method='hard_delete' DELETE row; if 'anonymize' UPDATE PII columns to anonymized markers; if 'tombstone' move to tombstone table or set tombstoned_at; emits AgentRun kind='saas-privacy-retention-sweep'; emits markos_audit_log_hash_chain entry per deletion"
    - "PII redaction utility (lib/markos/saas/privacy/pii-redaction.ts) — redactPII(text, classification) returns redacted string; detection patterns: email (regex), phone (regex), credit card (Luhn-validated regex), SSN-like, names via NER fallback (rule-based v1; ML deferred); used by support classifier + product event ingest before INSERT"
    - "Learning handoff anonymization (lib/markos/saas/privacy/learning-handoff.ts) — anonymizeForLearning({ rows, classification }) — strips PII per pii_classification ENUM level; sensitive/highly_sensitive rows blocked; pseudonymous rows hashed; no_pii passed-through; consumed by P212 LRN substrate when it lands"
    - "Audit views (lib/markos/saas/privacy/audit-views.ts + migration 116 SQL views) — v_retention_due (rows past retention_until awaiting sweep) + v_pii_class_summary (per-tenant PII class counts) + v_consent_status (per-customer retention class summary) — read-only views with RLS enforced via underlying tables"
    - "2 contracts ship — F-266 (saas-data-retention-classes-v1) covering data_retention_classes table contract + pii_classification ENUM enumeration + 5 P216 seed rows + 10 forward-looking rows; F-267 (saas-pii-redaction-v1) covering redactPII + anonymizeForLearning contracts"
    - "Plan 05 is FOUNDATIONAL — every downstream P217-P228 phase that creates a PII-containing table MUST FK reference data_retention_classes.class_key as NOT NULL; documented in V4.1.0-MIGRATION-SLOT-COORDINATION.md (Plan 01 Task 0.1 appended) + Plan 06 growth_signal_map; subsequent phases add NEW class_key rows via INSERT, NOT schema changes"
    - "Cross-tenant SELECT via audit views denied via underlying RLS — INSERT under tenant A then SELECT v_retention_due under tenant B returns 0 rows (RLS bypasses through views are blocked)"
    - "COMP-01 documented in frontmatter as `integrates_with` (NOT `requirements`) — P206 owns COMP-01 compliance substrate; P216 Plan 05 USES P206 redaction patterns and EXTENDS with SaaS-specific retention classes"
  artifacts:
    - path: scripts/preconditions/216-05-check-upstream.cjs
      provides: invokes assertUpstreamReady from Plan 01; hard-checks data_retention_classes (Plan 01 created in slot 112) + saas_health_scores + product_usage_events + saas_support_tickets + saas_churn_interventions (Plans 01-04); soft-checks markos_audit_log_hash_chain (foundation slot 82 — should always exist); exit 1 on hard miss
    - path: supabase/migrations/116_saas_privacy_retention.sql
      provides: ALTER-only migration — INSERT 10 forward-looking retention class seeds + CREATE OR REPLACE FUNCTION enforce_retention_policy_on_ticket (Plan 03 table) + enforce_retention_policy_on_usage (Plan 02 table) + enforce_retention_policy_on_intervention (Plan 04 table) — all 3 raise EXCEPTION 'SUPPORT_DATA_REQUIRES_RETENTION_POLICY' when retention_class/retention_until null; CREATE TRIGGERs on respective tables; CREATE VIEW v_retention_due + v_pii_class_summary + v_consent_status
    - path: lib/markos/saas/privacy/contracts.ts
      provides: zod schemas + enum constants — RETENTION_CLASS_KEYS const list (15 known classes — 5 P216 + 10 forward-looking) + DELETION_METHODS = ['hard_delete','anonymize','tombstone'] + RetentionPolicySchema zod + computeRetentionUntil({ classKey, baseTime })
      exports: [RETENTION_CLASS_KEYS, DELETION_METHODS, RetentionPolicySchema, computeRetentionUntil]
    - path: lib/markos/saas/privacy/retention-policy.ts
      provides: getRetentionClass(classKey, supabase) — returns class metadata; applyRetentionPolicy({ row, classKey }) stamps retention_class + retention_until on row before INSERT; sweepExpiredRows({ table, supabase }) returns array of rows past retention_until awaiting deletion_method action
      exports: [getRetentionClass, applyRetentionPolicy, sweepExpiredRows]
    - path: lib/markos/saas/privacy/pii-redaction.ts
      provides: PII_PATTERNS map (email/phone/credit_card/ssn-like) + redactPII(text, classification) returning redacted string with placeholders; detectPII(text) returning array of {type, span, value}; used by Plan 02 event-ingest scrubPII + Plan 03 classifier description-redaction
      exports: [PII_PATTERNS, redactPII, detectPII]
    - path: lib/markos/saas/privacy/learning-handoff.ts
      provides: anonymizeForLearning({ rows, classification }) — strips PII per ENUM level; blocks sensitive/highly_sensitive; hashes pseudonymous; consumed by P212 LRN substrate (deferred)
      exports: [anonymizeForLearning, ANONYMIZATION_RULES]
    - path: lib/markos/saas/privacy/audit-views.ts
      provides: getRetentionDue({ tenantId }, supabase) reading v_retention_due; getPIIClassSummary({ tenantId }, supabase) reading v_pii_class_summary; getConsentStatus({ tenantId, customerId }, supabase) reading v_consent_status
      exports: [getRetentionDue, getPIIClassSummary, getConsentStatus]
    - path: lib/markos/saas/privacy/index.cjs
      provides: CommonJS twin re-exporting full surface
    - path: api/cron/saas-privacy-retention-sweep.js
      provides: daily cron with x-markos-cron-secret; iterates 3 P216 PII tables; applies deletion_method per data_retention_classes; emits AgentRun + markos_audit_log_hash_chain entry per row affected
    - path: contracts/F-266-saas-data-retention-classes-v1.yaml
      provides: data_retention_classes contract + pii_classification ENUM + 15 seed rows (5 P216 + 10 forward-looking)
    - path: contracts/F-267-saas-pii-redaction-v1.yaml
      provides: redactPII + anonymizeForLearning contracts; PII detection pattern documentation
    - path: contracts/flow-registry.json
      provides: 2 entries appended for F-266 + F-267
    - path: test/saas-216/domain-5/retention-policy-trigger.test.js
      provides: Postgres-backed test — INSERT saas_support_tickets without retention_class → EXCEPTION 'SUPPORT_DATA_REQUIRES_RETENTION_POLICY'; without retention_until → same; valid → succeeds; same for product_usage_events + saas_churn_interventions
    - path: test/saas-216/domain-5/retention-classes-seed.test.js
      provides: SELECT count(*) FROM data_retention_classes after Plan 05 migration → 15 rows (5 P216 + 10 forward-looking); each class has valid pii_classification + deletion_method + retention_days
    - path: test/saas-216/domain-5/pii-redaction.test.js
      provides: redactPII unit tests — email 'foo@bar.com' → '[REDACTED-EMAIL]'; phone '555-123-4567' → '[REDACTED-PHONE]'; credit card '4111-1111-1111-1111' → '[REDACTED-CC]' (Luhn-valid); detectPII returns spans for all detected patterns
    - path: test/saas-216/domain-5/learning-handoff-anonymization.test.js
      provides: anonymizeForLearning — sensitive rows blocked entirely; pseudonymous rows return hashed ids; no_pii passes through unchanged
    - path: test/saas-216/domain-5/retention-sweep.test.js
      provides: integration — INSERT 3 rows with retention_until = now()-1d (past); cron invocation deletes hard_delete rows; anonymizes anonymize rows (PII columns set to placeholder); tombstones tombstone rows (tombstoned_at set); audit log entries written
  key_links:
    - from: lib/markos/saas/privacy/retention-policy.ts
      to: supabase/migrations/116_saas_privacy_retention.sql
      via: applyRetentionPolicy stamps retention_class + retention_until before INSERT; DB-trigger trg_*_retention_policy validates non-null on INSERT
      pattern: "SUPPORT_DATA_REQUIRES_RETENTION_POLICY"
    - from: lib/markos/saas/privacy/pii-redaction.ts
      to: lib/markos/saas/usage/event-ingest.ts (Plan 02)
      via: Plan 02 scrubPII delegates to redactPII for properties JSONB sanitization
      pattern: "redactPII"
    - from: lib/markos/saas/privacy/pii-redaction.ts
      to: lib/markos/saas/support/ticket-import.ts (Plan 03)
      via: Plan 03 importTicket calls redactPII on description before INSERT (defense-in-depth with retention_class='support_text')
      pattern: "redactPII"
    - from: api/cron/saas-privacy-retention-sweep.js
      to: lib/markos/audit/* (markos_audit_log_hash_chain)
      via: each retention sweep deletion/anonymization emits audit log entry with SHA-256 hash chain (foundation migration 82)
      pattern: "markos_audit_log_hash_chain"
    - from: lib/markos/saas/privacy/learning-handoff.ts
      to: P212 LRN substrate (SOFT — deferred)
      via: anonymizeForLearning produces output that P212 ArtifactPerformanceLog consumes; sensitive rows never leave tenant boundary
      pattern: "anonymizeForLearning"
    - from: contracts/F-266-saas-data-retention-classes-v1.yaml
      to: lib/markos/saas/privacy/contracts.ts (RETENTION_CLASS_KEYS)
      via: contract enumerates 15 seed rows; zod schema parity-tested
      pattern: "RETENTION_CLASS_KEYS"
---

<objective>
Ship Wave 3 privacy + retention substrate (FOUNDATIONAL for P217-P228), satisfying QA-11 (privacy/retention controls) + cross-phase data classification responsibility. Plan 05 is the SECOND HALF of the foundational privacy substrate — Plan 01 already CREATED the data_retention_classes lookup table + pii_classification ENUM in migration slot 112 (Q-4 RESOLVED 2026-04-26 to allow Plans 02/03/04 retention_class FK references); Plan 05 SEEDS 10 forward-looking rows for P217-P228, adds DB-trigger SUPPORT_DATA_REQUIRES_RETENTION_POLICY to Plan 02/03/04 tables, ships PII redaction utilities (consumed by Plans 02 + 03 ingest paths), ships learning-handoff anonymization (consumed by P212 when it lands), ships retention sweep cron + audit views.

Three architectural facts make Plan 05 FOUNDATIONAL:
1. Every PII-containing table in P217-P228 MUST FK reference data_retention_classes.class_key. Plan 05 seeds the 10 forward-looking class_keys per RESEARCH §Domain 5 Downstream Consumer Matrix (mrr_aggregate, pql_score, abm_account, community_profile, cdp_identity_graph_node, crm_personal, email_send_record, attribution_fact, sales_artifact, affiliate_record).
2. The SUPPORT_DATA_REQUIRES_RETENTION_POLICY DB-trigger applies to 3 P216 tables (saas_support_tickets, product_usage_events, saas_churn_interventions); downstream phases will add their own triggers using the same pattern.
3. Cross-tenant learning handoff (P212 ArtifactPerformanceLog substrate) MUST consume anonymizeForLearning to strip PII before tenant-crossing. Sensitive/highly_sensitive class rows are BLOCKED entirely; pseudonymous rows are hashed.

Plan 05 ships the retention sweep cron (saas-privacy-retention-sweep.js) — daily; iterates 3 PII tables; applies deletion_method per class (hard_delete / anonymize / tombstone); emits markos_audit_log_hash_chain entry per affected row.

COMP-01 documented as integrates_with (P206 owns).

Output: 1 preflight CLI + 1 ALTER-only migration (116 — 10 INSERTs + 3 trigger functions + 3 audit views) + 7 privacy lib files + 1 cron + 2 contract YAMLs + flow-registry update + 5 domain-5 test files + populated VALIDATION.md.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/216-saas-suite-health-churn-support-usage/216-CONTEXT.md
@.planning/phases/216-saas-suite-health-churn-support-usage/216-RESEARCH.md
@.planning/phases/216-saas-suite-health-churn-support-usage/216-REVIEWS.md
@.planning/phases/216-saas-suite-health-churn-support-usage/216-VALIDATION.md
@.planning/phases/216-saas-suite-health-churn-support-usage/216-01-PLAN.md
@.planning/phases/216-saas-suite-health-churn-support-usage/216-02-PLAN.md
@.planning/phases/216-saas-suite-health-churn-support-usage/216-03-PLAN.md
@.planning/phases/216-saas-suite-health-churn-support-usage/216-04-PLAN.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@lib/markos/saas/preflight/index.cjs
@supabase/migrations/82_markos_audit_log_hash_chain.sql
@supabase/migrations/112_saas_health_explainability.sql

<interfaces>
<!-- Plan 01 ships data_retention_classes table + pii_classification ENUM. Plan 05 ADDs additional rows + triggers. -->

From supabase/migrations/112_saas_health_explainability.sql (Plan 01):
```sql
CREATE TYPE pii_classification AS ENUM ('no_pii','pseudonymous','personal','sensitive','highly_sensitive');
CREATE TABLE data_retention_classes (
  class_key text PRIMARY KEY, display_name, pii_classification, retention_days, ...
);
-- 5 seed rows: support_text, product_event, health_score, churn_intervention, growth_signal
```

Forward-looking 10 retention classes (Plan 05 INSERTs per RESEARCH §Domain 5 Downstream Consumer Matrix):
| class_key | pii_class | retention_days | deletion_method | Used by |
|-----------|-----------|---------------|----------------|---------|
| mrr_aggregate | no_pii | null (forever) | hard_delete | P217 |
| pql_score | pseudonymous | 730 | anonymize | P218 |
| abm_account | personal | 1825 | tombstone | P219 |
| community_profile | pseudonymous | 1095 | anonymize | P220 |
| cdp_identity_graph_node | highly_sensitive | 2555 | tombstone | P221 |
| crm_personal | personal | 1825 | tombstone | P222 |
| email_send_record | personal | 1095 | hard_delete | P223 |
| attribution_fact | pseudonymous | 1825 | anonymize | P225 |
| sales_artifact | personal | 1825 | tombstone | P226 |
| affiliate_record | personal | 1825 | tombstone | P227 |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 116 (additional retention class seeds + 3 SUPPORT_DATA_REQUIRES_RETENTION_POLICY triggers + 3 audit views)</name>
  <files>supabase/migrations/116_saas_privacy_retention.sql, scripts/preconditions/216-05-check-upstream.cjs, test/fixtures/saas-216/retention-policy.js, test/fixtures/saas-216/pii-redaction.js, test/fixtures/saas-216/index.js</files>
  <read_first>
    - .planning/phases/216-saas-suite-health-churn-support-usage/216-RESEARCH.md §Domain 5 §PII Classification ENUM §data_retention_classes table §DB-Trigger Compliance §Downstream Consumer Matrix
    - .planning/phases/216-saas-suite-health-churn-support-usage/216-01-PLAN.md (data_retention_classes + pii_classification already in slot 112)
    - .planning/phases/216-saas-suite-health-churn-support-usage/216-02-PLAN.md (product_usage_events.retention_class column reference)
    - .planning/phases/216-saas-suite-health-churn-support-usage/216-03-PLAN.md (saas_support_tickets.retention_class column reference)
    - .planning/phases/216-saas-suite-health-churn-support-usage/216-04-PLAN.md (saas_churn_interventions.retention_class column reference)
  </read_first>
  <action>
    Step 1: Create supabase/migrations/116_saas_privacy_retention.sql:
      - INSERT 10 forward-looking rows into data_retention_classes (mrr_aggregate, pql_score, abm_account, community_profile, cdp_identity_graph_node, crm_personal, email_send_record, attribution_fact, sales_artifact, affiliate_record) — ON CONFLICT DO NOTHING for idempotency
      - CREATE OR REPLACE FUNCTION enforce_retention_policy_on_ticket (per RESEARCH §Domain 5 — RAISE EXCEPTION 'SUPPORT_DATA_REQUIRES_RETENTION_POLICY' when retention_class IS NULL OR retention_until IS NULL on INSERT into saas_support_tickets)
      - CREATE TRIGGER trg_ticket_retention_policy BEFORE INSERT ON saas_support_tickets
      - CREATE OR REPLACE FUNCTION enforce_retention_policy_on_usage (same pattern; product_usage_events)
      - CREATE TRIGGER trg_usage_retention_policy BEFORE INSERT ON product_usage_events
      - CREATE OR REPLACE FUNCTION enforce_retention_policy_on_intervention (same pattern; saas_churn_interventions)
      - CREATE TRIGGER trg_intervention_retention_policy BEFORE INSERT ON saas_churn_interventions
      - CREATE VIEW v_retention_due AS SELECT 'support_ticket' AS source, ticket_id AS id, tenant_id, retention_until, retention_class FROM saas_support_tickets WHERE retention_until < now() UNION ALL same for product_usage_events + saas_churn_interventions (RLS inherited)
      - CREATE VIEW v_pii_class_summary AS SELECT tenant_id, retention_class, count(*) FROM saas_support_tickets GROUP BY tenant_id, retention_class — UNION ALL across 3 tables
      - CREATE VIEW v_consent_status AS SELECT tenant_id, customer_id, retention_class, max(retention_until) AS soonest_expiry FROM saas_support_tickets GROUP BY tenant_id, customer_id, retention_class — UNION ALL

    Step 2: Create scripts/preconditions/216-05-check-upstream.cjs — invokes assertUpstreamReady; hard-checks data_retention_classes (Plan 01) + saas_health_scores + product_usage_events + saas_support_tickets + saas_churn_interventions (Plans 01-04); soft-checks markos_audit_log_hash_chain; exit 1 on hard miss.

    Step 3: Append to test/fixtures/saas-216/index.js re-exporting retentionPolicyFactory + piiRedactionFactory; create fixture files with 15-class catalog + sample PII strings.
  </action>
  <verify>
    <automated>node scripts/preconditions/216-05-check-upstream.cjs</automated>
  </verify>
  <done>Migration 116 applies cleanly; 10 additional retention class rows seeded; 3 SUPPORT_DATA_REQUIRES_RETENTION_POLICY triggers attached to 3 PII tables; 3 audit views created; preflight passes.</done>
</task>

<task type="auto">
  <name>Task 2: Lib surface (contracts + retention-policy + pii-redaction + learning-handoff + audit-views) + cron + contracts F-266 + F-267</name>
  <files>lib/markos/saas/privacy/contracts.ts, lib/markos/saas/privacy/contracts.cjs, lib/markos/saas/privacy/retention-policy.ts, lib/markos/saas/privacy/retention-policy.cjs, lib/markos/saas/privacy/pii-redaction.ts, lib/markos/saas/privacy/learning-handoff.ts, lib/markos/saas/privacy/audit-views.ts, lib/markos/saas/privacy/index.cjs, api/cron/saas-privacy-retention-sweep.js, contracts/F-266-saas-data-retention-classes-v1.yaml, contracts/F-267-saas-pii-redaction-v1.yaml, contracts/flow-registry.json</files>
  <read_first>
    - .planning/phases/216-saas-suite-health-churn-support-usage/216-RESEARCH.md §Domain 5 §PII Classification ENUM §Downstream Consumer Matrix
    - lib/markos/saas/preflight/index.cjs (Plan 01)
    - supabase/migrations/82_markos_audit_log_hash_chain.sql (audit emit pattern)
    - api/cron/webhooks-dlq-purge.js (cron handler pattern)
  </read_first>
  <action>
    Step 1: Create lib/markos/saas/privacy/contracts.ts exporting RETENTION_CLASS_KEYS (15 known classes) + DELETION_METHODS + RetentionPolicySchema zod + computeRetentionUntil({ classKey, baseTime }).

    Step 2: Create lib/markos/saas/privacy/retention-policy.ts exporting getRetentionClass + applyRetentionPolicy (stamps retention_class + retention_until on row before INSERT) + sweepExpiredRows.

    Step 3: Create lib/markos/saas/privacy/pii-redaction.ts exporting PII_PATTERNS (email/phone/credit_card with Luhn validation/SSN-like) + redactPII (returns text with placeholders) + detectPII (returns spans).

    Step 4: Create lib/markos/saas/privacy/learning-handoff.ts exporting anonymizeForLearning + ANONYMIZATION_RULES; sensitive/highly_sensitive blocked; pseudonymous hashed; no_pii passed-through.

    Step 5: Create lib/markos/saas/privacy/audit-views.ts exporting getRetentionDue + getPIIClassSummary + getConsentStatus (read v_* views).

    Step 6: Create CommonJS twins + lib/markos/saas/privacy/index.cjs barrel.

    Step 7: Create api/cron/saas-privacy-retention-sweep.js — x-markos-cron-secret; iterates 3 PII tables via sweepExpiredRows; for each row applies deletion_method (hard_delete = DELETE, anonymize = UPDATE PII columns to placeholders, tombstone = SET tombstoned_at OR move to tombstone table); emits markos_audit_log_hash_chain entry per row + AgentRun event.

    Step 8: Create contracts/F-266 + F-267 YAML; append 2 entries to flow-registry.json.
  </action>
  <verify>
    <automated>npm test -- test/saas-216/domain-5/pii-redaction.test.js test/saas-216/domain-5/learning-handoff-anonymization.test.js</automated>
  </verify>
  <done>7 privacy lib files + 1 cron exist; F-266+F-267 in flow-registry; pii-redaction + learning-handoff tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Domain-5 test files (5 tests) + 216-VALIDATION.md update</name>
  <files>test/saas-216/domain-5/retention-policy-trigger.test.js, test/saas-216/domain-5/retention-classes-seed.test.js, test/saas-216/domain-5/pii-redaction.test.js, test/saas-216/domain-5/learning-handoff-anonymization.test.js, test/saas-216/domain-5/retention-sweep.test.js, .planning/phases/216-saas-suite-health-churn-support-usage/216-VALIDATION.md</files>
  <read_first>
    - .planning/phases/216-saas-suite-health-churn-support-usage/216-RESEARCH.md §Validation Architecture (test map for QA-11)
  </read_first>
  <action>
    Step 1: Create retention-policy-trigger.test.js — Postgres-backed: 6 EXCEPTION assertions (3 tables × 2 missing-column scenarios — retention_class null + retention_until null) + 3 success paths (valid INSERT for each table).

    Step 2: Create retention-classes-seed.test.js — SELECT * FROM data_retention_classes after migration → 15 rows total (5 P216 from Plan 01 + 10 forward-looking from Plan 05); validates each row has valid pii_classification ENUM value + deletion_method CHECK + retention_days >= 0 OR null (forever).

    Step 3: Create pii-redaction.test.js — redactPII unit tests: email + phone + credit card (Luhn-valid 4111-1111-1111-1111) + SSN-like + multi-pattern strings; detectPII returns proper spans; classification-aware redaction (no_pii passes through; sensitive aggressive-redact).

    Step 4: Create learning-handoff-anonymization.test.js — anonymizeForLearning with sensitive row → blocked (returns null or empty array); pseudonymous → hashed user_id_external; no_pii → unchanged passthrough; mixed batch → only safe rows surfaced.

    Step 5: Create retention-sweep.test.js — integration: INSERT 3 rows with retention_until = now()-1d (one per deletion_method); cron invocation deletes hard_delete row entirely; anonymizes anonymize row (PII columns set to '[REDACTED]'); tombstones tombstone row (tombstoned_at set or row moved); audit log entries written for all 3.

    Step 6: Append Plan 05 row to 216-VALIDATION.md per-task verification map.
  </action>
  <verify>
    <automated>npm test -- test/saas-216/domain-5/</automated>
  </verify>
  <done>5 domain-5 tests green; SUPPORT_DATA_REQUIRES_RETENTION_POLICY exception verified across all 3 PII tables; 15-row retention class catalog verified; PII redaction + learning anonymization verified; 216-VALIDATION.md updated for Plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron→saas-privacy-retention-sweep | x-markos-cron-secret service-role retention sweep |
| internal→learning-handoff | tenant-crossing data MUST pass through anonymizeForLearning |
| audit views→client | underlying RLS enforces tenant isolation through views |
| data_retention_classes mutation | tenant-shared lookup; no tenant-specific INSERTs allowed (only foundation INSERTs in migrations) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-216-27 | T | retention_class column tampering | mitigate | SUPPORT_DATA_REQUIRES_RETENTION_POLICY DB-trigger blocks NULL inserts; FK to data_retention_classes |
| T-216-28 | I | PII leak via product event properties JSONB | mitigate | redactPII applied before INSERT (Plan 02 event-ingest); retention_class='product_event' (365d/hard_delete) |
| T-216-29 | I | PII leak via support ticket description | mitigate | redactPII applied + retention_class='support_text' (730d/anonymize); review_required_at_days=700 |
| T-216-30 | I | cross-tenant learning handoff PII leak | mitigate | anonymizeForLearning blocks sensitive/highly_sensitive; hashes pseudonymous; defense-in-depth with P212 substrate |
| T-216-31 | R | retention sweep cron deleted-row repudiation | mitigate | every deletion emits markos_audit_log_hash_chain entry (foundation slot 82 SHA-256 chain) |
| T-216-32 | D | retention sweep mass-delete | accept | daily cron rate-limited; deletion_method=tombstone for high-value classes preserves recovery |
| T-216-33 | E | tenant_id elevation via audit view | mitigate | views inherit RLS from underlying tables; cross-tenant denial test in CI |
</threat_model>

<verification>
- node scripts/preconditions/216-05-check-upstream.cjs (exits 0 when Plans 01-04 ready)
- npm test -- test/saas-216/domain-5/
- supabase migration up to slot 116 applies cleanly (10 INSERTs idempotent)
- flow-registry.json contains F-266 + F-267
- data_retention_classes contains 15 total seed rows
- 3 retention triggers attached to 3 PII tables
</verification>

<success_criteria>
- 10 forward-looking retention class rows INSERTed (15 total in data_retention_classes)
- SUPPORT_DATA_REQUIRES_RETENTION_POLICY trigger applied to saas_support_tickets + product_usage_events + saas_churn_interventions
- redactPII detects email/phone/credit_card/SSN patterns
- anonymizeForLearning blocks sensitive rows; hashes pseudonymous; passes no_pii
- 3 audit views created (v_retention_due, v_pii_class_summary, v_consent_status)
- Retention sweep cron emits markos_audit_log_hash_chain per affected row
- F-266 + F-267 contracts in flow-registry
- 5 domain-5 tests green (3-table trigger coverage + 15-row seed + PII redaction + learning anonymization + sweep integration)
- 216-VALIDATION.md updated for Plan 05
</success_criteria>

<output>
After completion, create `.planning/phases/216-saas-suite-health-churn-support-usage/216-05-SUMMARY.md` documenting: artifacts created, FOUNDATIONAL nature for P217-P228, 15-row retention class catalog, 3 DB-triggers attached, PII redaction utilities + learning handoff anonymization contract, COMP-01 integration boundary.
</output>
