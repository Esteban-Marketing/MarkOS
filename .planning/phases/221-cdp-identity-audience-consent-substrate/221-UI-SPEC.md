---
phase: 221
slug: cdp-identity-audience-consent-substrate
status: draft
shadcn_initialized: false
preset: not-applicable-no-ui-phase
domain: cdp-identity-graph-resolution-merge-policy-consent-state-of-record-event-envelope-trait-snapshot-audience-definition-snapshot-membership-dsr-tombstone-cascade-purge-dual-write-reconciliation-json-logic-dsl-operator-whitelist-double-gate-activation-pitfall-5-late-event-guard-architecture-lock-substrate
created: 2026-05-04
canonical_visual_contract: /DESIGN.md
design_md_version: v1.1.0
mode: no-ui-surface-phase
ui_scope: zero-surface
plans_in_scope: [221-01, 221-02, 221-03, 221-04, 221-05, 221-06]
plans_with_ui_surfaces: []
plans_no_ui: [221-01, 221-02, 221-03, 221-04, 221-05, 221-06]
milestone_position: v4.2.0-COMMERCIAL-ENGINES-LANE-OPENING-PHASE
parent_doctrine_chain:
  - 206-UI-SPEC.md (mutation-class doctrine — `data.export` for DSR full-profile export per D-25 emitting `{ IdentityProfile, IdentityLink[], cdp_events[], TraitSnapshot[], ConsentState[], AudienceSnapshotMembership[] }` AND audit-log export of merge/consent/audience/deletion events; `external.send` for consent override emits per D-13 (every downstream send/dispatch engine MUST consume `ConsentState` — never a channel-local flag — enforced at planner/reviewer time for P222-P226); `default_approval_mode == dual_approval` for `dsr_export_approval` per SOC2 P206 Type I posture (DSR exports are highly_sensitive PII surfaces — dual-approval gate inherited from 215 high-amount payout dual-approval pattern + 219 high-discount dual-approval pattern); `default_approval_mode == single_approval` for `consent_drift_resolution` (operator confirms divergence between `cdp_consent_states` and legacy `outboundConsentRecords` row + selects authoritative source) AND `audience_activation_approval` (operator confirms double-gate satisfied — snapshot membership + current ConsentState re-validation); autonomy-ceiling on `data.export` for all DSR export paths; autonomy-ceiling on `external.send` for all consent-override-driven dispatch paths)
  - 207-UI-SPEC.md (`RunApiEnvelope`; `run_id` linked to identity-merge runs (recordCdpMergeDecision via lib/markos/cdp/identity/merge.ts) + trait-recompute hourly cron (`cdp-trait-recompute-hourly.js` per 221-03) + trait-recompute daily cron (`cdp-trait-recompute-daily.js` per 221-03) + audience-snapshot-compute runs (lib/markos/cdp/audiences/snapshot.ts per 221-04) + DSR-tombstone runs (lib/markos/cdp/deletion/tombstone.ts per 221-06) + DSR-cascade-purge runs (lib/markos/cdp/deletion/cascade.ts per 221-06) + DSR-export runs (lib/markos/cdp/deletion/dsr-export.ts per 221-06) + dual-write-reconciliation daily cron (`cdp-dual-write-reconciliation.js` per 221-06) + consent-drift-audit hourly cron (`cdp-consent-drift-audit.js` per 221-02 D-40 RM1 fix); `AgentRunEventType` for `cdp_identity_profile_created` / `cdp_identity_link_created` / `cdp_merge_decision_recorded` / `cdp_merge_review_requested` / `cdp_merge_approved` / `cdp_merge_rejected` / `cdp_consent_state_set` / `cdp_consent_drift_detected` / `cdp_consent_drift_resolved` / `cdp_event_ingested` / `cdp_event_dual_write_succeeded` / `cdp_event_dual_write_failed` / `cdp_trait_snapshot_computed` / `cdp_trait_recompute_started` / `cdp_trait_recompute_completed` / `cdp_audience_definition_created` / `cdp_audience_dsl_rejected` (whitelist gate fired) / `cdp_audience_snapshot_computed` / `cdp_audience_activation_requested` / `cdp_audience_activation_approved` / `cdp_audience_activation_rejected` / `cdp_dsr_request_received` / `cdp_dsr_tombstone_executed` / `cdp_dsr_cascade_purged` / `cdp_dsr_export_requested` / `cdp_dsr_export_approved` / `cdp_dsr_export_dispatched` / `cdp_late_event_dropped_after_tombstone` (Pitfall 5 guard fired) / `cdp_dual_write_reconciliation_completed` / `cdp_dual_write_drift_detected` / `cdp_unauthorized_cross_tenant_access` / `cdp.consent_writer_violation` (single-writer trigger rejection); `ApprovalHandoffRecord` links 221 consent-drift-resolution + audience-activation + DSR-export to P208 inbox; `agent_run_id` linked to all 4 P221 cron handlers + 8 read-only API handlers + 2 read MCP tools)
  - 208-UI-SPEC.md (PARENT — Approval Inbox at `/operations/approvals` extends with THREE new handoff_kind literals: `consent_drift_resolution` (27th literal in canonical chain — 221-02 hourly drift audit detects divergence between `cdp_consent_states` and legacy `outboundConsentRecords` row; `lib/markos/cdp/consent/drift-audit.ts` `requestDriftResolution` calls `buildApprovalPackage` with `kind='consent_drift_resolution'`; mutation-class is informational + remediation — operator selects authoritative source between CDP ConsentState and legacy row, then library applies single-writer GUC `app.consent_writer_source = 'cdp_setConsentState'` to commit), `audience_activation_approval` (28th literal — 221-04 audience snapshot dispatch double-gate per D-18; `lib/markos/cdp/audiences/activation.ts` `requestAudienceActivation` calls `buildApprovalPackage` with `kind='audience_activation_approval'`; modal renders snapshot.membership_count + suppression_count_at_snapshot + consent re-validation preview against current ConsentState — snapshot freezes who COULD be reached; operator approval confirms who CAN legally be reached right now), `dsr_export_approval` (29th literal — 221-06 DSR export contract per D-25; `lib/markos/cdp/deletion/dsr-export.ts` `requestDsrExport` calls `buildApprovalPackage` with `kind='dsr_export_approval'`; mutation-class `data.export`; `default_approval_mode == dual_approval` per SOC2 P206 Type I — DSR exports contain highly_sensitive PII; modal renders profile_id + IdentityProfile + IdentityLink[] + cdp_events[] + TraitSnapshot[] + ConsentState[] + AudienceSnapshotMembership[] cardinality preview; reuses 215 billing-correction modal recipe with `<PIIRedactedField />` for raw PII display); the legacy 26-chip set (post-220 = end-of-v4.1.0: 4 P207 + P214 `billing_charge_approval` + P215 `billing_correction_approval` + P216 `support_response_approval` + P216 `save_offer_approval` + 218's 4 chips + 219's 8 chips + 220's 7 chips ending in `partner_payout_export_approval` 26th) extends to 29 chips after 221 (the prompt-cited "extending v4.1.0 closeout chain (post-220 = 26 chips) to 29 chips total" — confirmed: 26 + 3 = 29 handoff_kind literals start-of-v4.2.0); rendering of those three new chips is deferred to a future P208 admin extension that displays per-row classifier output, drift-audit divergence indicator (which-side-wins), audience double-gate consent re-validation preview (membership + current ConsentState), DSR export PII cardinality indicator with `<PIIRedactedField />` per row, late-event-after-tombstone guard indicator (Pitfall 5), single-writer trigger rejection indicator with audit row `cdp.consent_writer_violation`)
  - 209-UI-SPEC.md (PARENT — ConsentState provenance + audit log evidence binding; every ConsentState mutation writes `{ source, source_timestamp, source_event_ref, evidence_ref }` per D-11 + D-29 (audit trail mandatory on consent mutations) — provenance feeds P209 EvidenceMap; trait provenance `source_event_ref[]` mandatory per D-16 — no black-box traits; the 209 `<EvidenceMapPanel />` + `<EvidenceCitationChip />` extracted-component recipes are referenced by future 221 admin surfaces as load-bearing primitives; EVD doctrine carry — EVD-01 (evidence linked to factual claims for trait snapshots) + EVD-03 (research tiers + source quality on consent provenance) + EVD-04 (agents reuse non-stale ConsentState before send — operationalized by D-12 `evaluateOutboundEligibility` reading ConsentState first) + EVD-05 (approval UI exposes evidence — drift-resolution + audience-activation + DSR-export modals expose ConsentState provenance + AudienceSnapshot evidence_ref + DSR cascade evidence_ref); EVD-06 explicitly OUT OF SCOPE for P221 per Plan 06 must_haves §EVD-06 disclaimer)
  - 213-UI-SPEC.md (PARENT — Tenant 0 readiness gate consumer; 213-04 public-proof boundary applies STRICTLY to DSR exports — `data.export` mutation class outputs are PRIVATE doctrine forever; raw `cdp_identity_profiles.primary_email` + `cdp_identity_profiles.primary_phone` + `cdp_identity_profiles.company_name` + `cdp_consent_states.preference_tags` + `cdp_consent_states.quiet_hours` + `cdp_trait_snapshots.value` (when trait_name carries PII) are NEVER cited in case-studies or public surfaces; banned-lexicon zero-match required on every DSR export envelope text fields + consent override copy + audience activation copy + drift-resolution operator-note BEFORE any `data.export` or `external.send` mutation dispatches the content (mirror 216/218/219/220 pattern; 221 expands the validator-count by 3 new mutation classes — DSR export envelope rendering, audience activation copy, consent override copy); 213.4-VALIDATION.md carry-forward (D-08 token-only, D-09 mint-as-text, D-09b `.c-notice` mandatory, D-13 `.c-card--feature` reserved, D-14 no `.c-table`, D-15 selective extraction, D-21 server/client boundary) carries verbatim into all future 221-consuming admin/tenant surfaces)
  - 214-UI-SPEC.md (PARENT — SaaS Suite Activation; per D-21 `cdp_identity_profiles.profile_id` is the canonical identity reader for SaaS bridge consumed via `lib/markos/cdp/adapters/crm-projection.ts::getProfileForContact`; `saas_subscriptions` FK relationship — SaaS billing customer identity bridge reads CDP `IdentityProfile` via the same adapter, no parallel SaaS identity model; future P214 admin surfaces composing customer identity widgets MUST consume CDP via the read-through adapter, never via direct CDP SQL; CDP profile fields rendered via `<PIIRedactedField />` per 216 inheritance with `pii_classification` ENUM mapping)
  - 215-UI-SPEC.md (PARENT — sensitive credential UI binding contract Layer 6 carries verbatim from 215 (STRIPE/MP/SIIGO/DIAN/QB credential PII fields render `[REDACTED]` placeholder + `onCopy preventDefault()` + audit-log `event_type == 'credential_view'`) AND EXTENDS to PII data per 216 inheritance — every CDP IdentityProfile + ConsentState + TraitSnapshot PII field renders via `<PIIRedactedField />` per `pii_classification` ENUM 5-value taxonomy (`no_pii` / `pseudonymous` / `personal` / `sensitive` / `highly_sensitive`); audit-log `event_type == 'identity_view'` mirrors `credential_view` pattern (every PII field render writes audit row); `<PIIRedactedField />` `onCopy` interceptor MUST `preventDefault()` to block PII clipboard exfiltration; raw email/phone/company_name fields fall under `personal` or `sensitive` classification per data classification doctrine; the 215 billing-correction modal recipe is REUSED VERBATIM for `dsr_export_approval` AND `audience_activation_approval` AND `consent_drift_resolution` per 221 Approval Inbox extension — the modal renders `<PricingContextChip />` (n/a for P221 — pricing fields not in scope), `<PIIRedactedField />` (load-bearing for DSR export cardinality + ConsentState preview + AudienceSnapshot membership preview), audit log; **DSR export modal additionally renders `<PIIRedactedField />` per row for IdentityProfile + IdentityLink + cdp_events PII fields** with cardinality counts visible but raw content gated behind admin-role + dual-approval gate per Layer 6 + 216 inheritance)
  - 216-UI-SPEC.md (PARENT — PII+Retention UI binding contract; the 5 `pii_classification` ENUM badges (`no_pii` / `pseudonymous` / `personal` / `sensitive` / `highly_sensitive`) carry verbatim and apply to `cdp_identity_profiles.{primary_email, primary_phone, company_name, full_name, ip_address, user_agent, device_fingerprint}` + `cdp_consent_states.{preference_tags, quiet_hours, jurisdiction, source}` + `cdp_trait_snapshots.{value}` (when trait_name is PII-bearing); `[REDACTED]` placeholder rendered via `.c-code-inline` + `--color-on-surface-subtle`; clipboard `onCopy preventDefault()` enforced via `<PIIRedactedField />` component (extracted from 216-04 per RF-2 — load-bearing primitive for all 221 future surfaces); audit-log `event_type == 'identity_view'` extends 216's `credential_view` pattern — every PII field render writes audit row to `markos_audit_log` (P201 hash chain); the 216 9-row `growth_signal_map` reservation seed is orthogonal — 221 ships its OWN signal layer (cdp_events) with 10-domain ENUM `event_domain ∈ { website, product, email, messaging, crm, billing, support, social, ads, partner }` per D-08; the 216 `<HealthScoreBadge />` + `<RiskBandBadge />` + `<RetentionClassChip />` extracted-component recipes are referenced by future 221 admin surfaces as load-bearing primitives — `<HealthScoreBadge />` reused on profile detail (when health_score derivable from CDP traits) + audience definition editor (when health-band-gating audience logic uses health_score as JSON Logic var) + DSR request queue (when DSR priority gated by health_score); `<RetentionClassChip />` reused on profile detail (when retention class derivable from CDP traits) + ConsentState preview (legal_basis + retention_window) + audience definition editor (when retention-class-gating audience logic); future 221 admin surfaces inherit the 216 PII+Retention contract — the 5-classification ENUM is the load-bearing badge for all CDP PII rendering; the 216 inherited `data_retention_classes` pattern carries to ConsentState retention behavior — D-24 retains ConsentState on DSR deletion with `deletion_evidence_ref` populated per legal defensibility of suppression)
  - 217-UI-SPEC.md (PARENT — `saas_nav_visibility` 12 SG planned-only seed continues to apply; 221 substrate makes future admin namespace eligible: `saas_cdp` (NEW namespace anticipated for future 221 admin surface — unified profile viewer, identity merge approval queue, identity link browser, consent state browser, consent drift resolution queue, CDP event browser with monthly partition selector, trait snapshot viewer + recompute trigger, audience definition editor with JSON Logic DSL preview, DSL operator whitelist viewer, audience snapshot browser + membership preview, audience activation approval queue, DSR request queue + tombstone status, DSR export approval queue, dual-write reconciliation timeline, 8-table RLS denial test viewer); the 221-06 closeout does NOT directly modify the 217 seed because the planned 221 admin/tenant surfaces are deferred to future phases; however, future 221 admin/tenant frontend phases that compose `app/saas/cdp/{profiles,identity-links,consent,events,traits,audiences,dsr,reconciliation}/page.tsx` MUST extend `saas_nav_visibility` with new rows — each row gated by `SAAS_NAV_REQUIRES_ACTIVATION` DB-trigger; 217-06 `<SaaSActivationPanel />` + `<SaaSSubscriptionsTable />` + 7 NEW extracted components (`<HealthScoreBadge />`, `<RiskBandBadge />`, `<KbGroundingPanel />`, `<SaveOfferPricingBlock />`, `<RetentionClassChip />`, `<PIIRedactedField />`, `<ClassifierChipRow />`) are first-class production primitives that future 221 admin surfaces compose; **D-21 server/client boundary doctrine carries forward verbatim** — every future 221 admin / tenant surface MUST be a default server component reading via `requireHostedSupabaseAuth(request)` (per D-32 architecture-lock — NOT `requireSupabaseAuth`) + tenant-scoped supabase client; client components opt in via `'use client'` only for interactive primitives (unified profile viewer with timeline expansion, identity merge approval modal with hard/soft scoring breakdown, identity link browser with score breakdown, consent state browser with drift indicator, consent drift resolution modal with which-side-wins selector, CDP event browser with monthly partition selector, trait snapshot viewer with recompute-trigger button, audience definition editor with JSON Logic DSL preview + whitelist validator, DSL operator whitelist viewer with rejection log, audience snapshot browser + membership preview, audience activation approval modal with consent re-validation preview, DSR request queue with tombstone status, DSR export approval modal with PII cardinality preview, dual-write reconciliation timeline with divergence indicators, 8-table RLS denial test viewer); **D-32 ARCHITECTURE-LOCK CARRIES FORWARD VERBATIM** — future 221 admin surfaces are LEGACY `api/*.js` routes (NOT App Router `app/api/.../route.ts`); `requireHostedSupabaseAuth` from `onboarding/backend/runtime-context.cjs:491` (NOT `requireSupabaseAuth`); test runner is `npm test` (Node `--test`) with `node:test` + `node:assert/strict` imports, `*.test.js` files (`.test.ts` FORBIDDEN); OpenAPI lives at `contracts/openapi.json` (NOT `public/openapi.json`); MCP registry at `lib/markos/mcp/tools/index.cjs` (NOT `.ts`); cron in `api/cron/` (singular) with `*.js` filenames + `x-markos-cron-secret` header; helper canon `buildApprovalPackage` (NOT `createApprovalPackage`) — future surfaces MUST honor these locks)
  - 220-UI-SPEC.md (PARENT — END-OF-v4.1.0 milestone state; 26 P208 handoff_kind chips at v4.1.0 closeout; 221 opens v4.2.0 commercial-engines lane and EXTENDS to 29 chips with 3 new literals `consent_drift_resolution` 27th + `audience_activation_approval` 28th + `dsr_export_approval` 29th; the 220 6-future-surface UI binding contracts pattern (Referral / Community / Event Marketing / PR-Analyst-Review / Partnership-Affiliate-Developer / 28 Growth Agents Readiness) is REUSED with 5 parallel future-surface UI binding contracts in 221 (Unified Profile / Consent State / Audience Definition / DSR + Audit / Identity Merge); the 220 sibling translation gate pattern carries forward — 221 opens parallel translation gates `future_phase_221_admin_ui` + `future_phase_221_approval_inbox_extensions` for the 3 new handoff_kind chips + `future_phase_221_dsr_console` + `future_phase_221_audience_editor` + `future_phase_222_attribution_substrate` + `future_phase_223_dispatch_substrate` for downstream commercial-engines consumers)
  - 213.4-VALIDATION.md §Carry-forward (D-08 token-only, D-09 mint-as-text, D-09b `.c-notice` mandatory, D-13 `.c-card--feature` reserved, D-14 no `.c-table`, D-15 selective extraction, D-21 server/client boundary)
translation_gates_dissolved_by_221:
  - "220-UI-SPEC §end-of-v4.1.0 future_phase_222_attribution_substrate (anticipated future commercial-engines lane consumer placeholder) — DISSOLVED at the substrate-feed layer for the CDP-events-as-attribution-input contract. 221-03 ships `cdp_events` (10-domain ENUM monthly-partitioned append-only event SOR per D-08) + `cdp_trait_snapshots` (real_time/hourly/daily freshness modes per D-15) which are the load-bearing attribution-input substrate that future P222 attribution + P225 analytics consume. The future CRM 360 timeline (P222) reads CDP events via `lib/markos/cdp/adapters/crm-projection.ts::getProfileForContact` per D-20 (read-through adapter); CRM keeps its schema, adds `canonical_identity_id` FK only. The `<PlaceholderBanner variant=\"future_phase_222_attribution_substrate\">` is REMOVED from 220-consuming surfaces at the backend-contract layer; UI-layer placeholder removal requires the future P222+ commercial-engines tenant frontend phase."
  - "220-UI-SPEC §end-of-v4.1.0 future_phase_223_dispatch_substrate (anticipated future commercial-engines lane consumer placeholder) — DISSOLVED at the substrate-feed layer for the ConsentState + AudienceSnapshot dispatch contract. 221-02 ships `cdp_consent_states` (5-channel state-of-record + 5-state ENUM per channel per D-11) + 221-04 ships `cdp_audience_definitions` + `cdp_audience_snapshots` + `cdp_audience_snapshot_memberships` (immutable frozen membership per D-17 + D-18 + D-19) which are the load-bearing dispatch-input substrate that future P223 messaging engine + P224 conversion/launch + P225 analytics + P226 sales enablement consume. **D-13 enforces** every downstream send/dispatch engine MUST consume `ConsentState` (never a channel-local flag) — enforced at planner/reviewer time for P222-P226. **D-18 double-gate** — snapshot membership freezes who COULD be reached; dispatch confirms who CAN legally be reached right now via current ConsentState re-validation. The `<PlaceholderBanner variant=\"future_phase_223_dispatch_substrate\">` is REMOVED from 220-consuming surfaces at the backend-contract layer; UI-layer placeholder removal requires the future P223+ commercial-engines tenant frontend phase."
  - "215-UI-SPEC §future_phase_217_pii_audit_log (PII audit log placeholder for `cdp_consent_states` + `cdp_identity_profiles` + `cdp_trait_snapshots` rendering) — DISSOLVED at the substrate-feed layer. 221-05 migration 105 reserves audit-log indexes for the 8+ CDP event types Plan 06 emits (`cdp_identity_profile_created` / `cdp_identity_link_created` / `cdp_merge_decision_recorded` / `cdp_consent_state_set` / `cdp_consent_drift_detected` / `cdp_dsr_tombstone_executed` / `cdp_dsr_cascade_purged` / `cdp_dsr_export_dispatched` + `cdp.consent_writer_violation` single-writer trigger rejection) — extending the SOC2 admin audit log per P206 SOC2 Type I posture; future 215 admin UI surfaces composing CDP audit log views CAN read 221 substrate via the 8-table audit log query interface. The `<PlaceholderBanner variant=\"future_phase_217_pii_audit_log\">` is REMOVED at the backend-contract layer."
  - "MarkOS marketing operating loop CDP consumer placeholder (anticipated v2 operating loop CDP integration) — DISSOLVED at the substrate-feed layer. 221 ships first-party identity graph (CDP-01) + explainable merge/split with provenance (CDP-02) + audiences as governed data products (CDP-03) + CDP events flowing into CRM/analytics/launches (CDP-04) + privacy/retention/consent enforced consistently (CDP-05). The MarkOS v2 operating loop (per `obsidian/reference/MarkOS v2 Operating Loop Spec.md`) consumes CDP via the read-through adapter pattern — every operating-loop step that touches identity/consent/audience reads via `lib/markos/cdp/adapters/crm-projection.ts::getProfileForContact` rather than direct CDP SQL; the operating loop never bypasses CDP doctrine."
translation_gates_opened_by_221:
  - "future_phase_221_admin_ui — Multi-page CDP admin surface composing unified profile viewer (`app/saas/cdp/profiles/[profile_id]/page.tsx` future) + identity link browser (`app/saas/cdp/profiles/[profile_id]/identity-links/page.tsx` future) + identity merge approval queue (`app/saas/cdp/identity-merges/page.tsx` future) + consent state browser (`app/saas/cdp/profiles/[profile_id]/consent/page.tsx` future) + consent drift resolution queue (`app/saas/cdp/consent-drifts/page.tsx` future) + CDP event browser with monthly partition selector (`app/saas/cdp/profiles/[profile_id]/events/page.tsx` future) + trait snapshot viewer + recompute trigger (`app/saas/cdp/profiles/[profile_id]/traits/page.tsx` future) + audience definition editor with JSON Logic DSL preview (`app/saas/cdp/audiences/[audience_id]/edit/page.tsx` future) + DSL operator whitelist viewer (`app/saas/cdp/audiences/dsl-whitelist/page.tsx` future) + audience snapshot browser + membership preview (`app/saas/cdp/audiences/[audience_id]/snapshots/[snapshot_id]/page.tsx` future) + audience activation approval queue (`app/saas/cdp/audience-activations/page.tsx` future) + DSR request queue + tombstone status (`app/saas/cdp/dsr/page.tsx` future) + DSR export approval queue (`app/saas/cdp/dsr-exports/page.tsx` future) + dual-write reconciliation timeline (`app/saas/cdp/reconciliation/page.tsx` future) + 8-table RLS denial test viewer (`app/saas/cdp/rls-audit/page.tsx` future). All read via 221 `/v1/cdp/*` API contracts (F-106..F-112); all gate via 217-06 `isSaaSSurfaceEnabled` 3-condition gate; all extend 217-06 `saas_nav_visibility` with new rows under namespace `saas_cdp`. **D-32 architecture-lock holds** — all routes are LEGACY `api/*.js` (NOT App Router `app/api/.../route.ts`); auth via `requireHostedSupabaseAuth` from `onboarding/backend/runtime-context.cjs:491`; tests are `*.test.js` only; OpenAPI at `contracts/openapi.json`; MCP registry at `lib/markos/mcp/tools/index.cjs`; cron at `api/cron/*.js` with `x-markos-cron-secret` header; helper canon `buildApprovalPackage`. Future surfaces render `<PlaceholderBanner variant=\"future_phase_221_admin_ui\">` until those phases ship."
  - "future_phase_221_approval_inbox_extensions — P208 Approval Inbox at `/operations/approvals` rendering 3 NEW handoff_kind chips (`consent_drift_resolution` 27th + `audience_activation_approval` 28th + `dsr_export_approval` 29th), filter chip set extends from 26 chips (post-220 = end-of-v4.1.0) to **29 chips post-221 — start-of-v4.2.0 commercial-engines lane**. Each chip renders the per-row classifier (consent drift resolution: divergence-side indicator (`cdp_consent_states` vs `outboundConsentRecords`) + which-side-wins selector + audit row preview + single-writer GUC `app.consent_writer_source = 'cdp_setConsentState'` lock confirmation; audience activation approval: snapshot.membership_count + suppression_count_at_snapshot + consent re-validation preview against current ConsentState (D-18 double-gate visualization — snapshot freezes who COULD; dispatch confirms who CAN); DSR export approval: profile_id + IdentityProfile cardinality + IdentityLink[] cardinality + cdp_events[] cardinality + TraitSnapshot[] cardinality + ConsentState[] cardinality + AudienceSnapshotMembership[] cardinality + dual-approval indicator + `<PIIRedactedField />` per PII row + admin-role gate indicator). Row rendering extension is DEFERRED to a future P208 admin extension phase. Future surfaces render `<PlaceholderBanner variant=\"future_phase_221_approval_inbox_extensions\">` until that phase ships."
  - "future_phase_221_dsr_console — Tenant-facing DSR (Data Subject Request) console composing `<DsrRequestQueue />` (DSR request lifecycle states: `requested` / `tombstoned` / `exported` / `purged`; SLA timer per CCPA/GDPR jurisdiction lookup against `cdp_consent_states.jurisdiction`) + `<DsrTombstoneStatusIndicator />` (per profile_id — `tombstoned: true` + `deletion_evidence_ref` populated + PII columns nulled per D-24) + `<DsrCascadeTimeline />` (per profile_id — cdp_events partition drop + cdp_trait_snapshots delete + cdp_audience_snapshot_memberships delete + ConsentState retention indicator with `<.c-notice c-notice--info>` \"[info] ConsentState retained per legal defensibility of suppression\" per D-24) + `<DsrExportApprovalModal />` reusing 215 billing-correction modal recipe — 29th chip + dual-approval indicator + `<PIIRedactedField />` per row + 8-table RLS denial assertion preview + admin-role gate indicator + `data.export` mutation-class indicator + audit log + 213-04 public-proof boundary indicator (PRIVATE forever) + `<.c-badge--error>` \"[err] Highly sensitive PII export\" + envelope rendering of 6-payload-class structure per D-25. All consume 221 substrate via `/v1/cdp/dsr/*` (deferred — read-only profile API + future write API for DSR submission); all gate via SOC2 admin role + dual-approval. Future surfaces render `<PlaceholderBanner variant=\"future_phase_221_dsr_console\">` until those phases ship."
  - "future_phase_221_audience_editor — Tenant marketing audience editor composing `<AudienceDefinitionEditor />` (entity_type 2-chip `person` / `account` + objective text input + freshness_mode 3-chip `real_time` / `hourly` / `daily` + destination_families chip-multi-select + logic_json JSON Logic DSL editor with live preview against sample profile context `{ traits, consent, lifecycle_state, mode, jurisdiction }`) + `<DslOperatorWhitelistValidator />` (inline rejection on non-allowed operators per D-17 RL1 fix — whitelist is `{ if, ==, !=, <, <=, >, >=, in, and, or, not, +, -, *, /, var, missing, missing_some }`; rejection emits audit row `cdp_audience_dsl_rejected` with operator name + tenant_id + actor_id) + `<DslOperatorWhitelistViewer />` (read-only viewer of allowed operators with descriptive copy) + `<AudienceSnapshotBrowser />` (immutable snapshot rows with membership_count + suppression_count_at_snapshot + computed_at + actor_id + evidence_ref per D-19) + `<AudienceMembershipPreview />` (paginated CURSOR-based list of profile_ids + canonical_identity_id chips + `<.c-chip-protocol>` per profile + 4-pii_classification chip + `<.c-badge>` per consent state with drift indicator) + `<AudienceActivationApprovalModal />` reusing 215 billing-correction modal recipe — 28th chip + double-gate consent re-validation preview + dispatch eligibility indicator (per current ConsentState — NOT snapshot ConsentState) + jurisdiction filter + quiet_hours filter + audit log; activation invokes `validateBatch(profileIds[], channel)` from `lib/markos/cdp/audiences/activation.ts` exported for P223 dispatch pagination per Pitfall 4. All consume 221 substrate via `/v1/cdp/audiences/*` (F-111 + F-112); all gate via SaaSSuiteActivation + SaaS marketer role. Future surfaces render `<PlaceholderBanner variant=\"future_phase_221_audience_editor\">` until those phases ship."
  - "future_phase_222_attribution_substrate — Future commercial-engines lane consumer (P222 CRM Engine 360 Workspace + P225 Analytics Engine) consuming 221 substrate. P222 reads CDP via `lib/markos/cdp/adapters/crm-projection.ts::getProfileForContact` per D-20 (CRM-as-overlay; CDP is canonical SOR). P225 reads `cdp_events` partitioned monthly + `cdp_trait_snapshots` for attribution + journey + narrative semantic layer per ANL-01..05 carry-forward. Future surfaces render `<PlaceholderBanner variant=\"future_phase_222_attribution_substrate\">` until those phases ship."
  - "future_phase_223_dispatch_substrate — Future commercial-engines lane consumer (P223 Messaging Engine Orchestration + P224 Conversion + Launch + P226 Sales Enablement) consuming 221 substrate. P223 reads `cdp_consent_states` for channel-state-of-record per D-11 + D-13 (ConsentState replaces channel-local flags; never bypassed) AND reads `cdp_audience_snapshot_memberships` for activation + double-gate re-validation per D-18 (snapshot membership + current ConsentState consultation at send time). P224 reads `cdp_audience_snapshots` for conversion + launch coordination. P226 reads `cdp_identity_profiles` for sales enablement context. Future surfaces render `<PlaceholderBanner variant=\"future_phase_223_dispatch_substrate\">` until those phases ship."
---

# Phase 221 — UI Design Contract (no-UI-scope)

> **Phase 221 ships zero UI surfaces.** This document is the explicit no-surface declaration for the **CDP Identity, Audience, and Consent Substrate** phase — the **opening phase of the v4.2.0 Commercial Engines lane** (CDP-01..05 + start of CRM-01..05 → P222 / EML-01..05 + MSG-01..05 → P223 / CNV-01..05 + LCH-01..05 → P224 / ANL-01..05 → P225 / SEN-01..05 → P226 / ECO-01..05 → P227 / COM-01..06 → P228). There is no `app/`, no `components/`, no `*.stories.tsx`, no `page.tsx`, no `layout.tsx`, no `*.module.css`, and no `*.css` modified or created in any of the six plans (221-01 through 221-06). The existing P208 Approval Inbox + P217-06 saas_nav_visibility existing surfaces consume 221 contracts as downstream readers — they are NOT modified by 221.
>
> **Critical posture:** Phase 221 is the **first-party customer-data substrate (system-of-record)** for identity resolution, consent, event normalization, trait materialization, and activation-safe audience snapshots across MarkOS. It is the SOR layer consumed by CRM (P222), messaging (P223), conversion/launch (P224), analytics (P225), sales enablement (P226), ecosystem (P227), and the SaaS Suite revenue/health stack (P214-P217). The phase ships across 6 substrate layers:
>
> **Layer 1 — Identity Profiles + Identity Links + canonical_identity_id FK** (221-01): `cdp_identity_profiles` (tenant-scoped SOR; `profile_type` ENUM `person | account` per D-02; `mode` ENUM `b2b | b2c | plg_b2b | plg_b2c | b2b2c` per D-03; tenant fail-closed RLS) + `cdp_identity_links` (immutable lineage with `link_status` + `source_event_ref` + `reviewer_actor_id` per D-05) + canonical_identity_id FK on CRM entity tables (`crm_contacts`, `crm_companies`, `crm_accounts`, `crm_customers`) per D-20. **Hard-match signals (auto-resolve, confidence=1.0)**: verified email + authenticated user_id + billing_customer_id + subscription_id + workspace_id. **Soft-match signals (current weights from `lib/markos/crm/identity.ts:18-22` carried verbatim)**: email_exact_match=0.65 + domain_match=0.15 + device_match=0.10 + session_overlap=0.10 + form_submitted=0.15. **Thresholds (P101 D-04 carry-forward)**: ≥0.80 auto-accept (soft), 0.40-0.79 review, <0.40 reject. **Wave 0 install** of `json-logic-js@2.0.5` EXACT pin (no caret) for D-17 audience DSL reproducibility (RL2). **Wave 0 architecture-lock detector** + helper-presence test + `scripts/preconditions/221-check-upstream.cjs` hard-fails when REQUIRED_UPSTREAM = [P207, P209, P210, P214, P215, P216, P217] missing per D-35 (NO bridge stubs; NO "stub if missing"; NO soft-skip). **F-ID slot pre-allocation** (per RM4): F-106..F-112 per D-30. **Migration slot pre-allocation** (per RM4 high-water at plan time `100_crm_schema_identity_graph_hardening.sql`): 101_cdp_identity_core / 102a_cdp_consent_states / 102b_cdp_events / 103_cdp_trait_snapshots / 104_cdp_audience_objects / 105_cdp_deletion_and_audit / 106_cdp_consent_single_writer_trigger.
>
> **Layer 2 — Consent State of Record + Single-Writer DB-Trigger + Hourly Drift Audit** (221-02): `cdp_consent_states` (per-profile SOR with email_marketing/sms_marketing/whatsapp_marketing channel state ENUM per D-11; legal_basis ENUM `consent | contract | legitimate_interest | unknown`; jurisdiction; preference_tags[]; quiet_hours; source + source_timestamp). **Single-writer DB-trigger** on `outboundConsentRecords` (migration 106) reads PostgreSQL session GUC `app.consent_writer_source` and rejects writes outside `cdp_setConsentState` per D-39 (RM2 fix; forward-port of P223 D-51 pattern); library `setConsentState` (P221 sole writer) sets `SET LOCAL app.consent_writer_source = 'cdp_setConsentState'` inside its transaction. **D-12 ConsentState shim** — `lib/markos/outbound/consent.ts::evaluateOutboundEligibility` reads ConsentState first, falls back to legacy `outboundConsentRecords` on miss; public signature unchanged. **HOURLY drift audit cron** at `api/cron/cdp-consent-drift-audit.js` (`0 * * * *`) per D-40 (RM1 fix — NOT daily; divergence visible from first hour, not Plan 06 closeout); fail-closed send behavior when divergence detected for the affected contact; emits operator task + audit log row on divergence.
>
> **Layer 3 — cdp_events + cdp_trait_snapshots + Dual-Write + Recompute Crons** (221-03): `cdp_events` (canonical append-only event SOR per D-08; envelope `{ event_id, tenant_id, event_name, event_domain, occurred_at, profile_id?, account_id?, anonymous_id?, properties }`; `event_domain` ENUM 10-value `{ website, product, email, messaging, crm, billing, support, social, ads, partner }`; **partitioned by month on occurred_at** so DSR purge = drop partition; PK `(event_id, occurred_at)` for idempotency per RUN-06). `crm_activity` becomes a HIGH_SIGNAL projection filtered from `cdp_events` via `lib/markos/crm/tracking.ts::normalizeTrackedActivity` per D-09 (operator-facing timelines keep current noise posture). **Dual-write** at `api/tracking/ingest.js` + `api/tracking/identify.js` per D-10 — public HTTP contract preserved; internal dual-write to `cdp_events` (raw) + `crm_activity` (projection) during transition; CDP failure logged but does NOT suppress CRM write (grace mode). `cdp_trait_snapshots` (per-profile materialized rows per D-14; `{ trait_name, value, computed_at, source_event_ref[], freshness_mode, confidence }`; **trait provenance `source_event_ref[]` mandatory** per D-16 — no black-box traits; feeds P209 EvidenceMap). **Recompute crons**: hourly + daily at `api/cron/cdp-trait-recompute-{hourly,daily}.js` per D-15 (real_time inline in dual-write path; hourly for lifecycle/activation; daily for fit/persona/expansion/churn-risk; matches P209 claim TTL + freshness rules). **Pitfall 5 late-event guard**: tombstoned profiles reject new event linkage (dual-write writes `profile_id=NULL` when target is tombstoned; no PII leak to deleted profile).
>
> **Layer 4 — cdp_audience_definitions + cdp_audience_snapshots + cdp_audience_snapshot_memberships + JSON Logic DSL + Operator Whitelist + Double-Gate** (221-04): `cdp_audience_definitions` per D-17 (`{ segment_id, tenant_id, name, objective, entity_type, logic_json, freshness_mode, destination_families[] }`; **the existing `lib/markos/contracts/schema.ts` Segment brand-taxonomy type is NOT overloaded** — Pitfall 6; new namespace `lib/markos/cdp/audiences/*`). **DSL** = JSON Logic via `json-logic-js@2.0.5` EXACT pin (Wave 0 install per Plan 01) evaluated against `{ traits, consent, lifecycle_state, mode, jurisdiction }` profile context — **no SQL strings, no custom AST**. **Operator whitelist** per RL1 fix — `logic_json` operators RESTRICTED to `{ if, ==, !=, <, <=, >, >=, in, and, or, not, +, -, *, /, var, missing, missing_some }` BEFORE evaluation; any unknown operator → reject with `CDP_DSL_OPERATOR_REJECTED` + audit row `cdp_audience_dsl_rejected` (tenant-controlled `logic_json` is attack surface; whitelist is the safety boundary). `cdp_audience_snapshots` is **immutable after freeze** — rows in `cdp_audience_snapshots` + `cdp_audience_snapshot_memberships` are append-only; UPDATE/DELETE disallowed via RLS. **Every snapshot compute writes one audit row** per D-19 (`{ snapshot_id, audience_id, computed_at, membership_count, suppression_count_at_snapshot, actor_id, evidence_ref }` — operator-visible log). **Double-gate** per D-18 — snapshot membership is NECESSARY but NOT SUFFICIENT for activation; dispatch re-validates consent + suppression + jurisdiction + quiet_hours against CURRENT ConsentState (NOT the snapshot). Snapshot freezes who COULD be reached; dispatch confirms who CAN legally be reached right now. **Activation gate exports** `validateBatch(profileIds[], channel)` for P223 dispatch to paginate consent re-validation (Pitfall 4).
>
> **Layer 5 — CDP Read Surface + crm-projection Adapter + Legacy api/v1/cdp/*.js + 2 MCP Tools** (221-05): `lib/markos/cdp/adapters/crm-projection.ts::getProfileForContact(tenantId, contactId)` returns `CDPProjection { canonical_identity_id, consent_state, traits, profile_type, lifecycle_state }` — the seam CRM timeline + attribution + SaaS Suite bridge consume per D-20 + D-21 (CRM `timeline.ts` + `attribution.ts` behavior UNCHANGED — no code edits). **Public read-only API routes** at `api/v1/cdp/{profiles,consent,events,traits,audiences}.js` per D-22 (LEGACY `api/*.js` per D-32 RM3 — NOT App Router `app/api/.../route.ts`); auth via `requireHostedSupabaseAuth` from `onboarding/backend/runtime-context.cjs:491` (NOT `requireSupabaseAuth`); CURSOR-based pagination (NOT offset). **Tenant-isolation regression** — cross-tenant request returns 404 (NOT 403 — indistinguishable from non-existent profile to prevent enumeration) AND writes audit row `cdp.unauthorized_cross_tenant_access`. **MCP tools** at `lib/markos/mcp/tools/cdp/{get-unified-profile,get-consent-state}.cjs` per D-22 (CommonJS per D-38 — NOT `.ts`); registered via `lib/markos/mcp/tools/index.cjs` append; **NO raw PII in MCP payloads** per D-28; `consent_state` returned by `get_consent_state` REDACTS `deletion_evidence_ref` (operator-only field). **Tombstoned profiles return 404** from F-106 GET (D-24 PII scrub + DSR enumeration prevention). Mutations (merge review accept/reject, audience create, consent update, profile delete) per D-23 are **tenant-operator UI + review flows — NOT public API in P221**; public write APIs land with the consuming engine (P222/P223) so contracts match dispatch reality.
>
> **Layer 6 — DSR Tombstone + Cascade Purge + Dual-Write Reconciliation + Pitfall 5 Late-Event + Plan 06 checkpoint:human-action** (221-06): `tombstoneIdentityProfile(tenantId, profileId, evidenceRef)` scrubs PII (primary_email, primary_phone, company_name → NULL), sets `tombstoned=true` + `deletion_evidence_ref`, preserves `profile_id` for FK integrity per D-24. **Cascade purges** `cdp_events` (partition-aware DELETE plan) + `cdp_trait_snapshots` + `cdp_audience_snapshot_memberships` for the tombstoned profile per D-24. **ConsentState is RETAINED** (not deleted) with `deletion_evidence_ref` set — legal defensibility of suppression per D-24 (FK ON DELETE RESTRICT from Plan 02 enforces this); future re-contact attempts MUST fail-closed against retained consent tombstone. **DSR export contract** emits `{ IdentityProfile, IdentityLink[], cdp_events[], TraitSnapshot[], ConsentState[], AudienceSnapshotMembership[] }` per D-25; cascade to CRM via P222 DSR coordination. **Pitfall 5 late-event guard** ACTIVELY TESTED end-to-end — late-arriving ingest event after tombstone writes with `profile_id=NULL`. **Full 8-table RLS suite** runs in one file proving cross-tenant denial on cdp_identity_profiles + cdp_identity_links + cdp_events + cdp_consent_states + cdp_trait_snapshots + cdp_audience_definitions + cdp_audience_snapshots + cdp_audience_snapshot_memberships. **Dual-write drift reconciliation cron** DAILY at `0 7 * * *` (`api/cron/cdp-dual-write-reconciliation.js`) compares `crm_activity` vs `cdp_events` row counts per tenant; emits operator task if divergence > 0.1% (Pitfall 1 operationalized — deeper than Plan 02 hourly drift audit). **F-ID-collision regression test** (RM4) asserts `contracts/F-1{06,07,08,09,10,11,12}-*.yaml` exist + no other F-ID overlaps; flow-registry has all 7 entries. **Migration-slot-collision regression test** (RM4) asserts `supabase/migrations/{101,102a,102b,103,104,105,106}_*.sql` exist + no other CDP migrations reuse those slots. **Plan 06 `checkpoint:human-action`** (RL3 — `autonomous: false`) — operator validates first drift batch reconciliation classification thresholds + acceptable divergence patterns BEFORE the daily reconciliation cron runs unattended (P223 D-49 / P226 W1 pattern carry). **EVD-06 (pricing/competitor traits) explicitly OUT OF SCOPE** for P221 — noted in Plan 06 requirements field for coverage-gate compliance; deferred to later CDP enrichment phase.
>
> The three new Approval Inbox handoff_kind literals (`consent_drift_resolution` 27th + `audience_activation_approval` 28th + `dsr_export_approval` 29th) extend the P208 filter chip set from 26 literals (post-220 = end-of-v4.1.0) to **29 literals (post-221 — start-of-v4.2.0 commercial-engines lane)**.
>
> Authority chain: DESIGN.md v1.1.0 -> 213.4-VALIDATION.md carry-forward (D-08 token-only, D-09 mint-as-text, D-09b .c-notice mandatory, D-13 .c-card--feature reserved, D-14 no .c-table, D-15 selective extraction, D-21 server/client boundary) -> 206-UI-SPEC (mutation-class origin: `data.export` for DSR full-profile export + audit log export, `external.send` for consent override emits, `default_approval_mode == dual_approval` for `dsr_export_approval` per SOC2 P206) -> 207-UI-SPEC (RunApiEnvelope, AgentRunEventType for 30+ new event types across 6 substrate layers, ApprovalHandoffRecord) -> 208-UI-SPEC (PARENT — Approval Inbox extends 3 new handoff_kind literals 27th-29th in canonical chain; START-OF-v4.2.0 chip count = 29) -> 209-UI-SPEC (PARENT — ConsentState provenance + audit log evidence binding; trait provenance source_event_ref[] mandatory; EVD-01 + EVD-03 + EVD-04 + EVD-05 carry; immutable evidence_refs pattern) -> 213-UI-SPEC (213-04 public-proof boundary STRICT for DSR exports — PRIVATE forever; banned-lexicon zero-match BEFORE data.export / external.send dispatch) -> 214-UI-SPEC (PARENT — SaaS Suite Activation; saas_subscriptions FK; CDP profile read-through adapter pattern) -> 215-UI-SPEC (PARENT — sensitive credential UI binding Layer 6 EXTENDS to PII data; 215 billing-correction modal recipe REUSED for dsr_export_approval + audience_activation_approval + consent_drift_resolution) -> 216-UI-SPEC (PARENT — PII+Retention UI binding contract; 5 pii_classification badges (`no_pii`/`pseudonymous`/`personal`/`sensitive`/`highly_sensitive`) carry verbatim and apply to all CDP IdentityProfile + ConsentState + TraitSnapshot PII fields; `<PIIRedactedField />` clipboard preventDefault + audit-log identity_view extends 216 credential_view pattern; HealthScoreBadge + RiskBandBadge + RetentionClassChip + PIIRedactedField extracted-component recipes referenced as load-bearing primitives) -> 217-UI-SPEC (PARENT — saas_nav_visibility consumer eventually; D-21 server/client boundary doctrine carries verbatim; **D-32 architecture-lock carries verbatim** — legacy api/*.js + requireHostedSupabaseAuth + npm test Node `--test` + contracts/openapi.json + lib/markos/mcp/tools/index.cjs + buildApprovalPackage; future_phase_220_referral_console placeholder pattern reused as parallel future_phase_221_admin_ui pattern) -> 220-UI-SPEC (PARENT — END-OF-v4.1.0 milestone state; 26 P208 chips at v4.1.0 closeout; 221 opens v4.2.0 commercial-engines lane and EXTENDS to 29 chips with 3 new literals; the 220 6-future-surface UI binding contracts pattern reused as 5 parallel contracts in 221 — Unified Profile / Consent State / Audience Definition / DSR + Audit / Identity Merge) -> this document.
>
> **Start-of-v4.2.0 commercial-engines lane summary** (see Future-Surface UI Binding Contracts and Translation Gates sections below): 29 P208 handoff_kind chips total at start-of-v4.2.0. Generated by gsd-ui-researcher 2026-05-04. Status: draft (checker upgrades to approved once the no-UI declaration is verified).

---

## Scope Verification

The orchestrator preliminary finding has been verified by reading all six plans plus context, research, and reviews. The full file set declared in `files_modified` across 221-01..221-06 is enumerated below, with surface classification per file:

| File class | Path glob | Plan(s) | UI surface? |
|------------|-----------|---------|-------------|
| Phase doctrine | `.planning/phases/221-.../{221-CONTEXT, 221-RESEARCH, 221-REVIEWS, 221-VALIDATION, 221-DISCUSSION-LOG, 221-{01..06}-PLAN}.md` | 221-01..221-06 | NO |
| Preflight surface | `lib/markos/cdp/preflight/{architecture-lock.ts, upstream-gate.ts, errors.ts}` (3 files) | 221-01 | NO (TS modules; no JSX) |
| Preflight script | `scripts/preconditions/221-check-upstream.cjs` (1 file) | 221-01 | NO |
| Identity domain modules | `lib/markos/cdp/identity/{profile.ts, link.ts, merge.ts, graph.ts, hash.ts}` (5 files) | 221-01 | NO |
| Consent domain modules | `lib/markos/cdp/consent/{state.ts, shim.ts, drift-audit.ts}` (3 files) + `lib/markos/outbound/consent.ts` (edit) | 221-02 | NO |
| Events domain modules | `lib/markos/cdp/events/{envelope.ts, dual-write.ts}` (2 files) | 221-03 | NO |
| Traits domain modules | `lib/markos/cdp/traits/{snapshot.ts, recompute.ts}` (2 files) | 221-03 | NO |
| Audiences domain modules | `lib/markos/cdp/audiences/{definition.ts, snapshot.ts, dsl.ts, dsl-whitelist.ts, activation.ts}` (5 files) | 221-04 | NO |
| Adapters | `lib/markos/cdp/adapters/crm-projection.ts` (1 file) | 221-05 | NO |
| Deletion domain modules | `lib/markos/cdp/deletion/{tombstone.ts, cascade.ts, dsr-export.ts}` (3 files) | 221-06 | NO |
| Observability | `lib/markos/cdp/observability/dual-write-reconciliation.ts` (1 file) | 221-06 | NO |
| MCP tools | `lib/markos/mcp/tools/cdp/{get-unified-profile,get-consent-state}.cjs` (2 files) + `lib/markos/mcp/tools/index.cjs` (registry append) + `lib/markos/mcp/pii-guard.cjs` | 221-05 | NO (CommonJS MCP descriptors; no JSX) |
| Migrations | `supabase/migrations/{101,102a,102b,103,104,105,106}_cdp_*.sql` (7 files) | 221-01..221-06 | NO (SQL DDL) |
| Node API handlers | `api/v1/cdp/{profiles/index.js, profiles/[profile_id].js, profiles/[profile_id]/traits.js, consent/[profile_id].js, events/index.js, audiences/[audience_id].js, audiences/[audience_id]/snapshots/index.js, audiences/[audience_id]/snapshots/[snapshot_id].js}` (8 files) | 221-05 | NO (legacy api/*.js read-only JSON; no JSX) |
| Tracking ingest (edit) | `api/tracking/ingest.js` + `api/tracking/identify.js` (2 files; public contract preserved per D-10) | 221-03 | NO |
| Cron handlers | `api/cron/{cdp-consent-drift-audit, cdp-trait-recompute-hourly, cdp-trait-recompute-daily, cdp-dual-write-reconciliation}.js` (4 files) | 221-02..221-06 | NO |
| Cron config | `vercel.json` (edit; cron schedule entries) | 221-02..221-06 | NO |
| F-ID contract YAMLs | `contracts/F-{106..112}-cdp-*.yaml` (7 files) + `contracts/flow-registry.json` | 221-01..221-06 | NO |
| Test fixtures | `test/fixtures/cdp/{profiles,consent,events,audiences,index}.js` (5 files; Node `--test` compatible per D-36) | 221-01 | NO |
| Test files | `test/{cdp-preflight,cdp-identity,cdp-consent,cdp-events,cdp-traits,cdp-audiences,cdp-adapters,cdp-deletion,cdp-observability,api-contracts,agents,tenant-auth,crm-schema,crm-outbound,crm-timeline,crm-reporting,tracking}/*.test.js` | 221-01..221-06 | NO |
| package.json | `package.json` (edit; add `json-logic-js@2.0.5` EXACT pin per RL2) | 221-01 | NO |

**Search assertions** (verified by orchestrator via grep over all `files_modified` blocks; result: `NO UI PATHS FOUND IN ANY PLAN — confirmed pure backend phase`):

| Assertion | Result |
|-----------|--------|
| `files_modified` glob `app/**` across 221-01..221-06 | 0 matches |
| `files_modified` glob `app/(markos)/**` or `app/(public)/**` across 221-01..221-06 | 0 matches (architecture-lock D-32 forbidden) |
| `files_modified` glob `app/api/.../route.ts` across 221-01..221-06 | 0 matches (architecture-lock D-32 forbidden — App Router migration deferred) |
| `files_modified` glob `components/**` across 221-01..221-06 | 0 matches |
| `files_modified` glob `*.stories.tsx` across 221-01..221-06 | 0 matches |
| `files_modified` glob `*.module.css` or `*.css` across 221-01..221-06 | 0 matches |
| `files_modified` containing `page.tsx`, `layout.tsx`, or `route.ts` | 0 matches |
| `files_modified` containing `tailwind.config.*`, `app/globals.css`, `app/tokens.css`, `styles/components.css` | 0 matches |
| `files_modified` containing `.test.ts` (architecture-lock D-36 FORBIDDEN — Path A pinned) | 0 matches |
| `files_modified` containing `vitest.config.ts` or `playwright.config.ts` (D-36 FORBIDDEN) | 0 matches |
| `files_modified` containing `lib/markos/mcp/tools/index.ts` (D-38 FORBIDDEN — `.cjs` only) | 0 matches |
| `files_modified` containing `public/openapi.json` (D-37 FORBIDDEN — `contracts/openapi.json` only) | 0 matches |

**Conclusion:** No-UI-surface declaration **CONFIRMED**. **If the checker finds ANY UI surface in plan files_modified blocks, BLOCK.**

---

## Design System

| Property | Value |
|----------|-------|
| Tool | not applicable — no UI surface authored in this phase |
| Preset | not applicable |
| Component library | not applicable |
| Icon library | not applicable |
| Heading font | not applicable (DESIGN.md v1.1.0 inherited for future surfaces — JetBrains Mono) |
| Body font | not applicable (DESIGN.md v1.1.0 inherited for future surfaces — Inter) |
| Default theme | not applicable (DESIGN.md v1.1.0 default-dark Kernel Black `#0A0E14` inherited) |
| Form authoring posture | not applicable — no forms |
| Banner authoring posture | not applicable — no banners |
| Card authoring posture | not applicable — no cards |
| Modal authoring posture | not applicable — the three new P208 Approval Inbox handoff_kind chips (`consent_drift_resolution`, `audience_activation_approval`, `dsr_export_approval`) reuse the existing P208 modal recipe; all three additionally REUSE 215 billing-correction modal recipe verbatim with `<PIIRedactedField />` (216 inheritance) for PII display |
| Storybook posture | not applicable — `test/cdp-observability/` test surface scans contracts WHEN SHIPPED (deferred); v1 tests against API contracts (every read-only endpoint paired with corresponding task/approval write endpoint per RESEARCH §15 Anti-patterns inheritance from 220) |

---

## Spacing Scale / Typography / Color

Not applicable — Phase 221 ships zero UI surfaces. Future P221+ admin/tenant surfaces inherit the DESIGN.md v1.1.0 8px-grid spacing scale (0/2/8/16/24/32/48/96 — every dimension snaps; off-grid values are bugs), JetBrains Mono (display + headings + code) + Inter (body + UI) typography stack, and 60/30/10 color distribution (Kernel Black `#0A0E14` 60% / Border Mist `#2D3441` secondary 30% / Protocol Mint `#00D9A3` < 5% reserved as the single CTA / focus signal). State color is never the only signal — pair with bracketed glyphs `[ok]` / `[err]` / `[warn]` / `[info]` (DESIGN.md "Color blindness" rule). No emoji in product UI, CLI, error messages, or technical docs (use bracketed glyphs). Borders over shadows: 1px Border Mist `#2D3441` hairline on dark beats a soft shadow; shadows reserved for popover/modal/toast. Whitespace as primitive: ≥30% empty surface in every composition. Focus rings are 2px solid `#00D9A3` with 2px offset, never suppressed. `prefers-reduced-motion` collapses durations to 0ms.

---

## Copywriting Contract

Not applicable for this phase's own surfaces (zero surfaces). For future P221+ admin/tenant surfaces, the inherited copywriting contract is the DESIGN.md banned-lexicon zero-match rule applied verbatim to **all 3 new mutation classes** (consent override copy emit + audience activation copy + DSR export envelope text fields rendering + drift-resolution operator-note). **Banned-lexicon validator MUST run BEFORE any `data.export` / `external.send` mutation dispatches the content** (mirror 216/218/219/220 pattern; 221 expands the validator-count by 3 new mutation classes — see Future-Surface UI Binding Contracts below for per-domain validator placement).

Banned lexicon (per CLAUDE.md visual canon): `synergy`, `leverage`, `empower`, `unlock`, `transform`, `revolutionize`, `supercharge`, `holistic`, `seamless`, `cutting-edge`, `innovative`, `game-changer`, `next-generation`, `world-class`, `best-in-class`, `reimagine`, `disrupt`, `just` (as softener). No exclamation points in product surface copy.

The three new P208 Approval Inbox handoff_kind chip labels:

| Chip Index | Literal | Mutation-Class | Approval Mode |
|------------|---------|----------------|---------------|
| 27 | `consent_drift_resolution` | (informational + remediation; no external mutation — selects authoritative source between `cdp_consent_states` and legacy `outboundConsentRecords` row + commits via single-writer GUC) | single_approval |
| 28 | `audience_activation_approval` | `external.send` (downstream — D-13 enforcement) | single_approval |
| 29 | `dsr_export_approval` | `data.export` | dual_approval (per SOC2 P206 Type I — DSR exports contain highly_sensitive PII) |

---

## Registry Safety

Not applicable — Phase 221 ships zero UI surfaces; no shadcn registry or third-party component blocks are added. The 213.4 carry-forward shadcn registry posture (none — manual primitives in `styles/components.css`) remains in force for future P221+ admin/tenant surfaces.

The Wave 0 install of `json-logic-js@2.0.5` EXACT pin (Plan 01 Task 1; D-17; RL2) is a runtime DSL evaluator dependency — NOT a component registry. Its operator surface is restricted by the whitelist (`lib/markos/cdp/audiences/dsl-whitelist.ts`) per RL1 fix.

---

## Future-Surface UI Binding Contract 1 — Unified Profile

For future P221+ admin/tenant surfaces consuming Layer 1 substrate (`cdp_identity_profiles` + `cdp_identity_links` + canonical_identity_id FK + hard/soft merge decision logic + identity merge timeline). The contract is load-bearing — it specifies the visual primitives, copywriting rules, mutation wiring, PII display rules, and approval-modal reuse pattern that future unified-profile-viewer UI phases MUST honor. Until those phases ship, surfaces that reference identity profile substrate render `<PlaceholderBanner variant="future_phase_221_admin_ui">`.

| Surface element | DESIGN.md primitive | Token / class | Mutation wiring |
|-----------------|--------------------|----|-----------------|
| Unified profile viewer header | `<.c-card>` + `<.c-chip-protocol>` for canonical_identity_id (Protocol Mint reserved usage — `--color-primary` < 5%) + `<.c-badge>` for `profile_type` ENUM (`person` / `account`) + `<.c-badge>` for `mode` ENUM 5-value (`b2b` / `b2c` / `plg_b2b` / `plg_b2c` / `b2b2c`) + `<.c-badge>` for `lifecycle_state` | `--color-surface-raised` + `--color-text` + `--color-primary` (chip-protocol) + bracketed glyphs | GET /v1/cdp/profiles/{id} (F-106 read-only contract per D-22); CURSOR pagination on listing endpoints |
| **PII fields (primary_email, primary_phone, company_name, full_name, ip_address, user_agent, device_fingerprint)** | `<PIIRedactedField field_name={...} pii_classification={...} value={...} />` (216 inheritance — load-bearing primitive) | per `pii_classification` ENUM 5-value mapping per 216-04: `no_pii` → `--color-text` direct render; `pseudonymous` → `--color-text` direct render + `<.c-badge--info>` "[info] pseudonymous"; `personal` → `[REDACTED]` placeholder via `.c-code-inline` + `--color-on-surface-subtle` + `<.c-badge--info>` "[info] personal"; `sensitive` → `[REDACTED]` placeholder + admin-role gate + audit log + `<.c-badge--warning>` "[warn] sensitive"; `highly_sensitive` → `[REDACTED]` placeholder + admin-role + dual-approval gate + audit log + `<.c-badge--error>` "[err] highly_sensitive" | `<PIIRedactedField />` `onCopy` interceptor MUST `preventDefault()` to block PII clipboard exfiltration (216 inheritance verbatim); audit-log `event_type == 'identity_view'` mirrors 216 `credential_view` pattern (every PII field render writes audit row to `markos_audit_log` P201 hash chain) |
| Identity merge timeline | `<.c-card>` per merge decision row + ISO timestamp + actor_id + `<.c-badge>` per `link_status` (4-state per CRM identity link enum) + `<.c-badge>` for hard-match (confidence=1.0) vs soft-match (0.0-1.0 numeric) decision indicator | `--color-surface-raised` + `--color-text` + `--color-success` (auto-accept ≥0.80) + `--color-warning` (review 0.40-0.79) + `--color-error` (reject <0.40) + bracketed glyphs | GET /v1/cdp/identity-links/{profile_id} (F-107) |
| Consent state inline indicator | `<.c-badge>` per channel state ENUM (5-state — see Contract 2) + drift indicator `<.c-notice c-notice--warning>` "[warn] Drift detected with legacy outboundConsentRecords" when audit detects divergence | `--color-warning` + `[warn]` glyph | GET /v1/cdp/consent/{profile_id} (F-109); HOURLY drift audit cron updates indicator |
| Trait snapshot timeline | `<.c-card>` per trait_name row + numeric value + `<.c-chip>` per `freshness_mode` (3-value `real_time` / `hourly` / `daily`) + confidence numeric + `source_event_ref[]` cardinality indicator + `<EvidenceCitationChip />` (209 inheritance) per source_event_ref | `--color-surface-raised` + `--color-text` + `--color-success` (fresh) + `--color-warning` (stale per freshness_mode TTL) + bracketed glyphs | GET /v1/cdp/profiles/{id}/traits (F-110); UI re-renders on cron `cdp-trait-recompute-{hourly,daily}.js` |
| Tombstone status indicator | `<.c-notice c-notice--warning>` "[warn] Profile tombstoned — DSR deletion executed YYYY-MM-DD; PII scrubbed; ConsentState retained per legal defensibility of suppression" + `deletion_evidence_ref` link | `--color-warning` + `[warn]` glyph + bracketed glyph (DESIGN.md "Color blindness" rule) | GET /v1/cdp/profiles/{id} returns 404 for tombstoned per D-24 (PII scrub + DSR enumeration prevention); admin-view exposes tombstone metadata only |
| Banned-lexicon validator | (no UI element — runtime check) | (n/a) | runs BEFORE any `external.send` mutation on `cdp_identity_profiles.full_name` + `cdp_identity_profiles.company_name` (when present in dispatch envelope); zero-match required pre-dispatch |

**Wired event types** (per 207 inheritance): `cdp_identity_profile_created` / `cdp_identity_link_created` / `cdp_dsr_tombstone_executed` / `cdp_dsr_cascade_purged` / `cdp_late_event_dropped_after_tombstone` (Pitfall 5 guard fired) / `cdp_unauthorized_cross_tenant_access`.

**Server/client boundary** (D-21 + D-32 inheritance): unified profile viewer is a default server component reading via `requireHostedSupabaseAuth(request)` from `onboarding/backend/runtime-context.cjs:491` (NOT `requireSupabaseAuth`); `<PIIRedactedField />` is a `'use client'` component for `onCopy` interception.

---

## Future-Surface UI Binding Contract 2 — Consent State

For future P221+ admin/tenant surfaces consuming Layer 2 substrate (`cdp_consent_states` + single-writer DB-trigger + hourly drift audit + ConsentState shim + drift resolution queue). The contract is load-bearing — it specifies the channel state ENUM mapping, drift indicator, single-writer enforcement visibility, audit log gating, and approval-modal reuse pattern that future consent-state-browser UI phases MUST honor. Until those phases ship, surfaces render `<PlaceholderBanner variant="future_phase_221_admin_ui">`.

| Surface element | DESIGN.md primitive | Token / class | Mutation wiring |
|-----------------|--------------------|----|-----------------|
| Consent state browser per profile | `<.c-card>` per channel + `<.c-badge>` for channel state ENUM (5-state — `granted`/`opted_in` → `--color-success`; `withdrawn`/`opted_out` → `--color-error`; `pending` → `--color-warning`; `expired` → `--color-warning`; `never`/`unknown` → `--color-text-muted`) + `legal_basis` ENUM 4-chip (`consent` / `contract` / `legitimate_interest` / `unknown`) + jurisdiction text + preference_tags chip-row + quiet_hours display | `--color-surface-raised` + per-state mapping above + bracketed glyphs `[ok]` (granted) / `[err]` (withdrawn) / `[warn]` (pending/expired) / `[…]` (unknown) | GET /v1/cdp/consent/{profile_id} (F-109 read-only); MCP tool `get_consent_state` REDACTS `deletion_evidence_ref` |
| **5-state ENUM badge mapping** | `<.c-badge>` driven by ConsentState channel value | per state mapping above; ALWAYS paired with bracketed glyph (DESIGN.md "Color blindness" rule) | runtime |
| **Drift indicator** | `<.c-notice c-notice--warning>` "[warn] Drift detected: cdp_consent_states differs from legacy outboundConsentRecords for {channel}" + audit log link | `--color-warning` + `[warn]` glyph | runtime indicator; HOURLY drift audit cron `cdp-consent-drift-audit.js` (per D-40 RM1 fix) emits operator task on divergence |
| **Single-writer enforcement visibility** | `<.c-card>` "[info] All ConsentState mutations route through `lib/markos/cdp/consent/state.ts::setConsentState` (single writer per D-39 RM2)" — read-only display; NO direct UI write to legacy `outboundConsentRecords` (DB trigger blocks) | `--color-info` + `[info]` glyph | informational; DB-trigger on `outboundConsentRecords` (migration 106) reads PG GUC `app.consent_writer_source` and rejects writes outside `cdp_setConsentState` with error code `CDP_CONSENT_WRITER_VIOLATION` |
| Single-writer trigger rejection log | `<.c-card>` per audit row "audit row `cdp.consent_writer_violation` raised at YYYY-MM-DD HH:MM:SS by service_role / non-canonical writer" | `--color-error` + `[err]` glyph + `<.c-badge--error>` "[err] Trigger violation" | GET admin audit log query (extends 215 `future_phase_217_pii_audit_log` substrate); single-writer trigger rejections write to `markos_audit_log` P201 hash chain |
| **Audit-log every consent view** | (no UI element — runtime audit gate) | (n/a) | every consent state render writes audit row `event_type == 'consent_view'` to `markos_audit_log` (extending 216 `credential_view` pattern + identity_view extension) |
| **Consent drift resolution modal** | `<.c-modal>` + `<.c-backdrop>` + drift-divergence-side indicator (`cdp_consent_states` value vs `outboundConsentRecords` value side-by-side) + which-side-wins selector (radio chip group) + reason text + audit log preview | `--color-surface-raised` + `--color-primary` (approve/commit) + `--color-warning` (drift detected indicator) | POST /v1/cdp/consent-drifts/{id}/resolve → triggers `buildApprovalPackage(kind='consent_drift_resolution')` → P208 inbox 27th chip; on approval execute → library `setConsentState` sets `SET LOCAL app.consent_writer_source = 'cdp_setConsentState'` inside transaction + commits authoritative value to `cdp_consent_states` per D-12 + D-39 |
| ConsentState provenance display | `<.c-card>` "source: {source} | source_timestamp: {timestamp} | source_event_ref: {ref}" + `<EvidenceMapPanel />` (209 inheritance) per source_event_ref | `--color-surface-raised` + `--color-text` | GET /v1/cdp/consent/{profile_id} returns provenance per D-11 |
| ConsentState retention indicator (post-DSR) | `<.c-notice c-notice--info>` "[info] ConsentState retained per D-24 — legal defensibility of suppression; future re-contact attempts fail-closed against retained tombstone" + `deletion_evidence_ref` link | `--color-info` + `[info]` glyph | runtime indicator on tombstoned profile detail; FK ON DELETE RESTRICT from Plan 02 enforces retention |
| Banned-lexicon validator | (runtime check) | (n/a) | runs BEFORE `external.send` on consent override copy (when consent override emit dispatched per D-13 downstream enforcement); zero-match required pre-dispatch |

**Wired event types**: `cdp_consent_state_set` / `cdp_consent_drift_detected` / `cdp_consent_drift_resolved` / `cdp.consent_writer_violation`.

**Cron-handler-driven UI updates**: `cdp-consent-drift-audit.js` (HOURLY per D-40 RM1 fix) → drift indicator re-renders on detected divergence; emits operator task to drift resolution queue.

---

## Future-Surface UI Binding Contract 3 — Audience Definition

For future P221+ admin/tenant surfaces consuming Layer 4 substrate (`cdp_audience_definitions` + `cdp_audience_snapshots` + `cdp_audience_snapshot_memberships` + JSON Logic DSL + operator whitelist + double-gate activation). The contract is load-bearing — it specifies the JSON Logic DSL editor with operator whitelist enforcement, audience snapshot membership preview, double-gate visualization, and approval-modal reuse pattern that future audience-editor UI phases MUST honor. Until those phases ship, surfaces render `<PlaceholderBanner variant="future_phase_221_audience_editor">`.

| Surface element | DESIGN.md primitive | Token / class | Mutation wiring |
|-----------------|--------------------|----|-----------------|
| Audience definition editor | `<.c-card>` + `<.c-input>` (name + objective) + entity_type 2-chip (`person` / `account`) + freshness_mode 3-chip (`real_time` / `hourly` / `daily`) + destination_families chip-multi-select | `--color-surface-raised` + `--color-primary` | POST /v1/cdp/audiences (F-111); CHECK constraints in migration 104 |
| **JSON Logic DSL editor with live preview** | `<.c-field>` + `<.c-input as="textarea">` (logic_json source) + `<.c-card>` live preview against sample profile context `{ traits, consent, lifecycle_state, mode, jurisdiction }` returning boolean + intermediate evaluation steps | `--color-surface-raised` + `--color-text` + `--color-success` (preview returns true) + `--color-error` (preview returns false) | client-side evaluation via `json-logic-js@2.0.5` (Wave 0 install per Plan 01); server-side `lib/markos/cdp/audiences/dsl.ts::evaluateAudienceMembership` evaluates server-side at snapshot compute |
| **DSL operator whitelist validator (inline)** | `<.c-notice c-notice--error>` "[err] Operator `{op}` rejected — not in whitelist" rendering inline as user types | `--color-error` + `[err]` glyph + bracketed glyph | client-side validation; server-side `lib/markos/cdp/audiences/dsl-whitelist.ts` enforces RL1 — operators RESTRICTED to `{ if, ==, !=, <, <=, >, >=, in, and, or, not, +, -, *, /, var, missing, missing_some }` BEFORE evaluation; non-allowed operator → reject with `CDP_DSL_OPERATOR_REJECTED` + audit row `cdp_audience_dsl_rejected` (tenant_id + actor_id + operator name) |
| **DSL operator whitelist viewer** | `<.c-card>` per allowed operator + descriptive copy + example expression + `<.c-chip>` per operator | `--color-surface-raised` + `--color-text` per chip | read-only viewer at admin path; rejection log queryable |
| Audience snapshot browser | `<.c-card>` per snapshot row + `snapshot_id` + `audience_id` link + `computed_at` timestamp + `membership_count` numeric + `suppression_count_at_snapshot` numeric + `actor_id` + `evidence_ref` link + `<EvidenceCitationChip />` (209 inheritance) | `--color-surface-raised` + `--color-text` + `<.c-badge>` for snapshot age (fresh/stale per freshness_mode) | GET /v1/cdp/audiences/{audience_id}/snapshots (F-112 read-only); CURSOR pagination |
| **Audience membership preview** | `<.c-card>` paginated list (CURSOR) + per-row: `<.c-chip-protocol>` for canonical_identity_id (Protocol Mint reserved) + 4-pii_classification chip per profile + `<PIIRedactedField />` per PII column + `<.c-badge>` per consent state with drift indicator + `<.c-badge>` for jurisdiction + `<.c-badge>` for quiet_hours-active indicator | `--color-surface-raised` + `--color-text` + bracketed glyphs | GET /v1/cdp/audiences/{audience_id}/snapshots/{snapshot_id}/members; immutable per D-17/D-18 (UPDATE/DELETE disallowed via RLS default-deny) |
| **Double-gate consent re-validation preview** | `<.c-card>` "[info] Snapshot membership: {membership_count}; Current dispatch eligibility: {eligible_count_now} (re-validated against current ConsentState); Suppressed since snapshot: {delta}" + per-row indicator showing snapshot-eligible vs current-eligible delta | `--color-surface-raised` + `--color-text` + `--color-warning` for suppressed-since-snapshot | client+server runtime; invokes `validateBatch(profileIds[], channel)` from `lib/markos/cdp/audiences/activation.ts` exported for P223 dispatch pagination per Pitfall 4 |
| **Audience activation approval modal** | `<.c-modal>` + `<.c-backdrop>` + double-gate consent re-validation preview (above) + dispatch eligibility indicator (per current ConsentState — NOT snapshot ConsentState) + jurisdiction filter + quiet_hours filter + audit log + `<.c-card c-card--feature>` (DESIGN.md D-13 reserved use) for primary activation summary | `--color-surface-raised` + `--color-primary` (approve) + `--color-warning` (eligibility delta indicator) | POST /v1/cdp/audiences/{audience_id}/snapshots/{snapshot_id}/activate → triggers `buildApprovalPackage(kind='audience_activation_approval')` → P208 inbox 28th chip; mutation-class `external.send` (downstream — D-13 enforcement); on approval execute → invokes `validateBatch` for final consent gate + dispatches to channel via P223+ adapters |
| Banned-lexicon validator | (runtime check) | (n/a) | runs BEFORE `external.send` on audience activation copy (audience name + objective when included in dispatch envelope); zero-match required pre-dispatch |

**Wired event types**: `cdp_audience_definition_created` / `cdp_audience_dsl_rejected` (whitelist gate fired) / `cdp_audience_snapshot_computed` / `cdp_audience_activation_requested` / `cdp_audience_activation_approved` / `cdp_audience_activation_rejected`.

**Cron-handler-driven UI updates**: snapshot-compute runs (per `freshness_mode` cadence — real_time inline / hourly / daily) → snapshot browser re-renders with new snapshot rows.

---

## Future-Surface UI Binding Contract 4 — DSR + Audit

For future P221+ admin/tenant surfaces consuming Layer 6 substrate (DSR tombstone + cascade purge + DSR export contract + 8-table RLS denial suite + dual-write reconciliation + Pitfall 5 late-event guard). The contract is load-bearing — it specifies the DSR request lifecycle states, cascade purge timeline, DSR export envelope rendering with PII gating, dual-approval modal, and 8-table RLS denial visibility that future DSR-console UI phases MUST honor. Until those phases ship, surfaces render `<PlaceholderBanner variant="future_phase_221_dsr_console">`.

| Surface element | DESIGN.md primitive | Token / class | Mutation wiring |
|-----------------|--------------------|----|-----------------|
| **DSR request lifecycle state queue** | `<.c-card>` per request + 4-state badge timeline (`requested` → `tombstoned` → `exported` → `purged`; mapped: requested → `--color-warning`; tombstoned → `--color-text-muted`; exported → `--color-info`; purged → `--color-success`) + SLA timer per CCPA/GDPR jurisdiction lookup + `<HealthScoreBadge />` (216 inheritance) when DSR priority gated by health_score | `--color-surface-raised` + per-state mapping above + bracketed glyphs `[warn]` / `[…]` / `[info]` / `[ok]` | GET /v1/cdp/dsr (deferred — read-only profile API per D-22 + D-23 future write API for DSR submission lands with consuming engine) |
| Tombstone status indicator | `<.c-notice c-notice--warning>` "[warn] Profile {profile_id} tombstoned at {timestamp}; PII columns scrubbed (primary_email, primary_phone, company_name → NULL); profile_id preserved for FK integrity; deletion_evidence_ref: {ref}" | `--color-warning` + `[warn]` glyph | runtime; `tombstoneIdentityProfile(tenantId, profileId, evidenceRef)` writes per D-24 |
| **Cascade purge timeline** | `<.c-card>` horizontal timeline per cascade step + `<.c-chip>` per substrate table (`cdp_events` partition drop / `cdp_trait_snapshots` delete / `cdp_audience_snapshot_memberships` delete) + ConsentState retention indicator with `<.c-notice c-notice--info>` "[info] ConsentState retained per legal defensibility of suppression — D-24" | `--color-surface-raised` + `--color-success` (purged) + `--color-info` (retained) + bracketed glyphs | runtime; cascade purge executes via `lib/markos/cdp/deletion/cascade.ts` per D-24; FK ON DELETE RESTRICT from Plan 02 enforces ConsentState retention |
| **Mutation-class data.export indicator with dual_approval** | `<.c-badge--error>` "[err] data.export — DSR export contains highly_sensitive PII; dual_approval required per SOC2 P206 Type I" + `<.c-card>` dual-approval indicator | `--color-error` + `[err]` glyph + `--color-warning` (dual-approval indicator) | runtime indicator; mutation-class `data.export`; `default_approval_mode == dual_approval` per SOC2 P206 (mirrors 215 high-amount payout dual-approval pattern + 219 high-discount dual-approval pattern) |
| **DSR export approval modal** | `<.c-modal>` + `<.c-backdrop>` + 6-payload-class envelope cardinality preview (per D-25): IdentityProfile cardinality + IdentityLink[] cardinality + cdp_events[] cardinality + TraitSnapshot[] cardinality + ConsentState[] cardinality + AudienceSnapshotMembership[] cardinality + `<PIIRedactedField />` per PII row preview + 8-table RLS denial assertion preview + admin-role gate indicator + audit log + 213-04 public-proof boundary indicator (PRIVATE forever) + `<.c-badge--error>` "[err] Highly sensitive PII export"; **REUSES 215 billing-correction modal recipe verbatim** | `--color-surface-raised` + `--color-primary` (approve — dual-approval) + `--color-error` (PII gate indicator) | POST /v1/cdp/dsr/{request_id}/export → triggers `buildApprovalPackage(kind='dsr_export_approval')` → P208 inbox 29th chip; mutation-class `data.export`; `default_approval_mode == dual_approval`; on approval execute → `lib/markos/cdp/deletion/dsr-export.ts` emits envelope per D-25 + writes audit row `cdp_dsr_export_dispatched` |
| **8-table RLS denial visible audit** | `<.c-card>` per table + RLS policy name + cross-tenant denial test result (`<.c-badge--success>` "[ok] denied" or `<.c-badge--error>` "[err] policy missing") + audit row `cdp.unauthorized_cross_tenant_access` log | `--color-surface-raised` + per-result mapping + bracketed glyphs | GET /v1/cdp/rls-audit (admin); 8 tables: `cdp_identity_profiles` + `cdp_identity_links` + `cdp_events` + `cdp_consent_states` + `cdp_trait_snapshots` + `cdp_audience_definitions` + `cdp_audience_snapshots` + `cdp_audience_snapshot_memberships`; full RLS suite runs in `test/tenant-auth/cdp-rls-complete.test.js` (Plan 06) |
| **Pitfall 5 late-event-after-tombstone indicator** | `<.c-card>` per dropped late-event + `<.c-badge--info>` "[info] Late event dropped after tombstone — Pitfall 5 guard" + `event_id` + `occurred_at` + `tombstoned_at` + reason | `--color-info` + `[info]` glyph | runtime indicator; dual-write writes `profile_id=NULL` when target is tombstoned per D-24; emits audit row `cdp_late_event_dropped_after_tombstone` |
| **Dual-write reconciliation timeline** | `<.c-card>` per reconciliation run + timestamp + tenant_id + crm_activity row count + cdp_events row count + divergence numeric + `<.c-badge--warning>` "[warn] Divergence > 0.1%" when threshold breached + operator task link | `--color-surface-raised` + `--color-success` (within threshold) + `--color-warning` (divergence > 0.1%) + bracketed glyphs | DAILY cron `cdp-dual-write-reconciliation.js` at `0 7 * * *` per Plan 06; emits operator task on divergence (Pitfall 1 operationalized — deeper than Plan 02 hourly drift audit) |
| **Plan 06 checkpoint:human-action indicator** | `<.c-card c-card--feature>` (DESIGN.md D-13 reserved use) + 4-step ordered list (per Plan 06 Task 4 §how-to-verify checklist) + operator-validation gate "[…] Awaiting operator first-batch validation" | `--color-primary` (mint reserved for hero card) + `--color-text` + bracketed glyph | operator-driven; `autonomous: false` (RL3) per Plan 06; first drift batch reconciliation requires operator review checkpoint BEFORE daily cron runs unattended |
| Banned-lexicon validator | (runtime check) | (n/a) | runs BEFORE `data.export` on DSR export envelope text fields (when text fields rendered in envelope per D-25); zero-match required pre-dispatch |

**Wired event types**: `cdp_dsr_request_received` / `cdp_dsr_tombstone_executed` / `cdp_dsr_cascade_purged` / `cdp_dsr_export_requested` / `cdp_dsr_export_approved` / `cdp_dsr_export_dispatched` / `cdp_late_event_dropped_after_tombstone` / `cdp_dual_write_reconciliation_completed` / `cdp_dual_write_drift_detected`.

**Cron-handler-driven UI updates**: `cdp-dual-write-reconciliation.js` (DAILY 07:00 UTC) → reconciliation timeline re-renders with new rows; divergence > 0.1% emits operator task.

---

## Future-Surface UI Binding Contract 5 — Identity Merge

For future P221+ admin/tenant surfaces consuming Layer 1 substrate (hard/soft merge decision logic + identity link governance + reviewer_actor_id audit + `buildApprovalPackage` extension per D-33). The contract is load-bearing — it specifies the hard/soft merge decision rendering, scoring breakdown visualization, identity score chip-row, and approval-modal reuse pattern that future identity-merge-approval UI phases MUST honor. Until those phases ship, surfaces render `<PlaceholderBanner variant="future_phase_221_admin_ui">`.

| Surface element | DESIGN.md primitive | Token / class | Mutation wiring |
|-----------------|--------------------|----|-----------------|
| **Hard/soft merge decision badge** | `<.c-badge>` per merge decision row — `<.c-badge--success>` "[ok] hard-match (confidence=1.0)" for hard signals (verified email + authenticated user_id + billing_customer_id + subscription_id + workspace_id) + `<.c-badge--info>` "[info] soft-match ({score})" for soft signals (numeric 0.0-1.0) | per-decision: hard → `--color-success`; soft auto-accept (≥0.80) → `--color-success`; soft review (0.40-0.79) → `--color-warning`; soft reject (<0.40) → `--color-error`; ALWAYS paired with bracketed glyph | runtime; `lib/markos/cdp/identity/merge.ts::resolveIdentityMerge` returns decision; thresholds per P101 D-04 carry-forward |
| **Identity score breakdown chip-row** | `<.c-chip>` per scoring dimension (5-chip from `lib/markos/crm/identity.ts:18-22` carry-forward — `email_exact_match=0.65` + `domain_match=0.15` + `device_match=0.10` + `session_overlap=0.10` + `form_submitted=0.15`) with weight + applied-or-not indicator | `--color-surface-raised` + `--color-text` per chip; `--color-success` for applied weights; `--color-text-muted` for non-applied | runtime; `lib/markos/cdp/identity/link.ts` reuses `scoreIdentityCandidate` from `lib/markos/crm/identity.ts:18-22` as soft-match floor; hard-match layer stacks on top |
| **Hard-match signal chip-row** | `<.c-chip>` per hard-match signal (5-chip — `verified_email` + `authenticated_user_id` + `billing_customer_id` + `subscription_id` + `workspace_id`) with applied-or-not indicator | `--color-surface-raised` + `--color-success` (applied) + `--color-text-muted` (not applied) + bracketed glyphs `[ok]` / `[…]` | runtime |
| Identity link browser | `<.c-card>` per IdentityLink row + `link_status` ENUM badge (4-state from CRM identity link enum) + `source_event_ref` link + `reviewer_actor_id` + ISO timestamp + `<.c-badge>` for hard/soft indicator | `--color-surface-raised` + per-state mapping + bracketed glyphs | GET /v1/cdp/identity-links/{profile_id} (F-107 read-only) |
| **Identity merge approval modal** | `<.c-modal>` + `<.c-backdrop>` + `<.c-card c-card--feature>` (DESIGN.md D-13 reserved use) for merge decision summary + identity score breakdown chip-row (above) + hard-match signal chip-row (above) + IdentityLink lineage timeline + `<EvidenceMapPanel />` (209 inheritance) per source_event_ref + `<EvidenceCitationChip />` (209 inheritance) + audit log + reversibility indicator "[info] All merges reversible per D-05 — IdentityLink rows preserved with link_status + source_event_ref + reviewer_actor_id"; **REUSES 215 billing-correction modal recipe verbatim** with merge-specific evidence panel substitution | `--color-surface-raised` + `--color-primary` (approve) + `--color-error` (reject) + `--color-warning` (review-pending soft-match in 0.40-0.79 band) | POST /v1/cdp/identity-merges/{merge_id}/decide → routes through `lib/markos/crm/merge.cjs::buildApprovalPackage` per D-33 (canonical helper — `createApprovalPackage` is FORBIDDEN per D-33 + Plan 01 helper-presence test) → P208 inbox (extends existing merge-review chip — NOT a new chip; reuses governance UX from P100); on approval execute → `recordCdpMergeDecision` writes IdentityLink with link_status + source_event_ref + reviewer_actor_id |
| Anonymous-to-known stitching display | `<.c-card>` "[info] Pre-conversion history appears on CRM timeline via stitched-label projection AFTER acceptance, never before" + `<.c-chip>` for stitched-label indicator on timeline rows | `--color-info` + `[info]` glyph + `--color-text-muted` (stitched chip) | runtime; D-07 carries P101 D-05 — non-destructive stitching; review-pending exclusion preserved per `lib/markos/crm/timeline.ts` + `lib/markos/crm/attribution.ts` UNCHANGED behavior |
| Tenant fail-closed indicator | `<.c-notice c-notice--warning>` "[warn] Tenant context required — cross-tenant merge attempts fail closed per D-06" | `--color-warning` + `[warn]` glyph | runtime indicator on merge UI when tenant_id missing or mismatched per D-06 (P101 D-06 carry) |
| Banned-lexicon validator | (runtime check) | (n/a) | runs BEFORE any merge decision dispatch on reviewer_note + decision_rationale text fields; zero-match required pre-dispatch |

**Wired event types**: `cdp_merge_decision_recorded` / `cdp_merge_review_requested` / `cdp_merge_approved` / `cdp_merge_rejected`.

**Server/client boundary** (D-21 + D-32 inheritance): identity merge approval queue is a default server component reading via `requireHostedSupabaseAuth(request)`; merge approval modal is a `'use client'` component for interactive scoring breakdown + audit log preview.

---

## Sensitive PII Inheritance Carry from 215 + 216

This section documents the load-bearing PII contract that CARRIES VERBATIM into all 5 future-surface UI binding contracts above. All identity profile + consent record + trait snapshot PII fields render `[REDACTED]` placeholder per 5-classification ENUM (`no_pii` / `pseudonymous` / `personal` / `sensitive` / `highly_sensitive`); clipboard `onCopy preventDefault()`; audit-log `event_type == 'identity_view'` (mirror credential_view pattern from 215 Layer 6 + 216 PII binding).

### `pii_classification` ENUM mapping (216 inheritance verbatim)

| `pii_classification` | Display rule | `.c-badge` variant | CDP fields applying |
|----------------------|--------------|---------------------|---------------------|
| `no_pii` | Render value direct in `--color-text` | (none) | profile_id, canonical_identity_id, tenant_id, profile_type, mode, lifecycle_state, created_at, updated_at, tombstoned, segment_id, audience_id, snapshot_id, event_id, consent_state_id, trait_snapshot_id, identity_link_id |
| `pseudonymous` | Render direct + `<.c-badge--info>` "[info] pseudonymous" | `.c-badge--info` | anonymous_id, device_fingerprint (hashed), session_id, hashed_email_for_lookup |
| `personal` | Render `[REDACTED]` placeholder via `.c-code-inline` + `--color-on-surface-subtle`; never display content | `.c-badge--info` "[info] personal" | primary_email, primary_phone, full_name, company_name, jurisdiction, preference_tags |
| `sensitive` | Render `[REDACTED]` placeholder; never display content; access requires admin role + audit log | `.c-badge--warning` "[warn] sensitive" | ip_address, user_agent, raw_device_fingerprint (pre-hash), source (when contains PII), source_event_ref (when contains PII), trait value (when trait_name carries PII — e.g., `last_geo_city`) |
| `highly_sensitive` | Render `[REDACTED]` placeholder; never display content; access requires admin role + dual-approval gate + audit log | `.c-badge--error` "[err] highly_sensitive" | DSR export full envelope payload (IdentityProfile + IdentityLink[] + cdp_events[] + TraitSnapshot[] + ConsentState[] + AudienceSnapshotMembership[] composite); deletion_evidence_ref content; quiet_hours (when contains location PII) |

### `<PIIRedactedField />` component contract (216 inheritance — load-bearing primitive)

```
<PIIRedactedField field_name={...} pii_classification={...} value={...} />
```

- Renders per `pii_classification` ENUM mapping above
- `onCopy` event handler MUST `preventDefault()` for any DOM node
- Writes audit row `event_type == 'identity_view'` to `markos_audit_log` (P201 hash chain) on every render of `personal` / `sensitive` / `highly_sensitive` classifications
- Mirror of 216 `credential_view` pattern (which mirrored 215 Layer 6 sensitive credential UI binding)

### Audit log event_type extension

The 216 + 215 inheritance chain establishes:
- `credential_view` (215 Layer 6 — STRIPE/MP/SIIGO/DIAN/QB credential PII)
- `identity_view` (216 → 221 extension — CDP IdentityProfile + ConsentState + TraitSnapshot PII)
- `consent_view` (221 NEW — every consent state render writes audit row per Future-Surface UI Binding Contract 2)

All three event_types write to the unified `markos_audit_log` per-tenant hash chain from P201 per D-29.

### Sensitive credential UI binding Layer 6 (215 inheritance)

Layer 6 from 215 (`[REDACTED]` for STRIPE/MP/SIIGO/DIAN/QB PII fields) does NOT extend to credential fields in 221 (no payment processor PII in CDP scope). However, the PATTERN extends to PII data per 216 inheritance — `<PIIRedactedField />` is the load-bearing primitive for all CDP PII rendering with the 5-classification ENUM mapping above.

---

## Banned-Lexicon Enforcement Summary

Per CLAUDE.md visual canon + 213-UI-SPEC inheritance + 220-UI-SPEC pattern (which expanded the validator-count by 7 mutation classes), Phase 221 **expands the validator-count by 3 NEW mutation classes**:

| Mutation Class | Field(s) Validated | Where Validator Runs | Pre-Dispatch Gate |
|----------------|--------------------|----------------------|--------------------|
| Consent override copy emit (per D-13 — every downstream send/dispatch engine MUST consume ConsentState; never a channel-local flag) | Consent override message body when emitted in dispatch envelope | Future P223+ messaging engine adapter; runtime check before `external.send` | Zero-match required pre-dispatch |
| Audience activation copy (audience_activation_approval modal — 28th P208 chip) | `cdp_audience_definitions.name` + `cdp_audience_definitions.objective` (when included in dispatch envelope) | Future audience-editor surface; runtime check before `external.send` | Zero-match required pre-dispatch |
| DSR export envelope text rendering (dsr_export_approval modal — 29th P208 chip) | DSR export envelope text fields (when text fields rendered in 6-payload-class envelope per D-25) | Future DSR-console surface; runtime check before `data.export` | Zero-match required pre-dispatch |

**Banned lexicon (DESIGN.md v1.1.0 + CLAUDE.md visual canon)**: `synergy`, `leverage`, `empower`, `unlock`, `transform`, `revolutionize`, `supercharge`, `holistic`, `seamless`, `cutting-edge`, `innovative`, `game-changer`, `next-generation`, `world-class`, `best-in-class`, `reimagine`, `disrupt`, `just` (as softener). No exclamation points in product surface copy.

Validator placement is operational responsibility of the future P221+ admin/tenant frontend phase that ships each mutation class. The 213.4 carry-forward + 220 pattern require zero-match BEFORE any `data.export` or `external.send` mutation dispatches the content.

---

## Pricing Engine Discipline

Phase 221 ships ZERO pricing/packaging/usage/billing surfaces. The CDP substrate (identity + consent + events + traits + audiences + DSR) does not directly consume Pricing Engine context. Future P221+ admin surfaces that compose audience definitions referencing pricing-context-gated traits (e.g., `traits.pricing_tier` from P205 PricingRecommendation) inherit the standard sentinel discipline:

Per CLAUDE.md placeholder rule: unresolved pricing/packaging/usage/billing copy uses `{{MARKOS_PRICING_ENGINE_PENDING}}` until an approved `PricingRecommendation` exists. P221 has no direct pricing-copy fields; downstream consumers (P222 CRM Engine + P223 Messaging Engine + P226 Sales Enablement) inherit the sentinel discipline when CDP traits are referenced in pricing-sensitive copy.

EVD-06 (pricing/competitor traits) is **explicitly OUT OF SCOPE** for P221 per Plan 06 must_haves §EVD-06 disclaimer; deferred to later CDP enrichment phase.

---

## Architecture-Lock + Doctrine Carry-Forward (D-32 + 213.4 + 217 D-21)

This phase's no-UI declaration carries forward the following architecture-lock + doctrine constraints VERBATIM into all future P221+ admin/tenant frontend phases:

### D-32 Architecture-Lock (RH1 + RM3 review-driven; carries verbatim from CDP-CONTEXT.md)

| Constraint | Value | Forbidden alternative |
|------------|-------|------------------------|
| Routes | LEGACY `api/*.js` | App Router `app/api/.../route.ts` (FORBIDDEN; deferred per D-32) |
| Auth | `requireHostedSupabaseAuth` from `onboarding/backend/runtime-context.cjs:491` | `requireSupabaseAuth` (does not exist — FORBIDDEN) |
| Test runner | `npm test` (Node `--test`) with `node:test` + `node:assert/strict` imports | `vitest`, `playwright`, `vitest.config.ts`, `playwright.config.ts`, `.test.ts` (all FORBIDDEN per D-36 Path A) |
| OpenAPI | `contracts/openapi.json` | `public/openapi.json` (FORBIDDEN per D-37) |
| MCP registry | `lib/markos/mcp/tools/index.cjs` (CommonJS) | `lib/markos/mcp/tools/index.ts` (FORBIDDEN per D-38) |
| Cron | `api/cron/` (singular) with `*.js` filenames + `x-markos-cron-secret` header + `MARKOS_CRON_SECRET` env per `api/cron/webhooks-dlq-purge.js` pattern | `api/crons/` (plural) or `app/api/cron/.../route.ts` (FORBIDDEN) |
| Helper canon | `buildApprovalPackage` (NOT `createApprovalPackage`); `resolvePlugin` (NOT `lookupPlugin`) | `createApprovalPackage`, `lookupPlugin` (FORBIDDEN per D-33 + Plan 01 helper-presence test) |

### 213.4-VALIDATION.md §Carry-forward (213.4-VALIDATION.md inherited carry)

| Decision | Carry-forward rule |
|----------|--------------------|
| D-08 token-only | All CSS values in future P221+ surfaces MUST reference DESIGN.md tokens; no hex/rgb literals in `*.module.css` or inline styles |
| D-09 mint-as-text | Protocol Mint `#00D9A3` rendered as text only when reserved CTA/focus context applies; never as fill on surfaces > button-size |
| D-09b `.c-notice` mandatory | All inline status communication MUST use `<.c-notice>` primitive variants (`--info` / `--warning` / `--error` / `--success`); inline `<p>` styled-text status FORBIDDEN |
| D-13 `.c-card--feature` reserved | `<.c-card c-card--feature>` reserved for primary hero card per composition; max 1 per page; used in 221 future surfaces for primary activation summary (audience activation modal), primary merge decision summary (identity merge modal), primary first-batch validation gate (Plan 06 checkpoint:human-action) |
| D-14 no `.c-table` | Tabular data MUST render via `<.c-card>` row composition; `<.c-table>` primitive NOT in DESIGN.md v1.1.0 |
| D-15 selective extraction | Component extraction occurs ONLY when 3+ surfaces consume the same primitive (load-bearing rule); 221 future surfaces use 216 `<PIIRedactedField />` + 209 `<EvidenceMapPanel />` + 209 `<EvidenceCitationChip />` + 215 billing-correction modal recipe + Protocol Mint chip — all already extracted upstream |

### 217 D-21 server/client boundary doctrine (217 inheritance verbatim)

Every future 221 admin / tenant surface MUST be a default server component reading via `requireHostedSupabaseAuth(request)` + tenant-scoped supabase client. Client components (`'use client'`) opt in ONLY for interactive primitives:
- Unified profile viewer with timeline expansion (server) + `<PIIRedactedField />` `onCopy` interceptor (client)
- Identity merge approval modal with hard/soft scoring breakdown (client)
- Identity link browser with score breakdown (server)
- Consent state browser with drift indicator (server)
- Consent drift resolution modal with which-side-wins selector (client)
- CDP event browser with monthly partition selector (client filter; server data fetch)
- Trait snapshot viewer with recompute-trigger button (client)
- Audience definition editor with JSON Logic DSL preview + whitelist validator (client)
- DSL operator whitelist viewer with rejection log (server)
- Audience snapshot browser + membership preview (server with CURSOR pagination)
- Audience activation approval modal with consent re-validation preview (client)
- DSR request queue with tombstone status (server)
- DSR export approval modal with PII cardinality preview (client; modal interaction)
- Dual-write reconciliation timeline with divergence indicators (server)
- 8-table RLS denial test viewer (server)

---

## End-of-v4.1.0 / Start-of-v4.2.0 Milestone State

Phase 221 is the **opening phase of the v4.2.0 Commercial Engines lane** (CDP-01..05 → P221 / CRM-01..05 → P222 / EML-01..05 + MSG-01..05 → P223 / CNV-01..05 + LCH-01..05 → P224 / ANL-01..05 → P225 / SEN-01..05 → P226 / ECO-01..05 → P227 / COM-01..06 → P228). The v4.1.0 SaaS Suite Activation milestone CLOSED at end of P220 per 220-UI-SPEC §End-of-Milestone State. v4.2.0 OPENS with P221 substrate.

### Approval Inbox handoff_kind chip count growth (post-220 → post-221)

| Phase | Chips Added | Cumulative Total | Notable Literals |
|-------|------------|------------------|-----------------|
| End of v4.1.0 (post-220) | (4 P207 + P214 `billing_charge_approval` + P215 `billing_correction_approval` + P216 `support_response_approval` + P216 `save_offer_approval` + 218's 4 chips + 219's 8 chips + 220's 7 chips ending in `partner_payout_export_approval` 26th) | **26** | end-of-v4.1.0 closeout |
| **221 (CDP Identity, Audience, Consent Substrate)** | **3** | **29** | `consent_drift_resolution` (27th) + `audience_activation_approval` (28th) + `dsr_export_approval` (29th) |

**Start-of-v4.2.0 P208 Approval Inbox filter chip set** = **29 handoff_kind literals** (rendering of new chips deferred to future P208 admin extension phases).

### Migration slot map (v4.2.0 commercial-engines lane opening)

| Slot range | Phase | Tables added |
|------------|-------|--------------|
| 81-89 | 201 (markos_orgs base) + early phases | tenant + auth + governance + audit core |
| 90-95 + 97 (skip 96) | 220 (end of v4.1.0) | referral + community + events + pr + partnerships + growth_agent_readiness |
| 96 | 207-related | neuro_literacy_metadata (existing) |
| 97-99 + 100 | (217 / 218 + earlier coordination) | saas_nav_visibility + saas_growth_profiles + module_mode_eligibility seed + earlier phases |
| 100 | (CRM hardening high-water at plan time) | `100_crm_schema_identity_graph_hardening.sql` |
| **101-106** | **221 (this phase — CDP substrate)** | **101_cdp_identity_core (cdp_identity_profiles + cdp_identity_links + canonical_identity_id FK on CRM entities) + 102a_cdp_consent_states + 102b_cdp_events (monthly-partitioned) + 103_cdp_trait_snapshots + 104_cdp_audience_objects (3 tables: definitions + snapshots + memberships) + 105_cdp_deletion_and_audit (audit-log indexes for 8+ CDP event types) + 106_cdp_consent_single_writer_trigger (BEFORE INSERT/UPDATE on outboundConsentRecords)** |

**Note on slot allocation** (per RM4): high-water at plan time = `100_crm_schema_identity_graph_hardening.sql`; CDP starts at 101. Plan 06 ships migration-slot-collision regression test asserting `supabase/migrations/{101,102a,102b,103,104,105,106}_*.sql` exist + no other CDP migrations reuse those slots.

### F-ID slot map (v4.2.0 commercial-engines lane opening)

| F-ID range | Phase | Contracts |
|------------|-------|-----------|
| F-001..F-105 | Pre-v4.2.0 (P201..P217 + P218 F-106..F-112 cosmetic overlap with P221 CDP — verified per orchestrator note that F-106..F-112 in P218 PLG-substrate context is orthogonal to CDP F-106..F-112 because F-IDs are not constraint-bearing per Q-3 cosmetic ordering inheritance from 220) | core + auth + governance + tenant + 215 billing + 216 health + 217 saas-ui + 218 PLG (cosmetic) |
| F-113..F-121 | 219 (B2B Expansion subset) | account_expansion_opportunities + customer_marketing + abm + advocacy + b2b_pricing + b2b_agents |
| F-209..F-227 | 220 (end of v4.1.0) | referral + community + events + pr + partnerships + developer + growth_agent_readiness |
| **F-106..F-112** | **221 (this phase — pre-allocated per D-30 RM4)** | **F-106 cdp-identity-profile-v1 + F-107 cdp-identity-link-v1 + F-108 cdp-event-envelope-v1 + F-109 cdp-consent-state-v1 + F-110 cdp-trait-snapshot-v1 + F-111 cdp-audience-definition-v1 + F-112 cdp-audience-snapshot-v1** |

**F-ID-collision regression test** (RM4) in Plan 06 asserts `contracts/F-1{06,07,08,09,10,11,12}-*.yaml` exist + flow-registry has all 7 entries + no duplicate F-IDs.

### Translation gates state at start of v4.2.0

**Dissolved by P221** (at backend-substrate layer):
- 220 `future_phase_222_attribution_substrate` (CDP events + trait snapshots are the load-bearing attribution-input substrate)
- 220 `future_phase_223_dispatch_substrate` (ConsentState + AudienceSnapshot are the load-bearing dispatch-input substrate)
- 215 `future_phase_217_pii_audit_log` (CDP audit log extends SOC2 admin audit log)
- MarkOS marketing operating loop CDP consumer placeholder (CDP read-through adapter pattern operationalized)

**Opened by P221** (for future P221+ admin/tenant frontend phases + downstream commercial-engines consumers):
- `future_phase_221_admin_ui` (multi-page CDP admin surface composing 15+ pages)
- `future_phase_221_approval_inbox_extensions` (3 new handoff_kind chips render — 27th-29th)
- `future_phase_221_dsr_console` (tenant-facing DSR console)
- `future_phase_221_audience_editor` (tenant marketing audience editor with JSON Logic DSL)
- `future_phase_222_attribution_substrate` (P222 CRM Engine 360 + P225 Analytics Engine consumer)
- `future_phase_223_dispatch_substrate` (P223 Messaging Engine + P224 Conversion + Launch + P226 Sales Enablement consumer)

**Carried forward** (still open from earlier phases):
- All 217+218+219+220 future-phase placeholders for admin/tenant UI rendering remain DEFERRED to future phases
- `future_phase_227_ecosystem_alter` continues open (5 P220-owned tables ALTERable for ecosystem columns)
- `saas_nav_visibility` 12 SG planned-only seed continues to apply; future `saas_cdp` namespace anticipated for 221 admin surfaces

### v4.2.0 readiness signal

P221 opens v4.2.0 — the commercial-engines lane (P221 → P228) can now begin, gated on:
1. P221 substrate ships first (CDP-01..05)
2. P221 `assertUpstreamReady` HARD upstream check confirms REQUIRED_UPSTREAM = [P207, P209, P210, P214, P215, P216, P217] all landed per D-35 (NO bridge stubs; NO soft-skip)
3. P222 `assertUpstreamReady` will require P221 SOR tables present (P221 OWNS `lib/markos/cdp/*` greenfield per D-34; downstream phases CONSUME via read-through adapter per D-20)
4. D-13 enforcement at planner/reviewer time for P222-P226 (every downstream send/dispatch engine MUST consume ConsentState; never a channel-local flag)
5. D-18 double-gate enforcement at dispatch time for P223+ (snapshot membership + current ConsentState re-validation)

### v4.2.0 milestone phase position

- [ ] **P221 CDP Identity, Audience, Consent Substrate (this phase — pending plan-gate + execute-gate + verify-gate; UI-SPEC drafted 2026-05-04)**
- [ ] P222 CRM Engine 360 Workspace
- [ ] P223 Messaging Engine Orchestration (Email + SMS + WhatsApp + Push)
- [ ] P224 Conversion + Launch
- [ ] P225 Analytics Engine
- [ ] P226 Sales Enablement (Revenue + Pricing + Billing engines)
- [ ] P227 Ecosystem Engine
- [ ] P228 Commercial OS Integration Closure
- [ ] v4.2.0 milestone closeout (post-P228; ROADMAP marker)

When P221 ships, the v4.2.0 Commercial Engines lane OPENS. The downstream commercial-engines phases (P222 → P228) consume CDP via the read-through adapter pattern + ConsentState + AudienceSnapshot.

---

## Checker Sign-Off

This is a no-UI-scope phase. The 6 design quality dimensions are evaluated for the no-UI declaration only — pass criteria is "no UI surface declared in any plan" rather than visual/copywriting compliance:

- [ ] Dimension 1 Copywriting: PASS (no copywriting required for own surfaces; banned-lexicon validator placement contracts documented for future surfaces in §Banned-Lexicon Enforcement Summary covering 3 new mutation classes)
- [ ] Dimension 2 Visuals: PASS (no visuals required; future-surface UI binding contracts cite DESIGN.md primitives verbatim across 5 contracts)
- [ ] Dimension 3 Color: PASS (no color decisions for own surfaces; DESIGN.md v1.1.0 60/30/10 inheritance for future surfaces; Protocol Mint < 5% reserved as the single CTA / focus signal; bracketed-glyph dual-signal pairing rule applied to all state badges)
- [ ] Dimension 4 Typography: PASS (no typography decisions for own surfaces; DESIGN.md v1.1.0 JetBrains Mono + Inter inheritance for future surfaces)
- [ ] Dimension 5 Spacing: PASS (no spacing decisions for own surfaces; DESIGN.md v1.1.0 8px-grid inheritance for future surfaces)
- [ ] Dimension 6 Registry Safety: PASS (no shadcn registry; no third-party blocks; vetting gate not applicable; `json-logic-js@2.0.5` Wave 0 install is a runtime DSL evaluator with operator whitelist enforcement, NOT a component registry)
- [ ] Scope verification: PASS (orchestrator grep confirmed NO UI PATHS FOUND IN ANY PLAN; if checker re-runs grep and finds any UI surface in plan files_modified blocks, BLOCK)

**Approval:** pending (gsd-ui-checker upgrades to approved on no-UI-scope verification)

---

## File Inheritance Citations

**206-UI-SPEC.md** (mutation-class doctrine inheritance — `data.export` for DSR full-profile export per D-25 + audit log export; `external.send` for consent override emit per D-13 (downstream enforcement); `default_approval_mode == dual_approval` for `dsr_export_approval` per SOC2 P206 Type I posture)

**207-UI-SPEC.md** (`RunApiEnvelope` + `AgentRunEventType` 30+ new event types across 6 substrate layers + `ApprovalHandoffRecord`; `run_id` linked to all CDP runs and 4 cron handlers)

**208-UI-SPEC.md** (PARENT — Approval Inbox extension; 3 NEW handoff_kind chips → 29 chips post-221 START OF v4.2.0 commercial-engines lane)

**209-UI-SPEC.md** (PARENT — ConsentState provenance + audit log evidence binding; trait provenance source_event_ref[] mandatory; EVD-01 + EVD-03 + EVD-04 + EVD-05 carry; immutable evidence_refs pattern; `<EvidenceMapPanel />` + `<EvidenceCitationChip />` extracted-component recipes)

**213-UI-SPEC.md** (213-04 public-proof boundary STRICT for DSR exports — PRIVATE forever; banned-lexicon zero-match BEFORE `data.export` / `external.send` dispatch; 213.4-VALIDATION.md carry-forward)

**214-UI-SPEC.md** (PARENT — SaaS Suite Activation; `saas_subscriptions` FK; CDP profile read-through adapter pattern per D-21)

**215-UI-SPEC.md** (PARENT — sensitive credential UI binding Layer 6 EXTENDS to PII data per 216 inheritance; 215 billing-correction modal recipe REUSED VERBATIM for `dsr_export_approval` + `audience_activation_approval` + `consent_drift_resolution` per 221 Approval Inbox extension)

**216-UI-SPEC.md** (PARENT — PII+Retention UI binding contract; 5 `pii_classification` ENUM badges (`no_pii` / `pseudonymous` / `personal` / `sensitive` / `highly_sensitive`) carry verbatim and apply to all CDP IdentityProfile + ConsentState + TraitSnapshot PII fields; `<PIIRedactedField />` clipboard `preventDefault()` + audit-log `identity_view` extends 216 `credential_view` pattern; `<HealthScoreBadge />` + `<RiskBandBadge />` + `<RetentionClassChip />` + `<PIIRedactedField />` extracted-component recipes referenced as load-bearing primitives)

**217-UI-SPEC.md** (PARENT — `saas_nav_visibility` consumer eventually under namespace `saas_cdp`; D-21 server/client boundary doctrine carries verbatim; **D-32 architecture-lock carries verbatim** — legacy `api/*.js` + `requireHostedSupabaseAuth` + `npm test` Node `--test` + `contracts/openapi.json` + `lib/markos/mcp/tools/index.cjs` + `buildApprovalPackage`; future_phase_220_referral_console placeholder pattern reused as parallel future_phase_221_admin_ui pattern)

**220-UI-SPEC.md** (PARENT — END-OF-v4.1.0 milestone state; 26 P208 chips at v4.1.0 closeout; 221 opens v4.2.0 commercial-engines lane and EXTENDS to 29 chips with 3 new literals; the 220 6-future-surface UI binding contracts pattern reused as 5 parallel contracts in 221 — Unified Profile / Consent State / Audience Definition / DSR + Audit / Identity Merge)

**213.4-VALIDATION.md** §Carry-forward (D-08 token-only, D-09 mint-as-text, D-09b `.c-notice` mandatory, D-13 `.c-card--feature` reserved, D-14 no `.c-table`, D-15 selective extraction, D-21 server/client boundary)

**DESIGN.md v1.1.0** (canonical visual contract — full token set, primitive set, banned lexicon, motion + a11y rules; default dark Kernel Black `#0A0E14`; Protocol Mint `#00D9A3` reserved as single CTA/focus signal; JetBrains Mono + Inter typography stack; 8px-grid spacing scale; borders over shadows; bracketed-glyph dual-signal pairing rule; `prefers-reduced-motion` collapses durations to 0ms)

---

End of UI-SPEC for Phase 221 (no-UI-scope variant — opening phase of v4.2.0 Commercial Engines lane).
