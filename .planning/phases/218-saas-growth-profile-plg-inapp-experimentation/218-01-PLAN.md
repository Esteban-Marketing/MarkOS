---
phase: 218
plan: "01"
type: execute
wave: 1
depends_on: []
f_ids:
  - F-238
  - F-239
migration_slots:
  - 101
integrates_with:
  - "SAS-09 from P217"
files_modified:
  - .planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md
  - test/fixtures/growth-218/index.js
  - test/fixtures/growth-218/saas-growth-profile.js
  - test/fixtures/growth-218/module-mode-eligibility.js
  - test/fixtures/growth-218/activation-definition.js
  - test/fixtures/growth-218/pql-score.js
  - test/fixtures/growth-218/upgrade-trigger.js
  - test/fixtures/growth-218/in-app-campaign.js
  - test/fixtures/growth-218/marketing-experiment.js
  - test/fixtures/growth-218/experiment-guardrail.js
  - test/fixtures/growth-218/plg-agent-readiness.js
  - lib/markos/growth/preflight/upstream-gate.ts
  - lib/markos/growth/preflight/architecture-lock.ts
  - lib/markos/growth/preflight/errors.ts
  - lib/markos/growth/preflight/index.cjs
  - scripts/preconditions/218-01-check-upstream.cjs
  - test/growth-218/preflight/upstream-gate.test.js
  - test/growth-218/preflight/architecture-lock.test.js
  - test/growth-218/preflight/helper-presence.test.js
  - test/growth-218/preflight/wave-0-baseline.test.js
  - supabase/migrations/101_growth_saas_profile.sql
  - lib/markos/profile/contracts.ts
  - lib/markos/profile/contracts.cjs
  - lib/markos/profile/growth-profiles.ts
  - lib/markos/profile/growth-profiles.cjs
  - lib/markos/profile/mode-eligibility.ts
  - lib/markos/profile/mode-eligibility.cjs
  - lib/markos/profile/index.cjs
  - contracts/F-238-growth-saas-profile-v1.yaml
  - contracts/F-239-growth-mode-eligibility-v1.yaml
  - contracts/flow-registry.json
  - test/growth-218/profile/profile-crud.test.js
  - test/growth-218/profile/mode-eligibility-matrix.test.js
  - test/growth-218/profile/mode-saas-activation-trigger.test.js
  - test/growth-218/profile/mode-change-requires-approval.test.js
  - test/growth-218/profile/sas09-consume.test.js
  - test/growth-218/rls/profile-cross-tenant-denial.test.js
  - test/growth-218/openapi/parity-profile.test.js
  - .planning/phases/218-saas-growth-profile-plg-inapp-experimentation/218-VALIDATION.md
autonomous: true
requirements:
  - SG-01
  - SG-09
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - QA-05
must_haves:
  truths:
    - Wave 0 baseline guard test asserts no migrations 101-106 with growth_* prefix and no F-238..F-246 contracts exist before this plan ships (executed FIRST in Wave 1; locks slot allocation per RESEARCH §F-ID and Migration Slot Allocation)
    - Migration slot coordination document `.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md` is CREATED (P218 is first phase to ship the doc — neither P219 nor P220 has executed); document P218 reservation slots 101-106 + F-238..F-246; flag Q-7 cross-phase conflict (P219 reservation 85-89 in 219 plans collides with foundation 82-89 already on disk; P219 must replan); document P220 reservation 90-95+97 (locked, P220 schema lands at 90-97 BEFORE P218 schema at 101+ in numeric order — P220 module-table triggers must JOIN module_mode_eligibility via deferred runtime helper since P218 schema not yet applied at P220 migration apply time; lib/markos/profile/mode-eligibility.ts at app-layer is the runtime contract)
    - F-ID slot allocation P218 = F-238..F-246 (9 IDs for 8 contracts; cosmetic ordering — F-IDs are not constraint-bearing per P220 Q-3 path-A precedent; deviates from natural numerical ordering but avoids collision with P220 F-209..F-227 + P219 F-228..F-237)
    - assertUpstreamReady preflight CLI `scripts/preconditions/218-01-check-upstream.cjs` hard-fails when REQUIRED_UPSTREAM tables missing (P214 saas_suite_activations, P215 tenant_billing_subscriptions, P216 saas_health_scores, P217 saas_mrr_snapshots); soft-warns when SOFT_UPSTREAM tables missing (pricing_recommendations P205, agent_runs P207, approval_packages P208, evidence_map_records P209, posthog_connector_installs P210, pricing_placeholder_audit P211, artifact_performance_logs P212); exit 1 on hard miss
    - Architecture-lock test scans grep across `lib/markos/{growth,profile,activation,pql,upgrade-triggers,inapp,experiments,growth-agents}` + `api/v1/growth/*` + `api/cron/growth-*` and asserts ZERO occurrences of forbidden patterns — createApprovalPackage, requireSupabaseAuth, requireTenantContext, serviceRoleClient, lookupPlugin, public/openapi.json, app/(growth), app/(plg), api/v1/.../route.ts, app/api/cron/.../route.ts, vitest run, from 'vitest', .test.ts, "stub if missing", "if exists", openapi-generate, lib/markos/(b2b|referral|community|events|pr|partnerships|sales|cdp|conversion|launches|analytics|channels|ecosystem)/
    - Helper-presence test asserts `buildApprovalPackage` exists at `lib/markos/crm/agent-actions.ts:68`, `requireHostedSupabaseAuth` exists at `onboarding/backend/runtime-context.cjs:491`, `resolvePlugin` exists at `lib/markos/plugins/registry.js:102`, `lib/markos/mcp/tools/index.cjs` exists (CommonJS, not .ts); negative assertions — createApprovalPackage / requireSupabaseAuth / lookupPlugin DO NOT exist anywhere in lib/markos/
    - 2 SOR tables ship in migration 101 — `saas_growth_profiles` (RLS-enabled tenant_id-scoped UNIQUE per markos_orgs FK) + `module_mode_eligibility` (system-level service-role-only NO RLS — P219+P220 module-table triggers JOIN this table at app-layer runtime via lib/markos/profile/mode-eligibility.ts)
    - markos_growth_mode ENUM created with exactly 5 values: 'b2b', 'b2c', 'plg_b2b', 'plg_b2c', 'b2b2c' (canon source SaaS Marketing OS Strategy Canon lines 53-59)
    - module_mode_eligibility 60-row seed (12 modules × 5 modes) per RESEARCH §6 Domain 1 module activation matrix — full INSERT block in migration; row-by-row static seed (NOT app-layer hardcode); 39 rows eligible=true; 21 rows eligible=false
    - DB-trigger compliance pack ships in migration 101 — `MODE_REQUIRES_SAAS_ACTIVATION` BEFORE INSERT OR UPDATE on saas_growth_profiles checks tenant has active SaaSSuiteActivation in P214 saas_suite_activations table; `MODULE_REQUIRES_ELIGIBLE_GROWTH_MODE` is documented as the contract pattern for P219+P220 to consume (the actual triggers on P219+P220 module tables are written by those phases via JOIN against module_mode_eligibility and saas_growth_profiles.mode at runtime via lib/markos/profile/mode-eligibility.ts since P220 migrations apply BEFORE P218)
    - 2 contracts ship F-238 (growth-saas-profile-v1) + F-239 (growth-mode-eligibility-v1) with NO collision against F-001..F-237 — verified by `flow-registry.json` regen + parity test
    - `lib/markos/profile/growth-profiles.ts` calls `buildApprovalPackage({ kind: 'saas_growth_mode_change', ... })` for every mode update (SG-12 — mode change is approval-gated; non-mode field updates do NOT require approval)
    - `lib/markos/profile/mode-eligibility.ts` exports `isModuleEligible(tenantId, moduleKey)` — service-side helper consumed by P219+P220 lib code AT RUNTIME (defense-in-depth alongside their migration-time DB-triggers; this is the cross-phase contract per Q-7 sequencing resolution)
    - SAS-09 INTEGRATION test — Plan 01 reads `saas_health_scores` (P216) + `saas_mrr_snapshots` (P217) for module activation eligibility but does NOT modify P217 tables (consume-only contract per RESEARCH §Cross-Phase Coordination Q-3); static-analysis grep over lib/markos/profile/ for `from('saas_mrr_snapshots').insert` returns 0
    - Cross-tenant denial test asserts INSERT under tenant A then SELECT under tenant B returns 0 rows for saas_growth_profiles (proves RLS not bypassable via service-role-leak); module_mode_eligibility correctly readable across tenants (system-level by design)
    - `218-VALIDATION.md` per-task verification map updated with rows for Plan 01 + frontmatter `nyquist_compliant: true` + `wave_0_complete: true` flipped after Plan 01 closeout
  artifacts:
    - path: .planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md
      provides: NEW document (P218 ships first); P218 reservation table (slots 101-106 + F-238..F-246); P220 lock acknowledgment (90-95+97 + F-209..F-227); P219 collision flag (85-89 in plans collides with foundation 82-89; cross-phase replan required); cross-phase notes (Q-1 P218→P219+P220 module gate, Q-3 SAS-09 consume-only, Q-4 P205 SOFT sentinel, Q-7 sequencing); embeds 60-row module activation matrix
    - path: lib/markos/growth/preflight/upstream-gate.ts
      provides: assertUpstreamReady runtime helper; throws UpstreamPhaseNotLandedError on hard-miss (P214/P215/P216/P217); logs warning on soft-miss (P205/P207/P208/P209/P210/P211/P212); consumed by every Plan 02-06 first task
      exports: [assertUpstreamReady, REQUIRED_UPSTREAM, SOFT_UPSTREAM]
    - path: lib/markos/growth/preflight/architecture-lock.ts
      provides: forbidden-pattern detector + helper-presence asserter; consumed by architecture-lock test
      exports: [scanForbiddenPatterns, assertHelpersPresent, FORBIDDEN_PATTERNS, REQUIRED_HELPERS]
    - path: lib/markos/growth/preflight/errors.ts
      provides: UpstreamPhaseNotLandedError + ArchitectureLockViolationError + PricingEnginePendingError typed errors
      exports: [UpstreamPhaseNotLandedError, ArchitectureLockViolationError, PricingEnginePendingError]
    - path: lib/markos/growth/preflight/index.cjs
      provides: CommonJS twin re-exporting upstream-gate + architecture-lock + errors surface for legacy api/*.js consumers
    - path: scripts/preconditions/218-01-check-upstream.cjs
      provides: CLI preflight checker invoked before plan execution; matches `api/cron/webhooks-dlq-purge.js` createClient + supabase pattern; exit 1 on hard miss; exit 0 with stderr warning on soft miss
    - path: supabase/migrations/101_growth_saas_profile.sql
      provides: markos_growth_mode ENUM (5 values) + saas_growth_profiles (UNIQUE(tenant_id) + mode + has_sales_team + has_cs_team + has_developer_audience + plg_enabled GENERATED + b2b_enabled GENERATED + b2c_enabled GENERATED + active_modules text[] + gtm_motion CHECK 5 values + approval cols + version) + module_mode_eligibility (PRIMARY KEY (module_key, mode)) + 60-row seed + 1 DB-trigger fn_mode_requires_saas_activation BEFORE INSERT OR UPDATE on saas_growth_profiles + idx_sgp_tenant_mode index + RLS on saas_growth_profiles only (module_mode_eligibility is system-level)
    - path: lib/markos/profile/contracts.ts
      provides: 3 enum constants (GROWTH_MODES = ['b2b','b2c','plg_b2b','plg_b2c','b2b2c'] as const, GTM_MOTIONS = ['product_led','sales_led','marketing_led','hybrid','community_led'] as const, GROWTH_MODULE_KEYS = 12 module keys per RESEARCH matrix) + 1 approval-kind constant (GROWTH_APPROVAL_KINDS = ['saas_growth_mode_change']) + zod schemas (SaaSGrowthProfileSchema, ModuleModeEligibilitySchema)
      exports: [GROWTH_MODES, GTM_MOTIONS, GROWTH_MODULE_KEYS, GROWTH_APPROVAL_KINDS, SaaSGrowthProfileSchema, ModuleModeEligibilitySchema]
    - path: lib/markos/profile/growth-profiles.ts
      provides: createProfile / getProfile / updateProfile CRUD against saas_growth_profiles; mode-change path calls buildApprovalPackage with kind='saas_growth_mode_change' before UPDATE; honors RLS via runtimeContext.tenant_id passed by caller
      exports: [createProfile, getProfile, updateProfile, requestModeChange]
    - path: lib/markos/profile/mode-eligibility.ts
      provides: isModuleEligible({ tenantId, moduleKey }, supabase) returns boolean — JOIN saas_growth_profiles.mode against module_mode_eligibility row; getEligibleModulesForTenant({ tenantId }) returns string[]; consumed by P219+P220 lib code at runtime (Q-7 cross-phase contract)
      exports: [isModuleEligible, getEligibleModulesForTenant]
    - path: lib/markos/profile/index.cjs
      provides: CommonJS twin re-exporting full surface
    - path: contracts/F-238-growth-saas-profile-v1.yaml
      provides: GET /v1/growth/saas-growth-profiles (read tenant profile), POST /v1/growth/saas-growth-profiles (create), PATCH /v1/growth/saas-growth-profiles (update + mode-change path with approval gate); request/response schemas mirror lib/markos/profile/contracts.ts zod
    - path: contracts/F-239-growth-mode-eligibility-v1.yaml
      provides: GET /v1/growth/saas-growth-profiles/mode-eligibility (query module eligibility for current tenant mode); auth via requireHostedSupabaseAuth
    - path: contracts/flow-registry.json
      provides: 2 entries appended for F-238/F-239 with NO overlap against F-001..F-237 (P220's range F-209..F-227 + P219's range F-228..F-237 already locked)
    - path: test/fixtures/growth-218/index.js
      provides: barrel export of 9 P218 fixture factories used across 218-01..218-06 (saaSGrowthProfileFactory, moduleModeEligibilityFactory, activationDefinitionFactory, pqlScoreFactory, upgradeTriggerFactory, inAppCampaignFactory, marketingExperimentFactory, experimentGuardrailFactory, plgAgentReadinessFactory)
    - path: test/growth-218/preflight/architecture-lock.test.js
      provides: scans 218-*-PLAN.md bodies + lib/markos/{growth,profile,activation,pql,upgrade-triggers,inapp,experiments,growth-agents}/ + api/v1/growth/ + api/cron/growth-*/ for forbidden patterns; asserts grep count == 0 for ALL forbidden patterns
    - path: test/growth-218/preflight/helper-presence.test.js
      provides: imports lib/markos/crm/agent-actions and asserts buildApprovalPackage is exported function; asserts createApprovalPackage IS NOT EXPORTED; same for requireHostedSupabaseAuth (positive) + requireSupabaseAuth (negative); resolvePlugin (positive) + lookupPlugin (negative); index.cjs exists (positive) + index.ts does NOT exist (negative)
    - path: test/growth-218/profile/mode-eligibility-matrix.test.js
      provides: SELECT count(*) FROM module_mode_eligibility = 60 (12 × 5); SELECT count(*) WHERE eligible=true = 39; specific row probes (e.g., abm_engine + b2b = eligible=true; abm_engine + b2c = eligible=false; growth_experiments + all 5 modes = eligible=true)
    - path: test/growth-218/profile/mode-saas-activation-trigger.test.js
      provides: Postgres-backed test using service-role direct INSERT into saas_growth_profiles for tenant with NO active saas_suite_activations row → expect EXCEPTION 'MODE_REQUIRES_SAAS_ACTIVATION'; with active activation row → succeeds
    - path: test/growth-218/profile/sas09-consume.test.js
      provides: integration test — verifies Plan 01 reads from P217 saas_mrr_snapshots + P216 saas_health_scores via mocked supabase client; verifies Plan 01 does NOT execute INSERT/UPDATE/DELETE against P217 or P216 tables (consume-only contract; static-analysis grep over lib/markos/profile/ for `from('saas_mrr_snapshots')` returns ONLY .select() calls, never .insert/.update/.delete)
  key_links:
    - from: lib/markos/profile/growth-profiles.ts
      to: lib/markos/crm/agent-actions.ts
      via: const { buildApprovalPackage } = require('../crm/agent-actions') — verified function exists at line 68; SG-12 wiring on every mode-change UPDATE
      pattern: "require.*'\\.\\./crm/agent-actions'"
    - from: lib/markos/profile/growth-profiles.ts
      to: supabase/migrations/101_growth_saas_profile.sql
      via: app-layer requestModeChange produces approval_id; DB-trigger fn_mode_requires_saas_activation asserts P214 saas_suite_activations row exists when INSERT/UPDATE on saas_growth_profiles — defense-in-depth pair (matches P226 D-83/D-84 model)
      pattern: "approved_at"
    - from: contracts/F-238-growth-saas-profile-v1.yaml
      to: lib/markos/profile/contracts.ts
      via: SaaSGrowthProfileSchema zod parity-tested against F-238 YAML — column names + ENUM values + required-field set must match exactly
      pattern: "SaaSGrowthProfileSchema"
    - from: scripts/preconditions/218-01-check-upstream.cjs
      to: supabase tables saas_suite_activations + tenant_billing_subscriptions + saas_health_scores + saas_mrr_snapshots
      via: createClient(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY).from(table).select('*').limit(0); on /relation .* does not exist/ → console.error + process.exit(1)
      pattern: "REQUIRED_UPSTREAM"
    - from: lib/markos/profile/mode-eligibility.ts
      to: supabase/migrations/101_growth_saas_profile.sql
      via: isModuleEligible JOIN query against module_mode_eligibility table; consumed at runtime by P219+P220 lib code (Q-7 cross-phase contract)
      pattern: "module_mode_eligibility"
    - from: .planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md
      to: supabase/migrations/101_growth_saas_profile.sql
      via: doc reserves slots 101-106 for P218 plans; subsequent migrations reference doc by file path in commit message
      pattern: "MIGRATION-SLOT-COORDINATION"
---

<objective>
Establish the Wave 1 schema foundation for Phase 218: ship the architecture-lock + assertUpstreamReady preflight surface (Plan 01 owns this for ALL 6 plans), the migration slot coordination document CREATE (P218 is first phase to ship doc — neither P219 nor P220 has executed; resolves Q-7 collision pathway), the SaaSGrowthProfile + module_mode_eligibility foundation (2 SOR tables: 1 RLS-tenant-scoped + 1 system-level), the markos_growth_mode ENUM (5 values per canon), the 60-row module_mode_eligibility seed (foundational for P219+P220 module activation gate), the DB-trigger compliance pack (mode-requires-SaaS-activation per RESEARCH §6 Domain 1), 2 F-ID contracts (F-238/F-239), the mode-eligibility runtime helper (consumed by P219+P220 lib code), the cross-tenant RLS denial suite, the SAS-09 consume-only integration test, and the populated 218-VALIDATION.md per-task map.

Purpose: satisfy SG-01 (SaaS tenants have a growth-mode profile driving active modules/metrics/agents/UI/approvals/playbooks), SG-09 (growth modules create tasks/approvals/experiments — Plan 01 establishes the gate that ALL 5 active downstream domains check), QA-01..05 (architecture-lock + helper-presence + RLS isolation + F-ID parity + threat model). This plan unblocks every later 218-NN slice — the architecture-lock surface (lib/markos/growth/preflight/) is consumed by Plans 02-06 first tasks; the fixture barrel (test/fixtures/growth-218/) is consumed across all later test files; the migration slot doc constrains Plans 02-06 + downstream phase slot allocation; the saas_growth_profiles.mode ENUM column + module_mode_eligibility table is the contract that P219 + P220 module-activation triggers JOIN at runtime via lib/markos/profile/mode-eligibility.ts.

Output: 1 NEW coordination doc + 4 preflight files + 1 preflight CLI + 4 preflight test files + 9 fixture files + 1 SOR migration (101) + 6 profile lib files + 2 contract YAMLs + flow-registry update + 5 profile test files + 1 RLS test file + 1 SAS-09 consume test + 1 OpenAPI parity test + populated VALIDATION.md.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/218-saas-growth-profile-plg-inapp-experimentation/218-CONTEXT.md
@.planning/phases/218-saas-growth-profile-plg-inapp-experimentation/218-RESEARCH.md
@.planning/phases/218-saas-growth-profile-plg-inapp-experimentation/218-REVIEWS.md
@.planning/phases/218-saas-growth-profile-plg-inapp-experimentation/218-VALIDATION.md
@.planning/phases/218-saas-growth-profile-plg-inapp-experimentation/DISCUSS.md
@.planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-01-PLAN.md
@.planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-01-PLAN.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@obsidian/brain/SaaS Marketing OS Strategy Canon.md
@obsidian/brain/Pricing Engine Canon.md
@obsidian/brain/SaaS Suite Canon.md
@obsidian/reference/MarkOS v2 Operating Loop Spec.md
@obsidian/work/incoming/17-SAAS-MARKETING-OS-STRATEGY.md
@CLAUDE.md
@lib/markos/crm/agent-actions.ts
@onboarding/backend/runtime-context.cjs
@lib/markos/plugins/registry.js
@lib/markos/mcp/tools/index.cjs
@api/cron/webhooks-dlq-purge.js
@supabase/migrations/96_neuro_literacy_metadata.sql
@supabase/migrations/100_crm_schema_identity_graph_hardening.sql

<interfaces>
<!-- Verified existing symbols from 2026-04-26 codebase enumeration. Plan 01 wires against these directly. -->
<!-- Forbidden symbols (createApprovalPackage, requireSupabaseAuth, etc.) verified absent — see 218-RESEARCH.md §Architecture Lock. -->

From lib/markos/crm/agent-actions.ts:68:
```typescript
function buildApprovalPackage(input: BuildApprovalPackageInput): ApprovalPackage
// returns { approval_id: string, ... }
// module.exports = { buildApprovalPackage, ... } at line 133
```

From onboarding/backend/runtime-context.cjs:491:
```javascript
function requireHostedSupabaseAuth({ req, runtimeContext, operation, requiredProjectSlug, env })
// returns { tenant_id, user_id, project_slug, ... }
// module.exports = { requireHostedSupabaseAuth, ... } at line 1014
```

From lib/markos/plugins/registry.js:102:
```javascript
function resolvePlugin(registry, pluginId): object | null
```

From lib/markos/mcp/tools/index.cjs (CommonJS):
```javascript
'use strict';
// 30+ live tool descriptors — Plan 06 will add growth.cjs to this registry
```

From api/cron/webhooks-dlq-purge.js (cron pattern):
```javascript
// Reference: x-markos-cron-secret header + MARKOS_*_CRON_SECRET env + writeJson + {success, count, duration_ms} response
// P218 cron secret: MARKOS_GROWTH_CRON_SECRET (NEW — RESEARCH §Architecture Lock)
```

P218 module activation matrix (canon source SaaS Marketing OS Strategy Canon lines 53-59) — used to seed module_mode_eligibility:
| Module Key | b2b | b2c | plg_b2b | plg_b2c | b2b2c |
|------------|-----|-----|---------|---------|-------|
| plg_engine | F | F | T | T | T |
| upgrade_triggers | F | F | T | T | T |
| in_app_marketing | F | T | T | T | T |
| growth_experiments | T | T | T | T | T |
| account_expansion | T | F | T | F | T |
| abm_engine | T | F | T | F | T |
| revenue_alignment | T | F | T | F | T |
| viral_referral | F | T | F | T | T |
| community | F | T | T | T | T |
| pr_analyst | T | F | T | F | T |
| partnerships | T | F | T | F | T |
| developer_marketing | F | F | T | F | T |

Total = 12 modules × 5 modes = 60 rows; eligible=true count = 39; eligible=false count = 21.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0.1: Wave 0 baseline guards + migration slot coordination doc CREATE (P218 first phase to ship doc)</name>
  <files>.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md, test/growth-218/preflight/wave-0-baseline.test.js, test/fixtures/growth-218/index.js, test/fixtures/growth-218/saas-growth-profile.js, test/fixtures/growth-218/module-mode-eligibility.js, test/fixtures/growth-218/activation-definition.js, test/fixtures/growth-218/pql-score.js, test/fixtures/growth-218/upgrade-trigger.js, test/fixtures/growth-218/in-app-campaign.js, test/fixtures/growth-218/marketing-experiment.js, test/fixtures/growth-218/experiment-guardrail.js, test/fixtures/growth-218/plg-agent-readiness.js</files>
  <read_first>
    - .planning/phases/218-saas-growth-profile-plg-inapp-experimentation/218-RESEARCH.md §F-ID and Migration Slot Allocation (slot 101-106 + F-238..F-246; collision context Q-7)
    - .planning/phases/218-saas-growth-profile-plg-inapp-experimentation/218-RESEARCH.md §Cross-Phase Coordination Q-1 through Q-7
    - .planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-01-PLAN.md Task 0.1 (P219-01 was written assuming P220 had created the doc; P218-01 must CREATE the doc instead since neither P219 nor P220 have executed)
    - .planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-01-PLAN.md Task 0.1 (mirror this exact pattern; swap b2b/community → growth/profile)
    - supabase/migrations/ (verify highest existing slot is 100; slots 101-106 are unallocated; slots 90-95+97 reserved by P220 plans but not yet on disk; slots 85-89 NOT yet on disk and NOT available — P219 plans incorrectly reserved them)
    - contracts/ (verify highest existing F-ID; F-238..F-246 range unallocated on disk)
    - .planning/phases/226-sales-enablement-deal-execution/226-01-PLAN.md Task 0 (B6 allocation pattern)
  </read_first>
  <action>
    Step 1: CREATE `.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md` (P218 is the first v4.1.0 phase to actually execute). Document structure:
      - Top-of-file context block: explains v4.1.0 milestone slot allocation problem; lists 4 ranges (existing 82-89 + 96 + 100; P220 reserved 90-95+97; P218 reserves 101-106; P219 must replan 85-89→107+)
      - Section "## Reservations":
        | Phase | Plan | Migration slot(s) | F-ID range | Status | Notes |
        | P218 | 01 | 101 | F-238, F-239 | reserved (this plan) | saas_growth_profiles + module_mode_eligibility + 1 DB-trigger |
        | P218 | 02 | 102 | F-240, F-241 | reserved | activation_definitions + activation_milestones + pql_scores + 1 DB-trigger |
        | P218 | 03 | 103 | F-242 | reserved | upgrade_triggers + upgrade_trigger_events + 1 DB-trigger |
        | P218 | 04 | 104 | F-243, F-244 | reserved | in_app_campaigns + in_app_campaign_deliveries + in_app_suppression_rules + 2 DB-triggers |
        | P218 | 05 | 105 | F-245 | reserved | marketing_experiments + experiment_guardrails + experiment_decisions + 1 DB-trigger |
        | P218 | 06 | 106 (additive seed via 101_growth_saas_profile_part2.sql or own slot) | F-246 | reserved | plg_growth_agent_readiness registry + activation gate trigger |
        | P219 | 01-06 | RESERVATION_INVALID — 85-89 collide with foundation 82-89 already on disk | F-228..F-237 | INVALID — REPLAN REQUIRED | P219 plans (already written) reserve slots 85-89 but those are foundation-occupied (audit_log_hash_chain through mcp_cost_window); P219 must replan to slots 107-111 before execution |
        | P220 | 01 | 90 | F-209, F-210 | reserved | referral_programs + viral_loop_metrics |
        | P220 | 02 | 92 | F-211, F-212 | reserved | community_profiles |
        | P220 | 03 | 93 | F-213..F-216 | reserved | marketing_events |
        | P220 | 04 | 94 | F-217..F-219 | reserved | pr/analyst/review |
        | P220 | 05 | 95 | F-220..F-225 | reserved | partnerships/affiliate/devrel |
        | P220 | 06 | 97 | F-226, F-227 | reserved | growth_agent_readiness 28-agent registry + closeout |
      - Section "## Cross-Phase Coordination Resolutions":
        - Q-1 (P218 SaaSGrowthProfile.mode → P219+P220 module activation gate): RESOLVED — P218-01 ships saas_growth_profiles.mode column (markos_growth_mode ENUM) + module_mode_eligibility 60-row seed. P219+P220 module-table BEFORE INSERT triggers JOIN module_mode_eligibility (using saas_growth_profiles.mode for current tenant) at app-layer runtime via lib/markos/profile/mode-eligibility.ts. Raise EXCEPTION 'MODULE_REQUIRES_ELIGIBLE_GROWTH_MODE' on ineligible.
        - Q-2 (LRN-01..05 in P218-05 vs P212): RESOLVED — Plan 05 frontmatter `integrates_with: [LRN-01..05]`. P212 owns substrate; P218 writes after experiment decisions. No double-ownership drift.
        - Q-3 (SAS-09 in P218-01 vs P217): RESOLVED — Plan 01 frontmatter `integrates_with: [SAS-09 from P217]`. P218 reads saas_health_scores + saas_mrr_snapshots; consume-only.
        - Q-4 (P205 Pricing Engine for Plans 03+04): RESOLVED — SOFT dependency with sentinel `{{MARKOS_PRICING_ENGINE_PENDING}}` fallback. Both DB-triggers (`UPGRADE_TRIGGER_PRICING_REQUIRED` and `INAPP_CAMPAIGN_REQUIRES_APPROVAL_AND_PRICING`) accept either pricing_recommendation_id NOT NULL OR pricing_context_sentinel = '{{MARKOS_PRICING_ENGINE_PENDING}}'.
        - Q-5 (PRC-09 ownership): RESOLVED — split Plans 03+04. Both list PRC-09 in `requirements:`.
        - Q-6 (Mode routing matrix dynamic vs static): RESOLVED — dynamic DB table `module_mode_eligibility` 60-row seed; service-role-only NO RLS.
        - Q-7 (Migration slot ordering): ACCEPTED — P218 reserves slots 101-106 (above all existing); P220 90-97 already locked but P220 migrations DO NOT directly reference saas_growth_profiles in migration-time DDL. P220 module-table triggers must be implemented as deferred runtime checks via lib/markos/profile/mode-eligibility.ts at app-layer; module_mode_eligibility table existence is a soft-prereq for P220 trigger creation. Net: numeric apply order is 90-97 first, then 101-106; P220 triggers gracefully degrade if module_mode_eligibility not yet applied (fail-open with warn-log; cron handler reconciles once P218 migrations land). P219 plans currently reserve 85-89 INVALIDLY — must replan to 107-111 (planner decision deferred to P219 cross-phase replan).
      - Section "## P218 Module Activation Matrix" (the 60-row table from RESEARCH §6 Domain 1, embedded in the doc for cross-phase reference)
    Step 2: Create test/growth-218/preflight/wave-0-baseline.test.js using node:test + node:assert/strict:
      ```js
      'use strict';
      const test = require('node:test');
      const assert = require('node:assert/strict');
      const fs = require('node:fs');
      test('wave-0: no migrations 101-106 with growth_* prefix exist before this plan ships', () => {
        const present = fs.readdirSync('supabase/migrations').filter((f) => /^10[1-6]_growth_/.test(f));
        assert.deepStrictEqual(present, []);
      });
      test('wave-0: no contracts F-238..F-246 exist before this plan ships', () => {
        const present = fs.readdirSync('contracts').filter((f) => /^F-2(3[89]|4[0-6])-/.test(f));
        assert.deepStrictEqual(present, []);
      });
      test('wave-0: V4.1.0-MIGRATION-SLOT-COORDINATION.md contains P218 reservation', () => {
        const doc = fs.readFileSync('.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md', 'utf8');
        assert.ok(doc.includes('P218'));
        assert.ok(doc.includes('F-238'));
        assert.ok(doc.includes('F-246'));
        assert.ok(doc.includes('101'));
        assert.ok(doc.includes('module_mode_eligibility'));
      });
      test('wave-0: V4.1.0-MIGRATION-SLOT-COORDINATION.md flags P219 collision', () => {
        const doc = fs.readFileSync('.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md', 'utf8');
        assert.ok(doc.match(/P219.*REPLAN/i) || doc.match(/85-89.*collide/i));
      });
      test('wave-0: highest existing migration is at least 100', () => {
        const migrations = fs.readdirSync('supabase/migrations').filter((f) => /^\d+_/.test(f));
        const slots = migrations.map((f) => parseInt(f.match(/^(\d+)/)[1], 10));
        assert.ok(Math.max(...slots) >= 100, `expected highest slot >= 100, got ${Math.max(...slots)}`);
      });
      ```
    Step 3: Create test/fixtures/growth-218/{9 fixture factories}.js as named-export functions producing object shapes matching upcoming SOR table columns. Each fixture returns an object with sensible defaults; consumers override fields per test. Pure JS, no library deps.
      - saas-growth-profile.js: saaSGrowthProfileFactory({ tenant_id, suite_activation_id, mode, ... }) — defaults: mode='plg_b2b', has_sales_team=false, has_cs_team=true, has_developer_audience=false, gtm_motion='product_led'
      - module-mode-eligibility.js: moduleModeEligibilityFactory({ module_key, mode, eligible }) — defaults: module_key='plg_engine', mode='plg_b2b', eligible=true
      - activation-definition.js: activationDefinitionFactory({ tenant_id, profile_id, saas_model, aha_moment_description, aha_moment_event_name, ... }) — defaults: aha_moment_window_days=14, aha_moment_minimum_count=1
      - pql-score.js: pqlScoreFactory({ tenant_id, definition_id, user_id, score, pql_status, ... }) — defaults: score=50, pql_status='warming'
      - upgrade-trigger.js: upgradeTriggerFactory({ tenant_id, profile_id, name, trigger_type, prompt_format, ... }) — defaults: trigger_type='hard_limit_reached', prompt_format='modal', pricing_context_sentinel='{{MARKOS_PRICING_ENGINE_PENDING}}'
      - in-app-campaign.js: inAppCampaignFactory({ tenant_id, profile_id, name, format, primary_goal, max_shows_per_user, cooldown_days, ... }) — defaults: format='modal', primary_goal='upgrade', max_shows_per_user=3, cooldown_days=14
      - marketing-experiment.js: marketingExperimentFactory({ tenant_id, profile_id, name, hypothesis, experiment_type, surface, owner_id, decision_criteria, learning_handoff, ... }) — defaults: experiment_type='ab_test', priority='medium', traffic_split_pct=50, primary_metric='conversion_rate', significance_threshold=0.05, min_runtime_days=7
      - experiment-guardrail.js: experimentGuardrailFactory({ experiment_id, tenant_id, metric_name, direction, threshold_pct }) — defaults: direction='must_not_decrease', threshold_pct=5.0
      - plg-agent-readiness.js: plgAgentReadinessFactory({ agent_id, agent_name, agent_tier, blocking_phase, ... }) — defaults: agent_tier='PLG', blocking_phase='P218-06'; all 8 readiness booleans default false
      - index.js as barrel: `module.exports = { saaSGrowthProfileFactory, moduleModeEligibilityFactory, activationDefinitionFactory, pqlScoreFactory, upgradeTriggerFactory, inAppCampaignFactory, marketingExperimentFactory, experimentGuardrailFactory, plgAgentReadinessFactory };`
    Step 4: Run wave-0-baseline.test.js — must pass (5 assertions; coordination doc exists with P218 entries + P219 collision flag).
  </action>
  <acceptance_criteria>
    - `.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md` exists with P218 reservation table (rows for plans 01-06 listing slots 101-106 + F-IDs F-238..F-246) AND Cross-Phase Coordination Resolutions section (Q-1 through Q-7 all RESOLVED or flagged) AND module activation matrix (60 rows for cross-phase reference) AND P219 collision flag (85-89 invalid; replan required)
    - `node --test test/growth-218/preflight/wave-0-baseline.test.js` passes (5 assertions green)
    - `test/fixtures/growth-218/` directory contains exactly 9 factory files + index.js barrel (10 files total)
    - `node -e "console.log(Object.keys(require('./test/fixtures/growth-218/index.js')).length)"` outputs `9`
    - `grep -E "F-(2(3[89]|4[0-5]))" contracts/flow-registry.json | wc -l` returns 0 (no contracts written yet — only doc reservation)
    - `grep -E "P218" .planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md | wc -l` returns at least 6 (one row per P218 plan + section header)
    - `grep -iE "P219.*replan|85-89.*collide" .planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md | wc -l` returns at least 1 (collision flag present)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 0.5: Architecture-lock + assertUpstreamReady preflight surface</name>
  <files>lib/markos/growth/preflight/upstream-gate.ts, lib/markos/growth/preflight/architecture-lock.ts, lib/markos/growth/preflight/errors.ts, lib/markos/growth/preflight/index.cjs, scripts/preconditions/218-01-check-upstream.cjs, test/growth-218/preflight/upstream-gate.test.js, test/growth-218/preflight/architecture-lock.test.js, test/growth-218/preflight/helper-presence.test.js</files>
  <behavior>
    - Test 1: scanForbiddenPatterns({ paths }) scans for createApprovalPackage / requireSupabaseAuth / requireTenantContext / serviceRoleClient / lookupPlugin / public/openapi.json / app/(growth) / app/(plg) / api/v1/.../route.ts / app/api/cron/.../route.ts / vitest run / from 'vitest' / .test.ts / "stub if missing" / "if exists" / openapi-generate / lib/markos/(b2b|referral|community|events|pr|partnerships|sales|cdp|conversion|launches|analytics|channels|ecosystem)/; returns array of violations; should return [] when no violations exist
    - Test 2: scanForbiddenPatterns detects createApprovalPackage in a fixture file under test/growth-218/_fixtures/forbidden-fixture.js and returns array of length 1 with file path + line number + pattern matched
    - Test 3: assertHelpersPresent() asserts buildApprovalPackage exported from lib/markos/crm/agent-actions; assert requireHostedSupabaseAuth exported from onboarding/backend/runtime-context.cjs; assert resolvePlugin exported from lib/markos/plugins/registry.js; assert lib/markos/mcp/tools/index.cjs exists (and index.ts does NOT exist)
    - Test 4: assertUpstreamReady({ supabase, requiredTables, softTables }) — when REQUIRED_UPSTREAM table missing → throws UpstreamPhaseNotLandedError with phase identifier; when SOFT_UPSTREAM table missing → returns warning array (no throw)
    - Test 5: scripts/preconditions/218-01-check-upstream.cjs CLI exits 1 + console.error MISSING_UPSTREAM_PHASE on hard miss; exits 0 + console.warn SOFT_MISSING on soft miss only; exits 0 silently on all-green
    - Test 6: helper-presence test asserts createApprovalPackage NOT exported (negative); requireSupabaseAuth NOT exported (negative); lookupPlugin NOT exported (negative); requireTenantContext NOT exported (negative)
  </behavior>
  <read_first>
    - 218-RESEARCH.md §Architecture Lock + §Upstream Dependencies (assertUpstreamReady Gate)
    - .planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-01-PLAN.md Task 0.5 (REPLICATE this exact pattern; only swap "growth/community" → "growth/profile" + adjust forbidden patterns to add lib/markos/b2b/, lib/markos/referral/, lib/markos/community/, lib/markos/events/, lib/markos/pr/, lib/markos/partnerships/)
    - .planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-01-PLAN.md Task 0.5 (REPLICATE; same architecture-lock model)
    - lib/markos/crm/agent-actions.ts:68 + 133 (verify exports)
    - onboarding/backend/runtime-context.cjs:491 + 1014 (verify exports)
    - lib/markos/plugins/registry.js:102
    - lib/markos/mcp/tools/index.cjs (verify CommonJS, .cjs extension)
    - api/cron/webhooks-dlq-purge.js (cron auth pattern + createClient + writeJson)
    - .planning/phases/226-sales-enablement-deal-execution/226-01-PLAN.md Task 0.5 (D-78 architecture-lock pattern)
  </read_first>
  <action>
    Step 1: Write the 6 tests in test/growth-218/preflight/{upstream-gate.test.js, architecture-lock.test.js, helper-presence.test.js} BEFORE implementation (TDD RED). Each uses node:test + node:assert/strict. Helper-presence imports the actual codebase modules and asserts function references using `typeof === 'function'` checks.
    Step 2: Implement lib/markos/growth/preflight/errors.ts:
      ```typescript
      export class UpstreamPhaseNotLandedError extends Error {
        constructor(public phase: string, public missingTable: string) {
          super(`MISSING_UPSTREAM_PHASE: ${phase} (table ${missingTable} does not exist)`);
          this.name = 'UpstreamPhaseNotLandedError';
        }
      }
      export class ArchitectureLockViolationError extends Error {
        constructor(public pattern: string, public file: string, public line: number) {
          super(`ARCHITECTURE_LOCK_VIOLATION: pattern "${pattern}" found at ${file}:${line}`);
          this.name = 'ArchitectureLockViolationError';
        }
      }
      export class PricingEnginePendingError extends Error {
        constructor(public copyType: string) {
          super(`PRICING_ENGINE_PENDING: ${copyType} requires pricing_recommendation_id or {{MARKOS_PRICING_ENGINE_PENDING}} sentinel`);
          this.name = 'PricingEnginePendingError';
        }
      }
      ```
    Step 3: Implement lib/markos/growth/preflight/architecture-lock.ts with FORBIDDEN_PATTERNS array + scanForbiddenPatterns({ paths }) function using Node fs.readdirSync + fs.readFileSync + regex matching. Pattern list per RESEARCH §Forbidden Patterns:
      - createApprovalPackage, requireSupabaseAuth, requireTenantContext, serviceRoleClient, lookupPlugin
      - public/openapi.json, app/\\(growth\\), app/\\(plg\\), route\\.ts, api/v1/.*/route, api/cron/.*/route
      - vitest run, from ['"]vitest['"], \\.test\\.ts, "stub if missing", "if exists"
      - openapi-generate
      - lib/markos/(b2b|referral|community|events|pr|partnerships|sales|cdp|conversion|launches|analytics|channels|ecosystem)/
    Step 4: Implement lib/markos/growth/preflight/upstream-gate.ts with REQUIRED_UPSTREAM (P214/P215/P216/P217) + SOFT_UPSTREAM (P205/P207/P208/P209/P210/P211/P212) — see frontmatter for exact table list.
    Step 5: Implement lib/markos/growth/preflight/index.cjs as CommonJS twin re-exporting upstream-gate + architecture-lock surface + errors.
    Step 6: Implement scripts/preconditions/218-01-check-upstream.cjs matching api/cron/webhooks-dlq-purge.js createClient pattern; calls assertUpstreamReady; on UpstreamPhaseNotLandedError → console.error + process.exit(1); on warnings → forEach console.warn + exit 0; on success → console.log "P218-01 preflight green" + exit 0.
    Step 7: Run all 3 preflight test files — must pass (TDD GREEN).
  </action>
  <acceptance_criteria>
    - `node --test test/growth-218/preflight/architecture-lock.test.js` passes (2 tests green; positive scan returns 0 violations + negative fixture detected)
    - `node --test test/growth-218/preflight/helper-presence.test.js` passes (4 positive + 4 negative assertions green)
    - `node --test test/growth-218/preflight/upstream-gate.test.js` passes (mock-supabase tests green for hard-fail + soft-warn paths)
    - `node scripts/preconditions/218-01-check-upstream.cjs` exits 0 on green env or 1 with MISSING_UPSTREAM_PHASE on missing required table (saas_suite_activations / tenant_billing_subscriptions / saas_health_scores / saas_mrr_snapshots)
    - `grep -rn "createApprovalPackage\|requireSupabaseAuth\|lookupPlugin\|requireTenantContext\|serviceRoleClient\|app/(growth)\|app/(plg)\|route.ts\|vitest\|openapi-generate" lib/markos/growth/ scripts/preconditions/218-01-check-upstream.cjs | wc -l` returns 0 (architecture-lock applied to its own surface)
    - `node -e "console.log(typeof require('./lib/markos/growth/preflight/index.cjs').assertUpstreamReady)"` outputs `function`
    - `node -e "console.log(typeof require('./lib/markos/growth/preflight/index.cjs').scanForbiddenPatterns)"` outputs `function`
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 1: SaaSGrowthProfile + module_mode_eligibility SOR migration + DB-trigger compliance pack + 60-row eligibility seed + RLS</name>
  <files>supabase/migrations/101_growth_saas_profile.sql, test/growth-218/profile/profile-crud.test.js, test/growth-218/profile/mode-eligibility-matrix.test.js, test/growth-218/profile/mode-saas-activation-trigger.test.js, test/growth-218/rls/profile-cross-tenant-denial.test.js</files>
  <behavior>
    - Test 1: After migration 101, type markos_growth_mode exists as ENUM with exactly 5 values: 'b2b', 'b2c', 'plg_b2b', 'plg_b2c', 'b2b2c'
    - Test 2: After migration 101, `\d+ saas_growth_profiles` shows columns (profile_id PK + tenant_id FK markos_orgs(org_id) UNIQUE + suite_activation_id + mode markos_growth_mode + has_sales_team bool + has_cs_team bool + has_developer_audience bool + plg_enabled bool GENERATED + b2b_enabled bool GENERATED + b2c_enabled bool GENERATED + active_modules text[] + gtm_motion CHECK 5 values + approved_at + approved_by + version int DEFAULT 1 + created_at + updated_at); RLS enabled with tenant_id policy
    - Test 3: After migration 101, `\d+ module_mode_eligibility` shows 3 columns (module_key text + mode markos_growth_mode + eligible bool DEFAULT true) + PRIMARY KEY (module_key, mode); RLS NOT enabled (system-level table)
    - Test 4: SELECT count(*) FROM module_mode_eligibility = 60 (12 modules × 5 modes); SELECT count(*) WHERE eligible=true = 39; specific row probes per RESEARCH §6 Domain 1 matrix:
      - module_key='abm_engine' AND mode='b2b' → eligible=true
      - module_key='abm_engine' AND mode='b2c' → eligible=false
      - module_key='plg_engine' AND mode='b2b' → eligible=false
      - module_key='plg_engine' AND mode='plg_b2b' → eligible=true
      - module_key='community' AND mode IN ('b2c','plg_b2b','plg_b2c','b2b2c') → all 4 rows eligible=true
      - module_key='growth_experiments' AND mode IN (all 5 modes) → all 5 rows eligible=true
    - Test 5: DB-trigger MODE_REQUIRES_SAAS_ACTIVATION — INSERT saas_growth_profiles for tenant_id with NO active saas_suite_activations row → expect EXCEPTION 'MODE_REQUIRES_SAAS_ACTIVATION'; with active activation row → succeeds
    - Test 6: Cross-tenant RLS denial — INSERT saas_growth_profiles under tenant A; SELECT under tenant B (via SET LOCAL app.tenant_id) returns 0 rows; module_mode_eligibility correctly readable under both tenants (system-level by design)
    - Test 7: idx_sgp_tenant_mode index exists on saas_growth_profiles(tenant_id, mode); UNIQUE(tenant_id) enforced (second INSERT for same tenant raises unique violation)
  </behavior>
  <read_first>
    - 218-RESEARCH.md §6 Domain 1 (read SQL block lines 354-405 carefully; column types + CHECK + RLS + indexes + 60-row matrix)
    - 218-RESEARCH.md §Compliance Enforcement Boundary Per Domain (Plan 01 row — MODE_REQUIRES_SAAS_ACTIVATION + MODULE_REQUIRES_ELIGIBLE_GROWTH_MODE exact exception names)
    - obsidian/brain/SaaS Marketing OS Strategy Canon.md lines 53-59 (canon source for 5-mode list)
    - supabase/migrations/82_markos_audit_log_hash_chain.sql (audit pattern P218 inherits)
    - supabase/migrations/100_crm_schema_identity_graph_hardening.sql (most recent migration; verify pattern conventions)
    - .planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md (Task 0.1 output — confirm P218-01 owns slot 101)
    - .planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-01-PLAN.md Task 1 (REPLICATE migration scaffold pattern; swap referral → saas-growth-profile)
    - .planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-01-PLAN.md Task 1 (REPLICATE migration trigger structure)
  </read_first>
  <action>
    Step 1: Write the 7 tests BEFORE migration lands (TDD RED). Tests use a Postgres testcontainer or local supabase shadow database.
    Step 2: Write supabase/migrations/101_growth_saas_profile.sql per RESEARCH §6 Domain 1 SQL block. Include:
      - Comment header: `-- Plan 218-01 / Phase 218 / Slot 101 / RESEARCH §6 Domain 1`
      - CREATE TYPE markos_growth_mode AS ENUM ('b2b', 'b2c', 'plg_b2b', 'plg_b2c', 'b2b2c');
      - CREATE TABLE saas_growth_profiles (per RESEARCH §6 Domain 1 schema):
        - profile_id UUID PK DEFAULT gen_random_uuid()
        - tenant_id UUID NOT NULL REFERENCES markos_orgs(org_id) — UNIQUE(tenant_id)
        - suite_activation_id UUID NOT NULL — FK to saas_suite_activations (P214)
        - mode markos_growth_mode NOT NULL
        - has_sales_team boolean NOT NULL DEFAULT false
        - has_cs_team boolean NOT NULL DEFAULT false
        - has_developer_audience boolean NOT NULL DEFAULT false
        - plg_enabled boolean GENERATED ALWAYS AS (mode IN ('plg_b2b','plg_b2c','b2b2c')) STORED
        - b2b_enabled boolean GENERATED ALWAYS AS (mode IN ('b2b','plg_b2b','b2b2c')) STORED
        - b2c_enabled boolean GENERATED ALWAYS AS (mode IN ('b2c','plg_b2c','b2b2c')) STORED
        - active_modules text[] NOT NULL DEFAULT ARRAY[]::text[]
        - gtm_motion text CHECK (gtm_motion IN ('product_led','sales_led','marketing_led','hybrid','community_led'))
        - approved_at timestamptz, approved_by uuid, version int NOT NULL DEFAULT 1, created_at, updated_at
      - ALTER TABLE saas_growth_profiles ENABLE ROW LEVEL SECURITY;
      - CREATE POLICY sgp_tenant_isolation ON saas_growth_profiles FOR ALL USING (tenant_id = current_setting('app.tenant_id', true)::uuid);
      - CREATE INDEX idx_sgp_tenant_mode ON saas_growth_profiles(tenant_id, mode);
      - CREATE TABLE module_mode_eligibility (module_key text NOT NULL, mode markos_growth_mode NOT NULL, eligible boolean NOT NULL DEFAULT true, PRIMARY KEY (module_key, mode));
      - 60-row INSERT block per RESEARCH §6 Domain 1 matrix (12 modules × 5 modes); grouped by module_key for readability:
        - plg_engine: (b2b,F)(b2c,F)(plg_b2b,T)(plg_b2c,T)(b2b2c,T)
        - upgrade_triggers: (b2b,F)(b2c,F)(plg_b2b,T)(plg_b2c,T)(b2b2c,T)
        - in_app_marketing: (b2b,F)(b2c,T)(plg_b2b,T)(plg_b2c,T)(b2b2c,T)
        - growth_experiments: (b2b,T)(b2c,T)(plg_b2b,T)(plg_b2c,T)(b2b2c,T)
        - account_expansion: (b2b,T)(b2c,F)(plg_b2b,T)(plg_b2c,F)(b2b2c,T)
        - abm_engine: (b2b,T)(b2c,F)(plg_b2b,T)(plg_b2c,F)(b2b2c,T)
        - revenue_alignment: (b2b,T)(b2c,F)(plg_b2b,T)(plg_b2c,F)(b2b2c,T)
        - viral_referral: (b2b,F)(b2c,T)(plg_b2b,F)(plg_b2c,T)(b2b2c,T)
        - community: (b2b,F)(b2c,T)(plg_b2b,T)(plg_b2c,T)(b2b2c,T)
        - pr_analyst: (b2b,T)(b2c,F)(plg_b2b,T)(plg_b2c,F)(b2b2c,T)
        - partnerships: (b2b,T)(b2c,F)(plg_b2b,T)(plg_b2c,F)(b2b2c,T)
        - developer_marketing: (b2b,F)(b2c,F)(plg_b2b,T)(plg_b2c,F)(b2b2c,T)
        - Total = 60 rows; eligible=true count = 39
      - CREATE OR REPLACE FUNCTION fn_mode_requires_saas_activation() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF NOT EXISTS (SELECT 1 FROM saas_suite_activations WHERE tenant_id = NEW.tenant_id AND status = 'active') THEN RAISE EXCEPTION 'MODE_REQUIRES_SAAS_ACTIVATION: tenant % has no active SaaSSuiteActivation (P214 required)', NEW.tenant_id; END IF; RETURN NEW; END $$;
      - CREATE TRIGGER trg_mode_requires_saas_activation BEFORE INSERT OR UPDATE ON saas_growth_profiles FOR EACH ROW EXECUTE FUNCTION fn_mode_requires_saas_activation();
      - DOCUMENT (SQL comment block) the MODULE_REQUIRES_ELIGIBLE_GROWTH_MODE pattern that P219+P220 module-table triggers must JOIN against this table at runtime via lib/markos/profile/mode-eligibility.ts (since P220 migrations apply BEFORE P218 in numeric order — Q-7 sequencing resolution):
        ```sql
        -- CROSS-PHASE CONTRACT: P219+P220 module-table BEFORE INSERT triggers SHOULD be implemented as
        -- app-layer runtime checks via lib/markos/profile/mode-eligibility.ts.isModuleEligible() since
        -- P220 migrations (slots 90-97) numerically apply BEFORE P218 migration 101.
        -- The migration-time triggers gracefully degrade with WARN if module_mode_eligibility not yet present.
        -- Reconciliation cron handler (Plan 06 growth-profile-sync.js) re-validates after P218 lands.
        ```
      - DO $$ BEGIN ASSERT (SELECT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'saas_growth_profiles')); ASSERT (SELECT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'module_mode_eligibility')); ASSERT (SELECT count(*) FROM module_mode_eligibility) = 60; ASSERT (SELECT count(*) FROM module_mode_eligibility WHERE eligible = true) = 39; END $$;
    Step 3: Apply migration to test environment; run all 7 tests (TDD GREEN).
    Step 4: Commit migration with message: `db(218-01): saas_growth_profiles + module_mode_eligibility 60-row seed + MODE_REQUIRES_SAAS_ACTIVATION trigger / slot 101 (per V4.1.0-MIGRATION-SLOT-COORDINATION.md)`.
  </action>
  <acceptance_criteria>
    - `npm test -- test/growth-218/profile/profile-crud.test.js` passes (column-presence + RLS-enabled + UNIQUE(tenant_id) + GENERATED column behavior assertions green)
    - `npm test -- test/growth-218/profile/mode-eligibility-matrix.test.js` passes (60 rows assertion + 39 eligible=true + specific module/mode pairs probed)
    - `npm test -- test/growth-218/profile/mode-saas-activation-trigger.test.js` passes (positive + negative cases green for MODE_REQUIRES_SAAS_ACTIVATION)
    - `npm test -- test/growth-218/rls/profile-cross-tenant-denial.test.js` passes (saas_growth_profiles RLS-isolated; module_mode_eligibility cross-tenant readable as system-level)
    - `psql -c "SELECT count(*) FROM information_schema.tables WHERE table_name IN ('saas_growth_profiles','module_mode_eligibility')"` returns 2
    - `psql -c "SELECT count(*) FROM module_mode_eligibility"` returns 60
    - `psql -c "SELECT count(*) FROM module_mode_eligibility WHERE eligible=true"` returns 39
    - `psql -c "SELECT count(*) FROM pg_trigger WHERE tgname = 'trg_mode_requires_saas_activation'"` returns 1
    - `grep -E "MODE_REQUIRES_SAAS_ACTIVATION" supabase/migrations/101_growth_saas_profile.sql | wc -l` returns at least 1 (exact exception string per RESEARCH §Compliance Enforcement Boundary)
    - `grep -E "tenant_id.*current_setting" supabase/migrations/101_growth_saas_profile.sql | wc -l` returns at least 1 (RLS policy on saas_growth_profiles only — module_mode_eligibility intentionally service-role-only)
    - `psql -c "SELECT enumlabel FROM pg_enum WHERE enumtypid = 'markos_growth_mode'::regtype ORDER BY enumsortorder"` returns 5 rows: b2b, b2c, plg_b2b, plg_b2c, b2b2c
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Profile lib (contracts + growth-profiles + mode-eligibility) + 2 contracts + flow-registry sync + SAS-09 consume test + parity test + VALIDATION update</name>
  <files>lib/markos/profile/contracts.ts, lib/markos/profile/contracts.cjs, lib/markos/profile/growth-profiles.ts, lib/markos/profile/growth-profiles.cjs, lib/markos/profile/mode-eligibility.ts, lib/markos/profile/mode-eligibility.cjs, lib/markos/profile/index.cjs, contracts/F-238-growth-saas-profile-v1.yaml, contracts/F-239-growth-mode-eligibility-v1.yaml, contracts/flow-registry.json, test/growth-218/profile/sas09-consume.test.js, test/growth-218/profile/mode-change-requires-approval.test.js, test/growth-218/openapi/parity-profile.test.js, .planning/phases/218-saas-growth-profile-plg-inapp-experimentation/218-VALIDATION.md</files>
  <behavior>
    - Test 1 (mode-change-requires-approval): updateProfile({ tenant_id, mode: 'plg_b2b' }) — when existing mode is 'b2b' AND no approved_at — app-layer throws PermissionError before DB-call; via requestModeChange first → calls buildApprovalPackage with kind='saas_growth_mode_change' + payload, then UPDATE succeeds with approved_at populated
    - Test 2 (mode-change): UPDATE without mode-change (e.g., toggle has_sales_team=true) does NOT require approval — direct UPDATE succeeds
    - Test 3 (mode-eligibility-helper): isModuleEligible({ tenantId: A, moduleKey: 'plg_engine' }) — when tenant A's mode='plg_b2b' → returns true; when tenant A's mode='b2b' → returns false
    - Test 4 (mode-eligibility-helper): getEligibleModulesForTenant({ tenantId }) — returns string[] of eligible module keys for tenant's current mode; when mode='b2b2c' returns 12 modules (all eligible)
    - Test 5 (SAS-09 consume): static-analysis test — grep over lib/markos/profile/ for `from('saas_mrr_snapshots')` returns ONLY .select() calls; grep for `.insert\|.update\|.delete` against saas_mrr_snapshots / saas_health_scores returns 0 (consume-only contract per Q-3)
    - Test 6 (SAS-09 consume): integration test — Plan 01 reads from P217 saas_mrr_snapshots table when computing module activation gate signal; verify mock-supabase-client `.from('saas_mrr_snapshots').select(...)` invocation exists at least once
    - Test 7 (openapi-parity): F-238 YAML schema for SaaSGrowthProfile matches lib/markos/profile/contracts.ts SaaSGrowthProfileSchema zod (column names + types + required-field set); same for F-239 vs ModuleModeEligibilitySchema
    - Test 8 (openapi-parity): contracts/flow-registry.json contains entries for F-238..F-239 with no overlap against F-001..F-237
  </behavior>
  <read_first>
    - 218-RESEARCH.md §6 Domain 1 (lines 327-454, especially API surface + Cron handler + MCP tools tables)
    - 218-RESEARCH.md §Cross-Phase Coordination Q-1 + Q-3 (saas_growth_profiles.mode contract + SAS-09 consume-only)
    - obsidian/work/incoming/17-SAAS-MARKETING-OS-STRATEGY.md (TypeScript interface for SaaSGrowthProfile — column types are source-of-truth)
    - obsidian/reference/MarkOS v2 Operating Loop Spec.md line 53 (object shape canon)
    - lib/markos/crm/agent-actions.ts:68 (buildApprovalPackage signature)
    - .planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-01-PLAN.md Task 2 (REPLICATE contracts.ts + flow-registry pattern; swap referral → profile)
    - .planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-01-PLAN.md Task 2 (REPLICATE contracts pattern + zod schemas)
  </read_first>
  <action>
    Step 1: Write tests 1-8 BEFORE implementations (TDD RED).
    Step 2: Implement lib/markos/profile/contracts.ts:
      - Export 3 enum constants: GROWTH_MODES, GTM_MOTIONS, GROWTH_MODULE_KEYS (12 keys)
      - Export 1 approval-kind constant: GROWTH_APPROVAL_KINDS = ['saas_growth_mode_change'] as const
      - Export zod schemas: SaaSGrowthProfileSchema, ModuleModeEligibilitySchema; mirror migration 101 column types exactly
      - Export TS types via z.infer
      - CommonJS twin contracts.cjs re-exports the constants
    Step 3: Implement lib/markos/profile/growth-profiles.ts:
      ```typescript
      const { buildApprovalPackage } = require('../crm/agent-actions');
      export async function createProfile({ tenant_id, suite_activation_id, mode, ...fields }, supabase): Promise<{ profile_id: string }> { ... }
      export async function getProfile({ tenant_id }, supabase) { ... }
      export async function updateProfile({ tenant_id, ...fields }, supabase) {
        const existing = await getProfile({ tenant_id }, supabase);
        if (fields.mode !== undefined && fields.mode !== existing.mode && !fields.approved_at) {
          throw new Error('MODE_CHANGE_REQUIRES_APPROVAL: call requestModeChange first');
        }
        // UPDATE row
      }
      export async function requestModeChange({ tenant_id, new_mode, initiated_by }, supabase) {
        const pkg = buildApprovalPackage({ kind: 'saas_growth_mode_change', tenant_id, initiated_by, payload: { new_mode } });
        return { approval_id: pkg.approval_id };
      }
      ```
    Step 4: Implement lib/markos/profile/mode-eligibility.ts:
      ```typescript
      export async function isModuleEligible({ tenantId, moduleKey }, supabase): Promise<boolean> {
        const { data: profile } = await supabase.from('saas_growth_profiles').select('mode').eq('tenant_id', tenantId).single();
        if (!profile) return false;
        const { data: eligibility } = await supabase.from('module_mode_eligibility').select('eligible').eq('module_key', moduleKey).eq('mode', profile.mode).single();
        return Boolean(eligibility?.eligible);
      }
      export async function getEligibleModulesForTenant({ tenantId }, supabase): Promise<string[]> {
        const { data: profile } = await supabase.from('saas_growth_profiles').select('mode').eq('tenant_id', tenantId).single();
        if (!profile) return [];
        const { data: rows } = await supabase.from('module_mode_eligibility').select('module_key').eq('mode', profile.mode).eq('eligible', true);
        return (rows || []).map((r) => r.module_key);
      }
      ```
    Step 5: CommonJS twins for growth-profiles.cjs + mode-eligibility.cjs + index.cjs.
    Step 6: Write 2 contract YAMLs F-238/F-239 per RESEARCH §6 Domain 1 API surface table; mirror lib/markos/profile/contracts.ts schema. Auth: requireHostedSupabaseAuth bearer token. Errors: 401, 403, 422, 423 (when mode change requires approval).
    Step 7: Update contracts/flow-registry.json appending F-238..F-239 entries. Verify NO overlap with F-001..F-237.
    Step 8: Run all profile tests + SAS-09 consume + openapi-parity tests (TDD GREEN). Run `npm run openapi:build`.
    Step 9: Update 218-VALIDATION.md:
      - Frontmatter `nyquist_compliant: true` + `wave_0_complete: true`
      - Per-Task Verification Map — Plan 01 rows flipped to ✅ green
      - Wave 0 Requirements all P218-01-owned items checked
      - Dimensions Coverage row 1 (Requirements) + row 2 (Anti-shallow) + row 5 (Cross-phase) flipped to LOCKED
  </action>
  <acceptance_criteria>
    - `npm test -- test/growth-218/profile/mode-change-requires-approval.test.js` passes
    - `npm test -- test/growth-218/profile/sas09-consume.test.js` passes (static-analysis + integration consume-only)
    - `npm test -- test/growth-218/openapi/parity-profile.test.js` passes (2 contracts × zod parity)
    - `grep "F-238\|F-239" contracts/flow-registry.json | wc -l` returns 2
    - `grep -rn "createApprovalPackage\|requireSupabaseAuth\|lookupPlugin\|requireTenantContext\|app/(growth)\|app/(plg)\|route.ts\|vitest" lib/markos/profile/ | wc -l` returns 0 (architecture-lock applied)
    - `grep -rn "\.insert\|\.update\|\.delete" lib/markos/profile/ | grep -E "saas_mrr_snapshots|saas_health_scores" | wc -l` returns 0 (SAS-09 consume-only contract)
    - `npm run openapi:build` completes and contracts/openapi.json contains paths /v1/growth/saas-growth-profiles + /v1/growth/saas-growth-profiles/mode-eligibility
    - `node -e "console.log(typeof require('./lib/markos/profile/index.cjs').isModuleEligible)"` outputs `function`
    - `node -e "console.log(typeof require('./lib/markos/profile/index.cjs').requestModeChange)"` outputs `function`
    - `grep -E "nyquist_compliant: true" .planning/phases/218-saas-growth-profile-plg-inapp-experimentation/218-VALIDATION.md | wc -l` returns 1
    - `grep -E "wave_0_complete: true" .planning/phases/218-saas-growth-profile-plg-inapp-experimentation/218-VALIDATION.md | wc -l` returns 1
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser → /v1/growth/saas-growth-profiles | Untrusted operator input crosses here; mode mutations approval-gated |
| Cron worker → supabase service-role | High-trust path; protected by MARKOS_GROWTH_CRON_SECRET; DB-triggers still enforce compliance |
| App-layer requestModeChange → buildApprovalPackage | Tenant-isolated; approval_id required for mode changes (DB-trigger defense-in-depth via approved_at column) |
| Service-role direct INSERT on saas_growth_profiles | Bypasses app-layer; DB-trigger MODE_REQUIRES_SAAS_ACTIVATION blocks profile creation without P214 SaaSSuiteActivation |
| P219+P220 module-table INSERT triggers JOIN module_mode_eligibility (runtime via lib/markos/profile/mode-eligibility.ts) | System-level cross-phase contract; readable across tenants by design |
| P217 saas_mrr_snapshots → P218 reads | Consume-only; static-analysis test + integration test prove no .insert/.update/.delete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-218-01-01 | Tampering | saas_growth_profiles service-role INSERT | mitigate | DB-trigger fn_mode_requires_saas_activation enforces P214 SaaSSuiteActivation existence |
| T-218-01-02 | Tampering | saas_growth_profiles mode change without approval | mitigate | App-layer guard in updateProfile + requestModeChange calls buildApprovalPackage; defense-in-depth via approved_at |
| T-218-01-03 | Information Disclosure | Cross-tenant growth profile leakage | mitigate | RLS policy on saas_growth_profiles tenant_id; tested |
| T-218-01-04 | Repudiation | Operator denies approving mode change | mitigate | buildApprovalPackage writes approval row with approved_by + approved_at; SHA-256 audit hash chain |
| T-218-01-05 | Tampering | module_mode_eligibility row direct UPDATE bypassing canon matrix | mitigate | service-role-only RLS policy; canon-matrix invariant test in CI |
| T-218-01-06 | Spoofing | Cron token leaked → unauthorized scan | mitigate | crypto.timingSafeEqual on cron token; per-env MARKOS_GROWTH_CRON_SECRET; deferred to Plan 06 cron handlers |
| T-218-01-07 | Information Disclosure | module_mode_eligibility leaks across tenants | accept | Table is system-level by design — eligibility matrix is canonical product knowledge, not tenant-confidential |
| T-218-01-08 | Elevation of Privilege | Operator changes mode to 'b2b2c' to activate all modules without canonical permission | mitigate | mode change is approval-gated via buildApprovalPackage; approver scope required |
| T-218-01-09 | Denial of Service | P219+P220 module-INSERT triggers blocked because module_mode_eligibility not yet seeded | mitigate | Migration 101 DO $$ ASSERT count = 60 invariant; cross-phase coordination doc resolves Q-7 sequencing via app-layer runtime checks |
| T-218-01-10 | Tampering | Direct UPDATE on saas_growth_profiles.active_modules without mode change | accept | active_modules is a derived cache field; cron handler `growth-profile-sync.js` (Plan 06) re-reconciles daily |
</threat_model>

<verification>
**Plan-level verification (run after Task 2 closes):**
- `npm test -- test/growth-218/preflight/ test/growth-218/profile/ test/growth-218/rls/profile-* test/growth-218/openapi/parity-profile.test.js` → all green
- `node scripts/preconditions/218-01-check-upstream.cjs` → exits 0 with green env (or 1 with proper UpstreamPhaseNotLandedError on missing required upstream P214/P215/P216/P217)
- `psql -c "SELECT count(*) FROM information_schema.tables WHERE table_name IN ('saas_growth_profiles','module_mode_eligibility')"` returns 2
- `psql -c "SELECT count(*) FROM module_mode_eligibility"` returns 60
- `psql -c "SELECT count(*) FROM module_mode_eligibility WHERE eligible=true"` returns 39
- `psql -c "SELECT count(*) FROM pg_trigger WHERE tgname = 'trg_mode_requires_saas_activation'"` returns 1
- `psql -c "SELECT count(*) FROM pg_enum WHERE enumtypid = 'markos_growth_mode'::regtype"` returns 5
- `grep -E "F-23[89]" contracts/flow-registry.json | wc -l` returns 2
- `grep -rn "createApprovalPackage\|requireSupabaseAuth\|requireTenantContext\|serviceRoleClient\|lookupPlugin\|public/openapi.json\|app/(growth)\|app/(plg)\|api/v1/.*/route.ts\|vitest\|from 'vitest'\|.test.ts" lib/markos/growth/ lib/markos/profile/ scripts/preconditions/218-01-check-upstream.cjs | wc -l` returns 0
- `grep -E "P218" .planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md | wc -l` returns >= 6
- `grep -iE "P219.*replan|85-89.*collide" .planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md | wc -l` returns >= 1
</verification>

<success_criteria>
- All 4 tasks complete with passing tests; total ~28+ assertions green
- Migration slot coordination doc CREATED (P218 first phase to ship); resolves Q-1..Q-7; flags P219 collision (85-89 invalid)
- 1 SOR migration (101) + 6 profile lib files + 2 contracts ship with NO architecture-lock violations
- assertUpstreamReady preflight + architecture-lock + helper-presence test surface ready for Plans 02-06
- 218-VALIDATION.md per-task map populated; nyquist_compliant + wave_0_complete frontmatter flipped
- Domain 1 (SaaSGrowthProfile/ModeRouting) compliance enforcement boundary LOCKED at DB-trigger layer
- module_mode_eligibility 60-row seed ships AS THE FOUNDATION for P219+P220 module activation gate (Q-1 contract)
- SAS-09 consume-only contract proven via static-analysis + integration tests (Q-3)
</success_criteria>

<output>
After completion, create `.planning/phases/218-saas-growth-profile-plg-inapp-experimentation/218-01-SUMMARY.md` documenting:
- Migration slot claimed: 101
- F-IDs claimed: F-238, F-239
- Tables created: saas_growth_profiles, module_mode_eligibility (60 rows; 39 eligible)
- markos_growth_mode ENUM created (5 values)
- DB-triggers created: trg_mode_requires_saas_activation
- Cross-phase contract documented (MODULE_REQUIRES_ELIGIBLE_GROWTH_MODE pattern via runtime helper)
- Q-7 P219 collision flagged in coordination doc (85-89 invalid; cross-phase replan required)
</output>
