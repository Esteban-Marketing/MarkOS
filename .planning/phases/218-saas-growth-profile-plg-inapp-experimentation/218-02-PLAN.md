---
phase: 218
plan: "02"
type: execute
wave: 2
depends_on:
  - "218-01"
f_ids:
  - F-240
  - F-241
migration_slots:
  - 102
files_modified:
  - scripts/preconditions/218-02-check-upstream.cjs
  - supabase/migrations/102_growth_activation_pql.sql
  - lib/markos/activation/contracts.ts
  - lib/markos/activation/contracts.cjs
  - lib/markos/activation/definitions.ts
  - lib/markos/activation/milestones.ts
  - lib/markos/activation/index.cjs
  - lib/markos/pql/contracts.ts
  - lib/markos/pql/contracts.cjs
  - lib/markos/pql/scorer.ts
  - lib/markos/pql/scorer.cjs
  - lib/markos/pql/index.cjs
  - contracts/F-240-growth-activation-definition-v1.yaml
  - contracts/F-241-growth-pql-score-v1.yaml
  - contracts/flow-registry.json
  - test/growth-218/activation/definition-crud.test.js
  - test/growth-218/activation/milestone-funnel-steps.test.js
  - test/growth-218/pql/pql-score-evidence-trigger.test.js
  - test/growth-218/pql/pql-scorer-math.test.js
  - test/growth-218/rls/activation-pql-cross-tenant-denial.test.js
  - test/growth-218/openapi/parity-activation-pql.test.js
  - .planning/phases/218-saas-growth-profile-plg-inapp-experimentation/218-VALIDATION.md
autonomous: true
requirements:
  - SG-02
  - SG-09
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - QA-05
  - QA-06
must_haves:
  truths:
    - assertUpstreamReady preflight CLI `scripts/preconditions/218-02-check-upstream.cjs` hard-fails when REQUIRED_UPSTREAM tables missing (saas_growth_profiles from 218-01, saas_health_scores from P216 — Plan 02 PQL scorer reads health_score signals); soft-warns on P209 evidence_map_records / P210 posthog_connector_installs; reuses lib/markos/growth/preflight/upstream-gate.ts
    - 3 SOR tables ship in migration 102 — `activation_definitions` (UNIQUE(tenant_id) + RLS) + `activation_milestones` (UNIQUE(definition_id, step) + RLS) + `pql_scores` (append-only + RLS + idx_pql_account_status)
    - DB-trigger compliance pack ships in migration 102 — `PQL_SCORE_REQUIRES_EVIDENCE` BEFORE INSERT OR UPDATE on pql_scores raises EXCEPTION when pql_status IN ('pql','hot_pql') AND ALL of (hit_usage_limit=false AND high_frequency_user=false AND feature_breadth_pct < 0.3 AND visited_pricing_page=false AND visited_upgrade_page=false) — pql/hot_pql transition requires at least one explainable signal
    - 2 contracts ship F-240 (growth-activation-definition-v1) + F-241 (growth-pql-score-v1) with NO collision against F-001..F-239
    - PQL scorer math test — given known signals (e.g., hit_usage_limit=true, feature_breadth_pct=0.45, visited_pricing_page=true) computes score in expected band [60-90] and pql_status='hot_pql' or 'pql' (operator-configured threshold; test uses fixture defaults)
    - PQL evidence-required trigger test — service-role direct INSERT pql_scores with pql_status='pql' AND all signals false → expect EXCEPTION 'PQL_SCORE_REQUIRES_EVIDENCE'; with at least 1 signal true → succeeds
    - Activation definition UNIQUE(tenant_id) — second INSERT for same tenant raises unique violation (one definition per tenant per RESEARCH §6 Domain 2 schema)
    - Milestone UNIQUE(definition_id, step) — milestone funnel cannot have duplicate step numbers per definition
    - Cross-tenant RLS denial test — INSERT under tenant A then SELECT under tenant B returns 0 rows for activation_definitions / activation_milestones / pql_scores
    - lib/markos/pql/scorer.ts exports computePqlScore({ tenantId, userId, accountId }, supabase) → reads saas_health_scores (P216) signals + activation_definitions thresholds + writes pql_scores row; returns { score, pql_status, recommended_action }
    - SG-09 task-creation invariant — when scorer transitions user to 'pql' or 'hot_pql', creates a task linked via task_created_id (PLG signal handoff per RESEARCH §Cross-Phase Coordination Q-3 SAS-09 consume)
    - Activation definition approval — updateActivationDefinition() requires approved_at when changing aha_moment threshold values (mode-change-style approval; reuses pattern from Plan 01)
  artifacts:
    - path: scripts/preconditions/218-02-check-upstream.cjs
      provides: CLI preflight checker for Plan 02; consumes lib/markos/growth/preflight/index.cjs.assertUpstreamReady; HARD upstream = saas_growth_profiles + saas_health_scores; SOFT = pricing_recommendations / agent_runs / approval_packages / evidence_map_records / posthog_connector_installs
    - path: supabase/migrations/102_growth_activation_pql.sql
      provides: activation_definitions (definition_id PK + tenant_id FK markos_orgs UNIQUE + profile_id FK saas_growth_profiles + saas_model markos_growth_mode + aha_moment_description + aha_moment_event_name + aha_moment_minimum_count int CHECK > 0 + aha_moment_window_days int CHECK > 0 DEFAULT 14 + 4 baseline numeric(5,4) cols + approval cols + version) + activation_milestones (milestone_id PK + definition_id FK + tenant_id FK + step int CHECK >= 1 + name + event_key + completion_rate + median_time_hours + drop_off_reason + UNIQUE(definition_id, step)) + pql_scores (score_id PK + tenant_id + definition_id + user_id + account_id + subscription_id + score int CHECK 0-100 + pql_status text CHECK 4 values + 6 usage signal cols + 5 intent signal cols + 4 fit signal cols + evidence_refs uuid[] + recommended_action text CHECK 6 values + recommended_timing + recommended_channel + calculated_at + task_created_id) + RLS on all 3 + 1 DB-trigger fn_pql_score_requires_evidence + idx_pql_account_status ON pql_scores(tenant_id, account_id, pql_status, calculated_at DESC)
    - path: lib/markos/activation/contracts.ts
      provides: 1 enum constant ACTIVATION_RECOMMENDED_ACTIONS = (none — owned by pql); zod schemas (ActivationDefinitionSchema, ActivationMilestoneSchema)
      exports: [ActivationDefinitionSchema, ActivationMilestoneSchema, ACTIVATION_APPROVAL_KINDS]
    - path: lib/markos/activation/definitions.ts
      provides: createDefinition / getDefinition / updateDefinition CRUD against activation_definitions; threshold-change requires approval (reuse pattern from lib/markos/profile/growth-profiles.ts requestModeChange)
      exports: [createDefinition, getDefinition, updateDefinition, requestThresholdChange]
    - path: lib/markos/activation/milestones.ts
      provides: createMilestone / listMilestones / updateMilestone / deleteMilestone CRUD; UNIQUE(definition_id, step) enforced via DB
      exports: [createMilestone, listMilestones, updateMilestone, deleteMilestone]
    - path: lib/markos/pql/contracts.ts
      provides: 2 enum constants (PQL_STATUSES = ['not_ready','warming','pql','hot_pql'] as const, PQL_RECOMMENDED_ACTIONS = ['in_app_upgrade_prompt','targeted_email_sequence','sales_outreach','self_serve_offer','feature_unlock','continue_monitoring'] as const) + zod schema PqlScoreSchema
      exports: [PQL_STATUSES, PQL_RECOMMENDED_ACTIONS, PqlScoreSchema]
    - path: lib/markos/pql/scorer.ts
      provides: computePqlScore({ tenantId, userId, accountId }, supabase) — reads saas_health_scores (P216) + activation_definitions; writes pql_scores row with explainable signals; on pql/hot_pql transition creates task via lib/markos/audit task hook (SG-09 + LOOP-06-style evidence link); returns { score, pql_status, recommended_action }
      exports: [computePqlScore, getLatestPqlScore, recalculatePqlForAccount]
    - path: contracts/F-240-growth-activation-definition-v1.yaml
      provides: GET /v1/growth/activation-definitions (list + single) + POST + PATCH (with approval gate on threshold change); GET /v1/growth/activation-definitions/{id}/milestones + POST + PATCH + DELETE per RESEARCH §6 Domain 2 API surface
    - path: contracts/F-241-growth-pql-score-v1.yaml
      provides: GET /v1/growth/pql-scores (latest scores read-only) + POST /v1/growth/pql-scores/recalculate (trigger rescore for user/account)
    - path: test/growth-218/pql/pql-score-evidence-trigger.test.js
      provides: service-role direct INSERT with pql_status='pql' + all signals false → EXCEPTION 'PQL_SCORE_REQUIRES_EVIDENCE'; with hit_usage_limit=true → succeeds; with feature_breadth_pct=0.5 → succeeds (≥ 0.3 threshold)
    - path: test/growth-218/pql/pql-scorer-math.test.js
      provides: computePqlScore with known signal fixture → asserts score band + pql_status + recommended_action match expected; verifies P216 saas_health_scores read invocation (consume-only)
  key_links:
    - from: lib/markos/pql/scorer.ts
      to: supabase tables saas_health_scores (P216) + activation_definitions (218-02) + pql_scores (218-02)
      via: scorer reads health_score via .from('saas_health_scores').select(...); reads thresholds via .from('activation_definitions').select(...); writes pql_scores row via .from('pql_scores').insert(...)
      pattern: "saas_health_scores"
    - from: lib/markos/activation/definitions.ts
      to: lib/markos/crm/agent-actions.ts
      via: requestThresholdChange calls buildApprovalPackage with kind='activation_threshold_change'
      pattern: "buildApprovalPackage"
    - from: supabase/migrations/102_growth_activation_pql.sql
      to: supabase/migrations/101_growth_saas_profile.sql
      via: activation_definitions.profile_id FK references saas_growth_profiles(profile_id); migration 102 must apply AFTER 101
      pattern: "REFERENCES saas_growth_profiles"
    - from: lib/markos/pql/scorer.ts
      to: lib/markos/audit (task creation)
      via: on pql/hot_pql transition emits task with task_created_id linked to pql_scores row
      pattern: "task_created_id"
---

<objective>
Wave 2 schema for Phase 218 Domain 2 — ship ActivationDefinition (one per tenant; aha-moment specification), milestone funnels (UNIQUE(definition_id, step)), PQLScore (append-only, explainable signals across usage/intent/fit dimensions), the PQL_SCORE_REQUIRES_EVIDENCE DB-trigger (defense-in-depth — pql/hot_pql transition cannot land without at least one explainable signal), 2 F-ID contracts (F-240/F-241), the PQL scorer (reads P216 saas_health_scores, writes pql_scores rows, creates tasks on pql/hot_pql transition per SG-09), and the cross-tenant RLS denial suite.

Purpose: satisfy SG-02 (PLG capability — activation definitions, milestone funnels, PQL scoring), SG-09 (PQL transitions create tasks), QA-01..06 (architecture-lock + helper-presence + RLS isolation + F-ID parity + threat model + DB-trigger evidence enforcement). Plan 02 unblocks Plan 03 (UpgradeTrigger consumes PQL signals to time prompt firing) and Plan 04 (InAppCampaign segment targeting can filter by pql_status).

Output: 1 SOR migration (102) + 9 lib files across `lib/markos/activation/` + `lib/markos/pql/` + 2 contract YAMLs + flow-registry update + 6 test files + VALIDATION.md row updates.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/218-saas-growth-profile-plg-inapp-experimentation/218-CONTEXT.md
@.planning/phases/218-saas-growth-profile-plg-inapp-experimentation/218-RESEARCH.md
@.planning/phases/218-saas-growth-profile-plg-inapp-experimentation/218-01-SUMMARY.md
@.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@lib/markos/growth/preflight/index.cjs
@lib/markos/profile/index.cjs
@lib/markos/crm/agent-actions.ts
@onboarding/backend/runtime-context.cjs
@supabase/migrations/101_growth_saas_profile.sql
@obsidian/work/incoming/17-SAAS-MARKETING-OS-STRATEGY.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Plan 02 preflight — verify Plan 01 complete</name>
  <files>scripts/preconditions/218-02-check-upstream.cjs</files>
  <read_first>
    - lib/markos/growth/preflight/index.cjs (Plan 01 surface)
    - .planning/phases/218-saas-growth-profile-plg-inapp-experimentation/218-01-SUMMARY.md
    - 218-RESEARCH.md §6 Domain 2 (Domain dependencies — PQL scorer reads P216 saas_health_scores)
  </read_first>
  <action>
    Step 1: Verify Plan 01 complete: assert migration 101 exists; assert tables saas_growth_profiles + module_mode_eligibility exist; assert F-238 + F-239 in flow-registry.json.
    Step 2: Create scripts/preconditions/218-02-check-upstream.cjs replicating 218-01 pattern with HARD upstream = [P218-01 saas_growth_profiles, P216 saas_health_scores]; SOFT = [P205, P207, P208, P209, P210].
    Step 3: Run CLI; exit 0 only if Plan 01 complete + P216 health_scores available.
  </action>
  <acceptance_criteria>
    - `node scripts/preconditions/218-02-check-upstream.cjs` exits 0 when Plan 01 complete + P216 saas_health_scores present
    - `psql -c "SELECT count(*) FROM information_schema.tables WHERE table_name IN ('saas_growth_profiles','module_mode_eligibility','saas_health_scores')"` returns 3
    - `grep -E "F-238" contracts/flow-registry.json | wc -l` returns 1
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 1: ActivationDefinition + ActivationMilestone + PQLScore SOR migration + DB-trigger compliance + RLS</name>
  <files>supabase/migrations/102_growth_activation_pql.sql, test/growth-218/activation/definition-crud.test.js, test/growth-218/activation/milestone-funnel-steps.test.js, test/growth-218/pql/pql-score-evidence-trigger.test.js, test/growth-218/rls/activation-pql-cross-tenant-denial.test.js</files>
  <behavior>
    - Test 1: After migration 102, `\d+ activation_definitions` shows ~16 columns (definition_id PK + tenant_id FK markos_orgs UNIQUE + profile_id FK saas_growth_profiles + saas_model markos_growth_mode + aha_moment_description text NOT NULL + aha_moment_event_name text NOT NULL + aha_moment_minimum_count int CHECK > 0 DEFAULT 1 + aha_moment_window_days int CHECK > 0 DEFAULT 14 + baseline_signup_to_activation_rate numeric(5,4) DEFAULT 0 + baseline_median_time_hours numeric(10,2) + baseline_activation_to_paid_rate numeric(5,4) DEFAULT 0 + baseline_non_activated_churn_rate numeric(5,4) DEFAULT 0 + approved_at + approved_by + version int DEFAULT 1 + created_at + updated_at); RLS enabled
    - Test 2: After migration 102, `\d+ activation_milestones` shows 9 columns (milestone_id PK + definition_id FK NOT NULL + tenant_id FK + step int CHECK >= 1 + name + event_key + completion_rate + median_time_hours + drop_off_reason + created_at + updated_at) + UNIQUE(definition_id, step); RLS enabled
    - Test 3: After migration 102, `\d+ pql_scores` shows ~24 columns matching RESEARCH §6 Domain 2 schema (score_id PK + tenant_id + definition_id + user_id + account_id + subscription_id + score int CHECK 0-100 + pql_status text CHECK 4 values + 6 usage signals + 5 intent signals + 4 fit signals + evidence_refs uuid[] + recommended_action text CHECK 6 values + recommended_timing + recommended_channel + calculated_at + task_created_id); RLS enabled; index idx_pql_account_status ON (tenant_id, account_id, pql_status, calculated_at DESC)
    - Test 4: DB-trigger PQL_SCORE_REQUIRES_EVIDENCE — INSERT pql_scores with pql_status='pql' AND hit_usage_limit=false AND high_frequency_user=false AND feature_breadth_pct < 0.3 AND visited_pricing_page=false AND visited_upgrade_page=false → EXCEPTION 'PQL_SCORE_REQUIRES_EVIDENCE'; with hit_usage_limit=true → succeeds; with feature_breadth_pct=0.5 → succeeds (≥ 0.3 threshold)
    - Test 5: DB-trigger PQL_SCORE_REQUIRES_EVIDENCE — INSERT with pql_status='warming' AND all signals false → succeeds (only pql/hot_pql gated)
    - Test 6: UNIQUE(tenant_id) on activation_definitions enforced — second INSERT for same tenant raises unique violation
    - Test 7: UNIQUE(definition_id, step) on activation_milestones enforced — second INSERT with same definition_id + step raises unique violation
    - Test 8: Cross-tenant RLS denial — INSERT under tenant A; SELECT under tenant B returns 0 rows for all 3 tables
  </behavior>
  <read_first>
    - 218-RESEARCH.md §6 Domain 2 (read SQL block lines 466-557 carefully)
    - 218-RESEARCH.md §Compliance Enforcement Boundary Per Domain (Plan 02 row)
    - obsidian/work/incoming/17-SAAS-MARKETING-OS-STRATEGY.md line 159 (ActivationDefinition TS interface) + line 237 (PQLScore TS interface)
    - .planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md (confirm P218-02 owns slot 102)
    - supabase/migrations/101_growth_saas_profile.sql (Plan 01 output — saas_growth_profiles FK target)
    - .planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-01-PLAN.md Task 1 (REPLICATE migration scaffold pattern with multi-table + multi-trigger)
  </read_first>
  <action>
    Step 1: Write tests 1-8 BEFORE migration lands (TDD RED).
    Step 2: Write supabase/migrations/102_growth_activation_pql.sql per RESEARCH §6 Domain 2:
      - Comment header: `-- Plan 218-02 / Phase 218 / Slot 102 / RESEARCH §6 Domain 2`
      - 3 CREATE TABLE statements in order: activation_definitions, activation_milestones, pql_scores
      - All 3 ALTER TABLE ENABLE ROW LEVEL SECURITY + CREATE POLICY *_tenant_isolation USING tenant_id pattern
      - CREATE INDEX idx_pql_account_status ON pql_scores(tenant_id, account_id, pql_status, calculated_at DESC)
      - CREATE OR REPLACE FUNCTION fn_pql_score_requires_evidence() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF NEW.pql_status IN ('pql','hot_pql') AND NEW.hit_usage_limit = false AND NEW.high_frequency_user = false AND NEW.feature_breadth_pct < 0.3 AND NEW.visited_pricing_page = false AND NEW.visited_upgrade_page = false THEN RAISE EXCEPTION 'PQL_SCORE_REQUIRES_EVIDENCE: pql/hot_pql status requires at least one explainable signal for tenant %, user %', NEW.tenant_id, NEW.user_id; END IF; RETURN NEW; END $$;
      - CREATE TRIGGER trg_pql_score_requires_evidence BEFORE INSERT OR UPDATE ON pql_scores FOR EACH ROW EXECUTE FUNCTION fn_pql_score_requires_evidence();
      - DO $$ BEGIN ASSERT (3 tables exist); END $$;
    Step 3: Apply migration; run all 8 tests (TDD GREEN).
    Step 4: Commit: `db(218-02): activation_definitions + activation_milestones + pql_scores + PQL_SCORE_REQUIRES_EVIDENCE / slot 102`
  </action>
  <acceptance_criteria>
    - All 4 test files green
    - `psql -c "SELECT count(*) FROM information_schema.tables WHERE table_name IN ('activation_definitions','activation_milestones','pql_scores')"` returns 3
    - `psql -c "SELECT count(*) FROM pg_trigger WHERE tgname = 'trg_pql_score_requires_evidence'"` returns 1
    - `grep -E "PQL_SCORE_REQUIRES_EVIDENCE" supabase/migrations/102_growth_activation_pql.sql | wc -l` returns at least 1
    - `grep -E "tenant_id.*current_setting" supabase/migrations/102_growth_activation_pql.sql | wc -l` returns at least 3 (RLS on each of 3 tables)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Activation + PQL lib (contracts + definitions + milestones + scorer) + 2 contracts + flow-registry sync + scorer math test + parity test + VALIDATION update</name>
  <files>lib/markos/activation/contracts.ts, lib/markos/activation/contracts.cjs, lib/markos/activation/definitions.ts, lib/markos/activation/milestones.ts, lib/markos/activation/index.cjs, lib/markos/pql/contracts.ts, lib/markos/pql/contracts.cjs, lib/markos/pql/scorer.ts, lib/markos/pql/scorer.cjs, lib/markos/pql/index.cjs, contracts/F-240-growth-activation-definition-v1.yaml, contracts/F-241-growth-pql-score-v1.yaml, contracts/flow-registry.json, test/growth-218/pql/pql-scorer-math.test.js, test/growth-218/openapi/parity-activation-pql.test.js, .planning/phases/218-saas-growth-profile-plg-inapp-experimentation/218-VALIDATION.md</files>
  <behavior>
    - Test 1 (scorer-math): computePqlScore with hit_usage_limit=true + visited_pricing_page=true → score >= 60 + pql_status IN ('pql','hot_pql') + recommended_action='in_app_upgrade_prompt'
    - Test 2 (scorer-math): computePqlScore with all signals false → score < 30 + pql_status='not_ready' + recommended_action='continue_monitoring'
    - Test 3 (scorer-math): scorer reads from saas_health_scores (P216) — verify mock-supabase `.from('saas_health_scores').select(...)` invocation exists
    - Test 4 (scorer-task-creation): when scorer transitions user from 'warming' → 'pql', creates a task linked via task_created_id (SG-09 invariant)
    - Test 5 (definition-approval): updateDefinition with aha_moment_minimum_count change WITHOUT approved_at → app-layer throws; with approved_at via requestThresholdChange → succeeds
    - Test 6 (milestone-crud): createMilestone duplicate (definition_id, step) → DB-level unique violation
    - Test 7 (openapi-parity): F-240 + F-241 YAMLs match zod schemas in lib/markos/{activation,pql}/contracts.ts
    - Test 8 (openapi-parity): flow-registry.json contains F-240 + F-241 with no overlap against F-001..F-239
  </behavior>
  <read_first>
    - 218-RESEARCH.md §6 Domain 2 (lines 458-607)
    - obsidian/work/incoming/17-SAAS-MARKETING-OS-STRATEGY.md line 237 (PQLScore TS interface — column types canon)
    - lib/markos/profile/contracts.ts (Plan 01 output — replicate enum + zod patterns)
    - lib/markos/crm/agent-actions.ts:68 (buildApprovalPackage signature)
    - .planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-02-PLAN.md Task 2 (REPLICATE multi-domain lib pattern)
  </read_first>
  <action>
    Step 1: Write tests 1-8 BEFORE implementation (TDD RED).
    Step 2: Implement lib/markos/activation/contracts.ts:
      - 1 enum constant: ACTIVATION_APPROVAL_KINDS = ['activation_threshold_change'] as const
      - zod schemas: ActivationDefinitionSchema, ActivationMilestoneSchema
      - CommonJS twin contracts.cjs
    Step 3: Implement lib/markos/activation/definitions.ts:
      ```typescript
      const { buildApprovalPackage } = require('../crm/agent-actions');
      export async function createDefinition({ tenant_id, profile_id, saas_model, ... }, supabase): Promise<{ definition_id: string }>
      export async function getDefinition({ tenant_id }, supabase)
      export async function updateDefinition({ tenant_id, ...fields }, supabase) {
        const existing = await getDefinition({ tenant_id }, supabase);
        const thresholdFields = ['aha_moment_minimum_count', 'aha_moment_window_days'];
        const changingThreshold = thresholdFields.some((f) => fields[f] !== undefined && fields[f] !== existing[f]);
        if (changingThreshold && !fields.approved_at) {
          throw new Error('THRESHOLD_CHANGE_REQUIRES_APPROVAL');
        }
        // UPDATE row
      }
      export async function requestThresholdChange({ tenant_id, new_thresholds, initiated_by }, supabase) {
        const pkg = buildApprovalPackage({ kind: 'activation_threshold_change', tenant_id, initiated_by, payload: { new_thresholds } });
        return { approval_id: pkg.approval_id };
      }
      ```
    Step 4: Implement lib/markos/activation/milestones.ts (CRUD with UNIQUE(definition_id, step) DB-enforced).
    Step 5: Implement lib/markos/pql/contracts.ts:
      - PQL_STATUSES = ['not_ready','warming','pql','hot_pql'] as const
      - PQL_RECOMMENDED_ACTIONS = ['in_app_upgrade_prompt','targeted_email_sequence','sales_outreach','self_serve_offer','feature_unlock','continue_monitoring'] as const
      - zod schema PqlScoreSchema mirroring migration 102 columns
    Step 6: Implement lib/markos/pql/scorer.ts:
      ```typescript
      export async function computePqlScore({ tenantId, userId, accountId }, supabase): Promise<{ score_id: string; score: number; pql_status: string; recommended_action: string }> {
        // 1. Read activation definition for tenant
        const { data: def } = await supabase.from('activation_definitions').select('*').eq('tenant_id', tenantId).single();
        // 2. Read product usage signals from P216 saas_health_scores
        const { data: health } = await supabase.from('saas_health_scores').select('*').eq('tenant_id', tenantId).eq('account_id', accountId).single();
        // 3. Compute signals (usage = highest weight per doc 17; intent = medium; fit = contextual)
        const signals = { hit_usage_limit: health?.usage_pct >= 0.95, high_frequency_user: health?.daily_active_days_7 >= 5, feature_breadth_pct: health?.feature_breadth_pct ?? 0, /* ... */ };
        // 4. Score = sum(weighted_signals * 100); clamp 0-100
        const score = Math.min(100, Math.max(0, computeWeightedScore(signals)));
        // 5. pql_status from score thresholds (configurable)
        const pql_status = score >= 80 ? 'hot_pql' : score >= 60 ? 'pql' : score >= 30 ? 'warming' : 'not_ready';
        // 6. recommended_action by status
        const recommended_action = pql_status === 'hot_pql' ? 'in_app_upgrade_prompt' : pql_status === 'pql' ? 'targeted_email_sequence' : 'continue_monitoring';
        // 7. Create task on pql/hot_pql transition (SG-09)
        let task_created_id = null;
        if (pql_status === 'pql' || pql_status === 'hot_pql') {
          task_created_id = await createPqlTask({ tenantId, userId, accountId, pql_status, score });
        }
        // 8. INSERT pql_scores row with all signals + task_created_id
        const { data, error } = await supabase.from('pql_scores').insert({ tenant_id: tenantId, definition_id: def?.definition_id, user_id: userId, account_id: accountId, score, pql_status, ...signals, recommended_action, task_created_id }).select('score_id').single();
        if (error) throw error;
        return { score_id: data.score_id, score, pql_status, recommended_action };
      }
      ```
    Step 7: CommonJS twins + index.cjs for both lib/markos/activation/ and lib/markos/pql/.
    Step 8: Write 2 contract YAMLs F-240 + F-241 per RESEARCH §6 Domain 2 API surface table.
    Step 9: Update contracts/flow-registry.json appending F-240/F-241.
    Step 10: Run all tests (TDD GREEN). `npm run openapi:build`.
    Step 11: Update 218-VALIDATION.md per-task map for Plan 02 rows.
  </action>
  <acceptance_criteria>
    - `npm test -- test/growth-218/activation/ test/growth-218/pql/ test/growth-218/openapi/parity-activation-pql.test.js test/growth-218/rls/activation-pql-cross-tenant-denial.test.js` all green
    - `grep "F-240\|F-241" contracts/flow-registry.json | wc -l` returns 2
    - `grep -rn "createApprovalPackage\|requireSupabaseAuth\|lookupPlugin\|requireTenantContext\|app/(growth)\|app/(plg)\|route.ts\|vitest" lib/markos/activation/ lib/markos/pql/ | wc -l` returns 0
    - `npm run openapi:build` completes; contracts/openapi.json contains paths /v1/growth/activation-definitions + /v1/growth/pql-scores
    - `node -e "console.log(typeof require('./lib/markos/pql/index.cjs').computePqlScore)"` outputs `function`
    - `node -e "console.log(typeof require('./lib/markos/activation/index.cjs').requestThresholdChange)"` outputs `function`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser → /v1/growth/activation-definitions | Untrusted; threshold mutations approval-gated |
| PQL scorer cron → pql_scores INSERT | High-trust path; DB-trigger PQL_SCORE_REQUIRES_EVIDENCE blocks bare pql/hot_pql transitions |
| Service-role direct INSERT on pql_scores | Defense-in-depth via DB-trigger evidence check |
| P216 saas_health_scores → P218 PQL scorer reads | Consume-only; no .insert/.update/.delete calls allowed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-218-02-01 | Tampering | pql_scores insert with pql/hot_pql + no signals | mitigate | DB-trigger fn_pql_score_requires_evidence |
| T-218-02-02 | Information Disclosure | Cross-tenant PQL score leakage | mitigate | RLS on pql_scores tenant_id |
| T-218-02-03 | Tampering | activation_definitions threshold change without approval | mitigate | App-layer guard + buildApprovalPackage with kind='activation_threshold_change' |
| T-218-02-04 | Repudiation | Operator denies approving threshold change | mitigate | buildApprovalPackage approval row + audit hash chain |
| T-218-02-05 | Information Disclosure | PQL signals leak company-size or industry data cross-tenant | mitigate | RLS isolation; signals stored in tenant-scoped pql_scores rows only |
| T-218-02-06 | Elevation of Privilege | Operator manipulates score directly without scorer | mitigate | service-role-only INSERT path; DB-trigger evidence check still enforced |
| T-218-02-07 | DoS | Scorer runs unbounded query against saas_health_scores | mitigate | LIMIT 1 per (tenant, account) lookup; daily cron rate-limited |
| T-218-02-08 | Tampering | feature_breadth_pct manipulated to bypass evidence check | accept | Operator-configurable threshold; PQL is operator-configured per-tenant |
</threat_model>

<verification>
- `npm test -- test/growth-218/activation/ test/growth-218/pql/ test/growth-218/rls/activation-pql-cross-tenant-denial.test.js test/growth-218/openapi/parity-activation-pql.test.js` all green
- `node scripts/preconditions/218-02-check-upstream.cjs` exits 0 with green env
- `psql -c "SELECT count(*) FROM information_schema.tables WHERE table_name IN ('activation_definitions','activation_milestones','pql_scores')"` returns 3
- `psql -c "SELECT count(*) FROM pg_trigger WHERE tgname = 'trg_pql_score_requires_evidence'"` returns 1
- `grep -E "F-24[01]" contracts/flow-registry.json | wc -l` returns 2
- `grep -rn "createApprovalPackage\|requireSupabaseAuth\|lookupPlugin\|requireTenantContext\|public/openapi.json\|app/(growth)\|api/v1/.*/route.ts\|vitest\|.test.ts" lib/markos/activation/ lib/markos/pql/ scripts/preconditions/218-02-check-upstream.cjs | wc -l` returns 0
</verification>

<success_criteria>
- All 3 tasks complete with passing tests; 25+ assertions green
- 1 SOR migration (102) ships with 3 tables + 1 DB-trigger + RLS on all 3
- 9 lib files across lib/markos/activation/ + lib/markos/pql/ ship with 0 architecture-lock violations
- 2 F-ID contracts F-240/F-241 + flow-registry update
- PQL scorer reads P216 saas_health_scores (consume-only) + writes pql_scores + creates task on pql/hot_pql transition (SG-09)
- DB-trigger PQL_SCORE_REQUIRES_EVIDENCE locks pql/hot_pql transition behind explainable signals
- 218-VALIDATION.md per-task map updated for Plan 02 rows
</success_criteria>

<output>
After completion, create `.planning/phases/218-saas-growth-profile-plg-inapp-experimentation/218-02-SUMMARY.md` documenting:
- Migration slot claimed: 102
- F-IDs claimed: F-240, F-241
- Tables created: activation_definitions, activation_milestones, pql_scores
- DB-triggers created: trg_pql_score_requires_evidence
- PQL scorer ships at lib/markos/pql/scorer.ts (consumes P216 health_scores)
- Threshold-change approval kind 'activation_threshold_change'
</output>
