---
phase: 213.4
plan: 05
type: execute
wave: 1
depends_on: ["213.4-04"]
files_modified:
  - app/(markos)/operations/tasks/task-ui.module.css
  - app/(markos)/operations/tasks/page.tsx
  - app/(markos)/operations/tasks/approval-gate.tsx
  - app/(markos)/operations/tasks/evidence-panel.tsx
  - app/(markos)/operations/tasks/step-runner.tsx
  - app/(markos)/operations/tasks/task-graph.tsx
  - app/(markos)/operations/tasks/task-store.tsx
  - app/(markos)/operations/tasks/tasks.stories.tsx
autonomous: true
requirements:
  - OT-1
  - OT-2
  - OT-3
  - OT-4
  - OT-5
  - OT-6
  - OT-7
  - OT-8
  - OT-9
  - OT-10
  - OT-11
  - OT-12
must_haves:
  truths:
    - "app/(markos)/operations/tasks/task-ui.module.css contains zero inline hex (whitelist 1px/2px/4px/8px), zero off-grid spacing, zero Sora/Space Grotesk, zero radial-gradient, zero linear-gradient, zero box-shadow, zero `transform: translate*` (translateY hover-jiggle eliminated), zero teal/blue/slate legacy hex (#0d9488/#0f766e/#1d4ed8/#38bdf8/#bfdbfe/#dbeafe/#0f172a/#1e293b/#334155/#475569/#fef3c7/#fee2e2/#dcfce7 etc.)"
    - "task-ui.module.css contains an explicit @media (prefers-reduced-motion: reduce) block on .stepCard transitions per X-3 (CONTEXT D-24)"
    - "app/(markos)/operations/tasks/page.tsx contains zero Tailwind inline hex utilities (no `bg-[#`, `text-[#`, `border-gray-`); page wrapper uses CSS Module token-only classes per OT-9"
    - "5 sub-components (approval-gate.tsx, evidence-panel.tsx, step-runner.tsx, task-graph.tsx, task-store.tsx) updated with className-ONLY changes — DOM tree, prop interfaces, server/client boundaries UNCHANGED per CONTEXT D-15 + D-21 (Phase 207 AgentRun v2 substrate preserved)"
    - "task-graph.tsx STATE_BADGE_CONFIG composes .c-badge--{info,warning,success,error} (queued/approved→info, executing→warning, completed→success, failed→error) with bracketed-glyph prefixes ([–]/[ok]/[•]/[ok]/[err])"
    - "step-runner.tsx helper banners compose .c-notice c-notice--{info,warning,error} (helperCard/helperWarning/helperError replaced with primitive); approval-gate.tsx form composes .c-input + .c-field + .c-field__label + .c-button--primary (Approve) + .c-button--destructive (Reject) + .c-modal (reject confirm); evidence-panel.tsx uses .c-card + .c-chip-protocol for IDs"
    - "tasks.stories.tsx extended with ≥4 named state stories (QueuedTask / ExecutingTask / CompletedTask / FailedTask) plus ApprovalRequired (5 total) per UI-SPEC OT-12"
    - "Banned-lexicon scan = 0 across all 6 modified .tsx files"
    - "DOM tree + prop interface UNCHANGED on every sub-component (D-05, D-15, D-21); Phase 207 AgentRun v2 server/client boundaries preserved verbatim"
    - "npm run build exits 0; npm run build-storybook exits 0; npm test exits 0 (Phase 207 substrate test/operations + test/operations/tasks if exist stay green)"
  artifacts:
    - path: "app/(markos)/operations/tasks/task-ui.module.css"
      provides: "Token-only operations/tasks composite layout shell — 3-region grid, .stepCard layout modifier, .stepIndex token-only, prefers-reduced-motion block; primitives JSX-side"
      min_lines: 60
      contains: "var(--color-surface)"
    - path: "app/(markos)/operations/tasks/page.tsx"
      provides: "Updated wrapper with CSS Module token classes (no Tailwind inline hex per OT-9)"
      contains: "task-ui.module"
    - path: "app/(markos)/operations/tasks/task-graph.tsx"
      provides: "Updated STATE_BADGE_CONFIG composing .c-badge--{*} primitives with bracketed glyphs; SVG nodes preserved verbatim"
      contains: "c-badge"
    - path: "app/(markos)/operations/tasks/step-runner.tsx"
      provides: "Updated helper banners → .c-notice; status card → .c-card; step action buttons → .c-button{,--primary,--secondary,--destructive}; abort-run → .c-modal"
      contains: "c-notice"
    - path: "app/(markos)/operations/tasks/approval-gate.tsx"
      provides: "Updated form → .c-input + .c-field; Approve → .c-button--primary; Reject → .c-button--destructive + .c-modal confirm"
      contains: "c-input"
    - path: "app/(markos)/operations/tasks/evidence-panel.tsx"
      provides: "Updated entries → .c-card; IDs → .c-chip-protocol; Tailwind hex utilities eliminated"
      contains: "c-chip-protocol"
    - path: "app/(markos)/operations/tasks/task-store.tsx"
      provides: "Token-cited text colors only (state-management module, minimal UI surface)"
      contains: "var(--color-on-surface"
    - path: "app/(markos)/operations/tasks/tasks.stories.tsx"
      provides: "Storybook 8 CSF3 ≥4 named state exports (QueuedTask / ExecutingTask / CompletedTask / FailedTask); ApprovalRequired recommended (5 total)"
      exports: ["default", "QueuedTask", "ExecutingTask", "CompletedTask", "FailedTask"]
  key_links:
    - from: "app/(markos)/operations/tasks/step-runner.tsx"
      to: "styles/components.css .c-notice c-notice--{info,warning,error} primitive"
      via: "className composition for step-state helper banners per OT-6"
      pattern: "c-notice\\s+c-notice--(info|warning|error)"
    - from: "app/(markos)/operations/tasks/task-graph.tsx"
      to: "styles/components.css .c-badge--{info,warning,success,error} primitive"
      via: "STATE_BADGE_CONFIG mapping per OT-4"
      pattern: "c-badge\\s+c-badge--(info|warning|success|error)"
    - from: "app/(markos)/operations/tasks/approval-gate.tsx"
      to: ".c-input + .c-field form primitives + .c-modal reject confirm"
      via: "className composition per OT-10"
      pattern: "c-input|c-field|c-modal"
---

<objective>
LARGEST surface in the wave. Redesign the `operations/tasks` composite (`task-ui.module.css` 525 LOC, 79 inline hex; `page.tsx` 84 LOC with Tailwind inline hex; 5 sub-components 1300+ LOC; existing `tasks.stories.tsx`) into the canonical DESIGN.md v1.1.0 visual contract per `213.4-UI-SPEC.md` Plan 213.4-05 (12 ACs: OT-1..OT-12).

**Sub-components are className-update-only per D-15 + D-21.** No new sub-component extractions. DOM tree, prop interfaces, server/client boundaries (Phase 207 AgentRun v2 substrate) preserved verbatim. Inline SVG nodes in `task-graph.tsx` preserved verbatim — only wrapper className changes.

Compose `.c-card` (storyHero, panel, stepCard via local layout modifier, statusCard sub-section, evidence entries), `.c-button{,--primary,--secondary,--destructive}` (Execute/Continue/Mark complete + Retry + Abort/Fail/Reject), `.c-notice c-notice--{info,warning,error}` (helper banners — info on executing, warning on attention, error on failed/abort context), `.c-badge--{info,warning,success,error}` (5 step states via task-graph STATE_BADGE_CONFIG), `.c-chip` + `.c-chip--{warning,error}` (meta chips), `.c-chip-protocol` (task/run/tenant IDs), `.c-input` + `.c-field` + `.c-field__{label,help,error}` (approval-gate form), `.c-modal + .c-backdrop` (abort-run + reject confirms), `.t-label-caps` (eyebrows, statusLabel, sectionHeading). Eliminate gradient page background, `.storyHero` gradient fill, `.stepCardCurrent` blue gradient, `.statusCard` blue gradient, drop shadows, `transform: translateY(-1px)` hover-jiggle. Add explicit `@media (prefers-reduced-motion: reduce)` block on `.stepCard` transitions per X-3.

Purpose: Close UI-SPEC AC OT-1 (zero hex + gradients + shadows + jiggle eliminated), OT-2 (`.storyHero`/`.panel` → `.c-card`), OT-3 (step cards via `.c-card` + state modifiers; current step border `--color-primary`; dimmed opacity 0.5), OT-4 (5 step-state badges → `.c-badge--{*}`), OT-5 (meta chips → `.c-chip{,--warning,--error}`; stepIndex token-only), OT-6 (helper banners → `.c-notice c-notice--{*}`), OT-7 (statusCard → `.c-card` sub-section), OT-8 (step action buttons → primitives + `.c-modal` abort confirm), OT-9 (page.tsx Tailwind hex eliminated), OT-10 (approval-gate form → `.c-input` + `.c-field` + button primitives), OT-11 (evidence-panel → `.c-card` + `.c-chip-protocol`), OT-12 (≥4 named stories).

Output: `task-ui.module.css` rewritten (525 → ~120 LOC); `page.tsx` updated (Tailwind hex → CSS Module token classes); 5 sub-components className-only update; `tasks.stories.tsx` extended with 4-5 named state stories.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/213.4-ui-canon-adoption-wave-4-admin-operations-status-404-workspace-to-design-md-tokens/213.4-CONTEXT.md
@.planning/phases/213.4-ui-canon-adoption-wave-4-admin-operations-status-404-workspace-to-design-md-tokens/213.4-UI-SPEC.md
@.planning/phases/213.4-ui-canon-adoption-wave-4-admin-operations-status-404-workspace-to-design-md-tokens/213.4-RESEARCH.md
@DESIGN.md
@CLAUDE.md
@app/tokens.css
@app/globals.css
@styles/components.css
@app/(markos)/operations/tasks/task-ui.module.css
@app/(markos)/operations/tasks/page.tsx
@app/(markos)/operations/tasks/approval-gate.tsx
@app/(markos)/operations/tasks/evidence-panel.tsx
@app/(markos)/operations/tasks/step-runner.tsx
@app/(markos)/operations/tasks/task-graph.tsx
@app/(markos)/operations/tasks/task-store.tsx
@app/(markos)/operations/tasks/tasks.stories.tsx
@.planning/phases/213.4-ui-canon-adoption-wave-4-admin-operations-status-404-workspace-to-design-md-tokens/213.4-04-PLAN.md

<interfaces>
<!-- Class-Rename Map (RESEARCH.md "Plan 213.4-05 — Operations: Tasks Composite", lines 192–246): -->
<!-- .page → bg var(--color-surface); min-height 100vh; padding var(--space-md) — eliminates dual gradients + #f6fafc + Space Grotesk -->
<!-- .layout → max-width 1380px; margin 0 auto; display grid; grid-template-columns 0.9fr 1.2fr 0.95fr; gap var(--space-md); align-items start -->
<!-- .storyHero → DELETE; compose .c-card (eliminates gradient fill 135deg + 28px radius + shadow + #d8e6ee border) -->
<!-- .panel, .panelSticky → .c-card + position:sticky local override -->
<!-- .stepList → display grid; gap var(--space-xs) -->
<!-- .stepCard → .c-card + local layout modifier (eliminates 22px radius, hover transform, hover shadow, gradient) -->
<!-- .stepCardCurrent → border-color var(--color-primary) (eliminates blue gradient #38bdf8) -->
<!-- .stepCardDimmed → opacity 0.5 (upgraded from 0.8) -->
<!-- .stepIndex → token-only: bg var(--color-surface-overlay); color var(--color-on-surface-muted); border-radius var(--radius-full); font-size var(--fs-label); padding var(--space-xxs) var(--space-xs) -->
<!-- .badge, .badge{Queued,Approved,Executing,Completed,Failed} → DELETE; compose .c-badge--{info,info,warning,success,error} JSX-side in task-graph.tsx STATE_BADGE_CONFIG -->
<!-- .metaChip{,Warning,Error} → DELETE; compose .c-chip{,--warning,--error} -->
<!-- .helperCard, .helperWarning, .helperError → DELETE; compose .c-notice c-notice--{info,warning,error} in step-runner.tsx -->
<!-- .statusCard, .statusHeader, .statusLabel, .statusText → DELETE; compose .c-card sub-section + <h4> + .t-label-caps + <p> body-md -->
<!-- .runnerStack, .fieldGrid, .buttonRow → display grid/flex layout; token gaps -->
<!-- .fieldLabel → .c-field__label primitive -->
<!-- .textarea, .input → .c-input primitive -->
<!-- .buttonPrimary, .buttonDanger, .buttonSecondary → .c-button c-button--{primary,destructive,secondary} -->
<!-- .completedNote → token-only: color var(--color-success); font-weight var(--fw-semibold); NO italic on dark surface -->
<!-- .sectionBlock → margin-bottom var(--space-md) -->
<!-- .sectionHeading → .t-label-caps -->

<!-- STATE_BADGE_CONFIG mapping (UI-SPEC OT-4 + Copywriting): -->
<!-- queued      → 'c-badge c-badge--info'    + glyph '[–]'  + label 'Queued' -->
<!-- approved    → 'c-badge c-badge--info'    + glyph '[ok]' + label 'Approved' -->
<!-- executing   → 'c-badge c-badge--warning' + glyph '[•]'  + label 'Executing' -->
<!-- completed   → 'c-badge c-badge--success' + glyph '[ok]' + label 'Completed' -->
<!-- failed      → 'c-badge c-badge--error'   + glyph '[err]'+ label 'Failed' -->

<!-- Helper banner mapping (UI-SPEC OT-6 + Copywriting): -->
<!-- step executing → '.c-notice c-notice--info'    + '[•] Step is executing. Wait for completion or use Abort run to stop.' -->
<!-- step warning  → '.c-notice c-notice--warning' (situational) -->
<!-- step failed   → '.c-notice c-notice--error'   + '[err] Step failed. Review the error output and retry or abort the run.' -->

<!-- Step action buttons (UI-SPEC OT-8 + Copywriting): -->
<!-- Execute step / Continue / Mark complete → .c-button c-button--primary -->
<!-- Retry          → .c-button c-button--secondary -->
<!-- Fail step / Abort run → .c-button c-button--destructive + .c-modal confirm ("Abort this run?" / "All executing steps stop. Evidence captured so far remains for audit.") -->

<!-- Approval-gate (UI-SPEC OT-10): -->
<!-- comment textarea → .c-input + .c-field + .c-field__label -->
<!-- Approve → .c-button c-button--primary -->
<!-- Reject  → .c-button c-button--destructive + .c-modal confirm ("Reject this step?" / "A rejection is logged as evidence and transitions the task to failed state.") -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite app/(markos)/operations/tasks/task-ui.module.css to token-only 3-region layout shell + .stepCard layout modifier + .stepIndex + .completedNote + prefers-reduced-motion block; eliminate ALL legacy bespoke classes (525 → ~120 LOC) (OT-1, OT-2, OT-3, OT-5, OT-7, X-3)</name>
  <read_first>
    - app/(markos)/operations/tasks/task-ui.module.css (FULL 525 LOC — build a class→component usage map first; use Grep across all 6 .tsx siblings to confirm which class is used by which component before deleting)
    - app/(markos)/operations/tasks/page.tsx (FULL 84 LOC — wrapper layout + Tailwind hex inline)
    - app/(markos)/operations/tasks/step-runner.tsx (FULL ~344 LOC — largest sub-component; helperCard/helperWarning/helperError/statusCard usage)
    - app/(markos)/operations/tasks/task-graph.tsx (FULL ~344 LOC — badge classes + SVG nodes)
    - app/(markos)/operations/tasks/approval-gate.tsx (FULL ~84 LOC — form classes)
    - app/(markos)/operations/tasks/evidence-panel.tsx (FULL ~176 LOC — evidence entry classes)
    - app/(markos)/operations/tasks/task-store.tsx (FULL ~183 LOC — text-color usages)
    - .planning/phases/213.4-.../213.4-RESEARCH.md "Class-Rename Map: Plan 213.4-05" (lines 192–246) — authoritative table
    - .planning/phases/213.4-.../213.4-RESEARCH.md "Risks + Pitfalls" (task-ui scope creep + Tailwind inline + transform jiggle in HOVER selector)
    - .planning/phases/213.4-.../213.4-UI-SPEC.md OT-1..OT-12 + Primitive reuse + Copywriting
    - .planning/phases/213.4-.../213.4-04-PLAN.md Task 1 (precedent — same shape, smaller scope)
    - app/tokens.css + app/globals.css + styles/components.css v1.1.0
  </read_first>
  <files>app/(markos)/operations/tasks/task-ui.module.css</files>
  <action>
**STEP 1 — Build the class→component usage map.** Before deleting ANY class, run `grep -rEn "styles\.[a-zA-Z]+" "app/(markos)/operations/tasks/" --include="*.tsx"` and confirm which classes are actually consumed JSX-side. Classes consumed by siblings will be replaced JSX-side in Tasks 2–7; classes used only as descendant selectors (e.g., `.stepCard .stepHeader`) collapse into the parent's primitive composition.

**STEP 2 — Replace `task-ui.module.css` ENTIRELY** with a token-only ~120 LOC shell:

```css
/* Phase 213.4 Plan-05 — Token-only operations/tasks composite layout shell.
 * 525 LOC → ~120 LOC. 79 hex eliminated.
 * Composes .c-card (storyHero, panel, stepCard layout modifier, statusCard, evidence),
 * .c-button{,--primary,--secondary,--destructive} (CTAs + abort/reject),
 * .c-notice c-notice--{info,warning,error} (helper banners — OT-6),
 * .c-badge--{info,warning,success,error} (5 step states — OT-4 in task-graph.tsx),
 * .c-chip{,--warning,--error} (meta chips — OT-5 in step-runner/task-graph),
 * .c-chip-protocol (task/run/tenant IDs in evidence-panel + step-runner),
 * .c-input + .c-field + .c-field__{label,help,error} (approval-gate form — OT-10),
 * .c-modal + .c-backdrop (abort-run / reject confirms — OT-8 + OT-10),
 * .t-label-caps (eyebrows, statusLabel, sectionHeading).
 * All composition lives JSX-side. */

.page {
  background: var(--color-surface);
  min-height: 100vh;
  padding: var(--space-md);
}

.layout {
  max-width: var(--w-container);
  margin: 0 auto;
  display: grid;
  grid-template-columns: 0.9fr 1.2fr 0.95fr;
  gap: var(--space-md);
  align-items: start;
}

@media (max-width: 1199.98px) {
  .layout {
    grid-template-columns: 1fr;
  }
}

.panelSticky {
  position: sticky;
  top: var(--space-md);
}

.stepList {
  display: grid;
  gap: var(--space-xs);
}

/* .stepCard composes .c-card JSX-side; this modifier carries layout-only concerns. */
.stepCard {
  display: grid;
  gap: var(--space-xs);
}

.stepCardCurrent {
  border-color: var(--color-primary);
}

.stepCardDimmed {
  opacity: 0.5;
}

.stepHeader {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: var(--space-xs);
}

.stepTitleRow {
  display: flex;
  align-items: baseline;
  gap: var(--space-xs);
}

.stepIndex {
  background: var(--color-surface-overlay);
  color: var(--color-on-surface-muted);
  border-radius: var(--radius-full);
  font-size: var(--fs-label);
  font-weight: var(--fw-semibold);
  padding: var(--space-xxs) var(--space-xs);
  letter-spacing: var(--ls-label);
}

.metaRow,
.auditRow {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-xs);
  margin-top: var(--space-xs);
}

.runnerStack {
  display: grid;
  gap: var(--space-md);
}

.fieldGrid {
  display: grid;
  gap: var(--space-sm);
}

.buttonRow {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-xs);
}

.completedNote {
  color: var(--color-success);
  font-weight: var(--fw-semibold);
}

.sectionBlock {
  margin-bottom: var(--space-md);
}

/* Reduced-motion handling — explicit local block per X-3 (CONTEXT D-24).
 * task-ui is the only module in the wave with .stepCard transitions, so it
 * MUST carry an explicit override. The global cascade in tokens.css already
 * collapses --duration-* to 0ms, but explicit redundancy ensures consistency
 * with any third-party transitions inherited via .c-card primitive. */
@media (prefers-reduced-motion: reduce) {
  .stepCard {
    transition: none;
  }
}
```

This eliminates EVERY legacy literal:
- `font-family: 'Space Grotesk'` → globals
- `.page` dual radial+linear gradients → flat `var(--color-surface)`
- `.storyHero` rule → DELETE (`.c-card` JSX-side; eliminates 135deg gradient, 28px, drop shadow, `#d8e6ee`)
- `.panel` rule → DELETE (`.c-card`; eliminates `#ffffff`, 26px, `0 16px 38px rgba()`)
- `.stepCard` rule → minimal layout modifier (gap only); eliminates 22px radius, `#ffffff`, `transition: transform`, hover `box-shadow`, hover `transform: translateY(-1px)` JIGGLE
- `.stepCardCurrent` rule → border-color only; eliminates blue gradient + `#38bdf8`
- `.eyebrow` / `.panelEyebrow` → DELETE (`.t-label-caps`)
- `.heroTitle` / `.panelTitle` / `.stepTitle` → DELETE (`<h2>` / `<h3>` / `<h4>` globals)
- `.heroText` / `.panelText` / `.stepDescription` → DELETE (`.t-lead` / `<p>` globals)
- `.badge` + 5 state variants → DELETE (`.c-badge--{*}` in task-graph.tsx)
- `.metaChip{,Warning,Error}` → DELETE (`.c-chip{,--warning,--error}`)
- `.helperCard{,Warning,Error}` → DELETE (`.c-notice c-notice--{info,warning,error}` in step-runner.tsx)
- `.statusCard{,Header,Label,Text}` → DELETE (`.c-card` + `<h4>` + `.t-label-caps` + `<p>`; eliminates blue `eff6ff→f8fbff` gradient, `#bfdbfe`, 20px radius)
- `.fieldLabel` / `.textarea` / `.input` → DELETE (`.c-field__label` / `.c-input`)
- `.buttonPrimary` / `.buttonDanger` / `.buttonSecondary` → DELETE (`.c-button c-button--{primary,destructive,secondary}`)
- All off-grid 12/14/16/18/20/22/26/28/40/46px → token vars
- All blue/teal/slate/orange/red legacy hexes → tokens

DO NOT redeclare `@media (pointer: coarse)`. DO NOT add `font-family:` or `outline:`. Layout fractions are NOT spacing values (per UI-SPEC Spacing Scale exception #8).
  </action>
  <verify>
    <automated>! grep -nE '#[0-9a-fA-F]{3,8}' "app/(markos)/operations/tasks/task-ui.module.css" && ! grep -nE '(Sora|Space Grotesk|linear-gradient|radial-gradient|box-shadow|transform:\s*translate)' "app/(markos)/operations/tasks/task-ui.module.css" && ! grep -nE '#0d9488|#0f766e|#14b8a6|#1d4ed8|#38bdf8|#bfdbfe|#dbeafe|#0f172a|#1e293b|#334155|#475569|#fef3c7|#fee2e2|#dcfce7|#ffffff|#f6fafc|#f8fafc|#f0f9ff|#eff6ff' "app/(markos)/operations/tasks/task-ui.module.css" && ! grep -nE '@media[^{]*pointer[^{]*coarse' "app/(markos)/operations/tasks/task-ui.module.css" && grep -qE '@media[^{]*prefers-reduced-motion:\s*reduce' "app/(markos)/operations/tasks/task-ui.module.css" && grep -q "var(--color-surface)" "app/(markos)/operations/tasks/task-ui.module.css" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **OT-1 (zero hex + Sora + gradients + shadows + translate eliminated):** all greps return 0.
    - **X-3 (prefers-reduced-motion block present):** `grep -nE '@media[^{]*prefers-reduced-motion:\s*reduce' task-ui.module.css` returns ≥1 entry.
    - **No legacy bespoke classes (per RESEARCH map):** `grep -nE '\.storyHero\b|\.helperCard\b|\.helperWarning\b|\.helperError\b|\.statusCard\b|\.statusHeader\b|\.statusLabel\b|\.statusText\b|\.metaChip\b|\.metaChipWarning\b|\.metaChipError\b|\.badge\b|\.badgeQueued\b|\.badgeApproved\b|\.badgeExecuting\b|\.badgeCompleted\b|\.badgeFailed\b|\.fieldLabel\b|\.textarea\b|\.input\b|\.buttonPrimary\b|\.buttonDanger\b|\.buttonSecondary\b|\.heroTitle\b|\.panelTitle\b|\.stepTitle\b|\.stepDescription\b|\.heroText\b|\.panelText\b|\.eyebrow\b|\.panelEyebrow\b|\.sectionHeading\b' task-ui.module.css` returns 0.
    - **X-5 (no per-module pointer-coarse redeclaration):** 0.
    - **Layout containers retained:** `grep -cE '\.(page|layout|panelSticky|stepList|stepCard|stepCardCurrent|stepCardDimmed|stepHeader|stepTitleRow|stepIndex|metaRow|auditRow|runnerStack|fieldGrid|buttonRow|completedNote|sectionBlock)\s*\{' task-ui.module.css` ≥10.
  </acceptance_criteria>
  <done>`task-ui.module.css` rewritten 525 → ~120 LOC. Atomic commit: `feat(213.4-05): rewrite operations/tasks/task-ui.module.css to design-md tokens (OT-1, OT-2, OT-3, OT-5, OT-7, X-3)`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update app/(markos)/operations/tasks/page.tsx — convert Tailwind inline hex utilities (bg-[#f5f7fa], bg-white, border-gray-200, text-[#0f172a]) to CSS Module token classes from task-ui.module.css; preserve 3-region layout shape (OT-9)</name>
  <read_first>
    - app/(markos)/operations/tasks/page.tsx (FULL 84 LOC — locate every Tailwind hex utility)
    - app/(markos)/operations/tasks/task-ui.module.css (post-Task 1 — confirm available layout class names)
    - .planning/phases/213.4-.../213.4-UI-SPEC.md OT-9 + RESEARCH.md "Risks + Pitfalls > page.tsx Tailwind inline hex"
  </read_first>
  <files>app/(markos)/operations/tasks/page.tsx</files>
  <action>
**Replace Tailwind inline hex utilities with CSS Module token classes from `task-ui.module.css`.**

Find every occurrence of:
- `className="...bg-[#f5f7fa]..."` → use `styles.page` (token-only `var(--color-surface)`)
- `className="...bg-white..."` → use `styles.[panel-equivalent]` then compose `c-card` JSX-side OR use `.c-card` directly
- `className="...border-gray-200..."` → eliminated (composed via `.c-card` border)
- `className="...text-[#0f172a]..."` → remove (color inherited from globals)
- Any `bg-[#...]` / `text-[#...]` / `border-[#...]` Tailwind hex utility → eliminated entirely

Pattern (representative — actual file shape per executor inspection):

```tsx
// Before
<div className="min-h-screen bg-[#f5f7fa] text-[#0f172a]">
  <div className="grid grid-cols-3 gap-4">
    <aside className="bg-white border border-gray-200 rounded-2xl p-4">...</aside>
    <main className="bg-white border border-gray-200 rounded-2xl p-4">...</main>
    <aside className="bg-white border border-gray-200 rounded-2xl p-4">...</aside>
  </div>
</div>

// After
import styles from './task-ui.module.css';

<div className={styles.page}>
  <div className={styles.layout}>
    <aside className="c-card">...</aside>
    <main className="c-card">...</main>
    <aside className="c-card">...</aside>
  </div>
</div>
```

**KEY rules (per OT-9):**
- ZERO `bg-[#...]` / `text-[#...]` / `border-[#...]` / `border-gray-...` Tailwind hex utilities remain anywhere in the file.
- The 3-region layout shape (left task list / center step runner / right evidence) is PRESERVED — only the styling source changes.
- DOM tree + prop interfaces UNCHANGED. `'use client'` directive preserved verbatim.
- Phase 207 substrate wiring (any `await` calls, server actions, sub-component imports) UNTOUCHED.

After edit: `npm run build` MUST exit 0.
  </action>
  <verify>
    <automated>! grep -qE 'bg-\[#|text-\[#|border-\[#|border-gray-' "app/(markos)/operations/tasks/page.tsx" && grep -qE 'task-ui' "app/(markos)/operations/tasks/page.tsx" && npm run build && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **OT-9 (no Tailwind inline hex):** `grep -nE 'bg-\[#|text-\[#|border-\[#|border-gray-' page.tsx` returns 0.
    - **CSS Module imported:** `grep -c "task-ui" page.tsx` ≥1.
    - **Build green.**
  </acceptance_criteria>
  <done>`page.tsx` Tailwind hex eliminated. Atomic commit: `feat(213.4-05): convert operations/tasks/page.tsx Tailwind hex to CSS Module token classes (OT-9)`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update task-graph.tsx (STATE_BADGE_CONFIG → .c-badge--{*} primitives with bracketed glyphs) + step-runner.tsx (helper banners → .c-notice; statusCard → .c-card; step buttons → primitives + abort .c-modal) — className-only per D-15 + D-21 (OT-4, OT-6, OT-7, OT-8)</name>
  <read_first>
    - app/(markos)/operations/tasks/task-graph.tsx (FULL — locate STATE_BADGE_CONFIG; preserve SVG nodes verbatim)
    - app/(markos)/operations/tasks/step-runner.tsx (FULL ~200 LOC — locate helperCard/helperWarning/helperError, statusCard, step action buttons, abort-run handler)
    - app/(markos)/operations/tasks/task-ui.module.css (post-Task 1 — confirm available layout classes for retained wrappers)
    - .planning/phases/213.4-.../213.4-UI-SPEC.md OT-4 / OT-6 / OT-7 / OT-8 + Copywriting (Abort run modal copy)
    - .planning/phases/213.4-.../213.4-RESEARCH.md "Sub-component Posture: operations/tasks" + Notice/Banner Audit rows 7–9 + STATE_BADGE_CONFIG mapping
    - styles/components.css v1.1.0 (.c-badge, .c-notice, .c-card, .c-button, .c-modal primitives)
  </read_first>
  <files>app/(markos)/operations/tasks/task-graph.tsx, app/(markos)/operations/tasks/step-runner.tsx</files>
  <action>
**Edit A — `task-graph.tsx` STATE_BADGE_CONFIG (OT-4):**

Replace the existing badge-class config with primitive-class composition:

```tsx
const STATE_BADGE_CONFIG = {
  queued:    { cls: 'c-badge c-badge--info',    glyph: '[–]',  label: 'Queued' },
  approved:  { cls: 'c-badge c-badge--info',    glyph: '[ok]', label: 'Approved' },
  executing: { cls: 'c-badge c-badge--warning', glyph: '[•]',  label: 'Executing' },
  completed: { cls: 'c-badge c-badge--success', glyph: '[ok]', label: 'Completed' },
  failed:    { cls: 'c-badge c-badge--error',   glyph: '[err]', label: 'Failed' },
} as const;

// Render usage:
<span className={STATE_BADGE_CONFIG[state].cls}>
  {STATE_BADGE_CONFIG[state].glyph} {STATE_BADGE_CONFIG[state].label}
</span>
```

KEY: SVG nodes (DAG visualization) preserved verbatim. Only wrapper className references the new STATE_BADGE_CONFIG output. Server/client boundary unchanged.

**Edit B — `step-runner.tsx` helper banners (OT-6):**

Replace `<div className={styles.helperCard}>...</div>` (and `helperWarning` / `helperError` siblings) with state-driven primitive composition:

```tsx
// info / executing context:
<div className="c-notice c-notice--info" role="status">
  <strong>[•]</strong>{" "}Step is executing. Wait for completion or use Abort run to stop.
</div>

// failed context:
<div className="c-notice c-notice--error" role="status">
  <strong>[err]</strong>{" "}Step failed. Review the error output and retry or abort the run.
</div>

// situational warning:
<div className="c-notice c-notice--warning" role="status">
  <strong>[warn]</strong>{" "}{...situational copy preserved verbatim from existing register...}
</div>
```

**Edit C — `step-runner.tsx` statusCard → `.c-card` sub-section (OT-7):**

Replace `<div className={styles.statusCard}>` block with:

```tsx
<div className="c-card">
  <span className="t-label-caps">{statusLabel}</span>
  <h4>{statusTitle}</h4>
  <p>{statusBody}</p>
</div>
```

(Copy keys preserved from existing component register; only className shape changes.)

**Edit D — `step-runner.tsx` step action buttons (OT-8):**

Replace `<button className={styles.buttonPrimary}>Execute step</button>` etc. with:

```tsx
<button type="button" className="c-button c-button--primary" onClick={onExecute}>Execute step</button>
<button type="button" className="c-button c-button--primary" onClick={onContinue}>Continue</button>
<button type="button" className="c-button c-button--primary" onClick={onMarkComplete}>Mark complete</button>
<button type="button" className="c-button c-button--secondary" onClick={onRetry}>Retry</button>
<button type="button" className="c-button c-button--destructive" onClick={openAbortModal}>Abort run</button>
```

Plus the abort-run confirm modal:

```tsx
{showAbortModal && (
  <>
    <div className="c-backdrop" onClick={cancelAbort} />
    <div className="c-modal" role="dialog" aria-modal="true" aria-labelledby="abort-title">
      <h3 id="abort-title">Abort this run?</h3>
      <p>All executing steps stop. Evidence captured so far remains for audit.</p>
      <div className={styles.buttonRow}>
        <button type="button" className="c-button c-button--secondary" onClick={cancelAbort}>Cancel</button>
        <button type="button" className="c-button c-button--destructive" onClick={confirmAbort}>Abort run</button>
      </div>
    </div>
  </>
)}
```

**KEY for both files:** className-ONLY changes (D-15). Server/client directives, prop interfaces, business logic UNTOUCHED. SVG nodes in task-graph.tsx PRESERVED verbatim. Banned-lexicon scan = 0.
  </action>
  <verify>
    <automated>grep -qE 'c-badge c-badge--(info|warning|success|error)' "app/(markos)/operations/tasks/task-graph.tsx" && grep -qE '\[(–|ok|•|err)\]' "app/(markos)/operations/tasks/task-graph.tsx" && grep -qE 'c-notice c-notice--(info|warning|error)' "app/(markos)/operations/tasks/step-runner.tsx" && grep -qE 'c-button c-button--(primary|secondary|destructive)' "app/(markos)/operations/tasks/step-runner.tsx" && grep -qE 'c-modal' "app/(markos)/operations/tasks/step-runner.tsx" && grep -qE 'c-backdrop' "app/(markos)/operations/tasks/step-runner.tsx" && ! grep -qE 'styles\.(badge\b|badgeQueued|badgeApproved|badgeExecuting|badgeCompleted|badgeFailed|helperCard|helperWarning|helperError|statusCard|statusHeader|statusLabel|statusText|buttonPrimary|buttonDanger|buttonSecondary)' "app/(markos)/operations/tasks/task-graph.tsx" "app/(markos)/operations/tasks/step-runner.tsx" && ! grep -inE 'Please|synergy|leverage|empower|unlock|revolutionize|supercharge|holistic|seamless|cutting-edge|innovative|game-changer|next-generation|world-class|best-in-class|reimagine|disrupt' "app/(markos)/operations/tasks/task-graph.tsx" "app/(markos)/operations/tasks/step-runner.tsx" && npm run build && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **OT-4 (.c-badge primitives + glyphs):** `grep -cE 'c-badge c-badge--(info|warning|success|error)' task-graph.tsx` ≥4.
    - **OT-6 (.c-notice helper banners):** `grep -cE 'c-notice c-notice--(info|warning|error)' step-runner.tsx` ≥2.
    - **OT-7 (.c-card statusCard):** `grep -cE '"c-card"' step-runner.tsx` ≥1.
    - **OT-8 (button primitives + abort modal):** `grep -cE 'c-button c-button--(primary|secondary|destructive)' step-runner.tsx` ≥3; `grep -cE 'c-modal' step-runner.tsx` ≥1; `grep -cE 'c-backdrop' step-runner.tsx` ≥1.
    - **No legacy class refs in either file:** 0.
    - **Banned-lexicon = 0; Build green.**
  </acceptance_criteria>
  <done>`task-graph.tsx` + `step-runner.tsx` updated. Atomic commit: `feat(213.4-05): compose primitives in task-graph.tsx + step-runner.tsx — badges + helper notices + status card + abort modal (OT-4, OT-6, OT-7, OT-8)`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Update approval-gate.tsx (.c-input + .c-field + .c-button--primary Approve + .c-button--destructive Reject + .c-modal reject confirm) + evidence-panel.tsx (.c-card entries + .c-chip-protocol IDs) + task-store.tsx (token-cited text colors only) — className-only per D-15 + D-21 (OT-10, OT-11)</name>
  <read_first>
    - app/(markos)/operations/tasks/approval-gate.tsx (FULL ~84 LOC)
    - app/(markos)/operations/tasks/evidence-panel.tsx (FULL ~176 LOC — locate Tailwind hex utilities + ID display)
    - app/(markos)/operations/tasks/task-store.tsx (FULL ~183 LOC — locate any text-color classes; minimal UI surface)
    - .planning/phases/213.4-.../213.4-UI-SPEC.md OT-10 (approval-gate) + OT-11 (evidence-panel) + Copywriting (Reject modal copy)
    - .planning/phases/213.4-.../213.4-RESEARCH.md "Sub-component Posture: operations/tasks"
    - styles/components.css v1.1.0 (.c-input, .c-field, .c-field__{label,help,error}, .c-chip-protocol, .c-card, .c-modal primitives)
  </read_first>
  <files>app/(markos)/operations/tasks/approval-gate.tsx, app/(markos)/operations/tasks/evidence-panel.tsx, app/(markos)/operations/tasks/task-store.tsx</files>
  <action>
**Edit A — `approval-gate.tsx` form + buttons + reject modal (OT-10):**

```tsx
<form onSubmit={onApproveSubmit}>
  <div className="c-field">
    <label htmlFor="approval-comment" className="c-field__label">Comment (optional)</label>
    <textarea
      id="approval-comment"
      className="c-input"
      value={comment}
      onChange={(e) => setComment(e.target.value)}
      rows={4}
    />
    {commentError && <span className="c-field__error">[err] {commentError}</span>}
  </div>
  <div style={{ display: 'flex', gap: 'var(--space-xs)' }}>
    <button type="submit" className="c-button c-button--primary">Approve</button>
    <button type="button" className="c-button c-button--destructive" onClick={openRejectModal}>Reject</button>
  </div>
</form>

{showRejectModal && (
  <>
    <div className="c-backdrop" onClick={cancelReject} />
    <div className="c-modal" role="dialog" aria-modal="true" aria-labelledby="reject-step-title">
      <h3 id="reject-step-title">Reject this step?</h3>
      <p>A rejection is logged as evidence and transitions the task to failed state.</p>
      <div style={{ display: 'flex', gap: 'var(--space-xs)' }}>
        <button type="button" className="c-button c-button--secondary" onClick={cancelReject}>Cancel</button>
        <button type="button" className="c-button c-button--destructive" onClick={confirmReject}>Reject step</button>
      </div>
    </div>
  </>
)}
```

**Edit B — `evidence-panel.tsx` (OT-11):**

Replace each evidence entry's `<div className="bg-white border border-gray-200 ...">` with `<article className="c-card">`. Wrap evidence identifiers (run IDs, task IDs, tenant IDs) in `<span className="c-chip-protocol">[run_xxx]</span>` (or whichever protocol-id pattern exists). Eliminate ALL Tailwind hex utilities (`bg-[#`, `text-[#`, `border-gray-`) — exactly per OT-11 verification grep.

**Edit C — `task-store.tsx`:**

This module is state-management with minimal UI surface (per RESEARCH "Open Question" — likely zero className changes). Confirm by grepping for `className=` in the file. If any className references hex literals or Tailwind utilities, swap to token-cited values via `style={{ color: 'var(--color-on-surface-muted)' }}` or remove. Document explicitly in the commit message if zero changes were needed.

**KEY for all 3 files:** className-ONLY changes (D-15). Server/client directives + prop interfaces + business logic UNTOUCHED. Banned-lexicon scan = 0.
  </action>
  <verify>
    <automated>grep -qE 'c-input' "app/(markos)/operations/tasks/approval-gate.tsx" && grep -qE 'c-field' "app/(markos)/operations/tasks/approval-gate.tsx" && grep -qE 'c-button c-button--primary' "app/(markos)/operations/tasks/approval-gate.tsx" && grep -qE 'c-button c-button--destructive' "app/(markos)/operations/tasks/approval-gate.tsx" && grep -qE 'c-modal' "app/(markos)/operations/tasks/approval-gate.tsx" && grep -qE 'c-card' "app/(markos)/operations/tasks/evidence-panel.tsx" && grep -qE 'c-chip-protocol' "app/(markos)/operations/tasks/evidence-panel.tsx" && ! grep -qE 'bg-\[#|text-\[#|border-\[#|border-gray-|bg-white' "app/(markos)/operations/tasks/evidence-panel.tsx" && ! grep -inE 'Please|synergy|leverage|empower|unlock|revolutionize|supercharge|holistic|seamless|cutting-edge|innovative|game-changer|next-generation|world-class|best-in-class|reimagine|disrupt' "app/(markos)/operations/tasks/approval-gate.tsx" "app/(markos)/operations/tasks/evidence-panel.tsx" "app/(markos)/operations/tasks/task-store.tsx" && npm run build && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **OT-10 (approval-gate primitives):** `grep -cE 'c-input' approval-gate.tsx` ≥1; `grep -cE 'c-field' approval-gate.tsx` ≥1; `grep -cE 'c-button c-button--primary' approval-gate.tsx` ≥1; `grep -cE 'c-button c-button--destructive' approval-gate.tsx` ≥1; `grep -cE 'c-modal' approval-gate.tsx` ≥1.
    - **OT-11 (evidence-panel primitives + zero Tailwind hex):** `grep -cE 'c-card' evidence-panel.tsx` ≥1; `grep -cE 'c-chip-protocol' evidence-panel.tsx` ≥1; `grep -nE 'bg-\[#|text-\[#|border-\[#|border-gray-|bg-white' evidence-panel.tsx` returns 0.
    - **Banned-lexicon = 0; Build green.**
  </acceptance_criteria>
  <done>3 sub-components updated className-only. Atomic commit: `feat(213.4-05): compose primitives in approval-gate.tsx + evidence-panel.tsx + task-store.tsx (OT-10, OT-11)`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Extend tasks.stories.tsx with ≥4 named state stories (QueuedTask / ExecutingTask / CompletedTask / FailedTask) + ApprovalRequired (5 total recommended) per UI-SPEC OT-12 + run all verification gates including Phase 207 substrate tests</name>
  <read_first>
    - app/(markos)/operations/tasks/tasks.stories.tsx (FULL — confirm existing meta default; extend, don't replace)
    - app/(markos)/operations/tasks/page.tsx (post-Task 2 — confirm prop interface + sub-component imports)
    - .planning/phases/213.4-.../213.4-UI-SPEC.md OT-12 + Storybook State Coverage table (lines 467–474)
    - .planning/phases/213.4-.../213.4-04-PLAN.md Task 3 (precedent)
  </read_first>
  <files>app/(markos)/operations/tasks/tasks.stories.tsx</files>
  <action>
**Extend with 4–5 named state stories per UI-SPEC OT-12:**

```tsx
import type { Meta, StoryObj } from '@storybook/react';
import OperationsTasksPage from './page';

const meta = {
  title: 'Operations/Tasks',
  component: OperationsTasksPage,
  parameters: { layout: 'fullscreen' },
} satisfies Meta<typeof OperationsTasksPage>;

export default meta;
type Story = StoryObj<typeof meta>;

export const QueuedTask: Story = {
  args: { /* mock task in queued state */ },
  parameters: { docs: { description: { story: 'Task in queued state — .c-badge--info [–] Queued, .c-button--primary Execute step.' } } },
};

export const ExecutingTask: Story = {
  args: { /* mock task in executing state */ },
  parameters: { docs: { description: { story: 'Task in executing state — .c-badge--warning [•] Executing, .c-notice c-notice--info helper banner ("[•] Step is executing..."), .c-button--destructive Abort run.' } } },
};

export const CompletedTask: Story = {
  args: { /* mock task fully completed */ },
  parameters: { docs: { description: { story: 'Task fully completed — .c-badge--success [ok] Completed, evidence-panel .c-card entries, .c-chip-protocol IDs.' } } },
};

export const FailedTask: Story = {
  args: { /* mock task with failed step */ },
  parameters: { docs: { description: { story: 'Step failed — .c-badge--error [err] Failed, .c-notice c-notice--error helper ("[err] Step failed..."), .c-button--secondary Retry + .c-button--destructive Abort run.' } } },
};

export const ApprovalRequired: Story = {
  args: { /* mock task at approval gate */ },
  parameters: { docs: { description: { story: 'Approval gate rendered — .c-input + .c-field comment textarea, .c-button--primary Approve + .c-button--destructive Reject + .c-modal reject confirm.' } } },
};
```

KEY: 5 named exports `QueuedTask` / `ExecutingTask` / `CompletedTask` / `FailedTask` / `ApprovalRequired` matching UI-SPEC line 468–472 verbatim; `meta.title='Operations/Tasks'`; args mirror actual prop interface (executor inspects post-Task 2 file — task-store provides the state shape).

After edit: run **Phase 207 substrate tests** specifically + full `npm test` + `npm run build-storybook` — all MUST exit 0.
  </action>
  <verify>
    <automated>test -f "app/(markos)/operations/tasks/tasks.stories.tsx" && grep -cE '^export const (QueuedTask|ExecutingTask|CompletedTask|FailedTask|ApprovalRequired):' "app/(markos)/operations/tasks/tasks.stories.tsx" | awk '$1>=4 {exit 0} $1<4 {exit 1}' && grep -q "title: 'Operations/Tasks'" "app/(markos)/operations/tasks/tasks.stories.tsx" && grep -q 'export default meta' "app/(markos)/operations/tasks/tasks.stories.tsx" && npm run build-storybook && npm test && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **OT-12 (≥4 named state stories):** `grep -cE '^export const (QueuedTask|ExecutingTask|CompletedTask|FailedTask|ApprovalRequired):' tasks.stories.tsx` ≥4 (5 recommended).
    - **`meta.title='Operations/Tasks'`:** ≥1.
    - **Storybook build green.**
    - **`npm test` green:** Phase 207 substrate (test/operations + test/operations/tasks if exist) MUST stay green per D-21.
  </acceptance_criteria>
  <done>`tasks.stories.tsx` extended with 4–5 named state stories. Atomic commit: `feat(213.4-05): extend operations/tasks/tasks.stories.tsx with QueuedTask/ExecutingTask/CompletedTask/FailedTask/ApprovalRequired (OT-12)`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator→AgentRun v2 substrate | Phase 207 task-execution wiring crosses every sub-component boundary; D-21 mandates server/client preservation |
| approval gate→audit log | Reject/Approve emits Phase 207 audit-log events; preserved verbatim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213.4-05-01 | Tampering | Abort run / Reject step actions | mitigate | `.c-modal` confirm with explicit consequence copy ("All executing steps stop" / "logged as evidence and transitions to failed state"); server-side audit emit unchanged |
| T-213.4-05-02 | Elevation of Privilege | Phase 207 server/client boundaries | accept | D-15 + D-21 mandate className-only; no boundary changes |
| T-213.4-05-03 | Information Disclosure | step-state badges color-only signal | mitigate | OT-4 enforces bracketed glyph pairing on every state — color is never the only signal |
</threat_model>

<verification>
```bash
grep -nE '#[0-9a-fA-F]{3,8}|gradient|box-shadow|transform:\s*translate' "app/(markos)/operations/tasks/task-ui.module.css"  # 0
grep -nE 'prefers-reduced-motion:\s*reduce' "app/(markos)/operations/tasks/task-ui.module.css"  # ≥1
grep -nE 'bg-\[#|text-\[#|border-\[#|border-gray-' "app/(markos)/operations/tasks/page.tsx" "app/(markos)/operations/tasks/evidence-panel.tsx"  # 0
grep -cE 'c-badge c-badge--' "app/(markos)/operations/tasks/task-graph.tsx"  # ≥4
grep -cE 'c-notice c-notice--' "app/(markos)/operations/tasks/step-runner.tsx"  # ≥2
grep -cE '^export const (QueuedTask|ExecutingTask|CompletedTask|FailedTask|ApprovalRequired):' "app/(markos)/operations/tasks/tasks.stories.tsx"  # ≥4
npm run build && npm run build-storybook && npm test  # all 0
```
</verification>

<success_criteria>
UI-SPEC AC OT-1..OT-12 close. Phase 207 AgentRun v2 substrate preserved verbatim per D-21. 5 atomic commits.
</success_criteria>

<output>
Create `.planning/phases/213.4-.../213.4-05-SUMMARY.md`. Hand-off to Plan 213.4-06 (status/webhooks — public surface).
</output>
</content>
</invoke>
