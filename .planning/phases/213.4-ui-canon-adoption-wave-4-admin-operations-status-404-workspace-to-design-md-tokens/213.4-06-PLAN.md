---
phase: 213.4
plan: 06
type: execute
wave: 1
depends_on: ["213.4-05"]
files_modified:
  - app/(markos)/status/webhooks/page.module.css
  - app/(markos)/status/webhooks/page.tsx
  - app/(markos)/status/webhooks/page.stories.tsx
autonomous: true
requirements:
  - SW-1
  - SW-2
  - SW-3
  - SW-4
  - SW-5
  - SW-6
  - SW-7
must_haves:
  truths:
    - "app/(markos)/status/webhooks/page.module.css contains zero inline hex (whitelist 1px/2px/8px), zero off-grid spacing, zero Sora/Space Grotesk, zero gradients, zero box-shadow, zero transform-translate, zero teal #0d9488/#0f766e/#f0fdfa/#9a3412/#fef2f2/#fef3c7/#d97706/#dc2626 legacy state hex, zero `* { … !important }` reduced-motion wildcard"
    - "app/(markos)/status/webhooks/page.tsx composes .c-card on .contentCard with max-width var(--w-prose) override; .heroCard metric cards token-only with DLQ-alert variant (border --color-error); .statusLine → .c-notice c-notice--{success,warning,error} (data-status driven className not data-attribute selectors); .statusDot → .c-status-dot--live / default / --error; .footerLink uses var(--color-primary-text) (not raw --color-primary)"
    - "Phase 203 webhook GA wiring preserved verbatim per CONTEXT D-20: fetchStatus() endpoint /api/public/webhooks/status, classifyStatus() output values + statusCopy() register UNCHANGED. Status state register matches Phase 203 substrate verbatim: operational / retrying / elevated (3 states; do NOT introduce new states; do NOT rename)"
    - "Status copy strings match Phase 203 verbatim with bracketed-glyph prefix: '[ok] All systems operational.' / '[warn] Some deliveries are being retried.' / '[err] Elevated failure rate.'"
    - "app/(markos)/status/webhooks/page.stories.tsx is NEW file with ≥3 named state exports (Operational / Retrying / Elevated) per UI-SPEC SW-6; meta.title='Status/Webhooks'; uses mock snapshot prop (NOT live fetch)"
    - "Banned-lexicon scan = 0 in modified .tsx; zero exclamation; zero emoji; zero 'Please'"
    - "DOM tree + prop interface UNCHANGED (D-05); Phase 203 wiring tests in test/webhooks/ stay green per D-20"
    - "npm run build exits 0; npm run build-storybook exits 0; npm test exits 0"
  artifacts:
    - path: "app/(markos)/status/webhooks/page.module.css"
      provides: "Token-only public webhook status-page layout — centered card at --w-prose width, metric heroCard token-only with DLQ-alert variant, footerLink with --color-primary-text"
      min_lines: 35
      contains: "var(--color-surface)"
    - path: "app/(markos)/status/webhooks/page.tsx"
      provides: "Updated webhook status-page composing .c-card + .c-notice + .c-status-dot primitives; preserves Phase 203 fetchStatus() + classifyStatus() + copy register verbatim"
      contains: "c-notice c-notice--"
    - path: "app/(markos)/status/webhooks/page.stories.tsx"
      provides: "NEW Storybook 8 CSF3 file — ≥3 named state exports (Operational / Retrying / Elevated) with mock snapshot args"
      exports: ["default", "Operational", "Retrying", "Elevated"]
  key_links:
    - from: "app/(markos)/status/webhooks/page.tsx"
      to: "styles/components.css .c-notice c-notice--{success,warning,error} primitive"
      via: "classifyStatus() output 'operational'/'retrying'/'elevated' drives className composition"
      pattern: "c-notice\\s+c-notice--(success|warning|error)"
    - from: "app/(markos)/status/webhooks/page.tsx"
      to: "/api/public/webhooks/status (Phase 203 substrate)"
      via: "fetchStatus() call preserved verbatim"
      pattern: "api/public/webhooks/status"
    - from: "app/(markos)/status/webhooks/page.module.css"
      to: "app/tokens.css var(--*)"
      via: "var(--color-surface), var(--w-prose), var(--color-surface-overlay), var(--color-border), --color-error border on DLQ alert, --color-primary-text on footerLink"
      pattern: "var\\(--(color|space|fs|fw|w|h|radius|ls)-"
---

<objective>
Redesign `/status/webhooks` (`page.module.css` 179 LOC, 26 inline hex; `page.tsx` 140 LOC; NEW `page.stories.tsx`) — public-facing webhook delivery status page (Phase 203 substrate) — into the canonical DESIGN.md v1.1.0 visual contract per `213.4-UI-SPEC.md` Plan 213.4-06 (7 ACs: SW-1..SW-7). Compose `.c-card` (contentCard with `max-width: var(--w-prose)`), `.c-notice c-notice--{success,warning,error}` (statusLine — data-status driven by className not CSS attribute selectors per RESEARCH SW-4 pitfall), `.c-status-dot--{live,default,--error}` (per state). Eliminate `* { … !important }` reduced-motion wildcard (WCAG trap). `footerLink` uses `var(--color-primary-text)`. Metric heroCard tokens with DLQ-alert variant (border `--color-error`). NEW `page.stories.tsx` with mock `snapshot` prop (NOT live fetch — RESEARCH "Stories with useEffect/fetch" pitfall).

**Phase 203 wiring preservation is BLOCKING per D-20.** `fetchStatus()` endpoint `/api/public/webhooks/status` UNTOUCHED. `classifyStatus()` logic UNTOUCHED. `statusCopy()` register MUST match Phase 203 verbatim (3 states only: operational / retrying / elevated). Bracketed glyphs prepended to copy strings.

Purpose: Close UI-SPEC AC SW-1 (zero hex + Space Grotesk + reduced-motion wildcard eliminated), SW-2 (`.contentCard` → `.c-card` + `var(--w-prose)`), SW-3 (metric cards token-only with DLQ variant), SW-4 (`.statusLine` → `.c-notice c-notice--{*}` className-driven; `.statusDot` → `.c-status-dot` primitive), SW-5 (`.footerLink` → `--color-primary-text`), SW-6 (NEW `page.stories.tsx` ≥3 named exports), SW-7 (Phase 203 wiring preserved).

Output: `page.module.css` rewritten (179 → ~50 LOC); `page.tsx` updated (className-only — NOT logic); NEW `page.stories.tsx` with mock snapshot args.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/213.4-ui-canon-adoption-wave-4-admin-operations-status-404-workspace-to-design-md-tokens/213.4-CONTEXT.md
@.planning/phases/213.4-ui-canon-adoption-wave-4-admin-operations-status-404-workspace-to-design-md-tokens/213.4-UI-SPEC.md
@.planning/phases/213.4-ui-canon-adoption-wave-4-admin-operations-status-404-workspace-to-design-md-tokens/213.4-RESEARCH.md
@DESIGN.md
@CLAUDE.md
@app/tokens.css
@app/globals.css
@styles/components.css
@app/(markos)/status/webhooks/page.module.css
@app/(markos)/status/webhooks/page.tsx
@.planning/phases/213.4-ui-canon-adoption-wave-4-admin-operations-status-404-workspace-to-design-md-tokens/213.4-05-PLAN.md

<interfaces>
<!-- Class-Rename Map (RESEARCH.md "Plan 213.4-06 — Status: Webhooks", lines 250–278): -->
<!-- .page → min-height 100vh; padding var(--space-xl) var(--space-md); display flex; justify-content center; align-items flex-start; bg var(--color-surface) -->
<!-- .contentCard → DELETE; compose .c-card + local max-width: var(--w-prose); width: 100% -->
<!-- .heading → <h1> globals -->
<!-- .subheading → .t-lead -->
<!-- .heroGrid → display grid; grid-template-columns repeat(4, 1fr); gap var(--space-sm); margin-top var(--space-xs) -->
<!-- .heroCard → token-only: bg var(--color-surface-overlay); border 1px solid var(--color-border); border-radius var(--radius-md); padding var(--space-sm); display flex; flex-direction column; gap var(--space-xxs) -->
<!-- .heroCard[data-dlq='alert'] → border-color var(--color-error) -->
<!-- .heroNumber → token-only: font-size var(--fs-h3); font-weight var(--fw-bold); color var(--color-on-surface); line-height var(--lh-h3) -->
<!-- .heroCard[data-dlq='alert'] .heroNumber → color var(--color-error) -->
<!-- .heroLabel → .t-label-caps -->
<!-- .lastUpdated → token-only: color var(--color-on-surface-subtle); font-size var(--fs-body-sm) -->
<!-- .statusLine → DELETE; compose .c-notice c-notice--{success,warning,error} JSX-side className-driven (NOT data-status CSS attribute selectors per RESEARCH SW-4 pitfall) -->
<!-- .statusDot → DELETE; compose .c-status-dot--live / .c-status-dot / .c-status-dot--error -->
<!-- .footer → token-only: color var(--color-on-surface-muted); font-size var(--fs-body-sm) -->
<!-- .footerLink → token-only: color var(--color-primary-text); text-underline-offset 2px; font-weight var(--fw-semibold); font-size var(--fs-body-sm) -->
<!-- prefers-reduced-motion block → REMOVE `* { … !important }` wildcard (WCAG trap); rely on token cascade per SW-1 -->
<!-- @media (max-width: 640px) → retain responsive; replace 14px/13px with --fs-body-sm -->

<!-- classifyStatus() output → primitive map (UI-SPEC SW-4): -->
<!-- 'operational' → 'c-notice c-notice--success' + .c-status-dot c-status-dot--live + '[ok] All systems operational.' -->
<!-- 'retrying'    → 'c-notice c-notice--warning' + .c-status-dot (default)         + '[warn] Some deliveries are being retried.' -->
<!-- 'elevated'    → 'c-notice c-notice--error'   + .c-status-dot c-status-dot--error + '[err] Elevated failure rate.' -->

<!-- Phase 203 status state register (FROZEN — do NOT modify): -->
<!-- success_rate ≥ 0.999 AND dlq_count === 0 → 'operational' -->
<!-- success_rate ≥ 0.95                       → 'retrying' -->
<!-- success_rate < 0.95 OR fetch error         → 'elevated' -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite app/(markos)/status/webhooks/page.module.css to token-only centered-card layout shell + metric heroCard with DLQ-alert variant + token-only footerLink with --color-primary-text + remove !important reduced-motion wildcard (SW-1, SW-2, SW-3, SW-5)</name>
  <read_first>
    - app/(markos)/status/webhooks/page.module.css (FULL 179 LOC — confirm `* { … !important }` wildcard location and exact rules)
    - app/(markos)/status/webhooks/page.tsx (FULL 140 LOC — confirm classNames currently referenced)
    - .planning/phases/213.4-.../213.4-RESEARCH.md "Class-Rename Map: Plan 213.4-06" (lines 250–278) + "Risks + Pitfalls" SW-1/SW-4
    - .planning/phases/213.4-.../213.4-UI-SPEC.md SW-1..SW-7 + Primitive reuse + Copywriting (Phase 203 register verbatim)
    - .planning/phases/213.4-.../213.4-05-PLAN.md (precedent — same shape)
    - app/tokens.css + app/globals.css + styles/components.css v1.1.0 (.c-status-dot{,--live,--error})
    - DESIGN.md "Color > Forbidden values" + "Motion > prefers-reduced-motion handling"
  </read_first>
  <files>app/(markos)/status/webhooks/page.module.css</files>
  <action>
**Replace `app/(markos)/status/webhooks/page.module.css` ENTIRELY with:**

```css
/* Phase 213.4 Plan-06 — Token-only public webhook status-page layout shell.
 * Composes .c-card (contentCard via max-width override),
 * .c-notice c-notice--{success,warning,error} (statusLine — SW-4),
 * .c-status-dot{,--live,--error} (state dot — SW-4),
 * .t-label-caps (heroLabel),
 * .t-lead (subheading).
 * Phase 203 wiring preserved per D-20. */

.page {
  min-height: 100vh;
  padding: var(--space-xl) var(--space-md);
  display: flex;
  justify-content: center;
  align-items: flex-start;
  background: var(--color-surface);
}

.contentWrap {
  max-width: var(--w-prose);
  width: 100%;
  display: grid;
  gap: var(--space-md);
}

.heroGrid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: var(--space-sm);
  margin-top: var(--space-xs);
}

@media (max-width: 640px) {
  .heroGrid {
    grid-template-columns: repeat(2, 1fr);
  }
}

.heroCard {
  background: var(--color-surface-overlay);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  padding: var(--space-sm);
  display: flex;
  flex-direction: column;
  gap: var(--space-xxs);
}

.heroCard[data-dlq='alert'] {
  border-color: var(--color-error);
}

.heroNumber {
  font-size: var(--fs-h3);
  font-weight: var(--fw-bold);
  color: var(--color-on-surface);
  line-height: var(--lh-h3);
}

.heroCard[data-dlq='alert'] .heroNumber {
  color: var(--color-error);
}

.lastUpdated {
  color: var(--color-on-surface-subtle);
  font-size: var(--fs-body-sm);
}

.footer {
  color: var(--color-on-surface-muted);
  font-size: var(--fs-body-sm);
}

.footerLink {
  color: var(--color-primary-text);
  text-underline-offset: 2px;
  font-weight: var(--fw-semibold);
  font-size: var(--fs-body-sm);
}
```

This eliminates EVERY legacy literal:
- `font-family: 'Space Grotesk'` → globals
- `.contentCard` rule (28px radius, `0 18px 45px rgba()`, `#ffffff`) → DELETE; compose `.c-card` + local `max-width: var(--w-prose)` JSX-side via inline-style or layout-class wrapper
- `.heading` / `.subheading` rules → DELETE (`<h1>` globals + `.t-lead`)
- `.heroLabel` rule → DELETE (`.t-label-caps`; eliminates `12px`, `font-weight: 600`, `#475569`)
- `.statusLine` + `[data-status]` variants → DELETE (`.c-notice c-notice--{*}` JSX-side className-driven, NOT CSS attribute selectors)
- `.statusDot` + `[data-status]` variants → DELETE (`.c-status-dot--{live,error}` primitives)
- `.footerLink:focus-visible` `outline: 2px solid #0d9488` → DELETE (inherits global focus ring)
- `* { ... !important }` reduced-motion wildcard rule → DELETE entirely (WCAG trap; token cascade in tokens.css collapses durations to 0ms)
- All teal `#0d9488` / `#f0fdfa` / `#9a3412` / `#fef2f2` / `#fef3c7` / `#d97706` / `#dc2626` / `#0f766e` / `#0f172a` / `#475569` / `#64748b` legacy hex → tokens
- `13px` / `14px` / `12px` off-grid → `--fs-body-sm` / `--fs-label`
- `28px` / `16px` / `20px` radius → token vars

DO NOT add `font-family:`, `outline:`, `@media (pointer: coarse)`, `box-shadow:`, or `* { … !important }` declarations.

The `.c-card` width override for `.contentCard` is composed JSX-side: `<div className="c-card" style={{ maxWidth: 'var(--w-prose)', width: '100%' }}>` OR via wrapper `<div className={styles.contentWrap}>...</div>` containing the `.c-card` inside.
  </action>
  <verify>
    <automated>! grep -nE '#[0-9a-fA-F]{3,8}' "app/(markos)/status/webhooks/page.module.css" && ! grep -nE '(Sora|Space Grotesk|linear-gradient|radial-gradient|box-shadow|transform:\s*translate)' "app/(markos)/status/webhooks/page.module.css" && ! grep -nE '!important' "app/(markos)/status/webhooks/page.module.css" && ! grep -nE '#0d9488|#0f766e|#f0fdfa|#9a3412|#fef2f2|#fef3c7|#d97706|#dc2626|#0f172a|#475569|#64748b|#ffffff|#f8fafc' "app/(markos)/status/webhooks/page.module.css" && ! grep -nE '@media[^{]*pointer[^{]*coarse' "app/(markos)/status/webhooks/page.module.css" && grep -q "var(--color-surface)" "app/(markos)/status/webhooks/page.module.css" && grep -q "var(--w-prose)" "app/(markos)/status/webhooks/page.module.css" && grep -q "var(--color-primary-text)" "app/(markos)/status/webhooks/page.module.css" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **SW-1 (zero hex + Space Grotesk eliminated):** all greps return 0.
    - **SW-1 (no `!important` wildcard):** `grep -nE '!important' page.module.css` returns 0.
    - **SW-2 (`var(--w-prose)` adopted):** `grep -c "var(--w-prose)" page.module.css` ≥1.
    - **SW-3 (no DLQ teal/orange legacy):** `grep -nE '#f0fdfa|#9a3412|#0f766e' page.module.css` returns 0.
    - **SW-5 (`var(--color-primary-text)` for footerLink):** `grep -c "var(--color-primary-text)" page.module.css` ≥1; `grep -nE '#0d9488' page.module.css` returns 0.
    - **X-5:** `grep -nE '@media[^{]*pointer[^{]*coarse' page.module.css` returns 0.
  </acceptance_criteria>
  <done>`page.module.css` rewritten ~50 LOC. Atomic commit: `feat(213.4-06): rewrite status/webhooks/page.module.css to design-md tokens; remove !important wildcard (SW-1, SW-2, SW-3, SW-5)`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update app/(markos)/status/webhooks/page.tsx to compose .c-card with max-width var(--w-prose) on contentCard / .c-notice c-notice--{success,warning,error} className-driven on statusLine / .c-status-dot--{live,*,--error} primitives / .t-label-caps + bracketed glyphs prepended to status copy strings — Phase 203 wiring + classifyStatus() + statusCopy() register PRESERVED VERBATIM (SW-4, SW-7)</name>
  <read_first>
    - app/(markos)/status/webhooks/page.tsx (FULL 140 LOC — locate fetchStatus(), classifyStatus(), statusCopy(), statusLine JSX, statusDot JSX; preserve EVERY function body verbatim)
    - app/(markos)/status/webhooks/page.module.css (post-Task 1 — confirm available class names)
    - .planning/phases/213.4-.../213.4-UI-SPEC.md SW-4 + SW-7 + Copywriting (Phase 203 verbatim register: 'All systems operational' / 'Some deliveries are being retried' / 'Elevated failure rate')
    - .planning/phases/213.4-.../213.4-RESEARCH.md "Wiring Preservation Map > Phase 203" + "Risks + Pitfalls" SW (className driven not data-status selectors)
    - styles/components.css v1.1.0 (.c-status-dot{,--live,--error})
    - test/webhooks/* (Phase 203 wiring suite — confirm tests pass after edit)
  </read_first>
  <files>app/(markos)/status/webhooks/page.tsx</files>
  <action>
**Apply className substitutions and JSX shape changes — Phase 203 wiring + logic UNTOUCHED:**

Locate the existing JSX that renders the centered content card. Wrap with token-only structure:

```tsx
<div className={styles.page}>
  <div className={styles.contentWrap}>
    <div className="c-card" style={{ width: '100%' }}>
      <h1>Webhook delivery status</h1>
      <p className="t-lead">Live metrics for the MarkOS webhook platform. Updated every 60 seconds.</p>

      {/* Hero metric grid — heroCard data-dlq='alert' on DLQ overflow */}
      <div className={styles.heroGrid}>
        <div className={styles.heroCard} aria-label="Deliveries 24h (total_24h)">
          <span className="t-label-caps">Deliveries 24h</span>
          <span className={styles.heroNumber}>{snapshot?.total_24h ?? '—'}</span>
        </div>
        <div className={styles.heroCard} aria-label="Success rate">
          <span className="t-label-caps">Success rate</span>
          <span className={styles.heroNumber}>{...}</span>
        </div>
        <div className={styles.heroCard} aria-label="Avg latency (avg_latency_ms)">
          <span className="t-label-caps">Avg latency</span>
          <span className={styles.heroNumber}>{...}</span>
        </div>
        <div
          className={styles.heroCard}
          data-dlq={snapshot?.dlq_count > 0 ? 'alert' : undefined}
          aria-label="DLQ count (dlq_count)"
        >
          <span className="t-label-caps">DLQ count</span>
          <span className={styles.heroNumber}>{snapshot?.dlq_count ?? 0}</span>
        </div>
      </div>

      {/* statusLine — className-driven, NOT data-status CSS attribute selector */}
      {(() => {
        const status = classifyStatus(snapshot);  // PRESERVED Phase 203 logic
        const statusMap = {
          operational: { cls: 'c-notice c-notice--success', dot: 'c-status-dot c-status-dot--live',  glyph: '[ok]',   copy: 'All systems operational.' },
          retrying:    { cls: 'c-notice c-notice--warning', dot: 'c-status-dot',                     glyph: '[warn]', copy: 'Some deliveries are being retried.' },
          elevated:    { cls: 'c-notice c-notice--error',   dot: 'c-status-dot c-status-dot--error', glyph: '[err]',  copy: 'Elevated failure rate.' },
        } as const;
        const { cls, dot, glyph, copy } = statusMap[status];
        return (
          <div className={cls} role="status" aria-live="polite">
            <span className={dot} aria-hidden="true" />
            <strong>{glyph}</strong>{" "}{copy}
          </div>
        );
      })()}

      <time className={styles.lastUpdated} dateTime={...}>{...last-updated copy...}</time>

      <p className={styles.footer}>
        Subscriber? <a href="..." className={styles.footerLink}>Learn how to configure webhooks.</a>
      </p>
    </div>
  </div>
</div>
```

**KEY rules (per SW-4 + SW-7):**
- `fetchStatus()`, `classifyStatus()`, `statusCopy()` (if used) — function bodies UNTOUCHED. Endpoint `/api/public/webhooks/status` UNTOUCHED.
- `classifyStatus()` output values (`'operational'` / `'retrying'` / `'elevated'`) UNCHANGED. Drive the `statusMap` lookup, not CSS attribute selectors.
- Status copy strings match Phase 203 verbatim, with bracketed-glyph PREFIX added (`[ok]` / `[warn]` / `[err]`).
- The `aria-label` on each heroCard preserves the parenthetical raw key (`(total_24h)` / `(avg_latency_ms)` / `(dlq_count)`) for screen readers per UI-SPEC Copywriting; the visible label drops the parenthetical.
- `data-dlq='alert'` attribute preserved (CSS rule `.heroCard[data-dlq='alert']` in module.css fires on DLQ overflow).
- `aria-live="polite"` on the statusLine notice for SR announcement on dynamic state change.
- DOM tree + prop interface UNCHANGED. `'use client'` directive (if any) preserved.

**Banned-lexicon scan = 0.** No exclamation. No emoji.

After edit: `npm run build` MUST exit 0. Phase 203 wiring tests in `test/webhooks/` MUST stay green.
  </action>
  <verify>
    <automated>grep -qE 'c-notice c-notice--(success|warning|error)' "app/(markos)/status/webhooks/page.tsx" && grep -qE 'c-status-dot' "app/(markos)/status/webhooks/page.tsx" && grep -qE '\[ok\]|\[warn\]|\[err\]' "app/(markos)/status/webhooks/page.tsx" && grep -qE 'api/public/webhooks/status' "app/(markos)/status/webhooks/page.tsx" && grep -qE 'classifyStatus' "app/(markos)/status/webhooks/page.tsx" && ! grep -qE 'styles\.(statusLine|statusDot|heading|subheading|heroLabel|contentCard)' "app/(markos)/status/webhooks/page.tsx" && ! grep -inE 'Please|synergy|leverage|empower|unlock|revolutionize|supercharge|holistic|seamless|cutting-edge|innovative|game-changer|next-generation|world-class|best-in-class|reimagine|disrupt' "app/(markos)/status/webhooks/page.tsx" && npm run build && npm test 2>&1 | grep -E '(test/webhooks|webhooks-status)' | grep -v 'fail' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **SW-4 (.c-notice + .c-status-dot primitives):** `grep -cE 'c-notice c-notice--(success|warning|error)' page.tsx` ≥1; `grep -cE 'c-status-dot' page.tsx` ≥1.
    - **SW-4 (bracketed glyphs prepended):** `grep -cE '\[(ok|warn|err)\]' page.tsx` ≥3.
    - **SW-7 (Phase 203 endpoint preserved):** `grep -c 'api/public/webhooks/status' page.tsx` ≥1.
    - **SW-7 (classifyStatus preserved):** `grep -c 'classifyStatus' page.tsx` ≥1.
    - **No legacy class refs:** `grep -cE 'styles\.(statusLine|statusDot|heading|subheading|heroLabel|contentCard)' page.tsx` returns 0.
    - **Banned-lexicon = 0; Build green; Phase 203 wiring tests green.**
  </acceptance_criteria>
  <done>`page.tsx` updated. Atomic commit: `feat(213.4-06): compose primitives in status/webhooks/page.tsx — className-driven .c-notice + .c-status-dot; Phase 203 wiring preserved (SW-4, SW-7)`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create NEW app/(markos)/status/webhooks/page.stories.tsx with ≥3 named state stories (Operational / Retrying / Elevated) using mock snapshot args (NOT live fetch) per UI-SPEC SW-6 + run all verification gates</name>
  <read_first>
    - app/(markos)/status/webhooks/page.tsx (post-Task 2 — confirm prop interface and snapshot shape)
    - .planning/phases/213.4-.../213.4-UI-SPEC.md SW-6 + Storybook State Coverage table (lines 538–541) + Phase 203 status state register
    - .planning/phases/213.4-.../213.4-RESEARCH.md "Risks + Pitfalls > Stories with useEffect/fetch" (mock snapshot prop, NOT live fetch)
    - .planning/phases/213.4-.../213.4-05-PLAN.md Task 5 (precedent CSF3 pattern)
  </read_first>
  <files>app/(markos)/status/webhooks/page.stories.tsx</files>
  <action>
**Create NEW `app/(markos)/status/webhooks/page.stories.tsx`:**

```tsx
import type { Meta, StoryObj } from '@storybook/react';
import StatusWebhooksPage from './page';

const meta = {
  title: 'Status/Webhooks',
  component: StatusWebhooksPage,
  parameters: { layout: 'fullscreen' },
} satisfies Meta<typeof StatusWebhooksPage>;

export default meta;
type Story = StoryObj<typeof meta>;

// Mock snapshot prop shape (matches /api/public/webhooks/status response — Phase 203):
// { total_24h, success_rate, avg_latency_ms, dlq_count, fetched_at }

export const Operational: Story = {
  args: {
    snapshot: {
      total_24h: 12834,
      success_rate: 0.999,
      avg_latency_ms: 122,
      dlq_count: 0,
      fetched_at: '2026-04-29T12:00:00Z',
    },
  },
  parameters: { docs: { description: { story: 'success_rate ≥ 0.999 AND dlq_count === 0 → classifyStatus = operational. .c-notice c-notice--success + .c-status-dot--live + [ok] All systems operational.' } } },
};

export const Retrying: Story = {
  args: {
    snapshot: {
      total_24h: 8421,
      success_rate: 0.97,
      avg_latency_ms: 450,
      dlq_count: 2,
      fetched_at: '2026-04-29T12:00:00Z',
    },
  },
  parameters: { docs: { description: { story: 'success_rate ≥ 0.95 → classifyStatus = retrying. .c-notice c-notice--warning + .c-status-dot (default) + [warn] Some deliveries are being retried.' } } },
};

export const Elevated: Story = {
  args: {
    snapshot: {
      total_24h: 4203,
      success_rate: 0.88,
      avg_latency_ms: 980,
      dlq_count: 12,
      fetched_at: '2026-04-29T12:00:00Z',
    },
  },
  parameters: { docs: { description: { story: 'success_rate < 0.95 → classifyStatus = elevated. .c-notice c-notice--error + .c-status-dot--error + [err] Elevated failure rate. heroCard[data-dlq=alert] DLQ tile shows --color-error border.' } } },
};
```

**KEY rules (per SW-6 + RESEARCH pitfall):**
- 3 named exports `Operational` / `Retrying` / `Elevated` matching UI-SPEC line 538–540 verbatim.
- `meta.title='Status/Webhooks'` exactly.
- Mock `snapshot` prop value matches Phase 203 status state register (operational/retrying/elevated thresholds).
- Stories DO NOT trigger live fetch — `snapshot` is provided as args directly. If `page.tsx` performs `useEffect`/`fetch`, the story args bypass that path (or executor adds a Storybook decorator that supplies the snapshot context).
- `parameters.theme: "legacy"` MUST NOT appear (DESIGN.md v1.1.0 default render).

After edit: `npm run build-storybook` + `npm test` MUST exit 0. Phase 203 wiring tests stay green per D-20.
  </action>
  <verify>
    <automated>test -f "app/(markos)/status/webhooks/page.stories.tsx" && grep -cE '^export const (Operational|Retrying|Elevated):' "app/(markos)/status/webhooks/page.stories.tsx" | awk '$1>=3 {exit 0} $1<3 {exit 1}' && grep -q "title: 'Status/Webhooks'" "app/(markos)/status/webhooks/page.stories.tsx" && ! grep -qE 'parameters\.theme.*"legacy"' "app/(markos)/status/webhooks/page.stories.tsx" && grep -q 'export default meta' "app/(markos)/status/webhooks/page.stories.tsx" && npm run build-storybook && npm test && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **SW-6 (≥3 named state stories):** `grep -cE '^export const (Operational|Retrying|Elevated):' page.stories.tsx` ≥3.
    - **`meta.title='Status/Webhooks'`:** ≥1.
    - **No legacy theme; Storybook green; npm test green (Phase 203 preserved per D-20).**
  </acceptance_criteria>
  <done>NEW `page.stories.tsx` created with 3 mock-snapshot state stories. Atomic commit: `feat(213.4-06): create status/webhooks/page.stories.tsx with Operational/Retrying/Elevated (SW-6)`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public→/api/public/webhooks/status | Public-facing endpoint (no auth required); Phase 203 substrate |
| customer→status copy | Customer-grade copy register; redesign preserves Phase 203 verbatim states |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213.4-06-01 | Information Disclosure | public status data | accept | Phase 203 substrate already governs what's public; redesign is presentation-only |
| T-213.4-06-02 | Tampering | classifyStatus thresholds | accept | Logic UNTOUCHED per D-20; SW-7 verification grep enforces |
| T-213.4-06-03 | Denial of Service | reduced-motion `* { ... !important }` wildcard | mitigate | SW-1 removes the wildcard (was overriding inline styles from third-party embeds — WCAG trap) |
</threat_model>

<verification>
```bash
grep -nE '#[0-9a-fA-F]{3,8}|gradient|box-shadow|!important' "app/(markos)/status/webhooks/page.module.css"  # 0
grep -cE 'c-notice c-notice--(success|warning|error)' "app/(markos)/status/webhooks/page.tsx"  # ≥1
grep -cE 'c-status-dot' "app/(markos)/status/webhooks/page.tsx"  # ≥1
grep -c 'api/public/webhooks/status' "app/(markos)/status/webhooks/page.tsx"  # ≥1 (D-20)
grep -cE '^export const (Operational|Retrying|Elevated):' "app/(markos)/status/webhooks/page.stories.tsx"  # ≥3
npm run build && npm run build-storybook && npm test  # all 0
```
</verification>

<success_criteria>
UI-SPEC AC SW-1..SW-7 close. Phase 203 wiring + state register preserved verbatim per D-20. 3 atomic commits.
</success_criteria>

<output>
Create `.planning/phases/213.4-.../213.4-06-SUMMARY.md`. Hand-off to Plan 213.4-07 (404-workspace).
</output>
</content>
</invoke>
