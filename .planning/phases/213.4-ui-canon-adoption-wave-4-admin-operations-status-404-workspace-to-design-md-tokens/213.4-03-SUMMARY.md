---
phase: 213.4
plan: "03"
subsystem: admin/governance
tags: [ui-canon, design-md, tokens, admin, governance, storybook]
dependency_graph:
  requires: ["213.4-02"]
  provides: ["213.4-04"]
  affects:
    - app/(markos)/admin/governance/page.module.css
    - app/(markos)/admin/governance/page.tsx
    - app/(markos)/admin/governance/governance.stories.tsx
tech_stack:
  added: []
  patterns:
    - ".c-notice c-notice--{error,success} variant-driven via noticeMap"
    - ".c-badge c-badge--{error,success,info} + bracketed glyphs via DecisionBadge helper"
    - ".c-button--primary + .c-button--destructive + .c-modal/.c-backdrop Reject confirm"
    - "token-only <table> with descendant-selector th/td recipe (D-14)"
    - "CSF3 typed Meta<typeof> + StoryObj pattern"
key_files:
  created: []
  modified:
    - app/(markos)/admin/governance/page.module.css
    - app/(markos)/admin/governance/page.tsx
    - app/(markos)/admin/governance/governance.stories.tsx
decisions:
  - "noticeMap const drives denial/exportReady notice variants — no if/else JSX branches"
  - "DecisionBadge helper component encapsulates badgeMap lookup for reuse across all 4 tables"
  - "Inline flex styles moved to module CSS (heroActions/modalActions) to satisfy no-inline-style lint rule"
  - "Double-quoted title string ('Admin/Governance' value) satisfies AG-6 requirement — quote style is not semantic"
metrics:
  duration: "~20 minutes"
  completed: "2026-04-28T00:00:00Z"
  tasks_completed: 3
  tasks_total: 3
  files_modified: 3
---

# Phase 213.4 Plan 03: Admin/Governance Surface Summary

**One-liner:** Token-only admin/governance redesign eliminating teal stripe + Space Grotesk/Sora, composing `.c-notice`/`.c-badge`/`.c-modal`/`.c-button--destructive` primitives with variant-driven noticeMap and DecisionBadge across 4 semantic tables.

## Tasks Completed

| # | Name | Commit | Files |
|---|------|--------|-------|
| 1 | Rewrite page.module.css to token-only layout shell (AG-1, AG-2, AG-3, AG-4) | 31bd4b9 | page.module.css |
| 2 | Compose primitives in page.tsx (AG-2, AG-3, AG-5) | 918d2af | page.tsx + page.module.css |
| 3 | Extend governance.stories.tsx with Default/DeniedMapping/ExportReady (AG-6) | 71c887e | governance.stories.tsx |

## Acceptance Criteria Closed

| AC | Description | Status |
|----|-------------|--------|
| AG-1 | Zero inline hex; zero Sora/Space Grotesk in page.module.css | Closed |
| AG-2 | `.c-card` on heroCard/panel; active section `--color-primary` border + `--color-primary-subtle` bg | Closed |
| AG-3 | `.c-badge--{error,success,info}` + bracketed glyphs on all 4 table decision/status columns | Closed |
| AG-4 | `.detailCard` → `.c-card`; `border-left: 4px solid #0d9488` teal stripe eliminated | Closed |
| AG-5 | `.c-notice c-notice--{error,success}` variant-driven via noticeMap (denial/exportReady) | Closed |
| AG-6 | `governance.stories.tsx` extended: Default/DeniedMapping/ExportReady CSF3 typed exports | Closed |

## Verification Gate Results

```
grep -nE '#[0-9a-fA-F]{3,8}' page.module.css                    → 0 (PASS)
grep -nE 'border-left:\s*4px\s+solid' page.module.css            → 0 (PASS)
grep legacy class selectors in page.module.css                    → 0 (PASS)
grep -cE 'c-notice c-notice--(error|success)' page.tsx           → 2 (PASS)
grep -cE 'c-button c-button--primary' page.tsx                   → 1 (PASS)
grep -cE 'c-button c-button--destructive' page.tsx               → 2 (PASS)
grep -cE 'c-modal|c-backdrop' page.tsx                           → 1+1 (PASS)
grep -cE 'c-badge c-badge--(error|success|info)' page.tsx        → 4 (PASS)
grep legacy styles.* refs in page.tsx                             → 0 (PASS)
grep -cE '^export const (Default|DeniedMapping|ExportReady):' stories.tsx → 3 (PASS)
npm run build:llm (tsc --noEmit)                                  → exit 0 (PASS)
npm run build-storybook                                           → exit 0 (PASS)
npm test (node --test)                                            → 724/724 pass (PASS)
```

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing] Inline flex styles moved to module CSS**
- **Found during:** Task 2 (IDE post-write diagnostics)
- **Issue:** Plan prescribed `style={{ display: 'flex', gap: 'var(--space-xs)' }}` for button-row divs; lint rule flags inline CSS styles in TSX.
- **Fix:** Added `.heroActions` and `.modalActions` classes to `page.module.css`; replaced inline `style` props with `className={styles.heroActions}` / `className={styles.modalActions}`.
- **Files modified:** app/(markos)/admin/governance/page.module.css, page.tsx
- **Commit:** 918d2af

## Known Stubs

None. All primitives wire to live variant props and data.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes introduced. Reject confirm modal is UI-only; Phase 207 substrate audit-log emit stubs preserved in `onApprove` / `confirmReject` functions (D-21). Admin role-gate in `app/(markos)/admin/layout.tsx` untouched per D-22.

## Chromatic Review Reminder

Run Chromatic after merge to verify `Admin/Governance` → Default / DeniedMapping / ExportReady stories render correctly against DESIGN.md v1.1.0 dark token palette. Specifically verify:
- `.c-notice c-notice--error` (red left-stripe, `--color-error` text) in DeniedMapping
- `.c-notice c-notice--success` (mint left-stripe, `--color-success` text) in ExportReady
- Active section `border-color: var(--color-primary)` visible against dark surface
- `.c-badge--error` `[err]` glyph on Identity Federation denied row

## Hand-off

Next plan: **213.4-04** (operations root surface).

## Self-Check: PASSED

- `app/(markos)/admin/governance/page.module.css` — exists, token-only, no hex, no legacy selectors
- `app/(markos)/admin/governance/page.tsx` — exists, all primitive classes present, no legacy refs
- `app/(markos)/admin/governance/governance.stories.tsx` — exists, 3 named CSF3 typed exports
- Commits 31bd4b9, 918d2af, 71c887e — all present in git log
