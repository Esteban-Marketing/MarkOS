---
phase: 213.4
plan: 02
type: execute
wave: 1
depends_on: ["213.4-01"]
files_modified:
  - app/(markos)/admin/billing/page.module.css
  - app/(markos)/admin/billing/page.tsx
  - app/(markos)/admin/billing/reconciliation.stories.tsx
autonomous: true
requirements:
  - AB-1
  - AB-2
  - AB-3
  - AB-4
  - AB-5
  - AB-6
  - AB-7
  - AB-8
must_haves:
  truths:
    - "app/(markos)/admin/billing/page.module.css contains zero inline hex (whitelist: 1px borders, 2px focus offsets), zero off-grid spacing (whitelist: 1px/2px/4px/8px and documented max-width literals), zero Sora/Space Grotesk, zero gradients, zero box-shadow, zero transform-translate, zero teal #0d9488/#0f766e/#14b8a6/#155e75/#f8fbfd/#ffffff"
    - "app/(markos)/admin/billing/page.tsx composes .c-card on hero/panel sections, .c-button c-button--primary on Review Billing Evidence CTA, .c-button c-button--secondary on Place/Release Hold + Retry Sync, .c-button c-button--destructive on Write-off + .c-modal/.c-backdrop confirm, .c-notice c-notice--{success,warning,error} on review/status banner (variant-driven), .c-badge--{warning,info,success} on reconciliation row state (with bracketed-glyph prefixes), .t-label-caps on eyebrow + table th content"
    - "All legacy local classes eliminated from page.tsx: styles.heroCard / styles.panel / styles.eyebrow / styles.title / styles.sectionTitle / styles.tableHead / styles.tableCell / styles.primaryButton / styles.secondaryButton / styles.reviewCard / styles.reviewTitle / styles.reviewText"
    - "app/(markos)/admin/billing/reconciliation.stories.tsx extended with ≥3 named state stories (Healthy / HoldState / SyncFailure) per UI-SPEC AB-8 + Storybook State Coverage"
    - "Banned-lexicon scan = 0 in modified .tsx (CONTEXT D-07); zero exclamation points; zero emoji; zero 'Please' softeners"
    - "DOM tree + prop interface UNCHANGED (CONTEXT D-05); admin role-gate via app/(markos)/admin/layout.tsx preserved verbatim (CONTEXT D-22)"
    - "npm run build exits 0; npm run build-storybook exits 0; npm test exits 0 (Phase 200/201/202/203/204/205/207 wiring suites preserved)"
  artifacts:
    - path: "app/(markos)/admin/billing/page.module.css"
      provides: "Token-only billing-admin layout shell — composition + grid layout + tab + evidence-rail + table th/td recipe; primitives carry button/card/notice JSX-side"
      min_lines: 45
      contains: "var(--color-surface)"
    - path: "app/(markos)/admin/billing/page.tsx"
      provides: "Updated billing-admin shell composing primitives per UI-SPEC AB-2..AB-7; preserves Phase 205 Pricing Engine + admin role-gating wiring"
      contains: "c-notice c-notice--"
    - path: "app/(markos)/admin/billing/reconciliation.stories.tsx"
      provides: "Storybook 8 CSF3 ≥3 named state exports (Healthy / HoldState / SyncFailure) per UI-SPEC AB-8"
      exports: ["default", "Healthy", "HoldState", "SyncFailure"]
  key_links:
    - from: "app/(markos)/admin/billing/page.tsx"
      to: "styles/components.css .c-notice c-notice--{success,warning,error} primitive"
      via: "variant-driven className composition for review/status banner per AB-7"
      pattern: "c-notice\\s+c-notice--(success|warning|error)"
    - from: "app/(markos)/admin/billing/page.module.css"
      to: "app/tokens.css var(--*)"
      via: "var(--color-surface), var(--color-on-surface-muted), var(--space-*), var(--fs-label), var(--ls-label), var(--radius-md)"
      pattern: "var\\(--(color|space|fs|fw|radius|ls)-"
    - from: "app/(markos)/admin/billing/page.tsx"
      to: ".c-badge--{warning,info,success} on reconciliation row state with bracketed glyph"
      via: "<span className='c-badge c-badge--{state}'>[glyph] {label}</span> on each reconciliation row"
      pattern: "c-badge\\s+c-badge--(warning|info|success|error)"
---

<objective>
Redesign `/admin/billing` (`page.module.css` 175 LOC, 25 inline hex; `page.tsx` 148 LOC; existing `reconciliation.stories.tsx`) into the canonical DESIGN.md v1.1.0 visual contract per `213.4-UI-SPEC.md` Plan 213.4-02 (8 ACs: AB-1..AB-8). Compose `.c-card` (sections), `.c-button{,--primary,--secondary,--destructive}` (CTAs + write-off), `.c-modal + .c-backdrop` (write-off confirm), `.c-notice c-notice--{success,warning,error}` (variant-driven review/status banner per AB-7), `.c-badge--{warning,info,success}` (reconciliation row state), `.t-label-caps` (eyebrow + table th content). Token-only `<th>`/`<td>` recipe. Extend `reconciliation.stories.tsx` with ≥3 named state stories (`Healthy` / `HoldState` / `SyncFailure`) per UI-SPEC AB-8.

Purpose: Close UI-SPEC AC AB-1 (zero inline hex + Sora/Space Grotesk eliminated), AB-2 (`.heroCard` → `.c-card`; drop shadow eliminated), AB-3 (queue tabs token-only; active border `--color-primary` + bg `--color-primary-subtle`), AB-4 (table `<th>`/`<td>` token recipe), AB-5 (row state badges via `.c-badge--{*}` with bracketed glyphs), AB-6 (button primitives + write-off `.c-modal`), AB-7 (reviewCard → `.c-notice c-notice--{state}` variant-driven), AB-8 (≥3 named stories). Preserve Phase 205 Pricing Engine wiring (CONTEXT D-22) and admin role-gating (D-22). DOM tree + prop interfaces unchanged per D-05.

Output: `app/(markos)/admin/billing/page.module.css` rewritten (175 → ~55 LOC, token-only layout shell + table recipe); `app/(markos)/admin/billing/page.tsx` updated (className-only rewrite + `.c-notice` JSX swap on reviewCard + `.c-modal` JSX on write-off); `app/(markos)/admin/billing/reconciliation.stories.tsx` extended.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/213.4-ui-canon-adoption-wave-4-admin-operations-status-404-workspace-to-design-md-tokens/213.4-CONTEXT.md
@.planning/phases/213.4-ui-canon-adoption-wave-4-admin-operations-status-404-workspace-to-design-md-tokens/213.4-UI-SPEC.md
@.planning/phases/213.4-ui-canon-adoption-wave-4-admin-operations-status-404-workspace-to-design-md-tokens/213.4-RESEARCH.md
@DESIGN.md
@CLAUDE.md
@app/tokens.css
@app/globals.css
@styles/components.css
@app/(markos)/admin/billing/page.module.css
@app/(markos)/admin/billing/page.tsx
@app/(markos)/admin/billing/reconciliation.stories.tsx
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-01-PLAN.md
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-01-SUMMARY.md

<interfaces>
<!-- Class-Rename Map (RESEARCH.md "Plan 213.4-02 — Admin: Billing"): -->
<!-- .page → Rewrite: padding: var(--space-lg); background: var(--color-surface); min-height: 100vh -->
<!-- .shell → Rewrite: max-width: var(--w-container); margin: 0 auto; display: grid; gap: var(--space-lg) -->
<!-- .heroCard, .panel → DELETE; compose .c-card JSX-side -->
<!-- .eyebrow → DELETE; .t-label-caps utility JSX-side -->
<!-- .title → DELETE; plain <h1> (globals) -->
<!-- .heroText, .bodyText → DELETE; <p> body-md inheritance -->
<!-- .layoutGrid → Retain (layout): display: grid; grid-template-columns: 0.9fr 1.4fr 0.9fr; gap: var(--space-md) -->
<!-- .sectionTitle → DELETE; plain <h4> (globals) -->
<!-- .tabList, .evidenceList → Retain (layout): display: grid; gap: var(--space-xs) -->
<!-- .mainColumn → Retain (layout): display: grid; gap: var(--space-md) -->
<!-- .tab → Retain (token-only): padding var(--space-xs)/var(--space-sm); border-radius var(--radius-md); border 1px solid var(--color-border); bg transparent; color var(--color-on-surface) -->
<!-- .activeTab → Retain (token-only): border-color var(--color-primary); background var(--color-primary-subtle) (eliminates #0d9488 + #f0fdfa) -->
<!-- .evidenceItem → Retain (token-only): bg var(--color-surface-overlay); border-left 1px solid var(--color-border) -->
<!-- .activeEvidenceItem → Retain (token-only): border-left 4px solid var(--color-primary) (documented exception — evidence-rail affordance, NOT a notice) -->
<!-- .table → Retain (layout): width 100%; border-collapse: collapse -->
<!-- .tableHead, .tableCell → DELETE; <th>/<td> token recipe (D-14): padding var(--space-xs) var(--space-sm); color var(--color-on-surface-muted) (th) / var(--color-on-surface) (td); font-size var(--fs-label); font-weight var(--fw-semibold); text-transform uppercase; letter-spacing var(--ls-label); border-bottom 1px solid var(--color-border) -->
<!-- .buttonRow → Retain (layout): display: flex; gap: var(--space-xs); flex-wrap: wrap -->
<!-- .primaryButton → DELETE; compose .c-button c-button--primary -->
<!-- .secondaryButton → DELETE; compose .c-button c-button--secondary -->
<!-- .reviewCard, .reviewTitle, .reviewText → DELETE; compose .c-notice c-notice--{state} (variant-driven) -->
<!-- Row state badges → ADD: .c-badge--{warning,info,success} per row state -->
<!-- Write-off confirm → ADD: .c-modal + .c-backdrop + .c-button--destructive -->

<!-- Variant → primitive map (per UI-SPEC AB-7): -->
<!-- variant="healthy" → .c-notice c-notice--success + [ok] glyph -->
<!-- variant="hold" → .c-notice c-notice--warning + [warn] glyph -->
<!-- variant="syncFailure" → .c-notice c-notice--error + [err] glyph -->

<!-- Row state → badge map (per UI-SPEC AB-5): -->
<!-- "Mismatch" / "Sync failed" → .c-badge--warning -->
<!-- "Pending review" → .c-badge--info -->
<!-- "Reconciled" / "Released" / "Restored" → .c-badge--success -->
<!-- "Hold" → .c-badge--warning -->

<!-- Copy contract (UI-SPEC AB Copywriting — verbatim): -->
<!-- Healthy: [ok] Billing evidence reconciled. No operator intervention required. -->
<!-- Sync failure: [err] Provider sync failure unresolved. Review evidence to reconcile or place a hold. -->
<!-- Hold: [warn] Billing hold active. Restricted actions paused until billing health is restored. -->
<!-- Write-off modal title: Write off this billing discrepancy? -->
<!-- Write-off modal body: This action is logged as operator evidence and cannot be undone. -->
<!-- Write-off destructive button: Write off -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite app/(markos)/admin/billing/page.module.css to token-only layout shell + table recipe (AB-1, AB-3, AB-4) — delete bespoke card/button/banner/title/eyebrow styles, retain layout containers + tab + evidence-rail + table token recipe with all-token values</name>
  <read_first>
    - app/(markos)/admin/billing/page.module.css (FULL 175 LOC — map every existing class to RESEARCH class-rename table)
    - app/(markos)/admin/billing/page.tsx (FULL 148 LOC — confirm classNames currently referenced; nothing should be missed)
    - .planning/phases/213.4-.../213.4-RESEARCH.md "Class-Rename Map: Plan 213.4-02 — Admin: Billing" (lines 88–123) — authoritative class-by-class disposition
    - .planning/phases/213.4-.../213.4-UI-SPEC.md AB-1..AB-8 + Primitive reuse table + Copywriting table
    - .planning/phases/213.3-.../213.3-01-PLAN.md (precedent: same shape — billing surface to tokens; mirror Module CSS Rewrite Shape)
    - app/tokens.css (--color-surface, --color-on-surface, --color-on-surface-muted, --color-border, --color-primary, --color-primary-subtle, --color-surface-overlay, --space-xs/-sm/-md/-lg, --radius-md, --fs-label, --fw-semibold, --ls-label, --w-container)
    - app/globals.css (<h1>, <h2>, <h3>, <h4>, <p>, .t-lead, .t-label-caps utilities)
    - styles/components.css v1.1.0 (.c-card, .c-button, .c-notice, .c-badge, .c-modal, .c-backdrop primitives — confirm what they cover JSX-side so module.css does NOT redeclare)
    - DESIGN.md "Color > Forbidden values" + "Spacing Scale" + "Components > Notice / Banner" + "Components > Tables (vanilla recipe)"
  </read_first>
  <files>app/(markos)/admin/billing/page.module.css</files>
  <action>
**Replace `app/(markos)/admin/billing/page.module.css` ENTIRELY with the following content (RESEARCH class-rename map authoritative target):**

```css
/* Phase 213.4 Plan-02 — Token-only admin/billing layout shell.
 * Composes .c-card (sections), .c-button{,--primary,--secondary,--destructive} (CTAs),
 * .c-notice c-notice--{success,warning,error} (review/status banner — AB-7),
 * .c-badge--{warning,info,success} (reconciliation row state — AB-5),
 * .c-modal + .c-backdrop (write-off confirm — AB-6),
 * .t-label-caps (eyebrow + table headers),
 * vanilla <table> with token-cited <th>/<td> recipe (D-14).
 * All composition lives JSX-side in page.tsx. This module carries
 * layout + tab + evidence-rail + table-recipe concerns only. */

.page {
  padding: var(--space-lg);
  background: var(--color-surface);
  min-height: 100vh;
}

.shell {
  max-width: var(--w-container);
  margin: 0 auto;
  display: grid;
  gap: var(--space-lg);
}

.layoutGrid {
  display: grid;
  grid-template-columns: 0.9fr 1.4fr 0.9fr;
  gap: var(--space-md);
}

@media (max-width: 1023.98px) {
  .layoutGrid {
    grid-template-columns: 1fr;
  }
}

.tabList,
.evidenceList {
  display: grid;
  gap: var(--space-xs);
  list-style: none;
  margin: 0;
  padding: 0;
}

.mainColumn {
  display: grid;
  gap: var(--space-md);
}

/* Queue tabs — token-only local classes; no .c-tab primitive this wave. */
.tab {
  padding: var(--space-xs) var(--space-sm);
  border-radius: var(--radius-md);
  border: 1px solid var(--color-border);
  background: transparent;
  color: var(--color-on-surface);
  text-align: left;
  font: inherit;
  cursor: pointer;
}

.activeTab {
  border-color: var(--color-primary);
  background: var(--color-primary-subtle);
}

/* Evidence rail — 4px primary left-stripe on active item is a documented
 * exception per RESEARCH "Open Question": evidence-rail items are NOT banners,
 * the stripe is a visual affordance for an evidence timeline. .c-notice is
 * reserved for banner-shaped state signals (which compose JSX-side). */
.evidenceItem {
  background: var(--color-surface-overlay);
  border-left: 1px solid var(--color-border);
  padding: var(--space-xs) var(--space-sm);
  border-radius: var(--radius-md);
}

.activeEvidenceItem {
  border-left: 4px solid var(--color-primary);
}

.buttonRow {
  display: flex;
  gap: var(--space-xs);
  flex-wrap: wrap;
}

/* Reconciliation table — D-14 token-only recipe; no .c-table primitive this wave. */
.table {
  width: 100%;
  border-collapse: collapse;
}

.table th {
  padding: var(--space-xs) var(--space-sm);
  color: var(--color-on-surface-muted);
  font-size: var(--fs-label);
  font-weight: var(--fw-semibold);
  text-transform: uppercase;
  letter-spacing: var(--ls-label);
  border-bottom: 1px solid var(--color-border);
  text-align: left;
}

.table td {
  padding: var(--space-xs) var(--space-sm);
  border-bottom: 1px solid var(--color-border);
  color: var(--color-on-surface);
  font-size: var(--fs-body-md);
  vertical-align: middle;
}
```

This eliminates EVERY legacy literal in the previous file:
- `font-family: 'Sora'` / `'Space Grotesk'` declarations → inherited globals
- All `#hex` text/border/background literals → token vars
- `.heroCard` / `.panel` rules → DELETE; compose `.c-card` JSX-side (eliminates `#ffffff`, 20px/28px radius, `0 18px 45px rgba()` shadow, `padding: 32px`)
- `.eyebrow` rule → DELETE; apply `.t-label-caps` JSX-side
- `.title` / `.sectionTitle` → DELETE; `<h1>` / `<h4>` inherits globals (eliminates `font-size: 28px` / `20px` / `#0f172a`)
- `.heroText` / `.bodyText` → DELETE; `<p>` body-md inheritance
- `.tableHead` / `.tableCell` → DELETE; replaced by `<th>` / `<td>` token recipe
- `.primaryButton` / `.secondaryButton` → DELETE; compose `.c-button c-button--primary` / `.c-button c-button--secondary` JSX-side
- `.reviewCard` / `.reviewTitle` / `.reviewText` → DELETE; compose `.c-notice c-notice--{state}` JSX-side
- Tab `border-radius: 14px` / `999px` literals → `var(--radius-md)`
- Active tab `#0d9488` border + `#f0fdfa` bg → `var(--color-primary)` + `var(--color-primary-subtle)`
- All `12px` / `14px` / `18px` / `20px` off-grid spacing → `var(--space-xs)` / `var(--space-sm)` / `var(--space-md)`

DO NOT add `font-family:` declarations. DO NOT add `outline:` declarations on `:focus`. DO NOT redeclare `@media (pointer: coarse)` (global rule on `.c-button` per X-5). DO NOT add `box-shadow:` declarations.
  </action>
  <verify>
    <automated>! grep -nE '#[0-9a-fA-F]{3,8}' "app/(markos)/admin/billing/page.module.css" && ! grep -nE '(Sora|Space Grotesk|linear-gradient|radial-gradient|box-shadow|transform:\s*translate)' "app/(markos)/admin/billing/page.module.css" && ! grep -nE '#0d9488|#0f766e|#14b8a6|#155e75|#f0fdfa|#f8fbfd|#f5f7fa|#ffffff|#0f172a|#334155|#475569|#1d4ed8|#cbd5e1|#e2e8f0' "app/(markos)/admin/billing/page.module.css" && ! grep -nE '@media[^{]*pointer[^{]*coarse' "app/(markos)/admin/billing/page.module.css" && ! grep -nE '\.heroCard\b|\.panel\b|\.eyebrow\b|\.title\b|\.sectionTitle\b|\.tableHead\b|\.tableCell\b|\.primaryButton\b|\.secondaryButton\b|\.reviewCard\b|\.reviewTitle\b|\.reviewText\b|\.heroText\b|\.bodyText\b' "app/(markos)/admin/billing/page.module.css" && grep -q "var(--color-surface)" "app/(markos)/admin/billing/page.module.css" && grep -cE 'var\(--space-' "app/(markos)/admin/billing/page.module.css" | awk '$1>=4 {exit 0} $1<4 {exit 1}' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **AB-1 (zero inline hex):** `grep -nE '#[0-9a-fA-F]{3,8}' "app/(markos)/admin/billing/page.module.css"` returns 0.
    - **AB-1 (zero Sora/Space Grotesk):** `grep -nE '(Sora|Space Grotesk)' "app/(markos)/admin/billing/page.module.css"` returns 0.
    - **AB-3 (no legacy teal/light hex):** `grep -nE '#0d9488|#f0fdfa|14px|999px' "app/(markos)/admin/billing/page.module.css"` returns 0.
    - **AB-4 (no off-grid in table recipe):** `grep -nE '12px|14px|#e2e8f0|#334155' "app/(markos)/admin/billing/page.module.css"` returns 0.
    - **No legacy bespoke classes (delegated to primitives JSX-side):** `grep -nE '\.heroCard\b|\.panel\b|\.eyebrow\b|\.title\b|\.sectionTitle\b|\.tableHead\b|\.tableCell\b|\.primaryButton\b|\.secondaryButton\b|\.reviewCard\b|\.reviewTitle\b|\.reviewText\b' "app/(markos)/admin/billing/page.module.css"` returns 0.
    - **X-5 (no per-module pointer-coarse redeclaration):** `grep -nE '@media[^{]*pointer[^{]*coarse' "app/(markos)/admin/billing/page.module.css"` returns 0.
    - **Token consumption (positive assertions):** `grep -c "var(--color-surface)" page.module.css` ≥1; `grep -cE 'var\(--space-' page.module.css` ≥4.
  </acceptance_criteria>
  <done>`page.module.css` rewritten to ~55 LOC token-only layout shell + table recipe. All grep gates pass. Atomic commit: `feat(213.4-02): rewrite admin/billing/page.module.css to design-md tokens (AB-1, AB-3, AB-4)`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update app/(markos)/admin/billing/page.tsx to compose .c-card / .c-button{,--primary,--secondary,--destructive} / .c-modal + .c-backdrop / .c-notice c-notice--{success,warning,error} / .c-badge--{warning,info,success} / .t-label-caps + render semantic table (AB-2, AB-5, AB-6, AB-7)</name>
  <read_first>
    - app/(markos)/admin/billing/page.tsx (FULL 148 LOC — DOM tree to preserve per CONTEXT D-05; identify reviewCard variant prop, reconciliation row state field, write-off action handler)
    - app/(markos)/admin/billing/page.module.css (post-Task 1 shape — confirm class names referenced)
    - .planning/phases/213.4-.../213.4-UI-SPEC.md AB-2..AB-7 + Copywriting table (verbatim copy strings) + Primitive reuse table
    - .planning/phases/213.4-.../213.4-RESEARCH.md "Class-Rename Map: Plan 213.4-02" (lines 88–123) JSX changes section + Notice/Banner Audit (admin/billing rows)
    - styles/components.css v1.1.0 (.c-card, .c-button{,--primary,--secondary,--destructive}, .c-notice{,--success,--warning,--error}, .c-badge{,--success,--warning,--error,--info}, .c-modal, .c-backdrop)
    - app/globals.css (.t-label-caps, .t-lead utilities)
    - .planning/phases/213.3-.../213.3-01-PLAN.md Task 2 (precedent: same JSX-swap pattern — billing-hold → .c-notice c-notice--warning + Upgrade plan → .c-button--primary)
    - DESIGN.md "Components > Notice / Banner" + "Components > Buttons" + "Components > Modal" + "Components > Badges"
    - app/(markos)/admin/layout.tsx (CONFIRM existence + role-gating preservation per CONTEXT D-22; do NOT modify)
  </read_first>
  <files>app/(markos)/admin/billing/page.tsx</files>
  <action>
**Apply the following className substitutions and JSX shape changes throughout `app/(markos)/admin/billing/page.tsx` (DOM tree + prop interface UNCHANGED per CONTEXT D-05):**

| Existing JSX | New JSX | Token / primitive citation |
|--------------|---------|---------------------------|
| `<div className={styles.heroCard}>` | `<div className="c-card">` | `.c-card` provides surface-raised bg + 1px hairline + 8px radius + `var(--space-md)` padding |
| `<div className={styles.panel}>` | `<div className="c-card">` | Same |
| `<span className={styles.eyebrow}>...</span>` | `<span className="t-label-caps">...</span>` | `.t-label-caps` utility from globals.css |
| `<h1 className={styles.title}>Reconciliation queue</h1>` | `<h1>Reconciliation queue</h1>` | globals.css `<h1>` rule |
| `<h2 className={styles.sectionTitle}>...</h2>` | `<h4>...</h4>` (semantic — section heading inside card) | globals.css `<h4>` rule |
| `<p className={styles.heroText}>...</p>` | `<p className="t-lead">...</p>` for descriptors | `.t-lead` utility |
| `<button className={styles.primaryButton}>Review Billing Evidence</button>` | `<button type="button" className="c-button c-button--primary">Review Billing Evidence</button>` | `.c-button c-button--primary` |
| `<button className={styles.secondaryButton}>Place Hold</button>` (and Release Hold, Retry Provider Sync) | `<button type="button" className="c-button c-button--secondary">{label}</button>` | `.c-button c-button--secondary` |
| `<button className={styles.tableHead}>...</button>` | `<th scope="col">...</th>` (semantic upgrade) | token-recipe inherited via `.table th` selector |
| `<div className={styles.tab}>` (queue tab) | retain `className={`${styles.tab} ${isActive ? styles.activeTab : ''}`}` | layout class still token-cited (post-Task 1) |
| `<li className={styles.evidenceItem}>` | retain `className={`${styles.evidenceItem} ${isActive ? styles.activeEvidenceItem : ''}`}` | retained per RESEARCH evidence-rail exception |

**AB-7 — Replace `.reviewCard` composition with variant-driven `.c-notice`:**

Find the existing JSX that renders the reconciliation review card (currently uses `styles.reviewCard` + `styles.reviewTitle` + `styles.reviewText` driven by a `variant` prop with values `healthy` / `hold` / `syncFailure`):

```tsx
{/* Before (representative shape — actual prop names per file) */}
<div className={`${styles.reviewCard} ${styles[`reviewCard--${variant}`]}`}>
  <h4 className={styles.reviewTitle}>{title}</h4>
  <p className={styles.reviewText}>{body}</p>
</div>
```

Replace with the canonical `.c-notice c-notice--{state}` composition driven by the variant. Use a small mapper (inline `const` or extracted helper) that returns the primitive class string + the bracketed glyph + the canonical copy string per UI-SPEC AB Copywriting:

```tsx
{/* After */}
const noticeMap = {
  healthy:     { cls: 'c-notice c-notice--success', glyph: '[ok]',   copy: 'Billing evidence reconciled. No operator intervention required.' },
  hold:        { cls: 'c-notice c-notice--warning', glyph: '[warn]', copy: 'Billing hold active. Restricted actions paused until billing health is restored.' },
  syncFailure: { cls: 'c-notice c-notice--error',   glyph: '[err]',  copy: 'Provider sync failure unresolved. Review evidence to reconcile or place a hold.' },
} as const;

const { cls, glyph, copy } = noticeMap[variant];

return (
  <div className={cls} role="status">
    <strong>{glyph}</strong>{" "}{copy}
  </div>
);
```

KEY rules (verifiable per UI-SPEC AB-7):
- `<div className="c-notice c-notice--{state}">` — exact primitive class string; no `${styles.X}` interpolation.
- `<strong>{glyph}</strong>` carries the bracketed-glyph prefix (`[ok]` / `[warn]` / `[err]`).
- `{" "}` JSX-safe space between glyph and body.
- Copy strings are EXACTLY as specified above (UI-SPEC AB-7 verbatim register).
- `role="status"` for SR announcement on dynamic state appearance (live-region semantics; consult Phase 213.1 RotationGraceBanner precedent).
- All three variants render through the same composition — no separate `if/else` JSX trees.

**AB-5 — Add row state badges with bracketed glyphs:**

For each reconciliation row, render a `<span className="c-badge c-badge--{state}">[glyph] {label}</span>` cell driven by the row's status field per UI-SPEC mapping:

```tsx
const badgeMap = {
  Mismatch:        { cls: 'c-badge c-badge--warning', glyph: '[warn]' },
  'Sync failed':   { cls: 'c-badge c-badge--warning', glyph: '[warn]' },
  'Pending review':{ cls: 'c-badge c-badge--info',    glyph: '[info]' },
  Reconciled:      { cls: 'c-badge c-badge--success', glyph: '[ok]' },
  Released:        { cls: 'c-badge c-badge--success', glyph: '[ok]' },
  Restored:        { cls: 'c-badge c-badge--success', glyph: '[ok]' },
  Hold:            { cls: 'c-badge c-badge--warning', glyph: '[warn]' },
} as const;

// Inside row render:
<td>
  <span className={badgeMap[row.status].cls}>
    {badgeMap[row.status].glyph} {row.status}
  </span>
</td>
```

**AB-6 — Render the invoice list as a semantic `<table>`** with `<thead>` / `<tbody>` / `<tr>` / `<th scope="col">` / `<td>` per the recipe defined in module.css (`.table th` / `.table td` selectors apply automatically via `<table className={styles.table}>`). No className on `<th>` / `<td>`.

**AB-6 — Replace the write-off action with `.c-modal + .c-backdrop` confirm:**

The "Write off" action MUST be `.c-button c-button--destructive`. On click, render a confirm modal:

```tsx
{showWriteOff && (
  <>
    <div className="c-backdrop" onClick={cancelWriteOff} />
    <div className="c-modal" role="dialog" aria-modal="true" aria-labelledby="write-off-title">
      <h3 id="write-off-title">Write off this billing discrepancy?</h3>
      <p>This action is logged as operator evidence and cannot be undone.</p>
      <div className={styles.buttonRow}>
        <button type="button" className="c-button c-button--secondary" onClick={cancelWriteOff}>Cancel</button>
        <button type="button" className="c-button c-button--destructive" onClick={confirmWriteOff}>Write off</button>
      </div>
    </div>
  </>
)}
```

**Banned-lexicon check:** Scan all visible copy strings. Reject any of: `synergy|leverage|empower|unlock|transform|revolutionize|supercharge|holistic|seamless|cutting-edge|innovative|game-changer|next-generation|world-class|best-in-class|reimagine|disrupt|just (as softener)|Please`. NO exclamation points. NO emoji.

**Preservation:** Phase 205 Pricing Engine wiring (any `await` calls to billing API, Pricing Engine handles), `'use client'` directive (if any), prop interfaces on subcomponents — ALL preserved. Admin role-gating in `app/(markos)/admin/layout.tsx` UNTOUCHED per D-22.

After the edit, run `npm run build` — MUST exit 0.
  </action>
  <verify>
    <automated>grep -qE 'c-notice c-notice--(success|warning|error)' "app/(markos)/admin/billing/page.tsx" && grep -qE '\[ok\]|\[warn\]|\[err\]' "app/(markos)/admin/billing/page.tsx" && grep -qE 'c-button c-button--primary' "app/(markos)/admin/billing/page.tsx" && grep -qE 'c-button c-button--secondary' "app/(markos)/admin/billing/page.tsx" && grep -qE 'c-button c-button--destructive' "app/(markos)/admin/billing/page.tsx" && grep -qE 'c-modal' "app/(markos)/admin/billing/page.tsx" && grep -qE 'c-backdrop' "app/(markos)/admin/billing/page.tsx" && grep -qE 'c-badge c-badge--(warning|info|success)' "app/(markos)/admin/billing/page.tsx" && grep -qE '<table' "app/(markos)/admin/billing/page.tsx" && ! grep -qE 'styles\.(heroCard|panel|primaryButton|secondaryButton|reviewCard|reviewTitle|reviewText|tableHead|tableCell|eyebrow|title|sectionTitle)' "app/(markos)/admin/billing/page.tsx" && ! grep -inE 'Please|synergy|leverage|empower|unlock|revolutionize|supercharge|holistic|seamless|cutting-edge|innovative|game-changer|next-generation|world-class|best-in-class|reimagine|disrupt' "app/(markos)/admin/billing/page.tsx" && npm run build && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **AB-2 (.c-card on hero/panel):** `grep -cE '"c-card"' "app/(markos)/admin/billing/page.tsx"` ≥1.
    - **AB-5 (badge primitives + bracketed glyphs):** `grep -cE 'c-badge c-badge--(warning|info|success)' "app/(markos)/admin/billing/page.tsx"` ≥1; `grep -cE '\[(ok|warn|info|err)\]' "app/(markos)/admin/billing/page.tsx"` ≥3.
    - **AB-6 (button + modal primitives + semantic table):** `grep -cE 'c-button c-button--primary' page.tsx` ≥1; `grep -cE 'c-button c-button--secondary' page.tsx` ≥1; `grep -cE 'c-button c-button--destructive' page.tsx` ≥1; `grep -cE 'c-modal' page.tsx` ≥1; `grep -cE 'c-backdrop' page.tsx` ≥1; `grep -c '<table' page.tsx` ≥1.
    - **AB-7 (.c-notice variant-driven):** `grep -cE 'c-notice c-notice--(success|warning|error)' page.tsx` ≥3 (one per variant, OR one mapper-driven composition with 3 entries in the mapper).
    - **No legacy class refs:** `grep -cE 'styles\.(heroCard|panel|primaryButton|secondaryButton|reviewCard|reviewTitle|reviewText|tableHead|tableCell|eyebrow|title|sectionTitle)' page.tsx` returns 0.
    - **Banned-lexicon scan = 0:** `grep -inE 'Please|synergy|leverage|empower|unlock|revolutionize|supercharge|holistic|seamless|cutting-edge|innovative|game-changer|next-generation|world-class|best-in-class|reimagine|disrupt' page.tsx` returns 0.
    - **Build green:** `npm run build` exits 0.
  </acceptance_criteria>
  <done>`page.tsx` updated to compose primitives + render `.c-notice c-notice--{state}` for review banner + `.c-modal` for write-off confirm + `.c-badge` row state + semantic `<table>`; legacy class refs eliminated; banned-lexicon = 0; build green. Atomic commit: `feat(213.4-02): compose primitives in admin/billing/page.tsx (AB-2, AB-5, AB-6, AB-7)`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend app/(markos)/admin/billing/reconciliation.stories.tsx with ≥3 named state stories (Healthy / HoldState / SyncFailure) per UI-SPEC AB-8 + run all verification gates</name>
  <read_first>
    - app/(markos)/admin/billing/reconciliation.stories.tsx (FULL — confirm existing default export shape; extend, don't replace)
    - app/(markos)/admin/billing/page.tsx (post-Task 2 shape — confirm prop interface for stories args, particularly the `variant` prop and reconciliation row data shape)
    - .planning/phases/213.4-.../213.4-UI-SPEC.md AB-8 + Storybook State Coverage table (lines 286–294)
    - .planning/phases/213.4-.../213.4-CONTEXT.md D-17 (story state coverage)
    - .planning/phases/213.3-.../213.3-01-PLAN.md Task 3 (precedent CSF3 pattern)
    - .planning/phases/213.3-.../213.3-01-SUMMARY.md (precedent — Storybook 8 CSF3 typed `meta` default + named state exports)
  </read_first>
  <files>app/(markos)/admin/billing/reconciliation.stories.tsx</files>
  <action>
**Extend `app/(markos)/admin/billing/reconciliation.stories.tsx` with ≥3 named state stories per UI-SPEC AB-8.**

The existing file likely has a `meta` default-export with limited state-coverage exports. Extend (do not replace) by adding the following named exports per UI-SPEC line 287–293:

```tsx
import type { Meta, StoryObj } from '@storybook/react';
import AdminBillingPage from './page';

const meta = {
  title: 'Admin/Billing',
  component: AdminBillingPage,
  parameters: {
    layout: 'fullscreen',
  },
} satisfies Meta<typeof AdminBillingPage>;

export default meta;

type Story = StoryObj<typeof meta>;

export const Healthy: Story = {
  args: {
    variant: 'healthy',
    rows: [
      { id: 'rec_001', tenant: 'acme', status: 'Reconciled', amount: '$120.00' },
      { id: 'rec_002', tenant: 'globex', status: 'Released', amount: '$80.00' },
    ],
  },
  parameters: {
    docs: {
      description: {
        story: 'Renders .c-notice c-notice--success per UI-SPEC AC AB-7. [ok] glyph + reconciled-state copy. .c-badge--success on row state.',
      },
    },
  },
};

export const HoldState: Story = {
  args: {
    variant: 'hold',
    rows: [
      { id: 'rec_003', tenant: 'initech', status: 'Hold', amount: '$240.00' },
      { id: 'rec_004', tenant: 'acme', status: 'Pending review', amount: '$60.00' },
    ],
  },
  parameters: {
    docs: {
      description: {
        story: 'Renders .c-notice c-notice--warning per UI-SPEC AC AB-7. [warn] glyph + billing-hold copy. .c-badge--warning + .c-badge--info row states.',
      },
    },
  },
};

export const SyncFailure: Story = {
  args: {
    variant: 'syncFailure',
    rows: [
      { id: 'rec_005', tenant: 'umbrella', status: 'Sync failed', amount: '$420.00' },
      { id: 'rec_006', tenant: 'tyrell', status: 'Mismatch', amount: '$180.00' },
    ],
  },
  parameters: {
    docs: {
      description: {
        story: 'Renders .c-notice c-notice--error per UI-SPEC AC AB-7. [err] glyph + provider-sync-failure copy. .c-badge--warning row states + .c-button--destructive Write-off CTA.',
      },
    },
  },
};
```

KEY rules (verifiable per UI-SPEC AB-8 + D-17):
- `Meta<typeof AdminBillingPage>` — Storybook 8 CSF3 typed `meta` default-export.
- 3 named state exports: `Healthy`, `HoldState`, `SyncFailure` (matches UI-SPEC line 287–293 verbatim).
- `args` shape MUST mirror the actual prop interface of `page.tsx` (executor inspects post-Task 2 file to confirm exact prop names — replace placeholder shape if needed; the variant + row shape above is representative).
- `meta.title = 'Admin/Billing'` exactly per UI-SPEC AB-8.
- DOES NOT set `parameters.theme: "legacy"` — must render against DESIGN.md v1.1.0 tokens (Phase 213.1 Plan-03 preview.tsx wiring).

**If the existing file already has named exports** (e.g., `Default`), keep them but rename or replace one to satisfy the 3-state minimum (Healthy / HoldState / SyncFailure). The minimum is the named-state-coverage minimum, not an additive minimum.

After the edit, run `npm run build-storybook` — MUST exit 0. Run `npm test` — MUST exit 0 (Phase 200/201/202/203/204/205/207 wiring suites stay green).
  </action>
  <verify>
    <automated>test -f "app/(markos)/admin/billing/reconciliation.stories.tsx" && grep -cE '^export const (Healthy|HoldState|SyncFailure):' "app/(markos)/admin/billing/reconciliation.stories.tsx" | awk '$1>=3 {exit 0} $1<3 {exit 1}' && grep -q "title: 'Admin/Billing'" "app/(markos)/admin/billing/reconciliation.stories.tsx" && ! grep -qE 'parameters\.theme.*"legacy"' "app/(markos)/admin/billing/reconciliation.stories.tsx" && grep -q 'export default meta' "app/(markos)/admin/billing/reconciliation.stories.tsx" && npm run build-storybook && npm test && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **AB-8 (≥3 named state stories):** `grep -cE '^export const (Healthy|HoldState|SyncFailure):' "app/(markos)/admin/billing/reconciliation.stories.tsx"` ≥3.
    - **`meta.title = 'Admin/Billing'`:** `grep -c "title: 'Admin/Billing'" reconciliation.stories.tsx` ≥1.
    - **Default-not-legacy theme:** `grep -cE 'parameters\.theme.*"legacy"' reconciliation.stories.tsx` returns 0.
    - **`Meta<typeof ...>` typed default:** `grep -c 'export default meta' reconciliation.stories.tsx` ≥1.
    - **Storybook build green:** `npm run build-storybook` exits 0.
    - **Wiring suites green:** `npm test` exits 0 (Phase 200/201/202/203/204/205/207 preserved).
  </acceptance_criteria>
  <done>`reconciliation.stories.tsx` extended with 3 named state stories; CSF3 typed `meta` default; `npm run build-storybook` + `npm test` green. Atomic commit: `feat(213.4-02): extend admin/billing/reconciliation.stories.tsx with Healthy/HoldState/SyncFailure (AB-8)`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator→admin/billing API | Authenticated operator role accessing billing reconciliation actions; Phase 205 Pricing Engine wiring crosses here |
| admin role-gate | `app/(markos)/admin/layout.tsx` enforces `iam_role IN (owner, admin)` — preserved verbatim per CONTEXT D-22 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213.4-02-01 | Tampering | write-off action | mitigate | `.c-modal` confirm with explicit "logged as operator evidence and cannot be undone" copy; server-side audit-log emit unchanged from Phase 207 substrate |
| T-213.4-02-02 | Elevation of Privilege | admin role-gate | accept | `app/(markos)/admin/layout.tsx` role guards UNTOUCHED per D-22; redesign is className-only |
| T-213.4-02-03 | Information Disclosure | reconciliation row data | accept | Row data sourced from authenticated billing API (Phase 205 wiring); className-only redesign does not alter data exposure |
</threat_model>

<verification>
After all 3 tasks complete, run the executor verification gates per CONTEXT D-24:

```bash
grep -nE '#[0-9a-fA-F]{3,8}' "app/(markos)/admin/billing/page.module.css"  # 0
grep -nE '(Sora|Space Grotesk|linear-gradient|radial-gradient|box-shadow|transform:\s*translate)' "app/(markos)/admin/billing/page.module.css"  # 0
grep -nE '#0d9488|#0f766e|#14b8a6|#155e75|#f8fbfd|#ffffff|#0f172a|#475569' "app/(markos)/admin/billing/page.module.css"  # 0
grep -cE 'c-notice c-notice--(success|warning|error)' "app/(markos)/admin/billing/page.tsx"  # ≥3
grep -cE 'c-button c-button--(primary|secondary|destructive)' "app/(markos)/admin/billing/page.tsx"  # ≥3
grep -cE '^export const (Healthy|HoldState|SyncFailure):' "app/(markos)/admin/billing/reconciliation.stories.tsx"  # ≥3
npm run build  # exit 0
npm run build-storybook  # exit 0
npm test  # exit 0
```
</verification>

<success_criteria>
- All `must_haves.truths` verifiable via the verification commands above.
- UI-SPEC AC AB-1..AB-8 close.
- Phase 205 Pricing Engine wiring + admin role-gating preserved (CONTEXT D-22).
- Phase 213.1/213.2/213.3 a11y suites + Phase 200/201/202/203/204/207 wiring suites stay green.
- Atomic commit cadence per CONTEXT D-04 (3 commits).
</success_criteria>

<output>
After completion, create `.planning/phases/213.4-.../213.4-02-SUMMARY.md` documenting files modified, AC# closures (AB-1..AB-8), verification command outputs, manual Chromatic review reminder for `Admin/Billing` Healthy/HoldState/SyncFailure stories, hand-off to Plan 213.4-03 (admin/governance).
</output>
</content>
</invoke>