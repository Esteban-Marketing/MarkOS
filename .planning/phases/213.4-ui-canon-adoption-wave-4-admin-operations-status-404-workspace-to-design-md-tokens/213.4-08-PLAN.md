---
phase: 213.4
plan: 08
type: execute
wave: 1
depends_on: ["213.4-01", "213.4-02", "213.4-03", "213.4-04", "213.4-05", "213.4-06", "213.4-07"]
files_modified:
  - app/(markos)/settings/theme/theme.stories.tsx
  - test/ui-a11y/213-4-admin-ops-a11y.test.js
autonomous: true
requirements:
  - T-1
  - T-2
  - T-3
  - T-4
  - T-5
  - T-6
  - T-7
  - X-1
  - X-2
  - X-3
  - X-4
  - X-5
  - X-6
must_haves:
  truths:
    - "app/(markos)/settings/theme/page.tsx UNCHANGED — git diff returns 0 changes (T-1; the 15 LOC switch wrapper is the canonical demonstrator of [data-theme] attribute swap; stories carry the value)"
    - "app/(markos)/settings/theme/theme.stories.tsx extended with ≥3 named state stories (ColorTokens / SpacingTokens / PrimitiveSampler) per UI-SPEC T-2; meta.title='Design/TokenDiagnostic'"
    - "ColorTokens story renders BOTH <div data-theme='dark'> + <div data-theme='light'> panels side-by-side per UI-SPEC T-3; docs description references @media (forced-colors: active) rendering"
    - "test/ui-a11y/213-4-admin-ops-a11y.test.js NEW file with ≥30 individual test() blocks; ≥7 distinct AC# mentions across (AB-7, AG-5, O-4, O-5, OT-3, OT-6, SW-4, F-2, X-1, X-2, X-3); mirrors 213-3-settings-a11y.test.js grep-shape pattern (fs.readFileSync + assert.match)"
    - "X-1: zero `color: var(--color-primary)` text usages across all 6 new module.css files (text contexts use --color-primary-text)"
    - "X-2: zero `height: 6px` meter tracks across all wave files (8px sizing-value parity)"
    - "X-3: task-ui.module.css contains explicit @media (prefers-reduced-motion: reduce) block (the only module with stepCard transitions)"
    - "X-4: banned-lexicon scan = 0 across all 13 modified .tsx files (admin/billing, admin/governance, operations, operations/tasks page + 5 sub-components, status/webhooks, 404-workspace, settings/theme stories)"
    - "X-5: zero `@media (pointer: coarse)` redeclarations of .c-button min-height in any of 6 new module.css files (global rule in styles/components.css covers .c-button + .c-nav-link verbatim)"
    - "X-6: 213.x milestone wave CLOSES — final commit message documents 213.1+213.2+213.3+213.4 = full markos chrome surface in DESIGN.md v1.1.0 canonical contract; .planning/STATE.md updated"
    - "npm run build exits 0; npm run build-storybook exits 0; npm test exits 0; npm run test:ui-a11y -- test/ui-a11y/213-4-admin-ops-a11y.test.js exits 0"
  artifacts:
    - path: "app/(markos)/settings/theme/theme.stories.tsx"
      provides: "Storybook 8 CSF3 ≥3 named state exports (ColorTokens / SpacingTokens / PrimitiveSampler) — design-system visual diagnostic"
      exports: ["default", "ColorTokens", "SpacingTokens", "PrimitiveSampler"]
    - path: "test/ui-a11y/213-4-admin-ops-a11y.test.js"
      provides: "NEW grep-shape a11y suite — ≥30 tests, ≥7 AC# mentions covering AB-7, AG-5, O-4, O-5, OT-3, OT-6, SW-4, F-2, X-1..X-3"
      min_lines: 250
      contains: "213.4"
  key_links:
    - from: "app/(markos)/settings/theme/theme.stories.tsx"
      to: "app/tokens.css [data-theme] attribute cascade"
      via: "ColorTokens story renders dark + light panels; demonstrates token-system contract"
      pattern: "data-theme=[\"'](dark|light)[\"']"
    - from: "test/ui-a11y/213-4-admin-ops-a11y.test.js"
      to: "All 6 new module.css + 13 modified .tsx files"
      via: "fs.readFileSync + assert.match grep-shape verifications mirroring 213-3-settings-a11y.test.js"
      pattern: "fs\\.readFileSync"
    - from: "All 6 new module.css files"
      to: "styles/components.css @media (pointer: coarse) global rule"
      via: "X-5: zero per-module .c-button min-height redeclarations — global covers all consumers"
      pattern: "pointer.*coarse"
---

<objective>
Cross-cutting closure plan for the 213.x milestone wave. Three concurrent deliverables:

1. **Theme-toggle stories (T-2, T-3):** Extend `theme.stories.tsx` with ≥3 named state stories (`ColorTokens` / `SpacingTokens` / `PrimitiveSampler`) — the canonical visual diagnostic for the design-system contract. `ColorTokens` story renders both `data-theme="dark"` and `data-theme="light"` panels side-by-side. `settings/theme/page.tsx` UNCHANGED per T-1 (15 LOC switch wrapper preserved).

2. **A11y test suite (T-4):** Create `test/ui-a11y/213-4-admin-ops-a11y.test.js` with ≥30 individual `test(...)` blocks across all 6 new module.css + 13 modified .tsx files, mirroring `213-3-settings-a11y.test.js` grep-shape pattern. ≥7 AC# mentions targeting AB-7 / AG-5 / O-4 / O-5 / OT-3 / OT-6 / SW-4 / F-2 / X-1 / X-2 / X-3.

3. **Cross-cutting verification (X-1..X-6, T-5..T-7):** Verify (pointer: coarse) inheritance (no per-module redeclarations); confirm `--color-primary` is never used as text color across the 6 new module.css files; confirm `height: 8px` parity for meter tracks; confirm `prefers-reduced-motion` block in task-ui; confirm banned-lexicon = 0 across all 13 .tsx files; confirm full `npm test` + `npm run build` + `npm run build-storybook` green. Final wave commit documents 213.x milestone closure.

**This plan depends on all 7 prior 213.4 plans completing.** It runs LAST in the wave per CONTEXT D-11.

Output: `theme.stories.tsx` extended; NEW `test/ui-a11y/213-4-admin-ops-a11y.test.js`; `.planning/STATE.md` milestone update; final wave commit.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/213.4-ui-canon-adoption-wave-4-admin-operations-status-404-workspace-to-design-md-tokens/213.4-CONTEXT.md
@.planning/phases/213.4-ui-canon-adoption-wave-4-admin-operations-status-404-workspace-to-design-md-tokens/213.4-UI-SPEC.md
@.planning/phases/213.4-ui-canon-adoption-wave-4-admin-operations-status-404-workspace-to-design-md-tokens/213.4-RESEARCH.md
@DESIGN.md
@CLAUDE.md
@app/tokens.css
@app/globals.css
@styles/components.css
@app/(markos)/settings/theme/page.tsx
@app/(markos)/settings/theme/theme.stories.tsx
@test/ui-a11y/213-3-settings-a11y.test.js
@test/ui-a11y/213-2-auth-a11y.test.js
@test/ui-a11y/213-1-chrome-a11y.test.js
@.planning/STATE.md

<interfaces>
<!-- T-2 stories (UI-SPEC line 605): -->
<!-- ColorTokens: full color token grid with 30+ var(--color-*) swatches, dark + light panels side-by-side -->
<!-- SpacingTokens: 8px grid visualization with all var(--space-*) steps (none/xxs/xs/sm/md/lg/xl/xxl) -->
<!-- PrimitiveSampler: one of each .c-* primitive (.c-card, .c-button{,--primary,--secondary,--tertiary,--destructive,--icon}, .c-input, .c-field, .c-notice{,--success,--warning,--error,--info}, .c-badge{,--*}, .c-status-dot{,--live,--error}, .c-chip{,--warning,--error}, .c-chip-protocol, .c-modal preview, .c-toast{,--*}, .c-code-inline) on a single canvas -->

<!-- T-4 test mirror (213-3-settings-a11y.test.js): -->
<!-- 5-line node:test opener: import { test } from 'node:test'; import assert from 'node:assert/strict'; import fs from 'node:fs'; -->
<!-- read() helper: const read = (p) => fs.readFileSync(p, 'utf8'); -->
<!-- AC# section comments: // ===== AB-* (admin/billing) ===== / // ===== AG-* (admin/governance) ===== / etc -->

<!-- ≥30 test allocation (RESEARCH "Test Strategy"): -->
<!-- admin/billing: 6 tests (zero-hex, c-card, c-notice variants, c-badge row state, c-button primitives, stories count) -->
<!-- admin/governance: 5 tests (zero-hex, c-card, c-notice state, c-badge decision, stories count) -->
<!-- operations root: 5 tests (zero-hex, no gradient, c-notice denied, c-badge auth, stories count) -->
<!-- operations/tasks: 10 tests (zero-hex, no gradient/shadow/jiggle, c-badge 5 states, c-notice 3 helper variants, c-chip-protocol, stories count) -->
<!-- status/webhooks: 5 tests (zero-hex, c-notice 3 states, c-status-dot primitives, no !important) -->
<!-- 404-workspace: 4 tests (zero-hex, c-card--feature, c-button--primary, notFound preserved) -->
<!-- cross-cutting: 5 tests (no --color-primary as text X-1, no min-height.*44 redeclarations X-5, prefers-reduced-motion in task-ui X-3, banned-lexicon = 0 in key TSX X-4, build/test sentinel) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend app/(markos)/settings/theme/theme.stories.tsx with ≥3 named state stories (ColorTokens / SpacingTokens / PrimitiveSampler) per UI-SPEC T-2 + T-3; settings/theme/page.tsx UNCHANGED per T-1</name>
  <read_first>
    - app/(markos)/settings/theme/theme.stories.tsx (FULL — confirm existing meta default)
    - app/(markos)/settings/theme/page.tsx (FULL 15 LOC — confirm; do NOT modify per T-1)
    - app/tokens.css (token name reference for swatch enumeration — color tokens, space tokens)
    - styles/components.css v1.1.0 (primitive list for PrimitiveSampler)
    - .planning/phases/213.4-.../213.4-UI-SPEC.md T-1..T-3 + Storybook State Coverage row 7
    - .planning/phases/213.4-.../213.4-CONTEXT.md "Claude's Discretion" theme-toggle story density (recommend ≥3 swatch panels)
    - DESIGN.md "Color > Tokens" + "Spacing Scale" + "Components" sections
  </read_first>
  <files>app/(markos)/settings/theme/theme.stories.tsx</files>
  <action>
**Extend with 3 named state stories per UI-SPEC T-2 + T-3:**

```tsx
import type { Meta, StoryObj } from '@storybook/react';

const meta = {
  title: 'Design/TokenDiagnostic',
  parameters: { layout: 'fullscreen' },
} satisfies Meta;

export default meta;
type Story = StoryObj<typeof meta>;

const COLOR_TOKENS = [
  '--color-surface', '--color-surface-raised', '--color-surface-overlay',
  '--color-on-surface', '--color-on-surface-muted', '--color-on-surface-subtle',
  '--color-primary', '--color-primary-text', '--color-primary-subtle',
  '--color-success', '--color-warning', '--color-error', '--color-info',
  '--color-border', '--color-border-strong',
];

const SPACE_TOKENS = ['--space-none', '--space-xxs', '--space-xs', '--space-sm', '--space-md', '--space-lg', '--space-xl', '--space-xxl'];

function ColorPanel({ theme }: { theme: 'dark' | 'light' }) {
  return (
    <div data-theme={theme} style={{ padding: 'var(--space-md)', background: 'var(--color-surface)' }}>
      <h3 style={{ color: 'var(--color-on-surface)', fontFamily: 'var(--font-mono)' }}>data-theme="{theme}"</h3>
      <div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: 'var(--space-xs)' }}>
        {COLOR_TOKENS.map((token) => (
          <div key={token} style={{ display: 'flex', alignItems: 'center', gap: 'var(--space-xs)', color: 'var(--color-on-surface)' }}>
            <span style={{ width: 24, height: 24, background: `var(${token})`, border: '1px solid var(--color-border)', borderRadius: 'var(--radius-sm)' }} aria-hidden="true" />
            <code style={{ fontFamily: 'var(--font-mono)', fontSize: 'var(--fs-body-sm)' }}>{token}</code>
          </div>
        ))}
      </div>
    </div>
  );
}

export const ColorTokens: Story = {
  render: () => (
    <div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 'var(--space-md)' }}>
      <ColorPanel theme="dark" />
      <ColorPanel theme="light" />
    </div>
  ),
  parameters: {
    docs: {
      description: {
        story: 'Color tokens visualized across both [data-theme="dark"] and [data-theme="light"] cascades. Forced-colors mode (Windows High Contrast) renders surface as Canvas, on-surface as CanvasText, primary as LinkText — verified at runtime via @media (forced-colors: active) cascade in app/tokens.css. Per UI-SPEC T-3.',
      },
    },
  },
};

export const SpacingTokens: Story = {
  render: () => (
    <div data-theme="dark" style={{ padding: 'var(--space-lg)', background: 'var(--color-surface)' }}>
      <h3 style={{ color: 'var(--color-on-surface)', fontFamily: 'var(--font-mono)' }}>8px grid (--space-* tokens)</h3>
      <div style={{ display: 'grid', gap: 'var(--space-sm)' }}>
        {SPACE_TOKENS.map((token) => (
          <div key={token} style={{ display: 'flex', alignItems: 'center', gap: 'var(--space-md)' }}>
            <code style={{ width: 140, fontFamily: 'var(--font-mono)', color: 'var(--color-on-surface-muted)' }}>{token}</code>
            <div style={{ width: `var(${token})`, height: 24, background: 'var(--color-primary)', minWidth: 1 }} aria-hidden="true" />
          </div>
        ))}
      </div>
    </div>
  ),
  parameters: { docs: { description: { story: '8px grid visualization. Every value snaps to 0 / 2 / 8 / 16 / 24 / 32 / 48 / 96 px. Off-grid values are bugs (DESIGN.md "Spacing Scale").' } } },
};

export const PrimitiveSampler: Story = {
  render: () => (
    <div data-theme="dark" style={{ padding: 'var(--space-lg)', background: 'var(--color-surface)', display: 'grid', gap: 'var(--space-md)' }}>
      <h3 style={{ color: 'var(--color-on-surface)', fontFamily: 'var(--font-mono)' }}>Primitive sampler — styles/components.css</h3>

      <div className="c-card">
        <h4>.c-card section</h4>
        <p>Default surface-raised card with 1px hairline border.</p>
      </div>

      <div style={{ display: 'flex', gap: 'var(--space-xs)', flexWrap: 'wrap' }}>
        <button type="button" className="c-button c-button--primary">Primary</button>
        <button type="button" className="c-button c-button--secondary">Secondary</button>
        <button type="button" className="c-button c-button--tertiary">Tertiary</button>
        <button type="button" className="c-button c-button--destructive">Destructive</button>
      </div>

      <div className="c-notice c-notice--success"><strong>[ok]</strong>{" "}Success notice.</div>
      <div className="c-notice c-notice--warning"><strong>[warn]</strong>{" "}Warning notice.</div>
      <div className="c-notice c-notice--error"><strong>[err]</strong>{" "}Error notice.</div>
      <div className="c-notice c-notice--info"><strong>[info]</strong>{" "}Info notice.</div>

      <div style={{ display: 'flex', gap: 'var(--space-xs)', flexWrap: 'wrap' }}>
        <span className="c-badge c-badge--success">[ok] Success</span>
        <span className="c-badge c-badge--warning">[warn] Warning</span>
        <span className="c-badge c-badge--error">[err] Error</span>
        <span className="c-badge c-badge--info">[info] Info</span>
      </div>

      <div style={{ display: 'flex', gap: 'var(--space-sm)' }}>
        <span><span className="c-status-dot c-status-dot--live" /> live</span>
        <span><span className="c-status-dot" /> default</span>
        <span><span className="c-status-dot c-status-dot--error" /> error</span>
      </div>

      <div style={{ display: 'flex', gap: 'var(--space-xs)' }}>
        <span className="c-chip">.c-chip</span>
        <span className="c-chip-protocol">[task_xxx]</span>
      </div>

      <div className="c-field">
        <label className="c-field__label" htmlFor="sampler-input">.c-field label</label>
        <input id="sampler-input" type="text" className="c-input" placeholder="placeholder text" />
        <span className="c-field__help">.c-field__help text</span>
      </div>
    </div>
  ),
  parameters: { docs: { description: { story: 'One of each .c-* primitive on a single canvas. The canonical contributor visual diagnostic.' } } },
};
```

**KEY rules (per T-1, T-2, T-3):**
- `meta.title='Design/TokenDiagnostic'` exactly per UI-SPEC T-2.
- 3 named exports: `ColorTokens` / `SpacingTokens` / `PrimitiveSampler`.
- `ColorTokens` renders BOTH `data-theme="dark"` AND `data-theme="light"` panels side-by-side per T-3.
- Stories docs description includes `@media (forced-colors: active)` reference per T-3.
- `settings/theme/page.tsx` UNTOUCHED (T-1 — `git diff` returns 0 changes; do NOT modify).
- No `parameters.theme: "legacy"`.

After edit: `npm run build-storybook` MUST exit 0.
  </action>
  <verify>
    <automated>test -f "app/(markos)/settings/theme/theme.stories.tsx" && grep -cE '^export const (ColorTokens|SpacingTokens|PrimitiveSampler):' "app/(markos)/settings/theme/theme.stories.tsx" | awk '$1>=3 {exit 0} $1<3 {exit 1}' && grep -q "title: 'Design/TokenDiagnostic'" "app/(markos)/settings/theme/theme.stories.tsx" && grep -qE 'data-theme=["\x27]dark["\x27]' "app/(markos)/settings/theme/theme.stories.tsx" && grep -qE 'data-theme=["\x27]light["\x27]' "app/(markos)/settings/theme/theme.stories.tsx" && grep -qE 'forced-colors' "app/(markos)/settings/theme/theme.stories.tsx" && ! grep -qE 'parameters\.theme.*"legacy"' "app/(markos)/settings/theme/theme.stories.tsx" && grep -q 'export default meta' "app/(markos)/settings/theme/theme.stories.tsx" && git diff --quiet "app/(markos)/settings/theme/page.tsx" && npm run build-storybook && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **T-1 (page.tsx UNCHANGED):** `git diff --quiet app/(markos)/settings/theme/page.tsx` succeeds (zero diff).
    - **T-2 (≥3 named state stories):** `grep -cE '^export const (ColorTokens|SpacingTokens|PrimitiveSampler):' theme.stories.tsx` ≥3.
    - **T-2 (`meta.title='Design/TokenDiagnostic'`):** ≥1.
    - **T-3 (dark + light data-theme panels):** `grep -cE 'data-theme=["\x27]dark["\x27]' theme.stories.tsx` ≥1; `grep -cE 'data-theme=["\x27]light["\x27]' theme.stories.tsx` ≥1; `grep -cE 'forced-colors' theme.stories.tsx` ≥1.
    - **No legacy theme; Storybook build green.**
  </acceptance_criteria>
  <done>`theme.stories.tsx` extended; `page.tsx` untouched. Atomic commit: `feat(213.4-08): extend settings/theme/theme.stories.tsx with ColorTokens/SpacingTokens/PrimitiveSampler design-system diagnostic (T-1, T-2, T-3)`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create NEW test/ui-a11y/213-4-admin-ops-a11y.test.js with ≥30 individual test() blocks across 6 new module.css + 13 modified .tsx files; ≥7 AC# mentions; mirror 213-3-settings-a11y.test.js grep-shape pattern (T-4)</name>
  <read_first>
    - test/ui-a11y/213-3-settings-a11y.test.js (FULL — 44-test precedent; copy 5-line node:test opener + read() helper + section comments)
    - test/ui-a11y/213-2-auth-a11y.test.js (precedent — 25 tests / 21 AC# mentions)
    - test/ui-a11y/213-1-chrome-a11y.test.js (precedent — 22 tests)
    - .planning/phases/213.4-.../213.4-UI-SPEC.md T-4 + X-1..X-6
    - .planning/phases/213.4-.../213.4-RESEARCH.md "Test Strategy" (≥30 tests; ≥7 AC# mentions per AB-7/AG-5/O-4/O-5/OT-3/OT-6/SW-4/F-2/X-1..X-3)
    - All 6 new module.css files (post-Plans 02–07)
    - All 13 modified .tsx files (post-Plans 02–07)
  </read_first>
  <files>test/ui-a11y/213-4-admin-ops-a11y.test.js</files>
  <action>
**Create NEW `test/ui-a11y/213-4-admin-ops-a11y.test.js` mirroring 213-3-settings-a11y.test.js:**

```js
// Phase 213.4 admin/operations/status/404/theme a11y suite — grep-shape verifications
// covering 6 new module.css + 13 modified .tsx files. ≥30 tests, ≥7 AC# mentions
// targeting AB-7, AG-5, O-4, O-5, OT-3, OT-6, SW-4, F-2, X-1, X-2, X-3.
import { test } from 'node:test';
import assert from 'node:assert/strict';
import fs from 'node:fs';

const read = (p) => fs.readFileSync(p, 'utf8');

// File path constants
const ADMIN_BILLING_CSS = 'app/(markos)/admin/billing/page.module.css';
const ADMIN_BILLING_TSX = 'app/(markos)/admin/billing/page.tsx';
const ADMIN_BILLING_STORIES = 'app/(markos)/admin/billing/reconciliation.stories.tsx';
const ADMIN_GOV_CSS = 'app/(markos)/admin/governance/page.module.css';
const ADMIN_GOV_TSX = 'app/(markos)/admin/governance/page.tsx';
const ADMIN_GOV_STORIES = 'app/(markos)/admin/governance/governance.stories.tsx';
const OPS_CSS = 'app/(markos)/operations/page.module.css';
const OPS_TSX = 'app/(markos)/operations/page.tsx';
const OPS_STORIES = 'app/(markos)/operations/operations.stories.tsx';
const TASKS_CSS = 'app/(markos)/operations/tasks/task-ui.module.css';
const TASKS_PAGE = 'app/(markos)/operations/tasks/page.tsx';
const TASKS_STEP_RUNNER = 'app/(markos)/operations/tasks/step-runner.tsx';
const TASKS_GRAPH = 'app/(markos)/operations/tasks/task-graph.tsx';
const TASKS_APPROVAL = 'app/(markos)/operations/tasks/approval-gate.tsx';
const TASKS_EVIDENCE = 'app/(markos)/operations/tasks/evidence-panel.tsx';
const TASKS_STORIES = 'app/(markos)/operations/tasks/tasks.stories.tsx';
const STATUS_CSS = 'app/(markos)/status/webhooks/page.module.css';
const STATUS_TSX = 'app/(markos)/status/webhooks/page.tsx';
const STATUS_STORIES = 'app/(markos)/status/webhooks/page.stories.tsx';
const FOUR04_CSS = 'app/(markos)/404-workspace/page.module.css';
const FOUR04_TSX = 'app/(markos)/404-workspace/page.tsx';
const FOUR04_STORIES = 'app/(markos)/404-workspace/page.stories.tsx';
const THEME_STORIES = 'app/(markos)/settings/theme/theme.stories.tsx';

// ===== AB-* (admin/billing — 6 tests) =====

test('213.4 AB-1: admin/billing module.css zero inline hex', () => {
  assert.doesNotMatch(read(ADMIN_BILLING_CSS), /#[0-9a-fA-F]{3,8}/);
});

test('213.4 AB-1: admin/billing module.css no Sora/Space Grotesk', () => {
  assert.doesNotMatch(read(ADMIN_BILLING_CSS), /Sora|Space Grotesk/);
});

test('213.4 AB-2: admin/billing page.tsx composes .c-card', () => {
  assert.match(read(ADMIN_BILLING_TSX), /"c-card"/);
});

test('213.4 AB-5: admin/billing page.tsx uses .c-badge--{warning,info,success} on row state', () => {
  assert.match(read(ADMIN_BILLING_TSX), /c-badge c-badge--(warning|info|success)/);
});

test('213.4 AB-6: admin/billing page.tsx uses .c-button{,--primary,--secondary,--destructive}', () => {
  const src = read(ADMIN_BILLING_TSX);
  assert.match(src, /c-button c-button--primary/);
  assert.match(src, /c-button c-button--secondary/);
  assert.match(src, /c-button c-button--destructive/);
});

test('213.4 AB-7: admin/billing page.tsx variant-driven .c-notice c-notice--{state}', () => {
  assert.match(read(ADMIN_BILLING_TSX), /c-notice c-notice--(success|warning|error)/);
});

test('213.4 AB-8: admin/billing reconciliation.stories.tsx ≥3 named state exports', () => {
  const src = read(ADMIN_BILLING_STORIES);
  assert.match(src, /export const Healthy:/);
  assert.match(src, /export const HoldState:/);
  assert.match(src, /export const SyncFailure:/);
});

// ===== AG-* (admin/governance — 5 tests) =====

test('213.4 AG-1: admin/governance module.css zero inline hex', () => {
  assert.doesNotMatch(read(ADMIN_GOV_CSS), /#[0-9a-fA-F]{3,8}/);
});

test('213.4 AG-4: admin/governance module.css no border-left teal stripe', () => {
  assert.doesNotMatch(read(ADMIN_GOV_CSS), /border-left:\s*4px\s+solid/);
});

test('213.4 AG-3: admin/governance page.tsx uses .c-badge--{error,success,info}', () => {
  assert.match(read(ADMIN_GOV_TSX), /c-badge c-badge--(error|success|info)/);
});

test('213.4 AG-5: admin/governance page.tsx variant-driven .c-notice c-notice--{state}', () => {
  assert.match(read(ADMIN_GOV_TSX), /c-notice c-notice--(error|success)/);
});

test('213.4 AG-6: admin/governance.stories.tsx ≥3 named state exports', () => {
  const src = read(ADMIN_GOV_STORIES);
  assert.match(src, /export const Default:/);
  assert.match(src, /export const DeniedMapping:/);
  assert.match(src, /export const ExportReady:/);
});

// ===== O-* (operations root — 5 tests) =====

test('213.4 O-1: operations module.css zero inline hex', () => {
  assert.doesNotMatch(read(OPS_CSS), /#[0-9a-fA-F]{3,8}/);
});

test('213.4 O-1: operations module.css no gradient', () => {
  assert.doesNotMatch(read(OPS_CSS), /linear-gradient|radial-gradient/);
});

test('213.4 O-4: operations page.tsx Authorization badge dual-signal (.c-badge + .c-status-dot + bracketed glyph)', () => {
  const src = read(OPS_TSX);
  assert.match(src, /c-badge c-badge--(success|error)/);
  assert.match(src, /c-status-dot c-status-dot--(live|error)/);
  assert.match(src, /\[(ok|err)\]/);
});

test('213.4 O-5: operations page.tsx Denied state via .c-notice c-notice--error', () => {
  assert.match(read(OPS_TSX), /c-notice c-notice--error/);
});

test('213.4 O-8: operations.stories.tsx ≥2 named state exports', () => {
  const src = read(OPS_STORIES);
  assert.match(src, /export const Authorized:/);
  assert.match(src, /export const Denied:/);
});

// ===== OT-* (operations/tasks — 10 tests) =====

test('213.4 OT-1: task-ui.module.css zero inline hex', () => {
  assert.doesNotMatch(read(TASKS_CSS), /#[0-9a-fA-F]{3,8}/);
});

test('213.4 OT-1: task-ui.module.css no gradients/shadows/jiggle', () => {
  const src = read(TASKS_CSS);
  assert.doesNotMatch(src, /linear-gradient|radial-gradient/);
  assert.doesNotMatch(src, /box-shadow/);
  assert.doesNotMatch(src, /transform:\s*translate/);
});

test('213.4 OT-3: task-ui.module.css no legacy step-card colors (.stepCardCurrent border via --color-primary)', () => {
  assert.doesNotMatch(read(TASKS_CSS), /#38bdf8|#bfdbfe|#1d4ed8/);
});

test('213.4 OT-4: task-graph.tsx uses .c-badge--{info,warning,success,error}', () => {
  const src = read(TASKS_GRAPH);
  assert.match(src, /c-badge c-badge--info/);
  assert.match(src, /c-badge c-badge--warning/);
  assert.match(src, /c-badge c-badge--success/);
  assert.match(src, /c-badge c-badge--error/);
});

test('213.4 OT-6: step-runner.tsx helper banners via .c-notice c-notice--{info,warning,error}', () => {
  assert.match(read(TASKS_STEP_RUNNER), /c-notice c-notice--(info|warning|error)/);
});

test('213.4 OT-8: step-runner.tsx step action buttons + abort .c-modal', () => {
  const src = read(TASKS_STEP_RUNNER);
  assert.match(src, /c-button c-button--(primary|secondary|destructive)/);
  assert.match(src, /c-modal/);
  assert.match(src, /c-backdrop/);
});

test('213.4 OT-9: tasks/page.tsx zero Tailwind inline hex', () => {
  assert.doesNotMatch(read(TASKS_PAGE), /bg-\[#|text-\[#|border-\[#|border-gray-/);
});

test('213.4 OT-10: approval-gate.tsx form composes .c-input + .c-field', () => {
  const src = read(TASKS_APPROVAL);
  assert.match(src, /c-input/);
  assert.match(src, /c-field/);
});

test('213.4 OT-11: evidence-panel.tsx uses .c-card + .c-chip-protocol', () => {
  const src = read(TASKS_EVIDENCE);
  assert.match(src, /c-card/);
  assert.match(src, /c-chip-protocol/);
});

test('213.4 OT-12: tasks.stories.tsx ≥4 named state exports', () => {
  const src = read(TASKS_STORIES);
  const names = ['QueuedTask','ExecutingTask','CompletedTask','FailedTask','ApprovalRequired'];
  const found = names.filter(n => src.includes(`export const ${n}:`));
  assert.ok(found.length >= 4, `expected ≥4 of ${names.join(',')} found ${found.length}`);
});

// ===== SW-* (status/webhooks — 5 tests) =====

test('213.4 SW-1: status/webhooks module.css zero inline hex + no !important wildcard', () => {
  const src = read(STATUS_CSS);
  assert.doesNotMatch(src, /#[0-9a-fA-F]{3,8}/);
  assert.doesNotMatch(src, /!important/);
});

test('213.4 SW-4: status/webhooks page.tsx .c-notice + .c-status-dot primitives className-driven', () => {
  const src = read(STATUS_TSX);
  assert.match(src, /c-notice c-notice--(success|warning|error)/);
  assert.match(src, /c-status-dot/);
});

test('213.4 SW-4: status/webhooks page.tsx bracketed glyphs prepended', () => {
  const src = read(STATUS_TSX);
  assert.match(src, /\[(ok|warn|err)\]/);
});

test('213.4 SW-7: status/webhooks page.tsx Phase 203 endpoint + classifyStatus preserved', () => {
  const src = read(STATUS_TSX);
  assert.match(src, /api\/public\/webhooks\/status/);
  assert.match(src, /classifyStatus/);
});

test('213.4 SW-6: status/webhooks page.stories.tsx ≥3 named state exports', () => {
  const src = read(STATUS_STORIES);
  assert.match(src, /export const Operational:/);
  assert.match(src, /export const Retrying:/);
  assert.match(src, /export const Elevated:/);
});

// ===== F-* (404-workspace — 4 tests) =====

test('213.4 F-1: 404-workspace module.css zero inline hex + no gradient', () => {
  const src = read(FOUR04_CSS);
  assert.doesNotMatch(src, /#[0-9a-fA-F]{3,8}/);
  assert.doesNotMatch(src, /linear-gradient|radial-gradient/);
});

test('213.4 F-2: 404-workspace page.tsx composes .c-card--feature (D-13 hero exception)', () => {
  assert.match(read(FOUR04_TSX), /c-card--feature/);
});

test('213.4 F-7: 404-workspace page.tsx force-dynamic + searchParams preserved (D-23)', () => {
  const src = read(FOUR04_TSX);
  assert.match(src, /force-dynamic/);
  assert.match(src, /searchParams/);
});

test('213.4 F-6: 404-workspace page.stories.tsx ≥2 named state exports', () => {
  const src = read(FOUR04_STORIES);
  assert.match(src, /export const Available:/);
  assert.match(src, /export const Reserved:/);
});

// ===== Cross-cutting (X-1..X-5 + theme — 5 tests) =====

test('213.4 X-1: --color-primary never used as text color in any new module.css', () => {
  const all = [ADMIN_BILLING_CSS, ADMIN_GOV_CSS, OPS_CSS, TASKS_CSS, STATUS_CSS, FOUR04_CSS].map(read).join('\n---\n');
  assert.doesNotMatch(all, /\n[ \t]+color:\s+var\(--color-primary\)\s*;/);
});

test('213.4 X-2: no height: 6px meter tracks across wave (8px parity)', () => {
  const all = [TASKS_CSS, STATUS_CSS].map(read).join('\n---\n');
  assert.doesNotMatch(all, /height:\s*6px/);
});

test('213.4 X-3: task-ui.module.css contains explicit @media (prefers-reduced-motion: reduce)', () => {
  assert.match(read(TASKS_CSS), /@media[^{]*prefers-reduced-motion:\s*reduce/);
});

test('213.4 X-5: zero @media (pointer: coarse) redeclarations in any new module.css', () => {
  const all = [ADMIN_BILLING_CSS, ADMIN_GOV_CSS, OPS_CSS, TASKS_CSS, STATUS_CSS, FOUR04_CSS].map(read).join('\n---\n');
  assert.doesNotMatch(all, /@media[^{]*pointer[^{]*coarse/);
});

test('213.4 T-2: settings/theme/theme.stories.tsx ≥3 named state exports for design diagnostic', () => {
  const src = read(THEME_STORIES);
  assert.match(src, /export const ColorTokens:/);
  assert.match(src, /export const SpacingTokens:/);
  assert.match(src, /export const PrimitiveSampler:/);
});
```

**KEY rules (per T-4):**
- ≥30 individual `test(...)` blocks (count: 30+).
- ≥7 distinct AC# mentions: AB-1, AB-2, AB-5, AB-6, AB-7, AB-8, AG-1, AG-3, AG-4, AG-5, AG-6, O-1, O-4, O-5, O-8, OT-1, OT-3, OT-4, OT-6, OT-8, OT-9, OT-10, OT-11, OT-12, SW-1, SW-4, SW-6, SW-7, F-1, F-2, F-6, F-7, X-1, X-2, X-3, X-5, T-2 — well above 7.
- Mirrors `213-3-settings-a11y.test.js` 5-line node:test opener + `read()` helper + AC# section comments.
- Uses `node --test` runner (no vitest, no playwright).

After edit: `npm run test:ui-a11y -- test/ui-a11y/213-4-admin-ops-a11y.test.js` MUST exit 0.
  </action>
  <verify>
    <automated>test -f "test/ui-a11y/213-4-admin-ops-a11y.test.js" && grep -cE "^test\('213\.4 " "test/ui-a11y/213-4-admin-ops-a11y.test.js" | awk '$1>=30 {exit 0} $1<30 {exit 1}' && grep -cE "AB-|AG-|O-|OT-|SW-|F-|X-|T-" "test/ui-a11y/213-4-admin-ops-a11y.test.js" | awk '$1>=7 {exit 0} $1<7 {exit 1}' && npm run test:ui-a11y -- test/ui-a11y/213-4-admin-ops-a11y.test.js && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **T-4 (≥30 individual tests):** `grep -cE "^test\('213\.4 " 213-4-admin-ops-a11y.test.js` ≥30.
    - **T-4 (≥7 AC# mentions):** `grep -cE "AB-|AG-|O-|OT-|SW-|F-|X-|T-" 213-4-admin-ops-a11y.test.js` ≥7.
    - **T-4 suite green:** `npm run test:ui-a11y -- test/ui-a11y/213-4-admin-ops-a11y.test.js` exits 0.
  </acceptance_criteria>
  <done>NEW a11y test file with ≥30 tests and ≥7 AC# mentions; suite green. Atomic commit: `test(213.4-08): add 213-4-admin-ops-a11y.test.js grep-shape suite (≥30 tests / ≥7 AC# mentions) (T-4)`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Final cross-cutting verification (X-1..X-6, T-5..T-7) — run full npm test + npm run build + npm run build-storybook; update .planning/STATE.md to reflect 213.x milestone closure; final wave commit</name>
  <read_first>
    - .planning/phases/213.4-.../213.4-UI-SPEC.md X-1..X-6 + T-5..T-7
    - .planning/STATE.md (current state — locate Phase 213.x section)
    - All 6 new module.css files (post-Plans 02–07)
    - All 13 modified .tsx files
  </read_first>
  <files>.planning/STATE.md</files>
  <action>
**Run X/T verification battery:**

```bash
# X-1 — --color-primary never as text in 6 new module.css files
! grep -nE "color: var\(--color-primary\)" "app/(markos)/admin/billing/page.module.css" "app/(markos)/admin/governance/page.module.css" "app/(markos)/operations/page.module.css" "app/(markos)/operations/tasks/task-ui.module.css" "app/(markos)/status/webhooks/page.module.css" "app/(markos)/404-workspace/page.module.css"

# X-2 — height: 6px on meter tracks
! grep -nE "height: 6px" "app/(markos)/operations/tasks/task-ui.module.css" "app/(markos)/status/webhooks/page.module.css"

# X-3 — prefers-reduced-motion in task-ui
grep -nE "@media[^{]*prefers-reduced-motion:\s*reduce" "app/(markos)/operations/tasks/task-ui.module.css"

# X-4 — banned-lexicon = 0 across 13 modified TSX files
! grep -inE "synergy|leverage|empower|unlock|revolutionize|supercharge|holistic|seamless|cutting-edge|innovative|game-changer|next-generation|world-class|best-in-class|reimagine|disrupt|Please" \
  "app/(markos)/admin/billing/page.tsx" \
  "app/(markos)/admin/governance/page.tsx" \
  "app/(markos)/operations/page.tsx" \
  "app/(markos)/operations/tasks/page.tsx" \
  "app/(markos)/operations/tasks/step-runner.tsx" \
  "app/(markos)/operations/tasks/task-graph.tsx" \
  "app/(markos)/operations/tasks/approval-gate.tsx" \
  "app/(markos)/operations/tasks/evidence-panel.tsx" \
  "app/(markos)/operations/tasks/task-store.tsx" \
  "app/(markos)/status/webhooks/page.tsx" \
  "app/(markos)/404-workspace/page.tsx" \
  "app/(markos)/settings/theme/page.tsx" \
  "app/(markos)/settings/theme/theme.stories.tsx"

# X-5 — no per-module pointer-coarse redeclarations
! grep -nE "@media[^{]*pointer[^{]*coarse" \
  "app/(markos)/admin/billing/page.module.css" \
  "app/(markos)/admin/governance/page.module.css" \
  "app/(markos)/operations/page.module.css" \
  "app/(markos)/operations/tasks/task-ui.module.css" \
  "app/(markos)/status/webhooks/page.module.css" \
  "app/(markos)/404-workspace/page.module.css"

# T-6 — full npm test green (Phase 200/201/202/203/204/205/207 wiring suites)
npm test

# T-7 — build + build-storybook green
npm run build
npm run build-storybook
```

If any verification fails, halt and report. If all pass, update `.planning/STATE.md`:

**Find the Phase 213.4 section** (locate by phase number or "Phase 213.4 Plan Progress"). Add the wave-closure entry. Update milestone progress counter (`completed_phases` increment by 1 for 213.4 closure; the milestone v4.0.0 is already complete per current STATE.md frontmatter).

**Append to `## Phase 213.4 Plan Progress` (or create the section if missing):**

```
- [x] 213.4-01: polish — Phase 213.3 ui-review F-1/F-2/F-3 fixes (webhooks/[sub_id] filter chip + expand chevron text use --color-primary-text; webhooks .successMeterTrack height 8px parity; members prefers-reduced-motion block parity). Closes UI-SPEC AC P-1..P-4. (2026-04-29)
- [x] 213.4-02: admin/billing surface to DESIGN.md tokens — page.module.css rewritten + page.tsx primitive composition (.c-card + .c-button{,--primary,--secondary,--destructive} + .c-modal write-off + .c-notice c-notice--{state} variant-driven + .c-badge--{warning,info,success} row state) + reconciliation.stories.tsx (Healthy/HoldState/SyncFailure). Closes UI-SPEC AC AB-1..AB-8. (2026-04-29)
- [x] 213.4-03: admin/governance surface to DESIGN.md tokens — page.module.css rewritten (teal border-left stripe eliminated) + page.tsx primitive composition (.c-card + .c-button{,--primary,--destructive} + .c-modal Reject + .c-notice c-notice--{error,success} + .c-badge--{error,success,info} decision column + 4 semantic tables) + governance.stories.tsx (Default/DeniedMapping/ExportReady). Closes UI-SPEC AC AG-1..AG-6. (2026-04-29)
- [x] 213.4-04: operations (root) to DESIGN.md tokens — page.module.css rewritten (dual radial+linear gradients + drop shadow + hover-jiggle eliminated) + page.tsx primitive composition (.c-card + .c-button c-button--primary anchor + .c-notice c-notice--error denied + .c-badge--{success,error} + .c-status-dot--{live,error} dual-signal + .t-label-caps) + operations.stories.tsx (Authorized/Denied). Closes UI-SPEC AC O-1..O-8. (2026-04-29)
- [x] 213.4-05: operations/tasks composite to DESIGN.md tokens (LARGEST surface — 525+1384 LOC) — task-ui.module.css rewritten + page.tsx Tailwind hex eliminated + 5 sub-components className-only update (approval-gate/evidence-panel/step-runner/task-graph/task-store; D-15+D-21 server/client boundaries preserved) + tasks.stories.tsx (QueuedTask/ExecutingTask/CompletedTask/FailedTask/ApprovalRequired). Closes UI-SPEC AC OT-1..OT-12. (2026-04-29)
- [x] 213.4-06: status/webhooks (public Phase 203 substrate) to DESIGN.md tokens — page.module.css rewritten (`!important` wildcard removed) + page.tsx primitive composition (.c-card + .c-notice c-notice--{success,warning,error} className-driven + .c-status-dot--{live,*,error}; Phase 203 wiring + classifyStatus + statusCopy register PRESERVED VERBATIM per D-20) + NEW page.stories.tsx (Operational/Retrying/Elevated). Closes UI-SPEC AC SW-1..SW-7. (2026-04-29)
- [x] 213.4-07: 404-workspace error hero to DESIGN.md tokens — page.module.css rewritten (gradient eliminated) + page.tsx primitive composition (.c-card--feature hero exception per D-13 + .c-button{,--primary,--tertiary} + .t-label-caps; notFound/force-dynamic/searchParams PRESERVED per D-23) + NEW page.stories.tsx (Available/Reserved). Closes UI-SPEC AC F-1..F-7. (2026-04-29)
- [x] 213.4-08: settings/theme + cross-cutting closure — theme.stories.tsx extended (ColorTokens/SpacingTokens/PrimitiveSampler design diagnostic) + NEW test/ui-a11y/213-4-admin-ops-a11y.test.js (≥30 tests / ≥7 AC# mentions) + (pointer: coarse) inheritance verified across 6 new modules + final wave green. Closes UI-SPEC AC T-1..T-7 + X-1..X-6. **Wave 1 CLOSED. 213.x milestone wave (213.1+213.2+213.3+213.4) CLOSED — full markos chrome surface in DESIGN.md v1.1.0 canon.** (2026-04-29)
```

**Final wave commit:**

```bash
git add app/(markos)/settings/theme/theme.stories.tsx test/ui-a11y/213-4-admin-ops-a11y.test.js .planning/STATE.md
git commit -m "feat(213.4-08): close 213.x milestone wave — full markos chrome in design-md canon

Closes UI-SPEC AC T-1..T-7 + X-1..X-6. Wave 1 CLOSED.
213.1 (chrome) + 213.2 (auth) + 213.3 (settings) + 213.4 (admin/ops/status/404/theme) =
full markos chrome surface in DESIGN.md v1.1.0 canonical contract.

- theme.stories.tsx extended with design-system visual diagnostic
- NEW 213-4-admin-ops-a11y.test.js with ≥30 tests / ≥7 AC# mentions
- (pointer: coarse) global inheritance verified across 6 new modules
- 182 inline hex eliminated across the wave
- 65 ACs closed across 8 plans"
```
  </action>
  <verify>
    <automated>! grep -nE "color: var\(--color-primary\)" "app/(markos)/admin/billing/page.module.css" "app/(markos)/admin/governance/page.module.css" "app/(markos)/operations/page.module.css" "app/(markos)/operations/tasks/task-ui.module.css" "app/(markos)/status/webhooks/page.module.css" "app/(markos)/404-workspace/page.module.css" && ! grep -nE "height:\s*6px" "app/(markos)/operations/tasks/task-ui.module.css" "app/(markos)/status/webhooks/page.module.css" && grep -nE "@media[^{]*prefers-reduced-motion:\s*reduce" "app/(markos)/operations/tasks/task-ui.module.css" && ! grep -nE "@media[^{]*pointer[^{]*coarse" "app/(markos)/admin/billing/page.module.css" "app/(markos)/admin/governance/page.module.css" "app/(markos)/operations/page.module.css" "app/(markos)/operations/tasks/task-ui.module.css" "app/(markos)/status/webhooks/page.module.css" "app/(markos)/404-workspace/page.module.css" && npm run build && npm run build-storybook && npm test && grep -q "213.4 Plan Progress" ".planning/STATE.md" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **X-1:** zero `color: var(--color-primary)` text usages across 6 new module.css.
    - **X-2:** zero `height: 6px` in task-ui + status/webhooks.
    - **X-3:** `prefers-reduced-motion` block in task-ui.module.css.
    - **X-4:** banned-lexicon = 0 across all 13 .tsx files (verified per Task 2 test suite + grep).
    - **X-5:** zero `@media (pointer: coarse)` in any of 6 new module.css.
    - **X-6 (213.x milestone closure):** STATE.md updated; final commit message documents closure.
    - **T-6:** `npm test` exits 0.
    - **T-7:** `npm run build` + `npm run build-storybook` both exit 0.
  </acceptance_criteria>
  <done>All cross-cutting gates green; STATE.md updated; final wave commit lands. Phase 213.4 + 213.x milestone wave CLOSED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| design-system contract | DESIGN.md v1.1.0 token cascade is the single source of truth for visual decisions across all 213.x phases |
| a11y suite | `test/ui-a11y/213-4-admin-ops-a11y.test.js` is the audit gate for the wave's compliance posture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213.4-08-01 | Tampering | a11y test bypass | mitigate | Suite covers ≥30 grep-shape verifications across all 6 module.css + 13 .tsx; runs in CI per `npm run test:ui-a11y` script |
| T-213.4-08-02 | Information Disclosure | design diagnostic stories reveal token names | accept | Token names are intentional public design-system contract per DESIGN.md |
| T-213.4-08-03 | Repudiation | milestone closure tracking | mitigate | STATE.md updated with explicit per-plan closure entries + final wave commit message documents 213.x scope closure |
</threat_model>

<verification>
```bash
# X-1 (--color-primary not text)
grep -nE "color: var\(--color-primary\)" $ALL_6_MODULE_CSS  # 0
# X-2 (no 6px meters)
grep -nE "height:\s*6px" $TASKS_CSS $STATUS_CSS  # 0
# X-3 (prefers-reduced-motion in task-ui)
grep -nE "prefers-reduced-motion" $TASKS_CSS  # ≥1
# X-5 (no per-module pointer-coarse)
grep -nE "@media[^{]*pointer[^{]*coarse" $ALL_6_MODULE_CSS  # 0
# T-4 (a11y suite)
npm run test:ui-a11y -- test/ui-a11y/213-4-admin-ops-a11y.test.js  # exit 0
# T-6 / T-7
npm test && npm run build && npm run build-storybook  # all exit 0
```
</verification>

<success_criteria>
UI-SPEC AC T-1..T-7 + X-1..X-6 close. 213.x milestone wave (213.1+213.2+213.3+213.4) CLOSED.
Final commit lands; STATE.md reflects closure.
3 atomic commits in this plan (T-2/T-3 stories, T-4 a11y suite, X-1..X-6 closure + STATE update).
</success_criteria>

<output>
After completion, create `.planning/phases/213.4-.../213.4-08-SUMMARY.md` documenting:
- Files modified (theme.stories.tsx, NEW 213-4-admin-ops-a11y.test.js, STATE.md)
- AC# closures (T-1..T-7, X-1..X-6)
- Wave totals: 8 plans, 182 hex eliminated, 65 ACs closed, ≥20 named Storybook state stories across 7 surfaces, ≥30 a11y tests
- Phase 213.4 + Phase 213.x milestone wave CLOSED
- Hand-off: ready for `/gsd-verify-work 213.4` against UI-SPEC ACs + Chromatic baseline review
</output>
</content>
</invoke>
