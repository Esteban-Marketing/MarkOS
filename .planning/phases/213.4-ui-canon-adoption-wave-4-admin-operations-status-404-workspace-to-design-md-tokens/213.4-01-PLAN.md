---
phase: 213.4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(markos)/settings/webhooks/[sub_id]/page.module.css
  - app/(markos)/settings/webhooks/page.module.css
  - app/(markos)/settings/members/page.module.css
autonomous: true
requirements:
  - P-1
  - P-2
  - P-3
  - P-4
must_haves:
  truths:
    - "app/(markos)/settings/webhooks/[sub_id]/page.module.css uses var(--color-primary-text) (not raw var(--color-primary)) for text colors on .filterChipGroup button[data-active='true'] (line ~169) and .expandChevron[data-expanded='true'] (line ~212) — light-theme contrast ≥4.5:1 (P-1, P-2)"
    - "app/(markos)/settings/webhooks/page.module.css .successMeterTrack uses height: 8px with rationale comment matching members + mcp peer modules (P-3)"
    - "app/(markos)/settings/members/page.module.css contains an explicit @media (prefers-reduced-motion: reduce) { .meterFill { transition: none; } } block at end of file matching mcp + webhooks peer modules (P-4)"
    - "Zero changes to app/(markos)/settings/webhooks/[sub_id]/page.tsx, app/(markos)/settings/webhooks/page.tsx, or app/(markos)/settings/members/page.tsx (CSS-only polish; CONTEXT D-10)"
    - "npm run build exits 0; npm test exits 0; npm run test:ui-a11y -- test/ui-a11y/213-3-settings-a11y.test.js exits 0 (Phase 213.3 a11y suite preserved)"
  artifacts:
    - path: "app/(markos)/settings/webhooks/[sub_id]/page.module.css"
      provides: "F-1 polish — both text-color usages of var(--color-primary) replaced with var(--color-primary-text) for theme-aware light-theme WCAG compliance"
      contains: "var(--color-primary-text)"
    - path: "app/(markos)/settings/webhooks/page.module.css"
      provides: "F-2 polish — .successMeterTrack height parity with members + mcp peer modules (8px)"
      contains: "height: 8px"
    - path: "app/(markos)/settings/members/page.module.css"
      provides: "F-3 polish — explicit prefers-reduced-motion block on .meterFill matching peer modules"
      contains: "prefers-reduced-motion"
  key_links:
    - from: "app/(markos)/settings/webhooks/[sub_id]/page.module.css"
      to: "app/tokens.css var(--color-primary-text) (theme-aware mint-as-text token)"
      via: "color: var(--color-primary-text) on filter chip text + expand chevron"
      pattern: "var\\(--color-primary-text\\)"
    - from: "app/(markos)/settings/webhooks/page.module.css"
      to: ".successMeterTrack height parity"
      via: "height: 8px sizing value with peer-module rationale comment"
      pattern: "height:\\s*8px"
    - from: "app/(markos)/settings/members/page.module.css"
      to: "DESIGN.md prefers-reduced-motion contract"
      via: "@media (prefers-reduced-motion: reduce) { .meterFill { transition: none; } } block"
      pattern: "prefers-reduced-motion:\\s*reduce"
---

<objective>
Close Phase 213.3 ui-review (commit `34e4bee`) priority fixes F-1, F-2, F-3 BEFORE any new surface work in this phase begins. Three surgical one-line edits across three Phase 213.3 module.css files. Atomic commit per file. Closes 213.3 visual debt so subsequent 213.4 plans (02–08) start against a fully-clean predecessor surface set.

Purpose: Close UI-SPEC AC P-1 (filter chip text → `--color-primary-text`), P-2 (expand chevron text → `--color-primary-text`), P-3 (meter track height parity 8px), P-4 (members `prefers-reduced-motion` block parity). All four ACs are CSS-only polish — zero TSX changes; zero DOM/prop changes; zero wiring changes.

Output: `app/(markos)/settings/webhooks/[sub_id]/page.module.css` (2 line edits), `app/(markos)/settings/webhooks/page.module.css` (1 line edit), `app/(markos)/settings/members/page.module.css` (1 block append at EOF).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/213.4-ui-canon-adoption-wave-4-admin-operations-status-404-workspace-to-design-md-tokens/213.4-CONTEXT.md
@.planning/phases/213.4-ui-canon-adoption-wave-4-admin-operations-status-404-workspace-to-design-md-tokens/213.4-UI-SPEC.md
@.planning/phases/213.4-ui-canon-adoption-wave-4-admin-operations-status-404-workspace-to-design-md-tokens/213.4-RESEARCH.md
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-UI-REVIEW.md
@DESIGN.md
@CLAUDE.md
@app/tokens.css
@styles/components.css
@app/(markos)/settings/webhooks/[sub_id]/page.module.css
@app/(markos)/settings/webhooks/page.module.css
@app/(markos)/settings/members/page.module.css
@app/(markos)/settings/mcp/page.module.css

<interfaces>
<!-- Theme-aware mint-as-text token from app/tokens.css (v1.1.0): -->
<!-- :root[data-theme="dark"] { --color-primary-text: #00D9A3 } -->
<!-- :root[data-theme="light"] { --color-primary-text: #00805F } (4.95:1 AA on #F4F4F0) -->
<!-- @media (forced-colors: active) { --color-primary-text: LinkText } -->

<!-- Per RESEARCH.md "Class-Rename Map: Plan 213.4-01 — Polish": -->
<!-- File 1: app/(markos)/settings/webhooks/[sub_id]/page.module.css -->
<!--   Line 169 (or nearest): color: var(--color-primary) on .filterChipGroup button[data-active='true'] -->
<!--   Line 212 (or nearest): color: var(--color-primary) on .expandChevron[data-expanded='true'] -->
<!--   Action: Replace each with color: var(--color-primary-text) -->

<!-- File 2: app/(markos)/settings/webhooks/page.module.css -->
<!--   Line 152 (or nearest): height: 6px on .successMeterTrack -->
<!--   Action: Replace with `height: 8px /* meter track parity — members + mcp peer modules */` -->

<!-- File 3: app/(markos)/settings/members/page.module.css -->
<!--   EOF: append `@media (prefers-reduced-motion: reduce) { .meterFill { transition: none; } }` -->
<!--   Pattern matches mcp + webhooks peer modules (verify those files for exact selector style) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Polish F-1 + F-2 — Replace both var(--color-primary) text usages in webhooks/[sub_id]/page.module.css with var(--color-primary-text) (P-1, P-2)</name>
  <read_first>
    - app/(markos)/settings/webhooks/[sub_id]/page.module.css (FULL FILE — locate exact lines for `.filterChipGroup button[data-active='true']` and `.expandChevron[data-expanded='true']` text-color declarations; line numbers are approximate per RESEARCH 169 + 212 but file may have shifted)
    - app/tokens.css (`--color-primary-text` token definition — verify dark + light + forced-colors mapping)
    - .planning/phases/213.4-.../213.4-RESEARCH.md "Class-Rename Map: Plan 213.4-01 — Polish" (lines 75–84)
    - .planning/phases/213.3-.../213.3-UI-REVIEW.md (Pillar 1 + 4 findings — F-1 and F-2 source)
    - DESIGN.md "Color > Mint as text" (canon for `--color-primary-text` over `--color-primary` in text contexts)
  </read_first>
  <files>app/(markos)/settings/webhooks/[sub_id]/page.module.css</files>
  <action>
**Edit A — `app/(markos)/settings/webhooks/[sub_id]/page.module.css` line ~169 (F-1)**

Locate the rule on `.filterChipGroup button[data-active='true']` (or whichever active-filter-chip selector currently uses `color: var(--color-primary)` for chip text). Replace ONLY the text-color declaration:

```css
/* Before */
.filterChipGroup button[data-active='true'] {
  /* ...other properties unchanged... */
  color: var(--color-primary);
}

/* After */
.filterChipGroup button[data-active='true'] {
  /* ...other properties unchanged... */
  color: var(--color-primary-text);
}
```

**Edit B — `app/(markos)/settings/webhooks/[sub_id]/page.module.css` line ~212 (F-2)**

Locate the rule on `.expandChevron[data-expanded='true']` (or whichever expanded-chevron selector currently uses `color: var(--color-primary)` for icon/text color). Replace ONLY the text-color declaration:

```css
/* Before */
.expandChevron[data-expanded='true'] {
  /* ...other properties unchanged... */
  color: var(--color-primary);
}

/* After */
.expandChevron[data-expanded='true'] {
  /* ...other properties unchanged... */
  color: var(--color-primary-text);
}
```

**KEY rules (verifiable per UI-SPEC P-1, P-2 / RESEARCH F-1, F-2):**
- ONLY the `color:` property value changes. Border, background, focus-outline, shadow, padding, transitions are NOT touched (those legitimately use `var(--color-primary)` for fill/border/focus-ring contexts).
- Zero structural rule edits — no selectors added, no rules deleted, no class renames.
- Zero changes to any `.tsx` file in the surface (CSS-only polish).
- Light-theme contrast post-edit: `#00805F` on `#F4F4F0` = 4.95:1 (AA normal pass).

If grep at exact line numbers (169, 212) does NOT match the expected pattern, search the file for ALL occurrences of `color: var(--color-primary);` (or with whitespace variants) and apply the same swap to each — these are the only contexts where the token is used as TEXT color in this file. Border / background / focus-ring uses of `--color-primary` are PRESERVED (per RESEARCH "F-1/F-2 grep returns only non-text usages").
  </action>
  <verify>
    <automated>! grep -nE '^[[:space:]]+color:[[:space:]]+var\(--color-primary\)[[:space:]]*;' "app/(markos)/settings/webhooks/[sub_id]/page.module.css" && grep -cE 'var\(--color-primary-text\)' "app/(markos)/settings/webhooks/[sub_id]/page.module.css" | awk '$1>=2 {exit 0} $1<2 {exit 1}' && npm run build && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **P-1 + P-2 (no `--color-primary` as text color):** `grep -nE '^[[:space:]]+color:[[:space:]]+var\(--color-primary\)[[:space:]]*;' "app/(markos)/settings/webhooks/[sub_id]/page.module.css"` returns 0. (Only the `color:` declaration is flagged; border/background/focus-ring usages of `--color-primary` are still permitted and preserved.)
    - **`--color-primary-text` adopted (positive assertion):** `grep -cE 'var\(--color-primary-text\)' "app/(markos)/settings/webhooks/[sub_id]/page.module.css"` ≥2 (one per F-1, one per F-2).
    - **No structural edits:** `git diff --stat "app/(markos)/settings/webhooks/[sub_id]/page.module.css"` shows ≤4 lines changed (2 swaps × ≤2 lines per swap).
    - **No TSX touched:** `git status` shows zero changes under `app/(markos)/settings/webhooks/[sub_id]/page.tsx`.
    - **Build green:** `npm run build` exits 0.
  </acceptance_criteria>
  <done>Two text-color usages of `var(--color-primary)` in `webhooks/[sub_id]/page.module.css` swapped to `var(--color-primary-text)`. Light-theme WCAG 2.1 AA restored. Build green. Atomic commit: `feat(213.4-01): polish 213.3 F-1+F-2 — webhooks/[sub_id] filter chip + expand chevron text use --color-primary-text`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Polish F-3 — Replace .successMeterTrack height: 6px with height: 8px in webhooks/page.module.css for parity with members + mcp peer modules (P-3)</name>
  <read_first>
    - app/(markos)/settings/webhooks/page.module.css (FULL FILE — locate `.successMeterTrack` rule, exact line ~152)
    - app/(markos)/settings/members/page.module.css (peer module — confirm meter track sizing convention)
    - app/(markos)/settings/mcp/page.module.css (peer module — confirm meter track sizing convention)
    - .planning/phases/213.4-.../213.4-RESEARCH.md "Class-Rename Map: Plan 213.4-01" (line 83)
    - .planning/phases/213.3-.../213.3-UI-REVIEW.md (Pillar 1 — meter-height parity finding)
    - DESIGN.md "Spacing Scale > Allowed exceptions" (8px meter-track is the canonical sizing-value exception)
  </read_first>
  <files>app/(markos)/settings/webhooks/page.module.css</files>
  <action>
**Edit — `app/(markos)/settings/webhooks/page.module.css` line ~152**

Locate the `.successMeterTrack` rule. Replace `height: 6px` with `height: 8px` and append a rationale comment that matches the peer-module style:

```css
/* Before */
.successMeterTrack {
  /* ...other properties unchanged... */
  height: 6px;
  /* ...other properties unchanged... */
}

/* After */
.successMeterTrack {
  /* ...other properties unchanged... */
  height: 8px; /* meter track parity — members + mcp peer modules */
  /* ...other properties unchanged... */
}
```

**KEY rules (verifiable per UI-SPEC P-3 / RESEARCH F-3):**
- ONLY the `height:` value changes (6px → 8px) and the rationale comment is added on the same line.
- Width, background, border, border-radius, position properties on `.successMeterTrack` and `.meterFill` are PRESERVED.
- The 8px height is a documented Allowed Exception per UI-SPEC Spacing Scale (sizing value, not spacing) — comment cites the rationale to satisfy DESIGN.md "Off-grid values are bugs" lint with explicit context.
- Mirror the peer-module comment style: confirm whether `members/page.module.css` and `mcp/page.module.css` have inline comments on their meter-track height; if they say `/* meter track */` or similar, match that exact register.
- Zero changes to `.tsx`. Zero structural rule edits.
  </action>
  <verify>
    <automated>! grep -nE '\.successMeterTrack\b[^}]*height:[[:space:]]*6px' "app/(markos)/settings/webhooks/page.module.css" && grep -nE '\.successMeterTrack\b[^}]*height:[[:space:]]*8px' "app/(markos)/settings/webhooks/page.module.css" && grep -nE 'height:[[:space:]]*8px' "app/(markos)/settings/webhooks/page.module.css" | grep -qE 'meter track|peer module' && npm run build && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **P-3 (no `height: 6px` on `.successMeterTrack`):** `grep -nE '\.successMeterTrack\b[^}]*height:[[:space:]]*6px' "app/(markos)/settings/webhooks/page.module.css"` returns 0. Equivalently: `grep -nE 'height:[[:space:]]*6px' "app/(markos)/settings/webhooks/page.module.css"` returns 0 globally in the file.
    - **`height: 8px` adopted (positive assertion):** `grep -nE 'height:[[:space:]]*8px' "app/(markos)/settings/webhooks/page.module.css"` returns ≥1 entry.
    - **Rationale comment present:** the line carrying `height: 8px` includes a comment matching `meter track` or `peer module` text.
    - **No structural edits:** `git diff --stat` shows ≤2 lines changed.
    - **No TSX touched:** `git status` shows zero changes under `app/(markos)/settings/webhooks/page.tsx`.
    - **Build green:** `npm run build` exits 0.
  </acceptance_criteria>
  <done>`.successMeterTrack` height swapped 6px → 8px with rationale comment. Build green. Atomic commit: `feat(213.4-01): polish 213.3 F-3 — webhooks .successMeterTrack height 8px parity with members+mcp`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Polish F-4 — Append explicit @media (prefers-reduced-motion: reduce) { .meterFill { transition: none; } } block to members/page.module.css for parity with mcp + webhooks peer modules (P-4)</name>
  <read_first>
    - app/(markos)/settings/members/page.module.css (FULL FILE — confirm absence of `prefers-reduced-motion` block; identify EOF position; confirm `.meterFill` selector exists)
    - app/(markos)/settings/mcp/page.module.css (peer reference — extract exact `prefers-reduced-motion` block style)
    - app/(markos)/settings/webhooks/page.module.css (peer reference — extract exact `prefers-reduced-motion` block style)
    - .planning/phases/213.4-.../213.4-RESEARCH.md "Class-Rename Map: Plan 213.4-01" (line 84)
    - .planning/phases/213.3-.../213.3-UI-REVIEW.md (Pillar 4 — reduced-motion peer-module consistency finding)
    - DESIGN.md "Motion > Prefers-reduced-motion handling" (`prefers-reduced-motion: reduce` collapses durations to 0ms; status-dot freezes; `.meterFill` transitions disable)
  </read_first>
  <files>app/(markos)/settings/members/page.module.css</files>
  <action>
**Edit — Append at EOF of `app/(markos)/settings/members/page.module.css`**

Add the following block at the end of the file (after the last existing rule, with one blank line separator preceding it):

```css

@media (prefers-reduced-motion: reduce) {
  .meterFill {
    transition: none;
  }
}
```

**KEY rules (verifiable per UI-SPEC P-4 / RESEARCH F-4):**
- The block targets `.meterFill` ONLY (the seat-bar fill that transitions in width when count changes). The track itself (`.meterTrack` if present) does NOT need a rule — only animated elements need explicit overrides.
- The block is APPENDED at EOF — does not modify any existing rule.
- The block selector + property style EXACTLY matches the convention in `mcp/page.module.css` and `webhooks/page.module.css`. If those files use a different syntax (e.g., grouped selectors, `*` wildcard), match their convention. If they use the same single-selector style as shown above, this is correct as-is.
- Zero changes to `.tsx`. Zero structural rule edits to existing rules.
- Global `app/tokens.css` `prefers-reduced-motion` cascade already collapses `--duration-*` tokens to 0ms — this explicit block is redundancy-for-clarity (peer-module consistency), not a new behavior. Document this in the commit message.
  </action>
  <verify>
    <automated>grep -nE '@media[[:space:]]*\(prefers-reduced-motion:[[:space:]]*reduce\)' "app/(markos)/settings/members/page.module.css" && grep -nE '\.meterFill[[:space:]]*\{[^}]*transition:[[:space:]]*none' "app/(markos)/settings/members/page.module.css" && grep -nE '@media[[:space:]]*\(prefers-reduced-motion:[[:space:]]*reduce\)' "app/(markos)/settings/mcp/page.module.css" "app/(markos)/settings/webhooks/page.module.css" && npm run build && npm test 2>&1 | tail -3 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **P-4 (`prefers-reduced-motion` block present):** `grep -nE '@media[[:space:]]*\(prefers-reduced-motion:[[:space:]]*reduce\)' "app/(markos)/settings/members/page.module.css"` returns ≥1 entry.
    - **Block targets `.meterFill` with `transition: none`:** the block (when read in context) contains `.meterFill { transition: none; }`.
    - **Peer-module parity confirmed:** the same `@media (prefers-reduced-motion: reduce)` pattern exists in `mcp/page.module.css` and `webhooks/page.module.css` (positive assertion that members is now consistent with peers).
    - **No structural edits:** `git diff --stat` shows the added lines only at EOF; no existing rule edited.
    - **No TSX touched:** `git status` shows zero changes under `app/(markos)/settings/members/page.tsx` and `app/(markos)/_components/MembersPageView.tsx`.
    - **Build green:** `npm run build` exits 0.
    - **Phase 213.3 a11y suite preserved:** `npm test` exits 0 (no Phase 200/201/202/203/204 wiring breaks; no Phase 213.3 a11y regressions).
  </acceptance_criteria>
  <done>`@media (prefers-reduced-motion: reduce) { .meterFill { transition: none; } }` appended at EOF of `members/page.module.css`. Peer-module parity with mcp + webhooks restored. Build green. `npm test` green. Atomic commit: `feat(213.4-01): polish 213.3 F-4 — members prefers-reduced-motion block parity with mcp+webhooks`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→DOM | Polish edits affect rendered styles only; no untrusted input crosses |
| theme attribute | `data-theme="dark"` / `data-theme="light"` swap on documentElement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213.4-01-01 | Information Disclosure | light-theme contrast | mitigate | F-1/F-2 swap to `--color-primary-text` restores 4.95:1 AA contrast on `#F4F4F0` light bg (was failing in 213.3) |
| T-213.4-01-02 | Denial of Service | reduced-motion users | mitigate | F-4 explicit `prefers-reduced-motion` block on members.meterFill matches peer modules; users with `prefers-reduced-motion: reduce` see no width transitions |
| T-213.4-01-03 | Tampering | none — CSS-only polish, no input/output surface | accept | No data flow or wiring changes |
</threat_model>

<verification>
After all 3 tasks complete, run:

```bash
# P-1 + P-2 — webhooks/[sub_id]: no `color: var(--color-primary);` text usages
! grep -nE '^[[:space:]]+color:[[:space:]]+var\(--color-primary\)[[:space:]]*;' "app/(markos)/settings/webhooks/[sub_id]/page.module.css"

# P-1 + P-2 — webhooks/[sub_id]: ≥2 `--color-primary-text` adoptions
grep -cE 'var\(--color-primary-text\)' "app/(markos)/settings/webhooks/[sub_id]/page.module.css"  # ≥2

# P-3 — webhooks: no height: 6px globally
! grep -nE 'height:[[:space:]]*6px' "app/(markos)/settings/webhooks/page.module.css"

# P-3 — webhooks: height: 8px adopted on .successMeterTrack
grep -nE 'height:[[:space:]]*8px' "app/(markos)/settings/webhooks/page.module.css"  # ≥1

# P-4 — members: prefers-reduced-motion block exists
grep -nE '@media[[:space:]]*\(prefers-reduced-motion:[[:space:]]*reduce\)' "app/(markos)/settings/members/page.module.css"  # ≥1

# Build + tests green
npm run build  # exit 0
npm test       # exit 0
npm run test:ui-a11y -- test/ui-a11y/213-3-settings-a11y.test.js  # exit 0 (Phase 213.3 a11y suite preserved)
```
</verification>

<success_criteria>
- All `must_haves.truths` verifiable via the verification commands above.
- UI-SPEC AC P-1, P-2, P-3, P-4 close.
- Zero `.tsx` changes (CSS-only polish).
- Phase 213.3 a11y suite stays green; no Phase 200/201/202/203/204 wiring regressions.
- Three atomic commits per CONTEXT D-04 atomic-commit cadence:
  - `feat(213.4-01): polish 213.3 F-1+F-2 — webhooks/[sub_id] filter chip + expand chevron text use --color-primary-text`
  - `feat(213.4-01): polish 213.3 F-3 — webhooks .successMeterTrack height 8px parity with members+mcp`
  - `feat(213.4-01): polish 213.3 F-4 — members prefers-reduced-motion block parity with mcp+webhooks`
</success_criteria>

<output>
After completion, create `.planning/phases/213.4-ui-canon-adoption-wave-4-admin-operations-status-404-workspace-to-design-md-tokens/213.4-01-SUMMARY.md` documenting:
- Files modified (3 module.css files; line-count delta per file)
- AC# closures (P-1, P-2, P-3, P-4)
- Verification command outputs
- Manual reminder: re-render Phase 213.3 webhooks + members Storybook stories with light theme to visually confirm contrast restoration
- Hand-off to Plan 213.4-02 (admin/billing — first new-surface plan)
</output>
</content>
</invoke>