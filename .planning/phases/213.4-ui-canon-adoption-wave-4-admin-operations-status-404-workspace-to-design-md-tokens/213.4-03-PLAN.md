---
phase: 213.4
plan: 03
type: execute
wave: 1
depends_on: ["213.4-02"]
files_modified:
  - app/(markos)/admin/governance/page.module.css
  - app/(markos)/admin/governance/page.tsx
  - app/(markos)/admin/governance/governance.stories.tsx
autonomous: true
requirements:
  - AG-1
  - AG-2
  - AG-3
  - AG-4
  - AG-5
  - AG-6
must_haves:
  truths:
    - "app/(markos)/admin/governance/page.module.css contains zero inline hex (whitelist: 1px borders, 2px focus offsets), zero off-grid spacing, zero Sora/Space Grotesk, zero gradients, zero box-shadow, zero transform-translate, zero teal #0d9488/#0f766e/#14b8a6/#155e75/#f8fbfd/#ffffff, zero border-left teal stripe"
    - "app/(markos)/admin/governance/page.tsx composes .c-card on heroCard/panel/detailCard sections, .c-button c-button--primary on Approve, .c-button c-button--destructive on Reject + .c-modal/.c-backdrop confirm, .c-notice c-notice--{error,success} on denial/export-ready (variant-driven), .c-badge--{error,success,info} on decision/status columns (with bracketed glyphs), .t-label-caps on eyebrow + section nav labels"
    - "All legacy local classes eliminated from page.tsx: styles.heroCard / styles.panel / styles.detailCard / styles.detailTitle / styles.detailText / styles.eyebrow / styles.title / styles.sectionTitle / styles.tableHead / styles.tableCell"
    - "app/(markos)/admin/governance/governance.stories.tsx extended with ≥3 named state stories (Default / DeniedMapping / ExportReady) per UI-SPEC AG-6"
    - "Banned-lexicon scan = 0 in modified .tsx; zero exclamation points; zero emoji; zero 'Please' softeners"
    - "DOM tree + prop interface UNCHANGED (CONTEXT D-05); admin role-gate via app/(markos)/admin/layout.tsx preserved verbatim (CONTEXT D-22); Phase 207 substrate connector unchanged"
    - "npm run build exits 0; npm run build-storybook exits 0; npm test exits 0"
  artifacts:
    - path: "app/(markos)/admin/governance/page.module.css"
      provides: "Token-only governance-admin layout shell — composition + section nav + 4-table token recipe"
      min_lines: 35
      contains: "var(--color-surface)"
    - path: "app/(markos)/admin/governance/page.tsx"
      provides: "Updated governance-admin shell composing primitives per UI-SPEC AG-2..AG-5; preserves Phase 207 connector wiring"
      contains: "c-notice c-notice--"
    - path: "app/(markos)/admin/governance/governance.stories.tsx"
      provides: "Storybook 8 CSF3 ≥3 named state exports (Default / DeniedMapping / ExportReady)"
      exports: ["default", "Default", "DeniedMapping", "ExportReady"]
  key_links:
    - from: "app/(markos)/admin/governance/page.tsx"
      to: "styles/components.css .c-notice c-notice--{error,success} primitive"
      via: "variant-driven className composition for denial / export-ready notices per AG-5"
      pattern: "c-notice\\s+c-notice--(error|success)"
    - from: "app/(markos)/admin/governance/page.module.css"
      to: "app/tokens.css var(--*)"
      via: "var(--color-surface), var(--color-on-surface-muted), var(--space-*), var(--radius-md), --color-primary, --color-primary-subtle, --color-border"
      pattern: "var\\(--(color|space|fs|fw|radius|ls)-"
    - from: "app/(markos)/admin/governance/page.tsx"
      to: ".c-badge--{error,success,info} on decision/status columns with bracketed glyph"
      via: "<span className='c-badge c-badge--{state}'>[glyph] {label}</span> on each row decision cell"
      pattern: "c-badge\\s+c-badge--(error|success|info)"
---

<objective>
Redesign `/admin/governance` (`page.module.css` 120 LOC, 17 inline hex; `page.tsx` 180 LOC; existing `governance.stories.tsx`) into the canonical DESIGN.md v1.1.0 visual contract per `213.4-UI-SPEC.md` Plan 213.4-03 (6 ACs: AG-1..AG-6). Compose `.c-card` (heroCard, panel, detailCard sub-section), `.c-button{,--primary,--destructive}` (Approve / Reject), `.c-modal + .c-backdrop` (rejection confirm), `.c-notice c-notice--{error,success}` (denied-mapping / export-ready), `.c-badge--{error,success,info}` (decision / status column inline badges with bracketed glyphs), `.t-label-caps` (eyebrow / section nav). Token-only `<th>`/`<td>` recipe across all 4 tables (Identity Federation, Access Reviews, Retention and Export, Vendor Inventory). Eliminate the teal `border-left: 4px solid #0d9488` stripe on `.detailCard` (the 4px accent is reserved for `.c-notice`). Extend `governance.stories.tsx` with ≥3 named state stories (`Default` / `DeniedMapping` / `ExportReady`).

Purpose: Close UI-SPEC AC AG-1 (zero inline hex + Sora/Space Grotesk eliminated), AG-2 (heroCard/panel → .c-card; active section: `--color-primary` border + `--color-primary-subtle` bg), AG-3 (4-table token recipe + decision-column `.c-badge--{*}`), AG-4 (`.detailCard` → `.c-card`; teal left-stripe eliminated), AG-5 (denial/export-ready notices via `.c-notice c-notice--{error,success}`), AG-6 (≥3 named stories). Preserve Phase 207 governance-substrate wiring (D-21) and admin role-gating (D-22).

Output: `app/(markos)/admin/governance/page.module.css` rewritten (120 → ~45 LOC); `page.tsx` updated (className-only rewrite + `.c-notice` JSX swap on detailCard variants + `.c-modal` JSX on Reject); `governance.stories.tsx` extended.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/213.4-ui-canon-adoption-wave-4-admin-operations-status-404-workspace-to-design-md-tokens/213.4-CONTEXT.md
@.planning/phases/213.4-ui-canon-adoption-wave-4-admin-operations-status-404-workspace-to-design-md-tokens/213.4-UI-SPEC.md
@.planning/phases/213.4-ui-canon-adoption-wave-4-admin-operations-status-404-workspace-to-design-md-tokens/213.4-RESEARCH.md
@DESIGN.md
@CLAUDE.md
@app/tokens.css
@app/globals.css
@styles/components.css
@app/(markos)/admin/governance/page.module.css
@app/(markos)/admin/governance/page.tsx
@app/(markos)/admin/governance/governance.stories.tsx
@.planning/phases/213.4-ui-canon-adoption-wave-4-admin-operations-status-404-workspace-to-design-md-tokens/213.4-02-PLAN.md

<interfaces>
<!-- Class-Rename Map (RESEARCH.md "Plan 213.4-03 — Admin: Governance"): -->
<!-- .page → padding var(--space-lg); background var(--color-surface) -->
<!-- .shell → max-width var(--w-container); margin 0 auto; display grid; gap var(--space-lg) -->
<!-- .heroCard, .panel → DELETE; compose .c-card JSX-side -->
<!-- .eyebrow → .t-label-caps utility -->
<!-- .title → <h1>/<h2> globals -->
<!-- .layoutGrid → Retain: grid-template-columns 0.9fr 1.6fr 0.9fr; gap var(--space-md) -->
<!-- .sectionTitle → <h4> globals -->
<!-- .sectionList, .mainColumn → display grid; gap var(--space-sm) -->
<!-- .sectionItem → token-only: padding var(--space-xs)/var(--space-sm); border-radius var(--radius-md); border 1px solid var(--color-border); bg transparent; color var(--color-on-surface) -->
<!-- .activeSection → border-color var(--color-primary); background var(--color-primary-subtle) -->
<!-- .table → width 100%; border-collapse collapse -->
<!-- .tableHead, .tableCell → DELETE; <th>/<td> token recipe (D-14) -->
<!-- .detailCard → DELETE; compose .c-card (eliminates border-left: 4px solid #0d9488 teal stripe) -->
<!-- .detailTitle → <h3> globals -->
<!-- .detailText → <p> body-md -->
<!-- Decision badges → ADD: .c-badge--{error,success,info} per decision value -->
<!-- State notices → ADD: .c-notice c-notice--{error,success} for denied/export-ready -->

<!-- Decision-value → badge map (UI-SPEC AG-3 + Copywriting): -->
<!-- "denied" → .c-badge--error + [err] glyph -->
<!-- "ready" / "export_ready" → .c-badge--success + [ok] glyph -->
<!-- "pending" → .c-badge--info + [info] glyph -->

<!-- Notice variant → primitive map (UI-SPEC AG-5): -->
<!-- variant="default" → no notice; data-only display -->
<!-- variant="deniedMapping" → .c-notice c-notice--error + [err] + "Mapping denied. Source claim has no matching canonical role. Review the federation rules." -->
<!-- variant="exportReady" → .c-notice c-notice--success + [ok] + "Export ready. Retention window: P12M. Evidence reference visible for audit." -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite app/(markos)/admin/governance/page.module.css to token-only layout shell + 4-table recipe + section-nav classes (AG-1, AG-2, AG-3, AG-4)</name>
  <read_first>
    - app/(markos)/admin/governance/page.module.css (FULL 120 LOC)
    - app/(markos)/admin/governance/page.tsx (FULL 180 LOC — confirm classNames currently referenced; locate `border-left: 4px solid #0d9488` source)
    - .planning/phases/213.4-.../213.4-RESEARCH.md "Class-Rename Map: Plan 213.4-03 — Admin: Governance" (lines 126–155)
    - .planning/phases/213.4-.../213.4-UI-SPEC.md AG-1..AG-6 + Primitive reuse + Copywriting tables
    - .planning/phases/213.4-.../213.4-02-PLAN.md Task 1 (precedent — same shape)
    - app/tokens.css + app/globals.css + styles/components.css v1.1.0
    - DESIGN.md "Color > Forbidden values" + "Spacing Scale"
  </read_first>
  <files>app/(markos)/admin/governance/page.module.css</files>
  <action>
**Replace `app/(markos)/admin/governance/page.module.css` ENTIRELY with:**

```css
/* Phase 213.4 Plan-03 — Token-only admin/governance layout shell.
 * Composes .c-card (sections + detailCard), .c-button{,--primary,--destructive} (CTAs),
 * .c-notice c-notice--{error,success} (denial / export-ready notices — AG-5),
 * .c-badge--{error,success,info} (decision/status column inline — AG-3),
 * .c-modal + .c-backdrop (Reject confirm),
 * .t-label-caps (eyebrow / section nav labels),
 * vanilla <table> with token-cited <th>/<td> recipe (D-14).
 * All composition lives JSX-side. */

.page {
  padding: var(--space-lg);
  background: var(--color-surface);
  min-height: 100vh;
}

.shell {
  max-width: var(--w-container);
  margin: 0 auto;
  display: grid;
  gap: var(--space-lg);
}

.layoutGrid {
  display: grid;
  grid-template-columns: 0.9fr 1.6fr 0.9fr;
  gap: var(--space-md);
}

@media (max-width: 1023.98px) {
  .layoutGrid {
    grid-template-columns: 1fr;
  }
}

.sectionList,
.mainColumn {
  display: grid;
  gap: var(--space-sm);
  list-style: none;
  margin: 0;
  padding: 0;
}

.sectionItem {
  padding: var(--space-xs) var(--space-sm);
  border-radius: var(--radius-md);
  border: 1px solid var(--color-border);
  background: transparent;
  color: var(--color-on-surface);
  text-align: left;
  font: inherit;
  cursor: pointer;
}

.activeSection {
  border-color: var(--color-primary);
  background: var(--color-primary-subtle);
}

.table {
  width: 100%;
  border-collapse: collapse;
}

.table th {
  padding: var(--space-xs) var(--space-sm);
  color: var(--color-on-surface-muted);
  font-size: var(--fs-label);
  font-weight: var(--fw-semibold);
  text-transform: uppercase;
  letter-spacing: var(--ls-label);
  border-bottom: 1px solid var(--color-border);
  text-align: left;
}

.table td {
  padding: var(--space-xs) var(--space-sm);
  border-bottom: 1px solid var(--color-border);
  color: var(--color-on-surface);
  font-size: var(--fs-body-md);
  vertical-align: middle;
}
```

This eliminates EVERY legacy literal:
- `font-family: 'Sora'` / `'Space Grotesk'` → globals
- `.heroCard` / `.panel` rules → DELETE; compose `.c-card` JSX-side (eliminates `#ffffff`, 20px radius, drop shadow)
- `.detailCard` rule → DELETE INCLUDING `border-left: 4px solid #0d9488` (the 4px stripe is reserved for `.c-notice`; detailCard composes plain `.c-card` per AG-4)
- `.eyebrow` → DELETE (`.t-label-caps` JSX-side)
- `.title` / `.sectionTitle` → DELETE (`<h1>` / `<h4>` globals)
- `.detailTitle` / `.detailText` → DELETE (`<h3>` / `<p>` globals; eliminates `font-size: 16px`, `#0f172a`, `#475569`)
- `.tableHead` / `.tableCell` → DELETE (recipe applies via descendant selectors)
- Active section `#0d9488` border + `#f0fdfa` bg → `var(--color-primary)` + `var(--color-primary-subtle)`
- All off-grid 12/14/16/18/20px → token vars

DO NOT add `font-family:`, `outline:`, `@media (pointer: coarse)`, or `box-shadow:` declarations.
  </action>
  <verify>
    <automated>! grep -nE '#[0-9a-fA-F]{3,8}' "app/(markos)/admin/governance/page.module.css" && ! grep -nE '(Sora|Space Grotesk|linear-gradient|radial-gradient|box-shadow|transform:\s*translate)' "app/(markos)/admin/governance/page.module.css" && ! grep -nE '#0d9488|#0f766e|#14b8a6|#155e75|#f0fdfa|#f8fbfd|#ffffff|#0f172a|#475569' "app/(markos)/admin/governance/page.module.css" && ! grep -nE '@media[^{]*pointer[^{]*coarse' "app/(markos)/admin/governance/page.module.css" && ! grep -nE '\.heroCard\b|\.panel\b|\.detailCard\b|\.detailTitle\b|\.detailText\b|\.eyebrow\b|\.title\b|\.sectionTitle\b|\.tableHead\b|\.tableCell\b' "app/(markos)/admin/governance/page.module.css" && ! grep -nE 'border-left:\s*4px\s+solid' "app/(markos)/admin/governance/page.module.css" && grep -q "var(--color-surface)" "app/(markos)/admin/governance/page.module.css" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **AG-1 (zero hex + Sora):** `grep -nE '#[0-9a-fA-F]{3,8}|Sora|Space Grotesk' page.module.css` returns 0.
    - **AG-2 (no legacy active-section colors):** `grep -nE '#0d9488|#f0fdfa|20px|#e2e8f0' page.module.css` returns 0.
    - **AG-4 (no border-left teal stripe on .detailCard):** `grep -nE 'border-left:\s*4px\s+solid' page.module.css` returns 0 (the only legitimate 4px left-accent is on `.c-notice` primitive — JSX-side only).
    - **No legacy bespoke classes:** `grep -nE '\.heroCard\b|\.panel\b|\.detailCard\b|\.detailTitle\b|\.detailText\b|\.tableHead\b|\.tableCell\b' page.module.css` returns 0.
    - **X-5:** `grep -nE '@media[^{]*pointer[^{]*coarse' page.module.css` returns 0.
    - **Token consumption:** `grep -q "var(--color-surface)" page.module.css`.
  </acceptance_criteria>
  <done>`page.module.css` rewritten ~45 LOC token-only. Atomic commit: `feat(213.4-03): rewrite admin/governance/page.module.css to design-md tokens (AG-1, AG-2, AG-3, AG-4)`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update app/(markos)/admin/governance/page.tsx to compose .c-card (incl. detailCard) / .c-button{,--primary,--destructive} / .c-modal+.c-backdrop (Reject confirm) / .c-notice c-notice--{error,success} (variant-driven) / .c-badge--{error,success,info} on decision columns / .t-label-caps + render semantic <table> across all 4 tables (AG-2, AG-3, AG-5)</name>
  <read_first>
    - app/(markos)/admin/governance/page.tsx (FULL 180 LOC — DOM tree + variant prop + 4 tables to preserve)
    - app/(markos)/admin/governance/page.module.css (post-Task 1 shape)
    - .planning/phases/213.4-.../213.4-UI-SPEC.md AG-2..AG-5 + Copywriting (denial copy + export-ready copy + decision-badge mapping)
    - .planning/phases/213.4-.../213.4-RESEARCH.md "Class-Rename Map: Plan 213.4-03" (lines 126–155) JSX changes + Notice/Banner Audit row 5–6
    - .planning/phases/213.4-.../213.4-02-PLAN.md Task 2 (precedent — variant-driven `.c-notice` mapper + decision badge mapper + `.c-modal` confirm)
    - styles/components.css v1.1.0 + app/globals.css + DESIGN.md
    - app/(markos)/admin/layout.tsx (CONFIRM existence + role-gating preservation per D-22)
  </read_first>
  <files>app/(markos)/admin/governance/page.tsx</files>
  <action>
**Apply className substitutions and JSX shape changes (DOM tree + prop interface UNCHANGED per D-05):**

| Existing | New | Citation |
|----------|-----|----------|
| `<div className={styles.heroCard}>` | `<div className="c-card">` | AG-2 |
| `<div className={styles.panel}>` | `<div className="c-card">` | AG-2 |
| `<div className={styles.detailCard}>` | `<div className="c-card">` (NO border-left stripe) | AG-4 |
| `<span className={styles.eyebrow}>...</span>` | `<span className="t-label-caps">...</span>` | AG-2 |
| `<h1 className={styles.title}>...</h1>` | `<h1>...</h1>` | AG-2 |
| `<h2 className={styles.sectionTitle}>...</h2>` | `<h4>...</h4>` (semantic — sub-heading inside card) | AG-2 |
| `<h3 className={styles.detailTitle}>...</h3>` | `<h3>...</h3>` | AG-4 |
| `<p className={styles.detailText}>...</p>` | `<p>...</p>` | AG-4 |
| Section-nav buttons | `<button className={`${styles.sectionItem} ${isActive ? styles.activeSection : ''}`}>...</button>` | AG-2 layout retained |

**AG-3 — Decision/status column badge mapper.** For every `<td>` rendering a decision or status value, render through:

```tsx
const badgeMap = {
  denied:       { cls: 'c-badge c-badge--error',   glyph: '[err]' },
  ready:        { cls: 'c-badge c-badge--success', glyph: '[ok]' },
  export_ready: { cls: 'c-badge c-badge--success', glyph: '[ok]' },
  pending:      { cls: 'c-badge c-badge--info',    glyph: '[info]' },
} as const;

// In <td>:
<span className={badgeMap[value].cls}>{badgeMap[value].glyph} {value}</span>
```

**AG-5 — Variant-driven `.c-notice` for denial / export-ready states.** Where the surface currently renders state-specific `.detailCard` content driven by a `variant` prop, replace with:

```tsx
const noticeMap = {
  deniedMapping: { cls: 'c-notice c-notice--error',   glyph: '[err]', copy: 'Mapping denied. Source claim has no matching canonical role. Review the federation rules.' },
  exportReady:   { cls: 'c-notice c-notice--success', glyph: '[ok]',  copy: 'Export ready. Retention window: P12M. Evidence reference visible for audit.' },
} as const;

{variant in noticeMap && (
  <div className={noticeMap[variant].cls} role="status">
    <strong>{noticeMap[variant].glyph}</strong>{" "}{noticeMap[variant].copy}
  </div>
)}
```

**AG-3 + AG-5 — Render all 4 tables (Identity Federation, Access Reviews, Retention and Export, Vendor Inventory) as semantic `<table>` with `<thead>` / `<tbody>` / `<tr>` / `<th scope="col">` / `<td>`.** No `className` on `<th>` / `<td>` (token-recipe applies via descendant selectors). Each row renders the `badgeMap` cell on its decision/status column.

**Reject button + confirm modal** (governance approval workflow):

```tsx
<button type="button" className="c-button c-button--primary" onClick={onApprove}>Approve</button>
<button type="button" className="c-button c-button--destructive" onClick={openRejectModal}>Reject</button>

{showRejectModal && (
  <>
    <div className="c-backdrop" onClick={cancelReject} />
    <div className="c-modal" role="dialog" aria-modal="true" aria-labelledby="reject-title">
      <h3 id="reject-title">Reject this approval?</h3>
      <p>A rejection is logged as Phase 207 audit-log evidence and cannot be undone.</p>
      <div style={{ display: 'flex', gap: 'var(--space-xs)' }}>
        <button type="button" className="c-button c-button--secondary" onClick={cancelReject}>Cancel</button>
        <button type="button" className="c-button c-button--destructive" onClick={confirmReject}>Reject</button>
      </div>
    </div>
  </>
)}
```

**Banned-lexicon scan = 0.** No exclamation. No emoji. No "Please". Phase 207 governance-substrate wiring (any `await` calls, server actions, prop interfaces) PRESERVED. Admin role-gate in `app/(markos)/admin/layout.tsx` UNTOUCHED per D-22.

After edit: `npm run build` MUST exit 0.
  </action>
  <verify>
    <automated>grep -qE 'c-notice c-notice--(error|success)' "app/(markos)/admin/governance/page.tsx" && grep -qE '\[ok\]|\[err\]|\[info\]' "app/(markos)/admin/governance/page.tsx" && grep -qE 'c-button c-button--primary' "app/(markos)/admin/governance/page.tsx" && grep -qE 'c-button c-button--destructive' "app/(markos)/admin/governance/page.tsx" && grep -qE 'c-modal' "app/(markos)/admin/governance/page.tsx" && grep -qE 'c-backdrop' "app/(markos)/admin/governance/page.tsx" && grep -qE 'c-badge c-badge--(error|success|info)' "app/(markos)/admin/governance/page.tsx" && grep -qE '<table' "app/(markos)/admin/governance/page.tsx" && ! grep -qE 'styles\.(heroCard|panel|detailCard|detailTitle|detailText|primaryButton|secondaryButton|eyebrow|sectionTitle|title|tableHead|tableCell)' "app/(markos)/admin/governance/page.tsx" && ! grep -inE 'Please|synergy|leverage|empower|unlock|revolutionize|supercharge|holistic|seamless|cutting-edge|innovative|game-changer|next-generation|world-class|best-in-class|reimagine|disrupt' "app/(markos)/admin/governance/page.tsx" && npm run build && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **AG-2 (.c-card):** `grep -cE '"c-card"' page.tsx` ≥1.
    - **AG-3 (.c-badge--{*} on decision/status + bracketed glyphs):** `grep -cE 'c-badge c-badge--(error|success|info)' page.tsx` ≥1; `grep -cE '\[(ok|err|info)\]' page.tsx` ≥3.
    - **AG-5 (.c-notice variant-driven):** `grep -cE 'c-notice c-notice--(error|success)' page.tsx` ≥2.
    - **Reject + modal primitives:** `grep -cE 'c-button c-button--primary' page.tsx` ≥1; `grep -cE 'c-button c-button--destructive' page.tsx` ≥1; `grep -cE 'c-modal' page.tsx` ≥1; `grep -cE 'c-backdrop' page.tsx` ≥1.
    - **Semantic tables:** `grep -c '<table' page.tsx` ≥1; `grep -c '<thead' page.tsx` ≥1.
    - **No legacy class refs:** `grep -cE 'styles\.(heroCard|panel|detailCard|detailTitle|detailText|primaryButton|secondaryButton|eyebrow|sectionTitle|title|tableHead|tableCell)' page.tsx` returns 0.
    - **Banned-lexicon = 0:** `grep -inE 'Please|synergy|leverage|empower|unlock|revolutionize|supercharge|holistic|seamless|cutting-edge|innovative|game-changer|next-generation|world-class|best-in-class|reimagine|disrupt' page.tsx` returns 0.
    - **Build green:** `npm run build` exits 0.
  </acceptance_criteria>
  <done>`page.tsx` updated with primitives + variant-driven `.c-notice` + decision badges + `.c-modal` Reject confirm + semantic `<table>` × 4. Atomic commit: `feat(213.4-03): compose primitives in admin/governance/page.tsx (AG-2, AG-3, AG-5)`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend app/(markos)/admin/governance/governance.stories.tsx with ≥3 named state stories (Default / DeniedMapping / ExportReady) per UI-SPEC AG-6 + run verification gates</name>
  <read_first>
    - app/(markos)/admin/governance/governance.stories.tsx (FULL — confirm existing meta default; extend, don't replace)
    - app/(markos)/admin/governance/page.tsx (post-Task 2 — confirm prop interface; particularly variant prop + table data shape)
    - .planning/phases/213.4-.../213.4-UI-SPEC.md AG-6 + Storybook State Coverage table (lines 339–343)
    - .planning/phases/213.4-.../213.4-02-PLAN.md Task 3 (precedent CSF3 pattern)
  </read_first>
  <files>app/(markos)/admin/governance/governance.stories.tsx</files>
  <action>
**Extend with ≥3 named state stories per UI-SPEC AG-6:**

```tsx
import type { Meta, StoryObj } from '@storybook/react';
import AdminGovernancePage from './page';

const meta = {
  title: 'Admin/Governance',
  component: AdminGovernancePage,
  parameters: { layout: 'fullscreen' },
} satisfies Meta<typeof AdminGovernancePage>;

export default meta;
type Story = StoryObj<typeof meta>;

export const Default: Story = {
  args: {
    variant: 'default',
    activeSection: 'identity-federation',
    rows: [
      { id: 'fed_001', source: 'okta:engineering', target: 'role:engineer', decision: 'ready' },
      { id: 'fed_002', source: 'azure:finance',    target: 'role:finance',  decision: 'pending' },
    ],
  },
  parameters: { docs: { description: { story: 'Default identity-federation table view per UI-SPEC AC AG-3. .c-card sections + .c-badge--{success,info} decision column.' } } },
};

export const DeniedMapping: Story = {
  args: {
    variant: 'deniedMapping',
    activeSection: 'identity-federation',
    rows: [
      { id: 'fed_003', source: 'okta:contractors', target: 'role:owner', decision: 'denied' },
    ],
  },
  parameters: { docs: { description: { story: 'Renders .c-notice c-notice--error per UI-SPEC AC AG-5. [err] glyph + denial copy. .c-badge--error decision column.' } } },
};

export const ExportReady: Story = {
  args: {
    variant: 'exportReady',
    activeSection: 'retention-and-export',
    rows: [
      { id: 'ret_001', target: 'workspace:acme', retention: 'P12M', status: 'export_ready' },
    ],
  },
  parameters: { docs: { description: { story: 'Renders .c-notice c-notice--success per UI-SPEC AC AG-5. [ok] glyph + export-ready copy. .c-badge--success status column.' } } },
};
```

KEY rules: `Meta<typeof AdminGovernancePage>` typed; 3 named exports `Default` / `DeniedMapping` / `ExportReady`; `meta.title='Admin/Governance'`; no `parameters.theme: "legacy"`; args mirror actual prop interface (executor inspects post-Task 2 file).

After edit: `npm run build-storybook` + `npm test` MUST exit 0.
  </action>
  <verify>
    <automated>test -f "app/(markos)/admin/governance/governance.stories.tsx" && grep -cE '^export const (Default|DeniedMapping|ExportReady):' "app/(markos)/admin/governance/governance.stories.tsx" | awk '$1>=3 {exit 0} $1<3 {exit 1}' && grep -q "title: 'Admin/Governance'" "app/(markos)/admin/governance/governance.stories.tsx" && ! grep -qE 'parameters\.theme.*"legacy"' "app/(markos)/admin/governance/governance.stories.tsx" && grep -q 'export default meta' "app/(markos)/admin/governance/governance.stories.tsx" && npm run build-storybook && npm test && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **AG-6 (≥3 named state stories):** `grep -cE '^export const (Default|DeniedMapping|ExportReady):' governance.stories.tsx` ≥3.
    - **`meta.title='Admin/Governance'`:** `grep -c "title: 'Admin/Governance'" governance.stories.tsx` ≥1.
    - **No legacy theme:** `grep -cE 'parameters\.theme.*"legacy"' governance.stories.tsx` returns 0.
    - **Storybook build green:** `npm run build-storybook` exits 0.
    - **Wiring suites green:** `npm test` exits 0 (Phase 207 substrate preserved).
  </acceptance_criteria>
  <done>`governance.stories.tsx` extended with 3 named state stories. Atomic commit: `feat(213.4-03): extend admin/governance/governance.stories.tsx with Default/DeniedMapping/ExportReady (AG-6)`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator→admin/governance API | Authenticated admin role accessing federation mapping + access-review + retention actions; Phase 207 substrate connector crosses here |
| admin role-gate | `app/(markos)/admin/layout.tsx` enforces admin/owner — preserved per D-22 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213.4-03-01 | Tampering | Reject action on approval | mitigate | `.c-modal` confirm with explicit "logged as Phase 207 audit-log evidence" copy; server-side audit emit unchanged |
| T-213.4-03-02 | Elevation of Privilege | admin role-gate | accept | `app/(markos)/admin/layout.tsx` UNTOUCHED per D-22 |
| T-213.4-03-03 | Information Disclosure | denial reason copy | accept | Copy revisions are additive (`[err]` glyph + remediation); no change to data exposure |
</threat_model>

<verification>
```bash
grep -nE '#[0-9a-fA-F]{3,8}' "app/(markos)/admin/governance/page.module.css"  # 0
grep -nE 'border-left:\s*4px\s+solid' "app/(markos)/admin/governance/page.module.css"  # 0
grep -cE 'c-notice c-notice--(error|success)' "app/(markos)/admin/governance/page.tsx"  # ≥2
grep -cE 'c-badge c-badge--(error|success|info)' "app/(markos)/admin/governance/page.tsx"  # ≥1
grep -cE '^export const (Default|DeniedMapping|ExportReady):' "app/(markos)/admin/governance/governance.stories.tsx"  # ≥3
npm run build && npm run build-storybook && npm test  # all 0
```
</verification>

<success_criteria>
UI-SPEC AC AG-1..AG-6 close. Phase 207 substrate + admin role-gate preserved. 3 atomic commits.
</success_criteria>

<output>
Create `.planning/phases/213.4-.../213.4-03-SUMMARY.md` per precedent. Hand-off to Plan 213.4-04 (operations root).
</output>
</content>
</invoke>