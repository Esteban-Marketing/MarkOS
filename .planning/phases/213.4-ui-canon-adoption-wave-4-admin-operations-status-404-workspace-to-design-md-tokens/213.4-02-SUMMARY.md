---
phase: 213.4
plan: "02"
subsystem: admin/billing
tags: [ui-canon, design-md, tokens, admin, billing, reconciliation, storybook]
dependency_graph:
  requires: ["213.4-01"]
  provides: ["213.4-03"]
  affects: ["app/(markos)/admin/billing/page.module.css", "app/(markos)/admin/billing/page.tsx", "app/(markos)/admin/billing/reconciliation.stories.tsx"]
tech_stack:
  added: []
  patterns: [".c-notice c-notice--{state}", ".c-badge--{warning,info,success}", ".c-button--destructive + .c-modal", "noticeMap variant-driven composition", "RowBadge helper component"]
key_files:
  created: []
  modified:
    - app/(markos)/admin/billing/page.module.css
    - app/(markos)/admin/billing/page.tsx
    - app/(markos)/admin/billing/reconciliation.stories.tsx
decisions:
  - "noticeMap const drives all 3 notice variants from a single JSX composition — no if/else JSX trees"
  - "RowBadge helper component extracted for reuse across 6 row status fields"
  - "badgeMap covers both PascalCase labels (from UI-SPEC) and lowercase field values (from page.tsx data shape)"
  - "title: 'Admin/Billing' (single quotes) per UI-SPEC AB-8 literal requirement"
metrics:
  duration: "~15 minutes"
  completed: "2026-04-29T04:43:29Z"
  tasks_completed: 3
  tasks_total: 3
  files_modified: 3
---

# Phase 213.4 Plan 02: Admin/Billing Surface Summary

**One-liner:** Token-only admin/billing redesign composing `.c-notice`/`.c-badge`/`.c-modal`/`.c-button--destructive` primitives with variant-driven noticeMap and bracketed-glyph RowBadge on reconciliation rows.

## Tasks Completed

| # | Name | Commit | Files |
|---|------|--------|-------|
| 1 | Rewrite page.module.css to token-only layout shell (AB-1, AB-3, AB-4) | fc6a7ef | page.module.css |
| 2 | Compose primitives in page.tsx (AB-2, AB-5, AB-6, AB-7) | 5585427 | page.tsx |
| 3 | Extend reconciliation.stories.tsx with Healthy/HoldState/SyncFailure (AB-8) | 4c94b84 | reconciliation.stories.tsx |

## Acceptance Criteria Closed

| AC | Description | Status |
|----|-------------|--------|
| AB-1 | Zero inline hex; zero Sora/Space Grotesk in page.module.css | Closed |
| AB-2 | `.c-card` on hero/panel sections; drop shadow eliminated | Closed |
| AB-3 | Queue tabs token-only; active border `--color-primary` + bg `--color-primary-subtle` | Closed |
| AB-4 | Table `<th>`/`<td>` token recipe (D-14) | Closed |
| AB-5 | `.c-badge--{warning,info,success}` with bracketed glyphs on reconciliation row state | Closed |
| AB-6 | `.c-button--primary/--secondary/--destructive` + `.c-modal` write-off confirm | Closed |
| AB-7 | `.c-notice c-notice--{success,warning,error}` variant-driven via noticeMap | Closed |
| AB-8 | `reconciliation.stories.tsx` extended: Healthy/HoldState/SyncFailure CSF3 exports | Closed |

## Verification Gate Results

```
grep -nE '#[0-9a-fA-F]{3,8}' page.module.css          → 0 (PASS)
grep -nE '(Sora|Space Grotesk|box-shadow|gradient)' page.module.css → 0 (PASS)
grep legacy class refs in page.module.css              → 0 (PASS)
grep -cE 'c-notice c-notice--(success|warning|error)' page.tsx → 3 (PASS)
grep -cE 'c-button c-button--primary' page.tsx         → 1 (PASS)
grep -cE 'c-button c-button--destructive' page.tsx     → 2 (PASS)
grep -cE 'c-modal|c-backdrop' page.tsx                 → 1+1 (PASS)
grep -cE 'c-badge c-badge--(warning|info|success)' page.tsx → 15 (PASS)
grep legacy style refs in page.tsx                     → 0 (PASS)
grep -cE '^export const (Healthy|HoldState|SyncFailure):' stories.tsx → 3 (PASS)
npm run build:llm (tsc --noEmit)                       → exit 0 (PASS)
npm run build-storybook                                → exit 0 (PASS)
npm run test:ui-a11y                                   → 110/110 pass (PASS)
```

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] badgeMap keys extended to cover lowercase row field values**
- **Found during:** Task 2
- **Issue:** page.tsx data shape uses lowercase values (`"ready"`, `"reconciled"`, `"failed"`, `"pending"`, `"hold"`, `"released"`, `"restored"`, `"review"`) while UI-SPEC AB-5 map lists PascalCase labels. Both needed.
- **Fix:** badgeMap includes both PascalCase and lowercase keys so RowBadge works correctly against actual data.
- **Files modified:** app/(markos)/admin/billing/page.tsx
- **Commit:** 5585427

**2. [Rule 3 - Blocking] `npm run build` does not exist (Node/CLI project, not Next.js)**
- **Found during:** Task 2 verification
- **Issue:** Plan references `npm run build` but this project has no build script; it uses `npm run build:llm` (tsc --noEmit) for TypeScript validation.
- **Fix:** Used `npm run build:llm` for TS check + `npm run build-storybook` for Storybook bundle verification.
- **Commit:** n/a (verification adaptation only)

## Known Stubs

None. All primitives wire to live variant/data props.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes introduced. Write-off confirm modal is UI-only (existing server-side audit-log in Phase 207 substrate unchanged). Admin role-gate in `app/(markos)/admin/layout.tsx` untouched per D-22.

## Chromatic Review Reminder

Run Chromatic after merge to verify `Admin/Billing` → Healthy / HoldState / SyncFailure stories render correctly against DESIGN.md v1.1.0 dark token palette. Specifically verify:
- `.c-notice c-notice--success` (mint left-stripe, `--color-success` text)
- `.c-notice c-notice--warning` (amber left-stripe, `--color-warning` text)
- `.c-notice c-notice--error` (red left-stripe, `--color-error` text)
- Active tab `border-color: var(--color-primary)` visible against dark surface

## Hand-off

Next plan: **213.4-03** (admin/governance surface).

## Self-Check: PASSED

- `app/(markos)/admin/billing/page.module.css` — exists, token-only, no hex
- `app/(markos)/admin/billing/page.tsx` — exists, all primitive classes present
- `app/(markos)/admin/billing/reconciliation.stories.tsx` — exists, 3 named exports
- Commits fc6a7ef, 5585427, 4c94b84 — all present in git log
