---
phase: 97-universal-template-and-business-model-coverage-upgrade
plan: "03"
type: execute
wave: 3
depends_on:
  - "97-01"
  - "97-02"
files_modified:
  - onboarding/backend/agents/example-resolver.cjs
  - onboarding/backend/agents/skeleton-generator.cjs
  - onboarding/backend/literacy-chunker.cjs
  - bin/ingest-literacy.cjs
  - test/phase-97/resolver-fallbacks.test.js
autonomous: true
requirements:
  - NLI-03
  - NLI-04
must_haves:
  truths:
    - "deterministic runtime selection uses the new family registry so alias and edge-case business models resolve to a useful universal template path before generic fallback"
    - "stage-aware tone, proof, and naturality guidance are handed explicitly into the resolver selection path and round-trip through the repo's existing chunking and ingest preparation flow"
    - "current example and skeleton generation behavior remains backward-compatible and non-regressive"
    - "the phase remains additive, reusable, and deterministic without implementing Phase 98 reasoning or Phase 99 training alignment"
    - "when the resolver finds a canonical family match it checks for a per-model overlay doc per D-07 and returns both the base doc path and overlay doc path as a composition pair so Phase 98 ICP reasoning can select the right overlay at runtime without re-querying the family map"
  artifacts:
    - path: onboarding/backend/agents/example-resolver.cjs
      provides: deterministic family-aware fallback for examples and skeletons
    - path: onboarding/backend/agents/skeleton-generator.cjs
      provides: output generation that preserves current discipline coverage while consuming the upgraded business-model logic
    - path: onboarding/backend/literacy-chunker.cjs
      provides: chunking compatibility for newly authored template family metadata
    - path: bin/ingest-literacy.cjs
      provides: dry-run and ingest-path validation for the new Phase 97 template assets
    - path: test/phase-97/resolver-fallbacks.test.js
      provides: proof that alias inputs and unknown-edge cases resolve deterministically and do not regress current behavior
  key_links:
    - from: onboarding/backend/agents/example-resolver.cjs
      to: onboarding/backend/research/template-family-map.cjs
      via: existing resolver calls should use the new normalized family and alias logic before returning empty output
    - from: onboarding/backend/agents/skeleton-generator.cjs
      to: .agent/markos/literacy/
      via: generated starter outputs should remain compatible with the broader Phase 97 template-family coverage
    - from: onboarding/backend/literacy-chunker.cjs
      to: bin/ingest-literacy.cjs
      via: the newly authored frontmatter fields must stay stable through chunk generation and ingest preparation
---

<objective>
Wire the universal template-family upgrade into the deterministic runtime path and close the regression loop for NLI-03 and NLI-04.

Purpose: complete the phase by making the authored family map usable through the current example and skeleton surfaces, while explicitly preserving backward compatibility and the repo's approved existing baselines. Per D-07, the resolver must check for per-model overlay docs at steps 1 and 2 of the fallback order and return a base+overlay composition pair rather than a single path when an overlay exists — this prepares Phase 98 ICP reasoning to pick the right overlay at runtime.
Output: resolver fallback improvements, ingest and chunking compatibility for the new assets, and a focused regression suite proving the upgrade is stable.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/97-universal-template-and-business-model-coverage-upgrade/97-CONTEXT.md
@.planning/phases/97-universal-template-and-business-model-coverage-upgrade/97-RESEARCH.md
@.planning/phases/97-universal-template-and-business-model-coverage-upgrade/97-VALIDATION.md
@.planning/phases/97-universal-template-and-business-model-coverage-upgrade/97-01-PLAN.md
@.planning/phases/97-universal-template-and-business-model-coverage-upgrade/97-02-PLAN.md
@.planning/phases/96-neuro-aware-literacy-schema-and-taxonomy-expansion/96-VALIDATION.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@onboarding/backend/agents/example-resolver.cjs
@onboarding/backend/agents/skeleton-generator.cjs
@onboarding/backend/literacy-chunker.cjs
@bin/ingest-literacy.cjs

<interfaces>
Current runtime behavior to preserve:
- resolveExample(...) returns a markdown injection block or ''
- resolveSkeleton(discipline, businessModel) returns a starter skeleton or ''
- generateSkeletons(seed, approvedDrafts, outputBasePath, templatesBasePath) writes per-discipline starter outputs
- parseLiteracyFrontmatter(markdown) and chunkLiteracyFile(markdown, metadata) normalize and carry optional Phase 96 metadata

Phase 97 fallback order should become explicit and deterministic:
1. exact discipline + canonical family + funnel-stage match — if a per-model overlay doc exists (D-07), return { baseDoc, overlayDoc } composition pair; otherwise return { baseDoc } only
2. discipline + canonical family match — same overlay check applies
3. stage-aware shared universal template
4. existing starter skeleton or current model-specific example
5. only then generic fallback or empty string

Composition pair format (for steps 1 and 2 when overlay exists per D-07):
  { baseDoc: string, overlayDoc: string }  // both relative paths, consumer picks overlay based on resolved business-model slug
  { baseDoc: string }                      // no overlay available — Phase 98 will add ICP tailoring at runtime
</interfaces>
</context>

<threat_model>
- T-97-06 (silent empty fallback): alias or partial business-model inputs could still yield empty output even after authoring. Mitigation: route current resolver behavior through the new family-map fallback order and lock it with tests.
- T-97-03 (metadata drift): authored tone and naturality fields may be dropped during chunking or ingest prep. Mitigation: include chunker and ingest compatibility in the regression gate.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create failing regression tests for family-aware resolver fallback and non-empty coverage behavior</name>
  <files>test/phase-97/resolver-fallbacks.test.js</files>
  <behavior>
    - alias inputs such as agency, services, consulting, info-products, ecommerce, DTC, Marketplace, and Agents-aaS resolve predictably through the new family map
    - current B2B, B2C, and SaaS behavior does not regress
    - deterministic fallback uses a useful universal template path before degrading to empty output
  </behavior>
  <action>Write the tests first using the existing example-resolver and skeleton-generator surfaces as the real behavior under test. Per D-04, keep the work additive to the current runtime. Per D-05, do not add any reasoning engine or LLM selection logic; this is only deterministic aliasing, fallback, and regression enforcement.</action>
  <verify>
    <automated>node --test test/phase-97/resolver-fallbacks.test.js test/example-resolver.test.js test/skeleton-generator.test.js</automated>
  </verify>
  <done>Wave 0 regression tests prove the universal-library upgrade must remain deterministic and backward-compatible.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the example and skeleton resolvers to consume the universal family map with explicit fallback order</name>
  <files>onboarding/backend/agents/example-resolver.cjs, onboarding/backend/agents/skeleton-generator.cjs</files>
  <action>Update the runtime resolution path so the existing resolver surfaces consult the Phase 97 family map and explicitly accept funnel-stage and buying-maturity hints from template metadata before returning empty output. Preserve the current 7 canonical slugs and deterministic output shape, but add normalized aliases and governed family fallback per D-01 and D-04. Ensure the fallback order is exact match -> family match -> stage-aware shared template -> existing starter skeleton -> generic fallback. Per D-07, at steps 1 and 2 of the fallback order, check whether a per-model overlay doc is registered in the family entry's overlayDocs map; if a match exists, return a composition pair { baseDoc, overlayDoc } rather than a single path so Phase 98 ICP reasoning can select the right overlay without re-querying the map. Do not introduce hidden prompt logic, Phase 98 ICP reasoning, or Phase 99 agent-training behavior.</action>
  <verify>
    <automated>node --test test/phase-97/resolver-fallbacks.test.js test/example-resolver.test.js test/skeleton-generator.test.js</automated>
  </verify>
  <done>Runtime selection uses the new universal template-family coverage without regressing the shipped example and skeleton generator behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve metadata round-trip and close the focused regression gate for the Phase 97 library upgrade</name>
  <files>onboarding/backend/literacy-chunker.cjs, bin/ingest-literacy.cjs</files>
  <action>Confirm the new Phase 97 template-family docs and their stage, proof, tone, and naturality metadata remain stable through frontmatter parsing, chunk generation, and ingest preparation. Keep the existing required fields intact and preserve the current dry-run and non-destructive workflow posture. Finish by rerunning the focused Phase 97 regression suite plus the carried Phase 96 baselines.</action>
  <verify>
    <automated>node --test test/skeleton-generator.test.js test/example-resolver.test.js test/phase-96/*.test.js test/phase-97/*.test.js</automated>
  </verify>
  <done>The Phase 97 upgrade is runtime-usable, additive, deterministic, and regression-safe across the existing MarkOS surfaces.</done>
</task>

</tasks>

<verification>
- node --test test/phase-97/resolver-fallbacks.test.js test/example-resolver.test.js test/skeleton-generator.test.js
- node --test test/skeleton-generator.test.js test/example-resolver.test.js test/phase-96/*.test.js test/phase-97/*.test.js
</verification>

<success_criteria>
- Required business-model families resolve through deterministic runtime fallback instead of silent empties.
- Stage-aware tone, proof, and naturality metadata survive the current literacy pipeline.
- Existing example and skeleton workflows remain stable and backward-compatible.
- Phase 97 closes with reusable library coverage and regression evidence only, excluding later-phase reasoning and training work.
</success_criteria>

<output>
After completion, create .planning/phases/97-universal-template-and-business-model-coverage-upgrade/97-03-SUMMARY.md.
</output>
