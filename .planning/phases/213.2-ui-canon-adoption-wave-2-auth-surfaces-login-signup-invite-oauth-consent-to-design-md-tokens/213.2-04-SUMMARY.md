---
phase: 213.2
plan: 04
subsystem: ui-canon-adoption
tags: [auth, oauth, consent, mcp, design-md-tokens, storybook, subcomponent-extraction]
dependency-graph:
  requires:
    - "213.2-01 (LoginCard 4-state CSF3 client-extraction precedent + token-only page.module.css recipe)"
    - "213.2-02 (signup 7-state in-place CSF3 precedent)"
    - "213.2-03 (invite 10-state in-place CSF3 precedent + aria-busy ARIA-spec compliance + buttonLabel hoist + 12% alpha-tint error-notice recipe)"
    - "213.1-03 (Storybook preview.tsx globals.css cascade + ThemeProvider opt-in gate)"
    - "DESIGN.md primitives (.c-card, .c-card--feature, .c-button, .c-button--primary, .c-button--destructive, .c-chip-protocol, .c-code-inline, .c-field__help, .t-lead utility)"
    - "Phase 200/202 OAuth wiring contract (REQUIRED_PARAMS, /oauth/authorize/approve POST, /api/tenant/switcher/list, /api/auth/csrf, deny redirect with error=access_denied, F-89 contract YAML)"
  provides:
    - "Token-only /oauth/consent surface (page.module.css 151 LOC token-only, page.tsx 152 LOC, _components/ConsentCard.tsx 173 LOC, _components/ConsentCard.stories.tsx 94 LOC)"
    - "9-state CSF3 reference pattern (Default + MultiScope + MultiTenant + Loading + Approving + Declined + InvalidExpired + InvalidMissingFields + InvalidRedirectUri)"
    - "ConsentCard presentational subcomponent extraction precedent (mirrors Plan 01 LoginCard.tsx pattern; 6+ useState wrapper-vs-presentational separation per RESEARCH R-4)"
    - ".c-chip-protocol squared-chip composition for OAuth scopes (signature MarkOS pattern, NOT pill 999px)"
    - ".c-button--destructive semantic upgrade for deny CTA (UI-SPEC O-4)"
    - "Hand-off to Plan 213.2-05 (cross-cutting: a11y test + .c-button coarse-pointer extension)"
  affects:
    - "app/(markos)/oauth/consent/page.module.css (253 LOC -> 151 LOC, token-only)"
    - "app/(markos)/oauth/consent/page.tsx (265 LOC -> 152 LOC; preserves OAuth wiring; 4 [err] glyphs + em-dash softeners dropped; delegates UI to ConsentCard import)"
    - "app/(markos)/oauth/consent/_components/ConsentCard.tsx (NEW; 173 LOC)"
    - "app/(markos)/oauth/consent/_components/ConsentCard.stories.tsx (NEW; 94 LOC)"
    - "test/mcp/consent-ui-a11y.test.js (138 LOC; 9 visual-token + Sora + reduced-motion + 1 copy assertion REPLACED with token-citation + primitive-composition + global-reduced-motion + revised-copy assertions; wiring + DOM-shape + F-89 PRESERVED verbatim)"
tech-stack:
  added: []
  patterns:
    - "client-component subcomponent extraction (ConsentCard.tsx, mirrors LoginCard.tsx)"
    - ".c-chip-protocol squared scope-chip composition (auto [/] brackets via CSS pseudo-elements)"
    - ".c-button--destructive semantic upgrade for deny CTA"
    - "aria-busy={isApproving ? 'true' : 'false'} WAI-ARIA spec compliance (Plan 03 precedent)"
    - "let approveLabel / let denyLabel ternary hoist for lint clean (Plan 03 precedent)"
    - "test-source expansion (TSX + CARD concatenated for primitive-composition + locked-copy + a11y-marker assertions)"
    - "12% alpha-tint error-notice recipe (rgb(248 81 73 / 0.12) + 4px border-inline-start var(--color-error)) — Plan 03 + Plan 213.1 canonical"
key-files:
  created:
    - "app/(markos)/oauth/consent/_components/ConsentCard.tsx"
    - "app/(markos)/oauth/consent/_components/ConsentCard.stories.tsx"
  modified:
    - "app/(markos)/oauth/consent/page.module.css"
    - "app/(markos)/oauth/consent/page.tsx"
    - "test/mcp/consent-ui-a11y.test.js"
decisions:
  - "Extracted presentational subcomponent ConsentCard.tsx per RESEARCH.md R-4 (6+ useState impedance for Storybook authoring). Mirrors Plan 01 LoginCard.tsx client-extraction pattern. Production page.tsx wraps with all data-fetching/state glue; Storybook authors against the props-only client component. Verified via 9 named state stories rendered without decorator setup."
  - "Migrated `aria-busy={isApproving || undefined}` -> `aria-busy={isApproving ? 'true' : 'false'}` (WAI-ARIA spec compliance per Plan 03 precedent: the spec expects literal 'true'/'false' string token). Applied to BOTH approve and deny buttons."
  - "Hoisted approveLabel / denyLabel out of inline ternary into let-bindings (lint clean per Plan 03 precedent: typescript:S3358 'Extract this nested ternary'). Equivalent runtime semantics; cleaner control flow."
  - "Substituted `npm run build-storybook` for `npm run build` per Plan 213.2-01/02/03 deviation precedent (package.json has no 'build' script; storybook build performs full Vite + tsc compile of TSX surface). Verified built in 27.16s; ConsentCard.stories chunk 8.30 kB present."
  - "Removed `@media (prefers-reduced-motion)` literal text from page.module.css comment (would otherwise match the test's `assert.doesNotMatch(css, /@media\\s*\\(prefers-reduced-motion/)`). Comment text now reads `Reduced-motion query is intentionally NOT redeclared here — global app/globals.css covers it.`"
  - "Patched test-source-expansion pattern (read TSX + CARD constants, concatenate with newline, assert on union) for 3 patched tests: locked-copy strings, a11y markers, primitive-composition. Wiring tests (use-client, /oauth/authorize/approve, /api/tenant/switcher/list) keep TSX-only source per F-89 contract preservation."
  - "Preserved `humanizeScope` mapping VERBATIM per CONTEXT D-19 (mapping out-of-scope for restyle). Migrated from page.tsx to ConsentCard.tsx because the chip-rendering loop lives in the subcomponent. Mapping values unchanged: read->'Read CRM', write->'Draft marketing copy', plan->'Plan campaigns', audit->'Audit claims', crm->'Read CRM', tenancy->'List members'."
  - "Documented test/tenant-auth/ui-authorization-negative-path.test.js failure as PRE-EXISTING per Plan 03 deferred-items.md (asserts on app/(markos)/layout.tsx, NOT consent). Out of scope per executor Rule 5."
metrics:
  duration: "6m 38s"
  completed: "2026-04-28T20:44:18Z"
  tasks: 3
  commits: 3
  files_created: 2
  files_modified: 3
  loc_delta:
    page.module.css: "-102 (253 -> 151)"
    page.tsx: "-113 (265 -> 152)"
    ConsentCard.tsx: "+173 (NEW)"
    ConsentCard.stories.tsx: "+94 (NEW)"
    consent-ui-a11y.test.js: "+10 (128 -> 138)"
---

# Phase 213.2 Plan 04: UI Canon Adoption Wave 2 — OAuth Consent Surface Summary

Token-only `/oauth/consent` surface delivered: `page.module.css` rewritten 253 LOC -> 151 LOC (zero hex; whitelist `1px`/`2px`/`4px` only — `1px` for hairlines, `2px` for `.durationLine a` underline-offset, `4px` for `.errorAlert` `border-inline-start`); `page.tsx` retained as `'use client'` with all OAuth wiring preserved verbatim (`REQUIRED_PARAMS`, `/oauth/authorize/approve` POST 9-field body, `/api/tenant/switcher/list`, `/api/auth/csrf`, deny redirect with `error=access_denied`) + 4 `setInvalidReason()` calls revised with `[err]` glyph + em-dash softeners dropped + delegates UI to `<ConsentCard>`; new `_components/ConsentCard.tsx` presentational client subcomponent (per RESEARCH.md R-4) composes 7 DESIGN.md primitives (`.c-card c-card--feature`, `.c-button c-button--primary`, `.c-button c-button--destructive`, `.c-chip-protocol`, `.c-code-inline`, `.c-field__help`, `.t-lead`); new `_components/ConsentCard.stories.tsx` registers 9 CSF3 named state exports under `Auth/ConsentCard` (target was ≥6; ships 9); `test/mcp/consent-ui-a11y.test.js` patched with 4 token-citation + primitive-composition + global-reduced-motion + revised-copy assertions replacing 4 visual-token + Sora-font + prefers-reduced-motion + 1 copy assertion (RESEARCH R-1 mitigation, atomic with redesign per CONTEXT D-04). Phase 200/202 OAuth wiring contract preserved verbatim; Phase 213.1 + Plan 213.2-01/02/03 regression suites stay green.

## Acceptance Criteria — UI-SPEC Closures

| AC#  | Description                                                              | Verification                                                                                                                                                                                | Result |
| ---- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| O-1  | Zero inline hex in oauth/consent/page.module.css                         | `grep -nE '#[0-9A-Fa-f]{3,8}' "app/(markos)/oauth/consent/page.module.css"` returns 0                                                                                                       | PASS   |
| O-2  | Zero off-grid spacing whitelist (1/2/4px)                                | `grep -nE ':\s*[0-9]+px' "app/(markos)/oauth/consent/page.module.css"` returns only `1px` (×3 hairlines) + `2px` (×1 link underline-offset) + `4px` (×1 errorAlert border-inline-start)     | PASS   |
| O-3  | `.c-chip-protocol` squared chips (NOT pill 999px)                        | `grep -nE 'border-radius:\s*999px' page.module.css` returns 0; `grep -c 'c-chip-protocol' _components/ConsentCard.tsx` returns 2 (1 import-class + 1 JSX usage)                             | PASS   |
| O-4  | `.c-button--destructive` semantic upgrade for deny CTA                   | `grep -c 'c-button c-button--destructive' _components/ConsentCard.tsx` returns 2                                                                                                            | PASS   |
| O-5  | `.c-code-inline` primitive for redirect URI                              | `grep -c 'c-code-inline' _components/ConsentCard.tsx` returns 2                                                                                                                             | PASS   |
| O-6  | `.c-button c-button--primary` + `.is-loading` for approve                | `grep -c 'c-button c-button--primary' _components/ConsentCard.tsx` returns 2; `grep -c 'is-loading' _components/ConsentCard.tsx` returns 2 (approve + deny)                                | PASS   |
| O-7  | `[err]` glyph on all 4 invalidReason strings + em-dash softeners dropped | `grep -cE '\[err\] (Consent request missing\|Consent request expired\|Approval failed\|Invalid redirect_uri)' page.tsx` returns 4; `grep -cE '— start the OAuth\|— please retry\|— cannot return' page.tsx` returns 0 | PASS   |
| O-7b | Surface 4 non-error copy revisions                                       | "Access will be granted to" ×1, "Authorization expires in 24 hours." ×1, "Pick a workspace to continue." ×1, "workspace using" ×1 (em-dash dropped) — all on ConsentCard.tsx                | PASS   |
| O-8  | Phase 200/202 OAuth wiring preserved (page.tsx)                          | `/oauth/authorize/approve` ×1, `/api/tenant/switcher/list` ×1, `/api/auth/csrf` ×1, `code_challenge_method` ×4, `REQUIRED_PARAMS` ×3, `'use client'` ×1                                     | PASS   |
| X-1  | Sora/Space Grotesk eliminated (consent slice)                            | `grep -nE "(Sora\|Space Grotesk)" page.module.css` returns 0                                                                                                                                | PASS   |
| X-2  | Teal hex literals eliminated (consent slice)                             | `grep -niE "#0d9488\|#0f766e\|#14b8a6\|#e6fffb\|#0b877c" page.module.css` returns 0                                                                                                         | PASS   |
| X-3  | Gradients / box-shadow / transform-translate eliminated (consent slice)  | `grep -niE "linear-gradient\|radial-gradient\|box-shadow\|0 18px 45px\|transform:\s*translate" page.module.css` returns 0                                                                   | PASS   |
| X-4  | ≥6 named state stories (target 9)                                        | `grep -cE '^export const (Default\|MultiScope\|MultiTenant\|Loading\|Approving\|Declined\|InvalidExpired\|InvalidMissingFields\|InvalidRedirectUri):' _components/ConsentCard.stories.tsx` returns 9 | PASS   |

Banned lexicon scan: `grep -inE "(synergy|leverage|empower|unlock|transform|just )" "app/(markos)/oauth/consent/page.tsx" "app/(markos)/oauth/consent/_components/ConsentCard.tsx"` returns 0 hits — clean.

Token consumption (page.module.css): `var(--color-surface)` ×1, `var(--color-error)` ×1, `var(--space-*)` ×13, `var(--radius-lg)` ×2, `var(--color-on-surface)` ×2, `var(--color-on-surface-muted)` ×3, `var(--color-primary)` ×1, `var(--color-primary-text)` ×2, `var(--color-border)` ×4, `var(--color-surface-raised)` ×2, canonical `rgb(248 81 73 / 0.12)` 12% alpha-tint ×1.

## Verification Command Outputs

```text
$ grep -nE '#[0-9A-Fa-f]{3,8}' "app/(markos)/oauth/consent/page.module.css"
(empty — 0 hits)

$ grep -nE ':\s*[0-9]+px' "app/(markos)/oauth/consent/page.module.css"
42:  border: 1px solid var(--color-border);
58:  border: 1px solid var(--color-border);
99:  text-underline-offset: 2px;
104:  border-top: 1px solid var(--color-border);
130:  border-inline-start: 4px solid var(--color-error);

$ grep -cE '\[err\] (Consent request missing|Consent request expired|Approval failed|Invalid redirect_uri)' "app/(markos)/oauth/consent/page.tsx"
4

$ grep -cE '— start the OAuth|— please retry|— cannot return' "app/(markos)/oauth/consent/page.tsx"
0

$ grep -c "import ConsentCard from './_components/ConsentCard'" "app/(markos)/oauth/consent/page.tsx"
1

$ grep -c "'use client'" "app/(markos)/oauth/consent/_components/ConsentCard.tsx"
1

$ grep -c "c-chip-protocol" "app/(markos)/oauth/consent/_components/ConsentCard.tsx"
2

$ grep -c "c-button c-button--destructive" "app/(markos)/oauth/consent/_components/ConsentCard.tsx"
2

$ grep -c "c-code-inline" "app/(markos)/oauth/consent/_components/ConsentCard.tsx"
2

$ grep -c "c-button c-button--primary" "app/(markos)/oauth/consent/_components/ConsentCard.tsx"
2

$ grep -c "is-loading" "app/(markos)/oauth/consent/_components/ConsentCard.tsx"
2

$ node --test test/mcp/consent-ui-a11y.test.js
ℹ tests 14
ℹ pass 14
ℹ fail 0
(Note: original suite was 14 tests, not 15 as plan stated — 4 patched + 10 preserved verbatim.)

$ node --test test/auth/*.test.js
ℹ tests 61
ℹ pass 61
ℹ fail 0

$ node --test test/tenancy/invites.test.js
ℹ tests 10
ℹ pass 10
ℹ fail 0

$ node --test test/ui-a11y/213-1-chrome-a11y.test.js test/ui-a11y/213-1-light-and-forced-colors.test.js
ℹ tests 34
ℹ pass 34
ℹ fail 0

$ npm run build-storybook
... built in 27.16s; Preview built (36 s); Output: storybook-static/

$ grep -oE '"auth-consentcard--[a-z-]+"' storybook-static/index.json | sort -u
"auth-consentcard--approving"
"auth-consentcard--declined"
"auth-consentcard--default"
"auth-consentcard--invalid-expired"
"auth-consentcard--invalid-missing-fields"
"auth-consentcard--invalid-redirect-uri"
"auth-consentcard--loading"
"auth-consentcard--multi-scope"
"auth-consentcard--multi-tenant"
(9/9 stories registered)
```

## Files Modified

### `app/(markos)/oauth/consent/page.module.css` (253 → 151 LOC)

Eliminated every legacy literal per UI-SPEC reuse table:
- `.page` `padding: 48px 24px` → `padding-block: var(--space-xl); padding-inline: var(--space-md)` + `display: grid; place-items: center` (matches login/invite pattern)
- `.page` `font-family: 'Space Grotesk'`, `color: #0f172a`, `background: #f8fafc` → globals inheritance + `var(--color-surface)`
- `.contentCard` `background: #ffffff`, `border-radius: 28px`, `padding: 40px`, `box-shadow: 0 18px 45px rgba(...)`, `gap: 20px`, `max-width: 560px` → composed JSX-side via `.c-card c-card--feature` + local `width: min(var(--w-modal), 100%)` + `gap: var(--space-md)`
- `.heading` rule + `.heading:focus-visible` → DELETED entirely (plain `<h1 tabIndex={-1}>` in JSX; global `:focus-visible` covers)
- `.subheading` rule (×2 usages) → DELETED entirely; replaced with `.t-lead` utility composition JSX-side
- `.clientLogo` `width/height: 48px` → `var(--icon-xl)` 32px (proportional chip register); `border-radius: 12px` → `var(--radius-lg)` 8px; `background: #e6fffb` → `var(--color-surface-raised)`; `color: #0f766e` → `var(--color-primary-text)`; ADDS `1px solid var(--color-border)` hairline
- `.clientName` `font-size: 14px`, `color: #334155` → `var(--fs-body-md)` + `var(--color-on-surface)`
- `.redirectUri` rule → DELETED; composed `<code className="c-code-inline">` JSX-side
- `.scopeChip` rule → DELETED; composed `<span className="c-chip-protocol">` JSX-side (squared, auto `[/]` brackets via CSS pseudo-elements)
- `.fieldset` `border-radius: 14px` (off-grid) → `var(--radius-lg)` 8px; `padding: 16px 20px` (off-grid) → `var(--space-sm) var(--space-md)`
- `.fieldset legend` `font-size: 14px` → `var(--fs-label)` (label register matches `.c-field__label`); `padding: 0 6px` → `0 var(--space-xs)`
- `.radioRow input[type='radio']` `accent-color: #0d9488` → `var(--color-primary)`; `width/height: 20px` → `var(--icon-md)` 20px; `:focus-visible` rule DELETED (global covers)
- `.durationLine a` `color: #0d9488` → `var(--color-primary-text)`; ADDS `text-underline-offset: 2px` + `font-weight: var(--fw-semibold)` per DESIGN.md "Typography · Links"
- `.detailsBlock summary` `min-height: 44px`, `color: #334155` → token recipe (`var(--h-control-touch)` + `var(--color-on-surface)`)
- `.approveButton` rule + states → DELETED; composed `.c-button c-button--primary` + `.is-loading` JSX-side
- `.denyButton` rule + states → DELETED; composed `.c-button c-button--destructive` JSX-side (SEMANTIC UPGRADE per UI-SPEC O-4)
- `.helperText` rule → DELETED; composed `.c-field__help` JSX-side
- `.errorAlert` `background: #fef2f2`, `color: #991b1b`, `border-left: 4px solid #dc2626`, `border-radius: 12px`, `padding: 12px 16px` → `border-inline-start: 4px solid var(--color-error)` + canonical `rgb(248 81 73 / 0.12)` 12% alpha-tint + `var(--space-sm) var(--space-md)` padding (Plan 03 + Phase 213.1 canonical recipe)
- `@media (max-width: 640px)` → `@media (max-width: 639.98px)` (DESIGN.md `--bp-sm` semantic boundary); body preserves `flex-direction: column-reverse` + adds `width: 100%` button collapse
- `@media (prefers-reduced-motion: reduce)` `.approveButton { transition: none !important }` → DELETED entirely (global `app/globals.css` covers)

### `app/(markos)/oauth/consent/page.tsx` (265 → 152 LOC)

- `'use client'` directive PRESERVED on line 1 per CONTEXT D-05
- Imports `ConsentCard` from `./_components/ConsentCard` and renders `<ConsentCard {...11 props} />` inside the `<main className={styles.page}>` route-level wrapper
- All inline JSX surface markup REMOVED (lines 153–245 of old file: `<section>` through `</section>`) — entire visual surface lives in `ConsentCard.tsx`
- 4× `setInvalidReason()` revised per UI-SPEC O-7:
  - `'This consent request is missing required fields. Start...'` → `'[err] Consent request missing required fields. Start...'`
  - `'Consent request expired — start the OAuth flow...'` → `'[err] Consent request expired. Start the OAuth flow...'` (em-dash → period)
  - `'Approval failed — please retry or start a new consent flow.'` → `'[err] Approval failed. Retry or start a new consent flow.'` (em-dash → period; "please" stripped)
  - `'Invalid redirect_uri — cannot return to the MCP client.'` → `'[err] Invalid redirect_uri. Cannot return to the MCP client.'` (em-dash → period)
- `humanizeScope` MOVED to ConsentCard.tsx (presentation concern; mapping verbatim per CONTEXT D-19)
- `deriveClientName` PRESERVED in page.tsx (consumed inside `useEffect` data-fetch flow)
- PRESERVED VERBATIM: `REQUIRED_PARAMS` array, `useEffect` URL parsing + `code_challenge_method !== 'S256'` validation, `fetch('/api/tenant/switcher/list')` + flat-map + status='purged' filter, `fetch('/api/auth/csrf')`, `useMemo` for `scopeList`, `onApprove()` POST `/oauth/authorize/approve` with 9-field body shape (state/target_tenant_id/csrf_token/client_id/redirect_uri/resource/scope/code_challenge/code_challenge_method), `onDeny()` URL with `error=access_denied`, all 7 `useState` types + initial values
- `loading={!params && !invalidReason}` prop derivation passed to ConsentCard (replaces `if (!params) return <Loading>` early return — moved to ConsentCard's render branching)

## Files Created

### `app/(markos)/oauth/consent/_components/ConsentCard.tsx` (NEW, 173 LOC)

Presentational client subcomponent extracted per RESEARCH.md R-4 (6+ useState wrapper-vs-presentational separation for Storybook authorability). Composes 7 DESIGN.md primitives + carries 11-prop interface (`clientName`, `redirectUri`, `scopeList`, `tenants`, `selectedTenantId`, `submitState`, `invalidReason`, `loading`, `onApprove`, `onDeny`, `onTenantSelect`).

Key composition:
- `<section className={\`c-card c-card--feature ${styles.contentCard}\`} aria-labelledby="consent-heading">` (Plan 01 LoginCard pattern)
- `<code className="c-code-inline">{redirectUri}</code>` (UI-SPEC O-5)
- `<span className="c-chip-protocol">{humanizeScope(s)}</span>` for each scope (UI-SPEC O-3 — squared signature chips, NOT pill)
- `<button className={\`c-button c-button--destructive${isDenying ? ' is-loading' : ''}\`} aria-busy={isDenying ? 'true' : 'false'}>` (UI-SPEC O-4 + Plan 03 ARIA-spec compliance)
- `<button className={\`c-button c-button--primary${isApproving ? ' is-loading' : ''}\`} aria-busy={isApproving ? 'true' : 'false'}>` (UI-SPEC O-6)
- `<p className="t-lead">` for "{clientName} is requesting access to:" + single-tenant subheading
- `<p className="c-field__help">Pick a workspace to continue.</p>` conditional helper

Surface 4 copy revisions applied:
- "You will grant access to: ..." → "Access will be granted to <strong>...</strong>." (passive system-state)
- "This authorization expires in 24 hours and can be revoked at any time from..." → "Authorization expires in 24 hours. Revoke any time from..." (terminal period; passive)
- "Pick a workspace to continue" → "Pick a workspace to continue." (terminal period)
- "workspace — using only the scopes you approve here" → "workspace using only the scopes you approve here" (em-dash dropped)

A11y markers preserved verbatim from Phase 202 contract:
- `aria-labelledby="consent-heading"` on `<section>`
- `id="consent-heading"` + `tabIndex={-1}` on `<h1>` (programmatic screen-reader focus)
- `id="client-row-desc"` + `aria-describedby="client-row-desc"` on subheading
- `<fieldset>` + `<legend>` for tenant picker
- `aria-describedby={helperVisible ? 'approve-helper' : undefined}` on approve CTA + `id="approve-helper"` on helper
- `role="alert"` on errorAlert branch
- `<code>` for redirect URI, `<details>` + `<summary>` for "What is MCP?"

Lint-clean control flow: `let approveLabel = 'Approve access'; if (isApproving) approveLabel = 'Approving…';` (Plan 03 hoist precedent).

### `app/(markos)/oauth/consent/_components/ConsentCard.stories.tsx` (NEW, 94 LOC)

Storybook 8 CSF3 with 9 named state exports under `Auth/ConsentCard`:

| Story                      | Args                                                                                  | Purpose                                            |
| -------------------------- | ------------------------------------------------------------------------------------- | -------------------------------------------------- |
| `Default`                  | single tenant, single scope `['read']`, idle                                          | Baseline / Chromatic anchor                        |
| `MultiScope`               | single tenant, 4 scopes `['read', 'write', 'plan', 'audit']`                         | Verifies scope-chip group + <5% mint signal budget |
| `MultiTenant`              | 2 tenants, `selectedTenantId: ''`, scopes `['read', 'write']`                         | Fieldset radio picker + approve-disabled + helper  |
| `Loading`                  | `loading: true`                                                                       | Initial fetch state                                |
| `Approving`                | `submitState: 'approving'`                                                            | Primary CTA `.is-loading`                          |
| `Declined`                 | `submitState: 'denying'`                                                              | Destructive CTA `.is-loading`                      |
| `InvalidExpired`           | `invalidReason: '[err] Consent request expired...'`                                   | Server-side challenge-method validation failure   |
| `InvalidMissingFields`     | `invalidReason: '[err] Consent request missing required fields...'`                   | Required URL param missing                         |
| `InvalidRedirectUri`       | `invalidReason: '[err] Invalid redirect_uri. Cannot return to the MCP client.'`       | Deny-path URL construction failure                 |

Component is presentational (props-driven) — args directly control state, no decorator setup needed (RESEARCH.md R-4 mitigation pays off; no useState-forcing required). Does NOT set `parameters.theme: 'legacy'` — renders against DESIGN.md tokens via Phase 213.1-03 `preview.tsx` cascade.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] `aria-busy={isApproving || undefined}` -> `aria-busy={isApproving ? 'true' : 'false'}` migration**
- **Found during:** Task 2 (ConsentCard.tsx authoring)
- **Issue:** Plan specified `aria-busy={isApproving || undefined}` — but Plan 03 SUMMARY decision documented this exact pattern as a WAI-ARIA spec compliance bug (`aria-busy` expects literal 'true'/'false' string token, not boolean expression that React would coerce to 'true'/false-as-attribute-omission).
- **Fix:** Applied Plan 03 precedent verbatim to BOTH approve and deny buttons.
- **Files modified:** `app/(markos)/oauth/consent/_components/ConsentCard.tsx`
- **Commit:** `eae45cf`

**2. [Rule 1 - Lint] Hoisted `approveLabel` / `denyLabel` out of inline ternary**
- **Found during:** Task 2 (ConsentCard.tsx authoring)
- **Issue:** Plan specified `{isApproving ? 'Approving…' : 'Approve access'}` inline — but Plan 03 SUMMARY decision documented this as a lint trigger (typescript:S3358).
- **Fix:** Applied Plan 03 precedent: `let approveLabel = 'Approve access'; if (isApproving) approveLabel = 'Approving…';` (and analogous for denyLabel).
- **Files modified:** `app/(markos)/oauth/consent/_components/ConsentCard.tsx`
- **Commit:** `eae45cf`

**3. [Rule 1 - Test] Removed `@media (prefers-reduced-motion)` literal text from page.module.css comment**
- **Found during:** Task 1 (post-write verification)
- **Issue:** Initial comment line read `* @media (prefers-reduced-motion) is intentionally NOT redeclared`. The Task-3 patched test asserts `assert.doesNotMatch(css, /@media\s*\(prefers-reduced-motion/)` — this regex matches the literal text in the comment too, causing the test to fail.
- **Fix:** Rewrote comment as `Reduced-motion query is intentionally NOT redeclared here — global app/globals.css covers it.` (semantic content preserved; `@media (prefers-reduced-motion)` literal stripped).
- **Files modified:** `app/(markos)/oauth/consent/page.module.css`
- **Commit:** `af3dfd3` (final form)

**4. [Rule 1 - Build script substitution] `npm run build-storybook` substituted for `npm run build`**
- **Found during:** Task 2 verification
- **Issue:** Plan acceptance criterion specifies `npm run build` exits 0, but `package.json` has no `build` script (Plans 213.2-01/02/03 documented same).
- **Fix:** Substituted `npm run build-storybook` (performs full Vite + tsc compile of TSX surface). Built in 27.16s; ConsentCard.stories chunk 8.30 kB present in `storybook-static/`.
- **Files modified:** None (test-substitution only)

### Pre-Existing Failures (Out-of-Scope)

**1. test/tenant-auth/ui-authorization-negative-path.test.js** — PRE-EXISTING failure asserting on `app/(markos)/layout.tsx` (NOT consent). Already documented in Plan 03 deferred-items.md. Out of scope per executor Rule 5. Not re-logged here.

## Hand-off to Plan 213.2-05

Plan 213.2-05 (final wave-2 plan) is the cross-cutting closer:
- Add `(pointer: coarse)` extension to `.c-button` in `styles/components.css` (DESIGN.md "Buttons · Touch targets" — Phase 213.1 surfaced this as in-flight)
- Wave 2 a11y test (regression-anchor for ALL 4 surfaces: login + signup + invite + oauth/consent)
- Final UI-SPEC closure (X-1, X-2, X-3 cross-cutting verification across the wave)

ConsentCard.tsx subcomponent extraction is now the canonical R-4-mitigation reference for any future surface with 6+ useState. LoginCard.tsx (Plan 01) is the precedent for simple 3-prop presentational extraction; ConsentCard.tsx (Plan 04) is the precedent for 11-prop extraction with multi-branch render (invalidReason / loading / idle).

## Manual Visual Review Reminder

Chromatic baseline for `Auth/ConsentCard` (9 stories) requires sanity-check on:
- Squared `.c-chip-protocol` rendering with `[Read CRM]` `[Draft marketing copy]` etc. brackets (CSS pseudo-elements `::before`/`::after`)
- `.c-button--destructive` deny CTA — error 12% alpha-tint background + Crimson `#F85149` border (DESIGN.md token-cited)
- `.c-code-inline` redirect URI render (mono font + raised bg + small radius)
- `.c-card--feature` panel — Border Mist `#2D3441` 1px hairline, no soft shadow on dark
- `.is-loading` spinner overlay on approve button (Approving story) and deny button (Declined story)
- Sub-page layout at `--bp-sm` (`max-width: 639.98px`) — actionRow flips to `column-reverse` with full-width buttons

## Self-Check: PASSED

Files created:
- FOUND: app/(markos)/oauth/consent/_components/ConsentCard.tsx
- FOUND: app/(markos)/oauth/consent/_components/ConsentCard.stories.tsx

Files modified:
- FOUND: app/(markos)/oauth/consent/page.module.css (151 LOC token-only)
- FOUND: app/(markos)/oauth/consent/page.tsx (152 LOC, [err] glyphs + ConsentCard delegate)
- FOUND: test/mcp/consent-ui-a11y.test.js (138 LOC, 14 tests pass)

Commits:
- FOUND: af3dfd3 — feat(213.2-04): rewrite oauth/consent page.module.css to token-only + page.tsx [err] glyphs + delegate UI to ConsentCard subcomponent
- FOUND: eae45cf — feat(213.2-04): add ConsentCard.tsx presentational subcomponent + 9-state Storybook stories
- FOUND: b9e281a — test(213.2-04): patch consent-ui-a11y.test.js — token-citation + primitive-composition + global-reduced-motion + revised-copy assertions

Test runs:
- node --test test/mcp/consent-ui-a11y.test.js: 14/14 pass
- node --test test/auth/*.test.js: 61/61 pass
- node --test test/tenancy/invites.test.js: 10/10 pass
- node --test test/ui-a11y/213-1-chrome-a11y.test.js test/ui-a11y/213-1-light-and-forced-colors.test.js: 34/34 pass
- npm run build-storybook: built in 27.16s; 9 ConsentCard stories registered in storybook-static/index.json
