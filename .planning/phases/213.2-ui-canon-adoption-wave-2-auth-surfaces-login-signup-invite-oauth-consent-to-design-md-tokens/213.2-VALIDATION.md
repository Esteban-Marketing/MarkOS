---
phase: 213.2
slug: ui-canon-adoption-wave-2-auth-surfaces-login-signup-invite-oauth-consent-to-design-md-tokens
status: planned
nyquist_compliant: true
wave_0_complete: false
created: 2026-04-28
updated: 2026-04-28
---

# Phase 213.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Bootstrapped from `213.2-RESEARCH.md` `## Validation Architecture` section.
> Per-task verification map populated by planner after PLAN.md files written.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `node --test` + Storybook 8 + Chromatic + axe-core |
| **Config file** | None for `node --test`; `.storybook/main.ts`, `.storybook/preview.tsx` for Storybook (Phase 213.1 viteFinal automatic JSX runtime applied + globals.css cascade + ThemeProvider opt-in gate) |
| **Quick run command** | `node --test test/ui-a11y/213-1-chrome-a11y.test.js test/ui-a11y/213-1-light-and-forced-colors.test.js test/ui-a11y/213-2-auth-a11y.test.js test/auth/signup.test.js test/mcp/consent-ui-a11y.test.js` |
| **Full suite command** | `node --test test/auth/ test/tenant-auth/ test/tenancy/invites.test.js test/onboarding-server.test.js test/mcp/ test/ui-a11y/` |
| **Estimated runtime** | ~45s quick · ~5–8min full |

---

## Sampling Rate

- **After every task commit:** Run quick command (target a11y + auth wiring + consent contract).
- **After every plan wave:** Run full suite + `npm run build-storybook` + `grep -E "#[0-9a-fA-F]{3,8}" {scoped module.css}` returns 0.
- **Before `/gsd-verify-work`:** Full suite green; banned-lexicon scan = 0 in modified `.tsx`; Phase 213.1 a11y suite (29 tests) still passing.
- **Max feedback latency:** 45 seconds for quick command.

---

## Per-Task Verification Map

> Populated post-planning per CONTEXT D-21 + RESEARCH Validation Architecture > Per-task automated verify gates.

| Task ID | Plan | Wave | Requirement (UI-SPEC AC#) | Test Type | Automated Command | File Exists | Status |
|---------|------|------|---------------------------|-----------|-------------------|-------------|--------|
| 213.2-01-T1 | 01 | 1 | L-1, L-2, L-4, L-5, X-2, X-3 | grep + build | `! grep -nE '#[0-9A-Fa-f]{3,8}' "app/(markos)/login/page.module.css"`; `! grep -qE "'--accent'\|primary_color" "app/(markos)/login/page.tsx"`; `npm run build` | mixed (existing module.css + tsx; rewrite in place) | ⬜ |
| 213.2-01-T2 | 01 | 1 | L-3, X-4 (login slice) | grep + build + storybook + auth wiring | `grep -cE 'c-(card--feature\|input\|field\|button--primary)' "app/(markos)/login/_components/LoginCard.tsx"` ≥5; `grep -cE "^export const (Default\|Filled\|Branded\|ErrorState):" "app/(markos)/login/_components/LoginCard.stories.tsx"` returns 4; `npm run build && npm run build-storybook && node --test test/auth/ test/ui-a11y/213-1-chrome-a11y.test.js` | ❌ W0 (LoginCard.tsx + LoginCard.stories.tsx NEW) | ⬜ |
| 213.2-02-T1 | 02 | 1 | S-1, S-2, S-3, S-4, S-5, S-6, X-2, X-3 | grep + build | `! grep -nE '#[0-9A-Fa-f]{3,8}' "app/(marketing)/signup/page.module.css"`; `grep -cE 'c-(card--feature\|input\|field\|button--primary\|button--tertiary)' "app/(marketing)/signup/page.tsx"` ≥6; `grep -c '\[ok\] Check your inbox\.' "app/(marketing)/signup/page.tsx"` returns 1; `grep -c '\[warn\]' "app/(marketing)/signup/page.tsx"` ≥2; `npm run build` | mixed (existing module.css + tsx + layout.tsx; rewrite in place) | ⬜ |
| 213.2-02-T2 | 02 | 1 | X-4 (signup slice); test patch (CONTEXT D-19) | grep + node:test + storybook | `! grep -q "min-height: 44px" "test/auth/signup.test.js"`; `grep -q '213.2 redesigned' "test/auth/signup.test.js"`; `grep -cE "^export const (Default\|Filled\|Loading\|Sent\|BotBlocked\|RateLimited\|Error):" "app/(marketing)/signup/page.stories.tsx"` returns 7; `node --test test/auth/signup.test.js test/auth/ test/onboarding-server.test.js && npm run build-storybook` | ❌ W0 (page.stories.tsx NEW; signup.test.js patched) | ⬜ |
| 213.2-03-T1 | 03 | 1 | I-1, I-2, I-3, I-4, I-5, X-2, X-3 | grep + build + tenancy wiring | `! grep -nE '#[0-9A-Fa-f]{3,8}' "app/(markos)/invite/[token]/page.module.css"`; `grep -cE '\[err\] (Invite expired\|Email mismatch\|Invite withdrawn\|Invite already accepted\|Invite not found\|Seat limit reached\|Accept failed)' "app/(markos)/invite/[token]/page.tsx"` returns 7; `grep -c '\[ok\] Accepted' "app/(markos)/invite/[token]/page.tsx"` returns 1; `! grep -q "You'll sign in" "app/(markos)/invite/[token]/page.tsx"`; `npm run build && node --test test/tenancy/invites.test.js test/tenant-auth/` | mixed (existing module.css + tsx; rewrite in place) | ⬜ |
| 213.2-03-T2 | 03 | 1 | X-4 (invite slice) | grep + storybook | `grep -cE '^export const (Default\|Accepting\|Success\|Error\|ErrorEmailMismatch\|ErrorWithdrawn\|ErrorAlreadyAccepted\|ErrorNotFound\|ErrorSeatQuota\|ErrorAcceptFailed):' "app/(markos)/invite/[token]/page.stories.tsx"` ≥4 (target 10); `npm run build-storybook` | ❌ W0 (page.stories.tsx NEW) | ⬜ |
| 213.2-04-T1 | 04 | 1 | O-1, O-2, O-3, O-7, O-8, X-2, X-3, prefers-reduced-motion local override deletion | grep + build | `! grep -nE '#[0-9A-Fa-f]{3,8}' "app/(markos)/oauth/consent/page.module.css"`; `! grep -qE 'border-radius:\s*999px\|border-radius:\s*28px' "app/(markos)/oauth/consent/page.module.css"`; `! grep -q '@media\s*(prefers-reduced-motion' "app/(markos)/oauth/consent/page.module.css"`; `grep -cE '\[err\] (Consent request missing\|Consent request expired\|Approval failed\|Invalid redirect_uri)' "app/(markos)/oauth/consent/page.tsx"` returns 4; `grep -q "import ConsentCard from './_components/ConsentCard'" "app/(markos)/oauth/consent/page.tsx"`; `npm run build` (sequenced post-T2) | mixed (existing module.css + tsx; rewrite in place) | ⬜ |
| 213.2-04-T2 | 04 | 1 | O-3, O-4, O-5, O-6, O-7 (Surface 4 non-error copy), X-4 (consent slice) | grep + build + storybook | `grep -c "c-chip-protocol" "app/(markos)/oauth/consent/_components/ConsentCard.tsx"` ≥1; `grep -c "c-button c-button--destructive" "app/(markos)/oauth/consent/_components/ConsentCard.tsx"` ≥1; `grep -c "c-code-inline" "app/(markos)/oauth/consent/_components/ConsentCard.tsx"` ≥1; `grep -c "Access will be granted to" "app/(markos)/oauth/consent/_components/ConsentCard.tsx"` returns 1; `grep -c "Authorization expires in 24 hours\." "app/(markos)/oauth/consent/_components/ConsentCard.tsx"` returns 1; `grep -cE '^export const (Default\|MultiScope\|MultiTenant\|Loading\|Approving\|Declined\|InvalidExpired\|InvalidMissingFields\|InvalidRedirectUri):' "app/(markos)/oauth/consent/_components/ConsentCard.stories.tsx"` returns 9; `npm run build && npm run build-storybook` | ❌ W0 (ConsentCard.tsx + ConsentCard.stories.tsx NEW) | ⬜ |
| 213.2-04-T3 | 04 | 1 | test patch (CONTEXT D-19); O-3, O-4, O-5, O-6, O-7 (test contract); O-8 (wiring assertions preserved) | node:test + grep | `! grep -nE '#0d9488\|#0f766e\|border-radius:\s*28px\|outline:\s*2px solid #0d9488\|min-height:\s*44px\|#fef2f2\|#e6fffb\|0 18px 45px' "test/mcp/consent-ui-a11y.test.js"`; `grep -q "Authorization expires in 24 hours" "test/mcp/consent-ui-a11y.test.js"`; `grep -c "/oauth/authorize/approve" "test/mcp/consent-ui-a11y.test.js"` ≥1; `grep -c "F-89" "test/mcp/consent-ui-a11y.test.js"` ≥3; `node --test test/mcp/consent-ui-a11y.test.js test/auth/ test/ui-a11y/213-1-chrome-a11y.test.js` | mixed (existing test/mcp/consent-ui-a11y.test.js patched in place) | ⬜ |
| 213.2-05-T1 | 05 | 1 | AC#15 ((pointer: coarse) extension to .c-button); UI-SPEC Rule 4 + RESEARCH R-3 | grep + build + storybook + 213.1 a11y regression | `grep -c "@media (pointer: coarse)" "styles/components.css"` returns 1; `grep -A4 "@media (pointer: coarse)" "styles/components.css" \| grep -qE '\.c-button\s*\{\s*min-height:\s*var\(--h-control-touch\)'`; `grep -A4 "@media (pointer: coarse)" "styles/components.css" \| grep -qE '\.c-nav-link\s*\{\s*padding-block:\s*11px'`; auth modules return 0 for `@media (pointer: coarse)`; `npm run build && npm run build-storybook && node --test test/ui-a11y/213-1-chrome-a11y.test.js` | mixed (styles/components.css edit in place) | ⬜ |
| 213.2-05-T2 | 05 | 1 | X-5 (213-2-auth-a11y.test.js exists, ≥7 AC# mentions, ≥18 tests, exits 0); X-1, X-2, X-3, X-4 (cross-cutting closure) | node:test (NEW suite) + 213.1 a11y regression + full wiring | `test -f "test/ui-a11y/213-2-auth-a11y.test.js"`; `grep -cE "^test\(" "test/ui-a11y/213-2-auth-a11y.test.js"` ≥18; `grep -cE "AC#(3\|9\|11\|12\|13\|15)" "test/ui-a11y/213-2-auth-a11y.test.js"` ≥7; `node --test test/ui-a11y/213-2-auth-a11y.test.js test/ui-a11y/213-1-chrome-a11y.test.js test/ui-a11y/213-1-light-and-forced-colors.test.js test/auth/ test/tenancy/invites.test.js test/tenant-auth/ test/onboarding-server.test.js test/mcp/consent-ui-a11y.test.js` | ❌ W0 (213-2-auth-a11y.test.js NEW) | ⬜ |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `app/(markos)/login/_components/LoginCard.tsx` (NEW client subcomponent for Storybook authorability — RESEARCH.md R-2) — Plan 213.2-01 Task 2
- [ ] `app/(markos)/login/_components/LoginCard.stories.tsx` (NEW) — Plan 213.2-01 Task 2
- [ ] `app/(marketing)/signup/page.stories.tsx` (NEW) — Plan 213.2-02 Task 2
- [ ] `app/(markos)/invite/[token]/page.stories.tsx` (NEW) — Plan 213.2-03 Task 2
- [ ] `app/(markos)/oauth/consent/_components/ConsentCard.tsx` (NEW client subcomponent for Storybook authorability — RESEARCH.md R-4) — Plan 213.2-04 Task 2
- [ ] `app/(markos)/oauth/consent/_components/ConsentCard.stories.tsx` (NEW) — Plan 213.2-04 Task 2
- [ ] `test/ui-a11y/213-2-auth-a11y.test.js` (NEW grep-shape suite, ≥18 tests, ≥7 AC# mentions covering AC#3, 9, 11, 12, 13, 15) — Plan 213.2-05 Task 2
- [ ] `styles/components.css` `@media (pointer: coarse)` block extended with `.c-button { min-height: var(--h-control-touch); }` rule (mirrors `.c-nav-link` precedent from Phase 213.1) — Plan 213.2-05 Task 1

*All other infrastructure (`node --test`, Storybook 8 + automatic JSX runtime fix, Chromatic, axe-core, axe-playwright, addon-essentials ^8.6.18, ThemeProvider opt-in gate, globals.css cascade) already installed/wired per `package.json` and Phase 213.1 — no install steps required.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Chromatic visual baseline review | DESIGN.md visual contract per surface | Subjective brand-feel cannot be automated | Run `npm run chromatic`, approve all auth-surface stories (LoginCard 4 stories + signup 7 stories + invite 4–10 stories + ConsentCard 9 stories = 24+ named state stories total) against DESIGN.md visual canon |
| Windows High Contrast / `forced-colors` mode | DESIGN.md "Color blindness" + `app/tokens.css` `@media (forced-colors: active)` block | Browser does not expose forced-colors mode through axe-core | Enable Windows High Contrast, load each auth surface, verify focus rings remain visible, borders use `CanvasText`, bracketed-glyph state prefixes (`[err]`/`[warn]`/`[ok]`) survive |
| `[data-theme="light"]` light-mode flip | `app/tokens.css` `[data-theme="light"]` block + `--color-primary-text: #00805F` (Phase 213.1 commit `fce709d`) | Storybook theme-toolbar wiring deferred from Phase 213.1 | Set `<html data-theme="light">` via DevTools, verify auth surfaces flip to Lino Light + mint accent unchanged + no contrast collapse + no mint-on-light text |
| WebAuthn / passkey flow (login) | Phase 200 OAuth device flow + `@simplewebauthn/browser` deps preserved (CONTEXT D-20) | Browser-required hardware/credential flow | Manually exercise login passkey path; verify wiring untouched and visual primitives apply correctly |
| OAuth consent grant + deny end-to-end | Phase 200 OAuth integration + Phase 202 MCP server consent | Real OAuth client + multi-scope flow | Manually exercise approve + deny paths via test OAuth client; verify `.c-button--destructive` Deny semantics + scope `.c-chip-protocol` chips render correctly |
| BYOD `displayName` + `logo` end-to-end (login) | Phase 200/204 BYOD branding contract preserved (UI-SPEC L-5) | Real BYOD tenant header injection (`x-markos-byod` + `x-markos-tenant-id`) | Manually verify branded login surface renders heading "Sign in to {displayName}", logo image, "Powered by MarkOS" footer link with `vanity_login_enabled = true` |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (planner verified post PLAN.md creation — every task in 213.2-01 through 213.2-05 has an `<automated>` block in its `<verify>` section)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (every task has automated; Wave 0 tasks chain to subsequent task gates via `npm run build` + `node --test test/auth/`)
- [x] Wave 0 covers all MISSING references (LoginCard.tsx, LoginCard.stories.tsx, signup/page.stories.tsx, invite/[token]/page.stories.tsx, ConsentCard.tsx, ConsentCard.stories.tsx, 213-2-auth-a11y.test.js, .c-button extension to (pointer: coarse) rule)
- [x] No watch-mode flags (all `node --test` invocations are one-shot; Storybook builds via `npm run build-storybook`, not `npm run storybook`)
- [x] Feedback latency < 45s for quick command (`node --test test/ui-a11y/213-2-auth-a11y.test.js test/auth/signup.test.js test/mcp/consent-ui-a11y.test.js`)
- [x] `nyquist_compliant: true` set in frontmatter (every PLAN.md task has automated `<verify>` covering its own AC closures + Phase 213.1 a11y regression checks)

**Approval:** planner-finalized 2026-04-28; pending checker confirmation in `/gsd-plan-checker` Dimension 8.
