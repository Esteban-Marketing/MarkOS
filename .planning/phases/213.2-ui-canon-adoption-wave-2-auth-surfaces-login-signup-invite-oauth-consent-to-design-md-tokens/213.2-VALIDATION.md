---
phase: 213.2
slug: ui-canon-adoption-wave-2-auth-surfaces-login-signup-invite-oauth-consent-to-design-md-tokens
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-28
---

# Phase 213.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Bootstrapped from `213.2-RESEARCH.md` `## Validation Architecture` section. Planner finalizes the per-task verification map after PLAN.md files are created.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `node --test` + Storybook 8 + Chromatic + axe-core |
| **Config file** | None for `node --test`; `.storybook/main.ts`, `.storybook/preview.tsx` for Storybook (Phase 213.1 viteFinal automatic JSX runtime applied) |
| **Quick run command** | `node --test test/ui-a11y/213-1-chrome-a11y.test.js test/ui-a11y/213-1-light-and-forced-colors.test.js test/ui-a11y/213-2-auth-a11y.test.js test/auth/signup.test.js test/mcp/consent-ui-a11y.test.js` |
| **Full suite command** | `npm run test:ui-all && node --test test/auth/ test/tenant-auth/ test/tenancy/invites.test.js test/onboarding-server.test.js test/mcp/` |
| **Estimated runtime** | ~45s quick · ~5–8min full |

---

## Sampling Rate

- **After every task commit:** Run quick command (target a11y + auth wiring + consent contract).
- **After every plan wave:** Run full suite + `npm run build-storybook` + `grep -E "#[0-9a-fA-F]{3,8}" {scoped module.css}` returns 0.
- **Before `/gsd-verify-work`:** Full suite green; banned-lexicon scan = 0 in modified `.tsx`; Phase 213.1 a11y suite (29 tests) still passing.
- **Max feedback latency:** 45 seconds for quick command.

---

## Per-Task Verification Map

> Planner: populate this table after PLAN.md files written. Each task in each plan gets a row.
> Initial scaffold below — adjust task IDs to match planner's output.

| Task ID | Plan | Wave | Requirement (UI-SPEC AC#) | Test Type | Automated Command | File Exists | Status |
|---------|------|------|---------------------------|-----------|-------------------|-------------|--------|
| 213.2-01-T* | 01 | 1 | login AC#1..5 | grep + build + storybook | TBD by planner per task | mixed (existing + W0 LoginCard) | ⬜ |
| 213.2-02-T* | 02 | 1 | signup AC#1..6 | grep + build + storybook + unit (test/auth/signup.test.js) | TBD by planner | mixed | ⬜ |
| 213.2-03-T* | 03 | 1 | invite AC#1..5 | grep + build + storybook | TBD by planner | mixed | ⬜ |
| 213.2-04-T* | 04 | 1 | oauth-consent AC#1..8 | grep + build + storybook + unit (test/mcp/consent-ui-a11y.test.js) | TBD by planner | mixed (existing + W0 ConsentCard) | ⬜ |
| 213.2-05-T* | 05 | 1 | cross-cutting AC#1..5 (213-2-auth-a11y.test.js + .c-button (pointer: coarse) extension) | grep + a11y test | TBD by planner | ❌ W0 (created in plan) | ⬜ |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `app/(markos)/login/_components/LoginCard.tsx` (NEW client subcomponent for Storybook authorability) — Plan 213.2-01
- [ ] `app/(markos)/oauth/consent/_components/ConsentCard.tsx` (NEW client subcomponent for Storybook authorability) — Plan 213.2-04
- [ ] `app/(markos)/login/_components/LoginCard.stories.tsx` (NEW) — Plan 213.2-01
- [ ] `app/(marketing)/signup/page.stories.tsx` (NEW) — Plan 213.2-02
- [ ] `app/(markos)/invite/[token]/page.stories.tsx` (NEW) — Plan 213.2-03
- [ ] `app/(markos)/oauth/consent/_components/ConsentCard.stories.tsx` (NEW) — Plan 213.2-04
- [ ] `test/ui-a11y/213-2-auth-a11y.test.js` (NEW grep-shape suite, ≥18 tests, ≥7 AC# mentions covering AC#3, 9, 11, 12, 13, 15) — Plan 213.2-05
- [ ] `styles/components.css` `@media (pointer: coarse) { .c-button { min-height: var(--h-control-touch); } }` rule (extension; mirrors `.c-nav-link` precedent from Phase 213.1) — Plan 213.2-05

*All other infrastructure (`node --test`, Storybook 8 + automatic JSX runtime fix, Chromatic, axe-core, axe-playwright, addon-essentials ^8.6.18) already installed per `package.json` — no install steps required.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Chromatic visual baseline review | DESIGN.md visual contract per surface | Subjective brand-feel cannot be automated | Run `npm run chromatic`, approve all auth-surface stories (login, signup, invite, oauth-consent variants) against DESIGN.md visual canon |
| Windows High Contrast / `forced-colors` mode | DESIGN.md "Color blindness" + `app/tokens.css` `@media (forced-colors: active)` block | Browser does not expose forced-colors mode through axe-core | Enable Windows High Contrast, load each auth surface, verify focus rings remain visible, borders use `CanvasText`, bracketed-glyph state prefixes (`[err]`/`[warn]`/`[ok]`) survive |
| `[data-theme="light"]` light-mode flip | `app/tokens.css` `[data-theme="light"]` block + `--color-primary-text: #00805F` (Phase 213.1 commit `fce709d`) | Storybook theme-toolbar wiring deferred from Phase 213.1 | Set `<html data-theme="light">` via DevTools, verify auth surfaces flip to Lino Light + mint accent unchanged + no contrast collapse + no mint-on-light text |
| WebAuthn / passkey flow (login) | Phase 200 OAuth device flow + `@simplewebauthn/browser` deps preserved | Browser-required hardware/credential flow | Manually exercise login passkey path; verify wiring untouched and visual primitives apply correctly |
| OAuth consent grant + deny end-to-end | Phase 200 OAuth integration + Phase 202 MCP server consent | Real OAuth client + multi-scope flow | Manually exercise approve + deny paths via test OAuth client; verify `.c-button--destructive` Deny semantics + scope `.c-chip-protocol` chips render correctly |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies (planner populates after PLAN.md written)
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (LoginCard.tsx, ConsentCard.tsx, 4 stories, a11y suite, .c-button extension)
- [ ] No watch-mode flags
- [ ] Feedback latency < 45s for quick command
- [ ] `nyquist_compliant: true` set in frontmatter (after planner verifies coverage)

**Approval:** pending (planner verifies after PLAN.md creation; checker confirms Dimension 8 in /gsd-plan-checker)
