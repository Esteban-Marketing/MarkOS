---
status: partial
phase: 213.2-ui-canon-adoption-wave-2-auth-surfaces-login-signup-invite-oauth-consent-to-design-md-tokens
source: ["213.2-VERIFICATION.md"]
started: 2026-04-28
updated: 2026-04-28
---

## Current Test

[awaiting human testing — 5 manual gates flagged by gsd-verifier (status: human_needed); 23/23 automated must-haves PASS, 168/168 regression tests PASS, 29/29 UI-SPEC ACs satisfied]

## Tests

### 1. Chromatic visual baseline review (30 new stories)

expected: Run `npm run chromatic`, approve the 30 new auth-surface stories rendered against DESIGN.md tokens:
- `Auth/LoginCard` (4): Default, Filled, Branded, ErrorState
- `Auth/SignupPage` (7): Default, Filled, Loading, Sent, BotBlocked, RateLimited, Error
- `Auth/InvitePage` (10): Default, Accepting, Success + Error*7 (one per reason code)
- `Auth/ConsentCard` (9): Default, MultiScope, MultiTenant, Loading, Approving, Declined, InvalidExpired, InvalidMissingFields, InvalidRedirectUri

Verify each surface matches DESIGN.md visual contract (Kernel Black surface, Protocol Mint signal <5%, JetBrains Mono headlines, sharp 6/8/12px radii, 1px Border Mist hairlines, flat surfaces, no gradients/shadows/Sora-Space-Grotesk).

result: [pending]

### 2. Windows High Contrast / `forced-colors` mode verification

expected: On a Windows machine with High Contrast enabled (Settings → Accessibility → Contrast themes):
- Load each of the 4 auth surfaces (`/login`, `/signup`, `/invite/[token]`, `/oauth/consent`)
- Verify focus rings remain visible using `Highlight` system color (inherits from `app/tokens.css` `@media (forced-colors: active)`)
- Verify borders use `CanvasText`
- Verify bracketed-glyph state prefixes (`[err]`/`[warn]`/`[ok]`) survive forced-colors as text (they're rendered as JSX text, not CSS pseudo-elements)
- Verify OAuth consent scope chips (`[markos:read]`, etc.) remain readable under forced-colors
- Verify Deny button (`.c-button--destructive`) remains semantically distinct via `ButtonText`/`Highlight`

result: [pending]

### 3. `[data-theme="light"]` light-mode flip verification

expected: Set `<html data-theme="light">` via DevTools, verify:
- All 4 auth surfaces flip to Lino Light `#F4F4F0` surface
- Mint accent unchanged across modes (per `--color-primary-text: #00805F` light override from Phase 213.1)
- No contrast collapse on light raised surfaces
- Focus rings still 2px solid Protocol Mint with 2px offset
- Bracketed-glyph state prefixes remain readable
- OAuth scope chips (`.c-chip-protocol`) render with darker mint text on light raised surface
- Deny destructive tint (`rgb(248 81 73 / 0.12)` background + `var(--color-error)` text) readable on light
- NO mint-as-text on light surfaces (DESIGN.md forbidden — fails 1.6:1 contrast)

result: [pending]

### 4. WebAuthn / passkey flow (login surface)

expected: On a real browser session with passkey credential support:
- Exercise the login passkey flow end-to-end (Phase 200 OAuth device flow + `@simplewebauthn/browser` integration preserved)
- Verify visual primitives (`.c-card--feature`, `.c-input`, `.c-button--primary`) render correctly during passkey prompt
- Verify wiring (form action `/api/auth/signup`, `headers()`, `fetchBranding()`, `useTenantChrome`, `x-markos-byod`/`x-markos-tenant-id`) untouched per CONTEXT D-20
- Confirm BYOD branding (logo + powered-by link) survives the `--accent` inline-style removal

result: [pending]

### 5. OAuth consent grant + deny end-to-end (real OAuth client + multi-scope flow)

expected: Use a real test OAuth client to exercise the full consent flow:
- Approve path: scope chips render as `.c-chip-protocol` (squared signature MarkOS pattern); approval button (`.c-button--primary`) wires to `/oauth/authorize/approve` (Phase 202 MCP server consent contract preserved)
- Deny path: `.c-button--destructive` Deny button semantically rejects authorization
- Multi-scope rendering: each requested scope renders as separate `[scope_name]` chip
- Multi-tenant: when consent applies across tenants, ConsentCard handles the `MultiTenant` state correctly
- Invalid states: missing fields, expired consent, invalid `redirect_uri` all render with `[err]` bracketed-glyph prefix and revised remediation copy
- F-89 contract assertions in `test/mcp/consent-ui-a11y.test.js` already verified post-redesign — manual flow confirms wiring untouched

result: [pending]

## Summary

total: 5
passed: 0
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps

[None reported yet — fill in if any test surfaces an issue during human verification]
