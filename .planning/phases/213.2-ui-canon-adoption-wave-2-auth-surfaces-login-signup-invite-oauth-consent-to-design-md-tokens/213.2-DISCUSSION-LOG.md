# Phase 213.2: UI Canon Adoption Wave 2 — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `213.2-CONTEXT.md` — this log preserves the alternatives considered.

**Date:** 2026-04-28
**Phase:** 213.2-ui-canon-adoption-wave-2-auth-surfaces-login-signup-invite-oauth-consent-to-design-md-tokens
**Mode:** discuss (auto mode — system-level auto-execute active; mirrors Phase 213.1 posture)
**Areas discussed:** Visual contract source · Wave structure · Plan count · File handling · JSX touch boundary · Form primitives · State signal · Test strategy · Existing test surface preservation · DESIGN.md amendments timing · Light-mode storybook wiring · Marketing brand register · Banned lexicon · Token-only authoring

---

## Visual Contract (no discussion)

UI-SPEC.md will be generated next via `/gsd-ui-phase 213.2` and will lock every visual decision (color, spacing, radius, typography, motion, focus, accessibility, copywriting). Discuss-phase here focused on implementation strategy gray areas only — same posture as Phase 213.1.

---

## Wave structure

| Option | Description | Selected |
|---|---|---|
| A: Single wave (4 plans, 1 per surface) — sequential | Atomic commits, hook contention avoided | ✓ |
| B: Two waves (auth-2 = login+signup, auth-3 = invite+oauth) | Parallelize within waves | |
| C: Single big plan covering all 4 surfaces | Coupling acceptable since all consume same primitives | |

**Choice:** A.
**Reason:** 4 surfaces are mutually independent (no shared `.tsx` imports across login/signup/invite/oauth-consent), so single wave is safe. 1 plan per surface = atomic-commit cadence + clean SUMMARY.md per surface + clean rollback boundary if one redesign produces a wiring regression. Sequential within wave matches Phase 213.1 D-06 hook-contention avoidance.
**Auto-selection rationale:** Mirrors Phase 213.1; smallest deviation from established pattern.

---

## File handling

| Option | Description | Selected |
|---|---|---|
| A: Replace contents in place; keep filenames + import paths | `page.module.css` keeps name; contents fully rewritten | ✓ |
| B: New file + redirect | `page-v2.module.css` + import-path swap in `.tsx` | |
| C: Migrate to plain `.css` (drop modules) | Global CSS migration | |

**Choice:** A.
**Reason:** Lowest blast radius. Filename-stable replacement leaves Next.js routing + import resolver untouched. Class names may rename per UI-SPEC's primitive-reuse table; consuming `.tsx` updated in same plan as `.module.css` rewrite (zero broken `styles.X` refs after).
**Auto-selection rationale:** Identical decision to Phase 213.1 D-07.

---

## JSX touch boundary

| Option | Description | Selected |
|---|---|---|
| A: Class-name updates + copywriting revisions only | No DOM tree changes, no prop changes | ✓ |
| B: Full JSX redesign | New form decomposition, prop API revision | |

**Choice:** A.
**Reason:** Phase 213.2 is CSS canon adoption + token rewiring + copywriting cleanup. React surface untouched in shape. Visual primitives applied via className composition.
**Auto-selection rationale:** Identical to Phase 213.1 D-08. Smallest blast radius. UI-SPEC won't specify React-side restructuring.

---

## Form primitives strategy

| Option | Description | Selected |
|---|---|---|
| A: Compose `.c-input` + `.c-field` + `.c-button--*` from `styles/components.css` (zero bespoke form CSS) | Modules carry only layout/spacing concerns | ✓ |
| B: Hybrid — bespoke form classes per surface, primitives only for buttons | More flexibility per page | |
| C: Bespoke everything | Maximum customization | |

**Choice:** A.
**Reason:** DESIGN.md "Inputs" + "Buttons" sections lock the input/button visual contract. Bespoke form CSS would re-litigate that contract per page. Primitive-first approach: modules become thin layout shells; visual identity carried by `.c-*` global classes. Matches Phase 213.1 D-13 cross-cutting decision.
**Auto-selection rationale:** Highest leverage. Smallest module-CSS surface area.

---

## State signal pattern

| Option | Description | Selected |
|---|---|---|
| A: Bracketed-glyph prefixes (`[err]`/`[warn]`/`[ok]`) on every state-bearing text + alpha-tint background; never color-alone | Carry-forward from Phase 213.1 | ✓ |
| B: Color-only signals | Faster but fails DESIGN.md "Color blindness" | |
| C: Inline icons | Adds icon dependency surface | |

**Choice:** A.
**Reason:** DESIGN.md "Color blindness" rule + Phase 213.1 AC#9 precedent. `.c-field__error::before` already auto-prepends `[err] ` so error messages are zero-config.
**Auto-selection rationale:** Carry-forward from 213.1 acceptance gate.

---

## Test strategy

| Option | Description | Selected |
|---|---|---|
| A: `node --test` only (preserve existing `test/auth/` + `test/tenant-auth/` green) + new `test/ui-a11y/213-2-auth-a11y.test.js` | Mirrors Phase 213.1 D-03/D-05/D-17 | ✓ |
| B: Migrate auth tests to Vitest | Aligns with v4.0.0 planned stack | |
| C: Both | Adds churn | |

**Choice:** A.
**Reason:** Vitest migration is separate concern (`.planning/codebase/TESTING.md`). This wave preserves Phase 200/201/202 OAuth + tenant-signup wiring contracts (D-19) and adds a new grep-shape a11y suite mirroring `test/ui-a11y/213-1-chrome-a11y.test.js`.
**Auto-selection rationale:** Smallest test-stack churn. Existing wiring contracts protected.

---

## DESIGN.md amendments timing

| Option | Description | Selected |
|---|---|---|
| A: Land 5 amendment PR BEFORE `/gsd-plan-phase 213.2` (block phase) | gsd-ui-checker Rec D | |
| B: Defer; inline workaround for `c-notice` if oauth/consent banner needs it | Smallest schedule disruption | ✓ |
| C: Skip amendments entirely | Wrong (oauth surface needs `c-notice`) | |

**Choice:** B (deferred decision; final call at plan-phase).
**Reason:** Amendments are non-blocking per Phase 213.1. If user lands the amendment PR before plan-phase, Plan 213.2-04 composes `.c-notice--warning` directly. If not, plan uses inline scoped local-class workaround (same as Phase 213.1). Decision parking-lot to plan-phase.
**Auto-selection rationale:** Reversible default; doesn't gate discuss-phase.

---

## Light-mode storybook toolbar wiring

| Option | Description | Selected |
|---|---|---|
| A: Wire toolbar in this phase | Auth stories switchable in Storybook | |
| B: Defer to Phase 213.4 or follow-up | Auth surfaces inherit `[data-theme="light"]` correctly via tokens; toolbar is convenience-only | ✓ |
| C: Skip | Won't help auth UAT but blocks future phases too | |

**Choice:** B.
**Reason:** `app/tokens.css [data-theme="light"]` block already correctly overrides every theme-sensitive token (verified by `test/ui-a11y/213-1-light-and-forced-colors.test.js`). Toolbar wiring adds `addon-themes` config burden out-of-scope here.
**Auto-selection rationale:** Smallest scope creep.

---

## Marketing brand register for `(marketing)/signup`

| Option | Description | Selected |
|---|---|---|
| A: Same DESIGN.md tokens (Kernel Black surface, Protocol Mint signal) — uniform brand | Carry-forward, consistent with markos chrome | ✓ |
| B: Distinct marketing register (more aspirational hero, lighter palette) | Requires marketing-design phase | |

**Choice:** A.
**Reason:** No prior decision differentiates `(marketing)` from `(markos)` brand register. DESIGN.md is project-wide (per CLAUDE.md "Visual Design Canon" section). If marketing wants a different visual register later, file as separate phase. Consistency wins for now.
**Auto-selection rationale:** No conflicting prior decision. Smallest scope.

---

## Auto-Resolved (no Unclear items)

All gray areas above resolved with Confident or Likely confidence by carrying forward Phase 213.1 patterns. No `Unclear` items required user correction. Auto mode proceeded directly to context capture.

## Folded Todos

`gsd-tools todo match-phase 213.2` returned 0 matches. Nothing folded.

## External Research

Not performed. DESIGN.md + Phase 213.1 precedent + repo codebase maps + existing primitive set in `styles/components.css` provided sufficient evidence. No library-version or ecosystem-best-practice questions open after analysis.

---

*Phase: 213.2-ui-canon-adoption-wave-2-auth-surfaces-login-signup-invite-oauth-consent-to-design-md-tokens*
*Discussion log: 2026-04-28 (auto mode)*
