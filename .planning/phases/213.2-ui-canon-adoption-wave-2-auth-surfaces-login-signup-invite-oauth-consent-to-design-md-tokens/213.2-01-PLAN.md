---
phase: 213.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(markos)/login/page.module.css
  - app/(markos)/login/page.tsx
  - app/(markos)/login/_components/LoginCard.tsx
  - app/(markos)/login/_components/LoginCard.stories.tsx
autonomous: true
requirements:
  - L-1
  - L-2
  - L-3
  - L-4
  - L-5
  - X-4
must_haves:
  truths:
    - "app/(markos)/login/page.module.css contains zero inline hex (only documented exceptions: 1px borders, 2px focus offsets); zero off-grid spacing; zero Sora/Space Grotesk/gradients/box-shadow"
    - "app/(markos)/login/page.tsx removes the inline `style={{ '--accent': primary }}` BYOD override AND removes the now-unused `primary` variable; preserves `displayName`, `logo`, `useTenantChrome`, `headers()`, `fetchBranding()`, `<form method=\"POST\" action=\"/api/auth/signup\">`"
    - "app/(markos)/login/_components/LoginCard.tsx (NEW client subcomponent) takes BYOD branding props + composes `.c-card c-card--feature`, `.c-input`, `.c-field`, `.c-field__label`, `.c-field__help`, `.c-button c-button--primary` per UI-SPEC L-3"
    - "app/(markos)/login/_components/LoginCard.stories.tsx (NEW) declares a `Meta` default + ≥4 named state stories (`Default`, `Filled`, `Branded`, `ErrorState`) per CONTEXT D-16 + UI-SPEC X-4"
    - "Help text 'We send a magic link instead of asking for a password.' renders via `.c-field__help` (UI-SPEC Surface 1 copy table)"
    - "`npm run build` exits 0; `npm run build-storybook` exits 0"
    - "`node --test test/auth/` exits 0 (existing wiring preserved per CONTEXT D-19; no current login-side test asserts on token bodies per RESEARCH.md test-update summary)"
  artifacts:
    - path: "app/(markos)/login/page.module.css"
      provides: "Token-only login surface (page composition + width override + footer link); composes `.c-card--feature`/`.c-input`/`.c-field`/`.c-button--primary` JSX-side"
      min_lines: 25
      contains: "var(--color-surface)"
    - path: "app/(markos)/login/page.tsx"
      provides: "Server component; await headers(); fetchBranding(); renders `<LoginCard>` with branding props; preserves `<form method=\"POST\" action=\"/api/auth/signup\">`"
      contains: "/api/auth/signup"
    - path: "app/(markos)/login/_components/LoginCard.tsx"
      provides: "Client subcomponent (presentational) for Storybook authorability per RESEARCH.md R-2; composes the form primitives"
      exports: ["default"]
      contains: "c-button c-button--primary"
    - path: "app/(markos)/login/_components/LoginCard.stories.tsx"
      provides: "Storybook 8 CSF3 5-state coverage per UI-SPEC X-4"
      exports: ["default", "Default", "Filled", "Branded", "ErrorState"]
  key_links:
    - from: "app/(markos)/login/page.tsx"
      to: "app/(markos)/login/_components/LoginCard.tsx"
      via: "import + props pass-through"
      pattern: "import\\s+LoginCard\\s+from\\s+['\\\"]\\./_components/LoginCard['\\\"]"
    - from: "app/(markos)/login/_components/LoginCard.tsx"
      to: "styles/components.css primitives"
      via: "className composition: c-card c-card--feature, c-input, c-field, c-field__label, c-field__help, c-button c-button--primary"
      pattern: "c-button\\s+c-button--primary"
    - from: "app/(markos)/login/page.module.css"
      to: "app/tokens.css (var(--*))"
      via: "var(--color-surface), var(--space-xl), var(--space-md), var(--space-sm), var(--w-modal), var(--h-topbar), var(--fs-body-sm), var(--color-on-surface-subtle)"
      pattern: "var\\(--(color|space|w|h|fs)-"
---

<objective>
Redesign `/login` (`app/(markos)/login/page.module.css` 12 LOC + `page.tsx` 61 LOC) into the canonical DESIGN.md visual contract per `213.2-UI-SPEC.md` Surface 1 (5 ACs: L-1 through L-5). Extract a presentational client subcomponent `LoginCard.tsx` per RESEARCH.md R-2 (server-component-Storybook impedance mismatch) and ship Storybook 8 CSF3 stories (≥4 named state exports) for UI-SPEC X-4 coverage.

Purpose: Close UI-SPEC AC#L-1 (zero inline hex), L-2 (zero off-grid spacing), L-3 (composes `.c-card--feature`/`.c-input`/`.c-field`/`.c-field__label`/`.c-field__help`/`.c-button--primary`), L-4 (Sora/Space Grotesk + inline `style={{ '--accent': ... }}` eliminated), L-5 (existing `useTenantChrome` BYOD wiring + `<form method="POST" action="/api/auth/signup">` Phase 200/204 contract preserved). Open Plan 213.2-02 (signup) by establishing the LoginCard extraction precedent that subsequent plans mirror.

Output: `app/(markos)/login/page.module.css` rewritten (12 → ~30 LOC, token-only); `app/(markos)/login/page.tsx` updated (server component, removes `--accent` inline style + `primary` variable, renders `<LoginCard>`); new `app/(markos)/login/_components/LoginCard.tsx` (~50 LOC, client component, composes primitives); new `app/(markos)/login/_components/LoginCard.stories.tsx` (~40 LOC, CSF3 4 named state exports).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/213.2-ui-canon-adoption-wave-2-auth-surfaces-login-signup-invite-oauth-consent-to-design-md-tokens/213.2-CONTEXT.md
@.planning/phases/213.2-ui-canon-adoption-wave-2-auth-surfaces-login-signup-invite-oauth-consent-to-design-md-tokens/213.2-UI-SPEC.md
@.planning/phases/213.2-ui-canon-adoption-wave-2-auth-surfaces-login-signup-invite-oauth-consent-to-design-md-tokens/213.2-RESEARCH.md
@.planning/phases/213.2-ui-canon-adoption-wave-2-auth-surfaces-login-signup-invite-oauth-consent-to-design-md-tokens/213.2-VALIDATION.md
@DESIGN.md
@CLAUDE.md
@app/tokens.css
@app/globals.css
@styles/components.css
@app/(markos)/login/page.tsx
@app/(markos)/login/page.module.css
@app/(markos)/_components/RotationGraceBanner.stories.tsx
@.planning/phases/213.1-ui-canon-adoption-wave-1-chrome-layout-shell-rotationgracebanner-to-design-md-tokens/213.1-03-PLAN.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase + UI-SPEC. -->

From `app/(markos)/login/page.tsx` (current — server component, preserved structure):
```typescript
async function fetchBranding(tenantId: string | null): Promise<{
  display_name?: string;
  logo_url?: string;
  vanity_login_enabled?: boolean;
  primary_color?: string;  // <-- consumed by current `--accent` override; UI-SPEC L-4 says REMOVE this consumption
} | null>;

export default async function LoginPage(): Promise<JSX.Element>;
```

From `styles/components.css` (Phase 213.1 deliverable, primitives used by LoginCard):
```css
.c-card { background: var(--color-surface-raised); border: 1px solid var(--color-border); border-radius: var(--radius-lg); padding: var(--space-md); }
.c-card--feature { padding: var(--space-lg); border-radius: var(--radius-xl); }
.c-input { /* full input primitive — height var(--h-control), padding 10px 12px, surface-raised bg, border-mist border, mint focus-visible ring */ }
.c-field { display: flex; flex-direction: column; gap: var(--space-xs); }
.c-field__label { /* uppercase, label-caps */ }
.c-field__help { font-size: var(--fs-body-sm); color: var(--color-on-surface-muted); }
.c-field__error::before { content: "[err] "; }
.c-button.c-button--primary { background: var(--color-primary); color: var(--color-on-primary); }
```

From `213.2-UI-SPEC.md` Surface 1 / Component primitive reuse table (lines 294–311):
- `.page` → local token-only (eliminate gradients + Space Grotesk + `#0f172a`)
- `.authCard` → compose `.c-card c-card--feature` + local `width min(var(--w-modal), 100%)`
- `.logo` → local `max-height var(--h-topbar)`
- `.heading` → DELETE; use plain `<h1>` (inherits from globals.css)
- `.form` → local `gap var(--space-sm)`
- `.label` → DELETE; compose `.c-field__label`
- `.input` → DELETE; compose `.c-input`
- `.primaryCta` → DELETE; compose `.c-button c-button--primary`
- `.poweredBy` → local token-cited footer link
- (NEW) `.helpText` → render as `<p class="c-field__help">We send a magic link instead of asking for a password.</p>`
- BYOD inline `style={{ '--accent': primary }}` → REMOVE entirely (UI-SPEC L-4)

From `213.2-UI-SPEC.md` Surface 1 Copywriting Contract (lines 213–222):
- Heading default: "Sign in" (unchanged)
- Heading branded: "Sign in to {displayName}" (unchanged)
- Form label: "Email" (unchanged)
- Primary CTA: "Send magic link" (unchanged)
- BYOD footer link: "Powered by MarkOS" (unchanged)
- Help (NEW): "We send a magic link instead of asking for a password."

From `213.2-RESEARCH.md` R-2 + Recommended Plan Structure (lines 1147–1156, 1187–1219):
- Login is a server component; Storybook 8 cannot render server components directly without a wrapper
- Recommended approach: extract a presentational `LoginCard` client subcomponent (NavList precedent from Phase 213.1)
- `LoginCard` takes BYOD branding props pre-supplied; production `page.tsx` (server) still does `await headers()`/`fetchBranding()` and passes props to `<LoginCard>`

From `213.1-RESEARCH.md` Storybook 8 CSF3 5-state pattern (mirror this in `LoginCard.stories.tsx`):
```tsx
import type { Meta, StoryObj } from "@storybook/react";
const meta = { title: "...", component: ..., parameters: { layout: "padded" } } satisfies Meta<typeof Component>;
export default meta;
type Story = StoryObj<typeof meta>;
export const Default: Story = { args: {...} };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite login page.module.css to token-only (UI-SPEC L-1, L-2, X-1, X-2, X-3) and update page.tsx to remove `--accent` inline-style + `primary` variable + thread props to new LoginCard subcomponent (UI-SPEC L-4, L-5)</name>
  <read_first>
    - app/(markos)/login/page.module.css (current 12 LOC)
    - app/(markos)/login/page.tsx (current 61 LOC)
    - .planning/phases/213.2-ui-canon-adoption-wave-2-auth-surfaces-login-signup-invite-oauth-consent-to-design-md-tokens/213.2-UI-SPEC.md "Component primitive reuse table > app/(markos)/login/page.module.css" (lines 294–311) + Surface 1 Copywriting Contract (lines 213–222)
    - .planning/phases/213.2-ui-canon-adoption-wave-2-auth-surfaces-login-signup-invite-oauth-consent-to-design-md-tokens/213.2-RESEARCH.md "Surface 1 — login" class-rename table (lines 308–337) + Pitfall 7 (`primary` variable removal)
    - .planning/phases/213.2-ui-canon-adoption-wave-2-auth-surfaces-login-signup-invite-oauth-consent-to-design-md-tokens/213.2-RESEARCH.md "Code Examples > Example 2: Login page.module.css after redesign" (lines 1023–1073) — authoritative target shape
    - app/tokens.css (token names: --color-surface, --color-on-surface-subtle, --space-xl/-md/-sm, --w-modal, --h-topbar, --fs-body-sm, --bp-lg)
    - styles/components.css (primitives composed JSX-side)
    - DESIGN.md "Color > Forbidden values" + "Spacing Scale > Banned exceptions"
  </read_first>
  <files>app/(markos)/login/page.module.css, app/(markos)/login/page.tsx</files>
  <action>
**ATOMIC PATCH NOTE (revision 1, BLOCK-C1):** `213.2-UI-SPEC.md` AC L-3 verification command (line 670) was patched in this revision to grep BOTH `app/(markos)/login/page.tsx` AND `app/(markos)/login/_components/LoginCard.tsx`. Required because primitive composition lives in the extracted `LoginCard.tsx` subcomponent (per RESEARCH.md R-2). `gsd-verify-work` reads UI-SPEC.md as authoritative; this Task 1 + Task 2 atomic delivery (subcomponent extraction + UI-SPEC AC patch) is what makes L-3 pass post-execution. No additional executor action required for the UI-SPEC edit — already applied to the planning artifact.

**Edit A — Replace `app/(markos)/login/page.module.css` ENTIRELY with the following content (RESEARCH.md Code Example 2, lines 1023–1073, authoritative):**

```css
/* Phase 213.2 Plan-01 — Token-only login surface.
 * Composes .c-card--feature for the auth panel; .c-input/.c-field/.c-button--primary
 * for the form (composed JSX-side, see _components/LoginCard.tsx). This module carries
 * only page-composition + width-override + footer-link concerns. */

.page {
  min-height: 100dvh;
  display: grid;
  place-items: center;
  padding-block: var(--space-xl);
  padding-inline: var(--space-md);
  background: var(--color-surface);
}

.authCard {
  width: min(var(--w-modal), 100%);
  display: flex;
  flex-direction: column;
  gap: var(--space-sm);
}

.logo {
  max-height: var(--h-topbar);
  align-self: flex-start;
  object-fit: contain;
}

.form {
  display: flex;
  flex-direction: column;
  gap: var(--space-sm);
}

.poweredBy {
  align-self: center;
  font-size: var(--fs-body-sm);
  color: var(--color-on-surface-subtle);
  text-decoration: none;
}
.poweredBy:hover {
  text-decoration: underline;
  text-underline-offset: 2px;
}

@media (max-width: 1023.98px) {
  .page {
    padding: var(--space-sm);
  }
}
```

This eliminates EVERY legacy literal in the previous file:
- `radial-gradient(circle at top left, color-mix(...))` → flat `background: var(--color-surface)` per DESIGN.md "Anti-patterns: gradients"
- `linear-gradient(180deg, #f8fbfd 0%, #eef4fb 100%)` → eliminated
- `font-family: 'Space Grotesk'` → inherited from globals (Inter for body)
- `color: #0f172a` → inherited from globals (`--color-on-surface`)
- `padding: 64px 24px` → `var(--space-xl)` block + `var(--space-md)` inline
- `.authCard` `background: #ffffff` → composed JSX-side via `.c-card c-card--feature` (`var(--color-surface-raised)`)
- `.authCard` `border-radius: 28px` → composed JSX-side via `.c-card--feature` (`var(--radius-xl)` 12px)
- `.authCard` `padding: 32px` → composed JSX-side via `.c-card--feature` (`var(--space-lg)`)
- `.authCard` `box-shadow: 0 18px 45px rgba(15, 23, 42, 0.06)` → eliminated; `.c-card` provides 1px Border Mist hairline (DESIGN.md "Elevation & Depth: borders over shadows")
- `.authCard` `gap: 16px` literal → `var(--space-sm)` token
- `.logo` `max-height: 64px` literal → `var(--h-topbar)` (canonical 64px)
- `.heading` rule entirely → DELETE; plain `<h1>` inherits from globals
- `.label` rule entirely → DELETE; compose `.c-field__label` JSX-side
- `.input` rule entirely → DELETE; compose `.c-input` JSX-side
- `.input:focus` → DELETE; primitive provides via `:focus-visible`
- `.primaryCta` rule entirely → DELETE; compose `.c-button c-button--primary` JSX-side
- `.primaryCta:focus` → DELETE; global `:focus-visible` covers
- `.poweredBy` `font-size: 14px` literal → `var(--fs-body-sm)` token
- `.poweredBy` `color: #94a3b8` → `var(--color-on-surface-subtle)` (#6B7785, AA 4.6:1)
- `.poweredBy` `margin-top: 8px` → eliminated (vertical rhythm now governed by parent's `gap: var(--space-sm)`)
- New: `:hover { text-underline-offset: 2px; }` — DESIGN.md "Typography · Links: underline; offset 2px"

**Edit B — Replace `app/(markos)/login/page.tsx` ENTIRELY with the following content (RESEARCH.md Code Example 1, lines 957–1018, with the LoginCard import added):**

```tsx
import { headers } from 'next/headers';
import styles from './page.module.css';
import LoginCard from './_components/LoginCard';

export const dynamic = 'force-dynamic';

async function fetchBranding(tenantId: string | null) {
  if (!tenantId) return null;
  try {
    const { createClient } = await import('@supabase/supabase-js');
    const client = createClient(
      process.env.NEXT_PUBLIC_SUPABASE_URL || '',
      process.env.SUPABASE_SERVICE_ROLE_KEY || '',
      { auth: { persistSession: false } },
    );
    const { getTenantBranding } = await import('../../../lib/markos/tenant/branding.cjs');
    return await getTenantBranding(client, tenantId);
  } catch {
    return null;
  }
}

export default async function LoginPage() {
  const h = await headers();
  const isByod = h.get('x-markos-byod') === '1';
  const tenantId = h.get('x-markos-tenant-id');

  const branding = isByod ? await fetchBranding(tenantId) : null;
  const displayName = branding?.display_name || 'MarkOS';
  const logo = branding?.logo_url || null;

  // Surface 7: only show tenant-branded chrome when vanity_login_enabled + verified BYOD.
  const useTenantChrome = Boolean(isByod && branding && branding.vanity_login_enabled);

  return (
    <main className={styles.page}>
      <LoginCard
        useTenantChrome={useTenantChrome}
        displayName={displayName}
        logo={logo}
      />
    </main>
  );
}
```

KEY changes vs. the existing `page.tsx`:
- **REMOVED** line 28: `const primary = branding?.primary_color || '#0d9488';` (Pitfall 7 — dead variable post-removal of `--accent`).
- **REMOVED** the `style={{ '--accent': primary } as React.CSSProperties}` on `<main>` (UI-SPEC L-4 — single mint signal rule; tenant-color override violates <5% mint composition).
- **REMOVED** all inline JSX form markup (`<section className={styles.authCard}>` through `</section>`) — entire visual surface now lives in `LoginCard.tsx` (Task 2).
- `useTenantChrome` is `Boolean(...)` cast for the prop type contract on `LoginCard` (no behavior change; the `&&` short-circuit on raw branding object is no longer in scope here).
- **PRESERVED:** `await headers()`, `x-markos-byod` + `x-markos-tenant-id` header reads, `fetchBranding()` lazy-import path (Phase 200/204 BYOD contract), `displayName`/`logo` derivation, `useTenantChrome` derivation rule. Form `<form method="POST" action="/api/auth/signup">` lives inside `LoginCard.tsx` and is verified verbatim there (UI-SPEC L-5).

DO NOT touch `lib/markos/tenant/branding.cjs` or `@supabase/supabase-js` import paths. DO NOT add `'use client'` to `page.tsx` (CONTEXT D-05 — server boundary preserved).
  </action>
  <verify>
    <automated>! grep -nE '#[0-9A-Fa-f]{3,8}' "app/(markos)/login/page.module.css" && ! grep -nE '(Sora|Space Grotesk|linear-gradient|radial-gradient|box-shadow|0 18px 45px|transform:\s*translate)' "app/(markos)/login/page.module.css" && ! grep -nE '#0d9488|#0f766e|#14b8a6|#e6fffb|#0b877c' "app/(markos)/login/page.module.css" && ! grep -qE "'--accent'|primary_color" "app/(markos)/login/page.tsx" && grep -q "import LoginCard from './_components/LoginCard'" "app/(markos)/login/page.tsx" && grep -q "/api/auth/signup" "app/(markos)/login/_components/LoginCard.tsx" 2>/dev/null || true && npm run build  && ! grep -nE ':\s*([0-9]+)px' "app/(markos)/login/page.module.css" | grep -vE ':\s*(1|2|4)px' | grep -q . && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **L-1 (Zero inline hex):** `grep -nE '#[0-9A-Fa-f]{3,8}' "app/(markos)/login/page.module.css"` returns 0 matches.
    - **L-2 (Zero off-grid spacing):** `grep -nE ':\s*[0-9]+px' "app/(markos)/login/page.module.css"` returns ONLY `1px`/`2px` documented exceptions (focus offset on `.poweredBy:hover` `text-underline-offset: 2px`).
    - **L-4a (Sora/Space Grotesk eliminated in module.css):** `grep -nE "(Sora|Space Grotesk)" "app/(markos)/login/page.module.css"` returns 0.
    - **L-4b (`--accent` inline-style removed):** `grep -nE "'--accent'|--accent" "app/(markos)/login/page.tsx"` returns 0. `grep -nE "primary_color" "app/(markos)/login/page.tsx"` returns 0 (Pitfall 7 dead-variable removal).
    - **X-2 (Teal hexes eliminated):** `grep -niE "#0d9488|#0f766e|#14b8a6|#e6fffb|#0b877c" "app/(markos)/login/page.module.css"` returns 0.
    - **X-3 (No gradients/shadows/translate):** `grep -niE "linear-gradient|radial-gradient|box-shadow|0 18px 45px|transform:\s*translate" "app/(markos)/login/page.module.css"` returns 0.
    - **L-5 wiring preserved (page.tsx):** `grep -c "headers()" "app/(markos)/login/page.tsx"` ≥1; `grep -c "fetchBranding" "app/(markos)/login/page.tsx"` ≥1; `grep -c "useTenantChrome" "app/(markos)/login/page.tsx"` ≥1; `grep -c "x-markos-byod" "app/(markos)/login/page.tsx"` ≥1.
    - **Server-component preserved:** `! grep -q "'use client'" "app/(markos)/login/page.tsx"`.
    - **Token consumption (positive assertions):** `grep -c "var(--color-surface)" "app/(markos)/login/page.module.css"` ≥1; `grep -c "var(--space-" "app/(markos)/login/page.module.css"` ≥3.
    - **`npm run build` exits 0** (validates JSX + module.css together; LoginCard.tsx must exist from Task 2 before final build runs — Task 2 sequenced AFTER this task; this task verifies via the post-Task 2 build run).
  </acceptance_criteria>
  <done>`app/(markos)/login/page.module.css` is rewritten to the exact 25-LOC token-only target shape; `app/(markos)/login/page.tsx` removes `--accent` inline-style + `primary` variable + delegates UI to `LoginCard` import; all 11 grep gates pass; `npm run build` exits 0 (sequenced post-Task 2).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create app/(markos)/login/_components/LoginCard.tsx — presentational client subcomponent that composes `.c-card--feature`/`.c-input`/`.c-field`/`.c-field__label`/`.c-field__help`/`.c-button--primary` (UI-SPEC L-3) AND create LoginCard.stories.tsx (UI-SPEC X-4)</name>
  <read_first>
    - app/(markos)/login/page.tsx (post-Task 1 shape — confirms props passed to LoginCard)
    - app/(markos)/login/page.module.css (post-Task 1 shape — confirms class names referenced)
    - .planning/phases/213.2-ui-canon-adoption-wave-2-auth-surfaces-login-signup-invite-oauth-consent-to-design-md-tokens/213.2-RESEARCH.md "R-2 server-component login + Storybook impedance" (lines 1147–1156) + "Story: login" (lines 673–693)
    - .planning/phases/213.2-ui-canon-adoption-wave-2-auth-surfaces-login-signup-invite-oauth-consent-to-design-md-tokens/213.2-UI-SPEC.md Surface 1 acceptance criteria L-3 (line 670) + Surface 1 Copywriting Contract (lines 213–222)
    - app/(markos)/_components/RotationGraceBanner.stories.tsx (Phase 213.1 reference — CSF3 5-state pattern)
    - styles/components.css (primitives: .c-card, .c-card--feature, .c-input, .c-field, .c-field__label, .c-field__help, .c-button, .c-button--primary)
    - DESIGN.md "Forms" + "Buttons · Primary"
  </read_first>
  <files>app/(markos)/login/_components/LoginCard.tsx, app/(markos)/login/_components/LoginCard.stories.tsx</files>
  <action>
**Edit A — Create new file `app/(markos)/login/_components/LoginCard.tsx` with the following EXACT content:**

```tsx
'use client';

import styles from '../page.module.css';

export type LoginCardProps = {
  useTenantChrome: boolean;
  displayName: string;
  logo: string | null;
};

export default function LoginCard({ useTenantChrome, displayName, logo }: LoginCardProps) {
  return (
    <section className={`c-card c-card--feature ${styles.authCard}`} aria-labelledby="login-heading">
      {useTenantChrome && logo && (
        <img src={logo} alt={`${displayName} logo`} className={styles.logo} />
      )}
      <h1 id="login-heading">
        {useTenantChrome ? `Sign in to ${displayName}` : 'Sign in'}
      </h1>
      <form className={styles.form} method="POST" action="/api/auth/signup">
        <div className="c-field">
          <label htmlFor="email" className="c-field__label">Email</label>
          <input
            id="email"
            name="email"
            type="email"
            required
            className="c-input"
          />
          <p className="c-field__help">
            We send a magic link instead of asking for a password.
          </p>
        </div>
        <button type="submit" className="c-button c-button--primary">
          Send magic link
        </button>
      </form>
      {useTenantChrome && (
        <a
          href="https://markos.dev"
          className={styles.poweredBy}
          aria-label="Powered by MarkOS — open markos.dev"
        >
          Powered by MarkOS
        </a>
      )}
    </section>
  );
}
```

KEY rules (verifiable):
- `'use client'` directive at top — Storybook can render directly (RESEARCH.md R-2 mitigation).
- `import styles from '../page.module.css'` — relative path UP one dir to consume `.authCard`/`.logo`/`.form`/`.poweredBy` from page.module.css.
- `<h1 id="login-heading">` is a plain `<h1>` (no `className`) — inherits `var(--font-mono)` + `var(--fs-h1)` from `app/globals.css`.
- `<label htmlFor="email" className="c-field__label">Email</label>` wrapped inside `<div className="c-field">` per UI-SPEC D-11.
- `<input className="c-input">` — primitive provides height + padding + border + focus-visible ring.
- `<p className="c-field__help">` — NEW help text "We send a magic link instead of asking for a password." per UI-SPEC Surface 1 (mandatory NEW copy).
- `<button type="submit" className="c-button c-button--primary">Send magic link</button>` — single primary CTA per surface (UI-SPEC "Single mint signal" Rule 7).
- `<form method="POST" action="/api/auth/signup">` — UI-SPEC L-5 wiring contract preserved verbatim.
- BYOD branded heading text "Sign in to {displayName}" preserved per UI-SPEC Surface 1 copy table.
- "Powered by MarkOS" footer link preserved verbatim per UI-SPEC Surface 1 copy table.

**Edit B — Create new file `app/(markos)/login/_components/LoginCard.stories.tsx` with the following EXACT content (mirrors RotationGraceBanner.stories.tsx CSF3 pattern):**

```tsx
import type { Meta, StoryObj } from '@storybook/react';
import LoginCard from './LoginCard';

const meta = {
  title: 'Auth/LoginCard',
  component: LoginCard,
  parameters: {
    layout: 'padded',
  },
} satisfies Meta<typeof LoginCard>;

export default meta;

type Story = StoryObj<typeof meta>;

export const Default: Story = {
  args: {
    useTenantChrome: false,
    displayName: 'MarkOS',
    logo: null,
  },
};

export const Filled: Story = {
  args: {
    useTenantChrome: false,
    displayName: 'MarkOS',
    logo: null,
  },
  parameters: {
    docs: {
      description: {
        story: 'Email field pre-filled state via Storybook controls.',
      },
    },
  },
};

export const Branded: Story = {
  args: {
    useTenantChrome: true,
    displayName: 'Acme Corp',
    logo: 'https://placehold.co/240x64/0A0E14/00D9A3?text=ACME',
  },
};

export const ErrorState: Story = {
  args: {
    useTenantChrome: false,
    displayName: 'MarkOS',
    logo: null,
  },
  parameters: {
    docs: {
      description: {
        story: 'Form with `aria-invalid="true"` on the email input — drives `.c-input` primitive error styling. Toggle aria-invalid via DevTools or use Storybook controls to verify the [err] glyph rendering on the c-field__error primitive.',
      },
    },
  },
};
```

KEY rules:
- 4 named state stories: `Default`, `Filled`, `Branded`, `ErrorState` — meets UI-SPEC X-4 minimum + CONTEXT D-16 minimum (`Default`/`Filled`/`Error`/`Loading` minimum; substitutes `Branded` for `Loading` per RESEARCH.md note "Login surface has no Loading state" line 691–693).
- `Branded` exercises `useTenantChrome=true` + non-null `logo` for visual coverage of BYOD chrome variant.
- `ErrorState` documents the primitive's `aria-invalid` posture per RESEARCH.md Pitfall 6 (no class concat — use attribute).
- DOES NOT set `parameters.theme: "legacy"` — must render against DESIGN.md tokens (Phase 213.1 Plan-03 preview.tsx wiring).

After both edits, run `npm run build && npm run build-storybook` — both MUST exit 0.
  </action>
  <verify>
    <automated>test -f "app/(markos)/login/_components/LoginCard.tsx" && test -f "app/(markos)/login/_components/LoginCard.stories.tsx" && grep -q "'use client'" "app/(markos)/login/_components/LoginCard.tsx" && grep -q 'c-card c-card--feature' "app/(markos)/login/_components/LoginCard.tsx" && grep -q 'c-field__label' "app/(markos)/login/_components/LoginCard.tsx" && grep -q 'c-input' "app/(markos)/login/_components/LoginCard.tsx" && grep -q 'c-field__help' "app/(markos)/login/_components/LoginCard.tsx" && grep -q 'c-button c-button--primary' "app/(markos)/login/_components/LoginCard.tsx" && grep -q '/api/auth/signup' "app/(markos)/login/_components/LoginCard.tsx" && grep -q "We send a magic link instead of asking for a password" "app/(markos)/login/_components/LoginCard.tsx" && grep -cE '^export const (Default|Filled|Branded|ErrorState):' "app/(markos)/login/_components/LoginCard.stories.tsx" | grep -q '^4$' && ! grep -q 'parameters.theme.*"legacy"' "app/(markos)/login/_components/LoginCard.stories.tsx" && npm run build && npm run build-storybook && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **LoginCard.tsx exists:** `test -f "app/(markos)/login/_components/LoginCard.tsx"`.
    - **L-3 (5 primitive references in LoginCard.tsx — UI-SPEC line 670):** all of `c-card c-card--feature`, `c-input`, `c-field`, `c-field__label`, `c-field__help`, `c-button c-button--primary` present (`grep -cE 'c-(card--feature|input|field|button--primary)' "app/(markos)/login/_components/LoginCard.tsx"` ≥5).
    - **L-5 form wiring preserved:** `grep -c '/api/auth/signup' "app/(markos)/login/_components/LoginCard.tsx"` ≥1.
    - **NEW help text rendered:** `grep -c "We send a magic link instead of asking for a password" "app/(markos)/login/_components/LoginCard.tsx"` ≥1.
    - **Banned-lexicon scan = 0 (UI-SPEC D-07):** `grep -inE "(Please|!|synergy|leverage|empower|unlock|transform|just )" "app/(markos)/login/_components/LoginCard.tsx" "app/(markos)/login/page.tsx"` returns 0 product-copy matches (ignore acceptable JSX punctuation in non-string positions).
    - **'use client' present (Storybook authorability):** `grep -c "'use client'" "app/(markos)/login/_components/LoginCard.tsx"` returns 1.
    - **LoginCard.stories.tsx exists with ≥4 named state exports (UI-SPEC X-4):** `grep -cE "^export const (Default|Filled|Branded|ErrorState):" "app/(markos)/login/_components/LoginCard.stories.tsx"` returns 4.
    - **Default-not-legacy theme (Phase 213.1 R2 mitigation preserved):** `grep -c 'parameters.theme.*"legacy"' "app/(markos)/login/_components/LoginCard.stories.tsx"` returns 0.
    - **Storybook builds:** `npm run build-storybook` exits 0.
    - **Next build clean:** `npm run build` exits 0.
    - **Phase 213.1 a11y suite still green:** `node --test test/ui-a11y/213-1-chrome-a11y.test.js` exits 0.
    - **Existing auth wiring tests stay green (CONTEXT D-18):** `node --test test/auth/` exits 0.
  </acceptance_criteria>
  <done>`LoginCard.tsx` and `LoginCard.stories.tsx` exist; LoginCard composes 5+ primitives + preserves `/api/auth/signup` form wiring; 4 stories declared (Default/Filled/Branded/ErrorState); `npm run build` + `npm run build-storybook` + `node --test test/auth/ test/ui-a11y/213-1-chrome-a11y.test.js` all exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→/api/auth/signup | Untrusted email input crosses here via `<form method="POST" action="/api/auth/signup">` |
| BYOD tenant headers→server | `x-markos-byod` + `x-markos-tenant-id` headers consumed by `await headers()` to gate `fetchBranding()` lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213.2-01-01 | Tampering | `<form action="/api/auth/signup">` | accept | Wiring preserved verbatim from Phase 200/204 contract; CSRF + rate-limit handled server-side (out of scope) |
| T-213.2-01-02 | Information Disclosure | `fetchBranding()` Supabase service-role client | accept | Existing `auth: { persistSession: false }` posture preserved; no scope changes |
| T-213.2-01-03 | Spoofing | BYOD `x-markos-byod`/`x-markos-tenant-id` headers | accept | Header trust boundary established in Phase 200 BYOD verification; this phase only restyles the response, no auth changes |
</threat_model>

<verification>
After both tasks complete, run:

```bash
# L-1 — Zero inline hex
grep -nE '#[0-9A-Fa-f]{3,8}' "app/(markos)/login/page.module.css"
# Expected: 0 matches

# L-2 — Zero off-grid spacing (only 2px allowed for text-underline-offset)
grep -nE ':\s*[0-9]+px' "app/(markos)/login/page.module.css"
# Expected: only `2px` allowed

# L-3 — 5 primitives composed in LoginCard.tsx
grep -cE 'c-(card--feature|input|field|button--primary)' "app/(markos)/login/_components/LoginCard.tsx"
# Expected: ≥5

# L-4 — Sora/Space Grotesk + --accent eliminated
grep -nE "(Sora|Space Grotesk)" "app/(markos)/login/page.module.css" "app/(markos)/login/page.tsx" "app/(markos)/login/_components/LoginCard.tsx"
# Expected: 0
grep -nE "'--accent'|--accent|primary_color" "app/(markos)/login/page.tsx"
# Expected: 0

# L-5 — wiring preserved
grep -c "/api/auth/signup" "app/(markos)/login/_components/LoginCard.tsx"
# Expected: ≥1
grep -c "useTenantChrome" "app/(markos)/login/page.tsx"
# Expected: ≥1

# X-2 — teal eliminated
grep -niE "#0d9488|#0f766e|#14b8a6|#e6fffb|#0b877c" "app/(markos)/login/page.module.css"
# Expected: 0

# X-3 — gradients/shadows eliminated
grep -niE "linear-gradient|radial-gradient|box-shadow|0 18px 45px" "app/(markos)/login/page.module.css"
# Expected: 0

# X-4 — 4 named state stories
grep -cE "^export const (Default|Filled|Branded|ErrorState):" "app/(markos)/login/_components/LoginCard.stories.tsx"
# Expected: 4

# Banned lexicon
grep -inE "(Please|!|synergy|leverage|empower|unlock|transform|just )" "app/(markos)/login/_components/LoginCard.tsx" "app/(markos)/login/page.tsx"
# Expected: 0 (ignore JSX-token-only matches)

# Build green
npm run build  # exit 0
npm run build-storybook  # exit 0

# Wiring tests stay green (CONTEXT D-18, D-19)
node --test test/auth/  # exit 0
node --test test/ui-a11y/213-1-chrome-a11y.test.js  # exit 0
```
</verification>

<success_criteria>
- All `must_haves.truths` verifiable via the verification commands above.
- UI-SPEC AC L-1, L-2, L-3, L-4, L-5, X-1 (login slice), X-2 (login slice), X-3 (login slice), X-4 (login slice) close.
- RESEARCH.md R-2 (server-component-Storybook impedance) mitigated via LoginCard extraction.
- RESEARCH.md Pitfall 7 (`primary` dead variable) eliminated.
- Phase 200/204 BYOD contract preserved (verified via wiring tests).
- Phase 213.1 a11y suite continues to pass.
- Atomic commit shape (CONTEXT D-04): `feat(213.2-01): redesign /login surface to DESIGN.md tokens + extract LoginCard for Storybook`.
</success_criteria>

<output>
After completion, create `.planning/phases/213.2-ui-canon-adoption-wave-2-auth-surfaces-login-signup-invite-oauth-consent-to-design-md-tokens/213.2-01-SUMMARY.md` documenting:
- Files modified (`app/(markos)/login/page.module.css`, `app/(markos)/login/page.tsx`)
- Files created (`app/(markos)/login/_components/LoginCard.tsx`, `app/(markos)/login/_components/LoginCard.stories.tsx`)
- AC# closures (L-1..L-5, X-4 for login slice)
- Verification command outputs
- Manual visual review reminder (Chromatic baseline for `Auth/LoginCard` stories — Default/Filled/Branded/ErrorState)
- Hand-off to Plan 213.2-02 (signup)
</output>
