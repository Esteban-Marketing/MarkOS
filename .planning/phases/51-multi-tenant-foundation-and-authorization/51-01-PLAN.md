---
phase: 51-multi-tenant-foundation-and-authorization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/51_multi_tenant_foundation.sql
  - lib/markos/tenant/contracts.ts
  - test/tenant-auth/tenant-schema-rls.test.js
autonomous: true
requirements:
  - TEN-01
  - IAM-01

must_haves:
  truths:
    - "Tenant-scoped database entities enforce tenant_id boundaries and cannot be read or mutated across tenants."
    - "One user can resolve deterministic membership across multiple tenants without implicit trust."
    - "Legacy workspace identifiers are compatibility-only and do not override canonical tenant_id access decisions."
  artifacts:
    - path: "supabase/migrations/51_multi_tenant_foundation.sql"
      provides: "Tenant schema foundation, tenant_id backfill, and deny-by-default RLS policies with USING and WITH CHECK."
      exports: []
    - path: "lib/markos/tenant/contracts.ts"
      provides: "Canonical tenant principal and membership contract definitions shared by runtime and policy layers."
      exports:
        - "TenantMembership"
        - "TenantPrincipal"
        - "LegacyRole"
        - "IamRole"
    - path: "test/tenant-auth/tenant-schema-rls.test.js"
      provides: "Automated isolation checks for tenant_id presence and negative cross-tenant RLS behavior."
      exports: []
  key_links:
    - from: "supabase/migrations/51_multi_tenant_foundation.sql"
      to: "markos_workspaces"
      via: "compatibility bridge from workspace_id to tenant_id"
      pattern: "alter table.*add column.*tenant_id|update .* set tenant_id"
    - from: "supabase/migrations/51_multi_tenant_foundation.sql"
      to: "RLS policies"
      via: "tenant_id-based USING/WITH CHECK"
      pattern: "create policy|with check|auth.jwt\(\)"
    - from: "test/tenant-auth/tenant-schema-rls.test.js"
      to: "supabase/migrations/51_multi_tenant_foundation.sql"
      via: "contract assertions for policy existence and deny paths"
      pattern: "TEN-01|IAM-01|tenant_id"
---

<objective>
Define the tenant data and membership foundation so all downstream auth, API, and agent execution paths have enforceable tenant boundaries.

Purpose: Deliver TEN-01 and IAM-01 at the schema and contract level before runtime propagation work begins.
Output: New migration and tenant contract modules with executable isolation tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-multi-tenant-foundation-and-authorization/51-CONTEXT.md
@.planning/phases/51-multi-tenant-foundation-and-authorization/51-RESEARCH.md
@supabase/migrations/37_markos_ui_control_plane.sql
@supabase/migrations/47_operator_llm_management.sql
@onboarding/backend/runtime-context.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 51-01-01: Publish tenant and membership contracts (per D-01, D-03, D-07)</name>
  <read_first>onboarding/backend/runtime-context.cjs, lib/markos/rbac/policies.ts, .planning/phases/51-multi-tenant-foundation-and-authorization/51-CONTEXT.md</read_first>
  <files>lib/markos/tenant/contracts.ts</files>
  <action>
Create `lib/markos/tenant/contracts.ts` exporting canonical runtime contracts: `TenantMembership`, `TenantPrincipal`, `IamRole`, and `LegacyRole` with explicit legacy-to-v3.2 mapping table (`owner`, `operator`, `strategist`, `viewer`, `agent` -> canonical IAM roles). Include helper validation that rejects unmapped legacy roles with a deterministic error code. Keep `tenant_id` mandatory in all tenant-scoped types and mark `workspace_id` as compatibility-only metadata.
  </action>
  <acceptance_criteria>
`lib/markos/tenant/contracts.ts` exists, exports all listed symbols, and contains deterministic role mapping plus reject-on-unmapped behavior for IAM-01 migration safety.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/tenant-auth/tenant-schema-rls.test.js -x</automated>
  </verify>
  <done>
Tenant identity and membership semantics are centralized and reusable by runtime/auth layers without ambiguous role interpretation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 51-01-02: Add tenant schema, backfill bridge, and RLS policies (per D-01, D-02, D-03)</name>
  <read_first>supabase/migrations/37_markos_ui_control_plane.sql, supabase/migrations/47_operator_llm_management.sql, .planning/phases/51-multi-tenant-foundation-and-authorization/51-RESEARCH.md</read_first>
  <files>supabase/migrations/51_multi_tenant_foundation.sql</files>
  <action>
Create `supabase/migrations/51_multi_tenant_foundation.sql` to: (1) create tenant membership structures (`markos_tenants`, `markos_tenant_memberships`), (2) add `tenant_id` to tenant-scoped tables currently keyed by `workspace_id`, (3) backfill `tenant_id` from existing workspace rows, (4) add indexes on tenant_id, and (5) replace workspace-only RLS policies with tenant_id-based deny-by-default policies that include both `USING` and `WITH CHECK` clauses. Preserve compatibility reads by retaining `workspace_id` columns during this phase.
  </action>
  <acceptance_criteria>
Migration is idempotent, introduces tenant_id as canonical partition key, and policy definitions enforce cross-tenant read/write denial while retaining transitional compatibility fields.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/tenant-auth/tenant-schema-rls.test.js -x</automated>
  </verify>
  <done>
TEN-01 schema enforcement and IAM-01 membership persistence are present in one deterministic migration artifact.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 51-01-03: Add schema and RLS isolation contract tests (per D-02, D-05)</name>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/51-multi-tenant-foundation-and-authorization/51-CONTEXT.md, supabase/migrations/51_multi_tenant_foundation.sql</read_first>
  <files>test/tenant-auth/tenant-schema-rls.test.js</files>
  <action>
Create `test/tenant-auth/tenant-schema-rls.test.js` with explicit tests that parse/inspect the migration SQL and assert: required tables/columns exist, every tenant-scoped policy references tenant_id, write policies include WITH CHECK, and denial-path fixtures for cross-tenant read/write are declared. Include contract assertions tied to TEN-01 and IAM-01 IDs for traceability.
  </action>
  <acceptance_criteria>
Test suite fails when tenant_id or WITH CHECK rules are missing and passes only when migration artifact preserves required isolation semantics.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/tenant-auth/tenant-schema-rls.test.js -x</automated>
  </verify>
  <done>
Automated gate exists to prevent schema regressions that could reintroduce cross-tenant leakage.
  </done>
</task>

</tasks>

<verification>
Run `node --test test/tenant-auth/tenant-schema-rls.test.js -x` after each task and once at plan completion.
</verification>

<success_criteria>
Phase 51 has a concrete tenant schema baseline with executable isolation tests and canonical membership contracts required for runtime propagation.
</success_criteria>

<output>
After completion, create `.planning/phases/51-multi-tenant-foundation-and-authorization/51-01-SUMMARY.md`
</output>
