---
phase: 51-multi-tenant-foundation-and-authorization
plan: 03
type: execute
wave: 4
depends_on:
  - 51-01
  - 51-02
  - 51-04
files_modified:
  - lib/markos/rbac/policies.ts
  - lib/markos/rbac/iam-v32.ts
  - onboarding/backend/handlers.cjs
  - app/(markos)/operations/page.tsx
  - app/(markos)/operations/tasks/page.tsx
  - test/tenant-auth/iam-role-matrix.test.js
  - test/tenant-auth/legacy-role-compatibility.test.js
  - test/tenant-auth/ui-authorization-negative-path.test.js
autonomous: true
requirements:
  - IAM-01
  - IAM-02

must_haves:
  truths:
    - "Role resolution within a tenant is deterministic for users with one or more memberships."
    - "Protected actions enforce v3.2 IAM boundaries and default deny when role or action mapping is missing."
    - "Unauthorized actions fail closed in both UI and API layers with deterministic denial outcomes."
    - "Legacy roles remain compatibility-readable without widening effective permissions."
  artifacts:
    - path: "lib/markos/rbac/iam-v32.ts"
      provides: "Canonical role list and action-scoped permission map for v3.2 IAM roles."
      exports:
        - "IAM_V32_ROLES"
        - "ACTION_POLICY"
        - "canPerformAction"
    - path: "lib/markos/rbac/policies.ts"
      provides: "Compatibility bridge between legacy route checks and canonical action-level IAM decisions."
      exports:
        - "canAccess"
        - "canPublish"
    - path: "test/tenant-auth/iam-role-matrix.test.js"
      provides: "Automated action-role matrix assertions for owner, tenant-admin, manager, contributor, reviewer, billing-admin, readonly."
      exports: []
    - path: "test/tenant-auth/legacy-role-compatibility.test.js"
      provides: "Regression guard ensuring legacy role mapping does not broaden access."
      exports: []
    - path: "test/tenant-auth/ui-authorization-negative-path.test.js"
      provides: "UI-layer unauthorized-action regression coverage with deterministic fail-closed expectations."
      exports: []
  key_links:
    - from: "lib/markos/rbac/iam-v32.ts"
      to: "onboarding/backend/handlers.cjs"
      via: "action-level authorization checks before protected operations"
      pattern: "canPerformAction|deny"
    - from: "lib/markos/rbac/policies.ts"
      to: "lib/markos/rbac/iam-v32.ts"
      via: "legacy-role compatibility mapping to canonical IAM roles"
      pattern: "legacy|mapping|IAM"
    - from: "app/(markos)/operations/page.tsx"
      to: "lib/markos/rbac/iam-v32.ts"
      via: "UI authorization boundary checks route actions through canonical IAM policy"
      pattern: "canPerformAction|readonly|billing-admin"
    - from: "test/tenant-auth/ui-authorization-negative-path.test.js"
      to: "app/(markos)/operations/tasks/page.tsx"
      via: "UI negative-path assertions for unauthorized actions and deny-first rendering"
      pattern: "403|unauthorized|blocked|deny"
---

<objective>
Finalize enforceable role boundaries by introducing action-scoped IAM checks and locking compatibility migration behavior with deterministic tests.

Purpose: Deliver IAM-02 and complete IAM-01 role resolution guarantees on top of tenant context and execution-context foundations.
Output: Canonical IAM policy module, compatibility bridge updates, and compact role-matrix plus UI negative-path test suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-multi-tenant-foundation-and-authorization/51-CONTEXT.md
@.planning/phases/51-multi-tenant-foundation-and-authorization/51-RESEARCH.md
@.planning/phases/51-multi-tenant-foundation-and-authorization/51-01-PLAN.md
@.planning/phases/51-multi-tenant-foundation-and-authorization/51-02-PLAN.md
@.planning/phases/51-multi-tenant-foundation-and-authorization/51-04-PLAN.md
@lib/markos/rbac/policies.ts
@onboarding/backend/handlers.cjs
@app/(markos)/operations/page.tsx
@app/(markos)/operations/tasks/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 51-03-01: Introduce canonical IAM action policy module and matrix tests (per D-07, D-08)</name>
  <read_first>lib/markos/rbac/policies.ts, .planning/phases/51-multi-tenant-foundation-and-authorization/51-CONTEXT.md, .planning/REQUIREMENTS.md</read_first>
  <files>lib/markos/rbac/iam-v32.ts, test/tenant-auth/iam-role-matrix.test.js</files>
  <action>
Create `lib/markos/rbac/iam-v32.ts` exporting canonical v3.2 roles (`owner`, `tenant-admin`, `manager`, `contributor`, `reviewer`, `billing-admin`, `readonly`) and action-level permission helpers (`canPerformAction(role, action)`). Define explicit protected actions used by operations routes and enforce default deny for unknown actions or roles. Add `test/tenant-auth/iam-role-matrix.test.js` asserting complete role x action behavior and deny-default outcomes.
  </action>
  <acceptance_criteria>
Policy module and matrix test suite exist with deterministic default-deny behavior and complete IAM-02 role coverage.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/tenant-auth/iam-role-matrix.test.js -x</automated>
  </verify>
  <done>
IAM-02 canonical role boundary definitions are centralized and protected by explicit automated matrix checks.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 51-03-02: Enforce IAM checks in API layer with compatibility bridge (per D-07, D-08)</name>
  <read_first>lib/markos/rbac/policies.ts, lib/markos/rbac/iam-v32.ts, onboarding/backend/handlers.cjs, lib/markos/tenant/contracts.ts</read_first>
  <files>lib/markos/rbac/policies.ts, onboarding/backend/handlers.cjs, test/tenant-auth/legacy-role-compatibility.test.js</files>
  <action>
Update `lib/markos/rbac/policies.ts` so legacy role checks route through canonical IAM mapping from `lib/markos/tenant/contracts.ts` and `lib/markos/rbac/iam-v32.ts`, preserving backward compatibility while preventing privilege widening. In `onboarding/backend/handlers.cjs`, add action-level authorization checks at protected API entrypoints using active tenant role from execution context established in 51-04; return deterministic 403 with denial reason when action is not permitted. Add `test/tenant-auth/legacy-role-compatibility.test.js` to guard against compatibility widening.
  </action>
  <acceptance_criteria>
Protected handlers enforce action-scoped role checks and legacy role inputs resolve through explicit mapping without privilege expansion.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/tenant-auth/iam-role-matrix.test.js test/tenant-auth/legacy-role-compatibility.test.js -x</automated>
  </verify>
  <done>
IAM-01 compatibility and IAM-02 API enforcement are jointly validated by deterministic negative-path tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 51-03-03: Enforce IAM checks in UI authorization boundary (per D-07, D-08)</name>
  <read_first>app/(markos)/operations/page.tsx, app/(markos)/operations/tasks/page.tsx, lib/markos/rbac/iam-v32.ts</read_first>
  <files>app/(markos)/operations/page.tsx, app/(markos)/operations/tasks/page.tsx, test/tenant-auth/ui-authorization-negative-path.test.js</files>
  <action>
Implement an explicit UI authorization boundary for operations screens that resolves the active tenant role and checks action permissions before enabling protected controls. Ensure unauthorized roles render deterministic blocked states and cannot trigger API dispatch. Add `test/tenant-auth/ui-authorization-negative-path.test.js` to assert readonly, reviewer, and mismatched-role scenarios fail closed at render and interaction layers.
  </action>
  <acceptance_criteria>
UI authorization is enforced at render and interaction layers, and unauthorized user roles cannot trigger protected actions.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/tenant-auth/ui-authorization-negative-path.test.js -x</automated>
  </verify>
  <done>
IAM-02 UI-layer enforcement is explicit, deterministic, and independently test-gated.
  </done>
</task>

</tasks>

<verification>
Run `node --test test/tenant-auth/iam-role-matrix.test.js test/tenant-auth/legacy-role-compatibility.test.js test/tenant-auth/ui-authorization-negative-path.test.js -x` at completion.
</verification>

<success_criteria>
Tenant-aware IAM role boundaries are enforceable at action level in both API and UI layers with compatibility-safe migration and deterministic unauthorized-path coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/51-multi-tenant-foundation-and-authorization/51-03-SUMMARY.md`
</output>