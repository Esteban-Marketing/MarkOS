---
phase: 51-multi-tenant-foundation-and-authorization
plan: 02
type: execute
wave: 2
depends_on:
  - 51-01
files_modified:
  - app/(markos)/layout.tsx
  - app/(markos)/operations/tasks/page.tsx
  - onboarding/backend/runtime-context.cjs
  - api/config.js
  - api/status.js
  - api/migrate.js
  - test/tenant-auth/tenant-context-propagation.test.js
  - test/ui-security/security.test.js
autonomous: true
requirements:
  - TEN-02
  - IAM-01

must_haves:
  truths:
    - "Protected hosted flows fail closed when tenant context is missing or ambiguous."
    - "Tenant context is propagated deterministically through hosted wrappers and protected UI request paths."
    - "Wrapper and UI propagation contracts produce one canonical tenant principal shape for downstream consumers."
  artifacts:
    - path: "onboarding/backend/runtime-context.cjs"
      provides: "Tenant-aware hosted auth resolver and deterministic principal creation."
      exports:
        - "requireHostedSupabaseAuth"
        - "resolveRequestedProjectSlugFromRequest"
    - path: "test/tenant-auth/tenant-context-propagation.test.js"
      provides: "Automated fail-closed propagation checks across hosted API wrappers and tenant principal shape validation."
      exports: []
    - path: "test/ui-security/security.test.js"
      provides: "UI security contract assertions for tenant context propagation headers and fail-closed behavior."
      exports: []
  key_links:
    - from: "app/(markos)/layout.tsx"
      to: "api/status.js"
      via: "tenant-scoped request context propagation from UI shell to hosted API wrappers"
      pattern: "tenant|x-tenant-id|authorization"
    - from: "api/status.js"
      to: "onboarding/backend/runtime-context.cjs"
      via: "requireHostedSupabaseAuth with required tenant scope"
      pattern: "requireHostedSupabaseAuth|tenant"
    - from: "api/migrate.js"
      to: "onboarding/backend/runtime-context.cjs"
      via: "tenant-auth wrapper ensures protected migration endpoint always resolves tenant principal before downstream routing"
      pattern: "requireHostedSupabaseAuth|tenantPrincipal|markosAuth"
---

<objective>
Implement deterministic tenant context propagation across hosted auth wrappers and protected UI request paths.

Purpose: Deliver TEN-02 runtime propagation baseline and IAM-01 principal consistency before deeper execution-path propagation.
Output: Tenant-aware runtime auth boundary, wrapper/UI propagation wiring, and automated fail-closed propagation tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/51-multi-tenant-foundation-and-authorization/51-CONTEXT.md
@.planning/phases/51-multi-tenant-foundation-and-authorization/51-RESEARCH.md
@.planning/phases/51-multi-tenant-foundation-and-authorization/51-01-PLAN.md
@onboarding/backend/runtime-context.cjs
@api/config.js
@api/status.js
@api/migrate.js
@app/(markos)/layout.tsx
@app/(markos)/operations/tasks/page.tsx
@test/ui-security/security.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 51-02-01: Upgrade hosted auth boundary to tenant principal resolution (per D-04, D-05, D-06)</name>
  <read_first>onboarding/backend/runtime-context.cjs, api/config.js, api/status.js, api/migrate.js, lib/markos/tenant/contracts.ts</read_first>
  <files>onboarding/backend/runtime-context.cjs, api/config.js, api/status.js, api/migrate.js</files>
  <action>
Extend `requireHostedSupabaseAuth` in `onboarding/backend/runtime-context.cjs` to resolve `active_tenant_id` and tenant memberships from trusted claims, reject missing/ambiguous tenant context, and return deterministic error codes for missing tenant, audience mismatch, and scope denial. Update `api/config.js`, `api/status.js`, and `api/migrate.js` to pass required tenant selector from headers/query and to attach the returned tenant principal on `req.markosAuth` for downstream consumers.
  </action>
  <acceptance_criteria>
Hosted wrappers require tenant context for protected operations and return fail-closed 4xx outcomes when tenant scope is absent or invalid.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/tenant-auth/tenant-context-propagation.test.js -x</automated>
  </verify>
  <done>
All targeted hosted wrappers consume one shared tenant auth boundary with deterministic tenant principal output.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 51-02-02: Add UI tenant propagation contract and fail-closed UI tests (per D-04, D-05)</name>
  <read_first>app/(markos)/layout.tsx, app/(markos)/operations/tasks/page.tsx, api/status.js, test/ui-security/security.test.js</read_first>
  <files>app/(markos)/layout.tsx, app/(markos)/operations/tasks/page.tsx, test/ui-security/security.test.js</files>
  <action>
Define an explicit UI-layer propagation contract for protected MarkOS surfaces so tenant identity is attached to API calls in deterministic headers/context metadata and never inferred implicitly. Update `app/(markos)/layout.tsx` and `app/(markos)/operations/tasks/page.tsx` to source tenant identity from authenticated state only, propagate it to protected requests, and hard-stop protected actions when tenant context is missing. Extend `test/ui-security/security.test.js` with negative-path coverage asserting fail-closed UI behavior (no request dispatch plus visible denial state) when tenant context is null or ambiguous.
  </action>
  <acceptance_criteria>
Protected UI flows propagate tenant context deterministically to hosted APIs and fail closed before network execution when tenant context cannot be resolved.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/ui-security/security.test.js -x</automated>
  </verify>
  <done>
TEN-02 UI-layer propagation coverage is explicit and enforced by deterministic negative-path tests.
  </done>
</task>

</tasks>

<verification>
Run `node --test test/ui-security/security.test.js test/tenant-auth/tenant-context-propagation.test.js -x` at completion.
</verification>

<success_criteria>
Tenant context propagation is deterministic and fail-closed across protected hosted wrappers and UI request paths, producing one canonical tenant principal contract for downstream execution plans.
</success_criteria>

<output>
After completion, create `.planning/phases/51-multi-tenant-foundation-and-authorization/51-02-SUMMARY.md`
</output>