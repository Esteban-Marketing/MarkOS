---
phase: 43-onboarding-to-literacy-activation-pipeline
plan: 04
type: execute
wave: 3
depends_on:
  - 43-03
files_modified:
  - onboarding/backend/handlers.cjs
  - api/status.js
  - test/onboarding-server.test.js
autonomous: true
requirements:
  - LIT-14

must_haves:
  truths:
    - "Status endpoint exposes literacy readiness data with the same semantics used by submit."
    - "Status payload always includes literacy block even when providers are unconfigured."
    - "Hosted wrapper path preserves status literacy payload without custom duplication."
  artifacts:
    - path: "onboarding/backend/handlers.cjs"
      provides: "status literacy contract integration"
      exports: ["handleStatus"]
    - path: "api/status.js"
      provides: "hosted status wrapper parity for literacy block"
      exports: []
    - path: "test/onboarding-server.test.js"
      provides: "Wave 3 status contract verification"
      exports: []
  key_links:
    - from: "onboarding/backend/handlers.cjs"
      to: "onboarding/backend/literacy/activation-readiness.cjs"
      via: "shared readiness evaluator usage"
      pattern: "evaluateLiteracyReadiness"
    - from: "api/status.js"
      to: "onboarding/backend/handlers.cjs"
      via: "delegated status payload contract"
      pattern: "handleStatus"
---

<objective>
Integrate literacy readiness into status endpoint with parity to submit semantics.

Purpose: Provide operators a single health/readiness surface for literacy activation state.
Output: Updated status payload literacy block and passing status contract tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v3.0-LITERACY-SYSTEM-ROADMAP.md
@.planning/phases/43-onboarding-to-literacy-activation-pipeline/43-RESEARCH.md
@.planning/phases/43-onboarding-to-literacy-activation-pipeline/43-VALIDATION.md
@onboarding/backend/handlers.cjs
@api/status.js
@test/onboarding-server.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 43-04-01: Extend status response with readiness contract (per LIT-14)</name>
  <files>onboarding/backend/handlers.cjs, test/onboarding-server.test.js</files>
  <action>
Update `handleStatus` to include `literacy: { readiness, disciplines_available, gaps, last_ingestion_at }` in the response payload. Use readiness helper output for `readiness`, `disciplines_available`, and `gaps`. Default `last_ingestion_at` to `null` when no ingestion signal can be resolved.
  </action>
  <verify>
    <automated>node --test test/onboarding-server.test.js -x</automated>
  </verify>
  <done>
    Status response includes literacy block with stable field shapes across provider states.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 43-04-02: Enforce submit/status readiness parity contract (per LIT-14)</name>
  <files>onboarding/backend/handlers.cjs, test/onboarding-server.test.js</files>
  <action>
Ensure status readiness interpretation mirrors submit semantics for `ready|partial|unconfigured` under equivalent mocked conditions. Reuse the same helper and avoid duplicated state logic in status-specific branches.
  </action>
  <verify>
    <automated>node --test test/onboarding-server.test.js -x</automated>
  </verify>
  <done>
    Submit and status readiness states stay aligned by shared implementation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 43-04-03: Confirm hosted status wrapper parity (per LIT-14)</name>
  <files>api/status.js, test/onboarding-server.test.js</files>
  <action>
Validate hosted wrapper behavior so `api/status.js` continues delegating to `handleStatus` and returns literacy block unchanged after auth checks. Keep wrapper concise; only adjust if required to preserve response parity.
  </action>
  <verify>
    <automated>node --test test/onboarding-server.test.js -x</automated>
  </verify>
  <done>
    Hosted and local status pathways produce consistent literacy readiness contract payloads.
  </done>
</task>

</tasks>

<verification>
Run `node --test test/onboarding-server.test.js -x` and confirm status literacy block and parity assertions pass.
</verification>

<success_criteria>
Status endpoint becomes literacy-ready with consistent cross-runtime behavior and no contract drift.
</success_criteria>

<output>
After completion, create `.planning/phases/43-onboarding-to-literacy-activation-pipeline/43-04-SUMMARY.md`
</output>
