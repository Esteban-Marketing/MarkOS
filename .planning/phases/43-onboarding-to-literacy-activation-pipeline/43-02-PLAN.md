---
phase: 43-onboarding-to-literacy-activation-pipeline
plan: 02
type: execute
wave: 1
depends_on:
  - 43-01
files_modified:
  - onboarding/backend/literacy/activation-readiness.cjs
  - onboarding/backend/literacy/discipline-selection.cjs
  - test/onboarding-server.test.js
autonomous: true
requirements:
  - LIT-13

must_haves:
  truths:
    - "A shared readiness evaluator exists and can classify literacy as ready, partial, or unconfigured without blocking submit flow."
    - "Required discipline selection is deterministic even when the Phase 40 router module is unavailable."
    - "Literacy probes run with bounded cost using topK=1 and health-gated checks."
  artifacts:
    - path: "onboarding/backend/literacy/activation-readiness.cjs"
      provides: "health-aware readiness evaluator used by submit/status"
      exports: ["evaluateLiteracyReadiness"]
    - path: "onboarding/backend/literacy/discipline-selection.cjs"
      provides: "router adapter + deterministic fallback discipline resolver"
      exports: ["resolveRequiredDisciplines"]
    - path: "test/onboarding-server.test.js"
      provides: "Wave 1 readiness helper contract verification"
      exports: []
  key_links:
    - from: "onboarding/backend/literacy/activation-readiness.cjs"
      to: "onboarding/backend/vector-store-client.cjs"
      via: "health check and literacy probe calls"
      pattern: "healthCheck|getLiteracyContext"
    - from: "onboarding/backend/literacy/discipline-selection.cjs"
      to: "onboarding/backend/agents/discipline-router.cjs"
      via: "optional router usage with fallback"
      pattern: "rankDisciplines|fallback"
---

<objective>
Implement Wave 1 shared readiness and discipline-selection primitives required by submit/status activation flows.

Purpose: Centralize readiness logic to avoid drift and keep submit/status behavior parity.
Output: New literacy readiness module, deterministic discipline resolver, and green helper contracts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v3.0-LITERACY-SYSTEM-ROADMAP.md
@.planning/phases/43-onboarding-to-literacy-activation-pipeline/43-RESEARCH.md
@.planning/phases/43-onboarding-to-literacy-activation-pipeline/43-VALIDATION.md
@onboarding/backend/vector-store-client.cjs
@test/onboarding-server.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 43-02-01: Implement shared literacy readiness evaluator module (per LIT-13)</name>
  <files>onboarding/backend/literacy/activation-readiness.cjs, test/onboarding-server.test.js</files>
  <action>
Create `onboarding/backend/literacy/activation-readiness.cjs` exporting `evaluateLiteracyReadiness(seed, runtimeConfig, options)` that returns `{ readiness, disciplines_available, gaps, required_disciplines }`. Keep module side-effect free, return `unconfigured` when providers are not ready, and align state labels exactly to milestone contract.
  </action>
  <verify>
    <automated>node --test test/onboarding-server.test.js -x</automated>
  </verify>
  <done>
    Shared evaluator exists and satisfies Wave 0 readiness-state contracts.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 43-02-02: Add health-gated bounded discipline probes (per LIT-13)</name>
  <files>onboarding/backend/literacy/activation-readiness.cjs, test/onboarding-server.test.js</files>
  <action>
Implement health-first probing in readiness evaluator: call `healthCheck()` first, then probe required disciplines in parallel with `getLiteracyContext(discipline, 'health', {}, 1)`. Map probe outcomes to `ready|partial|unconfigured` without throwing on provider errors. Ensure probe logic supports configurable max discipline count with default top-3.
  </action>
  <verify>
    <automated>node --test test/onboarding-server.test.js -x</automated>
  </verify>
  <done>
    Readiness evaluator performs bounded, non-blocking literacy probes with deterministic classification.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 43-02-03: Implement deterministic discipline-router fallback adapter (per LIT-13)</name>
  <files>onboarding/backend/literacy/discipline-selection.cjs, onboarding/backend/literacy/activation-readiness.cjs, test/onboarding-server.test.js</files>
  <action>
Create `onboarding/backend/literacy/discipline-selection.cjs` that tries to consume `rankDisciplines(seed)` from Phase 40 router when present, and falls back to deterministic default discipline ordering when absent or invalid. Integrate this adapter into readiness evaluation so submit/status do not break on missing router module.
  </action>
  <verify>
    <automated>node --test test/onboarding-server.test.js -x</automated>
  </verify>
  <done>
    Required disciplines are always resolved consistently with graceful fallback behavior.
  </done>
</task>

</tasks>

<verification>
Run `node --test test/onboarding-server.test.js -x` and confirm all Wave 1 helper behaviors are green.
</verification>

<success_criteria>
Wave 1 provides reusable readiness primitives that can be consumed by submit and status without route-level duplication.
</success_criteria>

<output>
After completion, create `.planning/phases/43-onboarding-to-literacy-activation-pipeline/43-02-SUMMARY.md`
</output>
