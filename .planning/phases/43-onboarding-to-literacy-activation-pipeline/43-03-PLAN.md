---
phase: 43-onboarding-to-literacy-activation-pipeline
plan: 03
type: execute
wave: 2
depends_on:
  - 43-02
files_modified:
  - onboarding/backend/handlers.cjs
  - test/onboarding-server.test.js
autonomous: true
requirements:
  - LIT-13
  - LIT-15

must_haves:
  truths:
    - "Submit responses include literacy readiness and discipline gaps without changing successful onboarding semantics."
    - "Submit continues to succeed even when literacy is partial or unconfigured."
    - "Each successful submit emits a normalized `literacy_activation_observed` telemetry event."
  artifacts:
    - path: "onboarding/backend/handlers.cjs"
      provides: "submit readiness integration and activation telemetry emission"
      exports: ["handleSubmit"]
    - path: "test/onboarding-server.test.js"
      provides: "Wave 2 submit contract and telemetry verification"
      exports: []
  key_links:
    - from: "onboarding/backend/handlers.cjs"
      to: "onboarding/backend/literacy/activation-readiness.cjs"
      via: "post-orchestration readiness evaluation"
      pattern: "evaluateLiteracyReadiness"
    - from: "onboarding/backend/handlers.cjs"
      to: "onboarding/backend/agents/telemetry.cjs"
      via: "activation event emission"
      pattern: "literacy_activation_observed"
---

<objective>
Integrate literacy activation readiness and telemetry into submit flow after successful orchestration.

Purpose: Close onboarding-to-literacy activation gap while preserving current submit availability guarantees.
Output: Updated submit response contract + telemetry emission with passing integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v3.0-LITERACY-SYSTEM-ROADMAP.md
@.planning/phases/43-onboarding-to-literacy-activation-pipeline/43-RESEARCH.md
@.planning/phases/43-onboarding-to-literacy-activation-pipeline/43-VALIDATION.md
@onboarding/backend/handlers.cjs
@onboarding/backend/agents/telemetry.cjs
@onboarding/backend/literacy/activation-readiness.cjs
@test/onboarding-server.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 43-03-01: Attach readiness block to submit response contract (per LIT-13)</name>
  <files>onboarding/backend/handlers.cjs, test/onboarding-server.test.js</files>
  <action>
In `handleSubmit`, invoke the shared readiness evaluator after successful orchestration and include `literacy: { readiness, disciplines_available, gaps }` in the 200 response payload. Keep existing response fields (`drafts`, `vector_store`, `errors`, `session_url`, and intake metadata) intact.
  </action>
  <verify>
    <automated>node --test test/onboarding-server.test.js -x</automated>
  </verify>
  <done>
    Submit returns literacy readiness contract while preserving previous successful payload structure.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 43-03-02: Emit literacy activation telemetry on submit (per LIT-15)</name>
  <files>onboarding/backend/handlers.cjs, test/onboarding-server.test.js</files>
  <action>
Emit `telemetry.capture('literacy_activation_observed', payload)` once per successful submit with normalized payload: `readiness_status`, `disciplines_available`, `disciplines_missing`, `business_model`, `pain_point_count`, and `project_slug`. Do not include raw pain-point strings or large seed objects.
  </action>
  <verify>
    <automated>node --test test/onboarding-server.test.js -x</automated>
  </verify>
  <done>
    Activation telemetry contract is enforced and validated by tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 43-03-03: Preserve submit non-blocking degradation semantics (per LIT-13)</name>
  <files>onboarding/backend/handlers.cjs, test/onboarding-server.test.js</files>
  <action>
Ensure readiness probe failures or missing literacy content never downgrade successful submit to error responses. `partial` and `unconfigured` must remain informational contract states in 200 responses unless pre-existing submit validation/orchestration failures occur.
  </action>
  <verify>
    <automated>node --test test/onboarding-server.test.js -x</automated>
  </verify>
  <done>
    Submit remains operationally stable with literacy readiness as advisory state.
  </done>
</task>

</tasks>

<verification>
Run `node --test test/onboarding-server.test.js -x` after each task and confirm submit contract compatibility remains intact.
</verification>

<success_criteria>
Submit now surfaces literacy readiness and activation telemetry without introducing behavioral regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/43-onboarding-to-literacy-activation-pipeline/43-03-SUMMARY.md`
</output>
