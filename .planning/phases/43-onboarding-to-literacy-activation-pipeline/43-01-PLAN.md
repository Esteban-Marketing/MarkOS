---
phase: 43-onboarding-to-literacy-activation-pipeline
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/onboarding-server.test.js
autonomous: true
requirements:
  - LIT-13
  - LIT-14
  - LIT-15

must_haves:
  truths:
    - "Submit and status readiness contracts are executable and reproducible from a single fast test command before runtime changes."
    - "Telemetry activation behavior is test-locked, including normalized payload shape and no raw pain-point leakage."
    - "Phase 43 validation rows map directly to observable test outcomes for readiness, gaps, and telemetry emission."
  artifacts:
    - path: "test/onboarding-server.test.js"
      provides: "Wave 0 contract coverage for submit readiness states, status literacy block shape, and activation telemetry"
      exports: []
  key_links:
    - from: "test/onboarding-server.test.js"
      to: "onboarding/backend/handlers.cjs"
      via: "submit/status contract assertions"
      pattern: "handleSubmit|handleStatus|literacy"
    - from: "test/onboarding-server.test.js"
      to: "onboarding/backend/agents/telemetry.cjs"
      via: "activation event payload assertion"
      pattern: "literacy_activation_observed"
---

<objective>
Create Wave 0 Nyquist test scaffolding for Phase 43 so every requirement has an executable contract before implementation.

Purpose: Preserve 1:1 validation mapping and prevent silent readiness-contract regressions.
Output: Expanded `test/onboarding-server.test.js` with explicit task-ID-mapped assertions for Waves 1-4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v3.0-LITERACY-SYSTEM-ROADMAP.md
@.planning/phases/43-onboarding-to-literacy-activation-pipeline/43-RESEARCH.md
@.planning/phases/43-onboarding-to-literacy-activation-pipeline/43-VALIDATION.md
@test/onboarding-server.test.js
@onboarding/backend/handlers.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 43-01-01: Add submit literacy readiness contract stubs (per LIT-13)</name>
  <files>test/onboarding-server.test.js</files>
  <action>
Extend `test/onboarding-server.test.js` with task-ID-labeled test cases that lock submit literacy response behavior for `ready`, `partial`, and `unconfigured`. Use mocked vector literacy probes and deterministic seed payloads. Name tests so they map directly to validation rows for `43-02-01`, `43-03-01`, and `43-03-03`.
  </action>
  <verify>
    <automated>node --test test/onboarding-server.test.js -x</automated>
  </verify>
  <done>
    Submit readiness state contracts exist and fail/pass deterministically under `node:test`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 43-01-02: Add status literacy block contract stubs (per LIT-14)</name>
  <files>test/onboarding-server.test.js</files>
  <action>
Add status endpoint contract coverage to assert `literacy.readiness`, `literacy.disciplines_available`, and `literacy.last_ingestion_at` shape and semantics. Include parity assertions that status and submit use the same readiness-state interpretation under identical mocked provider/literacy conditions.
  </action>
  <verify>
    <automated>node --test test/onboarding-server.test.js -x</automated>
  </verify>
  <done>
    Status literacy response shape and readiness parity are codified in runnable tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 43-01-03: Add activation telemetry contract stubs (per LIT-15)</name>
  <files>test/onboarding-server.test.js</files>
  <action>
Add telemetry assertions ensuring submit emits exactly one `literacy_activation_observed` event per successful submission with normalized payload fields: `readiness_status`, `disciplines_available`, `disciplines_missing`, `business_model`, and `pain_point_count`. Ensure tests assert no raw pain-point text is emitted.
  </action>
  <verify>
    <automated>node --test test/onboarding-server.test.js -x</automated>
  </verify>
  <done>
    Telemetry contract for literacy activation is locked before implementation changes.
  </done>
</task>

</tasks>

<verification>
Run `node --test test/onboarding-server.test.js -x` after each task and once at plan completion.
</verification>

<success_criteria>
Wave 0 establishes executable contracts for all Phase 43 behaviors with direct mapping to 43-VALIDATION task IDs.
</success_criteria>

<output>
After completion, create `.planning/phases/43-onboarding-to-literacy-activation-pipeline/43-01-SUMMARY.md`
</output>
