---
phase: 94-mir-and-msp-delta-patch-engine
plan: "03"
type: execute
wave: 3
depends_on:
  - "94-01"
  - "94-02"
files_modified:
  - onboarding/backend/research/preview-review-packager.cjs
  - onboarding/backend/research/cross-surface-preview-adapter.cjs
  - onboarding/backend/research/mir-msp-preview-entrypoint.cjs
  - test/phase-94/cross-surface-preview-envelope.test.js
  - test/phase-94/approval-boundary.test.js
  - test/phase-94/evidence-linking.test.js
autonomous: true
requirements:
  - DRT-07
  - DRT-10
  - DRT-16
must_haves:
  truths:
    - "operators can review before and after excerpts, inline evidence, contradictions, warnings, and trace metadata in one portable payload"
    - "the same preview semantics hold across MCP, API, CLI, and editor clients"
    - "review packaging remains non-mutating and never introduces accept, apply, or write logic"
    - "audit identifiers and route trace remain visible so every diff can be traced back to supporting evidence"
  artifacts:
    - path: onboarding/backend/research/preview-review-packager.cjs
      provides: operator-friendly packaging of diffs, evidence support, contradiction notes, and warning summaries
    - path: onboarding/backend/research/cross-surface-preview-adapter.cjs
      provides: one transport-safe rendering layer for MCP, API, CLI, and editor clients without semantic drift
    - path: onboarding/backend/research/mir-msp-preview-entrypoint.cjs
      provides: the final preview-only entrypoint that assembles the patch engine output for downstream review surfaces
    - path: test/phase-94/cross-surface-preview-envelope.test.js
      provides: proof that all client surfaces receive the same preview meaning and required fields
  key_links:
    - from: onboarding/backend/research/mir-msp-preview-entrypoint.cjs
      to: onboarding/backend/agents/orchestrator.cjs
      via: the preview entrypoint should sit between research context generation and later human approval surfaces, not replace either system
    - from: onboarding/backend/research/preview-review-packager.cjs
      to: onboarding/backend/handlers.cjs
      via: packaged previews must remain ready for operator review flows without adding accept or apply logic here
    - from: onboarding/backend/research/cross-surface-preview-adapter.cjs
      to: lib/markos/crm/execution.ts
      via: mirror the repo's preview and suggestion-only semantics so cross-surface transport stays consistent and safe
---

<objective>
Package the Phase 94 preview results for operator review and ensure one consistent preview contract is delivered across every MarkOS command surface.

Purpose: satisfy the cross-surface and trust requirements while keeping the entire phase preview-only, auditable, and ready for later human approval without merging review packaging into write execution.
Output: review packagers, transport-safe adapters, a final preview entrypoint, and parity tests across CLI, MCP, API, and editor workflows.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/94-mir-and-msp-delta-patch-engine/94-CONTEXT.md
@.planning/phases/94-mir-and-msp-delta-patch-engine/94-RESEARCH.md
@.planning/phases/94-mir-and-msp-delta-patch-engine/94-VALIDATION.md
@.planning/phases/94-mir-and-msp-delta-patch-engine/94-01-PLAN.md
@.planning/phases/94-mir-and-msp-delta-patch-engine/94-02-PLAN.md
@.planning/phases/93-multi-source-deep-research-orchestration/93-03-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@onboarding/backend/agents/orchestrator.cjs
@onboarding/backend/handlers.cjs
@lib/markos/crm/execution.ts

<interfaces>
Required Phase 94 output bundle:
- preview envelope with target, diff, evidence, contradictions, and warnings
- route_trace and provider_attempts from Phase 93 preserved for auditability
- surface-safe formatting hooks for MCP, API, CLI, and editor rendering
- review-only metadata that never implies acceptance or direct application
- an explicit read-only operator entry path through the existing review-facing handlers or wrappers
</interfaces>
</context>

<threat_model>
- T-94-07 (surface-specific contract drift): one client surface could start receiving different preview semantics or missing audit fields. Mitigation: centralize transport-safe packaging and prove parity with cross-surface tests.
- T-94-08 (mutation hidden in review packaging): packaging logic could start to apply or store changes instead of only preparing them for review. Mitigation: keep the final entrypoint read-only and assert approval or write boundaries remain untouched.
- T-94-09 (opaque review experience): previews could technically exist but still hide contradictions or source traceability from operators. Mitigation: require inline evidence, contradiction notes, and route trace visibility in packaged output.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create failing tests for cross-surface preview parity and non-mutating review packaging</name>
  <files>test/phase-94/cross-surface-preview-envelope.test.js, test/phase-94/approval-boundary.test.js, test/phase-94/evidence-linking.test.js</files>
  <behavior>
    - CLI, MCP, API, and editor clients all receive the same preview states and audit fields
    - packaged previews preserve inline evidence, contradictions, warnings, and route trace visibility
    - the final entrypoint never implies direct apply behavior or hidden persistence
  </behavior>
  <action>Write the tests first using representative preview fixtures from the earlier plans. Assert DRT-16 parity across all surfaces, preserve DRT-10 trust through visible evidence and contradiction support, and prove that the review packaging remains preview-only with no mutation path or approval bypass.</action>
  <verify>
    <automated>node --test test/phase-94/cross-surface-preview-envelope.test.js test/phase-94/approval-boundary.test.js test/phase-94/evidence-linking.test.js</automated>
  </verify>
  <done>The cross-surface review and audit contract is locked before the packaging layer is implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the review packager, surface adapter, and preview entrypoint</name>
  <files>onboarding/backend/research/preview-review-packager.cjs, onboarding/backend/research/cross-surface-preview-adapter.cjs, onboarding/backend/research/mir-msp-preview-entrypoint.cjs</files>
  <behavior>
    - review payloads present before/after excerpts, rationale, inline evidence, and contradictions in one operator-friendly shape
    - transport adapters preserve semantics across clients without duplicating business logic
    - the final entrypoint assembles the Phase 93 context pack and Phase 94 delta engine result into one auditable preview bundle
  </behavior>
  <action>Implement three pure CommonJS modules. In preview-review-packager.cjs, package the diff, evidence, warnings, and contradictions for operator review. In cross-surface-preview-adapter.cjs, normalize the transport-facing shape for MCP, API, CLI, and editor clients without changing meaning. In mir-msp-preview-entrypoint.cjs, assemble the final preview bundle from the upstream research pack and downstream delta engine while staying strictly read-only and review-safe. Expose that bundle through the existing review-facing handler or wrapper surfaces as a preview-only pathway, not a write or apply action.</action>
  <verify>
    <automated>node --test test/phase-94/cross-surface-preview-envelope.test.js test/phase-94/approval-boundary.test.js test/phase-94/evidence-linking.test.js test/phase-94/*.test.js</automated>
  </verify>
  <done>Operators and downstream clients receive one consistent, traceable patch preview bundle across all supported MarkOS surfaces.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full phase regression gate and confirm the preview entrypoint stays inside scope</name>
  <files>onboarding/backend/research/mir-msp-preview-entrypoint.cjs</files>
  <action>Execute the full Phase 94 regression slice, then the repository-wide test gate. Confirm the final preview entrypoint remains read-only, preserves approval boundaries, and emits route_trace, provider_attempts, evidence support, and contradiction visibility in the packaged output. Do not add accept, apply, or storage behavior in this phase.</action>
  <verify>
    <automated>node --test test/phase-94/*.test.js test/onboarding-approve-handler.test.js test/vault-writer.test.js test/write-mir.test.js && npm test</automated>
  </verify>
  <done>The completed Phase 94 surface is portable, review-safe, auditable, and free of hidden mutation behavior.</done>
</task>

</tasks>

<verification>
- node --test test/phase-94/cross-surface-preview-envelope.test.js test/phase-94/approval-boundary.test.js test/phase-94/evidence-linking.test.js
- node --test test/phase-94/*.test.js test/onboarding-approve-handler.test.js test/vault-writer.test.js test/write-mir.test.js
- npm test
</verification>

<success_criteria>
- Every supported client surface receives the same patch-preview semantics and audit metadata.
- Operators can clearly inspect before/after content, inline evidence, contradictions, and warnings.
- The preview entrypoint stays strictly read-only and does not merge into write or apply behavior.
- The full phase regression gate passes without breaking existing onboarding, approval, or vault flows.
</success_criteria>

<output>
After completion, create .planning/phases/94-mir-and-msp-delta-patch-engine/94-03-SUMMARY.md.
</output>
