---
phase: 42-secure-database-provisioning-flow
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/db-setup.test.js
  - test/migration-runner.test.js
  - test/rls-verifier.test.js
  - test/namespace-auditor.test.js
autonomous: true
requirements:
  - LIT-09
  - LIT-10
  - LIT-11
  - LIT-12

must_haves:
  truths:
    - "All Phase 42 behaviors are locked by executable test contracts before implementation starts."
    - "Validation row task IDs map one-to-one to test coverage entry points."
    - "Wave 1-4 implementation cannot proceed without a runnable Wave 0 test baseline."
  artifacts:
    - path: "test/db-setup.test.js"
      provides: "db:setup command contract and secret handling tests"
      exports: []
    - path: "test/migration-runner.test.js"
      provides: "migration ordering, tracking, and stop-on-failure tests"
      exports: []
    - path: "test/rls-verifier.test.js"
      provides: "RLS and anon denial verification tests"
      exports: []
    - path: "test/namespace-auditor.test.js"
      provides: "namespace isolation and standards namespace audit tests"
      exports: []
  key_links:
    - from: "test/db-setup.test.js"
      to: "bin/db-setup.cjs"
      via: "CLI setup flow contract"
      pattern: "db:setup|writeEnv|redact"
    - from: "test/migration-runner.test.js"
      to: "onboarding/backend/provisioning/migration-runner.cjs"
      via: "ordered apply + applied ledger"
      pattern: "markos_migrations|applyPendingMigrations"
    - from: "test/rls-verifier.test.js"
      to: "onboarding/backend/provisioning/rls-verifier.cjs"
      via: "table security checks"
      pattern: "verifyRlsPolicies|anon"
---

<objective>
Create Wave 0 Nyquist scaffolding for Phase 42 so every required behavior has an automated contract before runtime code is introduced.

Purpose: Enforce task-level validation continuity across Waves 1-4.
Output: Four runnable test files mapping directly to 42-VALIDATION rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-secure-database-provisioning-flow/42-RESEARCH.md
@.planning/phases/42-secure-database-provisioning-flow/42-VALIDATION.md
@test/setup.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 42-01-01: Create db setup contract tests (per LIT-09)</name>
  <files>test/db-setup.test.js</files>
  <action>
Create `test/db-setup.test.js` using `node:test` and `node:assert/strict` with named test cases that cover: interactive credential capture, safe `.env` persistence, secret redaction in terminal output, connectivity probe success/failure paths, and idempotent rerun behavior. Include explicit test names for `42-02-01`, `42-02-02`, `42-02-03`, and `42-02-04` to preserve Nyquist mapping.
  </action>
  <verify>
    <automated>node --test test/db-setup.test.js -x</automated>
  </verify>
  <done>
    `test/db-setup.test.js` exists and encodes all Wave 1 db:setup behavior contracts with runnable stubs/assertions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 42-01-02: Create migration runner contract tests (per LIT-10)</name>
  <files>test/migration-runner.test.js</files>
  <action>
Create `test/migration-runner.test.js` covering deterministic migration ordering, `markos_migrations` apply ledger writes, skip-already-applied behavior, and stop-on-first-failure reporting with failing migration file surfaced. Include explicit test names for `42-03-01`, `42-03-02`, and `42-03-03`.
  </action>
  <verify>
    <automated>node --test test/migration-runner.test.js -x</automated>
  </verify>
  <done>
    Migration test file exists and locks idempotent/non-destructive runner behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 42-01-03: Create RLS + namespace audit contract tests (per LIT-11, LIT-12)</name>
  <files>test/rls-verifier.test.js, test/namespace-auditor.test.js</files>
  <action>
Create `test/rls-verifier.test.js` for RLS enabled checks and anon-role denial assertions on literacy tables, and `test/namespace-auditor.test.js` for client namespace prefix isolation plus `markos-standards-*` invariants. Include explicit task-name mapping for `42-04-01`, `42-04-02`, and `42-04-03`.
  </action>
  <verify>
    <automated>node --test test/rls-verifier.test.js test/namespace-auditor.test.js -x</automated>
  </verify>
  <done>
    Security and isolation tests exist and execute under `node:test` as the Wave 0 gate.
  </done>
</task>

</tasks>

<verification>
Run:
- `node --test test/db-setup.test.js -x`
- `node --test test/migration-runner.test.js -x`
- `node --test test/rls-verifier.test.js -x`
- `node --test test/namespace-auditor.test.js -x`
</verification>

<success_criteria>
All required Phase 42 test contracts exist, execute, and map cleanly to the validation table before implementation tasks begin.
</success_criteria>

<output>
After completion, create `.planning/phases/42-secure-database-provisioning-flow/42-01-SUMMARY.md`
</output>
