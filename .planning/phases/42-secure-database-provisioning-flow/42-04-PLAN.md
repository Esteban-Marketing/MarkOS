---
phase: 42-secure-database-provisioning-flow
plan: 04
type: execute
wave: 3
depends_on:
  - 42-03
files_modified:
  - onboarding/backend/provisioning/rls-verifier.cjs
  - onboarding/backend/provisioning/namespace-auditor.cjs
  - test/rls-verifier.test.js
  - test/namespace-auditor.test.js
  - bin/db-setup.cjs
autonomous: true
requirements:
  - LIT-11
  - LIT-12

must_haves:
  truths:
    - "Provisioning explicitly verifies literacy-table RLS and anon-role denial after migrations run."
    - "Namespace audit confirms client-slug isolation and standards namespace invariants."
    - "db:setup reports security/audit failures as actionable diagnostics rather than silent success."
  artifacts:
    - path: "onboarding/backend/provisioning/rls-verifier.cjs"
      provides: "RLS and anon access verification"
      exports: ["verifyRlsPolicies"]
    - path: "onboarding/backend/provisioning/namespace-auditor.cjs"
      provides: "namespace isolation checks and standards namespace audit"
      exports: ["auditNamespaces"]
    - path: "test/rls-verifier.test.js"
      provides: "Wave 3 RLS verification coverage"
      exports: []
    - path: "test/namespace-auditor.test.js"
      provides: "Wave 3 namespace audit coverage"
      exports: []
  key_links:
    - from: "bin/db-setup.cjs"
      to: "onboarding/backend/provisioning/rls-verifier.cjs"
      via: "post-migration security gate"
      pattern: "verifyRlsPolicies"
    - from: "bin/db-setup.cjs"
      to: "onboarding/backend/provisioning/namespace-auditor.cjs"
      via: "post-migration isolation audit"
      pattern: "auditNamespaces"
    - from: "onboarding/backend/provisioning/namespace-auditor.cjs"
      to: "onboarding/backend/vector-store-client.cjs"
      via: "standards namespace shape validation"
      pattern: "buildStandardsNamespaceName"
---

<objective>
Implement Wave 3 security verification gates (RLS + namespace isolation) and connect them to db setup execution.

Purpose: Convert provisioning from schema-only setup into a secure, auditable setup contract.
Output: Security verifier modules, passing unit tests, and db:setup integration for security gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-secure-database-provisioning-flow/42-RESEARCH.md
@.planning/phases/42-secure-database-provisioning-flow/42-VALIDATION.md
@onboarding/backend/vector-store-client.cjs
@test/rls-verifier.test.js
@test/namespace-auditor.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 42-04-01: Implement RLS policy verifier for literacy tables (per LIT-11)</name>
  <files>onboarding/backend/provisioning/rls-verifier.cjs, test/rls-verifier.test.js</files>
  <action>
Create `onboarding/backend/provisioning/rls-verifier.cjs` exporting `verifyRlsPolicies`. Verify target tables (`markos_literacy_chunks`, `markos_artifacts`, `markos_vector_index_config`) have RLS enabled and that anon-role read access is denied for protected operations. Return a structured report object usable by `db:setup` and tested through mocked Supabase responses.
  </action>
  <verify>
    <automated>node --test test/rls-verifier.test.js -x</automated>
  </verify>
  <done>
    RLS verifier exists and fails closed when required table/policy checks are not satisfied.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 42-04-02: Implement namespace isolation auditor (per LIT-12)</name>
  <files>onboarding/backend/provisioning/namespace-auditor.cjs, test/namespace-auditor.test.js</files>
  <action>
Create `onboarding/backend/provisioning/namespace-auditor.cjs` exporting `auditNamespaces` to validate client slug namespace prefixing and standards namespace shape (`markos-standards-*`). Reuse existing namespace helper contracts where possible so runtime and setup logic do not drift. Extend tests for positive and failure cases.
  </action>
  <verify>
    <automated>node --test test/namespace-auditor.test.js -x</automated>
  </verify>
  <done>
    Namespace isolation checks are explicit, deterministic, and covered by unit tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 42-04-03: Integrate security auditors into db:setup workflow (per LIT-12)</name>
  <files>bin/db-setup.cjs, onboarding/backend/provisioning/rls-verifier.cjs, onboarding/backend/provisioning/namespace-auditor.cjs, test/db-setup.test.js</files>
  <action>
Update `bin/db-setup.cjs` to invoke `verifyRlsPolicies` and `auditNamespaces` after migration completion and before final success output. Fail setup when security verifications fail, with structured actionable messages. Extend `test/db-setup.test.js` integration assertions so setup report includes audit outcomes.
  </action>
  <verify>
    <automated>node --test test/db-setup.test.js -x</automated>
  </verify>
  <done>
    db setup command enforces Wave 3 security gates and surfaces clear failure diagnostics.
  </done>
</task>

</tasks>

<verification>
Run:
- `node --test test/rls-verifier.test.js -x`
- `node --test test/namespace-auditor.test.js -x`
- `node --test test/db-setup.test.js -x`
</verification>

<success_criteria>
Phase 42 setup proves security posture (RLS and namespace isolation) before reporting success.
</success_criteria>

<output>
After completion, create `.planning/phases/42-secure-database-provisioning-flow/42-04-SUMMARY.md`
</output>
