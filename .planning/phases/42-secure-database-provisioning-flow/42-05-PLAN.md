---
phase: 42-secure-database-provisioning-flow
plan: 05
type: execute
wave: 4
depends_on:
  - 42-04
files_modified:
  - bin/db-setup.cjs
  - onboarding/backend/vector-store-client.cjs
  - .planning/codebase/LITERACY-OPERATIONS.md
  - README.md
  - test/db-setup.test.js
  - test/migration-runner.test.js
autonomous: true
requirements:
  - LIT-09
  - LIT-10
  - LIT-11
  - LIT-12

must_haves:
  truths:
    - "Setup execution emits a structured health snapshot that confirms both providers and security gates."
    - "Operational documentation describes setup, rerun safety, and troubleshooting paths."
    - "Phase 42 regression checks prove full flow readiness across setup, migrations, and security verifiers."
  artifacts:
    - path: "bin/db-setup.cjs"
      provides: "final orchestration and structured setup report output"
      exports: ["runDbSetup"]
    - path: ".planning/codebase/LITERACY-OPERATIONS.md"
      provides: "operator runbook for db setup + literacy readiness"
      exports: []
    - path: "README.md"
      provides: "public command reference for `npx markos db:setup`"
      exports: []
    - path: "test/db-setup.test.js"
      provides: "integration and e2e-style setup assertions"
      exports: []
  key_links:
    - from: "bin/db-setup.cjs"
      to: "onboarding/backend/vector-store-client.cjs"
      via: "health snapshot contract"
      pattern: "healthCheck"
    - from: "README.md"
      to: "bin/install.cjs"
      via: "documented CLI command surface"
      pattern: "npx markos db:setup"
    - from: ".planning/codebase/LITERACY-OPERATIONS.md"
      to: "supabase/migrations"
      via: "operator migration and rerun guidance"
      pattern: "markos_migrations|idempotent"
---

<objective>
Finalize Wave 4 integration, reporting, and docs for the secure database provisioning flow.

Purpose: Deliver an operator-ready, verifiable Phase 42 system with full command-to-documentation parity.
Output: Structured health snapshot output, updated ops docs, and full validation command coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-secure-database-provisioning-flow/42-RESEARCH.md
@.planning/phases/42-secure-database-provisioning-flow/42-VALIDATION.md
@.planning/codebase/TECH-MAP.md
@README.md
@bin/db-setup.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 42-05-01: Emit consolidated provisioning health snapshot (per LIT-12)</name>
  <files>bin/db-setup.cjs, onboarding/backend/vector-store-client.cjs, test/db-setup.test.js</files>
  <action>
Extend `runDbSetup` to return/print a structured snapshot containing provider reachability, migration summary, RLS verification summary, and namespace audit summary. Reuse existing vector-store health helpers where possible. Ensure snapshot output can be consumed by operators and CI diagnostics without exposing secrets.
  </action>
  <verify>
    <automated>node --test test/**/*.test.js</automated>
  </verify>
  <done>
    Setup command outputs a deterministic, structured post-provision health report with no secret leakage.
  </done>
</task>

<task type="auto">
  <name>Task 42-05-02: Document db setup runbook and command usage (per LIT-09)</name>
  <files>.planning/codebase/LITERACY-OPERATIONS.md, README.md</files>
  <action>
Update `.planning/codebase/LITERACY-OPERATIONS.md` with a dedicated Phase 42 section covering command usage, required env vars, fail-fast migration behavior, RLS/namespace checks, and rerun safety. Update `README.md` command references to include `npx markos db:setup` with concise prerequisites and troubleshooting pointers.
  </action>
  <verify>
    <automated>node --test test/db-setup.test.js -x</automated>
  </verify>
  <done>
    Operator documentation and top-level usage docs are aligned with implemented db setup behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 42-05-03: Execute final regression and phase verification gate (per LIT-10)</name>
  <files>test/db-setup.test.js, test/migration-runner.test.js</files>
  <action>
Run full Phase 42 regression by validating db setup, migration runner, RLS verifier, and namespace auditor coverage as a complete chain. Capture outcomes in phase summary with task-ID references to 42-VALIDATION rows.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    Full project suite passes and Phase 42 is verifiably ready for `/gsd:execute-phase`.
  </done>
</task>

</tasks>

<verification>
Run:
- `node --test test/db-setup.test.js -x`
- `node --test test/migration-runner.test.js -x`
- `node --test test/rls-verifier.test.js -x`
- `node --test test/namespace-auditor.test.js -x`
- `node --test test/**/*.test.js`
- `npm test`
</verification>

<success_criteria>
Phase 42 provisioning is secure, idempotent, documented, and validated end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/42-secure-database-provisioning-flow/42-05-SUMMARY.md`
</output>
