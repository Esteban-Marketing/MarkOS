---
phase: 42-secure-database-provisioning-flow
plan: 03
type: execute
wave: 2
depends_on:
  - 42-02
files_modified:
  - onboarding/backend/provisioning/migration-runner.cjs
  - supabase/migrations/42_markos_migrations.sql
  - test/migration-runner.test.js
  - bin/db-setup.cjs
autonomous: true
requirements:
  - LIT-10

must_haves:
  truths:
    - "Migration execution is deterministic, tracked, idempotent, and non-destructive."
    - "Rerunning provisioning skips already-applied files and reports clean status."
    - "Any failing migration stops execution immediately with precise file-level error output."
  artifacts:
    - path: "onboarding/backend/provisioning/migration-runner.cjs"
      provides: "ordered migration application and ledger management"
      exports: ["applyPendingMigrations"]
    - path: "supabase/migrations/42_markos_migrations.sql"
      provides: "`markos_migrations` tracking table"
      exports: []
    - path: "test/migration-runner.test.js"
      provides: "Wave 2 migration behavior verification"
      exports: []
  key_links:
    - from: "onboarding/backend/provisioning/migration-runner.cjs"
      to: "supabase/migrations"
      via: "ordered SQL file discovery and apply"
      pattern: "*.sql|sort"
    - from: "onboarding/backend/provisioning/migration-runner.cjs"
      to: "markos_migrations"
      via: "ledger insert/checksum and skip logic"
      pattern: "filename|checksum|applied_at"
    - from: "bin/db-setup.cjs"
      to: "onboarding/backend/provisioning/migration-runner.cjs"
      via: "post-validation migration execution"
      pattern: "applyPendingMigrations"
---

<objective>
Implement the Wave 2 migration engine so Phase 42 setup can safely provision schema with explicit apply tracking.

Purpose: Guarantee non-destructive idempotent schema setup before security verification.
Output: New migration runner, tracking-table migration, and green migration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-secure-database-provisioning-flow/42-RESEARCH.md
@.planning/phases/42-secure-database-provisioning-flow/42-VALIDATION.md
@supabase/migrations
@test/migration-runner.test.js
@bin/db-setup.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 42-03-01: Implement migration-runner core with deterministic ordering (per LIT-10)</name>
  <files>onboarding/backend/provisioning/migration-runner.cjs, test/migration-runner.test.js</files>
  <action>
Create `onboarding/backend/provisioning/migration-runner.cjs` exporting `applyPendingMigrations`. Implement lexical ordering of `supabase/migrations/*.sql`, controlled execution through Supabase SQL client calls, and non-destructive behavior (no drop/truncate operations introduced by this runner). Ensure test coverage asserts ordering and apply flow.
  </action>
  <verify>
    <automated>node --test test/migration-runner.test.js -x</automated>
  </verify>
  <done>
    Runner applies migrations in stable order and tests validate deterministic execution.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 42-03-02: Add `markos_migrations` ledger and skip semantics (per LIT-10)</name>
  <files>supabase/migrations/42_markos_migrations.sql, onboarding/backend/provisioning/migration-runner.cjs, test/migration-runner.test.js</files>
  <action>
Add `supabase/migrations/42_markos_migrations.sql` to create tracking table (`filename`, `checksum`, `applied_at`) with `IF NOT EXISTS`. Update runner to write/read ledger entries and skip already applied migrations on rerun. Extend tests to assert skip behavior and no duplicate ledger writes.
  </action>
  <verify>
    <automated>node --test test/migration-runner.test.js -x</automated>
  </verify>
  <done>
    Tracking table exists and reruns skip previously applied migrations safely.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 42-03-03: Stop-on-failure reporting and db:setup integration point (per LIT-10)</name>
  <files>onboarding/backend/provisioning/migration-runner.cjs, bin/db-setup.cjs, test/migration-runner.test.js</files>
  <action>
Implement fail-fast behavior in `applyPendingMigrations` so setup halts on first failed SQL file and returns/throws structured details including failing migration filename and reason. Wire `bin/db-setup.cjs` to call the runner after credential validation and before security audits. Extend tests for failure-path assertions.
  </action>
  <verify>
    <automated>node --test test/migration-runner.test.js -x</automated>
  </verify>
  <done>
    Setup flow now has enforceable migration safety with actionable failure diagnostics.
  </done>
</task>

</tasks>

<verification>
Run `node --test test/migration-runner.test.js -x` and confirm ordered apply, skip-on-rerun, and stop-on-failure are all green.
</verification>

<success_criteria>
Phase 42 migration layer is idempotent, observable, and safe to rerun across environments.
</success_criteria>

<output>
After completion, create `.planning/phases/42-secure-database-provisioning-flow/42-03-SUMMARY.md`
</output>
