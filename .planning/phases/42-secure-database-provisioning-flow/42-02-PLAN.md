---
phase: 42-secure-database-provisioning-flow
plan: 02
type: execute
wave: 1
depends_on:
  - 42-01
files_modified:
  - bin/install.cjs
  - bin/db-setup.cjs
  - bin/cli-runtime.cjs
  - .gitignore
  - test/db-setup.test.js
autonomous: true
requirements:
  - LIT-09
  - LIT-12

must_haves:
  truths:
    - "Operators can run a first-class `npx markos db:setup` flow from the existing CLI entrypoint."
    - "Credentials are validated against live provider probes before being persisted."
    - "Secrets are written to `.env` only and never echoed in logs."
    - "Provisioning setup checks `.gitignore` protection for secret files."
  artifacts:
    - path: "bin/db-setup.cjs"
      provides: "guided setup command and provider probe orchestration"
      exports: ["runDbSetup"]
    - path: "bin/install.cjs"
      provides: "CLI routing for db:setup"
      exports: []
    - path: ".gitignore"
      provides: "enforced ignore of `.env` and setup artifacts"
      exports: []
    - path: "test/db-setup.test.js"
      provides: "Wave 1 command-level verification"
      exports: []
  key_links:
    - from: "bin/install.cjs"
      to: "bin/db-setup.cjs"
      via: "command dispatch"
      pattern: "db:setup"
    - from: "bin/db-setup.cjs"
      to: "onboarding/backend/vector-store-client.cjs"
      via: "health and connectivity probes"
      pattern: "healthCheck|__health__"
    - from: "bin/db-setup.cjs"
      to: ".env"
      via: "safe credential persistence"
      pattern: "SUPABASE_URL|UPSTASH_VECTOR_REST_TOKEN"
---

<objective>
Implement the Wave 1 setup command surface and credential-validation flow for secure database provisioning.

Purpose: Establish a safe, operator-friendly entrypoint before migration and security audit layers.
Output: Working CLI command path, secure `.env` write behavior, and passing db-setup tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-secure-database-provisioning-flow/42-RESEARCH.md
@.planning/phases/42-secure-database-provisioning-flow/42-VALIDATION.md
@bin/install.cjs
@bin/cli-runtime.cjs
@onboarding/backend/vector-store-client.cjs
@test/db-setup.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 42-02-01: Route `db:setup` through CLI command parser (per LIT-09)</name>
  <files>bin/install.cjs, bin/cli-runtime.cjs, test/db-setup.test.js</files>
  <action>
Add a `db:setup` command branch to the existing `markos` CLI flow in `bin/install.cjs`, preserving current install/update behavior. Use runtime helpers from `bin/cli-runtime.cjs` for environment loading and execution classification. Extend `test/db-setup.test.js` with command-routing assertions so `npx markos db:setup` is treated as canonical operator entrypoint.
  </action>
  <verify>
    <automated>node --test test/db-setup.test.js -x</automated>
  </verify>
  <done>
    Command routing invokes db setup logic without regressing install/update paths.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 42-02-02: Implement interactive credential wizard with redaction safety (per LIT-09)</name>
  <files>bin/db-setup.cjs, test/db-setup.test.js</files>
  <action>
Create `bin/db-setup.cjs` with an interactive wizard (Node `readline`) collecting `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `UPSTASH_VECTOR_REST_URL`, and `UPSTASH_VECTOR_REST_TOKEN`. Ensure sensitive values are never printed back to stdout/stderr; use redacted summaries only. Keep a testable `runDbSetup` function boundary for mocked IO in tests.
  </action>
  <verify>
    <automated>node --test test/db-setup.test.js -x</automated>
  </verify>
  <done>
    Wizard captures required credentials and enforces non-leaking terminal behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 42-02-03: Add provider connectivity probes before persistence (per LIT-12)</name>
  <files>bin/db-setup.cjs, .gitignore, test/db-setup.test.js</files>
  <action>
In `bin/db-setup.cjs`, add pre-persist probes: Supabase lightweight SQL reachability check (`SELECT 1`) and Upstash `__health__` namespace probe. Fail fast with actionable error output when either provider check fails; do not write `.env` on probe failure. On successful probes, persist credentials deterministically to `.env`, enforce `.gitignore` contains `.env`, and keep idempotent key-update behavior on reruns.
  </action>
  <verify>
    <automated>node --test test/db-setup.test.js -x</automated>
  </verify>
  <done>
    Setup only proceeds to persistence after both providers are reachable, with `.env` safety and `.gitignore` protection enforced.
  </done>
</task>

</tasks>

<verification>
Run `node --test test/db-setup.test.js -x` after each task and once at plan completion.
</verification>

<success_criteria>
`npx markos db:setup` is reachable, secure, and validated before any migration/security work in later waves.
</success_criteria>

<output>
After completion, create `.planning/phases/42-secure-database-provisioning-flow/42-02-SUMMARY.md`
</output>
