---
phase: 210
plan: "05"
type: execute
wave: 5
depends_on:
  - "210-01"
  - "210-02"
  - "210-03"
  - "210-04"
f_ids: []
migration_slots: []
integrates_with:
  - "RUN-01..08 from P207 (connector degradation must pause unsafe downstream work with real run linkage)"
  - "TASK-01..05 from P208 (recovery tasks and impacted-work visibility must route into the human operating interface as `RecoveryItem` rows in 208-05 Recovery Center)"
translation_gate_for: []
ui_spec_fold:
  spec: .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md
  spec_version: v1.0.0 (draft 2026-04-29)
  scope_classification: NO_UI
  ac_range:
    - "B-8 (210-05 ConnectorRecoveryTask failure_kind literals + status literals)"
  citations:
    - "210-UI-SPEC.md §Inheritance Bindings — From 210-05 ConnectorRecoveryTask (failure_kind drives install-failure copy variant on the 210-03 page)"
    - "210-UI-SPEC.md §Inheritance Bindings — From 208-UI-SPEC §Surface E — Recovery Center (parent surface; this phase dissolves the `connector_placeholder` family `<PlaceholderBanner variant=\"future_phase_210\">`)"
    - "210-UI-SPEC.md §Acceptance Criteria — B-8 (4 failure_kind literals + 4 status literals)"
    - "210-UI-SPEC.md §Surface A copy register — install-failure notice variants per failure_kind verbatim"
    - "210-UI-SPEC.md §Surface A state matrix — 4 install-failure rows mapped to 4 failure_kind literals"
    - "210-UI-SPEC.md §Translation gates dissolved by Phase 210 — 208-05 `connector_placeholder` family + 208-02 Morning Brief `future_recovery_placeholder`"
files_modified:
  - .planning/connectors/recovery-and-pause-policy.md
  - lib/markos/connectors/dependency-pause.ts
  - lib/markos/connectors/recovery-task.ts
  - lib/markos/connectors/impact-projection.ts
  - api/v1/connectors/recovery.js
  - test/connectors/phase-210/domain-5/pause-and-recovery.test.js
  - test/connectors/phase-210/domain-5/recovery-routing-208-05.test.js
autonomous: true
requirements:
  - CONN-04
  - QA-11
  - QA-12
  - QA-13
must_haves:
  truths:
    - "Degraded connector data cannot silently power customer-facing or decision-bearing work (architecture-lock forbidden string `silent connector degradation` enforced; UI-SPEC §Copywriting Contract)."
    - "Recovery tasks are precise, owner-visible, and linked to impacted agents and blocked runs; `recovery_task_id` rendered as `.c-chip-protocol` deep-link from 210-03 page to 208-05 Recovery Center (UI-SPEC §CR-15 + §Inheritance Bindings — From 208-UI-SPEC §Surface E)."
    - "Resolved connector state can resume work only after the unsafe pause is cleared explicitly; resume action binds to `connector.mutate` mutation-class root literal (UI-SPEC §Inheritance Bindings — From 206-UI-SPEC)."
    - "The 4 failure_kind literals (`revoked_token`, `missing_scope`, `rate_limited`, `stale_sync`) drive 4 distinct install-failure notice copy variants on the 210-03 page (UI-SPEC §Surface A copy register install-failure rows)."
    - "Phase 210 dissolves the 208-05 `<PlaceholderBanner variant=\"future_phase_210\">` for `connector_placeholder` family; install failures from the 210-03 page route into 208-05 Recovery Center as full-class `RecoveryItem` rows with `failure_family ∈ {provider_failure, run_blocker}` (UI-SPEC §Translation gates dissolved by Phase 210 row 1)."
    - "Phase 210 dissolves the 208-02 Morning Brief `future_recovery_placeholder` section; real connector recovery summaries replace placeholder copy (UI-SPEC §Translation gates dissolved by Phase 210 row 3)."
  artifacts:
    - path: .planning/connectors/recovery-and-pause-policy.md
      provides: canonical pause reasons, recovery-task shape, safe resume posture; 7-field ConnectorDependencyPause + 12-field ConnectorRecoveryTask + 4 failure_kind + 4 status literals
    - path: lib/markos/connectors/recovery-task.ts
      provides: typed module exporting failure_kind and status discriminated unions consumed by 210-03 install-failure state matrix
    - path: api/v1/connectors/recovery.js
      provides: serverless endpoint emitting JSON envelope (no HTML/JSX) — consumed by 210-03 page client subcomponent + 208-05 Recovery Center
  key_links:
    - from: .planning/phases/208-human-operating-interface/208-RESEARCH.md
      to: .planning/connectors/recovery-and-pause-policy.md
      via: connector recovery must land in real operator surfaces (208-05 Recovery Center) rather than private logs
      pattern: "Recovery Center"
    - from: lib/markos/connectors/recovery-task.ts
      to: app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.tsx
      via: 4 failure_kind literals drive 4 install-failure notice copy variants on the 210-03 page (UI-SPEC §Surface A copy register install-failure rows)
      pattern: "failure_kind"
    - from: lib/markos/connectors/recovery-task.ts
      to: app/(markos)/operations/recovery/page.tsx
      via: ConnectorRecoveryTask joins 208-05 RecoveryItem; dissolves `connector_placeholder` family placeholder (UI-SPEC §Translation gates dissolved by Phase 210 row 1)
      pattern: "RecoveryItem"
---

<objective>
Stop connector-dependent agents from acting on broken or stale data by making pause behavior, impacted-work linkage, and recovery tasks explicit. The 4 failure_kind literals from this contract drive the 4 install-failure copy variants on the 210-03 page. ConnectorRecoveryTask rows route into 208-05 Recovery Center as full-class `RecoveryItem` rows, dissolving the `connector_placeholder` family `<PlaceholderBanner variant="future_phase_210">`.
</objective>

<context>
@.planning/phases/210-connector-wow-loop-and-recovery/210-CONTEXT.md
@.planning/phases/210-connector-wow-loop-and-recovery/210-RESEARCH.md
@.planning/phases/210-connector-wow-loop-and-recovery/210-VALIDATION.md
@.planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md
</context>

<tasks>
<task id="210-05-01">
  <name>ConnectorDependencyPause + ConnectorRecoveryTask policy + dissolution of 208-05 connector_placeholder + 208-02 future_recovery_placeholder gates</name>
  <files>.planning/connectors/recovery-and-pause-policy.md, lib/markos/connectors/dependency-pause.ts, lib/markos/connectors/recovery-task.ts, lib/markos/connectors/impact-projection.ts, api/v1/connectors/recovery.js, test/connectors/phase-210/domain-5/pause-and-recovery.test.js, test/connectors/phase-210/domain-5/recovery-routing-208-05.test.js</files>
  <read_first>
    - .planning/phases/210-connector-wow-loop-and-recovery/210-RESEARCH.md section "Domain 5 - Dependent-agent pause and recovery tasks"
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Inheritance Bindings — From 210-05 ConnectorRecoveryTask (UI consumption: failure_kind → install-failure copy variant)
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Inheritance Bindings — From 208-UI-SPEC §Surface E — Recovery Center (parent surface routing)
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Acceptance Criteria — B-8 (4 failure_kind + 4 status literals)
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Surface A copy register — install-failure notice rows (4 variants per failure_kind verbatim)
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Translation gates dissolved by Phase 210 (3 gates dissolved: 208-05 connector_placeholder, 209-04 claim_type==competitive, 208-02 Morning Brief future_recovery_placeholder)
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Copywriting Contract — Architecture-lock forbidden string `silent connector degradation`
    - .planning/phases/208-human-operating-interface/208-RESEARCH.md
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §Surface E — Recovery Center (RecoveryItem.failure_family literals)
  </read_first>
  <action>
    Step 1: Create `.planning/connectors/recovery-and-pause-policy.md` with the exact 7 `ConnectorDependencyPause` fields verbatim: `pause_id`, `connector_install_id`, `agent_id`, `run_id`, `pause_reason`, `resumable`, and `created_at`. Document the safe-resume rule: a pause cannot transition to resumed unless the underlying ConnectorInstall.status returns to `connected` AND any associated `ConnectorRecoveryTask.status === 'resolved'`.
    Step 2: In the same policy doc and in `lib/markos/connectors/recovery-task.ts`, add the exact 12 `ConnectorRecoveryTask` fields verbatim: `recovery_task_id`, `connector_install_id`, `provider`, `failure_kind`, `missing_scope`, `impacted_agent_ids`, `blocked_run_ids`, `safe_retry_action`, `owner_role`, `status`, `created_at`, and `updated_at`.
    Step 3: Constrain `failure_kind` to the exact 4 literals from UI-SPEC §B-8: `revoked_token | missing_scope | rate_limited | stale_sync`. Each literal MUST map to a verbatim install-failure copy variant on the 210-03 page per UI-SPEC §Surface A copy register: `revoked_token` → `[err] Could not connect to {provider_display_name}. Token was revoked. Recovery task created — operator review required.`; `missing_scope` → `[err] Could not connect to {provider_display_name}. Required permission missing: {missing_scope}. Recovery task created — operator review required.`; `rate_limited` → `[warn] {provider_display_name} is rate-limited. We'll retry automatically. Recovery task created.`; `stale_sync` → `[warn] {provider_display_name} connected but data is stale. Recovery task created — operator review required.`.
    Step 4: Constrain `status` to the exact 4 literals from UI-SPEC §B-8: `open | in_progress | resolved | dismissed`. Document the transition rules: any new ConnectorRecoveryTask starts as `open`; operator pickup transitions to `in_progress`; successful re-authorization transitions to `resolved`; explicit operator dismissal transitions to `dismissed`.
    Step 5: Implement `lib/markos/connectors/dependency-pause.ts` with `createPause(connector_install_id, agent_id, run_id, pause_reason)` that returns the full ConnectorDependencyPause record + emits a 207 AgentRunEvent on the affected run. The architecture-lock forbidden string `silent connector degradation` MUST be rejected — every pause emits a visible audit-log entry (no silent state changes).
    Step 6: Implement `lib/markos/connectors/impact-projection.ts` with `projectImpact(connector_install_id)` that walks `dependent_agent_ids[]` from the ConnectorInstall (210-01) + the active runs of those agents to populate `impacted_agent_ids[]` and `blocked_run_ids[]` on the ConnectorRecoveryTask (per UI-SPEC §Acceptance Criteria pause_reason field consumption).
    Step 7: Implement `api/v1/connectors/recovery.js` as a Node serverless route module emitting JSON envelopes only (no HTML/JSX, per 207-UI-SPEC API handler posture). Endpoints: `GET /api/v1/connectors/recovery?connector_install_id=...` returns the active ConnectorRecoveryTask + ConnectorDependencyPause records; `POST /api/v1/connectors/recovery/{recovery_task_id}/resolve` transitions status from `open` or `in_progress` to `resolved` with an evidence-required guard from 209.
    Step 8: Document the 208-05 Recovery Center routing per UI-SPEC §Inheritance Bindings — From 208-UI-SPEC §Surface E: every ConnectorRecoveryTask MUST surface as a 208-05 RecoveryItem row with `failure_family ∈ {provider_failure, run_blocker}` (NOT `connector_placeholder` — that family is dissolved by 210). Sub-classification chip displays the 210-05 `failure_kind` literal verbatim.
    Step 9: Document dissolution of 3 translation gates per UI-SPEC §Translation gates dissolved by Phase 210: (a) 208-05 Recovery Center `connector_placeholder` family `<PlaceholderBanner variant="future_phase_210">` retired or backfilled; (b) 209-04 evidence-summary `claim_type == 'competitive'` placeholder dissolved (covered by 209 connector-derived evidence rows); (c) 208-02 Morning Brief `future_recovery_placeholder` section replaced with real connector recovery summaries.
    Step 10: Add `pause-and-recovery.test.js` covering pause creation, recovery-task creation per failure_kind, status transitions, and safe-resume gating. Add `recovery-routing-208-05.test.js` asserting that a ConnectorRecoveryTask creates a corresponding 208-05 RecoveryItem with `failure_family === 'provider_failure'` (or `run_blocker` for blocked-run cases) and that `failure_family === 'connector_placeholder'` is NOT used (placeholder dissolved).
  </action>
  <acceptance_criteria>
    - `.planning/connectors/recovery-and-pause-policy.md` contains the exact field names `pause_reason`, `missing_scope`, `impacted_agent_ids`, `blocked_run_ids`, `safe_retry_action` (UI-SPEC §Inheritance Bindings)
    - `lib/markos/connectors/recovery-task.ts` contains the exact literals `revoked_token`, `missing_scope`, `rate_limited`, `stale_sync`, `open`, `in_progress`, `resolved`, `dismissed` (UI-SPEC §B-8): `grep -c "revoked_token\\|missing_scope\\|rate_limited\\|stale_sync\\|open\\|in_progress\\|resolved\\|dismissed" lib/markos/connectors/recovery-task.ts` >= 8
    - The architecture-lock forbidden string `silent connector degradation` does NOT appear in any of the recovery files: `! grep -n "silent connector degradation" .planning/connectors/recovery-and-pause-policy.md lib/markos/connectors/dependency-pause.ts lib/markos/connectors/recovery-task.ts lib/markos/connectors/impact-projection.ts api/v1/connectors/recovery.js` returns 0
    - The recovery doctrine cites the 4 dissolved/preserved translation gates (UI-SPEC §Translation gates dissolved): `grep -c "connector_placeholder\\|future_recovery_placeholder\\|claim_type" .planning/connectors/recovery-and-pause-policy.md` >= 1
    - `test/connectors/phase-210/domain-5/pause-and-recovery.test.js` contains the exact strings `blocked_run_ids` and `safe_retry_action`
    - `test/connectors/phase-210/domain-5/pause-and-recovery.test.js` contains the exact strings `resolved` and `dismissed`
    - `test/connectors/phase-210/domain-5/recovery-routing-208-05.test.js` asserts that ConnectorRecoveryTask routes to 208-05 RecoveryItem with `failure_family ∈ {provider_failure, run_blocker}` (NOT `connector_placeholder`)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/connectors/phase-210/domain-5/</automated>
  </verify>
  <done>Connector degradation now creates explicit pause posture and precise recovery work that routes into 208-05 Recovery Center as full-class `RecoveryItem` rows; the 4 failure_kind literals drive the 4 install-failure copy variants on the 210-03 page verbatim; 3 translation gates are dissolved (208-05 connector_placeholder, 209-04 claim_type==competitive, 208-02 future_recovery_placeholder); architecture-lock forbidden string `silent connector degradation` provably absent from all recovery files.</done>
</task>
</tasks>

<verification>
- npm test -- test/connectors/phase-210/domain-5/
</verification>

<success_criteria>
- Pause and recovery objects have explicit fields and status vocabulary matching UI-SPEC §B-8
- Unsafe downstream work is blocked on degraded connectors; pauses emit audit-log entries (no silent state changes)
- 4 failure_kind literals drive 4 install-failure notice copy variants on the 210-03 page verbatim
- ConnectorRecoveryTask routes to 208-05 RecoveryItem with `failure_family ∈ {provider_failure, run_blocker}` (NOT `connector_placeholder`); the placeholder gate is dissolved
- 208-02 Morning Brief `future_recovery_placeholder` is dissolved; real connector recovery summaries replace placeholder copy
- 209-04 evidence-summary `claim_type == 'competitive'` placeholder is dissolved by connector-derived evidence rows from 210
- Architecture-lock forbidden string `silent connector degradation` is provably absent
</success_criteria>
