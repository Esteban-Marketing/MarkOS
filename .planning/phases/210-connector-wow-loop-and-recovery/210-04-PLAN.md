---
phase: 210
plan: "04"
type: execute
wave: 4
depends_on:
  - "210-01"
  - "210-02"
  - "210-03"
f_ids: []
migration_slots: []
integrates_with:
  - "TASK-01..05 from P208 (wow audit outputs must create tasks instead of passive charts)"
  - "EVD-01..06 from P209 (connector-backed audit findings should carry evidence references into later workflows)"
translation_gate_for: []
ui_spec_fold:
  spec: .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md
  spec_version: v1.0.0 (draft 2026-04-29)
  scope_classification: NO_UI
  ac_range:
    - "B-7 (210-04 audit_type literals + audit_status literals)"
  citations:
    - "210-UI-SPEC.md §Inheritance Bindings — From 210-04 ConnectorWowAudit (audit_status drives install-success notice timing per 210-03 Surface A copy register)"
    - "210-UI-SPEC.md §Acceptance Criteria — B-7 (3 audit_type literals + 4 audit_status literals)"
    - "210-UI-SPEC.md §Inheritance Bindings tenant-profile bundles table (audit_type binding: `saas` → `ga4_gsc_acquisition`, `services` → `crm_pipeline_freshness`, `agency` → both)"
    - "210-UI-SPEC.md §Surface A copy register — Install-success notice copy uses `time_to_wow_minutes` from this contract"
    - "210-UI-SPEC.md §DESIGN.md Compliance Assertions — `No write-capable first-wow path` (read-only enforcement)"
files_modified:
  - .planning/connectors/first-wow-audit-contract.md
  - onboarding/backend/connectors/wow-audit.cjs
  - lib/markos/connectors/wow-audit.ts
  - lib/markos/connectors/wow-routing.ts
  - test/connectors/phase-210/domain-4/wow-audit.test.js
  - test/connectors/phase-210/domain-4/no-write-on-first-wow.test.js
autonomous: true
requirements:
  - CONN-03
  - CONN-05
  - QA-09
  - QA-10
must_haves:
  truths:
    - "The first wow path is read-only and low-risk: zero install actions on the first-wow path mutate provider-side data (UI-SPEC §DESIGN.md Compliance Assertions; architecture-lock forbidden string `write-capable first wow` enforced)."
    - "Operators see a useful audit within the target window or a precise recovery task; `audit_status === 'recovery_needed'` routes to 210-05 (UI-SPEC §Inheritance Bindings — `audit_status` literals)."
    - "Audit findings create work or evidence instead of passive charts; `evidence_refs[]` populated on `ConnectorWowAudit` per UI-SPEC §Inheritance Bindings — From 209-UI-SPEC."
    - "`time_to_wow_minutes` from this contract drives the 210-03 install-success notice copy verbatim: `[ok] Connected. First audit ready in {time_to_wow_minutes} minutes.` (UI-SPEC §Surface A copy register)."
  artifacts:
    - path: .planning/connectors/first-wow-audit-contract.md
      provides: canonical first-wow audit shape, fallback paths, and timing posture; ConnectorWowAudit 12 fields + 3 audit_type literals + 4 audit_status literals
    - path: lib/markos/connectors/wow-audit.ts
      provides: typed module exporting audit_type and audit_status discriminated unions consumed by 210-03 Surface A install-success notice timing
  key_links:
    - from: .planning/codebase/INTEGRATIONS.md
      to: .planning/connectors/first-wow-audit-contract.md
      via: the current integration inventory should constrain which first-wave connectors are realistic
      pattern: "PostHog"
    - from: lib/markos/connectors/wow-audit.ts
      to: app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.tsx
      via: install-success notice copy `[ok] Connected. First audit ready in {time_to_wow_minutes} minutes.` consumes this contract's `time_to_wow_minutes` field (UI-SPEC §Surface A copy register install-success rows)
      pattern: "time_to_wow_minutes"
---

<objective>
Produce a useful audit from live data within the target activation window using a read-only first-wow path plus explicit fallback behavior. Honor the UI-SPEC §DESIGN.md Compliance Assertion `No write-capable first-wow path` — the architecture-lock forbidden string `write-capable first wow` is enforced both in the doctrine doc and in the typed module.
</objective>

<context>
@.planning/phases/210-connector-wow-loop-and-recovery/210-CONTEXT.md
@.planning/phases/210-connector-wow-loop-and-recovery/210-RESEARCH.md
@.planning/phases/210-connector-wow-loop-and-recovery/210-VALIDATION.md
@.planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md
</context>

<tasks>
<task id="210-04-01">
  <name>ConnectorWowAudit contract and fallback paths (read-only first-wow enforced)</name>
  <files>.planning/connectors/first-wow-audit-contract.md, onboarding/backend/connectors/wow-audit.cjs, lib/markos/connectors/wow-audit.ts, lib/markos/connectors/wow-routing.ts, test/connectors/phase-210/domain-4/wow-audit.test.js, test/connectors/phase-210/domain-4/no-write-on-first-wow.test.js</files>
  <read_first>
    - .planning/phases/210-connector-wow-loop-and-recovery/210-RESEARCH.md section "Domain 4 - First wow audit"
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Inheritance Bindings — From 210-04 ConnectorWowAudit (UI consumption: `audit_status` drives install-success vs recovery routing; `time_to_wow_minutes` drives copy)
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Acceptance Criteria — B-7 (3 audit_type literals + 4 audit_status literals)
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Inheritance Bindings — supported tenant-profile bundles (audit_type per profile: `saas` → `ga4_gsc_acquisition`, `services` → `crm_pipeline_freshness`)
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §DESIGN.md Compliance Assertions — `No write-capable first-wow path`
    - .planning/phases/208-human-operating-interface/208-RESEARCH.md
    - .planning/phases/209-evidence-research-and-claim-safety/209-RESEARCH.md
  </read_first>
  <action>
    Step 1: Create `.planning/connectors/first-wow-audit-contract.md` with the exact 12 `ConnectorWowAudit` fields verbatim per UI-SPEC §Inheritance Bindings — From 210-04: `wow_audit_id`, `tenant_id`, `connector_install_id`, `audit_type`, `audit_status`, `insight_count`, `recovery_task_id`, `evidence_refs`, `time_to_connect_minutes`, `time_to_wow_minutes`, `created_at`, `updated_at`.
    Step 2: In `lib/markos/connectors/wow-audit.ts`, constrain `audit_type` to the exact 3 literals from UI-SPEC §B-7: `ga4_gsc_acquisition | crm_pipeline_freshness | social_profile_audit`. Bind audit_type to tenant-profile per UI-SPEC §Inheritance Bindings tenant-profile bundles table: `saas` and `ecommerce` → `ga4_gsc_acquisition`; `services` and `agency` → `crm_pipeline_freshness`; the social/profile audit applies to LinkedIn-led tenants.
    Step 3: In `lib/markos/connectors/wow-audit.ts`, constrain `audit_status` to the exact 4 literals from UI-SPEC §B-7: `ready | no_data | stale_data | recovery_needed`. Each literal MUST be exported as a runtime constant (consumed by 210-03 page state matrix branches).
    Step 4: Document the read-only first-wow path in the contract doc — the audit MUST query provider-side READ APIs only; any write operation (provider-side mutation) on the first-wow path is forbidden. The architecture-lock forbidden string `write-capable first wow` MUST appear nowhere in the doctrine, the module, or test fixtures.
    Step 5: Implement `lib/markos/connectors/wow-routing.ts` with deterministic routing logic: `audit_status === 'ready'` → emit `evidence_refs[]` populated payload (consumed by 209 EvidenceMap.source_class connector-derived row); `audit_status === 'recovery_needed'` → create a 210-05 ConnectorRecoveryTask + return reference; `audit_status === 'no_data'` and `'stale_data'` → emit a fallback notice for the 210-03 page (renders as `.c-notice c-notice--info` per UI-SPEC §Surface A copy register pre-engine wow-promise placeholder).
    Step 6: Document the `time_to_connect_minutes` and `time_to_wow_minutes` numeric targets — `10` for `time_to_connect_minutes` (target operator-perceived activation window) and `30` for `time_to_wow_minutes` (target wow-audit-ready window). Per-provider time_to_wow values cited from UI-SPEC §Surface A wow-promise copy contract: `ga4` → 5 minutes, `gsc` → 3 minutes, `hubspot` → 5 minutes, `linkedin` → 4 minutes, `meta` → 4 minutes, `posthog` → 3 minutes — all within the 30-minute upper bound.
    Step 7: Add `wow-audit.test.js` covering: success (`audit_status === 'ready'`), no-data (`audit_status === 'no_data'`), stale-data (`audit_status === 'stale_data'`), and recovery-needed (`audit_status === 'recovery_needed'`) outcomes; assert `evidence_refs[]` populated on `ready`; assert `recovery_task_id` populated on `recovery_needed`.
    Step 8: Add `no-write-on-first-wow.test.js` (Nyquist gate for UI-SPEC §DESIGN.md Compliance Assertion) — assert that `wow-audit.cjs` only invokes provider READ methods; mock fixture asserting any mutation method call throws.
  </action>
  <acceptance_criteria>
    - `.planning/connectors/first-wow-audit-contract.md` contains the exact field names `audit_type`, `audit_status`, `time_to_connect_minutes`, `time_to_wow_minutes`, `recovery_task_id` (UI-SPEC §Inheritance Bindings)
    - `lib/markos/connectors/wow-audit.ts` contains the exact literals `ga4_gsc_acquisition`, `crm_pipeline_freshness`, `social_profile_audit`, `ready`, `no_data`, `stale_data`, `recovery_needed` (UI-SPEC §B-7): `grep -c "ga4_gsc_acquisition\\|crm_pipeline_freshness\\|social_profile_audit\\|ready\\|no_data\\|stale_data\\|recovery_needed" lib/markos/connectors/wow-audit.ts` >= 7
    - `.planning/connectors/first-wow-audit-contract.md` contains the exact numbers `10` and `30` for connect and wow targets
    - The architecture-lock forbidden string `write-capable first wow` does NOT appear in any of `.planning/connectors/first-wow-audit-contract.md`, `lib/markos/connectors/wow-audit.ts`, `lib/markos/connectors/wow-routing.ts`, `onboarding/backend/connectors/wow-audit.cjs` (UI-SPEC §DESIGN.md Compliance Assertions): `! grep -n "write-capable first wow" .planning/connectors/first-wow-audit-contract.md lib/markos/connectors/wow-audit.ts lib/markos/connectors/wow-routing.ts onboarding/backend/connectors/wow-audit.cjs` returns 0
    - `test/connectors/phase-210/domain-4/wow-audit.test.js` contains the exact strings `no_data` and `recovery_needed`
    - `test/connectors/phase-210/domain-4/no-write-on-first-wow.test.js` asserts that mutation methods on the first-wow path throw
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/connectors/phase-210/domain-4/</automated>
  </verify>
  <done>Phase 210 now defines a low-risk wow path that either creates useful insight quickly or fails into precise recovery work; `time_to_wow_minutes` directly drives 210-03 install-success notice copy; the architecture-lock forbidden string `write-capable first wow` is rejected by both the contract doc and a dedicated no-write test.</done>
</task>
</tasks>

<verification>
- npm test -- test/connectors/phase-210/domain-4/
</verification>

<success_criteria>
- ConnectorWowAudit has explicit 12-field shape with 3 audit_type + 4 audit_status literals matching UI-SPEC §B-7 verbatim
- First-wow behavior stays read-only and low-risk; the architecture-lock forbidden string `write-capable first wow` is provably absent from contract + module + test fixtures
- Audit findings create work or evidence instead of passive charts (`audit_status === 'ready'` populates `evidence_refs[]`; `recovery_needed` creates 210-05 ConnectorRecoveryTask)
- `time_to_wow_minutes` from this contract directly drives the 210-03 install-success notice copy without re-derivation
</success_criteria>
