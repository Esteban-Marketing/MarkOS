---
phase: 210
plan: "02"
type: execute
wave: 2
depends_on:
  - "210-01"
f_ids: []
migration_slots: []
integrates_with:
  - "COMP-01 from P206 (adapter choices must record privacy and retention posture, not just engineering convenience)"
translation_gate_for: []
ui_spec_fold:
  spec: .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md
  spec_version: v1.0.0 (draft 2026-04-29)
  scope_classification: NO_UI
  ac_range:
    - "B-5 (210-02 adapter-decision-matrix rows for 6 first-wave providers)"
    - "B-6 (210-02 preferred_adapter literals nango | direct | defer)"
  citations:
    - "210-UI-SPEC.md §Inheritance Bindings — From 210-02 adapter decision matrix (5-field consumption table)"
    - "210-UI-SPEC.md §Acceptance Criteria — B-5 (provider rows), B-6 (preferred_adapter literals)"
    - "210-UI-SPEC.md §Surface A copy register: per-card `pending-verification` notice when adapter `evidence_status == 'pending_verification'`"
files_modified:
  - .planning/connectors/adapter-decision-matrix.md
  - .planning/connectors/official-doc-evidence-log.md
  - lib/markos/connectors/adapter-matrix.ts
  - lib/markos/connectors/provider-capabilities.ts
  - scripts/connectors/check-official-doc-traceability.mjs
  - test/connectors/phase-210/domain-2/adapter-matrix.test.js
autonomous: true
requirements:
  - CONN-06
  - QA-05
  - QA-06
must_haves:
  truths:
    - "Adapter decisions are evidence-backed and reusable instead of one-off engineering guesses (UI-SPEC §B-5)."
    - "All 6 first-wave providers (`ga4`, `gsc`, `hubspot`, `linkedin`, `meta`, `posthog`) have explicit official-doc traceability and privacy notes (UI-SPEC §B-5)."
    - "Nango, direct, and defer are the only allowed `preferred_adapter` choices (UI-SPEC §B-6 + §Inheritance Bindings)."
    - "When `evidence_status == 'pending_verification'`, the 210-03 page renders a `[warn]` notice above the Connect CTA (UI-SPEC §Surface A copy register row 'Documentation pending notice'); the matrix MUST emit this status only when the official-doc reference is unverified."
  artifacts:
    - path: .planning/connectors/adapter-decision-matrix.md
      provides: canonical provider-by-provider adapter choice and rationale with rows for 6 first-wave providers and 14 named fields
    - path: .planning/connectors/official-doc-evidence-log.md
      provides: execution-time verification log for official provider docs
  key_links:
    - from: .planning/codebase/INTEGRATIONS.md
      to: .planning/connectors/adapter-decision-matrix.md
      via: existing integration inventory should seed the first-wave provider list
      pattern: "PostHog"
    - from: lib/markos/outbound/providers/base-adapter.ts
      to: lib/markos/connectors/provider-capabilities.ts
      via: connector adapters should follow capability and normalization patterns rather than provider sprawl
      pattern: "supports_replies"
    - from: lib/markos/connectors/adapter-matrix.ts
      to: app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.tsx
      via: the recommendation card consumes `evidence_status` to render the `pending-verification` notice (UI-SPEC §Surface A state matrix row 'pending-verification')
      pattern: "evidence_status"
---

<objective>
Choose connector implementation posture with explicit official-doc evidence so Phase 210 can decide where Nango fits, where direct adapters are justified, and what should be deferred. Bind the matrix's `evidence_status` field to the 210-03 page's `pending-verification` notice state per UI-SPEC §Surface A state matrix.
</objective>

<context>
@.planning/phases/210-connector-wow-loop-and-recovery/210-CONTEXT.md
@.planning/phases/210-connector-wow-loop-and-recovery/210-RESEARCH.md
@.planning/phases/210-connector-wow-loop-and-recovery/210-VALIDATION.md
@.planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md
</context>

<tasks>
<task id="210-02-01">
  <name>Adapter decision matrix and official-doc traceability</name>
  <files>.planning/connectors/adapter-decision-matrix.md, .planning/connectors/official-doc-evidence-log.md, lib/markos/connectors/adapter-matrix.ts, lib/markos/connectors/provider-capabilities.ts, scripts/connectors/check-official-doc-traceability.mjs, test/connectors/phase-210/domain-2/adapter-matrix.test.js</files>
  <read_first>
    - .planning/phases/210-connector-wow-loop-and-recovery/210-RESEARCH.md section "Domain 2 - Adapter decision matrix"
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Inheritance Bindings — From 210-02 adapter decision matrix (5-field UI consumption table: `provider`, `preferred_adapter`, `evidence_status`, `scope_requirements`, `data_class`)
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Acceptance Criteria — B-5 (6 provider rows verbatim), B-6 (3 preferred_adapter literals)
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Surface A copy register — "Documentation pending notice (per card when adapter `evidence_status == 'pending_verification'`)"
    - .planning/codebase/INTEGRATIONS.md
    - lib/markos/outbound/providers/base-adapter.ts (existing pattern: capability-flag posture)
  </read_first>
  <action>
    Step 1: Create `.planning/connectors/adapter-decision-matrix.md` with the exact 14 fields verbatim: `decision_record_id`, `provider`, `primary_use_case`, `preferred_adapter`, `auth_model`, `official_doc_refs`, `scope_requirements`, `rate_limit_notes`, `webhook_support`, `retention_notes`, `data_class`, `evidence_status`, `exceptions`, and `reviewed_at`.
    Step 2: Add explicit rows for the exact 6 first-wave providers per UI-SPEC §B-5: `ga4`, `gsc`, `hubspot`, `linkedin`, `meta`, and `posthog`. Each row's `scope_requirements` MUST include the raw scope identifiers cited in UI-SPEC §Surface A scope display copy register (e.g. `https://www.googleapis.com/auth/analytics.readonly` for `ga4`, `https://www.googleapis.com/auth/webmasters.readonly` for `gsc`, `crm.objects.contacts.read` and `crm.objects.deals.read` for `hubspot`, `r_liteprofile` for `linkedin`, `pages_read_engagement` for `meta`, `read:projects` for `posthog`).
    Step 3: In `lib/markos/connectors/adapter-matrix.ts`, constrain `preferred_adapter` to the exact 3 literals from UI-SPEC §B-6: `nango | direct | defer`. Constrain `evidence_status` to the exact 2 literals from UI-SPEC §Inheritance Bindings: `verified | pending_verification`.
    Step 4: Implement `lib/markos/connectors/provider-capabilities.ts` with capability flags per provider (e.g. `supports_webhooks`, `supports_incremental_sync`, `supports_backfill`) following the `base-adapter.ts` pattern.
    Step 5: Implement `scripts/connectors/check-official-doc-traceability.mjs` to verify that every row in the matrix has at least one `official_doc_refs` entry pointing to a public URL. Rows with zero refs MUST cause the script to exit non-zero.
    Step 6: Add `adapter-matrix.test.js` asserting (a) all 6 provider rows exist verbatim, (b) `preferred_adapter` is constrained to {`nango`, `direct`, `defer`}, (c) `evidence_status` is constrained to {`verified`, `pending_verification`}, (d) every row has an `official_doc_refs` entry.
  </action>
  <acceptance_criteria>
    - `.planning/connectors/adapter-decision-matrix.md` contains the exact field names `preferred_adapter`, `official_doc_refs`, `scope_requirements`, `retention_notes`, and `evidence_status` (UI-SPEC §Inheritance Bindings + §B-5)
    - `.planning/connectors/adapter-decision-matrix.md` contains rows for `ga4`, `gsc`, `hubspot`, `linkedin`, `meta`, and `posthog` (UI-SPEC §B-5): `grep -c "ga4\|gsc\|hubspot\|linkedin\|meta\|posthog" .planning/connectors/adapter-decision-matrix.md` >= 6
    - `lib/markos/connectors/adapter-matrix.ts` contains the exact literals `nango`, `direct`, and `defer` (UI-SPEC §B-6): `grep -c "nango\|direct\|defer" lib/markos/connectors/adapter-matrix.ts` >= 3
    - `lib/markos/connectors/adapter-matrix.ts` contains the exact literals `verified` and `pending_verification` (UI-SPEC §Inheritance Bindings): `grep -c "verified\|pending_verification" lib/markos/connectors/adapter-matrix.ts` >= 2
    - `test/connectors/phase-210/domain-2/adapter-matrix.test.js` contains the exact string `official_doc_refs`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/connectors/phase-210/domain-2/</automated>
  </verify>
  <done>Connector implementation posture is now explicit, evidence-backed, and reusable across later connector decisions; the matrix's `evidence_status` field directly drives the 210-03 page's pending-verification notice without re-derivation.</done>
</task>
</tasks>

<verification>
- npm test -- test/connectors/phase-210/domain-2/
</verification>

<success_criteria>
- Adapter choices are explicit and evidence-backed
- All 6 first-wave providers (`ga4`, `gsc`, `hubspot`, `linkedin`, `meta`, `posthog`) have official-doc traceability with the raw scope identifiers cited in UI-SPEC §Surface A scope display copy register
- `preferred_adapter` constrained to 3 literals; `evidence_status` constrained to 2 literals — both used by the 210-03 page state matrix
</success_criteria>
