---
phase: 210
plan: "03"
type: execute
wave: 3
depends_on:
  - "210-01"
  - "210-02"
f_ids: []
migration_slots: []
integrates_with:
  - "TASK-01..05 from P208 (failed connector setup should create visible recovery work instead of dying inside onboarding)"
  - "COMP-01 from P206 (install action binds to `connector.mutate` mutation-class root literal)"
  - "RUN-01..08 from P207 (install creates a connector-install AgentRun; `RunApiEnvelope.run_id` rendered as `.c-chip-protocol`)"
  - "EVD-01..06 from P209 (install-success event populates `evidence_refs` on ConnectorInstall; connector-derived evidence dissolves the `future_phase_210` placeholder in 209-04)"
translation_gate_for:
  - "P211 — loop-dispatch substrate (`<PlaceholderBanner variant=\"future_phase_211\">` rendered for loop-dispatch-bound recommendations)"
  - "P212 — learning-fixture personalization (`<PlaceholderBanner variant=\"future_phase_212\">` rendered when tenant-profile resolution would leverage learning fixtures)"
ui_spec_fold:
  spec: .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md
  spec_version: v1.0.0 (draft 2026-04-29)
  scope_classification: IN_SCOPE (the only UI plan in Phase 210)
  ac_range:
    - "CR-1..CR-22 (22 surface ACs — all on the 210-03 page + ConnectorRecommendationCard subcomponent + recommendations.cjs / recommendation-copy.cjs assemblers)"
    - "X-1..X-10 (10 cross-cutting carry-forward ACs — D-08 token-only, D-09 mint-as-text, D-09b .c-notice mandatory, D-13 .c-card--feature reserved, D-14 no .c-table, D-15 selective extraction, touch-target 44px, banned-lexicon, no-emoji, prefers-reduced-motion)"
    - "B-6 (recommendation policy + wow-promise copy register + recovery-copy 3-clause + max-3 invariant on the data layer)"
  citations:
    - "210-UI-SPEC.md §Surface Inventory — Surface A — Onboarding Connector Recommendations"
    - "210-UI-SPEC.md §Inheritance Bindings — From 210-03 ConnectorRecommendationBundle (12 fields verbatim)"
    - "210-UI-SPEC.md §Inheritance Bindings — From 210-03 supported tenant-profile bundles (5 profiles + recommended-connectors triplets)"
    - "210-UI-SPEC.md §Surface A copy register (verbatim copy table)"
    - "210-UI-SPEC.md §Surface A wow-promise copy contract (6 providers verbatim)"
    - "210-UI-SPEC.md §Surface A recovery copy contract (3-clause: pause / Recovery Center / re-authorize)"
    - "210-UI-SPEC.md §Surface A scope display copy register (per-provider raw + human label)"
    - "210-UI-SPEC.md §Surface A state matrix (12 states)"
    - "210-UI-SPEC.md §Acceptance Criteria CR-1..CR-22 (all 22 ACs)"
    - "210-UI-SPEC.md §Acceptance Criteria X-1..X-10 (10 cross-cutting ACs)"
    - "210-UI-SPEC.md §Inheritance Bindings — From 213.4-VALIDATION.md (D-08, D-09, D-09b, D-13, D-14, D-15)"
    - "DESIGN.md v1.1.0 §Colors, §Typography, §Spacing scale, §Components, §Motion, §Accessibility"
files_modified:
  - .planning/connectors/onboarding-recommendation-policy.md
  - onboarding/backend/connectors/recommendations.cjs
  - onboarding/backend/connectors/recommendation-copy.cjs
  - app/(markos)/onboarding/connectors/page.tsx
  - app/(markos)/onboarding/connectors/page.module.css
  - app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.tsx
  - app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.stories.tsx
  - test/connectors/phase-210/domain-3/recommendation-logic.test.js
  - test/connectors/phase-210/domain-3/recommendation-card-render.test.js
autonomous: true
requirements:
  - CONN-02
  - QA-07
  - QA-08
must_haves:
  truths:
    - "Onboarding never recommends more than three connectors (UI-SPEC §CR-3 + §B-6 + max-3 invariant enforced both in `recommendations.cjs` constant `MAX_RECOMMENDED = 3` AND in render layer at most 3 cards mounted)."
    - "Every recommendation carries a concrete wow promise that names (a) the specific connector, (b) the specific time-to-connect numeral, (c) the specific time-to-wow numeral, (d) the specific named outcome (UI-SPEC §CR-10 + §CR-11 + §Surface A wow-promise copy contract — 6 providers enumerated verbatim)."
    - "Failed setup creates visible recovery work instead of disappearing into onboarding; the recovery_task_id is rendered as `.c-chip-protocol` deep-link to the 208-05 Recovery Center (UI-SPEC §CR-15 + §Surface A state matrix install-failure rows)."
    - "Recovery copy for every supported connector contains all three required clauses: `pause`, `Recovery Center`, and `re-authorize` (UI-SPEC §CR-12 + §Surface A recovery copy contract)."
    - "First-wow path is read-only and low-risk: zero install actions on this page mutate provider-side data; `.c-button--destructive` ZERO usage on first-install path (UI-SPEC §Destructive Actions + §DESIGN.md Compliance Assertions 'No write-capable first-wow path')."
    - "Page composes ONLY existing primitives from `styles/components.css`: `.c-card` (no `.c-card--feature` per D-13), `.c-button{--primary,--tertiary,--destructive}`, `.c-notice c-notice--{state}` (D-09b), `.c-status-dot--live`, `.c-chip-protocol`, `.c-code-inline`, `.c-chip--mint`, vanilla `<ul>` (D-14: no `.c-table`)."
    - "Token-only enforcement: every color via `var(--color-*)`; every spacing via `var(--space-*)`; zero inline hex literals (UI-SPEC §X-1 + §CR-1 + D-08 carry)."
    - "Mint-as-text doctrine: `[ok]` glyph color, `.c-chip-protocol` ID text, `.c-button--tertiary` text use `--color-primary-text`; mint never as fill on card backgrounds, page heading, or scope-list backgrounds (UI-SPEC §X-2 + D-09 carry)."
    - "Banned lexicon zero matches across `page.tsx`, `ConnectorRecommendationCard.tsx`, `recommendation-copy.cjs`, `recommendations.cjs`; no exclamation points; no emoji — bracketed glyphs only (UI-SPEC §CR-19 + §X-8 + §X-9 + CLAUDE.md)."
    - "Storybook ships ≥ 8 named state stories: `Default`, `InProgress`, `Success`, `FailureTokenRevoked`, `FailureMissingScope`, `RateLimited`, `PartialSuccess`, `PendingVerification` (UI-SPEC §CR-22 + §Storybook Coverage)."
    - "Forward translation gates `future_phase_211` (loop-dispatch) and `future_phase_212` (learning-fixture personalization) render conditionally; self-placeholder `future_phase_210` and `future_phase_209` are FORBIDDEN (UI-SPEC §CR-18)."
  artifacts:
    - path: .planning/connectors/onboarding-recommendation-policy.md
      provides: canonical max-3 recommendation logic by tenant profile and goal; cites every UI-SPEC §Surface A copy register entry verbatim
    - path: onboarding/backend/connectors/recommendations.cjs
      provides: data assembler emitting `ConnectorRecommendationBundle` with the constant `MAX_RECOMMENDED = 3` enforced at the data layer; emits a bundle for each of `saas`, `ecommerce`, `services`, `agency`, `low_data`
    - path: onboarding/backend/connectors/recommendation-copy.cjs
      provides: copy register for all 6 first-wave providers (`ga4`, `gsc`, `hubspot`, `linkedin`, `meta`, `posthog`) — wow-promise + setup-expectation + recovery-copy verbatim per UI-SPEC §Surface A wow-promise copy contract
    - path: app/(markos)/onboarding/connectors/page.tsx
      provides: server-component page route; renders max-3 `ConnectorRecommendationCard` peers; mounts state notices via `.c-notice` primitive; loads recommendations from `recommendations.cjs`; renders skip CTA `.c-button--tertiary` "Skip — start with read-only audit instead"
    - path: app/(markos)/onboarding/connectors/page.module.css
      provides: token-only CSS module composing `.c-card`, `.c-notice`, `.c-button*` primitives; includes `prefers-reduced-motion` block collapsing transitions to 0ms
    - path: app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.tsx
      provides: co-located client subcomponent (D-15 selective extraction; mirrors 213.2-04 ConsentCard pattern); renders 4 card sections (What you'll get / Setup time / Permissions / If something fails) + per-card primary CTA + 12 state branches per UI-SPEC §Surface A state matrix
    - path: app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.stories.tsx
      provides: CSF3 Storybook coverage with 8 named state stories
  key_links:
    - from: app/(markos)/oauth/consent/page.tsx
      to: app/(markos)/onboarding/connectors/page.tsx
      via: tenant-scoped consent + scope-display + CSRF + approve/deny pattern (existing migration anchor) — install action inherits without re-authoring (UI-SPEC §migration_anchor)
      pattern: "scope"
    - from: lib/markos/connectors/connector-install.ts
      to: app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.tsx
      via: 8 status literals + 4 auth_type literals consumed by the card's state matrix and per-CTA copy variant (UI-SPEC §Inheritance Bindings — From 210-01)
      pattern: "ConnectorInstall"
    - from: lib/markos/connectors/adapter-matrix.ts
      to: app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.tsx
      via: `evidence_status == 'pending_verification'` triggers `.c-notice c-notice--warning` "Documentation pending verification" above CTA (UI-SPEC §Surface A copy register)
      pattern: "evidence_status"
    - from: lib/markos/connectors/recovery-task.ts
      to: app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.tsx
      via: `failure_kind` literal drives the install-failure copy variant + `recovery_task_id` rendered as `.c-chip-protocol` deep-link (UI-SPEC §CR-15 + §Surface A state matrix install-failure rows)
      pattern: "failure_kind"
    - from: app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.tsx
      to: app/(markos)/operations/recovery/page.tsx
      via: install-failure notice deep-links to Recovery Center (208-05 parent surface) via `recovery_task_id` chip — dissolves 208-05 `<PlaceholderBanner variant="future_phase_210">` for connector_placeholder family (UI-SPEC §Inheritance Bindings — From 208-UI-SPEC §Surface E)
      pattern: "recovery_task_id"
---

<objective>
Recommend a small number of high-value connectors during onboarding with a clear wow promise, a setup expectation, and recovery work when setup fails. Ship the ONLY UI surface in Phase 210: `app/(markos)/onboarding/connectors/page.tsx` + co-located `ConnectorRecommendationCard.tsx`. Honor every UI-SPEC §Surface A acceptance criterion verbatim — token-only, mint-as-text, `.c-notice` mandatory, max-3 invariant, no `.c-card--feature`, no `.c-table`, no write-capable first-wow path, banned-lexicon zero matches, no emoji.
</objective>

<context>
@.planning/phases/210-connector-wow-loop-and-recovery/210-CONTEXT.md
@.planning/phases/210-connector-wow-loop-and-recovery/210-RESEARCH.md
@.planning/phases/210-connector-wow-loop-and-recovery/210-VALIDATION.md
@.planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md
@DESIGN.md
@CLAUDE.md
@app/(markos)/oauth/consent/page.tsx
@app/(markos)/oauth/consent/_components/ConsentCard.tsx
@styles/components.css
@app/tokens.css
</context>

<tasks>
<task id="210-03-01">
  <name>Connector recommendation backend: policy doc + recommendations.cjs (max-3) + recommendation-copy.cjs (6 providers verbatim)</name>
  <files>.planning/connectors/onboarding-recommendation-policy.md, onboarding/backend/connectors/recommendations.cjs, onboarding/backend/connectors/recommendation-copy.cjs, test/connectors/phase-210/domain-3/recommendation-logic.test.js</files>
  <read_first>
    - .planning/phases/210-connector-wow-loop-and-recovery/210-RESEARCH.md section "Domain 3 - Max-3 onboarding recommendation logic"
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Inheritance Bindings — From 210-03 ConnectorRecommendationBundle (12 fields)
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Inheritance Bindings — From 210-03 supported tenant-profile bundles (5 profiles + recommended-connectors triplets verbatim)
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Surface A wow-promise copy contract (6 providers verbatim with exact strings)
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Surface A recovery copy contract (3-clause: pause / Recovery Center / re-authorize)
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Surface A scope display copy register (per-provider raw + human label)
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Acceptance Criteria CR-3, CR-8, CR-9, CR-10, CR-11, CR-12, CR-19, CR-21, X-8
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Copywriting Contract — Architecture-lock forbidden strings (`recommend four connectors` is one of the 4)
    - CLAUDE.md (banned lexicon, no emoji, no exclamation points)
  </read_first>
  <action>
    Step 1: Create `.planning/connectors/onboarding-recommendation-policy.md` with the exact `ConnectorRecommendationBundle` 12 fields verbatim per UI-SPEC §Inheritance Bindings — From 210-03: `bundle_id`, `tenant_id`, `business_type`, `go_to_market_motion`, `primary_goal`, `recommended_connectors`, `wow_promise`, `setup_minutes_estimate`, `fallback_connector`, `failure_task_template`, `created_at`, `updated_at`. The doc must declare the literal "maximum 3 recommended connectors" and the architecture-lock forbidden string `recommend four connectors` so any future drift is rejected by `check-connector-architecture-lock.mjs`.
    Step 2: Implement `onboarding/backend/connectors/recommendations.cjs` with: (a) the literal `const MAX_RECOMMENDED = 3;` enforced as a hard cap (any policy that returns more than 3 throws); (b) a bundle for each of the exact 5 tenant-profile literals from UI-SPEC §Inheritance Bindings tenant-profile bundles table — `saas` returns `[ga4, gsc, posthog]`; `ecommerce` returns `[ga4, gsc, meta]`; `services` returns `[hubspot, gsc, linkedin]`; `agency` returns `[hubspot, ga4, linkedin]`; `low_data` returns `[]` with a non-null `fallback_connector` value; (c) every bundle's `recommended_connectors.length` MUST be enforced ≤ 3 by the constant.
    Step 3: Implement `onboarding/backend/connectors/recommendation-copy.cjs` as a copy register with the 6 providers verbatim per UI-SPEC §Surface A wow-promise copy contract. Each entry MUST include the exact wow_promise string from the table — for example `ga4` MUST emit verbatim: `[ok] Connect Google Analytics 4 in 90 seconds — first acquisition audit ready in 5 minutes.`, `setup_minutes_estimate` of `~90 seconds`, and recovery_copy verbatim: `If GA4 disconnects, runs that depend on acquisition data pause. A recovery task lands in Recovery Center — re-authorize to resume.`. Repeat verbatim for `gsc` (60 seconds → 3 minutes), `hubspot` (90 seconds → 5 minutes), `linkedin` (2 minutes → 4 minutes), `meta` (2 minutes → 4 minutes), `posthog` (60 seconds → 3 minutes).
    Step 4: Enforce specificity gate per UI-SPEC §CR-11: every wow_promise contains the literal "seconds" or "minutes" + zero generic strings (`Get insights`, `Unlock`, `Transform`, `Supercharge`). Recovery_copy 3-clause contract per UI-SPEC §CR-12: every entry contains all three substrings `pause`, `Recovery Center`, and `re-authorize`.
    Step 5: Banned-lexicon zero matches per UI-SPEC §X-8 + §CR-19 across both `recommendations.cjs` and `recommendation-copy.cjs`: zero `synergy|leverage|empower|unlock|transform|revolutionize|supercharge|holistic|seamless|cutting-edge|innovative|game-changer|next-generation|world-class|best-in-class|reimagine|disrupt|just[ ,]`. Zero exclamation points. Zero emoji (only bracketed glyphs `[ok]`, `[warn]`, `[err]`, `[info]` allowed).
    Step 6: Provider display-name register: `recommendation-copy.cjs` MUST export display names verbatim per UI-SPEC §Inheritance Bindings — From 210-01 (`provider` row): `ga4` → `Google Analytics 4`, `gsc` → `Google Search Console`, `hubspot` → `HubSpot CRM`, `linkedin` → `LinkedIn`, `meta` → `Meta Business`, `posthog` → `PostHog`.
    Step 7: Scope display register per UI-SPEC §Surface A scope display copy register: each provider exports an array of `{raw, human_label}` pairs — for example `ga4` exports `[{raw: 'https://www.googleapis.com/auth/analytics.readonly', human_label: 'Read your Analytics data'}]`; `gsc` exports `webmasters.readonly` + `Read your Search Console data`; `hubspot` exports `crm.objects.contacts.read` + `Read your CRM contacts` and `crm.objects.deals.read` + `Read your CRM deals`; `linkedin` exports `r_liteprofile` + `Read your LinkedIn profile`; `meta` exports `pages_read_engagement` + `Read your Meta page engagement metrics`; `posthog` exports `read:projects` + `Read your PostHog projects`.
    Step 8: Add `recommendation-logic.test.js` asserting (a) max-3 invariant via `MAX_RECOMMENDED === 3` constant; (b) all 5 tenant profiles emit bundles; (c) `low_data` profile has `recommended_connectors.length === 0` and non-null `fallback_connector`; (d) all 6 providers exist in copy register; (e) recovery_copy 3-clause contract holds for all 6 providers; (f) wow_promise specificity gate ("seconds"|"minutes") holds for all 6.
  </action>
  <acceptance_criteria>
    - `.planning/connectors/onboarding-recommendation-policy.md` contains the exact field names `recommended_connectors`, `wow_promise`, `setup_minutes_estimate`, `failure_task_template` (UI-SPEC §Inheritance Bindings)
    - `.planning/connectors/onboarding-recommendation-policy.md` contains the exact number `3` as the maximum connector count (UI-SPEC §CR-3 + §B-6)
    - `onboarding/backend/connectors/recommendations.cjs` declares `MAX_RECOMMENDED` constant (UI-SPEC §CR-3 max-3 invariant data-layer enforcement): `grep -c "MAX_RECOMMENDED\|MAX_CONNECTORS" onboarding/backend/connectors/recommendations.cjs` >= 1
    - `onboarding/backend/connectors/recommendations.cjs` contains the exact tenant-profile literals `saas`, `ecommerce`, `services`, `agency`, `low_data` (UI-SPEC §CR-8): `grep -c "saas\|ecommerce\|services\|agency\|low_data" onboarding/backend/connectors/recommendations.cjs` >= 5
    - `onboarding/backend/connectors/recommendations.cjs` exports `ConnectorRecommendationBundle` field names (UI-SPEC §CR-9): `grep -c "recommended_connectors\|wow_promise\|setup_minutes_estimate\|fallback_connector\|failure_task_template" onboarding/backend/connectors/recommendations.cjs` >= 5
    - `onboarding/backend/connectors/recommendation-copy.cjs` declares wow-promise copy for all 6 first-wave providers verbatim (UI-SPEC §CR-10): `grep -c "ga4\|gsc\|hubspot\|linkedin\|meta\|posthog" onboarding/backend/connectors/recommendation-copy.cjs` >= 6
    - `onboarding/backend/connectors/recommendation-copy.cjs` every wow-promise begins with `[ok]` glyph (UI-SPEC §CR-10): `grep -c "\\[ok\\]" onboarding/backend/connectors/recommendation-copy.cjs` >= 6
    - Specificity gate (UI-SPEC §CR-11): `grep -cE "(seconds|minutes)" onboarding/backend/connectors/recommendation-copy.cjs` >= 6; `! grep -nE "Get insights|Unlock|Transform|Supercharge" onboarding/backend/connectors/recommendation-copy.cjs` returns 0
    - Recovery 3-clause contract (UI-SPEC §CR-12): `grep -c "pause\|Recovery Center\|re-authorize" onboarding/backend/connectors/recommendation-copy.cjs` >= 3
    - Banned-lexicon zero matches (UI-SPEC §X-8 + §CR-19): `! grep -nE "synergy\|leverage\|empower\|unlock\|transform\|revolutionize\|supercharge\|holistic\|seamless\|cutting-edge\|innovative" onboarding/backend/connectors/recommendation-copy.cjs onboarding/backend/connectors/recommendations.cjs` returns 0
    - `test/connectors/phase-210/domain-3/recommendation-logic.test.js` contains tenant profiles + `wow_promise` (UI-SPEC §CR-21)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/connectors/phase-210/domain-3/recommendation-logic.test.js</automated>
  </verify>
  <done>Backend recommendation policy + copy register ship with max-3 invariant enforced at the data layer, all 6 first-wave providers' wow-promise / setup-expectation / recovery-copy registered verbatim, recovery 3-clause contract held, banned-lexicon zero matches.</done>
</task>

<task id="210-03-02" tdd="true">
  <name>UI render: page.tsx + ConnectorRecommendationCard.tsx + page.module.css + 8 Storybook state stories (token-only, .c-notice mandatory, max-3, no .c-card--feature, no .c-table, no emoji, banned-lexicon zero matches)</name>
  <files>app/(markos)/onboarding/connectors/page.tsx, app/(markos)/onboarding/connectors/page.module.css, app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.tsx, app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.stories.tsx, test/connectors/phase-210/domain-3/recommendation-card-render.test.js</files>
  <behavior>
    - Test 1: `<h1>` renders verbatim "Connect your data"; `.t-lead` descriptor renders below per UI-SPEC §CR-2
    - Test 2: At most 3 `<article class="c-card">` recommendation cards rendered for any bundle (UI-SPEC §CR-3 — render-layer max-3 enforcement); zero `.c-card--feature` (UI-SPEC §CR-4 + §X-4)
    - Test 3: Each card renders all 4 section eyebrows verbatim: "What you'll get", "Setup time", "Permissions", "If something fails" (UI-SPEC §CR-5)
    - Test 4: Each card primary CTA composes `.c-button c-button--primary` with literal "Connect " followed by provider display name (UI-SPEC §CR-6); page-level skip CTA composes `.c-button c-button--tertiary` with verbatim "Skip — start with read-only audit instead" (UI-SPEC §CR-7)
    - Test 5: Install-state notices compose `.c-notice c-notice--{info,warning,error,success}` per state matrix; bracketed glyphs `[ok]/[warn]/[err]/[info]` paired (UI-SPEC §CR-13 + §CR-14)
    - Test 6: Failure-recovery notice includes `.c-chip-protocol` with `recovery_task_id` + deep-link to Recovery Center (UI-SPEC §CR-15)
    - Test 7: Install-in-progress composes `.c-status-dot--live`; `prefers-reduced-motion` block in page.module.css (UI-SPEC §CR-16 + §X-10)
    - Test 8: Scopes rendered as vanilla `<ul>` + `<li>` with both human label (Inter) AND raw scope `.c-code-inline`; zero `.c-table` (UI-SPEC §CR-17 + §X-5)
    - Test 9: Forward translation gates `future_phase_211`/`future_phase_212` rendered conditionally; `future_phase_209`/`future_phase_210` ZERO usage (UI-SPEC §CR-18)
    - Test 10: Token-only — zero inline hex literals (UI-SPEC §CR-1 + §X-1); banned-lexicon zero matches (UI-SPEC §CR-19 + §X-8); zero emoji (UI-SPEC §X-9)
    - Test 11: Storybook ships 8 named stories: `Default`, `InProgress`, `Success`, `FailureTokenRevoked`, `FailureMissingScope`, `RateLimited`, `PartialSuccess`, `PendingVerification` (UI-SPEC §CR-22)
  </behavior>
  <read_first>
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Surface Inventory — Surface A — Onboarding Connector Recommendations (Files, Layout grid, Components used, Copy register, Wow-promise copy contract, State matrix, Accessibility focus order, ARIA contract, Motion, Mobile breakpoint behavior)
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Acceptance Criteria CR-1..CR-22 + X-1..X-10 verbatim
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Inheritance Bindings — From 213.4-VALIDATION.md (D-08, D-09, D-09b, D-13, D-14, D-15)
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Spacing Scale (every dimension cites a `--space-*` token; allowed exceptions enumerated)
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Typography (heading JetBrains Mono via `var(--font-mono)`; body Inter via `var(--font-sans)`; `.t-label-caps` for eyebrows)
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Color (composition 70-80% surface, 15-20% on-surface, 3-5% primary, 0-2% state)
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Destructive Actions (zero on first-install path; "Disconnect" + "Force-retry" only post-install)
    - DESIGN.md v1.1.0 §Components — `.c-card`, `.c-button{,--primary,--tertiary,--destructive}`, `.c-notice c-notice--{info,warning,success,error}`, `.c-chip{,--mint}`, `.c-chip-protocol`, `.c-status-dot--live`, `.c-code-inline`, `.c-modal`, `.c-backdrop`
    - DESIGN.md v1.1.0 §Colors — `--color-surface` `#0A0E14`, `--color-surface-raised` `#1A1F2A`, `--color-border` `#2D3441`, `--color-on-surface` `#E6EDF3`, `--color-on-surface-muted` `#7B8DA6`, `--color-primary` `#00D9A3`, `--color-primary-text`, `--color-error` `#F85149`, `--color-warning` `#FFB800`, `--color-success` `#3FB950`, `--color-info` `#58A6FF`
    - DESIGN.md v1.1.0 §Spacing — `--space-{xxs,xs,sm,md,lg,xl}` (2px/8px/16px/24px/32px/48px); `--w-prose: 720px`; `--h-control-touch: 44px`
    - DESIGN.md v1.1.0 §Motion — `prefers-reduced-motion` collapses to 0ms; kernel-pulse freezes at full opacity
    - app/(markos)/oauth/consent/page.tsx (existing pattern: tenant-scoped consent + scope-display + CSRF)
    - app/(markos)/oauth/consent/_components/ConsentCard.tsx (existing pattern: server-component page + co-located client subcomponent — 213.2-04 ConsentCard pattern)
    - styles/components.css (verify available primitive class names)
    - app/tokens.css (verify available CSS custom properties)
  </read_first>
  <action>
    Step 1: Create `app/(markos)/onboarding/connectors/page.tsx` as a server component. Page heading `<h1>` with verbatim text "Connect your data" inheriting `var(--font-mono)` + `var(--fs-h1)` (typography.h1 token). Page descriptor `<p class="t-lead">` with the literal copy register entry from UI-SPEC §Surface A: `[info] {N} connectors recommended for {tenant_profile} tenants. Each takes {min}–{max} minutes.`. Container constrains to `max-width: var(--w-prose)` (720px) per UI-SPEC §Spacing Scale exception 4. Page padding via `var(--space-lg)` desktop, `var(--space-md)` mobile.
    Step 2: Page imports `recommendations.cjs` server-side; renders at most 3 `ConnectorRecommendationCard` components — enforce max-3 at render layer via `bundle.recommended_connectors.slice(0, 3)` (UI-SPEC §CR-3 + §X-4). When `bundle.recommended_connectors.length === 0` (low_data profile), render `.c-notice c-notice--info` with copy register entry: `[info] We need a first-data event before recommendations can refine. {fallback hint}`.
    Step 3: Page renders skip CTA `<button class="c-button c-button--tertiary">` with verbatim text "Skip — start with read-only audit instead" (UI-SPEC §CR-7) — uses `.c-button--tertiary` (mint-as-text per D-09 carry); never `.c-button--destructive` on this page (UI-SPEC §Destructive Actions zero on first-install path).
    Step 4: Page conditionally renders `<PlaceholderBanner variant="future_phase_211">` ONLY when bundle would recommend a loop-dispatch-bound connector and 211 has not shipped; `<PlaceholderBanner variant="future_phase_212">` ONLY when tenant-profile resolution would leverage learning fixtures and 212 has not shipped (UI-SPEC §CR-18). `future_phase_209` and `future_phase_210` ZERO usage on this page (UI-SPEC §CR-18 self-placeholder forbidden).
    Step 5: Create `app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.tsx` as a co-located client subcomponent (D-15 selective extraction; mirrors `app/(markos)/oauth/consent/_components/ConsentCard.tsx`). Each card renders as `<article class="c-card" aria-labelledby="rec-{provider}-h2">` (UI-SPEC §CR-4 — never `.c-card--feature`).
    Step 6: Card renders `<h2 id="rec-{provider}-h2">` with the provider display name from `recommendation-copy.cjs` (e.g. "Google Analytics 4"). When `recommendation_source === 'tenant_profile_match'`, render `<span class="c-chip c-chip--mint">[ok] Recommended</span>` per UI-SPEC §Surface A copy register.
    Step 7: Card renders 4 sections in order with eyebrows as `<h3 class="t-label-caps">`: (a) "What you'll get" + body wow_promise from copy register; (b) "Setup time" + body setup_minutes_estimate; (c) "Permissions" + scope `<ul>` (each `<li>` contains Inter body human label + `.c-code-inline` raw scope); (d) "If something fails" + body recovery_copy. (UI-SPEC §CR-5 + §CR-17 + §X-5: vanilla `<ul>`, never `.c-table`).
    Step 8: Card primary CTA `<button class="c-button c-button--primary">` with copy variant by `auth_type` literal (UI-SPEC §Surface A copy register): `oauth` → "Connect {provider_display_name}"; `api_key` → "Add {provider_display_name} API key"; `service_account` → "Upload {provider_display_name} service account"; `webhook_secret` → "Configure {provider_display_name} webhook". CTA inherits `--h-control-touch` (44px) globally on `(pointer: coarse)` (UI-SPEC §X-7 + 213.2 cross-cutting fix).
    Step 9: Card renders 12 state branches per UI-SPEC §Surface A state matrix:
      - `loading` → `.c-notice c-notice--info` "[info] Loading recommendations…"
      - `error-fetch` → `.c-notice c-notice--error` page-top + retry CTA
      - `success-1/2/3` → 1, 2, or 3 cards rendered
      - `install-in-progress` → `.c-button.is-loading` + `.c-notice c-notice--info` "[info] Connecting to {provider_display_name}…" + `.c-status-dot--live` kernel-pulse with `aria-label="Connecting to {provider}"`
      - `install-success` → `.c-notice c-notice--success` "[ok] Connected. First audit ready in {time_to_wow_minutes} minutes." + `.c-chip-protocol` chip displaying `connector_install_id`
      - `install-failure` (token revoked) → `.c-notice c-notice--error` "[err] Could not connect to {provider_display_name}. Token was revoked. Recovery task created — operator review required." + `.c-chip-protocol` chip with `recovery_task_id` deep-linking to `app/(markos)/operations/recovery/page.tsx` (208-05)
      - `install-failure` (missing scope) → "[err] Could not connect to {provider}. Required permission missing: {missing_scope}. Recovery task created — operator review required."
      - `install-failure` (rate limited) → `.c-notice c-notice--warning` "[warn] {provider_display_name} is rate-limited. We'll retry automatically. Recovery task created."
      - `install-failure` (stale sync) → `.c-notice c-notice--warning` "[warn] {provider_display_name} connected but data is stale. Recovery task created — operator review required."
      - `install-partial-success` (degraded) → `.c-notice c-notice--warning` "[warn] {provider} connected with reduced scope. Some audit features will not be available."
      - `pending-verification` (when adapter `evidence_status === 'pending_verification'`) → `.c-notice c-notice--warning` above CTA "[warn] Documentation pending verification — connect at your own risk."
    Step 10: Create `app/(markos)/onboarding/connectors/page.module.css` token-only: every `padding`, `margin`, `gap`, `inset` cites `var(--space-*)`; every `color` cites `var(--color-*)`; every `font-size` and `font-weight` cites DESIGN.md `typography.*` token via the inherited primitives. Container: `max-width: var(--w-prose); padding-block: var(--space-xl); padding-inline: var(--space-lg);`. Card grid gap: `gap: var(--space-md)`. Mobile (`@media (max-width: 640px)`): `padding-inline: var(--space-md)`; cards stack with `gap: var(--space-md)`. `prefers-reduced-motion` block (UI-SPEC §CR-16 + §X-10): `@media (prefers-reduced-motion: reduce) { *, *::before, *::after { animation-duration: 0ms !important; transition-duration: 0ms !important; } }`. Zero inline hex literals (UI-SPEC §CR-1 + §X-1). Zero local `.banner`/`.alert`/`.warning`/`.recommendation-state`/`.installState` classes (UI-SPEC §X-3 — `.c-notice` mandatory).
    Step 11: Create `app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.stories.tsx` (CSF3) with exactly 8 named state stories per UI-SPEC §CR-22 + §Storybook Coverage: `Default`, `InProgress`, `Success`, `FailureTokenRevoked`, `FailureMissingScope`, `RateLimited`, `PartialSuccess`, `PendingVerification`. Each story mounts the card with the corresponding fixture from `recommendation-copy.cjs` + the corresponding `ConnectorInstall.status` literal.
    Step 12: ARIA contract per UI-SPEC §Surface A: `<main>` lands on the page heading; each recommendation card is `<section aria-labelledby="rec-{provider}-h2">`; install-state notices use `aria-live="polite"` for success/info and `aria-live="assertive"` for error/warning; `.c-status-dot--live` carries `aria-label="Connecting to {provider}"`. Bracketed glyph paired with state color — never color-only signal.
    Step 13: Banned-lexicon enforcement per UI-SPEC §CR-19 + §X-8: zero matches on the 17 banned words across `page.tsx`, `ConnectorRecommendationCard.tsx`. No exclamation points. No emoji per UI-SPEC §X-9 (only bracketed glyphs).
    Step 14: Add `recommendation-card-render.test.js` (Node `--test`) using `@testing-library/react` or jsdom snapshot to assert: max-3 cards render, all 4 eyebrows present, `.c-button--primary` + `.c-button--tertiary` composed, `.c-card--feature`/`.c-table`/local state classes ZERO, banned-lexicon zero matches via grep, 8 Storybook stories exist.
  </action>
  <acceptance_criteria>
    - CR-1 (token-only zero hex literals): `! grep -nE "#[0-9a-fA-F]{3,8}" app/(markos)/onboarding/connectors/page.module.css app/(markos)/onboarding/connectors/page.tsx app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.tsx` returns 0
    - CR-2 (page heading + .t-lead): `grep -c "Connect your data" app/(markos)/onboarding/connectors/page.tsx` >= 1; `grep -c "t-lead" app/(markos)/onboarding/connectors/page.tsx` >= 1
    - CR-3 (max-3 render layer): `grep -c "recommended_connectors" app/(markos)/onboarding/connectors/page.tsx` >= 1; `grep -c "length.*[<=]\\s*3\\|.slice(0,\\s*3)" app/(markos)/onboarding/connectors/page.tsx` >= 1
    - CR-4 (`.c-card` not feature): `grep -c "c-card[^-]" app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.tsx` >= 1; `grep -c "c-card--feature" app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.tsx app/(markos)/onboarding/connectors/page.tsx` returns 0
    - CR-5 (4 eyebrows verbatim): `grep -c "What you'll get\\|Setup time\\|Permissions\\|If something fails" app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.tsx` >= 4
    - CR-6 (`.c-button--primary` + "Connect" prefix): `grep -c "c-button--primary" app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.tsx` >= 1; `grep -c "Connect " app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.tsx` >= 1
    - CR-7 (skip CTA verbatim): `grep -c "c-button--tertiary" app/(markos)/onboarding/connectors/page.tsx` >= 1; `grep -c "Skip — start with read-only audit instead" app/(markos)/onboarding/connectors/page.tsx` >= 1
    - CR-13 (`.c-notice` per state): `grep -c "c-notice--info\\|c-notice--warning\\|c-notice--error\\|c-notice--success" app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.tsx` >= 4
    - CR-14 (bracketed glyphs + no emoji): `grep -c "\\[ok\\]\\|\\[warn\\]\\|\\[err\\]\\|\\[info\\]" app/(markos)/onboarding/connectors/page.tsx app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.tsx` >= 6
    - CR-15 (`.c-chip-protocol` recovery_task_id deep-link): `grep -c "c-chip-protocol\\|recovery_task_id" app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.tsx` >= 2
    - CR-16 (`.c-status-dot--live` + prefers-reduced-motion): `grep -c "c-status-dot--live" app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.tsx` >= 1; `grep -c "prefers-reduced-motion" app/(markos)/onboarding/connectors/page.module.css` >= 1
    - CR-17 (vanilla `<ul>` + `.c-code-inline`, no `.c-table`): `grep -c "c-code-inline" app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.tsx` >= 1; `grep -c "<ul\\|<li" app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.tsx` >= 2; `grep -c "c-table" app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.tsx app/(markos)/onboarding/connectors/page.tsx` returns 0
    - CR-18 (no self-placeholder): `grep -c "future_phase_210\\|future_phase_209" app/(markos)/onboarding/connectors/page.tsx` returns 0
    - CR-19 (banned-lexicon zero matches): `! grep -nE "synergy\\|leverage\\|empower\\|unlock\\|transform\\|revolutionize\\|supercharge\\|holistic\\|seamless\\|cutting-edge\\|innovative" app/(markos)/onboarding/connectors/page.tsx app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.tsx` returns 0
    - CR-22 (8 Storybook stories): `grep -c "Default\\|InProgress\\|Success\\|FailureTokenRevoked\\|FailureMissingScope\\|RateLimited\\|PartialSuccess\\|PendingVerification" app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.stories.tsx` >= 8
    - X-2 (mint never as fill): `! grep -nE "background:[[:space:]]+var\\(--color-primary\\)[^-]" app/(markos)/onboarding/connectors/page.module.css app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.tsx` returns 0
    - X-3 (no local state classes): `! grep -nE "className=\"(banner\\|alert\\|warning\\|recommendation-state\\|installState)" app/(markos)/onboarding/connectors/page.tsx app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.tsx` returns 0
    - X-6 (D-15 co-located, not extracted): `! find components/markos/onboarding -name "ConnectorRecommendationCard*" 2>/dev/null` returns nothing
    - X-9 (no emoji): `! grep -nE "[\\xF0-\\xF7][\\x80-\\xBF]{3}" app/(markos)/onboarding/connectors/page.tsx app/(markos)/onboarding/connectors/_components/ConnectorRecommendationCard.tsx` returns 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/connectors/phase-210/domain-3/recommendation-card-render.test.js && npx @google/design.md lint DESIGN.md</automated>
  </verify>
  <done>The single Phase 210 UI surface ships: server-component page + co-located client subcomponent + token-only CSS module + 8 Storybook state stories. Every UI-SPEC AC (CR-1..CR-22 + X-1..X-10) verifies via grep. Recovery routing dissolves the 208-05 `connector_placeholder` placeholder; pending-verification adapter status surfaces correctly; max-3 invariant enforced both at data layer and render layer; first-install path is read-only with zero `.c-button--destructive` usage.</done>
</task>
</tasks>

<verification>
- npm test -- test/connectors/phase-210/domain-3/
- npx @google/design.md lint DESIGN.md
- npm run storybook (manual: 8 ConnectorRecommendationCard stories render at iPhone 14 Pro emulation without horizontal scroll)
</verification>

<success_criteria>
- Recommendation bundles never exceed three connectors (data-layer constant + render-layer slice; UI-SPEC §CR-3)
- Every recommendation has a concrete wow promise verbatim per UI-SPEC §Surface A wow-promise copy contract for all 6 first-wave providers
- Failed setup produces visible recovery work that deep-links to 208-05 Recovery Center via `recovery_task_id` chip (UI-SPEC §CR-15)
- Recovery copy 3-clause contract held verbatim across all 6 providers (UI-SPEC §CR-12)
- The 210-03 page composes only existing primitives from `styles/components.css`; zero `.c-card--feature`; zero `.c-table`; zero local state classes; zero inline hex literals; zero emoji; zero banned-lexicon matches; zero exclamation points
- 8 named Storybook state stories ship per UI-SPEC §CR-22
- First-install path is read-only with zero `.c-button--destructive` usage (UI-SPEC §Destructive Actions)
- Forward translation gates `future_phase_211` and `future_phase_212` rendered conditionally; self-placeholder gates (`future_phase_209`, `future_phase_210`) ZERO usage
</success_criteria>
