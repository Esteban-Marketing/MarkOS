---
phase: 210
plan: "01"
type: execute
wave: 1
depends_on: []
f_ids: []
migration_slots: []
integrates_with:
  - "COMP-01 from P206 (connector privacy, consent, and retention posture must exist before live-data trust is granted)"
  - "RUN-01..08 from P207 (dependent-run linkage must be explicit, not implied)"
  - "TASK-01..05 from P208 (recovery and blocked-work routing must target real operator surfaces)"
  - "EVD-01..06 from P209 (connector outputs need evidence posture for downstream audits and narratives)"
translation_gate_for: []
ui_spec_fold:
  spec: .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md
  spec_version: v1.0.0 (draft 2026-04-29)
  scope_classification: NO_UI
  ac_range:
    - "B-1 (210-01 ConnectorInstall contract field set)"
    - "B-2 (210-01 status literals)"
    - "B-3 (210-01 auth_type literals)"
    - "B-4 (210-01 architecture-lock forbidden strings including `write-capable first wow`)"
    - "B-11 (210-01 Wave 0.5 preflight readiness rows for 206/207/208/209)"
  citations:
    - "210-UI-SPEC.md §Inheritance Bindings — From 210-01 ConnectorInstall contract (table maps every ConnectorInstall field to UI consumption)"
    - "210-UI-SPEC.md §Acceptance Criteria — B-1, B-2, B-3, B-4, B-11"
    - "210-UI-SPEC.md §DESIGN.md Compliance Assertions — Architecture-lock forbidden strings"
    - "210-UI-SPEC.md §Copywriting Contract — Architecture-lock forbidden strings (4 forbidden literals)"
files_modified:
  - .planning/phases/210-connector-wow-loop-and-recovery/210-VALIDATION.md
  - .planning/connectors/210-upstream-readiness.md
  - .planning/connectors/connector-install-contract.md
  - scripts/connectors/check-connector-upstream-readiness.mjs
  - scripts/connectors/check-connector-architecture-lock.mjs
  - scripts/connectors/assert-connector-contract-baseline.mjs
  - supabase/migrations/connector_install_foundation.sql
  - lib/markos/connectors/contracts.ts
  - lib/markos/connectors/contracts.cjs
  - lib/markos/connectors/connector-install.ts
  - lib/markos/connectors/index.cjs
  - lib/markos/operator/shell.ts
  - test/connectors/phase-210/preflight/upstream-readiness.test.js
  - test/connectors/phase-210/preflight/architecture-lock.test.js
  - test/connectors/phase-210/preflight/contract-baseline.test.js
  - test/connectors/phase-210/domain-1/connector-install-contract.test.js
  - test/connectors/phase-210/domain-1/status-transitions.test.js
  - test/connectors/phase-210/domain-1/operator-shell-mobile-critical.test.js
autonomous: true
requirements:
  - CONN-01
  - QA-01
  - QA-02
  - QA-03
  - QA-04
must_haves:
  truths:
    - "Wave 0.5 preflight hard-fails when required P206/P207/P208/P209 readiness artifacts are missing or still stubbed (UI-SPEC §B-11)."
    - "ConnectorInstall stores credential references and connector state explicitly instead of hiding them in loose provider config (UI-SPEC §Inheritance Bindings — From 210-01 ConnectorInstall contract)."
    - "Connector records are tenant-scoped, secret-safe, and recovery-linked; `credential_ref` is never rendered, only opaque references (UI-SPEC §Inheritance Bindings: `credential_ref` NEVER rendered in UI)."
    - "Architecture-lock rejects 4 forbidden strings exactly: `plain text access token`, `silent connector degradation`, `recommend four connectors`, `write-capable first wow` (UI-SPEC §Copywriting Contract — architecture-lock; §B-4)."
    - "`surface_family: connector_onboarding` is registered in `lib/markos/operator/shell.ts` with `mobile_priority: critical` so the 210-03 onboarding surface inherits 208-01 mobile-critical doctrine (UI-SPEC §Design System mobile posture; §Checker Sign-Off mobile-critical declaration gate)."
    - "Validation strategy is updated with rows for Tasks 210-01-00 and 210-01-01 before later plans begin."
  artifacts:
    - path: .planning/connectors/210-upstream-readiness.md
      provides: readiness checklist for compliance, run linkage, recovery tasks, and evidence posture (1 row each for 206, 207, 208, 209)
    - path: .planning/connectors/connector-install-contract.md
      provides: canonical ConnectorInstall shape with 22 named fields and connector state vocabulary (8 status literals, 4 auth_type literals)
    - path: scripts/connectors/check-connector-upstream-readiness.mjs
      provides: deterministic preflight for Phases 206-209 connector prerequisites
    - path: scripts/connectors/check-connector-architecture-lock.mjs
      provides: forbidden-string detector enforcing 4 architecture-lock literals from UI-SPEC §Copywriting Contract
    - path: lib/markos/operator/shell.ts
      provides: operator-shell registry adding `surface_family: connector_onboarding` row with `mobile_priority: critical`
  key_links:
    - from: lib/markos/mcp/sessions.cjs
      to: .planning/connectors/connector-install-contract.md
      via: token handling and revoke posture should inform connector credential-reference design
      pattern: "opaque_token"
    - from: lib/markos/webhooks/engine.cjs
      to: lib/markos/connectors/connector-install.ts
      via: tenant-scoped secret and active-state discipline should be inherited by connector records
      pattern: "tenant_id"
    - from: lib/markos/operator/shell.ts
      to: app/(markos)/onboarding/connectors/page.tsx
      via: surface_family registry — `connector_onboarding` declared here is consumed by the 210-03 page for mobile-critical breakpoint inheritance
      pattern: "connector_onboarding"
---

<objective>
Establish the live-data foundation for Phase 210: upstream readiness and architecture lock, then a durable ConnectorInstall contract that stores auth model, scope posture, secret references, sync state, and recovery linkage. Also register the `connector_onboarding` surface_family in the operator shell with `mobile_priority: critical` so the 210-03 onboarding page inherits 208-01 mobile-critical doctrine.
</objective>

<context>
@.planning/phases/210-connector-wow-loop-and-recovery/210-CONTEXT.md
@.planning/phases/210-connector-wow-loop-and-recovery/210-RESEARCH.md
@.planning/phases/210-connector-wow-loop-and-recovery/210-REVIEWS.md
@.planning/phases/210-connector-wow-loop-and-recovery/210-VALIDATION.md
@.planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>
<task id="210-01-00">
  <name>Wave 0.5 upstream preflight, architecture lock, and validation baseline</name>
  <files>.planning/connectors/210-upstream-readiness.md, scripts/connectors/check-connector-upstream-readiness.mjs, scripts/connectors/check-connector-architecture-lock.mjs, scripts/connectors/assert-connector-contract-baseline.mjs, test/connectors/phase-210/preflight/upstream-readiness.test.js, test/connectors/phase-210/preflight/architecture-lock.test.js, test/connectors/phase-210/preflight/contract-baseline.test.js, .planning/phases/210-connector-wow-loop-and-recovery/210-VALIDATION.md</files>
  <read_first>
    - .planning/phases/210-connector-wow-loop-and-recovery/210-RESEARCH.md section "Domain 0 - Upstream readiness and architecture lock"
    - .planning/phases/210-connector-wow-loop-and-recovery/210-REVIEWS.md sections HIGH and MEDIUM concerns
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Acceptance Criteria B-4 (architecture-lock forbidden strings) and §B-11 (Wave 0.5 readiness)
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Copywriting Contract — Architecture-lock forbidden strings (4 forbidden literals enumerated)
    - .planning/phases/206-soc2-type1-foundation/206-RESEARCH.md
    - .planning/phases/208-human-operating-interface/208-RESEARCH.md
    - .planning/phases/209-evidence-research-and-claim-safety/209-RESEARCH.md
  </read_first>
  <action>
    Step 1: Create `.planning/connectors/210-upstream-readiness.md` with one row each for phases `206`, `207`, `208`, and `209`, and columns `phase`, `required_input`, `evidence_path`, `status`, `blocker_owner`, and `notes`. Each row's `required_input` must cite the upstream artifact verbatim: 206 row cites `mutation-class-policy.md` (`connector.mutate` root literal); 207 row cites `RunApiEnvelope` from 207-01-CONTRACT-LOCK §4; 208 row cites Recovery Center surface from 208-05; 209 row cites `EvidenceMap.source_class ∈ connector-derived` from 209-01-01.
    Step 2: Implement `scripts/connectors/check-connector-upstream-readiness.mjs` so it hard-fails when those required artifacts are missing and soft-warns when they exist but are stale. The script MUST contain the exact phase keys `206`, `207`, `208`, and `209` as enumerable constants.
    Step 3: Implement `scripts/connectors/check-connector-architecture-lock.mjs` so it rejects all of: `vitest`, `playwright`, `.test.ts`, `route.ts`, `plain text access token`, `silent connector degradation`, `recommend four connectors`, and `write-capable first wow` in the Phase 210 surface. The 4 connector-specific literals MUST appear verbatim per UI-SPEC §Copywriting Contract — Architecture-lock forbidden strings.
    Step 4: Implement `scripts/connectors/assert-connector-contract-baseline.mjs` to verify plan-to-validation coverage (every plan's frontmatter `requirements:` field is non-empty and every requirement ID maps to at least one task in `210-VALIDATION.md`).
    Step 5: Add preflight tests: `upstream-readiness.test.js` asserts the readiness table contains rows for 206/207/208/209; `architecture-lock.test.js` asserts each of the 4 architecture-lock forbidden strings is rejected by `check-connector-architecture-lock.mjs`; `contract-baseline.test.js` asserts the validation map carries rows for every Phase 210 task.
    Step 6: Update `210-VALIDATION.md` so Tasks `210-01-00`, `210-01-01`, `210-01-02`, `210-02-01`, `210-03-01`, `210-03-02`, `210-04-01`, `210-05-01`, `210-06-01`, and `210-06-02` exist in the verification map before later plans begin.
  </action>
  <acceptance_criteria>
    - `.planning/connectors/210-upstream-readiness.md` contains one row each for phases `206`, `207`, `208`, and `209` (UI-SPEC §B-11)
    - `scripts/connectors/check-connector-upstream-readiness.mjs` contains the exact phase keys `206`, `207`, `208`, and `209` (UI-SPEC §B-11): `grep -c "206\|207\|208\|209" scripts/connectors/check-connector-upstream-readiness.mjs` >= 4
    - `scripts/connectors/check-connector-architecture-lock.mjs` contains the exact forbidden strings `plain text access token`, `silent connector degradation`, `recommend four connectors`, and `write-capable first wow` (UI-SPEC §B-4 + §Copywriting Contract): `grep -c "plain text access token\|silent connector degradation\|recommend four connectors\|write-capable first wow" scripts/connectors/check-connector-architecture-lock.mjs` >= 4
    - `210-VALIDATION.md` contains rows `210-01-00`, `210-01-01`, `210-01-02`, `210-02-01`, `210-03-01`, `210-03-02`, `210-04-01`, `210-05-01`, `210-06-01`, `210-06-02` (UI-SPEC §B-12 expanded for split UI plan): `grep -c "210-01-00\|210-01-01\|210-01-02\|210-02-01\|210-03-01\|210-03-02\|210-04-01\|210-05-01\|210-06-01\|210-06-02" .planning/phases/210-connector-wow-loop-and-recovery/210-VALIDATION.md` >= 10
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/connectors/phase-210/preflight/</automated>
  </verify>
  <done>Phase 210 now fails closed when compliance, run, recovery-task, or evidence substrate is not truly ready; architecture-lock rejects all 4 connector-specific forbidden literals.</done>
</task>

<task id="210-01-01">
  <name>ConnectorInstall contract and status vocabulary</name>
  <files>.planning/connectors/connector-install-contract.md, supabase/migrations/connector_install_foundation.sql, lib/markos/connectors/contracts.ts, lib/markos/connectors/contracts.cjs, lib/markos/connectors/connector-install.ts, lib/markos/connectors/index.cjs, test/connectors/phase-210/domain-1/connector-install-contract.test.js, test/connectors/phase-210/domain-1/status-transitions.test.js</files>
  <read_first>
    - .planning/phases/210-connector-wow-loop-and-recovery/210-RESEARCH.md section "Domain 1 - ConnectorInstall contract foundation"
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Inheritance Bindings — From 210-01 ConnectorInstall contract (full 22-field table mapping each field to UI consumption)
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Acceptance Criteria B-1 (5 named fields), B-2 (8 status literals), B-3 (4 auth_type literals)
    - .planning/phases/206-soc2-type1-foundation/206-UI-SPEC.md §Downstream UI Inheritance Map row "Approval-token issuance + mutation-class viewer" — `connector.mutate` root literal binding
    - .planning/phases/207-agentrun-v2-orchestration-substrate/207-UI-SPEC.md §RunApiEnvelope contract — `run_id` linkage for connector-install AgentRun
    - lib/markos/mcp/sessions.cjs (existing pattern: opaque_token credential reference)
    - lib/markos/webhooks/engine.cjs (existing pattern: tenant-scoped secret discipline)
  </read_first>
  <action>
    Step 1: Create `.planning/connectors/connector-install-contract.md` with the exact 22 fields verbatim per UI-SPEC §Inheritance Bindings — From 210-01 ConnectorInstall contract: `connector_install_id`, `tenant_id`, `provider`, `provider_account_id`, `status`, `status_reason`, `scopes`, `credential_ref`, `auth_type`, `installed_by`, `installed_at`, `last_sync_at`, `next_sync_at`, `sync_cursor_ref`, `retention_policy`, `data_classification`, `dependent_agent_ids`, `last_error`, `recovery_task_id`, `evidence_refs`, `created_at`, and `updated_at`. The doc must annotate `credential_ref` with the literal note "NEVER rendered in UI (per 210-CONTEXT non-negotiable)" per UI-SPEC §Inheritance Bindings.
    Step 2: In `lib/markos/connectors/connector-install.ts`, constrain `status` to the exact 8 literals from UI-SPEC §B-2: `connected | degraded | expired | revoked | rate_limited | permission_missing | sync_failed | recovery_needed`. Each literal MUST be exported as part of the discriminated-union type and as an enumerable runtime constant (used by the 210-03 page state matrix per UI-SPEC §Surface A state matrix).
    Step 3: In `lib/markos/connectors/connector-install.ts`, constrain `auth_type` to the exact 4 literals from UI-SPEC §B-3: `oauth | api_key | service_account | webhook_secret`. Each literal drives a different per-card primary CTA copy variant per UI-SPEC §Surface A copy register ("Connect {provider}", "Add {provider} API key", "Upload {provider} service account", "Configure {provider} webhook").
    Step 4: Bind ConnectorInstall to 206 mutation-class doctrine — the contract doc MUST cite `connector.mutate` as the root mutation class for any state-changing operation per UI-SPEC §Inheritance Bindings — From 206-UI-SPEC. Bind `recovery_task_id` to the 210-05 ConnectorRecoveryTask schema and `evidence_refs` to the 209 EvidenceMap schema (UI-SPEC §Inheritance Bindings rows for these fields).
    Step 5: Implement `supabase/migrations/connector_install_foundation.sql` with `connector_install` table carrying all 22 columns + RLS policy keyed on `tenant_id` + `credential_ref` typed as `text` (opaque reference, never raw secret) + `status` constrained to the 8 status literals via CHECK constraint + `auth_type` constrained to the 4 auth_type literals via CHECK constraint.
    Step 6: Add domain-1 tests: `connector-install-contract.test.js` asserts the contract module exports the 22 field names AND asserts `credential_ref` does not appear in any test fixture's user-facing payload (secret redaction sanity); `status-transitions.test.js` asserts every state transition originating from `connected` flows to one of the 8 status literals + asserts `installed → connected → degraded` and `connected → recovery_needed → connected` paths.
  </action>
  <acceptance_criteria>
    - `.planning/connectors/connector-install-contract.md` contains the exact field names `credential_ref`, `status_reason`, `retention_policy`, `data_classification`, and `recovery_task_id` (UI-SPEC §B-1): `grep -c "credential_ref\|status_reason\|retention_policy\|data_classification\|recovery_task_id" .planning/connectors/connector-install-contract.md` >= 5
    - `lib/markos/connectors/connector-install.ts` contains the exact literals `connected`, `degraded`, `expired`, `revoked`, `rate_limited`, `permission_missing`, `sync_failed`, and `recovery_needed` (UI-SPEC §B-2): `grep -c "connected\|degraded\|expired\|revoked\|rate_limited\|permission_missing\|sync_failed\|recovery_needed" lib/markos/connectors/connector-install.ts` >= 8
    - `lib/markos/connectors/connector-install.ts` contains the exact literals `oauth`, `api_key`, `service_account`, and `webhook_secret` (UI-SPEC §B-3): `grep -c "oauth\|api_key\|service_account\|webhook_secret" lib/markos/connectors/connector-install.ts` >= 4
    - `.planning/connectors/connector-install-contract.md` cites `connector.mutate` as the root mutation class (UI-SPEC §Inheritance Bindings — From 206-UI-SPEC): `grep -c "connector.mutate" .planning/connectors/connector-install-contract.md` >= 1
    - `test/connectors/phase-210/domain-1/connector-install-contract.test.js` contains the exact string `credential_ref` (UI-SPEC §Inheritance Bindings: secret redaction)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/connectors/phase-210/domain-1/connector-install-contract.test.js test/connectors/phase-210/domain-1/status-transitions.test.js</automated>
  </verify>
  <done>ConnectorInstall is now explicit, tenant-safe, and usable as the foundation for wow, recovery, and backfill behavior; 22 fields plus 8 status + 4 auth_type literals match the UI-SPEC inheritance map verbatim.</done>
</task>

<task id="210-01-02">
  <name>Operator-shell mobile-critical declaration for connector_onboarding surface_family</name>
  <files>lib/markos/operator/shell.ts, test/connectors/phase-210/domain-1/operator-shell-mobile-critical.test.js</files>
  <read_first>
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Design System — "Mobile posture" row (Onboarding-critical declaration; inherits 208-01 mobile-critical doctrine)
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Plan Scope Classification — Plan 210-03 row "Mobile Priority: critical"
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Checker Sign-Off — "Mobile-critical declaration added to lib/markos/operator/shell.ts" gate
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §Plan 208-01 mobile-critical declaration (parent precedent)
  </read_first>
  <action>
    Step 1: Create or extend `lib/markos/operator/shell.ts` to export a typed `SurfaceFamilyRegistry` constant that maps surface_family literals to `mobile_priority` literals (`critical | secondary | n/a`).
    Step 2: Add a row for `connector_onboarding` with `mobile_priority: 'critical'` per UI-SPEC §Plan Scope Classification (Plan 210-03 row carries Mobile Priority: critical) and §Design System mobile posture (onboarding is mobile-driver — first impression on phones drives wow-promise activation).
    Step 3: Type the registry so a TS error is raised if a downstream consumer references a surface_family literal not in the registry. Export the literal `connector_onboarding` from `surface_family` discriminated-union type.
    Step 4: Add `test/connectors/phase-210/domain-1/operator-shell-mobile-critical.test.js` that asserts the registry contains a `connector_onboarding` entry with `mobile_priority === 'critical'`.
  </action>
  <acceptance_criteria>
    - `lib/markos/operator/shell.ts` exports a `connector_onboarding` surface_family entry: `grep -c "connector_onboarding" lib/markos/operator/shell.ts` >= 1
    - `lib/markos/operator/shell.ts` declares `mobile_priority: 'critical'` for that entry: `grep -c "mobile_priority.*critical\|mobile_priority: 'critical'\|mobile_priority: \"critical\"" lib/markos/operator/shell.ts` >= 1
    - `test/connectors/phase-210/domain-1/operator-shell-mobile-critical.test.js` asserts the registry entry exists
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/connectors/phase-210/domain-1/operator-shell-mobile-critical.test.js</automated>
  </verify>
  <done>The 210-03 onboarding-connectors page now inherits mobile-critical doctrine from `lib/markos/operator/shell.ts` per the UI-SPEC checker gate; downstream surfaces consuming this registry get a typed handle to the connector-onboarding surface family.</done>
</task>
</tasks>

<verification>
- npm test -- test/connectors/phase-210/preflight/
- npm test -- test/connectors/phase-210/domain-1/
</verification>

<success_criteria>
- Upstream readiness artifacts exist for P206-P209 with cited inputs
- Architecture-lock rejects 4 connector-specific forbidden literals exactly
- ConnectorInstall has explicit 22-field shape + 8 status literals + 4 auth_type literals matching UI-SPEC inheritance map verbatim
- ConnectorInstall doctrine cites `connector.mutate` (206 mutation-class root literal)
- `connector_onboarding` surface_family registered with `mobile_priority: critical` so the 210-03 page inherits 208-01 mobile-critical posture
- Validation strategy is seeded with all 10 task rows before later plans execute
</success_criteria>
