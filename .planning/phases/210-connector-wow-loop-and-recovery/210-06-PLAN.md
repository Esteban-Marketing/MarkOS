---
phase: 210
plan: "06"
type: execute
wave: 6
depends_on:
  - "210-01"
  - "210-02"
  - "210-03"
  - "210-04"
  - "210-05"
f_ids: []
migration_slots: []
integrates_with:
  - "COMP-01 from P206 (recovery evidence and retention posture must support governance)"
  - "EVD-01..06 from P209 (connector freshness and recovery evidence must be sourceable by later workflows)"
translation_gate_for:
  - "P211 — loop-dispatch substrate (preserved as `<PlaceholderBanner variant=\"future_phase_211\">` on the 210-03 page; future-compatibility map row `content_loop` documents the consumer)"
  - "P212 — learning-fixture personalization (preserved as `<PlaceholderBanner variant=\"future_phase_212\">` on the 210-03 page; future-compatibility map row not yet enumerated — learning is its own future-consumer family)"
  - "P213 — Tenant 0 (future-compatibility map row `tenant0`)"
  - "P214 — SaaS Suite (future-compatibility map rows `saas_activation`, `saas_health`)"
  - "P216 — PLG growth (future-compatibility map row `plg_growth`)"
  - "P218 — ABM growth (future-compatibility map row `abm_growth`)"
  - "P219 — Community growth (future-compatibility map row `community_growth`)"
  - "P220 — Developer marketing (future-compatibility map row `developer_marketing`)"
ui_spec_fold:
  spec: .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md
  spec_version: v1.0.0 (draft 2026-04-29)
  scope_classification: NO_UI
  ac_range:
    - "B-9 (210-06 ConnectorSyncAttempt sync_mode + status + freshness_status literals)"
    - "B-10 (210-06 future-connector-compatibility-map.md 8 future-consumer rows)"
    - "B-12 (210-VALIDATION.md per-task verification map covers all 7 + 1 split UI tasks)"
  citations:
    - "210-UI-SPEC.md §Inheritance Bindings — From 210-06 ConnectorSyncAttempt (UI consumption: not on 210-03 page; downstream P211+ admin views consume sync_mode/status/freshness_status)"
    - "210-UI-SPEC.md §Acceptance Criteria — B-9 (3 sync_mode + 6 status + 3 freshness_status literals = 12 literals total), B-10 (8 future-consumer rows verbatim), B-12 (VALIDATION row coverage)"
    - "210-UI-SPEC.md §Downstream UI Inheritance Map — Connector backfill progress dashboard (future P211+ admin) + Country-compatibility overlay (future P214+) + Future connector consumers (8 rows)"
    - "210-UI-SPEC.md §Translation gates preserved — `future_phase_211` (loop-dispatch) + `future_phase_212` (learning fixtures); these gates are documented in this plan's future-compatibility map but NOT dissolved by 210"
files_modified:
  - .planning/phases/210-connector-wow-loop-and-recovery/210-VALIDATION.md
  - .planning/connectors/backfill-and-recovery-evidence.md
  - .planning/connectors/future-connector-compatibility-map.md
  - lib/markos/connectors/sync-attempt.ts
  - lib/markos/connectors/backfill-policy.ts
  - lib/markos/connectors/recovery-evidence.ts
  - test/connectors/phase-210/domain-6/sync-evidence.test.js
  - test/connectors/phase-210/domain-6/future-compatibility.test.js
autonomous: true
requirements:
  - CONN-01
  - CONN-04
  - QA-14
  - QA-15
must_haves:
  truths:
    - "Backfill and retry remain cursor-based, replay-safe, and visibly partial when needed (UI-SPEC §B-9: 6 status literals including `partial` and `dlq`)."
    - "Recovery evidence is durable enough for support and governance review; `evidence_refs[]` populated on every ConnectorSyncAttempt completion."
    - "Future connector consumers are explicitly mapped without expanding Phase 210 into later phases — 8 future-consumer rows with `future_consumer` status (UI-SPEC §B-10)."
    - "Forward translation gates `future_phase_211` (loop-dispatch) and `future_phase_212` (learning fixtures) are preserved on the 210-03 page; the future-compatibility map row `content_loop` documents the loop-dispatch consumer family for when 211 ships (UI-SPEC §Translation gates preserved)."
  artifacts:
    - path: .planning/connectors/backfill-and-recovery-evidence.md
      provides: canonical sync attempt, retry, DLQ, freshness, and evidence posture; 12 ConnectorSyncAttempt fields + 3 sync_mode + 6 status + 3 freshness_status literals
    - path: .planning/connectors/future-connector-compatibility-map.md
      provides: 8 named future connector consumers (`content_loop`, `tenant0`, `saas_activation`, `saas_health`, `plg_growth`, `abm_growth`, `community_growth`, `developer_marketing`) with status `future_consumer` for every row
    - path: lib/markos/connectors/sync-attempt.ts
      provides: typed module with 12 named literals (3 sync_mode + 6 status + 3 freshness_status)
  key_links:
    - from: lib/markos/webhooks/engine.ts
      to: lib/markos/connectors/sync-attempt.ts
      via: durable event-handling posture should inform replay-safe connector sync behavior
      pattern: "active"
    - from: .planning/connectors/future-connector-compatibility-map.md
      to: app/(markos)/onboarding/connectors/page.tsx
      via: forward translation gates `future_phase_211`, `future_phase_212` rendered on the 210-03 page consume the `content_loop` row + a learning row from this map (UI-SPEC §CR-18 + §Translation gates preserved)
      pattern: "future_consumer"
---

<objective>
Make connector sync, retry, and backfill replay-safe, observable, and auditable while explicitly mapping which later phases consume the connector substrate. Preserve the forward translation gates `future_phase_211` (loop-dispatch) and `future_phase_212` (learning fixtures) per UI-SPEC §Translation gates preserved; document them in the future-compatibility map. Update 210-VALIDATION.md so every Phase 210 task (including the 210-03 split) is covered (UI-SPEC §B-12).
</objective>

<context>
@.planning/phases/210-connector-wow-loop-and-recovery/210-CONTEXT.md
@.planning/phases/210-connector-wow-loop-and-recovery/210-RESEARCH.md
@.planning/phases/210-connector-wow-loop-and-recovery/210-VALIDATION.md
@.planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md
</context>

<tasks>
<task id="210-06-01">
  <name>ConnectorSyncAttempt state machine and recovery evidence</name>
  <files>.planning/connectors/backfill-and-recovery-evidence.md, lib/markos/connectors/sync-attempt.ts, lib/markos/connectors/backfill-policy.ts, lib/markos/connectors/recovery-evidence.ts, test/connectors/phase-210/domain-6/sync-evidence.test.js</files>
  <read_first>
    - .planning/phases/210-connector-wow-loop-and-recovery/210-RESEARCH.md section "Domain 6 - Backfill, retry, recovery evidence, and future compatibility"
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Inheritance Bindings — From 210-06 ConnectorSyncAttempt (downstream P211+ admin views consume these literals)
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Acceptance Criteria — B-9 (12 literals)
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Downstream UI Inheritance Map — Connector backfill progress dashboard (future P211+) consumes ConnectorSyncAttempt
    - lib/markos/webhooks/engine.ts (existing pattern: active-state discipline for durable event handling)
    - .planning/phases/206-soc2-type1-foundation/206-RESEARCH.md
  </read_first>
  <action>
    Step 1: Create `.planning/connectors/backfill-and-recovery-evidence.md` with the exact 12 `ConnectorSyncAttempt` fields verbatim per UI-SPEC §Inheritance Bindings: `sync_attempt_id`, `connector_install_id`, `sync_mode`, `cursor_ref`, `started_at`, `finished_at`, `status`, `retry_count`, `dlq_ref`, `freshness_status`, `evidence_refs`, and `recovery_task_id`.
    Step 2: In `lib/markos/connectors/sync-attempt.ts`, constrain `sync_mode` to the exact 3 literals from UI-SPEC §B-9: `incremental | backfill | retry`. Constrain `status` to the exact 6 literals: `queued | running | partial | failed | complete | dlq`. Constrain `freshness_status` to the exact 3 literals: `fresh | stale | unknown`. All 12 literals MUST be exported as runtime constants.
    Step 3: Implement `lib/markos/connectors/backfill-policy.ts` with cursor-based replay-safe backfill: every backfill MUST be idempotent on repeated runs; `cursor_ref` advances only on `complete`; on `partial`, the cursor remains at the last successful point; on `failed`, the cursor remains at the start; on `dlq`, the attempt is dead-lettered for human review.
    Step 4: Implement `lib/markos/connectors/recovery-evidence.ts` to populate `evidence_refs[]` on every ConnectorSyncAttempt completion (consumed by 209 EvidenceMap.source_class connector-derived row per UI-SPEC §Inheritance Bindings — From 209-UI-SPEC).
    Step 5: Add `sync-evidence.test.js` covering: duplicate sync (idempotency), partial failure (cursor preserved), retry (sync_mode === 'retry'), DLQ (status === 'dlq'), and freshness posture (`stale` vs `fresh` vs `unknown`) without masking incomplete data.
  </action>
  <acceptance_criteria>
    - `.planning/connectors/backfill-and-recovery-evidence.md` contains the exact field names `sync_mode`, `cursor_ref`, `retry_count`, `dlq_ref`, `freshness_status`, `evidence_refs` (UI-SPEC §Inheritance Bindings)
    - `lib/markos/connectors/sync-attempt.ts` contains the exact 12 literals (UI-SPEC §B-9): `grep -c "incremental\\|backfill\\|retry\\|queued\\|running\\|partial\\|failed\\|complete\\|dlq\\|fresh\\|stale\\|unknown" lib/markos/connectors/sync-attempt.ts` >= 12
    - `test/connectors/phase-210/domain-6/sync-evidence.test.js` contains the exact strings `partial`, `dlq`, and `stale`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/connectors/phase-210/domain-6/sync-evidence.test.js</automated>
  </verify>
  <done>Connector sync now has explicit replay, freshness, and recovery-evidence posture instead of vague background behavior; 12 named literals match UI-SPEC §B-9 verbatim; downstream P211+ admin backfill dashboard inherits from this contract.</done>
</task>

<task id="210-06-02">
  <name>Future connector compatibility map + 210-VALIDATION.md per-task coverage</name>
  <files>.planning/connectors/future-connector-compatibility-map.md, test/connectors/phase-210/domain-6/future-compatibility.test.js, .planning/phases/210-connector-wow-loop-and-recovery/210-VALIDATION.md</files>
  <read_first>
    - .planning/phases/210-connector-wow-loop-and-recovery/210-RESEARCH.md section "Domain 6 - Backfill, retry, recovery evidence, and future compatibility"
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Acceptance Criteria — B-10 (8 future-consumer rows verbatim) + B-12 (VALIDATION row coverage)
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Downstream UI Inheritance Map — Future connector consumers row (8 rows enumerated + Phases that ship them)
    - .planning/phases/210-connector-wow-loop-and-recovery/210-UI-SPEC.md §Translation gates preserved — `future_phase_211`, `future_phase_212`
    - .planning/phases/210-connector-wow-loop-and-recovery/210-CONTEXT.md
  </read_first>
  <action>
    Step 1: Create `.planning/connectors/future-connector-compatibility-map.md` with one row each for the exact 8 future-consumer keys per UI-SPEC §B-10: `content_loop` (P211 loop-dispatch), `tenant0` (P213 Tenant 0), `saas_activation` (P214 SaaS Suite), `saas_health` (P214 SaaS Suite), `plg_growth` (P216), `abm_growth` (P218), `community_growth` (P219), and `developer_marketing` (P220).
    Step 2: Make every row use the exact status `future_consumer` (UI-SPEC §B-10): each row carries columns `consumer_key`, `target_phase`, `expected_input` (which 210 contracts the consumer will read from), `expected_output` (what the consumer will produce on top of connector data), `status`, and `notes`.
    Step 3: Document the preserved translation gates per UI-SPEC §Translation gates preserved: the `content_loop` row notes that the 210-03 page renders `<PlaceholderBanner variant="future_phase_211">` for loop-dispatch-bound recommendations until 211 ships; a separate note documents that learning-fixture personalization is preserved as `<PlaceholderBanner variant="future_phase_212">` until 212 ships.
    Step 4: Add `future-compatibility.test.js` asserting (a) all 8 rows present, (b) every row uses `future_consumer` status, (c) no row is treated as active execution scope (status NOT `active` or `in_progress`).
    Step 5: Update `210-VALIDATION.md` so all 11 Phase 210 task IDs are represented in the per-task verification map per UI-SPEC §B-12: `210-01-00`, `210-01-01`, `210-01-02`, `210-02-01`, `210-03-01`, `210-03-02`, `210-04-01`, `210-05-01`, `210-06-01`, `210-06-02`. Each row carries Plan, Wave, Requirement, Test Type, Automated Command, File Exists, Status columns. (The 210-VALIDATION.md update is co-owned with 210-01 task `210-01-00` Step 6 — this task confirms the final state including the new 210-01-02 operator-shell row + 210-03-02 UI render row + UI-SPEC AC fold rows.)
  </action>
  <acceptance_criteria>
    - `.planning/connectors/future-connector-compatibility-map.md` contains rows for all 8 future-consumer keys (UI-SPEC §B-10): `grep -c "content_loop\\|tenant0\\|saas_activation\\|saas_health\\|plg_growth\\|abm_growth\\|community_growth\\|developer_marketing" .planning/connectors/future-connector-compatibility-map.md` >= 8
    - `.planning/connectors/future-connector-compatibility-map.md` contains the exact status `future_consumer` for every row (UI-SPEC §B-10): `grep -c "future_consumer" .planning/connectors/future-connector-compatibility-map.md` >= 8
    - `test/connectors/phase-210/domain-6/future-compatibility.test.js` contains the exact string `future_consumer`
    - `210-VALIDATION.md` contains rows for all 10 task IDs (UI-SPEC §B-12 expanded for split UI plan): `grep -c "210-01-00\\|210-01-01\\|210-01-02\\|210-02-01\\|210-03-01\\|210-03-02\\|210-04-01\\|210-05-01\\|210-06-01\\|210-06-02" .planning/phases/210-connector-wow-loop-and-recovery/210-VALIDATION.md` >= 10
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/connectors/phase-210/domain-6/future-compatibility.test.js</automated>
  </verify>
  <done>Later phases now consume connector outputs through an explicit compatibility map instead of implicit scope creep; 8 future-consumer rows ship; preserved translation gates (`future_phase_211`, `future_phase_212`) documented; `210-VALIDATION.md` per-task map covers all 10 task IDs including the 210-03 UI split.</done>
</task>
</tasks>

<verification>
- npm test -- test/connectors/phase-210/domain-6/sync-evidence.test.js
- npm test -- test/connectors/phase-210/domain-6/future-compatibility.test.js
</verification>

<success_criteria>
- ConnectorSyncAttempt has explicit replay, freshness, and evidence fields with 12 literals matching UI-SPEC §B-9 verbatim
- Recovery evidence is auditable and support-visible; `evidence_refs[]` populated on every completion
- Later phases are explicitly mapped as future consumers with 8 rows + `future_consumer` status (UI-SPEC §B-10)
- Preserved forward translation gates (`future_phase_211`, `future_phase_212`) documented in the future-compatibility map
- 210-VALIDATION.md per-task map covers all 10 Phase 210 task IDs (UI-SPEC §B-12 expanded for split UI plan)
</success_criteria>
