---
phase: 212
plan: "01"
type: execute
wave: 1
ui_scope: NO_UI
depends_on: []
f_ids: []
migration_slots: []
integrates_with:
  - "RUN-01..08 from P207 (performance logs must link to real AgentRun context, not inferred execution state)"
  - "EVD-01..06 from P209 (lesson evidence inherits EvidenceMap and source-quality posture)"
  - "LOOP-01..08 from P211 (expected performance envelopes are fed by the real loop-measurement handoff)"
  - "COMP-01 from P206 (privacy/compliance posture gates what can later be promoted cross-tenant)"
translation_gate_for: []
files_modified:
  - .planning/phases/212-learning-literacy-evolution/212-VALIDATION.md
  - .planning/literacy/212-upstream-readiness.md
  - .planning/literacy/artifact-performance-log-contract.md
  - scripts/literacy/check-learning-upstream-readiness.mjs
  - scripts/literacy/check-learning-architecture-lock.mjs
  - scripts/literacy/assert-artifact-performance-baseline.mjs
  - supabase/migrations/learning_artifact_performance_log.sql
  - lib/markos/learning/contracts.ts
  - lib/markos/learning/contracts.cjs
  - lib/markos/learning/artifact-performance.ts
  - lib/markos/learning/index.cjs
  - test/literacy/phase-212/preflight/upstream-readiness.test.js
  - test/literacy/phase-212/preflight/architecture-lock.test.js
  - test/literacy/phase-212/preflight/performance-baseline.test.js
  - test/literacy/phase-212/domain-1/performance-envelope.test.js
  - test/literacy/phase-212/domain-1/outcome-logging.test.js
autonomous: true
requirements:
  - LRN-01
  - LRN-02
  - QA-01
  - QA-02
  - QA-03
  - QA-04
must_haves:
  truths:
    - "Wave 0.5 preflight hard-fails when required P206/P207/P208/P209/P211 readiness artifacts are missing or still stubbed."
    - "ArtifactPerformanceLog stores expected and actual outcomes with explicit degraded states instead of silent missing-data behavior."
    - "Performance logs are tenant-scoped, evidence-linked, and AgentRun-linked."
    - "Validation strategy is updated with pending rows for Tasks 212-01-00 and 212-01-01 before later plans begin."
    - "ArtifactPerformanceLog 19-field envelope (per 212-UI-SPEC §Downstream UI Inheritance Map row 1, 'ArtifactPerformanceLog dashboard') is published verbatim so the future P208 admin extension or P213 Tenant 0 closeout surface can bind without re-deriving field names."
    - "The four `measurement_status` literals (`expected_only`, `measured`, `degraded`, `missing_attribution`) and three `attribution_status` literals (`ready`, `degraded`, `unavailable`) are the canonical state vocabulary for downstream `.c-badge--{state}` rendering (per 212-UI-SPEC §Downstream UI Inheritance Map row 1)."
    - "Architecture-lock CI script rejects three forbidden strings (`promote raw tenant copy`, `auto-promote literacy`, `silent prompt drift`) plus enforces carved-out scope for two additional doctrine strings (`single tenant raw lesson` allowed only in 212-03 endpoint, `must not apply customer-facing mutations directly` allowed only in 212-05 endpoint), so backend doctrine prose stays clean (per 212-UI-SPEC §Copywriting Contract Forbidden architecture-lock strings)."
    - "Banned-lexicon zero-match in `.planning/literacy/*.md` is enforced by the architecture-lock CI script per 212-UI-SPEC §DESIGN.md Compliance Assertions row `Architecture-lock forbidden strings`."
  artifacts:
    - path: .planning/literacy/212-upstream-readiness.md
      provides: readiness checklist for privacy, evidence, measurement, and task-handoff substrate
    - path: .planning/literacy/artifact-performance-log-contract.md
      provides: canonical ArtifactPerformanceLog shape and outcome-state vocabulary (consumed verbatim by 212-UI-SPEC §Downstream row 1 'ArtifactPerformanceLog dashboard' + row 11 'Learning-fixture evidence ledger per fixture' + row 13 'Recovery Center learning-failure variant')
    - path: scripts/literacy/check-learning-upstream-readiness.mjs
      provides: deterministic preflight for P206/P207/P208/P209/P211 requirements
    - path: scripts/literacy/check-learning-architecture-lock.mjs
      provides: forbidden-pattern detector for raw-tenant promotion + silent prompt drift + banned lexicon (the doctrine guardrail enforced across all 212 surface files per 212-UI-SPEC §Copywriting Contract)
  key_links:
    - from: .planning/phases/211-content-social-revenue-loop/211-06-PLAN.md
      to: .planning/literacy/artifact-performance-log-contract.md
      via: the Phase 211 measurement handoff becomes the baseline contract for expected and actual outcome capture
      pattern: "expected performance envelopes"
    - from: lib/markos/crm/attribution.ts
      to: lib/markos/learning/artifact-performance.ts
      via: degraded attribution and weighted evidence should be reused instead of redefined
      pattern: "buildWeightedAttributionModel"
---

<ui_spec_fold>
ui_scope: NO_UI

ui_spec_block:
  - 212-UI-SPEC.md §parent_doctrine_chain (frontmatter) — inheritance from 206 (mutation-class for downstream promotion surfaces), 207 (RunApiEnvelope; learning runs link via `agent_run_id`), 208 (operator cockpit consumers), 209 (EvidenceMap binding for `evidence_refs`), 211 (MeasurementHandoff seeding `expected_outcome` / `actual_outcome` / `lesson_summary`)
  - 212-UI-SPEC.md §Translation Gate Dissolution and Opening — this plan DISSOLVES three upstream gates: (1) 209 §Downstream learning-fixture evidence ledger row dissolves once `ArtifactPerformanceLog.evidence_refs` ships; (2) 209 `inference_label == 'inferred'` placeholder dissolves once `ArtifactPerformanceLog.lesson_summary` + `confidence_score` provide the explicit source for any inferred claim derived from learning; (3) 211 §Translation Gates Opened `MeasurementHandoff.learning_ready == true` placeholder dissolves once `ArtifactPerformanceLog` ingests P211 `MeasurementHandoff` outputs via `artifact_id` foreign key.
  - 212-UI-SPEC.md §Downstream UI Inheritance Map row 1 — ArtifactPerformanceLog dashboard (future P208 admin extension or P213 Tenant 0 closeout). 19-field `ArtifactPerformanceLog` envelope verbatim (`performance_log_id`, `tenant_id`, `artifact_id`, `artifact_type`, `channel`, `audience_segment`, `funnel_stage`, `expected_outcome`, `actual_outcome`, `measurement_window_days`, `measurement_status`, `attribution_status`, `evidence_refs`, `agent_run_id`, `dispatch_attempt_id`, `lesson_summary`, `confidence_score`, `next_task_needed`, `created_at`, `updated_at`). Four `measurement_status` literals render as `.c-badge--{info,success,warning,error}`. Three `attribution_status` literals render as `.c-badge--{success,warning,error}`. Per D-09 mint-as-text, `performance_log_id` copy-link CTAs use mint-as-text. Per D-14, vanilla-table recipe for the list view. Cross-binding: 207 `RunApiEnvelope` via `agent_run_id`, 209 `EvidenceMap` via `evidence_refs`, 211 `DispatchAttempt` via `dispatch_attempt_id`, 211 `MeasurementHandoff` as the upstream feed.
  - 212-UI-SPEC.md §Downstream UI Inheritance Map row 11 — Learning-fixture evidence ledger per fixture (dissolves 209 §Downstream row 4). Per-fixture, `<EvidenceSummary />` (209) composes over `ArtifactPerformanceLog.evidence_refs`; "what was learned" panel reads `ArtifactPerformanceLog.lesson_summary`; confidence reads `ArtifactPerformanceLog.confidence_score`; source reads `ArtifactPerformanceLog.agent_run_id` deep-link to 207.
  - 212-UI-SPEC.md §Downstream UI Inheritance Map row 13 — Recovery Center learning-failure variant (P208 Recovery Center extension). `ArtifactPerformanceLog` rows where `measurement_status == 'missing_attribution'` OR `attribution_status == 'unavailable'` render as recovery items with failure-family `learning_attribution_unavailable`.
  - 212-UI-SPEC.md §Copywriting Contract — Forbidden architecture-lock strings row (this plan owns the CI enforcement — `scripts/literacy/check-learning-architecture-lock.mjs` rejects the three exact strings `promote raw tenant copy`, `auto-promote literacy`, `silent prompt drift` plus four substrate-mismatch strings `vitest`, `playwright`, `.test.ts`, `route.ts`); Banned lexicon row (this plan ships the first 2 doctrine docs and validates the lock works); Pricing-placeholder rule row (`{{MARKOS_PRICING_ENGINE_PENDING}}` accepted by `evidence_refs` and `lesson_summary` only when upstream P211 carried it).

cross_cutting_carry_forward:
  - "Banned lexicon (CLAUDE.md): `synergy`, `leverage`, `empower`, `unlock`, `transform`, `revolutionize`, `supercharge`, `holistic`, `seamless`, `cutting-edge`, `innovative`, `game-changer`, `next-generation`, `world-class`, `best-in-class`, `reimagine`, `disrupt`, `just` (as softener) — zero matches in `.planning/literacy/212-upstream-readiness.md` and `.planning/literacy/artifact-performance-log-contract.md`. No exclamation points in doctrine prose. Verified by 212-01-00 architecture-lock CI script (per 212-UI-SPEC §DESIGN.md Compliance Assertions row `Architecture-lock forbidden strings`)."
  - "Architecture-lock forbidden strings (this plan owns AC.3 of 212-01-00): `promote raw tenant copy`, `auto-promote literacy`, `silent prompt drift`. Plus two additional plan-scoped doctrine forbidden strings — `single tenant raw lesson` (allowed ONLY in 212-03-01 `api/v1/literacy/review.js` direct-promotion-denial endpoint per 212-UI-SPEC §Copywriting Contract) and `must not apply customer-facing mutations directly` (allowed ONLY in 212-05-01 `api/v1/literacy/recommendations.js` per 212-UI-SPEC §Copywriting Contract). Plus four substrate-mismatch strings from the wider GSD architecture lock: `vitest`, `playwright`, `.test.ts`, `route.ts`. None of these strings may appear in any 212 doctrine markdown except the two explicitly carved-out endpoint files. Enforced by `scripts/literacy/check-learning-architecture-lock.mjs`."
  - "Pricing placeholder rule (CLAUDE.md Pricing Engine Canon): `{{MARKOS_PRICING_ENGINE_PENDING}}` is the only accepted placeholder for unresolved pricing copy. `ArtifactPerformanceLog.evidence_refs` and `ArtifactPerformanceLog.lesson_summary` (this plan's `lib/markos/learning/artifact-performance.ts`) accept the literal verbatim ONLY when the upstream loop substrate (P211 `MarketingArtifact.pricing_context_ref`) carried the placeholder. 212 introduces no new placeholder paths."
  - "Engineering-readability prose: short sentences, concrete contract field names, named owning plans. No marketing voice. No hedging language. Every contract row asserts a specific field, literal, threshold, or test-gate procedure (per 212-UI-SPEC §Copywriting Contract Engineering-readability rule)."
  - "Mutation-class doctrine inheritance (206-02): not directly applicable to ArtifactPerformanceLog reads in this plan; the dashboard surface itself is read-only when shipped downstream. Cross-tenant aggregate exports of performance logs would bind to `data.export` per 206-02. NOT authored by this plan."

downstream_ui_consumers_read_only:
  - "P208 admin extension or P213 Tenant 0 closeout — ArtifactPerformanceLog dashboard (212-UI-SPEC §Downstream row 1). Reads 19-field `ArtifactPerformanceLog` envelope. Renders 4 `measurement_status` and 3 `attribution_status` literals as `.c-badge--{state}` chips. NOT modified by this plan."
  - "P208 admin extension or P209-evidence consumer phase — Learning-fixture evidence ledger per fixture (212-UI-SPEC §Downstream row 11). Composes `<EvidenceSummary />` (209) over `ArtifactPerformanceLog.evidence_refs`; reads `lesson_summary` + `confidence_score` + `agent_run_id`. NOT modified by this plan."
  - "P208 Recovery Center extension — Learning-failure variant (212-UI-SPEC §Downstream row 13). Reads `ArtifactPerformanceLog` rows where `measurement_status == 'missing_attribution'` OR `attribution_status == 'unavailable'`. NOT modified by this plan."
</ui_spec_fold>

<objective>
Establish the learning foundation for Phase 212: upstream readiness and architecture lock, then a durable ArtifactPerformanceLog contract that records expected outcomes, actual outcomes, evidence, attribution posture, and lesson-ready states.
</objective>

<context>
@.planning/phases/212-learning-literacy-evolution/212-CONTEXT.md
@.planning/phases/212-learning-literacy-evolution/212-RESEARCH.md
@.planning/phases/212-learning-literacy-evolution/212-REVIEWS.md
@.planning/phases/212-learning-literacy-evolution/212-VALIDATION.md
@.planning/phases/212-learning-literacy-evolution/212-UI-SPEC.md
</context>

<tasks>
<task id="212-01-00">
  <name>Wave 0.5 upstream preflight, architecture lock, and validation baseline</name>
  <files>.planning/literacy/212-upstream-readiness.md, scripts/literacy/check-learning-upstream-readiness.mjs, scripts/literacy/check-learning-architecture-lock.mjs, scripts/literacy/assert-artifact-performance-baseline.mjs, test/literacy/phase-212/preflight/upstream-readiness.test.js, test/literacy/phase-212/preflight/architecture-lock.test.js, test/literacy/phase-212/preflight/performance-baseline.test.js, .planning/phases/212-learning-literacy-evolution/212-VALIDATION.md</files>
  <read_first>
    - .planning/phases/212-learning-literacy-evolution/212-RESEARCH.md section "Domain 0 - Upstream readiness and architecture lock"
    - .planning/phases/212-learning-literacy-evolution/212-REVIEWS.md sections HIGH and MEDIUM concerns
    - .planning/phases/212-learning-literacy-evolution/212-UI-SPEC.md §Copywriting Contract (forbidden architecture-lock strings + banned-lexicon row + pricing-placeholder rule)
    - .planning/phases/212-learning-literacy-evolution/212-UI-SPEC.md §DESIGN.md Compliance Assertions (Architecture-lock forbidden strings + Banned-lexicon enforcement)
    - .planning/phases/211-content-social-revenue-loop/211-06-PLAN.md
    - .planning/phases/209-evidence-research-and-claim-safety/209-RESEARCH.md
    - .planning/phases/206-soc2-type1-foundation/206-RESEARCH.md
  </read_first>
  <action>
    Step 1: Create `.planning/literacy/212-upstream-readiness.md` with one row each for phases `206`, `207`, `208`, `209`, and `211`, and columns `phase`, `required_input`, `evidence_path`, `status`, `blocker_owner`, and `notes`. Doctrine prose carries zero CLAUDE.md banned-lexicon matches and no exclamation points (per 212-UI-SPEC §Copywriting Contract Engineering-readability rule).
    Step 2: Implement `scripts/literacy/check-learning-upstream-readiness.mjs` so it hard-fails when required artifacts from those phases are missing and soft-warns when they exist but are stale.
    Step 3: Implement `scripts/literacy/check-learning-architecture-lock.mjs` so it rejects the exact strings `vitest`, `playwright`, `.test.ts`, `route.ts`, `promote raw tenant copy`, `auto-promote literacy`, and `silent prompt drift` in the Phase 212 surface (per 212-UI-SPEC §Copywriting Contract Forbidden architecture-lock strings list). It MUST also assert that the strings `single tenant raw lesson` and `must not apply customer-facing mutations directly` appear ONLY in the two carved-out endpoint files (`api/v1/literacy/review.js` for the first; `api/v1/literacy/recommendations.js` for the second) — appearance anywhere else fails the lock.
    Step 4: Add preflight tests and update `212-VALIDATION.md` so Tasks `212-01-00` and `212-01-01` exist in the verification map before later plans begin.
  </action>
  <acceptance_criteria>
    - `.planning/literacy/212-upstream-readiness.md` contains one row each for phases `206`, `207`, `208`, `209`, and `211`
    - `scripts/literacy/check-learning-upstream-readiness.mjs` contains the exact phase keys `206`, `207`, `208`, `209`, and `211`
    - `scripts/literacy/check-learning-architecture-lock.mjs` contains the exact forbidden strings `promote raw tenant copy`, `auto-promote literacy`, and `silent prompt drift`
    - `scripts/literacy/check-learning-architecture-lock.mjs` returns non-zero exit when run against a fixture containing any of the three forbidden strings (architecture-lock doctrine assertion per 212-UI-SPEC §DESIGN.md Compliance Assertions row `Architecture-lock forbidden strings`)
    - `scripts/literacy/check-learning-architecture-lock.mjs` enforces the carved-out scope rule — `single tenant raw lesson` allowed only in `api/v1/literacy/review.js` (per 212-UI-SPEC §Copywriting Contract); `must not apply customer-facing mutations directly` allowed only in `api/v1/literacy/recommendations.js` (per 212-UI-SPEC §Copywriting Contract); appearance elsewhere returns non-zero exit
    - `212-VALIDATION.md` contains rows `212-01-00` and `212-01-01`
    - `.planning/literacy/212-upstream-readiness.md` contains zero matches against the CLAUDE.md banned lexicon: `synergy`, `leverage`, `empower`, `unlock`, `transform`, `revolutionize`, `supercharge`, `holistic`, `seamless`, `cutting-edge`, `innovative`, `game-changer`, `next-generation`, `world-class`, `best-in-class`, `reimagine`, `disrupt` (per 212-UI-SPEC §DESIGN.md Compliance Assertions row `Banned lexicon`)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/literacy/phase-212/preflight/</automated>
  </verify>
  <done>Phase 212 now fails closed when evidence, measurement, privacy, or task-handoff substrate is not truly ready, AND the doctrine guardrail (architecture-lock + banned-lexicon + carved-out forbidden strings) is enforced before any later plan runs.</done>
</task>

<task id="212-01-01">
  <name>ArtifactPerformanceLog schema and outcome envelope</name>
  <files>.planning/literacy/artifact-performance-log-contract.md, supabase/migrations/learning_artifact_performance_log.sql, lib/markos/learning/contracts.ts, lib/markos/learning/contracts.cjs, lib/markos/learning/artifact-performance.ts, lib/markos/learning/index.cjs, test/literacy/phase-212/domain-1/performance-envelope.test.js, test/literacy/phase-212/domain-1/outcome-logging.test.js</files>
  <read_first>
    - .planning/phases/212-learning-literacy-evolution/212-RESEARCH.md section "Domain 1 - ArtifactPerformanceLog and outcome envelope"
    - .planning/phases/212-learning-literacy-evolution/212-UI-SPEC.md §Downstream UI Inheritance Map row 1 (ArtifactPerformanceLog dashboard) and row 11 (Learning-fixture evidence ledger per fixture) and row 13 (Recovery Center learning-failure variant)
    - .planning/phases/212-learning-literacy-evolution/212-UI-SPEC.md §Translation Gate Dissolution and Opening (this task DISSOLVES three upstream gates — 209 learning-fixture evidence ledger, 209 `inference_label == 'inferred'` placeholder, 211 `MeasurementHandoff.learning_ready == true` placeholder)
    - .planning/phases/211-content-social-revenue-loop/211-06-PLAN.md
    - lib/markos/crm/reporting.ts
    - lib/markos/crm/attribution.ts
  </read_first>
  <action>
    Step 1: Create `.planning/literacy/artifact-performance-log-contract.md` with the exact fields `performance_log_id`, `tenant_id`, `artifact_id`, `artifact_type`, `channel`, `audience_segment`, `funnel_stage`, `expected_outcome`, `actual_outcome`, `measurement_window_days`, `measurement_status`, `attribution_status`, `evidence_refs`, `agent_run_id`, `dispatch_attempt_id`, `lesson_summary`, `confidence_score`, `next_task_needed`, `created_at`, and `updated_at`. Doctrine prose carries zero banned-lexicon matches.
    Step 2: Create `supabase/migrations/learning_artifact_performance_log.sql` and `lib/markos/learning/artifact-performance.ts` so `measurement_status` is constrained to `expected_only | measured | degraded | missing_attribution` and `attribution_status` is constrained to `ready | degraded | unavailable`. The four `measurement_status` literals and three `attribution_status` literals are the canonical state vocabulary downstream surfaces will render as `.c-badge--{state}` chips per 212-UI-SPEC §Downstream UI Inheritance Map row 1.
    Step 3: Export the new learning contracts from `lib/markos/learning/contracts.ts`, `contracts.cjs`, and `index.cjs`. The `expected_outcome` and `actual_outcome` fields are seeded from P211 `MeasurementHandoff.expected_outcome` and `MeasurementHandoff.actual_outcome` respectively (per 212-UI-SPEC §Translation Gate Dissolution row 4 — this dissolves the 211 `learning_ready` placeholder).
    Step 4: Add domain-1 tests that cover expected-only, measured, degraded, and missing-attribution states.
  </action>
  <acceptance_criteria>
    - `.planning/literacy/artifact-performance-log-contract.md` contains the exact field names `expected_outcome`, `actual_outcome`, `measurement_status`, `attribution_status`, and `next_task_needed`
    - `lib/markos/learning/artifact-performance.ts` contains the exact status literals `expected_only`, `measured`, `degraded`, and `missing_attribution`
    - `lib/markos/learning/artifact-performance.ts` contains the exact attribution literals `ready`, `degraded`, and `unavailable`
    - `test/literacy/phase-212/domain-1/outcome-logging.test.js` contains the exact strings `expected_only`, `measured`, `degraded`, and `missing_attribution`
    - `.planning/literacy/artifact-performance-log-contract.md` contains zero matches against the CLAUDE.md banned lexicon (per 212-UI-SPEC §DESIGN.md Compliance Assertions row `Banned lexicon`)
    - `.planning/literacy/artifact-performance-log-contract.md` contains zero matches against the three architecture-lock forbidden strings `promote raw tenant copy`, `auto-promote literacy`, `silent prompt drift` (per 212-UI-SPEC §DESIGN.md Compliance Assertions row `Architecture-lock forbidden strings`)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/literacy/phase-212/domain-1/</automated>
  </verify>
  <done>Expected-vs-actual outcome capture is now explicit, explainable, and grounded in real evidence and attribution posture; the 19-field envelope and 7-literal state vocabulary are published verbatim for downstream UI inheritance per 212-UI-SPEC §Downstream row 1.</done>
</task>
</tasks>

<verification>
- npm test -- test/literacy/phase-212/preflight/
- npm test -- test/literacy/phase-212/domain-1/
</verification>

<success_criteria>
- Upstream readiness artifacts exist for P206/P207/P208/P209/P211
- Unsafe learning shortcuts are blocked by architecture lock (3 forbidden strings + 2 carved-out scope-restricted strings + banned lexicon zero-match)
- ArtifactPerformanceLog has explicit fields and degraded-state vocabulary published verbatim for downstream UI binding
- Translation gates dissolved: 209 learning-fixture evidence ledger row, 209 `inferred` learning-derived placeholder, 211 `learning_ready` placeholder (per 212-UI-SPEC §Translation Gate Dissolution and Opening)
- Validation strategy is seeded before later plans execute
</success_criteria>
