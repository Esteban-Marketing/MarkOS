---
phase: 212
plan: "04"
type: execute
wave: 4
ui_scope: NO_UI
depends_on:
  - "212-01"
  - "212-02"
  - "212-03"
f_ids: []
migration_slots: []
integrates_with:
  - "COMP-01 from P206 (privacy and compliance language must remain aligned with the SOC2 learning-risk posture; `data.export` mutation-class doctrine for any export of the audit trail or threshold configuration)"
  - "EVD-01..06 from P209 (promotion evidence must keep source-quality and freshness semantics; immutable-evidence pattern carry-forward for anonymization audit trail)"
translation_gate_for: []
files_modified:
  - .planning/literacy/cross-tenant-anonymization-policy.md
  - .planning/literacy/sample-size-thresholds.md
  - supabase/migrations/learning_cross_tenant_privacy_rules.sql
  - lib/markos/learning/anonymization.ts
  - lib/markos/learning/aggregation.ts
  - lib/markos/learning/privacy-rules.ts
  - lib/markos/learning/index.cjs
  - test/literacy/phase-212/domain-4/anonymization-transform.test.js
  - test/literacy/phase-212/domain-4/privacy-denial.test.js
  - test/literacy/phase-212/domain-4/sample-threshold.test.js
  - .planning/phases/212-learning-literacy-evolution/212-VALIDATION.md
autonomous: true
requirements:
  - LRN-04
  - QA-11
  - QA-12
  - QA-13
must_haves:
  truths:
    - "Cross-tenant learning payloads cannot include tenant identifiers, person names, customer names, contact data, raw pricing, support transcripts, or proprietary operator notes."
    - "Promotion candidates below `sample_size >= 3`, `supporting_artifact_count >= 5`, or `average_confidence_score >= 0.70` are blocked from central promotion."
    - "Privacy-unsafe payloads are rejected before admin review."
    - "Below-threshold learnings stay local as TenantOverlay candidates instead of being forced upward."
    - "The eight privacy-denylist literals (`tenant identifiers`, `person names`, `customer names`, `contact details`, `raw pricing`, `support transcripts`, `campaign copy that still reveals tenant identity`, `proprietary operator notes`) are the canonical denylist vocabulary for the future Anonymization audit trail surface (per 212-UI-SPEC §Downstream UI Inheritance Map row 6) — they MUST appear verbatim in `cross-tenant-anonymization-policy.md` and `lib/markos/learning/privacy-rules.ts`."
    - "The three threshold literals (`sample_size >= 3`, `supporting_artifact_count >= 5`, `average_confidence_score >= 0.70`) are the canonical threshold vocabulary for the future Privacy threshold viewer surface (per 212-UI-SPEC §Downstream UI Inheritance Map row 9) — they MUST appear verbatim in `sample-size-thresholds.md` and the threshold values `3`, `5`, `0.70` MUST appear verbatim in `lib/markos/learning/privacy-rules.ts`."
    - "The architecture-lock additional doctrine forbidden strings (`single tenant raw lesson` allowed only in 212-03 endpoint, `must not apply customer-facing mutations directly` allowed only in 212-05 endpoint) MUST NOT appear in this plan's surface — this plan is NOT either carved-out scope."
    - "Anonymization audit trail and Privacy threshold viewer are the two future admin surfaces (per 212-UI-SPEC §Downstream UI Inheritance Map rows 6 and 9) that consume this plan's doctrine verbatim."
  artifacts:
    - path: .planning/literacy/cross-tenant-anonymization-policy.md
      provides: denylist and redaction rules for central-learning eligibility (consumed verbatim by 212-UI-SPEC §Downstream row 6 'Anonymization audit trail' and row 9 'Privacy threshold viewer')
    - path: .planning/literacy/sample-size-thresholds.md
      provides: exact aggregation and confidence thresholds for promotion eligibility (consumed verbatim by 212-UI-SPEC §Downstream row 9 'Privacy threshold viewer')
    - path: lib/markos/learning/privacy-rules.ts
      provides: enforcement helpers for privacy denials and threshold gating (sources the 8 denylist literals and 3 threshold values for the future Anonymization audit trail surface)
  key_links:
    - from: .planning/phases/209-evidence-research-and-claim-safety/209-RESEARCH.md
      to: lib/markos/learning/privacy-rules.ts
      via: source-quality and freshness posture should be inherited, not reinterpreted locally
      pattern: "source_quality_score"
    - from: .planning/phases/206-soc2-type1-foundation/206-RESEARCH.md
      to: .planning/literacy/cross-tenant-anonymization-policy.md
      via: Phase 206 already identifies learning governance as a compliance risk family
      pattern: "Learning governance"
---

<ui_spec_fold>
ui_scope: NO_UI

ui_spec_block:
  - 212-UI-SPEC.md §parent_doctrine_chain (frontmatter) — inheritance from 206 (mutation-class `data.export` for audit-trail / threshold-configuration export), 209 (immutable-evidence pattern carry-forward; source-quality + freshness posture inheritance)
  - 212-UI-SPEC.md §Downstream UI Inheritance Map row 6 — Anonymization audit trail (admin-visible privacy-denial log; future P208 admin extension or future enterprise compliance surface). Per-event audit trail with: timestamp, tenant_id (redacted for cross-tenant denials), denial_reason (one of the 8 denylist literals — `tenant identifiers`, `person names`, `customer names`, `contact details`, `raw pricing`, `support transcripts`, `campaign copy that still reveals tenant identity`, `proprietary operator notes`), threshold_breach (one of: `sample_size_below_3`, `supporting_artifact_count_below_5`, `average_confidence_below_0.70`), source_overlay_ref, reviewer_id (if any). Denial events render as immutable rows (mirroring 209 evidence immutability). The eight denylist literals render as `.c-badge--error` chips verbatim. Threshold-breach literals render as `.c-badge--warning` chips. D-14 vanilla-table recipe. Cross-binding: 206 `data.export` mutation-class doctrine for any export of the audit trail itself; 209 immutable-evidence pattern carry-forward.
  - 212-UI-SPEC.md §Downstream UI Inheritance Map row 9 — Privacy threshold viewer (admin surface for sample-size / confidence thresholds; future P208 admin extension or future enterprise compliance). Read-only panel of the active thresholds: `sample_size >= 3`, `supporting_artifact_count >= 5`, `average_confidence_score >= 0.70`. Renders as a `.c-card` panel with each threshold formatted as a key-value pair (label + value chip + threshold-rationale prose). The eight denylist literals (per anonymization policy) render as `.c-badge--error` chips below the threshold panel. NEVER editable from this surface — threshold changes route through doctrine update + admin promotion review. Cross-binding: 206 `data.export` mutation-class doctrine for any export of threshold configuration.
  - 212-UI-SPEC.md §Copywriting Contract — Privacy-denylist copy rule (this plan owns AC for 8 denylist literals appearing verbatim per 212-04-01 AC); Threshold-value copy rule (this plan owns AC for 3 threshold strings appearing verbatim per 212-04-01 AC); Banned-lexicon row applies to both `cross-tenant-anonymization-policy.md` and `sample-size-thresholds.md` doctrine prose.
  - 212-UI-SPEC.md §DESIGN.md Compliance Assertions — Privacy-denylist doctrine row (ENFORCED — `cross-tenant-anonymization-policy.md` contains 8 denylist literals verbatim); Threshold-value doctrine row (ENFORCED — `sample-size-thresholds.md` contains 3 threshold strings verbatim AND `lib/markos/learning/privacy-rules.ts` contains threshold values `3`, `5`, `0.70` verbatim).

cross_cutting_carry_forward:
  - "Banned lexicon (CLAUDE.md): zero matches in `.planning/literacy/cross-tenant-anonymization-policy.md` and `.planning/literacy/sample-size-thresholds.md`. No exclamation points in doctrine prose. Verified by 212-01-00 architecture-lock CI script (per 212-UI-SPEC §DESIGN.md Compliance Assertions row `Architecture-lock forbidden strings`)."
  - "Architecture-lock forbidden strings: zero matches against `promote raw tenant copy`, `auto-promote literacy`, `silent prompt drift`, `single tenant raw lesson` (this plan is NOT the carved-out scope — that is 212-03), `must not apply customer-facing mutations directly` (this plan is NOT the carved-out scope — that is 212-05), `vitest`, `playwright`, `.test.ts`, `route.ts` across this plan's doctrine, runtime, and test files. Enforced by 212-01-00 `scripts/literacy/check-learning-architecture-lock.mjs`."
  - "Privacy-denylist literals (8 verbatim per 212-04-01 AC + 212-UI-SPEC §DESIGN.md Compliance Assertions row `Privacy-denylist doctrine`): `tenant identifiers`, `person names`, `customer names`, `contact details`, `raw pricing`, `support transcripts`, `campaign copy that still reveals tenant identity`, `proprietary operator notes`. No rephrasing, no abbreviation, no synonyms. MUST appear verbatim in `cross-tenant-anonymization-policy.md` AND in `lib/markos/learning/privacy-rules.ts` (so the runtime can match them when rejecting payloads)."
  - "Threshold-value literals (3 verbatim per 212-04-01 AC + 212-UI-SPEC §DESIGN.md Compliance Assertions row `Threshold-value doctrine`): `sample_size >= 3`, `supporting_artifact_count >= 5`, `average_confidence_score >= 0.70`. MUST appear verbatim in `sample-size-thresholds.md`. The threshold values `3`, `5`, and `0.70` MUST appear verbatim in `lib/markos/learning/privacy-rules.ts`."
  - "Mutation-class doctrine (206-02): `data.export` binds to any future export of the anonymization audit trail (212-UI-SPEC §Downstream row 6 cross-binding) AND any future export of threshold configuration (212-UI-SPEC §Downstream row 9 cross-binding). NOT directly authored by this plan — declared as forward-binding doctrine for the consuming surfaces."
  - "Immutable-evidence pattern (209 carry-forward): denial events in the future Anonymization audit trail render as immutable rows mirroring 209 evidence immutability (per 212-UI-SPEC §Downstream row 6). NOT directly authored by this plan — declared as forward-binding doctrine for the consuming surface."
  - "Node API error envelopes: the 8 denylist literals correspond to error_code literals like `candidate-anonymization-denied-tenant-identifiers`, `candidate-anonymization-denied-person-names`, `candidate-anonymization-denied-customer-names`, `candidate-anonymization-denied-contact-details`, `candidate-anonymization-denied-raw-pricing`, `candidate-anonymization-denied-support-transcripts`, `candidate-anonymization-denied-campaign-identity`, `candidate-anonymization-denied-operator-notes` (per 212-UI-SPEC §Copywriting Contract Node API error envelopes section). The 3 threshold breaches correspond to `candidate-below-sample-threshold`, `candidate-below-confidence-threshold`, `candidate-below-supporting-artifact-threshold`."

downstream_ui_consumers_read_only:
  - "P208 admin extension or future enterprise compliance — Anonymization audit trail (212-UI-SPEC §Downstream row 6). Reads denial events with the 8 denylist literals (rendered as `.c-badge--error` chips) AND the 3 threshold-breach literals (rendered as `.c-badge--warning` chips). Immutable rows. NOT modified by this plan."
  - "P208 admin extension or future enterprise compliance — Privacy threshold viewer (212-UI-SPEC §Downstream row 9). Reads the 3 threshold values verbatim AND the 8 denylist literals verbatim. Read-only panel. NOT modified by this plan."
  - "P208 Approval Inbox extension (212-UI-SPEC §Downstream row 4) — reads threshold-status panel from this plan's `sample-size-thresholds.md` to render below-threshold `.c-notice c-notice--warning` 'Below promotion threshold — local-only' chips on candidate detail. NOT modified by this plan."
</ui_spec_fold>

<objective>
Define the privacy boundary for learning promotion: anonymization, aggregation, redaction, consent, and threshold rules that stop unsafe or low-confidence cross-tenant learning from becoming doctrine.
</objective>

<context>
@.planning/phases/212-learning-literacy-evolution/212-RESEARCH.md
@.planning/phases/212-learning-literacy-evolution/212-VALIDATION.md
@.planning/phases/212-learning-literacy-evolution/212-UI-SPEC.md
</context>

<tasks>
<task id="212-04-01">
  <name>Cross-tenant anonymization, redaction, and threshold gates</name>
  <files>.planning/literacy/cross-tenant-anonymization-policy.md, .planning/literacy/sample-size-thresholds.md, supabase/migrations/learning_cross_tenant_privacy_rules.sql, lib/markos/learning/anonymization.ts, lib/markos/learning/aggregation.ts, lib/markos/learning/privacy-rules.ts, test/literacy/phase-212/domain-4/anonymization-transform.test.js, test/literacy/phase-212/domain-4/privacy-denial.test.js, test/literacy/phase-212/domain-4/sample-threshold.test.js</files>
  <read_first>
    - .planning/phases/212-learning-literacy-evolution/212-RESEARCH.md section "Domain 4 - Cross-tenant anonymization and privacy thresholds"
    - .planning/phases/212-learning-literacy-evolution/212-UI-SPEC.md §Downstream UI Inheritance Map row 6 (Anonymization audit trail) and row 9 (Privacy threshold viewer)
    - .planning/phases/212-learning-literacy-evolution/212-UI-SPEC.md §Copywriting Contract (Privacy-denylist copy rule + Threshold-value copy rule + Node API error envelopes)
    - .planning/phases/212-learning-literacy-evolution/212-UI-SPEC.md §DESIGN.md Compliance Assertions (Privacy-denylist doctrine row + Threshold-value doctrine row)
    - .planning/phases/209-evidence-research-and-claim-safety/209-RESEARCH.md (source-quality + freshness posture; immutable-evidence pattern)
    - .planning/phases/206-soc2-type1-foundation/206-RESEARCH.md (mutation-class `data.export` doctrine)
    - lib/markos/crm/attribution.ts
  </read_first>
  <action>
    Step 1: Create `.planning/literacy/cross-tenant-anonymization-policy.md` with the exact denylist items `tenant identifiers`, `person names`, `customer names`, `contact details`, `raw pricing`, `support transcripts`, `campaign copy that still reveals tenant identity`, and `proprietary operator notes`. Doctrine prose carries zero banned-lexicon matches. Per 212-UI-SPEC §Downstream row 6, these 8 literals will be rendered verbatim as `.c-badge--error` chips by the future Anonymization audit trail surface.
    Step 2: Create `.planning/literacy/sample-size-thresholds.md` with the exact thresholds `sample_size >= 3`, `supporting_artifact_count >= 5`, and `average_confidence_score >= 0.70`. Per 212-UI-SPEC §Downstream row 9, these 3 strings will be rendered verbatim by the future Privacy threshold viewer surface.
    Step 3: Create `supabase/migrations/learning_cross_tenant_privacy_rules.sql` plus `lib/markos/learning/anonymization.ts`, `aggregation.ts`, and `privacy-rules.ts` so below-threshold or privacy-unsafe payloads are blocked before admin review. The `privacy-rules.ts` module MUST contain the 8 denylist literals verbatim AND the 3 threshold values `3`, `5`, `0.70` verbatim — so the runtime can match them when rejecting payloads (per 212-UI-SPEC §DESIGN.md Compliance Assertions row `Privacy-denylist doctrine` + `Threshold-value doctrine`).
    Step 4: Add domain-4 tests for anonymization transforms, privacy denials, and threshold gating. The `privacy-denial.test.js` test asserts the 8 denylist literals appear in test fixture rejections; the `sample-threshold.test.js` test asserts the 3 threshold values appear in test fixture rejections.
  </action>
  <acceptance_criteria>
    - `.planning/literacy/cross-tenant-anonymization-policy.md` contains the exact denylist strings `tenant identifiers`, `person names`, `customer names`, `contact details`, `raw pricing`, `support transcripts`, `campaign copy that still reveals tenant identity`, and `proprietary operator notes` (all 8 verbatim per 212-UI-SPEC §DESIGN.md Compliance Assertions row `Privacy-denylist doctrine`)
    - `.planning/literacy/sample-size-thresholds.md` contains the exact strings `sample_size >= 3`, `supporting_artifact_count >= 5`, and `average_confidence_score >= 0.70` (all 3 verbatim per 212-UI-SPEC §DESIGN.md Compliance Assertions row `Threshold-value doctrine`)
    - `lib/markos/learning/privacy-rules.ts` contains the 8 denylist literals verbatim (per 212-UI-SPEC §DESIGN.md Compliance Assertions row `Privacy-denylist doctrine`)
    - `lib/markos/learning/privacy-rules.ts` contains the exact threshold values `3`, `5`, and `0.70` (per 212-UI-SPEC §DESIGN.md Compliance Assertions row `Threshold-value doctrine`)
    - `test/literacy/phase-212/domain-4/privacy-denial.test.js` contains the exact denylist strings `customer names` and `raw pricing` (representative subset; full 8-string match enforced via privacy-rules.ts and policy doctrine)
    - `.planning/literacy/cross-tenant-anonymization-policy.md` contains zero matches against the CLAUDE.md banned lexicon (per 212-UI-SPEC §DESIGN.md Compliance Assertions row `Banned lexicon`)
    - `.planning/literacy/sample-size-thresholds.md` contains zero matches against the CLAUDE.md banned lexicon
    - `.planning/literacy/cross-tenant-anonymization-policy.md` AND `.planning/literacy/sample-size-thresholds.md` contain zero matches against the three architecture-lock forbidden strings `promote raw tenant copy`, `auto-promote literacy`, `silent prompt drift` AND zero matches against `single tenant raw lesson` (this plan is NOT the carved-out scope) AND zero matches against `must not apply customer-facing mutations directly` (this plan is NOT the carved-out scope)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/literacy/phase-212/domain-4/</automated>
  </verify>
  <done>Cross-tenant learning is now privacy-gated, threshold-gated, and explicitly blocked from central promotion when it stays too specific or too weak; the 8 privacy-denylist literals and 3 threshold values are published verbatim in both doctrine and runtime for downstream UI inheritance per 212-UI-SPEC §Downstream rows 6 and 9.</done>
</task>
</tasks>

<verification>
- npm test -- test/literacy/phase-212/domain-4/
</verification>

<success_criteria>
- 8 privacy-denylist literals appear verbatim in `cross-tenant-anonymization-policy.md` AND `lib/markos/learning/privacy-rules.ts`
- 3 threshold strings appear verbatim in `sample-size-thresholds.md`; threshold values `3`, `5`, `0.70` appear verbatim in `lib/markos/learning/privacy-rules.ts`
- Denylist and threshold rules are explicit
- Privacy-unsafe payloads are rejected before review
- Below-threshold learnings remain local
- Cross-tenant promotion inherits evidence and compliance posture instead of bypassing it
- Mutation-class `data.export` doctrine declared for future audit-trail / threshold-config export per 206-02
- Banned-lexicon zero-match enforced; architecture-lock forbidden strings zero-match enforced (this plan is not either carved-out scope)
</success_criteria>
