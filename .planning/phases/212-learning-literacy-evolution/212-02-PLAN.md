---
phase: 212
plan: "02"
type: execute
wave: 2
ui_scope: NO_UI
depends_on:
  - "212-01"
f_ids: []
migration_slots: []
integrates_with:
  - "EVD-01..06 from P209 (overlay evidence refs must inherit EvidenceMap safety posture)"
  - "RUN-01..08 from P207 (overlays should be able to point back to source run context)"
translation_gate_for: []
files_modified:
  - .planning/literacy/tenant-overlay-policy.md
  - supabase/migrations/learning_tenant_overlays.sql
  - lib/markos/learning/tenant-overlay.ts
  - lib/markos/learning/overlay-merge.ts
  - lib/markos/learning/overlay-review.ts
  - lib/markos/learning/index.cjs
  - test/literacy/phase-212/domain-2/tenant-overlay-rls.test.js
  - test/literacy/phase-212/domain-2/overlay-expiry.test.js
  - test/literacy/phase-212/domain-2/overlay-merge-rules.test.js
  - test/literacy/phase-212/domain-2/overlay-review-state.test.js
  - .planning/phases/212-learning-literacy-evolution/212-VALIDATION.md
autonomous: true
requirements:
  - LRN-03
  - QA-05
  - QA-06
  - QA-07
must_haves:
  truths:
    - "TenantOverlay records stay tenant-scoped and RLS-protected."
    - "Every overlay carries confidence, provenance, evidence refs, expiry, and review status."
    - "Expired overlays cannot silently remain active."
    - "Overlay merge logic is explicit instead of informal prompt drift."
    - "TenantOverlay 14-field envelope (per 212-UI-SPEC §Downstream UI Inheritance Map row 2, 'TenantOverlay browser') is published verbatim so the future P208 admin extension or future enterprise admin surface can bind without re-deriving field names."
    - "The five `review_status` literals (`proposed`, `active`, `expiring`, `suppressed`, `rejected`) are the canonical state vocabulary for downstream `.c-badge--{info,success,warning,error,error}` rendering and the canonical filter set for the future Suppression list browser (per 212-UI-SPEC §Downstream UI Inheritance Map row 2 + row 7)."
    - "RLS posture at the database layer feeds the future TenantOverlay browser's tenant-scoped read pattern (per 212-UI-SPEC §Downstream UI Inheritance Map row 2)."
    - "213.4 D-08 token-only carry-forward applies to the future TenantOverlay browser surface — `confidence_score` percent chip, `expires_at` relative-time formatting, and `evidence_refs` mint-as-text chips MUST use DESIGN.md tokens only when downstream phase ships (per 212-UI-SPEC §Downstream UI Inheritance Map carry-forward table D-08)."
  artifacts:
    - path: .planning/literacy/tenant-overlay-policy.md
      provides: canonical TenantOverlay fields, states, expiry, and suppression rules (consumed verbatim by 212-UI-SPEC §Downstream row 2 'TenantOverlay browser' and row 7 'Suppression list browser')
    - path: lib/markos/learning/overlay-merge.ts
      provides: deterministic merge and suppression behavior for tenant-local learning
  key_links:
    - from: onboarding/backend/mir-lineage.cjs
      to: lib/markos/learning/tenant-overlay.ts
      via: append-only provenance and rationale posture should be mirrored for overlay creation and updates (downstream MIR-lineage deep-link consumed by 212-UI-SPEC §Downstream row 2 `provenance_ref` chip)
      pattern: "rationale"
    - from: onboarding/backend/literacy/discipline-selection.cjs
      to: .planning/literacy/tenant-overlay-policy.md
      via: discipline-scoped activation evidence informs which overlays are valid for a tenant
      pattern: "selected_disciplines"
---

<ui_spec_fold>
ui_scope: NO_UI

ui_spec_block:
  - 212-UI-SPEC.md §parent_doctrine_chain (frontmatter) — inheritance from 209 (EvidenceMap binding for `evidence_refs`), MIR lineage substrate (provenance_ref chip target), 207 (downstream agent_run_id linkage)
  - 212-UI-SPEC.md §Translation Gate Dissolution and Opening — this plan CONTRIBUTES to dissolving the 209 §Downstream learning-fixture evidence ledger gate (TenantOverlay.evidence_refs is the second source — alongside ArtifactPerformanceLog.evidence_refs from 212-01 — that the future evidence-ledger surface composes via `<EvidenceSummary />`).
  - 212-UI-SPEC.md §Downstream UI Inheritance Map row 2 — TenantOverlay browser (future P208 admin extension or future enterprise admin). 14-field `TenantOverlay` envelope verbatim (`overlay_id`, `tenant_id`, `discipline`, `lesson_type`, `overlay_payload`, `confidence_score`, `evidence_refs`, `provenance_ref`, `review_status`, `suppression_reason`, `expires_at`, `review_by`, `created_at`, `updated_at`). Five `review_status` literals (`proposed`, `active`, `expiring`, `suppressed`, `rejected`) render as state badges with `.c-badge--{info,success,warning,error,error}` mapping. `expires_at` renders with relative-time token formatting; rows where `expires_at` < now AND `review_status == 'active'` compose `.c-notice c-notice--warning` "[warn] Overlay expired — review required". `suppression_reason` renders inline as `.c-notice c-notice--info` when present. `confidence_score` renders as percent-formatted chip with token-only color thresholds. `evidence_refs` chips compose `.c-chip-protocol` (mint-as-text per D-09) deep-linked to 209 `<EvidenceSummary />`. `provenance_ref` renders as deep-link chip to MIR lineage record (per 212-02 key_links). `overlay_payload` renders as a `.c-code-block` with safe JSON formatting. D-14 vanilla-table recipe for the list view. Cross-binding: 209 `EvidenceMap` via `evidence_refs`, MIR lineage substrate via `provenance_ref`.
  - 212-UI-SPEC.md §Downstream UI Inheritance Map row 7 — Suppression list browser (future P208 admin extension or future enterprise admin). "Suppressed overlays" tab reads `TenantOverlay` rows where `review_status == 'suppressed'` OR `expires_at < now`; `suppression_reason` renders as `.c-notice c-notice--info`. Reactivation actions compose `.c-modal` + `.c-button--destructive` confirm gate (because un-suppression can re-introduce learning that was deliberately silenced). Cross-binding: 206 mutation-class doctrine (un-suppress binds to underlying record's mutation-class, typically `public.claim` if overlay would resurface in central literacy after un-suppression).
  - 212-UI-SPEC.md §Copywriting Contract — Banned-lexicon row (this plan ships `tenant-overlay-policy.md`); Architecture-lock forbidden strings row (zero matches required); Pricing-placeholder rule row (not directly applicable to overlay payloads but inherited if `overlay_payload` JSON references upstream pricing context).

cross_cutting_carry_forward:
  - "Banned lexicon (CLAUDE.md): zero matches in `.planning/literacy/tenant-overlay-policy.md`. No exclamation points in doctrine prose. Verified by 212-01-00 architecture-lock CI script (per 212-UI-SPEC §DESIGN.md Compliance Assertions row `Architecture-lock forbidden strings`)."
  - "Architecture-lock forbidden strings: zero matches against `promote raw tenant copy`, `auto-promote literacy`, `silent prompt drift`, `single tenant raw lesson` (this plan is NOT the carved-out scope), `must not apply customer-facing mutations directly` (this plan is NOT the carved-out scope), `vitest`, `playwright`, `.test.ts`, `route.ts` across this plan's doctrine, runtime, and test files. Enforced by 212-01-00 `scripts/literacy/check-learning-architecture-lock.mjs`."
  - "Pricing placeholder rule: not directly applicable to TenantOverlay records (overlays are tenant-local learning, not pricing). However, if an `overlay_payload` JSONB blob references upstream pricing context derived from P211, the placeholder discipline is inherited from 212-01 `ArtifactPerformanceLog` rules — `{{MARKOS_PRICING_ENGINE_PENDING}}` is the only accepted placeholder."
  - "Engineering-readability prose: short sentences, concrete contract field names, named owning plans. No marketing voice. No hedging. Every contract row asserts a specific field, literal, or test-gate (per 212-UI-SPEC §Copywriting Contract Engineering-readability rule)."
  - "Mutation-class doctrine inheritance (206-02): TenantOverlay reads in this plan are NOT mutation-class-bound. However, the future Suppression list browser (212-UI-SPEC §Downstream row 7) un-suppress action will bind to `public.claim` per 206-02 if un-suppression resurfaces overlays into central literacy. NOT authored by this plan."
  - "213.4 D-08 token-only carry-forward: when the future TenantOverlay browser ships, every chip color (confidence_score thresholds, review_status badges) MUST resolve via `var(--color-*)` tokens; no inline hex literals. NOT enforced in this plan — declared as forward-binding doctrine for the consuming surface."

downstream_ui_consumers_read_only:
  - "P208 admin extension or future enterprise admin — TenantOverlay browser (212-UI-SPEC §Downstream row 2). Reads 14-field `TenantOverlay` envelope. Renders 5 `review_status` literals as `.c-badge--{state}` chips and `expires_at` with relative-time token formatting. NOT modified by this plan."
  - "P208 admin extension or future enterprise admin — Suppression list browser (212-UI-SPEC §Downstream row 7). Reads `TenantOverlay` rows where `review_status == 'suppressed'` OR `expires_at < now`. NOT modified by this plan."
  - "P208 admin extension or P209-evidence consumer phase — Learning-fixture evidence ledger per fixture (212-UI-SPEC §Downstream row 11). Reads `TenantOverlay.evidence_refs` AND `provenance_ref` (when fixture has been promoted to overlay). Cross-source composition with `ArtifactPerformanceLog.evidence_refs`. NOT modified by this plan."
</ui_spec_fold>

<objective>
Create the tenant-local learning layer: TenantOverlay records with RLS, confidence, provenance, expiry, suppression, and deterministic merge behavior.
</objective>

<context>
@.planning/phases/212-learning-literacy-evolution/212-RESEARCH.md
@.planning/phases/212-learning-literacy-evolution/212-VALIDATION.md
@.planning/phases/212-learning-literacy-evolution/212-UI-SPEC.md
</context>

<tasks>
<task id="212-02-01">
  <name>TenantOverlay schema, merge rules, and review cadence</name>
  <files>.planning/literacy/tenant-overlay-policy.md, supabase/migrations/learning_tenant_overlays.sql, lib/markos/learning/tenant-overlay.ts, lib/markos/learning/overlay-merge.ts, lib/markos/learning/overlay-review.ts, test/literacy/phase-212/domain-2/tenant-overlay-rls.test.js, test/literacy/phase-212/domain-2/overlay-expiry.test.js, test/literacy/phase-212/domain-2/overlay-merge-rules.test.js, test/literacy/phase-212/domain-2/overlay-review-state.test.js</files>
  <read_first>
    - .planning/phases/212-learning-literacy-evolution/212-RESEARCH.md section "Domain 2 - TenantOverlay local learning"
    - .planning/phases/212-learning-literacy-evolution/212-UI-SPEC.md §Downstream UI Inheritance Map row 2 (TenantOverlay browser) and row 7 (Suppression list browser) and row 11 (Learning-fixture evidence ledger per fixture)
    - .planning/phases/212-learning-literacy-evolution/212-UI-SPEC.md §Translation Gate Dissolution and Opening (this plan contributes to dissolving the 209 learning-fixture evidence ledger gate via `TenantOverlay.evidence_refs`)
    - onboarding/backend/mir-lineage.cjs
    - onboarding/backend/literacy/discipline-selection.cjs
    - .planning/codebase/LITERACY-OPERATIONS.md
  </read_first>
  <action>
    Step 1: Create `.planning/literacy/tenant-overlay-policy.md` with the exact fields `overlay_id`, `tenant_id`, `discipline`, `lesson_type`, `overlay_payload`, `confidence_score`, `evidence_refs`, `provenance_ref`, `review_status`, `suppression_reason`, `expires_at`, `review_by`, `created_at`, and `updated_at`. Doctrine prose carries zero banned-lexicon matches.
    Step 2: Create `supabase/migrations/learning_tenant_overlays.sql` and `lib/markos/learning/tenant-overlay.ts` so `review_status` is constrained to `proposed | active | expiring | suppressed | rejected`. The five literals are the canonical state vocabulary downstream surfaces will render as `.c-badge--{info,success,warning,error,error}` chips per 212-UI-SPEC §Downstream UI Inheritance Map row 2. RLS posture at the migration layer enforces tenant-scoped reads — the future TenantOverlay browser inherits this RLS for its tenant-scoped read pattern.
    Step 3: Implement `lib/markos/learning/overlay-merge.ts` and `overlay-review.ts` so lower-confidence or expired overlays are suppressed instead of silently winning merges.
    Step 4: Add domain-2 tests for RLS, expiry, merge rules, and review-state transitions.
  </action>
  <acceptance_criteria>
    - `.planning/literacy/tenant-overlay-policy.md` contains the exact field names `confidence_score`, `provenance_ref`, `suppression_reason`, `expires_at`, and `review_by`
    - `lib/markos/learning/tenant-overlay.ts` contains the exact status literals `proposed`, `active`, `expiring`, `suppressed`, and `rejected`
    - `lib/markos/learning/overlay-merge.ts` contains the exact strings `suppressed` and `expiring`
    - `test/literacy/phase-212/domain-2/overlay-expiry.test.js` contains the exact state names `active`, `expiring`, and `suppressed`
    - `.planning/literacy/tenant-overlay-policy.md` contains zero matches against the CLAUDE.md banned lexicon (per 212-UI-SPEC §DESIGN.md Compliance Assertions row `Banned lexicon`)
    - `.planning/literacy/tenant-overlay-policy.md` contains zero matches against the three architecture-lock forbidden strings `promote raw tenant copy`, `auto-promote literacy`, `silent prompt drift` (per 212-UI-SPEC §DESIGN.md Compliance Assertions row `Architecture-lock forbidden strings`)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/literacy/phase-212/domain-2/</automated>
  </verify>
  <done>Tenant-local learning is now durable, scoped, reviewable, and protected from stale or low-confidence overlays lingering as hidden doctrine; the 14-field envelope and 5-literal state vocabulary are published verbatim for downstream UI inheritance per 212-UI-SPEC §Downstream rows 2 and 7.</done>
</task>
</tasks>

<verification>
- npm test -- test/literacy/phase-212/domain-2/
</verification>

<success_criteria>
- TenantOverlay fields and states are explicit and published verbatim for downstream UI binding
- Overlay records are tenant-scoped and RLS-protected (RLS posture inherited by future TenantOverlay browser)
- Expiry and suppression behavior is deterministic
- Merge rules prevent invisible prompt drift
- Banned-lexicon zero-match enforced; architecture-lock forbidden strings zero-match enforced
</success_criteria>
