---
phase: 212
plan: "05"
type: execute
wave: 5
ui_scope: NO_UI
depends_on:
  - "212-01"
  - "212-02"
  - "212-03"
  - "212-04"
f_ids: []
migration_slots: []
integrates_with:
  - "TASK-01..05 from P208 (learning must create governed work rather than mutate surfaces directly; Task Board is the consumer for `recommendation_kind == 'task_create'` handoff)"
  - "LOOP-01..08 from P211 (recommendations must point back to real loop outcomes; this plan dissolves the 211 `MeasurementHandoff.learning_ready` consumer placeholder by ingesting the handoff as `recommendation_kind` source signal)"
translation_gate_for:
  - "P213"
  - "P217"
  - "P218"
  - "P219"
  - "P220"
  - "P225"
files_modified:
  - .planning/literacy/learning-recommendation-handoff.md
  - .planning/literacy/growth-learning-compatibility-map.md
  - supabase/migrations/learning_recommendations.sql
  - lib/markos/learning/recommendation.ts
  - lib/markos/learning/growth-compatibility.ts
  - lib/markos/learning/index.cjs
  - api/v1/literacy/recommendations.js
  - test/literacy/phase-212/domain-5/recommendation-handoff.test.js
  - test/literacy/phase-212/domain-5/recommendation-expiry.test.js
  - test/literacy/phase-212/domain-5/growth-compatibility.test.js
  - .planning/phases/212-learning-literacy-evolution/212-VALIDATION.md
autonomous: true
requirements:
  - LRN-02
  - LRN-03
  - QA-14
  - QA-15
must_haves:
  truths:
    - "Learning creates governed tasks or strategy refresh work, not silent prompt drift."
    - "LearningRecommendation records carry owner, priority, expiry, evidence refs, and task linkage."
    - "Customer-facing or risky follow-up work remains approval-gated through existing task substrate."
    - "Doc 17 future learning categories remain `future_only` compatibility rows instead of becoming runnable automation in Phase 212."
    - "LearningRecommendation 12-field envelope (per 212-UI-SPEC §Downstream UI Inheritance Map row 5, 'LearningRecommendation Task Board feed') is published verbatim so the existing P208 Task Board (208-03) can bind via redeploy without re-deriving field names."
    - "The six `recommendation_kind` literals (`task_create`, `strategy_refresh`, `research_refresh`, `pricing_review`, `connector_fix`, `experiment_candidate`) are the canonical recommendation-class vocabulary for downstream chip rendering with the recipe `task_create` (info), `strategy_refresh` (info), `research_refresh` (info), `pricing_review` (warning), `connector_fix` (error), `experiment_candidate` (success) (per 212-UI-SPEC §Downstream UI Inheritance Map row 5)."
    - "This plan OPENS four downstream translation gates (per 212-UI-SPEC §Translation Gates Opened): (1) future_phase_213 Tenant 0 closeout consumers; (2) future_phase_217+ growth-learning-compatibility consumers; (3) future_phase_218..220 SaaS Marketing OS Strategy growth consumers (consume `recommendation_kind == 'experiment_candidate'`); (4) future_phase_225 Analytics + narrative intelligence consumers."
    - "This plan DISSOLVES the 211-UI-SPEC `MeasurementHandoff.learning_ready` consumer placeholder by ingesting the handoff and feeding `next_task_kind` literals (`brief_refresh`, `evidence_refresh`, `pricing_review`, `connector_recovery`, `social_follow_up`, `experiment_candidate` per 211-06-01) into `LearningRecommendation.recommendation_kind` (which adds `task_create`, `strategy_refresh`, `research_refresh` as a superset; per 212-UI-SPEC §Pre-Populated From row 211)."
    - "The literal `future_only` (NOT `future_consumer` which is the 211 vocabulary; NOT `planned`, `coming_soon`, `future_phase` which are synonyms forbidden by 212-05-02 AC) MUST appear verbatim for all 7 growth-module rows in `growth-learning-compatibility-map.md` AND in `lib/markos/learning/growth-compatibility.ts`."
    - "The `api/v1/literacy/recommendations.js` endpoint is the SOLE carved-out scope where the architecture-lock-restricted string `must not apply customer-facing mutations directly` may appear (per 212-UI-SPEC §Copywriting Contract carved-out scope rule)."
    - "Tenant 0 learning-readiness panel (per 212-UI-SPEC §Downstream UI Inheritance Map row 12) is the future P213 surface that proves dogfood readiness across all 4 learning record types from this phase."
  artifacts:
    - path: .planning/literacy/learning-recommendation-handoff.md
      provides: canonical handoff from logs or overlays into task or strategy work (consumed verbatim by 212-UI-SPEC §Downstream row 5 'LearningRecommendation Task Board feed' + row 7 'Suppression list browser' + row 12 'Tenant 0 learning-readiness panel' + row 13 'Recovery Center learning-failure variant')
    - path: .planning/literacy/growth-learning-compatibility-map.md
      provides: future-only mapping for PLG, ABM, community, events, PR, partnerships, and developer-marketing learning (consumed verbatim by 212-UI-SPEC §Downstream row 10 'Future-growth-learning compatibility browser')
    - path: api/v1/literacy/recommendations.js
      provides: governed recommendation-read surface without direct customer mutation; emits JSON envelopes consumed by future P208 Task Board redeploy
  key_links:
    - from: .planning/phases/208-human-operating-interface/208-RESEARCH.md
      to: .planning/literacy/learning-recommendation-handoff.md
      via: recommendations should land in governed task/brief workflows rather than bypass the operator substrate
      pattern: "Task Board"
    - from: .planning/phases/211-content-social-revenue-loop/211-06-PLAN.md
      to: lib/markos/learning/recommendation.ts
      via: next-task creation must stay tied to measured loop deltas; this plan ingests P211 `MeasurementHandoff.next_task_kind` literals as upstream feed for `recommendation_kind`
      pattern: "next-task recommendations"
---

<ui_spec_fold>
ui_scope: NO_UI

ui_spec_block:
  - 212-UI-SPEC.md §parent_doctrine_chain (frontmatter) — inheritance from 208 (PARENT — Task Board is the consumer for `recommendation_kind == 'task_create'`; Approval Inbox for approval-gated kinds; Recovery Center for learning-substrate failures; Morning Brief and Weekly Narrative for learning summaries), 211 (loop substrate; `MeasurementHandoff.learning_ready == true` is the binding signal; `next_task_kind` literals feed `recommendation_kind`)
  - 212-UI-SPEC.md §Translation Gate Dissolution and Opening — this plan DISSOLVES the 211-UI-SPEC §Translation Gates Opened `MeasurementHandoff.learning_ready == true` consumer placeholder (the 211 placeholder for "future_phase_212" learning-ledger consumers dissolves once `LearningRecommendation` ingests the handoff and feeds `recommendation_kind` from `next_task_kind`). This plan OPENS four NEW downstream gates: (1) `<PlaceholderBanner variant="future_phase_213_tenant0">` — Tenant 0 closeout consumers must prove ArtifactPerformanceLog has logged real outcomes AND LiteracyUpdateCandidate has at least one promoted entry on Tenant 0 data; (2) `<PlaceholderBanner variant="future_growth_learning_module">` — growth-learning-compatibility consumers (PLG, ABM, community, events, PR, partnerships, developer marketing) bind to P217+ growth-mode surfaces; (3) `<PlaceholderBanner variant="future_phase_218">` (or 219/220) — SaaS Marketing OS Strategy growth phases consume `recommendation_kind == 'experiment_candidate'` to seed growth-experiment registries; (4) `<PlaceholderBanner variant="future_phase_225">` — Analytics + narrative intelligence treats Phase 212 learning objects as governed inputs to narrative synthesis.
  - 212-UI-SPEC.md §Downstream UI Inheritance Map row 5 — LearningRecommendation Task Board feed (P208 Task Board 208-03 consumer; existing surface that will be redeployed). 12-field `LearningRecommendation` envelope verbatim (`recommendation_id`, `tenant_id`, `source_type`, `source_ref`, `recommendation_kind`, `priority`, `owner_role`, `status`, `expires_at`, `evidence_refs`, `task_id`, `suppression_reason`). Six `recommendation_kind` literals (`task_create`, `strategy_refresh`, `research_refresh`, `pricing_review`, `connector_fix`, `experiment_candidate`) render as recommendation-class chips with the recipe: `task_create` (info), `strategy_refresh` (info), `research_refresh` (info), `pricing_review` (warning), `connector_fix` (error), `experiment_candidate` (success). `priority` renders as priority chip (P0..P4 mapping per 207). `owner_role` renders as role chip (text-only, no fill). `expires_at` renders with relative-time token formatting; expired rows compose `.c-notice c-notice--warning`. `task_id` renders as deep-link chip to the P208 Task Board task. `evidence_refs[]` chips deep-link to 209 `<EvidenceSummary />`. `suppression_reason` renders inline as `.c-notice c-notice--info` when present. The recommendation feed itself uses D-14 vanilla-table recipe. Cross-binding: 208 task system owns the task envelope; 212 only generates the recommendation references. Mutation-class binding for actions on a recommendation is governed by the underlying linked task (which inherits its own mutation-class from 206-02).
  - 212-UI-SPEC.md §Downstream UI Inheritance Map row 7 — Suppression list browser (future P208 admin extension). "Suppressed recommendations" tab reads `LearningRecommendation` rows where `status == 'suppressed'` OR `expires_at < now`; `suppression_reason` renders as `.c-notice c-notice--info`.
  - 212-UI-SPEC.md §Downstream UI Inheritance Map row 10 — Future-growth-learning compatibility browser (admin; future P217+ growth phases or future enterprise admin). Surfaces seven growth-module rows verbatim (`plg`, `abm`, `community`, `events`, `pr`, `partnerships`, `developer_marketing`); every row renders the exact status `future_only` per 212-05-02 AC. Each row composes `.c-card` with `.c-badge--info` "[info] future_only" status. NEVER renders rows as runnable until corresponding growth phase ships (P218-P220 SaaS Growth phases per ROADMAP / REQUIREMENTS traceability, plus P226-P227 future commercial-engine phases). Until then, all rows render `<PlaceholderBanner variant="future_growth_learning_module">` as a guard.
  - 212-UI-SPEC.md §Downstream UI Inheritance Map row 12 — Tenant 0 learning-readiness panel (dogfood proof; future P213 Tenant 0 surface). Rows for `ArtifactPerformanceLog` (with `measurement_status == 'measured'`) count, `TenantOverlay` (with `review_status == 'active'`) count, `LiteracyUpdateCandidate` (with `decision == 'promoted'`) count, `LearningRecommendation` (with `status == 'open'`) count, anonymization-denial-event count (last 30 days), suppression-event count (last 30 days). Each row's status badge computed from underlying learning record state.
  - 212-UI-SPEC.md §Downstream UI Inheritance Map row 13 — Recovery Center learning-failure variant. `LearningRecommendation` rows where `status == 'expired'` AND no follow-up exists render as recovery items with failure-family `recommendation_unactioned`.
  - 212-UI-SPEC.md §Copywriting Contract — Future-only copy rule (this plan owns AC for 7 growth-module rows carrying `future_only` literal verbatim, with synonyms `planned`, `coming_soon`, `future_phase`, `future_consumer` forbidden); Carved-out scope for `must not apply customer-facing mutations directly` literal — this plan's `api/v1/literacy/recommendations.js` is the SOLE allowed file (per 212-UI-SPEC §Copywriting Contract recommendation customer-facing-mutation denials rule).

cross_cutting_carry_forward:
  - "Banned lexicon (CLAUDE.md): zero matches in `.planning/literacy/learning-recommendation-handoff.md` and `.planning/literacy/growth-learning-compatibility-map.md`. No exclamation points in doctrine prose. Verified by 212-01-00 architecture-lock CI script (per 212-UI-SPEC §DESIGN.md Compliance Assertions row `Architecture-lock forbidden strings`)."
  - "Architecture-lock forbidden strings: zero matches against `promote raw tenant copy`, `auto-promote literacy`, `silent prompt drift`, `single tenant raw lesson` (this plan is NOT the carved-out scope — that is 212-03), `vitest`, `playwright`, `.test.ts`, `route.ts` across this plan's doctrine, runtime, and test files. The string `must not apply customer-facing mutations directly` is permitted ONLY in `api/v1/literacy/recommendations.js` (carved-out scope per 212-UI-SPEC §Copywriting Contract recommendation customer-facing-mutation denials rule). Enforced by 212-01-00 `scripts/literacy/check-learning-architecture-lock.mjs`."
  - "Future-only doctrine (this plan owns the 212-05-02 AC + 212-UI-SPEC §DESIGN.md Compliance Assertions row `Future-only doctrine`): the 7 growth-module rows in `growth-learning-compatibility-map.md` MUST use the exact status string `future_only`. No row may use synonyms like `planned`, `coming_soon`, `future_phase`, or `future_consumer` (which is the 211 vocabulary; 212 uses `future_only` per its research doctrine). The literal `future_only` (NOT `future_consumer`) MUST appear in `lib/markos/learning/growth-compatibility.ts` AND in `test/literacy/phase-212/domain-5/growth-compatibility.test.js`."
  - "Pre-populated from P211 (per 212-UI-SPEC §Pre-Populated From row 211): the 6 `next_task_kind` literals from 211-06-01 (`brief_refresh`, `evidence_refresh`, `pricing_review`, `connector_recovery`, `social_follow_up`, `experiment_candidate`) feed `LearningRecommendation.recommendation_kind` literals — the 6 `recommendation_kind` literals are a superset that adds `task_create` and `strategy_refresh` and `research_refresh` to the loop's next-task vocabulary. The runtime mapping is implemented in `lib/markos/learning/recommendation.ts`."
  - "Mutation-class doctrine (206-02): actions on a recommendation are governed by the underlying linked task's mutation-class (the recommendation itself is read-only via `api/v1/literacy/recommendations.js`). When a `recommendation_kind == 'connector_fix'` action triggers a connector reconfiguration via P210 substrate, `connector.mutate` binds. When a `recommendation_kind == 'pricing_review'` leads to a P205 PricingRecommendation update, `price.change` binds. NOT directly authored by this plan — declared as forward-binding doctrine for the consuming surfaces."
  - "Node API error envelopes: `error_code` literals for this plan include `recommendation-not-found`, `recommendation-expired`, `recommendation-customer-facing-mutation-blocked`. `error_message` is engineering-grade English with no banned lexicon — the recommendation customer-facing-mutation denial endpoint MUST contain the exact substring `must not apply customer-facing mutations directly` per 212-UI-SPEC §Copywriting Contract recommendation customer-facing-mutation denials rule."
  - "Engineering-readability prose: short sentences, concrete contract field names, named owning plans. No marketing voice. No hedging."

downstream_ui_consumers_read_only:
  - "P208 Task Board (208-03 — already shipped; will be redeployed) — LearningRecommendation Task Board feed (212-UI-SPEC §Downstream row 5). Reads 12-field `LearningRecommendation` envelope. Renders 6 `recommendation_kind` literals as recommendation-class chips. Removes `<PlaceholderBanner variant=\"future_phase_212\">` for learning-originated tasks. NOT modified by this plan."
  - "P217+ growth phases or future enterprise admin — Future-growth-learning compatibility browser (212-UI-SPEC §Downstream row 10). Reads 7 `future_only` rows. NEVER renders as runnable until P218-P220 ship. NOT modified by this plan."
  - "P213 Tenant 0 surface — Tenant 0 learning-readiness panel (212-UI-SPEC §Downstream row 12). Reads readiness rollup over all 4 learning-record types from this phase. NOT modified by this plan."
</ui_spec_fold>

<objective>
Turn governed learning into governed action: LearningRecommendation records that create tasks or strategy refresh work, plus a future-growth compatibility map that preserves doc 17 scope without making it runnable here.
</objective>

<context>
@.planning/phases/212-learning-literacy-evolution/212-RESEARCH.md
@.planning/phases/212-learning-literacy-evolution/212-VALIDATION.md
@.planning/phases/212-learning-literacy-evolution/212-UI-SPEC.md
@.planning/phases/208-human-operating-interface/208-RESEARCH.md
</context>

<tasks>
<task id="212-05-01">
  <name>LearningRecommendation handoff to tasks and strategy work</name>
  <files>.planning/literacy/learning-recommendation-handoff.md, supabase/migrations/learning_recommendations.sql, lib/markos/learning/recommendation.ts, lib/markos/learning/index.cjs, api/v1/literacy/recommendations.js, test/literacy/phase-212/domain-5/recommendation-handoff.test.js, test/literacy/phase-212/domain-5/recommendation-expiry.test.js</files>
  <read_first>
    - .planning/phases/212-learning-literacy-evolution/212-RESEARCH.md section "Domain 5 - LearningRecommendation and future-growth compatibility"
    - .planning/phases/212-learning-literacy-evolution/212-UI-SPEC.md §Downstream UI Inheritance Map row 5 (LearningRecommendation Task Board feed) and row 7 (Suppression list browser) and row 12 (Tenant 0 learning-readiness panel) and row 13 (Recovery Center learning-failure variant)
    - .planning/phases/212-learning-literacy-evolution/212-UI-SPEC.md §Translation Gate Dissolution and Opening (this plan DISSOLVES the 211 `learning_ready` consumer placeholder AND OPENS the four downstream gates)
    - .planning/phases/212-learning-literacy-evolution/212-UI-SPEC.md §Copywriting Contract (carved-out scope for `must not apply customer-facing mutations directly` literal — this plan's `api/v1/literacy/recommendations.js` is the SOLE allowed file)
    - .planning/phases/208-human-operating-interface/208-RESEARCH.md
    - .planning/phases/211-content-social-revenue-loop/211-06-PLAN.md
    - lib/markos/crm/reporting.ts
  </read_first>
  <action>
    Step 1: Create `.planning/literacy/learning-recommendation-handoff.md` with the exact fields `recommendation_id`, `tenant_id`, `source_type`, `source_ref`, `recommendation_kind`, `priority`, `owner_role`, `status`, `expires_at`, `evidence_refs`, `task_id`, and `suppression_reason`. Doctrine prose carries zero banned-lexicon matches and zero matches against `must not apply customer-facing mutations directly` (the carved-out scope is `api/v1/literacy/recommendations.js` only, NOT the doctrine markdown).
    Step 2: Create `supabase/migrations/learning_recommendations.sql` and `lib/markos/learning/recommendation.ts` so `recommendation_kind` is constrained to `task_create | strategy_refresh | research_refresh | pricing_review | connector_fix | experiment_candidate`. The six literals are the canonical recommendation-class vocabulary downstream surfaces will render as chips with the recipe `task_create` (info), `strategy_refresh` (info), `research_refresh` (info), `pricing_review` (warning), `connector_fix` (error), `experiment_candidate` (success) per 212-UI-SPEC §Downstream UI Inheritance Map row 5. The runtime mapping from 211 `next_task_kind` to 212 `recommendation_kind` is implemented in this module (per 212-UI-SPEC §Pre-Populated From row 211).
    Step 3: Implement `api/v1/literacy/recommendations.js` as a governed read and handoff surface only; it must not apply customer-facing mutations directly. The error envelope MUST contain the exact substring `must not apply customer-facing mutations directly` (per 212-UI-SPEC §Copywriting Contract recommendation customer-facing-mutation denials rule). This file is the SOLE carved-out scope where that string may appear; architecture-lock CI script (212-01-00) enforces the scope rule.
    Step 4: Add domain-5 recommendation tests for creation, expiry, suppression, and task linkage.
  </action>
  <acceptance_criteria>
    - `.planning/literacy/learning-recommendation-handoff.md` contains the exact field names `recommendation_kind`, `owner_role`, `expires_at`, `evidence_refs`, and `task_id`
    - `lib/markos/learning/recommendation.ts` contains the exact kind literals `task_create`, `strategy_refresh`, `research_refresh`, `pricing_review`, `connector_fix`, and `experiment_candidate`
    - `api/v1/literacy/recommendations.js` contains the exact string `must not apply customer-facing mutations directly` (carved-out scope per 212-UI-SPEC §Copywriting Contract)
    - `.planning/literacy/learning-recommendation-handoff.md` contains zero matches against `must not apply customer-facing mutations directly` (NOT the carved-out scope; only `api/v1/literacy/recommendations.js` is)
    - `test/literacy/phase-212/domain-5/recommendation-handoff.test.js` contains the exact literals `task_create` and `strategy_refresh`
    - `.planning/literacy/learning-recommendation-handoff.md` contains zero matches against the CLAUDE.md banned lexicon (per 212-UI-SPEC §DESIGN.md Compliance Assertions row `Banned lexicon`)
    - `.planning/literacy/learning-recommendation-handoff.md` contains zero matches against the three architecture-lock forbidden strings `promote raw tenant copy`, `auto-promote literacy`, `silent prompt drift` AND zero matches against `single tenant raw lesson` (this plan is NOT the carved-out scope)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/literacy/phase-212/domain-5/recommendation-handoff.test.js</automated>
  </verify>
  <done>Learning now produces explicit governed follow-up work instead of hidden behavioral drift; the 12-field envelope and 6-literal `recommendation_kind` vocabulary are published verbatim for downstream UI inheritance per 212-UI-SPEC §Downstream rows 5, 7, 12, and 13; the carved-out scope for `must not apply customer-facing mutations directly` is enforced.</done>
</task>

<task id="212-05-02">
  <name>Future-growth learning compatibility map</name>
  <files>.planning/literacy/growth-learning-compatibility-map.md, lib/markos/learning/growth-compatibility.ts, test/literacy/phase-212/domain-5/growth-compatibility.test.js, .planning/phases/212-learning-literacy-evolution/212-VALIDATION.md</files>
  <read_first>
    - .planning/phases/212-learning-literacy-evolution/212-RESEARCH.md section "Domain 5 - LearningRecommendation and future-growth compatibility"
    - .planning/phases/212-learning-literacy-evolution/212-UI-SPEC.md §Downstream UI Inheritance Map row 10 (Future-growth-learning compatibility browser)
    - .planning/phases/212-learning-literacy-evolution/212-UI-SPEC.md §Translation Gate Dissolution and Opening (this task OPENS the future_growth_learning_module gate via 7 `future_only` rows binding to P217+)
    - .planning/phases/212-learning-literacy-evolution/212-UI-SPEC.md §Copywriting Contract (Future-only copy rule — `future_only` literal exact, synonyms `planned`, `coming_soon`, `future_phase`, `future_consumer` forbidden)
    - .planning/phases/212-learning-literacy-evolution/212-UI-SPEC.md §DESIGN.md Compliance Assertions (Future-only doctrine row)
    - obsidian/brain/SaaS Marketing OS Strategy Canon.md
    - .planning/tenant-zero/final-readiness-summary.md
  </read_first>
  <action>
    Step 1: Create `.planning/literacy/growth-learning-compatibility-map.md` with one row each for `plg`, `abm`, `community`, `events`, `pr`, `partnerships`, and `developer_marketing` (7 rows). Doctrine prose carries zero banned-lexicon matches.
    Step 2: Require each row to include `topic`, `future_phase`, `status`, `required_signal`, and `activation_rule`, and set every `status` to `future_only`. The literal `future_only` is the canonical status vocabulary per 212-UI-SPEC §DESIGN.md Compliance Assertions row `Future-only doctrine` — NO synonyms like `planned`, `coming_soon`, `future_phase`, or `future_consumer` (which is the 211 vocabulary; 212 uses `future_only`).
    Step 3: Implement `lib/markos/learning/growth-compatibility.ts` as a read-only mapping helper that never marks these topics runnable in Phase 212. The literal `future_only` MUST appear verbatim in this module.
    Step 4: Add the compatibility test and update `212-VALIDATION.md` so Task `212-05-02` exists in the verification map. The test asserts the literal `future_only` appears for all 7 rows.
  </action>
  <acceptance_criteria>
    - `.planning/literacy/growth-learning-compatibility-map.md` contains the exact topics `plg`, `abm`, `community`, `events`, `pr`, `partnerships`, and `developer_marketing` (7 rows)
    - `.planning/literacy/growth-learning-compatibility-map.md` contains the exact status value `future_only` for every row
    - `.planning/literacy/growth-learning-compatibility-map.md` contains zero matches against synonym strings `planned`, `coming_soon`, `future_phase`, `future_consumer` (per 212-UI-SPEC §Copywriting Contract Future-only copy rule)
    - `lib/markos/learning/growth-compatibility.ts` contains the exact string `future_only`
    - `lib/markos/learning/growth-compatibility.ts` contains zero matches against synonym strings `future_consumer`, `planned`, `coming_soon`
    - `test/literacy/phase-212/domain-5/growth-compatibility.test.js` contains the exact string `future_only`
    - `212-VALIDATION.md` contains row `212-05-02`
    - `.planning/literacy/growth-learning-compatibility-map.md` contains zero matches against the CLAUDE.md banned lexicon (per 212-UI-SPEC §DESIGN.md Compliance Assertions row `Banned lexicon`)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/literacy/phase-212/domain-5/growth-compatibility.test.js</automated>
  </verify>
  <done>Doc 17 learning categories are preserved as translation inputs for later phases without becoming prematurely active automation; the 7 growth-module rows render the literal `future_only` verbatim per 212-UI-SPEC §Downstream row 10 and §DESIGN.md Compliance Assertions row `Future-only doctrine`.</done>
</task>
</tasks>

<verification>
- npm test -- test/literacy/phase-212/domain-5/recommendation-handoff.test.js
- npm test -- test/literacy/phase-212/domain-5/growth-compatibility.test.js
</verification>

<success_criteria>
- LearningRecommendation records create governed follow-up work
- Recommendation kinds, ownership, and expiry are explicit and published verbatim for downstream UI binding
- Customer-facing mutations remain outside the learning layer
- Carved-out scope for `must not apply customer-facing mutations directly` literal enforced (allowed ONLY in `api/v1/literacy/recommendations.js`)
- Future-growth learning topics remain compatibility metadata only with `future_only` literal verbatim (NOT `future_consumer`, NOT synonyms)
- Translation gate dissolution: 211 `MeasurementHandoff.learning_ready` consumer placeholder dissolves once this plan ingests the handoff
- Translation gates opened: future_phase_213 (Tenant 0), future_phase_217+ (growth-learning), future_phase_218..220 (SaaS Growth), future_phase_225 (narrative intelligence)
- Banned-lexicon zero-match enforced; architecture-lock forbidden strings zero-match enforced (except carved-out scope)
</success_criteria>
