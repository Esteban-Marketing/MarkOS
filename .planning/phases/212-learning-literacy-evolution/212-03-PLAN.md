---
phase: 212
plan: "03"
type: execute
wave: 3
ui_scope: NO_UI
depends_on:
  - "212-01"
  - "212-02"
f_ids: []
migration_slots: []
integrates_with:
  - "LRN-04 privacy rules from Plan 04 (promotion rules must respect anonymization gates even before central promotion occurs)"
  - "COMP-01 from P206 (mutation-class doctrine — `public.claim` for promoted central-literacy entries that surface in tenant-readable canon for ALL tenants; DUAL-CLASS with `data.export` if promotion involves cross-tenant anonymized aggregate export)"
  - "RUN-01..08 from P207 (ApprovalHandoffRecord for admin-review handoff lineage; `handoff_kind == 'approval'` for LiteracyUpdateCandidate review)"
translation_gate_for: []
files_modified:
  - .planning/literacy/literacy-promotion-review.md
  - supabase/migrations/learning_literacy_update_candidates.sql
  - lib/markos/learning/literacy-update-candidate.ts
  - lib/markos/learning/admin-review.ts
  - api/v1/literacy/review.js
  - lib/markos/learning/index.cjs
  - test/literacy/phase-212/domain-3/candidate-queue.test.js
  - test/literacy/phase-212/domain-3/admin-review-transitions.test.js
  - test/literacy/phase-212/domain-3/direct-promotion-denial.test.js
  - .planning/phases/212-learning-literacy-evolution/212-VALIDATION.md
autonomous: true
requirements:
  - LRN-05
  - QA-08
  - QA-09
  - QA-10
must_haves:
  truths:
    - "Central literacy cannot update directly from one tenant's raw lesson."
    - "Every LiteracyUpdateCandidate carries anonymized pattern, evidence summary, sample size, average confidence, reviewer decision, and promoted target."
    - "Admin review transitions are explicit and auditable."
    - "Rejected or evidence-deficient candidates preserve rationale instead of disappearing."
    - "LiteracyUpdateCandidate 12-field envelope (per 212-UI-SPEC §Downstream UI Inheritance Map row 3, 'LiteracyUpdateCandidate admin review queue') is published verbatim so the future P208 Approval Inbox extension can bind without re-deriving field names."
    - "The five review-state literals (`proposed`, `needs_evidence`, `approved`, `rejected`, `promoted`) are the canonical decision vocabulary for downstream `.c-badge--{info,warning,success,error,success}` rendering (per 212-UI-SPEC §Downstream UI Inheritance Map row 3)."
    - "Mutation-class binding `public.claim` (DUAL-CLASS with `data.export` when cross-tenant aggregates are exported) per 206-02 doctrine governs the future approve action — `default_approval_mode == dual_approval` is the typical posture (per 212-UI-SPEC §Downstream UI Inheritance Map row 3 + row 4)."
    - "208-04 Approval Inbox is the parent surface for the future cross-tenant promotion approval inbox extension (per 212-UI-SPEC §Downstream UI Inheritance Map row 4)."
    - "The `api/v1/literacy/review.js` direct-promotion-denial endpoint is the SOLE carved-out scope where the architecture-lock-restricted string `single tenant raw lesson` may appear (per 212-UI-SPEC §Copywriting Contract carved-out scope rule)."
  artifacts:
    - path: .planning/literacy/literacy-promotion-review.md
      provides: canonical review states, reviewer duties, and promotion target rules (consumed verbatim by 212-UI-SPEC §Downstream row 3 'LiteracyUpdateCandidate admin review queue' + row 4 'Cross-tenant promotion approval inbox extension' + row 8 'Promotion lineage timeline')
    - path: api/v1/literacy/review.js
      provides: governed admin-review pathway without requiring a separate UI shell in this phase; emits JSON envelopes consumed by the future P208 Approval Inbox extension
  key_links:
    - from: lib/markos/mcp/tools/literacy/query-canon.cjs
      to: .planning/literacy/literacy-promotion-review.md
      via: central promotions need a clear landing target in tenant-readable canon surfaces
      pattern: "canon"
    - from: .planning/codebase/LITERACY-OPERATIONS.md
      to: api/v1/literacy/review.js
      via: review and promotion flows should preserve literacy namespace and RLS doctrine
      pattern: "namespace isolation"
---

<ui_spec_fold>
ui_scope: NO_UI

ui_spec_block:
  - 212-UI-SPEC.md §parent_doctrine_chain (frontmatter) — inheritance from 206 (mutation-class `public.claim` + DUAL-CLASS `data.export`), 207 (ApprovalHandoffRecord with `handoff_kind == 'approval'`), 208 (PARENT — Approval Inbox is the consumer for `LiteracyUpdateCandidate` admin review and `LearningRecommendation` approval-gated handoff), 209 (EvidenceMap binding for `evidence_summary`)
  - 212-UI-SPEC.md §Translation Gate Dissolution and Opening — this plan DISSOLVES the 208-UI-SPEC Approval Inbox handoff-kind filter learning admin-review variant gate (`<PlaceholderBanner variant="future_phase_212">` for LiteracyUpdateCandidate admin promotion review and LearningRecommendation approval-gated kinds dissolves once 212 ships); after 212-03, future P208 redeploy renders learning-originated approval items with `LiteracyUpdateCandidate.candidate_id`, `anonymized_pattern`, `evidence_summary`, `average_confidence_score`, and `promoted_target` linked through `ApprovalHandoffRecord.task_ref`. This plan also OPENS a P208-extension gate (`<PlaceholderBanner variant="future_phase_212_admin_extension">` rendered by any admin surface that would consume 212 substrate but the P208-extension phase has not yet shipped).
  - 212-UI-SPEC.md §Downstream UI Inheritance Map row 3 — LiteracyUpdateCandidate admin review queue (P208 Approval Inbox extension; P208-extension phase, NOT 212). 12-field `LiteracyUpdateCandidate` envelope verbatim (`candidate_id`, `source_overlay_refs`, `anonymized_pattern`, `sample_size`, `supporting_artifact_count`, `evidence_summary`, `average_confidence_score`, `reviewer_id`, `decision`, `promoted_target`, `created_at`, `updated_at`). Five review-state literals (`proposed`, `needs_evidence`, `approved`, `rejected`, `promoted`) verbatim as decision badges with `.c-badge--{info,warning,success,error,success}` mapping. `anonymized_pattern` renders as a read-only `.c-code-block` panel; UI MUST NOT allow direct editing of the anonymized pattern (the anonymization is the safety boundary). `evidence_summary` renders as a `.c-card` panel. `sample_size` and `supporting_artifact_count` and `average_confidence_score` render as a paired threshold-status panel comparing to `.planning/literacy/sample-size-thresholds.md`; below-threshold rows render `.c-notice c-notice--warning` "[warn] Below promotion threshold — local-only". `source_overlay_refs[]` chips compose `.c-chip-protocol` deep-linked to TenantOverlay browser. `promoted_target` renders as deep-link chip. Approve / Reject / Request-evidence actions compose `.c-modal` + `.c-button--destructive` Reject + `.c-button--primary` Approve + `.c-button--tertiary` Request more evidence; mutation-class binding per 206-02 — `public.claim` for the approve action, DUAL-CLASS with `data.export` if the promotion involves cross-tenant anonymized aggregates. The 213-style stricter approval mode applies (typically `dual_approval` for `public.claim`). D-14 vanilla-table recipe for the queue; per-candidate detail composes `.c-card`. Cross-binding: 206 `public.claim` + `data.export` mutation-class doctrine, 207 `ApprovalHandoffRecord` via reviewer-handoff lineage, 209 `EvidenceMap` via supporting-evidence ledger.
  - 212-UI-SPEC.md §Downstream UI Inheritance Map row 4 — Cross-tenant promotion approval inbox extension (P208 Approval Inbox extension variant of 208-04, NOT 212). When `ApprovalHandoffRecord.handoff_kind == 'approval'` AND the linked record is a `LiteracyUpdateCandidate`, the approval-token viewer composes the candidate's `anonymized_pattern` preview + `evidence_summary` + threshold-status panel + `promoted_target` deep-link + reviewer rationale field. Mutation-class binding: `public.claim` for approve; DUAL-CLASS with `data.export` if cross-tenant aggregate export. `.c-modal` + `.c-button--destructive` confirm gate REQUIRED because mutation-class default-approval-mode is `dual_approval` for `public.claim`. The override path inherits 209-04 doctrine: if `evidence_summary` does not meet 209 source-quality posture, operator MUST record an `override_reason` ≥ 10 chars and the override binds permanently to `LiteracyUpdateCandidate.decision` rationale.
  - 212-UI-SPEC.md §Downstream UI Inheritance Map row 8 — Promotion lineage timeline (per-promoted-canon-entry source-trail; future P208 admin extension or future enterprise compliance). Walk-back from central-literacy canon entry through `LiteracyUpdateCandidate.candidate_id` (decision rationale), `source_overlay_refs[]` (TenantOverlay evidence + provenance), `ArtifactPerformanceLog.evidence_refs` (original outcome that seeded the lesson), 209 `EvidenceMap`, 211 `MarketingArtifact` + `MeasurementHandoff`, 207 `RunApiEnvelope`. Each timeline node composes `.c-card` with `.c-status-dot--{live,error,*}`. No glow, no gradient on edges per DESIGN.md elevation rule. Reduced-motion freezes auto-traversal animation.
  - 212-UI-SPEC.md §Copywriting Contract — Carved-out scope for `single tenant raw lesson` literal: this plan's `api/v1/literacy/review.js` is the SOLE file allowed to contain the string `single tenant raw lesson` (per 212-UI-SPEC §Copywriting Contract direct-promotion denial endpoint rule); architecture-lock CI script (212-01-00) enforces this scope. Banned-lexicon row applies to `.planning/literacy/literacy-promotion-review.md` doctrine prose.

cross_cutting_carry_forward:
  - "Banned lexicon (CLAUDE.md): zero matches in `.planning/literacy/literacy-promotion-review.md`. No exclamation points in doctrine prose. Verified by 212-01-00 architecture-lock CI script (per 212-UI-SPEC §DESIGN.md Compliance Assertions row `Architecture-lock forbidden strings`)."
  - "Architecture-lock forbidden strings: zero matches against `promote raw tenant copy`, `auto-promote literacy`, `silent prompt drift`, `must not apply customer-facing mutations directly` (this plan is NOT the carved-out scope for that one — that is 212-05), `vitest`, `playwright`, `.test.ts`, `route.ts` across this plan's doctrine, runtime, and test files. The string `single tenant raw lesson` is permitted ONLY in `api/v1/literacy/review.js` (carved-out scope per 212-UI-SPEC §Copywriting Contract). Enforced by 212-01-00 `scripts/literacy/check-learning-architecture-lock.mjs`."
  - "Mutation-class doctrine (206-02): the future approve action on `LiteracyUpdateCandidate.decision == 'approved'` binds to `public.claim` per 206-02 (since promoted entry enters tenant-readable central literacy canon for ALL tenants, making it a public-facing claim from originating tenant's perspective). DUAL-CLASS with `data.export` if promotion involves cross-tenant anonymized aggregate export. Default-approval-mode `dual_approval` applies. NOT directly authored by this plan — declared as forward-binding doctrine for the consuming P208 Approval Inbox extension surface."
  - "ApprovalHandoffRecord doctrine (207): the future P208 Approval Inbox extension reads learning-originated approval items where `ApprovalHandoffRecord.handoff_kind == 'approval'` AND the linked record is a `LiteracyUpdateCandidate`. The handoff lineage chain composes `LiteracyUpdateCandidate.candidate_id` linked through `ApprovalHandoffRecord.task_ref` (per 212-UI-SPEC §Downstream row 4)."
  - "Override-evidence doctrine (209-04 inheritance): if `evidence_summary` does not meet 209 source-quality posture, the future operator surface MUST record `override_reason` ≥ 10 chars and bind it permanently to `LiteracyUpdateCandidate.decision` rationale (per 212-UI-SPEC §Downstream row 4)."
  - "Node API error envelopes: `error_code` literals for this plan include `candidate-not-found`, `candidate-below-sample-threshold`, `candidate-below-confidence-threshold`, `candidate-below-supporting-artifact-threshold`, `candidate-anonymization-denied-{tenant-identifiers,person-names,customer-names,contact-details,raw-pricing,support-transcripts,campaign-identity,operator-notes}`, `direct-promotion-denied-single-tenant-raw-lesson` (per 212-UI-SPEC §Copywriting Contract Node API error envelopes section). `error_message` is engineering-grade English with no banned lexicon — the direct-promotion denial endpoint MUST contain the exact substring `single tenant raw lesson` per 212-UI-SPEC §Copywriting Contract direct-promotion denial endpoint rule."
  - "Engineering-readability prose: short sentences, concrete contract field names, named owning plans. No marketing voice. No hedging language."

downstream_ui_consumers_read_only:
  - "P208 Approval Inbox extension (P208-extension phase, NOT 212) — LiteracyUpdateCandidate admin review queue (212-UI-SPEC §Downstream row 3). Reads 12-field `LiteracyUpdateCandidate` envelope. Renders 5 review-state literals as `.c-badge--{state}` decision badges. Approve action binds to `public.claim` mutation-class. NOT modified by this plan."
  - "P208 Approval Inbox extension (208-04 variant; P208-extension phase, NOT 212) — Cross-tenant promotion approval inbox extension (212-UI-SPEC §Downstream row 4). Reads `ApprovalHandoffRecord.handoff_kind == 'approval'` linked to `LiteracyUpdateCandidate`. Composes `.c-modal` + `.c-button--destructive` confirm gate. NOT modified by this plan."
  - "P208 admin extension or future enterprise compliance — Promotion lineage timeline (212-UI-SPEC §Downstream row 8). Walks back through 5 inheritance maps (206, 207, 209, 211, 212). NOT modified by this plan."
</ui_spec_fold>

<objective>
Define the admin-reviewed central promotion path through LiteracyUpdateCandidate so cross-tenant learning becomes governed canon only after explicit evidence-backed review.
</objective>

<context>
@.planning/phases/212-learning-literacy-evolution/212-RESEARCH.md
@.planning/phases/212-learning-literacy-evolution/212-VALIDATION.md
@.planning/phases/212-learning-literacy-evolution/212-UI-SPEC.md
</context>

<tasks>
<task id="212-03-01">
  <name>LiteracyUpdateCandidate queue and admin review transitions</name>
  <files>.planning/literacy/literacy-promotion-review.md, supabase/migrations/learning_literacy_update_candidates.sql, lib/markos/learning/literacy-update-candidate.ts, lib/markos/learning/admin-review.ts, api/v1/literacy/review.js, test/literacy/phase-212/domain-3/candidate-queue.test.js, test/literacy/phase-212/domain-3/admin-review-transitions.test.js, test/literacy/phase-212/domain-3/direct-promotion-denial.test.js</files>
  <read_first>
    - .planning/phases/212-learning-literacy-evolution/212-RESEARCH.md section "Domain 3 - LiteracyUpdateCandidate and admin review"
    - .planning/phases/212-learning-literacy-evolution/212-UI-SPEC.md §Downstream UI Inheritance Map row 3 (LiteracyUpdateCandidate admin review queue) and row 4 (Cross-tenant promotion approval inbox extension) and row 8 (Promotion lineage timeline)
    - .planning/phases/212-learning-literacy-evolution/212-UI-SPEC.md §Translation Gate Dissolution and Opening (this plan DISSOLVES the 208 Approval Inbox learning admin-review variant gate AND OPENS the P208-extension gate)
    - .planning/phases/212-learning-literacy-evolution/212-UI-SPEC.md §Copywriting Contract (carved-out scope for `single tenant raw lesson` literal — this plan's `api/v1/literacy/review.js` is the SOLE allowed file)
    - .planning/phases/206-soc2-type1-foundation/206-RESEARCH.md (mutation-class `public.claim` + `data.export` doctrine)
    - .planning/phases/207-agentrun-v2-orchestration-substrate/207-UI-SPEC.md (ApprovalHandoffRecord)
    - lib/markos/mcp/tools/literacy/query-canon.cjs
    - lib/markos/mcp/tools/literacy/walk-taxonomy.cjs
    - .planning/codebase/LITERACY-OPERATIONS.md
  </read_first>
  <action>
    Step 1: Create `.planning/literacy/literacy-promotion-review.md` with the exact fields `candidate_id`, `source_overlay_refs`, `anonymized_pattern`, `sample_size`, `supporting_artifact_count`, `evidence_summary`, `average_confidence_score`, `reviewer_id`, `decision`, `promoted_target`, `created_at`, and `updated_at`. Doctrine prose carries zero banned-lexicon matches and zero matches against `single tenant raw lesson` (the carved-out scope is `api/v1/literacy/review.js` only, NOT the doctrine markdown).
    Step 2: Create `supabase/migrations/learning_literacy_update_candidates.sql` and `lib/markos/learning/literacy-update-candidate.ts` so review states are constrained to `proposed | needs_evidence | approved | rejected | promoted`. The five literals are the canonical decision vocabulary downstream surfaces will render as `.c-badge--{info,warning,success,error,success}` chips per 212-UI-SPEC §Downstream UI Inheritance Map row 3.
    Step 3: Implement `lib/markos/learning/admin-review.ts` and `api/v1/literacy/review.js` so direct promotion from a single tenant raw lesson is blocked and review decisions are append-only and reason-bearing. The `api/v1/literacy/review.js` direct-promotion-denial error envelope MUST contain the exact substring `single tenant raw lesson` (per 212-UI-SPEC §Copywriting Contract direct-promotion denial endpoint rule). This file is the SOLE carved-out scope where that string may appear; architecture-lock CI script (212-01-00) enforces the scope rule.
    Step 4: Add domain-3 tests for queue rules, review transitions, and direct-promotion denial. The `direct-promotion-denial.test.js` test asserts the error message contains the exact substring `single tenant raw lesson`.
  </action>
  <acceptance_criteria>
    - `.planning/literacy/literacy-promotion-review.md` contains the exact field names `source_overlay_refs`, `anonymized_pattern`, `sample_size`, `supporting_artifact_count`, and `promoted_target`
    - `lib/markos/learning/literacy-update-candidate.ts` contains the exact status literals `proposed`, `needs_evidence`, `approved`, `rejected`, and `promoted`
    - `api/v1/literacy/review.js` contains the exact string `single tenant raw lesson` (carved-out scope per 212-UI-SPEC §Copywriting Contract)
    - `.planning/literacy/literacy-promotion-review.md` contains zero matches against `single tenant raw lesson` (NOT the carved-out scope; only `api/v1/literacy/review.js` is)
    - `test/literacy/phase-212/domain-3/direct-promotion-denial.test.js` contains the exact status names `proposed` and `promoted` AND the exact substring `single tenant raw lesson`
    - `.planning/literacy/literacy-promotion-review.md` contains zero matches against the CLAUDE.md banned lexicon (per 212-UI-SPEC §DESIGN.md Compliance Assertions row `Banned lexicon`)
    - `.planning/literacy/literacy-promotion-review.md` contains zero matches against the three architecture-lock forbidden strings `promote raw tenant copy`, `auto-promote literacy`, `silent prompt drift` (per 212-UI-SPEC §DESIGN.md Compliance Assertions row `Architecture-lock forbidden strings`)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/literacy/phase-212/domain-3/</automated>
  </verify>
  <done>Central literacy promotion is now gated through explicit admin review instead of informal reuse of tenant-local lessons; the 12-field envelope and 5-literal review-state vocabulary are published verbatim for downstream UI inheritance per 212-UI-SPEC §Downstream rows 3, 4, and 8; the carved-out scope for `single tenant raw lesson` is enforced.</done>
</task>
</tasks>

<verification>
- npm test -- test/literacy/phase-212/domain-3/
</verification>

<success_criteria>
- LiteracyUpdateCandidate fields and review states are explicit and published verbatim for downstream UI binding
- Direct raw-tenant promotion is blocked by the 212-03-01 endpoint
- Admin review decisions are auditable and reason-bearing
- Promotion targets are defined against real literacy surfaces
- Mutation-class doctrine (`public.claim` + DUAL-CLASS `data.export`) declared for the future approve action per 206-02
- Carved-out scope for `single tenant raw lesson` literal enforced (allowed ONLY in `api/v1/literacy/review.js`)
- Banned-lexicon zero-match enforced; architecture-lock forbidden strings zero-match enforced (except carved-out scope)
</success_criteria>
