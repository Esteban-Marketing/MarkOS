---
id: "17-02-install-update-rebrand"
wave: 2
depends_on: ["17-01-package-identity"]
discipline: marketing_operations
requirements: ["RBD-01"]
files_modified:
  - bin/install.cjs
  - bin/update.cjs
mir_files_referenced: []
tracking_required: false
requires_human_approval: false
autonomous: true
neuro_dimension: false
objective: "Update all user-facing text in bin/install.cjs and bin/update.cjs to reflect MarkOS branding. Functional paths remain unchanged until Phase 18/22."
must_haves:
  - install.cjs banner says "MarkOS Installer" not "MGSD Installer"
  - update.cjs banner says "MarkOS Update Engine" not "MGSD Update Engine"
  - All console output references "MarkOS" not "MGSD"
  - npx command references say "npx markos" not "npx marketing-get-shit-done"
  - JSDoc headers updated to MarkOS
  - Functional paths (.agent/marketing-get-shit-done/) NOT changed yet (Phase 18/22)
---

# Plan 17-02: Install & Update Script Display Rebrand

## Wave 2 — CLI Display Text Updates

> **CRITICAL CONSTRAINT:** This plan ONLY updates display text, banners, comments, and JSDoc. It does NOT touch functional paths like `.agent/marketing-get-shit-done/` or `.mgsd-install-manifest.json` — those are Phase 18 (directory renames) and Phase 22 (migration logic).

<task id="1" name="Update install.cjs JSDoc header">
  <read_first>
    - bin/install.cjs (lines 1-25: JSDoc block)
  </read_first>
  <action>
    Update the JSDoc header block in `bin/install.cjs`:
    - Line 2: `install.cjs — MGSD Interactive Installer & GSD Co-existence Wizard` → `install.cjs — MarkOS Interactive Installer & GSD Co-existence Wizard`
    - Line 5: `Handles the first-run setup of the MGSD protocol` → `Handles the first-run setup of the MarkOS protocol`
    - Line 6: `Ensures parallel co-existence with the \`get-shit-done\` (GSD) protocol.` — keep as-is (GSD is not rebranded)
    - Line 10: `GSD Integration: If GSD exists, merges ITM templates but keeps files isolated.` — keep as-is
    - Line 14: `npx marketing-get-shit-done          → runs interactive install` → `npx markos                    → runs interactive install`
    - Line 15: `npx marketing-get-shit-done update   → delegates to \`bin/update.cjs\`` → `npx markos update             → delegates to \`bin/update.cjs\``
  </action>
  <acceptance_criteria>
    - JSDoc contains "MarkOS Interactive Installer" not "MGSD Interactive Installer"
    - JSDoc npx commands reference `npx markos` not `npx marketing-get-shit-done`
    - No occurrence of "MGSD" in the JSDoc block (lines 1-25)
  </acceptance_criteria>
</task>

<task id="2" name="Update install.cjs banner and display text">
  <read_first>
    - bin/install.cjs (lines 90-100: banner call, lines 200-220: final output)
  </read_first>
  <action>
    Update display strings in `bin/install.cjs`:
    1. Banner call (~line 95): `MGSD Installer v${VERSION} — Marketing Get Shit Done` → `MarkOS Installer v${VERSION} — Marketing Operating System`
    2. Detection message (~line 97): `📦 MGSD is already installed` → `📦 MarkOS is already installed`
    3. GSD co-existence message: `✓ Existing GSD install detected — MGSD will be added alongside it.` → `✓ Existing GSD install detected — MarkOS will be added alongside it.`
    4. Standalone message: `ℹ No GSD install found — MGSD will be installed standalone.` → `ℹ No GSD install found — MarkOS will be installed standalone.`
    5. Install progress: `✓ MGSD protocol files installed` → `✓ MarkOS protocol files installed`
    6. Skills message: `✓ MGSD skills co-exist with GSD (no conflicts)` → `✓ MarkOS skills co-exist with GSD (no conflicts)`
    7. Final banner: `MGSD v${VERSION} installed ✓` → `MarkOS v${VERSION} installed ✓`
    8. Final output lines:
       - `Update:   npx marketing-get-shit-done update` → `Update:   npx markos update`
       - `Docs:     .agent/marketing-get-shit-done/MGSD-INDEX.md` → `Docs:     .agent/marketing-get-shit-done/MARKOS-INDEX.md` (NOTE: path stays old until Phase 18)
    9. No AI Keys message: `To power the MGSD AI agents` → `To power the MarkOS AI agents`
  </action>
  <acceptance_criteria>
    - `grep -c "MGSD" bin/install.cjs` returns 0 (no MGSD in display text)
    - `grep "MarkOS Installer" bin/install.cjs` returns the banner line
    - `grep "npx markos update" bin/install.cjs` returns at least 1 match
    - Functional paths (`.agent/marketing-get-shit-done/`) remain UNCHANGED in path.join() calls
  </acceptance_criteria>
</task>

<task id="3" name="Update install.cjs AI.md append text">
  <read_first>
    - bin/install.cjs (lines ~200-210: GEMINI.md/CLAUDE.md/AGENTS.md append block)
  </read_first>
  <action>
    Update the AI.md append block in `bin/install.cjs`:
    1. Check condition: `if (!existing.includes('MGSD'))` → `if (!existing.includes('MarkOS'))`
    2. Append text: 
       - `## MGSD — Marketing Get Shit Done` → `## MarkOS — Marketing Operating System`
       - `MGSD protocol installed at \`.agent/marketing-get-shit-done/\`.` → `MarkOS protocol installed at \`.agent/marketing-get-shit-done/\`.` (path stays old until Phase 18)
       - `See \`.agent/marketing-get-shit-done/MGSD-INDEX.md\` for full documentation.` → `See \`.agent/marketing-get-shit-done/MARKOS-INDEX.md\` for full documentation.` (path dir stays old, filename will be renamed in Phase 18)
  </action>
  <acceptance_criteria>
    - The check condition uses `'MarkOS'` not `'MGSD'`
    - Append text starts with `## MarkOS — Marketing Operating System`
    - No occurrence of "MGSD" in the append text block
  </acceptance_criteria>
</task>

<task id="4" name="Update update.cjs JSDoc header">
  <read_first>
    - bin/update.cjs (lines 1-25: JSDoc block)
  </read_first>
  <action>
    Update the JSDoc header block in `bin/update.cjs`:
    - Line 2: `update.cjs — MGSD Safe Update & Patch Engine` → `update.cjs — MarkOS Safe Update & Patch Engine`
    - Line 5: `Applies updates from the MGSD package` → `Applies updates from the MarkOS package`
    - Line 7: `preserving client modifications and \`.mgsd-local/\` overrides` — keep path as-is (Phase 20 changes paths)
    - All other path references in JSDoc RELATED FILES section — keep as-is (Phase 18/20)
  </action>
  <acceptance_criteria>
    - JSDoc contains "MarkOS Safe Update & Patch Engine" not "MGSD Safe Update & Patch Engine"
    - JSDoc says "from the MarkOS package" not "from the MGSD package"
  </acceptance_criteria>
</task>

<task id="5" name="Update update.cjs banner and display text">
  <read_first>
    - bin/update.cjs (lines 95-100: banner, line 100: error message, final banner)
  </read_first>
  <action>
    Update display strings in `bin/update.cjs`:
    1. Banner: `MGSD Update Engine v${NEW_VERSION}` → `MarkOS Update Engine v${NEW_VERSION}`
    2. Error message: `No .mgsd-install-manifest.json found. Run \`npx marketing-get-shit-done\` to install first.` → `No .mgsd-install-manifest.json found. Run \`npx markos\` to install first.` (keep `.mgsd-install-manifest.json` path reference — Phase 20 changes it)
    3. Error message: `Installed MGSD directory not found` → `Installed MarkOS directory not found`
    4. Final banner: `MGSD updated to v${NEW_VERSION} ✓` → `MarkOS updated to v${NEW_VERSION} ✓`
  </action>
  <acceptance_criteria>
    - `grep "MarkOS Update Engine" bin/update.cjs` returns the banner line
    - `grep "npx markos" bin/update.cjs` returns the install-first suggestion
    - `grep "MarkOS updated to" bin/update.cjs` returns the final banner
    - Functional paths (`.mgsd-install-manifest.json`, `.agent/marketing-get-shit-done/`) remain UNCHANGED
  </acceptance_criteria>
</task>

## Verification

After all tasks complete:

```bash
# Verify no "MGSD" display text remains (excluding functional path strings)
grep -n "MGSD" bin/install.cjs | grep -v "marketing-get-shit-done" | grep -v ".mgsd-"
# Expected: 0 lines (all MGSD display text replaced)

grep -n "MGSD" bin/update.cjs | grep -v "marketing-get-shit-done" | grep -v ".mgsd-"
# Expected: 0 lines

# Verify MarkOS branding present
grep -c "MarkOS" bin/install.cjs
# Expected: 8+ occurrences

grep -c "MarkOS" bin/update.cjs
# Expected: 4+ occurrences

# Verify functional paths NOT changed (these are Phase 18/20/22)
grep -c "marketing-get-shit-done" bin/install.cjs
# Expected: >0 (path.join calls still use old directory name)

grep -c ".mgsd-install-manifest" bin/update.cjs
# Expected: >0 (manifest path still uses old name)
```
