---
phase: 204
plan: 09
type: execute
wave: 3
depends_on: [01, 04]
files_modified:
  - bin/commands/doctor.cjs
  - bin/lib/cli/doctor-checks.cjs
  - bin/install.cjs
  - test/cli/doctor.test.js
  - test/cli/doctor-checks.test.js
autonomous: true
task_count: 2
requirements: [CLI-01, QA-01, QA-04, QA-11, QA-15]

must_haves:
  truths:
    - "bin/lib/cli/doctor-checks.cjs exports runChecks({ fix: boolean, checkOnly: boolean }) → Array&lt;{ id, label, status: 'ok'|'warn'|'error', message?, hint?, fixable?, fixed? }&gt;"
    - "Checks implemented (9 total): (1) node_version — process.version satisfies >= 22.0.0; (2) config_dir — $XDG_CONFIG_HOME/markos writable (fix: mkdir recursive + chmod 0o700); (3) active_token — getToken(resolveProfile()) returns non-null (fixable? false — user must run markos login); (4) token_valid — if token present, authedFetch GET /api/tenant/whoami returns 200 (fixable: prompt user to re-login); (5) markos_local_dir — .markos-local/ exists (fix: mkdir); (6) gitignore_protected — .markos-local/ listed in .gitignore (fix: call applyGitignoreProtections from bin/install.cjs — must EXPORT it from install.cjs in this plan); (7) keytar_available — try require('keytar') + call listProfiles; if fails → warn (not error; XDG fallback documented in hint); (8) server_reachable — authedFetch GET ${BASE_URL}/api/health OR HEAD / with 5s timeout → warn if unreachable (fixable: check internet); (9) supabase_connectivity — SKIP in this plan (server-side concern; Phase 206 covers)"
    - "bin/commands/doctor.cjs main({ cli }): parses --check-only + --fix + --json; default mode: run all checks, render, auto-fix fixable items unless --check-only; exit codes — 0 all green or warnings only, 1 any error"
    - "--check-only mode: runs checks + reports + does NOT attempt fixes even for fixable items; exit 0 on all green, 1 on any error (useful for CI gate)"
    - "--fix mode: auto-fixes fixable items that require filesystem mutation (config_dir, markos_local_dir, gitignore_protected); NEVER auto-runs markos login (phishing vector per CONTEXT §doctor + RESEARCH Open Question 1 — 'detects + prompts; user runs markos login explicitly')"
    - "TTY render: brew doctor-style checklist with ✓/⚠/✗ + one-line hint per failed check"
    - "JSON render: array of check results per `--json` flag; CI-friendly"
    - "bin/install.cjs exports applyGitignoreProtections (previously internal function at line ~109) so doctor.cjs can invoke it for gitignore fix — this is a small refactor (add `module.exports.applyGitignoreProtections = applyGitignoreProtections;` at bottom of install.cjs)"
  artifacts:
    - path: "bin/lib/cli/doctor-checks.cjs"
      provides: "runChecks with 9 check implementations + apply-fix helpers"
    - path: "bin/commands/doctor.cjs"
      provides: "markos doctor CLI entry + render + --check-only + --fix + --json"
    - path: "bin/install.cjs"
      provides: "exports applyGitignoreProtections for doctor reuse (refactor)"
  key_links:
    - from: "bin/commands/doctor.cjs"
      to: "bin/lib/cli/doctor-checks.cjs runChecks"
      via: "delegate + render"
      pattern: "runChecks"
    - from: "bin/lib/cli/doctor-checks.cjs gitignore check fix"
      to: "bin/install.cjs applyGitignoreProtections"
      via: "require export + invoke with cwd"
      pattern: "applyGitignoreProtections"
---

<objective>
Ship `markos doctor` — diagnose-and-fix mode that covers D-04 requirements: Node version floor, config dir writable, active token valid, `.markos-local/` exists + gitignored, keytar availability, server reachability. Support `--check-only` for CI (zero side-effects), `--fix` for auto-remediation (filesystem-only; never auth), `--json` for machine consumption.

Closes Wave 3. After this plan, all 11 CLI commands are functional + user-tested.
Output: 2 bin modules (doctor + doctor-checks) + install.cjs export refactor + 2 test files.
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/204-cli-markos-v1-ga/204-CONTEXT.md
@.planning/phases/204-cli-markos-v1-ga/204-RESEARCH.md
@.planning/phases/204-cli-markos-v1-ga/204-VALIDATION.md
@.planning/phases/204-cli-markos-v1-ga/204-01-PLAN.md
@.planning/phases/204-cli-markos-v1-ga/204-04-PLAN.md

<interfaces>
<!-- Plan 01 primitives: -->
```javascript
// bin/lib/cli/keychain.cjs getToken + listProfiles
// bin/lib/cli/config.cjs configPath + resolveProfile
// bin/lib/cli/http.cjs authedFetch + BASE_URL
// bin/lib/cli/output.cjs EXIT_CODES + shouldUseJson
// bin/lib/cli/errors.cjs formatError
// bin/cli-runtime.cjs assertSupportedNodeVersion (already exists)
```

<!-- bin/install.cjs refactor (current applyGitignoreProtections at line 109 — add export): -->
```javascript
// AFTER the function definition (line ~160ish, after final closing brace):
module.exports = {
  ...module.exports,
  applyGitignoreProtections,
};
// OR append at end of file:
module.exports.applyGitignoreProtections = applyGitignoreProtections;
// (Verify current install.cjs has no module.exports — check before adding)
```

<!-- Check result shape: -->
```javascript
{
  id:       'node_version',      // stable ID for CI grep
  label:    'Node.js version',   // human label
  status:   'ok' | 'warn' | 'error',
  message:  'v22.13.0 ≥ required 22.0.0',
  hint:     'Upgrade to Node 22 LTS or newer',      // only when not ok
  fixable:  true | false,        // can --fix remediate?
  fixed:    true | false | null, // did --fix actually remediate? (null if not attempted)
}
```

<!-- --check-only behavior precedent (RESEARCH): -->
```javascript
// --check-only === zero side effects; even --fix ignored. Useful for CI like:
//   if ! markos doctor --check-only; then exit 1; fi
```

<!-- Render examples (TTY): -->
```
┌─ markos doctor ──────────────────────────────────────┐
│ ✓ Node.js version         v22.13.0 ≥ required 22.0.0 │
│ ✓ Config directory         ~/.config/markos (writable)│
│ ⚠ Active token             no keychain entry          │
│   hint: Run `markos login` to get started             │
│ ✗ Gitignore protection    .markos-local/ not ignored  │
│   hint: Run `markos doctor --fix` to auto-add         │
│ ...                                                   │
└────────────────────────────────────────────────────────┘

Fixed 1 issue; 1 requires `markos login`.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: doctor-checks library (9 checks) + install.cjs export refactor + RED tests</name>
  <files>
    bin/lib/cli/doctor-checks.cjs,
    bin/install.cjs,
    test/cli/doctor-checks.test.js
  </files>
  <read_first>
    - bin/install.cjs lines 109-165 (applyGitignoreProtections function — must become exported)
    - bin/cli-runtime.cjs assertSupportedNodeVersion + compareSemver + MIN_NODE_VERSION
    - Plan 01 bin/lib/cli/keychain.cjs + config.cjs + http.cjs
    - 204-CONTEXT.md D-04 `markos doctor` spec
    - 204-RESEARCH.md Open Question 1 (doctor autofix safety — file fixes only, never auth)
  </read_first>
  <behavior>
    - Refactor: add `module.exports.applyGitignoreProtections = applyGitignoreProtections;` at end of bin/install.cjs (verify no existing module.exports conflict — bin/install.cjs is an executable script not a library, but can export for require-use; this is safe since require.main check at line 60-62ish guards direct-invocation behavior)
    - doctor-checks.cjs::runChecks({ fix, checkOnly, cwd? }): runs 9 checks in sequence; each check is a pure async function returning the result shape; applies fixes in --fix mode unless --check-only; returns array
    - check 1 (node_version): compareSemver(process.version.slice(1), '22.0.0') → ok/error; never fixable (user installs Node)
    - check 2 (config_dir): fs.accessSync(configPath()-parent, W_OK); on error → error + fixable via fs.mkdirSync(..., {recursive:true, mode:0o700})
    - check 3 (active_token): getToken(resolveProfile({})) → null → warn (not error — fresh install is normal); hint 'Run markos login'; not fixable here (never auto-run login)
    - check 4 (token_valid): if active_token warn → skip (status='skip'); else authedFetch /api/tenant/whoami with 5s timeout → 200 = ok; 401 = error (hint: token revoked, run markos login); network error = warn; not fixable
    - check 5 (markos_local_dir): fs.existsSync(path.resolve(cwd, '.markos-local')); missing → warn + fixable via mkdirSync
    - check 6 (gitignore_protected): read .gitignore in cwd; check for line matching `.markos-local/`; missing → error + fixable via applyGitignoreProtections(cwd); already-ignored → ok
    - check 7 (keytar_available): try require('keytar'); on fail → warn + hint 'XDG fallback file will be used; see docs/cli/installation.md'; success + listProfiles call works → ok; not fixable (user installs libsecret)
    - check 8 (server_reachable): authedFetch GET BASE_URL + '/api/health' with 5s AbortController timeout; fetch returns → ok; network fail → warn 'Cannot reach ${BASE_URL} — check internet'; not fixable
    - check 9 (supabase_connectivity): status='skip' with message 'skipped in 204; check Phase 206 observability' — future-proofs doctor for deeper checks
    - Each fix attempt in --fix mode sets result.fixed=true/false; --check-only mode never attempts (fixed=null always)
    - Test dc-01: node_version returns ok when process.version &gt;= 22.0.0 (stub compareSemver for lower-version test → error)
    - Test dc-02: config_dir fix creates directory (fs-mock or tmpdir)
    - Test dc-03: active_token no keychain entry → warn (not error)
    - Test dc-04: token_valid 401 → error
    - Test dc-05: markos_local_dir missing + --fix → creates dir + fixed=true
    - Test dc-06: gitignore_protected missing + --fix → applyGitignoreProtections called (spy) + fixed=true
    - Test dc-07: --check-only ignores --fix (never mutates FS even when fixable)
    - Test dc-08: keytar absent → warn (not error); listProfiles never invoked
    - Test dc-09: server_reachable timeout → warn
    - Test dc-10: all 9 checks return in order + each has id/label/status
    - Test dc-11: applyGitignoreProtections is exported from bin/install.cjs (require + typeof === 'function')
  </behavior>
  <action>
    Edit bin/install.cjs: at end of file (after last closing), add `module.exports.applyGitignoreProtections = applyGitignoreProtections;`. If bin/install.cjs already has a main execution guard via `if (require.main === module) { main(); }` pattern — verify the export doesn't trigger main when required by doctor-checks.cjs. If missing, wrap the bottom-of-file main invocation in that guard first. Implement bin/lib/cli/doctor-checks.cjs with 9 checks as pure async functions + orchestrator. Use AbortController for fetch timeout. Tests use fs tmpdir + fetch-mock + require.cache override.
  </action>
  <validation>
    <automated>node --test test/cli/doctor-checks.test.js</automated>
    <nyquist>VALIDATION.md row: CLI-01 doctor 9 checks + --fix + --check-only + applyGitignoreProtections export</nyquist>
  </validation>
  <acceptance_criteria>
    - `ls bin/lib/cli/doctor-checks.cjs test/cli/doctor-checks.test.js` both present
    - `grep -c "runChecks" bin/lib/cli/doctor-checks.cjs` &gt;= 1
    - `grep -cE "node_version|config_dir|active_token|token_valid|markos_local_dir|gitignore_protected|keytar_available|server_reachable|supabase_connectivity" bin/lib/cli/doctor-checks.cjs` &gt;= 9
    - `grep -c "applyGitignoreProtections" bin/lib/cli/doctor-checks.cjs` &gt;= 1
    - `grep -c "module.exports.applyGitignoreProtections\\|module.exports = {.*applyGitignoreProtections" bin/install.cjs` &gt;= 1
    - `grep -c "checkOnly\\|check_only\\|check-only" bin/lib/cli/doctor-checks.cjs` &gt;= 1
    - `node -e "const { applyGitignoreProtections } = require('./bin/install.cjs'); if (typeof applyGitignoreProtections !== 'function') process.exit(1); console.log('OK')"` exits 0
    - `node --test test/cli/doctor-checks.test.js` exits 0 with &gt;= 10 assertions
  </acceptance_criteria>
  <done>doctor-checks library ships 9 checks + applyGitignoreProtections exported from install.cjs; 10+ tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: markos doctor CLI + render + integration tests</name>
  <files>
    bin/commands/doctor.cjs,
    test/cli/doctor.test.js
  </files>
  <read_first>
    - Plan 01 output (bin/lib/cli/*)
    - Task 1 output: bin/lib/cli/doctor-checks.cjs runChecks signature
    - CONTEXT.md §Developer delight touches (`markos doctor` prints final checklist with ✓/✗ + 1-line fix hints per `brew doctor`)
    - 204-CONTEXT.md D-04 (--check-only for CI)
  </read_first>
  <behavior>
    - doctor.cjs main({ cli }):
      - parse cli.checkOnly (--check-only), cli.fix (--fix, default true when not check-only), cli.json (--json)
      - call runChecks({ fix: cli.fix && !cli.checkOnly, checkOnly: cli.checkOnly, cwd: process.cwd() })
      - Render:
        - TTY: boxed header + per-check row (✓/⚠/✗ + label + message in color variants green/yellow/red); show hint on new line if present; show 'fixed' marker on fixed items; footer summary `Fixed ${fixedCount} issue(s); ${errorCount} require attention`
        - JSON (--json or non-TTY): output.render({ checks: [...], summary: { total, ok, warn, error, fixed } }) as single-line JSON
      - Exit codes:
        - 0 if all status IN ('ok', 'warn') — warnings don't fail doctor by default
        - 1 if ANY status === 'error' (unless error was fixed successfully, which flips to 'ok' in summary → still 0)
        - 2 if network error prevented server_reachable check (transient)
      - `--check-only` + any error = exit 1 (CI gate friendly)
    - Test doc-01: all-green mock → exit 0 + TTY render contains ✓
    - Test doc-02: gitignore_protected error + --fix → fix applied + exit 0
    - Test doc-03: token_valid error (no fix possible) → exit 1
    - Test doc-04: --check-only + gitignore error → exit 1 (no fix attempted; fixed=null)
    - Test doc-05: --json outputs single-line JSON with summary
    - Test doc-06: TTY render grep'able for ✓ ⚠ ✗ unicode chars
    - Test doc-07: --quiet suppresses INFO/warnings, keeps errors only
    - Test doc-08: network-only warn-level → exit 0 (warn is not a fail)
  </behavior>
  <action>
    Implement bin/commands/doctor.cjs. Replace Plan 01 stub. Use output.render + color code mapping (\\x1b[32m green ok, \\x1b[33m yellow warn, \\x1b[31m red error). Tests stub runChecks via module-cache override or dependency injection pattern.
  </action>
  <validation>
    <automated>node --test test/cli/doctor.test.js</automated>
    <nyquist>VALIDATION.md row: CLI-01 doctor --check-only CI gate + --fix auto-remediation</nyquist>
  </validation>
  <acceptance_criteria>
    - `ls bin/commands/doctor.cjs test/cli/doctor.test.js` both present
    - `grep -c "not yet implemented" bin/commands/doctor.cjs` == 0
    - `grep -c "runChecks" bin/commands/doctor.cjs` &gt;= 1
    - `grep -c "cli.checkOnly\\|--check-only" bin/commands/doctor.cjs` &gt;= 1
    - `grep -c "cli.fix\\|--fix" bin/commands/doctor.cjs` &gt;= 1
    - `grep -c "✓\\|✗\\|⚠\\|OK\\|ERROR" bin/commands/doctor.cjs` &gt;= 3
    - `grep -c "process.exit(0)\\|process.exit(1)\\|exit.*EXIT_CODES" bin/commands/doctor.cjs` &gt;= 1
    - `node --test test/cli/doctor.test.js` exits 0 with &gt;= 8 assertions
  </acceptance_criteria>
  <done>markos doctor CLI delivers brew-doctor-style output + --check-only CI gate + --fix auto-remediation; 8 tests green; Wave 3 COMPLETE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI `doctor --fix` → filesystem | Limited to config dir + .markos-local + .gitignore — never auth |
| CLI `doctor` → network probes | 5s timeout; never leaks token; only BASE_URL + /api/health |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-204-09-01 | Tampering | --fix auto-runs markos login (phishing vector) | mitigate | active_token + token_valid checks NEVER mark `fixable: true` — user prompted to run markos login explicitly (CONTEXT + RESEARCH OQ1). Test dc-03 covers |
| T-204-09-02 | Tampering | --fix overwrites existing .gitignore | mitigate | applyGitignoreProtections is idempotent (Plan 01 verified — replaces managed-block only, preserves other lines) |
| T-204-09-03 | Info Disclosure | server_reachable probe leaks CLI version in User-Agent | accept | authedFetch sets x-markos-client: markos-cli/&lt;version&gt; by convention; not sensitive; aids server-side observability |
| T-204-09-04 | DoS | Rapid `markos doctor` calls from CI storm | accept | Each check is cheap; server-side /api/health is designed for this |
| T-204-09-05 | Info Disclosure | Error hint exposes full file paths | accept | Local paths (~/.config/markos) are user-specific; helpful for debugging |
| T-204-09-06 | Tampering | --check-only mis-implemented and still mutates | mitigate | runChecks({ checkOnly: true }) explicit guard before any FS write; Test dc-07 grep-asserts no mkdir/writeFile called |
</threat_model>

<verification>
- Task 1: `node --test test/cli/doctor-checks.test.js` → &gt;= 10 green
- Task 2: `node --test test/cli/doctor.test.js` → &gt;= 8 green
- Phase: `npm test` green
- Integration: `markos doctor --check-only` exit 0 on healthy install (manual verify)
- **Wave 3 COMPLETE** after this plan — Plans 10+ are distribution-only
</verification>

<success_criteria>
- 9 checks cover all D-04 diagnostic surfaces
- --check-only zero side effects; --fix auto-remediates FS-only items; --json for CI
- applyGitignoreProtections exported + reused (no duplication)
- 18+ new tests green
- **11/11 CLI commands functional post this plan**
</success_criteria>

<output>
After completion, create `.planning/phases/204-cli-markos-v1-ga/204-09-SUMMARY.md`.
</output>
