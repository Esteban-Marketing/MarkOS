---
phase: 204
plan: 13
type: execute
wave: 4
depends_on: [06, 08, 09, 12]
files_modified:
  - supabase/migrations/77_markos_cli_runs_v2_align.sql
  - supabase/migrations/rollback/77_markos_cli_runs_v2_align.down.sql
  - lib/markos/cli/runs.cjs
  - lib/markos/cli/status.cjs
  - bin/lib/cli/doctor-checks.cjs
  - test/cli/doctor-checks.test.js
  - test/cli/v2-compliance.test.js
  - contracts/F-103-cli-runs-v1.yaml
  - contracts/openapi.json
  - contracts/openapi.yaml
autonomous: true
task_count: 3
requirements: [CLI-01, QA-01, QA-04, QA-09, QA-11, QA-13, QA-15]

locked_assumption:
  A13: "204-13 is a bounded CLI compatibility lock. It translates already-known Pricing Engine placeholder doctrine, AgentRun v2 field doctrine, and vault freshness doctrine into present-tense CLI guardrails, but it does not wait on active implementation from Phases 205, 207, 214, or 218."

must_haves:
  truths:
    - "Migration 77 additively aligns `markos_cli_runs` to the CLI-visible AgentRun v2 posture with the exact columns `idempotency_key`, `parent_run_id`, `task_id`, `approval_policy`, `provider_policy`, `tool_policy`, `pricing_engine_context`, `cost_currency`, `tokens_input`, `tokens_output`, `retry_count`, `retry_after`, `last_error_code`, `closed_at`, and `v2_state`, plus deterministic back-fill from legacy status and an idempotent rollback file."
    - "lib/markos/cli/runs.cjs exports `buildV2Payload`, `assertV2PayloadShape`, `deriveIdempotencyKey`, `V2_REQUIRED_FIELDS`, `STATE_V1_TO_V2_MAP`, and `PRICING_PLACEHOLDER_SENTINEL`, and `submitRun` keeps a v1-safe fallback for pre-migration-77 databases."
    - "lib/markos/cli/status.cjs recent_runs projection surfaces `v2_state`, `priority`, `estimated_cost_usd_micro`, and `closed_at` when present while remaining safe for legacy rows."
    - "bin/lib/cli/doctor-checks.cjs adds `agentrun_v2_alignment`, `pricing_placeholder_policy`, and `vault_freshness`; hard-coded public pricing is error-level, vault freshness is warn-level, and `--check-only` remains CI-safe."
    - "contracts/F-103-cli-runs-v1.yaml gains only additive request-body fields and the priority enum extension needed for the v2-shaped CLI payload; `contracts/openapi.json` and `contracts/openapi.yaml` regenerate from that additive contract change."
    - "Freshness checks for doctrine surfaces such as `16-SAAS-SUITE.md` and `17-SAAS-MARKETING-OS-STRATEGY.md` are content-health checks only; they do not claim Phases 214 or 218 are implemented."
  artifacts:
    - path: "supabase/migrations/77_markos_cli_runs_v2_align.sql"
      provides: "additive CLI-run schema alignment for the late compatibility lock"
    - path: "lib/markos/cli/runs.cjs"
      provides: "canonical CLI-side v2 payload writer plus fallback and placeholder exports"
    - path: "bin/lib/cli/doctor-checks.cjs"
      provides: "v2-alignment, placeholder-policy, and vault-freshness checks"
    - path: "test/cli/v2-compliance.test.js"
      provides: "targeted regression proof for the gap closure"
  key_links:
    - from: "supabase/migrations/77_markos_cli_runs_v2_align.sql"
      to: "lib/markos/cli/runs.cjs"
      via: "the CLI writer and the table shape must evolve additively together"
      pattern: "v2_state|pricing_engine_context|idempotency_key"
    - from: "bin/lib/cli/doctor-checks.cjs"
      to: "contracts/F-103-cli-runs-v1.yaml"
      via: "doctor checks and contract shape must agree on the same v2-required fields"
      pattern: "agentrun_v2_alignment|V2_REQUIRED_FIELDS"
    - from: "bin/lib/cli/doctor-checks.cjs"
      to: "docs/cli/errors.md and public doctrine pages"
      via: "placeholder and freshness enforcement is a content-policy check, not a future-phase implementation claim"
      pattern: "MARKOS_PRICING_ENGINE_PENDING|SAAS-SUITE|SAAS-MARKETING-OS-STRATEGY"
---

<objective>
Close the late CLI GA doctrine gap: align `run`, `status`, and `doctor` with the locked v2 run shape, pricing placeholder policy, and vault freshness posture while keeping the work additive and self-contained.

This plan translates future doctrine into current CLI safeguards. It does not move Phase 205, 207, 214, or 218 implementation into Phase 204.
</objective>

<context>
@.planning/STATE.md
@.planning/phases/204-cli-markos-v1-ga/204-CONTEXT.md
@.planning/phases/204-cli-markos-v1-ga/204-RESEARCH.md
@.planning/phases/204-cli-markos-v1-ga/204-REVIEWS.md
@.planning/phases/204-cli-markos-v1-ga/204-VALIDATION.md
@.planning/phases/204-cli-markos-v1-ga/204-VERIFICATION.md
@.planning/phases/204-cli-markos-v1-ga/204-06-PLAN.md
@.planning/phases/204-cli-markos-v1-ga/204-08-PLAN.md
@.planning/phases/204-cli-markos-v1-ga/204-09-PLAN.md
@.planning/phases/204-cli-markos-v1-ga/204-12-PLAN.md
@.planning/phases/207-agentrun-v2-orchestration-substrate/207-01-CONTRACT-LOCK.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: additive CLI-run schema alignment and v2 payload writer</name>
  <files>
    supabase/migrations/77_markos_cli_runs_v2_align.sql,
    supabase/migrations/rollback/77_markos_cli_runs_v2_align.down.sql,
    lib/markos/cli/runs.cjs
  </files>
  <read_first>
    - 204-06-PLAN.md
    - 204-RESEARCH.md gap-closure addendum
    - 207-01-CONTRACT-LOCK.md sections that define the CLI-visible AgentRun v2 field posture
    - 204-13-SUMMARY.md
  </read_first>
  <behavior>
    - Add migration 77 as an additive alignment step instead of rewriting migration 75 in place.
    - Back-fill `v2_state` from the legacy status so old rows remain readable.
    - Export a single shared required-field list and a deterministic idempotency-key helper from `lib/markos/cli/runs.cjs`.
    - Keep a v1-safe insert fallback so the CLI can detect drift safely without hard-crashing on pre-migration databases.
  </behavior>
  <validation>
    <automated>node --test test/cli/v2-compliance.test.js</automated>
  </validation>
  <acceptance_criteria>
    - `supabase/migrations/77_markos_cli_runs_v2_align.sql` contains the exact fields `idempotency_key`, `pricing_engine_context`, and `v2_state`
    - `lib/markos/cli/runs.cjs` contains the exact exports `buildV2Payload`, `assertV2PayloadShape`, `deriveIdempotencyKey`, `V2_REQUIRED_FIELDS`, `STATE_V1_TO_V2_MAP`, and `PRICING_PLACEHOLDER_SENTINEL`
    - legacy rows remain readable through deterministic `status -> v2_state` synthesis
  </acceptance_criteria>
  <done>Additive schema alignment exists and the CLI can emit a canonical v2-shaped payload without requiring the full Phase 207 runtime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: v2-aware status projection and bounded doctrine checks</name>
  <files>
    lib/markos/cli/status.cjs,
    bin/lib/cli/doctor-checks.cjs,
    test/cli/doctor-checks.test.js
  </files>
  <read_first>
    - 204-08-PLAN.md
    - 204-09-PLAN.md
    - 204-CONTEXT.md non-negotiables and planning emphasis
    - 204-RESEARCH.md gap-closure addendum
  </read_first>
  <behavior>
    - Extend recent run projection with the additive v2-facing fields without breaking legacy rows.
    - Add `agentrun_v2_alignment`, `pricing_placeholder_policy`, and `vault_freshness` checks.
    - Treat hard-coded public pricing as CI-failing policy drift.
    - Treat SaaS Suite and growth doctrine pages as freshness surfaces only; do not imply those future phases are implemented.
  </behavior>
  <validation>
    <automated>node --test test/cli/doctor-checks.test.js test/cli/v2-compliance.test.js</automated>
  </validation>
  <acceptance_criteria>
    - `lib/markos/cli/status.cjs` contains the exact strings `v2_state`, `priority`, `estimated_cost_usd_micro`, and `closed_at`
    - `bin/lib/cli/doctor-checks.cjs` contains the exact checks `agentrun_v2_alignment`, `pricing_placeholder_policy`, and `vault_freshness`
    - `bin/lib/cli/doctor-checks.cjs` contains the exact token `MARKOS_PRICING_ENGINE_PENDING`
    - doctrine freshness coverage can mention `16-SAAS-SUITE.md` and `17-SAAS-MARKETING-OS-STRATEGY.md` without claiming those later phases are implemented
  </acceptance_criteria>
  <done>`status` and `doctor` now surface the CLI-visible v2 posture and bounded content-policy checks without turning future-phase doctrine into hidden runtime blockers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: additive contract refresh and regression proof</name>
  <files>
    contracts/F-103-cli-runs-v1.yaml,
    contracts/openapi.json,
    contracts/openapi.yaml,
    test/cli/v2-compliance.test.js
  </files>
  <read_first>
    - 204-06-PLAN.md contract notes
    - 204-13-SUMMARY.md
    - contracts/F-103-cli-runs-v1.yaml
  </read_first>
  <behavior>
    - Keep all contract changes additive.
    - Extend the targeted regression suite so the gap closure is provable in isolation.
    - Ensure the compatibility lock is documented through tests rather than through implicit future-phase assumptions.
  </behavior>
  <validation>
    <automated>node --test test/cli/v2-compliance.test.js test/cli/doctor-checks.test.js</automated>
  </validation>
  <acceptance_criteria>
    - `contracts/F-103-cli-runs-v1.yaml` gains only additive request-body fields
    - `test/cli/v2-compliance.test.js` contains at least one payload-shape assertion, one placeholder-policy assertion, and one vault-freshness assertion
    - `contracts/openapi.json` and `contracts/openapi.yaml` regenerate from the additive contract change
  </acceptance_criteria>
  <done>The late gap closure is backed by additive contract evidence and focused regression tests, not by implied future work.</done>
</task>

</tasks>

<verification>
- node --test test/cli/v2-compliance.test.js
- node --test test/cli/doctor-checks.test.js
</verification>

<success_criteria>
- `204-13` validates in the executable GSD schema
- CLI run/status/doctor surfaces align additively with the v2 compatibility target
- placeholder and freshness doctrine are enforced without claiming later phases are implemented
- the late gap closure stays self-contained instead of depending on active implementation from future phases
</success_criteria>
