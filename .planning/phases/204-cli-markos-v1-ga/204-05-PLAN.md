---
phase: 204
plan: 05
type: execute
wave: 2
depends_on: [01, 04]
files_modified:
  - bin/commands/init.cjs
  - bin/commands/plan.cjs
  - bin/commands/eval.cjs
  - bin/lib/eval-runner.cjs
  - api/tenant/runs/plan.js
  - test/cli/init.test.js
  - test/cli/plan.test.js
  - test/cli/eval.test.js
  - test/cli/runs-plan-endpoint.test.js
autonomous: true
task_count: 3
requirements: [CLI-01, QA-01, QA-02, QA-04, QA-11]

must_haves:
  truths:
    - "bin/commands/init.cjs is a thin wrapper that delegates to bin/install.cjs with `{ yes: true, noOnboarding: !cli.onboarding }`; it does NOT re-implement the installer; purpose is to provide `markos init` as a discoverable command alongside the preserved `npx markos` interactive entry"
    - "bin/commands/plan.cjs reads a brief.yaml via existing bin/lib/brief-parser.cjs, resolves profile + token, calls POST /api/tenant/runs/plan with the normalized brief, receives a { steps: [...], estimated_cost, estimated_tokens } envelope, and renders either table (TTY) or JSON (non-TTY). No state change server-side — pure dry-run."
    - "POST /api/tenant/runs/plan endpoint accepts Bearer auth (resolves tenant via whoami primitive Plan 04) OR legacy session headers; validates brief body against REQUIRED_FIELDS from brief-parser; returns { run_id: null, steps: Array<{name, inputs, estimated_tokens}>, estimated_cost_usd, estimated_tokens, tenant_id }; NO database write; idempotent (safe to re-call); member role sufficient (read-only inspection)"
    - "bin/commands/eval.cjs runs LOCAL rubric scoring — reads brief (--brief or positional); optionally reads --draft=<path> for pre-generated draft OR calls bin/lib/generate-runner.cjs::runDraft internally; applies lib/eval-runner.cjs::scoreDraft which produces { score: 0-100, dimensions: { voice, claims, structure, length }, issues: [] }; no tenant round-trip (CONTEXT D-03 'fully local; pluggable LLM'); exits 1 if score < threshold (default 60, override --threshold)"
    - "bin/lib/eval-runner.cjs::scoreDraft(draft, brief, { llm?: function }) runs 4 rubric checks: (a) voice alignment (brand keyword presence >= 1); (b) claims (promise substring in draft); (c) structure (at least 2 sentences); (d) length (20-300 words); returns dimensions with 0/25 scoring, overall score = sum, issues[] populated for any 0-score dimension; pluggable llm function (default: pure-rubric no-LLM mode) — matches 200-02 pluggable-llm precedent"
    - "All 3 commands (init, plan, eval) respect --profile + --json + --quiet + exit-code discipline (0/1/2/3/4/5 per D-10)"
  artifacts:
    - path: "bin/commands/init.cjs"
      provides: "markos init — delegates to bin/install.cjs programmatically"
    - path: "bin/commands/plan.cjs"
      provides: "markos plan <brief> — dry-run planner"
    - path: "bin/commands/eval.cjs"
      provides: "markos eval <brief> [--draft=<path>] — local rubric scoring"
    - path: "bin/lib/eval-runner.cjs"
      provides: "scoreDraft pluggable-llm primitive"
    - path: "api/tenant/runs/plan.js"
      provides: "POST /api/tenant/runs/plan — dry-run planner endpoint (no DB write)"
  key_links:
    - from: "bin/commands/plan.cjs"
      to: "bin/lib/brief-parser.cjs parseBrief"
      via: "reuse 200-02 parser"
      pattern: "parseBrief"
    - from: "bin/commands/eval.cjs"
      to: "bin/lib/eval-runner.cjs scoreDraft"
      via: "local scoring (no tenant call)"
      pattern: "scoreDraft"
    - from: "bin/commands/init.cjs"
      to: "bin/install.cjs"
      via: "require + invoke with --yes default"
      pattern: "require.*install.cjs\\|install.cjs"
---

<objective>
Ship 3 Wave-2 workspace commands + 1 new server endpoint:
- `markos init` → discoverable alias for the existing interactive installer (delegates, never duplicates logic)
- `markos plan <brief>` → calls new tenant endpoint `/api/tenant/runs/plan` → dry-run showing which steps would execute, no state change
- `markos eval <brief> [--draft=<path>]` → local rubric scoring, fully offline (CONTEXT D-03)

Purpose: Developer delight suite — users can inspect + validate briefs before running them (saves tenant quota). `plan` is a natural introspection command; `eval` is the contract with the 200-02 audit layer (pluggable LLM + dimension scoring). `init` closes the discoverability gap (users expect `markos init` to exist alongside `gh auth login`-style flat commands).
Output: 3 command modules + 1 shared lib primitive + 1 new endpoint (part of F-103 which Plan 06 completes) + 4 test files.
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/204-cli-markos-v1-ga/204-CONTEXT.md
@.planning/phases/204-cli-markos-v1-ga/204-RESEARCH.md
@.planning/phases/204-cli-markos-v1-ga/204-VALIDATION.md
@.planning/phases/204-cli-markos-v1-ga/204-01-PLAN.md
@.planning/phases/204-cli-markos-v1-ga/204-04-PLAN.md
@.planning/phases/200-saas-readiness-wave-0/200-02-cli-generate-SUMMARY.md

<interfaces>
<!-- From 200-02 brief parser (REUSE, don't re-implement): -->
```javascript
const { parseBrief, validateBrief, normalizeBrief } = require('../lib/brief-parser.cjs');
// parseBrief(source) — accepts path or inline text (YAML subset OR JSON)
// validateBrief(brief) — returns { ok, errors: string[] }
// normalizeBrief(brief) — trims + preserves extras
// REQUIRED_FIELDS = ['channel', 'audience', 'pain', 'promise', 'brand']
```

<!-- From 200-02 runDraft (REUSE for --draft regeneration mode): -->
```javascript
const { runDraft, stubLlm, auditDraft } = require('../lib/generate-runner.cjs');
// runDraft(source, { llm? }) → { success, draft: { text }, audit: { status, issues } }
```

<!-- From Plan 04 resolveWhoami (REUSE for tenant resolution): -->
```javascript
const { resolveWhoami } = require('../../../lib/markos/cli/whoami.cjs');
// Bearer hash → { tenant_id, role, ... }
```

<!-- From Plan 01 primitives: -->
```javascript
// bin/lib/cli/http.cjs authedFetch + BASE_URL
// bin/lib/cli/keychain.cjs getToken
// bin/lib/cli/config.cjs resolveProfile
// bin/lib/cli/errors.cjs formatError
// bin/lib/cli/output.cjs EXIT_CODES + render
```

<!-- From bin/install.cjs (REUSE programmatically): -->
```javascript
// bin/install.cjs is designed for direct invocation but exposes helpers via require.
// init command strategy: spawn node subprocess invoking install.cjs with --yes flag
//   (matches how npx markos is invoked; avoids global state mutation inside parent process).
```

<!-- Eval rubric spec (NEW): -->
```javascript
// scoreDraft(draft, brief) returns:
// {
//   score: number,                // 0-100, sum of 4 dimensions
//   dimensions: {
//     voice:     number,           // 0 or 25 — brand keyword presence
//     claims:    number,           // 0 or 25 — promise substring present
//     structure: number,           // 0 or 25 — >= 2 sentences
//     length:    number            // 0 or 25 — 20-300 words
//   },
//   issues: string[]               // human-readable failures
// }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: markos init delegator + tests</name>
  <files>
    bin/commands/init.cjs,
    test/cli/init.test.js
  </files>
  <read_first>
    - bin/install.cjs lines 1-100 (installer entry + arguments parsing via parseCliArgs)
    - 204-CONTEXT.md D-02/D-04 scope (init delegates; does NOT own interactive logic)
    - bin/cli-runtime.cjs parseCliArgs (--yes flag already supported)
    - Plan 01 bin/lib/cli/errors.cjs + output.cjs (exit code discipline)
  </read_first>
  <behavior>
    - init.cjs main({ cli }): spawns `node bin/install.cjs --yes ${cli.noOnboarding ? '--no-onboarding' : ''} ${cli.preset ? '--preset=' + cli.preset : ''}`; forwards stdio (inherit); exits with child's exit code
    - Alternative invocation (flag --programmatic): require install.cjs directly — used by tests to avoid subprocess spawn
    - If cli.help → print one-line usage 'markos init [--no-onboarding] [--preset=<bucket>]' + exit 0
    - Test init-01: main() spawns install.cjs subprocess with --yes flag (spy on child_process.spawn)
    - Test init-02: --no-onboarding flag forwarded (spy assertion)
    - Test init-03: --preset=b2b-saas forwarded (spy assertion)
    - Test init-04: --help prints usage + exit 0
    - Test init-05: subprocess exit 0 → parent exits 0
    - Test init-06: subprocess exit non-zero → parent propagates same exit code
  </behavior>
  <action>
    Implement bin/commands/init.cjs as thin delegator using node:child_process.spawn('node', [installScript, '--yes', ...]); inherit stdio; process.exit(child.status). Test via child_process.spawn mock (copy test/cli-generate.test.js spawnSync shape).
  </action>
  <validation>
    <automated>node --test test/cli/init.test.js</automated>
    <nyquist>VALIDATION.md row: CLI-01 markos init delegates to install.cjs --yes</nyquist>
  </validation>
  <acceptance_criteria>
    - `ls bin/commands/init.cjs test/cli/init.test.js` both present
    - `grep -c "not yet implemented" bin/commands/init.cjs` == 0
    - `grep -c "child_process\\|spawn" bin/commands/init.cjs` &gt;= 1
    - `grep -c "install.cjs" bin/commands/init.cjs` &gt;= 1
    - `grep -c "'--yes'\\|--yes" bin/commands/init.cjs` &gt;= 1
    - `node --test test/cli/init.test.js` exits 0 with &gt;= 6 assertions
  </acceptance_criteria>
  <done>markos init delegates cleanly without duplicating installer logic; 6 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /api/tenant/runs/plan endpoint + markos plan CLI + dry-run shape</name>
  <files>
    api/tenant/runs/plan.js,
    bin/commands/plan.cjs,
    test/cli/plan.test.js,
    test/cli/runs-plan-endpoint.test.js
  </files>
  <read_first>
    - Plan 04 api/tenant/whoami.js (Bearer auth extraction pattern — copy)
    - Plan 04 lib/markos/cli/whoami.cjs (resolveWhoami signature for tenant resolution)
    - bin/lib/brief-parser.cjs (REQUIRED_FIELDS + validateBrief return shape)
    - api/tenant/webhooks/subscriptions/list.js (writeJson + tenant-scope pattern)
    - 204-RESEARCH.md §New API Surface Inventory F-103 row (this plan ships /plan; Plan 06 ships /runs POST + /events SSE)
  </read_first>
  <behavior>
    - POST /api/tenant/runs/plan: method gate (POST); extract Bearer OR legacy session headers → resolve to tenant_id; parse JSON body { brief: object }; validateBrief → if errors → 400 { error: 'invalid_brief', errors: [...] }; normalize brief; compute plan envelope inline (step list hard-coded for v1: [{ name: 'audit', inputs: ['brief'], estimated_tokens: 200 }, { name: 'draft', inputs: ['brief','audit'], estimated_tokens: 1500 }, { name: 'score', inputs: ['draft','brief'], estimated_tokens: 300 }]); estimated_cost_usd = estimated_tokens * 0.000003 (placeholder using Claude-tier rate); returns 200 { run_id: null, steps: [...], estimated_tokens: 2000, estimated_cost_usd: 0.006, tenant_id }; NO DB write; NO audit emit (read-only introspection)
    - bin/commands/plan.cjs main({ cli }): read brief (cli.positional[0] or --brief flag); parseBrief → validateBrief → if errors → formatError + exit 1; resolveProfile + getToken; if !token → exit 3; authedFetch POST /api/tenant/runs/plan with { brief: normalized }; on 200 → render TTY table (3 rows: step, inputs, est_tokens) + summary line + estimated_cost; non-TTY → JSON; on 400 → exit 1 with issues; on 401 → exit 3; on 5xx → exit 2
    - Test pep-01: endpoint 401 no auth
    - Test pep-02: endpoint 400 missing required fields (channel absent) → errors listed
    - Test pep-03: endpoint 200 happy → returns 3 steps + tokens + cost + tenant_id matching resolved token
    - Test pep-04: endpoint is read-only (spy on supabase.from assertions — no INSERT/UPDATE calls)
    - Test p-01: CLI brief path missing → exit 1 'usage'
    - Test p-02: CLI invalid YAML → exit 1 'invalid_brief'
    - Test p-03: CLI happy → authedFetch called with normalized brief → exits 0
    - Test p-04: CLI non-TTY JSON output contains 'steps' array + 'estimated_cost_usd'
    - Test p-05: CLI 401 response → exits 3
  </behavior>
  <action>
    Implement api/tenant/runs/plan.js as read-only handler. Reuse validateBrief from bin/lib/brief-parser.cjs (require from lib side — verify path `../../../bin/lib/brief-parser.cjs` works; if not, duplicate REQUIRED_FIELDS enum inline + call validateBrief via require of brief-parser — or move parser to lib/markos/cli/brief-parser.cjs as duplicate export + leave bin/ unchanged). For simplicity this plan's choice: `require('../../../bin/lib/brief-parser.cjs')` in handler — cross-package require works in Node 22 given bin/ is committed to repo. Implement bin/commands/plan.cjs. Write 9 tests total.
  </action>
  <validation>
    <automated>node --test test/cli/plan.test.js test/cli/runs-plan-endpoint.test.js</automated>
    <nyquist>VALIDATION.md row: CLI-01 markos plan dry-run + endpoint no-write invariant</nyquist>
  </validation>
  <acceptance_criteria>
    - `ls api/tenant/runs/plan.js bin/commands/plan.cjs test/cli/plan.test.js test/cli/runs-plan-endpoint.test.js` all present
    - `grep -c "not yet implemented" bin/commands/plan.cjs` == 0
    - `grep -c "parseBrief\\|validateBrief" bin/commands/plan.cjs` &gt;= 1
    - `grep -c "/api/tenant/runs/plan" bin/commands/plan.cjs` &gt;= 1
    - `grep -c "estimated_tokens\\|estimated_cost_usd" api/tenant/runs/plan.js` &gt;= 2
    - `grep -c "\\.insert\\|\\.update\\|\\.upsert" api/tenant/runs/plan.js` == 0 (strict no-DB-write invariant)
    - `grep -c "run_id: null\\|run_id: null" api/tenant/runs/plan.js` &gt;= 1 (signals dry-run shape)
    - `node --test test/cli/plan.test.js test/cli/runs-plan-endpoint.test.js` exits 0 with &gt;= 9 assertions
  </acceptance_criteria>
  <done>Plan endpoint ships as no-write dry-run; CLI renders steps + cost; 9 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: markos eval CLI + eval-runner lib + local rubric scoring</name>
  <files>
    bin/lib/eval-runner.cjs,
    bin/commands/eval.cjs,
    test/cli/eval.test.js
  </files>
  <read_first>
    - bin/lib/generate-runner.cjs (runDraft + auditDraft structure — we mirror for scoreDraft)
    - 200-02-cli-generate-SUMMARY.md (pluggable-llm-stub pattern; audit tiered severity)
    - bin/lib/brief-parser.cjs (parseBrief + normalizeBrief to reuse)
    - 204-CONTEXT.md D-03 ('Local LLM-rubric quality scoring … no tenant round-trip (fully local; pluggable LLM)')
  </read_first>
  <behavior>
    - eval-runner.cjs::scoreDraft(draft, brief, { llm? } = {}):
      - draft.text → extract words (split on whitespace) + sentences (split on `[.!?]`)
      - voice dimension: `draft.text.toLowerCase().includes(brief.brand.toLowerCase())` → 25 else 0 + issue 'brand not mentioned'
      - claims dimension: `draft.text.toLowerCase().includes(brief.promise.toLowerCase().slice(0, 40))` → 25 else 0 + issue 'promise substring missing'
      - structure dimension: sentences.filter(s => s.trim()).length >= 2 → 25 else 0 + issue 'too few sentences'
      - length dimension: word count in [20, 300] → 25 else 0 + issue `length ${wordCount} out of range (20-300)`
      - score = sum of dimensions
      - if llm param provided → second-pass refinement via llm({ draft, brief, rubric: dimensions }) OR no-op
      - returns { score, dimensions, issues, word_count, sentence_count }
    - eval.cjs main({ cli }):
      - parse brief (positional[0] or --brief) + normalize
      - if cli.draft → read draft text from file path; else call bin/lib/generate-runner.cjs::runDraft to generate one
      - call scoreDraft(draft, brief)
      - threshold = Number(cli.threshold) || 60
      - TTY render: score bar + dimension table + issues list
      - non-TTY JSON: full object
      - exits: 0 if score >= threshold, 1 otherwise (clean CI composition)
    - Test eval-01: scoreDraft happy coherent text → score 100 + all dimensions 25
    - Test eval-02: scoreDraft missing brand → voice 0 + issue
    - Test eval-03: scoreDraft missing promise → claims 0
    - Test eval-04: scoreDraft 1 sentence → structure 0
    - Test eval-05: scoreDraft length 5 words → length 0
    - Test eval-06: CLI eval brief.yaml --draft=draft.txt → exits 0 when score >= 60
    - Test eval-07: CLI eval brief.yaml --threshold=80 + draft score 60 → exits 1
    - Test eval-08: CLI eval brief.yaml (no --draft) regenerates via runDraft → score displayed
    - Test eval-09: JSON output in non-TTY contains score + dimensions + issues
  </behavior>
  <action>
    Implement bin/lib/eval-runner.cjs with scoreDraft export — pure function, no I/O, no LLM by default (pluggable llm optional). Implement bin/commands/eval.cjs; reuse brief-parser + generate-runner. Tests use fixtures: tmpBriefFile + tmpDraftFile helpers (copy pattern from test/cli-generate.test.js lines 13-20).
  </action>
  <validation>
    <automated>node --test test/cli/eval.test.js</automated>
    <nyquist>VALIDATION.md row: CLI-01 markos eval local rubric scoring</nyquist>
  </validation>
  <acceptance_criteria>
    - `ls bin/lib/eval-runner.cjs bin/commands/eval.cjs test/cli/eval.test.js` all present
    - `grep -c "not yet implemented" bin/commands/eval.cjs` == 0
    - `grep -c "scoreDraft" bin/lib/eval-runner.cjs` &gt;= 1
    - `grep -cE "voice|claims|structure|length" bin/lib/eval-runner.cjs` &gt;= 4
    - `grep -c "--threshold\\|cli.threshold\\|threshold" bin/commands/eval.cjs` &gt;= 1
    - `grep -c "runDraft\\|cli.draft" bin/commands/eval.cjs` &gt;= 1
    - `grep -c "/api/tenant" bin/commands/eval.cjs` == 0 (NO tenant round-trip per D-03)
    - `node --test test/cli/eval.test.js` exits 0 with &gt;= 9 assertions
  </acceptance_criteria>
  <done>eval-runner + eval CLI work fully local; 4-dimension rubric with clear issue text; 9 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI brief file → server endpoint | Validated via validateBrief before accepted |
| /api/tenant/runs/plan → no DB write | Read-only introspection; not abusable for billing manipulation |
| CLI eval → local only | Privacy win: eval never transmits brief/draft to server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-204-05-01 | Info Disclosure | /runs/plan echoes brief back in response | accept | Brief is user-supplied; echoing is expected for confirmation UX. No secret material in briefs by convention |
| T-204-05-02 | Tampering | Plan endpoint used for DoS (flood with synthetic briefs) | mitigate | Inherits Phase 201 tenant-level rate-limit on /api/tenant/*; read-only so no persistent load |
| T-204-05-03 | EoP | member role uses /runs/plan to discover allowed tenants | accept | Plan endpoint is member-level read; no tenant discovery beyond their own membership (Bearer resolves only their tenant) |
| T-204-05-04 | Info Disclosure | eval runs LLM with brief locally — no leak concern | accept | Explicit CONTEXT D-03 design choice ("fully local; pluggable LLM") |
| T-204-05-05 | Tampering | eval-runner rubric trivially bypassed (copy brand name into draft) | accept | eval is heuristic advisory, not authoritative. CONTEXT D-09 notes LLM eval deferred per 203-10 precedent |
</threat_model>

<verification>
- Task 1: `node --test test/cli/init.test.js` → &gt;= 6 green
- Task 2: `node --test test/cli/plan.test.js test/cli/runs-plan-endpoint.test.js` → &gt;= 9 green
- Task 3: `node --test test/cli/eval.test.js` → &gt;= 9 green
- Phase: `npm test` green; no regression on Plans 01-04 or prior phases
- Validation: /runs/plan is strictly no-write (grep-asserted)
</verification>

<success_criteria>
- markos init delegates cleanly to bin/install.cjs (no duplication)
- /api/tenant/runs/plan is no-write dry-run returning 3-step plan + cost estimate
- markos plan renders plan envelope in TTY + JSON
- markos eval runs 4-dimension local rubric + pluggable LLM + --threshold
- 24+ new tests green
</success_criteria>

<output>
After completion, create `.planning/phases/204-cli-markos-v1-ga/204-05-SUMMARY.md`.
</output>
