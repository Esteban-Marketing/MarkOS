---
phase: 204
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - api/cli/oauth/device/start.js
  - api/cli/oauth/device/token.js
  - api/cli/oauth/device/authorize.js
  - lib/markos/cli/device-flow.cjs
  - lib/markos/cli/device-flow.ts
  - bin/commands/login.cjs
  - contracts/F-101-cli-oauth-device-v1.yaml
  - contracts/openapi.json
  - contracts/openapi.yaml
  - test/cli/login.test.js
  - test/cli/device-flow.test.js
  - test/cli/oauth-endpoints.test.js
autonomous: true
task_count: 3
requirements: [CLI-01, QA-01, QA-02, QA-09, QA-11, QA-15]

locked_assumption:
  A1: "Production CLI OAuth origin = https://app.markos.com (matches PROJECT.md homepage + existing 200-02 precedent). MARKOS_OAUTH_DEVICE_ENDPOINT env var overrides for dev/stage per RESEARCH §Runtime State Inventory"
  A2: "Device-flow server endpoints ship in THIS phase (not deferred to 205). Plan 02 implements all 3 endpoints + CLI login path. Any downstream plan blocking on them can proceed after Wave 1 merges"
  A14: "user_code format = `^[ABCDEFGHJKLMNPQRSTUVWXYZ23456789]{4}-[ABCDEFGHJKLMNPQRSTUVWXYZ23456789]{4}$` — 32-alphabet × 8 chars = 40 bits entropy (exceeds RFC 8628 §6.1 recommended 20 bits); device_code = `djNhcl8` prefix + base64url(randomBytes(16))"

must_haves:
  truths:
    - "lib/markos/cli/device-flow.cjs::createDeviceSession({ client, client_id, scope }) generates device_code (128-bit entropy) + user_code (8-char AAAA-BBBB from 32-alphabet) + expires_at = now + 900s + INSERT into markos_cli_device_sessions with status='pending'"
    - "lib/markos/cli/device-flow.cjs::pollToken({ client, device_code, client_id }) reads session; returns { error: 'authorization_pending' } when status=pending, { error: 'slow_down' } when now-last_poll_at < interval, { error: 'expired_token' } when now > expires_at OR status='expired', { error: 'access_denied' } when status='denied', { access_token, tenant_id, key_fingerprint } when status='approved' (AND issues API key via 204-03 primitive OR inline mintKey helper this plan owns)"
    - "lib/markos/cli/device-flow.cjs::approveDeviceSession({ client, user_code, tenant_id, user_id }) flips status='pending' → 'approved' within TX; raises 'already_approved' on re-approve; raises 'expired' when past expires_at"
    - "POST /api/cli/oauth/device/start — public (no auth); validates client_id='markos-cli' + scope='cli'; calls createDeviceSession; returns { device_code, user_code, verification_uri: 'https://app.markos.com/cli/authorize', verification_uri_complete: uri + '?user_code=' + user_code, expires_in: 900, interval: 5 }; rate-limit Upstash slidingWindow(10, '1m') per source IP (T-204-02-02 anti-DoS)"
    - "POST /api/cli/oauth/device/token — public; validates grant_type='urn:ietf:params:oauth:grant-type:device_code' + device_code + client_id; calls pollToken; returns 400 {error} for non-approved states, 200 {access_token, token_type:'bearer', tenant_id, key_fingerprint, scope:'cli'} on success; increments poll_count + last_poll_at on every call; revokes session after 3 slow_down violations"
    - "POST /api/cli/oauth/device/authorize — authenticated (Phase 201 session); accepts body={user_code, tenant_id}; verifies user has membership in tenant_id; calls approveDeviceSession; writes audit row source_domain='cli' action='device.approved'; returns 200 {approved:true}"
    - "bin/commands/login.cjs implements `main({ cli })`: (a) if cli.token non-empty, writes cli.token to keychain via setToken(resolveProfile(cli), cli.token) + prints 'Logged in with pasted token' + exits 0; (b) otherwise runs device flow: POST /start, openBrowser(verification_uri_complete) unless cli.noBrowser, prints user_code, polls /token every `interval` seconds honoring slow_down + expired_token + access_denied, on success writes access_token to keychain + exits 0; handles Ctrl+C cleanly"
    - "contracts/F-101-cli-oauth-device-v1.yaml declares 3 paths with request+response schemas + 5 error envelopes (authorization_pending, slow_down, expired_token, access_denied, invalid_client) + block-form tags: (not inline) to avoid Phase 203 tags-missing regression"
    - "openapi.json + openapi.yaml regenerated via `node scripts/openapi/build-openapi.cjs` — F-101's 3 paths merged, total flow count increases by 1 (64→65)"
    - "Server emits audit row on every approveDeviceSession (source_domain='cli', action='device.approved', actor_id=user_id)"
  artifacts:
    - path: "lib/markos/cli/device-flow.cjs"
      provides: "createDeviceSession + pollToken + approveDeviceSession + state-machine primitives"
    - path: "api/cli/oauth/device/start.js"
      provides: "POST /api/cli/oauth/device/start — public device code request endpoint"
    - path: "api/cli/oauth/device/token.js"
      provides: "POST /api/cli/oauth/device/token — CLI polling endpoint"
    - path: "api/cli/oauth/device/authorize.js"
      provides: "POST /api/cli/oauth/device/authorize — authenticated approval endpoint (used by 201 web UI; does not include the UI page — that's a separate micro-plan if needed post-204)"
    - path: "bin/commands/login.cjs"
      provides: "markos login device-flow + --token fallback"
    - path: "contracts/F-101-cli-oauth-device-v1.yaml"
      provides: "OAuth device flow F-NN contract"
  key_links:
    - from: "bin/commands/login.cjs"
      to: "bin/lib/cli/open-browser.cjs openBrowser"
      via: "require + invoke unless cli.noBrowser || !isTTY"
      pattern: "openBrowser"
    - from: "bin/commands/login.cjs"
      to: "bin/lib/cli/keychain.cjs setToken"
      via: "require + await setToken(resolveProfile(cli), access_token)"
      pattern: "setToken"
    - from: "api/cli/oauth/device/token.js"
      to: "lib/markos/cli/device-flow.cjs pollToken"
      via: "delegate after request parsing"
      pattern: "pollToken"
---

<objective>
Ship the OAuth device flow end-to-end: 3 server endpoints (start/token/authorize) + the CLI `markos login` command + F-101 contract + comprehensive tests. On completion a user can run `markos login`, browse to the verification URI on another device, approve the session, and have their new API key written to the OS keychain — OR run `markos login --token=<pasted-key>` as a non-interactive CI fallback.

Purpose: Every subsequent Wave 1-3 command depends on a valid token in the keychain. This plan unblocks keys/whoami/run/env/status/doctor/plan/eval/init.
Output: 3 tenant-facing endpoints + device-flow primitive library + login command + F-101 contract + openapi regen + 3 test files.
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/204-cli-markos-v1-ga/204-CONTEXT.md
@.planning/phases/204-cli-markos-v1-ga/204-RESEARCH.md
@.planning/phases/204-cli-markos-v1-ga/204-VALIDATION.md
@.planning/phases/204-cli-markos-v1-ga/204-01-PLAN.md
@.planning/phases/203-webhook-subscription-engine-ga/203-05-SUMMARY.md

<interfaces>
<!-- Plan 01 primitives available — executor imports directly: -->

From `bin/lib/cli/keychain.cjs` (Plan 01):
```javascript
const { getToken, setToken, deleteToken } = require('./lib/cli/keychain.cjs');
await setToken(profile, accessToken);  // writes to keytar or XDG fallback
```

From `bin/lib/cli/config.cjs` (Plan 01):
```javascript
const { resolveProfile } = require('./lib/cli/config.cjs');
const profile = resolveProfile(cli);  // --profile > MARKOS_PROFILE > config > 'default'
```

From `bin/lib/cli/open-browser.cjs` (Plan 01):
```javascript
const { openBrowser } = require('./lib/cli/open-browser.cjs');
openBrowser(verification_uri_complete);  // spawns or prints
```

From `bin/lib/cli/output.cjs` + `errors.cjs` (Plan 01):
```javascript
const { EXIT_CODES } = require('./lib/cli/output.cjs');
const { formatError } = require('./lib/cli/errors.cjs');
formatError({ error: 'expired_token', message: 'Login expired. Run `markos login` again.' });
process.exit(EXIT_CODES.AUTH_FAILURE);
```

From `bin/lib/cli/http.cjs` (Plan 01):
```javascript
const { BASE_URL, generateTraceId } = require('./lib/cli/http.cjs');
// BASE_URL = process.env.MARKOS_API_BASE_URL || 'https://app.markos.com'
```

From Plan 01 migration 73 (`markos_cli_device_sessions`):
```sql
-- Columns: device_code PK, user_code UNIQUE, tenant_id, user_id, status, issued_at, expires_at, approved_at, poll_count, last_poll_at
-- Partial index on user_code WHERE status='pending'
```

From Plan 01 migration 74 (`markos_cli_api_keys`):
```sql
-- id ('cak_<hex>') PK, tenant_id FK, user_id FK, key_hash UNIQUE, key_fingerprint, scope, created_at, last_used_at, revoked_at
-- RLS policy tenant_id = claim('tenant_id')
```

Server handler pattern (copy from `api/tenant/webhooks/subscriptions/[sub_id]/rotate.js`):
```javascript
'use strict';
const { writeJson } = require('../../../../../lib/markos/crm/api.cjs');  // adjust ../ per depth

async function handler(req, res, deps = {}) {
  if (req.method !== 'POST') return writeJson(res, 405, { error: 'method_not_allowed' });
  // parse body (JSON OR x-www-form-urlencoded for /token per RFC 8628)
  // validate required fields
  // delegate to lib/markos/cli/device-flow.cjs
  // map typed errors to HTTP codes
}
module.exports = handler;
module.exports.handler = handler;
```

Fixtures available from Plan 01:
```javascript
// test/cli/_fixtures/oauth-device-server.cjs
const { startStubOAuthServer } = require('./_fixtures/oauth-device-server.cjs');
const { port, close } = await startStubOAuthServer({ scenario: 'happy' });
// scenarios: 'happy' (pending x3 → success), 'expired', 'denied', 'slow-down'
```

Audit emit for device approval:
```javascript
const { enqueueAuditStaging } = require('../../../../lib/markos/audit/writer.cjs');
await enqueueAuditStaging(client, {
  tenant_id,
  source_domain: 'cli',           // NEW in Plan 01
  action: 'device.approved',
  actor_id: user_id,
  actor_role: 'owner',           // resolved from membership
  payload: { device_code_prefix: device_code.slice(0, 8), user_code }
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: device-flow library + state machine + RED unit tests</name>
  <files>
    lib/markos/cli/device-flow.cjs,
    lib/markos/cli/device-flow.ts,
    test/cli/device-flow.test.js
  </files>
  <read_first>
    - 204-RESEARCH.md §Pattern 3 (OAuth 2.0 Device Authorization Grant RFC 8628 step-by-step wire protocol)
    - lib/markos/webhooks/rotation.cjs (CJS library structure + Supabase client injection pattern)
    - lib/markos/webhooks/rotation.ts (TS twin-export convention — keep signatures identical)
    - Plan 01 migration 73 (markos_cli_device_sessions schema; columns needed by device-flow primitives)
    - RESEARCH §Common Pitfalls 2 (expired_token race — check expires_at locally + server-side)
    - RESEARCH §Common Pitfalls 6 (device code phishing — mitigated by approval UI IP display; scope: library verifies only, UI is out-of-scope for this plan)
  </read_first>
  <behavior>
    - createDeviceSession: generates device_code `djNhcl8` + base64url(crypto.randomBytes(16)); user_code `XXXX-YYYY` with 32-alphabet `ABCDEFGHJKLMNPQRSTUVWXYZ23456789` (no I/O/0/1); expires_at = new Date(now + 900_000); INSERT into markos_cli_device_sessions; returns { device_code, user_code, verification_uri: 'https://app.markos.com/cli/authorize', verification_uri_complete: uri + `?user_code=${user_code}`, expires_in: 900, interval: 5 }
    - pollToken: SELECT session by device_code; if !session return { error: 'invalid_grant' }; if now > expires_at OR status='expired' return { error: 'expired_token' }; if status='denied' return { error: 'access_denied' }; if status='pending': UPDATE poll_count+1, last_poll_at=now, if now - last_poll_at < interval_sec*1000 return { error: 'slow_down' } ELSE return { error: 'authorization_pending' }; if poll_count > 3 AND last slow_down violated → UPDATE status='expired' + return expired_token; if status='approved': mint new API key (32-byte hex) + INSERT into markos_cli_api_keys (key_hash=sha256(key), key_fingerprint=sha256(key).slice(0,8)); one-shot UPDATE status='approved'→'consumed' to prevent replay; return { access_token, token_type:'bearer', tenant_id, key_fingerprint, scope:'cli' }
    - approveDeviceSession: SELECT by user_code + status='pending'; raises 'user_code_not_found' if no row; raises 'already_approved' if status != 'pending'; raises 'expired' if now > expires_at; UPDATE status='approved', approved_at=now, tenant_id, user_id; emits audit row (source_domain='cli', action='device.approved')
    - GRACE constants: DEVICE_CODE_TTL_SEC=900, DEFAULT_INTERVAL_SEC=5, MAX_POLL_COUNT_BEFORE_REVOKE=180 (= 900/5)
    - Test df-01: createDeviceSession inserts row with correct schema + returns well-formed codes (user_code matches regex, device_code >= 22 chars)
    - Test df-02: pollToken returns authorization_pending when status='pending'
    - Test df-03: pollToken returns slow_down when now - last_poll_at < interval*1000
    - Test df-04: pollToken returns expired_token when now > expires_at
    - Test df-05: pollToken returns access_denied when status='denied'
    - Test df-06: pollToken returns { access_token, tenant_id, key_fingerprint } on status='approved'; mints API key (inserts row into markos_cli_api_keys with key_hash and 8-char fingerprint)
    - Test df-07: pollToken flips approved→consumed one-shot (second call returns invalid_grant — prevents replay T-204-02-08)
    - Test df-08: approveDeviceSession happy path — flips pending→approved and emits audit row
    - Test df-09: approveDeviceSession raises already_approved when status != 'pending'
    - Test df-10: approveDeviceSession raises expired when now > expires_at
  </behavior>
  <action>
    Implement lib/markos/cli/device-flow.cjs with the 3 exports + GRACE constants + internal helper `mintApiKey(client, tenant_id, user_id, name)` that returns { access_token, key_fingerprint }. Twin export as .ts with identical signatures (TypeScript interfaces). Use `crypto.randomBytes` + base64url encoding for device_code; hand-rolled generator for user_code (4+4 chars with dash). All state transitions guard against race conditions via conditional UPDATE `... WHERE device_code = $1 AND status = 'approved'` (returning-on-match). Audit emit is best-effort (swallow throw to avoid blocking approval — mirrors 203-03 pattern). Tests use supabase-stub mock (copy shape from test/webhooks/rotation.test.js lines with `createStubClient()`).
  </action>
  <validation>
    <automated>node --test test/cli/device-flow.test.js</automated>
    <nyquist>VALIDATION.md row: CLI-01 `markos login` state machine unit tests</nyquist>
  </validation>
  <acceptance_criteria>
    - `ls lib/markos/cli/device-flow.cjs lib/markos/cli/device-flow.ts test/cli/device-flow.test.js` all present
    - `grep -c "createDeviceSession\|pollToken\|approveDeviceSession" lib/markos/cli/device-flow.cjs` &gt;= 3
    - `grep -c "DEVICE_CODE_TTL_SEC = 900" lib/markos/cli/device-flow.cjs` == 1
    - `grep -c "'ABCDEFGHJKLMNPQRSTUVWXYZ23456789'" lib/markos/cli/device-flow.cjs` == 1
    - `grep -c "authorization_pending\|slow_down\|expired_token\|access_denied" lib/markos/cli/device-flow.cjs` &gt;= 4
    - `grep -c "source_domain: 'cli'" lib/markos/cli/device-flow.cjs` &gt;= 1
    - `grep -c "key_fingerprint" lib/markos/cli/device-flow.cjs` &gt;= 2
    - `node --test test/cli/device-flow.test.js` exits 0 with &gt;= 10 assertions
  </acceptance_criteria>
  <done>device-flow library exports 3 state-machine primitives; 10 tests green; mintApiKey helper wires into markos_cli_api_keys migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 3 OAuth device endpoints + F-101 contract + openapi regen</name>
  <files>
    api/cli/oauth/device/start.js,
    api/cli/oauth/device/token.js,
    api/cli/oauth/device/authorize.js,
    contracts/F-101-cli-oauth-device-v1.yaml,
    contracts/openapi.json,
    contracts/openapi.yaml,
    test/cli/oauth-endpoints.test.js
  </files>
  <read_first>
    - api/tenant/webhooks/subscriptions/[sub_id]/rotate.js (handler pattern: method gate → header auth → rate-limit → delegate → typed-error mapping); adapt for PUBLIC endpoint (no x-markos-user-id required for /start + /token)
    - api/tenant/webhooks/subscriptions/[sub_id]/update.js (body parsing helper `readJson(req)` at top — copy shape)
    - contracts/F-97-webhook-rotation-v1.yaml (F-NN contract structure: flow_id + version + phase + domain + paths + components + error envelopes)
    - contracts/F-96-webhook-dashboard-v1.yaml lines 1-30 (block-form tags: syntax — MUST use to avoid 35 tags-missing regression documented in deferred-items.md)
    - scripts/openapi/build-openapi.cjs (regen command; invoked as `node scripts/openapi/build-openapi.cjs`)
    - lib/markos/mcp/rate-limit.cjs (Upstash slidingWindow pattern for per-IP rate-limit on public endpoints — no tenant_id available for /start + /token)
  </read_first>
  <behavior>
    - POST /api/cli/oauth/device/start: accepts JSON body `{ client_id, scope }`; validates client_id === 'markos-cli' (returns 400 `invalid_client` else) + scope === 'cli'; rate-limits by source IP (slidingWindow(10, '1m'); 429 on trip); delegates to createDeviceSession; returns 200 full device-code envelope
    - POST /api/cli/oauth/device/token: accepts application/x-www-form-urlencoded body `grant_type=urn:ietf:params:oauth:grant-type:device_code&device_code=...&client_id=...`; validates grant_type (400 invalid_grant else); delegates to pollToken; maps typed errors (authorization_pending/slow_down/expired_token/access_denied → 400 {error}; invalid_grant → 400; success → 200 with full token envelope)
    - POST /api/cli/oauth/device/authorize: authenticated (requires x-markos-user-id + x-markos-tenant-id headers — Phase 201 session); accepts JSON body `{ user_code, tenant_id }`; verifies session.tenant_id matches body.tenant_id (403 cross_tenant_forbidden else); verifies user has membership in tenant_id (role IN owner/admin; 403 insufficient_role else); delegates to approveDeviceSession; maps already_approved→409, expired→410, user_code_not_found→404; 200 {approved:true} on success
    - F-101 contract declares all 3 paths + request/response schemas + 6 error envelopes (invalid_client, invalid_grant, authorization_pending, slow_down, expired_token, access_denied) + 2 additional approve-specific errors (already_approved, user_code_not_found); uses block-form `tags:\n  - "cli"\n  - "oauth"`
    - openapi regen increases flow count by 1 (total becomes 65) + paths by 3 (total becomes 100)
    - Test oauth-01: /start missing client_id → 400 invalid_client
    - Test oauth-02: /start happy → 200 with device_code matching `/^djNhcl8/` and user_code matching `/^[A-Z2-9]{4}-[A-Z2-9]{4}$/`
    - Test oauth-03: /start rate-limit trip → 429
    - Test oauth-04: /token wrong grant_type → 400 invalid_grant
    - Test oauth-05: /token authorization_pending → 400 with error body
    - Test oauth-06: /token success (status=approved) → 200 with access_token
    - Test oauth-07: /authorize missing auth → 401
    - Test oauth-08: /authorize cross-tenant → 403 cross_tenant_forbidden
    - Test oauth-09: /authorize happy → 200 + audit row written
    - Test oauth-10: F-101 YAML shape — loadable by JS-YAML + has 3 paths + 6 error codes
  </behavior>
  <action>
    Implement 3 handler files mirroring api/tenant/webhooks/subscriptions/[sub_id]/rotate.js pattern (module.exports = handler; module.exports.handler = handler). For /start + /token: NO tenant/user header auth gate (public endpoints per RFC 8628). For /authorize: standard header auth + membership role check (SELECT from markos_tenant_memberships WHERE user_id + tenant_id + role IN ('owner','admin')). For Content-Type: /start + /authorize read JSON via standard readJson helper; /token parses application/x-www-form-urlencoded via `new URLSearchParams(await readRawBody(req))`. Write F-101 YAML using block-form tags. Regen openapi via `node scripts/openapi/build-openapi.cjs` (spawnSync in a separate 1-line step or run manually during execution — commit includes regenerated openapi.{json,yaml}). Write 10 unit tests stubbing supabase client + injecting rate-limiter via deps.
  </action>
  <validation>
    <automated>node --test test/cli/oauth-endpoints.test.js</automated>
    <nyquist>VALIDATION.md row: CLI-01 device-flow endpoint integration</nyquist>
  </validation>
  <acceptance_criteria>
    - `ls api/cli/oauth/device/start.js api/cli/oauth/device/token.js api/cli/oauth/device/authorize.js` all present
    - `ls contracts/F-101-cli-oauth-device-v1.yaml` present
    - `grep -c "client_id === 'markos-cli'" api/cli/oauth/device/start.js` == 1
    - `grep -c "urn:ietf:params:oauth:grant-type:device_code" api/cli/oauth/device/token.js` == 1
    - `grep -c "approveDeviceSession\|authorize" api/cli/oauth/device/authorize.js` &gt;= 2
    - `grep -c "role IN.*'owner'.*'admin'\|role.*IN.*admin\|'owner'.*'admin'" api/cli/oauth/device/authorize.js` &gt;= 1
    - `grep -c "F-101" contracts/F-101-cli-oauth-device-v1.yaml` &gt;= 1
    - `grep -cE "^tags:|^\\s{2,}- cli" contracts/F-101-cli-oauth-device-v1.yaml` &gt;= 1
    - F-101 declares 3 paths (grep `-c "/api/cli/oauth/device/"` contracts/F-101-cli-oauth-device-v1.yaml &gt;= 3)
    - `grep -c '"F-101"' contracts/openapi.json` &gt;= 1 (regen happened)
    - `node --test test/cli/oauth-endpoints.test.js` exits 0 with &gt;= 10 assertions
  </acceptance_criteria>
  <done>3 endpoints shipped + F-101 contract merged + openapi regenerated + 10 endpoint tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: markos login CLI command (device flow + --token paste) + integration tests</name>
  <files>
    bin/commands/login.cjs,
    test/cli/login.test.js
  </files>
  <read_first>
    - Plan 01 output: bin/lib/cli/keychain.cjs + config.cjs + open-browser.cjs + http.cjs + output.cjs + errors.cjs (primitives to import)
    - Plan 01 fixture: test/cli/_fixtures/oauth-device-server.cjs (stub server for integration test)
    - 204-RESEARCH.md §Code Examples (device-flow CLI polling loop — copy structure verbatim)
    - bin/generate.cjs (module export pattern `module.exports = { main }`; main signature)
    - 204-CONTEXT.md D-02 (--token=&lt;pasted-api-key&gt; non-interactive fallback)
    - 204-VALIDATION.md §Manual-Only row: browser auto-open (Task 3 covers the programmatic path; actual browser spawn is manual-only)
  </read_first>
  <behavior>
    - login.cjs main({ cli }): parses cli.profile + cli.token + cli.noBrowser + cli.timeout (default 900s); calls resolveProfile(cli) → profile
    - Branch A (--token provided): validates token matches `/^mks_ak_[a-f0-9]{32,}$/` (400 invalid_token else via formatError + exit 1); calls setToken(profile, cli.token); prints to stdout `{"success":true,"profile":"default","mode":"token-paste"}` (JSON) or `✓ Logged in with pasted token (profile: default)` (TTY); exits 0
    - Branch B (device flow, no --token): POSTs to `${BASE_URL}/api/cli/oauth/device/start` with JSON body `{client_id:'markos-cli', scope:'cli'}`; parses response; if non-2xx → formatError + exit 2; prints stdout line `\n  Go to: ${verification_uri}\n  Code:  ${user_code}\n` (human) or `{"verification_uri":...,"user_code":...}` (JSON); unless cli.noBrowser, calls openBrowser(verification_uri_complete); sets deadline = Date.now() + expires_in*1000
    - Polling loop: every `interval` seconds (init 5s; bump by +5s on slow_down) POST to /token with x-www-form-urlencoded body; on authorization_pending → continue; on slow_down → interval += 5000; on expired_token → formatError({error:'expired_token',hint:'Run markos login again.'}) + exit 3; on access_denied → exit 3; on 200 → call setToken(profile, body.access_token) + print success + exit 0
    - Ctrl+C handling: process.on('SIGINT') → print 'Login aborted.' → exit 3
    - Test login-01: --token=mks_ak_abc123... writes to keychain (spy on setToken) + exits 0
    - Test login-02: --token invalid format → exit 1 + stderr contains 'invalid_token'
    - Test login-03: device flow happy path (uses stub server scenario=happy) → writes access_token to keychain + exits 0
    - Test login-04: device flow scenario=expired → exits 3 + stderr contains 'expired_token'
    - Test login-05: device flow scenario=slow-down → interval increases by 5000ms (assert via spy on setTimeout / interval arg)
    - Test login-06: MARKOS_NO_BROWSER=1 env → openBrowser not called (spy assertion)
    - Test login-07: --profile=prod writes to profile='prod' keychain entry (spy on setToken call args)
    - Test login-08: Ctrl+C mid-poll → exit 3 (simulate via process.emit('SIGINT'))
  </behavior>
  <action>
    Implement bin/commands/login.cjs as pure async function + module.exports = { main }. Replace Plan 01's stub with this file. Use global fetch (Node 22 built-in); import keychain/config/open-browser/errors/output from bin/lib/cli/*. Guard polling loop with `Date.now() < deadline` AND preemptively exit `expired_token` on deadline breach (prevents race per Pitfall 2). Tests spawn stub OAuth server on fresh port per test (via startStubOAuthServer fixture); override BASE_URL env var to `http://localhost:${port}`; stub keytar via keychain-stub fixture + require.cache override pattern. Use node:test + assert/strict.
  </action>
  <validation>
    <automated>node --test test/cli/login.test.js</automated>
    <nyquist>VALIDATION.md row: `markos login` happy-path + slow_down + expired + --token fallback</nyquist>
  </validation>
  <acceptance_criteria>
    - `ls bin/commands/login.cjs test/cli/login.test.js` both present
    - `grep -c "not yet implemented" bin/commands/login.cjs` == 0 (stub replaced)
    - `grep -c "authorization_pending\|slow_down\|expired_token" bin/commands/login.cjs` &gt;= 3
    - `grep -c "setToken" bin/commands/login.cjs` &gt;= 1
    - `grep -c "openBrowser" bin/commands/login.cjs` &gt;= 1
    - `grep -c "SIGINT" bin/commands/login.cjs` &gt;= 1
    - `grep -c "/api/cli/oauth/device/start\|/api/cli/oauth/device/token" bin/commands/login.cjs` &gt;= 2
    - `node --test test/cli/login.test.js` exits 0 with &gt;= 8 assertions
  </acceptance_criteria>
  <done>markos login works end-to-end against stub server; --token path is CI-friendly; 8 login tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (user) → /api/cli/oauth/device/authorize | Phishing surface; authenticated session + consent required |
| CLI → /api/cli/oauth/device/start | Public endpoint; per-IP rate limit + client_id whitelist |
| CLI → /api/cli/oauth/device/token | Public endpoint; state-machine enforces one-shot approved→consumed |
| Server → markos_cli_api_keys | API key issuance; sha256 hash stored, plaintext returned once |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-204-02-01 | Spoofing | Device code phishing (Pitfall 6) | mitigate | Server-side: approve endpoint requires authenticated session + ≥1h session age + role check. Full IP/geo display on UI deferred to 206 per RESEARCH §Pitfall 6 mitigation scope |
| T-204-02-02 | DoS | Brute-force polling of /token | mitigate | Upstash slidingWindow(10, '1m') per-IP on /start; per-device-code poll_count += 1 on every /token call; revoke session after 3 slow_down violations |
| T-204-02-03 | Tampering | Replay of approved device_code to mint 2nd key | mitigate | pollToken's approved→consumed one-shot transition (conditional UPDATE WHERE status='approved') — second call returns invalid_grant. Test df-07 covers |
| T-204-02-04 | Info Disclosure | access_token logged by CLI debug output | mitigate | login.cjs prints only `access_token` first 4 + last 4 chars when --debug set; structured output NEVER echoes full token to stdout |
| T-204-02-05 | Info Disclosure | API key hash in response body | mitigate | Response envelope returns fingerprint (first 8 chars of sha256) — full hash stays in DB + never echoed |
| T-204-02-06 | Tampering | CSRF on /authorize | mitigate | /authorize requires x-markos-user-id + x-markos-tenant-id session headers — Phase 201 session middleware enforces same-origin + CSRF token at edge (QA-12 platform baseline inherited) |
| T-204-02-07 | Elevation of Privilege | Non-admin approves device for owner-scoped operations | mitigate | /authorize requires markos_tenant_memberships.role IN ('owner','admin'); Test oauth-09 covers |
| T-204-02-08 | Repudiation | Device approval without audit trail | mitigate | approveDeviceSession emits audit row source_domain='cli' action='device.approved' actor_id=user_id; Test df-08 covers |
| T-204-02-09 | Info Disclosure | expired device_code returns different error per state (timing attack) | accept | Error codes are RFC 8628-mandated. Legitimate info, not a vulnerability |
</threat_model>

<verification>
- Task 1: `node --test test/cli/device-flow.test.js` → &gt;= 10 green
- Task 2: `node --test test/cli/oauth-endpoints.test.js` → &gt;= 10 green; openapi flow count = 65
- Task 3: `node --test test/cli/login.test.js` → &gt;= 8 green
- Phase: `npm test` clean (no regression on Plan 01 migrations/primitives + Phase 203 webhook suite)
- `grep -c "'F-101'" contracts/openapi.json` &gt;= 1 (openapi regen landed)
</verification>

<success_criteria>
- 3 OAuth device endpoints live (/start public, /token public, /authorize authenticated)
- lib/markos/cli/device-flow.cjs state machine enforces pending→approved→consumed one-shot
- bin/commands/login.cjs supports device flow + --token paste + --profile + --no-browser + SIGINT
- F-101 contract in openapi (path count + 3, flow count + 1)
- 28+ new tests green across 3 suites
- Phase 203 regression: 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/204-cli-markos-v1-ga/204-02-SUMMARY.md`.
</output>
