---
phase: 204
plan: 12
type: execute
wave: 4
depends_on: [10, 11]
files_modified:
  - .github/workflows/release-cli.yml
  - docs/cli/errors.md
  - docs/cli/environment.md
  - docs/cli/commands.md
  - public/llms.txt
  - test/distribution/release-workflow.test.js
  - test/cli/errors-map.test.js
autonomous: true
task_count: 3
requirements: [CLI-01, QA-01, QA-03, QA-09, QA-15]

locked_assumption:
  A10+A11: "Tap + bucket repos (markos/homebrew-tap, markos/scoop-bucket) + 3 PAT secrets (NPM_TOKEN, HOMEBREW_TAP_TOKEN, SCOOP_BUCKET_TOKEN) provisioned before first tagged release. Workflow file ships in repo; secrets are user-action prerequisites. Doctor/manual-install path documented in installation-{homebrew,scoop}.md as fallback until the CI is wired."

must_haves:
  truths:
    - ".github/workflows/release-cli.yml triggers on push of tags matching 'v*' and runs 5 jobs in a DAG: verify (needs: nothing) → npm (needs: verify) → brew + scoop (both need: npm) → smoke (needs: npm+brew+scoop)"
    - "verify job: Node 22 + npm ci + npm test + npm run release:smoke — gates all subsequent channels"
    - "npm job: npm publish --access public with NODE_AUTH_TOKEN=secrets.NPM_TOKEN (re-uses existing sdk-publish.yml precedent shape)"
    - "brew job: mislav/bump-homebrew-formula-action@v3 with formula-name=markos + formula-path=Formula/markos.rb + homebrew-tap=markos/homebrew-tap + download-url using ${{ github.ref_name }}; COMMITTER_TOKEN=secrets.HOMEBREW_TAP_TOKEN"
    - "scoop job: checkout markos/scoop-bucket repo + node scripts/distribution/bump-scoop-manifest.cjs ${{ github.ref_name }} + git commit + git push; uses SCOOP_BUCKET_TOKEN"
    - "smoke job: matrix [ubuntu-latest, macos-latest, windows-latest] runs `npm install -g markos@${{ github.ref_name }}` + `markos --version` + grep for version in output"
    - "docs/cli/errors.md enumerates ALL public-API error codes (stable across releases per D-10): INVALID_BRIEF, INVALID_ARGS, NOT_FOUND, NETWORK_ERROR, TIMEOUT, SERVER_ERROR, UNAUTHORIZED, NO_TOKEN, TOKEN_EXPIRED, FORBIDDEN, RATE_LIMITED, QUOTA_EXCEEDED, INTERNAL, and each device-flow error code (authorization_pending/slow_down/expired_token/access_denied); each entry has description + exit_code + hint_template + example"
    - "docs/cli/environment.md documents every env var the CLI reads: MARKOS_API_KEY (token bypass), MARKOS_PROFILE (profile override), MARKOS_API_BASE_URL (dev/stage override), MARKOS_OAUTH_DEVICE_ENDPOINT (deprecated alias for base URL — legacy), MARKOS_NO_BROWSER (suppress browser open), MARKOS_ENV_ENCRYPTION_KEY (SERVER-side; mentioned for operator awareness), XDG_CONFIG_HOME (standard), NO_COLOR (no-color.org), CI/GITHUB_ACTIONS (auto-detected non-interactive mode)"
    - "docs/cli/commands.md lists all 11 commands with 1-paragraph summary + link to per-command reference (or stubbed as 'see --help'); covers flags + subcommands for each (init, generate, plan, run, eval, login, keys, whoami, env, status, doctor); exit codes footer"
    - "public/llms.txt has a Phase 204 section with 4 entries: installation-homebrew.md, installation-scoop.md, commands.md, errors.md — extends the Phase 203 section (don't overwrite)"
    - "test/cli/errors-map.test.js asserts ERROR_TO_EXIT from bin/lib/cli/errors.cjs (Plan 01) matches the codes enumerated in docs/cli/errors.md (grep the doc + compare set)"
  artifacts:
    - path: ".github/workflows/release-cli.yml"
      provides: "5-job release CI (verify → npm → brew/scoop → smoke)"
    - path: "docs/cli/errors.md"
      provides: "Stable public-API error-code reference"
    - path: "docs/cli/environment.md"
      provides: "Env-var reference"
    - path: "docs/cli/commands.md"
      provides: "11-command index"
    - path: "public/llms.txt"
      provides: "Phase 204 section for AI-assistant consumption"
  key_links:
    - from: ".github/workflows/release-cli.yml brew job"
      to: "Formula/markos.rb (Plan 10) + markos/homebrew-tap remote repo"
      via: "mislav/bump-homebrew-formula-action@v3"
      pattern: "mislav/bump-homebrew-formula-action"
    - from: ".github/workflows/release-cli.yml scoop job"
      to: "scripts/distribution/bump-scoop-manifest.cjs (Plan 11) + markos/scoop-bucket remote"
      via: "checkout + bump + push"
      pattern: "bump-scoop-manifest.cjs"
    - from: "docs/cli/errors.md"
      to: "bin/lib/cli/errors.cjs ERROR_TO_EXIT (Plan 01)"
      via: "grep-asserted parity in errors-map test"
      pattern: "ERROR_TO_EXIT"
---

<objective>
Phase 204 close-out: wire the release CI matrix + ship the documentation trio (errors / environment / commands) + update llms.txt for AI-assistant discoverability.

When this plan merges:
- `git tag v3.4.0 && git push --tags` auto-publishes to npm + Homebrew tap + Scoop bucket in one atomic CI run
- Per-channel smoke tests validate `markos --version` on ubuntu + macOS + Windows
- Users + AI assistants have stable error-code + env-var + command references
- llms.txt reflects Phase 204 surface for LLM-native discovery

Output: 1 workflow file + 3 docs + llms.txt patch + 2 test files. **Phase 204 ships.**
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/204-cli-markos-v1-ga/204-CONTEXT.md
@.planning/phases/204-cli-markos-v1-ga/204-RESEARCH.md
@.planning/phases/204-cli-markos-v1-ga/204-VALIDATION.md
@.planning/phases/204-cli-markos-v1-ga/204-01-PLAN.md
@.planning/phases/204-cli-markos-v1-ga/204-10-PLAN.md
@.planning/phases/204-cli-markos-v1-ga/204-11-PLAN.md
@.planning/phases/203-webhook-subscription-engine-ga/203-10-SUMMARY.md

<interfaces>
<!-- Release workflow template (RESEARCH §GitHub Actions release workflow): -->
```yaml
name: Release markos CLI

on:
  push:
    tags: ['v*']

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '22', cache: 'npm' }
      - run: npm ci
      - run: npm test
      - run: npm run release:smoke

  npm:
    needs: [verify]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '22', registry-url: 'https://registry.npmjs.org' }
      - run: npm ci
      - run: npm publish --access public
        env: { NODE_AUTH_TOKEN: '${{ secrets.NPM_TOKEN }}' }

  brew:
    needs: [npm]
    runs-on: ubuntu-latest
    steps:
      - uses: mislav/bump-homebrew-formula-action@v3
        with:
          formula-name: markos
          formula-path: Formula/markos.rb
          homebrew-tap: markos/homebrew-tap
          download-url: 'https://registry.npmjs.org/markos/-/markos-${{ github.ref_name }}.tgz'
        env: { COMMITTER_TOKEN: '${{ secrets.HOMEBREW_TAP_TOKEN }}' }

  scoop:
    needs: [npm]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { repository: markos/scoop-bucket, token: '${{ secrets.SCOOP_BUCKET_TOKEN }}' }
      - uses: actions/setup-node@v4
        with: { node-version: '22' }
      - name: Bump manifest
        run: node scripts/distribution/bump-scoop-manifest.cjs '${{ github.ref_name }}'
      - name: Push
        run: |
          git config user.email "bot@markos.com"
          git config user.name  "markos-bot"
          git add bucket/markos.json
          git commit -m "markos: bump to ${{ github.ref_name }}"
          git push

  smoke:
    needs: [npm, brew, scoop]
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/setup-node@v4
        with: { node-version: '22' }
      - run: npm install -g markos@${{ github.ref_name }}
      - run: markos --version
```

<!-- Existing release workflow precedent to clone from: -->
<!-- .github/workflows/sdk-publish.yml — NPM_TOKEN pattern; -->
<!-- .github/workflows/openapi-ci.yml — contract CI pattern -->

<!-- Plan 01 ERROR_TO_EXIT map (source of truth for errors.md): -->
```javascript
// bin/lib/cli/errors.cjs exports ERROR_TO_EXIT with 13 keys
// docs/cli/errors.md enumerates each with description + exit_code + hint + example
// Test errors-map.test.js asserts parity (grep + set diff)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: release-cli.yml workflow + shape test</name>
  <files>
    .github/workflows/release-cli.yml,
    test/distribution/release-workflow.test.js
  </files>
  <read_first>
    - .github/workflows/sdk-publish.yml (NPM_TOKEN pattern + npm publish --access public shape)
    - .github/workflows/openapi-ci.yml (Node 22 setup-node pattern)
    - 204-RESEARCH.md §GitHub Actions release workflow template (copy verbatim)
    - Plan 10 Formula/markos.rb (formula-path for bump action)
    - Plan 11 scripts/distribution/bump-scoop-manifest.cjs (invocation shape)
  </read_first>
  <behavior>
    - .github/workflows/release-cli.yml verbatim from interfaces above; 5 jobs with explicit `needs:` dependencies forming a DAG (verify → npm → [brew, scoop] → smoke)
    - Test rw-01: YAML parses (use js-yaml if available OR regex-based assertion — file is loadable as text + contains expected keys)
    - Test rw-02: contains `on: push: tags: ['v*']`
    - Test rw-03: contains all 5 job names (verify, npm, brew, scoop, smoke)
    - Test rw-04: smoke matrix includes ubuntu-latest + macos-latest + windows-latest
    - Test rw-05: brew job uses mislav/bump-homebrew-formula-action@v3 (or similar pattern)
    - Test rw-06: scoop job invokes bump-scoop-manifest.cjs
    - Test rw-07: verify job runs `npm test` + `npm run release:smoke`
    - Test rw-08: 3 secrets referenced (NPM_TOKEN + HOMEBREW_TAP_TOKEN + SCOOP_BUCKET_TOKEN)
  </behavior>
  <action>
    Write .github/workflows/release-cli.yml verbatim from RESEARCH template. Test via fs.readFileSync + string assertions on key tokens.
  </action>
  <validation>
    <automated>node --test test/distribution/release-workflow.test.js</automated>
    <nyquist>VALIDATION.md row: CLI-01 release-cli matrix + per-channel smoke job</nyquist>
  </validation>
  <acceptance_criteria>
    - `ls .github/workflows/release-cli.yml test/distribution/release-workflow.test.js` both present
    - `grep -c "tags: \\['v\\*'\\]\\|tags:.*v\\*" .github/workflows/release-cli.yml` &gt;= 1
    - `grep -cE "verify:|npm:|brew:|scoop:|smoke:" .github/workflows/release-cli.yml` &gt;= 5
    - `grep -c "matrix:" .github/workflows/release-cli.yml` &gt;= 1
    - `grep -c "ubuntu-latest\\|macos-latest\\|windows-latest" .github/workflows/release-cli.yml` &gt;= 3
    - `grep -c "NPM_TOKEN\\|HOMEBREW_TAP_TOKEN\\|SCOOP_BUCKET_TOKEN" .github/workflows/release-cli.yml` &gt;= 3
    - `grep -c "mislav/bump-homebrew-formula-action\\|bump-homebrew-formula" .github/workflows/release-cli.yml` &gt;= 1
    - `grep -c "bump-scoop-manifest.cjs" .github/workflows/release-cli.yml` &gt;= 1
    - `grep -c "markos --version" .github/workflows/release-cli.yml` &gt;= 1
    - `node --test test/distribution/release-workflow.test.js` exits 0 with &gt;= 8 assertions
  </acceptance_criteria>
  <done>Release workflow + shape test ship; CI wires npm + brew + scoop + smoke atomically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: docs trio — errors.md + environment.md + commands.md + errors-map test</name>
  <files>
    docs/cli/errors.md,
    docs/cli/environment.md,
    docs/cli/commands.md,
    test/cli/errors-map.test.js
  </files>
  <read_first>
    - Plan 01 bin/lib/cli/errors.cjs ERROR_TO_EXIT map (source of truth)
    - 204-CONTEXT.md D-10 (exit codes 0-5 semantics)
    - 204-RESEARCH.md §Runtime State Inventory (MARKOS_API_KEY + MARKOS_PROFILE + MARKOS_OAUTH_DEVICE_ENDPOINT + MARKOS_ENV_ENCRYPTION_KEY)
    - All 11 command modules (bin/commands/*.cjs from Plans 01-09) for commands.md summaries
    - docs/webhooks.md (Phase 203 docs style — mirror tone + structure)
  </read_first>
  <behavior>
    - errors.md: table with 13+ rows; columns (code | exit_code | description | example_hint); must include every code in ERROR_TO_EXIT + OAuth-specific codes (authorization_pending / slow_down / expired_token / access_denied / invalid_token / revoked_token)
    - environment.md: table of env vars; columns (name | purpose | default | example); 9+ entries minimum (MARKOS_API_KEY / MARKOS_PROFILE / MARKOS_API_BASE_URL / MARKOS_NO_BROWSER / MARKOS_ENV_ENCRYPTION_KEY / MARKOS_NODE_VERSION_OVERRIDE / XDG_CONFIG_HOME / NO_COLOR / CI)
    - commands.md: 11-row index; columns (command | summary | common_flags | exit_codes_used); includes `markos &lt;cmd&gt; --help` note for detailed reference
    - errors-map.test.js: grep ERROR_TO_EXIT keys from bin/lib/cli/errors.cjs + grep error codes from docs/cli/errors.md table + assert set equivalence (every key in map is documented; every code in doc is in map — no orphans either direction)
  </behavior>
  <action>
    Write 3 docs as markdown tables + prose. Keep tone developer-native + quiet (PROJECT.md brand). errors-map.test.js reads both files, extracts codes via regex, asserts Set equivalence via assert.deepStrictEqual.
  </action>
  <validation>
    <automated>node --test test/cli/errors-map.test.js</automated>
    <nyquist>VALIDATION.md row: QA-15 docs-as-code + ERROR_TO_EXIT parity contract</nyquist>
  </validation>
  <acceptance_criteria>
    - `ls docs/cli/errors.md docs/cli/environment.md docs/cli/commands.md test/cli/errors-map.test.js` all present
    - `grep -c "UNAUTHORIZED\\|NO_TOKEN\\|RATE_LIMITED\\|QUOTA_EXCEEDED\\|INTERNAL" docs/cli/errors.md` &gt;= 5
    - `grep -c "authorization_pending\\|slow_down\\|expired_token\\|access_denied" docs/cli/errors.md` &gt;= 4
    - `grep -c "MARKOS_API_KEY\\|MARKOS_PROFILE\\|NO_COLOR\\|XDG_CONFIG_HOME" docs/cli/environment.md` &gt;= 4
    - `grep -cE "markos (init|generate|plan|run|eval|login|keys|whoami|env|status|doctor)" docs/cli/commands.md` &gt;= 11
    - `grep -c "Exit codes\\|exit code\\|0.*success\\|1.*user" docs/cli/commands.md` &gt;= 2
    - `node --test test/cli/errors-map.test.js` exits 0 with &gt;= 1 assertion (set equivalence)
  </acceptance_criteria>
  <done>3 docs + errors-map parity test ship; stable public-API reference + env-var + command index complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: llms.txt Phase 204 section + final phase regression</name>
  <files>
    public/llms.txt
  </files>
  <read_first>
    - public/llms.txt existing Phase 203 section (Phase 203-10-SUMMARY mentions it was updated with 5 entries)
    - Plan 203-10 llms/phase-203-webhooks.md (parallel artifact pattern)
    - Plan 10-12 docs output (installation-homebrew.md + installation-scoop.md + errors.md + commands.md)
  </read_first>
  <behavior>
    - Append to public/llms.txt a `## Phase 204 — MarkOS CLI` section with 4 entries:
      - `- /docs/cli/installation-homebrew.md — Homebrew install on macOS/Linux`
      - `- /docs/cli/installation-scoop.md — Scoop install on Windows`
      - `- /docs/cli/commands.md — 11-command index`
      - `- /docs/cli/errors.md — Stable error-code reference`
    - DO NOT overwrite Phase 203 section; append-only
    - If llms.txt missing, create it with just the Phase 204 section (minimum viable)
  </behavior>
  <action>
    Read existing public/llms.txt if present. Append section. If missing, create minimal file with Phase 204 section.
  </action>
  <validation>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('public/llms.txt','utf8'); const entries=['installation-homebrew.md','installation-scoop.md','commands.md','errors.md']; for (const e of entries) if (!t.includes(e)) { console.error('missing:',e); process.exit(1); } console.log('OK')"</automated>
    <nyquist>VALIDATION.md row: QA-15 llms.txt Phase 204 discoverability</nyquist>
  </validation>
  <acceptance_criteria>
    - `ls public/llms.txt` present
    - `grep -c "installation-homebrew.md\\|installation-scoop.md\\|commands.md\\|errors.md" public/llms.txt` &gt;= 4
    - `grep -c "Phase 204\\|MarkOS CLI" public/llms.txt` &gt;= 1
    - inline node script exits 0 (all 4 entries present)
  </acceptance_criteria>
  <done>llms.txt Phase 204 section appended; AI-assistant discovery complete; **Phase 204 ships**.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Release CI → 3 publishing channels | 3 PATs with minimum-scope; 3 job-isolated environments |
| Release CI verify job → test suite | Full `npm test` MUST pass before any publish step |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-204-12-01 | Tampering | Release CI publishes untested build | mitigate | verify job (runs `npm test` + release:smoke) is a hard `needs:` dependency for npm job; failure aborts entire DAG |
| T-204-12-02 | Info Disclosure | Secret leakage in CI logs | mitigate | Use GitHub Actions `env:` mapping (auto-masked); never echo secret values; test rw-08 asserts secrets referenced only as `${{ secrets.* }}` |
| T-204-12-03 | EoP | Compromised PAT publishes malicious version | mitigate | Per-channel PATs with minimum-scope; COMMITTER_TOKEN for brew restricted to tap repo only; rotation deferred to Phase 206 SOC 2 |
| T-204-12-04 | Availability | brew OR scoop job flakes, user mismatch across channels | accept | Version parity re-checked at smoke stage (matrix `markos --version`); release manager manually coordinates if 1 channel fails mid-release |
| T-204-12-05 | Repudiation | Public docs drift from code | mitigate | errors-map.test.js asserts grep parity between bin/lib/cli/errors.cjs + docs/cli/errors.md — CI-blocks PR if codes diverge |
</threat_model>

<verification>
- Task 1: `node --test test/distribution/release-workflow.test.js` → &gt;= 8 green
- Task 2: `node --test test/cli/errors-map.test.js` → &gt;= 1 assertion (set equivalence parity)
- Task 3: inline llms.txt script exits 0
- Phase: `npm test` fully green
- **Phase 204 SHIPS** — full CLI GA surface + 3-channel distribution live
</verification>

<success_criteria>
- .github/workflows/release-cli.yml wires 5-job DAG (verify → npm → brew+scoop → matrix smoke)
- docs/cli/errors.md + environment.md + commands.md published under docs/cli/
- llms.txt Phase 204 section added (4 entries)
- ERROR_TO_EXIT parity test guards doc drift
- Every PAT referenced as secret (no hardcoded values)
- Phase 204 closes at 12 plans, all 11 CLI commands functional, 3 distribution channels live
</success_criteria>

<output>
After completion, create `.planning/phases/204-cli-markos-v1-ga/204-12-SUMMARY.md`.
</output>
