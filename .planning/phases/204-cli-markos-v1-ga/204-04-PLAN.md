---
phase: 204
plan: 04
type: execute
wave: 1
depends_on: [01, 03]
files_modified:
  - api/tenant/whoami.js
  - lib/markos/cli/whoami.cjs
  - lib/markos/cli/whoami.ts
  - bin/commands/whoami.cjs
  - contracts/F-105-cli-whoami-status-v1.yaml
  - contracts/openapi.json
  - contracts/openapi.yaml
  - test/cli/whoami.test.js
  - test/cli/whoami-endpoint.test.js
autonomous: true
task_count: 2
requirements: [CLI-01, QA-01, QA-02, QA-04, QA-11, QA-15]

must_haves:
  truths:
    - "lib/markos/cli/whoami.cjs::resolveWhoami({ client, key_hash }) takes an incoming Bearer-key hash, calls resolveKeyByHash (Plan 03); if null or revoked → throws 'invalid_token'; else joins markos_tenants (name) + markos_users (email) + markos_tenant_memberships (role) on (tenant_id, user_id); returns { tenant_id, tenant_name, role, email, user_id, key_fingerprint, scope, last_used_at }"
    - "GET /api/tenant/whoami — accepts Bearer auth header (Authorization: Bearer mks_ak_...) OR legacy x-markos-user-id + x-markos-tenant-id headers; computes sha256(bearer_token) → delegates to resolveWhoami; returns 200 with profile envelope OR 401 invalid_token"
    - "bin/commands/whoami.cjs reads keychain token for resolved profile; calls GET /api/tenant/whoami; renders tenant + role + email + key_fingerprint + profile (from cli.profile); TTY boxed table; non-TTY JSON; NO token value printed"
    - "whoami command exits 3 when keychain empty (fresh machine); hint 'Run markos login to get started' (CONTEXT §Specifics first-run nudge)"
    - "F-105 contract declares /api/tenant/whoami AND a placeholder /api/tenant/status path (marked `x-markos-phase: 204-08` — filled by Plan 08); this plan only ships whoami; status endpoint is Plan 08's job"
    - "openapi regen: flow count 66→67 (F-105 new); paths 103→104 (+1 whoami)"
  artifacts:
    - path: "lib/markos/cli/whoami.cjs"
      provides: "resolveWhoami primitive (Bearer → profile envelope)"
    - path: "api/tenant/whoami.js"
      provides: "GET /api/tenant/whoami endpoint"
    - path: "bin/commands/whoami.cjs"
      provides: "markos whoami CLI"
    - path: "contracts/F-105-cli-whoami-status-v1.yaml"
      provides: "whoami + placeholder status contract (Plan 08 completes status path)"
  key_links:
    - from: "api/tenant/whoami.js"
      to: "lib/markos/cli/api-keys.cjs resolveKeyByHash"
      via: "Bearer token → sha256 → resolveKeyByHash → membership join"
      pattern: "resolveKeyByHash\\|resolveWhoami"
    - from: "bin/commands/whoami.cjs"
      to: "bin/lib/cli/keychain.cjs getToken"
      via: "read active profile token → authedFetch"
      pattern: "getToken"
---

<objective>
Close out Wave 1 with `markos whoami` — the observability command that proves auth + tenant routing is live. Ships `resolveWhoami` library primitive that other authed endpoints consume (converts incoming Bearer token to tenant/user context), the `/api/tenant/whoami` endpoint, the CLI command, and the F-105 contract scaffold (status path stays as placeholder until Plan 08 fills it).

Purpose: End-to-end smoke test of Wave 1. After this plan: user runs `markos login` (Plan 02), `markos keys create` (Plan 03), `markos whoami` returns their tenant + role + email. Wave 2 + 3 commands can all rely on `resolveWhoami` as the canonical Bearer-to-context resolver.
Output: 1 library + 1 endpoint + CLI command + F-105 scaffold + openapi regen + 2 test files.
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/204-cli-markos-v1-ga/204-CONTEXT.md
@.planning/phases/204-cli-markos-v1-ga/204-RESEARCH.md
@.planning/phases/204-cli-markos-v1-ga/204-VALIDATION.md
@.planning/phases/204-cli-markos-v1-ga/204-01-PLAN.md
@.planning/phases/204-cli-markos-v1-ga/204-03-PLAN.md

<interfaces>
<!-- From Plan 03 api-keys.cjs: -->
```javascript
const { resolveKeyByHash } = require('./api-keys.cjs');
// Returns { key_id, tenant_id, user_id, scope, revoked_at } or null; touches last_used_at
```

<!-- From Phase 201 membership + users tables: -->
```sql
-- markos_tenant_memberships: tenant_id, user_id, role IN ('owner','admin','member','readonly')
-- markos_tenants:            id, name
-- markos_users:              id, email
```

<!-- Bearer auth parsing helper (NEW — add to this plan's api/tenant/whoami.js): -->
```javascript
function extractBearer(req) {
  const auth = req.headers['authorization'] || req.headers['Authorization'];
  if (!auth || !auth.startsWith('Bearer ')) return null;
  return auth.slice(7).trim() || null;
}
function sha256(text) {
  return require('node:crypto').createHash('sha256').update(text).digest('hex');
}
```

<!-- Endpoint pattern (handler supports BOTH Bearer AND legacy session headers): -->
```javascript
// If Bearer present: use it (new CLI flow)
// If x-markos-user-id + x-markos-tenant-id present: use them (legacy Phase 201 web session)
// Neither: 401 unauthorized
```

<!-- From Plan 01 primitives: -->
```javascript
// bin/lib/cli/http.cjs BASE_URL + authedFetch
// bin/lib/cli/output.cjs render + EXIT_CODES
// bin/lib/cli/keychain.cjs getToken
// bin/lib/cli/config.cjs resolveProfile
// bin/lib/cli/errors.cjs formatError
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: resolveWhoami library + /api/tenant/whoami endpoint + F-105 scaffold + endpoint tests</name>
  <files>
    lib/markos/cli/whoami.cjs,
    lib/markos/cli/whoami.ts,
    api/tenant/whoami.js,
    contracts/F-105-cli-whoami-status-v1.yaml,
    contracts/openapi.json,
    contracts/openapi.yaml,
    test/cli/whoami-endpoint.test.js
  </files>
  <read_first>
    - Plan 03 lib/markos/cli/api-keys.cjs (resolveKeyByHash signature + return shape)
    - api/tenant/webhooks/subscriptions/list.js (auth header pattern + writeJson)
    - 201-CONTEXT.md tenant membership model (markos_tenant_memberships columns)
    - contracts/F-96-webhook-dashboard-v1.yaml (F-NN shape with block-form tags + multiple schemas)
    - 204-RESEARCH.md §New API Surface Inventory F-105 row
    - 204-RESEARCH.md §Pattern 2 endpoint skeleton
  </read_first>
  <behavior>
    - resolveWhoami({ client, key_hash }): delegate to resolveKeyByHash(client, key_hash); if null → throws 'invalid_token'; if revoked_at !== null → throws 'revoked_token'; perform tenant join: SELECT t.id, t.name FROM markos_tenants t WHERE t.id = $tenant_id; perform user join: SELECT u.email FROM markos_users u WHERE u.id = $user_id; perform membership join: SELECT role FROM markos_tenant_memberships WHERE tenant_id=$1 AND user_id=$2; returns { tenant_id, tenant_name, role, email, user_id, key_fingerprint, scope, last_used_at }
    - GET /api/tenant/whoami: method gate (GET else 405); extractBearer(req); if Bearer present → sha256 → resolveWhoami(client, { key_hash: hash }); else if x-markos-user-id + x-markos-tenant-id headers (legacy session path) → resolve via membership join directly; neither → 401 unauthorized; on invalid_token → 401; on revoked_token → 401 hint='Run markos login again'; on resolved → 200 with envelope
    - F-105 contract: paths include `/api/tenant/whoami` (GET, 200 schema WhoamiEnvelope + 401 error) + `/api/tenant/status` stubbed with `x-markos-phase: 204-08-PLAN` marker and description 'populated by Plan 204-08' (only whoami responds in this plan); 3 error envelopes (unauthorized, invalid_token, revoked_token); block-form tags
    - Test ep-01: no auth → 401 unauthorized
    - Test ep-02: malformed Bearer header (no 'Bearer ' prefix) → 401 unauthorized
    - Test ep-03: valid Bearer → 200 with { tenant_id, tenant_name, role, email, user_id, key_fingerprint, scope, last_used_at }
    - Test ep-04: revoked key → 401 revoked_token
    - Test ep-05: legacy session headers (x-markos-user-id + x-markos-tenant-id) → 200 with envelope (Plan 201 compat)
    - Test ep-06: unknown user_id (session purged) → 401 invalid_token
    - Test ep-07: F-105 YAML shape — loadable + contains whoami GET + status placeholder stub marker
  </behavior>
  <action>
    Implement lib/markos/cli/whoami.cjs with resolveWhoami export + twin .ts. Use Promise.all for tenant + user + membership joins. Implement api/tenant/whoami.js with extractBearer helper + sha256 helper inline; support dual auth mode (Bearer OR legacy session headers); no role gate (any authenticated user can whoami themselves). Write F-105 YAML with both paths — whoami fully specified, status marked as `summary: "Populated by Plan 204-08"` + `x-markos-phase: 204-08-PLAN` extension + single placeholder 200 response referencing `$ref: '#/components/schemas/StatusEnvelope'` (schema also stubbed with TODO description); this avoids breaking openapi-build.test.js path count. Regen openapi.
  </action>
  <validation>
    <automated>node --test test/cli/whoami-endpoint.test.js</automated>
    <nyquist>VALIDATION.md row: CLI-01 whoami endpoint Bearer auth + dual mode</nyquist>
  </validation>
  <acceptance_criteria>
    - `ls lib/markos/cli/whoami.cjs api/tenant/whoami.js contracts/F-105-cli-whoami-status-v1.yaml test/cli/whoami-endpoint.test.js` all present
    - `grep -c "resolveWhoami" lib/markos/cli/whoami.cjs` &gt;= 1
    - `grep -c "resolveKeyByHash" lib/markos/cli/whoami.cjs` &gt;= 1
    - `grep -c "markos_tenant_memberships\|markos_tenants\|markos_users" lib/markos/cli/whoami.cjs` &gt;= 3
    - `grep -c "extractBearer\|'Bearer '\|Authorization" api/tenant/whoami.js` &gt;= 2
    - `grep -c "sha256" api/tenant/whoami.js` &gt;= 1
    - `grep -c "F-105" contracts/F-105-cli-whoami-status-v1.yaml` &gt;= 1
    - `grep -c "204-08" contracts/F-105-cli-whoami-status-v1.yaml` &gt;= 1 (status placeholder marker)
    - `grep -c "/api/tenant/whoami" contracts/F-105-cli-whoami-status-v1.yaml` &gt;= 1
    - `grep -c '"F-105"' contracts/openapi.json` &gt;= 1 (regen)
    - `node --test test/cli/whoami-endpoint.test.js` exits 0 with &gt;= 7 assertions
  </acceptance_criteria>
  <done>whoami library + endpoint live supporting both Bearer and legacy session auth; F-105 scaffold merged; 7 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: markos whoami CLI command + first-run nudge + integration tests</name>
  <files>
    bin/commands/whoami.cjs,
    test/cli/whoami.test.js
  </files>
  <read_first>
    - Plan 01 output: bin/lib/cli/http.cjs + keychain.cjs + config.cjs + output.cjs + errors.cjs
    - Plan 03 bin/commands/keys.cjs (subcommand-free CLI pattern matching whoami's single-verb shape)
    - 204-CONTEXT.md §Specifics (first-run nudge: "if no config exists when any non-login/doctor command runs, print one-line hint 'Run `markos login` to get started' and exit 3")
    - 204-RESEARCH.md §A13 (error code 3 includes "no keychain entry")
  </read_first>
  <behavior>
    - whoami.cjs main({ cli }): resolveProfile(cli) → profile; getToken(profile) → token; if !token → formatError({error:'no_token', message:'Not logged in.', hint:'Run `markos login` to get started.'}) + exit 3
    - authedFetch GET /api/tenant/whoami with token; on 401 invalid_token → exit 3 with hint 'Session expired. Run `markos login` again.'; on 401 revoked_token → exit 3 with hint 'This API key was revoked. Run `markos login` for a new one.'; on 5xx → exit 2; on 200 → render envelope
    - TTY render (boxed table):
      ```
      ┌─ whoami (profile: default) ─────────────────┐
      │ Tenant:      Acme Inc (ten_abc123)          │
      │ Role:        owner                          │
      │ Email:       sam@acme.com                   │
      │ User ID:     usr_xyz987                     │
      │ Key:         sha256:a1b2c3d4… (last used 3m)│
      │ Scope:       cli                            │
      └─────────────────────────────────────────────┘
      ```
    - Non-TTY (JSON): `{"profile":"default","tenant_id":"...","tenant_name":"...","role":"...","email":"...","user_id":"...","key_fingerprint":"...","scope":"cli","last_used_at":"..."}`
    - NEVER print access_token or key_hash — only key_fingerprint
    - Test wh-01: `whoami` no token → exit 3 + stderr contains 'Run `markos login`'
    - Test wh-02: `whoami` valid token → exits 0 + stdout JSON in non-TTY
    - Test wh-03: `whoami` 401 invalid_token → exit 3
    - Test wh-04: `whoami` 401 revoked_token → exit 3
    - Test wh-05: `whoami --profile=prod` uses 'prod' profile keychain entry (spy assertion)
    - Test wh-06: `whoami` TTY mode emits boxed unicode table (grep ┌ character)
    - Test wh-07: stdout NEVER contains 'mks_ak_' (never leaks token)
  </behavior>
  <action>
    Implement bin/commands/whoami.cjs mirroring bin/commands/keys.cjs CLI shape (single-verb, no subcommand). Replace Plan 01 stub. Use authedFetch + EXIT_CODES + formatError + renderTable from Plan 01. Render boxed unicode table via output.renderTable in TTY mode with 2 columns (label + value); JSON in non-TTY. Tests spawn stub server on random port returning scenario-based whoami responses (happy / 401 invalid / 401 revoked / 500); override MARKOS_API_BASE_URL; stub keychain.
  </action>
  <validation>
    <automated>node --test test/cli/whoami.test.js</automated>
    <nyquist>VALIDATION.md row: CLI-01 whoami reads keychain + renders + respects --profile</nyquist>
  </validation>
  <acceptance_criteria>
    - `ls bin/commands/whoami.cjs test/cli/whoami.test.js` both present
    - `grep -c "not yet implemented" bin/commands/whoami.cjs` == 0
    - `grep -c "no_token\|Run .markos login." bin/commands/whoami.cjs` &gt;= 1
    - `grep -c "authedFetch.*whoami\|/api/tenant/whoami" bin/commands/whoami.cjs` &gt;= 1
    - `grep -c "mks_ak_" bin/commands/whoami.cjs` == 0 (never echoes token)
    - `grep -c "key_fingerprint" bin/commands/whoami.cjs` &gt;= 1
    - `node --test test/cli/whoami.test.js` exits 0 with &gt;= 7 assertions
  </acceptance_criteria>
  <done>markos whoami CLI works end-to-end; never echoes access_token; 7 tests green; first-run nudge correct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI Bearer → /api/tenant/whoami | Token → sha256 → resolveKeyByHash → tenant/user/role resolution |
| Dual auth paths | Bearer (new CLI) OR x-markos-user-id+tenant-id (Phase 201 web sessions) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-204-04-01 | Info Disclosure | CLI logs full Bearer token on --debug | mitigate | whoami.cjs NEVER prints token to stdout/stderr; only fingerprint displayed. Test wh-07 grep-asserts no 'mks_ak_' substring |
| T-204-04-02 | Spoofing | Attacker replays stolen Bearer | mitigate | Server-side resolveKeyByHash checks revoked_at; CLI user can `markos keys revoke` via Plan 03 to invalidate stolen keys. Test ep-04 covers revoked-token path |
| T-204-04-03 | Info Disclosure | Response leaks email to cross-tenant caller | mitigate | resolveWhoami scopes query by (tenant_id, user_id) both derived from Bearer's markos_cli_api_keys row; cross-tenant never resolvable |
| T-204-04-04 | Repudiation | whoami is read-only — no audit needed | accept | No mutation; last_used_at touch via resolveKeyByHash is enough observability |
| T-204-04-05 | Info Disclosure | Timing attack on invalid vs revoked token | accept | Differentiation is useful UX; both return 401 so no auth bypass |
</threat_model>

<verification>
- Task 1: `node --test test/cli/whoami-endpoint.test.js` → &gt;= 7 green
- Task 2: `node --test test/cli/whoami.test.js` → &gt;= 7 green
- Phase: `npm test` green
- Integration: Plan 02 login + Plan 03 keys create + Plan 04 whoami end-to-end against stub servers (manual exercise)
- Wave 1 COMPLETE after this plan — Waves 2/3 unblocked
</verification>

<success_criteria>
- resolveWhoami is the single canonical Bearer-to-context resolver (Wave 2+ endpoints reuse it)
- /api/tenant/whoami supports Bearer + legacy session auth
- markos whoami respects --profile + first-run nudge + never prints token
- F-105 contract scaffold in openapi; Plan 08 completes status path
- 14+ new tests green
</success_criteria>

<output>
After completion, create `.planning/phases/204-cli-markos-v1-ga/204-04-SUMMARY.md`.
</output>
