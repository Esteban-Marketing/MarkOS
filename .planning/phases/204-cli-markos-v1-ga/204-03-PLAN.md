---
phase: 204
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - api/tenant/api-keys/list.js
  - api/tenant/api-keys/create.js
  - api/tenant/api-keys/[key_id]/revoke.js
  - lib/markos/cli/api-keys.cjs
  - lib/markos/cli/api-keys.ts
  - bin/commands/keys.cjs
  - contracts/F-102-cli-api-keys-v1.yaml
  - contracts/openapi.json
  - contracts/openapi.yaml
  - test/cli/api-keys.test.js
  - test/cli/keys.test.js
  - test/cli/api-keys-endpoints.test.js
autonomous: true
task_count: 3
requirements: [CLI-01, QA-01, QA-02, QA-04, QA-11, QA-15]

locked_assumption:
  A3: "markos_cli_api_keys migration 74 landed in Plan 01 — VERIFIED by Plan 01 Task 1 acceptance criteria"
  A8: "source_domain='cli' available via AUDIT_SOURCE_DOMAINS extension in Plan 01"

must_haves:
  truths:
    - "lib/markos/cli/api-keys.cjs::mintKey({ client, tenant_id, user_id, name?, scope?='cli' }) generates 32-byte plaintext (mks_ak_<64 hex chars>), computes sha256, inserts into markos_cli_api_keys with key_hash + key_fingerprint=sha256.slice(0,8), returns { key_id, access_token, key_fingerprint, name, created_at } — access_token is the ONLY point plaintext is returned"
    - "lib/markos/cli/api-keys.cjs::listKeys({ client, tenant_id }) returns active keys (revoked_at IS NULL) tenant-scoped; response columns: id, name, key_fingerprint, scope, created_at, last_used_at (NEVER key_hash, NEVER access_token)"
    - "lib/markos/cli/api-keys.cjs::revokeKey({ client, tenant_id, user_id, key_id }) verifies key.tenant_id == tenant_id (else throws 'cross_tenant_forbidden'), UPDATE revoked_at=now() WHERE id=$1 AND tenant_id=$2 AND revoked_at IS NULL; emits audit row source_domain='cli' action='api_key.revoked'; throws 'key_not_found' when no row affected"
    - "lib/markos/cli/api-keys.cjs::resolveKeyByHash({ client, key_hash }) used by downstream auth middleware (Phase 205 consumers) — returns { key_id, tenant_id, user_id, scope, revoked_at } or null; UPDATE last_used_at=now() on successful resolution"
    - "GET /api/tenant/api-keys — tenant-scoped list (role IN 'owner','admin' OR member with own keys); returns { keys: [...] }"
    - "POST /api/tenant/api-keys — body {name?}; requires role IN 'owner','admin' (403 insufficient_role else); delegates to mintKey; audit 'api_key.created'; returns { key_id, access_token, key_fingerprint, name, created_at } — access_token returned ONCE ONLY"
    - "POST /api/tenant/api-keys/{key_id}/revoke — body {}; requires role IN 'owner','admin'; delegates to revokeKey; audit 'api_key.revoked'; returns { revoked_at }"
    - "bin/commands/keys.cjs dispatches subcommand: markos keys list | markos keys create [--name=X] | markos keys revoke &lt;key_id&gt; [--yes] — unknown subcommand → exit 1 + stderr 'usage: markos keys list|create|revoke'"
    - "markos keys revoke &lt;key_id&gt; requires --yes flag OR interactive TTY confirm; exits 1 if neither (CONTEXT.md line 109 mandate)"
    - "markos keys create prints access_token to stdout with warning 'This is the only time the full key is shown. Store it securely.' (TTY) or returns JSON (non-TTY)"
    - "F-102 contract declares 3 paths + response schemas + 4 error envelopes (insufficient_role 403, cross_tenant_forbidden 403, key_not_found 404, invalid_name 400)"
    - "openapi regenerated; flow count becomes 66; paths becomes 103 (+ 3 from this plan)"
  artifacts:
    - path: "lib/markos/cli/api-keys.cjs"
      provides: "mintKey + listKeys + revokeKey + resolveKeyByHash — API key library"
    - path: "api/tenant/api-keys/list.js"
      provides: "GET /api/tenant/api-keys"
    - path: "api/tenant/api-keys/create.js"
      provides: "POST /api/tenant/api-keys (owner/admin only)"
    - path: "api/tenant/api-keys/[key_id]/revoke.js"
      provides: "POST /api/tenant/api-keys/{key_id}/revoke (owner/admin only)"
    - path: "bin/commands/keys.cjs"
      provides: "markos keys list|create|revoke CRUD"
    - path: "contracts/F-102-cli-api-keys-v1.yaml"
      provides: "API-keys F-NN contract"
  key_links:
    - from: "bin/commands/keys.cjs"
      to: "bin/lib/cli/http.cjs authedFetch"
      via: "require + invoke with token from keychain.getToken"
      pattern: "authedFetch\\|getToken"
    - from: "api/tenant/api-keys/create.js"
      to: "lib/markos/cli/api-keys.cjs mintKey"
      via: "delegate after role gate"
      pattern: "mintKey"
    - from: "lib/markos/cli/device-flow.cjs pollToken"
      to: "lib/markos/cli/api-keys.cjs mintKey"
      via: "share mintKey helper (imported from api-keys.cjs)"
      pattern: "require.*api-keys.cjs.*mintKey"
---

<objective>
Ship API-key CRUD end-to-end: `lib/markos/cli/api-keys.cjs` primitive library + 3 tenant endpoints + `markos keys` CLI command + F-102 contract + 3 test files.

When this plan merges, users + server middleware can:
1. Mint new API keys programmatically (used by device-flow Plan 02's pollToken success branch)
2. List active keys via `markos keys list` (scope-filtered, no hash leak)
3. Revoke keys via `markos keys revoke &lt;id&gt; --yes` (soft-delete via revoked_at column)
4. Resolve incoming Bearer tokens to tenant_id + user_id server-side (foundation for Plan 04 whoami and every future authed endpoint)

Purpose: Unblocks `whoami` (204-04) + all authed CLI commands (runs, env, status) that need token-to-tenant resolution.
Output: 1 library + 3 endpoints + keys CLI + F-102 + openapi regen + 3 test files.
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/204-cli-markos-v1-ga/204-CONTEXT.md
@.planning/phases/204-cli-markos-v1-ga/204-RESEARCH.md
@.planning/phases/204-cli-markos-v1-ga/204-VALIDATION.md
@.planning/phases/204-cli-markos-v1-ga/204-01-PLAN.md
@.planning/phases/204-cli-markos-v1-ga/204-02-PLAN.md
@.planning/phases/203-webhook-subscription-engine-ga/203-09-SUMMARY.md

<interfaces>
<!-- From Plan 01 migration 74 (`markos_cli_api_keys`): -->
```sql
-- Columns: id ('cak_<hex>') PK, tenant_id FK, user_id FK, key_hash UNIQUE, key_fingerprint,
--          scope default 'cli', created_at, last_used_at, revoked_at, name
-- Index: idx_cli_api_keys_tenant (tenant_id) WHERE revoked_at IS NULL
-- Index: idx_cli_api_keys_fingerprint (key_fingerprint)
-- RLS:   tenant_id = claim('tenant_id')
```

<!-- From Plan 02 device-flow.cjs (will IMPORT from api-keys.cjs in this plan): -->
```javascript
// Plan 02's device-flow.cjs currently has an inline mintApiKey helper.
// In this plan, REFACTOR device-flow.cjs to import { mintKey } from './api-keys.cjs'.
// This lands in Task 1's deviation handling: when api-keys.cjs mintKey arrives,
// device-flow.cjs swaps its inline helper for the shared primitive.
const { mintKey } = require('./api-keys.cjs');
```

<!-- Handler pattern (copy from api/tenant/webhooks/subscriptions/[sub_id]/update.js): -->
```javascript
'use strict';
const { writeJson } = require('../../../lib/markos/crm/api.cjs');  // 3 levels for api/tenant/api-keys/list.js
const { enqueueAuditStaging } = require('../../../lib/markos/audit/writer.cjs');
const { mintKey, listKeys, revokeKey } = require('../../../lib/markos/cli/api-keys.cjs');

async function handler(req, res, deps = {}) {
  if (req.method !== 'GET') return writeJson(res, 405, { error: 'method_not_allowed' });
  const user_id   = req.headers['x-markos-user-id'];
  const tenant_id = req.headers['x-markos-tenant-id'];
  if (!user_id || !tenant_id) return writeJson(res, 401, { error: 'unauthorized' });
  // role check via markos_tenant_memberships for mutating ops
  // delegate + audit + return
}
```

<!-- Role gate pattern (from api/tenant/webhooks/subscriptions/[sub_id]/delete.js + 203-09 summary): -->
```javascript
const { data: membership } = await supabase
  .from('markos_tenant_memberships')
  .select('role')
  .eq('tenant_id', tenant_id)
  .eq('user_id', user_id)
  .maybeSingle();
const role = membership?.role;
if (!['owner', 'admin'].includes(role)) {
  return writeJson(res, 403, { error: 'insufficient_role', required: 'owner|admin', actual: role || 'none' });
}
```

<!-- From Plan 01 bin/lib/cli/http.cjs: -->
```javascript
const { authedFetch, BASE_URL } = require('../lib/cli/http.cjs');
const res = await authedFetch('/api/tenant/api-keys', { method: 'GET' }, { token, trace_id });
```

<!-- Audit emit for key mutations: -->
```javascript
await enqueueAuditStaging(client, {
  tenant_id,
  source_domain: 'cli',
  action: 'api_key.created',  // or 'api_key.revoked'
  actor_id: user_id,
  actor_role: membership.role,
  payload: { key_fingerprint, key_id, name }  // NEVER access_token or key_hash
});
```

<!-- From Plan 01 output.cjs: -->
```javascript
const { EXIT_CODES, render } = require('../lib/cli/output.cjs');
const { formatError } = require('../lib/cli/errors.cjs');
```

<!-- From Plan 01 keychain.cjs: -->
```javascript
const { getToken } = require('../lib/cli/keychain.cjs');
const { resolveProfile } = require('../lib/cli/config.cjs');
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: api-keys library primitives (mintKey + listKeys + revokeKey + resolveKeyByHash) + device-flow refactor</name>
  <files>
    lib/markos/cli/api-keys.cjs,
    lib/markos/cli/api-keys.ts,
    lib/markos/cli/device-flow.cjs,
    test/cli/api-keys.test.js
  </files>
  <read_first>
    - Plan 01 migration 74 (exact column names + constraints)
    - Plan 02 lib/markos/cli/device-flow.cjs (INLINE mintApiKey helper — refactor to import from api-keys.cjs after this plan's primitives land)
    - lib/markos/webhooks/rotation.cjs (library structure convention: CJS + TS twin + RPC-free direct-SQL for reads)
    - 204-RESEARCH.md §Security Domain (V6 Cryptography: `crypto.randomBytes(32)` for key generation + `sha256(key).slice(0,8)` fingerprint)
    - 204-RESEARCH.md §Architecture Patterns Pattern 2 (tenant endpoint skeleton)
  </read_first>
  <behavior>
    - mintKey({ client, tenant_id, user_id, name?, scope='cli' }): generates 32-byte hex (mks_ak_ prefix + 64 hex chars); sha256 hex of plaintext → key_hash; first 8 chars of key_hash → key_fingerprint; INSERT into markos_cli_api_keys; returns { key_id (cak_ prefix), access_token: plaintext, key_fingerprint, name, created_at }
    - listKeys({ client, tenant_id }): SELECT id, name, key_fingerprint, scope, created_at, last_used_at FROM markos_cli_api_keys WHERE tenant_id=$1 AND revoked_at IS NULL ORDER BY created_at DESC; returns { keys: array }
    - revokeKey({ client, tenant_id, user_id, key_id }): SELECT tenant_id FROM markos_cli_api_keys WHERE id=$1; throws 'key_not_found' if null row; throws 'cross_tenant_forbidden' if row.tenant_id !== tenant_id; UPDATE revoked_at=now() WHERE id=$1 AND revoked_at IS NULL; emits audit (source_domain='cli', action='api_key.revoked'); returns { revoked_at }
    - resolveKeyByHash({ client, key_hash }): SELECT id, tenant_id, user_id, scope, revoked_at FROM markos_cli_api_keys WHERE key_hash=$1 LIMIT 1; if found AND !revoked_at → UPDATE last_used_at=now() (best-effort) → return row; else return null
    - Device-flow refactor: replace the inline mintApiKey helper in lib/markos/cli/device-flow.cjs with `const { mintKey } = require('./api-keys.cjs');` and invoke `const { access_token, key_fingerprint } = await mintKey({ client, tenant_id, user_id, name: 'CLI device login' });` in pollToken's approved branch
    - Test ak-01: mintKey generates mks_ak_ + 64 hex chars; inserts row with key_hash = sha256(access_token); key_fingerprint = first 8 chars of key_hash
    - Test ak-02: mintKey returns access_token exactly once (column key_hash in DB, access_token only in return value)
    - Test ak-03: listKeys filters revoked_at IS NULL + sorts by created_at DESC
    - Test ak-04: listKeys does NOT include key_hash or access_token in response shape
    - Test ak-05: revokeKey cross-tenant → throws 'cross_tenant_forbidden' + does NOT update row
    - Test ak-06: revokeKey key_not_found → throws 'key_not_found'
    - Test ak-07: revokeKey happy → UPDATE revoked_at + audit row emitted with source_domain='cli' action='api_key.revoked'
    - Test ak-08: resolveKeyByHash active key → returns row + UPDATE last_used_at
    - Test ak-09: resolveKeyByHash revoked key → returns row with revoked_at set (caller decides policy)
    - Test ak-10: device-flow.cjs now imports mintKey from ./api-keys.cjs (grep assertion on device-flow.cjs)
  </behavior>
  <action>
    Create lib/markos/cli/api-keys.cjs with the 4 exports. Twin .ts with identical signatures. Use `crypto.randomBytes(32).toString('hex')` for access_token plaintext; `crypto.createHash('sha256').update(plaintext).digest('hex')` for key_hash; slice(0,8) for fingerprint. Refactor lib/markos/cli/device-flow.cjs: remove any inline mintApiKey / randomBytes(32) in pollToken approved branch and replace with require + mintKey call. Tests use supabase-client stub (copy shape from test/cli/device-flow.test.js in Plan 02).
  </action>
  <validation>
    <automated>node --test test/cli/api-keys.test.js</automated>
    <nyquist>VALIDATION.md row: CLI-01 markos_cli_api_keys CRUD library</nyquist>
  </validation>
  <acceptance_criteria>
    - `ls lib/markos/cli/api-keys.cjs lib/markos/cli/api-keys.ts test/cli/api-keys.test.js` all present
    - `grep -c "mintKey\|listKeys\|revokeKey\|resolveKeyByHash" lib/markos/cli/api-keys.cjs` &gt;= 4
    - `grep -c "randomBytes(32)" lib/markos/cli/api-keys.cjs` &gt;= 1
    - `grep -c "sha256" lib/markos/cli/api-keys.cjs` &gt;= 1
    - `grep -c "'api_key.created'\|'api_key.revoked'" lib/markos/cli/api-keys.cjs` &gt;= 2
    - `grep -c "require.*api-keys.cjs.*mintKey\|require.*\\./api-keys" lib/markos/cli/device-flow.cjs` &gt;= 1
    - `grep -c "access_token" lib/markos/cli/api-keys.cjs` &gt;= 2 (mintKey return + interface)
    - `node --test test/cli/api-keys.test.js` exits 0 with &gt;= 10 assertions
  </acceptance_criteria>
  <done>api-keys library shipped; device-flow refactored to share mintKey; 10 unit tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 3 tenant endpoints (list/create/revoke) + F-102 contract + openapi regen</name>
  <files>
    api/tenant/api-keys/list.js,
    api/tenant/api-keys/create.js,
    api/tenant/api-keys/[key_id]/revoke.js,
    contracts/F-102-cli-api-keys-v1.yaml,
    contracts/openapi.json,
    contracts/openapi.yaml,
    test/cli/api-keys-endpoints.test.js
  </files>
  <read_first>
    - api/tenant/webhooks/subscriptions/[sub_id]/update.js lines 1-80 (membership role gate pattern — copy for create + revoke)
    - api/tenant/webhooks/subscriptions/list.js (list endpoint pattern — no role gate, just tenant scope)
    - api/tenant/webhooks/subscriptions/[sub_id]/delete.js (endpoint with audit emit — copy audit shape)
    - contracts/F-97-webhook-rotation-v1.yaml (F-NN block-form tags + error envelopes)
    - 203-09-SUMMARY.md §Threat Model (T-203-09-02 — secret columns never echoed — same pattern applies to key_hash column)
    - scripts/openapi/build-openapi.cjs (regen command)
  </read_first>
  <behavior>
    - GET /api/tenant/api-keys (list.js): header auth gate; tenant-scope via listKeys; response shape `{ keys: [...] }` — explicit SELECT list excludes key_hash (T-204-03-01 defense-in-depth even though RLS protects)
    - POST /api/tenant/api-keys (create.js): header auth + role gate (owner/admin else 403 insufficient_role); reads body {name}; validates name (optional; if set must be 1-64 chars, else 400 invalid_name); delegates to mintKey; emits audit source_domain='cli' action='api_key.created' payload={ key_id, key_fingerprint, name }; returns 201 with { key_id, access_token, key_fingerprint, name, created_at } — access_token ONLY here
    - POST /api/tenant/api-keys/{key_id}/revoke (revoke.js): header auth + role gate; parses key_id from URL; delegates to revokeKey; maps key_not_found → 404, cross_tenant_forbidden → 403; emits audit action='api_key.revoked' payload={ key_id, key_fingerprint }; returns 200 { revoked_at }
    - F-102 contract: 3 paths with full schemas + 6 error envelopes (unauthorized 401, insufficient_role 403, cross_tenant_forbidden 403, key_not_found 404, invalid_name 400, method_not_allowed 405) + block-form tags
    - openapi regen: flow count 65→66, paths 100→103
    - Test ep-01: GET /api/tenant/api-keys 401 when no tenant_id header
    - Test ep-02: GET happy returns { keys: [...] } with NO key_hash field
    - Test ep-03: POST 401 when no auth
    - Test ep-04: POST 403 insufficient_role when role='member'
    - Test ep-05: POST happy (role='owner') returns 201 + access_token + audit row emitted
    - Test ep-06: POST invalid_name (1 char or 65+ chars) → 400 invalid_name
    - Test ep-07: revoke 401 no auth; 403 insufficient_role; 403 cross_tenant_forbidden; 404 key_not_found; 200 happy + audit row
    - Test ep-08: F-102 YAML shape — 3 paths + 6 error envelopes + block-form tags
  </behavior>
  <action>
    Implement 3 handler files mirroring api/tenant/webhooks/subscriptions/* patterns. For list.js use `writeJson(res, 200, { keys })` with explicit column enumeration. For create.js + revoke.js perform role gate BEFORE delegating to library; never leak key_hash in audit payload; access_token returned ONLY from create.js success response. Write F-102 with block-form tags; flow_id = 'F-102'; version = v1. Regen openapi. Write 8+ tests stubbing supabase + membership query; use scenario-based mock (default role='owner', override to 'member' for 403 test).
  </action>
  <validation>
    <automated>node --test test/cli/api-keys-endpoints.test.js</automated>
    <nyquist>VALIDATION.md row: CLI-01 api-keys CRUD endpoint tests</nyquist>
  </validation>
  <acceptance_criteria>
    - `ls api/tenant/api-keys/list.js api/tenant/api-keys/create.js api/tenant/api-keys/[key_id]/revoke.js` all present
    - `ls contracts/F-102-cli-api-keys-v1.yaml` present
    - `grep -c "insufficient_role" api/tenant/api-keys/create.js api/tenant/api-keys/[key_id]/revoke.js` &gt;= 2
    - `grep -c "source_domain: 'cli'" api/tenant/api-keys/create.js api/tenant/api-keys/[key_id]/revoke.js` &gt;= 2
    - `grep -c "key_hash" api/tenant/api-keys/list.js` == 0 (never SELECT key_hash for list response)
    - `grep -c "access_token" api/tenant/api-keys/create.js` &gt;= 1
    - `grep -c "access_token" api/tenant/api-keys/list.js api/tenant/api-keys/[key_id]/revoke.js` == 0 (only create.js returns plaintext)
    - `grep -c "F-102" contracts/F-102-cli-api-keys-v1.yaml` &gt;= 1
    - F-102 declares 3 paths (grep `-c "/api/tenant/api-keys"` contracts/F-102-cli-api-keys-v1.yaml &gt;= 3)
    - `grep -c '"F-102"' contracts/openapi.json` &gt;= 1 (regen landed)
    - `node --test test/cli/api-keys-endpoints.test.js` exits 0 with &gt;= 8 assertions
  </acceptance_criteria>
  <done>3 endpoints shipped with role gate + audit emit + access_token-once invariant; F-102 merged; 8 tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: markos keys CLI (list | create | revoke) with --yes confirmation + integration tests</name>
  <files>
    bin/commands/keys.cjs,
    test/cli/keys.test.js
  </files>
  <read_first>
    - Plan 01 output: bin/lib/cli/http.cjs + keychain.cjs + config.cjs + output.cjs + errors.cjs
    - 204-CONTEXT.md line 109 (`markos keys revoke` requires --yes or interactive confirmation)
    - bin/generate.cjs (command module export pattern)
    - 204-RESEARCH.md Anti-patterns "Prompting during --yes / CI"
    - node:readline docs — createInterface + question() pattern (already in bin/install.cjs)
  </read_first>
  <behavior>
    - keys.cjs main({ cli }): parses subcommand from cli.positional[0] (list|create|revoke); unknown → formatError + exit 1 with hint 'usage: markos keys list|create|revoke'
    - keys list: resolveProfile(cli) → profile; getToken(profile) → token; if !token → formatError({error:'no_token', hint:'Run markos login first'}) + exit 3; authedFetch GET /api/tenant/api-keys with token; on 200 → render JSON (non-TTY) or ANSI table (TTY) with columns [key_id, name, key_fingerprint, scope, created_at, last_used_at]; on 401 → exit 3; on 403 → exit 4; on 5xx → exit 2
    - keys create [--name=X]: getToken; authedFetch POST /api/tenant/api-keys with body {name: cli.name}; on 201 → print access_token with TTY warning or JSON; on 403 → exit 4; on 400 invalid_name → exit 1 with hint; TTY output format: `\n  Key ID:      ${key_id}\n  Name:        ${name}\n  Fingerprint: ${key_fingerprint}\n  Token:       ${access_token}\n\n  ⚠ This is the only time the full token is shown. Store it securely.\n`; JSON output: the full object
    - keys revoke &lt;key_id&gt;: positional[1] = key_id; if missing → exit 1 'usage: markos keys revoke &lt;key_id&gt;'; if !cli.yes AND process.stdout.isTTY → prompt 'Revoke key ${key_id}? This cannot be undone. [y/N] '; if response !== 'y' → exit 1 'Aborted.'; if !cli.yes AND !isTTY → exit 1 'refusing to revoke without --yes flag in non-interactive mode'; authedFetch POST /api/tenant/api-keys/{key_id}/revoke; on 200 → print `✓ Key ${key_id} revoked` + exit 0; on 404 → exit 1; on 403 → exit 4
    - Test keys-01: `keys list` with valid token → fetches + renders + exits 0
    - Test keys-02: `keys list` no token → exits 3
    - Test keys-03: `keys list` 401 → exits 3
    - Test keys-04: `keys list` 403 → exits 4
    - Test keys-05: `keys create --name=prod` returns access_token in output (TTY mode)
    - Test keys-06: `keys create` 403 insufficient_role → exits 4
    - Test keys-07: `keys revoke cak_abc123 --yes` happy → calls authedFetch POST revoke + exits 0
    - Test keys-08: `keys revoke cak_abc` no --yes + non-TTY → exits 1 with 'refusing'
    - Test keys-09: `keys revoke cak_abc` with TTY + user enters 'n' → exits 1 Aborted
    - Test keys-10: `keys unknown-subcommand` → exits 1 with 'usage' hint
    - Test keys-11: `keys revoke` missing key_id arg → exits 1
  </behavior>
  <action>
    Implement bin/commands/keys.cjs with subcommand switch + 3 handler functions (doList / doCreate / doRevoke). Import authedFetch + getToken + resolveProfile + EXIT_CODES + formatError + renderTable from Plan 01 primitives. Use global fetch indirectly via authedFetch. For interactive prompt use node:readline + readline.question wrapped in Promise. Replace Plan 01's stub content. Tests spawn a stub server on random port returning scenario-specific responses; override MARKOS_API_BASE_URL env var; stub keychain via fixtures; for TTY simulation use process.stdout.isTTY setter (writable).
  </action>
  <validation>
    <automated>node --test test/cli/keys.test.js</automated>
    <nyquist>VALIDATION.md rows: CLI-01 `keys list|create|revoke` CRUD; `keys revoke --yes` mandate</nyquist>
  </validation>
  <acceptance_criteria>
    - `ls bin/commands/keys.cjs test/cli/keys.test.js` both present
    - `grep -c "not yet implemented" bin/commands/keys.cjs` == 0
    - `grep -cE "(list|create|revoke)" bin/commands/keys.cjs` &gt;= 3
    - `grep -c "access_token" bin/commands/keys.cjs` &gt;= 1 (create prints it)
    - `grep -c "cli.yes\|--yes\|cli\\.yes" bin/commands/keys.cjs` &gt;= 1
    - `grep -c "refusing to revoke\\|refuse.*non-interactive" bin/commands/keys.cjs` &gt;= 1
    - `grep -c "only time the full token is shown\\|store it securely" bin/commands/keys.cjs` &gt;= 1
    - `node --test test/cli/keys.test.js` exits 0 with &gt;= 10 assertions
  </acceptance_criteria>
  <done>markos keys supports list + create + revoke with --yes gate + interactive fallback; 10+ CLI tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI token → /api/tenant/api-keys/* | Bearer API key authenticates; tenant resolved via resolveKeyByHash (downstream consumers) |
| Tenant membership → API key CRUD | Only role IN ('owner','admin') can create or revoke |
| access_token plaintext → user | Returned ONCE from create endpoint; never queryable later |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-204-03-01 | Info Disclosure | key_hash leaked via list endpoint | mitigate | list.js + listKeys explicit column allow-list excludes key_hash; Test ep-02 + ak-04 assert absence |
| T-204-03-02 | Info Disclosure | access_token leaked via non-create endpoints | mitigate | mintKey returns plaintext; NO other library function touches it; DB column `key_hash` is one-way (sha256); Test ak-02 covers |
| T-204-03-03 | Elevation of Privilege | Member role creates/revokes keys | mitigate | create.js + revoke.js role gate (owner/admin) BEFORE library delegation; Test ep-04 + ep-07 cover |
| T-204-03-04 | Tampering | Cross-tenant key revocation | mitigate | revokeKey SELECTs + compares tenant_id → throws cross_tenant_forbidden → 403; Test ak-05 + ep-07 cover |
| T-204-03-05 | Repudiation | Key creation/revocation without audit | mitigate | create.js + revoke.js emit audit rows source_domain='cli'; payload includes key_fingerprint (NOT key_hash or plaintext); Test ep-05 + ep-07 cover audit emit |
| T-204-03-06 | DoS | Mass key creation | accept | Rate-limit inherited from Phase 201 tenant-baseline per-key; each mint is cheap; revoke is idempotent |
| T-204-03-07 | Tampering | CLI revoke-without-confirmation footgun | mitigate | keys.cjs requires --yes OR interactive confirm; non-TTY without --yes → exit 1 with 'refusing' message; Test keys-08 covers |
</threat_model>

<verification>
- Task 1: `node --test test/cli/api-keys.test.js` → &gt;= 10 green
- Task 2: `node --test test/cli/api-keys-endpoints.test.js` → &gt;= 8 green; F-102 in openapi
- Task 3: `node --test test/cli/keys.test.js` → &gt;= 10 green
- Phase: `npm test` green (no regression on Plans 01/02 or Phase 203 suite)
- `grep -c "'F-102'" contracts/openapi.json` &gt;= 1
</verification>

<success_criteria>
- api-keys library primitives (mintKey, listKeys, revokeKey, resolveKeyByHash) tested + wired into device-flow.cjs
- 3 tenant endpoints enforce role gate + audit + tenant isolation + access_token-once invariant
- markos keys CLI supports all 3 subcommands + --yes + interactive confirmation fallback
- F-102 contract in openapi (flow count + 1, paths + 3)
- 28+ new tests green across 3 suites
- key_hash NEVER echoed through any endpoint or library function
</success_criteria>

<output>
After completion, create `.planning/phases/204-cli-markos-v1-ga/204-03-SUMMARY.md`.
</output>
