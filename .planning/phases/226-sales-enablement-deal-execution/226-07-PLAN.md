---
phase: 226
plan: "07"
type: execute
wave: 5
depends_on: ["226-01", "226-02", "226-03", "226-04", "226-05", "226-06"]
files_modified:
  - app/(markos)/sales/page.tsx
  - app/(markos)/sales/layout.tsx
  - app/(markos)/sales/deals/[opportunity_id]/page.tsx
  - app/(markos)/sales/battlecards/page.tsx
  - app/(markos)/sales/battlecards/[battlecard_id]/page.tsx
  - app/(markos)/sales/proof-packs/page.tsx
  - app/(markos)/sales/proof-packs/[proof_pack_id]/page.tsx
  - app/(markos)/sales/winloss/page.tsx
  - app/(markos)/sales/deal-rooms/[deal_room_id]/page.tsx
  - components/markos/sales/sales-enablement-workspace.tsx
  - components/markos/sales/deal-cockpit.tsx
  - components/markos/sales/battlecard-editor.tsx
  - components/markos/sales/battlecard-freshness-inspector.tsx
  - components/markos/sales/proof-pack-builder.tsx
  - components/markos/sales/proof-pack-claim-selector.tsx
  - components/markos/sales/winloss-analyzer.tsx
  - components/markos/sales/winloss-form.tsx
  - components/markos/sales/deal-room-viewer.tsx
  - components/markos/sales/deal-room-artifact-list.tsx
  - components/markos/sales/deal-room-share-generator.tsx
  - components/markos/sales/deal-health-card.tsx
  - components/markos/sales/sales-store.tsx
  - components/markos/operator/approval-inbox-sales-entries.tsx
  - components/markos/operator/morning-brief-sales-section.tsx
  - lib/markos/sales/cron/freshness-audit.ts
  - lib/markos/sales/cron/handoff-sla-escalation.ts
  - lib/markos/sales/cron/winloss-coverage.ts
  - lib/markos/sales/cron/dealroom-view-spike.ts
  - lib/markos/sales/cron/deal-health-recompute.ts
  - lib/markos/sales/cron/quote-expiry.ts
  - lib/markos/sales/cron/dealroom-expiry.ts
  - lib/markos/sales/lifecycle/outbox-drain.ts
  - lib/markos/sales/tombstone/cascade-drain.ts
  - lib/markos/sales/share/key-rotation-cron.ts
  - lib/markos/sales/cron/index.ts
  - api/cron/sales/route.ts
  - vercel.json
  - supabase/migrations/161_sales_rls_hardening.sql
  - supabase/migrations/162_sales_openapi_regen.sql
  - public/llms.txt
  - test/sales/cron/freshness-audit.test.ts
  - test/sales/cron/handoff-sla-escalation.test.ts
  - test/sales/cron/winloss-coverage.test.ts
  - test/sales/cron/dealroom-view-spike.test.ts
  - test/sales/cron/deal-health-recompute.test.ts
  - test/sales/cron/quote-expiry.test.ts
  - test/sales/cron/dealroom-expiry.test.ts
  - test/sales/lifecycle/outbox-drain.test.js
  - test/sales/tombstone/cascade-drain.test.js
  - test/sales/share/key-rotation-cron.test.js
  - test/sales/rls/rls-suite-full.test.ts
  - test/sales/rls/share-link-token-policy.test.ts
  - test/migrations/order.test.ts
  - test/regression/p221-p225.regression.test.ts
  - test/regression/p100-p105-p201.regression.test.ts
  - test/playwright/sales/battlecard-editor.spec.ts
  - test/playwright/sales/deal-room-build-share.spec.ts
  - test/playwright/sales/deal-handoff.spec.ts
  - test/playwright/sales/quote-send-supersede.spec.ts
  - test/playwright/sales/winloss-required-on-transition.spec.ts
  - test/playwright/sales/morning-brief-sales-section.spec.ts
  - test/chromatic/sales/sales-enablement-workspace.stories.tsx
  - test/chromatic/sales/deal-cockpit.stories.tsx
  - test/chromatic/sales/battlecard-editor.stories.tsx
  - test/chromatic/sales/proof-pack-builder.stories.tsx
  - test/chromatic/sales/winloss-analyzer.stories.tsx
  - test/chromatic/sales/deal-room-viewer.stories.tsx
  - .planning/phases/226-sales-enablement-deal-execution/226-VALIDATION.md
autonomous: true
requirements:
  - SEN-01
  - SEN-02
  - SEN-03
  - SEN-04
  - SEN-05
  - CRM-01
  - CRM-02
  - CRM-03
  - CRM-04
  - CRM-05
  - PRC-01
  - PRC-02
  - PRC-03
  - PRC-04
  - PRC-05
  - PRC-06
  - PRC-07
  - PRC-08
  - PRC-09
  - EVD-01
  - EVD-02
  - EVD-03
  - EVD-04
  - EVD-05
  - EVD-06
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - QA-05
  - QA-06
  - QA-07
  - QA-08
  - QA-09
  - QA-10
  - QA-11
  - QA-12
  - QA-13
  - QA-14
  - QA-15
must_haves:
  truths:
    - 6 UI workspaces ship per D-46 — SalesEnablementWorkspace (battlecards + objections + proof_packs + winloss feed); DealCockpit (per-Opportunity workspace with DealBrief + open objections + deal_health_score + handoff_records + ProposalSupport + Quote + linked DealRoom); BattlecardEditor (JSON-mode + freshness inspector + claim_ref binding panel); ProofPackBuilder (artifact picker + EvidenceMap claim selector + audience_type preview + approval CTA); WinLossAnalyzer (structured form + analytics drill-down P225 attribution_touches links + reason taxonomy bar chart); DealRoomViewer (artifact list editor + share-link generator with masked token + view tracking dashboard)
    - Approval Inbox (P208) gains 6 new entry types per D-48: proposal_publish, proofpack_approve, deal_room_enable_share, quote_send, customer_dealbrief_publish, handoff_acknowledge — components/markos/operator/approval-inbox-sales-entries.tsx renders sales-domain entries with payload-aware copy
    - Morning Brief (P208) gains sales section per D-49: at-risk deals (deal_health_score < 40), pending sales approvals, stale battlecards, quotes expiring within 72h (listExpiringSoonQuotes from 226-04), DealRooms expiring within 24h (listExpiringSoonDealRooms from 226-05), open objections by deal owner — components/markos/operator/morning-brief-sales-section.tsx renders aggregate
    - Public share renderer per D-47 was shipped in 226-05; this plan only verifies operator-side companion (DealRoomViewer)
    - 7 cron handlers ship under lib/markos/sales/cron/ + api/cron/sales/route.ts dispatcher per D-50..D-53: freshness-audit (battlecards + proof_packs claim TTL recheck), handoff-sla-escalation (24h SLA escalation task), winloss-coverage (monthly closed-opportunities-without-WinLoss task), dealroom-view-spike (D-51 >2σ alert), deal-health-recompute (hourly per-active-opp + on-demand 30-min cache per Pitfall 8), quote-expiry (D-23 expireExpiredQuotes invocation), dealroom-expiry (D-32 expireExpiredDealRooms invocation)
    - vercel.json updated to register 10 cron schedules: `/api/cron/sales?kind=freshness-audit` daily, `?kind=handoff-sla-escalation` hourly, `?kind=winloss-coverage` weekly, `?kind=dealroom-view-spike` hourly, `?kind=deal-health-recompute` hourly, `?kind=quote-expiry` daily, `?kind=dealroom-expiry` hourly + 3 review-driven (D-85/D-86/D-89): `?kind=outbox-drain` every 1min, `?kind=cascade-drain` every 5min, `?kind=key-rotation-cron` daily 03:00 UTC
    - Migration 161 (RLS hardening pass; slot 161 per B6 allocation table): verifies all 15 P226 tables have RLS enabled + tenant policy attached; adds share_link_token policy enforcement (SELECT-by-token public path explicit); adds honeypot field tracking (deal_room_views.honeypot_detected boolean column nullable; default false; populated by trackDealRoomView when honeypot fires)
    - Migration 162 (OpenAPI regen + Supabase type generation + flow-registry sync; slot 162 per B6 allocation table; LAST migration in P226): regenerate openapi.json from contracts/F-163..F-180; regenerate Supabase Database types via `npm run supabase-types`; assert flow-registry.json contains 18 entries for P226 phase=226 (final integrity check)
    - test/migrations/order.test.ts asserts wave-execution migration order for P226 per B6 allocation table: Wave 1 (226-01) applies 146-155 (SOR baseline); governance migrations apply per owning slice — 156=quote-immutability (Plan 04 Wave 3, D-83), 157=winloss-trigger (Plan 05 Wave 3, D-84), 158=lifecycle-outbox (Plan 03 Wave 2, D-85+D-89), 159=signing-keys (Plan 05 Wave 3, D-86), 160=share-audit (Plan 05 Wave 3, D-86); Wave 5 (226-07) applies 161 (RLS hardening) AFTER all of 146-160, then 162 (OpenAPI regen) LAST (carries P224 Blocker 5 + P225 Wave 0 pattern verbatim)
    - 6 Playwright operator journey specs per D-46 + verification per V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX: battlecard editor → approve → freshness inspector; deal-room-build → enable-share → stakeholder view tracking; deal handoff → acknowledge within SLA; quote send → pricing drift alert + supersede flow; WinLossRecord required gate on stage_transition; morning-brief sales section renders all 6 signals
    - 6 Chromatic stories per D-46 cover SalesEnablementWorkspace + DealCockpit + BattlecardEditor + ProofPackBuilder + WinLossAnalyzer + DealRoomViewer (each × 4 variants: empty + populated + at-risk/stale + retired/closed; total 24 visual snapshots)
    - RLS hardening test (test/sales/rls/rls-suite-full.test.ts) iterates ALL 15 P226 tables AND ALL P221-P225 tables AND ALL P100-P105 + P201 tables — single comprehensive cross-tenant denial proof; share-link-token-policy.test.ts asserts public-share path has no RLS bypass beyond HMAC verification
    - Tombstone propagation cascade test verifies P221 D-24 → P222 D-32 → P224 → P225 D-45 → P226 D-56 chain end-to-end (tombstone profile A → cascade scrubs P226 winloss_records.champion_id, objection_records.raised_by_committee_member_id, deal_room_views.stakeholder_email)
    - public/llms.txt updated with Phase 226 section: 18 contracts (F-163..F-180), 17 migrations (146-162), 8 MCP tools, 6 UI workspaces, 10 cron handlers, A1-A24 fallback handlers documented for AI assistant context
    - components/markos/sales/sales-store.tsx ships React store (Zustand pattern carry from P102 useExecutionStore + P103) with all sales domain state + selectors + mutators wired to /v1/sales/* via fetch
    - 226-VALIDATION.md rows 226-07-01 (Playwright 6 specs), 226-07-02 (Chromatic 6 stories × 4 variants), 226-07-03 (RLS suite full), 226-07-04 (legacy regression P100-P105 + P201 + P221-P225) populated and flipped to ✅ green
    - Legacy regression: `npm test` AND `npm test` AND `node --test test/regression/` AND `npx playwright test --grep sales` AND `npx playwright test --grep cdp` AND `npx playwright test --grep crm` AND existing P221-P225 + P100-P105 + P201 specs all pass (additive phase guarantee)
  artifacts:
    - path: app/(markos)/sales/page.tsx
      provides: SalesEnablementWorkspace landing page route; default-tab=battlecards
    - path: components/markos/sales/sales-enablement-workspace.tsx
      provides: top-level operator workspace with battlecards + objections + proof_packs + winloss tabs
    - path: components/markos/sales/deal-cockpit.tsx
      provides: per-Opportunity workspace; consumes DealBrief + open_objections + deal_health_score + handoff_records + ProposalSupport + Quote + linked DealRoom
    - path: components/markos/sales/battlecard-editor.tsx
      provides: JSON-mode editor + freshness inspector + claim_ref binding panel; carries P223 TemplateEditor pattern
    - path: components/markos/sales/proof-pack-builder.tsx
      provides: artifact picker + EvidenceMap claim selector + audience_type preview + approval CTA
    - path: components/markos/sales/winloss-analyzer.tsx
      provides: structured form (Pitfall 5 enforced via UI validation) + analytics drill-down + reason taxonomy bar chart; consumes P225 attribution_touches
    - path: components/markos/sales/deal-room-viewer.tsx
      provides: artifact list editor + share-link generator with masked token + view tracking dashboard
    - path: components/markos/sales/sales-store.tsx
      provides: React store wrapping all sales domain reads + writes; mirrors P102 useExecutionStore pattern
    - path: components/markos/operator/approval-inbox-sales-entries.tsx
      provides: 6 new approval inbox entry renderers (D-48)
    - path: components/markos/operator/morning-brief-sales-section.tsx
      provides: Morning Brief sales section (D-49) — 6 signals aggregated
    - path: lib/markos/sales/cron/freshness-audit.ts
      provides: daily cron handler iterating proof_packs + battlecards EvidenceMap claim TTL recheck (D-50)
    - path: lib/markos/sales/cron/handoff-sla-escalation.ts
      provides: hourly cron consuming listMissedSlaHandoffs from 226-03; creates escalation task (D-52)
    - path: lib/markos/sales/cron/winloss-coverage.ts
      provides: weekly cron checking closed Opportunities without WinLossRecord (D-53)
    - path: lib/markos/sales/cron/dealroom-view-spike.ts
      provides: hourly cron checking computeViewSpike for active deal rooms (D-51)
    - path: lib/markos/sales/cron/deal-health-recompute.ts
      provides: hourly cron computing deal_health_signals for active opportunities (Pitfall 8)
    - path: lib/markos/sales/cron/quote-expiry.ts
      provides: daily cron invoking expireExpiredQuotes from 226-04 (D-23)
    - path: lib/markos/sales/cron/dealroom-expiry.ts
      provides: hourly cron invoking expireExpiredDealRooms from 226-05 (D-32)
    - path: api/cron/sales/route.ts
      provides: Vercel cron dispatcher; routes ?kind=<handler> to one of 7 handlers; auth via CRON_SECRET header
    - path: vercel.json
      provides: 10 cron schedule entries registered (7 sales-domain + 3 review-driven: outbox-drain, cascade-drain, key-rotation-cron)
    - path: lib/markos/sales/lifecycle/outbox-drain.ts
      provides: D-85 outbox drain worker; polls sales_lifecycle_outbox WHERE status pending AND next_attempt_at <= now() ORDER BY created_at LIMIT 50; locks row, invokes handler matching trigger_kind, on success sets status completed; on failure exponential backoff (2^attempts minutes) up to max_attempts then dead_letter. Idempotent on re-process via outbox_id. Cron schedule: every 1 minute.
      exports: [drainSalesLifecycleOutbox, OUTBOX_HANDLERS]
    - path: lib/markos/sales/tombstone/cascade-drain.ts
      provides: D-89 tombstone cascade drain worker; processes sales_lifecycle_outbox rows WHERE trigger_kind tombstone_cascade. Each cascade row points to (target_table, target_pk, scrub_columns[]); worker UPDATEs target row to scrub columns to NULL. Idempotent (re-running scrub is no-op). Cron schedule: every 5 minutes.
      exports: [drainTombstoneCascade, CASCADE_TARGETS]
    - path: lib/markos/sales/share/key-rotation-cron.ts
      provides: D-86 HMAC key rotation cron handler; daily check — if newest active key in deal_room_share_signing_keys is older than 90 days, flips current active to retiring, inserts new row with key_version=max+1 + status active (encrypted via vault), schedules 24h-later transition to retire. Operators can mint new tokens for active dealrooms during grace window. Cron schedule: daily at 03:00 UTC.
      exports: [rotateShareSigningKey, retireGracedKey, KEY_ROTATION_INTERVAL_DAYS]
    - path: supabase/migrations/161_sales_rls_hardening.sql
      provides: RLS hardening pass + share_link_token policy + honeypot_detected column on deal_room_views (migration slot 161 per B6 allocation table; D-84/D-85/D-86 slots 157-160 reserved by Plans 03/04/05)
    - path: supabase/migrations/162_sales_openapi_regen.sql
      provides: OpenAPI regen marker + flow-registry final integrity assertion (no DDL; comment-only migration enforces wave-execution order; LAST migration in P226 per slot allocation table)
  key_links:
    - from: components/markos/sales/sales-enablement-workspace.tsx
      to: components/markos/sales/sales-store.tsx
      via: workspace renders tabs that read from useSalesStore (battlecards, objections, proof_packs, winloss); mutators dispatch to /v1/sales/* via fetch
    - from: components/markos/operator/approval-inbox-sales-entries.tsx
      to: lib/markos/crm/copilot.ts (buildApprovalPackage)
      via: 6 new entry types rendered with payload-aware copy; on user approve action, calls /v1/sales/{kind}/{id}/approve OR /v1/sales/{kind}/{id}/{send|enable-share|acknowledge}
    - from: lib/markos/sales/cron/freshness-audit.ts
      to: lib/markos/sales/proof-packs/freshness-auditor.ts (refreshClaimFreshness)
      via: cron iterates approved proof_packs + battlecards; calls refresh per row; emits operator task for each newly-stale (D-50)
    - from: api/cron/sales/route.ts
      to: lib/markos/sales/cron/index.ts
      via: dispatcher reads ?kind= query param; imports + invokes handler from cron/index barrel; CRON_SECRET header auth
    - from: components/markos/operator/morning-brief-sales-section.tsx
      to: lib/markos/sales/quotes/expiry-cron.ts (listExpiringSoonQuotes), lib/markos/sales/deal-rooms/expiry-cron.ts (listExpiringSoonDealRooms), lib/markos/sales/handoffs/sla.ts (listMissedSlaHandoffs)
      via: Morning Brief reads aggregated signals at render time; refresh interval 5min via SWR
    - from: test/migrations/order.test.ts
      to: supabase/migrations/146_*.sql..162_*.sql
      via: asserts wave-execution migration application order per B6 allocation: 146-155 in Wave 1 (226-01); 158 in Wave 2 (226-03 lifecycle-outbox); 156/157/159/160 in Wave 3 (226-04 quote-immutability + 226-05 winloss-trigger/signing-keys/share-audit); 161+162 in Wave 5 (226-07 RLS hardening + OpenAPI regen)
---

<objective>
Close Phase 226 with the full operator UI surface (6 workspaces + Approval Inbox extensions + Morning Brief sales section), 7 observability cron handlers, RLS hardening + OpenAPI regen migrations (161 + 162; slots renumbered per B6 allocation table — 157-160 reserved by Plans 03/04/05), 6 Playwright operator journeys, 6 Chromatic stories × 4 variants (24 visual snapshots), comprehensive RLS-suite-full + share-link-token-policy tests, tombstone cascade end-to-end test, legacy regression matrix (P100-P105 + P201 + P221-P225 + 226-01..06), llms.txt Phase 226 section, and the migration-order test asserting wave-execution pattern. This is THE closeout plan: ship-ready state for `/gsd-verify-work`.

Purpose: satisfy SEN-01..05 + CRM/PRC/EVD carry-forward requirements (UI exposes governed sales domain; class-based approval exposed via Approval Inbox), QA-01..15 (full 15-gate quality baseline applies — lint, typecheck, vitest, node --test, Playwright, Chromatic, OpenAPI parity, RLS suite, audit trail, tombstone, dependency scan, license scan, security scan, contract scan, regression matrix). Module boundary `lib/markos/sales/cron/*` for cron handlers; `components/markos/sales/*` for 13 UI components; `app/(markos)/sales/*` for 9 routes.

Output: 9 app routes + 13 UI components + 1 sales-store + 2 operator extensions + 10 cron handlers (7 sales + 3 review-driven D-85/D-86/D-89) + 1 cron dispatcher + vercel.json update + 2 migrations (161 hardening + 162 marker; slots renumbered per B6) + 6 Playwright specs + 6 Chromatic stories + 10 cron tests across 4 directories + 2 RLS tests + 1 migration order test + 2 regression test files + 1 llms.txt update.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/226-sales-enablement-deal-execution/226-CONTEXT.md
@.planning/phases/226-sales-enablement-deal-execution/226-RESEARCH.md
@.planning/phases/226-sales-enablement-deal-execution/226-VALIDATION.md
@.planning/phases/226-sales-enablement-deal-execution/226-01-PLAN.md
@.planning/phases/226-sales-enablement-deal-execution/226-02-PLAN.md
@.planning/phases/226-sales-enablement-deal-execution/226-03-PLAN.md
@.planning/phases/226-sales-enablement-deal-execution/226-04-PLAN.md
@.planning/phases/226-sales-enablement-deal-execution/226-05-PLAN.md
@.planning/phases/226-sales-enablement-deal-execution/226-06-PLAN.md
@.planning/phases/226-sales-enablement-deal-execution/226-01-SUMMARY.md
@.planning/phases/226-sales-enablement-deal-execution/226-02-SUMMARY.md
@.planning/phases/226-sales-enablement-deal-execution/226-03-SUMMARY.md
@.planning/phases/226-sales-enablement-deal-execution/226-04-SUMMARY.md
@.planning/phases/226-sales-enablement-deal-execution/226-05-SUMMARY.md
@.planning/phases/226-sales-enablement-deal-execution/226-06-SUMMARY.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/V4.0.0-TESTING-ENVIRONMENT-PLAN.md
@.planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md
@obsidian/reference/UI Components.md
@obsidian/reference/CRM Domain.md
@obsidian/reference/HTTP Layer.md
@CLAUDE.md
@vercel.json
@public/llms.txt
@.planning/phases/208-human-operating-interface/208-CONTEXT.md
@.planning/phases/225-analytics-attribution-narrative-intelligence/225-07-PLAN.md
@.planning/phases/224-conversion-launch-workspace/224-07-PLAN.md
@lib/markos/sales/contracts.ts
@lib/markos/sales/audit.ts
@components/markos/crm/workspace-shell.tsx
@components/markos/crm/copilot-record-panel.tsx
@components/markos/crm/execution-queue.tsx
@package.json

<!-- INFORMATIONAL ONLY (per CLAUDE.md source-of-truth precedence): -->
<!-- obsidian/work/incoming/24-SALES-ENABLEMENT-ENGINE.md INFORMATIONAL ONLY. Canonical surface for UI shape = obsidian/reference/UI Components.md + 226-CONTEXT.md D-46..D-49 + components/markos/crm/* existing patterns. -->
</context>

<interfaces>
<!-- Key contracts the executor needs. Embedded so no codebase exploration required. -->

From `obsidian/reference/UI Components.md` (canonical UI patterns):
- Operator pages live under `app/(markos)/<domain>/page.tsx`
- Workspace components in `components/markos/<domain>/<workspace>.tsx`
- React store pattern (Zustand): `components/markos/<domain>/<domain>-store.tsx` exporting `use<Domain>Store` hook
- Tri-pane layouts use grid; consent gates use amber color; suggestion-only drafting uses 'send_disabled' flag (carry from outbound-workspace.tsx)

From `components/markos/crm/workspace-shell.tsx` (existing pattern):
- Workspace shell uses view switcher (kanban/table/detail/timeline/calendar/funnel)
- Patches via /api/crm/records + /api/crm/calendar
- Sales workspace mirrors this: tabs (battlecards/objections/proof_packs/winloss) + RLS-aware fetches via /v1/sales/*

From `components/markos/crm/copilot-record-panel.tsx` + P208 Approval Inbox:
- Existing approval-package payload-aware rendering pattern; sales adds 6 new entry types
- payload field convention: `{kind: '<artifact_kind>', payload: {...artifact_specific_fields}, requested_by: user_id}`

From P208 Morning Brief (assumed module path: components/markos/operator/morning-brief.tsx):
- Section pattern: each domain contributes `<DomainSection />` rendering aggregated signals
- Sales section reads from /v1/sales/{deal-health,quotes/expiring-soon,deal-rooms/expiring-soon,handoffs/missed-sla} via SWR

From `vercel.json` cron pattern (existing):
```json
{
  "crons": [
    { "path": "/api/cron/<domain>", "schedule": "0 0 * * *" }
  ]
}
```
Sales adds 7 entries with kind query params.

From P201 CRON_SECRET pattern (existing api/cron/* routes):
- Authorization: `Bearer ${CRON_SECRET}` header required
- 401 if missing; 403 if mismatched

From `lib/markos/sales/quotes/expiry-cron.ts` (226-04 Task 1):
```typescript
export async function expireExpiredQuotes(supabase): Promise<{ expired_count: number; expired_ids: string[] }>;
export async function listExpiringSoonQuotes(supabase, window_hours?: number): Promise<QuoteRow[]>;
```

From `lib/markos/sales/deal-rooms/expiry-cron.ts` (226-05 Task 1):
```typescript
export async function expireExpiredDealRooms(supabase): Promise<{ closed_count: number; closed_ids: string[] }>;
export async function listExpiringSoonDealRooms(supabase, window_hours?: number): Promise<DealRoomRow[]>;
```

From `lib/markos/sales/handoffs/sla.ts` (226-03 Task 2):
```typescript
export const HANDOFF_SLA_HOURS = 24;
export async function listMissedSlaHandoffs(supabase, tenant_id?: string): Promise<HandoffRecordRow[]>;
```

From `lib/markos/sales/health-signals/signal-computer.ts` (226-03 Task 3):
```typescript
export async function computeDealHealthSignals(supabase, input): Promise<{ signal_id; deal_health_score; risk_factors }>;
```
</interfaces>

<tasks>


<!-- REVIEW-DRIVEN ADDITIONS (D-82 / D-85 / D-86 / D-89) -->

**REVIEW-DRIVEN REVISIONS (2026-04-25):**

Plan 07 adds 3 NEW cron handlers and clarifies test runner choice:

1. **D-85 lifecycle outbox drain cron** — `lib/markos/sales/lifecycle/outbox-drain.ts` (cron handler `/api/v1/sales/cron/outbox-drain.js`). Polls `sales_lifecycle_outbox WHERE status='pending' AND next_attempt_at <= now() ORDER BY created_at LIMIT 50`. For each row: lock (UPDATE status='processing'), invoke handler matching `trigger_kind`, on success → status='completed'. On failure → increment attempts, set next_attempt_at = now() + (2^attempts) minutes, status='pending' (retry) or 'dead_letter' (when attempts >= max_attempts). Idempotent on re-process via outbox_id. Replayable via manual `UPDATE sales_lifecycle_outbox SET status='pending' WHERE status='dead_letter'`. Cron schedule: every 1 minute via Vercel Cron.

2. **D-89 tombstone cascade drain cron** — `lib/markos/sales/tombstone/cascade-drain.ts`. Same outbox table, processes `trigger_kind='tombstone_cascade'` rows. Each cascade row points to (target_table, target_pk, scrub_columns[]); worker UPDATEs target row to scrub columns to NULL. Idempotent (re-running scrub is no-op). Cron: every 5 minutes.

3. **D-86 HMAC key rotation cron** — `lib/markos/sales/share/key-rotation-cron.ts`. Daily check: if newest active key in `deal_room_share_signing_keys` is older than 90 days, automatically (a) flips current 'active' key to 'retiring', (b) inserts new row with key_version = max+1, status='active', encrypted via vault, (c) schedules a 24-hour-later transition (separate cron) flipping 'retiring' → 'retired'. Operators can mint new tokens for active dealrooms during the grace window. Cron: daily at 03:00 UTC.

4. **D-82 test runner — npm test (Node --test)** — Plan 07 closeout regression suite uses `npm test` (Node test runner). NO vitest, NO playwright assumed. If E2E coverage of public share page is required, Task X.Y MUST add `playwright` script via `npm install --save-dev @playwright/test` + edit package.json. DEFAULT (cheapest path) is to skip E2E and rely on integration tests via Node test runner with `node:test` + supertest pattern (already used in repo).

5. **D-90 rate-limit acceptance** — Plan 07 RLS hardening pass MUST verify: (a) deal_room_share_audit RLS attached, (b) deal_room_share_signing_keys is NOT tenant-scoped (system-level table; restricted by service-role only), (c) sales_lifecycle_outbox tenant-scoped via tenant_id column, (d) `grep -c 'checkSignupRateLimit' .planning/phases/226-*-PLAN.md` returns 0 (D-90 acceptance).


<task type="auto" tdd="true">
  <name>Task 1: 6 UI workspaces + sales-store + Approval Inbox extensions + Morning Brief sales section</name>
  <read_first>
    - .planning/phases/226-sales-enablement-deal-execution/226-CONTEXT.md D-46 (6 workspaces); D-47 (public share renderer — already shipped 226-05); D-48 (Approval Inbox new entry types); D-49 (Morning Brief signals)
    - obsidian/reference/UI Components.md (canonical UI patterns: workspace shell, tabs, store pattern)
    - components/markos/crm/workspace-shell.tsx (existing tab + view switcher pattern)
    - components/markos/crm/copilot-record-panel.tsx (existing approval-package payload-aware rendering)
    - components/markos/crm/execution-queue.tsx (existing queue tab pattern; carry to sales-enablement-workspace)
    - .planning/phases/208-human-operating-interface/208-CONTEXT.md (Approval Inbox + Morning Brief shape)
    - lib/markos/sales/* libraries from 226-01..05 (UI consumes via /v1/sales/* — never imports library directly to preserve API boundary)
    - api/v1/sales/* routes from 226-06 (UI fetch endpoints)
  </read_first>
  <behavior>
    - Test 1 (Playwright morning-brief-sales-section.spec.ts):
      - Operator opens Morning Brief → sales section renders 6 signals: at-risk deals (count + top 3), pending sales approvals (count by kind: proposal/proofpack/quote/dealroom-share/customer-dealbrief/handoff), stale battlecards (count + clickthrough to BattlecardEditor), quotes expiring within 72h (count + top 3 with valid_until), DealRooms expiring within 24h (count + top 3), open objections by deal owner (count grouped by owner_user_id)
      - Each signal has clickthrough to relevant workspace
      - SWR refresh interval 5min; manual refresh button works
    - Test 2 (Chromatic 6 stories × 4 variants):
      - SalesEnablementWorkspace: empty (no battlecards/objections), populated, with-stale (1+ stale battlecard), retired (all archived)
      - DealCockpit: healthy deal (score >= 70), at-risk (score < 40), with-handoff-pending, closed-won
      - BattlecardEditor: editing-draft, approved-fresh, stale-claims-warning, retired
      - ProofPackBuilder: empty, populated-fresh, populated-stale, retired
      - WinLossAnalyzer: no records, populated (with reason taxonomy bar chart), with-attribution-drilldown, ghosted-only
      - DealRoomViewer: draft (no artifacts), live (3 artifacts + share-link masked), closed-with-views, expired-warning
  </behavior>
  <action>
    1. Create app routes + workspace components:
       - `app/(markos)/sales/layout.tsx` — wraps SalesEnablementWorkspace shell
       - `app/(markos)/sales/page.tsx` — landing; renders SalesEnablementWorkspace
       - `app/(markos)/sales/deals/[opportunity_id]/page.tsx` — DealCockpit
       - `app/(markos)/sales/battlecards/page.tsx` — Battlecard list view
       - `app/(markos)/sales/battlecards/[battlecard_id]/page.tsx` — BattlecardEditor for single card
       - `app/(markos)/sales/proof-packs/page.tsx` — ProofPack list
       - `app/(markos)/sales/proof-packs/[proof_pack_id]/page.tsx` — ProofPackBuilder for single pack
       - `app/(markos)/sales/winloss/page.tsx` — WinLossAnalyzer
       - `app/(markos)/sales/deal-rooms/[deal_room_id]/page.tsx` — DealRoomViewer (operator-side; public share lives in 226-05)

    2. Create 13 UI components:
       - `components/markos/sales/sales-enablement-workspace.tsx` — top-level operator workspace; tabs (battlecards/objections/proof_packs/winloss); reads useSalesStore
       - `components/markos/sales/deal-cockpit.tsx` — per-Opportunity multi-pane: DealBrief panel + ObjectionRecord list + DealHealthCard + HandoffRecord list + ProposalSupport panel + Quote panel + linked DealRoom card
       - `components/markos/sales/battlecard-editor.tsx` — JSON-mode editor with claim_ref binding panel (carries P223 TemplateEditor pattern); calls /v1/sales/battlecards/{id} PATCH
       - `components/markos/sales/battlecard-freshness-inspector.tsx` — sub-component showing claim_ref freshness chips (color-coded fresh/stale_claims/stale_competitor/retired)
       - `components/markos/sales/proof-pack-builder.tsx` — artifact picker + audience_type selector + EvidenceMap claim selector + approval CTA; calls /v1/sales/proof-packs/{id}/approve
       - `components/markos/sales/proof-pack-claim-selector.tsx` — sub-component listing available EvidenceMap claims by tenant
       - `components/markos/sales/winloss-analyzer.tsx` — list view + reason taxonomy bar chart; clicking bar drills into filtered list
       - `components/markos/sales/winloss-form.tsx` — Pitfall 5 enforced form: outcome (required dropdown), primary_reason (required dropdown), competitive_set (conditional required when primary_reason='competitor'), attribution_touch_ids (multi-select); validates BEFORE POST /v1/sales/winloss
       - `components/markos/sales/deal-room-viewer.tsx` — operator-side artifact list editor + share-link generator (token masked as `dr_*****`) + view tracking dashboard
       - `components/markos/sales/deal-room-artifact-list.tsx` — sub-component drag-handle reorder (display_order); polymorphic artifact rendering by kind
       - `components/markos/sales/deal-room-share-generator.tsx` — sub-component for enable-share action; shows masked token + expires_at picker
       - `components/markos/sales/deal-health-card.tsx` — composite score visualization with 4-component breakdown + risk_factors list
       - `components/markos/sales/sales-store.tsx` — Zustand store with all sales domain state + selectors + mutators; carries useExecutionStore pattern from P102/P103

    3. Create operator extensions:
       - `components/markos/operator/approval-inbox-sales-entries.tsx` — 6 entry-type renderers per D-48: ProposalPublishEntry, ProofPackApproveEntry, DealRoomEnableShareEntry, QuoteSendEntry, CustomerDealbriefPublishEntry, HandoffAcknowledgeEntry; each renders payload-aware copy + Approve/Reject actions calling appropriate /v1/sales/* endpoint
       - `components/markos/operator/morning-brief-sales-section.tsx` — sales section per D-49 aggregating 6 signals via SWR fetches to /v1/sales/* GET endpoints; clickthrough to relevant workspace

    4. Update existing P208 Approval Inbox + Morning Brief modules to register sales-domain extension (mounting points). If modules absent: components export self-mountable; documented in code comment for P208 maintainer.

    5. Create 6 Playwright specs covering D-46 + D-48 + D-49 user journeys (test/playwright/sales/*.spec.ts).

    6. Create 6 Chromatic stories × 4 variants each (test/chromatic/sales/*.stories.tsx).
  </action>
  <acceptance_criteria>
    - 9 app route files exist under `app/(markos)/sales/`
    - 13 component files exist under `components/markos/sales/`
    - 2 operator extension files exist under `components/markos/operator/`
    - `grep -E "useSalesStore" components/markos/sales/sales-enablement-workspace.tsx` returns 1+ usage
    - `grep -E "fetch\\(.*v1/sales" components/markos/sales/sales-store.tsx` returns 4+ usages (CRUD via /v1/sales/*)
    - `grep -E "ProposalPublishEntry|ProofPackApproveEntry|DealRoomEnableShareEntry|QuoteSendEntry|CustomerDealbriefPublishEntry|HandoffAcknowledgeEntry" components/markos/operator/approval-inbox-sales-entries.tsx` returns 6 entry-type renderers
    - `grep -E "at-risk deals|pending sales approvals|stale battlecards|quotes expiring|DealRooms expiring|open objections" components/markos/operator/morning-brief-sales-section.tsx` returns 6 signal references (case-insensitive comments OR labels)
    - 6 Playwright specs exist under `test/playwright/sales/`
    - 6 Chromatic stories exist under `test/chromatic/sales/`
    - `npx playwright test test/playwright/sales/morning-brief-sales-section.spec.ts test/playwright/sales/battlecard-editor.spec.ts test/playwright/sales/deal-room-build-share.spec.ts test/playwright/sales/deal-handoff.spec.ts test/playwright/sales/quote-send-supersede.spec.ts test/playwright/sales/winloss-required-on-transition.spec.ts` exits 0 (all 6 specs green)
    - `npm run chromatic -- --only-changed test/chromatic/sales/` exits 0 (24 visual snapshots; baseline established)
    - 226-VALIDATION.md row 226-07-01 (Playwright) flips to ✅ green; row 226-07-02 (Chromatic) flips to ✅ green
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --grep "sales" && node --test test/sales/ui-store/</automated>
  </verify>
  <done>6 UI workspaces + sales-store + Approval Inbox sales extensions + Morning Brief sales section shipped. 9 app routes + 13 components. 6 Playwright specs + 6 Chromatic stories × 4 variants green. 226-VALIDATION.md rows 226-07-01 + 226-07-02 green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 10 cron handlers (7 sales + 3 review-driven) + dispatcher + vercel.json schedules + migrations 161 + 162 + RLS suite full + tombstone cascade end-to-end</name>
  <read_first>
    - .planning/phases/226-sales-enablement-deal-execution/226-CONTEXT.md D-50 (freshness audit cron); D-51 (DealRoom view spike); D-52 (handoff SLA escalation); D-53 (WinLoss coverage)
    - .planning/phases/226-sales-enablement-deal-execution/226-RESEARCH.md Pitfall 8 lines 1024-1027 (hourly cron + on-demand recompute via 30-min cache)
    - .planning/phases/224-conversion-launch-workspace/224-07-PLAN.md (P224 cron pattern reference; vercel.json schedule format)
    - .planning/phases/225-analytics-attribution-narrative-intelligence/225-07-PLAN.md (P225 closeout pattern: RLS hardening migration + OpenAPI regen + flow-registry final integrity)
    - .planning/phases/221-cdp-identity-audience-consent-substrate/221-CONTEXT.md D-24 (tombstone cascade origin)
    - lib/markos/sales/quotes/expiry-cron.ts (226-04)
    - lib/markos/sales/deal-rooms/expiry-cron.ts (226-05)
    - lib/markos/sales/handoffs/sla.ts (226-03)
    - lib/markos/sales/health-signals/signal-computer.ts + threshold-evaluator.ts (226-03)
    - lib/markos/sales/proof-packs/freshness-auditor.ts (226-02)
    - lib/markos/sales/battlecards/freshness-auditor.ts (226-02)
    - lib/markos/sales/winloss/tombstone.ts (226-05)
    - vercel.json (existing crons; sales appends 7 schedules)
    - public/llms.txt (existing; sales appends Phase 226 section)
  </read_first>
  <behavior>
    - Test 1 (cron/freshness-audit.test.ts):
      - Inserts proof_packs with stale evidence_refs[]; cron iterates approved + non-stale rows; calls refreshClaimFreshness; verifies freshness_status updates to 'stale_claims' for those with stale claims
      - Inserts battlecards with stale proof_refs[]; cron iterates approved; calls recomputeBattlecardFreshness; verifies freshness_status updates
      - Each newly-stale row triggers operator task creation (via P208 task creation API; A4-style fallback to audit log if module absent)
    - Test 2 (cron/handoff-sla-escalation.test.ts):
      - Inserts handoff_records with handed_off_at = now - 25h, acknowledged_at = null
      - Cron consumes listMissedSlaHandoffs; for each missed: emits operator task ("escalation_handoff_overdue") via P208; emits audit 'handoff.escalated'
    - Test 3 (cron/winloss-coverage.test.ts):
      - Insert opportunities with stage='customer'/'lost'/'no_decision' AND no winloss_records row → cron creates "winloss_coverage_gap" operator task
      - Opportunities with WinLossRecord present → not flagged
    - Test 4 (cron/dealroom-view-spike.test.ts):
      - Insert deal_room_views simulating 7-day baseline (avg 5/day) + spike day (50 in 1h)
      - cron computes spike via computeViewSpike from 226-05; flags as spike (>2σ); emits operator task "deal_room_view_spike_alert" with deal_room_id + actual_count + baseline_avg + sigma
    - Test 5 (cron/deal-health-recompute.test.ts):
      - Cron iterates active opportunities (status IN ('mql','sql','opportunity')); calls computeDealHealthSignals for current period
      - For each at-risk score (<40), invokes evaluateDealHealthThresholds → creates 'at_risk_deal_review' P1 task
      - Pitfall 8 mitigation: 30-min cache check before recompute; skip if computed_at within last 30min
    - Test 6 (cron/quote-expiry.test.ts):
      - Insert quotes with valid_until in past, status='sent' → cron invokes expireExpiredQuotes; rows flip to status='expired'; operator task created per quote
    - Test 7 (cron/dealroom-expiry.test.ts):
      - Insert deal_rooms with share_link_expires_at in past, status='live' → cron invokes expireExpiredDealRooms; rows flip to status='closed'; ISR cache invalidated
    - Test 8 (rls/rls-suite-full.test.ts) — comprehensive RLS proof:
      - Iterate all 15 P226 tables + all P221-P225 tables + all P100-P105 + P201 markos_* tables (~50 total)
      - For each: INSERT row under tenant A; SET app.active_tenant_id=tenant B; SELECT → 0 rows (cross-tenant denial proven everywhere)
    - Test 9 (rls/share-link-token-policy.test.ts):
      - Verify deal_rooms public path bypass: when querying by share_link_token only (no tenant context), the path through getDealRoomByShareToken (226-05) requires verifyShareToken FIRST; bypassing HMAC verification + querying directly returns null/404
      - deal_room_views public INSERT policy works without tenant context (per migration 150 RLS); SELECT requires tenant join (verified in test)
    - Test 10 (test/migrations/order.test.ts):
      - Asserts wave-execution migration order per B6 allocation: 146-155 in Wave 1 (226-01); 158 in Wave 2 (226-03 lifecycle-outbox); 156 in Wave 3 (226-04 quote-immutability); 157/159/160 in Wave 3 (226-05 winloss-trigger/signing-keys/share-audit); 161+162 in Wave 5 (226-07 RLS hardening + OpenAPI regen)
      - Carries P224 Blocker 5 + P225 Wave 0 pattern verbatim
      - Numeric ordering would be 146,147...162 but execution order is wave-driven
    - Test 11 (test/regression/p221-p225.regression.test.ts) + (test/regression/p100-p105-p201.regression.test.ts):
      - Imports test files from prior phases; runs them; asserts all green (additive phase guarantee)
      - If any fails: phase 226 is non-additive somewhere — fix before ship
  </action>
  <action>
    1. Create 7 cron handler files under `lib/markos/sales/cron/`:
       
       a) `freshness-audit.ts`:
       ```typescript
       export async function freshnessAuditCron(supabase): Promise<{ proof_packs_checked: number; proof_packs_marked_stale: number; battlecards_checked: number; battlecards_marked_stale: number }> {
         // 1. SELECT proof_packs WHERE approval_state='approved' AND freshness_status='fresh'
         // 2. For each: refreshClaimFreshness; if newly-stale → create operator task
         // 3. SELECT battlecards WHERE status='approved' AND freshness_status='fresh'
         // 4. For each: recomputeBattlecardFreshness; if newly-stale → create operator task
         // 5. Return counts
       }
       ```
       
       b) `handoff-sla-escalation.ts`:
       ```typescript
       export async function handoffSlaEscalationCron(supabase): Promise<{ escalated_count: number; handoff_ids: string[] }> {
         const missed = await listMissedSlaHandoffs(supabase);
         for (const h of missed) {
           // Create P208 escalation task; emit audit 'handoff.escalated'
         }
         return { escalated_count: missed.length, handoff_ids: missed.map(h => h.handoff_id) };
       }
       ```
       
       c) `winloss-coverage.ts`:
       ```typescript
       export async function winlossCoverageCron(supabase): Promise<{ uncovered_count: number; opportunity_ids: string[] }> {
         // SELECT opportunities WHERE stage IN ('customer','lost','no_decision') AND created_at > now - 30days
         // LEFT JOIN winloss_records WHERE winloss_records.record_id IS NULL
         // For each: create P208 'winloss_coverage_gap' task
       }
       ```
       
       d) `dealroom-view-spike.ts`:
       ```typescript
       export async function dealroomViewSpikeCron(supabase): Promise<{ spike_count: number; deal_room_ids: string[] }> {
         // SELECT distinct deal_room_id FROM deal_rooms WHERE status='live'
         // For each: computeViewSpike(deal_room_id, window_minutes=60, baseline_days=7)
         // If is_spike: create P208 task 'deal_room_view_spike_alert' with deal_room_id + metrics
       }
       ```
       
       e) `deal-health-recompute.ts`:
       ```typescript
       export async function dealHealthRecomputeCron(supabase): Promise<{ computed_count: number; at_risk_count: number }> {
         // SELECT opportunities WHERE stage IN ('mql','sql','opportunity') AND created_at > now - 90days
         // For each: check last computeDealHealthSignals (cache 30min — Pitfall 8); if stale, computeDealHealthSignals
         // For each at-risk (score < 40): evaluateDealHealthThresholds → create P208 task
       }
       ```
       
       f) `quote-expiry.ts`:
       ```typescript
       import { expireExpiredQuotes } from '../quotes/expiry-cron.js';
       export async function quoteExpiryCron(supabase): Promise<{ expired_count: number; expired_ids: string[] }> {
         return await expireExpiredQuotes(supabase);
       }
       ```
       
       g) `dealroom-expiry.ts`:
       ```typescript
       import { expireExpiredDealRooms } from '../deal-rooms/expiry-cron.js';
       export async function dealroomExpiryCron(supabase): Promise<{ closed_count: number; closed_ids: string[] }> {
         return await expireExpiredDealRooms(supabase);
       }
       ```

    2. Create `lib/markos/sales/cron/index.ts` barrel: exports all 7 cron handlers + dispatcher.

    3. Create `api/cron/sales/route.ts`:
       ```typescript
       import { NextRequest, NextResponse } from 'next/server';
       import { freshnessAuditCron, handoffSlaEscalationCron, winlossCoverageCron, dealroomViewSpikeCron, dealHealthRecomputeCron, quoteExpiryCron, dealroomExpiryCron } from '@/lib/markos/sales/cron';
       import { drainSalesLifecycleOutbox } from '@/lib/markos/sales/lifecycle/outbox-drain';
       import { drainTombstoneCascade } from '@/lib/markos/sales/tombstone/cascade-drain';
       import { rotateShareSigningKey } from '@/lib/markos/sales/share/key-rotation-cron';
       
       const HANDLERS = {
         // 7 sales-domain crons
         'freshness-audit': freshnessAuditCron,
         'handoff-sla-escalation': handoffSlaEscalationCron,
         'winloss-coverage': winlossCoverageCron,
         'dealroom-view-spike': dealroomViewSpikeCron,
         'deal-health-recompute': dealHealthRecomputeCron,
         'quote-expiry': quoteExpiryCron,
         'dealroom-expiry': dealroomExpiryCron,
         // 3 review-driven crons (D-85, D-89, D-86)
         'outbox-drain': drainSalesLifecycleOutbox,
         'cascade-drain': drainTombstoneCascade,
         'key-rotation-cron': rotateShareSigningKey,
       };
       
       export async function GET(req: NextRequest) {
         const auth = req.headers.get('authorization');
         if (auth !== `Bearer ${process.env.CRON_SECRET}`) return NextResponse.json({ error: 'unauthorized' }, { status: 401 });
         const url = new URL(req.url);
         const kind = url.searchParams.get('kind');
         const handler = HANDLERS[kind ?? ''];
         if (!handler) return NextResponse.json({ error: 'invalid_kind', valid_kinds: Object.keys(HANDLERS) }, { status: 400 });
         const supabase = await getSupabaseAdminClient();  // service role for cron paths (no tenant context)
         try {
           const result = await handler(supabase);
           return NextResponse.json({ ok: true, kind, result });
         } catch (err) {
           return NextResponse.json({ ok: false, kind, error: String(err) }, { status: 500 });
         }
       }
       ```

    4. Update `vercel.json` to add 7 cron schedules:
       ```json
       {
         "crons": [
           ...existing,
           { "path": "/api/cron/sales?kind=freshness-audit", "schedule": "0 4 * * *" },
           { "path": "/api/cron/sales?kind=handoff-sla-escalation", "schedule": "15 * * * *" },
           { "path": "/api/cron/sales?kind=winloss-coverage", "schedule": "0 6 * * 1" },
           { "path": "/api/cron/sales?kind=dealroom-view-spike", "schedule": "30 * * * *" },
           { "path": "/api/cron/sales?kind=deal-health-recompute", "schedule": "45 * * * *" },
           { "path": "/api/cron/sales?kind=quote-expiry", "schedule": "0 3 * * *" },
           { "path": "/api/cron/sales?kind=dealroom-expiry", "schedule": "0 * * * *" },
           { "path": "/api/cron/sales?kind=outbox-drain", "schedule": "*/1 * * * *" },
           { "path": "/api/cron/sales?kind=cascade-drain", "schedule": "*/5 * * * *" },
           { "path": "/api/cron/sales?kind=key-rotation-cron", "schedule": "0 0 * * *" }
         ]
       }
       ```

    5. Create `supabase/migrations/161_sales_rls_hardening.sql` (slot 161 per B6 allocation; D-84/D-85/D-86 slots 157-160 reserved by Plans 03/04/05):
       ```sql
       -- Phase 226 — RLS hardening pass (D-54..D-58); migration slot 161 per B6 allocation table.
       -- Applied AFTER all 226-01..06 land; carries P224 Blocker 5 + P225 Wave 0 wave-execution pattern.
       SET search_path TO public;
       
       -- Verify RLS enabled on all 15 P226 tables (idempotent ALTER ... ENABLE)
       ALTER TABLE battlecards ENABLE ROW LEVEL SECURITY;
       ALTER TABLE battlecard_versions ENABLE ROW LEVEL SECURITY;
       ALTER TABLE objection_libraries ENABLE ROW LEVEL SECURITY;
       ALTER TABLE objection_library_entries ENABLE ROW LEVEL SECURITY;
       ALTER TABLE objection_records ENABLE ROW LEVEL SECURITY;
       ALTER TABLE proof_packs ENABLE ROW LEVEL SECURITY;
       ALTER TABLE proof_pack_versions ENABLE ROW LEVEL SECURITY;
       ALTER TABLE deal_briefs ENABLE ROW LEVEL SECURITY;
       ALTER TABLE deal_rooms ENABLE ROW LEVEL SECURITY;
       ALTER TABLE deal_room_artifacts ENABLE ROW LEVEL SECURITY;
       ALTER TABLE deal_room_views ENABLE ROW LEVEL SECURITY;
       ALTER TABLE proposal_supports ENABLE ROW LEVEL SECURITY;
       ALTER TABLE quotes ENABLE ROW LEVEL SECURITY;
       ALTER TABLE winloss_records ENABLE ROW LEVEL SECURITY;
       ALTER TABLE handoff_records ENABLE ROW LEVEL SECURITY;
       ALTER TABLE deal_health_signals ENABLE ROW LEVEL SECURITY;
       
       -- Add honeypot detection field to deal_room_views (carry P224 honeypot pattern)
       ALTER TABLE deal_room_views ADD COLUMN IF NOT EXISTS honeypot_detected boolean NOT NULL DEFAULT false;
       
       -- Hot-path index for honeypot analytics
       CREATE INDEX IF NOT EXISTS idx_deal_room_views_honeypot
         ON deal_room_views(deal_room_id, viewed_at DESC) WHERE honeypot_detected = true;
       ```

    6. Create `supabase/migrations/162_sales_openapi_regen.sql` (slot 162 per B6 allocation; LAST migration in P226):
       ```sql
       -- Phase 226 — OpenAPI regen + flow-registry final integrity assertion; migration slot 162 per B6 allocation table.
       -- This migration is comment-only (no DDL); it serves as a marker that 226-07 ran the OpenAPI + Supabase types regeneration scripts.
       -- Also asserts wave-execution migration order: this is the LAST migration in P226 (162).
       SET search_path TO public;
       
       -- No DDL; marker only.
       -- Operator runs the following commands AFTER this migration applies:
       --   1. npm run openapi-generate   (regenerates openapi.json from contracts/F-163..F-180)
       --   2. npm run supabase-types     (regenerates Database type definitions)
       --   3. node scripts/contracts/verify-flow-registry-integrity.js --phase 226 --expected-count 18
       --
       -- These commands are NOT idempotent across deployments; they must be invoked manually OR via CI.
       
       COMMENT ON TABLE battlecards IS 'P226 SOR — Battlecard with EvidenceMap claim TTL freshness inheritance (D-17)';
       COMMENT ON TABLE proof_packs IS 'P226 SOR — ProofPack with snapshot-at-first-approval + render-time fail-closed (D-11..D-13)';
       COMMENT ON TABLE quotes IS 'P226 SOR — Quote-as-Snapshot immutability + supersedence (D-21..D-24)';
       COMMENT ON TABLE deal_rooms IS 'P226 SOR — DealRoom with HMAC share_link_token + BotID + rate-limit + honeypot (D-29..D-33, D-54)';
       COMMENT ON TABLE winloss_records IS 'P226 SOR — Structured win/loss taxonomy with attribution_touch_ids[] + tombstone preserve audit (D-08, D-40..D-42, D-56)';
       ```

    7. Create test files:
       - 7 cron handler tests under `test/sales/cron/`
       - `test/sales/rls/rls-suite-full.test.ts` — comprehensive 50-table cross-tenant denial proof
       - `test/sales/rls/share-link-token-policy.test.ts` — public path RLS bypass verification
       - `test/migrations/order.test.ts` — wave-execution order assertion
       - `test/regression/p221-p225.regression.test.ts` — re-runs P221-P225 vitest test files; assert all green
       - `test/regression/p100-p105-p201.regression.test.ts` — re-runs node --test files for P100-P105 + P201

    8. Update `public/llms.txt` to add Phase 226 section:
       ```
       ## Phase 226: Sales Enablement and Deal Execution (v4.2.0)
       
       Status: Ready for /gsd-verify-work as of 2026-04-XX.
       
       Contracts: F-163..F-180 (18 total) — see contracts/F-163-sales-battlecard-v1.yaml through F-180-sales-deal-room-share-v1.yaml
       Migrations: 146..162 (17 total) — supabase/migrations/146_sales_battlecards.sql through 162_sales_openapi_regen.sql. Slot allocation: 146-155 SOR (Plan 01); 156 quote-immutability (Plan 04 D-83); 157 winloss-trigger (Plan 05 D-84); 158 lifecycle-outbox (Plan 03 D-85+D-89); 159 signing-keys + 160 share-audit (Plan 05 D-86); 161 RLS hardening + 162 OpenAPI regen (Plan 07).
       
       Library boundary: lib/markos/sales/{battlecards,objections,proof-packs,deal-briefs,deal-rooms,proposals,quotes,winloss,handoffs,health-signals,approvals,cron}
       
       MCP tools (8): get_battlecard, assemble_proof_pack, generate_deal_brief, create_deal_room, generate_quote, record_winloss, list_open_objections, get_handoff_brief
       
       UI workspaces (6): SalesEnablementWorkspace, DealCockpit, BattlecardEditor, ProofPackBuilder, WinLossAnalyzer, DealRoomViewer (all under app/(markos)/sales/*)
       
       Public route: /share/dr/{token} — HMAC + BotID + rate-limit + honeypot + 410-on-expiry
       
       Cron handlers (10): freshness-audit, handoff-sla-escalation, winloss-coverage, dealroom-view-spike, deal-health-recompute, quote-expiry, dealroom-expiry, outbox-drain (D-85; every 1min), cascade-drain (D-89; every 5min), key-rotation-cron (D-86; daily 03:00 UTC)
       
       A1-A24 fallback handlers documented for upstream module absence:
         A2 (P202 MCP) — local registration; A3 (P221 cdp_events) — markos_audit_log; A4 (P222 lifecycle) — manual API trigger;
         A5 (P207 AgentRun) — synchronous gen; A13 (P205 PricingRecommendation) — placeholder JSONB;
         A14 (P201 BotID) — degraded rate-limit 3/min; A15 (Edge Config) — in-memory rolling window;
         A16 (Next.js cacheTag) — no-op; A21 (P209 EvidenceMap) — fresh + warning;
         A22 (P225 narrative) — template-only; A23 (P225 attribution_touches) — UUID[] without FK;
         A24 (per-tenant signing key) — shared key with tenant_id embed.
       ```
  </action>
  <acceptance_criteria>
    - 10 cron handler files exist (7 under `lib/markos/sales/cron/` + 3 under `lib/markos/sales/{lifecycle,tombstone,share}/`); dispatcher HANDLERS map registers all 10 (cron/index barrel exports the 7 sales-domain handlers; review-driven 3 imported directly into dispatcher)
    - File exists: `api/cron/sales/route.ts`; uses `Bearer ${CRON_SECRET}` auth; dispatches to 10 handlers via `?kind=` query param
    - `vercel.json` has 10 sales cron entries; verify with `jq '.crons | map(select(.path | startswith("/api/cron/sales"))) | length' vercel.json` returns 10
    - Files exist: `supabase/migrations/161_sales_rls_hardening.sql`, `162_sales_openapi_regen.sql` (slots renumbered per B6 allocation)
    - `grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/161_sales_rls_hardening.sql` returns 16 (15 P226 tables; 16 because battlecard_versions counts)
    - `grep -E "honeypot_detected boolean" supabase/migrations/161_*.sql` returns the column add line
    - 10 cron tests across 4 directories (`test/sales/cron/` 7 + `test/sales/lifecycle/` 1 + `test/sales/tombstone/` 1 + `test/sales/share/` 1) all green: `node --test test/sales/cron/ test/sales/lifecycle/ test/sales/tombstone/ test/sales/share/` exits 0
    - 2 RLS tests under `test/sales/rls/` green: `node --test test/sales/rls/rls-suite-full.test.ts test/sales/rls/share-link-token-policy.test.ts` exits 0
    - File exists: `test/migrations/order.test.ts`; `node --test test/migrations/order.test.ts` exits 0
    - 2 regression test files under `test/regression/`:
      - `node --test test/regression/p221-p225.regression.test.ts` exits 0
      - `npm test test/regression/p100-p105-p201.regression.test.ts` exits 0 (node --test for legacy)
    - `public/llms.txt` contains "Phase 226" + "F-163..F-180" + "146..162" + "A1-A24 fallback handlers"
    - `npm test && npm test && npx playwright test --grep sales && npm run chromatic -- test/chromatic/sales/` ALL exit 0 (full P226 quality baseline gates green)
    - 226-VALIDATION.md row 226-07-03 (RLS suite full) flips to ✅ green; row 226-07-04 (legacy regression) flips to ✅ green
    - 226-VALIDATION.md frontmatter `status: ready` flag set; `wave_0_complete: true` (already set in 226-01); `nyquist_compliant: true` (already set in 226-01)
  </acceptance_criteria>
  <verify>
    <automated>node --test test/sales/cron/ test/sales/lifecycle/ test/sales/tombstone/ test/sales/share/ test/sales/rls/ test/migrations/order.test.ts test/regression/ && npm test && npx playwright test --grep sales</automated>
  </verify>
  <done>10 cron handlers (7 sales + 3 review-driven D-85/D-86/D-89) + dispatcher + vercel.json schedules + migrations 161/162 + RLS suite full + tombstone cascade end-to-end + legacy regression matrix + llms.txt update shipped. Phase 226 closeout complete; ready for /gsd-verify-work. 226-VALIDATION.md rows 226-07-03 + 226-07-04 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel cron → /api/cron/sales | Authenticated via CRON_SECRET header (P201 pattern); 401 if missing/mismatched |
| operator → 6 UI workspaces | requireSalesTenantContext via /v1/sales/* API layer (226-06); RBAC stub permissive in Wave 4 → tightened here for sales-domain roles |
| Approval Inbox → /v1/sales/{kind}/{id}/approve | Existing P208 pattern; sales adds 6 new entry types |
| Migration 161 RLS hardening | Idempotent ALTER ... ENABLE; safe to re-apply |
| Tombstone cascade test | Service role; verifies P221 D-24 → P226 D-56 chain end-to-end |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-226-07-01 | Spoofing | Cron endpoint | mitigate | CRON_SECRET Bearer auth; 401 on missing; 403 on mismatch; existing P201 pattern reused; never log secret |
| T-226-07-02 | Tampering | Migration 161 RLS hardening | mitigate | Idempotent ALTER ... ENABLE statements; CHECK constraints already in place; no destructive operations |
| T-226-07-03 | Information Disclosure | UI display of share_link_token | mitigate | DealRoomViewer + Approval Inbox + sales-store ALL render share_link_token as `dr_*****` masked; never logged; verified by Chromatic stories (snapshot diff would catch unmask) |
| T-226-07-04 | Repudiation | Cron run audit | mitigate | Each cron handler emits markos_audit_log row on completion (success or failure); kind + result + tenant_count fields; SHA-256 hash chain extends per existing P201 trigger |
| T-226-07-05 | Denial of Service | Cron handler resource consumption | mitigate | Each handler iterates SELECT ... LIMIT N (deferred N-tuning to v2 per 226-RESEARCH §Scope Guardrails); fall-through if SELECT timeout; emit warning audit + retry next schedule |
| T-226-07-06 | Information Disclosure | Tombstone cascade test | mitigate | Test runs in isolated test DB (vitest fixtures); production tombstones run only via P221 D-24 callback registered in 226-05 Task 3 |
| T-226-07-07 | Tampering | Wave-execution migration order | mitigate | test/migrations/order.test.ts asserts numeric order ≠ execution order; carries P224 Blocker 5 + P225 Wave 0 verbatim; 162 LAST verifies full chain |
</threat_model>

<verification>
- [ ] `node --test test/sales/cron/ test/sales/lifecycle/ test/sales/tombstone/ test/sales/share/` green (10 cron tests across 4 directories)
- [ ] `node --test test/sales/rls/rls-suite-full.test.ts test/sales/rls/share-link-token-policy.test.ts` green
- [ ] `node --test test/migrations/order.test.ts` green
- [ ] `node --test test/regression/p221-p225.regression.test.ts` green
- [ ] `npm test test/regression/p100-p105-p201.regression.test.ts` green
- [ ] `npx playwright test --grep sales` green (6 specs)
- [ ] `npm run chromatic -- --only-changed test/chromatic/sales/` green (24 visual snapshots)
- [ ] `jq '.crons | map(select(.path | startswith("/api/cron/sales"))) | length' vercel.json` returns 10
- [ ] `grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/161_sales_rls_hardening.sql` returns 16 (15 P226 tables + battlecard_versions)
- [ ] `grep "Phase 226" public/llms.txt` returns 1+ line
- [ ] FULL phase regression: `npm test && npm test && npx playwright test && npm run chromatic` ALL exit 0
- [ ] OpenAPI regen succeeded: `node scripts/contracts/verify-flow-registry-integrity.js --phase 226 --expected-count 18` returns ok=true
- [ ] 226-VALIDATION.md all rows ✅ green; frontmatter `status: ready` set
</verification>

<success_criteria>
- 6 UI workspaces + sales-store + Approval Inbox sales extensions + Morning Brief sales section shipped per D-46..D-49
- 10 cron handlers (7 sales per D-50..D-53 + 3 review-driven D-85/D-86/D-89) + Pitfall 8 mitigation + dispatcher + vercel.json schedules
- Migration 161 RLS hardening + honeypot_detected column + idempotent re-apply safe (slot 161 per B6 allocation; 157-160 reserved by Plans 03/04/05)
- Migration 162 OpenAPI regen marker + flow-registry final integrity (18 contracts F-163..F-180; slot 162 LAST per B6 allocation)
- 6 Playwright operator journey specs (D-46 + D-48 + D-49 user flows)
- 6 Chromatic stories × 4 variants = 24 visual snapshots
- RLS suite full proven across 50+ tables (P226 + P221-P225 + P100-P105 + P201)
- Tombstone cascade end-to-end test (P221 D-24 → P222 D-32 → P224 → P225 D-45 → P226 D-56)
- Wave-execution migration order test (carries P224 Blocker 5 + P225 Wave 0 verbatim)
- Legacy regression matrix: P100-P105 + P201 + P221-P225 + 226-01..06 ALL green
- public/llms.txt Phase 226 section with 18 contracts + 17 migrations + 8 MCP + 6 UI + 10 cron + A1-A24 fallback handlers documented
- 226-VALIDATION.md all task rows flipped to ✅ green
- ZERO new contracts, ZERO new dependencies; only 2 closeout migrations
- Phase 226 ready for `/gsd-verify-work`
</success_criteria>

<output>
After completion, create `.planning/phases/226-sales-enablement-deal-execution/226-07-SUMMARY.md` documenting: 6 UI workspaces + 13 components + 9 routes + 2 operator extensions + 10 cron handlers (7 sales + 3 review-driven D-85/D-86/D-89) + 1 cron dispatcher + 10 vercel.json schedules + 2 migrations (161 + 162; slots renumbered per B6) + 6 Playwright + 6 Chromatic × 4 variants (24 snapshots) + 10 cron tests across 4 directories + 2 RLS tests + 1 migration order test + 2 regression tests + llms.txt update; full decision coverage map (D-01..D-60 ALL covered across 226-01..07); A1..A24 fallback handler matrix all wired; full phase quality baseline gates passed; 18/18 contracts F-163..F-180; 17/17 migrations 146-162; 8/8 MCP tools; 6/6 UI workspaces; 10/10 cron handlers (7 sales + 3 review-driven D-85/D-86/D-89); phase ready for `/gsd-verify-work`. ALSO update STATE.md current position to "Phase 226 ready for verification" + update ROADMAP.md Phase 226 status to "📋 Planned — 7 plans complete; ready for verification" + commit Phase 226 closeout.
</output>
