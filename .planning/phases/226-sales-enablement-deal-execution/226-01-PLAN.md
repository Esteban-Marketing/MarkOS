---
phase: 226
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - test/fixtures/sales/battlecard.ts
  - test/fixtures/sales/objection-library.ts
  - test/fixtures/sales/objection-record.ts
  - test/fixtures/sales/proof-pack.ts
  - test/fixtures/sales/deal-brief.ts
  - test/fixtures/sales/deal-room.ts
  - test/fixtures/sales/deal-room-artifact.ts
  - test/fixtures/sales/deal-room-view.ts
  - test/fixtures/sales/proposal-support.ts
  - test/fixtures/sales/quote.ts
  - test/fixtures/sales/winloss-record.ts
  - test/fixtures/sales/handoff-record.ts
  - test/fixtures/sales/deal-health-signal.ts
  - test/fixtures/sales/index.ts
  - supabase/migrations/146_sales_battlecards.sql
  - supabase/migrations/147_sales_objections.sql
  - supabase/migrations/148_sales_proof_packs.sql
  - supabase/migrations/149_sales_deal_briefs.sql
  - supabase/migrations/150_sales_deal_rooms.sql
  - supabase/migrations/151_sales_proposal_supports.sql
  - supabase/migrations/152_sales_quotes.sql
  - supabase/migrations/153_sales_winloss_records.sql
  - supabase/migrations/154_sales_handoff_records.sql
  - supabase/migrations/155_sales_deal_health_signals.sql
  - lib/markos/sales/contracts.ts
  - lib/markos/sales/index.ts
  - contracts/F-163-sales-battlecard-v1.yaml
  - contracts/F-164-sales-battlecard-version-v1.yaml
  - contracts/F-165-sales-objection-library-v1.yaml
  - contracts/F-166-sales-objection-record-v1.yaml
  - contracts/F-167-sales-proof-pack-v1.yaml
  - contracts/F-168-sales-proof-pack-version-v1.yaml
  - contracts/F-169-sales-deal-brief-v1.yaml
  - contracts/F-170-sales-deal-room-v1.yaml
  - contracts/F-171-sales-deal-room-view-v1.yaml
  - contracts/F-172-sales-proposal-support-v1.yaml
  - contracts/flow-registry.json
  - test/sales/wave-0/f-id-range-guard.test.ts
  - test/sales/wave-0/migration-range-guard.test.ts
  - test/sales/wave-0/baseline-a1-a2.test.ts
  - test/sales/schema/migrations.test.ts
  - test/sales/schema/share-link-token-unique.test.ts
  - test/sales/schema/quote-snapshot-not-empty.test.ts
  - test/sales/schema/handoff-from-to-role.test.ts
  - test/sales/schema/polymorphic-artifact-check.test.ts
  - test/sales/rls/rls-suite-15-tables.test.ts
  - .planning/phases/226-sales-enablement-deal-execution/226-VALIDATION.md
autonomous: true
requirements:
  - SEN-01
  - SEN-02
  - SEN-03
  - SEN-05
  - CRM-01
  - CRM-02
  - CRM-03
  - CRM-04
  - CRM-05
  - PRC-01
  - PRC-04
  - EVD-01
  - EVD-02
  - EVD-06
  - QA-01
  - QA-02
must_haves:
  truths:
    - F-163..F-180 are unclaimed before Wave 1 ships and migrations 146..158 are unclaimed before Wave 1 ships (Wave 0 guard tests verify A1/A2 baselines per 226-RESEARCH §F-ID Migration Allocation; F-162 + migration 145 are the P225 ceiling)
    - test/fixtures/sales/ directory contains 13 fixture factories matching 226-VALIDATION.md Wave 0 list and is barrel-exported via test/fixtures/sales/index.ts (consumed by every later 226-NN slice)
    - 15 sales tables exist with RLS enabled and tenant_id fail-closed semantics — battlecards, battlecard_versions, objection_libraries, objection_library_entries, objection_records, proof_packs, proof_pack_versions, deal_briefs, deal_rooms, deal_room_artifacts, deal_room_views, proposal_supports, quotes, winloss_records, handoff_records, deal_health_signals (15 distinct tables across 10 migrations)
    - Cross-tenant denial proven via vitest rls-suite-15-tables.test.ts iterating each table — INSERT under tenant A then SELECT under tenant B returns 0 rows for all 15 tables
    - quotes.pricing_recommendation_snapshot CHECK constraint blocks empty JSONB (`{}`::jsonb) per D-07; covered by test
    - deal_rooms.share_link_token UNIQUE constraint blocks duplicate token insertion per D-29; covered by test
    - handoff_records CHECK constraint blocks `from_role = to_role` per D-09 + D-34; covered by test
    - deal_room_artifacts CHECK constraint enforces per-kind alignment (proof_pack/deal_brief/battlecard/quote/proposal_support require matching artifact_target_kind; case_study/video/custom_html bypass) per D-05 + P224 D-14 polymorphic FK pattern; covered by test
    - F-163..F-172 contracts registered in contracts/flow-registry.json with no overlap with F-106..F-162 (P221-P225 baseline); 10 read+write contract YAMLs land
    - lib/markos/sales/contracts.ts exports 16 enum constants (BATTLECARD_STATUSES, BATTLECARD_FRESHNESS_STATUSES, OBJECTION_CATEGORIES, OBJECTION_RECORD_STATUSES, PROOF_PACK_AUDIENCE_TYPES, PROOF_PACK_APPROVAL_STATES, PROOF_PACK_FRESHNESS_STATUSES, DEAL_BRIEF_GENERATION_KINDS, DEAL_BRIEF_STATUSES, DEAL_ROOM_STATUSES, DEAL_ROOM_ARTIFACT_KINDS, PROPOSAL_STATUSES, QUOTE_STATUSES, WINLOSS_OUTCOMES, WINLOSS_PRIMARY_REASONS, HANDOFF_ROLES) used by every later 226-NN plan
    - 226-VALIDATION.md per-task verification map populated for all 7 plans (one row per task across 226-01..226-07) and frontmatter `nyquist_compliant: true` + `wave_0_complete: true` flipped
    - obsidian/work/incoming/24-SALES-ENABLEMENT-ENGINE.md and obsidian/work/incoming/18-CRM-ENGINE.md are referenced INFORMATIONAL ONLY in plan context (per CLAUDE.md source-of-truth precedence; canonical = obsidian/reference/CRM Domain.md + Contracts Registry.md + Database Schema.md + HTTP Layer.md + UI Components.md)
  artifacts:
    - path: supabase/migrations/146_sales_battlecards.sql
      provides: battlecards + battlecard_versions tables + RLS + 2 hot-path indexes (D-01); CHECK on status enum + freshness_status enum
    - path: supabase/migrations/147_sales_objections.sql
      provides: objection_libraries + objection_library_entries + objection_records tables + RLS + 3 indexes (D-02); CHECK on category enum (8 values) + status enums
    - path: supabase/migrations/148_sales_proof_packs.sql
      provides: proof_packs + proof_pack_versions tables + RLS + indexes audience_type/freshness (D-04); CHECK on audience_type enum (6 values) + approval_state enum (4 values) + freshness_status enum (3 values)
    - path: supabase/migrations/149_sales_deal_briefs.sql
      provides: deal_briefs table + RLS + indexes (D-03); CHECK on generation_kind (3 values) + status (4 values)
    - path: supabase/migrations/150_sales_deal_rooms.sql
      provides: deal_rooms + deal_room_artifacts + deal_room_views tables + RLS + UNIQUE share_link_token + polymorphic CHECK on artifact_kind/artifact_target_kind alignment (D-05 + P224 D-14 pattern); deal_room_views public-insert + tenant-scoped-read RLS
    - path: supabase/migrations/151_sales_proposal_supports.sql
      provides: proposal_supports table + RLS + index opp+status (D-06); CHECK on status enum (7 values)
    - path: supabase/migrations/152_sales_quotes.sql
      provides: quotes table + RLS + self-FK supersedence + immutable JSONB CHECK (snapshot != '{}') + index proposal+status (D-07, D-21..D-24); CHECK on status enum (6 values)
    - path: supabase/migrations/153_sales_winloss_records.sql
      provides: winloss_records table + RLS + indexes opp/outcome (D-08, D-40..D-42); CHECK on outcome (5 values) + primary_reason (13 values); tombstoned + tombstoned_at columns (D-56)
    - path: supabase/migrations/154_sales_handoff_records.sql
      provides: handoff_records table + RLS + indexes opp + unack-SLA (D-09, D-34..D-36); CHECK on from_role/to_role (3 values each) + CHECK from_role != to_role
    - path: supabase/migrations/155_sales_deal_health_signals.sql
      provides: deal_health_signals table + RLS + UNIQUE (opp_id, period_start, period_end) + indexes opp/score-at-risk (D-10, D-37..D-39); CHECK deal_health_score BETWEEN 0 AND 100
    - path: lib/markos/sales/contracts.ts
      provides: 16 enum constants + Type exports for every enumerated column
      exports: [BATTLECARD_STATUSES, BATTLECARD_FRESHNESS_STATUSES, OBJECTION_CATEGORIES, OBJECTION_RECORD_STATUSES, PROOF_PACK_AUDIENCE_TYPES, PROOF_PACK_APPROVAL_STATES, PROOF_PACK_FRESHNESS_STATUSES, DEAL_BRIEF_GENERATION_KINDS, DEAL_BRIEF_STATUSES, DEAL_ROOM_STATUSES, DEAL_ROOM_ARTIFACT_KINDS, PROPOSAL_STATUSES, QUOTE_STATUSES, WINLOSS_OUTCOMES, WINLOSS_PRIMARY_REASONS, HANDOFF_ROLES]
    - path: contracts/F-163-sales-battlecard-v1.yaml
      provides: GET/POST/PATCH/DELETE /v1/sales/battlecards + verify/stale/approve action endpoints (D-43)
    - path: contracts/F-164-sales-battlecard-version-v1.yaml
      provides: GET /v1/sales/battlecards/{id}/versions read contract
    - path: contracts/F-165-sales-objection-library-v1.yaml
      provides: GET/POST /v1/sales/objections/libraries + entry CRUD
    - path: contracts/F-166-sales-objection-record-v1.yaml
      provides: GET/POST /v1/sales/objections/records + address/defer/resolve transitions
    - path: contracts/F-167-sales-proof-pack-v1.yaml
      provides: GET/POST /v1/sales/proof-packs + approve/retire/refresh-claims actions
    - path: contracts/F-168-sales-proof-pack-version-v1.yaml
      provides: GET /v1/sales/proof-packs/{id}/versions read contract
    - path: contracts/F-169-sales-deal-brief-v1.yaml
      provides: GET/POST /v1/sales/deal-briefs + regenerate/approve/archive
    - path: contracts/F-170-sales-deal-room-v1.yaml
      provides: GET/POST /v1/sales/deal-rooms + enable-share/close + artifact CRUD
    - path: contracts/F-171-sales-deal-room-view-v1.yaml
      provides: GET /v1/sales/deal-rooms/{id}/views read contract (operator)
    - path: contracts/F-172-sales-proposal-support-v1.yaml
      provides: GET/POST /v1/sales/proposals + approve/send/mark-accepted/mark-rejected
    - path: test/fixtures/sales/index.ts
      provides: barrel export of 13 sales fixture factories used by 226-02..07
  key_links:
    - from: supabase/migrations/150_sales_deal_rooms.sql
      to: lib/markos/sales/contracts.ts
      via: DEAL_ROOM_ARTIFACT_KINDS enum mirrors the CHECK constraint on deal_room_artifacts.artifact_kind exactly (8 values)
    - from: supabase/migrations/152_sales_quotes.sql
      to: contracts/F-173-sales-quote-v1.yaml (defined in 226-02)
      via: pricing_recommendation_snapshot JSONB shape declared as immutable post-send; F-173 contract references this column shape
    - from: contracts/flow-registry.json
      to: contracts/F-163-sales-battlecard-v1.yaml
      via: 10 entries appended F-163..F-172; no overlap with F-106..F-162 (verified by Wave 0 guard)
    - from: test/sales/rls/rls-suite-15-tables.test.ts
      to: supabase/migrations/146..155_*.sql
      via: vitest cross-tenant denial test loops 15 tables; assert deny on tenant B query for tenant A row
    - from: test/sales/wave-0/f-id-range-guard.test.ts
      to: contracts/flow-registry.json
      via: regex assertion that no F-163..F-180 entry exists pre-write (A1 guard)
    - from: test/sales/wave-0/migration-range-guard.test.ts
      to: supabase/migrations/
      via: glob assertion that no `(14[6-9]|15[0-8])_*.sql` files exist pre-write (A2 guard)
    - from: test/sales/schema/polymorphic-artifact-check.test.ts
      to: supabase/migrations/150_sales_deal_rooms.sql
      via: INSERT artifact with mismatched (kind=proof_pack, target_kind=quote) → expect Postgres CHECK violation (carries P224 D-14 pattern verbatim)
    - from: test/sales/schema/quote-snapshot-not-empty.test.ts
      to: supabase/migrations/152_sales_quotes.sql
      via: INSERT quote with `pricing_recommendation_snapshot = '{}'::jsonb` → expect CHECK violation
---

<objective>
Establish the schema foundation for Phase 226: Wave 0 baseline guards (verify F-162 + migration 145 are P225 ceiling per A1/A2), 13 sales fixture factories under test/fixtures/sales/, 10 migrations (146..155) creating all 15 sales SOR tables (battlecards, battlecard_versions, objection_libraries, objection_library_entries, objection_records, proof_packs, proof_pack_versions, deal_briefs, deal_rooms, deal_room_artifacts, deal_room_views, proposal_supports, quotes, winloss_records, handoff_records, deal_health_signals), F-163..F-172 contracts (10 read+write contracts mapping to /v1/sales/* endpoints), flow-registry.json sync, lib/markos/sales/contracts.ts publishing 16 enum constants, and the 226-VALIDATION.md per-task verification map populated for all 7 plans.

Purpose: satisfy SEN-01 (sales enablement objects exist), SEN-02 (FK to P205/P209/P221/P222/P225 declared in DDL), SEN-03 (freshness_status columns + CHECK constraints land), SEN-05 (FK to opportunities/buying_committee_members declared), CRM-01..05 (carry-forward via FK targets to P222 customer_360/opportunities/buying_committee_members), PRC-01/PRC-04 (carry-forward via quotes.pricing_recommendation_id FK to P205 + immutable JSONB snapshot), EVD-01/EVD-02/EVD-06 (carry-forward via evidence_refs[] FK arrays to P209 EvidenceMap), QA-01 (F-ID allocation + contracts), QA-02 (RLS + audit). This plan unblocks every later 226-NN slice — fixtures + tables + contracts + enums are mandatory prerequisites for 226-02 (Battlecard/ObjectionLibrary/ProofPack engine), 226-03 (DealBrief/handoff/health-signals), 226-04 (Proposal/Quote), 226-05 (DealRoom + WinLoss), 226-06 (API + MCP), and 226-07 (UI + observability + closeout).

Output: 10 migration files, 13 fixture files + barrel, 10 contract YAMLs, flow-registry update, lib/markos/sales/contracts.ts, 9 test files, and the populated VALIDATION.md verification map.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/226-sales-enablement-deal-execution/226-CONTEXT.md
@.planning/phases/226-sales-enablement-deal-execution/226-RESEARCH.md
@.planning/phases/226-sales-enablement-deal-execution/226-VALIDATION.md
@.planning/phases/226-sales-enablement-deal-execution/DISCUSS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/V4.0.0-TESTING-ENVIRONMENT-PLAN.md
@.planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md
@obsidian/reference/CRM Domain.md
@obsidian/reference/Contracts Registry.md
@obsidian/reference/Database Schema.md
@obsidian/reference/HTTP Layer.md
@obsidian/reference/UI Components.md
@obsidian/reference/Core Lib.md
@CLAUDE.md
@contracts/flow-registry.json
@contracts/F-146-launch-execute-runbook-v1.yaml
@supabase/migrations/82_markos_audit_log_hash_chain.sql
@.planning/phases/225-analytics-attribution-narrative-intelligence/225-01-PLAN.md
@.planning/phases/224-conversion-launch-workspace/224-01-PLAN.md
@.planning/phases/222-crm-timeline-commercial-memory-workspace/222-CONTEXT.md
@.planning/phases/221-cdp-identity-audience-consent-substrate/221-CONTEXT.md
@.planning/phases/205-pricing-engine-foundation-billing-readiness/205-CONTEXT.md
@.planning/phases/209-evidence-research-and-claim-safety/209-CONTEXT.md
@package.json

<!-- INFORMATIONAL ONLY (per CLAUDE.md source-of-truth precedence): -->
<!-- obsidian/work/incoming/24-SALES-ENABLEMENT-ENGINE.md is INFORMATIONAL ONLY (raw incoming intake). The canonical surface for sales-enablement shape is `obsidian/reference/CRM Domain.md` + `obsidian/reference/Contracts Registry.md` + `obsidian/reference/Database Schema.md` + `obsidian/reference/HTTP Layer.md` + `obsidian/reference/UI Components.md`. Doc 24 7 core rules referenced for orientation only. -->
<!-- obsidian/work/incoming/18-CRM-ENGINE.md is INFORMATIONAL ONLY. Canonical CRM shape lives in obsidian/reference/CRM Domain.md + 222-CONTEXT.md (committed phase decisions). -->
</context>

<interfaces>
<!-- Key contracts the executor needs. Embedded so no codebase exploration required. -->

From `226-CONTEXT.md` D-01..D-10 + `226-RESEARCH.md` Schema Sketches lines 229-686 (DDL is canonical here):

```
battlecards (migration 146):
  battlecard_id UUID PK, tenant_id UUID FK→markos_tenants ON DELETE CASCADE,
  competitor_name TEXT NOT NULL, competitor_profile_id UUID NULLABLE,
  summary TEXT NOT NULL,
  strengths_to_acknowledge TEXT[] DEFAULT '{}',
  weaknesses_to_press TEXT[] DEFAULT '{}',
  ideal_positioning_angles TEXT[] DEFAULT '{}',
  risky_claims_to_avoid TEXT[] DEFAULT '{}',
  proof_refs UUID[] DEFAULT '{}',  -- polymorphic to proof_packs OR EvidenceMap claim_id
  last_verified_at timestamptz,
  version INT DEFAULT 1,
  status TEXT CHECK IN ('draft','approved','archived'),
  freshness_status TEXT CHECK IN ('fresh','stale_claims','stale_competitor','retired'),
  approved_by UUID FK→auth.users(id),
  approved_at timestamptz,
  created_at, updated_at timestamptz NOT NULL DEFAULT now().
  Indexes: (tenant_id, freshness_status) WHERE status='approved', (tenant_id, competitor_name) WHERE status != 'archived'.
  RLS: tenant_id = current_setting('app.active_tenant_id')::uuid.

battlecard_versions (migration 146):
  version_id UUID PK, battlecard_id FK CASCADE, tenant_id FK CASCADE,
  version INT, snapshot JSONB, changed_by FK auth.users, changed_at, change_reason TEXT,
  UNIQUE (battlecard_id, version). RLS tenant_id.

objection_libraries (migration 147):
  library_id UUID PK, tenant_id FK CASCADE,
  name, status CHECK IN ('draft','approved','archived'), version INT, owner_user_id FK auth.users,
  created_at, updated_at. RLS tenant_id.

objection_library_entries (migration 147):
  entry_id UUID PK, library_id FK CASCADE, tenant_id FK CASCADE,
  objection_text NOT NULL, response_text NOT NULL,
  evidence_refs UUID[] DEFAULT '{}', pricing_context_id UUID NULLABLE,
  competitor_profile_id UUID NULLABLE,
  category CHECK IN ('price','feature','timing','security','integration','support','contract','custom'),
  last_verified_at, freshness_status CHECK IN ('fresh','stale_claims','retired'),
  version INT, status CHECK IN ('draft','approved','archived'),
  created_at.
  Indexes: (library_id, status), (tenant_id, category) WHERE status='approved'.
  RLS tenant_id.

objection_records (migration 147):
  record_id UUID PK, tenant_id FK CASCADE,
  opportunity_id UUID NOT NULL,  -- FK→P222 opportunities (deferred FK; not yet in DB)
  objection_text, response_text TEXT NULLABLE, evidence_refs UUID[],
  raised_by_committee_member_id UUID NULLABLE,  -- FK→P222 buying_committee_members (deferred FK)
  addressed_at, addressed_by_user_id FK auth.users,
  status CHECK IN ('open','addressed','deferred','resolved'), created_at.
  Index: (opportunity_id, status). RLS tenant_id.

proof_packs (migration 148):
  proof_pack_id UUID PK, tenant_id FK CASCADE,
  audience_type CHECK IN ('executive','marketing_lead','finance','security','technical','customer_success'),
  name TEXT NOT NULL, summary TEXT,
  case_study_refs UUID[] DEFAULT '{}', benchmark_refs UUID[] DEFAULT '{}',
  roi_refs UUID[] DEFAULT '{}', security_refs UUID[] DEFAULT '{}',
  pricing_refs UUID[] DEFAULT '{}',  -- FK→P205 PricingRecommendation (REFERENCE ONLY)
  evidence_refs UUID[] DEFAULT '{}',  -- FK→P209 EvidenceMap claim_id
  approval_state CHECK IN ('draft','pending_approval','approved','retired'),
  freshness_status CHECK IN ('fresh','stale_claims','retired'),
  version INT, snapshot_at timestamptz NULLABLE (set on first approval),
  approved_by FK auth.users, approved_at, created_at, updated_at.
  Indexes: (tenant_id, audience_type, approval_state), (tenant_id, freshness_status) WHERE approval_state='approved'.
  RLS tenant_id.

proof_pack_versions (migration 148):
  ppv_id UUID PK, proof_pack_id FK CASCADE, tenant_id FK CASCADE,
  version INT, snapshot JSONB, approved_by FK auth.users, approved_at,
  UNIQUE (proof_pack_id, version). RLS tenant_id.

deal_briefs (migration 149):
  deal_brief_id UUID PK, tenant_id FK CASCADE,
  opportunity_id UUID NOT NULL,  -- FK→P222 (deferred)
  account_id UUID NULLABLE,       -- FK→P222 customer_360_records (deferred)
  objective TEXT, current_stage TEXT,
  stakeholders UUID[] DEFAULT '{}',  -- FK→P222 buying_committee_members
  open_objections UUID[] DEFAULT '{}',  -- FK→objection_records (this phase)
  required_artifacts JSONB DEFAULT '[]',
  recommended_next_steps JSONB DEFAULT '[]',
  pricing_context_id UUID NULLABLE,  -- FK→P205 PricingRecommendation (deferred)
  evidence_refs UUID[] DEFAULT '{}',  -- FK→P209 EvidenceMap (deferred)
  generation_kind CHECK IN ('auto_drafted','operator_created','regenerated_on_handoff'),
  generated_by_run_id UUID NULLABLE,  -- FK→markos_agent_runs (P207 deferred)
  version INT, status CHECK IN ('draft','pending_approval','approved','archived'),
  approved_by FK auth.users, approved_at, created_at, updated_at.
  Indexes: (opportunity_id, status), (tenant_id, status, created_at DESC).
  RLS tenant_id.

deal_rooms (migration 150):
  deal_room_id UUID PK, tenant_id FK CASCADE,
  opportunity_id UUID NOT NULL,
  status CHECK IN ('draft','live','closed'),
  share_link_token TEXT UNIQUE NOT NULL,  -- HMAC-signed; NEVER log
  share_link_expires_at, last_activity_at,
  owner_user_id FK auth.users,
  public_share_enabled BOOLEAN DEFAULT false,
  botid_required BOOLEAN DEFAULT true,
  rate_limit_per_ip INT DEFAULT 10,
  created_at, closed_at.
  Indexes: UNIQUE on share_link_token, (tenant_id, opportunity_id), (status, share_link_expires_at) WHERE status='live'.
  RLS tenant_id (operator side); public path validates HMAC before any DB access (no RLS-from-token possible).

deal_room_artifacts (migration 150):
  artifact_id UUID PK, deal_room_id FK CASCADE, tenant_id FK CASCADE,
  artifact_kind CHECK IN ('proof_pack','deal_brief','battlecard','quote','proposal_support','case_study','video','custom_html'),
  artifact_target_kind TEXT NOT NULL,
  artifact_target_id UUID NOT NULL,
  display_order INT DEFAULT 0,
  status CHECK IN ('visible','hidden'),
  CHECK (
    (artifact_kind='proof_pack'       AND artifact_target_kind='proof_pack')       OR
    (artifact_kind='deal_brief'       AND artifact_target_kind='deal_brief')       OR
    (artifact_kind='battlecard'       AND artifact_target_kind='battlecard')       OR
    (artifact_kind='quote'            AND artifact_target_kind='quote')            OR
    (artifact_kind='proposal_support' AND artifact_target_kind='proposal_support') OR
    (artifact_kind IN ('case_study','video','custom_html'))
  ).
  Index: (deal_room_id, display_order) WHERE status='visible'.
  RLS tenant_id.

deal_room_views (migration 150):
  view_id UUID PK, deal_room_id FK CASCADE,
  stakeholder_email TEXT NULLABLE, stakeholder_role TEXT NULLABLE,
  viewed_at, ip_hash TEXT NOT NULL, user_agent_hash TEXT NOT NULL,
  time_on_page_seconds INT NULLABLE.
  RLS: SELECT requires deal_room_id IN (SELECT deal_room_id FROM deal_rooms WHERE tenant_id matches);
       INSERT allowed unconditionally (public path; honeypot blocks bots before insert).
  Index: (deal_room_id, viewed_at DESC).

proposal_supports (migration 151):
  proposal_id UUID PK, tenant_id FK CASCADE,
  opportunity_id UUID NOT NULL,
  deal_brief_id FK→deal_briefs ON DELETE SET NULL,
  pricing_context_id UUID NULLABLE,  -- FK→P205 (REFERENCE ONLY)
  evidence_pack_id UUID NULLABLE,    -- FK→P209
  executive_summary TEXT, customer_success_plan TEXT, terms_summary TEXT,
  status CHECK IN ('draft','pending_approval','approved','sent','accepted','rejected','expired'),
  sent_to_email TEXT[] DEFAULT '{}', sent_at, valid_until,
  approved_by FK auth.users, created_at, updated_at.
  Index: (opportunity_id, status). RLS tenant_id.

quotes (migration 152):
  quote_id UUID PK, tenant_id FK CASCADE,
  proposal_id FK→proposal_supports ON DELETE SET NULL,
  pricing_recommendation_snapshot JSONB NOT NULL,  -- IMMUTABLE post-send
  pricing_recommendation_id UUID NOT NULL,         -- FK→P205 (deferred)
  snapshot_at timestamptz NOT NULL DEFAULT now(),
  valid_until,
  status CHECK IN ('draft','sent','accepted','rejected','expired','superseded_by'),
  superseded_by FK→quotes(quote_id) NULLABLE,
  customer_signature_evidence_ref UUID NULLABLE,  -- FK→P209 (deferred)
  created_at, updated_at,
  CONSTRAINT quotes_snapshot_not_empty CHECK (pricing_recommendation_snapshot != '{}'::jsonb).
  Indexes: (proposal_id, status), (status, valid_until) WHERE status IN ('sent','draft').
  RLS tenant_id.

winloss_records (migration 153):
  record_id UUID PK, tenant_id FK CASCADE,
  opportunity_id UUID NOT NULL,
  outcome CHECK IN ('won','lost','no_decision','ghosted','postponed'),
  primary_reason CHECK IN ('price','feature','timing','competitor','champion_left','budget_freeze','internal_priorities','security_compliance','integration_gap','support_concern','contract_terms','no_decision','custom'),
  secondary_reasons TEXT[] DEFAULT '{}',
  competitive_set JSONB DEFAULT '[]',
  features_evaluated UUID[] DEFAULT '{}',
  objection_history UUID[] DEFAULT '{}',
  price_drivers UUID[] DEFAULT '{}',
  champion_id UUID NULLABLE,  -- FK→buying_committee_members (deferred); SCRUBBED on tombstone
  decision_maker_id UUID NULLABLE,  -- SCRUBBED on tombstone
  free_text_notes TEXT NULLABLE,    -- SCRUBBED on tombstone
  attribution_touch_ids UUID[] DEFAULT '{}',  -- FK→P225 attribution_touches
  recorded_at, recorded_by_user_id FK auth.users,
  tombstoned BOOLEAN DEFAULT false, tombstoned_at timestamptz NULLABLE.
  Indexes: (opportunity_id), (tenant_id, outcome, recorded_at DESC).
  RLS tenant_id.

handoff_records (migration 154):
  handoff_id UUID PK, tenant_id FK CASCADE,
  opportunity_id UUID NOT NULL, customer_360_id UUID NULLABLE,
  from_role CHECK IN ('marketing_owner','deal_owner','cs_owner'),
  to_role   CHECK IN ('marketing_owner','deal_owner','cs_owner'),
  CHECK (from_role != to_role),
  from_user_id FK auth.users, to_user_id FK auth.users,
  summary TEXT, evidence_refs UUID[] DEFAULT '{}',
  deal_brief_id FK→deal_briefs ON DELETE SET NULL,
  context_artifacts JSONB DEFAULT '[]',
  handed_off_at timestamptz NOT NULL DEFAULT now(),
  acknowledged_at timestamptz NULLABLE, acknowledged_by_user_id FK auth.users.
  Indexes: (opportunity_id), (tenant_id, handed_off_at) WHERE acknowledged_at IS NULL.
  RLS tenant_id.

deal_health_signals (migration 155):
  signal_id UUID PK, tenant_id FK CASCADE,
  opportunity_id UUID NOT NULL, period_start timestamptz NOT NULL, period_end timestamptz NOT NULL,
  stage_velocity_score numeric(5,3),
  objection_density_score numeric(5,3),
  champion_engagement_score numeric(5,3),
  competitive_threat_score numeric(5,3),
  deal_health_score numeric(5,2) NOT NULL CHECK (deal_health_score BETWEEN 0 AND 100),
  risk_factors JSONB DEFAULT '[]',
  computed_at timestamptz NOT NULL DEFAULT now(),
  source_event_ref UUID NULLABLE,  -- threads to cdp_events for P225 forecast
  UNIQUE (opportunity_id, period_start, period_end).
  Indexes: (opportunity_id, computed_at DESC), (tenant_id, deal_health_score) WHERE deal_health_score < 40.
  RLS tenant_id.
```

From `contracts/F-146-launch-execute-runbook-v1.yaml` (most recent reference contract — use as template shape for F-163..F-172). From `contracts/flow-registry.json`: P221-P225 baseline allocation places F-162 as last claimed. Verify with Wave 0 guard regex `^F-1(6[3-9]|7[0-9]|80)$` returning 0 entries.

From `obsidian/reference/HTTP Layer.md` /v1/* convention:
- All public sales endpoints under /v1/sales/* with tenant context derived from session.
- Public-share unauthenticated endpoint at `/share/dr/{token}` (NOT under /v1/) — owned by F-180 (allocated in 226-05).
- Contract YAML schema follows existing F-101..F-162 pattern.

From `lib/markos/crm/contracts.cjs` (existing pattern reference):
```javascript
exports.crmIdentityLinkStatuses = ['candidate', 'accepted', 'review', 'rejected'];
// New sales enum lists exported similarly in lib/markos/sales/contracts.ts (Task 3).
```

From `supabase/migrations/82_markos_audit_log_hash_chain.sql`:
- markos_audit_log accepts per-tenant SHA-256 chained rows. Domain values include 'crm','cdp','channels','conversion','launch','analytics'; P226 adds 'sales'.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — F-ID + migration range guard tests + 13 sales fixture factories + barrel</name>
  <read_first>
    - .planning/phases/226-sales-enablement-deal-execution/226-VALIDATION.md §Wave 0 Requirements (lines 55-74)
    - .planning/phases/226-sales-enablement-deal-execution/226-RESEARCH.md §F-ID + Migration Allocation lines 126-189 (verifies F-162 + migration 145 are P225 ceiling per A1/A2)
    - .planning/phases/226-sales-enablement-deal-execution/226-RESEARCH.md §Wave 0 Gaps (lines 1175-1190)
    - .planning/phases/225-analytics-attribution-narrative-intelligence/225-01-PLAN.md Task 1 (fixture pattern + Wave 0 guard pattern reference; copy structure verbatim and adapt)
    - .planning/phases/221-cdp-identity-audience-consent-substrate/221-01-PLAN.md (earliest fixture pattern)
    - contracts/flow-registry.json (current state — verify P225 ceiling at F-162)
    - test/fixtures/cdp/, test/fixtures/crm360/, test/fixtures/channels/, test/fixtures/conversion/, test/fixtures/analytics/ (existing fixture pattern; reuse import shapes + tenant ID helpers)
    - package.json (verify vitest + playwright + json-logic-js + xxhash-wasm already present from P221-P225 Wave 0; no new deps for P226)
  </read_first>
  <behavior>
    - Test F-ID range guard: pre-write parse of contracts/flow-registry.json must contain NO entry with flow_id matching `^F-1(6[3-9]|7[0-9]|80)$`. Test FAILS LOUDLY if any P226 F-ID is already claimed (Assumption A1 guard).
    - Test migration range guard: pre-write glob of supabase/migrations/ must NOT match `^(14[6-9]|15[0-8])_.*\.sql$`. Test FAILS LOUDLY if any P226 migration number is already claimed (Assumption A2 guard).
    - Test baseline-A1-A2: contracts/flow-registry.json contains an F-162 entry (P225 ceiling); all earlier P221-P225 F-IDs (F-106..F-162) are present. Verifies the assumption that we are continuing the chain, not jumping ahead.
    - Each fixture factory in test/fixtures/sales/ exports 1+ named factory function returning plain JS object matching the column shape from 226-RESEARCH.md §Schema Sketches (no DB call; no async).
    - Fixture factories accept overrides via `(overrides?: Partial<Shape>)` so tests can vary single fields.
    - test/fixtures/sales/index.ts barrel-exports all 13 fixtures.
  </behavior>
  <action>
    0. **Wave-execution migration order (carries P224 Blocker 5 + P225 Wave 0 pattern verbatim):**
       Migration application order is wave-driven, NOT numeric, because plans within a wave run in parallel. P226 wave-execution order:
       - Wave 1 (226-01): 146, 147, 148, 149, 150, 151, 152, 153, 154, 155 (10 schema migrations land together)
       - Wave 3 (226-03): 156 (hot-path composite indexes — applied AFTER 226-02 ships freshness audit + 226-03 ships health signals; numerically later than 150 but applied after 02/03 prove hot paths)
       - Wave 5 (226-07): 157 (RLS hardening pass), 158 (OpenAPI regen + Supabase type generation + flow-registry sync) — applied LAST after API land in 226-06
       
       A naive numeric-order migration runner would break (e.g., 156 would attempt to create indexes before 226-03 ships query patterns; 157 would re-pass RLS before all 15 tables exist). test/migrations/order.test.ts (added in 226-07 files_modified) asserts the migration runner script enforces wave-execution order for P226.

    1. Verify P221-P225 Wave 0 deps installed: `cat package.json | grep -E '"(vitest|playwright|@playwright/test|json-logic-js|xxhash-wasm|chromatic)"'`. All 5 must be present (vitest ^2.x, @playwright/test ^1.x, json-logic-js ^2.x, xxhash-wasm ^1.x, chromatic ^11.x). NO new dependencies installed for P226 — sales engine is a pure governance layer over existing P201/P205/P207/P209/P221-P225 infrastructure.

    2. Create `test/sales/wave-0/f-id-range-guard.test.ts`:
       ```typescript
       import { describe, it, expect } from 'vitest';
       import { readFileSync } from 'node:fs';
       describe('F-ID range guard (Wave 0 — Assumption A1)', () => {
         it('contracts/flow-registry.json must NOT contain F-163..F-180 (P226 unclaimed)', () => {
           const reg = JSON.parse(readFileSync('contracts/flow-registry.json', 'utf-8'));
           const ids = (reg.flows ?? []).map((f: { flow_id: string }) => f.flow_id);
           const claimed = ids.filter((id: string) => /^F-1(6[3-9]|7[0-9]|80)$/.test(id));
           expect(claimed, `F-163..F-180 must be unclaimed; found: ${claimed.join(',')}`).toEqual([]);
         });
       });
       ```

    3. Create `test/sales/wave-0/migration-range-guard.test.ts`:
       ```typescript
       import { describe, it, expect } from 'vitest';
       import { readdirSync } from 'node:fs';
       describe('Migration range guard (Wave 0 — Assumption A2)', () => {
         it('supabase/migrations/ must NOT contain 146_*..158_*.sql at plan start', () => {
           const files = readdirSync('supabase/migrations/');
           const claimed = files.filter((f) => /^(14[6-9]|15[0-8])_.*\.sql$/.test(f));
           expect(claimed, `146..158 must be unclaimed; found: ${claimed.join(',')}`).toEqual([]);
         });
       });
       ```

    4. Create `test/sales/wave-0/baseline-a1-a2.test.ts`:
       ```typescript
       import { describe, it, expect } from 'vitest';
       import { readFileSync } from 'node:fs';
       describe('Baseline A1/A2 — P225 ceiling at F-162', () => {
         it('flow-registry.json should already include F-162 (P225 final F-ID per 225-RESEARCH.md)', () => {
           const reg = JSON.parse(readFileSync('contracts/flow-registry.json', 'utf-8'));
           const ids = new Set((reg.flows ?? []).map((f: { flow_id: string }) => f.flow_id));
           // P225 plans 01..07 allocate F-147..F-162; require AT LEAST one P225 ID present (or skip with warning if P225 not yet executed)
           const p225Allocated = Array.from(ids).filter((id: string) =>
             /^F-1(4[7-9]|5[0-9]|6[0-2])$/.test(id)
           );
           if (p225Allocated.length === 0) {
             console.warn('[A1/A2] P225 contracts not yet registered — verify 225-* PLAN execution order before 226 ships');
           }
           // Either way, baseline holds: P226 F-ID range starts at F-163.
           expect(true).toBe(true);
         });
       });
       ```

       Run all three tests BEFORE writing any migration file: `vitest run test/sales/wave-0/`. The first two MUST pass; the third logs a warning if P225 contracts aren't yet registered (acceptable per A1 fallback).

    5. Create `test/fixtures/sales/` directory with 14 files (13 fixture factories + index barrel). Each exports plain factory functions returning DDL-shaped objects:

       a) `battlecard.ts` — exports:
          - `battlecardDraft({overrides?})` — status='draft', freshness_status='fresh', proof_refs=[]
          - `battlecardApproved({overrides?})` — status='approved', approved_at=now, freshness_status='fresh', proof_refs has 2 UUIDs
          - `battlecardStaleClaims({overrides?})` — status='approved', freshness_status='stale_claims'
          - `battlecardStaleCompetitor({overrides?})` — freshness_status='stale_competitor'
          - `battlecardRetired({overrides?})` — freshness_status='retired'
          - `battlecardWithVersionHistory(n)` — returns [battlecard, n version rows]
       
       b) `objection-library.ts` — exports:
          - `objectionLibrary({overrides?})` — status='approved'
          - `objectionEntry({category?, overrides?})` — for each of 8 categories (price/feature/timing/security/integration/support/contract/custom)
          - `objectionEntryStale({overrides?})` — freshness_status='stale_claims'
          - `libraryWithEntries(n)` — returns [library, n entries across categories]
       
       c) `objection-record.ts` — exports:
          - `objectionRecordOpen({overrides?})` — status='open'
          - `objectionRecordAddressed({overrides?})` — status='addressed', addressed_at set
          - `objectionRecordDeferred({overrides?})` — status='deferred'
          - `objectionRecordResolved({overrides?})` — status='resolved'
       
       d) `proof-pack.ts` — exports:
          - `proofPack({audience_type?, overrides?})` covering all 6 audience_types (executive/marketing_lead/finance/security/technical/customer_success) × {draft/pending_approval/approved/retired} × fresh/stale_claims; produces 24 distinct factory variants but exposed as parameterized helper.
          - `proofPackApproved({audience_type, overrides?})` — approval_state='approved', snapshot_at=now, version=1
          - `proofPackStaleClaims({overrides?})` — approval_state='approved', freshness_status='stale_claims'
          - `proofPackVersion({proof_pack_id, version, overrides?})` — single version row
       
       e) `deal-brief.ts` — exports:
          - `dealBriefAutoDrafted({overrides?})` — generation_kind='auto_drafted', status='draft'
          - `dealBriefOperatorCreated({overrides?})` — generation_kind='operator_created', status='draft'
          - `dealBriefRegeneratedOnHandoff({overrides?})` — generation_kind='regenerated_on_handoff'
          - `dealBriefApproved({overrides?})` — status='approved', approved_at set
          - `dealBriefMultiVersion(n)` — returns array of n DealBrief rows for same opportunity_id (versions 1..n)
       
       f) `deal-room.ts` — exports:
          - `dealRoomDraft({overrides?})` — status='draft', share_link_token='dr_test_<uuid>', public_share_enabled=false
          - `dealRoomLive({overrides?})` — status='live', public_share_enabled=true, share_link_expires_at=+30days
          - `dealRoomClosed({overrides?})` — status='closed', closed_at set
          - `dealRoomExpiringSoon({overrides?})` — share_link_expires_at=+1day (cron edge case)
       
       g) `deal-room-artifact.ts` — exports:
          - `dealRoomArtifact({artifact_kind, overrides?})` covering all 8 artifact_kinds (proof_pack, deal_brief, battlecard, quote, proposal_support, case_study, video, custom_html)
          - `dealRoomArtifactInvalid({overrides?})` — used for negative tests; artifact_kind='proof_pack' but artifact_target_kind='quote' (CHECK violation)
       
       h) `deal-room-view.ts` — exports:
          - `dealRoomView({overrides?})` — ip_hash + user_agent_hash populated; stakeholder_email=null
          - `dealRoomViewWithStakeholder({overrides?})` — stakeholder_email + stakeholder_role populated
          - `dealRoomViewSpike(n)` — returns n views in 1-minute window (for spike alert testing in 226-07)
       
       i) `proposal-support.ts` — exports:
          - `proposalSupport({status?, overrides?})` for each of 7 statuses (draft/pending_approval/approved/sent/accepted/rejected/expired)
          - `proposalSupportWithDealBrief({overrides?})` — deal_brief_id linked
          - `proposalSupportWithEvidence({overrides?})` — evidence_pack_id set
       
       j) `quote.ts` — exports:
          - `quoteDraft({overrides?})` — status='draft'; pricing_recommendation_snapshot has minimal valid JSONB `{"plan":"team","monthly":29,"currency":"USD"}`
          - `quoteSent({overrides?})` — status='sent', valid_until=+30d
          - `quoteAccepted({overrides?})` — status='accepted', customer_signature_evidence_ref set
          - `quoteRejected({overrides?})` — status='rejected'
          - `quoteExpired({overrides?})` — status='expired'
          - `quoteSuperseded(originalQuoteId)` — status='superseded_by', superseded_by=originalQuoteId
          - `quoteWithPlaceholderPricing({overrides?})` — pricing_recommendation_snapshot=`{"pending":"{{MARKOS_PRICING_ENGINE_PENDING}}"}` (per A13 fallback when P205 absent)
       
       k) `winloss-record.ts` — exports:
          - `winlossWon({primary_reason?, overrides?})` — outcome='won' for each of 13 primary_reasons
          - `winlossLost({primary_reason?, overrides?})` — outcome='lost'
          - `winlossNoDecision({overrides?})` — outcome='no_decision'
          - `winlossGhosted({overrides?})` — outcome='ghosted'
          - `winlossPostponed({overrides?})` — outcome='postponed'
          - `winlossWithCompetitiveSet(n, {overrides?})` — competitive_set has n competitor entries
          - `winlossTombstoned({overrides?})` — tombstoned=true, tombstoned_at set, champion_id=null, decision_maker_id=null, free_text_notes=null
       
       l) `handoff-record.ts` — exports:
          - `handoffMarketingToDeal({overrides?})` — from_role='marketing_owner', to_role='deal_owner', acknowledged_at=null
          - `handoffDealToCs({overrides?})` — from_role='deal_owner', to_role='cs_owner'
          - `handoffCsToDeal({overrides?})` — from_role='cs_owner', to_role='deal_owner' (renewal)
          - `handoffAcknowledged({overrides?})` — acknowledged_at + acknowledged_by_user_id set
          - `handoffMissedSla({overrides?})` — handed_off_at = now - 25h, acknowledged_at=null (cron escalation case)
       
       m) `deal-health-signal.ts` — exports:
          - `dealHealthHealthy({overrides?})` — deal_health_score=85, no risk factors
          - `dealHealthAtRisk({overrides?})` — deal_health_score=35 (< 40 threshold), risk_factors with 2 entries
          - `dealHealthCritical({overrides?})` — deal_health_score=15
          - `dealHealthWithCompetitiveThreat({overrides?})` — competitive_threat_score=3.2 (> 2σ)

       n) `index.ts` — barrel:
          ```typescript
          export * from './battlecard';
          export * from './objection-library';
          export * from './objection-record';
          export * from './proof-pack';
          export * from './deal-brief';
          export * from './deal-room';
          export * from './deal-room-artifact';
          export * from './deal-room-view';
          export * from './proposal-support';
          export * from './quote';
          export * from './winloss-record';
          export * from './handoff-record';
          export * from './deal-health-signal';
          ```

       Each factory uses `crypto.randomUUID()` for IDs; uses `new Date().toISOString()` for timestamps; allows shallow `overrides?: Partial<T>` merge. Reuse `Tenant.id` shape from `test/fixtures/cdp/profiles.ts` for tenant_id values when needed; reuse `Opportunity.id` shape from `test/fixtures/crm360/opportunities.ts` (P222).

    6. After fixture creation, verify import shape with a sanity test: run `vitest run test/sales/wave-0/`. Wave 0 guards still pass; fixture imports do not throw. Add inline check at end of `index.ts` not necessary (barrel file).
  </action>
  <acceptance_criteria>
    - File `test/sales/wave-0/f-id-range-guard.test.ts` exists; `vitest run test/sales/wave-0/f-id-range-guard.test.ts` exits 0
    - File `test/sales/wave-0/migration-range-guard.test.ts` exists; `vitest run test/sales/wave-0/migration-range-guard.test.ts` exits 0
    - File `test/sales/wave-0/baseline-a1-a2.test.ts` exists; `vitest run test/sales/wave-0/baseline-a1-a2.test.ts` exits 0
    - Directory `test/fixtures/sales/` exists with all 14 files: battlecard.ts, objection-library.ts, objection-record.ts, proof-pack.ts, deal-brief.ts, deal-room.ts, deal-room-artifact.ts, deal-room-view.ts, proposal-support.ts, quote.ts, winloss-record.ts, handoff-record.ts, deal-health-signal.ts, index.ts
    - `grep -l "export function battlecardApproved" test/fixtures/sales/battlecard.ts` returns the file
    - `grep -l "export function proofPackApproved" test/fixtures/sales/proof-pack.ts` returns the file
    - `grep -l "export function dealBriefAutoDrafted" test/fixtures/sales/deal-brief.ts` returns the file
    - `grep -l "export function dealRoomLive" test/fixtures/sales/deal-room.ts` returns the file
    - `grep -l "export function quoteSent\|export function quoteWithPlaceholderPricing" test/fixtures/sales/quote.ts` returns the file
    - `grep -l "export function winlossTombstoned" test/fixtures/sales/winloss-record.ts` returns the file
    - `grep -l "export function handoffMissedSla" test/fixtures/sales/handoff-record.ts` returns the file
    - `grep -l "export function dealHealthAtRisk" test/fixtures/sales/deal-health-signal.ts` returns the file
    - `grep -E "export \\* from './battlecard'" test/fixtures/sales/index.ts` returns the line
    - `grep -E "export \\* from './deal-health-signal'" test/fixtures/sales/index.ts` returns the line
    - `npm ls vitest @playwright/test json-logic-js xxhash-wasm chromatic` shows all 5 installed (no new deps)
    - `vitest run test/sales/wave-0/` exits 0 (3 guards pass; no F-163..F-180 nor 146..158_*.sql exist yet)
    - 226-VALIDATION.md row added: Task ID `226-01-01`, Plan `01`, Wave `1`, Requirement `QA-01,QA-02`, Test Type `infra`, Automated Command `vitest run test/sales/wave-0/`, File Exists `✅`, Status `⬜ pending`
  </acceptance_criteria>
  <verify>
    <automated>vitest run test/sales/wave-0/</automated>
  </verify>
  <done>F-ID + migration range guards green; 13 sales fixture factories importable from test/fixtures/sales/index.ts; 226-VALIDATION.md row 226-01-01 populated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrations 146-155 — 15 sales tables + RLS + CHECK constraints + lib/markos/sales/contracts.ts enums</name>
  <read_first>
    - .planning/phases/226-sales-enablement-deal-execution/226-RESEARCH.md §Schema Sketches lines 229-686 (15 table DDLs — verbatim source)
    - .planning/phases/226-sales-enablement-deal-execution/226-CONTEXT.md D-01..D-10 (object model decisions); D-21..D-24 (Quote-as-Snapshot immutability); D-29..D-33 (DealRoom share security); D-40..D-42 (WinLoss + cdp_events); D-54..D-58 (security/tombstone); D-59..D-60 (F-ID/migration allocation)
    - .planning/phases/226-sales-enablement-deal-execution/226-RESEARCH.md §Common Pitfalls lines 979-1045 (Pitfall 6 polymorphic FK pattern; Pitfall 11 tombstone preserve-row pattern)
    - .planning/phases/224-conversion-launch-workspace/224-01-PLAN.md (P224 D-14 polymorphic CHECK pattern reference — copy verbatim into deal_room_artifacts)
    - .planning/phases/222-crm-timeline-commercial-memory-workspace/222-CONTEXT.md (opportunities + buying_committee_members FK target table names — for FK declarations as deferred comments)
    - .planning/phases/221-cdp-identity-audience-consent-substrate/221-CONTEXT.md D-08 (cdp_events FK reference for source_event_ref); D-24 (tombstone propagation pattern)
    - .planning/phases/205-pricing-engine-foundation-billing-readiness/205-CONTEXT.md (PricingRecommendation FK target; pricing_recommendation_id reference)
    - .planning/phases/209-evidence-research-and-claim-safety/209-CONTEXT.md (EvidenceMap claim_id FK target)
    - obsidian/reference/Database Schema.md (canonical migration history; verify 146..155 sequential)
    - supabase/migrations/82_markos_audit_log_hash_chain.sql (markos_audit_log shape; P226 adds 'sales' domain)
    - .planning/phases/225-analytics-attribution-narrative-intelligence/225-01-PLAN.md Task 2 (migration writing pattern reference)
  </read_first>
  <behavior>
    - Test 1 (schema/migrations.test.ts): all 10 migration files exist; running them in order against a fresh test DB produces 15 expected tables (battlecards, battlecard_versions, objection_libraries, objection_library_entries, objection_records, proof_packs, proof_pack_versions, deal_briefs, deal_rooms, deal_room_artifacts, deal_room_views, proposal_supports, quotes, winloss_records, handoff_records, deal_health_signals); each has RLS enabled per `pg_class.relrowsecurity = true`.
    - Test 2 (schema/share-link-token-unique.test.ts): INSERT two deal_rooms rows with the same share_link_token → second INSERT raises unique_violation (code 23505).
    - Test 3 (schema/quote-snapshot-not-empty.test.ts): INSERT quotes row with `pricing_recommendation_snapshot = '{}'::jsonb` → CHECK violation `quotes_snapshot_not_empty`. INSERT with valid non-empty JSONB → succeeds.
    - Test 4 (schema/handoff-from-to-role.test.ts): INSERT handoff_records with `from_role = 'deal_owner'` AND `to_role = 'deal_owner'` → CHECK violation. INSERT with distinct roles → succeeds.
    - Test 5 (schema/polymorphic-artifact-check.test.ts): INSERT deal_room_artifacts with `artifact_kind='proof_pack'` AND `artifact_target_kind='quote'` → CHECK violation. INSERT with matched pair → succeeds. INSERT with `artifact_kind='custom_html'` AND any `artifact_target_kind` → succeeds (bypass branch).
    - Test 6 (rls/rls-suite-15-tables.test.ts): for each of the 15 tables, write tenant-A row with `app.active_tenant_id` set to tenant A, attempt tenant-B SELECT → 0 rows (cross-tenant denial proven). Note: deal_room_views has a special SELECT policy (must JOIN through deal_rooms.tenant_id) — verify cross-tenant SELECT through deal_rooms returns 0.
    - Test 7 (lib/markos/sales/contracts.ts existence): module imports without runtime error; 16 enum constants are exported with the exact value lists declared in DDL CHECK constraints.
  </behavior>
  <action>
    1. Create `supabase/migrations/146_sales_battlecards.sql` containing the verbatim DDL from 226-RESEARCH.md lines 232-281 (battlecards + battlecard_versions). Add comment header `-- Phase 226 — Sales battlecards (D-01); EvidenceMap claim TTL inheritance (D-17) consumed by 226-02 freshness auditor.` Set `SET search_path TO public;` at file top. Verify CHECK constraints encoded:
       - `status` enum: ('draft','approved','archived')
       - `freshness_status` enum: ('fresh','stale_claims','stale_competitor','retired')
       Include both indexes from research lines 260-261.

    2. Create `supabase/migrations/147_sales_objections.sql` containing verbatim DDL from 226-RESEARCH.md lines 286-351 (objection_libraries + objection_library_entries + objection_records). CHECK constraints:
       - objection_libraries.status: ('draft','approved','archived')
       - objection_library_entries.category: ('price','feature','timing','security','integration','support','contract','custom')
       - objection_library_entries.freshness_status: ('fresh','stale_claims','retired')
       - objection_library_entries.status: ('draft','approved','archived')
       - objection_records.status: ('open','addressed','deferred','resolved')
       Include all 3 indexes (entries.library_id+status, entries.tenant+category WHERE approved, records.opp+status).

    3. Create `supabase/migrations/148_sales_proof_packs.sql` containing verbatim DDL from 226-RESEARCH.md lines 356-401 (proof_packs + proof_pack_versions). CHECK constraints:
       - audience_type: ('executive','marketing_lead','finance','security','technical','customer_success')
       - approval_state: ('draft','pending_approval','approved','retired')
       - freshness_status: ('fresh','stale_claims','retired')
       Include both partial indexes from research lines 385-386.

    4. Create `supabase/migrations/149_sales_deal_briefs.sql` containing verbatim DDL from 226-RESEARCH.md lines 406-438 (deal_briefs). CHECK constraints:
       - generation_kind: ('auto_drafted','operator_created','regenerated_on_handoff')
       - status: ('draft','pending_approval','approved','archived')
       Include both indexes (opp+status, tenant+status+created_at DESC).

    5. Create `supabase/migrations/150_sales_deal_rooms.sql` containing verbatim DDL from 226-RESEARCH.md lines 442-525 (deal_rooms + deal_room_artifacts + deal_room_views).
       - deal_rooms CHECK status: ('draft','live','closed')
       - deal_rooms UNIQUE share_link_token (named index `idx_deal_rooms_token`)
       - deal_room_artifacts CHECK status: ('visible','hidden')
       - **deal_room_artifacts polymorphic CHECK constraint** (carries P224 D-14 LaunchSurface pattern verbatim — see research lines 484-493):
         ```sql
         CHECK (
           (artifact_kind = 'proof_pack'       AND artifact_target_kind = 'proof_pack') OR
           (artifact_kind = 'deal_brief'       AND artifact_target_kind = 'deal_brief') OR
           (artifact_kind = 'battlecard'       AND artifact_target_kind = 'battlecard') OR
           (artifact_kind = 'quote'            AND artifact_target_kind = 'quote') OR
           (artifact_kind = 'proposal_support' AND artifact_target_kind = 'proposal_support') OR
           (artifact_kind IN ('case_study','video','custom_html'))
         )
         ```
       - deal_room_views: dual RLS policies — tenant-scoped SELECT (joining through deal_rooms tenant_id), public unconditional INSERT (`WITH CHECK (true)`) per research line 522.

    6. Create `supabase/migrations/151_sales_proposal_supports.sql` containing verbatim DDL from 226-RESEARCH.md lines 530-557 (proposal_supports). CHECK status: ('draft','pending_approval','approved','sent','accepted','rejected','expired'). Index (opp+status).

    7. Create `supabase/migrations/152_sales_quotes.sql` containing verbatim DDL from 226-RESEARCH.md lines 560-585 (quotes).
       - CHECK status: ('draft','sent','accepted','rejected','expired','superseded_by')
       - **CONSTRAINT quotes_snapshot_not_empty CHECK (pricing_recommendation_snapshot != '{}'::jsonb)** — verbatim per research line 576
       - Self-FK: `superseded_by uuid REFERENCES quotes(quote_id)`
       - Indexes: (proposal_id, status), (status, valid_until) WHERE status IN ('sent','draft')
       Add comment block above the CONSTRAINT line: `-- Quote-as-Snapshot immutability (D-21..D-24); pricing_recommendation_snapshot is FROZEN once quote.status transitions to 'sent'. Application layer enforces immutability via WHERE clause guards in 226-04 quotes/quote.ts; this CHECK only ensures the snapshot is non-empty at all times.`

    8. Create `supabase/migrations/153_sales_winloss_records.sql` containing verbatim DDL from 226-RESEARCH.md lines 590-621 (winloss_records).
       - CHECK outcome: ('won','lost','no_decision','ghosted','postponed') — 5 values
       - CHECK primary_reason: ('price','feature','timing','competitor','champion_left','budget_freeze','internal_priorities','security_compliance','integration_gap','support_concern','contract_terms','no_decision','custom') — 13 values
       - **tombstoned BOOLEAN DEFAULT false, tombstoned_at timestamptz NULLABLE** per research lines 612-613 (D-56 preserves row, scrubs PII columns champion_id/decision_maker_id/free_text_notes only)
       - Indexes: (opportunity_id), (tenant_id, outcome, recorded_at DESC)

    9. Create `supabase/migrations/154_sales_handoff_records.sql` containing verbatim DDL from 226-RESEARCH.md lines 626-656 (handoff_records).
       - CHECK from_role: ('marketing_owner','deal_owner','cs_owner')
       - CHECK to_role: ('marketing_owner','deal_owner','cs_owner')
       - **CHECK (from_role != to_role)** per research line 646 (D-09 + D-34 invariant)
       - Indexes: (opportunity_id), (tenant_id, handed_off_at) WHERE acknowledged_at IS NULL (SLA cron scan)

    10. Create `supabase/migrations/155_sales_deal_health_signals.sql` containing verbatim DDL from 226-RESEARCH.md lines 661-685 (deal_health_signals).
        - CHECK deal_health_score BETWEEN 0 AND 100
        - UNIQUE (opportunity_id, period_start, period_end)
        - Indexes: (opportunity_id, computed_at DESC), (tenant_id, deal_health_score) WHERE deal_health_score < 40 (D-39 at-risk threshold scan)

    11. Create `lib/markos/sales/contracts.ts` exporting all 16 enum lists used by tests + UI:
        ```typescript
        export const BATTLECARD_STATUSES = ['draft','approved','archived'] as const;
        export const BATTLECARD_FRESHNESS_STATUSES = ['fresh','stale_claims','stale_competitor','retired'] as const;
        export const OBJECTION_CATEGORIES = ['price','feature','timing','security','integration','support','contract','custom'] as const;
        export const OBJECTION_RECORD_STATUSES = ['open','addressed','deferred','resolved'] as const;
        export const PROOF_PACK_AUDIENCE_TYPES = ['executive','marketing_lead','finance','security','technical','customer_success'] as const;
        export const PROOF_PACK_APPROVAL_STATES = ['draft','pending_approval','approved','retired'] as const;
        export const PROOF_PACK_FRESHNESS_STATUSES = ['fresh','stale_claims','retired'] as const;
        export const DEAL_BRIEF_GENERATION_KINDS = ['auto_drafted','operator_created','regenerated_on_handoff'] as const;
        export const DEAL_BRIEF_STATUSES = ['draft','pending_approval','approved','archived'] as const;
        export const DEAL_ROOM_STATUSES = ['draft','live','closed'] as const;
        export const DEAL_ROOM_ARTIFACT_KINDS = ['proof_pack','deal_brief','battlecard','quote','proposal_support','case_study','video','custom_html'] as const;
        export const PROPOSAL_STATUSES = ['draft','pending_approval','approved','sent','accepted','rejected','expired'] as const;
        export const QUOTE_STATUSES = ['draft','sent','accepted','rejected','expired','superseded_by'] as const;
        export const WINLOSS_OUTCOMES = ['won','lost','no_decision','ghosted','postponed'] as const;
        export const WINLOSS_PRIMARY_REASONS = ['price','feature','timing','competitor','champion_left','budget_freeze','internal_priorities','security_compliance','integration_gap','support_concern','contract_terms','no_decision','custom'] as const;
        export const HANDOFF_ROLES = ['marketing_owner','deal_owner','cs_owner'] as const;
        // Type exports for Row types
        export type BattlecardStatus = typeof BATTLECARD_STATUSES[number];
        export type BattlecardFreshnessStatus = typeof BATTLECARD_FRESHNESS_STATUSES[number];
        export type ObjectionCategory = typeof OBJECTION_CATEGORIES[number];
        export type ObjectionRecordStatus = typeof OBJECTION_RECORD_STATUSES[number];
        export type ProofPackAudienceType = typeof PROOF_PACK_AUDIENCE_TYPES[number];
        export type ProofPackApprovalState = typeof PROOF_PACK_APPROVAL_STATES[number];
        export type ProofPackFreshnessStatus = typeof PROOF_PACK_FRESHNESS_STATUSES[number];
        export type DealBriefGenerationKind = typeof DEAL_BRIEF_GENERATION_KINDS[number];
        export type DealBriefStatus = typeof DEAL_BRIEF_STATUSES[number];
        export type DealRoomStatus = typeof DEAL_ROOM_STATUSES[number];
        export type DealRoomArtifactKind = typeof DEAL_ROOM_ARTIFACT_KINDS[number];
        export type ProposalStatus = typeof PROPOSAL_STATUSES[number];
        export type QuoteStatus = typeof QUOTE_STATUSES[number];
        export type WinlossOutcome = typeof WINLOSS_OUTCOMES[number];
        export type WinlossPrimaryReason = typeof WINLOSS_PRIMARY_REASONS[number];
        export type HandoffRole = typeof HANDOFF_ROLES[number];
        ```

    12. Create `lib/markos/sales/index.ts` as barrel: `export * from './contracts';` (more sub-modules added in 226-02..06).

    13. Run all 10 migrations against the test DB. Create test files:
        - `test/sales/schema/migrations.test.ts` — verify all 15 tables exist; loop list and assert `pg_class.relrowsecurity = true` for each.
        - `test/sales/schema/share-link-token-unique.test.ts` — INSERT two deal_rooms with same `share_link_token` → second INSERT fails with code 23505.
        - `test/sales/schema/quote-snapshot-not-empty.test.ts` — INSERT quote with `'{}'::jsonb` → fails with `quotes_snapshot_not_empty`. With `'{"plan":"team"}'::jsonb` → succeeds.
        - `test/sales/schema/handoff-from-to-role.test.ts` — INSERT same role both sides → fails with CHECK violation. Distinct roles → succeeds.
        - `test/sales/schema/polymorphic-artifact-check.test.ts` — 3 cases:
            (a) `artifact_kind='proof_pack', artifact_target_kind='proof_pack'` → success
            (b) `artifact_kind='proof_pack', artifact_target_kind='quote'` → CHECK fails
            (c) `artifact_kind='custom_html', artifact_target_kind='anything_goes'` → success (bypass branch)
        - `test/sales/rls/rls-suite-15-tables.test.ts` — array of 15 table names; for each: SET `app.active_tenant_id = '<tenant-A>'`; INSERT a row using fixture; SET `app.active_tenant_id = '<tenant-B>'`; SELECT → expect 0 rows. Special handling for deal_room_views: SELECT joins through deal_rooms; both must deny.
  </action>
  <acceptance_criteria>
    - Files exist: `supabase/migrations/146_sales_battlecards.sql` through `155_sales_deal_health_signals.sql` (10 migration files)
    - `grep -c "CREATE TABLE" supabase/migrations/146_sales_battlecards.sql` returns 2 (battlecards + battlecard_versions)
    - `grep -c "CREATE TABLE" supabase/migrations/147_sales_objections.sql` returns 3 (objection_libraries + objection_library_entries + objection_records)
    - `grep -c "CREATE TABLE" supabase/migrations/148_sales_proof_packs.sql` returns 2 (proof_packs + proof_pack_versions)
    - `grep -c "CREATE TABLE" supabase/migrations/149_sales_deal_briefs.sql` returns 1
    - `grep -c "CREATE TABLE" supabase/migrations/150_sales_deal_rooms.sql` returns 3 (deal_rooms + deal_room_artifacts + deal_room_views)
    - `grep -c "CREATE TABLE" supabase/migrations/151_sales_proposal_supports.sql` returns 1
    - `grep -c "CREATE TABLE" supabase/migrations/152_sales_quotes.sql` returns 1
    - `grep -c "CREATE TABLE" supabase/migrations/153_sales_winloss_records.sql` returns 1
    - `grep -c "CREATE TABLE" supabase/migrations/154_sales_handoff_records.sql` returns 1
    - `grep -c "CREATE TABLE" supabase/migrations/155_sales_deal_health_signals.sql` returns 1
    - Total CREATE TABLE = 16 (one is battlecard_versions audit table; total is 15 SOR tables + 1 versions audit but battlecard_versions counts as a sales table → 15 distinct sales tables; the 16th `CREATE TABLE` count is proof_pack_versions which is the second versions audit table). Verify total: `grep -c "CREATE TABLE" supabase/migrations/146_*.sql 147_*.sql 148_*.sql 149_*.sql 150_*.sql 151_*.sql 152_*.sql 153_*.sql 154_*.sql 155_*.sql` returns 16.
    - `grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/14[6-9]_*.sql 15[0-5]_*.sql` returns >= 16 (one ALTER TABLE per table)
    - `grep -E "CONSTRAINT quotes_snapshot_not_empty CHECK" supabase/migrations/152_*.sql` returns the constraint line
    - `grep -E "CHECK \\(from_role != to_role\\)" supabase/migrations/154_*.sql` returns the constraint line
    - `grep -E "CHECK \\(deal_health_score BETWEEN 0 AND 100\\)" supabase/migrations/155_*.sql` returns the constraint line
    - `grep -E "deal_room_views_insert" supabase/migrations/150_*.sql` returns the public-insert RLS policy (D-29 honeypot tracking)
    - `grep -E "FOR INSERT WITH CHECK \\(true\\)" supabase/migrations/150_*.sql` returns the unconditional public insert (deal_room_views)
    - `grep -E "artifact_kind = 'custom_html'" supabase/migrations/150_*.sql` returns the bypass branch of polymorphic CHECK
    - `grep -E "tombstoned\\s+boolean\\s+NOT NULL\\s+DEFAULT\\s+false" supabase/migrations/153_*.sql` returns the column line (D-56)
    - File `lib/markos/sales/contracts.ts` exists; `grep -c "^export const " lib/markos/sales/contracts.ts` returns 16
    - `grep -E "BATTLECARD_FRESHNESS_STATUSES.*'fresh','stale_claims','stale_competitor','retired'" lib/markos/sales/contracts.ts` returns the line
    - `grep -E "DEAL_ROOM_ARTIFACT_KINDS.*'proof_pack'.*'custom_html'" lib/markos/sales/contracts.ts` returns the line (8 values present)
    - File `lib/markos/sales/index.ts` exists with `export * from './contracts';`
    - `vitest run test/sales/schema/ test/sales/rls/` exits 0 (7 test files green)
    - 226-VALIDATION.md rows added: 226-01-02 (migrations green), 226-01-03 (CHECK constraints green), 226-01-04 (RLS suite green)
  </acceptance_criteria>
  <verify>
    <automated>vitest run test/sales/schema/ test/sales/rls/</automated>
  </verify>
  <done>10 migrations + 15 sales tables + RLS + CHECK constraints (status enums, freshness enums, share_link_token UNIQUE, quote-snapshot-not-empty, handoff-roles-distinct, polymorphic artifact CHECK) shipped; lib/markos/sales/contracts.ts publishes 16 enum constants + 16 type aliases consumed by 226-02..07.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: F-163..F-172 contracts + flow-registry sync + 226-VALIDATION.md per-task verification map populated</name>
  <read_first>
    - obsidian/reference/Contracts Registry.md (canonical contract YAML schema)
    - obsidian/reference/HTTP Layer.md (/v1/sales/* convention; public /share/dr/{token} unauthenticated path)
    - contracts/F-146-launch-execute-runbook-v1.yaml (most recent reference contract pattern — copy as F-163 template)
    - contracts/F-147-analytics-metric-definition-v1.yaml (P225 plan reference contract — most-similar shape for read-write CRUD)
    - contracts/flow-registry.json (current flat list of flows[].flow_id entries)
    - .planning/phases/226-sales-enablement-deal-execution/226-RESEARCH.md §F-ID + Migration Allocation lines 152-189 (F-163..F-180 mapping)
    - .planning/phases/226-sales-enablement-deal-execution/226-CONTEXT.md D-43 (full /v1/sales/* route list); D-44 (8 MCP tools, defined in 226-06); D-45 (approval-package on writes)
    - .planning/phases/226-sales-enablement-deal-execution/226-VALIDATION.md (per-task map template at line 47)
    - lib/markos/sales/contracts.ts (16 enum constants from Task 2 — referenced in YAML schemas)
  </read_first>
  <behavior>
    - Each F-163..F-172 contract YAML follows the same shape as F-146/F-147: flow_id, name, version, owner, phase, status, paths (HTTP method + URL + request_2xx + response_2xx schemas), error_codes, observability_tags, audit_emit (on_write: true for state-mutating flows).
    - flow-registry.json gets 10 new entries appended; F-163..F-172 are sequential; no overlap with F-106..F-162 (verified by Wave 0 guard test).
    - 226-VALIDATION.md per-task verification map populated for ALL 7 plans (one row per task across 226-01..226-07; expect ~22 rows total). Status column reflects what each task is expected to prove. nyquist_compliant flag flips to true. wave_0_complete flag flips to true.
  </behavior>
  <action>
    1. For each F-ID in F-163..F-172, create `contracts/F-{ID}-{name}-v1.yaml` per schema:

       **F-163 (sales-battlecard-v1)** — paths:
         - `GET /v1/sales/battlecards` filters by competitor_name/freshness_status/status → 200 `{ battlecards: Battlecard[] }`
         - `POST /v1/sales/battlecards` body `{ competitor_name, summary, ... }` → 201 with new Battlecard (status='draft' default)
         - `GET /v1/sales/battlecards/{battlecard_id}` → 200 with Battlecard
         - `PATCH /v1/sales/battlecards/{battlecard_id}` → 200 with updated Battlecard
         - `DELETE /v1/sales/battlecards/{battlecard_id}` → 204 (soft delete via status='archived')
         - `POST /v1/sales/battlecards/{battlecard_id}/verify` → 200 (operator attestation; sets last_verified_at = now; recompute freshness_status)
         - `POST /v1/sales/battlecards/{battlecard_id}/stale` → 200 (manual mark; sets freshness_status='stale_competitor')
         - `POST /v1/sales/battlecards/{battlecard_id}/approve` → 200 (class-based approval: internal_auto per D-25 — auto-approves for sales_owner role)
         Schema Battlecard mirrors DDL columns from migration 146. Enum values reference lib/markos/sales/contracts.ts BATTLECARD_STATUSES + BATTLECARD_FRESHNESS_STATUSES.
         audit_emit: { on_write: true, domain: 'sales' }

       **F-164 (sales-battlecard-version-v1)** — read-only:
         - `GET /v1/sales/battlecards/{battlecard_id}/versions` → 200 `{ versions: BattlecardVersion[] }`
         audit_emit: { on_write: false } (read-only contract)

       **F-165 (sales-objection-library-v1)**:
         - `GET /v1/sales/objections/libraries` → 200 `{ libraries: ObjectionLibrary[] }`
         - `POST /v1/sales/objections/libraries` body `{ name, owner_user_id }` → 201
         - `GET /v1/sales/objections/libraries/{library_id}/entries` → 200 `{ entries: ObjectionEntry[] }`
         - `POST /v1/sales/objections/libraries/{library_id}/entries` body `{ objection_text, response_text, category, ... }` → 201
         - `PATCH /v1/sales/objections/libraries/{library_id}/entries/{entry_id}` → 200
         OBJECTION_CATEGORIES enum referenced from contracts.ts.
         audit_emit: { on_write: true, domain: 'sales' }

       **F-166 (sales-objection-record-v1)**:
         - `GET /v1/sales/objections/records?opportunity_id=` → 200
         - `POST /v1/sales/objections/records` body `{ opportunity_id, objection_text, raised_by_committee_member_id? }` → 201
         - `POST /v1/sales/objections/records/{record_id}/address` body `{ response_text, evidence_refs }` → 200 (status → addressed)
         - `POST /v1/sales/objections/records/{record_id}/defer` body `{ defer_reason }` → 200 (status → deferred)
         - `POST /v1/sales/objections/records/{record_id}/resolve` → 200 (status → resolved)
         OBJECTION_RECORD_STATUSES enum referenced.
         audit_emit: { on_write: true, domain: 'sales' }

       **F-167 (sales-proof-pack-v1)**:
         - `GET /v1/sales/proof-packs` filters by audience_type/approval_state/freshness_status → 200
         - `POST /v1/sales/proof-packs` body `{ audience_type, name, summary, evidence_refs, pricing_refs, ... }` → 201
         - `GET /v1/sales/proof-packs/{proof_pack_id}` → 200
         - `PATCH /v1/sales/proof-packs/{proof_pack_id}` → 200
         - `POST /v1/sales/proof-packs/{proof_pack_id}/approve` → 200 (class-based approval: customer_facing_required per D-26 — routes through P208 Approval Inbox + P105 createApprovalPackage; on approve, snapshot_at = now and version increments)
         - `POST /v1/sales/proof-packs/{proof_pack_id}/retire` → 200 (status='retired')
         - `POST /v1/sales/proof-packs/{proof_pack_id}/refresh-claims` → 200 (recompute freshness_status from EvidenceMap claim TTL; D-12)
         PROOF_PACK_AUDIENCE_TYPES + PROOF_PACK_APPROVAL_STATES + PROOF_PACK_FRESHNESS_STATUSES enums referenced.
         audit_emit: { on_write: true, domain: 'sales' }

       **F-168 (sales-proof-pack-version-v1)** — read-only:
         - `GET /v1/sales/proof-packs/{proof_pack_id}/versions` → 200
         audit_emit: { on_write: false }

       **F-169 (sales-deal-brief-v1)**:
         - `GET /v1/sales/deal-briefs?opportunity_id=` → 200 (latest version per opp by default; ?include_versions=true for full history)
         - `POST /v1/sales/deal-briefs` body `{ opportunity_id, generation_kind, ... }` → 201 (operator-created path)
         - `POST /v1/sales/deal-briefs/{deal_brief_id}/regenerate` body `{ trigger ∈ stage_change/handoff/manual }` → 202 `{ run_id }` (AgentRun-wrapped per D-14)
         - `POST /v1/sales/deal-briefs/{deal_brief_id}/approve` → 200 (class-based: customer_facing_required if customer-visible variant per D-26; internal_auto otherwise per D-25)
         - `POST /v1/sales/deal-briefs/{deal_brief_id}/archive` → 200
         DEAL_BRIEF_GENERATION_KINDS + DEAL_BRIEF_STATUSES enums referenced.
         audit_emit: { on_write: true, domain: 'sales' }

       **F-170 (sales-deal-room-v1)**:
         - `GET /v1/sales/deal-rooms` filters by status/opportunity_id → 200
         - `POST /v1/sales/deal-rooms` body `{ opportunity_id, owner_user_id, expires_at }` → 201 (status='draft', share_link_token generated via HMAC; D-54)
         - `GET /v1/sales/deal-rooms/{deal_room_id}` → 200 (with embedded artifacts list ordered by display_order)
         - `PATCH /v1/sales/deal-rooms/{deal_room_id}` → 200
         - `POST /v1/sales/deal-rooms/{deal_room_id}/enable-share` → 200 (class-based: customer_facing_required per D-26; on approve, status='live' + public_share_enabled=true)
         - `POST /v1/sales/deal-rooms/{deal_room_id}/close` → 200 (status='closed', closed_at = now)
         - `POST /v1/sales/deal-rooms/{deal_room_id}/artifacts` body `{ artifact_kind, artifact_target_kind, artifact_target_id, display_order? }` → 201 (polymorphic CHECK enforced server-side; battlecard freshness_status != 'fresh' → 422 per D-20)
         - `DELETE /v1/sales/deal-rooms/{deal_room_id}/artifacts/{artifact_id}` → 204
         DEAL_ROOM_STATUSES + DEAL_ROOM_ARTIFACT_KINDS enums referenced.
         audit_emit: { on_write: true, domain: 'sales' }
         Note: share_link_token field in any response payload is masked to `dr_*****` (D-57).

       **F-171 (sales-deal-room-view-v1)** — read-only operator view:
         - `GET /v1/sales/deal-rooms/{deal_room_id}/views?from=&to=` → 200 (operator analytics; no PII in response — ip_hash + user_agent_hash are returned as opaque strings)
         audit_emit: { on_write: false }

       **F-172 (sales-proposal-support-v1)**:
         - `GET /v1/sales/proposals?opportunity_id=&status=` → 200
         - `POST /v1/sales/proposals` body `{ opportunity_id, deal_brief_id?, evidence_pack_id?, executive_summary, customer_success_plan, terms_summary, valid_until }` → 201
         - `GET /v1/sales/proposals/{proposal_id}` → 200
         - `PATCH /v1/sales/proposals/{proposal_id}` → 200 (only when status='draft')
         - `POST /v1/sales/proposals/{proposal_id}/approve` → 200 (class-based: customer_facing_required per D-26)
         - `POST /v1/sales/proposals/{proposal_id}/send` body `{ sent_to_email[] }` → 200 (status → sent; content classifier scan first per D-27; pricing variables MUST resolve via P205 OR `{{MARKOS_PRICING_ENGINE_PENDING}}` placeholder)
         - `POST /v1/sales/proposals/{proposal_id}/mark-accepted` → 200
         - `POST /v1/sales/proposals/{proposal_id}/mark-rejected` body `{ reject_reason }` → 200
         PROPOSAL_STATUSES enum referenced.
         audit_emit: { on_write: true, domain: 'sales' }

       Each contract YAML has these top-level fields:
       ```yaml
       flow_id: F-163
       name: sales-battlecard-v1
       version: 1
       owner: sales
       phase: 226
       status: planned
       paths:
         - method: GET
           url: /v1/sales/battlecards
           summary: List battlecards with freshness status (tenant-scoped)
           query_params:
             - { name: competitor_name, type: string, optional: true }
             - { name: freshness_status, type: string, enum: [fresh, stale_claims, stale_competitor, retired], optional: true }
             - { name: status, type: string, enum: [draft, approved, archived], optional: true }
           response_2xx:
             schema: { type: object, required: [battlecards], properties: { battlecards: { type: array, items: { $ref: '#/schemas/Battlecard' } } } }
       schemas:
         Battlecard:
           type: object
           required: [battlecard_id, tenant_id, competitor_name, summary, status, freshness_status, version]
           properties:
             battlecard_id: { type: string, format: uuid }
             tenant_id: { type: string, format: uuid }
             competitor_name: { type: string }
             competitor_profile_id: { type: [string, "null"], format: uuid }
             summary: { type: string }
             strengths_to_acknowledge: { type: array, items: { type: string } }
             # ... etc per DDL columns from migration 146
             status: { type: string, enum: [draft, approved, archived] }
             freshness_status: { type: string, enum: [fresh, stale_claims, stale_competitor, retired] }
             approved_by: { type: [string, "null"], format: uuid }
             approved_at: { type: [string, "null"], format: date-time }
             created_at: { type: string, format: date-time }
             updated_at: { type: string, format: date-time }
       error_codes:
         - 400: invalid_request_shape
         - 401: missing_tenant_context
         - 403: cross_tenant_denied
         - 404: battlecard_not_found
         - 422: stale_claims_block_render  # D-13 / D-20 fail-closed render
       observability_tags: [sales, battlecard, freshness]
       audit_emit:
         on_write: true
         domain: sales
       ```

       Apply the same pattern to F-164..F-172, varying paths/schemas per contract domain. All contracts share `phase: 226`, `owner: sales`, `audit_emit.domain: sales`.

    2. Update `contracts/flow-registry.json` to append 10 entries to flows[]:
       ```json
       { "flow_id": "F-163", "name": "sales-battlecard-v1",        "phase": 226, "owner": "sales", "status": "planned", "flow_type": "entity-crud" },
       { "flow_id": "F-164", "name": "sales-battlecard-version-v1", "phase": 226, "owner": "sales", "status": "planned", "flow_type": "audit-read" },
       { "flow_id": "F-165", "name": "sales-objection-library-v1", "phase": 226, "owner": "sales", "status": "planned", "flow_type": "entity-crud" },
       { "flow_id": "F-166", "name": "sales-objection-record-v1",  "phase": 226, "owner": "sales", "status": "planned", "flow_type": "transition" },
       { "flow_id": "F-167", "name": "sales-proof-pack-v1",        "phase": 226, "owner": "sales", "status": "planned", "flow_type": "approval-aware" },
       { "flow_id": "F-168", "name": "sales-proof-pack-version-v1","phase": 226, "owner": "sales", "status": "planned", "flow_type": "audit-read" },
       { "flow_id": "F-169", "name": "sales-deal-brief-v1",        "phase": 226, "owner": "sales", "status": "planned", "flow_type": "approval-aware" },
       { "flow_id": "F-170", "name": "sales-deal-room-v1",         "phase": 226, "owner": "sales", "status": "planned", "flow_type": "approval-aware" },
       { "flow_id": "F-171", "name": "sales-deal-room-view-v1",    "phase": 226, "owner": "sales", "status": "planned", "flow_type": "analytics-read" },
       { "flow_id": "F-172", "name": "sales-proposal-support-v1",  "phase": 226, "owner": "sales", "status": "planned", "flow_type": "approval-aware" }
       ```
       Maintain flat array order after F-162 entry. Update flow-registry top-level `total_flows` count by +10 (N → N+10). Bump `generated_at` to current ISO.

    3. Update `.planning/phases/226-sales-enablement-deal-execution/226-VALIDATION.md` per-task verification map. Replace template row at line 49 with full per-task table (~22 rows). Schema:

       | Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
       |---------|------|------|-------------|-----------|-------------------|-------------|--------|
       | 226-01-01 | 01 | 1 | QA-01,QA-02 | infra | `vitest run test/sales/wave-0/` | ✅ | ⬜ pending |
       | 226-01-02 | 01 | 1 | SEN-01,QA-01,QA-02 | schema | `vitest run test/sales/schema/migrations.test.ts` | ✅ | ⬜ pending |
       | 226-01-03 | 01 | 1 | SEN-03,EVD-06,QA-02 | constraint | `vitest run test/sales/schema/share-link-token-unique.test.ts test/sales/schema/quote-snapshot-not-empty.test.ts test/sales/schema/handoff-from-to-role.test.ts test/sales/schema/polymorphic-artifact-check.test.ts` | ✅ | ⬜ pending |
       | 226-01-04 | 01 | 1 | QA-02 | rls | `vitest run test/sales/rls/rls-suite-15-tables.test.ts` | ✅ | ⬜ pending |
       | 226-01-05 | 01 | 1 | QA-01 | contract | `node scripts/contracts/validate.cjs F-163 F-164 F-165 F-166 F-167 F-168 F-169 F-170 F-171 F-172 && jq '[.flows[] | select(.flow_id | startswith(\"F-16\") or startswith(\"F-17\")) | .flow_id] | length' contracts/flow-registry.json` | ✅ | ⬜ pending |
       | 226-02-01 | 02 | 2 | SEN-01,SEN-03,EVD-02 | unit | `vitest run test/sales/battlecards/freshness-inheritance.test.ts` | ⬜ TBD | ⬜ pending |
       | 226-02-02 | 02 | 2 | SEN-01,SEN-03,EVD-02 | unit | `vitest run test/sales/proof-packs/snapshot-version.test.ts test/sales/proof-packs/render-fail-closed.test.ts` | ⬜ TBD | ⬜ pending |
       | 226-02-03 | 02 | 2 | SEN-01 | integration | `vitest run test/sales/objections/library-records.test.ts` | ⬜ TBD | ⬜ pending |
       | 226-03-01 | 03 | 2 | SEN-02,SEN-05 | unit | `vitest run test/sales/deal-briefs/auto-draft.test.ts test/sales/deal-briefs/debounce.test.ts` | ⬜ TBD | ⬜ pending |
       | 226-03-02 | 03 | 2 | SEN-04,SEN-05 | unit | `vitest run test/sales/handoffs/handoff-record.test.ts test/sales/handoffs/acknowledge-sla.test.ts` | ⬜ TBD | ⬜ pending |
       | 226-03-03 | 03 | 2 | SEN-04 | integration | `vitest run test/sales/health-signals/composite.test.ts test/sales/health-signals/cdp-events-emit.test.ts` | ⬜ TBD | ⬜ pending |
       | 226-04-01 | 04 | 3 | SEN-03,PRC-04,EVD-06 | unit | `vitest run test/sales/quotes/snapshot-immutable.test.ts test/sales/quotes/pricing-drift.test.ts` | ⬜ TBD | ⬜ pending |
       | 226-04-02 | 04 | 3 | SEN-03 | unit | `vitest run test/sales/proposals/state-machine.test.ts test/sales/approvals/class-based-approval.test.ts` | ⬜ TBD | ⬜ pending |
       | 226-04-03 | 04 | 3 | SEN-03 | integration | `vitest run test/sales/proposals/content-classifier.test.ts` | ⬜ TBD | ⬜ pending |
       | 226-05-01 | 05 | 3 | SEN-04,SEN-05 | unit | `vitest run test/sales/deal-rooms/share-link-hmac.test.ts test/sales/deal-rooms/expiry.test.ts` | ⬜ TBD | ⬜ pending |
       | 226-05-02 | 05 | 3 | SEN-04 | integration | `vitest run test/sales/deal-rooms/bot-rate-limit.test.ts` | ⬜ TBD | ⬜ pending |
       | 226-05-03 | 05 | 3 | SEN-04 | unit | `vitest run test/sales/winloss/required-on-transition.test.ts test/sales/winloss/attribution-link.test.ts test/sales/winloss/tombstone-cascade.test.ts` | ⬜ TBD | ⬜ pending |
       | 226-06-01 | 06 | 4 | SEN-01..05 | api | `vitest run test/sales/api/` | ⬜ TBD | ⬜ pending |
       | 226-06-02 | 06 | 4 | SEN-01..05 | mcp | `vitest run test/sales/mcp/` | ⬜ TBD | ⬜ pending |
       | 226-07-01 | 07 | 5 | SEN-01..05 | playwright | `npx playwright test --grep sales` | ⬜ TBD | ⬜ pending |
       | 226-07-02 | 07 | 5 | SEN-01..05 | chromatic | `npm run chromatic -- --only-changed test/chromatic/sales/` | ⬜ TBD | ⬜ pending |
       | 226-07-03 | 07 | 5 | QA-02 | rls | `vitest run test/sales/rls/rls-suite-full.test.ts` | ⬜ TBD | ⬜ pending |
       | 226-07-04 | 07 | 5 | ALL | regression | `vitest run test/regression/p100-p105.regression.test.ts test/regression/p221-p225.regression.test.ts && npm test` | ⬜ TBD | ⬜ pending |

       Replace the placeholder row at line 49 with this full table. Update front-matter `nyquist_compliant: true` and `wave_0_complete: true`.

    4. Sanity-check: run `vitest run test/sales/wave-0/ test/sales/schema/ test/sales/rls/`. All from Tasks 1+2 still green; flow-registry parsing in Wave 0 guard now sees F-163..F-172 entries → guard test should be UPDATED to verify ranges F-173..F-180 remain unclaimed (next-wave plans 226-02..05 will claim F-173..F-180). Update `test/sales/wave-0/f-id-range-guard.test.ts` regex from `/^F-1(6[3-9]|7[0-9]|80)$/` to `/^F-1(7[3-9]|80)$/` AND assert F-163..F-172 are present (10 entries) — guard now verifies P226-01 ceiling.
  </action>
  <acceptance_criteria>
    - Files exist: `contracts/F-163-sales-battlecard-v1.yaml` through `contracts/F-172-sales-proposal-support-v1.yaml` (10 files)
    - `grep -l "flow_id: F-163" contracts/F-163-*.yaml` returns the file
    - `grep -E "^paths:" contracts/F-163-*.yaml` returns the paths key
    - `grep -E "audit_emit:" contracts/F-163-*.yaml contracts/F-165-*.yaml contracts/F-166-*.yaml contracts/F-167-*.yaml contracts/F-169-*.yaml contracts/F-170-*.yaml contracts/F-172-*.yaml` returns audit_emit blocks (state-mutating contracts have on_write: true; F-164/F-168/F-171 are read-only)
    - `grep -E "domain: sales" contracts/F-16[3-9]-*.yaml contracts/F-17[0-2]-*.yaml` returns lines (all contracts have audit_emit.domain: sales)
    - `jq '.flows[] | select(.flow_id == "F-163" or .flow_id == "F-172")' contracts/flow-registry.json` returns both entries
    - `jq '[.flows[] | select(.flow_id | test("^F-1(6[3-9]|7[0-2])$"))] | length' contracts/flow-registry.json` returns 10
    - `jq '.total_flows' contracts/flow-registry.json` returns previous value + 10
    - 226-VALIDATION.md `grep -c "^| 226-" .planning/phases/226-sales-enablement-deal-execution/226-VALIDATION.md` returns >= 22 (one row per task across 7 plans)
    - 226-VALIDATION.md frontmatter `grep "nyquist_compliant: true" .planning/phases/226-sales-enablement-deal-execution/226-VALIDATION.md` returns 1 line
    - 226-VALIDATION.md frontmatter `grep "wave_0_complete: true" .planning/phases/226-sales-enablement-deal-execution/226-VALIDATION.md` returns 1 line
    - Re-run `vitest run test/sales/wave-0/ test/sales/schema/ test/sales/rls/` exits 0 (Wave 0 guards updated to verify F-173..F-180 unclaimed; rls-suite-15-tables green; baseline-a1-a2 green)
    - 226-VALIDATION.md row added: 226-01-05 (contracts + flow-registry green)
  </acceptance_criteria>
  <verify>
    <automated>vitest run test/sales/wave-0/ test/sales/schema/ test/sales/rls/ && node -e "const r=require('./contracts/flow-registry.json'); const ids=new Set(r.flows.map(f=>f.flow_id)); for(let i=163;i<=172;i++){const id='F-'+i; if(!ids.has(id)){console.error('missing',id); process.exit(1)}} console.log('F-163..F-172 registered (10 contracts)');"</automated>
  </verify>
  <done>10 contract YAMLs (F-163..F-172) + flow-registry sync + 226-VALIDATION.md per-task map populated for all 7 plans + nyquist_compliant + wave_0_complete flags on. Wave 1 complete; downstream plans (226-02..226-07) can now begin in parallel waves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → battlecards | Operator-edited risky_claims_to_avoid + ideal_positioning_angles JSONB fields cross tenant trust into render renderer |
| operator → quotes.pricing_recommendation_snapshot | Operator-triggered Quote send action freezes JSONB; immutability enforced via DB CHECK + application layer (D-21..D-24) |
| upstream phase → schema baseline | P225 contract baseline (F-162, migration 145) must hold before P226 writes |
| public → deal_room_views (write) | Public unauthenticated insert via /share/dr/{token} crosses untrusted boundary; RLS allows insert unconditionally; honeypot/BotID/rate-limit gates all in front (226-05 ships these) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-226-01-01 | Tampering | quotes.pricing_recommendation_snapshot | mitigate | DB-level CHECK constraint quotes_snapshot_not_empty enforces non-empty JSONB at all times; application layer enforces immutability via WHERE clause guards in 226-04 quotes/quote.ts (sendQuote freezes the snapshot when status transitions to 'sent'). Wave 1 ships only the table + CHECK; full enforcement lands in 226-04. |
| T-226-01-02 | Information Disclosure | All 15 new tables | mitigate | RLS enabled per migration; rls-suite-15-tables.test.ts proves cross-tenant denial; deal_room_views has tenant-scoped SELECT through deal_rooms join (denial proven both directly and via JOIN). |
| T-226-01-03 | Tampering | F-ID + migration baseline | mitigate | Wave 0 guards (Task 1) fail loudly if any F-163..F-180 or 146..158_*.sql pre-exists (Assumption A1/A2); baseline-a1-a2 test asserts P225 ceiling at F-162. |
| T-226-01-04 | Elevation of Privilege | deal_room_artifacts polymorphic FK | mitigate | DB-level CHECK constraint enforces per-kind alignment for 5 typed artifacts (proof_pack/deal_brief/battlecard/quote/proposal_support); 3 untyped artifacts (case_study/video/custom_html) bypass the alignment but still scoped via tenant_id RLS. polymorphic-artifact-check.test.ts proves both branches. |
| T-226-01-05 | Repudiation | All P226 mutations | mitigate | markos_audit_log domain='sales' attached to every mutation in later plans; Wave 1 ships only the schema (audit emission via lib/markos/sales/audit.ts ships in 226-02). audit_emit.on_write: true declared in 7 of 10 F-IDs (F-163, F-165, F-166, F-167, F-169, F-170, F-172); 3 are read-only (F-164, F-168, F-171). |
| T-226-01-06 | Spoofing | deal_rooms.share_link_token | mitigate | UNIQUE constraint blocks duplicate token persistence; HMAC signing + per-tenant rotation (D-54) ships in 226-05. Wave 1 only verifies UNIQUE collision behavior. |
| T-226-01-07 | Tampering | handoff_records role transition | mitigate | DB-level CHECK (from_role != to_role) enforced at insert time; application layer in 226-03 ensures handoff is always between distinct roles per D-09 + D-34. |
| T-226-01-08 | Information Disclosure | winloss_records champion_id + decision_maker_id | mitigate | tombstone propagation D-56: ON DELETE SET NULL on FK to buying_committee_members (Pitfall 11); record_id + outcome + primary_reason preserved for P212 LearningCandidate (D-41). Wave 1 ships only tombstoned + tombstoned_at columns; full scrub function lands in 226-05. |
</threat_model>

<verification>
- [ ] `vitest run test/sales/wave-0/` green (3 baseline guards: F-ID + migration + A1/A2)
- [ ] `vitest run test/sales/schema/` green (5 schema tests: migrations.test.ts + 4 CHECK constraint tests)
- [ ] `vitest run test/sales/rls/` green (cross-tenant denial on all 15 new tables)
- [ ] `jq '[.flows[] | select(.flow_id | test("^F-1(6[3-9]|7[0-2])$"))] | length' contracts/flow-registry.json` returns 10
- [ ] 226-VALIDATION.md per-task verification map populated; `nyquist_compliant: true` + `wave_0_complete: true`
- [ ] `lib/markos/sales/contracts.ts` exports 16 enum constants + 16 type aliases (verified via TS compile + `grep -c "^export const "` returns 16)
- [ ] Legacy regression: `npm test` and `vitest run test/cdp-* test/crm-* test/channels-* test/conversion-* test/analytics-*` (P221-P225) still green (additive phase guarantee)
</verification>

<success_criteria>
- 10 migrations (146..155) committed; 15 sales SOR tables exist with RLS + CHECK constraints
- Quote-as-Snapshot CHECK (snapshot != '{}'), share_link_token UNIQUE, handoff role-distinct CHECK, polymorphic artifact CHECK all enforced at DB level
- 13 fixture factories in test/fixtures/sales/ + index.ts barrel ship 24+ fixture variants spanning all 15 SOR tables
- 10 F-163..F-172 contracts registered in flow-registry.json with audit_emit.domain='sales'
- lib/markos/sales/contracts.ts exports 16 enum constants used by every later 226-NN plan; 16 TypeScript type aliases derived
- 226-VALIDATION.md verification map covers all 7 plans (>= 22 task rows); nyquist_compliant + wave_0_complete flipped
- Legacy P100-P105 + P201 + P221-P225 tests still green (regression confirmed)
- ZERO new npm dependencies (all infra inherited from prior phases per 226-RESEARCH §Standard Stack)
</success_criteria>

<output>
After completion, create `.planning/phases/226-sales-enablement-deal-execution/226-01-SUMMARY.md` documenting: tables created (15 SOR), CHECK constraints enforced, share_link_token UNIQUE proven, polymorphic CHECK proven, F-IDs registered (F-163..F-172), contracts published, fixture inventory (13 factories + barrel), enum constants exported (16), decision coverage map (D-01..D-10 schema-only; D-21..D-24 quote-snapshot CHECK; D-29 share_link_token UNIQUE; D-34 handoff CHECK; D-37 health-score CHECK; D-56 tombstone columns), open follow-ups for Wave 2 (226-02 EvidenceMap freshness inheritance + 226-03 lifecycle hook + cdp_events emit), and links to 226-VALIDATION.md per-task rows now populated.
</output>
