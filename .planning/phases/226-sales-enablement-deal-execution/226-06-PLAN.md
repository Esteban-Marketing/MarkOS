---
phase: 226
plan: "06"
type: execute
wave: 4
depends_on: ["226-01", "226-02", "226-03", "226-04", "226-05"]
files_modified:
  - api/v1/sales/battlecards/index.js
  - api/v1/sales/battlecards/[battlecard_id]/index.js
  - api/v1/sales/battlecards/[battlecard_id]/verify/index.js
  - api/v1/sales/battlecards/[battlecard_id]/stale/index.js
  - api/v1/sales/battlecards/[battlecard_id]/approve/index.js
  - api/v1/sales/battlecards/[battlecard_id]/refresh-claims/index.js
  - api/v1/sales/battlecards/[battlecard_id]/versions/index.js
  - api/v1/sales/objections/libraries/index.js
  - api/v1/sales/objections/libraries/[library_id]/entries/index.js
  - api/v1/sales/objections/records/index.js
  - api/v1/sales/objections/records/[record_id]/address/index.js
  - api/v1/sales/objections/records/[record_id]/defer/index.js
  - api/v1/sales/objections/records/[record_id]/resolve/index.js
  - api/v1/sales/proof-packs/index.js
  - api/v1/sales/proof-packs/[proof_pack_id]/index.js
  - api/v1/sales/proof-packs/[proof_pack_id]/approve/index.js
  - api/v1/sales/proof-packs/[proof_pack_id]/retire/index.js
  - api/v1/sales/proof-packs/[proof_pack_id]/refresh-claims/index.js
  - api/v1/sales/proof-packs/[proof_pack_id]/versions/index.js
  - api/v1/sales/deal-briefs/index.js
  - api/v1/sales/deal-briefs/[deal_brief_id]/index.js
  - api/v1/sales/deal-briefs/[deal_brief_id]/regenerate/index.js
  - api/v1/sales/deal-briefs/[deal_brief_id]/approve/index.js
  - api/v1/sales/deal-briefs/[deal_brief_id]/archive/index.js
  - api/v1/sales/deal-rooms/index.js
  - api/v1/sales/deal-rooms/[deal_room_id]/index.js
  - api/v1/sales/deal-rooms/[deal_room_id]/enable-share/index.js
  - api/v1/sales/deal-rooms/[deal_room_id]/close/index.js
  - api/v1/sales/deal-rooms/[deal_room_id]/artifacts/index.js
  - api/v1/sales/deal-rooms/[deal_room_id]/artifacts/[artifact_id]/index.js
  - api/v1/sales/deal-rooms/[deal_room_id]/views/index.js
  - api/v1/sales/proposals/index.js
  - api/v1/sales/proposals/[proposal_id]/index.js
  - api/v1/sales/proposals/[proposal_id]/approve/index.js
  - api/v1/sales/proposals/[proposal_id]/send/index.js
  - api/v1/sales/proposals/[proposal_id]/mark-accepted/index.js
  - api/v1/sales/proposals/[proposal_id]/mark-rejected/index.js
  - api/v1/sales/quotes/index.js
  - api/v1/sales/quotes/[quote_id]/index.js
  - api/v1/sales/quotes/[quote_id]/send/index.js
  - api/v1/sales/quotes/[quote_id]/mark-accepted/index.js
  - api/v1/sales/quotes/[quote_id]/mark-rejected/index.js
  - api/v1/sales/quotes/[quote_id]/expire/index.js
  - api/v1/sales/quotes/[quote_id]/supersede/index.js
  - api/v1/sales/winloss/index.js
  - api/v1/sales/handoffs/index.js
  - api/v1/sales/handoffs/[handoff_id]/acknowledge/index.js
  - api/v1/sales/deal-health/[opportunity_id]/index.js
  - lib/markos/sales/api-helpers.ts
  - lib/markos/mcp/sales-tools.ts
  - lib/markos/mcp/index.ts
  - test/sales/api/cross-tenant-denial.test.ts
  - test/sales/api/openapi-parity.test.ts
  - test/sales/api/approval-routing.test.ts
  - test/sales/api/v1-sales-routes-shape.test.ts
  - test/sales/mcp/get-battlecard.test.ts
  - test/sales/mcp/assemble-proof-pack.test.ts
  - test/sales/mcp/generate-deal-brief.test.ts
  - test/sales/mcp/create-deal-room.test.ts
  - test/sales/mcp/generate-quote.test.ts
  - test/sales/mcp/record-winloss.test.ts
  - test/sales/mcp/list-open-objections.test.ts
  - test/sales/mcp/get-handoff-brief.test.ts
  - .planning/phases/226-sales-enablement-deal-execution/226-VALIDATION.md
autonomous: true
requirements:
  - SEN-01
  - SEN-02
  - SEN-03
  - SEN-04
  - SEN-05
  - CRM-01
  - CRM-02
  - CRM-03
  - CRM-04
  - CRM-05
  - PRC-01
  - PRC-02
  - PRC-03
  - PRC-04
  - PRC-05
  - PRC-06
  - PRC-07
  - PRC-08
  - PRC-09
  - EVD-01
  - EVD-02
  - EVD-03
  - EVD-04
  - EVD-05
  - EVD-06
  - QA-01
  - QA-02
must_haves:
  truths:
    - 49 API route handlers under api/v1/sales/*.js (legacy flat-file convention per D-78/D-81; NOT App Router route.ts) land per D-43 (battlecards 7 + objections 6 + proof-packs 6 + deal-briefs 5 + deal-rooms 7 + proposals 6 + quotes 7 + winloss 1 + handoffs 2 + deal-health 1 + 1 share-link covered in 226-05 = 49 routes total under operator auth + 1 public route under 226-05); each route: (1) auth via requireHostedSupabaseAuth (D-78/D-81; from onboarding/backend/runtime-context.cjs:491) → tenant context resolution; (2) zod-validated request shape; (3) calls library function from lib/markos/sales/*; (4) returns JSON matching contract schema; (5) error codes per contract YAML; (6) audit emission via lib/markos/sales/audit.ts (already inside library calls)
    - lib/markos/sales/api-helpers.ts ships unified API helpers — requireSalesTenantContext (extends requireHostedSupabaseAuth with sales-domain RBAC stub; D-78/D-81), respondJson (zod-validated response), respondError (consistent error_codes per contract), wrapWithAudit (defensive double-emit guard); EVERY API route handler calls these helpers; cross-tenant denial proven via shared helper (no per-route divergence)
    - All 49 API routes return 403 for cross-tenant access (tenant_id mismatch between session + resource); proven by test/sales/api/cross-tenant-denial.test.ts iterating all routes with cross-tenant tokens
    - All write routes (~32 of 49) honor approval-package pattern per D-45: routes call gateApproval(artifactKind, payload, actor_user_id) BEFORE state mutation; returns 202 Accepted with approval_ref when customer_facing_required; returns 200 OK + new state when internal_auto OR approval already granted
    - OpenAPI parity: 18 contract YAMLs (F-163..F-180) reflect all 49 routes (multiple paths per F-ID); openapi.json regen in 226-07 picks up all routes; test/sales/api/openapi-parity.test.ts asserts every route file has matching path in flow-registry+contract paths
    - lib/markos/mcp/sales-tools.ts ships 8 MCP tools per D-44: get_battlecard, assemble_proof_pack, generate_deal_brief, create_deal_room, generate_quote, record_winloss, list_open_objections, get_handoff_brief — each MCP tool wraps the corresponding library function with MCP-protocol JSON-RPC envelope + tenant-context resolution from MCP session; A2 fallback when MCP server module absent (P202 GA): tools registered locally for unit testing without server
    - get_battlecard MCP tool: input {competitor_name, tenant_id (resolved from session)} → output Battlecard with freshness_status; calls listBattlecards then filter by competitor_name + status='approved'
    - assemble_proof_pack MCP tool: input {audience_type, tenant_id, refresh_claims?} → output ProofPack OR new assembly; if refresh_claims=true, calls refreshClaimFreshness first
    - generate_deal_brief MCP tool: input {opportunity_id, trigger='manual'} → wraps generateDealBrief from 226-03; returns deal_brief_id + status; debounce-aware; A5 + A22 fallbacks documented in tool description
    - create_deal_room MCP tool: input {opportunity_id, expires_at?, artifacts: ArtifactInput[]} → calls createDealRoom + iterative addArtifact; returns deal_room_id + share_link_token (masked) + artifact_count; class-based approval gate enforced (returns approval_ref if not yet granted)
    - generate_quote MCP tool: input {proposal_id, pricing_recommendation_id, valid_until?} → calls createQuote (frozen snapshot from P205); returns quote_id + snapshot_at; A13 fallback emits placeholder with `{{MARKOS_PRICING_ENGINE_PENDING}}`
    - record_winloss MCP tool: input {opportunity_id, outcome, primary_reason, secondary_reasons?, competitive_set?, ...} → calls createWinlossRecord with structured-fields validation (Pitfall 5 enforced); auto-emits LearningCandidate + cdp_event
    - list_open_objections MCP tool: input {opportunity_id?, competitor_profile_id?} → calls listObjectionRecords with status='open' filter
    - get_handoff_brief MCP tool: input {handoff_id} → calls getHandoffRecord + linked deal_brief + acknowledgement state; consumed by inbound handoff acknowledger (P208 Approval Inbox)
    - 226-VALIDATION.md rows 226-06-01 (API cross-tenant + approval routing) + 226-06-02 (MCP 8 tools registered) populated with green status pending
  artifacts:
    - path: lib/markos/sales/api-helpers.ts
      provides: 4 unified API helpers (requireSalesTenantContext, respondJson, respondError, wrapWithAudit); zod-validated; consistent error codes; cross-tenant denial baked in
      exports: [requireSalesTenantContext, respondJson, respondError, wrapWithAudit, SALES_API_ERROR_CODES]
    - path: lib/markos/mcp/sales-tools.ts
      provides: 8 MCP tools per D-44; JSON-RPC envelope; tenant context from MCP session; A2 fallback when MCP server module absent
      exports: [SALES_MCP_TOOLS, registerSalesMcpTools, getBattlecardTool, assembleProofPackTool, generateDealBriefTool, createDealRoomTool, generateQuoteTool, recordWinlossTool, listOpenObjectionsTool, getHandoffBriefTool]
    - path: api/v1/sales/battlecards/index.js (and 48 sibling files under api/v1/sales/*.js)
      provides: full /v1/sales/* read-write API per D-43; 49 route handlers covering 18 F-ID contracts F-163..F-179 (F-180 ships in 226-05 as public-share; counted separately)
  key_links:
    - from: api/v1/sales/quotes/[quote_id]/send/index.js
      to: lib/markos/sales/quotes/quote.ts (sendQuote)
      via: route handler delegates to sendQuote which enforces D-21..D-24 immutability + class-based approval (D-26) + content classifier (D-27)
    - from: api/v1/sales/proposals/[proposal_id]/send/index.js
      to: lib/markos/sales/proposals/proposal-support.ts (sendProposalSupport)
      via: route handler delegates to sendProposalSupport which enforces approval gate + content classifier + ProofPack freshness (D-13)
    - from: api/v1/sales/deal-rooms/[deal_room_id]/enable-share/index.js
      to: lib/markos/sales/deal-rooms/deal-room.ts (enableShare)
      via: route handler delegates to enableShare which enforces customer_facing_required approval (D-26)
    - from: lib/markos/mcp/sales-tools.ts (record_winloss)
      to: lib/markos/sales/winloss/winloss-record.ts (createWinlossRecord)
      via: MCP tool input schema enforces structured fields (Pitfall 5); LearningCandidate + cdp_event emit happen automatically inside library
    - from: api/v1/sales/* (every route)
      to: lib/markos/sales/api-helpers.ts (requireSalesTenantContext)
      via: every route's first call resolves tenant_id from session + asserts cross-tenant denial; consistent enforcement across 49 routes
---

<objective>
Build the API + MCP layer over 226-01..05 library functions. Land 49 route handlers under api/v1/sales/* covering 17 F-ID contracts (F-163..F-179; F-180 public share already shipped in 226-05). Land 8 MCP tools per D-44 with full input/output schemas and tenant-context resolution from MCP session. Wire approval-package routing (D-45) on every state-mutating route. Land cross-tenant denial helper (requireSalesTenantContext) used by every route. Land OpenAPI parity test asserting every route maps to a contract YAML path. Wave 4 single-thread (depends on all prior waves; no parallelism).

Purpose: satisfy SEN-01..05 (full operator API + MCP coverage), CRM-01..05 + PRC-01..09 + EVD-01..06 (carry-forward enforcement at API boundary; pricing/claim/competitor scans happen inside library calls invoked by route handlers), QA-01 (18 contracts wired), QA-02 (cross-tenant denial proven on every route; audit emission proven via library layer; OpenAPI parity test).

Output: 49 API route files + lib/markos/sales/api-helpers.ts + lib/markos/mcp/sales-tools.ts + lib/markos/mcp/index.ts barrel update + 12 test files (4 API tests + 8 MCP tool tests). No new contracts (18 already registered in 226-01..05). No new migrations.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/226-sales-enablement-deal-execution/226-CONTEXT.md
@.planning/phases/226-sales-enablement-deal-execution/226-RESEARCH.md
@.planning/phases/226-sales-enablement-deal-execution/226-VALIDATION.md
@.planning/phases/226-sales-enablement-deal-execution/226-01-PLAN.md
@.planning/phases/226-sales-enablement-deal-execution/226-02-PLAN.md
@.planning/phases/226-sales-enablement-deal-execution/226-03-PLAN.md
@.planning/phases/226-sales-enablement-deal-execution/226-04-PLAN.md
@.planning/phases/226-sales-enablement-deal-execution/226-05-PLAN.md
@.planning/phases/226-sales-enablement-deal-execution/226-01-SUMMARY.md
@.planning/phases/226-sales-enablement-deal-execution/226-02-SUMMARY.md
@.planning/phases/226-sales-enablement-deal-execution/226-03-SUMMARY.md
@.planning/phases/226-sales-enablement-deal-execution/226-04-SUMMARY.md
@.planning/phases/226-sales-enablement-deal-execution/226-05-SUMMARY.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/V4.0.0-TESTING-ENVIRONMENT-PLAN.md
@obsidian/reference/HTTP Layer.md
@obsidian/reference/Contracts Registry.md
@obsidian/reference/CRM Domain.md
@CLAUDE.md
@contracts/flow-registry.json
@contracts/F-163-sales-battlecard-v1.yaml
@contracts/F-180-sales-deal-room-share-v1.yaml
@.planning/phases/202-mcp-server-ga-claude-marketplace/DISCUSS.md
@lib/markos/sales/contracts.ts
@lib/markos/sales/audit.ts
@lib/markos/sales/approvals/class-gate.ts
@lib/markos/sales/battlecards/index.ts
@lib/markos/sales/proof-packs/index.ts
@lib/markos/sales/objections/index.ts
@lib/markos/sales/deal-briefs/index.ts
@lib/markos/sales/deal-rooms/index.ts
@lib/markos/sales/proposals/index.ts
@lib/markos/sales/quotes/index.ts
@lib/markos/sales/winloss/index.ts
@lib/markos/sales/handoffs/index.ts
@lib/markos/sales/health-signals/index.ts
@package.json

<!-- INFORMATIONAL ONLY (per CLAUDE.md source-of-truth precedence): -->
<!-- obsidian/work/incoming/24-SALES-ENABLEMENT-ENGINE.md INFORMATIONAL ONLY. Canonical surface for /v1/sales/* shape = obsidian/reference/HTTP Layer.md + 226-CONTEXT.md D-43 + contract YAMLs F-163..F-180. -->
</context>

<interfaces>
<!-- Key contracts the executor needs. Embedded so no codebase exploration required. -->

From `obsidian/reference/HTTP Layer.md` /v1/* convention:
- All public sales endpoints under api/v1/sales/*.js (legacy convention per D-78/D-81) with tenant context derived from session via `requireHostedSupabaseAuth` (NOT `requireSupabaseAuth` — D-78)
- Public-share unauthenticated endpoint at `/share/dr/{token}` (already shipped in 226-05)
- Response shape: `{ data: T }` for success; `{ error: string, error_code: string, details?: any }` for errors

From `lib/markos/sales/approvals/class-gate.ts` (226-04):
```typescript
export async function gateApproval(artifactKind: SalesArtifactKind, payload: unknown, actorId: string, options?: { force_customer_facing?: boolean }): Promise<GateApprovalResult>;
// Returns { approved: true } for internal-class auto-approve OR approval already granted
// Returns { approved: false, approval_ref } for customer-facing pending approval
// Routes call this BEFORE state mutation; if approved=false, return 202 Accepted with approval_ref
```

From `lib/markos/sales/quotes/quote.ts` (226-04):
```typescript
export async function sendQuote(supabase, quote_id, actor_user_id, options?): Promise<{ approved: boolean; approval_ref?: string; quote?: QuoteRow }>;
// Route handler api/v1/sales/quotes/[quote_id]/send/index.js calls sendQuote; returns 202 with approval_ref if not yet approved, 200 with quote if approved
```

From `lib/markos/auth/supabase-auth.ts` (P201 — module path expected):
- Expected: `requireHostedSupabaseAuth({req, runtimeContext, operation, requiredProjectSlug?, env?}): Promise<{ok: boolean, status?: number, error?: string, tenant_id?: string, user_id?: string, role?: string}>` per `onboarding/backend/runtime-context.cjs:491`. Returns `{ok: false, status, error}` on auth failure. D-78/D-81. NO `requireSupabaseAuth` symbol exists in repo.
- Used as base for requireSalesTenantContext (extends with sales-domain RBAC: sales_owner, deal_owner, cs_owner, account_owner, manager)

From P202 MCP server (expected at lib/markos/mcp/server.ts):
- Expected: `registerMcpTool({name, input_schema, output_schema, handler})` for runtime tool registration
- A2 fallback when MCP server module absent: tools registered locally; unit tests verify schemas + handler logic without server roundtrip

From contract YAMLs F-163..F-180 (pre-staged in 226-01..05):
- Each contract paths[] field declares all HTTP methods + URLs for that F-ID
- Total: 18 F-IDs × ~3 paths each = 49 unique routes (excluding F-180 public share path which is owned by 226-05 page.tsx)
- error_codes consistent across contracts: 400 (invalid_request_shape), 401 (missing_tenant_context), 403 (cross_tenant_denied), 404 (not_found), 409 (race_or_state_conflict), 422 (validation_failed)
</interfaces>

<tasks>


<!-- REVIEW-DRIVEN REVISIONS (D-78 / D-81 / RH3) -->

**REVIEW-DRIVEN REVISIONS (2026-04-25 — RH3 Architecture-lock):**

The 49 P226 API handlers land as **legacy `api/v1/sales/*.js` flat files** (NOT App Router `route.ts`). Auth helper is **`requireHostedSupabaseAuth`** from `onboarding/backend/runtime-context.cjs:491` (NOT `requireSupabaseAuth` — that symbol does not exist in repo).

**Handler shape (canonical):**

```javascript
// api/v1/sales/battlecards/index.js
'use strict';

const { requireHostedSupabaseAuth } = require('../../../../onboarding/backend/runtime-context.cjs');
const { listBattlecards, createBattlecard } = require('../../../../lib/markos/sales/battlecards/battlecard.js');

module.exports = async function handler(req, res) {
  const ctx = await requireHostedSupabaseAuth({
    req,
    runtimeContext: req.runtimeContext,
    operation: 'sales.battlecards.list',  // operation key per route
  });
  if (!ctx.ok) return res.status(ctx.status || 401).json({ error: ctx.error });

  if (req.method === 'GET') {
    const result = await listBattlecards(ctx, req.query);
    return res.status(200).json(result);
  }
  if (req.method === 'POST') {
    const result = await createBattlecard(ctx, req.body);
    return res.status(201).json(result);
  }
  return res.status(405).json({ error: 'METHOD_NOT_ALLOWED' });
};
```

**Public share track endpoint (NOT auth-required):**

```javascript
// api/v1/sales/public/share-track.js
'use strict';

const { trackDealRoomView } = require('../../../../lib/markos/sales/deal-rooms/share-tracker.js');
const { verifyShareToken } = require('../../../../lib/markos/sales/share/token.js');
const { checkPublicShareRateLimit } = require('../../../../lib/markos/sales/share/rate-limit-public-share.js');

module.exports = async function handler(req, res) {
  if (req.method !== 'POST') return res.status(405).json({ error: 'METHOD_NOT_ALLOWED' });
  // BotID + rate-limit + honeypot per D-90; HMAC token verify per D-86.
  // ...
};
```

**OpenAPI generation:** `npm run openapi:build` (D-78). NO `openapi-generate` script.

**App Router migration is OUT OF SCOPE** per D-78. App Router `app/(public)/share/dr/[token]/page.tsx` is the ONE Next.js exception — public share doctrine.


<task type="auto" tdd="true">
  <name>Task 1: lib/markos/sales/api-helpers.ts + 49 API route handlers under api/v1/sales/*</name>
  <read_first>
    - .planning/phases/226-sales-enablement-deal-execution/226-CONTEXT.md D-43 (full /v1/sales/* route list); D-45 (approval-package on writes)
    - obsidian/reference/HTTP Layer.md (App Router IS NOT in scope for P226 per D-78; reference informational only). Tenant context via `requireHostedSupabaseAuth` from `onboarding/backend/runtime-context.cjs`.
    - .planning/phases/201-saas-tenancy-hardening/201-CONTEXT.md (informational — `requireSupabaseAuth` referenced in P201 docs does not exist in repo as a symbol; the actual helper is `requireHostedSupabaseAuth` per D-78/D-81; cross-tenant denial pattern still applies)
    - contracts/F-163..F-180 (pre-staged; 18 contracts; paths[] declares all routes)
    - lib/markos/sales/* libraries from 226-01..05 (every route delegates to library function)
    - lib/markos/sales/approvals/class-gate.ts (gateApproval — 226-04)
    - .planning/phases/224-conversion-launch-workspace/224-04-PLAN.md (P224 API route pattern reference; reuse api/v1/* idiom)
    - .planning/phases/225-analytics-attribution-narrative-intelligence/225-07-PLAN.md (P225 API + cross-tenant denial test pattern)
  </read_first>
  <behavior>
    - Test 1 (api/cross-tenant-denial.test.ts):
      - For EACH of 49 routes (parameterized): create resource under tenant A; attempt access via session for tenant B → expect 403 with error_code='cross_tenant_denied'
      - Test enumerates routes from a constant list (kept in sync with files_modified); `route_count_must_be_49` test fails if list mismatches files_modified
    - Test 2 (api/openapi-parity.test.ts):
      - Read all api/v1/sales/**/*.js files; extract HTTP methods + URL paths
      - Read all contracts/F-16[3-9]-*.yaml + F-17[0-9]-*.yaml + F-180-*.yaml; extract paths[] from each YAML
      - Assert every route file has a matching contract path (and vice versa, except F-180 public path which lives outside /v1/)
      - Generates report listing orphaned routes (no contract) or orphaned contract paths (no route)
    - Test 3 (api/approval-routing.test.ts):
      - For EACH customer-facing artifact mutation route (~12 routes — proposal/send, proposal/approve, quote/send, deal-room/enable-share, proof-pack/approve, deal-brief/approve with audience='customer_facing', handoff/acknowledge):
        - First request without prior approval → 202 with approval_ref
        - Second request after approval grant (mock buildApprovalPackage) → 200 with mutated state
      - For EACH internal-auto artifact mutation route (~6 routes — battlecard/approve, battlecard/verify, battlecard/stale, objection-library entry CRUD, objection-record/address|defer|resolve, deal-brief/approve with audience='internal'):
        - Request → 200 with mutated state immediately (no approval_ref)
    - Test 4 (api/v1-sales-routes-shape.test.ts):
      - Lists all api/v1/sales/**/*.js route files; asserts count = 49 (matches files_modified declared count)
      - For each route, smoke-test happy path: HTTP 200 OR 202 with valid response shape per contract schema
      - zod validation: malformed body → 400 with error_code='invalid_request_shape'
  </behavior>
  <action>
    1. Create `lib/markos/sales/api-helpers.ts`:
       ```typescript
       import { z } from 'zod';
       import { NextRequest, NextResponse } from 'next/server';
       import { emitSalesAuditLog } from './audit.js';
       
       export const SALES_API_ERROR_CODES = {
         INVALID_REQUEST_SHAPE: 'invalid_request_shape',
         MISSING_TENANT_CONTEXT: 'missing_tenant_context',
         CROSS_TENANT_DENIED: 'cross_tenant_denied',
         NOT_FOUND: 'not_found',
         RACE_OR_STATE_CONFLICT: 'race_or_state_conflict',
         VALIDATION_FAILED: 'validation_failed',
         APPROVAL_PENDING: 'approval_pending',  // 202
         STALE_CLAIMS_BLOCK_RENDER: 'stale_claims_block_render',
         WINLOSS_REQUIRED_FOR_TRANSITION: 'winloss_required_for_transition',
         QUOTE_IMMUTABILITY_VIOLATION: 'quote_immutability_violation',
         CONTENT_CLASSIFIER_BLOCK: 'content_classifier_block',
         BOTID_FAILED: 'botid_failed',
         RATE_LIMITED: 'rate_limited',
         HONEYPOT_FILLED: 'honeypot_filled',
         HANDOFF_RACE_CONDITION: 'handoff_race_condition',
       } as const;
       
       export interface SalesTenantContext {
         tenant_id: string;
         user_id: string;
         role: 'sales_owner' | 'deal_owner' | 'cs_owner' | 'account_owner' | 'manager' | 'tenant_admin';
       }
       
       export async function requireSalesTenantContext(req: NextRequest): Promise<SalesTenantContext> {
         try {
           const { requireHostedSupabaseAuth } = await import('../../../onboarding/backend/runtime-context.cjs');  // D-78/D-81
           const session = await requireHostedSupabaseAuth({ req, runtimeContext: req.runtimeContext, operation: 'sales.api' });
           // Sales-domain RBAC stub: role must be one of allowed sales roles
           // Wave 4 ships permissive RBAC (any authenticated user); 226-07 closeout tightens RBAC
           return { tenant_id: session.tenant_id, user_id: session.user_id, role: session.role ?? 'sales_owner' };
         } catch (err) {
           throw new SalesApiError(401, SALES_API_ERROR_CODES.MISSING_TENANT_CONTEXT, 'Authentication required');
         }
       }
       
       export class SalesApiError extends Error {
         constructor(public readonly status: number, public readonly error_code: string, message: string, public readonly details?: unknown) {
           super(message);
           this.name = 'SalesApiError';
         }
       }
       
       export function respondJson<T>(data: T, status: number = 200): NextResponse {
         return NextResponse.json({ data }, { status });
       }
       
       export function respondError(error: SalesApiError | Error): NextResponse {
         if (error instanceof SalesApiError) {
           return NextResponse.json({ error: error.message, error_code: error.error_code, details: error.details ?? null }, { status: error.status });
         }
         // Map known library errors to API error codes
         const errName = error.name;
         const map: Record<string, { status: number; error_code: string }> = {
           ProofPackStaleError: { status: 422, error_code: SALES_API_ERROR_CODES.STALE_CLAIMS_BLOCK_RENDER },
           BattlecardStaleError: { status: 422, error_code: SALES_API_ERROR_CODES.STALE_CLAIMS_BLOCK_RENDER },
           QuoteImmutabilityError: { status: 409, error_code: SALES_API_ERROR_CODES.QUOTE_IMMUTABILITY_VIOLATION },
           QuoteStateError: { status: 409, error_code: SALES_API_ERROR_CODES.RACE_OR_STATE_CONFLICT },
           QuoteContentClassifierError: { status: 422, error_code: SALES_API_ERROR_CODES.CONTENT_CLASSIFIER_BLOCK },
           ProposalContentClassifierError: { status: 422, error_code: SALES_API_ERROR_CODES.CONTENT_CLASSIFIER_BLOCK },
           IllegalProposalTransitionError: { status: 409, error_code: SALES_API_ERROR_CODES.RACE_OR_STATE_CONFLICT },
           HandoffRaceConditionError: { status: 409, error_code: SALES_API_ERROR_CODES.HANDOFF_RACE_CONDITION },
           WinlossCoercionError: { status: 422, error_code: SALES_API_ERROR_CODES.VALIDATION_FAILED },
           WinlossRequiredError: { status: 422, error_code: SALES_API_ERROR_CODES.WINLOSS_REQUIRED_FOR_TRANSITION },
           DealBriefClaimAuditError: { status: 422, error_code: SALES_API_ERROR_CODES.VALIDATION_FAILED },
           ShareTokenInvalidError: { status: 404, error_code: SALES_API_ERROR_CODES.NOT_FOUND },  // D-86: no oracle on token existence
         };
         const mapped = map[errName] ?? { status: 500, error_code: 'internal_error' };
         return NextResponse.json({ error: error.message, error_code: mapped.error_code }, { status: mapped.status });
       }
       
       export async function wrapWithAudit<T>(ctx: SalesTenantContext, fn: () => Promise<T>): Promise<T> {
         try {
           return await fn();
         } catch (err) {
           // Defensive: log uncaught errors that escape library audit emission
           if (err instanceof Error && err.name !== 'SalesApiError') {
             await emitSalesAuditLog({
               tenant_id: ctx.tenant_id,
               event_type: 'evidence_map_unavailable',  // reuse generic; could add 'api_uncaught_error'
               actor_user_id: ctx.user_id,
               target_kind: 'api',
               target_id: '00000000-0000-0000-0000-000000000000',
               payload: { error_name: err.name, error_message: err.message },
               severity: 'error',
             });
           }
           throw err;
         }
       }
       ```

    2. For each of the 49 routes, create the route handler file. Pattern (using battlecards/index.js as exemplar):
       ```typescript
       // api/v1/sales/battlecards/index.js
       import { NextRequest } from 'next/server';
       import { z } from 'zod';
       import { requireSalesTenantContext, respondJson, respondError, SalesApiError, SALES_API_ERROR_CODES, wrapWithAudit } from '@/lib/markos/sales/api-helpers';
       import { listBattlecards, createBattlecard } from '@/lib/markos/sales/battlecards';
       
       const ListQuerySchema = z.object({
         competitor_name: z.string().optional(),
         freshness_status: z.enum(['fresh','stale_claims','stale_competitor','retired']).optional(),
         status: z.enum(['draft','approved','archived']).optional(),
       });
       
       const CreateBodySchema = z.object({
         competitor_name: z.string().min(1).max(200),
         competitor_profile_id: z.string().uuid().nullable().optional(),
         summary: z.string().min(1),
         strengths_to_acknowledge: z.array(z.string()).default([]),
         weaknesses_to_press: z.array(z.string()).default([]),
         ideal_positioning_angles: z.array(z.string()).default([]),
         risky_claims_to_avoid: z.array(z.string()).default([]),
         proof_refs: z.array(z.string().uuid()).default([]),
       });
       
       export async function GET(req: NextRequest) {
         try {
           const ctx = await requireSalesTenantContext(req);
           const url = new URL(req.url);
           const query = ListQuerySchema.parse(Object.fromEntries(url.searchParams));
           const supabase = await getSupabaseClient(ctx.tenant_id);
           const battlecards = await wrapWithAudit(ctx, () => listBattlecards(supabase, { tenant_id: ctx.tenant_id, ...query }));
           return respondJson({ battlecards });
         } catch (err) {
           if (err instanceof z.ZodError) return respondError(new SalesApiError(400, SALES_API_ERROR_CODES.INVALID_REQUEST_SHAPE, 'Invalid query', err.errors));
           return respondError(err as Error);
         }
       }
       
       export async function POST(req: NextRequest) {
         try {
           const ctx = await requireSalesTenantContext(req);
           const body = CreateBodySchema.parse(await req.json());
           const supabase = await getSupabaseClient(ctx.tenant_id);
           const row = await wrapWithAudit(ctx, () => createBattlecard(supabase, ctx.tenant_id, ctx.user_id, body));
           return respondJson(row, 201);
         } catch (err) {
           if (err instanceof z.ZodError) return respondError(new SalesApiError(400, SALES_API_ERROR_CODES.INVALID_REQUEST_SHAPE, 'Invalid body', err.errors));
           return respondError(err as Error);
         }
       }
       ```

    3. Create the remaining 48 route handlers following the same pattern. KEY ROUTES requiring class-based approval routing (return 202 with approval_ref when gate.approved=false):
       - `api/v1/sales/battlecards/[battlecard_id]/approve/index.js` POST → calls approveBattlecard (D-25 internal_auto → returns 200 immediately)
       - `api/v1/sales/proof-packs/[proof_pack_id]/approve/index.js` POST → calls approveProofPack (D-26 customer_facing → returns 202 if pending)
       - `api/v1/sales/deal-briefs/[deal_brief_id]/approve/index.js` POST → calls approveDealBrief (audience param routes between D-25 internal vs D-26 customer)
       - `api/v1/sales/deal-rooms/[deal_room_id]/enable-share/index.js` POST → calls enableShare (D-26)
       - `api/v1/sales/proposals/[proposal_id]/approve/index.js` POST → calls approveProposal (D-26)
       - `api/v1/sales/proposals/[proposal_id]/send/index.js` POST → calls sendProposalSupport (D-26 + D-27 content classifier)
       - `api/v1/sales/quotes/[quote_id]/send/index.js` POST → calls sendQuote (D-26 + D-27 + D-22)
       - `api/v1/sales/quotes/[quote_id]/supersede/index.js` POST → calls supersedeQuote with `force_customer_facing: true` per D-28 re-engagement
       - `api/v1/sales/handoffs/[handoff_id]/acknowledge/index.js` POST → calls acknowledgeHandoff (D-35)
       
       INTERNAL-AUTO routes (return 200 immediately):
       - `api/v1/sales/battlecards/[battlecard_id]/verify/index.js` POST → operator attestation; sets last_verified_at (does NOT override claim TTL per Pitfall 1)
       - `api/v1/sales/battlecards/[battlecard_id]/stale/index.js` POST → manual setStale; D-19
       - `api/v1/sales/battlecards/[battlecard_id]/refresh-claims/index.js` POST → recomputeBattlecardFreshness (D-17)
       - `api/v1/sales/proof-packs/[proof_pack_id]/refresh-claims/index.js` POST → refreshClaimFreshness (D-12)
       - `api/v1/sales/objections/records/[record_id]/{address,defer,resolve}/index.js` POST → 4 transitions (state changes only; not customer-facing)

    4. Special routes:
       - `api/v1/sales/deal-rooms/[deal_room_id]/views/index.js` GET → listDealRoomViews + computeViewSpike; operator-only RBAC
       - `api/v1/sales/deal-health/[opportunity_id]/index.js` GET → returns latest deal_health_signals; if computed_at < now - 30min, triggers immediate computeDealHealthSignals recompute (Pitfall 8); returns cache_status header
       - `api/v1/sales/winloss/index.js` GET (list) + POST (create) → uses createWinlossRecord with structured-fields validation (Pitfall 5)
       - `api/v1/sales/handoffs/index.js` GET (list) → listHandoffs filtered by opportunity_id; no POST here (handoffs created via lifecycle hook, not direct API)
       - `api/v1/sales/quotes/[quote_id]/expire/index.js` POST → manual operator expiry (versus cron)

    5. Create test files:
       - `test/sales/api/cross-tenant-denial.test.ts` — parameterized across all 49 routes; cross-tenant access → 403
       - `test/sales/api/openapi-parity.test.ts` — every route file ↔ contract YAML path bidirectional check
       - `test/sales/api/approval-routing.test.ts` — customer-facing routes return 202 with approval_ref; internal-auto routes return 200 immediately
       - `test/sales/api/v1-sales-routes-shape.test.ts` — all 49 routes exist; smoke-test happy path; zod validation triggers 400
  </action>
  <acceptance_criteria>
    - File exists: `lib/markos/sales/api-helpers.ts`
    - `grep -c "^export " lib/markos/sales/api-helpers.ts` returns >= 5 (SALES_API_ERROR_CODES, requireSalesTenantContext, SalesApiError, respondJson, respondError, wrapWithAudit)
    - `grep -E "SALES_API_ERROR_CODES" lib/markos/sales/api-helpers.ts | wc -l` returns 1+ const declaration; >= 14 error code keys present
    - 48 route files exist under api/v1/sales/* matching files_modified declaration (49 total = 48 operator + 1 public from 226-05); verify with `find api/v1/sales -name "*.js" | wc -l` returns 49 (48 from this plan + 1 from 226-05 public share-track.js)
    - `grep -l "requireSalesTenantContext" api/v1/sales/**/*.js | wc -l` returns 48 (every operator route uses helper; public share-track.js from 226-05 is unauthenticated by design)
    - `grep -l "z.object\\|z.parse" api/v1/sales/**/*.js | wc -l` returns >= 30 (zod validation widely used; some GET-only routes may lack body schema)
    - `node --test test/sales/api/` exits 0 (4 test files green)
    - 226-VALIDATION.md row 226-06-01 status flips from `⬜ pending` to `✅ green`
  </acceptance_criteria>
  <verify>
    <automated>node --test test/sales/api/</automated>
  </verify>
  <done>49 API route handlers + lib/markos/sales/api-helpers.ts shipped; cross-tenant denial proven across all routes; OpenAPI parity verified; approval routing matrix proven (D-25..D-28). 4 test files green. 226-VALIDATION.md row 226-06-01 green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 8 MCP tools per D-44 + sales-tools.ts registration + tenant context from MCP session (A2 fallback)</name>
  <read_first>
    - .planning/phases/226-sales-enablement-deal-execution/226-CONTEXT.md D-44 (8 MCP tools list); D-45 (approval-package on writes via library calls)
    - .planning/phases/202-mcp-server-ga-claude-marketplace/DISCUSS.md (MCP server scaffolding; expected at lib/markos/mcp/server.ts)
    - .planning/phases/200-saas-readiness-wave-0/ (200-06 MCP server with 10 skills shipped per ROADMAP; sales tools extend that registry)
    - lib/markos/sales/* libraries (each MCP tool wraps a library function)
    - contracts/F-177-sales-battlecard-freshness-v1.yaml (write/MCP contract for refresh-claims)
    - contracts/F-178-sales-proof-pack-assemble-v1.yaml (write/MCP contract for assemble)
    - contracts/F-179-sales-deal-brief-generate-v1.yaml (write/MCP contract for regenerate)
  </read_first>
  <behavior>
    - Test 1 (mcp/get-battlecard.test.ts):
      - getBattlecardTool input schema validates {competitor_name: string}
      - tenant_id resolved from MCP session context (mock); cross-tenant calls denied
      - Calls listBattlecards with filter; returns the most recent approved Battlecard; output schema validated against Battlecard YAML schema from F-163
    - Test 2 (mcp/assemble-proof-pack.test.ts):
      - assembleProofPackTool input {audience_type, refresh_claims?: boolean}
      - When refresh_claims=true, calls refreshClaimFreshness first; returns ProofPack with updated freshness_status
      - Returns existing approved ProofPack matching audience_type if refresh_claims=false (default)
    - Test 3 (mcp/generate-deal-brief.test.ts):
      - generateDealBriefTool input {opportunity_id, trigger?: 'manual'|'stage_change'|'handoff'}
      - Wraps generateDealBrief from 226-03; debounce-aware (returns suppressed status if within 1h window for non-handoff triggers)
      - A5/A22 fallback messages surface in tool output (`warnings` field)
    - Test 4 (mcp/create-deal-room.test.ts):
      - createDealRoomTool input {opportunity_id, expires_at?, artifacts: ArtifactInput[]}
      - Iterates artifacts: addArtifact for each (stale-blocking enforced from 226-05); returns deal_room_id + masked share_link_token (`dr_*****`) + artifact_count
      - Class-based approval gate: enableShare not auto-fired; tool returns approval_ref if user invokes enable; document this nuance in tool description
    - Test 5 (mcp/generate-quote.test.ts):
      - generateQuoteTool input {proposal_id, pricing_recommendation_id, valid_until?}
      - Calls createQuote (frozen snapshot from P205); returns quote_id + snapshot_at; A13 fallback surfaces placeholder warning
    - Test 6 (mcp/record-winloss.test.ts):
      - recordWinlossTool input schema validates structured fields per Pitfall 5: outcome (enum), primary_reason (enum), secondary_reasons (enum array), competitive_set (object array), attribution_touch_ids (uuid array)
      - Cross-field consistency enforced: primary_reason='competitor' + empty competitive_set → throws WinlossCoercionError
      - Returns winloss record_id + cdp_event_emitted: true + learning_candidate_emitted: boolean
    - Test 7 (mcp/list-open-objections.test.ts):
      - listOpenObjectionsTool input {opportunity_id?: uuid, competitor_profile_id?: uuid}
      - At least one filter required; returns objection_records with status='open' for the filter
    - Test 8 (mcp/get-handoff-brief.test.ts):
      - getHandoffBriefTool input {handoff_id}
      - Returns handoff_record + linked deal_brief + acknowledgement state (acknowledged_at, acknowledged_by_user_id)
      - Used by inbound handoff acknowledger; consumer reads brief before clicking acknowledge in P208 Approval Inbox
  </behavior>
  <action>
    1. Create `lib/markos/mcp/sales-tools.ts`:
       ```typescript
       import { z } from 'zod';
       import { listBattlecards, recomputeBattlecardFreshness } from '../sales/battlecards/index.js';
       import { listProofPacks, refreshClaimFreshness } from '../sales/proof-packs/index.js';
       import { generateDealBrief } from '../sales/deal-briefs/index.js';
       import { createDealRoom, addArtifact } from '../sales/deal-rooms/index.js';
       import { createQuote } from '../sales/quotes/index.js';
       import { createWinlossRecord } from '../sales/winloss/index.js';
       import { listObjectionRecords } from '../sales/objections/index.js';
       import { getHandoffRecord } from '../sales/handoffs/index.js';  // assumes 226-03 exposes this; if not, add export
       import { WINLOSS_OUTCOMES, WINLOSS_PRIMARY_REASONS, PROOF_PACK_AUDIENCE_TYPES, DEAL_ROOM_ARTIFACT_KINDS } from '../sales/contracts.js';
       
       export const SALES_MCP_TOOLS = [
         'get_battlecard',
         'assemble_proof_pack',
         'generate_deal_brief',
         'create_deal_room',
         'generate_quote',
         'record_winloss',
         'list_open_objections',
         'get_handoff_brief',
       ] as const;
       
       // 1. get_battlecard
       export const getBattlecardInputSchema = z.object({ competitor_name: z.string().min(1) });
       export async function getBattlecardTool(supabase, ctx: { tenant_id: string }, input: z.infer<typeof getBattlecardInputSchema>) {
         const list = await listBattlecards(supabase, { tenant_id: ctx.tenant_id, competitor_name: input.competitor_name, status: 'approved' });
         return { battlecard: list[0] ?? null, count: list.length };
       }
       
       // 2. assemble_proof_pack
       export const assembleProofPackInputSchema = z.object({
         audience_type: z.enum(PROOF_PACK_AUDIENCE_TYPES),
         refresh_claims: z.boolean().default(false),
         proof_pack_id: z.string().uuid().optional(),  // for explicit refresh of specific pack
       });
       export async function assembleProofPackTool(supabase, ctx, input) {
         if (input.refresh_claims && input.proof_pack_id) {
           await refreshClaimFreshness(supabase, input.proof_pack_id);
         }
         const list = await listProofPacks(supabase, { tenant_id: ctx.tenant_id, audience_type: input.audience_type, approval_state: 'approved' });
         return { proof_pack: list[0] ?? null, count: list.length, refreshed: input.refresh_claims };
       }
       
       // 3. generate_deal_brief
       export const generateDealBriefInputSchema = z.object({
         opportunity_id: z.string().uuid(),
         trigger: z.enum(['manual', 'stage_change', 'handoff']).default('manual'),
       });
       export async function generateDealBriefTool(supabase, ctx, input) {
         const result = await generateDealBrief(supabase, { tenant_id: ctx.tenant_id, opportunity_id: input.opportunity_id, trigger: input.trigger, actor_user_id: ctx.user_id });
         return { deal_brief_id: result.deal_brief_id, status: result.status, warnings: result.warnings };
       }
       
       // 4. create_deal_room
       export const createDealRoomInputSchema = z.object({
         opportunity_id: z.string().uuid(),
         expires_at: z.string().datetime().optional(),
         artifacts: z.array(z.object({
           artifact_kind: z.enum(DEAL_ROOM_ARTIFACT_KINDS),
           artifact_target_id: z.string().uuid(),
           display_order: z.number().int().nonnegative().default(0),
         })).default([]),
       });
       export async function createDealRoomTool(supabase, ctx, input) {
         const dr = await createDealRoom(supabase, ctx.tenant_id, ctx.user_id, { opportunity_id: input.opportunity_id, expires_at: input.expires_at });
         let added = 0;
         for (const art of input.artifacts) {
           try {
             await addArtifact(supabase, dr.deal_room_id, ctx.user_id, art);
             added++;
           } catch (err) {
             // Stale-blocking errors (BattlecardStaleError, ProofPackStaleError, DealRoomArtifactOrphanError) bubble in warnings
           }
         }
         return { deal_room_id: dr.deal_room_id, share_link_token_masked: 'dr_*****', artifact_count: added, total_attempted: input.artifacts.length };
       }
       
       // 5. generate_quote
       export const generateQuoteInputSchema = z.object({
         proposal_id: z.string().uuid().optional(),
         pricing_recommendation_id: z.string().uuid().nullable(),
         valid_until: z.string().datetime().optional(),
       });
       export async function generateQuoteTool(supabase, ctx, input) {
         const quote = await createQuote(supabase, ctx.tenant_id, ctx.user_id, input);
         return {
           quote_id: quote.quote_id,
           snapshot_at: quote.snapshot_at,
           pricing_source: quote.pricing_recommendation_snapshot._source ?? 'unknown',
           is_placeholder: quote.pricing_recommendation_snapshot.pending === '{{MARKOS_PRICING_ENGINE_PENDING}}',
         };
       }
       
       // 6. record_winloss
       export const recordWinlossInputSchema = z.object({
         opportunity_id: z.string().uuid(),
         outcome: z.enum(WINLOSS_OUTCOMES),
         primary_reason: z.enum(WINLOSS_PRIMARY_REASONS),
         secondary_reasons: z.array(z.enum(WINLOSS_PRIMARY_REASONS)).default([]),
         competitive_set: z.array(z.object({ competitor_name: z.string(), profile_id: z.string().uuid().optional() })).default([]),
         features_evaluated: z.array(z.string().uuid()).default([]),
         objection_history: z.array(z.string().uuid()).default([]),
         price_drivers: z.array(z.string().uuid()).default([]),
         champion_id: z.string().uuid().nullable().optional(),
         decision_maker_id: z.string().uuid().nullable().optional(),
         free_text_notes: z.string().optional(),
         attribution_touch_ids: z.array(z.string().uuid()).default([]),
       });
       export async function recordWinlossTool(supabase, ctx, input) {
         // Cross-field consistency (Pitfall 5) enforced inside createWinlossRecord
         const row = await createWinlossRecord(supabase, ctx.tenant_id, ctx.user_id, input);
         return { record_id: row.record_id, cdp_event_emitted: true, learning_candidate_attempted: ['won','lost','no_decision'].includes(input.outcome) };
       }
       
       // 7. list_open_objections
       export const listOpenObjectionsInputSchema = z.object({
         opportunity_id: z.string().uuid().optional(),
         competitor_profile_id: z.string().uuid().optional(),
       }).refine((v) => v.opportunity_id || v.competitor_profile_id, { message: 'At least one of opportunity_id or competitor_profile_id required' });
       export async function listOpenObjectionsTool(supabase, ctx, input) {
         const list = await listObjectionRecords(supabase, { opportunity_id: input.opportunity_id, status: 'open' });
         // Note: competitor_profile_id filter requires JOIN through objection_library_entries; deferred to library if not present
         return { objections: list, count: list.length };
       }
       
       // 8. get_handoff_brief
       export const getHandoffBriefInputSchema = z.object({ handoff_id: z.string().uuid() });
       export async function getHandoffBriefTool(supabase, ctx, input) {
         const handoff = await getHandoffRecord(supabase, input.handoff_id);
         if (!handoff) return { handoff: null };
         // Resolve linked deal_brief
         const { data: deal_brief } = await supabase.from('deal_briefs').select('*').eq('deal_brief_id', handoff.deal_brief_id).maybeSingle();
         return {
           handoff,
           deal_brief: deal_brief ?? null,
           acknowledgement: { acknowledged_at: handoff.acknowledged_at, acknowledged_by_user_id: handoff.acknowledged_by_user_id },
         };
       }
       
       export async function registerSalesMcpTools(supabase): Promise<{ registered: number; _source: 'p202' | 'a2_fallback' }> {
         try {
           const { registerMcpTool } = await import('./server.js' as any);
           // Register all 8 tools with the live MCP server
           // ... (loop over SALES_MCP_TOOLS calling registerMcpTool with input/output schemas + handler)
           return { registered: 8, _source: 'p202' };
         } catch (err) {
           // A2 fallback: tools are registered locally for unit testing only
           return { registered: 0, _source: 'a2_fallback' };
         }
       }
       ```

    2. Update `lib/markos/mcp/index.ts` barrel: `export * from './sales-tools.js';`

    3. Create 8 test files (one per MCP tool):
       - `test/sales/mcp/get-battlecard.test.ts` — input schema validation; tenant_id resolution; happy path returns Battlecard; cross-tenant denial
       - `test/sales/mcp/assemble-proof-pack.test.ts` — refresh_claims=true triggers refresh; audience_type filter
       - `test/sales/mcp/generate-deal-brief.test.ts` — debounce behavior (suppressed for stage_change within 1h; always-runs for handoff/manual); A5/A22 warnings surfaced
       - `test/sales/mcp/create-deal-room.test.ts` — iterative addArtifact; stale-blocking errors bubble in warnings; share_link_token masked in output
       - `test/sales/mcp/generate-quote.test.ts` — A13 placeholder; pricing_source field correct; is_placeholder boolean accurate
       - `test/sales/mcp/record-winloss.test.ts` — Pitfall 5 enforcement (cross-field consistency); structured-fields validation
       - `test/sales/mcp/list-open-objections.test.ts` — refine() requires at least one filter; status='open' filter applied
       - `test/sales/mcp/get-handoff-brief.test.ts` — handoff + deal_brief + acknowledgement returned together
  </action>
  <acceptance_criteria>
    - File exists: `lib/markos/mcp/sales-tools.ts`
    - `grep -E "^export const SALES_MCP_TOOLS" lib/markos/mcp/sales-tools.ts` returns the const with 8 entries
    - `grep -c "^export (async )?function .*Tool" lib/markos/mcp/sales-tools.ts` returns 8 (one handler per tool)
    - `grep -c "^export const get.*InputSchema\\|^export const assemble.*InputSchema\\|^export const generate.*InputSchema\\|^export const create.*InputSchema\\|^export const record.*InputSchema\\|^export const list.*InputSchema" lib/markos/mcp/sales-tools.ts` returns 8 (one zod schema per tool)
    - `grep -E "registerSalesMcpTools" lib/markos/mcp/sales-tools.ts` returns the export with A2 fallback
    - `grep -E "a2_fallback" lib/markos/mcp/sales-tools.ts` returns the line (A2 marker)
    - `node --test test/sales/mcp/` exits 0 (8 test files green)
    - 226-VALIDATION.md row 226-06-02 status flips from `⬜ pending` to `✅ green`
    - Legacy regression: `npm test` and `node --test test/cdp-* test/crm-* test/channels-* test/conversion-* test/analytics-* test/sales/{wave-0,schema,rls,battlecards,proof-packs,objections,deal-briefs,handoffs,health-signals,quotes,proposals,approvals,deal-rooms,winloss,api}/` (P221-P225 + 226-01..05 + 226-06 Task 1) all green
  </acceptance_criteria>
  <verify>
    <automated>node --test test/sales/mcp/ test/sales/api/</automated>
  </verify>
  <done>8 MCP tools per D-44 shipped with input/output zod schemas; tenant context from MCP session; A2 fallback for testing without MCP server. 8 test files green. 226-VALIDATION.md row 226-06-02 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → /v1/sales/* | Untrusted; requireSalesTenantContext resolves session + tenant; cross-tenant denial enforced |
| MCP client → MCP tool | Untrusted; tenant_id resolved from MCP session context (P202); same RBAC as HTTP routes |
| operator → state-mutating route | Class-based approval gate enforced inside library calls; route returns 202 with approval_ref when pending |
| upstream → P201 supabase auth | Required for all routes; no fallback (cannot serve traffic without auth) |
| upstream → P202 MCP server | A2 fallback: tools registered locally for unit testing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-226-06-01 | Spoofing | API session | mitigate | requireSalesTenantContext wraps `requireHostedSupabaseAuth` (D-78/D-81; from onboarding/backend/runtime-context.cjs:491); 401 on missing/invalid; sales-domain RBAC stub permissive in Wave 4; tightened in 226-07 closeout |
| T-226-06-02 | Tampering | Cross-tenant access | mitigate | requireSalesTenantContext returns tenant_id from session; library calls receive ctx.tenant_id; library RLS layer also enforces; cross-tenant-denial.test.ts proves all 49 routes |
| T-226-06-03 | Information Disclosure | Error responses | mitigate | respondError maps library errors to error_codes; never leaks stack traces in production; error.message safe by convention; details field omits PII |
| T-226-06-04 | Repudiation | API audits | mitigate | wrapWithAudit catches uncaught errors and emits 'evidence_map_unavailable' (or new 'api_uncaught_error' event) audit row; library calls already emit per-mutation audits |
| T-226-06-05 | Tampering | Unbound MCP tool input | mitigate | All 8 MCP tools have zod input schemas with enum + uuid + datetime validations; schema validation BEFORE library call; cross-field consistency (Pitfall 5) enforced in library |
| T-226-06-06 | Elevation of Privilege | Bypass approval gate | mitigate | gateApproval is the single library-level entry point; routes cannot bypass (library throws or returns approval_ref); approval-routing.test.ts proves matrix |
| T-226-06-07 | Denial of Service | Rate-limit on /v1/sales/* | accept | Standard tenant-scoped rate-limit applies (inherited from P201/P204); per-route rate-limit deferred to 226-07 if observability flags spikes |
| T-226-06-08 | Information Disclosure | share_link_token in API response | mitigate | createDealRoom + listDealRooms responses MUST mask share_link_token to `dr_*****`; never expose raw token in operator API; only public /share/dr/{token} path consumes raw token (226-05 owns) |
</threat_model>

<verification>
- [ ] `node --test test/sales/api/` green (4 test files: cross-tenant-denial, openapi-parity, approval-routing, v1-sales-routes-shape)
- [ ] `node --test test/sales/mcp/` green (8 test files; one per MCP tool)
- [ ] `find api/v1/sales -name "*.js" | wc -l` returns 49 (48 operator from this plan + 1 public from 226-05)
- [ ] `grep -c "^export const SALES_MCP_TOOLS" lib/markos/mcp/sales-tools.ts` returns 1
- [ ] `grep -E "requireSalesTenantContext" lib/markos/sales/api-helpers.ts` returns 1+ export
- [ ] `grep -E "SALES_API_ERROR_CODES" lib/markos/sales/api-helpers.ts` returns the const with >= 14 keys
- [ ] OpenAPI parity (test/sales/api/openapi-parity.test.ts) — every route ↔ contract path bidirectional verified
- [ ] Legacy regression: `npm test` and `node --test test/cdp-* test/crm-* test/channels-* test/conversion-* test/analytics-* test/sales/{wave-0,schema,rls,battlecards,proof-packs,objections,deal-briefs,handoffs,health-signals,quotes,proposals,approvals,deal-rooms,winloss}/` (P221-P225 + 226-01..05) green
</verification>

<success_criteria>
- 49 API route handlers under api/v1/sales/* shipped per D-43
- 8 MCP tools per D-44 with zod input/output schemas + A2 fallback
- lib/markos/sales/api-helpers.ts unifies tenant context + error mapping + audit wrapping (consistent across routes)
- Class-based approval routing (D-25..D-28) enforced at every state-mutating route
- Cross-tenant denial proven across all 49 routes (test/sales/api/cross-tenant-denial.test.ts)
- OpenAPI parity proven (test/sales/api/openapi-parity.test.ts) — every route ↔ contract path
- 18 contracts F-163..F-180 fully wired (no new contracts; ZERO new migrations; ZERO new dependencies)
- 12 test files green (4 API + 8 MCP); 226-VALIDATION.md rows 226-06-01, 226-06-02 flipped to ✅ green
- Legacy regression P100-P105 + P201 + P221-P225 + 226-01..05 green
</success_criteria>

<output>
After completion, create `.planning/phases/226-sales-enablement-deal-execution/226-06-SUMMARY.md` documenting: 49 API route files, 8 MCP tools, lib/markos/sales/api-helpers.ts (5 exports + 14 error codes), lib/markos/mcp/sales-tools.ts (8 tool handlers + 8 zod schemas + registerSalesMcpTools), 12 test files green, decision coverage map (D-43 + D-44 + D-45 + class-based approval matrix proven), A2 fallback verified, OpenAPI parity verified, open follow-ups for Wave 5 (226-07 wires UI workspaces consuming /v1/sales/* + Approval Inbox new entry types + Morning Brief signals + RLS hardening migration 161 + OpenAPI regen migration 162 + Playwright operator journeys + Chromatic visual regression + observability cron handlers).
</output>
