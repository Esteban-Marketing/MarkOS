---
id: 31-02-migration-promotion-controls
phase: 31-rollout-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 30-02-local-cloud-ingestion
  - 30-03-nextjs-auth-boundary
  - 31-01-rollout-telemetry-slos
files_modified:
  - onboarding/backend/runtime-context.cjs
  - onboarding/backend/handlers.cjs
  - api/migrate.js
  - .planning/phases/31-rollout-hardening/31-MIGRATION-CHECKPOINTS.json
  - TECH-MAP.md
  - test/onboarding-server.test.js
autonomous: true
requirements:
  - RLH-02
user_setup: []
must_haves:
  truths:
    - Rollout mode progresses only through dry-run -> dual-write -> cloud-primary per D-31-03.
    - Each promotion requires an owner sign-off record and rollback-ready checkpoint per D-31-04.
    - Hosted migration endpoints expose the active rollout mode and refuse unsafe promotion attempts.
  artifacts:
    - onboarding/backend/runtime-context.cjs exports rollout mode helpers and validation for promotion checkpoints.
    - .planning/phases/31-rollout-hardening/31-MIGRATION-CHECKPOINTS.json defines the operator sign-off record schema for transitions.
    - test/onboarding-server.test.js proves dry-run determinism, dual-write promotion gating, and cloud-primary rollback metadata requirements.
  key_links:
    - api/migrate.js -> runtime-context.cjs via hosted auth plus rollout mode validation.
    - handlers.cjs -> 31-MIGRATION-CHECKPOINTS.json via transition record loading and enforcement.
---

<objective>
Make migration rollout progression explicit, auditable, and rollback-safe.

Purpose: phase 30 proved dry-run and idempotent ingest behavior; phase 31 must turn that into an operator-safe promotion workflow instead of an implicit convention.
Output: rollout mode contract, machine-readable checkpoint file, guarded migration behavior, and transition tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/31-rollout-hardening/31-CONTEXT.md
@.planning/phases/31-rollout-hardening/31-RESEARCH.md
@.planning/phases/30-markosdb-migration/30-02-PLAN.md
@.planning/phases/30-markosdb-migration/30-03-PLAN.md
@onboarding/backend/runtime-context.cjs
@onboarding/backend/handlers.cjs
@api/migrate.js
@test/onboarding-server.test.js

<interfaces>
From onboarding/backend/runtime-context.cjs:
```javascript
function createRuntimeContext(env = process.env)
function requireHostedSupabaseAuth({ req, runtimeContext, operation, requiredProjectSlug = null, env = process.env })
module.exports = {
  createRuntimeContext,
  getMarkosdbAccessMatrix,
  requireHostedSupabaseAuth,
  resolveRequestedProjectSlugFromRequest,
}
```

From onboarding/backend/handlers.cjs:
```javascript
async function handleMarkosdbMigration(req, res)
async function handleConfig(req, res)
async function handleStatus(req, res)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define rollout mode and sign-off record contracts</name>
  <read_first>
    - .planning/phases/31-rollout-hardening/31-CONTEXT.md
    - onboarding/backend/runtime-context.cjs
    - api/migrate.js
  </read_first>
  <files>onboarding/backend/runtime-context.cjs, .planning/phases/31-rollout-hardening/31-MIGRATION-CHECKPOINTS.json</files>
  <behavior>
    - The only valid promotion path is dry-run -> dual-write -> cloud-primary per D-31-03.
    - Every transition record contains an owner, approval timestamp, checkpoint id, verification reference, and rollback details per D-31-04.
    - Invalid mode names or skipped promotions are rejected before any migration write occurs.
  </behavior>
  <action>Add a rollout mode contract to onboarding/backend/runtime-context.cjs using the exact enum values dry-run, dual-write, and cloud-primary with MARKOS_ROLLOUT_MODE as the single source of truth. Export helpers named getRolloutMode(env), loadMigrationCheckpoints(filePath), and assertRolloutPromotionAllowed({ currentMode, targetMode, projectSlug, checkpoints }). Create .planning/phases/31-rollout-hardening/31-MIGRATION-CHECKPOINTS.json as the machine-readable operator artifact with a top-level current_mode and transitions array. Each transition object must require from_mode, to_mode, project_slug, owner, approved_at, checkpoint_id, verification_ref, rollback_mode, rollback_command, and status. Reject skipped transitions such as dry-run -> cloud-primary, unknown modes, and records missing rollback metadata.</action>
  <verify>
    <automated>node --test test/onboarding-server.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The new rollout helpers exist and only allow the three literal mode names.
    - The migration checkpoint file documents every required sign-off key in a machine-readable format.
    - Promotion validation fails fast for skipped phases or incomplete rollback metadata.
  </acceptance_criteria>
  <done>Migration promotions now have an explicit contract instead of relying on undocumented operator memory.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce migration promotion rules in runtime surfaces</name>
  <read_first>
    - onboarding/backend/handlers.cjs
    - api/migrate.js
    - test/onboarding-server.test.js
    - TECH-MAP.md
  </read_first>
  <files>onboarding/backend/handlers.cjs, api/migrate.js, TECH-MAP.md, test/onboarding-server.test.js</files>
  <behavior>
    - /migrate/local-to-cloud dry-runs remain deterministic.
    - dual-write requires a matching approved dry-run checkpoint for the same project_slug.
    - cloud-primary requires an approved dual-write checkpoint and rollback metadata for the same project_slug.
  </behavior>
  <action>Update handleMarkosdbMigration and api/migrate.js to load the checkpoint file, enforce the current promotion rules, and expose rollout_mode plus promotion_checkpoint details from /config and /status responses. Preserve local compatibility reads and replay-safe upserts from phase 30; do not add destructive delete behavior. The migration handler must refuse write attempts that target dual-write or cloud-primary without an approved transition record for the requested project_slug. Extend TECH-MAP.md with the exact mode progression and the required checkpoint keys. Add onboarding-server tests that cover: deterministic dry-run still passing, dual-write denial when sign-off is missing, dual-write success when dry-run sign-off exists, and cloud-primary denial when rollback metadata is absent.</action>
  <verify>
    <automated>node --test test/onboarding-server.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Hosted migration writes are blocked when the checkpoint record is incomplete or out of order.
    - /config and /status expose the active rollout_mode so operators can verify runtime state.
    - Existing dry-run and replay-idempotent behavior remains green after the promotion guards are added.
  </acceptance_criteria>
  <done>The migration path is rollout-safe, rollback-aware, and externally observable from runtime endpoints.</done>
</task>

</tasks>

<verification>
Run node --test test/onboarding-server.test.js and confirm the migration suite covers dry-run determinism, replay safety, and mode promotion guardrails without altering hosted auth behavior.
</verification>

<success_criteria>
- MARKOS_ROLLOUT_MODE drives a three-step promotion model with no skipped transitions.
- Each migration promotion is tied to a checkpoint record containing owner sign-off and rollback details.
- /config, /status, and /migrate/local-to-cloud all reflect and enforce the same rollout mode contract.
</success_criteria>

<output>
After completion, create .planning/phases/31-rollout-hardening/31-02-SUMMARY.md
</output>
