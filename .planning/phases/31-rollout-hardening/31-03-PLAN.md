---
id: 31-03-security-retention-guards
phase: 31-rollout-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 31-02-migration-promotion-controls
files_modified:
  - onboarding/backend/runtime-context.cjs
  - onboarding/backend/handlers.cjs
  - onboarding/backend/agents/telemetry.cjs
  - README.md
  - TECH-MAP.md
  - test/onboarding-server.test.js
  - test/protocol.test.js
autonomous: true
requirements:
  - RLH-03
user_setup: []
must_haves:
  truths:
    - Hosted and rollout-sensitive operations fail fast when required secrets are missing per D-31-05.
    - Logs and telemetry never emit raw bearer tokens, API keys, or secret-like fields.
    - Retention bounds for rollout logs and reports are explicit, code-backed, and docs-tested.
  artifacts:
    - onboarding/backend/runtime-context.cjs exports secret validation, redaction, and retention policy helpers.
    - onboarding/backend/agents/telemetry.cjs sanitizes rollout telemetry properties before sending them.
    - README.md, TECH-MAP.md, and test/protocol.test.js stay aligned on the retention and redaction policy.
  key_links:
    - handlers.cjs -> runtime-context.cjs via validateRequiredSecrets and redactSensitive before privileged responses or error logs.
    - telemetry.cjs -> runtime-context.cjs via shared redaction rules applied before capture.
---

<objective>
Enforce production-grade secret, redaction, and retention guardrails across rollout-sensitive paths.

Purpose: the repo already has hosted auth boundaries, but Phase 31 must harden the runtime so misconfiguration or noisy error payloads cannot leak secrets during rollout.
Output: centralized secret validation, reusable redaction logic, fixed retention constants, and parity tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-rollout-hardening/31-CONTEXT.md
@.planning/phases/31-rollout-hardening/31-RESEARCH.md
@onboarding/backend/runtime-context.cjs
@onboarding/backend/handlers.cjs
@onboarding/backend/agents/telemetry.cjs
@README.md
@TECH-MAP.md
@test/onboarding-server.test.js
@test/protocol.test.js

<interfaces>
From onboarding/backend/runtime-context.cjs:
```javascript
function createRuntimeContext(env = process.env)
function getTelemetryPreference(env = process.env)
function requireHostedSupabaseAuth({ req, runtimeContext, operation, requiredProjectSlug = null, env = process.env })
```

From onboarding/backend/agents/telemetry.cjs:
```javascript
function capture(eventName, properties = {})
function captureExecutionCheckpoint(eventName, properties = {})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add centralized secret validation, redaction, and retention helpers</name>
  <read_first>
    - .planning/phases/31-rollout-hardening/31-CONTEXT.md
    - onboarding/backend/runtime-context.cjs
    - onboarding/backend/agents/telemetry.cjs
  </read_first>
  <files>onboarding/backend/runtime-context.cjs, onboarding/backend/agents/telemetry.cjs</files>
  <behavior>
    - Hosted config/status/migration operations fail fast when MARKOS_SUPABASE_AUD is missing in hosted rollout environments.
    - /linear/sync fails fast when LINEAR_API_KEY is absent instead of relying on downstream setup errors.
    - /submit, /approve, and /campaign/result each validate their own required secret set from the same matrix-backed policy surface.
    - Sensitive keys are replaced with [REDACTED] before they reach logs or telemetry.
  </behavior>
  <action>In onboarding/backend/runtime-context.cjs, add a REQUIRED_SECRET_MATRIX keyed by operation with explicit requirements for config_read, status_read, migration_write, linear_sync_write, submit_write, approve_write, campaign_result_write, and telemetry_write. Export validateRequiredSecrets({ runtimeMode, operation, env }) and redactSensitive(value). The matrix must be the single source of truth for every handler operation Task 2 validates against, including /submit, /approve, and /campaign/result. redactSensitive must sanitize objects, arrays, and strings by masking the keys authorization, token, access_token, refresh_token, POSTHOG_API_KEY, LINEAR_API_KEY, SUPABASE_SERVICE_ROLE_KEY, UPSTASH_VECTOR_REST_TOKEN, and x-markos-project-slug when they appear in raw payloads. Define a fixed RETENTION_POLICY export with exact values server_logs_days: 14, rollout_reports_days: 30, and migration_checkpoint_days: 90. Update telemetry.cjs so capture and captureRolloutEndpointEvent pass properties through redactSensitive before emission.</action>
  <verify>
    <automated>node --test test/onboarding-server.test.js</automated>
  </verify>
  <acceptance_criteria>
    - runtime-context.cjs exports REQUIRED_SECRET_MATRIX, validateRequiredSecrets, redactSensitive, and RETENTION_POLICY.
    - REQUIRED_SECRET_MATRIX includes config_read, status_read, migration_write, linear_sync_write, submit_write, approve_write, campaign_result_write, and telemetry_write so every guarded handler in Task 2 validates against a declared operation.
    - The redaction helper masks the listed secret-bearing keys with the literal [REDACTED].
    - Telemetry emission paths use the shared redaction helper instead of ad hoc masking.
  </acceptance_criteria>
  <done>Secret and redaction policy is centralized and ready to be enforced by handlers and docs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce the new policy in handlers and documentation</name>
  <read_first>
    - onboarding/backend/handlers.cjs
    - README.md
    - TECH-MAP.md
    - test/onboarding-server.test.js
    - test/protocol.test.js
  </read_first>
  <files>onboarding/backend/handlers.cjs, README.md, TECH-MAP.md, test/onboarding-server.test.js, test/protocol.test.js</files>
  <behavior>
    - Privileged routes validate required secrets before performing work using only operation names declared in REQUIRED_SECRET_MATRIX.
    - Error and warning paths redact sensitive payload fragments before console or telemetry use.
    - Documentation and protocol tests stay aligned on the exact retention numbers and redaction guarantees.
  </behavior>
  <action>Apply validateRequiredSecrets at the entry points for handleLinearSync, handleCampaignResult, handleSubmit, handleApprove, and handleMarkosdbMigration using only these matrix-backed operation names: linear_sync_write, campaign_result_write, submit_write, approve_write, and migration_write. Replace any direct console.error or telemetry property forwarding on those paths with redacted payloads produced by redactSensitive. Update README.md and TECH-MAP.md to publish the fixed retention bounds and the fail-fast secret policy required by D-31-05. Extend onboarding-server and protocol tests to prove: missing LINEAR_API_KEY produces a deterministic setup denial before downstream work; hosted migration/config/status respect the secret matrix; submit, approve, and campaign-result enforcement use declared matrix operations; and docs continue to contain the exact 14 / 30 / 90 day retention values.</action>
  <verify>
    <automated>node --test test/onboarding-server.test.js; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/protocol.test.js; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }</automated>
  </verify>
  <acceptance_criteria>
    - Privileged handlers deny execution early when required secrets are missing.
    - Tests cover matrix-backed enforcement for linear_sync_write, campaign_result_write, submit_write, approve_write, and migration_write without relying on undeclared operation names.
    - Logged or emitted payloads in the covered handlers no longer expose raw token or API-key values.
    - README.md, TECH-MAP.md, and protocol tests all assert the same retention numbers and policy language.
  </acceptance_criteria>
  <done>The rollout runtime enforces fail-fast secret policy, consistent redaction, and fixed retention bounds.</done>
</task>

</tasks>

<verification>
Run node --test test/onboarding-server.test.js; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; node --test test/protocol.test.js; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }. Confirm both suites remain green while the new secret/retention contract is enforced from a single runtime-policy source.
</verification>

<success_criteria>
- Missing secrets are rejected before rollout-sensitive work starts.
- Redaction is centralized and reused by both telemetry and handler error paths.
- Retention bounds are code-backed and docs-tested instead of being narrative-only guidance.
</success_criteria>

<output>
After completion, create .planning/phases/31-rollout-hardening/31-03-SUMMARY.md
</output>
