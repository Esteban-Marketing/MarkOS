---
id: 31-01-rollout-telemetry-slos
phase: 31-rollout-hardening
plan: 01
type: execute
wave: 1
depends_on:
  - 29-01-linear-sync-client
  - 29-02-campaign-result-loop
  - 30-03-nextjs-auth-boundary
files_modified:
  - onboarding/backend/agents/telemetry.cjs
  - onboarding/backend/handlers.cjs
  - README.md
  - TECH-MAP.md
  - test/onboarding-server.test.js
autonomous: true
requirements:
  - RLH-01
user_setup: []
must_haves:
  truths:
    - Operators can measure availability and p95 latency for /submit, /approve, /linear/sync, and /campaign/result from first-party telemetry rather than inferred logs.
    - /submit and /approve remain the strictest rollout gates, while /linear/sync and /campaign/result stay visible with looser SLOs per D-31-01 and D-31-02.
    - SLO numbers in runtime docs match the emitted telemetry contract and the tests that verify it.
  artifacts:
    - onboarding/backend/agents/telemetry.cjs exports a rollout endpoint telemetry helper and a single source of truth for endpoint SLO metadata.
    - onboarding/backend/handlers.cjs emits one stable rollout endpoint event for every /submit, /approve, /linear/sync, and /campaign/result request.
    - test/onboarding-server.test.js asserts telemetry payload shape and outcome mapping for the four rollout endpoints.
  key_links:
    - handlers.cjs -> telemetry.cjs via rollout endpoint event emission with endpoint, tier, state, status_code, duration_ms, and runtime_mode.
    - telemetry.cjs -> README.md and TECH-MAP.md via a shared SLO table that must stay numerically aligned.
---

<objective>
Make rollout reliability measurable for the four Phase 31 critical endpoints.

Purpose: convert the documented SLO policy into a runtime contract that can drive rollout decisions without manual log forensics.
Output: endpoint SLO constants, handler-level telemetry emission, and docs/tests that stay in sync.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-rollout-hardening/31-CONTEXT.md
@.planning/phases/31-rollout-hardening/31-RESEARCH.md
@onboarding/backend/agents/telemetry.cjs
@onboarding/backend/handlers.cjs
@test/onboarding-server.test.js
@README.md
@TECH-MAP.md

<interfaces>
From onboarding/backend/agents/telemetry.cjs:
```javascript
function capture(eventName, properties = {})
function captureExecutionCheckpoint(eventName, properties = {})
module.exports = { capture, captureExecutionCheckpoint, shutdown };
```

From onboarding/backend/handlers.cjs:
```javascript
async function handleLinearSync(req, res)
async function handleCampaignResult(req, res)
async function handleSubmit(req, res)
async function handleApprove(req, res)
module.exports = {
  handleConfig,
  handleStatus,
  handleSubmit,
  handleRegenerate,
  handleApprove,
  handleMarkosdbMigration,
  handleLinearSync,
  handleCampaignResult,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the rollout endpoint telemetry contract</name>
  <read_first>
    - .planning/phases/31-rollout-hardening/31-CONTEXT.md
    - .planning/phases/31-rollout-hardening/31-RESEARCH.md
    - onboarding/backend/agents/telemetry.cjs
    - README.md
  </read_first>
  <files>onboarding/backend/agents/telemetry.cjs, README.md, TECH-MAP.md</files>
  <behavior>
    - /submit has the strictest availability threshold except /approve and exposes its 99.5 / 1500ms policy as code-backed metadata.
    - /approve exposes 99.9 / 900ms as a release-blocking local-write gate.
    - /linear/sync and /campaign/result keep visible but looser SLO metadata per D-31-02.
  </behavior>
  <action>Create a single exported SLO registry in onboarding/backend/agents/telemetry.cjs for the four rollout endpoints with exact values: /submit => tier critical, availability 99.5, p95_ms 1500; /approve => tier critical, availability 99.9, p95_ms 900; /linear/sync => tier standard, availability 99.0, p95_ms 1200; /campaign/result => tier standard, availability 99.5, p95_ms 800. Add a helper named captureRolloutEndpointEvent(endpoint, properties) that emits one stable event name rollout_endpoint_observed with properties endpoint, endpoint_tier, slo_target_availability, slo_target_p95_ms, outcome_state, status_code, duration_ms, runtime_mode, and project_slug_hash. Hash the slug before sending telemetry; never emit raw project_slug in the rollout endpoint helper. Update README.md and TECH-MAP.md so the SLO table matches the exported constants exactly, including the tier distinction mandated by D-31-01 and D-31-02.</action>
  <verify>
    <automated>node --test test/onboarding-server.test.js</automated>
  </verify>
  <acceptance_criteria>
    - onboarding/backend/agents/telemetry.cjs exports both the SLO registry and captureRolloutEndpointEvent.
    - The four endpoint entries carry the exact threshold numbers listed in the action text.
    - README.md and TECH-MAP.md repeat the same endpoint names and threshold values with no numeric drift.
  </acceptance_criteria>
  <done>The endpoint SLO policy is code-defined, documented, and ready for handler instrumentation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Instrument the four rollout endpoints with stable outcome timing events</name>
  <read_first>
    - onboarding/backend/handlers.cjs
    - onboarding/backend/agents/telemetry.cjs
    - test/onboarding-server.test.js
  </read_first>
  <files>onboarding/backend/handlers.cjs, test/onboarding-server.test.js</files>
  <behavior>
    - Each of /submit, /approve, /linear/sync, and /campaign/result emits one rollout telemetry event for success and one for handled failure paths.
    - /approve maps its existing outcome.state values directly into telemetry outcome_state.
    - Unhandled exceptions still emit rollout_endpoint_observed with outcome_state=failure before the response returns.
  </behavior>
  <action>Wrap handleSubmit, handleApprove, handleLinearSync, and handleCampaignResult with start/stop timing and call captureRolloutEndpointEvent exactly once per request. Use the literal endpoint values /submit, /approve, /linear/sync, and /campaign/result. Derive status_code from the final response path. Map outcome_state as follows: /approve uses payload.outcome.state; /submit, /linear/sync, and /campaign/result use success for 2xx responses and failure for 4xx/5xx responses; if an explicit degraded or warning state is added to those handlers later, preserve it rather than collapsing to success. Extend test/onboarding-server.test.js with mocked telemetry assertions proving the event payload contains endpoint, outcome_state, status_code, duration_ms as a non-negative number, runtime_mode, and hashed project identity for all four handlers.</action>
  <verify>
    <automated>node --test test/onboarding-server.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The four rollout handlers emit rollout_endpoint_observed on both happy and error paths.
    - No handler emits raw project_slug through the new telemetry helper.
    - Tests fail if one of the four endpoints stops emitting the stable event payload.
  </acceptance_criteria>
  <done>Runtime telemetry now captures measurable endpoint reliability evidence for all rollout-critical APIs.</done>
</task>

</tasks>

<verification>
Run node --test test/onboarding-server.test.js and confirm the added telemetry assertions cover /submit, /approve, /linear/sync, and /campaign/result without breaking existing hosted/local guardrail tests.
</verification>

<success_criteria>
- All four rollout endpoints emit rollout_endpoint_observed with stable timing and outcome fields.
- The SLO numbers in code, README.md, and TECH-MAP.md are identical.
- Test coverage catches future drift in endpoint telemetry emission or payload shape.
</success_criteria>

<output>
After completion, create .planning/phases/31-rollout-hardening/31-01-SUMMARY.md
</output>
