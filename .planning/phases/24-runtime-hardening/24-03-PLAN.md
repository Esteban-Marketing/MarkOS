---
id: "24-03-runtime-parity-coverage"
wave: 2
depends_on: ["24-01-runtime-split-audit", "24-02-config-environment-centralization"]
discipline: engineering
requirements: ["RTH-01", "RTH-03"]
files_modified:
  - test/onboarding-server.test.js
  - test/protocol.test.js
  - onboarding/backend/handlers.cjs
  - onboarding/backend/server.cjs
  - api/approve.js
  - api/submit.js
  - api/status.js
mir_files_referenced: []
tracking_required: false
requires_human_approval: false
autonomous: true
neuro_dimension: false
objective: "Add test coverage that proves the shared onboarding runtime behaves consistently when invoked through the local server and the hosted API wrappers."
must_haves:
  - Shared handler behavior is asserted across both entrypoint styles
  - Success and failure paths cover environment-sensitive behavior
  - Hosted limitations are represented either as tests or explicit documented exclusions
  - Runtime hardening claims are backed by executable coverage
---

# Plan 24-03: Runtime Parity Coverage

## Wave 2 — Prove the Runtime Contract

<task id="1" name="Add parity tests for shared request flows">
  <read_first>
    - test/onboarding-server.test.js
    - onboarding/backend/handlers.cjs
    - api/submit.js
    - api/status.js
  </read_first>
  <action>
    Add or extend tests so the same handler-level flows are exercised through:
    - the local server entrypoint
    - the API wrapper entrypoints
    Focus on config fetch, submit, status, regenerate, and other shared flows where wrapper drift would create user-visible inconsistency.
  </action>
  <acceptance_criteria>
    - Core shared flows are covered in both entrypoint modes
    - Differences between local and hosted behavior are explicit in assertions rather than hidden in test setup
  </acceptance_criteria>
</task>

<task id="2" name="Cover guarded persistence and failure modes">
  <read_first>
    - api/approve.js
    - onboarding/backend/write-mir.cjs
    - onboarding/backend/handlers.cjs
  </read_first>
  <action>
    Add tests for approve/write behavior under both supported and unsupported runtime conditions, including cases where hosted execution should reject or constrain filesystem-dependent operations.
  </action>
  <acceptance_criteria>
    - Persistence-sensitive failure modes are tested instead of implied
    - Hosted-path behavior is validated as supported or intentionally rejected
  </acceptance_criteria>
</task>

<task id="3" name="Lock regression coverage around centralized runtime helpers">
  <read_first>
    - onboarding/backend/server.cjs
    - onboarding/backend/path-constants.cjs
    - .planning/research/PITFALLS.md
  </read_first>
  <action>
    Add regression coverage around any new runtime helpers created in 24-02 so future changes cannot reintroduce entrypoint drift, hidden config precedence, or silent hosted/local mismatches.
  </action>
  <acceptance_criteria>
    - New shared runtime helpers have direct regression coverage
    - The most likely drift vectors from the research docs are guarded by tests
  </acceptance_criteria>
</task>

## Verification

`npm test` or the equivalent targeted runtime test command should demonstrate that both entrypoint styles honor the same shared runtime rules and that unsupported persistence paths fail predictably.
