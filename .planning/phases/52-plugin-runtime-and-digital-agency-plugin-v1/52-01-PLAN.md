---
phase: 52-plugin-runtime-and-digital-agency-plugin-v1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/markos/plugins/contracts.js
  - lib/markos/plugins/registry.js
  - lib/markos/plugins/loader.js
  - onboarding/backend/runtime-context.cjs
  - bin/cli-runtime.cjs
  - supabase/migrations/52_plugin_runtime_foundation.sql
  - test/plugin-registry.test.js
  - test/plugin-control.test.js
autonomous: true
requirements:
  - PLG-DA-01

must_haves:
  truths:
    - "Plugin runtime can register first-party plugins deterministically at boot with fail-closed error boundaries."
    - "Tenant-scoped plugin enablement and capability grants are persisted and enforceable without cross-tenant leakage."
    - "Disabled plugin surfaces are unreachable while enabled authorized plugin surfaces can resolve."
  artifacts:
    - path: "lib/markos/plugins/contracts.js"
      provides: "Canonical plugin contract and capability schema for runtime validation."
      exports:
        - "PLUGIN_CONTRACT_VERSION"
        - "validatePluginContract"
    - path: "lib/markos/plugins/registry.js"
      provides: "Deterministic register/list/resolve operations with immutable plugin metadata."
      exports:
        - "registerPlugin"
        - "listPlugins"
        - "resolvePlugin"
    - path: "supabase/migrations/52_plugin_runtime_foundation.sql"
      provides: "Tenant-plugin config and capability grant persistence with RLS policies."
      exports: []
    - path: "test/plugin-registry.test.js"
      provides: "Automated runtime contract and loader boundary coverage."
      exports: []
  key_links:
    - from: "bin/cli-runtime.cjs"
      to: "lib/markos/plugins/loader.js"
      via: "boot-time plugin loading"
      pattern: "loadPlugins|registerPlugin|validatePluginContract"
    - from: "onboarding/backend/runtime-context.cjs"
      to: "supabase/migrations/52_plugin_runtime_foundation.sql"
      via: "tenant_id-scoped plugin enablement + capability lookup"
      pattern: "tenant_id|plugin_tenant_config|plugin_tenant_capability_grants"
    - from: "test/plugin-control.test.js"
      to: "supabase/migrations/52_plugin_runtime_foundation.sql"
      via: "disable returns not found and enable grants route reachability"
      pattern: "enabled|404|capability"
---

<objective>
Establish the Phase 52 plugin runtime foundation and tenant control persistence contracts.

Purpose: Deliver PLG-DA-01 baseline primitives before Digital Agency workflow implementation.
Output: Plugin contracts, registry/loader, tenant plugin config schema, and Wave 0 runtime/control tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/52-plugin-runtime-and-digital-agency-plugin-v1/52-CONTEXT.md
@.planning/phases/52-plugin-runtime-and-digital-agency-plugin-v1/52-RESEARCH.md
@.planning/phases/51-multi-tenant-foundation-and-authorization/51-02-SUMMARY.md
@.planning/phases/51-multi-tenant-foundation-and-authorization/51-04-SUMMARY.md
@onboarding/backend/runtime-context.cjs
@lib/markos/telemetry/events.ts
@supabase/migrations/51_multi_tenant_foundation.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 52-01-01: Create Wave 0 plugin runtime/control test scaffolds (per D-01, D-02, D-04)</name>
  <files>test/plugin-registry.test.js, test/plugin-control.test.js</files>
  <behavior>
    - Test 1: Invalid plugin contracts are rejected by loader validation with deterministic error codes.
    - Test 2: Plugin disabled for tenant returns not found at runtime boundary.
    - Test 3: Plugin enabled with required capability grants allows route resolution for authorized role.
  </behavior>
  <action>
Create initial failing node:test suites for plugin contract validation, deterministic registration order, tenant enable/disable gates, and capability checks. Keep tests scoped to first-party plugins only and explicitly assert that marketplace or third-party install paths are unsupported in this phase.
  </action>
  <verify>
    <automated>node --test test/plugin-registry.test.js test/plugin-control.test.js</automated>
  </verify>
  <done>
Wave 0 tests exist and fail for missing runtime/control implementation, establishing Nyquist coverage for PLG-DA-01.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 52-01-02: Implement in-process plugin contracts, registry, and loader boot integration (per D-01, D-02)</name>
  <files>lib/markos/plugins/contracts.js, lib/markos/plugins/registry.js, lib/markos/plugins/loader.js, bin/cli-runtime.cjs</files>
  <behavior>
    - Test 1: Loader registers plugin once, preserves deterministic registration order, and isolates plugin boot errors.
    - Test 2: Capability declarations are normalized and immutable after registration.
    - Test 3: Boot succeeds when one plugin fails validation, with failure recorded and process remaining available.
  </behavior>
  <action>
Implement in-process CommonJS plugin runtime primitives: contract validator, registry API, and loader with per-plugin error boundaries. Wire `bin/cli-runtime.cjs` to load plugins at server boot without blocking startup on a single plugin failure, and keep behavior fail-closed for invalid manifests.
  </action>
  <verify>
    <automated>node --test test/plugin-registry.test.js</automated>
  </verify>
  <done>
Runtime supports deterministic plugin registration and fail-closed loader boundaries required for PLG-DA-01.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 52-01-03: Add tenant plugin enablement and capability grant persistence with RLS (per D-04)</name>
  <files>supabase/migrations/52_plugin_runtime_foundation.sql, onboarding/backend/runtime-context.cjs, test/plugin-control.test.js</files>
  <behavior>
    - Test 1: Tenant A capability grants never authorize Tenant B requests.
    - Test 2: Disabled plugin state forces not found regardless of route existence.
    - Test 3: Capability grant rows are immutable append-only records with deterministic lookup by tenant_id + plugin_id.
  </behavior>
  <action>
Add migration for `plugin_tenant_config`, `plugin_tenant_capability_grants`, and `plugin_entitlements_by_plan` with tenant_id-indexed RLS policies aligned to Phase 51 deny-by-default conventions. Extend hosted runtime context helpers to read tenant plugin state and capability grants for downstream middleware checks.
  </action>
  <verify>
    <automated>node --test test/plugin-control.test.js</automated>
  </verify>
  <done>
Tenant-scoped plugin enablement and capability contract persistence are enforceable and regression-tested.
  </done>
</task>

</tasks>

<verification>
Run `node --test test/plugin-registry.test.js test/plugin-control.test.js` after each task and once at plan completion.
</verification>

<success_criteria>
Phase 52 has a concrete plugin runtime/control foundation with automated contract coverage and no out-of-scope marketplace complexity.
</success_criteria>

<output>
After completion, create `.planning/phases/52-plugin-runtime-and-digital-agency-plugin-v1/52-01-SUMMARY.md`
</output>
