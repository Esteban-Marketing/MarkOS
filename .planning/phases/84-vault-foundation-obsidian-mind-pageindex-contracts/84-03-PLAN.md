---
phase: 84-vault-foundation-obsidian-mind-pageindex-contracts
plan: 03
type: execute
wave: 3
depends_on: [84-02]
files_modified:
  - onboarding/backend/vector-store-client.cjs
  - bin/ensure-vector.cjs
  - package.json
  - test/phase-84/cutover-no-upstash.test.js
  - test/phase-84/cutover-parity.test.js
  - test/phase-84/provider-trace-contract.test.js
  - scripts/phase-84/static-cutover-scan.cjs
  - test/phase-84/isolation-matrix.test.js
  - .planning/phases/84-vault-foundation-obsidian-mind-pageindex-contracts/84-VALIDATION.md
autonomous: true
requirements: [VAULT-01, VAULT-02, VAULT-03]
must_haves:
  truths:
    - "Legacy Upstash retrieval dependency is removed from active retrieval paths and readiness contracts (D-05)."
    - "Cutover evidence proves deterministic retrieval-contract parity for required scenarios before closure (D-06)."
    - "Tenant isolation proof matrix validates Supabase RLS + PageIndex scoped-query boundaries with unit and integration checks (D-07, D-08)."
  artifacts:
    - path: test/phase-84/cutover-no-upstash.test.js
      provides: Hard-cutover no-upstash dependency gate.
    - path: test/phase-84/isolation-matrix.test.js
      provides: Tenant isolation matrix with positive and negative assertions across unit and integration lanes.
    - path: .planning/phases/84-vault-foundation-obsidian-mind-pageindex-contracts/84-VALIDATION.md
      provides: Cutover parity evidence and readiness contract closure ledger.
  key_links:
    - from: onboarding/backend/vector-store-client.cjs
      to: test/phase-84/cutover-no-upstash.test.js
      via: static/runtime checks proving no retrieval dependence on Upstash after cutover per D-05.
    - from: onboarding/backend/pageindex/pageindex-client.cjs
      to: test/phase-84/isolation-matrix.test.js
      via: tenant-scoped doc allow-list and fail-closed behavior assertions per D-07/D-08.
    - from: bin/ensure-vector.cjs
      to: .planning/phases/84-vault-foundation-obsidian-mind-pageindex-contracts/84-VALIDATION.md
      via: readiness contract update evidence for PageIndex-era health status after hard cutover.
---

<objective>
Complete Phase 84 cutover validation by removing legacy Upstash retrieval dependence and proving tenant-safe readiness for milestone progression.

Purpose: close the contract foundation with enforceable hard-cutover and isolation evidence before Phase 85+ implementation work.
Output: no-upstash cutover enforcement, tenant isolation matrix tests, and readiness/validation closure artifact.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/84-vault-foundation-obsidian-mind-pageindex-contracts/84-CONTEXT.md
@.planning/phases/84-vault-foundation-obsidian-mind-pageindex-contracts/84-RESEARCH.md
@.planning/phases/84-vault-foundation-obsidian-mind-pageindex-contracts/84-02-PLAN.md
@onboarding/backend/vector-store-client.cjs
@bin/ensure-vector.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce hard cutover and remove Upstash retrieval dependency</name>
  <files>onboarding/backend/vector-store-client.cjs, bin/ensure-vector.cjs, package.json, test/phase-84/cutover-no-upstash.test.js</files>
  <behavior>
    - Test 1: retrieval suite passes with `UPSTASH_VECTOR_REST_URL` and `UPSTASH_VECTOR_REST_TOKEN` unset.
    - Test 2: active retrieval paths do not import `@upstash/vector` or call `getUpstashIndex`.
    - Test 3: readiness checks report PageIndex/Supabase posture without requiring Upstash-ready state.
    - Test 4: static cutover scan output is emitted and fails when legacy retrieval symbols are present in active paths.
  </behavior>
  <action>Execute D-05 hard cutover by removing Upstash retrieval path usage and updating readiness contracts to PageIndex-era expectations. Add explicit no-upstash dependency tests and keep rollback strategy commit-based (no dual-run behavior). Avoid introducing ingestion-pipeline or role-view scope from Phases 85/87.</action>
  <verify>
    <automated>node --test test/phase-84/cutover-no-upstash.test.js</automated>
    <automated>node scripts/phase-84/static-cutover-scan.cjs</automated>
  </verify>
  <done>Runtime retrieval no longer depends on Upstash and cutover gates fail if legacy retrieval path is reintroduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build tenant isolation proof matrix with unit + integration assertions</name>
  <files>test/phase-84/isolation-matrix.test.js, onboarding/backend/pageindex/pageindex-client.cjs</files>
  <behavior>
    - Test 1: tenant A retrieves only tenant A authorized documents.
    - Test 2: cross-tenant doc injection attempts are denied or filtered before provider calls.
    - Test 3: missing tenant scope fails closed and no provider query executes.
    - Test 4: mixed allow-list contamination is rejected and logged as invariant breach.
  </behavior>
  <action>Implement D-07 and D-08 isolation matrix coverage that combines unit and integration checks for Supabase RLS prefilter and PageIndex scoped-query boundaries. Include negative-path invariants from research and ensure deterministic fail-closed behavior for missing/invalid tenant scope.</action>
  <verify>
    <automated>node --test test/phase-84/isolation-matrix.test.js</automated>
  </verify>
  <done>Isolation matrix proves no cross-tenant retrieval leakage and includes required unit + integration assertions.</done>
</task>

<task type="auto">
  <name>Task 3: Publish cutover parity and readiness closure evidence</name>
  <files>.planning/phases/84-vault-foundation-obsidian-mind-pageindex-contracts/84-VALIDATION.md</files>
  <action>Create validation ledger that records D-06 retrieval parity scenarios (reason/apply/iterate contract parity checks), hard-cutover evidence, provider invocation trace evidence, fail-closed prefilter failure semantics, and isolation matrix results. Capture command outputs and explicit pass/fail status so Phase 84 closes with auditable evidence and no hidden dependencies.</action>
  <verify>
    <automated>node --test test/phase-84/cutover-no-upstash.test.js test/phase-84/cutover-parity.test.js test/phase-84/isolation-matrix.test.js test/phase-84/pageindex-adapter.test.js test/phase-84/provider-trace-contract.test.js</automated>
  </verify>
  <done>Validation artifact documents parity, static scan results, provider invocation traces, fail-closed semantics, and isolation proof required for readiness contract updates.</done>
</task>

</tasks>

<verification>
- Run full Phase 84 gate lane: `node --test test/phase-84/*.test.js -x`
- Confirm hard-cutover, deterministic pathing, envelope validation, and isolation matrix tests are all present and passing.
</verification>

<success_criteria>
- D-05 and D-06 are satisfied with explicit no-upstash and parity evidence.
- D-07 and D-08 are satisfied with tenant isolation matrix unit + integration proof.
- Phase 84 closure package is execution-ready without adding Phase 85/87 scope.
</success_criteria>

<output>
After completion, create `.planning/phases/84-vault-foundation-obsidian-mind-pageindex-contracts/84-03-SUMMARY.md`
</output>
