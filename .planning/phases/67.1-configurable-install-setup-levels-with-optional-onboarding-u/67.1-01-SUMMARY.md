---
phase: 67.1
plan: 01
wave: 1
status: complete
requirements_completed:
  - BOOT-01
completed_at: 2026-04-11
---

# Phase 67.1 Plan 01 Summary

Implemented deterministic setup profile parsing and install-time profile persistence.

## Completed Tasks

- 67.1-01-01
  - Added profile parsing contract in `bin/cli-runtime.cjs` for `--profile full|cli|minimal`.
  - Added alias handling for `--cli-only` and `--minimal`.
  - Added conflict detection for ambiguous profile flags.
- 67.1-01-02
  - Added install profile resolution and component plan generation in `bin/install.cjs`.
  - Persisted `install_profile`, `profile_schema_version`, and `components` in `.markos-install-manifest.json`.
  - Added profile/component assertions in `test/install.test.js`.

## Verification

- `node --test test/install.test.js test/update.test.js` (pass)

## Notes

- Default behavior remains backward-safe (`full`) when no explicit profile is provided.
- `--no-onboarding` now acts as a component override without changing the selected profile.
