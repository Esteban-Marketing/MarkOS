---
phase: 91-filter-taxonomy-and-provider-contract
plan: "02"
type: execute
wave: 2
depends_on:
  - "91-01"
files_modified:
  - onboarding/backend/research/provider-routing-policy.cjs
  - onboarding/backend/research/context-pack-contract.cjs
  - onboarding/backend/research/patch-preview-policy.cjs
  - onboarding/backend/research/deep-research-envelope.cjs
  - test/phase-91/provider-routing-policy.test.js
  - test/phase-91/preview-patch-policy.test.js
autonomous: true
requirements:
  - DRT-02
  - DRT-08
  - DRT-11
must_haves:
  truths:
    - "internal approved knowledge is always the first authority layer in the research route"
    - "external outages or missing keys degrade to a valid internal-first response instead of hard-failing the request"
    - "operators can inspect active_filters, route_trace, evidence authority, and patch_preview before any approval decision"
    - "no Phase 91 code path permits auto-write or approval bypass"
  artifacts:
    - path: onboarding/backend/research/provider-routing-policy.cjs
      provides: deterministic internal-first route order, domain policy handling, skip/degraded statuses, and fallback warnings
    - path: onboarding/backend/research/context-pack-contract.cjs
      provides: ranked context_pack shape that merges literacy, MIR, MSP, company overrides, and optional external evidence into one preview-safe envelope
    - path: onboarding/backend/research/patch-preview-policy.cjs
      provides: preview-only artifact delta contract for literacy, MIR, and MSP updates with approval gating
    - path: test/phase-91/provider-routing-policy.test.js
      provides: route-order and degraded-fallback coverage for DRT-11
    - path: test/phase-91/preview-patch-policy.test.js
      provides: preview-only, internal-authority, and operator-inspection guardrail coverage for DRT-02 and DRT-08
  key_links:
    - from: onboarding/backend/research/provider-routing-policy.cjs
      to: lib/markos/llm/adapter.ts
      via: explicit separation between evidence-routing policy and generic LLM reliability fallback
    - from: onboarding/backend/research/provider-routing-policy.cjs
      to: onboarding/backend/scrapers/tavily-scraper.cjs
      via: external-enrichment stage inherits graceful degradation instead of crashing on provider absence
    - from: onboarding/backend/research/patch-preview-policy.cjs
      to: onboarding/backend/research/deep-research-envelope.cjs
      via: preview patch payload and approval block are embedded into the universal response envelope
---

<objective>
Lock the Phase 91 routing policy, evidence-authority rules, degraded fallback behavior, and preview-only patch posture so the contract is safe and operator-inspectable before deeper implementation phases.

Purpose: satisfy the internal-authority, pluggable-routing, and human-approval guardrails without prematurely building the full orchestrator.
Output: policy modules and tests that define route order, ranked context-pack semantics, and preview-only artifact delta behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-filter-taxonomy-and-provider-contract/91-CONTEXT.md
@.planning/phases/91-filter-taxonomy-and-provider-contract/91-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@onboarding/backend/scrapers/tavily-scraper.cjs
@onboarding/backend/enrichers/competitor-enricher.cjs
@lib/markos/llm/adapter.ts
@lib/markos/llm/fallback-chain.ts
@onboarding/backend/research/deep-research-envelope.cjs

<interfaces>
Current external evidence posture:
- tavily-scraper.cjs throws when the API key is absent or the network call fails
- competitor-enricher.cjs already degrades gracefully and returns success false with a reason when external evidence is unavailable

Current fallback boundary:
- fallback-chain.ts handles model-provider failover for reliability
- Phase 91 routing policy must remain a separate evidence-authority layer and must not be collapsed into generic LLM fallback

Phase 91 contract handoff from 91-01:
- deep-research-envelope.cjs provides the universal JSON request/response shell that this plan extends with route policy and preview metadata
</interfaces>
</context>

<threat_model>
- T-91-05 (Authority confusion): fresh web findings could silently override approved internal truth. Mitigation: label evidence as approved_internal, external_candidate, or synthesized and enforce internal authority precedence per D-03 and D-04.
- T-91-06 (Routing drift): developers may conflate model fallback with evidence order and break DRT-11. Mitigation: create a dedicated provider-routing-policy module with explicit stage order of markos_vault, markos_mcp, tavily, firecrawl, then openai_synthesis.
- T-91-07 (Unsafe mutation path): patch previews may accidentally become live writes. Mitigation: keep patch-preview-policy preview-only, set allow_write false, and require human approval in all cases per D-05 and D-06.
- T-91-08 (Provider unavailability): missing Tavily, Firecrawl, or synthesis access could invalidate the response contract. Mitigation: return a valid degraded response with warnings and internal-only context pack rather than throwing or emitting malformed JSON.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create failing policy tests for routing order, authority labels, and preview-only guardrails</name>
  <files>test/phase-91/provider-routing-policy.test.js, test/phase-91/preview-patch-policy.test.js</files>
  <behavior>
    - Route order is internal first, external second, synthesis last.
    - External provider absence returns degraded warnings but still yields a valid response contract.
    - approved_internal evidence wins over conflicting external_candidate evidence unless a human later approves a change.
    - Patch preview payloads never enable writes in Phase 91.
  </behavior>
  <action>Write the tests first and make them explicitly assert the Phase 91 safety posture: route_trace shows attempted or skipped stages, warnings appear for degraded providers, approval.write_mode stays preview_only, and human_approval_required stays true. Reuse the existing node:test style from the phase-84 through phase-90 contract suites. Do not add live network integration tests in this phase.</action>
  <verify>
    <automated>node --test test/phase-91/provider-routing-policy.test.js test/phase-91/preview-patch-policy.test.js</automated>
  </verify>
  <done>Policy and guardrail expectations are locked in executable tests before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement provider-routing policy with internal authority and degraded fallback</name>
  <files>onboarding/backend/research/provider-routing-policy.cjs, onboarding/backend/research/context-pack-contract.cjs, onboarding/backend/research/deep-research-envelope.cjs</files>
  <behavior>
    - One deterministic route is produced for every request.
    - Internal evidence is authoritative and external evidence is additive.
    - Allowed-domain policy and ranking/weighting hints are represented in the contract so later phases can enforce source boundaries without changing the schema.
    - A valid response can still be returned when external or synthesis providers are unavailable.
  </behavior>
  <action>Create a dedicated provider-routing-policy module that turns the request envelope into a stage-based policy trace. Default order must be markos_vault, markos_mcp, tavily, firecrawl, and openai_synthesis per D-03. Keep internal_authority true by default per D-04. Include contract fields for allowed-domain policy and ranking/weighting hints so evidence ordering rules are explicit even before the full orchestrator exists. Add a context-pack contract helper that represents ranked findings across literacy, MIR, MSP, company overrides, and optional external evidence for DRT-02, but stop at contract definition only; do not build the full multi-source orchestrator in this phase.</action>
  <verify>
    <automated>node --test test/phase-91/provider-routing-policy.test.js test/phase-91/preview-patch-policy.test.js</automated>
  </verify>
  <done>The routing-policy and context-pack contracts exist, reflect internal-first behavior, and degrade safely when external providers are absent.</done>
</task>

<task type="auto">
  <name>Task 3: Implement preview-only patch contract and operator-inspection response fields</name>
  <files>onboarding/backend/research/patch-preview-policy.cjs, onboarding/backend/research/deep-research-envelope.cjs</files>
  <action>Define the patch_preview contract for Literacy, MIR, and MSP section-level deltas only. Each preview item should carry artifact, section, change_type, rationale, supporting_evidence, and authority labels so operators can inspect proposed updates before any approval step. Wire the universal response envelope to expose active_filters, route_trace, context_pack, patch_preview, approval, and warnings for DRT-08. Explicitly prohibit write execution, approval bypass, or silent artifact mutation in this phase.</action>
  <verify>
    <automated>node --test test/phase-91/filter-taxonomy-v1.test.js test/phase-91/deep-research-envelope.test.js test/phase-91/provider-routing-policy.test.js test/phase-91/preview-patch-policy.test.js</automated>
  </verify>
  <done>Phase 91 responses are operator-inspectable, preview-only, and safe by default, with no auto-write behavior exposed.</done>
</task>

</tasks>

<verification>
- node --test test/phase-91/*.test.js
- node -e "const route=require('./onboarding/backend/research/provider-routing-policy.cjs'); const patch=require('./onboarding/backend/research/patch-preview-policy.cjs'); if(typeof route.buildResearchRoute!=='function'){process.exit(1)} if(typeof patch.createPatchPreview!=='function'){process.exit(1)}"
</verification>

<success_criteria>
- Provider routing is defined as internal first, external enrichment second, synthesis last.
- Allowed-domain policy and ranking/weighting behavior are explicitly represented in the contract and covered by policy tests.
- Internal authority and degraded fallback behavior are enforced in executable policy tests.
- Operators can inspect filter stack, evidence posture, and preview-only patch proposals before any human approval step.
- No multi-source orchestration runtime, MCP server implementation, or auto-write behavior is planned or built in this phase.
</success_criteria>

<output>
After completion, create .planning/phases/91-filter-taxonomy-and-provider-contract/91-02-SUMMARY.md.
</output>
