---
phase: 208
plan: "06"
type: execute
wave: 6
depends_on:
  - "208-01"
  - "208-02"
  - "208-03"
  - "208-04"
  - "208-05"
f_ids: []
migration_slots: []
integrates_with:
  - "PRC-01..09, BILL-02 from P205 (weekly recap should reflect pricing and billing posture where already available)"
  - "COMP-01 from P206 (narrative risks should surface governance posture where already available)"
  - "RUN-01..08 from P207 (operator decisions and blocked work should link to real execution lineage)"
translation_gate_for:
  - "P209"
  - "P211"
  - "P212"
files_modified:
  - .planning/phases/208-human-operating-interface/208-VALIDATION.md
  - .planning/operator-cockpit/weekly-narrative-contract.md
  - .planning/operator-cockpit/future-integration-boundaries.md
  - app/(markos)/operations/narrative/page.tsx
  - app/(markos)/operations/narrative/page.module.css
  - app/(markos)/operations/narrative/page.stories.tsx
  - lib/markos/operator/weekly-narrative.ts
  - lib/markos/operator/index.cjs
  - test/operator-cockpit/phase-208/domain-6/weekly-narrative.test.js
  - test/operator-cockpit/phase-208/domain-6/future-integrations.test.js
  - test/operator-cockpit/phase-208/domain-6/weekly-narrative-ui-spec.test.js
  - test/operator-cockpit/phase-208/wave-final/cross-cutting-x-axis-final.test.js
autonomous: true
requirements:
  - TASK-02
  - TASK-05
  - QA-13
  - QA-14
  - QA-15
must_haves:
  truths:
    - "Weekly Narrative explains wins, risks, blocked work, operator decisions, and next actions without inventing later-phase substrate."
    - "Narrative placeholder sections for evidence, connector, loop, and learning inputs are explicit."
    - "Future integration boundaries prevent later phases from silently reshaping the cockpit contract."
    - "Operator-decisions rows render the 206 mutation-class binding via .c-chip-protocol Class: {mutation_class} per 208-UI-SPEC §N-7."
    - "future-integration-boundaries.md doctrine document maps evidence_posture→phase_209, connector_recovery→phase_210, dispatch_outcomes→phase_211, learning_suggestions→phase_212 with status future_translation_gate per 208-UI-SPEC §N-10."
    - "208-UI-SPEC §Surface F ACs N-1..N-13 are realized verbatim (token-only, h1 + descriptor, WeeklyNarrative TS contract, 4 placeholder_sections literals, 5 content sections + 4 future-phase placeholders, .c-chip-protocol for run_id/task_id/mutation_class, time-window .c-chip group, bracketed-glyph state coding, prefers-reduced-motion, banned-lexicon zero match, ≥4 named state stories)."
    - "Final wave gate: cross-cutting X-1..X-10 tripwire passes across ALL 208 surface files (closure of the carry-forward axis)."
  artifacts:
    - path: .planning/operator-cockpit/weekly-narrative-contract.md
      provides: canonical Weekly Narrative shape and placeholder-section vocabulary
    - path: .planning/operator-cockpit/future-integration-boundaries.md
      provides: named translation gates for evidence (P209), connector recovery (P210), dispatch (P211), learning (P212) with status future_translation_gate
    - path: lib/markos/operator/weekly-narrative.ts
      provides: WeeklyNarrative TS contract + 4 placeholder_sections literals (208-UI-SPEC §N-3..N-4)
    - path: app/(markos)/operations/narrative/page.tsx
      provides: Weekly Narrative page composing 5 content sections + 4 future-phase placeholder sections per 208-UI-SPEC §Surface F layout grid
    - path: app/(markos)/operations/narrative/page.stories.tsx
      provides: ≥4 named state stories per 208-UI-SPEC §N-13 (Default, EmptyWeek, WinsHeavy, BlockedHeavy)
  key_links:
    - from: app/(markos)/operations/narrative/page.tsx
      to: lib/markos/operator/weekly-narrative.ts
      via: page consumes WeeklyNarrative; renders 5 content + 4 placeholder sections per UI-SPEC §Surface F layout grid
      pattern: "WeeklyNarrative"
    - from: app/(markos)/settings/mcp/page.tsx
      to: .planning/operator-cockpit/weekly-narrative-contract.md
      via: operator-readable wins and risk posture should be narratable without leaving the cockpit
      pattern: "top-tools"
    - from: .planning/phases/208-human-operating-interface/208-RESEARCH.md
      to: .planning/operator-cockpit/future-integration-boundaries.md
      via: research explicitly says later evidence, connector, loop, and learning systems plug into stable shell contracts
      pattern: "future translation gates"
    - from: components/markos/operator/PlaceholderBanner.tsx
      to: app/(markos)/operations/narrative/page.tsx
      via: future_phase_209/210/211/212 placeholders all reused from 208-02-02 PlaceholderBanner extraction (D-15 reuse threshold passed across 4 surfaces)
      pattern: "future_phase_"
ui_spec_acs_folded:
  - "N-1..N-13 (Weekly Narrative — 13 ACs)"
  - "X-1..X-10 (cross-cutting FINAL gate — full 76-AC closure across all 208 surfaces)"
---

<objective>
Define Weekly Narrative as a stable operator recap surface, then lock a future integration boundary matrix so later phases enrich the cockpit without reshaping or back-owning the interface contract. Fold 208-UI-SPEC.md §Surface F (Weekly Narrative — N-1..N-13) verbatim into per-AC tasks; the 4 future-phase placeholder sections (P209/P210/P211/P212) are first-class UI states. Final wave: re-run the cross-cutting X-1..X-10 axis across all 208 surface files to certify closure.
</objective>

<context>
@.planning/phases/208-human-operating-interface/208-CONTEXT.md
@.planning/phases/208-human-operating-interface/208-RESEARCH.md
@.planning/phases/208-human-operating-interface/208-VALIDATION.md
@.planning/phases/208-human-operating-interface/208-UI-SPEC.md
@.planning/phases/206-soc2-type1-foundation/206-UI-SPEC.md
@.planning/phases/207-agentrun-v2-orchestration-substrate/207-UI-SPEC.md
@DESIGN.md
@CLAUDE.md
@app/(markos)/settings/mcp/page.tsx
@app/(markos)/settings/billing/page-shell.tsx
@lib/markos/operator/shell.ts
@lib/markos/operator/approvals.ts
@components/markos/operator/PlaceholderBanner.tsx
</context>

<tasks>

<task id="208-06-00">
  <name>Wave 0 — UI-SPEC §Surface F fold preflight: read Weekly Narrative AC table</name>
  <files>test/operator-cockpit/phase-208/domain-6/weekly-narrative-ui-spec.test.js</files>
  <read_first>
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Surface F — Weekly Narrative (Plan 208-06)" full body — verbatim layout grid (desktop + mobile single-column), copy register table (every cell verbatim including 4 future-phase placeholder bodies), state matrix, accessibility focus order (read-only, no row-level actions)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"N-* — Weekly Narrative (Plan 208-06)" AC table (N-1..N-13)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Cross-surface Tokens Reference Table"
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Storybook Coverage" (page.stories.tsx 4 named states: Default, EmptyWeek, WinsHeavy, BlockedHeavy)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Inheritance Bindings" §"From 206-UI-SPEC §Downstream UI Inheritance Map" (operator-decisions row mutation-class chip)
    - DESIGN.md §"Iconography" + §"Color blindness"
    - styles/components.css (.c-card, .c-notice, .c-badge, .c-chip, .c-chip-protocol primitives)
    - components/markos/operator/PlaceholderBanner.tsx
  </read_first>
  <action>
    Step 1: Create `test/operator-cockpit/phase-208/domain-6/weekly-narrative-ui-spec.test.js` as the 13-AC verification harness for N-1..N-13.

    Step 2: 13 named test blocks, one per AC:

    - `test('N-1 token-only zero hex in narrative/page.module.css', ...)`: assert `! grep -nE "#[0-9a-fA-F]{3,8}" app/(markos)/operations/narrative/page.module.css` returns 0.
    - `test('N-2 page heading "Weekly narrative"', ...)`: assert `grep -c "Weekly narrative" app/(markos)/operations/narrative/page.tsx` >= 1.
    - `test('N-3 WeeklyNarrative TS contract', ...)`: assert `grep -c "wins\|risks\|blocked_work\|operator_decisions\|next_actions\|placeholder_sections" lib/markos/operator/weekly-narrative.ts` >= 6.
    - `test('N-4 4 placeholder_sections literals', ...)`: assert `grep -c "phase_209_evidence\|phase_210_connector\|phase_211_loop\|phase_212_learning" lib/markos/operator/weekly-narrative.ts` >= 4.
    - `test('N-5 5 content sections + no .c-card--feature', ...)`: assert `grep -c "Wins\|Risks\|Blocked work\|Operator decisions\|Next actions" app/(markos)/operations/narrative/page.tsx` >= 5 AND `grep -c "c-card--feature" app/(markos)/operations/narrative/page.tsx` returns 0.
    - `test('N-6 4 future_phase placeholder banners', ...)`: assert `grep -c "future_phase_209\|future_phase_210\|future_phase_211\|future_phase_212" app/(markos)/operations/narrative/page.tsx` >= 4.
    - `test('N-7 operator-decisions mutation-class chip', ...)`: assert `grep -c "c-chip-protocol\|mutation_class" app/(markos)/operations/narrative/page.tsx` >= 1.
    - `test('N-8 time-window .c-chip group', ...)`: assert `grep -c "This week\|c-chip" app/(markos)/operations/narrative/page.tsx` >= 1.
    - `test('N-9 bracketed-glyph state coding', ...)`: assert `grep -c "\\[ok\\]\\|\\[warn\\]\\|\\[err\\]\\|\\[info\\]" app/(markos)/operations/narrative/page.tsx` >= 4.
    - `test('N-10 future-integration-boundaries.md doctrine', ...)`: assert `grep -c "evidence_posture\|connector_recovery\|dispatch_outcomes\|learning_suggestions\|future_translation_gate" .planning/operator-cockpit/future-integration-boundaries.md` >= 5.
    - `test('N-11 prefers-reduced-motion block', ...)`: assert `grep -c "prefers-reduced-motion" app/(markos)/operations/narrative/page.module.css` >= 1.
    - `test('N-12 banned-lexicon zero match', ...)`: assert `! grep -nE "synergy|leverage|empower|unlock|transform" app/(markos)/operations/narrative/page.tsx` returns 0.
    - `test('N-13 Storybook ≥4 named state stories', ...)`: assert `grep -c "Default\|EmptyWeek\|WinsHeavy\|BlockedHeavy" app/(markos)/operations/narrative/page.stories.tsx` >= 4.

    Step 3: Tests use Node `--test`; skip-soft on missing files; final wave gate is hard.
  </action>
  <acceptance_criteria>
    - `test/operator-cockpit/phase-208/domain-6/weekly-narrative-ui-spec.test.js` exists with exactly 13 named test blocks N-1 through N-13.
    - Test file references all 4 placeholder_sections literals: `grep -c "phase_209_evidence\|phase_210_connector\|phase_211_loop\|phase_212_learning" test/operator-cockpit/phase-208/domain-6/weekly-narrative-ui-spec.test.js` >= 4.
    - Test file references all 4 future-integration-boundaries doctrine rows: `grep -c "evidence_posture\|connector_recovery\|dispatch_outcomes\|learning_suggestions" test/operator-cockpit/phase-208/domain-6/weekly-narrative-ui-spec.test.js` >= 4.
  </acceptance_criteria>
  <verify>
    <automated>node -e "require('node:fs').readFileSync('test/operator-cockpit/phase-208/domain-6/weekly-narrative-ui-spec.test.js')"</automated>
  </verify>
  <done>13-AC verification harness for Weekly Narrative is committed before implementation.</done>
</task>

<task id="208-06-01">
  <name>Weekly Narrative contract + TS data assembler (N-3, N-4) + UI render (N-1..N-2, N-5..N-9, N-11..N-13)</name>
  <files>.planning/operator-cockpit/weekly-narrative-contract.md, app/(markos)/operations/narrative/page.tsx, app/(markos)/operations/narrative/page.module.css, app/(markos)/operations/narrative/page.stories.tsx, lib/markos/operator/weekly-narrative.ts, lib/markos/operator/index.cjs, test/operator-cockpit/phase-208/domain-6/weekly-narrative.test.js</files>
  <read_first>
    - .planning/phases/208-human-operating-interface/208-RESEARCH.md section "Domain 6 - Weekly Narrative and future integration boundaries"
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Surface F — Weekly Narrative (Plan 208-06)" full body
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"N-* — Weekly Narrative (Plan 208-06)" AC table
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Cross-surface Tokens Reference Table"
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Inheritance Bindings" §"From 206-UI-SPEC" (operator-decisions mutation-class chip)
    - app/(markos)/settings/mcp/page.tsx (top-tools narrative source)
    - app/(markos)/settings/billing/page-shell.tsx (billing posture narrative source)
    - lib/markos/operator/approvals.ts (mutation_class_root literals reused)
    - components/markos/operator/PlaceholderBanner.tsx (future_phase_209/210/211/212 reuse)
    - styles/components.css (primitives)
    - DESIGN.md §"Iconography"
  </read_first>
  <action>
    Step 1 — `.planning/operator-cockpit/weekly-narrative-contract.md`: create with the exact `WeeklyNarrative` fields per 208-UI-SPEC §Surface F data dependencies: `narrative_id`, `tenant_id`, `time_window`, `wins`, `risks`, `blocked_work`, `operator_decisions`, `next_actions`, `placeholder_sections`, `created_at`, `updated_at`. Document `time_window` shape: `{ start: ISODateString, end: ISODateString }`.

    Step 2 — `lib/markos/operator/weekly-narrative.ts`:
      - Export `WeeklyNarrative` interface with the exact 11 fields from Step 1.
      - Export `placeholder_sections` literal union with the exact 4 literals per 208-UI-SPEC §N-4: `'phase_209_evidence' | 'phase_210_connector' | 'phase_211_loop' | 'phase_212_learning'`.
      - Define section-row types: `WinRow`, `RiskRow`, `BlockedWorkRow`, `OperatorDecisionRow`, `NextActionRow`. Each row has its own minimum field set:
        - `WinRow`: `{ text: string, source_run_id?: string, source_task_id?: string, completed_at: ISODateString }`
        - `RiskRow`: `{ text: string, severity: 'major' | 'minor', source_run_id?: string, surfaced_at: ISODateString }`
        - `BlockedWorkRow`: `{ text: string, blocked_run_ids: string[], blocked_task_ids: string[], owner_role: string }`
        - `OperatorDecisionRow`: `{ text: string, mutation_class: MutationClassRoot, decided_at: ISODateString, decided_by: string, run_id?: string }` — `mutation_class` reuses 6 root literals from 208-04-01 (`external.send | billing.charge | connector.mutate | price.change | public.claim | data.export`).
        - `NextActionRow`: `{ text: string, owner_role: string, due_at?: ISODateString }`
      - Export `assembleWeeklyNarrative(input: NarrativeAssemblerInput): WeeklyNarrative` function reading from 207 RunApiEnvelope (completed runs for wins, blocked runs for blocked_work), 206 control-registry exceptions (risks), 207 ApprovalHandoffRecord with terminal decisions (operator_decisions). Always returns the 4 placeholder_sections literals — they are first-class UI states, not derived.

    Step 3 — `lib/markos/operator/index.cjs`: CommonJS twin.

    Step 4 — `app/(markos)/operations/narrative/page.module.css`: token-only stylesheet per 208-UI-SPEC §Surface F layout grid:
      - Page wrapper: `var(--color-surface)` + `var(--space-xl)` top + `var(--space-lg)` horizontal padding.
      - Container `max-width: var(--w-container)`; text columns inside `.c-card` constrained to `max-width: var(--w-prose)` (720px).
      - Section gap: `var(--space-md)` between `.c-card` sections.
      - At `max-width: 640px`: single column; placeholder sections collapse to single-line summaries on smallest viewports (under 360px).
      - `@media (prefers-reduced-motion: reduce)` block (N-11): no transitions on chip toggle (instant).
      - **Zero hex literals** (N-1). **No local `.banner`/`.alert`/`.warning`/`.callout`/`.noticeBar`** classes (X-3).

    Step 5 — `app/(markos)/operations/narrative/page.tsx`: render Weekly Narrative per 208-UI-SPEC §Surface F layout grid:

      Page header:
      - `<h1>Weekly narrative</h1>` (N-2)
      - `<p class="t-lead">Week of {time_window.start} to {time_window.end}</p>` (descriptor with time-window range)
      - Time-window selector chip group (N-8): `<button class="c-chip">This week</button>` (default), `<button class="c-chip">Last week</button>`, plus N previous weeks rendered as `<button class="c-chip">Week of {YYYY-MM-DD}</button>`. Selected chip uses `.c-chip--mint`.

      5 content sections (N-5 — each composing `.c-card`, NOT `.c-card--feature`):

      1. **Wins** (`.c-card`):
         - `<h2>Wins</h2>`
         - `<ul>` of WinRow `<li>`: `[ok] {text}` + `.c-chip-protocol` for `source_run_id` if present + `.c-field__help` for `completed_at`.
         - Empty: `<div class="c-notice c-notice--info">[info] No wins recorded for this week.</div>`.

      2. **Risks** (`.c-card`):
         - `<h2>Risks</h2>`
         - `<ul>` of RiskRow `<li>`: severity badge (`.c-badge--error` `[err] Major` or `.c-badge--warning` `[warn] Minor`) + text + `.c-chip-protocol` for `source_run_id` if present.
         - Empty: `<div class="c-notice c-notice--success">[ok] No risks flagged this week.</div>`.

      3. **Blocked work** (`.c-card`):
         - `<h2>Blocked work</h2>`
         - `<ul>` of BlockedWorkRow `<li>`: `[block] {text}` + `.c-chip-protocol` for each `blocked_run_ids[]` and `blocked_task_ids[]` (deep-link chips).
         - Empty: `<div class="c-notice c-notice--success">[ok] No blocked work this week.</div>`.

      4. **Operator decisions** (`.c-card`):
         - `<h2>Operator decisions</h2>`
         - `<ul>` of OperatorDecisionRow `<li>`: text + `.c-chip-protocol` "Class: `{mutation_class}`" — verbatim per 208-UI-SPEC §N-7. The mutation_class chip uses one of the 6 root literals from 206 doctrine. Plus `.c-field__help` for `decided_at` + `decided_by`.
         - Empty: `<div class="c-notice c-notice--info">[info] No operator decisions recorded this week.</div>`.

      5. **Next actions** (`.c-card`):
         - `<h2>Next actions</h2>`
         - `<ul>` of NextActionRow `<li>`: text + owner_role + `.c-field__help` for `due_at` if present.
         - Empty: `<div class="c-notice c-notice--info">[info] No next actions recorded.</div>`.

      Awaiting later phases section (N-6 — single `.c-card` containing 4 stacked PlaceholderBanner components):
      - `<h2>Awaiting later phases</h2>`
      - `<PlaceholderBanner variant="future_phase_209" />` body verbatim from 208-UI-SPEC §Surface F copy register: "[info] Phase 209 evidence narrative is not yet available. Evidence freshness summaries will appear here once Phase 209 ships."
      - `<PlaceholderBanner variant="future_phase_210" />` body: "[info] Phase 210 connector recovery summaries are not yet available. Connector dispatch context will appear here once Phase 210 ships."
      - `<PlaceholderBanner variant="future_phase_211" />` body: "[info] Phase 211 dispatch and measurement outcomes are not yet available. Loop-attribution narrative will appear here once Phase 211 ships."
      - `<PlaceholderBanner variant="future_phase_212" />` body: "[info] Phase 212 learning suggestions are not yet available. Performance-log narrative will appear here once Phase 212 ships."

      Bracketed-glyph state coding (N-9): every state paired with `[ok]`, `[warn]`, `[err]`, `[info]`, or `[block]`. State color never the only signal.

      Mint-as-text: `.c-chip-protocol` for run_id, task_id, mutation_class (via `--color-primary-text`).

      Read-only surface: NO `.c-button--primary` on the page; rows are read-only (per 208-UI-SPEC §Surface F accessibility focus order).

    Step 6 — `app/(markos)/operations/narrative/page.stories.tsx`: CSF3 with 4 named state stories per 208-UI-SPEC §N-13:
      - `Default`: balanced narrative with 3 wins, 2 risks (1 major + 1 minor), 1 blocked work, 4 operator_decisions covering 4 mutation_class root literals, 3 next actions; 4 future-phase placeholders visible.
      - `EmptyWeek`: all 5 content sections empty (renders 5 `.c-notice` empty states); 4 future-phase placeholders still visible.
      - `WinsHeavy`: 12 wins, 0 risks, 0 blocked, 1 operator decision, 0 next actions.
      - `BlockedHeavy`: 0 wins, 4 risks (all major), 8 blocked work items with multiple blocked_run_ids per row, 2 operator decisions.
      - Story fixtures honor 207 contract field names; 206 mutation-class doctrine; bracketed glyphs only.

    Step 7 — Create `test/operator-cockpit/phase-208/domain-6/weekly-narrative.test.js` (existing AC retained):
      - Assert `WeeklyNarrative` field names present (N-3 verbatim register).
      - Assert all 4 `placeholder_sections` literals (N-4).
      - Assert `blocked_work` and `operator_decisions` strings present (existing AC).
      - Assert all 6 root mutation_class literals appear in the page.tsx for operator-decisions rendering.

    Step 8 — Banned-lexicon compliance (N-12, X-9): zero matches. No exclamation points.
  </action>
  <acceptance_criteria>
    - `.planning/operator-cockpit/weekly-narrative-contract.md` contains the exact field names `wins`, `risks`, `blocked_work`, `operator_decisions`, `next_actions`, `placeholder_sections`.
    - **N-1**: `! grep -nE "#[0-9a-fA-F]{3,8}" app/(markos)/operations/narrative/page.module.css` returns 0.
    - **N-2**: `grep -c "Weekly narrative" app/(markos)/operations/narrative/page.tsx` >= 1.
    - **N-3**: `grep -c "wins\|risks\|blocked_work\|operator_decisions\|next_actions\|placeholder_sections" lib/markos/operator/weekly-narrative.ts` >= 6.
    - **N-4**: `grep -c "phase_209_evidence\|phase_210_connector\|phase_211_loop\|phase_212_learning" lib/markos/operator/weekly-narrative.ts` >= 4.
    - **N-5**: `grep -c "Wins\|Risks\|Blocked work\|Operator decisions\|Next actions" app/(markos)/operations/narrative/page.tsx` >= 5 AND `grep -c "c-card--feature" app/(markos)/operations/narrative/page.tsx` returns 0.
    - **N-6**: `grep -c "future_phase_209\|future_phase_210\|future_phase_211\|future_phase_212" app/(markos)/operations/narrative/page.tsx` >= 4.
    - **N-7**: `grep -c "c-chip-protocol\|mutation_class" app/(markos)/operations/narrative/page.tsx` >= 1.
    - **N-8**: `grep -c "This week\|c-chip" app/(markos)/operations/narrative/page.tsx` >= 1.
    - **N-9**: `grep -c "\[ok\]\|\[warn\]\|\[err\]\|\[info\]" app/(markos)/operations/narrative/page.tsx` >= 4.
    - **N-11**: `grep -c "prefers-reduced-motion" app/(markos)/operations/narrative/page.module.css` >= 1.
    - **N-12**: `! grep -nE "synergy\|leverage\|empower\|unlock\|transform" app/(markos)/operations/narrative/page.tsx` returns 0.
    - **N-13**: `grep -c "Default\|EmptyWeek\|WinsHeavy\|BlockedHeavy" app/(markos)/operations/narrative/page.stories.tsx` >= 4.
    - `test/operator-cockpit/phase-208/domain-6/weekly-narrative.test.js` contains the exact strings `blocked_work` and `operator_decisions`.
    - **PlaceholderBanner reused 4 ways**: page imports `<PlaceholderBanner variant="future_phase_{209,210,211,212}">` from `components/markos/operator/PlaceholderBanner.tsx` (D-15 reuse threshold passed: 4 surfaces × 5 variants).
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/operator-cockpit/phase-208/domain-6/weekly-narrative.test.js test/operator-cockpit/phase-208/domain-6/weekly-narrative-ui-spec.test.js</automated>
  </verify>
  <done>Weekly Narrative renders 5 content sections + 4 future-phase placeholder sections per 208-UI-SPEC §Surface F layout grid. All 13 ACs N-1..N-13 are automated. Operator-decisions row carries the 206 mutation-class chip explicitly. Storybook covers 4 named states. Read-only surface (no row-level actions per UI-SPEC).</done>
</task>

<task id="208-06-02">
  <name>Future integration boundary matrix doctrine + N-10 verification</name>
  <files>.planning/operator-cockpit/future-integration-boundaries.md, test/operator-cockpit/phase-208/domain-6/future-integrations.test.js, .planning/phases/208-human-operating-interface/208-VALIDATION.md</files>
  <read_first>
    - .planning/phases/208-human-operating-interface/208-RESEARCH.md section "Domain 6 - Weekly Narrative and future integration boundaries"
    - .planning/phases/208-human-operating-interface/208-CONTEXT.md
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"N-10" AC (verbatim doctrine row register: evidence_posture, connector_recovery, dispatch_outcomes, learning_suggestions; status future_translation_gate)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Surface F" data dependencies (placeholder_sections mapping to phase_209/210/211/212)
  </read_first>
  <action>
    Step 1: Create `.planning/operator-cockpit/future-integration-boundaries.md` per 208-UI-SPEC §N-10 with one row each for `evidence_posture`, `connector_recovery`, `dispatch_outcomes`, `learning_suggestions`. Required columns: `boundary_name`, `target_phase`, `cockpit_consumer_surface`, `current_placeholder_variant`, `status`.

    Step 2: Map rows to exact target phases per 208-UI-SPEC §N-10:
      | boundary_name | target_phase | cockpit_consumer_surface | current_placeholder_variant | status |
      |---------------|--------------|--------------------------|-----------------------------|--------|
      | `evidence_posture` | `phase_209` | Approval Inbox + Morning Brief + Weekly Narrative | `future_phase_209` | `future_translation_gate` |
      | `connector_recovery` | `phase_210` | Recovery Center + Morning Brief + Weekly Narrative | `future_phase_210` | `future_translation_gate` |
      | `dispatch_outcomes` | `phase_211` | Weekly Narrative | `future_phase_211` | `future_translation_gate` |
      | `learning_suggestions` | `phase_212` | Weekly Narrative | `future_phase_212` | `future_translation_gate` |

    Step 3: Every row uses status `future_translation_gate` exactly (N-10 verbatim invariant).

    Step 4: Document the contract terms: each boundary is a named integration point that later phases (209/210/211/212) MUST plug into without reshaping the cockpit contract from this phase. Document migration posture: when Phase 209 ships, the cockpit consumer changes from `<PlaceholderBanner variant="future_phase_209">` to a real evidence-posture component but the consumer surface and placement remain stable.

    Step 5: Create `test/operator-cockpit/phase-208/domain-6/future-integrations.test.js`:
      - Assert all 4 row names present in `future-integration-boundaries.md`.
      - Assert all 4 target phases present.
      - Assert `future_translation_gate` status appears for every row.

    Step 6: Update `.planning/phases/208-human-operating-interface/208-VALIDATION.md`:
      - Add row `208-06-01` and `208-06-02` to per-task verification map.
      - Update `## UI-SPEC AC Coverage Map`: mark N-1..N-13.
      - Update Wave 0 Requirements section to confirm closure (all 8 contract docs created across plans 01-06).
      - Update `nyquist_compliant` frontmatter once final-wave gate (208-06-03) passes.
  </action>
  <acceptance_criteria>
    - `.planning/operator-cockpit/future-integration-boundaries.md` contains rows for `evidence_posture`, `connector_recovery`, `dispatch_outcomes`, `learning_suggestions` (208-UI-SPEC §N-10).
    - `.planning/operator-cockpit/future-integration-boundaries.md` contains the exact target phases `phase_209`, `phase_210`, `phase_211`, `phase_212`.
    - `.planning/operator-cockpit/future-integration-boundaries.md` contains the exact status `future_translation_gate` for every row: `grep -c "future_translation_gate" .planning/operator-cockpit/future-integration-boundaries.md` >= 4.
    - **N-10 (verbatim grep gate)**: `grep -c "evidence_posture\|connector_recovery\|dispatch_outcomes\|learning_suggestions\|future_translation_gate" .planning/operator-cockpit/future-integration-boundaries.md` >= 5.
    - `208-VALIDATION.md` contains row `208-06-02`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/operator-cockpit/phase-208/domain-6/future-integrations.test.js</automated>
  </verify>
  <done>Later phases now enrich a stable cockpit contract instead of silently reshaping Phase 208 around unfinished substrate. Doctrine document is the named integration anchor for P209/P210/P211/P212.</done>
</task>

<task id="208-06-03">
  <name>Final wave gate — re-run cross-cutting X-1..X-10 axis across ALL 208 surface files (76-AC closure)</name>
  <files>test/operator-cockpit/phase-208/wave-final/cross-cutting-x-axis-final.test.js, .planning/phases/208-human-operating-interface/208-VALIDATION.md</files>
  <read_first>
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"X-* — Cross-cutting (carry-forward closure)" AC table (X-1..X-10) — verbatim verification commands
    - test/operator-cockpit/phase-208/preflight/cross-cutting-x-axis.test.js (Wave-1 tripwire from 208-01-00; this final-wave test extends it to ALL 208 surface files now that Plans 02-06 are done)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Validation Strategy Summary" (Chromatic visual regression baselines for 23 new state stories)
    - All 6 page.tsx files: `app/(markos)/page.tsx`, `app/(markos)/operations/{tasks,approvals,recovery,narrative}/page.tsx`, `app/(markos)/layout-shell.tsx`
    - All 6 page.module.css files (or task-ui.module.css for Task Board): same paths
    - All 6 page.stories.tsx files
    - lib/markos/operator/{shell,morning-brief,task-board,approvals,recovery,weekly-narrative}.ts
    - components/markos/operator/PlaceholderBanner.tsx, components/markos/operator/Money.tsx
  </read_first>
  <action>
    Step 1: Create `test/operator-cockpit/phase-208/wave-final/cross-cutting-x-axis-final.test.js` — the closure version of the Wave-1 tripwire from 208-01-00. This test asserts X-1..X-10 across the ACTUAL Phase 208 file set (Plans 02-06 are now done, so files exist).

    Step 2: 10 named test blocks per 208-UI-SPEC §X-* AC table — verbatim verification commands. The HARD difference vs Wave-1 tripwire is: no skip-soft. Every file must exist; every AC must pass.

    - `test('X-1 (FINAL) zero hex across all 208 module.css', ...)`: `! grep -rnE "#[0-9a-fA-F]{3,8}" app/(markos)/page.module.css app/(markos)/operations/{approvals,recovery,narrative}/page.module.css app/(markos)/operations/tasks/task-ui.module.css app/(markos)/layout-shell.module.css` returns 0.
    - `test('X-2 (FINAL) mint never as fill on cards', ...)`: `! grep -nE "background:[[:space:]]+var\\(--color-primary\\)[^-]" app/(markos)/{page,operations/approvals/page,operations/recovery/page,operations/narrative/page}.module.css` returns 0.
    - `test('X-3 (FINAL) zero local .banner/.alert/.warning/.callout/.noticeBar', ...)`: `! grep -rnE "\\.banner\\|\\.alert\\|\\.warning\\|\\.callout\\|\\.noticeBar" app/(markos)/page.module.css app/(markos)/operations/{approvals,recovery,narrative}/page.module.css` returns 0.
    - `test('X-4 (FINAL) zero .c-card--feature in 208 page.tsx', ...)`: `! grep -rnE "c-card--feature" app/(markos)/page.tsx app/(markos)/operations/{approvals,recovery,narrative,tasks}/page.tsx` returns 0.
    - `test('X-5 (FINAL) zero .c-table in 208 page.tsx', ...)`: `! grep -rnE "c-table" app/(markos)/page.tsx app/(markos)/operations/{approvals,recovery,narrative,tasks}/page.tsx` returns 0.
    - `test('X-6 (FINAL) selective extraction', ...)`: assert `components/markos/operator/` contains exactly 2 reusable extractions (PlaceholderBanner + Money) — proven across ≥2 surfaces (PlaceholderBanner used in 02+04+05+06; Money used in 02+04+05). Per D-15, only proven-reuse extractions allowed.
    - `test('X-7 (FINAL) Storybook ≥4 new + 1 extended page.stories.tsx', ...)`: assert page.stories.tsx exists for `app/(markos)/page.stories.tsx`, `app/(markos)/operations/{approvals,recovery,narrative}/page.stories.tsx`, plus the existing tasks.stories.tsx is extended with ≥4 new stories (BoardEmpty, BoardSinglePriority, BoardCrossPriority, BoardWithBlocker).
    - `test('X-8 (FINAL) touch-target ≥44px global rule preserved', ...)`: `grep -nE "@media \\(pointer: coarse\\)" styles/components.css` returns ≥1 + manual viewport check documented in 208-VALIDATION.md "Manual-Only Verifications".
    - `test('X-9 (FINAL) banned-lexicon zero match across ALL 208 surface files', ...)`: `! grep -rnE "synergy|leverage|empower|unlock|transform|revolutionize|supercharge|holistic|seamless|cutting-edge|innovative|game-changer|next-generation|world-class|best-in-class|reimagine|disrupt" app/(markos)/page.tsx app/(markos)/operations/{approvals,recovery,narrative,tasks}/page.tsx lib/markos/operator/*.ts` returns 0.
    - `test('X-10 (FINAL) architecture-lock 4 forbidden strings rejected by check-interface-architecture-lock.mjs', ...)`: invoke the script from 208-01-01; assert exit code 0 (no forbidden strings found in 208 surface files) AND assert the script source contains the 4 verbatim forbidden literals.

    Step 3: Update `.planning/phases/208-human-operating-interface/208-VALIDATION.md`:
      - Add row `208-06-03` to per-task verification map (final wave gate).
      - Update `## UI-SPEC AC Coverage Map` Status column: every AC (S-1..S-10, B-1..B-16, T-1..T-17, A-1..A-19, R-1..R-14, N-1..N-13, X-1..X-10 = 89 entries) marked `verified` after this test passes.
      - Set `nyquist_compliant: true` in frontmatter (final wave gate complete).
      - Set `wave_0_complete: true` in frontmatter.
      - Document the Chromatic visual regression baseline step per 208-UI-SPEC §"Validation Strategy Summary": after final wave, capture Chromatic baselines for all 23 new state stories (4 + 4 + 6 + 5 + 4 = 23 per UI-SPEC §X-7).
  </action>
  <acceptance_criteria>
    - `test/operator-cockpit/phase-208/wave-final/cross-cutting-x-axis-final.test.js` exists with exactly 10 named test blocks X-1 through X-10.
    - `208-VALIDATION.md` `nyquist_compliant: true` in frontmatter.
    - `208-VALIDATION.md` contains row `208-06-03`.
    - `208-VALIDATION.md` `## UI-SPEC AC Coverage Map` has all 89 entries (S-1..S-10 + B-1..B-16 + T-1..T-17 + A-1..A-19 + R-1..R-14 + N-1..N-13 + X-1..X-10).
    - **Final-wave run**: `npm test -- test/operator-cockpit/phase-208/` exits 0 (full suite green).
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/operator-cockpit/phase-208/wave-final/cross-cutting-x-axis-final.test.js</automated>
  </verify>
  <done>The 76 UI-SPEC ACs (S+B+T+A+R+N) plus the 10 cross-cutting X axis (X-1..X-10) are verified across the entire Phase 208 surface file set. The carry-forward axis is closed. The 208-VALIDATION.md UI-SPEC AC Coverage Map is complete. Phase 208 is ready for `/gsd-verify-work 208` and Chromatic visual baseline capture.</done>
</task>

</tasks>

<verification>
- npm test -- test/operator-cockpit/phase-208/domain-6/weekly-narrative.test.js
- npm test -- test/operator-cockpit/phase-208/domain-6/future-integrations.test.js
- npm test -- test/operator-cockpit/phase-208/domain-6/weekly-narrative-ui-spec.test.js
- npm test -- test/operator-cockpit/phase-208/wave-final/cross-cutting-x-axis-final.test.js
- npm test -- test/operator-cockpit/phase-208/ (full suite, final wave gate)
- npm run build-storybook (verifies app/(markos)/operations/narrative/page.stories.tsx registers cleanly)
</verification>

<success_criteria>
- Weekly Narrative explains wins, risks, blocked work, operator decisions, and next actions
- Placeholder sections for evidence, connector, loop, and learning inputs are explicit
- Future integration boundaries are named and locked for later phases
- 208-UI-SPEC §Surface F ACs N-1..N-13 are realized verbatim and verified by Node `--test`
- Operator-decisions row carries the 206 mutation-class chip
- Future-integration-boundaries doctrine document maps 4 boundaries to phase_209/210/211/212 with future_translation_gate status
- **Cross-cutting X-1..X-10 final closure**: test passes across all 208 surface files
- **76 UI-SPEC ACs total** (S-* + B-* + T-* + A-* + R-* + N-*) verified — plus 10 X-* cross-cutting = 86 grep gates + 23 Storybook stories
- 208-VALIDATION.md UI-SPEC AC Coverage Map complete
- Phase 208 is ready for `/gsd-verify-work 208`
</success_criteria>
