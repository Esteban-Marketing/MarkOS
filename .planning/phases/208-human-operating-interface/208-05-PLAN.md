---
phase: 208
plan: "05"
type: execute
wave: 5
depends_on:
  - "208-01"
  - "208-02"
  - "208-03"
f_ids: []
migration_slots: []
integrates_with:
  - "RUN-01..08 from P207 (blocked runs and affected work should be visible in recovery items)"
translation_gate_for:
  - "P210"
files_modified:
  - .planning/phases/208-human-operating-interface/208-VALIDATION.md
  - .planning/operator-cockpit/recovery-center-contract.md
  - app/(markos)/operations/recovery/page.tsx
  - app/(markos)/operations/recovery/page.module.css
  - app/(markos)/operations/recovery/page.stories.tsx
  - lib/markos/operator/recovery.ts
  - lib/markos/operator/index.cjs
  - test/operator-cockpit/phase-208/domain-5/recovery-item.test.js
  - test/operator-cockpit/phase-208/domain-5/impacted-work.test.js
  - test/operator-cockpit/phase-208/domain-5/recovery-center-ui-spec.test.js
autonomous: true
requirements:
  - TASK-01
  - QA-11
  - QA-12
must_haves:
  truths:
    - "Recovery items show owner, impact scope, blocked work, and next action."
    - "Phase 208 defines recovery UX without re-owning the connector substrate."
    - "Connector-specific recovery remains an explicit Phase 210 translation gate rather than a fake current dependency."
    - "207 AgentFailureClass 7 literals (transient, policy, user_input, quota, upstream, poison, cancel) feed into the cockpit failure_family taxonomy via mapAgentFailureClass per 208-UI-SPEC §Inheritance Bindings."
    - "Resume run binds to 206 mutation-class doctrine (connector.mutate or external.send) when re-attempting failed actions per 208-UI-SPEC §Destructive actions table."
    - "208-UI-SPEC §Surface E ACs R-1..R-14 are realized verbatim (token-only, h1, RecoveryItem TS contract, 5 failure_family literals, 2 translation_gate literals, 5 family sections with connector_placeholder rendering future_phase_210, .c-chip-protocol blocked_run_ids/blocked_task_ids deep-links, Resume run conditional .c-button--primary with 206 mutation-class confirm modal, Move-to-DLQ .c-button--destructive modal, vanilla <table> per family, bracketed-glyph state coding, prefers-reduced-motion + static .c-status-dot--error no-pulse, banned-lexicon zero match, ≥5 named state stories)."
  artifacts:
    - path: .planning/operator-cockpit/recovery-center-contract.md
      provides: canonical Recovery Center item shape, failure vocabulary, translation-gate posture, and 207 AgentFailureClass mapping table
    - path: lib/markos/operator/recovery.ts
      provides: RecoveryItem TS contract + 5 failure_family literals + 2 translation_gate literals + mapAgentFailureClass function (208-UI-SPEC §R-3..R-5)
    - path: app/(markos)/operations/recovery/page.tsx
      provides: Recovery Center page composing 5 family sections (Provider failures / Billing holds / Webhook delivery / Run blockers / Connector recovery placeholder) per 208-UI-SPEC §Surface E layout grid
    - path: app/(markos)/operations/recovery/page.stories.tsx
      provides: ≥5 named state stories per 208-UI-SPEC §R-14 (Default, Empty, ProviderFailureBacklog, BillingHoldActive, ConnectorPlaceholderOnly)
  key_links:
    - from: app/(markos)/operations/recovery/page.tsx
      to: lib/markos/operator/recovery.ts
      via: page consumes RecoveryItem array + mapAgentFailureClass; renders 5 family sections per UI-SPEC §Surface E
      pattern: "RecoveryItem"
    - from: app/(markos)/settings/billing/page-shell.tsx
      to: .planning/operator-cockpit/recovery-center-contract.md
      via: billing holds and correction posture can already inform generic recovery cards
      pattern: "hold"
    - from: app/(markos)/operations/page.tsx
      to: lib/markos/operator/recovery.ts
      via: blocked-work visibility should live alongside the operator cockpit work cluster
      pattern: "operations"
    - from: components/markos/operator/PlaceholderBanner.tsx
      to: app/(markos)/operations/recovery/page.tsx
      via: future_phase_210 connector recovery placeholder reused from 208-02-02 PlaceholderBanner extraction
      pattern: "future_phase_210"
ui_spec_acs_folded:
  - "R-1..R-14 (Recovery Center — 14 ACs)"
  - "X-1..X-5 (cross-cutting verified by 208-01 tripwire applied to Recovery Center surface files)"
---

<objective>
Define the Recovery Center contract so broken providers, blocked runs, and operator-owned recovery work are visible now, while leaving connector-specific substrate to Phase 210 translation. Fold 208-UI-SPEC.md §Surface E (Recovery Center — R-1..R-14) verbatim into per-AC tasks. Resume run binds explicitly to 206 mutation-class doctrine; Move-to-DLQ records as data.export per audit; the connector_placeholder family is rendered as `<PlaceholderBanner variant="future_phase_210">` only — no rows.
</objective>

<context>
@.planning/phases/208-human-operating-interface/208-CONTEXT.md
@.planning/phases/208-human-operating-interface/208-RESEARCH.md
@.planning/phases/208-human-operating-interface/208-VALIDATION.md
@.planning/phases/208-human-operating-interface/208-UI-SPEC.md
@.planning/phases/206-soc2-type1-foundation/206-UI-SPEC.md
@.planning/phases/207-agentrun-v2-orchestration-substrate/207-UI-SPEC.md
@DESIGN.md
@CLAUDE.md
@app/(markos)/settings/billing/page-shell.tsx
@app/(markos)/operations/page.tsx
@lib/markos/operator/shell.ts
@lib/markos/operator/task-board.ts
@components/markos/operator/PlaceholderBanner.tsx
</context>

<tasks>

<task id="208-05-00">
  <name>Wave 0 — UI-SPEC §Surface E fold preflight: read Recovery Center AC table</name>
  <files>test/operator-cockpit/phase-208/domain-5/recovery-center-ui-spec.test.js</files>
  <read_first>
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Surface E — Recovery Center (Plan 208-05)" full body — verbatim layout grid (desktop + mobile), copy register table (every cell verbatim including DLQ modal + Resume modal copy), state matrix, accessibility focus order, motion notes
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"R-* — Recovery Center (Plan 208-05)" AC table (R-1..R-14)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Cross-surface Tokens Reference Table"
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Storybook Coverage" (page.stories.tsx 5 named states: Default, Empty, ProviderFailureBacklog, BillingHoldActive, ConnectorPlaceholderOnly)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Inheritance Bindings" §"From 207-UI-SPEC §Downstream UI Inheritance Map" (AgentFailureClass 7 literals: transient, policy, user_input, quota, upstream, poison, cancel)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Inheritance Bindings" §"From 206-UI-SPEC" (Resume run mutation-class binding)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Destructive actions" table (Move-to-DLQ + Resume run mutation-class binding)
    - DESIGN.md §"Iconography" + §"Color blindness"
    - styles/components.css (.c-card, .c-notice, .c-badge, .c-button, .c-chip, .c-chip-protocol, .c-modal, .c-backdrop, .c-input, .c-status-dot--error primitives)
    - components/markos/operator/PlaceholderBanner.tsx
  </read_first>
  <action>
    Step 1: Create `test/operator-cockpit/phase-208/domain-5/recovery-center-ui-spec.test.js` as the 14-AC verification harness for R-1..R-14.

    Step 2: 14 named test blocks, one per AC:

    - `test('R-1 token-only zero hex in recovery/page.module.css', ...)`: assert `! grep -nE "#[0-9a-fA-F]{3,8}" app/(markos)/operations/recovery/page.module.css` returns 0.
    - `test('R-2 page heading "Recovery center"', ...)`: assert `grep -c "Recovery center" app/(markos)/operations/recovery/page.tsx` >= 1.
    - `test('R-3 RecoveryItem TS contract', ...)`: assert `grep -c "impact_scope\|blocked_run_ids\|blocked_task_ids\|owner_role\|translation_gate" lib/markos/operator/recovery.ts` >= 5.
    - `test('R-4 5 failure_family literals', ...)`: assert `grep -c "provider_failure\|billing_hold\|webhook_delivery\|run_blocker\|connector_placeholder" lib/markos/operator/recovery.ts` >= 5.
    - `test('R-5 2 translation_gate literals', ...)`: assert `grep -c "translation_gate.*none\|translation_gate.*phase_210" lib/markos/operator/recovery.ts` >= 1.
    - `test('R-6 5 family sections + future_phase_210', ...)`: assert `grep -c "provider_failure\|billing_hold\|webhook_delivery\|run_blocker\|connector_placeholder\|future_phase_210" app/(markos)/operations/recovery/page.tsx` >= 6.
    - `test('R-7 .c-chip-protocol blocked_run_ids/blocked_task_ids', ...)`: assert `grep -c "c-chip-protocol" app/(markos)/operations/recovery/page.tsx` >= 2.
    - `test('R-8 Resume run conditional .c-button--primary', ...)`: assert `grep -c "Resume run\|c-button--primary" app/(markos)/operations/recovery/page.tsx` >= 1.
    - `test('R-9 Move-to-DLQ .c-button--destructive', ...)`: assert `grep -c "c-button--destructive\|Move to DLQ" app/(markos)/operations/recovery/page.tsx` >= 1.
    - `test('R-10 vanilla <table> + no .c-table', ...)`: assert `grep -c "c-table" app/(markos)/operations/recovery/page.tsx` returns 0.
    - `test('R-11 bracketed-glyph state coding', ...)`: assert `grep -c "\\[ok\\]\\|\\[warn\\]\\|\\[err\\]\\|\\[info\\]" app/(markos)/operations/recovery/page.tsx` >= 4.
    - `test('R-12 prefers-reduced-motion block', ...)`: assert `grep -c "prefers-reduced-motion" app/(markos)/operations/recovery/page.module.css` >= 1.
    - `test('R-13 banned-lexicon zero match', ...)`: assert `! grep -nE "synergy|leverage|empower|unlock|transform" app/(markos)/operations/recovery/page.tsx` returns 0.
    - `test('R-14 Storybook ≥5 named state stories', ...)`: assert `grep -c "Default\|Empty\|ProviderFailureBacklog\|BillingHoldActive\|ConnectorPlaceholderOnly" app/(markos)/operations/recovery/page.stories.tsx` >= 5.

    Step 3: Tests use Node `--test`; skip-soft on missing files; final wave gate is hard.
  </action>
  <acceptance_criteria>
    - `test/operator-cockpit/phase-208/domain-5/recovery-center-ui-spec.test.js` exists with exactly 14 named test blocks R-1 through R-14.
    - Test file references all 7 AgentFailureClass literals (input mapping): `grep -c "transient\|policy\|user_input\|quota\|upstream\|poison\|cancel" test/operator-cockpit/phase-208/domain-5/recovery-center-ui-spec.test.js` >= 7.
    - Test file references all 5 failure_family literals.
    - Test file references both translation_gate literals: `grep -c "none\|phase_210" test/operator-cockpit/phase-208/domain-5/recovery-center-ui-spec.test.js` >= 2.
  </acceptance_criteria>
  <verify>
    <automated>node -e "require('node:fs').readFileSync('test/operator-cockpit/phase-208/domain-5/recovery-center-ui-spec.test.js')"</automated>
  </verify>
  <done>14-AC verification harness for Recovery Center is committed before implementation. The 207 AgentFailureClass mapping is asserted explicitly.</done>
</task>

<task id="208-05-01">
  <name>Recovery Center contract + TS data assembler with 207 AgentFailureClass mapping (R-3..R-5)</name>
  <files>.planning/operator-cockpit/recovery-center-contract.md, lib/markos/operator/recovery.ts, lib/markos/operator/index.cjs, test/operator-cockpit/phase-208/domain-5/recovery-item.test.js, test/operator-cockpit/phase-208/domain-5/impacted-work.test.js</files>
  <read_first>
    - .planning/phases/208-human-operating-interface/208-RESEARCH.md section "Domain 5 - Recovery Center and impacted-work visibility"
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Surface E — Recovery Center" data dependencies block (verbatim RecoveryItem field set + 5 failure_family literals + 2 translation_gate literals)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Inheritance Bindings" §"From 207-UI-SPEC §Downstream UI Inheritance Map" (AgentFailureClass 7 literals + RunApiEnvelope.run_id + RunCostContext divergence rows)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Inheritance Bindings" §"From 206-UI-SPEC" (Resume run = connector.mutate or external.send mutation-class binding)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Destructive actions" (Move-to-DLQ = data.export mutation-class for audit)
    - app/(markos)/settings/billing/page-shell.tsx
    - app/(markos)/operations/page.tsx
    - lib/markos/operator/task-board.ts (TaskBoardItem.task_id type input)
  </read_first>
  <action>
    Step 1: Create `.planning/operator-cockpit/recovery-center-contract.md` with the exact `RecoveryItem` fields per 208-UI-SPEC §Surface E data dependencies: `recovery_item_id`, `tenant_id`, `failure_family`, `status`, `impact_scope`, `blocked_run_ids`, `blocked_task_ids`, `owner_role`, `next_action`, `translation_gate`, `created_at`, `updated_at`.

    Step 2: Document the 207 AgentFailureClass → cockpit failure_family mapping table verbatim:

      | 207 AgentFailureClass | Cockpit failure_family |
      |----------------------|----------------------|
      | `transient` | `run_blocker` (when retry-policy exhausted) |
      | `policy` | `run_blocker` |
      | `user_input` | `run_blocker` (operator must edit input) |
      | `quota` | `billing_hold` (when quota = billing-driven) OR `run_blocker` |
      | `upstream` | `provider_failure` (when external API) OR `webhook_delivery` (when webhook ingress) |
      | `poison` | `run_blocker` |
      | `cancel` | (no recovery row — terminal `done` per Task Board collapse) |

      Plus: `connector_placeholder` is reserved for Phase 210 translation; no AgentFailureClass maps to it in Phase 208.

    Step 3: Document the Resume + DLQ mutation-class binding table per 208-UI-SPEC §Destructive actions:

      | Action | mutation_class | UI gate |
      |--------|----------------|---------|
      | Resume run (mid-flight on external) | `connector.mutate` or `external.send` | `.c-modal` confirm; secondary-approval token if 206 default_approval_mode == `dual_approval` |
      | Move to DLQ | `data.export` (audit-export shape) | `.c-modal` + required reason input |

    Step 4: Create `lib/markos/operator/recovery.ts`:
      - Export `RecoveryItem` interface with the exact 12 fields from Step 1.
      - Export `failure_family` literal union with the exact 5 literals per 208-UI-SPEC §R-4: `'provider_failure' | 'billing_hold' | 'webhook_delivery' | 'run_blocker' | 'connector_placeholder'`.
      - Export `translation_gate` literal union with the exact 2 literals per 208-UI-SPEC §R-5: `'none' | 'phase_210'`.
      - Export `severity` literal union per 208-UI-SPEC §Surface E copy register: `'major' | 'minor'`.
      - Export `mapAgentFailureClass(agentClass: AgentFailureClass, context: { is_external_api?: boolean, is_webhook_ingress?: boolean, is_billing_quota?: boolean }): RecoveryFailureFamily | null` function implementing the Step 2 table verbatim. Returns `null` for `cancel` (terminal — no recovery row).
      - Export `RecoveryAction` literal union: `'resume_run' | 'move_to_dlq' | 'snooze' | 'reassign_owner'`.
      - Export `mapResumeMutationClass(item: RecoveryItem): MutationClassRoot` function — when failure_family ∈ {provider_failure, run_blocker} and run was mid-flight on external, returns `'connector.mutate'` or `'external.send'` per 206 doctrine.
      - Export `assembleRecoveryItems(input: RecoveryAssemblerInput): RecoveryItem[]` function that consumes 207 RunApiEnvelope failure inputs + RunCostContext divergence + AgentFailureClass and produces the array.

    Step 5: Mirror in `lib/markos/operator/index.cjs` (CommonJS twin).

    Step 6: Create `test/operator-cockpit/phase-208/domain-5/recovery-item.test.js` (existing AC retained):
      - Assert `RecoveryItem` field names present (R-3 verbatim register).
      - Assert all 5 `failure_family` literals (R-4).
      - Assert both `translation_gate` literals (R-5).

    Step 7: Create `test/operator-cockpit/phase-208/domain-5/impacted-work.test.js` (existing AC retained):
      - Assert `connector_placeholder` literal verbatim (existing AC).
      - Assert `mapAgentFailureClass` covers 6 of 7 source literals (transient, policy, user_input, quota, upstream, poison; cancel returns null).
      - Assert `mapResumeMutationClass` returns `connector.mutate` or `external.send` for provider_failure + run_blocker contexts.
  </action>
  <acceptance_criteria>
    - `.planning/operator-cockpit/recovery-center-contract.md` contains the exact field names `impact_scope`, `blocked_run_ids`, `blocked_task_ids`, `owner_role`, `translation_gate` (208-UI-SPEC §R-3).
    - `.planning/operator-cockpit/recovery-center-contract.md` contains the verbatim 207 AgentFailureClass → cockpit failure_family mapping table.
    - `.planning/operator-cockpit/recovery-center-contract.md` contains the verbatim Resume + DLQ mutation-class binding table.
    - **R-4**: `grep -c "provider_failure\|billing_hold\|webhook_delivery\|run_blocker\|connector_placeholder" lib/markos/operator/recovery.ts` >= 5.
    - **R-5**: `grep -c "translation_gate.*none\|translation_gate.*phase_210" lib/markos/operator/recovery.ts` >= 1 OR `grep -c "'none'\|'phase_210'" lib/markos/operator/recovery.ts` >= 2.
    - **AgentFailureClass mapping**: `grep -c "transient\|policy\|user_input\|quota\|upstream\|poison\|cancel" lib/markos/operator/recovery.ts` >= 7.
    - **Mutation-class binding**: `grep -c "connector\.mutate\|external\.send\|data\.export" lib/markos/operator/recovery.ts` >= 3.
    - `test/operator-cockpit/phase-208/domain-5/impacted-work.test.js` contains the exact string `connector_placeholder`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/operator-cockpit/phase-208/domain-5/recovery-item.test.js test/operator-cockpit/phase-208/domain-5/impacted-work.test.js</automated>
  </verify>
  <done>Recovery Center data contract is locked: 12-field RecoveryItem, 5 failure_family literals, 2 translation_gate literals, deterministic 207 AgentFailureClass → cockpit mapper, deterministic 206 mutation-class binder for Resume / DLQ. The contract is consumed by 208-05-02 (UI render).</done>
</task>

<task id="208-05-02">
  <name>Recovery Center UI render — fold §Surface E layout grid + R-1..R-2, R-6..R-14</name>
  <files>app/(markos)/operations/recovery/page.tsx, app/(markos)/operations/recovery/page.module.css, app/(markos)/operations/recovery/page.stories.tsx, .planning/phases/208-human-operating-interface/208-VALIDATION.md</files>
  <read_first>
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Surface E — Recovery Center (Plan 208-05)" full body — verbatim layout grid (desktop + mobile collapsible sections), copy register table (every cell verbatim including DLQ modal heading/body + Resume confirm modal mutation-class display), state matrix, accessibility focus order, motion notes
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"R-* — Recovery Center (Plan 208-05)" AC table (R-1..R-14)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Cross-surface Tokens Reference Table"
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Storybook Coverage" (5 named states)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Destructive actions" (Resume + DLQ mutation-class binding)
    - lib/markos/operator/recovery.ts (output of 208-05-01)
    - components/markos/operator/PlaceholderBanner.tsx (extracted in 208-02-02; reused for future_phase_210)
    - styles/components.css (primitives)
    - DESIGN.md §"Iconography" + §"Motion" (.c-status-dot--error STATIC, no pulse)
  </read_first>
  <action>
    Step 1 — `app/(markos)/operations/recovery/page.module.css`: token-only stylesheet per 208-UI-SPEC §Surface E layout grid:
      - Page wrapper: `var(--color-surface)` background, `var(--color-on-surface)` text, `var(--space-xl)` top padding, `var(--space-lg)` horizontal (`var(--space-md)` mobile).
      - Container `max-width: var(--w-container)`.
      - Family section gap: `var(--space-md)` per UI-SPEC §Spacing Scale.
      - Detail card `position: sticky; top: var(--space-lg)` at desktop; replaced by full-screen at `max-width: 640px`.
      - At `max-width: 640px`: family sections collapsible (default expanded for `provider_failure` and `run_blocker`, collapsed for others — UI-SPEC §Surface E mobile breakpoint).
      - `@media (prefers-reduced-motion: reduce)` block: collapses modal fade to 0 (R-12). **`.c-status-dot--error` is STATIC — no pulse animation** (R-12 + UI-SPEC §Motion).
      - **Zero hex literals** (R-1). **No local `.banner`/`.alert`/`.warning`/`.callout`/`.noticeBar`** classes (X-3).

    Step 2 — `app/(markos)/operations/recovery/page.tsx`: render Recovery Center per 208-UI-SPEC §Surface E layout grid:

      Page header:
      - `<h1>Recovery center</h1>` (R-2)
      - `<p class="t-lead">{N} blocked items. {M} owned by you. Connector-specific recovery is gated to Phase 210.</p>` (UI-SPEC copy register descriptor verbatim)
      - Filter chip group: failure_family (5 literals), translation_gate (none / phase_210), Owner.

      Empty page state: `<div class="c-notice c-notice--success">[ok] No blocked work. Recovery queue is current.</div>`.

      5 family sections per UI-SPEC §Surface E layout grid:

      1. **Provider failures** (`failure_family == 'provider_failure'`):
         - `<h2>Provider failures</h2>`
         - Vanilla `<table>` per D-14 / R-10
         - Empty: `<div class="c-notice c-notice--success">[ok] No provider_failure items open.</div>`

      2. **Billing holds** (`failure_family == 'billing_hold'`):
         - `<h2>Billing holds</h2>`
         - Vanilla `<table>`
         - Empty: `[ok] No billing_hold items open.` `.c-notice c-notice--success`

      3. **Webhook delivery** (`failure_family == 'webhook_delivery'`):
         - `<h2>Webhook delivery</h2>`
         - Vanilla `<table>`
         - Empty: `[ok] No webhook_delivery items open.` `.c-notice c-notice--success`

      4. **Run blockers** (`failure_family == 'run_blocker'`):
         - `<h2>Run blockers</h2>`
         - Vanilla `<table>`
         - Empty: `[ok] No run_blocker items open.` `.c-notice c-notice--success`

      5. **Connector recovery (Phase 210)** (`failure_family == 'connector_placeholder'`) — placeholder section per R-6:
         - `<h2>Connector recovery (Phase 210)</h2>`
         - **Body: `<PlaceholderBanner variant="future_phase_210" />` ONLY** — no rows. Verbatim per UI-SPEC §Surface E copy register: "[info] Connector failure recovery awaits Phase 210 translation. Phase 208 only renders the operator surface; connector substrate ships in Phase 210."

      Per-row anatomy (in vanilla `<table>` per family):
      - `<th>` columns: `Failure | Severity | Impact | Owner | Created`
      - `<tr>` per row: failure description, severity badge (`.c-badge--error` `[err] Major` for major; `.c-badge--warning` `[warn] Minor` for minor), impact_scope summary, owner_role, created_at via `.c-field__help`.
      - **Deep-link chips** (R-7): `blocked_run_ids[]` and `blocked_task_ids[]` rendered as `.c-chip-protocol` chips below row title; tap deep-links to run / task detail.
      - `.c-status-dot--error` STATIC indicator on row (no pulse — UI-SPEC §Motion + R-12).

      Detail card (right rail desktop):
      - `<article class="c-card">` (NOT `.c-card--feature` per X-4)
      - `<h3>{failure_family}: {next_action heading}</h3>`
      - `<ul>` of `.c-chip-protocol` for `blocked_run_ids[]`, `blocked_task_ids[]`
      - `<p>{next_action body}</p>`
      - **Action set per row**:
        - **Resume run CTA (R-8)**: `<button class="c-button c-button--primary">Resume run</button>` rendered ONLY when `failure_family ∈ {provider_failure, run_blocker}` AND `translation_gate == 'none'`. When mutation-class binding triggers (Resume = `connector.mutate` or `external.send` per 206), opens `.c-modal` first with body containing the verbatim mutation-class chip per UI-SPEC §Surface E copy register: "Resuming this run will re-attempt the failed action. The action is classified as `{mutation_class}` (per 206 doctrine) and will require approval-token consumption." CTA: `.c-button--primary` "Resume" + `.c-button--secondary` "Cancel".
        - **Move to DLQ CTA (R-9)**: `<button class="c-button c-button--destructive">Move to DLQ</button>` always opens `.c-modal` + `.c-backdrop`. Modal heading "Move this run to the DLQ?" + body verbatim "Once moved, the run will require operator triage before any re-attempt. Enter a reason for the audit record." Required `<textarea class="c-input">` rejection_reason. CTAs: `.c-button--destructive` "Move to DLQ" + `.c-button--secondary` "Cancel". Maps to `data.export` mutation-class for audit per UI-SPEC §Destructive actions.
        - **Snooze CTA**: `<button class="c-button c-button--secondary">Snooze 1 hour</button>`.

      After-action notices:
      - Resume success: `[ok] Run resumed.` `.c-notice c-notice--success`.
      - DLQ success: `[ok] Run moved to DLQ.` `.c-notice c-notice--success`.
      - Error: `[err] {error_message}` `.c-notice c-notice--error`.

      Bracketed-glyph state coding (R-11): `[ok]`, `[warn]`, `[err]`, `[info]` paired with every state. State color never the only signal.

      Mint-as-text: `.c-chip-protocol` for run_id, task_id (via `--color-primary-text`).

      **D-13 + D-14 carry**: zero `.c-card--feature`, zero `.c-table` (R-10 + X-4 + X-5).

    Step 3 — `app/(markos)/operations/recovery/page.stories.tsx`: CSF3 with 5 named state stories per 208-UI-SPEC §R-14:
      - `Default`: 2 provider_failure + 1 billing_hold + 1 webhook_delivery + 1 run_blocker + connector_placeholder section showing PlaceholderBanner.
      - `Empty`: 0 items across all families; empty page renders `[ok] No blocked work. Recovery queue is current.`
      - `ProviderFailureBacklog`: 5 provider_failure items with mixed severity (3 major + 2 minor); other families empty.
      - `BillingHoldActive`: 1 billing_hold item with `[err] Major` severity; Resume run CTA hidden (translation_gate !== 'none' or mutation-class binding requires confirm); Move-to-DLQ visible.
      - `ConnectorPlaceholderOnly`: All 4 active families empty; connector_placeholder section is the only visible content; renders `[ok] No blocked work...` page-level notice + connector_placeholder section with PlaceholderBanner.
      - Story fixtures honor 207 AgentFailureClass mapping; 206 mutation-class binding; bracketed glyphs only.

    Step 4 — Update `.planning/phases/208-human-operating-interface/208-VALIDATION.md`:
      - Add row `208-05-02` to per-task verification map.
      - Update `## UI-SPEC AC Coverage Map`: mark R-1..R-14.

    Step 5 — Banned-lexicon compliance (R-13, X-9): zero matches across page.tsx + page.module.css. No exclamation points.

    Step 6 — Mobile-priority compliance (X-8): page is `mobile_priority: 'secondary'` per shell contract (NOT critical — UI-SPEC §"Plan Scope Classification"). At `max-width: 640px`: family sections collapsible; detail card replaces list view (back-button); action bar full-width stacked. Touch targets inherit `--h-control-touch` via global rule.
  </action>
  <acceptance_criteria>
    - **R-1 (token-only)**: `! grep -nE "#[0-9a-fA-F]{3,8}" app/(markos)/operations/recovery/page.module.css` returns 0.
    - **R-2 (h1)**: `grep -c "Recovery center" app/(markos)/operations/recovery/page.tsx` >= 1.
    - **R-6 (5 family sections + future_phase_210)**: `grep -c "provider_failure\|billing_hold\|webhook_delivery\|run_blocker\|connector_placeholder\|future_phase_210" app/(markos)/operations/recovery/page.tsx` >= 6.
    - **R-7 (.c-chip-protocol blocked IDs)**: `grep -c "c-chip-protocol" app/(markos)/operations/recovery/page.tsx` >= 2.
    - **R-8 (Resume run conditional .c-button--primary)**: `grep -c "Resume run\|c-button--primary" app/(markos)/operations/recovery/page.tsx` >= 1.
    - **R-9 (Move-to-DLQ .c-button--destructive)**: `grep -c "c-button--destructive\|Move to DLQ" app/(markos)/operations/recovery/page.tsx` >= 1.
    - **R-10 (vanilla <table>, no .c-table)**: `grep -c "c-table" app/(markos)/operations/recovery/page.tsx` returns 0.
    - **R-11 (bracketed glyphs)**: `grep -c "\[ok\]\|\[warn\]\|\[err\]\|\[info\]" app/(markos)/operations/recovery/page.tsx` >= 4.
    - **R-12 (prefers-reduced-motion)**: `grep -c "prefers-reduced-motion" app/(markos)/operations/recovery/page.module.css` >= 1.
    - **R-13 (banned-lexicon)**: `! grep -nE "synergy\|leverage\|empower\|unlock\|transform" app/(markos)/operations/recovery/page.tsx` returns 0.
    - **R-14 (Storybook ≥5 named state stories)**: `grep -c "Default\|Empty\|ProviderFailureBacklog\|BillingHoldActive\|ConnectorPlaceholderOnly" app/(markos)/operations/recovery/page.stories.tsx` >= 5.
    - **D-13 carry (X-4)**: `grep -c "c-card--feature" app/(markos)/operations/recovery/page.tsx` returns 0.
    - **PlaceholderBanner reused**: page imports `<PlaceholderBanner variant="future_phase_210">` from `components/markos/operator/PlaceholderBanner.tsx`.
    - **208-VALIDATION.md updated**: row `208-05-02` present; UI-SPEC AC Coverage Map R-1..R-14 rows have verification commands.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/operator-cockpit/phase-208/domain-5/recovery-center-ui-spec.test.js</automated>
  </verify>
  <done>Recovery Center renders 5 family sections per 208-UI-SPEC §Surface E layout grid. All 14 ACs R-1..R-14 are automated. The 207 AgentFailureClass mapping + 206 mutation-class Resume/DLQ binding are visible in the UI (mutation-class chip in modal body, audit-class for DLQ). Connector_placeholder section renders future_phase_210 PlaceholderBanner only. Storybook covers 5 named states.</done>
</task>

</tasks>

<verification>
- npm test -- test/operator-cockpit/phase-208/domain-5/
- npm test -- test/operator-cockpit/phase-208/domain-5/recovery-center-ui-spec.test.js
- npm run build-storybook (verifies app/(markos)/operations/recovery/page.stories.tsx registers cleanly)
</verification>

<success_criteria>
- Recovery items show owner, impact scope, blocked work, and next action
- Generic recovery UX exists without re-owning connector substrate
- Connector translation remains explicit for Phase 210
- 208-UI-SPEC §Surface E ACs R-1..R-14 are realized verbatim and verified by Node `--test`
- 207 AgentFailureClass 7 literals feed cockpit failure_family taxonomy via deterministic mapper
- 206 mutation-class doctrine binds Resume run (connector.mutate or external.send) and Move-to-DLQ (data.export) explicitly
- Storybook covers 5 named state stories
- Page is `mobile_priority: 'secondary'` (not critical)
</success_criteria>
