---
phase: 208
plan: "01"
type: execute
wave: 1
depends_on: []
f_ids: []
migration_slots: []
integrates_with:
  - "PRC-01..09, BILL-02 from P205 (pricing posture and placeholder rules must exist before approvals and brief warnings become trustworthy)"
  - "COMP-01 from P206 (compliance posture must exist before operator approvals and governance warnings are surfaced)"
  - "RUN-01..08 from P207 (shell work surfaces should route into real AgentRun and task lineage)"
translation_gate_for:
  - "P209"
  - "P210"
  - "P211"
  - "P212"
files_modified:
  - .planning/phases/208-human-operating-interface/208-VALIDATION.md
  - .planning/operator-cockpit/208-upstream-readiness.md
  - .planning/operator-cockpit/shell-route-contract.md
  - scripts/operator-cockpit/check-interface-upstream-readiness.mjs
  - scripts/operator-cockpit/check-interface-architecture-lock.mjs
  - scripts/operator-cockpit/assert-shell-baseline.mjs
  - app/(markos)/layout-shell.tsx
  - app/(markos)/layout-shell.module.css
  - app/(markos)/_components/NavList.tsx
  - app/(markos)/page.tsx
  - app/(markos)/operations/page.tsx
  - lib/markos/operator/shell.ts
  - lib/markos/operator/index.cjs
  - test/operator-cockpit/phase-208/preflight/upstream-readiness.test.js
  - test/operator-cockpit/phase-208/preflight/architecture-lock.test.js
  - test/operator-cockpit/phase-208/preflight/shell-baseline.test.js
  - test/operator-cockpit/phase-208/preflight/cross-cutting-x-axis.test.js
  - test/operator-cockpit/phase-208/domain-1/route-contract.test.js
  - test/operator-cockpit/phase-208/domain-1/mobile-priority.test.js
  - test/operator-cockpit/phase-208/domain-1/shell-ui-spec.test.js
autonomous: true
requirements:
  - TASK-01
  - TASK-05
  - QA-01
  - QA-02
  - QA-03
must_haves:
  truths:
    - "Wave 0.5 preflight hard-fails when required P205/P206/P207 readiness artifacts are missing or still stubbed."
    - "Phase 208 defines one operator cockpit shell instead of spawning a second dashboard."
    - "Mobile-critical routes for Morning Brief, approvals, and quick work are explicit rather than implied."
    - "Validation strategy is updated with rows for Tasks 208-01-00, 208-01-01, and 208-01-02 before later plans begin."
    - "Cross-cutting carry-forward gates (D-08 token-only, D-09 mint-as-text, D-09b .c-notice mandatory, D-13 no .c-card--feature, D-14 no .c-table, banned-lexicon, no-emoji, mobile touch ≥44px, prefers-reduced-motion) are wired as a preflight test that gates the rest of Phase 208."
    - "Shell-surface ACs S-1..S-10 from 208-UI-SPEC.md are realized verbatim (token-only layout-shell, brand subtitle migration, NavList family enumeration, SurfaceRouteContract field set, surface_family + mobile_priority literal unions, landing replacement, architecture-lock forbidden strings, global pointer-coarse rule, focus-ring inheritance)."
  artifacts:
    - path: .planning/operator-cockpit/208-upstream-readiness.md
      provides: readiness checklist for pricing, compliance, and AgentRun substrate
    - path: .planning/operator-cockpit/shell-route-contract.md
      provides: canonical cockpit route map and mobile-priority policy
    - path: scripts/operator-cockpit/check-interface-upstream-readiness.mjs
      provides: deterministic preflight for Phases 205-207 cockpit prerequisites
    - path: scripts/operator-cockpit/check-interface-architecture-lock.mjs
      provides: architecture-lock + cross-cutting (X-1..X-10) preflight tripwire
    - path: lib/markos/operator/shell.ts
      provides: SurfaceRouteContract type + 5 surface_family literals + 3 mobile_priority literals (208-UI-SPEC §S-4..S-6)
  key_links:
    - from: app/(markos)/layout-shell.tsx
      to: .planning/operator-cockpit/shell-route-contract.md
      via: existing shell structure should be migrated, not replaced by a second dashboard
      pattern: "content"
    - from: app/(markos)/operations/page.tsx
      to: lib/markos/operator/shell.ts
      via: the current operations gateway is the natural migration anchor for the operator cockpit
      pattern: "operations"
    - from: app/(markos)/page.tsx
      to: 208-UI-SPEC §S-7
      via: landing replaces Phase 37 entity-list scaffold; renders Morning Brief content (Surface B in Plan 02)
      pattern: "ENTITY_ORDER"
ui_spec_acs_folded:
  - "S-1..S-10 (Shell + Default Landing — 10 ACs)"
  - "X-1..X-10 (cross-cutting carry-forward — 10 ACs, wired as preflight tripwire that gates 208-02..208-06)"
---

<objective>
Establish the cockpit foundation for Phase 208: upstream readiness and architecture lock, then a durable shell route contract that defines one operator shell, one landing posture, and clear mobile-critical surfaces. Fold 208-UI-SPEC.md surface-S (Shell + Default Landing — S-1..S-10) and the cross-cutting carry-forward axis (X-1..X-10) into this plan as preflight + per-AC tasks. The cross-cutting axis is a Wave-1 tripwire — if it fails, Phase 208 cannot proceed past 208-01.
</objective>

<context>
@.planning/phases/208-human-operating-interface/208-CONTEXT.md
@.planning/phases/208-human-operating-interface/208-RESEARCH.md
@.planning/phases/208-human-operating-interface/208-REVIEWS.md
@.planning/phases/208-human-operating-interface/208-VALIDATION.md
@.planning/phases/208-human-operating-interface/208-UI-SPEC.md
@.planning/phases/206-soc2-type1-foundation/206-UI-SPEC.md
@.planning/phases/207-agentrun-v2-orchestration-substrate/207-UI-SPEC.md
@DESIGN.md
@CLAUDE.md
@app/(markos)/layout-shell.tsx
@app/(markos)/layout-shell.module.css
@app/(markos)/_components/NavList.tsx
@app/(markos)/page.tsx
@app/(markos)/operations/page.tsx
</context>

<tasks>

<task id="208-01-00">
  <name>Wave 0.5 — UI-SPEC fold preflight: read UI-SPEC, ingest token canon, register cross-cutting axis</name>
  <files>test/operator-cockpit/phase-208/preflight/cross-cutting-x-axis.test.js, .planning/phases/208-human-operating-interface/208-VALIDATION.md</files>
  <read_first>
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md (entire file; primary fold input)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Surface A — Shell + Default Landing (Plan 208-01)" (lines for Surface A)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"S-* — Shell + Default Landing (Plan 208-01)" AC table (S-1..S-10)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"X-* — Cross-cutting (carry-forward closure)" AC table (X-1..X-10)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Cross-surface Tokens Reference Table" (single source of truth for token usage)
    - DESIGN.md (canonical visual contract; v1.1.0)
    - .planning/phases/206-soc2-type1-foundation/206-UI-SPEC.md §Downstream UI Inheritance Map (mutation-class doctrine)
    - .planning/phases/207-agentrun-v2-orchestration-substrate/207-UI-SPEC.md §Downstream UI Inheritance Map (RunApiEnvelope + 14 surface families)
    - styles/components.css (existing primitives v1.1.0)
    - app/tokens.css (existing token register)
    - .planning/phases/213.4-ui-canon-adoption-wave-4-admin-operations-status-404-workspace-to-design-md-tokens/213.4-VALIDATION.md (carry-forward decisions D-08, D-09, D-09b, D-13, D-14, D-15)
  </read_first>
  <action>
    Step 1: Create `test/operator-cockpit/phase-208/preflight/cross-cutting-x-axis.test.js`. This test is the load-bearing tripwire that gates 208-02..208-06; if any X-1..X-10 invariant fails for the shell scope, the rest of Phase 208 cannot start.

    Step 2: The test file must use Node `--test` and ship 10 named test blocks, one per X AC, mirroring the verbatim verification commands from 208-UI-SPEC §X-* AC table:

      - `test('X-1 token-only: zero hex in 208 module.css surfaces', ...)`: assert `! grep -rnE "#[0-9a-fA-F]{3,8}" app/(markos)/page.module.css app/(markos)/operations/{approvals,recovery,narrative}/page.module.css` returns 0 lines (skip files that do not yet exist; later plans wire in).
      - `test('X-2 mint-as-text: zero c-card border or fill using --color-primary', ...)`: assert `! grep -nE "background:[[:space:]]+var\(--color-primary\)[^-]" app/(markos)/{page,operations/approvals/page,operations/recovery/page,operations/narrative/page}.module.css` returns 0.
      - `test('X-3 .c-notice mandatory: zero local .banner/.alert/.warning/.callout/.noticeBar in 208 module.css', ...)`.
      - `test('X-4 .c-card--feature reserved: zero references in 208 page.tsx', ...)`.
      - `test('X-5 no .c-table primitive: zero references in 208 page.tsx', ...)`.
      - `test('X-6 selective extraction: components/markos/operator only contains proven-reuse extractions', ...)`.
      - `test('X-7 Storybook: ≥4 new page.stories.tsx exist by Plan 06 close', ...)`: skip-soft until later waves; final wave gate.
      - `test('X-8 touch-target ≥44px: (pointer: coarse) global rule preserved in styles/components.css', ...)`.
      - `test('X-9 banned lexicon: zero matches across page.tsx + lib/markos/operator/*.ts', ...)`: full banned register from 208-UI-SPEC §Copywriting Contract — `synergy|leverage|empower|unlock|transform|revolutionize|supercharge|holistic|seamless|cutting-edge|innovative|game-changer|next-generation|world-class|best-in-class|reimagine|disrupt`.
      - `test('X-10 architecture-lock: 4 forbidden strings rejected by check-interface-architecture-lock.mjs', ...)`: invokes the script created in 208-01-01 and asserts exit code + match output.

    Each test must use `node:fs` `readFileSync`, `node:child_process` `execSync` for grep invocations, and skip files that do not yet exist (Wave 1 may not have all surface files created — that is by design). Skipped files are tracked via `t.skip(reason)` so the report is auditable.

    Step 3: Update `.planning/phases/208-human-operating-interface/208-VALIDATION.md` per-task map: add row `208-01-00` (already present — extend `Test Type` to `preflight + cross-cutting (X-1..X-10)`), add row `208-01-02` for the new shell UI-SPEC fold, add an `X-1..X-10` row to track the cross-cutting tripwire separately.

    Step 4: Add a `## UI-SPEC AC Coverage Map` section to `208-VALIDATION.md` enumerating every UI-SPEC AC (S-1..S-10, B-1..B-16, T-1..T-17, A-1..A-19, R-1..R-14, N-1..N-13, X-1..X-10 = 89 AC entries) with columns: `AC`, `Surface`, `Plan`, `Task`, `Verification`, `Status`. This makes the fold auditable.
  </action>
  <acceptance_criteria>
    - `test/operator-cockpit/phase-208/preflight/cross-cutting-x-axis.test.js` exists and contains exactly 10 `test(...)` blocks named `X-1` through `X-10`.
    - `cross-cutting-x-axis.test.js` contains the exact banned-lexicon register: `grep -c "synergy\|leverage\|empower\|unlock\|transform\|revolutionize\|supercharge" test/operator-cockpit/phase-208/preflight/cross-cutting-x-axis.test.js` >= 7.
    - `cross-cutting-x-axis.test.js` contains the exact architecture-lock forbidden strings: `grep -c "second dashboard shell\|desktop-only approval\|future-phase data assumption\|hidden rejection reason" test/operator-cockpit/phase-208/preflight/cross-cutting-x-axis.test.js` >= 4.
    - `208-VALIDATION.md` contains a `## UI-SPEC AC Coverage Map` section with at least 76 AC rows (S-1..S-10, B-1..B-16, T-1..T-17, A-1..A-19, R-1..R-14, N-1..N-13).
    - `208-VALIDATION.md` contains rows `208-01-00`, `208-01-01`, and `208-01-02`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/operator-cockpit/phase-208/preflight/cross-cutting-x-axis.test.js</automated>
  </verify>
  <done>The cross-cutting carry-forward axis is a single, named, runnable Node-test tripwire. If any of X-1..X-10 trip during 208-02..208-06 execution, the suite fails fast with a named AC. The validation map carries every UI-SPEC AC verbatim.</done>
</task>

<task id="208-01-01">
  <name>Wave 0.5 upstream preflight, architecture lock, and validation baseline</name>
  <files>.planning/operator-cockpit/208-upstream-readiness.md, scripts/operator-cockpit/check-interface-upstream-readiness.mjs, scripts/operator-cockpit/check-interface-architecture-lock.mjs, scripts/operator-cockpit/assert-shell-baseline.mjs, test/operator-cockpit/phase-208/preflight/upstream-readiness.test.js, test/operator-cockpit/phase-208/preflight/architecture-lock.test.js, test/operator-cockpit/phase-208/preflight/shell-baseline.test.js, .planning/phases/208-human-operating-interface/208-VALIDATION.md</files>
  <read_first>
    - .planning/phases/208-human-operating-interface/208-RESEARCH.md section "Domain 0 - Upstream readiness and architecture lock"
    - .planning/phases/208-human-operating-interface/208-REVIEWS.md sections HIGH and MEDIUM concerns
    - .planning/phases/205-pricing-engine-foundation-billing-readiness/205-RESEARCH.md
    - .planning/phases/206-soc2-type1-foundation/206-RESEARCH.md
    - .planning/phases/207-agentrun-v2-orchestration-substrate/207-RESEARCH.md
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §S-8 (architecture-lock forbidden-string register)
  </read_first>
  <action>
    Step 1: Create `.planning/operator-cockpit/208-upstream-readiness.md` with one row each for phases `205`, `206`, and `207`, and columns `phase`, `required_input`, `evidence_path`, `status`, `blocker_owner`, and `notes`.

    Step 2: Implement `scripts/operator-cockpit/check-interface-upstream-readiness.mjs` so it hard-fails when those required artifacts are missing and soft-warns when they exist but are stale. Phase keys consumed: exact strings `205`, `206`, `207`.

    Step 3: Implement `scripts/operator-cockpit/check-interface-architecture-lock.mjs` so it rejects the verbatim forbidden strings from 208-UI-SPEC §S-8: `vitest`, `playwright`, `.test.ts`, `route.ts`, `second dashboard shell`, `desktop-only approval`, `future-phase data assumption`, and `hidden rejection reason`. The script scans 208 surface files (`app/(markos)/page.tsx`, `app/(markos)/operations/{page,approvals/page,recovery/page,narrative/page,tasks/page}.tsx`, `lib/markos/operator/*.ts`, `.planning/operator-cockpit/*.md`).

    Step 4: Implement `scripts/operator-cockpit/assert-shell-baseline.mjs` so it asserts: `app/(markos)/layout-shell.tsx` token-only (zero hex in `layout-shell.module.css` per 208-UI-SPEC AC S-1); brand subtitle copy is exactly "Operator cockpit" per S-2; NavList contains all 5 family labels per S-3 ("Morning brief", "Approvals", "Tasks", "Recovery", "Narrative"); landing `app/(markos)/page.tsx` does not contain `ENTITY_ORDER` or "Phase 37 scaffold" per S-7.

    Step 5: Add preflight tests:
      - `test/operator-cockpit/phase-208/preflight/upstream-readiness.test.js` — invokes the readiness script and asserts exit codes per phase.
      - `test/operator-cockpit/phase-208/preflight/architecture-lock.test.js` — invokes the architecture-lock script and asserts the 4 forbidden literal strings are present in the lock register (per 208-UI-SPEC AC S-8 + X-10).
      - `test/operator-cockpit/phase-208/preflight/shell-baseline.test.js` — invokes the baseline script and asserts S-1, S-2, S-3, S-7 invariants.

    Step 6: Update `208-VALIDATION.md` so Tasks `208-01-00`, `208-01-01`, and `208-01-02` exist in the verification map before later plans begin.
  </action>
  <acceptance_criteria>
    - `.planning/operator-cockpit/208-upstream-readiness.md` contains one row each for phases `205`, `206`, and `207`.
    - `scripts/operator-cockpit/check-interface-upstream-readiness.mjs` contains the exact phase keys `205`, `206`, and `207`.
    - `scripts/operator-cockpit/check-interface-architecture-lock.mjs` contains the exact forbidden strings `second dashboard shell`, `desktop-only approval`, `future-phase data assumption`, and `hidden rejection reason` (208-UI-SPEC AC S-8 + X-10): `grep -c "second dashboard shell\|desktop-only approval\|future-phase data assumption\|hidden rejection reason" scripts/operator-cockpit/check-interface-architecture-lock.mjs` >= 4.
    - `scripts/operator-cockpit/assert-shell-baseline.mjs` contains the exact strings "Operator cockpit", "Morning brief", "Approvals", "Tasks", "Recovery", "Narrative" (208-UI-SPEC AC S-2 + S-3): `grep -c "Operator cockpit\|Morning brief\|Approvals\|Tasks\|Recovery\|Narrative" scripts/operator-cockpit/assert-shell-baseline.mjs` >= 6.
    - `208-VALIDATION.md` contains rows `208-01-00`, `208-01-01`, and `208-01-02`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/operator-cockpit/phase-208/preflight/</automated>
  </verify>
  <done>Phase 208 now fails closed when pricing, compliance, or AgentRun substrate is not truly ready, and it cannot silently drift into a second-dashboard architecture. The shell baseline (S-1, S-2, S-3, S-7) is enforced by an automated script gate.</done>
</task>

<task id="208-01-02">
  <name>Shell route contract, mobile-priority policy, and Surface-A UI-SPEC fold (S-1..S-10)</name>
  <files>.planning/operator-cockpit/shell-route-contract.md, app/(markos)/layout-shell.tsx, app/(markos)/layout-shell.module.css, app/(markos)/_components/NavList.tsx, app/(markos)/page.tsx, app/(markos)/operations/page.tsx, lib/markos/operator/shell.ts, lib/markos/operator/index.cjs, test/operator-cockpit/phase-208/domain-1/route-contract.test.js, test/operator-cockpit/phase-208/domain-1/mobile-priority.test.js, test/operator-cockpit/phase-208/domain-1/shell-ui-spec.test.js</files>
  <read_first>
    - .planning/phases/208-human-operating-interface/208-RESEARCH.md section "Domain 1 - Shell and mobile route strategy"
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Surface A — Shell + Default Landing (Plan 208-01)" (verbatim layout grid, copy register, state matrix, accessibility focus order)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"S-* — Shell + Default Landing (Plan 208-01)" AC table (S-1..S-10) — verbatim verification gates
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Cross-surface Tokens Reference Table" (token names + values for S-1)
    - .planning/phases/213.4-ui-canon-adoption-wave-4-admin-operations-status-404-workspace-to-design-md-tokens/213.4-VALIDATION.md §"Carry-forward (D-08, D-09, D-09b, D-13, D-14, D-15)"
    - DESIGN.md §"Iconography" (bracketed-glyph register including `[block]` resolution per 208-UI-SPEC fallback note)
    - app/(markos)/layout-shell.tsx
    - app/(markos)/layout-shell.module.css
    - app/(markos)/_components/NavList.tsx
    - app/(markos)/page.tsx
    - app/(markos)/operations/page.tsx
    - styles/components.css §`@media (pointer: coarse)` (global rule from 213.2)
  </read_first>
  <action>
    Step 1 — `shell-route-contract.md`: Create `.planning/operator-cockpit/shell-route-contract.md` with the exact `SurfaceRouteContract` field set per 208-UI-SPEC AC S-4: `surface_id`, `route`, `surface_family`, `default_role`, `navigation_label`, `mobile_priority`, `placeholder_policy`, `source_dependencies`, `created_at`, `updated_at`. Include a route enumeration table for the 5 families (Morning brief = `/`, Approvals = `/operations/approvals`, Tasks = `/operations/tasks`, Recovery = `/operations/recovery`, Narrative = `/operations/narrative`).

    Step 2 — `lib/markos/operator/shell.ts` (TypeScript):
      - Export `SurfaceRouteContract` interface with the exact 10 fields from Step 1.
      - Export `surface_family` literal union with the exact 5 literals per 208-UI-SPEC AC S-5: `'morning_brief' | 'task_board' | 'approval_inbox' | 'recovery_center' | 'weekly_narrative'`.
      - Export `mobile_priority` literal union with the exact 3 literals per 208-UI-SPEC AC S-6: `'critical' | 'secondary' | 'desktop_only'`.
      - Export the surface contract array: Morning brief / Approvals / Task Board = `mobile_priority: 'critical'`; Recovery / Narrative = `mobile_priority: 'secondary'`. **Per 208-UI-SPEC AC S-6 invariant: no surface uses `desktop_only`.**

    Step 3 — `lib/markos/operator/index.cjs` (CommonJS twin): mirror the TypeScript contract for runtime / Node `--test` consumption.

    Step 4 — `app/(markos)/layout-shell.tsx` (S-2): replace brand subtitle copy "UI Control Plane" with exactly "Operator cockpit". Verify zero new hex in `layout-shell.module.css` (S-1 token-only).

    Step 5 — `app/(markos)/_components/NavList.tsx` (S-3): NavList items include all 5 cockpit family labels in order: "Morning brief", "Approvals", "Tasks", "Recovery", "Narrative". Each renders `.c-nav-link` and applies `aria-current="page"` via `.c-nav-link.is-active`. (Existing settings/admin items unchanged; only the cockpit family group is added or repositioned to the top of the list.)

    Step 6 — `app/(markos)/page.tsx` (S-7): remove Phase 37 entity-list scaffold. Replace with a thin landing component that imports the Morning Brief surface (filled in by Plan 208-02). For this Wave-1 commit, render a placeholder `<main>` with the `<h1>Morning brief</h1>` and the lead descriptor text from 208-UI-SPEC §Surface B copy register; Plan 208-02 then folds the section content. Verify `grep -c "ENTITY_ORDER\|Phase 37 scaffold" app/(markos)/page.tsx` returns 0 (S-7).

    Step 7 — `app/(markos)/operations/page.tsx`: preserve 51-03 IAM gate verbatim; this plan only adds nav-link parity to the new shell contract, not a rewrite.

    Step 8 — Domain-1 tests (one file per S-axis):
      - `route-contract.test.js`: assert all 10 `SurfaceRouteContract` field names present in `lib/markos/operator/shell.ts` (S-4), all 5 `surface_family` literals (S-5), all 3 `mobile_priority` literals (S-6).
      - `mobile-priority.test.js`: assert the invariant — `morning_brief`, `approval_inbox`, `task_board` never use `desktop_only` (S-6); `recovery_center` and `weekly_narrative` use `secondary`. Contains the exact string `desktop_only` for the AC verbatim grep gate.
      - `shell-ui-spec.test.js` (NEW; covers the rest of S-1..S-10):
        - S-1: `! grep -nE "#[0-9a-fA-F]{3,8}" app/(markos)/layout-shell.module.css` returns 0.
        - S-2: `grep -c "Operator cockpit" app/(markos)/layout-shell.tsx` >= 1.
        - S-3: `grep -c "Morning brief\|Approvals\|Tasks\|Recovery\|Narrative" app/(markos)/_components/NavList.tsx` >= 5.
        - S-7: `grep -c "ENTITY_ORDER\|Phase 37 scaffold" app/(markos)/page.tsx` returns 0.
        - S-8: defers to architecture-lock test in 208-01-01 (cross-reference; assert that test exists).
        - S-9: `grep -nE "@media \(pointer: coarse\)" styles/components.css` returns ≥1 (global rule preserved; this plan adds NO new local override).
        - S-10: `grep -E "outline:\s*(none|0)" app/(markos)/layout-shell.module.css` returns 0 (focus rings inherited globally; never suppressed).

    Step 9 — Note for 208-UI-SPEC §Surface A "[block] glyph canon resolution": DESIGN.md §Iconography enumerates `[ok]`, `[warn]`, `[err]`, `[info]`. The `[block]` glyph is referenced by 208-UI-SPEC for autonomy-ceiling-reached and Task Board blocked-state. Decision recorded in this plan: `[block]` is a token-equivalent fallback to `[err]` until DESIGN.md v1.2 lands; the cross-cutting test (X-9 banned-lexicon) explicitly allows both `[block]` and `[err]` for blocked-state copy. Plans 208-03/04/05 must use `[block]` first with `[err]` as fallback per 208-UI-SPEC AC fallback note in B-12, T-13, A-13, R-11.
  </action>
  <acceptance_criteria>
    - `.planning/operator-cockpit/shell-route-contract.md` contains the exact field names `surface_family`, `mobile_priority`, `placeholder_policy`, `source_dependencies` (208-UI-SPEC S-4): `grep -c "surface_family\|mobile_priority\|placeholder_policy\|source_dependencies" .planning/operator-cockpit/shell-route-contract.md` >= 4.
    - `lib/markos/operator/shell.ts` contains the exact 5 surface_family literals (208-UI-SPEC S-5): `grep -c "morning_brief\|task_board\|approval_inbox\|recovery_center\|weekly_narrative" lib/markos/operator/shell.ts` >= 5.
    - `lib/markos/operator/shell.ts` contains the exact 3 mobile_priority literals (208-UI-SPEC S-6): `grep -c "critical\|secondary\|desktop_only" lib/markos/operator/shell.ts` >= 3.
    - **S-6 invariant gate**: `grep -nE "(morning_brief|approval_inbox|task_board)\b.*desktop_only" lib/markos/operator/shell.ts` returns 0.
    - `test/operator-cockpit/phase-208/domain-1/mobile-priority.test.js` contains the exact string `desktop_only`.
    - **S-1 (token-only)**: `! grep -nE "#[0-9a-fA-F]{3,8}" app/(markos)/layout-shell.module.css` returns 0.
    - **S-2 (brand subtitle)**: `grep -c "Operator cockpit" app/(markos)/layout-shell.tsx` >= 1.
    - **S-3 (NavList families)**: `grep -c "Morning brief\|Approvals\|Tasks\|Recovery\|Narrative" app/(markos)/_components/NavList.tsx` >= 5.
    - **S-7 (landing replaced)**: `grep -c "ENTITY_ORDER\|Phase 37 scaffold" app/(markos)/page.tsx` returns 0.
    - **S-9 (pointer: coarse global rule preserved)**: `grep -nE "@media \(pointer: coarse\)" styles/components.css` returns ≥1.
    - **S-10 (focus inheritance)**: `grep -E "outline:\s*(none|0)" app/(markos)/layout-shell.module.css` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/operator-cockpit/phase-208/domain-1/</automated>
  </verify>
  <done>The shell now has a durable route contract, a single cockpit posture, an explicit mobile-critical priority policy, and verbatim adoption of all 10 Shell-Surface UI-SPEC ACs (S-1 through S-10). NavList exposes all 5 cockpit families. Landing has migrated off Phase 37 entity-list scaffold and is ready for Plan 208-02 to fold Morning Brief content.</done>
</task>

</tasks>

<verification>
- npm test -- test/operator-cockpit/phase-208/preflight/
- npm test -- test/operator-cockpit/phase-208/preflight/cross-cutting-x-axis.test.js
- npm test -- test/operator-cockpit/phase-208/domain-1/
</verification>

<success_criteria>
- Upstream readiness artifacts exist for P205-P207
- Unsafe shell shortcuts are blocked by architecture lock
- Shell routes and mobile priorities are explicit and singular
- Validation strategy is seeded before later plans execute
- 208-UI-SPEC §Surface A ACs S-1..S-10 are realized and verified by automated tests
- Cross-cutting carry-forward axis X-1..X-10 is wired as a Wave-1 tripwire that gates 208-02..208-06
- 208-VALIDATION.md UI-SPEC AC Coverage Map enumerates all 76 ACs (S-* + B-* + T-* + A-* + R-* + N-* + X-*)
</success_criteria>
</output>
