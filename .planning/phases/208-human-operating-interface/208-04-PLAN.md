---
phase: 208
plan: "04"
type: execute
wave: 4
depends_on:
  - "208-01"
  - "208-03"
f_ids: []
migration_slots: []
integrates_with:
  - "PRC-01..09, BILL-02 from P205 (pricing-sensitive approvals should surface approved-pricing versus placeholder posture)"
  - "COMP-01 from P206 (compliance posture and rejection reasons must be visible in the approval decision)"
  - "RUN-01..08 from P207 (approval items should route back into real runs and tasks)"
translation_gate_for:
  - "P209"
  - "P210"
files_modified:
  - .planning/phases/208-human-operating-interface/208-VALIDATION.md
  - .planning/operator-cockpit/approval-inbox-contract.md
  - app/(markos)/operations/approvals/page.tsx
  - app/(markos)/operations/approvals/page.module.css
  - app/(markos)/operations/approvals/page.stories.tsx
  - lib/markos/operator/approvals.ts
  - lib/markos/operator/index.cjs
  - components/markos/operator/Money.tsx
  - test/operator-cockpit/phase-208/domain-4/approval-item.test.js
  - test/operator-cockpit/phase-208/domain-4/blocker-states.test.js
  - test/operator-cockpit/phase-208/domain-4/approval-inbox-ui-spec.test.js
autonomous: true
requirements:
  - TASK-04
  - QA-09
  - QA-10
must_haves:
  truths:
    - "Approval items expose preview, edit, approve, reject, and rejection-reason posture."
    - "Pricing and compliance posture are explicit in the approval surface now."
    - "Evidence and connector-specific states remain explicit placeholders until later translation phases provide the substrate."
    - "Every mutation_family maps verbatim to one of the 6 root mutation-class literals from 206 (external.send, billing.charge, connector.mutate, price.change, public.claim, data.export) per 208-UI-SPEC §A-6."
    - "ApprovalHandoffRecord field set from 207 (handoff_kind, approval_required, side_effect_kind, target_system, task_ref, reason) drives the approval row data per 208-UI-SPEC §Surface D."
    - "208-UI-SPEC §Surface D ACs A-1..A-19 are realized verbatim (token-only page.module.css, h1 + descriptor, ApprovalItem TS contract, mutation_family + evidence_status + handoff_kind + mutation_class literal unions, dual-approval gate when default_approval_mode=='dual_approval', autonomy-ceiling block notice with [block] glyph, evidence_status==waiting_phase_209 → PlaceholderBanner future_phase_209, Money recipe consuming RunCostContext, .c-button --primary/--destructive/--secondary/--tertiary set, .c-modal+.c-backdrop with required reason input, .c-notice success/error after-action notices, prefers-reduced-motion, banned-lexicon zero match, mobile sequential pages with stacked full-width buttons + 44px touch targets, ≥6 named state stories)."
  artifacts:
    - path: .planning/operator-cockpit/approval-inbox-contract.md
      provides: canonical Approval Inbox item shape, 9 mutation_family literals, 4 evidence_status literals, mutation_family→mutation_class root mapping table
    - path: lib/markos/operator/approvals.ts
      provides: ApprovalItem TS contract + 9 mutation_family literals + 4 evidence_status literals + 6 root mutation_class literals + mutation_family→mutation_class mapping function (208-UI-SPEC §A-3..A-6)
    - path: app/(markos)/operations/approvals/page.tsx
      provides: Approval Inbox page composing list + detail card per 208-UI-SPEC §Surface D layout grid (filter chips + list table + detail rail with mutation/pricing/compliance/evidence/cost/preview rows + Approve/Decline/Snooze action set + dual-approval modal + autonomy-ceiling notice + future_phase_209 placeholder)
    - path: app/(markos)/operations/approvals/page.stories.tsx
      provides: ≥6 named state stories per 208-UI-SPEC §A-19 (Default, Empty, DualApprovalRequired, AutonomyCeilingReached, EvidenceWaitingPhase209, EvidenceBlocked)
    - path: components/markos/operator/Money.tsx
      provides: Money recipe consuming 207 RunCostContext.estimated_cost_usd_micro / actual_cost_usd_micro per 208-UI-SPEC §A-12; renders MARKOS_PRICING_ENGINE_PENDING when pricing_engine_context.schema_version unset
  key_links:
    - from: app/(markos)/operations/approvals/page.tsx
      to: lib/markos/operator/approvals.ts
      via: page consumes ApprovalItem array; renders list + detail card per UI-SPEC §Surface D
      pattern: "ApprovalItem"
    - from: app/(markos)/operations/tasks/evidence-panel.tsx
      to: .planning/operator-cockpit/approval-inbox-contract.md
      via: the current evidence drawer can inform preview posture without Phase 208 pretending EvidenceMap already exists
      pattern: "evidence"
    - from: app/(markos)/admin/governance/page.tsx
      to: lib/markos/operator/approvals.ts
      via: governance risk and controls should be operator-visible in approval decisions
      pattern: "governance"
    - from: components/markos/operator/PlaceholderBanner.tsx
      to: app/(markos)/operations/approvals/page.tsx
      via: future_phase_209 evidence placeholder reused from 208-02-02 PlaceholderBanner extraction (D-15 reuse threshold)
      pattern: "future_phase_209"
ui_spec_acs_folded:
  - "A-1..A-19 (Approval Inbox — 19 ACs)"
  - "X-1..X-5 (cross-cutting verified by 208-01 tripwire applied to Approval Inbox surface files)"
---

<objective>
Define a single Approval Inbox contract for customer-facing and external-world mutations, using pricing and compliance posture now while keeping evidence-specific and connector-specific substrate behind explicit translation gates. Fold 208-UI-SPEC.md §Surface D (Approval Inbox — A-1..A-19) verbatim into per-AC tasks. The 206 mutation-class doctrine (autonomy ceiling, dual approval, evidence_required) is load-bearing and explicit in the UI, not just the data.
</objective>

<context>
@.planning/phases/208-human-operating-interface/208-CONTEXT.md
@.planning/phases/208-human-operating-interface/208-RESEARCH.md
@.planning/phases/208-human-operating-interface/208-VALIDATION.md
@.planning/phases/208-human-operating-interface/208-UI-SPEC.md
@.planning/phases/206-soc2-type1-foundation/206-UI-SPEC.md
@.planning/phases/207-agentrun-v2-orchestration-substrate/207-UI-SPEC.md
@DESIGN.md
@CLAUDE.md
@app/(markos)/operations/tasks/evidence-panel.tsx
@app/(markos)/admin/governance/page.tsx
@lib/markos/operator/shell.ts
@components/markos/operator/PlaceholderBanner.tsx
</context>

<tasks>

<task id="208-04-00">
  <name>Wave 0 — UI-SPEC §Surface D fold preflight: read Approval Inbox AC table</name>
  <files>test/operator-cockpit/phase-208/domain-4/approval-inbox-ui-spec.test.js</files>
  <read_first>
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Surface D — Approval Inbox (Plan 208-04)" full body — verbatim layout grid (desktop split + mobile sequential), copy register table (every cell verbatim including dual-approval modal + decline modal + after-action notices), state matrix (10 states), accessibility focus order, motion notes, mobile breakpoint behavior
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"A-* — Approval Inbox (Plan 208-04)" AC table (A-1..A-19)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Cross-surface Tokens Reference Table"
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Storybook Coverage" (page.stories.tsx 6 named states: Default, Empty, DualApprovalRequired, AutonomyCeilingReached, EvidenceWaitingPhase209, EvidenceBlocked)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Inheritance Bindings" §"From 206-UI-SPEC §Downstream UI Inheritance Map" (mutation-class chip; default_approval_mode badge; autonomy-ceiling notice; evidence_required gate)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Inheritance Bindings" §"From 207-UI-SPEC §Downstream UI Inheritance Map" (ApprovalHandoffRecord field set; handoff_kind 4 literals)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Destructive actions" table (Decline approval mutation-class binding)
    - .planning/phases/206-soc2-type1-foundation/206-UI-SPEC.md §"mutation-class-policy.md" (6 root literals + default_approval_mode + autonomy_ceiling + evidence_required fields)
    - DESIGN.md §"Iconography" + §"Color blindness"
    - styles/components.css (.c-card, .c-notice, .c-badge, .c-button, .c-chip, .c-chip-protocol, .c-modal, .c-backdrop, .c-input, .c-field__error, .c-status-dot primitives)
    - components/markos/operator/PlaceholderBanner.tsx (extracted in 208-02-02; reused for future_phase_209)
  </read_first>
  <action>
    Step 1: Create `test/operator-cockpit/phase-208/domain-4/approval-inbox-ui-spec.test.js` as the 19-AC verification harness for A-1..A-19. Each `test(...)` block names the AC and runs the verbatim grep / file-existence check from 208-UI-SPEC §A-* AC table.

    Step 2: 19 named test blocks, one per AC:

    - `test('A-1 token-only zero hex in approvals/page.module.css', ...)`: assert `! grep -nE "#[0-9a-fA-F]{3,8}" app/(markos)/operations/approvals/page.module.css` returns 0.
    - `test('A-2 page heading "Approvals"', ...)`: assert `grep -c "Approvals" app/(markos)/operations/approvals/page.tsx` >= 1.
    - `test('A-3 ApprovalItem TS contract', ...)`: assert `grep -c "compliance_status\|pricing_status\|evidence_status\|cost_estimate_ref\|rejection_reason_required" lib/markos/operator/approvals.ts` >= 5.
    - `test('A-4 mutation_family literals subset', ...)`: assert `grep -c "content_publish\|crm_mutation\|price_change\|connector_action_placeholder" lib/markos/operator/approvals.ts` >= 4.
    - `test('A-5 evidence_status literals', ...)`: assert `grep -c "not_required\|waiting_phase_209\|ready\|blocked" lib/markos/operator/approvals.ts` >= 4.
    - `test('A-6 6 root mutation_class literals + mapping', ...)`: assert `grep -c "external\\.send\|billing\\.charge\|connector\\.mutate\|price\\.change\|public\\.claim\|data\\.export" lib/markos/operator/approvals.ts` >= 6.
    - `test('A-7 4 handoff_kind filter chips', ...)`: assert page.tsx contains all 4 handoff literals: approval, recovery, follow_up, manual_input.
    - `test('A-8 Approve/Decline/.c-modal/.c-input', ...)`: assert `grep -c "c-button--primary\|c-button--destructive\|c-modal\|c-input" app/(markos)/operations/approvals/page.tsx` >= 4.
    - `test('A-9 dual-approval gate', ...)`: assert `grep -c "dual_approval\|Second approval required" app/(markos)/operations/approvals/page.tsx` >= 1.
    - `test('A-10 autonomy-ceiling notice', ...)`: assert `grep -c "autonomy_ceiling\|Autonomy ceiling reached" app/(markos)/operations/approvals/page.tsx` >= 1.
    - `test('A-11 evidence future_phase_209 placeholder', ...)`: assert `grep -c "future_phase_209\|waiting_phase_209" app/(markos)/operations/approvals/page.tsx` >= 1.
    - `test('A-12 Money recipe + estimated_cost_usd_micro / pricing placeholder', ...)`: assert `grep -c "Money\|estimated_cost_usd_micro\|MARKOS_PRICING_ENGINE_PENDING" app/(markos)/operations/approvals/page.tsx` >= 2.
    - `test('A-13 bracketed-glyph state coding (5 glyphs)', ...)`: assert `grep -c "\\[ok\\]\\|\\[warn\\]\\|\\[err\\]\\|\\[info\\]\\|\\[block\\]" app/(markos)/operations/approvals/page.tsx` >= 6.
    - `test('A-14 D-13 + D-14 carry: no .c-table no .c-card--feature', ...)`: assert `grep -c "c-table\|c-card--feature" app/(markos)/operations/approvals/page.tsx` returns 0.
    - `test('A-15 .c-notice success/error after-action', ...)`: assert `grep -c "c-notice--success\|c-notice--error" app/(markos)/operations/approvals/page.tsx` >= 2.
    - `test('A-16 prefers-reduced-motion block', ...)`: assert `grep -c "prefers-reduced-motion" app/(markos)/operations/approvals/page.module.css` >= 1.
    - `test('A-17 mobile-critical sequential pages', ...)`: assert `grep -c "max-width:[[:space:]]*640px" app/(markos)/operations/approvals/page.module.css` >= 1.
    - `test('A-18 banned-lexicon zero match', ...)`: assert `! grep -nE "synergy|leverage|empower|unlock|transform" app/(markos)/operations/approvals/page.tsx` returns 0.
    - `test('A-19 Storybook ≥6 named state stories', ...)`: assert `grep -c "Default\|Empty\|DualApprovalRequired\|AutonomyCeilingReached\|EvidenceWaitingPhase209\|EvidenceBlocked" app/(markos)/operations/approvals/page.stories.tsx` >= 6.
  </action>
  <acceptance_criteria>
    - `test/operator-cockpit/phase-208/domain-4/approval-inbox-ui-spec.test.js` exists with exactly 19 named test blocks A-1 through A-19.
    - Test file references all 6 root mutation_class literals: `grep -c "external\.send\|billing\.charge\|connector\.mutate\|price\.change\|public\.claim\|data\.export" test/operator-cockpit/phase-208/domain-4/approval-inbox-ui-spec.test.js` >= 6.
    - Test file references all 9 mutation_family literals (subset check): `grep -c "content_publish\|crm_mutation\|price_change\|connector_action_placeholder" test/operator-cockpit/phase-208/domain-4/approval-inbox-ui-spec.test.js` >= 4.
    - Test file references the 4 handoff_kind literals: `grep -c "approval\|recovery\|follow_up\|manual_input" test/operator-cockpit/phase-208/domain-4/approval-inbox-ui-spec.test.js` >= 4.
  </acceptance_criteria>
  <verify>
    <automated>node -e "require('node:fs').readFileSync('test/operator-cockpit/phase-208/domain-4/approval-inbox-ui-spec.test.js')"</automated>
  </verify>
  <done>19-AC verification harness for Approval Inbox is committed before implementation. The 206 mutation-class doctrine + 207 ApprovalHandoffRecord inheritance is asserted verbatim.</done>
</task>

<task id="208-04-01">
  <name>Approval Inbox contract + TS data assembler with 206 mutation-class doctrine (A-3..A-6)</name>
  <files>.planning/operator-cockpit/approval-inbox-contract.md, lib/markos/operator/approvals.ts, lib/markos/operator/index.cjs, test/operator-cockpit/phase-208/domain-4/approval-item.test.js, test/operator-cockpit/phase-208/domain-4/blocker-states.test.js</files>
  <read_first>
    - .planning/phases/208-human-operating-interface/208-RESEARCH.md section "Domain 4 - Approval Inbox contract and blocker states"
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Surface D — Approval Inbox" data dependencies block (verbatim ApprovalItem field set + 9 mutation_family literals + 4 evidence_status literals + mutation_family→mutation_class root mapping)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Inheritance Bindings" §"From 206-UI-SPEC §Downstream UI Inheritance Map" (mutation-class-policy.md: 6 root literals + default_approval_mode 4 literals + autonomy_ceiling + evidence_required)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Inheritance Bindings" §"From 207-UI-SPEC §Downstream UI Inheritance Map" (ApprovalHandoffRecord verbatim field set: handoff_kind, approval_required, task_ref, reason, side_effect_kind, target_system, created_at)
    - app/(markos)/operations/tasks/evidence-panel.tsx (existing evidence drawer; informs preview posture without owning EvidenceMap)
    - app/(markos)/admin/governance/page.tsx (existing governance source for compliance_status posture)
  </read_first>
  <action>
    Step 1: Create `.planning/operator-cockpit/approval-inbox-contract.md` with the exact `ApprovalItem` fields per 208-UI-SPEC §Surface D data dependencies: `approval_item_id`, `tenant_id`, `mutation_family`, `preview_ref`, `brand_score`, `compliance_status`, `pricing_status`, `evidence_status`, `cost_estimate_ref`, `decision_options`, `rejection_reason_required`, `agent_run_id`, `task_id`, `created_at`, `updated_at`.

    Step 2: Document the mutation_family → root mutation_class mapping table verbatim in the contract markdown:

      | mutation_family | root mutation_class (206) |
      |-----------------|--------------------------|
      | `content_publish` | `public.claim` |
      | `social_reply` | `public.claim` |
      | `social_post` | `public.claim` |
      | `email_send` | `external.send` |
      | `crm_mutation` | `connector.mutate` |
      | `billing_correction` | `billing.charge` |
      | `support_reply` | `external.send` |
      | `price_change` | `price.change` |
      | `connector_action_placeholder` | `connector.mutate` |

    Step 3: Document the default_approval_mode behavior table verbatim per 206 doctrine + 208-UI-SPEC §Surface D state matrix:

      | default_approval_mode | UI behavior |
      |----------------------|-------------|
      | `auto` | Approve fires direct; Decline still requires modal + reason. |
      | `single_approval` | Approve fires direct (default); Decline modal as above. |
      | `dual_approval` | Approve opens "Second approval required" modal first; commits only after second token. |
      | `cooling_off` | Approve fires direct but action delayed by cooling-off window; row shows `.c-badge--info` "Cooling off". |

    Step 4: Document the autonomy_ceiling + evidence_required gating tables (208-UI-SPEC §Surface D state matrix rows `autonomy-ceiling-reached`, `evidence-waiting-phase-209`, `evidence-blocked`).

    Step 5: Create `lib/markos/operator/approvals.ts`:
      - Export `ApprovalItem` interface with the exact 15 fields from Step 1.
      - Export `mutation_family` literal union with the exact 9 literals per 208-UI-SPEC §A-4 + Surface D copy register: `'content_publish' | 'social_reply' | 'social_post' | 'email_send' | 'crm_mutation' | 'billing_correction' | 'support_reply' | 'price_change' | 'connector_action_placeholder'`.
      - Export `mutation_class_root` literal union with the exact 6 literals per 208-UI-SPEC §A-6 (verbatim from 206 mutation-class-policy.md): `'external.send' | 'billing.charge' | 'connector.mutate' | 'price.change' | 'public.claim' | 'data.export'`.
      - Export `evidence_status` literal union with the exact 4 literals per 208-UI-SPEC §A-5: `'not_required' | 'waiting_phase_209' | 'ready' | 'blocked'`.
      - Export `default_approval_mode` literal union with the exact 4 literals per 206 doctrine: `'auto' | 'single_approval' | 'dual_approval' | 'cooling_off'`.
      - Export `compliance_status` literal union: `'clear' | 'flagged' | 'blocked'` per 208-UI-SPEC §Surface D copy register.
      - Export `pricing_status` literal union: `'engine_backed' | 'placeholder' | 'diverged'` per 208-UI-SPEC §Surface D copy register.
      - Export `handoff_kind` literal union with the exact 4 literals per 208-UI-SPEC §A-7 (verbatim from 207): `'approval' | 'recovery' | 'follow_up' | 'manual_input'`.
      - Export `mapMutationFamilyToRoot(family: MutationFamily): MutationClassRoot` function implementing the Step 2 table verbatim.
      - Export `assembleApprovalItems(input: ApprovalAssemblerInput): ApprovalItem[]` function that consumes 207 ApprovalHandoffRecord + 206 mutation-class-policy lookups and produces the array. The assembler reads `default_approval_mode`, `autonomy_ceiling`, and `evidence_required` from policy at assembly time so the UI can render gating notices without re-querying.

    Step 6: Mirror the contract in `lib/markos/operator/index.cjs` (CommonJS twin).

    Step 7: Create `test/operator-cockpit/phase-208/domain-4/approval-item.test.js` (existing AC retained):
      - Assert `ApprovalItem` field names present (A-3 verbatim register).
      - Assert all 9 `mutation_family` literals (subset per existing AC: `content_publish`, `crm_mutation`, `price_change`, `connector_action_placeholder`).
      - Assert all 4 `evidence_status` literals (A-5).
      - Assert mapMutationFamilyToRoot covers all 9 families.

    Step 8: Create `test/operator-cockpit/phase-208/domain-4/blocker-states.test.js` (existing AC retained):
      - Assert `waiting_phase_209` literal verbatim (existing AC).
      - Assert autonomy-ceiling state literal coverage.
      - Assert dual-approval state literal coverage.
      - Assert all 6 root mutation_class literals (A-6).
  </action>
  <acceptance_criteria>
    - `.planning/operator-cockpit/approval-inbox-contract.md` contains the exact field names `compliance_status`, `pricing_status`, `evidence_status`, `cost_estimate_ref`, `rejection_reason_required` (208-UI-SPEC §A-3).
    - `.planning/operator-cockpit/approval-inbox-contract.md` contains the verbatim mutation_family → mutation_class mapping table (208-UI-SPEC §A-6).
    - **A-4**: `grep -c "content_publish\|crm_mutation\|price_change\|connector_action_placeholder" lib/markos/operator/approvals.ts` >= 4.
    - **A-5**: `grep -c "not_required\|waiting_phase_209\|ready\|blocked" lib/markos/operator/approvals.ts` >= 4.
    - **A-6**: `grep -c "external\.send\|billing\.charge\|connector\.mutate\|price\.change\|public\.claim\|data\.export" lib/markos/operator/approvals.ts` >= 6.
    - **handoff_kind** (A-7 input): `grep -c "approval\|recovery\|follow_up\|manual_input" lib/markos/operator/approvals.ts` >= 4.
    - **default_approval_mode**: `grep -c "auto\|single_approval\|dual_approval\|cooling_off" lib/markos/operator/approvals.ts` >= 4.
    - `test/operator-cockpit/phase-208/domain-4/blocker-states.test.js` contains the exact string `waiting_phase_209`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/operator-cockpit/phase-208/domain-4/approval-item.test.js test/operator-cockpit/phase-208/domain-4/blocker-states.test.js</automated>
  </verify>
  <done>Approval Inbox data contract is locked: 15-field ApprovalItem, 9 mutation_family literals, 6 root mutation_class literals, 4 evidence_status literals, 4 default_approval_mode literals, 4 handoff_kind literals, deterministic mapMutationFamilyToRoot function. The contract honors 206 mutation-class doctrine + 207 ApprovalHandoffRecord verbatim and is consumed by 208-04-02 (UI render).</done>
</task>

<task id="208-04-02">
  <name>Approval Inbox UI render — fold §Surface D layout grid + A-1..A-2, A-7..A-19</name>
  <files>app/(markos)/operations/approvals/page.tsx, app/(markos)/operations/approvals/page.module.css, app/(markos)/operations/approvals/page.stories.tsx, components/markos/operator/Money.tsx, .planning/phases/208-human-operating-interface/208-VALIDATION.md</files>
  <read_first>
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Surface D — Approval Inbox (Plan 208-04)" full body — verbatim layout grid (desktop split + mobile sequential), copy register table (every cell verbatim including all modal copy), state matrix (10 states), accessibility focus order, motion notes, mobile breakpoint behavior
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"A-* — Approval Inbox (Plan 208-04)" AC table (A-1..A-19)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Cross-surface Tokens Reference Table"
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Storybook Coverage" + §"Destructive actions" (Decline approval modal)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Money display posture" (Money recipe + .c-code-inline + 207 RunCostContext)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Inheritance Bindings" §"From 207" (RunCostContext.estimated_cost_usd_micro for A-12)
    - lib/markos/operator/approvals.ts (output of 208-04-01 — the contract this UI consumes)
    - components/markos/operator/PlaceholderBanner.tsx (extracted in 208-02-02; reused here for future_phase_209)
    - styles/components.css (.c-card, .c-notice, .c-badge, .c-button, .c-chip, .c-chip-protocol, .c-modal, .c-backdrop, .c-input, .c-field__error, .c-status-dot primitives)
    - DESIGN.md §"Iconography" (bracketed-glyph register)
  </read_first>
  <action>
    Step 1 — `components/markos/operator/Money.tsx`: Money recipe per 208-UI-SPEC §"Money display posture":
      - Props: `{ estimatedMicros?: number, actualMicros?: number, pricingContext?: { schema_version?: string, ... } }`.
      - When `pricingContext.schema_version` is unset → render `{{MARKOS_PRICING_ENGINE_PENDING}}` sentinel verbatim wrapped in `<span class="c-code-inline">`.
      - When schema_version is set → render `<span class="c-code-inline">${USD}</span>` derived from integer micros / 1_000_000 with JetBrains Mono via `--fs-code` token.
      - **Zero hard-coded dollar literals; zero tier names.** Component is shared across 208-04 + 208-02 (pricing_risks brief item) + 208-05 (cost-divergence rows).

    Step 2 — `app/(markos)/operations/approvals/page.module.css`: token-only stylesheet per 208-UI-SPEC §Surface D layout grid:
      - Page wrapper consumes `var(--color-surface)` background, `var(--color-on-surface)` text, `var(--space-xl)` top padding, `var(--space-lg)` horizontal padding (`var(--space-md)` at `max-width: 640px`).
      - Container `max-width: var(--w-container)`; `margin-inline: auto`.
      - Desktop layout: 60/40 split between list and detail rail (or list + detail-card-stack below at md viewport). Detail rail uses `position: sticky; top: var(--space-lg)`.
      - Mobile layout (`max-width: 640px`): list and detail are sequential pages; tap row navigates to full-screen detail; back-button to list. Bottom action bar: `position: sticky; bottom: 0; padding: var(--space-md); display: flex; flex-direction: column; gap: var(--space-xs)` for stacked full-width buttons (Approve / Decline / Snooze).
      - `@media (prefers-reduced-motion: reduce)` block: collapses modal fade transitions to 0; success-notice persistence freezes at full opacity (A-16).
      - **Zero hex literals** (A-1). All color via `var(--color-*)`. All spacing via `var(--space-*)`.
      - **No local `.banner`/`.alert`/`.warning`/`.callout`/`.noticeBar`** classes (X-3).

    Step 3 — `app/(markos)/operations/approvals/page.tsx`: render the Approval Inbox per 208-UI-SPEC §Surface D layout grid. Required structure:

      Page header:
      - `<h1>Approvals</h1>` (A-2)
      - `<p class="t-lead">{N} pending. {M} due within 24 hours.</p>` (UI-SPEC §Surface D copy register)
      - Filter chip group (A-7 verbatim from 207 + 206):
        - `<button class="c-chip c-chip--mint">All</button>` (default selected)
        - Mutation class chips (6 root literals via `.c-chip-protocol`): `external.send`, `billing.charge`, `connector.mutate`, `price.change`, `public.claim`, `data.export`.
        - Handoff kind chips (4 literals): `approval`, `recovery`, `follow_up`, `manual_input`. Default filter is `approval` (verbatim from UI-SPEC §A-7).

      Empty state: `<div class="c-notice c-notice--success">[ok] No approvals waiting.</div>`.

      List (vanilla `<table>` per D-14 / X-5):
      - `<th>` Item | Mutation | Approval mode | Status | Time
      - `<tr>` per row: title, mutation_family chip via `.c-chip-protocol`, approval_mode badge via `.c-badge--info`, status badge per state (proposed/pending/approved/rejected), timestamp via `.c-field__help`.

      Detail card (right rail desktop, below list mobile):
      - `<article class="c-card">` (NOT `.c-card--feature` per A-14)
      - `<h2>{ApprovalItem.title}</h2>`
      - Mutation class row: "Mutation class" eyebrow + `<span class="c-chip-protocol">{mutation_family}</span>` + below it `<span class="c-chip-protocol">Root: {mutation_class_root}</span>` (A-6 mapping rendered verbatim via mapMutationFamilyToRoot)
      - Pricing-status badge (A-12): engine → `.c-badge--success` `[ok] Engine-backed`; placeholder → `.c-badge--info` `[info] Placeholder`; diverged → `.c-badge--warning` `[warn] Diverged`.
      - Compliance-status badge: clear → `.c-badge--success` `[ok] Clear`; flagged → `.c-badge--warning` `[warn] Flagged`; blocked → `.c-badge--error` `[block] Blocked` (with `[err]` fallback).
      - Evidence-status badge: not_required → `.c-badge--success` `[ok] Not required`; ready → `.c-badge--success` `[ok] Ready`; waiting_phase_209 → `.c-badge--info` `[info] Awaiting Phase 209`; blocked → `.c-badge--error` `[err] Blocked`.
      - Cost estimate row: `Estimated: <Money estimatedMicros={cost} pricingContext={ctx} />` (A-12). When pricing_engine_context.schema_version unset, Money renders `{{MARKOS_PRICING_ENGINE_PENDING}}`.
      - Approval-mode badge: `.c-badge--info` showing "Auto" / "Single approval" / "Dual approval" / "Cooling off".
      - **Autonomy-ceiling notice (A-10)**: when ceiling reached for the row's mutation_class, render `<div class="c-notice c-notice--error">[block] Autonomy ceiling reached for {mutation_family}. Operator approval is required for every action of this class until ceiling resets.</div>` ABOVE the action buttons. Approve button disabled (aria-disabled="true").
      - **Evidence placeholder (A-11)**: when `evidence_status == 'waiting_phase_209'`, render `<PlaceholderBanner variant="future_phase_209" />` in evidence rail. Approve REMAINS ENABLED (translation gate, not block — verbatim per UI-SPEC §A-11). When `evidence_status == 'blocked'`, render `<div class="c-notice c-notice--error">[err] Evidence is blocked.</div>` and Approve is disabled.
      - Action set:
        - Approve CTA: `<button class="c-button c-button--primary">Approve</button>`. **Dual-approval gate (A-9)**: when `default_approval_mode == 'dual_approval'`, Approve CTA opens `.c-modal` first with heading "Second approval required" and verbatim body copy from UI-SPEC §Surface D copy register. CTA: "Notify second approver" + "Cancel".
        - Decline CTA: `<button class="c-button c-button--destructive">Decline</button>` always opens `.c-modal` + `.c-backdrop`. Modal heading "Decline this approval?" + body copy verbatim. Required `<textarea class="c-input">` rejection_reason field. Empty submit shows `<span class="c-field__error">[err] A reason is required to decline.</span>`. Modal CTAs: `.c-button--destructive` "Decline" + `.c-button--secondary` "Cancel".
        - Snooze CTA: `<button class="c-button c-button--secondary">Snooze 1 hour</button>`.

      After-action notices (A-15):
      - Approve success: `<div class="c-notice c-notice--success">[ok] Approved. The run will continue.</div>` (3s persist, then fade — frozen at full opacity under prefers-reduced-motion).
      - Decline success: `<div class="c-notice c-notice--success">[ok] Declined. The requester has been notified.</div>`.
      - Error: `<div class="c-notice c-notice--error">[err] Action failed. {error_code}: {error_message}</div>` (verbatim from 207 envelope error structure).

      Mint-as-text (A-13 + UI-SPEC mint rule): `.c-chip-protocol` for `mutation_family`, `run_id`, `task_id`, `approval_item_id`. `.c-button--tertiary` mint-text inline links for "Open run →".

      Bracketed-glyph state coding (A-13): every state badge paired with `[ok]`, `[warn]`, `[err]`, `[info]`, or `[block]`. State color never the only signal.

      **D-13 + D-14 carry (A-14)**: zero `.c-table` references; zero `.c-card--feature` references.

    Step 4 — `app/(markos)/operations/approvals/page.stories.tsx`: CSF3 with 6 named state stories per 208-UI-SPEC §A-19:
      - `Default`: 5 approvals, mixed mutation_family + evidence_status, all default_approval_mode='single_approval'.
      - `Empty`: 0 approvals; renders `[ok] No approvals waiting` notice.
      - `DualApprovalRequired`: 1 approval with mutation_family='price_change' (root: price.change) and default_approval_mode='dual_approval'; clicking Approve opens "Second approval required" modal.
      - `AutonomyCeilingReached`: 1 approval where ceiling for mutation_family='content_publish' is exhausted; renders `.c-notice c-notice--error` with `[block] Autonomy ceiling reached`; Approve disabled.
      - `EvidenceWaitingPhase209`: 1 approval with `evidence_status='waiting_phase_209'`; renders `<PlaceholderBanner variant="future_phase_209" />`; Approve enabled (translation gate, not block).
      - `EvidenceBlocked`: 1 approval with `evidence_status='blocked'`; renders `[err] Evidence is blocked` notice; Approve disabled.
      - Story fixtures honor 207 ApprovalHandoffRecord field names; 206 mutation-class doctrine; zero hard-coded dollars; bracketed glyphs only.

    Step 5 — Update `.planning/phases/208-human-operating-interface/208-VALIDATION.md`:
      - Add row `208-04-02` to per-task verification map.
      - Update `## UI-SPEC AC Coverage Map`: mark A-1..A-19 with verification commands.

    Step 6 — Banned-lexicon compliance (A-18, X-9): zero matches across page.tsx + page.module.css. No exclamation points.

    Step 7 — Mobile-priority compliance (A-17, X-8): page MUST be `mobile_priority: 'critical'` per shell contract. At `max-width: 640px`: list + detail are sequential pages; bottom action bar with stacked full-width buttons. `--h-control-touch` (44px) inherits via global `(pointer: coarse)` rule.
  </action>
  <acceptance_criteria>
    - **A-1 (token-only)**: `! grep -nE "#[0-9a-fA-F]{3,8}" app/(markos)/operations/approvals/page.module.css` returns 0.
    - **A-2 (h1)**: `grep -c "Approvals" app/(markos)/operations/approvals/page.tsx` >= 1.
    - **A-7 (handoff_kind chips)**: `grep -c "handoff_kind\|approval\b\|recovery\|follow_up\|manual_input" app/(markos)/operations/approvals/page.tsx` >= 4.
    - **A-8 (Approve+Decline+modal+input)**: `grep -c "c-button--primary\|c-button--destructive\|c-modal\|c-input" app/(markos)/operations/approvals/page.tsx` >= 4.
    - **A-9 (dual-approval modal)**: `grep -c "dual_approval\|Second approval required" app/(markos)/operations/approvals/page.tsx` >= 1.
    - **A-10 (autonomy-ceiling notice)**: `grep -c "autonomy_ceiling\|Autonomy ceiling reached" app/(markos)/operations/approvals/page.tsx` >= 1.
    - **A-11 (evidence Phase 209 placeholder)**: `grep -c "future_phase_209\|waiting_phase_209" app/(markos)/operations/approvals/page.tsx` >= 1.
    - **A-12 (Money + RunCostContext + pricing placeholder)**: `grep -c "Money\|estimated_cost_usd_micro\|MARKOS_PRICING_ENGINE_PENDING" app/(markos)/operations/approvals/page.tsx` >= 2.
    - **A-13 (5 bracketed glyphs)**: `grep -c "\[ok\]\|\[warn\]\|\[err\]\|\[info\]\|\[block\]" app/(markos)/operations/approvals/page.tsx` >= 6.
    - **A-14 (D-13+D-14 carry)**: `grep -c "c-table\|c-card--feature" app/(markos)/operations/approvals/page.tsx` returns 0.
    - **A-15 (after-action notices)**: `grep -c "c-notice--success\|c-notice--error" app/(markos)/operations/approvals/page.tsx` >= 2.
    - **A-16 (prefers-reduced-motion)**: `grep -c "prefers-reduced-motion" app/(markos)/operations/approvals/page.module.css` >= 1.
    - **A-18 (banned-lexicon)**: `! grep -nE "synergy\|leverage\|empower\|unlock\|transform" app/(markos)/operations/approvals/page.tsx` returns 0.
    - **A-19 (Storybook ≥6 named state stories)**: `grep -c "Default\|Empty\|DualApprovalRequired\|AutonomyCeilingReached\|EvidenceWaitingPhase209\|EvidenceBlocked" app/(markos)/operations/approvals/page.stories.tsx` >= 6.
    - **Money component reusable**: `components/markos/operator/Money.tsx` exists with `MARKOS_PRICING_ENGINE_PENDING` sentinel handling.
    - **PlaceholderBanner reused**: `app/(markos)/operations/approvals/page.tsx` imports `<PlaceholderBanner variant="future_phase_209">` from `components/markos/operator/PlaceholderBanner.tsx` (D-15 reuse threshold).
    - **208-VALIDATION.md updated**: row `208-04-02` present; UI-SPEC AC Coverage Map A-1..A-19 rows have verification commands.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/operator-cockpit/phase-208/domain-4/approval-inbox-ui-spec.test.js</automated>
  </verify>
  <done>Approval Inbox renders list + detail per 208-UI-SPEC §Surface D layout grid. All 19 ACs A-1..A-19 are automated. The 206 mutation-class doctrine is visibly enforced (mutation_family chip + Root: chip + autonomy-ceiling notice + dual-approval modal). The 207 ApprovalHandoffRecord field set drives every row. Money recipe consumes RunCostContext verbatim with placeholder fallback. Storybook covers 6 named states. Page is `mobile_priority: 'critical'` with stacked full-width action bar at `max-width: 640px`.</done>
</task>

</tasks>

<verification>
- npm test -- test/operator-cockpit/phase-208/domain-4/
- npm test -- test/operator-cockpit/phase-208/domain-4/approval-inbox-ui-spec.test.js
- npm run build-storybook (verifies app/(markos)/operations/approvals/page.stories.tsx registers cleanly)
</verification>

<success_criteria>
- Approval items expose preview, edit, approve, reject, and rejection reason
- Pricing/compliance posture is first-class
- Evidence and connector states remain explicit placeholders until later phases translate them
- 208-UI-SPEC §Surface D ACs A-1..A-19 are realized verbatim and verified by Node `--test`
- 206 mutation-class doctrine (6 root literals + default_approval_mode + autonomy_ceiling + evidence_required) is rendered explicitly in the UI
- 207 ApprovalHandoffRecord (handoff_kind, approval_required, side_effect_kind, target_system, task_ref, reason) drives every row
- Money component is shared across plans 02 + 04 + 05; PlaceholderBanner reused across 02 + 04 + 05 + 06 (D-15 reuse threshold passed)
- Storybook covers 6 named state stories
</success_criteria>
