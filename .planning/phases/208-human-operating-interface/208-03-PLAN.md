---
phase: 208
plan: "03"
type: execute
wave: 3
depends_on:
  - "208-01"
f_ids: []
migration_slots: []
integrates_with:
  - "PRC-01..09, BILL-02 from P205 (pricing-related work items should reference approved pricing posture or placeholder state)"
  - "RUN-01..08 from P207 (task records must link to real AgentRun and chain lineage)"
translation_gate_for:
  - "P209"
  - "P210"
  - "P212"
files_modified:
  - .planning/phases/208-human-operating-interface/208-VALIDATION.md
  - .planning/operator-cockpit/task-board-contract.md
  - app/(markos)/operations/tasks/page.tsx
  - app/(markos)/operations/tasks/task-types.ts
  - app/(markos)/operations/tasks/task-machine.ts
  - app/(markos)/operations/tasks/task-ui.module.css
  - app/(markos)/operations/tasks/tasks.stories.tsx
  - lib/markos/operator/task-board.ts
  - lib/markos/operator/index.cjs
  - test/operator-cockpit/phase-208/domain-3/task-record.test.js
  - test/operator-cockpit/phase-208/domain-3/grouping-state.test.js
  - test/operator-cockpit/phase-208/domain-3/task-board-ui-spec.test.js
autonomous: true
requirements:
  - TASK-01
  - TASK-03
  - QA-07
  - QA-08
must_haves:
  truths:
    - "Unified task records capture source, owner, priority, status, blockers, and run lineage."
    - "Grouping by priority, source, owner, status, and due date is contractually explicit."
    - "Evidence, connector, and learning inputs stay represented as placeholder states until later phases translate them."
    - "207 RunApiEnvelope 15-state machine collapses verbatim to 5 cockpit task statuses (queued, in_progress, blocked, awaiting_approval, done) per 208-UI-SPEC §T-6 collapse table."
    - "207 AgentRunPriority P0..P4 collapses verbatim to 4 cockpit priorities (urgent, high, normal, low) per 208-UI-SPEC §T-7 collapse table."
    - "207 AgentRunTriggerKind 7 literals (cli, ui, mcp, webhook, cron, chain, system) render verbatim as filter chips per 208-UI-SPEC §T-8."
    - "208-UI-SPEC §Surface C ACs T-1..T-17 are realized verbatim (token-only, h1, TaskBoardItem TS contract, priority + status literal unions, 15→5 + P0..P4→4 collapse maps, 7 trigger filter chips, .c-badge + .c-status-dot--live, vanilla <table> with no .c-table, .c-button--destructive Cancel run modal, single .c-button--primary Pick up next, bracketed-glyph state coding, mint-as-text via .c-chip-protocol + .c-button--tertiary, prefers-reduced-motion preserved from 213.4-05, banned-lexicon zero match, ≥4 new Storybook stories)."
  artifacts:
    - path: .planning/operator-cockpit/task-board-contract.md
      provides: canonical Task Board item shape, grouping vocabulary, blocked-state posture, and run-state collapse map
    - path: lib/markos/operator/task-board.ts
      provides: TaskBoardItem TS contract + 4 priority literals + 5 status literals + 15→5 collapseRunState + P0..P4→4 collapsePriority functions (208-UI-SPEC §T-3..T-7)
    - path: app/(markos)/operations/tasks/page.tsx
      provides: Task Board page composing 4 priority groups with vanilla <table> + .c-badge + .c-button--primary "Pick up next" + .c-button--destructive Cancel run modal per 208-UI-SPEC §Surface C
  key_links:
    - from: app/(markos)/operations/tasks/task-types.ts
      to: .planning/operator-cockpit/task-board-contract.md
      via: current task fixtures should evolve into the persisted Task Board contract
      pattern: "task"
    - from: app/(markos)/operations/tasks/task-machine.ts
      to: lib/markos/operator/task-board.ts
      via: 207 RunApiEnvelope.state literal union maps to cockpit task status via collapseRunState (208-UI-SPEC §T-6)
      pattern: "state"
    - from: app/(markos)/operations/tasks/page.tsx
      to: lib/markos/operator/task-board.ts
      via: page consumes TaskBoardItem array + collapse functions; renders 4 priority groups per UI-SPEC §Surface C layout grid
      pattern: "TaskBoardItem"
ui_spec_acs_folded:
  - "T-1..T-17 (Task Board — 17 ACs)"
  - "X-1..X-5 (cross-cutting verified by 208-01 tripwire applied to Task Board surface files)"
---

<objective>
Define the persisted Task Board contract so every agent output, escalation, approval, follow-up, and blocked-work item becomes one operator queue without assuming later evidence, connector, or learning substrate already exists. Fold 208-UI-SPEC.md §Surface C (Task Board — T-1..T-17) verbatim into per-AC tasks; the 207 RunApiEnvelope 15→5 status collapse and P0..P4→4 priority collapse are load-bearing and explicit.
</objective>

<context>
@.planning/phases/208-human-operating-interface/208-CONTEXT.md
@.planning/phases/208-human-operating-interface/208-RESEARCH.md
@.planning/phases/208-human-operating-interface/208-VALIDATION.md
@.planning/phases/208-human-operating-interface/208-UI-SPEC.md
@.planning/phases/207-agentrun-v2-orchestration-substrate/207-UI-SPEC.md
@DESIGN.md
@CLAUDE.md
@app/(markos)/operations/tasks/page.tsx
@app/(markos)/operations/tasks/task-types.ts
@app/(markos)/operations/tasks/task-machine.ts
@app/(markos)/operations/tasks/task-ui.module.css
@app/(markos)/operations/tasks/tasks.stories.tsx
@lib/markos/operator/shell.ts
</context>

<tasks>

<task id="208-03-00">
  <name>Wave 0 — UI-SPEC §Surface C fold preflight: read Task Board AC table</name>
  <files>test/operator-cockpit/phase-208/domain-3/task-board-ui-spec.test.js</files>
  <read_first>
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Surface C — Task Board (Plan 208-03)" full body — verbatim layout grid (desktop + mobile), copy register, state matrix, accessibility focus order, motion notes
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"T-* — Task Board (Plan 208-03)" AC table (T-1..T-17)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Cross-surface Tokens Reference Table"
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Storybook Coverage" (tasks.stories.tsx — extends existing 5 with +4 new: BoardEmpty, BoardSinglePriority, BoardCrossPriority, BoardWithBlocker)
    - .planning/phases/207-agentrun-v2-orchestration-substrate/207-UI-SPEC.md §Downstream UI Inheritance Map (RunApiEnvelope 15-state machine + AgentRunPriority P0..P4 + AgentRunTriggerKind 7 literals)
    - DESIGN.md §"Iconography" + §"Color blindness"
    - app/(markos)/operations/tasks/task-ui.module.css (already 213.4-05 token-only canon; reference for prefers-reduced-motion T-15)
    - styles/components.css (.c-card, .c-badge, .c-button, .c-chip, .c-chip-protocol, .c-status-dot--live, .c-modal, .c-backdrop, .c-input, .c-field__error primitives)
  </read_first>
  <action>
    Step 1: Create `test/operator-cockpit/phase-208/domain-3/task-board-ui-spec.test.js` as the 17-AC verification harness for T-1..T-17. Each `test(...)` block names the AC and runs the verbatim grep / file-existence check from 208-UI-SPEC §T-* AC table.

    Step 2: 17 named test blocks:
    - `test('T-1 token-only zero hex in task-ui.module.css', ...)`: assert `! grep -nE "#[0-9a-fA-F]{3,8}" app/(markos)/operations/tasks/task-ui.module.css` returns 0 (T-1 — already 213.4-05 canon; this plan adds NO regressions).
    - `test('T-2 page heading "Task board"', ...)`: assert `grep -c "Task board" app/(markos)/operations/tasks/page.tsx` >= 1.
    - `test('T-3 TaskBoardItem TS contract', ...)`: assert `grep -c "approval_required\|evidence_status\|agent_run_id\|chain_id\|pricing_context_ref\|blocked_by" lib/markos/operator/task-board.ts` >= 6.
    - `test('T-4 priority literals', ...)`: assert `grep -c "urgent\|high\|normal\|low" lib/markos/operator/task-board.ts` >= 4.
    - `test('T-5 status literals', ...)`: assert `grep -c "queued\|in_progress\|blocked\|awaiting_approval\|done" lib/markos/operator/task-board.ts` >= 5.
    - `test('T-6 run-state collapse 15→5', ...)`: assert all 13 representative 207 RunApiEnvelope state literals appear in `lib/markos/operator/task-board.ts`: requested, context_loaded, executing, awaiting_approval, approved, rejected, paused, retry_wait, failed, canceled, dlq, completed, archived. `grep -c` >= 13.
    - `test('T-7 priority collapse P0..P4 → 4', ...)`: assert P0, P1, P2, P3, P4 literals present in collapse function. `grep -c "P0\|P1\|P2\|P3\|P4" lib/markos/operator/task-board.ts` >= 5.
    - `test('T-8 7 trigger filter chips', ...)`: assert `grep -c "cli\|ui\|mcp\|webhook\|cron\|chain\|system" app/(markos)/operations/tasks/page.tsx` >= 7.
    - `test('T-9 .c-badge + .c-status-dot--live', ...)`: assert `grep -c "c-badge\|c-status-dot--live" app/(markos)/operations/tasks/page.tsx` >= 5.
    - `test('T-10 vanilla <table> + no .c-table + ≥4 table elements', ...)`: assert `grep -c "c-table" app/(markos)/operations/tasks/page.tsx` returns 0 AND `grep -c "<table\\|<tr\\|<th\\|<td" app/(markos)/operations/tasks/page.tsx` >= 4.
    - `test('T-11 Cancel run .c-button--destructive + .c-modal', ...)`: assert `grep -c "c-button--destructive\|c-modal" app/(markos)/operations/tasks/page.tsx` >= 2.
    - `test('T-12 single .c-button--primary "Pick up next"', ...)`: assert `grep -c "c-button--primary" app/(markos)/operations/tasks/page.tsx` <= 1 (state-conditional; final wave gate).
    - `test('T-13 bracketed-glyph state coding', ...)`: assert `grep -c "\\[ok\\]\\|\\[warn\\]\\|\\[err\\]\\|\\[info\\]" app/(markos)/operations/tasks/page.tsx` >= 5.
    - `test('T-14 mint-as-text via .c-chip-protocol + .c-button--tertiary', ...)`: assert `grep -c "c-chip-protocol\|c-button--tertiary" app/(markos)/operations/tasks/page.tsx` >= 2.
    - `test('T-15 prefers-reduced-motion preserved', ...)`: assert `grep -c "prefers-reduced-motion" app/(markos)/operations/tasks/task-ui.module.css` >= 1 (already 213.4-05 canon — regression check).
    - `test('T-16 banned-lexicon zero match', ...)`: assert `! grep -nE "synergy|leverage|empower|unlock|transform" app/(markos)/operations/tasks/page.tsx` returns 0.
    - `test('T-17 Storybook ≥4 new state stories', ...)`: assert `grep -c "BoardEmpty\|BoardSinglePriority\|BoardCrossPriority\|BoardWithBlocker" app/(markos)/operations/tasks/tasks.stories.tsx` >= 4.

    Step 3: Each test block uses Node `--test` `assert.ok` with exec-grep helper. Tests skip-soft if target files don't yet exist (Wave 0 may run before 03-01 + 03-02 implementations are complete).
  </action>
  <acceptance_criteria>
    - `test/operator-cockpit/phase-208/domain-3/task-board-ui-spec.test.js` exists with exactly 17 named test blocks T-1 through T-17.
    - Test file references all 13 RunApiEnvelope state literals: `grep -c "requested\|context_loaded\|executing\|awaiting_approval\|approved\|rejected\|paused\|retry_wait\|failed\|canceled\|dlq\|completed\|archived" test/operator-cockpit/phase-208/domain-3/task-board-ui-spec.test.js` >= 13.
    - Test file references all 7 AgentRunTriggerKind literals: `grep -c "cli\|ui\|mcp\|webhook\|cron\|chain\|system" test/operator-cockpit/phase-208/domain-3/task-board-ui-spec.test.js` >= 7.
  </acceptance_criteria>
  <verify>
    <automated>node -e "require('node:fs').readFileSync('test/operator-cockpit/phase-208/domain-3/task-board-ui-spec.test.js')"</automated>
  </verify>
  <done>17-AC verification harness for Task Board is committed before implementation. The 207 collapse maps and trigger filter chips have explicit verbatim assertions.</done>
</task>

<task id="208-03-01">
  <name>Task Board record, grouping, blocked-state contract + 207 collapse maps (T-3..T-7)</name>
  <files>.planning/operator-cockpit/task-board-contract.md, app/(markos)/operations/tasks/task-types.ts, app/(markos)/operations/tasks/task-machine.ts, lib/markos/operator/task-board.ts, lib/markos/operator/index.cjs, test/operator-cockpit/phase-208/domain-3/task-record.test.js, test/operator-cockpit/phase-208/domain-3/grouping-state.test.js</files>
  <read_first>
    - .planning/phases/208-human-operating-interface/208-RESEARCH.md section "Domain 3 - Persisted Task Board contract"
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Surface C — Task Board" data dependencies block (verbatim TaskBoardItem field set + priority literals + status literals + run-state collapse map + run-priority collapse map)
    - .planning/phases/207-agentrun-v2-orchestration-substrate/207-UI-SPEC.md §"Downstream UI Inheritance Map" (RunApiEnvelope.state 15 literals + AgentRunPriority P0..P4 + AgentRunTriggerKind 7 literals)
    - app/(markos)/operations/tasks/task-types.ts (existing fixture-driven types)
    - app/(markos)/operations/tasks/task-machine.ts (existing state-machine)
  </read_first>
  <action>
    Step 1: Create `.planning/operator-cockpit/task-board-contract.md` with the exact `TaskBoardItem` fields per 208-UI-SPEC §Surface C data dependencies: `task_id`, `tenant_id`, `source_type`, `source_id`, `priority`, `owner_role`, `owner_id`, `status`, `due_at`, `approval_required`, `evidence_status`, `agent_run_id`, `chain_id`, `pricing_context_ref`, `blocked_by`, `created_at`, `updated_at`.

    Step 2: Document the 15→5 run-state collapse map in the contract markdown verbatim from 208-UI-SPEC §"Run-state collapse map (15 → 5)":
      - requested → queued
      - queued → queued
      - accepted → queued
      - context_loaded → in_progress
      - executing → in_progress
      - awaiting_approval → awaiting_approval
      - approved → in_progress
      - rejected → done (rejected is terminal per 207)
      - paused → blocked
      - retry_wait → blocked
      - failed → blocked (also creates a recovery item per Surface E)
      - canceled → done
      - dlq → blocked
      - completed → done
      - archived → done

    Step 3: Document the P0..P4→4 priority collapse map verbatim:
      - P0 → urgent
      - P1 → high
      - P2 → normal
      - P3 → normal
      - P4 → low

    Step 4: Create `lib/markos/operator/task-board.ts`:
      - Export `TaskBoardItem` interface with the exact 17 fields from Step 1.
      - Export `priority` literal union with the exact 4 literals per 208-UI-SPEC §T-4: `'urgent' | 'high' | 'normal' | 'low'`.
      - Export `status` literal union with the exact 5 literals per 208-UI-SPEC §T-5: `'queued' | 'in_progress' | 'blocked' | 'awaiting_approval' | 'done'`.
      - Export `collapseRunState(state: RunApiEnvelopeState): TaskBoardStatus` function implementing the 15→5 map (T-6). Function MUST contain all 13 representative source literals (`requested`, `context_loaded`, `executing`, `awaiting_approval`, `approved`, `rejected`, `paused`, `retry_wait`, `failed`, `canceled`, `dlq`, `completed`, `archived`).
      - Export `collapsePriority(priority: AgentRunPriority): TaskBoardPriority` function implementing P0..P4→4 (T-7). Function MUST contain all 5 source literals (`P0`, `P1`, `P2`, `P3`, `P4`).
      - Export `TRIGGER_FILTER_CHIPS: readonly AgentRunTriggerKind[]` array with the verbatim 7 literals (T-8): `['cli', 'ui', 'mcp', 'webhook', 'cron', 'chain', 'system'] as const`.

    Step 5: Update `app/(markos)/operations/tasks/task-types.ts` to align with the 5 cockpit status literals + 4 cockpit priority literals from 208-UI-SPEC. Add re-exports of `TaskBoardItem`, `TaskBoardStatus`, `TaskBoardPriority` from `lib/markos/operator/task-board.ts`. Existing fixture types are preserved for backward compat but new code MUST consume the cockpit contract.

    Step 6: Update `app/(markos)/operations/tasks/task-machine.ts`: integrate `collapseRunState` from `lib/markos/operator/task-board.ts` so any state machine transition that reads a 207 RunApiEnvelope state collapses to the cockpit status before storing or rendering.

    Step 7: Mirror the contract in `lib/markos/operator/index.cjs` for Node `--test` consumption (CommonJS twin export).

    Step 8: Create `test/operator-cockpit/phase-208/domain-3/task-record.test.js` (existing AC retained):
      - Assert `TaskBoardItem` field names present in `lib/markos/operator/task-board.ts` (T-3 verbatim register).
      - Assert all 4 priority literals (T-4) and all 5 status literals (T-5).
      - Assert `awaiting_approval` literal verbatim (existing AC).

    Step 9: Create `test/operator-cockpit/phase-208/domain-3/grouping-state.test.js`:
      - Assert `collapseRunState` covers all 15 source literals (T-6).
      - Assert `collapsePriority` covers all 5 source literals (T-7).
      - Assert `TRIGGER_FILTER_CHIPS` contains the 7 literals (T-8).
      - Assert `awaiting_approval` literal verbatim (existing AC).
  </action>
  <acceptance_criteria>
    - `.planning/operator-cockpit/task-board-contract.md` contains the exact field names `approval_required`, `evidence_status`, `agent_run_id`, `chain_id`, `pricing_context_ref`, `blocked_by`.
    - `.planning/operator-cockpit/task-board-contract.md` contains the verbatim 15→5 run-state collapse map (`grep -c "requested\|context_loaded\|executing\|awaiting_approval\|approved\|rejected\|paused\|retry_wait\|failed\|canceled\|dlq\|completed\|archived" .planning/operator-cockpit/task-board-contract.md` >= 13).
    - **T-3**: `grep -c "approval_required\|evidence_status\|agent_run_id\|chain_id\|pricing_context_ref\|blocked_by" lib/markos/operator/task-board.ts` >= 6.
    - **T-4**: `grep -c "urgent\|high\|normal\|low" lib/markos/operator/task-board.ts` >= 4.
    - **T-5**: `grep -c "queued\|in_progress\|blocked\|awaiting_approval\|done" lib/markos/operator/task-board.ts` >= 5.
    - **T-6**: `grep -c "requested\|context_loaded\|executing\|awaiting_approval\|approved\|rejected\|paused\|retry_wait\|failed\|canceled\|dlq\|completed\|archived" lib/markos/operator/task-board.ts` >= 13.
    - **T-7**: `grep -c "P0\|P1\|P2\|P3\|P4" lib/markos/operator/task-board.ts` >= 5.
    - `test/operator-cockpit/phase-208/domain-3/grouping-state.test.js` contains the exact string `awaiting_approval`.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/operator-cockpit/phase-208/domain-3/task-record.test.js test/operator-cockpit/phase-208/domain-3/grouping-state.test.js</automated>
  </verify>
  <done>Task Board contract is locked: 17-field TaskBoardItem, 4 priority literals, 5 status literals, deterministic 15→5 + P0..P4→4 collapse functions, 7-literal trigger filter chip register. The contract consumes 207 RunApiEnvelope verbatim and is consumed by 208-03-02 (UI render).</done>
</task>

<task id="208-03-02">
  <name>Task Board UI render — fold §Surface C layout grid + T-1..T-2, T-8..T-17</name>
  <files>app/(markos)/operations/tasks/page.tsx, app/(markos)/operations/tasks/task-ui.module.css, app/(markos)/operations/tasks/tasks.stories.tsx, .planning/phases/208-human-operating-interface/208-VALIDATION.md</files>
  <read_first>
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Surface C — Task Board (Plan 208-03)" full body — verbatim layout grid (desktop + mobile with priority groups + filter chip group), copy register table (every cell verbatim including Cancel-run modal copy), state matrix, accessibility focus order, motion notes
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"T-* — Task Board (Plan 208-03)" AC table (T-1..T-17)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Cross-surface Tokens Reference Table"
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Storybook Coverage" (tasks.stories.tsx — extends existing 5 with +4 new state stories)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Destructive actions" table (Cancel run mutation-class binding)
    - .planning/phases/206-soc2-type1-foundation/206-UI-SPEC.md §Downstream UI Inheritance (mutation-class verbatim for Cancel run modal)
    - lib/markos/operator/task-board.ts (output of 208-03-01 — the contract this UI consumes)
    - app/(markos)/operations/tasks/page.tsx (existing — being migrated, not replaced)
    - app/(markos)/operations/tasks/task-ui.module.css (already 213.4-05 token-only canon — DO NOT regress)
    - app/(markos)/operations/tasks/tasks.stories.tsx (already exists per 213.4-05 — extend, do not replace)
    - styles/components.css (.c-card, .c-badge, .c-button, .c-chip, .c-chip-protocol, .c-status-dot--live, .c-modal, .c-backdrop, .c-input, .c-field__error primitives)
    - DESIGN.md §"Iconography" + §"Color blindness"
  </read_first>
  <action>
    Step 1 — `app/(markos)/operations/tasks/task-ui.module.css`: extend the existing 213.4-05 canon stylesheet. Add (without touching existing rules):
      - Filter chip group container (`.filterChipGroup`): `display: flex; gap: var(--space-xs); flex-wrap: wrap; padding-block: var(--space-sm)` per UI-SPEC §Surface C layout grid.
      - Priority group container (`.priorityGroup`): `display: flex; flex-direction: column; gap: var(--space-md); margin-block-end: var(--space-lg)`.
      - Mobile card collapse (`@media (max-width: 640px)`): table rows transform to vertical card stack per UI-SPEC §Surface C mobile layout grid.
      - **Zero new hex literals** (T-1; existing 213.4-05 canon already passes — regression check). Existing `prefers-reduced-motion` block preserved (T-15).
      - **No local `.banner`/`.alert`/`.warning`/`.callout`/`.noticeBar`** classes (X-3).

    Step 2 — `app/(markos)/operations/tasks/page.tsx`: migrate the fixture-driven UI to consume the cockpit contract from `lib/markos/operator/task-board.ts`. Required structure per 208-UI-SPEC §Surface C layout grid:

      - `<h1>Task board</h1>` (T-2; JetBrains Mono via globals)
      - `<p class="t-lead">Showing all open work. Filter by priority, source, owner, status, or trigger.</p>` (UI-SPEC §Surface C copy register descriptor)
      - Filter chip group: 5 filter classes (Priority, Source, Owner, Status, Trigger) using `.c-chip` primitive. Trigger filter chip group renders all 7 literals from `TRIGGER_FILTER_CHIPS`: `cli`, `ui`, `mcp`, `webhook`, `cron`, `chain`, `system` (T-8 verbatim).
      - Page-level primary CTA: `<button class="c-button c-button--primary">Pick up next</button>` rendered ONLY when `current_task_id == null`. At most ONE `.c-button--primary` per page (T-12).
      - 4 priority groups in order: `urgent`, `high`, `normal`, `low`. Each group:
        - `<h2>Urgent <span class="c-badge--info">{count}</span></h2>` (per UI-SPEC §Surface C copy register; count chip uses `.c-badge`)
        - Vanilla `<table>` with `<th>` headers: `Task | Status | Owner | Due | Source | Run` (T-10)
        - Each `<tr>`: title, status `.c-badge--{state}` per the run-state collapse:
          - queued → `.c-badge--info` `[info] Queued`
          - in_progress → `.c-badge--success` `[ok] In progress` + `.c-status-dot--live`
          - blocked → `.c-badge--error` `[block] Blocked` (with `[err]` fallback per 208-01-02 glyph canon resolution)
          - awaiting_approval → `.c-badge--warning` `[warn] Awaiting approval`
          - done → `.c-badge--success` `[ok] Done` (row dimmed via `--color-on-surface-subtle`)
        - Owner cell, Due cell (timestamp via `.c-field__help`), Source cell (`.c-chip-protocol` for `source_id`), Run cell (`.c-chip-protocol` for `run_id` + "Open run →" via `.c-button--tertiary` mint text — T-14).
        - Empty group: `<div class="c-notice c-notice--info">[info] No {priority} tasks open.</div>`.
      - Empty page state: single `<div class="c-notice c-notice--success">[ok] No open tasks. Inbox is current.</div>` (UI-SPEC §Surface C copy register).
      - Cancel run modal (T-11):
        - Trigger: `<button class="c-button c-button--destructive">Cancel run</button>` per row.
        - Modal: `<div class="c-modal">` + `<div class="c-backdrop">` overlay.
        - Heading: "Cancel this run?"
        - Body: "This stops the in-flight run and writes a `canceled` state to the audit log. Enter a reason for the record."
        - Reason input: `<textarea class="c-input" required>` with `.c-field__error` showing `[err] A reason is required to cancel.` when empty.
        - CTAs: `.c-button--destructive` "Cancel run" + `.c-button--secondary` "Keep running".
        - Mutation-class binding: per 206-UI-SPEC + 208-UI-SPEC §Destructive actions, Cancel run binds to `connector.mutate` (mid-flight on external system) or `external.send` (mid-send). The mutation_class chip displays in the modal body via `.c-chip-protocol` "Class: `{mutation_class}`".
      - Mobile collapse (UI-SPEC §Surface C mobile layout): at `max-width: 640px`, table rows become full-width `.c-card` stacks. Each card: title `<h3>`, status badge, eyebrow `.t-label-caps "Priority"`, metadata row (owner / due / source). Tap card expands in-place actions row (Open run, Reassign, Cancel run). Filter chips wrap to two rows.
      - **D-13 carry**: NO `.c-card--feature` references (X-4).
      - **D-14 carry**: NO `.c-table` references; vanilla `<table>` only (T-10 + X-5).

    Step 3 — `app/(markos)/operations/tasks/tasks.stories.tsx`: extend (do not replace) the existing 213.4-05 stories. Add 4 new named state stories per 208-UI-SPEC §T-17 + Storybook Coverage:
      - `BoardEmpty`: 0 tasks across all priorities; renders single `[ok] No open tasks` `.c-notice c-notice--success`.
      - `BoardSinglePriority`: 8 tasks all priority=urgent; renders only Urgent group with 8 rows; other groups show `[info] No {priority} tasks open` empty notices.
      - `BoardCrossPriority`: 2 urgent, 3 high, 4 normal, 1 low (10 total); all 4 groups populated.
      - `BoardWithBlocker`: 3 tasks status=blocked with `blocked_by` populated (1 awaiting_approval, 1 paused, 1 dlq from 207 collapse); rows render `.c-badge--error` `[block] Blocked` + `.c-status-dot--error`.
      - Story fixtures honor 207 contract field names (TaskBoardItem); zero hard-coded dollar literals; bracketed glyphs only.

    Step 4 — Update `.planning/phases/208-human-operating-interface/208-VALIDATION.md`:
      - Add row `208-03-02` to per-task verification map.
      - Update `## UI-SPEC AC Coverage Map`: mark T-1..T-17 with verification commands.

    Step 5 — Banned-lexicon compliance (T-16, X-9): scan page.tsx for the banned register. Zero matches. No exclamation points.

    Step 6 — Mobile-priority compliance (T-9 in-flight indicator + X-8): page MUST be `mobile_priority: 'critical'` per shell contract. `--h-control-touch` (44px) inherits via global `(pointer: coarse)` rule.
  </action>
  <acceptance_criteria>
    - **T-1 (token-only)**: `! grep -nE "#[0-9a-fA-F]{3,8}" app/(markos)/operations/tasks/task-ui.module.css` returns 0.
    - **T-2 (h1)**: `grep -c "Task board" app/(markos)/operations/tasks/page.tsx` >= 1.
    - **T-8 (7 trigger filter chips)**: `grep -c "cli\|ui\|mcp\|webhook\|cron\|chain\|system" app/(markos)/operations/tasks/page.tsx` >= 7.
    - **T-9 (.c-badge + .c-status-dot--live)**: `grep -c "c-badge\|c-status-dot--live" app/(markos)/operations/tasks/page.tsx` >= 5.
    - **T-10 (vanilla <table> + no .c-table)**: `grep -c "c-table" app/(markos)/operations/tasks/page.tsx` returns 0 AND `grep -c "<table\|<tr\|<th\|<td" app/(markos)/operations/tasks/page.tsx` >= 4.
    - **T-11 (Cancel run modal)**: `grep -c "c-button--destructive\|c-modal" app/(markos)/operations/tasks/page.tsx` >= 2.
    - **T-12 (single .c-button--primary)**: `grep -c "c-button--primary" app/(markos)/operations/tasks/page.tsx` <= 1.
    - **T-13 (bracketed-glyph state coding)**: `grep -c "\[ok\]\|\[warn\]\|\[err\]\|\[info\]" app/(markos)/operations/tasks/page.tsx` >= 5.
    - **T-14 (mint-as-text)**: `grep -c "c-chip-protocol\|c-button--tertiary" app/(markos)/operations/tasks/page.tsx` >= 2.
    - **T-15 (prefers-reduced-motion preserved)**: `grep -c "prefers-reduced-motion" app/(markos)/operations/tasks/task-ui.module.css` >= 1.
    - **T-16 (banned-lexicon)**: `! grep -nE "synergy\|leverage\|empower\|unlock\|transform" app/(markos)/operations/tasks/page.tsx` returns 0.
    - **T-17 (Storybook ≥4 new stories)**: `grep -c "BoardEmpty\|BoardSinglePriority\|BoardCrossPriority\|BoardWithBlocker" app/(markos)/operations/tasks/tasks.stories.tsx` >= 4.
    - **D-13 carry (X-4)**: `grep -c "c-card--feature" app/(markos)/operations/tasks/page.tsx` returns 0.
    - **208-VALIDATION.md updated**: row `208-03-02` present; UI-SPEC AC Coverage Map T-1..T-17 rows have verification commands.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/operator-cockpit/phase-208/domain-3/task-board-ui-spec.test.js</automated>
  </verify>
  <done>Task Board UI renders all 4 priority groups per 208-UI-SPEC §Surface C layout grid. All 17 ACs T-1..T-17 are automated. Cancel run modal binds to 206 mutation-class doctrine. Storybook extends existing 213.4-05 stories with 4 new named states. Page is `mobile_priority: 'critical'` and meets 44px touch-target on coarse pointers.</done>
</task>

</tasks>

<verification>
- npm test -- test/operator-cockpit/phase-208/domain-3/
- npm test -- test/operator-cockpit/phase-208/domain-3/task-board-ui-spec.test.js
- npm run build-storybook (verifies tasks.stories.tsx extension registers cleanly)
</verification>

<success_criteria>
- Task records capture source, priority, owner, blockers, and run lineage
- Grouping vocabulary is explicit (4 priority groups + 5 status literals)
- Placeholder states keep future evidence, connector, and learning data out of current ownership
- 208-UI-SPEC §Surface C ACs T-1..T-17 are realized verbatim and verified by Node `--test`
- 207 RunApiEnvelope 15→5 collapse + P0..P4→4 priority collapse + 7-literal trigger filter chips are deterministic functions exported from lib/markos/operator/task-board.ts
- Storybook covers 4 new named state stories beyond existing 213.4-05 baseline
- Page is `mobile_priority: 'critical'` and Cancel run modal binds to 206 mutation-class doctrine
</success_criteria>
