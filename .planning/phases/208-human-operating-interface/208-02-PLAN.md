---
phase: 208
plan: "02"
type: execute
wave: 2
depends_on:
  - "208-01"
f_ids: []
migration_slots: []
integrates_with:
  - "PRC-01..09, BILL-02 from P205 (pricing warnings and placeholder posture should appear in Morning Brief now)"
  - "COMP-01 from P206 (compliance alerts should be visible as operator work, not buried elsewhere)"
  - "RUN-01..08 from P207 (run failures and blocked execution should drive brief items)"
translation_gate_for:
  - "P209"
  - "P210"
  - "P211"
files_modified:
  - .planning/phases/208-human-operating-interface/208-VALIDATION.md
  - .planning/operator-cockpit/morning-brief-contract.md
  - app/(markos)/page.tsx
  - app/(markos)/page.module.css
  - app/(markos)/page.stories.tsx
  - lib/markos/operator/morning-brief.ts
  - lib/markos/operator/index.cjs
  - components/markos/operator/PlaceholderBanner.tsx
  - test/operator-cockpit/phase-208/domain-2/brief-sections.test.js
  - test/operator-cockpit/phase-208/domain-2/empty-states.test.js
  - test/operator-cockpit/phase-208/domain-2/morning-brief-ui-spec.test.js
autonomous: true
requirements:
  - TASK-02
  - QA-05
  - QA-06
must_haves:
  truths:
    - "Every Morning Brief item carries owner, reason, urgency, and next action."
    - "Pricing, compliance, and run-risk signals are visible without hunting through settings pages."
    - "Future evidence and connector sections use explicit placeholder states instead of fake current substrate."
    - "208-UI-SPEC §Surface B ACs B-1..B-16 are realized verbatim (token-only page.module.css, h1 + .t-lead descriptor, 8-section scaffold, PlaceholderBanner future_phase_209 + future_phase_210, MorningBriefItem TS contract, section + placeholder_state literals, .c-card default + no .c-card--feature, .c-badge state chips, MARKOS_PRICING_ENGINE_PENDING sentinel, mint-as-text via .c-button--tertiary + .c-chip-protocol, bracketed-glyph state coding, prefers-reduced-motion block, banned-lexicon zero-match, mobile single-column, ≥4 named state stories)."
    - "Morning Brief is the default operator landing surface — not a secondary dashboard tab."
  artifacts:
    - path: .planning/operator-cockpit/morning-brief-contract.md
      provides: canonical Morning Brief item shape, section vocabulary, and placeholder posture
    - path: lib/markos/operator/morning-brief.ts
      provides: MorningBriefItem TS contract + 8 section literals + 3 placeholder_state literals (208-UI-SPEC §B-5..B-7)
    - path: app/(markos)/page.tsx
      provides: 8-section Morning Brief landing surface composing .c-card + .c-notice + .c-button--tertiary + .c-chip-protocol per 208-UI-SPEC §Surface B
    - path: app/(markos)/page.stories.tsx
      provides: ≥4 named state stories per 208-UI-SPEC §B-16 (Default, EmptyAcrossSections, UrgentBacklog, PlaceholderHeavy)
    - path: components/markos/operator/PlaceholderBanner.tsx
      provides: variant-driven placeholder banner composing .c-notice c-notice--info per 208-UI-SPEC §Surface B + cross-surface reuse for 04/05/06
  key_links:
    - from: app/(markos)/page.tsx
      to: lib/markos/operator/morning-brief.ts
      via: page consumes MorningBriefItem array; renders 8 sections per UI-SPEC §Surface B layout grid
      pattern: "MorningBriefItem"
    - from: app/(markos)/settings/mcp/page.tsx
      to: .planning/operator-cockpit/morning-brief-contract.md
      via: budget and tool-risk signals should be promotable into the daily brief
      pattern: "role=\"meter\""
    - from: app/(markos)/settings/billing/page-shell.tsx
      to: lib/markos/operator/morning-brief.ts
      via: billing risk and hold posture should inform brief warnings
      pattern: "invoice"
ui_spec_acs_folded:
  - "B-1..B-16 (Morning Brief — 16 ACs)"
  - "X-1..X-5 (cross-cutting; verified by 208-01 tripwire applied to Morning Brief surface files)"
---

<objective>
Define Morning Brief as the default landing surface for operator decisions, using upstream pricing, compliance, and run-risk inputs now while reserving later evidence and connector enrichment behind explicit placeholders. Fold 208-UI-SPEC.md §Surface B (Morning Brief — B-1..B-16) verbatim into per-AC tasks; every brief section, every glyph, every placeholder banner is bound to a UI-SPEC AC.
</objective>

<context>
@.planning/phases/208-human-operating-interface/208-CONTEXT.md
@.planning/phases/208-human-operating-interface/208-RESEARCH.md
@.planning/phases/208-human-operating-interface/208-VALIDATION.md
@.planning/phases/208-human-operating-interface/208-UI-SPEC.md
@.planning/phases/207-agentrun-v2-orchestration-substrate/207-UI-SPEC.md
@DESIGN.md
@CLAUDE.md
@app/(markos)/page.tsx
@lib/markos/operator/shell.ts
@app/(markos)/settings/mcp/page.tsx
@app/(markos)/settings/billing/page-shell.tsx
</context>

<tasks>

<task id="208-02-00">
  <name>Wave 0 — UI-SPEC §Surface B fold preflight: read Morning Brief AC table</name>
  <files>test/operator-cockpit/phase-208/domain-2/morning-brief-ui-spec.test.js</files>
  <read_first>
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Surface B — Morning Brief (Plan 208-02)" (verbatim layout grid, copy register, state matrix)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"B-* — Morning Brief (Plan 208-02)" AC table (B-1..B-16) — verbatim verification gates
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Cross-surface Tokens Reference Table"
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Storybook Coverage" (Morning Brief: Default, EmptyAcrossSections, UrgentBacklog, PlaceholderHeavy)
    - .planning/phases/207-agentrun-v2-orchestration-substrate/207-UI-SPEC.md §Downstream UI Inheritance (RunApiEnvelope, RunCostContext, ApprovalHandoffRecord — upstream sources for brief items)
    - DESIGN.md §"Iconography" + §"Color blindness" (bracketed-glyph register)
    - styles/components.css (.c-card, .c-notice, .c-badge, .c-button, .c-chip-protocol, .c-status-dot--live primitives)
  </read_first>
  <action>
    Step 1: Create `test/operator-cockpit/phase-208/domain-2/morning-brief-ui-spec.test.js` as the per-AC verification harness for B-1..B-16. Each `test(...)` block names the AC and runs the verbatim grep / file-existence / Storybook check from 208-UI-SPEC §B-* AC table.

    Step 2: The test file must contain 16 named test blocks, one per AC, each with the verbatim verification command from 208-UI-SPEC. Tests target files that 208-02-01 / 208-02-02 will produce; the suite is created before the implementation tasks so failure is detectable from first commit (TDD-shape).

    Pseudocode for each test:

    - `test('B-1 token-only: zero hex in app/(markos)/page.module.css', ...)`: assert `! grep -nE "#[0-9a-fA-F]{3,8}" app/(markos)/page.module.css` returns 0 (skip-soft if file does not yet exist; final wave gate).
    - `test('B-2 page heading + .t-lead', ...)`: assert `grep -c "Morning brief" app/(markos)/page.tsx` >= 1 AND `grep -c "t-lead" app/(markos)/page.tsx` >= 1.
    - `test('B-3 8 sections rendered', ...)`: assert `grep -c "urgent_approvals\|run_failures\|pricing_risks\|compliance_alerts\|top_opportunities\|recent_wins\|future_evidence_placeholder\|future_recovery_placeholder" app/(markos)/page.tsx` >= 8.
    - `test('B-4 PlaceholderBanner future_phase_209 + future_phase_210', ...)`: assert `grep -c "PlaceholderBanner.*future_phase_209\|PlaceholderBanner.*future_phase_210" app/(markos)/page.tsx` >= 2.
    - `test('B-5 MorningBriefItem TS contract', ...)`: assert `grep -c "urgency\|next_action_type\|upstream_dependency\|placeholder_state" lib/markos/operator/morning-brief.ts` >= 4.
    - `test('B-6 section literals', ...)`: assert `grep -c "urgent_approvals\|run_failures\|pricing_risks\|compliance_alerts\|top_opportunities\|recent_wins" lib/markos/operator/morning-brief.ts` >= 6.
    - `test('B-7 placeholder_state literals', ...)`: assert `grep -c "waiting_phase_209\|waiting_phase_210\|waiting_phase_211" lib/markos/operator/morning-brief.ts` >= 3.
    - `test('B-8 .c-card default + no .c-card--feature + owner field', ...)`: assert `grep -c "c-card[^-]\|owner_role\|owner_id" app/(markos)/page.tsx` >= 4 AND `grep -c "c-card--feature" app/(markos)/page.tsx` returns 0.
    - `test('B-9 .c-badge state chips', ...)`: assert `grep -c "c-badge" app/(markos)/page.tsx` >= 6.
    - `test('B-10 MARKOS_PRICING_ENGINE_PENDING sentinel', ...)`: assert `grep -c "MARKOS_PRICING_ENGINE_PENDING\|billing_placeholder" app/(markos)/page.tsx` >= 1.
    - `test('B-11 mint-as-text via .c-button--tertiary + .c-chip-protocol', ...)`: assert `grep -c "c-button--tertiary\|c-chip-protocol" app/(markos)/page.tsx` >= 2.
    - `test('B-12 bracketed-glyph state coding', ...)`: assert `grep -c "\\[ok\\]\\|\\[warn\\]\\|\\[err\\]\\|\\[info\\]" app/(markos)/page.tsx` >= 8.
    - `test('B-13 prefers-reduced-motion block', ...)`: assert `grep -c "prefers-reduced-motion" app/(markos)/page.module.css` >= 1.
    - `test('B-14 banned-lexicon zero match', ...)`: assert `! grep -nE "synergy|leverage|empower|unlock|transform|revolutionize|supercharge|holistic|seamless|cutting-edge|innovative" app/(markos)/page.tsx` returns 0.
    - `test('B-15 mobile single-column at max-width: 640px', ...)`: assert `grep -c "max-width:[[:space:]]*640px" app/(markos)/page.module.css` >= 1 (manual + Storybook viewport check is documented in 208-VALIDATION.md "Manual-Only Verifications"; the automated portion is the @media query existence).
    - `test('B-16 Storybook ≥4 named state stories', ...)`: assert `grep -c "Default\|EmptyAcrossSections\|UrgentBacklog\|PlaceholderHeavy" app/(markos)/page.stories.tsx` >= 4.
  </action>
  <acceptance_criteria>
    - `test/operator-cockpit/phase-208/domain-2/morning-brief-ui-spec.test.js` exists and contains exactly 16 `test(...)` blocks named B-1 through B-16.
    - Test file references exact UI-SPEC strings: `grep -c "urgent_approvals\|run_failures\|pricing_risks\|compliance_alerts\|top_opportunities\|recent_wins\|future_evidence_placeholder\|future_recovery_placeholder" test/operator-cockpit/phase-208/domain-2/morning-brief-ui-spec.test.js` >= 8.
    - Test file references exact placeholder state literals: `grep -c "waiting_phase_209\|waiting_phase_210\|waiting_phase_211" test/operator-cockpit/phase-208/domain-2/morning-brief-ui-spec.test.js` >= 3.
  </acceptance_criteria>
  <verify>
    <automated>node -e "require('node:fs').readFileSync('test/operator-cockpit/phase-208/domain-2/morning-brief-ui-spec.test.js')"</automated>
  </verify>
  <done>The 16-AC verification harness for Morning Brief is in place before implementation. Failure of any B-* AC is detectable on every commit via Node `--test`.</done>
</task>

<task id="208-02-01">
  <name>Morning Brief contract + TS data assembler (208-UI-SPEC §B-5..B-7 + §B-10)</name>
  <files>.planning/operator-cockpit/morning-brief-contract.md, lib/markos/operator/morning-brief.ts, lib/markos/operator/index.cjs, test/operator-cockpit/phase-208/domain-2/brief-sections.test.js, test/operator-cockpit/phase-208/domain-2/empty-states.test.js</files>
  <read_first>
    - .planning/phases/208-human-operating-interface/208-RESEARCH.md section "Domain 2 - Morning Brief contract"
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Surface B — Morning Brief (Plan 208-02)" data dependencies block (verbatim MorningBriefItem field set + section literals + placeholder_state literals + upstream sources)
    - .planning/phases/207-agentrun-v2-orchestration-substrate/207-UI-SPEC.md §Downstream UI Inheritance (RunApiEnvelope, RunCostContext, ApprovalHandoffRecord)
    - app/(markos)/settings/mcp/page.tsx (top-tools breakdown source for top_opportunities section)
    - app/(markos)/settings/billing/page-shell.tsx (billing posture source for pricing_risks section)
  </read_first>
  <action>
    Step 1: Create `.planning/operator-cockpit/morning-brief-contract.md` with the exact `MorningBriefItem` fields per 208-UI-SPEC §Surface B data dependencies: `brief_item_id`, `tenant_id`, `section`, `title`, `reason`, `urgency`, `next_action_type`, `source_type`, `source_id`, `owner_role`, `owner_id`, `status`, `upstream_dependency`, `placeholder_state`, `created_at`, `updated_at`. Include an upstream-sources table mapping each section to its 207/206/205 input verbatim (urgent_approvals ← 207 ApprovalHandoffRecord with handoff_kind=='approval'; run_failures ← 207 RunApiEnvelope state ∈ {failed, dlq, paused, retry_wait}; pricing_risks ← 207 RunCostContext divergence + 205 PricingAlert; compliance_alerts ← 206 control-registry exception_id; top_opportunities ← MCP top-tools breakdown; recent_wins ← 207 runs state == 'completed' last 7 days).

    Step 2: Create `lib/markos/operator/morning-brief.ts`:
      - Export `MorningBriefItem` interface with the exact 16 fields from Step 1.
      - Export `section` literal union with the exact 8 literals per 208-UI-SPEC §B-3 + §Surface B data block: `'urgent_approvals' | 'run_failures' | 'pricing_risks' | 'compliance_alerts' | 'top_opportunities' | 'recent_wins' | 'future_recovery_placeholder' | 'future_evidence_placeholder'`.
      - Export `placeholder_state` literal union with the exact 4 literals per 208-UI-SPEC §B-7: `'none' | 'waiting_phase_209' | 'waiting_phase_210' | 'waiting_phase_211'`.
      - Export `urgency` literal union: `'critical' | 'high' | 'normal' | 'low'` (used for urgency badge color mapping per UI-SPEC §B-9).
      - Export `next_action_type` literal union: `'open_inbox' | 'open_run' | 'open_recovery' | 'open_task' | 'open_settings' | 'view_only'` (per 208-UI-SPEC Surface B copy register action-link CTAs).
      - Export `assembleMorningBrief(input: BriefAssemblerInput): MorningBriefItem[]` function that consumes 207/206/205 envelopes and produces the 8-section array. Per UI-SPEC §B-10: when `pricing_engine_context.schema_version` is unset, the pricing_risks section returns a single item with `placeholder_state: 'waiting_phase_211'` and reason containing the `{{MARKOS_PRICING_ENGINE_PENDING}}` sentinel verbatim.

    Step 3: Mirror the contract in `lib/markos/operator/index.cjs` (CommonJS twin) for Node `--test` consumption.

    Step 4: Create `test/operator-cockpit/phase-208/domain-2/brief-sections.test.js`:
      - Assert `MorningBriefItem` field names present in `lib/markos/operator/morning-brief.ts`.
      - Assert all 8 section literals present.
      - Assert `future_recovery_placeholder` and `future_evidence_placeholder` strings present (verbatim per existing AC).

    Step 5: Create `test/operator-cockpit/phase-208/domain-2/empty-states.test.js`:
      - Assert that `assembleMorningBrief()` returns the 8-section scaffold even when all inputs are empty (sections render with empty body; UI plan 208-02-02 wires the .c-notice empty-state copy).
      - Assert that pricing_risks placeholder behavior fires when `pricing_engine_context.schema_version` is unset (per UI-SPEC §B-10).
  </action>
  <acceptance_criteria>
    - `.planning/operator-cockpit/morning-brief-contract.md` contains the exact field names `urgency`, `next_action_type`, `upstream_dependency`, `placeholder_state` (208-UI-SPEC §B-5).
    - `lib/markos/operator/morning-brief.ts` contains the exact 6 visible section literals (208-UI-SPEC §B-6): `grep -c "urgent_approvals\|run_failures\|pricing_risks\|compliance_alerts\|top_opportunities\|recent_wins" lib/markos/operator/morning-brief.ts` >= 6.
    - `lib/markos/operator/morning-brief.ts` contains the exact 3 placeholder literals (208-UI-SPEC §B-7): `grep -c "waiting_phase_209\|waiting_phase_210\|waiting_phase_211" lib/markos/operator/morning-brief.ts` >= 3.
    - `test/operator-cockpit/phase-208/domain-2/brief-sections.test.js` contains the exact strings `future_recovery_placeholder` and `future_evidence_placeholder`.
    - `lib/markos/operator/morning-brief.ts` contains the exact `MARKOS_PRICING_ENGINE_PENDING` sentinel (208-UI-SPEC §B-10).
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/operator-cockpit/phase-208/domain-2/brief-sections.test.js test/operator-cockpit/phase-208/domain-2/empty-states.test.js</automated>
  </verify>
  <done>Morning Brief data contract is locked: 16-field MorningBriefItem, 8 section literals, 4 placeholder_state literals, pricing-engine placeholder behavior, and a deterministic assembler function. The contract is consumed by 208-02-02 (UI render) and downstream by 208-04 (urgent_approvals reuse).</done>
</task>

<task id="208-02-02">
  <name>Morning Brief landing UI render — fold §Surface B layout grid + B-1..B-4, B-8..B-16</name>
  <files>app/(markos)/page.tsx, app/(markos)/page.module.css, app/(markos)/page.stories.tsx, components/markos/operator/PlaceholderBanner.tsx, .planning/phases/208-human-operating-interface/208-VALIDATION.md</files>
  <read_first>
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Surface B — Morning Brief (Plan 208-02)" full body — verbatim layout grid (desktop + mobile), copy register table, state matrix, accessibility focus order, motion notes
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"B-* — Morning Brief (Plan 208-02)" AC table (B-1..B-16)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Cross-surface Tokens Reference Table" (var(--color-*), var(--space-*), var(--fs-*), var(--font-*) lookup)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §"Storybook Coverage" (page.stories.tsx 4 named states: Default, EmptyAcrossSections, UrgentBacklog, PlaceholderHeavy)
    - lib/markos/operator/morning-brief.ts (output of 208-02-01 — the contract this UI consumes)
    - app/(markos)/page.tsx (existing thin scaffold from 208-01-02)
    - styles/components.css (.c-card, .c-notice, .c-badge, .c-button--tertiary, .c-chip-protocol, .c-status-dot--live, .t-lead, .t-label-caps primitives)
    - DESIGN.md §"Iconography" (bracketed glyph register: `[ok]`, `[warn]`, `[err]`, `[info]`, `[block]`)
    - app/(markos)/operations/tasks/tasks.stories.tsx (CSF3 pattern reference from 213.4-05)
  </read_first>
  <action>
    Step 1 — `components/markos/operator/PlaceholderBanner.tsx`: create the cross-surface placeholder banner component. Variant prop: `'future_phase_209' | 'future_phase_210' | 'future_phase_211' | 'future_phase_212' | 'billing_placeholder'`. Each variant maps to a verbatim copy string from 208-UI-SPEC §Surface B copy register and renders `<div class="c-notice c-notice--info">[info] {variant_copy}</div>`. The component is shared with Plans 208-04 (future_phase_209), 208-05 (future_phase_210), 208-06 (future_phase_209+210+211+212). Reuse threshold: ≥4 surfaces — qualifies for shared extraction per D-15 (208-UI-SPEC §X-6).

    Step 2 — `app/(markos)/page.module.css`: token-only stylesheet for the Morning Brief landing per 208-UI-SPEC §Surface B layout grid. Required content:
      - Page wrapper consumes `var(--color-surface)` background and `var(--color-on-surface)` text per UI-SPEC §Color row 1.
      - Page padding: `var(--space-xl)` top + `var(--space-lg)` horizontal at desktop ≥ md; `var(--space-md)` horizontal at `max-width: 640px` (UI-SPEC §Spacing Scale row `--space-xl` + `--space-md`).
      - Container: `max-width: var(--w-container)` (1280px); `margin-inline: auto`.
      - Section gap: `var(--space-lg)` between brief sections (UI-SPEC §Spacing Scale row `--space-lg`).
      - Brief item grid: 1 column (mobile + desktop default — per Surface B layout grid; cards stack vertically except `urgent_approvals` which uses up to 3 cards horizontal at desktop).
      - At `max-width: 640px`: single column, full-width cards, `var(--space-md)` horizontal padding.
      - `@media (prefers-reduced-motion: reduce)` block: collapses all transition-duration to 0; `.c-status-dot--live` freezes at full opacity (UI-SPEC §B-13).
      - **Zero hex literals** (UI-SPEC §B-1). All color via `var(--color-*)`. All spacing via `var(--space-*)`.
      - **No local `.banner` / `.alert` / `.warning` / `.callout` / `.noticeBar`** classes (UI-SPEC §X-3).

    Step 3 — `app/(markos)/page.tsx`: replace the thin scaffold from 208-01-02 with the full Morning Brief landing. Required structure per 208-UI-SPEC §Surface B layout grid:

      Server-component wrapper (preserves Next.js App Router conventions):
      - `<h1>Morning brief</h1>` (B-2; renders heading via JetBrains Mono inherited from globals)
      - `<p class="t-lead">[info] Week of {YYYY-MM-DD}. Showing items created since your last sign-in.</p>` (B-2 .t-lead descriptor; verbatim copy from §Surface B copy register)
      - Page-level primary CTA: `<a class="c-button c-button--primary">Open inbox</a>` rendered ONLY when `urgent_approvals.length > 0`. Otherwise no `.c-button--primary` on the page. (UI-SPEC §Surface B copy register + §"Accent reserved-for list" rule 1.)

      Eight `<section>` blocks, each with `<h2>` + count chip `.c-badge--{state}`:

      1. `urgent_approvals` (header + count chip `.c-badge--warning`); empty state `<div class="c-notice c-notice--info">[info] No urgent approvals. Inbox is current.</div>`; populated: 1-3 `.c-card` brief items horizontal at desktop, vertical at mobile.
      2. `run_failures` (header + count chip `.c-badge--error`); empty state `[ok] No run failures since last brief.` `.c-notice c-notice--success`; populated: vertical `.c-card` list.
      3. `pricing_risks` (header + count chip `.c-badge--warning`); empty: `[ok] No pricing risks active.` `.c-notice c-notice--success`; placeholder when `placeholder_state == 'waiting_phase_211'`: `<PlaceholderBanner variant="billing_placeholder" />` containing `{{MARKOS_PRICING_ENGINE_PENDING}}` sentinel verbatim (B-10).
      4. `compliance_alerts` (header + count chip `.c-badge--warning`); empty: `[ok] No compliance alerts open.` `.c-notice c-notice--success`.
      5. `top_opportunities` (header — no count chip per UI-SPEC table); empty: `[info] No opportunities flagged this week.` `.c-notice c-notice--info`.
      6. `recent_wins` (header — no count chip); empty: `[info] No wins recorded this week.` `.c-notice c-notice--info`; populated cards dimmed via `--color-on-surface-subtle`.
      7. `future_evidence_placeholder` heading "Future evidence" + `<PlaceholderBanner variant="future_phase_209" />` body (B-4).
      8. `future_recovery_placeholder` heading "Future connector recovery" + `<PlaceholderBanner variant="future_phase_210" />` body (B-4).

      Brief item card structure (per Surface B):
      - `<article class="c-card">` (NOT `.c-card--feature` — D-13 carry per X-4 + B-8)
      - `<h3>{title}</h3>` (JetBrains Mono via globals; --fs-h3)
      - `<p>{reason}</p>` (Inter; --fs-body-md)
      - Metadata row: eyebrow `<span class="t-label-caps">{urgency}</span>` + owner + timestamp `<span class="c-field__help">{created_at}</span>` (--fs-body-sm via .c-field__help)
      - Bracketed-glyph eyebrow paired with urgency: `[err]` for critical, `[warn]` for high, `[info]` for normal/low (B-12; UI-SPEC §"Color blindness" rule)
      - Action link: `<a class="c-button c-button--tertiary">{next_action_label} →</a>` (mint-text via `--color-primary-text`; B-11)
      - Deep-link IDs: `<span class="c-chip-protocol">{source_id}</span>` for run_id / task_id / approval_item_id (B-11; mint-as-text via `--color-primary-text`)

      State badges (per item):
      - urgency: `.c-badge--error` (critical/`[err]`), `.c-badge--warning` (high/`[warn]`), `.c-badge--info` (normal/`[info]`), `.c-badge--success` (resolved/`[ok]`)

    Step 4 — `app/(markos)/page.stories.tsx`: CSF3 stories with ≥4 named states per UI-SPEC §B-16:
      - `Default`: assembled brief with 1 urgent approval, 2 run failures, 1 pricing risk, 1 compliance alert, 2 opportunities, 3 wins.
      - `EmptyAcrossSections`: every section returns empty array; page renders 8 `.c-notice` empty states.
      - `UrgentBacklog`: 5 urgent approvals, 8 run failures, 3 pricing risks, 4 compliance alerts; primary CTA "Open inbox" visible.
      - `PlaceholderHeavy`: pricing_risks in placeholder state (`{{MARKOS_PRICING_ENGINE_PENDING}}` visible); future_evidence_placeholder + future_recovery_placeholder both render `<PlaceholderBanner>`.

    Step 5 — Update `.planning/phases/208-human-operating-interface/208-VALIDATION.md`:
      - Add row `208-02-02` to per-task verification map.
      - Update `## UI-SPEC AC Coverage Map` (created in 208-01-00): mark B-1..B-16 with verification commands and target test paths.

    Step 6 — Banned-lexicon compliance (B-14, X-9): scan `app/(markos)/page.tsx` and `app/(markos)/page.module.css` for the banned register. Zero matches required. No exclamation points in any product copy.

    Step 7 — Mobile-priority compliance (B-15, X-8): the page MUST be `mobile_priority: 'critical'` per `lib/markos/operator/shell.ts` (208-01-02 contract). At `max-width: 640px`: single column, full-width "Open inbox" CTA, item-level `.c-button--tertiary` mint-text links inline. Touch targets inherit `--h-control-touch` (44px) via global `(pointer: coarse)` rule from styles/components.css.
  </action>
  <acceptance_criteria>
    - **B-1 (token-only)**: `! grep -nE "#[0-9a-fA-F]{3,8}" app/(markos)/page.module.css` returns 0.
    - **B-2 (h1 + .t-lead)**: `grep -c "Morning brief" app/(markos)/page.tsx` >= 1; `grep -c "t-lead" app/(markos)/page.tsx` >= 1.
    - **B-3 (8 sections)**: `grep -c "urgent_approvals\|run_failures\|pricing_risks\|compliance_alerts\|top_opportunities\|recent_wins\|future_evidence_placeholder\|future_recovery_placeholder" app/(markos)/page.tsx` >= 8.
    - **B-4 (PlaceholderBanner future_phase_209 + future_phase_210)**: `grep -c "PlaceholderBanner.*future_phase_209\|PlaceholderBanner.*future_phase_210" app/(markos)/page.tsx` >= 2.
    - **B-8 (.c-card no .c-card--feature + owner)**: `grep -c "c-card[^-]\|owner_role\|owner_id" app/(markos)/page.tsx` >= 4 AND `grep -c "c-card--feature" app/(markos)/page.tsx` returns 0.
    - **B-9 (.c-badge state chips)**: `grep -c "c-badge" app/(markos)/page.tsx` >= 6.
    - **B-10 (MARKOS_PRICING_ENGINE_PENDING + billing_placeholder)**: `grep -c "MARKOS_PRICING_ENGINE_PENDING\|billing_placeholder" app/(markos)/page.tsx` >= 1.
    - **B-11 (mint-as-text)**: `grep -c "c-button--tertiary\|c-chip-protocol" app/(markos)/page.tsx` >= 2.
    - **B-12 (bracketed-glyph state coding)**: `grep -c "\[ok\]\|\[warn\]\|\[err\]\|\[info\]" app/(markos)/page.tsx` >= 8.
    - **B-13 (prefers-reduced-motion)**: `grep -c "prefers-reduced-motion" app/(markos)/page.module.css` >= 1.
    - **B-14 (banned-lexicon zero match)**: `! grep -nE "synergy\|leverage\|empower\|unlock\|transform" app/(markos)/page.tsx` returns 0.
    - **B-16 (Storybook ≥4 named state stories)**: `grep -c "Default\|EmptyAcrossSections\|UrgentBacklog\|PlaceholderHeavy" app/(markos)/page.stories.tsx` >= 4.
    - **PlaceholderBanner reusable**: `components/markos/operator/PlaceholderBanner.tsx` exists with variant prop accepting `future_phase_209|future_phase_210|future_phase_211|future_phase_212|billing_placeholder`.
    - **208-VALIDATION.md updated**: row `208-02-02` present in per-task map; UI-SPEC AC Coverage Map B-1..B-16 rows have verification commands.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/operator-cockpit/phase-208/domain-2/morning-brief-ui-spec.test.js</automated>
  </verify>
  <done>Morning Brief landing surface renders all 8 sections per 208-UI-SPEC §Surface B layout grid. Every B-1..B-16 AC is automated. PlaceholderBanner is extracted and reused (qualifying D-15). Storybook covers 4 named states. Page is `mobile_priority: 'critical'` and meets 44px touch-target on coarse pointers.</done>
</task>

</tasks>

<verification>
- npm test -- test/operator-cockpit/phase-208/domain-2/
- npm test -- test/operator-cockpit/phase-208/domain-2/morning-brief-ui-spec.test.js
- npm run build-storybook (verifies app/(markos)/page.stories.tsx registers cleanly)
</verification>

<success_criteria>
- Morning Brief items have owner, urgency, reason, and next action
- Pricing/compliance/run risks surface in one landing view
- Future evidence and connector sections are explicit placeholders, not hidden assumptions
- 208-UI-SPEC §Surface B ACs B-1..B-16 are realized verbatim and verified by Node `--test`
- PlaceholderBanner is extracted to components/markos/operator/ and reused across Plans 04, 05, 06
- Page is the default operator landing (replaces Phase 37 entity-list scaffold from Plan 208-01)
</success_criteria>
