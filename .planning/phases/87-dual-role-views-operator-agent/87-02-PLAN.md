---
phase: 87-dual-role-views-operator-agent
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - onboarding/backend/vault/visibility-scope.cjs
  - onboarding/backend/vault/role-views.cjs
  - test/phase-87/role-views-scope.test.js
autonomous: true
requirements:
  - ROLEV-04
must_haves:
  truths:
    - "operator and agent role-views are explicitly separated by policy"
    - "operator view allows management surfaces without exposing agent execution internals"
    - "agent view allows retrieval/execution paths without management mutations"
    - "tenant mismatch fails closed for both role-views"
  artifacts:
    - path: onboarding/backend/vault/role-views.cjs
      provides: role-view orchestration and view-specific route/service guards.
    - path: onboarding/backend/vault/visibility-scope.cjs
      provides: extended policy helpers for operator-view and agent-view checks.
    - path: test/phase-87/role-views-scope.test.js
      provides: policy/authorization regression suite.
---

<objective>
Establish explicit dual role-view policy boundaries so operator management surfaces and agent retrieval/execution surfaces remain separated over shared artifact lineage.

Purpose: Implement ROLEV-04 role-view separation at the runtime policy layer.
Output: role-view policy module + updated scope checks + tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87-dual-role-views-operator-agent/87-CONTEXT.md
@.planning/phases/87-dual-role-views-operator-agent/87-RESEARCH.md
@onboarding/backend/vault/visibility-scope.cjs
@onboarding/backend/vault/vault-retriever.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create role-view scope tests</name>
  <files>test/phase-87/role-views-scope.test.js</files>
  <behavior>
    - operator claims can access operator view checks and are denied agent-only actions when configured.
    - agent claims can access retrieval view checks and are denied management-only actions.
    - tenant mismatch rejects for both views before data access.
    - unauthorized roles fail closed with explicit error codes.
  </behavior>
  <action>Write failing tests first for policy separation and fail-closed behavior.</action>
  <verify>
    <automated>node --test test/phase-87/role-views-scope.test.js</automated>
  </verify>
  <done>Tests encode required role-view separation contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement role-view policy module and scope extensions</name>
  <files>onboarding/backend/vault/role-views.cjs, onboarding/backend/vault/visibility-scope.cjs</files>
  <behavior>
    - Add `checkOperatorViewScope` and `checkAgentViewScope` helpers.
    - Keep existing retrieval scope checks backward compatible.
    - Expose deterministic policy result envelopes (`allowed`, `code`, `reason`).
  </behavior>
  <action>Implement explicit view-scoped policy helpers and wire them for future handler usage.</action>
  <verify>
    <automated>node --test test/phase-87/role-views-scope.test.js</automated>
  </verify>
  <done>Dual role-view policy boundary is implemented and test-covered.</done>
</task>

</tasks>

<verification>
- `node --test test/phase-87/role-views-scope.test.js`
- `node --test test/phase-86/vault-retriever.test.js` (policy regression)
</verification>

<success_criteria>
- Operator and agent views are policy-isolated with fail-closed defaults.
- Existing retrieval path remains compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/87-dual-role-views-operator-agent/87-02-SUMMARY.md`.
</output>
