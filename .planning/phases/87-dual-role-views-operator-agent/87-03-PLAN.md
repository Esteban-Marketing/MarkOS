---
phase: 87-dual-role-views-operator-agent
plan: 03
type: execute
wave: 2
depends_on: ["87-01", "87-02"]
files_modified:
  - onboarding/backend/vault/sync-service.cjs
  - onboarding/backend/vault/vault-retriever.cjs
  - onboarding/backend/vault/lineage-log.cjs
  - test/phase-87/unified-lineage.test.js
autonomous: true
requirements:
  - ROLEV-04
must_haves:
  truths:
    - "operator lifecycle events and agent retrieval/execution events share one artifact identity contract"
    - "lineage logs always include tenant_id, artifact_id, actor role/view, mode/action, and timestamp"
    - "agent retrieval events preserve mode reason/apply/iterate linkage"
    - "lineage append failure is explicit and does not silently drop audit signals"
  artifacts:
    - path: onboarding/backend/vault/lineage-log.cjs
      provides: unified lineage append/read helpers with deterministic event schema.
    - path: onboarding/backend/vault/sync-service.cjs
      provides: operator-side lifecycle lineage emission.
    - path: onboarding/backend/vault/vault-retriever.cjs
      provides: agent-side retrieval lineage emission.
    - path: test/phase-87/unified-lineage.test.js
      provides: cross-role lineage continuity tests.
---

<objective>
Implement unified lineage across both role-views so every operator management action and every agent retrieval/execution action can be audited through the same artifact identity chain.

Purpose: complete ROLEV-04 auditable access log behavior.
Output: lineage module + sync/retriever instrumentation + integration tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87-dual-role-views-operator-agent/87-CONTEXT.md
@.planning/phases/87-dual-role-views-operator-agent/87-RESEARCH.md
@onboarding/backend/vault/sync-service.cjs
@onboarding/backend/vault/vault-retriever.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author unified lineage integration tests</name>
  <files>test/phase-87/unified-lineage.test.js</files>
  <behavior>
    - operator sync event creates lineage entry with view=`operator`.
    - agent retrieveReason/retrieveApply/retrieveIterate create lineage entries with view=`agent` and corresponding mode.
    - all entries for same artifact share tenant_id + artifact_id identity linkage.
    - missing identity keys fail with deterministic error code.
  </behavior>
  <action>Write failing integration tests first with mocked store append hooks.</action>
  <verify>
    <automated>node --test test/phase-87/unified-lineage.test.js</automated>
  </verify>
  <done>Tests define unified lineage contract for cross-role events.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement lineage module and instrument sync + retriever</name>
  <files>onboarding/backend/vault/lineage-log.cjs, onboarding/backend/vault/sync-service.cjs, onboarding/backend/vault/vault-retriever.cjs</files>
  <behavior>
    - Introduce `appendLineageEvent` and `createLineageLogger` utilities.
    - Emit operator lifecycle lineage during sync handling.
    - Emit agent retrieval lineage in retriever pipeline after scope pass and before return.
  </behavior>
  <action>Implement deterministic lineage schema and wire with dependency injection for tests.</action>
  <verify>
    <automated>node --test test/phase-87/unified-lineage.test.js</automated>
  </verify>
  <done>Unified lineage is emitted and validated for both role views.</done>
</task>

</tasks>

<verification>
- `node --test test/phase-87/unified-lineage.test.js`
- `node --test test/phase-86/*.test.js` (retriever regression)
</verification>

<success_criteria>
- Dual role views preserve one auditable lineage chain by artifact identity.
- Retrieval mode and operator lifecycle actions are traceable in one schema.
</success_criteria>

<output>
After completion, create `.planning/phases/87-dual-role-views-operator-agent/87-03-SUMMARY.md`.
</output>
