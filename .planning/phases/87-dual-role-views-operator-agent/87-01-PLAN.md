---
phase: 87-dual-role-views-operator-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - onboarding/backend/vault/audit-store.cjs
  - onboarding/backend/vault/supabase-audit-store.cjs
  - test/phase-87/supabase-audit-store.test.js
autonomous: true
requirements:
  - ROLEV-04
must_haves:
  truths:
    - "audit-store public contract remains stable: append/getAll/size/clear"
    - "Supabase-backed implementation supports tenant-scoped reads"
    - "in-memory fallback remains available for local/test environments"
    - "store writes and reads preserve artifact_id + tenant_id lineage keys"
  artifacts:
    - path: onboarding/backend/vault/supabase-audit-store.cjs
      provides: Supabase adapter implementing audit-store contract.
    - path: onboarding/backend/vault/audit-store.cjs
      provides: runtime selection wrapper preserving existing module API.
    - path: test/phase-87/supabase-audit-store.test.js
      provides: contract tests for supabase-backed and fallback modes.
---

<objective>
Migrate Phase 87 audit persistence from in-memory-only behavior to a Supabase-backed adapter while keeping the existing `audit-store.cjs` call contract unchanged.

Purpose: Establish operational correctness for app data persistence and make role-view lineage durable.
Output: Supabase adapter + compatibility wrapper + passing tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/87-dual-role-views-operator-agent/87-CONTEXT.md
@.planning/phases/87-dual-role-views-operator-agent/87-RESEARCH.md
@onboarding/backend/vault/audit-store.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author contract tests for Supabase-backed audit store</name>
  <files>test/phase-87/supabase-audit-store.test.js</files>
  <behavior>
    - append persists entry with appended_at timestamp and lineage keys.
    - getAll({ tenantId }) returns only matching tenant rows.
    - size returns row count for selected mode.
    - clear empties store in fallback mode and test schema mode.
  </behavior>
  <action>Create failing tests first to lock compatibility with current audit-store API.</action>
  <verify>
    <automated>node --test test/phase-87/supabase-audit-store.test.js</automated>
  </verify>
  <done>Contract tests exist and encode compatibility requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Supabase adapter and compatibility wrapper</name>
  <files>onboarding/backend/vault/supabase-audit-store.cjs, onboarding/backend/vault/audit-store.cjs</files>
  <behavior>
    - Introduce createSupabaseAuditStore({supabase, tableName}) factory.
    - Keep audit-store exports unchanged for existing callers.
    - Runtime selects Supabase when configured; otherwise fallback to in-memory implementation.
  </behavior>
  <action>Implement adapter with strict tenant filters and deterministic field mapping.</action>
  <verify>
    <automated>node --test test/phase-87/supabase-audit-store.test.js</automated>
  </verify>
  <done>Supabase-backed path works without breaking existing module consumers.</done>
</task>

</tasks>

<verification>
- `node --test test/phase-87/supabase-audit-store.test.js`
- `node --test test/phase-86/*.test.js` (regression gate)
</verification>

<success_criteria>
- App data persistence for role-view lineage is durable in Supabase.
- Existing `audit-store.cjs` contract remains backward compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/87-dual-role-views-operator-agent/87-01-SUMMARY.md`.
</output>
