---
phase: 213.1
plan: 02
subsystem: chrome-rotation-grace-banner
tags: [ui-canon, design-md, tokens, rotation-grace-banner, bracketed-glyph, color-blindness, phase-203-test-rewrite]
dependency_graph:
  requires:
    - styles/components.css (.c-status-dot)
    - app/tokens.css (--color-warning, --color-error, --color-accent-hover, --space-xs/sm/md, --font-sans, --fs-body-md, --lh-body-md, --fw-semibold, --h-control-touch, --duration-base, --ease-out)
    - app/globals.css (:focus-visible, prefers-reduced-motion)
    - app/(markos)/layout-shell.tsx (Plan 01 — RotationBannerMount mount point preserved)
  provides:
    - app/(markos)/_components/RotationGraceBanner.module.css (token-only banner — 75 LOC, zero hex, zero Sora, zero local focus/motion overrides)
    - app/(markos)/_components/RotationGraceBanner.tsx (bracketed-glyph state-coded surface — 3x [warn] + 1x [err] + .c-status-dot composition)
    - test/webhooks/ui-s4-a11y.test.js (Phase 203 banner content/CSS regression test rewritten — 17 tests / 17 pass)
  affects:
    - test/webhooks/layout-shell-banner.test.js (Phase 203 wiring test — UNTOUCHED, verified 7/7 still pass)
tech_stack:
  added: []
  patterns:
    - "Bracketed-glyph state coding: <strong>[warn] ...</strong> / <strong>[err] ...</strong> per DESIGN.md 'Color blindness' rule (state color is never the only signal)"
    - "Template-literal class composition: `${styles.warningDot} c-status-dot` composes local override (background) with global primitive (size + radius)"
    - "Canonical alpha-tint formula for badges/banners: rgb(R G B / 0.12) bg + var(--color-{warning,error}) text + var(--color-{warning,error}) border-inline-start"
    - "Inherit-before-override: delete local :focus-visible and prefers-reduced-motion in modules that have global rules in app/globals.css"
key_files:
  created: []
  modified:
    - app/(markos)/_components/RotationGraceBanner.module.css (109 LOC -> 75 LOC, -34 LOC; rewrite)
    - app/(markos)/_components/RotationGraceBanner.tsx (118 LOC -> 117 LOC, -1 LOC; bracketed-glyph + class rename in lockstep)
    - test/webhooks/ui-s4-a11y.test.js (137 LOC -> 168 LOC, +31 LOC; rewrite to token-cited assertions)
decisions:
  - "Canonical RESEARCH.md correction applied: ui-s4-a11y.test.js (content/CSS regressions) is rewritten; layout-shell-banner.test.js (wiring) stays UNTOUCHED — both verified to pass after Plan-02"
  - "Class rename .pulseDot -> .warningDot reflects DESIGN.md kernel-pulse reservation for live-system signals; banner severity dot is static"
  - "T-1 banner span composes c-status-dot primitive via template literal (no clsx) — local .warningDot supplies background color, global primitive supplies size + radius"
  - "Three legacy CSS blocks deleted as no-ops: [data-stage='multi'] (duplicated default warn palette), @media (prefers-reduced-motion: reduce) (globals.css line 175-184 collapses ALL transitions globally), .graceLink:focus-visible outline override (globals.css line 121-125 supplies global focus ring)"
  - "Comment-only references to forbidden literals (#0d9488, prefers-reduced-motion, .pulseDot) were stripped from the rewritten module.css to satisfy strict grep-based AC counts"
metrics:
  duration: "00:12:32"
  completed_date: "2026-04-28"
  tasks_completed: 3
  files_created: 0
  files_modified: 3
  net_loc_delta: -4
---

# Phase 213.1 Plan 02: RotationGraceBanner — DESIGN.md token adoption Summary

**One-liner:** Rewrote `app/(markos)/_components/RotationGraceBanner.module.css` (109 LOC -> 75 LOC) to consume `var(--*)` tokens exclusively (warn palette = `rgb(255 184 0 / 0.12)` + `var(--color-warning)`; T-0 escalation = `rgb(248 81 73 / 0.12)` + `var(--color-error)`; zero inline hex; zero Sora; zero local focus/motion overrides), wired `RotationGraceBanner.tsx` with bracketed-glyph state coding (3x `[warn]` on T-7/T-1/multi `<strong>`, 1x `[err]` on T-0 `<strong>`) plus `styles.pulseDot` -> `styles.warningDot` rename composing the global `.c-status-dot` primitive, and rewrote the Phase 203 banner content/CSS regression suite `test/webhooks/ui-s4-a11y.test.js` (17 tests / 17 pass) to assert the new token-cited shape — all while leaving the Phase 203 wiring test `test/webhooks/layout-shell-banner.test.js` UNTOUCHED (verified 7/7 still pass). Closes UI-SPEC AC#1, AC#2, AC#3, AC#5, AC#6, AC#9, AC#10, AC#10b, AC#11, AC#12, AC#13.

## Files Modified

| File | Type | LOC Before | LOC After | Delta |
|------|------|-----------:|----------:|------:|
| `app/(markos)/_components/RotationGraceBanner.module.css` | rewrite | 109 | 75 | -34 |
| `app/(markos)/_components/RotationGraceBanner.tsx` | edit | 118 | 117 | -1 |
| `test/webhooks/ui-s4-a11y.test.js` | rewrite | 137 | 168 | +31 |

## Class-Name Rename Register (RotationGraceBanner.module.css)

### DELETED
- `.pulseDot` (renamed; see RENAMED below)
- `.graceLink:focus-visible { outline: 2px solid #0d9488; outline-offset: 2px; border-radius: 4px; }` -> inherited globally from `app/globals.css :focus-visible`
- `@media (prefers-reduced-motion: reduce) { .graceLink { transition: none; } }` -> inherited globally from `app/globals.css` (motion-collapse rule covers ALL transitions)
- `[data-stage="multi"].rotationGraceBanner { ... }` -> no-op (duplicated default warn palette; multi inherits the base styling automatically)
- `[data-stage="t-0"] .pulseDot { background: #dc2626; }` -> renamed to `.warningDot` and migrated to `var(--color-error)`
- `[data-stage="t-0"] .graceLink:hover { color: #7f1d1d; }` -> consolidated; T-0 hover follows base graceLink hover pattern (`var(--color-accent-hover)`)

### KEPT
`.rotationGraceBanner`, `.rotationGraceBanner strong`, `.graceLink`, `.graceLink:hover`, `[data-stage="t-0"].rotationGraceBanner`, `[data-stage="t-0"].rotationGraceBanner strong`, `[data-stage="t-0"] .graceLink`.

### RENAMED
`.pulseDot` -> `.warningDot` (and `[data-stage="t-0"] .pulseDot` -> `[data-stage="t-0"] .warningDot`). DESIGN.md reserves the kernel-pulse animation (2s opacity pulse) for genuinely live status; banner severity dots are static.

### Primitives composed JSX-side in `RotationGraceBanner.tsx`
- `.c-status-dot` (global primitive from `styles/components.css` — supplies `width: var(--status-dot)` + `height: var(--status-dot)` + `border-radius: var(--radius-full)`) — composed alongside `.warningDot` (which supplies `background: var(--color-warning)`) via template literal `${styles.warningDot} c-status-dot`.

## Bracketed-Glyph State Coding Edits (UI-SPEC AC#9 + DESIGN.md "Color blindness")

| Banner stage | Before | After | Glyph |
|---|---|---|:---:|
| T-7 (line 85) | `<strong>Signing-secret rotation in progress.</strong>` | `<strong>[warn] Signing-secret rotation in progress.</strong>` | `[warn]` |
| T-1 (line 98) | `<strong>Signing-secret rotation ends tomorrow.</strong>` | `<strong>[warn] Signing-secret rotation ends tomorrow.</strong>` | `[warn]` |
| Multi (line 64) | `<strong>{rotations.length} signing-secret rotations in progress.</strong>` | `<strong>[warn] {rotations.length} signing-secret rotations in progress.</strong>` | `[warn]` |
| T-0 (line 110) | `<strong>Grace window ends today.</strong>` | `<strong>[err] Grace window ends today.</strong>` | `[err]` |

Final counts: `grep -c '\[warn\]'` = `3`. `grep -c '\[err\]'` = `1`. The bracketed glyph IS announced to screen readers (intentional, per RESEARCH.md Pitfall 6 — color is never the only signal).

## Phase 203 Test Surface Delta

### `test/webhooks/ui-s4-a11y.test.js` (REWRITTEN)
**Deleted assertions (4 hex, 1 outline, 1 motion):**
- `assert.match(css, /#fef3c7/)`, `assert.match(css, /#78350f/)`, `assert.match(css, /#d97706/)` — replaced by `assert.match(css, /var\(--color-warning\)/)` + `assert.match(css, /rgb\(255 184 0 \/ 0\.12\)/)` + `assert.doesNotMatch(css, /#fef3c7|#78350f|#d97706|#451a03/)`
- `assert.match(css, /#fef2f2/)`, `assert.match(css, /#991b1b/)`, `assert.match(css, /#dc2626/)` — replaced by `assert.match(css, /var\(--color-error\)/)` + `assert.match(css, /rgb\(248 81 73 \/ 0\.12\)/)` + `assert.doesNotMatch(css, /#fef2f2|#991b1b|#dc2626|#7f1d1d/)`
- `assert.match(css, /min-height:\s*44px/)` + `assert.match(css, /outline:\s*2px solid #0d9488/)` — replaced by `assert.match(css, /min-height:\s*var\(--h-control-touch\)/)` + `assert.doesNotMatch(css, /outline:\s*2px solid #/)` + `assert.doesNotMatch(css, /#0d9488/)`
- `assert.match(css, /prefers-reduced-motion/)` — replaced by `assert.doesNotMatch(css, /prefers-reduced-motion/)` (CSS now inherits global rule)

**Added assertions (4 bracketed-glyph, 4 token-cited, 1 rename):**
- `assert.match(tsx, /<strong>\[warn\] Signing-secret rotation in progress\./)` (T-7)
- `assert.match(tsx, /<strong>\[warn\] Signing-secret rotation ends tomorrow\./)` (T-1)
- `assert.match(tsx, /<strong>\[err\] Grace window ends today\./)` (T-0)
- `assert.match(tsx, /<strong>\[warn\] \{rotations\.length\} signing-secret rotations in progress\./)` (multi)
- Token-cited CSS shape: `var(--color-warning)`, `var(--color-error)`, `rgb(255 184 0 / 0.12)`, `rgb(248 81 73 / 0.12)`, `var(--h-control-touch)`
- Pulse-dot rename + composition: `assert.doesNotMatch(tsx, /pulseDot/)` + `assert.doesNotMatch(css, /pulseDot/)` + `assert.match(tsx, /styles\.warningDot/)` + `assert.match(tsx, /c-status-dot/)` + `assert.match(css, /\.warningDot\s*\{/)`

**Test runner result:** `node --test test/webhooks/ui-s4-a11y.test.js` -> 17 tests / 17 pass / 0 fail.

### `test/webhooks/layout-shell-banner.test.js` (UNTOUCHED — preservation evidence)
| Check | Result |
|---|---|
| `git diff --name-only HEAD~3..HEAD test/webhooks/layout-shell-banner.test.js` | empty (file untouched across 3 commits) |
| `node --test test/webhooks/layout-shell-banner.test.js` | 7 tests / 7 pass |
| Wiring contract grep shape (use client, fetch URL, mount, no-dismiss) | preserved verbatim in `RotationBannerMount.tsx` and `layout-shell.tsx` |

## Acceptance Criteria Closures

| AC | Description | Verification | Result |
|---|---|---|:---:|
| AC#1 | Zero inline hex in `RotationGraceBanner.module.css` | `grep -cE "#[0-9A-Fa-f]{3,8}"` = 0 | PASS |
| AC#2 | Off-grid px purged in module.css | `grep -nE ":[[:space:]]+[0-9]+px"` shows only `4px` (border-inline-start) and `2px` (text-underline-offset) | PASS |
| AC#3 | Composes `.c-*` primitive (`.c-status-dot` in T-1 banner) | `grep -c "c-status-dot"` = 1 | PASS |
| AC#5 | No local `:focus-visible` outline override | `grep -ciE "outline:[[:space:]]*2px solid #"` = 0 | PASS |
| AC#6 | No local `prefers-reduced-motion` override | `grep -ci "prefers-reduced-motion"` = 0 | PASS |
| AC#9 | Bracketed-glyph state coding on every banner `<strong>` | 3x `[warn]` (T-7/T-1/multi) + 1x `[err]` (T-0) | PASS |
| AC#10 | Phase 203 wiring test green | `node --test layout-shell-banner.test.js` = 7/7 pass | PASS |
| AC#10b | Phase 203 banner content/CSS test rewritten and green | `node --test ui-s4-a11y.test.js` = 17/17 pass | PASS |
| AC#11 | No Sora / Space Grotesk fonts | `grep -ciE "Sora\|Space Grotesk"` = 0 | PASS |
| AC#12 | No legacy teal hex `#0d9488` | `grep -ci "#0d9488"` = 0 | PASS |
| AC#13 | Banned lexicon scan in tsx | `grep -ciE "\bclose\b\|\bdismiss\b\|aria-label=\"Close\"\|×"` = 0 | PASS |

## Build + Test Verification

| Command | Result |
|---|---|
| `node --test test/webhooks/layout-shell-banner.test.js` | 7 tests / 7 pass / 0 fail (Phase 203 wiring contract preserved) |
| `node --test test/webhooks/ui-s4-a11y.test.js` | 17 tests / 17 pass / 0 fail (token-cited shape verified) |
| `node --test test/webhooks/layout-shell-banner.test.js test/webhooks/ui-s4-a11y.test.js` (combined) | 24 tests / 24 pass / 0 fail |
| `npx tsc --noEmit -p tsconfig.json` (filtered to plan-touched files) | no errors in `RotationGraceBanner.{tsx,module.css}` (pre-existing project-wide TS errors in `lib/markos/crm/agent-actions.ts` and `app/(marketing)/docs/[[...slug]]/page.tsx` are out-of-scope per Rule "SCOPE BOUNDARY") |

`npm run build` does not exist as a script in `package.json`; per Plan 01's deviation precedent (commit `67e0036` SUMMARY), the substitution is `npx tsc --noEmit` filtered to plan-touched files. No new TS errors introduced.

## Deviations from Plan

**1. [Rule 3 — Blocking] Comment hygiene to satisfy strict grep AC counts**
- **Found during:** Task 1 verification.
- **Issue:** The initial rewrite preserved historical references to forbidden literals (`#0d9488`, `prefers-reduced-motion`, `.pulseDot`) inside the file's banner comment ("was a teal #0d9488 local override — now deleted", "Reduced motion: globally inherited from globals.css prefers-reduced-motion (was a redundant local override — deleted)", ".pulseDot renamed .warningDot"). These are descriptive comments — but the AC#1/AC#6/AC#12 grep counts treat the file as a single text blob. With the comments present, `grep -ci "#0d9488"` returned `1` and `grep -ci "prefers-reduced-motion"` returned `1`.
- **Fix:** Stripped the literal hex `#0d9488` from the focus-ring comment (paraphrased "was a teal local override — now deleted") and rephrased the reduced-motion + pulseDot comments to describe the rename without naming the deleted token / class verbatim. The rename is still documented; the literals are absent.
- **Files modified:** `app/(markos)/_components/RotationGraceBanner.module.css` (comment-only edits, no semantic CSS change).
- **Commit:** `48a14f1` (folded into Task 1).

No other deviations. Tasks 1, 2, 3 executed exactly per `<action>` blocks in PLAN.md.

## Authentication Gates

None encountered.

## Hand-off to Plan-03

Plan-03 may now begin. Inputs Plan-03 will consume from this plan:
- `.warningDot` class (renamed from `.pulseDot`) — used by RotationGraceBanner stories to render the T-1 dot in the static (non-pulsing) variant; Plan-03 stories should NOT compose `.c-status-dot--live` for the banner severity dot.
- Bracketed-glyph copy variants (`[warn]` / `[err]`) — Plan-03 stories should reflect these exact strings in their `<strong>` snapshots; Chromatic visual regression baselines will lock the new copy.
- Token-cited CSS (no inline hex) — Plan-03's `npm run test:ui-a11y` axe assertions can rely on the contrast computed from `var(--color-warning)` text on `rgb(255 184 0 / 0.12)` background (and `var(--color-error)` text on `rgb(248 81 73 / 0.12)` background); WCAG AA thresholds.
- No shared-file overlap with Plan-03; the only Plan-03 file that touches Plan-02 territory is the new `RotationGraceBanner.stories.tsx` (NEW file, no conflict). Plan-03 ALSO updates `styles/components.css` (adds `(pointer: coarse)` ≥44px on `.c-nav-link` per RESEARCH.md) — that is global, no conflict with the banner module.

Plan-03 wave: stories + axe a11y tests + components.css `(pointer: coarse)` + `.storybook/preview.tsx` globals.css import.

## Self-Check: PASSED

**Files created/modified — verified on disk:**
- `app/(markos)/_components/RotationGraceBanner.module.css` — FOUND (75 LOC, token-only, zero hex)
- `app/(markos)/_components/RotationGraceBanner.tsx` — FOUND (117 LOC, bracketed-glyph + warningDot composition)
- `test/webhooks/ui-s4-a11y.test.js` — FOUND (168 LOC, 17/17 pass)

**Commits — verified in git log:**
- `48a14f1` — `feat(213.1-02): rewrite RotationGraceBanner.module.css to DESIGN.md tokens` — FOUND
- `d2f577d` — `feat(213.1-02): wire RotationGraceBanner.tsx bracketed-glyph + class rename` — FOUND
- `71fdfa0` — `test(213.1-02): rewrite ui-s4-a11y.test.js for token-cited DESIGN.md shape` — FOUND

**Phase 203 preservation — verified by test execution:**
- `test/webhooks/layout-shell-banner.test.js` — 7/7 pass (UNTOUCHED across all 3 task commits)

All success criteria met. Plan 213.1-02 complete.
