---
phase: 213.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(markos)/layout-shell.module.css
  - app/(markos)/layout-shell.tsx
  - app/(markos)/_components/NavList.tsx
  - app/(markos)/layout.stories.tsx
autonomous: true
requirements:
  - AC#1
  - AC#2
  - AC#3
  - AC#5
  - AC#7
  - AC#8
  - AC#11
  - AC#12
  - AC#13
  - AC#14
must_haves:
  truths:
    - "layout-shell.module.css contains zero inline hex literals (only `var(--color-*)` + canonical alpha-tint formulas)"
    - "layout-shell.module.css contains zero off-grid px values (only 1px borders allowed)"
    - "layout-shell.module.css uses zero gradients, zero drop-shadows, zero `transform: translate*` rules"
    - "layout-shell.module.css references zero `Sora` / `Space Grotesk` fonts and zero teal hex (`#0d9488`/`#0f766e`/`#14b8a6`/`#e6fffb`)"
    - "layout-shell.tsx composes `.c-sidebar`, `.c-card`, `.c-card--feature`, `.c-chip`, `.c-chip--mint`, `.c-chip-protocol`, `.c-nav-link`, `.t-lead`, `.t-label-caps` primitives"
    - "Active nav state is wired via `usePathname()` + `aria-current=\"page\"` in NavList client subcomponent"
    - "Existing `app/(markos)/layout.stories.tsx` no longer references deleted classes (`heroCard`, `previewTitle`, `previewText`, `eyebrow`)"
    - "`MarkOSLayoutShell` remains a server component (no `'use client'` added at the layout-shell.tsx top level — only NavList.tsx is client)"
    - "Denied-state body copy reads `Unable to establish tenant context. Sign in again.` (no `Please` softener) and eyebrow reads `[err] Protected workspace route`"
    - "`npm run build` exits 0; `npm run build-storybook` exits 0 after this plan"
  artifacts:
    - path: "app/(markos)/layout-shell.module.css"
      provides: "Token-only chrome shell module — local classes consume `var(--color-*)`, `var(--space-*)`, `var(--radius-*)`, `var(--font-*)`, `var(--w-container)`, `var(--w-sidebar)`, `var(--w-modal)` exclusively"
      contains: "var(--color-surface)"
    - path: "app/(markos)/layout-shell.tsx"
      provides: "JSX surface composing `.c-*` primitives + `.t-*` utilities; renders `<NavList />` for active nav"
      contains: "c-card"
    - path: "app/(markos)/_components/NavList.tsx"
      provides: "Client subcomponent reading `usePathname()` + emitting `aria-current=\"page\"` on the matching `.c-nav-link`"
      contains: "'use client'"
    - path: "app/(markos)/layout.stories.tsx"
      provides: "Updated Storybook coverage of layout-shell (TenantResolved + AccessDenied) using composed `.c-*` primitives"
      contains: "c-card"
  key_links:
    - from: "app/(markos)/layout-shell.tsx"
      to: "app/(markos)/_components/NavList.tsx"
      via: "default import + render in <aside>"
      pattern: "import\\s+NavList\\s+from\\s+['\"]\\./_components/NavList['\"]"
    - from: "app/(markos)/_components/NavList.tsx"
      to: "next/navigation usePathname"
      via: "named import + ternary aria-current"
      pattern: "usePathname\\(\\)"
    - from: "app/(markos)/layout-shell.module.css"
      to: "app/tokens.css custom properties"
      via: "var(--*) consumption only — no inline hex"
      pattern: "var\\(--color-surface\\)"
---

<objective>
Redesign `app/(markos)/layout-shell.module.css` from the legacy light-mode/teal/Sora/gradient/28px-radius/drop-shadow chrome to the canonical DESIGN.md token system (Kernel Black surface, Protocol Mint signal, JetBrains Mono headings inherited globally, strict 8px grid, 1px Border Mist hairlines, no gradients, no drop-shadows, no hover-translate jiggle). Update `layout-shell.tsx` className-side in lockstep with the CSS rewrite per UI-SPEC.md "Component primitive reuse table" — composing `.c-sidebar`, `.c-card`, `.c-card--feature`, `.c-chip`, `.c-chip--mint`, `.c-chip-protocol`, `.c-nav-link`, `.t-lead`, `.t-label-caps` directly. Extract a new `app/(markos)/_components/NavList.tsx` client subcomponent so `usePathname()` can wire `aria-current="page"` on the active link without converting `MarkOSLayoutShell` to a client component (per RESEARCH.md Pitfall 2 + R3). Update existing `app/(markos)/layout.stories.tsx` so it composes the new primitive shapes (deletes `styles.heroCard` / `styles.previewTitle` / `styles.previewText` / `styles.eyebrow` references — per RESEARCH.md "Open Question #2: update layout.stories.tsx in Plan-01"). Strip "Please" softener from denied-state body copy (UI-SPEC AC#5) and prepend `[err]` bracketed-glyph prefix to denied-state eyebrow (UI-SPEC AC#9). DOM tree + prop interface of `MarkOSLayoutShell` and `MarkOSAccessDeniedState` remain UNCHANGED (CONTEXT.md D-08).

Purpose: Close UI-SPEC.md acceptance criteria #1, #2, #3, #5, #7, #8, #11, #12, #13, #14 for the layout-shell scope; ship a build-clean, storybook-clean, atomic-committable layer-0 of Wave 1.
Output: Wholesale redesigned `layout-shell.module.css` (~80 LOC of token-cited rules vs. legacy 187 LOC), refactored `layout-shell.tsx` (no DOM/prop changes — class-name + small NavList subcomponent extraction), new `NavList.tsx` client subcomponent (~25 LOC), updated `layout.stories.tsx` (composing new primitive shapes).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/213.1-ui-canon-adoption-wave-1-chrome-layout-shell-rotationgracebanner-to-design-md-tokens/213.1-CONTEXT.md
@.planning/phases/213.1-ui-canon-adoption-wave-1-chrome-layout-shell-rotationgracebanner-to-design-md-tokens/213.1-UI-SPEC.md
@.planning/phases/213.1-ui-canon-adoption-wave-1-chrome-layout-shell-rotationgracebanner-to-design-md-tokens/213.1-RESEARCH.md
@.planning/phases/213.1-ui-canon-adoption-wave-1-chrome-layout-shell-rotationgracebanner-to-design-md-tokens/213.1-VALIDATION.md
@DESIGN.md
@app/tokens.css
@app/globals.css
@styles/components.css
@app/(markos)/layout-shell.module.css
@app/(markos)/layout-shell.tsx
@app/(markos)/layout.stories.tsx
@app/(markos)/_components/RotationBannerMount.tsx
@CLAUDE.md

<interfaces>
<!-- Key types and contracts the executor needs. Do not re-derive from codebase exploration. -->

From `app/(markos)/layout-shell.tsx` (current — interface MUST be preserved per CONTEXT.md D-08):
```typescript
export function MarkOSAccessDeniedState(): JSX.Element;
export function MarkOSLayoutShell(props: Readonly<{
  tenantId: string;
  children: ReactNode;
}>): JSX.Element;
```

From `styles/components.css` (consumed primitives — verbatim signatures):
```
.c-sidebar       — width: var(--w-sidebar); background: var(--color-surface); border-right: 1px solid var(--color-border); padding: var(--space-md); display: flex; flex-direction: column; gap: var(--space-lg);
.c-card          — background: var(--color-surface-raised); border: 1px solid var(--color-border); border-radius: var(--radius-lg); padding: var(--space-md);
.c-card--feature — border-radius: var(--radius-xl); padding: var(--space-lg);
.c-chip          — display: inline-flex; align-items: center; gap: var(--space-xxs); height: 24px; padding: 4px 10px; border-radius: var(--radius-full); font-size: var(--fs-body-sm); font-weight: var(--fw-medium); line-height: 1;
.c-chip--mint    — background: var(--color-primary-subtle); color: var(--color-primary);
.c-chip-protocol — display: inline-flex; align-items: center; height: 20px; padding: 2px 6px; background: var(--color-surface-raised); color: var(--color-primary); font-family: var(--font-mono); font-size: var(--fs-code); border-radius: var(--radius-sm);
.c-nav-link      — display: inline-flex; align-items: center; gap: var(--space-xs); padding: 8px 12px; border-radius: var(--radius-sm); background: transparent; color: var(--color-on-surface-muted); font-size: var(--fs-body-md);
.c-nav-link[aria-current="page"] — background: var(--color-primary-subtle); color: var(--color-primary);
```

From `app/globals.css` (utilities — consumed via `className=`):
```
.t-lead       — font-family: var(--font-sans); font-size: var(--fs-lead); font-weight: var(--fw-regular); line-height: var(--lh-lead); letter-spacing: var(--ls-lead); color: var(--color-on-surface-muted);
.t-label-caps — font-family: var(--font-sans); font-size: var(--fs-label); font-weight: var(--fw-semibold); line-height: var(--lh-label); letter-spacing: var(--ls-label); text-transform: uppercase; font-feature-settings: "ss01" 1; color: var(--color-on-surface-muted);
```

From `app/tokens.css` (tokens consumed — verbatim names; do NOT invent):
```
--color-surface, --color-surface-raised, --color-border, --color-on-surface, --color-on-surface-muted, --color-primary, --color-primary-subtle, --color-error
--space-xxs, --space-xs, --space-sm, --space-md, --space-lg, --space-xl
--radius-sm, --radius-md, --radius-lg, --radius-xl, --radius-full
--font-sans, --font-mono, --fs-h1, --fs-h2, --fs-body-md, --fs-body-sm, --fs-label, --fs-code, --fw-bold, --fw-semibold, --fw-medium, --fw-regular, --lh-h1, --lh-h2, --ls-h1, --ls-h2
--w-container (1280px), --w-sidebar (240px), --w-modal (560px)
```

Phase 203 wiring contract (`test/webhooks/layout-shell-banner.test.js`) — MUST stay green; this plan does NOT touch banner JSX:
- `import RotationBannerMount` — preserved
- `<RotationBannerMount />` rendered inside `MarkOSLayoutShell.content` slot — preserved
- No `'use client'` added to `layout-shell.tsx` top level — preserved
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create NavList.tsx client subcomponent (Wave 0 — interface contract for Task 2)</name>
  <read_first>
    - app/(markos)/layout-shell.tsx (current NAV_ITEMS array — must be moved verbatim)
    - styles/components.css lines 194–212 (`.c-nav-link` + `[aria-current="page"]` selector)
    - .planning/phases/213.1-ui-canon-adoption-wave-1-chrome-layout-shell-rotationgracebanner-to-design-md-tokens/213.1-RESEARCH.md "Pitfall 2" + "Risk Register R3" + "Code Examples > Active nav with usePathname()"
    - .planning/phases/213.1-ui-canon-adoption-wave-1-chrome-layout-shell-rotationgracebanner-to-design-md-tokens/213.1-UI-SPEC.md AC#8 (active nav state wiring)
  </read_first>
  <files>app/(markos)/_components/NavList.tsx</files>
  <action>
Create new file `app/(markos)/_components/NavList.tsx` (~30 LOC). Exact contents (executor: copy this skeleton; do NOT invent variations):

```tsx
'use client';

import { usePathname } from 'next/navigation';

const NAV_ITEMS = [
  { href: "/markos", label: "Dashboard", route: "dashboard" },
  { href: "/markos/operations", label: "Operations", route: "operations" },
  { href: "/markos/crm", label: "CRM", route: "crm" },
  { href: "/markos/company", label: "Company", route: "company" },
  { href: "/markos/mir", label: "MIR", route: "mir" },
  { href: "/markos/msp", label: "MSP", route: "msp" },
  { href: "/markos/icps", label: "ICPs", route: "icps" },
  { href: "/markos/segments", label: "Segments", route: "segments" },
  { href: "/markos/campaigns", label: "Campaigns", route: "campaigns" },
  { href: "/markos/settings/theme", label: "Settings", route: "settings" },
  { href: "/settings/mcp", label: "MCP", route: "settings-mcp" },
  { href: "/settings/webhooks", label: "Webhooks", route: "settings-webhooks" },
] as const;

export default function NavList() {
  const pathname = usePathname();
  return (
    <nav aria-label="MarkOS primary navigation">
      <ul style={{ listStyle: 'none', margin: 0, padding: 0, display: 'flex', flexDirection: 'column', gap: 'var(--space-xs)' }}>
        {NAV_ITEMS.map((item) => (
          <li key={item.href}>
            <a
              className="c-nav-link"
              href={item.href}
              aria-current={pathname === item.href ? "page" : undefined}
            >
              {item.label}
            </a>
          </li>
        ))}
      </ul>
    </nav>
  );
}
```

Reasons for this exact shape (do NOT modify):
- `'use client'` directive at line 1 — required for `usePathname()` (RESEARCH.md Pitfall 2).
- NAV_ITEMS array copied VERBATIM from existing `layout-shell.tsx` lines 6–19 — same hrefs, same labels, same order.
- Inline `style` prop on `<ul>` consuming `var(--space-xs)` is acceptable here because the NAV list spacing does not warrant a module.css for one rule (RESEARCH.md states clsx is not a dep; the alternative would be a 4-line `.module.css` for one gap rule).
- `aria-label="MarkOS primary navigation"` provides semantic anchoring for screen readers (WCAG 2.4.6 — heading/label).
- `aria-current={pathname === item.href ? "page" : undefined}` — exactly the conditional shape the `.c-nav-link[aria-current="page"]` selector in `styles/components.css:209` styles. Do NOT use `aria-current="false"` — the WCAG-compliant absence is `undefined` (which omits the attribute entirely).
- `className="c-nav-link"` — composes the global primitive directly; no module.css class.

After creating, do NOT yet modify `layout-shell.tsx` to import this — Task 2 wires the consumption.
  </action>
  <verify>
    <automated>test -f "app/(markos)/_components/NavList.tsx" && grep -q "'use client'" "app/(markos)/_components/NavList.tsx" && grep -q "usePathname" "app/(markos)/_components/NavList.tsx" && grep -q 'aria-current={pathname === item.href ? "page" : undefined}' "app/(markos)/_components/NavList.tsx" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - File `app/(markos)/_components/NavList.tsx` exists.
    - Line 1 contains `'use client';` directive (verified by `head -n 1 app/\(markos\)/_components/NavList.tsx`).
    - File contains `import { usePathname } from 'next/navigation';`.
    - File contains `aria-current={pathname === item.href ? "page" : undefined}` (literal — not `?? undefined`, not `: false`).
    - NAV_ITEMS array has exactly 12 entries matching the existing `layout-shell.tsx` array (`grep -c '{ href: "' app/\(markos\)/_components/NavList.tsx` returns 12).
    - File uses `className="c-nav-link"` (string literal, not module-style import).
    - `npm run build` exits 0 (compiles without TypeScript or runtime errors).
  </acceptance_criteria>
  <done>NavList.tsx exists as a 'use client' component using `usePathname()` + `aria-current="page"` ternary on `.c-nav-link`. `npm run build` exits 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite layout-shell.module.css to DESIGN.md tokens (zero hex, zero gradients, zero drop-shadows, zero off-grid px)</name>
  <read_first>
    - app/(markos)/layout-shell.module.css (current 187 LOC — full file, before rewriting)
    - .planning/phases/213.1-ui-canon-adoption-wave-1-chrome-layout-shell-rotationgracebanner-to-design-md-tokens/213.1-UI-SPEC.md sections "Component primitive reuse table > layout-shell.module.css" + "Color > Forbidden values" + "Spacing Scale > Exceptions"
    - .planning/phases/213.1-ui-canon-adoption-wave-1-chrome-layout-shell-rotationgracebanner-to-design-md-tokens/213.1-RESEARCH.md "Class-Rename Plan > app/(markos)/layout-shell.module.css → layout-shell.tsx" table
    - app/tokens.css (tokens consumed; do NOT invent new ones)
    - styles/components.css (primitives — `.c-sidebar`, `.c-card`, `.c-nav-link`, etc.)
    - app/globals.css lines 70–94 (typography base) + 121–125 (focus-visible) + 175–184 (reduced-motion)
    - DESIGN.md "Colors > Forbidden color usage" + "Elevation & Depth: We prefer borders over shadows" + "Motion > Forbidden motion"
  </read_first>
  <files>app/(markos)/layout-shell.module.css</files>
  <action>
Replace the **entire contents** of `app/(markos)/layout-shell.module.css` with the token-cited rewrite below. Do NOT preserve any legacy lines. The file shrinks from 187 LOC to ~75 LOC. Filename + import path unchanged.

Final file contents (executor: copy verbatim):

```css
/* Phase 213.1 Plan-01 — Token-only chrome shell.
 * Every value cites DESIGN.md via app/tokens.css var(--*) tokens or
 * composes a primitive from styles/components.css. No inline hex, no
 * off-grid px (only 1px border accent allowed), no gradients, no
 * drop-shadows, no hover-translate jiggle.
 *
 * Primitives composed JSX-side (see layout-shell.tsx):
 *   .c-sidebar (replaces legacy .sidebar)
 *   .c-card / .c-card--feature (replaces .heroCard / .contentCard)
 *   .c-chip + .c-chip--mint (replaces .tenantPill)
 *   .c-chip-protocol (wraps {tenantId})
 *   .c-nav-link (replaces .navLink — wired via NavList.tsx + usePathname())
 *   .t-lead (replaces .brandText)
 *   .t-label-caps (replaces .eyebrow base — overridden with --color-error
 *                  for the denied-state context via local class)
 */

.page {
  min-height: 100dvh;
  background: var(--color-surface);
  padding: var(--space-md);
}

.shell {
  max-width: var(--w-container);
  margin: 0 auto;
  display: grid;
  grid-template-columns: var(--w-sidebar) minmax(0, 1fr);
  gap: var(--space-md);
}

.brandLockup {
  display: flex;
  flex-direction: column;
  gap: var(--space-xs);
  margin-bottom: var(--space-lg);
}

.tenantLabel {
  font-size: var(--fs-body-sm);
  color: var(--color-on-surface-muted);
  font-weight: var(--fw-medium);
}

.content {
  display: flex;
  flex-direction: column;
  gap: var(--space-md);
}

.heroCardLocal {
  /* Composed JSX-side with `c-card c-card--feature`; this local class is
   * intentionally empty (kept for grep-shape parity with stories that may
   * still attach styles.heroCardLocal for layout overrides in later waves). */
}

.previewTextLocal {
  color: var(--color-on-surface-muted);
  max-width: 75ch;
}

.deniedPage {
  min-height: 100dvh;
  display: grid;
  place-items: center;
  padding: var(--space-md);
  background: var(--color-surface);
}

.deniedCardLocal {
  /* Composed JSX-side with `c-card`; this local class supplies the modal
   * width override + larger focal-card padding override. */
  width: min(var(--w-modal), 100%);
  padding: var(--space-lg);
}

.deniedEyebrow {
  /* Composed JSX-side with `t-label-caps`; this local class overrides the
   * label-caps base color to error-red for the denied context (per UI-SPEC
   * AC#9). Bracketed-glyph prefix `[err]` is added in JSX text. */
  color: var(--color-error);
  margin-bottom: var(--space-xs);
}

.deniedTitleLocal {
  /* <h1> base supplies font-family/font-size/font-weight/line-height from
   * globals.css; this only overrides color to error. */
  color: var(--color-error);
  margin: 0;
}

.deniedTextLocal {
  color: var(--color-on-surface-muted);
  margin-top: var(--space-sm);
}

@media (max-width: 1023.98px) {
  .page,
  .deniedPage {
    padding: var(--space-sm);
  }

  .shell {
    grid-template-columns: 1fr;
  }
}
```

Class-name decision register (for Task 3 to consume):
- DELETED classes (do NOT keep in the new CSS): `.sidebar`, `.brandTitle`, `.brandText`, `.tenantPill`, `.nav`, `.navList`, `.navLink`, `.heroCard`, `.eyebrow` (replaced by `.deniedEyebrow` for denied state), `.previewTitle`, `.previewText` (replaced by `.previewTextLocal`), `.contentCard`, `.deniedCard` (replaced by `.deniedCardLocal`), `.deniedTitle` (replaced by `.deniedTitleLocal`), `.deniedText` (replaced by `.deniedTextLocal`).
- KEPT classes: `.page`, `.shell`, `.brandLockup`, `.tenantLabel`, `.content`, `.deniedPage`.
- NEW classes: `.heroCardLocal` (intentionally empty — JSX consumer composes `c-card c-card--feature`), `.previewTextLocal`, `.deniedCardLocal`, `.deniedEyebrow`, `.deniedTitleLocal`, `.deniedTextLocal`.

The `*Local` suffix prevents collision with the `c-*` primitive class names in the same `className=` attribute — the file consumer will write `className={`${styles.deniedCardLocal} c-card`}` etc.

**Forbidden patterns** (every one was verified present in legacy file; rewrite must purge ALL):
- ANY `#[0-9A-Fa-f]{3,8}` literal — count must be 0.
- ANY `linear-gradient(`, `radial-gradient(` — count must be 0.
- ANY `box-shadow:` (drop shadows) — count must be 0; chrome uses 1px border hairlines only.
- ANY `transform:\s*translate` — count must be 0.
- ANY `Sora` or `Space Grotesk` — count must be 0.
- ANY `font-weight:\s*[0-9]` literal — count must be 0; use `var(--fw-*)`.
- ANY `font-size:\s*[0-9.]+(rem|px)` literal — count must be 0; use `var(--fs-*)`.
- ANY off-grid px (`10px`, `12px`, `14px`, `18px`, `20px`, `22px`, `28px`, `36px`, `45px`) — count must be 0. Allowed: `1px` (none in this file — borders come from `.c-card`/`.c-sidebar` primitive), `2px` (none), `4px` (none).

After writing, verify the file is ~75 LOC (acceptable range: 60–90 LOC). Significantly larger means residual legacy rules.
  </action>
  <verify>
    <automated>grep -cE "#[0-9A-Fa-f]{3,8}" "app/(markos)/layout-shell.module.css" | grep -q "^0$" && grep -cE "linear-gradient|radial-gradient" "app/(markos)/layout-shell.module.css" | grep -q "^0$" && grep -cE "box-shadow" "app/(markos)/layout-shell.module.css" | grep -q "^0$" && grep -cE "transform:\s*translate" "app/(markos)/layout-shell.module.css" | grep -q "^0$" && grep -cE "Sora|Space Grotesk" "app/(markos)/layout-shell.module.css" | grep -q "^0$" && grep -q "var(--color-surface)" "app/(markos)/layout-shell.module.css" && grep -q "var(--w-container)" "app/(markos)/layout-shell.module.css" && grep -q "var(--w-sidebar)" "app/(markos)/layout-shell.module.css" && grep -q "max-width: 1023.98px" "app/(markos)/layout-shell.module.css" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - **AC#1 — Zero inline hex:** `grep -cE "#[0-9A-Fa-f]{3,8}" app/\(markos\)/layout-shell.module.css` returns `0`.
    - **AC#2 — Zero off-grid px:** `grep -nE ":[[:space:]]+[0-9]+px" app/\(markos\)/layout-shell.module.css` returns 0 lines (no px values in this file — the only px values come from primitives in `styles/components.css`).
    - **AC#11 — No Sora/Space Grotesk:** `grep -cE "Sora|Space Grotesk" app/\(markos\)/layout-shell.module.css` returns `0`.
    - **AC#12 — No teal hexes:** `grep -ciE "#0d9488|#0f766e|#14b8a6|#e6fffb" app/\(markos\)/layout-shell.module.css` returns `0`.
    - **AC#13 — No gradients, no drop-shadows:** `grep -ciE "gradient|box-shadow" app/\(markos\)/layout-shell.module.css` returns `0`.
    - **AC#14 — No translate jiggle:** `grep -cE "transform:\s*translate" app/\(markos\)/layout-shell.module.css` returns `0`.
    - File contains literal token references: `var(--color-surface)`, `var(--w-container)`, `var(--w-sidebar)`, `var(--w-modal)`, `var(--space-md)`, `var(--space-lg)`, `var(--space-sm)`, `var(--space-xs)`, `var(--color-error)`, `var(--color-on-surface-muted)` (each `grep -q` returns 0).
    - Mobile media query uses `max-width: 1023.98px` (per RESEARCH.md `< lg` semantic).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>layout-shell.module.css is rewritten to ~75 LOC of token-only CSS, zero hex, zero gradients, zero drop-shadows, zero off-grid px, zero forbidden fonts. `npm run build` passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update layout-shell.tsx + layout.stories.tsx in lockstep with class-name renames + denied-state copy revisions</name>
  <read_first>
    - app/(markos)/layout-shell.tsx (current 71 LOC — full file)
    - app/(markos)/layout.stories.tsx (current 55 LOC — full file)
    - app/(markos)/layout-shell.module.css (rewritten in Task 2 — class-name register)
    - .planning/phases/213.1-ui-canon-adoption-wave-1-chrome-layout-shell-rotationgracebanner-to-design-md-tokens/213.1-UI-SPEC.md "Copywriting Contract" rows for "Tenant chip outer label", "Eyebrow (denied state)", "Denied body" + AC#3, AC#5, AC#9
    - .planning/phases/213.1-ui-canon-adoption-wave-1-chrome-layout-shell-rotationgracebanner-to-design-md-tokens/213.1-RESEARCH.md "Pitfall 1: Renaming a class without updating every consumer" + "Code Examples > Composing primitive + local class"
    - app/(markos)/_components/NavList.tsx (created in Task 1 — import + render)
  </read_first>
  <files>app/(markos)/layout-shell.tsx, app/(markos)/layout.stories.tsx</files>
  <action>
**Step 3a — Replace `app/(markos)/layout-shell.tsx` entirely** with the version below. Filename + exports + prop interfaces unchanged. NO `'use client'` directive (server component preserved per CONTEXT.md D-08 + RESEARCH.md R3).

```tsx
import React, { type ReactNode } from "react";

import styles from "./layout-shell.module.css";
import RotationBannerMount from "./_components/RotationBannerMount";
import NavList from "./_components/NavList";

export function MarkOSAccessDeniedState() {
  return (
    <main className={styles.deniedPage}>
      <section className={`${styles.deniedCardLocal} c-card`}>
        <p className={`${styles.deniedEyebrow} t-label-caps`}>[err] Protected workspace route</p>
        <h1 className={styles.deniedTitleLocal}>Access Denied</h1>
        <p className={styles.deniedTextLocal}>
          Unable to establish tenant context. Sign in again.
        </p>
      </section>
    </main>
  );
}

export function MarkOSLayoutShell({
  tenantId,
  children,
}: Readonly<{
  tenantId: string;
  children: ReactNode;
}>) {
  return (
    <main className={styles.page}>
      <div className={styles.shell}>
        <aside className="c-sidebar">
          <div className={styles.brandLockup}>
            <h1>MarkOS</h1>
            <p className="t-lead">UI Control Plane</p>
            <div className="c-chip c-chip--mint">
              <span className={styles.tenantLabel}>tenant</span>
              <span className="c-chip-protocol">{tenantId}</span>
            </div>
          </div>
          <NavList />
        </aside>
        <section className={styles.content}>
          <RotationBannerMount />
          {children}
        </section>
      </div>
    </main>
  );
}
```

Diff register (for grep verification):
- Top-level imports: ADDED `import NavList from "./_components/NavList";` after `RotationBannerMount` import.
- `MarkOSAccessDeniedState`:
  - `<section className={styles.deniedCard}>` → `<section className={`${styles.deniedCardLocal} c-card`}>`
  - `<p className={styles.eyebrow}>Protected workspace route</p>` → `<p className={`${styles.deniedEyebrow} t-label-caps`}>[err] Protected workspace route</p>` (UI-SPEC AC#9 prefix + UI-SPEC AC#5 inheritance)
  - `<h1 className={styles.deniedTitle}>Access Denied</h1>` → `<h1 className={styles.deniedTitleLocal}>Access Denied</h1>`
  - `<p className={styles.deniedText}>Unable to establish tenant context. Please sign in again.</p>` → `<p className={styles.deniedTextLocal}>Unable to establish tenant context. Sign in again.</p>` ("Please" softener REMOVED per UI-SPEC AC#5)
- `MarkOSLayoutShell`:
  - `<aside className={styles.sidebar}>` → `<aside className="c-sidebar">`
  - `<h1 className={styles.brandTitle}>MarkOS</h1>` → `<h1>MarkOS</h1>` (typography inherited from `globals.css <h1>` base)
  - `<p className={styles.brandText}>UI Control Plane</p>` → `<p className="t-lead">UI Control Plane</p>`
  - `<div className={styles.tenantPill}>` → `<div className="c-chip c-chip--mint">`
  - `<span className={styles.tenantLabel}>Tenant context</span>` → `<span className={styles.tenantLabel}>tenant</span>` (UI-SPEC Copywriting Contract: "tenant" lowercase, single word)
  - `<span>{tenantId}</span>` → `<span className="c-chip-protocol">{tenantId}</span>` (UI-SPEC AC#3)
  - The entire `<nav className={styles.nav}><ul className={styles.navList}>{NAV_ITEMS.map…}</ul></nav>` block (lines 54–62 in legacy) is REPLACED with `<NavList />`.
- `<section className={styles.content}>...{children}</section>` — UNCHANGED (preserves Phase 203 wiring contract: `RotationBannerMount` is the first child of the content section).
- The `NAV_ITEMS` const in lines 6–19 of legacy is REMOVED (now lives in `NavList.tsx`).

**Step 3b — Replace `app/(markos)/layout.stories.tsx` entirely** with the version below. The file imports + Storybook meta interface preserved; only the in-story `LayoutStory` component's JSX is rewritten to use the new primitive shapes. The `styles.heroCard` / `styles.previewTitle` / `styles.previewText` / `styles.eyebrow` references are DELETED (those classes no longer exist).

```tsx
import React from "react";

import type { Meta, StoryObj } from "@storybook/react";
import { MarkOSAccessDeniedState, MarkOSLayoutShell } from "./layout-shell";
import styles from "./layout-shell.module.css";

function LayoutStory({
  tenantId,
  denied,
}: Readonly<{
  tenantId: string;
  denied?: boolean;
}>) {
  if (denied) {
    return <MarkOSAccessDeniedState />;
  }

  return (
    <MarkOSLayoutShell tenantId={tenantId}>
      <section className={`${styles.heroCardLocal} c-card c-card--feature`}>
        <p className="t-label-caps">Milestone workspace shell</p>
        <h2>Milestone UI Shell Preview</h2>
        <p className={styles.previewTextLocal}>
          Protected MarkOS shell with resolved tenant context and milestone navigation.
        </p>
      </section>
    </MarkOSLayoutShell>
  );
}

const meta = {
  title: "Routes/MarkOS Layout",
  component: LayoutStory,
  parameters: {
    layout: "fullscreen",
  },
} satisfies Meta<typeof LayoutStory>;

export default meta;

type Story = StoryObj<typeof meta>;

export const TenantResolved: Story = {
  args: {
    tenantId: "tenant-alpha-001",
    denied: false,
  },
};

export const AccessDenied: Story = {
  args: {
    tenantId: "tenant-alpha-001",
    denied: true,
  },
};
```

Diff register for stories file:
- Story body changes: `styles.heroCard` → `${styles.heroCardLocal} c-card c-card--feature`, `styles.eyebrow` → `t-label-caps`, `styles.previewTitle` → bare `<h2>`, `styles.previewText` → `styles.previewTextLocal`.
- Meta + Story exports: UNCHANGED (`TenantResolved`, `AccessDenied`, title `"Routes/MarkOS Layout"`, `parameters.layout: "fullscreen"`).

**Step 3c — Verify-grep before commit:**
```bash
grep -rn "styles\.\(heroCard\|previewTitle\|previewText\|eyebrow\|sidebar\|brandTitle\|brandText\|tenantPill\|nav\b\|navList\|navLink\|contentCard\|deniedCard\|deniedTitle\|deniedText\)\b" app/
```
Expected: ZERO matches. ANY match means a stale reference to a deleted class — fix before declaring task done. (Allowed surviving names: `styles.deniedCardLocal`, `styles.deniedTitleLocal`, `styles.deniedTextLocal`, `styles.heroCardLocal`, `styles.previewTextLocal`, `styles.deniedEyebrow`.)

**Step 3d — Verify Phase 203 wiring shape preserved:**
```bash
grep -q "RotationBannerMount" "app/(markos)/layout-shell.tsx"
grep -q "<RotationBannerMount" "app/(markos)/layout-shell.tsx"
! grep -q "'use client'" "app/(markos)/layout-shell.tsx"   # must succeed (no 'use client' directive at top of layout-shell.tsx)
```
  </action>
  <verify>
    <automated>grep -q 'NavList' "app/(markos)/layout-shell.tsx" && grep -q 'c-sidebar' "app/(markos)/layout-shell.tsx" && grep -q 'c-card' "app/(markos)/layout-shell.tsx" && grep -q 'c-chip c-chip--mint' "app/(markos)/layout-shell.tsx" && grep -q 'c-chip-protocol' "app/(markos)/layout-shell.tsx" && grep -q 't-lead' "app/(markos)/layout-shell.tsx" && grep -q 't-label-caps' "app/(markos)/layout-shell.tsx" && grep -q '\[err\] Protected workspace route' "app/(markos)/layout-shell.tsx" && grep -q 'Sign in again\.' "app/(markos)/layout-shell.tsx" && ! grep -qE 'styles\.(heroCard|previewTitle|previewText|eyebrow|sidebar|brandTitle|brandText|tenantPill|navLink|contentCard|deniedCard\b|deniedTitle\b|deniedText\b)' "app/(markos)/layout-shell.tsx" "app/(markos)/layout.stories.tsx" && ! grep -q 'Please sign in' "app/(markos)/layout-shell.tsx" && ! grep -q "'use client'" "app/(markos)/layout-shell.tsx" && grep -q 'RotationBannerMount' "app/(markos)/layout-shell.tsx" && grep -q 'heroCardLocal' "app/(markos)/layout.stories.tsx" && npm run build && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - **AC#3 — Composes `.c-*` primitives:** `grep -cE 'className="(c-|.*c-)' "app/(markos)/layout-shell.tsx"` returns ≥6 (c-sidebar, c-card, c-chip, c-chip--mint, c-chip-protocol, t-lead, t-label-caps).
    - **AC#5 — `Please` softener removed:** `grep -c "Please sign in" "app/(markos)/layout-shell.tsx"` returns `0`. `grep -c "Sign in again\." "app/(markos)/layout-shell.tsx"` returns ≥1.
    - **AC#7 — Inherits global focus + reduced-motion:** No new `outline:` or `prefers-reduced-motion` declarations added in `layout-shell.tsx` or stories file (these are not CSS — verified by grep returning 0 matches).
    - **AC#8 — Active nav state wired:** `grep -q 'usePathname' "app/(markos)/_components/NavList.tsx"` AND `grep -q 'aria-current' "app/(markos)/_components/NavList.tsx"` AND `grep -q '<NavList' "app/(markos)/layout-shell.tsx"` ALL pass.
    - **AC#9 — Bracketed-glyph prefix on denied-state eyebrow:** `grep -c '\[err\] Protected workspace route' "app/(markos)/layout-shell.tsx"` returns `1`.
    - **AC#13 — Banned lexicon scan:** `grep -ciE "synergy|leverage|empower|unlock|transform|revolutionize|supercharge|holistic|seamless|cutting-edge|innovative|game-changer|next-generation|world-class|best-in-class|reimagine|disrupt" "app/(markos)/layout-shell.tsx" "app/(markos)/_components/NavList.tsx"` returns `0`.
    - **No stale class references:** `grep -rnE "styles\.(heroCard|previewTitle|previewText|eyebrow|sidebar|brandTitle|brandText|tenantPill|navLink|contentCard|deniedCard\b|deniedTitle\b|deniedText\b)" app/` returns 0 matches (the bare class names without the `Local` suffix MUST be absent).
    - **Phase 203 wiring preserved:** `grep -c "RotationBannerMount" "app/(markos)/layout-shell.tsx"` returns ≥2 (one import, one JSX use). `grep -c "'use client'" "app/(markos)/layout-shell.tsx"` returns `0` (top-level shell stays server).
    - **NavList integration:** `grep -c "import NavList" "app/(markos)/layout-shell.tsx"` returns `1`. `grep -c "<NavList" "app/(markos)/layout-shell.tsx"` returns `1`.
    - **Stories file integrity:** `grep -c "heroCardLocal" "app/(markos)/layout.stories.tsx"` returns `1`. `grep -c "Routes/MarkOS Layout" "app/(markos)/layout.stories.tsx"` returns `1` (story title preserved).
    - `npm run build` exits 0.
    - `npm run build-storybook` exits 0 (existing layout.stories.tsx no longer references deleted classes).
  </acceptance_criteria>
  <done>layout-shell.tsx + layout.stories.tsx are updated in lockstep with the layout-shell.module.css class-name register from Task 2. Phase 203 banner wiring contract is preserved (`<RotationBannerMount />` still rendered, no `'use client'` at shell top level). Denied-state copy uses `[err]` prefix + no `Please` softener. Active nav is wired via `<NavList />`. Both `npm run build` and `npm run build-storybook` exit 0.</done>
</task>

</tasks>

<verification>
After all three tasks complete, run:

```bash
# AC#1 — zero inline hex in module.css
grep -cE "#[0-9A-Fa-f]{3,8}" "app/(markos)/layout-shell.module.css"
# Expected: 0

# AC#11 + AC#12 + AC#13 + AC#14 — forbidden patterns in module.css
grep -ciE "Sora|Space Grotesk|#0d9488|#0f766e|#14b8a6|#e6fffb|gradient|box-shadow|transform:\s*translate" "app/(markos)/layout-shell.module.css"
# Expected: 0

# AC#3 — primitive composition in tsx
grep -cE 'className="(c-|.*c-)|className=`\$\{styles\..*\}\s+(c-|t-)' "app/(markos)/layout-shell.tsx"
# Expected: ≥6

# AC#5 — softener removed
grep -c "Please sign in" "app/(markos)/layout-shell.tsx"
# Expected: 0

# AC#8 — active nav wired
grep -q "usePathname" "app/(markos)/_components/NavList.tsx" && grep -q '<NavList' "app/(markos)/layout-shell.tsx" && echo "AC#8 PASS"
# Expected: AC#8 PASS

# AC#9 — bracketed glyph on denied eyebrow
grep -c '\[err\] Protected workspace route' "app/(markos)/layout-shell.tsx"
# Expected: 1

# Phase 203 wiring preserved
grep -c "RotationBannerMount" "app/(markos)/layout-shell.tsx"
# Expected: ≥2

# Build clean
npm run build && echo "BUILD PASS"
# Expected: BUILD PASS

# Storybook build clean (no references to deleted classes)
npm run build-storybook && echo "STORYBOOK PASS"
# Expected: STORYBOOK PASS

# No stale class references
grep -rnE "styles\.(heroCard|previewTitle|previewText|eyebrow|sidebar|brandTitle|brandText|tenantPill|navLink|contentCard|deniedCard\b|deniedTitle\b|deniedText\b)" app/
# Expected: zero output
```
</verification>

<success_criteria>
- All 10 must_haves truths are verifiable by literal grep or build command.
- AC#1, AC#2, AC#3, AC#5, AC#7, AC#8, AC#11, AC#12, AC#13, AC#14 from UI-SPEC.md table close.
- Phase 203 wiring contract test (`test/webhooks/layout-shell-banner.test.js`) continues to pass unchanged (this plan does not touch banner JSX).
- `npm run build` exits 0.
- `npm run build-storybook` exits 0.
- Atomic commit shape: `feat(213.1): redesign layout-shell to DESIGN.md tokens + extract NavList client subcomponent`.
- Phase 213.1 Plan-02 + Plan-03 can begin (no shared file overlap; Plan-02 touches `RotationGraceBanner.*` + `test/webhooks/ui-s4-a11y.test.js`; Plan-03 touches stories + a11y test + components.css + preview.tsx).
</success_criteria>

<output>
After completion, create `.planning/phases/213.1-ui-canon-adoption-wave-1-chrome-layout-shell-rotationgracebanner-to-design-md-tokens/213.1-01-SUMMARY.md` documenting:
- Files modified (with LOC delta)
- Class-name rename register (deleted / kept / new)
- Phase 203 wiring contract preservation evidence (`RotationBannerMount` still rendered; no `'use client'` at shell top)
- AC# closures (1, 2, 3, 5, 7, 8, 11, 12, 13, 14)
- Build + storybook-build verification commands and outcomes
- Hand-off to Plan-02 (banner) + Plan-03 (stories/a11y/components.css)
</output>
