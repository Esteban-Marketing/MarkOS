# Phase 213.1: UI Canon Adoption Wave 1 — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `213.1-CONTEXT.md` — this log preserves the alternatives considered.

**Date:** 2026-04-27
**Phase:** 213.1-ui-canon-adoption-wave-1-chrome-layout-shell-rotationgracebanner-to-design-md-tokens
**Mode:** discuss (auto mode — system-level auto-execute active; UI-SPEC.md already locked all visual decisions, so discuss-phase here focuses on implementation strategy gray areas only)
**Areas discussed:** Test strategy · Wave structure · File handling · Class-name renaming · Commit cadence · Executor verification gates · DESIGN.md amendment timing · Touch-target enforcement scope

---

## Visual Contract (no discussion)

UI-SPEC.md (`213.1-UI-SPEC.md`, commit `1b79d01`) was approved by gsd-ui-checker with 7/7 dimensions PASS. Every visual decision (color, spacing, radius, typography, motion, focus, accessibility, copywriting) was locked there. Discuss-phase did NOT re-litigate any of those.

| Visual area | Locked source |
|---|---|
| Token-only authoring | UI-SPEC.md AC#1, AC#3, DESIGN.md "Implementation > CSS custom properties" |
| Component primitive reuse (21 layout-shell + 9 banner classes) | UI-SPEC.md "Component primitive reuse table" |
| Copywriting revisions | UI-SPEC.md AC#5, AC#9 |
| Bracketed-glyph state coding (`[warn]` / `[err]`) | UI-SPEC.md AC#9 + DESIGN.md "Color blindness" |
| WCAG 2.1 AA min / AAA on body+primary | UI-SPEC.md AC#11 + DESIGN.md "WCAG 2.1 contrast matrix" |
| Focus ring (2px solid `--color-primary`, 2px offset) | `app/globals.css` `:focus-visible` (line 121), inherited |
| `prefers-reduced-motion` collapse + kernel-pulse freeze | `app/tokens.css` (line 175) + `app/globals.css` (line 175), inherited |
| Forced-colors mode | `app/tokens.css` `@media (forced-colors: active)`, inherited |
| Single mint signal <5% composition | UI-SPEC.md "Color" section |
| Banned lexicon scan = 0 | UI-SPEC.md AC#13 + DESIGN.md "Don't" list |

---

## Test strategy

| Option | Description | Selected |
|---|---|---|
| A: Vitest-only | Migrate Phase 203 banner test to Vitest in this wave | |
| B: `node --test` only | Update existing Phase 203 grep-shape test in place; no Vitest | ✓ |
| C: Both | Add Vitest alongside `node --test` | |

**Choice:** B (node --test only).
**Reason:** v4.0.0 test-stack migration to Vitest is a separate concern (`.planning/codebase/TESTING.md`). This wave is too narrow to justify dragging in the migration. Update existing assertions; preserve `node --test` boundary.
**Auto-selection rationale:** Recommended-default heuristic. Existing test exists in `node --test` form. Smallest change to ship.

---

## Wave structure

| Option | Description | Selected |
|---|---|---|
| A: Single wave (3 plans) | layout-shell + RotationGraceBanner + Storybook/a11y in one wave | ✓ |
| B: Two waves (split shell vs banner) | Wave 1A layout-shell, Wave 1B banner + tests | |

**Choice:** A (single wave, 3 plans).
**Reason:** layout-shell and RotationGraceBanner are coupled (banner renders inside shell's content slot), share the same token surface, and split costs more context-switch than it saves in parallelism (no genuine parallelism — same files, same primitives).
**Auto-selection rationale:** Smaller plan-count + single executor wave is the default for tightly-coupled redesigns.

---

## File handling

| Option | Description | Selected |
|---|---|---|
| A: Replace contents in place; keep filenames | `layout-shell.module.css` keeps name; contents fully rewritten | ✓ |
| B: New file + redirect | `layout-shell-v2.module.css` + import-path swap in `.tsx` | |
| C: Rename to `.css` (drop modules) | Migrate to global CSS now | |

**Choice:** A.
**Reason:** Filename-stable replacement is least churn. JSX import paths unchanged. Class names may rename per UI-SPEC's primitive-reuse table; consuming `.tsx` updated in same plan as the `.module.css` rewrite.
**Auto-selection rationale:** Lowest blast radius for downstream consumers (pages that import these modules don't need to change).

---

## JSX touch boundary

| Option | Description | Selected |
|---|---|---|
| A: Class-name updates + bracketed-glyph prefix only | No DOM tree changes, no prop changes | ✓ |
| B: Full JSX redesign | New component decomposition, prop API revision | |

**Choice:** A.
**Reason:** Phase 213.1 is a CSS canon adoption, not a React refactor. Visual primitive composition is achieved through CSS class rewiring; React surface is untouched. Pillar 6 (Brand consistency) doesn't require DOM changes.
**Auto-selection rationale:** Smallest blast radius. UI-SPEC.md does not specify React-side restructuring.

---

## Commit cadence

| Option | Description | Selected |
|---|---|---|
| A: Atomic commit per plan (3 commits) | Each plan committable independently | ✓ |
| B: Single squash commit | One commit at wave end | |

**Choice:** A.
**Reason:** GSD `commit_docs: true` is set; plans must commit independently per workflow. Each plan must leave the build green between commits — verified via `npm run build` + targeted `npm test` between plans.
**Auto-selection rationale:** GSD convention default.

---

## Executor verification gates (per plan)

| Gate | Required output |
|---|---|
| Inline hex grep | `grep -E "#[0-9a-fA-F]{3,8}" {scoped files}` returns 0 matches (zero inline hex) |
| Build | `npm run build` exits 0 |
| Phase 203 banner test | `npm test -- --test-name-pattern="layout-shell-banner"` exits 0 |
| Storybook a11y | `npm run test:ui-a11y` exits 0 (after Plan-03 lands stories+tests) |
| Off-grid spacing scan | `grep -nE "[^0-9]([0-9]+)px" {scoped files}` only hits 1px borders / 2px focus offsets / 4px banner border-inline-start (whitelist of documented exceptions) |

These mirror UI-SPEC.md acceptance criteria 1, 7, 9, 10, 11, 12, 13, 15 verbatim. Plan-XX acceptance criteria carry the exact commands.

---

## DESIGN.md amendment timing

| Option | Description | Selected |
|---|---|---|
| A: Land amendments now (block this phase) | DESIGN.md PR before any plan-phase work | |
| B: Defer until before 213.2 | Land in 213.1.1 follow-up or parallel track | ✓ |
| C: Skip / never land | Inline workarounds permanent | |

**Choice:** B.
**Reason:** UI-SPEC.md filed 5 amendment proposals, all marked **non-blocking** with documented inline workarounds. Wave 1 ships with workarounds; amendments land before Wave 2 (213.2) consumes them. gsd-ui-checker Recommendation D supports this routing.
**Auto-selection rationale:** Smallest schedule disruption.

---

## Touch-target enforcement scope (gsd-ui-checker Recommendation A)

| Option | Description | Selected |
|---|---|---|
| A: Land in 213.1 (this wave) | Add `@media (pointer: coarse)` rule to `.c-nav-link` in `styles/components.css` as part of Plan-03 | ✓ |
| B: Defer to 213.1.1 | Carve out follow-up phase | |
| C: Defer to 213.2+ | Add when first auth surface composes nav | |

**Choice:** A.
**Reason:** Promoting to a global rule in `styles/components.css` means every future `.c-nav-link` consumer (waves 213.2 / 213.3 / 213.4) inherits the touch-target spec without re-spec. In-scope, low-cost, high-leverage.
**Auto-selection rationale:** Highest leverage / lowest cost.

---

## Storybook + a11y test inclusion

| Option | Description | Selected |
|---|---|---|
| A: Stories + axe tests in this wave | Add `.stories.tsx` + `test/ui-a11y/` entries in Plan-03 | ✓ |
| B: Code only; defer stories/tests | Land redesign first, add visual regression later | |

**Choice:** A.
**Reason:** Promotes gsd-ui-checker Recommendation B (replace UI-SPEC's arithmetic contrast estimates with measured ratios). Locks Pillar 1 (Tokens) regression coverage. Chromatic visual baseline established now while the redesign is fresh.
**Auto-selection rationale:** Highest verification leverage; small marginal cost (~30 min for 2 stories + 2 axe assertions).

---

## Auto-Resolved (no Unclear items)

All gray areas above were resolved with Confident or Likely confidence. No `Unclear` assumptions required user correction. Auto mode proceeded directly to context capture.

## Folded Todos

`gsd-tools todo match-phase 213.1` returned 0 matches. Nothing folded.

## External Research

Not performed. UI-SPEC.md + DESIGN.md + repo codebase maps + gsd-ui-checker verdict provided sufficient evidence; no library-version or ecosystem-best-practice questions were open after deep_codebase_analysis.

---

*Phase: 213.1-ui-canon-adoption-wave-1-chrome-layout-shell-rotationgracebanner-to-design-md-tokens*
*Discussion log: 2026-04-27 (auto mode)*
