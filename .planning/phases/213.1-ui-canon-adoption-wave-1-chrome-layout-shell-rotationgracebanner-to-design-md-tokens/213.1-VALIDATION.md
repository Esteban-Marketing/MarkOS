---
phase: 213.1
slug: ui-canon-adoption-wave-1-chrome-layout-shell-rotationgracebanner-to-design-md-tokens
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-27
---

# Phase 213.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Bootstrapped from RESEARCH.md `## Validation Architecture` section. Planner must populate the per-task verification map after PLAN.md files are created.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `node --test` (built-in Node test runner) + Storybook 8 + Chromatic + axe-core |
| **Config file** | None for `node --test`; `.storybook/main.ts`, `.storybook/preview.tsx` for Storybook |
| **Quick run command** | `npm test -- test/webhooks/ui-s4-a11y.test.js test/ui-a11y/213-1-chrome-a11y.test.js` |
| **Full suite command** | `npm run test:ui-all` (build-storybook + test:ui-a11y + test:ui-security) |
| **Estimated runtime** | ~30s quick (2 test files) · ~3-5min full (Storybook build + axe + security) |

---

## Sampling Rate

- **After every task commit:** Run quick command (banner shape regression + chrome a11y)
- **After every plan wave:** Run full suite (`npm run test:ui-all`)
- **Before `/gsd-verify-work`:** Full suite must be green; `grep -E "#[0-9a-fA-F]{3,8}" app/(markos)/layout-shell.module.css app/(markos)/_components/RotationGraceBanner.module.css` MUST return 0 lines
- **Max feedback latency:** 30 seconds for quick command

---

## Per-Task Verification Map

> Planner: populate this table after PLAN.md files are written. Each task in each plan gets a row.

| Task ID | Plan | Wave | Requirement (UI-SPEC AC# / DESIGN.md rule) | Test Type | Automated Command | File Exists | Status |
|---------|------|------|---------------------------------------------|-----------|-------------------|-------------|--------|
| 213.1-01-01 | 01 | 1 | UI-SPEC AC#1 (zero inline hex in `layout-shell.module.css`) | grep | `grep -E "#[0-9a-fA-F]{3,8}" app/(markos)/layout-shell.module.css \| wc -l` (expect 0) | ✅ existing | ⬜ pending |
| 213.1-01-02 | 01 | 1 | UI-SPEC AC#7 (off-grid spacing eliminated; only documented exceptions) | grep | `grep -nE "[^0-9]([0-9]+)px" app/(markos)/layout-shell.module.css \| grep -vE ":\\s*1px\\s+(solid\\\|dashed)\\\|outline-offset:\\s*2px" \| wc -l` (expect 0) | ✅ existing | ⬜ pending |
| 213.1-01-03 | 01 | 1 | UI-SPEC AC#13 (banned lexicon scan) | grep | `grep -iE "synergy\|leverage\|empower\|unlock\|transform\|revolutionize\|supercharge\|holistic\|seamless\|cutting-edge\|innovative\|game-changer\|next-generation\|world-class\|best-in-class\|reimagine\|disrupt" app/(markos)/layout-shell.tsx app/(markos)/_components/NavList.tsx \| wc -l` (expect 0) | ✅ existing | ⬜ pending |
| 213.1-01-04 | 01 | 1 | Build clean | build | `npm run build` (expect exit 0) | ✅ existing | ⬜ pending |
| 213.1-01-05 | 01 | 1 | Existing layout.stories.tsx still builds (deleted classes purged) | storybook | `npm run build-storybook` (expect exit 0) | ✅ existing | ⬜ pending |
| 213.1-02-01 | 02 | 1 | UI-SPEC AC#1 (zero inline hex in `RotationGraceBanner.module.css`) | grep | `grep -E "#[0-9a-fA-F]{3,8}" app/(markos)/_components/RotationGraceBanner.module.css \| wc -l` (expect 0) | ✅ existing | ⬜ pending |
| 213.1-02-02 | 02 | 1 | UI-SPEC AC#9 (`[warn]`/`[err]` bracketed glyph prefix on banner `<strong>`) | grep | `grep -E "\\[(warn\|err)\\]" app/(markos)/_components/RotationGraceBanner.tsx \| wc -l` (expect ≥1) | ✅ existing | ⬜ pending |
| 213.1-02-03 | 02 | 1 | Phase 203 banner shape regression updated (per RESEARCH correction: `ui-s4-a11y.test.js`) | unit | `npm test -- test/webhooks/ui-s4-a11y.test.js` (expect exit 0) | ✅ existing | ⬜ pending |
| 213.1-02-04 | 02 | 1 | Build clean after banner change | build | `npm run build` (expect exit 0) | ✅ existing | ⬜ pending |
| 213.1-03-01 | 03 | 1 | New axe-core a11y suite for chrome | a11y | `npm run test:ui-a11y -- test/ui-a11y/213-1-chrome-a11y.test.js` (expect exit 0) | ❌ W0 | ⬜ pending |
| 213.1-03-02 | 03 | 1 | RotationGraceBanner.stories.tsx (4 states: T-7, T-1, T-0, multi) | storybook | `npm run build-storybook` (expect exit 0) | ❌ W0 | ⬜ pending |
| 213.1-03-03 | 03 | 1 | `(pointer: coarse)` rule added to `styles/components.css` `.c-nav-link` | grep | `grep -A1 "@media (pointer: coarse)" styles/components.css \| grep "padding-block: 11px"` (expect 1 hit) | ✅ existing | ⬜ pending |
| 213.1-03-04 | 03 | 1 | `app/globals.css` imported in `.storybook/preview.tsx` | grep | `grep "import.*app/globals.css" .storybook/preview.tsx` (expect ≥1) | ✅ existing | ⬜ pending |
| 213.1-03-05 | 03 | 1 | Legacy ThemeProvider gated behind `parameters.theme: "legacy"` opt-in | grep | `grep -A2 "parameters.theme" .storybook/preview.tsx \| grep "legacy"` (expect 1) | ✅ existing | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `test/ui-a11y/213-1-chrome-a11y.test.js` — new axe-core suite for layout-shell + RotationGraceBanner (Plan-03)
- [ ] `app/(markos)/_components/RotationGraceBanner.stories.tsx` — new Storybook stories with 4 states (Plan-03)
- [ ] `app/(markos)/_components/NavList.tsx` — new client component for `usePathname()` `aria-current` wiring (Plan-01)
- [ ] `.storybook/preview.tsx` — add `import "../app/globals.css"` + gate legacy ThemeProvider behind `parameters.theme === "legacy"` opt-in (Plan-03)

*All other infrastructure (`node --test`, Storybook 8, Chromatic, axe-core, axe-playwright) already installed per `package.json`.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Visual diff vs. legacy chrome | DESIGN.md "Atmosphere" / UI-SPEC composition rules | Subjective brand-feel cannot be automated | Compare Chromatic baseline (pre-redesign snapshot) vs. new build snapshot; reviewer confirms Kernel Black surface, mint-only signal, no gradients, ≥30% empty surface, JetBrains Mono headlines |
| `forced-colors: active` (Windows High Contrast) | DESIGN.md "Forbidden color usage" + globals.css forced-colors media query | Browser does not expose forced-colors mode through axe-core | Manual test: enable Windows High Contrast in OS, load `/markos` chrome, verify focus rings remain visible (`Highlight` system color), borders use `CanvasText`, no contrast collapse |
| Light-mode flip via `[data-theme="light"]` | DESIGN.md "Light-mode complements" + tokens.css light theme block | Storybook may not switch theme attribute correctly without toolbar wiring | Manual test: in browser devtools, set `<html data-theme="light">`, verify chrome flips to Lino Light surface; mint accent unchanged; no contrast issues; if toolbar wiring missing, file follow-up |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies (planner populates after PLAN.md written)
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (Plan-03 creates `213-1-chrome-a11y.test.js`, `RotationGraceBanner.stories.tsx`)
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s for quick command
- [ ] `nyquist_compliant: true` set in frontmatter (after planner verifies coverage)

**Approval:** pending (planner verifies after PLAN.md creation; checker confirms Dimension 8 in /gsd-plan-checker)
