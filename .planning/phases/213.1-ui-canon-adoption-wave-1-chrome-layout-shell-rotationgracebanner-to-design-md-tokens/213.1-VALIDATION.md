---
phase: 213.1
slug: ui-canon-adoption-wave-1-chrome-layout-shell-rotationgracebanner-to-design-md-tokens
status: planned
nyquist_compliant: true
wave_0_complete: false
created: 2026-04-27
plans:
  - 213.1-01-PLAN.md
  - 213.1-02-PLAN.md
  - 213.1-03-PLAN.md
---

# Phase 213.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Updated 2026-04-27 by /gsd-plan-phase: per-task verification map populated with real task IDs from PLAN.md files.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `node --test` (built-in Node test runner) + Storybook 8 + Chromatic + axe-core |
| **Config file** | None for `node --test`; `.storybook/main.ts`, `.storybook/preview.tsx` for Storybook |
| **Quick run command** | `npm test -- test/webhooks/ui-s4-a11y.test.js test/webhooks/layout-shell-banner.test.js test/ui-a11y/213-1-chrome-a11y.test.js` |
| **Full suite command** | `npm test && npm run build && npm run build-storybook` |
| **Estimated runtime** | ~30s quick (3 test files) · ~3-5min full (Next.js build + Storybook build + axe + security) |

---

## Sampling Rate

- **After every task commit:** Run quick command (banner shape regression + chrome a11y + Phase 203 wiring contract).
- **After every plan wave:** Run full suite (`npm test && npm run build && npm run build-storybook`).
- **Before `/gsd-verify-work`:** Full suite must be green; `grep -E "#[0-9a-fA-F]{3,8}" app/(markos)/layout-shell.module.css app/(markos)/_components/RotationGraceBanner.module.css` MUST return 0 lines.
- **Max feedback latency:** 30 seconds for quick command.

---

## Per-Task Verification Map

> Every task in every plan has either an automated `<verify>` command or a Wave 0 dependency. Sampling continuity verified: no 3 consecutive tasks lack automated verification.

| Task ID | Plan | Wave | Requirement (UI-SPEC AC#) | Test Type | Automated Command | File Exists | Status |
|---------|------|------|---------------------------|-----------|-------------------|-------------|--------|
| 213.1-01-T1 | 01 | 1 | AC#8 (Active nav state wiring — Wave 0 NavList client subcomponent) | grep | `test -f "app/(markos)/_components/NavList.tsx" && grep -q "usePathname" "app/(markos)/_components/NavList.tsx" && grep -q "aria-current" "app/(markos)/_components/NavList.tsx"` | ❌ W0 (created in this task) | ⬜ pending |
| 213.1-01-T2 | 01 | 1 | AC#1 + AC#2 + AC#11 + AC#12 + AC#13 + AC#14 (zero hex / zero off-grid / zero Sora / zero teal / zero gradients/shadows / zero translate) | grep + build | `grep -cE "#[0-9A-Fa-f]{3,8}" "app/(markos)/layout-shell.module.css" \| grep -q "^0$" && grep -ciE "Sora\|Space Grotesk\|gradient\|box-shadow\|transform:\s*translate" "app/(markos)/layout-shell.module.css" \| grep -q "^0$" && npm run build` | ✅ existing (rewritten in place) | ⬜ pending |
| 213.1-01-T3 | 01 | 1 | AC#3 + AC#5 + AC#7 + AC#8 + AC#9 + AC#13 (primitive composition + denied copy + global focus inheritance + active nav + bracketed glyph + banned lexicon) | grep + build + storybook-build | `grep -q '\[err\] Protected workspace route' "app/(markos)/layout-shell.tsx" && ! grep -q "Please sign in" "app/(markos)/layout-shell.tsx" && ! grep -qE "styles\\.(heroCard\|previewTitle\|previewText\|eyebrow\|sidebar\|brandTitle\|brandText\|tenantPill\|navLink\|contentCard)" "app/(markos)/layout-shell.tsx" "app/(markos)/layout.stories.tsx" && npm run build && npm run build-storybook` | ✅ existing (modified) | ⬜ pending |
| 213.1-02-T1 | 02 | 1 | AC#1 + AC#5 + AC#6 + AC#11 + AC#12 (banner CSS hex purge + no local focus override + no local reduced-motion + no Sora + no teal) | grep + build | `! grep -qE "#fef3c7\|#78350f\|#d97706\|#fef2f2\|#991b1b\|#dc2626\|#0d9488" "app/(markos)/_components/RotationGraceBanner.module.css" && grep -q "var(--color-warning)" "app/(markos)/_components/RotationGraceBanner.module.css" && grep -q "rgb(255 184 0 / 0.12)" "app/(markos)/_components/RotationGraceBanner.module.css" && ! grep -q "prefers-reduced-motion" "app/(markos)/_components/RotationGraceBanner.module.css" && npm run build` | ✅ existing (rewritten in place) | ⬜ pending |
| 213.1-02-T2 | 02 | 1 | AC#9 + AC#10 + class rename (bracketed glyph + Phase 203 wiring preserved + .pulseDot → .warningDot) | grep + unit | `grep -c '\[warn\]' "app/(markos)/_components/RotationGraceBanner.tsx" \| grep -q "^3$" && grep -c '\[err\]' "app/(markos)/_components/RotationGraceBanner.tsx" \| grep -q "^1$" && ! grep -q "pulseDot" "app/(markos)/_components/RotationGraceBanner.tsx" && grep -q "c-status-dot" "app/(markos)/_components/RotationGraceBanner.tsx" && npm test -- test/webhooks/layout-shell-banner.test.js` | ✅ existing (modified) | ⬜ pending |
| 213.1-02-T3 | 02 | 1 | AC#10b (Phase 203 banner content/CSS test rewritten with token-cited assertions + bracketed-glyph assertions) | unit | `npm test -- test/webhooks/ui-s4-a11y.test.js && npm test -- test/webhooks/layout-shell-banner.test.js` | ✅ existing (rewritten in place) | ⬜ pending |
| 213.1-03-T1 | 03 | 1 | AC#15 ((pointer: coarse) ≥44px on .c-nav-link in styles/components.css) | grep + build | `grep -c "@media (pointer: coarse)" "styles/components.css" \| grep -q "^1$" && grep -A2 "@media (pointer: coarse)" "styles/components.css" \| grep -q "padding-block: 11px" && npm run build` | ✅ existing (1 block inserted) | ⬜ pending |
| 213.1-03-T2 | 03 | 1 | RESEARCH.md R2 mitigation (Storybook preview imports DESIGN.md cascade + legacy ThemeProvider gated) | grep + storybook-build | `grep -c 'import\\s*[^"]*"\\.\\./app/globals\\.css"' ".storybook/preview.tsx" \| grep -q "^1$" && grep -q 'themeVariant !== "legacy"' ".storybook/preview.tsx" && npm run build-storybook` | ✅ existing (3-edit surgery) | ⬜ pending |
| 213.1-03-T3 | 03 | 1 | AC#4 (Storybook coverage of 5 banner states for addon-a11y axe runtime check) | storybook | `test -f "app/(markos)/_components/RotationGraceBanner.stories.tsx" && grep -cE "^export const (Empty\|T7Warning\|T1Warning\|T0Error\|MultiWarning):" "app/(markos)/_components/RotationGraceBanner.stories.tsx" \| grep -q "^5$" && npm run build-storybook` | ❌ W0 (created in this task) | ⬜ pending |
| 213.1-03-T4 | 03 | 1 | AC#3 + AC#9 + AC#11 + AC#12 + AC#13 + AC#15 (chrome grep-shape a11y suite covering all binding ACs) | a11y | `test -f "test/ui-a11y/213-1-chrome-a11y.test.js" && npm run test:ui-a11y -- test/ui-a11y/213-1-chrome-a11y.test.js` | ❌ W0 (created in this task) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

All Wave 0 (newly created) artifacts are paired with the task that creates them — no separate Wave 0 plan is needed because these creations are part of the dependency chain (NavList must exist before layout-shell.tsx imports it; RotationGraceBanner.stories.tsx must exist before the a11y test asserts file existence).

- [ ] `app/(markos)/_components/NavList.tsx` — created in Plan-01 Task 1 (213.1-01-T1).
- [ ] `app/(markos)/_components/RotationGraceBanner.stories.tsx` — created in Plan-03 Task 3 (213.1-03-T3).
- [ ] `test/ui-a11y/213-1-chrome-a11y.test.js` — created in Plan-03 Task 4 (213.1-03-T4).

The fourth Wave 0 mention from the original VALIDATION.md (`.storybook/preview.tsx` updates) is in-place modification, not a new file — handled in Plan-03 Task 2 (213.1-03-T2).

*All other infrastructure (`node --test`, Storybook 8, Chromatic, axe-core, axe-playwright) already installed per `package.json` — no install steps required.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Visual diff vs. legacy chrome | DESIGN.md "Atmosphere" / UI-SPEC composition rules | Subjective brand-feel cannot be automated | Compare Chromatic baseline (pre-redesign snapshot) vs. new build snapshot for `Routes/MarkOS Layout` (TenantResolved + AccessDenied) and `Components/RotationGraceBanner` (Empty/T7Warning/T1Warning/T0Error/MultiWarning) stories. Reviewer confirms Kernel Black surface, mint-only signal (<5% composition), no gradients, ≥30% empty surface, JetBrains Mono headlines. **R6 mitigation:** explicit human approval required before declaring phase done; `--exit-zero-on-changes` in `npm run chromatic` does NOT count as approval. |
| `forced-colors: active` (Windows High Contrast) | DESIGN.md "Forbidden color usage" + globals.css forced-colors media query | Browser does not expose forced-colors mode through axe-core or Chromatic | Manual test: enable Windows High Contrast in OS, load `/markos` chrome, verify focus rings remain visible (`Highlight` system color), borders use `CanvasText`, no contrast collapse. Repeat for `MarkOSAccessDeniedState` route. |
| Light-mode flip via `[data-theme="light"]` | DESIGN.md "Light-mode complements" + tokens.css light theme block | Storybook may not switch theme attribute correctly without toolbar wiring | Manual test: in browser devtools, set `<html data-theme="light">`, verify chrome flips to Lino Light surface; mint accent unchanged; no contrast issues. If toolbar wiring missing, file follow-up under Phase 213.4. |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify commands or are paired with Wave 0 file creation in the same task.
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (every task in every plan has `<verify><automated>...</automated></verify>`).
- [x] Wave 0 covers all MISSING references: NavList.tsx (Plan-01-T1), RotationGraceBanner.stories.tsx (Plan-03-T3), 213-1-chrome-a11y.test.js (Plan-03-T4).
- [x] No watch-mode flags in any verify command.
- [x] Feedback latency < 30s for quick command (3 test files, ~30s total).
- [x] `nyquist_compliant: true` set in frontmatter.

**Approval:** populated by `/gsd-plan-phase` 2026-04-27; pending checker confirmation in `/gsd-plan-checker`.
