---
phase: 213.1
plan: 03
type: execute
wave: 1
depends_on:
  - "213.1-01"
  - "213.1-02"
files_modified:
  - app/(markos)/_components/RotationGraceBanner.stories.tsx
  - test/ui-a11y/213-1-chrome-a11y.test.js
  - styles/components.css
  - .storybook/preview.tsx
autonomous: true
requirements:
  - AC#3
  - AC#4
  - AC#9
  - AC#11
  - AC#12
  - AC#13
  - AC#15
must_haves:
  truths:
    - "RotationGraceBanner.stories.tsx exists with 5 named exports covering Empty, T7Warning, T1Warning, T0Error, MultiWarning states"
    - "test/ui-a11y/213-1-chrome-a11y.test.js exists and asserts grep-shape coverage of UI-SPEC AC#3, AC#9, AC#11, AC#12, AC#15"
    - "styles/components.css contains a single `@media (pointer: coarse)` block scoping `padding-block: 11px` to `.c-nav-link` (UI-SPEC AC#15)"
    - ".storybook/preview.tsx imports `app/globals.css` so DESIGN.md tokens are in scope at story-render time (RESEARCH.md R2)"
    - ".storybook/preview.tsx legacy ThemeProvider decorator is gated behind `parameters.theme === \"legacy\"` opt-in (does not clobber redesigned-chrome stories)"
    - "`npm run test:ui-a11y -- test/ui-a11y/213-1-chrome-a11y.test.js` exits 0"
    - "`npm run build-storybook` exits 0 with the new RotationGraceBanner stories"
    - "Phase 203 banner shape test (`test/webhooks/layout-shell-banner.test.js`) and Phase 213.1 banner content test (`test/webhooks/ui-s4-a11y.test.js`) both continue to pass"
  artifacts:
    - path: "app/(markos)/_components/RotationGraceBanner.stories.tsx"
      provides: "Storybook 8 CSF3 coverage of banner states (Empty, T7Warning, T1Warning, T0Error, MultiWarning) — feeds Chromatic visual regression + addon-a11y axe-core runtime check (AC#4)"
      exports: ["default", "Empty", "T7Warning", "T1Warning", "T0Error", "MultiWarning"]
    - path: "test/ui-a11y/213-1-chrome-a11y.test.js"
      provides: "Phase 213.1 chrome a11y grep-shape suite — asserts story files exist, primitive composition, bracketed glyphs, token-only CSS, (pointer: coarse) rule, preview.tsx globals.css import"
      contains: "213.1 chrome a11y"
    - path: "styles/components.css"
      provides: "Existing primitive file extended with `(pointer: coarse)` rule on `.c-nav-link` for ≥44px touch-target compliance (UI-SPEC AC#15)"
      contains: "@media (pointer: coarse)"
    - path: ".storybook/preview.tsx"
      provides: "Updated preview with DESIGN.md token cascade (`app/globals.css` import) + legacy ThemeProvider gated behind `parameters.theme === \"legacy\"`"
      contains: "globals.css"
  key_links:
    - from: ".storybook/preview.tsx"
      to: "app/globals.css → app/tokens.css → styles/components.css"
      via: "top-of-file `import \"../app/globals.css\";`"
      pattern: "import\\s+['\"]\\.\\./app/globals\\.css['\"]"
    - from: "app/(markos)/_components/RotationGraceBanner.stories.tsx"
      to: "RotationGraceBanner.tsx default export"
      via: "Storybook CSF3 Meta + StoryObj"
      pattern: "import\\s+RotationGraceBanner\\s+from\\s+['\"]\\./RotationGraceBanner['\"]"
    - from: "test/ui-a11y/213-1-chrome-a11y.test.js"
      to: "all 6 source files (preview.tsx, stories, banner module.css/tsx, layout-shell.tsx, components.css)"
      via: "fs.readFileSync + assert.match grep-shape regex"
      pattern: "fs\\.readFileSync"
---

<objective>
Close the remaining UI-SPEC.md acceptance criteria for the chrome wave: ship Storybook coverage of the redesigned banner (`RotationGraceBanner.stories.tsx` with 5 named state stories), a grep-shape a11y test (`test/ui-a11y/213-1-chrome-a11y.test.js` covering AC#3, AC#9, AC#11, AC#12, AC#15 — the canonical pattern from `test/ui-a11y/accessibility.test.js`), the `(pointer: coarse)` ≥44px rule on `.c-nav-link` in `styles/components.css` (UI-SPEC AC#15), and the Storybook decorator surgery (`import "../app/globals.css";` + gate legacy `ThemeProvider` behind `parameters.theme === "legacy"` opt-in) that lets the redesigned chrome stories actually render against DESIGN.md tokens (RESEARCH.md R2). Plan-01 + Plan-02 already shipped the redesigned components; this plan validates them via Storybook + a11y test + verifies the global primitive's coarse-pointer compliance.

Purpose: Close UI-SPEC.md acceptance criteria #3, #4, #9, #11, #12, #13, #15; complete RESEARCH.md "Validation Architecture > Wave 0 Gaps"; mitigate RESEARCH.md Risk Register R2 (Storybook legacy decorator clobbers DESIGN.md tokens) + R6 (Chromatic auto-approve gap — explicit human-review acceptance line).
Output: New `RotationGraceBanner.stories.tsx` (~40 LOC, CSF3), new `test/ui-a11y/213-1-chrome-a11y.test.js` (~70 LOC, grep-shape), 4-line edit to `styles/components.css` (one `@media (pointer: coarse)` block), surgical edit to `.storybook/preview.tsx` (1 import line at top + `themeVariant` ternary changes to gate behind `parameters.theme === "legacy"` instead of `parameters.theme === "white-label"`).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/213.1-ui-canon-adoption-wave-1-chrome-layout-shell-rotationgracebanner-to-design-md-tokens/213.1-CONTEXT.md
@.planning/phases/213.1-ui-canon-adoption-wave-1-chrome-layout-shell-rotationgracebanner-to-design-md-tokens/213.1-UI-SPEC.md
@.planning/phases/213.1-ui-canon-adoption-wave-1-chrome-layout-shell-rotationgracebanner-to-design-md-tokens/213.1-RESEARCH.md
@.planning/phases/213.1-ui-canon-adoption-wave-1-chrome-layout-shell-rotationgracebanner-to-design-md-tokens/213.1-VALIDATION.md
@DESIGN.md
@app/tokens.css
@app/globals.css
@styles/components.css
@app/(markos)/_components/RotationGraceBanner.tsx
@app/(markos)/layout-shell.tsx
@app/(markos)/layout.stories.tsx
@.storybook/main.ts
@.storybook/preview.tsx
@test/ui-a11y/accessibility.test.js
@CLAUDE.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From `app/(markos)/_components/RotationGraceBanner.tsx` (consumed by stories — interface from Plan-02):
```typescript
export type RotationStage = 't-7' | 't-1' | 't-0' | 'normal';
export interface Rotation {
  id: string;
  subscription_id: string;
  url: string;
  grace_ends_at: string; // ISO timestamp
  stage: RotationStage;
}
export interface RotationGraceBannerProps {
  rotations?: Rotation[];
}
export default function RotationGraceBanner(props: RotationGraceBannerProps): JSX.Element | null;
```

From existing `app/(markos)/layout.stories.tsx` (Storybook 8 CSF3 pattern — mirror this shape):
```tsx
import type { Meta, StoryObj } from "@storybook/react";
const meta = {
  title: "...",
  component: ...,
  parameters: { layout: "fullscreen" | "padded" },
} satisfies Meta<typeof Component>;
export default meta;
type Story = StoryObj<typeof meta>;
export const VariantName: Story = { args: {...} };
```

From `styles/components.css` line 194-212 (existing `.c-nav-link` block — insertion point for AC#15 coarse-pointer rule):
```
.c-nav-link { ... padding: 8px 12px; ... }
.c-nav-link:hover { ... }
.c-nav-link.is-active,
.c-nav-link[aria-current="page"] { ... }
```
Insert the `@media (pointer: coarse) { .c-nav-link { padding-block: 11px; } }` rule IMMEDIATELY AFTER the `.c-nav-link[aria-current="page"]` block (line ~213) and BEFORE the `.c-topbar` block (line ~214).

From `.storybook/preview.tsx` line 33-76 (legacy ThemeProvider decorator — must be gated, NOT removed):
- Currently triggered for ALL stories via `globalDecorators` array on line 161.
- Decorator branches by `context.parameters.theme === "white-label"` (line 36) — change branching key to `context.parameters.theme === "legacy"` so default stories bypass the decorator's inline `backgroundColor: tokens["color.bg.canvas"]` override (line 66).
- The default value `theme: "default"` on line 227 — change to `theme: undefined` (or remove) so absence is the default; `parameters.theme: "legacy"` opt-in is what triggers the decorator's wrapper rendering.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add `(pointer: coarse)` ≥44px rule to .c-nav-link in styles/components.css (UI-SPEC AC#15)</name>
  <read_first>
    - styles/components.css lines 194-220 (current `.c-nav-link` + `.c-topbar` blocks)
    - .planning/phases/213.1-ui-canon-adoption-wave-1-chrome-layout-shell-rotationgracebanner-to-design-md-tokens/213.1-RESEARCH.md "(pointer: coarse) ≥44px Rule Placement" section
    - .planning/phases/213.1-ui-canon-adoption-wave-1-chrome-layout-shell-rotationgracebanner-to-design-md-tokens/213.1-UI-SPEC.md AC#15 + Rule 4 last bullet
    - DESIGN.md "Responsive Behavior > Touch vs. pointer" + "Touch targets" rule
  </read_first>
  <files>styles/components.css</files>
  <action>
Insert exactly ONE new CSS block in `styles/components.css` immediately after the `.c-nav-link[aria-current="page"]` rule (the `}` closing brace at approximately line 213) and BEFORE the existing `.c-topbar` block (line ~214). DO NOT modify any other rule in the file.

The new block:

```css
/* (pointer: coarse) ≥44px touch-target — DESIGN.md "Responsive Behavior · Touch vs. pointer" + "Touch targets · 44×44 minimum on (pointer: coarse)". Phase 213.1 UI-SPEC AC#15. Padding-block 11px lifts c-nav-link to ~44.6px (11 + 11 + 16 × 1.6 line-height). */
@media (pointer: coarse) {
  .c-nav-link { padding-block: 11px; }
}
```

Do not change `.c-nav-link`'s default `padding: 8px 12px` (DESIGN.md baseline for desktop pointer). The `(pointer: coarse)` overlay raises padding-block only when the user is on a coarse-pointer device (touch screen).

Verify positioning: the new block must appear AFTER the `.c-nav-link[aria-current="page"]` block and BEFORE `.c-topbar`. Use `grep -nE "@media \(pointer: coarse\)|\.c-topbar|\.c-nav-link\[aria-current" styles/components.css` to verify line ordering: `.c-nav-link[aria-current` line < `(pointer: coarse)` line < `.c-topbar` line.

Do NOT add the rule anywhere else (not in `app/globals.css`, not in `app/(markos)/layout-shell.module.css`). The whole point of placing it on the global primitive is that ALL future `.c-nav-link` consumers (admin nav, settings nav, marketing nav) inherit the coarse-pointer bump (RESEARCH.md "Why styles/components.css (not globals.css or layout-shell.module.css)").
  </action>
  <verify>
    <automated>grep -c "@media (pointer: coarse)" "styles/components.css" | grep -q "^1$" && grep -A2 "@media (pointer: coarse)" "styles/components.css" | grep -q "padding-block: 11px" && grep -A2 "@media (pointer: coarse)" "styles/components.css" | grep -q "\.c-nav-link" && ! grep -q "@media (pointer: coarse)" "app/(markos)/layout-shell.module.css" && ! grep -q "@media (pointer: coarse)" "app/(markos)/_components/RotationGraceBanner.module.css" && npm run build && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - **AC#15 — `(pointer: coarse)` rule placed on global primitive:** `grep -c "@media (pointer: coarse)" styles/components.css` returns `1`.
    - **Rule scopes to `.c-nav-link`:** `grep -A2 "@media (pointer: coarse)" styles/components.css | grep -c "\.c-nav-link"` returns ≥1.
    - **Padding-block value matches RESEARCH.md prescription:** `grep -A2 "@media (pointer: coarse)" styles/components.css | grep -c "padding-block: 11px"` returns `1`.
    - **Rule placed correctly (not in chrome-only files):** `grep -c "@media (pointer: coarse)" "app/(markos)/layout-shell.module.css"` returns `0`. `grep -c "@media (pointer: coarse)" "app/(markos)/_components/RotationGraceBanner.module.css"` returns `0`.
    - **Block ordering preserved:** the line number of the new `@media` block is greater than the line number of `.c-nav-link[aria-current="page"]` and less than the line number of `.c-topbar` (verify via `grep -nE "@media \(pointer: coarse\)|\.c-topbar|\.c-nav-link\[aria-current" styles/components.css`).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>One `@media (pointer: coarse) { .c-nav-link { padding-block: 11px; } }` block exists in `styles/components.css` between the `.c-nav-link[aria-current="page"]` rule and `.c-topbar`. No other CSS files contain this media query. `npm run build` passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update .storybook/preview.tsx to import app/globals.css + gate legacy ThemeProvider decorator behind `parameters.theme === "legacy"` opt-in</name>
  <read_first>
    - .storybook/preview.tsx (current 237 LOC — full file)
    - .storybook/main.ts (Storybook config — verify Vite framework + addon-a11y are loaded)
    - app/globals.css (the cascade root: imports tokens.css + components.css)
    - .planning/phases/213.1-ui-canon-adoption-wave-1-chrome-layout-shell-rotationgracebanner-to-design-md-tokens/213.1-RESEARCH.md "Storybook Readiness Assessment > Gaps that Plan-03 must close" + Pitfall 3 + Risk Register R2
  </read_first>
  <files>.storybook/preview.tsx</files>
  <action>
Make the following EXACT edits to `.storybook/preview.tsx`. Do not refactor unrelated code; do not change `RoleContextProvider`, `DeterministicFixtures`, `viewportConfig`, or `parameters.a11y`.

**Edit 1 — Add `import "../app/globals.css";` after the existing imports.** Insert as new line 5 (after `import { tokenMap, baseSemanticTokens } from "../lib/markos/theme/tokens";` on existing line 4):

Before (current lines 1-4):
```tsx
import type { Preview, Decorator, Parameters } from "@storybook/react";
import React from "react";
import { defaultBrandPack, mergeBrandPack } from "../lib/markos/theme/brand-pack";
import { tokenMap, baseSemanticTokens } from "../lib/markos/theme/tokens";
```

After (new lines 1-5):
```tsx
import type { Preview, Decorator, Parameters } from "@storybook/react";
import React from "react";
import { defaultBrandPack, mergeBrandPack } from "../lib/markos/theme/brand-pack";
import { tokenMap, baseSemanticTokens } from "../lib/markos/theme/tokens";
import "../app/globals.css"; // Phase 213.1 — DESIGN.md cascade (tokens.css + components.css) for stories.
```

This pulls `--color-surface` (Kernel Black), `--color-primary` (Protocol Mint), `--font-mono` (JetBrains Mono), the `.c-*` primitive set, and the global `<h1>`/`<p>`/`:focus-visible`/reduced-motion rules into Storybook's iframe `:root`.

**Edit 2 — Gate the `ThemeProvider` decorator's wrapper rendering behind `parameters.theme === "legacy"` opt-in.** Change the `ThemeProvider` decorator (currently at lines 33-76) so it only renders the legacy `<div style={{ ... backgroundColor: tokens["color.bg.canvas"], ... }}>` wrapper when a story explicitly opts in via `parameters.theme: "legacy"`.

Replace the existing `ThemeProvider` decorator function body (lines 33-76) with:

```tsx
type ThemeVariant = "default" | "legacy" | "white-label";

const ThemeProvider: Decorator = (Story, context) => {
  const themeVariant: ThemeVariant = context.parameters.theme || "default";

  // Phase 213.1 — default + future DESIGN.md-aligned stories bypass the
  // legacy lib/markos/theme/brand-pack wrapper. The wrapper is preserved
  // for stories that explicitly opt in via `parameters.theme: "legacy"`
  // or `parameters.theme: "white-label"` (legacy white-label preview path).
  if (themeVariant !== "legacy" && themeVariant !== "white-label") {
    return <Story />;
  }

  const brandPack =
    themeVariant === "white-label"
      ? {
          ...defaultBrandPack,
          tenantId: "white-label-tenant",
          label: "White-label Theme",
          overrides: {
            ...defaultBrandPack.overrides,
            "color.action.primary": "#8b5cf6",
            "color.action.primaryText": "#ffffff",
            "color.text.primary": "#1e1b4b",
            "color.bg.canvas": "#fafaf9",
          },
        }
      : defaultBrandPack;

  const tokens = mergeBrandPack(brandPack);

  return (
    <div
      style={{
        "--color-bg-canvas": tokens["color.bg.canvas"],
        "--color-bg-panel": tokens["color.bg.panel"],
        "--color-text-primary": tokens["color.text.primary"],
        "--color-text-muted": tokens["color.text.muted"],
        "--color-action-primary": tokens["color.action.primary"],
        "--color-action-primary-text": tokens["color.action.primaryText"],
        "--radius-md": tokens["radius.md"],
        "--space-md": tokens["space.md"],
        "--font-body": tokens["font.body"],
        "--font-display": tokens["font.display"],
        backgroundColor: tokens["color.bg.canvas"],
        color: tokens["color.text.primary"],
        fontFamily: tokens["font.body"],
        padding: "2rem",
        minHeight: "100vh",
      } as React.CSSProperties}
    >
      <Story />
    </div>
  );
};
```

The KEY change is the `if (themeVariant !== "legacy" && themeVariant !== "white-label")` early-return. Default stories now render `<Story />` directly with `app/globals.css` already supplying the body's `var(--color-surface)` background through `:root` cascade. Legacy stories continue to render inside the wrapper.

**Note (no Edit 3 needed):** The `theme: "default"` literal in `parameters` (around lines 195-229) is what the early-return in Edit 2 keys off of — the `default` literal is the bypass case. Existing white-label stories that set `parameters: { theme: "white-label" }` per-story continue to opt into the wrapper unchanged. No code change required to the default value; only the comment above it could be refreshed for documentation, but that is purely cosmetic and not a required edit.

DO NOT change:
- `RoleContextProvider` decorator (lines 84-112).
- `DeterministicFixtures` decorator (lines 117-155).
- `globalDecorators` order (line 161): `[DeterministicFixtures, ThemeProvider, RoleContextProvider]` — preserve.
- `viewportConfig` (lines 167-189).
- `parameters.a11y.config.rules` (lines 202-215) — preserve a11y addon config.
- `parameters.layout: "fullscreen"` (line 216).
- `parameters.testRunner.a11y.enabled: true` (lines 220-225).
- `parameters.role` (line 228).

After editing, manually scan `app/`, `lib/`, and `onboarding/` for any `*.stories.tsx` file that previously relied on the legacy wrapper as default behavior. If any story sets `parameters: { theme: "white-label" }`, leave unchanged (white-label is preserved). If any story relies on the wrapper without setting the opt-in (i.e., the existing 18 stories that previously got the wrapper by default) — that's the regression we're correcting. Those stories are now expected to render against DESIGN.md tokens (Kernel Black surface, Protocol Mint signal). Run `npm run build-storybook` and verify it compiles.
  </action>
  <verify>
    <automated>grep -c 'import\s*[^"]*"\.\./app/globals\.css"' ".storybook/preview.tsx" | grep -q "^1$" && grep -q 'themeVariant !== "legacy"' ".storybook/preview.tsx" && grep -q 'themeVariant !== "white-label"' ".storybook/preview.tsx" && grep -q "DeterministicFixtures, ThemeProvider, RoleContextProvider" ".storybook/preview.tsx" && grep -q "color-contrast" ".storybook/preview.tsx" && npm run build-storybook && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - **DESIGN.md cascade imported:** `grep -c 'import\s*[^"]*"\.\./app/globals\.css"' ".storybook/preview.tsx"` returns `1`.
    - **ThemeProvider gated by opt-in:** `grep -c 'themeVariant !== "legacy"' ".storybook/preview.tsx"` returns `1`. `grep -c 'themeVariant !== "white-label"' ".storybook/preview.tsx"` returns `1`.
    - **Decorator order preserved:** `grep -c "DeterministicFixtures, ThemeProvider, RoleContextProvider" ".storybook/preview.tsx"` returns `1`.
    - **a11y addon config preserved:** `grep -c "color-contrast" ".storybook/preview.tsx"` returns `1`. `grep -c "valid-aria-role" ".storybook/preview.tsx"` returns `1`.
    - **Storybook builds cleanly:** `npm run build-storybook` exits 0.
  </acceptance_criteria>
  <done>.storybook/preview.tsx imports `../app/globals.css` at top; ThemeProvider decorator returns `<Story />` directly unless `parameters.theme === "legacy"` or `"white-label"`. `npm run build-storybook` passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create RotationGraceBanner.stories.tsx (Storybook 8 CSF3 — Empty, T7Warning, T1Warning, T0Error, MultiWarning)</name>
  <read_first>
    - app/(markos)/_components/RotationGraceBanner.tsx (Plan-02 output — interface + bracketed-glyph copy)
    - app/(markos)/layout.stories.tsx (existing CSF3 reference shape)
    - .planning/phases/213.1-ui-canon-adoption-wave-1-chrome-layout-shell-rotationgracebanner-to-design-md-tokens/213.1-RESEARCH.md "Recommended Storybook integration approach (Plan-03)" + "Code Examples > Storybook story with multiple variants (CSF3)"
    - .planning/phases/213.1-ui-canon-adoption-wave-1-chrome-layout-shell-rotationgracebanner-to-design-md-tokens/213.1-UI-SPEC.md AC#4 (axe addon-a11y over Chromatic)
  </read_first>
  <files>app/(markos)/_components/RotationGraceBanner.stories.tsx</files>
  <action>
Create new file `app/(markos)/_components/RotationGraceBanner.stories.tsx` with the exact contents below. Do not invent additional stories beyond the 5 named exports.

```tsx
import type { Meta, StoryObj } from "@storybook/react";
import RotationGraceBanner, { type Rotation } from "./RotationGraceBanner";

const meta = {
  title: "Components/RotationGraceBanner",
  component: RotationGraceBanner,
  parameters: {
    layout: "padded",
  },
} satisfies Meta<typeof RotationGraceBanner>;

export default meta;

type Story = StoryObj<typeof meta>;

const baseRotation: Rotation = {
  id: "r1",
  subscription_id: "sub-001",
  url: "https://example.com/webhook",
  grace_ends_at: "2026-04-28T12:00:00Z",
  stage: "t-7",
};

export const Empty: Story = {
  args: { rotations: [] },
};

export const T7Warning: Story = {
  args: { rotations: [{ ...baseRotation, stage: "t-7" }] },
};

export const T1Warning: Story = {
  args: { rotations: [{ ...baseRotation, stage: "t-1" }] },
};

export const T0Error: Story = {
  args: { rotations: [{ ...baseRotation, stage: "t-0" }] },
};

export const MultiWarning: Story = {
  args: {
    rotations: [
      { ...baseRotation, id: "r1", stage: "t-7" },
      { ...baseRotation, id: "r2", subscription_id: "sub-002", stage: "t-1" },
    ],
  },
};
```

The `parameters.layout: "padded"` (not `"fullscreen"`) gives the banner room to render with realistic side gutters in the Storybook viewport. The `Rotation` type is imported from the component to ensure type safety on `args`.

The 5 named exports cover:
- `Empty` — no rotations → component returns null (renders nothing — useful for a11y addon to confirm no console errors on empty path).
- `T7Warning` — single rotation, T-7 stage → warn palette + `[warn]` prefix on `<strong>`.
- `T1Warning` — single rotation, T-1 stage → warn palette + `[warn]` prefix + `.warningDot` (composes `c-status-dot`).
- `T0Error` — single rotation, T-0 stage → error palette + `[err]` prefix.
- `MultiWarning` — 2 rotations → warn palette + `[warn]` prefix on aggregate count.

Do NOT set `parameters.theme: "legacy"` on any of these stories — they MUST render against DESIGN.md tokens (the bypass path enabled in Task 2).

After creating, run `npm run build-storybook`. The build must include the new stories (verify by checking the output for the `Components/RotationGraceBanner` title).
  </action>
  <verify>
    <automated>test -f "app/(markos)/_components/RotationGraceBanner.stories.tsx" && grep -q 'title: "Components/RotationGraceBanner"' "app/(markos)/_components/RotationGraceBanner.stories.tsx" && grep -q "export const Empty" "app/(markos)/_components/RotationGraceBanner.stories.tsx" && grep -q "export const T7Warning" "app/(markos)/_components/RotationGraceBanner.stories.tsx" && grep -q "export const T1Warning" "app/(markos)/_components/RotationGraceBanner.stories.tsx" && grep -q "export const T0Error" "app/(markos)/_components/RotationGraceBanner.stories.tsx" && grep -q "export const MultiWarning" "app/(markos)/_components/RotationGraceBanner.stories.tsx" && ! grep -q 'parameters.theme.*"legacy"' "app/(markos)/_components/RotationGraceBanner.stories.tsx" && npm run build-storybook && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - **File exists:** `test -f "app/(markos)/_components/RotationGraceBanner.stories.tsx"`.
    - **Storybook title:** `grep -c 'title: "Components/RotationGraceBanner"' "app/(markos)/_components/RotationGraceBanner.stories.tsx"` returns `1`.
    - **5 named exports:** `grep -cE "^export const (Empty|T7Warning|T1Warning|T0Error|MultiWarning):" "app/(markos)/_components/RotationGraceBanner.stories.tsx"` returns `5`.
    - **CSF3 shape:** `grep -c "satisfies Meta" "app/(markos)/_components/RotationGraceBanner.stories.tsx"` returns `1`. `grep -c "StoryObj<typeof meta>" "app/(markos)/_components/RotationGraceBanner.stories.tsx"` returns `1`.
    - **Type-safe args:** `grep -c "type Rotation" "app/(markos)/_components/RotationGraceBanner.stories.tsx"` returns `1` (imports the type from the component).
    - **No legacy theme opt-in:** `grep -c 'parameters.theme.*"legacy"' "app/(markos)/_components/RotationGraceBanner.stories.tsx"` returns `0`.
    - **Realistic fixture data:** `grep -c "subscription_id" "app/(markos)/_components/RotationGraceBanner.stories.tsx"` returns ≥1 (per `Rotation` interface).
    - **`npm run build-storybook` exits 0.**
  </acceptance_criteria>
  <done>RotationGraceBanner.stories.tsx exists with 5 named state stories. `npm run build-storybook` includes them and exits 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Create test/ui-a11y/213-1-chrome-a11y.test.js (grep-shape a11y suite for AC#3, AC#9, AC#11, AC#12, AC#15)</name>
  <read_first>
    - test/ui-a11y/accessibility.test.js (existing canonical pattern — mirror its `node:test` + `fs.readFileSync` + `assert.match` shape)
    - .planning/phases/213.1-ui-canon-adoption-wave-1-chrome-layout-shell-rotationgracebanner-to-design-md-tokens/213.1-RESEARCH.md "axe-core Test Harness Pattern > Recommended Plan-03 axe-core test file"
    - .planning/phases/213.1-ui-canon-adoption-wave-1-chrome-layout-shell-rotationgracebanner-to-design-md-tokens/213.1-UI-SPEC.md AC#3, AC#9, AC#11, AC#12, AC#15
    - app/(markos)/_components/RotationGraceBanner.stories.tsx (created in Task 3)
    - styles/components.css (with Task 1's coarse-pointer rule)
    - .storybook/preview.tsx (with Task 2's globals.css import)
  </read_first>
  <files>test/ui-a11y/213-1-chrome-a11y.test.js</files>
  <action>
Create new file `test/ui-a11y/213-1-chrome-a11y.test.js` with the exact contents below.

```js
'use strict';

// Phase 213.1 — chrome a11y grep-shape suite.
//
// Asserts that the redesigned chrome (layout-shell + RotationGraceBanner)
// satisfies UI-SPEC AC#3 (.c-* primitive composition), AC#9 (bracketed-glyph
// state coding), AC#11 (Sora/Space Grotesk eliminated), AC#12 (teal hexes
// eliminated), AC#15 ((pointer: coarse) rule on global primitive), and the
// Storybook preview wiring that lets stories render against DESIGN.md tokens
// (RESEARCH.md Risk R2 mitigation).
//
// Mirrors the canonical pattern from test/ui-a11y/accessibility.test.js —
// grep-shape only; runtime axe assertions live in the Storybook addon-a11y
// + Chromatic pipeline per .github/workflows/ui-quality.yml.

const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');

const ROOT = process.cwd();
function read(file) {
  return fs.readFileSync(path.join(ROOT, file), 'utf8');
}

// ---------------------------------------------------------------------------
// Wave 0 artifact existence — story files must exist for addon-a11y to run.
// ---------------------------------------------------------------------------
test('213.1 chrome a11y: layout-shell story file exists', () => {
  assert.equal(fs.existsSync(path.join(ROOT, 'app/(markos)/layout.stories.tsx')), true);
});

test('213.1 chrome a11y: RotationGraceBanner story file exists', () => {
  assert.equal(fs.existsSync(path.join(ROOT, 'app/(markos)/_components/RotationGraceBanner.stories.tsx')), true);
});

test('213.1 chrome a11y: NavList client subcomponent file exists', () => {
  assert.equal(fs.existsSync(path.join(ROOT, 'app/(markos)/_components/NavList.tsx')), true);
});

// ---------------------------------------------------------------------------
// AC#3 — Component primitive composition in TSX.
// ---------------------------------------------------------------------------
test('213.1 chrome a11y AC#3: layout-shell.tsx composes c-sidebar / c-card / c-chip / c-chip-protocol / t-lead / t-label-caps', () => {
  const tsx = read('app/(markos)/layout-shell.tsx');
  assert.match(tsx, /c-sidebar/);
  assert.match(tsx, /c-card/);
  assert.match(tsx, /c-chip\s+c-chip--mint|c-chip c-chip--mint/);
  assert.match(tsx, /c-chip-protocol/);
  assert.match(tsx, /t-lead/);
  assert.match(tsx, /t-label-caps/);
});

test('213.1 chrome a11y AC#3: NavList.tsx composes c-nav-link primitive', () => {
  const tsx = read('app/(markos)/_components/NavList.tsx');
  assert.match(tsx, /c-nav-link/);
});

test('213.1 chrome a11y AC#3: RotationGraceBanner.tsx composes c-status-dot primitive', () => {
  const tsx = read('app/(markos)/_components/RotationGraceBanner.tsx');
  assert.match(tsx, /c-status-dot/);
});

// ---------------------------------------------------------------------------
// AC#8 — Active nav state wired via usePathname() + aria-current="page".
// ---------------------------------------------------------------------------
test('213.1 chrome a11y AC#8: NavList.tsx uses usePathname() + aria-current="page" ternary', () => {
  const tsx = read('app/(markos)/_components/NavList.tsx');
  assert.match(tsx, /usePathname\(\)/);
  assert.match(tsx, /aria-current=\{pathname === item\.href \? "page" : undefined\}/);
});

// ---------------------------------------------------------------------------
// AC#9 — Bracketed-glyph state coding on banner + denied-state eyebrow.
// ---------------------------------------------------------------------------
test('213.1 chrome a11y AC#9: RotationGraceBanner.tsx uses [warn] prefix on T-7/T-1/multi <strong>', () => {
  const tsx = read('app/(markos)/_components/RotationGraceBanner.tsx');
  assert.match(tsx, /<strong>\[warn\] Signing-secret rotation in progress\./);
  assert.match(tsx, /<strong>\[warn\] Signing-secret rotation ends tomorrow\./);
  assert.match(tsx, /<strong>\[warn\] \{rotations\.length\} signing-secret rotations in progress\./);
});

test('213.1 chrome a11y AC#9: RotationGraceBanner.tsx uses [err] prefix on T-0 <strong>', () => {
  const tsx = read('app/(markos)/_components/RotationGraceBanner.tsx');
  assert.match(tsx, /<strong>\[err\] Grace window ends today\./);
});

test('213.1 chrome a11y AC#9: layout-shell.tsx denied state uses [err] prefix on eyebrow', () => {
  const tsx = read('app/(markos)/layout-shell.tsx');
  assert.match(tsx, /\[err\] Protected workspace route/);
});

// ---------------------------------------------------------------------------
// AC#11 — Sora / Space Grotesk eliminated from chrome module.css files.
// ---------------------------------------------------------------------------
test('213.1 chrome a11y AC#11: layout-shell.module.css contains no Sora/Space Grotesk fonts', () => {
  const css = read('app/(markos)/layout-shell.module.css');
  assert.doesNotMatch(css, /Sora|Space Grotesk/);
});

test('213.1 chrome a11y AC#11: RotationGraceBanner.module.css contains no Sora/Space Grotesk fonts', () => {
  const css = read('app/(markos)/_components/RotationGraceBanner.module.css');
  assert.doesNotMatch(css, /Sora|Space Grotesk/);
});

// ---------------------------------------------------------------------------
// AC#12 — Teal legacy hexes eliminated.
// ---------------------------------------------------------------------------
test('213.1 chrome a11y AC#12: layout-shell.module.css contains no teal hexes', () => {
  const css = read('app/(markos)/layout-shell.module.css');
  assert.doesNotMatch(css, /#0d9488|#0f766e|#14b8a6|#e6fffb/i);
});

test('213.1 chrome a11y AC#12: RotationGraceBanner.module.css contains no teal hexes', () => {
  const css = read('app/(markos)/_components/RotationGraceBanner.module.css');
  assert.doesNotMatch(css, /#0d9488|#0f766e|#14b8a6|#e6fffb/i);
});

// ---------------------------------------------------------------------------
// AC#13 — No gradients, no drop-shadows on cards.
// ---------------------------------------------------------------------------
test('213.1 chrome a11y AC#13: chrome modules contain no gradients and no drop-shadows on cards', () => {
  const layoutCss = read('app/(markos)/layout-shell.module.css');
  const bannerCss = read('app/(markos)/_components/RotationGraceBanner.module.css');
  assert.doesNotMatch(layoutCss, /linear-gradient|radial-gradient|box-shadow/);
  assert.doesNotMatch(bannerCss, /linear-gradient|radial-gradient|box-shadow/);
});

// ---------------------------------------------------------------------------
// AC#15 — (pointer: coarse) rule on .c-nav-link in styles/components.css.
// ---------------------------------------------------------------------------
test('213.1 chrome a11y AC#15: styles/components.css declares (pointer: coarse) bump for .c-nav-link', () => {
  const css = read('styles/components.css');
  assert.match(css, /@media\s*\(pointer:\s*coarse\)/);
  // Rule body must scope to .c-nav-link with padding-block lifted.
  assert.match(css, /@media\s*\(pointer:\s*coarse\)\s*\{[\s\S]{0,200}\.c-nav-link[\s\S]{0,200}padding-block:\s*11px/);
});

test('213.1 chrome a11y AC#15: chrome module.css does NOT redeclare (pointer: coarse) (rule lives on global primitive)', () => {
  const layoutCss = read('app/(markos)/layout-shell.module.css');
  const bannerCss = read('app/(markos)/_components/RotationGraceBanner.module.css');
  assert.doesNotMatch(layoutCss, /@media\s*\(pointer:\s*coarse\)/);
  assert.doesNotMatch(bannerCss, /@media\s*\(pointer:\s*coarse\)/);
});

// ---------------------------------------------------------------------------
// Storybook readiness — preview.tsx imports DESIGN.md tokens (R2 mitigation).
// ---------------------------------------------------------------------------
test('213.1 chrome a11y: .storybook/preview.tsx imports app/globals.css for DESIGN.md cascade', () => {
  const preview = read('.storybook/preview.tsx');
  assert.match(preview, /import\s+["']\.\.\/app\/globals\.css["']/);
});

test('213.1 chrome a11y: .storybook/preview.tsx gates legacy ThemeProvider behind opt-in', () => {
  const preview = read('.storybook/preview.tsx');
  assert.match(preview, /themeVariant !== "legacy"/);
  assert.match(preview, /themeVariant !== "white-label"/);
});

// ---------------------------------------------------------------------------
// Token consumption positive assertions — modules cite var(--*) tokens.
// ---------------------------------------------------------------------------
test('213.1 chrome a11y: layout-shell.module.css consumes DESIGN.md var(--color-*) + var(--space-*) tokens', () => {
  const css = read('app/(markos)/layout-shell.module.css');
  assert.match(css, /var\(--color-surface\)/);
  assert.match(css, /var\(--space-md\)/);
  assert.match(css, /var\(--w-container\)/);
  assert.match(css, /var\(--w-sidebar\)/);
});

test('213.1 chrome a11y: RotationGraceBanner.module.css consumes var(--color-warning) + var(--color-error) + canonical alpha-tint formulas', () => {
  const css = read('app/(markos)/_components/RotationGraceBanner.module.css');
  assert.match(css, /var\(--color-warning\)/);
  assert.match(css, /var\(--color-error\)/);
  assert.match(css, /rgb\(255 184 0 \/ 0\.12\)/);
  assert.match(css, /rgb\(248 81 73 \/ 0\.12\)/);
});

// ---------------------------------------------------------------------------
// Banner stories cover all 5 states for addon-a11y runtime axe coverage.
// ---------------------------------------------------------------------------
test('213.1 chrome a11y: RotationGraceBanner stories cover Empty / T7Warning / T1Warning / T0Error / MultiWarning', () => {
  const content = read('app/(markos)/_components/RotationGraceBanner.stories.tsx');
  assert.match(content, /export const Empty/);
  assert.match(content, /export const T7Warning/);
  assert.match(content, /export const T1Warning/);
  assert.match(content, /export const T0Error/);
  assert.match(content, /export const MultiWarning/);
});
```

After creating, run `npm run test:ui-a11y -- test/ui-a11y/213-1-chrome-a11y.test.js`. Output should show ~17 tests passing, 0 failing.
  </action>
  <verify>
    <automated>test -f "test/ui-a11y/213-1-chrome-a11y.test.js" && grep -c "node:test" "test/ui-a11y/213-1-chrome-a11y.test.js" | grep -q "^1$" && grep -cE "AC#(3|8|9|11|12|13|15)" "test/ui-a11y/213-1-chrome-a11y.test.js" | grep -qE "^[7-9]$|^[0-9]{2,}$" && npm run test:ui-a11y -- test/ui-a11y/213-1-chrome-a11y.test.js && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - **File exists:** `test -f "test/ui-a11y/213-1-chrome-a11y.test.js"`.
    - **Uses node:test:** `grep -c "require('node:test')" "test/ui-a11y/213-1-chrome-a11y.test.js"` returns `1`.
    - **Tests every binding AC:** `grep -c "AC#3" "test/ui-a11y/213-1-chrome-a11y.test.js"` ≥1, `grep -c "AC#8" ` ≥1, `grep -c "AC#9" ` ≥1, `grep -c "AC#11" ` ≥1, `grep -c "AC#12" ` ≥1, `grep -c "AC#13" ` ≥1, `grep -c "AC#15" ` ≥1.
    - **Tests preview.tsx wiring:** `grep -c 'globals\.css' "test/ui-a11y/213-1-chrome-a11y.test.js"` returns ≥1. `grep -c 'themeVariant !== "legacy"' "test/ui-a11y/213-1-chrome-a11y.test.js"` returns ≥1.
    - **All 5 banner story exports asserted:** `grep -cE "Empty|T7Warning|T1Warning|T0Error|MultiWarning" "test/ui-a11y/213-1-chrome-a11y.test.js"` returns ≥5.
    - **`npm run test:ui-a11y -- test/ui-a11y/213-1-chrome-a11y.test.js` exits 0.**
    - **Phase 203 + Plan-02 tests still pass:** `npm test -- test/webhooks/layout-shell-banner.test.js` exits 0; `npm test -- test/webhooks/ui-s4-a11y.test.js` exits 0.
  </acceptance_criteria>
  <done>test/ui-a11y/213-1-chrome-a11y.test.js exists, asserts all binding ACs (3, 8, 9, 11, 12, 13, 15), and exits 0. Phase 203 wiring + Plan-02 content tests still pass.</done>
</task>

</tasks>

<verification>
After all four tasks complete, run:

```bash
# AC#15 — coarse-pointer rule placed correctly
grep -c "@media (pointer: coarse)" styles/components.css
# Expected: 1
grep -c "@media (pointer: coarse)" "app/(markos)/layout-shell.module.css"
# Expected: 0
grep -c "@media (pointer: coarse)" "app/(markos)/_components/RotationGraceBanner.module.css"
# Expected: 0

# Storybook preview wiring
grep -c 'import.*"\.\./app/globals\.css"' .storybook/preview.tsx
# Expected: 1
grep -c 'themeVariant !== "legacy"' .storybook/preview.tsx
# Expected: 1

# Storybook stories
grep -cE "^export const (Empty|T7Warning|T1Warning|T0Error|MultiWarning):" "app/(markos)/_components/RotationGraceBanner.stories.tsx"
# Expected: 5

# a11y test
test -f "test/ui-a11y/213-1-chrome-a11y.test.js" && echo "EXISTS"

# Test runs
npm run test:ui-a11y -- test/ui-a11y/213-1-chrome-a11y.test.js
# Expected: exit 0
npm test -- test/webhooks/ui-s4-a11y.test.js
# Expected: exit 0
npm test -- test/webhooks/layout-shell-banner.test.js
# Expected: exit 0

# Build clean
npm run build
# Expected: exit 0
npm run build-storybook
# Expected: exit 0
```
</verification>

<success_criteria>
- All 8 must_haves truths verifiable.
- AC#3, AC#4, AC#9, AC#11, AC#12, AC#13, AC#15 from UI-SPEC.md table close.
- RESEARCH.md Risk R2 (Storybook decorator clobbering DESIGN.md tokens) mitigated by globals.css import + ThemeProvider opt-in gate.
- RESEARCH.md Risk R6 (Chromatic auto-approve gap) — explicit human-review acceptance: this plan does NOT auto-approve Chromatic. The team must manually review the visual baseline diff for `Routes/MarkOS Layout` + `Components/RotationGraceBanner` stories before declaring the phase done. (Document this in the SUMMARY hand-off, NOT as an automated gate.)
- All Phase 203 + Plan-02 tests still pass.
- `npm run build` + `npm run build-storybook` + `npm run test:ui-a11y` all exit 0.
- Atomic commit shape: `docs(213.1): add RotationGraceBanner stories + 213-1 chrome a11y test + (pointer: coarse) on .c-nav-link + .storybook/preview.tsx DESIGN.md cascade`.
</success_criteria>

<output>
After completion, create `.planning/phases/213.1-ui-canon-adoption-wave-1-chrome-layout-shell-rotationgracebanner-to-design-md-tokens/213.1-03-SUMMARY.md` documenting:
- Files created (`RotationGraceBanner.stories.tsx`, `213-1-chrome-a11y.test.js`)
- Files modified (`styles/components.css` 1 block insert, `.storybook/preview.tsx` 3-edit surgery)
- AC# closures (3, 4, 9, 11, 12, 13, 15)
- Storybook + a11y test verification commands and outcomes
- **Manual Chromatic review gate (R6 mitigation):** explicit instruction for the team to review visual baselines for `Routes/MarkOS Layout` + `Components/RotationGraceBanner` stories before declaring Phase 213.1 done
- **Manual `forced-colors` + light-mode gate** (per VALIDATION.md Manual-Only Verifications): instructions for Windows High Contrast verification + `[data-theme="light"]` flip
- Hand-off to `/gsd-verify-work 213.1` for phase verification, then `/gsd-discuss-phase 213.2` for the next decimal phase (auth surfaces)
</output>
