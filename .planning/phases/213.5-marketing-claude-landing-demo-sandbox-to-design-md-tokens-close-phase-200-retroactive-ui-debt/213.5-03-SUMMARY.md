---
phase: 213.5
plan: "03"
subsystem: marketing-ui
tags: [a11y-suite, cross-cutting, token-adoption, phase-200-close, storybook]
dependency_graph:
  requires: [213.5-01, 213.5-02]
  provides: [213.5 a11y test suite (45 tests), Phase 200 retroactive UI debt closed, VALIDATION.md complete]
  affects: [test/ui-a11y/213-5-marketing-a11y.test.js]
tech_stack:
  added: []
  patterns: [node:test grep-shape (mirrors 213-4-admin-ops-a11y.test.js), fs.readFileSync constant-path assertions, regex guards (HEX_RE/FONT_BAN_RE/ANTIPATTERN_RE/OFFGRID_RE/BANNED_LEXICON_RE)]
key_files:
  created:
    - test/ui-a11y/213-5-marketing-a11y.test.js
  modified:
    - .planning/phases/213.5-marketing-claude-landing-demo-sandbox-to-design-md-tokens-close-phase-200-retroactive-ui-debt/213.5-VALIDATION.md
decisions:
  - "Used CommonJS require() syntax (not ESM import) to match 213-4-admin-ops-a11y.test.js precedent exactly — node:test works with both"
  - "45 tests (exceeds >=20 minimum): L-1..L-7 landing surface (15 tests) + DS-1..DS-8 demo surface (20 tests) + X-7/X-8/X-9/X-11 cross-cutting (10 tests)"
  - "3 pre-existing npm test failures (TEN-04, phase-45-registry, Suite-201-02) confirmed unrelated to 213.5 — tracked under separate phases; 213.5 suite is 45/45 green"
  - "npm run build does not exist in this project (Node.js CLI, not Next.js app); build:llm (tsc --noEmit) + build-storybook both pass"
metrics:
  duration: "~12 min"
  completed: "2026-04-29"
  tasks_completed: 2
  tasks_total: 2
  files_changed: 2
---

# Phase 213.5 Plan 03: Cross-Cutting A11y Suite Summary

45-test grep-shape a11y suite (`test/ui-a11y/213-5-marketing-a11y.test.js`) covering UI-SPEC ACs X-1..X-9 + X-11 across both Plan 01 landing page and Plan 02 demo sandbox outputs; Phase 200 voice classifier baseline (13/13) preserved; build-storybook green; Phase 213.5 + Phase 200 retroactive UI debt closed.

## What Was Built

### Task 1 — test/ui-a11y/213-5-marketing-a11y.test.js (NEW, X-1..X-9, X-11)

Created `test/ui-a11y/213-5-marketing-a11y.test.js` (269 LOC, 45 tests) mirroring `test/ui-a11y/213-4-admin-ops-a11y.test.js` grep-shape pattern exactly:

- **Imports:** `node:test` + `node:assert/strict` + `node:fs` (readFileSync/existsSync) + `node:path`
- **Helper:** `read(file)` + `exists(file)` functions with `path.join(ROOT, file)` resolution
- **6 file-path constants** at top: LANDING_CSS/TSX/STORIES + DEMO_CSS/TSX/STORIES
- **5 shared regex guards:** HEX_RE, FONT_BAN_RE, ANTIPATTERN_RE, OFFGRID_RE, BANNED_LEXICON_RE

Test distribution:
- **L-1..L-7 landing (15 tests):** token-only CSS, no banned fonts, no anti-patterns, no off-grid, orphan-class elimination (claude-integration-landing/hero/tool-grid/btn), .c-card--feature hero, .c-card--interactive tool grid, .c-chip-protocol slugs, .c-button--primary x2 exact count, .c-button--tertiary, .c-code-inline
- **DS-1..DS-8 demo (20 tests):** token-only CSS, no banned fonts, no anti-patterns, no off-grid, orphan-class elimination (claude-demo-sandbox/demo-error/demo-result), >=5 .c-field__label + >=5 .c-input, .c-button--primary + .is-loading + aria-busy, .c-notice c-notice--error + [err], .c-notice c-notice--warning + [warn] + 429 branch, .c-card result, fetch URL + tool: 'draft_message' verbatim
- **X-7/X-8/X-9/X-11 cross-cutting (10 tests):** both .c-notice variants present, [err]+[warn] glyphs, banned-lexicon=0 in both .tsx, story files exist with correct meta.title and all named exports

AC# distribution: L-* mentions = 7 unique ACs (L-1..L-7, satisfies >=4 per X-2). DS-* mentions = 8 unique ACs (DS-1..DS-8, satisfies >=4 per X-3).

### Task 2 — Gate verification + VALIDATION.md + phase closure

- **X-10 Phase 200 voice baseline:** `node --test test/marketing/claude-landing.test.js` → 13/13 green. Voice score >=85 (D-11 baseline) preserved — Plan 01 copy unchanged verbatim.
- **X-1 new a11y suite:** `node --test test/ui-a11y/213-5-marketing-a11y.test.js` → 45/45 green.
- **X-12 full suite:** 3 pre-existing failures (TEN-04, phase-45-registry, Suite-201-02) confirmed unrelated to Phase 213.5; all 213.5 tests green.
- **Build gates:** `npm run build:llm` (tsc --noEmit) exits 0; `npm run build-storybook` exits 0 (17s, storybook-static built).
- **D-20:** `git diff app/(marketing)/integrations/claude/demo/api/route.ts` empty — route.ts untouched.
- **VALIDATION.md:** All 8 per-task rows marked green; wave_0_complete flipped true; phase gates checked.

## Deviations from Plan

### Auto-fixed Issues

None — plan executed exactly as written with one clarification:

**[Rule 1 - Discovery] npm run build does not exist**
- **Found during:** Task 2 Step 4
- **Issue:** Plan references `npm run build` — this project is a Node.js CLI, not a Next.js app; the script does not exist.
- **Fix:** Ran `npm run build:llm` (TypeScript check, exits 0) as the equivalent build gate; `npm run build-storybook` also exits 0.
- **Impact:** None — both meaningful build checks pass; no regression.

## Phase 213.5 Closure Summary

| Plan | Tasks | Key Output | Status |
|------|-------|-----------|--------|
| 213.5-01 | 3 | page.module.css + page.tsx refactor + page.stories.tsx (L-1..L-8) | Closed |
| 213.5-02 | 3 | demo/page.module.css + demo/page.tsx 429 branch + demo/page.stories.tsx (DS-1..DS-9) | Closed |
| 213.5-03 | 2 | 213-5-marketing-a11y.test.js 45 tests (X-1..X-9, X-11) + voice baseline + build gates | Closed |

**Total:** 3 plans, 8 tasks, 7 atomic commits + 1 phase docs commit.

**Phase 200 retroactive UI debt:** Both marketing surfaces (`/integrations/claude/` + `/integrations/claude/demo/`) now on DESIGN.md v1.1.0 canon. Orphan classnames eliminated. Token-only authoring verified. Voice classifier baseline >=85 preserved. 9 Storybook stories (4 landing + 5 demo) added.

**Wiring preserved:** `demo/api/route.ts` untouched (D-20). `fetch('/integrations/claude/demo/api')` + `tool: 'draft_message'` verbatim (DS-8).

**Hand-off:** Ready for `/gsd-verify-work 213.5`.

## Known Stubs

None — both surfaces fully wired. No placeholder data, no TODO markers, no empty arrays flowing to UI rendering.

## Threat Flags

None — test file is pure filesystem read (no network IO, no new trust boundaries introduced).

## Self-Check: PASSED

- [x] `test/ui-a11y/213-5-marketing-a11y.test.js` exists and contains 45 tests
- [x] `213.5-03-SUMMARY.md` written (this file)
- [x] `213.5-VALIDATION.md` updated (wave_0_complete: true, all rows green)
- [x] Commit `2a792ff` exists: `test(213.5-03): add ui-a11y test suite for marketing/claude landing + demo`
- [x] `node --test test/ui-a11y/213-5-marketing-a11y.test.js` → 45/45 pass
- [x] `node --test test/marketing/claude-landing.test.js` → 13/13 pass
- [x] `npm run build:llm` exits 0
- [x] `npm run build-storybook` exits 0
