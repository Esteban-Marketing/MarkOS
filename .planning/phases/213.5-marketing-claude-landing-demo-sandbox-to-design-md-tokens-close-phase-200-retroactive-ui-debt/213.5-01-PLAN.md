---
phase: 213.5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(marketing)/integrations/claude/page.module.css
  - app/(marketing)/integrations/claude/page.tsx
  - app/(marketing)/integrations/claude/page.stories.tsx
autonomous: true
requirements:
  - L-1
  - L-2
  - L-3
  - L-4
  - L-5
  - L-6
  - L-7
  - L-8
must_haves:
  truths:
    - "app/(marketing)/integrations/claude/page.module.css is NEW, contains zero inline hex (whitelist 1px/2px/4px), zero Sora/Space Grotesk, zero radial-gradient/linear-gradient/box-shadow-rgba/transform-translate, zero off-grid spacing literals (10/12/13/14/15/18/20/22/28/36/40/45 px), every value cites var(--*) tokens"
    - "app/(marketing)/integrations/claude/page.tsx composes .c-card--feature on hero panel (32px radius hero exception per D-15, same precedent as 404-workspace per 213.4 D-13), .c-card--interactive + .c-chip-protocol on each tool grid <li>, .c-button c-button--primary on Try the demo CTA, .c-button c-button--tertiary on Read the quickstart CTA, .c-code-inline on MCP server URL <code>, .t-lead utility on hero sub <p>"
    - "Exactly 2 occurrences of c-button--primary in page.tsx (hero CTA + final-cta CTA per L-6 — scroll-separated, not simultaneously visible)"
    - "All legacy orphan classes eliminated from page.tsx: claude-integration-landing, hero, hero-sub, cta-row, value-props, value-list, tools, tool-grid, install, install-note, final-cta, btn, btn-primary, btn-secondary"
    - "DOM tree + props + aria-label attributes + TOOLS array (10 MCP allow-list slugs) preserved verbatim per D-20/D-21"
    - "app/(marketing)/integrations/claude/page.stories.tsx is NEW with exactly 4 named state exports (Default, ToolListExpanded, CTAFocus, MobileBreakpoint), meta.title='213.5/ClaudeLanding'"
    - "Banned-lexicon scan = 0 in modified .tsx; zero exclamation; zero emoji; zero 'Please' softener"
    - "Voice classifier baseline preserved (text content unchanged in <h1>, hero sub <p>, value-prop <h2>/<h3>, tool descriptions, install copy, final CTA copy)"
    - "npm run build exits 0; npm run build-storybook exits 0; npm test exits 0"
  artifacts:
    - path: "app/(marketing)/integrations/claude/page.module.css"
      provides: "NEW token-only landing-page layout — .landing page wrapper, .ctaRow, .valueProps, .valueList grid, .tools, .toolGrid grid, .install, .installLink, .finalCta. Hero card layout delegated to .c-card--feature primitive JSX-side."
      min_lines: 30
      contains: "var(--color-surface)"
    - path: "app/(marketing)/integrations/claude/page.tsx"
      provides: "Updated landing surface composing .c-card--feature hero + .c-card--interactive tool cards + .c-chip-protocol tool slugs + .c-button{,--primary,--tertiary} CTAs + .c-code-inline MCP URL + .t-lead hero sub. Preserves TOOLS array (10 slugs), DOM, aria-labels, copy."
      contains: "c-card--feature"
    - path: "app/(marketing)/integrations/claude/page.stories.tsx"
      provides: "NEW Storybook 8 CSF3 file — 4 named state exports (Default, ToolListExpanded, CTAFocus, MobileBreakpoint), meta.title='213.5/ClaudeLanding'"
      exports: ["default", "Default", "ToolListExpanded", "CTAFocus", "MobileBreakpoint"]
  key_links:
    - from: "app/(marketing)/integrations/claude/page.tsx"
      to: "styles/components.css .c-card--feature primitive (32px radius hero exception per D-15)"
      via: "className composition for hero panel"
      pattern: "c-card--feature"
    - from: "app/(marketing)/integrations/claude/page.tsx"
      to: "styles/components.css .c-card--interactive + .c-chip-protocol primitives"
      via: "className composition for each tool <li> + tool slug <span>"
      pattern: "c-card--interactive|c-chip-protocol"
    - from: "app/(marketing)/integrations/claude/page.module.css"
      to: "app/tokens.css var(--*)"
      via: "var(--color-surface), var(--color-on-surface), --space-{xs,sm,md,xl,xxl}, var(--color-primary-text), var(--fw-semibold)"
      pattern: "var\\(--(color|space|fw|fs|radius)-"
    - from: "app/(marketing)/integrations/claude/page.tsx"
      to: "Phase 200 voice classifier baseline test/marketing/claude-landing.test.js"
      via: "copy text nodes preserved verbatim"
      pattern: "Ship drafts from Claude|Try the demo|Read the quickstart"
---

<objective>
Redesign `/integrations/claude` (122 LOC `page.tsx` with orphan classNames `.btn`, `.btn-primary`, `.btn-secondary`, `.hero`, `.hero-sub`, `.cta-row`, `.value-props`, `.value-list`, `.tools`, `.tool-grid`, `.install`, `.install-note`, `.final-cta`, `.claude-integration-landing` and NO `.module.css`) into the canonical DESIGN.md v1.1.0 visual contract per `213.5-UI-SPEC.md` Plan 213.5-01 (8 ACs: L-1..L-8).

Compose **`.c-card--feature`** on hero (32px radius hero exception per D-15, same precedent as 404-workspace 213.4-07), **`.c-card--interactive`** + **`.c-chip-protocol`** per tool grid item, **`.c-button c-button--primary`** on "Try the demo" + closing "Open the demo sandbox" CTAs (L-6: 2 occurrences allowed, scroll-separated), **`.c-button c-button--tertiary`** on "Read the quickstart" CTA, **`.c-code-inline`** on MCP server URL `<code>`, **`.t-lead`** on hero sub `<p>`.

**DOM tree + props + aria-labels + TOOLS array (10 MCP allow-list slugs) preserved verbatim per D-20/D-21.** Voice classifier baseline (Phase 200, target ≥100 per D-11) preserved — copy text nodes unchanged.

Purpose: Close UI-SPEC L-1 (NEW page.module.css zero hex), L-2 (.landing wrapper, .claude-integration-landing eliminated), L-3 (.c-card--feature hero, .hero eliminated), L-4 (.c-card--interactive + .c-chip-protocol on tool grid, .tool-grid eliminated), L-5 (.c-button--primary/--tertiary on CTAs, .btn/.btn-primary/.btn-secondary eliminated), L-6 (exactly 2 .c-button--primary), L-7 (.c-code-inline on MCP URL), L-8 (NEW page.stories.tsx with 4 named state exports).

Output: `page.module.css` NEW (~50 LOC); `page.tsx` updated (122 LOC, className-side only); NEW `page.stories.tsx` with 4 stories.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/213.5-marketing-claude-landing-demo-sandbox-to-design-md-tokens-close-phase-200-retroactive-ui-debt/213.5-CONTEXT.md
@.planning/phases/213.5-marketing-claude-landing-demo-sandbox-to-design-md-tokens-close-phase-200-retroactive-ui-debt/213.5-UI-SPEC.md
@.planning/phases/213.5-marketing-claude-landing-demo-sandbox-to-design-md-tokens-close-phase-200-retroactive-ui-debt/213.5-RESEARCH.md
@DESIGN.md
@CLAUDE.md
@app/tokens.css
@app/globals.css
@styles/components.css
@app/(marketing)/integrations/claude/page.tsx
@.planning/phases/213.4-ui-canon-adoption-wave-4-admin-operations-status-404-workspace-to-design-md-tokens/213.4-07-PLAN.md

<interfaces>
<!-- Class-Rename Map (UI-SPEC.md "Surface 1 — Landing page" + RESEARCH.md "Class-Rename Map — Surface 1"): -->
<!-- .claude-integration-landing → local .landing: bg var(--color-surface); color var(--color-on-surface); min-height: 100dvh; padding-block var(--space-xl); padding-inline var(--space-md) -->
<!-- .hero → DELETE; section becomes child of .c-card--feature hero card (32px radius via primitive) -->
<!-- .hero-sub → utility .t-lead (globals.css) -->
<!-- .cta-row → local .ctaRow: flex; flex-wrap wrap; gap var(--space-sm) -->
<!-- .btn.btn-primary → .c-button c-button--primary (×2 occurrences allowed per L-6) -->
<!-- .btn.btn-secondary → .c-button c-button--tertiary -->
<!-- .value-props → local .valueProps: padding-block var(--space-xl); display flex; flex-direction column; gap var(--space-md) -->
<!-- .value-list → local .valueList: display grid; grid-template-columns repeat(auto-fit, minmax(240px, 1fr)); gap var(--space-md); list-style none; padding 0; margin 0 -->
<!-- .tools → local .tools: padding-block var(--space-xl) -->
<!-- .tool-grid → local .toolGrid: display grid; grid-template-columns repeat(auto-fill, minmax(200px, 1fr)); gap var(--space-sm); list-style none; padding 0; margin 0 -->
<!-- <li> bare → adds .c-card--interactive className -->
<!-- <code>{tool.name}</code> → <span className="c-chip-protocol">{tool.name}</span> -->
<!-- .install → local .install: padding-block var(--space-xl); display flex; flex-direction column; gap var(--space-sm) -->
<!-- .install-note → plain <p>; inline <Link> gets local .installLink: color var(--color-primary-text); font-weight var(--fw-semibold); text-decoration underline; text-underline-offset 2px -->
<!-- <code> MCP URL → <code className="c-code-inline">https://markos.dev/api/mcp/session</code> -->
<!-- .final-cta → local .finalCta: padding-block var(--space-xl); display flex; flex-direction column; align-items center; gap var(--space-md); text-align center -->

<!-- Storybook stories (UI-SPEC.md "Storybook State Coverage" + L-8): -->
<!-- meta.title='213.5/ClaudeLanding'; parameters.layout='fullscreen' -->
<!-- Default — full landing page, hero visible, tool grid rendered -->
<!-- ToolListExpanded — all 10 tool cards visible (no collapse), protocol chips visible -->
<!-- CTAFocus — primary CTA in :focus-visible state (parameters.pseudo.focusVisible per @storybook/addon-pseudo-states or autofocus + aria-pressed pattern) -->
<!-- MobileBreakpoint — viewport 375px (parameters.viewport.defaultViewport='mobile1') -->

<!-- Wiring preservation contracts (RESEARCH.md "Wiring Preservation Map"): -->
<!-- TOOLS const array (10 entries) UNCHANGED — MCP allow-list per D-21 -->
<!-- aria-label attributes on both <Link> CTAs in hero UNCHANGED -->
<!-- All copy text nodes UNCHANGED (voice classifier ≥ 100 per D-11) -->
<!-- metadata export UNCHANGED -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create NEW app/(marketing)/integrations/claude/page.module.css with token-only local layout classes (.landing, .ctaRow, .valueProps, .valueList, .tools, .toolGrid, .install, .installLink, .finalCta) — zero hex, zero off-grid, zero anti-pattern (L-1)</name>
  <read_first>
    - app/(marketing)/integrations/claude/page.tsx (FULL 122 LOC — confirm classNames currently referenced)
    - .planning/phases/213.5-.../213.5-UI-SPEC.md "Surface 1 — Landing page" (L-1..L-8 + Primitive reuse table)
    - .planning/phases/213.5-.../213.5-RESEARCH.md "Class-Rename Map — Surface 1: Landing page (page.tsx)"
    - .planning/phases/213.5-.../213.5-CONTEXT.md D-15 (.c-card--feature hero exception), D-22 (acceptance gates)
    - DESIGN.md "Spacing Scale" (8px grid + exceptions 1px/2px/4px) + "Color" (token-only) + "Typography" (no Sora/Space Grotesk)
    - app/tokens.css (confirm --w-modal=560px line 152, --color-surface, --color-on-surface, --space-*, --color-primary-text, --fw-semibold all present)
    - styles/components.css v1.1.0 (.c-card--feature, .c-card--interactive, .c-chip-protocol primitives — confirm exist)
    - .planning/phases/213.4-.../213.4-07-PLAN.md Task 1 (precedent NEW module.css for marketing-tone hero)
  </read_first>
  <files>app/(marketing)/integrations/claude/page.module.css</files>
  <action>
**Create NEW `app/(marketing)/integrations/claude/page.module.css` with EXACTLY this content (token-only, zero hex outside whitelist, zero off-grid):**

```css
/* Phase 213.5 Plan-01 — Token-only Claude Marketplace landing-page layout.
 * Composes .c-card--feature (hero panel, 32px radius hero exception per D-15,
 * same precedent as 404-workspace 213.4-07), .c-card--interactive (tool grid),
 * .c-chip-protocol (tool slugs), .c-button{,--primary,--tertiary} (CTAs),
 * .c-code-inline (MCP URL), .t-lead (hero sub).
 * NEW file (no prior module.css). Zero inline hex (whitelist: none here —
 * all colors via var(--*)). 8px grid only. */

.landing {
  background: var(--color-surface);
  color: var(--color-on-surface);
  min-height: 100dvh;
  padding-block: var(--space-xl);
  padding-inline: var(--space-md);
  display: flex;
  flex-direction: column;
  gap: var(--space-xl);
  max-width: 75rem;
  margin-inline: auto;
}

.ctaRow {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-sm);
}

.valueProps {
  padding-block: var(--space-xl);
  display: flex;
  flex-direction: column;
  gap: var(--space-md);
}

.valueList {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
  gap: var(--space-md);
  list-style: none;
  padding: 0;
  margin: 0;
}

.tools {
  padding-block: var(--space-xl);
  display: flex;
  flex-direction: column;
  gap: var(--space-md);
}

.toolGrid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
  gap: var(--space-sm);
  list-style: none;
  padding: 0;
  margin: 0;
}

.install {
  padding-block: var(--space-xl);
  display: flex;
  flex-direction: column;
  gap: var(--space-sm);
}

.installLink {
  color: var(--color-primary-text);
  font-weight: var(--fw-semibold);
  text-decoration: underline;
  text-underline-offset: 2px;
}

.finalCta {
  padding-block: var(--space-xl);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: var(--space-md);
  text-align: center;
}
```

**Authoring rules (per L-1 + DESIGN.md):**
- ZERO inline hex anywhere. Color values exclusively via `var(--color-*)`.
- ZERO off-grid spacing literals (`10/12/13/14/15/18/20/22/28/36/40/45 px`). Spacing exclusively via `var(--space-*)`.
- ZERO `font-family` declarations (typography inherits from `globals.css` JetBrains Mono / Inter cascade).
- ZERO `font-size` literals (typography inherits via `<h1>/<h2>/<h3>/<p>` from `globals.css`).
- ZERO `box-shadow` (borders over shadows per DESIGN.md non-negotiable rule 5).
- ZERO `gradient` of any kind (DESIGN.md anti-pattern).
- ZERO `transform: translate*` (no hover-jiggle per D-10).
- ZERO `@media (pointer: coarse)` redeclaration (inherits global per CONTEXT code_context).
- Exception literals allowed: `2px` (text-underline-offset), `0` (list reset), `100dvh` (viewport unit), `75rem`/`240px`/`200px` (rem/px container/grid sizing — these are OK; the off-grid ban targets internal spacing literals like padding/margin/gap, not max-widths or grid track sizes).

**Whitespace as primitive (DESIGN.md non-negotiable rule 7):** `--space-xl` (48px) page padding-block + `--space-xl` section gaps deliver ≥30% empty surface.
  </action>
  <verify>
    <automated>! grep -nE '#[0-9a-fA-F]{3,8}' "app/(marketing)/integrations/claude/page.module.css" && ! grep -nE '(Sora|Space Grotesk|linear-gradient|radial-gradient|box-shadow|transform:\s*translate)' "app/(marketing)/integrations/claude/page.module.css" && ! grep -nE '[^0-9](10|12|13|14|15|18|20|22|28|36|40|45)px' "app/(marketing)/integrations/claude/page.module.css" && ! grep -nE 'font-family|font-size:' "app/(marketing)/integrations/claude/page.module.css" && grep -q "var(--color-surface)" "app/(marketing)/integrations/claude/page.module.css" && grep -q "var(--color-on-surface)" "app/(marketing)/integrations/claude/page.module.css" && grep -q "var(--color-primary-text)" "app/(marketing)/integrations/claude/page.module.css" && grep -q "var(--space-xl)" "app/(marketing)/integrations/claude/page.module.css" && grep -q "var(--space-md)" "app/(marketing)/integrations/claude/page.module.css" && grep -q "var(--space-sm)" "app/(marketing)/integrations/claude/page.module.css" && grep -q "var(--fw-semibold)" "app/(marketing)/integrations/claude/page.module.css" && grep -qE '\.landing\b' "app/(marketing)/integrations/claude/page.module.css" && grep -qE '\.toolGrid\b' "app/(marketing)/integrations/claude/page.module.css" && grep -qE '\.installLink\b' "app/(marketing)/integrations/claude/page.module.css" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **L-1 (zero inline hex):** `grep -nE '#[0-9a-fA-F]{3,8}' page.module.css` returns 0.
    - **L-1 (no Sora/Space Grotesk):** `grep -nE '(Sora|Space Grotesk)' page.module.css` returns 0.
    - **L-1 (no gradient/box-shadow/translate):** `grep -nE '(gradient|box-shadow|transform:\s*translate)' page.module.css` returns 0.
    - **D-22 off-grid scan:** `grep -nE '[^0-9](10|12|13|14|15|18|20|22|28|36|40|45)px' page.module.css` returns 0.
    - **D-22 typography ban:** `grep -nE 'font-family|font-size:' page.module.css` returns 0 (inherits globals).
    - **Required local classes present:** `.landing`, `.ctaRow`, `.valueProps`, `.valueList`, `.tools`, `.toolGrid`, `.install`, `.installLink`, `.finalCta`.
    - **Required tokens cited:** `var(--color-surface)`, `var(--color-on-surface)`, `var(--color-primary-text)`, `var(--space-xl)`, `var(--space-md)`, `var(--space-sm)`, `var(--fw-semibold)`.
  </acceptance_criteria>
  <done>NEW `page.module.css` created (~50 LOC, token-only). Atomic commit: `feat(213.5-01): create marketing/claude/page.module.css token-only landing layout (L-1)`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update app/(marketing)/integrations/claude/page.tsx — swap orphan classNames to local module + primitive composition (.c-card--feature hero, .c-card--interactive + .c-chip-protocol tool grid, .c-button{,--primary,--tertiary} CTAs, .c-code-inline MCP URL, .t-lead hero sub); preserve TOOLS array, DOM, aria-labels, copy verbatim (L-2..L-7)</name>
  <read_first>
    - app/(marketing)/integrations/claude/page.tsx (FULL 122 LOC — confirm TOOLS array shape, aria-label attrs, exact copy strings)
    - app/(marketing)/integrations/claude/page.module.css (post-Task 1 shape — confirm local class names available)
    - .planning/phases/213.5-.../213.5-UI-SPEC.md L-2..L-7 + "Primitive reuse table — landing page" + "Copywriting Contract — Landing page" (all copy unchanged)
    - .planning/phases/213.5-.../213.5-RESEARCH.md "Class-Rename Map — Surface 1" + "Wiring Preservation Map"
    - .planning/phases/213.5-.../213.5-CONTEXT.md D-11 (voice classifier ≥100 preserved), D-20/D-21 (DOM + MCP allow-list slugs preserved), D-15 (.c-card--feature hero exception)
    - styles/components.css v1.1.0 (.c-card, .c-card--feature, .c-card--interactive, .c-chip-protocol, .c-button, .c-button--primary, .c-button--tertiary, .c-code-inline primitives — confirm all exist)
    - app/globals.css (.t-lead utility)
  </read_first>
  <files>app/(marketing)/integrations/claude/page.tsx</files>
  <action>
**Replace `app/(marketing)/integrations/claude/page.tsx` ENTIRELY with the following — `import styles from './page.module.css'` added; classNames swapped; TOOLS array UNCHANGED; DOM/aria/copy UNCHANGED:**

```tsx
/**
 * app/(marketing)/integrations/claude/page.tsx
 *
 * Claude Marketplace landing page.
 *
 * Canon inputs:
 *   archetypes: solopreneur, vibe-coder
 *   pains: content_engagement, pipeline_velocity
 *   brand: markos
 *   promise: ship drafts and audits from the same chat window you already use
 *
 * Voice target: score >= 100 via test/marketing/claude-landing.test.js heuristic
 * (Phase 200 baseline preserved per Phase 213.5 D-11 — copy text nodes unchanged).
 */

import type { Metadata } from 'next';
import Link from 'next/link';
import styles from './page.module.css';

export const metadata: Metadata = {
  title: 'MarkOS for Claude — Ship drafts from Claude',
  description:
    'Connect Claude to MarkOS via MCP. Ten tools for drafting, auditing, and scheduling campaigns — grounded in your canon, fired from the chat window you already use.',
  openGraph: {
    title: 'MarkOS for Claude',
    description:
      'Connect Claude to MarkOS via MCP. Draft, audit, and schedule campaigns from the chat window you already use.',
    images: ['/integrations/claude/og.png'],
  },
};

const TOOLS: Array<{ name: string; description: string }> = [
  { name: 'draft_message', description: 'Produce a channel-native draft from a brief.' },
  { name: 'plan_campaign', description: 'Outline a campaign from an objective and audience.' },
  { name: 'research_audience', description: 'Snapshot an audience segment with canonical pains.' },
  { name: 'run_neuro_audit', description: 'Score a draft against voice, clarity, and evidence rules.' },
  { name: 'generate_brief', description: 'Structure a freeform idea into a ready-to-draft brief.' },
  { name: 'audit_claim', description: 'Verify a marketing claim against canon evidence.' },
  { name: 'list_pain_points', description: 'Expose the canonical pain-point taxonomy.' },
  { name: 'rank_execution_queue', description: 'Rank CRM execution items by intent + urgency.' },
  { name: 'schedule_post', description: 'Push a draft onto a channel queue.' },
  { name: 'explain_literacy', description: 'Explain a literacy node or archetype slug.' },
];

export default function ClaudeIntegrationLanding() {
  return (
    <main className={styles.landing}>
      <section aria-labelledby="hero-heading" className="c-card c-card--feature">
        <h1 id="hero-heading">Ship drafts from Claude.</h1>
        <p className="t-lead">
          MarkOS connects to Claude via MCP. Ten tools for drafting, auditing, and scheduling
          campaigns — grounded in your canon, fired from the chat window you already use.
        </p>
        <div className={styles.ctaRow}>
          <Link href="/integrations/claude/demo" className="c-button c-button--primary" aria-label="Open the MarkOS for Claude demo sandbox">
            Try the demo
          </Link>
          <Link href="/docs/quickstart" className="c-button c-button--tertiary" aria-label="Read the Claude MCP quickstart guide">
            Read the quickstart
          </Link>
        </div>
      </section>

      <section aria-labelledby="value-heading" className={styles.valueProps}>
        <h2 id="value-heading">Why solopreneurs and vibe-coders ship with MarkOS for Claude</h2>
        <ul className={styles.valueList}>
          <li>
            <h3>Stop re-briefing the model</h3>
            <p>
              Your canon, pain taxonomy, and brand voice live inside MarkOS. Claude reads them in
              every tool call. No more pasting the same brand guide.
            </p>
          </li>
          <li>
            <h3>Audit every draft</h3>
            <p>
              Every generation runs through run_neuro_audit. Promise presence, brand coherence,
              claim safety — scored before you hit publish.
            </p>
          </li>
          <li>
            <h3>Pipeline velocity, not pipeline theater</h3>
            <p>
              rank_execution_queue tells you what actually moves the number today. schedule_post
              ships the draft. No more stalled tabs or blank calendars.
            </p>
          </li>
        </ul>
      </section>

      <section aria-labelledby="tools-heading" className={styles.tools}>
        <h2 id="tools-heading">Ten tools, one chat window</h2>
        <ul className={styles.toolGrid} aria-label="MCP tool grid">
          {TOOLS.map((tool) => (
            <li key={tool.name} className="c-card c-card--interactive">
              <span className="c-chip-protocol">{tool.name}</span>
              <p>{tool.description}</p>
            </li>
          ))}
        </ul>
      </section>

      <section aria-labelledby="install-heading" className={styles.install}>
        <h2 id="install-heading">Install in under a minute</h2>
        <ol>
          <li>Open Claude Desktop settings.</li>
          <li>Add a Custom MCP Server pointing at <code className="c-code-inline">https://markos.dev/api/mcp/session</code>.</li>
          <li>Ask Claude: <em>draft a LinkedIn post about pipeline velocity for founder-sam</em>.</li>
        </ol>
        <p>
          Full quickstart with tenant auth, webhook subscriptions, and drift-free canon sync lives
          in <Link href="/docs/quickstart" className={styles.installLink}>the quickstart</Link>.
        </p>
      </section>

      <section aria-labelledby="cta-heading" className={styles.finalCta}>
        <h2 id="cta-heading">Your pipeline wants drafts, not tabs.</h2>
        <Link href="/integrations/claude/demo" className="c-button c-button--primary">
          Open the demo sandbox
        </Link>
      </section>
    </main>
  );
}
```

**KEY rules (per L-2..L-7 + D-11 + D-15 + D-20 + D-21):**
- `<main className={styles.landing}>` — local module class. NO `claude-integration-landing` legacy.
- Hero `<section className="c-card c-card--feature">` — primitive composition (32px radius hero exception via primitive). NO `.hero` legacy. Card padding/radius from primitive.
- Hero sub `<p className="t-lead">` — utility from `globals.css`. NO `.hero-sub` legacy.
- CTA row `<div className={styles.ctaRow}>` — local module class.
- Hero CTAs: primary `c-button c-button--primary` ("Try the demo") + tertiary `c-button c-button--tertiary` ("Read the quickstart"). `aria-label` attrs PRESERVED VERBATIM. NO `.btn`/`.btn-primary`/`.btn-secondary`.
- Value-props `<section className={styles.valueProps}>` + `<ul className={styles.valueList}>`.
- Tools `<section className={styles.tools}>` + `<ul className={styles.toolGrid}>`. Each `<li className="c-card c-card--interactive">` composes primitive (hover-lift via primitive, NOT local). Tool name renders via `<span className="c-chip-protocol">{tool.name}</span>` — `.c-chip-protocol` adds `[`/`]` brackets via `::before`/`::after` per DESIGN.md Amendment 4. Do NOT double-wrap in `<code>`.
- Install `<section className={styles.install}>`. MCP URL: `<code className="c-code-inline">https://markos.dev/api/mcp/session</code>` — text content UNCHANGED per D-21 (MCP allow-list URL accuracy). Inline quickstart link uses `className={styles.installLink}` (mint-as-text underline link).
- Final CTA `<section className={styles.finalCta}>` + `<Link className="c-button c-button--primary">Open the demo sandbox</Link>` — second `c-button--primary` occurrence allowed per L-6 (scroll-separated from hero CTA, never simultaneously visible).
- TOOLS const array — UNCHANGED (10 entries verbatim per D-21).
- Both hero `<Link>` `aria-label` attributes — UNCHANGED.
- ALL copy text nodes (`<h1>`, hero sub `<p>`, value-prop `<h2>`/`<h3>`/`<p>`, tools `<h2>`, tool descriptions, install steps, install note, final-cta `<h2>`, final CTA label) — UNCHANGED VERBATIM (voice classifier ≥100 preserved per D-11; D-19 Phase 200 tests stay green).
- `metadata` export — UNCHANGED.
- NO emoji, NO `!` exclamation, NO "Please" softener, NO banned-lexicon term, NO `[err]`/`[ok]`/`[warn]` glyph (no state surface on landing).
- NO `clsx`/`classnames` imports (D-03 carry — no new deps).
- DO NOT modify `demo/api/route.ts` (D-20 — out of scope this plan).
  </action>
  <verify>
    <automated>grep -qE 'import styles from .{1,2}\./page\.module\.css' "app/(marketing)/integrations/claude/page.tsx" && grep -qE 'styles\.landing\b' "app/(marketing)/integrations/claude/page.tsx" && grep -qE '"c-card c-card--feature"' "app/(marketing)/integrations/claude/page.tsx" && grep -qE '"c-card c-card--interactive"' "app/(marketing)/integrations/claude/page.tsx" && grep -qE '"c-chip-protocol"' "app/(marketing)/integrations/claude/page.tsx" && grep -qE '"c-button c-button--primary"' "app/(marketing)/integrations/claude/page.tsx" && grep -qE '"c-button c-button--tertiary"' "app/(marketing)/integrations/claude/page.tsx" && grep -qE '"c-code-inline"' "app/(marketing)/integrations/claude/page.tsx" && grep -qE '"t-lead"' "app/(marketing)/integrations/claude/page.tsx" && [ "$(grep -c 'c-button c-button--primary' "app/(marketing)/integrations/claude/page.tsx")" = "2" ] && ! grep -qE '"(claude-integration-landing|hero|hero-sub|cta-row|tool-grid|btn|btn-primary|btn-secondary|value-props|value-list|install-note|final-cta|tools)"' "app/(marketing)/integrations/claude/page.tsx" && grep -q "Ship drafts from Claude\." "app/(marketing)/integrations/claude/page.tsx" && grep -q "Try the demo" "app/(marketing)/integrations/claude/page.tsx" && grep -q "Read the quickstart" "app/(marketing)/integrations/claude/page.tsx" && grep -q "Open the demo sandbox" "app/(marketing)/integrations/claude/page.tsx" && grep -q "draft_message" "app/(marketing)/integrations/claude/page.tsx" && grep -q "rank_execution_queue" "app/(marketing)/integrations/claude/page.tsx" && grep -q "https://markos.dev/api/mcp/session" "app/(marketing)/integrations/claude/page.tsx" && ! grep -inE '\b(synergy|leverage|empower|unlock|revolutionize|supercharge|holistic|seamless|cutting-edge|innovative|game-changer|next-generation|world-class|best-in-class|reimagine|disrupt)\b' "app/(marketing)/integrations/claude/page.tsx" && ! grep -qE 'clsx|classnames' "app/(marketing)/integrations/claude/page.tsx" && npm run build && npm test -- test/marketing/claude-landing.test.js && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **L-2 (.landing wrapper):** `grep -qE 'styles\.landing' page.tsx` matches; `! grep -q "claude-integration-landing" page.tsx` returns 0.
    - **L-3 (.c-card--feature hero):** `grep -qE '"c-card c-card--feature"' page.tsx` matches; `! grep -qE '"hero"' page.tsx` returns 0.
    - **L-4 (.c-card--interactive + .c-chip-protocol tool grid):** both match; `! grep -q "tool-grid" page.tsx` returns 0.
    - **L-5 (.c-button--primary + .c-button--tertiary CTAs):** both match; `! grep -qE '"(btn|btn-primary|btn-secondary)"' page.tsx` returns 0.
    - **L-6 (exactly 2 .c-button--primary):** `grep -c "c-button c-button--primary" page.tsx` returns 2.
    - **L-7 (.c-code-inline):** `grep -q "c-code-inline" page.tsx` matches.
    - **D-11 voice baseline:** copy strings preserved (verified via grep + voice classifier test green).
    - **D-21 MCP slugs preserved:** all 10 tool slugs + MCP URL present verbatim.
    - **D-20 route.ts untouched:** plan does not list it in files_modified.
    - **Banned-lexicon = 0; no clsx/classnames import; npm run build green; voice classifier test green.**
  </acceptance_criteria>
  <done>`page.tsx` updated (className-side + import only; copy + DOM + aria preserved). Atomic commit: `feat(213.5-01): compose .c-card--feature/--interactive + .c-chip-protocol + .c-button{,--primary,--tertiary} + .c-code-inline in marketing/claude/page.tsx (L-2..L-7)`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create NEW app/(marketing)/integrations/claude/page.stories.tsx with 4 named state stories (Default, ToolListExpanded, CTAFocus, MobileBreakpoint) per L-8 + run all verification gates</name>
  <read_first>
    - app/(marketing)/integrations/claude/page.tsx (post-Task 2 — Server Component, no async)
    - .planning/phases/213.5-.../213.5-UI-SPEC.md L-8 + "Storybook State Coverage" table (4 landing stories listed)
    - .planning/phases/213.5-.../213.5-CONTEXT.md D-17 (Storybook 8 CSF3, ≥4 named landing stories)
    - .planning/phases/213.4-.../213.4-07-PLAN.md Task 3 (precedent NEW story file pattern, meta + StoryObj typing)
    - app/(marketing)/integrations/claude/page.module.css (for context — no story-side overrides needed)
  </read_first>
  <files>app/(marketing)/integrations/claude/page.stories.tsx</files>
  <action>
**Create NEW `app/(marketing)/integrations/claude/page.stories.tsx`:**

The page is a synchronous Server Component (no `searchParams`, no `async`). Stories render it directly without an async wrapper.

```tsx
import type { Meta, StoryObj } from '@storybook/react';
import ClaudeIntegrationLanding from './page';

const meta = {
  title: '213.5/ClaudeLanding',
  component: ClaudeIntegrationLanding,
  parameters: {
    layout: 'fullscreen',
    docs: {
      description: {
        component:
          'Phase 213.5 Plan-01 — Claude Marketplace landing page redesigned to DESIGN.md v1.1.0 canonical contract. Composes .c-card--feature hero (32px radius hero exception per D-15), .c-card--interactive + .c-chip-protocol per tool, .c-button c-button--primary (×2: hero + final-cta) + .c-button c-button--tertiary (Read the quickstart), .c-code-inline (MCP URL), .t-lead (hero sub). DOM + aria-labels + TOOLS array (10 MCP allow-list slugs) + copy preserved verbatim from Phase 200 baseline (voice classifier ≥100).',
      },
    },
  },
} satisfies Meta<typeof ClaudeIntegrationLanding>;

export default meta;
type Story = StoryObj<typeof meta>;

export const Default: Story = {
  parameters: {
    docs: {
      description: {
        story:
          'Full landing page at desktop viewport. Hero panel (.c-card--feature, 32px radius), value-props grid, tool grid (10 tools as .c-card--interactive cards with .c-chip-protocol slugs), install section, final-cta. Mint primary CTA appears twice (hero + final-cta) — scroll-separated per L-6.',
      },
    },
  },
};

export const ToolListExpanded: Story = {
  parameters: {
    viewport: { defaultViewport: 'responsive' },
    docs: {
      description: {
        story:
          'All 10 MCP tool cards visible (no collapse). Each card composes .c-card--interactive (hover-lift via primitive) + .c-chip-protocol (slug rendered with [brackets] via primitive ::before/::after). Confirms D-21 MCP allow-list slug preservation: draft_message, plan_campaign, research_audience, run_neuro_audit, generate_brief, audit_claim, list_pain_points, rank_execution_queue, schedule_post, explain_literacy.',
      },
    },
  },
};

export const CTAFocus: Story = {
  parameters: {
    pseudo: { focusVisible: '.c-button--primary' },
    docs: {
      description: {
        story:
          'Primary CTA "Try the demo" in :focus-visible state. Demonstrates global focus ring (2px solid #00D9A3 + 2px offset per DESIGN.md non-negotiable rule 6) inherited from globals.css :focus-visible. Mint signal is the single CTA + focus color — never suppressed.',
      },
    },
  },
};

export const MobileBreakpoint: Story = {
  parameters: {
    viewport: { defaultViewport: 'mobile1' },
    docs: {
      description: {
        story:
          'Viewport 320px (mobile1). Hero stacks vertical, CTA row wraps (flex-wrap: wrap), tool grid collapses to 1-column (grid auto-fill minmax(200px, 1fr)). Confirms responsive token-driven layout — no scope-local media query needed.',
      },
    },
  },
};
```

**KEY rules (per L-8 + D-17):**
- 4 named exports `Default` / `ToolListExpanded` / `CTAFocus` / `MobileBreakpoint` matching UI-SPEC "Storybook State Coverage" verbatim.
- `meta.title = '213.5/ClaudeLanding'` exactly per L-8.
- `meta.component = ClaudeIntegrationLanding` (default export from `./page`).
- `parameters.layout = 'fullscreen'` for full-width landing render.
- `parameters.theme: "legacy"` MUST NOT appear.
- `pseudo.focusVisible` selector for CTAFocus story relies on `@storybook/addon-pseudo-states` (already configured per 213.x stories — falls back to no-op if addon absent without breaking build).
- `viewport.defaultViewport: 'mobile1'` is a Storybook-default viewport key (320px width).
- NO emoji, NO exclamation in story descriptions.

After edit: `npm run build-storybook` MUST exit 0 + `npm test` MUST exit 0 (Phase 200 + 213.x suites stay green per D-19).
  </action>
  <verify>
    <automated>test -f "app/(marketing)/integrations/claude/page.stories.tsx" && grep -qE "^export const Default:" "app/(marketing)/integrations/claude/page.stories.tsx" && grep -qE "^export const ToolListExpanded:" "app/(marketing)/integrations/claude/page.stories.tsx" && grep -qE "^export const CTAFocus:" "app/(marketing)/integrations/claude/page.stories.tsx" && grep -qE "^export const MobileBreakpoint:" "app/(marketing)/integrations/claude/page.stories.tsx" && grep -q "title: '213.5/ClaudeLanding'" "app/(marketing)/integrations/claude/page.stories.tsx" && grep -q "export default meta" "app/(marketing)/integrations/claude/page.stories.tsx" && ! grep -qE 'parameters\.theme.*"legacy"' "app/(marketing)/integrations/claude/page.stories.tsx" && [ "$(grep -cE '^export const (Default|ToolListExpanded|CTAFocus|MobileBreakpoint):' "app/(marketing)/integrations/claude/page.stories.tsx")" = "4" ] && npm run build-storybook && npm test && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **L-8 (4 named state exports):** all of `Default`, `ToolListExpanded`, `CTAFocus`, `MobileBreakpoint` present.
    - **L-8 (meta.title):** `'213.5/ClaudeLanding'` matches verbatim.
    - **D-19 Phase 200 tests stay green:** `npm test` exits 0 (voice classifier 100/100 preserved + structure + a11y + allow-list).
    - **D-22 build-storybook gate:** `npm run build-storybook` exits 0.
    - **No legacy theme; no emoji; no exclamation.**
  </acceptance_criteria>
  <done>NEW `page.stories.tsx` created with 4 named state stories. Atomic commit: `feat(213.5-01): create marketing/claude/page.stories.tsx with Default/ToolListExpanded/CTAFocus/MobileBreakpoint (L-8)`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public→/integrations/claude | Public-facing marketing landing (no auth); marketplace SEO surface |
| user→demo CTA | Primary CTA hands off to `/integrations/claude/demo` (Plan 213.5-02 surface) |
| user→quickstart link | Tertiary CTA links to `/docs/quickstart` (existing docs surface, out of scope) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213.5-01-01 | Information Disclosure | MCP server URL `https://markos.dev/api/mcp/session` rendered in install copy | accept | URL is intentionally public per D-21 (MCP allow-list); no secrets exposed; rate-limited at edge |
| T-213.5-01-02 | Spoofing | TOOLS array (10 MCP slugs) rendered as static text | accept | Server-rendered constant; no user input; D-21 enforces accuracy against MCP server allow-list |
| T-213.5-01-03 | Tampering | aria-label attrs on hero CTAs | mitigate | Preserved verbatim from Phase 200 baseline; a11y test suite (Plan 213.5-03) grep-asserts presence |
</threat_model>

<verification>
```bash
# Token-only authoring (L-1)
! grep -nE '#[0-9a-fA-F]{3,8}' "app/(marketing)/integrations/claude/page.module.css"
! grep -nE '[^0-9](10|12|13|14|15|18|20|22|28|36|40|45)px' "app/(marketing)/integrations/claude/page.module.css"
! grep -nE '(Sora|Space Grotesk|gradient|box-shadow|transform:\s*translate)' "app/(marketing)/integrations/claude/page.module.css"

# Primitive composition (L-2..L-7)
grep -qE '"c-card c-card--feature"' "app/(marketing)/integrations/claude/page.tsx"
grep -qE '"c-card c-card--interactive"' "app/(marketing)/integrations/claude/page.tsx"
grep -qE '"c-chip-protocol"' "app/(marketing)/integrations/claude/page.tsx"
[ "$(grep -c 'c-button c-button--primary' "app/(marketing)/integrations/claude/page.tsx")" = "2" ]   # L-6
grep -qE '"c-button c-button--tertiary"' "app/(marketing)/integrations/claude/page.tsx"
grep -qE '"c-code-inline"' "app/(marketing)/integrations/claude/page.tsx"

# Orphan elimination
! grep -qE '"(claude-integration-landing|hero|hero-sub|cta-row|tool-grid|btn|btn-primary|btn-secondary)"' "app/(marketing)/integrations/claude/page.tsx"

# Story file (L-8)
[ "$(grep -cE '^export const (Default|ToolListExpanded|CTAFocus|MobileBreakpoint):' "app/(marketing)/integrations/claude/page.stories.tsx")" = "4" ]

# Voice + build + Phase 200 suite (D-11, D-19, D-22)
npm run build && npm run build-storybook && npm test
```
</verification>

<success_criteria>
UI-SPEC AC L-1..L-8 close. DOM + aria + TOOLS array + copy preserved per D-11/D-20/D-21. Voice classifier ≥100 preserved. Banned-lexicon = 0. 3 atomic commits.
</success_criteria>

<output>
After completion, create `.planning/phases/213.5-marketing-claude-landing-demo-sandbox-to-design-md-tokens-close-phase-200-retroactive-ui-debt/213.5-01-SUMMARY.md`. Hand-off to Plan 213.5-02 (demo sandbox).
</output>
