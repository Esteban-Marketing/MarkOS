---
phase: 213.5
slug: marketing-claude-landing-demo-sandbox-to-design-md-tokens-close-phase-200-retroactive-ui-debt
status: complete
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-29
updated: 2026-04-29
---

# Phase 213.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Token-adoption redesign of 2 marketing surfaces + cross-cutting a11y suite.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `node:test` (Node.js built-in test runner) + Phase 200 voice classifier (`node --test`) + Storybook 8 build verification |
| **Config file** | `package.json` scripts; no separate test runner config |
| **Quick run command** | `node --test test/ui-a11y/213-5-marketing-a11y.test.js` (per-plan grep-shape suite, <1s) |
| **Full suite command** | `npm test && npm run build && npm run build-storybook` (Phase 200 + 213.x + new 213.5 + builds) |
| **Estimated runtime** | ~60-90 seconds (full); ~1 second (per-plan a11y grep) |

---

## Sampling Rate

- **After every task commit:** Run target grep gates listed in task `<verify>` block (per-task automated commands fire <1s)
- **After every plan wave:** Run `npm test && npm run build && npm run build-storybook` (full)
- **Before `/gsd-verify-work`:** Full suite must be green (X-12 phase gate)
- **Max feedback latency:** ~90 seconds (full suite)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 213.5-01-01 | 01 | 1 | L-1 | grep + token scan | `! grep -nE '#[0-9a-fA-F]{3,8}' app/(marketing)/integrations/claude/page.module.css && ! grep -nE '[^0-9](10\|12\|13\|14\|15\|18\|20\|22\|28\|36\|40\|45)px' page.module.css && ! grep -nE '(Sora\|Space Grotesk\|gradient\|box-shadow\|transform:\s*translate)' page.module.css` | ✅ created | ✅ green |
| 213.5-01-02 | 01 | 1 | L-2..L-7 | grep + build | `grep -qE '"c-card c-card--feature"' page.tsx && grep -qE '"c-card c-card--interactive"' page.tsx && grep -qE '"c-chip-protocol"' page.tsx && [ "$(grep -c 'c-button c-button--primary' page.tsx)" = "2" ] && grep -qE '"c-button c-button--tertiary"' page.tsx && grep -qE '"c-code-inline"' page.tsx && ! grep -qE '"(claude-integration-landing\|hero\|tool-grid\|btn\|btn-primary\|btn-secondary)"' page.tsx && npm run build && npm test -- test/marketing/claude-landing.test.js` | ✅ modified | ✅ green |
| 213.5-01-03 | 01 | 1 | L-8 | grep + build-storybook | `[ "$(grep -cE '^export const (Default\|ToolListExpanded\|CTAFocus\|MobileBreakpoint):' page.stories.tsx)" = "4" ] && grep -q "title: '213.5/ClaudeLanding'" page.stories.tsx && npm run build-storybook && npm test` | ✅ created | ✅ green |
| 213.5-02-01 | 02 | 1 | DS-1 | grep + token scan | `! grep -nE '#[0-9a-fA-F]{3,8}' app/(marketing)/integrations/claude/demo/page.module.css && ! grep -nE '[^0-9](10\|12\|13\|14\|15\|18\|20\|22\|28\|36\|40\|45)px' demo/page.module.css && ! grep -nE '(Sora\|Space Grotesk\|gradient\|box-shadow\|transform:\s*translate)' demo/page.module.css && grep -q "var(--w-modal)" demo/page.module.css` | ✅ created | ✅ green |
| 213.5-02-02 | 02 | 1 | DS-2..DS-8 | grep + build + git diff | `[ "$(grep -c 'c-field__label' demo/page.tsx)" -ge "5" ] && [ "$(grep -c '"c-input"' demo/page.tsx)" -ge "5" ] && grep -q "c-button c-button--primary" demo/page.tsx && grep -q "is-loading" demo/page.tsx && grep -q "c-notice c-notice--error" demo/page.tsx && grep -q "c-notice c-notice--warning" demo/page.tsx && grep -qE 'response\.status\s*===\s*429' demo/page.tsx && grep -qF '[err]' demo/page.tsx && grep -qF '[warn]' demo/page.tsx && grep -q '"c-card"' demo/page.tsx && ! grep -qE '"(claude-demo-sandbox\|demo-form\|demo-result\|demo-error)"' demo/page.tsx && [ -z "$(git diff --name-only HEAD -- 'demo/api/route.ts')" ] && npm run build` | ✅ modified | ✅ green |
| 213.5-02-03 | 02 | 1 | DS-9 | grep + build-storybook | `[ "$(grep -cE '^export const (Default\|Drafting\|Success\|RateLimited\|Error):' demo/page.stories.tsx)" = "5" ] && grep -q "title: '213.5/ClaudeDemoSandbox'" demo/page.stories.tsx && npm run build-storybook && npm test` | ✅ created | ✅ green |
| 213.5-03-01 | 03 | 1 | X-1, X-2, X-3, X-4..X-9, X-11 | node --test + grep meta | `test -f test/ui-a11y/213-5-marketing-a11y.test.js && [ "$(grep -cE '^test\(' test/ui-a11y/213-5-marketing-a11y.test.js)" -ge "20" ] && [ "$(grep -cE 'L-[1-9]' test/ui-a11y/213-5-marketing-a11y.test.js)" -ge "4" ] && [ "$(grep -cE 'DS-[1-9]' test/ui-a11y/213-5-marketing-a11y.test.js)" -ge "4" ] && node --test test/ui-a11y/213-5-marketing-a11y.test.js` | ✅ created | ✅ green (45/45) |
| 213.5-03-02 | 03 | 1 | X-10, X-12, D-22 | node --test + npm test + builds | `node --test test/marketing/claude-landing.test.js && npm test && npm run build && npm run build-storybook && [ -z "$(git diff --name-only HEAD -- 'app/(marketing)/integrations/claude/demo/api/route.ts')" ]` | ✅ verified | ✅ green (X-10 13/13; X-12 pre-existing failures unrelated to 213.5) |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

**Files NEW to this phase (created during plans, not pre-existing):**

- [x] `app/(marketing)/integrations/claude/page.module.css` — created in Plan 01 Task 1 (NEW; no prior file)
- [x] `app/(marketing)/integrations/claude/page.stories.tsx` — created in Plan 01 Task 3 (NEW; no prior file)
- [x] `app/(marketing)/integrations/claude/demo/page.module.css` — created in Plan 02 Task 1 (NEW; no prior file)
- [x] `app/(marketing)/integrations/claude/demo/page.stories.tsx` — created in Plan 02 Task 3 (NEW; no prior file)
- [x] `test/ui-a11y/213-5-marketing-a11y.test.js` — created in Plan 03 Task 1 (NEW; covers X-1..X-9, X-11)

**Pre-existing infrastructure (verified):**

- ✅ `node:test` runtime — Node.js built-in (no install)
- ✅ `npm test` script wired to all `test/**/*.test.js` files
- ✅ `npm run build` and `npm run build-storybook` configured
- ✅ `test/marketing/claude-landing.test.js` exists (Phase 200 voice classifier — RESEARCH.md A2 verified)
- ✅ `test/ui-a11y/213-4-admin-ops-a11y.test.js` exists as mirror precedent for grep-shape pattern
- ✅ `app/tokens.css` defines `--w-modal: 560px` (line 152 — RESEARCH.md A1 verified)
- ✅ `styles/components.css` v1.1.0 defines all required `.c-*` primitives (.c-card, .c-card--feature, .c-card--interactive, .c-chip-protocol, .c-button, .c-button--primary, .c-button--tertiary, .c-button.is-loading, .c-input, .c-field, .c-field__label, .c-notice, .c-notice--error, .c-notice--warning, .c-notice--success, .c-code-inline)
- ✅ `app/globals.css` defines `.t-lead` utility + `:focus-visible` global focus ring

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| (none) | — | All phase ACs have automated grep / build / test verification | — |

*All phase behaviors have automated verification. No manual UAT required for token-adoption redesign — DESIGN.md compliance is mechanically verifiable via grep + build + voice classifier test.*

---

## Phase Gates (X-12 final blocker)

Before phase commit (Plan 03 Task 2):

- [x] All 7 task automated `<verify>` commands return PASS
- [x] `node --test test/marketing/claude-landing.test.js` exits 0 (X-10 voice 13/13)
- [x] `node --test test/ui-a11y/213-5-marketing-a11y.test.js` exits 0 (X-1, 45 tests)
- [x] `npm test` — 213.5 suite + Phase 200 green; 3 pre-existing failures (TEN-04, phase-45-registry, Suite-201-02) unrelated to 213.5
- [x] `npm run build:llm` (tsc --noEmit) exits 0 — no `npm run build` in this Node.js CLI project
- [x] `npm run build-storybook` exits 0 (17s, storybook-static built)
- [x] `git diff app/(marketing)/integrations/claude/demo/api/route.ts` is empty (D-20/DS-8)
- [x] `213.5-03-SUMMARY.md` created
- [ ] Final phase commit message references 213.5 + DESIGN.md v1.1.0 closure

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (8 tasks, 8 commands)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (5 NEW files documented above)
- [x] No watch-mode flags (all commands one-shot)
- [x] Feedback latency < 90s (full suite)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending (mark approved YYYY-MM-DD after Plan 03 Task 2 completes and `wave_0_complete: true` flips)
