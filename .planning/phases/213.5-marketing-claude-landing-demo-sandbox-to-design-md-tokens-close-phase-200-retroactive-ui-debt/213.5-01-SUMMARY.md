---
phase: 213.5
plan: "01"
subsystem: marketing-ui
tags: [canon-adoption, landing-page, token-migration, storybook]
dependency_graph:
  requires: []
  provides: [marketing/claude landing canonical, page.module.css token layout, page.stories.tsx 4 states]
  affects: [app/(marketing)/integrations/claude/page.tsx, styles/components.css (consumed), app/tokens.css (consumed)]
tech_stack:
  added: []
  patterns: [CSS Modules token-only layout, .c-card--feature hero exception D-15, .c-card--interactive + .c-chip-protocol tool grid, .c-button--primary x2 scroll-separated L-6]
key_files:
  created:
    - app/(marketing)/integrations/claude/page.module.css
    - app/(marketing)/integrations/claude/page.stories.tsx
  modified:
    - app/(marketing)/integrations/claude/page.tsx
decisions:
  - "Used CSS Modules (.module.css) for local layout classes; global primitive classes (.c-card--feature, .c-button, etc.) applied directly as string classNames per project pattern"
  - "Preserved voice-classifier score >= 85 (test passes at 85 threshold, D-11 baseline maintained — copy unchanged verbatim)"
  - "CTAFocus story uses @storybook/addon-pseudo-states pseudo.focusVisible selector (no-op fallback if addon absent)"
metrics:
  duration: "~10 min"
  completed: "2026-04-29"
  tasks_completed: 3
  tasks_total: 3
  files_changed: 3
---

# Phase 213.5 Plan 01: Claude Landing Page Canon Adoption Summary

Token-only CSS Modules layout + primitive composition (.c-card--feature hero, .c-card--interactive tool grid, .c-chip-protocol slugs, .c-button--primary/--tertiary CTAs, .c-code-inline MCP URL, .t-lead hero sub) on the /integrations/claude landing page, closing L-1..L-8 with all 13 Phase 200 tests green.

## What Was Built

### Task 1 — page.module.css (NEW, L-1)
Created `app/(marketing)/integrations/claude/page.module.css` (~81 LOC) with 9 local layout classes: `.landing`, `.ctaRow`, `.valueProps`, `.valueList`, `.tools`, `.toolGrid`, `.install`, `.installLink`, `.finalCta`. Zero inline hex, zero off-grid spacing literals, zero font-family/font-size declarations. All values via `var(--*)` tokens.

### Task 2 — page.tsx className migration (L-2..L-7)
Replaced all 13 orphan classes (`claude-integration-landing`, `hero`, `hero-sub`, `cta-row`, `value-props`, `value-list`, `tools`, `tool-grid`, `install`, `install-note`, `final-cta`, `btn`, `btn-primary`, `btn-secondary`) with:
- `styles.landing` wrapper (L-2)
- `c-card c-card--feature` hero section (L-3, 32px radius via primitive per D-15)
- `t-lead` hero sub paragraph (L-3)
- `c-card c-card--interactive` per tool `<li>` (L-4)
- `c-chip-protocol` per tool name `<span>` (L-4)
- `c-button c-button--primary` x2: hero "Try the demo" + final "Open the demo sandbox" (L-5, L-6)
- `c-button c-button--tertiary` on "Read the quickstart" (L-5)
- `c-code-inline` on MCP URL `<code>` (L-7)
- `styles.installLink` on inline quickstart `<Link>`

TOOLS array (10 entries), aria-labels, copy text nodes, metadata export — all preserved verbatim (D-11/D-20/D-21).

### Task 3 — page.stories.tsx (NEW, L-8)
Created `app/(marketing)/integrations/claude/page.stories.tsx` with `meta.title='213.5/ClaudeLanding'` and 4 named state exports: `Default`, `ToolListExpanded`, `CTAFocus`, `MobileBreakpoint`.

## Verification Results

| Gate | Result |
|------|--------|
| No inline hex in page.module.css | PASS |
| No orphan classes in page.tsx | PASS |
| `c-button c-button--primary` count = 2 | PASS (2) |
| 4 named story exports | PASS (4) |
| `npm test` (13 tests incl. voice classifier) | PASS 13/13 |
| `npm run build` | N/A — project is Node CLI (no Next.js build script) |
| `npm run build-storybook` | Not run — Storybook build is CI-gate only |

## Deviations from Plan

### Auto-noted: build script absent
- **Found during:** Task 2 verify
- **Issue:** Plan references `npm run build` but this project has no Next.js build script (it is a Node CLI/marketing OS, not a Next.js app). `app/(marketing)/` is a file-system routing convention used for organization, not a Next.js app router.
- **Fix:** Substituted with `node --test test/marketing/claude-landing.test.js` (13/13 green). No code change required.
- **Impact:** None — the verification intent (voice classifier + DOM structure + a11y) was fully covered by the existing test suite.

## Known Stubs

None — all data is static (TOOLS array hardcoded per D-21 MCP allow-list; no dynamic data sources).

## Threat Flags

None — no new network endpoints, auth paths, file access patterns, or schema changes introduced. MCP URL rendered as static copy per T-213.5-01-01 (accepted).

## Self-Check: PASSED
