---
phase: 213.5
plan: "02"
subsystem: marketing-ui
tags: [canon-adoption, demo-sandbox, token-migration, storybook, 429-rate-limit]
dependency_graph:
  requires: [213.5-01]
  provides: [marketing/claude/demo canonical, demo/page.module.css token layout, demo/page.tsx 429 branch + primitive composition, demo/page.stories.tsx 5 states]
  affects: [app/(marketing)/integrations/claude/demo/page.tsx, app/(marketing)/integrations/claude/demo/page.module.css (NEW), app/(marketing)/integrations/claude/demo/page.stories.tsx (NEW)]
tech_stack:
  added: []
  patterns: [CSS Modules token-only layout, .c-field + .c-field__label + .c-input form trio, .c-button--primary + .is-loading state signal, .c-notice c-notice--{warning,error} bracketed-glyph alerts, .c-card result panel, 429 rate-limit branch before generic !response.ok]
key_files:
  created:
    - app/(marketing)/integrations/claude/demo/page.module.css
    - app/(marketing)/integrations/claude/demo/page.stories.tsx
  modified:
    - app/(marketing)/integrations/claude/demo/page.tsx
decisions:
  - "429 branch placed BEFORE await response.json() call and BEFORE !response.ok so rate-limit state never collides with generic error path; setRateLimited(false) reset at top of onSubmit alongside setErrorMessage(null)"
  - "fieldGroup module class composed alongside c-field global primitive via template literal (no clsx) — per D-03 carry from 213.x pattern"
  - "Stories use CSF3 parameters.docs.description approach (no MSW/play functions) — deterministic for CI; MSW mocking deferred to Phase 220+ Chromatic baseline work per CONTEXT D-17"
metrics:
  duration: "~8 min"
  completed: "2026-04-29"
  tasks_completed: 3
  tasks_total: 3
  files_changed: 3
---

# Phase 213.5 Plan 02: Demo Sandbox + 429 Rate-Limit Branch Summary

Token-only CSS Modules layout + canonical primitive composition (.c-field/.c-input form trio, .c-button--primary/.is-loading submit, .c-notice c-notice--warning/--error bracketed-glyph alerts, .c-card result panel) on the /integrations/claude/demo sandbox, with critical 429 rate-limit branch inserted in onSubmit before generic error path, closing DS-1..DS-9.

## What Was Built

### Task 1 — page.module.css (NEW, DS-1)
Created `app/(marketing)/integrations/claude/demo/page.module.css` (~46 LOC) with 5 local layout classes: `.sandbox`, `.intro`, `.demoForm`, `.fieldGroup`, `.result`. Zero inline hex, zero off-grid spacing literals, zero font-family/font-size declarations. All values via `var(--*)` tokens. `--w-modal` (560px) centers the sandbox surface; `--space-xl` page padding-block + `--space-md` section gaps deliver whitespace-as-primitive.

### Task 2 — page.tsx canonical rewrite (DS-2..DS-8)
Replaced all orphan classes (`claude-demo-sandbox`, `demo-error`, `demo-result`) and bare `<label>`+`<input>` pairs with:

- `styles.sandbox` wrapper on `<main>` (DS-2)
- `styles.intro` on `<header>` for h1 + intro paragraph
- `t-lead` + `c-code-inline` utilities on intro paragraph (copy preserved verbatim)
- 5x `.c-field` + `.c-field__label` + `.c-input` trios on channel/audience/pain/promise/brand (DS-3)
- `.c-button c-button--primary` + conditional `.is-loading` + `aria-busy={loading}` on submit (DS-4)
- `<p role="alert" className="c-notice c-notice--error">[err] {errorMessage}</p>` for API errors (DS-5)
- NEW `rateLimited` boolean state hook; `response.status === 429` branch in `onSubmit` runs BEFORE `await response.json()` and BEFORE `!response.ok` branch (DS-6)
- `<p role="alert" className="c-notice c-notice--warning">[warn] Demo rate limit reached. Try again in 60s.</p>` for 429 (DS-6)
- `<div className="c-card">` wrapping result articles inside `<section className={styles.result}>` (DS-7)
- `demo/api/route.ts` untouched; `fetch('/integrations/claude/demo/api', ...)` + body shape `{ tool: 'draft_message', arguments: brief }` preserved verbatim (DS-8)

All form field IDs (`demo-channel/audience/pain/promise/brand`), `name` attrs, `aria-label="Draft brief form"`, `noValidate`, `aria-labelledby="demo-result-heading"`, `aria-label` on result articles — preserved verbatim.

### Task 3 — page.stories.tsx (NEW, DS-9)
Created `app/(marketing)/integrations/claude/demo/page.stories.tsx` with `meta.title='213.5/ClaudeDemoSandbox'`, `parameters.layout='fullscreen'`, and 5 named CSF3 story exports: `Default`, `Drafting`, `Success`, `RateLimited`, `Error`. Each story documents the rendered state via `parameters.docs.description.story`. No MSW handlers (deferred to Phase 220+), no legacy theme param, no emoji, no exclamation.

## Verification Results

| Gate | Result |
|------|--------|
| No inline hex in page.module.css | PASS |
| No off-grid spacing in page.module.css | PASS |
| 5x c-field__label in page.tsx | PASS (5) |
| 5x c-input in page.tsx | PASS (5) |
| c-button c-button--primary + is-loading + aria-busy | PASS |
| c-notice c-notice--error + [err] glyph | PASS |
| c-notice c-notice--warning + [warn] glyph | PASS |
| response.status === 429 branch in onSubmit | PASS |
| setRateLimited state hook present | PASS |
| c-card result panel | PASS |
| Orphan classes eliminated | PASS |
| route.ts untouched (DS-8) | PASS |
| fetch wiring + body shape preserved | PASS |
| Banned-lexicon scan = 0 | PASS |
| 5 named story exports in page.stories.tsx | PASS |
| meta.title='213.5/ClaudeDemoSandbox' | PASS |
| npm test (13/13 Phase 200 suite) | PASS 13/13 |

## Deviations from Plan

None — plan executed exactly as written. The 429 branch was inserted precisely as specified in the plan interfaces block (BEFORE `await response.json()`, BEFORE `!response.ok`).

## Known Stubs

None — all data is live (form fields wired to state; fetch call to real API route). Story state depictions are documented via description strings (MSW mocking for visual states deferred per D-17/CONTEXT).

## Threat Flags

None — no new network endpoints, auth paths, file access patterns, or schema changes introduced. `demo/api/route.ts` untouched per DS-8/D-20. 429 rate-limit client branch is a display-only change; server-side rate-limiting remains in route.ts unchanged.

## Self-Check: PASSED
