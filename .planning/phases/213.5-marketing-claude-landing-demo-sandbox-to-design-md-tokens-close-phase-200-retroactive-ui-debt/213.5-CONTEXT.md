# Phase 213.5: marketing claude-landing + demo sandbox to DESIGN.md tokens — close Phase 200 retroactive UI debt — Context

**Gathered:** 2026-04-29 (auto mode — UI-SPEC.md will lock visual contract via `/gsd-ui-phase 213.5`)
**Status:** Ready for UI-SPEC then planning

<domain>
## Phase Boundary

Close Phase 200 retroactive UI debt (per `200-UI-SPEC.md` D-01 — orphan class names render as unstyled semantic HTML). Surfaces are customer-facing Claude Marketplace listing + demo sandbox — first impression at marketplace launch must be canonical DESIGN.md v1.1.0.

**In-scope (CREATE module.css NEW + update tsx classNames):**

| Surface | TSX LOC | Existing CSS | Notes |
|---|---:|---|---|
| `app/(marketing)/integrations/claude/page.tsx` | 122 | NONE (orphan classnames `.btn`, `.btn-primary`, `.hero`, `.tool-grid`, `.claude-integration-landing`) | Marketplace landing page; `<h1>` "Ship drafts from Claude." + tool grid + dual CTA |
| `app/(marketing)/integrations/claude/demo/page.tsx` | 165 | NONE (orphan classnames `.demo-form`, `.demo-result`, `.demo-error`, `.demo-loading`) | Interactive demo sandbox; pre-fills tool input + calls `/integrations/claude/demo/api` route |

**OUT-of-scope:**
- `demo/api/route.ts` — backend API, no UI
- Other marketing pages (`(marketing)/docs/*`, `(marketing)/signup/*` — already redesigned in 213.2)
- Marketing chrome layout (no `app/(marketing)/layout.tsx` exists — marketing pages inherit root `app/layout.tsx` which loads `globals.css` + `tokens.css` cascade; verified)

**Companion artifacts to create (in scope):**
- 2 NEW `*.module.css` files (one per surface)
- 2 NEW `*.stories.tsx` files (Storybook coverage)
- `test/ui-a11y/213-5-marketing-a11y.test.js` — grep-shape a11y suite (≥20 tests, ≥4 AC# mentions per surface)

</domain>

<decisions>
## Implementation Decisions

### Visual contract source
- **D-01:** Visual contract locked in `213.5-UI-SPEC.md` (to be generated). Mirror 213.2-UI-SPEC.md structure (smaller — 2 surfaces + cross-cutting = ~12-18 ACs).
- **D-02:** UI-SPEC.md cites DESIGN.md v1.1.0 token names. AC structure: L-N (landing), DS-N (demo sandbox), X-N (cross-cutting).

### Carrying forward from 213.1–213.4 (locked)
- **D-03..D-09b:** All 213.x decisions carry. No `clsx`. Atomic commits. DOM tree + props unchanged on `.tsx` (className-side + copy revisions only). Banned lexicon = 0. Token-only authoring (zero hex outside whitelist). Mint-as-text via `--color-primary-text`. `.c-notice` primitive for any banner-shaped surface (demo error states use `.c-notice c-notice--error` with `[err]` glyph). Bracketed-glyph state coding mandatory.

### Marketing context register
- **D-10:** Marketing surface tone = customer-facing (NOT operator/internal). Allowed: aspirational language, single mint signal as CTA, hero panel with `.c-card--feature` (32px radius — same exception as 404-workspace per 213.4 D-13). Forbidden (carry from 213.x): banned-lexicon list, exclamation points, emoji, "Please" softeners, gradients, drop-shadows, hover-translate jiggle.
- **D-11:** Phase 200 voice classifier 100/100 baseline preserved — copy revisions in this phase MUST keep voice score ≥ 100 (no regression). Verify post-edit via existing voice classifier test if it exists.

### Wave structure
- **D-12:** Single wave. **3 plans** (smallest 213.x phase). Sequential per atomic-commit cadence.
  - **Plan 213.5-01:** Landing page — create `app/(marketing)/integrations/claude/page.module.css` + update `page.tsx` classNames + new `page.stories.tsx` (≥4 named state stories: Default, ToolListExpanded, CTAFocus, MobileBreakpoint)
  - **Plan 213.5-02:** Demo sandbox — create `app/(marketing)/integrations/claude/demo/page.module.css` + update `demo/page.tsx` classNames + new `demo/page.stories.tsx` (≥5 named state stories: Default, Drafting, Success, RateLimited, Error)
  - **Plan 213.5-03:** Cross-cutting — `test/ui-a11y/213-5-marketing-a11y.test.js` (≥20 tests, ≥4 AC# mentions) + verify `app/(marketing)/integrations/claude/page.tsx` voice classifier ≥ 100/100 preserved + final phase commit

### Form primitives + state signals (cross-cutting — mirrors 213.x)
- **D-13:** Demo sandbox form (`demo/page.tsx` `<form>` element) composes `.c-input` (textarea) + `.c-field{,__label,__help,__error}` + `.c-button c-button--primary` (Draft message CTA) + `.c-button c-button--tertiary` (Reset/Try another).
- **D-14:** Demo error states: `.c-notice c-notice--error` with `[err]` glyph for API failures + `.c-notice c-notice--warning` with `[warn]` glyph for rate-limit. Loading state: `.c-button.is-loading` + `[info]` text or skeleton.
- **D-15:** Landing page hero: `.c-card--feature` (32px radius, hero panel — D-13 exception same as 404-workspace). Tool grid: `.c-card--interactive` per tool with `.c-chip-protocol` for tool slugs (e.g., `[tool:claim_audit]`). Dual CTA: `.c-button c-button--primary` (Try the demo) + `.c-button c-button--tertiary` (Read the quickstart).
- **D-16:** No `.c-table` (D-14 carry from 213.3). No `.c-card--feature` outside hero panel.

### Test strategy
- **D-17:** Storybook 8 CSF3. ≥9 named stories total across 2 surfaces (4 landing + 5 demo).
- **D-18:** New `test/ui-a11y/213-5-marketing-a11y.test.js` mirrors 213-4-admin-ops-a11y.test.js. Coverage: token-only authoring (zero hex), no Sora/Space Grotesk, no gradients/shadows/translate, primitive composition, .c-notice for demo error/rate-limit states, bracketed-glyph state coding, banned lexicon = 0, voice classifier preserved, story file existence.
- **D-19:** Existing Phase 200 tests (voice classifier, structure, ARIA, allow-list checks) MUST stay green. Run `npm test` after each plan.

### Wiring preservation (critical)
- **D-20:** Phase 200-08 `demo/api/route.ts` API contract unchanged — only client-side classNames + copy revisions in `demo/page.tsx`. `fetch()` to API route preserved verbatim.
- **D-21:** Phase 200-04 onboarding-seed.json + Phase 200-06 MCP server allow-list assertions in landing copy must stay accurate (don't drop tool slugs that the MCP server actually exposes).

### Executor verification gates (per plan, mirrors 213.x)
- **D-22:** Per-plan acceptance gates:
  - `grep -E "#[0-9a-fA-F]{3,8}" {created module.css}` returns 0 (token-only)
  - `grep -E "[^0-9](10|12|14|18|20|22|28|36|40|45)px" {created module.css}` returns 0 (off-grid eliminated)
  - `! grep -qE "Sora|Space Grotesk|gradient|box-shadow.*rgba|transform:\s*translate" {created module.css}`
  - `! grep -qE "btn-primary|tool-grid|claude-integration-landing|demo-form|demo-result|demo-error|demo-loading" {tsx}` (orphan class names purged)
  - `npm run build` exits 0
  - `npm run build-storybook` exits 0
  - `npm test` exits 0 (Phase 200 voice/structure/a11y tests stay green)
  - `npm run test:ui-a11y -- test/ui-a11y/213-5-marketing-a11y.test.js` exits 0 (after plan 03)
  - Banned lexicon scan = 0 in modified `.tsx`

### Claude's Discretion
- Storybook story `args` shape (mirror 213.x patterns)
- Whether landing page extracts a `ClaudeLandingHero` sub-component (recommend NO — 122 LOC tsx is small enough; keep inline)
- Demo sandbox extraction (recommend NO — 165 LOC; client-component logic stays in page.tsx)
- Loading skeleton style for demo (use `.c-card` reduced-opacity)
- Empty-state copy

### Folded Todos
None.

</decisions>

<canonical_refs>
## Canonical References

### Visual contract
- `DESIGN.md` (repo root, v1.1.0)
- `213.5-UI-SPEC.md` (TO BE GENERATED)
- `.planning/phases/200-saas-readiness-wave-0/200-UI-SPEC.md` (commits 76406d8 + f0c1006 — debt log this phase closes)

### 213.x precedents
- `.planning/phases/213.4-.../213.4-{CONTEXT,UI-SPEC,RESEARCH}.md`
- `.planning/phases/213.4-.../213.4-07-PLAN.md` (404-workspace — closest analog: small surface + `.c-card--feature` exception)
- `.planning/phases/213.2-.../213.2-02-{PLAN,SUMMARY}.md` (signup — closest marketing-context precedent)

### Generated token artifacts (v1.1.0)
- `tokens/tokens.json` · `tokens/index.ts` · `tailwind.config.ts` · `app/tokens.css` · `app/globals.css` · `styles/components.css`

### Project guardrails
- `CLAUDE.md` (repo root) — "Visual Design Canon" section
- `.claude/get-shit-done/references/ui-brand.md`

### Existing 213.5 redesign targets
- `app/(marketing)/integrations/claude/page.tsx` (122 LOC, orphan classnames, no module.css)
- `app/(marketing)/integrations/claude/demo/page.tsx` (165 LOC, orphan classnames, no module.css)
- `app/(marketing)/integrations/claude/demo/api/route.ts` (OUT OF SCOPE — backend)

### Phase 200 deliverables (preserve)
- 200-08 voice classifier baseline (100/100)
- 200-08 ARIA + allow-list test contracts
- 200-04 onboarding-seed.json + 200-06 MCP allow-list (tool slugs in landing copy must stay accurate)

### Existing tests (MUST stay green)
- Phase 200 test suites (voice/structure/a11y/allow-list checks)
- 213-{1,2,3,4} a11y suites under `test/ui-a11y/`

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- All `.c-*` primitives (v1.1.0): `.c-card{,--interactive,--feature}`, `.c-button{,--primary,--tertiary,--destructive}`, `.c-input`, `.c-field{,__label,__help,__error}`, `.c-notice c-notice--{success,warning,error,info}`, `.c-chip-protocol`, `.c-status-dot`, `.c-modal+.c-backdrop`, `.c-toast`
- `app/globals.css` — `<h1>`, `<h2>`, `<p>` base typography; `.t-lead` utility; `:focus-visible` global
- `app/tokens.css` — `--color-primary-text` theme-aware (mint as text)

### Established Patterns (mirror 213.x)
- Module rewrite + tsx className update in lockstep
- Atomic commit per task
- Storybook 8 CSF3 + named state exports
- Banner via `.c-notice` primitive
- Bracketed-glyph state coding
- `node --test` shape regression
- Server/client boundary preserved
- `(pointer: coarse)` global rule inherits — modules don't redeclare

### Integration Points
- Root `app/layout.tsx` — marketing pages inherit (no `(marketing)/layout.tsx` exists). Verify `globals.css` + `tokens.css` loaded.
- `demo/api/route.ts` — backend stays untouched
- Phase 200 test suites — preserve voice 100/100 + ARIA + allow-list

</code_context>

<specifics>
## Specific Ideas

- **Landing page hero copy:** preserve "Ship drafts from Claude." + tool list. Mint CTA = "Try the demo" (single primary). Tertiary = "Read the quickstart". NO new copy register — voice classifier 100/100 baseline locked.
- **Demo sandbox states:** Default (form pre-filled), Drafting (button is-loading), Success (`.c-notice c-notice--success` `[ok] Draft generated.` + `.c-card` result panel), RateLimited (`.c-notice c-notice--warning` `[warn] Demo rate limit reached. Try again in 60s.`), Error (`.c-notice c-notice--error` `[err] {api error}.`).
- **Tool grid:** each tool card uses `.c-card--interactive` + `<h3>` tool name + `.c-chip-protocol` for slug + body description. Hover state via primitive (no local hover).
- **Mobile breakpoint:** test at `<md` (640px); landing hero stacks vertical, tool grid 1-col.
- **Forced-colors:** mint CTA + tool slug chip survive via `--color-primary-text` cascade (LinkText alias).

</specifics>

<deferred>
## Deferred Ideas

- Other marketing surfaces (`(marketing)/docs/*` if exists) — separate phase or fold into Phase 220 marketing wave
- Light-theme cascade verification on Storybook (deferred from 213.x)
- Chromatic baselines for marketing surfaces — capture during Phase 220 marketing wave
- Marketing brand register beyond DESIGN.md (e.g., aspirational hero with marketing-distinct typography) — Phase 220+ scope; this phase keeps strict canon adherence

### Reviewed Todos (not folded)
None — todo match check at plan-phase time.

</deferred>

---

*Phase: 213.5-marketing-claude-landing-demo-sandbox-to-design-md-tokens-close-phase-200-retroactive-ui-debt*
*Context gathered: 2026-04-29 (auto mode)*
