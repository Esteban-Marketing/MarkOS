---
phase: 219
plan: "02"
type: execute
wave: 2
depends_on:
  - "219-01"
files_modified:
  - supabase/migrations/86_b2b_expansion_customer_marketing.sql
  - lib/markos/expansion/contracts.ts
  - lib/markos/expansion/contracts.cjs
  - lib/markos/expansion/opportunities.ts
  - lib/markos/expansion/opportunities.cjs
  - lib/markos/expansion/customer-marketing.ts
  - lib/markos/expansion/enrollment.ts
  - lib/markos/expansion/signal-scanner.ts
  - lib/markos/expansion/index.cjs
  - contracts/F-230-b2b-account-expansion-v1.yaml
  - contracts/F-231-b2b-customer-marketing-v1.yaml
  - contracts/flow-registry.json
  - scripts/preconditions/219-02-check-upstream.cjs
  - test/b2b-219/expansion/opportunities-crud.test.js
  - test/b2b-219/expansion/outreach-trigger.test.js
  - test/b2b-219/expansion/program-activation.test.js
  - test/b2b-219/expansion/enrollment-dedup.test.js
  - test/b2b-219/expansion/signal-scanner.test.js
  - test/b2b-219/rls/expansion-cross-tenant-denial.test.js
  - test/b2b-219/openapi/parity-expansion.test.js
autonomous: true
requirements:
  - SG-03
  - SG-09
  - SG-12
  - EVD-01
  - EVD-02
  - EVD-03
  - EVD-04
  - QA-01
  - QA-02
  - QA-03
  - QA-04
must_haves:
  truths:
    - Wave 2 entry — assertUpstreamReady preflight (scripts/preconditions/219-02-check-upstream.cjs) consumed from Plan 01 architecture-lock surface; hard-fails if 219-01 helper module trees missing OR P214/P215/P218 tables missing
    - Migration 86 ships per V4.1.0-MIGRATION-SLOT-COORDINATION.md slot allocation; CONTAINS 3 SOR tables (account_expansion_opportunities, customer_marketing_programs, program_enrollments) + 2 DB-trigger functions + 2 hot-path indexes
    - account_expansion_opportunities RLS-enabled with tenant_id policy; customer_marketing_programs RLS-enabled; program_enrollments RLS-enabled with UNIQUE(tenant_id, program_id, account_id) dedup constraint
    - DB-trigger compliance — fn_expansion_outreach_requires_approval BEFORE UPDATE on account_expansion_opportunities raises EXCEPTION 'EXPANSION_OUTREACH_REQUIRES_APPROVAL' when NEW.status = 'outreach_sent' AND NEW.approval_id IS NULL
    - DB-trigger compliance #2 — fn_program_activation_requires_approval BEFORE UPDATE on customer_marketing_programs raises EXCEPTION 'PROGRAM_ACTIVATION_REQUIRES_APPROVAL' when NEW.status = 'active' AND OLD.status = 'draft' AND NEW.approved_at IS NULL
    - 9 trigger_signal ENUM values land per RESEARCH §6 Domain 2 (usage_growth, seat_pressure, champion_role_change, company_growth_news, contract_renewal, qbr_upcoming, budget_cycle, pql_signal, health_score_high)
    - 5 expansion_type ENUM values land (seat_expansion, plan_upgrade, add_on_adoption, annual_conversion, custom)
    - 8 status ENUM values for opportunities (identified, warming, outreach_pending, outreach_sent, meeting_booked, closed_won, closed_lost, deferred)
    - 10 program_type ENUM values for customer_marketing_programs (customer_success_sequence, expansion_campaign, advocacy_recruitment, referral_program, beta_program, customer_advisory_board, community_champion, case_study_pipeline, review_generation, co_marketing)
    - lib/markos/expansion/opportunities.ts wires buildApprovalPackage on outreach_sent transition; kind='account_expansion_outreach'; idempotent via Idempotency-Key on opportunity_id
    - lib/markos/expansion/customer-marketing.ts wires buildApprovalPackage on draft→active program transition; kind='customer_marketing_program_activation'
    - lib/markos/expansion/signal-scanner.ts implements scanExpansionSignals({ tenant_id, asOf }) reading from saas_health_scores (P216 SOFT — degrade gracefully) + tenant_billing_subscriptions (P215 HARD); creates account_expansion_opportunities rows with trigger_signal populated; SG-03 + SG-09 (signals → opportunities → approvals, NOT passive dashboard)
    - 2 contracts ship F-230 (account-expansion-v1) + F-231 (customer-marketing-v1) with NO collision; flow-registry.json + openapi.json regenerated
    - Architecture-lock test passes against lib/markos/expansion/* — no createApprovalPackage / requireSupabaseAuth / etc.
    - Cross-tenant RLS denial proven across 3 expansion SOR tables
    - EVD-01..04 wiring — account_expansion_opportunities.evidence_refs uuid[] populated before status='outreach_sent' (defense-in-depth: app-layer validates non-empty + DB optional FK to evidence_map_records SOFT — per P209 dependency)
  artifacts:
    - path: supabase/migrations/86_b2b_expansion_customer_marketing.sql
      provides: account_expansion_opportunities (16 cols) + customer_marketing_programs (12 cols) + program_enrollments (8 cols) + RLS on all 3 + idx_aeo_account ON (tenant_id, account_id, status) + idx_program_enrollments_program ON (tenant_id, program_id) + 2 DB-triggers (fn_expansion_outreach_requires_approval BEFORE UPDATE on account_expansion_opportunities; fn_program_activation_requires_approval BEFORE UPDATE on customer_marketing_programs) + DO ASSERT existence check
    - path: lib/markos/expansion/contracts.ts
      provides: 6 enum constants (EXPANSION_TYPES, EXPANSION_TRIGGER_SIGNALS, EXPANSION_OPPORTUNITY_STATUSES, CUSTOMER_MARKETING_PROGRAM_TYPES, CUSTOMER_MARKETING_PROGRAM_STATUSES, CUSTOMER_MARKETING_GOALS) + zod schemas (AccountExpansionOpportunitySchema, CustomerMarketingProgramSchema, ProgramEnrollmentSchema)
      exports: [EXPANSION_TYPES, EXPANSION_TRIGGER_SIGNALS, EXPANSION_OPPORTUNITY_STATUSES, CUSTOMER_MARKETING_PROGRAM_TYPES, CUSTOMER_MARKETING_PROGRAM_STATUSES, CUSTOMER_MARKETING_GOALS, AccountExpansionOpportunitySchema, CustomerMarketingProgramSchema, ProgramEnrollmentSchema]
    - path: lib/markos/expansion/opportunities.ts
      provides: createOpportunity / getOpportunity / listOpportunities / updateOpportunity CRUD + transitionStatus helper that wraps approval-required transitions; outreach_sent path calls buildApprovalPackage with kind='account_expansion_outreach'; enforces evidence_refs non-empty before outreach_sent at app-layer
      exports: [createOpportunity, getOpportunity, listOpportunities, updateOpportunity, transitionStatus, requestOutreachApproval]
    - path: lib/markos/expansion/customer-marketing.ts
      provides: createProgram / getProgram / listPrograms / updateProgram CRUD + activateProgram that wraps draft to active transition with buildApprovalPackage kind='customer_marketing_program_activation'
      exports: [createProgram, getProgram, listPrograms, updateProgram, activateProgram]
    - path: lib/markos/expansion/enrollment.ts
      provides: enrollAccount with ON CONFLICT DO NOTHING (idempotent); unenrollAccount sets unenrolled_at + unenroll_reason; recomputeEnrolledCount cron-callable
      exports: [enrollAccount, unenrollAccount, recomputeEnrolledCount, listEnrollments]
    - path: lib/markos/expansion/signal-scanner.ts
      provides: scanExpansionSignals reads saas_health_scores (P216 SOFT) + tenant_billing_subscriptions (P215 HARD); detects 9 trigger_signal patterns; creates account_expansion_opportunities row per detected signal; idempotent via composite key
      exports: [scanExpansionSignals, detectTriggerSignal, SIGNAL_DETECTORS]
    - path: contracts/F-230-b2b-account-expansion-v1.yaml
      provides: GET /v1/b2b/expansion/opportunities (list/filter), POST + PATCH (status transitions with approval gate); auth via requireHostedSupabaseAuth
    - path: contracts/F-231-b2b-customer-marketing-v1.yaml
      provides: GET /v1/b2b/customer-marketing/programs, POST + PATCH (activation gated), GET/POST/DELETE /v1/b2b/customer-marketing/programs/{id}/enrollments; auth via requireHostedSupabaseAuth
    - path: scripts/preconditions/219-02-check-upstream.cjs
      provides: CLI preflight; verifies 219-01 module tree (lib/markos/b2b/preflight/index.cjs + revenue_team_configs table) + P214/P215/P218 tables; exit 1 on hard miss
    - path: test/b2b-219/expansion/outreach-trigger.test.js
      provides: service-role direct UPDATE on account_expansion_opportunities SET status='outreach_sent' WHERE approval_id IS NULL → expect EXCEPTION 'EXPANSION_OUTREACH_REQUIRES_APPROVAL'; with approval_id set → succeeds
    - path: test/b2b-219/expansion/program-activation.test.js
      provides: service-role direct UPDATE on customer_marketing_programs SET status='active' from existing status='draft' WHERE approved_at IS NULL → expect EXCEPTION 'PROGRAM_ACTIVATION_REQUIRES_APPROVAL'; with approved_at set → succeeds
    - path: test/b2b-219/expansion/enrollment-dedup.test.js
      provides: INSERT same (tenant_id, program_id, account_id) twice → second is no-op via ON CONFLICT DO NOTHING; SELECT returns 1 row not 2
    - path: test/b2b-219/expansion/signal-scanner.test.js
      provides: seed saas_health_scores row with health_score=88; call scanExpansionSignals; assert 1 account_expansion_opportunities row created with trigger_signal='health_score_high'; assert idempotent on second call
  key_links:
    - from: lib/markos/expansion/opportunities.ts
      to: lib/markos/crm/agent-actions.ts
      via: const buildApprovalPackage = require('../crm/agent-actions') — kind='account_expansion_outreach' on outreach_sent transition
      pattern: "buildApprovalPackage"
    - from: lib/markos/expansion/customer-marketing.ts
      to: lib/markos/crm/agent-actions.ts
      via: buildApprovalPackage with kind='customer_marketing_program_activation' on draft to active transition
      pattern: "buildApprovalPackage"
    - from: supabase/migrations/86_b2b_expansion_customer_marketing.sql
      to: lib/markos/expansion/contracts.ts
      via: EXPANSION_TRIGGER_SIGNALS array (9 values) mirrors account_expansion_opportunities.trigger_signal CHECK constraint exactly
      pattern: "EXPANSION_TRIGGER_SIGNALS"
    - from: lib/markos/expansion/enrollment.ts
      to: supabase/migrations/86_b2b_expansion_customer_marketing.sql
      via: enrollAccount uses INSERT ON CONFLICT (tenant_id, program_id, account_id) DO NOTHING; idempotency tested
      pattern: "ON CONFLICT.*DO NOTHING"
    - from: scripts/preconditions/219-02-check-upstream.cjs
      to: lib/markos/b2b/preflight/index.cjs
      via: const assertUpstreamReady = require('../../lib/markos/b2b/preflight/index.cjs')
      pattern: "assertUpstreamReady"
    - from: lib/markos/expansion/signal-scanner.ts
      to: supabase tables saas_health_scores + tenant_billing_subscriptions
      via: SOFT P216 read (graceful degrade) + HARD P215 read (assertUpstreamReady gates); detects health_score_high, contract_renewal
      pattern: "saas_health_scores"
---

<objective>
Wave 2 plan A — translate B2B account expansion + customer marketing into governed workflows per RESEARCH §6 Domain 2. Ship account_expansion_opportunities + customer_marketing_programs + program_enrollments SOR (migration 86), the outreach-approval gate via buildApprovalPackage, the program-activation approval gate, the DB-trigger compliance enforcement (fn_expansion_outreach_requires_approval + fn_program_activation_requires_approval per RESEARCH Compliance enforcement boundary), the enrollment dedup UNIQUE constraint, the 9-value trigger_signal taxonomy + 10-value program_type taxonomy, and the signal-scanner pipeline (saas_health_scores + tenant_billing_subscriptions reads).

Purpose: satisfy SG-03 (B2B SaaS growth covers account expansion + customer marketing), SG-09 (modules emit tasks/approvals/learnings — NO passive dashboards; every signal creates opportunity row + approval flow), SG-12 (external customer outreach mutations approval-gated), EVD-01..04 (evidence_refs populated before outreach; non-stale research reuse via signal-scanner idempotency), QA-01..04 (architecture-lock + helper-presence + RLS isolation + F-ID parity).

Output: 1 SOR migration + 7 expansion lib files + 2 contracts + flow-registry update + 1 preflight CLI + 5 expansion test files + 1 RLS test file + 1 OpenAPI parity test.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-CONTEXT.md
@.planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-RESEARCH.md
@.planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-01-SUMMARY.md
@.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@obsidian/brain/SaaS Marketing OS Strategy Canon.md
@obsidian/work/incoming/17-SAAS-MARKETING-OS-STRATEGY.md
@CLAUDE.md
@lib/markos/b2b/preflight/index.cjs
@lib/markos/crm/agent-actions.ts
@onboarding/backend/runtime-context.cjs
@supabase/migrations/81_markos_orgs.sql
@supabase/migrations/85_b2b_revenue_team.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expansion + customer-marketing SOR migration + DB-trigger compliance pack + RLS + preflight</name>
  <files>supabase/migrations/86_b2b_expansion_customer_marketing.sql, scripts/preconditions/219-02-check-upstream.cjs, test/b2b-219/expansion/opportunities-crud.test.js, test/b2b-219/expansion/outreach-trigger.test.js, test/b2b-219/expansion/program-activation.test.js, test/b2b-219/expansion/enrollment-dedup.test.js, test/b2b-219/rls/expansion-cross-tenant-denial.test.js</files>
  <behavior>
    - Test 1 (opportunities-crud): account_expansion_opportunities has 16 columns; expansion_type CHECK 5 values; trigger_signal CHECK 9 values; status CHECK 8 values DEFAULT 'identified'; RLS enabled
    - Test 2 (opportunities-crud): customer_marketing_programs has 12 columns; program_type CHECK 10 values; status CHECK 4 values DEFAULT 'draft'
    - Test 3 (opportunities-crud): program_enrollments has 8 columns; UNIQUE(tenant_id, program_id, account_id)
    - Test 4 (outreach-trigger): UPDATE account_expansion_opportunities SET status='outreach_sent' WHERE approval_id IS NULL → EXCEPTION 'EXPANSION_OUTREACH_REQUIRES_APPROVAL'
    - Test 5 (program-activation): UPDATE customer_marketing_programs SET status='active' from 'draft' WHERE approved_at IS NULL → EXCEPTION 'PROGRAM_ACTIVATION_REQUIRES_APPROVAL'
    - Test 6 (enrollment-dedup): duplicate INSERT no-op via ON CONFLICT DO NOTHING
    - Test 7 (cross-tenant RLS): tenant A INSERT, tenant B SELECT returns 0 rows for all 3 tables
    - Test 8 (preflight): scripts/preconditions/219-02-check-upstream.cjs exits 0 when env green; exits 1 with MISSING_UPSTREAM_PHASE when revenue_team_configs missing
  </behavior>
  <read_first>
    - 219-RESEARCH.md §6 Domain 2 (read SQL block lines 444-518 carefully)
    - 219-RESEARCH.md §Compliance Enforcement Boundary Summary (Plan 02 row — exact exception names)
    - supabase/migrations/85_b2b_revenue_team.sql (Plan 01 output reference)
    - .planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md (confirm P219-02 owns slot 86)
    - .planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-02-PLAN.md (REPLICATE pattern; swap community → expansion)
    - lib/markos/b2b/preflight/index.cjs (Plan 01 output for assertUpstreamReady)
  </read_first>
  <action>
    Step 1: Write tests 1-8 BEFORE migration (TDD RED). Use node:test + node:assert/strict; service-role-direct DB connections for trigger tests; SET LOCAL app.tenant_id for RLS tests.
    Step 2: Write supabase/migrations/86_b2b_expansion_customer_marketing.sql:
      - Comment header: `-- Plan 219-02 / Phase 219 / Slot 86 / RESEARCH §6 Domain 2`
      - CREATE TABLE account_expansion_opportunities with 16 cols per RESEARCH:
        opportunity_id uuid PK DEFAULT gen_random_uuid(),
        tenant_id uuid NOT NULL REFERENCES markos_orgs(org_id),
        account_id uuid,
        expansion_type text NOT NULL CHECK (expansion_type IN ('seat_expansion','plan_upgrade','add_on_adoption','annual_conversion','custom')),
        trigger_signal text NOT NULL CHECK (trigger_signal IN ('usage_growth','seat_pressure','champion_role_change','company_growth_news','contract_renewal','qbr_upcoming','budget_cycle','pql_signal','health_score_high')),
        estimated_arr_delta_cents bigint,
        recommended_approach text,
        timing_trigger text,
        evidence_refs uuid[] DEFAULT ARRAY[]::uuid[],
        pricing_context_id uuid,
        status text NOT NULL DEFAULT 'identified' CHECK (status IN ('identified','warming','outreach_pending','outreach_sent','meeting_booked','closed_won','closed_lost','deferred')),
        approval_id uuid,
        approved_at timestamptz,
        closed_at timestamptz,
        closed_reason text,
        created_at timestamptz NOT NULL DEFAULT now(),
        updated_at timestamptz NOT NULL DEFAULT now()
      - CREATE TABLE customer_marketing_programs with 12 cols per RESEARCH (program_id PK, tenant_id FK, program_type CHECK 10 values, name, target_segment, activation_criteria, goal CHECK 8 values, status CHECK 4 values DEFAULT 'draft', enrolled_count int DEFAULT 0, conversion_rate numeric(5,4) DEFAULT 0, approved_at, approved_by, created_at, updated_at)
      - CREATE TABLE program_enrollments with 8 cols + UNIQUE(tenant_id, program_id, account_id)
      - ALTER TABLE ... ENABLE ROW LEVEL SECURITY on each
      - CREATE POLICY tenant_isolation USING (tenant_id = current_setting('app.tenant_id', true)::uuid) on each
      - CREATE INDEX idx_aeo_account ON account_expansion_opportunities(tenant_id, account_id, status)
      - CREATE INDEX idx_program_enrollments_program ON program_enrollments(tenant_id, program_id)
      - CREATE OR REPLACE FUNCTION fn_expansion_outreach_requires_approval() RETURNS trigger AS BEGIN IF NEW.status = 'outreach_sent' AND NEW.approval_id IS NULL THEN RAISE EXCEPTION 'EXPANSION_OUTREACH_REQUIRES_APPROVAL'; END IF; RETURN NEW; END LANGUAGE plpgsql
      - CREATE TRIGGER trg_expansion_outreach_requires_approval BEFORE UPDATE ON account_expansion_opportunities
      - CREATE OR REPLACE FUNCTION fn_program_activation_requires_approval() — IF NEW.status = 'active' AND OLD.status = 'draft' AND NEW.approved_at IS NULL THEN RAISE EXCEPTION 'PROGRAM_ACTIVATION_REQUIRES_APPROVAL'
      - CREATE TRIGGER trg_program_activation_requires_approval BEFORE UPDATE ON customer_marketing_programs
      - DO BEGIN ASSERT (3 tables exist) END
    Step 3: Write scripts/preconditions/219-02-check-upstream.cjs replicating 219-01 pattern; ALSO checks revenue_team_configs table exists (Plan 01 output) — fails fast if Plan 01 didn't ship.
    Step 4: Apply migration; run all 8 tests (TDD GREEN).
    Step 5: Commit migration: `db(219-02): expansion + customer-marketing SOR + outreach + activation triggers / slot 86`.
  </action>
  <acceptance_criteria>
    - `npm test -- test/b2b-219/expansion/opportunities-crud.test.js` passes (3 column-presence + RLS-enabled assertions green)
    - `npm test -- test/b2b-219/expansion/outreach-trigger.test.js` passes (positive + negative cases green for EXPANSION_OUTREACH_REQUIRES_APPROVAL)
    - `npm test -- test/b2b-219/expansion/program-activation.test.js` passes (positive + negative cases green for PROGRAM_ACTIVATION_REQUIRES_APPROVAL)
    - `npm test -- test/b2b-219/expansion/enrollment-dedup.test.js` passes (UNIQUE + ON CONFLICT)
    - `npm test -- test/b2b-219/rls/expansion-cross-tenant-denial.test.js` passes
    - `psql -c "SELECT count(*) FROM information_schema.tables WHERE table_name IN ('account_expansion_opportunities','customer_marketing_programs','program_enrollments')"` returns 3
    - `psql -c "SELECT count(*) FROM pg_trigger WHERE tgname IN ('trg_expansion_outreach_requires_approval','trg_program_activation_requires_approval')"` returns 2
    - `grep -E "EXPANSION_OUTREACH_REQUIRES_APPROVAL|PROGRAM_ACTIVATION_REQUIRES_APPROVAL" supabase/migrations/86_b2b_expansion_customer_marketing.sql | wc -l` returns at least 2 (exact exception strings)
    - `node scripts/preconditions/219-02-check-upstream.cjs` exits 0 when env green
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expansion lib (opportunities + customer-marketing + enrollment + signal-scanner) + 2 contracts + flow-registry sync</name>
  <files>lib/markos/expansion/contracts.ts, lib/markos/expansion/contracts.cjs, lib/markos/expansion/opportunities.ts, lib/markos/expansion/opportunities.cjs, lib/markos/expansion/customer-marketing.ts, lib/markos/expansion/enrollment.ts, lib/markos/expansion/signal-scanner.ts, lib/markos/expansion/index.cjs, contracts/F-230-b2b-account-expansion-v1.yaml, contracts/F-231-b2b-customer-marketing-v1.yaml, contracts/flow-registry.json, test/b2b-219/expansion/signal-scanner.test.js, test/b2b-219/openapi/parity-expansion.test.js</files>
  <behavior>
    - Test 1 (signal-scanner): seed saas_health_scores row with health_score=88 for account A; call scanExpansionSignals({ tenant_id, asOf }); assert 1 account_expansion_opportunities row created with trigger_signal='health_score_high' AND account_id=A AND status='identified'
    - Test 2 (signal-scanner): same call twice → idempotent (composite key prevents duplicate row)
    - Test 3 (signal-scanner): saas_health_scores missing (P216 SOFT) → returns empty array + console.warn 'P216 saas_health_scores missing'; does NOT throw
    - Test 4 (signal-scanner): seed tenant_billing_subscriptions with renewal_at within 30 days → trigger_signal='contract_renewal' detected
    - Test 5 (opportunities lib): createOpportunity at status='identified' succeeds; transitionStatus to 'outreach_sent' WITHOUT evidence_refs throws EvidenceRequiredError BEFORE buildApprovalPackage call
    - Test 6 (opportunities lib): transitionStatus to 'outreach_sent' WITH evidence_refs non-empty calls requestOutreachApproval → buildApprovalPackage with kind='account_expansion_outreach'; UPDATE row status + approval_id
    - Test 7 (customer-marketing lib): createProgram at status='draft' succeeds; activateProgram calls buildApprovalPackage with kind='customer_marketing_program_activation' + sets approved_at/approved_by + UPDATE status='active'
    - Test 8 (enrollment lib): enrollAccount succeeds first call; second call no-op via ON CONFLICT DO NOTHING; recomputeEnrolledCount updates customer_marketing_programs.enrolled_count
    - Test 9 (openapi-parity): F-230 + F-231 YAML schemas match lib/markos/expansion/contracts.ts zod
  </behavior>
  <read_first>
    - 219-RESEARCH.md §6 Domain 2 (full domain — 16/12/8 column tables + DB-trigger + API surface + cron handler + test strategy)
    - obsidian/work/incoming/17-SAAS-MARKETING-OS-STRATEGY.md lines 377-490 (CustomerMarketingProgram interface)
    - lib/markos/crm/agent-actions.ts:68 (buildApprovalPackage signature)
    - lib/markos/b2b/preflight/index.cjs (assertUpstreamReady from Plan 01)
    - .planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-02-PLAN.md (REPLICATE lib structure pattern; swap community → expansion)
  </read_first>
  <action>
    Step 1: Write tests 1-9 BEFORE implementations (TDD RED).
    Step 2: Implement lib/markos/expansion/contracts.ts:
      - Export 6 enum constants:
        EXPANSION_TYPES = ['seat_expansion','plan_upgrade','add_on_adoption','annual_conversion','custom'] as const;
        EXPANSION_TRIGGER_SIGNALS = ['usage_growth','seat_pressure','champion_role_change','company_growth_news','contract_renewal','qbr_upcoming','budget_cycle','pql_signal','health_score_high'] as const;
        EXPANSION_OPPORTUNITY_STATUSES = ['identified','warming','outreach_pending','outreach_sent','meeting_booked','closed_won','closed_lost','deferred'] as const;
        CUSTOMER_MARKETING_PROGRAM_TYPES = ['customer_success_sequence','expansion_campaign','advocacy_recruitment','referral_program','beta_program','customer_advisory_board','community_champion','case_study_pipeline','review_generation','co_marketing'] as const;
        CUSTOMER_MARKETING_PROGRAM_STATUSES = ['active','paused','draft','archived'] as const;
        CUSTOMER_MARKETING_GOALS = ['expansion','advocacy','retention','education','review','beta','advisory','co_marketing'] as const
      - Export zod schemas: AccountExpansionOpportunitySchema, CustomerMarketingProgramSchema, ProgramEnrollmentSchema mirroring migration 86 column types
      - CommonJS twin contracts.cjs re-exports the constants
    Step 3: Implement lib/markos/expansion/opportunities.ts:
      - createOpportunity({ tenant_id, account_id, expansion_type, trigger_signal, estimated_arr_delta_cents, evidence_refs, ... }, supabase): INSERT row at status='identified'
      - getOpportunity / listOpportunities CRUD
      - transitionStatus({ tenant_id, opportunity_id, to_status, ...fields }) with state machine validation; outreach_sent transition delegates to requestOutreachApproval
      - requestOutreachApproval({ tenant_id, opportunity_id, initiated_by }) — fetch opportunity; assert evidence_refs.length > 0 OR throw EvidenceRequiredError; call buildApprovalPackage with kind='account_expansion_outreach' + payload {opportunity_id, evidence_refs, expansion_type, trigger_signal}; UPDATE row status + approval_id
    Step 4: Implement lib/markos/expansion/customer-marketing.ts:
      - createProgram, getProgram, listPrograms, updateProgram CRUD
      - activateProgram({ tenant_id, program_id, initiated_by }) — fetch program; assert status='draft' OR throw ValidationError; call buildApprovalPackage with kind='customer_marketing_program_activation'; UPDATE status='active' + approved_at + approved_by
    Step 5: Implement lib/markos/expansion/enrollment.ts:
      - enrollAccount with ON CONFLICT DO NOTHING; idempotent
      - unenrollAccount sets unenrolled_at + unenroll_reason
      - recomputeEnrolledCount({ tenant_id, program_id })
      - listEnrollments
    Step 6: Implement lib/markos/expansion/signal-scanner.ts with SIGNAL_DETECTORS map (one detector per trigger_signal value); health_score_high detector reads saas_health_scores (SOFT degrade); contract_renewal reads tenant_billing_subscriptions (HARD); scanExpansionSignals iterates detectors, INSERTs account_expansion_opportunities with composite-key idempotency via app-layer SELECT-then-INSERT.
    Step 7: CommonJS twins for opportunities.cjs + index.cjs (re-export full surface for legacy api/*.js consumers).
    Step 8: Write 2 contract YAMLs F-230/F-231 per RESEARCH §6 Domain 2 API surface table.
    Step 9: Update contracts/flow-registry.json appending F-230/F-231 entries.
    Step 10: Run all expansion tests + openapi-parity test (TDD GREEN). Run `npm run openapi:build`.
  </action>
  <acceptance_criteria>
    - `npm test -- test/b2b-219/expansion/signal-scanner.test.js` passes (4 tests green; health_score detection + idempotency + SOFT degrade + contract_renewal)
    - `npm test -- test/b2b-219/expansion/` all green
    - `npm test -- test/b2b-219/openapi/parity-expansion.test.js` passes (2 contracts × zod parity)
    - `grep "F-230\|F-231" contracts/flow-registry.json | wc -l` returns 2
    - `grep -rn "createApprovalPackage\|requireSupabaseAuth\|lookupPlugin\|requireTenantContext\|app/(b2b)\|app/(growth)\|route.ts\|vitest" lib/markos/expansion/ | wc -l` returns 0
    - `npm run openapi:build` regenerates with /v1/b2b/expansion/* + /v1/b2b/customer-marketing/* paths
    - `node -e "console.log(typeof require('./lib/markos/expansion/index.cjs').scanExpansionSignals)"` outputs `function`
    - `node -e "console.log(typeof require('./lib/markos/expansion/index.cjs').requestOutreachApproval)"` outputs `function`
    - `node -e "console.log(typeof require('./lib/markos/expansion/index.cjs').activateProgram)"` outputs `function`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser → /v1/b2b/expansion/opportunities | Untrusted; outreach mutations approval-gated |
| Cron worker → scanExpansionSignals | High-trust; protected by MARKOS_B2B_CRON_SECRET; DB-triggers enforce compliance |
| App-layer transitionStatus → buildApprovalPackage | Tenant-isolated; approval_id required for outreach (DB-trigger defense-in-depth) |
| Service-role direct UPDATE on account_expansion_opportunities | DB-trigger blocks status='outreach_sent' without approval_id |
| Service-role direct UPDATE on customer_marketing_programs | DB-trigger blocks draft to active without approved_at |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-219-02-01 | Tampering | account_expansion_opportunities service-role UPDATE bypasses approval | mitigate | DB-trigger fn_expansion_outreach_requires_approval; tested |
| T-219-02-02 | Tampering | customer_marketing_programs draft to active bypass | mitigate | DB-trigger fn_program_activation_requires_approval; tested |
| T-219-02-03 | Information Disclosure | Cross-tenant opportunity leak | mitigate | RLS policy on tenant_id; tested |
| T-219-02-04 | Repudiation | Operator denies approving outreach | mitigate | buildApprovalPackage writes approval row + audit log SHA-256 hash chain |
| T-219-02-05 | Spoofing | Bot creates spammy opportunities via unauthenticated API | mitigate | requireHostedSupabaseAuth on all POST endpoints |
| T-219-02-06 | Tampering | Duplicate enrollment rows farm conversion_rate | mitigate | UNIQUE(tenant_id, program_id, account_id); ON CONFLICT DO NOTHING tested |
| T-219-02-07 | Information Disclosure | evidence_refs leak via metrics endpoint | accept | RLS isolates per-tenant; evidence_refs UUIDs only (no PII) |
| T-219-02-08 | Elevation of Privilege | Operator dispatches outreach without evidence | mitigate | App-layer EvidenceRequiredError before DB-trigger; defense-in-depth |
| T-219-02-09 | Denial of Service | Signal-scanner creates unbounded opportunities for health_score_high accounts | mitigate | Composite-key idempotency; LIMIT 100 per tenant per scan |
| T-219-02-10 | Tampering | Signal scanner uses stale saas_health_scores | accept | EVD-04 reuse: signal-scanner reads scored_at; opportunities created with signal_period for traceability |
</threat_model>

<verification>
**Plan-level verification (run after Task 2 closes):**

- `npm test -- test/b2b-219/expansion/ test/b2b-219/rls/expansion-* test/b2b-219/openapi/parity-expansion.test.js` → all green
- `node scripts/preconditions/219-02-check-upstream.cjs` exits 0 with green env
- `psql -c "SELECT count(*) FROM information_schema.tables WHERE table_name IN ('account_expansion_opportunities','customer_marketing_programs','program_enrollments')"` returns 3
- `psql -c "SELECT count(*) FROM pg_trigger WHERE tgname IN ('trg_expansion_outreach_requires_approval','trg_program_activation_requires_approval')"` returns 2
- `grep -E "F-23[01]" contracts/flow-registry.json | wc -l` returns 2
- `grep -rn "createApprovalPackage\|requireSupabaseAuth\|requireTenantContext\|serviceRoleClient\|lookupPlugin\|public/openapi.json\|app/(b2b)\|app/(growth)\|api/v1/.*/route.ts\|vitest\|.test.ts" lib/markos/expansion/ scripts/preconditions/219-02-check-upstream.cjs | wc -l` returns 0
</verification>

<success_criteria>
- Both tasks complete with passing tests; total ~17+ assertions green
- 1 SOR migration + 7 lib files + 2 contracts ship with NO architecture-lock violations
- Domain 2 (Expansion + CustomerMarketing) compliance enforcement boundary LOCKED at DB-trigger layer
- SG-09 wiring shipped: signal-scanner creates opportunity rows + downstream approval flow (NOT passive)
- EVD-01..02 wiring: app-layer evidence_refs guard before outreach_sent
</success_criteria>

<output>
After completion, create `.planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-02-SUMMARY.md` documenting:

- Migration slot claimed: 86
- F-IDs claimed: F-230, F-231
- Tables created: account_expansion_opportunities, customer_marketing_programs, program_enrollments
- Triggers created: trg_expansion_outreach_requires_approval, trg_program_activation_requires_approval
- 9 trigger_signal ENUM values + 5 expansion_type + 8 status + 10 program_type + 8 goal ENUM values shipped
- Signal-scanner pipeline shipped (saas_health_scores SOFT degrade + tenant_billing_subscriptions HARD)
</output>
</content>
</invoke>