---
phase: 219
plan: "06"
type: execute
wave: 4
depends_on:
  - "219-01"
  - "219-02"
  - "219-03"
  - "219-04"
  - "219-05"
files_modified:
  - supabase/migrations/85_b2b_revenue_team.sql
  - supabase/migrations/85_b2b_revenue_team_part2.sql
  - lib/markos/b2b-agents/contracts.ts
  - lib/markos/b2b-agents/contracts.cjs
  - lib/markos/b2b-agents/readiness-registry.ts
  - lib/markos/b2b-agents/readiness-gate.ts
  - lib/markos/b2b-agents/index.cjs
  - lib/markos/mcp/tools/b2b.cjs
  - lib/markos/mcp/tools/index.cjs
  - api/v1/b2b/revenue-team-config.js
  - api/v1/b2b/revenue-team-config/sla-events.js
  - api/v1/b2b/revenue-team-config/feedback.js
  - api/v1/b2b/expansion/opportunities.js
  - api/v1/b2b/customer-marketing/programs.js
  - api/v1/b2b/customer-marketing/programs/enrollments.js
  - api/v1/b2b/abm/packages.js
  - api/v1/b2b/abm/packages/committee.js
  - api/v1/b2b/abm/packages/engagement.js
  - api/v1/b2b/advocacy/candidates.js
  - api/v1/b2b/advocacy/review-requests.js
  - api/v1/b2b/advocacy/proof.js
  - api/v1/b2b/advocacy/consent.js
  - api/v1/b2b/pricing/expansion-offers.js
  - api/v1/b2b/pricing/save-offers.js
  - api/v1/b2b/pricing/discount-authorizations.js
  - api/v1/b2b/agents/readiness.js
  - api/cron/b2b-sla-breach-monitor.js
  - api/cron/b2b-expansion-signal-scanner.js
  - api/cron/b2b-abm-enrichment-refresh.js
  - api/cron/b2b-advocacy-signal-scanner.js
  - api/cron/b2b-review-request-cadence.js
  - contracts/F-237-b2b-agents-readiness-v1.yaml
  - contracts/flow-registry.json
  - scripts/preconditions/219-06-check-upstream.cjs
  - test/b2b-219/agents/registry-non-runnable.test.js
  - test/b2b-219/agents/activation-gate-trigger.test.js
  - test/b2b-219/agents/readiness-progression.test.js
  - test/b2b-219/api/contract-cross-tenant-deny.test.js
  - test/b2b-219/api/architecture-lock-api-handlers.test.js
  - test/b2b-219/cron/cron-token-auth.test.js
  - test/b2b-219/mcp/tool-registration.test.js
  - test/b2b-219/mcp/cross-tenant-isolation.test.js
  - test/b2b-219/closeout/slot-collision-regression.test.js
  - test/b2b-219/closeout/all-domains-architecture-lock-rerun.test.js
  - test/b2b-219/closeout/requirements-coverage.test.js
  - test/b2b-219/openapi/parity-closeout.test.js
  - .planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-VALIDATION.md
autonomous: false
requirements:
  - SG-10
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - QA-05
  - QA-06
  - QA-07
  - QA-08
  - QA-09
  - QA-10
  - QA-11
  - QA-12
  - QA-13
  - QA-14
  - QA-15
must_haves:
  truths:
    - Wave 4 closeout — assertUpstreamReady preflight (scripts/preconditions/219-06-check-upstream.cjs); requires Plans 01-05 complete (all migration slots 85-89 occupied; this plan does NOT claim a new migration slot — it ALTERs migration 85 OR ships a new sub-migration via slot 85b for the b2b_growth_agent_readiness table; per RESEARCH §F-ID and Migration Slot Allocation Plan 06 strategy "additive seed in Plan 01 migration or separate slot TBD" — Plan 06 ALTERs migration 85 trailer to add b2b_growth_agent_readiness via DDL append)
    - b2b_growth_agent_readiness table ships (via ALTER to 85 or via a Plan-06-owned trailer SQL block) + activation gate trigger; system-level (not tenant-scoped) registry per RESEARCH §6 Domain 6
    - b2b_growth_agent_readiness pre-populated with 9 P219 B2B agent rows via INSERT ON CONFLICT (agent_id) DO UPDATE pattern (Pitfall 12 fix — re-seedable); rows include MARKOS-AGT-EXP-01..03 + MARKOS-AGT-ABM-01..03 + MARKOS-AGT-REV-01..02 + MARKOS-AGT-IAM-01 (IAM-01 marked blocking_phase='P218-06-CONFIRM' since P218 owns it; included for cross-phase visibility); all rows runnable=false
    - SG-10 invariant — runnable boolean is GENERATED ALWAYS AS (contracts_assigned AND cost_estimated AND approval_posture_defined AND tests_implemented AND api_surface_defined AND mcp_surface_defined AND ui_surface_defined AND failure_behavior_defined) STORED; cannot be set directly; tested via Pitfall 9 regression (runnable=false on insert with all booleans false; runnable=true ONLY when all 8 are true)
    - DB-trigger compliance — fn_b2b_agent_activation_gate BEFORE UPDATE on b2b_growth_agent_readiness raises EXCEPTION 'AGENT_ACTIVATION_REQUIRES_READINESS' when NEW.runnable=true (computed) AND (NEW.activation_approval_id IS NULL OR NEW.readiness_check_id IS NULL); only operator-approved + ready agents can flip runnable=true via app code path
    - 14 API handlers ship under api/v1/b2b/* (legacy *.js per architecture-lock); all use requireHostedSupabaseAuth from onboarding/backend/runtime-context.cjs; tenant-scoped via runtimeContext.tenant_id
    - 5 cron handlers ship under api/cron/b2b-*.js; all use x-markos-cron-secret + MARKOS_B2B_CRON_SECRET pattern (matches api/cron/webhooks-dlq-purge.js); JSON {success, count, duration_ms} response; crypto.timingSafeEqual on token compare; cron handlers AND each registered in dispatcher HANDLERS map (P226 iter 2 lesson)
    - MCP tool family — lib/markos/mcp/tools/b2b.cjs registers 12 read-mostly + write-gated tools; required from lib/markos/mcp/tools/index.cjs (CommonJS, NOT .ts); cross-tenant isolation tested
    - 1 contract ships F-237 (b2b-agents-readiness-v1); flow-registry FINAL — no overlap with F-001..F-236
    - SG-09 architecture-passive-system-detector test — every read-only API endpoint paired with corresponding task/approval write endpoint per SG-09 invariant; passive endpoints rejected
    - All-domains architecture-lock RE-RUN test — final regression scan of lib/markos/{b2b,revenue-team,expansion,abm,advocacy,b2b-pricing,b2b-agents}/ + api/v1/b2b/ + api/cron/b2b-* + lib/markos/mcp/tools/b2b.cjs for ZERO forbidden patterns (rerun of Plan 01 architecture-lock with full P219 domain coverage)
    - Slot collision regression test — verifies migration 85-89 + F-228..F-237 OWN those slots and no other phase pre-empts (final guard against P218/P220 plan-up landing meanwhile); reads contracts/flow-registry.json + supabase/migrations/ listing
    - Requirements coverage test — asserts every requirement ID (SG-03, SG-08, SG-09, SG-10, SG-11, SG-12, LOOP-06, EVD-01..06, T0-04, QA-01..15) maps to at least one Plan 01-06 task per VALIDATION.md
    - Plan 06 autonomous=false; contains checkpoint:human-action for first-run agent activation (operator validates first-batch readiness criteria before unattended cron permits agent activation flips per RESEARCH §6 Domain 6 Manual-Only Verifications + P226 W1 model)
    - Plan 06 task density justified by closeout role — cannot be split without breaking Wave 4 dependency on Plans 01-05 single-commit closure
  artifacts:
    - path: lib/markos/b2b-agents/contracts.ts
      provides: 1 enum constant B2B_AGENT_TIERS = ['EXP','ABM','REV','IAM'] as const + zod schema for B2bAgentReadiness; CommonJS twin contracts.cjs
      exports: [B2B_AGENT_TIERS, B2bAgentReadinessSchema]
    - path: lib/markos/b2b-agents/readiness-registry.ts
      provides: updateReadinessFlag({ agent_id, flag_name, value, evidence_ref }) UPDATE single boolean; recomputeRunnable triggered automatically via GENERATED column; getAgentReadiness, listAgentReadiness CRUD
      exports: [updateReadinessFlag, getAgentReadiness, listAgentReadiness]
    - path: lib/markos/b2b-agents/readiness-gate.ts
      provides: assertAgentRunnable({ agent_id }) — SELECT runnable; throw AgentNotRunnableError if false; blocks AgentRun creation per SG-10
      exports: [assertAgentRunnable, AgentNotRunnableError]
    - path: lib/markos/b2b-agents/index.cjs
      provides: CommonJS barrel re-exporting full surface
    - path: lib/markos/mcp/tools/b2b.cjs
      provides: 12 MCP tool descriptors (b2b_revenue_team_config_get, b2b_mql_sla_status, b2b_feedback_record_get, b2b_expansion_opportunities_list, b2b_customer_marketing_programs_list, b2b_abm_packages_list, b2b_abm_buying_committee_get, b2b_advocacy_candidates_list, b2b_proof_assets_list, b2b_expansion_offer_draft (write-gated via buildApprovalPackage), b2b_save_offer_draft (write-gated), b2b_agents_readiness_get); each descriptor + handler + cost_window registration; required from index.cjs
      exports: [B2B_MCP_TOOLS]
    - path: api/v1/b2b/revenue-team-config.js
      provides: GET (read tenant config) + POST + PATCH (with approval gate on SLA change via lib/markos/revenue-team/config.requestSlaChange) per F-228; uses requireHostedSupabaseAuth + writeJson; tenant-scoped via runtimeContext.tenant_id
    - path: api/v1/b2b/revenue-team-config/sla-events.js
      provides: GET /v1/b2b/revenue-team-config/sla-events (LOOP-06 evidence list); read-only with optional event_type filter
    - path: api/v1/b2b/revenue-team-config/feedback.js
      provides: GET (list periods) + POST (create + dispatch via lib/markos/revenue-team/feedback.createFeedbackRecord) per F-229
    - path: api/v1/b2b/expansion/opportunities.js
      provides: GET + POST + PATCH per F-230; PATCH transitions delegate to lib/markos/expansion/opportunities.transitionStatus
    - path: api/v1/b2b/customer-marketing/programs.js
      provides: GET + POST + PATCH per F-231; PATCH activation delegates to lib/markos/expansion/customer-marketing.activateProgram
    - path: api/v1/b2b/customer-marketing/programs/enrollments.js
      provides: GET + POST (enrollAccount) + DELETE (unenrollAccount) per F-231 subpath
    - path: api/v1/b2b/abm/packages.js
      provides: GET + POST + PATCH per F-232 (abm-account-package); PATCH stage transitions delegate to lib/markos/abm/packages.transitionStage
    - path: api/v1/b2b/abm/packages/committee.js
      provides: GET + POST + PATCH per F-233 (committee CRUD + markKnown)
    - path: api/v1/b2b/abm/packages/engagement.js
      provides: GET + POST per F-233 subpath (engagement event ingest + listing)
    - path: api/v1/b2b/advocacy/candidates.js
      provides: GET + POST + PATCH per F-234; assertContactable enforced before any ask
    - path: api/v1/b2b/advocacy/review-requests.js
      provides: GET + POST + PATCH per F-234 subpath; PATCH dispatch delegates to lib/markos/advocacy/review-requests.dispatchReviewRequest
    - path: api/v1/b2b/advocacy/proof.js
      provides: GET + POST + PATCH per F-235; PATCH publish delegates to lib/markos/advocacy/proof-assets.publishProofAsset
    - path: api/v1/b2b/advocacy/consent.js
      provides: GET + POST + PATCH per F-235 subpath; PATCH revoke delegates to lib/markos/advocacy/consent.revokeConsent (cascade to proof_assets archival)
    - path: api/v1/b2b/pricing/expansion-offers.js
      provides: GET + POST + PATCH per F-236; PATCH activation delegates to lib/markos/b2b-pricing/expansion-offers.activateExpansionOffer
    - path: api/v1/b2b/pricing/save-offers.js
      provides: GET + POST + PATCH per F-236 subpath; PATCH presentation delegates to lib/markos/b2b-pricing/save-offers.presentSaveOffer
    - path: api/v1/b2b/pricing/discount-authorizations.js
      provides: GET + POST per F-236 subpath; POST delegates to lib/markos/b2b-pricing/discount-authorizations.requestDiscountAuthorization
    - path: api/v1/b2b/agents/readiness.js
      provides: GET (operator-only read-only) + PATCH (per-flag update via lib/markos/b2b-agents/readiness-registry.updateReadinessFlag) per F-237
    - path: api/cron/b2b-sla-breach-monitor.js
      provides: hourly cron; calls lib/markos/revenue-team/sla-monitor.detectSlaBreaches per tenant; matches webhooks-dlq-purge.js auth pattern; MARKOS_B2B_CRON_SECRET env
    - path: api/cron/b2b-expansion-signal-scanner.js
      provides: daily cron; calls lib/markos/expansion/signal-scanner.scanExpansionSignals per tenant; MARKOS_B2B_CRON_SECRET
    - path: api/cron/b2b-abm-enrichment-refresh.js
      provides: weekly cron; calls lib/markos/abm/enrichment.listStalePackages per tenant + creates re-enrichment review tasks; MARKOS_B2B_CRON_SECRET
    - path: api/cron/b2b-advocacy-signal-scanner.js
      provides: daily cron; calls lib/markos/advocacy/signal-scanner.scanAdvocacySignals per tenant; MARKOS_B2B_CRON_SECRET
    - path: api/cron/b2b-review-request-cadence.js
      provides: daily cron; queues approved advocacy_review_requests for dispatch; MARKOS_B2B_CRON_SECRET
    - path: contracts/F-237-b2b-agents-readiness-v1.yaml
      provides: GET /v1/b2b/agents/readiness (operator read) + PATCH /v1/b2b/agents/readiness/{agent_id} (per-flag update); auth via requireHostedSupabaseAuth
    - path: scripts/preconditions/219-06-check-upstream.cjs
      provides: CLI preflight; verifies all 5 prior P219 plans shipped (all migrations 85-89 + ~14 SOR tables + all triggers + all F-IDs F-228..F-236); FAIL-CLOSED if any prior plan incomplete; HARD upstream now ALSO includes P207 agent_runs (RUN-01..08 hard requirement for closeout)
    - path: test/b2b-219/agents/registry-non-runnable.test.js
      provides: assert 9 rows pre-populated; assert each row.runnable=false on insert; assert agent_id UNIQUE
    - path: test/b2b-219/agents/activation-gate-trigger.test.js
      provides: service-role direct UPDATE b2b_growth_agent_readiness setting all 8 booleans true via service-role direct path WITHOUT activation_approval_id → expect EXCEPTION 'AGENT_ACTIVATION_REQUIRES_READINESS'; with activation_approval_id + readiness_check_id set → succeeds
    - path: test/b2b-219/agents/readiness-progression.test.js
      provides: setting 7 booleans true → runnable still false; setting all 8 true → runnable computes true; assertAgentRunnable succeeds
    - path: test/b2b-219/api/contract-cross-tenant-deny.test.js
      provides: every API handler tenant-scope-tested — INSERT under tenant A, GET under tenant B → 404 not 403; same for PATCH/DELETE
    - path: test/b2b-219/api/architecture-lock-api-handlers.test.js
      provides: scans api/v1/b2b/*/*.js + api/cron/b2b-*.js for forbidden patterns (createApprovalPackage, requireSupabaseAuth, app/(b2b), route.ts, vitest); MUST return 0 violations
    - path: test/b2b-219/cron/cron-token-auth.test.js
      provides: cron handler missing x-markos-cron-secret → 401; wrong token → 401 (timingSafeEqual prevents timing attack); correct token → 200 + JSON {success, count, duration_ms}
    - path: test/b2b-219/mcp/tool-registration.test.js
      provides: lib/markos/mcp/tools/index.cjs (after Plan 06 modification) registers 30 + 12 = 42 tools; b2b_* tools accessible via JSON-RPC schema
    - path: test/b2b-219/mcp/cross-tenant-isolation.test.js
      provides: MCP tool handler with mocked tenant context A returns only tenant A data; tenant B context returns only tenant B
    - path: test/b2b-219/closeout/slot-collision-regression.test.js
      provides: re-runs Plan 01 baseline guards — assert no migrations outside 85-89 range claim P219 slots; assert F-228..F-237 owned by P219 only; checks against contracts/flow-registry.json
    - path: test/b2b-219/closeout/all-domains-architecture-lock-rerun.test.js
      provides: full forbidden-pattern scan across ALL P219 surface (lib/markos/{b2b,revenue-team,expansion,abm,advocacy,b2b-pricing,b2b-agents}/, api/v1/b2b/, api/cron/b2b-*, lib/markos/mcp/tools/b2b.cjs); MUST equal 0 violations
    - path: test/b2b-219/closeout/requirements-coverage.test.js
      provides: parses 219-VALIDATION.md per-task verification map; assert SG-03 + SG-08 + SG-09 + SG-10 + SG-11 + SG-12 + LOOP-06 + EVD-01..06 + T0-04 + QA-01..15 each map to >= 1 task; total = 30 unique requirement IDs covered
  key_links:
    - from: lib/markos/mcp/tools/b2b.cjs
      to: lib/markos/mcp/tools/index.cjs
      via: index.cjs adds `const { B2B_MCP_TOOLS } = require('./b2b.cjs')` and concats into TOOL_DESCRIPTORS array
      pattern: "B2B_MCP_TOOLS"
    - from: api/v1/b2b/revenue-team-config.js
      to: onboarding/backend/runtime-context.cjs
      via: const requireHostedSupabaseAuth = require('../../onboarding/backend/runtime-context.cjs')
      pattern: "requireHostedSupabaseAuth"
    - from: api/cron/b2b-sla-breach-monitor.js
      to: api/cron/webhooks-dlq-purge.js
      via: replicate cron auth pattern + writeJson + JSON response shape; same x-markos-cron-secret + crypto.timingSafeEqual
      pattern: "x-markos-cron-secret"
    - from: lib/markos/b2b-agents/readiness-gate.ts
      to: b2b_growth_agent_readiness table
      via: assertAgentRunnable mirrors fn_b2b_agent_activation_gate DB-trigger; defense-in-depth
      pattern: "AGENT_ACTIVATION_REQUIRES_READINESS"
    - from: test/b2b-219/closeout/all-domains-architecture-lock-rerun.test.js
      to: lib/markos/b2b/preflight/architecture-lock.ts
      via: Plan 01 architecture-lock surface re-run with full domain coverage; consumes scanForbiddenPatterns from Plan 01
      pattern: "scanForbiddenPatterns"
---

<objective>
Wave 4 closeout (autonomous=false) — close the SaaS B2B Growth translation per RESEARCH §6 Domain 6. Ship the b2b_growth_agent_readiness registry (9 P219 B2B agents pre-populated, all runnable=false until criteria met per SG-10), the GENERATED runnable column (Pitfall 9 regression), the activation gate DB-trigger (defense-in-depth), 14 API handlers under api/v1/b2b/* (legacy *.js per architecture-lock), 5 cron handlers under api/cron/b2b-*.js (matching api/cron/webhooks-dlq-purge.js pattern), the MCP tool family (12 tools registered in lib/markos/mcp/tools/index.cjs), 1 final F-ID (F-237), the architecture-passive-system-detector test (SG-09 — implicit via API/lib pairing), the slot-collision regression test, the all-domains architecture-lock RE-RUN test, the requirements-coverage assertion, and the checkpoint:human-action for first-run agent activation (operator validates first-batch readiness flips before unattended cron permits ongoing activations).

Purpose: satisfy SG-10 (target B2B growth agent registry — non-runnable until GSD assigns contracts/costs/approval/tests/API/MCP/UI/failure-behavior), QA-01..15 (full Phase 200 quality baseline + closeout), and final architecture-lock + slot-collision + requirements-coverage closeout. Plan 06 ALSO implicitly satisfies API-01 (OpenAPI parity + 14 handlers) and MCP-01 (12 MCP tools registered tenant-safely) — those are P220 invariants here ensured for B2B surface.

Output: 1 SOR table addition (b2b_growth_agent_readiness via ALTER to migration 85 OR new sub-migration) + 4 b2b-agents lib files + 1 MCP tool registry + 14 API handlers + 5 cron handlers + 1 contract + flow-registry FINAL update + 1 preflight CLI + 11 closeout test files + 219-VALIDATION.md final flip.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-CONTEXT.md
@.planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-RESEARCH.md
@.planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-01-SUMMARY.md
@.planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-02-SUMMARY.md
@.planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-03-SUMMARY.md
@.planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-04-SUMMARY.md
@.planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-05-SUMMARY.md
@.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md
@.planning/REQUIREMENTS.md
@obsidian/brain/SaaS Marketing OS Strategy Canon.md
@obsidian/work/incoming/17-SAAS-MARKETING-OS-STRATEGY.md
@CLAUDE.md
@lib/markos/b2b/preflight/index.cjs
@lib/markos/crm/agent-actions.ts
@onboarding/backend/runtime-context.cjs
@lib/markos/mcp/tools/index.cjs
@api/cron/webhooks-dlq-purge.js
</context>

<tasks>

<task type="auto">
  <name>Task 0: Wave 4 closeout preflight + verify Plans 01-05 complete</name>
  <files>scripts/preconditions/219-06-check-upstream.cjs</files>
  <read_first>
    - lib/markos/b2b/preflight/index.cjs
    - .planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md (verify P219-06 strategy — additive seed in 85 OR separate slot)
    - All 5 prior summaries (219-01-SUMMARY.md..219-05-SUMMARY.md)
    - 219-RESEARCH.md §6 Domain 6
  </read_first>
  <action>
    Step 1: Verify all 5 prior plans complete — assert migrations 85, 86, 87, 88, 89 exist; assert 16 P219 SOR tables exist (revenue_team_configs, lead_qualification_sla_events, marketing_sales_feedback_records, account_expansion_opportunities, customer_marketing_programs, program_enrollments, abm_account_packages, abm_buying_committee_members, abm_engagement_events, advocacy_candidates, advocacy_review_requests, proof_assets, proof_consent_records, expansion_offers, save_offers, discount_authorizations); assert F-228..F-236 in flow-registry.json.
    Step 2: Verify all DB-triggers exist (count >= 12 across triggers from Plans 01-05).
    Step 3: Create scripts/preconditions/219-06-check-upstream.cjs replicating pattern; HARD upstream now ALSO includes P207 agent_runs (RUN-01..08 hard requirement for closeout).
    Step 4: Run CLI; exit 0 only if Plans 01-05 fully complete + P207 agent_runs available.
  </action>
  <acceptance_criteria>
    - `node scripts/preconditions/219-06-check-upstream.cjs` exits 0 only when all 5 prior plans complete + P207 agent_runs table present
    - `psql -c "SELECT count(*) FROM information_schema.tables WHERE table_name LIKE 'revenue_team_%' OR table_name LIKE 'lead_qualification_%' OR table_name LIKE 'marketing_sales_%' OR table_name LIKE 'account_expansion_%' OR table_name LIKE 'customer_marketing_%' OR table_name LIKE 'program_enrollments' OR table_name LIKE 'abm_%' OR table_name LIKE 'advocacy_%' OR table_name LIKE 'proof_%' OR table_name LIKE 'expansion_offers' OR table_name LIKE 'save_offers' OR table_name LIKE 'discount_authorizations'"` returns 16
    - `grep -E "F-22[89]|F-23[0-6]" contracts/flow-registry.json | wc -l` returns 8 (Plans 01-05 contracts F-228..F-236; F-237 added in Task 2)
    - migrations 85-89 all exist on disk
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 1: b2b_growth_agent_readiness table + 9-agent seed + activation gate trigger + 3 readiness tests</name>
  <files>supabase/migrations/85_b2b_revenue_team.sql, lib/markos/b2b-agents/contracts.ts, lib/markos/b2b-agents/contracts.cjs, lib/markos/b2b-agents/readiness-registry.ts, lib/markos/b2b-agents/readiness-gate.ts, lib/markos/b2b-agents/index.cjs, contracts/F-237-b2b-agents-readiness-v1.yaml, test/b2b-219/agents/registry-non-runnable.test.js, test/b2b-219/agents/activation-gate-trigger.test.js, test/b2b-219/agents/readiness-progression.test.js</files>
  <behavior>
    - Test 1 (registry): After Plan 06 b2b_growth_agent_readiness ships, table contains exactly 9 rows; agent_id UNIQUE; runnable=false on every row (Pitfall 9 regression)
    - Test 2 (registry): Each row's agent_tier is one of 4 valid values (EXP/ABM/REV/IAM); agent_name non-empty
    - Test 3 (registry): UPDATE single boolean (e.g., contracts_assigned=true) → runnable computes to false (still 7 booleans false); UPDATE all 8 booleans to true → runnable computes to true
    - Test 4 (registry): runnable column is GENERATED ALWAYS AS — direct INSERT runnable=true is silently overridden by GENERATED clause
    - Test 5 (activation-gate): UPDATE b2b_growth_agent_readiness SET (8 booleans = true) without activation_approval_id → EXCEPTION 'AGENT_ACTIVATION_REQUIRES_READINESS'; with activation_approval_id + readiness_check_id set → succeeds
    - Test 6 (readiness-progression): assertAgentRunnable({ agent_id: 'MARKOS-AGT-EXP-01' }) throws AgentNotRunnableError when row.runnable=false; returns void when runnable=true
    - Test 7 (re-seed safety): re-execute the agent seed INSERT (idempotent) → no duplicate rows; total count stays 9; ON CONFLICT (agent_id) DO UPDATE preserves modifications (Pitfall 12 fix)
  </behavior>
  <read_first>
    - 219-RESEARCH.md §6 Domain 6 (lines 894-963 — the 9-agent registry SQL block + tier-list + activation gate + Pitfall 9 + Pitfall 12)
    - obsidian/brain/SaaS Marketing OS Strategy Canon.md (agent module list)
    - obsidian/work/incoming/17-SAAS-MARKETING-OS-STRATEGY.md (B2B agent enumeration in Part 13)
    - .planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-06-PLAN.md Task 1 (REPLICATE the 28-agent registry pattern; downscope to P219's 9 B2B agents)
  </read_first>
  <action>
    Step 1: Write tests 1-7 BEFORE migration ALTER (TDD RED).
    Step 2: APPEND to supabase/migrations/85_b2b_revenue_team.sql (or ship as a new sub-migration 85b — choose based on whether migration 85 has already been applied at execution time. Per RESEARCH §F-ID and Migration Slot Allocation Plan 06 strategy, additive seed in Plan 01 migration is acceptable. If migration 85 already applied, Plan 06 ships supabase/migrations/85_b2b_revenue_team_part2.sql as additive companion file). The DDL appended:
      ```sql
      -- Plan 219-06 / Phase 219 / b2b_growth_agent_readiness registry / RESEARCH §6 Domain 6
      CREATE TABLE b2b_growth_agent_readiness (
        agent_id text PRIMARY KEY,
        agent_name text NOT NULL,
        agent_tier text NOT NULL CHECK (agent_tier IN ('EXP','ABM','REV','IAM')),
        contracts_assigned boolean NOT NULL DEFAULT false,
        cost_estimated boolean NOT NULL DEFAULT false,
        approval_posture_defined boolean NOT NULL DEFAULT false,
        tests_implemented boolean NOT NULL DEFAULT false,
        api_surface_defined boolean NOT NULL DEFAULT false,
        mcp_surface_defined boolean NOT NULL DEFAULT false,
        ui_surface_defined boolean NOT NULL DEFAULT false,
        failure_behavior_defined boolean NOT NULL DEFAULT false,
        runnable boolean GENERATED ALWAYS AS (
          contracts_assigned AND cost_estimated AND approval_posture_defined
          AND tests_implemented AND api_surface_defined AND mcp_surface_defined
          AND ui_surface_defined AND failure_behavior_defined
        ) STORED,
        activation_approval_id uuid,
        readiness_check_id uuid,
        blocking_phase text DEFAULT 'P219-06',
        notes text,
        created_at timestamptz NOT NULL DEFAULT now(),
        updated_at timestamptz NOT NULL DEFAULT now()
      );
      -- system-level (NOT tenant-scoped); service-role-only RLS
      ALTER TABLE b2b_growth_agent_readiness ENABLE ROW LEVEL SECURITY;
      CREATE POLICY service_role_only ON b2b_growth_agent_readiness FOR ALL USING (current_setting('role', true) = 'service_role');

      -- Seed 9 B2B agents (Pitfall 12: ON CONFLICT DO UPDATE for re-seed safety)
      INSERT INTO b2b_growth_agent_readiness (agent_id, agent_name, agent_tier, blocking_phase) VALUES
        ('MARKOS-AGT-EXP-01', 'Expansion Intelligence Agent', 'EXP', 'P219-06'),
        ('MARKOS-AGT-EXP-02', 'Customer Marketing Manager', 'EXP', 'P219-06'),
        ('MARKOS-AGT-EXP-03', 'Advocacy Engine', 'EXP', 'P219-06'),
        ('MARKOS-AGT-ABM-01', 'ABM Account Intelligence Agent', 'ABM', 'P219-06'),
        ('MARKOS-AGT-ABM-02', 'ABM Content Personalization Agent', 'ABM', 'P219-06'),
        ('MARKOS-AGT-ABM-03', 'ABM Orchestration Agent', 'ABM', 'P219-06'),
        ('MARKOS-AGT-REV-01', 'Revenue Intelligence Agent', 'REV', 'P219-06'),
        ('MARKOS-AGT-REV-02', 'Marketing-Sales Alignment Agent', 'REV', 'P219-06'),
        ('MARKOS-AGT-IAM-01', 'In-App Campaign Orchestrator', 'IAM', 'P218-06-CONFIRM')
      ON CONFLICT (agent_id) DO UPDATE SET
        agent_name = EXCLUDED.agent_name,
        agent_tier = EXCLUDED.agent_tier,
        blocking_phase = EXCLUDED.blocking_phase,
        updated_at = now();

      -- Activation gate trigger (defense-in-depth)
      CREATE OR REPLACE FUNCTION fn_b2b_agent_activation_gate() RETURNS trigger AS $$
      BEGIN
        IF NEW.runnable = true AND (NEW.activation_approval_id IS NULL OR NEW.readiness_check_id IS NULL) THEN
          RAISE EXCEPTION 'AGENT_ACTIVATION_REQUIRES_READINESS';
        END IF;
        RETURN NEW;
      END;
      $$ LANGUAGE plpgsql;
      CREATE TRIGGER trg_b2b_agent_activation_gate BEFORE UPDATE ON b2b_growth_agent_readiness FOR EACH ROW EXECUTE FUNCTION fn_b2b_agent_activation_gate();

      DO $$
      BEGIN
        ASSERT (SELECT count(*) FROM b2b_growth_agent_readiness) = 9;
        ASSERT (SELECT count(*) FROM b2b_growth_agent_readiness WHERE runnable = true) = 0;
      END;
      $$;
      ```
    Step 3: Implement lib/markos/b2b-agents/contracts.ts:
      - Export 1 enum constant B2B_AGENT_TIERS = ['EXP','ABM','REV','IAM'] as const
      - Export zod schema B2bAgentReadinessSchema mirroring table cols
      - CommonJS twin contracts.cjs
    Step 4: Implement lib/markos/b2b-agents/readiness-registry.ts:
      - updateReadinessFlag({ agent_id, flag_name, value, evidence_ref }) UPDATE single boolean; recomputeRunnable triggered automatically via GENERATED column
      - getAgentReadiness, listAgentReadiness CRUD
    Step 5: Implement lib/markos/b2b-agents/readiness-gate.ts:
      - assertAgentRunnable({ agent_id }) — SELECT runnable; throw AgentNotRunnableError if false
    Step 6: Write F-237 contract YAML (read-only GET + per-flag PATCH).
    Step 7: Apply ALTER + run all 7 tests (TDD GREEN).
  </action>
  <acceptance_criteria>
    - `npm test -- test/b2b-219/agents/` all green (7+ assertions)
    - `psql -c "SELECT count(*) FROM b2b_growth_agent_readiness"` returns 9
    - `psql -c "SELECT count(*) FROM b2b_growth_agent_readiness WHERE runnable=true"` returns 0 (SG-10 invariant)
    - `psql -c "SELECT count(*) FROM pg_trigger WHERE tgname = 'trg_b2b_agent_activation_gate'"` returns 1
    - `node -e "console.log(typeof require('./lib/markos/b2b-agents/index.cjs').assertAgentRunnable)"` outputs `function`
    - re-running migration 85 (`psql < migration.sql`) does not duplicate rows (Pitfall 12 fix)
    - `grep -E "MARKOS-AGT-(EXP|ABM|REV|IAM)" supabase/migrations/85_b2b_revenue_team.sql | wc -l` returns at least 9 (one row per agent)
    - `grep -E "AGENT_ACTIVATION_REQUIRES_READINESS" supabase/migrations/85_b2b_revenue_team.sql | wc -l` returns at least 1 (exact exception string)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 14 API handlers + 5 cron handlers + MCP tool family + F-237 + 6 closeout regression tests</name>
  <files>api/v1/b2b/revenue-team-config.js, api/v1/b2b/revenue-team-config/sla-events.js, api/v1/b2b/revenue-team-config/feedback.js, api/v1/b2b/expansion/opportunities.js, api/v1/b2b/customer-marketing/programs.js, api/v1/b2b/customer-marketing/programs/enrollments.js, api/v1/b2b/abm/packages.js, api/v1/b2b/abm/packages/committee.js, api/v1/b2b/abm/packages/engagement.js, api/v1/b2b/advocacy/candidates.js, api/v1/b2b/advocacy/review-requests.js, api/v1/b2b/advocacy/proof.js, api/v1/b2b/advocacy/consent.js, api/v1/b2b/pricing/expansion-offers.js, api/v1/b2b/pricing/save-offers.js, api/v1/b2b/pricing/discount-authorizations.js, api/v1/b2b/agents/readiness.js, api/cron/b2b-sla-breach-monitor.js, api/cron/b2b-expansion-signal-scanner.js, api/cron/b2b-abm-enrichment-refresh.js, api/cron/b2b-advocacy-signal-scanner.js, api/cron/b2b-review-request-cadence.js, lib/markos/mcp/tools/b2b.cjs, lib/markos/mcp/tools/index.cjs, contracts/flow-registry.json, test/b2b-219/api/contract-cross-tenant-deny.test.js, test/b2b-219/api/architecture-lock-api-handlers.test.js, test/b2b-219/cron/cron-token-auth.test.js, test/b2b-219/mcp/tool-registration.test.js, test/b2b-219/mcp/cross-tenant-isolation.test.js, test/b2b-219/closeout/slot-collision-regression.test.js, test/b2b-219/closeout/all-domains-architecture-lock-rerun.test.js, test/b2b-219/openapi/parity-closeout.test.js</files>
  <behavior>
    - Test 1 (api): every API handler under api/v1/b2b/* passes contract-cross-tenant-deny test (INSERT under tenant A, GET under tenant B → 404)
    - Test 2 (api): architecture-lock-api-handlers — grep "createApprovalPackage|requireSupabaseAuth|requireTenantContext|app/(b2b)|app/(growth)|route.ts|vitest|.test.ts" api/v1/b2b/ api/cron/b2b-* | wc -l = 0
    - Test 3 (cron): missing x-markos-cron-secret header → 401; wrong secret → 401 (timingSafeEqual); correct secret → 200 + JSON {success, count, duration_ms}
    - Test 4 (cron): each of 5 cron handlers callable independently; idempotent (second invocation does not duplicate work)
    - Test 5 (mcp): lib/markos/mcp/tools/index.cjs after Plan 06 modification has TOOL_DESCRIPTORS.length = 30 (existing) + 12 (b2b) = 42; b2b_* tools are last 12 entries; each has valid name + description + JSON-RPC schema
    - Test 6 (mcp): cross-tenant isolation — tool handler with tenant A context returns ONLY tenant A data (RLS enforced via runtimeContext.tenant_id)
    - Test 7 (slot-collision-regression): re-run Plan 01 baseline guards — assert no migrations outside 85-89 range claim P219 slots; F-228..F-237 owned exclusively
    - Test 8 (all-domains-architecture-lock-rerun): full forbidden-pattern scan across all P219 surface returns 0 violations
    - Test 9 (openapi-parity-closeout): contracts/openapi.json contains all paths from Plans 01-06 + 12 MCP tool descriptors + F-228..F-237 contracts; final regen succeeds
  </behavior>
  <read_first>
    - 219-RESEARCH.md §6 (all 6 domains — API surface tables + Cron handlers + MCP tools)
    - api/cron/webhooks-dlq-purge.js (REPLICATE this exact pattern for cron auth + writeJson + JSON response)
    - lib/markos/mcp/tools/index.cjs (existing 30 tool descriptors; will add 12 b2b tools)
    - All 5 P219 lib modules (lib/markos/{revenue-team,expansion,abm,advocacy,b2b-pricing}/) — API handlers consume their exports
    - onboarding/backend/runtime-context.cjs:491 (requireHostedSupabaseAuth signature)
    - .planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-06-PLAN.md Task 2 (REPLICATE 19 API handler + 6 cron + 12 MCP pattern; downscope to P219 14+5+12)
  </read_first>
  <action>
    Step 1: Write tests 1-9 BEFORE implementations (TDD RED).
    Step 2: Implement 14 API handlers under api/v1/b2b/* (legacy *.js per architecture-lock):
      - Each file uses `'use strict';` header + `const { requireHostedSupabaseAuth } = require('../../../onboarding/backend/runtime-context.cjs');` + `const writeJson = require('../../_helpers/writeJson');` + lib/markos/{domain}/* import
      - GET handlers list/read with tenant_id filter via runtimeContext
      - POST/PATCH handlers wrap mutations + delegate to lib functions (which already wrap buildApprovalPackage where required)
      - Each handler exports default async function (req, res) handler — module.exports
      - F-ID assignment is locked at planning time per V4.1.0-MIGRATION-SLOT-COORDINATION.md table:
        P219-01: F-228, F-229
        P219-02: F-230, F-231
        P219-03: F-232, F-233
        P219-04: F-234, F-235
        P219-05: F-236
        P219-06: F-237
        No runtime renaming required. If executor encounters duplicate F-ID in flow-registry.json, FAIL the closeout.
    Step 3: Implement 5 cron handlers under api/cron/b2b-*.js:
      - Each replicates api/cron/webhooks-dlq-purge.js exactly: POST-only + x-markos-cron-secret header + crypto.timingSafeEqual + writeJson + {success, count, duration_ms} JSON response
      - Cron secret env: MARKOS_B2B_CRON_SECRET (NEW per RESEARCH §Architecture-Lock)
      - Each calls respective lib function (detectSlaBreaches, scanExpansionSignals, listStalePackages-then-create-task, scanAdvocacySignals, dispatchPendingReviewRequests)
    Step 4: Implement lib/markos/mcp/tools/b2b.cjs:
      - 12 tool descriptors per RESEARCH §6 each domain MCP tool sections
      - 9 read-only: b2b_revenue_team_config_get, b2b_mql_sla_status, b2b_feedback_record_get, b2b_expansion_opportunities_list, b2b_customer_marketing_programs_list, b2b_abm_packages_list, b2b_abm_buying_committee_get, b2b_advocacy_candidates_list, b2b_proof_assets_list
      - 3 write-gated: b2b_expansion_offer_draft, b2b_save_offer_draft, b2b_agents_readiness_get (last is read but operator-only) — write tools each invoke buildApprovalPackage via the underlying lib
      - Each tool descriptor has name + description + JSON-RPC schema + cost_window envelope
      - Module exports B2B_MCP_TOOLS array
    Step 5: Modify lib/markos/mcp/tools/index.cjs to require b2b.cjs and concat into TOOL_DESCRIPTORS:
      ```javascript
      const { B2B_MCP_TOOLS } = require('./b2b.cjs');
      const TOOL_DESCRIPTORS = [...EXISTING_TOOLS, ...B2B_MCP_TOOLS];
      ```
    Step 6: Write F-237 contract YAML (b2b-agents-readiness-v1).
    Step 7: Update flow-registry.json appending F-237 (final P219 contract). Verify NO collision against F-001..F-236.
    Step 8: Run all tests (TDD GREEN). npm run openapi:build (final regen).
  </action>
  <acceptance_criteria>
    - `npm test -- test/b2b-219/api/ test/b2b-219/cron/ test/b2b-219/mcp/ test/b2b-219/closeout/ test/b2b-219/openapi/parity-closeout.test.js` all green (9+ assertions)
    - `ls api/v1/b2b/**/*.js | wc -l` returns >= 14
    - `ls api/cron/b2b-*.js | wc -l` returns 5
    - `grep -rn "createApprovalPackage|requireSupabaseAuth|requireTenantContext|serviceRoleClient|lookupPlugin|app/(b2b)|app/(growth)|route.ts|vitest|.test.ts|openapi-generate" api/v1/b2b/ api/cron/b2b-* lib/markos/mcp/tools/b2b.cjs | wc -l` returns 0
    - `grep "F-237" contracts/flow-registry.json | wc -l` returns 1
    - `node -e "console.log(require('./lib/markos/mcp/tools/index.cjs').TOOL_DESCRIPTORS.length)"` outputs >= 42
    - `npm run openapi:build` regenerates with all 14 P219 paths + agent_readiness + MCP tool descriptors
    - `grep -E "MARKOS_B2B_CRON_SECRET" api/cron/b2b-*.js | wc -l` returns at least 5 (one per cron)
    - `grep -E "x-markos-cron-secret" api/cron/b2b-*.js | wc -l` returns at least 5
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: First-run B2B agent activation operator validation</name>
  <files>(none — operator-driven validation; no file changes)</files>
  <action>Pause unattended execution. Surface the first-batch agent activation validation checklist below to the operator. Operator manually validates the 8 readiness criteria for each agent they intend to activate, updates readiness flags via authenticated PATCH /v1/b2b/agents/readiness/{agent_id} (audited per-flag), then approves activation via approval inbox. Plan resumes only when operator types "approved" or scopes deferrals. NO auto-flip permitted; cron handlers do NOT auto-flip readiness; SG-10 invariant maintained until GSD assigns full readiness per agent.</action>
  <what-built>14 API handlers + 5 cron handlers + 12 MCP tools live; b2b_growth_agent_readiness pre-populated with 9 P219 B2B agents (all runnable=false); activation gate DB-trigger blocks unauthorized flips. Plan 06 closeout deliverables shipped per RESEARCH §6 Domain 6 + Manual-Only Verifications table.</what-built>
  <how-to-verify>
    Operator validates first-batch agent readiness BEFORE permitting any agent-runnable=true flip in production. Per RESEARCH §6 Domain 6 Test strategy: registry pre-populates all 9 agents non-runnable; SG-10 invariant guarantees no agent runs until GSD assigns full readiness criteria.

    First-run validation steps (operator performs):
    1. Pull `psql -c "SELECT agent_id, agent_tier, runnable FROM b2b_growth_agent_readiness ORDER BY agent_tier, agent_id"` — verify 9 rows, all runnable=false
    2. Review the agent registry in operator UI (or fall back to SQL): `SELECT * FROM b2b_growth_agent_readiness WHERE agent_tier IN ('EXP','ABM','REV')` (P219-owned 8 tiers; IAM-01 is P218-routed)
    3. For first agent the operator wants to activate, validate readiness criteria:
       - contracts_assigned: F-ID YAML exists in contracts/
       - cost_estimated: cost_window registered for the agent's MCP tools
       - approval_posture_defined: buildApprovalPackage kind defined in code
       - tests_implemented: test/b2b-219/{domain}/*.test.js green
       - api_surface_defined: api/v1/b2b/{path}/*.js exists
       - mcp_surface_defined: tool descriptor in lib/markos/mcp/tools/b2b.cjs
       - ui_surface_defined: Storybook story exists (deferred to follow-up phase if not yet)
       - failure_behavior_defined: AgentRun retry policy documented
    4. Operator updates each readiness flag via authenticated PATCH /v1/b2b/agents/readiness/{agent_id} — each PATCH writes audit row
    5. When all 8 booleans flip to true, operator approves activation via approval inbox (fn_b2b_agent_activation_gate DB-trigger requires activation_approval_id + readiness_check_id)
    6. assertAgentRunnable({ agent_id }) succeeds + AgentRun creation permitted

    Cron handlers DO NOT auto-flip readiness — operator must validate first batch manually before unattended cron permits ongoing activations.
  </how-to-verify>
  <resume-signal>Type "approved" to confirm operator validated first-batch readiness criteria and acknowledges SG-10 invariant maintained, or describe any agent for which readiness flip needs deferral / additional GSD work.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 4: 219-VALIDATION.md final flip + 219-CLOSEOUT-SUMMARY + ROADMAP marker + requirements-coverage assertion</name>
  <files>.planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-VALIDATION.md, test/b2b-219/closeout/requirements-coverage.test.js</files>
  <behavior>
    - Test 1 (requirements-coverage): parses 219-VALIDATION.md per-task verification map; assert SG-03 + SG-08 + SG-09 + SG-10 + SG-11 + SG-12 + LOOP-06 + EVD-01..06 + T0-04 + QA-01..15 each map to >= 1 task; total = 30 unique requirement IDs covered (SG-03/08/09/10/11/12 = 6 + LOOP-06 = 1 + EVD-01..06 = 6 + T0-04 = 1 + QA-01..15 = 15 + 1 buffer = 30); INSERT failure (any ID with 0 task mappings) raises AssertionError
  </behavior>
  <read_first>
    - 219-VALIDATION.md (current state from Plan 01 + Plans 02-05 incremental updates)
    - 219-RESEARCH.md §Validation Architecture (Phase Requirements → Test Map — final populate)
    - .planning/REQUIREMENTS.md (verify all 30 IDs from phase scope)
  </read_first>
  <action>
    Step 1: Final populate 219-VALIDATION.md Per-Task Verification Map with ALL 6 plans + every task ID + Status column flipped to ✅ green where tests pass:
      | Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
      | 219-01-00 | 01 | 1 | QA-01,02 | preflight | npm test -- test/b2b-219/preflight/ | ✅ | ✅ green |
      | 219-01-0.5 | 01 | 1 | QA-01,02 | architecture-lock | npm test -- test/b2b-219/preflight/architecture-lock.test.js | ✅ | ✅ green |
      | 219-01-01 | 01 | 1 | SG-08, LOOP-06, QA-02 | schema + RLS + DB-trigger | npm test -- test/b2b-219/revenue-team/ test/b2b-219/rls/revenue-team-* | ✅ | ✅ green |
      | 219-01-02 | 01 | 1 | SG-08, LOOP-06, SG-12 | unit + DB-trigger + parity | npm test -- test/b2b-219/revenue-team/ test/b2b-219/openapi/parity-revenue-team.test.js | ✅ | ✅ green |
      | 219-02-01 | 02 | 2 | SG-03, SG-09, SG-12 | schema + DB-trigger + RLS | npm test -- test/b2b-219/expansion/ | ✅ | ✅ green |
      | 219-02-02 | 02 | 2 | SG-03, SG-09, EVD-01..04 | unit + signal-scanner | npm test -- test/b2b-219/expansion/signal-scanner.test.js | ✅ | ✅ green |
      | 219-03-01 | 03 | 2 | SG-03, EVD-03,04 | schema + DB-trigger + RLS | npm test -- test/b2b-219/abm/ | ✅ | ✅ green |
      | 219-03-02 | 03 | 2 | SG-03, EVD-01..06 | unit + committee + staleness | npm test -- test/b2b-219/abm/committee-inferred-flag.test.js | ✅ | ✅ green |
      | 219-04-01 | 04 | 3 | SG-03, T0-04, EVD-01,02,05 | schema + 2 DB-triggers + RLS + view | npm test -- test/b2b-219/advocacy/ | ✅ | ✅ green |
      | 219-04-02 | 04 | 3 | T0-04, EVD-05, SG-12 | T0-04 posture + approval payload | npm test -- test/b2b-219/advocacy/t0-04-proof-posture.test.js test/b2b-219/advocacy/approval-payload-shape.test.js | ✅ | ✅ green |
      | 219-05-01 | 05 | 3 | SG-11, EVD-06 | schema + 4 DB-triggers + RLS | npm test -- test/b2b-219/pricing/ | ✅ | ✅ green |
      | 219-05-02 | 05 | 3 | SG-11, EVD-06, SG-12 | sentinel + EVD fields | npm test -- test/b2b-219/pricing/sentinel-detection.test.js test/b2b-219/pricing/evidence-fields.test.js | ✅ | ✅ green |
      | 219-06-01 | 06 | 4 | SG-10 | registry + activation gate | npm test -- test/b2b-219/agents/ | ✅ | ✅ green |
      | 219-06-02 | 06 | 4 | API-01, MCP-01, QA-03..15 | API + cron + MCP + closeout | npm test -- test/b2b-219/api/ test/b2b-219/cron/ test/b2b-219/mcp/ test/b2b-219/closeout/ | ✅ | ✅ green |
    Step 2: Implement test/b2b-219/closeout/requirements-coverage.test.js — parses 219-VALIDATION.md per-task verification map (markdown table); assert SG-03, SG-08, SG-09, SG-10, SG-11, SG-12, LOOP-06, EVD-01, EVD-02, EVD-03, EVD-04, EVD-05, EVD-06, T0-04, QA-01..QA-15 each map to >= 1 task. Total = 29 unique requirement IDs covered (verify against actual phase scope from 219-CONTEXT and 219-RESEARCH).
    Step 3: Update Dimensions Coverage table — flip all rows to LOCKED (Requirements coverage / Anti-shallow execution / Architecture-lock / Compliance enforcement / Cross-phase coordination / Single-writer DB-trigger enforcement / Test runner pinned / Validation strategy).
    Step 4: Run requirements-coverage test — must pass (TDD GREEN).
    Step 5: Update ROADMAP.md Phase 219 entry — flip Status from "📋 Planned" to "✅ Plans replanned (6/6); ready for execution" + add cross-AI review note ("Plans replanned 2026-04-26 per cross-AI review override; addressed 1 HIGH stub-state + 7 MED + 2 LOW; Q-1 + Q-2 + Q-4 resolutions documented in V4.1.0-MIGRATION-SLOT-COORDINATION.md").
  </action>
  <acceptance_criteria>
    - `npm test -- test/b2b-219/closeout/requirements-coverage.test.js` passes (29 requirement IDs covered: SG-03/08/09/10/11/12 + LOOP-06 + EVD-01..06 + T0-04 + QA-01..15)
    - 219-VALIDATION.md per-task map fully populated with all Plans 01-06 tasks + all green
    - All 8 Dimensions Coverage rows = LOCKED
    - ROADMAP.md Phase 219 entry status updated
    - `grep -E "SG-03|SG-08|SG-09|SG-10|SG-11|SG-12|LOOP-06|EVD-0[1-6]|T0-04|QA-0[1-9]|QA-1[0-5]" .planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-VALIDATION.md | wc -l` returns >= 29
    - `grep -E "nyquist_compliant: true" .planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-VALIDATION.md | wc -l` returns 1
    - `grep -E "wave_0_complete: true" .planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-VALIDATION.md | wc -l` returns 1
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → PATCH /v1/b2b/agents/readiness/{agent_id} | Untrusted; per-flag audit + approval required for activation transition |
| Service-role direct UPDATE on b2b_growth_agent_readiness | DB-trigger blocks runnable=true flip without activation_approval_id + readiness_check_id |
| MCP client → b2b_* tools | Tenant-scoped via runtimeContext; cross-tenant isolation tested |
| Cron worker → b2b-*.js | x-markos-cron-secret + timingSafeEqual; per-env MARKOS_B2B_CRON_SECRET rotation |
| AgentRun creation → assertAgentRunnable | Throws AgentNotRunnableError when row.runnable=false; SG-10 invariant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-219-06-01 | Tampering | b2b_growth_agent_readiness UPDATE bypasses activation gate | mitigate | DB-trigger fn_b2b_agent_activation_gate; activation_approval_id + readiness_check_id NOT NULL when runnable=true |
| T-219-06-02 | Spoofing | Cron token leaked → unauthorized B2B cron invocation | mitigate | crypto.timingSafeEqual on cron token; per-env MARKOS_B2B_CRON_SECRET rotation |
| T-219-06-03 | Information Disclosure | MCP tool returns cross-tenant data | mitigate | tool handlers use runtimeContext.tenant_id; RLS enforced; tested |
| T-219-06-04 | Repudiation | Operator denies flipping readiness flag | mitigate | per-flag PATCH audit row + evidence_ref; SHA-256 hash chain |
| T-219-06-05 | Spoofing | Bot creates agent_id rows via unauthenticated API | mitigate | service-role-only RLS on b2b_growth_agent_readiness; PATCH gated by requireHostedSupabaseAuth |
| T-219-06-06 | Elevation of Privilege | Operator activates agent without all 8 readiness criteria | mitigate | runnable column GENERATED ALWAYS AS — cannot be set directly; DB-trigger checks runnable AND activation_approval_id |
| T-219-06-07 | Tampering | Re-seed migration overwrites operator-modified readiness flags | mitigate | ON CONFLICT DO UPDATE only updates name/tier/blocking_phase, NOT readiness booleans (Pitfall 12 fix) |
| T-219-06-08 | Information Disclosure | b2b_growth_agent_readiness leaks across tenants | accept | Table is system-level (NOT tenant-scoped); service-role only; agents are global by design |
| T-219-06-09 | Denial of Service | Cron handlers create unbounded SLA breach tasks | mitigate | Each cron handler LIMIT 100 per tenant per invocation |
| T-219-06-10 | Tampering | Architecture-lock RE-RUN test ignored in CI | mitigate | requirements-coverage assertion forces test inclusion; failing test blocks merge |
</threat_model>

<verification>
**Plan-level verification (run after Task 4 closes):**

- `npm test -- test/b2b-219/agents/ test/b2b-219/api/ test/b2b-219/cron/ test/b2b-219/mcp/ test/b2b-219/closeout/ test/b2b-219/openapi/parity-closeout.test.js` → all green
- `node scripts/preconditions/219-06-check-upstream.cjs` exits 0 only when all 5 prior plans complete + P207 agent_runs present
- `psql -c "SELECT count(*) FROM b2b_growth_agent_readiness"` returns 9
- `psql -c "SELECT count(*) FROM b2b_growth_agent_readiness WHERE runnable=true"` returns 0
- `psql -c "SELECT count(*) FROM pg_trigger WHERE tgname = 'trg_b2b_agent_activation_gate'"` returns 1
- `grep -E "F-237" contracts/flow-registry.json | wc -l` returns 1
- `grep -rn "createApprovalPackage|requireSupabaseAuth|requireTenantContext|serviceRoleClient|lookupPlugin|public/openapi.json|app/(b2b)|app/(growth)|api/v1/.*/route.ts|vitest|.test.ts" lib/markos/b2b/ lib/markos/revenue-team/ lib/markos/expansion/ lib/markos/abm/ lib/markos/advocacy/ lib/markos/b2b-pricing/ lib/markos/b2b-agents/ api/v1/b2b/ api/cron/b2b-* lib/markos/mcp/tools/b2b.cjs scripts/preconditions/219-06-check-upstream.cjs | wc -l` returns 0 (all-domains architecture-lock RE-RUN green)
- `node -e "console.log(require('./lib/markos/mcp/tools/index.cjs').TOOL_DESCRIPTORS.length)"` outputs >= 42
</verification>

<success_criteria>
- All 4 tasks complete with passing tests; total ~25+ closeout assertions green
- 1 SOR table addition + 4 b2b-agents lib files + 1 MCP registry + 14 API handlers + 5 cron handlers + 1 contract ship with NO architecture-lock violations
- SG-10 invariant proven via DB-trigger + GENERATED column + 9-agent registry pre-populated runnable=false
- Plan 06 autonomous=false + checkpoint:human-action operator validation gate maintained
- Closeout regression tests green (slot-collision + all-domains architecture-lock RE-RUN + requirements-coverage)
- 219-VALIDATION.md per-task map fully populated; nyquist_compliant + wave_0_complete + all 8 dimensions LOCKED
</success_criteria>

<output>
After completion, create `.planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-CLOSEOUT-SUMMARY.md` documenting:

- Wave 4 complete; all 6 plans shipped
- Total tables: 16 SOR + 1 readiness registry = 17 tables across slots 85-89 (+ Plan 06 ALTER to 85)
- Total DB-triggers: 12 + 1 activation gate = 13 triggers (defense-in-depth)
- Total contracts: F-228..F-237 = 10 F-IDs (P219 range fully claimed)
- Total API handlers: 14 under api/v1/b2b/*
- Total cron handlers: 5 under api/cron/b2b-*.js
- Total MCP tools: 12 under lib/markos/mcp/tools/b2b.cjs (registered in index.cjs)
- 9 P219 B2B agents seeded runnable=false (SG-10 invariant maintained until operator validation)
- All cross-phase coordination resolutions: Q-1 (P218 ICP vs P219 ABM committee), Q-2 (P219 advocacy vs P220 PR), Q-4 (P205 SOFT + sentinel)
- Architecture-lock RE-RUN green; slot-collision regression green; requirements-coverage assertion green
- Cross-AI review HIGH (1) + MEDIUM (7) + LOW (2) concerns from 219-REVIEWS.md addressed
</output>
</content>
</invoke>