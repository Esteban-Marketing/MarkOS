---
phase: 219
plan: "03"
type: execute
wave: 2
depends_on:
  - "219-01"
files_modified:
  - supabase/migrations/109_b2b_abm.sql
  - lib/markos/abm/contracts.ts
  - lib/markos/abm/contracts.cjs
  - lib/markos/abm/packages.ts
  - lib/markos/abm/packages.cjs
  - lib/markos/abm/buying-committee.ts
  - lib/markos/abm/engagement.ts
  - lib/markos/abm/enrichment.ts
  - lib/markos/abm/index.cjs
  - contracts/F-232-b2b-abm-account-package-v1.yaml
  - contracts/F-233-b2b-abm-buying-committee-v1.yaml
  - contracts/flow-registry.json
  - scripts/preconditions/219-03-check-upstream.cjs
  - test/b2b-219/abm/package-crud.test.js
  - test/b2b-219/abm/enrichment-audit-trigger.test.js
  - test/b2b-219/abm/external-outreach-trigger.test.js
  - test/b2b-219/abm/module-eligibility-b2b-only.test.js
  - test/b2b-219/abm/committee-inferred-flag.test.js
  - test/b2b-219/abm/stage-transition-approval.test.js
  - test/b2b-219/abm/evidence-refs-validation.test.js
  - test/b2b-219/abm/enrichment-staleness.test.js
  - test/b2b-219/rls/abm-cross-tenant-denial.test.js
  - test/b2b-219/openapi/parity-abm.test.js
autonomous: true
requirements:
  - SG-03
  - SG-09
  - SG-12
  - EVD-01
  - EVD-02
  - EVD-03
  - EVD-04
  - EVD-05
  - EVD-06
  - QA-01
  - QA-02
  - QA-03
  - QA-04
must_haves:
  truths:
    - Wave 2 entry — assertUpstreamReady preflight (scripts/preconditions/219-03-check-upstream.cjs) consumed from Plan 01 architecture-lock surface; hard-fails if 219-01 helper module trees missing OR P214/P215/P218 tables missing
    - Migration 109 ships per V4.1.0-MIGRATION-SLOT-COORDINATION.md (Q-7 reallocation from 87); CONTAINS 3 SOR tables (abm_account_packages, abm_buying_committee_members, abm_engagement_events) + 2 DB-trigger functions + 2 hot-path indexes
    - All 3 ABM SOR tables RLS-enabled with tenant_id policy; abm_account_packages UNIQUE(tenant_id, account_id) prevents duplicate packages per account; abm_buying_committee_members FK to abm_account_packages.package_id; abm_engagement_events FK to package + optional FK to member
    - DB-trigger compliance — fn_abm_enrichment_audit_required BEFORE UPDATE on abm_account_packages raises EXCEPTION 'ABM_ENRICHMENT_AUDIT_REQUIRED' when NEW.enriched_at IS NOT NULL AND NEW.enriched_at IS DISTINCT FROM OLD.enriched_at AND NEW.enrichment_source NOT IN ('manual', 'operator')
    - DB-trigger compliance #2 — fn_abm_external_outreach_requires_approval BEFORE INSERT OR UPDATE on abm_account_packages raises EXCEPTION 'ABM_EXTERNAL_OUTREACH_REQUIRES_APPROVAL' when NEW.stage IN ('meeting_booked', 'opportunity') AND NEW.approved_at IS NULL
    - P218 module_mode_eligibility consumption — every ABM SOR table (abm_account_packages, abm_buying_committee_members, abm_engagement_events) fires `MODULE_REQUIRES_ELIGIBLE_GROWTH_MODE` trigger before insert via fn_check_module_mode_eligibility (P218 migration 101 ships helper); abm_engine module RESTRICTED to b2b + plg_b2b modes ONLY (fails for b2c/plg_b2c/b2b2c per module_mode_eligibility seed)
    - 7 stage ENUM values land per RESEARCH §6 Domain 3 (identified, aware, engaged, meeting_booked, opportunity, customer, expansion_target); 3 abm_tier values (1, 2, 3); 4 enrichment_source values (manual, research_engine, intent_data_import, operator); 5 persona ENUM values (economic_buyer, champion, user, gatekeeper, influencer); 10 event_type ENUM values
    - lib/markos/abm/packages.ts wires buildApprovalPackage on stage transition to meeting_booked or opportunity; kind='abm_external_outreach'; idempotent via Idempotency-Key on package_id
    - lib/markos/abm/buying-committee.ts enforces is_inferred=true label for committee members lacking known_to_us=true (EVD-03); EVD-05 approval payload exposes is_inferred + assumptions
    - lib/markos/abm/enrichment.ts implements rescoreConfidence({ tenant_id, package_id, source, confidence, evidence_refs }) — UPDATE abm_account_packages SET enrichment_source/enrichment_confidence/enriched_at; for non-manual/non-operator sources, requires audit row written to lib/markos/audit (DB-trigger enforces); EVD-04 staleness check via enriched_at > 90d flag
    - 2 contracts ship F-232 (abm-account-package-v1) + F-233 (abm-buying-committee-v1) with NO collision; flow-registry.json + openapi.json regenerated
    - Architecture-lock test passes against lib/markos/abm/* — no createApprovalPackage / requireSupabaseAuth / etc.
    - Cross-tenant RLS denial proven across 3 ABM SOR tables
    - Cross-phase coordination Q-1 (P218 ICP vs P219 ABM committee) — RESOLVED with comment in migration 109 SQL: "P219-owned abm_buying_committee_members. P222 will ship buying_committee_members (CRM-side). Distinct tables, distinct FK parents. No collision."
    - EVD-06 — pricing/competitor intelligence in company_profile_jsonb / strategic_signals_jsonb has source_quality + extraction_method + timestamp + compliance_posture sub-fields per zod schema
  artifacts:
    - path: supabase/migrations/109_b2b_abm.sql
      provides: (slot 109 post Q-7 reallocation from 87) abm_account_packages (20 cols — package_id PK + tenant_id FK + account_id + company_domain + abm_tier CHECK 1/2/3 + stage CHECK 7 values DEFAULT 'identified' + company_profile_jsonb + strategic_signals_jsonb + messaging_jsonb + engagement_jsonb + evidence_refs uuid[] + enrichment_source CHECK 4 values + enrichment_confidence numeric(4,2) CHECK 0-1 + enriched_at + inferred_fields text[] + approved_at + approved_by + created_at + updated_at + UNIQUE(tenant_id, account_id)) + abm_buying_committee_members (12 cols — member_id PK + tenant_id + package_id FK abm_account_packages + contact_id + role_title + persona CHECK 5 values + linkedin_url + known_to_us bool DEFAULT false + last_interaction_at + preferred_content_format + key_concerns text[] + is_inferred bool DEFAULT false + created_at + updated_at) + abm_engagement_events (7 cols — engagement_id PK + tenant_id + package_id FK + event_type CHECK 10 values + member_id FK abm_buying_committee_members + payload_jsonb + created_at) + RLS on all 3 + idx_abm_tier_stage ON abm_account_packages(tenant_id, abm_tier, stage) + idx_abm_engagement_package ON abm_engagement_events(tenant_id, package_id, created_at) + 2 DB-triggers + DO ASSERT
    - path: lib/markos/abm/contracts.ts
      provides: 5 enum constants (ABM_STAGES, ABM_TIERS, ABM_ENRICHMENT_SOURCES, ABM_PERSONAS, ABM_EVENT_TYPES) + zod schemas (AbmAccountPackageSchema, AbmBuyingCommitteeMemberSchema, AbmEngagementEventSchema) — strategic_signals_jsonb fields include source_quality (numeric 0-1), extraction_method (text), extracted_at (timestamptz), compliance_posture (text) per EVD-06
      exports: [ABM_STAGES, ABM_TIERS, ABM_ENRICHMENT_SOURCES, ABM_PERSONAS, ABM_EVENT_TYPES, AbmAccountPackageSchema, AbmBuyingCommitteeMemberSchema, AbmEngagementEventSchema]
    - path: lib/markos/abm/packages.ts
      provides: createPackage / getPackage / listPackages CRUD + transitionStage helper that wraps approval-required stage transitions (meeting_booked, opportunity) via buildApprovalPackage kind='abm_external_outreach'
      exports: [createPackage, getPackage, listPackages, updatePackage, transitionStage, requestExternalOutreachApproval]
    - path: lib/markos/abm/buying-committee.ts
      provides: addCommitteeMember({ tenant_id, package_id, role_title, persona, known_to_us, ... }) auto-sets is_inferred=true when known_to_us=false (EVD-03); listCommitteeMembers; markKnown({ member_id, last_interaction_at }) flips known_to_us=true and is_inferred=false; getInferredMembers returns committee members where is_inferred=true (operator review queue per EVD-05)
      exports: [addCommitteeMember, listCommitteeMembers, markKnown, getInferredMembers]
    - path: lib/markos/abm/engagement.ts
      provides: ingestEngagementEvent({ tenant_id, package_id, event_type, member_id?, payload_jsonb }) INSERT row; listPackageEngagement returns ordered timeline; computeEngagementScore for stage-transition heuristics
      exports: [ingestEngagementEvent, listPackageEngagement, computeEngagementScore]
    - path: lib/markos/abm/enrichment.ts
      provides: rescoreConfidence({ tenant_id, package_id, enrichment_source, enrichment_confidence, inferred_fields, evidence_refs }) — UPDATE row + write audit row before UPDATE (DB-trigger requires for non-manual sources); listStalePackages({ tenant_id, max_age_days = 90 }) returns packages where enriched_at < now() - max_age_days for re-enrichment task creation (EVD-04)
      exports: [rescoreConfidence, listStalePackages, isStale]
    - path: contracts/F-232-b2b-abm-account-package-v1.yaml
      provides: GET /v1/b2b/abm/packages (list/filter by tier/stage), POST + PATCH (stage transitions with approval gate); auth via requireHostedSupabaseAuth
    - path: contracts/F-233-b2b-abm-buying-committee-v1.yaml
      provides: GET /v1/b2b/abm/packages/{id}/committee + POST + PATCH (member CRUD); GET/POST /v1/b2b/abm/packages/{id}/engagement; auth via requireHostedSupabaseAuth
    - path: scripts/preconditions/219-03-check-upstream.cjs
      provides: CLI preflight; verifies 219-01 module tree + P214/P215/P218 tables; SOFT P209 evidence_map_records check (EVD-01..06 graceful degrade)
    - path: test/b2b-219/abm/enrichment-audit-trigger.test.js
      provides: service-role direct UPDATE on abm_account_packages SET enriched_at=now(), enrichment_source='research_engine' WHERE no audit row written → expect EXCEPTION 'ABM_ENRICHMENT_AUDIT_REQUIRED' (when source is non-manual); with enrichment_source='manual' → succeeds
    - path: test/b2b-219/abm/external-outreach-trigger.test.js
      provides: UPDATE abm_account_packages SET stage='meeting_booked' WHERE approved_at IS NULL → expect EXCEPTION 'ABM_EXTERNAL_OUTREACH_REQUIRES_APPROVAL'; with approved_at set → succeeds
    - path: test/b2b-219/abm/committee-inferred-flag.test.js
      provides: addCommitteeMember with known_to_us=false → row has is_inferred=true; markKnown → flips both flags; getInferredMembers returns only is_inferred=true rows
    - path: test/b2b-219/abm/module-eligibility-b2b-only.test.js
      provides: seed tenant growth_mode='b2c' → INSERT abm_account_packages raises 'MODULE_REQUIRES_ELIGIBLE_GROWTH_MODE'; same for plg_b2c, b2b2c; seed growth_mode='b2b' OR 'plg_b2b' → INSERT succeeds; coverage across all 3 ABM SOR tables
    - path: test/b2b-219/abm/evidence-refs-validation.test.js
      provides: app-layer transitionStage to 'meeting_booked' WITHOUT evidence_refs throws EvidenceRequiredError BEFORE buildApprovalPackage; WITH evidence_refs non-empty proceeds
    - path: test/b2b-219/abm/enrichment-staleness.test.js
      provides: package with enriched_at > 90d ago → listStalePackages returns it; isStale returns true; package with enriched_at within 90d → not in stale list
  key_links:
    - from: lib/markos/abm/packages.ts
      to: lib/markos/crm/agent-actions.ts
      via: buildApprovalPackage with kind='abm_external_outreach' on stage transition to meeting_booked / opportunity
      pattern: "buildApprovalPackage"
    - from: supabase/migrations/109_b2b_abm.sql
      to: lib/markos/abm/contracts.ts
      via: ABM_STAGES (7 values) + ABM_PERSONAS (5 values) + ABM_EVENT_TYPES (10 values) mirror CHECK constraints exactly
      pattern: "ABM_STAGES"
    - from: lib/markos/abm/buying-committee.ts
      to: supabase/migrations/109_b2b_abm.sql
      via: addCommitteeMember sets is_inferred=true when known_to_us=false; EVD-03 enforced at app layer
      pattern: "is_inferred"
    - from: lib/markos/abm/enrichment.ts
      to: lib/markos/audit
      via: rescoreConfidence emits audit row BEFORE UPDATE; DB-trigger fn_abm_enrichment_audit_required enforces audit row exists for non-manual sources
      pattern: "audit"
    - from: scripts/preconditions/219-03-check-upstream.cjs
      to: lib/markos/b2b/preflight/index.cjs
      via: const assertUpstreamReady = require('../../lib/markos/b2b/preflight/index.cjs')
      pattern: "assertUpstreamReady"
---

<objective>
Wave 2 plan B — translate ABM account intelligence + buying committee mapping into governed workflows per RESEARCH §6 Domain 3. Ship abm_account_packages + abm_buying_committee_members + abm_engagement_events SOR (migration 109), the external-outreach-approval gate via buildApprovalPackage, the enrichment-audit DB-trigger compliance pack, the is_inferred labeling for non-known committee members (EVD-03), the stale-package detection (EVD-04), and the source-quality fields in strategic_signals_jsonb (EVD-06).

Purpose: satisfy SG-03 (B2B SaaS growth covers ABM account intelligence + buying committee), SG-09 (modules emit tasks/approvals/learnings — NOT passive dashboards), SG-12 (account-personalized external outreach approval-gated), EVD-01..06 (evidence_refs populated; inferred fields labeled; staleness detection; source quality), QA-01..04 (architecture-lock + helper-presence + RLS isolation + F-ID parity).

Output: 1 SOR migration + 7 ABM lib files + 2 contracts + flow-registry update + 1 preflight CLI + 7 ABM test files + 1 RLS test file + 1 OpenAPI parity test.

Cross-phase note: P219 owns abm_buying_committee_members (ABM overlay); P222 will later ship buying_committee_members (CRM-side). Distinct tables — no collision per RESEARCH §Q-1 + Q5 resolution.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-CONTEXT.md
@.planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-RESEARCH.md
@.planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-01-SUMMARY.md
@.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@obsidian/brain/SaaS Marketing OS Strategy Canon.md
@obsidian/work/incoming/17-SAAS-MARKETING-OS-STRATEGY.md
@CLAUDE.md
@lib/markos/b2b/preflight/index.cjs
@lib/markos/crm/agent-actions.ts
@onboarding/backend/runtime-context.cjs
@supabase/migrations/81_markos_orgs.sql
@supabase/migrations/107_b2b_revenue_team.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ABM SOR migration + DB-trigger compliance pack + RLS + preflight</name>
  <files>supabase/migrations/109_b2b_abm.sql, scripts/preconditions/219-03-check-upstream.cjs, test/b2b-219/abm/package-crud.test.js, test/b2b-219/abm/enrichment-audit-trigger.test.js, test/b2b-219/abm/external-outreach-trigger.test.js, test/b2b-219/abm/module-eligibility-b2b-only.test.js, test/b2b-219/rls/abm-cross-tenant-denial.test.js</files>
  <behavior>
    - Test 1 (package-crud): abm_account_packages has 20 columns; abm_tier CHECK IN (1,2,3); stage CHECK 7 values DEFAULT 'identified'; enrichment_source CHECK 4 values; enrichment_confidence numeric(4,2) CHECK 0-1; UNIQUE(tenant_id, account_id); RLS enabled
    - Test 2 (package-crud): abm_buying_committee_members has 12 columns; persona CHECK 5 values; FK to abm_account_packages.package_id ON DELETE CASCADE
    - Test 3 (package-crud): abm_engagement_events has 7 columns; event_type CHECK 10 values; member_id nullable FK
    - Test 4 (enrichment-audit-trigger): UPDATE abm_account_packages SET enriched_at=now(), enrichment_source='research_engine' (changed from prior value) → expect EXCEPTION 'ABM_ENRICHMENT_AUDIT_REQUIRED' (audit-row precondition not met for non-manual/operator sources); with enrichment_source='manual' → succeeds; with enrichment_source='operator' → succeeds
    - Test 5 (external-outreach-trigger): UPDATE abm_account_packages SET stage='meeting_booked' WHERE approved_at IS NULL → EXCEPTION 'ABM_EXTERNAL_OUTREACH_REQUIRES_APPROVAL'; with approved_at + approved_by set → succeeds; same for stage='opportunity'
    - Test 6 (cross-tenant RLS): tenant A INSERT, tenant B SELECT returns 0 rows for all 3 ABM tables
    - Test 7 (preflight): scripts/preconditions/219-03-check-upstream.cjs exits 0 when 219-01 module tree present + P214/P215/P217/P218 tables exist
    - Test 8 (module-eligibility ABM b2b-only): seed tenant with growth_mode='b2c' in saas_growth_profiles; INSERT abm_account_packages → expect EXCEPTION 'MODULE_REQUIRES_ELIGIBLE_GROWTH_MODE' (abm_engine NOT eligible for b2c); same for plg_b2c, b2b2c modes
    - Test 9 (module-eligibility ABM b2b-only): seed tenant with growth_mode='b2b' OR 'plg_b2b'; INSERT abm_account_packages → succeeds; same for abm_buying_committee_members + abm_engagement_events
  </behavior>
  <read_first>
    - 219-RESEARCH.md §6 Domain 3 (read SQL block lines 551-623 carefully; 20+12+7 column tables)
    - 219-RESEARCH.md §Compliance Enforcement Boundary Summary (Plan 03 row — ABM_ENRICHMENT_AUDIT_REQUIRED + ABM_EXTERNAL_OUTREACH_REQUIRES_APPROVAL exact exception names)
    - 219-RESEARCH.md §Cross-Phase Coordination Q-1 (P219 owns abm_buying_committee_members; comment block must appear in migration SQL)
    - .planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md (confirm P219-03 owns slot 109 post Q-7 reallocation)
    - lib/markos/b2b/preflight/index.cjs (Plan 01 output)
    - .planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-02-PLAN.md (REPLICATE migration scaffold pattern; swap to ABM)
  </read_first>
  <action>
    Step 1: Write tests 1-7 BEFORE migration (TDD RED).
    Step 2: Write supabase/migrations/109_b2b_abm.sql:
      - Comment header: `-- Plan 219-03 / Phase 219 / Slot 109 / RESEARCH §6 Domain 3 / Q-7 slot reallocation`
      - Comment block: `-- P219-owned: abm_buying_committee_members. P222 will ship buying_committee_members (CRM-side). Distinct tables, distinct FK parents. No collision. (Q-1 + Q-5 resolution)`
      - CREATE TABLE abm_account_packages with 20 cols per RESEARCH:
        package_id uuid PK DEFAULT gen_random_uuid(),
        tenant_id uuid NOT NULL REFERENCES markos_orgs(org_id),
        account_id uuid NOT NULL,
        company_domain text NOT NULL,
        abm_tier int NOT NULL CHECK (abm_tier IN (1, 2, 3)),
        stage text NOT NULL DEFAULT 'identified' CHECK (stage IN ('identified', 'aware', 'engaged', 'meeting_booked', 'opportunity', 'customer', 'expansion_target')),
        company_profile_jsonb jsonb NOT NULL DEFAULT '{}'::jsonb,
        strategic_signals_jsonb jsonb NOT NULL DEFAULT '{}'::jsonb,
        messaging_jsonb jsonb NOT NULL DEFAULT '{}'::jsonb,
        engagement_jsonb jsonb NOT NULL DEFAULT '{}'::jsonb,
        evidence_refs uuid[] DEFAULT ARRAY[]::uuid[],
        enrichment_source text CHECK (enrichment_source IN ('manual','research_engine','intent_data_import','operator')),
        enrichment_confidence numeric(4,2) DEFAULT 0 CHECK (enrichment_confidence BETWEEN 0 AND 1),
        enriched_at timestamptz,
        inferred_fields text[] DEFAULT ARRAY[]::text[],
        approved_at timestamptz,
        approved_by uuid,
        created_at timestamptz NOT NULL DEFAULT now(),
        updated_at timestamptz NOT NULL DEFAULT now(),
        UNIQUE(tenant_id, account_id)
      - CREATE TABLE abm_buying_committee_members with 12 cols + FK to abm_account_packages.package_id + CHECK persona IN ('economic_buyer','champion','user','gatekeeper','influencer')
      - CREATE TABLE abm_engagement_events with 7 cols + CHECK event_type IN ('website_visit','content_consumed','email_open','email_reply','ad_impression','event_attended','demo_booked','demo_completed','proposal_sent','objection_raised') + FK to package + nullable FK to member
      - ENABLE ROW LEVEL SECURITY on all 3
      - CREATE POLICY tenant_isolation per table
      - CREATE INDEX idx_abm_tier_stage ON abm_account_packages(tenant_id, abm_tier, stage)
      - CREATE INDEX idx_abm_engagement_package ON abm_engagement_events(tenant_id, package_id, created_at)
      - CREATE OR REPLACE FUNCTION fn_abm_enrichment_audit_required() RETURNS trigger AS BEGIN IF NEW.enriched_at IS NOT NULL AND NEW.enriched_at IS DISTINCT FROM OLD.enriched_at AND NEW.enrichment_source NOT IN ('manual','operator') THEN PERFORM 1 FROM markos_audit_log WHERE entity_type='abm_account_packages' AND entity_id=NEW.package_id AND action='enrichment' AND created_at >= now() - interval '5 minutes'; IF NOT FOUND THEN RAISE EXCEPTION 'ABM_ENRICHMENT_AUDIT_REQUIRED'; END IF; END IF; RETURN NEW; END LANGUAGE plpgsql
      - CREATE TRIGGER trg_abm_enrichment_audit_required BEFORE UPDATE ON abm_account_packages
      - CREATE OR REPLACE FUNCTION fn_abm_external_outreach_requires_approval() — IF NEW.stage IN ('meeting_booked','opportunity') AND NEW.approved_at IS NULL THEN RAISE EXCEPTION 'ABM_EXTERNAL_OUTREACH_REQUIRES_APPROVAL'
      - CREATE TRIGGER trg_abm_external_outreach_requires_approval BEFORE INSERT OR UPDATE ON abm_account_packages
      - P218 contract consumption — module_mode_eligibility BEFORE INSERT/UPDATE triggers (one per SOR table; abm module RESTRICTED to b2b + plg_b2b modes ONLY per RESEARCH cross-phase coordination):
        ```sql
        CREATE TRIGGER fn_abm_account_packages_module_eligibility
          BEFORE INSERT OR UPDATE ON abm_account_packages
          FOR EACH ROW EXECUTE FUNCTION fn_check_module_mode_eligibility('abm_engine', NEW.tenant_id);
        CREATE TRIGGER fn_abm_buying_committee_members_module_eligibility
          BEFORE INSERT OR UPDATE ON abm_buying_committee_members
          FOR EACH ROW EXECUTE FUNCTION fn_check_module_mode_eligibility('abm_engine', NEW.tenant_id);
        CREATE TRIGGER fn_abm_engagement_events_module_eligibility
          BEFORE INSERT OR UPDATE ON abm_engagement_events
          FOR EACH ROW EXECUTE FUNCTION fn_check_module_mode_eligibility('abm_engine', NEW.tenant_id);
        -- abm_engine module ONLY eligible for modes b2b + plg_b2b (per module_mode_eligibility seed)
        -- For ineligible mode (b2c, plg_b2c, b2b2c) → fn_check_module_mode_eligibility raises EXCEPTION 'MODULE_REQUIRES_ELIGIBLE_GROWTH_MODE'
        ```
      - DO BEGIN ASSERT (3 tables exist) ASSERT (fn_check_module_mode_eligibility exists) END
    Step 3: Write scripts/preconditions/219-03-check-upstream.cjs replicating 219-01/02 pattern; ALSO checks 219-01 helper module + revenue_team_configs table exists; SOFT-checks evidence_map_records (P209) for EVD-01..06 graceful degrade.
    Step 4: Apply migration; run all 7 tests (TDD GREEN).
    Step 5: Commit migration: `db(219-03): ABM SOR + enrichment audit + external outreach + module-eligibility (b2b/plg_b2b only) triggers / slot 109`.
  </action>
  <acceptance_criteria>
    - `npm test -- test/b2b-219/abm/package-crud.test.js` passes (3 column-presence + RLS-enabled assertions green)
    - `npm test -- test/b2b-219/abm/enrichment-audit-trigger.test.js` passes (research_engine without audit row → exception; manual succeeds; operator succeeds)
    - `npm test -- test/b2b-219/abm/external-outreach-trigger.test.js` passes (meeting_booked/opportunity without approved_at → exception; with approved_at → succeeds)
    - `npm test -- test/b2b-219/rls/abm-cross-tenant-denial.test.js` passes (3 tables × cross-tenant SELECT returns 0 rows)
    - `psql -c "SELECT count(*) FROM information_schema.tables WHERE table_name IN ('abm_account_packages','abm_buying_committee_members','abm_engagement_events')"` returns 3
    - `psql -c "SELECT count(*) FROM pg_trigger WHERE tgname IN ('trg_abm_enrichment_audit_required','trg_abm_external_outreach_requires_approval')"` returns 2
    - `psql -c "SELECT count(*) FROM pg_trigger WHERE tgname LIKE 'fn_abm_%_module_eligibility'"` returns 3 (P218 module_mode_eligibility triggers per ABM SOR table; restricted to b2b/plg_b2b)
    - `npm test -- test/b2b-219/abm/module-eligibility-b2b-only.test.js` passes (b2c/plg_b2c/b2b2c → EXCEPTION; b2b/plg_b2b → succeeds)
    - `grep -E "ABM_ENRICHMENT_AUDIT_REQUIRED|ABM_EXTERNAL_OUTREACH_REQUIRES_APPROVAL" supabase/migrations/109_b2b_abm.sql | wc -l` returns at least 2
    - `grep -E "P219-owned: abm_buying_committee_members" supabase/migrations/109_b2b_abm.sql | wc -l` returns 1 (Q-1 coordination comment)
    - `grep -E "fn_check_module_mode_eligibility.*'abm_engine'" supabase/migrations/109_b2b_abm.sql | wc -l` returns at least 3 (one per ABM SOR table)
    - `node scripts/preconditions/219-03-check-upstream.cjs` exits 0 when env green
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ABM lib (packages + buying-committee + engagement + enrichment) + 2 contracts + flow-registry sync + EVD validation tests</name>
  <files>lib/markos/abm/contracts.ts, lib/markos/abm/contracts.cjs, lib/markos/abm/packages.ts, lib/markos/abm/packages.cjs, lib/markos/abm/buying-committee.ts, lib/markos/abm/engagement.ts, lib/markos/abm/enrichment.ts, lib/markos/abm/index.cjs, contracts/F-232-b2b-abm-account-package-v1.yaml, contracts/F-233-b2b-abm-buying-committee-v1.yaml, contracts/flow-registry.json, test/b2b-219/abm/committee-inferred-flag.test.js, test/b2b-219/abm/stage-transition-approval.test.js, test/b2b-219/abm/evidence-refs-validation.test.js, test/b2b-219/abm/enrichment-staleness.test.js, test/b2b-219/openapi/parity-abm.test.js</files>
  <behavior>
    - Test 1 (committee-inferred-flag): addCommitteeMember({ known_to_us: false }) → row has is_inferred=true (EVD-03 auto-label)
    - Test 2 (committee-inferred-flag): addCommitteeMember({ known_to_us: true }) → row has is_inferred=false
    - Test 3 (committee-inferred-flag): markKnown({ member_id, last_interaction_at }) flips known_to_us=true AND is_inferred=false; getInferredMembers returns only is_inferred=true rows
    - Test 4 (stage-transition-approval): transitionStage to 'meeting_booked' WITHOUT approved_at → calls requestExternalOutreachApproval → buildApprovalPackage with kind='abm_external_outreach'; UPDATE row stage + approved_at; payload includes is_inferred members + evidence_refs (EVD-05)
    - Test 5 (stage-transition-approval): transitionStage to 'identified'/'aware'/'engaged' (non-external stages) → no approval required, direct UPDATE
    - Test 6 (evidence-refs-validation): transitionStage to 'meeting_booked' WITH evidence_refs=[] → throws EvidenceRequiredError BEFORE buildApprovalPackage
    - Test 7 (enrichment-staleness): seed package with enriched_at = now() - interval '95 days' → listStalePackages returns it; isStale returns true
    - Test 8 (enrichment-staleness): seed package with enriched_at = now() - interval '60 days' → not in stale list (default 90d threshold)
    - Test 9 (enrichment audit): rescoreConfidence({ enrichment_source: 'research_engine', ... }) writes audit row to lib/markos/audit BEFORE the UPDATE; DB-trigger satisfied
    - Test 10 (openapi-parity): F-232 + F-233 YAML schemas match lib/markos/abm/contracts.ts zod
  </behavior>
  <read_first>
    - 219-RESEARCH.md §6 Domain 3 full domain (lines 543-646)
    - 219-RESEARCH.md §Manual-Only Verifications row "ABM enrichment source validation"
    - obsidian/work/incoming/17-SAAS-MARKETING-OS-STRATEGY.md lines 496-569 (ABMAccountPackage + BuyingCommitteeMember interfaces)
    - lib/markos/crm/agent-actions.ts:68
    - lib/markos/audit/* (find existing audit emit pattern)
  </read_first>
  <action>
    Step 1: Write tests 1-10 BEFORE implementations (TDD RED).
    Step 2: Implement lib/markos/abm/contracts.ts:
      - Export 5 enum constants:
        ABM_STAGES = ['identified','aware','engaged','meeting_booked','opportunity','customer','expansion_target'] as const;
        ABM_TIERS = [1, 2, 3] as const;
        ABM_ENRICHMENT_SOURCES = ['manual','research_engine','intent_data_import','operator'] as const;
        ABM_PERSONAS = ['economic_buyer','champion','user','gatekeeper','influencer'] as const;
        ABM_EVENT_TYPES = ['website_visit','content_consumed','email_open','email_reply','ad_impression','event_attended','demo_booked','demo_completed','proposal_sent','objection_raised'] as const
      - Export zod schemas with strategic_signals_jsonb fields including source_quality (numeric 0-1), extraction_method (text), extracted_at (timestamptz iso), compliance_posture (text) per EVD-06
      - CommonJS twin contracts.cjs
    Step 3: Implement lib/markos/abm/packages.ts:
      - createPackage / getPackage / listPackages / updatePackage CRUD
      - transitionStage({ tenant_id, package_id, to_stage, ...fields }) — for to_stage in (meeting_booked, opportunity) delegates to requestExternalOutreachApproval
      - requestExternalOutreachApproval({ tenant_id, package_id, initiated_by }) — fetch package; assert evidence_refs.length > 0 OR throw EvidenceRequiredError; fetch inferred committee members for EVD-05 payload; call buildApprovalPackage with kind='abm_external_outreach' + payload {package_id, evidence_refs, inferred_members, current_stage, target_stage}; UPDATE row stage + approved_at + approved_by
    Step 4: Implement lib/markos/abm/buying-committee.ts:
      - addCommitteeMember({ tenant_id, package_id, role_title, persona, known_to_us, ... }) — auto-set is_inferred = !known_to_us (EVD-03)
      - listCommitteeMembers
      - markKnown({ member_id, last_interaction_at }) — UPDATE known_to_us=true AND is_inferred=false AND last_interaction_at
      - getInferredMembers({ tenant_id, package_id }) returns is_inferred=true rows for operator review (EVD-05)
    Step 5: Implement lib/markos/abm/engagement.ts:
      - ingestEngagementEvent INSERT row
      - listPackageEngagement returns ordered timeline DESC by created_at
      - computeEngagementScore aggregates events for stage-transition heuristic
    Step 6: Implement lib/markos/abm/enrichment.ts:
      - rescoreConfidence({ tenant_id, package_id, enrichment_source, enrichment_confidence, inferred_fields, evidence_refs }) — write audit row to markos_audit_log BEFORE UPDATE (DB-trigger requires for non-manual sources); UPDATE abm_account_packages
      - listStalePackages({ tenant_id, max_age_days = 90 }) returns packages WHERE enriched_at < now() - interval '$1 days' (EVD-04)
      - isStale({ enriched_at, max_age_days = 90 }) pure helper
    Step 7: CommonJS twins for packages.cjs + index.cjs (re-export full surface).
    Step 8: Write 2 contract YAMLs F-232/F-233 per RESEARCH §6 Domain 3 API surface table.
    Step 9: Update contracts/flow-registry.json appending F-232/F-233 entries.
    Step 10: Run all ABM tests + openapi-parity test (TDD GREEN). Run `npm run openapi:build`.
  </action>
  <acceptance_criteria>
    - `npm test -- test/b2b-219/abm/committee-inferred-flag.test.js` passes (3 tests green; auto-label + markKnown + getInferredMembers)
    - `npm test -- test/b2b-219/abm/stage-transition-approval.test.js` passes (3 tests green; external-stage approval + non-external direct + EVD-05 payload)
    - `npm test -- test/b2b-219/abm/evidence-refs-validation.test.js` passes (EvidenceRequiredError before buildApprovalPackage)
    - `npm test -- test/b2b-219/abm/enrichment-staleness.test.js` passes (90d threshold + isStale)
    - `npm test -- test/b2b-219/openapi/parity-abm.test.js` passes (2 contracts × zod parity)
    - `grep "F-232\|F-233" contracts/flow-registry.json | wc -l` returns 2
    - `grep -rn "createApprovalPackage\|requireSupabaseAuth\|lookupPlugin\|requireTenantContext\|app/(b2b)\|app/(growth)\|route.ts\|vitest" lib/markos/abm/ | wc -l` returns 0
    - `npm run openapi:build` regenerates with /v1/b2b/abm/* paths
    - `node -e "console.log(typeof require('./lib/markos/abm/index.cjs').requestExternalOutreachApproval)"` outputs `function`
    - `node -e "console.log(typeof require('./lib/markos/abm/index.cjs').rescoreConfidence)"` outputs `function`
    - `node -e "console.log(typeof require('./lib/markos/abm/index.cjs').listStalePackages)"` outputs `function`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser → /v1/b2b/abm/packages | Untrusted; stage-transition mutations approval-gated |
| External enrichment (research engine, intent data) → abm_account_packages | DB-trigger requires audit row written BEFORE UPDATE for non-manual sources |
| App-layer transitionStage → buildApprovalPackage | Tenant-isolated; approval_id required for meeting_booked / opportunity |
| Service-role direct UPDATE on abm_account_packages | DB-trigger blocks stage transitions without approved_at; blocks enrichment without audit row |
| Inferred buying-committee fields → external outreach | App-layer flags is_inferred; EVD-05 surfaces in approval payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-219-03-01 | Tampering | abm_account_packages service-role UPDATE bypasses approval | mitigate | DB-trigger fn_abm_external_outreach_requires_approval; tested |
| T-219-03-02 | Tampering | Automated enrichment writes without audit trail | mitigate | DB-trigger fn_abm_enrichment_audit_required; checks markos_audit_log within 5 min window |
| T-219-03-03 | Information Disclosure | Cross-tenant ABM package leak | mitigate | RLS policy on tenant_id; tested |
| T-219-03-04 | Repudiation | Operator denies approving ABM external outreach | mitigate | buildApprovalPackage writes approval row + audit log SHA-256 hash chain; payload includes inferred_members |
| T-219-03-05 | Spoofing | Bot creates spammy ABM packages via unauthenticated API | mitigate | requireHostedSupabaseAuth on POST endpoints |
| T-219-03-06 | Tampering | Inferred committee member unmarked manually without verification | mitigate | markKnown requires last_interaction_at; getInferredMembers operator review queue |
| T-219-03-07 | Information Disclosure | Buying committee LinkedIn URLs leak via metrics | accept | RLS isolates per-tenant; LinkedIn URLs are public anyway |
| T-219-03-08 | Elevation of Privilege | Operator advances stage without evidence_refs | mitigate | App-layer EvidenceRequiredError before DB-trigger; defense-in-depth |
| T-219-03-09 | Tampering | Stale enriched_at > 90d ignored, leads to wrong outreach | mitigate | listStalePackages cron creates re-enrichment task; EVD-04 reuse policy |
| T-219-03-10 | Information Disclosure | Pricing context in strategic_signals_jsonb leaks compliance_posture | accept | RLS isolates per-tenant; EVD-06 fields are tenant-internal |
</threat_model>

<verification>
**Plan-level verification (run after Task 2 closes):**

- `npm test -- test/b2b-219/abm/ test/b2b-219/rls/abm-* test/b2b-219/openapi/parity-abm.test.js` → all green
- `node scripts/preconditions/219-03-check-upstream.cjs` exits 0 with green env
- `psql -c "SELECT count(*) FROM information_schema.tables WHERE table_name IN ('abm_account_packages','abm_buying_committee_members','abm_engagement_events')"` returns 3
- `psql -c "SELECT count(*) FROM pg_trigger WHERE tgname IN ('trg_abm_enrichment_audit_required','trg_abm_external_outreach_requires_approval')"` returns 2
- `grep -E "F-23[12]" contracts/flow-registry.json | wc -l` returns 2
- `grep -rn "createApprovalPackage\|requireSupabaseAuth\|requireTenantContext\|serviceRoleClient\|lookupPlugin\|public/openapi.json\|app/(b2b)\|app/(growth)\|api/v1/.*/route.ts\|vitest\|.test.ts" lib/markos/abm/ scripts/preconditions/219-03-check-upstream.cjs | wc -l` returns 0
</verification>

<success_criteria>
- Both tasks complete with passing tests; total ~17+ assertions green
- 1 SOR migration + 7 lib files + 2 contracts ship with NO architecture-lock violations
- Domain 3 (ABM + BuyingCommittee) compliance enforcement boundary LOCKED at DB-trigger layer (defense-in-depth)
- EVD-01..06 wiring shipped: evidence_refs guard before external outreach; is_inferred labeling; enrichment-staleness detection; source_quality/extraction_method fields in zod schema
- Q-1 coordination comment in migration 109 (P219 vs P222 distinct tables)
</success_criteria>

<output>
After completion, create `.planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-03-SUMMARY.md` documenting:

- Migration slot claimed: 109 (Q-7 reallocation from 87)
- F-IDs claimed: F-232, F-233
- Tables created: abm_account_packages, abm_buying_committee_members, abm_engagement_events
- Triggers created: trg_abm_enrichment_audit_required, trg_abm_external_outreach_requires_approval
- 7 stage ENUM values + 5 persona ENUM values + 4 enrichment_source ENUM values + 10 event_type ENUM values shipped
- Q-1 (P219 vs P222 buying committee): RESOLVED — coordination comment in migration SQL
</output>
</content>
</invoke>