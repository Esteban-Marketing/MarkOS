---
phase: 85-ultimate-literacy-vault-foundation
plan: 02
type: execute
wave: 2
depends_on: [85-01]
files_modified:
  - onboarding/backend/vault/audit-log.cjs
  - onboarding/backend/vault/idempotency-key.cjs
  - onboarding/backend/vault/ingest-apply.cjs
  - onboarding/backend/vault/conflict-resolution.cjs
  - onboarding/backend/vault/ingest-router.cjs
  - test/phase-85/idempotency-lww.test.js
  - test/phase-85/audit-lineage.test.js
autonomous: true
requirements: [LITV-02, LITV-03]
must_haves:
  truths:
    - "Every accepted Obsidian sync event writes immutable lineage evidence before re-index dispatch (per D-07)."
    - "Repeated identical edits are idempotent and do not duplicate persisted revisions or audit mutations (LITV-03)."
    - "Conflict resolution follows deterministic last-write-wins while preserving superseded revision lineage (LITV-03, per D-05)."
  artifacts:
    - path: onboarding/backend/vault/audit-log.cjs
      provides: Append-only ingest lineage events with actor/source/timestamp fields.
    - path: onboarding/backend/vault/ingest-apply.cjs
      provides: Deterministic ingest write path with idempotency-aware upsert behavior.
    - path: onboarding/backend/vault/conflict-resolution.cjs
      provides: LWW conflict evaluator and supersede linkage contract.
    - path: test/phase-85/idempotency-lww.test.js
      provides: Regression coverage for duplicate events and conflict precedence.
  key_links:
    - from: onboarding/backend/vault/ingest-router.cjs
      to: onboarding/backend/vault/audit-log.cjs
      via: audit-first append prior to PageIndex enqueue.
    - from: onboarding/backend/vault/ingest-apply.cjs
      to: onboarding/backend/vault/idempotency-key.cjs
      via: deterministic key generation before upsert.
    - from: onboarding/backend/vault/conflict-resolution.cjs
      to: onboarding/backend/vault/audit-log.cjs
      via: superseded revision lineage capture.
---

<objective>
Implement deterministic ingest semantics for Phase 85 Wave 2: audit lineage capture, idempotent ingest behavior, and conflict-safe last-write-wins updates.

Purpose: satisfy LITV-02 and LITV-03 with evidence-first ingest behavior that remains deterministic under duplicate or competing edits.
Output: audit module, idempotency key contract, LWW resolver, and executable conflict/idempotency tests.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/85-ultimate-literacy-vault-foundation/85-CONTEXT.md
@.planning/phases/85-ultimate-literacy-vault-foundation/85-RESEARCH.md
@.planning/phases/85-ultimate-literacy-vault-foundation/85-01-PLAN.md
@onboarding/backend/vault/provenance-contract.cjs
@onboarding/backend/vault/vault-writer.cjs
@bin/ingest-literacy.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add idempotency and conflict-resolution test contracts</name>
  <files>test/phase-85/idempotency-lww.test.js, test/phase-85/audit-lineage.test.js</files>
  <behavior>
    - Test 1: replaying the same content hash/key does not create duplicate active revisions.
    - Test 2: newer observed write supersedes older write while preserving both in lineage.
    - Test 3: equivalent duplicate events do not create repeated audit rows for the same idempotency key.
    - Test 4: out-of-order event arrival still resolves deterministically by LWW policy.
  </behavior>
  <action>Create failing tests first for conflict resolution and idempotency per LITV-03, including duplicate and out-of-order event scenarios from research pitfall coverage.</action>
  <verify>
    <automated>node --test test/phase-85/idempotency-lww.test.js test/phase-85/audit-lineage.test.js</automated>
  </verify>
  <done>Test contracts fully define deterministic idempotent behavior and LWW conflict outcomes before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic idempotent ingest apply path</name>
  <files>onboarding/backend/vault/idempotency-key.cjs, onboarding/backend/vault/ingest-apply.cjs, onboarding/backend/vault/conflict-resolution.cjs, onboarding/backend/vault/ingest-router.cjs</files>
  <behavior>
    - Test 1: idempotency key includes tenant, doc identity, and content hash.
    - Test 2: duplicate events short-circuit to no-op apply path.
    - Test 3: conflict evaluator deterministically chooses winning revision by monotonic write ordering.
  </behavior>
  <action>Implement deterministic idempotency key generation and LWW conflict resolver in the ingest apply path. Ensure this lane remains ingestion-only and does not introduce retrieval mode or role-view behavior from Phases 86/87.</action>
  <verify>
    <automated>node --test test/phase-85/idempotency-lww.test.js</automated>
  </verify>
  <done>Ingest apply path is replay-safe and conflict deterministic with explicit supersede semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add immutable audit lineage capture and audit-first ordering</name>
  <files>onboarding/backend/vault/audit-log.cjs, onboarding/backend/vault/ingest-router.cjs, test/phase-85/audit-lineage.test.js</files>
  <behavior>
    - Test 1: accepted ingest events append immutable lineage records.
    - Test 2: audit append occurs before any re-index enqueue call.
    - Test 3: lineage records include doc identity, actor/source, and supersedes link when applicable.
  </behavior>
  <action>Implement append-only audit log contract and wire audit-first ordering per D-07 and LITV-02. Preserve failure semantics so re-index dispatch is skipped when audit append fails.</action>
  <verify>
    <automated>node --test test/phase-85/audit-lineage.test.js</automated>
  </verify>
  <done>Audit lineage is durable, ordered before indexing, and linked to deterministic ingest revisions.</done>
</task>

</tasks>

<verification>
- Run Wave 2 gate: `node --test test/phase-85/idempotency-lww.test.js test/phase-85/audit-lineage.test.js -x`
- Confirm conflict resolution, idempotency, and audit-first behavior are all validated by automated tests.
</verification>

<success_criteria>
- LITV-03 satisfied with deterministic idempotent ingest and LWW conflict handling.
- LITV-02 ingest lineage baseline satisfied with immutable audit-first event recording.
- Wave 2 outputs are ready for resilient re-index orchestration in 85-03.
</success_criteria>

<output>
After completion, create `.planning/phases/85-ultimate-literacy-vault-foundation/85-02-SUMMARY.md`
</output>
