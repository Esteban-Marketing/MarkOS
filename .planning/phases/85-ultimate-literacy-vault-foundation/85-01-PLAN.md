---
phase: 85-ultimate-literacy-vault-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - onboarding/backend/vault/sync-event-contract.cjs
  - onboarding/backend/vault/sync-orchestrator.cjs
  - onboarding/backend/vault/ingest-router.cjs
  - onboarding/backend/vault/audience-schema.cjs
  - bin/sync-vault.cjs
  - test/phase-85/sync-event-contract.test.js
  - test/phase-85/metadata-validation-gates.test.js
autonomous: true
requirements: [LITV-01, LITV-04]
must_haves:
  truths:
    - "Obsidian file edits produce a deterministic sync event contract with no manual publish step (per D-05)."
    - "Sync events enter a bounded ingest queue baseline instead of direct ad-hoc writes (per D-05)."
    - "Artifacts missing required audience metadata are rejected before persistence or indexing (per D-03)."
  artifacts:
    - path: onboarding/backend/vault/sync-event-contract.cjs
      provides: Canonical event schema and validation for add/change/unlink sync events.
    - path: onboarding/backend/vault/sync-orchestrator.cjs
      provides: Watcher-to-queue baseline orchestration with event normalization.
    - path: onboarding/backend/vault/audience-schema.cjs
      provides: Strict metadata and audience tag validation gate.
    - path: test/phase-85/sync-event-contract.test.js
      provides: Contract coverage for event normalization and no-manual-publish behavior.
  key_links:
    - from: onboarding/backend/vault/sync-orchestrator.cjs
      to: onboarding/backend/vault/sync-event-contract.cjs
      via: normalize-and-validate before enqueue.
    - from: onboarding/backend/vault/ingest-router.cjs
      to: onboarding/backend/vault/audience-schema.cjs
      via: metadata gate before any ingest action.
    - from: bin/sync-vault.cjs
      to: onboarding/backend/vault/sync-orchestrator.cjs
      via: executable watcher entrypoint for local and CI simulation.
---

<objective>
Create Phase 85 Wave 1 foundations: deterministic Obsidian sync event contract, queue baseline, and metadata validation gates.

Purpose: satisfy LITV-01 and LITV-04 with enforceable no-manual-publish and pre-ingest metadata constraints before deeper ingest/audit work.
Output: sync contract module, orchestrator baseline, validation gate, and executable test contracts.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/85-ultimate-literacy-vault-foundation/85-CONTEXT.md
@.planning/phases/85-ultimate-literacy-vault-foundation/85-RESEARCH.md
@.planning/phases/84-vault-foundation-obsidian-mind-pageindex-contracts/84-03-PLAN.md
@onboarding/backend/vault/provenance-contract.cjs
@onboarding/backend/vault/vault-writer.cjs
@onboarding/backend/vault/import-engine.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Wave 0 sync and metadata test contracts</name>
  <files>test/phase-85/sync-event-contract.test.js, test/phase-85/metadata-validation-gates.test.js</files>
  <behavior>
    - Test 1: add/change/unlink events normalize to one canonical payload shape.
    - Test 2: duplicate editor atomic-write bursts collapse into one ingestable queue event.
    - Test 3: no manual publish command is required for an edit to become ingest-eligible.
    - Test 4: missing/invalid audience metadata fails before persistence/index paths are called.
  </behavior>
  <action>Author failing Node tests first for sync-event normalization and metadata gate behavior per D-05 and D-03. Include explicit no-manual-publish assertions and malformed frontmatter rejection cases.</action>
  <verify>
    <automated>node --test test/phase-85/sync-event-contract.test.js test/phase-85/metadata-validation-gates.test.js</automated>
  </verify>
  <done>Wave 0 tests exist, fail for the right reasons before implementation, and capture no-manual-publish and metadata gate contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement sync event contract and queue baseline orchestrator</name>
  <files>onboarding/backend/vault/sync-event-contract.cjs, onboarding/backend/vault/sync-orchestrator.cjs, onboarding/backend/vault/ingest-router.cjs, bin/sync-vault.cjs</files>
  <behavior>
    - Test 1: normalized event payload always includes tenant scope, doc identity, source path, and event type.
    - Test 2: orchestrator enqueues events through one bounded pipeline API (not direct writer calls).
    - Test 3: Obsidian save events become ingest-ready automatically with no publish trigger.
  </behavior>
  <action>Implement contract-first event normalization and a bounded queue baseline using chokidar-backed watcher semantics from research. Route add/change/unlink through ingest-router and keep this plan limited to baseline queueing (no retry/dead-letter yet; that belongs to 85-03).</action>
  <verify>
    <automated>node --test test/phase-85/sync-event-contract.test.js</automated>
  </verify>
  <done>Watcher events flow into a deterministic queue contract and are consumable without manual operator publish actions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce audience and metadata validation gate before ingest</name>
  <files>onboarding/backend/vault/audience-schema.cjs, onboarding/backend/vault/ingest-router.cjs, test/phase-85/metadata-validation-gates.test.js</files>
  <behavior>
    - Test 1: required metadata fields must exist before ingest dispatch.
    - Test 2: audience tags outside allowed schema fail with explicit error contract.
    - Test 3: rejected events never invoke writer/index clients.
  </behavior>
  <action>Implement strict metadata + audience validation in ingest-router per D-03 and LITV-04. Keep fail-fast behavior ahead of persistence/index calls and emit stable error codes for downstream audit capture in 85-02.</action>
  <verify>
    <automated>node --test test/phase-85/metadata-validation-gates.test.js</automated>
  </verify>
  <done>Invalid metadata is blocked pre-ingest; valid events pass through queue baseline for next-wave processing.</done>
</task>

</tasks>

<verification>
- Run Wave 1 gate: `node --test test/phase-85/sync-event-contract.test.js test/phase-85/metadata-validation-gates.test.js -x`
- Confirm no plan task introduces retrieval role views or Phase 86/87 UI scope.
</verification>

<success_criteria>
- LITV-01 baseline achieved: Obsidian edits automatically become ingest events without manual publish.
- LITV-04 baseline achieved: metadata and audience gates run before persistence/index.
- Wave 1 artifacts are ready for audit and idempotent ingest implementation in 85-02.
</success_criteria>

<output>
After completion, create `.planning/phases/85-ultimate-literacy-vault-foundation/85-01-SUMMARY.md`
</output>
