---
phase: 85-ultimate-literacy-vault-foundation
plan: 03
type: execute
wave: 3
depends_on: [85-02]
files_modified:
  - onboarding/backend/pageindex/reindex-queue.cjs
  - onboarding/backend/pageindex/reindex-dispatch.cjs
  - onboarding/backend/pageindex/reindex-dead-letter.cjs
  - onboarding/backend/vault/ingest-router.cjs
  - scripts/phase-85/reindex-drain.cjs
  - test/phase-85/reindex-orchestration.test.js
  - test/phase-85/retry-deadletter.test.js
autonomous: true
requirements: [LITV-02]
must_haves:
  truths:
    - "Accepted ingest events enqueue PageIndex re-index jobs automatically with no operator intervention (LITV-02)."
    - "Transient PageIndex failures are retried with bounded backoff and do not create retry storms (per D-02)."
    - "Exhausted retries move to a dead-letter lane with recoverable diagnostics and replay contract (per D-07)."
  artifacts:
    - path: onboarding/backend/pageindex/reindex-queue.cjs
      provides: Bounded retry queue with backoff and concurrency control.
    - path: onboarding/backend/pageindex/reindex-dead-letter.cjs
      provides: Dead-letter persistence and replay metadata contract.
    - path: test/phase-85/retry-deadletter.test.js
      provides: Retry cap, backoff progression, and dead-letter routing coverage.
    - path: scripts/phase-85/reindex-drain.cjs
      provides: Deterministic queue drain utility for test and operational verification.
  key_links:
    - from: onboarding/backend/vault/ingest-router.cjs
      to: onboarding/backend/pageindex/reindex-queue.cjs
      via: post-audit enqueue contract from 85-02.
    - from: onboarding/backend/pageindex/reindex-queue.cjs
      to: onboarding/backend/pageindex/pageindex-client.cjs
      via: queue worker dispatch with retry policy.
    - from: onboarding/backend/pageindex/reindex-queue.cjs
      to: onboarding/backend/pageindex/reindex-dead-letter.cjs
      via: terminal failure routing after max attempts.
---

<objective>
Deliver resilient PageIndex re-index orchestration for Phase 85 Wave 3, including retry/backoff policies and dead-letter handling.

Purpose: complete LITV-02 automation by ensuring ingest-triggered re-index operations are robust under partial failures and operationally diagnosable.
Output: bounded re-index queue, retry/backoff orchestration, dead-letter lane, and orchestration tests.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/85-ultimate-literacy-vault-foundation/85-CONTEXT.md
@.planning/phases/85-ultimate-literacy-vault-foundation/85-RESEARCH.md
@.planning/phases/85-ultimate-literacy-vault-foundation/85-02-PLAN.md
@onboarding/backend/pageindex/pageindex-client.cjs
@onboarding/backend/pageindex/retrieval-envelope.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write re-index queue retry/dead-letter test contracts</name>
  <files>test/phase-85/reindex-orchestration.test.js, test/phase-85/retry-deadletter.test.js</files>
  <behavior>
    - Test 1: each accepted ingest event schedules one deterministic re-index job id.
    - Test 2: transient failures retry with bounded exponential backoff.
    - Test 3: duplicate enqueue attempts for same idempotency key are suppressed.
    - Test 4: max-attempt failures route to dead-letter with replay metadata.
  </behavior>
  <action>Create failing tests first for queue behavior, retry/backoff policy, and dead-letter routing. Include deterministic clock or injected scheduler controls so tests are stable in CI.</action>
  <verify>
    <automated>node --test test/phase-85/reindex-orchestration.test.js test/phase-85/retry-deadletter.test.js</automated>
  </verify>
  <done>Wave 0 test contracts fully cover queue scheduling, retry bounds, dedupe, and dead-letter behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bounded re-index queue with retry/backoff policy</name>
  <files>onboarding/backend/pageindex/reindex-queue.cjs, onboarding/backend/pageindex/reindex-dispatch.cjs, onboarding/backend/vault/ingest-router.cjs</files>
  <behavior>
    - Test 1: queue dispatches via PageIndex client with configured concurrency cap.
    - Test 2: retry schedule follows configured backoff curve and stops at max attempts.
    - Test 3: ingest router enqueues only after audit-first success from 85-02.
  </behavior>
  <action>Implement p-queue-backed orchestration with bounded concurrency, capped retries, and jitter/backoff from research guidance. Wire enqueue integration at ingest-router post-audit boundary.</action>
  <verify>
    <automated>node --test test/phase-85/reindex-orchestration.test.js</automated>
  </verify>
  <done>Re-index processing is resilient, bounded, and integrated into ingest flow without manual intervention.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add dead-letter persistence and deterministic replay tooling</name>
  <files>onboarding/backend/pageindex/reindex-dead-letter.cjs, scripts/phase-85/reindex-drain.cjs, test/phase-85/retry-deadletter.test.js</files>
  <behavior>
    - Test 1: exhausted jobs are captured with failure cause and attempt metadata.
    - Test 2: replay tool re-queues dead-letter entries deterministically.
    - Test 3: replay operations are idempotent and do not create duplicate active queue entries.
  </behavior>
  <action>Implement dead-letter capture plus a replay/drain utility for verification and operations. Keep this scope limited to ingestion-index resiliency and do not add retrieval-mode or role-view features.</action>
  <verify>
    <automated>node --test test/phase-85/retry-deadletter.test.js</automated>
  </verify>
  <done>Terminal failures are observable and recoverable through deterministic replay mechanics.</done>
</task>

</tasks>

<verification>
- Run Wave 3 gate: `node --test test/phase-85/reindex-orchestration.test.js test/phase-85/retry-deadletter.test.js -x`
- Confirm retry/backoff limits prevent unbounded re-index retries.
</verification>

<success_criteria>
- LITV-02 is fully automated through resilient re-index orchestration.
- Retry/backoff/dead-letter behavior is covered by executable tests.
- Queue path remains ingestion-only without Phase 86/87 retrieval-view expansion.
</success_criteria>

<output>
After completion, create `.planning/phases/85-ultimate-literacy-vault-foundation/85-03-SUMMARY.md`
</output>
