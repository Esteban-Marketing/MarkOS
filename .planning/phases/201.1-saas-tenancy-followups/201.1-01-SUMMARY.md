---
phase: 201.1
plan: 01
plan_id: 201.1-01
subsystem: audit
tags: [canonical-json, hash-chain, spec-lock, NFC, ECMA-262, binary-lex, postgres-fn, property-fuzzer]
completed_at: 2026-04-29T17:03:01Z
duration_minutes: 35
tasks_completed: 2
tasks_total: 2
files_created: 7
files_modified: 2

dependency_graph:
  requires: []
  provides:
    - markos_canonicalize_audit_payload(jsonb) — standalone Postgres canonicalizer
    - docs/canonical-audit-spec.md — locked spec (D-103)
    - canonical-fuzzer.test.js — 10k iteration DB-gated fuzzer scaffold
  affects:
    - lib/markos/audit/canonical.cjs (hardened)
    - supabase/migrations/82_markos_audit_log_hash_chain.sql::append_markos_audit_row (re-wired in migration 90)

tech_stack:
  added:
    - Buffer.compare(ba, bb) — binary-lex UTF-8 key sort in Node
    - String.prototype.normalize('NFC') — NFC normalization in Node
    - normalize(text, NFC) — Postgres 13+ built-in for NFC in plpgsql
    - convert_to(k, 'UTF8') ORDER BY — Postgres binary-lex key sort
  patterns:
    - TDD RED→GREEN for all spec-lock invariants
    - Dual-export .cjs + .ts for new lib/markos/audit/canonicalizer-rpc

key_files:
  created:
    - lib/markos/audit/canonicalizer-rpc.cjs
    - lib/markos/audit/canonicalizer-rpc.ts
    - supabase/migrations/90_markos_audit_canonicalizer.sql
    - supabase/migrations/rollback/90_markos_audit_canonicalizer_rollback.sql
    - test/audit/canonical-spec.test.js
    - test/audit/canonical-fuzzer.test.js
    - docs/canonical-audit-spec.md
  modified:
    - lib/markos/audit/canonical.cjs
    - lib/markos/audit/canonical.ts

decisions:
  - "D-103: NFC normalize string VALUES only (not keys) — keys are preserved as stored bytes"
  - "D-103: -0 detected via Object.is(value, -0) returning literal '0'"
  - "D-103: binary-lex key sort via Buffer.compare (Node) / convert_to(k,'UTF8') ORDER BY (Postgres) — not locale sort"
  - "SQL param fix: recursive helper parameter is p_value (not p_payload) — grep gate confirms zero p_payload->v_key in code"
  - "Migration 90 re-wires append_markos_audit_row to delegate to markos_canonicalize_audit_payload(coalesce(p_payload,'{}'))"
  - "Fuzzer DB-gated: skips gracefully without MARKOS_TEST_SUPABASE_URL; full activation in Plan 11/D-108"
---

# Phase 201.1 Plan 01: Canonical-JSON Spec Lock + Property Fuzzer Summary

**One-liner:** NFC + ECMA-262 §7.1.13 + binary-lex UTF-8 key sort locked in `canonical.cjs` and mirrored in Postgres `markos_canonicalize_audit_payload(jsonb)` — closes review concern H4.

---

## Tasks Completed

| Task | Name | Commit | Key files |
|------|------|--------|-----------|
| 1 | Lock canonical spec — NFC + ECMA-262 + binary-lex + spec doc | b3c5f1f | canonical.cjs, canonical.ts, canonical-spec.test.js, docs/canonical-audit-spec.md |
| 2 | Migration 90 — Postgres fn + re-wire append_markos_audit_row + RPC wrapper + fuzzer | 4c6ba09 | 90_markos_audit_canonicalizer.sql, rollback/90_rollback.sql, canonicalizer-rpc.cjs, canonical-fuzzer.test.js |

---

## Migration 90 Content Summary

**Forward (`90_markos_audit_canonicalizer.sql`, ~160 LoC SQL):**

1. `markos_canonicalize_audit_payload(p_payload jsonb) returns text` — entry point; delegates to recursive helper.
2. `markos_canonicalize_jsonb_recursive(p_value jsonb) returns text` — handles null / boolean / number / string (NFC) / array / object (binary-lex key sort via `convert_to(k,'UTF8') ORDER BY`). Parameter named `p_value` (not `p_payload`).
3. Re-creates `append_markos_audit_row` to call `markos_canonicalize_audit_payload(coalesce(p_payload,'{}'))` for the payload sub-object. Outer 6-key envelope unchanged.
4. `create extension if not exists pgcrypto` guard.

**Rollback (`rollback/90_markos_audit_canonicalizer_rollback.sql`, ~70 LoC SQL):**

- `drop function if exists markos_canonicalize_audit_payload(jsonb)`
- `drop function if exists markos_canonicalize_jsonb_recursive(jsonb)`
- Restores `append_markos_audit_row` to migration-82 inline body verbatim (coalesce(p_payload::text,'null') pattern).

---

## Spec Doc Invariants (`docs/canonical-audit-spec.md`)

8 sections covering all 4 locked invariants:

| Invariant | Node implementation | Postgres implementation |
|-----------|---------------------|------------------------|
| Numbers | `JSON.stringify(value)` (ECMA-262 §7.1.13 shortest-round-trip); `Object.is(value,-0)` → `'0'` | `p_value::text` (jsonb normalizes per ECMA-262) |
| Unicode | `value.normalize('NFC')` before JSON.stringify | `normalize(text, NFC)` Postgres 13+ built-in |
| String escape | V8 JSON.stringify — lowercase `\uXXXX`, no `/` escape | `to_jsonb(normalized_text)::text` |
| Key ordering | `Buffer.compare(ba, bb)` binary-lex UTF-8 | `ORDER BY convert_to(k,'UTF8')` bytea lex |

---

## Test Counts

| Suite | Tests | Status |
|-------|-------|--------|
| test/audit/canonical-spec.test.js (new) | 6 | All pass |
| test/audit/canonical-fuzzer.test.js (new) | 5 (2 pass, 3 skip — DB-gated) | Pass/skip (no fail) |
| test/audit/canonical.test.js (pre-existing) | 10 | All pass |
| test/audit/chain-checker.test.js (pre-existing) | 6 | All pass |
| test/audit/hash-chain.test.js (pre-existing, 201-CLI test) | 7 pass, 1 pre-existing fail | Pre-existing failure unrelated to this plan (AUDIT_SOURCE_DOMAINS has 'cli' added by Phase 204; test expects 12 entries, writer.cjs has 13) |

**New tests added: 11** (6 spec-lock + 5 fuzzer).
**Pre-existing tests unchanged: 22** (10 canonical + 6 chain-checker + 6 hash-chain pass).

---

## Backward-Compat Assertion Result

Golden canonical string for ASCII-only fixture:
```
{"action":"tenant.created","actor_id":"u-1","actor_role":"owner","occurred_at":"2026-04-17T00:00:00.000Z","payload":{"slug":"acme"},"tenant_id":"tenant-1"}
```

This string is byte-identical before and after the D-103 spec lock because: NFC normalization is a no-op on ASCII; binary-lex equals ASCII order for pure ASCII keys; no `-0` or `NaN` values exist. Pre-existing `markos_audit_log` rows with ASCII-only payloads continue to verify via `verifyTenantChain`. The golden hash test (`D-103 fuzzer: backward-compat golden hash`) asserts deterministic SHA-256 derivation from this string.

---

## H4 Closure Attestation

Review concern H4 ("canonicalization under-specified — single byte divergence breaks tamper detection across the chain") is closed:

- [x] `lib/markos/audit/canonical.cjs` contains `value.normalize('NFC')`, `Buffer.compare(ba, bb)`, `Object.is(value, -0)`
- [x] `docs/canonical-audit-spec.md` contains `ECMA-262 §7.1.13 ToString(Number)`, `NFC`, `binary-lexicographic`, `lowercase \uXXXX`
- [x] `supabase/migrations/90_markos_audit_canonicalizer.sql` mirrors all four rules in plpgsql
- [x] SQL parameter bug fix verified: `grep -E "p_payload[[:space:]]*->[[:space:]]*v_key" supabase/migrations/9*.sql` returns 0 code lines
- [x] Property fuzzer scaffold exists (`test/audit/canonical-fuzzer.test.js`) — activates against staging Supabase in Plan 11/D-108

---

## Deviations from Plan

### Pre-existing test failure (out of scope — not fixed)

`test/audit/hash-chain.test.js` — `Suite 201-02: AUDIT_SOURCE_DOMAINS is the locked list (11 core + 1 MCP)` was already failing before this plan. The test expects 12 source domains; `lib/markos/audit/writer.cjs` has 13 (includes `'cli'` added by Phase 204 Plan 01). This failure predates Plan 201.1-01 and is unrelated to canonicalization. Logged to deferred-items.

### Comment text in migration gate check

The acceptance criterion `grep -E "p_payload -> v_key" supabase/migrations/9*.sql returns 0 lines` initially matched a comment line. Comment was reworded to avoid the false positive. The SQL implementation itself never contained `p_payload -> v_key` in executable code — only in an explanatory comment.

---

## Known Stubs

None. All artifacts are fully wired. The fuzzer DB-gated skip is intentional scaffolding (not a stub) — it activates in Plan 11/D-108 once the staging Supabase environment is provisioned.

---

## Threat Flags

None. No new network endpoints, auth paths, or file access patterns introduced. The `markos_canonicalize_audit_payload` function is `immutable parallel safe` (read-only, no side effects).

---

## Self-Check: PASSED
