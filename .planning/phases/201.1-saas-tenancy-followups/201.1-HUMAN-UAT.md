---
status: partial
phase: 201.1-saas-tenancy-followups
source: [201.1-VERIFICATION.md]
started: 2026-04-29T05:00:00Z
updated: 2026-04-29T05:00:00Z
---

## Current Test

[awaiting human testing — autonomous mode deferred per /gsd:autonomous 201.1 invocation]

## Tests

### 1. Playwright golden-path (M7 + QA-06 reversal)

expected: Run `npx playwright test test-playwright/tenancy-golden-path.spec.ts` against a live staging instance with running app + Supabase. Confirm 27 interactions all pass (≥6 page.fill+page.click steps required, 27 actual). Validates signup → email-token callback → onboarding → TenantSwitcher → danger-confirm flow on real DOM. Static spec verified; live execution pending.
result: [pending]

### 2. Staging smoke harness live run

expected: Execute `node scripts/staging-smokes/run-all.cjs --mode=live` against staging environment with all Phase 200/201 wiring active. Push `v4.0.0-rc.1` tag and confirm `.github/workflows/staging-smokes.yml` CI gate fires + 8 smokes report green. Validates D-108 reclassification (informational → release-gate).
result: [pending]

### 3. GDPR download-once nonce live test (H3 sub-concern 2)

expected: Trigger a real GDPR export against staging tenant. Capture the signed URL from `/api/tenant/gdpr/reissue`. Successfully download the export bytes (200 OK). Re-present the same nonce in a second request. Confirm 410 Gone (nonce already consumed in `markos_gdpr_export_consumed` table) — NOT a 200 with re-served bytes. Tombstone audit row `gdpr_export.consumed` confirmed in `markos_audit_log`.
result: [pending]

## Summary

total: 3
passed: 0
issues: 0
pending: 3
skipped: 0
blocked: 0

## Gaps

(No gaps — automated verification 12/12 PASS. Items above are live-environment dependent and cannot be greppped or unit-tested.)
