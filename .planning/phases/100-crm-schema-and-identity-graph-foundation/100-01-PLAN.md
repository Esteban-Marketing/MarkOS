---
phase: 100-crm-schema-and-identity-graph-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/100_crm_schema_identity_graph_hardening.sql
  - lib/markos/crm/contracts.ts
  - lib/markos/crm/contracts.cjs
  - lib/markos/crm/entities.ts
  - lib/markos/crm/entities.cjs
  - lib/markos/crm/identity.ts
  - lib/markos/crm/merge.cjs
  - lib/markos/crm/api.cjs
  - api/crm/merge.js
  - test/crm-schema/crm-core-entities.test.js
  - test/crm-api/crm-merge-api.test.js
  - test/tenant-auth/crm-tenant-isolation.test.js
autonomous: true
requirements:
  - CRM-01
  - CRM-02
must_haves:
  truths:
    - tenant-scoped contacts, companies, accounts, customers, deals, tasks, notes, and activities remain the canonical CRM source of truth
    - custom fields stay explicit, object-scoped, and validated through shared CRM contracts
    - ambiguous identity matches fail closed into review, while approved merges preserve reversible lineage and audit evidence
  artifacts:
    - path: supabase/migrations/100_crm_schema_identity_graph_hardening.sql
      provides: forward-only schema hardening delta for CRM parity and audit guarantees
    - path: lib/markos/crm/identity.ts
      provides: confidence-aware review-first stitch decisions
    - path: lib/markos/crm/merge.cjs
      provides: immutable merge evidence and lineage preservation
    - path: test/crm-schema/crm-core-entities.test.js
      provides: CRM-01 regression and negative-path guardrail coverage
  key_links:
    - from: lib/markos/crm/contracts.cjs
      to: lib/markos/crm/entities.cjs
      via: shared record-kind and custom-field validators
    - from: lib/markos/crm/identity.ts
      to: supabase/migrations/100_crm_schema_identity_graph_hardening.sql
      via: aligned identity review status and confidence rules
    - from: api/crm/merge.js
      to: lib/markos/crm/merge.cjs
      via: review-first merge endpoint writes immutable lineage
---

<objective>
Harden and consolidate the existing CRM schema and identity graph foundation for Phase 100 without rebuilding the stack.

Purpose: satisfy CRM-01 and CRM-02 by reusing the current CRM contracts, entity helpers, merge lineage, and Supabase migrations while closing parity gaps and preserving tenant-safe audit behavior.
Output: one forward-only schema delta, aligned CRM helper contracts, and a focused negative-path regression slice that proves the foundation is ready for later tracking and workspace phases.
</objective>

<context>
@.planning/phases/100-crm-schema-and-identity-graph-foundation/100-CONTEXT.md
@.planning/phases/100-crm-schema-and-identity-graph-foundation/100-RESEARCH.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@contracts/F-58-crm-entity-crud-v1.yaml
@lib/markos/crm/contracts.cjs
@lib/markos/crm/entities.cjs
@lib/markos/crm/identity.ts
@lib/markos/crm/merge.cjs
@lib/markos/crm/api.cjs
@supabase/migrations/58_crm_core_entities.sql
@supabase/migrations/58_crm_custom_fields.sql
@supabase/migrations/58_crm_activity_and_identity.sql
@test/crm-schema/crm-core-entities.test.js
@test/crm-api/crm-merge-api.test.js
@test/tenant-auth/crm-tenant-isolation.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Step 1: Lock Phase 100 guardrails with focused CRM regression coverage</name>
  <files>test/crm-schema/crm-core-entities.test.js, test/crm-api/crm-merge-api.test.js, test/tenant-auth/crm-tenant-isolation.test.js</files>
  <behavior>
    - add explicit negative-path coverage for tenant mismatch and mutation denial per D-09.
    - add a parity assertion for the identity review state so ambiguous matches land in review instead of silently merging per D-04.
    - prove merge evidence remains queryable and source rows are marked merged rather than deleted per D-05 and D-10.
  </behavior>
  <action>Extend the existing CRM test slice first, keeping scope limited to CRM-01 and CRM-02. Reuse the current test files instead of creating a new harness. Do not add tracking ingestion, workspace UI, outbound, or copilot assertions in this phase.</action>
  <verify>
    <automated>node --test test/crm-schema/crm-core-entities.test.js test/crm-api/crm-merge-api.test.js test/tenant-auth/crm-tenant-isolation.test.js</automated>
  </verify>
  <done>The focused CRM slice encodes the missing Phase 100 guardrails and fails if schema or helper parity drifts again.</done>
</task>

<task type="auto" tdd="true">
  <name>Step 2: Ship a forward-only schema and contract parity hardening delta</name>
  <files>supabase/migrations/100_crm_schema_identity_graph_hardening.sql, lib/markos/crm/contracts.ts, lib/markos/crm/contracts.cjs, lib/markos/crm/entities.ts, lib/markos/crm/entities.cjs</files>
  <behavior>
    - preserve the Phase 58 tables as the baseline and add only additive migration changes.
    - reconcile helper and SQL expectations for identity link states, audit columns, and tenant-safe constraints.
    - keep custom fields explicit and object-scoped through the existing CRM validators per D-02, D-03, and D-07.
  </behavior>
  <action>Create a new Phase 100 migration rather than editing the historical 58 migrations. Make the existing CRM contract surface authoritative for record kinds, stable identifiers, status handling, and review-state parity. If TS and CJS surfaces both remain active, wire them to the same allowed-state rules so they cannot drift.</action>
  <verify>
    <automated>node --test test/crm-schema/crm-core-entities.test.js</automated>
  </verify>
  <done>Schema, contracts, and entity helpers agree on the canonical CRM foundation and still fail closed on missing or mismatched tenant context.</done>
</task>

<task type="auto" tdd="true">
  <name>Step 3: Harden identity review and merge lineage governance surfaces</name>
  <files>lib/markos/crm/identity.ts, lib/markos/crm/merge.cjs, lib/markos/crm/api.cjs, api/crm/merge.js, test/crm-api/crm-merge-api.test.js, test/tenant-auth/crm-tenant-isolation.test.js</files>
  <behavior>
    - ambiguous identity signals remain in review, high-confidence links can be accepted, and low-confidence links are rejected per D-04.
    - merge decisions keep reviewer, source event, rationale, and lineage history immutable per D-05 and D-10.
    - readonly or cross-tenant actors cannot perform merge mutations, preserving the current fail-closed boundary per D-09.
  </behavior>
  <action>Reuse the existing identity scoring, merge decision, lineage, and tenant-context helpers already in the repo. Preserve the merged marker and lineage trail instead of destructive dedupe. Explicitly defer high-volume stitching logic, workspace views, outbound delivery, and AI automation to later phases.</action>
  <verify>
    <automated>node --test test/crm-api/crm-merge-api.test.js test/tenant-auth/crm-tenant-isolation.test.js</automated>
  </verify>
  <done>CRM-02 is satisfied by a review-first, reversible, tenant-safe merge flow that is ready for Phase 101 to build on.</done>
</task>

</tasks>

<verification>
Phase gate commands:
- node --test test/crm-schema/crm-core-entities.test.js
- node --test test/crm-api/crm-merge-api.test.js test/tenant-auth/crm-tenant-isolation.test.js
- node --test test/crm-schema/crm-core-entities.test.js test/crm-api/crm-merge-api.test.js test/tenant-auth/crm-tenant-isolation.test.js

Acceptance checks:
- CRM-01 remains contract-first and tenant-safe across canonical records and custom fields.
- CRM-02 remains review-first, reversible, and audit-complete for identity links and merges.
- No Phase 101-105 scope leaks into this execution lane.
</verification>

<success_criteria>
- A forward-only Phase 100 migration closes the review-state and audit parity gaps without rewriting the existing CRM baseline.
- Shared CRM helpers and contracts remain the source of truth for entity validation and tenant-safe mutations.
- The focused CRM regression slice stays green with explicit negative-path coverage for merge and tenant isolation.
</success_criteria>

<output>
After completion, create .planning/phases/100-crm-schema-and-identity-graph-foundation/100-01-SUMMARY.md.
</output>
