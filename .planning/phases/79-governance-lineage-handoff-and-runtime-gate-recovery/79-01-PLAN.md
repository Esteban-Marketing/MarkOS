---
phase: 79-governance-lineage-handoff-and-runtime-gate-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - onboarding/backend/brand-governance/lineage-handoff.cjs
  - test/phase-79/lineage-handoff-helper.test.js
autonomous: true
requirements:
  - BRAND-GOV-01
must_haves:
  truths:
    - Submit-path lineage payload can be built directly from canonical artifact write metadata without recomputation in handlers.
    - Fingerprint lane names are emitted exactly as strategy, identity, design_system, and starter.
    - Missing fingerprint source values remain explicit nulls instead of silent lane omission.
  artifacts:
    - path: onboarding/backend/brand-governance/lineage-handoff.cjs
      provides: Deterministic helper that maps strategy, identity, design-system, and starter writer outputs into governance canonicalArtifacts including lineage_fingerprints.
    - path: test/phase-79/lineage-handoff-helper.test.js
      provides: Unit contract assertions for lane naming, metadata-first extraction, and null-preserving mapping behavior.
  key_links:
    - from: onboarding/backend/brand-governance/lineage-handoff.cjs
      to: onboarding/backend/handlers.cjs
      via: exported helper consumed by submit governance handoff in later wave.
    - from: test/phase-79/lineage-handoff-helper.test.js
      to: onboarding/backend/brand-governance/lineage-handoff.cjs
      via: deterministic lane-map assertions proving D-01 through D-03 contract.
---

<objective>
Define the metadata-first lineage handoff contract as an explicit helper and lock it with unit tests.

Purpose: Create a single deterministic contract for governance handoff payload assembly so handlers can wire it without recomputing fingerprints (D-01, D-02, D-03).
Output: lineage-handoff helper module and unit tests covering lane mapping and null behavior.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/79-governance-lineage-handoff-and-runtime-gate-recovery/79-CONTEXT.md
@.planning/phases/79-governance-lineage-handoff-and-runtime-gate-recovery/79-RESEARCH.md
@onboarding/backend/handlers.cjs
@onboarding/backend/brand-governance/bundle-registry.cjs
@onboarding/backend/brand-governance/governance-diagnostics.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement lineage handoff helper with fixed lane contract</name>
  <files>onboarding/backend/brand-governance/lineage-handoff.cjs</files>
  <action>Create an exported helper that accepts existing submit write results ({ strategyPersistenceResult, identityArtifactWrite, designSystemArtifactWrite, starterArtifactWrite }) and returns canonicalArtifacts with strategy_artifact_id, identity_artifact_id, design_system_artifact_id, starter_artifact_id, and lineage_fingerprints. Map lanes exactly to strategy, identity, design_system, starter per D-03. Source fingerprint values from writer metadata only (D-01, D-02): strategyPersistenceResult.artifact_fingerprint, identityArtifactWrite.artifact_fingerprint, designSystemArtifactWrite.token_contract_fingerprint, starterArtifactWrite.starter_fingerprint. If a source value is missing, keep the lane key with null rather than omitting it to preserve fail-closed behavior in governance validators (D-06).</action>
  <verify>node --test test/phase-79/lineage-handoff-helper.test.js</verify>
  <done>lineage-handoff helper exports deterministic canonicalArtifacts mapping with fixed lane names and null-preserving behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests proving metadata-first lane mapping contract</name>
  <files>test/phase-79/lineage-handoff-helper.test.js</files>
  <action>Add unit tests for three scenarios: (1) full metadata yields all four artifact IDs and four non-null lanes; (2) one missing fingerprint source keeps lane key present with null; (3) helper never recomputes hash values and only forwards source metadata values. Assert exact key names to prevent semantic drift and include explicit checks for design_system snake_case lane key per D-03.</action>
  <verify>node --test test/phase-79/lineage-handoff-helper.test.js</verify>
  <done>Unit tests fail on lane renaming or omission and pass only when helper follows metadata-first contract per D-01 through D-03.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] node --test test/phase-79/lineage-handoff-helper.test.js
</verification>

<success_criteria>
- Helper module exposes one deterministic metadata-to-canonicalArtifacts mapping contract.
- Lane keys are exactly strategy, identity, design_system, starter.
- Missing source values remain explicit nulls to preserve downstream fail-closed gate semantics.
</success_criteria>

<output>
After completion, create .planning/phases/79-governance-lineage-handoff-and-runtime-gate-recovery/79-01-SUMMARY.md
</output>
