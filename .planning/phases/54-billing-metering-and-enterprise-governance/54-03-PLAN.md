---
phase: 54-billing-metering-and-enterprise-governance
plan: 03
type: execute
wave: 2
depends_on:
  - 54-01
files_modified:
  - package.json
  - app/(markos)/layout.tsx
  - lib/markos/auth/session.ts
  - api/auth/sso/start.js
  - api/auth/sso/callback.js
  - lib/markos/identity/sso-bindings.ts
  - lib/markos/identity/role-mapping.ts
  - lib/markos/rbac/iam-v32.js
  - onboarding/backend/runtime-context.cjs
  - supabase/migrations/54_identity_federation.sql
  - test/auth/sso-role-mapping.test.js
  - test/auth/sso-negative-path.test.js
  - test/tenant-auth/ui-authorization-negative-path.test.js
autonomous: true
requirements:
  - IAM-04
user_setup:
  - service: supabase-enterprise-sso
    why: "Enterprise SAML federation requires provider metadata and attribute mappings from the tenant's IdP."
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase project settings"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase project settings"
    dashboard_config:
      - task: "Create the enterprise SSO provider and attribute mappings for each tenant"
        location: "Supabase Dashboard -> Authentication -> Enterprise SSO"

must_haves:
  truths:
    - "Protected billing and governance routes no longer rely on hard-coded role or tenant placeholders."
    - "Enterprise SSO binds external identity claims to tenant-scoped canonical IAM roles only."
    - "Privilege-escalating or unmapped claims fail closed and produce immutable mapping evidence."
  artifacts:
    - path: "lib/markos/auth/session.ts"
      provides: "Session-backed tenant and role resolution for app routes and server actions."
      exports:
        - "requireMarkosSession"
        - "getActiveTenantContext"
    - path: "lib/markos/identity/role-mapping.ts"
      provides: "Deterministic external-claim-to-canonical-role mapping rules and audit decision helpers."
      exports:
        - "mapExternalClaimsToRole"
        - "recordRoleMappingDecision"
    - path: "supabase/migrations/54_identity_federation.sql"
      provides: "Tenant SSO binding, mapping-rule, and mapping-event schema with tenant-safe access rules."
      exports: []
  key_links:
    - from: "app/(markos)/layout.tsx"
      to: "lib/markos/auth/session.ts"
      via: "session-backed tenant and role resolution"
      pattern: "requireMarkosSession|getActiveTenantContext"
    - from: "api/auth/sso/callback.js"
      to: "lib/markos/identity/role-mapping.ts"
      via: "canonical-role mapping after SSO callback"
      pattern: "mapExternalClaimsToRole|recordRoleMappingDecision"
    - from: "lib/markos/identity/role-mapping.ts"
      to: "lib/markos/rbac/iam-v32.js"
      via: "canonical IAM role enforcement"
      pattern: "IAM_V32_ROLES|canPerformAction"
---

<objective>
Harden auth/session boundaries and deliver the enterprise SSO plus canonical role-mapping foundation.

Purpose: Satisfy IAM-04 early so later billing and governance UIs can run on real tenant and role context, not scaffold placeholders.
Output: Session-backed route auth, SSO start/callback handlers, tenant SSO bindings, canonical role mapping, and negative-path audit coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/54-billing-metering-and-enterprise-governance/54-CONTEXT.md
@.planning/phases/54-billing-metering-and-enterprise-governance/54-RESEARCH.md
@.planning/phases/54-billing-metering-and-enterprise-governance/54-01-PLAN.md
@app/(markos)/layout.tsx
@onboarding/backend/runtime-context.cjs
@lib/markos/rbac/iam-v32.js

<interfaces>
From onboarding/backend/runtime-context.cjs:

```javascript
function createRuntimeContext(env = process.env)
function requireHostedSupabaseAuth({ req, runtimeContext, operation, requiredProjectSlug = null, env = process.env })
function emitDenyTelemetry(denyEvent, dependencies = {})
function buildRunPolicyMetadata(input = {}, env = process.env)
module.exports = { createRuntimeContext, emitDenyTelemetry, requireHostedSupabaseAuth, buildRunPolicyMetadata }
```

From lib/markos/rbac/iam-v32.js:

```javascript
export const IAM_V32_ROLES = Object.freeze([
  'owner', 'tenant-admin', 'manager', 'contributor', 'reviewer', 'billing-admin', 'readonly'
]);

export function canPerformAction(role, action) {}
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 54-03-01: Replace scaffold auth with session-backed tenant and role resolution (per D-10, D-11)</name>
  <read_first>
    - app/(markos)/layout.tsx
    - onboarding/backend/runtime-context.cjs
    - test/tenant-auth/ui-authorization-negative-path.test.js
  </read_first>
  <files>package.json, app/(markos)/layout.tsx, lib/markos/auth/session.ts, test/tenant-auth/ui-authorization-negative-path.test.js</files>
  <acceptance_criteria>
    - Protected app routes resolve tenant and role from session claims rather than hard-coded placeholders.
    - Missing or ambiguous tenant/session state renders the fail-closed denial contract already established in Phase 51.
    - App auth hardening uses `@supabase/ssr` and stays on the Supabase auth stack rather than introducing a second session system.
  </acceptance_criteria>
  <action>
Install and wire `@supabase/ssr`, then replace the current scaffold auth in `app/(markos)/layout.tsx` with session-backed tenant and role resolution. Preserve the fail-closed tenant-denial behavior from Phase 51 and do not infer tenant scope from URL or email. This task exists early because Phase 54 UI work cannot safely proceed on placeholder auth.
  </action>
  <verify>
    <automated>node --test test/tenant-auth/ui-authorization-negative-path.test.js test/auth/sso-negative-path.test.js --test-name-pattern="access|tenant|deny"</automated>
  </verify>
  <done>
Billing and governance routes can rely on real session-bound tenant and role context instead of scaffold constants.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 54-03-02: Add tenant SSO binding and Supabase SAML entry/callback flow (per D-10, D-11, D-12)</name>
  <read_first>
    - .planning/phases/54-billing-metering-and-enterprise-governance/54-RESEARCH.md
    - lib/markos/identity/contracts.ts
    - onboarding/backend/runtime-context.cjs
  </read_first>
  <files>api/auth/sso/start.js, api/auth/sso/callback.js, lib/markos/identity/sso-bindings.ts, supabase/migrations/54_identity_federation.sql, test/auth/sso-role-mapping.test.js</files>
  <acceptance_criteria>
    - Each enterprise tenant can bind a Supabase SSO provider without creating a second auth stack.
    - Start and callback handlers stay tenant-scoped and preserve provider metadata needed for mapping/audit.
    - Provisioning automation beyond deterministic binding remains deferred; no SCIM-style expansion is introduced.
  </acceptance_criteria>
  <action>
Implement tenant SSO bindings and Supabase SAML start/callback handlers so Phase 54 uses the approved federation path per D-10 and stays within the deferred-scope boundary from D-12. Persist provider bindings in MarkOS tables keyed by tenant and provider ID. Do not implement a separate Passport SAML stack.
  </action>
  <verify>
    <automated>node --test test/auth/sso-role-mapping.test.js test/auth/sso-negative-path.test.js --test-name-pattern="provider|callback|binding|sso"</automated>
  </verify>
  <done>
Enterprise SSO exists as a tenant-bound Supabase integration surface ready for canonical role mapping.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 54-03-03: Enforce canonical external-claim mapping and immutable identity audit events (per D-10, D-11, D-13, D-14)</name>
  <read_first>
    - lib/markos/rbac/iam-v32.js
    - lib/markos/identity/contracts.ts
    - onboarding/backend/runtime-context.cjs
  </read_first>
  <files>lib/markos/identity/role-mapping.ts, lib/markos/rbac/iam-v32.js, onboarding/backend/runtime-context.cjs, test/auth/sso-role-mapping.test.js, test/auth/sso-negative-path.test.js</files>
  <acceptance_criteria>
    - External claims resolve only to canonical IAM v3.2 roles.
    - Unmapped or escalating claims are denied deterministically and emit immutable identity evidence with source claims, matched rule, decision, actor, tenant, and correlation context.
    - Runtime helpers expose the mapping results to later billing and governance APIs without widening permissions.
  </acceptance_criteria>
  <action>
Implement deterministic claim-mapping rules that target only the canonical roles defined in `iam-v32.js` per D-11. Persist both success and denial events for identity-bound privilege changes per D-13/D-14, and reuse runtime-context deny/evidence helpers instead of inventing a second audit format.
  </action>
  <verify>
    <automated>node --test test/auth/sso-role-mapping.test.js test/auth/sso-negative-path.test.js test/tenant-auth/ui-authorization-negative-path.test.js</automated>
  </verify>
  <done>
IAM-04 is satisfied with tenant-bound SSO, canonical-role mapping, and immutable audit evidence for both positive and negative paths.
  </done>
</task>

</tasks>

<verification>
Run the SSO and UI denial suites after each task; no Phase 54 UI plan should start until scaffold auth is gone.

Human checkpoint before full phase closeout: execute a real enterprise IdP sign-in against non-production Supabase Enterprise SSO and confirm canonical-role mapping plus escalation denial behavior end-to-end.
</verification>

<success_criteria>
Enterprise identity federation is live on the existing Supabase auth stack, route auth is hardened, role mapping is canonical, explainable, and fail-closed, and the live non-production IdP callback path is queued as a mandatory human checkpoint for phase closeout.
</success_criteria>

<output>
After completion, create `.planning/phases/54-billing-metering-and-enterprise-governance/54-03-SUMMARY.md`
</output>
