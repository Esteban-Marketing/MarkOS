---
phase: 54-billing-metering-and-enterprise-governance
plan: 05
type: execute
wave: 4
depends_on:
  - 54-02
  - 54-03
  - 54-04
files_modified:
  - package.json
  - supabase/migrations/54_billing_provider_sync.sql
  - lib/markos/billing/stripe-sync.ts
  - lib/markos/billing/invoice-line-items.ts
  - api/billing/tenant-summary.js
  - api/billing/operator-reconciliation.js
  - api/billing/holds.js
  - app/(markos)/settings/billing/page.tsx
  - app/(markos)/settings/billing/actions.ts
  - app/(markos)/admin/billing/page.tsx
  - app/(markos)/admin/billing/reconciliation.stories.tsx
  - test/billing/invoice-reconciliation.test.js
  - test/billing/provider-sync-failure.test.js
  - test/ui-billing/billing-pages-contract.test.js
autonomous: true
requirements:
  - BIL-01
  - BIL-02
  - BIL-03
user_setup:
  - service: stripe
    why: "Stripe is the invoice and payment projection boundary for subscriptions, invoices, and dunning."
    env_vars:
      - name: STRIPE_SECRET_KEY
        source: "Stripe Dashboard -> Developers -> API keys"
      - name: STRIPE_WEBHOOK_SECRET
        source: "Stripe Dashboard -> Developers -> Webhooks"
    dashboard_config:
      - task: "Create billing webhook endpoint for invoice, subscription, and payment failure events"
        location: "Stripe Dashboard -> Developers -> Webhooks"

must_haves:
  truths:
    - "Operators can reconcile invoice-grade usage and billing-provider failures from the MarkOS ledger before invoice close."
    - "Tenant billing pages and operator billing pages read from the same reconciled ledger and invoice evidence, not separate reporting paths, while preserving audience-appropriate role boundaries."
    - "Stripe receives MarkOS-produced invoice and sync projections, while holds and restricted-action behavior remain governed by MarkOS state."
  artifacts:
    - path: "lib/markos/billing/stripe-sync.ts"
      provides: "Stripe customer/subscription/invoice projection helpers plus sync-attempt logging."
      exports:
        - "syncBillingProjectionToStripe"
        - "recordStripeSyncAttempt"
    - path: "lib/markos/billing/invoice-line-items.ts"
      provides: "Invoice line item generation from immutable MarkOS usage ledger rows and pricing snapshots."
      exports:
        - "buildInvoiceLineItems"
        - "buildReconciliationSummary"
    - path: "app/(markos)/settings/billing/page.tsx"
      provides: "Tenant billing page using the approved Phase 54 UI contract."
      exports: []
    - path: "app/(markos)/admin/billing/page.tsx"
      provides: "Operator billing reconciliation page using the approved Phase 54 UI contract."
      exports: []
  key_links:
    - from: "lib/markos/billing/invoice-line-items.ts"
      to: "lib/markos/billing/usage-ledger.ts"
      via: "ledger-to-line-item reconciliation"
      pattern: "BillingUsageLedgerRow|InvoiceLineItem|pricing_snapshot"
    - from: "api/billing/operator-reconciliation.js"
      to: "lib/markos/billing/stripe-sync.ts"
      via: "sync state and mismatch review"
      pattern: "syncBillingProjectionToStripe|recordStripeSyncAttempt|hold"
    - from: "app/(markos)/settings/billing/page.tsx"
      to: "api/billing/tenant-summary.js"
      via: "tenant view of the same reconciled ledger"
      pattern: "billing period|included usage|current invoice|hold"
---

<objective>
Deliver invoice evidence, Stripe sync projection, and the tenant plus operator billing surfaces.

Purpose: Satisfy BIL-03 without violating the ledger-first architecture by projecting MarkOS-owned line items to Stripe and exposing the same reconciled evidence to tenants and operators.
Output: Invoice line item builder, Stripe sync log, billing APIs, and UI-contract-compliant tenant/operator billing pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/54-billing-metering-and-enterprise-governance/54-CONTEXT.md
@.planning/phases/54-billing-metering-and-enterprise-governance/54-RESEARCH.md
@.planning/phases/54-billing-metering-and-enterprise-governance/54-UI-SPEC.md
@.planning/phases/54-billing-metering-and-enterprise-governance/54-02-PLAN.md
@.planning/phases/54-billing-metering-and-enterprise-governance/54-03-PLAN.md
@.planning/phases/54-billing-metering-and-enterprise-governance/54-04-PLAN.md
@app/(markos)/settings/plugins/page.tsx
@app/(markos)/settings/plugins/actions.ts
@app/(markos)/operations/tasks/evidence-panel.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 54-05-01: Build invoice line items and Stripe projection sync without surrendering ledger truth (per D-01, D-03, D-04, D-06)</name>
  <read_first>
    - lib/markos/billing/usage-ledger.ts
    - lib/markos/billing/contracts.ts
    - test/billing/invoice-reconciliation.test.js
  </read_first>
  <files>package.json, supabase/migrations/54_billing_provider_sync.sql, lib/markos/billing/stripe-sync.ts, lib/markos/billing/invoice-line-items.ts, test/billing/invoice-reconciliation.test.js, test/billing/provider-sync-failure.test.js</files>
  <acceptance_criteria>
    - Stripe integration is added as a projection layer only; invoice line items originate from MarkOS ledger rows and pricing snapshots.
    - Sync attempts, failures, retries, and holds are logged in MarkOS tables and remain auditable.
    - Provider failures trigger entitlement-safe degradation and never silently over-provision restricted actions.
    - Monthly and annual billing terms resolve through the same MarkOS-owned subscription, ledger, and invoice vocabulary even if proration, credits, or dispute workflows remain deferred.
  </acceptance_criteria>
  <action>
Install `stripe`, add provider-sync persistence, and implement invoice-line-item generation from the immutable MarkOS ledger. Push only finalized or bounded-period projections to Stripe per D-01/D-03. Persist sync failures, dunning state, and hold history in MarkOS so provider outages cannot bypass fail-closed behavior per D-04/D-06.
  </action>
  <verify>
    <automated>node --test test/billing/invoice-reconciliation.test.js test/billing/provider-sync-failure.test.js</automated>
  </verify>
  <done>
Invoice-grade billing evidence and Stripe sync projection exist with MarkOS still acting as the ledger and degradation authority.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 54-05-02: Expose shared tenant and operator billing APIs from the reconciled ledger (per D-07, D-08, D-09)</name>
  <read_first>
    - api/tenant-plugin-settings.js
    - lib/markos/billing/invoice-line-items.ts
    - lib/markos/billing/entitlements.ts
  </read_first>
  <files>api/billing/tenant-summary.js, api/billing/operator-reconciliation.js, api/billing/holds.js, test/billing/invoice-reconciliation.test.js, test/billing/provider-sync-failure.test.js</files>
  <acceptance_criteria>
    - Tenant and operator billing endpoints use the same ledger, invoice, and hold state source.
    - Operator APIs expose reconciliation mismatches, sync failures, and hold histories with invoice evidence drilldown.
    - Tenant APIs translate internal units into billing vocabulary without exposing raw telemetry jargon by default.
    - Operator reconciliation and hold actions stay restricted to canonical IAM billing/owner roles, and tenant-facing responses remain tenant-scoped with no cross-tenant leakage.
  </acceptance_criteria>
  <action>
Create billing APIs that serve the same reconciled ledger to both audiences per D-07/D-09. Keep operator capabilities for reconciliation, hold review, and sync retry per D-08 behind canonical IAM billing permissions, and prevent separate approximate reporting paths from emerging. Tenant responses should use plain billing vocabulary while retaining translated evidence drilldowns and must remain tenant-scoped with explicit fail-closed denial behavior for unauthorized operator access.
  </action>
  <verify>
    <automated>node --test test/billing/invoice-reconciliation.test.js test/billing/provider-sync-failure.test.js test/tenant-auth/ui-authorization-negative-path.test.js --test-name-pattern="tenant|operator|hold|sync|invoice|deny|billing"</automated>
  </verify>
  <done>
Billing data is available through one shared ledger-backed API surface for both tenants and operators.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 54-05-03: Build tenant and operator billing pages to the approved UI contract (per D-07, D-08, D-09)</name>
  <read_first>
    - .planning/phases/54-billing-metering-and-enterprise-governance/54-UI-SPEC.md
    - app/(markos)/settings/plugins/page.tsx
    - app/(markos)/operations/tasks/evidence-panel.tsx
  </read_first>
  <files>app/(markos)/settings/billing/page.tsx, app/(markos)/settings/billing/actions.ts, app/(markos)/admin/billing/page.tsx, app/(markos)/admin/billing/reconciliation.stories.tsx, test/ui-billing/billing-pages-contract.test.js</files>
  <acceptance_criteria>
    - Tenant billing page follows the required module order, quiet finance-style hierarchy, and fail-closed state treatment from `54-UI-SPEC.md`.
    - Operator billing page renders reconciliation queue, detail panel, and evidence rail from shared ledger data with no chart-first shortcuts.
    - Story and contract tests cover healthy, approaching-limit, blocked, hold, mismatch, sync-failure, route access-denied, and missing-tenant-context fail-closed states, with auth-foundation coverage allowed to satisfy the final two states.
    - Tenant and operator billing surfaces expose billing-term context for monthly and annual plans using the same reconciled ledger vocabulary.
  </acceptance_criteria>
  <action>
Implement `settings/billing` and `admin/billing` against the approved UI contract. Preserve the existing token system, typography, spacing, and evidence-first patterns; do not introduce a third-party component library. Ensure both pages render from the same ledger-backed APIs and display blocked/hold/sync-review states exactly as specified in `54-UI-SPEC.md`.
  </action>
  <verify>
    <automated>node --test test/billing/invoice-reconciliation.test.js test/billing/provider-sync-failure.test.js test/ui-billing/billing-pages-contract.test.js</automated>
  </verify>
  <done>
Tenants and operators can inspect the same reconciled billing evidence through UI-contract-compliant pages with deterministic hold and mismatch states.
  </done>
</task>

</tasks>

<verification>
Run the invoice and UI billing suites after each task; Stripe sync is incomplete unless the UI can explain failures from MarkOS-owned evidence.

Human checkpoint before full phase closeout: review tenant and operator billing pages in a running app session to confirm translated billing language, evidence readability, and monthly/annual term presentation.
</verification>

<success_criteria>
Operators can reconcile invoice-grade usage and provider failures, tenants can see their translated billing state from the same ledger, monthly and annual term context stays consistent across both views, and Stripe remains only a sync boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/54-billing-metering-and-enterprise-governance/54-05-SUMMARY.md`
</output>
