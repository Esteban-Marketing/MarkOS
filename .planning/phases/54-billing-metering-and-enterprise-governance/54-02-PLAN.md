---
phase: 54-billing-metering-and-enterprise-governance
plan: 02
type: execute
wave: 2
depends_on:
  - 54-01
files_modified:
  - supabase/migrations/54_billing_foundation.sql
  - lib/markos/billing/usage-normalizer.ts
  - lib/markos/billing/usage-ledger.ts
  - lib/markos/billing/pricing-catalog.ts
  - onboarding/backend/markosdb-contracts.cjs
  - test/billing/usage-normalization.test.js
  - test/billing/usage-ledger-aggregation.test.js
autonomous: true
requirements:
  - BIL-02

must_haves:
  truths:
    - "Plugin and agent telemetry normalize into deduplicated billing usage events without inflating usage on retries or redelivery."
    - "Billing-period ledger rows remain immutable and map every billable unit back to raw Phase 52 and Phase 53 lineage."
    - "MarkOS owns the priced usage ledger before any Stripe projection occurs."
  artifacts:
    - path: "supabase/migrations/54_billing_foundation.sql"
      provides: "Billing periods, normalized usage-event tables, usage-ledger tables, pricing snapshots, and lineage indexes."
      exports: []
    - path: "lib/markos/billing/usage-normalizer.ts"
      provides: "Telemetry-to-billing normalization and deterministic dedupe-key generation."
      exports:
        - "normalizePluginUsageEvent"
        - "normalizeAgentRunUsageEvent"
        - "buildUsageDedupeKey"
    - path: "lib/markos/billing/usage-ledger.ts"
      provides: "Billing-period aggregation and immutable ledger materialization helpers."
      exports:
        - "aggregateUsageForPeriod"
        - "buildLedgerRows"
  key_links:
    - from: "lib/markos/plugins/telemetry.js"
      to: "lib/markos/billing/usage-normalizer.ts"
      via: "plugin telemetry normalization"
      pattern: "tenant_id|correlation_id|emitPluginOperation"
    - from: "onboarding/backend/agents/telemetry.cjs"
      to: "lib/markos/billing/usage-normalizer.ts"
      via: "run-close and provider-attempt normalization"
      pattern: "captureProviderAttempt|captureRunClose|cost_usd"
    - from: "lib/markos/billing/usage-normalizer.ts"
      to: "lib/markos/billing/usage-ledger.ts"
      via: "usage-event aggregation into immutable period rows"
      pattern: "source_event_key|billing_period_start|billing_period_end"
---

<objective>
Build the Phase 54 billing data plane and usage normalization foundation.

Purpose: Deliver BIL-02 first so every later entitlement, invoice, and governance workflow depends on deduplicated MarkOS-owned billing records instead of raw telemetry or Stripe state.
Output: Billing schema migration, usage normalizer, usage ledger aggregator, and pricing snapshot helpers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/54-billing-metering-and-enterprise-governance/54-CONTEXT.md
@.planning/phases/54-billing-metering-and-enterprise-governance/54-RESEARCH.md
@.planning/phases/54-billing-metering-and-enterprise-governance/54-01-PLAN.md
@onboarding/backend/agents/telemetry.cjs
@lib/markos/plugins/telemetry.js
@supabase/migrations/52_plugin_runtime_foundation.sql
@supabase/migrations/53_agent_run_lifecycle.sql

<interfaces>
From onboarding/backend/agents/telemetry.cjs:
```javascript
function captureProviderAttempt(properties = {})
function captureRunClose(properties = {})
```

From lib/markos/plugins/telemetry.js:
```javascript
module.exports = { emitPluginOperation, emitPluginDeny, sanitizePayload };
```

From supabase/migrations/52_plugin_runtime_foundation.sql:
```sql
create table if not exists plugin_entitlements_by_plan (
create table if not exists plugin_tenant_config (
create table if not exists plugin_tenant_capability_grants (
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 54-02-01: Create billing foundation schema and RED-to-GREEN normalization path (per D-01, D-02, D-03)</name>
  <read_first>
    - supabase/migrations/52_plugin_runtime_foundation.sql
    - supabase/migrations/53_agent_run_lifecycle.sql
    - lib/markos/billing/contracts.ts
  </read_first>
  <files>supabase/migrations/54_billing_foundation.sql, onboarding/backend/markosdb-contracts.cjs, test/billing/usage-normalization.test.js</files>
  <acceptance_criteria>
    - Migration adds billing-period, normalized usage-event, pricing snapshot, and usage-ledger tables keyed by tenant and billing period.
    - New tables preserve raw source references for plugin telemetry and agent telemetry lineage.
    - MarkOSDB contract helpers expose the new billing tables without weakening tenant isolation or append-only behavior.
  </acceptance_criteria>
  <action>
Create the billing foundation migration with ledger-first tables and indexes needed for BIL-02. Preserve raw telemetry lineage and deterministic dedupe keys per D-02/D-03. Keep MarkOS as the source of truth per D-01 by storing priced usage snapshots inside MarkOSDB rather than treating Stripe as the primary ledger.
  </action>
  <verify>
    <automated>node --test test/billing/usage-normalization.test.js</automated>
  </verify>
  <done>
Billing persistence exists for normalized events and immutable period ledger rows with tenant-safe lineage references.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 54-02-02: Implement telemetry normalization and deterministic dedupe keys (per D-02, D-03)</name>
  <read_first>
    - onboarding/backend/agents/telemetry.cjs
    - lib/markos/plugins/telemetry.js
    - test/helpers/billing-fixtures.cjs
  </read_first>
  <files>lib/markos/billing/usage-normalizer.ts, test/billing/usage-normalization.test.js</files>
  <acceptance_criteria>
    - Plugin and agent telemetry each normalize into one canonical usage-event contract.
    - Dedupe keys are deterministic across retries/redelivery and preserve correlation lineage.
    - Normalization rejects malformed or incomplete billing inputs instead of silently approximating units.
  </acceptance_criteria>
  <action>
Implement the usage normalizer against the Phase 52 and 53 telemetry contracts. Normalize plugin operations, run-close records, and provider attempts into MarkOS billing usage events with explicit unit types, quantities, lineage refs, and dedupe keys per D-02/D-03. Fail closed on incomplete billing inputs rather than guessing billable units.
  </action>
  <verify>
    <automated>node --test test/billing/usage-normalization.test.js test/billing/usage-ledger-aggregation.test.js --test-name-pattern="normalize|dedupe|lineage"</automated>
  </verify>
  <done>
Usage events can be built deterministically from immutable telemetry with duplicate delivery protection.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 54-02-03: Aggregate normalized usage into immutable billing-period ledger rows (per D-01, D-03)</name>
  <read_first>
    - lib/markos/billing/usage-normalizer.ts
    - lib/markos/billing/contracts.ts
    - test/billing/usage-ledger-aggregation.test.js
  </read_first>
  <files>lib/markos/billing/usage-ledger.ts, lib/markos/billing/pricing-catalog.ts, test/billing/usage-ledger-aggregation.test.js</files>
  <acceptance_criteria>
    - Aggregation produces immutable ledger rows by tenant, period, unit type, and pricing snapshot.
    - Ledger rows keep back-references to normalized usage events and raw source lineage.
    - Pricing snapshots are versioned inside MarkOS so invoice evidence can later explain the exact unit price used.
  </acceptance_criteria>
  <action>
Implement usage aggregation and pricing snapshot helpers so BIL-02 ends with immutable billing-period ledger rows that can later drive entitlement snapshots and invoice line items. Keep pricing snapshots and aggregation state inside MarkOS per D-01, and retain event/lineage back-references per D-03.
  </action>
  <verify>
    <automated>node --test test/billing/usage-normalization.test.js test/billing/usage-ledger-aggregation.test.js</automated>
  </verify>
  <done>
MarkOS can materialize invoice-ready usage ledger rows from normalized telemetry without provider involvement.
  </done>
</task>

</tasks>

<verification>
Run the billing normalization suite after each task; no later Phase 54 plan should start before BIL-02 is green.
</verification>

<success_criteria>
Phase 54 has a tenant-safe, deduplicated, lineage-preserving usage ledger that is stable enough for entitlement and invoice work.
</success_criteria>

<output>
After completion, create `.planning/phases/54-billing-metering-and-enterprise-governance/54-02-SUMMARY.md`
</output>
