---
phase: 217
plan: "04"
type: execute
wave: 3
depends_on: ["01", "02", "03"]
f_ids:
  - F-252
  - F-253
  - F-254
migration_slots: []
integrates_with:
  - "P214 saas_suite_activations (READ via auth gate)"
  - "P215 tenant_billing_subscriptions (READ via subscriptions/plans/invoices handlers)"
  - "P215 billing_periods (READ via invoices handler)"
  - "P216 saas_health_scores (READ via health handler)"
  - "P208 agent_approval_packages (READ via approvals handler)"
  - "P207 markos_agent_runs (READ via tasks handler — SOFT)"
files_modified:
  - api/v1/saas/subscriptions.js
  - api/v1/saas/plans.js
  - api/v1/saas/invoices.js
  - api/v1/saas/health.js
  - api/v1/saas/metrics.js
  - api/v1/saas/agents.js
  - api/v1/saas/tasks.js
  - api/v1/saas/approvals.js
  - lib/markos/saas/api/handler-helpers.ts
  - lib/markos/saas/api/handler-helpers.cjs
  - lib/markos/saas/api/index.cjs
  - contracts/F-252-saas-billing-v1.yaml
  - contracts/F-253-saas-intelligence-v1.yaml
  - contracts/F-254-saas-agents-v1.yaml
  - contracts/openapi.json
  - contracts/flow-registry.json
  - test/saas-217/domain-4/api-subscriptions.test.js
  - test/saas-217/domain-4/api-plans.test.js
  - test/saas-217/domain-4/api-invoices.test.js
  - test/saas-217/domain-4/api-health.test.js
  - test/saas-217/domain-4/api-metrics.test.js
  - test/saas-217/domain-4/api-agents.test.js
  - test/saas-217/domain-4/api-tasks.test.js
  - test/saas-217/domain-4/api-approvals.test.js
  - test/saas-217/domain-4/api-contract-shape.test.js
  - test/saas-217/domain-4/api-architecture-lock.test.js
  - .planning/phases/217-saas-suite-revenue-agents-api-ui/217-VALIDATION.md
autonomous: true
requirements:
  - SAS-10
  - API-01
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - QA-05
  - QA-06
  - QA-07
  - QA-08
  - QA-09
  - QA-10
must_haves:
  truths:
    - "Plan 04 ships file-artifact API surface — NO new migration slot (file artifacts F-IDs only). All 8 handler files under `api/v1/saas/*.js` (NEW directory `api/v1/` created by P217 — verified absent on disk per RESEARCH §Architecture Lock)"
    - "All handlers follow legacy `api/*.js` pattern (NOT App Router `route.ts`) per architecture-lock — module.exports default async function handler(req, res); writeJson helper for response; CommonJS"
    - "Every handler imports `requireHostedSupabaseAuth` from `onboarding/backend/runtime-context.cjs:491` (verified path); auth result provides tenant_id; handler delegates to lib/markos/saas/* with tenant_id explicitly passed (RLS-bypass-proof)"
    - "Every handler is GET-only (read-only intelligence surface) — POST mutations remain in their owning phase handlers (P215 billing for subscription changes, P208 for approval mutations); P217 surface is read-only operator viewport per Canon §Revenue Intelligence read-only posture. Plans 01 + 03 ship the only POST endpoints (F-247 metric defs + F-251 agent activation request) — those handlers are written by Plan 04 here using same pattern"
    - "10 GET endpoints + 2 POST endpoints (Plan 01 F-247 definitions POST + Plan 03 F-251 activation POST) across 8 handler files: subscriptions.js (GET list+detail), plans.js (GET list+detail), invoices.js (GET list+detail), health.js (GET tenant health), metrics.js (GET mrr|nrr|grr|expansion|cohorts|forecast|waterfall|definitions; also POST /v1/saas/metrics/definitions for F-247), agents.js (GET list+detail; also POST /v1/saas/agents/:token/activate for F-251), tasks.js (GET SaaS-scoped task queue), approvals.js (GET SaaS-scoped approval queue)"
    - "Architecture-lock test (file-artifact compliance — NOT a DB-trigger because API contracts are file artifacts) — `test/saas-217/domain-4/api-architecture-lock.test.js` asserts: (a) every `api/v1/saas/*.js` handler imports `requireHostedSupabaseAuth` (positive grep); (b) NO handler contains `requireSupabaseAuth`, `serviceRoleClient`, `createApprovalPackage` (negative grep — read-only surface; mutations only via Plan 01 metric-definitions and Plan 03 sas-agent-activation paths which DO use buildApprovalPackage); (c) NO handler ends in `route.ts` (legacy pattern lock); (d) every mutating endpoint emits audit log via lib/markos/audit/*"
    - "3 contracts ship F-252 (saas-billing-v1: subscriptions/plans/invoices/health — 7 endpoints), F-253 (saas-intelligence-v1: metrics — 8 endpoints incl. F-247 definitions POST shape reference), F-254 (saas-agents-v1: agents/tasks/approvals — 5 endpoints incl. F-251 activation POST shape reference); NO collision against F-001..F-251"
    - "contracts/openapi.json regenerated after F-252+F-253+F-254 added — every P217 endpoint path resolvable; openapi build script (npm run openapi:build OR equivalent — verify exact command in package.json) produces valid spec; api-contract-shape.test asserts F-247..F-258 paths exist in spec"
    - "All handlers consume tenant_id from auth.tenant_id (NEVER from query params — prevents tenant_id forgery); RLS policy on each table provides defense-in-depth"
    - "Plan 04 architecture-lock RE-RUN — runs scanForbiddenPatterns across api/v1/saas/ + lib/markos/saas/api/; assertion: 0 violations"
    - "217-VALIDATION.md per-task verification map updated with rows for Plan 04 tasks (8 handler files + contract-shape + architecture-lock)"
  artifacts:
    - path: api/v1/saas/subscriptions.js
      provides: GET /v1/saas/subscriptions (list with pagination); GET /v1/saas/subscriptions/:id (detail). Reads P215 tenant_billing_subscriptions filtered by tenant_id. F-252.
    - path: api/v1/saas/plans.js
      provides: GET /v1/saas/plans (list); GET /v1/saas/plans/:id (detail). Reads P214/P215 plan tables. Pricing fields use `{{MARKOS_PRICING_ENGINE_PENDING}}` sentinel when P205 PricingRecommendation absent. F-252.
    - path: api/v1/saas/invoices.js
      provides: GET /v1/saas/invoices (list); GET /v1/saas/invoices/:id (detail with billing compliance state — DIAN/QuickBooks sync flag). Reads P215 invoice tables. F-252.
    - path: api/v1/saas/health.js
      provides: GET /v1/saas/health (tenant aggregate health — adoption, engagement, churn risk). Reads P216 saas_health_scores. F-252.
    - path: api/v1/saas/metrics.js
      provides: GET /v1/saas/metrics/{mrr|nrr|grr|expansion|cohorts|forecast|waterfall|definitions} (8 paths) + POST /v1/saas/metrics/definitions (F-247 — Plan 01 defined contract; Plan 04 ships handler using lib/markos/saas/revenue/metric-definitions.ts). F-253.
    - path: api/v1/saas/agents.js
      provides: GET /v1/saas/agents (list with readiness flags) + GET /v1/saas/agents/:token (detail) + POST /v1/saas/agents/:token/activate (F-251 — Plan 03 defined contract; Plan 04 ships handler using lib/markos/saas/sas-agents/readiness-check.ts). F-254.
    - path: api/v1/saas/tasks.js
      provides: GET /v1/saas/tasks (SaaS-scoped task queue — filtered to tasks where domain='saas' or kind matches saas_*). Reads P208 substrate. F-254.
    - path: api/v1/saas/approvals.js
      provides: GET /v1/saas/approvals (SaaS-scoped approval queue — filtered to approval packages where kind LIKE 'saas_%' OR 'sas_agent_%' OR 'mrr_reconciliation_%'). Reads P208 agent_approval_packages. F-254.
    - path: lib/markos/saas/api/handler-helpers.ts
      provides: writeJson(res, status, payload) + parsePagination(query) + extractTenantId(auth) + requireRole(auth, role) + emitAuditLog({ tenant_id, action, payload }) shared across all 8 handlers
      exports: [writeJson, parsePagination, extractTenantId, requireRole, emitAuditLog]
    - path: lib/markos/saas/api/index.cjs
      provides: CommonJS twin re-exporting handler-helpers
    - path: contracts/F-252-saas-billing-v1.yaml
      provides: subscriptions+plans+invoices+health endpoints (7 GET); auth requires saas_billing_read role; pagination via cursor parameter; error envelope shape
    - path: contracts/F-253-saas-intelligence-v1.yaml
      provides: metrics endpoints (8 GET + 1 POST F-247 ref); response schemas reference Plan 01/02 zod (RevenueMetricDefinitionSchema, SaaSMRRSnapshotSchema)
    - path: contracts/F-254-saas-agents-v1.yaml
      provides: agents+tasks+approvals endpoints (4 GET + 1 POST F-251 ref); response schemas reference Plan 03 zod (SASAgentReadinessSchema)
    - path: contracts/openapi.json
      provides: regenerated spec after F-247+F-248+F-249+F-250+F-251+F-252+F-253+F-254 contracts integrated; all 12 P217 endpoint paths resolvable
    - path: contracts/flow-registry.json
      provides: 3 entries appended for F-252 + F-253 + F-254
    - path: test/saas-217/domain-4/api-architecture-lock.test.js
      provides: scans api/v1/saas/*.js for: positive presence of requireHostedSupabaseAuth import + audit log emit on mutating endpoints; negative presence of requireSupabaseAuth / serviceRoleClient / createApprovalPackage / .ts extension / route.ts paths; asserts every handler reads tenant_id from auth (not query)
    - path: test/saas-217/domain-4/api-contract-shape.test.js
      provides: parses contracts/openapi.json; asserts F-247..F-258 paths all present (12 contract IDs); response schemas reference Plan 01/02/03 zod schemas correctly; no orphan paths
    - path: test/saas-217/domain-4/api-{subscriptions,plans,invoices,health,metrics,agents,tasks,approvals}.test.js
      provides: handler unit tests — mock supabase + mock auth; assert GET returns 200 + correct shape for valid request; 401 without auth; 403 without role; 404 on missing resource; 405 on wrong method
  key_links:
    - from: api/v1/saas/metrics.js
      to: lib/markos/saas/revenue/metric-definitions.cjs
      via: POST /v1/saas/metrics/definitions handler imports createMetricDefinition + seedDefaultDefinitions from Plan 01
      pattern: "createMetricDefinition"
    - from: api/v1/saas/agents.js
      to: lib/markos/saas/sas-agents/readiness-check.cjs
      via: POST /v1/saas/agents/:token/activate handler imports activateAgent from Plan 03
      pattern: "activateAgent"
    - from: api/v1/saas/metrics.js
      to: lib/markos/saas/mrr/snapshot-builder.cjs
      via: GET /v1/saas/metrics/mrr handler reads latest snapshot via lib (Plan 02 contract); GET /v1/saas/metrics/cohorts uses computeCohortRetention
      pattern: "saas_mrr_snapshots"
    - from: every api/v1/saas/*.js
      to: onboarding/backend/runtime-context.cjs
      via: requireHostedSupabaseAuth at handler entry; auth.tenant_id flows to all SELECTs
      pattern: "requireHostedSupabaseAuth"
    - from: api/v1/saas/agents.js + api/v1/saas/metrics.js
      to: lib/markos/audit/*
      via: POST endpoints emit audit log entry on success (mutation tracking)
      pattern: "emitAuditLog"
---

<objective>
Ship the 12 `/v1/saas/*` API endpoints across 8 handler files, the 3 F-ID contracts (F-252 saas-billing-v1, F-253 saas-intelligence-v1, F-254 saas-agents-v1), the openapi.json regeneration, and the architecture-lock test enforcing tenant-auth + RLS-aware queries + audit emission across the surface. This is a FILE-ARTIFACT domain — no new migration slot needed (Plan 04 + Plan 05 share file-artifact slots; only F-IDs reserved).

API surface posture is READ-ONLY for intelligence (Canon §Revenue Intelligence read-only posture); mutations remain in owning phase handlers EXCEPT for the 2 P217-defined POST endpoints: F-247 POST /v1/saas/metrics/definitions (Plan 01-defined; uses createMetricDefinition + buildApprovalPackage on changes) and F-251 POST /v1/saas/agents/:token/activate (Plan 03-defined; uses activateAgent + buildApprovalPackage). Both POST handlers ship in this plan.

Every handler:
1. Imports requireHostedSupabaseAuth from onboarding/backend/runtime-context.cjs:491
2. Authenticates first; gets tenant_id from auth (NEVER from query — prevents tenant_id forgery)
3. Delegates business logic to lib/markos/saas/* libraries (Plan 01/02/03 outputs)
4. Returns JSON via writeJson helper; respects HTTP method (405 on method mismatch)
5. Emits audit log on mutation success
6. Architecture-lock test asserts these properties via static analysis

api/v1/ directory is NEW — Plan 04 creates it (verified absent on disk per RESEARCH §Architecture Lock).

Output: 8 handler files + 1 helper lib + 3 F-ID contract YAMLs + regenerated openapi.json + flow-registry update + 10 domain-4 test files + populated VALIDATION.md.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/217-saas-suite-revenue-agents-api-ui/217-CONTEXT.md
@.planning/phases/217-saas-suite-revenue-agents-api-ui/217-RESEARCH.md
@.planning/phases/217-saas-suite-revenue-agents-api-ui/217-VALIDATION.md
@.planning/phases/217-saas-suite-revenue-agents-api-ui/217-01-PLAN.md
@.planning/phases/217-saas-suite-revenue-agents-api-ui/217-02-PLAN.md
@.planning/phases/217-saas-suite-revenue-agents-api-ui/217-03-PLAN.md
@.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md
@obsidian/reference/HTTP Layer.md
@obsidian/brain/SaaS Suite Canon.md
@CLAUDE.md
@onboarding/backend/runtime-context.cjs
@lib/markos/crm/agent-actions.ts
@lib/markos/saas/preflight/index.cjs
@lib/markos/saas/revenue/metric-definitions.cjs
@lib/markos/saas/mrr/snapshot-builder.cjs
@lib/markos/saas/mrr/cohort-retention.cjs
@lib/markos/saas/sas-agents/registry.cjs
@lib/markos/saas/sas-agents/readiness-check.cjs
@contracts/openapi.json
@contracts/flow-registry.json

<interfaces>
<!-- All from upstream P217 plans (Plans 01/02/03 already executed in Waves 1+2): -->

From onboarding/backend/runtime-context.cjs:491:
```javascript
async function requireHostedSupabaseAuth({ req, runtimeContext, operation, requiredProjectSlug, env }) → { tenant_id, user_id, project_slug, ... }
```

From lib/markos/saas/revenue/metric-definitions.cjs (Plan 01):
```javascript
async function createMetricDefinition(input, supabase) → row
async function getMetricDefinition({ tenantId, metricKey }, supabase) → row | null
async function seedDefaultDefinitions({ tenantId }, supabase) → number (count)
```

From lib/markos/saas/mrr/snapshot-builder.cjs + cohort-retention.cjs (Plan 02):
```javascript
// snapshot-builder.cjs reads — Plan 04 metrics.js calls these for GET endpoints
async function getLatestSnapshot({ tenantId, snapshotType }, supabase) → row
async function listSnapshotsForPeriod({ tenantId, periodStart, periodEnd }, supabase) → row[]
async function computeCohortRetention({ tenantId, asOfDate, cohortPeriods }, supabase) → object
```

From lib/markos/saas/sas-agents/registry.cjs + readiness-check.cjs (Plan 03):
```javascript
async function listAgentsForTenant({ tenantId }, supabase) → row[]
async function getAgentReadiness({ tenantId, agentToken }, supabase) → row | null
async function activateAgent({ tenantId, agentToken, approvalId, readinessEvidence }, supabase) → row
```

From lib/markos/crm/agent-actions.ts:68:
```typescript
function buildApprovalPackage({ kind, tenant_id, ...payload }) → { approval_id, ... }
```

P217 Endpoint Enumeration (RESEARCH §Domain 4 — 12 contract IDs across 8 handler files):
| Endpoint | Method | Handler | F-ID |
|----------|--------|---------|------|
| /v1/saas/subscriptions | GET (list+detail) | subscriptions.js | F-252 |
| /v1/saas/plans | GET (list+detail) | plans.js | F-252 |
| /v1/saas/invoices | GET (list+detail) | invoices.js | F-252 |
| /v1/saas/health | GET | health.js | F-252 |
| /v1/saas/metrics/definitions | GET+POST | metrics.js | F-247 (Plan 01) + F-253 |
| /v1/saas/metrics/{mrr,nrr,grr,expansion,cohorts,forecast,waterfall} | GET | metrics.js | F-253 |
| /v1/saas/agents | GET (list+detail) | agents.js | F-250 (Plan 03) + F-254 |
| /v1/saas/agents/:token/activate | POST | agents.js | F-251 (Plan 03) |
| /v1/saas/tasks | GET | tasks.js | F-254 |
| /v1/saas/approvals | GET | approvals.js | F-254 |

Handler skeleton pattern (RESEARCH §Domain 4 lines 765-787):
```js
'use strict';
const { createRuntimeContext, requireHostedSupabaseAuth } = require('../../../onboarding/backend/runtime-context.cjs');
const { writeJson, extractTenantId } = require('../../../lib/markos/saas/api/index.cjs');

module.exports = async function handler(req, res) {
  if (req.method !== 'GET') return writeJson(res, 405, { error: 'METHOD_NOT_ALLOWED' });
  const runtimeContext = createRuntimeContext();
  const auth = await requireHostedSupabaseAuth({ req, runtimeContext, operation: 'saas_metrics_read' });
  if (!auth.ok) return writeJson(res, auth.status, { error: auth.error });
  // ... domain logic reading lib/markos/saas/* with auth.tenant_id
  writeJson(res, 200, result);
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: api/v1/saas/ handler-helpers + 8 read-only GET handlers (subscriptions, plans, invoices, health, metrics-GET, agents-GET, tasks, approvals)</name>
  <files>api/v1/saas/subscriptions.js, api/v1/saas/plans.js, api/v1/saas/invoices.js, api/v1/saas/health.js, api/v1/saas/metrics.js, api/v1/saas/agents.js, api/v1/saas/tasks.js, api/v1/saas/approvals.js, lib/markos/saas/api/handler-helpers.ts, lib/markos/saas/api/handler-helpers.cjs, lib/markos/saas/api/index.cjs, test/saas-217/domain-4/api-subscriptions.test.js, test/saas-217/domain-4/api-plans.test.js, test/saas-217/domain-4/api-invoices.test.js, test/saas-217/domain-4/api-health.test.js, test/saas-217/domain-4/api-metrics.test.js, test/saas-217/domain-4/api-agents.test.js, test/saas-217/domain-4/api-tasks.test.js, test/saas-217/domain-4/api-approvals.test.js</files>
  <behavior>
    - Test 1: handler-helpers exports writeJson + parsePagination + extractTenantId + requireRole + emitAuditLog as functions; CommonJS module loads
    - Test 2: GET /v1/saas/subscriptions without auth → 401; with auth + 0 subscriptions → 200 {data: [], cursor: null}; with auth + tenant has 3 subs → 200 with 3 records; tenant_id filtering enforced (mock supabase verifies WHERE tenant_id = auth.tenant_id)
    - Test 3: GET /v1/saas/plans returns plan list; pricing field is `{{MARKOS_PRICING_ENGINE_PENDING}}` if P205 not landed (graceful degrade per assertUpstreamReady SOFT pattern)
    - Test 4: GET /v1/saas/invoices includes billing compliance state (dian_status, quickbooks_synced fields from P215)
    - Test 5: GET /v1/saas/health returns aggregate health score (reads P216 saas_health_scores)
    - Test 6: GET /v1/saas/metrics/mrr returns latest saas_mrr_snapshots row; GET /v1/saas/metrics/cohorts calls computeCohortRetention; GET /v1/saas/metrics/waterfall returns snapshot+entries; GET /v1/saas/metrics/definitions returns metric definition list
    - Test 7: GET /v1/saas/agents returns sas_agent_readiness list with runnable + missing flags; GET /v1/saas/agents/:token returns single agent
    - Test 8: GET /v1/saas/tasks returns tasks where domain='saas' (filtered against P208 substrate); GET /v1/saas/approvals returns approval packages where kind LIKE 'saas_%' OR 'sas_agent_%' OR 'mrr_reconciliation_%'
    - Test 9: ALL handlers — POST/PUT/DELETE returns 405; missing auth returns 401; missing required role returns 403
    - Test 10: ALL handlers — tenant_id is read from auth.tenant_id, NEVER from req.query.tenant_id (static analysis test asserts no `query.tenant_id` access in handler files)
  </behavior>
  <read_first>
    - 217-RESEARCH.md §Domain 4 (read endpoint enumeration table lines 725-746 + handler pattern lines 765-787)
    - 217-01-PLAN.md (lib/markos/saas/preflight/index.cjs + lib/markos/saas/revenue/* imports)
    - 217-02-PLAN.md (lib/markos/saas/mrr/* imports — getLatestSnapshot, computeCohortRetention)
    - 217-03-PLAN.md (lib/markos/saas/sas-agents/* imports — listAgentsForTenant, getAgentReadiness)
    - onboarding/backend/runtime-context.cjs:491 (requireHostedSupabaseAuth signature)
    - api/cron/mcp-kpi-digest.js (writeJson helper pattern)
    - api/cron/webhooks-dlq-purge.js (createClient pattern)
    - obsidian/reference/HTTP Layer.md (auth + RLS + pagination + error envelope conventions)
    - .planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-06-PLAN.md (P220 ships parallel api/v1/growth/* — pattern reference)
  </read_first>
  <action>
    Step 1: Write all 9 handler tests in test/saas-217/domain-4/api-*.test.js BEFORE implementation (TDD RED). Each uses node:test + mocked supabase + mocked auth.
    Step 2: Implement lib/markos/saas/api/handler-helpers.ts:
      - writeJson(res, statusCode, payload) — sets Content-Type + JSON.stringify
      - parsePagination(query) → { cursor, limit } with limit clamped 1..100
      - extractTenantId(auth) → throws if auth.tenant_id missing; returns string
      - requireRole(auth, role) → returns boolean (caller writeJson 403 if false)
      - emitAuditLog({ tenant_id, action, payload }, supabase) → INSERT into audit log table per lib/markos/audit/*
      - CommonJS twin in handler-helpers.cjs
    Step 3: Implement lib/markos/saas/api/index.cjs as barrel.
    Step 4: Implement 8 handler files following the skeleton pattern (RESEARCH §Domain 4 lines 765-787):
      - api/v1/saas/subscriptions.js: GET /v1/saas/subscriptions (list with cursor pagination); GET /v1/saas/subscriptions/:id (path-segment routing within single handler — req.url match)
      - api/v1/saas/plans.js: GET /v1/saas/plans + GET /v1/saas/plans/:id; pricing fields use `{{MARKOS_PRICING_ENGINE_PENDING}}` sentinel when P205 returns null
      - api/v1/saas/invoices.js: GET list + detail; reads P215
      - api/v1/saas/health.js: GET tenant aggregate; reads P216
      - api/v1/saas/metrics.js: routes by URL path — /metrics/mrr → getLatestSnapshot, /metrics/nrr → derived, /metrics/cohorts → computeCohortRetention, /metrics/waterfall → snapshot+entries, /metrics/definitions GET → list, /metrics/forecast → derived; handles all 8 metric paths
      - api/v1/saas/agents.js: GET list + GET :token; reads sas_agent_readiness
      - api/v1/saas/tasks.js: GET list filtered to saas domain
      - api/v1/saas/approvals.js: GET list filtered to saas+sas_agent+mrr_reconciliation kinds
    Step 5: Each handler: extracts tenant_id from auth ONLY; passes tenant_id to lib/markos/saas/* callers; returns response via writeJson.
    Step 6: Run all 8 handler tests + helper tests (TDD GREEN).
  </action>
  <acceptance_criteria>
    - All 8 `npm test -- test/saas-217/domain-4/api-*.test.js` files pass (9 behaviors × 8 handlers green)
    - `ls api/v1/saas/*.js | wc -l` returns 8
    - `grep -l "requireHostedSupabaseAuth" api/v1/saas/*.js | wc -l` returns 8 (every handler authenticates)
    - `grep -l "query.tenant_id\|req.query.tenant_id" api/v1/saas/*.js | wc -l` returns 0 (NO handler reads tenant_id from query)
    - `grep -l "requireSupabaseAuth\|serviceRoleClient" api/v1/saas/*.js | wc -l` returns 0 (no forbidden auth helpers)
    - `node -e "console.log(typeof require('./lib/markos/saas/api/index.cjs').writeJson)"` outputs `function`
    - `grep -E "route\.ts" api/v1/saas/*.js | wc -l` returns 0 (legacy api/*.js pattern)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST handlers (F-247 metric definitions + F-251 agent activation) + F-252/F-253/F-254 contracts + openapi.json regen + api-architecture-lock test + api-contract-shape test</name>
  <files>api/v1/saas/metrics.js, api/v1/saas/agents.js, contracts/F-252-saas-billing-v1.yaml, contracts/F-253-saas-intelligence-v1.yaml, contracts/F-254-saas-agents-v1.yaml, contracts/openapi.json, contracts/flow-registry.json, test/saas-217/domain-4/api-architecture-lock.test.js, test/saas-217/domain-4/api-contract-shape.test.js, .planning/phases/217-saas-suite-revenue-agents-api-ui/217-VALIDATION.md</files>
  <behavior>
    - Test 1: POST /v1/saas/metrics/definitions — without auth → 401; without manage_saas role → 403; with valid body matching RevenueMetricDefinitionSchema → calls createMetricDefinition + emitAuditLog → returns 201 with new row; invalid body (missing formula_description) → 400; existing metric_key → 409 conflict OR 200 update path
    - Test 2: POST /v1/saas/agents/:token/activate — without auth → 401; without manage_saas role → 403; with valid readiness_evidence body → calls activateAgent (which calls buildApprovalPackage internally if approvalId not provided) → returns 202 with {approval_id, agent_state}; agent already activated → 409
    - Test 3: api-architecture-lock.test scans api/v1/saas/*.js and asserts: every handler imports requireHostedSupabaseAuth (count=8); NO handler imports requireSupabaseAuth/serviceRoleClient (count=0); ONLY metrics.js + agents.js import buildApprovalPackage (count=2 — matches POST endpoints); NO handler ends in route.ts; NO handler reads tenant_id from query
    - Test 4: api-architecture-lock.test asserts every mutation endpoint emits emitAuditLog call (POST handlers in metrics.js + agents.js); GET handlers do not emit audit
    - Test 5: api-contract-shape.test parses contracts/openapi.json after regeneration; asserts F-247 path /v1/saas/metrics/definitions GET+POST present; F-248 paths /v1/saas/metrics/mrr + /v1/saas/metrics/waterfall GET present; F-249 path /v1/saas/metrics/cohorts GET present; F-250 paths /v1/saas/agents + /v1/saas/agents/:token GET present; F-251 path /v1/saas/agents/:token/activate POST present; F-252 paths subscriptions/plans/invoices/health all GET present; F-253 paths metrics/* all present; F-254 paths agents/tasks/approvals all present
    - Test 6: api-contract-shape.test asserts response schemas reference Plan 01/02/03 zod schemas correctly — RevenueMetricDefinitionSchema, SaaSMRRSnapshotSchema, MRRWaterfallEntrySchema, SASAgentReadinessSchema all referenced in openapi.json components.schemas
    - Test 7: F-252 + F-253 + F-254 YAML files exist + flow-registry.json has 3 new entries
    - Test 8: architecture-lock RE-RUN — scanForbiddenPatterns({ paths: ['api/v1/saas/', 'lib/markos/saas/api/'] }) returns []
  </behavior>
  <read_first>
    - 217-RESEARCH.md §Domain 4 (architecture-lock test assertion list lines 789-794)
    - 217-RESEARCH.md §Domain 4 file structure tree (lines 750-761)
    - 217-01-PLAN.md F-247 contract (Plan 01 defined POST shape; Plan 04 ships handler)
    - 217-03-PLAN.md F-251 contract (Plan 03 defined POST shape; Plan 04 ships handler)
    - 217-04-PLAN Task 1 output (8 handler files)
    - lib/markos/saas/revenue/metric-definitions.cjs (createMetricDefinition signature)
    - lib/markos/saas/sas-agents/readiness-check.cjs (activateAgent signature)
    - lib/markos/crm/agent-actions.ts (buildApprovalPackage signature)
    - contracts/openapi.json (existing spec — verify format + schemas section structure)
    - package.json (verify openapi build script command)
  </read_first>
  <action>
    Step 1: Write tests 1-8 BEFORE implementation (TDD RED).
    Step 2: ADD POST handler logic to api/v1/saas/metrics.js for /v1/saas/metrics/definitions:
      ```js
      // Inside metrics.js handler — after method check
      if (req.method === 'POST' && req.url === '/v1/saas/metrics/definitions') {
        const auth = await requireHostedSupabaseAuth({ req, runtimeContext, operation: 'saas_metric_define' });
        if (!auth.ok) return writeJson(res, auth.status, { error: auth.error });
        if (!requireRole(auth, 'manage_saas')) return writeJson(res, 403, { error: 'FORBIDDEN' });
        const body = await parseJsonBody(req);
        const validated = RevenueMetricDefinitionSchema.safeParse(body);
        if (!validated.success) return writeJson(res, 400, { error: 'VALIDATION_FAILED', details: validated.error.errors });
        const result = await createMetricDefinition({ ...validated.data, tenant_id: auth.tenant_id }, supabase);
        await emitAuditLog({ tenant_id: auth.tenant_id, action: 'saas.metric.definition.created', payload: { metric_key: result.metric_key } }, supabase);
        return writeJson(res, 201, result);
      }
      ```
    Step 3: ADD POST handler logic to api/v1/saas/agents.js for /v1/saas/agents/:token/activate:
      ```js
      if (req.method === 'POST' && /^\/v1\/saas\/agents\/[^/]+\/activate$/.test(req.url)) {
        const token = req.url.match(/\/v1\/saas\/agents\/([^/]+)\/activate/)[1];
        const auth = await requireHostedSupabaseAuth({ req, runtimeContext, operation: 'saas_agent_activate' });
        if (!auth.ok) return writeJson(res, auth.status, { error: auth.error });
        if (!requireRole(auth, 'manage_saas')) return writeJson(res, 403, { error: 'FORBIDDEN' });
        const body = await parseJsonBody(req);
        const result = await activateAgent({ tenantId: auth.tenant_id, agentToken: token, readinessEvidence: body.readiness_evidence }, supabase);
        await emitAuditLog({ tenant_id: auth.tenant_id, action: 'saas.agent.activation.requested', payload: { agent_token: token } }, supabase);
        return writeJson(res, 202, result);
      }
      ```
    Step 4: Write contracts/F-252-saas-billing-v1.yaml — OpenAPI 3.1 doc covering 7 endpoints (subscriptions list+detail, plans list+detail, invoices list+detail, health). Each endpoint: requireHostedSupabaseAuth security; tenant_id from auth; pagination via cursor for list endpoints; error envelope.
    Step 5: Write contracts/F-253-saas-intelligence-v1.yaml — covering 8 metric paths (GET) + reference to F-247 POST shape; response schemas reference RevenueMetricDefinitionSchema, SaaSMRRSnapshotSchema, MRRWaterfallEntrySchema.
    Step 6: Write contracts/F-254-saas-agents-v1.yaml — covering agents list+detail (F-250 ref) + tasks GET + approvals GET + reference to F-251 POST activate shape; response schemas reference SASAgentReadinessSchema.
    Step 7: Append F-252 + F-253 + F-254 entries to contracts/flow-registry.json.
    Step 8: Regenerate contracts/openapi.json — read package.json for openapi build script (likely `npm run openapi:build` or equivalent). Run script. Verify F-247..F-254 paths and schemas integrated into openapi.json. NOTE: per RESEARCH §Architecture Lock, openapi-generate is a forbidden pattern; use the project's actual build script (likely `node scripts/openapi-build.cjs` or similar — verify in package.json).
    Step 9: Write test/saas-217/domain-4/api-architecture-lock.test.js — uses Node fs to read every api/v1/saas/*.js file; asserts presence/absence per Test 3 + Test 4 behaviors; uses regex matchers for grep counts.
    Step 10: Write test/saas-217/domain-4/api-contract-shape.test.js — parses contracts/openapi.json (JSON.parse); walks paths object; asserts every F-247..F-254 endpoint path exists; walks components.schemas; asserts all P217 zod schemas referenced.
    Step 11: Run all 8 tests (TDD GREEN). Update 217-VALIDATION.md per-task verification map for 217-04 (10 rows: 8 handler tests + architecture-lock + contract-shape).
    Step 12: Commit: `feat(217-04): /v1/saas/* 8 handlers + 3 contracts (F-252/F-253/F-254) + openapi.json regen + architecture-lock test`.
  </action>
  <acceptance_criteria>
    - `npm test -- test/saas-217/domain-4/api-architecture-lock.test.js` passes (4 positive + 3 negative + 1 audit assertion green)
    - `npm test -- test/saas-217/domain-4/api-contract-shape.test.js` passes (12 F-IDs paths + schema refs green)
    - `npm test -- test/saas-217/domain-4/api-metrics.test.js` POST tests pass (auth + validation + audit emit green)
    - `npm test -- test/saas-217/domain-4/api-agents.test.js` POST tests pass (auth + activateAgent + audit emit green)
    - `ls contracts/F-252-saas-billing-v1.yaml contracts/F-253-saas-intelligence-v1.yaml contracts/F-254-saas-agents-v1.yaml` all exist
    - `grep -E "F-252|F-253|F-254" contracts/flow-registry.json | wc -l` returns at least 3
    - `grep -E "/v1/saas/metrics/definitions|/v1/saas/agents/.*activate|/v1/saas/subscriptions|/v1/saas/health" contracts/openapi.json | wc -l` returns at least 4 (key paths present)
    - `grep -l "buildApprovalPackage" api/v1/saas/*.js | wc -l` returns 2 (only metrics.js + agents.js for the 2 POST endpoints)
    - `grep -l "emitAuditLog" api/v1/saas/*.js | wc -l` returns at least 2 (mutations audit-emit)
    - `node -e "const { scanForbiddenPatterns } = require('./lib/markos/saas/preflight/index.cjs'); console.log(scanForbiddenPatterns({ paths: ['api/v1/saas/', 'lib/markos/saas/api/'] }).length)"` outputs `0`
    - `217-VALIDATION.md` Per-Task Verification Map row for 217-04 marked status=green (10 sub-tests verified)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | All 12 P217 endpoints accept auth-scoped requests; tenant_id from auth, never query |
| handler → DB | Handler delegates to lib/markos/saas/* with tenant_id; RLS policies on each table provide defense-in-depth |
| client → POST | F-247 POST creates metric definition; F-251 POST requests agent activation (both behind manage_saas role) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-217-21 | Spoofing | Forged tenant_id via query parameter | mitigate | Architecture-lock test asserts no handler reads req.query.tenant_id; auth.tenant_id only |
| T-217-22 | Tampering | Body bypass on POST endpoints | mitigate | RevenueMetricDefinitionSchema + readiness_evidence zod validation at API entry; DB-trigger defense-in-depth |
| T-217-23 | Repudiation | Mutation without audit | mitigate | emitAuditLog called on every POST success (architecture-lock test asserts call presence) |
| T-217-24 | Information Disclosure | Cross-tenant data leak | mitigate | Every SELECT scoped by auth.tenant_id; RLS policy enforces at DB layer |
| T-217-25 | Denial of Service | Pagination missing → unbounded query | mitigate | parsePagination clamps limit to 1..100; cursor-based pagination prevents offset abuse |
| T-217-26 | Elevation of Privilege | manage_saas role bypass | mitigate | requireRole(auth, 'manage_saas') check before mutation; 403 on failure |
| T-217-27 | Tampering | Direct call to buildApprovalPackage skipping role check | mitigate | Architecture-lock test asserts buildApprovalPackage only used in metrics.js + agents.js POST handlers (count=2) |
| T-217-28 | Information Disclosure | Pricing leak when P205 absent | mitigate | `{{MARKOS_PRICING_ENGINE_PENDING}}` sentinel substitutes — no real pricing exposed pre-P205 |
</threat_model>

<verification>
- 8 handler files in api/v1/saas/ — every one imports requireHostedSupabaseAuth; reads tenant_id from auth only
- 2 POST handlers (metrics.js + agents.js) — call buildApprovalPackage with manage_saas role gate + emitAuditLog
- 3 F-ID contracts (F-252, F-253, F-254) written + flow-registry.json updated
- contracts/openapi.json regenerated with all P217 endpoint paths (F-247..F-254 verified)
- Architecture-lock test green (8 positive + multiple negative assertions)
- Contract-shape test green (12 F-ID paths + schema references)
- All 8 handler unit tests green
- 0 violations in scanForbiddenPatterns RE-RUN over api/v1/saas/ + lib/markos/saas/api/
- 217-VALIDATION.md updated with Plan 04 task rows
</verification>

<success_criteria>
- 12 endpoints across 8 handler files implement F-IDs F-247..F-254 surface
- Read-only intelligence posture preserved (10 GET + 2 POST; mutations require approval)
- Architecture-lock enforces auth/RLS/audit/no-route.ts at file-artifact level
- All 2 task acceptance criteria sets pass
</success_criteria>

<output>
After completion, create `.planning/phases/217-saas-suite-revenue-agents-api-ui/217-04-SUMMARY.md`
</output>
