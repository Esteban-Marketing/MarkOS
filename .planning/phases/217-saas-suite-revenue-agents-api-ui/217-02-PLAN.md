---
phase: 217
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
f_ids:
  - F-248
  - F-249
migration_slots:
  - 98
integrates_with:
  - "P215 tenant_billing_subscriptions (READ via snapshot cron)"
  - "P215 billing_periods (READ via snapshot cron)"
  - "P215 billing_usage_events (READ via snapshot cron)"
  - "P218 Plan 01 mode-eligibility (DOWNSTREAM READ — mrr_end, snapshot_date, tenant_id)"
  - "P219 Plan 02 expansion-signal-scanner (DOWNSTREAM READ — mrr_end, expansion_mrr, nrr_pct, tenant_id, snapshot_date)"
files_modified:
  - supabase/migrations/98_saas_revenue_intelligence.sql
  - lib/markos/saas/mrr/contracts.ts
  - lib/markos/saas/mrr/contracts.cjs
  - lib/markos/saas/mrr/snapshot-builder.ts
  - lib/markos/saas/mrr/snapshot-builder.cjs
  - lib/markos/saas/mrr/waterfall-entries.ts
  - lib/markos/saas/mrr/waterfall-entries.cjs
  - lib/markos/saas/mrr/cohort-retention.ts
  - lib/markos/saas/mrr/cohort-retention.cjs
  - lib/markos/saas/mrr/index.cjs
  - api/cron/saas-mrr-snapshot.js
  - contracts/F-248-saas-mrr-snapshots-v1.yaml
  - contracts/F-249-saas-mrr-waterfall-entries-v1.yaml
  - contracts/flow-registry.json
  - test/saas-217/domain-2/mrr-snapshots.test.js
  - test/saas-217/domain-2/waterfall-entries.test.js
  - test/saas-217/domain-2/snapshot-builder.test.js
  - test/saas-217/domain-2/nightly-cron.test.js
  - test/saas-217/domain-2/cohort-retention.test.js
  - test/saas-217/domain-2/mrr-snapshot-trigger.test.js
  - test/saas-217/rls/mrr-snapshots-cross-tenant-denial.test.js
  - .planning/phases/217-saas-suite-revenue-agents-api-ui/217-VALIDATION.md
autonomous: true
requirements:
  - SAS-09
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - QA-05
  - QA-06
  - QA-07
  - QA-08
must_haves:
  truths:
    - "saas_mrr_snapshots and saas_mrr_waterfall_entries tables APPENDED to migration 98 (Plan 01 already shipped the file with placeholder DO-blocks; Plan 02 fills them) — RLS-enabled tenant_id-scoped per markos_orgs FK; UNIQUE(tenant_id, snapshot_date, snapshot_type); idx on (tenant_id, snapshot_date DESC) + (tenant_id, reconciliation_state)"
    - "saas_mrr_snapshots EXACT column set per RESEARCH §Domain 2 SQL — locked contract for downstream consumers (P218 Plan 01 + P219 Plan 02 read by exact column names): snapshot_id PK, tenant_id, snapshot_date, period_start, period_end, snapshot_type CHECK ('nightly','monthly','manual'), mrr_start numeric(18,2), new_mrr, expansion_mrr, contraction_mrr (negative), churn_mrr (negative), reactivation_mrr, net_new_mrr GENERATED, mrr_end GENERATED, arr GENERATED, nrr_pct numeric(7,4) nullable, grr_pct nullable, churn_rate_logo_pct nullable, churn_rate_revenue_pct nullable, active_subscriptions int, new/churned/expansion/reactivated_subscriptions int, currency text default 'USD', reconciliation_state CHECK 5 values default 'pending', reconciled_at, reconciled_by, source_billing_hash text (SHA-256), source_processor_hash text, evidence_refs text[], created_at, updated_at"
    - "saas_mrr_waterfall_entries line-item table — entry_id PK, snapshot_id FK CASCADE, tenant_id FK markos_orgs, subscription_id text (FK ref to P215), customer_id uuid, movement_type CHECK ('new','expansion','contraction','churn','reactivation','unchanged'), mrr_amount numeric(18,2) (signed), plan_id, seats_delta int, reason text, created_at; RLS-enabled with tenant_id policy; idx on snapshot_id + (tenant_id, movement_type)"
    - "DB-trigger compliance — `MRR_SNAPSHOT_REQUIRES_RECONCILIATION_STATE` BEFORE INSERT OR UPDATE on saas_mrr_snapshots raises EXCEPTION when reconciliation_state IS NULL OR when attempting INSERT of duplicate (tenant_id, snapshot_date, snapshot_type) where existing row has reconciliation_state='reconciled' (idempotency guard; per RESEARCH §Compliance Enforcement Boundary Plan 02 row — exact exception name verbatim)"
    - "MRR snapshot cron handler `api/cron/saas-mrr-snapshot.js` — auth via x-markos-cron-secret header matching MARKOS_SAAS_CRON_SECRET env (matches api/cron/mcp-kpi-digest.js pattern); schedule 0 2 * * * (2am UTC nightly); response shape {success: boolean, tenant_count: number, snapshot_ids: string[], duration_ms: number}; delegates to lib/markos/saas/mrr/snapshot-builder.cjs"
    - "snapshot-builder reads P215 tenant_billing_subscriptions + billing_usage_events for each active SaaS tenant (from P214 saas_suite_activations status='active'); writes saas_mrr_snapshots row + saas_mrr_waterfall_entries rows; computes mrr_start (previous reconciled snapshot mrr_end), new/expansion/contraction/churn/reactivation_mrr from subscription lifecycle deltas; sets reconciliation_state based on source agreement (uses lib/markos/saas/revenue/source-precedence.resolveSourceConflict from Plan 01)"
    - "When source-precedence resolution yields reconciliation_state='conflict_flagged' → snapshot-builder calls buildApprovalPackage({ kind: 'mrr_reconciliation_conflict', tenant_id, snapshot_id, divergence_pct, sources }) creating a P1 task in P208 substrate"
    - "cohort-retention helper exports computeCohortRetention({ tenantId, periods: ['1m','3m','6m','12m'] }) returning per-cohort retention table; reads saas_mrr_snapshots history grouped by acquisition cohort"
    - "2 contracts ship F-248 (saas-mrr-snapshots-v1) covering GET /v1/saas/metrics/mrr (latest snapshot) + GET /v1/saas/metrics/waterfall (snapshot history with waterfall entries); F-249 (saas-mrr-waterfall-entries-v1) covering GET /v1/saas/metrics/cohorts (cohort retention); NO collision against F-001..F-247"
    - "Cross-tenant denial test asserts INSERT saas_mrr_snapshots under tenant A; SELECT under tenant B returns 0 rows; same for saas_mrr_waterfall_entries"
    - "Plan 02 documents that downstream P218 Plan 01 mode-eligibility reads (mrr_end, snapshot_date, tenant_id) and P219 Plan 02 expansion-signal-scanner reads (mrr_end, expansion_mrr, nrr_pct, tenant_id, snapshot_date) — column-name lock; any rename invalidates downstream preflight"
    - "Plan 02 architecture-lock RE-RUN — runs lib/markos/saas/preflight/architecture-lock.scanForbiddenPatterns() across lib/markos/saas/mrr/ + api/cron/saas-* + contracts/F-248* + contracts/F-249*; assertion: 0 violations"
    - "217-VALIDATION.md per-task verification map updated with rows for Plan 02 tasks"
  artifacts:
    - path: supabase/migrations/98_saas_revenue_intelligence.sql
      provides: APPENDED Plan 02 section — saas_mrr_snapshots + saas_mrr_waterfall_entries CREATE TABLE statements + MRR_SNAPSHOT_REQUIRES_RECONCILIATION_STATE trigger + RLS policies + indexes; NOTE Plan 02 EDITS the existing file (does NOT create a new migration; combined slot 98 strategy)
    - path: lib/markos/saas/mrr/contracts.ts
      provides: zod schemas + enum constants — SNAPSHOT_TYPES = ['nightly','monthly','manual'] as const; MOVEMENT_TYPES = ['new','expansion','contraction','churn','reactivation','unchanged'] as const; SaaSMRRSnapshotSchema zod (matches DB column set); MRRWaterfallEntrySchema zod
      exports: [SNAPSHOT_TYPES, MOVEMENT_TYPES, SaaSMRRSnapshotSchema, MRRWaterfallEntrySchema]
    - path: lib/markos/saas/mrr/snapshot-builder.ts
      provides: buildSnapshotForTenant({ tenantId, snapshotDate, snapshotType }, supabase) — reads P215 billing tables; computes MRR waterfall; calls source-precedence.resolveSourceConflict; writes snapshot + waterfall entries; on conflict_flagged calls buildApprovalPackage; returns snapshot_id
      exports: [buildSnapshotForTenant, buildSnapshotForAllActiveTenants]
    - path: lib/markos/saas/mrr/waterfall-entries.ts
      provides: deriveWaterfallEntries({ tenantId, periodStart, periodEnd, billingEvents }) — pure function transforming billing lifecycle events into MRRWaterfallEntry[] with movement_type classification + signed mrr_amount
      exports: [deriveWaterfallEntries, classifyMovement]
    - path: lib/markos/saas/mrr/cohort-retention.ts
      provides: computeCohortRetention({ tenantId, asOfDate, cohortPeriods }) — reads snapshot history; computes T+1/T+3/T+6/T+12 retention per acquisition cohort
      exports: [computeCohortRetention, getAcquisitionCohorts]
    - path: lib/markos/saas/mrr/index.cjs
      provides: CommonJS twin re-exporting full surface
    - path: api/cron/saas-mrr-snapshot.js
      provides: nightly cron handler (CommonJS, legacy api/*.js pattern); auth via x-markos-cron-secret + MARKOS_SAAS_CRON_SECRET env; calls buildSnapshotForAllActiveTenants; response writeJson with success/tenant_count/snapshot_ids/duration_ms
    - path: contracts/F-248-saas-mrr-snapshots-v1.yaml
      provides: GET /v1/saas/metrics/mrr (latest snapshot) + GET /v1/saas/metrics/waterfall (snapshot+entries by period); response schema mirrors SaaSMRRSnapshotSchema
    - path: contracts/F-249-saas-mrr-waterfall-entries-v1.yaml
      provides: GET /v1/saas/metrics/cohorts (cohort retention table) — derived from snapshot history; response schema includes per-cohort retention pcts at T+1/3/6/12
    - path: contracts/flow-registry.json
      provides: 2 entries appended for F-248 + F-249
    - path: test/saas-217/domain-2/mrr-snapshots.test.js
      provides: Postgres-backed test asserting saas_mrr_snapshots schema (column-presence, GENERATED columns produce expected math, CHECK constraints enforce ENUM values, RLS policy applies)
    - path: test/saas-217/domain-2/snapshot-builder.test.js
      provides: integration test using mocked supabase + P215 fixture data — buildSnapshotForTenant produces expected snapshot row with correct mrr_start (from previous reconciled snapshot) + correct waterfall classification + reconciliation_state derived from source-precedence
    - path: test/saas-217/domain-2/nightly-cron.test.js
      provides: cron handler test — POST without x-markos-cron-secret returns 401; POST with valid secret + no active tenants returns {success: true, tenant_count: 0, snapshot_ids: [], duration_ms: number}; POST with 1 active tenant returns 1 snapshot_id; conflict_flagged tenant invokes buildApprovalPackage spy
    - path: test/saas-217/domain-2/mrr-snapshot-trigger.test.js
      provides: Postgres test — INSERT saas_mrr_snapshots with reconciliation_state=NULL → expect EXCEPTION 'MRR_SNAPSHOT_REQUIRES_RECONCILIATION_STATE'; INSERT duplicate (tenant_id, snapshot_date, snapshot_type) where prior is reconciled → same exception; INSERT valid pending row → succeeds
  key_links:
    - from: lib/markos/saas/mrr/snapshot-builder.ts
      to: lib/markos/saas/revenue/source-precedence.ts
      via: imports resolveSourceConflict from Plan 01; calls it per metric to determine reconciliation_state for the snapshot
      pattern: "resolveSourceConflict"
    - from: lib/markos/saas/mrr/snapshot-builder.ts
      to: lib/markos/crm/agent-actions.ts
      via: imports buildApprovalPackage; calls with kind='mrr_reconciliation_conflict' when conflict_flagged
      pattern: "buildApprovalPackage"
    - from: api/cron/saas-mrr-snapshot.js
      to: lib/markos/saas/mrr/snapshot-builder.cjs
      via: cron handler delegates to buildSnapshotForAllActiveTenants; matches api/cron/mcp-kpi-digest.js pattern
      pattern: "buildSnapshotForAllActiveTenants"
    - from: lib/markos/saas/mrr/snapshot-builder.ts
      to: supabase/migrations/98_saas_revenue_intelligence.sql
      via: app-layer INSERT into saas_mrr_snapshots; DB-trigger trg_mrr_snapshot_reconciliation enforces reconciliation_state non-null + idempotency (defense-in-depth)
      pattern: "MRR_SNAPSHOT_REQUIRES_RECONCILIATION_STATE"
---

<objective>
Ship the SaaSMRRSnapshot foundation that downstream P218+P219 phases hard-gate on. Plan 02 APPENDS to migration 98 (Plan 01 already shipped the file with placeholder DO-blocks): saas_mrr_snapshots table (locked column contract — P218 Plan 01 + P219 Plan 02 read by exact column names) + saas_mrr_waterfall_entries table + MRR_SNAPSHOT_REQUIRES_RECONCILIATION_STATE DB-trigger (verbatim per RESEARCH §Compliance Enforcement Boundary). Ships nightly snapshot cron (api/cron/saas-mrr-snapshot.js — 2am UTC) reading P215 billing tables, computing MRR waterfall, calling source-precedence.resolveSourceConflict (Plan 01), creating P1 approval task on conflict_flagged via buildApprovalPackage. Ships cohort-retention helper + 2 F-ID contracts (F-248 saas-mrr-snapshots-v1, F-249 saas-mrr-waterfall-entries-v1).

Purpose: satisfy SAS-09 (revenue intelligence — MRR/ARR/NRR/GRR/churn/expansion/cohorts/forecast as durable records). Plan 02 is the most P218/P219 critical plan in P217 — `saas_mrr_snapshots` is in P218-01-PLAN.md REQUIRED_UPSTREAM and P219-02-PLAN.md expansion-signal-scanner. Column-name contract is LOCKED (rename invalidates downstream preflight). QA-01..08 covered (architecture-lock + helper-presence + RLS + F-ID parity + threat model + assertUpstreamReady + DB-trigger compliance + idempotency).

Output: APPENDED migration 98 (Plan 02 portion) + 8 mrr lib files + 1 cron handler + 2 F-ID contracts + flow-registry update + 5 domain-2 test files + 1 RLS test + populated VALIDATION.md.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/217-saas-suite-revenue-agents-api-ui/217-CONTEXT.md
@.planning/phases/217-saas-suite-revenue-agents-api-ui/217-RESEARCH.md
@.planning/phases/217-saas-suite-revenue-agents-api-ui/217-VALIDATION.md
@.planning/phases/217-saas-suite-revenue-agents-api-ui/217-01-PLAN.md
@.planning/phases/218-saas-growth-profile-plg-inapp-experimentation/218-01-PLAN.md
@.planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-02-PLAN.md
@.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md
@obsidian/brain/SaaS Suite Canon.md
@obsidian/reference/MarkOS v2 Operating Loop Spec.md
@CLAUDE.md
@lib/markos/saas/preflight/index.cjs
@lib/markos/saas/revenue/source-precedence.cjs
@lib/markos/crm/agent-actions.ts
@onboarding/backend/runtime-context.cjs
@api/cron/mcp-kpi-digest.js
@supabase/migrations/82_markos_audit_log_hash_chain.sql

<interfaces>
<!-- From Plan 01 (already executed in Wave 1): -->

From lib/markos/saas/preflight/index.cjs:
```javascript
module.exports = { assertUpstreamReady, scanForbiddenPatterns, REQUIRED_UPSTREAM, SOFT_UPSTREAM };
```

From lib/markos/saas/revenue/source-precedence.cjs:
```javascript
function resolveSourceConflict({ sources, tolerance_pct }) → { chosen_source, reconciliation_state, divergence_pct }
function getSourcePrecedenceForMetric(metricKey) → string[]
```

From Plan 01 lib/markos/saas/revenue/contracts.ts:
```typescript
export const RECONCILIATION_STATES = ['reconciled','pending_reconciliation','conflict_flagged','manually_corrected','excluded'] as const;
```

From lib/markos/crm/agent-actions.ts:68:
```typescript
function buildApprovalPackage({ kind, tenant_id, ...payload }) → { approval_id, ... }
```

From api/cron/mcp-kpi-digest.js (cron pattern):
```javascript
// x-markos-cron-secret header + MARKOS_*_CRON_SECRET env match
// writeJson(res, 200, { success, count, duration_ms })
// P217 cron secret: MARKOS_SAAS_CRON_SECRET (NEW)
```

LOCKED downstream consumer column contract (do NOT rename):
- P218 Plan 01 reads: mrr_end, snapshot_date, tenant_id
- P219 Plan 02 reads: mrr_end, expansion_mrr, nrr_pct, tenant_id, snapshot_date
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: saas_mrr_snapshots + saas_mrr_waterfall_entries SOR migration APPEND + MRR_SNAPSHOT_REQUIRES_RECONCILIATION_STATE DB-trigger + RLS</name>
  <files>supabase/migrations/98_saas_revenue_intelligence.sql, lib/markos/saas/mrr/contracts.ts, lib/markos/saas/mrr/contracts.cjs, test/saas-217/domain-2/mrr-snapshots.test.js, test/saas-217/domain-2/waterfall-entries.test.js, test/saas-217/domain-2/mrr-snapshot-trigger.test.js, test/saas-217/rls/mrr-snapshots-cross-tenant-denial.test.js</files>
  <behavior>
    - Test 1: After migration 98 (Plan 02 APPEND), saas_mrr_snapshots has full column set per RESEARCH §Domain 2 (snapshot_id PK + tenant_id FK + snapshot_date + period_start + period_end + snapshot_type CHECK 3 values + 6 MRR columns + 2 GENERATED columns net_new_mrr/mrr_end + arr GENERATED + 4 nullable rate pcts + 5 subscription count int columns + currency text + reconciliation_state CHECK 5 values default 'pending' + reconciled_at + reconciled_by + 2 source hash columns + evidence_refs text[] + created_at/updated_at + UNIQUE(tenant_id, snapshot_date, snapshot_type)); RLS enabled; idx_sms_tenant_date on (tenant_id, snapshot_date DESC) + idx_sms_reconciliation on (tenant_id, reconciliation_state)
    - Test 2: GENERATED columns math correct — INSERT mrr_start=1000, new_mrr=200, expansion_mrr=50, contraction_mrr=-30, churn_mrr=-100, reactivation_mrr=20 → net_new_mrr=140 + mrr_end=1140 + arr=13680
    - Test 3: After migration, saas_mrr_waterfall_entries has full column set; entry_id PK + snapshot_id FK CASCADE + tenant_id FK + subscription_id text + customer_id uuid + movement_type CHECK 6 values + mrr_amount numeric(18,2) + plan_id + seats_delta + reason + created_at; RLS enabled; idx on snapshot_id + (tenant_id, movement_type)
    - Test 4: DB-trigger MRR_SNAPSHOT_REQUIRES_RECONCILIATION_STATE — INSERT saas_mrr_snapshots with reconciliation_state=NULL (or empty string after cast) → expect EXCEPTION 'MRR_SNAPSHOT_REQUIRES_RECONCILIATION_STATE'; INSERT 2nd row with same (tenant_id, snapshot_date, snapshot_type) when first is reconciliation_state='reconciled' → same exception; INSERT 2nd row when first is 'pending' → succeeds
    - Test 5: Cross-tenant RLS denial — INSERT saas_mrr_snapshots under tenant A; SELECT under tenant B returns 0 rows; same for saas_mrr_waterfall_entries
    - Test 6: snapshot_type CHECK enforces ('nightly','monthly','manual') — INSERT 'weekly' raises check_violation; reconciliation_state CHECK enforces 5 ENUM values — INSERT 'invalid' raises check_violation; movement_type CHECK enforces 6 values
    - Test 7: SaaSMRRSnapshotSchema zod validates (matches column types); rejects extra fields; required fields enforced
  </behavior>
  <read_first>
    - 217-RESEARCH.md §Domain 2 (read SQL block lines 444-510 carefully — exact column types + GENERATED expressions + CHECK constraints + RLS + indexes)
    - 217-RESEARCH.md §Compliance Enforcement Boundary (Plan 02 row — MRR_SNAPSHOT_REQUIRES_RECONCILIATION_STATE exact exception name verbatim)
    - 217-RESEARCH.md §Domain 2 trigger SQL block (lines 514-538) — exact 2-clause exception logic (NULL bypass + idempotency guard)
    - 217-01-PLAN.md Task 1 (Plan 01 ships migration 98 base file with DO-block placeholders for Plans 02+06; Plan 02 APPENDS its section between the placeholders)
    - .planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md (P217-02 owns slot 98 portion; combined with Plan 01)
    - .planning/phases/218-saas-growth-profile-plg-inapp-experimentation/218-01-PLAN.md REQUIRED_UPSTREAM (saas_mrr_snapshots locked contract)
    - .planning/phases/219-saas-b2b-expansion-abm-revenue-alignment/219-02-PLAN.md (expansion-signal-scanner reads exact columns)
    - obsidian/reference/MarkOS v2 Operating Loop Spec.md line 52 (SaaSMRRSnapshot definition)
    - supabase/migrations/100_crm_schema_identity_graph_hardening.sql (recent migration patterns)
  </read_first>
  <action>
    Step 1: Write the 7 tests BEFORE migration APPEND (TDD RED).
    Step 2: APPEND to supabase/migrations/98_saas_revenue_intelligence.sql — insert Plan 02 section between Plan 01 section and Plan 06 placeholder. Use comment block:
      ```sql
      -- ============================================================
      -- Plan 217-02 / Phase 217 / Slot 98 / RESEARCH §Domain 2
      -- saas_mrr_snapshots (downstream-locked column contract; P218 + P219 read)
      -- saas_mrr_waterfall_entries (line-item detail)
      -- MRR_SNAPSHOT_REQUIRES_RECONCILIATION_STATE DB-trigger
      -- ============================================================
      ```
      CREATE TABLE saas_mrr_snapshots per RESEARCH §Domain 2 SQL block (verbatim — every column, GENERATED clause, CHECK constraint, RLS policy, index).
      CREATE TABLE saas_mrr_waterfall_entries per RESEARCH §Domain 2 SQL block.
      CREATE OR REPLACE FUNCTION enforce_mrr_snapshot_reconciliation() per RESEARCH lines 514-538.
      CREATE TRIGGER trg_mrr_snapshot_reconciliation BEFORE INSERT OR UPDATE ON saas_mrr_snapshots FOR EACH ROW EXECUTE FUNCTION enforce_mrr_snapshot_reconciliation();
      DO $$ BEGIN ASSERT (SELECT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'saas_mrr_snapshots')); ASSERT (SELECT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'saas_mrr_waterfall_entries')); ASSERT (SELECT count(*) FROM pg_trigger WHERE tgname = 'trg_mrr_snapshot_reconciliation') = 1; END $$;
    Step 3: Implement lib/markos/saas/mrr/contracts.ts — SNAPSHOT_TYPES + MOVEMENT_TYPES enum + SaaSMRRSnapshotSchema zod (mirror DB columns including GENERATED nullability) + MRRWaterfallEntrySchema zod; CommonJS twin in contracts.cjs.
    Step 4: Apply migration; run all 7 tests (TDD GREEN).
    Step 5: Commit: `db(217-02): saas_mrr_snapshots + saas_mrr_waterfall_entries + MRR_SNAPSHOT_REQUIRES_RECONCILIATION_STATE / append to slot 98`.
  </action>
  <acceptance_criteria>
    - `npm test -- test/saas-217/domain-2/mrr-snapshots.test.js` passes
    - `npm test -- test/saas-217/domain-2/waterfall-entries.test.js` passes
    - `npm test -- test/saas-217/domain-2/mrr-snapshot-trigger.test.js` passes (NULL bypass + idempotency violation paths green)
    - `npm test -- test/saas-217/rls/mrr-snapshots-cross-tenant-denial.test.js` passes
    - `psql -c "SELECT count(*) FROM information_schema.tables WHERE table_name IN ('saas_mrr_snapshots','saas_mrr_waterfall_entries')"` returns 2
    - `psql -c "SELECT count(*) FROM pg_trigger WHERE tgname = 'trg_mrr_snapshot_reconciliation'"` returns 1
    - `psql -c "SELECT column_name FROM information_schema.columns WHERE table_name='saas_mrr_snapshots' AND column_name IN ('mrr_end','expansion_mrr','nrr_pct','tenant_id','snapshot_date')" | wc -l` returns at least 5 (P218+P219 locked column contract verified)
    - `node -e "const { SaaSMRRSnapshotSchema } = require('./lib/markos/saas/mrr/contracts.cjs'); console.log(typeof SaaSMRRSnapshotSchema.parse)"` outputs `function`
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: snapshot-builder + waterfall-entries + cohort-retention lib + nightly cron + 2 F-ID contracts</name>
  <files>lib/markos/saas/mrr/snapshot-builder.ts, lib/markos/saas/mrr/snapshot-builder.cjs, lib/markos/saas/mrr/waterfall-entries.ts, lib/markos/saas/mrr/waterfall-entries.cjs, lib/markos/saas/mrr/cohort-retention.ts, lib/markos/saas/mrr/cohort-retention.cjs, lib/markos/saas/mrr/index.cjs, api/cron/saas-mrr-snapshot.js, contracts/F-248-saas-mrr-snapshots-v1.yaml, contracts/F-249-saas-mrr-waterfall-entries-v1.yaml, contracts/flow-registry.json, test/saas-217/domain-2/snapshot-builder.test.js, test/saas-217/domain-2/nightly-cron.test.js, test/saas-217/domain-2/cohort-retention.test.js, .planning/phases/217-saas-suite-revenue-agents-api-ui/217-VALIDATION.md</files>
  <behavior>
    - Test 1: deriveWaterfallEntries({ tenantId, periodStart, periodEnd, billingEvents }) classifies each billing event into one of 6 movement_types (new/expansion/contraction/churn/reactivation/unchanged) with correct signed mrr_amount; returns array of MRRWaterfallEntry shapes
    - Test 2: classifyMovement({ event, prevState, currentState }) — given lifecycle event 'subscription.created' returns 'new'; 'subscription.updated' with seats_delta>0 returns 'expansion'; 'subscription.updated' with seats_delta<0 returns 'contraction'; 'subscription.canceled' returns 'churn'; 'subscription.reactivated' returns 'reactivation'
    - Test 3: buildSnapshotForTenant({ tenantId, snapshotDate, snapshotType: 'nightly' }, mockSupabase) — reads previous reconciled snapshot for mrr_start; reads tenant_billing_subscriptions + billing_usage_events for period; calls deriveWaterfallEntries; calls resolveSourceConflict; INSERTs saas_mrr_snapshots row + waterfall entries; returns snapshot_id; on conflict_flagged calls buildApprovalPackage spy with kind='mrr_reconciliation_conflict'
    - Test 4: buildSnapshotForAllActiveTenants({ snapshotDate, snapshotType }, supabase) — reads saas_suite_activations status='active'; iterates each tenant; calls buildSnapshotForTenant; returns array of snapshot_ids
    - Test 5: computeCohortRetention({ tenantId, asOfDate, cohortPeriods }) — reads snapshot history; groups subscriptions by acquisition month (cohort); computes T+1/T+3/T+6/T+12 retention pct as MRR_retained_at_T / MRR_at_acquisition × 100
    - Test 6: api/cron/saas-mrr-snapshot.js — POST without x-markos-cron-secret returns 401; POST with valid secret + 0 active tenants returns {success: true, tenant_count: 0, snapshot_ids: [], duration_ms: number}; GET method returns 405; method mismatch handled per cron pattern
    - Test 7: F-248 + F-249 contract YAML schema parity tested against zod schemas — column names match, ENUM values match, required fields match
    - Test 8: architecture-lock RE-RUN — scanForbiddenPatterns({ paths: ['lib/markos/saas/mrr/', 'api/cron/saas-mrr-snapshot.js'] }) returns []
  </behavior>
  <read_first>
    - 217-RESEARCH.md §Domain 2 (snapshot-builder cron pattern lines 565-580)
    - 217-RESEARCH.md §Domain 2 API surface table (F-248, F-249 endpoints)
    - 217-01-PLAN.md (lib/markos/saas/revenue/source-precedence.cjs — Plan 02 imports)
    - 217-01-PLAN.md (lib/markos/saas/preflight/index.cjs — architecture-lock RE-RUN imports)
    - api/cron/mcp-kpi-digest.js (cron auth + writeJson + response shape pattern)
    - api/cron/webhooks-dlq-purge.js (createClient pattern)
    - lib/markos/crm/agent-actions.ts:68 (buildApprovalPackage import)
    - .planning/phases/218-saas-growth-profile-plg-inapp-experimentation/218-02-PLAN.md (downstream snapshot consumer pattern)
    - obsidian/brain/SaaS Suite Canon.md (cohort retention spec)
  </read_first>
  <action>
    Step 1: Write all 8 tests in test/saas-217/domain-2/{snapshot-builder, nightly-cron, cohort-retention}.test.js + test/saas-217/preflight/architecture-lock.test.js (RE-RUN section) BEFORE implementation (TDD RED).
    Step 2: Implement lib/markos/saas/mrr/waterfall-entries.ts:
      - classifyMovement({ event, prevState, currentState }) — pure function returning movement_type string
      - deriveWaterfallEntries({ tenantId, periodStart, periodEnd, billingEvents }) — array transform; handles partial-period proration per RESEARCH formula edge cases
      - CommonJS twin
    Step 3: Implement lib/markos/saas/mrr/snapshot-builder.ts:
      - buildSnapshotForTenant({ tenantId, snapshotDate, snapshotType }, supabase):
        - Read previous reconciled snapshot for tenant (mrr_start = previous.mrr_end OR 0 if none)
        - Read tenant_billing_subscriptions + billing_usage_events for period
        - Call deriveWaterfallEntries → entries[]
        - Sum entries by movement_type → new/expansion/contraction/churn/reactivation_mrr
        - Compute source_billing_hash (SHA-256 of billing state) + source_processor_hash
        - For each metric, call resolveSourceConflict({ sources: { billing: x, processor: y, accounting: z } })
        - reconciliation_state = aggregate worst case (any 'conflict_flagged' → flag)
        - INSERT saas_mrr_snapshots (DB-trigger validates reconciliation_state non-null + idempotency)
        - INSERT saas_mrr_waterfall_entries[]
        - On conflict_flagged → buildApprovalPackage({ kind: 'mrr_reconciliation_conflict', tenant_id, snapshot_id, divergence_pct, sources })
        - Return snapshot_id
      - buildSnapshotForAllActiveTenants({ snapshotDate, snapshotType }, supabase) — iterates active SaaS tenants
      - CommonJS twin
    Step 4: Implement lib/markos/saas/mrr/cohort-retention.ts:
      - getAcquisitionCohorts({ tenantId, asOfDate }, supabase) — groups subscriptions by start month
      - computeCohortRetention({ tenantId, asOfDate, cohortPeriods }) — for each cohort, compute MRR_at_acquisition + MRR_at_T+1/3/6/12; return retention pct table
      - CommonJS twin
    Step 5: Implement lib/markos/saas/mrr/index.cjs as CommonJS barrel.
    Step 6: Implement api/cron/saas-mrr-snapshot.js (legacy api/*.js pattern):
      ```js
      'use strict';
      const { createClient } = require('@supabase/supabase-js');
      const { buildSnapshotForAllActiveTenants } = require('../../lib/markos/saas/mrr/snapshot-builder.cjs');
      function writeJson(res, statusCode, payload) { res.statusCode = statusCode; res.setHeader('Content-Type', 'application/json'); res.end(JSON.stringify(payload)); }
      module.exports = async function handler(req, res) {
        if (req.method !== 'POST') return writeJson(res, 405, { error: 'METHOD_NOT_ALLOWED' });
        if (req.headers['x-markos-cron-secret'] !== process.env.MARKOS_SAAS_CRON_SECRET) return writeJson(res, 401, { error: 'UNAUTHORIZED' });
        const start = Date.now();
        const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY);
        const snapshotIds = await buildSnapshotForAllActiveTenants({ snapshotDate: new Date().toISOString().slice(0,10), snapshotType: 'nightly' }, supabase);
        writeJson(res, 200, { success: true, tenant_count: snapshotIds.length, snapshot_ids: snapshotIds, duration_ms: Date.now() - start });
      };
      ```
    Step 7: Write contracts/F-248-saas-mrr-snapshots-v1.yaml — defines GET /v1/saas/metrics/mrr (latest snapshot for tenant) + GET /v1/saas/metrics/waterfall (snapshot+entries by period); response schemas match SaaSMRRSnapshotSchema + MRRWaterfallEntrySchema.
    Step 8: Write contracts/F-249-saas-mrr-waterfall-entries-v1.yaml — defines GET /v1/saas/metrics/cohorts (cohort retention); response includes per-cohort retention pcts.
    Step 9: Append F-248 + F-249 to contracts/flow-registry.json.
    Step 10: Run all 8 tests (TDD GREEN). Update 217-VALIDATION.md per-task verification map for 217-02.
    Step 11: Commit: `feat(217-02): saas-mrr snapshot-builder + cohort-retention + cron + F-248 + F-249 (per V4.1.0-MIGRATION-SLOT-COORDINATION.md)`.
  </action>
  <acceptance_criteria>
    - `npm test -- test/saas-217/domain-2/snapshot-builder.test.js` passes (mrr_start propagation + waterfall classification + conflict_flagged → buildApprovalPackage spy invocation green)
    - `npm test -- test/saas-217/domain-2/nightly-cron.test.js` passes (auth + 0-tenant + 1-tenant green)
    - `npm test -- test/saas-217/domain-2/cohort-retention.test.js` passes
    - `node -e "const { buildSnapshotForTenant } = require('./lib/markos/saas/mrr/snapshot-builder.cjs'); console.log(typeof buildSnapshotForTenant)"` outputs `function`
    - `ls api/cron/saas-mrr-snapshot.js` exists
    - `ls contracts/F-248-saas-mrr-snapshots-v1.yaml contracts/F-249-saas-mrr-waterfall-entries-v1.yaml` both exist
    - `grep -E "F-248|F-249" contracts/flow-registry.json | wc -l` returns at least 2
    - `node -e "const { scanForbiddenPatterns } = require('./lib/markos/saas/preflight/index.cjs'); console.log(scanForbiddenPatterns({ paths: ['lib/markos/saas/mrr/', 'api/cron/saas-mrr-snapshot.js'] }).length)"` outputs `0`
    - `217-VALIDATION.md` Per-Task Verification Map row for 217-02 marked status=green
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron → DB | api/cron/saas-mrr-snapshot.js uses service-role key; reads P215 + writes saas_mrr_snapshots (RLS-bypassing) |
| billing source → snapshot | source-precedence resolves disagreement (untrusted external sources) |
| client → API | F-248/F-249 GET endpoints accept tenant-scoped queries (read-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-217-08 | Spoofing | Unauthorized cron invocation | mitigate | x-markos-cron-secret header matches MARKOS_SAAS_CRON_SECRET env; 401 on mismatch (matches mcp-kpi-digest pattern) |
| T-217-09 | Tampering | reconciliation_state forged at app layer | mitigate | DB-trigger MRR_SNAPSHOT_REQUIRES_RECONCILIATION_STATE blocks NULL; idempotency clause blocks duplicate reconciled rows |
| T-217-10 | Information Disclosure | Cross-tenant snapshot leak via cron | mitigate | snapshot-builder iterates per active SaaS tenant; tenant_id passed explicitly to each insert; RLS policy is defense-in-depth |
| T-217-11 | Tampering | Manipulated billing event injects fake MRR | mitigate | source_billing_hash + source_processor_hash store SHA-256 of source state; mismatch surfaces conflict_flagged → operator approval gate |
| T-217-12 | Repudiation | Operator changes reconciled snapshot | mitigate | idempotency clause prevents UPDATE on reconciled rows; manual override requires reconciliation_state='manually_corrected' which sets reconciled_by + audit row |
| T-217-13 | Denial of Service | Cron timeout from large tenant count | mitigate | buildSnapshotForAllActiveTenants iterates with bounded concurrency; duration_ms in response surfaces slow runs |
| T-217-14 | Elevation of Privilege | Snapshot access without manage_saas role | mitigate | Plan 04 F-248/F-249 handlers will require requireHostedSupabaseAuth(req, ctx, 'saas_metrics_read'); tenant_id from auth, not query |
</threat_model>

<verification>
- Migration 98 (Plan 02 portion) APPENDED cleanly; 2 tables + 1 trigger + 2 RLS policies + 4 indexes added
- saas_mrr_snapshots column contract matches RESEARCH §Domain 2 SQL exactly (P218+P219 columns verified)
- MRR_SNAPSHOT_REQUIRES_RECONCILIATION_STATE trigger raises exact exception name verbatim
- snapshot-builder integration test green (mrr_start + waterfall + conflict path + buildApprovalPackage)
- Nightly cron handler auth green; 0-tenant + 1-tenant paths green
- cohort-retention test green
- F-248 + F-249 contracts written + flow-registry.json updated
- Architecture-lock RE-RUN scan returns 0 violations across lib/markos/saas/mrr/ + api/cron/saas-mrr-snapshot.js
- 217-VALIDATION.md updated with Plan 02 task rows
</verification>

<success_criteria>
- saas_mrr_snapshots ships with P218+P219 locked column contract intact
- DB-trigger MRR_SNAPSHOT_REQUIRES_RECONCILIATION_STATE enforces both NULL bypass + idempotency
- Nightly cron operational with x-markos-cron-secret auth
- Source-precedence integration with Plan 01 helper produces correct reconciliation_state per snapshot
- conflict_flagged path creates P1 approval task via buildApprovalPackage
- Cohort retention helper computes per-cohort T+1/3/6/12 retention
- All 2 task acceptance criteria sets pass
</success_criteria>

<output>
After completion, create `.planning/phases/217-saas-suite-revenue-agents-api-ui/217-02-SUMMARY.md`
</output>
