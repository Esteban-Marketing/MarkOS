---
phase: 217
plan: "03"
type: execute
wave: 2
depends_on: ["01"]
f_ids:
  - F-250
  - F-251
migration_slots:
  - 99
integrates_with:
  - "P207 markos_agent_runs (SOFT — agents emit AgentRun once activated)"
  - "P208 agent_approval_packages (READ via activation_approval_id FK)"
  - "P209 evidence_map_records (SOFT — readiness_check_id FK)"
  - "P214 saas_suite_activations (READ for tenant seed eligibility)"
files_modified:
  - supabase/migrations/99_sas_agent_readiness.sql
  - lib/markos/saas/sas-agents/contracts.ts
  - lib/markos/saas/sas-agents/contracts.cjs
  - lib/markos/saas/sas-agents/registry.ts
  - lib/markos/saas/sas-agents/registry.cjs
  - lib/markos/saas/sas-agents/readiness-check.ts
  - lib/markos/saas/sas-agents/readiness-check.cjs
  - lib/markos/saas/sas-agents/index.cjs
  - contracts/F-250-saas-sas-agent-readiness-v1.yaml
  - contracts/F-251-saas-sas-agent-activation-v1.yaml
  - contracts/flow-registry.json
  - test/saas-217/domain-3/sas-agent-readiness.test.js
  - test/saas-217/domain-3/agent-seed.test.js
  - test/saas-217/domain-3/readiness-check.test.js
  - test/saas-217/domain-3/sas-agent-activation-trigger.test.js
  - test/saas-217/rls/sas-agent-readiness-cross-tenant-denial.test.js
  - .planning/phases/217-saas-suite-revenue-agents-api-ui/217-VALIDATION.md
autonomous: true
requirements:
  - SAS-09
  - SAS-10
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - QA-05
  - QA-06
  - QA-07
  - QA-08
  - QA-09
must_haves:
  truths:
    - "sas_agent_readiness ships in standalone migration 99 (slot 99 — distinct from Plan 01+02+06 slot 98 because table has independent FK chain to markos_orgs only; no schema coupling with revenue/MRR/nav tables)"
    - "Table is named `sas_agent_readiness` (SAS prefix; P217-OWNED) — DISTINCT from P218 `plg_growth_agent_readiness`, P219 `b2b_growth_agent_readiness`, P220 `growth_agent_readiness` (Q-2 RESOLVED — no naming collision)"
    - "Plan 03 frontmatter `requirements:` = SAS-09, SAS-10, QA-01..09 (does NOT include SG-10 — Q-4 RESOLVED; SG-10 maps to P218/P219/P220 P06 growth-agent registries, NOT P217 SAS registry)"
    - "Schema includes 8-flag readiness gates (mirrors P218/P219/P220 P06 pattern): has_stable_token, has_role_definition, has_input_contract, has_output_contract, has_cost_posture, has_approval_posture, has_failure_behavior, has_ui_api_mcp_surface — all default false"
    - "Derived `runnable` boolean column GENERATED ALWAYS AS (AND of 8 flags) STORED — cannot be set directly by app code; structurally enforced as derivation"
    - "agent_token CHECK constraint enumerates exactly 6 tokens: 'MARKOS-AGT-SAS-01' (Subscription Lifecycle Manager) through 'MARKOS-AGT-SAS-06' (Expansion Revenue Scout)"
    - "DB-trigger compliance — `SAS_AGENT_ACTIVATION_REQUIRES_READINESS` BEFORE INSERT OR UPDATE on sas_agent_readiness raises EXCEPTION when all 8 readiness flags are true AND activation_approval_id IS NULL (per RESEARCH §Compliance Enforcement Boundary Plan 03 row — exact exception name verbatim; mirrors P226 W1 pattern of approval-gated activation)"
    - "Migration ships seed data — for each active SaaS tenant in saas_suite_activations status='active', INSERT 6 rows (one per SAS agent token) with all 8 readiness flags = false → runnable GENERATED = false; uses DO-block reading saas_suite_activations and INSERTing per agent_token; idempotent via UNIQUE(tenant_id, agent_token)"
    - "registry.ts exports getAgentReadiness({ tenantId, agentToken }, supabase) + listAgentsForTenant({ tenantId }, supabase) + countRunnableAgents({ tenantId }, supabase) — RLS-honoring; returns array of agent rows including runnable boolean"
    - "readiness-check.ts exports markReadinessFlag({ tenantId, agentToken, flagName }, supabase) for individual flag updates (operator-driven; each flag set requires evidence) + activateAgent({ tenantId, agentToken, approvalId }, supabase) which sets activation_approval_id (DB-trigger validates all 8 flags true at this point)"
    - "Activation flow — operator marks 8 flags true individually (each flag update is an UPDATE without approval needed); only the FINAL state where all 8 are true REQUIRES activation_approval_id be set; activateAgent calls buildApprovalPackage({ kind: 'sas_agent_activation', tenant_id, agent_token, readiness_evidence }) → operator approves → approval_id passed back to activateAgent → UPDATE sets activation_approval_id"
    - "2 contracts ship F-250 (saas-sas-agent-readiness-v1) covering GET /v1/saas/agents (list) + GET /v1/saas/agents/:token (detail); F-251 (saas-sas-agent-activation-v1) covering POST /v1/saas/agents/:token/activate (request approval package); NO collision against F-001..F-249"
    - "Cross-tenant denial test asserts INSERT sas_agent_readiness for tenant A; SELECT under tenant B returns 0 rows; tenant B cannot UPDATE tenant A's flags"
    - "Plan 03 architecture-lock RE-RUN — runs scanForbiddenPatterns across lib/markos/saas/sas-agents/ + contracts/F-250* + contracts/F-251*; assertion: 0 violations"
    - "All 6 agent files verified on disk: .agent/markos/agents/markos-saas-{billing-compliance-agent,churn-risk-assessor,expansion-revenue-scout,revenue-intelligence-analyst,subscription-lifecycle-manager,support-intelligence-agent}.md (status: planned in frontmatter — P217 registry references these as documentation-only)"
    - "217-VALIDATION.md per-task verification map updated with rows for Plan 03 tasks"
  artifacts:
    - path: supabase/migrations/99_sas_agent_readiness.sql
      provides: standalone migration — sas_agent_readiness table + SAS_AGENT_ACTIVATION_REQUIRES_READINESS trigger + RLS + tenant_id/agent_token indexes + seed DO-block (6 rows per active SaaS tenant)
    - path: lib/markos/saas/sas-agents/contracts.ts
      provides: zod schemas + enum constants — SAS_AGENT_TOKENS = ['MARKOS-AGT-SAS-01',..,'MARKOS-AGT-SAS-06'] as const; READINESS_FLAGS = ['has_stable_token','has_role_definition','has_input_contract','has_output_contract','has_cost_posture','has_approval_posture','has_failure_behavior','has_ui_api_mcp_surface'] as const; SASAgentReadinessSchema zod
      exports: [SAS_AGENT_TOKENS, READINESS_FLAGS, SASAgentReadinessSchema, AGENT_TOKEN_TO_NAME_MAP]
    - path: lib/markos/saas/sas-agents/registry.ts
      provides: getAgentReadiness + listAgentsForTenant + countRunnableAgents + getAgentByToken read helpers; honors RLS via runtimeContext.tenant_id
      exports: [getAgentReadiness, listAgentsForTenant, countRunnableAgents, getAgentByToken]
    - path: lib/markos/saas/sas-agents/readiness-check.ts
      provides: markReadinessFlag (per-flag UPDATE) + activateAgent (sets activation_approval_id; DB-trigger validates) + getMissingFlags (returns array of flag names where value=false)
      exports: [markReadinessFlag, activateAgent, getMissingFlags]
    - path: lib/markos/saas/sas-agents/index.cjs
      provides: CommonJS twin re-exporting full surface
    - path: contracts/F-250-saas-sas-agent-readiness-v1.yaml
      provides: GET /v1/saas/agents (list with readiness flags) + GET /v1/saas/agents/:token (single agent detail); response schema mirrors SASAgentReadinessSchema
    - path: contracts/F-251-saas-sas-agent-activation-v1.yaml
      provides: POST /v1/saas/agents/:token/activate (request activation; body=readiness_evidence; calls buildApprovalPackage; returns approval_id); requires manage_saas role
    - path: contracts/flow-registry.json
      provides: 2 entries appended for F-250 + F-251
    - path: test/saas-217/domain-3/sas-agent-readiness.test.js
      provides: schema-presence test (8 flag columns + GENERATED runnable + UNIQUE(tenant_id, agent_token) + agent_token CHECK 6 values)
    - path: test/saas-217/domain-3/agent-seed.test.js
      provides: integration test asserting after migration, for a tenant with saas_suite_activations status='active', exactly 6 rows exist in sas_agent_readiness for that tenant; all 8 flags = false; runnable GENERATED = false
    - path: test/saas-217/domain-3/sas-agent-activation-trigger.test.js
      provides: Postgres test — UPDATE setting all 8 flags=true with activation_approval_id=NULL → expect EXCEPTION 'SAS_AGENT_ACTIVATION_REQUIRES_READINESS'; with activation_approval_id set → succeeds; setting 7 of 8 flags true (any combination) → succeeds (no trigger fire); INSERT row with 8 flags true + activation_approval_id NULL → exception
    - path: test/saas-217/domain-3/readiness-check.test.js
      provides: integration test — markReadinessFlag updates single flag; getMissingFlags returns correct list; activateAgent calls buildApprovalPackage spy then UPDATE
  key_links:
    - from: lib/markos/saas/sas-agents/readiness-check.ts
      to: lib/markos/crm/agent-actions.ts
      via: imports buildApprovalPackage; activateAgent calls with kind='sas_agent_activation'
      pattern: "buildApprovalPackage"
    - from: lib/markos/saas/sas-agents/readiness-check.ts
      to: supabase/migrations/99_sas_agent_readiness.sql
      via: app-layer activateAgent UPDATE; DB-trigger trg_sas_agent_activation_readiness validates all-flags-true requires activation_approval_id (defense-in-depth)
      pattern: "SAS_AGENT_ACTIVATION_REQUIRES_READINESS"
    - from: contracts/F-250-saas-sas-agent-readiness-v1.yaml
      to: lib/markos/saas/sas-agents/contracts.ts
      via: SASAgentReadinessSchema zod parity-tested against F-250 YAML
      pattern: "SASAgentReadinessSchema"
    - from: lib/markos/saas/sas-agents/registry.ts
      to: supabase/migrations/99_sas_agent_readiness.sql
      via: SELECT queries with RLS; agent_token IN clause uses SAS_AGENT_TOKENS enum from contracts.ts
      pattern: "sas_agent_readiness"
---

<objective>
Ship the SAS agent readiness registry that enforces SAS-10 (no SAS agent without stable token, role, inputs, outputs, cost, approval posture, and failure behavior). Plan 03 owns slot 99 (standalone migration; distinct from Plans 01+02+06 slot 98 because the FK chain references only markos_orgs and is schema-decoupled from revenue tables).

Plan 03 ships: sas_agent_readiness table (RLS-tenant-scoped; UNIQUE per tenant+agent_token; CHECK constraint enumerates 6 SAS agent tokens; 8-flag readiness gates; runnable GENERATED ALWAYS column derived from AND of 8 flags), SAS_AGENT_ACTIVATION_REQUIRES_READINESS DB-trigger (verbatim per RESEARCH; mirrors P226 W1 approval-gated activation pattern — runnable=true requires activation_approval_id non-null), seed data (6 rows per active SaaS tenant — one per SAS-01..06 with all 8 flags = false → runnable=false), registry + readiness-check libs, 2 F-ID contracts (F-250 read surface, F-251 activation surface).

Frontmatter `requirements:` = SAS-09, SAS-10, QA-01..09 — does NOT include SG-10 (Q-4 RESOLVED — SG-10 maps to P218/P219/P220 P06 growth-agent registries, not P217 SAS registry). Table name `sas_agent_readiness` is DISTINCT from P218 `plg_growth_agent_readiness` / P219 `b2b_growth_agent_readiness` / P220 `growth_agent_readiness` (Q-2 RESOLVED).

Purpose: SAS-10 (SAS agents tenant-scoped, activation-gated, auditable, cost-visible, contract-ID fresh). Plan 03 is the gate that prevents premature SAS agent execution — the GENERATED runnable column + DB-trigger combination structurally blocks any code path from setting an agent runnable without operator approval. Document-only agent files (.agent/markos/agents/markos-saas-*.md status: planned) remain registry/documentation artifacts; the database is the truth source.

Output: 1 standalone migration (99) + 7 sas-agent lib files + 2 F-ID contracts + flow-registry update + 4 domain-3 test files + 1 RLS test file + populated VALIDATION.md.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/217-saas-suite-revenue-agents-api-ui/217-CONTEXT.md
@.planning/phases/217-saas-suite-revenue-agents-api-ui/217-RESEARCH.md
@.planning/phases/217-saas-suite-revenue-agents-api-ui/217-VALIDATION.md
@.planning/phases/217-saas-suite-revenue-agents-api-ui/217-01-PLAN.md
@.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md
@.planning/phases/218-saas-growth-profile-plg-inapp-experimentation/218-06-PLAN.md
@.planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-06-PLAN.md
@.planning/phases/226-sales-enablement-deal-execution/226-01-PLAN.md
@obsidian/brain/SaaS Suite Canon.md
@.agent/markos/agents/markos-saas-billing-compliance-agent.md
@.agent/markos/agents/markos-saas-churn-risk-assessor.md
@.agent/markos/agents/markos-saas-expansion-revenue-scout.md
@.agent/markos/agents/markos-saas-revenue-intelligence-analyst.md
@.agent/markos/agents/markos-saas-subscription-lifecycle-manager.md
@.agent/markos/agents/markos-saas-support-intelligence-agent.md
@CLAUDE.md
@lib/markos/saas/preflight/index.cjs
@lib/markos/crm/agent-actions.ts

<interfaces>
<!-- From Plan 01 (already in Wave 1): -->

From lib/markos/saas/preflight/index.cjs:
```javascript
module.exports = { assertUpstreamReady, scanForbiddenPatterns };
```

From lib/markos/crm/agent-actions.ts:68:
```typescript
function buildApprovalPackage({ kind, tenant_id, ...payload }) → { approval_id, ... }
```

SAS Agent Definitions (from RESEARCH §Domain 3 + agent files verified on disk):
| Token | Agent Name | Domain |
|-------|------------|--------|
| MARKOS-AGT-SAS-01 | Subscription Lifecycle Manager | SAS |
| MARKOS-AGT-SAS-02 | Revenue Intelligence Analyst | SAS |
| MARKOS-AGT-SAS-03 | Billing Compliance Agent | SAS |
| MARKOS-AGT-SAS-04 | Churn Risk Assessor | SAS |
| MARKOS-AGT-SAS-05 | Support Intelligence Agent | SAS |
| MARKOS-AGT-SAS-06 | Expansion Revenue Scout | SAS |

8 readiness flags (per RESEARCH §Domain 3 schema):
- has_stable_token, has_role_definition, has_input_contract, has_output_contract
- has_cost_posture, has_approval_posture, has_failure_behavior, has_ui_api_mcp_surface

GENERATED column:
runnable boolean GENERATED ALWAYS AS (AND of 8 flags) STORED
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sas_agent_readiness migration (slot 99) + SAS_AGENT_ACTIVATION_REQUIRES_READINESS trigger + 6-row per-tenant seed + RLS</name>
  <files>supabase/migrations/99_sas_agent_readiness.sql, lib/markos/saas/sas-agents/contracts.ts, lib/markos/saas/sas-agents/contracts.cjs, test/saas-217/domain-3/sas-agent-readiness.test.js, test/saas-217/domain-3/agent-seed.test.js, test/saas-217/domain-3/sas-agent-activation-trigger.test.js, test/saas-217/rls/sas-agent-readiness-cross-tenant-denial.test.js</files>
  <behavior>
    - Test 1: After migration 99, sas_agent_readiness has columns (agent_id PK + tenant_id FK markos_orgs + agent_token CHECK 6 values + agent_name + agent_domain default 'SAS' + 8 readiness boolean flags default false + runnable boolean GENERATED ALWAYS AS (AND of 8 flags) STORED + activation_approval_id uuid + readiness_check_id uuid + readiness_notes + approved_at + approved_by + created_at + updated_at + UNIQUE(tenant_id, agent_token)); RLS enabled; idx on (tenant_id, agent_token) + (tenant_id, runnable)
    - Test 2: GENERATED runnable column — INSERT row with all 8 flags=false → runnable=false; flip 1 flag to true → runnable still false; flip all 8 to true → runnable=true (provided activation_approval_id is set, otherwise trigger fires)
    - Test 3: agent_token CHECK enforces exactly 6 values — INSERT 'MARKOS-AGT-SAS-07' raises check_violation
    - Test 4: DB-trigger SAS_AGENT_ACTIVATION_REQUIRES_READINESS — UPDATE setting all 8 flags=true with activation_approval_id=NULL → expect EXCEPTION 'SAS_AGENT_ACTIVATION_REQUIRES_READINESS: agent=MARKOS-AGT-SAS-XX cannot be fully ready without activation_approval_id'; with activation_approval_id set → succeeds; INSERT row with 8 flags true + NULL approval_id → exception
    - Test 5: Seed correctness — for a markos_orgs row with corresponding saas_suite_activations status='active', after migration 99 applies, sas_agent_readiness has exactly 6 rows for that tenant_id (one per SAS-01..06), all 8 flags = false, runnable = false
    - Test 6: For a tenant with saas_suite_activations status='inactive' or no activation row, sas_agent_readiness has 0 rows for that tenant
    - Test 7: Cross-tenant RLS denial — INSERT under tenant A; SELECT under tenant B returns 0 rows; UPDATE under tenant B affects 0 rows
    - Test 8: SASAgentReadinessSchema zod validates row shape; rejects extra fields; AGENT_TOKEN_TO_NAME_MAP exports 6 entries
  </behavior>
  <read_first>
    - 217-RESEARCH.md §Domain 3 (read SQL block lines 628-679 carefully — sas_agent_readiness column types + GENERATED runnable + CHECK + RLS + indexes)
    - 217-RESEARCH.md §Compliance Enforcement Boundary (Plan 03 row — SAS_AGENT_ACTIVATION_REQUIRES_READINESS exact exception name)
    - 217-RESEARCH.md §Domain 3 trigger SQL (lines 681-702)
    - 217-RESEARCH.md §Domain 3 SAS Agent Definitions table (lines 615-622) for 6 token + name mapping
    - .agent/markos/agents/markos-saas-{6 files}.md — verify all 6 exist with status:planned in frontmatter
    - .planning/phases/218-saas-growth-profile-plg-inapp-experimentation/218-06-PLAN.md (P218 plg_growth_agent_readiness — same readiness pattern; verify naming non-collision)
    - .planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-06-PLAN.md (P220 growth_agent_readiness — same readiness pattern)
    - .planning/phases/226-sales-enablement-deal-execution/226-01-PLAN.md (P226 W1 approval-gated activation pattern — mirror for SAS_AGENT_ACTIVATION)
    - .planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md (P217-03 owns standalone slot 99)
    - supabase/migrations/100_crm_schema_identity_graph_hardening.sql (recent migration patterns)
  </read_first>
  <action>
    Step 1: Write the 8 tests BEFORE migration lands (TDD RED).
    Step 2: Write supabase/migrations/99_sas_agent_readiness.sql per RESEARCH §Domain 3 SQL block. Include:
      - Comment header: `-- Plan 217-03 / Phase 217 / Slot 99 / RESEARCH §Domain 3 / SAS_AGENT_ACTIVATION_REQUIRES_READINESS`
      - CREATE TABLE sas_agent_readiness:
        - agent_id UUID PK DEFAULT gen_random_uuid()
        - tenant_id UUID NOT NULL REFERENCES markos_orgs(org_id)
        - agent_token text NOT NULL CHECK (agent_token IN ('MARKOS-AGT-SAS-01','MARKOS-AGT-SAS-02','MARKOS-AGT-SAS-03','MARKOS-AGT-SAS-04','MARKOS-AGT-SAS-05','MARKOS-AGT-SAS-06'))
        - agent_name text NOT NULL
        - agent_domain text NOT NULL DEFAULT 'SAS'
        - has_stable_token boolean NOT NULL DEFAULT false
        - has_role_definition boolean NOT NULL DEFAULT false
        - has_input_contract boolean NOT NULL DEFAULT false
        - has_output_contract boolean NOT NULL DEFAULT false
        - has_cost_posture boolean NOT NULL DEFAULT false
        - has_approval_posture boolean NOT NULL DEFAULT false
        - has_failure_behavior boolean NOT NULL DEFAULT false
        - has_ui_api_mcp_surface boolean NOT NULL DEFAULT false
        - runnable boolean GENERATED ALWAYS AS (has_stable_token AND has_role_definition AND has_input_contract AND has_output_contract AND has_cost_posture AND has_approval_posture AND has_failure_behavior AND has_ui_api_mcp_surface) STORED
        - activation_approval_id uuid
        - readiness_check_id uuid
        - readiness_notes text
        - approved_at timestamptz
        - approved_by uuid
        - created_at timestamptz NOT NULL DEFAULT now()
        - updated_at timestamptz NOT NULL DEFAULT now()
        - UNIQUE(tenant_id, agent_token)
      - ALTER TABLE sas_agent_readiness ENABLE ROW LEVEL SECURITY;
      - CREATE POLICY sar_tenant_isolation ON sas_agent_readiness FOR ALL USING (tenant_id = current_setting('app.tenant_id', true)::uuid);
      - CREATE INDEX idx_sar_tenant_token ON sas_agent_readiness(tenant_id, agent_token);
      - CREATE INDEX idx_sar_runnable ON sas_agent_readiness(tenant_id, runnable);
      - CREATE OR REPLACE FUNCTION enforce_sas_agent_activation_readiness() RETURNS trigger LANGUAGE plpgsql AS $$ BEGIN IF (NEW.has_stable_token AND NEW.has_role_definition AND NEW.has_input_contract AND NEW.has_output_contract AND NEW.has_cost_posture AND NEW.has_approval_posture AND NEW.has_failure_behavior AND NEW.has_ui_api_mcp_surface) AND NEW.activation_approval_id IS NULL THEN RAISE EXCEPTION 'SAS_AGENT_ACTIVATION_REQUIRES_READINESS: agent=% cannot be fully ready without activation_approval_id', NEW.agent_token; END IF; RETURN NEW; END $$;
      - CREATE TRIGGER trg_sas_agent_activation_readiness BEFORE INSERT OR UPDATE ON sas_agent_readiness FOR EACH ROW EXECUTE FUNCTION enforce_sas_agent_activation_readiness();
      - DO-block to seed 6 rows per active SaaS tenant:
        ```sql
        DO $$
        DECLARE t_id uuid; agent_record record; agent_map text[][] := ARRAY[
          ['MARKOS-AGT-SAS-01','Subscription Lifecycle Manager'],
          ['MARKOS-AGT-SAS-02','Revenue Intelligence Analyst'],
          ['MARKOS-AGT-SAS-03','Billing Compliance Agent'],
          ['MARKOS-AGT-SAS-04','Churn Risk Assessor'],
          ['MARKOS-AGT-SAS-05','Support Intelligence Agent'],
          ['MARKOS-AGT-SAS-06','Expansion Revenue Scout']
        ];
        BEGIN
          FOR t_id IN SELECT tenant_id FROM saas_suite_activations WHERE status = 'active' LOOP
            FOR i IN 1..6 LOOP
              INSERT INTO sas_agent_readiness (tenant_id, agent_token, agent_name)
              VALUES (t_id, agent_map[i][1], agent_map[i][2])
              ON CONFLICT (tenant_id, agent_token) DO NOTHING;
            END LOOP;
          END LOOP;
        END $$;
        ```
      - DO $$ BEGIN ASSERT (SELECT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'sas_agent_readiness')); ASSERT (SELECT count(*) FROM pg_trigger WHERE tgname = 'trg_sas_agent_activation_readiness') = 1; END $$;
    Step 3: Implement lib/markos/saas/sas-agents/contracts.ts:
      - SAS_AGENT_TOKENS = ['MARKOS-AGT-SAS-01',..,'MARKOS-AGT-SAS-06'] as const
      - READINESS_FLAGS = [8 flag names] as const
      - AGENT_TOKEN_TO_NAME_MAP = { 'MARKOS-AGT-SAS-01': 'Subscription Lifecycle Manager', ... }
      - SASAgentReadinessSchema zod (mirrors DB columns)
      - CommonJS twin in contracts.cjs
    Step 4: Apply migration; run all 8 tests (TDD GREEN).
    Step 5: Commit: `db(217-03): sas_agent_readiness + SAS_AGENT_ACTIVATION_REQUIRES_READINESS trigger + 6-agent seed / slot 99 (per V4.1.0-MIGRATION-SLOT-COORDINATION.md)`.
  </action>
  <acceptance_criteria>
    - `npm test -- test/saas-217/domain-3/sas-agent-readiness.test.js` passes
    - `npm test -- test/saas-217/domain-3/agent-seed.test.js` passes (6 rows per active SaaS tenant; 0 rows for inactive)
    - `npm test -- test/saas-217/domain-3/sas-agent-activation-trigger.test.js` passes (positive activation + 2 negative cases for SAS_AGENT_ACTIVATION_REQUIRES_READINESS)
    - `npm test -- test/saas-217/rls/sas-agent-readiness-cross-tenant-denial.test.js` passes
    - `psql -c "SELECT count(*) FROM information_schema.tables WHERE table_name = 'sas_agent_readiness'"` returns 1
    - `psql -c "SELECT count(*) FROM pg_trigger WHERE tgname = 'trg_sas_agent_activation_readiness'"` returns 1
    - `psql -c "SELECT count(*) FROM information_schema.columns WHERE table_name='sas_agent_readiness' AND column_name='runnable' AND is_generated='ALWAYS'"` returns 1 (GENERATED column verified)
    - `node -e "const { SAS_AGENT_TOKENS } = require('./lib/markos/saas/sas-agents/contracts.cjs'); console.log(SAS_AGENT_TOKENS.length)"` outputs `6`
    - `ls .agent/markos/agents/markos-saas-{billing-compliance-agent,churn-risk-assessor,expansion-revenue-scout,revenue-intelligence-analyst,subscription-lifecycle-manager,support-intelligence-agent}.md | wc -l` returns 6
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: registry + readiness-check libs + F-250 + F-251 contracts</name>
  <files>lib/markos/saas/sas-agents/registry.ts, lib/markos/saas/sas-agents/registry.cjs, lib/markos/saas/sas-agents/readiness-check.ts, lib/markos/saas/sas-agents/readiness-check.cjs, lib/markos/saas/sas-agents/index.cjs, contracts/F-250-saas-sas-agent-readiness-v1.yaml, contracts/F-251-saas-sas-agent-activation-v1.yaml, contracts/flow-registry.json, test/saas-217/domain-3/readiness-check.test.js, .planning/phases/217-saas-suite-revenue-agents-api-ui/217-VALIDATION.md</files>
  <behavior>
    - Test 1: getAgentReadiness({ tenantId, agentToken: 'MARKOS-AGT-SAS-01' }, supabase) returns single row with all readiness fields
    - Test 2: listAgentsForTenant({ tenantId }, supabase) returns array of 6 rows for active SaaS tenant; 0 rows for inactive tenant
    - Test 3: countRunnableAgents({ tenantId }, supabase) returns 0 immediately after migration (all flags false); after manually setting all 8 flags + activation_approval_id for 1 agent → returns 1
    - Test 4: getMissingFlags({ tenantId, agentToken }, supabase) returns array of 8 flag names when all false; returns empty array when all true
    - Test 5: markReadinessFlag({ tenantId, agentToken, flagName: 'has_stable_token', value: true }, supabase) updates single flag; getMissingFlags shows 7 remaining
    - Test 6: activateAgent({ tenantId, agentToken, approvalId }, supabase) — calls buildApprovalPackage spy with kind='sas_agent_activation' if approvalId not provided; UPDATEs activation_approval_id when approvalId provided; trigger validates all 8 flags true at this point
    - Test 7: F-250 + F-251 contract YAML schema parity tested against zod schemas
    - Test 8: architecture-lock RE-RUN — scanForbiddenPatterns({ paths: ['lib/markos/saas/sas-agents/'] }) returns []
  </behavior>
  <read_first>
    - 217-RESEARCH.md §Domain 3 (API surface table — F-250 + F-251 endpoints)
    - 217-01-PLAN.md (lib/markos/saas/preflight/index.cjs imports)
    - 217-03-PLAN Task 1 output (sas_agent_readiness schema + 8 readiness flags)
    - lib/markos/crm/agent-actions.ts:68 (buildApprovalPackage signature)
    - .planning/phases/218-saas-growth-profile-plg-inapp-experimentation/218-06-PLAN.md (parallel growth-agent registry pattern for reference)
    - contracts/openapi.json (verify schema reference structure)
  </read_first>
  <action>
    Step 1: Write all 8 tests in test/saas-217/domain-3/readiness-check.test.js + test/saas-217/preflight/architecture-lock.test.js (RE-RUN section) BEFORE implementation (TDD RED).
    Step 2: Implement lib/markos/saas/sas-agents/registry.ts:
      - getAgentReadiness({ tenantId, agentToken }, supabase) — SELECT * FROM sas_agent_readiness WHERE tenant_id=$1 AND agent_token=$2; returns single row or null
      - listAgentsForTenant({ tenantId }, supabase) — SELECT * FROM sas_agent_readiness WHERE tenant_id=$1 ORDER BY agent_token
      - countRunnableAgents({ tenantId }, supabase) — SELECT count(*) WHERE tenant_id=$1 AND runnable=true
      - getAgentByToken(token) — pure helper returning name from AGENT_TOKEN_TO_NAME_MAP
      - CommonJS twin
    Step 3: Implement lib/markos/saas/sas-agents/readiness-check.ts:
      - markReadinessFlag({ tenantId, agentToken, flagName, value }, supabase) — UPDATE sas_agent_readiness SET <flagName>=$value WHERE tenant_id=$1 AND agent_token=$2; returns updated row
      - getMissingFlags({ tenantId, agentToken }, supabase) — reads row; returns READINESS_FLAGS.filter(f => row[f] === false)
      - activateAgent({ tenantId, agentToken, approvalId, readinessEvidence }, supabase):
        - If approvalId not provided → call buildApprovalPackage({ kind: 'sas_agent_activation', tenant_id: tenantId, agent_token: agentToken, readiness_evidence: readinessEvidence }) → returns { approval_id }
        - UPDATE sas_agent_readiness SET activation_approval_id=$approvalId, approved_at=now(), approved_by=$user_id WHERE tenant_id AND agent_token
        - DB-trigger validates all 8 flags true at this point; if not, exception fires
      - CommonJS twin
    Step 4: Implement lib/markos/saas/sas-agents/index.cjs as barrel.
    Step 5: Write contracts/F-250-saas-sas-agent-readiness-v1.yaml — defines GET /v1/saas/agents (list, returns array) + GET /v1/saas/agents/:token (single agent detail with readiness flags); response schema mirrors SASAgentReadinessSchema; auth via requireHostedSupabaseAuth(req, ctx, 'saas_agents_read').
    Step 6: Write contracts/F-251-saas-sas-agent-activation-v1.yaml — defines POST /v1/saas/agents/:token/activate (body=readiness_evidence; calls buildApprovalPackage; returns approval_id + agent state); requires manage_saas role.
    Step 7: Append F-250 + F-251 to contracts/flow-registry.json.
    Step 8: Run all 8 tests (TDD GREEN). Update 217-VALIDATION.md per-task verification map for 217-03.
    Step 9: Commit: `feat(217-03): sas-agents registry + readiness-check + F-250 + F-251 (per V4.1.0-MIGRATION-SLOT-COORDINATION.md)`.
  </action>
  <acceptance_criteria>
    - `npm test -- test/saas-217/domain-3/readiness-check.test.js` passes (8 tests green)
    - `node -e "const { activateAgent } = require('./lib/markos/saas/sas-agents/readiness-check.cjs'); console.log(typeof activateAgent)"` outputs `function`
    - `node -e "const { listAgentsForTenant, countRunnableAgents } = require('./lib/markos/saas/sas-agents/index.cjs'); console.log(typeof listAgentsForTenant, typeof countRunnableAgents)"` outputs `function function`
    - `ls contracts/F-250-saas-sas-agent-readiness-v1.yaml contracts/F-251-saas-sas-agent-activation-v1.yaml` both exist
    - `grep -E "F-250|F-251" contracts/flow-registry.json | wc -l` returns at least 2
    - `node -e "const { scanForbiddenPatterns } = require('./lib/markos/saas/preflight/index.cjs'); console.log(scanForbiddenPatterns({ paths: ['lib/markos/saas/sas-agents/'] }).length)"` outputs `0`
    - `217-VALIDATION.md` Per-Task Verification Map row for 217-03 marked status=green
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → DB | activateAgent UPDATE on sas_agent_readiness (RLS enforced; trigger validates approval) |
| client → API | F-250 GET reads; F-251 POST triggers approval package creation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-217-15 | Elevation of Privilege | Direct UPDATE setting runnable=true at app layer | mitigate | runnable is GENERATED ALWAYS column — cannot be set directly; SAS_AGENT_ACTIVATION_REQUIRES_READINESS trigger fires on flag updates that would derive runnable=true without activation_approval_id |
| T-217-16 | Tampering | Skipping approval gate by setting all 8 flags + bypassing buildApprovalPackage | mitigate | DB-trigger requires activation_approval_id IS NOT NULL when all 8 flags true; activation_approval_id FKs to P208 agent_approval_packages (must exist) |
| T-217-17 | Information Disclosure | Cross-tenant agent readiness leak | mitigate | RLS policy sar_tenant_isolation; cross-tenant denial test in test/saas-217/rls/ |
| T-217-18 | Spoofing | Forged activation_approval_id (unsigned UUID) | mitigate | activation_approval_id will FK to P208 agent_approval_packages once that table is verified upstream-ready (Plan 01 assertUpstreamReady ensures P208 landed; FK constraint added in Plan 06 closeout if required) |
| T-217-19 | Repudiation | Operator activates agent without audit trail | mitigate | approved_at/approved_by columns set on activation; future Plan 04 POST handler emits audit log via lib/markos/audit/* |
| T-217-20 | Denial of Service | Mass-INSERT 1000 agent readiness rows | accept | UNIQUE(tenant_id, agent_token) caps at 6 rows per tenant; minimal storage |
</threat_model>

<verification>
- Migration 99 applies cleanly; sas_agent_readiness table + trigger + RLS policy + 2 indexes + seed DO-block created
- 6 rows seeded per active SaaS tenant; runnable=false for all; all 8 flags=false
- SAS_AGENT_ACTIVATION_REQUIRES_READINESS trigger raises exact exception verbatim
- Registry + readiness-check libs functional
- F-250 + F-251 contracts written + flow-registry.json updated
- Cross-tenant RLS denial test green
- Architecture-lock RE-RUN returns 0 violations
- 217-VALIDATION.md updated with Plan 03 task rows
- All 6 .agent/markos/agents/markos-saas-*.md files verified on disk
</verification>

<success_criteria>
- sas_agent_readiness ships with 8-flag GENERATED runnable column
- DB-trigger structurally enforces approval-gated activation
- Naming non-collision verified vs P218/P219/P220 growth-agent tables
- Plan 03 frontmatter requirements correctly excludes SG-10 (Q-4 RESOLVED)
- All 2 task acceptance criteria sets pass
</success_criteria>

<output>
After completion, create `.planning/phases/217-saas-suite-revenue-agents-api-ui/217-03-SUMMARY.md`
</output>
