---
phase: 217
plan: "08"
type: execute
wave: 6
depends_on: ["01", "02", "03", "04", "05", "06", "07"]
f_ids: []
migration_slots: []
integrates_with:
  - "P214 SaaSActivationPanel + SaaSSubscriptionsTable (REUSED verbatim per UI-SPEC §D-15)"
  - "P215 sensitive credential UI binding contract Layer 6 (5 PII constants STRIPE/MP/QB/SIIGO/DIAN)"
  - "P215 billing-correction approval modal pattern (REUSED on save offer activation)"
  - "P216 7-playbook seed catalog (rendered as chip per row on churn page)"
  - "P208 Approval Inbox handoff_kind filter chips (deep-link from sub-pages)"
  - "P208 SurfaceRouteContract surface_family registration (saas_*)"
ui_spec_fold:
  source: 217-UI-SPEC.md
  surfaces:
    - Surface C — app/saas/subscriptions/page.tsx
    - Surface D — app/saas/plans/page.tsx
    - Surface E — app/saas/revenue/page.tsx
    - Surface F — app/saas/revenue/waterfall/page.tsx
    - Surface G — app/saas/churn/page.tsx
    - Surface H — app/saas/invoices/page.tsx
    - Surface I — app/saas/support/page.tsx
    - Surface J — app/saas/agents/page.tsx
  ac_count: 89
  acs_folded:
    - "S-1..S-8 (Subscriptions; 8 ACs)"
    - "P-1..P-6 (Plans; 6 ACs)"
    - "R-1..R-9 (Revenue; 9 ACs)"
    - "W-1..W-10 (Waterfall; 10 ACs)"
    - "C-1..C-11 (Churn; 11 ACs)"
    - "I-1..I-11 (Invoices; 11 ACs)"
    - "SP-1..SP-15 (Support; 15 ACs)"
    - "G-1..G-9 (Agents; 9 ACs)"
    - "X-1..X-10 (Cross-cutting; 10 ACs across all surfaces)"
files_modified:
  - app/saas/subscriptions/page.tsx
  - app/saas/subscriptions/page.module.css
  - app/saas/subscriptions/page.stories.tsx
  - app/saas/plans/page.tsx
  - app/saas/plans/page.module.css
  - app/saas/plans/page.stories.tsx
  - app/saas/revenue/page.tsx
  - app/saas/revenue/page.module.css
  - app/saas/revenue/page.stories.tsx
  - app/saas/revenue/waterfall/page.tsx
  - app/saas/revenue/waterfall/page.module.css
  - app/saas/revenue/waterfall/page.stories.tsx
  - app/saas/revenue/waterfall/_components/MrrWaterfall.tsx
  - app/saas/revenue/waterfall/_components/MrrWaterfall.module.css
  - app/saas/revenue/waterfall/_components/CohortRetentionHeatmap.tsx
  - app/saas/revenue/waterfall/_components/CohortRetentionHeatmap.module.css
  - app/saas/churn/page.tsx
  - app/saas/churn/page.module.css
  - app/saas/churn/page.stories.tsx
  - app/saas/churn/_components/SaveOfferActivationModal.tsx
  - app/saas/churn/_components/SaveOfferActivationModal.module.css
  - app/saas/invoices/page.tsx
  - app/saas/invoices/page.module.css
  - app/saas/invoices/page.stories.tsx
  - app/saas/support/page.tsx
  - app/saas/support/page.module.css
  - app/saas/support/page.stories.tsx
  - app/saas/support/_components/SupportResponseReviewModal.tsx
  - app/saas/support/_components/SupportResponseReviewModal.module.css
  - app/saas/support/_components/ClassifierFilters.tsx
  - app/saas/support/_components/ClassifierFilters.module.css
  - app/saas/agents/page.tsx
  - app/saas/agents/page.module.css
  - app/saas/agents/page.stories.tsx
  - test/saas-217/domain-8/subscriptions-page.test.js
  - test/saas-217/domain-8/plans-page.test.js
  - test/saas-217/domain-8/revenue-page.test.js
  - test/saas-217/domain-8/waterfall-page.test.js
  - test/saas-217/domain-8/churn-page.test.js
  - test/saas-217/domain-8/invoices-page.test.js
  - test/saas-217/domain-8/support-page.test.js
  - test/saas-217/domain-8/agents-page.test.js
  - test/saas-217/domain-8/cross-cutting.test.js
  - test/ui-a11y/217-saas-a11y.test.js
  - .planning/phases/217-saas-suite-revenue-agents-api-ui/217-VALIDATION.md
autonomous: false
requirements:
  - SAS-10
  - QA-09
  - QA-10
  - QA-11
  - QA-12
  - QA-13
  - QA-14
  - QA-15
must_haves:
  truths:
    - "8 sub-route pages ship under `app/saas/{subscriptions,plans,revenue,revenue/waterfall,churn,invoices,support,agents}/page.tsx`; each is a server component fetching from `/v1/saas/*` endpoints (Plan 04); D-21 server/client boundary enforced — interactive subcomponents extract to `_components/*.tsx` with `'use client'`; per UI-SPEC §Surfaces C-J + cross-cutting X-1..X-10"
    - "Subscriptions page (Surface C) REUSES `<SaaSSubscriptionsTable />` from 214 verbatim with 217 metric joins (active count → MRR contribution; lifecycle event count rollups; per-row health score via `<HealthScoreBadge />` + `<RiskBandBadge />`); 8 lifecycle state literals + 16 event type chips render verbatim from 214-RESEARCH §Domain 3; folds S-1..S-8"
    - "Plans page (Surface D) is read-only catalog in P217 (admin editing deferred to P218+); pricing rendered via `<Money fromPricingRecommendation>` XOR `{{MARKOS_PRICING_ENGINE_PENDING}}` sentinel via `<PlaceholderBanner variant=\"billing_placeholder\">`; zero hard-coded dollar/peso literals; zero tier name strings; folds P-1..P-6"
    - "Revenue page (Surface E) renders 15 revenue metrics per 217-01 formula table verbatim; 5-tier source precedence column (Billing > Processor > Accounting > CRM > Manual) verbatim labels; 5 reconciliation_state badges verbatim ([ok] Reconciled / [info] Pending reconciliation / [err] Conflict flagged — review / [warn] Manually corrected / [info] Excluded); cron-stale state (>36h) triggers warning notice; cohort retention table T+1/T+3/T+6/T+12; forecast 3-month linear; tabular-numerals on monetary; folds R-1..R-9"
    - "Waterfall page (Surface F) renders 6-column MRR waterfall (`new` `expansion` `contraction` `churn` `reactivation` `net_new`) with COLOR DISCIPLINE — positive=success, negative=error, net=primary mint (single mint-fill exception); JBM tabular-numerals on monetary; cohort retention heatmap T+1..T+12; hover tooltip server-rendered for keyboard/screen-reader accessibility (D-21 client subcomponent for hover state only); `prefers-reduced-motion` disables hover transitions; folds W-1..W-10"
    - "Churn page (Surface G) renders 4 risk bands via `<RiskBandBadge />` + 5-dim health via `<HealthScoreBadge />` per row; 7-playbook seed catalog from 216-04 rendered as chip per row (engagement_deepening / ebr_scheduling / executive_outreach / champion_re_engagement / billing_recovery / expansion_conversation / account_brief); `<SaveOfferActivationModal />` REUSES 215 billing-correction modal pattern; `<SaveOfferPricingBlock />` (Plan 07) renders Money XOR sentinel; reason capture ≥20 chars + evidence_pack_ref REQUIRED; DB-trigger CHURN_SAVE_OFFER_REQUIRES_PRICING_AND_APPROVAL validates server-side; autonomy ceiling notice with [block] glyph; Approval Inbox deep-link `/operations/approvals?handoff_kind=save_offer_approval&task_ref={intervention_id}`; folds C-1..C-11"
    - "Invoices page (Surface H) — Sensitive Credential UI Binding Layer 6 ENFORCED — `<PIIRedactedField />` (Plan 07) renders `[REDACTED]` for all 5 PII-field-list constants (STRIPE_PII_FIELDS / MP_PII_FIELDS / QB_PII_FIELDS / SIIGO_PII_FIELDS / DIAN_PII_FIELDS) verbatim; `<.c-chip-protocol>` renders `vault_ref` UUID via mint-as-text only; API handler emits `saas_billing_events` row with `event_type == 'credential_view'` per fetch (server-side enforcement at Plan 04); architecture-lock test asserts banned credential field names absent from page source; tabular-numerals on monetary columns; 4 invoice status badges + 5 reconciliation_state badges verbatim; folds I-1..I-11"
    - "Support page (Surface I) — `mobile_priority: 'critical'` per X-8 (CS reps need mobile for floor / off-desk triage); `<ClassifierChipRow />` (Plan 07) renders 5 classifier dimensions verbatim; `<KbGroundingPanel />` (Plan 07) renders top-3 KB sources with relevance_score badges; Approve and send DISABLED when KB confidence below KB_CONFIDENCE_THRESHOLD (0.7) OR autonomy ceiling reached OR banned-lexicon zero-match server-side check fails; banned-lexicon scan runs server-side BEFORE the dispatch UPDATE call via `scripts/marketing-loop/check-banned-lexicon.mjs`; reason capture ≥20 chars enforced on Reject path; PII redaction applied to ticket subject + description fields per 216 §UI Binding Contract 4; safe auto-response toggle deferred to P218+ via `<PlaceholderBanner variant=\"future_phase_218_safe_auto_response_toggle\">`; bottom-sheet modal pattern; 44px touch targets via global `(pointer: coarse)` rule; Approval Inbox deep-link `/operations/approvals?handoff_kind=support_response_approval&task_ref={ticket_id}`; folds SP-1..SP-15"
    - "Agents page (Surface J) — 6 SAS rows always present (verified by translation-gate test); each row renders agent_token + role + inputs + outputs + cost + approval mode + failure class verbatim from 217-03 contract; ALL 6 rows show `runnable=false` in P217 (SAS-10 invariant; closeout test asserts); 4 approval mode badges verbatim from 206 mutation-class doctrine ([info] Auto / [info] Single approval / [warn] Dual approval / [warn] Cooling off); 7 failure class badges verbatim from 207 AgentFailureClass; cost preview uses `<Money fromPricingRecommendation>` XOR sentinel; future SAS targets section renders `<PlaceholderBanner variant=\"future_phase_218_growth_dashboards\">`; folds G-1..G-9"
    - "Cross-cutting X-1..X-10 — Zero hex literals (D-08); zero hard-coded font-size/font-weight/color (D-08); zero hard-coded dollar/peso literals (X-3); zero hard-coded tier name strings (X-4); zero `.c-card--feature` use anywhere (D-13); zero `.c-table` use anywhere (D-14); banned-lexicon zero-match across all 217 surface text content (X-7) — CI assertion `scripts/marketing-loop/check-banned-lexicon.mjs` runs against `app/saas/**` and returns 0 matches; all 9 surfaces register `mobile_priority` in `lib/markos/operator/shell.ts` SurfaceRouteContract with `surface_family: saas_*` (8 secondary + 1 critical); `desktop_only` is FORBIDDEN (X-8); all 9 surfaces register CSF3 named-state Storybook stories with ≥3 stories each (X-9); accessibility tests under `test/ui-a11y/217-saas-a11y.test.js` mirroring 213.2/213.4 chrome-a11y patterns (X-10)"
    - "5 manual-only verifications consolidated into checkpoint:human-verify task (this plan): mobile breakpoint at iPhone 14 Pro for `/saas/support` critical priority + Chromatic baselines for 10+7 component stories + axe a11y across 9 surfaces + activation-gate sanity (4 named reasons rendered correctly) + sensitive credential Layer 6 manual sanity (PII redaction visible + clipboard block confirmed)"
    - "217-VALIDATION.md per-task verification map updated with rows for Plan 08 tasks (8 sub-page test rows + cross-cutting + a11y + checkpoint) and final UI-SPEC AC Coverage Map enumerating all 121 ACs (99 surface + 12 backend + 10 cross-cutting); 213.4 carry D-08..D-15 enforcement table; 7 NEW extracted components inheritance map for P218+"
  artifacts:
    - path: app/saas/subscriptions/page.tsx
      provides: server component reusing <SaaSSubscriptionsTable /> from 214 + 217 metric joins; 8 lifecycle state badges + 16 event type chips
    - path: app/saas/plans/page.tsx
      provides: server component plan catalog read-only; vanilla `<table>`; <Money fromPricingRecommendation> XOR sentinel
    - path: app/saas/revenue/page.tsx
      provides: server component 15 metrics + 5-tier source precedence + 5 reconciliation_state badges + cohort retention + forecast + reconciliation queue link
    - path: app/saas/revenue/waterfall/page.tsx
      provides: server component 6-column MRR waterfall (color discipline); cohort retention heatmap T+1..T+12; tabular-numerals
    - path: app/saas/revenue/waterfall/_components/MrrWaterfall.tsx
      provides: '`use client`' SVG bar chart; hover tooltip; reduced-motion freeze
      exports: [MrrWaterfall]
    - path: app/saas/revenue/waterfall/_components/CohortRetentionHeatmap.tsx
      provides: '`use client`' SVG heatmap; 4-band legend
      exports: [CohortRetentionHeatmap]
    - path: app/saas/churn/page.tsx
      provides: server component at-risk accounts + open interventions + recently resolved; 7-playbook chip; <SaveOfferPricingBlock /> + Approval Inbox deep-link
    - path: app/saas/churn/_components/SaveOfferActivationModal.tsx
      provides: '`use client`' modal reusing 215 billing-correction modal pattern; reason ≥20 chars + evidence_pack_ref REQUIRED; DB-trigger CHURN_SAVE_OFFER_REQUIRES_PRICING_AND_APPROVAL mirrored with disabled CTA
      exports: [SaveOfferActivationModal]
    - path: app/saas/invoices/page.tsx
      provides: server component invoice list with PII redaction (Sensitive Credential UI Binding Layer 6); 4 status badges + 5 reconciliation_state badges; <.c-chip-protocol> for vault_ref; "View webhook DLQ →" deep-link to Recovery Center
    - path: app/saas/support/page.tsx
      provides: server component support inbox + classifier filters + review modal; mobile_priority CRITICAL; banned-lexicon scan server-side BEFORE dispatch
    - path: app/saas/support/_components/SupportResponseReviewModal.tsx
      provides: '`use client`' review modal with Approve and send / Reject and capture reason / Edit suggested response
      exports: [SupportResponseReviewModal]
    - path: app/saas/support/_components/ClassifierFilters.tsx
      provides: '`use client`' sortable filter chips for sentiment / intent / churn_signal / topic_tags / urgency_score
      exports: [ClassifierFilters]
    - path: app/saas/agents/page.tsx
      provides: server component SAS agent registry — 6 SAS rows always present + future SAS targets placeholder banner
    - path: test/saas-217/domain-8/cross-cutting.test.js
      provides: X-1..X-10 enforcement tests (zero hex, zero hard-coded font/color, zero hard-coded $, zero tier names, zero .c-card--feature, zero .c-table, banned-lexicon zero-match, mobile_priority registration, Storybook story coverage, a11y test presence)
    - path: test/ui-a11y/217-saas-a11y.test.js
      provides: axe-core a11y tests across 9 surfaces; mirrors 213.2/213.4 chrome-a11y patterns; assertion zero violations on critical+serious axe rules
  key_links:
    - from: app/saas/subscriptions/page.tsx
      to: 214 <SaaSSubscriptionsTable /> (extracted reuse)
      via: REUSE verbatim with 217 metric joins (per-row health via HealthScoreBadge + RiskBandBadge)
      pattern: "SaaSSubscriptionsTable"
    - from: app/saas/churn/_components/SaveOfferActivationModal.tsx
      to: 215 billing-correction modal pattern
      via: REUSE pattern; reason ≥20 chars + evidence_pack_ref REQUIRED before offer_activated_at; DB-trigger CHURN_SAVE_OFFER_REQUIRES_PRICING_AND_APPROVAL mirrored with disabled CTA
      pattern: "CHURN_SAVE_OFFER_REQUIRES_PRICING_AND_APPROVAL"
    - from: app/saas/invoices/page.tsx
      to: api/v1/saas/invoices.js (Plan 04)
      via: server-side fetch; API handler emits saas_billing_events row with event_type == 'credential_view' per fetch (B-5 enforcement)
      pattern: "credential_view"
    - from: app/saas/support/page.tsx
      to: scripts/marketing-loop/check-banned-lexicon.mjs
      via: banned-lexicon scan runs server-side BEFORE dispatch UPDATE call (UI Binding Contract 2 enforcement)
      pattern: "check-banned-lexicon"
    - from: app/saas/agents/page.tsx
      to: 217-03 sas_agent_readiness table (Plan 03)
      via: 6 SAS rows + 7 readiness flags; SAS-10 invariant runnable=false in P217
      pattern: "runnable"
    - from: app/saas/{8 sub-pages}/page.tsx
      to: lib/markos/operator/shell.ts SurfaceRouteContract (Plan 06 extension)
      via: surface_family: saas_*; mobile_priority literal (8 secondary + 1 critical)
      pattern: "surface_family"
---

<objective>
Ship the 8 sub-route pages under `app/saas/{subscriptions,plans,revenue,revenue/waterfall,churn,invoices,support,agents}/page.tsx` + Storybook story registry per page + cross-cutting verification tests + axe a11y suite + checkpoint:human-verify consolidating 5 manual-only verifications.

Plan 08 folds 89 UI-SPEC ACs:
- S-1..S-8 (Subscriptions) — REUSES 214 SaaSSubscriptionsTable
- P-1..P-6 (Plans) — read-only catalog
- R-1..R-9 (Revenue) — 15 metrics + 5-tier source precedence
- W-1..W-10 (Waterfall) — 6-column MRR waterfall + cohort retention heatmap
- C-1..C-11 (Churn) — 7-playbook + save offer modal (215 reuse)
- I-1..I-11 (Invoices) — Sensitive Credential UI Binding Layer 6
- SP-1..SP-15 (Support) — 5-classifier + KB grounding + banned-lexicon zero-match
- G-1..G-9 (Agents) — 6 SAS rows + readiness flags
- X-1..X-10 (Cross-cutting) — D-08/D-09/D-09b/D-13/D-14/D-15/D-21 enforcement

Plan 08 includes ONE checkpoint:human-verify task consolidating 5 manual sanity checks.

Output: 8 page files + 5 client subcomponents + 8 stories files + 8 module.css files + 9 test files + 1 a11y test + populated VALIDATION.md.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/217-saas-suite-revenue-agents-api-ui/217-CONTEXT.md
@.planning/phases/217-saas-suite-revenue-agents-api-ui/217-RESEARCH.md
@.planning/phases/217-saas-suite-revenue-agents-api-ui/217-UI-SPEC.md
@.planning/phases/217-saas-suite-revenue-agents-api-ui/217-VALIDATION.md
@.planning/phases/217-saas-suite-revenue-agents-api-ui/217-01-PLAN.md
@.planning/phases/217-saas-suite-revenue-agents-api-ui/217-02-PLAN.md
@.planning/phases/217-saas-suite-revenue-agents-api-ui/217-03-PLAN.md
@.planning/phases/217-saas-suite-revenue-agents-api-ui/217-04-PLAN.md
@.planning/phases/217-saas-suite-revenue-agents-api-ui/217-05-PLAN.md
@.planning/phases/217-saas-suite-revenue-agents-api-ui/217-06-PLAN.md
@.planning/phases/217-saas-suite-revenue-agents-api-ui/217-07-PLAN.md
@.planning/phases/206-soc2-type1-foundation/206-UI-SPEC.md
@.planning/phases/207-agentrun-v2-orchestration-substrate/207-UI-SPEC.md
@.planning/phases/208-human-operating-interface/208-UI-SPEC.md
@.planning/phases/213-tenant0-dogfood-compliance-validation/213-UI-SPEC.md
@.planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md
@.planning/phases/215-saas-suite-billing-payments-compliance/215-UI-SPEC.md
@.planning/phases/216-saas-suite-health-churn-support-usage/216-UI-SPEC.md
@DESIGN.md
@CLAUDE.md
@app/(markos)/settings/saas/page.tsx
@app/(markos)/admin/billing/page.tsx
@styles/components.css
@app/tokens.css
@scripts/marketing-loop/check-banned-lexicon.mjs

<interfaces>
<!-- Plan 08 consumes — must be FULLY DEFINED before execution. -->

From Plan 07 (extracted components):
```typescript
import { HealthScoreBadge } from '@/app/saas/_components/HealthScoreBadge';
import { RiskBandBadge } from '@/app/saas/_components/RiskBandBadge';
import { ClassifierChipRow } from '@/app/saas/_components/ClassifierChipRow';
import { KbGroundingPanel } from '@/app/saas/_components/KbGroundingPanel';
import { SaveOfferPricingBlock } from '@/app/saas/_components/SaveOfferPricingBlock';
import { RetentionClassChip } from '@/app/saas/_components/RetentionClassChip';
import { PIIRedactedField } from '@/app/saas/_components/PIIRedactedField';
```

From 214 (existing extracted reuse):
```typescript
import { SaaSActivationPanel } from '@/app/(markos)/_components/SaaSActivationPanel';
import { SaaSSubscriptionsTable } from '@/app/(markos)/_components/SaaSSubscriptionsTable';
```

8 lifecycle state literals (verbatim from 214-RESEARCH §Domain 3):
- `draft`, `trialing`, `active`, `past_due`, `paused`, `canceled`, `upgrade_pending`, `downgrade_pending`

16 event_type literals (verbatim from 214-RESEARCH §Domain 3):
- `activation_started`, `trial_started`, `trial_converted`, `payment_failed`, `payment_recovered`, `pause_requested`, `pause_applied`, `reactivation_requested`, `reactivated`, `upgrade_requested`, `upgrade_applied`, `downgrade_requested`, `downgrade_applied`, `cancellation_requested`, `canceled`, `rollback_applied`

15 revenue metrics (verbatim from 217-01 Revenue Metric Formula Table):
- `mrr`, `arr`, `new_mrr`, `expansion_mrr`, `contraction_mrr`, `churn_mrr`, `reactivation_mrr`, `net_new_mrr`, `nrr`, `grr`, `churn_rate_logo`, `churn_rate_revenue`, `ltv`, `cohort_retention`, `forecast_3m`

5-tier source precedence (verbatim labels from UI-SPEC §Surface E Copy register):
- "Billing engine", "Processor webhook", "Accounting sync", "CRM record", "Manual correction"

5 reconciliation_state badges (verbatim from UI-SPEC §Surface E Copy register):
- "[ok] Reconciled", "[info] Pending reconciliation", "[err] Conflict flagged — review", "[warn] Manually corrected", "[info] Excluded"

6-column MRR waterfall (verbatim from UI-SPEC §Surface F):
| column | color discipline |
|--------|------------------|
| `new_mrr` | `--color-success` |
| `expansion_mrr` | `--color-success` |
| `contraction_mrr` | `--color-error` (negative bar) |
| `churn_mrr` | `--color-error` (negative bar) |
| `reactivation_mrr` | `--color-success` |
| `net_new_mrr` | `--color-primary` (single mint signal exception) |

7-playbook seed catalog (verbatim from 216-04 playbook-seeds.ts):
- `engagement_deepening`, `ebr_scheduling`, `executive_outreach`, `champion_re_engagement`, `billing_recovery`, `expansion_conversation`, `account_brief`
- intervention types: `save_offer`, `win_back_offer`

5 PII field constants (verbatim from 215-01 lib/markos/saas/billing/log-redaction.ts):
- `STRIPE_PII_FIELDS` — `[email, last4, postal_code, ip_address, phone, name]`
- `MP_PII_FIELDS` — `[email, last4, ip_address, phone, name, payer_doc]`
- `QB_PII_FIELDS` — `[realm_id, customer_email, customer_phone]`
- `SIIGO_PII_FIELDS` — `[customer_nit, customer_name, customer_email]`
- `DIAN_PII_FIELDS` — `[nit, certificate_subject, software_pin, environment]`

12 CREDENTIAL_FIELDS (banned in source per B-1):
- `credential_value`, `secret_key`, `webhook_secret`, `access_token`, `oauth_token`, `realm_id`, `pkcs12_certificate`, `pkcs12_password`, `authorization_token`, `software_pin`, `client_secret`, `refresh_token`

4 sentiment literals (verbatim from 216):
- `positive`, `neutral`, `negative`, `frustrated`

7 intent literals (verbatim from 216):
- `upgrade`, `get_help`, `report_bug`, `request_feature`, `complaint`, `cancel`, `billing_dispute`

5 PII classification literals (verbatim from 216):
- `no_pii`, `pseudonymous`, `personal`, `sensitive`, `highly_sensitive`

KB_CONFIDENCE_THRESHOLD = 0.7 (default per 216)

6 SAS agent tokens (verbatim from 217-03):
- `sas-onboarding`, `sas-narrative`, `sas-feedback`, `sas-churn-risk`, `sas-revenue-narrative`, `sas-savings-tracker`

4 approval mode badges (verbatim from 206 mutation-class doctrine):
- `[info] Auto`, `[info] Single approval`, `[warn] Dual approval`, `[warn] Cooling off`

7 failure class badges (verbatim from 207 AgentFailureClass):
- `[info] Transient`, `[warn] Policy`, `[info] User input`, `[warn] Quota`, `[warn] Upstream`, `[err] Poison`, `[info] Cancel`

8 Approval Inbox handoff_kind chips (when 217 ships in production — UI-SPEC §From 208-UI-SPEC):
- 4 P207: `approval`, `recovery`, `follow_up`, `manual_input`
- 5th P214: `billing_charge_approval`
- 6th P215: `billing_correction_approval`
- 7th P216: `support_response_approval`
- 8th P216: `save_offer_approval`
(217 does NOT add a 9th literal)

Banned-lexicon (CLAUDE.md): `synergy`, `leverage`, `empower`, `unlock`, `transform`, `revolutionize`, `supercharge`, `holistic`, `seamless`, `cutting-edge`, `innovative`, `game-changer`, `next-generation`, `world-class`, `best-in-class`, `reimagine`, `disrupt`, `just` (as softener); zero exclamation points in product surface copy.

Banned phrases for public-proof boundary (213-04): `unlimited`, `instant`, `guaranteed`, `100%`, `bank-grade`, `military-grade`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 4 read-mostly sub-pages — Subscriptions + Plans + Revenue + Waterfall (folds S/P/R/W = 33 ACs)</name>
  <files>app/saas/subscriptions/page.tsx, app/saas/subscriptions/page.module.css, app/saas/subscriptions/page.stories.tsx, app/saas/plans/page.tsx, app/saas/plans/page.module.css, app/saas/plans/page.stories.tsx, app/saas/revenue/page.tsx, app/saas/revenue/page.module.css, app/saas/revenue/page.stories.tsx, app/saas/revenue/waterfall/page.tsx, app/saas/revenue/waterfall/page.module.css, app/saas/revenue/waterfall/page.stories.tsx, app/saas/revenue/waterfall/_components/MrrWaterfall.tsx, app/saas/revenue/waterfall/_components/MrrWaterfall.module.css, app/saas/revenue/waterfall/_components/CohortRetentionHeatmap.tsx, app/saas/revenue/waterfall/_components/CohortRetentionHeatmap.module.css, test/saas-217/domain-8/subscriptions-page.test.js, test/saas-217/domain-8/plans-page.test.js, test/saas-217/domain-8/revenue-page.test.js, test/saas-217/domain-8/waterfall-page.test.js</files>
  <behavior>
    SUBSCRIPTIONS (folds S-1..S-8):
    - Test 1: <SaaSSubscriptionsTable /> reused verbatim with 217 metric joins (per-row HealthScoreBadge + RiskBandBadge join via subscription_id)
    - Test 2: 8 lifecycle state literals render with bracketed glyph (`[info] Draft`, `[info] Trialing`, `[ok] Active`, `[warn] Past due`, `[warn] Paused`, `[err] Canceled`, `[warn] Upgrade pending`, `[warn] Downgrade pending`)
    - Test 3: 16 event_type chips render with verbatim copy
    - Test 4: Mutation queue deep-links to `/operations/approvals?handoff_kind=billing_charge_approval&mutation_request_id={id}` (mint-text inline)
    - Test 5: Failed run failure indicator renders AgentFailureClass literal (7 literals) verbatim
    - Test 6: Mobile breakpoint vertical card stack
    - Test 7: Storybook story registers 6 named-state stories (Empty/Active/Trialing/PastDue/Canceled/PendingMutation)
    - Test 8: Per-row "Open subscription →" mint-text link to /saas/subscriptions/[id] (or `/operations/tasks?run_id={agent_run_id}` deep-link in P217 — 207 substrate)

    PLANS (folds P-1..P-6):
    - Test 9: Plan catalog read-only in P217
    - Test 10: Sentinel `{{MARKOS_PRICING_ENGINE_PENDING}}` rendered verbatim where pricing context missing via `<PlaceholderBanner variant="billing_placeholder">`
    - Test 11: Status badges render verbatim copy: `[ok] Published`, `[info] Draft`, `[info] Archived`
    - Test 12: pricing_recommendation_id resolved server-side via 205 substrate
    - Test 13: Zero hard-coded dollar literals; zero tier-name strings
    - Test 14: Mobile vertical card stack

    REVENUE (folds R-1..R-9):
    - Test 15: 15 revenue metrics rendered per 217-01 formula table (mrr/arr/new_mrr/expansion_mrr/contraction_mrr/churn_mrr/reactivation_mrr/net_new_mrr/nrr/grr/churn_rate_logo/churn_rate_revenue/ltv/cohort_retention/forecast_3m)
    - Test 16: Source precedence column shows 5-tier model literally with verbatim labels: "Billing engine", "Processor webhook", "Accounting sync", "CRM record", "Manual correction"
    - Test 17: 5 reconciliation_state badges render verbatim ([ok] Reconciled / [info] Pending reconciliation / [err] Conflict flagged — review / [warn] Manually corrected / [info] Excluded)
    - Test 18: Tabular-numeral discipline (`tnum`) on monetary columns
    - Test 19: Cron-stale state (>36h) triggers `<.c-notice c-notice--warning>` "[warn] MRR snapshot cron stale (last run {ago})"
    - Test 20: Cohort retention table renders T+1/T+3/T+6/T+12 columns
    - Test 21: Forecast (3-month linear) renders per 217-01 formula
    - Test 22: Mobile breakpoint: tables become vertical card stacks
    - Test 23: Storybook story registers 4 named-state stories (Empty/Healthy/ConflictFlagged/CronStale)

    WATERFALL (folds W-1..W-10):
    - Test 24: 6-column waterfall renders 5 contribution columns + net column (New / Expansion / Contraction / Churn / Reactivation / Net new MRR)
    - Test 25: Negative contributions (contraction, churn) render as downward bars in `--color-error`
    - Test 26: Positive contributions (new, expansion, reactivation) render as upward bars in `--color-success`
    - Test 27: Net column renders in `--color-primary` (the single mint signal exception per UI-SPEC §Color)
    - Test 28: Tabular-numerals (`tnum`) on all monetary labels
    - Test 29: Cohort retention heatmap renders T+1..T+12 columns
    - Test 30: Hover tooltip server-rendered for keyboard/screen-reader accessibility (D-21 client subcomponent for hover state only)
    - Test 31: `prefers-reduced-motion` disables hover transitions
    - Test 32: Mobile breakpoint: waterfall scrolls horizontally; legend stacks vertically
    - Test 33: Storybook story registers 4 named-state stories (Empty/Healthy/ChurnHeavy/CronStale)
  </behavior>
  <read_first>
    - 217-UI-SPEC.md §Surface C — app/saas/subscriptions/page.tsx FULL SECTION
    - 217-UI-SPEC.md §Surface D — app/saas/plans/page.tsx FULL SECTION
    - 217-UI-SPEC.md §Surface E — app/saas/revenue/page.tsx FULL SECTION
    - 217-UI-SPEC.md §Surface F — app/saas/revenue/waterfall/page.tsx FULL SECTION
    - 217-UI-SPEC.md §From 214-UI-SPEC §SaaSSubscriptionsTable verbatim reuse
    - 214-UI-SPEC.md §Subscription lifecycle 8-state machine + 16 events + 16 transitions
    - 217-UI-SPEC.md §Inheritance Bindings — From 205-UI-SPEC pricing ownership
    - 217-01-PLAN.md Revenue Metric Formula Table (15 metrics + formulas + source precedence)
    - 217-02-PLAN.md saas_mrr_waterfall_entries movement_type CHECK constraint (6 values)
    - DESIGN.md v1.1.0 §Color "Composition proportion" + mint signal exception for net_new_mrr
    - styles/components.css (.c-card, .c-badge--{state}, .c-chip-protocol, .c-status-dot--live)
    - app/(markos)/_components/SaaSSubscriptionsTable.tsx (214 extracted; verify props + reuse pattern)
    - app/saas/_components/{HealthScoreBadge,RiskBandBadge,RetentionClassChip}.tsx (Plan 07 outputs)
    - lib/markos/saas/preflight/index.cjs (scanForbiddenPatterns)
  </read_first>
  <action>
    Step 1: Write the 33 tests (across 4 test files) BEFORE implementation (TDD RED).
    Step 2: Implement app/saas/subscriptions/page.tsx as server component:
      - Fetch /v1/saas/subscriptions (Plan 04)
      - Reuse <SaaSSubscriptionsTable /> from 214 verbatim
      - Augment with 217 metric joins: per-row <HealthScoreBadge /> + <RiskBandBadge /> via subscription_id
      - Render 8 lifecycle state literals + 16 event type chips with verbatim copy
      - Mutation queue rows render mint-text "Open Approval Inbox →" deep-link
    Step 3: Implement app/saas/plans/page.tsx as server component (read-only):
      - Fetch /v1/saas/plans (Plan 04)
      - Vanilla `<table>` with columns: Plan / Version / Price / Discount / Value Metric / Status
      - Pricing rendered via <Money fromPricingRecommendation> XOR <PlaceholderBanner variant="billing_placeholder"> sentinel
      - Status badges verbatim copy
    Step 4: Implement app/saas/revenue/page.tsx as server component:
      - Fetch /v1/saas/metrics/* (Plan 04)
      - Sections: "Trailing 12 months" (15 metrics with formula + source + reconciled?), "Cohort retention" (T+1/T+3/T+6/T+12 heatmap), "Forecast (3-month linear)", "Reconciliation queue"
      - Vanilla `<table>` with `font-feature-settings: 'tnum' 1` on numeric columns
      - 5-tier source precedence labels verbatim
      - 5 reconciliation_state badges verbatim
      - Cron-stale check (>36h) triggers warning notice
    Step 5: Implement app/saas/revenue/waterfall/page.tsx as server component:
      - Fetch /v1/saas/metrics/waterfall (Plan 04)
      - Render <MrrWaterfall /> SVG bar chart 6 columns (new / expansion / contraction / churn / reactivation / net_new) with COLOR DISCIPLINE (positive=success, negative=error, net=primary mint)
      - Render <CohortRetentionHeatmap /> 12 columns × N cohorts; 4-band legend
      - Vanilla `<table>` waterfall summary view with tabular-numerals
    Step 6: Implement MrrWaterfall.tsx as 'use client' SVG bar chart with hover state; reduced-motion freezes hover transitions; bar fills use `var(--color-success)` / `var(--color-error)` / `var(--color-primary)`.
    Step 7: Implement CohortRetentionHeatmap.tsx as 'use client' SVG heatmap; cell border via `var(--color-border)`; 4-band legend chips.
    Step 8: Each page ships a `.stories.tsx` Storybook file with named-state stories per UI-SPEC §Surface C-F.
    Step 9: Run all 33 tests (TDD GREEN). Banned-lexicon scan returns 0.
    Step 10: Commit: `feat(217-08): 4 read-mostly sub-pages — subscriptions/plans/revenue/waterfall (folds S+P+R+W = 33 ACs)`.
  </action>
  <acceptance_criteria>
    - `npm test -- test/saas-217/domain-8/subscriptions-page.test.js test/saas-217/domain-8/plans-page.test.js test/saas-217/domain-8/revenue-page.test.js test/saas-217/domain-8/waterfall-page.test.js` passes (33 assertions green)
    - `grep -E "SaaSSubscriptionsTable" app/saas/subscriptions/page.tsx | wc -l` returns at least 1 (214 reuse)
    - `grep -E "MARKOS_PRICING_ENGINE_PENDING" app/saas/plans/page.tsx | wc -l` returns at least 1
    - `grep -F "Billing engine" app/saas/revenue/page.tsx | wc -l` returns 1 (verbatim source label)
    - `grep -F "[err] Conflict flagged — review" app/saas/revenue/page.tsx | wc -l` returns 1 (verbatim badge)
    - `grep -F "[warn] MRR snapshot cron stale" app/saas/revenue/page.tsx | wc -l` returns 1
    - `grep -F "tnum" app/saas/revenue/page.module.css app/saas/revenue/waterfall/page.module.css | wc -l` returns at least 2 (tabular-numerals)
    - `grep -E "var\\(--color-success\\)|var\\(--color-error\\)|var\\(--color-primary\\)" app/saas/revenue/waterfall/_components/MrrWaterfall.module.css | wc -l` returns at least 3 (color discipline)
    - `grep -E "#[0-9a-fA-F]{3,8}" app/saas/{subscriptions,plans,revenue,revenue/waterfall}/**/*.{tsx,module.css} | wc -l` returns 0 (D-08)
    - `grep -E "c-card--feature|c-table" app/saas/{subscriptions,plans,revenue,revenue/waterfall}/**/*.{tsx,module.css} | wc -l` returns 0 (D-13 + D-14)
    - `grep -E "\\\$[0-9]|USD [0-9]|COP [0-9]" app/saas/{subscriptions,plans,revenue,revenue/waterfall}/**/*.tsx | wc -l` returns 0 (no hard-coded $)
    - `node scripts/marketing-loop/check-banned-lexicon.mjs app/saas/{subscriptions,plans,revenue,revenue/waterfall}/` returns 0 matches
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 4 high-binding sub-pages — Churn + Invoices + Support + Agents (folds C/I/SP/G = 46 ACs)</name>
  <files>app/saas/churn/page.tsx, app/saas/churn/page.module.css, app/saas/churn/page.stories.tsx, app/saas/churn/_components/SaveOfferActivationModal.tsx, app/saas/churn/_components/SaveOfferActivationModal.module.css, app/saas/invoices/page.tsx, app/saas/invoices/page.module.css, app/saas/invoices/page.stories.tsx, app/saas/support/page.tsx, app/saas/support/page.module.css, app/saas/support/page.stories.tsx, app/saas/support/_components/SupportResponseReviewModal.tsx, app/saas/support/_components/SupportResponseReviewModal.module.css, app/saas/support/_components/ClassifierFilters.tsx, app/saas/support/_components/ClassifierFilters.module.css, app/saas/agents/page.tsx, app/saas/agents/page.module.css, app/saas/agents/page.stories.tsx, test/saas-217/domain-8/churn-page.test.js, test/saas-217/domain-8/invoices-page.test.js, test/saas-217/domain-8/support-page.test.js, test/saas-217/domain-8/agents-page.test.js</files>
  <behavior>
    CHURN (folds C-1..C-11):
    - Test 1: 4 risk bands render via <RiskBandBadge />
    - Test 2: 5-dim health score per row via <HealthScoreBadge />
    - Test 3: 7-playbook seed catalog rendered as chip per row (engagement_deepening / ebr_scheduling / executive_outreach / champion_re_engagement / billing_recovery / expansion_conversation / account_brief)
    - Test 4: Save-offer activation modal reuses 215 billing-correction modal pattern
    - Test 5: <SaveOfferPricingBlock /> renders <Money> XOR sentinel verbatim
    - Test 6: Reason capture ≥20 chars enforced; evidence_pack_ref REQUIRED
    - Test 7: DB-trigger CHURN_SAVE_OFFER_REQUIRES_PRICING_AND_APPROVAL validates server-side; UI mirrors with disabled CTA
    - Test 8: Autonomy ceiling notice renders with [block] glyph "[block] Autonomy ceiling reached for billing.charge (save offers)"
    - Test 9: Approval Inbox deep-link `/operations/approvals?handoff_kind=save_offer_approval&task_ref={intervention_id}`
    - Test 10: Mobile breakpoint vertical card stack
    - Test 11: Storybook story registers 5 named-state stories (Empty/AtRisk/PendingPricing/PendingApproval/AutonomyCeiling)

    INVOICES (folds I-1..I-11):
    - Test 12: <PIIRedactedField /> renders [REDACTED] for all 5 PII-field-list constants verbatim (STRIPE_PII_FIELDS / MP_PII_FIELDS / QB_PII_FIELDS / SIIGO_PII_FIELDS / DIAN_PII_FIELDS)
    - Test 13: onCopy preventDefault() enforced on PII display nodes (Storybook interaction test asserts clipboard.writeText never invoked)
    - Test 14: <.c-chip-protocol> renders vault_ref UUID via mint-as-text only
    - Test 15: API handler emits saas_billing_events row with event_type == 'credential_view' per fetch (server-side at Plan 04)
    - Test 16: Architecture-lock test asserts banned credential field names absent from page source (12 CREDENTIAL_FIELDS: credential_value/secret_key/webhook_secret/access_token/oauth_token/realm_id/pkcs12_certificate/pkcs12_password/authorization_token/software_pin/client_secret/refresh_token)
    - Test 17: Tabular-numerals on monetary columns
    - Test 18: 4 invoice status badges render with bracketed glyph ([ok] Paid / [warn] Past due / [info] Pending / [err] Failed)
    - Test 19: 5 reconciliation_state badges render verbatim
    - Test 20: Mobile vertical card stack; row PII fields preserve [REDACTED] rendering
    - Test 21: Storybook story registers 5 named-state stories (Empty/Healthy/PastDue/Failed/ConflictFlagged)
    - Test 22: Banned-lexicon zero-match across all module text

    SUPPORT (folds SP-1..SP-15):
    - Test 23: <ClassifierChipRow /> renders 5 classifier dimensions verbatim (sentiment / intent / churn_signal / topic_tags / urgency_score)
    - Test 24: <KbGroundingPanel /> renders top-3 KB sources with relevance_score badges
    - Test 25: 4 sentiment + 7 intent + churn_signal boolean + topic_tags array + urgency_score numeric all rendered with bracketed-glyph pairing
    - Test 26: Approve and send DISABLED when KB confidence below KB_CONFIDENCE_THRESHOLD (0.7)
    - Test 27: Approve and send DISABLED when autonomy ceiling reached
    - Test 28: Approve and send DISABLED when banned-lexicon zero-match server-side check fails
    - Test 29: Banned-lexicon scan runs server-side BEFORE the dispatch UPDATE call (via scripts/marketing-loop/check-banned-lexicon.mjs)
    - Test 30: Reason capture ≥20 chars enforced on Reject path
    - Test 31: Edit path re-runs groundResponse; if confidence drops, CTA re-disables
    - Test 32: PII redaction applied to ticket subject + description fields per 216 §UI Binding Contract 4
    - Test 33: Mobile priority `critical` registered in lib/markos/operator/shell.ts; bottom-sheet modal pattern
    - Test 34: 44px touch targets enforced via global `(pointer: coarse)` rule
    - Test 35: Storybook story registers 7 named-state stories (Empty/Open/Reviewing/KBBelow/AutonomyCeiling/BannedLexicon/Approved)
    - Test 36: Safe auto-response toggle deferred to P218+ via `<PlaceholderBanner variant="future_phase_218_safe_auto_response_toggle">`
    - Test 37: Approval Inbox deep-link `/operations/approvals?handoff_kind=support_response_approval&task_ref={ticket_id}` mint-text inline

    AGENTS (folds G-1..G-9):
    - Test 38: 6 SAS rows always present (verified by translation-gate test)
    - Test 39: Each row renders agent_token + role + inputs + outputs + cost + approval mode + failure class verbatim from 217-03 contract
    - Test 40: All 6 rows show runnable=false in P217 (SAS-10 invariant; closeout test asserts)
    - Test 41: 4 approval mode badges render verbatim from 206 mutation-class doctrine ([info] Auto / [info] Single approval / [warn] Dual approval / [warn] Cooling off)
    - Test 42: 7 failure class badges render verbatim from 207 AgentFailureClass ([info] Transient / [warn] Policy / [info] User input / [warn] Quota / [warn] Upstream / [err] Poison / [info] Cancel)
    - Test 43: Cost preview uses <Money fromPricingRecommendation> XOR sentinel
    - Test 44: Future SAS targets section renders `<PlaceholderBanner variant="future_phase_218_growth_dashboards">` with verbatim copy "[info] Awaiting Phase 218 translation. Growth target agents (PLG, EXP, ABM, VRL, IAM, CMT, EVT, XP, PR, PRT, DEV, REV) become runnable in Phase 218-220 plans."
    - Test 45: Mobile vertical card stack
    - Test 46: Storybook story registers 3 named-state stories (Default/AllNotRunnable/CostSentinel)
  </behavior>
  <read_first>
    - 217-UI-SPEC.md §Surface G — app/saas/churn/page.tsx FULL SECTION
    - 217-UI-SPEC.md §Surface H — app/saas/invoices/page.tsx FULL SECTION
    - 217-UI-SPEC.md §Surface I — app/saas/support/page.tsx FULL SECTION (mobile_priority: critical)
    - 217-UI-SPEC.md §Surface J — app/saas/agents/page.tsx FULL SECTION
    - 217-UI-SPEC.md §Sensitive Credential UI Binding (Layer 6) bindings B-1..B-8 verbatim
    - 217-UI-SPEC.md §From 215-UI-SPEC PARENT SaaS Billing inheritance
    - 217-UI-SPEC.md §From 216-UI-SPEC PARENT SaaS Health/Churn/Support/Usage inheritance
    - 215-UI-SPEC.md §Sensitive Credential Handling — UI Binding Contract Layer 6 verbatim
    - 215-UI-SPEC.md §billing-correction modal pattern (CHURN reuse)
    - 216-UI-SPEC.md §UI Binding Contract 1 (5-dim health + 4 bands)
    - 216-UI-SPEC.md §UI Binding Contract 2 (5-classifier + KB grounding + banned-lexicon zero-match)
    - 216-UI-SPEC.md §UI Binding Contract 3 (save offer pricing block + reuse 215 modal)
    - 216-UI-SPEC.md §UI Binding Contract 4 (5 PII classifications + retention)
    - 216-04 lib/markos/saas/churn/playbook-seeds.ts (7-playbook catalog)
    - 217-03-PLAN.md frontmatter (6 SAS agent tokens + 7 readiness flags + SAS-10 invariant)
    - 207-UI-SPEC.md §AgentFailureClass 7 literals
    - 206-UI-SPEC.md §mutation-class doctrine 4 approval modes
    - DESIGN.md v1.1.0 §Components — `.c-modal` + `.c-backdrop` + `.c-button--{primary,secondary,tertiary,destructive}`
    - styles/components.css (.c-modal verify; reduced-motion behavior)
    - scripts/marketing-loop/check-banned-lexicon.mjs (server-side scan utility)
    - lib/markos/saas/billing/log-redaction.ts (5 PII field constants verbatim)
    - app/saas/_components/{HealthScoreBadge,RiskBandBadge,ClassifierChipRow,KbGroundingPanel,SaveOfferPricingBlock,RetentionClassChip,PIIRedactedField}.tsx (Plan 07 outputs)
    - lib/markos/operator/shell.ts (Plan 06 surface_family registration; mobile_priority for /saas/support = critical)
  </read_first>
  <action>
    Step 1: Write the 46 tests (across 4 test files) BEFORE implementation (TDD RED).

    Step 2: Implement app/saas/churn/page.tsx as server component:
      - Fetch /v1/saas/churn (216 substrate via Plan 04)
      - Sections: "At-risk accounts" (rows where risk_level IN ('at_risk','critical')), "Open interventions" (status IN ('pending','awaiting_approval','active')), "Recently resolved" (resolved_at > now() - 30d)
      - Per row: <RiskBandBadge /> + <HealthScoreBadge /> + 7-playbook chip + <SaveOfferPricingBlock /> + activate save offer CTA
      - Activate save offer CTA opens <SaveOfferActivationModal /> ('use client'); reuses 215 billing-correction modal pattern
      - Reason ≥20 chars + evidence_pack_ref REQUIRED before submit
      - Autonomy ceiling notice renders with [block] glyph
      - Approval Inbox deep-link mint-text inline

    Step 3: Implement SaveOfferActivationModal.tsx as 'use client' subcomponent:
      ```tsx
      'use client';
      // [217-08] D-21 client subcomponent — save offer activation modal per UI-SPEC §Surface G.
      // Reuses 215 billing-correction modal pattern.
      // CHURN_SAVE_OFFER_REQUIRES_PRICING_AND_APPROVAL DB-trigger validates server-side; UI mirrors with disabled CTA.
      import { useState } from 'react';
      export function SaveOfferActivationModal({ interventionId, pricingRecommendationId, onClose }) {
        const [reason, setReason] = useState('');
        const [evidencePackRef, setEvidencePackRef] = useState('');
        const reasonValid = reason.length >= 20;
        const evidenceValid = evidencePackRef.length > 0;
        const ctaDisabled = !reasonValid || !evidenceValid || !pricingRecommendationId;
        // ... render .c-modal with reason + evidence inputs + Approve and activate CTA
      }
      ```

    Step 4: Implement app/saas/invoices/page.tsx as server component (Sensitive Credential UI Binding Layer 6):
      - Fetch /v1/saas/invoices (Plan 04 — handler emits saas_billing_events credential_view audit row)
      - Sections: "Open invoices", "Recently paid", "Reconciliation status"
      - Vanilla `<table>` with columns: Invoice ID / Issued / Due / Amount / Tax / Status / Customer / Vault ref / Actions
      - `<.c-chip-protocol>` for invoice_id + vault_ref UUIDs (mint-as-text only)
      - `<PIIRedactedField field="customer_name" classification="personal" />` for PII fields
      - Tabular-numerals on monetary
      - 4 invoice status + 5 reconciliation_state badges verbatim
      - "View timeline →" + "View webhook DLQ →" mint-text inline links

    Step 5: Implement app/saas/support/page.tsx as server component (mobile_priority: critical):
      - Fetch /v1/saas/support (216 substrate via Plan 04)
      - Sections: "Inbox" + "Recently resolved"
      - Vanilla `<table>` with columns: Ticket ID / Received / Subject (PII-redacted) / Sentiment / Intent / Churn signal / Urgency / Status / Actions
      - <ClassifierFilters /> ('use client') for sortable filter chips
      - <SupportResponseReviewModal /> ('use client') on row click
      - Approve and send DISABLED logic mirroring server-side check (KB confidence threshold + autonomy ceiling + banned-lexicon match)
      - 44px touch targets via global rule
      - Bottom-sheet modal pattern for mobile (CS rep field use)
      - Safe auto-response placeholder banner

    Step 6: Implement SupportResponseReviewModal.tsx as 'use client' subcomponent:
      - <ClassifierChipRow /> (5 dims)
      - <KbGroundingPanel /> (top-3 sources)
      - <textarea> editable suggested response
      - .c-button--primary "Approve and send" — disabled state logic (3 conditions)
      - .c-button--secondary "Reject and capture reason" (≥20 chars)
      - .c-button--tertiary "Edit suggested response" — re-runs groundResponse on save

    Step 7: Implement ClassifierFilters.tsx as 'use client' sortable filter chips for 5 dims.

    Step 8: Implement app/saas/agents/page.tsx as server component:
      - Fetch /v1/saas/agents (Plan 04)
      - Vanilla `<table>` 6 rows with columns: Token / Role / Inputs / Outputs / Cost / Approval mode / Failure class / Runnable
      - 4 approval mode badges + 7 failure class badges verbatim
      - Cost preview via <Money fromPricingRecommendation> XOR sentinel
      - All 6 rows show .c-status-dot--error + "[err] Not runnable" (SAS-10 invariant in P217)
      - Future SAS targets section renders `<PlaceholderBanner variant="future_phase_218_growth_dashboards">` with verbatim copy

    Step 9: Each page ships `.stories.tsx` Storybook file with named-state stories per UI-SPEC §Surfaces G-J.

    Step 10: Run all 46 tests (TDD GREEN). Banned-lexicon scan returns 0 across all 4 sub-pages.

    Step 11: Commit: `feat(217-08): 4 high-binding sub-pages — churn/invoices/support/agents (folds C+I+SP+G = 46 ACs)`.
  </action>
  <acceptance_criteria>
    - `npm test -- test/saas-217/domain-8/churn-page.test.js test/saas-217/domain-8/invoices-page.test.js test/saas-217/domain-8/support-page.test.js test/saas-217/domain-8/agents-page.test.js` passes (46 assertions green)
    - `grep -F "engagement_deepening" app/saas/churn/page.tsx | wc -l` returns at least 1 (7-playbook chip)
    - `grep -F "billing_recovery" app/saas/churn/page.tsx | wc -l` returns at least 1
    - `grep -F "[block] Autonomy ceiling reached for billing.charge (save offers)" app/saas/churn/page.tsx | wc -l` returns 1 (verbatim)
    - `grep -F "save_offer_approval" app/saas/churn/page.tsx | wc -l` returns at least 1 (Approval Inbox handoff_kind)
    - `grep -E "secret_key|webhook_secret|access_token|oauth_token|pkcs12_password|authorization_token|software_pin" app/saas/invoices/page.tsx | wc -l` returns 0 (B-1 — banned credential fields absent)
    - `grep -F "PIIRedactedField" app/saas/invoices/page.tsx | wc -l` returns at least 5 (one per PII field constant)
    - `grep -F "c-chip-protocol" app/saas/invoices/page.tsx | wc -l` returns at least 1 (vault_ref render)
    - `grep -E "STRIPE_PII_FIELDS|MP_PII_FIELDS|QB_PII_FIELDS|SIIGO_PII_FIELDS|DIAN_PII_FIELDS" app/saas/invoices/page.tsx | wc -l` returns at least 5 (B-3 enforcement)
    - `grep -F "ClassifierChipRow" app/saas/support/page.tsx | wc -l` returns at least 1
    - `grep -F "KbGroundingPanel" app/saas/support/_components/SupportResponseReviewModal.tsx | wc -l` returns at least 1
    - `grep -F "KB_CONFIDENCE_THRESHOLD" app/saas/support/_components/SupportResponseReviewModal.tsx | wc -l` returns at least 1
    - `grep -F "check-banned-lexicon" app/saas/support/page.tsx | wc -l` returns at least 1 (server-side scan BEFORE dispatch)
    - `grep -F "support_response_approval" app/saas/support/page.tsx | wc -l` returns at least 1 (Approval Inbox handoff_kind)
    - `grep -F "future_phase_218_safe_auto_response_toggle" app/saas/support/page.tsx | wc -l` returns 1 (placeholder)
    - `grep -F "[err] Not runnable" app/saas/agents/page.tsx | wc -l` returns at least 1 (SAS-10 invariant render)
    - `grep -F "future_phase_218_growth_dashboards" app/saas/agents/page.tsx | wc -l` returns 1
    - `grep -F "[info] Auto" app/saas/agents/page.tsx | wc -l` returns 1 (4 approval mode badges)
    - `grep -F "[err] Poison" app/saas/agents/page.tsx | wc -l` returns 1 (7 failure class badges)
    - `grep -E "#[0-9a-fA-F]{3,8}" app/saas/{churn,invoices,support,agents}/**/*.{tsx,module.css} | wc -l` returns 0 (D-08)
    - `grep -E "c-card--feature|c-table" app/saas/{churn,invoices,support,agents}/**/*.{tsx,module.css} | wc -l` returns 0 (D-13 + D-14)
    - `node scripts/marketing-loop/check-banned-lexicon.mjs app/saas/{churn,invoices,support,agents}/` returns 0 matches
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cross-cutting verification suite + axe a11y test + 217-VALIDATION.md final per-task map + UI-SPEC AC Coverage Map</name>
  <files>test/saas-217/domain-8/cross-cutting.test.js, test/ui-a11y/217-saas-a11y.test.js, .planning/phases/217-saas-suite-revenue-agents-api-ui/217-VALIDATION.md</files>
  <behavior>
    CROSS-CUTTING (folds X-1..X-10 across ALL surfaces):
    - Test 1 (X-1): Zero hex literals in any `app/saas/**/*.{tsx,module.css}` (D-08 token-only); all colors via `var(--color-*)`
    - Test 2 (X-2): Zero hard-coded `font-size`/`font-weight`/`color` in any `app/saas/**`; all via DESIGN.md `typography.*` tokens
    - Test 3 (X-3): Zero hard-coded dollar/peso literals (`\\$[0-9]`, `USD [0-9]`, `COP [0-9]`); all monetary via `<Money fromPricingRecommendation>` XOR sentinel
    - Test 4 (X-4): Zero hard-coded tier name strings (`"Growth Monthly"`, `"Starter"`, `"Professional"`, `"Enterprise"`)
    - Test 5 (X-5): Zero `.c-card--feature` use anywhere in `app/saas/**` (D-13 reserved for marketing)
    - Test 6 (X-6): Zero `.c-table` use anywhere; vanilla `<table>` only (D-14)
    - Test 7 (X-7): Banned-lexicon zero-match across all 217 surface text content — CI assertion `scripts/marketing-loop/check-banned-lexicon.mjs` runs against `app/saas/**` and returns 0 matches
    - Test 8 (X-8): All 9 surfaces register `mobile_priority` in `lib/markos/operator/shell.ts` SurfaceRouteContract with `surface_family: saas_*`; 8 surfaces `secondary`, 1 surface (`/saas/support`) `critical`; `desktop_only` is FORBIDDEN
    - Test 9 (X-9): All 9 surfaces register CSF3 named-state Storybook stories with ≥3 stories each (per 213.2 carry-forward)
    - Test 10 (X-10): Accessibility tests under `test/ui-a11y/217-saas-a11y.test.js` mirroring 213.2/213.4 chrome-a11y patterns — zero violations on critical+serious axe rules

    A11Y SUITE:
    - Test 11: axe-core scan on app/saas/page.tsx rendered output → 0 critical violations
    - Test 12: axe-core scan on app/saas/subscriptions/page.tsx → 0 critical violations
    - Test 13: axe-core scan on app/saas/plans/page.tsx → 0 critical violations
    - Test 14: axe-core scan on app/saas/revenue/page.tsx → 0 critical violations
    - Test 15: axe-core scan on app/saas/revenue/waterfall/page.tsx → 0 critical violations
    - Test 16: axe-core scan on app/saas/churn/page.tsx → 0 critical violations
    - Test 17: axe-core scan on app/saas/invoices/page.tsx → 0 critical violations
    - Test 18: axe-core scan on app/saas/support/page.tsx → 0 critical violations (mobile breakpoint at iPhone 14 Pro)
    - Test 19: axe-core scan on app/saas/agents/page.tsx → 0 critical violations

    VALIDATION.md UPDATE:
    - Update `## Per-Task Verification Map` table with rows for Plan 07 (4 task rows) + Plan 08 (3 task rows) + a11y suite + checkpoint
    - ADD `## UI-SPEC AC Coverage Map` section enumerating all 121 ACs (99 surface + 12 backend + 10 cross-cutting):
      - Layout: L-1..L-10 (Plan 07)
      - Overview: O-1..O-10 (Plan 07)
      - 7 component ACs (Plan 07)
      - Subscriptions: S-1..S-8 (Plan 08 Task 1)
      - Plans: P-1..P-6 (Plan 08 Task 1)
      - Revenue: R-1..R-9 (Plan 08 Task 1)
      - Waterfall: W-1..W-10 (Plan 08 Task 1)
      - Churn: C-1..C-11 (Plan 08 Task 2)
      - Invoices: I-1..I-11 (Plan 08 Task 2)
      - Support: SP-1..SP-15 (Plan 08 Task 2)
      - Agents: G-1..G-9 (Plan 08 Task 2)
      - Cross-cutting: X-1..X-10 (Plan 08 Task 3)
      - Backend doctrine: B-1..B-12 (Plans 01-06)
    - ADD `## 7 NEW Extracted Components Inheritance Map for P218+`:
      - HealthScoreBadge — first consumed 217 Plan 07; consumed by P218 PLG dashboards + P219 expansion + P220 referral console
      - RiskBandBadge — first consumed 217 Plan 07
      - ClassifierChipRow — first consumed 217 Plan 08 Support page
      - KbGroundingPanel — first consumed 217 Plan 08 Support page
      - SaveOfferPricingBlock — first consumed 217 Plan 08 Churn page; reused on P219 expansion
      - RetentionClassChip — first consumed 217 Plan 07
      - PIIRedactedField — first consumed 217 Plan 08 Invoices + Support pages
    - ADD `## 213.4 Carry D-08..D-15 Enforcement Table`:
      | Decision | Phase 217 enforcement | Closeout test |
      |---|---|---|
      | D-08 token-only | grep -E "#[0-9a-fA-F]" app/saas/** = 0 | architecture-lock-rerun.test.js (Plan 06) |
      | D-09 mint-as-text | mint never as fill except net_new_mrr | manual sanity (checkpoint Task 4) |
      | D-09b .c-notice mandatory | grep banner/alert/warning/callout = 0 | architecture-lock-rerun.test.js |
      | D-13 .c-card--feature reserved | grep c-card--feature app/saas/** = 0 | cross-cutting.test.js X-5 |
      | D-14 vanilla `<table>` | grep c-table app/saas/** = 0 | cross-cutting.test.js X-6 |
      | D-15 selective extraction | 7 NEW + 2 reused (214) | extracted-components.test.js (Plan 07) |
      | D-21 server/client boundary | 'use client' in _components/ only | architecture-lock-rerun.test.js |
  </behavior>
  <read_first>
    - 217-UI-SPEC.md §Cross-Cutting Acceptance Criteria X-1..X-10
    - 217-UI-SPEC.md §Surface Backend Doctrine B-1..B-12
    - 217-UI-SPEC.md §Total Acceptance Criteria (121 total)
    - 217-UI-SPEC.md §From 213.4-VALIDATION.md (carry-forward decisions D-08..D-15 + D-21)
    - 213.4-VALIDATION.md (carry-forward enforcement table)
    - test/ui-a11y/213-2-chrome-a11y.test.js (existing 213.2 axe pattern)
    - test/ui-a11y/213-4-chrome-a11y.test.js (existing 213.4 axe pattern)
    - lib/markos/operator/shell.ts (Plan 06 surface_family registration)
    - scripts/marketing-loop/check-banned-lexicon.mjs (server-side scan)
    - .planning/phases/217-saas-suite-revenue-agents-api-ui/217-VALIDATION.md (existing per-task map)
  </read_first>
  <action>
    Step 1: Write test/saas-217/domain-8/cross-cutting.test.js with 10 X-* tests:
      ```js
      'use strict';
      const test = require('node:test');
      const assert = require('node:assert/strict');
      const fs = require('node:fs');
      const path = require('node:path');
      function readAllUnder(dir, exts) {
        // recursive read; returns string[] of file paths
      }
      const surfaceFiles = readAllUnder('app/saas', ['.tsx', '.module.css']);
      test('X-1: zero hex literals across app/saas/**', () => {
        const violations = surfaceFiles.flatMap(f => {
          const content = fs.readFileSync(f, 'utf8');
          const matches = content.match(/#[0-9a-fA-F]{3,8}\b/g) || [];
          return matches.map(m => ({ file: f, match: m }));
        });
        assert.deepStrictEqual(violations, []);
      });
      // ... similar for X-2 through X-10
      ```

    Step 2: Write test/ui-a11y/217-saas-a11y.test.js using axe-core via JSDOM:
      ```js
      'use strict';
      const test = require('node:test');
      const assert = require('node:assert/strict');
      const { axe } = require('axe-core');
      const { JSDOM } = require('jsdom');
      // ... for each surface, render server component (mock fetch), inject into JSDOM, run axe.run, assert critical+serious violations === 0
      ```
      Mirror existing test/ui-a11y/213-2-chrome-a11y.test.js pattern.

    Step 3: Update 217-VALIDATION.md:
      - Replace existing `## Per-Task Verification Map` with full table including Plan 07 + Plan 08 task rows
      - ADD `## UI-SPEC AC Coverage Map` section (121 AC enumeration)
      - ADD `## 7 NEW Extracted Components Inheritance Map for P218+`
      - ADD `## 213.4 Carry D-08..D-15 Enforcement Table`
      - Update Manual-Only Verifications section to consolidate 5 manual sanity checks into a single checkpoint:human-verify task (Task 4 below)
      - Mark frontmatter `nyquist_compliant: true` + `wave_0_complete: true` (already set in Plan 01) + `phase_status: COMPLETE` once all plans land

    Step 4: Run all 19 tests + a11y suite (TDD GREEN). Banned-lexicon scan across `app/saas/**` returns 0.

    Step 5: Commit: `feat(217-08): cross-cutting X-1..X-10 verification + axe a11y suite + 217-VALIDATION.md UI-SPEC AC Coverage Map (121 ACs)`.
  </action>
  <acceptance_criteria>
    - `npm test -- test/saas-217/domain-8/cross-cutting.test.js` passes (10 X-* assertions green)
    - `npm test -- test/ui-a11y/217-saas-a11y.test.js` passes (9 surface a11y tests green; 0 critical+serious violations)
    - `node scripts/marketing-loop/check-banned-lexicon.mjs app/saas/` returns 0 matches across full surface
    - 217-VALIDATION.md `## Per-Task Verification Map` table has rows for ALL 8 plans (217-01..08)
    - 217-VALIDATION.md `## UI-SPEC AC Coverage Map` enumerates all 121 ACs
    - 217-VALIDATION.md `## 7 NEW Extracted Components Inheritance Map for P218+` present
    - 217-VALIDATION.md `## 213.4 Carry D-08..D-15 Enforcement Table` present
    - `grep -E "X-1.*Plan 08\|X-10.*Plan 08" .planning/phases/217-saas-suite-revenue-agents-api-ui/217-VALIDATION.md | wc -l` returns at least 2
    - `grep -E "L-1\|L-10\|O-1\|O-10\|S-1\|SP-1\|SP-15" .planning/phases/217-saas-suite-revenue-agents-api-ui/217-VALIDATION.md | wc -l` returns at least 7
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint — 5 manual sanity verifications consolidated (mobile breakpoint + Chromatic baselines + axe a11y + activation gate + sensitive credential Layer 6)</name>
  <what-built>
    Plan 07 + Plan 08 ship the full SaaS Suite UI surface — 1 layout + 9 sub-route pages + 7 NEW extracted components + 5 client subcomponents + Storybook story registry (10 surface stories + 7 component stories) + cross-cutting + axe a11y suite. All 121 UI-SPEC ACs are folded.
  </what-built>
  <how-to-verify>
    Five manual sanity verifications consolidated (per UI-SPEC §Manual-Only Verifications + 217-VALIDATION.md updated):

    1. **Mobile breakpoint at iPhone 14 Pro for `/saas/support` critical priority:**
       - Open `/saas/support` in browser DevTools mobile emulation (iPhone 14 Pro: 393×852)
       - Confirm: bottom-sheet modal pattern works on row click; 44px touch targets visible (.c-button min-height: 44px); ClassifierFilters chips wrap correctly; row table converts to vertical card stack
       - Expected: full functionality intact; no horizontal scroll on body content; tap targets meet WCAG 2.1 AA

    2. **Chromatic baselines for 10+7 component stories:**
       - Run `npm run chromatic` (or local Storybook capture) and confirm baselines accepted for:
         - Saas/Layout (5 stories: Loading/Gated/Pending/T0Closed/Active)
         - Saas/Overview (5 stories: Empty/Active/RecalcInProgress/RecalcSuccess/RawFactsIncomplete)
         - 7 component stories (HealthScoreBadge / RiskBandBadge / ClassifierChipRow / KbGroundingPanel / SaveOfferPricingBlock / RetentionClassChip / PIIRedactedField — ≥3 named states each)
         - 8 sub-page stories
       - Expected: baselines captured + reviewed; no unexpected visual regressions

    3. **axe a11y across 9 surfaces:**
       - Run `npm test -- test/ui-a11y/217-saas-a11y.test.js` (already in Task 3 — re-run as sanity)
       - Manually verify focus order on each surface using keyboard tab navigation
       - Expected: zero critical+serious violations; focus order matches UI-SPEC §Accessibility focus order per surface

    4. **Activation gate sanity (4 named reasons rendered correctly):**
       - Test tenant 1 (business_type='ecommerce') → expect `gated_by_business_type` notice rendered with verbatim copy
       - Test tenant 2 (saas_suite_activations.activation_status='draft') → expect `activation_pending` with mint-text link to `/settings/saas`
       - Test tenant 3 (tenant_0_readiness_gate.status='closed') → expect `tenant_0_gate_closed` warning notice
       - Test tenant 4 (saas_suite_activations.activation_status='disabled') → expect `saas_surface_disabled` error notice with mint-text link
       - Test tenant 5 (all 3 conditions pass) → expect children render + sidebar shows 9 active rows
       - Expected: 5/5 paths render correctly; banned reasons (`unauthorized`/`loading`/`error`/`unknown`) never appear

    5. **Sensitive Credential UI Binding Layer 6 manual sanity:**
       - Open `/saas/invoices` for an active SaaS tenant with at least one invoice containing PII fields
       - Confirm: 5 PII field constants render `[REDACTED]` placeholder via `<PIIRedactedField />` (visible inspection)
       - Try Cmd+C / Ctrl+C on `[REDACTED]` field: expect clipboard.writeText NOT invoked (DevTools Console assertion)
       - Try Cmd+C on `vault_ref` `<.c-chip-protocol>` chip: expect chip allowed to copy (UUID is public)
       - Confirm: `saas_billing_events` row inserted with `event_type='credential_view'` after page load (psql query)
       - Expected: 5/5 PII fields show `[REDACTED]`; clipboard block confirmed; audit row emitted
  </how-to-verify>
  <action>
    Operator (human reviewer) runs all 5 manual sanity checks listed in <how-to-verify> above (mobile breakpoint at iPhone 14 Pro for `/saas/support` critical priority + Chromatic baselines for 10+7 component stories + axe a11y manual focus-order across 9 surfaces + activation-gate sanity for 5 tenant scenarios + Sensitive Credential UI Binding Layer 6 PII redaction + clipboard block + audit-log emission). Each check has explicit expected outcomes. On any failure, the operator returns blocking issues per check; on all-pass, the operator types "approved" to resume Plan 08 closeout. The 5 checks are also enumerated in `217-VALIDATION.md ## Manual-Only Verifications`.
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify — manual sanity by operator; no automated verify"</automated>
    <manual>5 sanity checks above; operator types "approved" or returns blocking issues per check</manual>
  </verify>
  <done>
    Operator returns "approved" after all 5 manual sanity checks pass: mobile breakpoint OK + Chromatic baselines accepted + axe a11y zero critical+serious + activation gate 5/5 paths verbatim + Sensitive Credential Layer 6 (PII redaction visible + clipboard block confirmed + audit row emitted).
  </done>
  <resume-signal>Type "approved" or describe any blocking issues per check.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → app/saas/{8 sub-pages}/page.tsx | server-component fetches; tenant-scoped via Plan 04 handlers |
| app/saas/churn/_components/SaveOfferActivationModal → /v1/saas/* mutation | client modal; reason ≥20 chars + evidence_pack_ref; CHURN_SAVE_OFFER_REQUIRES_PRICING_AND_APPROVAL DB-trigger validates server-side |
| app/saas/support/page.tsx → /v1/saas/support dispatch | server-side banned-lexicon scan BEFORE UPDATE; 3-condition CTA disabled state |
| app/saas/invoices/_components/PIIRedactedField → clipboard | onCopy preventDefault() blocks clipboard write per Sensitive Credential UI Binding B-4 |
| app/saas/agents/page.tsx → sas_agent_readiness | read-only; SAS-10 invariant runnable=false |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-217-51 | Information Disclosure | Invoice PII leaked via clipboard from `/saas/invoices` | mitigate | <PIIRedactedField /> onCopy preventDefault() per B-4; field-level redaction via 5 PII field constants from 215; audit-log emission per fetch (B-5) |
| T-217-52 | Tampering | Save offer activation without pricing context | mitigate | CHURN_SAVE_OFFER_REQUIRES_PRICING_AND_APPROVAL DB-trigger raises EXCEPTION; UI mirrors with disabled CTA when pricing_recommendation_id IS NULL or sentinel literal active |
| T-217-53 | Information Disclosure | Support ticket PII leaked via subject/description | mitigate | 216 §UI Binding Contract 4 PII redaction applied to ticket subject + description fields via <PIIRedactedField /> |
| T-217-54 | Elevation of Privilege | Banned-lexicon dispatch via support response Approve and send | mitigate | Server-side banned-lexicon scan BEFORE dispatch UPDATE (scripts/marketing-loop/check-banned-lexicon.mjs); UI disables CTA on match |
| T-217-55 | Tampering | Autonomy ceiling bypassed via direct API call | mitigate | API handler (Plan 04) enforces autonomy ceiling at handler level; UI disabled state is defense-in-depth |
| T-217-56 | Information Disclosure | KB grounding source snippet leaks customer PII | mitigate | 216-03 KB grounding pipeline strips PII upstream; <KbGroundingPanel /> renders source_type + relevance_score; snippet preview is sanitized |
| T-217-57 | Repudiation | Approve and send dispatch without approval audit | mitigate | API handler emits AgentRun + audit log (207 substrate); 8th support_response_approval handoff_kind for ApprovalHandoffRecord chain |
| T-217-58 | Spoofing | Tier name forgery in plan catalog | mitigate | <Money fromPricingRecommendation> resolves server-side via 205 substrate; zero hard-coded tier strings (X-4 enforcement) |
| T-217-59 | Information Disclosure | Cross-tenant subscription/invoice/intervention leak | mitigate | RLS policies on saas_* tables (Plans 01/02/03/06); cross-tenant denial tests in Plans 01/02/03/06 |
| T-217-60 | Denial of Service | Storybook visual regression flooding Chromatic | accept | Chromatic baselines reviewed in checkpoint Task 4; ≤17 stories per build (10 surfaces + 7 components) |
</threat_model>

<verification>
- 8 sub-route pages ship under app/saas/{subscriptions,plans,revenue,revenue/waterfall,churn,invoices,support,agents}/
- 5 client subcomponents (MrrWaterfall, CohortRetentionHeatmap, SaveOfferActivationModal, SupportResponseReviewModal, ClassifierFilters)
- 8 stories files + 8 module.css files
- 9 page test files + cross-cutting test + a11y test = 11 test files
- 89 UI-SPEC ACs folded (S/P/R/W/C/I/SP/G + X)
- Cross-cutting X-1..X-10 enforced
- axe a11y zero critical+serious violations across 9 surfaces
- 217-VALIDATION.md UI-SPEC AC Coverage Map enumerates all 121 ACs
- 7 NEW Extracted Components Inheritance Map for P218+ documented
- Checkpoint Task 4 manual sanity 5/5 verifications passed
</verification>

<success_criteria>
- 8 sub-route pages render correctly with all UI-SPEC ACs honored
- All 89 UI-SPEC ACs folded into Plan 08 (S/P/R/W/C/I/SP/G + X)
- D-08/D-09/D-09b/D-13/D-14/D-15/D-21 all enforced via cross-cutting tests
- axe a11y zero critical violations
- Banned-lexicon zero-match across app/saas/**
- Sensitive Credential UI Binding Layer 6 enforced on Invoices + Support pages
- Plan 08 unblocks phase 217 closeout (slot-collision + architecture-lock-rerun + requirements-coverage closeout regression in Plan 06 Task 2 scans Plan 07/08 outputs)
- Checkpoint Task 4 5/5 manual sanity verifications passed
</success_criteria>

<output>
After completion, create `.planning/phases/217-saas-suite-revenue-agents-api-ui/217-08-SUMMARY.md`
</output>
