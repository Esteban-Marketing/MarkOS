---
phase: 46-operator-task-graph-ui-mvp
plan: 03
type: execute
wave: 2
depends_on:
  - 46-01
  - 46-02
files_modified:
  - app/(markos)/operations/tasks/page.tsx
  - app/(markos)/operations/tasks/task-graph.tsx
  - app/(markos)/operations/tasks/step-runner.tsx
autonomous: true
requirements:
  - OPS-01
  - OPS-02

must_haves:
  truths:
    - "Task graph UI renders as a linear vertical step list with visible state chips for all enum states."
    - "Step runner can trigger start/in-progress/complete transitions through reducer actions."
    - "Only one actionable sequential step is interactive at a time."
  artifacts:
    - path: "app/(markos)/operations/tasks/page.tsx"
      provides: "operations task surface composition"
      exports: []
    - path: "app/(markos)/operations/tasks/task-graph.tsx"
      provides: "linear vertical step list renderer"
      exports: ["TaskGraph"]
    - path: "app/(markos)/operations/tasks/step-runner.tsx"
      provides: "sequential action controls"
      exports: ["StepRunner"]
  key_links:
    - from: "app/(markos)/operations/tasks/page.tsx"
      to: "app/(markos)/operations/tasks/task-store.tsx"
      via: "TaskStoreProvider/useTaskStore"
      pattern: "TaskStoreProvider"
    - from: "app/(markos)/operations/tasks/step-runner.tsx"
      to: "app/(markos)/operations/tasks/task-machine.ts"
      via: "dispatch START_STEP/COMPLETE_STEP/FAIL_STEP"
      pattern: "START_STEP|COMPLETE_STEP|FAIL_STEP"
---

<objective>
Build the core task graph and step runner UI for the Phase 46 MVP surface.

Purpose: Deliver the visible operator execution experience on top of the reducer/store foundation.
Output: Task page composition with linear step list and sequential run controls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/46-operator-task-graph-ui-mvp/46-UI-SPEC.md
@app/(markos)/operations/tasks/task-types.ts
@app/(markos)/operations/tasks/task-machine.ts
@app/(markos)/operations/tasks/task-store.tsx
@lib/markos/theme/tokens.ts

<interfaces>
From task-machine action contract:
```ts
"START_STEP" | "COMPLETE_STEP" | "FAIL_STEP" | "SELECT_TASK" | "SELECT_EVIDENCE_STEP"
```

From UI spec:
```text
linear vertical step list, no graph library, strictly sequential interaction
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 46-03-01: Compose operations/tasks route and provider shell (per D-01, D-05, D-09)</name>
  <read_first>
    app/(markos)/operations/page.tsx
    app/(markos)/operations/tasks/task-store.tsx
    .planning/phases/46-operator-task-graph-ui-mvp/46-UI-SPEC.md
  </read_first>
  <files>app/(markos)/operations/tasks/page.tsx</files>
  <action>
Create app/(markos)/operations/tasks/page.tsx to mount TaskStoreProvider and render the three-region shell from the UI spec: left task list/graph region, center step runner region, right evidence placeholder region. Keep region order task list -> runner -> evidence and mobile fallback structure defined in 46-UI-SPEC.md. Wire selected task and actionable step selectors from useTaskStore.
  </action>
  <acceptance_criteria>
    - /markos/operations/tasks route file exists and exports default component.
    - Page wraps child UI with TaskStoreProvider.
    - Region placeholders/components for graph and runner are visible and ordered correctly.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('app/(markos)/operations/tasks/page.tsx','utf8');if(!t.includes('TaskStoreProvider')||!t.includes('TaskGraph')||!t.includes('StepRunner'))process.exit(1);"</automated>
  </verify>
  <done>
    Route-level task UI shell is wired to shared state and ready for component behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 46-03-02: Implement linear TaskGraph and sequential StepRunner controls (per D-01, D-02, D-03)</name>
  <read_first>
    app/(markos)/operations/tasks/task-types.ts
    app/(markos)/operations/tasks/task-machine.ts
    lib/markos/theme/tokens.ts
  </read_first>
  <files>app/(markos)/operations/tasks/task-graph.tsx, app/(markos)/operations/tasks/step-runner.tsx</files>
  <behavior>
    - Test 1: TaskGraph renders step cards in ascending order_index.
    - Test 2: TaskGraph displays exact state badge text: queued, approved, executing, completed, failed.
    - Test 3: StepRunner dispatches reducer actions in sequential order and disables future steps.
  </behavior>
  <action>
Create app/(markos)/operations/tasks/task-graph.tsx rendering a numbered vertical list of step cards (no graph library) with domain/type tags from fixture metadata and state badge chips showing exact enum strings. Create app/(markos)/operations/tasks/step-runner.tsx with explicit controls for Execute Task Step (START_STEP), mark complete (COMPLETE_STEP), and fail (FAIL_STEP); disable controls when step is not actionable sequentially. Use tokenMap() from lib/markos/theme/tokens.ts for semantic class/style mapping where practical.
  </action>
  <acceptance_criteria>
    - TaskGraph card layout is vertical/linear and does not import any graph package.
    - StepRunner action controls dispatch START_STEP, COMPLETE_STEP, FAIL_STEP.
    - Non-actionable future steps render disabled controls with explanatory text.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const g=fs.readFileSync('app/(markos)/operations/tasks/task-graph.tsx','utf8');const r=fs.readFileSync('app/(markos)/operations/tasks/step-runner.tsx','utf8');if(g.includes('reactflow')||g.includes('cytoscape')||!g.includes('queued')||!r.includes('START_STEP')||!r.includes('COMPLETE_STEP')||!r.includes('FAIL_STEP'))process.exit(1);"</automated>
  </verify>
  <done>
    Linear task graph and runner controls provide the primary OPS-01/OPS-02 operator execution experience.
  </done>
</task>

</tasks>

<verification>
Run file-level contract checks and confirm the tasks route renders through the existing MarkOS layout without unresolved imports.
</verification>

<success_criteria>
Task graph and step runner are functional, sequential, and aligned to the locked MVP interaction model.
</success_criteria>

<output>
After completion, create `.planning/phases/46-operator-task-graph-ui-mvp/46-03-SUMMARY.md`
</output>
