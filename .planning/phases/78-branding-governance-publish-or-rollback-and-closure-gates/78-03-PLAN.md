---
phase: 78-branding-governance-publish-or-rollback-and-closure-gates
plan: 03
type: execute
wave: 3
depends_on:
  - "78-02"
files_modified:
  - onboarding/backend/brand-governance/governance-artifact-writer.cjs
  - onboarding/backend/handlers.cjs
  - test/phase-78/tenant-isolation.test.js
  - test/phase-78/contract-integrity.test.js
autonomous: true
requirements:
  - BRAND-GOV-02

must_haves:
  truths:
    - Governance operations for one tenant cannot read or affect another tenant's bundle registry, pointer, or drift evidence.
    - Contract integrity gate fails when any required lineage fingerprint lane is null, absent, or has a non-deterministic value.
    - Governance evidence artifacts are machine-readable, include an evidence_hash, and are appended as an additive branding_governance field in the existing submit response payload.
    - The submit response remains backward-compatible and no existing response fields are removed or mutated.
  artifacts:
    - path: onboarding/backend/brand-governance/governance-artifact-writer.cjs
      provides: Machine-readable governance evidence persistence envelope appended to submit response without mutating existing fields.
    - path: onboarding/backend/handlers.cjs
      provides: Additive governance integration emitting bundle creation, closure gate results, and drift summary as branding_governance block in submit response.
    - path: test/phase-78/tenant-isolation.test.js
      provides: Cross-tenant access denial tests asserting registry, pointer, and drift reads are namespaced by tenant_id with deny-not-data behavior.
    - path: test/phase-78/contract-integrity.test.js
      provides: Contract integrity gate behavioral tests asserting fail-closed on null, absent, or non-deterministic lineage fingerprint entries.
  key_links:
    - from: onboarding/backend/handlers.cjs
      to: onboarding/backend/brand-governance/bundle-registry.cjs
      via: submit flow calls createBundle after all existing artifact writers complete — additive only, no existing handler fields mutated.
    - from: onboarding/backend/handlers.cjs
      to: onboarding/backend/brand-governance/governance-artifact-writer.cjs
      via: submit flow calls writeGovernanceEvidence to produce the branding_governance evidence block in the response.
    - from: onboarding/backend/brand-governance/governance-artifact-writer.cjs
      to: onboarding/backend/brand-governance/closure-gates.cjs
      via: evidence envelope records gate run results for machine-readable audit output per D-08.
---

<objective>
Integrate governance modules additively into the onboarding submit handler and prove tenant isolation and contract integrity guarantees.

Purpose: Fulfills D-05, D-06, D-07, D-08 — closes the governance loop by emitting machine-readable governance evidence on submit, enforcing cross-tenant denial, and validating contract integrity gates; satisfies BRAND-GOV-02 closure.
Output: governance-artifact-writer.cjs, additive handlers.cjs integration, tenant isolation tests, and contract integrity tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-branding-governance-publish-or-rollback-and-closure-gates/78-CONTEXT.md
@.planning/phases/78-branding-governance-publish-or-rollback-and-closure-gates/78-RESEARCH.md
@.planning/phases/78-branding-governance-publish-or-rollback-and-closure-gates/78-VALIDATION.md
@.planning/phases/78-branding-governance-publish-or-rollback-and-closure-gates/78-02-SUMMARY.md
@onboarding/backend/handlers.cjs
@onboarding/backend/brand-governance/bundle-registry.cjs
@onboarding/backend/brand-governance/active-pointer.cjs
@onboarding/backend/brand-governance/drift-auditor.cjs
@onboarding/backend/brand-governance/closure-gates.cjs
@onboarding/backend/brand-governance/governance-diagnostics.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement governance evidence writer and additive handler integration</name>
  <files>onboarding/backend/brand-governance/governance-artifact-writer.cjs, onboarding/backend/handlers.cjs</files>
  <read_first>onboarding/backend/handlers.cjs, onboarding/backend/brand-governance/bundle-registry.cjs, onboarding/backend/brand-governance/closure-gates.cjs, .planning/phases/78-branding-governance-publish-or-rollback-and-closure-gates/78-RESEARCH.md</read_first>
  <action>Implement governance-artifact-writer.cjs exporting writeGovernanceEvidence(tenant_id, bundle_id, gateResults, driftSummary) that returns a machine-readable evidence envelope: { tenant_id, bundle_id, gate_results: gateResults, drift_summary: driftSummary, evidence_hash: sha256(stableSort({ tenant_id, bundle_id, gate_results: gateResults })), written_at: new Date().toISOString() } per D-08. The returned envelope is also used to set verification_evidence_hash on the registry bundle record (call a bundle-registry helper setVerificationEvidence(tenant_id, bundle_id, evidence_hash)) so rollback validation in active-pointer works per D-03. In handlers.cjs, after all existing artifact writers complete, additively add these steps using buildExecutionContext to derive tenant_id per D-06 (no new tenant parsing layer): (1) assemble canonicalArtifacts from existing artifact output fields; (2) call createBundle(tenant_id, canonicalArtifacts); (3) call runClosureGates(tenant_id, bundle, context); (4) call auditDrift(tenant_id, canonicalArtifacts); (5) call writeGovernanceEvidence(tenant_id, bundle.bundle_id, gateResults, driftSummary). Append the returned envelope as branding_governance to the existing json() call following the additive pattern: json(res, 200, { ...existingPayload, branding_governance: evidenceEnvelope }). Do NOT remove, rename, reorder, or shadow any existing response field per D-07 and D-09. Wrap governance steps in a try/catch that sets branding_governance to { error: 'governance_unavailable', machine_readable: true } on failure so submit never regresses on governance errors.</action>
  <verify>node --test test/phase-78/governance-schema.test.js test/phase-78/closure-gates.test.js test/phase-78/publish-rollback.test.js test/phase-78/drift-evidence.test.js</verify>
  <acceptance_criteria>
    - governance-artifact-writer.cjs exports writeGovernanceEvidence returning envelope with evidence_hash and written_at fields.
    - handlers.cjs response includes branding_governance field with tenant_id, bundle_id, gate_results, and drift_summary.
    - All existing response fields remain unchanged; branding_governance is purely additive.
    - No new standalone route added to handlers.cjs.
    - bundle-registry verification_evidence_hash is set after writeGovernanceEvidence succeeds.
  </acceptance_criteria>
  <done>Governance evidence is emitted additively in the submit response with machine-readable envelope and deterministic evidence hash per D-08; rollback candidates carry verification status per D-03.</done>
</task>

<task type="auto">
  <name>Task 2: Create tenant isolation and contract integrity verification tests</name>
  <files>test/phase-78/tenant-isolation.test.js, test/phase-78/contract-integrity.test.js</files>
  <read_first>onboarding/backend/brand-governance/bundle-registry.cjs, onboarding/backend/brand-governance/active-pointer.cjs, onboarding/backend/brand-governance/drift-auditor.cjs, onboarding/backend/brand-governance/closure-gates.cjs</read_first>
  <action>Create test/phase-78/tenant-isolation.test.js asserting four invariants per D-06 Pattern 4 from RESEARCH.md: (a) getBundle(tenant_id_A, bundle_id_from_tenant_B) returns null, not bundle data; (b) getActiveBundle(tenant_id_A) after setting active pointer for tenant_id_B returns null, not tenant-B pointer; (c) auditDrift(tenant_id_A, canonicalArtifacts) returns has_drift: true when the active pointer belongs to tenant_id_B only; (d) publishBundle denies with BRAND_GOV_TENANT_MISMATCH when the request tenant_id differs from bundle.tenant_id. Create test/phase-78/contract-integrity.test.js asserting five contract integrity scenarios per D-05: (a) runClosureGates returns contract_integrity gate passed: false when lineage_fingerprints.strategy is null; (b) same when lineage_fingerprints.identity is absent; (c) same when lineage_fingerprints.design_system is absent; (d) same when lineage_fingerprints.starter is null; (e) contract_integrity gate returns passed: true only when all lane fingerprints are present and non-null strings. Each test uses inline fixtures — do not rely on external fixture files for isolation correctness tests.</action>
  <verify>node --test test/phase-78/tenant-isolation.test.js test/phase-78/contract-integrity.test.js</verify>
  <acceptance_criteria>
    - tenant-isolation.test.js confirms cross-tenant registry read returns null, not bundle data.
    - tenant-isolation.test.js confirms cross-tenant active pointer read returns null.
    - tenant-isolation.test.js confirms auditDrift returns has_drift: true for tenant-A with no A-scoped active pointer.
    - tenant-isolation.test.js confirms publishBundle denies with BRAND_GOV_TENANT_MISMATCH on tenant mismatch.
    - contract-integrity.test.js confirms contract integrity gate fails on each individual null or absent fingerprint lane.
    - contract-integrity.test.js confirms contract integrity gate passes when all lanes are present and non-null.
  </acceptance_criteria>
  <done>Tenant isolation is verified across all governance operations; contract integrity gate is provably fail-closed on incomplete lineage per D-05 and D-06.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] node --test test/phase-78/tenant-isolation.test.js
- [ ] node --test test/phase-78/contract-integrity.test.js
- [ ] node --test test/phase-78/*.test.js
</verification>

<success_criteria>
- BRAND-GOV-02 tenant isolation is provably enforced across all governance module operations.
- BRAND-GOV-02 contract integrity gate is fail-closed on null or absent lineage fingerprint lanes.
- Governance evidence is machine-readable and additive; no existing handler response fields are mutated per D-07 and D-09.
- Full phase test suite passes: node --test test/phase-78/*.test.js.
</success_criteria>

<output>
After completion, create .planning/phases/78-branding-governance-publish-or-rollback-and-closure-gates/78-03-SUMMARY.md
</output>
