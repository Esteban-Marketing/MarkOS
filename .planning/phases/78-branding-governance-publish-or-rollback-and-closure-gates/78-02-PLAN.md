---
phase: 78-branding-governance-publish-or-rollback-and-closure-gates
plan: 02
type: execute
wave: 2
depends_on:
  - "78-01"
files_modified:
  - onboarding/backend/brand-governance/bundle-registry.cjs
  - onboarding/backend/brand-governance/active-pointer.cjs
  - onboarding/backend/brand-governance/drift-auditor.cjs
  - test/phase-78/publish-rollback.test.js
  - test/phase-78/drift-evidence.test.js
autonomous: true
requirements:
  - BRAND-GOV-01

must_haves:
  truths:
    - Bundles once created cannot be mutated; every new governance state produces a new immutable bundle record.
    - Publishing a bundle switches the active pointer to the candidate bundle_id only after all closure gates pass.
    - Rollback switches the active pointer to a prior bundle that carries a verified evidence snapshot and gate status.
    - Drift evidence accurately identifies divergence between the currently active bundle fingerprint and the deterministically recomputed expected bundle from canonical artifact fingerprints.
  artifacts:
    - path: onboarding/backend/brand-governance/bundle-registry.cjs
      provides: Immutable tenant-scoped lineage bundle records supporting create-only (no update/delete), keyed by tenant_id and bundle_id.
    - path: onboarding/backend/brand-governance/active-pointer.cjs
      provides: Publish and rollback operations via pointer-only switch with traceability log entries per operation.
    - path: onboarding/backend/brand-governance/drift-auditor.cjs
      provides: Deterministic drift detection comparing active pointer bundle fingerprint against recomputed expected fingerprint from canonical artifact IDs.
    - path: test/phase-78/publish-rollback.test.js
      provides: Behavioral tests for publish and rollback pointer-switch sequences, gate-blocked deny paths, and traceability log entries.
    - path: test/phase-78/drift-evidence.test.js
      provides: Behavioral tests for drift detection pass and fail conditions across active pointer and recomputed expected bundle scenarios.
  key_links:
    - from: onboarding/backend/brand-governance/active-pointer.cjs
      to: onboarding/backend/brand-governance/bundle-registry.cjs
      via: pointer switch validates bundle exists in registry before accepting publish or rollback operation.
    - from: onboarding/backend/brand-governance/active-pointer.cjs
      to: onboarding/backend/brand-governance/closure-gates.cjs
      via: publishBundle calls runClosureGates and denies on gate failure per D-02 and D-05.
    - from: onboarding/backend/brand-governance/drift-auditor.cjs
      to: onboarding/backend/brand-governance/active-pointer.cjs
      via: drift audit reads active pointer to compare against recomputed expected bundle fingerprint.
---

<objective>
Implement immutable bundle registry, pointer-based publish/rollback module, and deterministic drift auditor.

Purpose: Fulfills D-01, D-02, D-03, D-04, and D-09 — enables safe tenant-scoped promotion and rollback of lineage bundles with full traceability and drift evidence.
Output: bundle-registry.cjs, active-pointer.cjs, drift-auditor.cjs, and behavioral tests for publish/rollback controls and drift evidence.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-branding-governance-publish-or-rollback-and-closure-gates/78-CONTEXT.md
@.planning/phases/78-branding-governance-publish-or-rollback-and-closure-gates/78-RESEARCH.md
@.planning/phases/78-branding-governance-publish-or-rollback-and-closure-gates/78-VALIDATION.md
@.planning/phases/78-branding-governance-publish-or-rollback-and-closure-gates/78-01-SUMMARY.md
@onboarding/backend/brand-governance/governance-diagnostics.cjs
@onboarding/backend/brand-governance/closure-gates.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement immutable bundle registry and publish/rollback active-pointer module</name>
  <files>onboarding/backend/brand-governance/bundle-registry.cjs, onboarding/backend/brand-governance/active-pointer.cjs, test/phase-78/publish-rollback.test.js</files>
  <read_first>onboarding/backend/brand-governance/governance-diagnostics.cjs, onboarding/backend/brand-governance/closure-gates.cjs, .planning/phases/78-branding-governance-publish-or-rollback-and-closure-gates/78-RESEARCH.md</read_first>
  <action>Implement bundle-registry.cjs with createBundle(tenant_id, lineagePayload) that computes bundle_id as sha256(stableSort(lineagePayload)) using Node crypto and the existing stableSort helper, stores the record immutably in an in-memory store keyed by `${tenant_id}:${bundle_id}`, and exposes only getBundle(tenant_id, bundle_id) and listBundles(tenant_id) — no update or delete operations per D-09. createBundle must fail closed (throw or return deny) if any required lane field (strategy_artifact_id, identity_artifact_id, design_system_artifact_id, starter_artifact_id, lineage_fingerprints) is absent. Implement active-pointer.cjs with: (a) publishBundle(tenant_id, candidate_bundle_id, { actor_id, reason }) that calls runClosureGates from closure-gates.cjs on the candidate bundle, returns { denied: true, reason_code: BRAND_GOV_CLOSURE_GATE_FAIL, diagnostics } if any gate fails per D-02 and D-05, otherwise switches the active pointer and appends traceability entry { action: 'publish', tenant_id, bundle_id: candidate_bundle_id, actor_id, reason, timestamp: new Date().toISOString() } per D-10; (b) rollbackBundle(tenant_id, target_bundle_id, { actor_id, reason }) that checks the target bundle exists in the registry with a verification_evidence_hash field (set by governance-artifact-writer in plan 03) and denies with BRAND_GOV_BUNDLE_NOT_VERIFIED if absent per D-03, otherwise switches pointer and appends traceability entry with action: 'rollback'; (c) getActiveBundle(tenant_id) returning the current pointer record or null. Use DENY_CODES from governance-diagnostics.cjs for all denial reason codes per D-10. No standalone route per D-07.</action>
  <verify>node --test test/phase-78/publish-rollback.test.js</verify>
  <acceptance_criteria>
    - publish-rollback.test.js confirms publishBundle denies with BRAND_GOV_CLOSURE_GATE_FAIL when any closure gate fails.
    - publish-rollback.test.js confirms publishBundle switches active pointer and appends traceability entry on gate pass.
    - publish-rollback.test.js confirms rollbackBundle denies with BRAND_GOV_BUNDLE_NOT_VERIFIED when target bundle lacks verification_evidence_hash.
    - publish-rollback.test.js confirms rollbackBundle switches active pointer to valid target with traceability entry action: 'rollback'.
    - bundle-registry.cjs exposes no mutate or delete operation.
  </acceptance_criteria>
  <done>Publish and rollback operations are pointer-only, gate-guarded, immutable, and fully traceable per D-01 through D-03 and D-09.</done>
</task>

<task type="auto">
  <name>Task 2: Implement deterministic drift auditor and drift evidence tests</name>
  <files>onboarding/backend/brand-governance/drift-auditor.cjs, test/phase-78/drift-evidence.test.js</files>
  <read_first>onboarding/backend/brand-governance/bundle-registry.cjs, onboarding/backend/brand-governance/active-pointer.cjs, .planning/phases/78-branding-governance-publish-or-rollback-and-closure-gates/78-RESEARCH.md</read_first>
  <action>Implement drift-auditor.cjs with auditDrift(tenant_id, canonicalArtifacts) that: (1) recomputes expected bundle fingerprint from { strategy_artifact_id, identity_artifact_id, design_system_artifact_id, starter_artifact_id } using buildFingerprint(stableSort(canonicalArtifacts)) — the same stable-sort + fingerprint pattern used in existing brand modules; (2) reads the active pointer via getActiveBundle(tenant_id), treating null as has_drift: true per D-04 (no active pointer = drift); (3) returns machine-readable drift evidence: { tenant_id, has_drift: boolean, expected_fingerprint: string, active_fingerprint: string | null, evidence_hash: sha256(stableSort({ tenant_id, has_drift, expected_fingerprint, active_fingerprint })), evaluated_at: new Date().toISOString() } per D-08. Create test/phase-78/drift-evidence.test.js with three scenarios: (a) no-drift — canonicalArtifacts matches active bundle fingerprint, has_drift: false; (b) drift-detected — active bundle fingerprint differs from recomputed expected, has_drift: true; (c) no-active-pointer — getActiveBundle returns null, has_drift: true. Each scenario asserts evidence_hash is a non-empty string and evaluated_at is present.</action>
  <verify>node --test test/phase-78/drift-evidence.test.js</verify>
  <acceptance_criteria>
    - drift-evidence.test.js confirms auditDrift returns has_drift: false when active and expected fingerprints match.
    - drift-evidence.test.js confirms auditDrift returns has_drift: true when active bundle fingerprint diverges from recomputed expected.
    - drift-evidence.test.js confirms auditDrift returns has_drift: true when no active pointer exists.
    - Drift evidence payload includes evidence_hash and evaluated_at in all three scenarios per D-08.
  </acceptance_criteria>
  <done>Drift auditor produces deterministic, machine-readable evidence for divergence between active bundle and canonical artifact lineage per D-04.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] node --test test/phase-78/publish-rollback.test.js
- [ ] node --test test/phase-78/drift-evidence.test.js
- [ ] node --test test/phase-78/publish-rollback.test.js test/phase-78/drift-evidence.test.js
</verification>

<success_criteria>
- BRAND-GOV-01 publish and rollback controls are pointer-only, gate-guarded, and immutable with traceability entries.
- BRAND-GOV-01 drift evidence is deterministic and machine-readable with correct has_drift signal in all three scenarios.
- All governance operations are tenant-scoped and fail-closed per D-06.
- No standalone public route additions per D-07.
</success_criteria>

<output>
After completion, create .planning/phases/78-branding-governance-publish-or-rollback-and-closure-gates/78-02-SUMMARY.md
</output>
