---
phase: 64.3-gsd-verification-and-customization-closure
plan: 03
type: execute
wave: 2
depends_on:
  - 64.2-gsd-surface-and-instruction-alignment
  - 64.3-01
files_modified:
  - .planning/codebase/GSD-CUSTOMIZATION-BOUNDARY.md
  - .planning/codebase/FILES.md
  - .planning/codebase/STRUCTURE.md
autonomous: true
requirements:
  - GSD-ALIGN-07

must_haves:
  truths:
    - "The long-lived `.planning/codebase/` map now states which surfaces are canonical shared framework content, localized runtime content, and client-owned override content."
    - "The ownership boundary is visible from the normal codebase inventories, not buried only in phase-local artifacts."
    - "The documentation preserves the 64.2 ownership model: root `copilot-instructions.md` is the shared project contract, root `CLAUDE.md` is the localized contract, `.github/**` stays canonical shared content, `.claude/**` stays localized, and `.markos-local/**` stays client-owned."
  artifacts:
    - path: ".planning/codebase/GSD-CUSTOMIZATION-BOUNDARY.md"
      provides: "Canonical ownership-boundary reference for shared `.github`, localized `.claude`, and client-owned `.markos-local` customization surfaces."
      exports: []
    - path: ".planning/codebase/FILES.md"
      provides: "Updated root-file inventory that points readers at the new boundary contract and names the dual-root instruction files."
      exports: []
    - path: ".planning/codebase/STRUCTURE.md"
      provides: "Updated top-level responsibility map that exposes where shared, localized, and client-override work belongs."
      exports: []
  key_links:
    - from: ".planning/codebase/FILES.md"
      to: ".planning/codebase/GSD-CUSTOMIZATION-BOUNDARY.md"
      via: "Root file inventory points readers to the canonical boundary document instead of duplicating ownership truth."
      pattern: "GSD-CUSTOMIZATION-BOUNDARY\.md|copilot-instructions\.md|CLAUDE\.md"
    - from: ".planning/codebase/STRUCTURE.md"
      to: ".planning/codebase/GSD-CUSTOMIZATION-BOUNDARY.md"
      via: "Top-level structure summary routes ownership questions to the canonical boundary document."
      pattern: "GSD-CUSTOMIZATION-BOUNDARY\.md|\.github|\.claude|\.markos-local"
---

<objective>
Create the durable codebase-level ownership contract for the dual-root GSD system and make it discoverable from the existing inventories.

Purpose: satisfy D-05 and D-06 so future phases do not have to rediscover the 64.2/64.3 ownership boundary from phase-local artifacts.
Output: one canonical `.planning/codebase/` boundary document plus inventory updates in `FILES.md` and `STRUCTURE.md` that surface the same model.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/FILES.md
@.planning/codebase/STRUCTURE.md
@.planning/phases/33-codebase-documentation-mapping/33-01-SUMMARY.md
@.planning/phases/33-codebase-documentation-mapping/33-04-SUMMARY.md
@.planning/phases/64.2-gsd-surface-and-instruction-alignment/64.2-CONTEXT.md
@.planning/phases/64.2-gsd-surface-and-instruction-alignment/64.2-VERIFICATION.md
@.planning/phases/64.3-gsd-verification-and-customization-closure/64.3-CONTEXT.md
@.planning/phases/64.3-gsd-verification-and-customization-closure/64.3-RESEARCH.md
</context>

<threat_model>
  <threat>
    <severity>medium</severity>
    <risk>The new boundary doc duplicates topology truth in conflicting language across multiple docs.</risk>
    <mitigation>Keep the new document canonical and update `FILES.md` and `STRUCTURE.md` to point at it rather than restating full ownership rules independently.</mitigation>
  </threat>
  <threat>
    <severity>high</severity>
    <risk>The documentation accidentally redefines ownership by collapsing `.claude` into `.github`, treating `.github/copilot-instructions.md` as the project contract, or positioning `.markos-local` as a framework surface.</risk>
    <mitigation>Encode the locked 64.2/64.3 boundary literally: `.github/**` shared canonical, `.claude/**` localized runtime, `.markos-local/**` client overrides only, root `copilot-instructions.md` shared project contract, root `CLAUDE.md` localized project contract. If the doc cannot be written without violating those rules, stop and escalate.</mitigation>
    <blocker>true</blocker>
  </threat>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 64.3-03-01: Create the canonical GSD customization-boundary document</name>
  <read_first>.planning/codebase/CONVENTIONS.md, .planning/phases/64.2-gsd-surface-and-instruction-alignment/64.2-CONTEXT.md, .planning/phases/64.2-gsd-surface-and-instruction-alignment/64.2-VERIFICATION.md, .planning/phases/64.3-gsd-verification-and-customization-closure/64.3-CONTEXT.md, .planning/phases/64.3-gsd-verification-and-customization-closure/64.3-RESEARCH.md</read_first>
  <files>.planning/codebase/GSD-CUSTOMIZATION-BOUNDARY.md</files>
  <acceptance_criteria>
    - A new file exists at `.planning/codebase/GSD-CUSTOMIZATION-BOUNDARY.md`.
    - The document includes explicit sections or tables for shared `.github/**`, localized `.claude/**`, and client-owned `.markos-local/**` surfaces.
    - The document explicitly names root `copilot-instructions.md` as the shared project contract and root `CLAUDE.md` as the localized project contract.
    - The document explicitly states that `.github/copilot-instructions.md` is a hidden shared-framework artifact and not a substitute for root `copilot-instructions.md`.
  </acceptance_criteria>
  <action>
Create `.planning/codebase/GSD-CUSTOMIZATION-BOUNDARY.md` as the canonical ownership guide required by D-05. Structure it so future planners can answer three questions quickly: what is shared canonical framework content, what is localized runtime content, and what is client-owned override content. Include a concise purpose section, one ownership table for `.github/**`, one ownership table for `.claude/**`, one ownership table for `.markos-local/**`, and one short section on root instruction artifacts that explicitly distinguishes root `copilot-instructions.md`, root `CLAUDE.md`, and hidden `.github/copilot-instructions.md`. Do not reopen the 64.2 model, do not suggest repointing `.claude/skills/**` into `.github/**`, and do not broaden into installer or runtime redesign.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const text=fs.readFileSync('.planning/codebase/GSD-CUSTOMIZATION-BOUNDARY.md','utf8'); for (const needle of ['.github/**','.claude/**','.markos-local/**','copilot-instructions.md','CLAUDE.md','.github/copilot-instructions.md']) { if (!text.includes(needle)) throw new Error('missing '+needle); }"</automated>
  </verify>
  <done>
There is one durable codebase-level document that captures the exact shared-versus-localized-versus-client boundary for the dual-root GSD system.
  </done>
</task>

<task type="auto">
  <name>Task 64.3-03-02: Patch FILES.md and STRUCTURE.md to surface the boundary from normal codebase maps</name>
  <read_first>.planning/codebase/FILES.md, .planning/codebase/STRUCTURE.md, .planning/codebase/GSD-CUSTOMIZATION-BOUNDARY.md, .planning/phases/33-codebase-documentation-mapping/33-04-SUMMARY.md</read_first>
  <files>.planning/codebase/FILES.md, .planning/codebase/STRUCTURE.md</files>
  <acceptance_criteria>
    - `FILES.md` explicitly lists root `copilot-instructions.md` alongside `CLAUDE.md` and points readers to `GSD-CUSTOMIZATION-BOUNDARY.md` for ownership truth.
    - `STRUCTURE.md` explicitly names `.github/`, `.claude/`, and `.markos-local/` responsibilities and points to `GSD-CUSTOMIZATION-BOUNDARY.md`.
    - Neither file contradicts the canonical boundary document.
  </acceptance_criteria>
  <action>
Update `.planning/codebase/FILES.md` so the root-file inventory no longer omits `copilot-instructions.md`, clarifies the dual-root instruction pair, and links ownership questions to `.planning/codebase/GSD-CUSTOMIZATION-BOUNDARY.md`. Update `.planning/codebase/STRUCTURE.md` so the top-level placement rules include `.github/` as canonical shared framework content, `.claude/` as localized runtime and dispatch content, and `.markos-local/` as client override content; add a short note that the full ownership contract lives in `.planning/codebase/GSD-CUSTOMIZATION-BOUNDARY.md`. Keep both docs summary-level per the Phase 33 hierarchy, not full duplicates of the new boundary doc.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const files=fs.readFileSync('.planning/codebase/FILES.md','utf8'); const structure=fs.readFileSync('.planning/codebase/STRUCTURE.md','utf8'); if (!/copilot-instructions\.md/.test(files) || !/CLAUDE\.md/.test(files)) throw new Error('FILES missing dual-root artifacts'); for (const text of [files, structure]) { if (!/GSD-CUSTOMIZATION-BOUNDARY\.md/.test(text)) throw new Error('missing boundary pointer'); } if (!/\.github\//.test(structure) || !/\.claude\//.test(structure) || !/\.markos-local\//.test(structure)) throw new Error('STRUCTURE missing ownership dirs');"</automated>
  </verify>
  <done>
The normal codebase inventory docs expose the ownership model and route readers to the canonical boundary doc instead of hiding the rule in phase-local artifacts.
  </done>
</task>

</tasks>

<verification>
Plan 03 is complete only when the ownership boundary is durable at `.planning/codebase/` level and both inventory docs surface it without duplicating or contradicting the canonical contract.
</verification>

<success_criteria>
Future work can discover the dual-root ownership model directly from `.planning/codebase/` without reopening 64.2 decisions or reading only the 64.2/64.3 phase folders.
</success_criteria>

<output>
After completion, create `.planning/phases/64.3-gsd-verification-and-customization-closure/64.3-03-SUMMARY.md`
</output>
