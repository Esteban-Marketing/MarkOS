---
phase: 64.3-gsd-verification-and-customization-closure
scope: user-home-global-follow-through
status: pending-human-follow-through
created: 2026-04-06T20:20:00Z
---

# Phase 64.3 Human Follow-Through

This file covers only the instruction targets that live outside the repository workspace. Repo-local proof is already recorded in `64.3-VERIFICATION.md`; do not treat the checklist below as automated evidence.

## Why This Is Separate

Both root-instruction policy helpers advertise `getProjectInstructionTargets({ global: true })` under the user home `.claude` directory. The repository can prove the generator logic and the repo-local outputs, but it cannot honestly claim the current user-home files are fresh unless a human runs the generators against those paths.

Current user-home targets:

- `C:\Users\User PC\.claude\copilot-instructions.md`
- `C:\Users\User PC\.claude\CLAUDE.md`

## Manual Checks

### 1. Refresh the shared user-home contract

Run from the repository root:

```powershell
node ".github/get-shit-done/bin/gsd-tools.cjs" generate-claude-md --output "C:\Users\User PC\.claude\copilot-instructions.md"
```

Expected result:

- `C:\Users\User PC\.claude\copilot-instructions.md` exists.
- The file contains the managed shared project-contract sections.
- The user-home file reflects the shared `.github` lane, not the localized `.claude` lane.

### 2. Refresh the localized user-home contract

Run from the repository root:

```powershell
node ".claude/get-shit-done/bin/gsd-tools.cjs" generate-claude-md --output "C:\Users\User PC\.claude\CLAUDE.md"
```

Expected result:

- `C:\Users\User PC\.claude\CLAUDE.md` exists.
- The file contains the managed localized runtime-contract sections.
- The user-home file reflects the localized `.claude` lane and remains distinct from `copilot-instructions.md`.

### 3. Spot-check the dual-root separation in user-home

Confirm the two user-home files are both present and not interchangeable:

- `C:\Users\User PC\.claude\copilot-instructions.md` should be the shared project contract.
- `C:\Users\User PC\.claude\CLAUDE.md` should be the localized project contract.
- Neither file should be replaced by the hidden repository artifact `.github/copilot-instructions.md`.

### 4. Record completion

After the manual checks pass, update this file:

- Change `status` to `complete`.
- Add the date and the operator who performed the follow-through.
- If either file differs unexpectedly, record the mismatch before changing anything else.

## Completion Record

- [ ] Shared user-home `copilot-instructions.md` regenerated and spot-checked
- [ ] Localized user-home `CLAUDE.md` regenerated and spot-checked
- [ ] Dual-root separation confirmed in user-home `.claude`
- [ ] This file status flipped to `complete` after manual verification