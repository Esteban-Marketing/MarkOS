---
phase: 64.3-gsd-verification-and-customization-closure
plan: 02
type: execute
wave: 2
depends_on:
  - 64.2-gsd-surface-and-instruction-alignment
  - 64.3-01
files_modified:
  - tools/gsd-refresh-manifest.cjs
  - package.json
  - .github/gsd-file-manifest.json
  - .claude/gsd-file-manifest.json
autonomous: true
requirements:
  - GSD-ALIGN-06

must_haves:
  truths:
    - "The repository has one reusable repo-local helper that deterministically refreshes both GSD manifests without manual hash editing."
    - "Shared and localized GSD manifests can be regenerated using the same SHA-256 file-walk pattern already used by MarkOS runtime tooling."
    - "After the helper runs, `.github/gsd-file-manifest.json` and `.claude/gsd-file-manifest.json` describe the live trees instead of stale 64.2-era content."
  artifacts:
    - path: "tools/gsd-refresh-manifest.cjs"
      provides: "Repo-local CLI helper that recomputes deterministic manifest hashes for `.github` and `.claude` GSD trees using the existing JSON shape."
      exports: []
    - path: ".github/gsd-file-manifest.json"
      provides: "Fresh shared GSD hash inventory for the live `.github/get-shit-done/**` tree."
      exports: []
    - path: ".claude/gsd-file-manifest.json"
      provides: "Fresh localized GSD hash inventory for the live `.claude/get-shit-done/**` tree."
      exports: []
    - path: "package.json"
      provides: "Documented repo-local command entrypoint for deterministic GSD manifest refresh."
      exports: []
  key_links:
    - from: "tools/gsd-refresh-manifest.cjs"
      to: "bin/cli-runtime.cjs"
      via: "The helper reuses the repo's established `buildFileHashes()` SHA-256 pattern instead of inventing a new hashing contract."
      pattern: "sha256|buildFileHashes|digest\\('hex'\\)"
    - from: "tools/gsd-refresh-manifest.cjs"
      to: ".github/gsd-file-manifest.json"
      via: "Helper writes the shared manifest in the existing `version`, `timestamp`, `files` shape."
      pattern: "version|timestamp|files"
    - from: "tools/gsd-refresh-manifest.cjs"
      to: ".claude/gsd-file-manifest.json"
      via: "Helper writes the localized manifest in the same deterministic shape as the shared manifest."
      pattern: "version|timestamp|files"
---

<objective>
Add the reusable refresh primitive that 64.3 needs for manifest closure and use it to regenerate both stale GSD manifests.

Purpose: satisfy D-01 and D-02 with one repo-local command that mirrors the existing MarkOS hash pattern and removes all manual manifest surgery from this closure path.
Output: a checked-in refresh helper, an optional `package.json` script entry for that helper, and freshly regenerated `.github` and `.claude` manifest files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/64.3-gsd-verification-and-customization-closure/64.3-CONTEXT.md
@.planning/phases/64.3-gsd-verification-and-customization-closure/64.3-RESEARCH.md
@.planning/phases/64.3-gsd-verification-and-customization-closure/64.3-VALIDATION.md
@bin/cli-runtime.cjs
@bin/update.cjs
@.github/gsd-file-manifest.json
@.claude/gsd-file-manifest.json
@package.json
</context>

<threat_model>
  <threat>
    <severity>medium</severity>
    <risk>The helper writes a new manifest shape or hash scheme that no longer matches the existing GSD manifest contract.</risk>
    <mitigation>Reuse the `sha256` walk pattern from `bin/cli-runtime.cjs` and preserve the current top-level manifest fields exactly as `version`, `timestamp`, and `files`.</mitigation>
  </threat>
  <threat>
    <severity>high</severity>
    <risk>The helper targets the wrong directories or broadens into install/update/runtime redesign, causing closure work to mutate unrelated surfaces.</risk>
    <mitigation>Limit the helper to `.github/get-shit-done/**` and `.claude/get-shit-done/**` inputs with outputs only to `.github/gsd-file-manifest.json` and `.claude/gsd-file-manifest.json`. If satisfying the plan would require touching installer or runtime ownership logic, stop and escalate.</mitigation>
    <blocker>true</blocker>
  </threat>
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 64.3-02-01: Add a deterministic repo-local GSD manifest refresh helper</name>
  <read_first>package.json, bin/cli-runtime.cjs, bin/update.cjs, .planning/phases/64.3-gsd-verification-and-customization-closure/64.3-CONTEXT.md, .planning/phases/64.3-gsd-verification-and-customization-closure/64.3-RESEARCH.md</read_first>
  <files>tools/gsd-refresh-manifest.cjs, package.json</files>
  <acceptance_criteria>
    - A checked-in executable helper exists at `tools/gsd-refresh-manifest.cjs`.
    - The helper walks file trees with `sha256` hashing and normalizes relative paths to `/`, matching the established `buildFileHashes()` pattern.
    - The helper supports both manifest targets and writes JSON with the keys `version`, `timestamp`, and `files`.
    - `package.json` exposes a repo-local script or command entry that invokes the helper without referencing user-home paths.
  </acceptance_criteria>
  <behavior>
    - Test 1: Running the helper for the shared tree produces a manifest whose `files` entries hash the live `.github/get-shit-done/**` files.
    - Test 2: Running the helper for the localized tree produces a manifest whose `files` entries hash the live `.claude/get-shit-done/**` files.
    - Test 3: Path formatting is stable on Windows and uses forward slashes in manifest keys.
  </behavior>
  <action>
Create `tools/gsd-refresh-manifest.cjs` as the repo-local refresh helper required by D-01 and D-02. Base the implementation on the existing `buildFileHashes()` logic in `bin/cli-runtime.cjs` and the manifest-writing pattern already used in `bin/update.cjs`: compute SHA-256 hashes for each file, normalize manifest keys to forward slashes, and write one JSON object per target containing `version`, `timestamp`, and `files`. Scope the helper to exactly two source trees and outputs: `.github/get-shit-done` -> `.github/gsd-file-manifest.json` and `.claude/get-shit-done` -> `.claude/gsd-file-manifest.json`. Add a `package.json` script such as `gsd:refresh-manifests` that runs the helper in repo-local mode. Do not make the helper update installer manifests, user-home files, or any `.agent` or `.markos-local` surfaces.
  </action>
  <verify>
    <automated>node tools/gsd-refresh-manifest.cjs --check</automated>
  </verify>
  <done>
The repo has one deterministic, reusable command for refreshing both GSD manifests and that command stays within the 64.3 closure boundary.
  </done>
</task>

<task type="auto">
  <name>Task 64.3-02-02: Refresh the shared and localized GSD manifests with the new helper</name>
  <read_first>tools/gsd-refresh-manifest.cjs, .github/gsd-file-manifest.json, .claude/gsd-file-manifest.json, .planning/phases/64.3-gsd-verification-and-customization-closure/64.3-VALIDATION.md</read_first>
  <files>.github/gsd-file-manifest.json, .claude/gsd-file-manifest.json</files>
  <acceptance_criteria>
    - `.github/gsd-file-manifest.json` and `.claude/gsd-file-manifest.json` both change only through the helper output, not by manual edits.
    - Each manifest contains a fresh `timestamp` and a populated `files` object.
    - The refreshed manifests include entries for the 64.2-touched generator files such as `bin/lib/profile-output.cjs` and no stale hash mismatch remains for those files.
  </acceptance_criteria>
  <action>
Run the new repo-local helper to rewrite both checked-in GSD manifest files from live disk state. Commit the generated JSON exactly as written by the helper, preserving the existing shape and file ordering chosen by the implementation. Confirm that entries exist for the touched 64.2 generator and workflow surfaces named in 64.3 research, including `bin/lib/profile-output.cjs` in both trees. Do not hand-edit hash strings or timestamps after generation.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const crypto=require('crypto'); const path=require('path'); function check(root, manifestPath, relPath){ const manifest=JSON.parse(fs.readFileSync(manifestPath,'utf8')); if (!manifest.timestamp || !manifest.files || typeof manifest.files!=='object') throw new Error('bad manifest shape '+manifestPath); if (!manifest.files[relPath]) throw new Error('missing '+relPath+' in '+manifestPath); const full=path.join(root, relPath.replace(/\//g,path.sep)); const hash=crypto.createHash('sha256').update(fs.readFileSync(full)).digest('hex'); if (manifest.files[relPath]!==hash) throw new Error('stale hash for '+relPath+' in '+manifestPath); } check('.github/get-shit-done','.github/gsd-file-manifest.json','bin/lib/profile-output.cjs'); check('.claude/get-shit-done','.claude/gsd-file-manifest.json','bin/lib/profile-output.cjs');"</automated>
  </verify>
  <done>
Both checked-in GSD manifests are freshly regenerated from live files and no longer lag the 64.2 alignment work.
  </done>
</task>

</tasks>

<verification>
Plan 02 is incomplete until one helper can regenerate both manifests repeatably and the checked-in manifest hashes match live files in both trees without manual intervention.
</verification>

<success_criteria>
The repo contains a reusable manifest-refresh command aligned to existing MarkOS hash patterns, and the shared plus localized GSD manifests are demonstrably fresh after running it.
</success_criteria>

<output>
After completion, create `.planning/phases/64.3-gsd-verification-and-customization-closure/64.3-02-SUMMARY.md`
</output>
