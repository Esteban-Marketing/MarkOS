---
phase: 11-rich-examples
plan: 01
type: execute
status: complete
---
# Phase 11 — Plan 01: Seed Schema + UI Enrichment

**Phase:** 11 — Rich Business-Model Examples  
**Plan:** 01 of 05  
**Status:** ✅ COMPLETE  
**Completed:** 2026-03-25

---

## Objective

Extend `onboarding-seed.schema.json` to v2.1 by adding `company.business_model` (required enum, 7 values) and 9 conditional fields for product and audience. Update the onboarding UI to render the `business_model` selector and conditionally show/hide the new fields.

## Deliverables

- `onboarding/onboarding-seed.schema.json` — v2.1 with `business_model` + 9 conditional fields
- `onboarding/index.html` — `business_model` selector in Step 1; 8 conditional form groups in Steps 2–3 with `data-model-group` attributes
- `onboarding/onboarding.js` — `MODEL_GROUPS` map, `onBusinessModelChange()`, `buildSeed()` v2.1
- `onboarding/onboarding.css` — `.model-hidden` class (replaces inline `display:none`)

## Acceptance Criteria

- [x] `business_model` is first field in Step 1
- [x] Conditional fields show/hide based on selected model
- [x] No inline CSS styles — lint clean
- [x] `buildSeed()` returns all new fields in seed object

