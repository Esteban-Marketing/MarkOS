---
phase: 11-rich-examples
plan: 05
type: execute
status: complete
---
# Phase 11 — Plan 05: Filler Agent Injection

**Phase:** 11 — Rich Business-Model Examples  
**Plan:** 05 of 05  
**Status:** ✅ COMPLETE  
**Completed:** 2026-03-25

---

## Objective

Wire `example-resolver.cjs` into both filler agents so that every LLM prompt for a templated section receives the correct model-specific reference example automatically.

## Deliverables

### `onboarding/backend/agents/mir-filler.cjs` — modified
- `require('./example-resolver.cjs')` imported
- `path` required; `AUDIENCES_DIR` + `CORE_STRAT_DIR` constants defined
- `generateAudienceProfile()` — calls `resolveExample('AUDIENCES', company.business_model, AUDIENCES_DIR)`, prepends block to prompt
- `generateCompanyProfile()` — adds `Business Model: ${company.business_model}` field to prompt context

### `onboarding/backend/agents/msp-filler.cjs` — modified
- Same path + resolver import
- `generateBrandVoice()` — calls `resolveExample('BRAND-VOICE', company.business_model, CORE_STRAT_DIR)`
- `generateChannelStrategy()` — calls `resolveExample('CHANNEL-STRATEGY', company.business_model, MSP_STRAT_DIR)`
- Business Model field added to both prompt contexts

## Injection Point (in each prompt)

```
${content fields}

${exampleBlock}
Write:
## [Template sections...]
```

## Acceptance Criteria

- [x] Both fillers import and call `resolveExample`
- [x] Prompts include `Business Model:` field
- [x] Graceful degradation — empty `exampleBlock` is a no-op
- [x] All generator functions that have a Tier 1 example file receive injection

