---
phase: 213.3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(markos)/settings/billing/page.module.css
  - app/(markos)/settings/billing/page.tsx
  - app/(markos)/settings/billing/page-shell.tsx
  - app/(markos)/settings/billing/page.stories.tsx
autonomous: true
requirements:
  - B-1
  - B-2
  - B-3
  - B-4
  - B-5
must_haves:
  truths:
    - "app/(markos)/settings/billing/page.module.css contains zero inline hex (whitelist: 1px borders, 2px focus offsets, 8px meter track height, canonical alpha-tint formulas via primitive only); zero off-grid spacing; zero Sora/Space Grotesk/gradients/box-shadow/transform-translate; zero teal #0d9488/#0f766e/#14b8a6/#e6fffb/#0b877c"
    - "app/(markos)/settings/billing/page-shell.tsx renders the billing-hold state via `<div className=\"c-notice c-notice--warning\"><strong>[warn]</strong>{...}</div>` (B-3); no `.holdCard`/`.holdTitle`/`.holdText` className references remain"
    - "app/(markos)/settings/billing/page-shell.tsx renders invoice list as semantic `<table>` with `<thead><tr><th>` + `<tbody><tr><td>` structure; no `.invoiceRow` grid composition (B-4)"
    - "app/(markos)/settings/billing/page-shell.tsx composes `.c-card`, `.c-button c-button--primary`, `.c-button c-button--secondary`, `.t-label-caps` per UI-SPEC Surface 1 primitive reuse table"
    - "app/(markos)/settings/billing/page.stories.tsx (extended) declares ≥3 named state stories: `Default`, `HoldState`, `InvoiceList` per UI-SPEC Storybook State Coverage table"
    - "Banned-lexicon scan = 0 in modified `.tsx` files; no exclamation points; no emoji; no \"Please\" softeners (CONTEXT D-07)"
    - "DOM tree + prop interface UNCHANGED on `page.tsx`/`page-shell.tsx` per CONTEXT D-05 (className-only rewrite + .c-notice JSX swap + invoice-table-shape JSX swap; preserves server/client boundaries)"
    - "`npm run build` exits 0; `npm run build-storybook` exits 0; `node --test test/` exits 0 (existing wiring preserved per CONTEXT D-19)"
  artifacts:
    - path: "app/(markos)/settings/billing/page.module.css"
      provides: "Token-only billing layout module — composition + width override + grid layout only; primitives carry button/card/notice styling JSX-side"
      min_lines: 35
      contains: "var(--color-surface)"
    - path: "app/(markos)/settings/billing/page-shell.tsx"
      provides: "Updated billing shell — composes `.c-card`/`.c-button--primary`/`.c-button--secondary`/`.c-notice c-notice--warning`/semantic `<table>`; preserves Phase 205 Pricing Engine + billing wiring"
      contains: "c-notice c-notice--warning"
    - path: "app/(markos)/settings/billing/page.stories.tsx"
      provides: "Storybook 8 CSF3 ≥3 named state exports (Default / HoldState / InvoiceList)"
      exports: ["default", "Default", "HoldState", "InvoiceList"]
  key_links:
    - from: "app/(markos)/settings/billing/page-shell.tsx"
      to: "styles/components.css `.c-notice c-notice--warning` primitive"
      via: "className composition for billing-hold banner"
      pattern: "c-notice\\s+c-notice--warning"
    - from: "app/(markos)/settings/billing/page.module.css"
      to: "app/tokens.css var(--*)"
      via: "var(--color-surface), var(--space-md), var(--space-lg), var(--space-xs)"
      pattern: "var\\(--(color|space|fs|fw|radius)-"
    - from: "app/(markos)/settings/billing/page-shell.tsx"
      to: "semantic `<table>` invoice rendering"
      via: "<table><thead><tr><th>...</th></tr></thead><tbody><tr><td>...</td></tr></tbody></table>"
      pattern: "<table[^>]*>"
---

<objective>
Redesign `/settings/billing` (`page.module.css` 155 LOC + `page.tsx` 15 LOC + `page-shell.tsx` 129 LOC) into the canonical DESIGN.md v1.1.0 visual contract per `213.3-UI-SPEC.md` Surface 1 (5 ACs: B-1 through B-5). Compose `.c-card` / `.c-button--primary` / `.c-button--secondary` / `.c-notice c-notice--warning` (NEW primitive — DESIGN.md v1.1.0) for the billing-hold banner, semantic `<table>` for invoices, and `.t-label-caps` for eyebrows. Extend existing `page.stories.tsx` with ≥3 named state stories (`Default` / `HoldState` / `InvoiceList`) per UI-SPEC X-4 + Storybook State Coverage.

Purpose: Close UI-SPEC AC B-1 (zero inline hex), B-2 (zero off-grid spacing), B-3 (`.holdCard` → `.c-notice c-notice--warning` with `[warn]` glyph), B-4 (invoice grid → semantic `<table>` with token-cited `<th>`/`<td>`), B-5 (Sora/Space Grotesk/teal/`#0d9488`/`#0f766e`/drop-shadow/20px-28px-radius eliminated). Preserve Phase 205 Pricing Engine wiring + Phase 200/204 billing copy parity (CONTEXT D-23). Open Plans 213.3-02..09 by establishing the per-surface plan structure that subsequent surface plans mirror (single primary CTA + `.c-notice` composition + Storybook state coverage).

Output: `app/(markos)/settings/billing/page.module.css` rewritten (155 → ~55 LOC, token-only layout shell); `app/(markos)/settings/billing/page-shell.tsx` updated (className-only rewrite + `.c-notice` JSX + semantic `<table>` JSX; DOM-shape preserved otherwise); `app/(markos)/settings/billing/page.tsx` minor update if it carries any wrapper className; `app/(markos)/settings/billing/page.stories.tsx` extended with state args (existing default export retained, new state exports added).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-CONTEXT.md
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-UI-SPEC.md
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-RESEARCH.md
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-VALIDATION.md
@DESIGN.md
@CLAUDE.md
@app/tokens.css
@app/globals.css
@styles/components.css
@app/(markos)/settings/billing/page.module.css
@app/(markos)/settings/billing/page.tsx
@app/(markos)/settings/billing/page-shell.tsx
@app/(markos)/settings/billing/page.stories.tsx
@.planning/phases/213.2-ui-canon-adoption-wave-2-auth-surfaces-login-signup-invite-oauth-consent-to-design-md-tokens/213.2-01-PLAN.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase + RESEARCH.md class-rename map. -->

From `styles/components.css` v1.1.0 (Phase 213.1 deliverable + v1.1.0 `.c-notice` extension):
```css
.c-card { background: var(--color-surface-raised); border: 1px solid var(--color-border); border-radius: var(--radius-lg); padding: var(--space-md); }
.c-button { /* var(--h-control) 40px height, primary/secondary/tertiary/destructive variants */ }
.c-button.c-button--primary { background: var(--color-primary); color: var(--color-on-primary); }
.c-button.c-button--secondary { background: var(--color-surface-raised); border: 1px solid var(--color-border); color: var(--color-on-surface); }
.c-notice { /* 4px border-inline-start accent + 12% alpha-tint bg + state-color text */ }
.c-notice--warning { /* warn variant: rgb(255 184 0 / 0.12) bg + var(--color-warning) text + border-inline-start */ }
.c-notice a:hover { color: var(--color-{state}-hover); }
.t-label-caps { /* uppercase, label-caps utility from globals.css */ }
```

From `213.3-RESEARCH.md` Class-Rename Map: Surface 1 — Billing (lines 325–354):
- `.page` → Rewrite local: `padding: var(--space-md); background: var(--color-surface); display: flex; flex-direction: column; gap: var(--space-md);`
- `.shell` → Rewrite local: `max-width: 1200px; margin: 0 auto; display: grid; gap: var(--space-lg);`
- `.summaryCard`, `.panel` → DELETE; compose `.c-card` JSX-side
- `.eyebrow` → DELETE; apply `.t-label-caps` utility JSX-side
- `.title` → DELETE; plain `<h2>` (globals.css)
- `.summaryText`, `.bodyText` → DELETE; body inherits from `<p>` globals
- `.actionRow` → Rewrite local: `display: flex; gap: var(--space-xs); flex-wrap: wrap;`
- `.primaryButton` → DELETE; compose `.c-button c-button--primary`
- `.secondaryButton` → DELETE; compose `.c-button c-button--secondary`
- `.contentGrid` → Rewrite local: `display: grid; grid-template-columns: 1.4fr 1fr; gap: var(--space-md); @media (max-width: 959.98px) { grid-template-columns: 1fr; }`
- `.mainColumn`, `.sideColumn` → Rewrite local: `display: grid; gap: var(--space-md);`
- `.sectionTitle` → DELETE; plain `<h4>` (globals.css)
- `.table` → Retain layout-only: `width: 100%; border-collapse: collapse;`
- `.tableHead` → DELETE; `<th>` token recipe (D-14) — `padding: var(--space-xs) var(--space-md); color: var(--color-on-surface-muted); font-size: var(--fs-label); font-weight: var(--fw-semibold); text-transform: uppercase; letter-spacing: var(--ls-label); border-bottom: 1px solid var(--color-border);`
- `.tableCell` → DELETE; `<td>` token recipe (D-14) — `padding: var(--space-xs) var(--space-md); border-bottom: 1px solid var(--color-border); color: var(--color-on-surface); font-size: var(--fs-body-md); vertical-align: middle;`
- `.invoiceRow` (grid) → DELETE; replace with semantic `<tbody><tr><td>` rows (B-4)
- `.holdCard` → DELETE; compose `.c-notice c-notice--warning` JSX-side (B-3)
- `.holdTitle`, `.holdText` → DELETE; `<strong>[warn]</strong>` + body inside `.c-notice`

From `213.3-UI-SPEC.md` Surface 1 Copywriting Contract (lines 207–217):
- Page heading: "Billing" (unchanged)
- Plan summary eyebrow: "Current plan" via `.t-label-caps`
- Hold/alert banner: `[warn] Payment issue. Resolve to continue using MarkOS.` via `.c-notice c-notice--warning`
- Invoice table headers: "Date" / "Amount" / "Status" / "Invoice"
- Invoice empty state: "No invoices yet." + body "Invoices appear here after your first billing cycle."
- Primary CTA: "Upgrade plan" or "Manage billing" (verb + noun)
- Secondary CTA: "Download invoice" or "View details"

From `213.3-RESEARCH.md` Pattern 1 — Module CSS Rewrite Shape (lines 199–254): retain only layout containers + table `<th>/<td>` recipe + meter track/fill recipe (n/a for billing); delete font-family literals, `#hex` values, `box-shadow`, button/input/badge bespoke styling.

From `213.3-RESEARCH.md` Pitfall 5 — `12px` is the most common off-grid value: `gap: 12px` → `var(--space-xs)` (8px); `padding: 12px` → `var(--space-sm)` (16px). Audit each `12px` usage during rewrite.

From `213.2-01-PLAN.md` (precedent): atomic commit `feat(213.3-01): redesign /settings/billing surface to DESIGN.md v1.1.0 tokens + .c-notice composition + semantic invoice table + Storybook state coverage`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite app/(markos)/settings/billing/page.module.css to token-only layout shell (B-1, B-2, B-5) — delete bespoke card/button/banner styles, retain only `.page`/`.shell`/`.contentGrid`/`.mainColumn`/`.sideColumn`/`.actionRow`/`.table` layout classes with token-cited values + `<th>/<td>` token recipe</name>
  <read_first>
    - app/(markos)/settings/billing/page.module.css (current 155 LOC — full file; map every existing class to RESEARCH.md class-rename table)
    - app/(markos)/settings/billing/page.tsx (current 15 LOC — confirm what classNames are referenced)
    - app/(markos)/settings/billing/page-shell.tsx (current 129 LOC — confirm what classNames are referenced)
    - .planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-RESEARCH.md "Class-Rename Map: Surface 1 — Billing" (lines 325–354) — authoritative class-by-class disposition
    - .planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-RESEARCH.md "Pattern 1 — Module CSS Rewrite Shape" (lines 199–254) — authoritative target shape
    - .planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-RESEARCH.md "Pitfall 5: 12px off-grid" (lines 778–781)
    - .planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-UI-SPEC.md "Surface 1 — Billing" Copywriting Contract (lines 207–217) + Component Primitive Reuse "Billing" (lines 384–395)
    - app/tokens.css (token names: --color-surface, --color-on-surface, --color-on-surface-muted, --color-border, --space-xs/-sm/-md/-lg, --radius-lg, --fs-body-md, --fs-label, --fw-semibold, --ls-label)
    - app/globals.css (`<h2>`, `<h4>`, `<p>` rules + `.t-lead`, `.t-label-caps` utilities)
    - styles/components.css v1.1.0 lines 1–480 (`.c-card`, `.c-button`, `.c-notice` primitives — confirm what they cover JSX-side so module.css does NOT redeclare)
    - DESIGN.md "Color > Forbidden values" + "Spacing Scale" + "Components > Notice/Banner"
  </read_first>
  <files>app/(markos)/settings/billing/page.module.css</files>
  <action>
**Replace `app/(markos)/settings/billing/page.module.css` ENTIRELY with the following content (RESEARCH.md Pattern 1 + Class-Rename Map Surface 1, authoritative target):**

```css
/* Phase 213.3 Plan-01 — Token-only billing layout shell.
 * Composes .c-card (sections), .c-button{,--primary,--secondary} (CTAs),
 * .c-notice c-notice--warning (billing-hold banner), .t-label-caps (eyebrow),
 * semantic <table> with token-cited <th>/<td> recipe (D-14). All composition
 * lives JSX-side in page.tsx/page-shell.tsx. This module carries layout +
 * width-override + table-recipe + grid concerns only. */

.page {
  padding: var(--space-md);
  background: var(--color-surface);
  display: flex;
  flex-direction: column;
  gap: var(--space-md);
}

.shell {
  max-width: 1200px;
  margin: 0 auto;
  display: grid;
  gap: var(--space-lg);
}

.contentGrid {
  display: grid;
  grid-template-columns: 1.4fr 1fr;
  gap: var(--space-md);
}

@media (max-width: 959.98px) {
  .contentGrid {
    grid-template-columns: 1fr;
  }
}

.mainColumn,
.sideColumn {
  display: grid;
  gap: var(--space-md);
}

.actionRow {
  display: flex;
  gap: var(--space-xs);
  flex-wrap: wrap;
}

/* Invoice table — D-14 token-only recipe; no .c-table primitive this wave. */
.table {
  width: 100%;
  border-collapse: collapse;
}

.table th {
  padding: var(--space-xs) var(--space-md);
  color: var(--color-on-surface-muted);
  font-size: var(--fs-label);
  font-weight: var(--fw-semibold);
  text-transform: uppercase;
  letter-spacing: var(--ls-label);
  border-bottom: 1px solid var(--color-border);
  text-align: left;
}

.table td {
  padding: var(--space-xs) var(--space-md);
  border-bottom: 1px solid var(--color-border);
  color: var(--color-on-surface);
  font-size: var(--fs-body-md);
  vertical-align: middle;
}

.emptyState {
  font-size: var(--fs-body-md);
  color: var(--color-on-surface-muted);
  padding: var(--space-md);
}
```

This eliminates EVERY legacy literal in the previous file:
- `font-family: 'Space Grotesk', sans-serif` on `.page` → inherited from globals (Inter for body)
- `color: #0f172a` → inherited from globals (`--color-on-surface`)
- `background: #f5f7fa` / `#f8fbfd` page bg → `var(--color-surface)` (Kernel Black `#0A0E14`)
- `.summaryCard` / `.panel` rules entirely → DELETE; compose `.c-card` JSX-side
- `.summaryCard` `background: #ffffff` → composed via `.c-card` (`var(--color-surface-raised)`)
- `.summaryCard` `border-radius: 28px` / `20px` → composed via `.c-card` (`var(--radius-lg)` 8px)
- `.summaryCard` `box-shadow: 0 18px 45px rgba(15, 23, 42, 0.06)` → eliminated; `.c-card` provides 1px Border Mist hairline (DESIGN.md "Elevation: borders over shadows")
- `.summaryCard` `padding: 32px` → composed via `.c-card` (`var(--space-md)`)
- `.eyebrow` rule entirely → DELETE; apply `.t-label-caps` utility JSX-side
- `.title` rule entirely → DELETE; plain `<h2>` inherits from globals
- `.summaryText` / `.bodyText` rules → DELETE; body inherits from `<p>` globals
- `.primaryButton` rule entirely → DELETE; compose `.c-button c-button--primary` JSX-side
- `.secondaryButton` rule entirely → DELETE; compose `.c-button c-button--secondary` JSX-side
- `.sectionTitle` rule entirely → DELETE; plain `<h4>` inherits from globals (eliminates `font-family: 'Sora'`, `font-size: 18px`/`20px`)
- `.tableHead` rule → DELETE; replaced by `<th>` token recipe in `.table th` selector (eliminates `font-size: 14px`, `padding-bottom: 12px`, `color: #475569`)
- `.tableCell` rule → DELETE; replaced by `<td>` token recipe in `.table td` selector (eliminates `font-size: 14px`, `color: #0f172a`)
- `.invoiceRow` (grid) rule entirely → DELETE; replaced by semantic `<tbody><tr><td>` JSX (B-4)
- `.holdCard` / `.holdTitle` / `.holdText` rules entirely → DELETE; composed JSX-side via `.c-notice c-notice--warning` (B-3) — primitive provides 4px `border-inline-start: var(--color-warning)` + `rgb(255 184 0 / 0.12)` bg + `var(--color-warning)` text
- `.contentGrid` `gap: 16px` literal → `var(--space-md)` token
- `.actionRow` `gap: 12px` literal (Pitfall 5) → `var(--space-xs)` (8px)
- All teal `#0d9488` / `#0f766e` CTA fills, focus outlines, link colors → eliminated (composed JSX-side)

Width retained: `.shell` `max-width: 1200px` is the existing layout convention for billing's wide summary+sidebar+invoices grid; preserved as a layout literal (NOT a spacing value; documented exception per UI-SPEC Q6 max-width table).

Width retained: `.actionRow` `flex-wrap: wrap` retained for responsive button-row reflow.

DO NOT add `font-family:` declarations anywhere (globals handles this). DO NOT add `outline:` declarations on focus (`:focus-visible` global handles this). DO NOT redeclare `@media (pointer: coarse)` (global rule in `styles/components.css` line 215–218 handles `.c-button` + `.c-nav-link` per CONTEXT D-24 X-6). DO NOT add `box-shadow:` declarations.
  </action>
  <verify>
    <automated>! grep -nE '#[0-9a-fA-F]{3,8}' "app/(markos)/settings/billing/page.module.css" && ! grep -nE '(Sora|Space Grotesk|linear-gradient|radial-gradient|box-shadow|0 18px 45px|transform:\s*translate)' "app/(markos)/settings/billing/page.module.css" && ! grep -nE '#0d9488|#0f766e|#14b8a6|#e6fffb|#0b877c|#f8fbfd|#f5f7fa|#ffffff|#0f172a|#334155|#475569|#cbd5e1|#e2e8f0|#9a3412|#fca5a5' "app/(markos)/settings/billing/page.module.css" && ! grep -nE '@media.*pointer.*coarse' "app/(markos)/settings/billing/page.module.css" && ! grep -nE '\.holdCard|\.holdTitle|\.holdText|\.invoiceRow|\.summaryCard|\.primaryButton|\.secondaryButton|\.eyebrow|\.title|\.sectionTitle|\.tableHead|\.tableCell' "app/(markos)/settings/billing/page.module.css" && grep -q "var(--color-surface)" "app/(markos)/settings/billing/page.module.css" && grep -cE 'var\(--space-' "app/(markos)/settings/billing/page.module.css" | awk '$1>=4 {exit 0} $1<4 {exit 1}' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **B-1 (Zero inline hex):** `grep -nE '#[0-9a-fA-F]{3,8}' "app/(markos)/settings/billing/page.module.css"` returns 0.
    - **B-2 (Zero off-grid spacing):** `grep -nE ':\s*[0-9]+px' "app/(markos)/settings/billing/page.module.css"` returns ONLY `1px` (border hairline). No `10px`, `12px`, `13px`, `14px`, `18px`, `20px`, `22px`, `28px`.
    - **B-5a (Sora/Space Grotesk eliminated):** `grep -nE "(Sora|Space Grotesk)" "app/(markos)/settings/billing/page.module.css"` returns 0.
    - **B-5b (Teal hexes eliminated):** `grep -niE "#0d9488|#0f766e|#14b8a6|#e6fffb|#0b877c" "app/(markos)/settings/billing/page.module.css"` returns 0.
    - **B-5c (No gradients/shadows/translate):** `grep -niE "linear-gradient|radial-gradient|box-shadow|0 18px 45px|transform:\s*translate" "app/(markos)/settings/billing/page.module.css"` returns 0.
    - **B-5d (No legacy bespoke classes):** `grep -nE '\.holdCard|\.holdTitle|\.holdText|\.invoiceRow|\.summaryCard|\.primaryButton|\.secondaryButton|\.eyebrow|\.title|\.sectionTitle|\.tableHead|\.tableCell' "app/(markos)/settings/billing/page.module.css"` returns 0 (all eliminated; composed JSX-side via primitives).
    - **X-6 (no per-module pointer-coarse redeclaration):** `grep -nE '@media.*pointer.*coarse' "app/(markos)/settings/billing/page.module.css"` returns 0.
    - **Token consumption (positive assertions):** `grep -c "var(--color-surface)" "app/(markos)/settings/billing/page.module.css"` ≥1; `grep -cE 'var\(--space-' "app/(markos)/settings/billing/page.module.css"` ≥4.
    - **Layout containers retained:** `grep -cE '\.(page|shell|contentGrid|mainColumn|sideColumn|actionRow|table|emptyState)\s*\{' "app/(markos)/settings/billing/page.module.css"` ≥7.
  </acceptance_criteria>
  <done>`app/(markos)/settings/billing/page.module.css` rewritten to ~55 LOC token-only layout shell; 9 grep gates pass; all legacy bespoke classes eliminated; layout containers retained with token-cited values.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update app/(markos)/settings/billing/page-shell.tsx (+ page.tsx if needed) to compose `.c-card`/`.c-button c-button--primary`/`.c-button c-button--secondary`/`.t-label-caps`/`.c-notice c-notice--warning`/semantic `<table>` per UI-SPEC Surface 1 Copywriting Contract (B-3, B-4)</name>
  <read_first>
    - app/(markos)/settings/billing/page-shell.tsx (current 129 LOC — DOM tree to preserve per CONTEXT D-05)
    - app/(markos)/settings/billing/page.tsx (current 15 LOC — confirm wrapper structure)
    - app/(markos)/settings/billing/page.module.css (post-Task 1 shape — confirm class names referenced)
    - .planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-UI-SPEC.md "Surface 1 — Billing" Copywriting Contract (lines 207–217) + Component Primitive Reuse (lines 384–395)
    - .planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-RESEARCH.md "Class-Rename Map: Surface 1 — Billing" JSX changes section (line 351)
    - .planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-RESEARCH.md "Pattern 2 — `.c-notice` Composition" (lines 256–293)
    - .planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-RESEARCH.md "Pattern 3 — Class Rename in TSX" (lines 294–312)
    - styles/components.css v1.1.0 (`.c-card`, `.c-button`, `.c-button--primary`, `.c-button--secondary`, `.c-notice`, `.c-notice--warning` — confirm primitive class names)
    - app/globals.css (`.t-label-caps`, `.t-lead`, `<h2>`, `<h4>`, `<p>` rules)
    - DESIGN.md "Components > Notice / Banner" + "Buttons" + "Forms"
  </read_first>
  <files>app/(markos)/settings/billing/page-shell.tsx, app/(markos)/settings/billing/page.tsx</files>
  <action>
**Edit A — Update `app/(markos)/settings/billing/page-shell.tsx` per the Class-Rename Map JSX swaps. Apply the following className substitutions and JSX shape changes throughout the file (DOM tree + prop interface UNCHANGED per CONTEXT D-05):**

| Existing JSX | New JSX | Token / primitive citation |
|--------------|---------|---------------------------|
| `<div className={styles.summaryCard}>` | `<div className={`c-card ${styles.mainColumn}`}>` (or `${styles.sideColumn}` per layout slot) | `.c-card` primitive provides surface-raised bg + 1px hairline + 8px radius + `var(--space-md)` padding |
| `<div className={styles.panel}>` | `<div className="c-card">` | Same as above |
| `<span className={styles.eyebrow}>Current plan</span>` | `<span className="t-label-caps">Current plan</span>` | `.t-label-caps` utility from globals.css |
| `<h2 className={styles.title}>Billing</h2>` | `<h2>Billing</h2>` | globals.css `<h2>` rule |
| `<h3 className={styles.sectionTitle}>Invoices</h3>` | `<h4>Invoices</h4>` (semantic upgrade — section-heading inside settings card) | globals.css `<h4>` rule |
| `<p className={styles.summaryText}>...</p>` | `<p className="t-lead">...</p>` (for surface descriptors) or plain `<p>` (for body) | `.t-lead` for descriptors |
| `<button className={styles.primaryButton}>Upgrade plan</button>` | `<button type="button" className="c-button c-button--primary">Upgrade plan</button>` | `.c-button c-button--primary` |
| `<button className={styles.secondaryButton}>Manage billing</button>` | `<button type="button" className="c-button c-button--secondary">Manage billing</button>` | `.c-button c-button--secondary` |
| `<div className={styles.actionRow}>...</div>` | `<div className={styles.actionRow}>...</div>` (UNCHANGED — layout class retained) | layout class still token-cited |

**B-3 — Replace the billing-hold composition:**

Find the existing JSX that renders the billing-hold/payment-issue banner (currently uses `styles.holdCard` + `styles.holdTitle` + `styles.holdText`):

```tsx
{/* Before */}
<div className={styles.holdCard}>
  <h4 className={styles.holdTitle}>Account on hold</h4>
  <p className={styles.holdText}>{...billing hold copy}</p>
</div>
```

Replace with the canonical `.c-notice c-notice--warning` composition per RESEARCH.md Pattern 2:

```tsx
{/* After */}
<div className="c-notice c-notice--warning" role="status">
  <strong>[warn]</strong>{" "}Payment issue. Resolve to continue using MarkOS.
</div>
```

KEY rules (verifiable per UI-SPEC AC B-3):
- `<div className="c-notice c-notice--warning">` is the EXACT class string — no `${styles.X}` interpolation, no local class composition (the primitive carries everything; D-09b/D-15).
- `<strong>[warn]</strong>` is the bracketed-glyph prefix in JetBrains Mono — primitive's `strong` selector applies `var(--font-mono)` per `.c-notice` rules.
- `{" "}` is the JSX-safe space between `</strong>` and the body text (do not use `&nbsp;` — primitive handles spacing).
- Copy: `Payment issue. Resolve to continue using MarkOS.` per UI-SPEC Surface 1 Copywriting Contract (no "Please", no exclamation, no banned lexicon, leads with imperative remediation).
- `role="status"` for SR announcement on dynamic appearance (live-region semantics — not added to error/info notices unless they're dynamically inserted; consult Phase 213.1 RotationGraceBanner for precedent if uncertain).

**B-4 — Replace the invoice grid composition:**

Find the existing JSX that renders invoice rows (currently uses `styles.invoiceRow` grid divs):

```tsx
{/* Before */}
<div className={styles.invoiceTable}>
  {invoices.map((inv) => (
    <div key={inv.id} className={styles.invoiceRow}>
      <span>{inv.date}</span>
      <span>{inv.amount}</span>
      <span>{inv.status}</span>
      <a href={inv.url}>Download</a>
    </div>
  ))}
</div>
```

Replace with semantic `<table>` per UI-SPEC AC B-4 + RESEARCH.md D-14 vanilla table recipe:

```tsx
{/* After */}
<table className={styles.table}>
  <thead>
    <tr>
      <th scope="col">Date</th>
      <th scope="col">Amount</th>
      <th scope="col">Status</th>
      <th scope="col">Invoice</th>
    </tr>
  </thead>
  <tbody>
    {invoices.length === 0 ? (
      <tr>
        <td colSpan={4} className={styles.emptyState}>
          No invoices yet. Invoices appear here after your first billing cycle.
        </td>
      </tr>
    ) : (
      invoices.map((inv) => (
        <tr key={inv.id}>
          <td>{inv.date}</td>
          <td>{inv.amount}</td>
          <td>{inv.status}</td>
          <td>
            <a href={inv.url} className="c-button c-button--tertiary">Download</a>
          </td>
        </tr>
      ))
    )}
  </tbody>
</table>
```

KEY rules (verifiable per UI-SPEC AC B-4):
- Use `<table>` / `<thead>` / `<tbody>` / `<tr>` / `<th>` / `<td>` semantic markup (NOT `<div>`).
- `scope="col"` on `<th>` for a11y (column header semantics).
- `className={styles.table}` ONLY on the `<table>` element (carries `width: 100%; border-collapse: collapse;` from Task 1's module.css).
- `<th>` and `<td>` carry NO className — token recipe applies via `.table th`/`.table td` descendant selectors in module.css.
- Empty state copy: "No invoices yet. Invoices appear here after your first billing cycle." per UI-SPEC Copywriting Contract.
- Download CTA composes `.c-button c-button--tertiary` (text-button posture, not primary/secondary — invoice-row inline action).

**Banned-lexicon check:** Scan all visible copy strings in `page-shell.tsx`. Reject any of: `synergy|leverage|empower|unlock|transform|revolutionize|supercharge|holistic|seamless|cutting-edge|innovative|game-changer|next-generation|world-class|best-in-class|reimagine|disrupt|just (as softener)|Please`. NO exclamation points. NO emoji.

**Edit B — Update `app/(markos)/settings/billing/page.tsx` if it carries any wrapper className:**

If `page.tsx` is a thin server-component wrapper that only renders `<PageShell>`, no changes required (the wrapper structure is preserved). If it carries `className={styles.page}` or similar, update to ensure the wrapper composition matches Task 1's module.css class names (`.page`, `.shell`).

**Edit C — Confirm preservation:** Phase 205 Pricing Engine wiring (any `await` calls to billing API/Pricing Engine, `fetchBilling()` or similar functions, server actions) MUST stay intact. Any `'use client'` directive must remain unchanged. Any prop interface on subcomponents must remain unchanged. ONLY className strings + JSX shape (banner→notice, grid→table) change.

After both edits, run `npm run build` — MUST exit 0.
  </action>
  <verify>
    <automated>grep -qE 'c-notice c-notice--warning' "app/(markos)/settings/billing/page-shell.tsx" && grep -qE '\[warn\]' "app/(markos)/settings/billing/page-shell.tsx" && grep -qE '<table' "app/(markos)/settings/billing/page-shell.tsx" && grep -qE '<thead>' "app/(markos)/settings/billing/page-shell.tsx" && grep -qE '<tbody>' "app/(markos)/settings/billing/page-shell.tsx" && grep -qE 'c-button c-button--primary' "app/(markos)/settings/billing/page-shell.tsx" && grep -qE 'c-button c-button--secondary' "app/(markos)/settings/billing/page-shell.tsx" && ! grep -qE 'styles\.(holdCard|holdTitle|holdText|invoiceRow|primaryButton|secondaryButton|eyebrow|sectionTitle|summaryCard|panel)' "app/(markos)/settings/billing/page-shell.tsx" && ! grep -inE "Please|synergy|leverage|empower|unlock|transform|revolutionize|supercharge|holistic|seamless|cutting-edge|innovative|game-changer|next-generation|world-class|best-in-class|reimagine|disrupt" "app/(markos)/settings/billing/page-shell.tsx" "app/(markos)/settings/billing/page.tsx" && npm run build && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **B-3 (`.c-notice c-notice--warning` composition):** `grep -cE 'c-notice c-notice--warning' "app/(markos)/settings/billing/page-shell.tsx"` ≥1.
    - **B-3 (bracketed-glyph `[warn]` present):** `grep -cE '\[warn\]' "app/(markos)/settings/billing/page-shell.tsx"` ≥1.
    - **B-4 (semantic `<table>` for invoices):** `grep -c '<table' "app/(markos)/settings/billing/page-shell.tsx"` ≥1; `grep -c '<thead>' "app/(markos)/settings/billing/page-shell.tsx"` ≥1; `grep -c '<tbody>' "app/(markos)/settings/billing/page-shell.tsx"` ≥1.
    - **Primary CTA composes `.c-button--primary`:** `grep -cE 'c-button c-button--primary' "app/(markos)/settings/billing/page-shell.tsx"` ≥1.
    - **Secondary CTA composes `.c-button--secondary`:** `grep -cE 'c-button c-button--secondary' "app/(markos)/settings/billing/page-shell.tsx"` ≥1.
    - **No legacy class refs in TSX:** `grep -cE 'styles\.(holdCard|holdTitle|holdText|invoiceRow|primaryButton|secondaryButton|eyebrow|sectionTitle|summaryCard|panel)' "app/(markos)/settings/billing/page-shell.tsx"` returns 0.
    - **Banned-lexicon scan = 0 (CONTEXT D-07):** `grep -inE "Please|synergy|leverage|empower|unlock|transform|revolutionize|supercharge|holistic|seamless|cutting-edge|innovative|game-changer|next-generation|world-class|best-in-class|reimagine|disrupt" "app/(markos)/settings/billing/page-shell.tsx" "app/(markos)/settings/billing/page.tsx"` returns 0.
    - **No exclamation points in copy:** `grep -E '"[^"]*![^"]*"' "app/(markos)/settings/billing/page-shell.tsx"` returns 0 (or carefully reviewed false-positives such as JSX boolean attribute usage).
    - **`npm run build` exits 0** (validates JSX + module.css together).
    - **DOM-tree preserved (no `'use client'` change):** `grep -c "'use client'" "app/(markos)/settings/billing/page-shell.tsx"` matches pre-edit value (no new directive added or removed).
  </acceptance_criteria>
  <done>`page-shell.tsx` updated to compose primitives + render `.c-notice c-notice--warning` for billing-hold + render semantic `<table>` for invoices; legacy class refs eliminated; banned-lexicon = 0; `npm run build` exits 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend app/(markos)/settings/billing/page.stories.tsx with ≥3 named state stories (`Default`, `HoldState`, `InvoiceList`) per UI-SPEC X-4 + Storybook State Coverage table</name>
  <read_first>
    - app/(markos)/settings/billing/page.stories.tsx (current — confirm existing default export shape; extend, don't replace)
    - app/(markos)/settings/billing/page-shell.tsx (post-Task 2 shape — confirm prop interface for stories args)
    - .planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-UI-SPEC.md "Storybook State Coverage" table (lines 575–586)
    - .planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-CONTEXT.md D-17 (story state coverage)
    - .planning/phases/213.2-ui-canon-adoption-wave-2-auth-surfaces-login-signup-invite-oauth-consent-to-design-md-tokens/213.2-01-PLAN.md Task 2 Edit B (LoginCard.stories.tsx CSF3 pattern — mirror this shape)
    - app/(markos)/_components/RotationGraceBanner.stories.tsx (Phase 213.1 reference — CSF3 5-state pattern)
  </read_first>
  <files>app/(markos)/settings/billing/page.stories.tsx</files>
  <action>
**Extend `app/(markos)/settings/billing/page.stories.tsx` (existing file) with ≥3 named state stories per UI-SPEC X-4.**

The existing file likely has a `meta` default-export but limited (or zero) state-coverage exports. Extend (do not replace) by adding the following named exports (per UI-SPEC Storybook State Coverage table line 577):

```tsx
import type { Meta, StoryObj } from '@storybook/react';
import PageShell from './page-shell';

// (existing meta default-export retained — confirm shape during implementation; if absent, add this:)
const meta = {
  title: 'Settings/Billing',
  component: PageShell,
  parameters: {
    layout: 'fullscreen',
  },
} satisfies Meta<typeof PageShell>;

export default meta;

type Story = StoryObj<typeof meta>;

export const Default: Story = {
  args: {
    // mock props per page-shell.tsx interface — current plan, no hold, has invoices
    // (real prop names will be discovered by reading post-Task 2 page-shell.tsx; below is a representative shape)
    plan: { name: 'Pro', status: 'active', renewsAt: '2026-05-28' },
    holdState: null,
    invoices: [
      { id: 'inv_001', date: '2026-04-01', amount: '$99.00', status: 'Paid', url: '#' },
      { id: 'inv_002', date: '2026-03-01', amount: '$99.00', status: 'Paid', url: '#' },
    ],
  },
};

export const HoldState: Story = {
  args: {
    plan: { name: 'Pro', status: 'on_hold', renewsAt: '2026-05-28' },
    holdState: { reason: 'payment_failed', since: '2026-04-25' },
    invoices: [
      { id: 'inv_001', date: '2026-04-01', amount: '$99.00', status: 'Failed', url: '#' },
    ],
  },
  parameters: {
    docs: {
      description: {
        story: 'Renders the `.c-notice c-notice--warning` payment-issue banner per UI-SPEC AC B-3. Bracketed `[warn]` glyph + remediation copy.',
      },
    },
  },
};

export const InvoiceList: Story = {
  args: {
    plan: { name: 'Pro', status: 'active', renewsAt: '2026-05-28' },
    holdState: null,
    invoices: Array.from({ length: 8 }).map((_, i) => ({
      id: `inv_00${i + 1}`,
      date: `2026-${String(4 - (i % 4)).padStart(2, '0')}-01`,
      amount: '$99.00',
      status: i < 2 ? 'Paid' : (i < 5 ? 'Paid' : 'Pending'),
      url: '#',
    })),
  },
  parameters: {
    docs: {
      description: {
        story: 'Multi-row invoice table per UI-SPEC AC B-4. Semantic `<table>` with token-cited `<th>`/`<td>`.',
      },
    },
  },
};
```

KEY rules (verifiable per UI-SPEC X-4 + D-17):
- `Meta<typeof PageShell>` — Storybook 8 CSF3 typed `meta` default-export.
- 3 named state exports: `Default`, `HoldState`, `InvoiceList` (matches UI-SPEC line 577 verbatim).
- `args` shape mirrors actual `PageShell` prop interface (executor MUST inspect post-Task 2 file to confirm exact prop names — replace placeholder shape if needed).
- `HoldState` story documents the AC B-3 closure (notice banner).
- `InvoiceList` story documents the AC B-4 closure (semantic table).
- DOES NOT set `parameters.theme: "legacy"` — must render against DESIGN.md v1.1.0 tokens (Phase 213.1 Plan-03 preview.tsx wiring).

**If the existing `page.stories.tsx` already has named exports** (e.g., `Default`), extend in place: keep existing exports, add `HoldState` and `InvoiceList` if missing. Reach the ≥3 named state minimum.

After editing, run `npm run build-storybook` — MUST exit 0.
  </action>
  <verify>
    <automated>test -f "app/(markos)/settings/billing/page.stories.tsx" && grep -cE '^export const (Default|HoldState|InvoiceList):' "app/(markos)/settings/billing/page.stories.tsx" | awk '$1>=3 {exit 0} $1<3 {exit 1}' && ! grep -qE 'parameters\.theme.*"legacy"' "app/(markos)/settings/billing/page.stories.tsx" && grep -q 'export default meta' "app/(markos)/settings/billing/page.stories.tsx" && npm run build-storybook && node --test test/ui-a11y/213-1-chrome-a11y.test.js test/ui-a11y/213-2-auth-a11y.test.js && echo PASS  </verify>
  <acceptance_criteria>
    - **X-4 (≥3 named state stories):** `grep -cE '^export const (Default|HoldState|InvoiceList):' "app/(markos)/settings/billing/page.stories.tsx"` ≥3.
    - **`Meta<typeof PageShell>` typed default export:** `grep -c 'export default meta' "app/(markos)/settings/billing/page.stories.tsx"` ≥1.
    - **Default-not-legacy theme (Phase 213.1 R2 mitigation preserved):** `grep -cE 'parameters\.theme.*"legacy"' "app/(markos)/settings/billing/page.stories.tsx"` returns 0.
    - **`npm run build-storybook` exits 0** (validates story file shape + Storybook 8 CSF3 compatibility).
    - **Phase 213.1 + 213.2 a11y suites still green:** `node --test test/ui-a11y/213-1-chrome-a11y.test.js test/ui-a11y/213-2-auth-a11y.test.js` exits 0.
  </acceptance_criteria>
  <done>`page.stories.tsx` extended with ≥3 named state stories (`Default` / `HoldState` / `InvoiceList`); CSF3 typed `meta` default export; `npm run build-storybook` exits 0; existing 213.1 + 213.2 a11y suites stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→billing API | Untrusted billing actions (upgrade, manage) cross here via Phase 205 wiring |
| Pricing Engine→billing copy | Billing-card content sourced from Pricing Engine context per BILL-02; redesign does not change source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213.3-01-01 | Tampering | billing CTAs (Upgrade plan / Manage billing) | accept | Wiring preserved verbatim from Phase 205 contract; CSRF + auth handled server-side (out of scope) |
| T-213.3-01-02 | Information Disclosure | billing-hold copy | accept | Copy revisions are additive (`[warn]` glyph + imperative remediation); no change to data exposure |
| T-213.3-01-03 | Spoofing | invoice download links | accept | Links use signed URLs from billing API (Phase 205 wiring); className-only redesign does not alter URL generation |
</threat_model>

<verification>
After all 3 tasks complete, run:

```bash
# B-1 — Zero inline hex
grep -nE '#[0-9a-fA-F]{3,8}' "app/(markos)/settings/billing/page.module.css"
# Expected: 0 matches

# B-2 — Zero off-grid spacing (only 1px allowed)
grep -nE ':\s*[0-9]+px' "app/(markos)/settings/billing/page.module.css"
# Expected: only `1px` for borders

# B-3 — `.c-notice c-notice--warning` composition + bracketed glyph
grep -cE 'c-notice c-notice--warning' "app/(markos)/settings/billing/page-shell.tsx"  # ≥1
grep -cE '\[warn\]' "app/(markos)/settings/billing/page-shell.tsx"  # ≥1

# B-4 — semantic <table>
grep -c '<table' "app/(markos)/settings/billing/page-shell.tsx"  # ≥1
grep -c '<thead>' "app/(markos)/settings/billing/page-shell.tsx"  # ≥1

# B-5 — Sora/Space Grotesk + teal + drop shadow + 20/28px radius eliminated
grep -niE "(Sora|Space Grotesk|#0d9488|#0f766e|18px 45px|border-radius: 20px|border-radius: 28px)" "app/(markos)/settings/billing/page.module.css"
# Expected: 0

# Banned lexicon
grep -inE "(synergy|leverage|empower|unlock|transform|seamless|innovative|just )" "app/(markos)/settings/billing/page-shell.tsx" "app/(markos)/settings/billing/page.tsx"
# Expected: 0

# X-4 — ≥3 named state stories
grep -cE '^export const (Default|HoldState|InvoiceList):' "app/(markos)/settings/billing/page.stories.tsx"  # ≥3

# Build green
npm run build  # exit 0
npm run build-storybook  # exit 0

# Wiring tests stay green (CONTEXT D-19, D-23)
node --test test/  # exit 0 (Phase 205 billing + 200/204 CLI parity wiring)
node --test test/ui-a11y/213-1-chrome-a11y.test.js test/ui-a11y/213-2-auth-a11y.test.js  # exit 0
```
</verification>

<success_criteria>
- All `must_haves.truths` verifiable via the verification commands above.
- UI-SPEC AC B-1, B-2, B-3, B-4, B-5 close.
- Phase 205 Pricing Engine + billing wiring contract preserved (verified via wiring tests).
- Phase 213.1 + 213.2 a11y suites continue to pass.
- DOM-tree + prop interface preserved per CONTEXT D-05.
- Atomic commit shape (CONTEXT D-04): `feat(213.3-01): redesign /settings/billing surface to DESIGN.md v1.1.0 tokens + .c-notice composition + semantic invoice table + Storybook state coverage`.
</success_criteria>

<output>
After completion, create `.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-01-SUMMARY.md` documenting:
- Files modified (`page.module.css`, `page.tsx`, `page-shell.tsx`, `page.stories.tsx`)
- AC# closures (B-1..B-5)
- Verification command outputs
- Manual visual review reminder (Chromatic baseline for `Settings/Billing` stories — Default / HoldState / InvoiceList)
- Hand-off to Plan 213.3-02 (members)
</output>
