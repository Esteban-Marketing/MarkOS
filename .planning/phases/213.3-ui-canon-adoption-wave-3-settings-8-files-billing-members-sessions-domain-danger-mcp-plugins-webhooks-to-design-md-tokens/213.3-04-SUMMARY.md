---
phase: 213.3
plan: "04"
subsystem: ui-canon-adoption
tags: [css-tokens, domain-surface, c-notice-4-states, c-status-dot, c-code-block, c-terminal, c-button-destructive, c-button-tertiary, storybook-csf3, design-md-v1.1.0, highest-hex-density-surface]
requires: [213.3-03]
provides: [domain-surface-token-canon]
affects: [settings/domain, app/(markos)/settings/domain/page.module.css, app/(markos)/settings/domain/page.tsx, app/(markos)/settings/domain/page.stories.tsx]
tech_stack_added: []
tech_stack_patterns:
  - "4 DNS notice states composed via .c-notice c-notice--{success,info,warning,error} (D-3) — most state-dense notice surface in wave"
  - ".c-status-dot--live (verified) / .c-status-dot default (pending) / .c-status-dot--error (failed) + bracketed-glyph pairing (D-3)"
  - ".c-code-block inside .c-terminal replaces Space Grotesk cnameBlock (D-2)"
  - ".c-button--destructive Remove domain replaces .removeButton (D-4)"
  - ".c-button--tertiary Resolve now inside c-notice--warning rotation-grace notice (D-4)"
  - "DomainPageView presentational subcomponent extracted to page.tsx exports — props-driven for Storybook isolation (mirrors Sessions/Members pattern)"
  - "Storybook 8 CSF3 5 named state exports: Default / Pending / Verified / RotationGrace / Failed (X-4)"
key_files_created:
  - app/(markos)/settings/domain/page.stories.tsx
key_files_modified:
  - app/(markos)/settings/domain/page.module.css
  - app/(markos)/settings/domain/page.tsx
decisions:
  - "DomainPageView extracted from default export — page.tsx 'use client' retains internal useState/useEffect wiring (D-5); DomainPageView is exported named for Storybook story import, matching SessionsPageView (213.3-03) and MembersPageView (213.3-02) pattern."
  - "page.module.css LOC grows 22 → 54 by design — legacy statusDot color-only attribute selectors replaced by .statusCell layout helper + primitive composition (.c-status-dot variants set color via global primitive)."
  - "rotationDeadline prop added to DomainPageView — encodes deadline into resolveUrl for the tertiary Resolve now link; wired to null in default DomainSettingsPage."
  - "Pre-existing tenancy test failure (Suite 201-06: Surface 7 vanity login page reads x-markos-byod header) confirmed pre-existing via git stash retest — logged to deferred-items scope per Rule 5 (out-of-scope, caused by 213.2-01 refactor)."
metrics:
  duration_minutes: 18
  tasks_completed: 3
  tasks_total: 3
  files_modified: 3
  files_created: 1
  completed_date: "2026-04-29"
---

# Phase 213.3 Plan 04: Domain Surface Token-Canon Summary

**One-liner:** Token-only domain layout module (22 → 54 LOC GROWS, zero hex) + 4 `.c-notice` DNS state compositions (success/info/warning/error) + `.c-status-dot--live`/default/`--error` modifiers + bracketed-glyph pairing + `.c-code-block` inside `.c-terminal` CNAME display + `.c-button--destructive` + `.c-button--tertiary` Resolve now + Storybook CSF3 5-state coverage — highest hex-density surface in wave (19 hex / 22 LOC eliminated).

---

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| T1 | Rewrite page.module.css to token-only layout module (D-1) | `706c866` | `app/(markos)/settings/domain/page.module.css` |
| T2 | Update page.tsx to compose 4 .c-notice states + .c-status-dot + .c-code-block/.c-terminal (D-2..D-5) | `3212f32` | `app/(markos)/settings/domain/page.tsx` |
| T3 | Create page.stories.tsx with 5 CSF3 named state stories (X-4) | `b2b3fee` | `app/(markos)/settings/domain/page.stories.tsx` + storybook-static/ |

---

## AC Closures

| AC | Description | Verification |
|----|-------------|--------------|
| D-1 | Zero inline hex in page.module.css (eliminates all 19 originals — densest hex/LOC in wave) | `grep -nE '#[0-9a-fA-F]{3,8}' page.module.css` → 0 matches |
| D-2 | `.c-code-block` inside `.c-terminal` replaces Space Grotesk `.cnameBlock` | `grep -cE 'c-terminal' page.tsx` → 1; `grep -cE 'c-code-block' page.tsx` → 1 |
| D-3 | 4 DNS notice states via `.c-notice c-notice--{success,info,warning,error}` + status-dot modifiers + bracketed glyphs | `grep -cE 'c-notice c-notice--(success|info|warning|error)' page.tsx` → 4; `grep -cE 'c-status-dot--(live|error)' page.tsx` → 2; `grep -cE '\[ok\]|\[warn\]|\[err\]|\[info\]' page.tsx` → 7 |
| D-4 | Rotation grace `.c-notice c-notice--warning` + `.c-button--tertiary` Resolve now; `.c-button--destructive` Remove domain | `grep -cE 'c-notice--warning' page.tsx` → 1; `grep -cE 'c-button--tertiary' page.tsx` → 1; `grep -cE 'c-button--destructive' page.tsx` → 1 |
| D-5 | Phase 201 domain wiring preserved (addDomain/removeDomain/saveBranding fetch calls unchanged) | `node --test test/tenancy/domain-api.test.js` → 6/6 pass |
| X-4 | ≥5 named state stories: Default / Pending / Verified / RotationGrace / Failed | `grep -cE '^export const (Default|Pending|Verified|RotationGrace|Failed):' page.stories.tsx` → 5; 5 entries in storybook-static/index.json under `Settings/Domain` |

---

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Pattern] DomainPageView presentational subcomponent extraction**
- **Found during:** Task 3 (stories authoring)
- **Issue:** Plan's Task 3 `args: { domain: baseDomain, dnsState: 'pending' }` requires the component to accept props, but the existing page.tsx used only internal `useState` — no props surface for Storybook story control.
- **Fix:** Extracted `DomainPageView` as a named export from `page.tsx` (props-driven presentational component) following the established Sessions/Members/LoginCard extraction pattern. Default export `DomainSettingsPage` retains all Phase 201 wiring via internal state, wiring into `DomainPageView`. Stories import `DomainPageView` directly.
- **Files modified:** `app/(markos)/settings/domain/page.tsx`
- **Commits:** `3212f32`

**2. [Rule 1 - Comment] `.statusDot[data-status]` string in CSS comment triggered grep gate**
- **Found during:** Task 1 verification
- **Issue:** `! grep -nE 'statusDot\[data-status' page.module.css` false-positive from doc comment containing the banned pattern literal.
- **Fix:** Reworded comment to `statusDot color-only attribute selectors` — avoids the attribute selector syntax in comment body.
- **Files modified:** `app/(markos)/settings/domain/page.module.css`
- **Commits:** `706c866`

---

## Known Stubs

None. All 4 DNS notice states render from the `dnsState` prop. `DomainSettingsPage` initializes `dnsState: 'idle'` and transitions to `'pending'` on successful `addDomain` — full state machine wiring deferred to Phase 201 DNS poll integration (existing design), but component compositions are complete.

---

## Threat Flags

None. T-213.3-04-03 (Spoofing — rotation-grace Resolve now link) mitigated per plan: `.c-button--tertiary` text-link composition + bracketed `[warn]` glyph + explicit copy "Both the old and new domains are active during the grace period." No new trust boundaries introduced beyond Phase 201 design.

---

## Self-Check: PASSED

| Check | Result |
|-------|--------|
| `app/(markos)/settings/domain/page.module.css` exists | FOUND |
| `app/(markos)/settings/domain/page.tsx` exists | FOUND |
| `app/(markos)/settings/domain/page.stories.tsx` exists | FOUND |
| Commit `706c866` (T1 module.css rewrite) | FOUND |
| Commit `3212f32` (T2 page.tsx primitives) | FOUND |
| Commit `b2b3fee` (T3 stories + storybook build) | FOUND |
| Domain-specific tests 6/6 pass | PASS |
| Auth tests 61/61 pass | PASS |
| Storybook build exits 0 | PASS (14.63s) |
| 5 domain stories in storybook-static/index.json | PASS |
