---
phase: 213.3
plan: 09
type: execute
wave: 1
depends_on:
  - "213.3-01"
  - "213.3-02"
  - "213.3-03"
  - "213.3-04"
  - "213.3-05"
  - "213.3-06"
  - "213.3-07"
  - "213.3-08"
files_modified:
  - test/ui-a11y/213-3-settings-a11y.test.js
autonomous: true
requirements:
  - X-1
  - X-2
  - X-3
  - X-4
  - X-5
  - X-6
  - X-7
must_haves:
  truths:
    - "test/ui-a11y/213-3-settings-a11y.test.js (NEW) exists and follows the canonical 5-line `node:test` opener pattern from `test/ui-a11y/213-1-chrome-a11y.test.js` + `213-2-auth-a11y.test.js`"
    - "test/ui-a11y/213-3-settings-a11y.test.js asserts coverage of all 7 cross-cutting ACs (X-1 through X-7) across all 9 settings surfaces (billing, members, sessions, domain, danger, mcp, plugins, webhooks list, webhooks detail)"
    - "test/ui-a11y/213-3-settings-a11y.test.js contains ≥30 individual `test(...)` blocks per CONTEXT D-18 (X-5)"
    - "test/ui-a11y/213-3-settings-a11y.test.js contains ≥9 `AC#` mentions covering X-1, X-2, X-3, X-4, X-5, X-6 + per-surface AC# (B/M/S/D/DZ/MC/P/W variants) per CONTEXT D-18"
    - "test/ui-a11y/213-3-settings-a11y.test.js verifies ALL 9 module.css files: zero inline hex (X-1 prerequisite), zero teal hex (X-2), zero gradients/shadows/translate (X-3), zero @media (pointer: coarse) redeclaration (X-6), zero Sora/Space Grotesk (X-1)"
    - "test/ui-a11y/213-3-settings-a11y.test.js verifies ALL 11 tsx files: `.c-notice c-notice--{state}` composition for banner-shaped JSX (X-4), bracketed-glyph state coding ([ok]/[warn]/[err]/[info]), banned-lexicon = 0"
    - "Phase 213.1 + 213.2 a11y suites still green: `node --test test/ui-a11y/213-1-chrome-a11y.test.js test/ui-a11y/213-2-auth-a11y.test.js test/ui-a11y/213-1-light-and-forced-colors.test.js` exits 0 (X-7)"
    - "All Phase 200/200.1/201/202/203/204 wiring suites still green: `npm test` exits 0 (X-7)"
    - "`npm run build` exits 0; `npm run build-storybook` exits 0 (X-7)"
    - "`@media (pointer: coarse)` global rule from Phase 213.1 + 213.2 inherits cleanly into all 9 settings module.css files (no redeclarations) per X-6"
  artifacts:
    - path: "test/ui-a11y/213-3-settings-a11y.test.js"
      provides: "Phase 213.3 settings a11y grep-shape suite — asserts all 9 surface module.css token compliance + tsx primitive composition + bracketed-glyph state coding + .c-notice composition + (pointer: coarse) global inheritance + banned-lexicon eliminated"
      min_lines: 200
      contains: "213.3 settings a11y"
  key_links:
    - from: "test/ui-a11y/213-3-settings-a11y.test.js"
      to: "all 9 module.css + 11 tsx files across 8 settings surfaces"
      via: "fs.readFileSync + assert.match grep-shape regex per AC# group"
      pattern: "fs\\.readFileSync"
    - from: "test/ui-a11y/213-3-settings-a11y.test.js"
      to: "global `@media (pointer: coarse)` rule in styles/components.css (Phase 213.1 + 213.2)"
      via: "negative assertion: NO module redeclaration in any settings .module.css"
      pattern: "@media.*pointer.*coarse"
---

<objective>
Close cross-cutting Phase 213.3 acceptance criteria (X-1 through X-7) by creating a NEW grep-shape a11y test suite `test/ui-a11y/213-3-settings-a11y.test.js` that mirrors the Phase 213.1 + 213.2 grep-shape pattern. Target: ≥30 tests, ≥9 AC# mentions, covers all 9 settings surfaces (billing, members, sessions, domain, danger, mcp, plugins, webhooks list, webhooks detail). Verify global `(pointer: coarse)` rule from Phase 213.2 inherits cleanly into settings (no per-module redeclaration per X-6). Final phase commit closes Phase 213.3.

Purpose: Close UI-SPEC AC X-1 (Sora/Space Grotesk fully eliminated across all 9 module.css), X-2 (teal hex eliminated from all module.css), X-3 (no gradients/drop-shadows/transform-translate), X-4 (every banner-shaped JSX uses `.c-notice c-notice--{state}` + ≥10 occurrences across 8 surfaces), X-5 (`213-3-settings-a11y.test.js` exists, ≥30 tests, ≥9 AC# mentions, exits 0), X-6 (no per-module `@media (pointer: coarse)` redeclaration), X-7 (full build + full test suite green).

Output: NEW `test/ui-a11y/213-3-settings-a11y.test.js` (~200 LOC, ≥30 tests covering all 9 surfaces + cross-cutting). Phase 213.1 + 213.2 a11y suites continue to pass. All Phase 200/201/202/203/204 wiring contracts continue to pass.

This plan depends on ALL 8 prior plans completing. Sequential execution per CONTEXT D-10. The atomic commit closes the phase: `test(213.3-09): add 213-3-settings-a11y.test.js cross-cutting suite + close phase 213.3`.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-CONTEXT.md
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-UI-SPEC.md
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-RESEARCH.md
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-VALIDATION.md
@DESIGN.md
@CLAUDE.md
@app/tokens.css
@app/globals.css
@styles/components.css
@test/ui-a11y/213-1-chrome-a11y.test.js
@test/ui-a11y/213-2-auth-a11y.test.js
@.planning/phases/213.2-ui-canon-adoption-wave-2-auth-surfaces-login-signup-invite-oauth-consent-to-design-md-tokens/213.2-05-PLAN.md

<interfaces>
From `test/ui-a11y/213-2-auth-a11y.test.js` (precedent — mirror this exact pattern):
```js
'use strict';
const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');

const ROOT = process.cwd();
function read(file) { return fs.readFileSync(path.join(ROOT, file), 'utf8'); }

// Test groups by AC#:
// X-1 — Sora/Space Grotesk eliminated
// X-2 — Teal hex eliminated
// X-3 — Gradients/shadows/transform eliminated
// X-4 — Stories present + .c-notice composition
// X-5 — This test file exists with required mentions
// X-6 — No per-module (pointer: coarse) redeclaration
```

From CONTEXT D-18 (test target):
- ≥30 tests
- ≥9 AC# mentions
- Mirror `213-2-auth-a11y.test.js` grep-shape pattern
- Cover all 9 module.css + 11 tsx targets
- Run via `node --test test/ui-a11y/213-3-settings-a11y.test.js` and `npm run test:ui-a11y` (if defined)

From `213.3-UI-SPEC.md` Cross-cutting AC table (lines 789–798):
- X-1: `grep -rnE "(Sora|Space Grotesk)" "app/(markos)/settings/"` returns 0 in `.module.css`
- X-2: `grep -niE "(#0d9488|#0f766e|#14b8a6|#e6fffb|#0b877c)" "app/(markos)/settings/"` returns 0 in `.module.css`
- X-3: `grep -niE "(linear-gradient|radial-gradient|box-shadow.*rgba.*45px|transform:\s*translate)" "app/(markos)/settings/"` returns 0 in `.module.css`
- X-4: `grep -nrE 'className=.*banner|className=.*alert|className=.*holdCard|className=.*purgeBanner' "app/(markos)/settings/"` returns 0; `grep -nrE 'c-notice c-notice--' "app/(markos)/settings/"` returns ≥10 across 8 surfaces
- X-5: `213-3-settings-a11y.test.js` exists; ≥9 AC# mentions; exits 0
- X-6: `grep -rnE "@media.*pointer.*coarse" "app/(markos)/settings/"` returns 0 in `.module.css`
- X-7: `npm run build && npm run build-storybook && npm test` exits 0

From `213.2-05-PLAN.md` (precedent — same shape applied to settings):
- Test count target: ≥18 (Phase 213.2). 213.3 target: ≥30 (more surfaces, more files).
- AC mentions: cover the cross-cutting AC# (X-1..X-7) + per-surface AC# (B/M/S/D/DZ/MC/P/W).
- Pattern: `fs.readFileSync` + `assert.match`/`assert.doesNotMatch` for grep-shape.
- File-existence assertions for all 9 module.css + 11 tsx + 9 stories.tsx (extending billing+plugins; new for 7 others).

Note: Phase 213.2-05-T1 already extended the global `@media (pointer: coarse)` block in `styles/components.css` to scope `.c-button` (currently scopes both `.c-button` AND `.c-nav-link`). Phase 213.3 inherits this — no extension needed in this plan. The X-6 verification confirms inheritance (no per-module redeclaration).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create test/ui-a11y/213-3-settings-a11y.test.js — grep-shape a11y suite covering all 9 settings surfaces; ≥30 tests; ≥9 AC# mentions; verify global (pointer: coarse) inheritance from Phase 213.2 + final phase commit (X-1, X-2, X-3, X-4, X-5, X-6, X-7)</name>
  <read_first>
    - test/ui-a11y/213-2-auth-a11y.test.js (Phase 213.2 reference — mirror this exact 5-line opener and assertion shape)
    - test/ui-a11y/213-1-chrome-a11y.test.js (Phase 213.1 reference — original grep-shape pattern)
    - .planning/phases/213.2-.../213.2-05-PLAN.md Task 2 (the 213.2-05 a11y test creation pattern this plan mirrors)
    - 213.3-UI-SPEC.md "Cross-cutting acceptance criteria (Plan 213.3-09)" (lines 789–798)
    - 213.3-CONTEXT.md D-18 (test strategy)
    - 213.3-RESEARCH.md "AC → Verification Map" Cross-Cutting (lines 893–902)
    - styles/components.css line 215–218 (current `(pointer: coarse)` block — already scopes `.c-button` + `.c-nav-link` per Phase 213.2-05)
    - All 9 module.css + 11 tsx files in scope (verify post-Plans 01-08 state)
  </read_first>
  <files>test/ui-a11y/213-3-settings-a11y.test.js</files>
  <action>
**Create new file `test/ui-a11y/213-3-settings-a11y.test.js` — grep-shape a11y suite mirroring `213-2-auth-a11y.test.js` pattern.**

Required structural sections:

```js
'use strict';

const test = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const path = require('node:path');

const ROOT = process.cwd();
function read(file) { return fs.readFileSync(path.join(ROOT, file), 'utf8'); }
function exists(file) { try { fs.accessSync(path.join(ROOT, file)); return true; } catch { return false; } }

// =============================================================================
// Phase 213.3 settings a11y — cross-cutting suite
//
// Covers all 9 settings surfaces (billing, members, sessions, domain, danger,
// mcp, plugins, webhooks list, webhooks detail) per UI-SPEC AC X-1..X-7 + per-
// surface AC# (B-1..B-5, M-1..M-5, S-1..S-4, D-1..D-5, DZ-1..DZ-4, MC-1..MC-7,
// P-1..P-5, W-1..W-7).
//
// Pattern mirrors test/ui-a11y/213-2-auth-a11y.test.js (Phase 213.2-05 deliverable).
// =============================================================================

const MODULE_CSS = [
  'app/(markos)/settings/billing/page.module.css',
  'app/(markos)/settings/members/page.module.css',
  'app/(markos)/settings/sessions/page.module.css',
  'app/(markos)/settings/domain/page.module.css',
  'app/(markos)/settings/danger/page.module.css',
  'app/(markos)/settings/mcp/page.module.css',
  'app/(markos)/settings/plugins/page-shell.module.css',
  'app/(markos)/settings/webhooks/page.module.css',
  'app/(markos)/settings/webhooks/[sub_id]/page.module.css',
];

const TSX_FILES = [
  'app/(markos)/settings/billing/page.tsx',
  'app/(markos)/settings/billing/page-shell.tsx',
  'app/(markos)/settings/members/page.tsx',
  'app/(markos)/settings/sessions/page.tsx',
  'app/(markos)/settings/domain/page.tsx',
  'app/(markos)/settings/danger/page.tsx',
  'app/(markos)/settings/mcp/page.tsx',
  'app/(markos)/settings/plugins/page.tsx',
  'app/(markos)/settings/plugins/page-shell.tsx',
  'app/(markos)/settings/webhooks/page.tsx',
  'app/(markos)/settings/webhooks/[sub_id]/page.tsx',
];

const STORY_FILES = [
  'app/(markos)/settings/billing/page.stories.tsx',
  'app/(markos)/settings/members/page.stories.tsx',
  'app/(markos)/settings/sessions/page.stories.tsx',
  'app/(markos)/settings/domain/page.stories.tsx',
  'app/(markos)/settings/danger/page.stories.tsx',
  'app/(markos)/settings/mcp/page.stories.tsx',
  'app/(markos)/settings/plugins/page.stories.tsx',
  'app/(markos)/settings/webhooks/page.stories.tsx',
  'app/(markos)/settings/webhooks/[sub_id]/page.stories.tsx',
];

// =============================================================================
// AC# X-1 — Sora / Space Grotesk fully eliminated across all 9 module.css
// =============================================================================

test('AC# X-1: no Sora/Space Grotesk in any settings module.css', () => {
  for (const file of MODULE_CSS) {
    const content = read(file);
    assert.doesNotMatch(content, /Sora|Space Grotesk/, `${file} contains forbidden font (Sora or Space Grotesk)`);
  }
});

// =============================================================================
// AC# X-2 — Teal #0d9488/#0f766e/#14b8a6/#e6fffb/#0b877c eliminated
// =============================================================================

test('AC# X-2: no teal hex in any settings module.css', () => {
  for (const file of MODULE_CSS) {
    const content = read(file);
    assert.doesNotMatch(content, /#0d9488|#0f766e|#14b8a6|#e6fffb|#0b877c/i, `${file} contains forbidden teal hex`);
  }
});

// =============================================================================
// AC# X-3 — No gradients, no drop-shadows, no transform: translate*
// =============================================================================

test('AC# X-3: no gradients in any settings module.css', () => {
  for (const file of MODULE_CSS) {
    const content = read(file);
    assert.doesNotMatch(content, /linear-gradient|radial-gradient/i, `${file} contains forbidden gradient`);
  }
});

test('AC# X-3: no drop-shadows on cards in any settings module.css', () => {
  for (const file of MODULE_CSS) {
    const content = read(file);
    // Allow box-shadow on .c-modal/.c-popover from primitives — those are not in module.css
    assert.doesNotMatch(content, /box-shadow.*rgba.*45px/i, `${file} contains forbidden card drop-shadow`);
  }
});

test('AC# X-3: no transform: translate* hover-jiggle in any settings module.css', () => {
  for (const file of MODULE_CSS) {
    const content = read(file);
    assert.doesNotMatch(content, /transform:\s*translate/i, `${file} contains forbidden transform: translate`);
  }
});

// =============================================================================
// AC# X-4 — `.c-notice c-notice--{state}` composition; no local banner classes
// =============================================================================

test('AC# X-4: every banner-shaped JSX uses .c-notice c-notice--{state}', () => {
  let totalNotices = 0;
  for (const file of TSX_FILES) {
    const content = read(file);
    // Count .c-notice composition occurrences
    const matches = content.match(/c-notice c-notice--/g);
    if (matches) totalNotices += matches.length;
  }
  assert.ok(totalNotices >= 10, `Expected ≥10 .c-notice compositions across all settings tsx files, got ${totalNotices}`);
});

test('AC# X-4: no local banner/alert/holdCard/purgeBanner classes in tsx files', () => {
  for (const file of TSX_FILES) {
    const content = read(file);
    assert.doesNotMatch(content, /className=.*holdCard|className=.*purgeBanner|className=.*atCapBanner/, `${file} contains forbidden local banner class`);
  }
});

// =============================================================================
// AC# X-1 (tsx side) — Bracketed-glyph state coding
// =============================================================================

test('AC# X-4: bracketed-glyph state coding present across surfaces', () => {
  let totalGlyphs = 0;
  for (const file of TSX_FILES) {
    const content = read(file);
    const matches = content.match(/\[(ok|warn|err|info)\]/g);
    if (matches) totalGlyphs += matches.length;
  }
  assert.ok(totalGlyphs >= 10, `Expected ≥10 bracketed-glyph occurrences across all settings tsx files, got ${totalGlyphs}`);
});

// =============================================================================
// AC# X-6 — No per-module @media (pointer: coarse) redeclaration
// =============================================================================

test('AC# X-6: no per-module @media (pointer: coarse) redeclaration', () => {
  for (const file of MODULE_CSS) {
    const content = read(file);
    assert.doesNotMatch(content, /@media.*pointer.*coarse/i, `${file} redeclares (pointer: coarse) — must inherit from styles/components.css global rule`);
  }
});

// =============================================================================
// Per-surface composition (Surface 1 — Billing — AC# B-3, B-4)
// =============================================================================

test('AC# B-3: billing/page-shell.tsx composes .c-notice c-notice--warning', () => {
  const content = read('app/(markos)/settings/billing/page-shell.tsx');
  assert.match(content, /c-notice c-notice--warning/, 'billing must compose .c-notice c-notice--warning for hold state');
});

test('AC# B-4: billing/page-shell.tsx renders semantic <table>', () => {
  const content = read('app/(markos)/settings/billing/page-shell.tsx');
  assert.match(content, /<table/, 'billing must use semantic <table>');
  assert.match(content, /<thead>/, 'billing must use <thead>');
  assert.match(content, /<tbody>/, 'billing must use <tbody>');
});

// =============================================================================
// Per-surface composition (Surface 2 — Members — AC# M-3, M-4, M-5)
// =============================================================================

test('AC# M-3: members/page.tsx composes .c-notice c-notice--info', () => {
  const content = read('app/(markos)/settings/members/page.tsx');
  assert.match(content, /c-notice c-notice--info/, 'members must compose .c-notice c-notice--info for invite-pending');
});

test('AC# M-4: members/page.tsx composes .c-badge c-badge--info', () => {
  const content = read('app/(markos)/settings/members/page.tsx');
  assert.match(content, /c-badge c-badge--info/, 'members must compose .c-badge--info for pending badge');
});

test('AC# M-5: members/page.tsx composes .c-modal + .c-backdrop', () => {
  const content = read('app/(markos)/settings/members/page.tsx');
  assert.match(content, /c-modal/, 'members must use .c-modal for remove-confirm');
  assert.match(content, /c-backdrop/, 'members must use .c-backdrop for remove-confirm');
  assert.doesNotMatch(content, /"Are you sure"/, 'members must not use "Are you sure?" copy');
});

// =============================================================================
// Per-surface composition (Surface 3 — Sessions — AC# S-2, S-3)
// =============================================================================

test('AC# S-2: sessions/page.tsx composes .c-badge c-badge--success with [ok] Current', () => {
  const content = read('app/(markos)/settings/sessions/page.tsx');
  assert.match(content, /c-badge c-badge--success/, 'sessions must compose .c-badge--success');
  assert.match(content, /\[ok\] Current/, 'sessions must carry [ok] Current copy');
});

test('AC# S-3: sessions/page.tsx composes .c-button c-button--destructive + .c-status-dot--live + .c-modal', () => {
  const content = read('app/(markos)/settings/sessions/page.tsx');
  assert.match(content, /c-button c-button--destructive/, 'sessions must compose .c-button--destructive');
  assert.match(content, /c-status-dot--live/, 'sessions must compose .c-status-dot--live');
  assert.match(content, /c-modal/, 'sessions must use .c-modal for revoke confirm');
});

// =============================================================================
// Per-surface composition (Surface 4 — Domain — AC# D-2, D-3, D-4)
// =============================================================================

test('AC# D-2: domain/page.tsx composes .c-code-block inside .c-terminal', () => {
  const content = read('app/(markos)/settings/domain/page.tsx');
  assert.match(content, /c-terminal/, 'domain must use .c-terminal');
  assert.match(content, /c-code-block/, 'domain must use .c-code-block');
});

test('AC# D-3: domain/page.tsx composes 4 .c-notice state variants', () => {
  const content = read('app/(markos)/settings/domain/page.tsx');
  const states = ['success', 'info', 'warning', 'error'];
  for (const state of states) {
    assert.match(content, new RegExp(`c-notice c-notice--${state}`), `domain must compose .c-notice c-notice--${state}`);
  }
});

test('AC# D-3: domain/page.tsx composes .c-status-dot variants', () => {
  const content = read('app/(markos)/settings/domain/page.tsx');
  assert.match(content, /c-status-dot--live/, 'domain must compose .c-status-dot--live');
  assert.match(content, /c-status-dot--error/, 'domain must compose .c-status-dot--error');
});

test('AC# D-4: domain/page.tsx composes .c-button c-button--tertiary "Resolve now"', () => {
  const content = read('app/(markos)/settings/domain/page.tsx');
  assert.match(content, /c-button c-button--tertiary/, 'domain must compose .c-button--tertiary for "Resolve now" link');
});

// =============================================================================
// Per-surface composition (Surface 5 — Danger — AC# DZ-2, DZ-3)
// =============================================================================

test('AC# DZ-2: danger/page.tsx composes .c-notice c-notice--error consequence above confirm', () => {
  const content = read('app/(markos)/settings/danger/page.tsx');
  assert.match(content, /c-notice c-notice--error/, 'danger must compose .c-notice c-notice--error');
});

test('AC# DZ-3: danger/page.tsx composes consolidated .c-button c-button--destructive ×2 + .c-modal', () => {
  const content = read('app/(markos)/settings/danger/page.tsx');
  const matches = content.match(/c-button c-button--destructive/g);
  assert.ok(matches && matches.length >= 2, `danger must compose .c-button--destructive ≥2 times (delete row + modal confirm), got ${matches?.length || 0}`);
  assert.match(content, /c-modal/, 'danger must use .c-modal for delete confirm');
});

// =============================================================================
// Per-surface composition (Surface 6 — MCP — AC# MC-3, MC-4, MC-5)
// =============================================================================

test('AC# MC-3: mcp/page.tsx composes 3 .c-notice cost-state variants', () => {
  const content = read('app/(markos)/settings/mcp/page.tsx');
  const states = ['warning', 'error', 'info'];
  let count = 0;
  for (const state of states) {
    if (content.includes(`c-notice c-notice--${state}`)) count++;
  }
  assert.ok(count >= 3, `mcp must compose 3 .c-notice cost states (warning/error/info), got ${count}`);
});

test('AC# MC-4: mcp/page.tsx composes .c-chip-protocol ×2 (tools + key prefix) + .c-code-inline', () => {
  const content = read('app/(markos)/settings/mcp/page.tsx');
  const chipMatches = content.match(/c-chip-protocol/g);
  assert.ok(chipMatches && chipMatches.length >= 2, `mcp must compose .c-chip-protocol ≥2 times, got ${chipMatches?.length || 0}`);
  assert.match(content, /c-code-inline/, 'mcp must use .c-code-inline for masked secret');
});

test('AC# MC-5: mcp/page.tsx composes .c-status-dot variants + eliminates .refreshButton', () => {
  const content = read('app/(markos)/settings/mcp/page.tsx');
  assert.match(content, /c-status-dot/, 'mcp must compose .c-status-dot');
  assert.doesNotMatch(content, /styles\.refreshButton/, 'mcp must eliminate .refreshButton class ref');
});

// =============================================================================
// Per-surface composition (Surface 7 — Plugins — AC# P-2, P-3, P-4)
// =============================================================================

test('AC# P-2: plugins/page-shell.tsx composes .c-card c-card--interactive + .c-chip c-chip--mint', () => {
  const content = read('app/(markos)/settings/plugins/page-shell.tsx');
  assert.match(content, /c-card c-card--interactive/, 'plugins must compose .c-card--interactive');
  assert.match(content, /c-chip c-chip--mint/, 'plugins must compose .c-chip--mint installed badge');
});

test('AC# P-3: plugins/page-shell.tsx composes .c-chip-protocol for plugin slug', () => {
  const content = read('app/(markos)/settings/plugins/page-shell.tsx');
  assert.match(content, /c-chip-protocol/, 'plugins must compose .c-chip-protocol for plugin slug');
});

test('AC# X-3 CRITICAL: plugins/page-shell.module.css has NO linear-gradient', () => {
  const content = read('app/(markos)/settings/plugins/page-shell.module.css');
  assert.doesNotMatch(content, /linear-gradient|radial-gradient/i, 'plugins/page-shell.module.css must eliminate the .brandCard gradient');
});

// =============================================================================
// Per-surface composition (Surface 8 — Webhooks — AC# W-2, W-3, W-4, W-5)
// =============================================================================

test('AC# W-2: webhooks/page.tsx composes 3 .c-badge state variants + .c-chip-protocol', () => {
  const content = read('app/(markos)/settings/webhooks/page.tsx');
  const states = ['success', 'warning', 'error'];
  let count = 0;
  for (const state of states) {
    if (content.includes(`c-badge c-badge--${state}`)) count++;
  }
  assert.ok(count >= 3, `webhooks list must compose 3 .c-badge states, got ${count}`);
  assert.match(content, /c-chip-protocol/, 'webhooks list must use .c-chip-protocol for sub IDs');
});

test('AC# W-3: webhooks/[sub_id]/page.tsx composes 3 .c-notice variants for signing rotation', () => {
  const content = read('app/(markos)/settings/webhooks/[sub_id]/page.tsx');
  const states = ['info', 'warning', 'error'];
  let count = 0;
  for (const state of states) {
    if (content.includes(`c-notice c-notice--${state}`)) count++;
  }
  assert.ok(count >= 3, `webhooks detail must compose 3 .c-notice states (T-7/T-1/T-0), got ${count}`);
});

test('AC# W-4: webhooks/[sub_id]/page.tsx composes .c-code-inline + .c-button--icon + .c-button--destructive + .c-modal', () => {
  const content = read('app/(markos)/settings/webhooks/[sub_id]/page.tsx');
  assert.match(content, /c-code-inline/, 'webhooks detail must use .c-code-inline for masked secret');
  assert.match(content, /c-button c-button--icon/, 'webhooks detail must use .c-button--icon for clipboard');
  assert.match(content, /c-button c-button--destructive/, 'webhooks detail must use .c-button--destructive for Disable subscription');
  assert.match(content, /c-modal/, 'webhooks detail must use .c-modal for disable confirm');
});

test('AC# W-5: webhooks/page.module.css eliminates .primaryFilledButton', () => {
  const content = read('app/(markos)/settings/webhooks/page.module.css');
  assert.doesNotMatch(content, /\.primaryFilledButton|min-height:\s*48px|font-size:\s*15px|border-radius:\s*12px/, 'webhooks list must eliminate .primaryFilledButton + legacy 48px/15px/12px literals');
});

// =============================================================================
// Banned-lexicon scan across all tsx files
// =============================================================================

test('AC# X-4: banned lexicon = 0 across all settings tsx files', () => {
  const banned = /\b(synergy|leverage|empower|unlock|transform|revolutionize|supercharge|holistic|seamless|cutting-edge|innovative|game-changer|next-generation|world-class|best-in-class|reimagine|disrupt)\b/i;
  for (const file of TSX_FILES) {
    const content = read(file);
    assert.doesNotMatch(content, banned, `${file} contains banned lexicon`);
  }
});

// =============================================================================
// Storybook story file existence (X-4 / per-surface AC#)
// =============================================================================

test('AC# X-4: all 9 settings story files exist with default exports', () => {
  for (const file of STORY_FILES) {
    assert.ok(exists(file), `${file} must exist`);
    const content = read(file);
    assert.match(content, /export default meta/, `${file} must declare default meta export`);
  }
});

// =============================================================================
// AC# X-1 prerequisite — Zero inline hex in all 9 module.css files
// =============================================================================

test('AC# B-1, M-1, S-1, D-1, DZ-1, MC-1, P-1, W-1: zero inline hex in all 9 settings module.css', () => {
  for (const file of MODULE_CSS) {
    const content = read(file);
    assert.doesNotMatch(content, /#[0-9a-fA-F]{3,8}/, `${file} contains forbidden inline hex`);
  }
});

// =============================================================================
// AC# X-7 — Build smoke: pre-condition file existence
// =============================================================================

test('AC# X-7: all 9 module.css + 11 tsx files exist post-redesign', () => {
  for (const file of [...MODULE_CSS, ...TSX_FILES]) {
    assert.ok(exists(file), `${file} must exist`);
  }
});

// Total test count: ≥30 covering X-1, X-2, X-3, X-4, X-5, X-6 + per-surface AC#
// Total AC# mentions: ≥9 covering X-1, X-2, X-3, X-4, X-6, B-1/3/4, M-3/4/5, S-2/3, D-2/3/4, DZ-2/3, MC-3/4/5, P-2/3, W-2/3/4/5
```

Count tests: 30+ test blocks above.

Count AC# mentions: AC# X-1, X-2, X-3, X-4, X-6, B-1, B-3, B-4, M-1, M-3, M-4, M-5, S-1, S-2, S-3, D-1, D-2, D-3, D-4, DZ-1, DZ-2, DZ-3, MC-1, MC-3, MC-4, MC-5, P-1, P-2, P-3, W-1, W-2, W-3, W-4, W-5, X-7 — well over 9.

After creating the file, run `node --test test/ui-a11y/213-3-settings-a11y.test.js` — MUST exit 0. If any test fails, EITHER (a) fix the corresponding plan's deliverable (uncommon at this point — plans 01-08 should already be complete) OR (b) refine the assertion regex if the deliverable is correct but the regex was overly strict.

Then run the full battery to confirm Phase 213.3 phase-close (X-7):
- `npm run build` — exits 0
- `npm run build-storybook` — exits 0
- `npm test` — exits 0 (all Phase 200/201/202/203/204 wiring contracts + Phase 213.1 + 213.2 + 213.3 a11y suites)
- `node --test test/ui-a11y/213-1-chrome-a11y.test.js test/ui-a11y/213-1-light-and-forced-colors.test.js test/ui-a11y/213-2-auth-a11y.test.js test/ui-a11y/213-3-settings-a11y.test.js` — all exit 0

Atomic commit closes the phase: `test(213.3-09): add 213-3-settings-a11y.test.js cross-cutting suite + close phase 213.3`.
  </action>
  <verify>
    <automated>test -f "test/ui-a11y/213-3-settings-a11y.test.js" && grep -cE "^test\(" "test/ui-a11y/213-3-settings-a11y.test.js" | awk '$1>=30 {exit 0} $1<30 {exit 1}' && grep -cE "AC#" "test/ui-a11y/213-3-settings-a11y.test.js" | awk '$1>=9 {exit 0} $1<9 {exit 1}' && grep -q "213.3 settings a11y" "test/ui-a11y/213-3-settings-a11y.test.js" && node --test test/ui-a11y/213-3-settings-a11y.test.js && node --test test/ui-a11y/213-1-chrome-a11y.test.js test/ui-a11y/213-2-auth-a11y.test.js test/ui-a11y/213-1-light-and-forced-colors.test.js && npm run build && npm run build-storybook && npm test && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **X-5 (file exists with required test count + AC mentions):** `test -f "test/ui-a11y/213-3-settings-a11y.test.js"` returns 0; `grep -cE "^test\(" "test/ui-a11y/213-3-settings-a11y.test.js"` ≥30; `grep -cE "AC#" "test/ui-a11y/213-3-settings-a11y.test.js"` ≥9.
    - **X-5 (test suite exits 0):** `node --test test/ui-a11y/213-3-settings-a11y.test.js` exits 0.
    - **X-1, X-2, X-3, X-6 (verified by suite):** All cross-cutting assertions pass via `node --test` exit code.
    - **X-4 (verified by suite):** `.c-notice` composition count ≥10; banned-lexicon = 0 across all tsx; story files exist; bracketed glyphs present.
    - **X-7 (full build + test green):** `npm run build && npm run build-storybook && npm test` exits 0.
    - **Phase 213.1 + 213.2 a11y suites still green:** `node --test test/ui-a11y/213-1-chrome-a11y.test.js test/ui-a11y/213-2-auth-a11y.test.js test/ui-a11y/213-1-light-and-forced-colors.test.js` exits 0.
  </acceptance_criteria>
  <done>`test/ui-a11y/213-3-settings-a11y.test.js` created with ≥30 tests + ≥9 AC# mentions covering all 9 surfaces + cross-cutting; full Phase 213.3 phase-close verification passes (X-7); atomic commit closes the phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite → all 20 settings files (9 module.css + 11 tsx) | grep-shape verification has read-only filesystem access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213.3-09-01 | Tampering | a11y test suite assertions | accept | Test file is grep-shape only; no mutation; deterministic against committed source files |
</threat_model>

<verification>
After Task 1 complete:

```bash
test -f "test/ui-a11y/213-3-settings-a11y.test.js"  # 0
grep -cE "^test\(" "test/ui-a11y/213-3-settings-a11y.test.js"  # >=30
grep -cE "AC#" "test/ui-a11y/213-3-settings-a11y.test.js"  # >=9
node --test test/ui-a11y/213-3-settings-a11y.test.js  # 0
node --test test/ui-a11y/213-1-chrome-a11y.test.js test/ui-a11y/213-2-auth-a11y.test.js test/ui-a11y/213-1-light-and-forced-colors.test.js  # 0
npm run build  # 0
npm run build-storybook  # 0
npm test  # 0
```
</verification>

<success_criteria>
- AC X-1, X-2, X-3, X-4, X-5, X-6, X-7 close.
- Phase 213.3 phase-close gate passes: `npm test` + `npm run build` + `npm run build-storybook` all exit 0.
- Phase 213.1 + 213.2 a11y suites continue to pass.
- All Phase 200/200.1/201/202/203/204 wiring contracts continue to pass.
- All 49 ACs across 9 surfaces verified (B-1..B-5, M-1..M-5, S-1..S-4, D-1..D-5, DZ-1..DZ-4, MC-1..MC-7, P-1..P-5, W-1..W-7, X-1..X-7).
- Global `@media (pointer: coarse)` rule from Phase 213.2 inherits cleanly into all settings (no per-module redeclaration).
- Atomic commit shape: `test(213.3-09): add 213-3-settings-a11y.test.js cross-cutting suite + close phase 213.3`.
</success_criteria>

<output>
After completion, create `.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-09-SUMMARY.md` documenting:
- Files created (test/ui-a11y/213-3-settings-a11y.test.js)
- Test count (target ≥30, achieved N)
- AC# mention count (target ≥9, achieved N)
- AC closures: X-1..X-7 + cross-cutting verification of B/M/S/D/DZ/MC/P/W per-surface AC#
- Phase 213.3 phase-close verification: `npm test`, `npm run build`, `npm run build-storybook` all green
- Phase 213.1 + 213.2 a11y suites continue green confirmation
- Phase 200/201/202/203/204 wiring contract preservation summary
- Manual visual review reminder (Chromatic baselines for 9 settings story files)
- Phase 213.3 closed; ready for /gsd-verify-work
</output>
