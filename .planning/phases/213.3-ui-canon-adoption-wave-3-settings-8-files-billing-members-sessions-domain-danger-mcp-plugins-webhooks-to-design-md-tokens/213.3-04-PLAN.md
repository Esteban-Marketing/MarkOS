---
phase: 213.3
plan: 04
type: execute
wave: 1
depends_on:
  - "213.3-03"
files_modified:
  - app/(markos)/settings/domain/page.module.css
  - app/(markos)/settings/domain/page.tsx
  - app/(markos)/settings/domain/page.stories.tsx
autonomous: true
requirements:
  - D-1
  - D-2
  - D-3
  - D-4
  - D-5
must_haves:
  truths:
    - "app/(markos)/settings/domain/page.module.css contains zero inline hex (note: original file is 22 LOC with 19 hex — densest hex/LOC ratio in the wave); zero off-grid spacing; zero Sora/Space Grotesk/gradients/box-shadow/transform-translate; zero teal hexes; zero `Space Grotesk` monospace"
    - "app/(markos)/settings/domain/page.tsx replaces `.cnameBlock` with `.c-code-block` inside `.c-terminal` wrapper (D-2); no `font-family: 'Space Grotesk', monospace`"
    - "app/(markos)/settings/domain/page.tsx all 4 DNS states render via `.c-notice c-notice--{success,info,warning,error}` per state (D-3); status-dot color-only signal eliminated; `.c-status-dot--live`/default/`--error` modifiers used + bracketed glyph"
    - "app/(markos)/settings/domain/page.tsx rotation grace renders `.c-notice c-notice--warning` + `.c-button c-button--tertiary` 'Resolve now' link (D-4); `.removeButton` replaced by `.c-button c-button--destructive`"
    - "app/(markos)/settings/domain/page.tsx composes `.c-card`, `.c-input`, `.c-field`, `.c-field__label`, `.c-field__help`, `.c-button{,--primary,--destructive,--tertiary}`, `.c-code-block`, `.c-terminal` per UI-SPEC Surface 4"
    - "app/(markos)/settings/domain/page.stories.tsx (NEW) declares ≥5 named state stories: `Default`, `Pending`, `Verified`, `RotationGrace`, `Failed`"
    - "Banned-lexicon scan = 0; no exclamation; no emoji; no \"Please\""
    - "DOM tree + prop interface UNCHANGED per CONTEXT D-05; Phase 201 domain wiring (claim flow + DNS verification poll + rotation-grace API calls) preserved per D-21 (D-5)"
    - "`npm run build` exits 0; `npm run build-storybook` exits 0; `node --test test/auth/ test/tenancy/ test/tenant-auth/` exits 0"
  artifacts:
    - path: "app/(markos)/settings/domain/page.module.css"
      provides: "Token-only domain layout module — page/card/form layout + status-dot helper; primitives carry button/input/field/notice/code-block composition. LOC will GROW (22 → ~35) due to 4 new notice state compositions replacing color-only `.statusDot[data-status]`."
      min_lines: 30
      contains: "var(--color-surface)"
    - path: "app/(markos)/settings/domain/page.tsx"
      provides: "Updated domain page composing primitives; 4 `.c-notice` states (success/info/warning/error) + `.c-status-dot` modifiers + bracketed-glyph state coding; preserves Phase 201 wiring"
      contains: "c-notice c-notice--"
    - path: "app/(markos)/settings/domain/page.stories.tsx"
      provides: "Storybook 8 CSF3 ≥5 named state exports (Default / Pending / Verified / RotationGrace / Failed)"
      exports: ["default", "Default", "Pending", "Verified", "RotationGrace", "Failed"]
  key_links:
    - from: "app/(markos)/settings/domain/page.tsx"
      to: "styles/components.css 4 `.c-notice` state variants"
      via: "className composition for DNS state banners"
      pattern: "c-notice c-notice--(success|info|warning|error)"
    - from: "app/(markos)/settings/domain/page.tsx"
      to: "styles/components.css `.c-code-block` + `.c-terminal` primitives"
      via: "CNAME record display"
      pattern: "c-(code-block|terminal)"
---

<objective>
Redesign `/settings/domain` (`page.module.css` 22 LOC + `page.tsx` 179 LOC) into the canonical DESIGN.md v1.1.0 visual contract per `213.3-UI-SPEC.md` Surface 4 (5 ACs: D-1 through D-5). This is the surface with the highest hex-density (19 hex / 22 LOC) and the only surface where module.css LOC GROWS (22 → ~35) — driven by 4 new notice state compositions (DNS Pending/Verified/RotationGrace/Failed) that replace the legacy color-only `.statusDot[data-status]` pattern.

Compose `.c-card` / `.c-input` / `.c-field{,__label,__help}` / `.c-button{,--primary,--destructive,--tertiary}` / `.c-code-block` inside `.c-terminal` / `.c-notice c-notice--{success,info,warning,error}` / `.c-status-dot{,--live,--error}` per UI-SPEC primitive reuse.

Purpose: Close UI-SPEC AC D-1 (zero inline hex), D-2 (`.cnameBlock` → `.c-code-block` inside `.c-terminal`, no `Space Grotesk` monospace), D-3 (4 DNS states via `.c-notice` + status-dot modifiers + bracketed glyph), D-4 (rotation grace `.c-notice c-notice--warning` + `.c-button--tertiary` + `.removeButton` → `.c-button--destructive`), D-5 (Phase 201 domain wiring preserved).

Output: `page.module.css` rewritten (22 → ~35 LOC, GROWS); `page.tsx` updated (4 new `.c-notice` JSX compositions, status-dot pairing); new `page.stories.tsx` (~80 LOC, 5 CSF3 named state exports — most state-dense surface in the wave).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-CONTEXT.md
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-UI-SPEC.md
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-RESEARCH.md
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-VALIDATION.md
@DESIGN.md
@CLAUDE.md
@app/tokens.css
@app/globals.css
@styles/components.css
@app/(markos)/settings/domain/page.module.css
@app/(markos)/settings/domain/page.tsx
@app/(markos)/_components/RotationGraceBanner.tsx
@app/(markos)/_components/RotationGraceBanner.module.css
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-03-PLAN.md

<interfaces>
From `213.3-RESEARCH.md` Class-Rename Map: Surface 4 — Domain (lines 426–457):
- `.page` → Standard pattern
- `.contentCard` → DELETE; compose `.c-card` + local `max-width: 900px`
- `.heading` → DELETE; plain `<h2>` (globals)
- `.subheading` → DELETE; apply `.t-lead`
- `.addForm` → Local: `display: flex; flex-direction: column; gap: var(--space-xs);`
- `.label` → DELETE; compose `.c-field__label` in `.c-field` wrapper
- `.input` → DELETE; compose `.c-input` (D-2 — eliminates `#f8fbfd` bg, `border-radius: 12px`, teal `#0d9488` focus)
- `.primaryCta` → DELETE; compose `.c-button c-button--primary`
- `.removeButton` → DELETE; compose `.c-button c-button--destructive` (D-4)
- `.cnameBlock` → DELETE; compose `.c-code-block` inside `.c-terminal` wrapper (D-2 — eliminates `'Space Grotesk', monospace`, `#f5f7fa` bg, `border-radius: 12px`)
- `.helpText` → DELETE; compose `.c-field__help`
- `.domainPanel` → DELETE; compose `.c-card`
- `.statusDot[data-status]` → DELETE; compose `.c-status-dot--live` / `.c-status-dot` (default) / `.c-status-dot--error` + bracketed glyph (D-3 — eliminates color-only signals `#0f766e`/`#b45309`/`#f87171`)
- `.colorInput` → out-of-scope (preserve token-cited if remains)
- `.toggleRow` → Local: token-cited
- `.toast` → DELETE; compose `.c-toast c-toast--{success,error}`
- (NEW) DNS verified notice → `.c-notice c-notice--success` `[ok] Domain verified.` (D-3)
- (NEW) DNS pending notice → `.c-notice c-notice--info` `[info] DNS verification pending.` (D-3)
- (NEW) Rotation grace notice → `.c-notice c-notice--warning` `[warn] Domain rotation in progress.` + `.c-button c-button--tertiary` "Resolve now" (D-4)
- (NEW) DNS failed notice → `.c-notice c-notice--error` `[err] Verification failed.` (D-3)

From `213.3-UI-SPEC.md` Surface 4 Copywriting Contract (lines 248–262):
- Page heading: "Custom domain"
- Input label: "Subdomain"
- Input help: "Enter your custom subdomain (e.g. acme.markos.run)."
- Add domain CTA: "Add domain" (verb + noun)
- Remove domain CTA: "Remove domain" (verb + noun)
- Rotation grace notice: `[warn] Domain rotation in progress. Both the old and new domains are active during the grace period. Resolve now.` + "Resolve now" tertiary link
- Verified notice: `[ok] Domain verified. Your custom domain is active.`
- Pending notice: `[info] DNS verification pending. This may take up to 48 hours.`
- Failed notice: `[err] Verification failed. Check your DNS records and retry.`
- CNAME block label: "CNAME record" via `.t-label-caps`
- Status dot pairing: `.c-status-dot--live` + `[ok] Verified` / `.c-status-dot` + `[warn] Pending` / `.c-status-dot--error` + `[err] Failed`

From `styles/components.css` v1.1.0 (`.c-code-block` + `.c-terminal`):
```css
.c-terminal { /* dark surface frame, monospace, padding, border-radius */ }
.c-code-block { /* inside .c-terminal: font-family var(--font-mono), word-break, padding */ }
```

From CONTEXT.md (Specifics): "domain rotation grace mirrors the chrome-level RotationGraceBanner pattern. The settings/domain surface composes `.c-notice c-notice--warning` with bracketed `[warn]` and a `.c-button.c-button--tertiary` for 'Resolve now' linking to the domain resolution flow. The chrome-level RotationGraceBanner stays as-is (Phase 213.1) — settings/domain is the deeper drill-down surface."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite app/(markos)/settings/domain/page.module.css to token-only layout module (D-1, X-1, X-2, X-3) — GROWS from 22 LOC to ~35 LOC due to 4 new notice-state JSX-side compositions; module retains layout + form-wrapper + status-cell helper</name>
  <read_first>
    - app/(markos)/settings/domain/page.module.css (current 22 LOC, 19 hex — densest hex/LOC ratio)
    - app/(markos)/settings/domain/page.tsx (current 179 LOC — confirm classNames referenced)
    - 213.3-RESEARCH.md "Class-Rename Map: Surface 4 — Domain" (lines 426–457)
    - 213.3-UI-SPEC.md "Domain" Component Primitive Reuse (lines 423–442)
    - app/(markos)/_components/RotationGraceBanner.module.css (Phase 213.1 reference for legacy banner alpha-tint pattern — superseded by `.c-notice` primitive)
    - app/tokens.css (--color-surface/-on-surface/-muted, --space-xs/-md, --radius-md, --fs-body-md/-label)
    - styles/components.css v1.1.0 (`.c-card`, `.c-input`, `.c-field`, `.c-field__label`, `.c-field__help`, `.c-button*`, `.c-code-block`, `.c-terminal`, `.c-notice*`, `.c-status-dot*`)
    - .planning/phases/213.3-.../213.3-03-PLAN.md Task 1 (precedent)
  </read_first>
  <files>app/(markos)/settings/domain/page.module.css</files>
  <action>
**Replace `app/(markos)/settings/domain/page.module.css` ENTIRELY with a token-only layout module. LOC GROWS (22 → ~35) because the legacy `.statusDot[data-status]` color-only pattern is replaced by primitive composition + 4 new notice states (composed JSX-side, but module needs a `.statusCell` helper to position dot+label).**

Required structural sections:

1. **`.page`**: `padding: var(--space-md); background: var(--color-surface); display: flex; flex-direction: column; gap: var(--space-md);`
2. **`.contentCard`**: local `max-width: 900px;`
3. **`.addForm`**: `display: flex; flex-direction: column; gap: var(--space-xs);`
4. **`.toggleRow`**: `display: flex; gap: var(--space-xs); align-items: center; font-size: var(--fs-body-md); color: var(--color-on-surface-muted);`
5. **`.statusCell`**: `display: inline-flex; align-items: center; gap: var(--space-xs);` (for status-dot + bracketed-glyph label pairing — D-3)
6. **`.cnameLabel`**: layout-only — `display: block; margin-bottom: var(--space-xs);` (label spacing above the `.c-terminal` block)
7. **`.actionRow`**: `display: flex; gap: var(--space-xs); flex-wrap: wrap; margin-top: var(--space-sm);`
8. **`.emptyState`**: `font-size: var(--fs-body-md); color: var(--color-on-surface-muted);`

DELETE entirely:
- `.heading` / `.subheading`
- `.label` / `.input` / `.primaryCta` / `.removeButton` / `.helpText` / `.cnameBlock` / `.domainPanel` / `.statusDot` (all primitives)
- `.toast`
- All teal `#0d9488`/`#0f766e`/`#14b8a6`/`#e6fffb`/`#0b877c` hexes
- All slate hexes
- All orange-red `#9a3412`/`#fca5a5`/`#b45309`/`#f87171`
- All `font-family: 'Space Grotesk', monospace` declarations (replaced by `.c-terminal` + `.c-code-block` primitives)
- All `background: #f5f7fa`/`#f8fbfd`/`#ffffff` declarations
- All `border-radius: 12px`/`20px` literals (composed via primitives)
- All `font-size: 14px` literals

DO NOT redeclare `@media (pointer: coarse)`. DO NOT add `outline:` declarations. DO NOT keep `.statusDot[data-status="..."]` selectors.
  </action>
  <verify>
    <automated>! grep -nE '#[0-9a-fA-F]{3,8}' "app/(markos)/settings/domain/page.module.css" && ! grep -niE '(Sora|Space Grotesk|linear-gradient|radial-gradient|box-shadow|0 18px 45px|transform:\s*translate)' "app/(markos)/settings/domain/page.module.css" && ! grep -niE '#0d9488|#0f766e|#14b8a6|#e6fffb|#0b877c|#9a3412|#fca5a5|#b45309|#f87171' "app/(markos)/settings/domain/page.module.css" && ! grep -nE '@media.*pointer.*coarse' "app/(markos)/settings/domain/page.module.css" && ! grep -nE 'statusDot\[data-status' "app/(markos)/settings/domain/page.module.css" && grep -q "var(--color-surface)" "app/(markos)/settings/domain/page.module.css" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **D-1 (Zero inline hex):** `grep -nE '#[0-9a-fA-F]{3,8}' "app/(markos)/settings/domain/page.module.css"` returns 0 (eliminates all 19 originals).
    - **No off-grid:** `grep -nE ':\s*[0-9]+px' "app/(markos)/settings/domain/page.module.css"` returns ONLY `1px` (border).
    - **No Space Grotesk monospace:** `grep -niE "Space Grotesk" "app/(markos)/settings/domain/page.module.css"` returns 0.
    - **No teal/orange-red:** `grep -niE "#0d9488|#0f766e|#14b8a6|#e6fffb|#0b877c|#9a3412|#fca5a5|#b45309|#f87171" "app/(markos)/settings/domain/page.module.css"` returns 0.
    - **No `.statusDot[data-status]` color-only pattern:** `grep -nE 'statusDot\[data-status' "app/(markos)/settings/domain/page.module.css"` returns 0.
    - **X-6:** `grep -nE '@media.*pointer.*coarse' "app/(markos)/settings/domain/page.module.css"` returns 0.
  </acceptance_criteria>
  <done>`page.module.css` rewritten to ~35 LOC token-only layout (GROWS from 22 LOC by design); 6 grep gates pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update app/(markos)/settings/domain/page.tsx to compose 4 `.c-notice c-notice--{success,info,warning,error}` states + `.c-status-dot{,--live,--error}` modifiers + bracketed glyph + `.c-code-block` inside `.c-terminal` + `.c-input`/`.c-field`/`.c-button*` per UI-SPEC Surface 4 (D-2, D-3, D-4); preserve Phase 201 wiring (D-5)</name>
  <read_first>
    - app/(markos)/settings/domain/page.tsx (current 179 LOC — DOM tree to preserve per D-05)
    - app/(markos)/settings/domain/page.module.css (post-Task 1 shape)
    - 213.3-UI-SPEC.md "Domain" Copywriting Contract (lines 248–262) + Component Primitive Reuse (lines 423–442)
    - 213.3-RESEARCH.md "Class-Rename Map: Surface 4 — Domain" (lines 426–457)
    - 213.3-RESEARCH.md "Pattern 2 — `.c-notice` Composition" (lines 256–293)
    - styles/components.css v1.1.0 (.c-card, .c-input, .c-field*, .c-button*, .c-code-block, .c-terminal, .c-notice*, .c-status-dot*)
    - DESIGN.md "Components > Notice/Banner" + "Components > Code blocks" + "Status indicators · Color blindness"
    - test/auth/ + test/tenancy/ + test/tenant-auth/ (Phase 201 domain wiring tests)
  </read_first>
  <files>app/(markos)/settings/domain/page.tsx</files>
  <action>
**Update `app/(markos)/settings/domain/page.tsx` per the Class-Rename Map JSX swaps (DOM tree + prop interface UNCHANGED per D-05).**

ClassName + JSX-shape substitutions:

| Existing JSX | New JSX | Citation |
|--------------|---------|----------|
| `<div className={styles.contentCard}>` | `<div className={`c-card ${styles.contentCard}`}>` | `.c-card` |
| `<h2 className={styles.heading}>Custom domain</h2>` | `<h2>Custom domain</h2>` | globals |
| `<p className={styles.subheading}>...</p>` | `<p className="t-lead">...</p>` | `.t-lead` |
| `<form className={styles.addForm}>` | `<form className={styles.addForm}>` (UNCHANGED — layout) | layout |
| `<label className={styles.label}>Subdomain</label>` | (wrapped in `.c-field`): `<div className="c-field"><label htmlFor="subdomain" className="c-field__label">Subdomain</label>...</div>` | `.c-field` + `.c-field__label` |
| `<input className={styles.input} ...>` | `<input id="subdomain" name="subdomain" type="text" required className="c-input" />` | `.c-input` |
| `<p className={styles.helpText}>...</p>` | `<p className="c-field__help">Enter your custom subdomain (e.g. acme.markos.run).</p>` | `.c-field__help` |
| `<button className={styles.primaryCta}>Add domain</button>` | `<button type="submit" className="c-button c-button--primary">Add domain</button>` | `.c-button--primary` |
| `<button className={styles.removeButton}>Remove</button>` | `<button type="button" className="c-button c-button--destructive">Remove domain</button>` | `.c-button--destructive` (D-4) + verb+noun |

**D-2 — CNAME block via `.c-code-block` inside `.c-terminal`:**

```tsx
<div className={styles.cnameLabel}>
  <span className="t-label-caps">CNAME record</span>
</div>
<div className="c-terminal">
  <pre className="c-code-block">
    <code>{cnameValue}</code>
  </pre>
</div>
```

**D-3 — 4 DNS state notice compositions (one renders at a time per state):**

```tsx
{dnsState === 'verified' && (
  <div className="c-notice c-notice--success" role="status">
    <strong>[ok]</strong>{" "}Domain verified. Your custom domain is active.
  </div>
)}
{dnsState === 'pending' && (
  <div className="c-notice c-notice--info" role="status">
    <strong>[info]</strong>{" "}DNS verification pending. This may take up to 48 hours.
  </div>
)}
{dnsState === 'rotating' && (
  <div className="c-notice c-notice--warning" role="status">
    <strong>[warn]</strong>{" "}Domain rotation in progress. Both the old and new domains are active during the grace period.{" "}
    <a href={resolveUrl} className="c-button c-button--tertiary">Resolve now</a>
  </div>
)}
{dnsState === 'failed' && (
  <div className="c-notice c-notice--error" role="status">
    <strong>[err]</strong>{" "}Verification failed. Check your DNS records and retry.
  </div>
)}
```

**D-3 — Status-dot + bracketed glyph pairing (replaces `.statusDot[data-status]`):**

```tsx
{/* In the domain summary row, render status-dot + label per state */}
<div className={styles.statusCell}>
  {dnsState === 'verified' && (
    <>
      <span className="c-status-dot c-status-dot--live" aria-hidden="true" />
      <span>[ok] Verified</span>
    </>
  )}
  {dnsState === 'pending' && (
    <>
      <span className="c-status-dot" aria-hidden="true" />
      <span>[warn] Pending</span>
    </>
  )}
  {dnsState === 'failed' && (
    <>
      <span className="c-status-dot c-status-dot--error" aria-hidden="true" />
      <span>[err] Failed</span>
    </>
  )}
</div>
```

KEY rules:
- `<a className="c-button c-button--tertiary">Resolve now</a>` — text-link button posture for D-4 "Resolve now" link inside the rotation-grace notice
- Each notice leads with `<strong>[glyph]</strong>` in JetBrains Mono (primitive applies font-family)
- `aria-hidden="true"` on status-dot (purely decorative; label text carries the meaning)
- `role="status"` on notices that may render dynamically (announce to SR)

**Banned-lexicon check:** Reject `Please|Are you sure|synergy|leverage|empower|unlock|transform|seamless|innovative`. NO exclamation. NO emoji.

**Phase 201 wiring (D-5 / D-21):** Domain claim flow, DNS verification poll, rotation-grace API calls — ALL unchanged. ONLY className strings + JSX shape change.

After editing, run `npm run build` + `node --test test/auth/ test/tenancy/ test/tenant-auth/` — all MUST exit 0.
  </action>
  <verify>
    <automated>grep -cE 'c-notice c-notice--(success|info|warning|error)' "app/(markos)/settings/domain/page.tsx" | awk '$1>=4 {exit 0} $1<4 {exit 1}' && grep -qE 'c-status-dot--live' "app/(markos)/settings/domain/page.tsx" && grep -qE 'c-status-dot--error' "app/(markos)/settings/domain/page.tsx" && grep -qE 'c-terminal' "app/(markos)/settings/domain/page.tsx" && grep -qE 'c-code-block' "app/(markos)/settings/domain/page.tsx" && grep -qE 'c-button c-button--destructive' "app/(markos)/settings/domain/page.tsx" && grep -qE 'c-button c-button--tertiary' "app/(markos)/settings/domain/page.tsx" && grep -qE 'c-input' "app/(markos)/settings/domain/page.tsx" && grep -qE 'c-field__help' "app/(markos)/settings/domain/page.tsx" && grep -qE '\[ok\]|\[warn\]|\[err\]|\[info\]' "app/(markos)/settings/domain/page.tsx" && ! grep -inE 'Please|synergy|leverage|empower|unlock|transform|seamless|innovative' "app/(markos)/settings/domain/page.tsx" && npm run build && node --test test/auth/ test/tenancy/ test/tenant-auth/ && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **D-2 (`.c-code-block` inside `.c-terminal`):** `grep -cE 'c-terminal' "app/(markos)/settings/domain/page.tsx"` ≥1; `grep -cE 'c-code-block' "app/(markos)/settings/domain/page.tsx"` ≥1.
    - **D-3 (4 notice states present):** `grep -cE 'c-notice c-notice--(success|info|warning|error)' "app/(markos)/settings/domain/page.tsx"` ≥4 distinct.
    - **D-3 (status-dot variants):** `grep -cE 'c-status-dot--live' "app/(markos)/settings/domain/page.tsx"` ≥1; `grep -cE 'c-status-dot--error' "app/(markos)/settings/domain/page.tsx"` ≥1.
    - **D-3 (bracketed glyphs):** `grep -cE '\[ok\]|\[warn\]|\[err\]|\[info\]' "app/(markos)/settings/domain/page.tsx"` ≥3 distinct glyph types.
    - **D-4 (rotation `.c-notice c-notice--warning` + tertiary "Resolve now"):** `grep -cE 'c-notice c-notice--warning' "app/(markos)/settings/domain/page.tsx"` ≥1; `grep -cE 'c-button c-button--tertiary' "app/(markos)/settings/domain/page.tsx"` ≥1.
    - **D-4 (`.removeButton` → `.c-button--destructive`):** `grep -cE 'c-button c-button--destructive' "app/(markos)/settings/domain/page.tsx"` ≥1.
    - **Form primitives:** `grep -cE 'c-input' "app/(markos)/settings/domain/page.tsx"` ≥1; `grep -cE 'c-field__help' "app/(markos)/settings/domain/page.tsx"` ≥1.
    - **Banned-lexicon = 0:** `grep -inE 'Please|synergy|leverage|empower|unlock|transform|seamless|innovative' "app/(markos)/settings/domain/page.tsx"` returns 0.
    - **D-5 (Phase 201 wiring):** `node --test test/auth/ test/tenancy/ test/tenant-auth/` exits 0.
    - **`npm run build` exits 0.**
  </acceptance_criteria>
  <done>`page.tsx` updated; 4 notice states + 3 status-dot variants + bracketed glyphs + code-block inside terminal; `.c-button--destructive` for Remove + `.c-button--tertiary` for Resolve now; Phase 201 wiring preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create app/(markos)/settings/domain/page.stories.tsx with ≥5 named state stories (`Default`, `Pending`, `Verified`, `RotationGrace`, `Failed`) per UI-SPEC X-4 — exercises all 4 notice states + status-dot variants</name>
  <read_first>
    - app/(markos)/settings/domain/page.tsx (post-Task 2 shape)
    - 213.3-UI-SPEC.md "Storybook State Coverage" line 580
    - .planning/phases/213.3-.../213.3-03-PLAN.md Task 3 (precedent)
  </read_first>
  <files>app/(markos)/settings/domain/page.stories.tsx</files>
  <action>
**Create new `app/(markos)/settings/domain/page.stories.tsx` with 5 CSF3 named state stories per UI-SPEC X-4 line 580:**

```tsx
import type { Meta, StoryObj } from '@storybook/react';
import DomainPage from './page';

const meta = {
  title: 'Settings/Domain',
  component: DomainPage,
  parameters: { layout: 'fullscreen' },
} satisfies Meta<typeof DomainPage>;

export default meta;

type Story = StoryObj<typeof meta>;

const baseDomain = {
  subdomain: 'acme',
  fullDomain: 'acme.markos.run',
  cnameTarget: 'cname.markos.run',
};

export const Default: Story = {
  args: { domain: null, dnsState: 'idle' },
};

export const Pending: Story = {
  args: { domain: baseDomain, dnsState: 'pending' },
  parameters: {
    docs: { description: { story: 'Renders `.c-notice c-notice--info` + `.c-status-dot` (default) + `[info] DNS verification pending` per UI-SPEC AC D-3.' } },
  },
};

export const Verified: Story = {
  args: { domain: baseDomain, dnsState: 'verified' },
  parameters: {
    docs: { description: { story: 'Renders `.c-notice c-notice--success` + `.c-status-dot--live` + `[ok] Verified` per UI-SPEC AC D-3.' } },
  },
};

export const RotationGrace: Story = {
  args: { domain: baseDomain, dnsState: 'rotating', rotationDeadline: '2026-05-15' },
  parameters: {
    docs: { description: { story: 'Renders `.c-notice c-notice--warning` + `.c-button c-button--tertiary` "Resolve now" per UI-SPEC AC D-4.' } },
  },
};

export const Failed: Story = {
  args: { domain: baseDomain, dnsState: 'failed', errorMessage: 'CNAME record not found' },
  parameters: {
    docs: { description: { story: 'Renders `.c-notice c-notice--error` + `.c-status-dot--error` + `[err] Failed` per UI-SPEC AC D-3.' } },
  },
};
```

KEY rules:
- 5 named state exports per UI-SPEC line 580
- Each non-Default state exercises a distinct `.c-notice` variant + `.c-status-dot` modifier
- `args` shape mirrors `DomainPage` prop interface
- DOES NOT set `parameters.theme: "legacy"`

After editing, run `npm run build-storybook` — MUST exit 0.
  </action>
  <verify>
    <automated>test -f "app/(markos)/settings/domain/page.stories.tsx" && grep -cE '^export const (Default|Pending|Verified|RotationGrace|Failed):' "app/(markos)/settings/domain/page.stories.tsx" | awk '$1>=5 {exit 0} $1<5 {exit 1}' && ! grep -qE 'parameters\.theme.*"legacy"' "app/(markos)/settings/domain/page.stories.tsx" && grep -q 'export default meta' "app/(markos)/settings/domain/page.stories.tsx" && npm run build-storybook && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **X-4 (≥5 named state stories):** `grep -cE '^export const (Default|Pending|Verified|RotationGrace|Failed):' "app/(markos)/settings/domain/page.stories.tsx"` ≥5.
    - **CSF3 typed `meta`:** `grep -c 'export default meta' "app/(markos)/settings/domain/page.stories.tsx"` ≥1.
    - **Default-not-legacy theme:** `grep -cE 'parameters\.theme.*"legacy"' "app/(markos)/settings/domain/page.stories.tsx"` returns 0.
    - **`npm run build-storybook` exits 0.**
  </acceptance_criteria>
  <done>`page.stories.tsx` created with 5 CSF3 named state exports exercising all 4 DNS notice states; `npm run build-storybook` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→domain claim API | Untrusted subdomain claim crosses via Phase 201 tenancy wiring |
| DNS verification poll | Server polls DNS records (3rd-party); copy explicitly states 48-hour timeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213.3-04-01 | Tampering | domain claim handler | accept | Wiring preserved verbatim from Phase 201 contract |
| T-213.3-04-02 | Information Disclosure | CNAME record exposure | accept | CNAME values are tenant-scoped + non-secret per Phase 201 design |
| T-213.3-04-03 | Spoofing | rotation-grace "Resolve now" link | mitigate | `.c-button--tertiary` text-link composition + bracketed `[warn]` glyph + explicit copy "Both the old and new domains are active" |
</threat_model>

<verification>
After all 3 tasks:

```bash
grep -nE '#[0-9a-fA-F]{3,8}' "app/(markos)/settings/domain/page.module.css"  # 0
grep -cE 'c-notice c-notice--(success|info|warning|error)' "app/(markos)/settings/domain/page.tsx"  # >=4
grep -cE 'c-status-dot--(live|error)' "app/(markos)/settings/domain/page.tsx"  # >=2
grep -cE 'c-terminal' "app/(markos)/settings/domain/page.tsx"  # >=1
grep -cE '^export const (Default|Pending|Verified|RotationGrace|Failed):' "app/(markos)/settings/domain/page.stories.tsx"  # >=5
npm run build  # 0
npm run build-storybook  # 0
node --test test/auth/ test/tenancy/ test/tenant-auth/  # 0
```
</verification>

<success_criteria>
- AC D-1, D-2, D-3, D-4, D-5 close.
- Phase 201 domain wiring preserved.
- 4 DNS notice states + 3 status-dot variants composed.
- Atomic commit: `feat(213.3-04): redesign /settings/domain surface to DESIGN.md v1.1.0 tokens + 4 .c-notice states + .c-status-dot/.c-code-block/.c-terminal composition + Storybook state coverage`.
</success_criteria>

<output>
Create `213.3-04-SUMMARY.md` documenting files modified, AC closures (D-1..D-5), hand-off to Plan 213.3-05 (danger).
</output>
