---
phase: 213.3
plan: 03
type: execute
wave: 1
depends_on:
  - "213.3-02"
files_modified:
  - app/(markos)/settings/sessions/page.module.css
  - app/(markos)/settings/sessions/page.tsx
  - app/(markos)/settings/sessions/page.stories.tsx
autonomous: true
requirements:
  - S-1
  - S-2
  - S-3
  - S-4
must_haves:
  truths:
    - "app/(markos)/settings/sessions/page.module.css contains zero inline hex; zero off-grid spacing; zero Sora/Space Grotesk/gradients/box-shadow/transform-translate; zero teal hexes"
    - "app/(markos)/settings/sessions/page.tsx replaces `.currentBadge` with `.c-badge c-badge--success` carrying `[ok] Current` copy (S-2)"
    - "app/(markos)/settings/sessions/page.tsx revoke buttons compose `.c-button c-button--destructive`; revoke-all confirm uses `.c-modal` + `.c-backdrop`; current-session row pairs `.c-status-dot--live` with `[ok] Active now` text (S-3)"
    - "app/(markos)/settings/sessions/page.tsx composes `.c-card`, `.c-button c-button--destructive`, `.c-button c-button--secondary`, `.c-badge c-badge--success`, `.c-modal`, `.c-backdrop`, `.c-status-dot--live`"
    - "app/(markos)/settings/sessions/page.stories.tsx (NEW) declares ≥4 named state stories: `Default`, `RevokeConfirm`, `SingleSession`, `Empty`"
    - "Banned-lexicon scan = 0; no exclamation; no emoji; no \"Please\"/\"Are you sure\""
    - "DOM tree + prop interface UNCHANGED per CONTEXT D-05; Phase 201 tenancy wiring (session list fetch + revoke API + tenant-scope guard) preserved per D-21"
    - "`npm run build` exits 0; `npm run build-storybook` exits 0; `node --test test/auth/ test/tenancy/ test/tenant-auth/` exits 0 (S-4)"
  artifacts:
    - path: "app/(markos)/settings/sessions/page.module.css"
      provides: "Token-only sessions layout module — page/card layout + table recipe + empty-state; primitives carry button/badge/modal/status-dot composition"
      min_lines: 30
      contains: "var(--color-surface)"
    - path: "app/(markos)/settings/sessions/page.tsx"
      provides: "Updated sessions page composing primitives; `.c-modal` revoke-confirm; `.c-status-dot--live` + `[ok] Active now` on current row; preserves Phase 201 wiring"
      contains: "c-status-dot--live"
    - path: "app/(markos)/settings/sessions/page.stories.tsx"
      provides: "Storybook 8 CSF3 ≥4 named state exports (Default / RevokeConfirm / SingleSession / Empty)"
      exports: ["default", "Default", "RevokeConfirm", "SingleSession", "Empty"]
  key_links:
    - from: "app/(markos)/settings/sessions/page.tsx"
      to: "styles/components.css `.c-status-dot--live` + `.c-modal` primitives"
      via: "className composition"
      pattern: "c-status-dot--live"
---

<objective>
Redesign `/settings/sessions` (`page.module.css` 133 LOC + `page.tsx` 141 LOC) into the canonical DESIGN.md v1.1.0 visual contract per `213.3-UI-SPEC.md` Surface 3 (4 ACs: S-1 through S-4). Compose `.c-card` / `.c-button c-button--destructive` / `.c-button c-button--secondary` / `.c-badge--success` / `.c-modal` + `.c-backdrop` / `.c-status-dot--live` per UI-SPEC primitive reuse. Create new `page.stories.tsx` with ≥4 named state stories.

Purpose: Close UI-SPEC AC S-1 (zero inline hex), S-2 (`.currentBadge` → `.c-badge--success` `[ok] Current`), S-3 (revoke `.c-button--destructive` + `.c-status-dot--live` + `.c-modal` confirm), S-4 (Phase 201 tenancy wiring preserved). Mirror Plan 213.3-02 structure.

Output: `page.module.css` rewritten (133 → ~40 LOC); `page.tsx` updated; new `page.stories.tsx` (~70 LOC, 4 CSF3 named state exports).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-CONTEXT.md
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-UI-SPEC.md
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-RESEARCH.md
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-VALIDATION.md
@DESIGN.md
@CLAUDE.md
@app/tokens.css
@app/globals.css
@styles/components.css
@app/(markos)/settings/sessions/page.module.css
@app/(markos)/settings/sessions/page.tsx
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-02-PLAN.md

<interfaces>
From `213.3-RESEARCH.md` Class-Rename Map: Surface 3 — Sessions (lines 398–422):
- `.page` → Standard pattern (`var(--space-md)`, `var(--color-surface)`)
- `.contentCard` → DELETE; compose `.c-card` + local `max-width: 900px`
- `.heading` → DELETE; plain `<h2>` (globals)
- `.subheading` → DELETE; apply `.t-lead`
- `.sessionsTable` → Retain layout: `width: 100%; border-collapse: collapse; margin-top: var(--space-md);`
- `.sessionsTable caption` → `.t-label-caps` posture (eliminates teal `#0f766e`)
- `.sessionsTable th/td` → token recipe (D-14)
- `.currentBadge` → DELETE; compose `.c-badge c-badge--success` with `[ok] Current` copy (S-2) — eliminates `#e6fffb`/`#0f766e`/`border-radius: 999px`
- `.revokeButton` → DELETE; compose `.c-button c-button--destructive` (S-3) — eliminates `#9a3412`/`#fca5a5`
- `.revokeAllRow` → Local: `margin-top: var(--space-md);`
- `.revokeAllButton` → DELETE; compose `.c-button c-button--destructive`
- `.emptyState` → Local: `font-size: var(--fs-body-md); color: var(--color-on-surface-muted);`
- `.toast` → DELETE; compose `.c-toast c-toast--{state}`
- (NEW) Revoke confirm modal → `.c-modal` + `.c-backdrop` (S-3)
- (NEW) `.c-status-dot--live` + `[ok] Active now` on current-session row (S-3)

From `213.3-UI-SPEC.md` Surface 3 Copywriting Contract (lines 234–246):
- Page heading: "Active sessions" (unchanged)
- Current session badge: `[ok] Current` via `.c-badge--success`
- Revoke per-row CTA: `"Revoke"` via `.c-button--destructive`
- Revoke confirm modal: `"Revoke this session?"` + body `"The device will be signed out immediately."` + `.c-button--destructive` "Revoke session"
- Revoke-all CTA: `"Revoke all other sessions"` via `.c-button--destructive`
- Revoke-all confirm: `"Revoke all other sessions?"` + body `"You will remain signed in on this device."` + `.c-button--destructive` "Revoke all"
- Empty state: `"This is your only active session."`

From `styles/components.css` v1.1.0:
```css
.c-status-dot { /* 8x8px, kernel heartbeat var(--color-on-surface-subtle) default */ }
.c-status-dot--live { /* mint var(--color-primary) with breathing animation; freezes on prefers-reduced-motion */ }
.c-status-dot--error { /* var(--color-error) */ }
.c-badge--success { /* rgb(63 185 80 / 0.12) bg + var(--color-success) text + uppercase label-caps */ }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite app/(markos)/settings/sessions/page.module.css to token-only layout module (S-1, X-1, X-2, X-3) — delete bespoke card/button/badge styles, retain only layout containers + table recipe + empty-state</name>
  <read_first>
    - app/(markos)/settings/sessions/page.module.css (current 133 LOC)
    - app/(markos)/settings/sessions/page.tsx (current 141 LOC — confirm classNames referenced)
    - 213.3-RESEARCH.md "Class-Rename Map: Surface 3 — Sessions" (lines 398–422)
    - 213.3-UI-SPEC.md "Sessions" Component Primitive Reuse (lines 411–421)
    - app/tokens.css (--color-surface/-on-surface/-muted, --color-border, --space-xs/-md/-lg, --fs-body-md/-label, --fw-semibold, --ls-label)
    - styles/components.css v1.1.0 (`.c-card`, `.c-button*`, `.c-badge--success`, `.c-modal`, `.c-status-dot*`)
    - .planning/phases/213.3-.../213.3-01-PLAN.md Task 1 (precedent)
    - .planning/phases/213.3-.../213.3-02-PLAN.md Task 1 (precedent — same shape)
  </read_first>
  <files>app/(markos)/settings/sessions/page.module.css</files>
  <action>
**Replace `app/(markos)/settings/sessions/page.module.css` ENTIRELY with a token-only layout module mirroring Plan 213.3-01 / 213.3-02 Task 1 shape.**

Required structural sections:

1. **`.page`**: `padding: var(--space-md); background: var(--color-surface); display: flex; flex-direction: column; gap: var(--space-md);`
2. **`.contentCard`**: local `max-width: 900px;` only (`.c-card` JSX-side handles the rest)
3. **`.sessionsTable`**: `width: 100%; border-collapse: collapse; margin-top: var(--space-md);`
4. **`.sessionsTable caption`**: `.t-label-caps` posture token recipe (eliminates teal caption)
5. **`.sessionsTable th`** (D-14 token recipe): `padding: var(--space-xs) var(--space-md); color: var(--color-on-surface-muted); font-size: var(--fs-label); font-weight: var(--fw-semibold); text-transform: uppercase; letter-spacing: var(--ls-label); border-bottom: 1px solid var(--color-border); text-align: left;`
6. **`.sessionsTable td`** (D-14 token recipe): `padding: var(--space-xs) var(--space-md); border-bottom: 1px solid var(--color-border); color: var(--color-on-surface); font-size: var(--fs-body-md); vertical-align: middle;`
7. **`.revokeAllRow`**: `margin-top: var(--space-md); display: flex; justify-content: flex-end;`
8. **`.statusCell`**: `display: flex; align-items: center; gap: var(--space-xs);` (for status-dot + label pairing)
9. **`.emptyState`**: `font-size: var(--fs-body-md); color: var(--color-on-surface-muted); padding: var(--space-md);`

DELETE entirely:
- `.heading` / `.subheading` (globals)
- `.currentBadge` (`.c-badge--success` primitive)
- `.revokeButton` / `.revokeAllButton` (`.c-button c-button--destructive` primitive)
- `.toast` (`.c-toast` primitive)
- All `font-family: 'Sora'/'Space Grotesk'`
- All teal hexes `#0d9488`/`#0f766e`/`#14b8a6`/`#e6fffb`/`#0b877c`
- All slate hexes `#0f172a`/`#334155`/`#475569`/`#cbd5e1`/`#e2e8f0`/`#f1f5f9`
- All orange-red `#9a3412`/`#fca5a5`
- All `box-shadow:` declarations
- All `border-radius: 999px`/`12px`/`28px` literals (composed via primitives)
- All `font-size: 14px/12px` literals (replaced via tokens)

DO NOT redeclare `@media (pointer: coarse)`. DO NOT add `outline:` declarations.
  </action>
  <verify>
    <automated>! grep -nE '#[0-9a-fA-F]{3,8}' "app/(markos)/settings/sessions/page.module.css" && ! grep -niE '(Sora|Space Grotesk|linear-gradient|radial-gradient|box-shadow|0 18px 45px|transform:\s*translate)' "app/(markos)/settings/sessions/page.module.css" && ! grep -niE '#0d9488|#0f766e|#14b8a6|#e6fffb|#0b877c|#9a3412|#fca5a5' "app/(markos)/settings/sessions/page.module.css" && ! grep -nE '@media.*pointer.*coarse' "app/(markos)/settings/sessions/page.module.css" && grep -q "var(--color-surface)" "app/(markos)/settings/sessions/page.module.css" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **S-1 (Zero inline hex):** `grep -nE '#[0-9a-fA-F]{3,8}' "app/(markos)/settings/sessions/page.module.css"` returns 0.
    - **No off-grid spacing:** `grep -nE ':\s*[0-9]+px' "app/(markos)/settings/sessions/page.module.css"` returns ONLY `1px` (border).
    - **No Sora/Space Grotesk/gradients/shadows:** `grep -niE "Sora|Space Grotesk|linear-gradient|radial-gradient|box-shadow|0 18px 45px|transform:\s*translate" "app/(markos)/settings/sessions/page.module.css"` returns 0.
    - **No teal/orange-red:** `grep -niE "#0d9488|#0f766e|#14b8a6|#e6fffb|#0b877c|#9a3412|#fca5a5" "app/(markos)/settings/sessions/page.module.css"` returns 0.
    - **X-6 (no per-module pointer-coarse):** `grep -nE '@media.*pointer.*coarse' "app/(markos)/settings/sessions/page.module.css"` returns 0.
    - **No legacy bespoke classes:** `grep -nE '\.(currentBadge|revokeButton|revokeAllButton|heading|subheading|toast)' "app/(markos)/settings/sessions/page.module.css"` returns 0.
  </acceptance_criteria>
  <done>`page.module.css` rewritten to ~40 LOC token-only layout module; 6 grep gates pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update app/(markos)/settings/sessions/page.tsx to compose `.c-card`/`.c-button c-button--destructive`/`.c-badge c-badge--success`/`.c-modal` + `.c-backdrop`/`.c-status-dot--live` per UI-SPEC Surface 3 (S-2, S-3); preserve Phase 201 wiring (S-4)</name>
  <read_first>
    - app/(markos)/settings/sessions/page.tsx (current 141 LOC — DOM tree to preserve per D-05)
    - app/(markos)/settings/sessions/page.module.css (post-Task 1 shape)
    - 213.3-UI-SPEC.md "Sessions" Copywriting Contract (lines 234–246) + Component Primitive Reuse (lines 411–421)
    - 213.3-RESEARCH.md "Class-Rename Map: Surface 3" (lines 398–422)
    - 213.3-RESEARCH.md "Pattern 2 — `.c-notice` Composition" + "Pattern 3 — Class Rename in TSX"
    - styles/components.css v1.1.0 (.c-card, .c-button*, .c-badge--success, .c-modal, .c-backdrop, .c-status-dot*)
    - DESIGN.md "Components > Modal" + "Status indicators"
    - test/auth/ + test/tenancy/ + test/tenant-auth/ (Phase 201 wiring tests)
  </read_first>
  <files>app/(markos)/settings/sessions/page.tsx</files>
  <action>
**Update `app/(markos)/settings/sessions/page.tsx` per the Class-Rename Map JSX swaps (DOM tree + prop interface UNCHANGED per D-05).**

ClassName substitutions:

| Existing JSX | New JSX | Citation |
|--------------|---------|----------|
| `<div className={styles.contentCard}>` | `<div className={`c-card ${styles.contentCard}`}>` | `.c-card` |
| `<h2 className={styles.heading}>Active sessions</h2>` | `<h2>Active sessions</h2>` | globals |
| `<p className={styles.subheading}>...</p>` | `<p className="t-lead">...</p>` | `.t-lead` |
| `<span className={styles.currentBadge}>Current</span>` | `<span className="c-badge c-badge--success">[ok] Current</span>` | `.c-badge--success` (S-2) |
| `<button className={styles.revokeButton}>Revoke</button>` | `<button type="button" className="c-button c-button--destructive" onClick={() => askRevoke(session.id)}>Revoke</button>` | `.c-button--destructive` (S-3) |
| `<button className={styles.revokeAllButton}>Revoke all</button>` | `<button type="button" className="c-button c-button--destructive" onClick={() => askRevokeAll()}>Revoke all other sessions</button>` | `.c-button--destructive` |

**S-3 — Add `.c-status-dot--live` + `[ok] Active now` on current-session row:**

For the current-session table row, replace any color-only "current" indicator with status-dot + bracketed-glyph pairing inside `.statusCell`:

```tsx
<td>
  <div className={styles.statusCell}>
    <span className="c-status-dot c-status-dot--live" aria-hidden="true" />
    <span>[ok] Active now</span>
  </div>
</td>
```

**S-3 — Add `.c-modal` + `.c-backdrop` revoke-confirm dialogs:**

```tsx
{/* Per-session revoke confirm */}
{revokeTarget && (
  <>
    <div className="c-backdrop" onClick={() => setRevokeTarget(null)} aria-hidden="true" />
    <div className="c-modal" role="dialog" aria-labelledby="revoke-confirm-heading" aria-modal="true">
      <h3 id="revoke-confirm-heading">Revoke this session?</h3>
      <p>The device will be signed out immediately.</p>
      <div className={styles.actionRow}>
        <button type="button" className="c-button c-button--secondary" onClick={() => setRevokeTarget(null)}>Cancel</button>
        <button type="button" className="c-button c-button--destructive" onClick={confirmRevoke}>Revoke session</button>
      </div>
    </div>
  </>
)}

{/* Revoke-all confirm */}
{showRevokeAll && (
  <>
    <div className="c-backdrop" onClick={() => setShowRevokeAll(false)} aria-hidden="true" />
    <div className="c-modal" role="dialog" aria-labelledby="revoke-all-confirm-heading" aria-modal="true">
      <h3 id="revoke-all-confirm-heading">Revoke all other sessions?</h3>
      <p>You will remain signed in on this device.</p>
      <div className={styles.actionRow}>
        <button type="button" className="c-button c-button--secondary" onClick={() => setShowRevokeAll(false)}>Cancel</button>
        <button type="button" className="c-button c-button--destructive" onClick={confirmRevokeAll}>Revoke all</button>
      </div>
    </div>
  </>
)}
```

**Empty state:** `"This is your only active session."` per UI-SPEC.

**Banned-lexicon scan = 0.** No "Are you sure?". No "Please". No exclamation. No emoji.

**Phase 201 wiring (S-4 / D-21):** Session list fetch (`fetchSessions()` or similar), revoke API call, revoke-all API call, tenant-scope guard, audit-log emit — ALL unchanged.

After editing, run `npm run build` + `node --test test/auth/ test/tenancy/ test/tenant-auth/` — all MUST exit 0.
  </action>
  <verify>
    <automated>grep -qE 'c-badge c-badge--success' "app/(markos)/settings/sessions/page.tsx" && grep -qE '\[ok\] Current' "app/(markos)/settings/sessions/page.tsx" && grep -qE 'c-button c-button--destructive' "app/(markos)/settings/sessions/page.tsx" && grep -qE 'c-status-dot--live' "app/(markos)/settings/sessions/page.tsx" && grep -qE '\[ok\] Active now' "app/(markos)/settings/sessions/page.tsx" && grep -qE 'c-modal' "app/(markos)/settings/sessions/page.tsx" && grep -qE 'c-backdrop' "app/(markos)/settings/sessions/page.tsx" && ! grep -qE 'styles\.(currentBadge|revokeButton|revokeAllButton)' "app/(markos)/settings/sessions/page.tsx" && ! grep -inE 'Please|Are you sure|synergy|leverage|empower|unlock|transform|seamless|innovative' "app/(markos)/settings/sessions/page.tsx" && npm run build && node --test test/auth/ test/tenancy/ test/tenant-auth/ && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **S-2 (`.c-badge--success` + `[ok] Current`):** `grep -cE 'c-badge c-badge--success' "app/(markos)/settings/sessions/page.tsx"` ≥1; `grep -cE '\[ok\] Current' "app/(markos)/settings/sessions/page.tsx"` ≥1.
    - **S-3 (revoke `.c-button--destructive`):** `grep -cE 'c-button c-button--destructive' "app/(markos)/settings/sessions/page.tsx"` ≥1.
    - **S-3 (`.c-status-dot--live` + `[ok] Active now`):** `grep -cE 'c-status-dot--live' "app/(markos)/settings/sessions/page.tsx"` ≥1; `grep -cE '\[ok\] Active now' "app/(markos)/settings/sessions/page.tsx"` ≥1.
    - **S-3 (`.c-modal` + `.c-backdrop`):** `grep -cE 'c-modal' "app/(markos)/settings/sessions/page.tsx"` ≥1; `grep -cE 'c-backdrop' "app/(markos)/settings/sessions/page.tsx"` ≥1.
    - **No legacy class refs:** `grep -cE 'styles\.(currentBadge|revokeButton|revokeAllButton)' "app/(markos)/settings/sessions/page.tsx"` returns 0.
    - **Banned-lexicon = 0:** `grep -inE 'Please|Are you sure|synergy|leverage|empower|unlock|transform|seamless|innovative' "app/(markos)/settings/sessions/page.tsx"` returns 0.
    - **`npm run build` exits 0; Phase 201 wiring (S-4):** `node --test test/auth/ test/tenancy/ test/tenant-auth/` exits 0.
  </acceptance_criteria>
  <done>`page.tsx` updated; primitives composed; `.c-modal` confirm flows + `.c-status-dot--live` indicator added; Phase 201 wiring preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create app/(markos)/settings/sessions/page.stories.tsx with ≥4 named state stories (`Default`, `RevokeConfirm`, `SingleSession`, `Empty`) per UI-SPEC X-4 + Storybook State Coverage</name>
  <read_first>
    - app/(markos)/settings/sessions/page.tsx (post-Task 2 shape)
    - 213.3-UI-SPEC.md "Storybook State Coverage" line 579
    - .planning/phases/213.3-.../213.3-02-PLAN.md Task 3 (CSF3 5-state precedent)
  </read_first>
  <files>app/(markos)/settings/sessions/page.stories.tsx</files>
  <action>
**Create new `app/(markos)/settings/sessions/page.stories.tsx` with Storybook 8 CSF3 ≥4 named state stories per UI-SPEC X-4 line 579:**

```tsx
import type { Meta, StoryObj } from '@storybook/react';
import SessionsPage from './page';

const meta = {
  title: 'Settings/Sessions',
  component: SessionsPage,
  parameters: {
    layout: 'fullscreen',
  },
} satisfies Meta<typeof SessionsPage>;

export default meta;

type Story = StoryObj<typeof meta>;

const sampleSessions = [
  { id: 'sess_1', device: 'MacBook Pro · Chrome 120', ip: '192.0.2.1', lastSeen: '2026-04-28T10:00:00Z', isCurrent: true },
  { id: 'sess_2', device: 'iPhone 15 · Safari 17', ip: '198.51.100.5', lastSeen: '2026-04-27T18:30:00Z', isCurrent: false },
  { id: 'sess_3', device: 'Windows · Firefox 122', ip: '203.0.113.42', lastSeen: '2026-04-26T09:15:00Z', isCurrent: false },
];

export const Default: Story = {
  args: {
    sessions: sampleSessions,
  },
};

export const RevokeConfirm: Story = {
  args: {
    sessions: sampleSessions,
    confirmingRevokeFor: 'sess_2',
  },
  parameters: {
    docs: { description: { story: 'Per-session revoke confirmation modal — `.c-modal` + `.c-backdrop` + `.c-button--destructive` per UI-SPEC AC S-3.' } },
  },
};

export const SingleSession: Story = {
  args: {
    sessions: [sampleSessions[0]],
  },
  parameters: {
    docs: { description: { story: 'Only the current session — empty-state copy "This is your only active session." applies.' } },
  },
};

export const Empty: Story = {
  args: {
    sessions: [],
  },
  parameters: {
    docs: { description: { story: 'Edge case (defensive empty handling).' } },
  },
};
```

KEY rules:
- 4 named state exports per UI-SPEC line 579
- `args` shape mirrors `SessionsPage` prop interface (executor inspects post-Task 2 file)
- DOES NOT set `parameters.theme: "legacy"`

After editing, run `npm run build-storybook` — MUST exit 0.
  </action>
  <verify>
    <automated>test -f "app/(markos)/settings/sessions/page.stories.tsx" && grep -cE '^export const (Default|RevokeConfirm|SingleSession|Empty):' "app/(markos)/settings/sessions/page.stories.tsx" | awk '$1>=4 {exit 0} $1<4 {exit 1}' && ! grep -qE 'parameters\.theme.*"legacy"' "app/(markos)/settings/sessions/page.stories.tsx" && grep -q 'export default meta' "app/(markos)/settings/sessions/page.stories.tsx" && npm run build-storybook && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **X-4 (≥4 named state stories):** `grep -cE '^export const (Default|RevokeConfirm|SingleSession|Empty):' "app/(markos)/settings/sessions/page.stories.tsx"` ≥4.
    - **CSF3 typed `meta`:** `grep -c 'export default meta' "app/(markos)/settings/sessions/page.stories.tsx"` ≥1.
    - **Default-not-legacy theme:** `grep -cE 'parameters\.theme.*"legacy"' "app/(markos)/settings/sessions/page.stories.tsx"` returns 0.
    - **`npm run build-storybook` exits 0.**
  </acceptance_criteria>
  <done>`page.stories.tsx` created with 4 CSF3 named state exports; `npm run build-storybook` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→sessions API | Untrusted revoke action crosses via Phase 201 tenancy wiring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213.3-03-01 | Denial of Service | accidental session revoke | mitigate | `.c-modal` confirm preserved; "Revoke all OTHER sessions" scoping copy explicit; current-session row clearly marked with `.c-status-dot--live` + `[ok] Active now` |
| T-213.3-03-02 | Tampering | revoke API call | accept | Wiring preserved verbatim from Phase 201 contract |
</threat_model>

<verification>
After all 3 tasks:

```bash
grep -nE '#[0-9a-fA-F]{3,8}' "app/(markos)/settings/sessions/page.module.css"  # 0
grep -cE 'c-badge c-badge--success' "app/(markos)/settings/sessions/page.tsx"  # >=1
grep -cE 'c-status-dot--live' "app/(markos)/settings/sessions/page.tsx"  # >=1
grep -cE '^export const (Default|RevokeConfirm|SingleSession|Empty):' "app/(markos)/settings/sessions/page.stories.tsx"  # >=4
npm run build  # 0
npm run build-storybook  # 0
node --test test/auth/ test/tenancy/ test/tenant-auth/  # 0
```
</verification>

<success_criteria>
- AC S-1, S-2, S-3, S-4 close.
- Phase 201 tenancy wiring preserved.
- Phase 213.1 + 213.2 a11y suites green.
- Atomic commit: `feat(213.3-03): redesign /settings/sessions surface to DESIGN.md v1.1.0 tokens + .c-badge--success/.c-status-dot--live/.c-modal composition + Storybook state coverage`.
</success_criteria>

<output>
Create `213.3-03-SUMMARY.md` documenting files modified, AC closures, hand-off to Plan 213.3-04 (domain).
</output>
