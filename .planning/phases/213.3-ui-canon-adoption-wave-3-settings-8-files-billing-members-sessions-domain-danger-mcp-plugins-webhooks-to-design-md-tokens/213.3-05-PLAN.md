---
phase: 213.3
plan: 05
type: execute
wave: 1
depends_on:
  - "213.3-04"
files_modified:
  - app/(markos)/settings/danger/page.module.css
  - app/(markos)/settings/danger/page.tsx
  - app/(markos)/settings/danger/page.stories.tsx
autonomous: true
requirements:
  - DZ-1
  - DZ-2
  - DZ-3
  - DZ-4
must_haves:
  truths:
    - "app/(markos)/settings/danger/page.module.css contains zero inline hex; zero off-grid spacing; zero Sora/Space Grotesk/gradients/box-shadow/transform-translate; zero teal hexes; zero error-red `#9a3412`/`#7c2d12`/`#fca5a5` heading colors (DZ-2)"
    - "app/(markos)/settings/danger/page.tsx renders danger-consequence notice via `.c-notice c-notice--error` ABOVE confirm form (DZ-2); page heading `<h2>` uses `var(--color-on-surface)` not error-red"
    - "app/(markos)/settings/danger/page.tsx confirm field composes `.c-input` + `.c-field` + `.c-field__error` (auto `[err]` prefix); `.deleteOutlineButton` + `.deleteFilledButton` consolidated to single `.c-button c-button--destructive` primitive (DZ-3)"
    - "app/(markos)/settings/danger/page.tsx delete-confirm modal uses `.c-modal` + `.c-backdrop` + `.c-button c-button--destructive` 'Delete workspace permanently' (DZ-3); copy is `\"Delete workspace?\"` (no \"Are you sure?\"); confirm-by-typing-tenant-name validation preserved"
    - "app/(markos)/settings/danger/page.tsx composes `.c-card`, `.c-input`, `.c-field`, `.c-field__error`, `.c-button{,--secondary,--destructive,--tertiary}`, `.c-modal`, `.c-backdrop`, `.c-notice c-notice--error`, `.c-notice c-notice--info` (export-in-progress), `.c-notice c-notice--warning` (purge-banner replacement)"
    - "app/(markos)/settings/danger/page.stories.tsx (NEW) declares ≥3 named state stories: `Default`, `DeleteConfirm`, `ExportInProgress`"
    - "Banned-lexicon scan = 0; no exclamation; no emoji; no \"Please\"/\"Are you sure\""
    - "DOM tree + prop interface UNCHANGED per CONTEXT D-05; Phase 201 tenant-delete wiring (confirm-by-typing-tenant-name validation, GDPR signed-URL semantics, audit-log emit) preserved per D-21 (DZ-4)"
    - "`npm run build` exits 0; `npm run build-storybook` exits 0; `node --test test/auth/ test/tenancy/ test/tenant-auth/` exits 0"
  artifacts:
    - path: "app/(markos)/settings/danger/page.module.css"
      provides: "Token-only danger-zone layout module — page/card layout + danger-row layout + max-width var(--w-prose) for narrower confirmation form; primitives carry button/input/field/notice/modal composition"
      min_lines: 50
      contains: "var(--w-prose)"
    - path: "app/(markos)/settings/danger/page.tsx"
      provides: "Updated danger page composing primitives; `.c-notice c-notice--error` consequence banner + `.c-modal` delete-confirm with confirm-by-typing pattern; preserves Phase 201 wiring"
      contains: "c-notice c-notice--error"
    - path: "app/(markos)/settings/danger/page.stories.tsx"
      provides: "Storybook 8 CSF3 ≥3 named state exports (Default / DeleteConfirm / ExportInProgress)"
      exports: ["default", "Default", "DeleteConfirm", "ExportInProgress"]
  key_links:
    - from: "app/(markos)/settings/danger/page.tsx"
      to: "styles/components.css `.c-notice c-notice--error` + `.c-modal` + `.c-backdrop` primitives"
      via: "className composition for danger consequence + delete-confirm modal"
      pattern: "c-notice c-notice--error"
    - from: "app/(markos)/settings/danger/page.tsx"
      to: "Phase 201 tenant-delete wiring"
      via: "preserved confirm-by-typing-tenant-name + GDPR signed-URL export"
      pattern: "confirmText|tenantName"
---

<objective>
Redesign `/settings/danger` (`page.module.css` 215 LOC + `page.tsx` 157 LOC) into the canonical DESIGN.md v1.1.0 visual contract per `213.3-UI-SPEC.md` Surface 5 (4 ACs: DZ-1 through DZ-4). Compose `.c-card` + `.c-input` + `.c-field{,__label,__error}` + `.c-button{,--secondary,--destructive,--tertiary}` + `.c-modal` + `.c-backdrop` + `.c-notice c-notice--{error,warning,info}` per UI-SPEC primitive reuse.

Critical UX shift (DZ-2): danger is communicated via `.c-notice c-notice--error` ABOVE the confirm form, NOT via error-red heading color. The page heading `<h2>` uses default `var(--color-on-surface)` (not `#9a3412`). The dashed-border `.dangerRow` becomes a flat `.c-card` with solid `var(--color-border)` hairline. The dual `.deleteOutlineButton`/`.deleteFilledButton` pattern consolidates to a single `.c-button--destructive` primitive (alpha-fill canon). Confirm-by-typing-tenant-name validation preserved verbatim.

Output: `page.module.css` rewritten (215 → ~65 LOC, narrower `max-width: var(--w-prose)`); `page.tsx` updated; new `page.stories.tsx` (~70 LOC, 3 CSF3 named state exports).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-CONTEXT.md
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-UI-SPEC.md
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-RESEARCH.md
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-VALIDATION.md
@DESIGN.md
@CLAUDE.md
@app/tokens.css
@app/globals.css
@styles/components.css
@app/(markos)/settings/danger/page.module.css
@app/(markos)/settings/danger/page.tsx
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-04-PLAN.md

<interfaces>
From `213.3-RESEARCH.md` Class-Rename Map: Surface 5 — Danger (lines 461–490):
- `.page` → Standard pattern
- `.contentCard` → DELETE; compose `.c-card` + local `max-width: var(--w-prose)` (720px) — narrower for confirmation form per UI-SPEC
- `.heading` → DELETE; plain `<h2>` — color CHANGES from `#9a3412` (error-red) to `var(--color-on-surface)` (DZ-2)
- `.subheading` → DELETE; apply `.t-lead`
- `.dangerRow` → DELETE; compose `.c-card` + local flexbox layout — eliminates `border: 1px dashed #fca5a5` (dashed legacy; canon uses solid `var(--color-border)`)
- `.dangerLabel` → DELETE; plain `<h4>` or `.c-field__label` posture
- `.dangerHelp` → DELETE; compose `.c-field__help` (DZ-3) — eliminates `#64748b`, `13px`
- `.deleteOutlineButton` → DELETE; compose `.c-button c-button--destructive` (DZ-3)
- `.deleteFilledButton` → DELETE; compose `.c-button c-button--destructive` (DZ-3 — consolidation: dual classes → single primitive)
- `.cancelButton` → DELETE; compose `.c-button c-button--secondary`
- `.dialog` → DELETE; compose `.c-modal` + `.c-backdrop` (DZ-3)
- `.dialogHeading` / `.dialogBody` / `.dialogActions` → DELETE; nested layout inside `.c-modal`
- `.field` / `.field input` → DELETE; compose `.c-field` + `.c-input`
- `.purgeBanner` → DELETE; compose `.c-notice c-notice--warning` — eliminates `#fef3c7`/`#78350f`/`#d97706`
- `.cancelLink` → DELETE; compose `.c-button c-button--tertiary`
- `.toast` → DELETE; compose `.c-toast c-toast--{state}`
- (NEW) Danger consequence notice → `.c-notice c-notice--error` ABOVE confirm form (DZ-2)
- (NEW) Export in-progress notice → `.c-notice c-notice--info` `[info] Export in progress.`

From `213.3-UI-SPEC.md` Surface 5 Copywriting Contract (lines 264–279):
- Page heading: "Danger zone"
- Delete tenant row label: "Delete workspace"
- Delete tenant help: "Permanently deletes this workspace and all data. This action cannot be undone."
- Delete CTA: "Delete workspace" via `.c-button--destructive`
- Delete confirm modal heading: `"Delete workspace?"`
- Confirm field label: "Type the workspace name to confirm" (no "Please")
- Confirm field error (auto): `[err] Workspace name does not match.`
- Confirm modal CTA: "Delete workspace permanently" via `.c-button--destructive`
- Danger notice (NEW above confirm): `[err] Deleting this workspace removes all members, data, API keys, and webhook subscriptions.`
- Export row label: "Export workspace data"
- Export CTA: "Export data" via `.c-button--secondary`
- Export in-progress notice: `[info] Export in progress. A download link will be sent to your email.`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite app/(markos)/settings/danger/page.module.css to token-only layout module (DZ-1, X-1, X-2, X-3) — replace dashed-border `.dangerRow` legacy with `.c-card` + solid `var(--color-border)`; narrower max-width `var(--w-prose)`; consolidate delete-button styling into primitive composition</name>
  <read_first>
    - app/(markos)/settings/danger/page.module.css (current 215 LOC, 30 hex)
    - app/(markos)/settings/danger/page.tsx (current 157 LOC)
    - 213.3-RESEARCH.md "Class-Rename Map: Surface 5 — Danger" (lines 461–490)
    - 213.3-UI-SPEC.md "Danger" Component Primitive Reuse (lines 444–459)
    - app/tokens.css (--color-surface, --color-on-surface, --color-border, --w-prose, --space-xs/-md/-lg, --fs-body-md, --radius-lg)
    - styles/components.css v1.1.0 (`.c-card`, `.c-input`, `.c-field*`, `.c-button*`, `.c-modal`, `.c-backdrop`, `.c-notice*`)
    - DESIGN.md "Color > Forbidden values" + "Components > Modal" + "Components > Notice/Banner"
    - .planning/phases/213.3-.../213.3-04-PLAN.md Task 1 (precedent for module rewrite)
  </read_first>
  <files>app/(markos)/settings/danger/page.module.css</files>
  <action>
**Replace `app/(markos)/settings/danger/page.module.css` ENTIRELY with token-only layout module mirroring Plan 213.3-04 Task 1 shape.**

Required structural sections:

1. **`.page`**: `padding: var(--space-md); background: var(--color-surface); display: flex; flex-direction: column; gap: var(--space-md);`
2. **`.contentCard`**: local `max-width: var(--w-prose);` only — narrower for confirmation form per UI-SPEC Q6
3. **`.dangerRow`**: layout flex row inside `.c-card` (composed JSX-side): `display: flex; flex-direction: column; gap: var(--space-xs);` (eliminates `border: 1px dashed #fca5a5` legacy — canon uses solid `var(--color-border)` provided by `.c-card`)
4. **`.dangerActionRow`**: `display: flex; gap: var(--space-xs); align-items: center; justify-content: flex-end;`
5. **`.dangerStack`**: `display: grid; gap: var(--space-md);` (vertical stack of multiple danger rows)
6. **`.modalActionRow`**: `display: flex; gap: var(--space-xs); justify-content: flex-end; margin-top: var(--space-md);`
7. **`.confirmField`**: `display: flex; flex-direction: column; gap: var(--space-xs);`

DELETE entirely:
- `.heading` / `.subheading` (globals)
- `.dangerLabel` / `.dangerHelp` (globals + `.c-field__help`)
- `.deleteOutlineButton` / `.deleteFilledButton` / `.cancelButton` / `.cancelLink` (`.c-button` primitives)
- `.dialog` / `.dialogHeading` / `.dialogBody` / `.dialogActions` (`.c-modal` primitives)
- `.field` / `.field input` (`.c-field` + `.c-input` primitives)
- `.purgeBanner` (`.c-notice c-notice--warning` primitive)
- `.toast` (`.c-toast` primitive)
- All teal hexes / orange-red hexes `#9a3412`/`#7c2d12`/`#fca5a5`/`#fef3c7`/`#78350f`/`#d97706`
- All slate hexes
- All `font-family: 'Sora'/'Space Grotesk'`
- All `border: 1px dashed *` (canon uses solid)
- All `border-radius: 16px`/`20px`/`24px`/`28px` literals (composed via primitives)
- All `padding: 20px`/`28px` literals (composed via `.c-card` `var(--space-md)`)
- All `font-size: 13px`/`14px` literals (replaced via tokens)

DO NOT redeclare `@media (pointer: coarse)`. DO NOT add `outline:` declarations.
  </action>
  <verify>
    <automated>! grep -nE '#[0-9a-fA-F]{3,8}' "app/(markos)/settings/danger/page.module.css" && ! grep -niE '(Sora|Space Grotesk|linear-gradient|radial-gradient|box-shadow|0 18px 45px|transform:\s*translate)' "app/(markos)/settings/danger/page.module.css" && ! grep -niE '#0d9488|#0f766e|#9a3412|#7c2d12|#fca5a5|#fef3c7|#78350f|#d97706' "app/(markos)/settings/danger/page.module.css" && ! grep -nE '@media.*pointer.*coarse' "app/(markos)/settings/danger/page.module.css" && ! grep -nE 'border:\s*1px\s+dashed' "app/(markos)/settings/danger/page.module.css" && grep -q "var(--w-prose)" "app/(markos)/settings/danger/page.module.css" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **DZ-1 (Zero inline hex):** `grep -nE '#[0-9a-fA-F]{3,8}' "app/(markos)/settings/danger/page.module.css"` returns 0.
    - **No off-grid spacing:** `grep -nE ':\s*[0-9]+px' "app/(markos)/settings/danger/page.module.css"` returns ONLY `1px` (border).
    - **DZ-2 prerequisite (no error-red heading color):** `grep -niE "#9a3412|#7c2d12" "app/(markos)/settings/danger/page.module.css"` returns 0.
    - **No legacy orange-red:** `grep -niE "#fca5a5|#fef3c7|#78350f|#d97706" "app/(markos)/settings/danger/page.module.css"` returns 0.
    - **No dashed border (canon uses solid):** `grep -nE 'border:\s*1px\s+dashed' "app/(markos)/settings/danger/page.module.css"` returns 0.
    - **No Sora/Space Grotesk:** `grep -niE "Sora|Space Grotesk" "app/(markos)/settings/danger/page.module.css"` returns 0.
    - **`var(--w-prose)` for narrower confirmation form:** `grep -q "var(--w-prose)" "app/(markos)/settings/danger/page.module.css"` returns 1.
    - **X-6:** `grep -nE '@media.*pointer.*coarse' "app/(markos)/settings/danger/page.module.css"` returns 0.
  </acceptance_criteria>
  <done>`page.module.css` rewritten to ~65 LOC token-only layout; dashed-border eliminated; narrower `var(--w-prose)`; 7 grep gates pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update app/(markos)/settings/danger/page.tsx to compose `.c-card`/`.c-input`/`.c-field{,__error}`/`.c-button{,--secondary,--destructive,--tertiary}`/`.c-modal` + `.c-backdrop`/`.c-notice c-notice--{error,warning,info}` per UI-SPEC Surface 5 (DZ-2, DZ-3); preserve Phase 201 tenant-delete wiring (DZ-4)</name>
  <read_first>
    - app/(markos)/settings/danger/page.tsx (current 157 LOC — DOM tree to preserve per D-05; confirm-by-typing-tenant-name validation logic to preserve)
    - app/(markos)/settings/danger/page.module.css (post-Task 1 shape)
    - 213.3-UI-SPEC.md "Danger" Copywriting Contract (lines 264–279) + Component Primitive Reuse (lines 444–459)
    - 213.3-RESEARCH.md "Class-Rename Map: Surface 5 — Danger" (lines 461–490) + "Pattern 2 — `.c-notice` Composition"
    - styles/components.css v1.1.0 (.c-card, .c-input, .c-field*, .c-button*, .c-modal, .c-backdrop, .c-notice*)
    - DESIGN.md "Components > Notice/Banner" + "Components > Modal" + "Forms"
    - test/auth/ + test/tenancy/ + test/tenant-auth/ (Phase 201 tenant-delete wiring tests)
  </read_first>
  <files>app/(markos)/settings/danger/page.tsx</files>
  <action>
**Update `app/(markos)/settings/danger/page.tsx` per the Class-Rename Map JSX swaps (DOM tree + prop interface UNCHANGED per D-05).**

KEY UX SHIFT (DZ-2): the page heading `<h2>` does NOT carry error-red color. Danger is communicated via the consequence `.c-notice c-notice--error` ABOVE the confirm form.

ClassName + JSX-shape substitutions:

| Existing JSX | New JSX | Citation |
|--------------|---------|----------|
| `<div className={styles.contentCard}>` | `<div className={`c-card ${styles.contentCard}`}>` | `.c-card` + narrower max-width |
| `<h2 className={styles.heading} style={{color: '#9a3412'}}>Danger zone</h2>` | `<h2>Danger zone</h2>` (no inline style; default `var(--color-on-surface)`) | DZ-2 |
| `<p className={styles.subheading}>...</p>` | `<p className="t-lead">...</p>` | `.t-lead` |
| `<div className={styles.dangerRow}>` | `<div className={`c-card ${styles.dangerRow}`}>` | `.c-card` + local layout |
| `<h3 className={styles.dangerLabel}>Delete workspace</h3>` | `<h4>Delete workspace</h4>` | globals `<h4>` |
| `<p className={styles.dangerHelp}>...</p>` | `<p className="c-field__help">Permanently deletes this workspace and all data. This action cannot be undone.</p>` | `.c-field__help` |
| `<button className={styles.deleteOutlineButton}>Delete</button>` | `<button type="button" className="c-button c-button--destructive" onClick={() => setShowDeleteConfirm(true)}>Delete workspace</button>` | `.c-button--destructive` (DZ-3) |
| `<button className={styles.cancelLink}>Cancel</button>` | `<button type="button" className="c-button c-button--tertiary" onClick={...}>Cancel</button>` | `.c-button--tertiary` |

**DZ-3 — Delete-confirm modal with `.c-modal` + `.c-backdrop` + confirm-by-typing-tenant-name pattern:**

```tsx
{showDeleteConfirm && (
  <>
    <div className="c-backdrop" onClick={() => setShowDeleteConfirm(false)} aria-hidden="true" />
    <div className="c-modal" role="dialog" aria-labelledby="delete-confirm-heading" aria-modal="true">
      <h3 id="delete-confirm-heading">Delete workspace?</h3>

      {/* DZ-2: Consequence notice ABOVE the confirm form */}
      <div className="c-notice c-notice--error" role="status">
        <strong>[err]</strong>{" "}Deleting this workspace removes all members, data, API keys, and webhook subscriptions.
      </div>

      <div className={`c-field ${styles.confirmField}`}>
        <label htmlFor="confirm-name" className="c-field__label">Type the workspace name to confirm</label>
        <input
          id="confirm-name"
          type="text"
          required
          className="c-input"
          value={confirmText}
          onChange={(e) => setConfirmText(e.target.value)}
          aria-invalid={confirmText.length > 0 && confirmText !== tenantName}
        />
        {confirmText.length > 0 && confirmText !== tenantName && (
          <p className="c-field__error">Workspace name does not match.</p>
        )}
      </div>

      <div className={styles.modalActionRow}>
        <button type="button" className="c-button c-button--secondary" onClick={() => setShowDeleteConfirm(false)}>
          Cancel
        </button>
        <button
          type="button"
          className="c-button c-button--destructive"
          onClick={confirmDelete}
          disabled={confirmText !== tenantName}
        >
          Delete workspace permanently
        </button>
      </div>
    </div>
  </>
)}
```

KEY rules (DZ-3):
- Confirm-by-typing-tenant-name validation logic preserved verbatim (state + onChange + match check + button disabled)
- `.c-field__error` primitive auto-prepends `[err] ` via `::before` (no manual `[err]` prefix needed in `<p>` body)
- Modal heading uses `<h3>` (question form, no exclamation)
- Cancel button is `.c-button--secondary` (not destructive — standard cancel posture)

**Purge-banner replacement (export-in-progress):**

```tsx
{exportInProgress && (
  <div className="c-notice c-notice--info" role="status">
    <strong>[info]</strong>{" "}Export in progress. A download link will be sent to your email.
  </div>
)}
```

**Banned-lexicon scan = 0.** No "Are you sure?". No "Please". No exclamation.

**Phase 201 wiring (DZ-4 / D-21):** Confirm-by-typing-tenant-name validation, GDPR signed-URL export semantics, audit-log emit, server actions — ALL unchanged.

After editing, run `npm run build` + `node --test test/auth/ test/tenancy/ test/tenant-auth/` — all MUST exit 0.
  </action>
  <verify>
    <automated>grep -qE 'c-notice c-notice--error' "app/(markos)/settings/danger/page.tsx" && grep -cE 'c-button c-button--destructive' "app/(markos)/settings/danger/page.tsx" | awk '$1>=2 {exit 0} $1<2 {exit 1}' && grep -qE 'c-modal' "app/(markos)/settings/danger/page.tsx" && grep -qE 'c-backdrop' "app/(markos)/settings/danger/page.tsx" && grep -qE 'c-input' "app/(markos)/settings/danger/page.tsx" && grep -qE 'c-field__error|c-field__help' "app/(markos)/settings/danger/page.tsx" && grep -qE 'Delete workspace' "app/(markos)/settings/danger/page.tsx" && ! grep -qE 'styles\.(deleteOutlineButton|deleteFilledButton|cancelButton|cancelLink|dangerLabel|dangerHelp|dialog|dialogHeading|dialogBody|dialogActions|purgeBanner)' "app/(markos)/settings/danger/page.tsx" && ! grep -qE "color:\s*['\"]#9a3412" "app/(markos)/settings/danger/page.tsx" && ! grep -inE 'Please|Are you sure|synergy|leverage|empower|unlock|transform|seamless|innovative' "app/(markos)/settings/danger/page.tsx" && npm run build && node --test test/auth/ test/tenancy/ test/tenant-auth/ && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **DZ-2 (`.c-notice c-notice--error` consequence):** `grep -cE 'c-notice c-notice--error' "app/(markos)/settings/danger/page.tsx"` ≥1.
    - **DZ-2 (no error-red heading inline-style):** `grep -cE "color:\s*['\"]#9a3412" "app/(markos)/settings/danger/page.tsx"` returns 0.
    - **DZ-3 (consolidated `.c-button--destructive` ×2):** `grep -cE 'c-button c-button--destructive' "app/(markos)/settings/danger/page.tsx"` ≥2.
    - **DZ-3 (`.c-modal` + `.c-backdrop`):** `grep -cE 'c-modal' "app/(markos)/settings/danger/page.tsx"` ≥1; `grep -cE 'c-backdrop' "app/(markos)/settings/danger/page.tsx"` ≥1.
    - **DZ-3 (`.c-input` + `.c-field__error`):** `grep -cE 'c-input' "app/(markos)/settings/danger/page.tsx"` ≥1; `grep -cE 'c-field__error|c-field__help' "app/(markos)/settings/danger/page.tsx"` ≥1.
    - **No legacy class refs:** `grep -cE 'styles\.(deleteOutlineButton|deleteFilledButton|cancelButton|cancelLink|dangerLabel|dangerHelp|dialog|dialogHeading|dialogBody|dialogActions|purgeBanner)' "app/(markos)/settings/danger/page.tsx"` returns 0.
    - **Banned-lexicon = 0:** `grep -inE 'Please|Are you sure|synergy|leverage|empower|unlock|transform|seamless|innovative' "app/(markos)/settings/danger/page.tsx"` returns 0.
    - **DZ-4 (Phase 201 tenant-delete wiring):** `node --test test/auth/ test/tenancy/ test/tenant-auth/` exits 0.
    - **`npm run build` exits 0.**
  </acceptance_criteria>
  <done>`page.tsx` updated; consequence `.c-notice c-notice--error` ABOVE confirm form; `.c-modal` + confirm-by-typing pattern preserved; consolidated `.c-button--destructive` (×2); Phase 201 wiring preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create app/(markos)/settings/danger/page.stories.tsx with ≥3 named state stories (`Default`, `DeleteConfirm`, `ExportInProgress`) per UI-SPEC X-4</name>
  <read_first>
    - app/(markos)/settings/danger/page.tsx (post-Task 2 shape)
    - 213.3-UI-SPEC.md "Storybook State Coverage" line 581
    - .planning/phases/213.3-.../213.3-04-PLAN.md Task 3 (precedent)
  </read_first>
  <files>app/(markos)/settings/danger/page.stories.tsx</files>
  <action>
**Create new `app/(markos)/settings/danger/page.stories.tsx` with 3 CSF3 named state stories per UI-SPEC X-4 line 581:**

```tsx
import type { Meta, StoryObj } from '@storybook/react';
import DangerPage from './page';

const meta = {
  title: 'Settings/Danger',
  component: DangerPage,
  parameters: { layout: 'fullscreen' },
} satisfies Meta<typeof DangerPage>;

export default meta;

type Story = StoryObj<typeof meta>;

export const Default: Story = {
  args: {
    tenantName: 'acme-prod',
  },
};

export const DeleteConfirm: Story = {
  args: {
    tenantName: 'acme-prod',
    showDeleteConfirm: true,
    confirmText: '',
  },
  parameters: {
    docs: { description: { story: '`.c-modal` + `.c-backdrop` confirm with `.c-notice c-notice--error` consequence above + `.c-input` + `.c-field__error` validation per UI-SPEC AC DZ-2/DZ-3.' } },
  },
};

export const ExportInProgress: Story = {
  args: {
    tenantName: 'acme-prod',
    exportInProgress: true,
    exportStartedAt: '2026-04-28T10:30:00Z',
  },
  parameters: {
    docs: { description: { story: '`.c-notice c-notice--info` "Export in progress" banner.' } },
  },
};
```

KEY rules:
- 3 named state exports per UI-SPEC line 581
- `args` shape mirrors `DangerPage` prop interface
- DOES NOT set `parameters.theme: "legacy"`

After editing, run `npm run build-storybook` — MUST exit 0.
  </action>
  <verify>
    <automated>test -f "app/(markos)/settings/danger/page.stories.tsx" && grep -cE '^export const (Default|DeleteConfirm|ExportInProgress):' "app/(markos)/settings/danger/page.stories.tsx" | awk '$1>=3 {exit 0} $1<3 {exit 1}' && ! grep -qE 'parameters\.theme.*"legacy"' "app/(markos)/settings/danger/page.stories.tsx" && grep -q 'export default meta' "app/(markos)/settings/danger/page.stories.tsx" && npm run build-storybook && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **X-4 (≥3 named state stories):** `grep -cE '^export const (Default|DeleteConfirm|ExportInProgress):' "app/(markos)/settings/danger/page.stories.tsx"` ≥3.
    - **CSF3 typed `meta`:** `grep -c 'export default meta' "app/(markos)/settings/danger/page.stories.tsx"` ≥1.
    - **Default-not-legacy theme:** `grep -cE 'parameters\.theme.*"legacy"' "app/(markos)/settings/danger/page.stories.tsx"` returns 0.
    - **`npm run build-storybook` exits 0.**
  </acceptance_criteria>
  <done>`page.stories.tsx` created with 3 CSF3 named state exports; `npm run build-storybook` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→tenant-delete API | Untrusted delete action crosses via Phase 201 tenancy wiring |
| client→tenant-export API | Data-export action; signed-URL crosses via Phase 201 GDPR contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213.3-05-01 | Elevation of Privilege | tenant-delete handler | mitigate | Confirm-by-typing-tenant-name validation preserved (DZ-3); `.c-modal` + consequence `.c-notice c-notice--error` BEFORE confirm form (DZ-2); button disabled until match |
| T-213.3-05-02 | Information Disclosure | export signed URL | accept | Phase 201 GDPR signed-URL semantics preserved verbatim |
| T-213.3-05-03 | Denial of Service | accidental tenant deletion | mitigate | Two-step confirm: (1) Click delete → modal opens; (2) Type tenant name → button enables; explicit "permanently" copy on final CTA |
</threat_model>

<verification>
After all 3 tasks:

```bash
grep -nE '#[0-9a-fA-F]{3,8}' "app/(markos)/settings/danger/page.module.css"  # 0
grep -cE 'c-notice c-notice--error' "app/(markos)/settings/danger/page.tsx"  # >=1
grep -cE 'c-button c-button--destructive' "app/(markos)/settings/danger/page.tsx"  # >=2
grep -cE 'c-modal' "app/(markos)/settings/danger/page.tsx"  # >=1
grep -cE '^export const (Default|DeleteConfirm|ExportInProgress):' "app/(markos)/settings/danger/page.stories.tsx"  # >=3
npm run build  # 0
npm run build-storybook  # 0
node --test test/auth/ test/tenancy/ test/tenant-auth/  # 0
```
</verification>

<success_criteria>
- AC DZ-1, DZ-2, DZ-3, DZ-4 close.
- Phase 201 tenant-delete + GDPR-export wiring preserved.
- Confirm-by-typing-tenant-name validation logic preserved verbatim.
- Atomic commit: `feat(213.3-05): redesign /settings/danger surface to DESIGN.md v1.1.0 tokens + .c-notice c-notice--error consequence + .c-modal confirm + consolidated .c-button--destructive + Storybook state coverage`.
</success_criteria>

<output>
Create `213.3-05-SUMMARY.md` documenting files modified, AC closures (DZ-1..DZ-4), hand-off to Plan 213.3-06 (mcp).
</output>
