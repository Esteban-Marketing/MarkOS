---
phase: 213.3
plan: 07
type: execute
wave: 1
depends_on:
  - "213.3-06"
files_modified:
  - app/(markos)/settings/plugins/page-shell.module.css
  - app/(markos)/settings/plugins/page.tsx
  - app/(markos)/settings/plugins/page-shell.tsx
  - app/(markos)/settings/plugins/page.stories.tsx
autonomous: true
requirements:
  - P-1
  - P-2
  - P-3
  - P-4
  - P-5
must_haves:
  truths:
    - "app/(markos)/settings/plugins/page-shell.module.css contains zero inline hex; zero off-grid spacing; zero Sora/Space Grotesk; zero teal hexes; **CRITICAL: `linear-gradient(135deg, #0f766e, #155e75)` on `.brandCard` ELIMINATED** (replaced with `background: var(--color-surface-raised); border: 1px solid var(--color-border);` per RESEARCH.md key finding #2 + UI-SPEC AC X-3)"
    - "app/(markos)/settings/plugins/page-shell.tsx plugin cards compose `.c-card c-card--interactive` (clickable marketplace cards); installed badge composes `.c-chip c-chip--mint` `[ok] Installed`; disabled badge composes `.c-badge c-badge--warning` `[warn] Disabled`; update-available composes `.c-badge c-badge--info` `[info] Update available` per P-2"
    - "app/(markos)/settings/plugins/page-shell.tsx plugin slug renders via `.c-chip-protocol` (e.g. `[plugin:posthog]`); no product photography (P-3)"
    - "app/(markos)/settings/plugins/page-shell.tsx uninstall CTA composes `.c-button c-button--destructive`; compatibility warning composes `.c-notice c-notice--warning` (P-4)"
    - "app/(markos)/settings/plugins/page-shell.tsx composes `.c-card`, `.c-card--interactive`, `.c-button{,--primary,--destructive}`, `.c-chip{,--mint}`, `.c-chip-protocol`, `.c-badge--{warning,info}`, `.c-notice c-notice--warning` per UI-SPEC Surface 7"
    - "app/(markos)/settings/plugins/page.stories.tsx (extending existing) declares ≥5 named state stories: `Default`, `Installed`, `Disabled`, `Updated`, `Marketplace` per P-5"
    - "Banned-lexicon scan = 0; no exclamation; no emoji; no \"Please\""
    - "DOM tree + prop interface UNCHANGED per CONTEXT D-05; existing plugin marketplace + tenant-installed listing wiring preserved"
    - "`npm run build` exits 0; `npm run build-storybook` exits 0; existing test suites (if any for plugins) stay green"
  artifacts:
    - path: "app/(markos)/settings/plugins/page-shell.module.css"
      provides: "Token-only plugin marketplace + tenant-installed shell — page/grid/card layout + brand-card layout (gradient ELIMINATED) + logo/logoFallback recipe; primitives carry button/chip/badge/notice composition"
      min_lines: 60
      contains: "var(--color-surface-raised)"
    - path: "app/(markos)/settings/plugins/page-shell.tsx"
      provides: "Updated plugin shell — `.c-card--interactive` plugin cards + `.c-chip--mint`/`.c-badge--warning`/`.c-badge--info` badges + `.c-chip-protocol` for plugin slug + `.c-button--destructive` uninstall + `.c-notice c-notice--warning` compatibility"
      contains: "c-card--interactive"
    - path: "app/(markos)/settings/plugins/page.tsx"
      provides: "Thin server wrapper unchanged structurally; className updates if any"
      contains: "page-shell"
    - path: "app/(markos)/settings/plugins/page.stories.tsx"
      provides: "Storybook 8 CSF3 ≥5 named state exports (Default / Installed / Disabled / Updated / Marketplace)"
      exports: ["default", "Default", "Installed", "Disabled", "Updated", "Marketplace"]
  key_links:
    - from: "app/(markos)/settings/plugins/page-shell.tsx"
      to: "styles/components.css `.c-card--interactive` + `.c-chip--mint` + `.c-chip-protocol` primitives"
      via: "className composition"
      pattern: "c-card--interactive|c-chip--mint|c-chip-protocol"
    - from: "app/(markos)/settings/plugins/page-shell.module.css"
      to: "elimination of `linear-gradient(135deg, #0f766e, #155e75)` on `.brandCard`"
      via: "replacement with `background: var(--color-surface-raised); border: 1px solid var(--color-border);`"
      pattern: "var\\(--color-surface-raised\\)"
---

<objective>
Redesign `/settings/plugins` (`page-shell.module.css` 329 LOC + `page.tsx` 14 LOC + `page-shell.tsx` 156 LOC) into the canonical DESIGN.md v1.1.0 visual contract per `213.3-UI-SPEC.md` Surface 7 (5 ACs: P-1 through P-5). **CRITICAL: this plan eliminates the only gradient violation in the wave** — `linear-gradient(135deg, #0f766e, #155e75)` on `.brandCard` (RESEARCH.md key finding #2 + UI-SPEC AC X-3). Replacement: flat `background: var(--color-surface-raised); border: 1px solid var(--color-border); border-radius: var(--radius-lg);`.

Compose `.c-card--interactive` (clickable marketplace tiles) / `.c-chip c-chip--mint` (installed badge) / `.c-badge--warning` (disabled badge) / `.c-badge--info` (update-available badge) / `.c-chip-protocol` (plugin slug) / `.c-button{,--primary,--destructive}` / `.c-notice c-notice--warning` (compatibility warning) per UI-SPEC primitive reuse. Extend existing `page.stories.tsx` with ≥5 named state stories.

Output: `page-shell.module.css` rewritten (329 → ~80 LOC); `page.tsx` (14 LOC, likely thin wrapper — minimal changes) + `page-shell.tsx` updated; `page.stories.tsx` extended (~100 LOC, 5 CSF3 named state exports).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-CONTEXT.md
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-UI-SPEC.md
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-RESEARCH.md
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-VALIDATION.md
@DESIGN.md
@CLAUDE.md
@app/tokens.css
@app/globals.css
@styles/components.css
@app/(markos)/settings/plugins/page-shell.module.css
@app/(markos)/settings/plugins/page.tsx
@app/(markos)/settings/plugins/page-shell.tsx
@app/(markos)/settings/plugins/page.stories.tsx
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-06-PLAN.md

<interfaces>
From `213.3-RESEARCH.md` Class-Rename Map: Surface 7 — Plugins (lines 530–560):
- **CRITICAL FIND #2 (line 535):** `page-shell.module.css` line 61–68 contains `.brandCard { background: linear-gradient(135deg, #0f766e, #155e75); }` — gradient (C-11 violation) MUST be eliminated. Replacement: `background: var(--color-surface-raised); border: 1px solid var(--color-border);`
- `.page` → Standard pattern
- `.shell` → Local: `max-width: 1200px; margin: 0 auto; display: grid; gap: var(--space-lg);`
- `.summaryCard`, `.panel` → DELETE; compose `.c-card`
- `.summaryHeader` → Local: `display: grid; grid-template-columns: 1.3fr 0.9fr; gap: var(--space-md); align-items: start;`
- `.eyebrow` → DELETE; apply `.t-label-caps`
- `.title` → DELETE; plain `<h2>`
- `.summaryText`, `.bodyText`, `.toggleDescription`, `.dangerText`, `.brandMeta` → DELETE; body `<p>` globals
- `.brandCard` → REWRITE (CRITICAL gradient elimination): `background: var(--color-surface-raised); border: 1px solid var(--color-border); border-radius: var(--radius-lg); padding: var(--space-md); display: flex; align-items: center;`
- `.cardHeader` → Local: `display: flex; align-items: center; gap: var(--space-md);`
- `.logo` → Local: `width: 48px; height: 48px; border-radius: var(--radius-sm); object-fit: cover;`
- `.logoFallback` → Local: `width: 48px; height: 48px; border-radius: var(--radius-sm); background: var(--color-surface-overlay); display: flex; align-items: center; justify-content: center; color: var(--color-on-surface); font-weight: var(--fw-bold); font-size: var(--fs-h4);`
- `.contentGrid` → Local: `display: grid; grid-template-columns: 1.4fr 1fr; gap: var(--space-md);`
- Plugin cards (`.contentCard`) → DELETE; compose `.c-card c-card--interactive` (P-2)
- Plugin installed badge → `.c-chip c-chip--mint` `[ok] Installed` (P-2)
- Plugin disabled badge → `.c-badge c-badge--warning` `[warn] Disabled`
- Plugin update-available badge → `.c-badge c-badge--info` `[info] Update available`
- Plugin slug → `.c-chip-protocol` (P-3)
- Plugin tag chips → `.c-chip` (pill-shaped, muted)
- Install CTA → `.c-button c-button--primary`
- Uninstall CTA → `.c-button c-button--destructive` (P-4)
- (NEW) Compatibility warning → `.c-notice c-notice--warning` (P-4)

From `213.3-UI-SPEC.md` Surface 7 Copywriting Contract (lines 300–315):
- Page heading: "Plugins"
- Installed badge: `[ok] Installed` via `.c-chip--mint`
- Disabled badge: `[warn] Disabled` via `.c-badge--warning`
- Update available badge: `[info] Update available` via `.c-badge--info`
- Install CTA: "Install" via `.c-button--primary`
- Uninstall CTA: "Uninstall" via `.c-button--destructive`
- Plugin slug: `[plugin:posthog]` via `.c-chip-protocol`
- Compatibility warning: `[warn] This plugin requires MarkOS v{N} or later.`
- Empty state (marketplace): "No plugins available."
- Empty state (none installed): "No plugins installed. Browse the marketplace to add integrations."

From `styles/components.css` v1.1.0 (`.c-card--interactive`, `.c-chip--mint`):
```css
.c-card--interactive { /* hover: border-strong; cursor: pointer; transition var(--duration-base) */ }
.c-chip--mint { /* var(--color-primary-text) text + var(--color-primary-subtle) bg */ }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite app/(markos)/settings/plugins/page-shell.module.css to token-only layout module (P-1, X-1, X-2, X-3) — **CRITICAL: eliminate `linear-gradient(135deg, #0f766e, #155e75)` on `.brandCard`** + retain layout containers + brand-card flat surface + logo/logoFallback recipe</name>
  <read_first>
    - app/(markos)/settings/plugins/page-shell.module.css (current 329 LOC, 34 hex — focus on lines 61–68 `.brandCard` gradient discovery from RESEARCH.md)
    - app/(markos)/settings/plugins/page.tsx (current 14 LOC — likely thin wrapper)
    - app/(markos)/settings/plugins/page-shell.tsx (current 156 LOC — confirm classNames referenced)
    - 213.3-RESEARCH.md "Class-Rename Map: Surface 7 — Plugins" (lines 530–560) + "Critical discovery" line 535
    - 213.3-UI-SPEC.md "Plugins" Component Primitive Reuse (lines 489–502)
    - app/tokens.css (--color-surface-raised, --color-border, --color-surface-overlay, --color-on-surface, --space-md/-lg, --radius-lg/-sm, --fw-bold, --fs-h4)
    - styles/components.css v1.1.0 (`.c-card{,--interactive}`, `.c-button*`, `.c-chip{,--mint}`, `.c-chip-protocol`, `.c-badge--{warning,info}`, `.c-notice--warning`)
    - DESIGN.md "Color > Forbidden values" (no gradients) + CLAUDE.md C-11
    - .planning/phases/213.3-.../213.3-06-PLAN.md Task 1 (precedent)
  </read_first>
  <files>app/(markos)/settings/plugins/page-shell.module.css</files>
  <action>
**Replace `app/(markos)/settings/plugins/page-shell.module.css` ENTIRELY with token-only layout module. CRITICAL: eliminate the `linear-gradient(135deg, #0f766e, #155e75)` on `.brandCard`.**

Required structural sections:

1. **`.page`**: `padding: var(--space-md); background: var(--color-surface); display: flex; flex-direction: column; gap: var(--space-md);`
2. **`.shell`**: `max-width: 1200px; margin: 0 auto; display: grid; gap: var(--space-lg);`
3. **`.summaryHeader`**: `display: grid; grid-template-columns: 1.3fr 0.9fr; gap: var(--space-md); align-items: start;` + `@media (max-width: 959.98px) { grid-template-columns: 1fr; }`
4. **`.brandCard` (CRITICAL — gradient eliminated):** `background: var(--color-surface-raised); border: 1px solid var(--color-border); border-radius: var(--radius-lg); padding: var(--space-md); display: flex; align-items: center; gap: var(--space-md);`
5. **`.cardHeader`**: `display: flex; align-items: center; gap: var(--space-md);`
6. **`.logo`**: `width: 48px; height: 48px; border-radius: var(--radius-sm); object-fit: cover;` (48px is sizing literal — logo image dimension)
7. **`.logoFallback`**: `width: 48px; height: 48px; border-radius: var(--radius-sm); background: var(--color-surface-overlay); display: flex; align-items: center; justify-content: center; color: var(--color-on-surface); font-weight: var(--fw-bold); font-size: var(--fs-h4);`
8. **`.contentGrid`**: `display: grid; grid-template-columns: 1.4fr 1fr; gap: var(--space-md);` + `@media (max-width: 959.98px) { grid-template-columns: 1fr; }`
9. **`.pluginGrid`**: `display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: var(--space-md);` (marketplace grid)
10. **`.pluginCard`**: layout-only — composed JSX-side via `.c-card c-card--interactive`; carries local `display: flex; flex-direction: column; gap: var(--space-sm);`
11. **`.pluginHeader`**: `display: flex; align-items: flex-start; justify-content: space-between; gap: var(--space-sm);`
12. **`.pluginMeta`**: `display: flex; flex-direction: column; gap: var(--space-xxs);`
13. **`.tagRow`**: `display: flex; gap: var(--space-xs); flex-wrap: wrap;`
14. **`.actionRow`**: `display: flex; gap: var(--space-xs); margin-top: var(--space-sm);`
15. **`.emptyState`**: `font-size: var(--fs-body-md); color: var(--color-on-surface-muted); padding: var(--space-md);`

DELETE entirely:
- `.summaryCard`, `.panel`, `.contentCard` (`.c-card` / `.c-card--interactive` primitives)
- `.eyebrow`, `.title`, `.summaryText`, `.bodyText`, `.toggleDescription`, `.dangerText`, `.brandMeta` (globals + `.t-label-caps` utility)
- `.installedBadge`, `.disabledBadge`, `.updateBadge`, `.tagChip`, `.pluginSlug` (`.c-chip*`/`.c-badge*`/`.c-chip-protocol` primitives)
- `.installButton`, `.uninstallButton` (`.c-button*` primitives)
- `.compatibilityWarning` (`.c-notice c-notice--warning` primitive)
- `.toast`, `.dialog*` (primitives)
- All teal hexes `#0d9488`/`#0f766e`/`#155e75`/`#14b8a6`/`#e6fffb`
- All `linear-gradient(*)` declarations (CRITICAL — gradient elimination)
- All `font-family: 'Sora'/'Space Grotesk'`
- All `border-radius: 28px/20px/16px/12px` literals (composed via primitives)
- All slate hexes / orange-red hexes
- All drop-shadow declarations

DO NOT redeclare `@media (pointer: coarse)`. DO NOT add `outline:` declarations.
  </action>
  <verify>
    <automated>! grep -nE '#[0-9a-fA-F]{3,8}' "app/(markos)/settings/plugins/page-shell.module.css" && ! grep -niE 'linear-gradient|radial-gradient' "app/(markos)/settings/plugins/page-shell.module.css" && ! grep -niE '(Sora|Space Grotesk|box-shadow|0 18px 45px|transform:\s*translate)' "app/(markos)/settings/plugins/page-shell.module.css" && ! grep -niE '#0d9488|#0f766e|#155e75|#14b8a6|#e6fffb|#0b877c' "app/(markos)/settings/plugins/page-shell.module.css" && ! grep -nE '@media.*pointer.*coarse' "app/(markos)/settings/plugins/page-shell.module.css" && grep -q "var(--color-surface-raised)" "app/(markos)/settings/plugins/page-shell.module.css" && grep -q "var(--color-border)" "app/(markos)/settings/plugins/page-shell.module.css" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **P-1 (Zero inline hex):** `grep -nE '#[0-9a-fA-F]{3,8}' "app/(markos)/settings/plugins/page-shell.module.css"` returns 0.
    - **X-3 CRITICAL (no gradients):** `grep -niE "linear-gradient|radial-gradient" "app/(markos)/settings/plugins/page-shell.module.css"` returns 0.
    - **No teal `#0d9488/#0f766e/#155e75`:** `grep -niE "#0d9488|#0f766e|#155e75|#14b8a6|#e6fffb|#0b877c" "app/(markos)/settings/plugins/page-shell.module.css"` returns 0.
    - **`.brandCard` flat surface (token-cited):** `grep -q "var(--color-surface-raised)" "app/(markos)/settings/plugins/page-shell.module.css"` returns 1; `grep -q "var(--color-border)" "app/(markos)/settings/plugins/page-shell.module.css"` returns 1.
    - **No off-grid spacing:** `grep -nE ':\s*[0-9]+px' "app/(markos)/settings/plugins/page-shell.module.css"` returns ONLY `1px` (border) + `48px` (logo size).
    - **No Sora/Space Grotesk/shadows:** `grep -niE "Sora|Space Grotesk|box-shadow|0 18px 45px|transform:\s*translate" "app/(markos)/settings/plugins/page-shell.module.css"` returns 0.
    - **X-6:** `grep -nE '@media.*pointer.*coarse' "app/(markos)/settings/plugins/page-shell.module.css"` returns 0.
  </acceptance_criteria>
  <done>`page-shell.module.css` rewritten 329 → ~80 LOC; **`linear-gradient` on `.brandCard` ELIMINATED** + replaced with token-cited flat surface; 7 grep gates pass (including the critical no-gradient gate).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update app/(markos)/settings/plugins/page-shell.tsx (+ page.tsx if needed) to compose `.c-card--interactive`/`.c-chip--mint`/`.c-chip-protocol`/`.c-badge--{warning,info}`/`.c-button{,--primary,--destructive}`/`.c-notice c-notice--warning` per UI-SPEC Surface 7 (P-2, P-3, P-4)</name>
  <read_first>
    - app/(markos)/settings/plugins/page-shell.tsx (current 156 LOC — DOM tree to preserve per D-05)
    - app/(markos)/settings/plugins/page.tsx (current 14 LOC — thin wrapper, likely no significant changes)
    - app/(markos)/settings/plugins/page-shell.module.css (post-Task 1 shape)
    - 213.3-UI-SPEC.md "Plugins" Copywriting Contract (lines 300–315) + Component Primitive Reuse (lines 489–502)
    - 213.3-RESEARCH.md "Class-Rename Map: Surface 7" (lines 530–560)
    - styles/components.css v1.1.0 (.c-card--interactive, .c-chip--mint, .c-chip-protocol, .c-badge--{warning,info}, .c-button*, .c-notice--warning)
    - DESIGN.md "Components > Cards · Interactive" + "Iconography"
  </read_first>
  <files>app/(markos)/settings/plugins/page-shell.tsx, app/(markos)/settings/plugins/page.tsx</files>
  <action>
**Edit A — Update `app/(markos)/settings/plugins/page-shell.tsx` per the Class-Rename Map JSX swaps (DOM tree + prop interface UNCHANGED per D-05).**

ClassName + JSX-shape substitutions:

| Existing JSX | New JSX | Citation |
|--------------|---------|----------|
| `<div className={styles.summaryCard}>` | `<div className="c-card">` | `.c-card` |
| `<div className={styles.brandCard}>` | `<div className={styles.brandCard}>` (UNCHANGED — local layout class with token-cited flat surface from Task 1) | layout |
| `<h2 className={styles.title}>Plugins</h2>` | `<h2>Plugins</h2>` | globals |
| `<p className={styles.bodyText}>...</p>` | `<p>...</p>` (or `<p className="t-lead">...</p>` for descriptors) | globals |
| Plugin card `<article className={styles.contentCard}>` | `<article className={`c-card c-card--interactive ${styles.pluginCard}`}>` (P-2) | `.c-card--interactive` |
| `<span className={styles.installedBadge}>Installed</span>` | `<span className="c-chip c-chip--mint">[ok] Installed</span>` | `.c-chip--mint` (P-2) |
| `<span className={styles.disabledBadge}>Disabled</span>` | `<span className="c-badge c-badge--warning">[warn] Disabled</span>` | `.c-badge--warning` (P-2) |
| `<span className={styles.updateBadge}>Update</span>` | `<span className="c-badge c-badge--info">[info] Update available</span>` | `.c-badge--info` (P-2) |
| `<code className={styles.pluginSlug}>posthog</code>` | `<span className="c-chip-protocol">plugin:posthog</span>` | `.c-chip-protocol` (P-3) |
| `<span className={styles.tagChip}>analytics</span>` | `<span className="c-chip">analytics</span>` | `.c-chip` (muted pill) |
| `<button className={styles.installButton}>Install</button>` | `<button type="button" className="c-button c-button--primary" onClick={...}>Install</button>` | `.c-button--primary` |
| `<button className={styles.uninstallButton}>Uninstall</button>` | `<button type="button" className="c-button c-button--destructive" onClick={...}>Uninstall</button>` | `.c-button--destructive` (P-4) |

**P-4 — Compatibility warning notice (NEW composition):**

```tsx
{!plugin.compatible && (
  <div className="c-notice c-notice--warning" role="status">
    <strong>[warn]</strong>{" "}This plugin requires MarkOS v{plugin.minVersion} or later.
  </div>
)}
```

**Empty states:**
- Marketplace empty: `"No plugins available."`
- None installed: `"No plugins installed. Browse the marketplace to add integrations."`

**Banned-lexicon scan = 0.** Reject `Please|synergy|leverage|empower|unlock|transform|seamless|innovative|just `. NO exclamation. NO emoji.

**Edit B — Update `app/(markos)/settings/plugins/page.tsx` if needed.** Likely a thin server-component wrapper rendering `<PageShell>` — minimal changes (only if it carries direct className references).

After both edits, run `npm run build` — MUST exit 0.
  </action>
  <verify>
    <automated>grep -qE 'c-card c-card--interactive' "app/(markos)/settings/plugins/page-shell.tsx" && grep -qE 'c-chip c-chip--mint' "app/(markos)/settings/plugins/page-shell.tsx" && grep -qE 'c-chip-protocol' "app/(markos)/settings/plugins/page-shell.tsx" && grep -qE 'c-badge c-badge--warning' "app/(markos)/settings/plugins/page-shell.tsx" && grep -qE 'c-button c-button--primary' "app/(markos)/settings/plugins/page-shell.tsx" && grep -qE 'c-button c-button--destructive' "app/(markos)/settings/plugins/page-shell.tsx" && grep -qE 'c-notice c-notice--warning' "app/(markos)/settings/plugins/page-shell.tsx" && grep -qE '\[ok\] Installed' "app/(markos)/settings/plugins/page-shell.tsx" && ! grep -qE 'styles\.(installedBadge|disabledBadge|updateBadge|installButton|uninstallButton|pluginSlug|tagChip)' "app/(markos)/settings/plugins/page-shell.tsx" && ! grep -inE 'Please|synergy|leverage|empower|unlock|transform|seamless|innovative' "app/(markos)/settings/plugins/page-shell.tsx" "app/(markos)/settings/plugins/page.tsx" && npm run build && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **P-2 (`.c-card--interactive` plugin cards):** `grep -cE 'c-card c-card--interactive' "app/(markos)/settings/plugins/page-shell.tsx"` ≥1.
    - **P-2 (`.c-chip--mint` installed badge):** `grep -cE 'c-chip c-chip--mint' "app/(markos)/settings/plugins/page-shell.tsx"` ≥1; `grep -cE '\[ok\] Installed' "app/(markos)/settings/plugins/page-shell.tsx"` ≥1.
    - **P-2 (`.c-badge--warning` disabled):** `grep -cE 'c-badge c-badge--warning' "app/(markos)/settings/plugins/page-shell.tsx"` ≥1.
    - **P-3 (`.c-chip-protocol` plugin slug):** `grep -cE 'c-chip-protocol' "app/(markos)/settings/plugins/page-shell.tsx"` ≥1.
    - **P-4 (`.c-button--destructive` uninstall + `.c-notice--warning` compatibility):** `grep -cE 'c-button c-button--destructive' "app/(markos)/settings/plugins/page-shell.tsx"` ≥1; `grep -cE 'c-notice c-notice--warning' "app/(markos)/settings/plugins/page-shell.tsx"` ≥1.
    - **No legacy class refs:** `grep -cE 'styles\.(installedBadge|disabledBadge|updateBadge|installButton|uninstallButton|pluginSlug|tagChip)' "app/(markos)/settings/plugins/page-shell.tsx"` returns 0.
    - **Banned-lexicon = 0:** `grep -inE 'Please|synergy|leverage|empower|unlock|transform|seamless|innovative' "app/(markos)/settings/plugins/page-shell.tsx" "app/(markos)/settings/plugins/page.tsx"` returns 0.
    - **`npm run build` exits 0.**
  </acceptance_criteria>
  <done>`page-shell.tsx` updated; `.c-card--interactive` + `.c-chip--mint` + `.c-chip-protocol` + `.c-badge--{warning,info}` + `.c-button--{primary,destructive}` + `.c-notice c-notice--warning` composed; banned-lexicon = 0; build clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend app/(markos)/settings/plugins/page.stories.tsx with ≥5 named state stories (`Default`, `Installed`, `Disabled`, `Updated`, `Marketplace`) per UI-SPEC X-4 + P-5</name>
  <read_first>
    - app/(markos)/settings/plugins/page.stories.tsx (existing — confirm shape; extend, don't replace)
    - app/(markos)/settings/plugins/page-shell.tsx (post-Task 2 shape)
    - 213.3-UI-SPEC.md "Storybook State Coverage" line 583
    - .planning/phases/213.3-.../213.3-01-PLAN.md Task 3 (existing-stories extension precedent)
  </read_first>
  <files>app/(markos)/settings/plugins/page.stories.tsx</files>
  <action>
**Extend existing `app/(markos)/settings/plugins/page.stories.tsx` (do not replace) with ≥5 named state stories per UI-SPEC X-4 + P-5 line 583:**

```tsx
import type { Meta, StoryObj } from '@storybook/react';
import PluginsShell from './page-shell';

// Existing meta default-export retained — confirm shape during implementation
const meta = {
  title: 'Settings/Plugins',
  component: PluginsShell,
  parameters: { layout: 'fullscreen' },
} satisfies Meta<typeof PluginsShell>;

export default meta;

type Story = StoryObj<typeof meta>;

const samplePlugin = (overrides: Partial<any> = {}) => ({
  slug: 'posthog',
  name: 'PostHog Analytics',
  description: 'Product analytics integration',
  tags: ['analytics', 'tracking'],
  installed: false,
  disabled: false,
  updateAvailable: false,
  compatible: true,
  minVersion: '1.0.0',
  ...overrides,
});

export const Default: Story = {
  args: {
    plugins: [samplePlugin(), samplePlugin({ slug: 'segment', name: 'Segment' }), samplePlugin({ slug: 'mixpanel', name: 'Mixpanel' })],
  },
};

export const Installed: Story = {
  args: {
    plugins: [
      samplePlugin({ slug: 'posthog', installed: true }),
      samplePlugin({ slug: 'segment', name: 'Segment', installed: true }),
    ],
  },
  parameters: { docs: { description: { story: '`.c-chip c-chip--mint` `[ok] Installed` badge per UI-SPEC AC P-2.' } } },
};

export const Disabled: Story = {
  args: {
    plugins: [
      samplePlugin({ slug: 'posthog', installed: true, disabled: true }),
    ],
  },
  parameters: { docs: { description: { story: '`.c-badge c-badge--warning` `[warn] Disabled` badge per UI-SPEC AC P-2.' } } },
};

export const Updated: Story = {
  args: {
    plugins: [
      samplePlugin({ slug: 'posthog', installed: true, updateAvailable: true }),
    ],
  },
  parameters: { docs: { description: { story: '`.c-badge c-badge--info` `[info] Update available` badge per UI-SPEC AC P-2.' } } },
};

export const Marketplace: Story = {
  args: {
    plugins: [
      samplePlugin({ slug: 'posthog', name: 'PostHog Analytics' }),
      samplePlugin({ slug: 'segment', name: 'Segment' }),
      samplePlugin({ slug: 'mixpanel', name: 'Mixpanel' }),
      samplePlugin({ slug: 'amplitude', name: 'Amplitude' }),
      samplePlugin({ slug: 'incompatible', name: 'Old Plugin', compatible: false, minVersion: '2.0.0' }),
    ],
  },
  parameters: { docs: { description: { story: 'Marketplace grid with one incompatible plugin showing `.c-notice c-notice--warning` compatibility notice per UI-SPEC AC P-4.' } } },
};
```

KEY rules:
- 5 named state exports per UI-SPEC line 583
- Each state exercises specific primitive variant (chip-mint / badge-warning / badge-info / notice-warning)
- DOES NOT set `parameters.theme: "legacy"`

After editing, run `npm run build-storybook` — MUST exit 0.
  </action>
  <verify>
    <automated>test -f "app/(markos)/settings/plugins/page.stories.tsx" && grep -cE '^export const (Default|Installed|Disabled|Updated|Marketplace):' "app/(markos)/settings/plugins/page.stories.tsx" | awk '$1>=5 {exit 0} $1<5 {exit 1}' && ! grep -qE 'parameters\.theme.*"legacy"' "app/(markos)/settings/plugins/page.stories.tsx" && grep -q 'export default meta' "app/(markos)/settings/plugins/page.stories.tsx" && npm run build-storybook && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **P-5 (≥5 named state stories):** `grep -cE '^export const (Default|Installed|Disabled|Updated|Marketplace):' "app/(markos)/settings/plugins/page.stories.tsx"` ≥5.
    - **CSF3 typed `meta`:** `grep -c 'export default meta' "app/(markos)/settings/plugins/page.stories.tsx"` ≥1.
    - **Default-not-legacy theme:** `grep -cE 'parameters\.theme.*"legacy"' "app/(markos)/settings/plugins/page.stories.tsx"` returns 0.
    - **`npm run build-storybook` exits 0** (P-5 closure).
  </acceptance_criteria>
  <done>`page.stories.tsx` extended with 5 CSF3 named state exports; `npm run build-storybook` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→plugin marketplace | Untrusted plugin install/uninstall actions; existing plugin engine wiring preserved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213.3-07-01 | Tampering | plugin install action | accept | Wiring preserved verbatim from existing plugin engine |
| T-213.3-07-02 | Spoofing | plugin slug display | mitigate | `.c-chip-protocol` register makes plugin slug unambiguous (`[plugin:posthog]` format); compatibility warning surfaces incompatible-version risk |
</threat_model>

<verification>
After all 3 tasks:

```bash
grep -nE '#[0-9a-fA-F]{3,8}' "app/(markos)/settings/plugins/page-shell.module.css"  # 0
grep -niE "linear-gradient|radial-gradient" "app/(markos)/settings/plugins/page-shell.module.css"  # 0 (CRITICAL)
grep -cE 'c-card c-card--interactive' "app/(markos)/settings/plugins/page-shell.tsx"  # >=1
grep -cE 'c-chip c-chip--mint' "app/(markos)/settings/plugins/page-shell.tsx"  # >=1
grep -cE 'c-chip-protocol' "app/(markos)/settings/plugins/page-shell.tsx"  # >=1
grep -cE '^export const (Default|Installed|Disabled|Updated|Marketplace):' "app/(markos)/settings/plugins/page.stories.tsx"  # >=5
npm run build  # 0
npm run build-storybook  # 0
```
</verification>

<success_criteria>
- AC P-1, P-2, P-3, P-4, P-5 close.
- **CRITICAL: `linear-gradient(135deg, #0f766e, #155e75)` on `.brandCard` ELIMINATED** (X-3 closure for plugins surface).
- DOM-tree + plugin engine wiring preserved.
- Atomic commit: `feat(213.3-07): redesign /settings/plugins surface to DESIGN.md v1.1.0 tokens + .c-card--interactive/.c-chip--mint/.c-chip-protocol/.c-badge composition + .c-notice c-notice--warning compatibility + GRADIENT ELIMINATION on .brandCard + Storybook state coverage`.
</success_criteria>

<output>
Create `213.3-07-SUMMARY.md` documenting files modified, AC closures (P-1..P-5), gradient elimination on `.brandCard`, hand-off to Plan 213.3-08 (webhooks).
</output>
