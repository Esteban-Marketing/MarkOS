---
phase: 213.3
plan: 02
type: execute
wave: 1
depends_on:
  - "213.3-01"
files_modified:
  - app/(markos)/settings/members/page.module.css
  - app/(markos)/settings/members/page.tsx
  - app/(markos)/settings/members/page.stories.tsx
autonomous: true
requirements:
  - M-1
  - M-2
  - M-3
  - M-4
  - M-5
must_haves:
  truths:
    - "app/(markos)/settings/members/page.module.css contains zero inline hex; zero off-grid spacing; zero Sora/Space Grotesk/gradients/box-shadow/transform-translate; zero teal hexes (#0d9488/#0f766e/#14b8a6/#e6fffb/#0b877c)"
    - "app/(markos)/settings/members/page.module.css seat-bar uses token-cited `.meterTrack`/`.meterFill` recipe with state-color fill (var(--color-success) <70%, var(--color-warning) 70-90%, var(--color-error) >=90%) per UI-SPEC AC M-2"
    - "app/(markos)/settings/members/page.tsx renders invite-pending state via `.c-notice c-notice--info` with `[info]` bracketed glyph (M-3)"
    - "app/(markos)/settings/members/page.tsx pending-invite badge uses `.c-badge c-badge--info` with `[info] Pending` copy (M-4); role badges use `.c-badge` or `.c-badge--info`"
    - "app/(markos)/settings/members/page.tsx member-remove confirm uses `.c-modal` + `.c-backdrop` + `.c-button c-button--destructive` (M-5); copy is `\"Remove {name} from this workspace?\"` (no \"Are you sure?\")"
    - "app/(markos)/settings/members/page.tsx composes `.c-card`, `.c-input`, `.c-field`, `.c-field__label`, `.c-button c-button--primary`, `.c-button c-button--destructive`, `.c-button c-button--tertiary` per UI-SPEC Surface 2 primitive reuse"
    - "app/(markos)/settings/members/page.stories.tsx (NEW) declares ≥5 named state stories: `Default`, `Filled`, `InvitePending`, `RoleEdit`, `Empty`"
    - "Banned-lexicon scan = 0 in modified `.tsx`; no exclamation points; no emoji; no \"Please\" softeners (CONTEXT D-07)"
    - "DOM tree + prop interface UNCHANGED per CONTEXT D-05; Phase 201 tenancy wiring (member invite, member remove, role-edit RLS) preserved per D-21"
    - "`npm run build` exits 0; `npm run build-storybook` exits 0; `node --test test/auth/ test/tenancy/ test/tenant-auth/` exits 0"
  artifacts:
    - path: "app/(markos)/settings/members/page.module.css"
      provides: "Token-only members layout module — page/card layout + invite-form grid + meter recipe + table recipe + avatar recipe; primitives carry button/input/field/badge/notice composition JSX-side"
      min_lines: 50
      contains: "var(--color-surface)"
    - path: "app/(markos)/settings/members/page.tsx"
      provides: "Updated members page composing primitives; `.c-notice c-notice--info` invite-pending banner; `.c-modal` remove-confirm; preserves Phase 201 tenancy wiring"
      contains: "c-notice c-notice--info"
    - path: "app/(markos)/settings/members/page.stories.tsx"
      provides: "Storybook 8 CSF3 ≥5 named state exports (Default / Filled / InvitePending / RoleEdit / Empty)"
      exports: ["default", "Default", "Filled", "InvitePending", "RoleEdit", "Empty"]
  key_links:
    - from: "app/(markos)/settings/members/page.tsx"
      to: "styles/components.css `.c-notice c-notice--info` + `.c-modal` + `.c-backdrop` primitives"
      via: "className composition for invite-pending banner + remove-confirm modal"
      pattern: "c-notice\\s+c-notice--info"
    - from: "app/(markos)/settings/members/page.module.css"
      to: "app/tokens.css var(--color-{success,warning,error}) state tokens"
      via: ".meterFill state-aware color"
      pattern: "var\\(--color-(success|warning|error)\\)"
---

<objective>
Redesign `/settings/members` (`page.module.css` 210 LOC + `page.tsx` 205 LOC) into the canonical DESIGN.md v1.1.0 visual contract per `213.3-UI-SPEC.md` Surface 2 (5 ACs: M-1 through M-5). Compose `.c-card` / `.c-button{,--primary,--destructive,--tertiary}` / `.c-input` / `.c-field{,__label,__help}` / `.c-badge--info` / `.c-notice c-notice--info` / `.c-modal` + `.c-backdrop` per UI-SPEC primitive reuse. Add token-cited `.meterTrack` / `.meterFill` recipe for the seat bar with state-color fill (`var(--color-success)` < 70%, `var(--color-warning)` 70–90%, `var(--color-error)` ≥ 90%). Create new `page.stories.tsx` with ≥5 named state stories (`Default`, `Filled`, `InvitePending`, `RoleEdit`, `Empty`).

Purpose: Close UI-SPEC AC M-1 (zero inline hex), M-2 (seat bar state-token fill, no `#0d9488`), M-3 (invite-pending `.c-notice c-notice--info` + teal caption color eliminated), M-4 (pending badge `.c-badge--info`, no teal pill), M-5 (member-remove `.c-modal` + `.c-backdrop` + `.c-button--destructive` with imperative copy). Preserve Phase 201 tenancy wiring (D-21).

Output: `page.module.css` rewritten (210 → ~60 LOC, token-only); `page.tsx` updated (className-only rewrite + `.c-notice` JSX + `.c-modal` confirm JSX; DOM-shape preserved); new `page.stories.tsx` (~80 LOC, 5 CSF3 named state exports).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-CONTEXT.md
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-UI-SPEC.md
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-RESEARCH.md
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-VALIDATION.md
@DESIGN.md
@CLAUDE.md
@app/tokens.css
@app/globals.css
@styles/components.css
@app/(markos)/settings/members/page.module.css
@app/(markos)/settings/members/page.tsx
@.planning/phases/213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens/213.3-01-PLAN.md

<interfaces>
From `213.3-RESEARCH.md` Class-Rename Map: Surface 2 — Members (lines 358–391):
- `.page` → Rewrite local: `padding: var(--space-md); background: var(--color-surface); display: flex; flex-direction: column; gap: var(--space-md);`
- `.contentCard` → DELETE; compose `.c-card` + local `max-width: 900px`
- `.heading` → DELETE; plain `<h2>` (globals)
- `.subHeading` (18px Sora h) → DELETE; plain `<h4>` (globals)
- `.subheading` (16px muted) → DELETE; apply `.t-lead`
- `.seatBar` → Rename local: `display: flex; flex-direction: column; gap: var(--space-xs);`
- `.seatBarLabel` → DELETE; apply `.t-label-caps`
- `.seatBarTrack` → Rename local `.meterTrack`: `height: 8px; background: var(--color-surface-overlay); border-radius: var(--radius-full); overflow: hidden;`
- `.seatBarFill` → Rename local `.meterFill`: `height: 100%; background: var(--color-success); transition: width var(--duration-base) var(--ease-out);` (state-aware fill via JSX inline-style or class modifier)
- `.membersTable` → Retain layout: `width: 100%; border-collapse: collapse; margin-top: var(--space-xs);`
- `.membersTable caption` → Rewrite: `.t-label-caps` posture (eliminates teal `#0f766e`)
- `.membersTable th` → Rewrite token recipe (D-14)
- `.membersTable td` → Rewrite token recipe (D-14)
- `.avatar` → Rewrite local: `display: inline-flex; width: 32px; height: 32px; border-radius: var(--radius-full); background: var(--color-surface-overlay); color: var(--color-on-surface-muted); align-items: center; justify-content: center; font-weight: var(--fw-semibold); font-size: var(--fs-code);`
- `.inviteForm` → Rewrite local: `display: grid; grid-template-columns: 2fr 1fr auto; gap: var(--space-xs); align-items: end;`
- `.field` / `.field input` / `.field select` → DELETE; compose `.c-field` + `.c-input`
- `.sendInviteButton` → DELETE; compose `.c-button c-button--primary`
- `.removeButton` → DELETE; compose `.c-button c-button--destructive`
- `.pendingList ul` → DELETE; replace with `.c-notice c-notice--info` banner (M-3)
- `.emptyState` → Rewrite local: `font-size: var(--fs-body-md); color: var(--color-on-surface-muted);`
- `.toast` → DELETE; compose `.c-toast c-toast--{state}` in `.c-toast-region`
- (NEW) Invite-pending notice → `.c-notice c-notice--info` `[info] Invite pending. Resend or revoke if the recipient has not responded.`
- (NEW) Member remove confirm → `.c-modal` + `.c-backdrop` + `.c-button c-button--destructive` "Remove member"
- Pending invite badge → `.c-badge c-badge--info` `[info] Pending`
- Role badge (Admin/Member) → `.c-badge` (neutral) or `.c-badge--info`

From `213.3-UI-SPEC.md` Surface 2 Copywriting Contract (lines 219–232):
- Page heading: "Members" (unchanged)
- Invite-pending notice: `[info] Invite pending. Resend or revoke if the recipient has not responded.` via `.c-notice c-notice--info`
- Members table caption: "Team members" via `.t-label-caps` on `<caption>`
- Invite CTA: "Invite member"
- Member remove confirm: `"Remove {name} from this workspace?"` + body `"They will lose access immediately."` + `.c-button--destructive` "Remove member"
- Resend invite CTA: `"Resend invite"` via `.c-button--tertiary`
- Revoke invite CTA: `"Revoke invite"` via `.c-button--destructive`
- Empty state: `"No members yet. Invite your first team member to get started."`
- Pending badge: `[info] Pending` via `.c-badge--info`

From `styles/components.css` v1.1.0 (`.c-modal` + `.c-backdrop`, `.c-badge--info`, `.c-toast`):
```css
.c-modal { /* fixed positioning, surface-overlay bg, padding var(--space-lg), focus-trap semantics */ }
.c-backdrop { /* fixed full-viewport, blur(4px), z-index */ }
.c-badge--info { /* rgb(88 166 255 / 0.12) bg + var(--color-info) text + uppercase label-caps */ }
```

Sub-component extraction recommendation (Claude's Discretion per CONTEXT D-15): If member-row JSX (avatar + name + role badge + action buttons) exceeds 40 LOC of repetitive JSX in `page.tsx`, extract `MemberRow` to `app/(markos)/_components/MemberRow.tsx`. Final decision in Task 2 — review JSX complexity before extracting.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite app/(markos)/settings/members/page.module.css to token-only layout module (M-1, M-2, X-1, X-2, X-3) — delete bespoke card/button/input/badge styles, retain only layout containers + meter recipe + table recipe + avatar recipe</name>
  <read_first>
    - app/(markos)/settings/members/page.module.css (current 210 LOC — full file)
    - app/(markos)/settings/members/page.tsx (current 205 LOC — confirm classNames referenced)
    - 213.3-RESEARCH.md "Class-Rename Map: Surface 2 — Members" (lines 358–391)
    - 213.3-UI-SPEC.md "Members — page.module.css" Component Primitive Reuse (lines 397–409)
    - 213.3-RESEARCH.md "Pattern 1 — Module CSS Rewrite Shape" (lines 199–254) + "Pitfall 5: 12px off-grid"
    - app/tokens.css (--color-surface/-raised/-overlay, --color-on-surface/-muted, --color-border, --color-success/-warning/-error, --space-xs/-sm/-md/-lg, --radius-full, --fs-body-md/-code/-label, --fw-semibold, --duration-base, --ease-out)
    - styles/components.css v1.1.0 (`.c-card`, `.c-input`, `.c-field`, `.c-button{,--primary,--destructive,--tertiary}`, `.c-badge--info`, `.c-notice--info`, `.c-modal`, `.c-backdrop`, `.c-toast`)
    - DESIGN.md "Spacing Scale" + "Color > Forbidden values" + "Components > Notice/Banner"
    - .planning/phases/213.3-.../213.3-01-PLAN.md Task 1 (token-only layout module precedent)
  </read_first>
  <files>app/(markos)/settings/members/page.module.css</files>
  <action>
**Replace `app/(markos)/settings/members/page.module.css` ENTIRELY with the following token-only layout module (mirror Plan 213.3-01 Task 1 shape; class-rename map from RESEARCH.md Surface 2 authoritative):**

Required structural sections in the rewritten module (executor MUST include each — value snippets cited per RESEARCH.md):

1. **`.page`** (page wrapper): `padding: var(--space-md); background: var(--color-surface); display: flex; flex-direction: column; gap: var(--space-md);`
2. **`.contentCard`** (local override, only `max-width: 900px;` — `.c-card` provides bg/border/radius/padding JSX-side)
3. **`.seatBar`** (`display: flex; flex-direction: column; gap: var(--space-xs);`)
4. **`.meterTrack`** (`height: 8px; background: var(--color-surface-overlay); border-radius: var(--radius-full); overflow: hidden;`) — 8px sizing exception per UI-SPEC Spacing Scale documented exception #4
5. **`.meterFill`** (`height: 100%; background: var(--color-success); transition: width var(--duration-base) var(--ease-out);`) — state-color fill via JSX-inline-style or modifier class for `var(--color-warning)` (70–90%) and `var(--color-error)` (≥ 90%) per UI-SPEC AC M-2
6. **`.meterFill--warning`** (`background: var(--color-warning);`) — state modifier
7. **`.meterFill--error`** (`background: var(--color-error);`) — state modifier
8. **`.membersTable`** (`width: 100%; border-collapse: collapse; margin-top: var(--space-xs);`)
9. **`.membersTable caption`** — apply `.t-label-caps` posture: `padding-block: var(--space-xs); color: var(--color-on-surface-muted); font-size: var(--fs-label); font-weight: var(--fw-semibold); text-transform: uppercase; letter-spacing: var(--ls-label); text-align: left;`
10. **`.membersTable th`** (token recipe D-14) — `padding: var(--space-xs) var(--space-md); color: var(--color-on-surface-muted); font-size: var(--fs-label); font-weight: var(--fw-semibold); text-transform: uppercase; letter-spacing: var(--ls-label); border-bottom: 1px solid var(--color-border); text-align: left;`
11. **`.membersTable td`** (token recipe D-14) — `padding: var(--space-xs) var(--space-md); border-bottom: 1px solid var(--color-border); color: var(--color-on-surface); font-size: var(--fs-body-md); vertical-align: middle;`
12. **`.avatar`** — `display: inline-flex; width: 32px; height: 32px; border-radius: var(--radius-full); background: var(--color-surface-overlay); color: var(--color-on-surface-muted); align-items: center; justify-content: center; font-weight: var(--fw-semibold); font-size: var(--fs-code);` (32px is sizing literal per Pitfall avatar-shaped element; document inline)
13. **`.inviteForm`** (`display: grid; grid-template-columns: 2fr 1fr auto; gap: var(--space-xs); align-items: end;`)
14. **`.actionRow`** (`display: flex; gap: var(--space-xs); align-items: center;`)
15. **`.emptyState`** (`font-size: var(--fs-body-md); color: var(--color-on-surface-muted); padding: var(--space-md);`)

DELETE entirely (replaced by primitives JSX-side):
- `.heading` (plain `<h2>` inherits globals)
- `.subHeading` (plain `<h4>` inherits globals)
- `.subheading` (`.t-lead` utility)
- `.seatBarLabel` (`.t-label-caps` utility)
- `.field`, `.field input`, `.field select` (`.c-field` + `.c-input` primitives)
- `.sendInviteButton`, `.removeButton`, `.resendButton`, `.revokeButton` (`.c-button{,--primary,--destructive,--tertiary}` primitives)
- `.pendingList`, `.pendingList ul`, `.pendingItem` (`.c-notice c-notice--info` primitive)
- `.toast` (`.c-toast c-toast--{state}` primitive)
- `.pendingBadge`, `.roleBadge`, `.statusBadge` (`.c-badge--{info,warning}` primitives)
- All Sora / Space Grotesk `font-family` declarations (globals.css inheritance)
- All `#0d9488`, `#0f766e`, `#14b8a6`, `#e6fffb`, `#0b877c` teal hexes
- All `#0f172a`, `#334155`, `#475569`, `#64748b`, `#cbd5e1`, `#e2e8f0`, `#f1f5f9` slate hexes
- All `#9a3412`, `#fca5a5` orange-red hexes
- All `box-shadow:` declarations
- All `border-radius: 28px/20px/16px/12px/999px` literals (composed via primitives or replaced with `var(--radius-full)` for avatar/meter-track)
- All `font-size: 14px/12px/18px` literals (replaced via `var(--fs-*)` tokens or `<h2>/<h4>` inheritance)
- All `padding: 12px/16px/20px` off-grid literals (replaced with `var(--space-xs/-sm/-md)`)

DO NOT redeclare `@media (pointer: coarse)` (global rule handles `.c-button` per X-6). DO NOT add `outline:` declarations (global `:focus-visible` handles).
  </action>
  <verify>
    <automated>! grep -nE '#[0-9a-fA-F]{3,8}' "app/(markos)/settings/members/page.module.css" && ! grep -niE '(Sora|Space Grotesk|linear-gradient|radial-gradient|box-shadow|0 18px 45px|transform:\s*translate)' "app/(markos)/settings/members/page.module.css" && ! grep -niE '#0d9488|#0f766e|#14b8a6|#e6fffb|#0b877c|#9a3412|#fca5a5' "app/(markos)/settings/members/page.module.css" && ! grep -nE '@media.*pointer.*coarse' "app/(markos)/settings/members/page.module.css" && grep -q "var(--color-surface)" "app/(markos)/settings/members/page.module.css" && grep -cE 'var\(--color-(success|warning|error)' "app/(markos)/settings/members/page.module.css" | awk '$1>=2 {exit 0} $1<2 {exit 1}' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **M-1 (Zero inline hex):** `grep -nE '#[0-9a-fA-F]{3,8}' "app/(markos)/settings/members/page.module.css"` returns 0.
    - **M-2 (state-color fill):** `grep -cE 'var\(--color-(success|warning|error)\)' "app/(markos)/settings/members/page.module.css"` ≥2 (success + warning or success + error at minimum); `grep -niE "#0d9488|#0f766e|#e2e8f0" "app/(markos)/settings/members/page.module.css"` returns 0.
    - **No off-grid spacing (B-2 carry):** `grep -nE ':\s*[0-9]+px' "app/(markos)/settings/members/page.module.css"` returns ONLY `1px` (border) + `8px` (meter track height — UI-SPEC Spacing Scale documented exception #4) + `32px` (avatar size literal — sizing not spacing).
    - **No Sora/Space Grotesk/gradients/shadows:** `grep -niE "Sora|Space Grotesk|linear-gradient|radial-gradient|box-shadow|0 18px 45px|transform:\s*translate" "app/(markos)/settings/members/page.module.css"` returns 0.
    - **No legacy teal/orange-red:** `grep -niE "#0d9488|#0f766e|#14b8a6|#e6fffb|#0b877c|#9a3412|#fca5a5" "app/(markos)/settings/members/page.module.css"` returns 0.
    - **X-6 (no per-module pointer-coarse):** `grep -nE '@media.*pointer.*coarse' "app/(markos)/settings/members/page.module.css"` returns 0.
    - **Token consumption:** `grep -c "var(--color-surface)" "app/(markos)/settings/members/page.module.css"` ≥1; `grep -cE 'var\(--space-' "app/(markos)/settings/members/page.module.css"` ≥6.
  </acceptance_criteria>
  <done>`page.module.css` rewritten to ~60 LOC token-only layout module; all legacy bespoke classes eliminated; meter recipe with state-color fill modifiers in place; 7 grep gates pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update app/(markos)/settings/members/page.tsx to compose `.c-card`/`.c-input`/`.c-field`/`.c-button{,--primary,--destructive,--tertiary}`/`.c-badge--info`/`.c-notice c-notice--info`/`.c-modal` + `.c-backdrop` per UI-SPEC Surface 2 (M-3, M-4, M-5); preserve Phase 201 tenancy wiring (D-21)</name>
  <read_first>
    - app/(markos)/settings/members/page.tsx (current 205 LOC — DOM tree to preserve per CONTEXT D-05)
    - app/(markos)/settings/members/page.module.css (post-Task 1 shape — confirm class names referenced)
    - 213.3-UI-SPEC.md "Members" Copywriting Contract (lines 219–232) + Component Primitive Reuse "Members" (lines 397–409)
    - 213.3-RESEARCH.md "Class-Rename Map: Surface 2 — Members" JSX changes (lines 391–392) + Sub-Component Extraction Recommendations table (lines 676–684)
    - 213.3-RESEARCH.md "Pattern 2 — `.c-notice` Composition" (lines 256–293)
    - styles/components.css v1.1.0 (.c-card, .c-input, .c-field, .c-field__label, .c-button*, .c-badge--info, .c-notice--info, .c-modal, .c-backdrop)
    - DESIGN.md "Forms" + "Components > Notice/Banner" + "Components > Modal"
    - .planning/phases/213.3-.../213.3-01-PLAN.md Task 2 (className-only rewrite + .c-notice JSX precedent)
    - test/auth/ + test/tenancy/ + test/tenant-auth/ (Phase 201 wiring tests — must stay green)
  </read_first>
  <files>app/(markos)/settings/members/page.tsx</files>
  <action>
**Update `app/(markos)/settings/members/page.tsx` per the Class-Rename Map JSX swaps (DOM tree + prop interface UNCHANGED per CONTEXT D-05).**

Apply the following className substitutions throughout the file:

| Existing JSX | New JSX | Token / primitive citation |
|--------------|---------|---------------------------|
| `<div className={styles.contentCard}>` | `<div className={`c-card ${styles.contentCard}`}>` | `.c-card` + local max-width override |
| `<h2 className={styles.heading}>Members</h2>` | `<h2>Members</h2>` | globals.css `<h2>` |
| `<h3 className={styles.subHeading}>Seat usage</h3>` | `<h4>Seat usage</h4>` | globals.css `<h4>` |
| `<p className={styles.subheading}>...</p>` | `<p className="t-lead">...</p>` | `.t-lead` utility |
| `<span className={styles.seatBarLabel}>5 of 10 seats used</span>` | `<span className="t-label-caps">5 of 10 seats used</span>` | `.t-label-caps` utility |
| `<div className={styles.seatBarTrack}>...</div>` | `<div className={styles.meterTrack}>...</div>` | renamed local class |
| `<div className={styles.seatBarFill} style={{ width: pct }} />` | `<div className={pct >= 90 ? `${styles.meterFill} ${styles['meterFill--error']}` : pct >= 70 ? `${styles.meterFill} ${styles['meterFill--warning']}` : styles.meterFill} style={{ width: `${pct}%` }} />` | state-aware fill modifier per AC M-2 |
| `<form className={styles.inviteForm}>` | (UNCHANGED — layout class retained, token-cited) | layout |
| `<div className={styles.field}>` | `<div className="c-field">` | `.c-field` primitive |
| `<label className={styles.label}>Email</label>` | `<label htmlFor="invite-email" className="c-field__label">Email</label>` | `.c-field__label` |
| `<input className={styles.input} ...>` | `<input id="invite-email" name="email" type="email" required className="c-input" />` | `.c-input` |
| `<select className={styles.input}>...</select>` | `<select className="c-input">...</select>` | `.c-input` (select-form variant) |
| `<button className={styles.sendInviteButton}>Invite</button>` | `<button type="submit" className="c-button c-button--primary">Invite member</button>` | `.c-button--primary` + verb+noun copy |
| `<button className={styles.removeButton}>Remove</button>` | `<button type="button" className="c-button c-button--destructive" onClick={...}>Remove</button>` | `.c-button--destructive` |
| `<button className={styles.resendButton}>Resend</button>` | `<button type="button" className="c-button c-button--tertiary" onClick={...}>Resend invite</button>` | `.c-button--tertiary` + verb+noun |
| `<button className={styles.revokeButton}>Revoke</button>` | `<button type="button" className="c-button c-button--destructive" onClick={...}>Revoke invite</button>` | `.c-button--destructive` + verb+noun |
| `<span className={styles.pendingBadge}>Pending</span>` | `<span className="c-badge c-badge--info">[info] Pending</span>` | `.c-badge--info` + bracketed glyph (M-4) |
| `<span className={styles.roleBadge}>Admin</span>` | `<span className="c-badge">Admin</span>` (or `.c-badge--info` for elevated roles) | `.c-badge` |
| Pending invites list (existing `.pendingList ul`) | `<div className="c-notice c-notice--info" role="status"><strong>[info]</strong>{" "}Invite pending. Resend or revoke if the recipient has not responded.</div>` | `.c-notice c-notice--info` (M-3) |

**M-5 — Member-remove confirm modal (NEW composition):**

Add a `.c-modal` + `.c-backdrop` confirm-dialog when the user clicks remove. Replace any existing inline confirm or browser `window.confirm()` with the canonical primitive composition:

```tsx
{showRemoveConfirm && memberToRemove && (
  <>
    <div className="c-backdrop" onClick={() => setShowRemoveConfirm(false)} aria-hidden="true" />
    <div className="c-modal" role="dialog" aria-labelledby="remove-confirm-heading" aria-modal="true">
      <h3 id="remove-confirm-heading">Remove {memberToRemove.name} from this workspace?</h3>
      <p>They will lose access immediately.</p>
      <div className={styles.actionRow}>
        <button type="button" className="c-button c-button--secondary" onClick={() => setShowRemoveConfirm(false)}>
          Cancel
        </button>
        <button type="button" className="c-button c-button--destructive" onClick={confirmRemove}>
          Remove member
        </button>
      </div>
    </div>
  </>
)}
```

KEY rules (M-5):
- Modal heading uses `<h3>` (not `<h4>`) for question-form modal pattern
- Copy: `"Remove {name} from this workspace?"` + body `"They will lose access immediately."` per UI-SPEC Copywriting Contract (no "Are you sure?")
- Cancel + Remove member buttons in `.actionRow` (locally-styled flex gap)
- `aria-labelledby` + `aria-modal="true"` for a11y
- Backdrop click closes modal (preserves user-cancel pattern)

**Empty state copy:** `"No members yet. Invite your first team member to get started."` per UI-SPEC Copywriting Contract.

**Banned-lexicon check:** Scan all visible copy strings. Reject `synergy|leverage|empower|unlock|transform|seamless|innovative|just (as softener)|Please|Are you sure`. NO exclamation. NO emoji.

**Sub-component extraction decision:** Inspect the per-row JSX (avatar + name + email + role badge + action buttons). If the `<tr>` body exceeds ~40 LOC of repetitive JSX with handlers, extract to `app/(markos)/_components/MemberRow.tsx` (follow `LoginCard.tsx` pattern from 213.2-01). If under 40 LOC, leave inline. Document the decision in the SUMMARY.md.

**Phase 201 wiring preservation (D-21):** Member invite handler, member remove handler, role-edit handler, RLS-scoped fetch — ALL unchanged. ONLY className strings + JSX shape (banner→notice, inline confirm→modal) change. Server actions / API routes unchanged.

After editing, run `npm run build` + `node --test test/auth/ test/tenancy/ test/tenant-auth/` — all MUST exit 0.
  </action>
  <verify>
    <automated>grep -qE 'c-notice c-notice--info' "app/(markos)/settings/members/page.tsx" && grep -qE '\[info\]' "app/(markos)/settings/members/page.tsx" && grep -qE 'c-badge c-badge--info' "app/(markos)/settings/members/page.tsx" && grep -qE 'c-modal' "app/(markos)/settings/members/page.tsx" && grep -qE 'c-backdrop' "app/(markos)/settings/members/page.tsx" && grep -qE 'c-button c-button--primary' "app/(markos)/settings/members/page.tsx" && grep -qE 'c-button c-button--destructive' "app/(markos)/settings/members/page.tsx" && grep -qE 'c-input' "app/(markos)/settings/members/page.tsx" && grep -qE 'c-field' "app/(markos)/settings/members/page.tsx" && ! grep -qE 'styles\.(pendingBadge|roleBadge|sendInviteButton|removeButton|resendButton|pendingList|toast)' "app/(markos)/settings/members/page.tsx" && ! grep -inE 'Please|Are you sure|synergy|leverage|empower|unlock|transform|seamless|innovative' "app/(markos)/settings/members/page.tsx" && npm run build && node --test test/auth/ test/tenancy/ test/tenant-auth/ && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **M-3 (`.c-notice c-notice--info` invite-pending):** `grep -cE 'c-notice c-notice--info' "app/(markos)/settings/members/page.tsx"` ≥1; `grep -cE '\[info\]' "app/(markos)/settings/members/page.tsx"` ≥1.
    - **M-4 (`.c-badge--info` pending badge):** `grep -cE 'c-badge c-badge--info' "app/(markos)/settings/members/page.tsx"` ≥1.
    - **M-5 (`.c-modal` + `.c-backdrop` remove-confirm):** `grep -cE 'c-modal' "app/(markos)/settings/members/page.tsx"` ≥1; `grep -cE 'c-backdrop' "app/(markos)/settings/members/page.tsx"` ≥1; `grep -cE '"Are you sure"' "app/(markos)/settings/members/page.tsx"` returns 0.
    - **Primitive composition (UI-SPEC Surface 2):** `grep -cE 'c-button c-button--primary' "app/(markos)/settings/members/page.tsx"` ≥1; `grep -cE 'c-button c-button--destructive' "app/(markos)/settings/members/page.tsx"` ≥1; `grep -cE 'c-input' "app/(markos)/settings/members/page.tsx"` ≥1; `grep -cE 'c-field' "app/(markos)/settings/members/page.tsx"` ≥1.
    - **No legacy class refs:** `grep -cE 'styles\.(pendingBadge|roleBadge|sendInviteButton|removeButton|resendButton|pendingList|toast|seatBarFill)' "app/(markos)/settings/members/page.tsx"` returns 0.
    - **Banned-lexicon = 0:** `grep -inE 'Please|Are you sure|synergy|leverage|empower|unlock|transform|seamless|innovative' "app/(markos)/settings/members/page.tsx"` returns 0.
    - **`npm run build` exits 0** (validates JSX + module.css together).
    - **Phase 201 wiring preserved:** `node --test test/auth/ test/tenancy/ test/tenant-auth/` exits 0.
  </acceptance_criteria>
  <done>`page.tsx` updated; `.c-notice c-notice--info` for invite-pending banner; `.c-badge--info` for pending badge; `.c-modal` + `.c-backdrop` for remove-confirm; primitives composed per UI-SPEC; Phase 201 wiring preserved; `npm run build` + Phase 201 test suites exit 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create app/(markos)/settings/members/page.stories.tsx with ≥5 named state stories (`Default`, `Filled`, `InvitePending`, `RoleEdit`, `Empty`) per UI-SPEC X-4 + Storybook State Coverage table</name>
  <read_first>
    - app/(markos)/settings/members/page.tsx (post-Task 2 shape — confirm prop interface for stories args)
    - 213.3-UI-SPEC.md "Storybook State Coverage" table line 578
    - 213.3-CONTEXT.md D-17
    - .planning/phases/213.3-.../213.3-01-PLAN.md Task 3 (existing-stories extension precedent)
    - .planning/phases/213.2-.../213.2-01-PLAN.md Task 2 Edit B (CSF3 5-state stories pattern)
    - app/(markos)/_components/RotationGraceBanner.stories.tsx (Phase 213.1 reference)
  </read_first>
  <files>app/(markos)/settings/members/page.stories.tsx</files>
  <action>
**Create new file `app/(markos)/settings/members/page.stories.tsx` with the following Storybook 8 CSF3 ≥5 named state stories per UI-SPEC X-4 + Storybook State Coverage table line 578:**

```tsx
import type { Meta, StoryObj } from '@storybook/react';
import MembersPage from './page';

const meta = {
  title: 'Settings/Members',
  component: MembersPage,
  parameters: {
    layout: 'fullscreen',
  },
} satisfies Meta<typeof MembersPage>;

export default meta;

type Story = StoryObj<typeof meta>;

const sampleMembers = [
  { id: '1', name: 'Ada Lovelace', email: 'ada@example.com', role: 'Admin', status: 'active' },
  { id: '2', name: 'Alan Turing', email: 'alan@example.com', role: 'Member', status: 'active' },
  { id: '3', name: 'Grace Hopper', email: 'grace@example.com', role: 'Member', status: 'active' },
];

export const Default: Story = {
  args: {
    members: sampleMembers,
    pendingInvites: [],
    seatUsage: { used: 3, total: 10 },
  },
};

export const Filled: Story = {
  args: {
    members: [
      ...sampleMembers,
      ...Array.from({ length: 5 }).map((_, i) => ({
        id: `${i + 4}`,
        name: `Member ${i + 4}`,
        email: `member${i + 4}@example.com`,
        role: i === 0 ? 'Admin' : 'Member',
        status: 'active',
      })),
    ],
    pendingInvites: [],
    seatUsage: { used: 8, total: 10 },
  },
  parameters: {
    docs: { description: { story: 'Seat bar at 80% — `.meterFill--warning` state per UI-SPEC AC M-2.' } },
  },
};

export const InvitePending: Story = {
  args: {
    members: sampleMembers,
    pendingInvites: [
      { id: 'inv_1', email: 'pending@example.com', invitedAt: '2026-04-25', role: 'Member' },
    ],
    seatUsage: { used: 4, total: 10 },
  },
  parameters: {
    docs: { description: { story: 'Renders `.c-notice c-notice--info` with `[info]` glyph per UI-SPEC AC M-3.' } },
  },
};

export const RoleEdit: Story = {
  args: {
    members: sampleMembers,
    pendingInvites: [],
    seatUsage: { used: 3, total: 10 },
    editingRoleFor: '2',
  },
  parameters: {
    docs: { description: { story: 'Role-edit dropdown active for one member; `.c-input` (select variant) composition.' } },
  },
};

export const Empty: Story = {
  args: {
    members: [],
    pendingInvites: [],
    seatUsage: { used: 0, total: 10 },
  },
  parameters: {
    docs: { description: { story: 'Empty state copy: "No members yet. Invite your first team member to get started."' } },
  },
};
```

KEY rules:
- 5 named state exports: `Default`, `Filled`, `InvitePending`, `RoleEdit`, `Empty` (matches UI-SPEC line 578 verbatim).
- `args` shape mirrors actual `MembersPage` prop interface (executor MUST inspect post-Task 2 file to confirm exact prop names — replace placeholder shape if the page is server-component-only and props need a wrapper).
- If `MembersPage` is a server component, may need to extract a presentational `MembersList` client subcomponent (mirror Plan 213.2-01 LoginCard pattern). Document decision in SUMMARY.
- DOES NOT set `parameters.theme: "legacy"`.

After editing, run `npm run build-storybook` — MUST exit 0.
  </action>
  <verify>
    <automated>test -f "app/(markos)/settings/members/page.stories.tsx" && grep -cE '^export const (Default|Filled|InvitePending|RoleEdit|Empty):' "app/(markos)/settings/members/page.stories.tsx" | awk '$1>=5 {exit 0} $1<5 {exit 1}' && ! grep -qE 'parameters\.theme.*"legacy"' "app/(markos)/settings/members/page.stories.tsx" && grep -q 'export default meta' "app/(markos)/settings/members/page.stories.tsx" && npm run build-storybook && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - **X-4 (≥5 named state stories):** `grep -cE '^export const (Default|Filled|InvitePending|RoleEdit|Empty):' "app/(markos)/settings/members/page.stories.tsx"` ≥5.
    - **CSF3 typed `meta`:** `grep -c 'export default meta' "app/(markos)/settings/members/page.stories.tsx"` ≥1.
    - **Default-not-legacy theme:** `grep -cE 'parameters\.theme.*"legacy"' "app/(markos)/settings/members/page.stories.tsx"` returns 0.
    - **`npm run build-storybook` exits 0.**
  </acceptance_criteria>
  <done>`page.stories.tsx` created with 5 CSF3 named state exports; `npm run build-storybook` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→members API | Untrusted invite/remove/role-edit actions cross via Phase 201 tenancy wiring |
| RLS guard→members fetch | Tenant-scope guard enforces tenancy isolation per Phase 201 contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-213.3-02-01 | Tampering | member invite/remove handlers | accept | Wiring preserved verbatim from Phase 201 contract |
| T-213.3-02-02 | Elevation of Privilege | role-edit handler | accept | RLS guard enforced server-side; redesign does not alter authz |
| T-213.3-02-03 | Spoofing | member-remove confirm | mitigate | `.c-modal` + typed name confirmation pattern preserved; copy explicitly states "They will lose access immediately" |
</threat_model>

<verification>
After all 3 tasks complete:

```bash
grep -nE '#[0-9a-fA-F]{3,8}' "app/(markos)/settings/members/page.module.css"  # 0
grep -cE 'c-notice c-notice--info' "app/(markos)/settings/members/page.tsx"  # >=1
grep -cE 'c-badge c-badge--info' "app/(markos)/settings/members/page.tsx"  # >=1
grep -cE 'c-modal' "app/(markos)/settings/members/page.tsx"  # >=1
grep -cE '^export const (Default|Filled|InvitePending|RoleEdit|Empty):' "app/(markos)/settings/members/page.stories.tsx"  # >=5
npm run build  # 0
npm run build-storybook  # 0
node --test test/auth/ test/tenancy/ test/tenant-auth/  # 0
node --test test/ui-a11y/213-1-chrome-a11y.test.js test/ui-a11y/213-2-auth-a11y.test.js  # 0
```
</verification>

<success_criteria>
- All `must_haves.truths` verifiable via the verification commands above.
- UI-SPEC AC M-1, M-2, M-3, M-4, M-5 close.
- Phase 201 tenancy wiring preserved (D-21).
- Phase 213.1 + 213.2 a11y suites continue to pass.
- DOM-tree + prop interface preserved (D-05).
- Atomic commit shape: `feat(213.3-02): redesign /settings/members surface to DESIGN.md v1.1.0 tokens + .c-notice/.c-modal/.c-badge composition + state-color seat bar + Storybook state coverage`.
</success_criteria>

<output>
After completion, create `213.3-02-SUMMARY.md` documenting:
- Files modified
- AC# closures (M-1..M-5)
- Verification command outputs
- Sub-component extraction decision (extracted MemberRow / kept inline)
- Hand-off to Plan 213.3-03 (sessions)
</output>
