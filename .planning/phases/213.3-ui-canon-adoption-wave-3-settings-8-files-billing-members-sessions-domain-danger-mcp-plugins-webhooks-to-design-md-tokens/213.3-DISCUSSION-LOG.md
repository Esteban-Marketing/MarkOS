# Phase 213.3: UI Canon Adoption Wave 3 — settings/* — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `213.3-CONTEXT.md` — this log preserves the alternatives considered.

**Date:** 2026-04-28
**Phase:** 213.3-ui-canon-adoption-wave-3-settings-8-files-billing-members-sessions-domain-danger-mcp-plugins-webhooks-to-design-md-tokens
**Mode:** `--auto` (no interactive grey-area questioning; recommended defaults selected per discuss-phase auto convention)
**Areas analyzed:** Visual contract source · Wave structure · Plan count · Form primitives · Container primitives · Notice/banner primitive (NEW v1.1.0) · Table strategy · State signals · Storybook coverage · Test strategy · Existing wiring preservation (Phase 200/201/202/203/204) · Sub-component extraction · Banned lexicon · Theme inheritance

---

## Visual contract source

| Option | Description | Selected |
|---|---|---|
| UI-SPEC.md generated next via `/gsd-ui-phase 213.3` | Mirrors 213.2 precedent — UI-SPEC binds AC table per surface | ✓ |
| Skip UI-SPEC; let plan-phase research drive design | Risks token drift; no per-surface AC table | |

**Auto-selected:** UI-SPEC.md generated next (mirrors 213.1 + 213.2 precedent)
**Reasoning:** UI-SPEC is the binding visual contract per CLAUDE.md "Visual Design Canon" + GSD workflow `gsd:ui-phase` skill. Settings surfaces are the densest in the app — without UI-SPEC.md AC table, tokens will drift across 17 files.

---

## Wave structure

| Option | Description | Selected |
|---|---|---|
| Single wave, 9 plans (1 per surface + 1 cross-cutting) | Mirrors 213.2 D-10 (5 plans) — surfaces independent, sequential commits | ✓ |
| Two waves: discovery (UI-SPEC + research) + implementation (8 plans) | Adds orchestration overhead; UI-SPEC is one artifact | |
| 17 plans (1 per file) | Atomic but excessive; modules + tsx commit together by convention | |
| 5 plans grouping by domain (commerce: billing+plugins; ops: mcp+webhooks; identity: members+sessions; tenancy: domain+danger; tests) | Hides per-surface verification; harder to verify acceptance gates | |

**Auto-selected:** Single wave, 9 plans
**Reasoning:** Mirrors 213.2 D-10 cadence (5 plans / 4 surfaces + 1 cross-cutting). 213.3 has 8 surfaces + 1 cross-cutting = 9 plans. Sequential per atomic-commit convention. Surfaces are mutually independent (no shared `.tsx` imports).

---

## Form primitives

| Option | Description | Selected |
|---|---|---|
| Compose `.c-input` + `.c-field` + `.c-button` from `styles/components.css` | Carry from 213.2 D-11/D-12; no bespoke form CSS | ✓ |
| Author tenant-scoped form CSS in modules | Violates D-08 (token-only) and 213.2 D-11 precedent | |

**Auto-selected:** Compose primitives (carry-forward)

---

## Notice/banner primitive (NEW for 213.3)

| Option | Description | Selected |
|---|---|---|
| Compose `<div class="c-notice c-notice--{state}">…</div>` directly | DESIGN.md v1.1.0 primitive landed in PR #2; settings = densest banner surface in app | ✓ |
| Local `.banner-warning` / `.alert-error` classes per module | Violates D-09b; `.c-notice` is canonical now; would create wave-4 cleanup debt | |
| Wrap notice composition in a per-surface React subcomponent | Premature abstraction — `.c-notice` JSX is 1-liner | |

**Auto-selected:** Compose `.c-notice` directly
**Reasoning:** This is the single biggest delta from Phases 213.1 + 213.2 (which had no `.c-notice` primitive yet). Settings surfaces are notice-heavy: billing alerts, sessions warnings, danger confirms, MCP cost meters, plugin warnings, webhook failures, domain grace, member invite-pending. Per gsd-ui-checker Rec D + 213.2 CONTEXT D-22, the amendment PR landing was the gating step to make this composition possible. Now landed via PR #2 → no inline workarounds.

---

## Container primitive

| Option | Description | Selected |
|---|---|---|
| `.c-card` (default, 8px radius) for distinct sections | Standard settings layout; mirrors most existing surfaces | ✓ |
| `.c-card--feature` (32px radius) for hero panels | Reserved for hero auth (213.2) + landing-page features per DESIGN.md | |
| Bespoke `.settings-section` class | Violates D-08 + token-only; primitive exists | |

**Auto-selected:** `.c-card` default

---

## Table strategy

| Option | Description | Selected |
|---|---|---|
| Vanilla `<table>` semantic + token-cited row dividers + `.c-badge--{state}` | Existing pattern in 213.x; no `.c-table` primitive defined | ✓ |
| Add `.c-table` primitive to DESIGN.md and consume here | Out of scope — DESIGN.md v1.1.0 just landed; new primitives = Phase 213.4+ | |
| Card-grid pattern (`.c-card--interactive` per row) | Worse density for settings/members + sessions + webhooks; harder to scan | |

**Auto-selected:** Vanilla `<table>` + tokens

---

## Storybook coverage

| Option | Description | Selected |
|---|---|---|
| 4–7 named state stories per surface (mirror 213.2) | 213.2 averaged ~5 named states per surface; consistent patter | ✓ |
| Single Default story per surface | Insufficient — surfaces have stateful variants (pending, error, etc.) | |
| Full state matrix (10+ stories) | Excessive; 4–7 covers acceptance gate without bloat | |

**Auto-selected:** 4–7 named state stories per surface
**Per-surface targets:** billing 3+, members 5, sessions 4, domain 5, danger 3, mcp 5, plugins 5, webhooks-list 6, webhooks-detail 6 = ~42 stories total

---

## Test strategy

| Option | Description | Selected |
|---|---|---|
| `test/ui-a11y/213-3-settings-a11y.test.js` grep-shape suite, ≥30 tests, ≥9 AC# mentions | Mirrors 213.2 a11y suite (47/47 pass; 21 AC mentions); proven shape | ✓ |
| Vitest migration of settings test suites | Out of scope; tracked at v4.0.0 test-stack-migration | |
| Single integration test rendering all surfaces | Doesn't verify per-surface acceptance criteria | |

**Auto-selected:** Grep-shape suite (mirror 213.2)

---

## Existing wiring preservation

| Option | Description | Selected |
|---|---|---|
| Preserve all Phase 200/200.1/201/202/203/204 wiring contracts; CSS-canon-only redesign | Standard 213.x precedent (213.1 preserved Phase 203 banner; 213.2 preserved Phase 200/201 auth) | ✓ |
| Bundle Phase 201 tenancy fixes into 213.3 | Scope expansion; tenancy fixes belong in 201.1 | |
| Defer Phase 203 webhook detail page to a separate phase | Splits the canonical-adoption boundary; webhooks-list and webhooks-detail are conceptually one surface | |

**Auto-selected:** Preserve all wiring (D-20 / D-21 / D-22 / D-23)

---

## Sub-component extraction

| Option | Description | Selected |
|---|---|---|
| Selective — only extract when JSX complexity warrants (per-plan decision) | Matches 213.2 precedent (LoginCard / ConsentCard / NavList extracted; signup + invite kept inline) | ✓ |
| Mandatory extraction for all 8 surfaces | Premature abstraction; many surfaces are <250 LOC `.tsx` | |
| No extractions; keep all logic in `page.tsx` | MCP (339 LOC) + webhooks-detail (813 LOC) likely benefit from extraction | |

**Auto-selected:** Selective per-plan
**Likely candidates:** `MCPToolList` (settings/mcp), `WebhookDeliveryTable` + `WebhookDetailHeader` (settings/webhooks/[sub_id]), `MemberRow` (settings/members). Decision deferred to plan-phase per 213.2 D-15 precedent.

---

## Banned lexicon

| Option | Description | Selected |
|---|---|---|
| Carry-forward 213.2 D-07 banned-lexicon list | Same brand voice; same enforcement gate | ✓ |
| Allow some marketing-style copy on settings/billing for upgrade prompts | Brand consistency violation; settings stays in markos register | |

**Auto-selected:** Carry-forward
**Banned terms:** synergy, leverage, empower, unlock, transform, revolutionize, supercharge, holistic, seamless, cutting-edge, innovative, game-changer, next-generation, world-class, best-in-class, reimagine, disrupt, just (as softener). No exclamation points.

---

## Theme inheritance

| Option | Description | Selected |
|---|---|---|
| Inherit `[data-theme="light"]` + forced-colors + reduced-motion via `app/tokens.css` | Carry from 213.1 D-9 / 213.2 D-9 | ✓ |
| Per-module light-theme override blocks | Violates D-08 token-only + duplicates work | |

**Auto-selected:** Global inheritance (no module overrides)

---

## Claude's Discretion

The following decisions are deferred to executor / plan-phase per usual 213.x convention:

- Exact ordering of class declarations within rewritten `.module.css` files
- Storybook story `args` shape (mirror 213.2 patterns)
- Whether MCP and webhooks-detail need sub-component extraction (decide during plan-phase)
- Loading skeleton styling (use `.c-card` with reduced-opacity content)
- Empty-state copy details (delegate to UI-SPEC.md)
- Whether `*.stories.tsx` colocate next to `page.tsx` (default: colocate, matches existing settings pattern)

---

## Deferred Ideas

(See `<deferred>` section in 213.3-CONTEXT.md for full list. Summary:)

- `settings/theme/` redesign — Phase 213.4
- DESIGN.md primitives beyond v1.1.0 (`.c-table`, `.c-empty-state`) — Phase 213.4+ amendment proposals
- Vitest migration — v4.0.0 test-stack-migration
- Light-mode Storybook toolbar — separate Storybook-config phase
- Marketing brand layer for billing copy — separate marketing-design phase
- MCP ecosystem-aware skill suggestions — Phase 216+
- Webhook full-secret display toggle — `WONTFIX` (security regression)
- Tenant-deletion grace period — Phase 201.x followup

---

*Discussion mode: `--auto` (no AskUserQuestion calls; recommended defaults selected and logged inline)*
*Auto-advance trigger: `--auto` flag detected → workflow auto-advances to `gsd:plan-phase 213.3 --auto` after commit (or to `gsd:ui-phase 213.3` first since this is a frontend phase)*
