---
status: partial
phase: 213.3-ui-canon-adoption-wave-3-settings
source: [213.3-VERIFICATION.md]
started: 2026-04-29T02:30:00Z
updated: 2026-04-29T02:30:00Z
---

## Current Test

[awaiting human testing — autonomous mode deferred per /gsd-autonomous --from 213.3 invocation]

## Tests

### 1. Visual fidelity audit — all 9 settings surfaces

expected: Dark Kernel Black `#0A0E14` surface, Protocol Mint `#00D9A3` signal <5%, JetBrains Mono headings, Inter body, flat cards with 1px borders, no gradients, no soft shadows. All notice banners show correct accent colors (amber=warning, red=error, blue=info, green=success). Run `npm run storybook` and review the 41 named state stories across 9 surface titles (Settings/Billing, Settings/Members, Settings/Sessions, Settings/Domain, Settings/Danger, Settings/MCP, Routes/Settings - Plugins, Settings/Webhooks/List, Settings/Webhooks/Detail).
result: [pending]

### 2. Interactive flows — browser smoke test (billing, members, sessions)

expected: In a running dev instance (`npm run dev`):
(a) Billing: hold-state notice renders amber `.c-notice c-notice--warning` with `[warn]` prefix; invoice table renders semantically.
(b) Members: invite-pending state shows `[info]` notice + `[info] Pending` badge; remove-member modal overlays with backdrop, confirms via destructive button.
(c) Sessions: current-session row shows pulsing `.c-status-dot--live` + `[ok] Active now`; revoke modal renders with `.c-button--destructive`.
result: [pending]

### 3. prefers-reduced-motion — c-status-dot freeze

expected: With OS-level "Reduce motion" enabled (System Settings → Accessibility → Display → Reduce motion on macOS, or equivalent on Windows/Linux), the `.c-status-dot--live` kernel-pulse animation freezes at full opacity (does not animate). Confirm on the sessions surface (current session row) and the mcp surface (live MCP server indicator).
result: [pending]

### 4. Light-theme cascade — webhooks detail filter chip contrast

expected: Add `data-theme="light"` to `<html>` in DevTools while viewing `/settings/webhooks/[sub_id]`. Inspect the active filter chip text color — should swap from raw `#00D9A3` (dark theme) to `#00805F` (light theme darker mint) per `--color-primary-text` theme-aware token. If the chip uses raw `var(--color-primary)` instead of `var(--color-primary-text)`, the light theme contrast will fail WCAG AA (1.6:1 vs required 4.5:1). The verifier flagged this as a potential issue at `webhooks/[sub_id]/page.module.css:169` — confirm whether the active filter chip is using the theme-aware token.
result: [pending]

### 5. Chromatic baseline capture

expected: Run `npm run chromatic` (or equivalent project Chromatic command) to capture initial visual regression snapshots for all 41 settings story variants. This baseline serves as the reference for Phase 213.4 visual regression detection. Document the Chromatic build URL in this file under Summary once captured.
result: [pending]

## Summary

total: 5
passed: 0
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps

(No gaps yet — automated verification passed 12/12. Items above are manual UAT only — they do NOT block phase completion in autonomous mode. Each item is independently runnable post-merge.)
