---
phase: 40-multi-discipline-orchestrator-retrieval
plan: 02
type: execute
wave: 2
depends_on:
  - 40-01
files_modified:
  - test/orchestrator-literacy.test.js
  - onboarding/backend/agents/orchestrator.cjs
autonomous: true
requirements:
  - LIT-04
  - LIT-05
  - LIT-06

must_haves:
  truths:
    - "The orchestrator queries the top 3 ranked disciplines in parallel instead of hardcoding Paid_Media"
    - "Each queried discipline performs the locked dual-query merge: one business-model-filtered query plus one universal query without business_model"
    - "Merged hits are deduped by metadata.doc_id with fallback to chunk_id or stable chunk hash, then capped by literacy.max_context_chunks defaulting to 6"
    - "Empty literacy retrieval still degrades to no standards_context instead of crashing or inventing content"
    - "Telemetry event literacy_retrieval_observed reports disciplines_queried, total_hits, pain_point_match_count, and context_tokens from the final capped context"
  artifacts:
    - path: "onboarding/backend/agents/orchestrator.cjs"
      provides: "Multi-discipline dual-query retrieval pipeline with explicit context budget enforcement"
      exports: ["orchestrate"]
    - path: "test/orchestrator-literacy.test.js"
      provides: "Integration coverage for top-3 routing, dual-query dedupe, empty fallback, config-driven chunk cap, and telemetry payload"
  key_links:
    - from: "onboarding/backend/agents/orchestrator.cjs"
      to: "onboarding/backend/agents/discipline-router.cjs"
      via: "rankDisciplines(seed).slice(0, 3)"
      pattern: "rankDisciplines\\(seed\\).*slice\\(0, 3\\)"
    - from: "onboarding/backend/agents/orchestrator.cjs"
      to: "onboarding/backend/vector-store-client.cjs"
      via: "two getLiteracyContext() calls per discipline followed by doc_id-first dedupe"
      pattern: "Promise\\.all|getLiteracyContext|doc_id"
    - from: "onboarding/backend/agents/orchestrator.cjs"
      to: ".planning/config.json"
      via: "literacy.max_context_chunks fallback to 6"
      pattern: "max_context_chunks|Buffer\\.byteLength"
---

<objective>
Refactor the onboarding orchestrator to perform ranked multi-discipline retrieval with dual-query merge, dedupe, budget trim, and telemetry while preserving the current empty-context degradation path.

Purpose: This is the runtime behavior change that fulfills Phase 40. It consumes the Wave 1 router/filter contracts and enforces the fixed context budget without introducing a tokenizer dependency.
Output: A tested orchestrator retrieval block that queries top-3 disciplines, merges and trims results, and emits the new literacy retrieval telemetry event.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-multi-discipline-orchestrator-retrieval/40-CONTEXT.md
@.planning/phases/40-multi-discipline-orchestrator-retrieval/40-RESEARCH.md
@.planning/phases/40-multi-discipline-orchestrator-retrieval/40-VALIDATION.md
@.planning/config.json
@onboarding/backend/agents/orchestrator.cjs
@onboarding/backend/agents/discipline-router.cjs
@onboarding/backend/vector-store-client.cjs
@test/setup.js
</context>

<interfaces>
From onboarding/backend/agents/discipline-router.cjs after Plan 40-01:
```javascript
function rankDisciplines(seed) => string[]
```

From onboarding/backend/vector-store-client.cjs after Plan 40-01:
```javascript
async function getLiteracyContext(discipline, query = 'summary', filters = {}, topK = 5) {
  return [{ id, text, metadata, score }]
}
```

From onboarding/backend/agents/orchestrator.cjs current state:
```javascript
const discipline = 'Paid_Media';
literacyContextHits = await vectorStore.getLiteracyContext(
  discipline,
  `${seed?.product?.name || ''} ${seed?.audience?.segment_name || ''}`.trim() || 'summary',
  { business_model: businessModel || null },
  3
);
telemetry.capture('literacy_context_observed', { ... });
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 orchestrator retrieval integration tests</name>
  <files>test/orchestrator-literacy.test.js</files>
  <read_first>
    - .planning/phases/40-multi-discipline-orchestrator-retrieval/40-CONTEXT.md
    - .planning/phases/40-multi-discipline-orchestrator-retrieval/40-RESEARCH.md
    - .planning/phases/40-multi-discipline-orchestrator-retrieval/40-VALIDATION.md
    - onboarding/backend/agents/orchestrator.cjs
    - onboarding/backend/agents/discipline-router.cjs
    - onboarding/backend/vector-store-client.cjs
    - test/setup.js
  </read_first>
  <acceptance_criteria>
    - `test/orchestrator-literacy.test.js` contains the exact test names `orchestrate queries top three ranked disciplines with dual-query merge and doc_id dedupe`, `orchestrate caps standards_context by literacy.max_context_chunks with default 6`, and `orchestrate leaves standards_context empty when literacy retrieval returns zero hits`.
    - The test file asserts telemetry event name `literacy_retrieval_observed` and payload keys `disciplines_queried`, `total_hits`, `pain_point_match_count`, and `context_tokens`.
    - The test file asserts six `getLiteracyContext` calls for three disciplines and two query variants per discipline.
  </acceptance_criteria>
  <action>
Create `test/orchestrator-literacy.test.js` as a focused integration suite using `withMockedModule()` to mock `./mir-filler.cjs`, `./msp-filler.cjs`, `../vector-store-client.cjs`, `./telemetry.cjs`, and `./llm-adapter.cjs` around `onboarding/backend/agents/orchestrator.cjs`.

Add these exact cases:

1. `orchestrate queries top three ranked disciplines with dual-query merge and doc_id dedupe`
   - Mock `rankDisciplines(seed)` to return `['Paid_Media', 'Content_SEO', 'Lifecycle_Email', 'Social']`.
   - Mock `getLiteracyContext()` so each of the first three disciplines is called twice: one call with `{ business_model: 'SaaS', pain_point_tags: [...] }` and one call with `{ pain_point_tags: [...] }`.
   - Return overlapping hits that share `metadata.doc_id: 'DOC-001'` across filtered/universal queries and assert the higher-score copy survives.
   - Assert `drafts.standards_context` joins only deduped hits.

2. `orchestrate caps standards_context by literacy.max_context_chunks with default 6`
   - Use more than 6 merged hits and assert the joined context contains exactly 6 chunk texts when no config override is present.
   - In the same file, add a subcase or second assertion path that mocks `.planning/config.json` to `{ literacy: { max_context_chunks: 4 } }` and asserts the cap becomes 4.

3. `orchestrate leaves standards_context empty when literacy retrieval returns zero hits`
   - Mock all literacy queries to return `[]`.
   - Assert `drafts.standards_context` is `undefined` and no error is thrown.
   - Assert `literacy_retrieval_observed` still fires with `total_hits: 0`, `pain_point_match_count: 0`, and `context_tokens: 0`.

Keep this suite isolated to literacy retrieval behavior only. Do not duplicate the broader onboarding-server tests.
  </action>
  <verify>
    <automated>MISSING — Task 2 in this plan must make `node --test test/orchestrator-literacy.test.js` pass.</automated>
  </verify>
  <done>
    The dedicated orchestrator retrieval suite exists and encodes the exact top-3, dual-query, cap, and telemetry behavior that Task 2 must satisfy.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the hardcoded literacy fetch with ranked dual-query orchestration</name>
  <files>onboarding/backend/agents/orchestrator.cjs</files>
  <read_first>
    - .planning/phases/40-multi-discipline-orchestrator-retrieval/40-CONTEXT.md
    - .planning/phases/40-multi-discipline-orchestrator-retrieval/40-RESEARCH.md
    - onboarding/backend/agents/orchestrator.cjs
    - onboarding/backend/agents/discipline-router.cjs
    - onboarding/backend/vector-store-client.cjs
    - .planning/config.json
    - test/orchestrator-literacy.test.js
  </read_first>
  <acceptance_criteria>
    - `onboarding/backend/agents/orchestrator.cjs` imports `discipline-router.cjs` and uses `rankDisciplines(seed).slice(0, 3)`.
    - `onboarding/backend/agents/orchestrator.cjs` contains `Promise.all(` over the selected disciplines and makes two `getLiteracyContext(` calls per discipline.
    - `onboarding/backend/agents/orchestrator.cjs` dedupes by `metadata.doc_id` first, then `metadata.chunk_id`, then a stable hash key.
    - `onboarding/backend/agents/orchestrator.cjs` reads `.planning/config.json` and falls back to `6` when `literacy.max_context_chunks` is absent or invalid.
    - `onboarding/backend/agents/orchestrator.cjs` emits `telemetry.capture('literacy_retrieval_observed', ...)` and computes `context_tokens` with `Math.ceil(Buffer.byteLength(text, 'utf8') / 4)`.
    - `node --test test/orchestrator-literacy.test.js` exits with code 0.
  </acceptance_criteria>
  <action>
Refactor the literacy retrieval block in `onboarding/backend/agents/orchestrator.cjs` to implement D-40-01 through D-40-04 exactly.

1. Import the router module and determine the queried disciplines with `rankDisciplines(seed).slice(0, 3)`.
2. Build the literacy query string from `seed.product.name`, `seed.audience.segment_name`, and every entry in `seed.audience.pain_points`, joined with spaces and falling back to `summary`.
3. Read `.planning/config.json` once with a small helper. Use `literacy.max_context_chunks` when it is a positive integer; otherwise default to `6`.
4. For each discipline, run two retrieval calls in parallel with `Promise.all()`:
   - filtered query: `{ business_model: seed.company?.business_model || null, pain_point_tags: matchedParentTags }`
   - universal query: `{ pain_point_tags: matchedParentTags }`
5. Derive `matchedParentTags` from the same seed pain-point phrases used by the router. Keep the tag list limited to parent tags only so it matches the Phase 39 routing contract.
6. Normalize every returned hit to `{ discipline, id, text, metadata, score, pain_point_match_count, effective_score }`, where `pain_point_match_count` is the count of overlapping tags between `metadata.pain_point_tags` and `matchedParentTags`, and `effective_score` is `score + Math.min(0.3, pain_point_match_count * 0.1)`.
7. Dedupe per-discipline and then globally using this exact key priority:
   - `metadata.doc_id`
   - else `metadata.chunk_id`
   - else `crypto.createHash('sha1').update(text, 'utf8').digest('hex')`
   Keep the higher `effective_score` copy for duplicate keys.
8. Sort global hits by `effective_score` descending, take `max_context_chunks`, and assign `drafts.standards_context = mergedHits.map((entry) => entry.text).join('\n\n')` only when the capped array is non-empty.
9. Replace the old `literacy_context_observed` event with `literacy_retrieval_observed`, sending:
   - `disciplines_queried`: the 3 queried discipline names
   - `total_hits`: deduped global hit count before the final cap
   - `pain_point_match_count`: number of deduped global hits whose `pain_point_match_count` is greater than 0 before the final cap
   - `context_tokens`: `Math.ceil(Buffer.byteLength(standardsContext || '', 'utf8') / 4)`
10. Preserve backward compatibility: if all queries return zero hits or retrieval throws, leave `drafts.standards_context` unset exactly as today and continue orchestration.

Do not add new dependencies. Use built-in `fs`, `path`, and `crypto` only.
  </action>
  <verify>
    <automated>node --test test/orchestrator-literacy.test.js</automated>
  </verify>
  <done>
    The orchestrator now performs ranked multi-discipline retrieval, enforces the fixed context budget, emits the new telemetry event, and preserves the empty-context degradation path.
  </done>
</task>

</tasks>

<verification>
Run `node --test test/orchestrator-literacy.test.js` after Task 2, then run `node --test test/discipline-router.test.js test/vector-store-client.test.js test/orchestrator-literacy.test.js` as the phase-level focused suite before handing off for execution.
</verification>

<success_criteria>
The hardcoded single-discipline literacy fetch is removed, the orchestrator now issues three parallel discipline pairs with dual-query merge and doc_id-first dedupe, and the final standards context is capped by config/default while telemetry reports the actual capped payload size.
</success_criteria>

<output>
After completion, create `.planning/phases/40-multi-discipline-orchestrator-retrieval/40-02-SUMMARY.md`
</output>
