---
phase: 40-multi-discipline-orchestrator-retrieval
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/discipline-router.test.js
  - test/vector-store-client.test.js
  - onboarding/backend/agents/discipline-router.cjs
  - onboarding/backend/vector-store-client.cjs
autonomous: true
requirements:
  - LIT-04
  - LIT-05

must_haves:
  truths:
    - "rankDisciplines(seed) returns a deterministic ordered list using content.active_channels first, then audience.pain_points boosts, and always fills to the default top-3 fallback when signals are empty"
    - "The router does not crash when .agent/markos/literacy/taxonomy.json is missing in the live repo and instead uses an explicit compatibility fallback"
    - "buildLiteracyFilter() supports OR-style pain_point_tags matching and remains exportable for orchestrator use"
    - "getLiteracyContext() returns enough metadata for later doc_id-first dedupe with fallback keys"
    - "Router and vector filter contracts are covered by focused node:test suites"
  artifacts:
    - path: "onboarding/backend/agents/discipline-router.cjs"
      provides: "Pure routing module for multi-discipline retrieval"
      exports: ["rankDisciplines"]
    - path: "onboarding/backend/vector-store-client.cjs"
      provides: "Exported pain-point-aware literacy filter and retrieval metadata contract"
      exports: ["buildLiteracyFilter", "getLiteracyContext", "upsertLiteracyChunk"]
    - path: "test/discipline-router.test.js"
      provides: "Deterministic router coverage for channel mapping, pain-point boosting, and empty-signal fallback"
    - path: "test/vector-store-client.test.js"
      provides: "Regression coverage for OR-style pain_point_tags filter support and export contract"
  key_links:
    - from: "onboarding/backend/agents/discipline-router.cjs"
      to: "onboarding/onboarding-seed.schema.json"
      via: "content.active_channels and audience.pain_points parsing"
      pattern: "active_channels|pain_points"
    - from: "onboarding/backend/agents/discipline-router.cjs"
      to: ".agent/markos/literacy/taxonomy.json"
      via: "optional file load with explicit fallback constant when missing"
      pattern: "taxonomy.json|DEFAULT_DISCIPLINE_FALLBACK"
    - from: "onboarding/backend/vector-store-client.cjs"
      to: "onboarding/backend/agents/orchestrator.cjs"
      via: "exported buildLiteracyFilter and getLiteracyContext metadata contract"
      pattern: "buildLiteracyFilter|metadata.doc_id|chunk_id"
---

<objective>
Establish the routing and filter contracts Phase 40 needs before touching the orchestrator: add Wave 0 tests, create the pure discipline router, and extend the vector client so pain-point-aware multi-discipline retrieval is possible against the repo as it exists today.

Purpose: Phase 39 runtime artifacts are not yet present in the live workspace, so this plan makes the dependency explicit by using an optional taxonomy artifact with a controlled fallback instead of silently assuming the file exists.
Output: Tested router and vector-store contracts that Wave 2 can consume without re-deciding routing, filter semantics, or missing-taxonomy behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-multi-discipline-orchestrator-retrieval/40-CONTEXT.md
@.planning/phases/40-multi-discipline-orchestrator-retrieval/40-RESEARCH.md
@.planning/phases/40-multi-discipline-orchestrator-retrieval/40-VALIDATION.md
@.planning/phases/39-pain-points-first-content-corpus/39-CONTEXT.md
@.planning/milestones/v3.0-LITERACY-SYSTEM-ROADMAP.md
@onboarding/backend/vector-store-client.cjs
@onboarding/onboarding-seed.schema.json
@test/vector-store-client.test.js
@test/setup.js
</context>

<interfaces>
From onboarding/backend/vector-store-client.cjs:
```javascript
function buildStandardsNamespaceName(discipline)
function buildLiteracyFilter(filters = {})
async function getLiteracyContext(discipline, query = 'summary', filters = {}, topK = 5)

module.exports = {
  buildStandardsNamespaceName,
  getLiteracyContext,
  upsertLiteracyChunk,
  supersedeLiteracyDoc,
}
```

From onboarding/onboarding-seed.schema.json:
```json
{
  "content": { "active_channels": ["string"] },
  "audience": { "pain_points": ["string"] },
  "company": { "business_model": "B2B|B2C|B2B2C|DTC|Marketplace|SaaS|Agents-aaS" }
}
```

From test/setup.js:
```javascript
async function withMockedModule(modulePath, mockedExports, run)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 router and vector-filter test scaffolding</name>
  <files>test/discipline-router.test.js, test/vector-store-client.test.js</files>
  <read_first>
    - .planning/phases/40-multi-discipline-orchestrator-retrieval/40-CONTEXT.md
    - .planning/phases/40-multi-discipline-orchestrator-retrieval/40-RESEARCH.md
    - onboarding/onboarding-seed.schema.json
    - onboarding/backend/vector-store-client.cjs
    - test/vector-store-client.test.js
    - test/setup.js
  </read_first>
  <acceptance_criteria>
    - `test/discipline-router.test.js` contains the exact test names `rankDisciplines maps active_channels into deterministic discipline order`, `rankDisciplines boosts taxonomy-matched pain points without using business_model`, and `rankDisciplines falls back to Paid_Media, Content_SEO, Lifecycle_Email when signals are empty`.
    - `test/vector-store-client.test.js` contains the exact test name `buildLiteracyFilter adds OR pain_point_tags clauses and stays exported`.
    - `test/discipline-router.test.js` imports `../onboarding/backend/agents/discipline-router.cjs` and uses `node:test` + `node:assert/strict`.
    - `test/vector-store-client.test.js` asserts the filter string contains `(pain_point_tags CONTAINS 'high_acquisition_cost' OR pain_point_tags CONTAINS 'low_conversions')`.
  </acceptance_criteria>
  <action>
Create `test/discipline-router.test.js` as a new focused unit suite using Node's built-in test runner and `test/setup.js` helpers only. Add these exact behavior cases per D-40-01 and D-40-04:

1. A seed with `content.active_channels: ['Google Ads', 'LinkedIn', 'Email']` returns the first three disciplines in this exact order: `['Paid_Media', 'Social', 'Lifecycle_Email']`.
2. A seed with no active channels and `audience.pain_points: ['high churn', 'retention drop']` ranks `Lifecycle_Email` first and still returns three disciplines total.
3. A seed with empty `active_channels` and empty `pain_points` returns exactly `['Paid_Media', 'Content_SEO', 'Lifecycle_Email']`.
4. A seed with `company.business_model: 'SaaS'` but no routing signals proves business model does not change the ranked order.

Extend `test/vector-store-client.test.js` with one new test that requires `buildLiteracyFilter` to be exported and to accept `pain_point_tags: ['high_acquisition_cost', 'low_conversions']`, producing a canonical filter string that preserves `status = 'canonical'` and appends an OR clause over both tags.

Do not add orchestrator tests in this plan. Keep this Wave 0 scope limited to the router and vector client contracts so the plan can finish green after Task 2.
  </action>
  <verify>
    <automated>MISSING — Task 2 in this plan must make `node --test test/discipline-router.test.js test/vector-store-client.test.js` pass.</automated>
  </verify>
  <done>
    Router and vector-filter test files exist with the exact behavior names and expected filter string assertions required for Task 2 implementation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the router and exported pain-point filter contract</name>
  <files>onboarding/backend/agents/discipline-router.cjs, onboarding/backend/vector-store-client.cjs</files>
  <read_first>
    - .planning/phases/40-multi-discipline-orchestrator-retrieval/40-CONTEXT.md
    - .planning/phases/40-multi-discipline-orchestrator-retrieval/40-RESEARCH.md
    - .planning/phases/39-pain-points-first-content-corpus/39-CONTEXT.md
    - onboarding/backend/vector-store-client.cjs
    - onboarding/onboarding-seed.schema.json
    - test/discipline-router.test.js
    - test/vector-store-client.test.js
  </read_first>
  <acceptance_criteria>
    - `onboarding/backend/agents/discipline-router.cjs` contains the exact fallback constant `['Paid_Media', 'Content_SEO', 'Lifecycle_Email']`.
    - `onboarding/backend/agents/discipline-router.cjs` reads `content.active_channels` and `audience.pain_points`, and does not read `company.business_model` inside ranking logic.
    - `onboarding/backend/vector-store-client.cjs` exports `buildLiteracyFilter` from `module.exports`.
    - `onboarding/backend/vector-store-client.cjs` builds OR-style `pain_point_tags CONTAINS '...'` clauses when `filters.pain_point_tags` is a non-empty array.
    - `onboarding/backend/vector-store-client.cjs` includes `id`, `metadata`, and `score` in `getLiteracyContext()` return objects so Wave 2 can dedupe by `metadata.doc_id` with fallback keys.
    - `node --test test/discipline-router.test.js test/vector-store-client.test.js` exits with code 0.
  </acceptance_criteria>
  <action>
Create `onboarding/backend/agents/discipline-router.cjs` as a pure CommonJS module. Export `rankDisciplines(seed)` and keep all logic deterministic. Implement these exact rules:

1. Normalize channel strings from `seed.content.active_channels` to lowercase.
2. Apply these exact channel-to-discipline aliases with a `+4` score per hit:
   - `email`, `newsletter`, `drip`, `crm` -> `Lifecycle_Email`
   - `seo`, `organic search`, `blog`, `content`, `search` -> `Content_SEO`
   - `google ads`, `meta ads`, `facebook ads`, `linkedin ads`, `tiktok ads`, `ppc`, `paid search`, `paid social` -> `Paid_Media`
   - `linkedin`, `instagram`, `tiktok`, `facebook`, `x`, `twitter`, `youtube`, `social` -> `Social`
   - `landing page`, `cro`, `conversion`, `form`, `website`, `webinar page` -> `Landing_Pages`
3. Load `.agent/markos/literacy/taxonomy.json` if it exists. If it does not exist, use an inline fallback parent-tag map matching the eight parent tags documented in `40-RESEARCH.md` and `39-01-PLAN.md`, and log one explicit warning instead of throwing.
4. Match free-text `seed.audience.pain_points` to parent pain tags using deterministic keyword buckets from `40-RESEARCH.md`, then add `+2` to each mapped discipline.
5. Use this exact tie-break order: `Paid_Media`, `Content_SEO`, `Lifecycle_Email`, `Social`, `Landing_Pages`.
6. Return up to 5 unique disciplines. If fewer than 3 disciplines score above zero, append defaults from `['Paid_Media', 'Content_SEO', 'Lifecycle_Email']` until the first 3 positions are filled. This implements D-40-04 and supersedes the stale single-discipline fallback text.

Extend `onboarding/backend/vector-store-client.cjs` so `buildLiteracyFilter(filters = {})` accepts `pain_point_tags: string[]` and appends a single parenthesized OR clause. Preserve existing `business_model`, `funnel_stage`, and `content_type` clauses. Export `buildLiteracyFilter` in `module.exports`. Update `getLiteracyContext()` to return objects shaped as `{ id, text, metadata, score }`, using `entry.id || null` for `id`.

Do not modify orchestrator behavior in this plan. The only runtime changes here are the router contract and vector filter/retrieval contract that Wave 2 will consume.
  </action>
  <verify>
    <automated>node --test test/discipline-router.test.js test/vector-store-client.test.js</automated>
  </verify>
  <done>
    The router and vector filter contracts are implemented, taxonomy-missing behavior is explicit instead of hidden, and the dedicated router/vector tests pass.
  </done>
</task>

</tasks>

<verification>
Run `node --test test/discipline-router.test.js test/vector-store-client.test.js` after Task 2. Do not advance to Wave 2 until both suites pass and the router fallback behavior is explicit in code.
</verification>

<success_criteria>
Phase 40 has a stable, tested contract for routing and pain-point filtering that does not assume Phase 39 files already exist on disk, while still preserving the declared dependency on the eventual taxonomy/corpus artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/40-multi-discipline-orchestrator-retrieval/40-01-SUMMARY.md`
</output>
