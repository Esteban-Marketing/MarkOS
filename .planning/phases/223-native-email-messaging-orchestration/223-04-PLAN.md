---
phase: 223
plan: "04"
type: execute
wave: 3
depends_on: ["223-01", "223-02"]
files_modified:
  - lib/markos/outbound/providers/resend-adapter.ts
  - lib/markos/outbound/providers/twilio-adapter.ts
  - lib/markos/outbound/providers/knock-adapter.ts
  - lib/markos/outbound/providers/in-app-adapter.ts
  - lib/markos/outbound/providers/resend-inbound.ts
  - lib/markos/outbound/events.ts
  - lib/markos/channels/events/emit.ts
  - lib/markos/channels/events/auto-reply-detect.ts
  - lib/markos/channels/events/commercial-signal-map.ts
  - lib/markos/channels/events/reply-flow.ts
  - api/webhooks/resend-events.js
  - api/webhooks/twilio-events.js
  - api/webhooks/knock-events.js
  - lib/markos/channels/programs/messaging-thread.ts
  - test/channels/providers/resend-adapter.test.ts
  - test/channels/providers/resend-inbound.test.ts
  - test/channels/providers/twilio-adapter.test.ts
  - test/channels/providers/knock-adapter.test.ts
  - test/channels/providers/in-app-adapter.test.ts
  - test/channels/events/emit.test.ts
  - test/channels/events/reply-continuity.test.ts
  - test/channels/events/auto-reply-filter.test.ts
  - test/channels/events/commercial-signal-map.test.ts
  - test/channels/webhooks/resend-events.test.ts
  - test/channels/webhooks/twilio-events.test.ts
  - test/channels/webhooks/knock-events.test.ts
  - .planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md
autonomous: true
requirements:
  - EML-04
  - MSG-01
  - MSG-04
  - MSG-05
  - CRM-01
  - CRM-02
  - CRM-03
  - CRM-04
  - QA-02
must_haves:
  truths:
    - All 4 provider adapters (resend, twilio, knock, in_app) implement the full base-adapter contract from 223-01: send + normalizeEvent + verifyWebhookSignature + getRateLimitState (D-08, D-38)
    - Knock adapter uses @knocklabs/node SDK; FCM/APNS abstracted by Knock; signed webhooks verified via HMAC SHA-256 (D-09)
    - in_app adapter writes to in_app_messages table; never calls external API (D-10)
    - Resend inbound: webhook payload contains METADATA only; reply handler MUST call Resend Received Emails API to fetch body BEFORE opt-out detection / sentiment classification (Pitfall 9 + A11)
    - Single fan-out emit() in lib/markos/channels/events/emit.ts called by every webhook normalizer; writes 5 destinations in ONE transaction (cdp_events + crm_activity + aggregate-counter + dispatch_events + setConsentState if consent-affecting). Fail-closed: any step fails -> full rollback (D-29)
    - Reply continuity flow: inbound reply -> lookupOrCreateThread by (tenantId, profileId, channel) -> set thread.current_status='reply_pending' -> append crm_activity row -> emit cdp_events -> create CRM task -> fire opt-out detector -> if matched, setConsentState (single writer rule D-12) (D-22, D-23)
    - Auto-reply detection (Pitfall 7): vacation responder + Auto-Submitted header + In-Reply-To + min body length filters BEFORE reply flow runs; auto-replies log only, no thread.reply_pending transition
    - commercial_signal mapping (D-29): open->interest, click->interest, reply (positive sentiment)->interest, reply (negative)->risk, bounce->risk, complaint->risk, unsubscribe->risk
    - Webhook signature verification mandatory on all 3 webhook endpoints (resend/twilio/knock); failure -> 401 + audit_log row event_type='webhook_signature_invalid' (D-38)
    - Tombstone propagation hook to suppression chain wires here (P221 D-24 -> propagateTombstoneToSuppressions from 223-02)
    - Push delivery failure (Knock 'delivery_failed' event) maps to bounce-handler.handleBounceEvent('hard') so suppression list is updated (Pitfall 8)
    - No raw provider secrets in MCP/API responses or logs (D-39)
  artifacts:
    - path: lib/markos/outbound/providers/resend-adapter.ts
      provides: extended createResendAdapter with verifyWebhookSignature (Resend signing-secret HMAC), getRateLimitState (return cached limits from last response), inbound parsing helper
    - path: lib/markos/outbound/providers/resend-inbound.ts
      provides: fetchInboundEmailBody(eventId, apiKey) calling Resend Received Emails API GET /emails/received/{id}; cache by event_id (Pitfall 9 + A11)
    - path: lib/markos/outbound/providers/twilio-adapter.ts
      provides: extended createTwilioAdapter with verifyWebhookSignature (X-Twilio-Signature HMAC), getRateLimitState
    - path: lib/markos/outbound/providers/knock-adapter.ts
      provides: createKnockAdapter using @knocklabs/node — send + normalizeEvent + verifyWebhookSignature (Knock HMAC) + getRateLimitState
    - path: lib/markos/outbound/providers/in-app-adapter.ts
      provides: createInAppAdapter — send writes in_app_messages row; normalizeEvent for 'message_read' webhook (operator UI page-load delivers); verifyWebhookSignature is no-op (internal)
    - path: lib/markos/outbound/events.ts
      provides: extended normalizeOutboundEventForLedger handling email.received (Resend inbound) + Knock push events; routes to fan-out emit()
    - path: lib/markos/channels/events/emit.ts
      provides: emitChannelEvent({ ctx }) — single fan-out function: db.transaction across 5 writes; commercial_signal mapped per D-29
    - path: lib/markos/channels/events/auto-reply-detect.ts
      provides: isLikelyAutoReply(email_message) — heuristics per Pitfall 7: Auto-Submitted header, common subject patterns (Out of Office | Auto-Reply | OOO | Vacation), min body length, In-Reply-To presence
    - path: lib/markos/channels/events/commercial-signal-map.ts
      provides: mapEventToCommercialSignal(eventType, sentiment?) -> 'interest' | 'risk' | 'neutral' (D-29 mapping table; tested for all 9 event types)
    - path: api/webhooks/resend-events.js
      provides: extended handler — verifySignature -> if email.received fetch body via resend-inbound -> auto-reply filter -> normalizeOutboundEventForLedger -> emitChannelEvent
    - path: api/webhooks/twilio-events.js
      provides: extended handler — verifySignature -> normalizeOutboundEventForLedger -> emitChannelEvent
    - path: api/webhooks/knock-events.js
      provides: NEW handler — verify Knock signature -> normalizeOutboundEventForLedger -> emitChannelEvent; delivery_failed -> bounce-handler.handleBounceEvent('hard') (Pitfall 8)
    - path: lib/markos/channels/programs/messaging-thread.ts
      provides: lookupOrCreateThreadByProfileChannel({ tenantId, profileId, channel, sender_identity_id?, related_crm_id? }) — single thread per (profile, channel); for email, scoped per sender_identity per D-23; called by reply flow
  key_links:
    - from: api/webhooks/resend-events.js
      to: lib/markos/outbound/providers/resend-inbound.ts
      via: on email.received event, handler calls fetchInboundEmailBody(event.id, RESEND_API_KEY) BEFORE auto-reply filter or opt-out regex
    - from: lib/markos/channels/events/emit.ts
      to: lib/markos/cdp/adapters/crm-projection.ts
      via: setConsentState (P221 single writer) called when event affects ConsentState (unsubscribe/opt-out/complaint)
    - from: lib/markos/channels/events/emit.ts
      to: lib/markos/crm360/* (P222)
      via: emitCrmEventEnvelope writes crm_activity + cdp_events in transaction; commercial_signal mapped per D-29
    - from: lib/markos/channels/events/emit.ts
      to: supabase/migrations/115_channel_programs.sql
      via: aggregate counter UPDATE on email_campaigns OR messaging_threads
    - from: api/webhooks/knock-events.js
      to: lib/markos/channels/suppression/bounce-handler.ts (Wave 2 output)
      via: Knock delivery_failed -> handleBounceEvent({ severity: 'hard' }) (Pitfall 8)
---

<objective>
Wave 3 (parallel with 223-03). Provider adapters complete the contract from Wave 1 (resend + twilio + knock + in_app), inbound parse handles Resend's metadata-only webhook (A11/Pitfall 9), webhook normalization extends events.ts for email replies + Knock push events, the single fan-out emit() function (D-29) fans every event to 5 destinations transactionally, the reply continuity flow connects inbound -> MessagingThread -> Customer360 -> CRM task -> opt-out detector, auto-reply detection (Pitfall 7) prevents vacation responders from triggering CRM tasks, and Knock delivery failures cascade into the suppression chain (Pitfall 8).

Purpose: satisfies EML-04 (reply intelligence: inbound parse + thread + task + opt-out), MSG-01 (5 channels via correct adapter), MSG-04 (webhook normalization + reply flow + escalation), MSG-05 (Knock push: FCM + APNS via Knock + signed webhooks), CRM-01..04 (timeline writeback + Customer360 task creation per D-29), QA-02 (webhook signature verification audit).

Output: 4 provider adapter files (2 extended + 2 new), 1 Resend inbound helper, extended events.ts, 3 new lib/markos/channels/events files, 3 webhook handlers (2 extended + 1 new), 1 messaging-thread program extension, 12 test files.

**Note:** Runs in parallel with 223-03. Both depend ONLY on 223-01. No file conflicts: this plan touches `lib/markos/outbound/providers/*` + `lib/markos/channels/events/*` + `api/webhooks/*` + `lib/markos/channels/programs/messaging-thread.ts`; 223-03 touches `lib/markos/channels/templates/*` + `lib/markos/crm/copilot.ts`.
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md
@.planning/phases/223-native-email-messaging-orchestration/223-RESEARCH.md
@.planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md
@.planning/phases/223-native-email-messaging-orchestration/223-01-SUMMARY.md
@.planning/phases/221-cdp-identity-audience-consent-substrate/221-CONTEXT.md
@.planning/phases/222-crm-timeline-commercial-memory-workspace/222-CONTEXT.md
@.planning/phases/210-connector-wow-loop-and-recovery/210-CONTEXT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@obsidian/work/incoming/19-EMAIL-ENGINE.md
# informational only — decisions already locked in 223-CONTEXT.md per CLAUDE.md §source-of-truth precedence
@obsidian/work/incoming/21-MESSAGING-ENGINE.md
# informational only — decisions already locked in 223-CONTEXT.md per CLAUDE.md §source-of-truth precedence
@obsidian/reference/Core Lib.md
@obsidian/reference/HTTP Layer.md
@lib/markos/outbound/providers/base-adapter.ts
@lib/markos/outbound/providers/resend-adapter.ts
@lib/markos/outbound/providers/twilio-adapter.ts
@lib/markos/outbound/events.ts
@lib/markos/outbound/conversations.ts
@api/webhooks/resend-events.js
@api/webhooks/twilio-events.js
@lib/markos/channels/programs/messaging-thread.ts
@supabase/migrations/115_channel_programs.sql
@supabase/migrations/118_channel_in_app_messages.sql
@package.json
</context>

<interfaces>
<!-- Knock SDK (verify via Context7 MCP at execute time) -->
```typescript
// @knocklabs/node ^1.x
import Knock from '@knocklabs/node';
const knock = new Knock(process.env.KNOCK_API_KEY!);

// Send: knock.workflows.trigger('push_dispatch', { recipients, data });
// Webhook signature verification: knock.signatureVerifier.verify(payload, signature);
```

<!-- Resend Received Emails API (Pitfall 9 + A11) -->
```
GET https://api.resend.com/emails/received/{id}
Authorization: Bearer ${RESEND_API_KEY}

Response:
{
  "id": "...",
  "from": "...",
  "to": [...],
  "subject": "...",
  "html": "<...>",
  "text": "...",
  "headers": { "auto-submitted": "auto-replied", ... }
}
```

<!-- Single fan-out emit() (D-29) -->
```typescript
type ChannelEventContext = {
  tenantId: string;
  profileId: string;
  channel: 'email' | 'sms' | 'whatsapp' | 'push' | 'in_app';
  campaignId?: string;
  threadId?: string;
  journeyRunId?: string;
  dispatchAttemptId: string;
  eventType: 'queued' | 'delivered' | 'opened' | 'clicked' | 'replied' | 'bounced_soft' | 'bounced_hard' | 'complained' | 'unsubscribed' | 'failed';
  sentiment?: 'positive' | 'neutral' | 'negative';
  providerMessageId?: string;
  body?: string; // for replies
  occurredAt: string;
};
```

<!-- commercial_signal mapping (D-29) -->
```
open      -> interest
click     -> interest
replied + positive -> interest
replied + negative -> risk
replied + neutral  -> neutral
bounced_*          -> risk
complained         -> risk
unsubscribed       -> risk
queued / delivered -> neutral (not user-driven)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend resend + twilio adapters; build knock adapter (using @knocklabs/node) and in_app adapter; ship Resend inbound body-fetch helper</name>
  <read_first>
    - lib/markos/outbound/providers/base-adapter.ts (Wave 1 — validateAdapterContract requires send + normalizeEvent + verifyWebhookSignature + getRateLimitState)
    - lib/markos/outbound/providers/resend-adapter.ts (current shape — preserve send + normalizeEvent)
    - lib/markos/outbound/providers/twilio-adapter.ts (current shape — preserve)
    - .planning/phases/223-native-email-messaging-orchestration/223-RESEARCH.md (§Push Provider Recommendation §Pitfalls 8+9 §Vercel Queues §Don't Hand-Roll)
    - .planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md (D-08, D-09, D-10, D-22, D-38)
    - obsidian/work/incoming/21-MESSAGING-ENGINE.md (push provider rules)
    - test/fixtures/channels/in-app-message.ts (Wave 1)
    - package.json (verify @knocklabs/node + handlebars deps installed in 223-01)
  </read_first>
  <behavior>
    - Test 1 (resend extended contract): createResendAdapter() returned object satisfies validateAdapterContract — send + normalizeEvent + verifyWebhookSignature + getRateLimitState all present.
    - Test 2 (resend signature verify): verifyWebhookSignature(rawBody, header['svix-signature'], RESEND_WEBHOOK_SECRET) returns true on valid signature; tampered body returns false.
    - Test 3 (resend inbound body fetch — Pitfall 9): fetchInboundEmailBody(eventId, apiKey) calls GET https://api.resend.com/emails/received/{eventId}; returns { from, to, subject, html, text, headers }. Mock fetch in test.
    - Test 4 (twilio extended contract): createTwilioAdapter() satisfies validateAdapterContract.
    - Test 5 (twilio signature verify): verifyWebhookSignature uses X-Twilio-Signature HMAC scheme; valid -> true; missing -> false.
    - Test 6 (knock adapter contract): createKnockAdapter() satisfies validateAdapterContract.
    - Test 7 (knock send): adapter.send({ profileId, recipientToken, content }) calls knock.workflows.trigger('push_dispatch', ...) with recipients=[{ id: profileId }] and data; returns { provider: 'knock', channel: 'push', provider_message_id, status }.
    - Test 8 (knock signature verify): verifyWebhookSignature uses Knock HMAC; tampered -> false.
    - Test 9 (knock normalizeEvent): Knock 'message.delivered' webhook -> { event_type: 'delivered', dispatch_attempt_id: payload.metadata.dispatch_attempt_id, ... }; 'message.delivery_failed' (permanent) -> { event_type: 'bounced_hard' }; 'message.delivery_failed' (transient) -> { event_type: 'failed' }.
    - Test 10 (in_app adapter contract): createInAppAdapter() satisfies validateAdapterContract; verifyWebhookSignature returns true (no-op for internal).
    - Test 11 (in_app send): adapter.send({ profileId, content, threadId? }) INSERTs in_app_messages row (read_at=null); returns { provider: 'markos', channel: 'in_app', provider_message_id: message_id, status: 'delivered' }.
    - Test 12 (in_app normalizeEvent): on 'message_read' (web app sends back), returns { event_type: 'opened', ... }.
    - Test 13 (rate limit state): each adapter.getRateLimitState() returns { remaining, reset_at } shape; values are cached from last API response (or static fallback for in_app).
    - Test 14 (no raw secrets in serialization — D-39): adapter object's JSON.stringify and toString DO NOT contain process.env values; getReputationSnapshot (when present) returns { reputation_score, trend } only.
  </behavior>
  <action>
    1. Modify `lib/markos/outbound/providers/resend-adapter.ts`:
       - Preserve current `send`, `normalizeEvent`, factory `createResendAdapter`.
       - Add `verifyWebhookSignature(rawBody: string, signature: string, secret: string): boolean` using Resend's webhook signing scheme (Svix-style HMAC SHA-256 of `${msg.id}.${msg.timestamp}.${rawBody}`; verify timestamp within 5 minutes of now).
       - Add `getRateLimitState()` returning the most recent rate-limit state observed from a Resend API response (track in module-level cache; fallback `{ remaining: 100, reset_at: now+1h }`).
       - Update `normalizeEvent(payload)` to handle the new event type `email.received` (Resend inbound). Returns `{ ..., event_type: 'replied', body: payload.body || null }` — body fetched separately via task 1.2.
    2. Create `lib/markos/outbound/providers/resend-inbound.ts`:
       - Export `fetchInboundEmailBody(eventId, apiKey, opts = {})`:
         - GET https://api.resend.com/emails/received/{eventId} with Authorization: Bearer ${apiKey}.
         - Return { from, to, subject, html, text, headers }.
         - Cache by event_id in an in-memory LRU (avoid duplicate fetches on webhook retry — Pitfall 9).
         - Throw `ResendInboundFetchError` on non-200 (caller handles 401 -> audit_log).
    3. Modify `lib/markos/outbound/providers/twilio-adapter.ts`:
       - Add `verifyWebhookSignature(rawBody, twilioSignature, authToken, requestUrl)` using Twilio's official scheme (HMAC SHA1 of base URL + sorted form params; matches X-Twilio-Signature header).
       - Add `getRateLimitState()` returning Twilio rate-limit observed from last response.
    4. Create `lib/markos/outbound/providers/knock-adapter.ts`:
       - `createKnockAdapter(options = {})` returning object:
         - `provider: 'knock'`, `channels: ['push']`, `supports_inbound: false`.
         - `async send(message)`:
           - Validate ConsentState push_enabled BEFORE Knock call (call getConsentState; bail if opted_out — defensive layer; primary gate is recipient-handler).
           - Initialize Knock client lazily: `const knock = new Knock(process.env.KNOCK_API_KEY)`.
           - Call `knock.workflows.trigger('push_dispatch', { recipients: [{ id: message.profile_id }], data: { title, body, deeplink } })`.
           - Return `{ provider: 'knock', channel: 'push', provider_message_id: response.workflow_run_id, status: 'queued' }`.
         - `normalizeEvent(payload)`:
           - Map Knock event types:
             - 'message.delivered' -> { event_type: 'delivered', ... }
             - 'message.delivery_failed' WITH payload.error.code === 'INVALID_TOKEN' -> { event_type: 'bounced_hard' } (Pitfall 8)
             - 'message.delivery_failed' WITH transient code -> { event_type: 'failed' }
             - 'message.read' -> { event_type: 'opened' }
             - 'message.clicked' -> { event_type: 'clicked' }
           - Carry through dispatch_attempt_id from `payload.metadata.dispatch_attempt_id`.
         - `verifyWebhookSignature(rawBody, signature, secret)`:
           - HMAC SHA-256 of rawBody using secret; constant-time compare with provided signature header.
         - `getRateLimitState()`: reads from cache; fallback static.
    5. Create `lib/markos/outbound/providers/in-app-adapter.ts`:
       - `createInAppAdapter(options = {})`:
         - `provider: 'markos'`, `channels: ['in_app']`.
         - `async send(message)`:
           - INSERT INTO in_app_messages (tenant_id, profile_id, thread_id?, content, expires_at?) RETURNING message_id.
           - Return `{ provider: 'markos', channel: 'in_app', provider_message_id: message_id, status: 'delivered' }`.
         - `normalizeEvent(payload)`: handles `{ kind: 'message_read', message_id, profile_id }` -> { event_type: 'opened', dispatch_attempt_id: payload.dispatch_attempt_id, ... }.
         - `verifyWebhookSignature()`: returns true (internal — no external webhook).
         - `getRateLimitState()`: returns `{ remaining: Infinity, reset_at: null }`.
    6. Create test files:
       - `test/channels/providers/resend-adapter.test.ts` (Tests 1, 2, 13, 14).
       - `test/channels/providers/resend-inbound.test.ts` (Test 3 — mock fetch with msw or vi.fn()).
       - `test/channels/providers/twilio-adapter.test.ts` (Tests 4, 5, 13, 14).
       - `test/channels/providers/knock-adapter.test.ts` (Tests 6-9, 13, 14 — mock @knocklabs/node).
       - `test/channels/providers/in-app-adapter.test.ts` (Tests 10-13).
    7. Append VALIDATION rows.
  </action>
  <verify>
    <automated>vitest run test/channels/providers/</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/markos/outbound/providers/resend-adapter.ts` contains `verifyWebhookSignature` AND `getRateLimitState`
    - File `lib/markos/outbound/providers/resend-inbound.ts` exports `fetchInboundEmailBody` AND contains the URL `https://api.resend.com/emails/received/`
    - File `lib/markos/outbound/providers/twilio-adapter.ts` contains `verifyWebhookSignature` AND `X-Twilio-Signature`
    - File `lib/markos/outbound/providers/knock-adapter.ts` exists; contains `@knocklabs/node` import AND `INVALID_TOKEN` mapping (Pitfall 8) AND `provider: 'knock'`
    - File `lib/markos/outbound/providers/in-app-adapter.ts` exists; contains `in_app_messages` INSERT (grep)
    - `vitest run test/channels/providers/` exits 0 with ≥25 passing assertions across 5 adapter test files
    - All 4 adapter factories pass `validateAdapterContract` (test asserts no throw)
    - No raw secrets test (`JSON.stringify(adapter)` contains no `KNOCK_API_KEY` or `RESEND_API_KEY` value) passes
  </acceptance_criteria>
  <done>4 provider adapters complete contract; Resend inbound body-fetch helper handles Pitfall 9; Knock invalid-token cascade ready for bounce handler; D-39 secret hygiene verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Single fan-out emit() (D-29) + commercial_signal map + auto-reply detection (Pitfall 7) + reply continuity flow + thread lookupOrCreate</name>
  <read_first>
    - lib/markos/outbound/events.ts (current normalizeOutboundEventForLedger)
    - lib/markos/channels/programs/messaging-thread.ts (Wave 1 stub — implement lookupOrCreateThreadByProfileChannel)
    - lib/markos/cdp/adapters/crm-projection.ts (P221 — setConsentState single writer)
    - lib/markos/crm360/* (P222 — Customer360 + emitCrmEventEnvelope; A4 fail-fast if absent)
    - lib/markos/channels/suppression/bounce-handler.ts (Wave 2 — propagateTombstoneToSuppressions; handleBounceEvent + handleComplaintEvent)
    - .planning/phases/223-native-email-messaging-orchestration/223-RESEARCH.md (§Architecture Patterns 2 §Integration Contracts §Pitfalls 3+7)
    - .planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md (D-22, D-23, D-24, D-29)
    - obsidian/work/incoming/19-EMAIL-ENGINE.md (rule 6: replies are signals)
    - obsidian/work/incoming/21-MESSAGING-ENGINE.md (rule 7: every send must feed CRM/CDP/Analytics)
    - test/fixtures/channels/messaging-thread.ts
    - test/fixtures/channels/dispatch.ts
  </read_first>
  <behavior>
    - Test 1 (commercial_signal map): mapEventToCommercialSignal('open') -> 'interest'; ('clicked') -> 'interest'; ('replied','positive') -> 'interest'; ('replied','negative') -> 'risk'; ('bounced_hard') -> 'risk'; ('complained') -> 'risk'; ('unsubscribed') -> 'risk'; ('queued') -> 'neutral'; ('delivered') -> 'neutral'.
    - Test 2 (auto-reply detect — Pitfall 7): isLikelyAutoReply({ headers: { 'auto-submitted': 'auto-replied' } }) -> true. ({ subject: 'Out of Office: Re: Hello' }) -> true. ({ body: 'short.' }) -> true (body < 20 chars). Empty subject + missing In-Reply-To + body 'unsubscribe' -> false (real opt-out signal).
    - Test 3 (lookupOrCreateThread email scope): for channel='email', thread is keyed on (tenantId, profileId, channel, sender_identity_id) — separate threads per sender. Calling lookupOrCreate twice in a row returns same thread_id.
    - Test 4 (lookupOrCreateThread non-email): for channel IN ('whatsapp','sms','push','in_app'), thread is keyed on (tenantId, profileId, channel) only.
    - Test 5 (emit transactional happy path): emitChannelEvent({ delivered email event with campaign_id }) -> in single transaction: cdp_events row written (event_domain='email', source_event_ref); crm_activity row written (commercial_signal='neutral'); email_campaigns.delivered_count incremented; dispatch_events row written; no setConsentState call (delivered is not consent-affecting).
    - Test 6 (emit transactional consent-affecting): emitChannelEvent({ unsubscribed event }) -> 5 writes including setConsentState({ channel_status: 'opted_out', source_event_ref }) call.
    - Test 7 (emit fail-closed): when crm_activity insert fails (forced via mock), full transaction rollback — no cdp_events row, no aggregate counter increment, no setConsentState call.
    - Test 8 (reply continuity full flow): inbound email reply received with body 'thanks, looks great' (positive sentiment via simple regex/length-based heuristic for v1):
      1. lookupOrCreateThread by (tenantId, profileId, 'email', sender_identity_id) -> thread row.
      2. Set thread.current_status='reply_pending', last_message_at=now, last_direction='inbound', sentiment='positive'.
      3. emitChannelEvent({ event_type: 'replied', sentiment: 'positive', threadId, body }):
         - cdp_events row event_domain='email', event_name='email.reply_received'.
         - crm_activity row commercial_signal='interest'.
         - dispatch_events row event_type='replied'.
         - thread aggregate updated.
      4. CRM task created assigned to thread.owner_user_id (or fallback customer_360.primary_owner_user_id).
      5. Opt-out detector run on body — 'thanks, looks great' does NOT match -> no setConsentState call.
    - Test 9 (reply -> opt-out detection): inbound reply body 'STOP' or 'unsubscribe' -> opt-out detector matches -> setConsentState({ channel: 'email', channel_status: 'opted_out', source_event_ref: thread.thread_id }).
    - Test 10 (auto-reply skips reply flow): inbound reply with Auto-Submitted header -> isLikelyAutoReply=true -> NO thread.reply_pending transition, NO CRM task created, dispatch_events row written with event_type='replied' AND auto_reply: true metadata for analytics.
    - Test 11 (cross-channel thread continuity D-24): same profile sends sms reply, then whatsapp reply, then email reply (different sender) — three threads created; all share related_crm_id (Customer360 resolved by P222 getCustomer360ByProfileId).
  </behavior>
  <action>
    1. Create `lib/markos/channels/events/commercial-signal-map.ts`:
       - Export `mapEventToCommercialSignal(eventType, sentiment)` per the table above.
       - Document mapping table inline.
    2. Create `lib/markos/channels/events/auto-reply-detect.ts`:
       - Export `isLikelyAutoReply({ headers, subject, body, inReplyTo })`:
         - If headers['auto-submitted'] in ['auto-replied','auto-generated'] -> true.
         - If subject matches `/^(out of office|auto[-\s]?reply|ooo|vacation)/i` -> true.
         - If body && body.trim().length < 20 -> true (too short to be meaningful).
         - If !inReplyTo (no thread context) AND subject is generic (e.g., 'Re:' alone) -> true.
         - Else false.
    3. Implement `lib/markos/channels/programs/messaging-thread.ts::lookupOrCreateThreadByProfileChannel`:
       - For channel='email' AND sender_identity_id provided: SELECT WHERE (tenant_id, profile_id, channel, sender_identity_id is custom-derived from JSONB metadata or via a new column — but since D-03 doesn't add sender_identity_id to messaging_threads, store it in thread_metadata JSONB OR use related_crm_id contextual fallback). DECISION: add an optional `sender_identity_id` column via a tiny ALTER in migration 120 (already in Wave 2). [Verify migration 120 includes this; if not, add ALTER TABLE messaging_threads ADD COLUMN sender_identity_id UUID NULL in migration 120.]
       - Else: SELECT WHERE (tenant_id, profile_id, channel).
       - If found, return; else INSERT with current_status='open', last_direction=null, related_crm_id resolved via P222 getCustomer360ByProfileId.
       - Idempotency on race: use INSERT ... ON CONFLICT DO NOTHING, then SELECT.
    4. Create `lib/markos/channels/events/emit.ts`:
       - Export `emitChannelEvent(ctx)`:
         - Inside `db.transaction(async trx => {...})`:
           1. INSERT cdp_events row (event_domain = ctx.channel === 'email' ? 'email' : 'messaging', event_name = `${ctx.channel}.${ctx.eventType}`, source_event_ref = ctx.dispatchAttemptId, payload).
           2. INSERT crm_activity row (source_domain = ctx.channel, commercial_signal = mapEventToCommercialSignal(ctx.eventType, ctx.sentiment), related to thread or campaign).
           3. UPDATE aggregate counter on email_campaigns OR messaging_threads OR lifecycle_journey_runs as applicable.
           4. INSERT dispatch_events row (matching the eventType + dispatch_attempt_id; UNIQUE constraint dedups webhook replays per Pitfall 3).
           5. If ctx.eventType IN ('unsubscribed','complained'): call await setConsentState(ctx.tenantId, ctx.profileId, ctx.channel, { channel_status: 'opted_out', source_event_ref }). NOTE (Warning 4 single-detection contract): emit.ts MUST NOT scan body for opt-out keywords. Body scanning + opt-out detection is owned exclusively by handleInboundReply (reply-flow.ts), which sets ctx.eventType='unsubscribed' BEFORE calling emitChannelEvent. Acceptance: `grep "OPT_OUT_REGEX" lib/markos/channels/events/emit.ts` returns zero matches.
         - On any throw: transaction rolls back (no partial state — D-29 fail-closed).
       - Re-throw caught errors so webhook handler can audit + return 500 vs 200 per Pitfall 3.
    5. Extend `lib/markos/outbound/events.ts::normalizeOutboundEventForLedger`:
       - Add Knock provider branch: `provider === 'knock'` -> use createKnockAdapter().normalizeEvent(payload).
       - Add 'email.received' handling: when Resend webhook event_type === 'email.received', call fetchInboundEmailBody to populate body before opt-out regex.
       - Preserve existing email/sms/whatsapp paths.
       - Return shape extended with `auto_reply: boolean` field (set true when isLikelyAutoReply matched).
    6. Implement reply continuity orchestration as `lib/markos/channels/events/reply-flow.ts` (or extend events.ts). Export `handleInboundReply({ tenantId, profileId, channel, sender_identity_id?, body, headers, providerMessageId, dispatchAttemptId })`:
       - autoReply = isLikelyAutoReply({ headers, subject, body, inReplyTo: headers['in-reply-to'] }).
       - thread = lookupOrCreateThreadByProfileChannel({ tenantId, profileId, channel, sender_identity_id }).
       - sentiment = simple v1 classifier: 'positive' if body matches positive keywords (great|love|excited|interested|yes), 'negative' if matches (no|not interested|cancel|stop), else 'neutral'.
       - If !autoReply: setThreadStatus(thread.thread_id, 'reply_pending', { last_direction: 'inbound', last_message_at: now(), sentiment }); create CRM task assigned to thread.owner_user_id (fallback customer_360.primary_owner_user_id).
       - emitChannelEvent({ ..., eventType: 'replied', sentiment, threadId: thread.thread_id, body, auto_reply: autoReply }).
       - Opt-out detector (Warning 4 single-detection contract): const optOut = OPT_OUT_REGEX.test(body); if optOut, MUTATE the eventType passed to emitChannelEvent: `emitChannelEvent({ ..., eventType: 'unsubscribed', sentiment, threadId, body, auto_reply: autoReply })` (instead of 'replied'). The single fan-out emit() then routes the unsubscribed eventType through its consent-affecting branch (step 5 above) which calls setConsentState. emit.ts is NEVER allowed to scan body itself. OPT_OUT_REGEX is defined and used ONLY in reply-flow.ts.
    7. Create test files:
       - `test/channels/events/commercial-signal-map.test.ts` (Test 1).
       - `test/channels/events/auto-reply-filter.test.ts` (Test 2).
       - `test/channels/events/emit.test.ts` (Tests 5-7).
       - `test/channels/events/reply-continuity.test.ts` (Tests 3, 4, 8-11).
    8. Append VALIDATION rows.
  </action>
  <verify>
    <automated>vitest run test/channels/events/</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/markos/channels/events/commercial-signal-map.ts` exports `mapEventToCommercialSignal` (grep)
    - File `lib/markos/channels/events/auto-reply-detect.ts` exports `isLikelyAutoReply` AND contains `auto-submitted` (grep)
    - File `lib/markos/channels/events/emit.ts` exports `emitChannelEvent` AND contains `db.transaction` (grep)
    - File `lib/markos/channels/events/emit.ts` calls `setConsentState` (grep) inside the consent-affecting branch
    - File `lib/markos/channels/programs/messaging-thread.ts` exports `lookupOrCreateThreadByProfileChannel` AND contains `ON CONFLICT DO NOTHING`
    - File `lib/markos/channels/events/reply-flow.ts` (or events.ts extension) contains `isLikelyAutoReply` call BEFORE thread.reply_pending transition (grep order)
    - `vitest run test/channels/events/` exits 0 with ≥20 passing assertions including transactional happy path + fail-closed rollback
    - File `lib/markos/outbound/events.ts` contains `provider === 'knock'` branch
    - File `lib/markos/channels/events/emit.ts` does NOT contain `OPT_OUT_REGEX` (grep `OPT_OUT_REGEX` returns 0 matches in emit.ts) — Warning 4 single-detection contract
    - File `lib/markos/channels/events/reply-flow.ts` contains `OPT_OUT_REGEX` (grep returns ≥1) and is the SOLE owner of the opt-out detection regex
  </acceptance_criteria>
  <done>Single fan-out emit() transactional; auto-reply filter blocks vacation responders; reply continuity creates thread + CRM task + cdp_events + crm_activity; opt-out keyword triggers setConsentState single writer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Webhook handlers — extend resend-events.js + twilio-events.js + create knock-events.js with signature verification + audit on failure + Knock-failure -> bounce-handler propagation (Pitfall 8)</name>
  <read_first>
    - api/webhooks/resend-events.js (current handler)
    - api/webhooks/twilio-events.js (current handler)
    - api/webhooks/subscribe.js + unsubscribe.js (existing webhook handler patterns)
    - lib/markos/outbound/providers/resend-adapter.ts + twilio-adapter.ts + knock-adapter.ts (Task 1 outputs)
    - lib/markos/channels/events/emit.ts (Task 2 output)
    - lib/markos/channels/events/auto-reply-detect.ts (Task 2 output)
    - lib/markos/outbound/providers/resend-inbound.ts (Task 1 output)
    - lib/markos/channels/suppression/bounce-handler.ts (Wave 2 output)
    - .planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md (D-22, D-29, D-38, D-39)
    - .planning/phases/223-native-email-messaging-orchestration/223-RESEARCH.md (§Pitfalls 3+8 §Knock §Resend)
    - obsidian/reference/HTTP Layer.md (webhook auth conventions)
  </read_first>
  <behavior>
    - Test 1 (resend handler signature verify): valid Svix-style signature -> 200; tampered body -> 401 + audit_log row event_type='webhook_signature_invalid'.
    - Test 2 (resend handler email.delivered): valid event -> emitChannelEvent called with eventType='delivered'; campaign delivered_count incremented; dispatch_events row written.
    - Test 3 (resend handler email.received -> body fetch + auto-reply filter + reply flow):
      - On email.received, handler calls fetchInboundEmailBody(eventId, RESEND_API_KEY).
      - If body returned: handleInboundReply called.
      - If isLikelyAutoReply -> reply flow short-circuits (no CRM task, no thread.reply_pending), but dispatch_events row STILL written for analytics with auto_reply: true metadata.
    - Test 4 (resend handler webhook replay — Pitfall 3): same event_id submitted twice -> second insert hits dispatch_events UNIQUE (dispatch_attempt_id, event_type) constraint -> handler catches 23505 and returns 200 (NOT 500); no duplicate aggregate counter increment.
    - Test 5 (twilio handler signature verify): valid X-Twilio-Signature -> 200; missing -> 401 + audit_log.
    - Test 6 (twilio handler sms.received): valid inbound SMS -> handleInboundReply (channel='sms') -> thread lookupOrCreate + crm_activity + cdp_events.
    - Test 7 (knock handler signature verify): valid Knock HMAC -> 200; tampered -> 401 + audit_log event_type='webhook_signature_invalid'.
    - Test 8 (knock handler delivery_failed permanent — Pitfall 8): event 'message.delivery_failed' with error.code='INVALID_TOKEN' -> handleBounceEvent({ severity: 'hard', channel: 'push' }) -> channel_suppressions row with reason='hard_bounce'; cdp_events emitted.
    - Test 9 (knock handler delivery_failed transient): error.code='RATE_LIMITED' -> dispatch_events row event_type='failed' but NO suppression added.
    - Test 10 (no raw secrets in audit log — D-39): audit_log row from any webhook handler does NOT contain RESEND_WEBHOOK_SECRET, TWILIO_AUTH_TOKEN, or KNOCK_WEBHOOK_SECRET values; only sanitized metadata.
  </behavior>
  <action>
    1. Modify `api/webhooks/resend-events.js`:
       - On request: read raw body + svix-id + svix-timestamp + svix-signature headers.
       - Call `createResendAdapter().verifyWebhookSignature(rawBody, signature, process.env.RESEND_WEBHOOK_SECRET)`. If false -> append audit_log row event_type='webhook_signature_invalid' (sanitized: log only event-type + IP + timestamp; NO secret) -> return 401.
       - Parse JSON body. If event.type === 'email.received':
         - body = await fetchInboundEmailBody(event.data.id, process.env.RESEND_API_KEY).
         - autoReply = isLikelyAutoReply({ headers: body.headers, subject: body.subject, body: body.text, inReplyTo: body.headers['in-reply-to'] }).
         - profileId = await resolveProfileFromEmail(event.data.from); if missing -> log + 200 (orphan inbound).
         - dispatchAttemptId = derive from in-reply-to header OR generate v5 uuid keyed on (event.data.id).
         - await handleInboundReply({ tenantId, profileId, channel: 'email', sender_identity_id, body: body.text, headers: body.headers, providerMessageId: event.data.id, dispatchAttemptId }) — handles autoReply gating internally.
       - Else (delivered/opened/clicked/bounced/complained/unsubscribed/etc):
         - normalized = normalizeOutboundEventForLedger({ provider: 'resend', payload: event.data }).
         - try { await emitChannelEvent({ ...normalized, ... }); } catch (err) { if (err.code === '23505') return 200; throw; } (Pitfall 3).
       - Return 200.
    2. Modify `api/webhooks/twilio-events.js`:
       - On request: read raw form body + X-Twilio-Signature header.
       - Call `createTwilioAdapter().verifyWebhookSignature(rawBody, signature, process.env.TWILIO_AUTH_TOKEN, fullRequestUrl)`. If false -> 401 + audit row.
       - Parse Twilio webhook body. If MessageStatus or inbound MessageSid:
         - normalized = normalizeOutboundEventForLedger({ provider: 'twilio', payload }).
         - If inbound (direction === 'inbound'): handleInboundReply({ tenantId, profileId, channel: payload.From.startsWith('whatsapp:') ? 'whatsapp' : 'sms', body: payload.Body, ... }).
         - Else: emitChannelEvent.
       - Return 200.
    3. Create `api/webhooks/knock-events.js`:
       - On request: read raw body + X-Knock-Signature header.
       - Call `createKnockAdapter().verifyWebhookSignature(rawBody, signature, process.env.KNOCK_WEBHOOK_SECRET)`. If false -> 401 + audit row.
       - Parse JSON body. switch event.type:
         - 'message.delivered' -> emitChannelEvent({ eventType: 'delivered', channel: 'push', ... }).
         - 'message.delivery_failed':
           - if event.data.error.code === 'INVALID_TOKEN' (or other permanent codes per Knock docs): await handleBounceEvent({ severity: 'hard', tenantId, profileId, channel: 'push', dispatchAttemptId: event.data.metadata.dispatch_attempt_id }) (Pitfall 8). Then emitChannelEvent({ eventType: 'bounced_hard' }).
           - else: emitChannelEvent({ eventType: 'failed' }) (transient — no suppression).
         - 'message.read' -> emitChannelEvent({ eventType: 'opened', channel: 'push' }).
         - 'message.clicked' -> emitChannelEvent({ eventType: 'clicked', channel: 'push' }).
       - Return 200.
    4. Add (or extend) `lib/markos/audit/webhook-audit.ts` (or use existing audit_log writer) helper `auditWebhookSignatureFailure({ provider, ip, eventType })` that writes audit_log row WITHOUT any secret material per D-39.
    5. Create test files:
       - `test/channels/webhooks/resend-events.test.ts` (Tests 1-4 — signature verify + email.received + auto-reply gate + replay 23505 swallow).
       - `test/channels/webhooks/twilio-events.test.ts` (Tests 5, 6).
       - `test/channels/webhooks/knock-events.test.ts` (Tests 7-10 — incl Pitfall 8 INVALID_TOKEN propagation).
    6. Append VALIDATION rows.
  </action>
  <verify>
    <automated>vitest run test/channels/webhooks/ && npm test</automated>
  </verify>
  <acceptance_criteria>
    - File `api/webhooks/resend-events.js` contains `verifyWebhookSignature` call AND `fetchInboundEmailBody` call AND a `23505` (UNIQUE violation swallow) handler comment or branch
    - File `api/webhooks/twilio-events.js` contains `verifyWebhookSignature` call AND `X-Twilio-Signature` reference
    - File `api/webhooks/knock-events.js` exists; contains `INVALID_TOKEN` branch AND `handleBounceEvent` call AND `'message.delivery_failed'` case
    - All 3 handlers write audit_log on signature failure (grep `webhook_signature_invalid` in each file)
    - `vitest run test/channels/webhooks/` exits 0 with ≥10 assertions per provider
    - `npm test` (existing P104/P105 webhook regression) exits 0
    - No raw secret in audit_log: test asserts `audit_log_row.metadata` does NOT contain `RESEND_WEBHOOK_SECRET` or any env value
  </acceptance_criteria>
  <done>3 webhook handlers verify signatures, route inbound replies via auto-reply filter + body fetch, swallow replays per Pitfall 3, propagate Knock invalid-token to suppression chain per Pitfall 8, never log secrets per D-39.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet -> webhook handler | Untrusted POST; signature verification is first gate |
| Resend Received Emails API -> handler | Authenticated outbound call; body fetched separately from webhook payload (Pitfall 9) |
| webhook payload -> emit() transaction | Untrusted data crosses into DB writes; column types enforced at DB layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-223-04-01 | Spoofing | forged webhook event | mitigate | verifyWebhookSignature on all 3 endpoints; failure -> 401 + audit_log (D-38) |
| T-223-04-02 | Information Disclosure | secret leak in audit_log | mitigate | auditWebhookSignatureFailure sanitizes payload (D-39); test asserts no env value present |
| T-223-04-03 | Tampering | webhook replay -> double-counted aggregates | mitigate | dispatch_events UNIQUE (dispatch_attempt_id, event_type); handler swallows 23505 (Pitfall 3) |
| T-223-04-04 | Denial of Service | inbound bot replies create CRM task storm | mitigate | isLikelyAutoReply filter (Pitfall 7); orphan profile -> 200 without action |
| T-223-04-05 | Tampering | reply body swap via subsequent Resend API call | accept | Resend Received Emails API is authenticated; trusts Resend tenant |
| T-223-04-06 | Elevation | rogue webhook subscriber bypasses suppression | mitigate | webhook handler is the only entry point to emit(); recipient-handler from 223-02 is the only outbound path |
| T-223-04-07 | Information Disclosure | reply body contains PII -> stored in cdp_events | accept | tenant SOR; PII deletion handled by P221 D-24 tombstone cascade |
</threat_model>

<verification>
- 4 provider adapters complete contract from 223-01 (validateAdapterContract green for all).
- Resend inbound body-fetch helper handles Pitfall 9.
- Knock adapter sends + receives signed webhooks; INVALID_TOKEN propagates to bounce-handler (Pitfall 8).
- Single fan-out emit() writes 5 destinations transactionally; fail-closed verified.
- commercial_signal mapping covers all 9 event types.
- Auto-reply detector blocks vacation responders from CRM task creation (Pitfall 7).
- Reply continuity: lookupOrCreateThread + emit + CRM task + opt-out detector + setConsentState chain.
- 3 webhook handlers verify signatures, swallow replays (Pitfall 3), audit failures without secrets (D-39).
- Tombstone propagation hook from P221 D-24 triggers propagateTombstoneToSuppressions.
- VALIDATION rows added.
</verification>

<success_criteria>
- 4 adapters + Resend inbound + 3 webhook handlers + emit() + reply-flow + auto-reply-detect + thread lookup all green.
- Pitfalls 3, 7, 8, 9 explicitly mitigated and tested.
- D-38 webhook signature verification and D-39 secret hygiene proven.
- Legacy P104/P105 webhook tests still green.
</success_criteria>

<output>
After completion, create `.planning/phases/223-native-email-messaging-orchestration/223-04-SUMMARY.md` capturing:
- 4 provider adapters complete the contract (resend, twilio, knock, in_app)
- Resend inbound body-fetch helper + Pitfall 9 mitigation
- Knock invalid-token cascade + Pitfall 8 mitigation
- Single fan-out emit() transactional; commercial_signal map operational
- Reply continuity flow with auto-reply filter + opt-out detection + thread + CRM task
- 3 webhook handlers signed + dedup-safe + secret-clean
- Hand-off to 223-05 API: emit() ready for /v1/channels/* writes; thread lookup ready for messaging endpoints
</output>
