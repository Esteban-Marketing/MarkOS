---
phase: 223
plan: "03"
type: execute
wave: 3
depends_on: ["223-01", "223-02"]
files_modified:
  - lib/markos/channels/templates/renderer.ts
  - lib/markos/channels/templates/variable-resolver.ts
  - lib/markos/channels/templates/content-classifier.ts
  - lib/markos/channels/templates/preview.ts
  - lib/markos/channels/templates/handlebars-helpers.ts
  - lib/markos/channels/programs/email-campaign.ts
  - lib/markos/channels/programs/lifecycle-journey.ts
  - lib/markos/channels/templates/channel-template.ts
  - lib/markos/channels/templates/approval-trigger.ts
  - lib/markos/crm/agent-actions.ts
  - contracts/F-131-channel-approval-flow.yaml
  - contracts/flow-registry.json
  - test/channels/templates/renderer.test.js
  - test/channels/templates/variable-resolver.test.js
  - test/channels/templates/content-classifier.test.js
  - test/channels/templates/locale-variant.test.js
  - test/channels/templates/preview.test.js
  - test/channels/templates/approval-matrix.test.js
  - test/channels/templates/approval-revocation.test.js
  - .planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md
autonomous: true
requirements:
  - EML-01
  - EML-03
  - CRM-05
  - QA-01
  - QA-02
must_haves:
  truths:
    - "Plan 03 owns F-131 contract slot per D-40. Description: extends P105 buildApprovalPackage from lib/markos/crm/agent-actions.ts:68 (per D-43 — createApprovalPackage does NOT exist anywhere in the codebase)."
    - "Handlebars rendering uses a conservative helper set: {{var}} substitution + {{#if}} + {{#each}} + default HTML escape; NO partials, NO custom helpers, NO unsafe-string allow (D-26 + RESEARCH §Research Decision 2)"
    - "Variable resolver pulls from 4 sources in fixed precedence: dispatch_context_overrides > Opportunity > Customer360 > CDP TraitSnapshot (D-26)"
    - "Missing required variable -> per-recipient fail-closed (write dispatch_skip with reason='missing_variable'); never aborts the entire batch (Pitfall 5)"
    - "Content classifier is P223-OWNED greenfield (D-53 — NEW file lib/markos/channels/templates/content-classifier.ts; NOT 'carry from P211'). Implements 4 checks via @babel/parser AST + allowlist (D-52 — replaces 5-line regex heuristic): (1) pricing_bindings AST/allowlist enforcement (ALLOWLISTED_ACCESSORS = ['getPricingFor','formatPrice','usePricing','pricingFromEngine'] OR literal {{MARKOS_PRICING_ENGINE_PENDING}} placeholder per CLAUDE.md §placeholder rule); (2) raw body pricing-pattern regex scan as complementary cheap pre-filter; (3) evidence_bindings TTL via P209 EvidenceMap when present (graceful flag if P209 absent); (4) competitor mention flag"
    - "Layered approval triggers (D-16): class (broadcast + revenue ALWAYS), count (>=500 recipients ALWAYS), content classifier blocks ANY, manual override email_campaigns.requires_approval, re-engagement >90d OR lifecycle_stage='lost' (D-18) — first-trigger short-circuits with explicit reason in buildApprovalPackage call (D-43)"
    - "Re-engagement detector reads dispatch_events history per profile (last_send_at) and customer_360.lifecycle_stage; >90d gap OR stage='lost' -> always approve (D-18)"
    - "buildApprovalPackage (D-43 — from lib/markos/crm/agent-actions.ts:68, NOT createApprovalPackage which does not exist) is extended to accept channel entry types (email_campaign, lifecycle_journey, channel_template) carrying triggerReason + evidenceRefs + pricingRefs + contentClassifierFindings (D-17/D-32)"
    - "Approval revocation pre-dispatch transitions email_campaigns.status from approved -> pending_approval; mid-dispatch sends pause signal to AgentRun (D-17)"
    - "Locale variant resolution: parent_template_id chain -> recipient locale match wins; fallback to parent (D-28)"
    - "F-131 (channel-approval-flow) contract published with description 'extends P105 buildApprovalPackage' (NOT createApprovalPackage)"
  artifacts:
    - path: lib/markos/channels/templates/renderer.ts
      provides: renderTemplate({ template, variables, locale }) -> { subject, content_html, content_text } using Handlebars instance with conservative helper set; throws on missing required variable; sanitizes via Handlebars default escape
    - path: lib/markos/channels/templates/variable-resolver.ts
      provides: resolveVariables({ tenantId, profileId, template, dispatchContextOverrides, opportunityId? }) -> { variables: Record<string, unknown>, missing: string[] }; precedence overrides > Opportunity > Customer360 > TraitSnapshot
    - path: lib/markos/channels/templates/content-classifier.ts
      provides: classifyTemplate({ template, resolvedVariables, pricingEngineAdapter?, evidenceMapAdapter? }) -> { findings: ClassifierFinding[], blocks_dispatch: boolean }; covers 4 checks via @babel/parser AST/allowlist (D-52) + raw-body regex pre-filter (Pitfall 6)
    - path: lib/markos/channels/templates/preview.ts
      provides: renderPreview({ templateId, sampleProfileId? }) -> rendered preview + classifier findings (used by D-30 POST /v1/channels/templates/{id}/preview)
    - path: lib/markos/channels/templates/handlebars-helpers.ts
      provides: registerSafeHelpers(handlebars) — registers ONLY {{#if}} and {{#each}}; documented allow-list; throws on attempts to register others
    - path: lib/markos/channels/templates/approval-trigger.ts
      provides: evaluateApprovalTriggers({ campaign, audience, template, classifierFindings, customer360 }) -> { required: boolean, reasons: string[] }; layered first-fail-CLOSED order: class -> count -> content -> re-engagement -> manual
    - path: lib/markos/crm/agent-actions.ts
      provides: buildApprovalPackage extended (per D-43) with channel entry types email_campaign | lifecycle_journey | channel_template + triggerReason + classifier findings payload + pricingRefs (preserves existing CRM/agent-actions callers backward compat). NEW export revokeApproval added to same file for revocation flow.
    - path: contracts/F-131-channel-approval-flow.yaml
      provides: F-131 contract — channel approval-package extension entry types + triggerReason enum + GET /v1/channels/approvals + POST /v1/channels/approvals/{id}/approve + POST /v1/channels/approvals/{id}/reject + POST /v1/channels/approvals/{id}/revoke. Description references buildApprovalPackage (D-43).
  key_links:
    - from: lib/markos/channels/templates/renderer.ts
      to: lib/markos/channels/templates/variable-resolver.ts
      via: renderer calls resolveVariables before substituting; missing variable -> emits per-recipient skip
    - from: lib/markos/channels/templates/content-classifier.ts
      to: lib/markos/cdp/adapters/crm-projection.ts (P221 — preflight guarantees presence)
      via: classifier reads pricing_bindings + evidence_bindings via P221 adapter (Pricing Engine context lookup; graceful flag for evidence when P209 absent)
    - from: lib/markos/channels/templates/approval-trigger.ts
      to: lib/markos/crm/agent-actions.ts
      via: when required=true, evaluateApprovalTriggers calls buildApprovalPackage (D-43) with subjectType + triggerReason + findings
    - from: contracts/F-131-channel-approval-flow.yaml
      to: contracts/flow-registry.json
      via: F-131 entry registered append-only
---

<objective>
Wave 3 (parallel with 223-04). Templates + personalization + content classifier + layered approval engine.

Builds the Handlebars rendering pipeline with conservative helper set, the variable resolver pulling from 4 sources in fixed precedence, the content classifier (P223-owned greenfield per D-53; AST/allowlist per D-52) enforcing pricing + evidence + competitor + raw-body pricing-pattern checks, the approval trigger evaluator covering 5 layers (class + count + content + re-engagement + manual), and the F-131 approval-flow contract extending P105 `buildApprovalPackage` (per D-43 — NOT `createApprovalPackage`, which does not exist).

Provides the template preview endpoint primitive consumed by 223-05 API.

Purpose: satisfies EML-01 (template-driven email program governance), EML-03 (lifecycle journey step rendering), CRM-05 (CRM-generated outreach approval-aware + evidence-linked + pricing-safe), QA-01 (F-131 contract current), QA-02 (RLS preserved on channel_templates from 223-01).

Output: 9 library files (5 new + 4 wired into existing programs/agent-actions), 1 F-ID contract, flow-registry update, 7 test files (.test.js per D-46) covering Handlebars + variable resolver + 4-check classifier + locale chain + approval matrix + revocation.

**Note (D-44):** Runs in parallel with 223-04. Both depend ONLY on 223-01. No file conflicts: this plan touches `lib/markos/channels/templates/*` + `lib/markos/crm/agent-actions.ts` extension (NOT copilot.ts; D-43 — buildApprovalPackage lives in agent-actions.ts:68) + F-131; 223-04 touches `lib/markos/outbound/providers/*` + `lib/markos/channels/events/*` + `api/webhooks/*`.
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md
@.planning/phases/223-native-email-messaging-orchestration/223-RESEARCH.md
@.planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md
@.planning/phases/223-native-email-messaging-orchestration/223-REVIEWS.md
@.planning/phases/223-native-email-messaging-orchestration/223-01-SUMMARY.md
@.planning/phases/105-approval-aware-ai-copilot-and-reporting-closeout/105-CONTEXT.md
@.planning/phases/208-human-operating-interface/208-CONTEXT.md
@.planning/phases/209-evidence-research-and-claim-safety/209-CONTEXT.md
@.planning/phases/211-content-social-revenue-loop/211-CONTEXT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@obsidian/brain/Pricing Engine Canon.md
@obsidian/work/incoming/19-EMAIL-ENGINE.md
@obsidian/reference/Core Lib.md
@lib/markos/crm/agent-actions.ts
@lib/markos/channels/templates/channel-template.ts
@lib/markos/channels/programs/email-campaign.ts
@lib/markos/channels/programs/lifecycle-journey.ts
@supabase/migrations/114_channel_templates.sql
@supabase/migrations/115_channel_programs.sql
@contracts/F-127-channel-template-v1.yaml
@contracts/flow-registry.json
@package.json
</context>

<interfaces>
<!-- Existing buildApprovalPackage shape (D-43; from lib/markos/crm/agent-actions.ts:68) -->

```typescript
// lib/markos/crm/agent-actions.ts:68 — preserve current shape; ADD channel entry types
type BuildApprovalPackageInput = {
  tenant_id: string;
  review_tenant_id?: string;
  mutation_family: string; // EXTENDED: 'channel_email_campaign' | 'channel_lifecycle_journey' | 'channel_template' | (existing values)
  package_id?: string;
  run_id?: string;
  target_record_kind?: string; // EXTENDED: 'email_campaign' | 'lifecycle_journey' | 'channel_template'
  target_record_id?: string;
  status?: string; // 'awaiting_approval' default
  actor_id?: string;
  actor_role?: string;
  rationale?: object; // EXTENDED to carry triggerReason + classifierFindings + pricingRefs + evidenceRefs
  evidence?: Array<object>;
  proposed_changes?: object;
  correlation_id?: string;
  request_id?: string;
  created_at?: string;
};

export function buildApprovalPackage(input: BuildApprovalPackageInput): Readonly<ApprovalPackage>;
```

<!-- Channel template shape (D-25; from migration 114) -->

```typescript
type ChannelTemplate = {
  template_id: string;
  tenant_id: string;
  name: string;
  channel: 'email' | 'sms' | 'whatsapp' | 'push' | 'in_app';
  template_type: 'marketing' | 'transactional' | 'lifecycle' | 'support';
  subject?: string;
  content_blocks: Array<{ type: 'text' | 'html' | 'markdown' | 'mjml'; content: string }>;
  variables_schema: Record<string, unknown>; // JSON Schema
  evidence_bindings: Array<{ variable: string; claim_id: string; freshness_ttl: number }>;
  pricing_bindings: Array<{ variable: string; pricing_context_id: string }>;
  locale: string;
  parent_template_id?: string;
  version: number;
  status: 'draft' | 'approved' | 'archived';
};
```

<!-- ClassifierFinding (NEW) -->

```typescript
type ClassifierFinding = {
  check: 'pricing_binding' | 'pricing_text_scan' | 'pricing_ast_violation' | 'evidence_ttl' | 'competitor_mention';
  variable_or_block_ref?: string;
  severity: 'block' | 'flag';
  detail: string;
};
```

<!-- D-52 AST/allowlist for pricing classifier -->

```javascript
// Used by content-classifier.ts via @babel/parser
const ALLOWLISTED_ACCESSORS = ['getPricingFor', 'formatPrice', 'usePricing', 'pricingFromEngine'];
const PRICING_PLACEHOLDER = '{{MARKOS_PRICING_ENGINE_PENDING}}';

// AST scan walks CallExpression nodes in content_blocks (when content type is js-ish);
// any callee.name not in ALLOWLISTED_ACCESSORS that produces a pricing-shaped string -> block.
// For raw text content blocks, complementary regex pre-filter:
const PRICING_PATTERN_REGEX = /\$\d+|pricing|per month|per year|\bplan\b|\btier\b|\b\d+\s?\/\s?(mo|month|year|yr)\b/i;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Handlebars renderer with conservative helper set + variable-resolver with 4-source precedence + locale-chain resolver + per-recipient missing-variable fail-closed</name>
  <read_first>
    - lib/markos/channels/templates/channel-template.ts (Wave 1 stub — extend resolveLocaleVariant)
    - lib/markos/cdp/adapters/crm-projection.ts (P221 — getTraitSnapshot; preflight guarantees presence per D-45)
    - lib/markos/crm360/* (P222 — getCustomer360ByProfileId, getOpportunityById; preflight guarantees presence per D-45)
    - .planning/phases/223-native-email-messaging-orchestration/223-RESEARCH.md (§Architecture Patterns §Pitfalls 5+6 §Research Decision 2)
    - .planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md (D-25, D-26, D-28)
    - obsidian/work/incoming/19-EMAIL-ENGINE.md (template/personalization rules)
    - test/fixtures/channels/template.js (Wave 1)
    - package.json (verify handlebars dep installed in 223-01 Task 0.5)
  </read_first>
  <behavior>
    - Test 1 (Handlebars conservative set): renderTemplate with template body "Hello {{name}}, plan: {{plan}}" + variables {name: 'Ada', plan: 'Pro'} -> renders "Hello Ada, plan: Pro". HTML in template is escaped by default ({{name}} with name="<b>x</b>" outputs "&lt;b&gt;x&lt;/b&gt;").
    - Test 2 ({{#if}} {{#each}} only): registerSafeHelpers throws if caller attempts to register a custom helper. {{#partial 'foo'}} usage throws at template parse.
    - Test 3 (variable resolver precedence): given dispatch_context_overrides={name:'X'}, opportunity={name:'Y'}, customer360={name:'Z'}, trait_snapshot={name:'W'}, resolveVariables returns {name:'X'} (overrides win).
    - Test 4 (variable resolver missing required): variables_schema declares required: ['plan']; trait_snapshot has no 'plan'; customer360 has no 'plan'; opportunity has no 'plan'; resolveVariables returns missing=['plan'].
    - Test 5 (per-recipient fail-closed missing variable — Pitfall 5 mitigation): renderTemplate called with missing required variable throws TemplateRenderError; downstream caller (recipient-handler from 223-02) writes dispatch_skip with reason='missing_variable'; batch is NOT aborted.
    - Test 6 (locale variant resolution): parent template (locale='en') with child (parent_template_id=parent.id, locale='fr-FR'). resolveLocaleVariant({ parent_template_id: parent.id, recipient_locale: 'fr-FR' }) returns child. With recipient_locale='es-ES' (no child) returns parent fallback.
    - Test 7 (P222 hard prereq): getCustomer360ByProfileId is invoked unconditionally; preflight guarantees P222 present per D-45 — NO graceful fallback path in code.
  </behavior>
  <action>
    1. Create `lib/markos/channels/templates/handlebars-helpers.ts`:
       - Export `registerSafeHelpers(handlebars)` that registers a no-op (Handlebars 4.7.x ships {{#if}} and {{#each}} as built-ins; do not override). Throw on attempts to register others.
       - Export `createSafeHandlebarsInstance()` returning a fresh Handlebars instance with allow-list enforcement (intercept registerHelper / registerPartial -> throw `HANDLEBARS_HELPER_DISALLOWED:<name>`).
       - Document at top: "Conservative subset per RESEARCH §Research Decision 2 — limits attack surface."
    2. Create `lib/markos/channels/templates/renderer.ts`:
       - Export `renderTemplate({ template, variables, locale }) -> { subject?, content_html?, content_text?, body }`.
       - Use createSafeHandlebarsInstance.
       - Compile template once, cache by template_id+version key (in-memory LRU).
       - Render content_blocks per type: 'text' -> raw string; 'html' -> Handlebars-escaped; 'markdown' -> Handlebars then markdown-it (if installed) else passthrough; 'mjml' -> deferred (P224 conversion surfaces) — NOOP with warning.
       - Throw `TemplateRenderError({ kind: 'missing_variable', variable_name })` when variables resolver returns missing names.
       - Throw `TemplateRenderError({ kind: 'unsafe_helper', name })` if Handlebars compile encounters disallowed helper.
    3. Create `lib/markos/channels/templates/variable-resolver.ts`:
       - Export `resolveVariables({ tenantId, profileId, template, dispatchContextOverrides = {}, opportunityId? }) -> { variables, missing }`.
       - Build precedence merge: { ...traitSnapshot, ...customer360, ...opportunity, ...dispatchContextOverrides } (later wins).
       - Validate against template.variables_schema (use ajv or zod via existing project pattern — confirm at execute time).
       - Returns { variables: mergedAndValidated, missing: required[] - present[] }.
       - **Per D-45 hard preflight:** unconditional calls to getTraitSnapshot (P221) + getCustomer360ByProfileId (P222) + getOpportunityById (P222). NO graceful absence paths.
    4. Update `lib/markos/channels/templates/channel-template.ts` (Wave 1 stub) to fully implement `resolveLocaleVariant({ parent_template_id, recipient_locale })`:
       - SELECT * FROM channel_templates WHERE parent_template_id = $1 AND locale = $2 AND status = 'approved' LIMIT 1.
       - If found, return child; else fallback SELECT WHERE template_id = parent_template_id (parent).
    5. Create `lib/markos/channels/templates/preview.ts`:
       - Export `renderPreview({ templateId, sampleProfileId?, dispatchContextOverrides? }) -> { rendered: { subject, body }, classifier_findings: ClassifierFinding[], missing_variables: string[] }`.
       - Used by D-30 POST /v1/channels/templates/{id}/preview API (wired in 223-05).
    6. Create test files (all `.test.js` per D-46):
       - `test/channels/templates/renderer.test.js` (Tests 1, 2).
       - `test/channels/templates/variable-resolver.test.js` (Tests 3, 4, 5, 7).
       - `test/channels/templates/locale-variant.test.js` (Test 6).
       - `test/channels/templates/preview.test.js` (smoke test for renderPreview shape).
    7. Append VALIDATION rows.
  </action>
  <verify>
    <automated>npm test -- test/channels/templates/renderer.test.js test/channels/templates/variable-resolver.test.js test/channels/templates/locale-variant.test.js test/channels/templates/preview.test.js</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/markos/channels/templates/renderer.ts` exports `renderTemplate` (grep)
    - File `lib/markos/channels/templates/handlebars-helpers.ts` contains `registerHelper.*throw` AND `registerPartial.*throw` enforcement (grep both throw clauses)
    - File `lib/markos/channels/templates/variable-resolver.ts` precedence merge order is overrides last (grep `dispatchContextOverrides` appears AFTER `traitSnapshot` in spread merge)
    - `npm test -- test/channels/templates/renderer.test.js` exits 0 with HTML escape verified
    - `npm test -- test/channels/templates/variable-resolver.test.js` exits 0 with precedence + missing detection
    - `npm test -- test/channels/templates/locale-variant.test.js` exits 0 with parent fallback
    - File `lib/markos/channels/templates/preview.ts` exports `renderPreview`
    - All 4 test files end with `.test.js` (D-46)
  </acceptance_criteria>
  <done>Renderer + resolver + locale chain + preview operational; conservative helper set enforced; missing variable per-recipient fail-closed proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Content classifier (P223-OWNED greenfield per D-53; AST/allowlist per D-52) — pricing_bindings + raw-body pricing-pattern scan + AST CallExpression check + evidence_bindings TTL + competitor mention flag (4 checks)</name>
  <read_first>
    - lib/markos/channels/templates/variable-resolver.ts (Task 1 output)
    - obsidian/brain/Pricing Engine Canon.md (placeholder rule)
    - .planning/phases/211-content-social-revenue-loop/211-CONTEXT.md (D-23 placeholder rule reference — note D-53: classifier IMPL is P223-owned, NOT P211 carry)
    - .planning/phases/209-evidence-research-and-claim-safety/209-CONTEXT.md (EvidenceMap shape)
    - .planning/phases/223-native-email-messaging-orchestration/223-RESEARCH.md (§Pitfalls 6 §Pattern 1)
    - .planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md (D-16, D-25, D-27, D-52, D-53)
    - test/fixtures/channels/template.js
    - CLAUDE.md (§placeholder rule)
    - package.json (verify @babel/parser added in Task 0.5)
  </read_first>
  <behavior>
    - Test 1 (pricing_binding to pending value blocks via AST): template with pricing_bindings=[{variable:'plan_price', pricing_context_id:'pending-1'}] + resolved variable plan_price='$49/mo' (NOT from approved Pricing Engine record, NOT placeholder) -> classifier returns blocks_dispatch=true, finding.check='pricing_binding' OR 'pricing_ast_violation', finding.severity='block'.
    - Test 2 (pricing_binding to placeholder allows): template with pricing_bindings=[{variable:'plan_price', pricing_context_id:'pending-1'}] + resolved variable plan_price='{{MARKOS_PRICING_ENGINE_PENDING}}' -> blocks_dispatch=false (placeholder is acceptable per CLAUDE.md). finding.severity='flag' (audit trail).
    - Test 3 (pricing_binding via allowlisted accessor allows — D-52): template content block contains JS-ish `getPricingFor('pro').amount` -> AST scan recognizes ALLOWLISTED_ACCESSOR getPricingFor; blocks_dispatch=false; no finding.
    - Test 4 (pricing_binding via non-allowlisted accessor blocks — D-52): template content block contains `someInternalApi.fetchPrice('pro')` -> AST scan finds CallExpression with callee not in ALLOWLISTED_ACCESSORS that produces a pricing-shaped value -> finding.check='pricing_ast_violation', severity='block', blocks_dispatch=true.
    - Test 5 (raw body pricing-pattern regex pre-filter — Pitfall 6): template content_blocks contains "Get started for $49/month" with NO pricing_bindings entry, NO allowlisted accessor -> regex matches; AST pass confirms no allowlisted source; finding.check='pricing_text_scan', severity='block'.
    - Test 6 (cross-module pricing assembly — D-52): template content_blocks A contains `subject = '$' + price`; raw body has `${subject}`. 5-line regex would have missed; AST pass detects the string concatenation with a pricing-shaped literal `$` and a non-allowlisted producer of `price` -> finding.check='pricing_ast_violation', severity='block'. Confirms D-52 catches cross-file assembly that 5-line regex misses (P226 RM3 carry).
    - Test 7 (evidence_binding TTL fresh): evidence_bindings=[{variable:'stat', claim_id:'C-7', freshness_ttl:30}] AND P209 EvidenceMap (graceful — when P209 absent, classifier returns finding.check='evidence_ttl' severity='flag' with detail='P209 unavailable; cannot verify') -> blocks_dispatch=false (flag only when P209 absent; block only when expired AND P209 present).
    - Test 8 (evidence_binding TTL stale, P209 present): claim freshness > TTL -> finding.severity='block', blocks_dispatch=true.
    - Test 9 (competitor mention): content_blocks contains "we're better than Hubspot" -> finding.check='competitor_mention', severity='flag', blocks_dispatch=false (flag for review, not blocked).
    - Test 10 (no findings -> blocks_dispatch=false): clean template with allowlisted accessor or placeholder -> findings=[], blocks_dispatch=false.
  </behavior>
  <action>
    1. Create `lib/markos/channels/templates/content-classifier.ts` (P223-OWNED greenfield per D-53):
       - File header comment: "Content classifier — P223-OWNED greenfield (D-53). NOT a carry from P211; P211 introduced the {{MARKOS_PRICING_ENGINE_PENDING}} placeholder rule, but the classifier IMPL belongs to P223."
       - Import `@babel/parser` (added to package.json in Plan 01 Task 0.5 per D-52).
       - Export `ALLOWLISTED_ACCESSORS = ['getPricingFor', 'formatPrice', 'usePricing', 'pricingFromEngine']` (D-52).
       - Export `PRICING_PLACEHOLDER = '{{MARKOS_PRICING_ENGINE_PENDING}}'` (CLAUDE.md placeholder rule).
       - Export `PRICING_PATTERN_REGEX = /\$\d+|pricing|per month|per year|\bplan\b|\btier\b|\b\d+\s?\/\s?(mo|month|year|yr)\b/i` (cheap pre-filter; D-52 says regex stays as complementary check, AST is authoritative).
       - Export `classifyTemplate({ template, resolvedVariables, pricingEngineAdapter? = optional, evidenceMapAdapter? = optional }) -> { findings: ClassifierFinding[], blocks_dispatch: boolean }`.
         - **Check 1 (pricing_bindings):** for each pricing_bindings entry: get resolvedVariables[binding.variable].
           - If value === PRICING_PLACEHOLDER -> findings.push({ check:'pricing_binding', severity:'flag', variable:binding.variable, detail:'placeholder used' }).
           - If pricingEngineAdapter present and pricing_context_id maps to approved record AND value derives from allowlisted accessor (verified via AST pass — Check 2) -> no finding.
           - Else (pricing-shaped value with no approved Pricing Engine backing AND no allowlisted accessor) -> findings.push({ check:'pricing_binding', severity:'block', detail:'pricing value not from approved Pricing Engine record via allowlisted accessor' }).
         - **Check 2 (AST/allowlist scan — D-52, replaces 5-line regex):** for each content_block where type is 'js' or 'js-template' (or any block with embedded {{var}} that resolves through a JS expression):
           - Parse via `@babel/parser` with `{ sourceType: 'module', plugins: ['jsx','typescript'] }`.
           - Walk AST; for each CallExpression node: if `node.callee.name` exists AND not in ALLOWLISTED_ACCESSORS AND the surrounding context is a pricing-shaped expression (returns string concatenation with `$`, returns property like `.amount`/`.price`, etc — heuristic via node parent inspection) -> findings.push({ check:'pricing_ast_violation', severity:'block', detail:'pricing produced by non-allowlisted accessor: ' + node.callee.name }).
           - For raw text content blocks: run PRICING_PATTERN_REGEX as cheap pre-filter; if matched and the matched substring did not come from an allowlisted accessor (cross-reference with Check 1 + Check 2 AST results) -> findings.push({ check:'pricing_text_scan', severity:'block', detail:'unbound pricing string detected; not produced by allowlisted accessor' }).
         - **Check 3 (evidence_bindings TTL):** for each evidence_bindings entry: if evidenceMapAdapter absent -> findings.push({ check:'evidence_ttl', severity:'flag', detail:'P209 EvidenceMap unavailable' }). Else: claim = await evidenceMapAdapter.getClaim(binding.claim_id); if (now - claim.fetched_at) > binding.freshness_ttl -> findings.push({ check:'evidence_ttl', severity:'block', variable:binding.variable, detail:'evidence stale' }).
         - **Check 4 (competitor mention):** read tenant competitors list (initial: ['hubspot','salesforce','marketo','klaviyo','mailchimp','iterable','braze','customer.io']). Case-insensitive scan of content_blocks; if matched -> findings.push({ check:'competitor_mention', severity:'flag', detail:'competitor mentioned' }).
         - blocks_dispatch = findings.some(f => f.severity === 'block').
         - Throw on missing template.content_blocks (defensive).
    2. Create `test/channels/templates/content-classifier.test.js` running Tests 1-10 above. Mock pricingEngineAdapter + evidenceMapAdapter with tunable behavior per test.
    3. Append VALIDATION rows.
  </action>
  <verify>
    <automated>npm test -- test/channels/templates/content-classifier.test.js</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/markos/channels/templates/content-classifier.ts` exports `classifyTemplate` (grep)
    - File contains 5 distinct check identifiers (`pricing_binding`, `pricing_text_scan`, `pricing_ast_violation`, `evidence_ttl`, `competitor_mention`)
    - File imports `@babel/parser` (grep `from '@babel/parser'` OR `require('@babel/parser')`)
    - File exports `ALLOWLISTED_ACCESSORS` array containing `'getPricingFor'`, `'formatPrice'`, `'usePricing'`, `'pricingFromEngine'` (D-52)
    - File contains `MARKOS_PRICING_ENGINE_PENDING` placeholder allow-list (grep)
    - File header comment contains "P223-OWNED greenfield (D-53)" (grep)
    - `npm test -- test/channels/templates/content-classifier.test.js` exits 0 with all 10 tests green
    - File contains graceful absence path for P209 (grep `evidenceMapAdapter` + conditional `if (!evidenceMapAdapter)`)
  </acceptance_criteria>
  <done>4-check classifier operational; pricing_binding via AST/allowlist (D-52) + raw-body regex pre-filter (Pitfall 6) + evidence TTL + competitor flagging all proven; CLAUDE.md placeholder rule honored. Cross-module pricing assembly (Test 6) verifies D-52 advantage over 5-line regex (P226 RM3 carry).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Approval trigger evaluator (5-layer fail-CLOSED) + extend buildApprovalPackage with channel entry types (D-43 — NOT createApprovalPackage) + revocation flow + F-131 contract</name>
  <read_first>
    - lib/markos/crm/agent-actions.ts (P105 buildApprovalPackage current shape at line 68 — preserve; per D-43)
    - lib/markos/channels/templates/content-classifier.ts (Task 2 output)
    - lib/markos/channels/programs/email-campaign.ts (Wave 1 — setEmailCampaignStatus state machine)
    - lib/markos/channels/dispatch/dispatch-coordinator.ts (Wave 2 — markDispatchComplete + AgentRun pause path)
    - lib/markos/crm360/* (P222 — getCustomer360ByProfileId for re-engagement detection lifecycle_stage='lost'; preflight guarantees presence per D-45)
    - .planning/phases/105-approval-aware-ai-copilot-and-reporting-closeout/105-CONTEXT.md (approval-package pattern)
    - .planning/phases/208-human-operating-interface/208-CONTEXT.md (Approval Inbox entry types)
    - .planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md (D-16, D-17, D-18, D-32, D-43)
    - .planning/phases/223-native-email-messaging-orchestration/223-RESEARCH.md (§Approval Package §Slice 223-03)
    - test/fixtures/channels/email-campaign.js + test/fixtures/channels/template.js
  </read_first>
  <behavior>
    - Test 1 (Layer 1 class=broadcast): campaign_type='broadcast' -> required=true, reasons=['class_broadcast'].
    - Test 2 (Layer 1 class=revenue): campaign_type='revenue' -> required=true, reasons=['class_revenue'].
    - Test 3 (Layer 1 class=transactional): campaign_type='transactional' AND no other layer triggers -> required=false (auto-approve within sender.class_permissions).
    - Test 4 (Layer 2 count >= 500): campaign_type='lifecycle' + audience_size=500 -> required=true, reasons=['count_threshold'].
    - Test 5 (Layer 2 count < 500): campaign_type='lifecycle' + audience_size=100 + clean classifier -> required=false.
    - Test 6 (Layer 3 content classifier flag): clean class+count, classifier_findings=[{severity:'block', check:'pricing_binding'}] -> required=true, reasons=['content_classifier:pricing_binding'].
    - Test 7 (Layer 3 content classifier no block): classifier_findings=[{severity:'flag', check:'competitor_mention'}] -> required=false (flag-only does not gate).
    - Test 8 (Layer 4 re-engagement >90d — D-18): customer_360.last_email_send_at = NOW - 91 days -> required=true, reasons=['reengagement'].
    - Test 9 (Layer 4 lifecycle_stage='lost' — D-18): customer_360.lifecycle_stage='lost' -> required=true, reasons=['reengagement'].
    - Test 10 (Layer 5 manual override): campaign.requires_approval=true even with class=transactional + count<500 + clean classifier -> required=true, reasons=['manual'].
    - Test 11 (multiple layers stacked): broadcast + count=600 + classifier block -> required=true, reasons=['class_broadcast','count_threshold','content_classifier:pricing_binding'] (all gathered, fail-CLOSED).
    - Test 12 (buildApprovalPackage extended — D-43): when required=true, evaluateApprovalTriggers calls **buildApprovalPackage** (NOT createApprovalPackage which does not exist) with mutation_family='channel_email_campaign' + target_record_kind='email_campaign' + rationale.triggerReason=reasons.join(',') + rationale.classifierFindings + rationale.pricingRefs; returns approval package with package_id; campaign.status -> 'pending_approval' + approval_ref set.
    - Test 13 (revocation pre-dispatch): campaign.status='approved'; revokeApproval(approval_id) -> status -> 'pending_approval' + approval_ref cleared.
    - Test 14 (revocation mid-dispatch): campaign.status='dispatching'; revokeApproval -> sends pause signal to dispatch-coordinator (markRunPaused) + audit_log row event_type='channel_dispatch_paused_by_approval_revocation'.
    - Test 15 (F-131 contract): file exists; flow_id=F-131; paths include POST /v1/channels/approvals/{id}/approve, POST /v1/channels/approvals/{id}/reject, POST /v1/channels/approvals/{id}/revoke. Description references buildApprovalPackage (NOT createApprovalPackage).
    - Test 16 (D-43 enforcement — meta): grep `createApprovalPackage` across `lib/markos/channels/` AND `lib/markos/crm/` returns 0 matches. grep `buildApprovalPackage` across same returns ≥3 matches (extension target + invocation sites).
  </behavior>
  <action>
    1. Create `lib/markos/channels/templates/approval-trigger.ts`:
       - Export `evaluateApprovalTriggers({ campaign, audience, classifierFindings = [], customer360 })`:
         - reasons = []
         - Layer 1 (class): if campaign.campaign_type IN ('broadcast','revenue') -> push 'class_' + campaign.campaign_type.
         - Layer 2 (count): if audience.membership_count >= getTenantApprovalCountThreshold(campaign.tenant_id) (default 500; tenant-overridable via tenants.config.approval_count_threshold) -> push 'count_threshold'.
         - Layer 3 (content): for each finding in classifierFindings where severity='block' -> push `content_classifier:${finding.check}`.
         - Layer 4 (re-engagement, D-18): if customer360 provided AND (now - customer360.last_email_send_at > 90 days OR customer360.lifecycle_stage === 'lost') -> push 'reengagement'.
         - Layer 5 (manual): if campaign.requires_approval === true -> push 'manual'.
         - Return { required: reasons.length > 0, reasons }.
    2. Extend `lib/markos/crm/agent-actions.ts::buildApprovalPackage` (per D-43 — preserve all existing fields):
       - Accept new `mutation_family` values: 'channel_email_campaign', 'channel_lifecycle_journey', 'channel_template'.
       - Accept new `target_record_kind` values: 'email_campaign', 'lifecycle_journey', 'channel_template'.
       - Accept extended `rationale` payload carrying triggerReason (string array or comma-joined), classifierFindings: ClassifierFinding[], pricingRefs: string[], evidenceRefs: string[].
       - When mutation_family is one of the channel families, route to a new branch that links the approval row back to the channel program (UPDATE email_campaigns SET approval_ref=... etc).
       - Existing CRM/agent-actions callers MUST keep working — add backward-compat default for new fields.
    3. Add `revokeApproval({ approval_id, actorId, reason })` exported from `lib/markos/crm/agent-actions.ts`:
       - Look up approval -> read mutation_family + target_record_id.
       - If mutation_family='channel_email_campaign' AND campaign.status='approved' -> setEmailCampaignStatus(target_record_id, 'pending_approval'). Clear approval_ref.
       - If campaign.status='dispatching' -> call dispatch-coordinator.markRunPaused(campaign.dispatch_run_id, { reason: 'approval_revoked', revokerId: actorId }) + audit_log row.
       - If campaign.status='dispatched' or 'completed' -> NO-OP (revocation cannot recall sent messages); audit_log row with reason='revocation_too_late'.
    4. Wire `evaluateApprovalTriggers` into `lib/markos/channels/programs/email-campaign.ts::setEmailCampaignStatus(_, 'pending_approval' | 'approved')` transitions:
       - On status='draft' -> 'pending_approval': require evaluateApprovalTriggers.required=true OR campaign.requires_approval=true; otherwise auto-skip to 'approved' (transactional fast path).
       - On status='approved' -> 'dispatching': verify approval_ref still pending=false (i.e., approved); if revoked since -> reject transition.
    5. Wire `evaluateApprovalTriggers` into `lib/markos/channels/programs/lifecycle-journey.ts::buildJourneyRun` and step transitions: each journey step also runs the trigger eval against (step.template + step.audience).
    6. Create `contracts/F-131-channel-approval-flow.yaml` with:
       - `flow_id: F-131`, `flow_name: channel-approval-flow`.
       - description: "Channel approval-flow contract. Extends P105 `buildApprovalPackage` from `lib/markos/crm/agent-actions.ts:68` (per D-43 — `createApprovalPackage` does NOT exist anywhere in the codebase). Adds channel mutation_family values: channel_email_campaign, channel_lifecycle_journey, channel_template."
       - paths: GET /v1/channels/approvals (list pending), GET /v1/channels/approvals/{id}, POST /v1/channels/approvals/{id}/approve, POST /v1/channels/approvals/{id}/reject, POST /v1/channels/approvals/{id}/revoke.
       - approval_policy: triggerReason enum ['class_broadcast','class_revenue','count_threshold','content_classifier','reengagement','manual']; mutation_families ['channel_email_campaign','channel_lifecycle_journey','channel_template']; target_record_kinds ['email_campaign','lifecycle_journey','channel_template'].
       - approval_inbox_extension: yes (P208 inbox accepts these types).
    7. Update `contracts/flow-registry.json` with F-131 append-only.
    8. Create test files (all `.test.js` per D-46):
       - `test/channels/templates/approval-matrix.test.js` (Tests 1-11; class × count × content × reengagement × manual matrix per channel + Test 16 D-43 enforcement grep).
       - `test/channels/templates/approval-revocation.test.js` (Tests 12-14 — revocation pre + mid + post dispatch + verifies buildApprovalPackage call signature).
    9. Append VALIDATION rows.
  </action>
  <verify>
    <automated>npm test -- test/channels/templates/approval-matrix.test.js test/channels/templates/approval-revocation.test.js && node bin/validate-flow-contracts.cjs</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/markos/channels/templates/approval-trigger.ts` exports `evaluateApprovalTriggers` (grep)
    - File contains 5 layer comments labeled `Layer 1`, `Layer 2`, `Layer 3`, `Layer 4`, `Layer 5` (grep all 5 layer labels)
    - File `lib/markos/crm/agent-actions.ts` accepts `mutation_family: 'channel_email_campaign'` (grep `channel_email_campaign` after agent-actions.ts file path)
    - File `lib/markos/crm/agent-actions.ts` exports `revokeApproval`
    - File `contracts/F-131-channel-approval-flow.yaml` contains `flow_id: F-131` AND `revoke` path AND `buildApprovalPackage` reference (NOT `createApprovalPackage`)
    - **D-43 enforcement:** `grep -c "createApprovalPackage" lib/markos/channels/ lib/markos/crm/` returns 0 (NO references to the fictional helper). `grep -c "buildApprovalPackage" lib/markos/channels/ lib/markos/crm/agent-actions.ts` returns ≥3 (positive use).
    - **D-43 enforcement (plan files):** `grep -c "createApprovalPackage" .planning/phases/223-*-PLAN.md` returns 0 positive invocations (only doctrinal "rejected" mentions in CONTEXT/REVIEWS allowed).
    - `npm test -- test/channels/templates/approval-matrix.test.js` exits 0 with ≥11 assertions
    - `npm test -- test/channels/templates/approval-revocation.test.js` exits 0
    - `contracts/flow-registry.json` includes F-131 entry
    - `node bin/validate-flow-contracts.cjs` exits 0
    - All test files end with `.test.js` (D-46)
  </acceptance_criteria>
  <done>5-layer approval engine operational; buildApprovalPackage extended (D-43 — NOT createApprovalPackage); revocation pre/mid/post dispatch handled; F-131 published with correct helper reference; multi-layer stacking proven fail-CLOSED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-supplied template body -> Handlebars compile | Template content treated as untrusted; conservative helper allow-list |
| variable resolution sources -> renderer | Multiple sources merged; precedence rules deterministic |
| pricing_bindings/evidence_bindings -> external lookup | Adapters may be absent (only P209); classifier degrades to flag (never silently allows) |
| template content_blocks -> AST parser | Untrusted JS-ish content parsed via @babel/parser; AST scan enforces allowlist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-223-03-01 | Injection | XSS via template variable in email body | mitigate | Handlebars default {{var}} HTML escape; tested in renderer.test.js |
| T-223-03-02 | Tampering | helper sandbox escape via custom Handlebars partial | mitigate | createSafeHandlebarsInstance overrides registerHelper/registerPartial to throw |
| T-223-03-03 | Information Disclosure | template variable reveals other-tenant data | mitigate | variable-resolver scoped to tenantId; resolveVariables uses RLS-protected adapters |
| T-223-03-04 | Tampering | pricing copy bypass via raw template text | mitigate | D-52 AST/allowlist scan + Pitfall 6 raw-body PRICING_PATTERN_REGEX pre-filter |
| T-223-03-05 | Tampering | pricing copy bypass via cross-module assembly (P226 RM3 carry) | mitigate | D-52 AST CallExpression scan catches non-allowlisted pricing accessors that 5-line regex would miss |
| T-223-03-06 | Elevation | approval bypass via direct status update | mitigate | setEmailCampaignStatus checks approval_ref + status transitions; only revokeApproval can move backward |
| T-223-03-07 | Repudiation | revocation without audit | mitigate | revokeApproval writes audit_log row with reason + actorId |
| T-223-03-08 | Tampering | competitor-name evasion (case variations, leetspeak) | accept | initial regex is case-insensitive; advanced evasion deferred to P226 deal-execution copilot |
| T-223-03-09 | Spoofing | approval payload references fictional helper (createApprovalPackage) | mitigate | D-43 — grep enforcement in architecture-lock test (Plan 01 Task 0.5) catches any plan-file regression; Plan 03 wires buildApprovalPackage exclusively |
</threat_model>

<verification>
- 9 library files (5 new + 4 wired): renderer, variable-resolver, content-classifier, preview, handlebars-helpers, approval-trigger, channel-template (locale extension), email-campaign (status transition wiring), agent-actions.ts (extension per D-43).
- Conservative Handlebars helper set enforced.
- Variable resolver precedence (overrides > Opportunity > Customer360 > TraitSnapshot) tested.
- Per-recipient missing-variable fail-closed (Pitfall 5) integrated with 223-02 recipient-handler.
- Content classifier 4 checks operational via D-52 AST/allowlist + raw-body regex pre-filter; blocks_dispatch only on severity='block'.
- D-53 — classifier IMPL is P223-owned greenfield, NOT carry from P211.
- Approval engine 5 layers fail-CLOSED (class + count + content + reengagement + manual); reasons stack on multi-layer triggers.
- buildApprovalPackage extended (D-43 — NOT createApprovalPackage); revokeApproval handles pre/mid/post dispatch.
- F-131 contract published with buildApprovalPackage reference.
- VALIDATION rows added.
- Architecture-lock test (Plan 01 Task 0.5) passes — no createApprovalPackage references regress.
</verification>

<success_criteria>
- Renderer + resolver + classifier + approval engine all green via npm test.
- Tests cover 4 channels × allow/block matrix + revocation × 3 dispatch states.
- F-131 in flow-registry.
- Pricing placeholder rule honored (D-27 + D-52 + CLAUDE.md).
- buildApprovalPackage usage exclusive (D-43 enforcement).
- All test files use `.test.js` per D-46.
</success_criteria>

<output>
After completion, create `.planning/phases/223-native-email-messaging-orchestration/223-03-SUMMARY.md` capturing:
- Conservative Handlebars set + variable resolver precedence + locale chain operational
- 4-check content classifier shipped (D-53 P223-owned greenfield) with D-52 AST/allowlist + Pitfall 6 raw-body scan
- 5-layer approval trigger evaluator + extension to lib/markos/crm/agent-actions.ts buildApprovalPackage (D-43) + revocation flow
- F-131 contract + flow-registry update; description references buildApprovalPackage (not createApprovalPackage)
- Hand-off to 223-05 API: template preview endpoint primitive ready; approval inbox entry types defined
</output>
