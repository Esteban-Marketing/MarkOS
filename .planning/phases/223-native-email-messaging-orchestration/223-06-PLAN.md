---
phase: 223
plan: "06"
type: execute
wave: 5
depends_on: ["223-01", "223-02", "223-03", "223-04", "223-05"]
files_modified:
  - lib/markos/channels/deliverability/posture-rollup.ts
  - lib/markos/channels/deliverability/spike-alert.ts
  - lib/markos/channels/dispatch/lifecycle-journey-poll.ts
  - lib/markos/channels/dispatch/soft-bounce-promotion.ts
  - app/api/cron/channels-deliverability-rollup/route.ts
  - app/api/cron/channels-lifecycle-journey-poll/route.ts
  - app/api/cron/channels-bounce-spike-alert/route.ts
  - app/api/cron/channels-tombstone-cascade/route.ts
  - app/api/cron/channels-soft-bounce-promote/route.ts
  - vercel.json
  - test/channels/closeout/rls-suite.test.ts
  - test/channels/closeout/deliverability-rollup.test.ts
  - test/channels/closeout/spike-alert.test.ts
  - test/channels/closeout/lifecycle-journey-poll.test.ts
  - test/channels/closeout/tombstone-cascade.test.ts
  - test/channels/closeout/soft-bounce-promotion.test.ts
  - test/channels/closeout/legacy-regression.test.ts
  - test/channels/closeout/qa-gates-coverage.test.ts
  - e2e/channels/campaign-create-approve-dispatch-reply-bounce.spec.ts
  - e2e/channels/messaging-thread-reply-flow.spec.ts
  - e2e/channels/lifecycle-journey-create-pause-resume.spec.ts
  - e2e/channels/template-editor-pricing-classifier.spec.ts
  - e2e/channels/deliverability-workspace-warming.spec.ts
  - .storybook/stories/LifecycleJourneyEditor.stories.tsx
  - chromatic.config.json
  - .planning/phases/223-native-email-messaging-orchestration/223-SUMMARY.md
  - .planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - EML-01
  - EML-02
  - EML-03
  - EML-04
  - EML-05
  - MSG-01
  - MSG-02
  - MSG-03
  - MSG-04
  - MSG-05
  - CDP-01
  - CDP-02
  - CDP-03
  - CDP-04
  - CDP-05
  - CRM-01
  - CRM-02
  - CRM-03
  - CRM-04
  - CRM-05
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - QA-05
  - QA-06
  - QA-07
  - QA-08
  - QA-09
  - QA-10
  - QA-11
  - QA-12
  - QA-13
  - QA-14
  - QA-15
must_haves:
  truths:
    - All 11 new tables (sender_identities, deliverability_posture, channel_templates, email_campaigns, messaging_threads, lifecycle_journeys, lifecycle_journey_runs, channel_suppressions, dispatch_events, dispatch_skips, in_app_messages) RLS-verified cross-tenant denied via the rls-suite.test.ts matrix (CRUD × all tables)
    - Hourly deliverability_posture rollup cron runs and produces window_24h_start row with reputation_score correctly derived from bounce_rate + complaint_rate (D-07)
    - Bounce/complaint spike alert (>2σ from 7-day baseline) fires operator task — D-35
    - Lifecycle journey poll runs every 60s; due lifecycle_journey_runs are advanced (RESEARCH §Research Decision 4)
    - Soft-bounce promotion cron promotes recipients with 3+ soft_bounce occurrences in 7 days to hard_bounce + suppression (D-21)
    - Tombstone cascade: P221 D-24 profile tombstone -> propagateTombstoneToSuppressions writes channel_suppressions row across all 5 channels (D-21 + P221 D-24 + P222 D-32)
    - Legacy regression: P100-P105 + P221 + P222 test suites all green (no regressions from substitution work)
    - Playwright operator-journey: campaign create -> approve -> dispatch -> reply -> bounce in one E2E test (RESEARCH §Slice 223-06 Playwright)
    - Chromatic visual regression covers all 5 new components × 4 variant states each (Empty + Populated + Error + Tombstoned)
    - 223-SUMMARY.md captures phase outcomes; STATE.md reflects phase closure; ROADMAP.md marks Phase 223 status complete with hand-off to 224
    - phase gate: full suite green (vitest run && playwright test && chromatic && npm test) before /gsd:verify-work
    - VALIDATION.md `nyquist_compliant: true` flipped on once all per-task rows populated (NOTE: nyquist flip is owned by execute-phase, not the planner)
    - QA-03..QA-15 concrete gates each have a grep/test-runnable target (see <qa_gates> block); qa-gates-coverage.test.ts asserts each gate has at least one passing test in the suite
  artifacts:
    - path: lib/markos/channels/deliverability/posture-rollup.ts
      provides: aggregateDeliverabilityPosture(tenantId, channel, window_24h_start) — computes rates from dispatch_events; INSERT/UPSERT into deliverability_posture; sets reputation_score + trend
    - path: lib/markos/channels/deliverability/spike-alert.ts
      provides: detectBounceSpike(tenantId, channel, sender_id) — 7-day baseline mean + stddev; if current 24h > baseline + 2σ -> create operator task
    - path: lib/markos/channels/dispatch/lifecycle-journey-poll.ts
      provides: pollDueJourneyRuns(tenantId) — SELECT lifecycle_journey_runs WHERE state='pending' AND due_at <= now() AND tenant_id; advanceJourneyRun per row (runs gate -> emit + advance step)
    - path: lib/markos/channels/dispatch/soft-bounce-promotion.ts
      provides: promoteSoftBounces(tenantId) — count soft_bounce in 7d per (profile, channel); >=3 -> handleBounceEvent('hard') + audit
    - path: app/api/cron/channels-deliverability-rollup/route.ts
      provides: Vercel Cron endpoint hourly invocation
    - path: app/api/cron/channels-lifecycle-journey-poll/route.ts
      provides: Vercel Cron endpoint every 60s invocation
    - path: app/api/cron/channels-bounce-spike-alert/route.ts
      provides: Vercel Cron endpoint daily invocation
    - path: app/api/cron/channels-tombstone-cascade/route.ts
      provides: Vercel Cron endpoint daily — reads cdp_identity_profiles where tombstoned_at > last_run; calls propagateTombstoneToSuppressions
    - path: app/api/cron/channels-soft-bounce-promote/route.ts
      provides: Vercel Cron endpoint daily invocation
    - path: vercel.json
      provides: cron schedule registrations for the 5 new cron endpoints
    - path: test/channels/closeout/rls-suite.test.ts
      provides: RLS matrix — all 11 new tables × {SELECT, INSERT, UPDATE, DELETE} × cross-tenant denial proof (QA-09)
    - path: test/channels/closeout/qa-gates-coverage.test.ts
      provides: meta-test asserting QA-03..QA-15 each have at least one passing concrete gate test (per <qa_gates> block)
    - path: 5 e2e/channels/*.spec.ts
      provides: Playwright operator journeys covering full lifecycle from create to dispatch to reply to bounce
    - path: chromatic.config.json
      provides: chromatic config covering 5 new component stories
    - path: .planning/phases/223-native-email-messaging-orchestration/223-SUMMARY.md
      provides: phase closeout summary for hand-off to Phase 224
    - path: .planning/STATE.md + ROADMAP.md
      provides: phase 223 status='Complete - verification pending', next phase = 224
  key_links:
    - from: app/api/cron/channels-deliverability-rollup/route.ts
      to: vercel.json
      via: cron schedule "0 * * * *" (every hour)
    - from: app/api/cron/channels-lifecycle-journey-poll/route.ts
      to: vercel.json
      via: cron schedule "*/1 * * * *" (every 60 seconds — verify Vercel Cron min-interval; if 60s not supported, fallback "*/2 * * * *" with poll batch size 2x)
    - from: lib/markos/channels/dispatch/lifecycle-journey-poll.ts
      to: lib/markos/channels/dispatch/recipient-handler.ts (Wave 2)
      via: advanceJourneyRun calls handleRecipient on the step's template+audience
    - from: app/api/cron/channels-tombstone-cascade/route.ts
      to: lib/markos/channels/suppression/bounce-handler.ts (Wave 2)
      via: cron iterates new tombstones and calls propagateTombstoneToSuppressions
    - from: e2e/channels/campaign-create-approve-dispatch-reply-bounce.spec.ts
      to: components/markos/crm/outbound/* (Wave 4)
      via: Playwright drives ChannelProgramsList -> ApprovalReviewPanel -> dispatch -> simulated reply -> simulated bounce
    - from: .planning/STATE.md
      to: .planning/ROADMAP.md
      via: Phase 223 marked complete; next phase 224 readiness asserted
---

<objective>
Wave 5 phase closeout. Ships 5 cron endpoints (deliverability rollup, lifecycle journey poll, bounce spike alert, tombstone cascade, soft-bounce promotion) + vercel.json schedule entries; full RLS suite across all 11 new tables (cross-tenant denial × CRUD matrix); Playwright operator-journey specs for the 5 critical flows; Chromatic snapshot gate; phase SUMMARY + STATE + ROADMAP updates; populates per-task rows in 223-VALIDATION.md (NOTE: `nyquist_compliant: true` flip is owned by execute-phase, not by this plan; the planner only ensures the rows are populated).

Purpose: completes EML-01..05 (all aggregated; reply intelligence + ConsentState cutover verified end-to-end), MSG-01..05 (all aggregated; 5 channels + double-gate + thread + reply + push verified), CDP-01..05 (carry-forward verified through integration tests + tombstone cascade), CRM-01..05 (carry-forward verified through emit + thread + customer 360), QA-01..15 (contracts current, RLS verified, audit log hash chain continuous, tests cover all decision branches, Playwright + Chromatic gates pass, regression preserved on P100-P105 + P221 + P222). The QA-03..QA-15 concrete gate definitions are below in <qa_gates>.

Output: 5 cron endpoints + library implementations, vercel.json updates, 7 closeout test files (RLS suite + 5 cron tests + legacy regression + qa-gates coverage), 5 Playwright e2e specs, 1 Storybook addition (LifecycleJourneyEditor), Chromatic config, 4 planning file updates.
</objective>

<qa_gates>
## QA-03..QA-15 Concrete Gates

QA-01 and QA-02 are already concretely defined in 223-CONTEXT.md / RESEARCH (contracts current; RLS on new tables). QA-03..QA-15 below are derived from V4.0.0-TESTING-ENVIRONMENT-PLAN.md / V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md and the Phase 223 testable surface. Each gate has a grep/test-runnable target. `qa-gates-coverage.test.ts` (Task 2) asserts every QA-XX has at least one corresponding passing test in the channels suite.

| ID | Gate | Satisfied via (test target) | Grep / acceptance signal |
|----|------|------------------------------|--------------------------|
| QA-03 | dispatch idempotency (`dispatch_attempt_id` UNIQUE constraint + handler dedup check) | `test/channels/dispatch/idempotency.test.ts` (223-02 Task 3) + `test/channels/schema/dispatch-tracking.test.ts` (223-02 Task 1) | grep `UNIQUE \(dispatch_attempt_id, event_type\)` in `supabase/migrations/117_*.sql`; `vitest run test/channels/dispatch/idempotency.test.ts` exits 0 |
| QA-04 | webhook signature verification per provider (100% coverage) | `test/channels/webhooks/resend-events.test.ts` + `twilio-events.test.ts` + `knock-events.test.ts` (223-04 Task 3) | grep `verifyWebhookSignature` in all 3 webhook handlers; `vitest run test/channels/webhooks/` exits 0 |
| QA-05 | per-recipient 5-layer gate coverage (consent + suppression + frequency + quiet + jurisdiction) | `test/channels/gate/{consent,suppression,frequency-cap,quiet-hours,jurisdiction}-check.test.ts` + `eligibility-gate.test.ts` (223-02 Task 2) | `vitest run test/channels/gate/` exits 0 with ≥30 assertions covering allow/skip/defer per layer per channel |
| QA-06 | approval threshold matrix (class × count × content × manual × reengagement) | `test/channels/templates/approval-matrix.test.ts` (223-03 Task 3) | `vitest run test/channels/templates/approval-matrix.test.ts` exits 0 with ≥11 assertions |
| QA-07 | reply continuity (MessagingThread + Customer360 + CRM task per channel) | `test/channels/events/reply-continuity.test.ts` (223-04 Task 2) | grep `lookupOrCreateThreadByProfileChannel` in `lib/markos/channels/events/reply-flow.ts`; test exits 0 |
| QA-08 | bounce/complaint/tombstone propagation chain | `test/channels/suppression/bounce-handler.test.ts` (223-02 Task 3) + `test/channels/closeout/tombstone-cascade.test.ts` (this plan Task 1) | grep `propagateTombstoneToSuppressions` in `lib/markos/channels/suppression/bounce-handler.ts`; both tests exit 0 |
| QA-09 | RLS on all 11 new tables | `test/channels/closeout/rls-suite.test.ts` (this plan Task 2) | `vitest run test/channels/closeout/rls-suite.test.ts` exits 0 with ≥44 assertions (11 tables × 4 ops) |
| QA-10 | audit log hash chain integrity preserved across channel writes | `test/channels/closeout/legacy-regression.test.ts` (this plan Task 2) — re-runs P201 hash-chain regression | `vitest run test/channels/closeout/legacy-regression.test.ts` exits 0; hash chain verifier (existing P201) green |
| QA-11 | legacy regression (P100-P105 + P221 + P222 tests green) | `test/channels/closeout/legacy-regression.test.ts` (this plan Task 2) + `npm test` | `npm test` exits 0; legacy-regression.test.ts describe blocks for P100-P105 + P221 + P222 all green |
| QA-12 | pricing binding enforcement (`{{MARKOS_PRICING_ENGINE_PENDING}}` OR Pricing Engine record) | `test/channels/templates/content-classifier.test.ts` (223-03 Task 2) | grep `MARKOS_PRICING_ENGINE_PENDING` in `lib/markos/channels/templates/content-classifier.ts`; test exits 0 |
| QA-13 | evidence binding enforcement (P209 EvidenceMap + TTL check, graceful when P209 absent) | `test/channels/templates/content-classifier.test.ts` (223-03 Task 2) — Tests 5+6 | grep `evidence_ttl` in classifier file; test exits 0 |
| QA-14 | deliverability rollup + reputation state machine | `test/channels/closeout/deliverability-rollup.test.ts` + `test/channels/closeout/spike-alert.test.ts` (this plan Task 1) | both tests exit 0; reputation_status enum transitions warming->healthy->watch->at_risk verified |
| QA-15 | SUMMARY/STATE/ROADMAP updated + per-task rows populated (planner contract) | `node` verify command in this plan Task 3 (`<verify>`) | verify command exits 0; `.planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md` per-task rows populated for all 6 plans |

NOTE on `nyquist_compliant`: per execute-phase ownership convention, the `nyquist_compliant: true` flip is performed by execute-phase after running the full automated suite, not by the planner. Task 3 below populates rows and writes SUMMARY/STATE/ROADMAP only.
</qa_gates>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md
@.planning/phases/223-native-email-messaging-orchestration/223-RESEARCH.md
@.planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md
@.planning/phases/223-native-email-messaging-orchestration/223-01-SUMMARY.md
@.planning/phases/223-native-email-messaging-orchestration/223-02-SUMMARY.md
@.planning/phases/223-native-email-messaging-orchestration/223-03-SUMMARY.md
@.planning/phases/223-native-email-messaging-orchestration/223-04-SUMMARY.md
@.planning/phases/223-native-email-messaging-orchestration/223-05-SUMMARY.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/V4.0.0-TESTING-ENVIRONMENT-PLAN.md
@.planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md
@CLAUDE.md
@vercel.json
@lib/markos/channels/deliverability/posture.ts
@lib/markos/channels/dispatch/dispatch-coordinator.ts
@lib/markos/channels/suppression/bounce-handler.ts
@supabase/migrations/117_channel_dispatch_tracking.sql
@.storybook/main.cjs
@playwright.config.ts
@chromatic.config.json
@obsidian/work/incoming/19-EMAIL-ENGINE.md
# informational only — decisions already locked in 223-CONTEXT.md per CLAUDE.md §source-of-truth precedence
@obsidian/work/incoming/21-MESSAGING-ENGINE.md
# informational only — decisions already locked in 223-CONTEXT.md per CLAUDE.md §source-of-truth precedence
</context>

<interfaces>
<!-- Existing P222 cron pattern (reference) -->
```typescript
// app/api/cron/crm360-drift/route.ts (P222) — pattern to mirror
export const POST = async (req: Request) => {
  const isVercelCron = req.headers.get('x-vercel-cron') === '1';
  if (!isVercelCron) return new Response('Forbidden', { status: 403 });
  await runForAllTenants(async (tenantId) => { ... });
  return new Response('OK', { status: 200 });
};
```

<!-- Spike alert detection -->
```typescript
// detectBounceSpike(tenantId, channel, sender_id):
//   baseline_window = past 7 days; current_window = past 24 hours
//   baseline_mean = AVG(bounce_rate) over baseline_window
//   baseline_stddev = STDDEV(bounce_rate) over baseline_window
//   if current_window.bounce_rate > baseline_mean + 2 * baseline_stddev AND sample_size > 50 -> spike
//   on spike -> create operator task with reasoning + sender card link
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crons + library implementations — deliverability rollup, lifecycle journey poll, bounce spike alert, tombstone cascade, soft-bounce promotion</name>
  <read_first>
    - app/api/cron/crm360-drift/route.ts (P222 cron pattern reference; if absent, use vercel.json + any prior cron route as template)
    - vercel.json (current cron registrations)
    - .planning/phases/223-native-email-messaging-orchestration/223-RESEARCH.md (§Lifecycle Journey Trigger Strategy §Frequency Cap §Pitfalls 4)
    - .planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md (D-07, D-21, D-35, D-04)
    - lib/markos/channels/dispatch/dispatch-coordinator.ts + recipient-handler.ts (Wave 2)
    - lib/markos/channels/suppression/bounce-handler.ts (Wave 2 — propagateTombstoneToSuppressions, handleBounceEvent)
    - lib/markos/channels/programs/lifecycle-journey.ts + journey-run.ts (Wave 1+3)
    - supabase/migrations/117_channel_dispatch_tracking.sql (dispatch_events shape)
    - test/fixtures/channels/dispatch.ts + suppression.ts
  </read_first>
  <behavior>
    - Test 1 (deliverability rollup): given dispatch_events seeded with mix of delivered/bounced_hard/bounced_soft/complained over the last 24h, aggregateDeliverabilityPosture inserts a deliverability_posture row with bounce_rate = (hard+soft) / sample_size, complaint_rate = complaints / sample_size, reputation_score = 100 - (bounce_rate*60 + complaint_rate*40) clamped [0,100], trend derived from window-over-window delta (improving/stable/watch/degrading thresholds: +5/+0/-5/-10).
    - Test 2 (deliverability rollup idempotency): re-running for the same window_24h_start UPSERTs (no duplicate row).
    - Test 3 (lifecycle journey poll happy path): seeded lifecycle_journey_runs row with state='pending' AND due_at < now() -> advanceJourneyRun runs the step's template through recipient-handler -> emit -> dispatch_events row + advance current_step + set due_at = now() + step.delay_seconds for next step. State transitions: pending -> in_progress -> completed when last step done.
    - Test 4 (lifecycle journey poll fail-closed at gate): step's recipient blocked by eligibility-gate -> dispatch_skips row + journey_run state='paused' (NOT 'failed'); operator must intervene.
    - Test 5 (spike alert detection): seeded baseline 7d bounce_rate=0.02 stddev=0.005; current 24h bounce_rate=0.05 -> 0.05 > 0.02 + 2*0.005 = 0.03 -> spike detected -> operator task created with reason='bounce_spike_detected', sender_id link, suggested action='pause broadcast class until reputation_status returns to healthy'.
    - Test 6 (spike alert sample-size threshold): same spike with sample_size=10 (< 50 threshold) -> no task (insufficient signal).
    - Test 7 (tombstone cascade): seed cdp_identity_profiles row with tombstoned_at=now and tombstoned_since_last_run=true -> cron iterates -> propagateTombstoneToSuppressions called -> channel_suppressions row added for each of 5 channels with reason='dsr_deletion'.
    - Test 8 (soft-bounce promotion): seed dispatch_events with 3 bounced_soft for same (profile_id, channel, last 7 days) -> promoteSoftBounces calls handleBounceEvent('hard') -> channel_suppressions row reason='hard_bounce' + cdp_events emit + audit_log row.
    - Test 9 (cron auth): all 5 cron endpoints reject requests missing x-vercel-cron=1 (403); accept when present.
    - Test 10 (vercel.json schedules): file contains all 5 new cron path registrations with appropriate schedules.
  </behavior>
  <action>
    1. Create `lib/markos/channels/deliverability/posture-rollup.ts`:
       - Export `aggregateDeliverabilityPosture({ tenantId, channel, sender_id, window_24h_start })`:
         - SELECT counts of dispatch_events grouped by event_type for the window.
         - Compute rates per Test 1 formulas.
         - Derive trend by comparing to previous 24h window.
         - INSERT INTO deliverability_posture ON CONFLICT (composite key) DO UPDATE.
       - Export `runForAllTenantsAndSenders()` — convenience runner used by cron endpoint.
    2. Create `lib/markos/channels/deliverability/spike-alert.ts`:
       - Export `detectBounceSpike({ tenantId, channel, sender_id })`:
         - Read past 7d baseline from deliverability_posture (excluding current window).
         - Read current 24h window.
         - If current > baseline_mean + 2*stddev AND sample_size > 50 -> return { spike: true, reason }.
       - Export `createSpikeOperatorTask({ tenantId, sender_id, channel, baseline, current })` — calls existing tasks lib (P208) to create task.
    3. Create `lib/markos/channels/dispatch/lifecycle-journey-poll.ts`:
       - Export `pollDueJourneyRuns(tenantId)`:
         - SELECT lifecycle_journey_runs WHERE state='pending' AND due_at <= now() AND tenant_id LIMIT 100 (per-cycle batch).
         - For each row: read journey.step_definitions[current_step]; build dispatch context; call recipient-handler.handleRecipient.
         - On success: advance current_step += 1; set due_at = now() + step.delay_seconds; update state.
         - On gate skip/defer: handle per Test 4 (state='paused').
         - On failure (adapter error): state='failed', emit operator task.
    4. Create `lib/markos/channels/dispatch/soft-bounce-promotion.ts`:
       - Export `promoteSoftBounces(tenantId)`:
         - SELECT (profile_id, channel, COUNT(*)) FROM dispatch_events WHERE event_type='bounced_soft' AND occurred_at > now()-INTERVAL '7 days' AND tenant_id GROUP BY (profile_id, channel) HAVING COUNT(*) >= 3.
         - For each: handleBounceEvent({ severity: 'hard', tenantId, profileId, channel, dispatchAttemptId: <last attempt> }).
    5. Create 5 Vercel Cron route files under `app/api/cron/`:
       - `channels-deliverability-rollup/route.ts` — POST, requires x-vercel-cron=1, calls runForAllTenantsAndSenders.
       - `channels-lifecycle-journey-poll/route.ts` — same auth, calls pollDueJourneyRuns for all tenants.
       - `channels-bounce-spike-alert/route.ts` — same auth, iterates senders × tenants.
       - `channels-tombstone-cascade/route.ts` — same auth; SELECT cdp_identity_profiles WHERE tombstoned_at > coalesce(last_run, '1970-01-01') AND tombstoned_at <= now(); for each call propagateTombstoneToSuppressions; persist last_run watermark in tenants config or a dedicated `markos_cron_watermarks` table (use existing watermark pattern from P203 webhook engine if present, else add ALTER tenants.config jsonb path).
       - `channels-soft-bounce-promote/route.ts` — same auth, calls promoteSoftBounces.
    6. Update `vercel.json` with cron entries (validate against Vercel cron min interval — `*/1 * * * *` may not be supported; fallback to `*/2 * * * *` AND batch size 2x in pollDueJourneyRuns):
       ```
       { "path": "/api/cron/channels-deliverability-rollup", "schedule": "0 * * * *" }       // hourly
       { "path": "/api/cron/channels-lifecycle-journey-poll",  "schedule": "*/2 * * * *" }    // every 2 minutes (Vercel min)
       { "path": "/api/cron/channels-bounce-spike-alert",      "schedule": "0 4 * * *" }      // daily 04:00 UTC
       { "path": "/api/cron/channels-tombstone-cascade",       "schedule": "0 5 * * *" }      // daily 05:00 UTC
       { "path": "/api/cron/channels-soft-bounce-promote",     "schedule": "0 6 * * *" }      // daily 06:00 UTC
       ```
       (Verify current vercel.json schema; add to existing cron[] array.)
    7. Create test files:
       - `test/channels/closeout/deliverability-rollup.test.ts` (Tests 1, 2).
       - `test/channels/closeout/spike-alert.test.ts` (Tests 5, 6).
       - `test/channels/closeout/lifecycle-journey-poll.test.ts` (Tests 3, 4).
       - `test/channels/closeout/tombstone-cascade.test.ts` (Test 7).
       - `test/channels/closeout/soft-bounce-promotion.test.ts` (Test 8).
       - Add cron-auth assertions (Test 9) inline in each cron-related test file.
    8. Append VALIDATION rows.
  </action>
  <verify>
    <automated>vitest run test/channels/closeout/deliverability-rollup.test.ts test/channels/closeout/spike-alert.test.ts test/channels/closeout/lifecycle-journey-poll.test.ts test/channels/closeout/tombstone-cascade.test.ts test/channels/closeout/soft-bounce-promotion.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 5 library files exist under `lib/markos/channels/deliverability/` and `lib/markos/channels/dispatch/`
    - 5 cron route files exist under `app/api/cron/channels-*/route.ts`
    - File `vercel.json` contains all 5 new cron paths (grep `channels-deliverability-rollup` AND `channels-lifecycle-journey-poll` AND `channels-bounce-spike-alert` AND `channels-tombstone-cascade` AND `channels-soft-bounce-promote`)
    - `vitest run test/channels/closeout/deliverability-rollup.test.ts test/channels/closeout/spike-alert.test.ts test/channels/closeout/lifecycle-journey-poll.test.ts test/channels/closeout/tombstone-cascade.test.ts test/channels/closeout/soft-bounce-promotion.test.ts` exits 0
    - Cron auth: each route rejects requests without `x-vercel-cron: 1` (test asserts 403)
    - Tombstone cascade test: P221 tombstone -> 5 channel_suppressions rows verified (one per channel) — satisfies QA-08
    - Deliverability rollup test verifies reputation_score formula + trend transitions — satisfies QA-14
    - Spike alert test verifies >2σ detection + sample_size threshold — satisfies QA-14
  </acceptance_criteria>
  <done>5 crons + 5 libraries operational; deliverability rollup, lifecycle poll, spike alert, tombstone cascade, soft-bounce promotion all green and on schedule; QA-08 + QA-14 concrete gates green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Full RLS suite (11 tables × CRUD × cross-tenant) + legacy regression suite (P100-P105 + P221 + P222) + Playwright operator journeys + Chromatic gate + QA gates coverage meta-test</name>
  <read_first>
    - test/crm360/rls-suite.test.ts (P222 RLS suite reference if exists; else build from P201 RLS pattern)
    - test/fixtures/channels/* (all)
    - playwright.config.ts (existing config from P221 Wave 0)
    - .storybook/main.* (Storybook config)
    - chromatic.config.json (current chromatic gate config)
    - .planning/phases/223-native-email-messaging-orchestration/223-RESEARCH.md (§Validation Architecture §Slice 223-06 §Tests Implied)
    - .planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md (D-36, D-37)
    - .planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md
    - components/markos/crm/outbound/* (Wave 4 outputs — for Playwright selectors)
  </read_first>
  <behavior>
    - Test 1 (RLS suite): for each of 11 new tables × {SELECT, INSERT, UPDATE, DELETE} × cross-tenant: with app.tenant_id set to tenant_A, attempt operation on a row owned by tenant_B -> denied (SELECT returns empty; INSERT/UPDATE/DELETE no-op or error). 11 × 4 = 44 assertions minimum. Satisfies QA-09.
    - Test 2 (legacy regression P100-P105): outbound test fixtures from P104 still pass with the cutover (A12 mock injection from 223-02). Satisfies QA-11.
    - Test 3 (legacy regression P221): all P221 cdp-* tests still pass — proves view-swap migration 119 + getConsentState reads + tombstone cascade did not break P221 surfaces. Satisfies QA-11.
    - Test 4 (legacy regression P222): all P222 crm360-* tests still pass — proves emit() fan-out + thread.related_crm_id link + commercial_signal mapping did not regress P222. Satisfies QA-11.
    - Test 5 (audit log hash chain): re-runs P201 hash chain verifier across all events written during channel test runs (campaign, dispatch, suppression, approval, consent change). Hash chain remains continuous. Satisfies QA-10.
    - Test 6 (Playwright: campaign create -> approve -> dispatch -> reply -> bounce):
      1. Operator logs in (existing P201 auth flow).
      2. Navigates to /crm/outbound -> ChannelProgramsList.
      3. Creates broadcast email campaign (audience >= 500 to trigger approval). Selects valid template + sender. Saves -> status='pending_approval'.
      4. Approval Inbox shows new entry with triggerReason='class_broadcast,count_threshold'. Approver clicks approve.
      5. Campaign status transitions: approved -> dispatching (via dispatch endpoint mocked).
      6. Simulated inbound reply via webhook fixture: thread.current_status='reply_pending'; CRM task visible in operator queue.
      7. Simulated hard bounce via webhook fixture: channel_suppressions row gains entry; dispatch_events shows bounced_hard; cdp_events shows suppression_added.
    - Test 7 (Playwright: messaging thread reply flow): operator opens MessagingThreadsList -> clicks a reply_pending thread -> sends reply via /v1/channels/messaging/sends -> thread.last_direction='outbound'.
    - Test 8 (Playwright: lifecycle journey create + pause + resume): operator creates 3-step journey -> verifies journey_run rows pending -> pauses journey -> verifies state='paused' -> resumes -> verifies state='active'.
    - Test 9 (Playwright: template editor pricing classifier): operator creates template with pricing-shaped raw text -> classifier finding banner appears -> save blocked with 422.
    - Test 10 (Playwright: deliverability workspace warming -> healthy transition): seeded sender warming with sample_size=600 + bounce_rate=0.01 -> reputation_status badge changes to healthy on next rollup.
    - Test 11 (Chromatic gate): 5 components × 4 variants each = 20 stories; all snapshots either exact match OR explicitly approved. CI gate fails on unapproved diff.
    - Test 12 (QA gates coverage meta-test): qa-gates-coverage.test.ts asserts every QA-XX in the phase requirements list (QA-01..QA-15) maps to at least one passing test in the channels suite per the <qa_gates> mapping table. Implementation: hardcoded mapping object; for each QA-XX, locate the listed test file and assert it exists + was green in the most recent run (read vitest cache or re-run). Fails build if any QA-XX has no green test.
  </behavior>
  <action>
    1. Create `test/channels/closeout/rls-suite.test.ts`:
       - Helper: `withTenantContext(tenantId, fn)` sets app.tenant_id session config.
       - Iterate 11 tables × 4 operations × 2 tenants:
         - Seed row in tenant_A.
         - Switch context to tenant_B; attempt op; assert denied.
         - Switch back to tenant_A; attempt op; assert success.
       - Final assertion: total assertions >= 44.
    2. Create `test/channels/closeout/legacy-regression.test.ts`:
       - Top-level describe: P100-P105 outbound regression.
       - Imports and re-runs the existing test suites under test/outbound-* (or whatever path P104 uses) with the new ConsentState backend (A12 mock injection pattern).
       - Top-level describe: P221 carry-forward — runs the P221 suite. Top-level describe: P222 carry-forward — runs the P222 suite.
       - Top-level describe: P201 audit log hash chain — invokes the existing hash chain verifier across all channel-domain audit_log rows from the test run.
       - Output: legacy-regression-summary.json captured for SUMMARY artifacts.
    3. Create `test/channels/closeout/qa-gates-coverage.test.ts`:
       - Hardcode mapping per the <qa_gates> table in this plan: { 'QA-01': 'test/channels/schema/baseline.test.ts', 'QA-02': 'test/channels/schema/', 'QA-03': 'test/channels/dispatch/idempotency.test.ts', ... }.
       - For each QA-XX in mapping: assert file (or directory) exists; then run the test (or rely on run-once cache) and assert exit code 0.
       - Emit a coverage table to console for reviewer verification.
    4. Create 5 Playwright spec files under `e2e/channels/`:
       - `campaign-create-approve-dispatch-reply-bounce.spec.ts` (Test 6).
       - `messaging-thread-reply-flow.spec.ts` (Test 7).
       - `lifecycle-journey-create-pause-resume.spec.ts` (Test 8).
       - `template-editor-pricing-classifier.spec.ts` (Test 9).
       - `deliverability-workspace-warming.spec.ts` (Test 10).
       Each spec: use page object pattern; mock provider webhooks via fixtures; assert UI state transitions + DB side effects via test API endpoint.
    5. Add `LifecycleJourneyEditor.stories.tsx` Storybook story (mentioned in VALIDATION 223-06 row) — covers Empty + 3-step + Paused variants. (Note: LifecycleJourneyEditor component itself can either be added inline in this task as a thin wrapper around existing form primitives OR documented as deferred to P224 if its full implementation requires more scope. If component does not exist, create a stub component that renders an editor for journey.step_definitions[] using the existing form primitive library.)
    6. Update `chromatic.config.json` to include the 5 new component story files.
    7. Append VALIDATION rows.
  </action>
  <verify>
    <automated>vitest run test/channels/closeout/rls-suite.test.ts test/channels/closeout/legacy-regression.test.ts test/channels/closeout/qa-gates-coverage.test.ts && playwright test e2e/channels/ && npx chromatic --project-token=${CHROMATIC_PROJECT_TOKEN:-dummy} --exit-zero-on-changes</automated>
  </verify>
  <acceptance_criteria>
    - File `test/channels/closeout/rls-suite.test.ts` covers ≥44 assertions (11 tables × 4 ops); test exits 0 (QA-09)
    - File `test/channels/closeout/legacy-regression.test.ts` includes P100-P105, P221, P222, P201 hash-chain describe blocks; exits 0 (QA-10 + QA-11)
    - File `test/channels/closeout/qa-gates-coverage.test.ts` exists; asserts QA-01..QA-15 each have at least one green test mapping; exits 0 (planner contract)
    - 5 Playwright spec files exist under `e2e/channels/`
    - `playwright test e2e/channels/` exits 0 (run sequentially or in parallel per existing config)
    - File `.storybook/stories/LifecycleJourneyEditor.stories.tsx` exists OR documented stub note
    - File `chromatic.config.json` includes the 5 new component story keys
    - No P100-P105 + P221 + P222 regression failures (legacy-regression.test.ts green)
  </acceptance_criteria>
  <done>RLS matrix proven (QA-09); legacy regression green for P100-P105 + P221 + P222 (QA-11); audit hash chain continuous (QA-10); 5 Playwright operator journeys green; Chromatic gate covers 5 new components with 4 variants each; QA-01..QA-15 each have a green concrete-gate test (planner coverage contract).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Phase SUMMARY + STATE.md + ROADMAP.md updates + per-task VALIDATION rows populated (planner contract; nyquist flip belongs to execute-phase)</name>
  <read_first>
    - .planning/STATE.md (current shape)
    - .planning/ROADMAP.md (Phase 223 entry)
    - .planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md (verify per-task rows populated)
    - .planning/phases/222-crm-timeline-commercial-memory-workspace/222-SUMMARY.md (template — if exists; else use phase 204-*-SUMMARY for template)
    - .planning/phases/223-native-email-messaging-orchestration/223-01-SUMMARY.md through 223-05-SUMMARY.md (aggregate inputs)
  </read_first>
  <behavior>
    - Test 1 (VALIDATION rows complete): every plan 223-01..223-06 has at least one per-task row in 223-VALIDATION.md §Per-Task Verification Map (15-18 rows total).
    - Test 2 (SUMMARY shape): 223-SUMMARY.md contains sections — Goals, Plans Shipped, Migrations (113-120), Contracts (F-122..F-131), Components (5 new), Crons (5 new), Tests (counts per category), Open Risks, Hand-Off Notes, QA-01..QA-15 coverage table.
    - Test 3 (STATE.md update): current_position for Phase 223 advanced to "Verification pending" or equivalent; next_actions list points to 224-* if planned.
    - Test 4 (ROADMAP.md update): Phase 223 Plans listed with status checkboxes; aggregate status updated to "Complete - verified pending" or equivalent.
    - Test 5 (planner contract — no nyquist flip): VALIDATION.md frontmatter `nyquist_compliant` is left at `false` (or its current value); the planner does NOT flip it. Execute-phase owns the flip after running the full automated suite.
  </behavior>
  <action>
    1. Update `.planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md`:
       - Verify per-task rows populated for all 6 plans (223-01-1 through 223-06-3); add any missing rows pulling the `<verify>` `<automated>` command from each task as the row's "Automated Command" cell.
       - Do NOT flip `nyquist_compliant`. Leave it as current value. Add a comment line: "<!-- nyquist_compliant flip is owned by execute-phase per get-shit-done convention; planner only populates rows. -->"
       - `wave_0_complete` likewise NOT flipped here; that is execute-phase's responsibility once Wave 0 (223-01 Task 1) completes. Leave at `false`.
    2. Create `.planning/phases/223-native-email-messaging-orchestration/223-SUMMARY.md` with:
       - Phase Goals achieved (per CONTEXT.md scope).
       - Plans shipped: 6 plans across 5 waves.
       - Migrations landed: 113-120 (8 new — full DDL hash recorded).
       - Contracts published: F-122..F-131 (10 new).
       - Library modules: lib/markos/channels/* tree complete.
       - Components: 5 new + outbound-workspace evolution + TimelineDetailView extension + 2 P208 entry-type renderers.
       - Crons: 5 new (deliverability rollup, lifecycle journey poll, bounce spike alert, tombstone cascade, soft-bounce promotion).
       - Tests: counts per category (vitest unit, vitest integration, playwright e2e, chromatic, legacy regression).
       - QA-01..QA-15 coverage table: copy from <qa_gates> block above with status column.
       - Risks open at phase end (e.g., Vercel Queues public beta SLAs, Knock pricing tier limits, Resend inbound metadata-only quirk).
       - Hand-off notes: P224 conversion surfaces consume EmailCampaign + LifecycleJourney shapes; P225 attribution consumes dispatch_events + cdp_events.
       - Carry-forward verification: CDP-01..05 + CRM-01..05 marked satisfied via integration tests in 223-06 closeout.
    3. Update `.planning/STATE.md`:
       - current_position.phase = "223 (native-email-messaging-orchestration) — verification pending"
       - progress: increment completed_plans by 6.
       - next_actions: "Run /gsd-verify-work for Phase 223 -> advance to Phase 224 (conversion-launch-workspace)"
    4. Update `.planning/ROADMAP.md` Phase 223 section:
       - Status: "Complete - verification pending" (or matching project convention).
       - Plans list: all 6 plans with [x] checkboxes.
       - Add summary line: "v4.2.0 commercial engines: Phase 223 (native channels) shipped; 8 migrations + 10 contracts + 11 new tables + Vercel Queues fan-out + Knock push + ConsentState cutover + 5 cron jobs."
    5. Sanity-check: every requirement ID in the phase header (EML-01..05, MSG-01..05, CDP-01..05, CRM-01..05, QA-01..15) appears in at least one plan's `requirements` field. Output a coverage table.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const v = fs.readFileSync('.planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md','utf8'); const rows = (v.match(/\| 223-\d{2}-\d+ \|/g) || []).length; if (rows < 15) { console.error('VALIDATION rows insufficient: ' + rows + ' (expected >=15)'); process.exit(1); } const s = fs.readFileSync('.planning/phases/223-native-email-messaging-orchestration/223-SUMMARY.md','utf8'); if (!s.match(/Phase Goals/) || !s.match(/Hand-Off/) || !s.match(/QA-01/)) { console.error('SUMMARY missing required sections'); process.exit(1); } const r = fs.readFileSync('.planning/ROADMAP.md','utf8'); if (!r.match(/223-06-PLAN.md/)) { console.error('ROADMAP missing 223-06 reference'); process.exit(1); } console.log('OK rows=' + rows); process.exit(0);"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md` has ≥15 per-task rows (covers all 6 plans × all tasks)
    - File `.planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md` `nyquist_compliant` value left UNCHANGED by this task (planner must not flip)
    - File `.planning/phases/223-native-email-messaging-orchestration/223-SUMMARY.md` exists with sections "Phase Goals", "Plans Shipped", "Migrations", "Contracts", "Components", "Crons", "Tests", "QA-01..QA-15 Coverage", "Open Risks", "Hand-Off Notes"
    - File `.planning/STATE.md` references Phase 223 status update
    - File `.planning/ROADMAP.md` Phase 223 plans listed with checkboxes
    - All requirement IDs from phase header (EML/MSG/CDP/CRM/QA) appear in at least one plan's `requirements` field (validated via grep aggregation)
    - Verify command above exits 0
  </acceptance_criteria>
  <done>VALIDATION rows populated; SUMMARY captured; STATE + ROADMAP updated; phase ready for /gsd-verify-work invocation (which owns the nyquist flip).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel Cron header -> route handler | x-vercel-cron=1 enforces cron-only invocation |
| cron iteration -> per-tenant runner | runForAllTenants must scope to tenant_id; no cross-tenant leakage |
| Playwright fixtures -> webhook simulation | Fixture-driven webhook payloads exercise emit + bounce-handler paths in deterministic order |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-223-06-01 | Spoofing | rogue invocation of cron endpoint | mitigate | x-vercel-cron=1 header check on every cron route; failure -> 403 |
| T-223-06-02 | Information Disclosure | tombstone cascade reads cross-tenant profiles | mitigate | propagateTombstoneToSuppressions scoped per-tenant; cron iterator uses tenant-scoped query loop |
| T-223-06-03 | Denial of Service | unbounded lifecycle journey poll batch | mitigate | LIMIT 100 per cycle; spread load via cron interval |
| T-223-06-04 | Tampering | RLS misconfig on a single new table allows cross-tenant write | mitigate | rls-suite.test.ts 11 tables × 4 ops × 2 tenants matrix |
| T-223-06-05 | Repudiation | spike alert task created without audit | mitigate | createSpikeOperatorTask emits audit_log row alongside task |
| T-223-06-06 | Elevation | Playwright fixtures simulate operator approval without auth | accept | Playwright runs against test env only; production gates auth |
</threat_model>

<verification>
- 5 cron endpoints + 5 library implementations + vercel.json schedules all green.
- Full RLS suite (11 × 4 × cross-tenant denial) green (QA-09).
- Legacy P100-P105 + P221 + P222 regression green (QA-11).
- Audit log hash chain continuous (QA-10).
- 5 Playwright operator journeys green.
- Chromatic gate covers 5 new components × 4 variants each.
- 223-SUMMARY.md + STATE.md + ROADMAP.md updated.
- 223-VALIDATION.md per-task rows populated; `nyquist_compliant` left unchanged (execute-phase owns the flip).
- All 35 requirement IDs from phase header verified present in plan `requirements` fields.
- QA-01..QA-15 each have a green concrete-gate test per qa-gates-coverage.test.ts.
</verification>

<success_criteria>
- Phase 223 ships:
  - 8 migrations (113-120)
  - 10 contracts (F-122..F-131)
  - 11 new tables RLS-verified
  - 5 channel adapters (resend extended, twilio extended, knock new, in_app new, base extended)
  - Per-recipient 5-layer gate
  - Single fan-out emit() with commercial_signal map
  - Reply continuity flow with auto-reply filter
  - 18 API routes + 5 MCP tools
  - 5 new UI components + outbound-workspace evolution + TimelineDetailView extension
  - 5 cron jobs (deliverability, lifecycle, spike, tombstone, soft-bounce)
  - Full Playwright + Chromatic gate
- Carry-forward CDP-01..05 + CRM-01..05 verified through integration tests.
- QA-01..QA-15 each have a concrete grep/test-runnable gate (see <qa_gates>).
- Phase ready for /gsd-verify-work invocation.
</success_criteria>

<output>
After completion, the phase is ready for `/gsd-verify-work` invocation. The Wave 5 outputs include `.planning/phases/223-native-email-messaging-orchestration/223-SUMMARY.md` (final phase summary), `.planning/STATE.md` (advanced to verification-pending), and `.planning/ROADMAP.md` (Phase 223 plans checked off). Next phase: **224 (conversion-launch-workspace)** which consumes EmailCampaign + LifecycleJourney shapes for landing pages, forms, CTAs, and launch orchestration.
</output>
