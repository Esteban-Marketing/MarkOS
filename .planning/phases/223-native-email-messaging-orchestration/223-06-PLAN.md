---
phase: 223
plan: "06"
type: execute
wave: 5
depends_on: ["223-01", "223-02", "223-03", "223-04", "223-05"]
files_modified:
  - lib/markos/channels/deliverability/posture-rollup.ts
  - lib/markos/channels/deliverability/spike-alert.ts
  - lib/markos/channels/dispatch/lifecycle-journey-poll.ts
  - lib/markos/channels/dispatch/soft-bounce-promotion.ts
  - api/cron/channels-deliverability-rollup.js
  - api/cron/channels-lifecycle-journey-poll.js
  - api/cron/channels-bounce-spike-alert.js
  - api/cron/channels-tombstone-cascade.js
  - api/cron/channels-soft-bounce-promote.js
  - vercel.json
  - test/channels/closeout/rls-suite.test.js
  - test/channels/closeout/deliverability-rollup.test.js
  - test/channels/closeout/spike-alert.test.js
  - test/channels/closeout/lifecycle-journey-poll.test.js
  - test/channels/closeout/tombstone-cascade.test.js
  - test/channels/closeout/soft-bounce-promotion.test.js
  - test/channels/closeout/legacy-regression.test.js
  - test/channels/closeout/qa-gates-coverage.test.js
  - test/channels/closeout/f-id-collision.test.js
  - test/channels/closeout/cron-routes-architecture-lock.test.js
  - .storybook/stories/LifecycleJourneyEditor.stories.tsx
  - chromatic.config.json
  - .planning/phases/223-native-email-messaging-orchestration/223-SUMMARY.md
  - .planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false  # checkpoint task for operator sender warming + DKIM/SPF/DMARC + Knock provisioning per RL1
requirements:
  - EML-01
  - EML-02
  - EML-03
  - EML-04
  - EML-05
  - MSG-01
  - MSG-02
  - MSG-03
  - MSG-04
  - MSG-05
  - CDP-01
  - CDP-02
  - CDP-03
  - CDP-04
  - CDP-05
  - CRM-01
  - CRM-02
  - CRM-03
  - CRM-04
  - CRM-05
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - QA-05
  - QA-06
  - QA-07
  - QA-08
  - QA-09
  - QA-10
  - QA-11
  - QA-12
  - QA-13
  - QA-14
  - QA-15
must_haves:
  truths:
    - "All 5 cron handlers ship as legacy api/cron/*.js per D-49 (NOT App Router app/api/cron/*/route.ts). Auth wrapper: shared-secret header x-markos-cron-secret matching process.env.MARKOS_WEBHOOK_CRON_SECRET (mirrors existing api/cron/webhooks-dlq-purge.js verified pattern at lines 20-30)."
    - "Plan 06 owns NO new F-IDs (all F-122..F-131 published in Plans 01-05). Plan 06 closeout runs F-ID-collision regression test (RL2) verifying flow_id uniqueness across contracts/F-*.yaml."
    - "All 11 new tables (sender_identities, deliverability_posture, channel_templates, email_campaigns, messaging_threads, lifecycle_journeys, lifecycle_journey_runs, channel_suppressions, dispatch_events, dispatch_skips, in_app_messages) RLS-verified cross-tenant denied via the rls-suite.test.js matrix (CRUD × all tables)"
    - "Hourly deliverability_posture rollup cron runs and produces window_24h_start row with reputation_score correctly derived from bounce_rate + complaint_rate (D-07)"
    - "Bounce/complaint spike alert (>2σ from 7-day baseline) fires operator task — D-35"
    - "Lifecycle journey poll runs every 2 minutes (Vercel Cron min interval); due lifecycle_journey_runs are advanced (RESEARCH §Research Decision 4)"
    - "Soft-bounce promotion cron promotes recipients with 3+ soft_bounce occurrences in 7 days to hard_bounce + suppression (D-21)"
    - "Tombstone cascade: P221 D-24 profile tombstone -> propagateTombstoneToSuppressions writes channel_suppressions row across all 5 channels (D-21 + P221 D-24 + P222 D-32)"
    - "Legacy regression: P100-P105 + P221 + P222 test suites all green (no regressions from substitution work) — verified via npm test"
    - "Operator-journey: campaign create -> approve -> dispatch -> reply -> bounce in one E2E test using existing axe-playwright (NOT a new playwright runtime per D-46 — only the existing accessibility test infrastructure is reused)"
    - "Chromatic visual regression covers all 5 new components × 4 variant states each (Empty + Populated + Error + Tombstoned)"
    - "223-SUMMARY.md captures phase outcomes; STATE.md reflects phase closure; ROADMAP.md marks Phase 223 status complete with hand-off to 224"
    - "Per RL1: this plan has autonomous: false because Task 4 (NEW) is a checkpoint:human-action gate for operator sender warming + DKIM/SPF/DMARC verification + Knock app provisioning + Resend dashboard webhook configuration — actions that require external dashboards and cannot be automated by Claude."
    - "QA-03..QA-15 concrete gates each have a grep/test-runnable target (see <qa_gates> block); qa-gates-coverage.test.js asserts each gate has at least one passing test in the suite"
  artifacts:
    - path: lib/markos/channels/deliverability/posture-rollup.ts
      provides: aggregateDeliverabilityPosture(tenantId, channel, window_24h_start) — computes rates from dispatch_events; INSERT/UPSERT into deliverability_posture; sets reputation_score + trend
    - path: lib/markos/channels/deliverability/spike-alert.ts
      provides: detectBounceSpike(tenantId, channel, sender_id) — 7-day baseline mean + stddev; if current 24h > baseline + 2σ -> create operator task
    - path: lib/markos/channels/dispatch/lifecycle-journey-poll.ts
      provides: pollDueJourneyRuns(tenantId) — SELECT lifecycle_journey_runs WHERE state='pending' AND due_at <= now() AND tenant_id; advanceJourneyRun per row (runs gate -> emit + advance step)
    - path: lib/markos/channels/dispatch/soft-bounce-promotion.ts
      provides: promoteSoftBounces(tenantId) — count soft_bounce in 7d per (profile, channel); >=3 -> handleBounceEvent('hard') + audit
    - path: api/cron/channels-deliverability-rollup.js
      provides: legacy cron endpoint (D-49) hourly invocation; auth via x-markos-cron-secret shared header (mirrors webhooks-dlq-purge.js pattern)
    - path: api/cron/channels-lifecycle-journey-poll.js
      provides: legacy cron endpoint (D-49) every 2 minutes invocation
    - path: api/cron/channels-bounce-spike-alert.js
      provides: legacy cron endpoint (D-49) daily invocation
    - path: api/cron/channels-tombstone-cascade.js
      provides: legacy cron endpoint (D-49) daily — reads cdp_identity_profiles where tombstoned_at > last_run; calls propagateTombstoneToSuppressions
    - path: api/cron/channels-soft-bounce-promote.js
      provides: legacy cron endpoint (D-49) daily invocation
    - path: vercel.json
      provides: cron schedule registrations for the 5 new cron endpoints (legacy /api/cron/channels-*.js paths per D-49)
    - path: test/channels/closeout/rls-suite.test.js
      provides: RLS matrix — all 11 new tables × {SELECT, INSERT, UPDATE, DELETE} × cross-tenant denial proof (QA-09)
    - path: test/channels/closeout/qa-gates-coverage.test.js
      provides: meta-test asserting QA-03..QA-15 each have at least one passing concrete gate test (per <qa_gates> block)
    - path: test/channels/closeout/f-id-collision.test.js
      provides: regression test (RL2 / D-55) asserting flow_id uniqueness across contracts/F-*.yaml; fails if any F-122..F-131 (or any other F-ID) duplicates
    - path: test/channels/closeout/cron-routes-architecture-lock.test.js
      provides: regression test asserting all 5 new cron files are at api/cron/*.js (NOT app/api/cron/*/route.ts) per D-49; verifies vercel.json paths use /api/cron/channels-* (NOT /app/api/cron/...)
    - path: chromatic.config.json
      provides: chromatic config covering 5 new component stories
    - path: .planning/phases/223-native-email-messaging-orchestration/223-SUMMARY.md
      provides: phase closeout summary for hand-off to Phase 224
    - path: .planning/STATE.md + ROADMAP.md
      provides: phase 223 status='Complete - verification pending', next phase = 224
  key_links:
    - from: api/cron/channels-deliverability-rollup.js
      to: vercel.json
      via: cron schedule "0 * * * *" (every hour); path /api/cron/channels-deliverability-rollup
    - from: api/cron/channels-lifecycle-journey-poll.js
      to: vercel.json
      via: cron schedule "*/2 * * * *" (every 2 minutes — Vercel Cron min interval); path /api/cron/channels-lifecycle-journey-poll
    - from: lib/markos/channels/dispatch/lifecycle-journey-poll.ts
      to: lib/markos/channels/dispatch/recipient-handler.ts (Wave 2)
      via: advanceJourneyRun calls handleRecipient on the step's template+audience
    - from: api/cron/channels-tombstone-cascade.js
      to: lib/markos/channels/suppression/bounce-handler.ts (Wave 2)
      via: cron iterates new tombstones and calls propagateTombstoneToSuppressions
    - from: .planning/STATE.md
      to: .planning/ROADMAP.md
      via: Phase 223 marked complete; next phase 224 readiness asserted
---

<objective>
Wave 5 phase closeout. Ships 5 cron endpoints (deliverability rollup, lifecycle journey poll, bounce spike alert, tombstone cascade, soft-bounce promotion) per D-49 legacy `api/cron/*.js` convention (NOT App Router `route.ts`); + vercel.json schedule entries; full RLS suite across all 11 new tables (cross-tenant denial × CRUD matrix); operator-journey specs using existing axe-playwright (D-46 — no new playwright runtime); Chromatic snapshot gate; phase SUMMARY + STATE + ROADMAP updates; populates per-task rows in 223-VALIDATION.md (NOTE: `nyquist_compliant: true` flip is owned by execute-phase, not by this plan).

**Per RL1:** This plan has `autonomous: false` because Task 4 (NEW) is a `checkpoint:human-action` gate for operator-only setup that Claude cannot automate (sender warming, DKIM/SPF/DMARC DNS verification, Knock app provisioning, Resend dashboard webhook configuration).

**Per RL2:** Task 2 includes `test/channels/closeout/f-id-collision.test.js` regression test asserting F-ID uniqueness across contracts/F-*.yaml (catches cross-plan slot collisions).

Purpose: completes EML-01..05 (all aggregated; reply intelligence + ConsentState cutover verified end-to-end), MSG-01..05 (all aggregated; 5 channels + double-gate + thread + reply + push verified), CDP-01..05 (carry-forward verified through integration tests + tombstone cascade), CRM-01..05 (carry-forward verified through emit + thread + customer 360), QA-01..15 (contracts current, RLS verified, audit log hash chain continuous, tests cover all decision branches, Chromatic gate passes, regression preserved on P100-P105 + P221 + P222). The QA-03..QA-15 concrete gate definitions are below in <qa_gates>.

Output: 5 cron endpoints + library implementations (legacy api/cron/*.js per D-49), vercel.json updates, 9 closeout test files (.test.js per D-46 — RLS suite + 5 cron tests + legacy regression + qa-gates coverage + f-id-collision regression + cron-routes architecture-lock regression), 1 Storybook addition (LifecycleJourneyEditor), Chromatic config, 4 planning file updates, 1 operator checkpoint task.
</objective>

<qa_gates>
## QA-03..QA-15 Concrete Gates

Each gate has a grep/test-runnable target. `qa-gates-coverage.test.js` (Task 2) asserts every QA-XX has at least one corresponding passing test in the channels suite.

| ID | Gate | Satisfied via (test target) | Grep / acceptance signal |
|----|------|------------------------------|--------------------------|
| QA-03 | dispatch idempotency (`dispatch_attempt_id` UNIQUE constraint + handler dedup check) | `test/channels/dispatch/idempotency.test.js` (223-02 Task 3) + `test/channels/schema/dispatch-tracking.test.js` (223-02 Task 1) | grep `UNIQUE \(dispatch_attempt_id, event_type\)` in `supabase/migrations/117_*.sql`; `npm test -- test/channels/dispatch/idempotency.test.js` exits 0 |
| QA-04 | webhook signature verification per provider (100% coverage) | `test/channels/webhooks/resend-events.test.js` + `twilio-events.test.js` + `knock-events.test.js` (223-04 Task 3) | grep `verifyWebhookSignature` in all 3 webhook handlers; `npm test -- test/channels/webhooks/` exits 0 |
| QA-05 | per-recipient 6-layer gate coverage (consent + suppression + whatsapp_session + frequency + quiet + jurisdiction per D-19+D-54) | `test/channels/gate/{consent,suppression,whatsapp-session,frequency-cap,quiet-hours,jurisdiction}-check.test.js` + `eligibility-gate.test.js` (223-02 Task 2) | `npm test -- test/channels/gate/` exits 0 with ≥35 assertions covering allow/skip/defer per layer per channel |
| QA-06 | approval threshold matrix (class × count × content × manual × reengagement) | `test/channels/templates/approval-matrix.test.js` (223-03 Task 3) | `npm test -- test/channels/templates/approval-matrix.test.js` exits 0 with ≥11 assertions |
| QA-07 | reply continuity (MessagingThread + Customer360 + CRM task per channel) | `test/channels/events/reply-continuity.test.js` (223-04 Task 2) | grep `lookupOrCreateThreadByProfileChannel` in `lib/markos/channels/events/reply-flow.ts`; test exits 0 |
| QA-08 | bounce/complaint/tombstone propagation chain | `test/channels/suppression/bounce-handler.test.js` (223-02 Task 3) + `test/channels/closeout/tombstone-cascade.test.js` (this plan Task 1) | grep `propagateTombstoneToSuppressions` in `lib/markos/channels/suppression/bounce-handler.ts`; both tests exit 0 |
| QA-09 | RLS on all 11 new tables | `test/channels/closeout/rls-suite.test.js` (this plan Task 2) | `npm test -- test/channels/closeout/rls-suite.test.js` exits 0 with ≥44 assertions (11 tables × 4 ops) |
| QA-10 | audit log hash chain integrity preserved across channel writes | `test/channels/closeout/legacy-regression.test.js` (this plan Task 2) — re-runs P201 hash-chain regression | `npm test -- test/channels/closeout/legacy-regression.test.js` exits 0; hash chain verifier (existing P201) green |
| QA-11 | legacy regression (P100-P105 + P221 + P222 tests green) | `test/channels/closeout/legacy-regression.test.js` (this plan Task 2) + `npm test` | `npm test` exits 0; legacy-regression.test.js describe blocks for P100-P105 + P221 + P222 all green |
| QA-12 | pricing binding enforcement (`{{MARKOS_PRICING_ENGINE_PENDING}}` OR allowlisted accessor via D-52 AST/allowlist) | `test/channels/templates/content-classifier.test.js` (223-03 Task 2) | grep `MARKOS_PRICING_ENGINE_PENDING` AND `ALLOWLISTED_ACCESSORS` in `lib/markos/channels/templates/content-classifier.ts`; test exits 0 |
| QA-13 | evidence binding enforcement (P209 EvidenceMap + TTL check, graceful when P209 absent) | `test/channels/templates/content-classifier.test.js` (223-03 Task 2) — Tests 7+8 | grep `evidence_ttl` in classifier file; test exits 0 |
| QA-14 | deliverability rollup + reputation state machine | `test/channels/closeout/deliverability-rollup.test.js` + `test/channels/closeout/spike-alert.test.js` (this plan Task 1) | both tests exit 0; reputation_status enum transitions warming->healthy->watch->at_risk verified |
| QA-15 | SUMMARY/STATE/ROADMAP updated + per-task rows populated (planner contract) | `node` verify command in this plan Task 3 (`<verify>`) | verify command exits 0; `.planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md` per-task rows populated for all 6 plans |

NOTE on `nyquist_compliant`: per execute-phase ownership convention, the `nyquist_compliant: true` flip is performed by execute-phase after running the full automated suite, not by the planner. Task 3 below populates rows and writes SUMMARY/STATE/ROADMAP only.
</qa_gates>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md
@.planning/phases/223-native-email-messaging-orchestration/223-RESEARCH.md
@.planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md
@.planning/phases/223-native-email-messaging-orchestration/223-REVIEWS.md
@.planning/phases/223-native-email-messaging-orchestration/223-01-SUMMARY.md
@.planning/phases/223-native-email-messaging-orchestration/223-02-SUMMARY.md
@.planning/phases/223-native-email-messaging-orchestration/223-03-SUMMARY.md
@.planning/phases/223-native-email-messaging-orchestration/223-04-SUMMARY.md
@.planning/phases/223-native-email-messaging-orchestration/223-05-SUMMARY.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/V4.0.0-TESTING-ENVIRONMENT-PLAN.md
@.planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md
@CLAUDE.md
@vercel.json
@api/cron/webhooks-dlq-purge.js
@lib/markos/channels/deliverability/posture-rollup.ts
@lib/markos/channels/dispatch/dispatch-coordinator.ts
@lib/markos/channels/suppression/bounce-handler.ts
@supabase/migrations/117_channel_dispatch_tracking.sql
@.storybook/main.cjs
@chromatic.config.json
@obsidian/work/incoming/19-EMAIL-ENGINE.md
@obsidian/work/incoming/21-MESSAGING-ENGINE.md
</context>

<interfaces>
<!-- Existing legacy cron pattern (D-49 — verified at api/cron/webhooks-dlq-purge.js:20-30) -->

```javascript
// Existing pattern (api/cron/webhooks-dlq-purge.js — REFERENCE for all 5 new crons)
'use strict';

function authorized(req) {
  const expected = process.env.MARKOS_WEBHOOK_CRON_SECRET;
  if (!expected) return false;
  const header = req.headers['x-markos-cron-secret']
    || req.headers['X-Markos-Cron-Secret']
    || '';
  const auth = (req.headers.authorization || req.headers.Authorization || '')
    .replace(/^Bearer\s+/i, '');
  return header === expected || auth === expected;
}

module.exports = async (req, res) => {
  if (!authorized(req)) {
    return writeJson(res, 401, { error: 'unauthorized' });
  }
  // ... cron-specific work, then writeJson(res, 200, { success: true, ... })
};
```

<!-- Spike alert detection -->

```typescript
// detectBounceSpike(tenantId, channel, sender_id):
//   baseline_window = past 7 days; current_window = past 24 hours
//   baseline_mean = AVG(bounce_rate) over baseline_window
//   baseline_stddev = STDDEV(bounce_rate) over baseline_window
//   if current_window.bounce_rate > baseline_mean + 2 * baseline_stddev AND sample_size > 50 -> spike
//   on spike -> create operator task with reasoning + sender card link
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crons + library implementations — deliverability rollup, lifecycle journey poll, bounce spike alert, tombstone cascade, soft-bounce promotion (D-49 legacy api/cron/*.js convention)</name>
  <read_first>
    - api/cron/webhooks-dlq-purge.js (verified existing legacy pattern at lines 20-30; reference for D-49 — shared-secret header x-markos-cron-secret + MARKOS_WEBHOOK_CRON_SECRET env)
    - api/cron/webhooks-rotation-notify.js + api/cron/mcp-kpi-digest.js (other existing legacy cron files for consistency)
    - vercel.json (current cron registrations)
    - .planning/phases/223-native-email-messaging-orchestration/223-RESEARCH.md (§Lifecycle Journey Trigger Strategy §Frequency Cap §Pitfalls 4)
    - .planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md (D-07, D-21, D-35, D-04, D-49)
    - lib/markos/channels/dispatch/dispatch-coordinator.ts + recipient-handler.ts (Wave 2)
    - lib/markos/channels/suppression/bounce-handler.ts (Wave 2 — propagateTombstoneToSuppressions, handleBounceEvent)
    - lib/markos/channels/programs/lifecycle-journey.ts + journey-run.ts (Wave 1+3)
    - supabase/migrations/117_channel_dispatch_tracking.sql (dispatch_events shape)
    - test/fixtures/channels/dispatch.js + suppression.js
  </read_first>
  <behavior>
    - Test 1 (deliverability rollup): given dispatch_events seeded with mix of delivered/bounced_hard/bounced_soft/complained over the last 24h, aggregateDeliverabilityPosture inserts a deliverability_posture row with bounce_rate = (hard+soft) / sample_size, complaint_rate = complaints / sample_size, reputation_score = 100 - (bounce_rate*60 + complaint_rate*40) clamped [0,100], trend derived from window-over-window delta (improving/stable/watch/degrading thresholds: +5/+0/-5/-10).
    - Test 2 (deliverability rollup idempotency): re-running for the same window_24h_start UPSERTs (no duplicate row).
    - Test 3 (lifecycle journey poll happy path): seeded lifecycle_journey_runs row with state='pending' AND due_at < now() -> advanceJourneyRun runs the step's template through recipient-handler -> emit -> dispatch_events row + advance current_step + set due_at = now() + step.delay_seconds for next step. State transitions: pending -> in_progress -> completed when last step done.
    - Test 4 (lifecycle journey poll fail-closed at gate): step's recipient blocked by eligibility-gate -> dispatch_skips row + journey_run state='paused' (NOT 'failed'); operator must intervene.
    - Test 5 (spike alert detection): seeded baseline 7d bounce_rate=0.02 stddev=0.005; current 24h bounce_rate=0.05 -> 0.05 > 0.02 + 2*0.005 = 0.03 -> spike detected -> operator task created with reason='bounce_spike_detected', sender_id link, suggested action='pause broadcast class until reputation_status returns to healthy'.
    - Test 6 (spike alert sample-size threshold): same spike with sample_size=10 (< 50 threshold) -> no task (insufficient signal).
    - Test 7 (tombstone cascade): seed cdp_identity_profiles row with tombstoned_at=now and tombstoned_since_last_run=true -> cron iterates -> propagateTombstoneToSuppressions called -> channel_suppressions row added for each of 5 channels with reason='dsr_deletion'.
    - Test 8 (soft-bounce promotion): seed dispatch_events with 3 bounced_soft for same (profile_id, channel, last 7 days) -> promoteSoftBounces calls handleBounceEvent('hard') -> channel_suppressions row reason='hard_bounce' + cdp_events emit + audit_log row.
    - Test 9 (cron auth — D-49 shared-secret): all 5 cron endpoints reject requests missing `x-markos-cron-secret` header (401); accept when present and matching MARKOS_WEBHOOK_CRON_SECRET. Mirrors webhooks-dlq-purge.js pattern.
    - Test 10 (vercel.json schedules): file contains all 5 new cron path registrations with appropriate schedules; paths use `/api/cron/channels-*` (NOT `/app/api/cron/...`).
    - Test 11 (architecture-lock — D-49 enforcement): grep `route.ts` across `api/cron/` returns 0 (no App Router files). All 5 new cron files end with `.js` (NOT `.ts`, NOT `route.ts`).
  </behavior>
  <action>
    1. Create `lib/markos/channels/deliverability/posture-rollup.ts`:
       - Export `aggregateDeliverabilityPosture({ tenantId, channel, sender_id, window_24h_start })`:
         - SELECT counts of dispatch_events grouped by event_type for the window.
         - Compute rates per Test 1 formulas.
         - Derive trend by comparing to previous 24h window.
         - INSERT INTO deliverability_posture ON CONFLICT (composite key) DO UPDATE.
       - Export `runForAllTenantsAndSenders()` — convenience runner used by cron endpoint.
    2. Create `lib/markos/channels/deliverability/spike-alert.ts`:
       - Export `detectBounceSpike({ tenantId, channel, sender_id })`:
         - Read past 7d baseline from deliverability_posture (excluding current window).
         - Read current 24h window.
         - If current > baseline_mean + 2*stddev AND sample_size > 50 -> return { spike: true, reason }.
       - Export `createSpikeOperatorTask({ tenantId, sender_id, channel, baseline, current })` — calls existing tasks lib (P208) to create task.
    3. Create `lib/markos/channels/dispatch/lifecycle-journey-poll.ts`:
       - Export `pollDueJourneyRuns(tenantId)`:
         - SELECT lifecycle_journey_runs WHERE state='pending' AND due_at <= now() AND tenant_id LIMIT 100 (per-cycle batch).
         - For each row: read journey.step_definitions[current_step]; build dispatch context; call recipient-handler.handleRecipient.
         - On success: advance current_step += 1; set due_at = now() + step.delay_seconds; update state.
         - On gate skip/defer: handle per Test 4 (state='paused').
         - On failure (adapter error): state='failed', emit operator task.
    4. Create `lib/markos/channels/dispatch/soft-bounce-promotion.ts`:
       - Export `promoteSoftBounces(tenantId)`:
         - SELECT (profile_id, channel, COUNT(*)) FROM dispatch_events WHERE event_type='bounced_soft' AND occurred_at > now()-INTERVAL '7 days' AND tenant_id GROUP BY (profile_id, channel) HAVING COUNT(*) >= 3.
         - For each: handleBounceEvent({ severity: 'hard', tenantId, profileId, channel, dispatchAttemptId: <last attempt> }).
    5. Create 5 legacy cron handler files under `api/cron/` per D-49 (mirror `api/cron/webhooks-dlq-purge.js` pattern at lines 20-30):
       - `api/cron/channels-deliverability-rollup.js` — module.exports async (req, res) => { authorized(req) check via x-markos-cron-secret + MARKOS_WEBHOOK_CRON_SECRET; if !authorized -> 401; else call runForAllTenantsAndSenders; writeJson 200 }.
       - `api/cron/channels-lifecycle-journey-poll.js` — same auth gate; call pollDueJourneyRuns for all tenants.
       - `api/cron/channels-bounce-spike-alert.js` — same auth gate; iterate senders × tenants.
       - `api/cron/channels-tombstone-cascade.js` — same auth gate; SELECT cdp_identity_profiles WHERE tombstoned_at > coalesce(last_run, '1970-01-01') AND tombstoned_at <= now(); for each call propagateTombstoneToSuppressions; persist last_run watermark in tenants config or a dedicated `markos_cron_watermarks` table (use existing watermark pattern from P203 webhook engine if present, else add ALTER tenants.config jsonb path).
       - `api/cron/channels-soft-bounce-promote.js` — same auth gate; call promoteSoftBounces.
       **All 5 files use `'use strict';` + CommonJS exports + the same `authorized()` helper inlined per existing webhooks-dlq-purge.js pattern. NO App Router. NO `route.ts`. NO `app/api/*`.**
    6. Update `vercel.json` with cron entries (validate against Vercel cron min interval — `*/1 * * * *` may not be supported; use `*/2 * * * *`):
       ```
       { "path": "/api/cron/channels-deliverability-rollup", "schedule": "0 * * * *" }       // hourly
       { "path": "/api/cron/channels-lifecycle-journey-poll",  "schedule": "*/2 * * * *" }    // every 2 minutes (Vercel min)
       { "path": "/api/cron/channels-bounce-spike-alert",      "schedule": "0 4 * * *" }      // daily 04:00 UTC
       { "path": "/api/cron/channels-tombstone-cascade",       "schedule": "0 5 * * *" }      // daily 05:00 UTC
       { "path": "/api/cron/channels-soft-bounce-promote",     "schedule": "0 6 * * *" }      // daily 06:00 UTC
       ```
       (All paths use `/api/cron/channels-*` — NOT `/app/api/cron/...` per D-49.)
    7. Create test files (all `.test.js` per D-46):
       - `test/channels/closeout/deliverability-rollup.test.js` (Tests 1, 2).
       - `test/channels/closeout/spike-alert.test.js` (Tests 5, 6).
       - `test/channels/closeout/lifecycle-journey-poll.test.js` (Tests 3, 4).
       - `test/channels/closeout/tombstone-cascade.test.js` (Test 7).
       - `test/channels/closeout/soft-bounce-promotion.test.js` (Test 8).
       - Add cron-auth assertions (Test 9) inline in each cron-related test file.
    8. Create `test/channels/closeout/cron-routes-architecture-lock.test.js` (Test 11):
       - Asserts all 5 cron files exist at `api/cron/channels-*.js` (NOT `app/api/cron/...`).
       - Asserts NO `route.ts` files exist under `api/cron/`.
       - Asserts vercel.json paths match `/api/cron/channels-*`.
    9. Append VALIDATION rows.
  </action>
  <verify>
    <automated>npm test -- test/channels/closeout/deliverability-rollup.test.js test/channels/closeout/spike-alert.test.js test/channels/closeout/lifecycle-journey-poll.test.js test/channels/closeout/tombstone-cascade.test.js test/channels/closeout/soft-bounce-promotion.test.js test/channels/closeout/cron-routes-architecture-lock.test.js</automated>
  </verify>
  <acceptance_criteria>
    - 5 library files exist under `lib/markos/channels/deliverability/` and `lib/markos/channels/dispatch/`
    - 5 cron handler files exist as legacy flat `api/cron/channels-*.js` (D-49 — NOT `app/api/cron/.../route.ts`)
    - File `vercel.json` contains all 5 new cron paths starting with `/api/cron/channels-` (grep `channels-deliverability-rollup` AND `channels-lifecycle-journey-poll` AND `channels-bounce-spike-alert` AND `channels-tombstone-cascade` AND `channels-soft-bounce-promote`)
    - File `vercel.json` does NOT contain any `/app/api/cron/` paths (D-49 enforcement)
    - All 5 cron handlers contain `x-markos-cron-secret` shared-header auth (mirrors webhooks-dlq-purge.js pattern; grep `MARKOS_WEBHOOK_CRON_SECRET` in each)
    - `npm test -- test/channels/closeout/{deliverability-rollup,spike-alert,lifecycle-journey-poll,tombstone-cascade,soft-bounce-promotion,cron-routes-architecture-lock}.test.js` exits 0
    - Cron auth: each route rejects requests without correct `x-markos-cron-secret` (test asserts 401)
    - Tombstone cascade test: P221 tombstone -> 5 channel_suppressions rows verified (one per channel) — satisfies QA-08
    - Deliverability rollup test verifies reputation_score formula + trend transitions — satisfies QA-14
    - Spike alert test verifies >2σ detection + sample_size threshold — satisfies QA-14
    - find under `api/cron/` returns 0 files matching `route.ts` (D-49 architecture-lock)
    - All test files end with `.test.js` (D-46)
  </acceptance_criteria>
  <done>5 crons + 5 libraries operational at api/cron/*.js (D-49); deliverability rollup, lifecycle poll, spike alert, tombstone cascade, soft-bounce promotion all green and on schedule; QA-08 + QA-14 concrete gates green; architecture-lock D-49 + D-46 enforced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Full RLS suite (11 tables × CRUD × cross-tenant) + legacy regression suite (P100-P105 + P221 + P222) + Chromatic gate + QA gates coverage meta-test + F-ID-collision regression test (RL2)</name>
  <read_first>
    - test/crm360/rls-suite.test.js (P222 RLS suite reference if exists; else build from P201 RLS pattern)
    - test/fixtures/channels/* (all)
    - .storybook/main.* (Storybook config)
    - chromatic.config.json (current chromatic gate config)
    - .planning/phases/223-native-email-messaging-orchestration/223-RESEARCH.md (§Validation Architecture §Slice 223-06 §Tests Implied)
    - .planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md (D-36, D-37, D-40 F-ID slot table, D-46)
    - .planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md
    - components/markos/crm/outbound/* (Wave 4 outputs — for component test references)
    - contracts/F-*.yaml (F-122..F-131 — for F-ID-collision regression)
  </read_first>
  <behavior>
    - Test 1 (RLS suite): for each of 11 new tables × {SELECT, INSERT, UPDATE, DELETE} × cross-tenant: with app.tenant_id set to tenant_A, attempt operation on a row owned by tenant_B -> denied (SELECT returns empty; INSERT/UPDATE/DELETE no-op or error). 11 × 4 = 44 assertions minimum. Satisfies QA-09.
    - Test 2 (legacy regression P100-P105): outbound test fixtures from P104 still pass with the cutover (A12 mock injection from 223-02). Satisfies QA-11.
    - Test 3 (legacy regression P221): all P221 cdp-* tests still pass — proves view-swap migration 119 + getConsentState reads + tombstone cascade did not break P221 surfaces. Satisfies QA-11.
    - Test 4 (legacy regression P222): all P222 crm360-* tests still pass — proves emit() fan-out + thread.related_crm_id link + commercial_signal mapping did not regress P222. Satisfies QA-11.
    - Test 5 (audit log hash chain): re-runs P201 hash chain verifier across all events written during channel test runs (campaign, dispatch, suppression, approval, consent change). Hash chain remains continuous. Satisfies QA-10.
    - Test 6 (Chromatic gate): 5 components × 4 variants each = 20 stories; all snapshots either exact match OR explicitly approved. CI gate fails on unapproved diff.
    - Test 7 (QA gates coverage meta-test): qa-gates-coverage.test.js asserts every QA-XX in the phase requirements list (QA-01..QA-15) maps to at least one passing test in the channels suite per the <qa_gates> mapping table. Implementation: hardcoded mapping object; for each QA-XX, locate the listed test file and assert it exists + was green in the most recent run (read node:test cache or re-run). Fails build if any QA-XX has no green test.
    - Test 8 (F-ID-collision regression — RL2 / D-55): scans all `contracts/F-*.yaml` files; parses flow_id from each; asserts no duplicates. Verifies F-122..F-131 each appear exactly once across the contracts directory. Catches cross-plan slot collisions per P226 B6 lesson.
  </behavior>
  <action>
    1. Create `test/channels/closeout/rls-suite.test.js`:
       - Helper: `withTenantContext(tenantId, fn)` sets app.tenant_id session config.
       - Iterate 11 tables × 4 operations × 2 tenants:
         - Seed row in tenant_A.
         - Switch context to tenant_B; attempt op; assert denied.
         - Switch back to tenant_A; attempt op; assert success.
       - Final assertion: total assertions >= 44.
    2. Create `test/channels/closeout/legacy-regression.test.js`:
       - Top-level describe: P100-P105 outbound regression. Imports and re-runs the existing test suites under `test/crm-outbound/*.test.js` with the new ConsentState backend (A12 mock injection pattern).
       - Top-level describe: P221 carry-forward — runs the P221 suite. Top-level describe: P222 carry-forward — runs the P222 suite.
       - Top-level describe: P201 audit log hash chain — invokes the existing hash chain verifier across all channel-domain audit_log rows from the test run.
       - Output: legacy-regression-summary.json captured for SUMMARY artifacts.
    3. Create `test/channels/closeout/qa-gates-coverage.test.js`:
       - Hardcode mapping per the <qa_gates> table in this plan: { 'QA-01': 'test/channels/schema/baseline.test.js', 'QA-02': 'test/channels/schema/', 'QA-03': 'test/channels/dispatch/idempotency.test.js', ... }.
       - For each QA-XX in mapping: assert file (or directory) exists; then run the test (or rely on run-once cache) and assert exit code 0.
       - Emit a coverage table to console for reviewer verification.
    4. Create `test/channels/closeout/f-id-collision.test.js` (RL2 / D-55 regression):
       - Read every file under `contracts/` matching `F-*.yaml`.
       - Parse `flow_id` field from each YAML.
       - Build a Map<flow_id, file_path[]>.
       - Assert no flow_id appears in 2+ files.
       - Assert F-122..F-131 each appear exactly once (matches D-40 slot table).
       - Use `node:test` + `node:assert/strict` per D-46.
    5. Add `LifecycleJourneyEditor.stories.tsx` Storybook story (mentioned in VALIDATION 223-06 row) — covers Empty + 3-step + Paused variants. (Note: LifecycleJourneyEditor component itself can either be added inline in this task as a thin wrapper around existing form primitives OR documented as deferred to P224 if its full implementation requires more scope.)
    6. Update `chromatic.config.json` to include the 5 new component story files.
    7. Append VALIDATION rows.
  </action>
  <verify>
    <automated>npm test -- test/channels/closeout/rls-suite.test.js test/channels/closeout/legacy-regression.test.js test/channels/closeout/qa-gates-coverage.test.js test/channels/closeout/f-id-collision.test.js && npx chromatic --project-token=${CHROMATIC_PROJECT_TOKEN:-dummy} --exit-zero-on-changes</automated>
  </verify>
  <acceptance_criteria>
    - File `test/channels/closeout/rls-suite.test.js` covers ≥44 assertions (11 tables × 4 ops); test exits 0 (QA-09)
    - File `test/channels/closeout/legacy-regression.test.js` includes P100-P105, P221, P222, P201 hash-chain describe blocks; exits 0 (QA-10 + QA-11)
    - File `test/channels/closeout/qa-gates-coverage.test.js` exists; asserts QA-01..QA-15 each have at least one green test mapping; exits 0 (planner contract)
    - File `test/channels/closeout/f-id-collision.test.js` exists; asserts no flow_id duplicates across contracts/F-*.yaml; verifies F-122..F-131 each appear once (RL2 / D-55)
    - File `.storybook/stories/LifecycleJourneyEditor.stories.tsx` exists OR documented stub note
    - File `chromatic.config.json` includes the 5 new component story keys
    - No P100-P105 + P221 + P222 regression failures (legacy-regression.test.js green)
    - All test files end with `.test.js` (D-46)
  </acceptance_criteria>
  <done>RLS matrix proven (QA-09); legacy regression green for P100-P105 + P221 + P222 (QA-11); audit hash chain continuous (QA-10); F-ID-collision regression green (RL2 / D-55); Chromatic gate covers 5 new components with 4 variants each; QA-01..QA-15 each have a green concrete-gate test (planner coverage contract).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Phase SUMMARY + STATE.md + ROADMAP.md updates + per-task VALIDATION rows populated (planner contract; nyquist flip belongs to execute-phase)</name>
  <read_first>
    - .planning/STATE.md (current shape)
    - .planning/ROADMAP.md (Phase 223 entry)
    - .planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md (verify per-task rows populated)
    - .planning/phases/222-crm-timeline-commercial-memory-workspace/222-SUMMARY.md (template — if exists; else use phase 204-*-SUMMARY for template)
    - .planning/phases/223-native-email-messaging-orchestration/223-01-SUMMARY.md through 223-05-SUMMARY.md (aggregate inputs)
  </read_first>
  <behavior>
    - Test 1 (VALIDATION rows complete): every plan 223-01..223-06 has at least one per-task row in 223-VALIDATION.md §Per-Task Verification Map (15-18 rows total).
    - Test 2 (SUMMARY shape): 223-SUMMARY.md contains sections — Goals, Plans Shipped, Migrations (113-121), Contracts (F-122..F-131), Components (5 new), Crons (5 new — legacy api/cron/*.js per D-49), Tests (counts per category), Open Risks, Hand-Off Notes, QA-01..QA-15 coverage table, Review-driven decisions D-42..D-55.
    - Test 3 (STATE.md update): current_position for Phase 223 advanced to "Verification pending" or equivalent; next_actions list points to 224-* if planned.
    - Test 4 (ROADMAP.md update): Phase 223 Plans listed with status checkboxes; aggregate status updated to "Complete - verified pending" or equivalent.
    - Test 5 (planner contract — no nyquist flip): VALIDATION.md frontmatter `nyquist_compliant` is left at `false` (or its current value); the planner does NOT flip it. Execute-phase owns the flip after running the full automated suite.
  </behavior>
  <action>
    1. Update `.planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md`:
       - Verify per-task rows populated for all 6 plans (223-01-0.5 through 223-06-3); add any missing rows pulling the `<verify>` `<automated>` command from each task as the row's "Automated Command" cell. **All commands use `npm test`, NOT `vitest run` (D-46).**
       - Do NOT flip `nyquist_compliant`. Leave it as current value. Add a comment line: "<!-- nyquist_compliant flip is owned by execute-phase per get-shit-done convention; planner only populates rows. -->"
       - `wave_0_complete` likewise NOT flipped here; that is execute-phase's responsibility once Wave 0 (223-01 Task 0.5 + Task 1) completes. Leave at `false`.
    2. Create `.planning/phases/223-native-email-messaging-orchestration/223-SUMMARY.md` with:
       - Phase Goals achieved (per CONTEXT.md scope).
       - Plans shipped: 6 plans across 5 waves.
       - Migrations landed: 113-121 (incl D-50 + D-51 triggers — 9 migrations).
       - Contracts published: F-122..F-131 (10 new).
       - Library modules: lib/markos/channels/* P223-OWNED greenfield tree complete (D-44).
       - Components: 5 new + outbound-workspace evolution + TimelineDetailView extension + 2 P208 entry-type renderers.
       - Crons: 5 new at `api/cron/channels-*.js` per D-49 (NOT App Router).
       - Tests: counts per category (npm test unit, integration, Chromatic visual, legacy regression).
       - QA-01..QA-15 coverage table: copy from <qa_gates> block above with status column.
       - Review-driven decisions D-42..D-55 (architecture-lock + hard preflight + DB-trigger compliance gates + AST/allowlist + WhatsApp ordering + F-ID slot table).
       - Risks open at phase end (e.g., Vercel Queues public beta SLAs, Knock pricing tier limits, Resend inbound metadata-only quirk, sender warming requires operator action per RL1).
       - Hand-off notes: P224 conversion surfaces consume EmailCampaign + LifecycleJourney shapes; P225 attribution consumes dispatch_events + cdp_events.
       - Carry-forward verification: CDP-01..05 + CRM-01..05 marked satisfied via integration tests in 223-06 closeout.
    3. Update `.planning/STATE.md`:
       - current_position.phase = "223 (native-email-messaging-orchestration) — verification pending"
       - progress: increment completed_plans by 6.
       - next_actions: "Run /gsd-verify-work for Phase 223 -> advance to Phase 224 (conversion-launch-workspace)"
    4. Update `.planning/ROADMAP.md` Phase 223 section:
       - Status: "Complete - verification pending" (or matching project convention).
       - Plans list: all 6 plans with [x] checkboxes.
       - Add summary line: "v4.2.0 commercial engines: Phase 223 (native channels) shipped; 9 migrations + 10 contracts + 11 new tables + Vercel Queues fan-out + Knock push + ConsentState cutover (D-51 trigger) + frequency-cap DB trigger (D-50) + 5 cron jobs at api/cron/*.js (D-49)."
    5. Sanity-check: every requirement ID in the phase header (EML-01..05, MSG-01..05, CDP-01..05, CRM-01..05, QA-01..15) appears in at least one plan's `requirements` field. Output a coverage table.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const v = fs.readFileSync('.planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md','utf8'); const rows = (v.match(/\| 223-\d{2}-(?:\d+|0\.5) \|/g) || []).length; if (rows < 15) { console.error('VALIDATION rows insufficient: ' + rows + ' (expected >=15)'); process.exit(1); } const s = fs.readFileSync('.planning/phases/223-native-email-messaging-orchestration/223-SUMMARY.md','utf8'); if (!s.match(/Phase Goals/) || !s.match(/Hand-Off/) || !s.match(/QA-01/) || !s.match(/D-42/)) { console.error('SUMMARY missing required sections'); process.exit(1); } const r = fs.readFileSync('.planning/ROADMAP.md','utf8'); if (!r.match(/223-06-PLAN.md/)) { console.error('ROADMAP missing 223-06 reference'); process.exit(1); } console.log('OK rows=' + rows); process.exit(0);"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md` has ≥15 per-task rows (covers all 6 plans × all tasks including Plan 01 Task 0.5)
    - File `.planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md` `nyquist_compliant` value left UNCHANGED by this task (planner must not flip)
    - File `.planning/phases/223-native-email-messaging-orchestration/223-SUMMARY.md` exists with sections "Phase Goals", "Plans Shipped", "Migrations", "Contracts", "Components", "Crons", "Tests", "QA-01..QA-15 Coverage", "Open Risks", "Hand-Off Notes", "Review-driven decisions D-42..D-55"
    - File `.planning/STATE.md` references Phase 223 status update
    - File `.planning/ROADMAP.md` Phase 223 plans listed with checkboxes
    - All requirement IDs from phase header (EML/MSG/CDP/CRM/QA) appear in at least one plan's `requirements` field (validated via grep aggregation)
    - Verify command above exits 0
    - All test runner commands in VALIDATION.md use `npm test` (NOT `vitest run`); D-46 enforcement
  </acceptance_criteria>
  <done>VALIDATION rows populated; SUMMARY captured (incl D-42..D-55 review-driven decisions); STATE + ROADMAP updated; phase ready for /gsd-verify-work invocation (which owns the nyquist flip).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4 (NEW per RL1): Operator-action gate for sender warming + DKIM/SPF/DMARC + Knock provisioning + Resend dashboard webhook setup</name>
  <what-built>
    Phase 223 ships native owned-channel execution (email + WhatsApp + SMS + push + in-app) with consent + suppression + frequency cap + dispatch + reply continuity + approval triggers. The ENTIRE software surface is automated via Plans 01-05 and Tasks 1-3 of Plan 06.

    However, the PHASE LAUNCH posture requires several setup steps that Claude cannot perform because they require external dashboards / DNS records / multi-day warming operations:

    1. **Sender warming (D-06):** new SenderIdentity rows must be warmed by sending small volumes (e.g., <500/day) over 7 days before reputation_status transitions warming -> healthy. This is a multi-day operator process — the system surfaces the warming status and operator-actionable next steps, but the warming itself is operator-driven.

    2. **DKIM / SPF / DMARC DNS records:** for each new sending_domain (e.g., `mail.acme.com`), DNS records must be added at the customer's DNS provider. Resend dashboard generates the values; operator copies them into DNS. Until verification passes, sender_identities.verification_status='pending' and class_permissions does NOT include 'broadcast'.

    3. **Knock app provisioning (D-09):** Knock dashboard at knock.app — operator creates a new Knock app, copies KNOCK_API_KEY + KNOCK_WEBHOOK_SECRET into deployment env vars, configures webhook endpoint URL (`https://<host>/api/webhooks/knock-events`).

    4. **Resend dashboard webhook configuration:** operator configures inbound webhook in Resend dashboard pointing at `https://<host>/api/webhooks/resend-events` with events `email.delivered`, `email.opened`, `email.clicked`, `email.bounced`, `email.complained`, `email.received`. Resend signing secret -> RESEND_WEBHOOK_SECRET env var.

    5. **Twilio webhook configuration (if not already from P104):** Verify webhook URL points at `/api/webhooks/twilio-events` with TWILIO_AUTH_TOKEN env set.

    6. **Vercel Queues activation:** confirm Vercel Queues (or @vercel/functions queue API) is enabled at the deployment account tier per D-45 trim. If not enabled, dispatcher fails-closed with a clear error message naming the missing capability.

    7. **MARKOS_WEBHOOK_CRON_SECRET env var:** must be set in deployment env (already set if existing crons work). Verify at least one of the existing crons (e.g., `api/cron/webhooks-dlq-purge.js`) returns 200 when invoked with the secret.
  </what-built>
  <how-to-verify>
    1. Sender warming: in operator UI (DeliverabilityWorkspace from Plan 05), confirm at least one sender_identities row is visible with reputation_status='warming' and a "next action" suggestion. After 7 days of operator-driven warming, status should transition to 'healthy'. (For initial deploy, just verify the sender row exists and the UI suggestion is sensible.)

    2. DKIM/SPF/DMARC: for each new sending_domain, visit Resend dashboard -> Domains -> verify DKIM/SPF/DMARC are all 'verified'. Then call `PATCH /v1/channels/email/senders/{id}/verification` to update sender_identities.verification_status='verified'. Confirm DeliverabilityWorkspace UI reflects the new status.

    3. Knock: visit knock.app dashboard -> Apps -> create app named 'markos-{tenant_slug}'. Copy KNOCK_API_KEY + KNOCK_WEBHOOK_SECRET into Vercel env vars. Configure webhook endpoint URL in Knock dashboard pointing at deployment URL `/api/webhooks/knock-events`. Send a test push via knock.workflows.trigger; verify it reaches the test device AND `api/webhooks/knock-events.js` receives the delivery confirmation.

    4. Resend webhooks: visit Resend dashboard -> Webhooks -> add endpoint `/api/webhooks/resend-events` with all events listed above. Copy signing secret to RESEND_WEBHOOK_SECRET env var. Send a test email via Resend; verify webhook fires AND `api/webhooks/resend-events.js` returns 200.

    5. Twilio webhooks: visit Twilio Console -> Phone Numbers -> Configure -> Messaging webhook URL set to `/api/webhooks/twilio-events`. Verify TWILIO_AUTH_TOKEN env var matches account-level auth token.

    6. Vercel Queues: visit Vercel dashboard -> deployment -> verify Queues feature is enabled OR confirm `@vercel/functions` queue API is supported on the deployment plan. If not enabled, file a deployment ticket; Phase 223 dispatch path will hard-fail until resolved.

    7. Cron secret: trigger any existing cron manually (e.g., `curl -H "x-markos-cron-secret: $MARKOS_WEBHOOK_CRON_SECRET" https://<host>/api/cron/webhooks-dlq-purge`); verify 200 response. Then trigger one of the new Plan 06 crons (e.g., `/api/cron/channels-deliverability-rollup`); verify 200 response.

    Once all 7 verification steps pass, type "approved" to resume execute-phase. If any step fails, describe the issue (which step, what error message, what dashboard you saw); execute-phase will pause until resolved.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues per step number above</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel Cron header / x-markos-cron-secret -> route handler | Shared-secret + MARKOS_WEBHOOK_CRON_SECRET env enforces cron-only invocation per D-49 |
| cron iteration -> per-tenant runner | runForAllTenants must scope to tenant_id; no cross-tenant leakage |
| operator dashboards (Resend/Knock/Twilio/Vercel) -> deployment env | Operator-driven configuration; Task 4 checkpoint gates phase launch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-223-06-01 | Spoofing | rogue invocation of cron endpoint | mitigate | x-markos-cron-secret shared-header check on every cron route per D-49; failure -> 401 |
| T-223-06-02 | Information Disclosure | tombstone cascade reads cross-tenant profiles | mitigate | propagateTombstoneToSuppressions scoped per-tenant; cron iterator uses tenant-scoped query loop |
| T-223-06-03 | Denial of Service | unbounded lifecycle journey poll batch | mitigate | LIMIT 100 per cycle; spread load via cron interval |
| T-223-06-04 | Tampering | RLS misconfig on a single new table allows cross-tenant write | mitigate | rls-suite.test.js 11 tables × 4 ops × 2 tenants matrix |
| T-223-06-05 | Repudiation | spike alert task created without audit | mitigate | createSpikeOperatorTask emits audit_log row alongside task |
| T-223-06-06 | Tampering | cron handler regresses to App Router route.ts | mitigate | cron-routes-architecture-lock.test.js asserts only api/cron/*.js exists; D-49 enforcement |
| T-223-06-07 | Tampering | F-ID slot collision across plans | mitigate | f-id-collision.test.js per RL2 / D-55 |
| T-223-06-08 | Spoofing | sender_identity launches broadcast without DKIM/SPF/DMARC verification | mitigate | Task 4 checkpoint blocks phase launch until operator verifies; class_permissions gate prevents broadcast send |
</threat_model>

<verification>
- 5 cron endpoints + 5 library implementations + vercel.json schedules all green via `npm test` (D-46) at `api/cron/channels-*.js` (D-49).
- Full RLS suite (11 × 4 × cross-tenant denial) green (QA-09).
- Legacy P100-P105 + P221 + P222 regression green (QA-11).
- Audit log hash chain continuous (QA-10).
- F-ID-collision regression (RL2 / D-55) green.
- Cron-routes architecture-lock (D-49) regression green.
- Chromatic gate covers 5 new components × 4 variants each.
- 223-SUMMARY.md + STATE.md + ROADMAP.md updated (incl D-42..D-55).
- 223-VALIDATION.md per-task rows populated; `nyquist_compliant` left unchanged (execute-phase owns the flip).
- All 35 requirement IDs from phase header verified present in plan `requirements` fields.
- QA-01..QA-15 each have a green concrete-gate test per qa-gates-coverage.test.js.
- Operator-action checkpoint (Task 4) gates phase launch on sender warming + DKIM/SPF/DMARC + Knock provisioning + webhook configuration per RL1.
</verification>

<success_criteria>
- Phase 223 ships:
  - 9 migrations (113-121, incl D-50 + D-51 triggers)
  - 10 contracts (F-122..F-131)
  - 11 new tables RLS-verified
  - 5 channel adapters (resend extended, twilio extended, knock new, in_app new, base extended)
  - Per-recipient 6-layer gate (incl D-54 WhatsApp session ordering)
  - Single fan-out emit() with commercial_signal map + D-51 GUC
  - Reply continuity flow with auto-reply filter + single-detection contract
  - 19 API routes (legacy flat per D-42) + 5 MCP tools at index.cjs (D-48)
  - 5 new UI components + outbound-workspace evolution + TimelineDetailView extension
  - 5 cron jobs at api/cron/*.js (D-49)
  - Architecture-lock (D-42) + helper presence (D-43) + greenfield map (D-44) + hard preflight (D-45) + npm test runner (D-46) + contracts/openapi.json (D-47) + index.cjs registry (D-48) + DB-trigger compliance gates (D-50/D-51) + AST/allowlist classifier (D-52) + P223-owned classifier (D-53) + WhatsApp ordering (D-54) + F-ID slot table (D-55) all enforced and tested
  - Full Chromatic gate
  - Operator checkpoint (RL1) for sender warming + provider provisioning
- Carry-forward CDP-01..05 + CRM-01..05 verified through integration tests.
- QA-01..QA-15 each have a concrete grep/test-runnable gate (see <qa_gates>).
- Phase ready for /gsd-verify-work invocation.
</success_criteria>

<output>
After completion, the phase is ready for `/gsd-verify-work` invocation. The Wave 5 outputs include `.planning/phases/223-native-email-messaging-orchestration/223-SUMMARY.md` (final phase summary), `.planning/STATE.md` (advanced to verification-pending), and `.planning/ROADMAP.md` (Phase 223 plans checked off). Next phase: **224 (conversion-launch-workspace)** which consumes EmailCampaign + LifecycleJourney shapes for landing pages, forms, CTAs, and launch orchestration.
</output>
