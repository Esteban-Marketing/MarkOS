---
phase: 223
plan: "05"
type: execute
wave: 4
depends_on: ["223-01", "223-02", "223-03", "223-04"]
files_modified:
  - api/v1/channels/email/programs.js
  - api/v1/channels/email/programs-by-id.js
  - api/v1/channels/email/programs-dispatch.js
  - api/v1/channels/email/sends-by-id.js
  - api/v1/channels/email/senders.js
  - api/v1/channels/email/deliverability.js
  - api/v1/channels/messaging/threads.js
  - api/v1/channels/messaging/threads-escalate.js
  - api/v1/channels/messaging/sends.js
  - api/v1/channels/lifecycle/journeys.js
  - api/v1/channels/lifecycle/journeys-pause.js
  - api/v1/channels/lifecycle/journeys-resume.js
  - api/v1/channels/templates.js
  - api/v1/channels/templates-preview.js
  - api/v1/channels/approvals.js
  - api/v1/channels/approvals-approve.js
  - api/v1/channels/approvals-reject.js
  - api/v1/channels/approvals-revoke.js
  - api/v1/channels/suppressions.js
  - lib/markos/channels/api/handlers.js
  - lib/markos/channels/mcp/tools.js
  - lib/markos/mcp/tools/index.cjs
  - contracts/F-130-channel-program-dispatch-write.yaml
  - contracts/flow-registry.json
  - contracts/openapi.json
  - components/markos/crm/outbound/ChannelProgramsList.tsx
  - components/markos/crm/outbound/ChannelProgramsList.module.css
  - components/markos/crm/outbound/MessagingThreadsList.tsx
  - components/markos/crm/outbound/MessagingThreadsList.module.css
  - components/markos/crm/outbound/DeliverabilityWorkspace.tsx
  - components/markos/crm/outbound/DeliverabilityWorkspace.module.css
  - components/markos/crm/outbound/TemplateEditor.tsx
  - components/markos/crm/outbound/TemplateEditor.module.css
  - components/markos/crm/outbound/ApprovalReviewPanel.tsx
  - components/markos/crm/outbound/ApprovalReviewPanel.module.css
  - components/markos/crm/outbound/outbound-workspace.tsx
  - components/markos/crm/TimelineDetailView.tsx
  - components/markos/operator/MorningBriefChannelEntries.tsx
  - components/markos/operator/ApprovalInboxChannelEntries.tsx
  - .storybook/stories/ChannelProgramsList.stories.tsx
  - .storybook/stories/MessagingThreadsList.stories.tsx
  - .storybook/stories/DeliverabilityWorkspace.stories.tsx
  - .storybook/stories/TemplateEditor.stories.tsx
  - .storybook/stories/ApprovalReviewPanel.stories.tsx
  - test/channels/api/email-programs.test.js
  - test/channels/api/sends-route.test.js
  - test/channels/api/email-senders.test.js
  - test/channels/api/email-deliverability.test.js
  - test/channels/api/messaging-threads.test.js
  - test/channels/api/lifecycle-journeys.test.js
  - test/channels/api/templates.test.js
  - test/channels/api/approvals.test.js
  - test/channels/api/suppressions.test.js
  - test/channels/api/tenant-isolation.test.js
  - test/channels/mcp/tools.test.js
  - .planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md
autonomous: true
requirements:
  - EML-01
  - EML-02
  - EML-03
  - MSG-01
  - MSG-02
  - MSG-03
  - MSG-04
  - CRM-01
  - CRM-02
  - CRM-03
  - CRM-04
  - CRM-05
  - QA-01
  - QA-02
must_haves:
  truths:
    - "11 /v1/channels/* route surfaces ship per D-30 (email programs, sends, senders, deliverability; messaging threads, sends, escalate; lifecycle journeys + pause + resume; templates + preview; approvals + approve + reject + revoke; suppressions). Per D-42 architecture-lock: ALL routes are legacy flat api/v1/channels/*.js (NOT App Router api/v1/.../route.ts; NOT app/api/* tree). Auth via requireHostedSupabaseAuth from onboarding/backend/runtime-context.cjs:491 (NOT requireSupabaseAuth which does not exist)."
    - "Plan 05 owns F-130 contract slot per D-40 — channel-program-dispatch-write (write contract for MCP tools)."
    - "All write APIs honor approval-package pattern (D-32): high-risk mutations require approval_ref OR self-trigger evaluateApprovalTriggers and route to P208 inbox via buildApprovalPackage (D-43 — NOT createApprovalPackage)"
    - "5 MCP tools per D-31: send_email_program, send_messaging, get_thread, get_deliverability_posture, list_pending_approvals — registered with markos MCP registry at lib/markos/mcp/tools/index.cjs (per D-48 — `.cjs` not `.ts`); tenant-scoped"
    - "Tenant isolation tested: request with tenant_A bearer token cannot read or write tenant_B campaigns/threads/templates/approvals"
    - "5 new UI components ship under components/markos/crm/outbound/: ChannelProgramsList (kanban), MessagingThreadsList (queue by owner), DeliverabilityWorkspace (sender + posture + suppression count), TemplateEditor (per-channel preview + bindings), ApprovalReviewPanel (P208 entries)"
    - "outbound-workspace.tsx evolved (D-33) to consume legacy-outbound adapter from 223-01; existing CRM outbound shell preserved"
    - "TimelineDetailView (P222 D-25) gains channel event chips (open/click/reply/bounce per send) — D-34"
    - "Morning Brief (P208) gains channel entries: top reply_pending threads by primary_owner_user_id + at_risk senders requiring action (D-35)"
    - "Approval Inbox (P208) renders channel approval entries (email_campaign | lifecycle_journey | channel_template) with triggerReason + classifierFindings + pricingRefs panels"
    - "contracts/openapi.json (per D-47 — NOT public/openapi.json) regenerated with /v1/channels/* paths and F-122..F-131 schemas"
    - "5 Storybook stories shipped for each new component (Empty + Populated + Error + Tombstoned variants where applicable)"
    - "All written D-30 paths verified by Node `--test` API contract tests; cross-tenant denial proven on every endpoint"
  artifacts:
    - path: api/v1/channels/email/programs.js + 18 sibling routes (legacy flat per D-42)
      provides: 19 HTTP handler files for /v1/channels/* surface (1 file per HTTP path; uses legacy flat naming since file system can't have dynamic segment brackets in `.js` flat layout)
    - path: lib/markos/channels/api/handlers.js
      provides: shared route handler primitives (requireHostedSupabaseAuth wrap, tenant resolve, body validate via zod, error mapping); reused by all 19 routes
    - path: lib/markos/channels/mcp/tools.js
      provides: 5 MCP tool implementations satisfying D-31 contract
    - path: lib/markos/mcp/tools/index.cjs
      provides: extended MCP tool registry registering markos-channel-* tools alongside existing markos-marketing/markos-crm/markos-tenancy families (D-48 — .cjs file, NOT .ts)
    - path: contracts/F-130-channel-program-dispatch-write.yaml
      provides: F-130 contract — campaign/thread/journey send trigger + approval gate + fan-out write API. Description references buildApprovalPackage (D-43).
    - path: contracts/openapi.json
      provides: regenerated OpenAPI 3.1 with /v1/channels/* paths and 10 new F-ID schemas (D-47 — `contracts/openapi.json` NOT `public/openapi.json`)
    - path: components/markos/crm/outbound/ChannelProgramsList.tsx
      provides: kanban-by-status of email_campaigns + lifecycle_journeys; columns = status enum from D-02; item card shows class + audience size + sender + approval state
    - path: components/markos/crm/outbound/MessagingThreadsList.tsx
      provides: queue grouped by owner_user_id; reply_pending entries pinned to top; channel chip (email/sms/whatsapp/push/in_app); related_crm_id link
    - path: components/markos/crm/outbound/DeliverabilityWorkspace.tsx
      provides: per-sender reputation panel (warming/healthy/watch/at_risk) + 24h posture metrics + suppression count + concrete operator next-action suggestion
    - path: components/markos/crm/outbound/TemplateEditor.tsx
      provides: per-channel preview (email/sms/whatsapp/push/in_app); variable inspector; evidence + pricing binding panels; classifier finding overlay; locale variant chain
    - path: components/markos/crm/outbound/ApprovalReviewPanel.tsx
      provides: P208 inbox entry renderer for channel approvals; shows triggerReason chips + classifierFindings + pricingRefs + approve/reject/revoke buttons
    - path: components/markos/crm/TimelineDetailView.tsx
      provides: extended (P222 D-25) — adds channel event chips per send (open/click/reply/bounce)
    - path: components/markos/operator/MorningBriefChannelEntries.tsx + ApprovalInboxChannelEntries.tsx
      provides: P208 entry-type renderers for channel signals (reply_pending threads, at_risk senders) and channel approvals
  key_links:
    - from: api/v1/channels/email/programs.js
      to: lib/markos/channels/programs/email-campaign.ts (Wave 1) + lib/markos/channels/templates/approval-trigger.ts (Wave 3)
      via: POST creates campaign -> evaluateApprovalTriggers -> if required, buildApprovalPackage (D-43) and set status='pending_approval'; else status='draft'
    - from: api/v1/channels/email/programs-dispatch.js
      to: lib/markos/channels/dispatch/dispatch-coordinator.ts (Wave 2) + lib/markos/channels/dispatch/fan-out.ts
      via: POST -> verify campaign.status='approved' -> createDispatchRun (calls assertUpstreamReady(['P207']) per D-15-revised) -> enqueueRecipients
    - from: api/v1/channels/messaging/sends.js
      to: lib/markos/channels/dispatch/recipient-handler.ts (Wave 2) + lib/markos/outbound/providers/* (Wave 3)
      via: POST single message send -> handleRecipient (single-recipient short-circuit)
    - from: lib/markos/channels/mcp/tools.js
      to: api/v1/channels/* handlers
      via: MCP tools call into shared handler primitives in lib/markos/channels/api/handlers.js
    - from: lib/markos/mcp/tools/index.cjs
      to: lib/markos/channels/mcp/tools.js
      via: registry at index.cjs (D-48) requires/registers the channel tools
    - from: components/markos/operator/MorningBriefChannelEntries.tsx
      to: lib/markos/channels/programs/messaging-thread.ts + lib/markos/channels/senders/sender-identity.ts
      via: queries reply_pending threads + at_risk senders for the operator dashboard
---

<objective>
Wave 4. API surface (19 route files for 11 D-30 paths in legacy flat `api/v1/channels/*.js` convention per D-42), MCP tool family (5 tools registered via `lib/markos/mcp/tools/index.cjs` per D-48), UI evolution (5 new components + 2 P208 entry-type renderers + outbound-workspace evolution + TimelineDetailView extension), OpenAPI regen at `contracts/openapi.json` per D-47, and Storybook gating. Wires everything from Waves 1-3 into operator-facing surfaces.

Purpose: satisfies D-30 (read-write /v1/channels/*), D-31 (5 MCP tools), D-32 (approval-package pattern on writes via buildApprovalPackage per D-43), D-33 (CRM outbound workspace evolution + 5 new components), D-34 (TimelineDetailView channel chips), D-35 (Morning Brief + Approval Inbox channel entries). Closes EML-01..EML-03 + MSG-01..MSG-04 + CRM-01..CRM-05 by giving operators concrete surfaces.

Output: 19 API route files (legacy flat per D-42), 1 shared handler module, 1 MCP tools module, MCP registry extension at `.cjs`, F-130 contract, regenerated `contracts/openapi.json`, 5 new components + CSS modules, 2 P208 entry-type renderers, 1 evolved outbound-workspace, 1 extended TimelineDetailView, 5 Storybook stories, 11 API/MCP test files (.test.js per D-46).

**NOTE on D-30 vs D-17 (revoke path):** D-30 API path list omits revoke; D-17 locked decision mandates revocation support. F-131 contract (223-03) includes revoke path. Implementation follows D-17 as the authority. Plan 05 implements GET /v1/channels/approvals + POST /v1/channels/approvals/{id}/{approve|reject|revoke}.

**Architecture-lock (D-42) compliance:**
- All routes: legacy flat `api/v1/channels/*.js` (NOT App Router `route.ts`; NOT `app/api/*` tree).
- Auth: `requireHostedSupabaseAuth` from `onboarding/backend/runtime-context.cjs:481` (NOT `requireSupabaseAuth`, NOT `requireTenantContext`).
- MCP registry: `lib/markos/mcp/tools/index.cjs` (NOT `.ts`).
- OpenAPI spec: `contracts/openapi.json` (NOT `public/openapi.json`).
- Test runner: `npm test` (Node `--test`); test files end `.test.js` (NOT `.test.ts`); NO vitest/playwright commands in `<verify>`.

**Wave note:** This is the largest plan. It depends on outputs from 223-01 (programs/senders/templates stubs + adapters), 223-02 (gate + dispatch + suppression), 223-03 (templates + classifier + approval), 223-04 (events emit + reply flow). Wave 4 cannot start until all four are complete.
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md
@.planning/phases/223-native-email-messaging-orchestration/223-RESEARCH.md
@.planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md
@.planning/phases/223-native-email-messaging-orchestration/223-REVIEWS.md
@.planning/phases/223-native-email-messaging-orchestration/223-01-SUMMARY.md
@.planning/phases/223-native-email-messaging-orchestration/223-02-SUMMARY.md
@.planning/phases/223-native-email-messaging-orchestration/223-03-SUMMARY.md
@.planning/phases/223-native-email-messaging-orchestration/223-04-SUMMARY.md
@.planning/phases/208-human-operating-interface/208-CONTEXT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@obsidian/reference/HTTP Layer.md
@obsidian/reference/UI Components.md
@obsidian/reference/Core Lib.md
@obsidian/work/incoming/19-EMAIL-ENGINE.md
@obsidian/work/incoming/21-MESSAGING-ENGINE.md
@onboarding/backend/runtime-context.cjs
@components/markos/crm/outbound/outbound-workspace.tsx
@components/markos/crm/TimelineDetailView.tsx
@lib/markos/mcp/tools/index.cjs
@lib/markos/channels/programs/email-campaign.ts
@lib/markos/channels/programs/messaging-thread.ts
@lib/markos/channels/programs/lifecycle-journey.ts
@lib/markos/channels/templates/channel-template.ts
@lib/markos/channels/templates/approval-trigger.ts
@lib/markos/channels/templates/preview.ts
@lib/markos/channels/dispatch/dispatch-coordinator.ts
@lib/markos/channels/dispatch/fan-out.ts
@lib/markos/channels/suppression/suppression.ts
@lib/markos/channels/events/emit.ts
@lib/markos/crm/agent-actions.ts
@contracts/F-122-channel-email-campaign-v1.yaml
@contracts/F-123-channel-messaging-thread-v1.yaml
@contracts/F-124-channel-lifecycle-journey-v1.yaml
@contracts/F-125-channel-sender-identity-v1.yaml
@contracts/F-126-channel-deliverability-posture-v1.yaml
@contracts/F-127-channel-template-v1.yaml
@contracts/F-128-channel-suppression-v1.yaml
@contracts/F-129-channel-dispatch-event-v1.yaml
@contracts/F-131-channel-approval-flow.yaml
@contracts/flow-registry.json
@contracts/openapi.json
</context>

<interfaces>
<!-- Shared handler primitive shape (D-42 — uses requireHostedSupabaseAuth) -->

```javascript
// lib/markos/channels/api/handlers.js
const { requireHostedSupabaseAuth } = require('../../../onboarding/backend/runtime-context.cjs');

async function withChannelRouteContext(req, res, handler) {
  // 1. requireHostedSupabaseAuth (D-42) — NOT requireSupabaseAuth, NOT requireTenantContext
  const auth = requireHostedSupabaseAuth({ req, runtimeContext, operation: 'channels_api' });
  if (!auth.allowed) return writeJson(res, auth.status, { error: auth.error });
  // 2. Set app.tenant_id session config from auth.tenant_id
  // 3. Validate body via zod schema in channelRouteSchemas
  // 4. Call handler(ctx)
  // 5. Map errors to JSON
}

const channelRouteSchemas = {
  createEmailCampaign: { /* zod schema */ },
  // ... 10 more schemas
};

module.exports = { withChannelRouteContext, channelRouteSchemas };
```

<!-- MCP tool registration shape (D-48 — registry is index.cjs) -->

```javascript
// lib/markos/mcp/tools/index.cjs (extended)
const channelMcpTools = require('../../channels/mcp/tools.js');

const tools = [
  // ...existing markos-marketing/markos-crm/markos-tenancy tools
  ...channelMcpTools, // adds 5 new tools per D-31
];

module.exports = { tools, channelMcpTools };
```

<!-- TimelineDetailView extension (P222 D-25 + D-34) -->

```tsx
// components/markos/crm/TimelineDetailView.tsx
// EXISTING: renders timeline event rows
// EXTEND: each row gains a "channel event chips" section showing open/click/reply/bounce icons
// when source_domain is email|messaging|push|in_app
```

<!-- Legacy flat route file convention (D-42) — NOT App Router -->

```
api/v1/channels/email/programs.js                  # GET list + POST create
api/v1/channels/email/programs-by-id.js            # GET/PATCH/DELETE /v1/channels/email/programs/:id (parses :id from query/path)
api/v1/channels/email/programs-dispatch.js         # POST /v1/channels/email/programs/:id/dispatch
api/v1/channels/email/sends-by-id.js               # GET/POST/DELETE /v1/channels/email/sends/:id
api/v1/channels/email/senders.js                   # GET list + POST create + PATCH /:id/verification
api/v1/channels/email/deliverability.js            # GET (read-only)
api/v1/channels/messaging/threads.js               # GET list + POST create + GET /:id
api/v1/channels/messaging/threads-escalate.js      # POST /:id/escalate
api/v1/channels/messaging/sends.js                 # POST single message
api/v1/channels/lifecycle/journeys.js              # GET list + POST create + GET /:id
api/v1/channels/lifecycle/journeys-pause.js        # POST /:id/pause
api/v1/channels/lifecycle/journeys-resume.js       # POST /:id/resume
api/v1/channels/templates.js                       # GET list + POST create + GET/PATCH /:id
api/v1/channels/templates-preview.js               # POST /:id/preview
api/v1/channels/approvals.js                       # GET list
api/v1/channels/approvals-approve.js               # POST /:id/approve
api/v1/channels/approvals-reject.js                # POST /:id/reject
api/v1/channels/approvals-revoke.js                # POST /:id/revoke
api/v1/channels/suppressions.js                    # GET list + POST bulk add + DELETE /:id
```

(Vercel rewrite/route config in `vercel.json` translates `/v1/channels/email/programs/:id/dispatch` -> `api/v1/channels/email/programs-dispatch.js?id=:id` — same pattern as existing `api/cron/*.js`. NO App Router; NO `route.ts`.)
</interfaces>

<ui_spec_fold>
<!-- Per 223-UI-SPEC.md heavy-UI hybrid fold. Plan 05 = IN_SCOPE (only UI plan). 5 NEW components (ChannelProgramsList / MessagingThreadsList / DeliverabilityWorkspace / TemplateEditor / ApprovalReviewPanel) + 2 P208 entry-type renderers (MorningBriefChannelEntries / ApprovalInboxChannelEntries) + 2 EVOLVED (outbound-workspace.tsx + TimelineDetailView.tsx). 19 /v1/channels/* legacy flat .js routes per D-42 + 5 MCP tools at lib/markos/mcp/tools/index.cjs per D-48 + OpenAPI regen at contracts/openapi.json per D-47 + 5 Storybook stories. END-OF-v4.2.0-Wave-2 chip count post-223 = 36 (4 NEW handoff_kind chips wired here: 33rd-36th). Full 195 AC fold (164 surface + 31 XC). -->

<ui_scope>IN_SCOPE</ui_scope>

<ui_spec_block_reference>
- 223-UI-SPEC.md frontmatter heavy-UI hybrid mode (line 11): "5 no-UI backend plans + 1 heavy-UI plan shipping 5 NEW components + 2 P208 entry-type renderers + 2 EVOLVED components". Plan 05 = the 1 heavy-UI plan.
- 223-UI-SPEC.md Plan Scope Classification table (lines 67-77) — Plan 05 row: IN_SCOPE; primary surface = 19 api/v1/channels/*.js (legacy flat per D-42) + lib/markos/channels/{api,mcp}/*.js + lib/markos/mcp/tools/index.cjs (extend per D-48) + F-130 + contracts/openapi.json (regen per D-47) + 5 NEW components + 2 P208 entry-type renderers + 2 EVOLVED + 5 module CSS + 5 Storybook stories.
- 223-UI-SPEC.md §Surface A — ChannelProgramsList (CPL-1..19; lines 394-523) — kanban-by-status of EmailCampaign + LifecycleJourney programs; 10 status columns + 4 class chips + 5 channel chips + filter chip groups; per-card RunStatusBadge (P207) + dispatch_run_id chip + audience_snapshot_id chip + sender_identity_id chip + class chip + audience size + approval state badge + Schedule dispatch -> mint-text inline link (status='approved' AND scheduled_at IS NULL).
- 223-UI-SPEC.md §Surface B — MessagingThreadsList (MTL-1..21; mobile_priority=critical; lines 525-660) — queue grouped by owner_user_id; reply_pending pinned with red dot; 6 status group headers + 5 channel chips; per-row HealthScoreBadge + RiskBandBadge (216 D-15 reuse when related_crm_id resolves to Customer360); PIIRedactedField pii_classification="personal" recipient_name; sentiment 3-enum (positive/neutral/negative) verbatim per 222 inheritance; Open Customer360 -> mint-text inline (related_crm_id present); Open NBA -> mint-text inline (related_nba_id present); Auto-reply (filtered) indicator (Plan 04 Pitfall 7); WhatsApp session expired badge (D-54); opt-out detected badge (D-23 step 6).
- 223-UI-SPEC.md §Surface C — DeliverabilityWorkspace (DLW-1..17; lines 662-822) — per-sender card with reputation_status badge (warming/healthy/watch/at_risk) + 24h posture metrics (bounce_rate / complaint_rate / reply_rate / open_rate / click_rate / unsubscribe_rate) + sample_size + reputation_score (0-100) + trend (improving/stable/watch/degrading) + 8 sender-detail labels (From/Reply-to/Sending domain/DKIM/SPF/DMARC/Daily limit/Last warmed); RiskBandBadge reused (216 D-15); spike detected notice (>2σ from 7-day baseline per D-35); DKIM/SPF/DMARC bracketed-glyph pairing; verbatim next-action copy ("At-risk: pause broadcast class until reputation recovers." / "Warming: send <500/day for 7 days.").
- 223-UI-SPEC.md §Surface D — TemplateEditor (TPE-1..26; lines 824-1037) — per-channel preview pane (email shows subject + content_html_rendered; sms/whatsapp shows content_text_rendered; push shows title + body; in_app shows content_html_rendered) + variable inspector (4 variable sources verbatim per D-26: TraitSnapshot / Customer360 / Opportunity / dispatch_context_overrides) + bindings (evidence + pricing) + classifier findings overlay (5 finding kinds verbatim per D-52: pricing_binding / pricing_text_scan / pricing_ast_violation / evidence_ttl / competitor_mention; 3 severity literals: block / flag / info) + locale variant chain navigator + KbGroundingPanel (216 D-15 reuse) for evidence_bindings + ClassifierChipRow (216 D-15 reuse) for classifier findings + PIIRedactedField (216 D-15 reuse) for sample recipient render + Money fromPricingRecommendation XOR PlaceholderBanner billing_placeholder sentinel; Publish CTA opens 215 billing-correction modal recipe when classifier finding severity='block' on pricing_binding/factual_claim AND calls buildApprovalPackage(kind='channel_template_publish_approval') per D-43.
- 223-UI-SPEC.md §Surface E — ApprovalReviewPanel (ARP-1..21; lines 1040-1218) — header card with handoff_kind chip (4 NEW per 223 verbatim: channel_dispatch_approval / channel_template_publish_approval / channel_suppression_bulk_approval / channel_program_pause_approval) + approval_ref chip + status badge (pending/approved/rejected/revoked); 7 triggerReason labels verbatim per D-16 + D-18 (class_broadcast / class_revenue / count_500_plus / content_classifier_block / manual_override / re_engagement_90d / lifecycle_stage_lost); ClassifierChipRow + KbGroundingPanel + HealthScoreBadge (when subject is reply-related approval) + PIIRedactedField (recipient sample list) + RunStatusBadge run_id={dispatch_run_id} (when revoke modal active for mid-dispatch revocation per D-17); Approve/Reject/Revoke CTAs use 215 billing-correction modal recipe; reason capture (>=20 chars per 216 carry); Approve calls buildApprovalPackage per D-43; Revoke mid-dispatch sends pause signal to AgentRun per D-17.
- 223-UI-SPEC.md §Surface F — MorningBriefChannelEntries (MBC-1..20; mobile_priority=critical; lines 1219-1346) — P208 Morning Brief 2 NEW channel-related rollup sections: (1) "Reply pending threads" (top 5 newest reply first, filtered by owner_user_id == current_operator_user_id per D-23 reply continuity); (2) "At-risk senders" (ALL senders with reputation_status='at_risk' — no limit) with verbatim at-risk next-action "Pause broadcast class until reputation recovers." + spike detected notice "[warn] Bounce spike — {N}σ above 7d baseline." per D-35; PIIRedactedField pii_classification="personal" for thread recipient; mobile_priority=critical registered in lib/markos/operator/shell.ts SurfaceRouteContract (surface_family: channels_brief).
- 223-UI-SPEC.md §Surface G — ApprovalInboxChannelEntries (AIC-1..15; lines 1347-1466) — P208 Approval Inbox row renderer for 4 NEW handoff_kind chips (33rd-36th); 5 subject_type-specific row titles ("Email campaign: {name}" / "Lifecycle journey: {name}" / "Template publish: {name}" / "Suppression bulk add: {N} recipients" / "Pause dispatch: {name}"); 4 class chip labels verbatim per D-02 (broadcast / lifecycle / transactional / revenue); 5 channel chip labels verbatim; 7 triggerReason labels verbatim per D-16 + D-18; subject_type='channel_program_pause' renders RunStatusBadge run_id={dispatch_run_id} + recipients_already_sent + remaining_recipients counters with tabular numerals; PIIRedactedField for recipient sample.
- 223-UI-SPEC.md §Surface H — outbound-workspace.tsx EVOLVED per D-33 (OWE-1..11; lines 1467-1492) — outer shell preserved verbatim (existing CRM outbound entry points + tenant context wrapper); tab bar added "Programs | Threads | Deliverability | Templates | Approvals" with mint-underline on active tab (--color-primary-text per D-09); each tab renders the corresponding NEW component; legacy sections remain mounted via legacy-outbound adapter from 223-01 + tagged "Legacy (deprecating)" (.c-badge--info + bracketed glyph); mobile breakpoint behavior unchanged; tab bar collapses to dropdown on mobile < 640px.
- 223-UI-SPEC.md §Surface I — TimelineDetailView.tsx EVOLVED per D-34 (TDE-1..14; lines 1495-1522) — 222 baseline preserved verbatim (15 222 ACs continue to pass); channel-event-chips section ADDED per row when source_domain IN ('email','messaging','push','in_app'); 7 chip event types verbatim (opened / clicked / replied / bounced / complained / unsubscribed / suppressed); chips read from dispatch_events keyed on source_event_ref (per D-29 fan-out); click chip opens detail pop showing dispatch_attempt_id + provider_message_id (NOT secrets per D-39); 222 commercial_signal taxonomy 7-enum verbatim honored (interest/risk/expansion/renewal/support/pricing/silence); 222 source_domain 11-enum verbatim labels honored.
- 223-UI-SPEC.md §Cross-Surface Acceptance Criteria — XC-1..XC-31 (lines 1525-1560) — 31 cross-cutting ACs covering Storybook + Chromatic gate + token-only + mint-as-text + .c-notice mandatory + .c-card--feature reserved + no .c-table + 'use client' + PIIRedactedField + SurfaceRouteContract registration + banned-lexicon zero-match + buildApprovalPackage exclusive + cron path D-49 + REST path D-42 + npm test runner D-46 + meta-test no vitest/playwright + architecture-lock detector + hard preflight + migration slot + F-ID slot + 11-table RLS + frequency-cap trigger D-50 + consent-write trigger D-51 + AST/allowlist classifier D-52 + single fan-out emit D-29 + 6-layer gate D-19 + WhatsApp ordering D-54 + auto-reply detect Pitfall 7 + Knock cascade Pitfall 8 + Resend metadata-only webhook Pitfall 9/A11 + reply continuity flow + Chromatic snapshot gate + axe-playwright operator-journey + closeout regression + manual operator checkpoints.
- 223-UI-SPEC.md §Approval Inbox Handoff Chain Extension (lines 1563-1592) — POST-223 NEW chips: 33rd channel_dispatch_approval (ApprovalReviewPanel "Approve" path) + 34th channel_template_publish_approval (TemplateEditor "Publish template" path) + 35th channel_suppression_bulk_approval (DeliverabilityWorkspace "Bulk add suppressions" > 100 path per D-32) + 36th channel_program_pause_approval (ApprovalReviewPanel "Revoke" mid-dispatch path per D-17). All 4 calls go through buildApprovalPackage per D-43.
- 223-UI-SPEC.md §D-15 Extracted Component Reuse Manifest (lines 1655-1669) — 7 extracted components from 217-06 origin REUSED in 223 surfaces (NOT re-implemented): HealthScoreBadge / RiskBandBadge / KbGroundingPanel / RetentionClassChip / PIIRedactedField / ClassifierChipRow + SaveOfferPricingBlock NOT consumed in 223. Storybook stories register UNDER Channels/* path and IMPORT extracted components from 217-06 origin.
- 223-UI-SPEC.md §Sensitive Credential UI Binding Layer 6 + B-9 NEW (lines 361-391) — provider secrets RESEND_API_KEY / TWILIO_AUTH_TOKEN / KNOCK_API_KEY / RESEND_SIGNING_SECRET / TWILIO_WEBHOOK_SECRET / KNOCK_WEBHOOK_SECRET / MARKOS_WEBHOOK_CRON_SECRET NEVER in UI surface, MCP response, or audit_log payload; only provider_message_id (public per-message ID) is exposed.
</ui_spec_block_reference>

<full_surface_acceptance_criteria_fold>
<!-- 195 ACs total fold (164 surface + 31 cross-cutting). Plan 05 task 2 implements 5 NEW + 2 P208 renderer + 2 EVOLVED with all surface ACs verbatim. Each AC range maps to a Surface section in UI-SPEC. -->

<surface_a_channel_programs_list ac_count="19">
- AC CPL-1: Component is a client component ('use client' directive)
- AC CPL-2: Component renders all 10 status column headers verbatim (Draft / Pending approval / Approved / Scheduled / Dispatching / Dispatched / Paused / Cancelled / Completed / Failed)
- AC CPL-3: Component renders all 4 class filter chips verbatim per D-02 (broadcast / lifecycle / transactional / revenue)
- AC CPL-4: Component renders all 5 channel filter chips verbatim per D-03+D-08 (email / sms / whatsapp / push / in_app)
- AC CPL-5: Component fetches /v1/channels/email/programs?class=X&channel=Y&owner=Z&status=W with proper URL encoding
- AC CPL-6: Per-card aggregate counters render with tabular numerals (.c-code-inline + 'tnum' 1)
- AC CPL-7: Per-card RunStatusBadge run_id={dispatch_run_id} renders for dispatching/completed/failed cards (P207 lineage)
- AC CPL-8: Dispatching cards render .c-status-dot--live kernel-pulse (freezes under prefers-reduced-motion)
- AC CPL-9: Empty state renders "[info] No channel programs yet. Create campaign →" verbatim
- AC CPL-10: Per-card "Schedule dispatch →" mint-text inline link renders ONLY when status='approved' AND scheduled_at IS NULL
- AC CPL-11: Approval-required notice renders verbatim "Awaiting approval — {triggerReason}." with triggerReason from buildApprovalPackage payload (D-43)
- AC CPL-12: Autonomy ceiling notice renders "[block] Autonomy ceiling reached for channel_dispatch_approval." when external.send triggers without approval_ref
- AC CPL-13: Per-card audience_snapshot_id chip + "Open audience →" mint-text inline link to P221 surface
- AC CPL-14: Per-card sender_identity label renders (read from sender_identities D-06)
- AC CPL-15: D-21 server/client boundary noted in file header comment
- AC CPL-16: D-08 token-only enforced (zero hex literals in module.css)
- AC CPL-17: Banned-lexicon zero-match on campaign name + sender_label (CI assertion)
- AC CPL-18: Storybook story Channels/ChannelProgramsList registers >=4 named-state stories (Empty / Populated / Tombstoned / Filtered)
- AC CPL-19: Mobile breakpoint stacks kanban columns vertically with horizontal swipe nav
</surface_a_channel_programs_list>

<surface_b_messaging_threads_list ac_count="21" mobile_priority="critical">
- AC MTL-1: Component is a client component ('use client' directive)
- AC MTL-2: Component renders all 6 status group headers verbatim (Reply pending / Open / Waiting / Escalated / Resolved / Blocked)
- AC MTL-3: Component renders all 5 channel filter chips verbatim
- AC MTL-4: Component fetches /v1/channels/messaging/threads?owner_user_id=X&status=Y with proper URL encoding
- AC MTL-5: Reply_pending group pinned to top of list (sort order)
- AC MTL-6: Per-row recipient name rendered via PIIRedactedField pii_classification="personal"
- AC MTL-7: Per-row HealthScoreBadge renders when related_crm_id resolves to Customer360 with health_score (216 D-15 reuse)
- AC MTL-8: Per-row RiskBandBadge renders when related_crm_id resolves to Customer360 with risk_band (216 D-15 reuse)
- AC MTL-9: Per-row sentiment chip renders 3 verbatim labels per 222 sentiment 3-enum
- AC MTL-10: Per-row "Open Customer360 →" mint-text inline link renders ONLY when related_crm_id present (222 D-03)
- AC MTL-11: Per-row "Open NBA →" mint-text inline link renders ONLY when related_nba_id present (222 D-08)
- AC MTL-12: Per-row "Open thread →" mint-text inline link always renders
- AC MTL-13: Opt-out detected badge renders when ConsentState mutated to opted_out via D-23 step 6
- AC MTL-14: WhatsApp session expired badge renders when channel='whatsapp' AND last_message_at > 24h (per D-54)
- AC MTL-15: Auto-reply rows excluded from reply_pending group (Plan 04 Pitfall 7 — auto-replies log only)
- AC MTL-16: mobile_priority=critical registered in lib/markos/operator/shell.ts SurfaceRouteContract with surface_family: channels_threads
- AC MTL-17: Touch target >=44px on coarse pointers (already global per 213.2)
- AC MTL-18: D-21 server/client boundary noted in file header comment
- AC MTL-19: D-08 token-only enforced (zero hex literals in module.css)
- AC MTL-20: Banned-lexicon zero-match on last_message_snippet preview (CI assertion)
- AC MTL-21: Storybook story Channels/MessagingThreadsList registers >=4 named-state stories (Empty / ReplyPending / Mixed / Tombstoned)
</surface_b_messaging_threads_list>

<surface_c_deliverability_workspace ac_count="17">
- AC DLW-1: Component is a client component ('use client' directive)
- AC DLW-2: Component renders all 4 reputation_status filter chips verbatim (warming / healthy / watch / at_risk)
- AC DLW-3: Component renders all 6 deliverability metric labels verbatim per D-07
- AC DLW-4: Component renders all 4 trend literals + bracketed glyph per D-07
- AC DLW-5: Component renders all 8 sender-detail labels (From / Reply-to / Sending domain / DKIM / SPF / DMARC / Daily limit / Last warmed)
- AC DLW-6: All rate columns + sample_size + reputation_score render with tabular numerals
- AC DLW-7: Per-sender at_risk next-action notice renders verbatim "At-risk: pause broadcast class until reputation recovers."
- AC DLW-8: Per-sender warming next-action notice renders verbatim "Warming: send <500/day for 7 days."
- AC DLW-9: Spike detected notice renders when bounce_rate > baseline + 2σ (per D-35); references σ value verbatim
- AC DLW-10: DKIM/SPF/DMARC status renders with bracketed glyph pairing ([ok]/[info]/[err])
- AC DLW-11: Component fetches /v1/channels/email/deliverability?sender_id=X&channel=Y with proper URL encoding
- AC DLW-12: RiskBandBadge reused from 216 D-15 (NOT re-implemented)
- AC DLW-13: Per-card "Recompute posture →" CTA emits AgentRun kind='channels-deliverability-rollup' (manual trigger of Plan 06 cron)
- AC DLW-14: D-21 server/client boundary noted in file header comment
- AC DLW-15: D-08 token-only enforced (zero hex literals in module.css)
- AC DLW-16: Banned-lexicon zero-match on sender warming notes (CI assertion)
- AC DLW-17: Storybook story Channels/DeliverabilityWorkspace registers >=4 named-state stories (Warming / Healthy / Watch / AtRisk)
</surface_c_deliverability_workspace>

<surface_d_template_editor ac_count="26">
- AC TPE-1: Component is a client component ('use client' directive)
- AC TPE-2: Component renders all 5 channel filter chips verbatim per D-25
- AC TPE-3: Component renders all 4 template_type labels verbatim per D-25 (marketing / transactional / lifecycle / support)
- AC TPE-4: Component renders all 5 classifier finding kinds verbatim per D-52 (pricing_binding / pricing_text_scan / pricing_ast_violation / evidence_ttl / competitor_mention)
- AC TPE-5: Component renders all 3 classifier severity labels verbatim per D-52 (block / flag / info)
- AC TPE-6: Per-channel preview pane renders correctly: email shows subject + content_html_rendered; sms/whatsapp shows content_text_rendered; push shows title + body; in_app shows content_html_rendered
- AC TPE-7: Variable inspector table renders all 4 variable source labels verbatim per D-26 precedence
- AC TPE-8: Missing variables notice renders verbatim "[err] {N} required variable(s) missing."
- AC TPE-9: Classifier blocks_dispatch=true renders verbatim "[err] Template publish blocked — resolve {N} severity=block findings before save."
- AC TPE-10: Classifier flags only renders verbatim "[warn] {N} finding(s) flagged for review."
- AC TPE-11: Pricing allowlisted accessor renders .c-badge--info "allowlisted" + Money resolved value
- AC TPE-12: Pricing non-allowlisted renders PlaceholderBanner variant="billing_placeholder" with "{{MARKOS_PRICING_ENGINE_PENDING}}" verbatim
- AC TPE-13: Evidence binding TTL stale renders .c-badge--warning "TTL expired" + classifier finding severity='block' on evidence_ttl
- AC TPE-14: KbGroundingPanel reused from 216 D-15 (NOT re-implemented) for evidence_bindings rendering
- AC TPE-15: ClassifierChipRow reused from 216 D-15 (NOT re-implemented) for classifier findings rendering
- AC TPE-16: Sample recipient name in preview rendered via PIIRedactedField pii_classification="personal"
- AC TPE-17: Publish template CTA disabled when classifier_findings contains severity='block' OR banned-lexicon CI check fails
- AC TPE-18: Publish template CTA opens approval modal (215 billing-correction modal recipe) when classifier finding severity='block' on pricing_binding/factual_claim; calls buildApprovalPackage(kind='channel_template_publish_approval') per D-43
- AC TPE-19: Locale variant chain rendered: .c-chip-protocol parent_template_id when present; fallback notice when locale has no child
- AC TPE-20: Component fetches /v1/channels/templates/{id}/preview POST to render preview
- AC TPE-21: AST violation excerpt rendered with @babel/parser line/col reference per D-52
- AC TPE-22: Future phase placeholder "AI-suggest subject" renders PlaceholderBanner variant="future_phase_226_sales_enablement"
- AC TPE-23: D-21 server/client boundary noted in file header comment
- AC TPE-24: D-08 token-only enforced (zero hex literals in module.css)
- AC TPE-25: Banned-lexicon zero-match on template body content_blocks + EmailCampaign subject (CI assertion BEFORE save)
- AC TPE-26: Storybook story Channels/TemplateEditor registers >=6 named-state stories (Empty / Preview / ClassifierFindings / PricingFlagged / EvidenceTtlStale / LocaleFallback)
</surface_d_template_editor>

<surface_e_approval_review_panel ac_count="21">
- AC ARP-1: Component is a client component ('use client' directive)
- AC ARP-2: Component renders all 4 handoff_kind chip labels verbatim (33-36th chips per 208-04 chain extension; channel_dispatch_approval / channel_template_publish_approval / channel_suppression_bulk_approval / channel_program_pause_approval)
- AC ARP-3: Component renders all 4 status labels verbatim (pending / approved / rejected / revoked)
- AC ARP-4: Component renders all 7 triggerReason labels verbatim per D-16+D-18 (class_broadcast / class_revenue / count_500_plus / content_classifier_block / manual_override / re_engagement_90d / lifecycle_stage_lost)
- AC ARP-5: Approve CTA opens approval modal (215 billing-correction modal recipe per inheritance); on confirm POST /v1/channels/approvals/{id}/approve
- AC ARP-6: Reject CTA opens reject modal with reason capture (>=20 chars validation per 216 carry); on confirm POST /v1/channels/approvals/{id}/reject
- AC ARP-7: Revoke CTA opens revoke modal with reason capture (>=20 chars); when dispatch_run_id present AND campaign.status='dispatching', shows recipients_already_sent + remaining_recipients counters via RunStatusBadge run_id={dispatch_run_id} (P207); on confirm POST /v1/channels/approvals/{id}/revoke AND sends pause signal via P207 AgentRun pause API per D-17
- AC ARP-8: All approval-package CTAs call buildApprovalPackage from lib/markos/crm/agent-actions.ts:68 per D-43 (NEVER createApprovalPackage)
- AC ARP-9: ClassifierChipRow reused from 216 D-15 (NOT re-implemented) for classifier_findings rendering
- AC ARP-10: KbGroundingPanel reused from 216 D-15 (NOT re-implemented) for evidence_refs rendering
- AC ARP-11: HealthScoreBadge reused from 216 D-15 when subject is reply-related approval AND related_crm_id present
- AC ARP-12: Sample recipient name in sample render rendered via PIIRedactedField pii_classification="personal"
- AC ARP-13: Pricing refs allowlisted accessor renders Money resolved value; non-allowlisted renders PlaceholderBanner variant="billing_placeholder" with "{{MARKOS_PRICING_ENGINE_PENDING}}" verbatim
- AC ARP-14: Autonomy ceiling notice renders verbatim "[block] Autonomy ceiling reached for channel_dispatch_approval."
- AC ARP-15: Approved/Rejected/Revoked notices render verbatim with timestamp + actor + reason
- AC ARP-16: Recipient count + audience_snapshot_id chip + "Open audience →" mint-text inline link to P221 surface
- AC ARP-17: Component fetches /v1/channels/approvals/{id} GET to load approval detail
- AC ARP-18: D-21 server/client boundary noted in file header comment
- AC ARP-19: D-08 token-only enforced (zero hex literals in module.css)
- AC ARP-20: Banned-lexicon zero-match on reason capture (CI assertion)
- AC ARP-21: Storybook story Channels/ApprovalReviewPanel registers >=4 named-state stories (Pending / Approved / Rejected / Revoked)
</surface_e_approval_review_panel>

<surface_f_morning_brief_channel_entries ac_count="20" mobile_priority="critical">
- AC MBC-1: Component is a client component ('use client' directive)
- AC MBC-2: Component renders 2 sections: "Reply pending threads" + "At-risk senders" verbatim
- AC MBC-3: Section 1 reply_pending threads limited to top 5 newest reply first
- AC MBC-4: Section 1 filtered by owner_user_id == current_operator_user_id (per D-23 reply continuity)
- AC MBC-5: Section 2 includes ALL senders with reputation_status='at_risk' (no limit)
- AC MBC-6: Per-thread recipient name rendered via PIIRedactedField pii_classification="personal"
- AC MBC-7: Per-sender at_risk next-action renders verbatim "Pause broadcast class until reputation recovers."
- AC MBC-8: Spike detected notice renders verbatim "[warn] Bounce spike — {N}σ above 7d baseline." when cron channels-bounce-spike-alert detected >2σ
- AC MBC-9: Empty Section 1 renders verbatim "[ok] All threads attended."
- AC MBC-10: Empty Section 2 renders verbatim "[ok] All senders healthy."
- AC MBC-11: Per-thread "Open thread →" mint-text inline link deep-links to MessagingThreadsList row
- AC MBC-12: Per-sender "Open sender →" mint-text inline link deep-links to DeliverabilityWorkspace per-sender card
- AC MBC-13: Per-thread escalated badge renders when current_status='escalated' (per D-23)
- AC MBC-14: Reputation score renders with tabular numerals (.c-code-inline)
- AC MBC-15: mobile_priority=critical registered in lib/markos/operator/shell.ts SurfaceRouteContract with surface_family: channels_brief
- AC MBC-16: Touch target >=44px on coarse pointers (already global per 213.2)
- AC MBC-17: Component fetches both endpoints in parallel; renders independently
- AC MBC-18: Renders alongside existing 208-02 Morning Brief sections (additive — does NOT replace existing content)
- AC MBC-19: D-21 server/client boundary noted in file header comment
- AC MBC-20: D-08 token-only enforced (zero hex literals in module.css)
</surface_f_morning_brief_channel_entries>

<surface_g_approval_inbox_channel_entries ac_count="15">
- AC AIC-1: Component is a client component ('use client' directive)
- AC AIC-2: Component renders all 4 handoff_kind chip labels verbatim (33-36th chips per 208-04 chain extension)
- AC AIC-3: Component renders 5 subject_type-specific row titles correctly
- AC AIC-4: Component renders all 4 class chip labels verbatim per D-02 (when subject_type='email_campaign')
- AC AIC-5: Component renders all 5 channel chip labels verbatim
- AC AIC-6: Component renders all 7 triggerReason labels verbatim per D-16+D-18
- AC AIC-7: Per-row recipient sample rendered via PIIRedactedField pii_classification="personal"
- AC AIC-8: Subject_type='channel_program_pause' renders RunStatusBadge run_id={dispatch_run_id} (P207) + recipients_already_sent + remaining_recipients counters with tabular numerals
- AC AIC-9: Subject_type='channel_suppression_bulk' renders DSR-source flag chip when applicable
- AC AIC-10: Per-row "Open approval →" mint-text inline link deep-links to ApprovalReviewPanel for the row's approval_ref
- AC AIC-11: Empty state renders verbatim "[info] No channel approvals pending."
- AC AIC-12: Component fetches /v1/channels/approvals?status=pending GET to load approval list
- AC AIC-13: Renders alongside existing 208-04 Approval Inbox row renderers (additive — handles 4 new handoff_kind chips; CRM kinds remain DEFERRED to future P208 admin extension per future_phase_222_approval_inbox_extensions translation gate)
- AC AIC-14: D-21 server/client boundary noted in file header comment
- AC AIC-15: D-08 token-only enforced (zero hex literals in module.css)
</surface_g_approval_inbox_channel_entries>

<surface_h_outbound_workspace_evolved ac_count="11">
- AC OWE-1: Outer shell tenant context wrapper preserved verbatim
- AC OWE-2: Existing CRM outbound entry points (api/crm/outbound/*.js legacy paths) continue to function via legacy adapter (NOT broken)
- AC OWE-3: Tab bar renders 5 tabs verbatim: "Programs" / "Threads" / "Deliverability" / "Templates" / "Approvals"
- AC OWE-4: Active tab indicator uses mint underline (--color-primary-text per D-09)
- AC OWE-5: Each tab content renders the corresponding NEW component (ChannelProgramsList / MessagingThreadsList / DeliverabilityWorkspace / TemplateEditor / ApprovalReviewPanel)
- AC OWE-6: Legacy sections render with "Legacy (deprecating)" badge composed from .c-badge--info + bracketed glyph
- AC OWE-7: Legacy adapter lib/markos/channels/adapters/legacy-outbound.ts (P223-owned greenfield per D-44) bridges legacy outbound -> new channel program models
- AC OWE-8: Mobile breakpoint behavior unchanged; tab bar collapses to dropdown on mobile < 640px
- AC OWE-9: D-21 server/client boundary noted in file header comment
- AC OWE-10: D-08 token-only enforced (zero hex literals in module.css)
- AC OWE-11: outbound-workspace integration test asserts tab bar renders all 5 tabs AND legacy sections remain mounted
</surface_h_outbound_workspace_evolved>

<surface_i_timeline_detail_view_evolved ac_count="14">
- AC TDE-1: 222 baseline TimelineDetailView functionality preserved (all 15 222 ACs continue to pass)
- AC TDE-2: Channel-event-chips section renders ONLY when source_domain IN ('email','messaging','push','in_app')
- AC TDE-3: Per-row chips render 7 event types verbatim (opened / clicked / replied / bounced / complained / unsubscribed / suppressed)
- AC TDE-4: Per-row chip color coding: opened/clicked → success; replied → sentiment-coded (positive→success, negative→error, neutral→info); bounced/complained → error; unsubscribed → warning; suppressed → subtle neutral
- AC TDE-5: Per-row chip bracketed-glyph pairing: opened/clicked/replied[positive] → [ok]; bounced/complained → [err]; unsubscribed → [warn]; suppressed → [block]; replied[negative] → [err]; replied[neutral] → [info]
- AC TDE-6: Chips read from dispatch_events keyed on source_event_ref (per D-29 fan-out)
- AC TDE-7: Click chip opens detail pop showing .c-chip-protocol dispatch_attempt_id + provider_message_id
- AC TDE-8: Provider secrets NEVER appear in detail pop (only public IDs per D-39)
- AC TDE-9: 222 commercial_signal taxonomy 7-enum verbatim (interest/risk/expansion/renewal/support/pricing/silence) honored in chip color coding
- AC TDE-10: 222 source_domain 11-enum verbatim labels honored ("email", "messaging" — per D-05)
- AC TDE-11: D-21 server/client boundary preserved (existing 222 'use client' directive)
- AC TDE-12: D-08 token-only enforced (zero NEW hex literals introduced by 223 evolution)
- AC TDE-13: TimelineDetailView integration test asserts channel-event-chips render for source_domain IN ('email','messaging','push','in_app') AND do NOT render for other source_domains
- AC TDE-14: Storybook story Crm360/TimelineDetailView (existing 222 story) gains new variant "WithChannelEvents" demonstrating chip rendering
</surface_i_timeline_detail_view_evolved>

<cross_surface_acceptance_criteria ac_count="31">
- AC XC-1: All 5 NEW components + 2 P208 entry-type renderers register Storybook CSF3 named-state stories under Channels/* path; 223-06 Plan ships chromatic.config.json covering 5 NEW components × >=4 variants each (>=20 snapshots)
- AC XC-2: All 5 NEW + 2 P208 renderer + 2 EVOLVED components honor D-08 token-only (zero hex literals in module.css files)
- AC XC-3: All 5 NEW + 2 P208 renderer components honor D-09 mint-as-text + D-09b .c-notice mandatory + D-13 .c-card--feature reserved + D-14 no .c-table
- AC XC-4: All 5 NEW + 2 P208 renderer components are 'use client' per existing convention; consuming pages remain server components per D-21
- AC XC-5: All 5 NEW + 2 P208 renderer components consume PIIRedactedField (216 D-15) for any CDP-resolved IdentityProfile recipient PII field
- AC XC-6: All 5 NEW + 2 P208 renderer + 2 EVOLVED components register in lib/markos/operator/shell.ts SurfaceRouteContract with surface_family: channels_* and correct mobile_priority literal (MessagingThreadsList + MorningBriefChannelEntries = critical; 7 surfaces = secondary)
- AC XC-7: Banned-lexicon zero-match enforced on template body content_blocks + EmailCampaign subject + reply suggestion body + classifier-finding operator-note + sender warming notes + suppression operator-note + ApprovalReviewPanel reason capture BEFORE TemplateEditor "Publish template" save AND BEFORE external.send dispatch
- AC XC-8: All approval-package call paths use buildApprovalPackage from lib/markos/crm/agent-actions.ts:68 per D-43 (NEVER createApprovalPackage); grep createApprovalPackage across components/markos/crm/outbound/ + components/markos/operator/ returns 0 matches
- AC XC-9: All 5 cron handlers (Plan 06) ship under api/cron/channels-*.js (NOT app/api/cron/channels-*/route.ts) per D-49; auth via x-markos-cron-secret header
- AC XC-10: All 19 REST handlers ship under api/v1/channels/*.js (NOT app/api/v1/channels/*/route.ts) per D-42; auth via requireHostedSupabaseAuth from onboarding/backend/runtime-context.cjs:491 per D-42
- AC XC-11: Test runner is npm test (Node --test) per D-46; all *.test.js files (NOT .test.ts); imports node:test + node:assert/strict; NO NEW vitest, NO NEW playwright runtime install in P223
- AC XC-12: Plan 06 closeout meta-test asserts package.json did not gain vitest or NEW @playwright/test keys during P223 execution (existing axe-playwright devDep is preserved per D-46)
- AC XC-13: Plan 01 Task 0.5 architecture-lock detector test scans every .planning/phases/223-*-PLAN.md for forbidden tokens
- AC XC-14: Hard preflight upstream gate via assertUpstreamReady(['P205','P207','P208','P209','P210','P211','P221','P222']) per D-45; NO soft-skip, NO "stub if missing"
- AC XC-15: All 9 migrations slot-pre-allocated per D-41 (P223 owns 113..121; 9 slots; no gaps); closeout migration-slot-collision.test.js (Plan 06) asserts no overlap
- AC XC-16: All 10 F-IDs slot-pre-allocated per D-40 (P223 owns F-122..F-131; 10 slots; no gaps); closeout f-id-collision.test.js (Plan 06) asserts no overlap
- AC XC-17: All 11 new tables (sender_identities / deliverability_posture / channel_templates / email_campaigns / messaging_threads / lifecycle_journeys / lifecycle_journey_runs / channel_suppressions / dispatch_events / dispatch_skips / in_app_messages) have RLS verified cross-tenant denied via Plan 06 test/channels/closeout/rls-suite.test.js
- AC XC-18: D-50 frequency-cap BEFORE INSERT trigger on dispatch_events enforces rolling-window cap (default 7 days, tenant-configurable); rejects when count >= cap; replaces previous "Claude's Discretion" service-layer approach
- AC XC-19: D-51 consent-write trigger on outbound_consent_records_legacy enforces single-writer rule via app.consent_writer_source GUC; only setConsentState (P221) writes
- AC XC-20: D-52 AST/allowlist content classifier via @babel/parser (NOT 5-line regex); ALLOWLISTED_ACCESSORS = ['getPricingFor','formatPrice','usePricing','pricingFromEngine']
- AC XC-21: D-29 single fan-out emit() function in lib/markos/channels/events/emit.ts writes 5 destinations transactionally (cdp_events + crm_activity + aggregate-counter + dispatch_events + setConsentState if consent-affecting); fail-closed
- AC XC-22: D-19 per-recipient 6-layer dispatch gate enforced (Consent → Suppression → SessionWindow [WhatsApp only per D-54] → FrequencyCap [DB trigger per D-50] → QuietHours → Jurisdiction); skipped recipients write dispatch_skips row with gate_failed reason
- AC XC-23: D-54 WhatsApp 24-hour session window check runs BEFORE quiet-hours layer; defer-past-session is fail-closed reason='whatsapp_session_lost'
- AC XC-24: Pitfall 7 auto-reply detection (Plan 04) — vacation responder + Auto-Submitted header + In-Reply-To + min body length filters BEFORE reply flow; auto-replies log only, no thread.reply_pending transition
- AC XC-25: Pitfall 8 Knock cascade (Plan 04) — push delivery_failed event maps to handleBounceEvent('hard') → suppression chain
- AC XC-26: Pitfall 9 + A11 Resend metadata-only webhook (Plan 04) — inbound webhook payload contains METADATA only; reply handler MUST call Resend Received Emails API to fetch body BEFORE opt-out detection / sentiment classification
- AC XC-27: Reply continuity flow (D-22 + D-23) — inbound reply → lookupOrCreateThread → set thread.current_status='reply_pending' → append crm_activity → emit cdp_events → create CRM task → fire opt-out detector → if matched, setConsentState (single writer rule D-12 + D-51 trigger)
- AC XC-28: Plan 06 ships Chromatic snapshot gate covering 5 NEW UI surfaces with Empty / Populated / Error / Tombstoned variants (>=4 named-state stories per surface; TemplateEditor adds ClassifierFindings + PricingFlagged for >=6)
- AC XC-29: Plan 06 ships ONE operator-journey E2E test using EXISTING axe-playwright devDep per D-46 (campaign create → approve → dispatch → reply → bounce); NOT a new playwright runtime
- AC XC-30: Plan 06 closeout regression — P100-P105 + P102 Kanban default + P103 urgency bias + P105 record brief + P101 HIGH_SIGNAL + P221 consent + P222 customer360/NBA/lifecycle/committee all green
- AC XC-31: Plan 06 autonomous: false per Plan 06 RL1 — operator review checkpoint:human-action for sender warming + DKIM/SPF/DMARC verification + Knock app provisioning + Resend dashboard webhook configuration
</cross_surface_acceptance_criteria>

</full_surface_acceptance_criteria_fold>

<cross_cutting_carry_forward>
- **D-15 extracted components REUSED VERBATIM (carry from 222 D-15 reuse manifest; UI-SPEC line 1655-1669):** 7 components first consumed in 217-06, REUSED in 222, REUSED in 223 — NEVER re-implemented. Plan 05 surfaces consume: HealthScoreBadge (MessagingThreadsList per-row when related_crm_id resolves to Customer360 with health_score; ApprovalReviewPanel header when subject is reply-related approval); RiskBandBadge (MessagingThreadsList per-row + DeliverabilityWorkspace per-sender at_risk indicator); KbGroundingPanel (TemplateEditor "Bindings" panel evidence_bindings rendering; ApprovalReviewPanel evidenceRefs panel); RetentionClassChip (per-PII-field rendering on TemplateEditor preview, MessagingThreadsList per-row, ApprovalReviewPanel recipient sample, DeliverabilityWorkspace suppression list); PIIRedactedField (5 surfaces: TemplateEditor preview pane sample recipient + MessagingThreadsList per-row recipient name + ApprovalReviewPanel recipient sample list + DeliverabilityWorkspace suppression list per-row + MorningBriefChannelEntries reply_pending recipient name + ApprovalInboxChannelEntries recipient sample); ClassifierChipRow (TemplateEditor "Classifier findings" overlay 5 finding kinds + ApprovalReviewPanel classifier_findings panel + TimelineDetailView per-channel-event-row chip rendering D-34); SaveOfferPricingBlock NOT consumed in 223. Per-component grep gate: each Plan 05 component imports from extracted-component origin (NOT re-defines). Plan 06 closeout d-15-reuse.test.js asserts no re-implementations.
- **4 NEW handoff_kind literals (33rd-36th) verbatim (carry from UI-SPEC §Approval Inbox Handoff Chain Extension lines 1563-1592):** 33rd channel_dispatch_approval (ApprovalReviewPanel "Approve" path when class IN ('broadcast','revenue') OR audience_size >= 500 OR re-engagement >90d D-18 OR lifecycle_stage='lost' D-18; calls buildApprovalPackage per D-43); 34th channel_template_publish_approval (TemplateEditor "Publish template" path when content_classifier finding severity='block' on pricing_binding/factual_claim D-16/D-52; calls buildApprovalPackage per D-43); 35th channel_suppression_bulk_approval (DeliverabilityWorkspace "Bulk add suppressions" path when count > 100 D-32; calls buildApprovalPackage per D-43); 36th channel_program_pause_approval (ApprovalReviewPanel "Revoke" path mid-dispatch D-17; calls buildApprovalPackage per D-43; on confirm sends pause signal via P207 AgentRun pause API). END-OF-v4.2.0-Wave-2 chip count post-223 = 36 (32 pre-223 + 4 NEW). Plan 06 closeout chip-count grep gate verifies all 4 literals appear in lib/markos/crm/agent-actions.ts (Plan 03 buildApprovalPackage extension) + components/markos/operator/ApprovalInboxChannelEntries.tsx (Plan 05).
- **4 channel kinds + 4 provider adapters verbatim (carry from UI-SPEC §Plan Scope Classification + §UI Compatibility Note):** 5 channel kinds = email / sms / whatsapp / push / in_app (SMS and WhatsApp share Twilio adapter per D-08). 4 provider adapters: Resend (email; @resend/node SDK per Plan 04) + Twilio (SMS+WhatsApp; per Plan 04) + Knock (push; @knocklabs/node SDK per Plan 04) + in_app (internal markos provider; INSERT in_app_messages row; no external API). Wired in Plan 04. Plan 05 surfaces consume verbatim: ChannelProgramsList class chip group (4 chips) + channel chip group (5 chips); MessagingThreadsList channel chip group (5 chips); DeliverabilityWorkspace per-sender channel chip; TemplateEditor channel chip group (5 chips); ApprovalInboxChannelEntries class chip + channel chip per row.
- **D-29 single fan-out emit() 5-destination UI binding (carry from UI-SPEC line 61 + AC XC-21):** Plan 04 emit.ts writes 5 destinations transactionally (cdp_events + crm_activity + aggregate-counter + dispatch_events + setConsentState if consent-affecting). Plan 05 surfaces consume the 5-destination output: (1) cdp_events feeds TimelineDetailView per-row chip rendering via P222 commercial_signal taxonomy; (2) crm_activity feeds Customer360 timeline (P222 surface — consumed by 222 D-25 TimelineDetailView via D-34 channel event chips); (3) aggregate-counter feeds ChannelProgramsList per-card delivered/opened/clicked/replied counters AND MessagingThreadsList per-row last_message_at; (4) dispatch_events feeds DeliverabilityWorkspace 24h posture metrics; (5) setConsentState feeds ConsentState UI in P221 surfaces (NOT 223; out of scope for 223-05).
- **Banned-lexicon zero-match BEFORE save AND BEFORE external.send dispatch (carry from UI-SPEC AC XC-7):** 7 surfaces enforce — TemplateEditor template body content_blocks + EmailCampaign subject (CI assertion BEFORE Publish save) + MessagingThreadsList reply suggestion body + ClassifierChipRow operator-note + DeliverabilityWorkspace sender warming notes + DeliverabilityWorkspace suppression operator-note + ApprovalReviewPanel reason capture (>=20 chars per 216 carry). Each component file header comment cites "AC XC-7 — banned-lexicon zero-match required". Server-side CI runs scripts/marketing-loop/check-banned-lexicon.mjs against the full string set; failure -> reject save AND reject dispatch.
- **B-9 NEW provider secrets env-only (carry from UI-SPEC §Sensitive Credential UI Binding Layer 6 + B-9 NEW per D-39):** RESEND_API_KEY / TWILIO_AUTH_TOKEN / KNOCK_API_KEY / RESEND_SIGNING_SECRET / TWILIO_WEBHOOK_SECRET / KNOCK_WEBHOOK_SECRET / MARKOS_WEBHOOK_CRON_SECRET NEVER in UI surface, MCP response, or audit_log payload; only provider_message_id (public per-message ID) is exposed. Plan 05 components.markos.crm.outbound.* + components.markos.operator.* AND api/v1/channels/*.js + lib/markos/channels/api/* + lib/markos/channels/mcp/* MUST NOT contain any RESEND_API_KEY / TWILIO_AUTH_TOKEN / KNOCK_API_KEY string literal (verified by Plan 06 closeout secret-leak.test.js).
- **Architecture-lock D-42 + D-46 + D-47 + D-48 verbatim (carry from 217 architecture-lock; UI-SPEC AC XC-9..XC-13):** legacy api/v1/channels/*.js (NOT App Router app/api/v1/channels/*/route.ts); requireHostedSupabaseAuth from onboarding/backend/runtime-context.cjs:491 (NOT requireSupabaseAuth which does not exist); npm test runner Node --test (NO vitest run, NO playwright test in <verify><automated>); .test.js files (NOT .test.ts per D-46); contracts/openapi.json (NOT public/openapi.json per D-47); lib/markos/mcp/tools/index.cjs (NOT .ts per D-48); api/cron/*.js for cron (NOT app/api/cron/*/route.ts per D-49 — cron in Plan 06). Forbidden tokens scanned by Plan 01 Task 0.5 architecture-lock detector test.
- **Helper canon D-43 verbatim (carry from 217 architecture-lock):** buildApprovalPackage from lib/markos/crm/agent-actions.ts:68 — NEVER createApprovalPackage (verified non-existent in codebase). Plan 05 ApprovalReviewPanel Approve/Reject/Revoke calls + ChannelProgramsList "Awaiting approval" notice + TemplateEditor "Publish template" approval modal + DeliverabilityWorkspace "Bulk add suppressions" approval modal — ALL route through buildApprovalPackage per D-43. Closeout grep gate: grep createApprovalPackage components/markos/crm/outbound/ + components/markos/operator/ + api/v1/channels/ + lib/markos/channels/{api,mcp}/ returns 0.
- **Mobile priority literals (UI-SPEC line 80-93; AC XC-6 + AC MTL-16 + AC MBC-15):** 2 critical surfaces (MessagingThreadsList + MorningBriefChannelEntries) registered with surface_family: channels_threads + channels_brief in lib/markos/operator/shell.ts SurfaceRouteContract; 7 secondary surfaces. desktop_only is FORBIDDEN as a mobile_priority value (208-01 architecture-lock). Touch target >=44px on coarse pointers (already global per 213.2 cross-cutting fix).
- **Chromatic snapshot gate (carry from UI-SPEC AC XC-1 + XC-28; gate ownership = Plan 06):** 5 NEW UI surfaces × >=4 variants each = >=20 snapshots; TemplateEditor adds ClassifierFindings + PricingFlagged for >=6. Plan 05 ships 5 .stories.tsx files under .storybook/stories/ with CSF3 named-state stories. Plan 06 ships chromatic.config.json registering all stories under Channels/* path. Storybook stories register UNDER Channels/* path and IMPORT extracted components from 217-06 origin per D-15 manifest.
- **PII inheritance from 215+216+221+222 (UI-SPEC §Design System line 114; AC XC-5):** All recipient PII (CDP-resolved primary_email / primary_phone / company_name / display_name) consumed by Plan 05 surfaces is sourced via P221 lib/markos/cdp/adapters/crm-projection.ts::getProfileForContact (per D-44 + D-45 hard preflight). Surfaces render via PIIRedactedField with pii_classification ENUM 5-value taxonomy. Audit-log event_type='identity_view' mirrors credential_view pattern (every PII field render writes audit row to markos_audit_log P201 hash chain). PIIRedactedField onCopy interceptor MUST preventDefault() to block PII clipboard exfiltration.
- **Pricing display posture (UI-SPEC §Design System line 115):** All template pricing_bindings[] references render via Money fromPricingRecommendation={pr_id} recipe consuming P205 PricingRecommendation context XOR {{MARKOS_PRICING_ENGINE_PENDING}} sentinel via PlaceholderBanner variant="billing_placeholder" whenever pricing variable resolves through non-allowlisted accessor per D-27 + D-52. Phase 223 MUST NOT take pricing ownership. Zero hard-coded dollar/peso literals in template body or campaign subject. JBM font-feature-settings: 'tnum' 1 (tabular-numerals) for all monetary preview columns + classifier finding metadata + deliverability rate columns.
- **Server/client boundary D-21 (UI-SPEC §Design System line 118; AC XC-4):** 5 NEW + 2 P208 renderer components are 'use client' per existing convention in components/markos/crm/* + components/markos/operator/* since they own interactive state (kanban filter chips with local state, thread row click → detail panel, deliverability sender selector, template variable input + preview re-render, approve/reject/revoke optimistic update, Morning Brief refresh interval, Approval Inbox row expansion). Consuming pages remain server components by default per D-21. outbound-workspace.tsx EVOLVED parent shell is 'use client' (existing convention). TimelineDetailView.tsx EVOLVED inherits existing 222 'use client' directive. Server/client boundary named in each component's file header comment per D-21.
- **Translation gates dissolved by Plan 05 (UI-SPEC §translation_gates_opened_by_223 future_phase_223_approval_inbox_renderers + future_phase_223_morning_brief_renderers):** Plan 05 ships ApprovalInboxChannelEntries.tsx renderer (4 NEW handoff_kind chips render — DISSOLVES future_phase_222_approval_inbox_extensions for channel-related entry types) + MorningBriefChannelEntries.tsx renderer (2 channel-related rollup sections render — DISSOLVES future_phase_222_approval_inbox_extensions for channel-related rollups). CRM kinds (30/31/32) remain DEFERRED to future P208 admin extension.
</cross_cutting_carry_forward>

<downstream_ui_consumers>
<!-- Plan 05 IS the UI plan. Direct UI surfaces ship here. Downstream consumers: Plan 06 Chromatic gate + axe-playwright operator-journey E2E + manual operator checkpoints; future P224+ phases reuse 5 NEW components in dedicated admin pages. -->
- ChannelProgramsList (CPL-1..19; UI-SPEC Surface A) — 5 component files ship here; reads sender_identities + channel_templates + email_campaigns + lifecycle_journeys + messaging_threads (Plan 01 SOR); reads buildApprovalPackage extension fields (Plan 03 — triggerReason); per-card RunStatusBadge run_id={dispatch_run_id} reads markos_agent_runs (P207); aggregate counters read email_campaigns aggregate columns (Plan 04 emit() writes).
- MessagingThreadsList (MTL-1..21; UI-SPEC Surface B; mobile_priority=critical) — reads messaging_threads (Plan 01 SOR); reads customer_360_records + nba_records (P222) for per-row HealthScoreBadge + RiskBandBadge + Open Customer360 / Open NBA links; reads thread.current_status='reply_pending' (Plan 04 reply-flow); reads dispatch_events.metadata.auto_reply (Plan 04 Pitfall 7); reads dispatch_skips.skip_reason='whatsapp_session_lost' (Plan 02 D-54); reads ConsentState mutation via P221 adapter (D-23 step 6 opt-out detected).
- DeliverabilityWorkspace (DLW-1..17; UI-SPEC Surface C) — reads sender_identities + deliverability_posture (Plan 01 SOR); reads channel_suppressions (Plan 02 SOR); reads dispatch_events for spike alert (Plan 04 emit() writes); reputation_status badge reads sender_identities.reputation_status (Plan 06 cron rolls up).
- TemplateEditor (TPE-1..26; UI-SPEC Surface D) — reads channel_templates (Plan 01 SOR); calls renderPreview (Plan 03 Task 1) for per-channel preview; calls classifyTemplate (Plan 03 Task 2) for classifier findings overlay; on Publish CTA when classifier finding severity='block' on pricing_binding/factual_claim, calls buildApprovalPackage(kind='channel_template_publish_approval') per D-43 (Plan 03 extension).
- ApprovalReviewPanel (ARP-1..21; UI-SPEC Surface E) — reads approval_packages SOR (P208); reads buildApprovalPackage extension fields (rationale.triggerReason + rationale.classifierFindings + rationale.pricingRefs + rationale.evidenceRefs from Plan 03 extension); Approve/Reject/Revoke CTAs call /v1/channels/approvals/{id}/{action} (this plan Task 1) which routes through buildApprovalPackage + revokeApproval (Plan 03 Task 3) for Revoke path. Mid-dispatch revoke sends pause signal to AgentRun (P207).
- MorningBriefChannelEntries (MBC-1..20; UI-SPEC Surface F; mobile_priority=critical) — reads messaging_threads.current_status='reply_pending' filtered by owner_user_id (Plan 04 reply-flow sets this) + sender_identities.reputation_status='at_risk' (Plan 06 cron rolls up from dispatch_events Plan 04 writes).
- ApprovalInboxChannelEntries (AIC-1..15; UI-SPEC Surface G) — reads approval_packages SOR (P208) for channel-related entries (handoff_kind starts with 'channel_'); reads email_campaigns / lifecycle_journeys / channel_templates / channel_suppressions / markos_agent_runs (per subject_type) for subject metadata.
- outbound-workspace.tsx EVOLVED (OWE-1..11; UI-SPEC Surface H per D-33) — preserves shell + adds tab bar + renders 5 NEW components; legacy sections remain mounted via legacy-outbound adapter from 223-01.
- TimelineDetailView.tsx EVOLVED (TDE-1..14; UI-SPEC Surface I per D-34) — gains channel-event-chips when source_domain in ('email','messaging','push','in_app'); reads dispatch_events keyed on source_event_ref (Plan 04 emit() writes).
- 19 /v1/channels/* legacy flat .js routes (this plan Task 1) — power all 5 NEW + 2 P208 renderer components via fetch.
- 5 MCP tools at lib/markos/mcp/tools/index.cjs (this plan Task 1) — power MCP-driven outreach (P226 sales enablement future consumer).
- 5 cron handlers (Plan 06) — feed all 5 NEW Plan 05 surfaces with rolling 24h posture + spike alerts + lifecycle journey advancement + tombstone cascade + soft-bounce promotion.
- Chromatic snapshot gate (Plan 06) — gates all 5 NEW UI surfaces + 2 P208 renderers + 2 EVOLVED surfaces × >=4 variants each = >=22 snapshots.
- axe-playwright operator-journey E2E (Plan 06) — 1 E2E test using EXISTING devDep verifies campaign create -> approve -> dispatch -> reply -> bounce flow across all 9 surfaces.
- AC XC-1..XC-31 (all Cross-Surface) verified by Plan 06 closeout regression suite.
- Future-Surface 1 (future_phase_223_admin_ui) — multi-page Channel Engine admin surface composes 5 NEW Plan 05 components in dedicated routes; D-32 architecture-lock holds (legacy api/*.js routes; auth canon; test runner; OpenAPI; MCP registry; cron; helper canon buildApprovalPackage).
- Future-Surface 5 (future_phase_223_chromatic_baselines) — Plan 06 ships gate; downstream phases (P224+) MUST NOT add components/markos/crm/outbound/* without registering corresponding Storybook stories + Chromatic snapshots.
</downstream_ui_consumers>

</ui_spec_fold>


<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build shared route handler primitives + 19 /v1/channels/* HTTP route files (legacy flat per D-42) + MCP tool family at lib/markos/mcp/tools/index.cjs (D-48) + tenant-isolation tests + OpenAPI regen at contracts/openapi.json (D-47)</name>
  <read_first>
    - api/tenant/whoami.js + api/tenant/runs.js + api/tenant/keys.js (existing route patterns from P204 — legacy flat .js)
    - onboarding/backend/runtime-context.cjs (line 491 requireHostedSupabaseAuth — D-42)
    - lib/markos/cli/runs.cjs (Pattern reference for tenant-scoped handler primitives)
    - lib/markos/mcp/tools/index.cjs (current MCP tool registration pattern — D-48 .cjs)
    - .planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md (D-30, D-31, D-39, D-42, D-47, D-48)
    - .planning/phases/223-native-email-messaging-orchestration/223-RESEARCH.md (§API + MCP Surface §Schema sketches)
    - contracts/F-122..F-131 yaml (Wave 1-3 outputs — schemas)
    - lib/markos/channels/programs/* + lib/markos/channels/templates/* + lib/markos/channels/dispatch/* + lib/markos/channels/suppression/* (Waves 1-3 lib outputs)
    - obsidian/reference/HTTP Layer.md (auth + middleware + error mapping conventions)
    - contracts/openapi.json (current shape — append paths, not rewrite; D-47)
  </read_first>
  <behavior>
    - Test 1 (POST /v1/channels/email/programs class=transactional): bearer + valid body + transactional class -> 201 + campaign object with status='draft'; no approval_ref required.
    - Test 2 (POST /v1/channels/email/programs class=broadcast without approval_ref): same but class='broadcast' -> evaluateApprovalTriggers fires -> buildApprovalPackage (D-43) -> response 201 with status='pending_approval' + approval_ref populated. Approval Inbox entry created (P208).
    - Test 3 (POST /v1/channels/email/programs/{id}/dispatch unapproved): campaign.status='draft' -> 422 reason_code='approval_required'.
    - Test 4 (POST /v1/channels/email/programs/{id}/dispatch approved): campaign.status='approved' -> createDispatchRun (calls assertUpstreamReady(['P207']) per D-15-revised) -> enqueueRecipients -> 202 with run_id.
    - Test 5 (GET /v1/channels/email/deliverability): returns array of { sender_id, sender_label, current_window: { reputation_score, trend, bounce_rate, complaint_rate, ... } }.
    - Test 6 (GET /v1/channels/messaging/threads with filter): ?owner_user_id=X&status=reply_pending -> filtered list scoped to tenant.
    - Test 7 (POST /v1/channels/messaging/sends): single recipient send via handleRecipient short-circuit; returns dispatch_attempt_id.
    - Test 8 (POST /v1/channels/messaging/threads/{id}/escalate): updates thread.current_status='escalated' + creates CRM task; audit_log row written.
    - Test 9 (POST /v1/channels/lifecycle/journeys/{id}/pause + /resume): status transitions journey + pauses pending journey_runs.
    - Test 10 (POST /v1/channels/templates with pricing-touching content): triggers content classifier (D-52 AST/allowlist); if blocks_dispatch=true -> 422 with classifier_findings; else 201.
    - Test 11 (POST /v1/channels/templates/{id}/preview): returns rendered preview + classifier_findings + missing_variables.
    - Test 12 (GET /v1/channels/approvals): pending channel approvals only.
    - Test 13 (POST /v1/channels/approvals/{id}/approve|reject|revoke): mutates approval state via buildApprovalPackage (D-43); revoke triggers AgentRun pause if mid-dispatch (D-17).
    - Test 14 (GET /v1/channels/suppressions + POST bulk add): bulk add requires approval_ref (D-32).
    - Test 15 (TENANT ISOLATION — all 11 routes): bearer token for tenant_A makes any request for resources owned by tenant_B -> 403 or 404 with no resource leak; cross-tenant SELECT returns empty.
    - Test 16 (MCP send_email_program approved): existing approved campaign -> dispatch.
    - Test 17 (MCP send_email_program unapproved): -> error response 'approval_required'.
    - Test 18 (MCP get_thread): valid thread_id -> thread + last 20 messages; non-existent -> not_found.
    - Test 19 (MCP list_pending_approvals): returns only this tenant's channel approvals.
    - Test 20 (MCP get_deliverability_posture): returns current posture object per sender.
    - Test 21 (No raw secrets in MCP responses — D-39): get_deliverability_posture response does NOT contain RESEND_API_KEY/TWILIO_AUTH_TOKEN/KNOCK_API_KEY material.
    - Test 22 (architecture-lock — D-42 enforcement): grep `requireHostedSupabaseAuth` across all 19 route files succeeds (≥19 matches). grep `requireSupabaseAuth(?!\w)` returns 0. grep `route.ts` across `api/v1/channels/` returns 0. grep `app/api/` does NOT include channels paths.
  </behavior>
  <action>
    1. Create `lib/markos/channels/api/handlers.js` (`.js` per D-46, exposed via require()):
       - `withChannelRouteContext(req, res, handler)` — extracts bearer, calls `requireHostedSupabaseAuth({ req, runtimeContext, operation })` from `onboarding/backend/runtime-context.cjs:491`, sets app.tenant_id session config, wraps handler with audit context, maps errors to JSON {error, code, request_id}.
       - `channelRouteSchemas` object with zod schemas for every body shape (createEmailCampaign, dispatchCampaign, sendMessagingMessage, escalateThread, pauseJourney, resumeJourney, createTemplate, previewTemplate, approveApproval, rejectApproval, revokeApproval, bulkAddSuppressions).
       - Reusable error builder + response shape: `{ error, code, request_id }`.
       - **D-42 compliance:** does NOT import `requireSupabaseAuth` or `requireTenantContext` (they don't exist). Does NOT use `serviceRoleClient` directly.
    2. Create 19 route file groups under `api/v1/channels/` (legacy flat per D-42 — NOT `route.ts`, NOT App Router):
       - `email/programs.js` (GET list + POST create); `email/programs-by-id.js` (GET + PATCH + DELETE for /:id); `email/programs-dispatch.js` (POST /:id/dispatch); `email/sends-by-id.js` (GET retrieve individual send + POST re-send with new dispatch_attempt_id for idempotent retry + DELETE cancel in-flight via AgentRun pause signal — D-30 explicit).
       - `email/senders.js` (GET list + POST create + PATCH /:id/verification).
       - `email/deliverability.js` (GET — read-only).
       - `messaging/threads.js` (GET list + POST create + GET /:id); `messaging/threads-escalate.js` (POST /:id/escalate).
       - `messaging/sends.js` (POST single message).
       - `lifecycle/journeys.js` (GET list + POST create + GET /:id); `lifecycle/journeys-pause.js` (POST /:id/pause); `lifecycle/journeys-resume.js` (POST /:id/resume).
       - `templates.js` (GET list + POST create + GET/PATCH /:id); `templates-preview.js` (POST /:id/preview).
       - `approvals.js` (GET list); `approvals-approve.js` (POST /:id/approve); `approvals-reject.js` (POST /:id/reject); `approvals-revoke.js` (POST /:id/revoke).
       - `suppressions.js` (GET list + POST bulk add + DELETE /:id).
       Each route file: `module.exports = async (req, res) => withChannelRouteContext(req, res, async ctx => { validate body via channelRouteSchemas; call lib/markos/channels/* function; return JSON })`.
       **All 19 files use legacy flat .js convention. NO `route.ts`. NO App Router. NO `app/api/*` paths.**
    3. Wire write APIs to approval-package via buildApprovalPackage (D-43):
       - POST /email/programs: after createEmailCampaign, run evaluateApprovalTriggers; if required AND approval_ref not provided -> auto-create approval-package via buildApprovalPackage (NOT createApprovalPackage which does not exist).
       - POST /email/programs-dispatch: assert campaign.status='approved'; else 422 reason_code='approval_required'.
       - POST /templates with pricing_bindings or content blocks containing pricing-shaped strings: run classifyTemplate (D-52 AST/allowlist); if blocks_dispatch -> 422 with findings; else proceed.
       - POST /suppressions bulk: require approval_ref; if missing for >100 suppression rows -> 422.
    4. Create `lib/markos/channels/mcp/tools.js` exporting 5 tool definitions (`.js` per D-46):
       - `send_email_program(input: { campaign_id })` — verifies approval state -> calls dispatch endpoint logic. Returns { run_id }.
       - `send_messaging(input: { profile_id, channel, content, sender_identity_id? })` — calls handleRecipient short-circuit; returns { dispatch_attempt_id, status }.
       - `get_thread(input: { thread_id, message_limit? = 20 })` — returns thread + last N messages.
       - `get_deliverability_posture(input: { sender_id?, channel? })` — returns current 24h posture.
       - `list_pending_approvals(input: { subject_type? })` — returns this-tenant pending approvals (queries via buildApprovalPackage-produced rows; NOT createApprovalPackage which does not exist).
       Each tool: tenant-scoped, no raw secrets in response, audit_log on every call.
    5. Update `lib/markos/mcp/tools/index.cjs` (per D-48 — `.cjs`, NOT `.ts`) to register channel tools alongside existing families. Use the same pattern as markos-marketing/markos-crm/markos-tenancy registration. Module exports are CommonJS (`module.exports = { tools }`).
    6. Create `contracts/F-130-channel-program-dispatch-write.yaml`:
       - flow_id: F-130
       - flow_name: channel-program-dispatch-write
       - description: "Write contract for channel program dispatch (MCP tools + dispatch endpoints). Approval gate via buildApprovalPackage (D-43 — NOT createApprovalPackage). Approval_ref required when class IN ('broadcast','revenue') OR audience_size >= 500."
       - paths: POST /v1/channels/email/programs/{id}/dispatch, POST /v1/channels/messaging/threads/{id}/send, POST /v1/channels/lifecycle/journeys/{id}/run-step.
       - approval_policy.high_risk_fields: [audience_snapshot_id, sender_identity_id, requires_approval]; precondition: approval_ref required when class IN ('broadcast','revenue') OR audience_size >= 500.
    7. Update `contracts/flow-registry.json` append-only with F-130 entry.
    8. Regenerate `contracts/openapi.json` (per D-47):
       - Run existing OpenAPI build script (`npm run openapi:build` per package.json scripts).
       - Verify 11 paths added: /v1/channels/email/programs, /v1/channels/email/programs/{id}, /v1/channels/email/programs/{id}/dispatch, /v1/channels/email/sends/{id}, /v1/channels/email/senders, /v1/channels/email/deliverability, /v1/channels/messaging/threads, /v1/channels/messaging/threads/{id}/escalate, /v1/channels/messaging/sends, /v1/channels/lifecycle/journeys, /v1/channels/lifecycle/journeys/{id}/pause, /v1/channels/lifecycle/journeys/{id}/resume, /v1/channels/templates, /v1/channels/templates/{id}/preview, /v1/channels/approvals, /v1/channels/approvals/{id}/approve, /v1/channels/approvals/{id}/reject, /v1/channels/approvals/{id}/revoke, /v1/channels/suppressions.
       - **D-47 compliance:** spec written to `contracts/openapi.json` (NOT `public/openapi.json`).
    9. Create test files (all `.test.js` per D-46):
       - `test/channels/api/email-programs.test.js` (Tests 1-4).
       - `test/channels/api/sends-route.test.js` (sends-by-id GET/POST/DELETE per D-30; idempotent re-send creates new dispatch_attempt_id; DELETE during dispatching emits AgentRun pause signal).
       - `test/channels/api/email-senders.test.js` (sender CRUD + verification).
       - `test/channels/api/email-deliverability.test.js` (Test 5).
       - `test/channels/api/messaging-threads.test.js` (Tests 6-8).
       - `test/channels/api/lifecycle-journeys.test.js` (Test 9).
       - `test/channels/api/templates.test.js` (Tests 10, 11).
       - `test/channels/api/approvals.test.js` (Tests 12, 13 — verifies buildApprovalPackage call signature).
       - `test/channels/api/suppressions.test.js` (Test 14).
       - `test/channels/api/tenant-isolation.test.js` (Test 15 — all 11 routes × cross-tenant denial).
       - `test/channels/mcp/tools.test.js` (Tests 16-22 — incl architecture-lock greps).
    10. Append VALIDATION rows.
  </action>
  <verify>
    <automated>npm test -- test/channels/api/ test/channels/mcp/ && node bin/validate-flow-contracts.cjs</automated>
  </verify>
  <acceptance_criteria>
    - 19 route files exist under `api/v1/channels/` as legacy flat `.js` files (NOT `.ts`, NOT `route.ts`); count via find
    - File `api/v1/channels/email/sends-by-id.js` exists and contains `req.method === 'GET'` AND `req.method === 'POST'` AND `req.method === 'DELETE'` (or equivalent legacy router exports for GET/POST/DELETE per D-30)
    - File `lib/markos/channels/api/handlers.js` exports `withChannelRouteContext` AND `channelRouteSchemas`
    - File `lib/markos/channels/api/handlers.js` imports `requireHostedSupabaseAuth` from `onboarding/backend/runtime-context.cjs` (D-42; grep)
    - File `lib/markos/channels/api/handlers.js` does NOT contain `requireSupabaseAuth(?!\w)` (D-42 enforcement; grep returns 0)
    - File `lib/markos/channels/mcp/tools.js` exports 5 tool definitions (grep `send_email_program` AND `send_messaging` AND `get_thread` AND `get_deliverability_posture` AND `list_pending_approvals`)
    - File `lib/markos/mcp/tools/index.cjs` (D-48 — `.cjs` NOT `.ts`) registers channel tools (grep `channelMcpTools` OR equivalent)
    - File `contracts/openapi.json` (D-47) contains paths starting with `/v1/channels/` (grep `"/v1/channels/email/programs"` AND `"/v1/channels/messaging/threads"` AND `"/v1/channels/templates"`)
    - File `public/openapi.json` is NOT modified by this plan (D-47 enforcement; spec lives at contracts/openapi.json)
    - File `contracts/F-130-channel-program-dispatch-write.yaml` exists; description references `buildApprovalPackage` (D-43 — NOT createApprovalPackage)
    - **D-43 enforcement:** `grep -c "createApprovalPackage" api/v1/channels/ lib/markos/channels/api/ lib/markos/channels/mcp/` returns 0
    - **D-43 positive:** `grep -c "buildApprovalPackage" api/v1/channels/ lib/markos/channels/api/ lib/markos/channels/mcp/` returns ≥3
    - `npm test -- test/channels/api/` exits 0 with ≥40 assertions (incl tenant isolation matrix)
    - `npm test -- test/channels/mcp/tools.test.js` exits 0 with ≥10 assertions
    - Test `test/channels/api/tenant-isolation.test.js` covers all 11 routes (count assertions ≥11)
    - `node bin/validate-flow-contracts.cjs` exits 0
    - All 11 test files end with `.test.js` (D-46)
    - **D-42 architecture-lock:** find under `api/v1/channels/` returns 0 files matching `route.ts`. find under `app/api/` returns 0 files matching `channels`.
  </acceptance_criteria>
  <done>11 D-30 routes operational across 19 legacy-flat .js files (D-42); MCP tool family registered at lib/markos/mcp/tools/index.cjs (D-48); OpenAPI regenerated at contracts/openapi.json (D-47); buildApprovalPackage exclusively used (D-43); tenant isolation proven across all routes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build 5 new UI components + outbound-workspace evolution + TimelineDetailView extension + 5 Storybook stories</name>
  <read_first>
    - components/markos/crm/outbound/outbound-workspace.tsx (current — preserve shell, evolve internals)
    - components/markos/crm/TimelineDetailView.tsx (P222 D-25 — extend with channel chips per D-34)
    - components/markos/crm/* (other CRM components for CSS module convention reference)
    - .storybook/main.* + .storybook/preview.* (storybook config)
    - .planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md (D-33, D-34)
    - .planning/phases/223-native-email-messaging-orchestration/223-RESEARCH.md (§UI Compatibility Note)
    - obsidian/reference/UI Components.md
    - lib/markos/channels/adapters/legacy-outbound.ts (Wave 1)
    - lib/markos/channels/programs/* (Waves 1+3)
    - lib/markos/channels/templates/preview.ts (Wave 3)
  </read_first>
  <behavior>
    - Test 1 (Storybook story renders ChannelProgramsList Empty): no campaigns -> shows empty-state with "Create your first email campaign" CTA.
    - Test 2 (Storybook ChannelProgramsList Populated): mix of statuses -> kanban with 10 columns matching D-02 status enum; cards show class chip + audience size + sender name + approval state; filter by class works.
    - Test 3 (Storybook ChannelProgramsList Tombstoned): legacy campaigns from outboundConsentRecords legacy that have been migrated -> shown with deprecated indicator.
    - Test 4 (Storybook MessagingThreadsList Reply Pending): list grouped by owner_user_id; threads with current_status='reply_pending' pinned to top with red dot indicator; channel chip per row.
    - Test 5 (Storybook DeliverabilityWorkspace Healthy + Warming + AtRisk): mix of senders shows reputation_status indicator + suggested next action ('warming: send <500/day for 7 days', 'at_risk: pause broadcast class').
    - Test 6 (Storybook TemplateEditor with classifier findings): pricing-touching template shows finding banner; pricing_bindings panel highlights non-approved value; preview pane shows rendered output OR error.
    - Test 7 (Storybook ApprovalReviewPanel): renders triggerReason chips (class_broadcast, count_threshold, etc), classifierFindings list, pricingRefs panel; approve/reject/revoke buttons gated by approval state.
    - Test 8 (TimelineDetailView with channel chips — D-34): timeline event row source_domain='email' shows chips for opened, clicked, replied; bounced shows red chip; chips link to dispatch_events row.
    - Test 9 (outbound-workspace evolution): existing outbound-workspace.tsx shell preserved; new ChannelProgramsList rendered inside via legacy-outbound adapter; legacy CRM outbound tests still green.
    - Test 10 (Morning Brief channel entries): MorningBriefChannelEntries renders top reply_pending threads (limited 5) by primary_owner_user_id + at_risk senders.
    - Test 11 (Approval Inbox channel entries): ApprovalInboxChannelEntries renders entry types email_campaign | lifecycle_journey | channel_template; clicking opens ApprovalReviewPanel.
  </behavior>
  <action>
    0. Pre-step (Blocker 5 + Warning 1 — script availability): ensure `package.json` scripts include `"storybook": "storybook dev -p 6006"` and `"build-storybook": "storybook build"` (verified at planner-time via package.json — both already exist). Then proceed.
    1. Create `components/markos/crm/outbound/ChannelProgramsList.tsx` + `.module.css`:
       - Kanban with 10 status columns from D-02.
       - Card shows: class chip (broadcast/lifecycle/transactional/revenue), audience snapshot size badge, sender_identity label, schedule_at, approval_ref state.
       - Filter controls: class, status, owner.
       - Loading/empty/error states.
    2. Create `components/markos/crm/outbound/MessagingThreadsList.tsx` + `.module.css`:
       - Group by owner_user_id; reply_pending pinned with red dot.
       - Per-row: channel chip (icon per channel), profile name from related_crm_id, last_message_at, sentiment chip.
       - Click row -> opens thread detail (uses GET /v1/channels/messaging/threads/{id}).
    3. Create `components/markos/crm/outbound/DeliverabilityWorkspace.tsx` + `.module.css`:
       - Per-sender card showing reputation_status (color-coded: warming=blue, healthy=green, watch=amber, at_risk=red), 24h posture metrics (bounce_rate, complaint_rate, reply_rate, sample_size), suppression_count, suggested next-action text.
       - Reads from GET /v1/channels/email/deliverability.
    4. Create `components/markos/crm/outbound/TemplateEditor.tsx` + `.module.css`:
       - Per-channel preview pane (calls /v1/channels/templates/{id}/preview).
       - Variable inspector (lists template.variables_schema with current sample values).
       - Bindings panels: evidence_bindings + pricing_bindings (with placeholder badge for `{{MARKOS_PRICING_ENGINE_PENDING}}`).
       - Classifier findings overlay (pricing_binding/pricing_text_scan/pricing_ast_violation/evidence_ttl/competitor_mention per D-52).
       - Locale variant chain navigator.
    5. Create `components/markos/crm/outbound/ApprovalReviewPanel.tsx` + `.module.css`:
       - Renders triggerReason chips.
       - classifierFindings list with severity chips.
       - pricingRefs + evidenceRefs panels.
       - approve/reject/revoke buttons calling /v1/channels/approvals/{id}/{action}.
    6. Modify `components/markos/crm/outbound/outbound-workspace.tsx` (D-33 evolution):
       - Preserve outer shell + existing CRM-bound entry points.
       - Add tab bar: "Programs | Threads | Deliverability | Templates | Approvals".
       - Each tab renders the corresponding new component.
       - Legacy sections (existing CRM sequences/templates) remain mounted via legacy-outbound adapter and tagged "Legacy (deprecating)".
    7. Modify `components/markos/crm/TimelineDetailView.tsx` (P222 D-25 + D-34):
       - For each timeline event row where source_domain IN ('email','messaging','push','in_app'): render channel-event-chips section with icons for opened/clicked/replied/bounced/complained/unsubscribed.
       - Chips read from dispatch_events keyed on source_event_ref.
       - Click chip -> opens lightweight detail pop showing dispatch_attempt_id + provider_message_id.
    8. Create `components/markos/operator/MorningBriefChannelEntries.tsx`:
       - Reads top 5 reply_pending threads for primary_owner_user_id (current operator) via /v1/channels/messaging/threads?owner_user_id=ME&status=reply_pending&limit=5.
       - Reads at_risk senders via /v1/channels/email/deliverability filter reputation_status='at_risk'.
       - Renders alongside existing P208 Morning Brief sections.
    9. Create `components/markos/operator/ApprovalInboxChannelEntries.tsx`:
       - Renders entry types email_campaign | lifecycle_journey | channel_template in P208 Approval Inbox.
       - On click -> opens ApprovalReviewPanel with entry context.
    10. Create 5 Storybook stories under `.storybook/stories/`:
        - `ChannelProgramsList.stories.tsx` (Empty + Populated + Tombstoned).
        - `MessagingThreadsList.stories.tsx` (Empty + ReplyPending + Mixed).
        - `DeliverabilityWorkspace.stories.tsx` (Warming + Healthy + Watch + AtRisk).
        - `TemplateEditor.stories.tsx` (Empty + Preview + ClassifierFindings + PricingFlagged).
        - `ApprovalReviewPanel.stories.tsx` (Pending + Approved + Rejected + Revoked).
        Each story uses fixtures from test/fixtures/channels/* (Wave 1 — `.js` per D-46).
    11. Append VALIDATION rows.
  </action>
  <verify>
    <automated>npm run build-storybook && npx chromatic --project-token=${CHROMATIC_PROJECT_TOKEN:-dummy} --exit-zero-on-changes --only-changed && npm test</automated>
  </verify>
  <acceptance_criteria>
    - 5 component files exist under `components/markos/crm/outbound/` with matching `.module.css` files
    - File `components/markos/crm/outbound/outbound-workspace.tsx` contains `<ChannelProgramsList` import + render (grep)
    - File `components/markos/crm/TimelineDetailView.tsx` contains `channel-event-chips` className OR `ChannelEventChips` component reference (grep)
    - 5 Storybook story files exist under `.storybook/stories/`
    - File `components/markos/operator/MorningBriefChannelEntries.tsx` exists; references `/v1/channels/messaging/threads?owner_user_id`
    - File `components/markos/operator/ApprovalInboxChannelEntries.tsx` exists; references entry_type values
    - `npm test` (legacy P102/P103/P105 component regression — outbound-workspace shell still renders) exits 0
    - File `package.json` contains `"build-storybook"` script (verified pre-existing at planner time)
    - Storybook builds without error (run `npm run build-storybook`); 5 component story files × 4 variants each = 20 snapshots produced for Chromatic
    - `npx chromatic --exit-zero-on-changes --only-changed` exits 0 (gate captures unapproved diffs without failing CI on first run)
  </acceptance_criteria>
  <done>5 new components + outbound-workspace evolution + TimelineDetailView extension + Morning Brief + Approval Inbox channel entries + 5 Storybook stories shipped; legacy CRM shell preserved per D-33.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet -> /v1/channels/* (legacy api/*.js per D-42) | Bearer auth via requireHostedSupabaseAuth + tenant-scoped resolution; cross-tenant denied at every route |
| MCP client -> markos-channel tools (registered at index.cjs per D-48) | OAuth-scoped (existing MCP auth); tenant-scoped; no raw secrets in response |
| operator UI -> API | Same-origin + bearer; legacy outbound shell preserved via legacy-outbound adapter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-223-05-01 | Information Disclosure | tenant_A reads tenant_B campaign | mitigate | RLS on email_campaigns + 11-route tenant isolation matrix tested in tenant-isolation.test.js |
| T-223-05-02 | Tampering | bypass approval-package via direct dispatch endpoint | mitigate | dispatch route asserts campaign.status='approved'; revocation path catches in-flight via AgentRun pause; buildApprovalPackage (D-43) is sole approval factory |
| T-223-05-03 | Spoofing | rogue MCP tool call for another tenant | mitigate | MCP tools call withChannelRouteContext same as routes; audit_log on every invocation |
| T-223-05-04 | Information Disclosure | reputation_score leaks competitor data | accept | reputation is intra-tenant; no inter-tenant comparison |
| T-223-05-05 | Tampering | OpenAPI spec drift (hand-edited) | mitigate | regen via build script; CI gate validates contracts/openapi.json (D-47) matches contracts/F-*.yaml |
| T-223-05-06 | Information Disclosure | UI exposes raw secrets in HTML | mitigate | components only consume sanitized API responses; D-39 verified by tooling regex scan |
| T-223-05-07 | Spoofing | route uses fictional auth wrapper (requireSupabaseAuth) | mitigate | D-42 architecture-lock test (Plan 01 Task 0.5) catches any plan/code regression; Plan 05 wires requireHostedSupabaseAuth exclusively |
</threat_model>

<verification>
- 19 API route files exist as legacy flat `.js` (D-42); all 11 D-30 paths reachable via Node `--test` API tests.
- 5 MCP tools registered at `lib/markos/mcp/tools/index.cjs` (D-48) + tenant-scoped + no raw secrets.
- Tenant-isolation matrix proven across all routes.
- 5 new components + 5 Storybook stories shipped.
- outbound-workspace + TimelineDetailView evolved without breaking legacy.
- Morning Brief + Approval Inbox gain channel entry types.
- OpenAPI regenerated at `contracts/openapi.json` (D-47).
- VALIDATION rows added.
- buildApprovalPackage exclusive (D-43); architecture-lock D-42/D-46/D-47/D-48 enforcement verified.
</verification>

<success_criteria>
- 19 API routes (legacy flat .js per D-42) + MCP tool family at index.cjs (D-48) operational.
- 5 UI components + 5 Storybook stories rendered.
- All approval-aware writes route through buildApprovalPackage (D-43).
- Tenant isolation tested on every endpoint.
- OpenAPI updated at contracts/openapi.json (D-47).
- Legacy CRM outbound regression green via npm test.
- All test files use `.test.js` per D-46.
</success_criteria>

<output>
After completion, create `.planning/phases/223-native-email-messaging-orchestration/223-05-SUMMARY.md` capturing:
- 11 D-30 paths delivered across 19 legacy-flat api/v1/channels/*.js files (D-42) + 5 MCP tools at lib/markos/mcp/tools/index.cjs (D-48)
- Tenant isolation proven on all routes
- 5 UI components + outbound-workspace evolved + TimelineDetailView extended (D-34)
- Morning Brief + Approval Inbox channel entries (D-35)
- OpenAPI regenerated at contracts/openapi.json (D-47)
- F-130 contract published; description references buildApprovalPackage (D-43)
- Architecture-lock D-42/D-46/D-47/D-48 holds — no createApprovalPackage / requireSupabaseAuth / route.ts / vitest references
- Hand-off to 223-06: full surface ready; Wave 5 closeout (RLS suite, crons, spike alerts, Playwright via existing axe-playwright, Chromatic, SUMMARY/STATE/ROADMAP)
</output>
