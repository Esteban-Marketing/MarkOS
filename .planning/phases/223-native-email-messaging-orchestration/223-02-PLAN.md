---
phase: 223
plan: "02"
type: execute
wave: 2
depends_on: ["223-01"]
files_modified:
  - supabase/migrations/117_channel_dispatch_tracking.sql
  - supabase/migrations/118_channel_in_app_messages.sql
  - supabase/migrations/119_consent_cutover_view_swap.sql
  - supabase/migrations/120_channel_indexes_rls_hardening.sql
  - lib/markos/outbound/consent.ts
  - lib/markos/channels/gate/eligibility-gate.ts
  - lib/markos/channels/gate/consent-check.ts
  - lib/markos/channels/gate/suppression-check.ts
  - lib/markos/channels/gate/frequency-cap.ts
  - lib/markos/channels/gate/quiet-hours.ts
  - lib/markos/channels/gate/jurisdiction-check.ts
  - lib/markos/channels/dispatch/dispatch-coordinator.ts
  - lib/markos/channels/dispatch/fan-out.ts
  - lib/markos/channels/dispatch/recipient-handler.ts
  - lib/markos/channels/dispatch/idempotency.ts
  - lib/markos/channels/suppression/suppression.ts
  - lib/markos/channels/suppression/bounce-handler.ts
  - .planning/config.json
  - contracts/F-128-channel-suppression-v1.yaml
  - contracts/F-129-channel-dispatch-event-v1.yaml
  - contracts/F-130-channel-program-dispatch-write.yaml
  - contracts/flow-registry.json
  - test/channels/schema/dispatch-tracking.test.ts
  - test/channels/schema/in-app-messages.test.ts
  - test/channels/schema/consent-view-swap.test.ts
  - test/channels/consent/eligibility-cutover.test.ts
  - test/channels/gate/consent-check.test.ts
  - test/channels/gate/suppression-check.test.ts
  - test/channels/gate/frequency-cap.test.ts
  - test/channels/gate/quiet-hours.test.ts
  - test/channels/gate/jurisdiction-check.test.ts
  - test/channels/gate/eligibility-gate.test.ts
  - test/channels/dispatch/dispatch-coordinator.test.ts
  - test/channels/dispatch/recipient-handler.test.ts
  - test/channels/dispatch/idempotency.test.ts
  - test/channels/suppression/bounce-handler.test.ts
  - test/crm-outbound/*.test.js (Warning 3: A12 migration if these files INSERT outboundConsentRecords directly; glob expanded at execute time)
  - .planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md
autonomous: true
requirements:
  - EML-05
  - MSG-02
  - CDP-05
  - QA-02
must_haves:
  truths:
    - lib/markos/outbound/consent.ts::evaluateOutboundEligibility reads cdp_consent_states ONLY via P221 adapter getConsentState; legacy outboundConsentRecords path retired (D-11)
    - outbound_consent_records is a derived VIEW backed by cdp_consent_states; direct INSERT/UPDATE on outbound_consent_records_legacy raises a trigger exception (D-12 single writer rule)
    - Per-recipient eligibility gate runs 5 layers in fixed order (consent -> suppression -> frequency_cap -> quiet_hours -> jurisdiction) with first-fail short-circuit and dispatch_skips audit row on every block (D-19 + D-20)
    - dispatch_attempt_id idempotency: re-invoking the recipient handler with the same dispatch_attempt_id is a no-op (no second send, no duplicate dispatch_events row) — Pitfall 1 mitigation
    - AgentRun bridge (D-15) writes to markos_agent_runs when workflow.agentrun_v2_available=true; falls back to markos_audit_log writes when flag=false; both paths return a DispatchRun handle
    - Vercel Queues fan-out used when workflow.vercel_queues_available=true; pg_boss on Supabase Postgres used as fallback when flag=false
    - Bounce handler propagates: hard_bounce -> channel_suppressions row + cdp_events 'channel.suppression_added' emit; complaint -> same path with reason='complaint'; soft_bounce -> 3 retries over 7d then promotes to hard
    - Tombstone propagation hook: P221 D-24 tombstone -> P222 D-32 -> channel_suppressions row added with reason='dsr_deletion' (link verified at integration test)
    - F-128, F-129, F-130 contracts published; flow-registry.json updated
    - All P104 evaluateOutboundEligibility tests still green after consent.ts cutover (D-11 backward compatibility mandate; A12 fixture mocking handler)
  artifacts:
    - path: supabase/migrations/117_channel_dispatch_tracking.sql
      provides: dispatch_events + dispatch_skips tables + RLS + UNIQUE (dispatch_attempt_id, event_type) + dispatch_events_freq_cap_idx composite
    - path: supabase/migrations/118_channel_in_app_messages.sql
      provides: in_app_messages table + RLS + FK to messaging_threads
    - path: supabase/migrations/119_consent_cutover_view_swap.sql
      provides: rename outbound_consent_records to outbound_consent_records_legacy + CREATE VIEW outbound_consent_records SELECT FROM cdp_consent_states + trigger blocking direct legacy writes
    - path: supabase/migrations/120_channel_indexes_rls_hardening.sql
      provides: cross-table FK indexes + audit_log event-type registrations for all 11 new tables (RLS policy review pass)
    - path: lib/markos/outbound/consent.ts
      provides: evaluateOutboundEligibility refactored to call getConsentState via P221 adapter; preserves return shape (allowed, requires_approval, reason_code, contact, consent, contact_point) for backward compat
    - path: lib/markos/channels/gate/eligibility-gate.ts
      provides: evaluateDispatchEligibility(ctx) -> EligibilityResult; orchestrates 5-layer gate; first-fail short-circuit; writes dispatch_skips on block
    - path: lib/markos/channels/gate/consent-check.ts
      provides: Layer 1 — calls getConsentState(P221); honors legal_basis policy
    - path: lib/markos/channels/gate/suppression-check.ts
      provides: Layer 2 — channel_suppressions lookup
    - path: lib/markos/channels/gate/frequency-cap.ts
      provides: Layer 3 — rolling-window count via dispatch_events with per-tenant + per-campaign override
    - path: lib/markos/channels/gate/quiet-hours.ts
      provides: Layer 4 — TraitSnapshot time_zone + ConsentState quiet_hours window; returns DEFER (re-enqueue with delay) not SKIP when in window
    - path: lib/markos/channels/gate/jurisdiction-check.ts
      provides: Layer 5 — tenant config (jurisdiction, channel, marketing_legal_basis) matrix
    - path: lib/markos/channels/dispatch/dispatch-coordinator.ts
      provides: createDispatchRun + AgentRun bridge (D-15) using config flag workflow.agentrun_v2_available
    - path: lib/markos/channels/dispatch/fan-out.ts
      provides: enqueueRecipients + Vercel Queues primary path + pg_boss fallback (workflow.vercel_queues_available flag)
    - path: lib/markos/channels/dispatch/recipient-handler.ts
      provides: per-recipient handler — dispatch_attempt_id dedup check at top -> evaluateDispatchEligibility -> adapter.send -> dispatch_events insert (transactional)
    - path: lib/markos/channels/dispatch/idempotency.ts
      provides: deriveDispatchAttemptId + checkExistingDispatchEvent (Pitfall 1 mitigation)
    - path: lib/markos/channels/suppression/suppression.ts
      provides: addSuppression + listSuppressions + removeSuppression (operator override with audit row)
    - path: lib/markos/channels/suppression/bounce-handler.ts
      provides: handleBounceEvent (hard/soft) + handleComplaintEvent + soft-bounce retry tracker (3 retries / 7 days)
    - path: contracts/F-128-channel-suppression-v1.yaml
      provides: F-128 contract — channel_suppressions CRUD + bulk add + DSR-sourced blocks
    - path: contracts/F-129-channel-dispatch-event-v1.yaml
      provides: F-129 contract — dispatch_events + dispatch_skips read surface
    - path: contracts/F-130-channel-program-dispatch-write.yaml
      provides: F-130 contract — campaign/thread/journey send trigger + approval gate + fan-out write API
  key_links:
    - from: lib/markos/outbound/consent.ts
      to: lib/markos/cdp/adapters/crm-projection.ts
      via: getConsentState(tenantId, profileId, channel) replaces direct outboundConsentRecords reads
    - from: lib/markos/channels/dispatch/recipient-handler.ts
      to: lib/markos/channels/gate/eligibility-gate.ts
      via: gate runs at top of every per-recipient job; on SKIP -> dispatch_skips row + return; on DEFER -> re-enqueue with delay; on ALLOW -> proceed to adapter.send
    - from: supabase/migrations/119_consent_cutover_view_swap.sql
      to: supabase/migrations/102a_cdp_consent_states.sql (P221)
      via: CREATE VIEW outbound_consent_records SELECT FROM cdp_consent_states (P221 SOR)
    - from: lib/markos/channels/dispatch/idempotency.ts
      to: supabase/migrations/117_channel_dispatch_tracking.sql
      via: dispatch_events UNIQUE (dispatch_attempt_id, event_type) constraint enforces idempotency
    - from: lib/markos/channels/suppression/bounce-handler.ts
      to: lib/markos/cdp/events/* (P221) for emitCdpEvent('channel.suppression_added')
      via: tombstone + bounce + complaint propagation chain
---

<objective>
Wave 2 dispatch infrastructure + ConsentState cutover. Lands migrations 117-120 (dispatch tracking, in-app messages, consent view swap, RLS hardening). Refactors `lib/markos/outbound/consent.ts::evaluateOutboundEligibility` to call P221 `getConsentState` adapter (no behavior change for callers; D-11 mandate). Builds the 5-layer per-recipient eligibility gate (D-19 + D-20). Builds the AgentRun bridge + Vercel Queues fan-out + dispatch_attempt_id idempotency layer (D-14, D-15, Pitfall 1 mitigation). Builds the bounce/complaint suppression propagation. Publishes F-128, F-129, F-130 contracts.

Purpose: satisfies EML-05 (ConsentState cutover; outboundConsentRecords -> derived view), MSG-02 (per-recipient 5-layer dispatch gate), CDP-05 (privacy/retention/consent enforced — tombstone + jurisdiction + DSR cascade), QA-02 (RLS on dispatch tracking + view-swap trigger).

Output: 4 migration files, 1 refactored library file (consent.ts), 13 new library files (gate, dispatch, suppression), 3 contract files, flow-registry update, 14 test files covering view-swap + 5 gate layers + idempotency + bounce/complaint propagation, plus full P104 regression suite.

**Dependency note:** This plan ASSUMES P221 has executed (cdp_consent_states + cdp_identity_profiles + getConsentState adapter). If 223-01 task 1 emitted A3 BLOCKER, this plan must HALT at task 1 with explicit error. Execute-phase MUST gate plan 223-02 on P221 readiness.
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md
@.planning/phases/223-native-email-messaging-orchestration/223-RESEARCH.md
@.planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md
@.planning/phases/223-native-email-messaging-orchestration/223-01-SUMMARY.md
@.planning/phases/221-cdp-identity-audience-consent-substrate/221-CONTEXT.md
@.planning/phases/221-cdp-identity-audience-consent-substrate/221-RESEARCH.md
@.planning/phases/104-native-outbound-execution/104-CONTEXT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@obsidian/reference/Database Schema.md
@obsidian/reference/Core Lib.md
@obsidian/work/incoming/19-EMAIL-ENGINE.md
# informational only — decisions already locked in 223-CONTEXT.md per CLAUDE.md §source-of-truth precedence
@obsidian/work/incoming/21-MESSAGING-ENGINE.md
# informational only — decisions already locked in 223-CONTEXT.md per CLAUDE.md §source-of-truth precedence
@lib/markos/outbound/consent.ts
@lib/markos/outbound/conversations.ts
@supabase/migrations/116_channel_suppressions.sql
@contracts/flow-registry.json
@.planning/config.json
</context>

<interfaces>
<!-- P221 adapter contract (REQUIRED — A3 fail-fast if missing) -->
```typescript
// lib/markos/cdp/adapters/crm-projection.ts (P221)
export async function getConsentState(
  tenantId: string,
  profileId: string,
  channel: 'email' | 'sms' | 'whatsapp' | 'push' | 'in_app'
): Promise<{
  consent_id: string;
  channel_status: 'opted_in' | 'opted_out' | 'unknown';
  legal_basis: 'consent' | 'legitimate_interest' | 'transactional';
  quiet_hours: { start: string; end: string; timezone: string } | null;
  preference_tags: string[];
  jurisdiction: string;
  updated_at: string;
}>;

export async function setConsentState(
  tenantId: string,
  profileId: string,
  channel: string,
  update: { channel_status?: string; legal_basis?: string; reason?: string; source_event_ref?: string }
): Promise<void>;

export async function getTraitSnapshot(
  tenantId: string,
  profileId: string
): Promise<{ time_zone: string; preferred_locale: string; [key: string]: unknown }>;
```

<!-- Existing consent.ts buildEligibilityResult shape (MUST be preserved) -->
```typescript
// lib/markos/outbound/consent.ts current return shape — D-11 mandate: do NOT change
type EligibilityResult = Readonly<{
  allowed: boolean;
  requires_approval: boolean;
  reason_code: string;
  contact: ContactRecord | null;
  consent: ConsentRecord | null;
  contact_point: string;
}>;
```

<!-- Target gate result shape (NEW for 5-layer gate) -->
```typescript
// lib/markos/channels/gate/eligibility-gate.ts
type DispatchEligibilityResult = {
  decision: 'allow' | 'skip' | 'defer';
  reason_code?: 'consent' | 'suppression' | 'frequency_cap' | 'quiet_hours' | 'jurisdiction' | 'missing_variable' | 'pricing_unresolved';
  evidence_ref?: string;
  next_send_window?: string; // only when decision='defer'
};
```

<!-- Config flags (D-15 + Vercel Queues fallback) -->
```json
// .planning/config.json
{
  "workflow": {
    "agentrun_v2_available": false,
    "vercel_queues_available": true,
    "outbound_consent_legacy_view": true
  }
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrations 117-120 (dispatch_events, dispatch_skips, in_app_messages, consent view swap, RLS hardening) + view-swap trigger semantics</name>
  <read_first>
    - supabase/migrations/116_channel_suppressions.sql (Wave 1 output — pattern reference)
    - supabase/migrations/82_markos_audit_log_hash_chain.sql (audit pattern)
    - .planning/phases/221-cdp-identity-audience-consent-substrate/221-RESEARCH.md (cdp_consent_states schema — view source)
    - .planning/phases/223-native-email-messaging-orchestration/223-RESEARCH.md (§Schema Sketches Migrations 117-119, §Pitfalls 1+2+3)
    - .planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md (D-11, D-12, D-19, D-20, D-36, D-37)
    - test/fixtures/channels/dispatch.ts (Wave 1 output)
    - test/fixtures/channels/suppression.ts (Wave 1 output)
  </read_first>
  <behavior>
    - Test 1: dispatch_events UNIQUE (dispatch_attempt_id, event_type) — duplicate insert raises 23505. RLS tenant_isolation enforced.
    - Test 2: dispatch_events_freq_cap_idx exists and is used by EXPLAIN ANALYZE on a representative count query (rolling-window optimization).
    - Test 3: dispatch_skips RLS tenant_isolation enforced; gate_failed CHECK constraint rejects values outside the documented enum.
    - Test 4: in_app_messages FK to messaging_threads(thread_id); RLS tenant_isolation enforced.
    - Test 5: AFTER migration 119 — direct INSERT INTO outbound_consent_records_legacy raises exception "Direct writes to outbound_consent_records are blocked. Use setConsentState (P221).". UPDATE blocked too.
    - Test 6: AFTER migration 119 — SELECT * FROM outbound_consent_records (now a view) returns rows derived from cdp_consent_states with correct column mapping (channel email_marketing='opted_in' -> status='subscribed', email_marketing='opted_out' -> status='opted_out', else 'pending').
    - Test 7: AFTER migration 119 — outbound_consent_records_legacy table still exists (data preserved) but is read-only from app; deprecation window flag `workflow.outbound_consent_legacy_view = true` honored.
    - Test 8: migration 120 — confirms RLS policies present on all 11 new tables (sender_identities, deliverability_posture, channel_templates, email_campaigns, messaging_threads, lifecycle_journeys, lifecycle_journey_runs, channel_suppressions, dispatch_events, dispatch_skips, in_app_messages); audit_log event-type entries registered (channel_dispatch_start, channel_dispatch_complete, channel_consent_change, channel_suppression_added, channel_template_approved).
    - Test 9: idempotent re-run for migrations 117-120.
  </behavior>
  <action>
    1. Create `supabase/migrations/117_channel_dispatch_tracking.sql` per RESEARCH §Migration 117:
       - dispatch_events table (composite UNIQUE (dispatch_attempt_id, event_type), event_type CHECK constraint covering all 10 event types per RESEARCH listing).
       - dispatch_skips table (gate_failed CHECK constraint with all 8 reason codes per D-19/D-20 + missing_variable + pricing_unresolved + approval_revoked).
       - dispatch_events_freq_cap_idx partial index ON (tenant_id, profile_id, channel, occurred_at) WHERE event_type IN ('delivered','queued').
       - RLS tenant_isolation on both tables.
    2. Create `supabase/migrations/118_channel_in_app_messages.sql` per RESEARCH §Migration 118:
       - in_app_messages table with FK to messaging_threads(thread_id), JSONB content {title, body, cta_url, cta_label, image_url}.
       - RLS tenant_isolation policy.
    3. Create `supabase/migrations/119_consent_cutover_view_swap.sql` per RESEARCH §Migration 119:
       - DO block: only proceed if cdp_consent_states table exists (A3 graceful) — if absent, RAISE NOTICE 'P221 not yet executed; migration 119 NO-OP' and return without renaming.
       - ALTER TABLE outbound_consent_records RENAME TO outbound_consent_records_legacy.
       - CREATE VIEW outbound_consent_records AS SELECT (mapping per RESEARCH SQL) FROM cdp_consent_states WHERE channel IN ('email','sms','whatsapp').
       - CREATE FUNCTION block_direct_consent_writes() RAISE EXCEPTION ... ; CREATE TRIGGER block_outbound_consent_insert BEFORE INSERT OR UPDATE ON outbound_consent_records_legacy FOR EACH ROW EXECUTE FUNCTION block_direct_consent_writes().
       - Insert config row `.planning/config.json` workflow.outbound_consent_legacy_view = true (3-month deprecation flag).
       - Note in migration comment: "Drift audit cron from P221 D-26 covers any divergence during deprecation window."
    4. Create `supabase/migrations/120_channel_indexes_rls_hardening.sql` per RESEARCH §Migration 120:
       - Cross-table FK indexes (e.g., email_campaigns.template_id, email_campaigns.sender_identity_id, messaging_threads.related_crm_id).
       - INSERT INTO markos_audit_log_event_types (or equivalent) the 5 new event-type registrations: 'channel_dispatch_start', 'channel_dispatch_complete', 'channel_consent_change', 'channel_suppression_added', 'channel_template_approved'.
       - RLS policy review pass: confirm tenant_isolation on all 11 new tables; if any missing, ALTER TABLE ... ENABLE ROW LEVEL SECURITY + CREATE POLICY tenant_isolation.
    5. Create `test/channels/schema/dispatch-tracking.test.ts` (Tests 1, 2, 3, 9 above).
    6. Create `test/channels/schema/in-app-messages.test.ts` (Tests 4, 9).
    7. Create `test/channels/schema/consent-view-swap.test.ts` (Tests 5, 6, 7) — must mock cdp_consent_states existence for sandbox runs.
    8. Append per-task rows to 223-VALIDATION.md.
  </action>
  <verify>
    <automated>vitest run test/channels/schema/dispatch-tracking.test.ts test/channels/schema/in-app-messages.test.ts test/channels/schema/consent-view-swap.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/117_channel_dispatch_tracking.sql` contains `CREATE TABLE dispatch_events` AND `CREATE TABLE dispatch_skips` AND `UNIQUE (dispatch_attempt_id, event_type)` AND `CREATE INDEX dispatch_events_freq_cap_idx`
    - File `supabase/migrations/119_consent_cutover_view_swap.sql` contains `ALTER TABLE outbound_consent_records RENAME TO outbound_consent_records_legacy` AND `CREATE VIEW outbound_consent_records` AND `CREATE TRIGGER block_outbound_consent_insert` AND a DO block guarding cdp_consent_states existence (grep "cdp_consent_states")
    - File `.planning/config.json` contains `"outbound_consent_legacy_view": true` AND `"agentrun_v2_available"` AND `"vercel_queues_available"`
    - `vitest run test/channels/schema/` exits 0
    - Direct INSERT to outbound_consent_records_legacy fails with substring "Direct writes to outbound_consent_records are blocked"
  </acceptance_criteria>
  <done>4 migrations land dispatch tracking + view swap + RLS hardening; A3 graceful path for view-swap when P221 not yet executed; consent view + trigger validated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor consent.ts to call getConsentState adapter (D-11 cutover) + build 5-layer per-recipient gate (D-19) + dispatch_skips audit on every block</name>
  <read_first>
    - lib/markos/outbound/consent.ts (current evaluateOutboundEligibility — 200+ lines; preserve buildEligibilityResult shape)
    - lib/markos/cdp/adapters/crm-projection.ts (P221 adapter — A3 fail-fast on missing) — getConsentState/setConsentState/getTraitSnapshot signatures
    - .planning/phases/223-native-email-messaging-orchestration/223-RESEARCH.md (§Architecture Patterns Pattern 1, §Pitfalls 5)
    - .planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md (D-11, D-13, D-19, D-20)
    - .planning/phases/104-native-outbound-execution/104-CONTEXT.md (D-04 channel-specific consent fail-closed)
    - test/fixtures/channels/dispatch.ts + test/fixtures/channels/suppression.ts
    - existing test files under test/outbound-* or whichever pattern P104 used (regression fixtures)
    - **P104 regression A12 pattern (Warning 3 fix):** read `test/crm-outbound/*.test.js` (and `test/crm-outbound/*.test.ts` if present) — these are the actual P104 outbound test files. If any of those files INSERT directly into `outboundConsentRecords` table, they MUST be migrated to the A12 adapter-injection pattern (mock `getConsentState` instead of seeding the legacy table). Add migrated tests to `files_modified` for this plan if changes are required. If no direct INSERT pattern found, document that finding in this task's SUMMARY.
  </read_first>
  <behavior>
    - Test 1 (D-11 cutover regression): existing P104 evaluateOutboundEligibility test vectors pass against the new ConsentState backend (A12 mocking — tests inject getConsentState mock instead of populating outboundConsentRecords). Same `allowed` + `reason_code` outputs.
    - Test 2 (Layer 1 consent): channel_status='opted_in' -> ALLOW; 'opted_out' -> SKIP reason='consent'; 'unknown' AND legal_basis_policy='legitimate_interest' -> ALLOW; 'unknown' AND policy='strict' (consent required) -> SKIP reason='consent'.
    - Test 3 (Layer 2 suppression): channel_suppressions row exists -> SKIP reason='suppression' with evidence_ref=suppression_id; no row -> pass to Layer 3.
    - Test 4 (Layer 3 frequency_cap): campaign_type='transactional' -> ALWAYS ALLOW (never capped); campaign_type='lifecycle'|'broadcast'|'revenue' AND count<2 in 7d -> ALLOW; count>=2 -> SKIP reason='frequency_cap'.
    - Test 5 (Layer 4 quiet_hours): TraitSnapshot.time_zone='America/New_York' AND now() falls within ConsentState.quiet_hours window -> DEFER reason='quiet_hours' with next_send_window=next day 09:00 in recipient TZ; outside window -> pass to Layer 5.
    - Test 6 (Layer 5 jurisdiction): tenant config matrix entry (jurisdiction='EU', channel='email', marketing_legal_basis='consent') AND consent.legal_basis='legitimate_interest' -> SKIP reason='jurisdiction'; (jurisdiction='US', channel='email') -> ALLOW per default; transactional always passes regardless of jurisdiction.
    - Test 7 (first-fail short-circuit): blocked at Layer 2 -> Layers 3, 4, 5 NOT executed (verified by mock spy counts).
    - Test 8 (dispatch_skips audit): every SKIP/DEFER decision writes a dispatch_skips row with gate_failed + evidence_ref + attempted_at populated.
    - Test 9 (consent.ts return shape preservation): all existing P104 callers continue to receive { allowed, requires_approval, reason_code, contact, consent, contact_point } unchanged (legacy regression).
  </behavior>
  <action>
    1. Refactor `lib/markos/outbound/consent.ts::evaluateOutboundEligibility`:
       - REMOVE direct reads of `outboundConsentRecords` from store.
       - ADD: const consent = await getConsentState(tenantId, profileId, channel) via P221 adapter (require()'d at top of file).
       - Map ConsentState fields to legacy ConsentRecord shape (status, lawful_basis, verified_at) so buildEligibilityResult return signature is unchanged.
       - PRESERVE all existing reason_code values (CONSENT_REQUIRED, CONTACT_NOT_FOUND, CONTACT_POINT_MISSING, etc).
       - Add TODO comment: "Phase 223 D-11: cutover from outboundConsentRecords to cdp_consent_states via P221 adapter. Legacy view kept for 3-month deprecation; trigger blocks legacy direct writes."
       - Add A12 fixture-injection pattern: accept optional `dependencies = { getConsentState }` arg so tests can inject mocks without monkeypatching.
    2. Create `lib/markos/channels/gate/consent-check.ts` exporting `checkConsent({ tenantId, profileId, channel, legalBasisPolicy })` returning `{ decision, reason_code?, evidence_ref? }`. Calls getConsentState; mapping per Test 2 above.
    3. Create `lib/markos/channels/gate/suppression-check.ts` exporting `checkSuppression({ tenantId, profileId, channel })`. Queries channel_suppressions; returns SKIP if row found.
    4. Create `lib/markos/channels/gate/frequency-cap.ts` exporting `checkFrequencyCap({ tenantId, profileId, channel, campaignType })`. Per RESEARCH §Frequency Cap Implementation:
       - Transactional -> always ALLOW.
       - Marketing -> SELECT COUNT(*) FROM dispatch_events WHERE tenant_id=$1 AND profile_id=$2 AND channel=$3 AND event_type IN ('delivered','queued') AND occurred_at >= NOW() - INTERVAL '$4 days'. Default policy: 2 sends / 7 days. Per-tenant override via tenants table or per-campaign override via campaign.frequency_cap_override.
    5. Create `lib/markos/channels/gate/quiet-hours.ts` exporting `checkQuietHours({ tenantId, profileId })`. Calls getTraitSnapshot for time_zone + getConsentState for quiet_hours; if now() in window -> DEFER with next_send_window calculated using tz; else PASS.
    6. Create `lib/markos/channels/gate/jurisdiction-check.ts` exporting `checkJurisdiction({ tenantId, profileId, channel, legalBasis })`. Reads tenant config matrix (initially shipped as JSON in lib/markos/channels/gate/jurisdiction-matrix.json with entries for EU/US/UK/CA × email/sms/whatsapp/push × marketing/transactional). Returns SKIP if mismatch.
    7. Create `lib/markos/channels/gate/eligibility-gate.ts` exporting `evaluateDispatchEligibility(ctx)`:
       ```typescript
       async function evaluateDispatchEligibility(ctx) {
         const layer1 = await checkConsent(ctx); if (layer1.decision !== 'allow') return await recordSkip(ctx, layer1);
         const layer2 = await checkSuppression(ctx); if (layer2.decision !== 'allow') return await recordSkip(ctx, layer2);
         const layer3 = await checkFrequencyCap(ctx); if (layer3.decision !== 'allow') return await recordSkip(ctx, layer3);
         const layer4 = await checkQuietHours(ctx); if (layer4.decision === 'defer') return await recordDefer(ctx, layer4);
         const layer5 = await checkJurisdiction({ ...ctx, legalBasis: layer1.legalBasis }); if (layer5.decision !== 'allow') return await recordSkip(ctx, layer5);
         return { decision: 'allow' };
       }
       ```
       - `recordSkip(ctx, decision)` INSERTs dispatch_skips row.
       - `recordDefer(ctx, decision)` returns next_send_window without DB write (re-enqueue happens in fan-out.ts).
    8. Create test files:
       - `test/channels/consent/eligibility-cutover.test.ts` (Tests 1, 9 — runs P104 vector regression with mock injection).
       - `test/channels/gate/consent-check.test.ts` (Test 2).
       - `test/channels/gate/suppression-check.test.ts` (Test 3).
       - `test/channels/gate/frequency-cap.test.ts` (Test 4).
       - `test/channels/gate/quiet-hours.test.ts` (Test 5).
       - `test/channels/gate/jurisdiction-check.test.ts` (Test 6).
       - `test/channels/gate/eligibility-gate.test.ts` (Tests 7, 8 — first-fail short-circuit + dispatch_skips audit row).
    9. Append VALIDATION rows.
  </action>
  <verify>
    <automated>vitest run test/channels/consent/ test/channels/gate/ && npm test</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/markos/outbound/consent.ts` contains `getConsentState` import (grep `getConsentState` in file)
    - File `lib/markos/outbound/consent.ts` does NOT contain `outboundConsentRecords` direct read access (grep should return zero — or only inside legacy compatibility shim functions)
    - File `lib/markos/channels/gate/eligibility-gate.ts` exports `evaluateDispatchEligibility` (grep)
    - All 5 layer files exist under `lib/markos/channels/gate/`
    - `vitest run test/channels/consent/eligibility-cutover.test.ts` exits 0 with all P104 regression vectors green
    - `vitest run test/channels/gate/` exits 0 with ≥30 passing assertions covering all 5 layers × allow/skip/defer paths × per-channel
    - `npm test` (legacy P104 outbound regression) exits 0
    - File `lib/markos/channels/gate/eligibility-gate.ts` first-fail short-circuit verified by spy: when checkSuppression returns SKIP, checkFrequencyCap is NOT called (grep test contains `expect(freqCapMock).not.toHaveBeenCalled`)
  </acceptance_criteria>
  <done>consent.ts cutover complete; 5-layer gate operational; dispatch_skips audit rows written; P104 regression suite green; gate fail-closed posture proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Dispatch infrastructure — AgentRun bridge (D-15) + Vercel Queues fan-out (with pg_boss fallback) + dispatch_attempt_id idempotency + suppression bounce-handler + F-128/F-129/F-130 contracts</name>
  <read_first>
    - lib/markos/channels/programs/email-campaign.ts (Wave 1 stub — markCampaignDispatchRun)
    - lib/markos/channels/gate/eligibility-gate.ts (Task 2 output — invoked by recipient-handler)
    - .planning/phases/223-native-email-messaging-orchestration/223-RESEARCH.md (§Architecture Patterns 3 §Vercel Queues §Pitfalls 1+8 §Frequency Cap)
    - .planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md (D-14, D-15, D-21, D-32, D-39)
    - .planning/phases/207-agentrun-v2-orchestration-substrate/207-CONTEXT.md (markos_agent_runs shape if available; A5 fallback)
    - .planning/config.json (workflow.agentrun_v2_available + workflow.vercel_queues_available)
    - supabase/migrations/117_channel_dispatch_tracking.sql (Task 1 output)
    - lib/markos/outbound/providers/base-adapter.ts (Wave 1 — sendOutboundMessage)
    - contracts/F-127-channel-template-v1.yaml (Wave 1 — yaml shape reference)
  </read_first>
  <behavior>
    - Test 1 (createDispatchRun bridge): when workflow.agentrun_v2_available=true -> writes markos_agent_runs row with run_type='channel_dispatch'; returns { run_id, source: 'agentrun' }. When flag=false -> writes markos_audit_log row with event_type='channel_dispatch_start'; returns { run_id: synthetic_uuid, source: 'audit_log_bridge' }.
    - Test 2 (Vercel Queues primary path): workflow.vercel_queues_available=true -> enqueueRecipients calls vercel queue API with idempotency_key=dispatch_attempt_id; verifies dedup at re-enqueue.
    - Test 3 (pg_boss fallback): workflow.vercel_queues_available=false -> enqueueRecipients calls pg_boss send with same idempotency contract; identical handler runs.
    - Test 4 (recipient-handler idempotency — Pitfall 1 mitigation): first call inserts dispatch_events row with dispatch_attempt_id, calls adapter.send. Second call with SAME dispatch_attempt_id -> early return (no second send, no second dispatch_events row). UNIQUE (dispatch_attempt_id, event_type) constraint enforces dedup.
    - Test 5 (recipient-handler with gate SKIP): evaluateDispatchEligibility -> SKIP -> dispatch_skips row written; dispatch_events row NOT written; adapter.send NOT called.
    - Test 6 (recipient-handler with gate DEFER): evaluateDispatchEligibility -> DEFER -> re-enqueue with delay = next_send_window - now(); no dispatch_events row.
    - Test 7 (handleBounceEvent hard): hard_bounce -> channel_suppressions row inserted with reason='hard_bounce'; cdp_events row emitted with event_name='channel.suppression_added'; both within transaction.
    - Test 8 (handleBounceEvent soft): soft_bounce_count incremented in dispatch_events; on 3rd retry within 7 days -> promote to hard via handleBounceEvent('hard'). Per-recipient retry tracker honored.
    - Test 9 (handleComplaintEvent): identical to hard bounce path with reason='complaint'.
    - Test 10 (tombstone propagation hook): when P221 emits profile tombstone (cdp_identity_profiles.tombstoned_at NOT NULL), cron or trigger inserts channel_suppressions row with reason='dsr_deletion' for all 5 channels — tested via direct invocation (cron wiring lands 223-06).
    - Test 11 (F-128/F-129/F-130 contracts): all three yaml files exist, valid YAML, flow_id matches filename, paths populated, approval_policy.high_risk_fields populated for F-130.
  </behavior>
  <action>
    1. Create `lib/markos/channels/dispatch/idempotency.ts`:
       - `deriveDispatchAttemptId(seedUuid, profileId)` -> v5-UUID derivation (deterministic per (seed, profile) pair).
       - `checkExistingDispatchEvent({ tenantId, dispatchAttemptId, eventType })` -> SELECT 1 from dispatch_events; returns boolean.
    2. Create `lib/markos/channels/dispatch/dispatch-coordinator.ts`:
       - `createDispatchRun({ tenantId, subjectType, subjectId })`:
         - Reads workflow.agentrun_v2_available from .planning/config.json (resolve at runtime via lib/markos/config helper).
         - If true: write markos_agent_runs row (run_type='channel_dispatch'); return { run_id, source: 'agentrun' }.
         - If false: write markos_audit_log row event_type='channel_dispatch_start' subject_type/subject_id; return { run_id: crypto.randomUUID(), source: 'audit_log_bridge' }.
       - `markDispatchComplete(runId, summary)` -> writes audit_log event_type='channel_dispatch_complete' OR updates markos_agent_runs status.
    3. Create `lib/markos/channels/dispatch/fan-out.ts`:
       - `enqueueRecipients({ campaignId, runId, recipients, dispatchAttemptIdSeed })`:
         - Reads workflow.vercel_queues_available.
         - If true: import @vercel/queue (or @vercel/functions queue API per latest docs — verify via Context7 MCP at execute time); for each recipient, call queue.send({ type: 'channel_dispatch', campaignId, profileId, dispatchAttemptId, runId }, { idempotencyKey: dispatchAttemptId }).
         - If false: import('pg-boss') (require at runtime to skip dependency when flag true); boss.send('channel_dispatch', {...}, { singletonKey: dispatchAttemptId }) for dedup. pg-boss config uses Supabase connection string from process.env.SUPABASE_DB_URL.
    4. Create `lib/markos/channels/dispatch/recipient-handler.ts`:
       - `handleRecipient({ campaignId, profileId, dispatchAttemptId, runId, channel, campaignType })`:
         1. existing = await checkExistingDispatchEvent({ tenantId, dispatchAttemptId, eventType: 'queued' }); if existing -> return { skipped: 'idempotent_replay' }.
         2. const decision = await evaluateDispatchEligibility(ctx); if decision != 'allow' -> handle skip/defer per Test 5/6 above.
         3. const adapter = getAdapterForChannel(channel) (wraps existing createResendAdapter / createTwilioAdapter / + Knock/in-app from 223-04).
         4. Inside transaction: INSERT dispatch_events { event_type: 'queued', dispatch_attempt_id, tenant_id, campaign_id, profile_id, channel, occurred_at: now() }; call adapter.send(message); INSERT dispatch_events { event_type: 'delivered', dispatch_attempt_id, ... } on synchronous adapter ack.
         5. Errors: catch and INSERT dispatch_events with event_type='failed' + audit_log entry.
       - All 5 ChannelType cases routed to correct adapter (handles email/sms/whatsapp now; Knock + in_app come from 223-04 — gracefully NOOP for those if adapter not yet present).
    5. Create `lib/markos/channels/suppression/suppression.ts`:
       - addSuppression({ tenantId, profileId, channel, reason, evidence_ref }) — INSERT channel_suppressions; emit cdp_events 'channel.suppression_added' (writes via P221 emit primitive when present, else direct INSERT into cdp_events).
       - listSuppressions({ tenantId, profileId? }) — SELECT.
       - removeSuppression({ tenantId, profileId, channel, actorId, reason }) — DELETE + audit_log row (operator override only; high-risk approval_ref required at API layer 223-05).
    6. Create `lib/markos/channels/suppression/bounce-handler.ts`:
       - handleBounceEvent({ tenantId, profileId, channel, dispatchAttemptId, severity }) — severity='hard' -> addSuppression(reason='hard_bounce'). severity='soft' -> increment soft_bounce_count; check retry tracker (3 retries / 7 days from dispatch_events soft_bounce occurrences); if exceeded -> promote to handleBounceEvent({ ..., severity: 'hard' }).
       - handleComplaintEvent({ tenantId, profileId, channel, dispatchAttemptId }) — addSuppression(reason='complaint').
       - propagateTombstoneToSuppressions({ tenantId, profileId, source_event_ref }) — for all 5 channels, addSuppression(reason='dsr_deletion'). Called from P221 D-24 tombstone trigger (cron wiring 223-06).
    7. Create 3 contract YAMLs in `contracts/`:
       - F-128-channel-suppression-v1.yaml: paths GET/POST /v1/channels/suppressions + DELETE /v1/channels/suppressions/{id}; approval_policy.high_risk_fields: [bulk_add, removal].
       - F-129-channel-dispatch-event-v1.yaml: read-only paths GET /v1/channels/dispatch/events?campaign_id=... and GET /v1/channels/dispatch/skips?campaign_id=... .
       - F-130-channel-program-dispatch-write.yaml: write paths POST /v1/channels/email/programs/{id}/dispatch, POST /v1/channels/messaging/threads/{id}/send, POST /v1/channels/lifecycle/journeys/{id}/run-step. approval_policy.high_risk_fields: [audience_snapshot_id, sender_identity_id, requires_approval]; precondition: approval_ref required when class IN ('broadcast','revenue') OR audience_size >= 500.
    8. Update `contracts/flow-registry.json` append-only with 3 new entries (F-128, F-129, F-130).
    9. Update `.planning/config.json` adding workflow flags: `agentrun_v2_available: false`, `vercel_queues_available: true` (or whatever is current Vercel default), `outbound_consent_legacy_view: true`.
    10. Create test files:
        - `test/channels/dispatch/dispatch-coordinator.test.ts` (Test 1).
        - `test/channels/dispatch/recipient-handler.test.ts` (Tests 2-6, 11 — fan-out + handler + idempotency).
        - `test/channels/dispatch/idempotency.test.ts` (Test 4 — UNIQUE constraint via DB; deriveDispatchAttemptId determinism).
        - `test/channels/suppression/bounce-handler.test.ts` (Tests 7-10).
    11. Append VALIDATION rows.
  </action>
  <verify>
    <automated>vitest run test/channels/dispatch/ test/channels/suppression/ && node bin/validate-flow-contracts.cjs</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/markos/channels/dispatch/dispatch-coordinator.ts` exports `createDispatchRun` (grep)
    - File `lib/markos/channels/dispatch/idempotency.ts` exports `deriveDispatchAttemptId` AND `checkExistingDispatchEvent`
    - File `lib/markos/channels/dispatch/recipient-handler.ts` contains `checkExistingDispatchEvent` call BEFORE adapter.send (grep order — first occurrence of checkExistingDispatchEvent at lower line number than first occurrence of adapter.send)
    - File `lib/markos/channels/suppression/bounce-handler.ts` contains `propagateTombstoneToSuppressions` (grep)
    - 3 contract files (F-128, F-129, F-130) exist; each contains its own flow_id
    - `contracts/flow-registry.json` includes F-128, F-129, F-130 entries
    - `vitest run test/channels/dispatch/` exits 0 with ≥10 assertions covering AgentRun bridge both flag values + Vercel/pg_boss fallback + idempotency
    - `vitest run test/channels/suppression/` exits 0 with bounce promote + tombstone propagation tests green
    - `node bin/validate-flow-contracts.cjs` exits 0 (all 3 new contracts valid YAML)
    - File `.planning/config.json` contains `vercel_queues_available` AND `agentrun_v2_available`
  </acceptance_criteria>
  <done>Dispatch coordinator + fan-out + recipient handler + idempotency operational; bounce/complaint/tombstone -> suppression propagation green; 3 contracts published; A5 (P207 absent) and A10 (Vercel Queues fallback) flag-controlled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recipient-handler -> external provider API | Adapter.send crosses out-of-process boundary; idempotency must protect against retry storms |
| outbound_consent_records_legacy -> trigger | Direct writes blocked at DB layer; only setConsentState (P221) is the single writer |
| dispatch_skips audit -> operator review | Skip rows are evidence; missing rows = compliance gap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-223-02-01 | Tampering | dispatch_attempt_id collision -> duplicate sends | mitigate | UNIQUE (dispatch_attempt_id, event_type) constraint + checkExistingDispatchEvent at handler top |
| T-223-02-02 | Information Disclosure | cross-tenant dispatch_events read | mitigate | RLS tenant_isolation on dispatch_events + dispatch_skips (migration 117) |
| T-223-02-03 | Spoofing | rogue legacy writer to outbound_consent_records | mitigate | block_direct_consent_writes trigger (migration 119) raises exception |
| T-223-02-04 | Elevation | gate bypass via direct adapter.send call | mitigate | recipient-handler is the only documented entry point; static analysis check in 223-06 RLS suite |
| T-223-02-05 | Denial of Service | Vercel Queues retry storm | mitigate | dispatch_attempt_id dedup + handler returns 200 on UNIQUE violation (Pitfall 3) |
| T-223-02-06 | Repudiation | bounce -> suppression without audit | mitigate | addSuppression emits cdp_events + writes audit_log; transaction-bound |
| T-223-02-07 | Information Disclosure | jurisdiction matrix exposes EU policy in MCP responses | accept | matrix is policy data, not secrets; no PII; tenant_id scoped |
</threat_model>

<verification>
- 4 migrations 117-120 land dispatch tracking + view swap + RLS hardening with idempotent re-run.
- View-swap trigger blocks direct legacy writes (verified test).
- consent.ts refactored to call getConsentState; P104 regression suite green via A12 mock injection.
- 5-layer gate fully implemented; first-fail short-circuit + dispatch_skips audit row on every block.
- AgentRun bridge handles both workflow.agentrun_v2_available true/false.
- Vercel Queues primary + pg_boss fallback handled by workflow.vercel_queues_available flag.
- dispatch_attempt_id dedup proven (Pitfall 1 mitigation).
- Bounce handler propagates hard/soft/complaint -> suppression + cdp_events emit.
- Tombstone -> suppression propagation hook present.
- F-128, F-129, F-130 contracts published.
- VALIDATION.md per-task rows added.
</verification>

<success_criteria>
- 4 migrations + 13 library files + 3 contract files + flow-registry update + 14 test files committed.
- Full P104 regression suite green.
- 5-layer gate × 5 channels × allow/skip/defer paths covered ≥75% (sufficient for compliance).
- dispatch_attempt_id idempotency proven (Pitfall 1 mitigation).
- View-swap trigger blocks direct legacy writes.
</success_criteria>

<output>
After completion, create `.planning/phases/223-native-email-messaging-orchestration/223-02-SUMMARY.md` capturing:
- Migrations 117-120 schema landed
- consent.ts cutover complete; P104 regression A12 mock pattern documented
- 5-layer gate operational; jurisdiction-matrix.json shipped as initial data
- AgentRun bridge + Vercel Queues fan-out + pg_boss fallback ready
- Bounce/complaint/tombstone -> suppression propagation chain green
- F-128/F-129/F-130 contracts registered
- Hand-off to 223-03 + 223-04: dispatch infra ready; templates/providers can plug in
</output>
