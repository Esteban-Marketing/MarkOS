---
phase: 223
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - scripts/preconditions/223-check-upstream.cjs
  - lib/markos/channels/preflight/upstream-gate.ts
  - lib/markos/channels/preflight/errors.ts
  - lib/markos/channels/preflight/architecture-lock.ts
  - test/channels/preflight/upstream-gate.test.js
  - test/channels/preflight/architecture-lock.test.js
  - test/channels/preflight/helper-presence.test.js
  - test/fixtures/channels/sender-identity.js
  - test/fixtures/channels/email-campaign.js
  - test/fixtures/channels/messaging-thread.js
  - test/fixtures/channels/lifecycle-journey.js
  - test/fixtures/channels/template.js
  - test/fixtures/channels/suppression.js
  - test/fixtures/channels/dispatch.js
  - test/fixtures/channels/in-app-message.js
  - test/fixtures/channels/deliverability-posture.js
  - supabase/migrations/113_channel_sender_identity.sql
  - supabase/migrations/114_channel_templates.sql
  - supabase/migrations/115_channel_programs.sql
  - supabase/migrations/116_channel_suppressions.sql
  - lib/markos/outbound/providers/base-adapter.ts
  - lib/markos/channels/index.ts
  - lib/markos/channels/programs/email-campaign.ts
  - lib/markos/channels/programs/messaging-thread.ts
  - lib/markos/channels/programs/lifecycle-journey.ts
  - lib/markos/channels/senders/sender-identity.ts
  - lib/markos/channels/templates/channel-template.ts
  - lib/markos/channels/adapters/legacy-outbound.ts
  - contracts/F-122-channel-email-campaign-v1.yaml
  - contracts/F-123-channel-messaging-thread-v1.yaml
  - contracts/F-124-channel-lifecycle-journey-v1.yaml
  - contracts/F-125-channel-sender-identity-v1.yaml
  - contracts/F-126-channel-deliverability-posture-v1.yaml
  - contracts/F-127-channel-template-v1.yaml
  - contracts/flow-registry.json
  - test/channels/schema/baseline.test.js
  - test/channels/schema/sender-identity.test.js
  - test/channels/schema/channel-template.test.js
  - test/channels/schema/email-campaign.test.js
  - test/channels/schema/messaging-thread.test.js
  - test/channels/schema/lifecycle-journey.test.js
  - test/channels/schema/channel-suppressions.test.js
  - test/channels/providers/base-adapter.test.js
  - test/channels/adapters/legacy-outbound.test.js
  - .planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md
autonomous: true
requirements:
  - EML-01
  - EML-02
  - MSG-01
  - MSG-03
  - QA-01
  - QA-02
must_haves:
  truths:
    - "Phase 223 architecture is locked to repo conventions (D-42): legacy api/*.js routes, requireHostedSupabaseAuth wrapper, npm test (Node --test) runner, contracts/openapi.json, lib/markos/mcp/tools/index.cjs registry, resolvePlugin helper. Plan 01 Task 0.5 ships the architecture-lock test that fails if any forbidden pattern reappears."
    - "Upstream preflight gate is HARD per D-45: REQUIRED_UPSTREAM = [P205, P207, P208, P209, P210, P211, P221, P222]. assertUpstreamReady() throws UpstreamPhaseNotLandedError if any phase is absent. No bridge stubs. No fallback flags."
    - "F-IDs F-122..F-131 and migrations 113..121 are pre-allocated per D-40/D-41/D-55: Plan 01 owns F-122/F-123/F-124/F-127 reservation; Plan 02 owns F-125/F-126/F-128/F-129; Plan 03 owns F-131; Plan 05 owns F-130. Plan 06 closeout includes F-ID-collision regression test."
    - "lib/markos/channels/* tree is created NEW by P223 (D-44 — P223-owned greenfield). Subdirectories (programs/, senders/, templates/, gate/, dispatch/, events/, suppression/, adapters/, deliverability/, preflight/, mcp/, api/) do NOT exist before P223. Tasks use 'CREATE NEW' language, not 'evolve existing'."
    - "lib/markos/cdp/* and lib/markos/crm360/* are upstream-owned per D-44 (P221/P222 ship them). P223 references via assertUpstreamReady — no shim, no bridge, no graceful absence path."
    - "buildApprovalPackage from lib/markos/crm/agent-actions.ts:68 is the approval-package factory per D-43. createApprovalPackage does NOT exist anywhere in the codebase — every Plan in P223 uses buildApprovalPackage."
    - "sender_identities + deliverability_posture + channel_templates + channel_suppressions + email_campaigns + messaging_threads + lifecycle_journeys + lifecycle_journey_runs are tenant-scoped, RLS-enforced systems of record (D-01..D-08, D-25, D-36)"
    - "base-adapter.ts CHANNEL_CAPABILITIES expanded from 3 (email/sms/whatsapp) to 5 (adds push/in_app); adapter contract gains verifyWebhookSignature + getRateLimitState + getReputationSnapshot per D-08"
    - "legacy-outbound.ts adapter (P223-owned greenfield NEW file) bridges existing api/crm/outbound/* routes to the new channel programs surface (D-33 transition path); zero behavior change for current callers"
    - "F-122..F-127 contract YAML files exist with flow_id, paths, and approval_policy fields populated; contracts/flow-registry.json is updated to include all six new entries (QA-01)"
    - "test/fixtures/channels/* directory ships fixture factories (.js, ESM-compatible per D-46) for every new table so 223-02..06 can import them"
    - "All P100-P105 outbound tests still green after base-adapter.ts extension (legacy regression — verified via npm test)"
  artifacts:
    - path: scripts/preconditions/223-check-upstream.cjs
      provides: CLI invocation of assertUpstreamReady; node scripts/preconditions/223-check-upstream.cjs returns 0 if all upstream landed, non-zero with explicit phase list if any absent
    - path: lib/markos/channels/preflight/upstream-gate.ts
      provides: assertUpstreamReady(REQUIRED_UPSTREAM) function — checks for sentinel files/migrations from each upstream phase; throws UpstreamPhaseNotLandedError listing missing phases (D-45)
    - path: lib/markos/channels/preflight/errors.ts
      provides: UpstreamPhaseNotLandedError + ArchitectureLockViolationError exception classes
    - path: lib/markos/channels/preflight/architecture-lock.ts
      provides: scanForForbiddenPatterns(planFiles) — scans 223-*-PLAN.md files for forbidden patterns (createApprovalPackage, requireSupabaseAuth, requireTenantContext, serviceRoleClient, lookupPlugin, public/openapi.json, app/(public), app/(markos), api/v1/.../route.ts, app/api/cron/.../route.ts, vitest run, from 'vitest', .test.ts, bridge stub, workflow.agentrun_v2_available, workflow.outbound_consent_legacy_view); returns violations[] (D-42)
    - path: test/channels/preflight/upstream-gate.test.js
      provides: tests assertUpstreamReady throw path + happy path
    - path: test/channels/preflight/architecture-lock.test.js
      provides: scans every 223-*-PLAN.md and asserts zero forbidden-pattern violations
    - path: test/channels/preflight/helper-presence.test.js
      provides: positive verification that buildApprovalPackage + requireHostedSupabaseAuth + resolvePlugin EXIST in their canonical files; negative verification that createApprovalPackage + requireSupabaseAuth + lookupPlugin DO NOT exist anywhere in lib/
    - path: supabase/migrations/113_channel_sender_identity.sql
      provides: sender_identities + deliverability_posture tables + RLS policies + composite PK on (tenant_id, channel, sender_id, window_24h_start)
    - path: supabase/migrations/114_channel_templates.sql
      provides: channel_templates table + RLS + composite UNIQUE (tenant_id, name, locale, version) + parent_template_id self-FK
    - path: supabase/migrations/115_channel_programs.sql
      provides: email_campaigns + messaging_threads + lifecycle_journeys + lifecycle_journey_runs + RLS + counters + UNIQUE(journey_id, profile_id)
    - path: supabase/migrations/116_channel_suppressions.sql
      provides: channel_suppressions + RLS + UNIQUE (tenant_id, profile_id, channel)
    - path: lib/markos/outbound/providers/base-adapter.ts
      provides: 5-channel CHANNEL_CAPABILITIES (email/sms/whatsapp/push/in_app); validateAdapterContract(adapter) requiring send + normalizeEvent + verifyWebhookSignature + getRateLimitState; getReputationSnapshot optional
    - path: lib/markos/channels/programs/email-campaign.ts
      provides: createEmailCampaign + getEmailCampaign + listEmailCampaigns + setEmailCampaignStatus stubs (DB CRUD only; dispatch wiring lands in 223-02)
    - path: lib/markos/channels/programs/messaging-thread.ts
      provides: createMessagingThread + getMessagingThread + setThreadStatus + appendThreadMessage stubs
    - path: lib/markos/channels/programs/lifecycle-journey.ts
      provides: createLifecycleJourney + listLifecycleJourneys + setJourneyStatus + buildJourneyRun stubs
    - path: lib/markos/channels/senders/sender-identity.ts
      provides: createSenderIdentity + listSenderIdentities + updateVerificationStatus + getReputationStatus stubs
    - path: lib/markos/channels/templates/channel-template.ts
      provides: createChannelTemplate + getChannelTemplate + listChannelTemplates + setTemplateStatus + locale chain resolver stubs
    - path: lib/markos/channels/adapters/legacy-outbound.ts
      provides: legacy CRM outbound -> channel programs adapter so api/crm/outbound/{send,bulk-send,sequences,templates}.js continue to work
    - path: contracts/F-122-channel-email-campaign-v1.yaml
      provides: F-122 read-write contract for email campaigns (CRUD + status lifecycle + approval gate field)
    - path: contracts/F-123-channel-messaging-thread-v1.yaml
      provides: F-123 read-write contract for messaging threads (CRUD + status + related_crm_id linkage)
    - path: contracts/F-124-channel-lifecycle-journey-v1.yaml
      provides: F-124 read-write contract for lifecycle journeys + pause/resume + run management
    - path: contracts/F-125-channel-sender-identity-v1.yaml
      provides: F-125 read-write contract for SenderIdentity CRUD + verification + reputation status
    - path: contracts/F-126-channel-deliverability-posture-v1.yaml
      provides: F-126 read-only contract for DeliverabilityPosture 24h rolling window per sender/channel
    - path: contracts/F-127-channel-template-v1.yaml
      provides: F-127 read-write contract for ChannelTemplate CRUD + locale variants + approval + preview
  key_links:
    - from: scripts/preconditions/223-check-upstream.cjs
      to: lib/markos/channels/preflight/upstream-gate.ts
      via: CLI shells out to upstream-gate.ts assertUpstreamReady; execute-phase boot can invoke this script as a precondition
    - from: test/channels/preflight/architecture-lock.test.js
      to: .planning/phases/223-native-email-messaging-orchestration/223-*-PLAN.md
      via: scans every plan file; fails if any forbidden pattern from D-42 reappears
    - from: lib/markos/outbound/providers/base-adapter.ts
      to: lib/markos/channels/programs/email-campaign.ts
      via: getOutboundChannelCapabilities('email') resolves to 5-channel map; new 'push' and 'in_app' entries reachable
    - from: supabase/migrations/115_channel_programs.sql
      to: supabase/migrations/113_channel_sender_identity.sql
      via: email_campaigns.sender_identity_id FK -> sender_identities.sender_id
    - from: supabase/migrations/115_channel_programs.sql
      to: supabase/migrations/114_channel_templates.sql
      via: email_campaigns.template_id FK -> channel_templates.template_id
    - from: lib/markos/channels/adapters/legacy-outbound.ts
      to: api/crm/outbound/send.js
      via: legacy adapter intercepts current outbound calls and shape-maps to new channel programs surface; tests assert byte-equivalent response shape
    - from: contracts/flow-registry.json
      to: contracts/F-122-channel-email-campaign-v1.yaml
      via: registry entries for F-122..F-127 added in append-only fashion (preserves F-01..F-99 + F-101..F-121)
---

<objective>
Wave 1 architecture-lock + preflight gate + schema foundation for the Channel Engine.

**Task 0.5 (NEW per D-42/D-45):** Architecture-lock + upstream preflight gate. Ships scripts/preconditions/223-check-upstream.cjs CLI + lib/markos/channels/preflight/{upstream-gate,errors,architecture-lock}.ts + 3 test files (architecture-lock test scans all 223-*-PLAN.md for forbidden patterns; helper-presence test verifies buildApprovalPackage exists and createApprovalPackage does NOT; upstream-gate test verifies hard-fail behavior). Adds @babel/parser dependency to package.json.

**Task 1:** F-ID + migration baseline verification + 9 fixture factories (.js, Node --test compatible per D-46). Now reads architecture-lock + upstream-gate first.

**Task 2:** Migrations 113-116 (sender_identities, deliverability_posture, channel_templates, email_campaigns, messaging_threads, lifecycle_journeys, lifecycle_journey_runs, channel_suppressions) + RLS.

**Task 3:** Extend base-adapter.ts (3 -> 5 channels), scaffold lib/markos/channels/* P223-OWNED GREENFIELD module tree (per D-44 — files do NOT exist before this task), publish F-122..F-127 contracts + flow-registry update, ship legacy-outbound.ts bridge.

Purpose: satisfies EML-01, EML-02, MSG-01, MSG-03, QA-01, QA-02. Unblocks every subsequent 223-* plan via the schema + module tree + fixtures + preflight gate.

Output: 4 migration files, 4 preflight files (3 lib + 1 CLI script), 12 library/module files (1 extension + 11 NEW greenfield), 6 F-ID contracts, flow-registry.json update, 9 fixture factories, 11 test files.
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md
@.planning/phases/223-native-email-messaging-orchestration/223-RESEARCH.md
@.planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md
@.planning/phases/223-native-email-messaging-orchestration/223-REVIEWS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/V4.0.0-TESTING-ENVIRONMENT-PLAN.md
@.planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md
@.planning/phases/221-cdp-identity-audience-consent-substrate/221-CONTEXT.md
@.planning/phases/222-crm-timeline-commercial-memory-workspace/222-CONTEXT.md
@CLAUDE.md
@obsidian/reference/Contracts Registry.md
@obsidian/reference/Database Schema.md
@obsidian/reference/Core Lib.md
@obsidian/reference/HTTP Layer.md
@obsidian/work/incoming/19-EMAIL-ENGINE.md
@obsidian/work/incoming/21-MESSAGING-ENGINE.md
@lib/markos/outbound/providers/base-adapter.ts
@lib/markos/outbound/providers/resend-adapter.ts
@lib/markos/outbound/providers/twilio-adapter.ts
@lib/markos/outbound/consent.ts
@lib/markos/outbound/conversations.ts
@lib/markos/crm/agent-actions.ts
@onboarding/backend/runtime-context.cjs
@api/cron/webhooks-dlq-purge.js
@api/crm/outbound/send.js
@api/crm/outbound/bulk-send.js
@contracts/F-99-webhook-status-v1.yaml
@contracts/flow-registry.json
@contracts/openapi.json
@package.json
</context>

<interfaces>
<!-- Existing CHANNEL_CAPABILITIES (lib/markos/outbound/providers/base-adapter.ts) -->
```javascript
const CHANNEL_CAPABILITIES = Object.freeze({
  email:     { channel: 'email', provider: 'resend', supports_subject: true, supports_templates: true, supports_replies: true },
  sms:       { channel: 'sms', provider: 'twilio', supports_subject: false, supports_templates: false, supports_replies: true },
  whatsapp:  { channel: 'whatsapp', provider: 'twilio', supports_subject: false, supports_templates: true, supports_replies: true },
});
```

<!-- Target CHANNEL_CAPABILITIES (after this plan) -->
```javascript
const CHANNEL_CAPABILITIES = Object.freeze({
  email:     { channel: 'email', provider: 'resend', supports_subject: true, supports_templates: true, supports_replies: true,  supports_inbound: true },
  sms:       { channel: 'sms', provider: 'twilio', supports_subject: false, supports_templates: false, supports_replies: true, supports_inbound: true },
  whatsapp:  { channel: 'whatsapp', provider: 'twilio', supports_subject: false, supports_templates: true, supports_replies: true, supports_inbound: true },
  push:      { channel: 'push', provider: 'knock', supports_subject: true, supports_templates: true, supports_replies: false, supports_inbound: false },
  in_app:    { channel: 'in_app', provider: 'markos', supports_subject: true, supports_templates: true, supports_replies: false, supports_inbound: false },
});
```

<!-- Adapter contract (validateAdapterContract) -->
```typescript
type ChannelAdapter = {
  provider: string;
  channels: string[];
  send(message: SendInput): Promise<SendResult>;
  normalizeEvent(payload: unknown): NormalizedEvent;
  verifyWebhookSignature(payload: string, signature: string, secret: string): boolean; // NEW (D-08, D-38)
  getRateLimitState(): { remaining: number; reset_at: string };                          // NEW (D-08)
  getReputationSnapshot?(): { reputation_score: number; trend: string };                 // OPTIONAL (D-08)
};
```

<!-- Architecture-lock forbidden patterns (D-42) — scanned in 223-*-PLAN.md by Task 0.5 -->
```javascript
const FORBIDDEN_PATTERNS = [
  /createApprovalPackage/,                              // D-43 — does not exist
  /requireSupabaseAuth(?!\w)/,                          // D-42 — wrong wrapper
  /requireTenantContext/,                               // D-42 — wrong wrapper
  /serviceRoleClient/,                                  // D-42 — bypass
  /lookupPlugin/,                                       // D-42 — does not exist
  /public\/openapi\.json/,                              // D-47 — wrong path
  /app\/\(public\)/,                                    // D-42 — App Router not used
  /app\/\(markos\)/,                                    // D-42 — App Router not used
  /api\/v1\/[^\s]*\/route\.ts/,                         // D-42 — App Router not used
  /app\/api\/cron\/[^\s]*\/route\.ts/,                  // D-49 — legacy api/cron/*.js only
  /vitest\s+run/,                                       // D-46 — npm test only
  /from\s+['"]vitest['"]/,                              // D-46 — npm test only
  /\.test\.ts(?:\s|"|'|$)/,                             // D-46 — .test.js only
  /bridge\s*stub|stub\s*if\s*absent/i,                  // D-45 — no soft-skip
  /workflow\.agentrun_v2_available/,                    // D-45 — REJECTED flag
  /workflow\.outbound_consent_legacy_view/,             // D-51 — REJECTED flag
  /workflow\.vercel_queues_available/,                  // D-45 trim — REJECTED flag
];
```

<!-- Existing flow-registry.json shape (append-only) -->
```json
{
  "generated_at": "...",
  "phase": "...",
  "total_flows": 17,
  "flows": [ { "flow_id": "F-01", "flow_name": "...", ... }, ... ]
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0.5 (NEW per D-42/D-45): Architecture-lock + upstream preflight gate + npm install dependencies</name>
  <read_first>
    - .planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md (D-42..D-55, especially D-42 forbidden patterns + D-45 REQUIRED_UPSTREAM list)
    - .planning/phases/223-native-email-messaging-orchestration/223-REVIEWS.md (RH1..RH7 + RM1..RM5)
    - lib/markos/crm/agent-actions.ts (verify buildApprovalPackage at line 68)
    - onboarding/backend/runtime-context.cjs (verify requireHostedSupabaseAuth at line 491)
    - lib/markos/plugins/registry.js (verify resolvePlugin at line 102)
    - lib/markos/mcp/tools/index.cjs (verify file exists as .cjs)
    - contracts/openapi.json (verify path)
    - api/cron/webhooks-dlq-purge.js (verify legacy api/cron/*.js + shared-secret pattern)
    - package.json (current dependencies + scripts)
  </read_first>
  <behavior>
    - Test 1 (helper-presence): grep `^function buildApprovalPackage` in lib/markos/crm/agent-actions.ts succeeds. grep `requireHostedSupabaseAuth` in onboarding/backend/runtime-context.cjs succeeds. grep `resolvePlugin` in lib/markos/plugins/registry.js succeeds.
    - Test 2 (negative helper-presence — D-43 verification): grep `createApprovalPackage` across `lib/` returns 0 matches. grep `requireSupabaseAuth(?!\w)` across `lib/` + `onboarding/` returns 0 matches. grep `lookupPlugin` across `lib/` returns 0 matches.
    - Test 3 (architecture-lock scan): scanForForbiddenPatterns reads every .planning/phases/223-native-email-messaging-orchestration/223-*-PLAN.md and asserts ZERO matches against FORBIDDEN_PATTERNS list above. (Doctrinal "REJECTED" mentions inside code-fenced blocks or `<deferred>` markers in CONTEXT/REVIEWS are the only allowed exceptions; the scan flags only PLAN files, not CONTEXT/REVIEWS.)
    - Test 4 (assertUpstreamReady happy path): mock all 8 sentinel files/migrations present -> assertUpstreamReady() returns void.
    - Test 5 (assertUpstreamReady throw path): mock P207 sentinel absent -> throws UpstreamPhaseNotLandedError with message containing 'P207' and listing every missing phase.
    - Test 6 (CLI script): `node scripts/preconditions/223-check-upstream.cjs` returns exit 0 when all upstream landed; non-zero with explicit phase list when any absent. stderr contains the phase list.
    - Test 7 (npm dependencies): package.json contains `@babel/parser` (for D-52 AST classifier) + `handlebars` (for D-26 renderer) + `@knocklabs/node` (for D-09 push). Versions pinned via npm view at install time.
  </behavior>
  <action>
    1. Create `lib/markos/channels/preflight/errors.ts`:
       - Export `UpstreamPhaseNotLandedError` (Error subclass) with `missingPhases: string[]` field.
       - Export `ArchitectureLockViolationError` with `violations: Array<{ pattern: string, file: string, line: number }>`.
    2. Create `lib/markos/channels/preflight/upstream-gate.ts`:
       - Define `REQUIRED_UPSTREAM = ['P205', 'P207', 'P208', 'P209', 'P210', 'P211', 'P221', 'P222']`.
       - Define `UPSTREAM_SENTINELS` map: each phase to a verifiable artifact (e.g., P207 -> `supabase/migrations/*_markos_agent_runs.sql` OR `lib/markos/agent-runs/index.ts`; P221 -> `lib/markos/cdp/adapters/crm-projection.ts`; etc.). When a phase has not yet landed, the sentinel does not exist.
       - Export `assertUpstreamReady(required = REQUIRED_UPSTREAM): void`. Iterates required phases, checks sentinels via `fs.existsSync`. Collects missing -> throws `UpstreamPhaseNotLandedError(missing)`.
       - Export `getUpstreamStatus(): Record<string, { landed: boolean, sentinel: string }>` for diagnostics.
    3. Create `lib/markos/channels/preflight/architecture-lock.ts`:
       - Export `FORBIDDEN_PATTERNS` array (per `<interfaces>` block above).
       - Export `scanForForbiddenPatterns(planFiles: string[]): Array<{ pattern: string, file: string, line: number, snippet: string }>`. Reads each file, tests every line against every forbidden pattern, returns list of violations.
       - Export `assertNoArchitectureViolations(planDir: string): void`. Globs `${planDir}/223-*-PLAN.md`, runs scanForForbiddenPatterns, throws ArchitectureLockViolationError if any.
    4. Create `scripts/preconditions/223-check-upstream.cjs` (CommonJS for CLI invocation):
       - Requires the compiled upstream-gate via ts-node OR uses native Node 22+ TS support if available; else inlines the sentinel check logic.
       - Calls assertUpstreamReady().
       - On success: `console.log('OK: all upstream phases landed')` + exit 0.
       - On failure: `console.error('BLOCKED: missing phases:', err.missingPhases.join(','))` + exit 1.
       - Documents at top: "Phase 223 preflight gate per D-45. Invoked by execute-phase before plan execution begins."
    5. Create `test/channels/preflight/upstream-gate.test.js`:
       - Uses `node:test` + `node:assert/strict` per D-46.
       - Mocks fs.existsSync via `node:test` mock or by setting up fixture filesystem (write sentinels under temp dir, point gate at temp dir).
       - Tests Test 4 (happy path) and Test 5 (throw path with explicit message check).
    6. Create `test/channels/preflight/architecture-lock.test.js`:
       - Reads every `.planning/phases/223-native-email-messaging-orchestration/223-*-PLAN.md` file via fs.readdirSync.
       - For each forbidden pattern, asserts ZERO matches across the joined contents.
       - Test 3 in <behavior>; this test is the gate that catches regression of forbidden patterns in any subsequent plan revision.
       - Output is detailed: `assert.deepStrictEqual(violations, [], 'forbidden patterns found in plan files: ' + JSON.stringify(violations, null, 2))`.
    7. Create `test/channels/preflight/helper-presence.test.js`:
       - POSITIVE: assert `lib/markos/crm/agent-actions.ts` content matches `/^function buildApprovalPackage/m` AND `/^module\.exports.*buildApprovalPackage/m` (or equivalent CommonJS/ESM export pattern).
       - POSITIVE: assert `onboarding/backend/runtime-context.cjs` matches `/function requireHostedSupabaseAuth/`.
       - POSITIVE: assert `lib/markos/plugins/registry.js` matches `/resolvePlugin/`.
       - POSITIVE: assert `lib/markos/mcp/tools/index.cjs` exists as `.cjs` file.
       - POSITIVE: assert `contracts/openapi.json` exists.
       - NEGATIVE: assert grep across `lib/` returns 0 matches for `createApprovalPackage`. (Test uses `child_process.execSync('grep -r "createApprovalPackage" lib/')` and asserts non-zero exit OR empty output.)
       - NEGATIVE: assert 0 matches for `requireSupabaseAuth(?!\w)` across `lib/` + `onboarding/`.
       - NEGATIVE: assert 0 matches for `lookupPlugin` across `lib/`.
    8. Update `package.json`:
       - Add to devDependencies: `"@babel/parser": "^7.x"` (for D-52 AST pricing classifier).
       - Add to dependencies: `"handlebars": "^4.7.8"` + `"@knocklabs/node": "^1.x"`.
       - Run `npm install` to lock versions; planner reads `npm view <pkg> version` to suggest pin (Plan 01 task action notes the actual version selected at execute time).
       - Add npm script: `"preflight:223": "node scripts/preconditions/223-check-upstream.cjs"`.
       - **Do NOT add vitest or playwright** — D-46 keeps the runner as `npm test` (Node `--test`).
    9. Sanity: append a row to 223-VALIDATION.md for Task 0.5 with automated command `npm test -- test/channels/preflight/`.
  </action>
  <verify>
    <automated>npm test -- --test-reporter=spec test/channels/preflight/upstream-gate.test.js test/channels/preflight/architecture-lock.test.js test/channels/preflight/helper-presence.test.js</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/preconditions/223-check-upstream.cjs` exists and is executable (`node scripts/preconditions/223-check-upstream.cjs` exits 0 when sentinels present, non-zero with phase list when absent)
    - File `lib/markos/channels/preflight/upstream-gate.ts` exports `assertUpstreamReady` AND `REQUIRED_UPSTREAM` (grep both)
    - File `lib/markos/channels/preflight/upstream-gate.ts` REQUIRED_UPSTREAM array contains exactly: P205, P207, P208, P209, P210, P211, P221, P222
    - File `lib/markos/channels/preflight/errors.ts` exports `UpstreamPhaseNotLandedError` AND `ArchitectureLockViolationError`
    - File `lib/markos/channels/preflight/architecture-lock.ts` exports `FORBIDDEN_PATTERNS` AND `scanForForbiddenPatterns` AND `assertNoArchitectureViolations`
    - 3 test files exist under `test/channels/preflight/`; each ends with `.test.js` (NOT `.test.ts` per D-46)
    - `npm test -- test/channels/preflight/` exits 0 with all tests green
    - architecture-lock.test.js asserts ZERO violations across all 223-*-PLAN.md files
    - helper-presence.test.js verifies positive existence of buildApprovalPackage/requireHostedSupabaseAuth/resolvePlugin AND negative non-existence of createApprovalPackage/requireSupabaseAuth/lookupPlugin
    - package.json contains `@babel/parser` AND `handlebars` AND `@knocklabs/node` AND `preflight:223` script
    - package.json does NOT contain `vitest` or new `playwright` deps (D-46 enforcement)
  </acceptance_criteria>
  <done>Architecture-lock + upstream preflight gate operational. Plans 02-06 cannot regress the forbidden-pattern set without test failure. Execute-phase can invoke `npm run preflight:223` before plan execution to gate on upstream readiness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Verify F-ID + migration baseline (A1/A2 handlers) and seed test fixtures</name>
  <read_first>
    - .planning/phases/223-native-email-messaging-orchestration/223-RESEARCH.md (§F-ID + Migration Allocation, §Assumptions Log A1/A2)
    - .planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md (§Wave 0 Requirements)
    - .planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md (D-40 F-ID slot table, D-41 migration slot table, D-44 greenfield ownership map, D-45 REQUIRED_UPSTREAM)
    - lib/markos/channels/preflight/upstream-gate.ts (Task 0.5 output)
    - contracts/flow-registry.json (existing F-01..F-99 entries)
    - supabase/migrations/ directory (highest existing migration number)
    - test/fixtures/cdp/ (P221 fixture pattern reference) — if missing, FAIL via assertUpstreamReady (D-45)
    - test/fixtures/crm360/ (P222 fixture pattern reference) — if missing, FAIL via assertUpstreamReady (D-45)
    - package.json (verify @babel/parser + handlebars + @knocklabs/node added in Task 0.5)
  </read_first>
  <behavior>
    - Test 1 (preflight gate runs first): `assertUpstreamReady()` is called at the top of baseline.test.js. If any of P205/P207/P208/P209/P210/P211/P221/P222 sentinels are absent, the test throws UpstreamPhaseNotLandedError BEFORE any other assertion runs (per D-45).
    - Test 2: ls supabase/migrations/ returns no file with prefix 113_ through 121_ (assertion: P223 migration range 113-121 unclaimed per D-41). If any of those numbers exist, throw with explicit conflict message.
    - Test 3: jq '.flows[].flow_id' contracts/flow-registry.json | sort -u contains no F-122..F-131 entries (assertion: P223 F-ID range unclaimed per D-40). If conflict, throw with renumber instruction.
    - Test 4: each fixture file under test/fixtures/channels/ exports a factory function returning the documented shape (e.g., createEmailCampaignFixture({ campaign_type: 'broadcast' }) returns object with all D-02 columns).
    - Test 5: package.json contains @babel/parser + handlebars + @knocklabs/node (verifies Task 0.5 install completed).
  </behavior>
  <action>
    1. Create `test/fixtures/channels/` directory.
    2. Create `test/fixtures/channels/sender-identity.js` (per D-46 — `.js` not `.ts`) exporting `createSenderIdentityFixture(overrides = {})` returning a row matching D-06 with defaults: `{ sender_id, tenant_id, label: 'Default', from_name: 'MarkOS', from_email: 'no-reply@example.com', sending_domain: 'example.com', verification_status: 'pending', dkim_status: 'pending', spf_status: 'pending', dmarc_status: 'pending', reputation_status: 'warming', daily_send_limit: 1000, class_permissions: ['transactional'], created_at: new Date().toISOString() }`. Provide variants: `verifiedHealthy()`, `warming()`, `atRisk()`. Use CommonJS-compatible exports (`module.exports = {...}`) since `node --test` parses both ESM and CJS.
    3. Create `test/fixtures/channels/email-campaign.js` exporting `createEmailCampaignFixture(overrides = {})` returning row matching D-02. Provide variants: `draft()`, `broadcastPendingApproval()`, `transactionalAutoApproved()`, `dispatching()`, `revenueOneOff()`.
    4. Create `test/fixtures/channels/messaging-thread.js` exporting `createMessagingThreadFixture(overrides = {})` returning row matching D-03. Provide variants per channel × per status: `whatsappOpen()`, `smsReplyPending()`, `pushEscalated()`, `inAppBlocked()`, `withRelatedCrm(crmId)`.
    5. Create `test/fixtures/channels/lifecycle-journey.js` exporting `createLifecycleJourneyFixture(overrides = {})` returning row matching D-04 + companion `createJourneyRunFixture(overrides = {})` matching `lifecycle_journey_runs`. Variants: `singleStep()`, `multiStepWithBranch()`, `paused()`.
    6. Create `test/fixtures/channels/template.js` exporting `createChannelTemplateFixture(overrides = {})` returning row matching D-25. Variants: `marketingPricingBound()` (pricing_bindings populated), `evidenceBound()` (evidence_bindings populated), `localeVariant(parentId, locale)`, `pushTemplate()`.
    7. Create `test/fixtures/channels/suppression.js` exporting `createSuppressionFixture(overrides = {})` matching D-19/D-21. Variants: `hardBounce()`, `complaint()`, `manualOp()`, `dsrDeletion()`, `jurisdiction()`, `optOut()`.
    8. Create `test/fixtures/channels/dispatch.js` exporting `createDispatchEventFixture(overrides = {})` + `createDispatchSkipFixture(overrides = {})` matching the D-19 6-gate failure modes (consent, suppression, whatsapp_session_lost, frequency_cap, quiet_hours, jurisdiction) + missing_variable + pricing_unresolved + approval_revoked.
    9. Create `test/fixtures/channels/in-app-message.js` exporting `createInAppMessageFixture(overrides = {})` per D-10 (variants: `unread()`, `read()`, `expired()`).
    10. Create `test/fixtures/channels/deliverability-posture.js` exporting `createDeliverabilityPostureFixture(overrides = {})` matching D-07. Variants: `healthy()` (bounce<1%, complaint<0.1%, reputation 85-100, trend="stable"), `watch()` (bounce 1-3%, reputation 60-85, trend="watch"), `atRisk()` (bounce>3% OR complaint>0.5%, reputation<60, trend="degrading"), `improving()` (week-over-week +5 reputation_score), `stable()` (delta within ±1), `degrading()` (delta -5 or worse). Used by 223-06 deliverability rollup + spike alert tests.
    11. Create `test/channels/schema/baseline.test.js` (per D-46 — `.js` not `.ts`) running the five tests above using `node:test` + `node:assert/strict`. Test 1 invokes `assertUpstreamReady()` at the top of the suite — hard fail per D-45 if upstream missing.
  </action>
  <verify>
    <automated>npm test -- test/channels/schema/baseline.test.js</automated>
  </verify>
  <acceptance_criteria>
    - File `test/fixtures/channels/sender-identity.js` exports `createSenderIdentityFixture` with all 17 columns from D-06 (grep "createSenderIdentityFixture" in file)
    - File `test/fixtures/channels/email-campaign.js` exports `createEmailCampaignFixture` with `campaign_type` enum check, `audience_snapshot_id`, `template_id`, `sender_identity_id`, `status` (grep "campaign_type" + "status")
    - File `test/fixtures/channels/messaging-thread.js` exports `createMessagingThreadFixture` containing `related_crm_id` field (grep "related_crm_id")
    - All 9 fixture files end with `.js` (NOT `.ts`) per D-46
    - `npm test -- test/channels/schema/baseline.test.js` exits 0; output contains "F-ID range F-122..F-131 unclaimed: OK" AND "Migration range 113..121 unclaimed: OK"
    - `package.json` dependencies (or devDependencies) include `handlebars` AND `@knocklabs/node` AND `@babel/parser`
    - When upstream sentinels absent, baseline.test.js throws UpstreamPhaseNotLandedError BEFORE any baseline assertion (verified via mock-fs test variant)
  </acceptance_criteria>
  <done>F-ID + migration baseline verified disjoint, all 9 fixture factories exist as `.js`, upstream preflight gate hard-fails on missing prereqs (D-45), baseline test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrations 113-116 (sender_identities, deliverability_posture, channel_templates, email_campaigns, messaging_threads, lifecycle_journeys, lifecycle_journey_runs, channel_suppressions) + RLS</name>
  <read_first>
    - supabase/migrations/96_neuro_literacy_metadata.sql (most recent migration — RLS pattern reference)
    - supabase/migrations/82_markos_audit_log_hash_chain.sql (audit_log integration pattern)
    - .planning/phases/223-native-email-messaging-orchestration/223-RESEARCH.md (§Schema Sketches Migrations 113-116)
    - .planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md (D-01..D-08, D-25, D-36, D-41 migration slot table, D-44 greenfield ownership)
    - obsidian/reference/Database Schema.md (RLS + hash-chain canon)
    - test/fixtures/channels/* (built in Task 1)
  </read_first>
  <behavior>
    - Test 1: each of the 8 new tables has `current_setting('app.tenant_id')::UUID` RLS policy enabled (`tenant_isolation`). Insert with mismatched tenant context fails with "tenant_isolation" violation.
    - Test 2: sender_identities has UNIQUE (tenant_id, from_email); duplicate insert raises 23505.
    - Test 3: deliverability_posture composite PK (tenant_id, channel, sender_id, window_24h_start) accepts overlapping windows for different sender_ids; rejects exact-match duplicate.
    - Test 4: channel_templates UNIQUE (tenant_id, name, locale, version) — insert version=1 then version=2 with same name+locale succeeds; duplicate version raises 23505.
    - Test 5: email_campaigns CHECK constraints reject invalid campaign_type, status, time_zone_strategy values.
    - Test 6: messaging_threads CHECK constraints reject invalid channel, current_status, last_direction, sentiment values.
    - Test 7: lifecycle_journey_runs UNIQUE (journey_id, profile_id) — second insert for same profile in same journey raises 23505.
    - Test 8: channel_suppressions UNIQUE (tenant_id, profile_id, channel) — second suppression for same triple raises 23505.
    - Test 9: all 8 tables have FK to markos_tenants(id); tenant deletion cascades or restricts per existing P201 pattern.
    - Test 10: re-running each migration is idempotent (CREATE TABLE IF NOT EXISTS path or wrapping in DO block — match existing project pattern from migration 96).
    - Test 11: messaging_threads + lifecycle_journey_runs reference upstream tables (cdp_identity_profiles, customer_360_records). Per D-45, FK is created unconditionally because preflight gate (Task 0.5) guarantees P221/P222 landed. NO conditional ALTER, NO graceful absence path.
  </behavior>
  <action>
    1. Create `supabase/migrations/113_channel_sender_identity.sql` with content from 223-RESEARCH.md §Migration 113 (DDL for `sender_identities` and `deliverability_posture`). Match the project's existing migration idempotency style (look at `supabase/migrations/96_neuro_literacy_metadata.sql` for `IF NOT EXISTS` and DO-block patterns; replicate exactly).
    2. Create `supabase/migrations/114_channel_templates.sql` with content from 223-RESEARCH.md §Migration 114. Composite UNIQUE (tenant_id, name, locale, version), self-FK parent_template_id, tenant_isolation RLS policy.
    3. Create `supabase/migrations/115_channel_programs.sql` with content from 223-RESEARCH.md §Migration 115 (4 tables: email_campaigns, messaging_threads, lifecycle_journeys, lifecycle_journey_runs). FKs:
       - email_campaigns.template_id -> channel_templates.template_id (UNCONDITIONAL — Wave 1 produces channel_templates)
       - email_campaigns.sender_identity_id -> sender_identities.sender_id (UNCONDITIONAL)
       - messaging_threads.profile_id -> cdp_identity_profiles.profile_id (UNCONDITIONAL — P221 landed per D-45)
       - messaging_threads.related_crm_id -> customer_360_records.customer_360_id (UNCONDITIONAL — P222 landed per D-45)
       - lifecycle_journey_runs.journey_id -> lifecycle_journeys.journey_id
       - lifecycle_journey_runs UNIQUE (journey_id, profile_id)
       - All 4 tables: ENABLE ROW LEVEL SECURITY + CREATE POLICY tenant_isolation USING (tenant_id = current_setting('app.tenant_id')::UUID)
       - Per D-45: NO conditional ALTER blocks, NO "if exists" graceful paths. Migration assumes upstream landed (preflight gate enforces).
    4. Create `supabase/migrations/116_channel_suppressions.sql` with content from 223-RESEARCH.md §Migration 116. UNIQUE (tenant_id, profile_id, channel). RLS policy.
    5. Create `test/channels/schema/sender-identity.test.js` running Tests 1, 2, 9, 10, 11 against migration 113.
    6. Create `test/channels/schema/channel-template.test.js` running Tests 1, 4, 9, 10 against migration 114.
    7. Create `test/channels/schema/email-campaign.test.js` running Tests 1, 5, 9, 10, 11 against migration 115 (email_campaigns slice).
    8. Create `test/channels/schema/messaging-thread.test.js` running Tests 1, 6, 7, 9, 10, 11 against migration 115 (messaging_threads + lifecycle_journeys + lifecycle_journey_runs).
    9. Create `test/channels/schema/lifecycle-journey.test.js` covering lifecycle_journey_runs UNIQUE (journey_id, profile_id) — Test 7 above.
    10. Create `test/channels/schema/channel-suppressions.test.js` running Tests 1, 8, 9, 10 against migration 116.
    11. Per-task VALIDATION row in 223-VALIDATION.md: append rows for 223-01-2 (schema CRUD + RLS + idempotency).
  </action>
  <verify>
    <automated>npm test -- test/channels/schema/sender-identity.test.js test/channels/schema/channel-template.test.js test/channels/schema/email-campaign.test.js test/channels/schema/messaging-thread.test.js test/channels/schema/lifecycle-journey.test.js test/channels/schema/channel-suppressions.test.js</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/113_channel_sender_identity.sql` contains `CREATE TABLE` for `sender_identities` AND `deliverability_posture` AND `CREATE POLICY tenant_isolation`
    - File `supabase/migrations/114_channel_templates.sql` contains `CREATE TABLE channel_templates` AND `UNIQUE (tenant_id, name, locale, version)`
    - File `supabase/migrations/115_channel_programs.sql` contains `CREATE TABLE email_campaigns` AND `CREATE TABLE messaging_threads` AND `CREATE TABLE lifecycle_journeys` AND `CREATE TABLE lifecycle_journey_runs` AND `UNIQUE (journey_id, profile_id)` AND 4 occurrences of `CREATE POLICY tenant_isolation`
    - Migration 115 contains UNCONDITIONAL FK to `cdp_identity_profiles` AND `customer_360_records` (NO `IF EXISTS` guards per D-45 hard preflight)
    - File `supabase/migrations/116_channel_suppressions.sql` contains `CREATE TABLE channel_suppressions` AND `UNIQUE (tenant_id, profile_id, channel)` AND `CREATE POLICY tenant_isolation`
    - All 6 test files exist as `.test.js` (NOT `.test.ts` per D-46); `npm test -- test/channels/schema/` exits 0 with `≥30` passing assertions
    - Migration files re-runnable: running each twice does not error (idempotency test passes)
  </acceptance_criteria>
  <done>All 4 migrations land 8 tables with full RLS, idempotent re-run, CHECK + UNIQUE constraints validated. Schema gate is green for waves 2-5. No graceful absence paths (preflight enforces upstream presence per D-45).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend base-adapter.ts (3 -> 5 channels + new contract methods); CREATE NEW lib/markos/channels/* P223-OWNED greenfield module tree (per D-44); write F-122..F-127 contracts + flow-registry update; ship legacy-outbound.ts bridge; regression-prove existing P104 tests still green</name>
  <read_first>
    - lib/markos/outbound/providers/base-adapter.ts (CHANNEL_CAPABILITIES + getOutboundChannelCapabilities + sendOutboundMessage + normalizeOutboundProviderEvent — current shape)
    - lib/markos/outbound/providers/resend-adapter.ts (factory pattern reference)
    - lib/markos/outbound/providers/twilio-adapter.ts (factory pattern reference)
    - api/crm/outbound/send.js + bulk-send.js + sequences.js + templates.js (legacy callers — must keep working)
    - contracts/F-99-webhook-status-v1.yaml (most recent contract — yaml shape reference)
    - contracts/flow-registry.json (append-only update target)
    - .planning/phases/223-native-email-messaging-orchestration/223-RESEARCH.md (§Recommended Module Structure §Integration Contracts §Adapter Contract)
    - .planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md (D-08, D-33, D-40 F-ID slot table, D-44 greenfield ownership)
    - obsidian/reference/Core Lib.md (lib/markos/* conventions)
    - test/fixtures/channels/* (Task 1 outputs)
  </read_first>
  <behavior>
    - Test 1: getOutboundChannelCapabilities('email'|'sms'|'whatsapp') still returns the existing shape unchanged (legacy regression).
    - Test 2: getOutboundChannelCapabilities('push') returns { channel: 'push', provider: 'knock', supports_subject: true, supports_templates: true, supports_replies: false, supports_inbound: false }.
    - Test 3: getOutboundChannelCapabilities('in_app') returns { channel: 'in_app', provider: 'markos', supports_subject: true, supports_templates: true, supports_replies: false, supports_inbound: false }.
    - Test 4: getOutboundChannelCapabilities('invalid') still throws CRM_OUTBOUND_CHANNEL_INVALID:invalid.
    - Test 5: validateAdapterContract(adapter) (NEW export) requires methods send + normalizeEvent + verifyWebhookSignature + getRateLimitState; missing any throws CRM_OUTBOUND_ADAPTER_CONTRACT_INCOMPLETE:<missing-method>.
    - Test 6: getReputationSnapshot is OPTIONAL — adapter without it passes contract validation.
    - Test 7: each lib/markos/channels/programs/* + senders/* + templates/* stub exports the documented function names (must_haves.artifacts) returning pending stub responses (e.g., createEmailCampaign throws "NOT_IMPLEMENTED:223-02" or returns {pending: true}).
    - Test 8: legacy-outbound.ts bridge: api/crm/outbound/send.js test (existing) still returns 200 with same response shape after refactor; baseline regression suite (P104 + P105 outbound tests under `test/crm-outbound/*` or wherever existing) all green via `npm test`.
    - Test 9: contracts/flow-registry.json contains entries for F-122..F-127 with flow_id field set; entries for F-01..F-99 still present (append-only invariant).
    - Test 10 (greenfield assertion per D-44): before this task runs, `lib/markos/channels/` directory does NOT exist (verified via fs.existsSync at test setup time). After task runs, directory + 11 new files exist. Task asserts NEW creation, not modification.
  </behavior>
  <action>
    1. Modify `lib/markos/outbound/providers/base-adapter.ts`:
       - Update CHANNEL_CAPABILITIES from 3 to 5 (add `push` and `in_app` entries per `<interfaces>` block above).
       - Add `supports_inbound` field to all 5 entries.
       - Export new function `validateAdapterContract(adapter)` that asserts presence of `send`, `normalizeEvent`, `verifyWebhookSignature`, `getRateLimitState` (all 4 required) and tolerates absence of `getReputationSnapshot`. Throws `CRM_OUTBOUND_ADAPTER_CONTRACT_INCOMPLETE:<method-name>` on failure.
       - Preserve existing exports `getOutboundChannelCapabilities`, `sendOutboundMessage`, `normalizeOutboundProviderEvent` unchanged.
       - Append to module.exports: `validateAdapterContract`, `CHANNEL_CAPABILITIES` (frozen).
    2. CREATE NEW directory `lib/markos/channels/` (per D-44 — greenfield) and CREATE NEW `lib/markos/channels/index.ts` re-exporting submodule API (`programs`, `senders`, `templates`, `gate`, `dispatch`, `events`, `suppression`, `adapters`, `preflight` from Task 0.5). For now, only `programs/*`, `senders/*`, `templates/*`, `adapters/*`, and `preflight/*` exist; the rest are added in 223-02..04.
    3. CREATE NEW `lib/markos/channels/programs/email-campaign.ts` exporting:
       - `createEmailCampaign({ tenantId, campaign_type, objective, audience_snapshot_id, template_id, sender_identity_id, schedule_at, throttle_per_minute, time_zone_strategy, frequency_cap_override, requires_approval })` — DB INSERT with status='draft'. Throws if tenant context missing.
       - `getEmailCampaign(campaignId)` — SELECT with RLS.
       - `listEmailCampaigns({ status, campaign_type, page })` — SELECT with filters.
       - `setEmailCampaignStatus(campaignId, newStatus, { actorId, reason })` — UPDATE with status transition validation (draft -> pending_approval, pending_approval -> approved | rejected, approved -> scheduled, scheduled -> dispatching, etc per D-02). Logs audit row stub (full audit wiring lands 223-06).
       - `markCampaignDispatchRun(campaignId, runId)` — UPDATE dispatch_run_id (called from 223-02 dispatch coordinator).
    4. CREATE NEW `lib/markos/channels/programs/messaging-thread.ts` exporting createMessagingThread, getMessagingThread, setThreadStatus, appendThreadMessage, lookupOrCreateThreadByProfileChannel — last one used by 223-04 reply flow.
    5. CREATE NEW `lib/markos/channels/programs/lifecycle-journey.ts` exporting createLifecycleJourney, listLifecycleJourneys, setJourneyStatus, buildJourneyRun (creates lifecycle_journey_runs row), pauseJourney, resumeJourney.
    6. CREATE NEW `lib/markos/channels/senders/sender-identity.ts` exporting createSenderIdentity, listSenderIdentities, updateVerificationStatus, getReputationStatus, validateClassPermission(senderId, campaignType) (Pitfall 4 mitigation per RESEARCH).
    7. CREATE NEW `lib/markos/channels/templates/channel-template.ts` exporting createChannelTemplate, getChannelTemplate, listChannelTemplates, setTemplateStatus, resolveLocaleVariant(parentId, locale) (returns child template or parent fallback per D-28).
    8. CREATE NEW `lib/markos/channels/adapters/legacy-outbound.ts`:
       - Re-export shape-mapping helpers from existing `lib/markos/outbound/*` modules.
       - Provide `mapLegacyOutboundToCampaign(legacyPayload)` that takes the existing `api/crm/outbound/send.js` request body and returns the equivalent `email_campaigns` insert input (single-row audience_snapshot per D-05).
       - Provide `mapLegacyConversationToThread(legacyConversation)` reading `lib/markos/outbound/conversations.ts` shape and returning a `messaging_threads` row.
       - Document at top of file: "P223-owned greenfield (D-44). Legacy adapter for D-33 transition. To be retired post-P223 once api/crm/outbound/* is rewritten to call /v1/channels/* directly."
    9. Create 6 contract YAML files in `contracts/` following the format of `contracts/F-99-webhook-status-v1.yaml`:
       - `F-122-channel-email-campaign-v1.yaml` with `flow_id: F-122`, `flow_name: channel-email-campaign-v1`, paths: GET /v1/channels/email/programs, POST /v1/channels/email/programs, GET /v1/channels/email/programs/{id}, PATCH /v1/channels/email/programs/{id}, DELETE /v1/channels/email/programs/{id}. `approval_policy.high_risk_fields: [campaign_type, audience_snapshot_id, sender_identity_id]`. Pricing placeholder note in comments: `# {{MARKOS_PRICING_ENGINE_PENDING}} resolves via D-27/D-52 content classifier (AST/allowlist)`.
       - `F-123-channel-messaging-thread-v1.yaml` paths: GET /v1/channels/messaging/threads, POST /v1/channels/messaging/threads, GET /v1/channels/messaging/threads/{id}, POST /v1/channels/messaging/threads/{id}/escalate.
       - `F-124-channel-lifecycle-journey-v1.yaml` paths: GET /v1/channels/lifecycle/journeys, POST /v1/channels/lifecycle/journeys, POST /v1/channels/lifecycle/journeys/{id}/pause, POST /v1/channels/lifecycle/journeys/{id}/resume.
       - `F-125-channel-sender-identity-v1.yaml` paths: GET /v1/channels/email/senders, POST /v1/channels/email/senders, PATCH /v1/channels/email/senders/{id}/verification.
       - `F-126-channel-deliverability-posture-v1.yaml` paths: GET /v1/channels/email/deliverability (read-only).
       - `F-127-channel-template-v1.yaml` paths: GET /v1/channels/templates, POST /v1/channels/templates, POST /v1/channels/templates/{id}/preview, PATCH /v1/channels/templates/{id}/status. `approval_policy.high_risk_fields: [pricing_bindings, content_blocks, evidence_bindings]`.
    10. Update `contracts/flow-registry.json`: append 6 entries for F-122..F-127 (preserve existing entries; add new entries after the highest existing F-ID; bump `total_flows` count).
    11. Create `test/channels/providers/base-adapter.test.js` running Tests 1-6 above.
    12. Create `test/channels/adapters/legacy-outbound.test.js` running Test 8 (legacy regression — load fixtures from existing P104 outbound tests OR reuse current `test/crm-outbound/*` test harness if present).
    13. Append per-task rows to `.planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md` §Per-Task Verification Map.
  </action>
  <verify>
    <automated>npm test -- test/channels/providers/base-adapter.test.js test/channels/adapters/legacy-outbound.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - File `lib/markos/outbound/providers/base-adapter.ts` CHANNEL_CAPABILITIES contains 5 keys matching `email|sms|whatsapp|push|in_app` (grep `provider: 'knock'` AND `provider: 'markos'`)
    - File `lib/markos/outbound/providers/base-adapter.ts` exports `validateAdapterContract` (grep `validateAdapterContract` in module.exports)
    - All 5 stubs CREATED NEW under `lib/markos/channels/` — `grep -l "exports.createEmailCampaign\|export.*createEmailCampaign\|module.exports.*createEmailCampaign" lib/markos/channels/programs/email-campaign.ts` returns the file
    - 6 contract files `contracts/F-122-*.yaml` through `contracts/F-127-*.yaml` exist; each contains `flow_id: F-1XX` matching its number
    - `contracts/flow-registry.json` valid JSON; jq '.flows[].flow_id' includes F-122..F-127
    - `npm test -- test/channels/providers/base-adapter.test.js` exits 0
    - `npm test -- test/channels/adapters/legacy-outbound.test.js` exits 0 (legacy regression)
    - `npm test` (existing P100-P105 + outbound regression suite) exits 0
    - File `lib/markos/channels/adapters/legacy-outbound.ts` documented as "P223-owned greenfield (D-44). Legacy adapter for D-33 transition" (grep "P223-owned greenfield")
    - All test files end with `.test.js` (D-46)
  </acceptance_criteria>
  <done>5 channels in adapter contract, P223-owned greenfield module tree CREATED NEW (per D-44), 6 F-IDs registered, legacy-outbound bridge keeps existing CRM outbound green, all tests pass via `npm test`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| writer -> migrations | Schema mutations require Supabase migration runner; manual DDL bypass blocked by RLS scaffold |
| api/crm/outbound -> legacy-outbound.ts | Legacy CRM callers traverse adapter; raw CRM outbound payload becomes channel program shape |
| 223-*-PLAN.md authors -> architecture-lock test | Plan files cannot regress forbidden patterns; CI gate via Task 0.5 architecture-lock test |
| upstream phases (P205..P222) -> Phase 223 boot | Hard preflight gate per D-45; no soft-skip surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-223-01-01 | Tampering | sender_identities row replay across tenants | mitigate | RLS tenant_isolation policy + UNIQUE (tenant_id, from_email) reject duplicate registrations; tested in Task 2 |
| T-223-01-02 | Information Disclosure | email_campaigns leaks across tenants | mitigate | RLS tenant_isolation USING (tenant_id = current_setting('app.tenant_id')::UUID); cross-tenant SELECT test in Task 2 |
| T-223-01-03 | Spoofing | adapter contract bypass (skipping verifyWebhookSignature) | mitigate | validateAdapterContract throws CRM_OUTBOUND_ADAPTER_CONTRACT_INCOMPLETE if methods missing; gates all adapter use sites in 223-04 |
| T-223-01-04 | Elevation | rogue migration insertion (e.g., 113-2.sql injecting non-RLS table) | accept | append-only migration filename convention + git review process out of band |
| T-223-01-05 | Repudiation | sender verification status change without audit | mitigate | Task 3 sender-identity.ts updateVerificationStatus stub queues audit_log row (real wiring 223-06) |
| T-223-01-06 | Denial of Service | unbounded email_campaigns insertion exhausts disk | accept | tenant quota enforcement deferred to Pricing Engine (P205); storage-tier alerts cover infra |
| T-223-01-07 | Tampering | plan-file regression of forbidden patterns (e.g., createApprovalPackage reappears) | mitigate | Task 0.5 architecture-lock test scans every 223-*-PLAN.md and fails if any forbidden pattern detected |
| T-223-01-08 | Spoofing | execute-phase boot without P207/P221/P222 landed -> dispatches without AgentRun audit | mitigate | Task 0.5 assertUpstreamReady CLI gates execute-phase boot per D-45; no bridge stub |
</threat_model>

<verification>
- All 4 migrations created and idempotent (re-running each migration twice causes no error).
- All 8 new tables have RLS policy `tenant_isolation` enabled.
- `lib/markos/outbound/providers/base-adapter.ts` CHANNEL_CAPABILITIES has 5 entries (email/sms/whatsapp/push/in_app).
- All 11 stub modules CREATED NEW under `lib/markos/channels/*` with the documented function exports (D-44 greenfield).
- All 6 F-122..F-127 contract YAMLs exist; flow-registry.json updated append-only.
- Architecture-lock test (Task 0.5) passes — zero forbidden-pattern matches across 223-*-PLAN.md files.
- Helper-presence test (Task 0.5) passes — buildApprovalPackage exists at agent-actions.ts:68; createApprovalPackage does NOT exist.
- Upstream-gate test (Task 0.5) passes — assertUpstreamReady throws when sentinels absent, returns void when present.
- `npm test -- test/channels/schema/` + `test/channels/providers/` + `test/channels/adapters/` + `test/channels/preflight/` all green.
- `npm test` (legacy P100-P105 outbound regression) green.
- `.planning/phases/223-native-email-messaging-orchestration/223-VALIDATION.md` per-task rows populated for 223-01-0.5, 223-01-1, 223-01-2, 223-01-3.
</verification>

<success_criteria>
- 4 migration files exist and idempotent.
- 4 preflight files exist (3 lib + 1 CLI script per Task 0.5).
- 11 module stub files CREATED NEW (per D-44 greenfield).
- 6 F-IDs registered (F-122..F-127).
- Legacy regression green via `npm test`.
- VALIDATION.md per-task rows added.
- F-ID + migration baseline disjoint (A1/A2 verified).
- Architecture-lock + upstream-gate operational (D-42/D-45 enforcement).
</success_criteria>

<output>
After completion, create `.planning/phases/223-native-email-messaging-orchestration/223-01-SUMMARY.md` capturing:
- Architecture-lock + upstream preflight gate operational (Task 0.5; D-42/D-45)
- Migrations 113-116 schema landed (8 tables; full DDL hash for re-verification)
- F-ID range F-122..F-127 reserved + flow-registry.json updated
- base-adapter.ts now serves 5 channels; validateAdapterContract added (D-08)
- lib/markos/channels/* P223-OWNED greenfield tree CREATED NEW (D-44) for Wave 2-4
- Legacy-outbound bridge kept api/crm/outbound/* green
- @babel/parser + handlebars + @knocklabs/node added to package.json
- Hand-off to 223-02: schema + adapter + fixture + preflight gates open
</output>
