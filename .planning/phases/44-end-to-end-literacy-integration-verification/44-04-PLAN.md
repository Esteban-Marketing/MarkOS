---
phase: 44-end-to-end-literacy-integration-verification
plan: 04
type: execute
wave: 3
depends_on:
  - 44-03
files_modified:
  - test/literacy-e2e.test.js
  - .github/workflows/ui-quality.yml
autonomous: true
requirements:
  - LIT-18

must_haves:
  truths:
    - "CI fails when fixture corpus is populated but literacy retrieval returns zero hits."
    - "Regression failures include actionable diagnostics describing the zero-hit condition."
    - "Literacy E2E verification is part of repository CI execution path, not manual-only."
  artifacts:
    - path: "test/literacy-e2e.test.js"
      provides: "explicit zero-hit regression guard tests and failure messages"
      exports: []
    - path: ".github/workflows/ui-quality.yml"
      provides: "CI integration for literacy E2E regression command"
      exports: []
  key_links:
    - from: "test/literacy-e2e.test.js"
      to: "test/fixtures/literacy/"
      via: "populated-corpus guard branch"
      pattern: "zero hits|populated corpus"
    - from: ".github/workflows/ui-quality.yml"
      to: "test/literacy-e2e.test.js"
      via: "CI command execution"
      pattern: "node --test test/literacy-e2e.test.js -x"
---

<objective>
Implement Wave 3 regression gate and CI enforcement for literacy retrieval.

Purpose: Prevent silent regressions where populated literacy corpora produce empty standards context.
Output: Zero-hit guard tests wired into CI with clear diagnostic failures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-end-to-end-literacy-integration-verification/44-RESEARCH.md
@.planning/phases/44-end-to-end-literacy-integration-verification/44-VALIDATION.md
@test/literacy-e2e.test.js
@.github/workflows/ui-quality.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 44-04-01: Add populated-corpus zero-hit regression assertion (per LIT-18)</name>
  <files>test/literacy-e2e.test.js</files>
  <action>
Implement a strict guard test branch that fails whenever fixtures are populated but retrieval produces zero literacy hits or empty standards context. Ensure assertion text explicitly identifies this as a regression condition.
  </action>
  <verify>
    <automated>node --test test/literacy-e2e.test.js -x</automated>
  </verify>
  <done>
    Regression gate test fails deterministically under zero-hit conditions with populated fixtures.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 44-04-02: Integrate literacy E2E regression command into CI path (per LIT-18)</name>
  <files>.github/workflows/ui-quality.yml</files>
  <action>
Add a CI step in existing workflow execution path that runs `node --test test/literacy-e2e.test.js -x` before completion, so zero-hit regressions block merges.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
    CI now executes literacy E2E guard command as a merge-blocking gate.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 44-04-03: Add failure diagnostics contract for retrieval regressions (per LIT-18)</name>
  <files>test/literacy-e2e.test.js</files>
  <action>
Refine regression assertions/messages so CI output reports missing disciplines/hits and points to fixture corpus expectation, enabling quick triage when gates fail.
  </action>
  <verify>
    <automated>node --test test/literacy-e2e.test.js -x</automated>
  </verify>
  <done>
    Failure messaging is explicit enough to diagnose zero-hit regressions without additional debugging passes.
  </done>
</task>

</tasks>

<verification>
Run:
- `node --test test/literacy-e2e.test.js -x`
- `npm test`
</verification>

<success_criteria>
Wave 3 enforces automated regression blocking for empty retrieval outcomes under populated corpus conditions.
</success_criteria>

<output>
After completion, create `.planning/phases/44-end-to-end-literacy-integration-verification/44-04-SUMMARY.md`
</output>
