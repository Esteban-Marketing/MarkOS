---
phase: 207
plan: 207-01
title: AgentRun v2 Contracts, Schema, F-ID Allocation, Migration Allocation, CONTRACT-LOCK
wave: 1
autonomous: false
solo: true
depends_on: []
requirements_addressed: [RUN-01, RUN-02, RUN-07, RUN-08, QA-01, QA-02, QA-03, QA-04, QA-05, QA-06, QA-07, QA-08, QA-09, QA-10, QA-11, QA-12, QA-13, QA-14, QA-15]
files_modified:
  - lib/markos/contracts/agent-run-v2.ts
  - test/contracts/agent-run-v2.test.js
  - contracts/F-106-agent-run-v2.yaml
  - contracts/F-107-agent-chain-v2.yaml
  - contracts/F-108-agent-retry-dlq-v2.yaml
  - contracts/F-109-agent-approval-task-v2.yaml
  - contracts/F-110-agent-cost-v2.yaml
  - contracts/F-111-agent-run-adoption-v2.yaml
  - supabase/migrations/101_markos_agent_run_v2_extend.sql
  - supabase/migrations/102_markos_agent_chain.sql
  - supabase/migrations/103_markos_agent_failure_dlq.sql
  - supabase/migrations/104_markos_agent_task_link.sql
  - supabase/migrations/105_markos_agent_run_cost.sql
  - supabase/migrations/106_markos_agent_run_adoption.sql
  - supabase/migrations/rollback/101_markos_agent_run_v2_extend.down.sql
  - supabase/migrations/rollback/102_markos_agent_chain.down.sql
  - supabase/migrations/rollback/103_markos_agent_failure_dlq.down.sql
  - supabase/migrations/rollback/104_markos_agent_task_link.down.sql
  - supabase/migrations/rollback/105_markos_agent_run_cost.down.sql
  - supabase/migrations/rollback/106_markos_agent_run_adoption.down.sql
  - .planning/phases/207-agentrun-v2-orchestration-substrate/207-01-CONTRACT-LOCK.md
  - obsidian/reference/Contracts Registry.md
---

# Plan 207-01 — AgentRun v2 Contracts, Schema, and ID Allocation (HARD GATE)

## Objective

Lock the AgentRun v2 contract surface, Zod schemas, F-ID range (F-106..F-111), and migration range (101-106), and emit the CONTRACT-LOCK artifact that plans 207-02..06 must consume verbatim. No plan in 207 may proceed until this plan ships green.

## Current code path this migrates

Extends (does not replace):
- `supabase/migrations/53_agent_run_lifecycle.sql` (markos_agent_runs, _events, _side_effects — keeps v1 tables, adds v2 columns via `ALTER TABLE`)
- `onboarding/backend/agents/run-engine.cjs` (stays; becomes a shim that re-exports v2 in Plan 207-06)
- `lib/markos/crm/playbooks.ts` (untouched in this plan; adapter ships in 207-06)

## must_haves (goal-backward)

- `lib/markos/contracts/agent-run-v2.ts` exports Zod schemas for every object in CONTRACT-LOCK §4.
- All 6 migrations (101-106) exist as stub files with a single `TODO(207-<NN>)` marker and valid skeleton SQL that parses without error.
- CONTRACT-LOCK.md is present with `status: locked` and references each F-ID + migration exactly once.
- Contracts Registry entry appended (allocation block dated 2026-04-23).
- Test `test/contracts/agent-run-v2.test.js` passes: parses sample AgentRun/AgentRunEvent/AgentChain/AgentChainEdge/AgentSideEffect/AgentFailure payloads.
- Every 207-02..06 plan frontmatter sets `depends_on: [207-01]`.

## Tasks

<task id="1" name="Lock CONTRACT-LOCK artifact and publish to registry">
  <read_first>
    - .planning/phases/207-agentrun-v2-orchestration-substrate/207-01-CONTRACT-LOCK.md
    - .planning/phases/207-agentrun-v2-orchestration-substrate/207-CONTEXT.md
    - .planning/phases/207-agentrun-v2-orchestration-substrate/207-RESEARCH.md (§Proposed contract direction)
    - obsidian/reference/Contracts Registry.md (§v2 Contract Gap Overlay — confirm F-106..F-111 not reused)
    - .planning/ROADMAP.md (Phase 207 section — depends_on; downstream consumers 208+)
  </read_first>
  <action>
    Verify `.planning/phases/207-agentrun-v2-orchestration-substrate/207-01-CONTRACT-LOCK.md` exists and contains sections 1-13. Leave its content as-authored by planner.

    Append a new block to the END of `obsidian/reference/Contracts Registry.md` under `## v2 Contract Gap Overlay` with exact text:

    ### AgentRun v2 (F-106..F-111) — allocated 2026-04-23

    | Code | Name | Purpose |
    |---|---|---|
    | F-106 | agent-run-v2 | AgentRun + AgentRunEvent + state machine |
    | F-107 | agent-chain-v2 | AgentChain DAG + edges |
    | F-108 | agent-retry-dlq-v2 | AgentFailure + retry + DLQ |
    | F-109 | agent-approval-task-v2 | ApprovalGate + Task handoff envelope |
    | F-110 | agent-cost-v2 | Cost estimate/actual + billing emit |
    | F-111 | agent-run-adoption-v2 | Adoption adapters + agent readiness registry |
  </action>
  <acceptance_criteria>
    - `ls .planning/phases/207-agentrun-v2-orchestration-substrate/207-01-CONTRACT-LOCK.md` exits 0.
    - `grep -c "F-106\|F-107\|F-108\|F-109\|F-110\|F-111" .planning/phases/207-agentrun-v2-orchestration-substrate/207-01-CONTRACT-LOCK.md` ≥ 6.
    - `grep -c "101_markos_agent_run_v2_extend\|102_markos_agent_chain\|103_markos_agent_failure_dlq\|104_markos_agent_task_link\|105_markos_agent_run_cost\|106_markos_agent_run_adoption" .planning/phases/207-agentrun-v2-orchestration-substrate/207-01-CONTRACT-LOCK.md` ≥ 6.
    - `grep -c "AgentRun v2 (F-106..F-111) — allocated 2026-04-23" "obsidian/reference/Contracts Registry.md"` == 1.
    - `grep -rn "F-106\|F-107\|F-108\|F-109\|F-110\|F-111" contracts/` returns no existing file matches (no collisions) prior to Task 5 execution.
  </acceptance_criteria>
</task>

<task id="2" name="Write lib/markos/contracts/agent-run-v2.ts (Zod contracts)">
  <read_first>
    - .planning/phases/207-agentrun-v2-orchestration-substrate/207-01-CONTRACT-LOCK.md (§4 Zod schemas, §6 RETRY_POLICY)
    - lib/markos/contracts/ (existing TS contracts pattern)
    - onboarding/backend/agents/run-engine.cjs (v1 RUN_STATES + ALLOWED_TRANSITIONS — preserve backward compat)
    - supabase/migrations/53_agent_run_lifecycle.sql (v1 tables/columns)
  </read_first>
  <action>
    Create `lib/markos/contracts/agent-run-v2.ts` with the exact schemas from CONTRACT-LOCK §4 (AgentRunStateV2, AgentRunPriority, AgentRunTriggerKind, AgentRun, AgentRunEventType, AgentRunEvent, AgentChainStrategy, AgentChain, AgentChainEdge, AgentSideEffect, AgentFailureClass, AgentFailure) plus these additional named exports:

    - `export const RETRY_POLICY = Object.freeze({ MAX_RETRIES_DEFAULT: 5, BACKOFF_BASE_MS: 2000, BACKOFF_CAP_MS: 600000, BACKOFF_JITTER_FRACTION: 0.2, DLQ_AFTER_POISON_HITS: 1, DLQ_AFTER_QUOTA_EXHAUSTION_HITS: 10, TIMEOUT_MS: Object.freeze({ simple: 30000, llm: 120000, long: 300000 }) });`
    - `export const PRIORITY_CONCURRENCY_DEFAULTS = Object.freeze({ P0: { tenant_max_concurrent: 1, emergency: true, starvation_floor_seconds: 0 }, P1: { tenant_max_concurrent: 2, emergency: false, starvation_floor_seconds: 30 }, P2: { tenant_max_concurrent: 5, emergency: false, starvation_floor_seconds: 120 }, P3: { tenant_max_concurrent: 10, emergency: false, starvation_floor_seconds: 600 }, P4: { tenant_max_concurrent: 20, emergency: false, starvation_floor_seconds: 3600 } });`
    - `export const ALLOWED_TRANSITIONS_V2` mapping with the 15 states listed in CONTRACT-LOCK §5 (preserving v1 compat — `requested → accepted` AND `requested → queued` both allowed).
    - `export function isTransitionAllowedV2(from, to): boolean` returning `(ALLOWED_TRANSITIONS_V2[from] || []).includes(to)`.

    File must be TypeScript (.ts), use `import { z } from 'zod'`, and export all Zod schemas AND their inferred TypeScript types (`export type AgentRun = z.infer<typeof AgentRun>` etc).
  </action>
  <acceptance_criteria>
    - `ls lib/markos/contracts/agent-run-v2.ts` exits 0.
    - `grep -c "export const AgentRun =\|export const AgentRunEvent =\|export const AgentChain =\|export const AgentChainEdge =\|export const AgentSideEffect =\|export const AgentFailure =" lib/markos/contracts/agent-run-v2.ts` == 6.
    - `grep -c "export const AgentRunStateV2\|export const AgentRunPriority\|export const AgentRunTriggerKind\|export const AgentRunEventType\|export const AgentChainStrategy\|export const AgentFailureClass" lib/markos/contracts/agent-run-v2.ts` == 6.
    - `grep -c "export const RETRY_POLICY\|export const PRIORITY_CONCURRENCY_DEFAULTS\|export const ALLOWED_TRANSITIONS_V2\|export function isTransitionAllowedV2" lib/markos/contracts/agent-run-v2.ts` == 4.
    - `grep -c "'queued'\|'paused'\|'retry_wait'\|'canceled'\|'dlq'" lib/markos/contracts/agent-run-v2.ts` ≥ 5 (v2 net-new states present).
    - `npx tsc --noEmit --esModuleInterop --target es2020 --moduleResolution node lib/markos/contracts/agent-run-v2.ts` exits 0.
  </acceptance_criteria>
</task>

<task id="3" name="Write contract tests test/contracts/agent-run-v2.test.js">
  <read_first>
    - lib/markos/contracts/agent-run-v2.ts (just authored in Task 2)
    - .planning/V4.0.0-TESTING-ENVIRONMENT-PLAN.md (Vitest-for-business-logic doctrine)
    - test/contracts/ (existing pattern if present)
  </read_first>
  <action>
    Create `test/contracts/agent-run-v2.test.js` as a node:test file (or `test/contracts/agent-run-v2.test.ts` for Vitest if the project already uses Vitest for contract tests — follow the existing convention in `test/contracts/`). The suite MUST include at minimum these 10 tests:

    1. `AgentRun.parse` succeeds on fixture with all required fields populated and null-optional fields set to null.
    2. `AgentRun.parse` throws on `state: 'frobozz'`.
    3. `AgentRunStateV2.options.length === 15`.
    4. `isTransitionAllowedV2('executing', 'retry_wait') === true`.
    5. `isTransitionAllowedV2('archived', 'executing') === false`.
    6. `isTransitionAllowedV2('requested', 'queued') === true` AND `isTransitionAllowedV2('requested', 'accepted') === true` (v1 compat preserved).
    7. `AgentChainEdge.parse({ ...fixture, dependency_type: 'all_of', fan_in_requirement: 3 })` succeeds.
    8. `AgentSideEffect.parse` throws when `effect_hash` is missing.
    9. `AgentFailureClass.options.length === 7` AND `AgentFailure.parse` throws on `failure_class: 'bogus'`.
    10. `RETRY_POLICY.MAX_RETRIES_DEFAULT === 5` AND `PRIORITY_CONCURRENCY_DEFAULTS.P0.tenant_max_concurrent === 1`.
  </action>
  <acceptance_criteria>
    - `ls test/contracts/agent-run-v2.test.*` resolves exactly 1 file.
    - Running tests (`node --test test/contracts/agent-run-v2.test.js` OR `npx vitest run test/contracts/agent-run-v2.test.ts`) exits 0.
    - `grep -c "^test(\|^it(\|test('.*',\|it('.*'," test/contracts/agent-run-v2.test.*` ≥ 10 (10+ test cases).
  </acceptance_criteria>
</task>

<task id="4" name="Write 6 migration stub files (101-106) + rollbacks">
  <read_first>
    - supabase/migrations/53_agent_run_lifecycle.sql (pattern for RLS policies, FK to markos_tenants)
    - supabase/migrations/72_markos_webhook_dlq_and_rotation.sql (DLQ table pattern)
    - .planning/phases/207-agentrun-v2-orchestration-substrate/207-01-CONTRACT-LOCK.md (§3)
  </read_first>
  <action>
    Create 6 migration files + 6 rollbacks. Every stub MUST:
    - Start with a comment line: `-- Phase 207 Plan 01: migration <N> stub`
    - Contain exactly one `-- TODO(207-<NN>): complete in 207-<NN>-PLAN (<summary>)` line
    - End with `-- end stub`
    - Contain NO `DROP TABLE`, `TRUNCATE`, or `DELETE FROM` statements
    - Use `create table if not exists` / `alter table ... add column if not exists` only

    Write the skeleton SQL for each migration exactly as specified in CONTRACT-LOCK §3 and 207-01-PLAN execution evidence below:

    **`supabase/migrations/101_markos_agent_run_v2_extend.sql`** — adds `priority, chain_id, parent_run_id, agent_id, agent_registry_version, trigger_kind, source_surface, approval_policy` columns to `markos_agent_runs`. TODO(207-02) marker.

    **`supabase/migrations/102_markos_agent_chain.sql`** — creates `markos_agent_chains` (chain_id PK, tenant_id FK markos_tenants, strategy check IN ('sequential','parallel_fan_out','conditional','dag'), state check IN ('pending','running','paused','completed','failed','canceled')) and `markos_agent_chain_edges` (edge_id, chain_id FK, from_run_id, to_run_id, dependency_type check IN ('sequential','all_of','any_of','conditional'), fan_in_requirement int default 1, failure_policy check IN ('abort_chain','skip_downstream','retry_branch','continue')). TODO(207-03) marker.

    **`supabase/migrations/103_markos_agent_failure_dlq.sql`** — creates `markos_agent_failures` (failure_id, run_id FK, tenant_id FK, failure_class check IN ('transient','policy','user_input','quota','upstream','poison','cancel'), error_code, error_message, retry_count int default 0, retry_after timestamptz, dlq_reason, recovery_task_id). Adds `retry_count, retry_after, last_error_code` columns to `markos_agent_runs`. TODO(207-03) marker.

    **`supabase/migrations/104_markos_agent_task_link.sql`** — creates `markos_agent_tasks` (task_id PK, tenant_id FK, run_id nullable FK, chain_id nullable FK, task_kind check IN ('approval','recovery','follow_up','manual_input'), status check IN ('open','approved','rejected','resolved','expired'), assignee_id, payload jsonb). Adds `task_id` column to `markos_agent_runs`. TODO(207-04) marker.

    **`supabase/migrations/105_markos_agent_run_cost.sql`** — adds `estimated_cost_usd_micro bigint default 0, actual_cost_usd_micro bigint default 0, cost_currency text default 'USD', pricing_engine_context jsonb default '{}'::jsonb, model text, tokens_input int default 0, tokens_output int default 0` to `markos_agent_runs`. TODO(207-05) marker.

    **`supabase/migrations/106_markos_agent_run_adoption.sql`** — creates `markos_agent_registry` (agent_id PK, status check IN ('planned','experimental','ga','retired'), current_version, approval_posture check IN ('always','risk_gated','autonomous'), cost_visibility check IN ('full','estimate_only','hidden'), test_gates_passed jsonb default '[]', notes, created_at, updated_at). TODO(207-06) marker.

    Each rollback at `supabase/migrations/rollback/<N>_*.down.sql` contains inverse `drop table if exists <name> cascade;` or `alter table ... drop column if exists ...;` in reverse order. Include `-- Phase 207 Plan 01: rollback <N>` header.
  </action>
  <acceptance_criteria>
    - `ls supabase/migrations/10{1,2,3,4,5,6}_markos_agent*.sql 2>/dev/null | wc -l` == 6
    - `ls supabase/migrations/rollback/10{1,2,3,4,5,6}_markos_agent*.down.sql 2>/dev/null | wc -l` == 6
    - `grep -c "TODO(207-" supabase/migrations/10{1,2,3,4,5,6}_*.sql` reports exactly 1 per file (total 6).
    - `grep -c "end stub" supabase/migrations/10{1,2,3,4,5,6}_*.sql` reports exactly 1 per file.
    - `grep -rnE "drop table|truncate|delete from" supabase/migrations/10{1,2,3,4,5,6}_*.sql` returns 0 matches.
    - `grep -c "create table if not exists markos_agent_chains" supabase/migrations/102_markos_agent_chain.sql` == 1.
    - `grep -c "create table if not exists markos_agent_chain_edges" supabase/migrations/102_markos_agent_chain.sql` == 1.
    - `grep -c "create table if not exists markos_agent_failures" supabase/migrations/103_markos_agent_failure_dlq.sql` == 1.
    - `grep -c "create table if not exists markos_agent_tasks" supabase/migrations/104_markos_agent_task_link.sql` == 1.
    - `grep -c "create table if not exists markos_agent_registry" supabase/migrations/106_markos_agent_run_adoption.sql` == 1.
    - `grep -c "pricing_engine_context" supabase/migrations/105_markos_agent_run_cost.sql` == 1.
    - `grep -c "approval_policy" supabase/migrations/101_markos_agent_run_v2_extend.sql` == 1.
  </acceptance_criteria>
</task>

<task id="5" name="Scaffold 6 F-NN OpenAPI contract stubs (F-106..F-111)">
  <read_first>
    - contracts/F-100-webhook-breaker-v1.yaml (declarative-only stub pattern)
    - contracts/F-71-mcp-session-v2.yaml (v2 pattern)
    - contracts/schema.json (x-markos-meta block validator)
    - .planning/phases/207-agentrun-v2-orchestration-substrate/207-01-CONTRACT-LOCK.md (§2 F-ID allocation)
  </read_first>
  <action>
    Create 6 OpenAPI 3.0.3 stub files. Each file has:
    - `openapi: 3.0.3`
    - `info: { title: "<F-NN name> (AgentRun v2) — <short>", version: "0.1.0" }`
    - `x-markos-meta: { actor: "Tenant operator / Agent runtime", auth_local: none, auth_hosted: "supabase bearer", handler: "TBD", local_path: "TBD", hosted_path: "TBD", slo_tier: critical, method: "POST" }`
    - `x-markos-phase: 207-01-PLAN` (top-level marker so downstream plans detect stub)
    - `paths: {}` (empty — filled by owning downstream plan)
    - `components.schemas` with the JSON-Schema mirror of the Zod shape for that domain (AgentRun, AgentRunEvent for F-106; AgentChain + AgentChainEdge + AgentChainStrategy for F-107; AgentFailure + RetryPolicyEnvelope + DlqEnvelope for F-108; AgentApprovalGate + AgentTask + task_kind enum for F-109; AgentRunCost + CostBillingEvent + PricingEngineContext for F-110; AgentRegistry + ReadinessChecklist for F-111).

    Each file MUST contain literally:
    - `title:` line containing "AgentRun v2"
    - `x-markos-phase: 207-01-PLAN`
    - At least one `components:` with `schemas:` block
    - At least 1 schema whose name matches the domain object
  </action>
  <acceptance_criteria>
    - `ls contracts/F-10{6,7,8,9}-*.yaml contracts/F-11{0,1}-*.yaml 2>/dev/null | wc -l` == 6.
    - `grep -c "x-markos-phase: 207-01-PLAN" contracts/F-{106,107,108,109,110,111}-*.yaml` reports 6 total (1 per file).
    - `grep -c "openapi: 3.0.3" contracts/F-{106,107,108,109,110,111}-*.yaml` reports 6.
    - `grep -l "AgentRun\b\|AgentChain\b\|AgentFailure\b\|AgentTask\b\|AgentRegistry\b\|AgentRunCost\b" contracts/F-{106,107,108,109,110,111}-*.yaml | wc -l` == 6.
    - If `bin/validate-flow-contracts.cjs` exists: `node bin/validate-flow-contracts.cjs contracts/F-106-agent-run-v2.yaml contracts/F-107-agent-chain-v2.yaml contracts/F-108-agent-retry-dlq-v2.yaml contracts/F-109-agent-approval-task-v2.yaml contracts/F-110-agent-cost-v2.yaml contracts/F-111-agent-run-adoption-v2.yaml` exits 0. If not, skip (declarative-only is acceptable).
  </acceptance_criteria>
</task>

<task id="6" name="Flip CONTRACT-LOCK status to locked and add verification table">
  <read_first>
    - .planning/phases/207-agentrun-v2-orchestration-substrate/207-01-CONTRACT-LOCK.md
    - lib/markos/contracts/agent-run-v2.ts (Task 2 output)
    - contracts/F-106..F-111 (Task 5 output)
    - supabase/migrations/101..106_*.sql (Task 4 output)
  </read_first>
  <action>
    Open `.planning/phases/207-agentrun-v2-orchestration-substrate/207-01-CONTRACT-LOCK.md`. Change the frontmatter field `status: draft` to `status: locked`. Do NOT remove any other frontmatter fields.

    Append to the end of the file a new section titled `## 14. Lockdown verification (207-01 Task 6)` with a 14-row markdown table listing every artifact from Tasks 2-5 (Zod contracts, contract tests, 6 migrations, 6 F-NN contracts) with a `YES` verified column and a single-sentence lock-date confirmation: `Lock date: 2026-04-23. Downstream plans 207-02..06 may now proceed.`
  </action>
  <acceptance_criteria>
    - `grep -c "^status: locked" .planning/phases/207-agentrun-v2-orchestration-substrate/207-01-CONTRACT-LOCK.md` == 1.
    - `grep -c "^status: draft" .planning/phases/207-agentrun-v2-orchestration-substrate/207-01-CONTRACT-LOCK.md` == 0.
    - `grep -c "## 14. Lockdown verification" .planning/phases/207-agentrun-v2-orchestration-substrate/207-01-CONTRACT-LOCK.md` == 1.
    - `grep -c "Lock date: 2026-04-23" .planning/phases/207-agentrun-v2-orchestration-substrate/207-01-CONTRACT-LOCK.md` == 1.
    - Re-running `node --test test/contracts/agent-run-v2.test.js` (or vitest) exits 0.
  </acceptance_criteria>
</task>

## Verification criteria

- [ ] `lib/markos/contracts/agent-run-v2.ts` exports 6 object Zod schemas + 6 enum schemas + 4 const/helper exports.
- [ ] Contract tests green.
- [ ] All 6 migrations + 6 rollbacks exist with `TODO(207-<NN>)` markers and no destructive SQL.
- [ ] All 6 F-NN YAMLs exist with `x-markos-phase: 207-01-PLAN` placeholder.
- [ ] CONTRACT-LOCK.md `status: locked` and contains section 14 verification table.
- [ ] Contracts Registry has "AgentRun v2 (F-106..F-111) — allocated 2026-04-23" block.
- [ ] `grep -rn "F-106\|F-107\|F-108\|F-109\|F-110\|F-111" contracts/` lists only the 6 207-owned files.

## Hard-gate contract for downstream 207-02..06 plans

Every downstream 207 plan's Task 1 MUST include in `<read_first>`:
```
- .planning/phases/207-agentrun-v2-orchestration-substrate/207-01-CONTRACT-LOCK.md
```
If CONTRACT-LOCK.md `status` is not `locked`, downstream executor MUST abort and re-invoke 207-01.
