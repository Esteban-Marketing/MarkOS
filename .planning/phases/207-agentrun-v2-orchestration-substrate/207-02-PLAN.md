---
phase: 207
plan: "02"
type: execute
wave: 2
depends_on:
  - "207-01"
f_ids:
  - "F-106"
migration_slots:
  - "101"
integrates_with:
  - "CLI-01 from P204 (run, status, and watch surfaces must consume one durable run and event API)"
  - "TASK-01..05 from P208 (operator surfaces should read run state and handoff readiness from this API instead of inventing a parallel store)"
  - "EVD-01..06 from P209 (evidence lineage later depends on stable run and event identifiers)"
translation_gate_for: []
files_modified:
  - .planning/orchestration/durable-run-api-contract.md
  - supabase/migrations/101_markos_agent_run_v2_extend.sql
  - supabase/migrations/rollback/101_markos_agent_run_v2_extend.down.sql
  - contracts/F-106-agent-run-v2.yaml
  - lib/markos/agents/run-api.ts
  - lib/markos/agents/run-events.ts
  - lib/markos/agents/run-store.ts
  - api/tenant/runs/index.js
  - api/tenant/runs/[run_id]/index.js
  - api/tenant/runs/[run_id]/events.js
  - test/agentrun/phase-207/domain-2/durable-run-api.test.js
  - test/agentrun/phase-207/domain-2/run-event-stream.test.js
  - test/migrations/101_markos_agent_run_v2_extend.test.js
autonomous: true
requirements:
  - RUN-01
  - RUN-07
  - RUN-08
  - QA-04
  - QA-05
  - QA-06
must_haves:
  truths:
    - "Every run-producing surface writes to one durable API instead of local or in-memory helpers."
    - "The event stream is the canonical truth source for CLI, UI, MCP, adapters, and later evidence consumers."
    - "Run read APIs expose approval and handoff readiness without claiming ownership of the Phase 208 task system."
  artifacts:
    - path: .planning/orchestration/durable-run-api-contract.md
      provides: canonical RunApiEnvelope fields and endpoint semantics
    - path: contracts/F-106-agent-run-v2.yaml
      provides: formal contract for run creation, read, and event streaming
  key_links:
    - from: api/tenant/runs/index.js
      to: .planning/orchestration/durable-run-api-contract.md
      via: the create endpoint must implement the locked shared envelope and no local divergence
      pattern: "POST /api/tenant/runs"
    - from: api/tenant/runs/[run_id]/events.js
      to: lib/markos/agents/run-events.ts
      via: the SSE or event feed should be backed by the shared event stream, not a second event source
      pattern: "events"
---

<objective>
Ship the durable run creation, read, and event-stream surfaces so CLI, UI, MCP, and later phases all consume one shared runtime API.
</objective>

<context>
@.planning/phases/207-agentrun-v2-orchestration-substrate/207-CONTEXT.md
@.planning/phases/207-agentrun-v2-orchestration-substrate/207-RESEARCH.md
@.planning/phases/207-agentrun-v2-orchestration-substrate/207-VALIDATION.md
@.planning/phases/207-agentrun-v2-orchestration-substrate/207-01-CONTRACT-LOCK.md
</context>

<tasks>
<task id="207-02-01">
  <name>Durable run API, store, and shared event stream</name>
  <files>.planning/orchestration/durable-run-api-contract.md, supabase/migrations/101_markos_agent_run_v2_extend.sql, supabase/migrations/rollback/101_markos_agent_run_v2_extend.down.sql, contracts/F-106-agent-run-v2.yaml, lib/markos/agents/run-api.ts, lib/markos/agents/run-events.ts, lib/markos/agents/run-store.ts, api/tenant/runs/index.js, api/tenant/runs/[run_id]/index.js, api/tenant/runs/[run_id]/events.js, test/agentrun/phase-207/domain-2/durable-run-api.test.js, test/agentrun/phase-207/domain-2/run-event-stream.test.js, test/migrations/101_markos_agent_run_v2_extend.test.js</files>
  <read_first>
    - .planning/phases/207-agentrun-v2-orchestration-substrate/207-RESEARCH.md section "Domain 2 - Durable run API and shared event stream"
    - .planning/phases/207-agentrun-v2-orchestration-substrate/207-01-CONTRACT-LOCK.md
    - onboarding/backend/agents/run-engine.cjs
    - .planning/phases/204-cli-markos-v1-ga/204-CONTEXT.md
  </read_first>
  <action>
    Step 1: Create `.planning/orchestration/durable-run-api-contract.md` with the exact `RunApiEnvelope` fields `run_id`, `tenant_id`, `agent_id`, `trigger_kind`, `source_surface`, `state`, `priority`, `approval_policy`, `estimated_cost_usd_micro`, `actual_cost_usd_micro`, `task_link_state`, `created_at`, and `updated_at`.
    Step 2: Complete migration `101` and `contracts/F-106-agent-run-v2.yaml` so the durable store and event stream match the locked schema rather than redefining it.
    Step 3: Implement the canonical shared surfaces `POST /api/tenant/runs`, `GET /api/tenant/runs/{run_id}`, and `GET /api/tenant/runs/{run_id}/events`.
    Step 4: Add tests that prove CLI, UI, and adapter consumers can observe the same run and event records without an alternate state source.
  </action>
  <acceptance_criteria>
    - `.planning/orchestration/durable-run-api-contract.md` contains the exact field names `trigger_kind`, `source_surface`, `approval_policy`, and `task_link_state`
    - `api/tenant/runs/index.js` contains the exact string `POST /api/tenant/runs`
    - `api/tenant/runs/[run_id]/events.js` contains the exact string `GET /api/tenant/runs/{run_id}/events`
    - `test/agentrun/phase-207/domain-2/run-event-stream.test.js` contains the exact string `task_link_state`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/agentrun/phase-207/domain-2/</automated>
  </verify>
  <done>AgentRun v2 now has one durable API and one shared event stream that every runtime surface can consume consistently.</done>
</task>
</tasks>

<verification>
- npm test -- test/agentrun/phase-207/domain-2/
</verification>

<success_criteria>
- One durable run API exists for create and read paths
- Event streaming is canonical and shared
- CLI, UI, and adapters stop depending on local helper state
</success_criteria>
