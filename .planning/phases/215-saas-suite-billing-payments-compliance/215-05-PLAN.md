---
phase: 215
plan: "05"
type: execute
wave: 4
depends_on: ["01", "02", "03"]
f_ids:
  - F-281
  - F-282
  - F-283
migration_slots:
  - 122
integrates_with:
  - "PRC-01..09 from P205 (saas_billing_corrections.pricing_recommendation_id FK or sentinel for save offers; SAS-04 enforcement)"
  - "TASK-01..05 from P208 (buildApprovalPackage from lib/markos/crm/agent-actions.ts:68 creates approval inbox row; correction blocks INSERT without approval_id)"
  - "EVD-01..06 from P209 (evidence_pack_ref FK to governance_evidence_packs P206; SOC2 audit trail)"
co_owns_with:
  - "COMP-01 from P206 (P206 owns SOC2 baseline; P215 Plan 05 owns billing-correction slice of evidence — `governance_evidence_packs` row created for every correction/refund/credit/write-off)"
translation_gate_for: []
files_modified:
  - test/fixtures/saas-215/index.js
  - test/fixtures/saas-215/billing-correction.js
  - test/fixtures/saas-215/dunning-schedule.js
  - scripts/preconditions/215-05-check-upstream.cjs
  - supabase/migrations/122_saas_billing_corrections_dunning.sql
  - lib/markos/saas/billing/corrections/contracts.ts
  - lib/markos/saas/billing/corrections/contracts.cjs
  - lib/markos/saas/billing/corrections/correction.ts
  - lib/markos/saas/billing/corrections/refund.ts
  - lib/markos/saas/billing/corrections/credit-note.ts
  - lib/markos/saas/billing/corrections/write-off.ts
  - lib/markos/saas/billing/corrections/dunning.ts
  - lib/markos/saas/billing/corrections/evidence-pack.ts
  - lib/markos/saas/billing/corrections/index.cjs
  - api/v1/saas/billing/corrections.js
  - api/v1/saas/billing/dunning.js
  - api/cron/saas-billing-dunning-runner.js
  - contracts/F-281-saas-billing-correction-v1.yaml
  - contracts/F-282-saas-dunning-schedule-v1.yaml
  - contracts/F-283-saas-billing-evidence-v1.yaml
  - contracts/flow-registry.json
  - test/saas-215/domain-5/correction-shape.test.js
  - test/saas-215/domain-5/correction-approval-trigger.test.js
  - test/saas-215/domain-5/refund-pricing-engine-routing.test.js
  - test/saas-215/domain-5/credit-note-flow.test.js
  - test/saas-215/domain-5/write-off-approval-flow.test.js
  - test/saas-215/domain-5/dunning-state-machine.test.js
  - test/saas-215/domain-5/dunning-schedule-day-0-to-45.test.js
  - test/saas-215/domain-5/evidence-pack-creation.test.js
  - test/saas-215/domain-5/save-offer-pricing-sentinel.test.js
  - test/saas-215/rls/correction-cross-tenant-denial.test.js
  - .planning/phases/215-saas-suite-billing-payments-compliance/215-VALIDATION.md
autonomous: true
requirements:
  - SAS-04
  - SAS-05
  - COMP-01
  - QA-01
  - QA-02
  - QA-13
  - QA-14
  - QA-15
must_haves:
  truths:
    - "Plan 05 depends on Plans 01 + 02 + 03 — assertUpstreamReady CLI hard-checks saas_invoices + saas_payment_attempts + saas_billing_events + saas_credential_vault_refs (Plan 01); soft-checks saas_processor_configs + saas_accounting_configs (Plan 02 — refund flows back to processor) + saas_dian_config (Plan 03 — Colombia credit notes via Siigo); SOFT-checks agent_approval_packages (P208 — required for correction approval; trigger blocks INSERT without approval_id) + governance_evidence_packs (P206 — required for evidence_pack_ref) + evidence_map_records (P209 — soft, deferred sync) + pricing_recommendations (P205 — required for save offer pricing FK or sentinel)"
    - "Migration 122 ships saas_billing_corrections table per RESEARCH §Domain 5 §saas_billing_corrections Table Schema (lines 1013-1041) — RLS-enabled tenant_id-scoped; columns: correction_id PK uuid, tenant_id FK markos_orgs, original_invoice_id FK saas_invoices NOT NULL, correction_invoice_id FK saas_invoices (the credit note or corrected invoice), correction_type CHECK 5 values ('refund','credit_note','write_off','discount','invoice_correction'), amount numeric(15,4) NOT NULL, currency text NOT NULL, reason text NOT NULL, approval_id uuid (FK agent_approval_packages from P208 — soft-FK because P208 may not exist; if P208 absent at runtime, approval_id holds string id from local stub), evidence_pack_ref uuid (FK governance_evidence_packs from P206), approved_by uuid, approved_at timestamptz, pricing_recommendation_id uuid (FK pricing_recommendations from P205 OR sentinel), offer_details text (= '{{MARKOS_PRICING_ENGINE_PENDING}}' until P205 lands), created_at, updated_at"
    - "Migration 122 also ships saas_dunning_schedules table per RESEARCH §Domain 5 §saas_dunning_schedules Table Schema (lines 1046-1064) — RLS-enabled; columns: schedule_id PK uuid, tenant_id FK, subscription_id NOT NULL, invoice_id FK saas_invoices, status CHECK 5 values ('active','resolved','failed','paused','expired'), current_attempt int DEFAULT 1, max_attempts int DEFAULT 4, next_retry_at timestamptz, grace_period_ends_at timestamptz NOT NULL, suspension_at timestamptz, outcome_jsonb jsonb, created_at"
    - "DB-trigger BILLING_CORRECTION_REQUIRES_APPROVAL ships in migration 122 BEFORE INSERT OR UPDATE on saas_billing_corrections — RAISE EXCEPTION (verbatim per RESEARCH §Domain 5 lines 1069-1091): (1) approved_at IS NOT NULL AND (approval_id IS NULL OR evidence_pack_ref IS NULL) → block (cannot mark approved without proof); (2) on INSERT for correction_type IN ('refund','write_off','discount') AND approval_id IS NULL → block (these correction types REQUIRE prior approval before creation; credit_note and invoice_correction allowed without prior approval but must have approval before approved_at is set)"
    - "Architecture-lock RE-RUN against Plan 05 paths — scanForbiddenPatterns({paths:['lib/markos/saas/billing/corrections/','api/v1/saas/billing/corrections.js','api/v1/saas/billing/dunning.js','api/cron/saas-billing-dunning-runner.js']}); asserts ZERO violations; CRITICAL: Plan 05 MUST use buildApprovalPackage from lib/markos/crm/agent-actions.ts:68 (NOT createApprovalPackage which is forbidden); MUST NOT use requireSupabaseAuth (use requireHostedSupabaseAuth)"
    - "Refund workflow per RESEARCH §Domain 5 (correction_type='refund') — lib/markos/saas/billing/corrections/refund.ts exports requestRefund({tenantId, invoiceId, amount, reason}, supabase) → calls buildApprovalPackage from lib/markos/crm/agent-actions.ts (P208 substrate) creating an approval task; returns approval_id; createRefund({tenantId, correctionId, approvalId}, supabase) → on approved_at set: writes governance_evidence_packs row (P206), UPDATEs saas_billing_corrections.approved_at + evidence_pack_ref; if processor='stripe' → calls stripe.refunds.create via Plan 02 stripe-client; if processor='mercado_pago' → calls Plan 03 MP refund endpoint; on success creates correction_invoice_id (credit note in saas_invoices)"
    - "Credit note workflow per RESEARCH §Domain 5 (correction_type='credit_note') — lib/markos/saas/billing/corrections/credit-note.ts exports createCreditNote({tenantId, originalInvoiceId, amount, reason}, supabase) → for US: createCreditMemoInQBO via Plan 02 quickbooks-sync; for Colombia: syncCreditNoteToSiigo via Plan 03 siigo-sync (DIAN credit note via Siigo); INSERT saas_invoices invoice_type='credit_note' + correction_of_invoice_id = originalInvoiceId; INSERT saas_billing_corrections correction_type='credit_note' correction_invoice_id = new credit note ID"
    - "Write-off workflow per RESEARCH §Domain 5 (correction_type='write_off') — lib/markos/saas/billing/corrections/write-off.ts exports requestWriteOff({tenantId, invoiceId, amount, reason}, supabase) → ALWAYS requires approval (BILLING_CORRECTION_REQUIRES_APPROVAL trigger blocks unapproved write_off INSERT); on approval: UPDATE saas_invoices.status='uncollectible'; emit governance_evidence_pack with full audit trail (actor, timestamp, original_amount, write_off_amount, reason, approval_id)"
    - "Discount workflow per RESEARCH §Domain 5 (correction_type='discount') — lib/markos/saas/billing/corrections/correction.ts (shared) exports requestDiscount({tenantId, invoiceId, discountAmount, reason}, supabase) → ALWAYS requires approval AND pricing_recommendation_id FK or sentinel (SAS-04 enforcement — discount is a pricing decision and must trace through Pricing Engine); offer_details = '{{MARKOS_PRICING_ENGINE_PENDING}}' until P205 lands; trigger BILLING_CORRECTION_REQUIRES_APPROVAL blocks discount without approval"
    - "Invoice correction workflow per RESEARCH §Domain 5 (correction_type='invoice_correction') — fixes legal invoice errors (wrong customer NIT, incorrect tax jurisdiction); UPDATE saas_invoices ONLY allowed in draft status; once invoice issued (Colombia DIAN transmitted OR US Stripe finalized) correction requires creating a credit note + new invoice (DIAN does not allow modifying transmitted invoices)"
    - "Dunning state machine per RESEARCH §Domain 5 §Dunning Workflow (lines 1098-1108) — lib/markos/saas/billing/corrections/dunning.ts exports startDunning({tenantId, invoiceId, subscriptionId, processor}, supabase) → INSERT saas_dunning_schedules status='active' grace_period_ends_at=NOW()+45 days max_attempts=4 next_retry_at=NOW()+3 days; advanceDunning({tenantId, scheduleId}, supabase) → switch on day-since-failure: day-0 first failure→past_due+email; day-3 attempt 2 smart retry; day-5 escalation email + CRM at-risk flag (P207 task); day-7 attempt 3 final retry; day-8 if still failed→unpaid+access suspended (UPDATE saas_subscriptions.status='unpaid' P214); day-38 data deletion warning email; day-45 data deletion via P206 governance workflow"
    - "Dunning cron (api/cron/saas-billing-dunning-runner.js) — runs every 6 hours; for each saas_dunning_schedules row WHERE status='active' AND next_retry_at <= NOW() → fetches saas_invoices + saas_payment_attempts + saas_processor_configs; calls processor retry via Plan 02 stripe.invoices.retrieveUpcoming + invoice.pay OR Plan 03 MP payment retry; UPDATEs saas_payment_attempts + saas_dunning_schedules; uses x-markos-cron-secret header matching MARKOS_SAAS_BILLING_CRON_SECRET env per api/cron/mcp-kpi-digest.js pattern; emits AgentRun event kind='saas-billing-dunning-runner'"
    - "Save offer routing through Pricing Engine per CONTEXT non-negotiable #4 — when dunning escalates to save-offer (day-5+), lib/markos/saas/billing/corrections/dunning.ts calls createSaveOffer which sets pricing_recommendation_id on saas_billing_corrections (or sentinel until P205 lands); test/saas-215/domain-5/save-offer-pricing-sentinel.test.js asserts INSERT save offer with offer_details='{{MARKOS_PRICING_ENGINE_PENDING}}' AND pricing_recommendation_id IS NULL → succeeds (sentinel valid); INSERT save offer with offer_details='free for 3 months' AND pricing_recommendation_id IS NULL → blocks (must have FK or sentinel)"
    - "Evidence pack creation per RESEARCH §Domain 5 §SOC2 Evidence Pack — lib/markos/saas/billing/corrections/evidence-pack.ts exports createBillingCorrectionEvidence({tenantId, correctionId, action, actor, ...}, supabase) → INSERT into governance_evidence_packs (P206 substrate; SOFT — if absent, evidence stored in saas_billing_corrections.outcome_jsonb stub); evidence pack contains: action ('refund'/'credit'/'write_off'/'discount'/'invoice_correction'), actor uuid, timestamp, original_invoice_id, correction_type, amount, currency, reason, approval_id, processor_response (sanitized via Plan 01 sanitizeBillingResponse — no credentials in evidence)"
    - "API handlers — api/v1/saas/billing/corrections.js (GET list with status filter; POST {correction_type, original_invoice_id, amount, reason} → routes to refund/credit-note/write-off/discount/invoice-correction handler; ALL require requireHostedSupabaseAuth + admin role; ALL response bodies sanitized) + api/v1/saas/billing/dunning.js (GET dunning schedules per tenant; POST {action='pause'|'resume'|'override', scheduleId} for operator override of automatic dunning — ALSO requires approval per BILLING_CORRECTION_REQUIRES_APPROVAL pattern)"
    - "3 contracts ship — F-281 (saas-billing-correction-v1) covering saas_billing_corrections schema + 5 correction_type workflows + BILLING_CORRECTION_REQUIRES_APPROVAL exception + Pricing Engine sentinel pattern for save offers; F-282 (saas-dunning-schedule-v1) covering saas_dunning_schedules schema + day-0-to-45 dunning workflow + cron pattern; F-283 (saas-billing-evidence-v1) covering governance_evidence_packs billing-correction slice + SOC2 audit trail format; NO collision with F-001..F-280"
    - "Cross-tenant denial test — INSERT saas_billing_corrections + saas_dunning_schedules under tenant A; SELECT under tenant B returns 0 rows"
    - "PRC-01..09 + TASK-01..05 + EVD-01..06 documented in frontmatter as `integrates_with` (NOT `requirements`); COMP-01 documented as `co_owns_with: P206` per REVIEWS.md ownership clarification — P206 owns SOC2 baseline; P215 Plan 05 owns billing-correction slice of evidence"
    - "215-VALIDATION.md per-task verification map updated with rows for Plan 05 + domain-5 test paths under `test/saas-215/domain-5/`"
    - "UI-SPEC fold (215-UI-SPEC §Downstream UI Inheritance Map rows 8 dunning timeline + 9 refund/credit-note approval modal + 10 evidence pack browser + §Translation Gate Dissolution row 1 — 214 `billing_charge_approval` extends to 6th P215 `billing_correction_approval` literal + §Inheritance Bindings §From 213-UI-SPEC §Public-Proof Boundary): ui_scope = NO_UI; Plan 05 dissolves the 214-UI-SPEC translation gate by extending the P207 4-set + P214 5th `billing_charge_approval` literal to the 6th P215 `billing_correction_approval` literal — the canonical chain is now `approval`, `recovery`, `follow_up`, `manual_input` (P207) + `billing_charge_approval` (P214) + `billing_correction_approval` (P215) = 6 handoff_kind literals. The literal `billing_correction_approval` MUST appear verbatim in `lib/markos/saas/billing/corrections/correction.ts` `buildApprovalPackage` invocation. Mutation-class binding `billing.charge` (per 206-UI-SPEC) applies to all 5 correction_type literals — `refund`, `credit_note`, `write_off`, `discount`, `invoice_correction` — appear verbatim as CHECK-clause values in `saas_billing_corrections.correction_type` column AND as 5 distinct const string literals in `lib/markos/saas/billing/corrections/contracts.ts` `CORRECTION_TYPES` array. Mutation-class binding `data.export` (per 206-UI-SPEC) applies to evidence-pack export action in future P208 admin extension `<EvidencePackBrowser>` (UI-SPEC §Downstream row 10). Sentinel `{{MARKOS_PRICING_ENGINE_PENDING}}` discipline on `saas_billing_corrections.offer_details` column per Plan 05 closeout — literal appears in app-layer validator + sentinel test. SOC2 evidence pack creation per Plan 05 (`governance_evidence_packs` row co_owns_with COMP-01 from P206) carries `action + actor + timestamp + amount + reason + approval_id + sanitized processor_response` — `processor_response` sanitized via Plan 01 `sanitizeBillingResponse` (no credentials in evidence; satisfies UI-SPEC §Sensitive Credential Handling B-1 + B-3). 213-04 §Public-Proof Boundary enforced — 215 evidence packs are PRIVATE doctrine, NEVER published as public proof; case-study claims may cite the EXISTENCE of `evidence_pack_ref` UUID via `<.c-chip-protocol>` only (D-09 mint-as-text), but evidence content (refund amount, customer NIT, processor response) is NEVER embedded inline. The 213-04 banned-phrases contract — `unlimited`, `instant`, `guaranteed`, `100%`, `bank-grade`, `military-grade` — applies to any future case-study referencing a 215 correction outcome (load-bearing for downstream P209 evidence map). DB-trigger name `BILLING_CORRECTION_REQUIRES_APPROVAL` appears as RAISE EXCEPTION literal in migration 122. Banned-lexicon zero-match on F-281 + F-282 + F-283 contract YAMLs + `lib/markos/saas/billing/corrections/correction.ts` doctrine prose. Downstream UI consumers (read-only — DO NOT modify here): future_phase_217_correction_modal `<RefundCreditNoteApprovalModal>` (UI-SPEC §Downstream row 9 — extends P208-04 Approval Inbox via 6th `billing_correction_approval` filter chip; renders 5 correction_type literals as `.c-badge--info` chips reading `saas_billing_corrections.correction_type`; mutation-class `billing.charge` per 206-UI-SPEC; `default_approval_mode==dual_approval` for refund-above-threshold + write_off + discount per 215-05 BILLING_CORRECTION_REQUIRES_APPROVAL trigger); future_phase_217_dunning_timeline `<DunningTimelineViewer>` (UI-SPEC §Downstream row 8 — reads `saas_dunning_schedules` + day-0-to-45 state transitions; mutation-class `billing.charge` for retry action + `data.export` for timeline export); future_phase_217_evidence_browser `<EvidencePackBrowser>` (UI-SPEC §Downstream row 10 — reads `governance_evidence_packs` linked from `saas_billing_corrections.evidence_pack_ref`; mutation-class `data.export`)."
  artifacts:
    - path: scripts/preconditions/215-05-check-upstream.cjs
      provides: assertUpstreamReady — HARD = [saas_invoices + saas_payment_attempts + saas_billing_events + saas_credential_vault_refs (Plan 01), saas_subscriptions (P214), markos_webhook_subscriptions (P203 — for processor refund webhook hooks)]; SOFT = [agent_approval_packages (P208 — required at runtime for INSERT, soft because P208 may not be landed yet — graceful degrade with stub), governance_evidence_packs (P206), pricing_recommendations (P205), evidence_map_records (P209)]
    - path: supabase/migrations/122_saas_billing_corrections_dunning.sql
      provides: combined migration — saas_billing_corrections + saas_dunning_schedules SOR tables + BILLING_CORRECTION_REQUIRES_APPROVAL DB-trigger + RLS policies
    - path: lib/markos/saas/billing/corrections/contracts.ts
      provides: zod schemas + enum constants — CORRECTION_TYPES (5: 'refund','credit_note','write_off','discount','invoice_correction') + DUNNING_STATUSES (5) + DUNNING_DAY_ACTIONS (8 — day 0/3/5/7/8/38/45) + SaaSBillingCorrectionSchema + SaaSDunningScheduleSchema + EvidencePackSchema zod
      exports: [CORRECTION_TYPES, DUNNING_STATUSES, DUNNING_DAY_ACTIONS, SaaSBillingCorrectionSchema, SaaSDunningScheduleSchema, EvidencePackSchema]
    - path: lib/markos/saas/billing/corrections/correction.ts
      provides: shared helpers — requestCorrection (router by correction_type), validateCorrectionInput, getCorrection
      exports: [requestCorrection, validateCorrectionInput, getCorrection, requestDiscount]
    - path: lib/markos/saas/billing/corrections/refund.ts
      provides: requestRefund + createRefund + executeProcessorRefund (delegates to Plan 02 stripe-client OR Plan 03 mercadopago-preapproval based on saas_invoices.processor)
      exports: [requestRefund, createRefund, executeProcessorRefund]
    - path: lib/markos/saas/billing/corrections/credit-note.ts
      provides: createCreditNote — for US delegates to Plan 02 createCreditMemoInQBO; for Colombia delegates to Plan 03 syncCreditNoteToSiigo; INSERT saas_invoices invoice_type='credit_note' + correction_of_invoice_id
      exports: [createCreditNote]
    - path: lib/markos/saas/billing/corrections/write-off.ts
      provides: requestWriteOff (always requires approval); on approval UPDATE saas_invoices.status='uncollectible' + emit evidence pack
      exports: [requestWriteOff, executeWriteOff]
    - path: lib/markos/saas/billing/corrections/dunning.ts
      provides: startDunning + advanceDunning + pauseDunning + resumeDunning + overrideDunning (operator action; requires approval); state machine for day-0-to-45 workflow
      exports: [startDunning, advanceDunning, pauseDunning, resumeDunning, overrideDunning, DUNNING_DAY_HANDLERS]
    - path: lib/markos/saas/billing/corrections/evidence-pack.ts
      provides: createBillingCorrectionEvidence — INSERT governance_evidence_packs (P206); fallback to outcome_jsonb stub if P206 absent; ALL processor_response data sanitized via Plan 01 sanitizeBillingResponse (no credentials in evidence)
      exports: [createBillingCorrectionEvidence, getEvidencePack]
    - path: lib/markos/saas/billing/corrections/index.cjs
      provides: CommonJS twin re-exporting full surface
    - path: api/v1/saas/billing/corrections.js
      provides: GET /v1/saas/billing/corrections (paginated by status); POST /v1/saas/billing/corrections {correction_type, original_invoice_id, amount, reason} → routes; requires admin role; ALL responses sanitized
    - path: api/v1/saas/billing/dunning.js
      provides: GET /v1/saas/billing/dunning (schedules paginated); POST /v1/saas/billing/dunning {action, scheduleId} → operator override (requires approval)
    - path: api/cron/saas-billing-dunning-runner.js
      provides: every-6h cron — iterates active dunning schedules; advances state per day-since-failure; uses x-markos-cron-secret header matching MARKOS_SAAS_BILLING_CRON_SECRET; emits AgentRun event
    - path: contracts/F-281-saas-billing-correction-v1.yaml
      provides: documents saas_billing_corrections schema + 5 correction_type workflows + BILLING_CORRECTION_REQUIRES_APPROVAL exception + save offer Pricing Engine sentinel
    - path: contracts/F-282-saas-dunning-schedule-v1.yaml
      provides: documents saas_dunning_schedules schema + day-0-to-45 workflow + 8 dunning day actions + cron pattern
    - path: contracts/F-283-saas-billing-evidence-v1.yaml
      provides: documents governance_evidence_packs billing-correction slice (action + actor + timestamp + original_invoice_id + correction_type + amount + reason + approval_id + sanitized processor_response) + SOC2 Type I audit trail format
    - path: contracts/flow-registry.json
      provides: 3 entries appended for F-281 + F-282 + F-283
    - path: test/fixtures/saas-215/billing-correction.js
      provides: billingCorrectionFactory({tenantId, originalInvoiceId, correctionType='refund', amount, currency='USD', reason, ...}) returning a row shape
    - path: test/fixtures/saas-215/dunning-schedule.js
      provides: dunningScheduleFactory({tenantId, subscriptionId, invoiceId, status='active', currentAttempt=1, ...}) returning a row shape
    - path: test/saas-215/domain-5/correction-shape.test.js
      provides: zod parity assertion against F-281 YAML
    - path: test/saas-215/domain-5/correction-approval-trigger.test.js
      provides: Postgres-backed — INSERT saas_billing_corrections correction_type='refund' AND approval_id IS NULL → exception 'BILLING_CORRECTION_REQUIRES_APPROVAL'; with approval_id set → succeeds; UPDATE setting approved_at AND evidence_pack_ref IS NULL → exception
    - path: test/saas-215/domain-5/refund-pricing-engine-routing.test.js
      provides: requestRefund with reason='customer save offer' AND offer_details='{{MARKOS_PRICING_ENGINE_PENDING}}' AND pricing_recommendation_id IS NULL → succeeds (sentinel valid); offer_details='free for 3 months' AND pricing_recommendation_id IS NULL → blocks (must have FK or sentinel per SAS-04)
    - path: test/saas-215/domain-5/credit-note-flow.test.js
      provides: createCreditNote for US Stripe invoice → calls Plan 02 createCreditMemoInQBO + INSERT saas_invoices invoice_type='credit_note' + correction_of_invoice_id; for CO Mercado Pago → calls Plan 03 syncCreditNoteToSiigo
    - path: test/saas-215/domain-5/write-off-approval-flow.test.js
      provides: requestWriteOff without approval → blocks at trigger; with approval → UPDATE saas_invoices.status='uncollectible' + governance_evidence_packs row created
    - path: test/saas-215/domain-5/dunning-state-machine.test.js
      provides: state transitions (active→resolved on payment success; active→failed on max_attempts exceeded; active→paused on operator override; paused→active on resume; expired transition at grace_period_ends_at)
    - path: test/saas-215/domain-5/dunning-schedule-day-0-to-45.test.js
      provides: simulates day 0/3/5/7/8/38/45 actions per RESEARCH §Domain 5 Dunning Workflow table; verifies email triggers + CRM flag + access suspension UPDATE saas_subscriptions
    - path: test/saas-215/domain-5/evidence-pack-creation.test.js
      provides: createBillingCorrectionEvidence with sample correction → INSERT governance_evidence_packs row with all required fields; processor_response sanitized via Plan 01 sanitizeBillingResponse (no credentials present)
    - path: test/saas-215/domain-5/save-offer-pricing-sentinel.test.js
      provides: save offer with sentinel valid; save offer without sentinel AND without FK invalid (pricing engine ownership enforced)
    - path: test/saas-215/rls/correction-cross-tenant-denial.test.js
      provides: INSERT saas_billing_corrections + saas_dunning_schedules under tenant A; SELECT under tenant B returns 0 rows
  key_links:
    - from: lib/markos/saas/billing/corrections/refund.ts
      to: lib/markos/crm/agent-actions.ts:68 (P208 substrate)
      via: requestRefund calls buildApprovalPackage to create approval task; approval_id stored on saas_billing_corrections.approval_id
      pattern: "buildApprovalPackage"
    - from: lib/markos/saas/billing/corrections/refund.ts
      to: lib/markos/saas/billing/us/stripe-client.ts (Plan 02) + lib/markos/saas/billing/co/mercadopago-preapproval.ts (Plan 03)
      via: executeProcessorRefund delegates by saas_invoices.processor — Stripe stripe.refunds.create OR MP refund endpoint
      pattern: "executeProcessorRefund"
    - from: lib/markos/saas/billing/corrections/credit-note.ts
      to: lib/markos/saas/billing/us/quickbooks-sync.ts (Plan 02) + lib/markos/saas/billing/co/siigo-sync.ts (Plan 03)
      via: createCreditNote delegates by billing_country — US createCreditMemoInQBO OR CO syncCreditNoteToSiigo (DIAN credit note)
      pattern: "createCreditMemoInQBO"
    - from: lib/markos/saas/billing/corrections/dunning.ts
      to: supabase saas_subscriptions (P214) + saas_payment_attempts (Plan 01)
      via: day-8 transition UPDATEs saas_subscriptions.status='unpaid'; smart retries write saas_payment_attempts
      pattern: "saas_subscriptions"
    - from: api/cron/saas-billing-dunning-runner.js
      to: lib/markos/saas/billing/corrections/dunning.ts (advanceDunning) + lib/markos/audit/* (AgentRun emit)
      via: every-6h cron iterates active dunning schedules; calls advanceDunning per row; emits AgentRun event
      pattern: "advanceDunning"
    - from: lib/markos/saas/billing/corrections/evidence-pack.ts
      to: supabase governance_evidence_packs (P206)
      via: createBillingCorrectionEvidence INSERTs evidence pack row; fallback to saas_billing_corrections.outcome_jsonb if P206 absent
      pattern: "governance_evidence_packs"
---

<objective>
Ship Wave 3 billing approvals + corrections + dunning + evidence: saas_billing_corrections + saas_dunning_schedules SOR tables in migration 122 + BILLING_CORRECTION_REQUIRES_APPROVAL DB-trigger (verbatim per RESEARCH §Domain 5 — blocks refund/write_off/discount INSERT without approval_id; blocks UPDATE setting approved_at without approval_id + evidence_pack_ref), 5 correction_type workflows (refund + credit_note + write_off + discount + invoice_correction) all routed through buildApprovalPackage (P208 substrate) and pricing_recommendation_id FK or sentinel `{{MARKOS_PRICING_ENGINE_PENDING}}` for save offers (CONTEXT non-negotiable #4 — Pricing Engine ownership), dunning state machine implementing the day-0-to-45 workflow per RESEARCH §Domain 5 (day-0 first failure → past_due+email; day-3/5/7 retries; day-8 access suspended via P214 saas_subscriptions UPDATE; day-38 deletion warning; day-45 data deletion via P206), 6-hour cron runner (api/cron/saas-billing-dunning-runner.js using MARKOS_SAAS_BILLING_CRON_SECRET pattern), SOC2 evidence pack creation for every correction (governance_evidence_packs row with action + actor + timestamp + amount + reason + approval_id + sanitized processor_response — co_owns_with COMP-01 from P206), 3 F-ID contracts (F-281 correction + F-282 dunning + F-283 evidence), and the cross-tenant RLS denial.

Purpose: satisfy SAS-04 (save offers + discounts route through Pricing Engine FK or sentinel) + SAS-05 (failed-payment recovery via dunning workflow + refunds + credits + corrections) + COMP-01 (SOC2 Type I billing-correction evidence — co_owns_with P206); QA-01..02 + QA-13..15 (architecture-lock + RLS isolation + approval enforcement + evidence trail + Pricing Engine sentinel parity). Plan 05 unblocks Plan 06 closeout regression.

Output: 1 preflight CLI + 1 SOR migration (122 — saas_billing_corrections + saas_dunning_schedules + BILLING_CORRECTION_REQUIRES_APPROVAL trigger) + 8 corrections lib files + 2 API handlers + 1 cron + 3 contract YAMLs + flow-registry update + 2 fixture files + 9 domain-5 test files + 1 RLS test + populated VALIDATION.md.
</objective>

<ui_spec_fold>
ui_scope: NO_UI
ui_spec_block: 215-UI-SPEC.md §Downstream UI Inheritance Map rows 8 (Dunning state timeline viewer) + 9 (Refund/credit-note approval modal — P208-04 Approval Inbox extension) + 10 (Evidence pack browser); §Translation Gate Dissolution row 1 — 214 `billing_charge_approval` extends to 6th `billing_correction_approval` handoff_kind literal; §Inheritance Bindings §From 213-UI-SPEC §Public-Proof Boundary (215 evidence packs are PRIVATE doctrine)

Cross-cutting carry-forward applicable to backend (D-08..D-15 from 213.4-VALIDATION.md):
- D-08 token-only / D-09 mint-as-text (`evidence_pack_ref` UUID rendered via `<.c-chip-protocol>` mint text in future case-study citations) / D-09b `.c-notice` mandatory (correction-approval-pending gating) / D-13 `.c-card--feature` reserved (correction modals + dunning timeline rows + evidence-pack browser rows ALL use `.c-card` default) / D-14 no `.c-table` (correction queue + dunning schedule list + evidence-pack browser ALL use vanilla `<table>` + `.c-badge--{state}`) / D-15 selective extraction (`<BillingCorrectionRow>` recommended for reuse across Approval Inbox + correction queue + evidence browser)

Downstream UI consumers (read-only — DO NOT modify here):
- future_phase_217_correction_modal `<RefundCreditNoteApprovalModal>` extends P208-04 Approval Inbox via 6th `billing_correction_approval` filter chip; renders 5 correction_type literals as `.c-badge--info` chips; mutation-class `billing.charge` per 206-UI-SPEC; `default_approval_mode==dual_approval` for refund-above-threshold + write_off + discount per 215-05 BILLING_CORRECTION_REQUIRES_APPROVAL trigger
- future_phase_217_dunning_timeline `<DunningTimelineViewer>` reads `saas_dunning_schedules` + day-0-to-45 state transitions (`active`, `resolved`, `failed`, `paused`, `expired`); mutation-class `billing.charge` for retry + `data.export` for timeline export per 206-UI-SPEC
- future_phase_217_evidence_browser `<EvidencePackBrowser>` reads `governance_evidence_packs` linked from `saas_billing_corrections.evidence_pack_ref`; mutation-class `data.export` per 206-UI-SPEC; 213-04 §Public-Proof Boundary applies — case-study citations render UUID via `<.c-chip-protocol>` only, never embed evidence content

Backend doctrine ACs added by this fold (closeout grep gates):
- 5 correction_type literals (`refund`, `credit_note`, `write_off`, `discount`, `invoice_correction`) appear verbatim in `lib/markos/saas/billing/corrections/contracts.ts` CORRECTION_TYPES array AND in CHECK clause of `saas_billing_corrections.correction_type` in migration 122
- 6th handoff_kind literal `billing_correction_approval` extension — passed to `buildApprovalPackage` from P208 substrate in `lib/markos/saas/billing/corrections/correction.ts`
- Sentinel `{{MARKOS_PRICING_ENGINE_PENDING}}` discipline on `saas_billing_corrections.offer_details`
- SOC2 evidence pack creation co_owns_with COMP-01 from P206 — `governance_evidence_packs` INSERT row carries `action + actor + timestamp + amount + reason + approval_id + sanitized processor_response`; `processor_response` sanitized via Plan 01 `sanitizeBillingResponse`
- DB-trigger name verbatim `BILLING_CORRECTION_REQUIRES_APPROVAL` appears in migration 122
- 213-04 §Public-Proof Boundary enforced — 215 evidence packs PRIVATE doctrine; banned-phrases contract (`unlimited`, `instant`, `guaranteed`, `100%`, `bank-grade`, `military-grade`) carries forward
- Banned-lexicon zero-match on F-281 + F-282 + F-283 contract YAMLs
</ui_spec_fold>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/215-saas-suite-billing-payments-compliance/215-CONTEXT.md
@.planning/phases/215-saas-suite-billing-payments-compliance/215-RESEARCH.md
@.planning/phases/215-saas-suite-billing-payments-compliance/215-VALIDATION.md
@.planning/phases/215-saas-suite-billing-payments-compliance/215-01-PLAN.md
@.planning/phases/215-saas-suite-billing-payments-compliance/215-02-PLAN.md
@.planning/phases/215-saas-suite-billing-payments-compliance/215-03-PLAN.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@lib/markos/crm/agent-actions.ts
@onboarding/backend/runtime-context.cjs
@lib/markos/saas/billing/index.cjs
@lib/markos/saas/billing/us/index.cjs
@lib/markos/saas/billing/co/index.cjs
@api/cron/mcp-kpi-digest.js
@obsidian/brain/Pricing Engine Canon.md

<interfaces>
<!-- Plan 01-03 surfaces (DEPENDENCY) -->

From lib/markos/crm/agent-actions.ts:68 (P208 substrate REUSE):

```typescript
function buildApprovalPackage(input: BuildApprovalPackageInput): ApprovalPackage
// returns { approval_id: string, ... }
// Plan 05 calls this for refund/credit/write-off/discount approval gates
```

5 correction types per RESEARCH §Domain 5:

| correction_type | Requires approval before INSERT? | Pricing Engine FK/sentinel? | Processor action |
|----------------|----------------------------------|----------------------------|------------------|
| refund | YES | NO | stripe.refunds.create OR MP refund endpoint |
| credit_note | NO (allowed without prior approval) | NO | QBO CreateCreditMemo OR Siigo credit note |
| write_off | YES | NO | UPDATE saas_invoices.status='uncollectible' |
| discount | YES | YES (offer_details = sentinel until P205) | none — pricing-only |
| invoice_correction | NO (draft only) OR YES (post-issue) | NO | UPDATE draft OR create new credit note + new invoice |

Dunning Workflow (RESEARCH §Domain 5 lines 1098-1108):

| Day | Action | System |
|-----|--------|--------|
| 0 | First failure → `past_due`, schedule created | saas_dunning_schedules + saas_payment_attempts |
| 0 | Email: payment failed | P223 email substrate (or stub) |
| 3 | Attempt 2 smart retry | saas_payment_attempts |
| 5 | Escalation email + at-risk flag | P223 + P207 task |
| 7 | Attempt 3 final retry | saas_payment_attempts |
| 8 | Access suspended | UPDATE saas_subscriptions (P214) |
| 38 | Data deletion warning email | P223 |
| 45 | Data deletion | P206 governance workflow |

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0.5: Plan 05 preflight</name>
  <files>scripts/preconditions/215-05-check-upstream.cjs</files>
  <read_first>
    - lib/markos/saas/billing/preflight/index.cjs (Plan 01)
    - scripts/preconditions/215-04-check-upstream.cjs (mirror)
  </read_first>
  <action>
    Create scripts/preconditions/215-05-check-upstream.cjs — assertUpstreamReady; HARD = [saas_invoices + saas_payment_attempts + saas_billing_events + saas_credential_vault_refs (Plan 01), saas_subscriptions (P214), markos_webhook_subscriptions (P203)]; SOFT = [agent_approval_packages (P208), governance_evidence_packs (P206), pricing_recommendations (P205), evidence_map_records (P209), markos_agent_runs (P207)]; exit 1 on hard miss; SOFT misses log warning + caller code degrades gracefully (P208/P206 absent → stub approval/evidence in outcome_jsonb).
  </action>
  <verify>
    <automated>node scripts/preconditions/215-05-check-upstream.cjs</automated>
  </verify>
  <done>Preflight CLI exits 0 with HARD tables present; soft-warns appropriately.</done>
</task>

<task type="auto">
  <name>Task 1: Migration 122 + 8 corrections lib files (correction + refund + credit-note + write-off + dunning + evidence-pack + contracts + index.cjs) + 2 API handlers + dunning cron + 3 contracts + 9 domain-5 + RLS tests</name>
  <files>supabase/migrations/122_saas_billing_corrections_dunning.sql, lib/markos/saas/billing/corrections/contracts.ts, lib/markos/saas/billing/corrections/contracts.cjs, lib/markos/saas/billing/corrections/correction.ts, lib/markos/saas/billing/corrections/refund.ts, lib/markos/saas/billing/corrections/credit-note.ts, lib/markos/saas/billing/corrections/write-off.ts, lib/markos/saas/billing/corrections/dunning.ts, lib/markos/saas/billing/corrections/evidence-pack.ts, lib/markos/saas/billing/corrections/index.cjs, api/v1/saas/billing/corrections.js, api/v1/saas/billing/dunning.js, api/cron/saas-billing-dunning-runner.js, contracts/F-281-saas-billing-correction-v1.yaml, contracts/F-282-saas-dunning-schedule-v1.yaml, contracts/F-283-saas-billing-evidence-v1.yaml, contracts/flow-registry.json, test/fixtures/saas-215/index.js, test/fixtures/saas-215/billing-correction.js, test/fixtures/saas-215/dunning-schedule.js, test/saas-215/domain-5/correction-shape.test.js, test/saas-215/domain-5/correction-approval-trigger.test.js, test/saas-215/domain-5/refund-pricing-engine-routing.test.js, test/saas-215/domain-5/credit-note-flow.test.js, test/saas-215/domain-5/write-off-approval-flow.test.js, test/saas-215/domain-5/dunning-state-machine.test.js, test/saas-215/domain-5/dunning-schedule-day-0-to-45.test.js, test/saas-215/domain-5/evidence-pack-creation.test.js, test/saas-215/domain-5/save-offer-pricing-sentinel.test.js, test/saas-215/rls/correction-cross-tenant-denial.test.js, .planning/phases/215-saas-suite-billing-payments-compliance/215-VALIDATION.md</files>
  <read_first>
    - .planning/phases/215-saas-suite-billing-payments-compliance/215-RESEARCH.md §Domain 5 §saas_billing_corrections Table Schema §saas_dunning_schedules Table Schema §DB-Trigger Compliance BILLING_CORRECTION_REQUIRES_APPROVAL §Dunning Workflow §SOC2 Evidence Pack
    - lib/markos/crm/agent-actions.ts:68 (buildApprovalPackage signature — P208 REUSE)
    - lib/markos/saas/billing/credential-vault.ts (Plan 01 — sanitize processor responses)
    - lib/markos/saas/billing/us/stripe-client.ts + quickbooks-sync.ts (Plan 02 — refund + credit memo delegation)
    - lib/markos/saas/billing/co/mercadopago-preapproval.ts + siigo-sync.ts (Plan 03 — refund + DIAN credit note delegation)
    - api/cron/mcp-kpi-digest.js (cron + x-markos-cron-secret pattern)
    - obsidian/brain/Pricing Engine Canon.md (sentinel `{{MARKOS_PRICING_ENGINE_PENDING}}` enforcement)
  </read_first>
  <action>
    Step 1: Create supabase/migrations/122_saas_billing_corrections_dunning.sql:
      - CREATE TABLE saas_billing_corrections (full schema per RESEARCH §Domain 5 lines 1013-1041 — verbatim + RLS)
      - CREATE POLICY sbc_tenant_isolation
      - CREATE TABLE saas_dunning_schedules (full schema per RESEARCH §Domain 5 lines 1046-1064 — verbatim + RLS)
      - CREATE POLICY sds_tenant_isolation
      - CREATE OR REPLACE FUNCTION enforce_billing_correction_approval() — verbatim per RESEARCH lines 1069-1091 (approved_at + missing approval_id/evidence_pack_ref → block; INSERT refund/write_off/discount without approval_id → block)
      - CREATE TRIGGER trg_billing_correction_approval BEFORE INSERT OR UPDATE ON saas_billing_corrections FOR EACH ROW EXECUTE FUNCTION enforce_billing_correction_approval()

    Step 2: Create lib/markos/saas/billing/corrections/contracts.ts exporting CORRECTION_TYPES (5) + DUNNING_STATUSES (5) + DUNNING_DAY_ACTIONS (8) + 3 zod schemas.

    Step 3: Create lib/markos/saas/billing/corrections/correction.ts shared helpers — requestCorrection (router by type), validateCorrectionInput (enforces SAS-04 sentinel for discount), getCorrection, requestDiscount.

    Step 4: Create lib/markos/saas/billing/corrections/refund.ts:
      - Import buildApprovalPackage (P208 — lib/markos/crm/agent-actions.ts:68)
      - Export requestRefund({tenantId, invoiceId, amount, reason}, supabase) → calls buildApprovalPackage; returns {approval_id, status: 'pending_approval'}
      - Export createRefund({tenantId, correctionId, approvalId}, supabase) → guarded by trigger (must have approval_id); on approval: emit governance_evidence_pack via Plan 05 evidence-pack.ts; UPDATE saas_billing_corrections.approved_at
      - Export executeProcessorRefund({correctionId}, supabase) → fetches saas_invoices.processor; if 'stripe' calls Plan 02 stripe.refunds.create; if 'mercado_pago' calls Plan 03 MP refund; on success creates credit_note saas_invoices

    Step 5: Create lib/markos/saas/billing/corrections/credit-note.ts:
      - Export createCreditNote({tenantId, originalInvoiceId, amount, reason}, supabase) → fetches original invoice; if billing_country='US' delegates to Plan 02 createCreditMemoInQBO; if 'CO' delegates to Plan 03 syncCreditNoteToSiigo (DIAN credit note); INSERT saas_invoices invoice_type='credit_note' + correction_of_invoice_id; INSERT saas_billing_corrections correction_type='credit_note'

    Step 6: Create lib/markos/saas/billing/corrections/write-off.ts:
      - Import buildApprovalPackage
      - Export requestWriteOff({tenantId, invoiceId, amount, reason}, supabase) → ALWAYS calls buildApprovalPackage; trigger blocks INSERT without approval_id
      - Export executeWriteOff({correctionId}, supabase) → on approval: UPDATE saas_invoices.status='uncollectible'; emit evidence pack with full audit trail

    Step 7: Create lib/markos/saas/billing/corrections/dunning.ts:
      - Export startDunning({tenantId, invoiceId, subscriptionId, processor}, supabase) → INSERT saas_dunning_schedules
      - Export DUNNING_DAY_HANDLERS = {0: handleDay0, 3: handleDay3, 5: handleDay5, 7: handleDay7, 8: handleDay8, 38: handleDay38, 45: handleDay45}
      - handleDay0: UPDATE saas_invoices.status='past_due'; emit email via P223 stub
      - handleDay3: smart retry via Plan 02 stripe-client OR Plan 03 mercadopago-preapproval
      - handleDay5: escalation email + INSERT P207 markos_agent_runs at-risk flag (soft-degrade if P207 absent)
      - handleDay7: final retry
      - handleDay8: UPDATE saas_subscriptions.status='unpaid' (P214); status='access_suspended'
      - handleDay38: data deletion warning email
      - handleDay45: trigger P206 governance_deletion_workflow (soft if P206 absent)
      - Export advanceDunning(scheduleId, supabase) → calculates day-since-failure; calls appropriate handler; UPDATEs saas_dunning_schedules.next_retry_at
      - Export pauseDunning + resumeDunning + overrideDunning (last requires approval — calls buildApprovalPackage)

    Step 8: Create lib/markos/saas/billing/corrections/evidence-pack.ts:
      - Export createBillingCorrectionEvidence({tenantId, correctionId, action, actor, processorResponse}, supabase) → fetches saas_billing_corrections row; if governance_evidence_packs (P206) exists INSERT row {action, actor, timestamp, original_invoice_id, correction_type, amount, currency, reason, approval_id, processor_response: sanitizeBillingResponse(processorResponse)}; else fallback to UPDATE saas_billing_corrections.outcome_jsonb stub; returns evidence_pack_ref uuid

    Step 9: Create lib/markos/saas/billing/corrections/index.cjs CommonJS twin.

    Step 10: Create api/v1/saas/billing/corrections.js — GET list + POST router by correction_type to refund/credit-note/write-off/discount; require admin role; sanitize responses.

    Step 11: Create api/v1/saas/billing/dunning.js — GET schedules; POST {action, scheduleId} → routes to pauseDunning/resumeDunning/overrideDunning.

    Step 12: Create api/cron/saas-billing-dunning-runner.js — every-6h cron; iterates active dunning schedules WHERE next_retry_at <= NOW(); calls advanceDunning per row; uses x-markos-cron-secret header matching MARKOS_SAAS_BILLING_CRON_SECRET env per api/cron/mcp-kpi-digest.js pattern; emits AgentRun event kind='saas-billing-dunning-runner'.

    Step 13: Create 3 contract YAMLs (F-281 + F-282 + F-283). Append 3 to contracts/flow-registry.json.

    Step 14: Add billingCorrectionFactory + dunningScheduleFactory to test/fixtures/saas-215/.

    Step 15: Create 9 test files:
      - correction-shape.test.js: zod parity vs F-281
      - correction-approval-trigger.test.js: Postgres-backed — INSERT correction_type='refund' AND approval_id IS NULL → exception 'BILLING_CORRECTION_REQUIRES_APPROVAL'; with approval_id → succeeds; UPDATE setting approved_at AND evidence_pack_ref IS NULL → exception
      - refund-pricing-engine-routing.test.js: requestRefund with sentinel → succeeds; without FK or sentinel → blocks
      - credit-note-flow.test.js: createCreditNote US → calls Plan 02 createCreditMemoInQBO; CO → calls Plan 03 syncCreditNoteToSiigo
      - write-off-approval-flow.test.js: requestWriteOff without approval → blocks; with approval → status='uncollectible' + evidence pack created
      - dunning-state-machine.test.js: 5 status transitions (active→resolved/failed/paused/expired; paused→active)
      - dunning-schedule-day-0-to-45.test.js: simulates each day-action per RESEARCH workflow table
      - evidence-pack-creation.test.js: governance_evidence_packs row created with sanitized processor_response (no credentials present)
      - save-offer-pricing-sentinel.test.js: discount with sentinel valid; without FK or sentinel invalid

    Step 16: Create test/saas-215/rls/correction-cross-tenant-denial.test.js.

    Step 17: Append Plan 05 row to 215-VALIDATION.md.
  </action>
  <verify>
    <automated>npm test -- test/saas-215/domain-5/ test/saas-215/rls/correction-cross-tenant-denial.test.js</automated>
  </verify>
  <done>Migration 122 applies cleanly; saas_billing_corrections + saas_dunning_schedules exist with RLS + BILLING_CORRECTION_REQUIRES_APPROVAL trigger; 5 correction workflows route via buildApprovalPackage; dunning state machine implements day-0-to-45 workflow; cron uses x-markos-cron-secret pattern; F-281 + F-282 + F-283 in flow-registry.json; 9 domain-5 + 1 RLS test files green; 215-VALIDATION.md updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator→/v1/saas/billing/corrections POST | Operator submits correction request; goes through buildApprovalPackage (P208) approval gate before persistence |
| BILLING_CORRECTION_REQUIRES_APPROVAL trigger | DB-level enforcement; cannot bypass via direct SQL; refund/write_off/discount blocked without approval_id |
| processor refund→evidence pack | processor_response sanitized via Plan 01 sanitizeBillingResponse before persisting in governance_evidence_packs |
| dunning cron→x-markos-cron-secret | Header matches MARKOS_SAAS_BILLING_CRON_SECRET env; service-role-scoped writes |
| save offer→Pricing Engine | offer_details = sentinel `{{MARKOS_PRICING_ENGINE_PENDING}}` until P205; pricing_recommendation_id FK enforced post-P205 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-215-05-01 | T | Refund without approval | mitigate | BILLING_CORRECTION_REQUIRES_APPROVAL DB-trigger blocks INSERT without approval_id for refund/write_off/discount |
| T-215-05-02 | T | Approval bypass via direct UPDATE setting approved_at | mitigate | Trigger blocks UPDATE setting approved_at without approval_id + evidence_pack_ref |
| T-215-05-03 | I | Processor response leak via evidence pack | mitigate | sanitizeBillingResponse strips credentials before persist; evidence pack contains only business fields |
| T-215-05-04 | E | Operator override of dunning escalation | mitigate | overrideDunning requires buildApprovalPackage approval; not directly bypassable |
| T-215-05-05 | T | Save offer bypassing Pricing Engine | mitigate | Discount requires pricing_recommendation_id FK or sentinel; app-layer + future DB-trigger enforcement |
| T-215-05-06 | R | Dunning cron silent failure | mitigate | AgentRun event emitted on each invocation; markos_audit_log_hash_chain captures runs |
| T-215-05-07 | I | Cross-tenant correction read | mitigate | RLS sbc_tenant_isolation + sds_tenant_isolation |
| T-215-05-08 | D | Dunning runner mass write | accept | rate-limited to active schedules with next_retry_at <= NOW(); subscription count bounded |
| T-215-05-09 | T | Write-off without operator review | mitigate | Always requires buildApprovalPackage approval; UPDATE saas_invoices.status='uncollectible' only after approved_at set |
</threat_model>

<verification>
- node scripts/preconditions/215-05-check-upstream.cjs (exits 0)
- supabase migration up to slot 122 applies cleanly
- npm test -- test/saas-215/domain-5/
- npm test -- test/saas-215/rls/correction-cross-tenant-denial.test.js
</verification>

<success_criteria>
- saas_billing_corrections + saas_dunning_schedules tables exist with RLS + BILLING_CORRECTION_REQUIRES_APPROVAL trigger
- 5 correction workflows operational; refund/write_off/discount require buildApprovalPackage approval
- Dunning state machine + day-0-to-45 workflow implemented
- Save offer Pricing Engine sentinel enforced (SAS-04)
- SOC2 evidence pack created for every correction (governance_evidence_packs); processor_response sanitized
- Dunning cron runs every 6h via x-markos-cron-secret pattern
- F-281 + F-282 + F-283 in flow-registry.json
- 9 domain-5 + 1 RLS test files green
- 215-VALIDATION.md per-task verification map updated for Plan 05
</success_criteria>

<output>
After completion, create `.planning/phases/215-saas-suite-billing-payments-compliance/215-05-SUMMARY.md` documenting: artifacts created, BILLING_CORRECTION_REQUIRES_APPROVAL exception name, 5 correction_type workflows, dunning day-0-to-45 state machine, P208 buildApprovalPackage REUSE pattern, COMP-01 co_owns_with P206 ownership clarification, save offer Pricing Engine sentinel pattern (SAS-04), downstream-phase impact (Plan 06 closeout regression verifies all 5 P215 active-schema DB-triggers ATTACHED + ENABLED).
</output>
</content>
