---
phase: 215
plan: "03"
type: execute
wave: 3
depends_on: ["01", "02"]
f_ids:
  - F-277
  - F-278
  - F-279
migration_slots:
  - 120
integrates_with:
  - "PRC-01..09 from P205 (saas_invoices.pricing_recommendation_id FK or sentinel; IVA/retention rates may use P205-approved per-tenant overrides in v4.2.0)"
  - "TASK-01..05 from P208 (DIAN_REJECTION_CREATES_P1_TASK trigger creates approval_tasks row; P208 substrate soft-fallback handled per Assumption A4)"
translation_gate_for: []
files_modified:
  - test/fixtures/saas-215/index.js
  - test/fixtures/saas-215/dian-config.js
  - scripts/preconditions/215-03-check-upstream.cjs
  - supabase/migrations/120_saas_billing_co_path.sql
  - lib/markos/saas/billing/co/contracts.ts
  - lib/markos/saas/billing/co/contracts.cjs
  - lib/markos/saas/billing/co/mercadopago-client.ts
  - lib/markos/saas/billing/co/mercadopago-preapproval.ts
  - lib/markos/saas/billing/co/siigo-client.ts
  - lib/markos/saas/billing/co/siigo-sync.ts
  - lib/markos/saas/billing/co/dian-cufe.ts
  - lib/markos/saas/billing/co/dian-wizard.ts
  - lib/markos/saas/billing/co/colombia-tax.ts
  - lib/markos/saas/billing/co/index.cjs
  - api/v1/saas/billing/co-config.js
  - api/v1/saas/billing/dian-wizard.js
  - contracts/F-277-saas-mercadopago-config-v1.yaml
  - contracts/F-278-saas-siigo-config-v1.yaml
  - contracts/F-279-saas-dian-config-v1.yaml
  - contracts/flow-registry.json
  - test/saas-215/domain-3/mercadopago-config-shape.test.js
  - test/saas-215/domain-3/siigo-config-shape.test.js
  - test/saas-215/domain-3/dian-config-shape.test.js
  - test/saas-215/domain-3/dian-invoice-trigger.test.js
  - test/saas-215/domain-3/dian-rejection-task.test.js
  - test/saas-215/domain-3/cufe-calculation.test.js
  - test/saas-215/domain-3/colombia-tax-iva-retention.test.js
  - test/saas-215/domain-3/dian-wizard-state-machine.test.js
  - test/saas-215/domain-3/credential-vault-trigger-co.test.js
  - test/saas-215/domain-3/mcp-credential-sanitize-co.test.js
  - test/saas-215/domain-3/log-redaction-mp.test.js
  - test/saas-215/rls/dian-config-cross-tenant-denial.test.js
  - .planning/phases/215-saas-suite-billing-payments-compliance/215-VALIDATION.md
autonomous: true
requirements:
  - SAS-06
  - QA-01
  - QA-02
  - QA-08
  - QA-09
  - QA-10
must_haves:
  truths:
    - "Plan 03 depends on Plan 01 — assertUpstreamReady CLI hard-checks saas_invoices + saas_credential_vault_refs (Plan 01) tables exist; soft-checks saas_processor_configs + saas_accounting_configs (Plan 02) — Plan 03 INSERTs Mercado Pago row into saas_processor_configs and Siigo row into saas_accounting_configs (Plan 02 created the tables; Plan 03 reuses)"
    - "Migration 120 (Colombia path) ships saas_dian_config table per RESEARCH §Domain 3 §saas_dian_config Table Schema (lines 759-803) — RLS-enabled tenant_id-scoped + UNIQUE(tenant_id) (one DIAN config per tenant); columns: dian_config_id PK uuid, tenant_id FK markos_orgs UNIQUE, nit text NOT NULL (NIT sin DV), nit_dv text NOT NULL (digito verificador), razon_social text NOT NULL, regimen_tributario CHECK 4 values ('responsable_iva','no_responsable_iva','gran_contribuyente','auto_retenedor'), ciudad text NOT NULL, departamento text NOT NULL, direccion text NOT NULL, software_id text, software_pin text (the technical key ClTec — but NOTE: the actual ClTec value is stored in saas_credential_vault_refs as credential_name='dian_authorization_token'; this column may store a vault-ref pointer or be NULL with the value-of-record in vault), numero_resolucion text, rango_desde bigint, rango_hasta bigint, fecha_inicio_resolucion date, fecha_fin_resolucion date, certificate_expires_at timestamptz, environment CHECK ('habilitacion','production'), siigo_config_id uuid REFERENCES saas_accounting_configs(config_id), wizard_state CHECK 6 values ('pending','nit_registered','certificate_uploaded','test_invoice_sent','test_invoice_accepted','production_ready'), test_invoice_cufe text, created_at, updated_at"
    - "DB-trigger DIAN_INVOICE_REQUIRES_CUFE_AND_PROVIDER_RESPONSE ships in migration 120 BEFORE INSERT OR UPDATE on saas_invoices — RAISE EXCEPTION (verbatim per RESEARCH §Domain 3 lines 809-827): billing_country='CO' AND status NOT IN ('draft','void') AND processor='mercado_pago' AND (cufe IS NULL OR dian_provider_response_id IS NULL) → block; defense-in-depth pair with app-layer Siigo sync that populates these fields"
    - "DB-trigger DIAN_REJECTION_CREATES_P1_TASK ships in migration 120 AFTER INSERT on saas_billing_events — verbatim per RESEARCH §Domain 3 lines 835-865: when event_type='dian.rejected' AND processor='dian' → INSERT INTO approval_tasks (tenant_id, task_type='dian_rejection_resolution', priority='P1', title, description, source_table='saas_billing_events', source_id=event_id, sla_at=NOW()+24h, created_at=NOW()) ON CONFLICT DO NOTHING; if approval_tasks does not exist (P208 not landed) → ON CONFLICT swallows error + RAISE NOTICE (degrade gracefully per Assumption A4); SLA = 24-hour response window per canon default"
    - "CUFE (Código Único de Facturación Electrónica) calculation per RESEARCH §Domain 3 lines 870-889: SHA-384(NumFac + FecFac + HorFac + ValFac + CodImp1 + ValImp1 + CodImp2 + ValImp2 + CodImp3 + ValImp3 + ValTot + NitOFE + NumAdq + ClTec) where ClTec (technical key) is fetched from saas_credential_vault_refs at calculation time (credential_name='dian_authorization_token') and is NEVER stored in the invoice record or logged; lib/markos/saas/billing/co/dian-cufe.ts exports calculateCUFE(invoice, dianConfig, supabase) using node:crypto SHA-384"
    - "Mercado Pago direct integration per Q-8 RESOLVED — lib/markos/saas/billing/co/mercadopago-client.ts exports getMercadoPagoClient({tenantId, environment}, supabase) initialized request-scoped via Plan 01 resolveCredential (mercadopago_access_token from Vault); supports Preapproval API for recurring billing (external_reference=subscription_id from P214) and Payment API for one-time charges; abstraction layer reservation: saas_processor_configs.fallback_processor_id allows Stripe LATAM swap in v4.2.0 (per Q-8 NOT activated in v4.1.0)"
    - "Mercado Pago Preapproval integration per RESEARCH §Domain 3 lines 895-910: lib/markos/saas/billing/co/mercadopago-preapproval.ts exports createPreapproval + cancelPreapproval + handleMPWebhook (called by Plan 04); 7 MP webhook events (preapproval.authorized → subscription activated; preapproval.paused; preapproval.cancelled → trigger churn; payment.created → create saas_payment_attempts pending; payment.approved → update payment_attempts + trigger DIAN invoice via Siigo + Siigo sync; payment.failed → update + start dunning; payment.refunded → create credit note invoice)"
    - "Siigo integration (Q-6 RESOLVED — Siigo first for v4.1.0, Alegra deferred to v4.2.0) — lib/markos/saas/billing/co/siigo-client.ts exports getSiigoClient({tenantId, environment}, supabase) initialized request-scoped via resolveCredential (siigo_api_key); siigo-sync.ts exports issueDIANInvoiceViaSiigo({tenantId, invoiceId}, supabase) — calls Siigo /v1/invoices endpoint with electronic invoice payload (UBL 2.1 XML or JSON shape), receives back CUFE + provider_response_id + electronic_invoice_pdf_url; UPDATE saas_invoices SET cufe = response.cufe, dian_provider_response_id = response.id, electronic_invoice_xml = response.xml, electronic_invoice_pdf_url = response.pdf_url, dian_transmission_state = 'transmitted'; on Siigo error response with DIAN rejection: write saas_billing_events row with event_type='dian.rejected' (DIAN_REJECTION_CREATES_P1_TASK trigger fires)"
    - "Colombian tax math (IVA + retentions per RESEARCH §Domain 1 saas_invoices retention columns) — lib/markos/saas/billing/co/colombia-tax.ts exports calculateColombianTax({subtotal, regimenTributario, customerType, hasReteIVA, hasReteFuente, hasReteICA, ica_municipality}) returning {iva_rate, iva_amount, retefuente_rate, retefuente_amount, reteiva_rate, reteiva_amount, reteica_rate, reteica_amount, tax_total}; default IVA rate 0.19 (19%) for standard SaaS; retentions applied based on customer regimen_tributario + tenant configuration; rates documented as hard-coded for v4.1.0 (manual review flagged in MANUAL-ONLY for regulatory drift)"
    - "DIAN setup wizard (6-step state machine per RESEARCH §Domain 3 lines 920-929) — lib/markos/saas/billing/co/dian-wizard.ts exports advanceWizardState({tenantId, action, payload}, supabase) with strict state transitions: pending → nit_registered (operator submits NIT/dv/razon_social/regimen) → certificate_uploaded (operator uploads PKCS12 cert + password; STORED via storeCredentialVaultRef as 'dian_pkcs12_certificate' + 'dian_pkcs12_password' — NEVER plaintext anywhere; certificate_expires_at extracted from cert) → test_invoice_sent (generate + transmit DIAN test invoice to habilitación via Siigo) → test_invoice_accepted (when DIAN responds with valid CUFE; test_invoice_cufe stored) → production_ready (operator explicitly switches environment='production' — MANUAL-ONLY verification gate)"
    - "DIAN provider response state machine — saas_invoices.dian_transmission_state CHECK 5 values ('pending','transmitted','accepted','rejected','void'); transitions: pending → transmitted (after Siigo issueDIANInvoiceViaSiigo) → accepted (when DIAN webhook responds) OR rejected (creates P1 task via DIAN_REJECTION_CREATES_P1_TASK trigger); void only via Plan 05 correction workflow (BILLING_CORRECTION_REQUIRES_APPROVAL trigger applies)"
    - "6 credential ENUMs for Plan 03 (Colombia path) ALL flow through Plan 01 storeCredentialVaultRef → saas_credential_vault_refs (BILLING_CREDENTIAL_REQUIRES_VAULT_REF DB-trigger validates pattern): mercadopago_access_token, mercadopago_webhook_secret, siigo_api_key, dian_pkcs12_certificate (base64-encoded PKCS12 cert; LARGE — Vault primary, pgcrypto fallback acceptable), dian_pkcs12_password, dian_authorization_token (the ClTec technical key for CUFE calculation)"
    - "Defense-in-depth credential test — for each of 6 CO credentials INSERT plaintext into saas_credential_vault_refs → expect EXCEPTION 'BILLING_CREDENTIAL_REQUIRES_VAULT_REF'; INSERT through storeCredentialVaultRef → succeeds; resolveCredential('dian_pkcs12_password', ...) returns plaintext to in-process caller (used by CUFE calculator); plaintext NEVER appears in any saas_billing_events.payload_redacted, MCP output, or LLM context (HIGH 2 stack RE-applied)"
    - "MCP credential sanitization test — invokes get_invoice MCP tool with Colombia DIAN invoice fixture; asserts no mercadopago_access_token / mercadopago_webhook_secret / siigo_api_key / dian_pkcs12_certificate / dian_pkcs12_password / dian_authorization_token appears in output; ALSO asserts cufe is RETURNED (cufe is not a credential — it's a public legal field); ALSO asserts software_pin column value is REDACTED (it's a vault pointer / sensitive field)"
    - "Log redaction test — invokes redactWebhookPayload(mpPayload, 'mercado_pago') with payload containing email/first_name/last_name/phone/identification/card → all replaced with '[REDACTED]'; saas_billing_events.payload_redacted always stores redacted version"
    - "3 contracts ship — F-277 (saas-mercadopago-config-v1) covering Mercado Pago row in saas_processor_configs + Preapproval/Payment/Checkout Bricks integration + 7 MP webhook events; F-278 (saas-siigo-config-v1) covering Siigo row in saas_accounting_configs + DIAN issuance pattern + UBL 2.1 XML schema reference + Alegra reservation note (v4.2.0); F-279 (saas-dian-config-v1) covering saas_dian_config table + 6-step wizard state machine + DIAN_INVOICE_REQUIRES_CUFE_AND_PROVIDER_RESPONSE + DIAN_REJECTION_CREATES_P1_TASK exceptions + CUFE SHA-384 formula + 6 CO credential ENUMs; NO collision with F-001..F-276"
    - "API handlers — api/v1/saas/billing/co-config.js (GET sanitized status of Mercado Pago + Siigo configs; POST stores credentials via Plan 01 storeCredentialVaultRef) + api/v1/saas/billing/dian-wizard.js (GET wizard state; POST advances wizard step — operator submits payload; advanceWizardState validates state transition; certificate upload uses multipart form-data with PKCS12 + password; password ALWAYS stored via storeCredentialVaultRef on receipt, request body cleared from log)"
    - "Cross-tenant denial test (test/saas-215/rls/dian-config-cross-tenant-denial.test.js) — INSERT saas_dian_config under tenant A → SELECT under tenant B returns 0 rows; same for Mercado Pago + Siigo rows in saas_processor_configs / saas_accounting_configs (RE-RUN of Plan 02 RLS test confirms isolation holds across tenants for tax-jurisdiction-different rows)"
    - "PRC-01..09 + TASK-01..05 documented in frontmatter as `integrates_with` (NOT `requirements`) — P205 OWNS Pricing Engine substrate; P208 OWNS approval task substrate; P215 Plan 03 INSERTs into approval_tasks via DB-trigger when DIAN rejects (P208 must be landed for P1 task creation; soft-fallback degrades gracefully per Assumption A4)"
    - "215-VALIDATION.md per-task verification map updated with rows for Plan 03 + domain-3 test paths under `test/saas-215/domain-3/`"
  artifacts:
    - path: scripts/preconditions/215-03-check-upstream.cjs
      provides: invokes assertUpstreamReady from Plan 01; HARD = [saas_invoices + saas_credential_vault_refs (Plan 01), saas_suite_activations + saas_subscriptions (P214)]; SOFT = [saas_processor_configs + saas_accounting_configs (Plan 02 — required for INSERT but degrade gracefully if absent), pricing_recommendations (P205), approval_tasks (P208 — DIAN P1 task target), governance_evidence_packs (P206)]
    - path: supabase/migrations/120_saas_billing_co_path.sql
      provides: combined migration — saas_dian_config SOR table + DIAN_INVOICE_REQUIRES_CUFE_AND_PROVIDER_RESPONSE DB-trigger + DIAN_REJECTION_CREATES_P1_TASK DB-trigger + RLS policy + UNIQUE(tenant_id)
    - path: lib/markos/saas/billing/co/contracts.ts
      provides: zod schemas + enum constants — MP_WEBHOOK_EVENTS (7 per RESEARCH lines 902-910) + DIAN_TRANSMISSION_STATES (5) + WIZARD_STATES (6) + REGIMEN_TRIBUTARIO (4) + CO_CREDENTIAL_FIELDS (6) + MercadoPagoProcessorConfigSchema + SiigoAccountingConfigSchema + DianConfigSchema + ColombianTaxBreakdownSchema zod parity with F-277/F-278/F-279 YAMLs
      exports: [MP_WEBHOOK_EVENTS, DIAN_TRANSMISSION_STATES, WIZARD_STATES, REGIMEN_TRIBUTARIO, CO_CREDENTIAL_FIELDS, MercadoPagoProcessorConfigSchema, SiigoAccountingConfigSchema, DianConfigSchema, ColombianTaxBreakdownSchema]
    - path: lib/markos/saas/billing/co/mercadopago-client.ts
      provides: getMercadoPagoClient({tenantId, environment}, supabase) returns request-scoped MP SDK instance; uses mercadopagoSDK + access_token from Plan 01 resolveCredential; access_token validated as starts with 'TEST-' (sandbox) or 'APP_USR-' (production)
      exports: [getMercadoPagoClient]
    - path: lib/markos/saas/billing/co/mercadopago-preapproval.ts
      provides: createPreapproval({tenantId, subscriptionId, amount, currency='COP', frequency, ...}, supabase) → calls MP Preapproval API with external_reference=subscriptionId; cancelPreapproval; handleMPWebhook(event, supabase) — routes 7 MP event types to appropriate domain action; logs via redactWebhookPayload(payload, 'mercado_pago')
      exports: [createPreapproval, cancelPreapproval, handleMPWebhook]
    - path: lib/markos/saas/billing/co/siigo-client.ts
      provides: getSiigoClient({tenantId, environment}, supabase) returns request-scoped Siigo API client; uses siigo_api_key from Plan 01 resolveCredential
      exports: [getSiigoClient]
    - path: lib/markos/saas/billing/co/siigo-sync.ts
      provides: issueDIANInvoiceViaSiigo({tenantId, invoiceId}, supabase) → fetches saas_invoices + saas_dian_config + customer; constructs Siigo electronic invoice payload (UBL 2.1 derivative); calls Siigo /v1/invoices; UPDATE saas_invoices SET cufe + dian_provider_response_id + electronic_invoice_xml + electronic_invoice_pdf_url + dian_transmission_state='transmitted'; on rejection: INSERT saas_billing_events event_type='dian.rejected' (triggers DIAN_REJECTION_CREATES_P1_TASK); also: syncCreditNoteToSiigo + handleSiigoWebhook helpers
      exports: [issueDIANInvoiceViaSiigo, syncCreditNoteToSiigo, handleSiigoWebhook]
    - path: lib/markos/saas/billing/co/dian-cufe.ts
      provides: calculateCUFE({invoice, dianConfig}, supabase) → fetches ClTec via Plan 01 resolveCredential('dian_authorization_token'); concatenates 14 fields per RESEARCH §Domain 3 CUFE Calculation; computes SHA-384 via node:crypto; returns 96-character hex CUFE; ClTec NEVER logged or returned in any field other than the SHA-384 computation
      exports: [calculateCUFE]
    - path: lib/markos/saas/billing/co/dian-wizard.ts
      provides: advanceWizardState({tenantId, action, payload}, supabase) — strict 6-state state machine; validates state transition is legal; for certificate_upload action immediately stores PKCS12 + password via Plan 01 storeCredentialVaultRef before persisting any other state
      exports: [advanceWizardState, WIZARD_STATE_TRANSITIONS]
    - path: lib/markos/saas/billing/co/colombia-tax.ts
      provides: calculateColombianTax({subtotal, regimenTributario, customerType, hasReteIVA, hasReteFuente, hasReteICA, ica_municipality}) returning {iva_rate, iva_amount, retefuente_rate, retefuente_amount, reteiva_rate, reteiva_amount, reteica_rate, reteica_amount, tax_total}; default IVA 0.19; retention rates from canon table; municipal ICA rates (Bogotá 0.0114, Medellín, Cali, etc.)
      exports: [calculateColombianTax, IVA_RATE_DEFAULT, RETENTION_RATES]
    - path: lib/markos/saas/billing/co/index.cjs
      provides: CommonJS twin re-exporting full surface
    - path: api/v1/saas/billing/co-config.js
      provides: GET /v1/saas/billing/co-config (sanitized Mercado Pago + Siigo status); POST /v1/saas/billing/co-config (operator stores credentials via storeCredentialVaultRef); legacy api/*.js handler with requireHostedSupabaseAuth + sanitizeBillingResponse
    - path: api/v1/saas/billing/dian-wizard.js
      provides: GET /v1/saas/billing/dian-wizard (returns saas_dian_config wizard_state + sanitized config); POST /v1/saas/billing/dian-wizard {action, payload} (advances state via advanceWizardState; certificate upload accepts PKCS12 multipart + password — both immediately stored via storeCredentialVaultRef on receipt)
    - path: contracts/F-277-saas-mercadopago-config-v1.yaml
      provides: documents Mercado Pago row in saas_processor_configs schema + Preapproval API + 7 MP webhook events + 2 MP credential fields (mercadopago_access_token + mercadopago_webhook_secret); abstraction layer reservation for Stripe LATAM swap (v4.2.0)
    - path: contracts/F-278-saas-siigo-config-v1.yaml
      provides: documents Siigo row in saas_accounting_configs schema + DIAN issuance pattern + UBL 2.1 XML reference + 1 Siigo credential field (siigo_api_key); Alegra reservation note (v4.2.0)
    - path: contracts/F-279-saas-dian-config-v1.yaml
      provides: documents saas_dian_config table + 6-step wizard state machine + DIAN_INVOICE_REQUIRES_CUFE_AND_PROVIDER_RESPONSE + DIAN_REJECTION_CREATES_P1_TASK exception names + CUFE SHA-384 formula + 3 DIAN credential fields (dian_pkcs12_certificate + dian_pkcs12_password + dian_authorization_token)
    - path: contracts/flow-registry.json
      provides: 3 entries appended for F-277 + F-278 + F-279; NO collision with F-001..F-276
    - path: test/fixtures/saas-215/dian-config.js
      provides: dianConfigFactory({tenantId, nit, nitDv, razonSocial, regimenTributario='responsable_iva', wizardState='pending', environment='habilitacion', ...}) returning a row shape
    - path: test/saas-215/domain-3/mercadopago-config-shape.test.js
      provides: zod parity assertion against F-277 YAML
    - path: test/saas-215/domain-3/siigo-config-shape.test.js
      provides: zod parity assertion against F-278 YAML
    - path: test/saas-215/domain-3/dian-config-shape.test.js
      provides: zod parity assertion against F-279 YAML
    - path: test/saas-215/domain-3/dian-invoice-trigger.test.js
      provides: Postgres-backed test — INSERT saas_invoices with billing_country='CO' + processor='mercado_pago' + status='open' + cufe IS NULL → exception 'DIAN_INVOICE_REQUIRES_CUFE_AND_PROVIDER_RESPONSE'; with cufe + dian_provider_response_id populated → succeeds; status='draft' → trigger skips
    - path: test/saas-215/domain-3/dian-rejection-task.test.js
      provides: Postgres-backed test — INSERT saas_billing_events with event_type='dian.rejected' AND processor='dian' → AFTER INSERT trigger fires → INSERT into approval_tasks with priority='P1' + sla_at=NOW()+24h + task_type='dian_rejection_resolution'; if approval_tasks does not exist (P208 not landed) trigger ON CONFLICT swallows error + RAISE NOTICE (graceful degrade)
    - path: test/saas-215/domain-3/cufe-calculation.test.js
      provides: calculateCUFE with fixture invoice + dianConfig + mocked resolveCredential('dian_authorization_token') → returns 96-character hex CUFE; SHA-384 of concatenated fields per RESEARCH §Domain 3 CUFE Calculation; ClTec NEVER appears in any return value or thrown error
    - path: test/saas-215/domain-3/colombia-tax-iva-retention.test.js
      provides: calculateColombianTax with various regimenTributario + customerType combinations → returns expected IVA + retention rates per canon table; default IVA 0.19; gran_contribuyente exempt from reteIVA; etc.
    - path: test/saas-215/domain-3/dian-wizard-state-machine.test.js
      provides: advanceWizardState with valid transitions (pending → nit_registered → certificate_uploaded → test_invoice_sent → test_invoice_accepted → production_ready) succeeds; invalid transitions (pending → production_ready directly) throws WizardStateTransitionError; certificate_upload action stores PKCS12 + password via storeCredentialVaultRef before persisting wizard_state
    - path: test/saas-215/domain-3/credential-vault-trigger-co.test.js
      provides: 6 CO credentials INSERT plaintext → exception 'BILLING_CREDENTIAL_REQUIRES_VAULT_REF'; via storeCredentialVaultRef → succeeds with vault:// or pgcrypto-ref: pattern; PKCS12 cert (large base64 blob) succeeds via pgcrypto fallback if Vault has size limit
    - path: test/saas-215/domain-3/mcp-credential-sanitize-co.test.js
      provides: get_invoice MCP tool with Colombia DIAN invoice fixture → 6 CO credential fields absent from output; cufe present (public legal field); software_pin redacted (sensitive)
    - path: test/saas-215/domain-3/log-redaction-mp.test.js
      provides: redactWebhookPayload(mpPayload, 'mercado_pago') → email/first_name/last_name/phone/identification/card all '[REDACTED]'
    - path: test/saas-215/rls/dian-config-cross-tenant-denial.test.js
      provides: INSERT saas_dian_config under tenant A; SELECT under tenant B returns 0 rows
  key_links:
    - from: lib/markos/saas/billing/co/dian-cufe.ts
      to: lib/markos/saas/billing/credential-vault.ts (Plan 01)
      via: imports resolveCredential — fetches dian_authorization_token (ClTec) at calculation time; ClTec is concatenated into SHA-384 input but NEVER returned or logged
      pattern: "resolveCredential"
    - from: lib/markos/saas/billing/co/siigo-sync.ts
      to: supabase saas_invoices.cufe + dian_provider_response_id + dian_transmission_state
      via: issueDIANInvoiceViaSiigo updates these columns; defense-in-depth pair with DIAN_INVOICE_REQUIRES_CUFE_AND_PROVIDER_RESPONSE trigger from migration 120
      pattern: "DIAN_INVOICE_REQUIRES_CUFE_AND_PROVIDER_RESPONSE"
    - from: lib/markos/saas/billing/co/siigo-sync.ts
      to: supabase saas_billing_events
      via: on Siigo error response with DIAN rejection → INSERT saas_billing_events event_type='dian.rejected' → triggers DIAN_REJECTION_CREATES_P1_TASK → INSERT approval_tasks
      pattern: "dian.rejected"
    - from: lib/markos/saas/billing/co/dian-wizard.ts
      to: lib/markos/saas/billing/credential-vault.ts (Plan 01)
      via: certificate_upload action calls storeCredentialVaultRef('dian_pkcs12_certificate', ...) AND storeCredentialVaultRef('dian_pkcs12_password', ...) BEFORE persisting wizard_state='certificate_uploaded'
      pattern: "storeCredentialVaultRef"
    - from: api/v1/saas/billing/dian-wizard.js
      to: lib/markos/saas/billing/co/dian-wizard.ts
      via: POST handler routes wizard advancement; multipart PKCS12 upload immediately delegated to Plan 01 vault storage
      pattern: "advanceWizardState"
---

<objective>
Ship Wave 2 Colombia billing path: saas_dian_config SOR table in migration 120 + DIAN_INVOICE_REQUIRES_CUFE_AND_PROVIDER_RESPONSE DB-trigger (verbatim per RESEARCH §Domain 3 — defense-in-depth pair with Siigo sync) + DIAN_REJECTION_CREATES_P1_TASK DB-trigger (verbatim — creates P208 approval_tasks row with 24h SLA on DIAN rejection event), Mercado Pago direct integration with abstraction layer reservation for v4.2.0 Stripe LATAM swap (Q-8 RESOLVED — direct + abstraction), Siigo electronic invoice issuance for DIAN compliance (Q-6 RESOLVED — Siigo first, Alegra deferred to v4.2.0), DIAN CUFE SHA-384 calculation library (lib/markos/saas/billing/co/dian-cufe.ts using node:crypto + 14-field input per RESEARCH; ClTec technical key fetched from Plan 01 Vault per calculation, NEVER logged), Colombian tax math library (calculateColombianTax for IVA + retefuente + reteIVA + reteICA with default IVA 0.19 and regimen-tributario-aware retention rates), DIAN setup wizard 6-step state machine (lib/markos/saas/billing/co/dian-wizard.ts; certificate upload immediately stores PKCS12 + password via Plan 01 storeCredentialVaultRef), 3 F-ID contracts (F-277 mercadopago-config + F-278 siigo-config + F-279 dian-config), 6 CO credential ENUMs flowing through Plan 01 saas_credential_vault_refs (mercadopago_access_token + mercadopago_webhook_secret + siigo_api_key + dian_pkcs12_certificate + dian_pkcs12_password + dian_authorization_token) — all 5 HIGH 2 defense-in-depth layers RE-applied (Layer 1 storage + Layer 2 DB-trigger + Layer 3 MCP sanitization + Layer 4 log redaction with MP_PII_FIELDS / SIIGO_PII_FIELDS / DIAN_PII_FIELDS + Layer 5 LLM shield), 2 API handlers (api/v1/saas/billing/co-config.js + dian-wizard.js), and the cross-tenant RLS denial RE-RUN.

Purpose: satisfy SAS-06 (Launch-country compliance — Colombia Mercado Pago + DIAN/Siigo + IVA/retefuente/reteIVA/reteICA tax compliance + DIAN rejection P1 task creation within 24h SLA); QA-01..02 + QA-08..10 (architecture-lock RE-RUN + RLS isolation + tax compliance + credential sanitization + DIAN P1 incident response). Plan 03 unblocks Plan 04 (webhook handler routing) for Mercado Pago + Siigo + DIAN events — Plan 04 will REUSE Plan 03's mercadopago-preapproval.ts + siigo-sync.ts handlers.

Output: 1 preflight CLI + 1 SOR migration (120 — saas_dian_config + DIAN_INVOICE_REQUIRES_CUFE_AND_PROVIDER_RESPONSE + DIAN_REJECTION_CREATES_P1_TASK 2 triggers) + 9 co-path lib files + 2 API handlers + 3 contract YAMLs + flow-registry update + 1 fixture file + 11 domain-3 test files + 1 RLS test + populated VALIDATION.md.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/215-saas-suite-billing-payments-compliance/215-CONTEXT.md
@.planning/phases/215-saas-suite-billing-payments-compliance/215-RESEARCH.md
@.planning/phases/215-saas-suite-billing-payments-compliance/215-VALIDATION.md
@.planning/phases/215-saas-suite-billing-payments-compliance/215-01-PLAN.md
@.planning/phases/215-saas-suite-billing-payments-compliance/215-02-PLAN.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@lib/markos/saas/billing/preflight/index.cjs
@lib/markos/saas/billing/index.cjs
@lib/markos/mcp/tools/saas-billing.cjs
@onboarding/backend/runtime-context.cjs

<interfaces>
<!-- Plan 01 + Plan 02 surfaces (DEPENDENCY) — Plan 03 imports from these -->

From lib/markos/saas/billing/credential-vault.ts (Plan 01):

```typescript
function storeCredentialVaultRef({tenantId, credentialName, plaintextValue, configTable, configId}, supabase): Promise<{vault_ref: string}>
function resolveCredential({tenantId, credentialName, configId}, supabase): Promise<string>
const CREDENTIAL_FIELDS  // 12 + 'vault_ref'
```

CUFE Calculation Formula (RESEARCH §Domain 3 lines 870-889):

```text
CUFE = SHA-384(
  NumFac      -- invoice number
  + FecFac    -- issue date YYYY-MM-DD
  + HorFac    -- issue time HH:MM:SS-05:00
  + ValFac    -- subtotal amount (COP, 2 decimal places)
  + CodImp1   -- '01' (IVA)
  + ValImp1   -- IVA amount
  + CodImp2   -- '04' (INC) if applicable, else '00'
  + ValImp2   -- INC amount (or 0.00)
  + CodImp3   -- '03' (ICA) if applicable, else '00'
  + ValImp3   -- ICA amount (or 0.00)
  + ValTot    -- total amount (COP, 2 decimal places)
  + NitOFE    -- vendor NIT (without check digit)
  + NumAdq    -- customer NIT/CC/CE
  + ClTec     -- technical key from saas_dian_config / vault
)
```

Mercado Pago webhook events (RESEARCH §Domain 3 lines 902-910):

| Event | Action |
|-------|--------|
| preapproval.authorized | subscription activated |
| preapproval.paused | subscription paused |
| preapproval.cancelled | trigger churn workflow |
| payment.created | create saas_payment_attempts (pending) |
| payment.approved | update payment_attempts + trigger Siigo DIAN sync |
| payment.failed | update + start dunning |
| payment.refunded | create credit note invoice |

DIAN wizard state transitions (6 states):

| From | To | Trigger | Notes |
|------|-----|---------|-------|
| pending | nit_registered | operator submits NIT | |
| nit_registered | certificate_uploaded | operator uploads PKCS12 + password | both stored via Plan 01 vault before state advance |
| certificate_uploaded | test_invoice_sent | operator triggers test invoice | calls Siigo with test invoice |
| test_invoice_sent | test_invoice_accepted | DIAN webhook accepts | test_invoice_cufe stored |
| test_invoice_accepted | production_ready | operator switches environment='production' | MANUAL-ONLY gate |

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0.5: Plan 03 preflight</name>
  <files>scripts/preconditions/215-03-check-upstream.cjs</files>
  <read_first>
    - lib/markos/saas/billing/preflight/index.cjs (Plan 01 — REUSE assertUpstreamReady)
    - scripts/preconditions/215-02-check-upstream.cjs (Plan 02 — mirror)
  </read_first>
  <action>
    Create scripts/preconditions/215-03-check-upstream.cjs — invokes assertUpstreamReady from Plan 01; HARD = [saas_invoices + saas_credential_vault_refs (Plan 01), saas_processor_configs + saas_accounting_configs (Plan 02), saas_suite_activations + saas_subscriptions (P214)]; SOFT = [pricing_recommendations (P205), governance_evidence_packs (P206), approval_tasks (P208 — DIAN P1 task target; soft because trigger degrades gracefully via ON CONFLICT)]; matches api/cron/webhooks-dlq-purge.js pattern; exit 1 on hard miss.
  </action>
  <verify>
    <automated>node scripts/preconditions/215-03-check-upstream.cjs</automated>
  </verify>
  <done>Preflight CLI hard-fails on missing Plan 01 or Plan 02 tables; soft-warns on missing P205/P206/P208.</done>
</task>

<task type="auto">
  <name>Task 1: Migration 120 + 9 co-path lib files + Mercado Pago + Siigo + DIAN CUFE + Colombian tax + DIAN wizard + 3 contracts + 2 API handlers + domain-3 + RLS tests</name>
  <files>supabase/migrations/120_saas_billing_co_path.sql, lib/markos/saas/billing/co/contracts.ts, lib/markos/saas/billing/co/contracts.cjs, lib/markos/saas/billing/co/mercadopago-client.ts, lib/markos/saas/billing/co/mercadopago-preapproval.ts, lib/markos/saas/billing/co/siigo-client.ts, lib/markos/saas/billing/co/siigo-sync.ts, lib/markos/saas/billing/co/dian-cufe.ts, lib/markos/saas/billing/co/dian-wizard.ts, lib/markos/saas/billing/co/colombia-tax.ts, lib/markos/saas/billing/co/index.cjs, api/v1/saas/billing/co-config.js, api/v1/saas/billing/dian-wizard.js, contracts/F-277-saas-mercadopago-config-v1.yaml, contracts/F-278-saas-siigo-config-v1.yaml, contracts/F-279-saas-dian-config-v1.yaml, contracts/flow-registry.json, test/fixtures/saas-215/index.js, test/fixtures/saas-215/dian-config.js, test/saas-215/domain-3/mercadopago-config-shape.test.js, test/saas-215/domain-3/siigo-config-shape.test.js, test/saas-215/domain-3/dian-config-shape.test.js, test/saas-215/domain-3/dian-invoice-trigger.test.js, test/saas-215/domain-3/dian-rejection-task.test.js, test/saas-215/domain-3/cufe-calculation.test.js, test/saas-215/domain-3/colombia-tax-iva-retention.test.js, test/saas-215/domain-3/dian-wizard-state-machine.test.js, test/saas-215/domain-3/credential-vault-trigger-co.test.js, test/saas-215/domain-3/mcp-credential-sanitize-co.test.js, test/saas-215/domain-3/log-redaction-mp.test.js, test/saas-215/rls/dian-config-cross-tenant-denial.test.js, .planning/phases/215-saas-suite-billing-payments-compliance/215-VALIDATION.md</files>
  <read_first>
    - .planning/phases/215-saas-suite-billing-payments-compliance/215-RESEARCH.md §Domain 3 §saas_dian_config Table Schema §DB-Trigger Compliance DIAN_INVOICE_REQUIRES_CUFE_AND_PROVIDER_RESPONSE §DB-Trigger Compliance DIAN_REJECTION_CREATES_P1_TASK §CUFE Calculation §Mercado Pago Integration Specifics §DIAN Setup Wizard Steps §Credential ENUM list for Plan 03
    - obsidian/work/incoming/16-SAAS-SUITE.md Part 3 (Mercado Pago) + Part 4 (Colombia Compliance) + §DIAN Facturación Electrónica Step 4 (CUFE formula source)
    - lib/markos/saas/billing/credential-vault.ts (Plan 01)
    - lib/markos/saas/billing/log-redaction.ts (Plan 01 — MP_PII_FIELDS reference)
    - supabase/migrations/118_saas_billing_invoice_contracts.sql (Plan 01 — saas_invoices schema)
    - supabase/migrations/119_saas_billing_us_path.sql (Plan 02 — saas_processor_configs / saas_accounting_configs schema for Mercado Pago / Siigo INSERT)
    - supabase/migrations/76_markos_cli_tenant_env.sql (pgcrypto pattern for PKCS12 fallback if Vault size-limited)
  </read_first>
  <action>
    Step 1: Create supabase/migrations/120_saas_billing_co_path.sql:
      - CREATE TABLE saas_dian_config (full schema per RESEARCH §Domain 3 lines 759-803 — verbatim columns + RLS + UNIQUE(tenant_id))
      - ALTER TABLE saas_dian_config ENABLE ROW LEVEL SECURITY + CREATE POLICY sdc_tenant_isolation
      - CREATE OR REPLACE FUNCTION enforce_dian_invoice_compliance() — verbatim per RESEARCH lines 809-827 (RAISE EXCEPTION 'DIAN_INVOICE_REQUIRES_CUFE_AND_PROVIDER_RESPONSE' on billing_country='CO' AND status NOT IN ('draft','void') AND processor='mercado_pago' AND (cufe IS NULL OR dian_provider_response_id IS NULL))
      - CREATE TRIGGER trg_dian_invoice_compliance BEFORE INSERT OR UPDATE ON saas_invoices FOR EACH ROW EXECUTE FUNCTION enforce_dian_invoice_compliance()
      - CREATE OR REPLACE FUNCTION enforce_dian_rejection_task() — verbatim per RESEARCH lines 835-865 (event_type='dian.rejected' AND processor='dian' → INSERT INTO approval_tasks priority='P1' sla_at=NOW()+24h ON CONFLICT DO NOTHING + RAISE NOTICE)
      - CREATE TRIGGER trg_dian_rejection_task AFTER INSERT ON saas_billing_events FOR EACH ROW EXECUTE FUNCTION enforce_dian_rejection_task()

    Step 2: Create lib/markos/saas/billing/co/contracts.ts exporting MP_WEBHOOK_EVENTS (7) + DIAN_TRANSMISSION_STATES (5) + WIZARD_STATES (6) + REGIMEN_TRIBUTARIO (4) + CO_CREDENTIAL_FIELDS (6) + 4 zod schemas (MercadoPagoProcessorConfigSchema + SiigoAccountingConfigSchema + DianConfigSchema + ColombianTaxBreakdownSchema) parity with F-277/F-278/F-279.

    Step 3: Create lib/markos/saas/billing/co/mercadopago-client.ts:
      - Import resolveCredential from Plan 01
      - Export getMercadoPagoClient({tenantId, environment}, supabase) → fetches mercadopago_access_token via resolveCredential; initializes mercadopago SDK request-scoped; access_token validated as 'TEST-' (sandbox) or 'APP_USR-' (production)

    Step 4: Create lib/markos/saas/billing/co/mercadopago-preapproval.ts:
      - createPreapproval({tenantId, subscriptionId, amount, currency='COP', frequency, customerEmail}, supabase) → calls MP Preapproval API with external_reference=subscriptionId; returns preapproval_id stored on saas_subscriptions
      - cancelPreapproval({tenantId, preapprovalId}, supabase) → calls MP cancel endpoint
      - handleMPWebhook(event, supabase) — switch on event.type for 7 MP events; for each route to appropriate domain action (createPaymentAttempt + recordBillingEvent from Plan 01); calls redactWebhookPayload(event, 'mercado_pago') BEFORE any console.log

    Step 5: Create lib/markos/saas/billing/co/siigo-client.ts:
      - Import resolveCredential
      - Export getSiigoClient({tenantId, environment}, supabase) → fetches siigo_api_key via resolveCredential; initializes axios/fetch client with `https://api.siigo.com/v1` base URL + Authorization header

    Step 6: Create lib/markos/saas/billing/co/siigo-sync.ts:
      - issueDIANInvoiceViaSiigo({tenantId, invoiceId}, supabase) → fetches saas_invoices + saas_dian_config + customer; constructs Siigo electronic invoice payload (UBL 2.1 derivative — Siigo accepts simplified JSON which it converts to UBL XML internally); calls Siigo POST /v1/invoices; on success: UPDATE saas_invoices SET cufe = response.cufe, dian_provider_response_id = response.id, electronic_invoice_xml = response.xml, electronic_invoice_pdf_url = response.pdf_url, dian_transmission_state='transmitted'; on Siigo error response containing DIAN rejection: INSERT saas_billing_events (event_type='dian.rejected', processor='dian', payload_redacted={...}); the AFTER INSERT trigger DIAN_REJECTION_CREATES_P1_TASK fires automatically
      - syncCreditNoteToSiigo({tenantId, originalInvoiceId, amount, reason}, supabase) → calls Siigo credit note endpoint
      - handleSiigoWebhook(event, supabase) → routes Siigo webhook events (DIAN acceptance + rejection notifications)

    Step 7: Create lib/markos/saas/billing/co/dian-cufe.ts:
      - Import resolveCredential + node:crypto
      - Export calculateCUFE({invoice, dianConfig}, supabase) — fetches ClTec via resolveCredential('dian_authorization_token', tenantId, dianConfig.dian_config_id); concatenates 14 fields per RESEARCH formula (NumFac + FecFac + HorFac + ValFac + CodImp1 + ValImp1 + CodImp2 + ValImp2 + CodImp3 + ValImp3 + ValTot + NitOFE + NumAdq + ClTec); computes crypto.createHash('sha384').update(concatenated).digest('hex'); returns 96-char hex string; ClTec NEVER appears in any return path or error message

    Step 8: Create lib/markos/saas/billing/co/dian-wizard.ts:
      - Export advanceWizardState({tenantId, action, payload}, supabase) — strict state machine with validation: pending→nit_registered (action='register_nit' + payload validates regimen + ciudad + departamento); nit_registered→certificate_uploaded (action='upload_certificate' + payload contains base64 PKCS12 + password — IMMEDIATELY calls storeCredentialVaultRef('dian_pkcs12_certificate', cert) AND storeCredentialVaultRef('dian_pkcs12_password', pwd) BEFORE updating saas_dian_config.wizard_state); certificate_uploaded→test_invoice_sent (action='send_test_invoice' — calls issueDIANInvoiceViaSiigo with synthetic test invoice); test_invoice_sent→test_invoice_accepted (action='confirm_test_acceptance' — DIAN webhook OR manual operator confirmation); test_invoice_accepted→production_ready (action='switch_to_production' — MANUAL-ONLY gate; sets environment='production')
      - Export WIZARD_STATE_TRANSITIONS = {pending: ['nit_registered'], nit_registered: ['certificate_uploaded'], ...}; throws WizardStateTransitionError on invalid transition

    Step 9: Create lib/markos/saas/billing/co/colombia-tax.ts:
      - Export calculateColombianTax({subtotal, regimenTributario, customerType, hasReteIVA, hasReteFuente, hasReteICA, ica_municipality}) — IVA = subtotal * 0.19 (default for SaaS); retefuente based on regimen; reteIVA only if customerType='gran_contribuyente' AND tenant has retefuente posture; reteICA based on ica_municipality (Bogotá 0.0114, Medellín 0.0070, Cali 0.0110, etc.)
      - Export IVA_RATE_DEFAULT = 0.19 + RETENTION_RATES = { gran_contribuyente: 0.025, ... } per canon

    Step 10: Create lib/markos/saas/billing/co/index.cjs CommonJS twin.

    Step 11: Create api/v1/saas/billing/co-config.js handler — GET sanitized status; POST stores Mercado Pago + Siigo credentials via Plan 01 storeCredentialVaultRef.

    Step 12: Create api/v1/saas/billing/dian-wizard.js handler — GET wizard state + saas_dian_config sanitized; POST {action, payload} routes to advanceWizardState; multipart PKCS12 upload via formidable or busboy → password and cert immediately stored via storeCredentialVaultRef on receipt; request body cleared from log.

    Step 13: Create 3 contract YAMLs (F-277 + F-278 + F-279). Append 3 entries to contracts/flow-registry.json.

    Step 14: Add dianConfigFactory to test/fixtures/saas-215/.

    Step 15: Create 11 test files:
      - mercadopago-config-shape.test.js + siigo-config-shape.test.js + dian-config-shape.test.js: zod parity assertions
      - dian-invoice-trigger.test.js: Postgres-backed — INSERT Colombia open invoice without cufe → exception 'DIAN_INVOICE_REQUIRES_CUFE_AND_PROVIDER_RESPONSE'; with cufe + dian_provider_response_id → succeeds
      - dian-rejection-task.test.js: INSERT saas_billing_events event_type='dian.rejected' processor='dian' → AFTER trigger fires → approval_tasks row inserted with priority='P1' sla_at=NOW()+24h; if approval_tasks does not exist (P208 not landed), trigger swallows ON CONFLICT + RAISE NOTICE
      - cufe-calculation.test.js: calculateCUFE with fixture invoice + dianConfig + mocked resolveCredential('dian_authorization_token') → returns 96-char hex; ClTec never in error/result
      - colombia-tax-iva-retention.test.js: calculateColombianTax with various combos → IVA 0.19 default; retentions per canon table
      - dian-wizard-state-machine.test.js: valid transitions succeed; invalid throws WizardStateTransitionError; certificate_upload action stores PKCS12 + password BEFORE wizard_state advances
      - credential-vault-trigger-co.test.js: 6 CO credentials INSERT plaintext → exception; via storeCredentialVaultRef → succeeds; PKCS12 large blob via pgcrypto fallback succeeds
      - mcp-credential-sanitize-co.test.js: get_invoice MCP tool with Colombia DIAN invoice → 6 CO credential fields absent; cufe present (public legal); software_pin redacted (sensitive)
      - log-redaction-mp.test.js: redactWebhookPayload(mpPayload, 'mercado_pago') → 6 PII fields '[REDACTED]'

    Step 16: Create test/saas-215/rls/dian-config-cross-tenant-denial.test.js — INSERT saas_dian_config tenant A; SELECT tenant B returns 0 rows.

    Step 17: Append Plan 03 row to 215-VALIDATION.md.
  </action>
  <verify>
    <automated>npm test -- test/saas-215/domain-3/ test/saas-215/rls/dian-config-cross-tenant-denial.test.js</automated>
  </verify>
  <done>Migration 120 applies cleanly; saas_dian_config exists with RLS + 2 DB-triggers; calculateCUFE returns valid SHA-384 hex; calculateColombianTax returns IVA + retentions; advanceWizardState enforces state machine + stores PKCS12 via vault; F-277 + F-278 + F-279 in flow-registry.json; 11 domain-3 + 1 RLS test files green; 215-VALIDATION.md updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→/v1/saas/billing/dian-wizard POST upload_certificate | PKCS12 cert + password immediately stored via Plan 01 storeCredentialVaultRef before any other state change |
| Mercado Pago SDK→saas_credential_vault_refs | mercadopago_access_token fetched per-request; never module-level |
| Siigo API→saas_invoices.cufe + dian_provider_response_id | Siigo response stored; defense-in-depth pair with DIAN_INVOICE_REQUIRES_CUFE_AND_PROVIDER_RESPONSE trigger |
| ClTec (dian_authorization_token)→CUFE SHA-384 | ClTec fetched at calculation time; concatenated into hash input but NEVER returned in any path |
| DIAN webhook (Siigo) → Plan 04 handler | Signature verified via P203 lib/markos/webhooks/signing.cjs; rejection event triggers DIAN_REJECTION_CREATES_P1_TASK |
| MP webhook → Plan 04 handler | Signature verified via P203; payload redacted via Plan 01 redactWebhookPayload(payload, 'mercado_pago') |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-215-03-01 | T | saas_invoices INSERT without DIAN CUFE | mitigate | DIAN_INVOICE_REQUIRES_CUFE_AND_PROVIDER_RESPONSE DB-trigger blocks Colombia legal invoice without cufe + provider_response |
| T-215-03-02 | T | DIAN rejection silent failure (no operator notification) | mitigate | DIAN_REJECTION_CREATES_P1_TASK trigger creates P208 approval_tasks row priority='P1' sla_at=NOW()+24h; degrades gracefully if P208 absent |
| T-215-03-03 | I | PKCS12 cert + password leak via wizard request body in logs | mitigate | dian-wizard.js immediately calls storeCredentialVaultRef BEFORE any other persistence; request body cleared from log |
| T-215-03-04 | I | ClTec (dian_authorization_token) leak via CUFE error message | mitigate | calculateCUFE never includes ClTec in any return value or thrown error; only used as SHA-384 input |
| T-215-03-05 | I | Mercado Pago webhook payload PII in logs | mitigate | redactWebhookPayload(payload, 'mercado_pago') called BEFORE any console.log; MP_PII_FIELDS list applied |
| T-215-03-06 | I | DIAN cert expiry undetected (compliance failure) | mitigate | saas_dian_config.certificate_expires_at populated on cert upload; daily cron (P217 ships) checks for upcoming expiry; alerts via P208 task |
| T-215-03-07 | E | Cross-tenant DIAN config leak | mitigate | RLS sdc_tenant_isolation enforces tenant_id; UNIQUE(tenant_id) prevents duplicate config |
| T-215-03-08 | T | Siigo API timeout / partial DIAN issuance | mitigate | issueDIANInvoiceViaSiigo idempotent via RequestId header; circuit breaker via P203 lib/markos/webhooks/breaker.cjs (Plan 04 wires this) |
| T-215-03-09 | E | Wizard state bypass (skip certificate upload to production) | mitigate | advanceWizardState strict state machine; production_ready requires test_invoice_accepted prerequisite + MANUAL-ONLY operator gate |
</threat_model>

<verification>
- node scripts/preconditions/215-03-check-upstream.cjs (exits 0 when Plan 01 + Plan 02 tables present)
- supabase migration up to slot 120 applies cleanly
- npm test -- test/saas-215/domain-3/
- npm test -- test/saas-215/rls/dian-config-cross-tenant-denial.test.js
</verification>

<success_criteria>
- saas_dian_config exists with RLS + 2 DB-triggers (DIAN_INVOICE_REQUIRES_CUFE_AND_PROVIDER_RESPONSE + DIAN_REJECTION_CREATES_P1_TASK)
- getMercadoPagoClient + getSiigoClient initialize per-request from Vault
- issueDIANInvoiceViaSiigo populates cufe + dian_provider_response_id (defense-in-depth pair with trigger)
- calculateCUFE returns 96-char SHA-384 hex; ClTec never leaks
- calculateColombianTax returns IVA + retentions per canon
- advanceWizardState enforces 6-state machine; PKCS12 stored via vault before state advance
- DIAN_REJECTION_CREATES_P1_TASK creates approval_tasks row with 24h SLA
- 6 CO credential fields stored exclusively in saas_credential_vault_refs (Plan 01)
- F-277 + F-278 + F-279 in flow-registry.json
- 11 domain-3 + 1 RLS test files green
- 215-VALIDATION.md per-task verification map updated for Plan 03
</success_criteria>

<output>
After completion, create `.planning/phases/215-saas-suite-billing-payments-compliance/215-03-SUMMARY.md` documenting: artifacts created, 2 DB-trigger exception names (DIAN_INVOICE_REQUIRES_CUFE_AND_PROVIDER_RESPONSE + DIAN_REJECTION_CREATES_P1_TASK), 6 CO credential ENUMs flowing through Plan 01 vault, CUFE SHA-384 ClTec-protection invariant, DIAN rejection 24h SLA P1 task creation pattern, Siigo+Alegra ownership decision (Q-6 RESOLVED — Siigo first), Mercado Pago direct+abstraction decision (Q-8 RESOLVED), downstream-phase impact (Plan 04 will REUSE handleMPWebhook + handleSiigoWebhook for webhook handler routing).
</output>
</content>
