---
phase: 215
plan: "04"
type: execute
wave: 4
depends_on: ["01", "02", "03"]
f_ids:
  - F-280
  - F-285
migration_slots:
  - 121
integrates_with:
  - "WHK-01 from P203 (REUSE lib/markos/webhooks/{engine,signing,dlq,replay,breaker,rate-limit,metrics}.cjs without modification)"
  - "PRC-01..09 from P205 (webhook handlers may UPDATE saas_invoices.pricing_recommendation_id when processor reports plan change)"
translation_gate_for: []
files_modified:
  - test/fixtures/saas-215/index.js
  - test/fixtures/saas-215/webhook-payload.js
  - scripts/preconditions/215-04-check-upstream.cjs
  - supabase/migrations/121_saas_billing_webhook_routing.sql
  - lib/markos/saas/billing/webhooks/contracts.ts
  - lib/markos/saas/billing/webhooks/contracts.cjs
  - lib/markos/saas/billing/webhooks/dispatcher.ts
  - lib/markos/saas/billing/webhooks/stripe-handler.ts
  - lib/markos/saas/billing/webhooks/mercadopago-handler.ts
  - lib/markos/saas/billing/webhooks/quickbooks-handler.ts
  - lib/markos/saas/billing/webhooks/siigo-handler.ts
  - lib/markos/saas/billing/webhooks/dian-handler.ts
  - lib/markos/saas/billing/webhooks/index.cjs
  - api/v1/saas/webhooks/stripe.js
  - api/v1/saas/webhooks/mercadopago.js
  - api/v1/saas/webhooks/quickbooks.js
  - api/v1/saas/webhooks/siigo.js
  - api/v1/saas/webhooks/dian.js
  - contracts/F-280-saas-webhook-routing-v1.yaml
  - contracts/F-285-saas-webhook-endpoints-v1.yaml
  - contracts/flow-registry.json
  - test/saas-215/domain-4/webhook-signature-verification.test.js
  - test/saas-215/domain-4/webhook-idempotency-trigger.test.js
  - test/saas-215/domain-4/webhook-payload-redaction.test.js
  - test/saas-215/domain-4/webhook-replay-attack.test.js
  - test/saas-215/domain-4/webhook-dlq-routing.test.js
  - test/saas-215/domain-4/webhook-circuit-breaker.test.js
  - test/saas-215/domain-4/webhook-rate-limit.test.js
  - test/saas-215/domain-4/webhook-stripe-events.test.js
  - test/saas-215/domain-4/webhook-mp-events.test.js
  - test/saas-215/domain-4/webhook-dian-rejection-routes-to-task.test.js
  - test/saas-215/domain-4/dispatcher-handlers-map.test.js
  - .planning/phases/215-saas-suite-billing-payments-compliance/215-VALIDATION.md
autonomous: true
requirements:
  - SAS-05
  - QA-01
  - QA-02
  - QA-08
  - QA-11
  - QA-12
must_haves:
  truths:
    - "Plan 04 depends on Plan 01 (webhook event recordBillingEvent + saas_billing_events) + Plan 02 (Stripe + QuickBooks event handlers REUSE Plan 02 client + sync libraries) + Plan 03 (Mercado Pago + Siigo + DIAN handlers REUSE Plan 03 mercadopago-preapproval handleMPWebhook + siigo-sync handleSiigoWebhook); assertUpstreamReady CLI hard-checks all 3 prior plans' tables exist; SOFT-checks markos_webhook_subscriptions + markos_webhook_deliveries (P203 substrate REQUIRED — Plan 04 REUSES this engine)"
    - "Plan 04 ships ONLY contract-level migration 121 (no new tables — REUSES P203 markos_webhook_deliveries store and Plan 01 saas_billing_events store) — PROCESSOR_WEBHOOK_REQUIRES_SIGNATURE_AND_IDEMPOTENCY DB-trigger ships in this migration"
    - "DB-trigger PROCESSOR_WEBHOOK_REQUIRES_SIGNATURE_AND_IDEMPOTENCY ships in migration 121 BEFORE INSERT on saas_billing_events — RAISE EXCEPTION (verbatim per RESEARCH §Domain 4 lines 944-959): processor IN ('stripe','mercado_pago','quickbooks','siigo') AND idempotency_key IS NULL → block; this trigger enforces the invariant that every processor billing event must have routed through the signed handler chain (idempotency_key is the proxy for verified routing); signature_verified_at column on saas_payment_attempts (Plan 01) carries the explicit verification timestamp"
    - "Architecture-lock RE-RUN against Plan 04 paths — scanForbiddenPatterns({paths:['lib/markos/saas/billing/webhooks/','api/v1/saas/webhooks/']}); asserts ZERO violations; CRITICAL: Plan 04 must NOT greenfield any webhook engine code — all signing/replay/DLQ/rate-limit/circuit-breaker logic MUST come from `require('../../../webhooks/engine.cjs')` etc.; helper-presence test asserts all 7 P203 engine files exist (Plan 01 already covers this; Plan 04 makes consumption mandatory by importing in stripe-handler.ts etc.)"
    - "Webhook engine REUSE pattern (RESEARCH §Domain 4 §Webhook Handler Pattern lines 967-986 — CONTEXT non-negotiable #1) — every handler in api/v1/saas/webhooks/{stripe,mercadopago,quickbooks,siigo,dian}.js follows this 5-step sequence: (1) Signature verification via require('lib/markos/webhooks/signing.cjs').verifySignature(secret, req.rawBody, sig, timestamp) — secret fetched per-request via Plan 01 resolveCredential; on failure: 401 + WEBHOOK_SIGNATURE_INVALID error; (2) Idempotency check via require('lib/markos/webhooks/replay.cjs').checkIdempotency(idempotency_key) — for Stripe key=event.id, MP key=data.id, QB key=eventNotifications[].name+id, Siigo key=event_id, DIAN key=cufe; if duplicate: 200 + skip (idempotent ack); (3) Rate limit via require('lib/markos/webhooks/rate-limit.cjs').applyRateLimit(req, {windowMs: 60000, max: 1000}) — on exceed: 429 + Retry-After header; (4) Circuit breaker via require('lib/markos/webhooks/breaker.cjs').withCircuitBreaker(handler, {threshold: 5, resetMs: 30000}); (5) Dispatch to domain handler (Plan 02 handleStripeWebhook / Plan 03 handleMPWebhook / Plan 03 handleSiigoWebhook); (6) Record event via Plan 01 recordBillingEvent (which writes to saas_billing_events with payload_redacted via Plan 01 redactWebhookPayload BEFORE persist); (7) On handler error: enqueue to DLQ via require('lib/markos/webhooks/dlq.cjs').enqueueToDLQ(event, error)"
    - "Webhook handler dispatcher (lib/markos/saas/billing/webhooks/dispatcher.ts) — exports HANDLERS map: {stripe: stripeHandler, mercado_pago: mercadopagoHandler, quickbooks: quickbooksHandler, siigo: siigoHandler, dian: dianHandler}; closeout regression test (Plan 06) asserts every key in HANDLERS map has a corresponding registered MCP tool descriptor + matching api/v1/saas/webhooks/* file (P226 iter 2 lesson — dispatcher HANDLERS map verified end-to-end)"
    - "Stripe webhook handler (api/v1/saas/webhooks/stripe.js) — handles 9 events per Plan 02 RESEARCH §Domain 2 lines 718-728 by delegating to lib/markos/saas/billing/webhooks/stripe-handler.ts which imports from Plan 02 stripe-client.ts + stripe-tax.ts + quickbooks-sync.ts: customer.subscription.{created,updated,deleted} updates saas_subscriptions (P214 mirror); invoice.payment_succeeded → updates saas_invoices.status='paid' + amount_paid + paid_at + triggers QBO sync via syncInvoiceToQBO; invoice.payment_failed → creates saas_payment_attempts(status='failed') + triggers Plan 05 dunning workflow (Plan 05 ships dunning state machine; Plan 04 only writes the failed payment row); invoice.finalized → status='open'; customer.updated; payment_method.attached; charge.dispute.created → P0 alert via P208 approval_tasks (priority='P0')"
    - "Mercado Pago webhook handler (api/v1/saas/webhooks/mercadopago.js) — handles 7 MP events per Plan 03 by delegating to lib/markos/saas/billing/webhooks/mercadopago-handler.ts which calls Plan 03 mercadopago-preapproval.ts handleMPWebhook(event, supabase); preapproval.{authorized,paused,cancelled} updates saas_subscriptions; payment.{created,approved,failed,refunded} writes saas_payment_attempts + on payment.approved triggers Plan 03 issueDIANInvoiceViaSiigo (DIAN compliance flow) + on payment.refunded creates credit note invoice"
    - "QuickBooks webhook handler (api/v1/saas/webhooks/quickbooks.js) — handles QBO eventNotifications array per QBO API spec; for each entity-changed event delegates to lib/markos/saas/billing/webhooks/quickbooks-handler.ts which UPDATEs saas_invoices.quickbooks_invoice_id sync state; QBO sends signature in 'intuit-signature' header (HMAC-SHA256 over rawBody using webhook verifier token from saas_credential_vault_refs)"
    - "Siigo webhook handler (api/v1/saas/webhooks/siigo.js) — handles Siigo event notifications (DIAN acceptance + rejection); delegates to lib/markos/saas/billing/webhooks/siigo-handler.ts which calls Plan 03 siigo-sync.ts handleSiigoWebhook; on DIAN acceptance: UPDATE saas_invoices.dian_transmission_state='accepted'; on DIAN rejection: INSERT saas_billing_events event_type='dian.rejected' processor='dian' (triggers Plan 03 DIAN_REJECTION_CREATES_P1_TASK)"
    - "DIAN webhook handler (api/v1/saas/webhooks/dian.js) — handles direct DIAN response webhooks (if Siigo passes through OR future direct-DIAN integration v4.2.0); delegates to dian-handler.ts; same DIAN_REJECTION_CREATES_P1_TASK invariant"
    - "Replay attack defense — webhook handlers reject events with timestamps older than 5 minutes (per Stripe + MP webhook timestamp tolerance); idempotency_key UNIQUE constraint on saas_billing_events prevents duplicate event processing across replays; DLQ retention 7 days per P203 dlq.cjs default"
    - "Log redaction across 5 processor webhook handlers — every handler invokes Plan 01 redactWebhookPayload(payload, processor) BEFORE any console.log/audit emit; saas_billing_events.payload_redacted column ALWAYS contains the redacted version (raw payload NEVER persisted); processor-specific PII field arrays from Plan 01 (STRIPE_PII_FIELDS / MP_PII_FIELDS / QB_PII_FIELDS / SIIGO_PII_FIELDS / DIAN_PII_FIELDS)"
    - "Circuit breaker integration — Plan 04 wraps all 5 handlers via P203 breaker.cjs withCircuitBreaker; threshold=5 consecutive failures → opens 30s; downstream phases observe via P203 metrics.cjs (saas_webhook_circuit_state metric)"
    - "Rate limiting — applied per processor (rateKey = `webhook:${processor}:${tenantId || 'global'}`); default 1000 req/min per processor per tenant; processor-level DDoS defense"
    - "2 contracts ship — F-280 (saas-webhook-routing-v1) covering 5-step routing pattern + PROCESSOR_WEBHOOK_REQUIRES_SIGNATURE_AND_IDEMPOTENCY exception + dispatcher HANDLERS map invariant; F-285 (saas-webhook-endpoints-v1) covering 5 webhook endpoint contracts (api/v1/saas/webhooks/{stripe,mercadopago,quickbooks,siigo,dian}.js); NO collision with F-001..F-279"
    - "Cross-handler test (test/saas-215/domain-4/dispatcher-handlers-map.test.js — P226 iter 2 lesson) — asserts dispatcher HANDLERS map has exactly 5 keys matching 5 api/v1/saas/webhooks/*.js files exist matching 5 lib/markos/saas/billing/webhooks/*-handler.ts files exist; no orphan handler files; no missing handler keys"
    - "WHK-01 documented in frontmatter as `integrates_with: P203` (NOT requirements) — P203 OWNS webhook engine; Plan 04 REUSES the substrate without modification (CONTEXT non-negotiable #1: 'No processor webhook bypasses webhook engine durability, signing, replay, DLQ, rate limit, and observability')"
    - "215-VALIDATION.md per-task verification map updated with rows for Plan 04 + domain-4 test paths under `test/saas-215/domain-4/`"
    - "UI-SPEC fold (215-UI-SPEC §Downstream UI Inheritance Map row 7 webhook DLQ inspector + §Translation Gate Dissolution row 2 — 208 Recovery Center connector_placeholder family extension): ui_scope = NO_UI; Plan 04 emits DLQ records via P203 `dlq.cjs` with 7-day retention for processor IN (`stripe`, `mercado_pago`, `quickbooks`, `siigo`, `dian`); future P208-05 Recovery Center extension reads `markos_webhook_deliveries` rows where `dlq_at IS NOT NULL AND processor IN (stripe,mercado_pago,quickbooks,siigo,dian)` (UI-SPEC §Downstream row 7 — `<WebhookDLQInspector>` mutation-class binding `connector.mutate` for replay action per 206-UI-SPEC). The DLQ row carries `payload_redacted` ONLY — raw payloads NEVER persisted (Plan 01 `redactWebhookPayload` is upstream of every persist; satisfies UI-SPEC §Sensitive Credential Handling B-3 PII-field redaction for all 5 PII-field-list constants STRIPE/MP/QB/SIIGO/DIAN). The 7-step routing pattern (signature -> idempotency -> rate-limit -> circuit-breaker -> dispatch -> recordBillingEvent -> DLQ-on-error) is implemented across 5 endpoints (`api/v1/saas/webhooks/{stripe,mercadopago,quickbooks,siigo,dian}.js`); architectural enforcement via `require(lib/markos/webhooks/*.cjs)` pattern — Plan 04 MUST NEVER greenfield signing/replay/DLQ/rate-limit/breaker logic (CONTEXT non-negotiable #1). HANDLERS map regression test (P226 iter 2 lesson MANDATORY) — `lib/markos/saas/billing/webhooks/dispatcher.ts` exports `HANDLERS = {stripe, mercado_pago, quickbooks, siigo, dian}` with EXACTLY 5 keys; Plan 06 closeout regression test asserts every key in HANDLERS map has corresponding registered MCP tool descriptor (Plan 01 saas-billing.cjs: get_invoice + list_invoices_by_tenant + get_payment_attempts + get_billing_events = 4 descriptors registered) AND matching `api/v1/saas/webhooks/*.js` file (5 files) AND matching `lib/markos/saas/billing/webhooks/*-handler.ts` file (5 files); no orphan handler files; no missing handler keys. DB-trigger name `PROCESSOR_WEBHOOK_REQUIRES_SIGNATURE_AND_IDEMPOTENCY` appears as RAISE EXCEPTION literal in migration 121. Banned-lexicon zero-match on F-280 + F-285 contract YAMLs. Downstream UI consumers (read-only — DO NOT modify here): future_phase_217_webhook_dlq_inspector `<WebhookDLQInspector>` (UI-SPEC §Downstream row 7 — extends P208-05 Recovery Center connector_placeholder family; mutation-class `connector.mutate` for replay action; renders `<.c-card>` with `<.c-badge--error>` DLQ chip; payload column renders `payload_redacted` only); future_phase_217_payment_timeline `<PaymentAttemptTimeline>` reads webhook-routed `signature_verified_at` updates."
  artifacts:
    - path: scripts/preconditions/215-04-check-upstream.cjs
      provides: assertUpstreamReady — HARD = [saas_invoices + saas_payment_attempts + saas_billing_events + saas_credential_vault_refs (Plan 01), saas_processor_configs + saas_accounting_configs (Plan 02), saas_dian_config (Plan 03), markos_webhook_subscriptions + markos_webhook_deliveries (P203)]; SOFT = [approval_tasks (P208 — for charge.dispute P0 + DIAN rejection P1)]
    - path: supabase/migrations/121_saas_billing_webhook_routing.sql
      provides: contract-only migration (no new tables — reuses P203 + Plan 01 stores) — adds PROCESSOR_WEBHOOK_REQUIRES_SIGNATURE_AND_IDEMPOTENCY DB-trigger BEFORE INSERT on saas_billing_events; checks idempotency_key NOT NULL when processor IN ('stripe','mercado_pago','quickbooks','siigo'); RAISE EXCEPTION verbatim per RESEARCH §Domain 4 lines 944-959
    - path: lib/markos/saas/billing/webhooks/contracts.ts
      provides: zod schemas + enum constants — PROCESSORS_FOR_WEBHOOK = ['stripe','mercado_pago','quickbooks','siigo','dian'] (5) + STRIPE_EVENTS (9) + MP_EVENTS (7) + QBO_EVENTS (4) + SIIGO_EVENTS (3) + DIAN_EVENTS (3); StripeWebhookEnvelopeSchema + MPWebhookEnvelopeSchema + QBWebhookEnvelopeSchema + SiigoWebhookEnvelopeSchema + DianWebhookEnvelopeSchema zod
      exports: [PROCESSORS_FOR_WEBHOOK, STRIPE_EVENTS, MP_EVENTS, QBO_EVENTS, SIIGO_EVENTS, DIAN_EVENTS, StripeWebhookEnvelopeSchema, MPWebhookEnvelopeSchema, QBWebhookEnvelopeSchema, SiigoWebhookEnvelopeSchema, DianWebhookEnvelopeSchema]
    - path: lib/markos/saas/billing/webhooks/dispatcher.ts
      provides: HANDLERS = {stripe: stripeHandler, mercado_pago: mercadopagoHandler, quickbooks: quickbooksHandler, siigo: siigoHandler, dian: dianHandler} map; dispatchWebhook({processor, event, supabase}) routes to HANDLERS[processor]; REUSES P203 engine via require('lib/markos/webhooks/engine.cjs')
      exports: [HANDLERS, dispatchWebhook]
    - path: lib/markos/saas/billing/webhooks/stripe-handler.ts
      provides: stripeHandler({event, tenantId, supabase}) → switch on event.type (9 types from Plan 02); imports getStripeClient + stripeTax + quickbooksSync from Plan 02; calls recordBillingEvent (Plan 01); calls redactWebhookPayload (Plan 01); on charge.dispute.created creates P0 approval_tasks
      exports: [stripeHandler]
    - path: lib/markos/saas/billing/webhooks/mercadopago-handler.ts
      provides: mercadopagoHandler({event, tenantId, supabase}) → delegates to Plan 03 mercadopago-preapproval handleMPWebhook (which already has 7-event switch); calls recordBillingEvent + redactWebhookPayload
      exports: [mercadopagoHandler]
    - path: lib/markos/saas/billing/webhooks/quickbooks-handler.ts
      provides: quickbooksHandler({event, tenantId, supabase}) → iterates QBO eventNotifications array; for each entity-changed event UPDATEs saas_invoices.quickbooks_invoice_id sync metadata; verifies HMAC-SHA256 signature in intuit-signature header
      exports: [quickbooksHandler]
    - path: lib/markos/saas/billing/webhooks/siigo-handler.ts
      provides: siigoHandler({event, tenantId, supabase}) → delegates to Plan 03 siigo-sync handleSiigoWebhook; on DIAN rejection writes saas_billing_events event_type='dian.rejected' (DIAN_REJECTION_CREATES_P1_TASK trigger fires)
      exports: [siigoHandler]
    - path: lib/markos/saas/billing/webhooks/dian-handler.ts
      provides: dianHandler({event, tenantId, supabase}) → handles direct DIAN response (if available); same DIAN_REJECTION_CREATES_P1_TASK invariant; v4.2.0 expansion target
      exports: [dianHandler]
    - path: lib/markos/saas/billing/webhooks/index.cjs
      provides: CommonJS twin re-exporting full surface
    - path: api/v1/saas/webhooks/stripe.js
      provides: POST /v1/saas/webhooks/stripe endpoint — implements 7-step routing pattern (signature → idempotency → rate-limit → circuit-breaker → dispatch → recordBillingEvent → DLQ on error); legacy api/*.js handler; tenant_id resolved from stripe_account_id mapping in saas_processor_configs
    - path: api/v1/saas/webhooks/mercadopago.js
      provides: POST /v1/saas/webhooks/mercadopago — same 7-step pattern; tenant_id resolved from MP user_id in saas_processor_configs
    - path: api/v1/saas/webhooks/quickbooks.js
      provides: POST /v1/saas/webhooks/quickbooks — same 7-step pattern; tenant_id resolved from QBO realmId in saas_accounting_configs
    - path: api/v1/saas/webhooks/siigo.js
      provides: POST /v1/saas/webhooks/siigo — same 7-step pattern; tenant_id resolved from Siigo account_id in saas_accounting_configs
    - path: api/v1/saas/webhooks/dian.js
      provides: POST /v1/saas/webhooks/dian — same 7-step pattern; tenant_id resolved from NIT in saas_dian_config
    - path: contracts/F-280-saas-webhook-routing-v1.yaml
      provides: documents 7-step routing pattern + PROCESSOR_WEBHOOK_REQUIRES_SIGNATURE_AND_IDEMPOTENCY exception + dispatcher HANDLERS map + replay attack defense + circuit breaker + rate limiting
    - path: contracts/F-285-saas-webhook-endpoints-v1.yaml
      provides: documents 5 webhook endpoint contracts; signature header per processor (stripe-signature, x-mercadopago-signature, intuit-signature, siigo-signature, dian-signature); idempotency key per processor
    - path: contracts/flow-registry.json
      provides: 2 entries appended for F-280 + F-285
    - path: test/fixtures/saas-215/webhook-payload.js
      provides: webhookPayloadFactory({processor, eventType, tenantId, ...}) returning a fixture event envelope per processor (Stripe event shape, MP topic+id shape, QBO eventNotifications shape, Siigo shape, DIAN shape)
    - path: test/saas-215/domain-4/webhook-signature-verification.test.js
      provides: for each of 5 processors INVALID signature → 401 WEBHOOK_SIGNATURE_INVALID; VALID signature → 200; uses P203 lib/markos/webhooks/signing.cjs verifySignature
    - path: test/saas-215/domain-4/webhook-idempotency-trigger.test.js
      provides: Postgres-backed — INSERT saas_billing_events with processor='stripe' AND idempotency_key IS NULL → exception 'PROCESSOR_WEBHOOK_REQUIRES_SIGNATURE_AND_IDEMPOTENCY'; with idempotency_key set → succeeds; processor='dian' is exempt from trigger (per CHECK clause: only stripe/mp/qb/siigo)
    - path: test/saas-215/domain-4/webhook-payload-redaction.test.js
      provides: for each of 5 processors send webhook with PII payload → assert saas_billing_events.payload_redacted has all PII fields '[REDACTED]'
    - path: test/saas-215/domain-4/webhook-replay-attack.test.js
      provides: send same Stripe event id twice → second returns 200 (idempotent skip); replay-attack with timestamp >5min old → 401
    - path: test/saas-215/domain-4/webhook-dlq-routing.test.js
      provides: handler that throws → event enqueued to DLQ via P203 dlq.cjs
    - path: test/saas-215/domain-4/webhook-circuit-breaker.test.js
      provides: 5 consecutive handler failures → circuit opens 30s; 6th request → returns 503 immediately
    - path: test/saas-215/domain-4/webhook-rate-limit.test.js
      provides: 1001 requests in 60s for tenant A processor=stripe → 1001th returns 429 + Retry-After header
    - path: test/saas-215/domain-4/webhook-stripe-events.test.js
      provides: 9 Stripe events with mocked Plan 02 surface → assert each event triggers correct domain action (subscription update, invoice update, payment attempt write, dunning trigger flag, P0 task on dispute)
    - path: test/saas-215/domain-4/webhook-mp-events.test.js
      provides: 7 MP events → assert correct domain action via Plan 03 handleMPWebhook delegation
    - path: test/saas-215/domain-4/webhook-dian-rejection-routes-to-task.test.js
      provides: DIAN rejection event → siigo-handler writes saas_billing_events event_type='dian.rejected' → AFTER trigger DIAN_REJECTION_CREATES_P1_TASK fires → approval_tasks row created with priority='P1' sla_at=NOW()+24h
    - path: test/saas-215/domain-4/dispatcher-handlers-map.test.js
      provides: P226 iter 2 lesson — asserts HANDLERS map has 5 keys matching 5 api/v1/saas/webhooks/*.js files exist matching 5 lib/markos/saas/billing/webhooks/*-handler.ts files; no orphans; no missing
  key_links:
    - from: lib/markos/saas/billing/webhooks/dispatcher.ts
      to: lib/markos/webhooks/engine.cjs (P203 substrate REUSE)
      via: requires P203 engine for orchestration; NEVER greenfields signing/replay/DLQ/rate-limit/breaker
      pattern: "lib/markos/webhooks/"
    - from: api/v1/saas/webhooks/stripe.js
      to: lib/markos/webhooks/signing.cjs (P203)
      via: 7-step routing — calls verifySignature(stripe_webhook_secret, rawBody, sig, timestamp) BEFORE any processing
      pattern: "verifySignature"
    - from: lib/markos/saas/billing/webhooks/stripe-handler.ts
      to: lib/markos/saas/billing/us/stripe-client.ts (Plan 02) + lib/markos/saas/billing/us/quickbooks-sync.ts (Plan 02)
      via: imports from Plan 02 — invoice.payment_succeeded triggers syncInvoiceToQBO; charge.dispute.created creates P0 approval_tasks
      pattern: "syncInvoiceToQBO"
    - from: lib/markos/saas/billing/webhooks/mercadopago-handler.ts
      to: lib/markos/saas/billing/co/mercadopago-preapproval.ts (Plan 03)
      via: delegates to handleMPWebhook from Plan 03
      pattern: "handleMPWebhook"
    - from: lib/markos/saas/billing/webhooks/siigo-handler.ts
      to: lib/markos/saas/billing/co/siigo-sync.ts (Plan 03) + supabase saas_billing_events
      via: handleSiigoWebhook delegation; on DIAN rejection writes event_type='dian.rejected'; AFTER trigger DIAN_REJECTION_CREATES_P1_TASK fires
      pattern: "dian.rejected"
    - from: api/v1/saas/webhooks/*.js (5 files)
      to: lib/markos/saas/billing/billing-event.ts (Plan 01) + lib/markos/saas/billing/log-redaction.ts (Plan 01)
      via: every handler calls recordBillingEvent (which internally calls redactWebhookPayload before persist)
      pattern: "recordBillingEvent"
---

<objective>
Ship Wave 3 processor webhook routing through the existing P203 webhook engine: 5 processor webhook endpoints (api/v1/saas/webhooks/{stripe,mercadopago,quickbooks,siigo,dian}.js) implementing the 7-step routing pattern (signature verification → idempotency check → rate limit → circuit breaker → dispatch to domain handler → recordBillingEvent → DLQ on error), 5 domain handler libraries delegating to Plan 02 (Stripe + QBO) and Plan 03 (MP + Siigo + DIAN) functions, dispatcher HANDLERS map (P226 iter 2 lesson — closeout regression asserts map completeness end-to-end), PROCESSOR_WEBHOOK_REQUIRES_SIGNATURE_AND_IDEMPOTENCY DB-trigger (verbatim per RESEARCH §Domain 4 — enforces idempotency_key as proxy for verified routing), 2 F-ID contracts (F-280 routing pattern + F-285 endpoint contracts), and the cross-handler dispatcher map verification regression. Plan 04 REUSES the P203 webhook engine substrate (lib/markos/webhooks/{engine,signing,dlq,replay,breaker,rate-limit,metrics}.cjs) without modification — CONTEXT non-negotiable #1 ('No processor webhook bypasses webhook engine durability, signing, replay, DLQ, rate limit, and observability') is enforced architecturally by the require('lib/markos/webhooks/*.cjs') pattern.

Purpose: satisfy SAS-05 (SaaS Billing Engine — webhook routing for failed-payment recovery + dunning + accounting sync); QA-01..02 + QA-08 + QA-11..12 (architecture-lock RE-RUN + RLS isolation + replay attack defense + DLQ routing + idempotency enforcement). Plan 04 unblocks Plan 05 (dunning workflow consumes invoice.payment_failed events) and unblocks downstream P216-P219 (which read saas_invoices + saas_payment_attempts updated by webhook handlers).

Output: 1 preflight CLI + 1 contract-only migration (121 — PROCESSOR_WEBHOOK_REQUIRES_SIGNATURE_AND_IDEMPOTENCY DB-trigger only; no new tables) + 8 webhook lib files (contracts + dispatcher + 5 handlers + index.cjs) + 5 API webhook handlers + 2 contract YAMLs + flow-registry update + 1 fixture file + 11 domain-4 test files + populated VALIDATION.md.
</objective>

<ui_spec_fold>
ui_scope: NO_UI
ui_spec_block: 215-UI-SPEC.md §Downstream UI Inheritance Map row 7 (Webhook DLQ inspector — P208-05 Recovery Center extension); §Translation Gate Dissolution row 2 — 208-UI-SPEC §Recovery Center connector_placeholder family extends with webhook DLQ inspector for the 5 processor endpoints

Cross-cutting carry-forward applicable to backend (D-08..D-15 from 213.4-VALIDATION.md):
- D-08 token-only / D-09 mint-as-text / D-09b `.c-notice` mandatory (future DLQ inspector renders `<.c-notice c-notice--error>` for >5min stale rows) / D-13 `.c-card--feature` reserved / D-14 no `.c-table` (DLQ row list uses vanilla `<table>` + `.c-badge--error` "DLQ" chip) / D-15 selective extraction (`<WebhookDLQRow>` recommended for reuse across Recovery Center + admin DLQ inspector)

Downstream UI consumers (read-only — DO NOT modify here):
- future_phase_217_webhook_dlq_inspector `<WebhookDLQInspector>` (P208-05 Recovery Center extension) reads `markos_webhook_deliveries` rows where `dlq_at IS NOT NULL AND processor IN ('stripe','mercado_pago','quickbooks','siigo','dian')`; mutation-class binding `connector.mutate` for replay action per 206-UI-SPEC; renders `payload_redacted` only — never raw payload
- existing P208-03 Task Board renders DIAN P1 tasks created by 215-03 DIAN_REJECTION_CREATES_P1_TASK trigger when 215-04 siigo-handler writes `event_type='dian.rejected'`; NO placeholder needed (P208 Task Board already renders P1 rows)

Backend doctrine ACs added by this fold (closeout grep gates):
- 7-step routing pattern enforced via `require('lib/markos/webhooks/*.cjs')` substrate REUSE (NEVER greenfield signing/replay/DLQ/rate-limit/breaker)
- HANDLERS map regression — `lib/markos/saas/billing/webhooks/dispatcher.ts` exports HANDLERS with exactly 5 keys; Plan 06 closeout asserts 5 webhook files + 5 handler files + 4 MCP tool descriptors; no orphans
- DB-trigger name verbatim `PROCESSOR_WEBHOOK_REQUIRES_SIGNATURE_AND_IDEMPOTENCY` appears in migration 121
- Plan 01 `redactWebhookPayload` invoked BEFORE any persist across all 5 webhook handler delegations
- Banned-lexicon zero-match on F-280 + F-285 contract YAMLs
</ui_spec_fold>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/215-saas-suite-billing-payments-compliance/215-CONTEXT.md
@.planning/phases/215-saas-suite-billing-payments-compliance/215-RESEARCH.md
@.planning/phases/215-saas-suite-billing-payments-compliance/215-VALIDATION.md
@.planning/phases/215-saas-suite-billing-payments-compliance/215-01-PLAN.md
@.planning/phases/215-saas-suite-billing-payments-compliance/215-02-PLAN.md
@.planning/phases/215-saas-suite-billing-payments-compliance/215-03-PLAN.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@lib/markos/webhooks/engine.cjs
@lib/markos/webhooks/signing.cjs
@lib/markos/webhooks/dlq.cjs
@lib/markos/webhooks/replay.cjs
@lib/markos/webhooks/breaker.cjs
@lib/markos/webhooks/rate-limit.cjs
@lib/markos/webhooks/metrics.cjs
@lib/markos/saas/billing/index.cjs
@lib/markos/saas/billing/us/index.cjs
@lib/markos/saas/billing/co/index.cjs
@onboarding/backend/runtime-context.cjs

<interfaces>
<!-- P203 webhook engine substrate (REUSE — DO NOT GREENFIELD) -->

```javascript
// lib/markos/webhooks/signing.cjs
function verifySignature(secret, rawBody, signatureHeader, timestampHeader): boolean

// lib/markos/webhooks/replay.cjs
async function checkIdempotency(idempotencyKey): Promise<{isDuplicate: boolean, existingEventId?: string}>

// lib/markos/webhooks/rate-limit.cjs
async function applyRateLimit(req, opts): Promise<{allowed: boolean, retryAfter?: number}>

// lib/markos/webhooks/breaker.cjs
function withCircuitBreaker(handler, opts): wrappedHandler

// lib/markos/webhooks/dlq.cjs
async function enqueueToDLQ(event, error): Promise<void>

// lib/markos/webhooks/metrics.cjs
function recordWebhookMetric(metricName, labels, value)

// lib/markos/webhooks/engine.cjs (orchestrator)
async function processWebhookEvent({processor, event, handler, opts}): Promise<{status: number, body: object}>
```

7-step routing pattern (every webhook handler MUST follow):

| Step | Action | Failure Mode |
|------|--------|--------------|
| 1 | verifySignature | 401 WEBHOOK_SIGNATURE_INVALID |
| 2 | checkIdempotency | 200 + skip (idempotent ack) |
| 3 | applyRateLimit | 429 + Retry-After |
| 4 | withCircuitBreaker(handler) | 503 if circuit open |
| 5 | dispatchWebhook to domain handler | varies |
| 6 | recordBillingEvent (writes saas_billing_events with payload_redacted) | n/a |
| 7 | On dispatch error: enqueueToDLQ | event in DLQ for replay |

Per-processor signature header + idempotency key map:

| Processor | Signature header | Idempotency key |
|-----------|------------------|-----------------|
| Stripe | stripe-signature | event.id |
| Mercado Pago | x-mercadopago-signature | data.id |
| QuickBooks | intuit-signature | eventNotifications[0].dataChangeEvent.entities[0].id |
| Siigo | siigo-signature | event_id |
| DIAN | dian-signature | cufe |

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0.5: Plan 04 preflight</name>
  <files>scripts/preconditions/215-04-check-upstream.cjs</files>
  <read_first>
    - lib/markos/saas/billing/preflight/index.cjs (Plan 01)
    - scripts/preconditions/215-03-check-upstream.cjs (mirror)
  </read_first>
  <action>
    Create scripts/preconditions/215-04-check-upstream.cjs — assertUpstreamReady; HARD = [saas_invoices + saas_payment_attempts + saas_billing_events + saas_credential_vault_refs (Plan 01), saas_processor_configs + saas_accounting_configs (Plan 02), saas_dian_config (Plan 03), markos_webhook_subscriptions + markos_webhook_deliveries (P203)]; SOFT = [approval_tasks (P208 — for charge.dispute P0 + DIAN rejection P1; soft because triggers degrade gracefully)]; exit 1 on hard miss.
  </action>
  <verify>
    <automated>node scripts/preconditions/215-04-check-upstream.cjs</automated>
  </verify>
  <done>Preflight CLI exits 0 when all 9 hard tables present; soft-warns on missing approval_tasks.</done>
</task>

<task type="auto">
  <name>Task 1: Migration 121 + 8 webhook lib files (dispatcher + 5 handlers + contracts + index.cjs) + 5 API webhook handlers + 2 contracts + 11 domain-4 tests + dispatcher HANDLERS map regression</name>
  <files>supabase/migrations/121_saas_billing_webhook_routing.sql, lib/markos/saas/billing/webhooks/contracts.ts, lib/markos/saas/billing/webhooks/contracts.cjs, lib/markos/saas/billing/webhooks/dispatcher.ts, lib/markos/saas/billing/webhooks/stripe-handler.ts, lib/markos/saas/billing/webhooks/mercadopago-handler.ts, lib/markos/saas/billing/webhooks/quickbooks-handler.ts, lib/markos/saas/billing/webhooks/siigo-handler.ts, lib/markos/saas/billing/webhooks/dian-handler.ts, lib/markos/saas/billing/webhooks/index.cjs, api/v1/saas/webhooks/stripe.js, api/v1/saas/webhooks/mercadopago.js, api/v1/saas/webhooks/quickbooks.js, api/v1/saas/webhooks/siigo.js, api/v1/saas/webhooks/dian.js, contracts/F-280-saas-webhook-routing-v1.yaml, contracts/F-285-saas-webhook-endpoints-v1.yaml, contracts/flow-registry.json, test/fixtures/saas-215/index.js, test/fixtures/saas-215/webhook-payload.js, test/saas-215/domain-4/webhook-signature-verification.test.js, test/saas-215/domain-4/webhook-idempotency-trigger.test.js, test/saas-215/domain-4/webhook-payload-redaction.test.js, test/saas-215/domain-4/webhook-replay-attack.test.js, test/saas-215/domain-4/webhook-dlq-routing.test.js, test/saas-215/domain-4/webhook-circuit-breaker.test.js, test/saas-215/domain-4/webhook-rate-limit.test.js, test/saas-215/domain-4/webhook-stripe-events.test.js, test/saas-215/domain-4/webhook-mp-events.test.js, test/saas-215/domain-4/webhook-dian-rejection-routes-to-task.test.js, test/saas-215/domain-4/dispatcher-handlers-map.test.js, .planning/phases/215-saas-suite-billing-payments-compliance/215-VALIDATION.md</files>
  <read_first>
    - .planning/phases/215-saas-suite-billing-payments-compliance/215-RESEARCH.md §Domain 4 §DB-Trigger Compliance PROCESSOR_WEBHOOK_REQUIRES_SIGNATURE_AND_IDEMPOTENCY §Webhook Handler Pattern §Log Redaction for Processor Webhook Payloads
    - lib/markos/webhooks/engine.cjs + signing.cjs + dlq.cjs + replay.cjs + breaker.cjs + rate-limit.cjs + metrics.cjs (P203 — all REUSE; NO modification)
    - lib/markos/saas/billing/billing-event.ts (Plan 01 — recordBillingEvent)
    - lib/markos/saas/billing/log-redaction.ts (Plan 01 — redactWebhookPayload + 5 PII field arrays)
    - lib/markos/saas/billing/credential-vault.ts (Plan 01 — resolveCredential for fetching webhook secrets)
    - lib/markos/saas/billing/us/stripe-client.ts + quickbooks-sync.ts (Plan 02 — handler delegation targets)
    - lib/markos/saas/billing/co/mercadopago-preapproval.ts + siigo-sync.ts (Plan 03 — handler delegation targets)
    - api/cron/webhooks-dlq-purge.js (P203 cron pattern reference)
  </read_first>
  <action>
    Step 1: Create supabase/migrations/121_saas_billing_webhook_routing.sql:
      - CREATE OR REPLACE FUNCTION enforce_webhook_signature_idempotency() — verbatim per RESEARCH §Domain 4 lines 944-959 (RAISE EXCEPTION 'PROCESSOR_WEBHOOK_REQUIRES_SIGNATURE_AND_IDEMPOTENCY' on processor IN ('stripe','mercado_pago','quickbooks','siigo') AND idempotency_key IS NULL)
      - CREATE TRIGGER trg_webhook_signature_idempotency BEFORE INSERT ON saas_billing_events FOR EACH ROW EXECUTE FUNCTION enforce_webhook_signature_idempotency()
      - Note: 'dian' processor exempt because DIAN events are emitted internally via Plan 03 siigo-sync (CUFE rejection passthrough), not directly received from external processor

    Step 2: Create lib/markos/saas/billing/webhooks/contracts.ts exporting PROCESSORS_FOR_WEBHOOK + STRIPE_EVENTS (9) + MP_EVENTS (7) + QBO_EVENTS (4) + SIIGO_EVENTS (3) + DIAN_EVENTS (3) + 5 zod envelope schemas.

    Step 3: Create lib/markos/saas/billing/webhooks/dispatcher.ts:
      - Import HANDLERS_BY_PROCESSOR each handler module
      - Export HANDLERS = {stripe, mercado_pago, quickbooks, siigo, dian} map
      - Export dispatchWebhook({processor, event, tenantId, supabase}) — looks up HANDLERS[processor]; throws UnknownProcessorError if not found; calls handler

    Step 4: Create lib/markos/saas/billing/webhooks/stripe-handler.ts:
      - Import getStripeClient + stripeTax + quickbooksSync from Plan 02
      - Import recordBillingEvent + redactWebhookPayload from Plan 01
      - Export stripeHandler({event, tenantId, supabase}) — switch on event.type for 9 Stripe events:
        - customer.subscription.{created,updated,deleted}: UPDATE saas_subscriptions (P214 mirror)
        - invoice.payment_succeeded: UPDATE saas_invoices.status='paid' + amount_paid + paid_at; trigger syncInvoiceToQBO
        - invoice.payment_failed: createPaymentAttempt(status='failed') via Plan 01; flag for Plan 05 dunning workflow
        - invoice.finalized: UPDATE saas_invoices.status='open'
        - customer.updated: UPDATE customer record
        - payment_method.attached: UPDATE processor config
        - charge.dispute.created: INSERT approval_tasks priority='P0' freeze_account=true (P208 substrate; soft-degrade if absent)
      - Always calls recordBillingEvent at start (writes payload_redacted)

    Step 5: Create lib/markos/saas/billing/webhooks/mercadopago-handler.ts:
      - Import handleMPWebhook from Plan 03 mercadopago-preapproval
      - Export mercadopagoHandler({event, tenantId, supabase}) — calls recordBillingEvent (Plan 01) + delegates to Plan 03 handleMPWebhook

    Step 6: Create lib/markos/saas/billing/webhooks/quickbooks-handler.ts:
      - Import getQBOClient from Plan 02
      - Export quickbooksHandler({event, tenantId, supabase}) — iterates eventNotifications array; for each entity-changed event UPDATEs saas_invoices.quickbooks_invoice_id sync metadata; HMAC-SHA256 verifySignature uses webhook verifier token from saas_credential_vault_refs (credential_name='quickbooks_realm_id' is realm not webhook secret — additional credential needed: register 'quickbooks_webhook_verifier_token' in CREDENTIAL_FIELDS Plan 01 — flag in execution if not already present)

    Step 7: Create lib/markos/saas/billing/webhooks/siigo-handler.ts:
      - Import handleSiigoWebhook from Plan 03 siigo-sync
      - Export siigoHandler({event, tenantId, supabase}) — calls recordBillingEvent + delegates to Plan 03 handleSiigoWebhook; on DIAN rejection passthrough writes saas_billing_events event_type='dian.rejected' processor='dian' (NOT 'siigo' — the DIAN_REJECTION_CREATES_P1_TASK trigger checks processor='dian')

    Step 8: Create lib/markos/saas/billing/webhooks/dian-handler.ts:
      - Export dianHandler({event, tenantId, supabase}) — direct DIAN response handler (v4.2.0 expansion); same DIAN_REJECTION_CREATES_P1_TASK invariant; for v4.1.0 typically not invoked (Siigo handles passthrough)

    Step 9: Create lib/markos/saas/billing/webhooks/index.cjs CommonJS twin.

    Step 10: Create 5 API webhook handlers (api/v1/saas/webhooks/{stripe,mercadopago,quickbooks,siigo,dian}.js) — each implements the 7-step pattern:
      ```javascript
      // api/v1/saas/webhooks/stripe.js
      'use strict';
      const { verifySignature } = require('../../../../lib/markos/webhooks/signing.cjs');
      const { checkIdempotency } = require('../../../../lib/markos/webhooks/replay.cjs');
      const { applyRateLimit } = require('../../../../lib/markos/webhooks/rate-limit.cjs');
      const { withCircuitBreaker } = require('../../../../lib/markos/webhooks/breaker.cjs');
      const { enqueueToDLQ } = require('../../../../lib/markos/webhooks/dlq.cjs');
      const { resolveCredential } = require('../../../../lib/markos/saas/billing/credential-vault');
      const { recordBillingEvent } = require('../../../../lib/markos/saas/billing/billing-event');
      const { stripeHandler } = require('../../../../lib/markos/saas/billing/webhooks/stripe-handler');
      module.exports = async function stripeWebhookHandler(req, res) {
        // 1. Resolve tenant from stripe_account_id
        const tenantId = await resolveTenantFromStripeAccount(req.body.account, supabase);
        // 2. Verify signature
        const sig = req.headers['stripe-signature'];
        const secret = await resolveCredential({tenantId, credentialName: 'stripe_webhook_secret'}, supabase);
        if (!verifySignature(secret, req.rawBody, sig, /* extracted timestamp */)) return res.status(401).json({error: 'WEBHOOK_SIGNATURE_INVALID'});
        // 3. Idempotency
        const dup = await checkIdempotency(req.body.id);
        if (dup.isDuplicate) return res.status(200).json({status: 'duplicate', existingEventId: dup.existingEventId});
        // 4. Rate limit
        const rl = await applyRateLimit(req, {windowMs: 60000, max: 1000, key: `webhook:stripe:${tenantId}`});
        if (!rl.allowed) return res.status(429).set('Retry-After', rl.retryAfter).json({error: 'RATE_LIMIT'});
        // 5. Circuit breaker + dispatch
        const wrapped = withCircuitBreaker(stripeHandler, {threshold: 5, resetMs: 30000});
        try {
          await wrapped({event: req.body, tenantId, supabase});
          // 6. Record event (recordBillingEvent calls redactWebhookPayload internally before persist)
          await recordBillingEvent({tenantId, processor: 'stripe', eventType: req.body.type, processorEventId: req.body.id, idempotencyKey: req.body.id, payload: req.body}, supabase);
          return res.status(200).json({status: 'processed'});
        } catch (err) {
          // 7. DLQ on dispatch error
          await enqueueToDLQ({processor: 'stripe', event: req.body}, err);
          return res.status(500).json({error: 'HANDLER_ERROR'});
        }
      };
      ```
      - Mirror this pattern for mercadopago.js (signature header `x-mercadopago-signature`, idempotency `data.id`), quickbooks.js (`intuit-signature`, idempotency from eventNotifications), siigo.js (`siigo-signature`, `event_id`), dian.js (`dian-signature`, `cufe`)

    Step 11: Create 2 contract YAMLs (F-280 routing-pattern + F-285 endpoint-list). Append 2 to contracts/flow-registry.json.

    Step 12: Add webhookPayloadFactory to test/fixtures/saas-215/.

    Step 13: Create 11 test files:
      - webhook-signature-verification.test.js: 5 processors × invalid signature → 401; valid → 200
      - webhook-idempotency-trigger.test.js: Postgres-backed — INSERT saas_billing_events with processor='stripe' AND idempotency_key IS NULL → exception 'PROCESSOR_WEBHOOK_REQUIRES_SIGNATURE_AND_IDEMPOTENCY'; processor='dian' AND idempotency_key IS NULL → succeeds (exempt)
      - webhook-payload-redaction.test.js: 5 processors × send PII payload → saas_billing_events.payload_redacted has all PII '[REDACTED]'
      - webhook-replay-attack.test.js: same Stripe event id sent twice → second 200 idempotent skip; timestamp >5min old → 401
      - webhook-dlq-routing.test.js: handler throws → enqueueToDLQ called
      - webhook-circuit-breaker.test.js: 5 consecutive handler throws → circuit opens; 6th request → 503
      - webhook-rate-limit.test.js: 1001 requests in 60s → 429 + Retry-After
      - webhook-stripe-events.test.js: 9 Stripe event types → correct domain action mocked
      - webhook-mp-events.test.js: 7 MP events → handleMPWebhook delegation verified
      - webhook-dian-rejection-routes-to-task.test.js: DIAN rejection event flows through siigo-handler → INSERT saas_billing_events event_type='dian.rejected' processor='dian' → AFTER trigger creates approval_tasks priority='P1' sla_at=NOW()+24h
      - dispatcher-handlers-map.test.js (P226 iter 2 lesson): asserts HANDLERS map keys = 5; api/v1/saas/webhooks/*.js = 5 files; lib/markos/saas/billing/webhooks/*-handler.ts = 5 files; no orphans

    Step 14: Append Plan 04 row to 215-VALIDATION.md.
  </action>
  <verify>
    <automated>npm test -- test/saas-215/domain-4/</automated>
  </verify>
  <done>Migration 121 applies cleanly with PROCESSOR_WEBHOOK_REQUIRES_SIGNATURE_AND_IDEMPOTENCY trigger; 5 webhook handlers REUSE P203 engine without modification; dispatcher HANDLERS map verified end-to-end; F-280 + F-285 in flow-registry.json; 11 domain-4 test files green; 215-VALIDATION.md updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| processor→/v1/saas/webhooks/* | Untrusted external processor; signature verification required (step 1 of 7-step pattern) |
| signature header→verifySignature | HMAC-SHA256 verification; secret fetched from Plan 01 vault per-request |
| webhook payload→recordBillingEvent | Payload redacted via Plan 01 redactWebhookPayload BEFORE persist; raw payload NEVER stored |
| handler error→DLQ | P203 dlq.cjs enqueueToDLQ; 7-day retention; replay possible |
| circuit breaker open→503 | P203 breaker.cjs; protects downstream P216-P219 from cascading failures |
| siigo handler→DIAN rejection event | Writes processor='dian' event; AFTER trigger creates P1 task with 24h SLA |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-215-04-01 | S | Webhook signature forgery | mitigate | verifySignature (P203) HMAC-SHA256; rejects on mismatch with 401 |
| T-215-04-02 | T | Replay attack via stale event | mitigate | Timestamp tolerance check >5min reject; idempotency_key UNIQUE on saas_billing_events |
| T-215-04-03 | T | Duplicate event processing | mitigate | checkIdempotency (P203) on key; PROCESSOR_WEBHOOK_REQUIRES_SIGNATURE_AND_IDEMPOTENCY DB-trigger ensures idempotency_key NOT NULL |
| T-215-04-04 | I | Webhook payload PII in logs | mitigate | redactWebhookPayload (Plan 01) called by recordBillingEvent BEFORE persist; saas_billing_events.payload_redacted only stored form |
| T-215-04-05 | D | Processor flooding endpoint with events | mitigate | applyRateLimit (P203) — 1000 req/min per processor per tenant; 429 on exceed |
| T-215-04-06 | D | Failing handler cascading to downstream | mitigate | withCircuitBreaker (P203) — 5 failures opens 30s; protects P216-P219 readers |
| T-215-04-07 | T | Lost event on handler crash | mitigate | enqueueToDLQ (P203) — 7-day retention + replay capability |
| T-215-04-08 | E | Webhook handler bypassing engine substrate | mitigate | Architecture-lock test scans for greenfield signing/replay logic; require('lib/markos/webhooks/*.cjs') pattern enforced |
| T-215-04-09 | I | charge.dispute.created leaking customer data via P0 task | mitigate | approval_tasks row contains only invoice_id + dispute_amount + freeze_flag; PII redacted before persist |
</threat_model>

<verification>
- node scripts/preconditions/215-04-check-upstream.cjs (exits 0)
- supabase migration up to slot 121 applies cleanly
- npm test -- test/saas-215/domain-4/
</verification>

<success_criteria>
- 5 webhook endpoints implement 7-step routing pattern; REUSE P203 engine
- PROCESSOR_WEBHOOK_REQUIRES_SIGNATURE_AND_IDEMPOTENCY trigger blocks idempotency_key NULL inserts (except processor='dian' exempt)
- dispatcher HANDLERS map = 5 keys; matches 5 endpoint files + 5 handler libraries (P226 iter 2 lesson)
- All 5 handlers call recordBillingEvent (Plan 01) which calls redactWebhookPayload before persist
- DIAN rejection event flow → DIAN_REJECTION_CREATES_P1_TASK → approval_tasks P1 24h SLA
- Replay attack defended (timestamp + idempotency)
- Rate limit 1000/min per processor per tenant
- Circuit breaker opens after 5 consecutive failures
- F-280 + F-285 in flow-registry.json
- 11 domain-4 test files green
- 215-VALIDATION.md per-task verification map updated for Plan 04
</success_criteria>

<output>
After completion, create `.planning/phases/215-saas-suite-billing-payments-compliance/215-04-SUMMARY.md` documenting: artifacts created, PROCESSOR_WEBHOOK_REQUIRES_SIGNATURE_AND_IDEMPOTENCY exception name, 7-step routing pattern, P203 engine REUSE invariant (no greenfield), dispatcher HANDLERS map invariant (P226 iter 2 lesson), DIAN rejection routing through siigo-handler, downstream-phase impact (Plan 05 reads invoice.payment_failed events for dunning workflow; P216-P219 read invoice/payment-attempt updates from webhook handlers).
</output>
</content>
