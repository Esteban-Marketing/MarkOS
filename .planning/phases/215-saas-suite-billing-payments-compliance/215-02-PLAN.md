---
phase: 215
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
f_ids:
  - F-275
  - F-276
migration_slots:
  - 119
integrates_with:
  - "PRC-01..09 from P205 (saas_invoices.pricing_recommendation_id FK or sentinel; Stripe Tax calculation lookup uses P205-approved rate)"
  - "BILL-02 from P205 (pricing copy ownership)"
translation_gate_for: []
files_modified:
  - test/fixtures/saas-215/index.js
  - test/fixtures/saas-215/processor-config.js
  - test/fixtures/saas-215/accounting-config.js
  - scripts/preconditions/215-02-check-upstream.cjs
  - supabase/migrations/119_saas_billing_us_path.sql
  - lib/markos/saas/billing/us/contracts.ts
  - lib/markos/saas/billing/us/contracts.cjs
  - lib/markos/saas/billing/us/stripe-client.ts
  - lib/markos/saas/billing/us/stripe-tax.ts
  - lib/markos/saas/billing/us/quickbooks-client.ts
  - lib/markos/saas/billing/us/quickbooks-sync.ts
  - lib/markos/saas/billing/us/index.cjs
  - api/v1/saas/billing/us-config.js
  - contracts/F-275-saas-stripe-config-v1.yaml
  - contracts/F-276-saas-quickbooks-config-v1.yaml
  - contracts/flow-registry.json
  - test/saas-215/domain-2/processor-config-shape.test.js
  - test/saas-215/domain-2/accounting-config-shape.test.js
  - test/saas-215/domain-2/stripe-invoice-tax-trigger.test.js
  - test/saas-215/domain-2/stripe-tax-calculation.test.js
  - test/saas-215/domain-2/quickbooks-invoice-sync.test.js
  - test/saas-215/domain-2/credential-vault-trigger-stripe.test.js
  - test/saas-215/domain-2/mcp-credential-sanitize-stripe.test.js
  - test/saas-215/domain-2/log-redaction-stripe.test.js
  - test/saas-215/rls/processor-config-cross-tenant-denial.test.js
  - .planning/phases/215-saas-suite-billing-payments-compliance/215-VALIDATION.md
autonomous: true
requirements:
  - SAS-05
  - SAS-06
  - QA-01
  - QA-02
  - QA-08
  - QA-09
must_haves:
  truths:
    - "Plan 02 depends on Plan 01 — assertUpstreamReady CLI hard-checks saas_invoices + saas_credential_vault_refs tables exist (Plan 01 migration 118) before Plan 02 migration 119 applies; if Plan 01 has not run, Plan 02 halts at preflight per Q-7 SLOT ORDERING CRISIS resolution"
    - "Migration 119 (US path) ships saas_processor_configs table per RESEARCH §Domain 2 §saas_processor_configs Table Schema (lines 631-657) — RLS-enabled tenant_id-scoped + UNIQUE(tenant_id, processor_type, environment); columns: config_id PK uuid, tenant_id FK markos_orgs, processor_type CHECK ('stripe','mercado_pago'), environment CHECK ('sandbox','production'), countries text[] NOT NULL, is_active boolean DEFAULT false, config_jsonb (publishable_key + webhook_endpoint_id + stripe_account_id + automatic_tax_enabled — NO credential fields), supports_recurring, supports_local_methods, fallback_processor_id (self-FK for Mercado Pago→Stripe LATAM swap reservation per Q-8), created_at, updated_at"
    - "Migration 119 also ships saas_accounting_configs table per RESEARCH §Domain 2 §saas_accounting_configs Table Schema (lines 661-682) — RLS + UNIQUE(tenant_id, accounting_type, environment); columns: config_id PK, tenant_id FK, accounting_type CHECK ('quickbooks','siigo','alegra'), environment CHECK 2 values, is_active, account_mapping_jsonb (saas_revenue_account + ar_account + deferred_revenue_account + tax_payable_account + bank_account for QBO; income_account + iva_account + customer_account + bank_account for Siigo — NO credential fields), auto_sync, created_at, updated_at"
    - "DB-trigger STRIPE_INVOICE_REQUIRES_TAX_CALCULATION ships in migration 119 BEFORE INSERT OR UPDATE on saas_invoices — RAISE EXCEPTION (verbatim per RESEARCH §Domain 2 lines 687-705): processor='stripe' AND billing_country='US' AND status NOT IN ('draft','void') AND sales_tax_calculated_at IS NULL AND stripe_tax_calculation_id IS NULL → block; this trigger does NOT replace the Plan 01 INVOICE_REQUIRES_PRICING_AND_LEGAL_FIELDS trigger — both apply to saas_invoices simultaneously (BEFORE INSERT OR UPDATE — multiple triggers fire alphabetically by trigger name; both must pass for INSERT to succeed)"
    - "Architecture-lock surface from Plan 01 RE-RUN against Plan 02 paths — scanForbiddenPatterns({paths:['lib/markos/saas/billing/us/','api/v1/saas/billing/us-config.js']}); asserts ZERO violations; ALSO asserts CREDENTIAL_FORBIDDEN_LITERALS check on lib/markos/saas/billing/us/* — stripe_secret_key + stripe_webhook_secret + stripe_tax_api_key must NEVER appear as plaintext string literal except inside lib/markos/saas/billing/credential-vault.ts CREDENTIAL_FIELDS array (Plan 01 allowlist)"
    - "Stripe SDK installed at version ^14.x (verify via `npm view stripe version` at execution; pin in package.json); resolved at runtime via storeCredentialVaultRef + resolveCredential (Plan 01 surface) — stripe_secret_key fetched from Vault for in-process Stripe client init, NEVER persisted plaintext; lib/markos/saas/billing/us/stripe-client.ts initializes Stripe instance per request using resolveCredential; the Stripe client instance is request-scoped (not module-level) to avoid credential reuse across tenants"
    - "Stripe Tax integration via lib/markos/saas/billing/us/stripe-tax.ts exporting calculateTaxForInvoice({tenantId, invoiceId, lineItems, customerAddress}, supabase) — calls stripe.tax.calculations.create with line items and customer address; on success stores tax_calculation_id + sales_tax_calculated_at on saas_invoices (defense-in-depth pair with STRIPE_INVOICE_REQUIRES_TAX_CALCULATION trigger); supports US sales tax across all 50 states via Stripe automatic_tax"
    - "QuickBooks Online (QBO) integration via lib/markos/saas/billing/us/quickbooks-client.ts + quickbooks-sync.ts exporting syncInvoiceToQBO({tenantId, invoiceId}, supabase) — fetches invoice + customer + line items, calls QBO Invoice CreateInvoice API, stores returned QBO invoice_id on saas_invoices.quickbooks_invoice_id + accounting_synced_at timestamp; refund/credit path via createCreditMemoInQBO; idempotent via QBO RequestId header; OAuth token refresh handled by P210 connector substrate (soft dependency — if absent, manual refresh required, flagged in MANUAL-ONLY verifications)"
    - "Stripe webhook events handled by Plan 04 (declared here, IMPLEMENTED in Plan 04 webhook handler): customer.subscription.{created,updated,deleted} + invoice.{payment_succeeded,payment_failed,finalized} + customer.updated + payment_method.attached + charge.dispute.created (per RESEARCH §Domain 2 lines 718-728); Plan 02 ships ONLY the data shape contracts + Stripe client initialization + Stripe Tax + QBO sync libraries — Plan 04 wires these to webhook handler routing"
    - "QBO sync events handled per RESEARCH §Domain 2 lines 732-737: invoice.payment_succeeded → QBO CreateInvoice + CreatePayment; invoice refund/credit → QBO CreateCreditMemo; new customer → QBO CreateCustomer + sync tax ID; monthly reconciliation → QBO balance vs Stripe reconciliation task (manual review for v4.1.0)"
    - "5 credential ENUMs for Plan 02 (US path) ALL flow through Plan 01 storeCredentialVaultRef → saas_credential_vault_refs (BILLING_CREDENTIAL_REQUIRES_VAULT_REF DB-trigger validates pattern): stripe_secret_key, stripe_webhook_secret, stripe_tax_api_key, quickbooks_oauth_token, quickbooks_realm_id"
    - "Defense-in-depth credential test (test/saas-215/domain-2/credential-vault-trigger-stripe.test.js): for each of the 5 US credentials INSERT plaintext into saas_credential_vault_refs → expect EXCEPTION 'BILLING_CREDENTIAL_REQUIRES_VAULT_REF'; INSERT through storeCredentialVaultRef helper → succeeds (vault:// or pgcrypto-ref: ref persisted); resolveCredential returns plaintext to in-process caller (verify via Vault test fixture)"
    - "MCP credential sanitization test (test/saas-215/domain-2/mcp-credential-sanitize-stripe.test.js) — invokes saas-billing.cjs get_invoice with US Stripe invoice fixture; asserts no stripe_secret_key / stripe_webhook_secret / stripe_tax_api_key / quickbooks_oauth_token / quickbooks_realm_id appears in output (sanitizeBillingResponse from Plan 01 strips these)"
    - "Log redaction test (test/saas-215/domain-2/log-redaction-stripe.test.js) — invokes redactWebhookPayload(stripePayload, 'stripe') with payload containing email/name/last4/fingerprint/ip_address/brand → all replaced with '[REDACTED]'; saas_billing_events.payload_redacted column ALWAYS contains the redacted version (raw Stripe payload NEVER persisted)"
    - "2 contracts ship — F-275 (saas-stripe-config-v1) covering saas_processor_configs Stripe-row schema + Stripe client init pattern + 9 Stripe webhook event types (Plan 04 implements handler) + STRIPE_INVOICE_REQUIRES_TAX_CALCULATION exception + Stripe Tax calculation pattern; F-276 (saas-quickbooks-config-v1) covering saas_accounting_configs QuickBooks-row schema + QBO Invoice/Payment/CreditMemo sync patterns + OAuth token refresh dependency on P210; NO collision with F-001..F-274"
    - "API handler `api/v1/saas/billing/us-config.js` ships GET /v1/saas/billing/us-config (returns sanitized US processor + accounting config status — has Stripe configured? has QBO configured? environment? automatic_tax_enabled?) + POST /v1/saas/billing/us-config (operator action — supplies plaintext credentials in request body which are immediately stored via Plan 01 storeCredentialVaultRef + persisted as vault-ref; request body cleared from request log; response NEVER echoes credentials)"
    - "Cross-tenant denial test (test/saas-215/rls/processor-config-cross-tenant-denial.test.js) — INSERT saas_processor_configs under tenant A then SELECT under tenant B returns 0 rows; same for saas_accounting_configs; same for saas_credential_vault_refs (already covered by Plan 01 but defensive RE-RUN here)"
    - "PRC-01..09 + BILL-02 documented in frontmatter as `integrates_with: P205` (NOT requirements) — P205 OWNS Pricing Engine substrate; P215 invoices reference pricing_recommendation_id FK or sentinel; Stripe Tax calculation may use P205-approved rate adjustments (future v4.2.0 — currently uses Stripe's default tax rates)"
    - "215-VALIDATION.md per-task verification map updated with rows for Plan 02 + domain-2 test paths under `test/saas-215/domain-2/`"
  artifacts:
    - path: scripts/preconditions/215-02-check-upstream.cjs
      provides: invokes assertUpstreamReady from Plan 01 surface; HARD-checks saas_invoices + saas_credential_vault_refs (Plan 01) + saas_suite_activations (P214); soft-checks P205/P206; exit 1 on hard miss
    - path: supabase/migrations/119_saas_billing_us_path.sql
      provides: combined migration — saas_processor_configs + saas_accounting_configs SOR tables + STRIPE_INVOICE_REQUIRES_TAX_CALCULATION DB-trigger + RLS policies + UNIQUE constraints
    - path: lib/markos/saas/billing/us/contracts.ts
      provides: zod schemas + enum constants — STRIPE_WEBHOOK_EVENTS (9) + QBO_SYNC_EVENTS (4) + US_CREDENTIAL_FIELDS (5) + StripeProcessorConfigSchema + QuickBooksAccountingConfigSchema zod parity with F-275/F-276 YAMLs
      exports: [STRIPE_WEBHOOK_EVENTS, QBO_SYNC_EVENTS, US_CREDENTIAL_FIELDS, StripeProcessorConfigSchema, QuickBooksAccountingConfigSchema]
    - path: lib/markos/saas/billing/us/stripe-client.ts
      provides: getStripeClient({tenantId, environment}, supabase) returns request-scoped Stripe SDK instance initialized with stripe_secret_key fetched from Vault via Plan 01 resolveCredential; never module-level (avoids cross-tenant credential reuse)
      exports: [getStripeClient]
    - path: lib/markos/saas/billing/us/stripe-tax.ts
      provides: calculateTaxForInvoice({tenantId, invoiceId, lineItems, customerAddress}, supabase) → calls stripe.tax.calculations.create + stores tax_calculation_id + sales_tax_calculated_at on saas_invoices (defense-in-depth with STRIPE_INVOICE_REQUIRES_TAX_CALCULATION trigger); supports US 50 states
      exports: [calculateTaxForInvoice]
    - path: lib/markos/saas/billing/us/quickbooks-client.ts
      provides: getQBOClient({tenantId, environment}, supabase) returns request-scoped QuickBooks Online API client initialized with quickbooks_oauth_token + quickbooks_realm_id from Vault; OAuth token refresh delegated to P210 substrate (soft fallback flagged)
      exports: [getQBOClient]
    - path: lib/markos/saas/billing/us/quickbooks-sync.ts
      provides: syncInvoiceToQBO + createCreditMemoInQBO + syncCustomerToQBO + monthlyReconciliationCheck helpers; idempotent via QBO RequestId header; stores returned QBO invoice_id on saas_invoices.quickbooks_invoice_id
      exports: [syncInvoiceToQBO, createCreditMemoInQBO, syncCustomerToQBO, monthlyReconciliationCheck]
    - path: lib/markos/saas/billing/us/index.cjs
      provides: CommonJS twin re-exporting full surface
    - path: api/v1/saas/billing/us-config.js
      provides: GET /v1/saas/billing/us-config (sanitized status) + POST /v1/saas/billing/us-config (operator stores credentials via Plan 01 storeCredentialVaultRef); legacy api/*.js handler — uses requireHostedSupabaseAuth + tenant-scoped supabase client; responses ALWAYS pass through sanitizeBillingResponse
    - path: contracts/F-275-saas-stripe-config-v1.yaml
      provides: documents saas_processor_configs Stripe-row schema + Stripe webhook event mapping (9 events; Plan 04 implements handler) + STRIPE_INVOICE_REQUIRES_TAX_CALCULATION exception + Stripe Tax integration pattern + 3 Stripe credential fields (stripe_secret_key + stripe_webhook_secret + stripe_tax_api_key) all stored via Plan 01 saas_credential_vault_refs
    - path: contracts/F-276-saas-quickbooks-config-v1.yaml
      provides: documents saas_accounting_configs QuickBooks-row schema + QBO Invoice/Payment/CreditMemo sync patterns + OAuth token refresh dependency + 2 QBO credential fields
    - path: contracts/flow-registry.json
      provides: 2 entries appended for F-275 + F-276; NO collision with F-001..F-274
    - path: test/fixtures/saas-215/processor-config.js
      provides: processorConfigFactory returning a row shape for saas_processor_configs
    - path: test/fixtures/saas-215/accounting-config.js
      provides: accountingConfigFactory returning a row shape for saas_accounting_configs
    - path: test/saas-215/domain-2/processor-config-shape.test.js
      provides: zod parity assertion against F-275 YAML; SaaS Stripe config shape validates
    - path: test/saas-215/domain-2/accounting-config-shape.test.js
      provides: zod parity assertion against F-276 YAML; QBO config shape validates
    - path: test/saas-215/domain-2/stripe-invoice-tax-trigger.test.js
      provides: Postgres-backed test — INSERT saas_invoices with processor='stripe' + billing_country='US' + status='open' + sales_tax_calculated_at IS NULL + stripe_tax_calculation_id IS NULL → expect EXCEPTION 'STRIPE_INVOICE_REQUIRES_TAX_CALCULATION'; with sales_tax_calculated_at populated → succeeds; with stripe_tax_calculation_id populated → succeeds; status='draft' → trigger skips (succeeds)
    - path: test/saas-215/domain-2/stripe-tax-calculation.test.js
      provides: mocked Stripe SDK test — calculateTaxForInvoice with sample line items + customer address → calls stripe.tax.calculations.create + stores tax_calculation_id + sales_tax_calculated_at; verifies INSERT path satisfies STRIPE_INVOICE_REQUIRES_TAX_CALCULATION trigger
    - path: test/saas-215/domain-2/quickbooks-invoice-sync.test.js
      provides: mocked QBO SDK test — syncInvoiceToQBO with paid invoice → calls QBO CreateInvoice + CreatePayment; stores returned quickbooks_invoice_id + accounting_synced_at on saas_invoices; idempotent on retry (RequestId header)
    - path: test/saas-215/domain-2/credential-vault-trigger-stripe.test.js
      provides: for each of 5 US credentials INSERT plaintext → exception; INSERT through storeCredentialVaultRef → succeeds with vault:// or pgcrypto-ref: pattern
    - path: test/saas-215/domain-2/mcp-credential-sanitize-stripe.test.js
      provides: invokes get_invoice MCP tool with US Stripe invoice fixture; asserts 5 US credential fields absent from output (sanitizeBillingResponse from Plan 01)
    - path: test/saas-215/domain-2/log-redaction-stripe.test.js
      provides: redactWebhookPayload with Stripe charge.succeeded payload (email, name, address, phone, last4, fingerprint, ip_address, brand) → all replaced with '[REDACTED]'
    - path: test/saas-215/rls/processor-config-cross-tenant-denial.test.js
      provides: INSERT saas_processor_configs + saas_accounting_configs under tenant A; SELECT under tenant B returns 0 rows
  key_links:
    - from: lib/markos/saas/billing/us/stripe-client.ts
      to: lib/markos/saas/billing/credential-vault.ts (Plan 01)
      via: imports resolveCredential — fetches stripe_secret_key from saas_credential_vault_refs at request time; never module-level
      pattern: "resolveCredential"
    - from: lib/markos/saas/billing/us/stripe-tax.ts
      to: supabase saas_invoices
      via: stores tax_calculation_id + sales_tax_calculated_at; defense-in-depth pair with STRIPE_INVOICE_REQUIRES_TAX_CALCULATION trigger from migration 119
      pattern: "STRIPE_INVOICE_REQUIRES_TAX_CALCULATION"
    - from: lib/markos/saas/billing/us/quickbooks-sync.ts
      to: supabase saas_invoices.quickbooks_invoice_id + accounting_synced_at
      via: syncInvoiceToQBO updates these columns after QBO CreateInvoice
      pattern: "quickbooks_invoice_id"
    - from: api/v1/saas/billing/us-config.js
      to: lib/markos/saas/billing/us/index.cjs + lib/markos/saas/billing/credential-vault.ts (Plan 01)
      via: POST handler stores plaintext credentials immediately via storeCredentialVaultRef; response sanitized via sanitizeBillingResponse
      pattern: "storeCredentialVaultRef"
    - from: scripts/preconditions/215-02-check-upstream.cjs
      to: supabase saas_invoices + saas_credential_vault_refs (Plan 01) + saas_suite_activations (P214)
      via: assertUpstreamReady from Plan 01; exit 1 if any HARD table missing
      pattern: "assertUpstreamReady"
---

<objective>
Ship Wave 2 US billing path: saas_processor_configs (Stripe row) + saas_accounting_configs (QuickBooks row) SOR tables in migration 119 + STRIPE_INVOICE_REQUIRES_TAX_CALCULATION DB-trigger (verbatim per RESEARCH §Domain 2 — defense-in-depth pair with app-layer Stripe Tax calculation), Stripe Billing SDK integration with request-scoped client initialization (lib/markos/saas/billing/us/stripe-client.ts using Plan 01 resolveCredential — Stripe credentials fetched from Supabase Vault per request, never module-level), Stripe Tax calculation library (calculates US sales tax across 50 states; stores tax_calculation_id + sales_tax_calculated_at on saas_invoices), QuickBooks Online sync library (Invoice + Payment + CreditMemo + Customer sync; idempotent via RequestId header; OAuth token refresh delegated to P210 soft dependency), 2 F-ID contracts (F-275 stripe-config + F-276 quickbooks-config), 5 US credential ENUMs flowing through Plan 01 saas_credential_vault_refs (stripe_secret_key + stripe_webhook_secret + stripe_tax_api_key + quickbooks_oauth_token + quickbooks_realm_id) — all 4 HIGH 2 defense-in-depth layers RE-applied (Layer 1 storage via storeCredentialVaultRef + Layer 2 BILLING_CREDENTIAL_REQUIRES_VAULT_REF trigger from Plan 01 + Layer 3 MCP sanitization via Plan 01 saas-billing.cjs + Layer 4 log redaction via Plan 01 redactWebhookPayload with STRIPE_PII_FIELDS), 1 API handler (api/v1/saas/billing/us-config.js for operator credential setup), and the cross-tenant RLS denial RE-RUN.

Purpose: satisfy SAS-05 (SaaS Billing Engine — US Stripe path with sales tax + QBO accounting sync) + SAS-06 (Launch-country compliance — US Stripe Tax + QuickBooks); QA-01..02 + QA-08..09 (architecture-lock RE-RUN + RLS isolation + tax compliance + credential sanitization). Plan 02 unblocks Plan 04 (webhook handler routing) for Stripe events — Plan 04 will REUSE Plan 02's stripe-client.ts + stripe-tax.ts for invoice.payment_succeeded handlers.

Output: 1 preflight CLI + 1 SOR migration (119 — combined US processor + accounting configs + STRIPE_INVOICE_REQUIRES_TAX_CALCULATION trigger) + 7 us-path lib files (contracts + stripe-client + stripe-tax + quickbooks-client + quickbooks-sync + index.cjs) + 1 API handler + 2 contract YAMLs + flow-registry update + 2 fixture files + 8 domain-2 test files + 1 RLS test + populated VALIDATION.md.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/215-saas-suite-billing-payments-compliance/215-CONTEXT.md
@.planning/phases/215-saas-suite-billing-payments-compliance/215-RESEARCH.md
@.planning/phases/215-saas-suite-billing-payments-compliance/215-VALIDATION.md
@.planning/phases/215-saas-suite-billing-payments-compliance/215-01-PLAN.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@lib/markos/saas/billing/preflight/index.cjs
@lib/markos/saas/billing/index.cjs
@lib/markos/mcp/tools/saas-billing.cjs
@onboarding/backend/runtime-context.cjs

<interfaces>
<!-- Plan 01 surface (DEPENDENCY) — Plan 02 imports from these directly, no codebase exploration needed -->

From lib/markos/saas/billing/credential-vault.ts (Plan 01):

```typescript
function storeCredentialVaultRef({tenantId, credentialName, plaintextValue, configTable, configId}, supabase): Promise<{vault_ref: string}>
function resolveCredential({tenantId, credentialName, configId}, supabase): Promise<string>  // returns plaintext to in-process caller ONLY
const CREDENTIAL_FIELDS: readonly string[]  // 12 + 'vault_ref'
```

From lib/markos/saas/billing/log-redaction.ts (Plan 01):

```typescript
function redactWebhookPayload(payload: object, processor: 'stripe'|'mercado_pago'|'quickbooks'|'siigo'|'dian'): object
const STRIPE_PII_FIELDS: readonly string[]  // ['email','name','address','phone','last4','fingerprint','ip_address','brand']
```

From lib/markos/mcp/tools/saas-billing.cjs (Plan 01):

```javascript
function sanitizeBillingResponse(obj): obj  // recursively strips 12 CREDENTIAL_FIELDS + vault_ref
```

Stripe webhook events handled via Plan 04 (RESEARCH §Domain 2 lines 718-728):

| Event | Action |
|-------|--------|
| customer.subscription.created | create/update saas_subscriptions (P214 mirror) |
| customer.subscription.updated | sync status changes |
| customer.subscription.deleted | trigger dunning/churn workflow |
| invoice.payment_succeeded | update saas_invoices + trigger QBO sync |
| invoice.payment_failed | create saas_payment_attempts (failed) + start dunning |
| invoice.finalized | update saas_invoices.status='open' |
| customer.updated | sync customer data |
| payment_method.attached | update processor config |
| charge.dispute.created | P0 alert + freeze account |

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0.5: Plan 02 preflight + Plan 01 architecture-lock RE-RUN against US path</name>
  <files>scripts/preconditions/215-02-check-upstream.cjs</files>
  <read_first>
    - lib/markos/saas/billing/preflight/index.cjs (Plan 01 — REUSE assertUpstreamReady)
    - scripts/preconditions/215-01-check-upstream.cjs (Plan 01 — mirror pattern)
    - api/cron/webhooks-dlq-purge.js (createClient pattern)
  </read_first>
  <action>
    Step 1: Create scripts/preconditions/215-02-check-upstream.cjs — invokes assertUpstreamReady from Plan 01 surface; HARD = [saas_invoices (Plan 01), saas_credential_vault_refs (Plan 01), saas_suite_activations (P214), saas_subscriptions (P214)]; SOFT = [pricing_recommendations (P205), governance_evidence_packs (P206), markos_agent_runs (P207)]; matches api/cron/webhooks-dlq-purge.js createClient pattern verbatim; exit 1 on hard miss.

    Step 2: NO new architecture-lock test file — Plan 06 closeout regression will RE-RUN scanForbiddenPatterns across lib/markos/saas/billing/us/*. The Plan 01 architecture-lock test scope already includes lib/markos/saas/billing/* recursively, so Plan 02 surface auto-coverage applies.
  </action>
  <verify>
    <automated>node scripts/preconditions/215-02-check-upstream.cjs</automated>
  </verify>
  <done>Preflight CLI hard-fails on missing Plan 01 tables; soft-warns on missing P205/P206/P207.</done>
</task>

<task type="auto">
  <name>Task 1: Migration 119 + 7 us-path lib files + Stripe Tax + QuickBooks sync + 2 contracts + API handler + domain-2 + RLS tests</name>
  <files>supabase/migrations/119_saas_billing_us_path.sql, lib/markos/saas/billing/us/contracts.ts, lib/markos/saas/billing/us/contracts.cjs, lib/markos/saas/billing/us/stripe-client.ts, lib/markos/saas/billing/us/stripe-tax.ts, lib/markos/saas/billing/us/quickbooks-client.ts, lib/markos/saas/billing/us/quickbooks-sync.ts, lib/markos/saas/billing/us/index.cjs, api/v1/saas/billing/us-config.js, contracts/F-275-saas-stripe-config-v1.yaml, contracts/F-276-saas-quickbooks-config-v1.yaml, contracts/flow-registry.json, test/fixtures/saas-215/index.js, test/fixtures/saas-215/processor-config.js, test/fixtures/saas-215/accounting-config.js, test/saas-215/domain-2/processor-config-shape.test.js, test/saas-215/domain-2/accounting-config-shape.test.js, test/saas-215/domain-2/stripe-invoice-tax-trigger.test.js, test/saas-215/domain-2/stripe-tax-calculation.test.js, test/saas-215/domain-2/quickbooks-invoice-sync.test.js, test/saas-215/domain-2/credential-vault-trigger-stripe.test.js, test/saas-215/domain-2/mcp-credential-sanitize-stripe.test.js, test/saas-215/domain-2/log-redaction-stripe.test.js, test/saas-215/rls/processor-config-cross-tenant-denial.test.js, .planning/phases/215-saas-suite-billing-payments-compliance/215-VALIDATION.md</files>
  <read_first>
    - .planning/phases/215-saas-suite-billing-payments-compliance/215-RESEARCH.md §Domain 2 §saas_processor_configs Table Schema §saas_accounting_configs Table Schema §DB-Trigger Compliance STRIPE_INVOICE_REQUIRES_TAX_CALCULATION §Stripe Integration Specifics §Credential ENUM list for Plan 02
    - obsidian/work/incoming/16-SAAS-SUITE.md Part 3 (Stripe + QuickBooks integration) + Part 4 (US Compliance)
    - lib/markos/saas/billing/credential-vault.ts (Plan 01 — REUSE storeCredentialVaultRef + resolveCredential)
    - lib/markos/saas/billing/log-redaction.ts (Plan 01 — REUSE redactWebhookPayload + STRIPE_PII_FIELDS)
    - lib/markos/mcp/tools/saas-billing.cjs (Plan 01 — REUSE sanitizeBillingResponse)
    - lib/markos/webhooks/signing.cjs (P203 — Plan 04 will REUSE for Stripe webhook signature verification)
    - supabase/migrations/118_saas_billing_invoice_contracts.sql (Plan 01 — saas_invoices schema reference for cross-trigger interaction)
    - supabase/migrations/100_crm_schema_identity_graph_hardening.sql (RLS pattern reference)
  </read_first>
  <action>
    Step 1: Create supabase/migrations/119_saas_billing_us_path.sql:
      - CREATE TABLE saas_processor_configs (full schema per RESEARCH §Domain 2 lines 631-657 — verbatim columns + RLS + UNIQUE(tenant_id, processor_type, environment))
      - ALTER TABLE saas_processor_configs ENABLE ROW LEVEL SECURITY + CREATE POLICY spc_tenant_isolation
      - CREATE TABLE saas_accounting_configs (full schema per RESEARCH §Domain 2 lines 661-682 — verbatim + RLS + UNIQUE(tenant_id, accounting_type, environment))
      - ALTER TABLE saas_accounting_configs ENABLE ROW LEVEL SECURITY + CREATE POLICY sac_tenant_isolation
      - CREATE OR REPLACE FUNCTION enforce_stripe_invoice_tax() — verbatim per RESEARCH lines 687-705 (RAISE EXCEPTION 'STRIPE_INVOICE_REQUIRES_TAX_CALCULATION' on processor='stripe' AND billing_country='US' AND status NOT IN ('draft','void') AND sales_tax_calculated_at IS NULL AND stripe_tax_calculation_id IS NULL)
      - CREATE TRIGGER trg_stripe_invoice_tax BEFORE INSERT OR UPDATE ON saas_invoices FOR EACH ROW EXECUTE FUNCTION enforce_stripe_invoice_tax()
      - Note: trg_invoice_pricing_and_legal (Plan 01) and trg_stripe_invoice_tax (Plan 02) BOTH apply to saas_invoices; trigger fire order is alphabetical by trigger name; both must pass for INSERT to succeed (defense-in-depth)

    Step 2: Create lib/markos/saas/billing/us/contracts.ts exporting STRIPE_WEBHOOK_EVENTS (9 per RESEARCH lines 718-728) + QBO_SYNC_EVENTS (4 per RESEARCH lines 732-737) + US_CREDENTIAL_FIELDS = ['stripe_secret_key','stripe_webhook_secret','stripe_tax_api_key','quickbooks_oauth_token','quickbooks_realm_id'] + StripeProcessorConfigSchema + QuickBooksAccountingConfigSchema zod parity with F-275/F-276 YAMLs.

    Step 3: Create lib/markos/saas/billing/us/stripe-client.ts:
      - Import resolveCredential from Plan 01
      - Export getStripeClient({tenantId, environment}, supabase) → fetches stripe_secret_key via resolveCredential + initializes Stripe SDK instance request-scoped (NOT module-level — avoids cross-tenant credential reuse)
      - Validate stripe_secret_key starts with 'sk_test_' (sandbox) or 'sk_live_' (production) — defense-in-depth check

    Step 4: Create lib/markos/saas/billing/us/stripe-tax.ts:
      - Import getStripeClient
      - Export calculateTaxForInvoice({tenantId, invoiceId, lineItems, customerAddress}, supabase) → fetches Stripe client, calls stripe.tax.calculations.create({line_items, customer_details: {address: customerAddress}, currency: 'usd'}); on success UPDATE saas_invoices SET stripe_tax_calculation_id = result.id, sales_tax_calculated_at = now(), tax_total = result.tax_amount_exclusive, tax_lines = result.tax_breakdown WHERE invoice_id = invoiceId; defense-in-depth pair with STRIPE_INVOICE_REQUIRES_TAX_CALCULATION trigger

    Step 5: Create lib/markos/saas/billing/us/quickbooks-client.ts:
      - Import resolveCredential from Plan 01
      - Export getQBOClient({tenantId, environment}, supabase) → fetches quickbooks_oauth_token + quickbooks_realm_id via resolveCredential; initializes QBO API client request-scoped; OAuth refresh logic: if 401 from QBO → flag P210 connector substrate (soft) + log SOFT_DEGRADE warning + return null (caller handles)

    Step 6: Create lib/markos/saas/billing/us/quickbooks-sync.ts exporting:
      - syncInvoiceToQBO({tenantId, invoiceId}, supabase) → fetches saas_invoices + saas_subscriptions + customer; constructs QBO Invoice payload; idempotent via RequestId header (= invoiceId); calls QBO Invoice API; UPDATE saas_invoices SET quickbooks_invoice_id = result.Id, accounting_synced_at = now()
      - createCreditMemoInQBO({tenantId, originalInvoiceId, amount, reason}, supabase) → calls QBO CreditMemo API
      - syncCustomerToQBO({tenantId, customerId}, supabase) → calls QBO Customer Create or Update
      - monthlyReconciliationCheck({tenantId, month}, supabase) → fetches QBO balance vs Stripe balance; logs discrepancies for human review (manual reconciliation flagged in MANUAL-ONLY)

    Step 7: Create lib/markos/saas/billing/us/index.cjs CommonJS twin re-exporting full surface.

    Step 8: Create api/v1/saas/billing/us-config.js handler:
      - GET /v1/saas/billing/us-config: requires requireHostedSupabaseAuth; returns sanitized status — `{stripe: {configured: bool, environment, automatic_tax_enabled, countries: ['US']}, quickbooks: {configured: bool, environment, auto_sync, last_synced_at}}`; response passes through sanitizeBillingResponse
      - POST /v1/saas/billing/us-config: requires requireHostedSupabaseAuth + admin role; body `{stripe: {secret_key, webhook_secret, tax_api_key?, environment}, quickbooks: {oauth_token, realm_id, environment}}`; immediately stores plaintext credentials via Plan 01 storeCredentialVaultRef → vault_ref persisted in saas_credential_vault_refs (BILLING_CREDENTIAL_REQUIRES_VAULT_REF trigger validates pattern); request body cleared from request log; response `{success: true, stripe_config_id, quickbooks_config_id}` — NEVER echoes credentials

    Step 9: Create 2 contract YAMLs (F-275 + F-276) per RESEARCH §F-ID Pre-Allocation Table. Append 2 entries to contracts/flow-registry.json.

    Step 10: Add processorConfigFactory + accountingConfigFactory to test/fixtures/saas-215/.

    Step 11: Create 8 test files:
      - processor-config-shape.test.js: zod parity vs F-275 YAML
      - accounting-config-shape.test.js: zod parity vs F-276 YAML
      - stripe-invoice-tax-trigger.test.js: Postgres-backed — INSERT saas_invoices with processor='stripe' + billing_country='US' + status='open' + sales_tax_calculated_at IS NULL + stripe_tax_calculation_id IS NULL → exception 'STRIPE_INVOICE_REQUIRES_TAX_CALCULATION'; with sales_tax_calculated_at populated → succeeds; status='draft' + tax fields NULL → trigger skips (succeeds)
      - stripe-tax-calculation.test.js: mocked Stripe SDK — calculateTaxForInvoice with fixture line items + CA address → calls stripe.tax.calculations.create + UPDATE saas_invoices.stripe_tax_calculation_id + sales_tax_calculated_at; verifies satisfies trigger
      - quickbooks-invoice-sync.test.js: mocked QBO SDK — syncInvoiceToQBO with paid invoice → CreateInvoice + CreatePayment; idempotent on retry
      - credential-vault-trigger-stripe.test.js: 5 US credentials INSERT plaintext → exception; via storeCredentialVaultRef → succeeds with vault:// pattern
      - mcp-credential-sanitize-stripe.test.js: get_invoice MCP tool with Stripe invoice fixture → 5 US credential fields absent
      - log-redaction-stripe.test.js: redactWebhookPayload(stripePayload, 'stripe') → 8 PII fields all '[REDACTED]'

    Step 12: Create test/saas-215/rls/processor-config-cross-tenant-denial.test.js — INSERT saas_processor_configs + saas_accounting_configs under tenant A; SELECT under tenant B returns 0 rows.

    Step 13: Append Plan 02 row to .planning/phases/215-saas-suite-billing-payments-compliance/215-VALIDATION.md per-task verification map.
  </action>
  <verify>
    <automated>npm test -- test/saas-215/domain-2/ test/saas-215/rls/processor-config-cross-tenant-denial.test.js</automated>
  </verify>
  <done>Migration 119 applies cleanly; saas_processor_configs + saas_accounting_configs exist with RLS + STRIPE_INVOICE_REQUIRES_TAX_CALCULATION trigger; calculateTaxForInvoice satisfies trigger; syncInvoiceToQBO writes quickbooks_invoice_id; F-275 + F-276 in flow-registry.json; 8 domain-2 + 1 RLS test files green; 215-VALIDATION.md updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→/v1/saas/billing/us-config POST | Operator submits plaintext Stripe + QBO credentials; immediately stored via Plan 01 storeCredentialVaultRef; request body cleared from log |
| Stripe SDK→saas_credential_vault_refs | Stripe credentials fetched per-request via resolveCredential; never module-level (cross-tenant leak avoided) |
| stripe.tax.calculations.create→Stripe API | External call carrying tenant address + line items; Stripe response stored as tax_calculation_id (no PII in our DB) |
| QBO API→saas_invoices.quickbooks_invoice_id | QBO sync stores returned ID; OAuth token refresh delegated to P210 (soft fallback) |
| Stripe webhook→Plan 04 handler | Signature verified via P203 lib/markos/webhooks/signing.cjs; payload redacted via Plan 01 redactWebhookPayload before persist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-215-02-01 | T | saas_invoices INSERT without Stripe Tax calculation | mitigate | STRIPE_INVOICE_REQUIRES_TAX_CALCULATION DB-trigger blocks insert; defense-in-depth pair with calculateTaxForInvoice app-layer |
| T-215-02-02 | I | Stripe webhook payload PII in logs | mitigate | redactWebhookPayload (Plan 01) called BEFORE any console.log; STRIPE_PII_FIELDS list applied |
| T-215-02-03 | T | Cross-tenant Stripe credential reuse via module-level client | mitigate | getStripeClient is request-scoped; resolves credential per-request via Plan 01 resolveCredential |
| T-215-02-04 | I | QBO OAuth token leak via response body | mitigate | sanitizeBillingResponse strips quickbooks_oauth_token from MCP + API output |
| T-215-02-05 | E | Privilege escalation via saas_processor_configs UPDATE | mitigate | RLS spc_tenant_isolation enforces tenant_id; service-role-only writes via api/v1/saas/billing/us-config admin role |
| T-215-02-06 | T | QBO CreateInvoice double-write on retry | mitigate | RequestId header = invoiceId for idempotency |
| T-215-02-07 | R | POST /v1/saas/billing/us-config credential submission | mitigate | Audit log emit via lib/markos/audit/* (hash chain per migration 82); request body cleared from request log; only vault_ref + config_id persisted |
</threat_model>

<verification>
- node scripts/preconditions/215-02-check-upstream.cjs (exits 0 when Plan 01 tables present)
- supabase migration up to slot 119 applies cleanly
- npm test -- test/saas-215/domain-2/
- npm test -- test/saas-215/rls/processor-config-cross-tenant-denial.test.js
</verification>

<success_criteria>
- saas_processor_configs + saas_accounting_configs tables exist with RLS + STRIPE_INVOICE_REQUIRES_TAX_CALCULATION trigger
- getStripeClient initializes per-request from Vault (not module-level)
- calculateTaxForInvoice satisfies STRIPE_INVOICE_REQUIRES_TAX_CALCULATION trigger
- syncInvoiceToQBO stores quickbooks_invoice_id + accounting_synced_at; idempotent
- 5 US credential fields stored exclusively in saas_credential_vault_refs (Plan 01)
- F-275 + F-276 in flow-registry.json
- 8 domain-2 + 1 RLS test files green
- 215-VALIDATION.md per-task verification map updated for Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/215-saas-suite-billing-payments-compliance/215-02-SUMMARY.md` documenting: artifacts created, STRIPE_INVOICE_REQUIRES_TAX_CALCULATION exception name, 5 US credential ENUMs flowing through Plan 01 saas_credential_vault_refs, request-scoped Stripe client pattern, QBO sync idempotency pattern, downstream-phase impact (Plan 04 will REUSE getStripeClient + calculateTaxForInvoice for Stripe webhook handlers).
</output>
</content>
