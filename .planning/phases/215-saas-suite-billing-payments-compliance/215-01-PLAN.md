---
phase: 215
plan: "01"
type: execute
wave: 1
depends_on: []
f_ids:
  - F-271
  - F-272
  - F-273
  - F-274
  - F-284
migration_slots:
  - 118
integrates_with:
  - "PRC-01..09 from P205 (saas_invoices.pricing_recommendation_id FK or sentinel; INVOICE_REQUIRES_PRICING_AND_LEGAL_FIELDS trigger; P205 OWNS Pricing Engine substrate)"
  - "WHK-01 from P203 (Plan 04 reuses lib/markos/webhooks/* engine; this plan ships saas_billing_events table that webhook handlers write into)"
  - "BILL-02 from P205 (pricing copy ownership stays in P205; saas_invoices REFERENCES not redefines)"
translation_gate_for: []
files_modified:
  - .planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md
  - test/fixtures/saas-215/index.js
  - test/fixtures/saas-215/invoice.js
  - test/fixtures/saas-215/payment-attempt.js
  - test/fixtures/saas-215/billing-event.js
  - test/fixtures/saas-215/credential-vault-ref.js
  - lib/markos/saas/billing/preflight/upstream-gate.ts
  - lib/markos/saas/billing/preflight/architecture-lock.ts
  - lib/markos/saas/billing/preflight/errors.ts
  - lib/markos/saas/billing/preflight/index.cjs
  - scripts/preconditions/215-01-check-upstream.cjs
  - test/saas-215/preflight/wave-0-baseline.test.js
  - test/saas-215/preflight/architecture-lock.test.js
  - test/saas-215/preflight/upstream-gate.test.js
  - test/saas-215/preflight/helper-presence.test.js
  - test/saas-215/preflight/credential-sanitization.test.js
  - supabase/migrations/118_saas_billing_invoice_contracts.sql
  - lib/markos/saas/billing/contracts.ts
  - lib/markos/saas/billing/contracts.cjs
  - lib/markos/saas/billing/invoice.ts
  - lib/markos/saas/billing/payment-attempt.ts
  - lib/markos/saas/billing/billing-event.ts
  - lib/markos/saas/billing/credential-vault.ts
  - lib/markos/saas/billing/log-redaction.ts
  - lib/markos/saas/billing/llm-shield.ts
  - lib/markos/saas/billing/index.cjs
  - lib/markos/mcp/tools/saas-billing.cjs
  - lib/markos/mcp/tools/index.cjs
  - api/v1/saas/billing.js
  - api/v1/saas/invoices.js
  - contracts/F-271-saas-invoice-v1.yaml
  - contracts/F-272-saas-payment-attempt-v1.yaml
  - contracts/F-273-saas-billing-event-v1.yaml
  - contracts/F-274-saas-credential-vault-ref-v1.yaml
  - contracts/F-284-saas-billing-api-v1.yaml
  - contracts/flow-registry.json
  - test/saas-215/domain-1/invoice-pricing-trigger.test.js
  - test/saas-215/domain-1/invoice-tax-jurisdiction-trigger.test.js
  - test/saas-215/domain-1/invoice-legal-number-trigger.test.js
  - test/saas-215/domain-1/payment-attempt-idempotency.test.js
  - test/saas-215/domain-1/billing-event-shape.test.js
  - test/saas-215/domain-1/credential-vault-trigger.test.js
  - test/saas-215/domain-1/mcp-credential-sanitize.test.js
  - test/saas-215/domain-1/log-redaction.test.js
  - test/saas-215/rls/invoice-cross-tenant-denial.test.js
  - .planning/phases/215-saas-suite-billing-payments-compliance/215-VALIDATION.md
autonomous: true
requirements:
  - SAS-04
  - SAS-05
  - BILL-01
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - QA-05
  - QA-06
  - QA-07
must_haves:
  truths:
    - "Wave 0 baseline guard test asserts NO migration 118 with saas_billing prefix exists, NO contracts F-271..F-274 + F-284 exist, NO V4.1.0-MIGRATION-SLOT-COORDINATION.md exists OR if present has NO P215 reservation row before this plan ships (P215 is FIRST V4.1.0 phase to execute; CREATE-or-APPEND defensive pattern handles the case where P216/P217 ran first in some replay scenario)"
    - "Migration slot coordination document `.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md` is CREATED-or-APPENDED with the full v4.1.0 history per Q-7 SLOT ORDERING CRISIS resolution: foundation rows (82-89, 96, 100), P220 (90-95+97), P217 (98-99), P218 (101-106), P219 (107-111), P216 (112-117), P215 (118-123 + F-271..F-286) and Q-7 commentary block — `P215 execution-order BEFORE P216-P220 (foundational) but slot-order LATEST in V4.1.0 (118-123). FK direction strictly P216-P220 READ P215. Execution gating via assertUpstreamReady, not slot numbers.`"
    - "F-ID slot allocation P215 = F-271..F-286 (16 IDs total across 6 plans; cosmetic ordering — F-IDs are not constraint-bearing per P220 Q-3 path-A precedent; avoids collision with P220 F-209..F-227, P219 F-228..F-237, P218 F-238..F-246, P217 F-247..F-258, P216 F-259..F-270); Plan 01 owns F-271 (saas-invoice-v1) + F-272 (saas-payment-attempt-v1) + F-273 (saas-billing-event-v1) + F-274 (saas-credential-vault-ref-v1) + F-284 (saas-billing-api-v1)"
    - "assertUpstreamReady preflight CLI `scripts/preconditions/215-01-check-upstream.cjs` hard-fails when REQUIRED_UPSTREAM tables missing (P203 markos_webhook_subscriptions + markos_webhook_deliveries — webhook engine substrate; P205 pricing_recommendations — Pricing Engine substrate; P206 governance_evidence_packs — SOC2 evidence; P214 saas_suite_activations + saas_subscriptions — SaaS activation gate); soft-warns when SOFT_UPSTREAM tables missing (P207 markos_agent_runs, P208 agent_approval_packages, P209 evidence_map_records, P211, P212, P213); exit 1 on hard miss; matches `api/cron/webhooks-dlq-purge.js` createClient pattern verbatim"
    - "Architecture-lock test scans grep across `lib/markos/saas/billing/`, `lib/markos/saas/billing/preflight/`, `api/v1/saas/billing.js`, `api/v1/saas/invoices.js`, `lib/markos/mcp/tools/saas-billing.cjs` and asserts ZERO occurrences of forbidden patterns — createApprovalPackage, requireSupabaseAuth, requireTenantContext, serviceRoleClient, lookupPlugin, public/openapi.json, app/(saas), app/(growth), api/v1/.../route.ts, app/api/cron/.../route.ts, vitest, from 'vitest', .test.ts, openapi-generate, lib/markos/(saas/health|saas/revenue|profile|b2b|referral|community|events|pr|partnerships|sales|cdp|conversion|launches|analytics|channels|ecosystem)/ (downstream-phase paths reserved); ALSO P215-specific credential forbidden-text patterns — stripe_secret_key, stripe_webhook_secret, mercadopago_access_token, dian_pkcs12_password as plaintext literal in any lib/api code (these names may appear ONLY in lib/markos/saas/billing/credential-vault.ts CREDENTIAL_FIELDS array constant and in lib/markos/mcp/tools/saas-billing.cjs CREDENTIAL_FIELDS list)"
    - "Helper-presence test asserts `buildApprovalPackage` exists at `lib/markos/crm/agent-actions.ts:68`, `requireHostedSupabaseAuth` exists at `onboarding/backend/runtime-context.cjs:491`, `resolvePlugin` exists at `lib/markos/plugins/registry.js:102`, `lib/markos/mcp/tools/index.cjs` exists (CommonJS, NOT .ts), `contracts/openapi.json` exists (NOT public/openapi.json), `lib/markos/webhooks/engine.cjs` + `signing.cjs` + `dlq.cjs` + `replay.cjs` + `breaker.cjs` + `rate-limit.cjs` + `metrics.cjs` ALL exist (P203 substrate REUSE precondition); negative assertions — createApprovalPackage / requireSupabaseAuth / lookupPlugin / requireTenantContext / serviceRoleClient DO NOT exist anywhere in lib/markos/saas/billing/"
    - "Credential sanitization test asserts MCP tool `lib/markos/mcp/tools/saas-billing.cjs` invokes sanitizeBillingResponse on every tool descriptor handler return; output never contains any of the 12 credential field names — stripe_secret_key, stripe_webhook_secret, stripe_tax_api_key, mercadopago_access_token, mercadopago_webhook_secret, quickbooks_oauth_token, quickbooks_realm_id, siigo_api_key, alegra_api_key, dian_pkcs12_certificate, dian_pkcs12_password, dian_authorization_token; PLUS vault_ref values (vault://[uuid] strings never returned to MCP caller — replaced with [REDACTED])"
    - "saas_invoices table ships in migration 118 — RLS-enabled tenant_id-scoped per RESEARCH §Domain 1 §saas_invoices Table Schema; full column set: invoice_id PK uuid, tenant_id FK markos_orgs, subscription_id, customer_id, invoice_number, invoice_number_legal, invoice_type CHECK 5 values, status CHECK 6 values, pricing_recommendation_id (FK to pricing_recommendations OR sentinel `{{MARKOS_PRICING_ENGINE_PENDING}}` allowed), pricing_sentinel text, currency, subtotal, discount_amount, tax_lines jsonb, tax_total, total, amount_paid, amount_due, billing_country, tax_jurisdiction NOT NULL, sales_tax_calculated_at, stripe_tax_calculation_id (US fields), cufe, electronic_invoice_xml, electronic_invoice_pdf_url, dian_transmission_state CHECK 5 values, dian_provider_response_id, iva_rate, iva_amount, retefuente_rate, retefuente_amount, reteiva_rate, reteiva_amount, reteica_rate, reteica_amount (Colombian retention fields), processor CHECK 2 values, processor_invoice_id, processor_payment_intent_id, payment_method_type, quickbooks_invoice_id, siigo_invoice_id, alegra_invoice_id, accounting_synced_at, sent_to_email, sent_at, viewed_at, due_at, issued_at, correction_of_invoice_id, created_at, updated_at; 4 indexes — si_tenant_subscription_idx, si_tenant_status_idx, si_billing_country_idx, si_processor_invoice_id_idx"
    - "saas_payment_attempts table ships in migration 118 — RLS-enabled, columns per RESEARCH §Domain 1 §saas_payment_attempts: attempt_id PK uuid, tenant_id FK, invoice_id FK saas_invoices, subscription_id, processor CHECK 2 values, processor_payment_id, idempotency_key UNIQUE NOT NULL (prevents duplicate charges per SAS-05), status CHECK 5 values, failure_code, failure_message, amount, currency, dunning_attempt_number, next_retry_at, signature_verified_at (PROCESSOR_WEBHOOK_REQUIRES_SIGNATURE_AND_IDEMPOTENCY pre-write), attempted_at, created_at; 2 indexes — spa_tenant_invoice_idx, spa_tenant_failed_idx (partial WHERE status='failed' for P218 trigger)"
    - "saas_billing_events table ships in migration 118 — RLS-enabled, columns per RESEARCH §Domain 1 §saas_billing_events: event_id PK uuid, tenant_id FK, invoice_id FK (nullable), subscription_id, event_type, processor CHECK 5 values ('stripe','mercado_pago','quickbooks','siigo','dian'), processor_event_id, idempotency_key UNIQUE, payload_redacted jsonb NOT NULL DEFAULT '{}' (PII/credential-scrubbed payload — RAW payload NEVER persisted), occurred_at, created_at; 2 indexes — sbe_tenant_event_idx, sbe_idempotency_idx"
    - "saas_credential_vault_refs table ships in migration 118 — RLS-enabled, columns per RESEARCH §Domain 1 §saas_credential_vault_refs: ref_id PK uuid, tenant_id FK, credential_name text NOT NULL (one of CREDENTIAL_FIELDS ENUM), vault_ref text NOT NULL (must match `^vault://[a-zA-Z0-9_\\-/]+$` OR `^pgcrypto-ref:[A-Za-z0-9+/=]+$` per Q-9 resolution), config_table CHECK 3 values ('saas_processor_configs','saas_accounting_configs','saas_dian_config'), config_id, rotated_at, expires_at, created_at; UNIQUE(tenant_id, credential_name, config_id)"
    - "DB-trigger `INVOICE_REQUIRES_PRICING_AND_LEGAL_FIELDS` ships in migration 118 BEFORE INSERT OR UPDATE on saas_invoices — RAISE EXCEPTION (verbatim per RESEARCH §Domain 1 DB-Trigger Compliance): (1) pricing_recommendation_id IS NULL AND pricing_sentinel IS NULL → block; (2) tax_jurisdiction IS NULL OR empty → block; (3) status != 'draft' AND invoice_number_legal IS NULL → block; defense-in-depth pair with app-layer invoice.ts validation"
    - "DB-trigger `BILLING_CREDENTIAL_REQUIRES_VAULT_REF` ships in migration 118 BEFORE INSERT OR UPDATE on saas_credential_vault_refs — RAISE EXCEPTION (verbatim per RESEARCH §Sensitive Credential Handling Layer 2): (1) vault_ref NOT LIKE 'vault://%' AND NOT LIKE 'pgcrypto-ref:%' → block; (2) vault_ref = credential_name (bootstrap mistake guard) → block; this trigger is the cross-plan defense — Plans 02/03/04 ALL insert credentials through this single chokepoint"
    - "Pricing Engine sentinel pattern enforced per CLAUDE.md placeholder rule: when pricing_recommendation_id IS NULL, pricing_sentinel MUST equal `{{MARKOS_PRICING_ENGINE_PENDING}}` (literal); insert with pricing_sentinel='something_else' should be blocked at app-layer validation in lib/markos/saas/billing/invoice.ts (DB-trigger only checks NOT NULL, app-layer enforces literal value)"
    - "MCP tools registered in `lib/markos/mcp/tools/saas-billing.cjs` (CommonJS — NOT .ts) and added to `lib/markos/mcp/tools/index.cjs` registry: get_invoice (returns invoice with credential fields stripped), list_invoices_by_tenant (paginated by status), get_payment_attempts (filtered by status), get_billing_events (paginated by event_type); ALL responses pass through sanitizeBillingResponse() before return — strips 12 CREDENTIAL_FIELDS + vault_ref values"
    - "Log redaction module `lib/markos/saas/billing/log-redaction.ts` exports redactWebhookPayload(payload, processor) — for processor in {stripe, mercado_pago, quickbooks, siigo, dian} deep-clone payload and replace fields matching processor-specific PII list with `[REDACTED]`; called by Plan 04 webhook handlers BEFORE any console.log/audit emit; saas_billing_events.payload_redacted column stores ONLY the redacted version"
    - "Prompt-injection defense module `lib/markos/saas/billing/llm-shield.ts` exports stripCredentialsForLLM(context) — for any LLM call processing processor data, removes all CREDENTIAL_FIELDS + vault_ref + raw payload fields; only allows whitelisted business fields (amount, status, invoice_id, event_type, processor) to reach the LLM prompt builder; consumed by SAS-03 Billing Compliance Agent in P217 (Plan 01 ships the shield; P217 wires it)"
    - "5 contracts ship — F-271 (saas-invoice-v1) covering saas_invoices table + GET /v1/saas/invoices + GET /v1/saas/invoices/:invoice_id; F-272 (saas-payment-attempt-v1) covering saas_payment_attempts table + GET /v1/saas/billing/payment-attempts; F-273 (saas-billing-event-v1) covering saas_billing_events table; F-274 (saas-credential-vault-ref-v1) covering saas_credential_vault_refs table + BILLING_CREDENTIAL_REQUIRES_VAULT_REF trigger contract; F-284 (saas-billing-api-v1) documenting handler at api/v1/saas/billing.js + api/v1/saas/invoices.js + MCP tool registration in lib/markos/mcp/tools/saas-billing.cjs; NO collision with F-001..F-270 verified by `flow-registry.json` regen + parity test"
    - "Cross-tenant denial test asserts INSERT under tenant A then SELECT under tenant B returns 0 rows for saas_invoices + saas_payment_attempts + saas_billing_events + saas_credential_vault_refs (proves RLS not bypassable via service-role-leak); credential vault refs RLS is critical — leak would expose tenant credential mappings"
    - "PRC-01..09 + WHK-01 + BILL-02 documented in frontmatter as `integrates_with` (NOT `requirements`) — P205 OWNS PRC-01..09 + BILL-02 Pricing Engine substrate per REQUIREMENTS.md line 214; P203 OWNS WHK-01 webhook engine substrate; P215 invoices REFERENCE pricing_recommendation_id FK or use sentinel; P215 webhook handlers (Plan 04) REUSE P203 engine without modification"
    - "`215-VALIDATION.md` per-task verification map updated with rows for Plan 01 + frontmatter `nyquist_compliant: true` + `wave_0_complete: true` flipped after Plan 01 closeout (matches P216 Plan 01 pattern)"
    - "Plan 01 Task 1 intentionally bundles 35 steps + 28 files (foundation defense-in-depth across schema + contracts + lib surface + Vault wiring + 7-trigger compliance pack — atomic execution required so Plans 02-05 can FK reference foundational tables; per checker WARN 1 — file count exceeds 15-per-task threshold by design)"
  artifacts:
    - path: .planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md
      provides: CREATE-or-APPEND P215 reservation block (slots 118-123 + F-271..F-286) with Q-7 SLOT ORDERING CRISIS commentary; if doc missing creates with full v4.1.0 history (foundation 82-89/96/100 + P220 90-95+97 + P217 98-99 + P218 101-106 + P219 107-111 + P216 112-117 + P215 118-123); if exists APPENDS P215 rows
    - path: lib/markos/saas/billing/preflight/upstream-gate.ts
      provides: assertUpstreamReady runtime helper; throws UpstreamPhaseNotLandedError on hard-miss (P203/P205/P206/P214); logs warning on soft-miss (P207/P208/P209/P211/P212/P213); consumed by every Plan 02-06 first task
      exports: [assertUpstreamReady, REQUIRED_UPSTREAM, SOFT_UPSTREAM]
    - path: lib/markos/saas/billing/preflight/architecture-lock.ts
      provides: forbidden-pattern detector + helper-presence asserter; consumed by architecture-lock test
      exports: [scanForbiddenPatterns, assertHelpersPresent, FORBIDDEN_PATTERNS, REQUIRED_HELPERS, CREDENTIAL_FORBIDDEN_LITERALS]
    - path: lib/markos/saas/billing/preflight/errors.ts
      provides: UpstreamPhaseNotLandedError + ArchitectureLockViolationError + PricingEnginePendingError + CredentialNotVaultRefError + WebhookSignatureInvalidError typed errors
      exports: [UpstreamPhaseNotLandedError, ArchitectureLockViolationError, PricingEnginePendingError, CredentialNotVaultRefError, WebhookSignatureInvalidError]
    - path: lib/markos/saas/billing/preflight/index.cjs
      provides: CommonJS twin re-exporting upstream-gate + architecture-lock + errors surface for legacy api/*.js consumers
    - path: scripts/preconditions/215-01-check-upstream.cjs
      provides: CLI preflight checker invoked before plan execution; matches `api/cron/webhooks-dlq-purge.js` createClient + supabase pattern; exit 1 on hard miss; exit 0 with stderr warning on soft miss; HARD = [P203 markos_webhook_subscriptions+markos_webhook_deliveries, P205 pricing_recommendations, P206 governance_evidence_packs, P214 saas_suite_activations+saas_subscriptions]
    - path: supabase/migrations/118_saas_billing_invoice_contracts.sql
      provides: combined migration — saas_invoices + saas_payment_attempts + saas_billing_events + saas_credential_vault_refs SOR tables; INVOICE_REQUIRES_PRICING_AND_LEGAL_FIELDS DB-trigger; BILLING_CREDENTIAL_REQUIRES_VAULT_REF DB-trigger; RLS policies for all 4 tables; full index set per RESEARCH; CREATE EXTENSION IF NOT EXISTS pgcrypto (already loaded in migration 76 but defensive idempotent)
    - path: lib/markos/saas/billing/contracts.ts
      provides: zod schemas + enum constants — INVOICE_TYPES (5) + INVOICE_STATUSES (6) + PROCESSORS (2) + DIAN_TRANSMISSION_STATES (5) + PAYMENT_ATTEMPT_STATUSES (5) + CREDENTIAL_FIELDS (12 + vault_ref) + PRICING_SENTINEL = '{{MARKOS_PRICING_ENGINE_PENDING}}' + SaaSInvoiceSchema + SaaSPaymentAttemptSchema + SaaSBillingEventSchema + SaaSCredentialVaultRefSchema zod schemas
      exports: [INVOICE_TYPES, INVOICE_STATUSES, PROCESSORS, DIAN_TRANSMISSION_STATES, PAYMENT_ATTEMPT_STATUSES, CREDENTIAL_FIELDS, PRICING_SENTINEL, SaaSInvoiceSchema, SaaSPaymentAttemptSchema, SaaSBillingEventSchema, SaaSCredentialVaultRefSchema]
    - path: lib/markos/saas/billing/invoice.ts
      provides: createInvoice({tenantId, subscriptionId, customerId, pricingRecommendationId, currency, subtotal, taxLines, billingCountry, taxJurisdiction, processor, ...}, supabase) → validates pricing FK or sentinel literal '{{MARKOS_PRICING_ENGINE_PENDING}}' + tax_jurisdiction non-empty + status='draft'-allowed-without-legal-number; INSERT through DB-trigger pair; getInvoice / updateInvoiceStatus helpers
      exports: [createInvoice, getInvoice, updateInvoiceStatus, validateInvoiceInput]
    - path: lib/markos/saas/billing/payment-attempt.ts
      provides: createPaymentAttempt({tenantId, invoiceId, processor, idempotencyKey, amount, currency, ...}, supabase) → enforces idempotency_key UNIQUE; recordSignatureVerification(attemptId, verifiedAt) helper; getFailedAttempts(tenantId, since) for P218 trigger
      exports: [createPaymentAttempt, recordSignatureVerification, getFailedAttempts]
    - path: lib/markos/saas/billing/billing-event.ts
      provides: recordBillingEvent({tenantId, invoiceId, processor, eventType, processorEventId, idempotencyKey, payload}, supabase) → calls redactWebhookPayload before persist; INSERT into saas_billing_events; idempotent via idempotency_key UNIQUE
      exports: [recordBillingEvent, getBillingEvents]
    - path: lib/markos/saas/billing/credential-vault.ts
      provides: storeCredentialVaultRef({tenantId, credentialName, plaintextValue, configTable, configId}, supabase) → calls Supabase Vault vault.create_secret() (preferred) OR pgcrypto pgp_sym_encrypt fallback; inserts vault-ref pattern into saas_credential_vault_refs (DB-trigger validates pattern); resolveCredential({tenantId, credentialName, configId}, supabase) → Vault decryption via vault.decrypted_secrets view OR pgp_sym_decrypt; CREDENTIAL_FIELDS array exported
      exports: [storeCredentialVaultRef, resolveCredential, rotateCredential, CREDENTIAL_FIELDS]
    - path: lib/markos/saas/billing/log-redaction.ts
      provides: redactWebhookPayload(payload, processor) deep-clones payload and replaces fields matching processor-specific PII list with '[REDACTED]'; STRIPE_PII_FIELDS / MP_PII_FIELDS / QB_PII_FIELDS / SIIGO_PII_FIELDS / DIAN_PII_FIELDS arrays; called by Plan 04 webhook handlers
      exports: [redactWebhookPayload, STRIPE_PII_FIELDS, MP_PII_FIELDS, QB_PII_FIELDS, SIIGO_PII_FIELDS, DIAN_PII_FIELDS]
    - path: lib/markos/saas/billing/llm-shield.ts
      provides: stripCredentialsForLLM(context) removes CREDENTIAL_FIELDS + vault_ref + raw payload; only whitelisted business fields (amount, status, invoice_id, event_type, processor) reach LLM prompt; LLM_SAFE_FIELDS allowlist exported
      exports: [stripCredentialsForLLM, LLM_SAFE_FIELDS]
    - path: lib/markos/saas/billing/index.cjs
      provides: CommonJS twin re-exporting full surface (invoice + payment-attempt + billing-event + credential-vault + log-redaction + llm-shield + contracts)
    - path: lib/markos/mcp/tools/saas-billing.cjs
      provides: 4 MCP tool descriptors — get_invoice + list_invoices_by_tenant + get_payment_attempts + get_billing_events; ALL responses pass through sanitizeBillingResponse(obj) which recursively strips 12 CREDENTIAL_FIELDS + vault_ref values; registered into lib/markos/mcp/tools/index.cjs
    - path: api/v1/saas/billing.js
      provides: GET /v1/saas/billing (overview — invoice counts by status, payment attempts last 30d, dunning state) — paginated, tenant-scoped, requires requireHostedSupabaseAuth
    - path: api/v1/saas/invoices.js
      provides: GET /v1/saas/invoices (paginated, filterable by status/billing_country) + GET /v1/saas/invoices/:invoice_id (full payload with credential fields stripped) — legacy api/*.js handler — uses requireHostedSupabaseAuth + tenant-scoped supabase client; 401 on auth fail
    - path: contracts/F-271-saas-invoice-v1.yaml
      provides: documents saas_invoices schema + GET /v1/saas/invoices + GET /v1/saas/invoices/:invoice_id; request/response schemas mirror lib/markos/saas/billing/contracts.ts SaaSInvoiceSchema; documents pricing_recommendation_id FK / sentinel pattern + INVOICE_REQUIRES_PRICING_AND_LEGAL_FIELDS exception
    - path: contracts/F-272-saas-payment-attempt-v1.yaml
      provides: documents saas_payment_attempts schema + GET /v1/saas/billing/payment-attempts; documents idempotency_key UNIQUE constraint + signature_verified_at semantics
    - path: contracts/F-273-saas-billing-event-v1.yaml
      provides: documents saas_billing_events schema + payload_redacted invariant (raw payload NEVER persisted); 5 processor enum values
    - path: contracts/F-274-saas-credential-vault-ref-v1.yaml
      provides: documents saas_credential_vault_refs schema + BILLING_CREDENTIAL_REQUIRES_VAULT_REF exception + 12 CREDENTIAL_FIELDS ENUM + vault:// and pgcrypto-ref: pattern regex
    - path: contracts/F-284-saas-billing-api-v1.yaml
      provides: documents api/v1/saas/billing.js + api/v1/saas/invoices.js handlers + MCP tools registration in lib/markos/mcp/tools/saas-billing.cjs (4 tools); credential sanitization invariant (sanitizeBillingResponse called on every return)
    - path: contracts/flow-registry.json
      provides: 5 entries appended for F-271 + F-272 + F-273 + F-274 + F-284; NO overlap against F-001..F-270 (P220 F-209..F-227, P219 F-228..F-237, P218 F-238..F-246, P217 F-247..F-258, P216 F-259..F-270 already locked)
    - path: test/fixtures/saas-215/index.js
      provides: barrel export of P215 fixture factories — invoiceFactory + paymentAttemptFactory + billingEventFactory + credentialVaultRefFactory (Plans 02-06 add processorConfigFactory, accountingConfigFactory, dianConfigFactory, billingCorrectionFactory, dunningScheduleFactory, payoutPolicyFactory)
    - path: test/saas-215/preflight/wave-0-baseline.test.js
      provides: 3 assertions — (a) NO supabase/migrations/ files matching `^118_saas_billing` exist before this plan ships; (b) NO contracts files matching `^F-(27[1-4]|284)-` exist; (c) V4.1.0-MIGRATION-SLOT-COORDINATION.md after CREATE-or-APPEND contains 'P215' + 'F-271' + 'F-286' + '118' + '123' + 'Q-7'
    - path: test/saas-215/preflight/architecture-lock.test.js
      provides: scans 215-*-PLAN.md bodies + lib/markos/saas/billing/* + api/v1/saas/* + lib/markos/mcp/tools/saas-billing.cjs for forbidden patterns; asserts grep count == 0 across FORBIDDEN_PATTERNS + CREDENTIAL_FORBIDDEN_LITERALS (with allowlist exception for credential-vault.ts CREDENTIAL_FIELDS array + saas-billing.cjs CREDENTIAL_FIELDS list)
    - path: test/saas-215/preflight/credential-sanitization.test.js
      provides: invokes each MCP tool descriptor with a fixture row containing all 12 credential fields populated; asserts return value has every credential field replaced with '[REDACTED]' AND vault_ref also '[REDACTED]'; per HIGH 2 RESEARCH §Sensitive Credential Handling Layer 3
    - path: test/saas-215/domain-1/invoice-pricing-trigger.test.js
      provides: Postgres-backed test using service-role direct INSERT into saas_invoices — pricing_recommendation_id IS NULL AND pricing_sentinel IS NULL → expect EXCEPTION 'INVOICE_REQUIRES_PRICING_AND_LEGAL_FIELDS'; pricing_sentinel='{{MARKOS_PRICING_ENGINE_PENDING}}' → succeeds; valid FK → succeeds
    - path: test/saas-215/domain-1/credential-vault-trigger.test.js
      provides: Postgres-backed test — INSERT into saas_credential_vault_refs with vault_ref='plaintext_secret_value' → expect EXCEPTION 'BILLING_CREDENTIAL_REQUIRES_VAULT_REF'; vault_ref='vault://abc-123' → succeeds; vault_ref='pgcrypto-ref:dGVzdA==' → succeeds; vault_ref equals credential_name → exception (bootstrap mistake guard)
  key_links:
    - from: lib/markos/saas/billing/invoice.ts
      to: lib/markos/saas/billing/contracts.ts
      via: imports SaaSInvoiceSchema + INVOICE_TYPES + INVOICE_STATUSES + PRICING_SENTINEL
      pattern: "PRICING_SENTINEL"
    - from: lib/markos/saas/billing/invoice.ts
      to: supabase/migrations/118_saas_billing_invoice_contracts.sql
      via: app-layer createInvoice INSERT; DB-trigger trg_invoice_pricing_and_legal asserts pricing_recommendation_id OR pricing_sentinel + tax_jurisdiction + invoice_number_legal-when-not-draft (defense-in-depth pair)
      pattern: "INVOICE_REQUIRES_PRICING_AND_LEGAL_FIELDS"
    - from: lib/markos/saas/billing/credential-vault.ts
      to: supabase/migrations/118_saas_billing_invoice_contracts.sql
      via: app-layer storeCredentialVaultRef calls Supabase Vault or pgcrypto + INSERT into saas_credential_vault_refs; DB-trigger trg_credential_vault_ref asserts vault:// or pgcrypto-ref: prefix
      pattern: "BILLING_CREDENTIAL_REQUIRES_VAULT_REF"
    - from: lib/markos/mcp/tools/saas-billing.cjs
      to: lib/markos/saas/billing/credential-vault.ts
      via: imports CREDENTIAL_FIELDS array; sanitizeBillingResponse strips these fields from every tool descriptor return
      pattern: "sanitizeBillingResponse"
    - from: scripts/preconditions/215-01-check-upstream.cjs
      to: supabase tables markos_webhook_subscriptions + markos_webhook_deliveries + pricing_recommendations + governance_evidence_packs + saas_suite_activations + saas_subscriptions
      via: createClient(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY).from(table).select('*').limit(0); on /relation .* does not exist/ → console.error + process.exit(1)
      pattern: "REQUIRED_UPSTREAM"
    - from: .planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md
      to: supabase/migrations/118_saas_billing_invoice_contracts.sql
      via: doc reserves slot 118 for P215 Plan 01; commit message references doc by file path; documents Q-7 SLOT ORDERING CRISIS resolution
      pattern: "MIGRATION-SLOT-COORDINATION"
    - from: api/v1/saas/invoices.js
      to: lib/markos/saas/billing/invoice.ts (via index.cjs)
      via: legacy api/*.js handler imports CommonJS twin; calls getInvoice + listInvoicesByTenant; uses requireHostedSupabaseAuth for tenant-scoped supabase client
      pattern: "getInvoice"
---

<objective>
Establish the Wave 1 schema foundation for Phase 215: ship the architecture-lock + assertUpstreamReady preflight surface (Plan 01 owns this for ALL 6 plans), CREATE-or-APPEND the migration slot coordination document with P215 reservation rows + Q-7 SLOT ORDERING CRISIS resolution commentary (P215 is FIRST V4.1.0 phase to execute), the saas_invoices + saas_payment_attempts + saas_billing_events + saas_credential_vault_refs SOR tables in migration 118, the INVOICE_REQUIRES_PRICING_AND_LEGAL_FIELDS DB-trigger (verbatim exception name per RESEARCH §Domain 1) + BILLING_CREDENTIAL_REQUIRES_VAULT_REF cross-plan DB-trigger (per RESEARCH §Sensitive Credential Handling Layer 2 — this is the HIGH 2 chokepoint that protects Plans 02/03/04 from plaintext credential storage), 5 F-ID contracts (F-271 invoice + F-272 payment-attempt + F-273 billing-event + F-274 credential-vault-ref + F-284 billing-api), 4 MCP tools (get_invoice + list_invoices_by_tenant + get_payment_attempts + get_billing_events) wrapped by sanitizeBillingResponse credential redactor, the 5-layer defense-in-depth credential handling stack — Layer 1 storage (storeCredentialVaultRef Vault/pgcrypto fallback), Layer 2 DB-trigger (BILLING_CREDENTIAL_REQUIRES_VAULT_REF), Layer 3 MCP sanitization (saas-billing.cjs sanitizeBillingResponse), Layer 4 log redaction (log-redaction.ts redactWebhookPayload), Layer 5 prompt-injection defense (llm-shield.ts stripCredentialsForLLM) — the cross-tenant RLS denial suite, and the populated 215-VALIDATION.md per-task map.

Purpose: satisfy SAS-04 (SaaS plan prices, packages, discounts, value metrics, save offers consume Pricing Engine recommendations or sentinel — Plan 01 ships saas_invoices.pricing_recommendation_id FK + sentinel pattern + DB-trigger gate) + SAS-05 (SaaS Billing Engine substrate — invoices + payment attempts + billing events + idempotency keys; corrections + dunning ship in Plan 05) + BILL-01 (Billing usage/ledger/invoice/entitlement reconciliation — Plan 01 ships the invoice + payment-attempt layers on top of existing migration 54 billing_usage_ledger_rows substrate) + QA-01..07 (architecture-lock + helper-presence + assertUpstreamReady + RLS isolation + F-ID parity + threat model + credential sanitization). Plan 01 unblocks every later 215-NN slice — the architecture-lock surface (lib/markos/saas/billing/preflight/) is consumed by Plans 02-06 first tasks; the credential-vault.ts + log-redaction.ts + llm-shield.ts surface is consumed by Plans 02/03/04 webhook handlers; the fixture barrel (test/fixtures/saas-215/) is consumed across all later test files; the migration slot doc (CREATEd-or-APPENDed) constrains all subsequent phases; the saas_invoices + saas_payment_attempts tables are the contracts that P216/P217/P218/P219 HARD-depend on at runtime via assertUpstreamReady; the saas_billing_events table is the substrate Plan 04 webhook handlers write into.

PRC-01..09 + WHK-01 + BILL-02 are documented as `integrates_with` (NOT `requirements`) — P205 owns Pricing Engine substrate; P203 owns webhook engine; P215 invoices REFERENCE pricing_recommendation_id FK or use sentinel `{{MARKOS_PRICING_ENGINE_PENDING}}` per Pricing Engine Canon; P215 webhook handlers (Plan 04) REUSE P203 engine without modification. This resolves the ownership conflict flagged in REVIEWS.md MEDIUM RM-7.

HIGH 2 (sensitive credential handling) is the architectural anchor of this plan — Plan 01 ships ALL 5 defense-in-depth layers because Plans 02/03/04 ALL consume the same chokepoints. The DB-trigger BILLING_CREDENTIAL_REQUIRES_VAULT_REF on saas_credential_vault_refs is the single non-bypassable enforcement point that blocks plaintext credentials at the database level regardless of which plan attempted the insert.

Output: 1 coordination doc CREATE-or-APPEND + 4 preflight files + 1 preflight CLI + 5 preflight test files + 5 fixture files + 1 SOR migration (118 — combined invoices + payment-attempts + billing-events + credential-vault-refs + 2 DB-triggers) + 9 billing lib files (contracts.ts/cjs + invoice + payment-attempt + billing-event + credential-vault + log-redaction + llm-shield + index.cjs) + 1 MCP tool file + 2 API handlers + 5 contract YAMLs + flow-registry update + 8 domain-1 test files + 1 RLS test file + populated VALIDATION.md.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/215-saas-suite-billing-payments-compliance/215-CONTEXT.md
@.planning/phases/215-saas-suite-billing-payments-compliance/215-RESEARCH.md
@.planning/phases/215-saas-suite-billing-payments-compliance/215-REVIEWS.md
@.planning/phases/215-saas-suite-billing-payments-compliance/215-VALIDATION.md
@.planning/phases/215-saas-suite-billing-payments-compliance/DISCUSS.md
@.planning/phases/216-saas-suite-health-churn-support-usage/216-01-PLAN.md
@.planning/phases/217-saas-suite-revenue-agents-api-ui/217-01-PLAN.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@obsidian/brain/SaaS Suite Canon.md
@obsidian/brain/Pricing Engine Canon.md
@obsidian/reference/MarkOS v2 Operating Loop Spec.md
@CLAUDE.md
@lib/markos/crm/agent-actions.ts
@onboarding/backend/runtime-context.cjs
@lib/markos/plugins/registry.js
@lib/markos/mcp/tools/index.cjs
@lib/markos/webhooks/engine.cjs
@lib/markos/webhooks/signing.cjs
@lib/markos/webhooks/dlq.cjs
@api/cron/webhooks-dlq-purge.js
@supabase/migrations/76_markos_cli_tenant_env.sql

<interfaces>
<!-- Verified existing symbols from 2026-04-26 codebase enumeration. Plan 01 wires against these directly. -->

From lib/markos/crm/agent-actions.ts:68:
```typescript
function buildApprovalPackage(input: BuildApprovalPackageInput): ApprovalPackage
// returns { approval_id: string, ... }
// module.exports = { buildApprovalPackage, ... } at line 133
```

From onboarding/backend/runtime-context.cjs:491:
```javascript
function requireHostedSupabaseAuth({ req, runtimeContext, operation, requiredProjectSlug, env })
// returns { tenant_id, user_id, project_slug, ... }
// module.exports = { requireHostedSupabaseAuth, ... } at line 1014
```

From lib/markos/plugins/registry.js:102:
```javascript
function resolvePlugin(registry, pluginId): object | null
```

From lib/markos/webhooks/engine.cjs (REUSE substrate — Plan 04 wraps these):
```javascript
// signing.cjs: verifySignature(secret, rawBody, signatureHeader, timestampHeader): boolean
// dlq.cjs: enqueueToDLQ(event, reason): Promise<void>
// replay.cjs: checkIdempotency(idempotencyKey): Promise<boolean>
// breaker.cjs: withCircuitBreaker(handler, opts): wrappedHandler
// rate-limit.cjs: applyRateLimit(req, opts): Promise<{allowed, retryAfter}>
```

From supabase/migrations/76_markos_cli_tenant_env.sql:
```sql
CREATE EXTENSION IF NOT EXISTS pgcrypto;
-- pgp_sym_encrypt(secret, key) and pgp_sym_decrypt(ciphertext, key) available for fallback
```

P215 CREDENTIAL_FIELDS ENUM (12 fields + vault_ref redaction sentinel — RESEARCH §Sensitive Credential Handling):
| # | Field | Storage | Sanitized in MCP? |
|---|-------|---------|-------------------|
| 1 | stripe_secret_key | saas_credential_vault_refs | YES |
| 2 | stripe_webhook_secret | saas_credential_vault_refs | YES |
| 3 | stripe_tax_api_key | saas_credential_vault_refs | YES |
| 4 | mercadopago_access_token | saas_credential_vault_refs | YES |
| 5 | mercadopago_webhook_secret | saas_credential_vault_refs | YES |
| 6 | quickbooks_oauth_token | saas_credential_vault_refs | YES |
| 7 | quickbooks_realm_id | saas_credential_vault_refs | YES |
| 8 | siigo_api_key | saas_credential_vault_refs | YES |
| 9 | alegra_api_key (v4.2.0 reserved) | saas_credential_vault_refs | YES |
| 10 | dian_pkcs12_certificate | saas_credential_vault_refs | YES |
| 11 | dian_pkcs12_password | saas_credential_vault_refs | YES |
| 12 | dian_authorization_token | saas_credential_vault_refs | YES |
| 13 | vault_ref (the ref itself) | n/a | YES |

Vault ref pattern (DB-trigger BILLING_CREDENTIAL_REQUIRES_VAULT_REF enforces):
- `^vault://[a-zA-Z0-9_\-/]+$` (Supabase Vault — preferred)
- `^pgcrypto-ref:[A-Za-z0-9+/=]+$` (pgcrypto fallback for fields not yet Vault-supported)

Pricing sentinel (CLAUDE.md placeholder rule):
- `{{MARKOS_PRICING_ENGINE_PENDING}}` (literal — app-layer enforces literal value; DB-trigger only checks NOT NULL)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0.1: Wave 0 baseline guards + CREATE V4.1.0-MIGRATION-SLOT-COORDINATION.md (P215 is FIRST V4.1.0 phase to execute; CREATE-or-APPEND defensive pattern)</name>
  <files>.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md, test/saas-215/preflight/wave-0-baseline.test.js, test/fixtures/saas-215/index.js, test/fixtures/saas-215/invoice.js, test/fixtures/saas-215/payment-attempt.js, test/fixtures/saas-215/billing-event.js, test/fixtures/saas-215/credential-vault-ref.js</files>
  <read_first>
    - .planning/phases/215-saas-suite-billing-payments-compliance/215-RESEARCH.md §F-ID and Migration Slot Allocation §Slot Inventory §Q-7 SLOT ORDERING CRISIS — Resolution
    - .planning/phases/215-saas-suite-billing-payments-compliance/215-RESEARCH.md §Cross-Phase Coordination §Q-1 through §Q-5 (P215 → P216/P217/P218/P219/P220 column contract locks)
    - .planning/phases/216-saas-suite-health-churn-support-usage/216-01-PLAN.md Task 0.1 (mirror CREATE-or-APPEND pattern; in P216 scenario doc was created by P217; in P215 scenario P215 creates first)
    - .planning/phases/217-saas-suite-revenue-agents-api-ui/217-01-PLAN.md Task 0.1 (CREATE-or-APPEND pattern reference)
    - supabase/migrations/ (verify highest existing slot is 100; slots 118-123 are unallocated — `ls supabase/migrations/`)
    - contracts/ (verify F-271..F-286 range unallocated on disk — `ls contracts/`)
  </read_first>
  <action>
    Step 1: CREATE-or-APPEND `.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md`. Use Node.js fs.existsSync check: if file exists (P216 or P217 created it first in some replay scenario), APPEND P215 reservation block; if missing (this is the normal path — P215 is FIRST V4.1.0 phase to execute), CREATE doc with full v4.1.0 context.

    CREATE PATH (file missing — normal):
      - Top-of-file context block:
        > # V4.1.0 Migration Slot Coordination
        >
        > **Created:** 2026-04-26 by P215 Plan 01 (FIRST V4.1.0 phase to execute).
        > **Purpose:** Reserve migration slot ranges + F-ID ranges for the v4.1.0 SaaS Suite milestone (P214–P220) so concurrent phase plans do not collide.
        >
        > ## Q-7 SLOT ORDERING CRISIS — Resolved 2026-04-26
        >
        > P215 execution-order is BEFORE P216/P217/P218/P219/P220 (foundational — all five downstream phases HARD-depend on saas_invoices + saas_payment_attempts), but slot-order is LATEST in V4.1.0 (118-123) because slots 82-117 are occupied or reserved.
        >
        > **How this works without breaking P216–P220:** slot ordering is COSMETIC — slot apply order does NOT match execution dependency order. P216–P220 migration SQL does NOT include FK constraints referencing P215 tables; they JOIN P215 tables at runtime. The `assertUpstreamReady` preflight in P216–P220 Plan 01 scripts checks that P215 tables exist before each phase executes. If P215 has not run, downstream phases halt at preflight.
        >
        > **FK Direction Lock (NEVER VIOLATE):**
        > - P215 saas_invoices READ by P216 (billing health dimension), P217 (MRR snapshot), P218 (upgrade context), P219 (expansion-signal-scanner)
        > - P215 saas_payment_attempts READ by P216 (failed-payment health signal), P218 (failed-payment InAppCampaign trigger)
        > - P215 saas_dunning_schedules READ by P216 (dunning state health dimension)
        > - P215 saas_future_payout_policies READ by P218 (incentive experiment), P220 (referral/affiliate/partner payouts)
        > - **P215 has ZERO FKs INTO any P216/P217/P218/P219/P220 table**

      - Section "## Reservations":
        | Phase | Plan | Migration slot(s) | F-ID range | Status | Notes |
        |-------|------|-------------------|------------|--------|-------|
        | Foundation | n/a | 82-89 | n/a | ON DISK | audit_log_hash_chain through mcp_cost_window |
        | Foundation | n/a | 96 | n/a | ON DISK | neuro_literacy_metadata |
        | Foundation | n/a | 100 | n/a | RESERVED | crm_schema_identity_graph_hardening (planned; not yet on disk in some branches) |
        | P220 | 01-06 | 90-95+97 (skip 96) | F-209..F-227 | RESERVED | 28 SOR tables across 6 SaaS growth domains |
        | P217 | 01+02+06 | 98 | F-247..F-258 | RESERVED | revenue intel + nav + 4 DB-triggers |
        | P217 | 03 | 99 | F-250..F-251 | RESERVED | sas_agent_readiness |
        | P218 | 01-06 | 101-106 | F-238..F-246 | RESERVED | growth profile + activation/PQL/upgrade/inapp/experiment |
        | P219 | 01-05 | 107-111 | F-228..F-237 | RESERVED | revenue team + expansion + ABM + advocacy + pricing |
        | P216 | 01-06 | 112-117 | F-259..F-270 | RESERVED | health + usage + support + churn + privacy + handoff |
        | P215 | 01 | 118 | F-271, F-272, F-273, F-274, F-284 | RESERVED (this commit) | saas_invoices + saas_payment_attempts + saas_billing_events + saas_credential_vault_refs + INVOICE_REQUIRES_PRICING_AND_LEGAL_FIELDS + BILLING_CREDENTIAL_REQUIRES_VAULT_REF |
        | P215 | 02 | 119 | F-275, F-276 | RESERVED | US path — saas_processor_configs (Stripe row) + saas_accounting_configs (QuickBooks row) + STRIPE_INVOICE_REQUIRES_TAX_CALCULATION |
        | P215 | 03 | 120 | F-277, F-278, F-279 | RESERVED | Colombia path — Mercado Pago config + Siigo config + saas_dian_config + DIAN_INVOICE_REQUIRES_CUFE_AND_PROVIDER_RESPONSE + DIAN_REJECTION_CREATES_P1_TASK |
        | P215 | 04 | 121 (contract-only) | F-280, F-285 | RESERVED | processor webhook routing — REUSES P203 engine; PROCESSOR_WEBHOOK_REQUIRES_SIGNATURE_AND_IDEMPOTENCY |
        | P215 | 05 | 122 | F-281, F-282, F-283 | RESERVED | saas_billing_corrections + saas_dunning_schedules + BILLING_CORRECTION_REQUIRES_APPROVAL |
        | P215 | 06 | 123 | F-286 | RESERVED | saas_future_payout_policies (planned-only seed) + closeout regression |

    APPEND PATH (file exists — P216/P217 created it first in replay scenario): locate the "## Reservations" section, insert P215 rows AFTER any P216 rows (preserving alphanumeric phase ordering); ensure Q-7 commentary block exists or append it.

    Step 2: Create test/saas-215/preflight/wave-0-baseline.test.js using node:test + node:assert/strict:
      - test('wave-0: no migration 118 with saas_billing prefix exists before this plan ships', () => assert.deepStrictEqual(fs.readdirSync('supabase/migrations').filter((f) => /^118_saas_billing/.test(f)), []))
      - test('wave-0: no contracts F-271..F-274 + F-284 exist before this plan ships', () => assert.deepStrictEqual(fs.readdirSync('contracts').filter((f) => /^F-(27[1-4]|284)-/.test(f)), []))
      - test('wave-0: V4.1.0-MIGRATION-SLOT-COORDINATION.md after CREATE-or-APPEND contains P215 reservation', () => { const doc = fs.readFileSync('.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md', 'utf8'); assert.ok(doc.includes('P215') && doc.includes('F-271') && doc.includes('F-286') && doc.includes('118') && doc.includes('123') && doc.includes('Q-7') && doc.includes('SLOT ORDERING CRISIS')); })

    Step 3: Create test/fixtures/saas-215/index.js barrel exporting invoiceFactory + paymentAttemptFactory + billingEventFactory + credentialVaultRefFactory; create the four fixture files with factory functions accepting partial input + returning a complete row shape with sensible defaults (UUID-generated PKs, tenant_id from arg, ISO 4217 'USD', issued_at = now()).
  </action>
  <verify>
    <automated>npm test -- test/saas-215/preflight/wave-0-baseline.test.js</automated>
  </verify>
  <done>V4.1.0-MIGRATION-SLOT-COORDINATION.md contains P215 rows for slots 118-123 + F-271..F-286 + Q-7 resolution block; wave-0-baseline.test.js passes (3 assertions); fixture barrel exports 4 factories.</done>
</task>

<task type="auto">
  <name>Task 0.5: Architecture-lock surface + assertUpstreamReady preflight + helper-presence asserter + credential-sanitization scaffold (Wave 0.5 — consumed by Plans 02-06 first tasks; HIGH 2 defense layer 3 chokepoint)</name>
  <files>lib/markos/saas/billing/preflight/upstream-gate.ts, lib/markos/saas/billing/preflight/architecture-lock.ts, lib/markos/saas/billing/preflight/errors.ts, lib/markos/saas/billing/preflight/index.cjs, scripts/preconditions/215-01-check-upstream.cjs, lib/markos/mcp/tools/saas-billing.cjs, test/saas-215/preflight/architecture-lock.test.js, test/saas-215/preflight/upstream-gate.test.js, test/saas-215/preflight/helper-presence.test.js, test/saas-215/preflight/credential-sanitization.test.js</files>
  <read_first>
    - .planning/phases/215-saas-suite-billing-payments-compliance/215-RESEARCH.md §Architecture Lock §Pin Table §Helper File Presence Verification Table §Forbidden Patterns
    - .planning/phases/215-saas-suite-billing-payments-compliance/215-RESEARCH.md §Upstream Dependencies §Hard Upstreams §Soft Upstreams §Preflight Script Pattern (line 259+)
    - .planning/phases/215-saas-suite-billing-payments-compliance/215-RESEARCH.md §Sensitive Credential Handling §Layer 3 MCP Tool Sanitization (saas-billing.cjs sanitizeBillingResponse pattern, lines 1226-1252)
    - .planning/phases/216-saas-suite-health-churn-support-usage/216-01-PLAN.md Task 0.5 (mirror surface; rename saas/health → saas/billing, saas/preflight → saas/billing/preflight)
    - api/cron/webhooks-dlq-purge.js (createClient pattern for preflight script)
    - lib/markos/crm/agent-actions.ts:68 (buildApprovalPackage signature for helper-presence positive assertion)
    - onboarding/backend/runtime-context.cjs:491 (requireHostedSupabaseAuth signature)
    - lib/markos/plugins/registry.js:102 (resolvePlugin signature)
    - lib/markos/webhooks/engine.cjs + signing.cjs + dlq.cjs + replay.cjs + breaker.cjs + rate-limit.cjs + metrics.cjs (P203 substrate REUSE precondition for helper-presence)
  </read_first>
  <action>
    Step 1: Create lib/markos/saas/billing/preflight/errors.ts exporting UpstreamPhaseNotLandedError + ArchitectureLockViolationError + PricingEnginePendingError + CredentialNotVaultRefError + WebhookSignatureInvalidError typed error classes (extend Error; carry `code`, `phase`, `missingTable` / `forbiddenPattern` / `tenantId` / `credentialName` / `vaultRef` properties).

    Step 2: Create lib/markos/saas/billing/preflight/upstream-gate.ts:
      - Export REQUIRED_UPSTREAM = [{phase:'P203', table:'markos_webhook_subscriptions', why:'Webhook engine substrate'}, {phase:'P203', table:'markos_webhook_deliveries', why:'Webhook delivery store'}, {phase:'P205', table:'pricing_recommendations', why:'Pricing Engine substrate — saas_invoices.pricing_recommendation_id FK target'}, {phase:'P206', table:'governance_evidence_packs', why:'SOC2 evidence substrate — Plan 05 correction evidence packs'}, {phase:'P214', table:'saas_suite_activations', why:'SaaS Suite activation gate'}, {phase:'P214', table:'saas_subscriptions', why:'subscription FK target — saas_invoices.subscription_id'}]
      - Export SOFT_UPSTREAM = [{phase:'P207', table:'markos_agent_runs'}, {phase:'P208', table:'agent_approval_packages'}, {phase:'P209', table:'evidence_map_records'}, {phase:'P211', table:'loop_runs'}, {phase:'P212', table:'lrn_artifact_performance_log'}, {phase:'P213', table:'tenant_zero_state'}]
      - Export async function assertUpstreamReady({ supabase, throwOnSoft = false }) — for each REQUIRED_UPSTREAM table call supabase.from(table).select('*').limit(0); on /relation .* does not exist/ throw UpstreamPhaseNotLandedError; for each SOFT_UPSTREAM emit warning to stderr.

    Step 3: Create lib/markos/saas/billing/preflight/architecture-lock.ts:
      - Export FORBIDDEN_PATTERNS = ['createApprovalPackage', 'requireSupabaseAuth', 'lookupPlugin', 'requireTenantContext', 'serviceRoleClient', 'lib/markos/saas/health/', 'lib/markos/saas/revenue/', 'lib/markos/profile/', 'lib/markos/b2b/', 'lib/markos/referral/', 'lib/markos/community/', 'lib/markos/events/', 'lib/markos/pr/', 'lib/markos/partnerships/', 'lib/markos/sales/', 'lib/markos/cdp/', 'lib/markos/conversion/', 'lib/markos/launches/', 'lib/markos/analytics/', 'lib/markos/channels/', 'lib/markos/ecosystem/', 'public/openapi.json', 'app/(saas)/', 'app/(growth)/', 'route.ts', 'vitest', 'playwright', 'openapi-generate', '.test.ts']
      - Export CREDENTIAL_FORBIDDEN_LITERALS = ['stripe_secret_key', 'stripe_webhook_secret', 'mercadopago_access_token', 'dian_pkcs12_password'] — these names appearing as plaintext STRING LITERALS (not as identifiers in CREDENTIAL_FIELDS array constant) anywhere in lib/api code is forbidden. The architecture-lock test uses an allowlist exception for `lib/markos/saas/billing/credential-vault.ts` (CREDENTIAL_FIELDS array constant) and `lib/markos/mcp/tools/saas-billing.cjs` (CREDENTIAL_FIELDS list). All other appearances trigger violation.
      - Export REQUIRED_HELPERS = [{path:'lib/markos/crm/agent-actions.ts', symbol:'buildApprovalPackage', line:68}, {path:'onboarding/backend/runtime-context.cjs', symbol:'requireHostedSupabaseAuth', line:491}, {path:'lib/markos/plugins/registry.js', symbol:'resolvePlugin', line:102}, {path:'lib/markos/mcp/tools/index.cjs', symbol:null}, {path:'contracts/openapi.json', symbol:null}, {path:'lib/markos/webhooks/engine.cjs', symbol:null}, {path:'lib/markos/webhooks/signing.cjs', symbol:null}, {path:'lib/markos/webhooks/dlq.cjs', symbol:null}, {path:'lib/markos/webhooks/replay.cjs', symbol:null}, {path:'lib/markos/webhooks/breaker.cjs', symbol:null}, {path:'lib/markos/webhooks/rate-limit.cjs', symbol:null}, {path:'lib/markos/webhooks/metrics.cjs', symbol:null}]
      - Export async function scanForbiddenPatterns({ paths }) — uses fs.readFileSync recursively; returns array of {file, line, pattern} matches; throw ArchitectureLockViolationError if any matches found
      - Export async function assertHelpersPresent() — for each REQUIRED_HELPERS check existence + symbol if specified

    Step 4: Create lib/markos/saas/billing/preflight/index.cjs CommonJS twin re-exporting full surface for legacy api/*.js consumers.

    Step 5: Create scripts/preconditions/215-01-check-upstream.cjs — CLI wrapper invoking assertUpstreamReady; matches api/cron/webhooks-dlq-purge.js createClient pattern verbatim; on hard miss: console.error('MISSING_UPSTREAM_PHASE: <table>') + process.exit(1); on soft miss: console.warn('SOFT_MISSING: <table>'); on success: console.log('P215 upstream preflight: PASSED').

    Step 6: Create lib/markos/mcp/tools/saas-billing.cjs SKELETON (full descriptor implementation lands in Task 1; this step ships the credential sanitization layer):
      - Export CREDENTIAL_FIELDS = ['stripe_secret_key','stripe_webhook_secret','stripe_tax_api_key','mercadopago_access_token','mercadopago_webhook_secret','quickbooks_oauth_token','quickbooks_realm_id','siigo_api_key','alegra_api_key','dian_pkcs12_certificate','dian_pkcs12_password','dian_authorization_token','vault_ref']
      - Export sanitizeBillingResponse(obj) — recursively walks object/array; for each key in CREDENTIAL_FIELDS replaces value with '[REDACTED]'; preserves all other keys; handles null/primitive base case
      - Export tools = { /* placeholder — Task 1 fills in get_invoice + list_invoices_by_tenant + get_payment_attempts + get_billing_events */ }
      - DO NOT register into lib/markos/mcp/tools/index.cjs yet (Task 1 does that with full descriptors)

    Step 7: Create test/saas-215/preflight/architecture-lock.test.js — invokes scanForbiddenPatterns({paths:['lib/markos/saas/billing','api/v1/saas','lib/markos/mcp/tools/saas-billing.cjs']}); asserts 0 violations; ALSO asserts scan for CREDENTIAL_FORBIDDEN_LITERALS with allowlist exception for credential-vault.ts and saas-billing.cjs CREDENTIAL_FIELDS array constants.

    Step 8: Create test/saas-215/preflight/upstream-gate.test.js — mocks supabase client; tests assertUpstreamReady throws UpstreamPhaseNotLandedError when REQUIRED table missing; logs warning when SOFT table missing.

    Step 9: Create test/saas-215/preflight/helper-presence.test.js — imports lib/markos/crm/agent-actions and asserts buildApprovalPackage exported; same positive assertions for requireHostedSupabaseAuth + resolvePlugin + lib/markos/mcp/tools/index.cjs + contracts/openapi.json + 7 webhook engine files; negative assertions: createApprovalPackage / requireSupabaseAuth / lookupPlugin / requireTenantContext / serviceRoleClient NOT exported anywhere in lib/markos/saas/billing/.

    Step 10: Create test/saas-215/preflight/credential-sanitization.test.js — invokes sanitizeBillingResponse with a fixture containing all 12 CREDENTIAL_FIELDS + vault_ref populated; asserts every field replaced with '[REDACTED]'; nested object/array case covered; null/primitive base case covered.
  </action>
  <verify>
    <automated>npm test -- test/saas-215/preflight/</automated>
  </verify>
  <done>4 preflight files exported; preflight CLI hard-fails with exit 1 when any REQUIRED_UPSTREAM table missing; architecture-lock test scans 3 path globs and asserts 0 violations + 0 credential literal leaks; helper-presence test asserts 12 positive + 5 negative symbols (5 forbidden helpers absent); credential-sanitization test asserts 13 fields redacted; saas-billing.cjs skeleton ships sanitizeBillingResponse function.</done>
</task>

<task type="auto">
  <name>Task 1: Migration 118 — saas_invoices + saas_payment_attempts + saas_billing_events + saas_credential_vault_refs SOR tables + INVOICE_REQUIRES_PRICING_AND_LEGAL_FIELDS + BILLING_CREDENTIAL_REQUIRES_VAULT_REF DB-triggers + 9 billing lib files + MCP tool descriptors + 2 API handlers + 5 contracts + domain-1 + RLS tests</name>
  <files>supabase/migrations/118_saas_billing_invoice_contracts.sql, lib/markos/saas/billing/contracts.ts, lib/markos/saas/billing/contracts.cjs, lib/markos/saas/billing/invoice.ts, lib/markos/saas/billing/payment-attempt.ts, lib/markos/saas/billing/billing-event.ts, lib/markos/saas/billing/credential-vault.ts, lib/markos/saas/billing/log-redaction.ts, lib/markos/saas/billing/llm-shield.ts, lib/markos/saas/billing/index.cjs, lib/markos/mcp/tools/saas-billing.cjs, lib/markos/mcp/tools/index.cjs, api/v1/saas/billing.js, api/v1/saas/invoices.js, contracts/F-271-saas-invoice-v1.yaml, contracts/F-272-saas-payment-attempt-v1.yaml, contracts/F-273-saas-billing-event-v1.yaml, contracts/F-274-saas-credential-vault-ref-v1.yaml, contracts/F-284-saas-billing-api-v1.yaml, contracts/flow-registry.json, test/saas-215/domain-1/invoice-pricing-trigger.test.js, test/saas-215/domain-1/invoice-tax-jurisdiction-trigger.test.js, test/saas-215/domain-1/invoice-legal-number-trigger.test.js, test/saas-215/domain-1/payment-attempt-idempotency.test.js, test/saas-215/domain-1/billing-event-shape.test.js, test/saas-215/domain-1/credential-vault-trigger.test.js, test/saas-215/domain-1/mcp-credential-sanitize.test.js, test/saas-215/domain-1/log-redaction.test.js, test/saas-215/rls/invoice-cross-tenant-denial.test.js, .planning/phases/215-saas-suite-billing-payments-compliance/215-VALIDATION.md</files>
  <read_first>
    - .planning/phases/215-saas-suite-billing-payments-compliance/215-RESEARCH.md §Domain 1 §saas_invoices Table Schema §saas_payment_attempts Table Schema §saas_billing_events Table Schema §saas_credential_vault_refs Table Schema §DB-Trigger Compliance INVOICE_REQUIRES_PRICING_AND_LEGAL_FIELDS
    - .planning/phases/215-saas-suite-billing-payments-compliance/215-RESEARCH.md §Sensitive Credential Handling Layer 1 (storage), Layer 2 (BILLING_CREDENTIAL_REQUIRES_VAULT_REF trigger lines 1199-1222), Layer 3 (MCP sanitization lines 1226-1252), Layer 4 (log redaction lines 1255-1265), Layer 5 (LLM shield lines 1267-1273)
    - obsidian/brain/SaaS Suite Canon.md §Core Objects §SaaSInvoice (canonical column-level shape)
    - obsidian/brain/Pricing Engine Canon.md §Canonical Placeholder (sentinel `{{MARKOS_PRICING_ENGINE_PENDING}}`)
    - obsidian/reference/MarkOS v2 Operating Loop Spec.md line 49 (SaaSInvoice spec — "SaaS invoice and legal billing record: processor status, taxes, credits, accounting sync, US/Colombia compliance fields")
    - supabase/migrations/76_markos_cli_tenant_env.sql (pgcrypto pattern for fallback storage)
    - supabase/migrations/100_crm_schema_identity_graph_hardening.sql (RLS pattern reference)
    - lib/markos/mcp/tools/index.cjs (registry pattern; ADD saas-billing.cjs entry without modifying other tool descriptors)
  </read_first>
  <action>
    Step 1: Create supabase/migrations/118_saas_billing_invoice_contracts.sql:
      - `CREATE EXTENSION IF NOT EXISTS pgcrypto;` (defensive idempotent)
      - CREATE TABLE saas_invoices (full schema per RESEARCH §Domain 1 §saas_invoices Table Schema lines 390-484 — verbatim columns: invoice_id PK uuid DEFAULT gen_random_uuid(), tenant_id NOT NULL REFERENCES markos_orgs(org_id), subscription_id NOT NULL, customer_id NOT NULL, invoice_number text NOT NULL, invoice_number_legal text, invoice_type CHECK 5 values, status CHECK 6 values, pricing_recommendation_id uuid, pricing_sentinel text, currency text NOT NULL, subtotal numeric(15,4) NOT NULL, discount_amount numeric(15,4) NOT NULL DEFAULT 0, tax_lines jsonb NOT NULL DEFAULT '[]', tax_total numeric(15,4) DEFAULT 0, total numeric(15,4) NOT NULL, amount_paid numeric(15,4) DEFAULT 0, amount_due numeric(15,4) NOT NULL, billing_country text NOT NULL, tax_jurisdiction text NOT NULL, sales_tax_calculated_at timestamptz, stripe_tax_calculation_id text, cufe text, electronic_invoice_xml text, electronic_invoice_pdf_url text, dian_transmission_state CHECK 5 values, dian_provider_response_id text, iva_rate numeric(5,4), iva_amount numeric(15,4), retefuente_rate numeric(5,4), retefuente_amount numeric(15,4), reteiva_rate numeric(5,4), reteiva_amount numeric(15,4), reteica_rate numeric(5,4), reteica_amount numeric(15,4), processor CHECK 2 values NOT NULL, processor_invoice_id text, processor_payment_intent_id text, payment_method_type text, quickbooks_invoice_id text, siigo_invoice_id text, alegra_invoice_id text, accounting_synced_at timestamptz, sent_to_email text, sent_at timestamptz, viewed_at timestamptz, due_at timestamptz NOT NULL, issued_at timestamptz NOT NULL DEFAULT now(), correction_of_invoice_id uuid, created_at timestamptz NOT NULL DEFAULT now(), updated_at timestamptz NOT NULL DEFAULT now())
      - ALTER TABLE saas_invoices ENABLE ROW LEVEL SECURITY
      - CREATE POLICY saas_invoices_tenant_isolation ON saas_invoices FOR ALL USING (tenant_id = current_setting('app.tenant_id', true)::uuid)
      - 4 indexes per RESEARCH (si_tenant_subscription_idx, si_tenant_status_idx, si_billing_country_idx, si_processor_invoice_id_idx)
      - CREATE TABLE saas_payment_attempts (full schema per RESEARCH §Domain 1 §saas_payment_attempts lines 491-528 — verbatim) + RLS + 2 indexes (spa_tenant_invoice_idx, spa_tenant_failed_idx partial WHERE status='failed')
      - CREATE TABLE saas_billing_events (full schema per RESEARCH lines 534-553) + RLS + 2 indexes
      - CREATE TABLE saas_credential_vault_refs (full schema per RESEARCH lines 560-577) + RLS + UNIQUE(tenant_id, credential_name, config_id)
      - CREATE OR REPLACE FUNCTION enforce_invoice_pricing_and_legal() — verbatim per RESEARCH lines 582-605 (RAISE EXCEPTION 'INVOICE_REQUIRES_PRICING_AND_LEGAL_FIELDS' on the 3 conditions)
      - CREATE TRIGGER trg_invoice_pricing_and_legal BEFORE INSERT OR UPDATE ON saas_invoices FOR EACH ROW EXECUTE FUNCTION enforce_invoice_pricing_and_legal()
      - CREATE OR REPLACE FUNCTION enforce_credential_vault_ref() — verbatim per RESEARCH lines 1200-1218 (RAISE EXCEPTION 'BILLING_CREDENTIAL_REQUIRES_VAULT_REF' on the 2 conditions: pattern mismatch + bootstrap-mistake guard)
      - CREATE TRIGGER trg_credential_vault_ref BEFORE INSERT OR UPDATE ON saas_credential_vault_refs FOR EACH ROW EXECUTE FUNCTION enforce_credential_vault_ref()

    Step 2: Create lib/markos/saas/billing/contracts.ts exporting INVOICE_TYPES (5: 'subscription','one_time','credit_note','void','prorated') + INVOICE_STATUSES (6: 'draft','open','paid','void','uncollectible','past_due') + PROCESSORS (2: 'stripe','mercado_pago') + DIAN_TRANSMISSION_STATES (5: 'pending','transmitted','accepted','rejected','void') + PAYMENT_ATTEMPT_STATUSES (5: 'pending','succeeded','failed','requires_action','canceled') + CREDENTIAL_FIELDS (12 + 'vault_ref') + PRICING_SENTINEL = '{{MARKOS_PRICING_ENGINE_PENDING}}' + 4 zod schemas (SaaSInvoiceSchema / SaaSPaymentAttemptSchema / SaaSBillingEventSchema / SaaSCredentialVaultRefSchema) parity with F-271..F-274 YAMLs.

    Step 3: Create lib/markos/saas/billing/invoice.ts exporting createInvoice + getInvoice + updateInvoiceStatus + validateInvoiceInput. validateInvoiceInput enforces: pricing_recommendation_id IS NOT NULL OR pricing_sentinel === PRICING_SENTINEL (literal); tax_jurisdiction non-empty; if status !== 'draft' then invoice_number_legal IS NOT NULL. createInvoice INSERTs through DB-trigger (defense-in-depth pair).

    Step 4: Create lib/markos/saas/billing/payment-attempt.ts exporting createPaymentAttempt + recordSignatureVerification + getFailedAttempts. createPaymentAttempt enforces idempotency_key UNIQUE (catches Postgres duplicate key error and returns existing row — idempotent). getFailedAttempts(tenantId, since) filters status='failed' for P218 trigger consumption.

    Step 5: Create lib/markos/saas/billing/billing-event.ts exporting recordBillingEvent + getBillingEvents. recordBillingEvent calls redactWebhookPayload(payload, processor) before INSERT — saas_billing_events.payload_redacted ALWAYS stores the redacted version; raw payload NEVER persisted.

    Step 6: Create lib/markos/saas/billing/credential-vault.ts exporting storeCredentialVaultRef + resolveCredential + rotateCredential + CREDENTIAL_FIELDS. storeCredentialVaultRef calls Supabase Vault `vault.create_secret(plaintext_value, name)` (preferred) which returns a UUID → store as `vault://${uuid}`; on Vault error fallback to pgcrypto `pgp_sym_encrypt(plaintext_value, MARKOS_BILLING_ENCRYPTION_KEY)` → store as `pgcrypto-ref:${base64(ciphertext)}`. resolveCredential reads from `vault.decrypted_secrets` view (Vault path) or `pgp_sym_decrypt(ciphertext, key)` (pgcrypto path) — resolution returns plaintext ONLY to in-process caller; the plaintext is NEVER written to log/MCP/prompt.

    Step 7: Create lib/markos/saas/billing/log-redaction.ts exporting redactWebhookPayload(payload, processor) + 5 PII field arrays. STRIPE_PII_FIELDS = ['email','name','address','phone','last4','fingerprint','ip_address','brand']; MP_PII_FIELDS = ['email','first_name','last_name','phone','identification','card']; QB_PII_FIELDS = ['email','phone','address','tax_id']; SIIGO_PII_FIELDS = ['email','phone','address','document_id']; DIAN_PII_FIELDS = ['nit','razon_social','email','phone','address']. redactWebhookPayload deep-clones + recursively replaces matching field values with '[REDACTED]'.

    Step 8: Create lib/markos/saas/billing/llm-shield.ts exporting stripCredentialsForLLM + LLM_SAFE_FIELDS. LLM_SAFE_FIELDS = ['amount','status','invoice_id','event_type','processor','currency','tenant_id','subscription_id','dunning_attempt_number','tax_jurisdiction','billing_country']. stripCredentialsForLLM walks input object and ONLY copies LLM_SAFE_FIELDS to output — all other fields stripped (including raw payload, vault_ref, credential names).

    Step 9: Create lib/markos/saas/billing/index.cjs CommonJS twin re-exporting full surface: contracts + invoice + payment-attempt + billing-event + credential-vault + log-redaction + llm-shield.

    Step 10: Update lib/markos/mcp/tools/saas-billing.cjs from skeleton (Task 0.5) to full descriptors:
      - get_invoice: input { tenant_id, invoice_id }; output saas_invoices row with credential fields stripped; uses sanitizeBillingResponse on return
      - list_invoices_by_tenant: input { tenant_id, status?, billing_country?, limit, offset }; output paginated list; sanitized
      - get_payment_attempts: input { tenant_id, invoice_id?, status?, limit, offset }; output filtered list; sanitized
      - get_billing_events: input { tenant_id, event_type?, processor?, limit, offset }; output filtered list; sanitized (payload_redacted is already pre-redacted but sanitizeBillingResponse adds defense-in-depth)
      - Register into lib/markos/mcp/tools/index.cjs registry by adding require('./saas-billing.cjs') call (do NOT modify other entries)

    Step 11: Create api/v1/saas/billing.js handler: requires onboarding/backend/runtime-context.cjs requireHostedSupabaseAuth; supports GET /v1/saas/billing (overview — invoice counts grouped by status + payment attempt counts last 30d + active dunning count); returns 401 on auth fail, 200 + JSON otherwise; ALL response bodies pass through sanitizeBillingResponse.

    Step 12: Create api/v1/saas/invoices.js handler: GET /v1/saas/invoices (paginated, filterable by status/billing_country) + GET /v1/saas/invoices/:invoice_id (full payload with credential fields stripped). Use requireHostedSupabaseAuth + tenant-scoped supabase client. Sanitize all responses.

    Step 13: Create 5 contract YAMLs (F-271 + F-272 + F-273 + F-274 + F-284) per RESEARCH §F-ID Pre-Allocation Table. Append 5 entries to contracts/flow-registry.json (ensure NO collision with F-001..F-270).

    Step 14: Create test files:
      - invoice-pricing-trigger.test.js: Postgres-backed — INSERT with pricing_recommendation_id IS NULL AND pricing_sentinel IS NULL → expect EXCEPTION 'INVOICE_REQUIRES_PRICING_AND_LEGAL_FIELDS'; pricing_sentinel='{{MARKOS_PRICING_ENGINE_PENDING}}' AND tax_jurisdiction='US_STATE_CA' → succeeds; valid FK → succeeds
      - invoice-tax-jurisdiction-trigger.test.js: INSERT with tax_jurisdiction='' OR NULL → exception
      - invoice-legal-number-trigger.test.js: INSERT with status='open' AND invoice_number_legal IS NULL → exception; status='draft' AND invoice_number_legal IS NULL → succeeds
      - payment-attempt-idempotency.test.js: INSERT 2 rows with same idempotency_key → second fails with UNIQUE violation; createPaymentAttempt catches violation and returns existing row
      - billing-event-shape.test.js: zod parity assertion against F-273 YAML; payload_redacted required
      - credential-vault-trigger.test.js: INSERT with vault_ref='plaintext_secret_value' → exception 'BILLING_CREDENTIAL_REQUIRES_VAULT_REF'; vault_ref='vault://abc-123-uuid' → succeeds; vault_ref='pgcrypto-ref:dGVzdA==' → succeeds; vault_ref equals credential_name (e.g. 'stripe_secret_key' literal in vault_ref column) → exception
      - mcp-credential-sanitize.test.js: invoke each of 4 MCP tool descriptors with fixture row containing all 12 credential fields populated; assert every credential field replaced with '[REDACTED]' AND vault_ref also '[REDACTED]'
      - log-redaction.test.js: redactWebhookPayload with Stripe payload containing email/name/last4 → all replaced with '[REDACTED]'; same for MP/QB/Siigo/DIAN
      - invoice-cross-tenant-denial.test.js: INSERT under tenant A then SELECT under tenant B returns 0 rows for saas_invoices + saas_payment_attempts + saas_billing_events + saas_credential_vault_refs

    Step 15: Append Plan 01 row to .planning/phases/215-saas-suite-billing-payments-compliance/215-VALIDATION.md per-task verification map; flip frontmatter `wave_0_complete: true` and `nyquist_compliant: true` after Plan 01 closeout.
  </action>
  <verify>
    <automated>npm test -- test/saas-215/domain-1/ test/saas-215/rls/invoice-cross-tenant-denial.test.js</automated>
  </verify>
  <done>Migration 118 applies cleanly; saas_invoices + saas_payment_attempts + saas_billing_events + saas_credential_vault_refs exist with RLS + INVOICE_REQUIRES_PRICING_AND_LEGAL_FIELDS + BILLING_CREDENTIAL_REQUIRES_VAULT_REF triggers; createInvoice returns valid row; F-271..F-274 + F-284 in flow-registry.json; 8 domain-1 test files green; cross-tenant denial passes; 215-VALIDATION.md updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→/v1/saas/billing + /v1/saas/invoices | Tenant-scoped read of own billing data; auth via requireHostedSupabaseAuth |
| MCP→saas-billing tools | Tenant-scoped read; ALL responses pass through sanitizeBillingResponse credential redactor |
| service-role→saas_credential_vault_refs INSERT | Plaintext-credential leak risk; defense via BILLING_CREDENTIAL_REQUIRES_VAULT_REF DB-trigger blocks at database level |
| LLM call→processor data | Prompt-injection risk via webhook payloads; defense via llm-shield.ts stripCredentialsForLLM whitelist |
| log emit→processor webhook payload | PII/credential leak risk; defense via log-redaction.ts redactWebhookPayload before any console.log |
| Vault decryption→in-process plaintext | Plaintext ONLY in resolveCredential return value; never written to log/MCP/prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-215-01 | S | api/v1/saas/invoices.js | mitigate | requireHostedSupabaseAuth; tenant_id stamped from session, never request body |
| T-215-02 | T | saas_invoices INSERT path (no Pricing Engine FK) | mitigate | INVOICE_REQUIRES_PRICING_AND_LEGAL_FIELDS DB-trigger blocks insert without pricing_recommendation_id OR sentinel |
| T-215-03 | T | saas_credential_vault_refs INSERT plaintext credential | mitigate | BILLING_CREDENTIAL_REQUIRES_VAULT_REF DB-trigger blocks vault_ref not matching vault://%/pgcrypto-ref:% pattern |
| T-215-04 | I | MCP tool output leaking credential fields | mitigate | sanitizeBillingResponse strips 12 CREDENTIAL_FIELDS + vault_ref on every tool descriptor return |
| T-215-05 | I | Webhook payload PII in logs | mitigate | redactWebhookPayload deep-clones + redacts BEFORE any console.log/audit emit; saas_billing_events.payload_redacted is the only persisted form |
| T-215-06 | I | LLM prompt injection via processor payload | mitigate | stripCredentialsForLLM whitelists business fields only; raw payload + credential fields never reach LLM context |
| T-215-07 | I | saas_invoices cross-tenant SELECT | mitigate | RLS policy saas_invoices_tenant_isolation enforces tenant_id; cross-tenant denial test in CI |
| T-215-08 | E | service-role-leak via SUPABASE_SERVICE_ROLE_KEY | mitigate | env-scoped key; rotated via foundation; preflight CLI uses service role only for table existence check (limit 0) |
| T-215-09 | T | Pricing sentinel literal forgery | mitigate | App-layer validateInvoiceInput enforces pricing_sentinel === PRICING_SENTINEL exact literal; DB-trigger only checks NOT NULL (defense-in-depth pair) |
</threat_model>

<verification>
- npm test -- test/saas-215/preflight/wave-0-baseline.test.js
- npm test -- test/saas-215/preflight/architecture-lock.test.js
- npm test -- test/saas-215/preflight/upstream-gate.test.js
- npm test -- test/saas-215/preflight/helper-presence.test.js
- npm test -- test/saas-215/preflight/credential-sanitization.test.js
- npm test -- test/saas-215/domain-1/
- npm test -- test/saas-215/rls/invoice-cross-tenant-denial.test.js
- node scripts/preconditions/215-01-check-upstream.cjs (exits 0 when upstream tables present)
- supabase migration up to slot 118 applies cleanly
</verification>

<success_criteria>
- saas_invoices + saas_payment_attempts + saas_billing_events + saas_credential_vault_refs tables exist in DB with RLS + 2 DB-triggers (INVOICE_REQUIRES_PRICING_AND_LEGAL_FIELDS + BILLING_CREDENTIAL_REQUIRES_VAULT_REF)
- assertUpstreamReady CLI hard-fails when any of P203/P205/P206/P214 tables missing
- architecture-lock test asserts 0 forbidden patterns + 0 credential literal leaks in P215 surface
- helper-presence test asserts 12 positive helpers (incl. 7 webhook engine files) + 5 negative forbidden symbols
- credential-sanitization test asserts 13 fields redacted by sanitizeBillingResponse
- 5 contracts F-271 + F-272 + F-273 + F-274 + F-284 in flow-registry.json
- 4 MCP tools registered in lib/markos/mcp/tools/index.cjs (saas-billing.cjs)
- V4.1.0-MIGRATION-SLOT-COORDINATION.md contains P215 reservation rows + Q-7 SLOT ORDERING CRISIS resolution block
- 215-VALIDATION.md per-task verification map updated for Plan 01; wave_0_complete=true; nyquist_compliant=true
</success_criteria>

<output>
After completion, create `.planning/phases/215-saas-suite-billing-payments-compliance/215-01-SUMMARY.md` documenting: artifacts created, 2 DB-trigger exception names (INVOICE_REQUIRES_PRICING_AND_LEGAL_FIELDS + BILLING_CREDENTIAL_REQUIRES_VAULT_REF), FK direction lock (P215 has zero FKs into P216-P220), HIGH 2 5-layer credential defense status (Layer 1 storage + Layer 2 DB-trigger + Layer 3 MCP sanitization + Layer 4 log redaction + Layer 5 LLM shield ALL SHIPPED), downstream-phase impact (P216-P220 assertUpstreamReady can now check saas_invoices + saas_payment_attempts; Plans 02-06 can FK saas_credential_vault_refs).
</output>
</content>
</invoke>