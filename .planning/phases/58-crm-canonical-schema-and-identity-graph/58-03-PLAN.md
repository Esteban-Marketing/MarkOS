---
phase: 58-crm-canonical-schema-and-identity-graph
plan: 03
type: execute
wave: 3
depends_on:
  - 58-01
  - 58-02
  - 51-multi-tenant-foundation-and-authorization
files_modified:
  - api/crm/contacts.js
  - api/crm/companies.js
  - api/crm/deals.js
  - api/crm/activities.js
  - api/crm/merge.js
  - lib/markos/crm/api.ts
  - .planning/phases/58-crm-canonical-schema-and-identity-graph/58-VALIDATION.md
  - test/crm-api/crm-api-contracts.test.js
  - test/crm-api/crm-merge-api.test.js
  - test/tenant-auth/crm-tenant-isolation.test.js
autonomous: true
requirements:
  - CRM-01
  - CRM-02
  - TRK-04

must_haves:
  truths:
    - "Phase 58 ends with tenant-safe API seams for CRUD, timeline reads, and merge review so later phases can build on contracts instead of ad hoc direct-table access."
    - "CRM APIs remain fail-closed on missing tenant or unauthorized role context and do not infer scope from route parameters alone."
    - "The phase validation ledger names concrete automated seams for schema, timeline, identity, and API coverage rather than leaving Phase 58 verification to future reconstruction."
  artifacts:
    - path: "api/crm/contacts.js"
      provides: "Tenant-safe contact CRUD and search boundary using the canonical CRM entity helpers and auth posture."
      exports: []
    - path: "api/crm/activities.js"
      provides: "Unified CRM timeline query boundary for record-linked activity reads."
      exports: []
    - path: "api/crm/merge.js"
      provides: "Reviewable merge and stitch decision surface with immutable lineage persistence."
      exports: []
    - path: ".planning/phases/58-crm-canonical-schema-and-identity-graph/58-VALIDATION.md"
      provides: "Concrete Phase 58 validation ledger covering schema, timeline, identity, and API seams across all execution waves."
      exports: []
  key_links:
    - from: "api/crm/contacts.js"
      to: "lib/markos/crm/api.ts"
      via: "shared tenant and role enforcement plus contract normalization"
      pattern: "requireCrmTenantContext|assertCrmMutationAllowed"
    - from: "api/crm/activities.js"
      to: "lib/markos/crm/timeline.ts"
      via: "timeline assembly remains a shared contract, not route-local logic"
      pattern: "buildCrmTimeline|normalizeActivityFamily"
    - from: "api/crm/merge.js"
      to: "lib/markos/crm/merge.ts"
      via: "merge review and immutable lineage persistence"
      pattern: "recordMergeDecision|applyApprovedMerge"
---

<objective>
Expose the canonical Phase 58 CRM model through tenant-safe API boundaries and lock the phase validation ledger.

Purpose: finish Phase 58 as an execution-ready foundation by turning schema and lineage work into stable API seams with explicit verification coverage.
Output: CRM CRUD, timeline, and merge APIs; shared API enforcement helpers; and a concrete Phase 58 validation strategy tied to the delivered seams.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/58-crm-canonical-schema-and-identity-graph/58-CONTEXT.md
@.planning/phases/58-crm-canonical-schema-and-identity-graph/58-RESEARCH.md
@.planning/phases/58-crm-canonical-schema-and-identity-graph/58-01-PLAN.md
@.planning/phases/58-crm-canonical-schema-and-identity-graph/58-02-PLAN.md
@api/tenant-plugin-settings.js
@api/status.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 58-03-01: Create the CRM API contract and fail-closed test boundary</name>
  <read_first>
    - api/tenant-plugin-settings.js
    - api/status.js
    - .planning/phases/58-crm-canonical-schema-and-identity-graph/58-VALIDATION.md
  </read_first>
  <files>test/crm-api/crm-api-contracts.test.js, test/crm-api/crm-merge-api.test.js, test/tenant-auth/crm-tenant-isolation.test.js</files>
  <acceptance_criteria>
    - API tests cover CRUD, timeline reads, merge review, and unauthorized negative paths.
    - Test expectations explicitly require tenant-bound scope, canonical record identifiers, and immutable merge-decision evidence.
    - The API test layer does not assume UI pages or dashboard flows to prove correctness.
  </acceptance_criteria>
  <action>
Create the failing test and contract boundary for the API layer before implementing handlers. Reuse the current fail-closed authorization style from tenant and billing APIs, but make the CRM-specific expectations explicit: canonical IDs, role-aware mutation gates, timeline read semantics, and merge review evidence.
  </action>
  <verify>
    <automated>node --test test/crm-api/crm-api-contracts.test.js test/crm-api/crm-merge-api.test.js test/tenant-auth/crm-tenant-isolation.test.js</automated>
  </verify>
  <done>
Wave 3 begins with a concrete API contract and regression boundary rather than route-by-route improvisation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 58-03-02: Implement tenant-safe CRM CRUD, timeline, and merge handlers</name>
  <read_first>
    - lib/markos/crm/entities.ts
    - lib/markos/crm/timeline.ts
    - lib/markos/crm/merge.ts
  </read_first>
  <files>api/crm/contacts.js, api/crm/companies.js, api/crm/deals.js, api/crm/activities.js, api/crm/merge.js, lib/markos/crm/api.ts, test/crm-api/crm-api-contracts.test.js, test/crm-api/crm-merge-api.test.js, test/tenant-auth/crm-tenant-isolation.test.js</files>
  <acceptance_criteria>
    - Contacts, companies, and deals are exposed through tenant-safe CRUD handlers built on the canonical CRM helpers.
    - Timeline reads go through the shared timeline contract and preserve source references and ordering semantics.
    - Merge and stitch decisions remain reviewable, role-aware, and append-only.
    - Missing tenant context and unauthorized mutation attempts fail closed with deterministic response semantics.
  </acceptance_criteria>
  <action>
Implement the first CRM API layer on top of the Phase 58 contract modules. Keep route scope narrow and explicit: CRUD for the core entity families needed immediately, timeline reads against the shared activity ledger, and merge-review mutations against immutable lineage helpers. Do not let handlers access raw tables directly without the shared CRM enforcement and normalization layer.
  </action>
  <verify>
    <automated>node --test test/crm-api/crm-api-contracts.test.js test/crm-api/crm-merge-api.test.js test/tenant-auth/crm-tenant-isolation.test.js --test-name-pattern="crm|timeline|merge|tenant|deny|contact|deal|company"</automated>
  </verify>
  <done>
Phase 58 leaves behind stable tenant-safe API seams that later pipeline, tracking, and UI work can consume directly.
  </done>
</task>

<task type="auto">
  <name>Task 58-03-03: Finalize the Phase 58 validation ledger for execution and later verification</name>
  <read_first>
    - .planning/phases/58-crm-canonical-schema-and-identity-graph/58-RESEARCH.md
    - .planning/phases/58-crm-canonical-schema-and-identity-graph/58-01-PLAN.md
    - .planning/phases/58-crm-canonical-schema-and-identity-graph/58-02-PLAN.md
  </read_first>
  <files>.planning/phases/58-crm-canonical-schema-and-identity-graph/58-VALIDATION.md</files>
  <acceptance_criteria>
    - The validation ledger names the automated commands, portable evidence checks, and manual checks for each Phase 58 wave.
    - Schema, timeline, identity, and API coverage are all represented explicitly.
    - Exit conditions are defined in terms of direct evidence, not implied milestone progress.
  </acceptance_criteria>
  <action>
Replace any placeholder validation notes with a concrete Phase 58 validation ledger that execution and later verification can use directly. The ledger must make it obvious how CRM-01, CRM-02, and the Phase 58 foundation for TRK-04 will be proven without reopening architectural scope.
  </action>
  <verify>
    <manual>Confirm `58-VALIDATION.md` names every wave, maps them to explicit requirements, lists the automated commands, and defines a direct exit condition for the phase.</manual>
  </verify>
  <done>
Phase 58 planning ends with an execution-grade validation ledger instead of deferred verification guesswork.
  </done>
</task>

</tasks>

<verification>
Run the CRM API and tenant-isolation suites after each handler task, then confirm the validation ledger reflects the actual Wave 1 through Wave 3 evidence seams.
</verification>

<success_criteria>
Phase 58 is execution-ready with canonical CRM entities, append-only timeline and identity lineage, tenant-safe API seams, and a direct validation contract for the whole phase.
</success_criteria>

<output>
After completion, create `.planning/phases/58-crm-canonical-schema-and-identity-graph/58-03-SUMMARY.md`
</output>