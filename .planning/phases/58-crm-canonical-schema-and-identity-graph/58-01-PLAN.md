---
phase: 58-crm-canonical-schema-and-identity-graph
plan: 01
type: execute
wave: 1
depends_on:
  - 51-multi-tenant-foundation-and-authorization
  - 53-agentic-markos-orchestration-and-mir-msp-intelligence
  - 54-billing-metering-and-enterprise-governance
  - 57-observability-and-incident-closure
files_modified:
  - contracts/F-58-crm-entity-crud-v1.yaml
  - supabase/migrations/58_crm_core_entities.sql
  - supabase/migrations/58_crm_custom_fields.sql
  - lib/markos/crm/contracts.ts
  - lib/markos/crm/entities.ts
  - test/crm-schema/crm-core-entities.test.js
  - test/tenant-auth/crm-tenant-isolation.test.js
autonomous: true
requirements:
  - CRM-01

must_haves:
  truths:
    - "Contacts, companies, deals, accounts, customers, tasks, and notes exist as first-class tenant-safe CRM entities rather than being inferred from analytics or legacy workspace tables."
    - "Custom fields are governed through explicit definitions and scoped values, not ad hoc JSON sprawl on canonical records."
    - "Every new CRM table inherits Phase 51 tenant partitioning, RLS, immutable actor metadata where needed, and fail-closed negative-path coverage."
  artifacts:
    - path: "supabase/migrations/58_crm_core_entities.sql"
      provides: "Canonical CRM entity schema, indexes, foreign keys, and tenant-safe access policies for the first-class object layer."
      exports: []
    - path: "supabase/migrations/58_crm_custom_fields.sql"
      provides: "Custom-field definition and value schema that keeps extensibility explicit, object-scoped, and queryable."
      exports: []
    - path: "lib/markos/crm/contracts.ts"
      provides: "TypeScript contracts for canonical CRM records and custom-field payloads used by later API and timeline layers."
      exports:
        - "crmRecordKinds"
        - "crmEntitySchema"
        - "crmCustomFieldDefinitionSchema"
        - "crmCustomFieldValueSchema"
    - path: "lib/markos/crm/entities.ts"
      provides: "Shared entity helpers for create, update, read, and tenant-scoped record normalization across CRM primitives."
      exports:
        - "createCrmEntity"
        - "updateCrmEntity"
        - "listCrmEntities"
  key_links:
    - from: "lib/markos/crm/entities.ts"
      to: "lib/markos/crm/contracts.ts"
      via: "canonical record validation and normalization"
      pattern: "crmEntitySchema|crmRecordKinds"
    - from: "supabase/migrations/58_crm_core_entities.sql"
      to: "supabase/migrations/51_multi_tenant_foundation.sql"
      via: "tenant_id, membership checks, and RLS inheritance"
      pattern: "tenant_id|ENABLE ROW LEVEL SECURITY|markos_tenant_memberships"
    - from: "test/tenant-auth/crm-tenant-isolation.test.js"
      to: "supabase/migrations/58_crm_core_entities.sql"
      via: "cross-tenant denial and WITH CHECK enforcement"
      pattern: "contact|company|deal|account|customer|tenant"
---

<objective>
Create the canonical CRM object layer and governed extensibility foundation.

Purpose: satisfy CRM-01 by making tenant-safe CRM entities explicit before timeline, stitching, pipeline, and outbound work build on top of them.
Output: core CRM schema migrations, governed custom-field schema, shared TypeScript contracts, entity helpers, and tenant-isolation tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/58-crm-canonical-schema-and-identity-graph/58-CONTEXT.md
@.planning/phases/58-crm-canonical-schema-and-identity-graph/58-RESEARCH.md
@supabase/migrations/51_multi_tenant_foundation.sql
@supabase/migrations/54_identity_federation.sql
@lib/markos/contracts/schema.ts
@api/tenant-plugin-settings.js
@test/tenant-auth/tenant-schema-rls.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 58-01-01: Establish failing schema and tenant-isolation test seams for the CRM core</name>
  <read_first>
    - test/tenant-auth/tenant-schema-rls.test.js
    - test/billing/usage-normalization.test.js
    - lib/markos/contracts/schema.ts
  </read_first>
  <files>test/crm-schema/crm-core-entities.test.js, test/tenant-auth/crm-tenant-isolation.test.js</files>
  <acceptance_criteria>
    - New test seams name the required first-class CRM entity families directly: contact, company, deal, account, customer, task, and note.
    - Tenant-isolation tests cover cross-tenant reads and writes against the new CRM tables.
    - Test scaffolds make custom-field scoping and legacy-table non-reliance explicit.
  </acceptance_criteria>
  <action>
Create the Phase 58 Wave 1 TDD scaffold before touching schema or helpers. Reuse the strongest existing RLS and lineage test patterns from Phase 51 and Phase 54, but make the CRM entity family names and expected denial semantics explicit so later waves do not drift into legacy `markos_company` or analytics-driven shortcuts.
  </action>
  <verify>
    <automated>node --test test/crm-schema/crm-core-entities.test.js test/tenant-auth/crm-tenant-isolation.test.js</automated>
  </verify>
  <done>
Wave 1 has concrete failing tests that define the CRM object boundary and tenant-safe behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 58-01-02: Add canonical CRM entity tables, indexes, and RLS policies</name>
  <read_first>
    - supabase/migrations/51_multi_tenant_foundation.sql
    - supabase/migrations/54_identity_federation.sql
    - .planning/phases/58-crm-canonical-schema-and-identity-graph/58-RESEARCH.md
  </read_first>
  <files>supabase/migrations/58_crm_core_entities.sql, test/crm-schema/crm-core-entities.test.js, test/tenant-auth/crm-tenant-isolation.test.js</files>
  <acceptance_criteria>
    - Core CRM tables exist for contacts, companies, deals, accounts, customers, tasks, and notes.
    - Every table includes `tenant_id`, stable primary keys, timestamps, and explicit relationship boundaries where applicable.
    - RLS policies follow the current tenant-membership model and fail closed on missing tenant context.
    - The migration does not overload or mutate legacy workspace tables into canonical CRM truth without an explicit adapter path.
  </acceptance_criteria>
  <action>
Implement the additive CRM schema in a new migration instead of stretching current workspace-era tables. Reuse the Phase 51 tenant partitioning model and the immutable-decision posture from later phases, but keep this wave focused on canonical objects, relationship shape, and safe indexing rather than UI or event ingestion concerns.
  </action>
  <verify>
    <automated>node --test test/crm-schema/crm-core-entities.test.js test/tenant-auth/crm-tenant-isolation.test.js --test-name-pattern="contact|company|deal|account|customer|tenant|rls"</automated>
  </verify>
  <done>
The database now has a tenant-safe CRM object layer that later waves can reference without semantic ambiguity.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 58-01-03: Lock TypeScript contracts, governed custom fields, and shared entity helpers</name>
  <read_first>
    - lib/markos/contracts/schema.ts
    - api/tenant-plugin-settings.js
    - .planning/phases/58-crm-canonical-schema-and-identity-graph/58-CONTEXT.md
  </read_first>
  <files>contracts/F-58-crm-entity-crud-v1.yaml, supabase/migrations/58_crm_custom_fields.sql, lib/markos/crm/contracts.ts, lib/markos/crm/entities.ts, test/crm-schema/crm-core-entities.test.js</files>
  <acceptance_criteria>
    - CRM TypeScript contracts expose canonical object families and governed custom-field schemas.
    - CRUD contract documentation names the supported entity families, tenant-bound identifiers, and fail-closed authorization expectations.
    - Shared entity helpers normalize reads and writes around the new contract layer rather than around placeholder UI routes.
  </acceptance_criteria>
  <action>
Define the Phase 58 contract layer under `lib/markos/crm/` and add the initial flow contract for CRUD boundaries. Separate custom-field definitions from values so later pipeline, reporting, and search work can extend the schema without turning canonical records into unbounded JSON blobs.
  </action>
  <verify>
    <automated>node --test test/crm-schema/crm-core-entities.test.js test/tenant-auth/crm-tenant-isolation.test.js --test-name-pattern="contract|custom|crud|entity"</automated>
  </verify>
  <done>
Wave 1 leaves a stable CRM contract and schema foundation that later phases can consume directly.
  </done>
</task>

</tasks>

<verification>
Run the CRM schema and tenant-isolation suites after each task; Wave 1 is incomplete until entity families, custom-field governance, and tenant-safe RLS all agree on one canonical object model.
</verification>

<success_criteria>
MarkOS has a first-class CRM object layer with governed extensibility, tenant-safe access control, and a shared contract vocabulary that does not depend on analytics events or legacy workspace tables for relationship truth.
</success_criteria>

<output>
After completion, create `.planning/phases/58-crm-canonical-schema-and-identity-graph/58-01-SUMMARY.md`
</output>