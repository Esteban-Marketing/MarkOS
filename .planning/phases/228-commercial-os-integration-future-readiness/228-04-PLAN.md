---
phase: 228-commercial-os-integration-future-readiness
plan: 04
type: execute
wave: 2
depends_on: ["228-01"]
files_modified:
  - package.json
  - vitest.config.ts
  - playwright.config.ts
  - test/vitest/commercial-contracts.spec.ts
  - tests/e2e/commercial-evidence-chain.spec.ts
  - tests/e2e/commercial-evidence-chain.contract.spec.ts
  - tests/e2e/fixtures/commercial-evidence-chain.json
  - .planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md
  - scripts/commercial/verify-test-coverage.cjs
  - test/commercial/test-coverage-matrix.test.js
  - test/commercial/governed-mutation.test.js
  - test/commercial/passive-system-detector.test.js
  - test/commercial/pricing-bypass-detector.test.js
  - test/commercial/evidence-freshness-detector.test.js
  - .storybook/stories/commercial/approval-queues.stories.tsx
  - .storybook/stories/commercial/launch-cockpits.stories.tsx
  - .storybook/stories/commercial/audience-activation.stories.tsx
  - .storybook/stories/commercial/deal-rooms.stories.tsx
  - .storybook/stories/commercial/partner-consoles.stories.tsx
  - .env.example
autonomous: true
requirements:
  - COM-05
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - QA-05
  - QA-06
  - QA-07
  - QA-08
  - QA-09
  - QA-10
  - QA-11
  - QA-12
  - QA-13
  - QA-14
  - QA-15
user_setup: []

must_haves:
  truths:
    - "Vitest is installed and configured at the repo root with `vitest.config.ts`; `npm run test:vitest` runs `*.spec.ts` files under `test/vitest/**` and `test/commercial/**`"
    - "Playwright is installed (chromium binary present) and configured with `playwright.config.ts`; `npm run test:playwright:commercial` runs `tests/e2e/commercial-*.spec.ts`"
    - "Single Playwright E2E suite `tests/e2e/commercial-evidence-chain.spec.ts` proves the CRM → channel → conversion → analytics → sales → ecosystem evidence trace end-to-end against a running dev server (per CONTEXT specifics line 153 — ONE suite per CONTEXT, D-23, D-29). Slice 04 verify command brings up the dev server, runs the suite, and exits 0."
    - "Selector/route-reachability contract suite `tests/e2e/commercial-evidence-chain.contract.spec.ts` always runs (no env gating) and asserts every operator-journey selector + route is declared in fixtures and resolves against a static asset map — used to pre-empt drift when engine routes change"
    - "Cross-phase test-coverage CLI gate enforces: every COM-01..06 + QA-01..15 requirement has its own row with a CONCRETE test_path that resolves via fs.existsSync (no roll-up alias); every gate 3-6 has at least one detector test (governed mutation, passive system, pricing bypass, evidence freshness)"
    - "Detectors fail-closed (hard exit 1) when scan target dirs are missing — per CONTEXT D-01 route-back rule, missing engine dirs mean upstream phase 221-227 has not shipped and Phase 228 cannot legitimately ship green over absent surfaces"
    - "Chromatic visual-regression coverage exists for the 5 D-24 surface families (approval queues, launch cockpits, audience activation, deal rooms, partner consoles) — Storybook stories ship with the slice; CI hook documented and runnable via `npm run commercial:chromatic`"
    - "V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md is EXTENDED (not forked per CONTEXT non-negotiable) — appended new section `## Phase 228 Cross-Engine Detection Matrix` with 6 COM rows + 15 individual QA rows + 10 gate rows; every test_path resolves to a real file"
  artifacts:
    - path: "package.json"
      provides: "vitest + playwright + chromatic dependencies + 8 new scripts: test:vitest, test:vitest:watch, test:playwright, test:playwright:commercial, test:commercial:coverage, test:commercial:gates, commercial:chromatic, commercial:chromatic:dry"
    - path: "vitest.config.ts"
      provides: "Vitest configuration covering test/vitest/**, test/commercial/**.spec.ts"
    - path: "playwright.config.ts"
      provides: "Playwright configuration covering tests/e2e/**, includes webServer config to bring up dev server (onboarding server) when no PLAYWRIGHT_BASE_URL set"
    - path: "tests/e2e/commercial-evidence-chain.spec.ts"
      provides: "Single end-to-end Playwright suite proving evidence chain CRM → channel → conversion → analytics → sales → ecosystem; runs against live dev server. NO env-gating skip — slice 04 verify must run this and exit 0."
    - path: "tests/e2e/commercial-evidence-chain.contract.spec.ts"
      provides: "Selector/route reachability stubs that always run; asserts every operator-journey selector + route declared in fixtures resolves; used as pre-flight to the full E2E"
    - path: "scripts/commercial/verify-test-coverage.cjs"
      provides: "CLI gate that asserts every COM-01..06 and QA-01..15 requirement has a CONCRETE referenced test/script file (resolved via fs.existsSync) and every gate 3-6 detector exists. Supports --enumerate-qa flag that emits 15 rows for QA-01..15."
    - path: "test/commercial/governed-mutation.test.js"
      provides: "Detector for Gate 3 — every commercial mutation passes through task/approval/evidence/audit/AgentRun. HARD FAIL when target engine API dirs missing."
    - path: "test/commercial/passive-system-detector.test.js"
      provides: "Detector for Gate 4 — CDP/CRM/analytics/ecosystem/sales must produce tasks/alerts/decisions. HARD FAIL when target dashboard dirs missing."
    - path: "test/commercial/pricing-bypass-detector.test.js"
      provides: "Detector for Gate 5 — every pricing-sensitive surface routes through Pricing Engine or sanctioned `{{MARKOS_PRICING_ENGINE_PENDING}}` placeholder. HARD FAIL when target dirs missing."
    - path: "test/commercial/evidence-freshness-detector.test.js"
      provides: "Detector for Gate 6 — every external claim is evidence-linked + freshness-aware. HARD FAIL when target dirs missing."
    - path: ".planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md"
      provides: "Extended with `## Phase 228 Cross-Engine Detection Matrix` section appended at end (do not modify earlier sections); 21 individual requirement rows (6 COM + 15 QA) + 10 gate rows; every test_path concrete and fs-resolvable"
    - path: ".storybook/stories/commercial/{approval-queues,launch-cockpits,audience-activation,deal-rooms,partner-consoles}.stories.tsx"
      provides: "5 Storybook story files for D-24 Chromatic coverage targets — one per surface family (approval queues, launch cockpits, audience activation, deal rooms, partner consoles)"
    - path: ".env.example"
      provides: "CHROMATIC_PROJECT_TOKEN placeholder + comment pointing to Chromatic dashboard for token retrieval"
  key_links:
    - from: "tests/e2e/commercial-evidence-chain.spec.ts"
      to: "lib/markos/contracts/commercial-registry.json"
      via: "Playwright suite asserts every F-ID with `mcp_status === 'shipped'` is reachable from one operator journey"
      pattern: "commercial-registry"
    - from: "scripts/commercial/verify-test-coverage.cjs"
      to: ".planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md"
      via: "CLI parses appended Phase 228 section; checks each requirement+detector has a referenced test file path that exists via fs.existsSync"
      pattern: "Phase 228 Cross-Engine Detection Matrix"
    - from: "test/commercial/pricing-bypass-detector.test.js"
      to: "obsidian/brain/Pricing Engine Canon.md"
      via: "detector enforces `{{MARKOS_PRICING_ENGINE_PENDING}}` placeholder rule per Pricing Engine Canon"
      pattern: "MARKOS_PRICING_ENGINE_PENDING"
    - from: ".storybook/stories/commercial/*.stories.tsx"
      to: "obsidian/reference/UI Components.md (D-24 surface families)"
      via: "stories shape Chromatic snapshots for the 5 governed-mutation surfaces"
      pattern: "approval-queues|launch-cockpits|audience-activation|deal-rooms|partner-consoles"
---

<objective>
Close Gate 10 (testability is part of design) and provide enforcement coverage for Gates 3 (governed mutation), 4 (no passive systems), 5 (no pricing bypasses), and 6 (no unsupported external claim). This slice introduces Vitest + Playwright + Chromatic dependencies (research line 25-26 confirmed neither vitest nor playwright exist yet; D-24 mandates chromatic), wires the cross-phase test-coverage gate, and ships the SINGLE Playwright suite that proves the full commercial evidence chain in one run (per CONTEXT specifics line 153, D-23, D-29).

**Phase-level dependency note (per S1 + CONTEXT D-01):** Phase 228 as a whole assumes phases 221-227 are landed (per ROADMAP `Depends on: Phases 221-227`). The detectors fail-closed (hard exit 1) when scan target directories are missing — this is intentional. A missing `app/api/v1/{cdp,crm,email,messaging,conversion,launch,analytics,sales,ecosystem}/` directory means an upstream engine has not shipped, which per CONTEXT D-01 routes the gap back to that upstream phase. Phase 228 cannot legitimately ship green over absent surfaces. The detectors do NOT emit `coverage_pending` warnings — they hard-fail with an error message naming the upstream phase that owns the gap.

Purpose: Without Vitest + Playwright + Chromatic, the doctrine in `.planning/V4.0.0-TESTING-ENVIRONMENT-PLAN.md` is aspirational. This slice makes it executable. The detectors for gates 3-6 turn each parity gate into a runnable test instead of a checklist. Per CONTEXT non-negotiable, the test matrix is EXTENDED (appended), not forked. Per CONTEXT D-29 + RM1 fix: the evidence-chain suite is the single artifact that proves Gate 9 + Gate 10 in one go — it MUST run as part of slice 04 acceptance. **Slice 04 verify command brings up the dev server, runs the full E2E suite, and exits 0** (this resolves the prior contradiction where Task 2 deferred E2E to slice 05). The full E2E does NOT defer to slice 05 — slice 05 simply re-runs it as part of readiness aggregation.

Output: vitest + playwright + chromatic installed and configured, evidence-chain E2E suite + contract spec, 4 hard-failing gate detectors, test-coverage CLI gate with 15-individual-QA enumeration, appended test matrix with concrete test_paths, 5 Chromatic stories.
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/228-commercial-os-integration-future-readiness/228-CONTEXT.md
@.planning/phases/228-commercial-os-integration-future-readiness/228-RESEARCH.md
@.planning/phases/228-commercial-os-integration-future-readiness/228-01-PLAN.md
@.planning/V4.2.0-INCOMING-18-26-GSD-DISCUSS-RESEARCH-ROUTING.md
@.planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md
@.planning/V4.0.0-TESTING-ENVIRONMENT-PLAN.md
@.planning/REQUIREMENTS.md
@obsidian/brain/Pricing Engine Canon.md
@package.json
@.storybook/main.ts

<interfaces>
<!-- The executor adds new test infrastructure without forking existing matrix. -->

Required `package.json` script additions (devDependency installs handled by Task 1):
```json
"scripts": {
  "test": "node --test test/**/*.test.js",                              // unchanged
  "test:vitest": "vitest run --config vitest.config.ts",
  "test:vitest:watch": "vitest --config vitest.config.ts",
  "test:playwright": "playwright test",
  "test:playwright:commercial": "playwright test tests/e2e/commercial-*.spec.ts",
  "test:commercial:coverage": "node scripts/commercial/verify-test-coverage.cjs",
  "test:commercial:gates": "node --test test/commercial/governed-mutation.test.js test/commercial/passive-system-detector.test.js test/commercial/pricing-bypass-detector.test.js test/commercial/evidence-freshness-detector.test.js",
  "commercial:chromatic": "chromatic --exit-zero-on-changes",
  "commercial:chromatic:dry": "chromatic --dry-run --exit-zero-on-changes"
}
```

Required `package.json` devDependency additions:
- `vitest` ^1.6.0 (or current latest stable in node 22 line)
- `@vitest/coverage-v8` ^1.6.0
- `@playwright/test` ^1.45.0 (or current latest stable)
- `chromatic` ^11.0.0 (latest stable; supports `chromatic --dry-run`)

Vitest config (vitest.config.ts):
```ts
import { defineConfig } from 'vitest/config';
export default defineConfig({
  test: {
    include: ['test/vitest/**/*.spec.ts', 'test/commercial/**/*.spec.ts'],
    exclude: ['test/**/*.test.js'],   // legacy node --test files stay separate
    environment: 'node',
    globals: false,
    coverage: { provider: 'v8', reporter: ['text','json','html'], exclude: ['scripts/**','test/**'] },
  },
});
```

Playwright config (playwright.config.ts):
```ts
import { defineConfig, devices } from '@playwright/test';
export default defineConfig({
  testDir: './tests/e2e',
  fullyParallel: false,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  reporter: [['list'], ['html', { open: 'never' }]],
  use: {
    baseURL: process.env.PLAYWRIGHT_BASE_URL || 'http://localhost:3000',
    trace: 'on-first-retry',
  },
  // webServer is omitted by default; slice 04 verify command brings up dev server externally
  // to keep the config flexible across local/CI environments
  projects: [{ name: 'chromium', use: { ...devices['Desktop Chrome'] } }],
});
```

Chromatic configuration:
- `.env.example` includes `CHROMATIC_PROJECT_TOKEN=` placeholder with comment: `# Get from https://www.chromatic.com/start (project settings)`
- Chromatic projects scope: 5 stories under `.storybook/stories/commercial/` covering D-24 surface families
- Story shape: each `.stories.tsx` exports a default with `title: 'Commercial/<SurfaceFamily>'` + at least 3 named exports per family (Empty, Populated, ApprovalRequired/ErrorState)

Evidence chain operator journey (Playwright spec — per CONTEXT specifics line 153 + D-23 + D-29):
1. Operator opens CRM workspace, selects an account
2. Sends an email through email engine (visible in CRM timeline)
3. Recipient clicks a conversion-engine landing page
4. Conversion event surfaces in analytics engine
5. Operator generates sales deal_brief from analytics
6. Ecosystem co_sell_opportunity links to that deal
7. Audit trail visible in approval inbox connecting all steps

Gates 3-6 detectors (test/commercial/*-detector.test.js) — HARD FAIL on missing target dirs:
- governed-mutation: scans `app/api/v1/{cdp,crm,email,messaging,conversion,launch,analytics,sales,ecosystem}/**/route.ts` for write endpoints; asserts each imports/calls task/approval/evidence/audit/AgentRun substrate. **If target engine dir missing → exit 1 with `MISSING_ENGINE_API_DIR:<engine>` and message naming upstream phase (221=cdp, 222=crm, 223=email/messaging, 224=conversion/launch, 225=analytics, 226=sales, 227=ecosystem)**
- passive-system-detector: scans operator surface routes (`app/dashboard/{cdp,crm,analytics,ecosystem,sales}/**`) for at least one path that emits `task` or `alert` or `decision` (not just dashboards). **If target dashboard dir missing → exit 1 with `MISSING_ENGINE_DASHBOARD_DIR:<engine>` and message naming upstream phase**
- pricing-bypass-detector: **Per D-37 (RM3 fix): AST/allowlist enforcement.** Parses TS/JS source in `app/**`, `components/**`, `lib/markos/{conversion,launch,sales,ecosystem,messaging,email}/**` (using `@typescript-eslint/parser` or `@babel/parser` — pick the lib already present in repo or add via vitest's optional deps). For each pricing-string literal (matching `/\$\d+(\.\d+)?/`, `/\d+\s*\/\s*(mo|month|year|annual)/i`, currency patterns), walks the AST upward to find the enclosing function/method call. Asserts the enclosing context is one of: ALLOWLISTED accessors (`getPricingFor`, `formatPrice`, `usePricing`, `pricingFromEngine`, `PricingEngine.format`) OR is the literal `{{MARKOS_PRICING_ENGINE_PENDING}}` placeholder. Pricing literals reached through any other path fail. Cross-file/cross-module assembly is caught because the AST walks the actual call expression, not just the 5-line text neighborhood. **If target lib dirs missing → exit 1 with `MISSING_PRICING_SCOPE_DIR:<engine>` and message naming upstream phase**.
- evidence-freshness-detector: scans claim-emitting paths (battlecards, proof packs, launch announcements, ecosystem listings, weekly narrative); asserts each reads from EvidenceMap with TTL/freshness check. **If target dirs missing → exit 1 with `MISSING_EVIDENCE_SCOPE_DIR:<engine>` and message naming upstream phase**

QA-01..15 individual operationalizations (per REQUIREMENTS.md line 38 + V4.0.0-TESTING-ENVIRONMENT-PLAN.md):
- QA-01 (test substrate exists)              → `test/commercial/test-coverage-matrix.test.js`
- QA-02 (vitest harness)                      → `test/vitest/commercial-contracts.spec.ts`
- QA-03 (playwright harness)                  → `tests/e2e/commercial-evidence-chain.contract.spec.ts`
- QA-04 (governed-mutation gate)              → `test/commercial/governed-mutation.test.js`
- QA-05 (passive-system gate)                 → `test/commercial/passive-system-detector.test.js`
- QA-06 (pricing-bypass gate)                 → `test/commercial/pricing-bypass-detector.test.js`
- QA-07 (evidence-freshness gate)             → `test/commercial/evidence-freshness-detector.test.js`
- QA-08 (provider replaceability)             → `test/commercial/adapter-swap.test.js`
- QA-09 (CI-aware policy on detectors)        → `test/integration/qa-09-ci-aware-policy.test.js`
- QA-10 (parity matrix machine-verifiable)    → `test/commercial/parity-matrix.test.js`
- QA-11 (migration round-trip per engine)     → `test/commercial/migration-roundtrip.test.js`
- QA-12 (archival recovery per engine)        → `test/commercial/archival-recovery.test.js`
- QA-13 (operability — coverage CLI runs)     → `test/integration/qa-13-operability.test.js`
- QA-14 (chromatic visual coverage)           → `scripts/commercial/verify-chromatic-coverage.cjs`
- QA-15 (readiness matrix machine-verifiable) → `scripts/commercial/verify-readiness-matrix.cjs --qa-id QA-15`

Slice 04 owns the creation of: QA-01..04, QA-05..07, QA-09, QA-13, QA-14 paths. Slice 02 owns QA-08. Slices 02/03 own QA-10..12 (already created in their test files). Slice 05 owns QA-15.

Chromatic stories (D-24 surface families — required for slice 04 closure):
- approval-queues.stories.tsx: Operator approval inbox; states: Empty, Populated, ApprovalRequired
- launch-cockpits.stories.tsx: Launch plan + rollback cockpit; states: Planning, InFlight, RolledBack
- audience-activation.stories.tsx: CDP audience targeting; states: Empty, Sized, Activated
- deal-rooms.stories.tsx: Sales deal workspace; states: Open, AwaitingApproval, ProofRequired
- partner-consoles.stories.tsx: Ecosystem partner workspace; states: Pending, Certified, Suspended

Each story uses minimal placeholder JSX (`<div>State: <state></div>`) — slice 04's job is shape the Chromatic surface, not finalize visuals; visual finalization belongs to upstream engine phases.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install vitest + playwright + chromatic + add package.json scripts + write configs + install Playwright chromium</name>
  <files>
    package.json,
    vitest.config.ts,
    playwright.config.ts,
    package-lock.json,
    .env.example
  </files>
  <read_first>
    - package.json (current scripts + dependencies sections — full file)
    - package-lock.json (current lockfile — note format)
    - .env.example (current placeholders)
    - .planning/V4.0.0-TESTING-ENVIRONMENT-PLAN.md (lines 16-26: doctrine "no vitest dependency or vitest.config.* exists yet")
    - .planning/phases/228-commercial-os-integration-future-readiness/228-CONTEXT.md (D-22..24 test doctrine bindings — vitest, playwright, chromatic all required)
  </read_first>
  <action>
    1. Modify `package.json` — add to `devDependencies` (alphabetical):
       - `"@playwright/test": "^1.45.0"`
       - `"@vitest/coverage-v8": "^1.6.0"`
       - `"chromatic": "^11.0.0"`
       - `"vitest": "^1.6.0"`
       Add to `scripts` (preserve existing — append the 8 new scripts from `<interfaces>` block).

    2. Run `npm install` to update `package-lock.json`. (If npm registry unreachable, document failure but the contract MUST be checked in.)

    3. **Run `npx playwright install chromium --with-deps`** — chromium binary install is non-optional for slice 04 acceptance (per S3, fix option (a)). Verify install succeeded by running `npx playwright --version` and asserting it prints a version. If the binary download fails in offline env, this slice cannot complete — the install belongs HERE, not in slice 06.

    4. Create `vitest.config.ts` with content per `<interfaces>` block.

    5. Create `playwright.config.ts` with content per `<interfaces>` block.

    6. Modify `.env.example` (or create if missing) — add at bottom:
       ```
       # Chromatic visual regression (D-24 governed-mutation surfaces)
       # Get from https://www.chromatic.com/start → project settings → manage → token
       CHROMATIC_PROJECT_TOKEN=
       ```

    7. Verify config files parse: `npx vitest --version` and `npx playwright --version` both exit 0.

    DO NOT modify the existing `"test": "node --test test/**/*.test.js"` script — legacy tests stay (per testing-env-plan line 14-15).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const need=['vitest','@vitest/coverage-v8','@playwright/test','chromatic']; for(const d of need){if(!p.devDependencies[d]){console.error('missing devDep',d); process.exit(1);}} const scripts=['test:vitest','test:playwright','test:playwright:commercial','test:commercial:coverage','test:commercial:gates','commercial:chromatic','commercial:chromatic:dry']; for(const s of scripts){if(!p.scripts[s]){console.error('missing script',s); process.exit(1);}} if(p.scripts.test!=='node --test test/**/*.test.js'){console.error('legacy test script changed'); process.exit(1);} console.log('OK');" && npx playwright --version && npx vitest --version</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `devDependencies` contains `vitest`, `@vitest/coverage-v8`, `@playwright/test`, `chromatic`
    - `package.json` `scripts` contains `test:vitest`, `test:vitest:watch`, `test:playwright`, `test:playwright:commercial`, `test:commercial:coverage`, `test:commercial:gates`, `commercial:chromatic`, `commercial:chromatic:dry`
    - `package.json` `scripts.test` equals `"node --test test/**/*.test.js"` (legacy unchanged)
    - File exists: `vitest.config.ts`
    - File exists: `playwright.config.ts`
    - `.env.example` contains `CHROMATIC_PROJECT_TOKEN=` line
    - `npx playwright --version` exits 0 (proves install)
    - `npx vitest --version` exits 0 (proves install)
    - `grep -c "test/vitest/\\*\\*\\|test/commercial/\\*\\*" vitest.config.ts` returns >= 1
    - `grep -c "tests/e2e" playwright.config.ts` returns >= 1
    - `grep -c "PLAYWRIGHT_BASE_URL" playwright.config.ts` returns >= 1
    - Chromium binary resolvable: `npx playwright install --dry-run chromium` exits 0 OR ls of `~/.cache/ms-playwright/chromium-*` (or platform-equivalent) returns at least one entry
  </acceptance_criteria>
  <done>
    Vitest + Playwright + Chromatic wired in package.json, configs exist, Playwright chromium binary installed, .env.example has CHROMATIC_PROJECT_TOKEN placeholder, legacy test script preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement gates 3-6 detectors (HARD FAIL on missing dirs) + commercial evidence-chain Playwright suites (E2E + contract) + Chromatic stories</name>
  <files>
    test/commercial/governed-mutation.test.js,
    test/commercial/passive-system-detector.test.js,
    test/commercial/pricing-bypass-detector.test.js,
    test/commercial/evidence-freshness-detector.test.js,
    tests/e2e/commercial-evidence-chain.spec.ts,
    tests/e2e/commercial-evidence-chain.contract.spec.ts,
    tests/e2e/fixtures/commercial-evidence-chain.json,
    test/vitest/commercial-contracts.spec.ts,
    .storybook/stories/commercial/approval-queues.stories.tsx,
    .storybook/stories/commercial/launch-cockpits.stories.tsx,
    .storybook/stories/commercial/audience-activation.stories.tsx,
    .storybook/stories/commercial/deal-rooms.stories.tsx,
    .storybook/stories/commercial/partner-consoles.stories.tsx
  </files>
  <read_first>
    - .planning/V4.2.0-INCOMING-18-26-GSD-DISCUSS-RESEARCH-ROUTING.md lines 113-126 (Gates 3-6 verbatim)
    - .planning/phases/228-commercial-os-integration-future-readiness/228-CONTEXT.md (D-09..12 gates 3-6, D-23 evidence-chain Playwright single suite, D-24 Chromatic 5 surface families, D-01 route-back rule, D-29 evidence trace end-to-end)
    - obsidian/brain/Pricing Engine Canon.md (Gate 5 placeholder rule)
    - .planning/phases/221-cdp-identity-audience-consent-substrate/221-CONTEXT.md (cdp surfaces for governed-mutation scan)
    - .planning/phases/222-crm-timeline-commercial-memory-workspace/222-CONTEXT.md (crm timeline write paths)
    - .planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md (email/messaging governed dispatch)
    - .planning/phases/226-sales-enablement-deal-execution/226-CONTEXT.md (deal_room evidence emit + ProofPack freshness)
    - .planning/phases/227-ecosystem-partner-community-developer-growth/227-CONTEXT.md (ecosystem listing freshness + community signal routing)
    - lib/markos/contracts/commercial-registry.json (slice 01 — F-ID list)
    - .storybook/main.ts (existing storybook config)
  </read_first>
  <behavior>
    - Detector 1 (governed-mutation): scans `app/api/v1/{cdp,crm,email,messaging,conversion,launch,analytics,sales,ecosystem}/**/route.ts` for write methods (POST/PATCH/DELETE/PUT). For each write route, asserts the route file imports OR references at least one of: `createTask`, `requireApproval`, `recordEvidence`, `appendAuditLog`, `enqueueAgentRun`. **If a target engine dir does NOT exist (i.e., upstream phase 221-227 has not landed), the detector exits 1 with `MISSING_ENGINE_API_DIR:<engine>` and stderr message "upstream phase <NNN> must ship `app/api/v1/<engine>/` before Phase 228 can verify Gate 3 for this engine — route gap back to phase <NNN> per CONTEXT D-01".** No `coverage_pending` translation.
    - Detector 2 (passive-system): scans `app/dashboard/{cdp,crm,analytics,ecosystem,sales}/**/page.tsx` (or `.jsx`/`.ts`); for each engine, asserts at least one page emits `Task` or `Alert` or `Decision` component. **If target dashboard dir missing → exit 1 with `MISSING_ENGINE_DASHBOARD_DIR:<engine>` and upstream-phase route-back message.**
    - Detector 3 (pricing-bypass): greps `app/**`, `components/**`, `lib/markos/{conversion,launch,sales,ecosystem,messaging,email}/**`. Excludes test files and `{{MARKOS_PRICING_ENGINE_PENDING}}` placeholder context. **If target `lib/markos/<engine>/` dir missing for ANY of the 6 pricing-sensitive engines → exit 1 with `MISSING_PRICING_SCOPE_DIR:<engine>` and route-back message.**
    - Detector 4 (evidence-freshness): scans `lib/markos/{sales,launch,ecosystem,analytics}/**`. **If target dirs missing → exit 1 with `MISSING_EVIDENCE_SCOPE_DIR:<engine>` and route-back message.**
    - Vitest spec (commercial-contracts.spec.ts): asserts the contract registry from slice 01 is loadable via Vitest, asserts shape stability of one entry per engine (9 assertions + total count = 10 tests).
    - Playwright E2E suite (commercial-evidence-chain.spec.ts): uses fixture data from `tests/e2e/fixtures/commercial-evidence-chain.json`; mocks external providers; walks the 7-step operator journey from `<interfaces>`. **NO test.skip env-gating.** The slice 04 verify command brings up the dev server, runs this suite, and exits 0 — it is NOT skipped.
    - Playwright contract spec (commercial-evidence-chain.contract.spec.ts): selector/route reachability stubs that always run. Loads the fixture, asserts every selector + route declared in the fixture is structurally valid (regex check on selector strings, URL parse on routes). Does NOT spawn a browser — runs as fast static validation. Catches selector drift before E2E.
    - Chromatic stories: 5 placeholder stories under `.storybook/stories/commercial/` per D-24 surface families. Each story exports default + 3 states. Stories use trivial JSX placeholders (slice 04 ships the surface family contract, not the visual implementation).
  </behavior>
  <action>
    Create `test/commercial/governed-mutation.test.js` using `node --test`. Use `node:fs` + `node:path` to walk `app/api/v1/{engine}/`. For each engine in `['cdp','crm','email','messaging','conversion','launch','analytics','sales','ecosystem']`, **first check if `app/api/v1/<engine>/` exists**. If NOT, fail the test with assert.fail(`MISSING_ENGINE_API_DIR:<engine> — upstream phase <NNN> must ship app/api/v1/<engine>/ before Phase 228 can verify Gate 3. Route gap back to phase <NNN> per CONTEXT D-01.`). Engine→phase map: cdp=221, crm=222, email=223, messaging=223, conversion=224, launch=224, analytics=225, sales=226, ecosystem=227. If dir exists, walk it for `route.ts` files containing exported `POST|PATCH|PUT|DELETE` and assert each contains at least one of: `'createTask'`, `'requireApproval'`, `'recordEvidence'`, `'appendAuditLog'`, `'enqueueAgentRun'`, `'agent_runs'`, `'audit_log'`. Allowlist: routes under `/v1/{engine}/health` or `/v1/{engine}/metrics`.

    Create `test/commercial/passive-system-detector.test.js` using `node --test`. Walk `app/dashboard/{cdp,crm,analytics,ecosystem,sales}/`. For each engine, **first check dir existence**. Missing → assert.fail(`MISSING_ENGINE_DASHBOARD_DIR:<engine> — upstream phase <NNN> must ship app/dashboard/<engine>/ before Phase 228 can verify Gate 4. Route gap back to phase <NNN> per CONTEXT D-01.`). Engine→phase map: cdp=221, crm=222, analytics=225, ecosystem=227, sales=226. If dir exists, assert at least one `page.tsx`/`page.ts`/`page.jsx` contains a reference to `<Task|<Alert|<Decision|useTask\\(|useDecision\\(|notifyTask\\(`.

    Create `test/commercial/pricing-bypass-detector.test.js` using `node --test`. **First check existence of `lib/markos/conversion/`, `lib/markos/launch/`, `lib/markos/sales/`, `lib/markos/ecosystem/`, `lib/markos/messaging/`, `lib/markos/email/`. Any missing → assert.fail(`MISSING_PRICING_SCOPE_DIR:<engine> — upstream phase <NNN> must ship lib/markos/<engine>/ before Phase 228 can verify Gate 5. Route gap back to phase <NNN> per CONTEXT D-01.`).** Engine→phase map: conversion=224, launch=224, sales=226, ecosystem=227, messaging=223, email=223.

    **Per D-37 (RM3 fix): AST/allowlist enforcement, NOT 5-line regex heuristic.** Use `@babel/parser` (or `@typescript-eslint/parser` if already in repo deps; check package.json before adding) to parse each `.ts`/`.tsx`/`.js`/`.jsx` file under target dirs + `app/**` + `components/**`. Exclude `**/*.test.{js,ts}`, `**/*.stories.{ts,tsx}`, `node_modules/**`, `.next/**`.

    For each parsed AST:
    1. Visit every `StringLiteral` and `TemplateLiteral` node.
    2. If the literal value matches any pricing-string regex (`/\$\d+(\.\d+)?/`, `/\d+\s*\/\s*(mo|month|year|annual|hour)/i`, `/\b(USD|EUR|GBP)\s*\d/`, `/\bdiscount\s*[:=]\s*\d/i`, `/\bcommission\s*[:=]\s*\d/i`):
       - If the literal value IS the exact string `{{MARKOS_PRICING_ENGINE_PENDING}}` → ALLOWED (sanctioned placeholder per Pricing Engine Canon).
       - Otherwise, walk the AST upward (parent chain) to find the enclosing CallExpression or MemberExpression. The enclosing call MUST match one of the ALLOWLISTED accessors:
         ```js
         const ALLOWLISTED_ACCESSORS = [
           'getPricingFor', 'formatPrice', 'usePricing', 'pricingFromEngine',
           'PricingEngine.format', 'PricingEngine.getPricingFor', 'PricingEngine.usePricing'
         ];
         ```
       - If the enclosing call is NOT allowlisted → record violation `{ file, line, literal, enclosing_call }`.
    3. Cross-file/cross-module assembly: if a pricing literal is exported (e.g., `export const PRICE_TIER_A = '$99/mo'`) AND the export is consumed elsewhere outside an allowlisted accessor, that consumption fails the check. Track export→import chains during the walk.

    Fail with `code: 'PRICING_BYPASS'` if any violations. The 5-line text neighborhood check is REPLACED by AST traversal. Document the chosen parser (`@babel/parser` vs `@typescript-eslint/parser`) in the detector header comment + reference D-37.

    If neither parser is available in repo deps, the detector adds `@babel/parser` as a devDependency in Task 1's package.json edit (Task 1 already adds vitest etc.; add `@babel/parser` ^7.0.0 if not present).

    Create `test/commercial/evidence-freshness-detector.test.js` using `node --test`. **First check existence of `lib/markos/sales/`, `lib/markos/launch/`, `lib/markos/ecosystem/`, `lib/markos/analytics/`. Any missing → assert.fail(`MISSING_EVIDENCE_SCOPE_DIR:<engine> — upstream phase <NNN> must ship lib/markos/<engine>/ before Phase 228 can verify Gate 6. Route gap back to phase <NNN>.`).** Engine→phase map: sales=226, launch=224, ecosystem=227, analytics=225. Then walk those dirs; for each file containing `\\b(claim|proof|case_study|roi|competitor|announcement)\\b`, assert it also references `evidenceMap|evidence_map|freshness|ttl_seconds|expires_at|recorded_at`. Fail with `code: 'CLAIM_WITHOUT_FRESHNESS'` on any violations.

    Create `test/vitest/commercial-contracts.spec.ts` using vitest:
    ```ts
    import { describe, it, expect } from 'vitest';
    import registry from '../../lib/markos/contracts/commercial-registry.json';
    describe('commercial registry contract shape', () => {
      it.each(['cdp','crm','email','messaging','conversion','launch','analytics','sales','ecosystem'])('engine %s has at least one entry', (engine) => {
        expect(registry.filter((e: any) => e.engine === engine).length).toBeGreaterThan(0);
      });
      it('total entries >= 45', () => expect(registry.length).toBeGreaterThanOrEqual(45));
    });
    ```

    Create `tests/e2e/fixtures/commercial-evidence-chain.json` with seeded scenario data (account_id, person_id, opportunity_id, deal_brief_id, listing_id, expected timeline events, expected evidence chain links, list of selectors used in the journey, list of routes navigated).

    Create `tests/e2e/commercial-evidence-chain.spec.ts` using Playwright (NO env-gating skip):
    ```ts
    import { test, expect } from '@playwright/test';
    import fixture from './fixtures/commercial-evidence-chain.json';

    test.describe('commercial evidence chain (CRM → channel → conversion → analytics → sales → ecosystem)', () => {
      test('walks 7-step operator journey end-to-end with audit trail', async ({ page }) => {
        // Step 1: open CRM workspace
        await page.goto(`/dashboard/crm/accounts/${fixture.account_id}`);
        await expect(page.getByRole('heading', { name: /timeline/i })).toBeVisible();
        // Steps 2-7: per <interfaces> 7-step journey...
        // Final assertion: approval inbox shows audit trail with all 6 stage references
        await page.goto('/dashboard/approvals');
        for (const stage of ['crm','email','conversion','analytics','sales','ecosystem']) {
          await expect(page.getByTestId(`audit-stage-${stage}`)).toBeVisible();
        }
      });
    });
    ```

    Create `tests/e2e/commercial-evidence-chain.contract.spec.ts` using Playwright (always runs — no skip):
    ```ts
    import { test, expect } from '@playwright/test';
    import fixture from './fixtures/commercial-evidence-chain.json';

    test.describe('commercial evidence chain — contract spec (selectors + routes)', () => {
      test('every fixture selector parses as a valid CSS/role/testid selector', () => {
        for (const sel of fixture.selectors ?? []) {
          // Reject empty / malformed selectors before E2E spends browser time
          expect(typeof sel.selector).toBe('string');
          expect(sel.selector.length).toBeGreaterThan(0);
        }
      });
      test('every fixture route is a valid URL path starting with /', () => {
        for (const route of fixture.routes ?? []) {
          expect(route).toMatch(/^\//);
        }
      });
      test('fixture references all 6 evidence-chain stages', () => {
        const stages = ['crm','email','conversion','analytics','sales','ecosystem'];
        const fixtureText = JSON.stringify(fixture);
        for (const stage of stages) {
          expect(fixtureText).toContain(stage);
        }
      });
    });
    ```

    Create 5 Storybook story files under `.storybook/stories/commercial/`. **Per RM2 / D-36: each story MUST import the real upstream component / view-model from a non-`.storybook/` path. Placeholder JSX is FORBIDDEN.** If the upstream component does not exist, slice 04 hard-fails with `MISSING_UPSTREAM_COMPONENT:<component>` and routes back to the upstream phase per D-01.

    Real-upstream component bindings (per D-24 surface families):
    - `approval-queues.stories.tsx` — title `'Commercial/Approval Queues'`. **Imports `ApprovalQueue` from `app/(operator)/approvals/approval-queue.tsx`** (or wherever upstream phase ships it). Exports: `Empty`, `Populated`, `ApprovalRequired` — each renders `<ApprovalQueue items={fixtureFor(state)} />`. Upstream owners: phases 221-227 (cross-cutting; primary owner = 226 sales + 227 ecosystem + 224 launch + 221 cdp).
    - `launch-cockpits.stories.tsx` — title `'Commercial/Launch Cockpits'`. **Imports `LaunchCockpit` from `app/dashboard/launch/cockpit.tsx`** (phase 224 owner). Exports: `Planning`, `InFlight`, `RolledBack`.
    - `audience-activation.stories.tsx` — title `'Commercial/Audience Activation'`. **Imports `AudienceActivation` from `app/dashboard/cdp/audience-activation.tsx`** (phase 221 owner). Exports: `Empty`, `Sized`, `Activated`.
    - `deal-rooms.stories.tsx` — title `'Commercial/Deal Rooms'`. **Imports `DealRoom` from `app/dashboard/sales/deal-room.tsx`** (phase 226 owner). Exports: `Open`, `AwaitingApproval`, `ProofRequired`.
    - `partner-consoles.stories.tsx` — title `'Commercial/Partner Consoles'`. **Imports `PartnerConsole` from `app/dashboard/ecosystem/partner-console.tsx`** (phase 227 owner). Exports: `Pending`, `Certified`, `Suspended`.

    **Hard precondition (per D-36)**: each story file BEFORE rendering imports the upstream component. If `fs.existsSync` for the imported source path fails at story-build time (TypeScript module resolution will fail at compile time, which Chromatic will report as a build error), slice 04 routes back to the upstream phase that owns that component. **No placeholder `<div>` rendering as a substitute** — that was the RM2 anti-pattern.

    The story file's fixture data (`fixtureFor(state)`) is allowed to be local synthetic data — only the rendered component MUST be real.

    **IMPORTANT (per RM1 fix):** The slice 04 verify command runs the FULL evidence-chain E2E + 4 gate detectors against a running dev server. There is no "defer to slice 05" pathway. Per CONTEXT D-01 hard precondition, if upstream engine dirs (`app/api/v1/{engine}/`, `app/dashboard/{engine}/`, `lib/markos/{engine}/`) are missing, the detectors hard-fail with `MISSING_ENGINE_*_DIR` and slice 04 routes back to the upstream phase. Slice 04 cannot ship green over absent surfaces — that is the correct failure mode.

    For slice 04 acceptance, the verify command runs (per RM1 — full E2E in slice 04, not deferred):
    1. **Engine-dir precondition assertion**: `node scripts/commercial/precondition-engine-dirs.cjs` (created by Plan 01 Task 0). Exit 1 if any engine dir missing → slice 04 halts with route-back per D-01.
    2. `npx vitest run test/vitest/commercial-contracts.spec.ts` (must exit 0)
    3. `npx playwright test tests/e2e/commercial-evidence-chain.contract.spec.ts --reporter=line` (always-run contract spec, must exit 0)
    4. **Bring up dev server** (e.g., `npm run dev &` in background, with health-check loop until http://localhost:3000 responds OR timeout 60s). Slice 04 owns dev-server bringup; slice 05 reuses the same step. If dev server fails to start → exit 1 with diagnostic.
    5. **`npx playwright test tests/e2e/commercial-evidence-chain.spec.ts --reporter=line` (FULL E2E suite, must exit 0)** — per RM1, this runs in slice 04 acceptance, not slice 05.
    6. `node --test test/commercial/governed-mutation.test.js test/commercial/passive-system-detector.test.js test/commercial/pricing-bypass-detector.test.js test/commercial/evidence-freshness-detector.test.js` (4 detectors, must exit 0).
    7. `npm run commercial:chromatic:dry` (chromatic --dry-run, must exit 0).
    8. Tear down dev server (`kill $DEV_SERVER_PID`).

    Slice 05 readiness aggregation REUSES this same sequence (re-runs to populate the matrix), but the canonical proof of Gate 9 + Gate 10 lives in slice 04 verify per CONTEXT D-29 + RM1.
  </action>
  <verify>
    <automated>node scripts/commercial/precondition-engine-dirs.cjs && npx vitest run test/vitest/commercial-contracts.spec.ts && npx playwright test tests/e2e/commercial-evidence-chain.contract.spec.ts --reporter=line && (npm run dev > /tmp/dev-server.log 2>&1 &) && DEV_PID=$! && for i in $(seq 1 30); do sleep 2; curl -sf http://localhost:3000/api/health 2>/dev/null && break; done && npx playwright test tests/e2e/commercial-evidence-chain.spec.ts --reporter=line && node --test test/commercial/governed-mutation.test.js test/commercial/passive-system-detector.test.js test/commercial/pricing-bypass-detector.test.js test/commercial/evidence-freshness-detector.test.js && npm run commercial:chromatic:dry && kill $DEV_PID 2>/dev/null; ls .storybook/stories/commercial/*.stories.tsx | wc -l | grep -q 5</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test/commercial/governed-mutation.test.js`
    - File exists: `test/commercial/passive-system-detector.test.js`
    - File exists: `test/commercial/pricing-bypass-detector.test.js`
    - File exists: `test/commercial/evidence-freshness-detector.test.js`
    - File exists: `test/vitest/commercial-contracts.spec.ts`
    - File exists: `tests/e2e/commercial-evidence-chain.spec.ts`
    - File exists: `tests/e2e/commercial-evidence-chain.contract.spec.ts`
    - File exists: `tests/e2e/fixtures/commercial-evidence-chain.json`
    - 5 files exist under `.storybook/stories/commercial/`: approval-queues.stories.tsx, launch-cockpits.stories.tsx, audience-activation.stories.tsx, deal-rooms.stories.tsx, partner-consoles.stories.tsx
    - `npx vitest run test/vitest/commercial-contracts.spec.ts` exits 0 with 10 tests passing
    - `npx playwright test tests/e2e/commercial-evidence-chain.contract.spec.ts` exits 0 (always-run contract spec)
    - `npm run commercial:chromatic:dry` exits 0 (dry-run mode does not require token)
    - `grep -c "MISSING_ENGINE_API_DIR" test/commercial/governed-mutation.test.js` returns >= 1
    - `grep -c "MISSING_ENGINE_DASHBOARD_DIR" test/commercial/passive-system-detector.test.js` returns >= 1
    - `grep -c "MISSING_PRICING_SCOPE_DIR" test/commercial/pricing-bypass-detector.test.js` returns >= 1
    - `grep -c "MISSING_EVIDENCE_SCOPE_DIR" test/commercial/evidence-freshness-detector.test.js` returns >= 1
    - `grep -c "Route gap back" test/commercial/governed-mutation.test.js` returns >= 1 (route-back message present)
    - `grep -c "test.skip" tests/e2e/commercial-evidence-chain.spec.ts` returns 0 (NO env-gating skip on E2E suite — per B1 fix option (b))
    - `grep -c "audit-stage-" tests/e2e/commercial-evidence-chain.spec.ts` returns >= 6 (asserts every stage in evidence chain)
    - `grep -c "MARKOS_PRICING_ENGINE_PENDING" test/commercial/pricing-bypass-detector.test.js` returns >= 1
    - **Per D-37 (RM3 fix): pricing detector uses AST/allowlist enforcement, NOT 5-line regex:**
      - `grep -c "@babel/parser\|@typescript-eslint/parser\|require.*parser" test/commercial/pricing-bypass-detector.test.js` returns >= 1 (proves AST library used)
      - `grep -c "ALLOWLISTED_ACCESSORS\|allowlisted\|getPricingFor\|formatPrice" test/commercial/pricing-bypass-detector.test.js` returns >= 1 (proves allowlist enforced)
      - `grep -E "within 5 lines|within \\\\d lines|context lines" test/commercial/pricing-bypass-detector.test.js | wc -l` returns 0 (proves no 5-line heuristic)
    - Each storybook file under `.storybook/stories/commercial/` exports at least 3 named stories (verify by `grep -c "export const " <file>` returning >= 3 for each)
    - **Per RM2 / D-36: each story file imports from a non-`.storybook/` path (no placeholder JSX):**
      `node -e "const fs=require('fs'); const stories=['approval-queues','launch-cockpits','audience-activation','deal-rooms','partner-consoles']; const re=/from\s+['\"]([^'\"]+)['\"]/g; for(const s of stories){ const c=fs.readFileSync('.storybook/stories/commercial/'+s+'.stories.tsx','utf8'); const imports=[]; let m; while((m=re.exec(c))){imports.push(m[1])} const nonStorybookImports=imports.filter(p=>!p.startsWith('.storybook')&&!p.startsWith('@storybook')&&!p.match(/^['\"]?(react|@types)/)); if(nonStorybookImports.length===0){console.error(s,'has no upstream-component import — RM2 violation; placeholder JSX forbidden per D-36'); process.exit(1);} } console.log('OK 5 stories import from upstream component paths');"` exits 0
    - **Per D-36 hard precondition: every imported upstream component path resolves via fs.existsSync** (note: at exec time, TypeScript module resolution will catch this — but the precondition CLI also asserts it as a defensive check):
      `node -e "const fs=require('fs'); const expected={'approval-queues':'app/(operator)/approvals/approval-queue.tsx','launch-cockpits':'app/dashboard/launch/cockpit.tsx','audience-activation':'app/dashboard/cdp/audience-activation.tsx','deal-rooms':'app/dashboard/sales/deal-room.tsx','partner-consoles':'app/dashboard/ecosystem/partner-console.tsx'}; const phaseFor={'approval-queues':'221-227 (cross)','launch-cockpits':'224','audience-activation':'221','deal-rooms':'226','partner-consoles':'227'}; const missing=[]; for(const [story,p] of Object.entries(expected)){ if(!fs.existsSync(p)){ missing.push(story+' -> '+p+' (upstream phase '+phaseFor[story]+')'); } } if(missing.length){console.error('MISSING_UPSTREAM_COMPONENT — route back per D-01:',missing); process.exit(1);} console.log('OK 5 D-24 components resolve');"` exits 0 (or exits 1 with route-back-to-upstream-phase message — that is correct failure if upstream components have not landed)
  </acceptance_criteria>
  <done>
    4 gate detectors exist (HARD FAIL on missing dirs per D-01), vitest contract spec runs green, Playwright contract spec runs green (always), **Playwright E2E suite runs in slice 04 verify against live dev server** (per RM1 — no longer deferred to slice 05), Chromatic dry-run exits 0, 5 D-24 storybook stories shipped from real upstream components (per RM2 / D-36).
  </done>
</task>

<task type="auto">
  <name>Task 3: Append Phase 228 detection matrix (21 individual rows + 10 gate rows + Chromatic column) + verify-test-coverage.cjs CLI gate with --enumerate-qa</name>
  <files>
    .planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md,
    scripts/commercial/verify-test-coverage.cjs,
    scripts/commercial/verify-chromatic-coverage.cjs,
    test/commercial/test-coverage-matrix.test.js,
    test/integration/qa-09-ci-aware-policy.test.js,
    test/integration/qa-13-operability.test.js
  </files>
  <read_first>
    - .planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md (full file — append to end, do NOT modify earlier sections)
    - .planning/V4.0.0-TESTING-ENVIRONMENT-PLAN.md (mandatory phase-close expectation lines 261-269)
    - .planning/phases/228-commercial-os-integration-future-readiness/228-CONTEXT.md (D-04 contract gaps non-negotiable, D-22..24 test doctrine bindings, D-27 F-IDs enumerated)
    - lib/markos/contracts/commercial-registry.json (slice 01)
    - .planning/REQUIREMENTS.md (COM-01..06 line 200-205, QA-01..15 line 38)
    - V4.0.0-TESTING-ENVIRONMENT-PLAN.md and V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md for canonical QA-NN definitions per phase
  </read_first>
  <action>
    1. **Create QA-09 + QA-13 test files** (referenced in `<interfaces>` QA mapping; per B4 every QA-NN must have a CONCRETE test_path that resolves):
       - `test/integration/qa-09-ci-aware-policy.test.js` (node --test) — asserts that detectors check engine-dir existence BEFORE attempting to scan, proving CI-aware fail-closed policy. Spawns `node --test test/commercial/governed-mutation.test.js`; if upstream engine dirs missing, expects exit 1 with `MISSING_ENGINE_API_DIR` in stderr. Marks the test as expecting non-zero exit when engine dirs are missing (this IS the CI-aware policy — fail-closed when upstream gap exists).
       - `test/integration/qa-13-operability.test.js` (node --test) — asserts `node scripts/commercial/verify-test-coverage.cjs --enumerate-qa` exits 0 AND emits exactly 15 QA rows AND every test_path in the output resolves via fs.existsSync. Proves operability of the test-coverage CLI.

    2. **Create `scripts/commercial/verify-chromatic-coverage.cjs`** (referenced as QA-14 path):
       - Asserts 5 files exist under `.storybook/stories/commercial/`: approval-queues.stories.tsx, launch-cockpits.stories.tsx, audience-activation.stories.tsx, deal-rooms.stories.tsx, partner-consoles.stories.tsx
       - Asserts each file has at least 3 exported stories (regex `export const \\w+`)
       - Asserts `package.json` has `commercial:chromatic` script
       - `--qa-id QA-14` flag emits a single-row JSON report: `{"qa_id":"QA-14","status":"pass","files":[...]}`
       - Exit 0 if all checks pass, exit 1 otherwise

    3. **Append Phase 228 detection matrix** to `.planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md` (DO NOT modify earlier sections — strict append). Section title: `## Phase 228 Cross-Engine Detection Matrix`. Content:

       **Table 1 — Requirement → Test row mapping (21 INDIVIDUAL rows, one per COM-01..06 + QA-01..15 — NO roll-up alias).** Columns: Requirement | Vitest must cover | Playwright must cover | Detector / verifier path | Last verified.

       Concrete rows (every test_path MUST resolve via fs.existsSync):
       ```
       | COM-01 (substrate sharing)                    | n/a                                         | tests/e2e/commercial-evidence-chain.spec.ts | scripts/commercial/verify-test-coverage.cjs                                                            | <iso> |
       | COM-02 (UI/API/MCP parity)                    | test/vitest/commercial-contracts.spec.ts    | tests/e2e/commercial-evidence-chain.spec.ts | scripts/commercial/verify-parity-matrix.cjs                                                            | <iso> |
       | COM-03 (provider replaceability)              | test/commercial/adapter-swap.test.js        | n/a                                         | scripts/commercial/test-adapter-swap.cjs                                                               | <iso> |
       | COM-04 (migration/archival)                   | test/commercial/migration-roundtrip.test.js | n/a                                         | scripts/commercial/test-migration-roundtrip.cjs + scripts/commercial/test-archival-recovery.cjs        | <iso> |
       | COM-05 (gate detector tests)                  | test/commercial/governed-mutation.test.js, test/commercial/passive-system-detector.test.js, test/commercial/pricing-bypass-detector.test.js, test/commercial/evidence-freshness-detector.test.js | tests/e2e/commercial-evidence-chain.spec.ts | scripts/commercial/verify-test-coverage.cjs                                                            | <iso> |
       | COM-06 (versioned schemas + override audit)   | test/commercial/migration-manifest-coverage.test.js | n/a                                | scripts/commercial/test-archival-recovery.cjs                                                          | <iso> |
       | QA-01 (test substrate exists)                 | n/a                                         | n/a                                         | test/commercial/test-coverage-matrix.test.js                                                           | <iso> |
       | QA-02 (vitest harness)                        | test/vitest/commercial-contracts.spec.ts    | n/a                                         | scripts/commercial/verify-test-coverage.cjs --qa-id QA-02                                              | <iso> |
       | QA-03 (playwright harness)                    | n/a                                         | tests/e2e/commercial-evidence-chain.contract.spec.ts | scripts/commercial/verify-test-coverage.cjs --qa-id QA-03                                     | <iso> |
       | QA-04 (governed-mutation gate)                | test/commercial/governed-mutation.test.js   | n/a                                         | test/commercial/governed-mutation.test.js                                                              | <iso> |
       | QA-05 (passive-system gate)                   | test/commercial/passive-system-detector.test.js | n/a                                     | test/commercial/passive-system-detector.test.js                                                        | <iso> |
       | QA-06 (pricing-bypass gate)                   | test/commercial/pricing-bypass-detector.test.js | n/a                                     | test/commercial/pricing-bypass-detector.test.js                                                        | <iso> |
       | QA-07 (evidence-freshness gate)               | test/commercial/evidence-freshness-detector.test.js | n/a                                 | test/commercial/evidence-freshness-detector.test.js                                                    | <iso> |
       | QA-08 (provider replaceability)               | test/commercial/adapter-swap.test.js        | n/a                                         | scripts/commercial/test-adapter-swap.cjs                                                               | <iso> |
       | QA-09 (CI-aware policy on detectors)          | test/integration/qa-09-ci-aware-policy.test.js | n/a                                       | test/integration/qa-09-ci-aware-policy.test.js                                                         | <iso> |
       | QA-10 (parity matrix machine-verifiable)      | test/commercial/parity-matrix.test.js       | n/a                                         | scripts/commercial/verify-parity-matrix.cjs                                                            | <iso> |
       | QA-11 (migration round-trip per engine)       | test/commercial/migration-roundtrip.test.js | n/a                                         | scripts/commercial/test-migration-roundtrip.cjs                                                        | <iso> |
       | QA-12 (archival recovery per engine)          | test/commercial/archival-recovery.test.js   | n/a                                         | scripts/commercial/test-archival-recovery.cjs                                                          | <iso> |
       | QA-13 (operability — coverage CLI runs)       | test/integration/qa-13-operability.test.js  | n/a                                         | test/integration/qa-13-operability.test.js                                                             | <iso> |
       | QA-14 (chromatic visual coverage)             | n/a                                         | n/a                                         | scripts/commercial/verify-chromatic-coverage.cjs --qa-id QA-14                                         | <iso> |
       | QA-15 (readiness matrix machine-verifiable)   | test/commercial/readiness-matrix.test.js    | n/a                                         | scripts/commercial/verify-readiness-matrix.cjs --qa-id QA-15                                           | <iso> |
       ```

       **Table 2 — Gate → Detector mapping (10 rows, one per gate 1-10):**
       - Gate 1 (one identity truth) → `lib/markos/contracts/commercial-registry-validator.js` GATE1_DUPLICATE_IDENTITY_OWNER
       - Gate 2 (one commercial memory) → GATE2_DUPLICATE_TIMELINE_OWNER
       - Gate 3 (governed mutation) → `test/commercial/governed-mutation.test.js`
       - Gate 4 (no passive systems) → `test/commercial/passive-system-detector.test.js`
       - Gate 5 (no pricing bypasses) → `test/commercial/pricing-bypass-detector.test.js`
       - Gate 6 (no unsupported claim) → `test/commercial/evidence-freshness-detector.test.js`
       - Gate 7 (no provider lock) → `scripts/commercial/test-adapter-swap.cjs`
       - Gate 8 (no designed obsolescence) → `scripts/commercial/test-migration-roundtrip.cjs` + `scripts/commercial/test-archival-recovery.cjs`
       - Gate 9 (UI/API/MCP parity) → `scripts/commercial/verify-parity-matrix.cjs`
       - Gate 10 (testability) → `scripts/commercial/verify-test-coverage.cjs` + `scripts/commercial/verify-chromatic-coverage.cjs`

       **Table 3 — Mandatory phase-close gate** (matches testing-env-plan lines 261-269 doctrine):
       Phase 228 closes only when: every requirement row in Table 1 has a non-null test reference that resolves via fs.existsSync AND every detector in Table 2 exits 0 AND `tests/e2e/commercial-evidence-chain.spec.ts` runs and passes (NOT skipped).

    4. Create `scripts/commercial/verify-test-coverage.cjs`:
       - Read the appended section by parsing the markdown (split on `## Phase 228 Cross-Engine Detection Matrix` heading, parse the three tables).
       - For each requirement row, extract every referenced file path; assert each exists via `fs.existsSync`. Missing → exit 1 with `MISSING_TEST_FILE:<requirement>:<path>`.
       - **NEW: `--enumerate-qa` flag** — outputs 15 lines (one per QA-01..15) in format `QA-NN | <path>` to stdout, AND verifies each path exists. Exit 0 if all 15 resolve, exit 1 if any does not. Used by `test/integration/qa-13-operability.test.js`.
       - **NEW: `--qa-id <id>` flag** — outputs a single QA row in JSON, verifies its path exists. Used by Table 1 cells.
       - For each gate row, resolve detector path; assert each detector test file exists.
       - `--format json` outputs `{ summary: { total_requirements, total_gates, failures }, requirements, gates }` JSON.
       - `--update-timestamps` flag rewrites each row's `Last verified` column with current ISO timestamp (only when invoked from CI after all referenced commands pass).
       - Exit 0 if all references resolve, exit 1 if any reference missing, exit 2 if matrix section missing or unparseable.

    5. Create `test/commercial/test-coverage-matrix.test.js`:
       - Test 1: matrix file contains the verbatim heading `## Phase 228 Cross-Engine Detection Matrix`
       - Test 2: matrix is parseable (CLI exit 0 in dry mode)
       - Test 3: spawn `node scripts/commercial/verify-test-coverage.cjs`, expect exit 0
       - Test 4: spawn with `--format json`, parse JSON, assert `summary.total_requirements === 21` (COM-01..06 = 6, QA-01..15 = 15)
       - Test 5: spawn with `--format json`, assert `summary.total_gates === 10`
       - Test 6: temporarily delete one detector file, spawn CLI, expect exit 1 + stderr `MISSING_TEST_FILE`
       - Test 7: spawn `--enumerate-qa`, parse stdout, assert exactly 15 lines emitted (QA-01..QA-15) and exit 0
       - Test 8: every QA path emitted by `--enumerate-qa` resolves via fs.existsSync (proves no roll-up alias and no circular reference)
       - Test 9: spawn `--qa-id QA-09`, parse JSON, assert path resolves to `test/integration/qa-09-ci-aware-policy.test.js`
       - Test 10: spawn `--qa-id QA-13`, parse JSON, assert path resolves to `test/integration/qa-13-operability.test.js`
       - Test 11: matrix earlier sections (Phase 204) unchanged: `grep -c "## Phase 204 - CLI" .planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md` returns 1
  </action>
  <verify>
    <automated>node scripts/commercial/verify-test-coverage.cjs && node scripts/commercial/verify-test-coverage.cjs --enumerate-qa | wc -l | grep -q 15 && node --test test/commercial/test-coverage-matrix.test.js && node --test test/integration/qa-09-ci-aware-policy.test.js test/integration/qa-13-operability.test.js && node scripts/commercial/verify-chromatic-coverage.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md` contains the heading `## Phase 228 Cross-Engine Detection Matrix` (verbatim)
    - File exists: `scripts/commercial/verify-test-coverage.cjs`
    - File exists: `scripts/commercial/verify-chromatic-coverage.cjs`
    - File exists: `test/commercial/test-coverage-matrix.test.js`
    - File exists: `test/integration/qa-09-ci-aware-policy.test.js`
    - File exists: `test/integration/qa-13-operability.test.js`
    - `node scripts/commercial/verify-test-coverage.cjs` exits 0
    - `node scripts/commercial/verify-test-coverage.cjs --enumerate-qa` exits 0 AND emits exactly 15 QA rows (verifiable via `wc -l` returning 15)
    - Every QA-01..15 row's test_path resolves via fs.existsSync (verifiable via the `--enumerate-qa` exit code 0)
    - `node scripts/commercial/verify-test-coverage.cjs --format json | node -e "const j=JSON.parse(require('fs').readFileSync(0,'utf8')); if(j.summary.total_requirements!==21||j.summary.total_gates!==10){console.error(j.summary); process.exit(1);}"` exits 0
    - `node --test test/commercial/test-coverage-matrix.test.js` exits 0 with 11 tests passing
    - `node --test test/integration/qa-09-ci-aware-policy.test.js` exits 0
    - `node --test test/integration/qa-13-operability.test.js` exits 0
    - `node scripts/commercial/verify-chromatic-coverage.cjs` exits 0
    - `grep -c "## Phase 228 Cross-Engine Detection Matrix" .planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md` returns 1
    - `grep -c "^| COM-0" .planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md` returns 6 (exactly 6 individual COM rows in matrix)
    - `grep -c "^| QA-0\|^| QA-1" .planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md` returns 15 (exactly 15 individual QA rows — NO roll-up alias)
    - `grep -c "see existing matrix\|aggregated by" .planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md` returns 0 in the Phase 228 section (no roll-up shortcut)
    - Earlier sections of `V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md` are unchanged (verify by `grep -c "## Phase 204 - CLI" .planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md` returns 1 — non-zero confirms append-only)
  </acceptance_criteria>
  <done>
    Test matrix is APPENDED with 21 individual rows (no roll-up alias), CLI gate parses + verifies coverage with `--enumerate-qa` enumerating all 15 QA rows individually, every test_path resolves via fs.existsSync, 11 coverage matrix tests pass, QA-09/QA-13/QA-14 backing files exist.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json -> production install | new devDeps could break legacy CI; must not change `"test"` script |
| matrix append -> earlier sections | test matrix is shared; modification of earlier rows would break upstream phase tests |
| detector regex -> false positives | regex on hardcoded prices may flag tests/stories — exclusion list is the mitigation |
| Playwright E2E -> dev server | E2E suite assumes a running dev server; slice 04 contract spec runs always (no dev server needed) for safety |
| QA enumeration -> fs.existsSync | test_paths must resolve; circular alias defeats traceability |
| Chromatic CI hook -> token leakage | CHROMATIC_PROJECT_TOKEN is in .env.example as placeholder only; real token comes from operator dashboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-228-04-01 | Tampering | matrix earlier sections | mitigate | acceptance criterion checks Phase 204 section still exists; CI grep guard |
| T-228-04-02 | Tampering | package.json `"test"` script | mitigate | verify command asserts `scripts.test === "node --test test/**/*.test.js"` |
| T-228-04-03 | Information disclosure | hardcoded credentials in detectors | accept | detectors read source code only; do not exfiltrate |
| T-228-04-04 | Repudiation | E2E suite skipped silently | mitigate | per B1 fix: NO env-gating skip on `commercial-evidence-chain.spec.ts`; contract spec always runs as fast pre-flight; full E2E runs in slice 05 readiness aggregation after engine-dir checks pass |
| T-228-04-05 | Denial of service | Playwright spawns dev server | mitigate | slice 04 verify command runs ONLY the contract spec (no browser); full E2E with dev server runs in slice 05 readiness aggregation |
| T-228-04-06 | Elevation of privilege | detector misses governed-mutation route | mitigate | per B2 fix: detector HARD FAILS (exit 1) on missing engine dirs with route-back-to-upstream-phase message; no `coverage_pending` deferral translation |
| T-228-04-07 | Tampering | pricing-bypass false positive | accept | exclusion list (test/stories) + `{{MARKOS_PRICING_ENGINE_PENDING}}` placeholder context check; documented in detector comments |
| T-228-04-08 | Repudiation | QA-NN traceability via roll-up alias | mitigate | per B4 fix: 15 individual QA rows in matrix; `--enumerate-qa` flag emits 15 rows with concrete fs-resolvable paths; no circular alias |
| T-228-04-09 | Tampering | Chromatic project token | mitigate | `.env.example` placeholder only; real token from operator dashboard; `--dry-run` mode does not require token |
| T-228-04-10 | Information disclosure | Storybook stories exposing real data | accept | story placeholders are minimal JSX; visual finalization is upstream-phase work |
</threat_model>

<verification>
- `npx vitest run test/vitest/commercial-contracts.spec.ts` exits 0 (10 tests)
- `npx playwright test tests/e2e/commercial-evidence-chain.contract.spec.ts` exits 0 (always-run contract spec)
- `npm run commercial:chromatic:dry` exits 0
- `node scripts/commercial/verify-test-coverage.cjs` exits 0
- `node scripts/commercial/verify-test-coverage.cjs --enumerate-qa` exits 0 and emits 15 lines
- `node scripts/commercial/verify-chromatic-coverage.cjs` exits 0
- `node --test test/commercial/test-coverage-matrix.test.js` exits 0 (11 tests)
- `node --test test/integration/qa-09-ci-aware-policy.test.js test/integration/qa-13-operability.test.js` exits 0
- `vitest.config.ts` and `playwright.config.ts` exist; chromium binary installed (npx playwright --version exits 0)
- `package.json` has new scripts + devDeps; legacy `"test"` script unchanged
- 5 Storybook story files under `.storybook/stories/commercial/` (3 named exports each)
- `.planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md` is APPENDED (Phase 204 section still present), Phase 228 detection matrix added with 21 individual rows + 10 gate rows + Chromatic column
- Detectors HARD FAIL on missing engine dirs (per B2): `MISSING_ENGINE_API_DIR/DASHBOARD_DIR/PRICING_SCOPE_DIR/EVIDENCE_SCOPE_DIR` codes present in source files
- Playwright E2E spec has NO `test.skip` env-gating (per B1)
</verification>

<success_criteria>
- Gate 10 (testability is part of design): vitest + playwright + chromatic wired, single E2E commercial-evidence-chain suite exists without env-gating skip, 4 gate-3-6 detectors operational with hard-fail-on-missing-dirs
- Gates 3-6 enforcement coverage: detector test per gate; HARD FAIL when upstream engine dirs absent (per CONTEXT D-01 route-back rule)
- D-24 Chromatic coverage: 5 surface families have stories + dry-run exits 0
- Test matrix EXTENDED (not forked) per CONTEXT non-negotiable; 21 INDIVIDUAL rows (6 COM + 15 QA) + 10 gate rows; no roll-up alias
- Every QA-01..15 has CONCRETE test_path that resolves via fs.existsSync (provable via `--enumerate-qa`)
- Phase-level dependency on 221-227 explicit in objective + threat model + detector messages (per S1)
- Single Playwright suite proves Gate 9 + Gate 10 in one go (per CONTEXT D-29)
</success_criteria>

<output>
After completion, create `.planning/phases/228-commercial-os-integration-future-readiness/228-04-SUMMARY.md` documenting:
- vitest + playwright + chromatic versions
- detector hard-fail behavior (route-back to upstream phase per D-01)
- 21-individual-row coverage matrix (6 COM + 15 QA, no roll-up)
- 10-gate detector mapping
- Chromatic 5-surface-family coverage
- Playwright E2E run policy: contract spec always runs in slice 04 verify; full E2E runs in slice 05 readiness aggregation after engine-dir checks pass
- The contract slice 05 inherits: final readiness matrix consuming all 4 prior slices, gated on engine-dir existence (no `coverage_pending` translation to `deferred`)
</output>
</output>
