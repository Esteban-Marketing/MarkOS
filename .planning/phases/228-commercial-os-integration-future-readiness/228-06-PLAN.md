---
phase: 228-commercial-os-integration-future-readiness
plan: 06
type: execute
wave: 5
depends_on: ["228-05"]
files_modified:
  - obsidian/reference/Contracts Registry.md
  - obsidian/reference/MarkOS v2 Operating Loop Spec.md
  - public/llms.txt
  - docs/cli/commands.md
  - .planning/STATE.md
  - .agent/markos/MARKOS-INDEX.md
  - package.json
  - test/commercial/canon-sync.test.js
  - test/commercial/handoff-closure.test.js
  - .planning/phases/228-commercial-os-integration-future-readiness/vault-drift-report.json
autonomous: false
requirements:
  - COM-01
  - COM-02
  - COM-06
  - QA-15
user_setup: []

must_haves:
  truths:
    - "obsidian/reference/Contracts Registry.md is updated to cite `lib/markos/contracts/commercial-registry.json` as the canonical commercial F-ID source-of-truth (per CLAUDE.md source-of-truth precedence: vault reference > .planning > .agent/markos)"
    - "obsidian/reference/MarkOS v2 Operating Loop Spec.md is updated to reference the parity gates 1-10 and link to phase 228 READINESS-MATRIX.md"
    - "public/llms.txt has a Phase 228 section documenting the commercial registry, parity verifier, adapter swap CLI, migration roundtrip CLI, archival recovery CLI, readiness matrix"
    - "docs/cli/commands.md documents the 6 new `npm run` scripts from slice 04 + the 5 slice CLIs from slices 01-05"
    - ".planning/STATE.md is updated to mark Phase 228 complete and route execution to the next milestone (per B2/D-34, missing engine surfaces route back to upstream phase as a hard gate; deferred cells route to follow-up Phase 228.X entries committed in ROADMAP.md per D-34. There is NO `coverage_pending` model.)"
    - "package.json scripts include 5 named entry points for the commercial CLIs: `commercial:verify-parity`, `commercial:test-adapter-swap`, `commercial:test-migration`, `commercial:test-archival`, `commercial:readiness-matrix`"
    - ".agent/markos/MARKOS-INDEX.md is checked for any commercial-engine token references; updated only if existing tokens reference the now-canonical registry"
    - "Canon sync gate (`test/commercial/canon-sync.test.js`) asserts no drift: every F-ID in registry is referenced in at least one of {Contracts Registry.md, llms.txt}; every CLI is documented in docs/cli/commands.md"
    - "Handoff closure test asserts ROADMAP.md Phase 228 status reflects sign-off (slice 05 Task 3 produced the sign-off; slice 06 verifies it carried through)"
    - "TWO-WAY semantic drift check (per S5 + RM6 hardening): Task 1 reads Contracts Registry.md AND MarkOS v2 Operating Loop Spec.md in FULL, searches for any 'single contract registry' / 'no parallel store' / 'contract registration pattern' statements, compares to proposed `lib/markos/contracts/commercial-registry.json` semantics, and asserts NO SEMANTIC CONFLICT (not just heading absence). **Per RM6 hardening:** if the regex scan finds ZERO matches AND ZERO conflicts, the check writes `.planning/phases/228-commercial-os-integration-future-readiness/vault-drift-report.json` containing the proposed canon edits + a list of the 6 patterns scanned + matched-text excerpts (or `null` if no matches). This snapshot is for human review — silent zero-match passes are not acceptable per RM6. If semantic conflict exists: HALT, write 228-DRIFT-DETECTED.md, escalate per CLAUDE.md drift rule."
  artifacts:
    - path: "obsidian/reference/Contracts Registry.md"
      provides: "Updated section linking to lib/markos/contracts/commercial-registry.json + per-engine F-ID summary"
    - path: "obsidian/reference/MarkOS v2 Operating Loop Spec.md"
      provides: "Updated parity-gates reference linking to READINESS-MATRIX.md"
    - path: "public/llms.txt"
      provides: "Phase 228 section listing CLIs + registry + matrix"
    - path: "docs/cli/commands.md"
      provides: "New section documenting commercial:* npm scripts + CLI flags"
    - path: ".planning/STATE.md"
      provides: "Phase 228 marked complete; current position updated"
    - path: "package.json"
      provides: "5 new commercial:* npm scripts (named aliases for slice 01-05 CLIs)"
    - path: "test/commercial/canon-sync.test.js"
      provides: "node --test asserting registry F-IDs are documented in vault + llms.txt + TWO-WAY semantic drift detection per S5 + RM6 snapshot existence"
    - path: ".planning/phases/228-commercial-os-integration-future-readiness/vault-drift-report.json"
      provides: "Per RM6 hardening: audit snapshot of every drift-check run (patterns scanned, matches found with compatibility assessment, proposed canon edits summary, conflicts detected count). Silent zero-match passes are not acceptable; this snapshot is the human-review trail."
    - path: "test/commercial/handoff-closure.test.js"
      provides: "node --test asserting ROADMAP.md + STATE.md reflect closeout"
  key_links:
    - from: "obsidian/reference/Contracts Registry.md"
      to: "lib/markos/contracts/commercial-registry.json"
      via: "vault reference cites the JSON file as canonical commercial-engine contract source"
      pattern: "commercial-registry\\.json"
    - from: "public/llms.txt"
      to: "scripts/commercial/*.cjs"
      via: "CLI list documents each verifier"
      pattern: "scripts/commercial"
    - from: "package.json"
      to: "scripts/commercial/*.cjs"
      via: "named npm scripts as entry points"
      pattern: "commercial:verify-parity|commercial:test-adapter-swap|commercial:test-migration|commercial:test-archival|commercial:readiness-matrix"
---

<objective>
Close the lane by syncing canon (Obsidian vault), public docs (`llms.txt`, `docs/cli/commands.md`), execution state (`.planning/STATE.md`), and protocol registry references (`.agent/markos/MARKOS-INDEX.md`). This slice is the doc/canon/handoff layer that turns the slice-05 sign-off into a discoverable, cite-able artifact.

Per CONTEXT D-21b ("Slice 228-06 — Documentation, canon sync, execution handoff closure"; planner judges based on doc churn whether to fold into 05). Decision: keep this slice separate because (a) doc churn is non-trivial — vault canon edit + llms.txt + STATE.md + cli docs + canon-sync test + handoff test = 9 files + 2 tests; (b) slice 05 is autonomous=false (human checkpoint) and bundling more work into a checkpoint slice degrades execution quality.

**Per S5 fix:** Task 1 performs a TWO-WAY semantic drift check. The original plan's drift check only verified the heading didn't exist (one-way: "if heading exists, halt"). The corrected drift check reads the FULL vault reference files, searches for semantic statements about single registries / parallel stores / contract registration patterns, and asserts NO SEMANTIC CONFLICT with the proposed commercial-registry.json semantics. If conflict found, HALT and escalate per CLAUDE.md drift rule — do NOT silently extend either side.

Output: vault canon updates + public docs + STATE.md handoff + canon-sync test (with two-way semantic drift detection) + handoff test.
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/phases/228-commercial-os-integration-future-readiness/228-CONTEXT.md
@.planning/phases/228-commercial-os-integration-future-readiness/228-01-PLAN.md
@.planning/phases/228-commercial-os-integration-future-readiness/228-02-PLAN.md
@.planning/phases/228-commercial-os-integration-future-readiness/228-03-PLAN.md
@.planning/phases/228-commercial-os-integration-future-readiness/228-04-PLAN.md
@.planning/phases/228-commercial-os-integration-future-readiness/228-05-PLAN.md
@.planning/phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md
@obsidian/reference/Contracts Registry.md
@obsidian/reference/MarkOS v2 Operating Loop Spec.md
@public/llms.txt
@docs/cli/commands.md
@.agent/markos/MARKOS-INDEX.md
@lib/markos/contracts/commercial-registry.json
@package.json

<interfaces>
<!-- Doc edits per CLAUDE.md source-of-truth precedence: vault reference (HOW objects are shaped) > .planning (WHEN). Neither contradicts the other; this slice merely cross-references them. -->

Section to append to `obsidian/reference/Contracts Registry.md`:
```markdown
## Commercial Registry (Phase 228 — v4.2.0)

**Canonical source:** `lib/markos/contracts/commercial-registry.json` (66 entries — CDP-01..05, CRM-01..05, EML-01..05, MSG-01..05, CNV-01..05, LCH-01..05, ANL-01..05, SEN-01..05, ECO-01..05, COM-01..06, QA-01..15).

**Validation:** `lib/markos/contracts/commercial-registry-validator.js` enforces the registry contract: F-ID format, engine ownership (Gate 1, Gate 2), surface posture (UI/API/MCP), auditable deferrals.

**Doctrine:** Mirrors `lib/markos/plugins/registry.js` (insertion-order Map, frozen entries, idempotent registration, fail-closed validation). The commercial registry is NOT a parallel store; it extends the same substrate.

**Verifier:** `npm run commercial:verify-parity` (alias for `node scripts/commercial/verify-parity-matrix.cjs`).

**Readiness matrix:** `.planning/phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md`
```

Section to append to `obsidian/reference/MarkOS v2 Operating Loop Spec.md`:
```markdown
## Phase 228 — Parity Gates (v4.2.0 commercial lane closeout)

The 10 parity gates from `.planning/V4.2.0-INCOMING-18-26-GSD-DISCUSS-RESEARCH-ROUTING.md` lines 113-126 are operationalized as machine-verifiable CLIs:

1. One identity truth → `commercial-registry-validator.js` GATE1_DUPLICATE_IDENTITY_OWNER
2. One commercial memory → GATE2_DUPLICATE_TIMELINE_OWNER
3. One governed mutation path → `test/commercial/governed-mutation.test.js`
4. No passive systems → `test/commercial/passive-system-detector.test.js`
5. No pricing bypasses → `test/commercial/pricing-bypass-detector.test.js`
6. No unsupported external claim → `test/commercial/evidence-freshness-detector.test.js`
7. No provider lock as architecture → `npm run commercial:test-adapter-swap`
8. No designed obsolescence → `npm run commercial:test-migration` + `npm run commercial:test-archival`
9. UI/API/MCP parity → `npm run commercial:verify-parity`
10. Testability is part of design → `node scripts/commercial/verify-test-coverage.cjs`

Final readiness matrix: `.planning/phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md`.
```

Section to append to `public/llms.txt`:
```
## Phase 228 — Commercial OS readiness (v4.2.0)

Commercial registry: lib/markos/contracts/commercial-registry.json (66 entries across cdp/crm/email/messaging/conversion/launch/analytics/sales/ecosystem/commercial/quality)

CLIs (npm scripts):
- npm run commercial:verify-parity         — UI/API/MCP parity per F-ID
- npm run commercial:test-adapter-swap     — Provider replaceability per binding
- npm run commercial:test-migration         — Migration roundtrip per durable object
- npm run commercial:test-archival          — Archival/recovery per engine
- npm run commercial:readiness-matrix       — Final readiness matrix

Detectors (gate enforcement):
- test/commercial/governed-mutation.test.js       — Gate 3
- test/commercial/passive-system-detector.test.js — Gate 4
- test/commercial/pricing-bypass-detector.test.js — Gate 5
- test/commercial/evidence-freshness-detector.test.js — Gate 6

Readiness matrix:
.planning/phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md
```

Section to append to `docs/cli/commands.md`:
```markdown
## Commercial OS Verification Commands (Phase 228)

| Command | Purpose | Exit codes |
|---|---|---|
| `npm run commercial:verify-parity` | Verify UI/API/MCP parity per F-ID | 0 = clean, 1 = parity gap, 2 = registry missing |
| `npm run commercial:test-adapter-swap` | Run swap test for every external-provider binding | 0 = all pass, 1 = swap mismatch, 2 = missing fixture |
| `npm run commercial:test-migration` | Round-trip every durable-object class | 0 = clean, 1 = round-trip diff, 2 = missing exporter/importer |
| `npm run commercial:test-archival` | Simulated archive + restore + verify per engine | 0 = clean, 1 = restore diff or broken cascade, 2 = missing stub |
| `npm run commercial:readiness-matrix` | Build readiness matrix from all gates | 0 = matrix emitted |
| `node scripts/commercial/verify-readiness-matrix.cjs` | Verify built matrix has no fail/unfilled cells (sign-off mode) | 0 = clean, 1 = matrix has fail or undocumented deferral, 2 = matrix file missing |
| `node scripts/commercial/verify-readiness-matrix.cjs --strict` | Verify matrix in post-sign-off CI mode (deferred = fail) | 0 = lane fully clean, 1 = any deferred or fail cells exist |
| `node scripts/commercial/verify-test-coverage.cjs` | Verify every COM/QA requirement has a referenced test file | 0 = clean, 1 = missing test, 2 = matrix section missing |
| `node scripts/commercial/verify-test-coverage.cjs --enumerate-qa` | Enumerate all 15 QA-NN rows individually | 0 = all 15 paths resolve, 1 = any missing |

Common flags: `--engine <name>`, `--format json|table`, `--no-write` (dry-run), `--simulated` (archival CLI), `--strict` (verifier — treat deferred as failure), `--cascade <engine>/<object>` (archival cascade walk), `--qa-id <id>` (verify-readiness-matrix surface specific QA row).
```

Required `package.json` script aliases:
```json
"commercial:verify-parity":      "node scripts/commercial/verify-parity-matrix.cjs",
"commercial:test-adapter-swap":  "node scripts/commercial/test-adapter-swap.cjs",
"commercial:test-migration":     "node scripts/commercial/test-migration-roundtrip.cjs",
"commercial:test-archival":      "node scripts/commercial/test-archival-recovery.cjs --simulated",
"commercial:readiness-matrix":   "node scripts/commercial/build-readiness-matrix.cjs",
"commercial:build-registry":     "node scripts/commercial/build-commercial-registry.cjs"
```

S5 — Two-way semantic drift detection patterns (used by Task 1 step 1):
```js
// Phrases in vault references that, if found, may indicate a parallel/conflicting registry
const semantic_conflict_patterns = [
  /single\s+contract\s+registry/i,
  /the\s+only\s+contract\s+registration\s+(?:point|pattern)/i,
  /no\s+parallel\s+(?:registry|store)/i,
  /contract\s+registration\s+pattern\s+(?:must|shall|is)/i,
  /one\s+canonical\s+registry/i,
  /registry\s+is\s+the\s+(?:sole|only)\s+source/i,
];
// For each match, examine context (10 lines before+after); compare semantics to commercial-registry.json doctrine
// (which is: extends plugins/registry.js doctrine, NOT a parallel store). If a vault statement excludes
// extension by stating "no contracts may be registered outside <single registry X>" where X != plugins/registry.js,
// that is a semantic conflict.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update vault canon (Contracts Registry.md + Operating Loop Spec.md) per source-of-truth precedence — with TWO-WAY semantic drift detection per S5</name>
  <files>
    obsidian/reference/Contracts Registry.md,
    obsidian/reference/MarkOS v2 Operating Loop Spec.md,
    test/commercial/canon-sync.test.js
  </files>
  <read_first>
    - **obsidian/reference/Contracts Registry.md (FULL FILE — read end-to-end for semantic drift check per S5)**
    - **obsidian/reference/MarkOS v2 Operating Loop Spec.md (FULL FILE — read end-to-end for semantic drift check per S5)**
    - CLAUDE.md (source-of-truth precedence + drift rule: "if .planning/ appears to contradict vault brain/reference ... STOP and flag")
    - lib/markos/contracts/commercial-registry.json (66 entries — verify count before referencing)
    - lib/markos/contracts/commercial-registry.js (slice 01 — semantics: extends plugins/registry.js, NOT a parallel store)
    - lib/markos/plugins/registry.js (slice 01 doctrine source)
    - .planning/V4.2.0-INCOMING-18-26-GSD-DISCUSS-RESEARCH-ROUTING.md lines 113-126 (10 gates verbatim)
    - .planning/phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md (slice 05 — verify exists before linking)
  </read_first>
  <action>
    1. **TWO-WAY semantic drift check FIRST (per S5 fix + CLAUDE.md drift rule):**
       - Read `obsidian/reference/Contracts Registry.md` end-to-end (full file, all sections).
       - Read `obsidian/reference/MarkOS v2 Operating Loop Spec.md` end-to-end (full file, all sections).
       - Search both files for any of the semantic_conflict_patterns from `<interfaces>`:
         - `/single\s+contract\s+registry/i`
         - `/the\s+only\s+contract\s+registration\s+(?:point|pattern)/i`
         - `/no\s+parallel\s+(?:registry|store)/i`
         - `/contract\s+registration\s+pattern\s+(?:must|shall|is)/i`
         - `/one\s+canonical\s+registry/i`
         - `/registry\s+is\s+the\s+(?:sole|only)\s+source/i`
       - For each match found:
         - Extract 10 lines of context before + 10 lines after
         - Read the matched statement carefully — does it specify a SINGLE specific registry that excludes extension?
         - Compare to commercial-registry.json semantics: it EXTENDS `lib/markos/plugins/registry.js` doctrine; it is NOT a parallel store; it shares Map-backed insertion-order, frozen entries, idempotent registration.
         - **If the vault statement explicitly excludes extending the substrate (e.g., "no contracts may be registered outside the agent contracts registry") AND the proposed commercial-registry would violate that exclusion: HALT.** Print to stderr: "SEMANTIC DRIFT DETECTED: <vault file>:<line>: <matched statement> — proposed commercial-registry.json semantics conflict. Per CLAUDE.md drift rule, STOP and escalate to user. Do NOT silently extend either side." Exit task with non-zero status. Document the conflict in `.planning/phases/228-commercial-os-integration-future-readiness/228-DRIFT-DETECTED.md` with the conflicting statement, line number, and proposed reconciliation options.
         - **If the vault statement is compatible** (e.g., it describes plugins/registry.js as the foundational doctrine and commercial-registry extends it; or it does not explicitly exclude extension): proceed to step 2 with no halt. Document the compatibility check result as a comment in canon-sync.test.js (Test 6 below).
       - **Expected outcome:** vault has general contracts registry doctrine; commercial-registry extends that doctrine. No conflict expected. The check is fail-safe, not fail-noisy.
       - **Per RM6 hardening: snapshot every drift-check run.** After completing the scan (regardless of result — match or no-match, conflict or no-conflict), Task 1 writes `.planning/phases/228-commercial-os-integration-future-readiness/vault-drift-report.json` with shape:
         ```json
         {
           "scanned_at": "<ISO timestamp>",
           "vault_files": ["obsidian/reference/Contracts Registry.md", "obsidian/reference/MarkOS v2 Operating Loop Spec.md"],
           "patterns_scanned": ["single contract registry", "the only contract registration", "no parallel registry", "contract registration pattern must|shall|is", "one canonical registry", "registry is the sole|only source"],
           "matches": [{"file": "...", "line": N, "text": "...", "context_before": "...", "context_after": "...", "compatibility_assessment": "compatible|conflict", "reasoning": "..."}],
           "conflicts_detected": <number>,
           "proposed_canon_edits_summary": [{"file": "obsidian/reference/Contracts Registry.md", "section_added": "## Commercial Registry (Phase 228 — v4.2.0)", "line_count": N}, {"file": "obsidian/reference/MarkOS v2 Operating Loop Spec.md", "section_added": "## Phase 228 — Parity Gates (v4.2.0 commercial lane closeout)", "line_count": N}]
         }
         ```
         If `conflicts_detected > 0` → also writes `228-DRIFT-DETECTED.md` AND halts edits. If `conflicts_detected === 0 && matches.length === 0` → snapshot still produced (silent zero-match passes are not acceptable per RM6 — the snapshot is the human-review trail). If `conflicts_detected === 0 && matches.length > 0` → snapshot lists each match with compatibility assessment, edits proceed.

    2. (Only if step 1 passes) Append the "Commercial Registry (Phase 228 — v4.2.0)" section verbatim per `<interfaces>` block to `obsidian/reference/Contracts Registry.md`. Place it at the end of the file under a new `## Commercial Registry (Phase 228 — v4.2.0)` heading.

    3. (Only if step 1 passes) Append the "Phase 228 — Parity Gates (v4.2.0 commercial lane closeout)" section verbatim per `<interfaces>` block to `obsidian/reference/MarkOS v2 Operating Loop Spec.md`.

    4. Create `test/commercial/canon-sync.test.js`:
       - Test 1: `obsidian/reference/Contracts Registry.md` contains the verbatim heading `## Commercial Registry (Phase 228 — v4.2.0)`
       - Test 2: that section contains a reference to `lib/markos/contracts/commercial-registry.json`
       - Test 3: `obsidian/reference/MarkOS v2 Operating Loop Spec.md` contains the verbatim heading `## Phase 228 — Parity Gates (v4.2.0 commercial lane closeout)`
       - Test 4: that section contains a reference to all 10 gates (one regex match per gate id)
       - Test 5: every F-ID prefix in commercial-registry.json (`CDP|CRM|EML|MSG|CNV|LCH|ANL|SEN|ECO|COM|QA`) is mentioned in EITHER `obsidian/reference/Contracts Registry.md` OR `public/llms.txt` (proves no orphan F-IDs in canon — slice 06 Task 2 also touches llms.txt)
       - **Test 6 (NEW per S5 — semantic drift detection automated):** load both vault files; for each `semantic_conflict_pattern`, scan both files; for each match, assert the matched statement is COMPATIBLE with commercial-registry doctrine. The test loads `lib/markos/plugins/registry.js` to confirm the foundational doctrine (`createRegistry`, `registerPlugin`) and asserts that commercial-registry.js mirrors it (verifiable by `grep -c "from.*plugins/registry\|require.*plugins/registry" lib/markos/contracts/commercial-registry.js`). If any vault statement explicitly forbids extending the substrate, the test FAILS with `code: 'SEMANTIC_DRIFT'` and a description of the conflict. If no conflict (expected), test passes.
       - **Test 7 (per S5):** assert `.planning/phases/228-commercial-os-integration-future-readiness/228-DRIFT-DETECTED.md` does NOT exist (proves Task 1 step 1 found no drift). If it exists, this test fails — surfacing the drift to the operator and routing escalation per CLAUDE.md.
       - **Test 8 (NEW per RM6 hardening):** assert `.planning/phases/228-commercial-os-integration-future-readiness/vault-drift-report.json` DOES exist (proves Task 1 step 1 ran and produced an audit snapshot). Parse the JSON; assert it has `scanned_at`, `vault_files`, `patterns_scanned`, `matches`, `conflicts_detected`, `proposed_canon_edits_summary` keys. Assert `conflicts_detected === 0` (no conflict). If `matches.length > 0`, assert each match has a `compatibility_assessment` of `compatible` (not `conflict`). This test surfaces the drift-check evidence trail to humans/CI — silent zero-match passes are not acceptable per RM6.
  </action>
  <verify>
    <automated>node --test test/commercial/canon-sync.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Commercial Registry (Phase 228 — v4.2.0)" "obsidian/reference/Contracts Registry.md"` returns 1
    - `grep -c "lib/markos/contracts/commercial-registry.json" "obsidian/reference/Contracts Registry.md"` returns >= 1
    - `grep -c "## Phase 228 — Parity Gates (v4.2.0 commercial lane closeout)" "obsidian/reference/MarkOS v2 Operating Loop Spec.md"` returns 1
    - `grep -c "GATE1_DUPLICATE_IDENTITY_OWNER\|GATE2_DUPLICATE_TIMELINE_OWNER" "obsidian/reference/MarkOS v2 Operating Loop Spec.md"` returns 2
    - `grep -c "commercial:verify-parity\|commercial:test-adapter-swap\|commercial:test-migration\|commercial:test-archival" "obsidian/reference/MarkOS v2 Operating Loop Spec.md"` returns >= 4
    - File exists: `test/commercial/canon-sync.test.js`
    - `node --test test/commercial/canon-sync.test.js` exits 0 with 8 tests passing (5 original + 2 S5 + 1 RM6 hardening)
    - **Per S5: file does NOT exist: `.planning/phases/228-commercial-os-integration-future-readiness/228-DRIFT-DETECTED.md`** (proves step 1 two-way semantic check found no conflict; if file exists, vault canon edits MUST NOT have been made and operator escalation is required)
    - **Per RM6 hardening: file DOES exist: `.planning/phases/228-commercial-os-integration-future-readiness/vault-drift-report.json`** (proves step 1 produced an audit snapshot — silent zero-match passes not acceptable):
      `node -e "const j=require('./.planning/phases/228-commercial-os-integration-future-readiness/vault-drift-report.json'); if(typeof j.conflicts_detected !== 'number'){console.error('invalid shape'); process.exit(1);} if(j.conflicts_detected !== 0){console.error('drift detected — see 228-DRIFT-DETECTED.md'); process.exit(1);} if(!Array.isArray(j.matches)){console.error('matches missing'); process.exit(1);} if(!Array.isArray(j.patterns_scanned) || j.patterns_scanned.length !== 6){console.error('expected 6 patterns'); process.exit(1);} console.log('OK drift report present, conflicts=0, patterns=',j.patterns_scanned.length,'matches=',j.matches.length);"` exits 0
    - Earlier sections of vault files unchanged: `grep -c "^# " "obsidian/reference/Contracts Registry.md"` returns same value as before edit (verify by reading file before + after)
  </acceptance_criteria>
  <done>
    Vault canon updated to cite commercial registry + parity gates; canon-sync test passes (8 tests including 2 S5 + 1 RM6 snapshot); two-way semantic drift check completed per S5 fix; vault-drift-report.json audit snapshot produced per RM6 hardening (silent zero-match passes not acceptable).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update public docs (llms.txt + docs/cli/commands.md) + add package.json npm script aliases</name>
  <files>
    public/llms.txt,
    docs/cli/commands.md,
    package.json
  </files>
  <read_first>
    - public/llms.txt (full file — Phase 204 section already exists; append Phase 228 section)
    - docs/cli/commands.md (full file — current command list)
    - package.json (current scripts section)
    - .planning/phases/228-commercial-os-integration-future-readiness/228-04-PLAN.md (slice 04 npm scripts — keep them; this task adds aliases without disturbing slice 04 names)
    - lib/markos/contracts/commercial-registry.json (verify entries are loadable before referencing in llms.txt)
  </read_first>
  <action>
    1. Append the "Phase 228 — Commercial OS readiness (v4.2.0)" section verbatim per `<interfaces>` block to `public/llms.txt`. Place at end of file (after Phase 204 section).

    2. Append the "Commercial OS Verification Commands (Phase 228)" section verbatim per `<interfaces>` block to `docs/cli/commands.md`. Place at end.

    3. Modify `package.json` to add 6 named npm script aliases per `<interfaces>` block. Place them in the `scripts` object alphabetically (Node tooling and existing scripts are alphabetical-ish; preserve order conventions of the file). DO NOT remove or rename slice 04 scripts (`test:vitest`, `test:playwright`, etc.) — these aliases are additive entry points, not replacements.

    4. Verify each new alias resolves to its CLI: `npm run commercial:verify-parity` should exit 0 (slice 01 CLI must already be in place from prior slice execution).

    Self-check before commit:
    - `npm run` (no args) lists all 6 new commercial:* scripts
    - `node -e "const p=require('./package.json'); const a=['commercial:verify-parity','commercial:test-adapter-swap','commercial:test-migration','commercial:test-archival','commercial:readiness-matrix','commercial:build-registry']; for(const s of a){if(!p.scripts[s]){console.error('missing',s); process.exit(1);}} console.log('OK');"` exits 0
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const a=['commercial:verify-parity','commercial:test-adapter-swap','commercial:test-migration','commercial:test-archival','commercial:readiness-matrix','commercial:build-registry']; for(const s of a){if(!p.scripts[s]){console.error('missing',s); process.exit(1);}} console.log('OK');" && grep -c "Phase 228 — Commercial OS readiness" public/llms.txt && grep -c "Commercial OS Verification Commands (Phase 228)" docs/cli/commands.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Phase 228 — Commercial OS readiness" public/llms.txt` returns 1
    - `grep -c "npm run commercial:verify-parity\|npm run commercial:test-adapter-swap\|npm run commercial:test-migration\|npm run commercial:test-archival\|npm run commercial:readiness-matrix" public/llms.txt` returns >= 5
    - `grep -c "## Commercial OS Verification Commands (Phase 228)" docs/cli/commands.md` returns 1
    - `grep -c "commercial:verify-parity\|commercial:test-adapter-swap\|commercial:test-migration\|commercial:test-archival\|commercial:readiness-matrix\|commercial:build-registry" package.json` returns >= 6
    - `npm run` (no args) lists `commercial:verify-parity` (verify by `npm run 2>&1 | grep -c "commercial:verify-parity"` returning >= 1)
    - Phase 204 section in llms.txt unchanged: `grep -c "Phase 204" public/llms.txt` returns same value as before edit
  </acceptance_criteria>
  <done>
    Public docs updated, npm script aliases land, no regression on Phase 204 docs or existing scripts.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Update STATE.md + verify handoff closure (lane closed)</name>
  <files>
    .planning/STATE.md,
    .agent/markos/MARKOS-INDEX.md,
    test/commercial/handoff-closure.test.js
  </files>
  <read_first>
    - .planning/STATE.md (full file — current position section)
    - .agent/markos/MARKOS-INDEX.md (full file — check for any commercial-engine token references)
    - .planning/ROADMAP.md (Phase 228 block — slice 05 Task 3 should have updated this)
    - .planning/phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md (slice 05 — verify sign-off block populated)
    - .planning/phases/228-commercial-os-integration-future-readiness/228-CONTEXT.md (D-30 done criteria)
  </read_first>
  <what-built>
    Slices 01-05 produced: 66-entry commercial registry (with INDIVIDUAL QA-NN linked_verifier per B4), parity verifier, adapter swap harness + 7 bindings, migration manifest with 36 round-trip-tested durable objects, archival policy + recovery runbook + cascade enforcement (with explicit per-class SOC2-I retention floor per S4), vitest+playwright wired (no env-gating skip on E2E per B1), 4 hard-failing gate detectors (per B2), single E2E evidence-chain Playwright suite, Chromatic stories for D-24 surfaces (per B3), test-coverage CLI gate with --enumerate-qa, V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md appended Phase 228 section with 21 INDIVIDUAL rows + 10 gate rows + Chromatic column, READINESS-MATRIX.md signed off, ROADMAP.md updated.

    Slice 06 Tasks 1-2: vault canon (with TWO-WAY semantic drift check per S5 + RM6 audit snapshot) + public docs + npm scripts updated.

    **Per D-34 (RH4 fix — hard follow-through enforcement):** sign-off was only valid in slice 05 because either (a) `verify-readiness-matrix.cjs --strict` exited 0 (zero deferred cells), OR (b) every deferred cell had a corresponding `Phase 228.X` follow-up entry committed to ROADMAP.md. Slice 06 Task 3 now also asserts the follow-up phase entries actually exist (i.e., were not removed after sign-off).

    This checkpoint: STATE.md + MARKOS-INDEX.md + handoff-closure test.
  </what-built>
  <how-to-verify>
    1. **STATE.md update** (manual edit by human OR Claude pre-fill — checkpoint approves):
       - Update `## Current Position` section: replace existing position with text reflecting Phase 228 closeout + next lane.
         Suggested text:
         ```
         Phase: 228 (commercial-os-integration-future-readiness) — COMPLETE; READINESS-MATRIX.md signed off <ISO date>
         Plan: All 6 plans complete
         Next: v4.2.0 lane closeout achieved. Resume v4.0.0 execution at Phase 205 (Pricing Engine foundation) OR begin v4.2.0 engine implementation per ROADMAP order. **Per B2 + D-34 (RH4 fix): there is NO `coverage_pending` model.** Missing engine surfaces hard-fail the precondition and route back to upstream phases 221-227 (per D-01); auditable deferrals route to follow-up Phase 228.X entries committed in ROADMAP.md (per D-34). Sign-off was only valid because every deferral has a follow-up phase OR `--strict` exited 0.
         ```
       - Update `## Phase 228 Plan Progress` block (or create one if absent), listing all 6 plans as `[x]` with one-line summary each.
       - Append a new planning-overlay block: `## Planning overlay (<ISO date> Phase 228 commercial-OS lane closed)` summarizing the 5 CLI gates + 4 detectors + readiness matrix + sign-off + S5 semantic drift check passed.

    2. **MARKOS-INDEX.md drift check**:
       - Read `.agent/markos/MARKOS-INDEX.md`. If it references any of the 9 commercial engines (CDP, CRM, email, messaging, conversion, launch, analytics, sales, ecosystem) with token IDs, verify those tokens still resolve to the canonical files post phase 228.
       - If MARKOS-INDEX.md references a now-incorrect path, update it. If MARKOS-INDEX.md does NOT reference commercial engines (which is expected — MarkOS protocol is marketing protocol layer; commercial registry is engineering substrate), no edit needed.
       - If unsure, do nothing and note in resume-signal.

    3. **Handoff closure test** (Claude creates this autonomously even though task is checkpoint-typed — the human verifies it passes):
       Create `test/commercial/handoff-closure.test.js`:
       - Test 1: `.planning/STATE.md` contains string `Phase: 228` AND `COMPLETE` AND `READINESS-MATRIX`
       - Test 2: `.planning/ROADMAP.md` Phase 228 block contains string `signed off`
       - Test 3: `.planning/ROADMAP.md` Phase 228 block contains link to `READINESS-MATRIX.md`
       - Test 4: `.planning/phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md` `## Sign-off` block contains a non-empty `Signed off by:` line (not the literal placeholder `<your name>`)
       - Test 5: `node scripts/commercial/verify-readiness-matrix.cjs` exits 0 (sign-off mode — default)
       - Test 5b (per N3 — informational, NOT asserted): `node scripts/commercial/verify-readiness-matrix.cjs --strict` exit code is captured and documented in test output. May exit 1 if auditable deferrals exist; that is acceptable for sign-off as long as deferrals are listed in sign-off doc. Test does NOT fail on `--strict` exit 1 — it merely records the exit code for human review.
       - Test 6: `npm run commercial:verify-parity` exits 0
       - Test 7: `npm run commercial:test-migration` exits 0
       - Test 8: `npm run commercial:test-archival` exits 0
       - Test 9: spawn `node --test test/commercial/canon-sync.test.js`, expect exit 0
       - Test 10: spawn `node --test test/commercial/com-qa-coverage.test.js`, expect exit 0
       - Test 11: spawn `node --test test/commercial/readiness-matrix.test.js`, expect exit 0
       - **Test 12 (per S5):** assert `.planning/phases/228-commercial-os-integration-future-readiness/228-DRIFT-DETECTED.md` does NOT exist (no semantic drift escalation outstanding)
       - **Test 13 (NEW per D-34, RH4 fix): if READINESS-MATRIX.md sign-off block lists ANY deferred cells, assert each has a corresponding `### Phase 228.\d+` entry in `.planning/ROADMAP.md` with explicit dependency linkage. To exercise: parse sign-off block for `Deferred:` lines (if any); for each deferred cell, grep ROADMAP.md for matching `Phase 228.X` entry. Mismatch → fail. If no deferred cells → test trivially passes.**
       - **Test 14 (NEW per RM6 hardening): assert `.planning/phases/228-commercial-os-integration-future-readiness/vault-drift-report.json` exists AND has `conflicts_detected === 0`** (proves Task 1 ran and audit snapshot is present)

    4. **Final verification** (human-eyes review):
       - Open `.planning/STATE.md` and confirm Phase 228 status updated correctly
       - Open `.planning/ROADMAP.md` Phase 228 block and confirm sign-off + readiness matrix link
       - Open READINESS-MATRIX.md and confirm sign-off block has real signer + date (not placeholder); confirm any `deferred` cells are listed in the sign-off block per N3 reconciliation
       - Run `node --test test/commercial/handoff-closure.test.js` — expect exit 0 with 12 tests passing (Test 5b is informational and does not fail)

    Acceptance signals:
    - 'approved' → STATE.md is updated, MARKOS-INDEX.md drift checked, handoff-closure test passes; lane is CLOSED
    - 'fix STATE.md' → describe what's wrong; Claude updates and re-presents
    - 'route gap-closure for X engine' → human flags an upstream-phase gap that surfaced during slice 05 sign-off (per B2/D-01: this manifests as `MISSING_ENGINE_*_DIR` from the precondition CLI, NOT a `coverage_pending` cell — those do not exist in this plan model). Claude updates STATE.md to reflect the gap routing and creates a gap-closure plan via `/gsd-plan-phase --gaps` for the upstream phase, OR creates a follow-up Phase 228.X entry per D-34. Sign-off does NOT proceed until the route-back is committed.
    - 'semantic drift detected' → 228-DRIFT-DETECTED.md exists; Task 1 step 1 found a conflict; HALT closure and escalate per CLAUDE.md drift rule
  </how-to-verify>
  <action>
    Pre-fill the STATE.md updates (Phase 228 status + plan progress block + planning-overlay block) per <how-to-verify> step 1. Drift-check MARKOS-INDEX.md per step 2 — edit ONLY if existing tokens reference now-canonical commercial registry paths; do nothing otherwise. Create test/commercial/handoff-closure.test.js with 12 assertions per step 3. Present STATE.md edits + MARKOS-INDEX.md disposition to human for approval. After human "approved" signal, run final verification: "node --test test/commercial/handoff-closure.test.js" + "node scripts/commercial/verify-readiness-matrix.cjs" — both must exit 0 to declare lane CLOSED (note: --strict exit code is informational per N3, not asserted).
  </action>
  <verify>
    <automated>node --test test/commercial/handoff-closure.test.js && node scripts/commercial/verify-readiness-matrix.cjs && grep -c "Phase: 228" .planning/STATE.md && grep -c "COMPLETE" .planning/STATE.md</automated>
  </verify>
  <done>
    Human approved closure. STATE.md reflects Phase 228 complete with next-lane routing. MARKOS-INDEX.md drift-checked (edited or unchanged). test/commercial/handoff-closure.test.js exists and all 14 assertions pass (12 prior + 1 D-34 follow-up + 1 RM6 snapshot). `node scripts/commercial/verify-readiness-matrix.cjs --strict` exits 0 (per D-34 — closeout gate, not advisory). Phase 228 lane CLOSED.
  </done>
  <resume-signal>Type "approved" + add `Signed off by: <your name>` line to READINESS-MATRIX.md if not already done in slice 05 (and list deferred cells with target_phase if --strict exits 1), OR describe issues that block closure</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vault canon edits -> drift | per CLAUDE.md drift rule + S5 fix: TWO-WAY semantic check (read both files in full, scan for conflict patterns, escalate on match); not a one-way heading-absence check |
| llms.txt -> public surface | content is publicly served via /llms.txt; no secrets allowed |
| MARKOS-INDEX.md -> protocol routing | tampering with token IDs could re-route marketing agent execution |
| STATE.md -> next agent | future agents read STATE.md to know what to do; mis-marked status would mis-route work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-228-06-01 | Tampering | obsidian/reference vault files | mitigate | per S5 fix: Task 1 step 1 reads BOTH vault files in full, scans for 6 semantic_conflict_patterns, examines context, asserts no semantic conflict (not just heading absence). On conflict: HALT, write 228-DRIFT-DETECTED.md, escalate per CLAUDE.md. Task 1 acceptance asserts 228-DRIFT-DETECTED.md does NOT exist. |
| T-228-06-02 | Tampering | STATE.md current position | mitigate | Task 3 is checkpoint:human-verify with explicit human approval signal |
| T-228-06-03 | Information disclosure | llms.txt section published publicly | accept | F-IDs and CLI names are non-secret; no credentials in commercial registry |
| T-228-06-04 | Repudiation | vault edits without git commit | mitigate | git commit at end of slice (autonomous) per gsd-tools commit step in execution flow |
| T-228-06-05 | Denial of service | npm run alias misconfigured | mitigate | Task 2 verify command spawns each alias and confirms it resolves |
| T-228-06-06 | Elevation of privilege | unauthorized signer signs READINESS-MATRIX | mitigate | Task 3 checkpoint requires human resume-signal; Claude does not self-sign |
| T-228-06-07 | Tampering | MARKOS-INDEX.md unrelated changes | mitigate | Task 3 read_first instructs drift-check; do-nothing-if-unsure default |
| T-228-06-08 | Repudiation | semantic drift silently extended | mitigate | per S5 fix: Task 1 canon-sync test 6 + 7 fail-closed if 228-DRIFT-DETECTED.md exists OR if vault statement found conflicting; tests assert no conflict before edits proceed |
</threat_model>

<verification>
- `node --test test/commercial/canon-sync.test.js` exits 0 (8 tests — 5 original + 2 S5 + 1 RM6 snapshot existence)
- `node --test test/commercial/handoff-closure.test.js` exits 0 (14 tests including S5 drift-not-detected, D-34 follow-up phase, RM6 snapshot existence)
- `npm run commercial:verify-parity` exits 0
- `npm run commercial:test-migration` exits 0
- `npm run commercial:test-archival` exits 0
- `node scripts/commercial/verify-readiness-matrix.cjs --strict` exits 0 (per D-34 — closeout gate, replaces sign-off-mode + advisory --strict reconciliation)
- vault files updated with append-only edits (Phase 228 sections present, earlier sections unchanged) AFTER S5 two-way semantic drift check passed
- public/llms.txt + docs/cli/commands.md + package.json scripts updated
- STATE.md reflects Phase 228 closeout
- READINESS-MATRIX.md sign-off block has real signer (not placeholder)
- ROADMAP.md Phase 228 block links to READINESS-MATRIX.md and shows sign-off date
- `.planning/phases/228-commercial-os-integration-future-readiness/228-DRIFT-DETECTED.md` does NOT exist (no semantic drift outstanding per S5)
- `.planning/phases/228-commercial-os-integration-future-readiness/vault-drift-report.json` DOES exist with `conflicts_detected === 0` (per RM6 hardening — audit snapshot)
- ROADMAP.md follow-up Phase 228.X entries exist for every deferred cell in READINESS-MATRIX.md sign-off block (per D-34 — RH4 hard follow-through)
</verification>

<success_criteria>
- Vault canon (Contracts Registry.md + Operating Loop Spec.md) updated per source-of-truth precedence — AFTER two-way semantic drift check passes (per S5)
- public/llms.txt + docs/cli/commands.md + package.json scripts updated
- STATE.md reflects Phase 228 complete + next-lane routing
- MARKOS-INDEX.md drift-checked (edited only if necessary)
- 22 new tests pass (8 + 14) — total slice 06 contribution
- Phase 228 lane CLOSED — commercial OS integration and future-readiness gates all enforced, signed off via `--strict` closeout gate (D-34 RH4 fix), discoverable; no semantic drift outstanding; vault-drift-report.json audit snapshot present (RM6); follow-up Phase 228.X entries exist for any deferred cells (D-34)
</success_criteria>

<output>
After completion, create `.planning/phases/228-commercial-os-integration-future-readiness/228-06-SUMMARY.md` documenting: vault canon sections added (with S5 two-way drift-check result), llms.txt + cli docs sections added, npm script aliases added, STATE.md update lines, MARKOS-INDEX.md disposition (edited or unchanged), final test count across all 6 slices.

Aggregate phase summary: total CLIs (5 user-facing + 1 supporting builder + 2 verifiers), total detectors (4 hard-failing per B2), total tests (~80+ across slices), total registry entries (66 with individual QA-NN linked_verifier per B4), total adapter bindings (7), total durable-object classes (36), parity gates enforced (10/10 + Chromatic per B3), readiness matrix signed off (with N3 strict-mode reconciliation), lane closed.
</output>
</output>
