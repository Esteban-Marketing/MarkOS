---
phase: 228-commercial-os-integration-future-readiness
plan: 02
type: execute
wave: 2
depends_on: ["228-01"]
files_modified:
  - lib/markos/outbound/providers/adapter-contract.ts
  - lib/markos/outbound/providers/swap-harness.js
  - lib/markos/contracts/adapter-registry.json
  - lib/markos/contracts/migration-manifest.json
  - scripts/commercial/test-adapter-swap.cjs
  - scripts/commercial/test-migration-roundtrip.cjs
  - test/commercial/adapter-swap.test.js
  - test/commercial/migration-roundtrip.test.js
autonomous: true
requirements:
  - COM-03
  - COM-04
  - QA-04
  - QA-05
  - QA-06
user_setup: []

# Preconditions per D-31, D-32 (hard route-back gate before any adapter binding registration)
# Plan 02 cannot register a binding unless the real adapter module exists in lib/markos/outbound/providers/
# Missing real adapter → route-back to upstream phase per D-01.
preconditions:
  required_real_adapter_modules:
    - { binding: email,                    real_module_path: "lib/markos/outbound/providers/resend-adapter.ts",   upstream_phase: 223 }
    - { binding: sms,                      real_module_path: "lib/markos/outbound/providers/twilio-adapter.ts",   upstream_phase: 223 }
    - { binding: whatsapp,                 real_module_path: "lib/markos/outbound/providers/twilio-adapter.ts",   upstream_phase: 223 }
    - { binding: analytics_sink,           real_module_path: "lib/markos/analytics/adapters/segment-adapter.ts",  upstream_phase: 225 }
    - { binding: ecosystem_directory,      real_module_path: "lib/markos/ecosystem/adapters/producthunt-adapter.ts", upstream_phase: 227 }
    - { binding: ecosystem_dev_events,     real_module_path: "lib/markos/ecosystem/adapters/discord-adapter.ts",     upstream_phase: 227 }
    - { binding: launch_distribution_email, real_module_path: "lib/markos/outbound/providers/resend-adapter.ts",  upstream_phase: 224 }
    - { binding: launch_distribution_chat,  real_module_path: "lib/markos/launch/adapters/slack-adapter.ts",      upstream_phase: 224 }
  fail_mode: "exit 1 with MISSING_REAL_ADAPTER_MODULE:<binding>=<path> — route gap back to phase <NNN> per CONTEXT D-01"
  composite_split_per_D32:
    note: "launch_distribution composite (resend+slack) is SPLIT into launch_distribution_email and launch_distribution_chat per D-32 — single composite binding form is REJECTED."

must_haves:
  truths:
    - "Every external-dependency engine (email, messaging, analytics sink, ecosystem connectors, launch distribution) declares an adapter contract that mirrors `lib/markos/outbound/providers/base-adapter.ts` and ships with a fail-closed swap test"
    - "Adapter swap harness can substitute provider B for provider A in any registered binding and assert send/normalize behavior is identical (same input → same observable contract output)"
    - "Every durable object across 221-227 declares a versioned export schema in `lib/markos/contracts/migration-manifest.json` with import/export round-trip test"
    - "Migration round-trip test (`scripts/commercial/test-migration-roundtrip.cjs`) re-imports an exported snapshot and asserts byte-equal payload modulo timestamp normalization for every engine"
    - "Adapter registry extends `lib/markos/outbound/providers/base-adapter.ts` doctrine — does NOT introduce a new adapter base class (per CONTEXT D-02 + code_context Integration Points)"
    - "Per D-32 (RH1 fix): every adapter-registry.json binding declares `swap_test_real_module_path` pointing at a real adapter file in repo (e.g., `lib/markos/outbound/providers/resend-adapter.ts`). `fs.existsSync(swap_test_real_module_path)` MUST resolve AND the path MUST NOT contain `_stub-` substring. Stub adapters synthesized from fixture JSON are FORBIDDEN as the proof source."
    - "Per D-32 (RH1 fix): `launch_distribution` composite is SPLIT into 8 distinct bindings: email, sms, whatsapp, analytics_sink, ecosystem_directory, ecosystem_dev_events, launch_distribution_email, launch_distribution_chat. Single composite binding form is REJECTED."
  artifacts:
    - path: "lib/markos/outbound/providers/adapter-contract.ts"
      provides: "Type-level adapter contract describing required methods (send, normalizeEvent, capabilities, swap-test hook) — extends existing base-adapter.ts"
      exports: ["AdapterContract", "ADAPTER_REQUIRED_METHODS", "validateAdapterContract"]
    - path: "lib/markos/outbound/providers/swap-harness.js"
      provides: "swapAdapter(channel, replacementAdapter) and runSwapTest(adapterA, adapterB, fixtures) — proves provider replaceability"
      exports: ["swapAdapter", "runSwapTest"]
    - path: "lib/markos/contracts/adapter-registry.json"
      provides: "List of every external-provider binding across 221-227 with current adapter, candidate replacements, and last-passing swap-test timestamp"
    - path: "lib/markos/contracts/migration-manifest.json"
      provides: "Per-engine versioned export schema declaration (engine, table, schema_version, exporter_path, importer_path, round_trip_fixture_path)"
    - path: "scripts/commercial/test-adapter-swap.cjs"
      provides: "CLI that runs swap test for one or all bindings; exits 0 when every binding has a passing swap test, exits 1 otherwise"
    - path: "scripts/commercial/test-migration-roundtrip.cjs"
      provides: "CLI that runs export → import → diff for every engine in migration-manifest.json"
    - path: "test/commercial/adapter-swap.test.js"
      provides: "node --test asserting swap-harness behavior and adapter-contract validation"
    - path: "test/commercial/migration-roundtrip.test.js"
      provides: "node --test asserting migration-manifest entries have valid exporter/importer paths and round-trip yields identical payload"
  key_links:
    - from: "lib/markos/outbound/providers/adapter-contract.ts"
      to: "lib/markos/outbound/providers/base-adapter.ts"
      via: "type contract over existing base — re-imports CHANNEL_CAPABILITIES + sendOutboundMessage signature"
      pattern: "from ['\"].*base-adapter['\"]"
    - from: "scripts/commercial/test-adapter-swap.cjs"
      to: "lib/markos/contracts/adapter-registry.json"
      via: "CLI iterates each binding, loads adapters via dynamic require, calls runSwapTest"
      pattern: "adapter-registry\\.json"
    - from: "scripts/commercial/test-migration-roundtrip.cjs"
      to: "lib/markos/contracts/migration-manifest.json"
      via: "CLI loads manifest, runs each engine's exporter then importer, diffs result"
      pattern: "migration-manifest\\.json"
---

<objective>
Establish provider adapter, export, and migration standards that close Gate 7 (no provider lock as architecture) and Gate 8 partial (no designed obsolescence — adapter side). Slice 03 closes the recovery/archival side of Gate 8.

Purpose: Today, `lib/markos/outbound/providers/base-adapter.ts` has the doctrine but there is no registry of bindings, no swap-test harness, and no migration manifest covering durable objects across 221-227. Without these, "provider replaceability" and "no designed obsolescence" stay slogans (CONTEXT specifics line 153 + research recommended path step 3). This slice extends the existing base — does NOT introduce a parallel base.

Output: adapter-contract type + swap harness + adapter registry + migration manifest + two CLI gates + two test files that prove every binding can be swapped and every durable object can round-trip.
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/228-commercial-os-integration-future-readiness/228-CONTEXT.md
@.planning/phases/228-commercial-os-integration-future-readiness/228-RESEARCH.md
@.planning/phases/228-commercial-os-integration-future-readiness/228-01-PLAN.md
@.planning/V4.2.0-INCOMING-18-26-GSD-DISCUSS-RESEARCH-ROUTING.md
@.planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md
@.planning/phases/225-analytics-attribution-narrative-intelligence/225-CONTEXT.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-CONTEXT.md
@lib/markos/outbound/providers/base-adapter.ts
@lib/markos/plugins/registry.js

<interfaces>
<!-- The executor extends these contracts directly. -->

From lib/markos/outbound/providers/base-adapter.ts:
```ts
const CHANNEL_CAPABILITIES = Object.freeze({
  email:    { channel: 'email',    provider: 'resend', supports_subject: true,  supports_templates: true, supports_replies: true },
  sms:      { channel: 'sms',      provider: 'twilio', supports_subject: false, supports_templates: false, supports_replies: true },
  whatsapp: { channel: 'whatsapp', provider: 'twilio', supports_subject: false, supports_templates: true, supports_replies: true },
});
function getOutboundChannelCapabilities(channel);
async function sendOutboundMessage(adapter, message);     // adapter MUST have .send(message)
function normalizeOutboundProviderEvent(adapter, payload); // adapter MUST have .normalizeEvent(payload)
```

Required adapter contract this plan defines:
```ts
interface AdapterContract {
  channel: string;                       // e.g., 'email', 'sms', 'analytics_sink', 'launch_distribution', 'ecosystem_directory'
  provider: string;                      // e.g., 'resend', 'twilio', 'segment', 'producthunt'
  send(message: object): Promise<{ id: string, status: 'queued'|'sent'|'failed', provider_id?: string, error?: string }>;
  normalizeEvent(payload: object): { kind: string, occurred_at: string, ref?: string, raw: object };
  capabilities(): { channel: string, provider: string, [k: string]: unknown };
  // NEW for swap-test:
  swapTestFixtures?(): { send_inputs: object[], event_payloads: object[] };  // per-adapter fixtures for swap parity
}
```

Bindings to register in adapter-registry.json (extracted from sibling phase CONTEXTs):
| Channel | Engine | Current adapter | Candidate replacement |
|---|---|---|---|
| email | 223 (EML) | `resend` | `postmark`, `sendgrid` |
| sms | 223 (MSG) | `twilio` | `messagebird`, `vonage` |
| whatsapp | 223 (MSG) | `twilio` | `360dialog`, `messagebird` |
| analytics_sink | 225 (ANL) | `segment` | `rudderstack`, `customerio_data_pipelines` |
| ecosystem_directory | 227 (ECO) | `producthunt` | `appsumo`, `g2` |
| ecosystem_dev_events | 227 (ECO) | `discord` | `slack`, `pagerduty_events` |
| launch_distribution_email | 224 (LCH) | `resend` (announce) | `postmark`, `sendgrid` |
| launch_distribution_chat  | 224 (LCH) | `slack` (notify)    | `mattermost`, `discord` |
Total: 8 bindings (per D-32 — `launch_distribution` composite SPLIT into per-component bindings; single composite REJECTED).

Migration manifest entries (one per engine, per durable object class):
- cdp/identity_profile (schema_version 1)
- cdp/audience_snapshot (schema_version 1)
- cdp/consent_state (schema_version 1)
- crm/timeline_event (schema_version 1)
- crm/account, crm/person, crm/opportunity (schema_version 1 each)
- email/template, email/dispatch_record, email/suppression (schema_version 1 each)
- messaging/thread, messaging/message, messaging/suppression (schema_version 1 each)
- conversion/funnel_event, conversion/landing, conversion/form, conversion/offer (schema_version 1 each)
- launch/plan, launch/rollout, launch/rollback_record (schema_version 1 each)
- analytics/event, analytics/metric_snapshot, analytics/attribution_touch (schema_version 1 each)
- sales/deal_brief, sales/proof_pack, sales/quote, sales/winloss_record, sales/handoff_record (schema_version 1 each)
- ecosystem/integration_listing, ecosystem/partner_profile, ecosystem/referral, ecosystem/affiliate_program, ecosystem/community_signal, ecosystem/developer_event, ecosystem/certification_record, ecosystem/co_sell_opportunity (schema_version 1 each)
Total: 30 durable-object classes minimum.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define adapter contract type + swap harness + adapter-registry.json</name>
  <files>
    lib/markos/outbound/providers/adapter-contract.ts,
    lib/markos/outbound/providers/swap-harness.js,
    lib/markos/contracts/adapter-registry.json,
    test/commercial/adapter-swap.test.js
  </files>
  <read_first>
    - lib/markos/outbound/providers/base-adapter.ts (full file — do NOT introduce a new base)
    - .planning/phases/228-commercial-os-integration-future-readiness/228-CONTEXT.md (D-13 Gate 7, D-25 Replaceable-adapter tests)
    - .planning/V4.2.0-INCOMING-18-26-GSD-DISCUSS-RESEARCH-ROUTING.md lines 113-126 (Gate 7 verbatim)
    - .planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md (current email/messaging adapters)
    - .planning/phases/225-analytics-attribution-narrative-intelligence/225-CONTEXT.md (analytics sink decisions)
    - .planning/phases/227-ecosystem-partner-community-developer-growth/227-CONTEXT.md (ecosystem connector adapters — webhook adapters listed in 227-05)
    - lib/markos/contracts/commercial-registry.js (Task 1 of slice 01 — registry doctrine to mirror)
  </read_first>
  <behavior>
    - Test 1: `validateAdapterContract(adapter)` returns `{ ok: true }` for an object with required methods (send, normalizeEvent, capabilities)
    - Test 2: `validateAdapterContract` returns `{ ok: false, code: 'MISSING_SEND' }` when `send` is not a function
    - Test 3: `validateAdapterContract` returns `{ ok: false, code: 'MISSING_NORMALIZE_EVENT' }` when `normalizeEvent` is not a function
    - Test 4: `validateAdapterContract` returns `{ ok: false, code: 'MISSING_CAPABILITIES' }` when `capabilities` is not a function
    - Test 5: `runSwapTest(adapterA, adapterB, fixtures)` returns `{ ok: true, channel, provider_a, provider_b, send_results, event_results }` when both adapters produce same status enum + same normalized event kinds for given fixtures
    - Test 6: `runSwapTest` returns `{ ok: false, code: 'SEND_STATUS_MISMATCH' }` when adapter A returns `'sent'` and adapter B returns `'failed'` for the same fixture
    - Test 7: `runSwapTest` returns `{ ok: false, code: 'EVENT_KIND_MISMATCH' }` when adapter A normalizes payload to kind `'delivered'` and adapter B to `'received'`
    - Test 8: `swapAdapter(channel, replacement)` only accepts a replacement that passes `validateAdapterContract`; throws `INVALID_ADAPTER_CONTRACT:<code>` otherwise
    - Test 9: `adapter-registry.json` is loadable, contains exactly 8 bindings (per D-32 split), every binding has fields `channel`, `engine`, `current_adapter`, `candidate_replacements[]`, `swap_test_fixture_path`, `swap_test_real_module_path` (per D-32), `last_passing_swap_test_at`
    - Test 10 (NEW per S2): `adapter-registry.json` declares EXACTLY one entry per channel, with the 8 channels being a Set of size 8 (proves no duplicates and covers all 8 distinct channels per D-32 split)
    - Test 11 (NEW per D-32, RH1 fix): every binding's `swap_test_real_module_path` resolves via `fs.existsSync` AND does NOT contain `_stub-` substring (proves real-adapter-module binding, not synthesized stub)
  </behavior>
  <action>
    Create `lib/markos/outbound/providers/adapter-contract.ts` exporting:
    - `ADAPTER_REQUIRED_METHODS = Object.freeze(['send', 'normalizeEvent', 'capabilities'])` — frozen array of required method names
    - `interface AdapterContract` (TypeScript shape per the `<interfaces>` block above)
    - `validateAdapterContract(adapter): { ok: true } | { ok: false, code: 'MISSING_SEND'|'MISSING_NORMALIZE_EVENT'|'MISSING_CAPABILITIES'|'INVALID_CHANNEL', detail: string }` — checks each method is `typeof === 'function'` and `capabilities().channel` matches one of the registry channels.
    - Re-export `getOutboundChannelCapabilities` from `./base-adapter.ts` so consumers continue to use the canonical capability map.

    Create `lib/markos/outbound/providers/swap-harness.js` exporting:
    - `swapAdapter(channel, replacementAdapter)` — validates contract; throws on failure; returns `{ channel, swapped_to: replacementAdapter.capabilities().provider }`
    - `runSwapTest(adapterA, adapterB, fixtures)` — fixtures = `{ send_inputs: object[], event_payloads: object[] }`. For each `send_input`, calls both adapters' `send`; returns `ok: false` with `SEND_STATUS_MISMATCH` if status enums differ. For each `event_payload`, calls both `normalizeEvent`; returns `ok: false` with `EVENT_KIND_MISMATCH` if `kind` differs. Returns `{ ok: true, channel, provider_a, provider_b, send_results: [...], event_results: [...] }` on success.

    Create `lib/markos/contracts/adapter-registry.json` as a JSON array of exactly 8 bindings (per D-32 split — channels: email, sms, whatsapp, analytics_sink, ecosystem_directory, ecosystem_dev_events, launch_distribution_email, launch_distribution_chat). For each binding include:
    - `swap_test_fixture_path` pointing at `test/commercial/fixtures/adapter-swap-<channel>.json`
    - **`swap_test_real_module_path` (NEW per D-32, RH1 fix)** pointing at the real adapter module file in repo (e.g., `lib/markos/outbound/providers/resend-adapter.ts` for `email` and `launch_distribution_email`; `lib/markos/outbound/providers/twilio-adapter.ts` for `sms` and `whatsapp`; `lib/markos/analytics/adapters/segment-adapter.ts` for `analytics_sink`; `lib/markos/ecosystem/adapters/producthunt-adapter.ts` for `ecosystem_directory`; `lib/markos/ecosystem/adapters/discord-adapter.ts` for `ecosystem_dev_events`; `lib/markos/launch/adapters/slack-adapter.ts` for `launch_distribution_chat`)
    - `last_passing_swap_test_at: null` placeholder

    Task 2 will populate fixtures AND wire the swap test against the real module paths. If a real module file does NOT exist, Task 2 emits the route-back per D-01 — this slice does NOT create stub adapter files to satisfy the precondition.

    Create `test/commercial/adapter-swap.test.js` using `node --test` with all 10 tests above. Use stub adapter objects (in-test object literals) — do NOT require real Resend/Twilio packages.
  </action>
  <verify>
    <automated>node --test test/commercial/adapter-swap.test.js</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `lib/markos/outbound/providers/adapter-contract.ts`
    - File exists: `lib/markos/outbound/providers/swap-harness.js`
    - File exists: `lib/markos/contracts/adapter-registry.json`
    - File exists: `test/commercial/adapter-swap.test.js`
    - `node --test test/commercial/adapter-swap.test.js` exits 0 with 11 tests passing (10 original + 1 new per D-32 real-module check)
    - `node -e "console.log(require('./lib/markos/contracts/adapter-registry.json').length)"` prints `8` (per D-32 split)
    - `grep -c '"channel":' lib/markos/contracts/adapter-registry.json` returns 8
    - **Per S2 fix + D-32 split — assert exactly 8 DISTINCT channels:**
      - `grep -c '"channel": "email"' lib/markos/contracts/adapter-registry.json` returns 1
      - `grep -c '"channel": "sms"' lib/markos/contracts/adapter-registry.json` returns 1
      - `grep -c '"channel": "whatsapp"' lib/markos/contracts/adapter-registry.json` returns 1
      - `grep -c '"channel": "analytics_sink"' lib/markos/contracts/adapter-registry.json` returns 1
      - `grep -c '"channel": "ecosystem_directory"' lib/markos/contracts/adapter-registry.json` returns 1
      - `grep -c '"channel": "ecosystem_dev_events"' lib/markos/contracts/adapter-registry.json` returns 1
      - `grep -c '"channel": "launch_distribution_email"' lib/markos/contracts/adapter-registry.json` returns 1
      - `grep -c '"channel": "launch_distribution_chat"' lib/markos/contracts/adapter-registry.json` returns 1
      - `node -e "const r=require('./lib/markos/contracts/adapter-registry.json'); const channels=new Set(r.map(e=>e.channel)); if(channels.size!==8){console.error('expected 8 distinct channels, got',channels.size,Array.from(channels)); process.exit(1);} const expected=['email','sms','whatsapp','analytics_sink','ecosystem_directory','ecosystem_dev_events','launch_distribution_email','launch_distribution_chat']; for(const c of expected){if(!channels.has(c)){console.error('missing channel',c); process.exit(1);}} console.log('OK 8 distinct channels');"` exits 0
    - `grep -c "from.*base-adapter" lib/markos/outbound/providers/adapter-contract.ts` returns >= 1 (extends existing base, does not replace)
    - `grep -c "ADAPTER_REQUIRED_METHODS" lib/markos/outbound/providers/adapter-contract.ts` returns >= 1
    - `grep -c "validateAdapterContract\|swapAdapter\|runSwapTest" lib/markos/outbound/providers/swap-harness.js` returns >= 2
    - **Per D-32 (RH1 fix): every binding has `swap_test_real_module_path` declared and resolvable:**
      `node -e "const fs=require('fs'); const r=require('./lib/markos/contracts/adapter-registry.json'); for(const b of r){ if(typeof b.swap_test_real_module_path !== 'string' || b.swap_test_real_module_path.includes('_stub-')){ console.error('binding',b.channel,'missing real-module path or in stub namespace'); process.exit(1); } } console.log('OK 8 bindings have real-module path declarations'); "` exits 0
  </acceptance_criteria>
  <done>
    Adapter contract + swap harness + adapter-registry.json exist; 8 bindings declared (D-32 split); every binding has real-module path declared; 11-test contract suite passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement test-adapter-swap.cjs CLI gate (Gate 7 enforcement)</name>
  <files>
    scripts/commercial/test-adapter-swap.cjs,
    test/commercial/fixtures/adapter-swap-email.json,
    test/commercial/fixtures/adapter-swap-sms.json,
    test/commercial/fixtures/adapter-swap-whatsapp.json,
    test/commercial/fixtures/adapter-swap-analytics_sink.json,
    test/commercial/fixtures/adapter-swap-ecosystem_directory.json,
    test/commercial/fixtures/adapter-swap-ecosystem_dev_events.json,
    test/commercial/fixtures/adapter-swap-launch_distribution_email.json,
    test/commercial/fixtures/adapter-swap-launch_distribution_chat.json
  </files>
  <read_first>
    - lib/markos/outbound/providers/swap-harness.js (created Task 1)
    - lib/markos/contracts/adapter-registry.json (created Task 1)
    - lib/markos/outbound/providers/base-adapter.ts (canonical capabilities)
    - .planning/phases/228-commercial-os-integration-future-readiness/228-CONTEXT.md (D-03 provider-replaceability proof, D-25 swap-test patterns)
    - .planning/phases/228-commercial-os-integration-future-readiness/228-01-PLAN.md (parity-verifier exit-code conventions to mirror)
    - .planning/phases/228-commercial-os-integration-future-readiness/228-05-PLAN.md (slice 05 N2 — `--engine` flag will be added by slice 05; this task's `--channel` flag is the foundation)
  </read_first>
  <behavior>
    - Behavior 1: `node scripts/commercial/test-adapter-swap.cjs` runs swap test for every binding in adapter-registry.json and exits 0 if every binding passes
    - Behavior 2: `node scripts/commercial/test-adapter-swap.cjs --channel email` runs swap test for one channel only
    - Behavior 3: `node scripts/commercial/test-adapter-swap.cjs --format json` outputs JSON `{ summary: {total, passed, failed}, results: [...] }`
    - Behavior 4: when any binding's `runSwapTest` returns `ok: false`, CLI exits 1 with stderr containing the error code (`SEND_STATUS_MISMATCH` or `EVENT_KIND_MISMATCH`)
    - **Behavior 4b (NEW per D-32, RH1 fix): hard precondition** — for each binding, BEFORE attempting any swap test, CLI calls `fs.existsSync(binding.swap_test_real_module_path)`. If missing → exit 1 with stderr `MISSING_REAL_ADAPTER_MODULE:<channel>=<path> — route gap back to phase <NNN> per CONTEXT D-01.` Engine→phase: email/launch_distribution_email=223/224 (resend), sms/whatsapp=223 (twilio), analytics_sink=225, ecosystem_directory/ecosystem_dev_events=227, launch_distribution_chat=224. The CLI does NOT synthesize stub adapters from fixture JSON when real modules are missing — it routes back per D-01.
    - Behavior 5: when registry-binding's `swap_test_fixture_path` points to non-existent file, CLI exits 2 with stderr `MISSING_SWAP_TEST_FIXTURE:<channel>`
    - Behavior 6: on success, CLI updates `last_passing_swap_test_at` to current ISO timestamp in adapter-registry.json (idempotent — only writes if all bindings pass; uses `--no-write` flag to skip the update for CI dry-runs)
  </behavior>
  <action>
    Create 8 fixture files under `test/commercial/fixtures/adapter-swap-<channel>.json` (per D-32 split — channels: email, sms, whatsapp, analytics_sink, ecosystem_directory, ecosystem_dev_events, launch_distribution_email, launch_distribution_chat). Each fixture has shape:
    ```json
    {
      "channel": "email",
      "send_inputs": [
        { "to": "test@example.com", "subject": "fixture", "body": "hello" }
      ],
      "event_payloads": [
        { "type": "email.delivered", "id": "evt_1", "ts": "2026-04-25T00:00:00Z" }
      ],
      "stub_adapter_a": { "provider": "resend", "send_status": "queued", "event_kind": "delivered" },
      "stub_adapter_b": { "provider": "postmark", "send_status": "queued", "event_kind": "delivered" }
    }
    ```
    The CLI synthesizes adapter A and adapter B from the `stub_adapter_a/b` fields (this is a contract test, not an integration test — slice 04 may layer real adapters later).

    Create `scripts/commercial/test-adapter-swap.cjs`:
    1. Parse `--channel <c>`, `--format json|table`, `--no-write` flags. (Slice 05 will add `--engine <e>` — this task ensures the parser is extensible.)
    2. Load `lib/markos/contracts/adapter-registry.json`.
    3. **Per D-32 hard precondition (RH1 fix)** — for each binding (or filtered by `--channel`), call `fs.existsSync(binding.swap_test_real_module_path)`. If missing → exit 1 with `MISSING_REAL_ADAPTER_MODULE:<channel>=<path>` and route-back-to-upstream-phase message. **CLI does NOT proceed with stub synthesis when real modules absent** — it routes back per D-01.
    4. Load fixture file at `swap_test_fixture_path`. If missing → exit 2 with `MISSING_SWAP_TEST_FIXTURE:<channel>`.
    5. **Adapter A: REAL** — dynamic-`require` the real adapter module at `swap_test_real_module_path`. Use the real adapter's `send`, `normalizeEvent`, `capabilities` methods. (Type-script paths are resolved via `tsx`/`ts-node` if needed — Plan 04 has installed Vitest; the swap CLI may invoke via vitest's runtime.)
    6. **Adapter B: candidate replacement (conformance proof)** — for the swap test, instantiate a candidate replacement adapter from `binding.candidate_replacements[0]`. If a real adapter for that candidate exists in repo (`fs.existsSync` via convention `lib/markos/<engine>/adapters/<candidate>-adapter.ts`) → use it. Otherwise → use a strict conformance harness that wraps the real Adapter A's contract and substitutes the candidate's provider name (proves the contract is provider-agnostic; explicitly documented in the swap report as `b_kind: "conformance_harness"` so review can spot it). The fixture's `stub_adapter_b` block becomes the candidate-replacement contract spec, NOT a synthesized adapter — the harness reads expected `send_status` and `event_kind` and asserts the real Adapter A produces equivalent output for the fixture inputs.
    7. Call `runSwapTest(adapterA, adapterB, { send_inputs: fixture.send_inputs, event_payloads: fixture.event_payloads })`. Capture B's `b_kind` in the result so reviewers see whether B was a real adapter module or a conformance harness.
    8. Aggregate results. If all `ok: true` and `--no-write` not set → update `last_passing_swap_test_at` for each binding to current ISO timestamp + write file. Exit 0.
    9. Otherwise exit 1 with stderr listing failed channels and error codes.

    Extend `test/commercial/adapter-swap.test.js` (modify) with 4 additional tests:
    - Test 11: spawn CLI with no flags, expect exit 0
    - Test 12: spawn CLI with `--channel email`, expect exit 0 + stdout contains `email`
    - Test 13: spawn CLI with `--format json`, parse JSON, assert `summary.total === 7`
    - Test 14: temporarily move one fixture file → spawn CLI → expect exit 2 + stderr contains `MISSING_SWAP_TEST_FIXTURE`
  </action>
  <verify>
    <automated>node scripts/commercial/test-adapter-swap.cjs --no-write && node --test test/commercial/adapter-swap.test.js</automated>
  </verify>
  <acceptance_criteria>
    - 8 fixture files exist under `test/commercial/fixtures/adapter-swap-<channel>.json` (per D-32 split)
    - File exists: `scripts/commercial/test-adapter-swap.cjs`
    - `node scripts/commercial/test-adapter-swap.cjs --no-write` exits 0
    - `node scripts/commercial/test-adapter-swap.cjs --channel email --no-write` exits 0
    - `node scripts/commercial/test-adapter-swap.cjs --format json --no-write` outputs valid JSON with `summary.total === 8`
    - `node --test test/commercial/adapter-swap.test.js` exits 0 with 15 tests passing (11 from Task 1 + 4 added here)
    - `grep -c "MISSING_SWAP_TEST_FIXTURE" scripts/commercial/test-adapter-swap.cjs` returns >= 1
    - `grep -c "last_passing_swap_test_at" scripts/commercial/test-adapter-swap.cjs` returns >= 1
    - **Per D-32 (RH1 fix): real-module precondition asserted in CLI source:**
      - `grep -c "MISSING_REAL_ADAPTER_MODULE" scripts/commercial/test-adapter-swap.cjs` returns >= 1
      - `grep -c "swap_test_real_module_path" scripts/commercial/test-adapter-swap.cjs` returns >= 1
      - `grep -c "route gap back" scripts/commercial/test-adapter-swap.cjs` returns >= 1
    - **Per D-32: launch_distribution composite REJECTED — no entry with `"channel": "launch_distribution"` (without `_email` or `_chat` suffix) exists:**
      - `grep -E '"channel": "launch_distribution"[ ,}]' lib/markos/contracts/adapter-registry.json | wc -l` returns 0
  </acceptance_criteria>
  <done>
    Adapter swap CLI exits 0 across all 8 bindings (D-32 split), fail-closed on missing real-module path (D-32) AND missing fixture, idempotent on re-run, 15 tests prove the gate.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migration manifest + round-trip CLI gate (Gate 8 partial — adapter side)</name>
  <files>
    lib/markos/contracts/migration-manifest.json,
    scripts/commercial/test-migration-roundtrip.cjs,
    test/commercial/migration-roundtrip.test.js,
    test/commercial/fixtures/migration/cdp-identity_profile.json,
    test/commercial/fixtures/migration/crm-timeline_event.json,
    test/commercial/fixtures/migration/email-template.json,
    test/commercial/fixtures/migration/messaging-thread.json,
    test/commercial/fixtures/migration/conversion-funnel_event.json,
    test/commercial/fixtures/migration/launch-plan.json,
    test/commercial/fixtures/migration/analytics-event.json,
    test/commercial/fixtures/migration/sales-deal_brief.json,
    test/commercial/fixtures/migration/ecosystem-integration_listing.json
  </files>
  <read_first>
    - .planning/phases/228-commercial-os-integration-future-readiness/228-CONTEXT.md (D-14 Gate 8, D-26 migration/export invariants)
    - .planning/V4.2.0-INCOMING-18-26-GSD-DISCUSS-RESEARCH-ROUTING.md lines 113-126 (Gate 8 verbatim)
    - obsidian/reference/Database Schema.md (versioned schema posture)
    - .planning/phases/221-cdp-identity-audience-consent-substrate/221-CONTEXT.md (CDP durable objects)
    - .planning/phases/227-ecosystem-partner-community-developer-growth/227-CONTEXT.md (ecosystem durable objects + tombstone cascade)
    - .planning/phases/228-commercial-os-integration-future-readiness/228-01-PLAN.md (registry doctrine + exit-code conventions)
  </read_first>
  <behavior>
    - Behavior 1: `lib/markos/contracts/migration-manifest.json` is a JSON array with one entry per durable-object class listed in `<interfaces>` block (minimum 30 entries, but this task ships 9 representative entries — one per engine — and slice 03 expands to full coverage)
    - Behavior 2: each manifest entry has `engine`, `object`, `schema_version`, `exporter_path`, `importer_path`, `round_trip_fixture_path`, `last_passing_roundtrip_at`
    - Behavior 3: for representative coverage (1 per engine), the round-trip CLI loads fixture, calls a stub exporter (returns the fixture), calls a stub importer (re-parses the export and returns canonical form), diffs, exits 0 if byte-equal modulo timestamps
    - Behavior 4: round-trip CLI exits 1 with `ROUND_TRIP_DIFF:<engine>/<object>` if export → import does not yield identical canonical payload
    - Behavior 5: round-trip CLI exits 2 if exporter_path or importer_path or fixture file is missing
    - Behavior 6: `node scripts/commercial/test-migration-roundtrip.cjs --engine cdp` runs only CDP round-trip
    - Behavior 7: `node scripts/commercial/test-migration-roundtrip.cjs --format json` returns `{ summary, results }` JSON
  </behavior>
  <action>
    Create `lib/markos/contracts/migration-manifest.json` with 9 representative entries (one per engine — cdp/identity_profile, crm/timeline_event, email/template, messaging/thread, conversion/funnel_event, launch/plan, analytics/event, sales/deal_brief, ecosystem/integration_listing). For this slice, use stub exporter/importer paths pointing at `lib/markos/contracts/migration-stubs/<engine>-<object>.cjs` (the stub file just re-exports the fixture). Slice 03 will replace stubs with real exporters when expanding coverage to 30 entries.

    Create stub files at `lib/markos/contracts/migration-stubs/<engine>-<object>.cjs` (9 files) — each exports `{ exportSnapshot(input), importSnapshot(serialized) }` where exportSnapshot returns `JSON.parse(JSON.stringify(input))` and importSnapshot returns `JSON.parse(serialized)`. This is the parity contract slice 03 will enforce against real exporters.

    Create 9 fixture JSON files under `test/commercial/fixtures/migration/<engine>-<object>.json` — each contains a representative durable-object record with all required fields per sibling phase CONTEXT.md schema.

    Create `scripts/commercial/test-migration-roundtrip.cjs`:
    1. Parse `--engine <e>`, `--format json|table` flags.
    2. Load `migration-manifest.json`.
    3. For each entry (or filtered): require exporter_path, require importer_path, load fixture.
    4. Call `exporter.exportSnapshot(fixture)` → `serialized`. Call `importer.importSnapshot(JSON.stringify(serialized))` → `restored`.
    5. Normalize both: strip `*_at`/`*_timestamp`/`exported_at` keys (deep), JSON-stringify with sorted keys.
    6. Compare normalized strings. If equal → `ok: true`. If different → `ok: false, code: 'ROUND_TRIP_DIFF', diff: <first 200 chars of diff>`.
    7. Aggregate. Exit 0 if all ok, exit 1 if any diff, exit 2 if any path/fixture missing.

    Create `test/commercial/migration-roundtrip.test.js`:
    - Test 1: spawn CLI no flags, expect exit 0
    - Test 2: spawn CLI `--engine cdp`, expect exit 0 + stdout contains `cdp`
    - Test 3: spawn CLI `--format json`, parse JSON, assert `summary.total === 9`
    - Test 4: temporarily corrupt one fixture (write file with extra non-timestamp field), spawn CLI, expect exit 1 + stderr `ROUND_TRIP_DIFF`
    - Test 5: temporarily move stub exporter, spawn CLI, expect exit 2
    - Test 6: assert `migration-manifest.json` length === 9 AND every entry has all 7 required fields
  </action>
  <verify>
    <automated>node scripts/commercial/test-migration-roundtrip.cjs && node --test test/commercial/migration-roundtrip.test.js</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `lib/markos/contracts/migration-manifest.json`
    - File exists: `scripts/commercial/test-migration-roundtrip.cjs`
    - File exists: `test/commercial/migration-roundtrip.test.js`
    - 9 stub files exist under `lib/markos/contracts/migration-stubs/`
    - 9 fixture files exist under `test/commercial/fixtures/migration/`
    - `node scripts/commercial/test-migration-roundtrip.cjs` exits 0
    - `node -e "console.log(require('./lib/markos/contracts/migration-manifest.json').length)"` prints `9`
    - `node --test test/commercial/migration-roundtrip.test.js` exits 0 with 6 tests passing
    - `grep -c "ROUND_TRIP_DIFF" scripts/commercial/test-migration-roundtrip.cjs` returns >= 1
    - `node -e "const m=require('./lib/markos/contracts/migration-manifest.json'); const engines=new Set(m.map(e=>e.engine)); const expected=['cdp','crm','email','messaging','conversion','launch','analytics','sales','ecosystem']; for(const e of expected){if(!engines.has(e)){console.error('missing engine',e); process.exit(1);}} if(engines.size!==9){console.error('expected 9 distinct engines, got',engines.size); process.exit(1);} console.log('OK 9 distinct engines');"` exits 0
  </acceptance_criteria>
  <done>
    Migration manifest covers all 9 engines with stub exporters that round-trip identically; CLI gate passes; 6 tests prove the gate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stub adapter -> swap harness | stub adapter parity does not prove real provider parity; this is intentional (contract test, not integration test) |
| migration manifest -> stub exporters | stub exporters re-emit input; real exporters land in slice 03 — parity contract is what binds them |
| adapter-registry.json -> CLI | tampered registry could mask missing bindings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-228-02-01 | Tampering | adapter-registry.json | mitigate | CLI re-validates each binding has `swap_test_fixture_path`; missing fixture exits 2; per S2 fix Set-size assertion proves 7 distinct channels |
| T-228-02-02 | Tampering | migration-manifest.json | mitigate | CLI re-validates `exporter_path` + `importer_path` exist; missing exits 2 |
| T-228-02-03 | Information disclosure | fixture files | accept | fixtures are synthetic; no real PII |
| T-228-02-04 | Repudiation | last_passing_swap_test_at written by CLI | mitigate | `--no-write` flag for CI dry-runs; production CI uses default mode and checks-in updated registry |
| T-228-02-05 | Denial of service | CLI hangs on real provider call | mitigate | this slice uses stub adapters only; real-provider integration tests are slice 04 scope |
| T-228-02-06 | Elevation of privilege | new adapter base introduced | mitigate | code review acceptance criterion: `grep -c "from.*base-adapter" lib/markos/outbound/providers/adapter-contract.ts` MUST be >= 1 (extends, does not replace) |
</threat_model>

<verification>
- `node --test test/commercial/adapter-swap.test.js` exits 0 (14 tests)
- `node --test test/commercial/migration-roundtrip.test.js` exits 0 (6 tests)
- `node scripts/commercial/test-adapter-swap.cjs --no-write` exits 0 (7 bindings pass)
- `node scripts/commercial/test-migration-roundtrip.cjs` exits 0 (9 engines round-trip)
- adapter-registry.json contains 7 distinct channels (Set-size assertion per S2 fix)
- migration-manifest.json contains 9 distinct engine entries
- adapter-contract.ts EXTENDS base-adapter.ts (re-exports getOutboundChannelCapabilities)
</verification>

<success_criteria>
- Gate 7 (no provider lock): every external-dependency binding has a passing swap test
- Gate 8 partial (no designed obsolescence — adapter side): every engine has a round-trip-tested durable-object class
- 20 new tests pass (14 + 6)
- No new adapter base class introduced (extends `base-adapter.ts`)
- CLI gates exit-code semantics match slice 01 (0 = clean, 1 = violation, 2 = missing precondition)
- S2 fix: 7-distinct-channel Set-size assertion replaces single grep alternation that counted matches not distinct channels
</success_criteria>

<output>
After completion, create `.planning/phases/228-commercial-os-integration-future-readiness/228-02-SUMMARY.md` documenting: 7 adapter bindings (with Set-size proof), 9 engine round-trips, exit-code semantics, the contract for slice 03 (expand migration-manifest.json from 9 → 30 entries with archival/recovery layer), and slice 05 (add `--engine` flag per N2).
</output>
</output>
