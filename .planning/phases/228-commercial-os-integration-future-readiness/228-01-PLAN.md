---
phase: 228-commercial-os-integration-future-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/markos/contracts/commercial-registry.json
  - lib/markos/contracts/commercial-registry.js
  - lib/markos/contracts/commercial-registry-validator.js
  - scripts/commercial/build-commercial-registry.cjs
  - scripts/commercial/verify-parity-matrix.cjs
  - test/commercial/contract-registry.test.js
  - test/commercial/parity-matrix.test.js
autonomous: true
requirements:
  - COM-01
  - COM-02
  - QA-01
  - QA-02
  - QA-03
user_setup: []

# Preconditions per D-31 (hard route-back gate before any registry creation)
# Plan 01 cannot begin until upstream engine API surface dirs exist.
# Missing dir → Task 0 exit 1 with route-back per D-01.
preconditions:
  required_engine_api_dirs:
    - { engine: cdp,        path: "app/api/v1/cdp/",        upstream_phase: 221 }
    - { engine: crm,        path: "app/api/v1/crm/",        upstream_phase: 222 }
    - { engine: email,      path: "app/api/v1/email/",      upstream_phase: 223 }
    - { engine: messaging,  path: "app/api/v1/messaging/",  upstream_phase: 223 }
    - { engine: conversion, path: "app/api/v1/conversion/", upstream_phase: 224 }
    - { engine: launch,     path: "app/api/v1/launch/",     upstream_phase: 224 }
    - { engine: analytics,  path: "app/api/v1/analytics/",  upstream_phase: 225 }
    - { engine: sales,      path: "app/api/v1/sales/",      upstream_phase: 226 }
    - { engine: ecosystem,  path: "app/api/v1/ecosystem/",  upstream_phase: 227 }
  fail_mode: "exit 1 with MISSING_ENGINE_API_DIR:<engine> — route gap back to phase <NNN> per CONTEXT D-01"

must_haves:
  truths:
    - "Every F-ID across phases 221-227 (CDP-01..05, CRM-01..05, EML-01..05, MSG-01..05, CNV-01..05, LCH-01..05, ANL-01..05, SEN-01..05, ECO-01..05) is registered in lib/markos/contracts/commercial-registry.json with engine, surface posture (UI/API/MCP), and either contract reference or auditable deferral reason"
    - "Parity verifier (scripts/commercial/verify-parity-matrix.cjs) exits non-zero when any F-ID lacks UI/API/MCP coverage without a registered deferral_reason"
    - "Registry is fail-closed: contract entries with missing required fields throw INVALID_COMMERCIAL_CONTRACT (mirrors plugins/registry.js validatePluginContract pattern)"
    - "Identity-truth gate (Gate 1) and commercial-memory gate (Gate 2) verifiable: only phase 221 registers identity/profile/consent/audience F-IDs; only phase 222 registers timeline/account/person/opportunity F-IDs — verifier flags duplicates"
    - "Registry extends `lib/markos/plugins/registry.js` doctrine (insertion-order Map, frozen entries, idempotent registration); does NOT introduce a parallel store"
    - "Per D-31 (RH3 fix): Task 0 hard precondition asserts every `app/api/v1/<engine>/` dir exists via `fs.existsSync` BEFORE any registry creation; missing dir → exit 1 with route-back per D-01. Plan 01 cannot accumulate synthetic registry entries against absent upstream surfaces."
  artifacts:
    - path: "lib/markos/contracts/commercial-registry.json"
      provides: "Canonical contract registry with one entry per F-ID across 221-227"
      contains: "every F-ID listed in goal_backward.truths above (CDP-01..05, CRM-01..05, EML-01..05, MSG-01..05, CNV-01..05, LCH-01..05, ANL-01..05, SEN-01..05, ECO-01..05)"
    - path: "lib/markos/contracts/commercial-registry.js"
      provides: "createCommercialRegistry, registerCommercialContract, listContracts, resolveContract, listByEngine, listBySurface (mirrors plugins/registry.js export shape)"
      exports: ["createCommercialRegistry", "registerCommercialContract", "listContracts", "resolveContract", "listByEngine", "listBySurface"]
    - path: "lib/markos/contracts/commercial-registry-validator.js"
      provides: "validateCommercialContract — required fields f_id, engine, ui_path, api_path, mcp_verb, deferred_reason (mutually exclusive with the path fields)"
      exports: ["validateCommercialContract"]
    - path: "scripts/commercial/verify-parity-matrix.cjs"
      provides: "CLI gate that loads commercial-registry.json and prints any F-ID with missing surface coverage that lacks a deferral_reason; exit 0 if clean, exit 1 otherwise"
    - path: "test/commercial/contract-registry.test.js"
      provides: "Vitest-style assertions over registry validator + duplicate-engine-ownership rule (Gate 1, Gate 2)"
    - path: "test/commercial/parity-matrix.test.js"
      provides: "End-to-end gate test asserting `node scripts/commercial/verify-parity-matrix.cjs` exits 0 over the seeded registry"
  key_links:
    - from: "lib/markos/contracts/commercial-registry.js"
      to: "lib/markos/plugins/registry.js"
      via: "doctrine extension — same Map-backed insertion-order, fail-closed validation, idempotent registration"
      pattern: "createCommercialRegistry|registerCommercialContract"
    - from: "scripts/commercial/verify-parity-matrix.cjs"
      to: "lib/markos/contracts/commercial-registry.json"
      via: "CLI loads JSON, validates each entry, reports parity gaps"
      pattern: "require.*commercial-registry\\.json"
    - from: "test/commercial/parity-matrix.test.js"
      to: "scripts/commercial/verify-parity-matrix.cjs"
      via: "spawn child process, assert exit 0 + zero-gap stdout"
      pattern: "child_process.*verify-parity-matrix"
---

<objective>
Define the cross-engine commercial contract registry and parity gate that closes Gate 1 (one identity truth), Gate 2 (one commercial memory), and Gate 9 (UI/API/MCP parity). This slice is the foundation for slices 02-06.

Purpose: Make contract drift between the eight commercial engines (221-227) machine-detectable instead of prose. Today, no single store knows which F-IDs exist, which surfaces they cover, or where deferrals are auditable. The registry extends the existing `lib/markos/plugins/registry.js` substrate (per CONTEXT D-02 and code_context Integration Points) — it is NOT a parallel store, it is the same doctrine applied to commercial F-IDs.

Output: `lib/markos/contracts/commercial-registry.json` + `commercial-registry.js` + `commercial-registry-validator.js` + `scripts/commercial/verify-parity-matrix.cjs` + two test files that prove the registry is loaded, validated, parity-checked, and Gate 1/Gate 2 ownership-uniqueness is enforced.
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/228-commercial-os-integration-future-readiness/228-CONTEXT.md
@.planning/phases/228-commercial-os-integration-future-readiness/228-RESEARCH.md
@.planning/V4.2.0-INCOMING-18-26-GSD-DISCUSS-RESEARCH-ROUTING.md
@lib/markos/plugins/registry.js
@lib/markos/plugins/loader.js

<interfaces>
<!-- The executor reuses these contracts directly. Do not invent new ones. -->

From lib/markos/plugins/registry.js:
```js
function createRegistry();                                  // returns { plugins: Map<string, frozen-manifest> }
function registerPlugin(manifest, opts = {});              // throws INVALID_PLUGIN_CONTRACT on validation failure; idempotent on duplicate id
function listPlugins(registry);                             // snapshot array, insertion order
function resolvePlugin(registry, pluginId);                 // returns frozen entry or null
```

From lib/markos/plugins/loader.js:
```js
function loadPlugins(manifests);                           // returns { registry, failures: [{ manifest, error }] }
```

Required commercial-registry entry schema (this plan creates it):
```json
{
  "f_id": "CDP-01",                       // required, must match /^(CDP|CRM|EML|MSG|CNV|LCH|ANL|SEN|ECO|COM|QA)-(0[1-9]|1[0-5])$/
  "engine": "cdp",                        // required, one of: cdp, crm, email, messaging, conversion, launch, analytics, sales, ecosystem, commercial, quality
  "phase": "221-cdp-identity-audience-consent-substrate",  // required
  "ui_path": "/dashboard/cdp/...",        // required OR explicitly null
  "api_path": "/v1/cdp/...",              // required OR explicitly null
  "mcp_verb": "cdp.identity.merge",       // required OR explicitly null
  "ui_status":  "shipped|deferred|n_a",  // required
  "api_status": "shipped|deferred|n_a",  // required
  "mcp_status": "shipped|deferred|n_a",  // required
  "deferred_reason": null                 // required when any *_status === "deferred"; null otherwise
}
```

F-IDs that MUST be registered (full enumeration from CONTEXT D-27):
- CDP-01..05 (engine: cdp, phase: 221)
- CRM-01..05 (engine: crm, phase: 222)
- EML-01..05 (engine: email, phase: 223)
- MSG-01..05 (engine: messaging, phase: 223)
- CNV-01..05 (engine: conversion, phase: 224)
- LCH-01..05 (engine: launch, phase: 224)
- ANL-01..05 (engine: analytics, phase: 225)
- SEN-01..05 (engine: sales, phase: 226)
- ECO-01..05 (engine: ecosystem, phase: 227)
Total: 45 entries minimum. COM-01..06 and QA-01..15 are registered in slices 04/05 to avoid coupling foundation creation to those slices.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Route-back precondition — assert upstream engine API surface dirs exist (per D-31, RH3 fix)</name>
  <files>
    scripts/commercial/precondition-engine-dirs.cjs,
    test/commercial/precondition-engine-dirs.test.js
  </files>
  <read_first>
    - .planning/phases/228-commercial-os-integration-future-readiness/228-CONTEXT.md (D-01 route-back rule, D-31 hard precondition)
    - .planning/ROADMAP.md (Phase 228 `Depends on: Phases 221-227` — confirms upstream phases must ship first)
  </read_first>
  <action>
    Create `scripts/commercial/precondition-engine-dirs.cjs` — a Node CLI that:
    1. Reads the precondition list from `.planning/phases/228-commercial-os-integration-future-readiness/228-01-PLAN.md` frontmatter `preconditions.required_engine_api_dirs` (or hard-codes the same 9-entry list with explicit engine→phase mapping; whichever the executor prefers).
    2. For each entry, calls `fs.existsSync(path)`.
    3. Aggregates missing dirs. If empty → exit 0 with stdout "OK all 9 engine API surfaces present".
    4. If any missing → exit 1 with stderr listing each missing path AND the upstream phase that owns it: `MISSING_ENGINE_API_DIR:<engine>=<path> — route gap back to phase <NNN> per CONTEXT D-01. Plan 01 cannot proceed until phase <NNN> ships this surface.`
    5. Supports `--format json` flag emitting `{ ok: bool, missing: [{ engine, path, upstream_phase }] }`.

    Create `test/commercial/precondition-engine-dirs.test.js` (node --test):
    - Test 1: spawn CLI, capture exit code; if upstream phases are landed → expect exit 0; if not → expect exit 1 with stderr containing `MISSING_ENGINE_API_DIR`. Test does NOT assert a specific exit code — it asserts that **whichever exit code occurs, the CLI's behavior is well-defined**: exit 0 ⇒ stdout "OK all 9"; exit 1 ⇒ stderr lists every missing path with engine→phase mapping.
    - Test 2: spawn CLI with `--format json`, parse output, assert shape `{ ok: bool, missing: array }`.
    - Test 3: assert at least one of `["app/api/v1/cdp/","app/api/v1/crm/", "app/api/v1/email/","app/api/v1/messaging/","app/api/v1/conversion/","app/api/v1/launch/","app/api/v1/analytics/","app/api/v1/sales/","app/api/v1/ecosystem/"]` is checked (proves the precondition list is non-empty).

    **CRITICAL: If this task exits 1 in the actual execution environment, Tasks 1-3 of Plan 01 do NOT proceed.** The executor halts Plan 01 and reports the missing engine dirs to the orchestrator, which routes gap-closure back to the upstream phase per D-01. This is the same hard-fail behavior slice 04 detectors use (per Plan 04 Task 2 `MISSING_ENGINE_API_DIR`).

    No registry seeding, no validator creation, no parity verifier wiring proceeds until this gate passes.
  </action>
  <verify>
    <automated>node --test test/commercial/precondition-engine-dirs.test.js</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `scripts/commercial/precondition-engine-dirs.cjs`
    - File exists: `test/commercial/precondition-engine-dirs.test.js`
    - `grep -c "MISSING_ENGINE_API_DIR" scripts/commercial/precondition-engine-dirs.cjs` returns >= 1
    - `grep -c "route gap back" scripts/commercial/precondition-engine-dirs.cjs` returns >= 1
    - `grep -c "upstream_phase" scripts/commercial/precondition-engine-dirs.cjs` returns >= 1
    - `node scripts/commercial/precondition-engine-dirs.cjs --format json` produces parseable JSON with `ok` and `missing` keys
    - `node --test test/commercial/precondition-engine-dirs.test.js` exits 0 with 3 tests passing
    - **If `node scripts/commercial/precondition-engine-dirs.cjs` exits 1: Tasks 1-3 of Plan 01 are BLOCKED — executor halts and routes back to upstream phase per D-01. This is the correct behavior; Plan 01 is not "complete" with synthetic entries against absent surfaces.**
  </acceptance_criteria>
  <done>
    Precondition CLI exists; route-back behavior on missing engine dirs is well-defined; if all 9 engine API dirs exist, Tasks 1-3 proceed; if any are missing, Plan 01 halts with route-back to upstream phase per D-01 (RH3 fix).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Create commercial registry library + validator (mirrors plugins/registry.js doctrine)</name>
  <files>
    lib/markos/contracts/commercial-registry.js,
    lib/markos/contracts/commercial-registry-validator.js,
    test/commercial/contract-registry.test.js
  </files>
  <read_first>
    - lib/markos/plugins/registry.js (full file — copy doctrine)
    - lib/markos/plugins/loader.js (fail-closed boot pattern)
    - .planning/phases/228-commercial-os-integration-future-readiness/228-CONTEXT.md (D-01..D-06 non-negotiables, D-07 Gate 1, D-08 Gate 2)
    - .planning/V4.2.0-INCOMING-18-26-GSD-DISCUSS-RESEARCH-ROUTING.md lines 113-126 (10 quality gates verbatim)
    - obsidian/reference/Contracts Registry.md (registry doctrine)
  </read_first>
  <behavior>
    - Test 1: `createCommercialRegistry()` returns `{ contracts: Map<string, frozen-entry> }`, empty
    - Test 2: `registerCommercialContract(entry, { registry })` validates fields and freezes the entry
    - Test 3: registering the same `f_id` twice is a no-op (idempotent), matches plugins/registry.js D-01
    - Test 4: `validateCommercialContract` throws `INVALID_COMMERCIAL_CONTRACT:MISSING_F_ID` when f_id absent
    - Test 5: validator throws `INVALID_COMMERCIAL_CONTRACT:INVALID_F_ID_FORMAT` when f_id does not match `/^(CDP|CRM|EML|MSG|CNV|LCH|ANL|SEN|ECO|COM|QA)-(0[1-9]|1[0-5])$/`
    - Test 6: validator throws `INVALID_COMMERCIAL_CONTRACT:INVALID_ENGINE` for unknown engine token
    - Test 7: validator throws `INVALID_COMMERCIAL_CONTRACT:DEFERRED_REASON_REQUIRED` when any of `ui_status|api_status|mcp_status` is `"deferred"` and `deferred_reason` is null/empty
    - Test 8: validator throws `INVALID_COMMERCIAL_CONTRACT:DEFERRED_REASON_FORBIDDEN` when no surface is `"deferred"` but `deferred_reason` is non-null (Gate 9 — explicit auditable deferral only)
    - Test 9: Gate 1 enforcement — `listByEngine(registry, 'cdp')` for any F-ID owning identity/profile/consent/audience must come from phase 221 only; if a non-CDP entry registers an `f_id` starting with `CDP-`, validator throws `INVALID_COMMERCIAL_CONTRACT:GATE1_DUPLICATE_IDENTITY_OWNER`
    - Test 10: Gate 2 enforcement — same rule for CRM-prefixed F-IDs against phase 222 only (`GATE2_DUPLICATE_TIMELINE_OWNER`)
    - Test 11: `listContracts(registry)` returns insertion-order snapshot
    - Test 12: `resolveContract(registry, 'CDP-01')` returns frozen entry or null
    - Test 13: `listBySurface(registry, 'mcp')` returns only entries where `mcp_status === 'shipped'`
  </behavior>
  <action>
    Create `lib/markos/contracts/commercial-registry-validator.js` exporting `validateCommercialContract(entry)` that returns `{ ok: true }` or `{ ok: false, code: 'MISSING_F_ID'|'INVALID_F_ID_FORMAT'|'INVALID_ENGINE'|'INVALID_PHASE'|'INVALID_SURFACE_STATUS'|'DEFERRED_REASON_REQUIRED'|'DEFERRED_REASON_FORBIDDEN'|'GATE1_DUPLICATE_IDENTITY_OWNER'|'GATE2_DUPLICATE_TIMELINE_OWNER', detail: '<human-readable>' }`. Engine token whitelist: `['cdp','crm','email','messaging','conversion','launch','analytics','sales','ecosystem','commercial','quality']`. F-ID regex: `/^(CDP|CRM|EML|MSG|CNV|LCH|ANL|SEN|ECO|COM|QA)-(0[1-9]|1[0-5])$/`. Surface status enum: `['shipped','deferred','n_a']`. Gate 1: if `f_id` starts with `CDP-` then `engine` MUST equal `'cdp'` AND `phase` MUST equal `'221-cdp-identity-audience-consent-substrate'`. Gate 2: if `f_id` starts with `CRM-` then `engine` MUST equal `'crm'` AND `phase` MUST equal `'222-crm-timeline-commercial-memory-workspace'`.

    Create `lib/markos/contracts/commercial-registry.js` that mirrors `lib/markos/plugins/registry.js` line-for-line in doctrine: `createCommercialRegistry()`, `registerCommercialContract(entry, opts = {})` (calls validator, throws `Error('INVALID_COMMERCIAL_CONTRACT:' + code + ' — ' + detail)` with `err.code = 'INVALID_COMMERCIAL_CONTRACT'`, `err.contractCode = code`, idempotent on duplicate `f_id`, freezes entry copy with `Object.freeze`), `listContracts(registry)` (insertion-order snapshot), `resolveContract(registry, fId)`, `listByEngine(registry, engine)`, `listBySurface(registry, surface)` where surface is `'ui'|'api'|'mcp'` and returns entries where `${surface}_status === 'shipped'`.

    Create `test/commercial/contract-registry.test.js` using `node --test` and `node:assert/strict` (matching repo test convention from `test/plugin-registry.test.js`). Implement all 13 test cases above as `test('...', () => { ... })` blocks. Use `require('../../lib/markos/contracts/commercial-registry.js')` and `require('../../lib/markos/contracts/commercial-registry-validator.js')`.
  </action>
  <verify>
    <automated>node --test test/commercial/contract-registry.test.js</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `lib/markos/contracts/commercial-registry.js`
    - File exists: `lib/markos/contracts/commercial-registry-validator.js`
    - File exists: `test/commercial/contract-registry.test.js`
    - `node --test test/commercial/contract-registry.test.js` exits 0 with 13 tests passing
    - `grep -c "module.exports" lib/markos/contracts/commercial-registry.js` returns 1
    - `grep -E "createCommercialRegistry|registerCommercialContract|listContracts|resolveContract|listByEngine|listBySurface" lib/markos/contracts/commercial-registry.js` finds all 6 exports
    - `grep -c "INVALID_COMMERCIAL_CONTRACT" lib/markos/contracts/commercial-registry.js` returns >= 1
    - `grep -c "Object.freeze" lib/markos/contracts/commercial-registry.js` returns >= 2 (entry copy + arrays)
    - `grep -c "GATE1_DUPLICATE_IDENTITY_OWNER\|GATE2_DUPLICATE_TIMELINE_OWNER" lib/markos/contracts/commercial-registry-validator.js` returns 2
  </acceptance_criteria>
  <done>
    Registry library + validator exist, mirror plugins/registry.js doctrine exactly, fail-closed on invalid contracts, enforce Gate 1 and Gate 2 ownership uniqueness, and 13 tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Seed commercial-registry.json with 45 F-ID entries (CDP-01..05 ... ECO-01..05) + builder script</name>
  <files>
    lib/markos/contracts/commercial-registry.json,
    scripts/commercial/build-commercial-registry.cjs
  </files>
  <read_first>
    - lib/markos/contracts/commercial-registry.js (created in Task 1 — re-read after creation)
    - lib/markos/contracts/commercial-registry-validator.js (created in Task 1)
    - .planning/phases/221-cdp-identity-audience-consent-substrate/221-CONTEXT.md (CDP F-IDs and surfaces)
    - .planning/phases/222-crm-timeline-commercial-memory-workspace/222-CONTEXT.md (CRM F-IDs and surfaces)
    - .planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md (EML/MSG F-IDs)
    - .planning/phases/224-conversion-launch-workspace/224-CONTEXT.md (CNV/LCH F-IDs)
    - .planning/phases/225-analytics-attribution-narrative-intelligence/225-CONTEXT.md (ANL F-IDs)
    - .planning/phases/226-sales-enablement-deal-execution/226-CONTEXT.md (SEN F-IDs — uses F-163..F-180; map to SEN-01..05 per requirement bucket)
    - .planning/phases/227-ecosystem-partner-community-developer-growth/227-CONTEXT.md (ECO F-IDs — uses F-181..F-198; map to ECO-01..05)
  </read_first>
  <action>
    Create `scripts/commercial/build-commercial-registry.cjs` that reads each sibling phase CONTEXT.md, extracts the F-ID inventory the upstream phase declares (or, when sibling phase enumerates F-NNN-style IDs like F-163..F-180, fold them into the requirement bucket SEN-01..05 per ROADMAP "Requirements Mapped" line), and emits `lib/markos/contracts/commercial-registry.json` as a JSON array of 45 entries. For every entry emitted, the script MUST call `registerCommercialContract` from Task 1 to validate the entry; if any entry fails validation, the script MUST throw and write nothing.

    For each F-ID, populate `engine`, `phase`, `ui_path`, `api_path`, `mcp_verb`, `ui_status`, `api_status`, `mcp_status`, `deferred_reason`. Use the sibling phase CONTEXT/ROADMAP "Plans" lines as the source of truth for surface paths. Where a sibling phase has explicitly deferred a surface (e.g., CONTEXT.md `<deferred>` block names a surface), set the corresponding `*_status` to `"deferred"` and copy the rationale verbatim into `deferred_reason`. Where surface is genuinely n_a (e.g., a pure-data F-ID with no UI), set `*_status` to `"n_a"`. Otherwise `"shipped"`.

    Write `lib/markos/contracts/commercial-registry.json` as the validated array. Pretty-print with 2-space indent.

    Add `package.json` script (modify package.json) — but DO NOT modify package.json in this plan; instead document the invocation in the script header so slice 06 can wire `npm run commercial:build-registry`. (Wiring deferred to slice 06 doc-sync to keep this slice focused on data correctness.)

    Run `node scripts/commercial/build-commercial-registry.cjs` once during the task to emit the JSON. After emission, manually verify the file contains exactly 45 entries (`jq 'length' lib/markos/contracts/commercial-registry.json` should return 45) and every entry passes validation.
  </action>
  <verify>
    <automated>node scripts/commercial/build-commercial-registry.cjs && node -e "const r=require('./lib/markos/contracts/commercial-registry.json'); if(r.length!==45) process.exit(1); const {validateCommercialContract}=require('./lib/markos/contracts/commercial-registry-validator.js'); for(const e of r){const v=validateCommercialContract(e); if(!v.ok){console.error(e.f_id, v); process.exit(1);}}"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `lib/markos/contracts/commercial-registry.json`
    - File exists: `scripts/commercial/build-commercial-registry.cjs`
    - `node -e "console.log(require('./lib/markos/contracts/commercial-registry.json').length)"` prints `45`
    - `grep -c '"f_id":' lib/markos/contracts/commercial-registry.json` returns 45
    - `grep -c '"engine": "cdp"' lib/markos/contracts/commercial-registry.json` returns 5
    - `grep -c '"engine": "crm"' lib/markos/contracts/commercial-registry.json` returns 5
    - `grep -c '"engine": "email"' lib/markos/contracts/commercial-registry.json` returns 5
    - `grep -c '"engine": "messaging"' lib/markos/contracts/commercial-registry.json` returns 5
    - `grep -c '"engine": "conversion"' lib/markos/contracts/commercial-registry.json` returns 5
    - `grep -c '"engine": "launch"' lib/markos/contracts/commercial-registry.json` returns 5
    - `grep -c '"engine": "analytics"' lib/markos/contracts/commercial-registry.json` returns 5
    - `grep -c '"engine": "sales"' lib/markos/contracts/commercial-registry.json` returns 5
    - `grep -c '"engine": "ecosystem"' lib/markos/contracts/commercial-registry.json` returns 5
    - Every entry passes `validateCommercialContract` (the verify command above exits 0)
    - For every entry where any `*_status === "deferred"`, the `deferred_reason` field is non-empty and references the sibling-phase CONTEXT deferral entry verbatim
  </acceptance_criteria>
  <done>
    45 F-IDs registered, every entry validates, builder script is idempotent and re-runnable.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement parity verifier CLI + automated parity-matrix test (Gate 9 enforcement)</name>
  <files>
    scripts/commercial/verify-parity-matrix.cjs,
    test/commercial/parity-matrix.test.js
  </files>
  <read_first>
    - lib/markos/contracts/commercial-registry.json (created in Task 2)
    - lib/markos/contracts/commercial-registry.js (Task 1)
    - .planning/V4.2.0-INCOMING-18-26-GSD-DISCUSS-RESEARCH-ROUTING.md lines 113-126 (Gate 9 verbatim: "UI / API / MCP parity — engines are incomplete if they exist in only one surface")
    - .planning/phases/228-commercial-os-integration-future-readiness/228-CONTEXT.md (D-15 Gate 9, D-04 contract gaps non-negotiable)
  </read_first>
  <action>
    Create `scripts/commercial/verify-parity-matrix.cjs` — a Node CLI that:
    1. Loads `lib/markos/contracts/commercial-registry.json` via `require`.
    2. For each entry, computes `gap = (ui_status === 'shipped' ? 0 : 1) + (api_status === 'shipped' ? 0 : 1) + (mcp_status === 'shipped' ? 0 : 1)`. If `gap > 0` AND no `deferred_reason` covering each non-shipped surface → record a parity violation `{ f_id, missing_surfaces: [...], reason: 'NO_AUDITABLE_DEFERRAL' }`.
    3. Supports flags: `--engine <token>` (filter to one engine), `--format json|table` (default table), `--fail-on-deferred` (treat deferred surfaces as gaps too — used in slice 05 final readiness gate).
    4. Prints a Markdown table with columns: F-ID | Engine | UI | API | MCP | Status (clean/deferred/violation) | Notes.
    5. Exits 0 if zero violations, exit 1 if any violation, exit 2 if registry file missing or invalid JSON.

    Create `test/commercial/parity-matrix.test.js` using `node --test`:
    - Test 1: spawning `node scripts/commercial/verify-parity-matrix.cjs` over the seeded 45-entry registry exits 0 (because all deferrals have `deferred_reason` per Task 2 acceptance).
    - Test 2: spawning with `--engine cdp` filters to 5 entries, exits 0.
    - Test 3: spawning with `--format json` returns parseable JSON with shape `{ summary: { total, clean, deferred, violations }, entries: [...] }`.
    - Test 4: write a temporary registry copy with one entry mutated to have `ui_status: "deferred"` and `deferred_reason: null` → spawn the verifier with `COMMERCIAL_REGISTRY_PATH=<tmpfile>` env override → expect exit 1 and stderr contains `NO_AUDITABLE_DEFERRAL`.

    The CLI must honor `COMMERCIAL_REGISTRY_PATH` env var to enable Test 4's tmpfile redirection.
  </action>
  <verify>
    <automated>node --test test/commercial/parity-matrix.test.js && node scripts/commercial/verify-parity-matrix.cjs</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `scripts/commercial/verify-parity-matrix.cjs`
    - File exists: `test/commercial/parity-matrix.test.js`
    - `node scripts/commercial/verify-parity-matrix.cjs` exits 0 against seeded registry
    - `node scripts/commercial/verify-parity-matrix.cjs --engine cdp` prints 5 rows and exits 0
    - `node scripts/commercial/verify-parity-matrix.cjs --format json` outputs valid JSON parseable by `node -e "JSON.parse(...)"`
    - `node --test test/commercial/parity-matrix.test.js` exits 0 with 4 tests passing
    - `grep -c "COMMERCIAL_REGISTRY_PATH" scripts/commercial/verify-parity-matrix.cjs` returns >= 1 (env override implemented)
    - `grep -c "NO_AUDITABLE_DEFERRAL" scripts/commercial/verify-parity-matrix.cjs` returns >= 1
  </acceptance_criteria>
  <done>
    Parity verifier passes against seeded registry, fails closed on missing deferral reasons, and 4 tests prove the gate behavior.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build script -> registry JSON | builder writes the canonical JSON; tampering would silently weaken Gate 1/2/9 |
| registry JSON -> verifier CLI | CLI reads the JSON; spoofed file would mask parity violations |
| verifier CLI -> CI | CI consumes exit code; if CLI defaults to exit 0 on parse failure, the gate is bypassed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-228-01-01 | Tampering | commercial-registry.json | mitigate | builder script re-validates every entry through `validateCommercialContract` before emission; verifier re-validates on load (Task 3 step 2) |
| T-228-01-02 | Spoofing | COMMERCIAL_REGISTRY_PATH env override | accept | env override is intentional for tests; production CI does not set it. Documented in CLI `--help`. |
| T-228-01-03 | Information disclosure | registry JSON contents | accept | F-IDs and surface paths are non-secret; they appear in OpenAPI and llms.txt already. |
| T-228-01-04 | Denial of service | parse failure on malformed JSON | mitigate | verifier exits 2 (distinct from violation exit 1) so CI can distinguish missing-registry from policy-violation |
| T-228-01-05 | Elevation of privilege | non-CDP phase registers CDP-* F-ID | mitigate | Gate 1 enforcement in validator throws GATE1_DUPLICATE_IDENTITY_OWNER (validator Task 1) |
| T-228-01-06 | Repudiation | deferred surface without auditable reason | mitigate | DEFERRED_REASON_REQUIRED validator code; verifier exits 1 with NO_AUDITABLE_DEFERRAL |
</threat_model>

<verification>
- `node --test test/commercial/contract-registry.test.js` exits 0 (13 tests)
- `node --test test/commercial/parity-matrix.test.js` exits 0 (4 tests)
- `node scripts/commercial/verify-parity-matrix.cjs` exits 0
- `node -e "console.log(require('./lib/markos/contracts/commercial-registry.json').length)"` prints `45`
- Registry mirrors `lib/markos/plugins/registry.js` doctrine: insertion-order Map, frozen entries, idempotent registration, fail-closed validation
</verification>

<success_criteria>
- 45 F-IDs registered (CDP-01..05, CRM-01..05, EML-01..05, MSG-01..05, CNV-01..05, LCH-01..05, ANL-01..05, SEN-01..05, ECO-01..05)
- Validator enforces Gate 1 (one identity truth — only phase 221 owns CDP-*) and Gate 2 (one commercial memory — only phase 222 owns CRM-*)
- Parity verifier exits 0 against seeded registry and exits 1 against any registry containing a deferred surface without `deferred_reason`
- Registry library exports parallel `lib/markos/plugins/registry.js` shape; no parallel store created
- 17 tests pass (13 + 4)
</success_criteria>

<output>
After completion, create `.planning/phases/228-commercial-os-integration-future-readiness/228-01-SUMMARY.md` documenting: 45 F-IDs registered, validator code list, exit-code semantics for verifier, and the contract for slices 02-06 to extend (slice 05 will add COM-01..06 and QA-01..15 once test infrastructure is in place).
</output>
