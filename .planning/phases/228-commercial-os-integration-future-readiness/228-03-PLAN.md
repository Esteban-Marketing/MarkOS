---
phase: 228-commercial-os-integration-future-readiness
plan: 03
type: execute
wave: 3
depends_on: ["228-02"]
files_modified:
  - lib/markos/contracts/archival-policy.json
  - lib/markos/contracts/recovery-runbook.json
  - lib/markos/contracts/migration-manifest.json
  - scripts/commercial/test-archival-recovery.cjs
  - scripts/commercial/expand-migration-manifest.cjs
  - test/commercial/archival-recovery.test.js
  - test/commercial/migration-manifest-coverage.test.js
autonomous: true
requirements:
  - COM-04
  - COM-06
  - QA-07
  - QA-08
  - QA-09
user_setup: []

must_haves:
  truths:
    - "Migration manifest expanded from 9 representative entries to >=30 durable-object entries — every durable-object class declared by phases 221-227 has versioned schema + exporter/importer paths + round-trip fixture"
    - "Every durable-object class has a declared archival policy (retention_days, archival_target, recoverable_until, tombstone_cascade_chain) registered in `lib/markos/contracts/archival-policy.json`"
    - "Recovery runbook (`lib/markos/contracts/recovery-runbook.json`) declares: per-engine restore procedure, prerequisites, restore-from-archive script path, verification command, expected restore time"
    - "Archival/recovery CLI gate (`scripts/commercial/test-archival-recovery.cjs`) exits 0 only when every entry's restore script + verification command + tombstone cascade chain is reachable and the simulated restore round-trips identically to original"
    - "Tombstone cascade enforcement: ecosystem tombstone scrub from phase 227 D-65 + sales tombstone cascade from phase 226 D-56 are reachable via runbook (entries link to their cascade chains)"
    - "SOC2-I retention floor (>= 2555 days = 7 years) is asserted PER-CLASS via explicit allow-list (not a fragile floor count) — per S4 fix"
  artifacts:
    - path: "lib/markos/contracts/migration-manifest.json"
      provides: "Expanded to >=30 entries — full durable-object coverage across CDP/CRM/email/messaging/conversion/launch/analytics/sales/ecosystem"
      contains: "engine entries: cdp, crm, email, messaging, conversion, launch, analytics, sales, ecosystem"
    - path: "lib/markos/contracts/archival-policy.json"
      provides: "Per durable-object retention/archival/recovery policy with tombstone cascade chains"
    - path: "lib/markos/contracts/recovery-runbook.json"
      provides: "Per-engine restore procedure with verification command + expected duration"
    - path: "scripts/commercial/test-archival-recovery.cjs"
      provides: "CLI gate that simulates archive + restore + verify for every engine; exits 0 if all pass"
    - path: "scripts/commercial/expand-migration-manifest.cjs"
      provides: "Builder that extends migration-manifest.json from 9 → 30 entries; idempotent"
    - path: "test/commercial/archival-recovery.test.js"
      provides: "node --test asserting CLI gate behavior + tombstone cascade reachability"
    - path: "test/commercial/migration-manifest-coverage.test.js"
      provides: "node --test asserting expanded manifest has full coverage and every entry passes round-trip"
  key_links:
    - from: "lib/markos/contracts/archival-policy.json"
      to: "lib/markos/contracts/migration-manifest.json"
      via: "every archival-policy entry references a migration-manifest entry by `engine`+`object` composite key"
      pattern: "\"engine\":.*\"object\":"
    - from: "lib/markos/contracts/recovery-runbook.json"
      to: "lib/markos/contracts/migration-manifest.json"
      via: "per-engine restore step references migration-manifest importer path"
      pattern: "importer_path"
    - from: "scripts/commercial/test-archival-recovery.cjs"
      to: "scripts/commercial/test-migration-roundtrip.cjs"
      via: "archival CLI invokes round-trip CLI as a sub-step (archival = migration round-trip + retention metadata)"
      pattern: "test-migration-roundtrip"
---

<objective>
Close Gate 8 (no designed obsolescence) by adding archival, recovery, and tombstone-cascade controls on top of the migration scaffolding from slice 02. Expand migration-manifest.json from 9 representative entries to ≥30 — full durable-object coverage across all nine commercial engines.

Purpose: Slice 02 proved 9 engines can round-trip via stubs. Slice 03 extends that to every durable-object class declared by 221-227 and adds the archival/retention/recovery layer that makes "no designed obsolescence" enforceable instead of aspirational. Tombstone cascade chains from phase 226 D-56 (sales) and phase 227 D-65 (ecosystem) are made reachable via a single runbook.

Output: expanded migration-manifest.json + archival-policy.json + recovery-runbook.json + archival/recovery CLI gate + 2 test files proving every engine's durable objects are recoverable.
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/228-commercial-os-integration-future-readiness/228-CONTEXT.md
@.planning/phases/228-commercial-os-integration-future-readiness/228-RESEARCH.md
@.planning/phases/228-commercial-os-integration-future-readiness/228-01-PLAN.md
@.planning/phases/228-commercial-os-integration-future-readiness/228-02-PLAN.md
@.planning/V4.2.0-INCOMING-18-26-GSD-DISCUSS-RESEARCH-ROUTING.md
@.planning/phases/221-cdp-identity-audience-consent-substrate/221-CONTEXT.md
@.planning/phases/222-crm-timeline-commercial-memory-workspace/222-CONTEXT.md
@.planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md
@.planning/phases/224-conversion-launch-workspace/224-CONTEXT.md
@.planning/phases/225-analytics-attribution-narrative-intelligence/225-CONTEXT.md
@.planning/phases/226-sales-enablement-deal-execution/226-CONTEXT.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-CONTEXT.md
@lib/markos/contracts/migration-manifest.json
@scripts/commercial/test-migration-roundtrip.cjs

<interfaces>
<!-- The executor extends slice 02 contracts; doctrine and exit codes are unchanged. -->

Migration manifest target inventory (≥30 entries — full enumeration from slice 02 `<interfaces>`):
- cdp: identity_profile, audience_snapshot, consent_state                              (3)
- crm: timeline_event, account, person, opportunity                                    (4)
- email: template, dispatch_record, suppression                                        (3)
- messaging: thread, message, suppression                                              (3)
- conversion: funnel_event, landing, form, offer                                       (4)
- launch: plan, rollout, rollback_record                                               (3)
- analytics: event, metric_snapshot, attribution_touch                                 (3)
- sales: deal_brief, proof_pack, quote, winloss_record, handoff_record                 (5)
- ecosystem: integration_listing, partner_profile, referral, affiliate_program, community_signal, developer_event, certification_record, co_sell_opportunity (8)
TOTAL: 36 entries (>= the 30-entry floor)

Archival policy entry shape:
```json
{
  "engine": "cdp",
  "object": "identity_profile",
  "retention_days": 2555,                              // ~7 years; SOC2-I friendly default
  "archival_target": "supabase_storage://archives/cdp/identity_profile/",
  "recoverable_until": "P10Y",                         // ISO 8601 duration
  "tombstone_cascade_chain": [],                        // ordered list of downstream cascades; null/[] if leaf
  "archival_trigger": "manual",                         // 'manual' | 'cron' | 'tenant_delete'
  "archival_script_path": "lib/markos/contracts/archival-stubs/cdp-identity_profile-archive.cjs",
  "restore_script_path": "lib/markos/contracts/archival-stubs/cdp-identity_profile-restore.cjs"
}
```

Recovery runbook entry shape:
```json
{
  "engine": "cdp",
  "objects": ["identity_profile", "audience_snapshot", "consent_state"],
  "prerequisites": ["supabase storage credentials", "tenant admin role"],
  "restore_procedure": [
    "Step 1: list archive entries via `node lib/markos/contracts/archival-stubs/cdp-identity_profile-restore.cjs --list`",
    "Step 2: restore objects via `<script> --tenant <id> --since <iso>`"
  ],
  "verification_command": "node scripts/commercial/test-archival-recovery.cjs --engine cdp --simulated",
  "expected_restore_minutes": 30
}
```

Tombstone cascade chains (from sibling CONTEXTs):
- ecosystem/integration_listing -> downstream: ecosystem/install_requests, ecosystem/listing_views (per phase 227 D-65)
- ecosystem/partner_profile     -> downstream: ecosystem/integration_listing, ecosystem/certification_record, ecosystem/affiliate_program (per phase 227 tombstone scrub)
- sales/deal_brief              -> downstream: sales/proof_pack, sales/quote, sales/winloss_record, sales/handoff_record (per phase 226 D-56)
- crm/account                   -> downstream: crm/person, crm/opportunity (per phase 222 cascade)
- cdp/identity_profile          -> downstream: cdp/audience_snapshot, cdp/consent_state (per phase 221 ConsentState model)

SOC2-I retention floor — REQUIRED 2555-day retention classes (per S4 fix, replaces fragile `>= 7` count):
```js
const required_2555 = [
  'cdp/identity_profile',
  'cdp/audience_snapshot',
  'cdp/consent_state',
  'crm/account',
  'crm/person',
  'crm/opportunity',
  'crm/timeline_event',
  'sales/winloss_record',
  'sales/handoff_record',
  'ecosystem/certification_record',
];
// Each MUST have retention_days >= 2555 in archival-policy.json
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand migration-manifest.json from 9 to >=30 entries (full durable-object coverage)</name>
  <files>
    lib/markos/contracts/migration-manifest.json,
    scripts/commercial/expand-migration-manifest.cjs,
    test/commercial/migration-manifest-coverage.test.js,
    lib/markos/contracts/migration-stubs/ (27 new stub files for the added entries),
    test/commercial/fixtures/migration/ (27 new fixture files)
  </files>
  <read_first>
    - lib/markos/contracts/migration-manifest.json (slice 02 — 9 entries)
    - scripts/commercial/test-migration-roundtrip.cjs (slice 02)
    - lib/markos/contracts/migration-stubs/ (slice 02 stubs — copy pattern)
    - .planning/phases/221-cdp-identity-audience-consent-substrate/221-CONTEXT.md (CDP durable objects + RLS)
    - .planning/phases/222-crm-timeline-commercial-memory-workspace/222-CONTEXT.md (CRM durable objects + 4-table inventory)
    - .planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md (email + messaging durable objects)
    - .planning/phases/224-conversion-launch-workspace/224-CONTEXT.md (conversion + launch durable objects)
    - .planning/phases/225-analytics-attribution-narrative-intelligence/225-CONTEXT.md (analytics 3 durable objects)
    - .planning/phases/226-sales-enablement-deal-execution/226-CONTEXT.md (sales 5 durable objects + tombstone)
    - .planning/phases/227-ecosystem-partner-community-developer-growth/227-CONTEXT.md (ecosystem 8 durable objects + tombstone)
  </read_first>
  <action>
    Create `scripts/commercial/expand-migration-manifest.cjs` — a builder that:
    1. Loads existing `migration-manifest.json` (9 entries from slice 02).
    2. For each engine in the target inventory (`<interfaces>` block), generates the missing entries with stub exporter/importer/fixture paths following slice 02 naming convention (`lib/markos/contracts/migration-stubs/<engine>-<object>.cjs` and `test/commercial/fixtures/migration/<engine>-<object>.json`).
    3. For each new entry, generates a stub `.cjs` exporter file (same pattern as slice 02 stubs — `exportSnapshot(input)` returns `JSON.parse(JSON.stringify(input))`, `importSnapshot(serialized)` returns `JSON.parse(serialized)`).
    4. For each new entry, generates a representative fixture JSON file with all required fields per sibling phase CONTEXT schema (extract field names from the relevant CONTEXT.md `<schema>` or `<sql>` blocks).
    5. Idempotent — re-running does not duplicate entries; if an entry already exists for `(engine, object)`, skip it.
    6. Final manifest length must be >=30 (target: 36 per `<interfaces>` enumeration).

    Run the builder once: `node scripts/commercial/expand-migration-manifest.cjs`.

    Then run the slice 02 round-trip CLI to confirm all 36 entries round-trip: `node scripts/commercial/test-migration-roundtrip.cjs`.

    Create `test/commercial/migration-manifest-coverage.test.js`:
    - Test 1: `migration-manifest.json` length >= 30
    - Test 2: every engine in `['cdp','crm','email','messaging','conversion','launch','analytics','sales','ecosystem']` has at least one entry
    - Test 3: cdp has exactly 3 objects (`identity_profile`, `audience_snapshot`, `consent_state`)
    - Test 4: crm has exactly 4 objects
    - Test 5: ecosystem has exactly 8 objects (largest engine)
    - Test 6: every entry has all 7 required fields (engine, object, schema_version, exporter_path, importer_path, round_trip_fixture_path, last_passing_roundtrip_at)
    - Test 7: every `exporter_path` and `importer_path` resolves to an existing file
    - Test 8: every `round_trip_fixture_path` resolves to an existing file
    - Test 9: spawn `node scripts/commercial/test-migration-roundtrip.cjs` over the expanded manifest, expect exit 0
  </action>
  <verify>
    <automated>node scripts/commercial/expand-migration-manifest.cjs && node scripts/commercial/test-migration-roundtrip.cjs && node --test test/commercial/migration-manifest-coverage.test.js</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `scripts/commercial/expand-migration-manifest.cjs`
    - File exists: `test/commercial/migration-manifest-coverage.test.js`
    - `node -e "console.log(require('./lib/markos/contracts/migration-manifest.json').length)"` prints a number >= 30
    - `node scripts/commercial/test-migration-roundtrip.cjs` exits 0 over expanded manifest
    - `node --test test/commercial/migration-manifest-coverage.test.js` exits 0 with 9 tests passing
    - `grep -c '"engine": "ecosystem"' lib/markos/contracts/migration-manifest.json` returns 8
    - `grep -c '"engine": "sales"' lib/markos/contracts/migration-manifest.json` returns 5
    - `grep -c '"engine": "cdp"' lib/markos/contracts/migration-manifest.json` returns 3
    - All stub files in `lib/markos/contracts/migration-stubs/` are valid CommonJS modules (`node -e "require('./lib/markos/contracts/migration-stubs/<engine>-<object>.cjs')"` exits 0 for at least 5 random samples)
    - Builder is idempotent: running it twice does not duplicate entries (post-second-run length unchanged)
  </acceptance_criteria>
  <done>
    Migration manifest covers >=30 durable-object classes; every entry round-trips; coverage test asserts engine-by-engine completeness.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create archival-policy.json + recovery-runbook.json with tombstone cascade chains + EXPLICIT per-class SOC2-I retention floor (per S4)</name>
  <files>
    lib/markos/contracts/archival-policy.json,
    lib/markos/contracts/recovery-runbook.json
  </files>
  <read_first>
    - lib/markos/contracts/migration-manifest.json (post-Task-1 — full inventory)
    - .planning/phases/226-sales-enablement-deal-execution/226-CONTEXT.md (D-56 tombstone cascade — sales chain)
    - .planning/phases/227-ecosystem-partner-community-developer-growth/227-CONTEXT.md (D-65 tombstone scrub — ecosystem chain)
    - .planning/phases/221-cdp-identity-audience-consent-substrate/221-CONTEXT.md (consent withdrawal cascade)
    - .planning/phases/222-crm-timeline-commercial-memory-workspace/222-CONTEXT.md (crm cascade)
    - obsidian/reference/Database Schema.md (versioned schema posture)
    - .planning/phases/228-commercial-os-integration-future-readiness/228-CONTEXT.md (D-14 Gate 8, D-26 archival recovery test per engine, D-30 SOC2-I posture)
  </read_first>
  <behavior>
    - Behavior 1: `archival-policy.json` is a JSON array with one entry per migration-manifest entry (>=30 entries)
    - Behavior 2: every entry has 8 required fields per `<interfaces>` block (engine, object, retention_days, archival_target, recoverable_until, tombstone_cascade_chain, archival_trigger, archival_script_path, restore_script_path)
    - Behavior 3: `recovery-runbook.json` has one entry per engine (9 entries)
    - Behavior 4: tombstone cascade chains accurately reflect sibling-phase declarations:
      - `ecosystem/integration_listing.tombstone_cascade_chain` includes `["ecosystem/install_requests", "ecosystem/listing_views"]`
      - `ecosystem/partner_profile.tombstone_cascade_chain` includes `["ecosystem/integration_listing", "ecosystem/certification_record", "ecosystem/affiliate_program"]`
      - `sales/deal_brief.tombstone_cascade_chain` includes `["sales/proof_pack", "sales/quote", "sales/winloss_record", "sales/handoff_record"]`
      - `crm/account.tombstone_cascade_chain` includes `["crm/person", "crm/opportunity"]`
      - `cdp/identity_profile.tombstone_cascade_chain` includes `["cdp/audience_snapshot", "cdp/consent_state"]`
    - Behavior 5: every entry's `archival_script_path` and `restore_script_path` is non-empty (slice 03 may use stubs; real scripts can be wired in upstream-phase gap-closure runs)
    - Behavior 6: SOC2-I posture — **PER S4 FIX:** the explicit list of 10 classes from `<interfaces>` `required_2555` MUST each have `retention_days >= 2555` (replaces fragile floor count `>= 7`); for ephemeral objects (e.g., `analytics/event`) retention may be shorter but >= 365
  </behavior>
  <action>
    Create `lib/markos/contracts/archival-policy.json` with one entry per migration-manifest entry. For each entry:
    - `engine` and `object` mirror migration-manifest
    - `retention_days`:
      - **2555 (7 years) for ALL classes in `<interfaces>` `required_2555` list**: `cdp/identity_profile`, `cdp/audience_snapshot`, `cdp/consent_state`, `crm/account`, `crm/person`, `crm/opportunity`, `crm/timeline_event`, `sales/winloss_record`, `sales/handoff_record`, `ecosystem/certification_record`
      - 1825 (5y) for: `ecosystem/partner_profile`, `ecosystem/integration_listing`, `ecosystem/referral`, `ecosystem/affiliate_program`, `ecosystem/community_signal`, `ecosystem/developer_event`, `ecosystem/co_sell_opportunity`, `sales/deal_brief`, `sales/proof_pack`, `sales/quote`, `conversion/funnel_event`, `conversion/landing`, `conversion/form`, `conversion/offer`, `launch/plan`, `launch/rollout`, `launch/rollback_record`
      - 730 for `analytics/event`, `analytics/metric_snapshot`, `analytics/attribution_touch`, `email/dispatch_record`, `email/template`, `messaging/message`, `messaging/thread`
      - 365 for `email/suppression`, `messaging/suppression`
    - `archival_target`: `"supabase_storage://archives/<engine>/<object>/"` (string template; actual storage URI lives elsewhere)
    - `recoverable_until`: `"P10Y"` for SOC2-I-relevant, `"P5Y"` otherwise
    - `tombstone_cascade_chain`: per `<interfaces>` block — full list for cascade roots, empty array for leaves
    - `archival_trigger`: `"tenant_delete"` for `cdp/*`, `crm/account`, `ecosystem/partner_profile`; `"cron"` for `analytics/*`, `email/dispatch_record`, `messaging/message`; `"manual"` otherwise
    - `archival_script_path` and `restore_script_path` point at `lib/markos/contracts/archival-stubs/<engine>-<object>-archive.cjs` and `-restore.cjs` (Task 3 will create stubs)

    Create `lib/markos/contracts/recovery-runbook.json` with 9 engine entries. Each entry has shape per `<interfaces>` block. `verification_command` for every entry: `"node scripts/commercial/test-archival-recovery.cjs --engine <engine> --simulated"`. `expected_restore_minutes`: 30 for cdp/crm (heavy joins), 60 for ecosystem (8 objects + cascades), 15 for analytics/email/messaging (lower coupling), 45 for sales/conversion/launch.

    No CLI changes in this task — Task 3 builds the gate.
  </action>
  <verify>
    <automated>node -e "const ap=require('./lib/markos/contracts/archival-policy.json'); const mm=require('./lib/markos/contracts/migration-manifest.json'); if(ap.length<mm.length){console.error('archival-policy missing entries'); process.exit(1);} for(const e of ap){if(!e.engine||!e.object||typeof e.retention_days!=='number'||!e.archival_target||!e.recoverable_until||!Array.isArray(e.tombstone_cascade_chain)||!e.archival_trigger||!e.archival_script_path||!e.restore_script_path){console.error('invalid entry',e); process.exit(1);}} const rb=require('./lib/markos/contracts/recovery-runbook.json'); if(rb.length!==9){console.error('recovery runbook should have 9 engine entries'); process.exit(1);} console.log('OK',ap.length,'archival entries,',rb.length,'runbook entries');" && node -e "const m=require('./lib/markos/contracts/archival-policy.json'); const required=['cdp/identity_profile','cdp/audience_snapshot','cdp/consent_state','crm/account','crm/person','crm/opportunity','crm/timeline_event','sales/winloss_record','sales/handoff_record','ecosystem/certification_record']; const missing=required.filter(c=>{const [e,o]=c.split('/'); const entry=m.find(x=>x.engine===e&&x.object===o); return !entry || !(entry.retention_days>=2555);}); if(missing.length){console.error('SOC2-I retention floor violation — missing 2555-day retention for:',missing); process.exit(1);} console.log('OK SOC2-I retention floor enforced for all',required.length,'required classes');"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `lib/markos/contracts/archival-policy.json`
    - File exists: `lib/markos/contracts/recovery-runbook.json`
    - `node -e "console.log(require('./lib/markos/contracts/archival-policy.json').length)"` prints number equal to migration-manifest length (>=30)
    - `node -e "console.log(require('./lib/markos/contracts/recovery-runbook.json').length)"` prints `9`
    - `grep -c '"engine": "ecosystem"' lib/markos/contracts/recovery-runbook.json` returns 1
    - `grep -c "ecosystem/install_requests\|ecosystem/listing_views" lib/markos/contracts/archival-policy.json` returns >= 2 (cascade chain populated for ecosystem/integration_listing)
    - `grep -c "sales/proof_pack" lib/markos/contracts/archival-policy.json` returns >= 1 (sales/deal_brief cascade)
    - `grep -c "cdp/audience_snapshot" lib/markos/contracts/archival-policy.json` returns >= 2 (in cdp/identity_profile cascade chain + own entry)
    - **Per S4 fix — SOC2-I retention floor asserted via EXPLICIT per-class allow-list (replaces fragile `>= 7` count):**
      ```
      node -e "const m=require('./lib/markos/contracts/archival-policy.json'); const required=['cdp/identity_profile','cdp/audience_snapshot','cdp/consent_state','crm/account','crm/person','crm/opportunity','crm/timeline_event','sales/winloss_record','sales/handoff_record','ecosystem/certification_record']; const missing=required.filter(c=>{const [e,o]=c.split('/'); const entry=m.find(x=>x.engine===e&&x.object===o); return !entry || !(entry.retention_days>=2555);}); if(missing.length){console.error('Missing 2555-day retention:',missing); process.exit(1);} console.log('OK',required.length,'classes have 2555-day retention');"
      ```
      MUST exit 0 (proves all 10 SOC2-I-relevant classes have `retention_days >= 2555`)
  </acceptance_criteria>
  <done>
    archival-policy.json + recovery-runbook.json exist, cover every migration-manifest entry, tombstone cascade chains match sibling-phase declarations, and PER S4 — SOC2-I 2555-day retention is asserted via explicit per-class allow-list of 10 classes (not a fragile floor count).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement test-archival-recovery.cjs CLI gate (Gate 8 closure) + archival stubs</name>
  <files>
    scripts/commercial/test-archival-recovery.cjs,
    test/commercial/archival-recovery.test.js,
    lib/markos/contracts/archival-stubs/ (per-engine archive + restore stubs — at least one pair per engine for the simulated mode)
  </files>
  <read_first>
    - lib/markos/contracts/archival-policy.json (Task 2)
    - lib/markos/contracts/recovery-runbook.json (Task 2)
    - lib/markos/contracts/migration-manifest.json (Task 1)
    - scripts/commercial/test-migration-roundtrip.cjs (slice 02 — invoke as sub-step)
    - .planning/phases/228-commercial-os-integration-future-readiness/228-CONTEXT.md (D-14 Gate 8, D-26 archival recovery test per engine)
    - .planning/phases/226-sales-enablement-deal-execution/226-CONTEXT.md (D-56 tombstone cascade behavior)
    - .planning/phases/227-ecosystem-partner-community-developer-growth/227-CONTEXT.md (D-65 tombstone scrub behavior)
  </read_first>
  <behavior>
    - Behavior 1: `node scripts/commercial/test-archival-recovery.cjs --simulated` runs simulated archive → restore → verify for every archival-policy entry
    - Behavior 2: `--engine <e>` filter scopes to one engine
    - Behavior 3: simulated mode: load fixture from migration-manifest, call archive_stub.archive(fixture) → archived blob, call restore_stub.restore(archived blob) → restored, diff vs original (modulo timestamps), exit 0 if all match
    - Behavior 4: `--format json` outputs `{ summary, results }` JSON
    - Behavior 5: tombstone cascade verification: when `--cascade <engine>/<object>` is specified, CLI walks `tombstone_cascade_chain` and asserts every downstream entry exists in archival-policy AND has reachable archive/restore stubs; exit 1 if chain is broken
    - Behavior 6: exit 0 = all clean; exit 1 = restore diff or broken cascade; exit 2 = missing stub/path/fixture; exit 3 = expected_restore_minutes exceeded for a real (non-simulated) run (reserved — not exercised in this slice)
  </behavior>
  <action>
    Create `lib/markos/contracts/archival-stubs/<engine>-<object>-archive.cjs` and `<engine>-<object>-restore.cjs` for at least one pair per engine (9 minimum: cdp/identity_profile, crm/timeline_event, email/template, messaging/thread, conversion/funnel_event, launch/plan, analytics/event, sales/deal_brief, ecosystem/integration_listing). Each stub:
    - archive: `module.exports = { archive: (record) => ({ archived_at: '<frozen ISO>', payload: JSON.parse(JSON.stringify(record)) }) }`
    - restore: `module.exports = { restore: (archived) => archived.payload }`

    For non-stub-covered entries (the remaining 21+ migration-manifest entries), point archival-policy.json paths at a generic stub `lib/markos/contracts/archival-stubs/_default-archive.cjs` and `_default-restore.cjs` that perform the same identity round-trip. This keeps the CLI gate green for the full inventory while real archival scripts can be wired in subsequent gap-closure work.

    Create `scripts/commercial/test-archival-recovery.cjs`:
    1. Parse `--engine <e>`, `--simulated` (default true for now), `--cascade <engine>/<object>`, `--format json|table` flags.
    2. Load `archival-policy.json`, `recovery-runbook.json`, `migration-manifest.json`.
    3. For each entry (or filtered):
       - Resolve `archival_script_path` + `restore_script_path` via `require`. If missing → exit 2 with `MISSING_ARCHIVAL_STUB:<engine>/<object>`.
       - Load fixture from migration-manifest's `round_trip_fixture_path`.
       - Call `archive(fixture)` → `archived`. Call `restore(archived)` → `restored`.
       - Strip timestamp keys (deep) from both, JSON-stringify with sorted keys, compare. Mismatch → `ok: false, code: 'RESTORE_DIFF'`.
    4. If `--cascade` flag set: walk `tombstone_cascade_chain`, assert every downstream `<engine>/<object>` exists in archival-policy AND its stubs are loadable. Mismatch → `ok: false, code: 'BROKEN_CASCADE_CHAIN'`.
    5. Aggregate. Exit 0 / 1 / 2 per behavior table.

    Create `test/commercial/archival-recovery.test.js`:
    - Test 1: spawn `--simulated`, expect exit 0 (full inventory)
    - Test 2: spawn `--simulated --engine cdp`, expect exit 0 + stdout contains all 3 cdp objects
    - Test 3: spawn `--simulated --format json`, parse JSON, assert `summary.total === <archival-policy length>`
    - Test 4: spawn `--simulated --cascade ecosystem/integration_listing`, expect exit 0 + stdout mentions `install_requests` and `listing_views`
    - Test 5: spawn `--simulated --cascade sales/deal_brief`, expect stdout mentions `proof_pack`, `quote`, `winloss_record`, `handoff_record`
    - Test 6: temporarily corrupt one fixture (add non-timestamp field), spawn CLI, expect exit 1 + stderr `RESTORE_DIFF`
    - Test 7: temporarily move one stub file, spawn CLI, expect exit 2 + stderr `MISSING_ARCHIVAL_STUB`
    - Test 8: temporarily mutate archival-policy to set `tombstone_cascade_chain` of one entry to `["nonexistent/object"]`, spawn `--cascade <that-entry>`, expect exit 1 + stderr `BROKEN_CASCADE_CHAIN`
  </action>
  <verify>
    <automated>node scripts/commercial/test-archival-recovery.cjs --simulated && node --test test/commercial/archival-recovery.test.js</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `scripts/commercial/test-archival-recovery.cjs`
    - File exists: `test/commercial/archival-recovery.test.js`
    - `node scripts/commercial/test-archival-recovery.cjs --simulated` exits 0
    - `node scripts/commercial/test-archival-recovery.cjs --simulated --engine cdp` exits 0
    - `node scripts/commercial/test-archival-recovery.cjs --simulated --cascade sales/deal_brief` exits 0 + stdout mentions all 4 sales cascade objects
    - `node --test test/commercial/archival-recovery.test.js` exits 0 with 8 tests passing
    - `grep -c "RESTORE_DIFF\|BROKEN_CASCADE_CHAIN\|MISSING_ARCHIVAL_STUB" scripts/commercial/test-archival-recovery.cjs` returns 3
    - 9 engine-specific archive/restore stub pairs exist under `lib/markos/contracts/archival-stubs/` (18 files minimum) + 2 default stubs = 20+ files
  </acceptance_criteria>
  <done>
    Archival/recovery CLI gate exits 0 over the expanded manifest, tombstone cascade chains are walkable, and 8 tests prove the gate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| archival-policy.json -> CLI | tampered retention_days could weaken SOC2-I posture |
| stubs -> production | stubs perform identity round-trip; real archival scripts (storage IO) will replace them in gap-closure work |
| tombstone_cascade_chain -> consent withdrawal | broken chain means consent withdrawal does not cascade |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-228-03-01 | Tampering | archival-policy.json `retention_days` | mitigate | per S4 fix: Task 2 acceptance asserts EXPLICIT per-class allow-list (10 classes MUST each have `retention_days >= 2555`); not a fragile floor count |
| T-228-03-02 | Tampering | tombstone_cascade_chain | mitigate | CLI `--cascade` mode walks chain and exits 1 with BROKEN_CASCADE_CHAIN if any link is missing |
| T-228-03-03 | Information disclosure | fixture files containing PII | accept | fixtures are synthetic; no real data |
| T-228-03-04 | Repudiation | restore without verification | mitigate | every recovery-runbook entry has `verification_command`; CLI is the verification command |
| T-228-03-05 | Denial of service | CLI hangs on real archive IO | mitigate | this slice runs `--simulated` only; real archive IO is gap-closure work |
| T-228-03-06 | Elevation of privilege | non-tenant-admin invokes restore | accept | restore scripts are stubs in this slice; real scripts MUST gate on tenant-admin role per recovery-runbook prerequisites — flagged for gap-closure |
| T-228-03-07 | Information disclosure | archived blob contains decrypted PII | accept | archive_target field declares storage location; encryption-at-rest is downstream concern (SOC2-I scope, phase 206) |
</threat_model>

<verification>
- `node --test test/commercial/migration-manifest-coverage.test.js` exits 0 (9 tests)
- `node --test test/commercial/archival-recovery.test.js` exits 0 (8 tests)
- `node scripts/commercial/test-migration-roundtrip.cjs` exits 0 (>=30 entries)
- `node scripts/commercial/test-archival-recovery.cjs --simulated` exits 0
- archival-policy.json covers every migration-manifest entry
- recovery-runbook.json has 9 engine entries
- Tombstone cascades reachable: ecosystem (227 D-65), sales (226 D-56), crm (222), cdp (221) chains all walkable via CLI
- **Per S4 fix:** SOC2-I retention floor asserted via explicit per-class allow-list (10 classes) — node -e check exits 0
</verification>

<success_criteria>
- Gate 8 (no designed obsolescence): every durable-object class has versioned schema + exporter/importer + archival policy + recovery runbook entry + simulated round-trip pass
- Migration manifest expanded from 9 → >=30 entries
- archival-policy.json + recovery-runbook.json exist and pass structural validation
- Tombstone cascade chains match sibling-phase declarations and are walkable
- 17 new tests pass (9 + 8)
- SOC2-I retention floor (2555 days = 7 years) applied to 10 EXPLICIT classes per allow-list (per S4 — not a fragile count)
</success_criteria>

<output>
After completion, create `.planning/phases/228-commercial-os-integration-future-readiness/228-03-SUMMARY.md` documenting: full migration coverage (per-engine count), archival-policy SOC2-I retention map (with explicit 10-class allow-list per S4), tombstone cascade chains by source, and the contract slice 04 will inherit (testing-environment dependencies + cross-engine evidence-trace E2E).
</output>
</output>
