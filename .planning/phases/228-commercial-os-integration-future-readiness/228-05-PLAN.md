---
phase: 228-commercial-os-integration-future-readiness
plan: 05
type: execute
wave: 4
depends_on: ["228-01", "228-02", "228-03", "228-04"]
files_modified:
  - .planning/phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md
  - lib/markos/contracts/commercial-registry.json
  - scripts/commercial/build-readiness-matrix.cjs
  - scripts/commercial/verify-readiness-matrix.cjs
  - scripts/commercial/verify-readiness-from-raw.cjs
  - scripts/commercial/test-adapter-swap.cjs
  - test/commercial/readiness-matrix.test.js
  - test/commercial/com-qa-coverage.test.js
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - COM-01
  - COM-02
  - COM-03
  - COM-04
  - COM-05
  - COM-06
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - QA-05
  - QA-06
  - QA-07
  - QA-08
  - QA-09
  - QA-10
  - QA-11
  - QA-12
  - QA-13
  - QA-14
  - QA-15
user_setup: []

must_haves:
  truths:
    - "Final readiness matrix artifact exists at `.planning/phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md` AND contains a row for every engine in {cdp, crm, email, messaging, conversion, launch, analytics, sales, ecosystem}"
    - "Readiness matrix is machine-verifiable: `node scripts/commercial/verify-readiness-matrix.cjs` parses the markdown and asserts every column for every engine resolves to a passing gate (parity, adapter swap, migration roundtrip, archival recovery, evidence chain, gate detectors)"
    - "commercial-registry.json now includes COM-01..06 + QA-01..15 entries (slice 01 deferred their registration; slice 05 lands them after test infrastructure is real)"
    - "ROADMAP.md Phase 228 block links to the signed-off READINESS-MATRIX.md and updates Status to 'Plans complete; READINESS-MATRIX.md signed off' (per CONTEXT D-30)"
    - "Single-checkpoint sign-off: human operator confirms full chain passes (parity + adapter + migration + archival + evidence + detectors) before flipping ROADMAP.md status — this is the lane closeout signal"
    - "Engine-dir precondition assertion (per CONTEXT D-01 route-back rule): builder asserts `app/api/v1/{cdp,crm,email,messaging,conversion,launch,analytics,sales,ecosystem}/` ALL exist before invoking detectors. Missing dir → builder exits 1 with route-back message naming upstream phase. NO `coverage_pending` translation to `deferred` for missing engine surfaces."
    - "Slice 05 readiness matrix has explicit Chromatic column (G10 includes `verify-chromatic-coverage.cjs` per slice 04 D-24) — Chromatic surface coverage tracked per-engine where applicable"
    - "Adapter-swap CLI gains `--engine` flag (per N2) so per-engine cell-fill in readiness matrix aggregates all channels for that engine"
    - "Per D-34 (RH4 fix — hard follow-through enforcement): sign-off is valid ONLY if either (a) `verify-readiness-matrix.cjs --strict` exits 0, OR (b) every auditable deferral has a written-and-committed follow-up phase entry in ROADMAP.md (e.g., `Phase 228.1 — Deferred-cell closure: <list>`) with explicit dependency linkage and target date. The advisory-only `--strict` mode is REMOVED — `--strict` is the closeout gate."
    - "Per D-35 (RM4 fix — independent QA-15 evidence): QA-15 closeout is validated by `scripts/commercial/verify-readiness-from-raw.cjs` which reads raw gate output JSON files (e.g., `governed-mutation.test.json`, `passive-system-detector.test.json`, `test-adapter-swap.json`) and reconstructs expected matrix cells, then compares to the README matrix. Asserts `compute(matrix) === matrix`. The QA-15 verifier is `verify-readiness-from-raw.cjs`, NOT `verify-readiness-matrix.cjs --qa-id QA-15`."
  artifacts:
    - path: ".planning/phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md"
      provides: "Per-engine readiness table with one row per engine + columns for every gate (1-10) + final SOC2-I posture cell + sign-off signature block + per-engine Chromatic cell"
    - path: "lib/markos/contracts/commercial-registry.json"
      provides: "Updated to include 21 additional entries (COM-01..06 + QA-01..15) — total now >=66 entries"
    - path: "scripts/commercial/build-readiness-matrix.cjs"
      provides: "Builder that runs every prior gate (slice 01-04 CLIs) and emits the matrix with current pass/fail/deferred status per engine per gate. PRECONDITION: asserts engine-dirs exist before invoking detectors."
    - path: "scripts/commercial/verify-readiness-matrix.cjs"
      provides: "Verifier that re-reads the matrix and asserts all cells are 'pass', 'n/a', or auditably 'deferred' (with reason + target_phase + ROADMAP follow-up phase entry per D-34). `--strict` mode (CLOSEOUT GATE per D-34) treats `deferred` as failure UNLESS a follow-up phase exists. `--qa-id <id>` flag surfaces a specific QA row's pass/fail. Exit 1 if any cell is 'fail' or unfilled."
    - path: "scripts/commercial/verify-readiness-from-raw.cjs"
      provides: "Per D-35 (RM4 fix): independent QA-15 verifier that reads raw gate output JSON files and reconstructs expected matrix cells, comparing to README matrix. Asserts `compute(matrix) === matrix`. NOT a self-referential alias to verify-readiness-matrix.cjs."
    - path: "scripts/commercial/test-adapter-swap.cjs"
      provides: "EXTENDED with `--engine <name>` flag (per N2) — runs all channels owned by that engine + aggregates pass/fail. Existing `--channel` flag retained."
    - path: "test/commercial/readiness-matrix.test.js"
      provides: "node --test asserting builder produces complete matrix and verifier exits 0; asserts engine-dir precondition triggers on missing dirs"
    - path: "test/commercial/com-qa-coverage.test.js"
      provides: "node --test asserting every COM-01..06 + QA-01..15 requirement is registered in commercial-registry.json"
  key_links:
    - from: "scripts/commercial/build-readiness-matrix.cjs"
      to: "scripts/commercial/verify-parity-matrix.cjs (slice 01) + test-adapter-swap.cjs (slice 02) + test-migration-roundtrip.cjs (slice 02-03) + test-archival-recovery.cjs (slice 03) + verify-test-coverage.cjs (slice 04) + verify-chromatic-coverage.cjs (slice 04)"
      via: "child_process spawn each upstream CLI per engine; collect exit codes + JSON output; emit consolidated matrix. Engine-dir precondition asserted FIRST."
      pattern: "child_process.*verify-parity-matrix|test-adapter-swap|test-migration-roundtrip|test-archival-recovery|verify-test-coverage|verify-chromatic-coverage"
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md"
      via: "Phase 228 block links to matrix file + status updated to closeout"
      pattern: "READINESS-MATRIX\\.md"
    - from: "scripts/commercial/verify-readiness-matrix.cjs"
      to: ".planning/phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md"
      via: "verifier parses markdown table, validates every cell"
      pattern: "READINESS-MATRIX"
---

<objective>
Run the final commercial-stack audit and emit the signed-off readiness matrix that closes the v4.2.0 commercial lane (per CONTEXT D-21, D-27..30). This slice consumes all four prior slice outputs (registry + adapter swap + migration/archival + test coverage + Chromatic) and produces a single machine-verifiable artifact that proves the eight commercial engines ship as a single coherent Commercial OS.

**Phase-level dependency on 221-227 enforced as precondition** (per CONTEXT D-01 + slice 04 hard-fail detectors): Before slice 05 can build the readiness matrix, all 9 engine API/dashboard/lib directories MUST exist. If any are missing, the builder exits 1 with a route-back-to-upstream-phase message. Phase 228 cannot ship green over absent surfaces. **There is NO `coverage_pending` → `deferred` translation** — missing engine surfaces are BLOCKERS that route back to the upstream phase, not deferrals that get tracked through.

Purpose: Slices 01-04 built the gates. Slice 05 runs them, summarizes the result, requires human sign-off, and updates ROADMAP.md. Without this slice, the prior work is reusable infrastructure; with it, phase 228 hits its exit criteria. This slice also lands COM-01..06 + QA-01..15 in commercial-registry.json (slice 01 deferred those because their tests didn't exist yet — slice 04 created them).

Output: READINESS-MATRIX.md + builder + verifier (with `--strict`/`--qa-id` flags) + extended adapter-swap with `--engine` flag + 2 tests + ROADMAP.md update + human sign-off checkpoint.
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/228-commercial-os-integration-future-readiness/228-CONTEXT.md
@.planning/phases/228-commercial-os-integration-future-readiness/228-RESEARCH.md
@.planning/phases/228-commercial-os-integration-future-readiness/228-01-PLAN.md
@.planning/phases/228-commercial-os-integration-future-readiness/228-02-PLAN.md
@.planning/phases/228-commercial-os-integration-future-readiness/228-03-PLAN.md
@.planning/phases/228-commercial-os-integration-future-readiness/228-04-PLAN.md
@.planning/V4.2.0-INCOMING-18-26-GSD-DISCUSS-RESEARCH-ROUTING.md
@.planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md
@lib/markos/contracts/commercial-registry.json
@lib/markos/contracts/commercial-registry-validator.js

<interfaces>
<!-- The executor consumes prior slice CLIs as sub-steps; emits the final matrix. -->

Required matrix shape (READINESS-MATRIX.md):
```markdown
# Phase 228 — Commercial OS Readiness Matrix

**Generated:** <ISO timestamp>
**Generator:** scripts/commercial/build-readiness-matrix.cjs
**Verifier:** scripts/commercial/verify-readiness-matrix.cjs
**Sign-off:** <name> on <date>  (Sign-off block — populated by Task 3 checkpoint)

## Per-Engine Readiness

| Engine | F-IDs | G1 Identity | G2 Memory | G3 Mutation | G4 Active | G5 Pricing | G6 Evidence | G7 Adapter | G8 Archival | G9 Parity | G10 Tests | G10-Chromatic | SOC2-I |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| cdp        | 5  | pass | n/a  | pass | pass | n/a  | n/a  | n/a        | pass | pass | pass | pass | pass |
| crm        | 5  | n/a  | pass | pass | pass | n/a  | n/a  | n/a        | pass | pass | pass | pass | pass |
| email      | 5  | n/a  | n/a  | pass | n/a  | n/a  | pass | pass       | pass | pass | pass | n/a  | pass |
| messaging  | 5  | n/a  | n/a  | pass | n/a  | n/a  | pass | pass       | pass | pass | pass | n/a  | pass |
| conversion | 5  | n/a  | n/a  | pass | pass | pass | pass | n/a        | pass | pass | pass | n/a  | pass |
| launch     | 5  | n/a  | n/a  | pass | n/a  | pass | pass | pass       | pass | pass | pass | pass | pass |
| analytics  | 5  | n/a  | n/a  | pass | pass | n/a  | pass | pass       | pass | pass | pass | n/a  | pass |
| sales      | 5  | n/a  | pass | pass | pass | pass | pass | n/a        | pass | pass | pass | pass | pass |
| ecosystem  | 5  | n/a  | n/a  | pass | pass | pass | pass | pass       | pass | pass | pass | pass | pass |

## Cross-Cutting Requirements

| Requirement | Status | Verifier | Notes |
|---|---|---|---|
| COM-01 (substrate sharing) | pass | scripts/commercial/verify-test-coverage.cjs | substrate audit pass |
| COM-02 (UI/API/MCP parity) | pass | scripts/commercial/verify-parity-matrix.cjs | 0 unauditable deferrals |
| COM-03 (provider replaceability) | pass | scripts/commercial/test-adapter-swap.cjs | 7 bindings, all swap-tested |
| COM-04 (migration/archival) | pass | scripts/commercial/test-migration-roundtrip.cjs + test-archival-recovery.cjs | 36 round-trips, simulated archival pass |
| COM-05 (cross-phase tests) | pass | scripts/commercial/verify-test-coverage.cjs | 21 reqs traced via --enumerate-qa |
| COM-06 (versioned schemas + override audit) | pass | scripts/commercial/test-archival-recovery.cjs (retention) + parity matrix | SOC2-I retention floor applied |
| QA-01..15 | INDIVIDUAL ROWS | per matrix table 1 below | each row resolved via fs.existsSync — see Phase 228 Cross-Engine Detection Matrix appended to V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md |

## QA-NN Individual Rows
(Each row's verifier MUST resolve to a real file via fs.existsSync — circular alias is rejected by `verify-readiness-matrix.cjs --qa-id <id>`)

| QA-NN | Status | Verifier | Notes |
|---|---|---|---|
| QA-01 | pass | test/commercial/test-coverage-matrix.test.js | test substrate exists |
| QA-02 | pass | test/vitest/commercial-contracts.spec.ts | vitest harness |
| QA-03 | pass | tests/e2e/commercial-evidence-chain.contract.spec.ts | playwright contract harness |
| QA-04 | pass | test/commercial/governed-mutation.test.js | governed-mutation gate |
| QA-05 | pass | test/commercial/passive-system-detector.test.js | passive-system gate |
| QA-06 | pass | test/commercial/pricing-bypass-detector.test.js | pricing-bypass gate |
| QA-07 | pass | test/commercial/evidence-freshness-detector.test.js | evidence-freshness gate |
| QA-08 | pass | test/commercial/adapter-swap.test.js | provider replaceability |
| QA-09 | pass | test/integration/qa-09-ci-aware-policy.test.js | CI-aware policy on detectors |
| QA-10 | pass | test/commercial/parity-matrix.test.js | parity matrix machine-verifiable |
| QA-11 | pass | test/commercial/migration-roundtrip.test.js | migration round-trip per engine |
| QA-12 | pass | test/commercial/archival-recovery.test.js | archival recovery per engine |
| QA-13 | pass | test/integration/qa-13-operability.test.js | operability — coverage CLI runs |
| QA-14 | pass | scripts/commercial/verify-chromatic-coverage.cjs | chromatic visual coverage |
| QA-15 | pass | scripts/commercial/verify-readiness-from-raw.cjs | per D-35 (RM4 fix): independent verifier that reconstructs matrix from raw gate output JSON files (NOT self-referential) |

## Cell legend
- pass    : gate passed (verifier exit 0 or n/a-applicable)
- fail    : gate failed (BLOCKER — phase cannot close)
- deferred: explicit auditable deferral with reason + target_phase in `deferral_log` below (NEVER used for missing engine surfaces — those are routed back to upstream phase per D-01)
- n/a     : gate does not apply to this engine (e.g., G1 only applies to CDP; Chromatic only applies to D-24 surface families)

## Deferral Log
<populated by builder ONLY when explicit auditable deferrals exist (e.g., a Chromatic surface intentionally deferred to a later milestone). Missing engine surfaces do NOT appear here — they cause builder exit 1.>

## Sign-off
<populated by checkpoint Task 3>
```

Engine → applicable gates table (used by the builder to compute n/a vs expected pass):
- G1 (one identity truth): only `cdp` (gate is "did anyone else register CDP-* F-IDs?" — n/a for non-cdp)
- G2 (one commercial memory): only `crm`
- G3 (governed mutation): all engines with write API → all 9 engines
- G4 (no passive systems): cdp, crm, analytics, ecosystem, sales (per CONTEXT D-10)
- G5 (no pricing bypasses): conversion, launch, sales, ecosystem (engines with pricing-sensitive copy)
- G6 (no unsupported claim): email, messaging, launch, analytics, sales, ecosystem (engines emitting claims)
- G7 (no provider lock): email, messaging, analytics, ecosystem, launch (engines with external bindings)
- G8 (archival): all 9 engines
- G9 (UI/API/MCP parity): all 9 engines
- G10 (testability): all 9 engines
- G10-Chromatic (D-24 surfaces): cdp (audience activation), crm (deal rooms — also sales), launch (launch cockpits), sales (deal rooms), ecosystem (partner consoles); approval queues spans all engines but is most relevant to those that produce governed mutations. Per D-24 the 5 surface families map to: approval queues (cross-engine, primary owner = sales+ecosystem+launch+cdp); launch cockpits = launch; audience activation = cdp; deal rooms = sales; partner consoles = ecosystem. Engines without a primary D-24 surface get `n/a` in the G10-Chromatic column.

Engine-dir precondition (asserted by builder BEFORE invoking detectors — per CONTEXT D-01):
```
required_dirs = [
  'app/api/v1/cdp/',
  'app/api/v1/crm/',
  'app/api/v1/email/',
  'app/api/v1/messaging/',
  'app/api/v1/conversion/',
  'app/api/v1/launch/',
  'app/api/v1/analytics/',
  'app/api/v1/sales/',
  'app/api/v1/ecosystem/',
  'app/dashboard/cdp/',
  'app/dashboard/crm/',
  'app/dashboard/analytics/',
  'app/dashboard/ecosystem/',
  'app/dashboard/sales/',
  'lib/markos/cdp/',
  'lib/markos/crm/',
  'lib/markos/email/',
  'lib/markos/messaging/',
  'lib/markos/conversion/',
  'lib/markos/launch/',
  'lib/markos/analytics/',
  'lib/markos/sales/',
  'lib/markos/ecosystem/',
]
engine_to_phase = { cdp: 221, crm: 222, email: 223, messaging: 223, conversion: 224, launch: 224, analytics: 225, sales: 226, ecosystem: 227 }
```

Additional commercial-registry.json entries to land in this slice (COM-01..06 + QA-01..15 = 21 entries):
- engine: `'commercial'` for COM-01..06, phase: `'228-commercial-os-integration-future-readiness'`
- engine: `'quality'` for QA-01..15, phase: `'228-commercial-os-integration-future-readiness'`
- All 21 entries have `ui_status: 'n_a', api_status: 'n_a', mcp_status: 'n_a'` (these are cross-cutting requirements, not user-facing surfaces — set all surfaces to `n_a`; deferred_reason: null)
- Each QA-NN entry has a `linked_verifier` field pointing at its individual verifier path (per slice 04 matrix Table 1) — NOT a roll-up alias
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Land COM-01..06 + QA-01..15 in commercial-registry.json with INDIVIDUAL linked_verifier per QA + coverage test</name>
  <files>
    lib/markos/contracts/commercial-registry.json,
    test/commercial/com-qa-coverage.test.js
  </files>
  <read_first>
    - lib/markos/contracts/commercial-registry.json (slice 01 — 45 entries)
    - lib/markos/contracts/commercial-registry-validator.js (slice 01 — F-ID regex must already accept COM/QA)
    - .planning/REQUIREMENTS.md (COM-01..06 line 200-205 + QA-01..15 line 38 — re-read for canonical text)
    - .planning/phases/228-commercial-os-integration-future-readiness/228-CONTEXT.md (D-27 done criteria — full F-ID enumeration)
    - .planning/phases/228-commercial-os-integration-future-readiness/228-04-PLAN.md (slice 04 QA-01..15 individual mapping in `<interfaces>`)
    - scripts/commercial/build-commercial-registry.cjs (slice 01 — extend with COM/QA seeding)
  </read_first>
  <action>
    1. Modify `scripts/commercial/build-commercial-registry.cjs` (or extend it via a new appender) to also emit 21 additional entries:
       - 6 entries: `f_id: 'COM-01'..'COM-06'`, `engine: 'commercial'`, `phase: '228-commercial-os-integration-future-readiness'`, `ui_status: 'n_a'`, `api_status: 'n_a'`, `mcp_status: 'n_a'`, `deferred_reason: null`. For each, additionally include `linked_verifier` field pointing to the slice CLI that proves the requirement (per `<interfaces>` Cross-Cutting Requirements table).
       - 15 entries: `f_id: 'QA-01'..'QA-15'`, `engine: 'quality'`, `phase: '228-commercial-os-integration-future-readiness'`, `ui_status: 'n_a'`, `api_status: 'n_a'`, `mcp_status: 'n_a'`, `deferred_reason: null`. **Each QA entry has its OWN `linked_verifier` value per slice 04 individual mapping** — NOT a roll-up alias. Use the exact paths from slice 04 `<interfaces>` QA-01..15 mapping (e.g., QA-09 → `test/integration/qa-09-ci-aware-policy.test.js`, QA-13 → `test/integration/qa-13-operability.test.js`, QA-14 → `scripts/commercial/verify-chromatic-coverage.cjs`, QA-15 → `scripts/commercial/verify-readiness-from-raw.cjs` (per D-35, RM4 fix — NOT self-referential to verify-readiness-matrix.cjs)).

    2. Run the builder. Expect commercial-registry.json length to grow from 45 → 66.

    3. Confirm the slice-01 validator still accepts every entry — no validator changes needed (validator already accepts COM- and QA- prefixes per slice 01 regex).

    4. Run slice 01 parity verifier: `node scripts/commercial/verify-parity-matrix.cjs` MUST still exit 0.

    5. Create `test/commercial/com-qa-coverage.test.js` using `node --test`:
       - Test 1: registry length === 66
       - Test 2: every COM-01..06 is registered (loop, assert each `f_id` resolves)
       - Test 3: every QA-01..15 is registered (loop, assert each `f_id` resolves)
       - Test 4: every COM/QA entry has `linked_verifier` pointing to an existing file (resolve via fs.existsSync; for paths with CLI args like `--qa-id QA-15`, strip args and resolve the script path only)
       - Test 5: every COM/QA entry has all 3 surface_status === 'n_a'
       - Test 6: parity verifier still exits 0
       - **Test 7 (per B4 + D-35): every QA-01..15 has a UNIQUE `linked_verifier` path (no roll-up alias).** Count of unique paths === 15. **Per D-35 (RM4 fix): QA-15 now points at `scripts/commercial/verify-readiness-from-raw.cjs` (NOT a self-referential alias to readiness-matrix verifier) — so all 15 paths are genuinely distinct.**
       - **Test 8 (NEW): no QA entry's `linked_verifier` equals the literal string `"see existing matrix"` or contains `"aggregated by"` (proves no hand-waving)**
  </action>
  <verify>
    <automated>node scripts/commercial/build-commercial-registry.cjs && node --test test/commercial/com-qa-coverage.test.js && node scripts/commercial/verify-parity-matrix.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "console.log(require('./lib/markos/contracts/commercial-registry.json').length)"` prints `66`
    - `grep -c '"engine": "commercial"' lib/markos/contracts/commercial-registry.json` returns 6
    - `grep -c '"engine": "quality"' lib/markos/contracts/commercial-registry.json` returns 15
    - `grep -c '"f_id": "COM-' lib/markos/contracts/commercial-registry.json` returns 6
    - `grep -c '"f_id": "QA-' lib/markos/contracts/commercial-registry.json` returns 15
    - `grep -c '"linked_verifier":' lib/markos/contracts/commercial-registry.json` returns >= 21
    - `grep -c "see existing matrix\|aggregated by" lib/markos/contracts/commercial-registry.json` returns 0 (no roll-up alias per B4)
    - `node scripts/commercial/verify-parity-matrix.cjs` exits 0
    - `node --test test/commercial/com-qa-coverage.test.js` exits 0 with 8 tests passing
  </acceptance_criteria>
  <done>
    21 cross-cutting requirement entries land in registry with INDIVIDUAL linked_verifier per QA-01..15 (no roll-up alias per B4), parity verifier still passes, 8-test coverage suite proves shape + no roll-up.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build readiness matrix + verifier with engine-dir precondition + extend adapter-swap with --engine flag</name>
  <files>
    .planning/phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md,
    scripts/commercial/build-readiness-matrix.cjs,
    scripts/commercial/verify-readiness-matrix.cjs,
    scripts/commercial/test-adapter-swap.cjs,
    test/commercial/readiness-matrix.test.js
  </files>
  <read_first>
    - .planning/phases/228-commercial-os-integration-future-readiness/228-01-PLAN.md (verify-parity-matrix.cjs exit codes)
    - .planning/phases/228-commercial-os-integration-future-readiness/228-02-PLAN.md (test-adapter-swap.cjs + test-migration-roundtrip.cjs exit codes)
    - .planning/phases/228-commercial-os-integration-future-readiness/228-03-PLAN.md (test-archival-recovery.cjs exit codes + cascade behavior)
    - .planning/phases/228-commercial-os-integration-future-readiness/228-04-PLAN.md (verify-test-coverage.cjs --enumerate-qa + verify-chromatic-coverage.cjs + detector hard-fail behavior)
    - .planning/phases/228-commercial-os-integration-future-readiness/228-CONTEXT.md (D-01 route-back rule + D-27..30 done criteria + D-21 readiness matrix slice)
    - lib/markos/contracts/commercial-registry.json (post-Task-1 — 66 entries)
    - lib/markos/contracts/adapter-registry.json (slice 02)
    - lib/markos/contracts/migration-manifest.json (slice 03 — 36 entries)
    - lib/markos/contracts/archival-policy.json (slice 03)
    - lib/markos/contracts/recovery-runbook.json (slice 03)
    - scripts/commercial/test-adapter-swap.cjs (slice 02 — extend with --engine)
  </read_first>
  <action>
    1. **EXTEND `scripts/commercial/test-adapter-swap.cjs` with `--engine <name>` flag (per N2 fix):**
       - Engine→channel mapping: email→[email], messaging→[sms,whatsapp], analytics→[analytics_sink], ecosystem→[ecosystem_directory,ecosystem_dev_events], launch→[launch_distribution]
       - When `--engine <name>` is set, CLI runs all channels for that engine, aggregates pass/fail, exits 0 if all pass.
       - Existing `--channel <c>` flag retained.
       - Existing `--no-write` and `--format json` flags retained.
       - Add a unit test in slice 02's existing `test/commercial/adapter-swap.test.js` (or as Test 14 in that file) — but since this is a slice 05 modification, instead add the test to `test/commercial/readiness-matrix.test.js` Task 2 Test 8 (see test list below).

    2. Create `scripts/commercial/build-readiness-matrix.cjs`:
       - **STEP 1 (PRECONDITION — per CONTEXT D-01 route-back rule per B2 fix):** Before invoking ANY detector, builder asserts every required dir from `<interfaces>` engine_dir_precondition list exists via `fs.existsSync`. If ANY missing → exit 1 with stderr `MISSING_ENGINE_DIRS: <list> — upstream phases <list> must ship before Phase 228 readiness can build. Route gap back to upstream phase per CONTEXT D-01.` Builder does NOT translate missing dirs to `coverage_pending` or `deferred`. Phase 228 acceptance is gated on 221-227 being landed.
       - **STEP 2:** For each of the 9 engines (after precondition passes), **and for each child CLI invocation, write the raw JSON output to `.gate-output/<gate-id>-<engine>.json` BEFORE aggregating into the matrix (per D-35, RM4 fix — this produces the audit trail verify-readiness-from-raw.cjs consumes)**:
         - G1: if engine === 'cdp', spawn `node scripts/commercial/verify-parity-matrix.cjs --engine cdp --format json`; assert no other engine has registered a `CDP-*` F-ID. Cell: `pass` (or `n/a` for non-cdp engines).
         - G2: same logic for `crm` engine and `CRM-*` F-IDs.
         - G3: spawn `node --test test/commercial/governed-mutation.test.js` (detector now has hard-fail-on-missing-dirs from slice 04 — if precondition passed, detector will run; if detector fails for content reasons, cell = `fail` BLOCKER, not `deferred`).
         - G4: spawn `node --test test/commercial/passive-system-detector.test.js`. Pass / fail. NO `coverage_pending` translation.
         - G5: spawn `node --test test/commercial/pricing-bypass-detector.test.js`. Pass / fail per engine table in `<interfaces>`.
         - G6: spawn `node --test test/commercial/evidence-freshness-detector.test.js`. Pass / n/a per engine table.
         - G7: for engines in {email, messaging, analytics, ecosystem, launch} → **spawn `node scripts/commercial/test-adapter-swap.cjs --engine <engine> --no-write --format json` (USING NEW --engine FLAG per N2)**; cell `pass` if all bindings for that engine pass. n/a for engines without external bindings.
         - G8: spawn `node scripts/commercial/test-migration-roundtrip.cjs --engine <engine> --format json` AND `node scripts/commercial/test-archival-recovery.cjs --simulated --engine <engine> --format json`. Both must pass for cell to be `pass`.
         - G9: spawn `node scripts/commercial/verify-parity-matrix.cjs --engine <engine> --format json`. Cell `pass` if engine has zero unauditable deferrals.
         - G10: spawn `node scripts/commercial/verify-test-coverage.cjs --format json`; if its `summary.failures` includes any reference to this engine → `fail`; else `pass`.
         - **G10-Chromatic (NEW per B3 + D-24):** spawn `node scripts/commercial/verify-chromatic-coverage.cjs --format json`. Engine cell is `pass` if the engine's primary D-24 surface family has Chromatic coverage; `n/a` if engine has no primary D-24 surface (per `<interfaces>` G10-Chromatic mapping). For D-24 cross-cutting "approval queues" surface, cell maps to all engines that produce governed mutations.
         - SOC2-I cell: derived: `pass` if G3 + G6 + G8 are all `pass`/`n/a` AND archival-policy entries for this engine all have `retention_days >= 365` AND tombstone_cascade_chain non-empty for at least one entry.
       - For cross-cutting COM-01..06 + QA-01..15 rows: each cell maps to its `linked_verifier` script's exit code. **Use INDIVIDUAL QA paths per Task 1 — no roll-up.**
       - Emit `.planning/phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md` per `<interfaces>` shape exactly (with G10-Chromatic column AND QA-NN Individual Rows section).
       - Builder accepts `--include-real-e2e` flag — when set, builder also spawns `npx playwright test tests/e2e/commercial-evidence-chain.spec.ts` (slice 04 E2E suite no longer env-gated per B1 fix). The E2E run becomes a hard cell in the matrix (not a deferral).
       - Builder is idempotent — re-running regenerates the matrix; all timestamps and statuses come from the live CLIs.
       - **NO `coverage_pending` translation:** any cell that cannot be computed is reported as `fail` (BLOCKER) with stderr explanation. Per B2, missing engine dirs trigger STEP 1 precondition exit 1; cells that fail for content reasons (e.g., detector finds a real violation) are `fail` BLOCKERS that prevent sign-off.

    3. Create `scripts/commercial/verify-readiness-matrix.cjs`:
       - Read `READINESS-MATRIX.md`, parse the per-engine table.
       - Assert: every cell is `pass`, `n/a`, or `deferred` with an entry in `## Deferral Log`. Any `fail` or unfilled cell → exit 1.
       - For each `deferred` cell, assert the deferral-log entry contains a non-empty `reason` AND a `target_phase` for resolution AND a clear statement that the deferral is NOT for a missing engine surface (those are routed back to upstream phases, not deferred).
       - **`--strict` flag (per D-34 — RH4 fix, CLOSEOUT GATE, replaces N3 advisory-only mode):** treats `deferred` as failure UNLESS the deferral has a corresponding follow-up phase committed to `.planning/ROADMAP.md`. The verifier scans ROADMAP.md for entries matching the pattern `### Phase 228\.\d+:.*<gate>.*<engine>` (or alternatively a structured `**Closes Deferral:** <id>` line). For each deferred cell, asserts at least one matching ROADMAP entry exists. If any deferred cell has NO follow-up phase → exit 1 with stderr `MISSING_FOLLOWUP_PHASE:<engine>/<gate> — D-34 requires every deferral to have a follow-up phase in ROADMAP.md before sign-off`.
       - **`--qa-id <id>` flag (per B4):** outputs a single QA row's status in JSON, RESOLVES the linked_verifier path via fs.existsSync, exits 0 if path exists AND row status is `pass`, exit 1 otherwise. **Per D-35 (RM4 fix): the QA-15 verifier is now `scripts/commercial/verify-readiness-from-raw.cjs`, NOT this script with `--qa-id QA-15`. The self-referential alias is REMOVED.**

    4. **NEW per D-35 (RM4 fix): create `scripts/commercial/verify-readiness-from-raw.cjs`** — independent QA-15 verifier:
       - Reads raw gate output JSON files from a known location (`.gate-output/<gate-id>.json` — each gate CLI writes its raw result here when invoked with `--write-raw <path>`). Sources include `.gate-output/parity-matrix.json`, `.gate-output/test-adapter-swap.json`, `.gate-output/test-migration-roundtrip.json`, `.gate-output/test-archival-recovery.json`, `.gate-output/governed-mutation.json`, `.gate-output/passive-system.json`, `.gate-output/pricing-bypass.json`, `.gate-output/evidence-freshness.json`, `.gate-output/verify-test-coverage.json`, `.gate-output/verify-chromatic-coverage.json`.
       - Reconstructs the expected per-engine readiness matrix cells from these raw outputs (applies the same engine→gate mapping as `build-readiness-matrix.cjs`).
       - Reads the actual `READINESS-MATRIX.md` per-engine table.
       - Asserts `reconstructed_matrix === actual_matrix` (deep equal). Mismatch → exit 1 with diff in stderr.
       - This proves QA-15 is independent: the verifier doesn't ask `verify-readiness-matrix.cjs` if its own row says pass — it reconstructs the matrix from the raw gate JSON outputs and compares.
       - To make this work, **build-readiness-matrix.cjs MUST be modified to write each child CLI's raw JSON output to `.gate-output/<gate-id>.json` BEFORE aggregating into the matrix**. This produces the audit trail `verify-readiness-from-raw.cjs` consumes.
       - Exit 0 if matrix is fully `pass`/`n/a` (or auditably `deferred` in non-strict mode), exit 1 otherwise.
       - **D-34 closeout rule (replaces N3 advisory-only):** `--strict` is THE sign-off gate. Sign-off can ONLY succeed if `--strict` exits 0. Auditable deferrals are allowed only if a follow-up phase is committed to ROADMAP.md before sign-off. The non-strict mode (no flag) is for in-progress development checks during slices 02-04 — it is NEVER the basis for sign-off. This is documented at the top of the script as a comment block citing D-34 + RH4.

    4. Create `test/commercial/readiness-matrix.test.js`:
       - Test 1: spawn builder, expect exit 0, assert matrix file exists (precondition: engine dirs must exist; if not, this test will surface that the upstream phases need to land first — that is the correct failure mode)
       - Test 2: matrix file contains rows for all 9 engines (regex check)
       - Test 3: matrix file contains all 13 gate columns (G1..G10 + G10-Chromatic + F-IDs + SOC2-I — note 13 columns now per B3)
       - Test 4: spawn verifier (non-strict), expect exit 0
       - Test 5: spawn verifier `--strict`, exit code documented (0 = lane fully clean, 1 = auditable deferrals exist; either is acceptable depending on milestone state — Task 3 checkpoint resolves)
       - Test 6: matrix contains cross-cutting requirements section with COM-01..06 + QA-01..15 (21 individual rows; QA section has its own table with 15 rows per `<interfaces>`)
       - Test 7: temporarily mutate a matrix cell to `fail`, spawn verifier, expect exit 1
       - Test 8 (NEW per N2): spawn `node scripts/commercial/test-adapter-swap.cjs --engine email --no-write`, expect exit 0 (proves --engine flag works)
       - Test 9 (NEW per B4): spawn `node scripts/commercial/verify-readiness-matrix.cjs --qa-id QA-09`, expect exit 0 + JSON output containing `qa_id: 'QA-09'` and `verifier: 'test/integration/qa-09-ci-aware-policy.test.js'`
       - Test 10 (NEW per B4): spawn `node scripts/commercial/verify-readiness-matrix.cjs --qa-id QA-13`, expect exit 0 + verifier resolves to `test/integration/qa-13-operability.test.js`
       - Test 11 (per B4 + D-35): spawn `node scripts/commercial/verify-readiness-from-raw.cjs`, expect exit 0 (or exit 2 if `.gate-output/` not yet populated); verifier path is `scripts/commercial/verify-readiness-from-raw.cjs` — NOT self-referential to readiness-matrix verifier per RM4 fix
       - Test 12 (NEW per B2): temporarily rename `app/api/v1/cdp/` (if it exists) to a backup; spawn builder, expect exit 1 + stderr `MISSING_ENGINE_DIRS`; restore. (This test only runs if `app/api/v1/cdp/` exists; otherwise it pre-passes with note "engine dir already missing — upstream phase 221 must ship".)
       - Test 13 (NEW per B2): grep matrix file for `coverage_pending` — count MUST be 0 (proves no `coverage_pending` translation to `deferred`)
       - **Test 14 (NEW per D-34, RH4 fix): when `verify-readiness-matrix.cjs --strict` is invoked AND a deferred cell exists WITHOUT a corresponding ROADMAP `Phase 228.X` follow-up entry → CLI exits 1 with `MISSING_FOLLOWUP_PHASE`. To exercise this: temporarily mutate matrix to add a deferred cell with no ROADMAP follow-up; spawn `--strict`; expect exit 1 + stderr `MISSING_FOLLOWUP_PHASE`. Restore.**
       - **Test 15 (NEW per D-34): when `--strict` is invoked AND a deferred cell HAS a ROADMAP `Phase 228.X` follow-up entry → CLI exits 0 (deferral with follow-up is allowed). To exercise: temporarily add deferred cell + temporarily append `### Phase 228.1: Deferred-cell closure — <gate>/<engine>` to ROADMAP; spawn `--strict`; expect exit 0. Restore.**
       - **Test 16 (NEW per D-35, RM4 fix): spawn `node scripts/commercial/verify-readiness-from-raw.cjs`; if `.gate-output/` exists with raw outputs → expect exit 0 AND assert reconstructed matrix matches actual; if `.gate-output/` missing → expect exit 2 with `MISSING_GATE_OUTPUTS — invoke build-readiness-matrix.cjs first`.**
  </action>
  <verify>
    <automated>node scripts/commercial/build-readiness-matrix.cjs && node scripts/commercial/verify-readiness-matrix.cjs && node --test test/commercial/readiness-matrix.test.js</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `.planning/phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md`
    - File exists: `scripts/commercial/build-readiness-matrix.cjs`
    - File exists: `scripts/commercial/verify-readiness-matrix.cjs`
    - File exists: `test/commercial/readiness-matrix.test.js`
    - `scripts/commercial/test-adapter-swap.cjs` has `--engine` flag (verify by `grep -c "'--engine'\\|--engine <" scripts/commercial/test-adapter-swap.cjs` returns >= 1)
    - `node scripts/commercial/build-readiness-matrix.cjs` exits 0 (idempotent re-run also exits 0; assumes engine dirs landed — if not, exits 1 with `MISSING_ENGINE_DIRS` per B2 precondition)
    - `node scripts/commercial/verify-readiness-matrix.cjs` exits 0
    - `node scripts/commercial/verify-readiness-matrix.cjs --qa-id QA-09` exits 0 and outputs JSON with `verifier` field resolving to a real file
    - `grep -c "| cdp\|| crm\|| email\|| messaging\|| conversion\|| launch\|| analytics\|| sales\|| ecosystem" .planning/phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md` returns >= 9
    - `grep -c "| COM-0\|| QA-0\|| QA-1" .planning/phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md` returns >= 21 (cross-cutting reqs + 15 individual QA rows)
    - `grep -c "G10-Chromatic" .planning/phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md` returns >= 1 (per B3 — Chromatic column present)
    - `grep -c "G1 Identity\|G2 Memory\|G3 Mutation\|G4 Active\|G5 Pricing\|G6 Evidence\|G7 Adapter\|G8 Archival\|G9 Parity\|G10 Tests\|G10-Chromatic\|SOC2-I" .planning/phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md` returns >= 12
    - `grep -c "## Deferral Log" .planning/phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md` returns 1
    - `grep -c "## Sign-off" .planning/phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md` returns 1
    - `grep -c "## QA-NN Individual Rows" .planning/phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md` returns 1 (per B4 — individual QA section, no roll-up)
    - `grep -c "coverage_pending" .planning/phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md` returns 0 (per B2 — no coverage_pending translation)
    - `grep -c "MISSING_ENGINE_DIRS" scripts/commercial/build-readiness-matrix.cjs` returns >= 1 (per B2 — precondition assertion present)
    - `node --test test/commercial/readiness-matrix.test.js` exits 0 with 16 tests passing (13 prior + 3 new per D-34/D-35)
    - **Per D-34 (RH4 fix): `verify-readiness-matrix.cjs --strict` enforces follow-up phase requirement:**
      - `grep -c "MISSING_FOLLOWUP_PHASE\|228\\\\.\\\\d" scripts/commercial/verify-readiness-matrix.cjs` returns >= 1 (proves follow-up phase scan implemented)
    - **Per D-35 (RM4 fix): `verify-readiness-from-raw.cjs` exists and is independent of `verify-readiness-matrix.cjs`:**
      - File exists: `scripts/commercial/verify-readiness-from-raw.cjs`
      - `grep -c "\\.gate-output\|raw.*output\|reconstructed_matrix" scripts/commercial/verify-readiness-from-raw.cjs` returns >= 1 (proves raw-output reading implemented)
      - `grep -c "verify-readiness-matrix" scripts/commercial/verify-readiness-from-raw.cjs` returns 0 (proves NO self-reference to readiness-matrix verifier per RM4)
    - **Per D-35: `build-readiness-matrix.cjs` writes raw outputs to `.gate-output/`:**
      - `grep -c "\\.gate-output\|writeFileSync.*gate-output" scripts/commercial/build-readiness-matrix.cjs` returns >= 1 (proves audit trail produced)
  </acceptance_criteria>
  <done>
    Readiness matrix builds with engine-dir precondition (per B2), `--strict` is the closeout gate enforcing follow-up-phase rule per D-34 (RH4 fix), `verify-readiness-from-raw.cjs` provides independent QA-15 evidence per D-35 (RM4 fix), 13 columns including G10-Chromatic, 9 engine rows + 21 cross-cutting + 15 individual QA rows, deferral log + sign-off blocks present, NO `coverage_pending` translation, 16 tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human sign-off — final lane closeout (per CONTEXT D-30)</name>
  <files>
    .planning/phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md,
    .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md (Task 2)
    - .planning/phases/228-commercial-os-integration-future-readiness/228-CONTEXT.md (D-30 final readiness sign-off + D-01 route-back rule)
    - .planning/ROADMAP.md (Phase 228 block lines 487-493)
  </read_first>
  <what-built>
    Slices 02-04 produced parity registry + adapter swap + migration/archival + test infrastructure + 4 hard-failing gate detectors + single E2E suite + Chromatic stories + QA-01..15 individual test files. Slice 05 ran them all and emitted READINESS-MATRIX.md with engine-dir precondition asserted. The matrix is the lane-closeout artifact.

    **Important per B2:** If slice 05 builder failed with `MISSING_ENGINE_DIRS`, that means upstream phases 221-227 have not landed. This is the correct failure mode — Phase 228 cannot ship green over absent surfaces. The route-back is to those upstream phases, not a deferral in 228.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md` and review:
       - Every engine row: confirm gate cells match expected pass/n/a state per CONTEXT D-07..16 + D-24 G10-Chromatic mapping
       - Every `deferred` cell (if any): confirm `## Deferral Log` has a reason + target_phase for resolution AND that the deferral is NOT for a missing engine surface
       - Cross-cutting requirements: confirm every COM-01..06 + QA-01..15 row says `pass`
       - QA-NN Individual Rows section: confirm 15 rows present with INDIVIDUAL `linked_verifier` paths (no roll-up)
       - SOC2-I column: confirm every engine is `pass` (D-30 SOC2-I posture)
       - G10-Chromatic column: confirm 5 D-24 surface families covered (per B3)
    2. Run the full gate suite end-to-end:
       ```bash
       node scripts/commercial/build-readiness-matrix.cjs --include-real-e2e   # slice 04 E2E now runs without env-gating skip per B1
       node scripts/commercial/verify-readiness-matrix.cjs                      # default = sign-off mode (deferred OK with reason)
       node scripts/commercial/verify-readiness-matrix.cjs --strict             # strict = post-sign-off CI mode (deferred = fail)
       ```
       - **Sign-off mode (no flag):** MUST exit 0 — required for human approval gate
       - **Strict mode (`--strict`):** exit code informational. May exit 1 if any auditable deferrals exist; that is acceptable for sign-off as long as deferrals are listed in the sign-off doc per N3 reconciliation. Slice 06 verify command will treat strict-mode exit code as advisory unless all deferrals are explicitly listed in sign-off doc.
    3. **D-34 sign-off rule (CLOSEOUT GATE — RH4 fix, replaces N3 advisory-only mode):** Human can only sign off if EITHER:
       - (a) `verify-readiness-matrix.cjs --strict` exits 0 (zero deferred cells), OR
       - (b) every deferred cell has a corresponding follow-up phase entry COMMITTED to `.planning/ROADMAP.md` (e.g., `### Phase 228.1: Deferred-cell closure — <list>` with `**Depends on:** Phase 228 sign-off` and a target completion date).

       **The verifier (`verify-readiness-matrix.cjs --strict`) checks (b) automatically:** when in `--strict` mode, deferred cells are tolerated ONLY if `node -e "const fs=require('fs'); const r=fs.readFileSync('.planning/ROADMAP.md','utf8'); const need=<deferred list>; for(const d of need){if(!r.match(new RegExp('Phase 228\\\\.\\\\d+.*'+d.gate+'.*'+d.engine))){ process.exit(1); }}"` exits 0. The script that performs this check is part of `verify-readiness-matrix.cjs --strict`'s implementation per D-34.

       **No advisory-only mode.** `--strict` is THE closeout gate. Sign-off is INVALID without it exiting 0.
    4. If acceptable, sign the matrix:
       - Edit `## Sign-off` block in READINESS-MATRIX.md to add `Signed off by: <your name> on <ISO date>`. If `--strict` exits 1, ALSO list every deferred cell in the sign-off block with reason + target_phase.
       - Update `.planning/ROADMAP.md` Phase 228 block:
         - Change `**Status:** Discuss/context/research drafted` to `**Status:** Plans 01-06 complete; READINESS-MATRIX.md signed off <ISO date>`.
         - Append `**Readiness Matrix:** [.planning/phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md](./phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md)` to the Phase 228 block.
         - Update `**Plans:** 6/6 plans complete`.
       - Run `node scripts/commercial/verify-readiness-matrix.cjs` (sign-off mode) one final time. If it exits 0, the lane is closed.

    Acceptance signals (in order of priority):
    - 'approved' → matrix is signed (with deferred cells listed if any), ROADMAP updated, lane closed
    - 'defer X gate Y reason Z' → record additional deferral in matrix WITH target_phase, do NOT update ROADMAP, route gap-closure plan via slice 06
    - 'fix X' → describes a specific blocker; slice 05 cannot complete; revisit slice 02/03/04 to address
    - 'route back to phase NNN' → if `MISSING_ENGINE_DIRS` triggered, route gap to upstream phase NNN; slice 05 BLOCKED until that phase lands
  </how-to-verify>
  <action>
    Execute the verification described in <how-to-verify> and present the matrix state to the human operator. Wait for explicit "approved" resume-signal before flipping ROADMAP.md status. On approval: edit READINESS-MATRIX.md "## Sign-off" block (listing deferred cells if `--strict` exits 1) + edit ROADMAP.md Phase 228 block per <how-to-verify> step 4. Re-run "node scripts/commercial/verify-readiness-matrix.cjs" (sign-off mode) — must exit 0 to confirm closure.
  </action>
  <verify>
    <automated>node scripts/commercial/verify-readiness-matrix.cjs --strict && grep -c "Signed off by:" .planning/phases/228-commercial-os-integration-future-readiness/READINESS-MATRIX.md && grep -c "signed off" .planning/ROADMAP.md</automated>
  </verify>
  <done>
    Human approved closure. READINESS-MATRIX.md has "Signed off by: <signer> on <ISO date>" line AND lists all deferred cells (if any) with target_phase. ROADMAP.md Phase 228 block reflects "signed off" status and links to the matrix. "node scripts/commercial/verify-readiness-matrix.cjs" exits 0 (sign-off mode); `--strict` exit code documented per N3 reconciliation.
  </done>
  <resume-signal>Type "approved" + add `Signed off by: <your name>` line to READINESS-MATRIX.md (and list deferred cells with target_phase if `--strict` exits 1), OR describe issues that block closure</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| matrix builder -> child CLIs | builder trusts CLI exit codes; tampered CLI could falsely report pass |
| sign-off block -> ROADMAP.md | sign-off line is human-authored; cannot be forged by Claude alone (checkpoint enforces) |
| --strict flag -> closeout | strict mode rejects deferred cells; non-strict allows tracked deferrals (per N3 reconciliation) |
| engine-dir precondition -> route-back | missing dirs trigger exit 1 with route-back-to-upstream-phase message; NO `coverage_pending` translation per B2 |
| QA-NN traceability -> fs.existsSync | every QA path must resolve via fs.existsSync (per B4 — no roll-up alias, no circular reference) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-228-05-01 | Tampering | READINESS-MATRIX.md after sign-off | mitigate | git history is the audit trail; future builder runs will overwrite the matrix and force re-sign |
| T-228-05-02 | Tampering | child CLI exit code | mitigate | each child CLI re-validates its own inputs (slice 01-04 acceptance criteria); detectors hard-fail on missing dirs (slice 04 B2 fix) |
| T-228-05-03 | Information disclosure | matrix contains engine status | accept | non-secret; serves auditor SOC2-I review |
| T-228-05-04 | Repudiation | sign-off without verification | mitigate | checkpoint resume-signal requires explicit "approved" + ROADMAP edit + final sign-off-mode pass; deferred cells must be listed in sign-off block per N3 |
| T-228-05-05 | Denial of service | builder hangs on Playwright run | mitigate | builder uses `--include-real-e2e` flag; without flag, evidence-chain row is read from prior CI run timestamp; with flag, runs with timeout |
| T-228-05-06 | Elevation of privilege | claude updates ROADMAP.md without sign-off | mitigate | the checkpoint task is `checkpoint:human-verify`, autonomous=false, blocking gate; ROADMAP edit is human-authored |
| T-228-05-07 | Information disclosure | deferral_log contains target_phase pointers | accept | target_phase is a planning artifact; non-sensitive |
| T-228-05-08 | Repudiation | missing engine dir silently translated to deferred | mitigate | per B2 fix: builder STEP 1 precondition asserts every engine dir exists BEFORE invoking detectors; missing dir → exit 1 with route-back message; NO `coverage_pending` translation |
| T-228-05-09 | Tampering | QA-NN circular alias | mitigate | per B4 fix: 15 individual QA rows in matrix; `--qa-id` flag resolves linked_verifier via fs.existsSync; Test 7 in com-qa-coverage asserts uniqueness |
| T-228-05-10 | Repudiation | Chromatic deferral hidden | mitigate | per B3 fix: G10-Chromatic column explicit in matrix; if Chromatic infrastructure deferred, must be in `<deferred>` section of CONTEXT.md (not silently dropped) |
</threat_model>

<verification>
- `node scripts/commercial/build-readiness-matrix.cjs` exits 0 (or exits 1 with `MISSING_ENGINE_DIRS` if upstream phases 221-227 not landed — that is the correct failure mode per CONTEXT D-01)
- `node scripts/commercial/verify-readiness-matrix.cjs` exits 0 (sign-off mode)
- `node scripts/commercial/verify-readiness-matrix.cjs --strict` exit code documented per N3 (sign-off can succeed with auditable deferrals if listed in sign-off block; strict mode is post-sign-off CI guard)
- `node scripts/commercial/verify-readiness-matrix.cjs --qa-id <id>` exits 0 for any of QA-01..15 (proves individual resolution per B4)
- `node --test test/commercial/com-qa-coverage.test.js` exits 0 (8 tests)
- `node --test test/commercial/readiness-matrix.test.js` exits 0 (13 tests)
- `node scripts/commercial/verify-parity-matrix.cjs` exits 0 (still passing after registry expansion to 66 entries)
- READINESS-MATRIX.md has 9 engine rows + 21 cross-cutting rows (with QA-01..15 individually enumerated) + 13 columns (G1..G10 + G10-Chromatic + F-IDs + SOC2-I) + sign-off block
- ROADMAP.md Phase 228 block updated with status + readiness-matrix link (post-checkpoint)
- `coverage_pending` count in READINESS-MATRIX.md = 0 (per B2)
- `scripts/commercial/test-adapter-swap.cjs` has `--engine` flag (per N2)
</verification>

<success_criteria>
- Readiness matrix exists, machine-verifiable, signed off
- All 10 gates + G10-Chromatic have a green or auditably-deferred cell for every engine they apply to (per B3)
- COM-01..06 + QA-01..15 registered in commercial-registry.json (66 total entries) with INDIVIDUAL linked_verifier per QA (per B4 — no roll-up alias)
- Engine-dir precondition asserted before any detector runs (per B2 — NO `coverage_pending` translation)
- ROADMAP.md updated to reflect closeout
- Phase 228 done criteria (CONTEXT D-27..30) all satisfied
- 21 new tests pass (8 + 13)
- `--strict` vs sign-off mode reconciled per N3
</success_criteria>

<output>
After completion, create `.planning/phases/228-commercial-os-integration-future-readiness/228-05-SUMMARY.md` documenting:
- Registry final size (66 entries with INDIVIDUAL QA-NN linked_verifier per B4)
- Readiness matrix per-engine status (with G10-Chromatic column per B3)
- Engine-dir precondition behavior (per B2 — route-back to upstream phase if missing)
- Deferral log entries (if any — listing target_phase for each)
- Sign-off date + signer (with deferred cells listed per N3 if `--strict` exit 1)
- ROADMAP.md update lines changed
- Adapter-swap `--engine` flag added (per N2)

This summary is the lane-closeout signal slice 06 will cite in canon sync + handoff.
</output>
</output>
