---
phase: 41-dynamic-skeleton-template-generator
plan: 02
type: execute
wave: 2
depends_on:
  - 41-01
files_modified:
  - onboarding/backend/agents/example-resolver.cjs
  - onboarding/backend/path-constants.cjs
  - onboarding/backend/agents/skeleton-generator.cjs
  - onboarding/backend/handlers.cjs
  - test/skeleton-generator.test.js
autonomous: true
requirements:
  - LIT-07
  - LIT-08

must_haves:
  truths:
    - "POST /approve writes MIR/MSP drafts first, then awaits skeleton generation before composing the HTTP 200 response (per D-41-06)."
    - "Skeleton generation always attempts all five disciplines and reports partial failures without changing approve success status (per D-41-05 and D-41-06)."
    - "Generated skeleton files are derived from model-specific base templates and include frontmatter + pain-point interpolation (per D-41-01 through D-41-04)."
    - "Resolver and generator contracts degrade gracefully to empty-string/template-miss outcomes, never throwing for missing files or unknown model slugs (per D-41-07)."
  artifacts:
    - path: "onboarding/backend/agents/example-resolver.cjs"
      provides: "resolveSkeleton(discipline, businessModel, basePath) export sharing MODEL_SLUG"
      exports: ["resolveExample", "resolveSkeleton", "MODEL_SLUG", "DEFAULT_BASE"]
    - path: "onboarding/backend/agents/skeleton-generator.cjs"
      provides: "generateSkeletons + interpolation/frontmatter utilities"
      exports: ["generateSkeletons", "interpolatePainPoints", "buildFrontmatter"]
    - path: "onboarding/backend/path-constants.cjs"
      provides: "SEED_PATH constant for onboarding-seed.json"
      exports: ["SEED_PATH"]
    - path: "onboarding/backend/handlers.cjs"
      provides: "approve hook integration returning skeletons summary"
      exports: ["handleApprove"]
    - path: "test/skeleton-generator.test.js"
      provides: "Phase 41 behavior coverage for resolver/generator/approve integration"
      exports: []
  key_links:
    - from: "onboarding/backend/handlers.cjs"
      to: "onboarding/backend/agents/skeleton-generator.cjs"
      via: "await generateSkeletons(seed, approvedDrafts) between write guard and execution readiness"
      pattern: "generateSkeletons\(seed, approvedDrafts\)"
    - from: "onboarding/backend/handlers.cjs"
      to: "onboarding/backend/path-constants.cjs"
      via: "SEED_PATH read using fs.existsSync + fs.readFileSync"
      pattern: "SEED_PATH|onboarding-seed.json"
    - from: "onboarding/backend/agents/skeleton-generator.cjs"
      to: "onboarding/backend/agents/example-resolver.cjs"
      via: "resolveSkeleton + MODEL_SLUG"
      pattern: "resolveSkeleton|MODEL_SLUG"
---

<objective>
Implement the full runtime and test contract for Phase 41 skeleton hydration: resolver extension, generator module, approve hook integration, and executable tests.

Purpose: Deliver approval-triggered skeleton generation that is deterministic, non-fatal on partial failure, and fully validated by automated tests.
Output: New generator module, extended resolver/paths/handlers integration, and green Phase 41 test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-dynamic-skeleton-template-generator/41-CONTEXT.md
@.planning/phases/41-dynamic-skeleton-template-generator/41-RESEARCH.md
@.planning/phases/41-dynamic-skeleton-template-generator/41-VALIDATION.md
@onboarding/backend/agents/example-resolver.cjs
@onboarding/backend/path-constants.cjs
@onboarding/backend/handlers.cjs
@test/skeleton-generator.test.js
</context>

<interfaces>
From onboarding/backend/agents/example-resolver.cjs current state:
```javascript
function resolveExample(templateName, businessModel, templateSubdir = '', basePath = DEFAULT_BASE)
module.exports = { resolveExample, MODEL_SLUG, DEFAULT_BASE };
```

From onboarding/backend/path-constants.cjs current state:
```javascript
module.exports = {
  PROJECT_ROOT,
  ONBOARDING_DIR,
  BACKEND_DIR,
  PROTOCOL_DIR,
  TEMPLATES_DIR,
  MIR_TEMPLATES,
  MSP_TEMPLATES,
  LEGACY_LOCAL_DIR,
  MARKOS_LOCAL_DIR,
  COMPATIBILITY_LOCAL_DIRS,
  PROJECT_CONFIG_PATH,
  INSTALL_MANIFEST_PATH,
  CONFIG_PATH,
  SCHEMA_PATH
};
```

From onboarding/backend/handlers.cjs current approve behavior:
```javascript
if (written.length === 0) {
  return json(res, 500, { error: 'APPROVE_WRITE_FAILED', ... });
}
const readiness = buildExecutionReadiness(approvedDrafts);
return json(res, 200, { success: true, written, ... });
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement resolver, seed-path constant, and skeleton generator contracts</name>
  <files>onboarding/backend/agents/example-resolver.cjs, onboarding/backend/path-constants.cjs, onboarding/backend/agents/skeleton-generator.cjs</files>
  <read_first>
    - onboarding/backend/agents/example-resolver.cjs
    - onboarding/backend/path-constants.cjs
    - .planning/phases/41-dynamic-skeleton-template-generator/41-CONTEXT.md
    - .planning/phases/41-dynamic-skeleton-template-generator/41-RESEARCH.md
    - .planning/phases/41-dynamic-skeleton-template-generator/41-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - `onboarding/backend/agents/example-resolver.cjs` contains `function resolveSkeleton(discipline, businessModel, basePath = DEFAULT_BASE)`.
    - `onboarding/backend/agents/example-resolver.cjs` contains `path.join(basePath, 'SKELETONS', discipline, \`_SKELETON-${slug}.md\`)`.
    - `onboarding/backend/agents/example-resolver.cjs` exports `resolveSkeleton` in `module.exports`.
    - `onboarding/backend/path-constants.cjs` contains `const SEED_PATH = path.join(PROJECT_ROOT, 'onboarding-seed.json');`.
    - `onboarding/backend/path-constants.cjs` exports `SEED_PATH`.
    - `onboarding/backend/agents/skeleton-generator.cjs` exports `generateSkeletons`, `interpolatePainPoints`, and `buildFrontmatter`.
    - `onboarding/backend/agents/skeleton-generator.cjs` contains the exact discipline list `['Paid_Media','Content_SEO','Lifecycle_Email','Social','Landing_Pages']`.
    - `onboarding/backend/agents/skeleton-generator.cjs` writes output paths under `MSP/{discipline}/SKELETONS/_SKELETON-{slug}.md`.
    - `onboarding/backend/agents/skeleton-generator.cjs` includes the exact orphan-removal regex `/{{pain_point_\\d+}}/`.
  </acceptance_criteria>
  <action>
Implement D-41-05 and D-41-07 first:

1. In `onboarding/backend/agents/example-resolver.cjs`, add:
   - `resolveSkeleton(discipline, businessModel, basePath = DEFAULT_BASE)`.
   - Resolve file path exactly: `{basePath}/SKELETONS/{discipline}/_SKELETON-{MODEL_SLUG[businessModel]}.md`.
   - Return `''` when model slug is unknown, file is missing, or file read fails.
   - Export `resolveSkeleton` alongside existing exports.

2. In `onboarding/backend/path-constants.cjs`, add:
   - `const SEED_PATH = path.join(PROJECT_ROOT, 'onboarding-seed.json');`
   - Include `SEED_PATH` in `module.exports`.

3. Create `onboarding/backend/agents/skeleton-generator.cjs` exporting exactly:
   - `generateSkeletons(seed, approvedDrafts, outputBasePath = MARKOS_LOCAL_DIR, templatesBasePath = TEMPLATES_DIR)`.
   - `interpolatePainPoints(content, painPoints)` with the exact implementation:
     ```js
     function interpolatePainPoints(content, painPoints) {
       let result = content;
       painPoints.forEach((pp, i) => { result = result.replace(`{{pain_point_${i + 1}}}`, pp); });
       result = result.split('\n').filter(line => !line.match(/{{pain_point_\d+}}/)).join('\n');
       return result;
     }
     ```
   - `buildFrontmatter(discipline, businessModel, painPoints, generatedAt)` returning YAML containing exactly `discipline`, `business_model`, `generated_at`, and `pain_points` list.

4. `generateSkeletons` behavior must be concrete:
   - Iterate all 5 disciplines exactly: `Paid_Media`, `Content_SEO`, `Lifecycle_Email`, `Social`, `Landing_Pages` (per D-41-05).
   - Load base template via `resolveSkeleton(discipline, businessModel, templatesBasePath)`.
   - Build output dir `path.join(outputBasePath, 'MSP', discipline, 'SKELETONS')` and call `fs.mkdirSync(outputDir, { recursive: true })`.
   - Output filename `_SKELETON-${MODEL_SLUG[businessModel]}.md`.
   - Write frontmatter + interpolated body with `fs.writeFileSync(filePath, ..., 'utf8')`.
   - Return one result per discipline in shape `{ discipline, files, error }`.
   - Keep `approvedDrafts` in signature and add code comment `// reserved for future enrichment` without using it.
  </action>
  <verify>
    <automated>node --test test/skeleton-generator.test.js</automated>
  </verify>
  <done>
    Resolver + generator contracts are implemented with deterministic path behavior, graceful misses, and phase-locked interpolation/frontmatter logic.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate blocking approve hook and response skeleton summary</name>
  <files>onboarding/backend/handlers.cjs</files>
  <read_first>
    - onboarding/backend/handlers.cjs
    - onboarding/backend/agents/skeleton-generator.cjs
    - onboarding/backend/path-constants.cjs
    - .planning/phases/41-dynamic-skeleton-template-generator/41-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `onboarding/backend/handlers.cjs` imports `generateSkeletons` from `./agents/skeleton-generator.cjs`.
    - `onboarding/backend/handlers.cjs` imports `SEED_PATH` from `./path-constants.cjs`.
    - `onboarding/backend/handlers.cjs` contains `let skeletonsSummary = { generated: [], failed: [] };` after the `written.length === 0` guard.
    - `onboarding/backend/handlers.cjs` contains `if (fs.existsSync(SEED_PATH)) { seed = JSON.parse(fs.readFileSync(SEED_PATH, 'utf8')); }`.
    - `onboarding/backend/handlers.cjs` contains `const skeletonResults = await generateSkeletons(seed, approvedDrafts);`.
    - `onboarding/backend/handlers.cjs` contains `skeletonsSummary = { generated: [], failed: ['all'] };` in the catch block.
    - `onboarding/backend/handlers.cjs` contains `skeletons: skeletonsSummary` in both HTTP 200 response payloads.
  </acceptance_criteria>
  <action>
Modify `handleApprove` in `onboarding/backend/handlers.cjs` per D-41-06 with this exact insertion point and logic:

1. Add requires at the top:
   - `const { generateSkeletons } = require('./agents/skeleton-generator.cjs');`
   - `const { SEED_PATH } = require('./path-constants.cjs');`

2. Insert hook block after:
   - the `if (written.length === 0) { return json(res, 500, APPROVE_WRITE_FAILED); }` guard,
   - and before `const readiness = buildExecutionReadiness(approvedDrafts);`

3. Use this exact hook behavior:
   - Initialize `skeletonsSummary = { generated: [], failed: [] }`.
   - In `try`: load `seed` from `SEED_PATH` if present, call `await generateSkeletons(seed, approvedDrafts)`, and reduce to `generated` flat paths plus `failed` disciplines where `r.error` exists.
   - In `catch`: set `skeletonsSummary = { generated: [], failed: ['all'] }`.

4. Add `skeletons: skeletonsSummary` to both success and warning HTTP 200 payloads; do not change status code behavior or existing `combinedErrors` handling.
  </action>
  <verify>
    <automated>node --test test/skeleton-generator.test.js</automated>
  </verify>
  <done>
    Approve flow now blocks on skeleton generation, reports generated/failed discipline outcomes, and keeps non-fatal behavior for skeleton failures.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace TODOs with executable unit and integration tests for Phase 41</name>
  <files>test/skeleton-generator.test.js</files>
  <read_first>
    - test/skeleton-generator.test.js
    - test/example-resolver.test.js
    - onboarding/backend/agents/example-resolver.cjs
    - onboarding/backend/agents/skeleton-generator.cjs
    - onboarding/backend/handlers.cjs
    - onboarding/backend/path-constants.cjs
    - test/setup.js
  </read_first>
  <acceptance_criteria>
    - `test/skeleton-generator.test.js` no longer contains `{ todo: true }`.
    - `test/skeleton-generator.test.js` implements all eight named tests from Plan 41-01.
    - `test/skeleton-generator.test.js` includes assertions for `resolveSkeleton` known model, unknown model, missing file, and `Agents-aaS` slug.
    - `test/skeleton-generator.test.js` includes an assertion that `generateSkeletons` writes 5 files.
    - `test/skeleton-generator.test.js` includes assertions that output files contain frontmatter keys `discipline:`, `business_model:`, `generated_at:`, and `pain_points:`.
    - `test/skeleton-generator.test.js` includes assertions that no output line contains `/{{pain_point_\\d+}}/`.
    - `test/skeleton-generator.test.js` includes integration assertion that approve response JSON includes `skeletons` object with `generated` and `failed` arrays.
    - `test/skeleton-generator.test.js` includes integration assertion that when generator throws, approve still returns HTTP 200 and `skeletons.failed` equals `['all']`.
    - `node --test test/skeleton-generator.test.js test/example-resolver.test.js` exits with code 0.
  </acceptance_criteria>
  <action>
Convert the eight Wave 0 todos into runnable tests using Node's built-in test runner and temporary directories:

1. `resolveSkeleton` unit tests:
   - Create temporary template roots containing `SKELETONS/{discipline}/_SKELETON-{slug}.md` files.
   - Assert known resolution returns non-empty content.
   - Assert unknown model returns `''`.
   - Assert missing template returns `''`.
   - Assert `Agents-aaS` maps to `agents-aas` slug path.

2. `generateSkeletons` + interpolation tests:
   - Build seed fixture with `company.business_model` and 2-3 `audience.pain_points`.
   - Create all five base template files with three placeholders in `## Your Priority Challenges`.
   - Call `generateSkeletons(seed, approvedDrafts, outputBasePath, templatesBasePath)` and assert 5 result entries plus file writes.
   - Assert output markdown includes YAML frontmatter and contains replaced pain points while unresolved placeholder lines are removed.

3. `handleApprove` integration tests:
   - Mock required dependencies with existing test helpers (`test/setup.js`) to avoid full server startup.
   - Assert HTTP 200 response payload includes `skeletons: { generated: [...], failed: [] }` when generator succeeds.
   - Assert HTTP 200 still returned and payload has `skeletons.failed` exactly `['all']` when generator throws.

Keep test names exactly unchanged from Wave 0 for continuity with 41-VALIDATION.md tracking.
  </action>
  <verify>
    <automated>node --test test/skeleton-generator.test.js test/example-resolver.test.js</automated>
  </verify>
  <done>
    Phase 41 runtime contracts are protected by executable resolver/generator/approve tests with non-fatal failure behavior verified.
  </done>
</task>

</tasks>

<verification>
Run `node --test test/skeleton-generator.test.js test/example-resolver.test.js` after Task 3, then run `node --test test/**/*.test.js` as the wave-level regression gate.
</verification>

<success_criteria>
Approval-triggered skeleton hydration is fully implemented and verified: five-discipline generation, graceful misses, seed-driven pain-point interpolation, and response-level skeleton status reporting.
</success_criteria>

<output>
After completion, create `.planning/phases/41-dynamic-skeleton-template-generator/41-02-SUMMARY.md`
</output>
