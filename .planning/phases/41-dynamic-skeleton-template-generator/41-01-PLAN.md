---
phase: 41-dynamic-skeleton-template-generator
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/skeleton-generator.test.js
autonomous: true
requirements:
  - LIT-07

must_haves:
  truths:
    - "A dedicated phase test file exists for skeleton generation and approval-hook behavior before implementation begins."
    - "All eight required Phase 41 behaviors are represented as named test stubs so later tasks cannot skip coverage."
    - "The test scaffold runs under node:test without failing, enabling Wave 1 TDD implementation."
  artifacts:
    - path: "test/skeleton-generator.test.js"
      provides: "Wave 0 test scaffold for resolver, generator, interpolation, and approve-hook behavior"
      exports: []
  key_links:
    - from: "test/skeleton-generator.test.js"
      to: "onboarding/backend/agents/example-resolver.cjs"
      via: "resolveSkeleton test stubs"
      pattern: "resolveSkeleton"
    - from: "test/skeleton-generator.test.js"
      to: "onboarding/backend/handlers.cjs"
      via: "handleApprove hook behavior test stubs"
      pattern: "handleApprove|skeletons"
---

<objective>
Create the Wave 0 contract-first test scaffold for Phase 41 so implementation in Wave 1 is constrained by explicit, named behavior checks.

Purpose: Lock the minimum quality bar before touching runtime code, per the phase validation strategy.
Output: A passing node:test scaffold file with all eight required test names marked as todo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-dynamic-skeleton-template-generator/41-CONTEXT.md
@.planning/phases/41-dynamic-skeleton-template-generator/41-RESEARCH.md
@.planning/phases/41-dynamic-skeleton-template-generator/41-VALIDATION.md
@test/example-resolver.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Phase 41 skeleton-generator test scaffold with 8 exact todo stubs</name>
  <files>test/skeleton-generator.test.js</files>
  <read_first>
    - test/example-resolver.test.js
    - .planning/phases/41-dynamic-skeleton-template-generator/41-VALIDATION.md
    - .planning/phases/41-dynamic-skeleton-template-generator/41-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - `test/skeleton-generator.test.js` contains `const test = require('node:test');` and `const assert = require('node:assert/strict');`.
    - `test/skeleton-generator.test.js` contains exactly eight `test(` declarations using `{ todo: true }`.
    - `test/skeleton-generator.test.js` contains the exact test name `resolveSkeleton resolves correct base template for known discipline + model`.
    - `test/skeleton-generator.test.js` contains the exact test name `resolveSkeleton returns empty string for unknown business model`.
    - `test/skeleton-generator.test.js` contains the exact test name `resolveSkeleton returns empty string for missing template file`.
    - `test/skeleton-generator.test.js` contains the exact test name `resolveSkeleton resolves Agents-aaS slug (agents-aas)`.
    - `test/skeleton-generator.test.js` contains the exact test name `generateSkeletons writes 5 output files for a valid seed`.
    - `test/skeleton-generator.test.js` contains the exact test name `interpolatePainPoints replaces all slots and removes orphans`.
    - `test/skeleton-generator.test.js` contains the exact test name `handleApprove response includes skeletons block`.
    - `test/skeleton-generator.test.js` contains the exact test name `skeleton generation failure does not affect HTTP 200 response`.
  </acceptance_criteria>
  <action>
Create `test/skeleton-generator.test.js` using the same suite style used in `test/example-resolver.test.js`: CommonJS requires, `node:test`, and `node:assert/strict`. Add the eight required tests as `test('name', { todo: true }, () => {});` with no implementation logic. Preserve the exact test names listed in Phase 41 validation so Wave 1 can implement them one-for-one.

Do not add placeholder assertions, no-op file writes, or runtime imports yet. This task is naming-contract scaffolding only.
  </action>
  <verify>
    <automated>node --test test/skeleton-generator.test.js</automated>
  </verify>
  <done>
    The Phase 41 scaffold file exists, exposes all eight required behavior names, and executes cleanly as a todo-only test suite.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enforce scaffold integrity and prevent premature implementation drift</name>
  <files>test/skeleton-generator.test.js</files>
  <read_first>
    - test/skeleton-generator.test.js
    - .planning/phases/41-dynamic-skeleton-template-generator/41-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - `test/skeleton-generator.test.js` contains the string `{ todo: true }` exactly eight times.
    - `test/skeleton-generator.test.js` does not contain `generateSkeletons(` outside of a test-name string.
    - `test/skeleton-generator.test.js` does not contain `handleApprove(` outside of a test-name string.
    - `node --test test/skeleton-generator.test.js` exits with code 0.
  </acceptance_criteria>
  <action>
Review `test/skeleton-generator.test.js` and keep all eight tests as todo-only placeholders. Ensure no runtime-module imports for `skeleton-generator.cjs` or `handlers.cjs` are introduced in this wave. Keep this file as a strict behavior contract for Wave 1 implementation.
  </action>
  <verify>
    <automated>node --test test/skeleton-generator.test.js</automated>
  </verify>
  <done>
    Wave 0 remains a pure scaffold with zero implementation coupling and a stable executable baseline.
  </done>
</task>

</tasks>

<verification>
Run `node --test test/skeleton-generator.test.js` and confirm all eight tests are reported as TODO with no failing assertions.
</verification>

<success_criteria>
Wave 1 executors have an unambiguous test contract file covering resolver behavior, skeleton generation behavior, interpolation behavior, and approve-hook behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/41-dynamic-skeleton-template-generator/41-01-SUMMARY.md`
</output>
