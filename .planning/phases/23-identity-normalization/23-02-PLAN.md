---
id: "23-02-public-identity-normalization"
wave: 1
depends_on: ["23-01-identity-audit"]
discipline: marketing_operations
requirements: ["IDN-01", "IDN-02"]
files_modified:
  - package.json
  - README.md
  - CHANGELOG.md
  - .planning/PROJECT.md
  - bin/install.cjs
  - bin/update.cjs
  - onboarding/index.html
  - onboarding/onboarding.js
mir_files_referenced: []
tracking_required: false
requires_human_approval: true
autonomous: false
neuro_dimension: false
objective: "Normalize package metadata, installer/update UX, onboarding copy, and primary public-facing docs so MarkOS is the clear primary identity."
must_haves:
  - MarkOS is the first-name product identity in all primary user-facing surfaces
  - Legacy names remain only where compatibility requires them
  - Install/update and onboarding journeys do not present mixed branding unnecessarily
  - Public docs and runtime messages match the v2.1 milestone decision
---

# Plan 23-02: Public Identity Normalization

## Wave 1 — MarkOS-First User Journeys

<task id="1" name="Normalize package and install/update identity">
  <read_first>
    - package.json
    - bin/install.cjs
    - bin/update.cjs
    - .planning/phases/23-identity-normalization/23-01-PLAN.md
  </read_first>
  <action>
    Update package metadata and install/update messaging so MarkOS is the visible primary identity.
    Keep compatibility aliases or legacy references only where execution requires them, and label them as compatibility behavior rather than primary branding.
  </action>
  <acceptance_criteria>
    - `package.json` presents MarkOS as the primary package identity
    - install/update banners and help text are MarkOS-first
    - any remaining legacy references are explicit compatibility notes rather than mixed-brand defaults
  </acceptance_criteria>
</task>

<task id="2" name="Normalize primary docs and milestone-facing planning docs">
  <read_first>
    - README.md
    - CHANGELOG.md
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
  </read_first>
  <action>
    Update the highest-visibility docs so the product is consistently described as MarkOS.
    Preserve historical references only in milestone/archive contexts where they are materially useful.
  </action>
  <acceptance_criteria>
    - README and CHANGELOG are MarkOS-first in headers, summaries, and install language
    - planning docs describe MGSD as the protocol lineage, not the primary external identity
    - historical sections are preserved without leaking obsolete branding into current instructions
  </acceptance_criteria>
</task>

<task id="3" name="Normalize onboarding copy">
  <read_first>
    - onboarding/index.html
    - onboarding/onboarding.js
    - onboarding/backend/server.cjs
  </read_first>
  <action>
    Review onboarding UI strings and server-side operator messages.
    Convert user-facing copy to MarkOS-first language while preserving compatibility-sensitive runtime behavior for later plans.
  </action>
  <acceptance_criteria>
    - onboarding title, headings, and primary calls to action are MarkOS-first
    - server logs and operator-facing messages no longer mix MarkOS and MGSD casually
    - any legacy naming that remains is intentional and documented
  </acceptance_criteria>
</task>

## Verification

```bash
rg -n "marketing-get-shit-done|MGSD" README.md CHANGELOG.md package.json bin/install.cjs bin/update.cjs onboarding/index.html onboarding/onboarding.js .planning/PROJECT.md
```

Review results to confirm that remaining legacy references are compatibility-only or historical-only.