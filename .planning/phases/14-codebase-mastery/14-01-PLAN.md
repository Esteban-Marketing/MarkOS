---
phase: 14-codebase-mastery
plan: 01
type: execute
status: complete
---
# 📋 Plan 14-01: MARKOS Codebase Mastery

This plan executes the hardening and refinement objectives identified during the comprehensive codebase review.

## 🎯 Objectives
- **Hardening:** Fix the broken installer and path resolution in agent fillers.
- **Harmonization:** Sync data structures between frontend and backend.
- **Optimization:** Consolidate path resolution and hoist module imports.

---

## 🏗️ Technical Approach

### 1. Installer & Agents (Hardening)
- [x] Restore `banner()`, `ask()`, and `rl` to `bin/install.cjs`.
- [x] Correct `..` levels in `mir-filler.cjs`, `msp-filler.cjs`, and `example-resolver.cjs`.

### 2. API & Refactoring (Harmonization)
- [x] Update `handleGenerateQuestion` and `handleParseAnswer` in `handlers.cjs` to support both `schema` and top-level fields.
- [x] Hoist all `require` calls in `server.cjs`, `handlers.cjs`, and `orchestrator.cjs`.

### 3. Path Standardization (Optimization)
- Create `onboarding/backend/path-constants.cjs` to centralize the definition of `PROJECT_ROOT`, `ONBOARDING_DIR`, etc.
- Update all backend scripts to use this centralized utility instead of multiple `path.resolve(__dirname, '..')` calls.

### 4. Verification
- Final comprehensive test run across all suites.
- Manual smoke test of Step 2 (Interview).

---

## 📝 Tasks

### Wave 1: Path Standardization & Refinement
- [x] **Create `path-constants.cjs`**: Centralize all path resolution logic.
- [x] **Refactor all backend components**: Use `path-constants.cjs` for project root and template paths.

### Wave 2: Verification & UAT
- [x] **Run all tests**: `node --test test/`
- [x] **Manual Smoke Test**: Verify onboarding flow starts and generates a question.

---

## 📊 Success Criteria
- [ ] Installer functions correctly without crashing.
- [ ] Agent fillers successfully load templates from the root.
- [ ] AI interview session can parse answers and generate next questions.
- [ ] Only one file is responsible for defining the project root.

