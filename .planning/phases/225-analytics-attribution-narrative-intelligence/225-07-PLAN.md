---
phase: 225
plan: "07"
type: execute
wave: 5
depends_on: ["225-01", "225-02", "225-03", "225-04", "225-05", "225-06"]
files_modified:
  - supabase/migrations/144_analytics_rls_hardening.sql
  - supabase/migrations/145_analytics_openapi_sync.sql
  - api/v1/analytics/metrics.js
  - api/v1/analytics/metrics-detail.js
  - api/v1/analytics/metrics-value.js
  - api/v1/analytics/metrics-recompute.js
  - api/v1/analytics/metrics-overrides.js
  - api/v1/analytics/attribution-touches.js
  - api/v1/analytics/attribution-models.js
  - api/v1/analytics/attribution-recompute.js
  - api/v1/analytics/anomalies.js
  - api/v1/analytics/anomalies-acknowledge.js
  - api/v1/analytics/anomalies-resolve.js
  - api/v1/analytics/anomalies-dismiss.js
  - api/v1/analytics/anomalies-evaluate-rules.js
  - api/v1/analytics/narratives.js
  - api/v1/analytics/narratives-detail.js
  - api/v1/analytics/narratives-audit.js
  - api/v1/analytics/narratives-approve.js
  - api/v1/analytics/narratives-publish.js
  - api/v1/analytics/narratives-claim-override.js
  - api/v1/analytics/narratives-generate.js
  - api/v1/analytics/decision-rules.js
  - api/v1/analytics/decision-rules-detail.js
  - api/v1/analytics/decision-rules-enable.js
  - api/v1/analytics/decision-rules-disable.js
  - api/v1/analytics/decision-rules-evaluate.js
  - api/v1/analytics/journeys-by-profile.js
  - api/v1/analytics/journeys.js
  - api/v1/analytics/experiments-winners.js
  - api/v1/analytics/experiments-declare-winner.js
  - api/v1/analytics/experiments-reject.js
  - api/v1/analytics/benchmarks.js
  - lib/markos/mcp/tools/analytics/get-metric.cjs
  - lib/markos/mcp/tools/analytics/get-attribution.cjs
  - lib/markos/mcp/tools/analytics/list-anomalies.cjs
  - lib/markos/mcp/tools/analytics/generate-narrative.cjs
  - lib/markos/mcp/tools/analytics/evaluate-decision.cjs
  - lib/markos/mcp/tools/analytics/get-journey.cjs
  - lib/markos/mcp/tools/analytics/list-experiment-winners.cjs
  - lib/markos/mcp/tools/index.cjs
  - app/(markos)/analytics/page.tsx
  - app/(markos)/analytics/AnalyticsWorkspace.tsx
  - app/(markos)/analytics/MetricCatalogEditor.tsx
  - app/(markos)/analytics/attribution/page.tsx
  - app/(markos)/analytics/attribution/AttributionExplorer.tsx
  - app/(markos)/analytics/anomalies/page.tsx
  - app/(markos)/analytics/anomalies/AnomalyInbox.tsx
  - app/(markos)/analytics/narratives/page.tsx
  - app/(markos)/analytics/narratives/NarrativeFeed.tsx
  - app/(markos)/analytics/experiments/page.tsx
  - app/(markos)/analytics/experiments/ExperimentsCockpit.tsx
  - app/(markos)/analytics/decision-rules/page.tsx
  - app/(markos)/analytics/decision-rules/DecisionRulesEditor.tsx
  - app/(markos)/analytics/benchmarks/page.tsx
  - app/(markos)/analytics/benchmarks/BenchmarksPanel.tsx
  - components/markos/analytics/analytics-workspace.module.css
  - components/markos/analytics/anomaly-inbox.module.css
  - components/markos/analytics/narrative-feed.module.css
  - components/markos/analytics/experiments-cockpit.module.css
  - lib/markos/operating/approvals/entry-types.cjs
  - lib/markos/operating/morning-brief/analytics-section.cjs
  - contracts/openapi.json
  - public/llms.txt
  - docs/analytics/index.md
  - docs/analytics/api.md
  - docs/analytics/mcp-tools.md
  - test/analytics/api/metrics-api.test.js
  - test/analytics/api/attribution-api.test.js
  - test/analytics/api/anomalies-api.test.js
  - test/analytics/api/narratives-api.test.js
  - test/analytics/api/decision-rules-api.test.js
  - test/analytics/api/journeys-api.test.js
  - test/analytics/api/experiments-api.test.js
  - test/analytics/api/benchmarks-api.test.js
  - test/analytics/api/openapi-parity.test.js
  - test/analytics/mcp/get-metric.tool.test.js
  - test/analytics/mcp/get-attribution.tool.test.js
  - test/analytics/mcp/list-anomalies.tool.test.js
  - test/analytics/mcp/generate-narrative.tool.test.js
  - test/analytics/mcp/evaluate-decision.tool.test.js
  - test/analytics/mcp/get-journey.tool.test.js
  - test/analytics/mcp/list-experiment-winners.tool.test.js
  - test/analytics/rls/rls-suite-full.test.js
  - test/analytics/tombstone/full-cascade-suite.test.js
  - test/analytics/operating/approval-inbox-entries.test.js
  - test/analytics/operating/morning-brief-section.test.js
  - test/regression/p221-p224.regression.test.js
  - test/migrations/order.test.js
  - test/playwright/analytics/analytics-workspace.spec.ts
  - test/playwright/analytics/anomaly-inbox.spec.ts
  - test/playwright/analytics/narrative-feed.spec.ts
  - test/playwright/analytics/experiments-cockpit.spec.ts
  - test/playwright/analytics/morning-brief-analytics.spec.ts
  - test/playwright/analytics/approval-inbox-analytics.spec.ts
  - test/chromatic/analytics/AnalyticsWorkspace.stories.tsx
  - test/chromatic/analytics/AttributionExplorer.stories.tsx
  - test/chromatic/analytics/AnomalyInbox.stories.tsx
  - test/chromatic/analytics/NarrativeFeed.stories.tsx
  - test/chromatic/analytics/ExperimentsCockpit.stories.tsx
  - test/chromatic/analytics/DecisionRulesEditor.stories.tsx
  - test/chromatic/analytics/BenchmarksPanel.stories.tsx
  - .planning/phases/225-analytics-attribution-narrative-intelligence/225-VALIDATION.md
  - .planning/phases/225-analytics-attribution-narrative-intelligence/225-SUMMARY.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - ANL-01
  - ANL-02
  - ANL-03
  - ANL-04
  - ANL-05
  - EVD-01
  - EVD-02
  - EVD-03
  - EVD-04
  - EVD-05
  - EVD-06
  - LOOP-07
  - LOOP-08
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - QA-05
  - QA-06
  - QA-07
  - QA-08
  - QA-09
  - QA-10
  - QA-11
  - QA-12
  - QA-13
  - QA-14
  - QA-15
must_haves:
  truths:
    - 16 F-IDs (F-147..F-162) backed by HTTP route handlers under `api/v1/analytics/*.js` (legacy convention per D-49) matching contract YAML schemas (D-35)
    - All handlers call `requireHostedSupabaseAuth({ req, runtimeContext, operation })` from `onboarding/backend/runtime-context.cjs:491` for auth (D-49)
    - All handlers call `assertUpstreamReady()` from `lib/markos/analytics/preflight/upstream-gate.ts` (D-54) — fail-closed on missing upstream phases
    - Service-role client constructed inline using `createClient(...)` per `api/submit.js:39-43` pattern (D-49)
    - MCP tool registry is `lib/markos/mcp/tools/index.cjs` extension `.cjs` (D-49) NOT `.ts`
    - OpenAPI artifact is `contracts/openapi.json` (RM1/D-49) NOT `public/openapi.json`
    - `lib/markos/operating/*` is greenfield owned by the P208 closure phase (D-53). P225 references `entry-types.cjs` and `analytics-section.cjs` as imports but does NOT create the directory tree itself. If P208 closure has not landed, P225 hard-fails per `assertUpstreamReady` upstream-gate.
    - 7 MCP tools registered in lib/markos/mcp/tools/index.cjs (D-36) — get_metric, get_attribution, list_anomalies, generate_narrative, evaluate_decision, get_journey, list_experiment_winners
    - 6 UI workspaces shipped under app/(markos)/analytics/* (D-37) — AnalyticsWorkspace, AttributionExplorer, AnomalyInbox, NarrativeFeed, ExperimentsCockpit, DecisionRulesEditor + BenchmarksPanel (7 components total)
    - Approval Inbox (P208) gains 5 new entry types (D-38) — anomaly_critical, narrative_publish, decision_rule_change, experiment_winner_promotion, metric_definition_change
    - Morning Brief (P208) gains analytics section (D-39) — top open anomalies, top narrative published 24h, experiment winners pending, freshness SLA violations
    - Migration 144 hardens RLS across all 15 analytics tables (final pass; defense in depth)
    - Migration 145 publishes OpenAPI 3.1 + Supabase type generation + flow-registry sync
    - Cross-tenant API denial proven: 403 + zero rows on every /v1/analytics/* endpoint with mismatched tenant
    - Tombstone full-cascade suite proves PII scrubbed across ALL 4 affected tables (attribution_touches, journey_steps, narrative_claims, freshness_audit)
    - Legacy regression suite (P100-P105 + P201 + P221-P224) green; this is an additive phase
    - Playwright operator journeys covering: AnomalyInbox triage, NarrativeFeed approve+publish, ExperimentsCockpit winner promote, AnalyticsWorkspace metric drill-down, Morning Brief analytics section, Approval Inbox analytics entry types
    - Chromatic stories for 7 UI components × 4 variants (empty/loading/healthy/stale/error/forbidden)
    - contracts/openapi.json regenerated to include /v1/analytics/* paths
    - public/llms.txt updated with Phase 225 section
    - docs/analytics/{index,api,mcp-tools}.md published
    - 225-SUMMARY.md + STATE.md + ROADMAP.md updated; phase 225 marked planning-complete; 50/50 D-XX coverage table green
    - High closeout scope (113 files modified across 5 tasks) is acknowledged and mitigated by the autonomous=false flag + final human-verify checkpoint (Task 4) which blocks until operator + visual review + decision coverage map completes; no plan split required because tasks group naturally (Task 1 = API surface; Task 2 = MCP + operating; Task 3 = UI + e2e; Task 4 = checkpoint; Task 5 = SUMMARY/STATE/ROADMAP)
    - Checkpoint: human-verify operator journey + visual regression + 225-VALIDATION.md fully populated
  artifacts:
    - path: supabase/migrations/144_analytics_rls_hardening.sql
      provides: final RLS pass (15 tables) + cross-tenant aggregated_metrics policy verification + audit_log column nullability fixes
    - path: supabase/migrations/145_analytics_openapi_sync.sql
      provides: trigger-based OpenAPI doc sync + Supabase type generation hint
    - path: api/v1/analytics/* route handlers
      provides: 26 HTTP route handlers covering F-147..F-162 contract paths
    - path: lib/markos/mcp/tools/analytics/*
      provides: 7 MCP tool implementations (D-36)
    - path: lib/markos/mcp/tools/index.cjs
      provides: tool registry export with all 7 analytics tools
    - path: app/(markos)/analytics/* page.tsx + workspace components
      provides: 6 UI workspaces + 7th BenchmarksPanel
    - path: components/markos/analytics/*.module.css
      provides: 4 CSS modules for workspaces (analytics-workspace, anomaly-inbox, narrative-feed, experiments-cockpit)
    - path: lib/markos/operating/approvals/entry-types.cjs
      provides: 5 new entry_type enum values + handlers
    - path: lib/markos/operating/morning-brief/analytics-section.cjs
      provides: analytics section for Morning Brief composition
    - path: contracts/openapi.json
      provides: regenerated OpenAPI 3.1 with /v1/analytics/* paths
    - path: docs/analytics/*.md
      provides: 3 doc pages (index, api, mcp-tools)
    - path: .planning/phases/225-analytics-attribution-narrative-intelligence/225-SUMMARY.md
      provides: phase closeout — D-01..D-48 coverage map + verification status + handoff to P226
  key_links:
    - from: api/v1/analytics/metrics.js
      to: contracts/F-147-analytics-metric-definition-v1.yaml
      via: Zod schemas derived from F-147 contract; OpenAPI parity test asserts roundtrip
    - from: lib/markos/mcp/tools/analytics/get-metric.cjs
      to: lib/markos/analytics/catalog/metric-definition.ts
      via: MCP tool wraps resolveMetricForTenant + recomputeMetricForTenant
    - from: app/(markos)/analytics/anomalies/AnomalyInbox.tsx
      to: lib/markos/analytics/anomaly/state-machine.ts
      via: kanban-by-status + transition buttons invoke transitionAnomaly
    - from: lib/markos/operating/approvals/entry-types.cjs
      to: lib/markos/operating/approvals/* (P208)
      via: 5 new entry_types append to existing enum + handler dispatch
    - from: lib/markos/operating/morning-brief/analytics-section.cjs
      to: lib/markos/operating/morning-brief/* (P208)
      via: analytics section appended to Morning Brief composition pipeline
    - from: test/regression/p221-p224.regression.test.js
      to: test/cdp-* + test/crm-* + test/channels-* + test/conversion-* (P221-P224)
      via: spawns existing test suites; asserts all green; locks in additive guarantee
---

<objective>
**Scope acknowledgment:** Phase 225 closeout is the largest plan in P225 (113 files across 5 tasks); high scope is intentional and mitigated by autonomous=false + final human-verify checkpoint (Task 4) which gates publication. No split is required because task boundaries are natural (Task 1 = API surface; Task 2 = MCP + operating integrations; Task 3 = UI + Playwright + Chromatic; Task 4 = human-verify; Task 5 = SUMMARY closeout). Phase 225 closeout: ship the read-write `/v1/analytics/*.js` API (26 legacy-style route handlers covering 16 F-IDs per D-49), 7 MCP tools (D-36), 6 UI workspaces + BenchmarksPanel (D-37), Approval Inbox 5 new entry types (D-38), Morning Brief analytics section (D-39), final RLS hardening (migration 144), OpenAPI regen + Supabase type sync (migration 145), full Playwright operator journeys, Chromatic visual regression, full RLS denial suite, tombstone cascade test across all 4 affected tables, legacy P221-P224 regression guard, docs trio, llms.txt update, and 225-SUMMARY.md + STATE.md + ROADMAP.md updates.

Purpose: complete every requirement (ANL-01..05, EVD-01..06, LOOP-07/08, QA-01..15) by producing the operator-facing surface that consumes everything from 225-01 through 225-06. This is the longest plan in P225 because it is the integration + closeout that the rest of the phase exists to enable. The plan is `autonomous: false` — final task is a human-verify checkpoint covering operator journey + visual regression + decision coverage map.

Output: 26 API route handlers, 7 MCP tools, 7 UI components + 4 CSS modules, 2 P208 integrations (Approval Inbox + Morning Brief), 2 closeout migrations, OpenAPI regen, llms.txt + docs trio, 11 node:test API tests + 7 MCP tests + 4 operating integration tests + RLS suite + tombstone full suite + regression suite, 6 Playwright e2e specs, 7 Chromatic stories, SUMMARY + STATE + ROADMAP updates, and a human-verify checkpoint.
</objective>

<context>
@.planning/phases/225-analytics-attribution-narrative-intelligence/225-CONTEXT.md
@.planning/phases/225-analytics-attribution-narrative-intelligence/225-RESEARCH.md
@.planning/phases/225-analytics-attribution-narrative-intelligence/225-VALIDATION.md
@.planning/phases/225-analytics-attribution-narrative-intelligence/225-01-PLAN.md
@.planning/phases/225-analytics-attribution-narrative-intelligence/225-02-PLAN.md
@.planning/phases/225-analytics-attribution-narrative-intelligence/225-03-PLAN.md
@.planning/phases/225-analytics-attribution-narrative-intelligence/225-04-PLAN.md
@.planning/phases/225-analytics-attribution-narrative-intelligence/225-05-PLAN.md
@.planning/phases/225-analytics-attribution-narrative-intelligence/225-06-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/V4.0.0-TESTING-ENVIRONMENT-PLAN.md
@.planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md
@obsidian/reference/HTTP Layer.md
@obsidian/reference/UI Components.md
@obsidian/reference/Contracts Registry.md
@obsidian/brain/Pricing Engine Canon.md
@CLAUDE.md
@app/(markos)/crm/reporting/page.tsx
@app/(markos)/conversion/page.tsx
@app/(markos)/launches/page.tsx
@components/markos/conversion/conversion-workspace.module.css
@lib/markos/mcp/tools/index.cjs
@lib/markos/operating/approvals/entry-types.cjs
@lib/markos/operating/morning-brief/launches-section.cjs
@contracts/openapi.json
@public/llms.txt
@vercel.json
@.planning/phases/208-human-operating-interface/208-CONTEXT.md
@.planning/phases/202-mcp-server-ga-claude-marketplace/DISCUSS.md
@.planning/phases/224-conversion-launch-workspace/224-07-PLAN.md

<!-- INFORMATIONAL ONLY (per CLAUDE.md): -->
<!-- obsidian/work/incoming/22-ANALYTICS-ENGINE.md (Doc 22 7 core rules) — context only; canonical = obsidian/reference/HTTP Layer.md + UI Components.md + Contracts Registry.md -->
-->
</context>

<interfaces>
<!-- HTTP Layer.md /v1/analytics/* convention (canonical): -->
- Route handlers under api/v1/analytics/*.js; legacy api/*.js convention (per CONTEXT.md D-49; mirrors api/submit.js, api/approve.js, api/cron/*.js patterns)
- Each handler exports HTTP-method functions (GET, POST, PATCH, DELETE)
- Tenant context derived from session: `await requireHostedSupabaseAuth({ req, runtimeContext, operation })` returns `{ tenant_id, actor_id }` or 401
- Validation via Zod schemas matching F-147..F-162 contract YAML schemas
- markos_audit_log written on every state-mutating endpoint

<!-- MCP tool shape — CommonJS .cjs per existing lib/markos/mcp/tools/index.cjs convention (D-49): -->
```javascript
// lib/markos/mcp/tools/analytics/get-metric.cjs
const { z } = require('zod');

module.exports = {
  name: 'analytics_get_metric',
  description: 'Read a metric value with breakdown + freshness',
  inputSchema: z.object({ metric_id: z.string().uuid(), dimension_filter: z.record(z.string()).optional() }),
  handler: async function (input, ctx) {
    // invoke resolveMetricForTenant via ctx.client + return value
    // ctx.tenant_id sourced from MCP session via requireHostedSupabaseAuth
  },
};
```

<!-- 225-CONTEXT.md D-36 7 MCP tools: -->
- get_metric — metric value + breakdown + freshness
- get_attribution — touchpoints for Opportunity/Profile/Account by model
- list_anomalies — open anomalies for tenant
- generate_narrative — request narrative generation by kind + period
- evaluate_decision — manually trigger decision_rule evaluation
- get_journey — journey_steps for profile/account
- list_experiment_winners — pending + recent winner detections

<!-- 225-CONTEXT.md D-37 6 UI workspaces (7th: BenchmarksPanel embedded in AnalyticsWorkspace OR separate page): -->
- AnalyticsWorkspace + MetricCatalogEditor (app/(markos)/analytics/page.tsx)
- AttributionExplorer (.../attribution/page.tsx)
- AnomalyInbox (.../anomalies/page.tsx)
- NarrativeFeed (.../narratives/page.tsx)
- ExperimentsCockpit (.../experiments/page.tsx)
- DecisionRulesEditor (.../decision-rules/page.tsx)
- BenchmarksPanel (.../benchmarks/page.tsx) — separate page per D-37

<!-- 225-CONTEXT.md D-38 5 new Approval Inbox entry types: -->
- anomaly_critical (severity=critical anomalies need acknowledgment)
- narrative_publish (narrative awaiting approval)
- decision_rule_change (operator-edited rule changes need approval)
- experiment_winner_promotion (winner pending operator promotion)
- metric_definition_change (global metric mutation needs approval-package per D-04)

<!-- 225-CONTEXT.md D-39 Morning Brief analytics section: -->
- Top open anomalies (severity descending; max 5)
- Top narrative published in last 24h (one)
- Experiment winners pending approval (count + link)
- Freshness SLA violations (count + top 3 by violation_delta_seconds)

<!-- Existing Approval Inbox entry_type pattern (lib/markos/operating/approvals/entry-types.cjs from P208): -->
```typescript
export type EntryType =
  | 'launch_gate' | 'launch_outcome' | 'campaign_dispatch' | 'pricing_recommendation'
  | 'opportunity_close' | 'narrative_publish' | 'experiment_promotion'
  // ... new entries from P225 appended ...
  | 'anomaly_critical' | 'decision_rule_change' | 'metric_definition_change';
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 26 /v1/analytics/* HTTP route handlers + Zod schemas + cross-tenant denial test suite + OpenAPI regen + RLS hardening migration 144</name>
  <read_first>
    - **Migration application order is wave-driven, NOT numeric (carries P224 Blocker 5 pattern verbatim).** The migration runner MUST apply migrations in wave-execution order, not numeric order, because plans run in parallel within a wave. P225 wave-execution sequence:
      - Wave 1 (225-01): 134, 135, 136, 137, 138 (+ 3 seed files)
      - Wave 2 (225-02): 139, 141 — applied BEFORE 140 even though 140 is numerically earlier
      - Wave 2 (225-03): 142
      - Wave 3 (225-04): 143
      - Wave 3 (225-05): seed 225_narrative_templates only (no schema migration)
      - Wave 4 (225-06): 140 — numerically earliest in this group, applied LAST because plan 06 depends on 225-01..05; THIS is the canonical wave-driven anomaly Blocker 1 documents
      - Wave 5 (225-07): 144, 145
      A naive numeric-order migration runner would apply 140 before 141/142/143 and break dependencies. test/migrations/order.test.js (added in 225-07 files_modified) asserts the migration runner script enforces wave-execution order.
    - .planning/phases/225-analytics-attribution-narrative-intelligence/225-CONTEXT.md D-35 (full API surface)
    - .planning/phases/225-analytics-attribution-narrative-intelligence/225-01-PLAN.md (F-147..F-156 contract YAMLs)
    - .planning/phases/225-analytics-attribution-narrative-intelligence/225-03-PLAN.md (F-157)
    - .planning/phases/225-analytics-attribution-narrative-intelligence/225-04-PLAN.md (F-158)
    - .planning/phases/225-analytics-attribution-narrative-intelligence/225-05-PLAN.md (F-159, F-160)
    - .planning/phases/225-analytics-attribution-narrative-intelligence/225-06-PLAN.md (F-161, F-162)
    - obsidian/reference/HTTP Layer.md (canonical /v1/* convention)
    - api/v1/conversion/pages/route.ts (P224 reference handler shape)
    - api/v1/conversion/forms/[form_id]/route.ts (P224 reference shape with [param] route)
    - lib/markos/auth/middleware.ts (existing requireHostedSupabaseAuth)
    - lib/markos/analytics/* (all libraries from 225-02..06)
    - contracts/openapi.json (current state)
    - test/conversion/api/* (P224 test shape reference)
  </read_first>
  <behavior>
    - Each route handler: (1) validate tenant context (401 if missing), (2) parse request body via Zod schema (400 on invalid shape), (3) invoke library function, (4) write markos_audit_log on state-mutating ops, (5) return JSON response matching contract YAML response_2xx schema, (6) error_codes per F-* YAML.
    - Cross-tenant denial: every state-mutating endpoint must reject when session tenant_id != resource tenant_id; returns 403 with body `{ error: 'cross_tenant_denied' }`.
    - Migration 144 verifies RLS on all 15 analytics tables (audit pass; no schema changes); writes a CHECK constraint catalog row for documentation.
    - OpenAPI regen: `npm run openapi:regen` (existing script from P200) reads contracts/F-*.yaml + route handlers and writes contracts/openapi.json. Test parity: each F-ID's path + request/response schema present in openapi.json.
  </behavior>
  <action>
    1. Create 26 HTTP route handlers under `api/v1/analytics/*.js`. Each follows P224 conversion handler pattern. Group by F-ID:

       **F-147 (analytics-metric-definition-v1)** — 4 handlers:
       - `api/v1/analytics/metrics.js`: GET (list filtered by tenant; query params category, status); POST (create; require_approval if global per D-04)
       - `api/v1/analytics/metrics-detail.js`: GET (single); PATCH (update status with approval-package per D-04)
       - `api/v1/analytics/metrics-value.js`: GET (with dimension breakdown query param)
       - `api/v1/analytics/metrics-recompute.js`: POST (returns 202 + run_id; AgentRun-wrapped)

       **F-148 (metric-override-v1)** — 1 handler:
       - `api/v1/analytics/metrics-overrides.js`: GET (per-tenant), POST (create override), PATCH (approve), DELETE (deprecate)

       **F-149 (attribution-model-v1)** — 1 handler:
       - `api/v1/analytics/attribution-models.js`: GET only (list 6 standard + tenant-cloned)

       **F-150 (attribution-touch-v1) + F-157 (attribution-recompute-v1)** — 2 handlers:
       - `api/v1/analytics/attribution-touches.js`: GET with filters opportunity_id, profile_id, account_id, model_id
       - `api/v1/analytics/attribution-recompute.js`: POST `{ opportunity_id }` → 202 + run_id

       **F-151 (anomaly-detection-v1) + F-158 (anomaly-evaluate-v1)** — 5 handlers:
       - `api/v1/analytics/anomalies.js`: GET (list with filters), POST (manual create — operator only)
       - `api/v1/analytics/anomalies-acknowledge.js`: POST → 200
       - `api/v1/analytics/anomalies-resolve.js`: POST `{ resolution_summary }` → 200
       - `api/v1/analytics/anomalies-dismiss.js`: POST `{ dismiss_reason }` → 200
       - `api/v1/analytics/anomalies-evaluate-rules.js`: POST manual rule re-eval

       **F-152 (narrative-v1) + F-153 (narrative-template-v1) + F-154 (narrative-claim-v1) + F-159 (narrative-generate-v1)** — 7 handlers:
       - `api/v1/analytics/narratives.js`: GET (list), POST (create empty narrative draft — rare path; usually generate)
       - `api/v1/analytics/narratives-detail.js`: GET (single with claims expanded), PATCH (update text — operator only with approval-package)
       - `api/v1/analytics/narratives-audit.js`: POST (run claim audit)
       - `api/v1/analytics/narratives-approve.js`: POST (transition to approved)
       - `api/v1/analytics/narratives-publish.js`: POST (transition to published)
       - `api/v1/analytics/narratives-claim-override.js`: PATCH `{ override_reason }`
       - `api/v1/analytics/narratives-generate.js`: POST `{ narrative_kind, period_start, period_end }` → 202 (invokes generator)

       **F-155 (decision-rule-v1) + F-162 (decision-evaluate-v1)** — 4 handlers:
       - `api/v1/analytics/decision-rules.js`: GET, POST
       - `api/v1/analytics/decision-rules-detail.js`: GET, PATCH, DELETE
       - `api/v1/analytics/decision-rules-enable.js`: POST
       - `api/v1/analytics/decision-rules-disable.js`: POST
       - `api/v1/analytics/decision-rules-evaluate.js`: POST manual trigger (F-162)

       **F-160 (journey-step-v1)** — 2 handlers:
       - `api/v1/analytics/journeys-by-profile.js`: GET
       - `api/v1/analytics/journeys.js`: GET with `account_id` query param

       **F-156 (experiment-winner-v1)** — 3 handlers:
       - `api/v1/analytics/experiments-winners.js`: GET with filters experiment_id, status
       - `api/v1/analytics/experiments-declare-winner.js`: POST → 200 with winner record
       - `api/v1/analytics/experiments-reject.js`: POST → 200

       **F-161 (aggregated-metric-v1)** — 1 handler:
       - `api/v1/analytics/benchmarks.js`: GET with filters dimension, dimension_value, metric_id, period

       Each handler imports Zod schemas + invokes library function + writes audit log + returns JSON.

    2. Create `supabase/migrations/144_analytics_rls_hardening.sql` (final RLS pass + audit log column nullability cleanup):
       ```sql
       -- Verify RLS enabled on all 15 analytics tables
       DO $$
       DECLARE
         tbl text;
         tables text[] := ARRAY['metric_definitions','metric_overrides','attribution_models','attribution_touches','anomaly_detections','narrative_templates','narratives','narrative_claims','decision_rules','decision_rule_templates','experiment_winners','experiment_decisions','journey_steps','aggregated_metrics','pricing_signals','freshness_audit'];
       BEGIN
         FOREACH tbl IN ARRAY tables LOOP
           IF NOT EXISTS (SELECT 1 FROM pg_class WHERE relname = tbl AND relrowsecurity = true) THEN
             RAISE EXCEPTION 'RLS not enabled on %', tbl;
           END IF;
         END LOOP;
       END $$;

       -- Defense in depth: ensure aggregated_metrics policy hardened
       DROP POLICY IF EXISTS aggregated_metrics_read ON aggregated_metrics;
       CREATE POLICY aggregated_metrics_read ON aggregated_metrics
         FOR SELECT USING (
           sample_size >= k_anonymity_threshold
           AND k_anonymity_threshold >= 5
           AND status = 'published'
         );

       -- Lock RLS bypass: confirm pricing_signals + decision_rules + narratives + anomaly_detections have NO PUBLIC select policy (only tenant)
       -- (Already enforced in earlier migrations; this is a sanity gate)
       ```

    3. Create `supabase/migrations/145_analytics_openapi_sync.sql` (OpenAPI regen trigger via documentation comment + Supabase type generation hint):
       ```sql
       -- Phase 225 OpenAPI sync gate
       -- This migration is a no-op data migration; its presence triggers the post-migration hook
       -- that runs `npm run openapi:regen && npm run supabase:gen-types`
       -- (See scripts/migrate-and-regen.cjs in repo)
       COMMENT ON SCHEMA public IS 'phase 225 analytics layer published';
       ```

    4. Run OpenAPI regen: `npm run openapi:regen` (existing script). Verify contracts/openapi.json includes `/v1/analytics/*` paths.

    5. Update `public/llms.txt` to add a Phase 225 section listing the 26 endpoints + 7 MCP tools + summary.

    5b. Create `test/migrations/order.test.js` (Blocker 1 wave-execution guard — carries P224 pattern):
       ```typescript
       import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
       import { readdirSync } from 'node:fs';
       describe('Migration order is wave-driven, not numeric (P225 Blocker 1)', () => {
         it('asserts migration runner enforces wave-execution order for P225 migrations 134..145', () => {
           // P225 wave-execution sequence (NOT numeric):
           //  W1: 134, 135, 136, 137, 138
           //  W2: 139, 141 (BEFORE 140)
           //  W2: 142
           //  W3: 143
           //  W4: 140 (numerically earliest, applied LAST in this group)
           //  W5: 144, 145
           const expected = ['134','135','136','137','138','139','141','142','143','140','144','145'];
           // Read migrations.json or the runner config that encodes the order; assert it matches expected.
           // (Implementation depends on how repo stores migration order; this test asserts the contract,
           //  not the file system order.)
           const files = readdirSync('supabase/migrations/').filter((f) => /^1(3[4-9]|4[0-5])_.*\.sql$/.test(f)).map((f) => f.match(/^(1[34]\d)_/)![1]);
           expect(files.sort()).toEqual(['134','135','136','137','138','139','140','141','142','143','144','145']); // files exist on disk in numeric order
           // The runner config (e.g. migrations-order.json) MUST list them in wave order:
           const orderConfig = JSON.parse(require('fs').readFileSync('supabase/migrations-order.json', 'utf-8').toString() || '[]');
           const p225Order = orderConfig.filter((m: string) => /^1(3[4-9]|4[0-5])_/.test(m)).map((m: string) => m.match(/^(1[34]\d)_/)![1]);
           assert.deepStrictEqual(p225Order, expected);
         });
       });
       ```
       (If supabase/migrations-order.json does not yet exist, this test creates the precedent — Wave 5 closeout writes the manifest with P225 wave-execution order.)

    6. Test files:
       - 8 API test files (one per F-ID family) testing GET + POST + cross-tenant denial. E.g.:
         - `test/analytics/api/metrics-api.test.js`: GET list returns global + tenant-scoped; POST creates metric (status='draft'); PATCH approve requires approval-package; cross-tenant: tenant B cannot read tenant A draft metric.
         - `test/analytics/api/anomalies-api.test.js`: GET list filters by status; POST acknowledge transitions state; cross-tenant: 403 on tenant B accessing tenant A detection.
         - `test/analytics/api/narratives-api.test.js`: POST generate returns 202 + narrative_id; POST audit runs claim audit; POST publish requires status='approved'.
         - `test/analytics/api/decision-rules-api.test.js`: full CRUD + enable/disable + manual evaluate.
         - `test/analytics/api/journeys-api.test.js`: GET by profile_id returns ordered steps; cross-tenant denial.
         - `test/analytics/api/experiments-api.test.js`: GET winners; declare-winner validates Bayesian + frequentist; reject writes rejection_reason.
         - `test/analytics/api/benchmarks-api.test.js`: GET returns row when k-anonymity met (sample_size >= 5); 404 or empty when not met.
         - `test/analytics/api/attribution-api.test.js`: GET touches by opp + model_id; POST recompute returns 202 + run_id.

       - `test/analytics/api/openapi-parity.test.js`: read contracts/openapi.json; assert each F-147..F-162 contract path is present; assert request body schema in openapi.json matches contract YAML schema (recursive shape match).

       - `test/analytics/rls/rls-suite-full.test.js`: loops all 15 analytics tables; for each, writes tenant-A row + attempts tenant-B SELECT/UPDATE/DELETE → confirms denial. Special case: aggregated_metrics — tenant-A and tenant-B both read row with sample_size >= 5; both denied row with sample_size < 5.
  </action>
  <acceptance_criteria>
    - File `test/migrations/order.test.js` exists; `npm test -- --test-name-pattern "test/migrations/order.test.js"` exits 0 (wave-execution order asserted; Blocker 1 closure)
    - 26 route handler files exist under `api/v1/analytics/`; `ls api/v1/analytics/*.js | wc -l` returns 26 (legacy api/*.js convention per D-49)
    - File `supabase/migrations/144_analytics_rls_hardening.sql` exists; `grep -E "RAISE EXCEPTION 'RLS not enabled" supabase/migrations/144_*.sql` returns the verification line
    - File `supabase/migrations/145_analytics_openapi_sync.sql` exists
    - `grep -E "/v1/analytics/metrics\\|/v1/analytics/anomalies\\|/v1/analytics/narratives\\|/v1/analytics/journeys" contracts/openapi.json` returns >= 4 path matches
    - `grep -E "Phase 225\\|analytics" public/llms.txt` returns >= 2 matches
    - `npm test -- --test-name-pattern "test/analytics/api/ test/analytics/rls/rls-suite-full.test.js"` exits 0 (9 test files green)
    - openapi-parity.test.js proves F-147..F-162 paths + schemas in contracts/openapi.json
    - rls-suite-full.test.js loops all 15 tables and proves cross-tenant denial
    - `grep -rE "requireTenantContext|serviceRoleClient\\(\\)|lookupPlugin\\b|public/openapi\\.json|index\\.ts.*mcp/tools|app/\\(markos\\)/conversion|app/\\(markos\\)/launches|app/\\(markos\\)/operating" api/v1/analytics/ lib/markos/analytics/ lib/markos/mcp/tools/analytics/ test/analytics/ 2>/dev/null` returns zero positive invocations (architecture-lock + RH6 closure)
    - `find api/v1/analytics -name 'route.ts' 2>/dev/null | wc -l` returns 0 (no App Router files per D-49)
    - `find lib/markos/mcp/tools/analytics -name '*.ts' 2>/dev/null | wc -l` returns 0 (.cjs only per D-49)
    - 225-VALIDATION.md rows 225-07-01 + 225-07-05 ✅ green
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/analytics/api/ test/analytics/rls/rls-suite-full.test.js</automated>
  </verify>
  <done>26 HTTP route handlers + 2 closeout migrations + OpenAPI regen + llms.txt update + 9 test files green; cross-tenant denial proven on every endpoint + table.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 7 MCP tools + Approval Inbox 5 entry types + Morning Brief analytics section + tombstone full cascade + legacy regression</name>
  <read_first>
    - .planning/phases/225-analytics-attribution-narrative-intelligence/225-CONTEXT.md D-36 (7 MCP tools)
    - .planning/phases/225-analytics-attribution-narrative-intelligence/225-CONTEXT.md D-38, D-39
    - lib/markos/mcp/tools/index.cjs (existing tool registry from P200/P202)
    - lib/markos/mcp/tools/publish-page.ts (P224 MCP tool — pattern reference)
    - lib/markos/operating/approvals/entry-types.cjs (P208 enum from earlier phases)
    - lib/markos/operating/morning-brief/launches-section.cjs (P224 reference)
    - lib/markos/analytics/tombstone.ts (225-03)
    - lib/markos/analytics/* (all from 225-02..06)
    - test/regression/p221-p222.regression.test.js (existing pattern; planner adds P223+P224)
  </read_first>
  <behavior>
    - Each MCP tool wraps a Wave 2-6 library function; input/output Zod schemas mirror F-ID contract; tenant context derived from MCP session.
    - Approval Inbox entry types: append 5 to existing union type; add handler in dispatch table that routes each entry_type to the appropriate library (e.g. anomaly_critical → transitionAnomaly, narrative_publish → transitionNarrative).
    - Morning Brief analytics section: pure function returning HTML/markdown fragment with the 4 D-39 sub-sections; integrated into existing brief composition pipeline.
    - Tombstone full-cascade test: writes tombstone for profile + asserts scrubs across attribution_touches + journey_steps + narrative_claims (where evidence_ref pointed to that profile's data) + freshness_audit (no PII bound; just confirm no leakage).
    - Legacy regression: `npm test -- --test-name-pattern "test/cdp-* test/crm-* test/channels-* test/conversion-*"` AND `npm test` both green.
  </behavior>
  <action>
    1. Create 7 MCP tools under `lib/markos/mcp/tools/analytics/`:

       a) `get-metric.ts`:
       ```typescript
       export const getMetricTool = createTool({
         name: 'analytics_get_metric',
         description: 'Read a metric value with breakdown and freshness for the current tenant',
         inputSchema: z.object({
           metric_id: z.string().uuid(),
           dimension_filter: z.record(z.string()).optional(),
           period_start: z.string().datetime().optional(),
           period_end: z.string().datetime().optional(),
         }),
         handler: async (input, ctx) => {
           const metric = await resolveMetricForTenant(ctx.client, input.metric_id, ctx.tenant_id);
           if (!metric) return { error: 'metric_not_found' };
           const value = await recomputeMetricForTenant(ctx.client, { metric_id: input.metric_id, tenant_id: ctx.tenant_id, period_start: input.period_start ?? defaultPeriodStart(metric.freshness_mode), period_end: input.period_end ?? new Date().toISOString() });
           const freshness = isMetricFresh(metric, new Date());
           return { metric, value: value.value, freshness, sample_size: value.sample_size };
         },
       });
       ```

       b) `get-attribution.ts`: input `{ opportunity_id?, profile_id?, account_id?, model_id }`; returns AttributionTouch[] with model + breakdown.

       c) `list-anomalies.ts`: input `{ status?, severity?, metric_id? }`; returns AnomalyDetection[].

       d) `generate-narrative.ts`: input `{ narrative_kind, period_start, period_end }`; invokes generateNarrative; returns narrative_id + claim_count + status.

       e) `evaluate-decision.ts`: input `{ rule_id }`; manually triggers evaluator for that rule; returns FireResult.

       f) `get-journey.ts`: input `{ profile_id?, account_id?, period_start? }`; returns JourneyStep[].

       g) `list-experiment-winners.ts`: input `{ experiment_id?, status? }`; returns ExperimentWinner[].

    2. Update `lib/markos/mcp/tools/index.cjs`:
       ```typescript
       import { getMetricTool } from './analytics/get-metric';
       import { getAttributionTool } from './analytics/get-attribution';
       import { listAnomaliesTool } from './analytics/list-anomalies';
       import { generateNarrativeTool } from './analytics/generate-narrative';
       import { evaluateDecisionTool } from './analytics/evaluate-decision';
       import { getJourneyTool } from './analytics/get-journey';
       import { listExperimentWinnersTool } from './analytics/list-experiment-winners';

       export const ANALYTICS_TOOLS = [
         getMetricTool, getAttributionTool, listAnomaliesTool,
         generateNarrativeTool, evaluateDecisionTool, getJourneyTool, listExperimentWinnersTool,
       ];

       export const ALL_TOOLS = [
         ...EXISTING_TOOLS, // from prior phases
         ...ANALYTICS_TOOLS,
       ];
       ```

    3. Update `lib/markos/operating/approvals/entry-types.cjs` to append 5 new entry types:
       ```typescript
       export type EntryType =
         | /* existing */
         | 'anomaly_critical'
         | 'narrative_publish'
         | 'decision_rule_change'
         | 'experiment_winner_promotion'
         | 'metric_definition_change';

       const ENTRY_TYPE_HANDLERS: Record<EntryType, EntryTypeHandler> = {
         /* existing */,
         anomaly_critical: { description: 'Critical anomaly needs acknowledgment', resolve: handleAnomalyAcknowledge, render: renderAnomalyCard },
         narrative_publish: { description: 'Narrative awaiting approval', resolve: handleNarrativeApprove, render: renderNarrativeCard },
         decision_rule_change: { description: 'Decision rule edit awaiting approval', resolve: handleRuleApprove, render: renderRuleCard },
         experiment_winner_promotion: { description: 'Experiment winner pending operator promotion', resolve: handleWinnerPromote, render: renderWinnerCard },
         metric_definition_change: { description: 'Global metric mutation needs approval-package', resolve: handleMetricApprove, render: renderMetricCard },
       };
       ```

    4. Create `lib/markos/operating/morning-brief/analytics-section.cjs`:
       ```typescript
       export async function composeAnalyticsSection(client, tenantId: string): Promise<{ markdown: string; html: string; sections: AnalyticsBriefData }> {
         // Fetch:
         // 1. Top 5 open anomalies (severity desc)
         const { data: openAnomalies } = await client.from('anomaly_detections').select('*').eq('tenant_id', tenantId).eq('status', 'detected').order('severity', { ascending: false }).order('detected_at', { ascending: false }).limit(5);
         // 2. Top narrative published in last 24h
         const { data: recentNarrative } = await client.from('narratives').select('*').eq('tenant_id', tenantId).eq('status', 'published').gte('approved_at', new Date(Date.now() - 86400_000).toISOString()).order('approved_at', { ascending: false }).limit(1);
         // 3. Experiment winners pending approval
         const { count: pendingWinners } = await client.from('experiment_winners').select('*', { count: 'exact', head: true }).eq('tenant_id', tenantId).eq('status', 'pending_approval');
         // 4. Top 3 freshness SLA violations (descending violation_delta_seconds)
         const { data: freshness } = await client.from('freshness_audit').select('*').eq('tenant_id', tenantId).order('violation_delta_seconds', { ascending: false }).limit(3);

         const markdown = renderMarkdownAnalyticsSection({ anomalies: openAnomalies, narrative: recentNarrative?.[0], pendingWinners, freshness });
         const html = renderHtmlAnalyticsSection(/* same data */);
         return { markdown, html, sections: { anomalies, narrative, pendingWinners, freshness } };
       }
       ```

    5. Create `test/regression/p221-p224.regression.test.js`:
       ```typescript
       import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
       import { execSync } from 'node:child_process';
       describe('Legacy P221-P224 regression (additive guarantee for P225)', () => {
         it('npm test -- test/cdp-* exits 0', () => {
           execSync('npx npm test -- test/cdp-*', { stdio: 'inherit' });
         });
         it('npm test -- test/crm-* exits 0', () => {
           execSync('npx npm test -- test/crm-*', { stdio: 'inherit' });
         });
         it('npm test -- test/channels-* exits 0', () => {
           execSync('npx npm test -- test/channels-*', { stdio: 'inherit' });
         });
         it('npm test -- test/conversion-* exits 0', () => {
           execSync('npx npm test -- test/conversion-*', { stdio: 'inherit' });
         });
         it('npm test (node --test) exits 0', () => {
           execSync('npm test', { stdio: 'inherit' });
         });
       });
       ```

    6. Test files:
       - 7 MCP test files (one per tool): `test/analytics/mcp/{get-metric,get-attribution,list-anomalies,generate-narrative,evaluate-decision,get-journey,list-experiment-winners}.tool.test.js` — input validation, output shape, tenant context required.
       - `test/analytics/operating/approval-inbox-entries.test.js`: 5 entry types render correctly + handler dispatches to correct library.
       - `test/analytics/operating/morning-brief-section.test.js`: composeAnalyticsSection returns 4 sub-sections with correct shape.
       - `test/analytics/tombstone/full-cascade-suite.test.js`: full E2E in single transaction asserting Blocker 4 closure — write a tombstone for a profile, then assert ALL THREE PII-bearing tables are scrubbed within the same tx: (1) attribution_touches.profile_id=null + source_event_ref=null; (2) journey_steps.profile_id=null + ordered_event_refs=[]; (3) narrative_claims.claim_text='[redacted]' + evidence_ref=null + audit_status='manual_override' + override_reason='profile_tombstoned'. Also assert freshness_audit (no PII bound; no scrub needed; no leak). Verify markos_audit_log row metadata reports all three counts (touches_scrubbed + journey_steps_scrubbed + narrative_claims_scrubbed).
  </action>
  <acceptance_criteria>
    - Files exist under lib/markos/mcp/tools/analytics/ (7 tool files)
    - `grep -c "createTool" lib/markos/mcp/tools/analytics/*.ts` returns 7
    - File `lib/markos/mcp/tools/index.cjs` exports ANALYTICS_TOOLS list of 7
    - File `lib/markos/operating/approvals/entry-types.cjs` contains 5 new entry types: `grep -c "anomaly_critical\\|narrative_publish\\|decision_rule_change\\|experiment_winner_promotion\\|metric_definition_change" lib/markos/operating/approvals/entry-types.cjs` returns >= 5
    - File `lib/markos/operating/morning-brief/analytics-section.cjs` exports composeAnalyticsSection
    - File `test/regression/p221-p224.regression.test.js` exists; running it exits 0
    - `npm test -- --test-name-pattern "test/analytics/mcp/ test/analytics/operating/ test/analytics/tombstone/full-cascade-suite.test.js test/regression/p221-p224.regression.test.js"` exits 0
    - 225-VALIDATION.md rows 225-07-02, 225-07-06, 225-07-07 ✅ green
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/analytics/mcp/ test/analytics/operating/ test/analytics/tombstone/full-cascade-suite.test.js test/regression/p221-p224.regression.test.js</automated>
  </verify>
  <done>7 MCP tools registered; Approval Inbox + Morning Brief integrations shipped; tombstone full-cascade suite + legacy regression suite green; 13 test files green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 7 UI workspaces + 4 CSS modules + 6 Playwright e2e specs + 7 Chromatic stories + docs trio</name>
  <read_first>
    - .planning/phases/225-analytics-attribution-narrative-intelligence/225-CONTEXT.md D-37, D-38, D-39
    - .planning/phases/225-analytics-attribution-narrative-intelligence/225-RESEARCH.md §UI Compatibility Note (lines 1276-1287)
    - obsidian/reference/UI Components.md (canonical CSS module + naming conventions)
    - app/(markos)/conversion/page.tsx + ConversionWorkspace component (P224 reference shape)
    - app/(markos)/launches/LaunchCockpit.tsx (P224 reference for cockpit pattern)
    - components/markos/conversion/conversion-workspace.module.css (existing CSS module)
    - lib/markos/analytics/render/stale-indicator.tsx (225-02 — first shared analytics primitive)
    - test/playwright/launches/launch-cockpit.spec.ts (P224 Playwright pattern reference)
    - test/chromatic/launches/LaunchCockpit.stories.tsx (P224 Chromatic pattern reference)
    - app/(markos)/crm/reporting/page.tsx (P105 cockpit; AnalyticsWorkspace evolves from this — see RESEARCH UI Compatibility Note)
  </read_first>
  <behavior>
    - 7 UI components are React Server Components or 'use client' hybrids matching project convention; CSS via Tailwind + CSS modules per UI Components.md.
    - AnalyticsWorkspace shows metric catalog grouped by category with StaleIndicator chips per metric + "Recompute" + "Edit override" buttons.
    - AttributionExplorer: opportunity selector + model selector + touchpoint timeline + revenue contribution chart.
    - AnomalyInbox: kanban-by-status (detected/acknowledged/investigating/resolved/dismissed) + assignment + resolution panel + severity chips.
    - NarrativeFeed: chronological list of narratives by kind + claim audit panel (expandable) + approve/publish controls.
    - ExperimentsCockpit: experiment list + winner detection state + Bayesian/frequentist confidence + ICE backlog + variant promotion approval.
    - DecisionRulesEditor: per-tenant decision_rule CRUD + audit log of fires.
    - BenchmarksPanel: cross-tenant aggregated metrics with k-anonymity gating + opt-in toggle.
    - 6 Playwright specs cover operator journeys: anomaly triage, narrative approve+publish, experiment winner promote, metric drill-down, Morning Brief analytics section visible, Approval Inbox analytics entry types.
    - 7 Chromatic stories cover empty/loading/healthy/stale/error/forbidden states for each component.
    - Docs trio: index.md (overview + workflow), api.md (26 endpoints + Zod schemas), mcp-tools.md (7 MCP tools).
  </behavior>
  <action>
    1. Create 7 UI page components under `app/(markos)/analytics/`:

       a) `page.tsx` (AnalyticsWorkspace):
       ```tsx
       import { AnalyticsWorkspace } from './AnalyticsWorkspace';
       export default async function AnalyticsPage() {
         const session = await requireSession();
         const metrics = await listMetricsForTenant(supabase, session.tenant_id);
         return <AnalyticsWorkspace metrics={metrics} tenantId={session.tenant_id} />;
       }
       ```
       `AnalyticsWorkspace.tsx`: groups metrics by category; renders MetricCatalogEditor for global metric mutations (D-04 approval-package); StaleIndicator per metric.

       b) `attribution/page.tsx` + `AttributionExplorer.tsx`: opp selector dropdown + model selector dropdown + touchpoint timeline list + revenue contribution chart (Chart.js or Recharts already in repo).

       c) `anomalies/page.tsx` + `AnomalyInbox.tsx`: kanban with 5 columns (one per status); each card shows metric name, deviation_sigma, severity chip, "Acknowledge"/"Investigate"/"Resolve"/"Dismiss" buttons; click opens resolution panel.

       d) `narratives/page.tsx` + `NarrativeFeed.tsx`: vertical list of narratives ordered by created_at DESC; each card has narrative_kind chip, status chip, "View" + "Audit claims" + "Approve" + "Publish" buttons; expand reveals claim audit panel (each claim with audit_status badge + override controls).

       e) `experiments/page.tsx` + `ExperimentsCockpit.tsx`: table of active experiments + winner state + Bayesian/frequentist columns + ICE backlog sub-panel + "Promote variant" button (requires Approval Inbox approval per D-25, D-38).

       f) `decision-rules/page.tsx` + `DecisionRulesEditor.tsx`: list of decision_rules + filter by trigger_kind/status; each row "Edit" / "Enable" / "Disable" / "Delete" / "Evaluate now" actions; audit log of fires (last 50) sub-panel.

       g) `benchmarks/page.tsx` + `BenchmarksPanel.tsx`: opt-in toggle (writes feature_flags.analytics_cross_tenant_share); list of aggregated_metrics rows (industry baseline, segment baseline, etc.); "Pending k-anonymity (n=4)" badge for blocked rows; sample_size + period column.

    2. Create 4 CSS modules under `components/markos/analytics/`:
       - `analytics-workspace.module.css`: metric grid (3-col on lg, 1 on mobile); category headers; metric card hover/focus.
       - `anomaly-inbox.module.css`: kanban columns (5 fixed); severity chip colors (low: gray, medium: amber, high: orange, critical: red); resolution panel slide-in.
       - `narrative-feed.module.css`: card layout; status badge variants; expandable claim audit accordion.
       - `experiments-cockpit.module.css`: table layout; winner state cards (detected/pending_approval/approved/rejected); ICE backlog sub-panel.

    3. Create 6 Playwright e2e specs under `test/playwright/analytics/`:
       - `analytics-workspace.spec.ts`: navigate to /analytics; assert metric catalog renders; click "Recompute" on a metric; assert recompute job started toast.
       - `anomaly-inbox.spec.ts`: navigate to /analytics/anomalies; create critical anomaly via fixture; assert appears in "detected" column; click "Acknowledge" → moves to "acknowledged" column; click "Resolve" with summary → moves to resolved.
       - `narrative-feed.spec.ts`: navigate to /analytics/narratives; click on draft narrative; click "Audit claims" → audit findings render; click "Approve" → status chip flips; click "Publish" → confirms approval and status flips.
       - `experiments-cockpit.spec.ts`: navigate to /analytics/experiments; assert winner state visible; click "Promote variant" → opens Approval Inbox entry; check entry from Approval Inbox spec.
       - `morning-brief-analytics.spec.ts`: navigate to /morning-brief (P208 surface); assert analytics section visible with 4 sub-sections (top anomalies, recent narrative, pending winners, freshness violations).
       - `approval-inbox-analytics.spec.ts`: navigate to /approval-inbox; assert 5 new entry types appear when fixture data populates them.

    4. Create 7 Chromatic stories under `test/chromatic/analytics/`:
       - `AnalyticsWorkspace.stories.tsx`: 4 variants (empty=no metrics, loading, healthy=10 metrics, stale=critical freshness violations).
       - `AttributionExplorer.stories.tsx`: 4 variants (no-opportunity-selected, single-touch, multi-touch-multi-model, error-loading).
       - `AnomalyInbox.stories.tsx`: 4 variants (empty=no anomalies, healthy=mixed states, all-critical, error-state).
       - `NarrativeFeed.stories.tsx`: 4 variants (empty, draft-with-claims-failing, audited-pending-approval, published-recent).
       - `ExperimentsCockpit.stories.tsx`: 4 variants (no-experiments, detected-no-winner, winner-pending, winner-approved).
       - `DecisionRulesEditor.stories.tsx`: 4 variants (default-7-rules, custom-rules, disabled-rules, audit-log-expanded).
       - `BenchmarksPanel.stories.tsx`: 4 variants (opt-out, k-anonymity-blocked-row, k-anonymity-passed-row, mixed-dimensions).

    5. Create 3 doc files under `docs/analytics/`:
       - `index.md`: Phase 225 overview; what analytics provides; how to use the API + MCP tools + UI workspaces; placeholder rule for {{MARKOS_PRICING_ENGINE_PENDING}}.
       - `api.md`: full /v1/analytics/* endpoint reference; for each endpoint show request body Zod schema + response 2xx schema + error codes (links to F-* contract YAMLs).
       - `mcp-tools.md`: 7 MCP tools with input + output schemas + example calls.

    6. Update public/llms.txt to add Phase 225 section linking to docs/analytics/.
  </action>
  <acceptance_criteria>
    - 7 page.tsx files exist under app/(markos)/analytics/* (page.tsx in each subdir)
    - 7 component files exist (AnalyticsWorkspace.tsx, AttributionExplorer.tsx, AnomalyInbox.tsx, NarrativeFeed.tsx, ExperimentsCockpit.tsx, DecisionRulesEditor.tsx, BenchmarksPanel.tsx)
    - 4 CSS module files exist under components/markos/analytics/
    - 6 Playwright spec files exist under test/playwright/analytics/
    - 7 Chromatic story files exist under test/chromatic/analytics/
    - 3 doc files exist under docs/analytics/
    - `echo "Playwright deferred per D-50 — visual regression handled by chromatic + npm test --test-name-pattern playwright-stub"` runs without configuration error (skipped gracefully if no live server)
    - `npm test -- --test-name-pattern "test/playwright/analytics/"` collected as test files (Playwright tests run via npx playwright; this just validates files exist)
    - 225-VALIDATION.md rows 225-07-03, 225-07-04 ✅ green
  </acceptance_criteria>
  <verify>
    <automated>echo 'Playwright deferred per D-50; chromatic handles visual regression' || echo "playwright-checked" && ls test/chromatic/analytics/ | wc -l | grep -E "^7$" && npm run chromatic -- --only-changed --exit-zero-on-changes</automated>
  </verify>
  <done>7 UI workspaces + 4 CSS modules + 6 Playwright specs + 7 Chromatic stories + 3 doc pages shipped; visual + e2e regression suites scaffolded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Phase 225 closeout — human-verify operator journey + visual regression + 225-SUMMARY.md + STATE.md + ROADMAP.md updates</name>
  <what-built>
    Phase 225 ships the semantic analytics layer: 15 tables (12 migrations 134-145), 16 contracts (F-147..F-162), 25 metric seeds (D-03 canonical: 9 core + 11 SaaS + 5 messaging) + 6 attribution model seeds + 7 default decision-rule seeds, full 7-slice 5-wave intelligence stack:
    - Wave 1 (225-01): schema foundation
    - Wave 2 (225-02 + 225-03 parallel): metric catalog runtime + freshness contract; attribution engine (6 models) + legacy adapter
    - Wave 3 (225-04 + 225-05 parallel): anomaly detection + decision rules engine; narrative generation (template + LLM + claim audit + pricing-guard) + journey materializer
    - Wave 4 (225-06): experiment winner detection (Bayesian + frequentist both-must-agree) + pricing signal emission + cross-tenant k-anonymity aggregation
    - Wave 5 (225-07): API + 7 MCP tools + 6 UI workspaces + Approval Inbox + Morning Brief + RLS hardening + OpenAPI regen + Playwright + Chromatic + closeout
  </what-built>
  <how-to-verify>
    1. **Decision coverage map (CRITICAL):** Open `.planning/phases/225-analytics-attribution-narrative-intelligence/225-SUMMARY.md` and verify each of D-01 through D-48 has a row in the coverage table mapping to a specific plan (225-01..07) + task + status (Full/Partial). Any "Partial" or "Deferred" must have an explicit phase reference (P226/P227/P228 OR a documented v2 deferral).
    2. **Operator journey — Anomaly triage:** Visit /analytics/anomalies (with seeded test tenant); verify a synthesized critical anomaly appears in "detected" column; click "Acknowledge" → state advances; click "Resolve" with summary → state advances; verify markos_audit_log has 3 entries (transitions).
    3. **Operator journey — Narrative approve+publish:** Visit /analytics/narratives; trigger a weekly_summary generation via UI; verify narrative appears with status='draft'; click "Audit claims" → all claims resolve to passed (or note manual_override needed); click "Approve" → status='approved'; click "Publish" → status='published'; verify narrative appears in next Morning Brief.
    4. **Operator journey — Experiment winner promote:** Visit /analytics/experiments; verify a winner card with Bayesian + frequentist confidence; click "Promote variant" → Approval Inbox shows experiment_winner_promotion entry; approve → P224 conversion_experiments.status flips to 'completed' + experiment_winners.status='approved'.
    5. **{{MARKOS_PRICING_ENGINE_PENDING}} verification:** Trigger a pricing_signal narrative for a tenant WITHOUT P205 approved record; verify narrative_text contains `{{MARKOS_PRICING_ENGINE_PENDING}}` replacements (CLAUDE.md + D-16 rule).
    6. **k-anonymity verification:** Visit /analytics/benchmarks; verify a row with sample_size=4 is hidden; row with sample_size=5 visible; opt-out tenant excluded from aggregation.
    7. **Cross-tenant denial spot check:** Use API client to GET /v1/analytics/anomalies with tenant B's session for tenant A's anomaly_id → expect 403.
    8. **Visual regression:** Run `npm run chromatic` (or visit Chromatic dashboard); verify all 7 component × 4 variant stories pass; review any drift screenshots.
    9. **VALIDATION.md per-task map status:** Confirm `nyquist_compliant: true` and every row has Status ✅ green (no ⬜ pending or ❌ red).
    10. **Legacy regression:** `npm test && npm test -- test/cdp-* test/crm-* test/channels-* test/conversion-*` — all green (additive guarantee).
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 225 complete, OR describe gaps + freezes (e.g. "Bayesian sampler too slow" or "Approval Inbox missing entry icon" or "Morning Brief layout drift") for /gsd-plan-phase --gaps follow-up</resume-signal>
</task>

<task type="auto">
  <name>Task 5: SUMMARY.md + STATE.md + ROADMAP.md updates (post-checkpoint approval)</name>
  <read_first>
    - .planning/phases/225-analytics-attribution-narrative-intelligence/225-CONTEXT.md (D-01..D-48)
    - .planning/STATE.md (current state pre-225)
    - .planning/ROADMAP.md (Phase 225 entry)
    - .planning/phases/224-conversion-launch-workspace/224-07-PLAN.md output section (SUMMARY pattern)
  </read_first>
  <action>
    1. Create `.planning/phases/225-analytics-attribution-narrative-intelligence/225-SUMMARY.md`:
       - Header: Phase 225 — Analytics, Attribution, and Narrative Intelligence — Complete
       - Status: shipped 2026-04-{date}; verification approved
       - Plan inventory: 7 plans / 5 waves / 16 contracts / 12 migrations / 27 + 6 + 7 + 8 seeds (metric/attribution/decision-rule/narrative-template) / 15 tables / 7 MCP tools / 7 UI components / 26 API endpoints
       - Decision coverage table:
         ```
         | Decision | Plan | Task | Status |
         |----------|------|------|--------|
         | D-01 metric_definitions | 225-01 | T2 + 225-02 T1 | Full |
         | D-02 metric_overrides | 225-01 | T2 + 225-02 T1 | Full |
         | D-03 25 canonical universal seeds (9 core + 11 SaaS + 5 messaging) | 225-01 | T2 | Full |
         | D-04 approval-package on global mutation | 225-07 | T1 (PATCH endpoint) | Full |
         | D-05 attribution_models | 225-01 + 225-03 | T1 | Full |
         | D-06 attribution_touches | 225-01 + 225-03 | T1 | Full |
         | D-07 3 trigger sources | 225-03 | T2 | Full |
         | D-08 multi-model coexistence | 225-03 | T1 | Full |
         | D-09 anomaly_detections | 225-01 + 225-04 | T1 | Full |
         | D-10 anomaly_config | 225-04 | T1 | Full |
         | D-11 state machine | 225-04 | T1 | Full |
         | D-12 severity scoring | 225-04 | T1 | Full |
         | D-13 narratives SOR | 225-01 + 225-05 | T1 | Full |
         | D-14 hybrid generation | 225-05 | T1 | Full |
         | D-15 narrative_claims | 225-01 + 225-05 | T2 | Full |
         | D-16 placeholder rule | 225-05 | T1 (pricing-guard) | Full |
         | D-17 narrative_kind tones | 225-05 | T1 (8 templates) | Full |
         | D-18 decision_rules | 225-01 + 225-04 | T2 | Full |
         | D-19 pluggable evaluators | 225-04 | T2 | Full |
         | D-20 default seeds | 225-01 + 225-04 | T2 (cloneTemplatesForTenant) | Full |
         | D-21 cross-domain action emit | 225-04 + 225-06 | T2 + T3 | Full |
         | D-22 experiment_winners | 225-01 + 225-06 | T1 | Full |
         | D-23 hybrid Bayesian + frequentist | 225-06 | T1 | Full |
         | D-24 statistical guardrails | 225-06 | T1 | Full |
         | D-25 ICE scoring | 225-06 | T1 | Full |
         | D-26 pricing signal via cdp_events | 225-06 | T2 | Full |
         | D-27 pricing_signals audit | 225-01 + 225-06 | T2 | Full |
         | D-28 aggregated_metrics k-anonymity | 225-06 | T2 | Full |
         | D-29 tenant opt-in toggle | 225-06 | T2 | Full |
         | D-30 trusted server context | 225-06 | T2 | Full |
         | D-31 journey_steps | 225-02 (table) + 225-05 (materialize) | T1+T3 | Full |
         | D-32 cadence + AgentRun bridge | 225-02 + 225-05 | T1+T3 | Full |
         | D-33 freshness contract | 225-02 | T3 | Full |
         | D-34 freshness_audit | 225-02 | T1+T3 | Full |
         | D-35 read-write API | 225-07 | T1 | Full |
         | D-36 7 MCP tools | 225-07 | T2 | Full |
         | D-37 6 UI workspaces (+ BenchmarksPanel) | 225-07 | T3 | Full |
         | D-38 Approval Inbox 5 entry types | 225-07 | T2 | Full |
         | D-39 Morning Brief analytics section | 225-07 | T2 | Full |
         | D-40 freshness audit cron | 225-02 | T3 | Full |
         | D-41 anomaly-of-anomalies | 225-04 | T1+T3 | Full |
         | D-42 narrative audit log | 225-05 | T2 (publisher) | Full |
         | D-43 RLS on 15 tables | 225-01..06 + 225-07 hardening | T2 + T1 | Full |
         | D-44 audit trail mandatory | 225-01..07 | all tasks (audit_log on every mutation) | Full |
         | D-45 tombstone propagation | 225-03 + 225-05 cron + 225-07 full suite | T2 + T3 + T2 | Full |
         | D-46 cross-tenant aggregation trusted server | 225-06 | T2 | Full |
         | D-47 F-IDs F-147..F-162 | 225-01..07 (all plans) | each F-ID per plan | Full (16/16 registered) |
         | D-48 12 migrations 134..145 | 225-01..07 (all plans) | each migration per plan | Full (12/12 shipped) |
         ```
       - Carry-forward: EVD-01..06 satisfied via P209 EvidenceMap adapter (or A21 stub); LOOP-07/08 via narrative + decision rules.
       - Open follow-ups: P226 sales enablement consumes attribution_touches + journey_steps + experiment_winners; P227 ecosystem analytics; P228 commercial OS integration closure.
       - Vault canonical reference updates needed: obsidian/reference/CRM Domain.md should reference analytics layer.

    2. Update `.planning/STATE.md`:
       - Current Position: Phase 225 — closeout complete; advance to Phase 226 (sales enablement & deal execution)
       - Phase 225 Plan Progress: mark all 7 plans `[x] complete` with date
       - Add planning overlay note: P225 ships full analytics SOR + 7 MCP tools + 6 UI workspaces; downstream P226 consumes attribution + journeys + winners
       - Update progress counters: completed_plans += 7

    3. Update `.planning/ROADMAP.md` Phase 225 entry:
       - Status: ✅ Complete — verified {date}
       - Plans: 7/7 plans complete
       - Plans list: mark `[x]` for 225-01..07 with one-line summary for each
       - Add note about decision coverage: 48/48 decisions delivered

    4. Final commit message: `docs(225): phase 225 GA — analytics, attribution, narrative intelligence complete (16 contracts, 12 migrations, 7 MCP tools, 6 UI workspaces)`
  </action>
  <acceptance_criteria>
    - File `.planning/phases/225-analytics-attribution-narrative-intelligence/225-SUMMARY.md` exists with D-01..D-48 coverage table
    - File `.planning/STATE.md` Phase 225 marked complete; current position advances to P226
    - File `.planning/ROADMAP.md` Phase 225 status flips to ✅ Complete
    - `grep -c "Full" .planning/phases/225-analytics-attribution-narrative-intelligence/225-SUMMARY.md` returns >= 48 (one per decision)
    - 225-VALIDATION.md all rows ✅ green; nyquist_compliant: true
  </acceptance_criteria>
  <verify>
    <automated>node -e "const c = require('fs').readFileSync('.planning/phases/225-analytics-attribution-narrative-intelligence/225-SUMMARY.md','utf-8'); const fullCount = (c.match(/\\| Full \\|/g) || []).length; if (fullCount < 48) { console.error('SUMMARY decision coverage incomplete: '+fullCount+'/48'); process.exit(1); } console.log('SUMMARY: '+fullCount+'/48 decisions Full');"</automated>
  </verify>
  <done>225-SUMMARY.md + STATE.md + ROADMAP.md updated; phase 225 marked GA; 48/48 decisions delivered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External API client → /v1/analytics/* | Tenant context required; cross-tenant denial enforced |
| MCP session → tool invocation | MCP server provides tenant context; tool uses it |
| Operator UI → state mutation | UI calls API; API enforces RLS + audit log |
| Approval Inbox entry → library handler | Dispatch table maps entry_type → handler; no untrusted code execution |
| Morning Brief composition → tenant data | Per-tenant query; service-role for aggregation only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-225-07-01 | Spoofing | API endpoint without tenant context | mitigate | requireHostedSupabaseAuth({ req, runtimeContext, operation }) called from EVERY api/v1/analytics/*.js handler; returns 401 with structured error envelope if no session/tenant (per D-49); enforced in Task 1 step 1 |
| T-225-07-02 | Information Disclosure | Cross-tenant API call | mitigate | RLS on every analytics table; route handler also checks resource.tenant_id == session.tenant_id; rls-suite-full.test.js proves on all 15 tables |
| T-225-07-03 | Tampering | Operator-edited metric_definition global mutation | mitigate | PATCH endpoint requires approval-package per D-04 (P105 + P208 pattern); markos_audit_log per mutation |
| T-225-07-04 | Spoofing | MCP tool invocation without authenticated session | mitigate | MCP server (P202) requires OAuth session + tenant context; tools inherit |
| T-225-07-05 | Repudiation | Narrative publish without audit | mitigate | publisher.transitionNarrative draft→audited gate enforces auditClaims; markos_audit_log per transition (225-05) |
| T-225-07-06 | DoS | Bulk recompute via API spam | mitigate | Rate limit per tenant (P203 webhook engine pattern); recompute always wrapped in AgentRun (queueing) |
| T-225-07-07 | Information Disclosure | Tombstoned profile data leaking via API | mitigate | tombstone full-cascade test (Task 2 step 6) proves PII scrubbed across 4 tables; nightly cron + on-demand cascade |
| T-225-07-08 | Tampering | OpenAPI doc drift vs actual handler | mitigate | openapi-parity.test.js (Task 1) asserts roundtrip; CI runs on every PR |
| T-225-07-09 | Spoofing | UI state mutation without CSRF token | mitigate | Next.js Server Actions handle CSRF natively; mutations via Server Actions only (no client-side direct DB writes) |
| T-225-07-10 | Information Disclosure | aggregated_metrics threshold tampering via UI | mitigate | BenchmarksPanel reads RLS-gated rows; no direct k_anonymity_threshold UI control; trusted server cron only writes |
| T-225-07-11 | Repudiation | Decision rule fire chain | mitigate | analytics_decision_rule_record_fire writes audit_log_chain JSONB; markos_audit_log row per fire; circuit_breaker_until enforced |
| T-225-07-12 | Tampering | Approval Inbox dispatch table accepting unknown entry_type | mitigate | TypeScript exhaustive switch on EntryType; build fails if entry_type not in dispatch (Task 2 step 3) |
</threat_model>

<verification>
- [ ] `npm test -- --test-name-pattern "test/analytics/api/ test/analytics/rls/rls-suite-full.test.js test/analytics/mcp/ test/analytics/operating/ test/analytics/tombstone/full-cascade-suite.test.js test/regression/p221-p224.regression.test.js"` all green
- [ ] `npm test` (node --test) green (legacy regression)
- [ ] OpenAPI parity test proves /v1/analytics/* paths in contracts/openapi.json
- [ ] `echo "Playwright deferred per D-50 — visual regression handled by chromatic + npm test --test-name-pattern playwright-stub"` green for 6 specs
- [ ] Chromatic visual regression: 7 components × 4 variants pass
- [ ] All 16 P225 F-IDs (F-147..F-162) registered in flow-registry.json
- [ ] All 12 P225 migrations (134..145) shipped
- [ ] Migration 144 RLS audit pass on 15 analytics tables
- [ ] 5 Approval Inbox entry types added (D-38)
- [ ] Morning Brief analytics section composes 4 sub-sections (D-39)
- [ ] 7 MCP tools registered in lib/markos/mcp/tools/index.cjs
- [ ] 7 UI workspaces under app/(markos)/analytics/
- [ ] Tombstone full-cascade suite proves PII scrubbed across 4 affected tables
- [ ] {{MARKOS_PRICING_ENGINE_PENDING}} placeholder enforced in narrative gen + pricing signals (D-16, CLAUDE.md)
- [ ] 225-SUMMARY.md decision coverage table 48/48 Full
- [ ] STATE.md + ROADMAP.md updated; Phase 225 marked complete
- [ ] 225-VALIDATION.md nyquist_compliant: true; all rows ✅ green
- [ ] HUMAN-VERIFY checkpoint approved (operator journey + visual + decision coverage)
</verification>

<success_criteria>
- 26 HTTP route handlers under /v1/analytics/* + Zod schemas + cross-tenant denial
- 7 MCP tools registered (D-36)
- 6 UI workspaces + BenchmarksPanel (D-37) + 4 CSS modules
- 5 Approval Inbox entry types (D-38) + Morning Brief analytics section (D-39)
- 2 closeout migrations (144 RLS hardening + 145 OpenAPI sync)
- OpenAPI regen + llms.txt + 3 doc pages
- 9 node:test API + 7 MCP + 4 operating + RLS suite + tombstone full + regression suite green
- 6 Playwright operator journey specs
- 7 Chromatic stories × 4 variants
- 225-SUMMARY.md with 48/48 D-XX coverage Full
- STATE.md + ROADMAP.md flip Phase 225 to GA
- HUMAN-VERIFY checkpoint approved
</success_criteria>

<output>
Final phase 225 closeout. Phase complete after Task 5 commit. Next milestone path: P226 (sales enablement & deal execution) consumes attribution_touches + journey_steps + experiment_winners.
</output>
