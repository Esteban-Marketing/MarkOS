---
phase: 220
plan: "03"
type: execute
wave: 2
depends_on:
  - "220-01"
files_modified:
  - supabase/migrations/93_growth_events.sql
  - lib/markos/events/contracts.ts
  - lib/markos/events/contracts.cjs
  - lib/markos/events/events.ts
  - lib/markos/events/events.cjs
  - lib/markos/events/promotion-plan.ts
  - lib/markos/events/reminders.ts
  - lib/markos/events/attribution.ts
  - lib/markos/events/approval-gates.ts
  - lib/markos/events/index.cjs
  - contracts/F-215-growth-event-v1.yaml
  - contracts/F-216-growth-event-registration-v1.yaml
  - contracts/F-217-growth-event-results-v1.yaml
  - contracts/flow-registry.json
  - scripts/preconditions/220-03-check-upstream.cjs
  - test/growth-220/events/events.test.js
  - test/growth-220/events/promotion-plan.test.js
  - test/growth-220/events/reminders.test.js
  - test/growth-220/events/attribution.test.js
  - test/growth-220/events/approval-gates.test.js
  - test/growth-220/events/db-trigger-promoting-requires-approval.test.js
  - test/growth-220/rls/events-cross-tenant-denial.test.js
  - test/growth-220/openapi/parity-events.test.js
autonomous: true
requirements:
  - SG-06
  - LOOP-06
  - LOOP-08
  - QA-01
  - QA-02
  - QA-03
must_haves:
  truths:
    - Wave 2 entry — assertUpstreamReady preflight (scripts/preconditions/220-03-check-upstream.cjs); requires P214/P215/P218; SOFT P210 connector + P222 opportunities + P225 attribution_touches (graceful degrade per RESEARCH §6 Domain 3)
    - Migration 93 ships per V4.1.0-MIGRATION-SLOT-COORDINATION.md slot allocation; CONTAINS 5 SOR tables (marketing_events, event_speakers, event_registrations, event_reminders, event_results) + 1 DB-trigger function (event-status-promoting-requires-approval) + indexes
    - DB-trigger compliance — fn_event_promoting_requires_approval BEFORE UPDATE on marketing_events raises EXCEPTION 'EVENT_EXTERNAL_DISPATCH_REQUIRES_APPROVAL' when NEW.status IN ('promoting','live') AND OLD.status = 'planning' AND NEW.approved_at IS NULL
    - 9 event_type ENUM values + 3 format ENUM (virtual/in_person/hybrid) + 9 reminder window_kind values (T-28d/T-21d/T-14d/T-7d/T-2d/T-1d/T-1h/day_of/post_replay) per RESEARCH §6 Domain 3 schema + EventPromotionPlan auto-generation per doc 17 line 996-1048
    - approval-gates.ts wires buildApprovalPackage on (a) status 'planning' → 'promoting' transition (locks promotion plan + dispatches reminders), (b) external invitation send via promotion_plan dispatch, (c) post-event sales sequence to attendees/no-shows
    - reminders.ts — 9-window reminder schedule generation; idempotency on (event_id, send_at, window_kind); NEVER dispatches when scheduled_at < now() - 1h (Pitfall 6 fix)
    - attribution.ts — pipeline_attribution_window_days lookback against event_registrations.contact_id → opportunities (P222 SOFT_UPSTREAM); pipeline_sourced_cents + pipeline_influenced_cents rollup into event_results
    - 3 contracts ship F-215 + F-216 + F-217; flow-registry + openapi regen
    - Architecture-lock PASS on lib/markos/events/* + scripts/preconditions/220-03-check-upstream.cjs (0 forbidden patterns)
    - Cross-tenant RLS denial proven across all 5 event SOR tables
  artifacts:
    - path: supabase/migrations/93_growth_events.sql
      provides: marketing_events (15 cols — event_id PK + tenant_id FK + event_type CHECK 9 values + name + description + scheduled_at + duration_minutes CHECK > 0 + format CHECK 3 values + platform text + capacity + status CHECK 5 values planning/promoting/live/completed/canceled + registration_target + attendance_rate_target numeric(5,4) + pipeline_target_cents bigint + promotion_plan_jsonb + attribution_window_days INT DEFAULT 90 + created_at + created_by NOT NULL + approved_at + approved_by) + event_speakers (8 cols + ON DELETE CASCADE FK) + event_registrations (12 cols — registration_id PK + event_id FK + contact_id soft-FK to P222 + email + utm_jsonb + attended bool + attended_at + attended_minutes + no_show_sequence_sent_at + attendee_sequence_sent_at + pipeline_opportunity_id soft-FK to P222) + event_reminders (8 cols — UNIQUE(event_id, send_at, window_kind) + window_kind CHECK 9 values + status CHECK 4 values pending/dispatched/failed/canceled) + event_results (8 cols — UNIQUE event_id + pipeline_sourced_cents + pipeline_influenced_cents + cost_cents + computed_at) + RLS on all 5 + idx_event_registrations_event ON (tenant_id, event_id) + fn_event_promoting_requires_approval trigger
    - path: lib/markos/events/contracts.ts
      provides: 5 enum constants (EVENT_TYPES, EVENT_FORMATS, EVENT_STATUSES, REMINDER_WINDOW_KINDS, REMINDER_STATUSES) + zod schemas + TS types
      exports: [EVENT_TYPES, EVENT_FORMATS, EVENT_STATUSES, REMINDER_WINDOW_KINDS, REMINDER_STATUSES, MarketingEventSchema, EventRegistrationSchema, EventReminderSchema, EventResultSchema]
    - path: lib/markos/events/promotion-plan.ts
      provides: generatePromotionPlan({ event_id, event_type, scheduled_at, channels[] }) → EventPromotionPlan jsonb structure; auto-generates per doc 17 line 996-1048; consumed by approval-gates on activation
      exports: [generatePromotionPlan, EventPromotionPlan]
    - path: lib/markos/events/reminders.ts
      provides: scheduleReminders({ event_id, scheduled_at, window_kinds[] }) — generates rows in event_reminders for each window relative to scheduled_at; dispatchPendingReminders({ now }) — selects pending where send_at <= now AND scheduled_at > now - INTERVAL '1 hour' (Pitfall 6 buffer); marks dispatched_at; emits via lib/markos/outbound
      exports: [scheduleReminders, dispatchPendingReminders, computeReminderTimes]
    - path: lib/markos/events/attribution.ts
      provides: rollupEventAttribution({ event_id, lookback_days }) — JOIN event_registrations.contact_id → opportunities (P222 SOFT) WHERE opportunity.created_at BETWEEN event.scheduled_at AND event.scheduled_at + lookback_days; sum amount → pipeline_sourced_cents; assist via attribution_touches (P225 SOFT)
      exports: [rollupEventAttribution, computeSourceAttribution, computeInfluenceAttribution]
    - path: lib/markos/events/approval-gates.ts
      provides: requestEventActivation, requestExternalInviteSend, requestPostEventSequence — all call buildApprovalPackage with event-specific kind values; activate sets approved_at + approved_by + status='promoting'
      exports: [requestEventActivation, requestExternalInviteSend, requestPostEventSequence]
    - path: contracts/F-215-growth-event-v1.yaml
      provides: GET/POST/PATCH /v1/growth/events; activate path with approval gate (status 'planning' → 'promoting')
    - path: contracts/F-216-growth-event-registration-v1.yaml
      provides: GET/POST /v1/growth/events/{id}/registrations + utm capture
    - path: contracts/F-217-growth-event-results-v1.yaml
      provides: GET /v1/growth/events/{id}/results — read-only post-event aggregation
    - path: scripts/preconditions/220-03-check-upstream.cjs
      provides: CLI preflight; calls assertUpstreamReady; SOFT_UPSTREAM includes P222 opportunities + P225 attribution_touches (graceful degrade)
    - path: test/growth-220/events/db-trigger-promoting-requires-approval.test.js
      provides: service-role direct UPDATE marketing_events SET status='promoting' WHERE OLD.status='planning' AND approved_at IS NULL → expect EXCEPTION 'EVENT_EXTERNAL_DISPATCH_REQUIRES_APPROVAL'
  key_links:
    - from: lib/markos/events/approval-gates.ts
      to: lib/markos/crm/agent-actions.ts
      via: const { buildApprovalPackage } = require('../crm/agent-actions') — kinds event_activation/event_external_invite/event_post_sequence
      pattern: "buildApprovalPackage"
    - from: lib/markos/events/reminders.ts
      to: lib/markos/outbound
      via: dispatchPendingReminders calls outbound send (existing P210/P223 substrate); never dispatches when scheduled_at < now - 1h (Pitfall 6)
      pattern: "scheduled_at > now"
    - from: supabase/migrations/93_growth_events.sql
      to: lib/markos/events/contracts.ts
      via: REMINDER_WINDOW_KINDS array (9 values) mirrors event_reminders.window_kind CHECK exactly
      pattern: "REMINDER_WINDOW_KINDS"
---

<objective>
Wave 2 plan B (parallel with Plan 02) — translate event marketing into governed workflow per RESEARCH §6 Domain 3. Ship marketing_events + event_speakers + event_registrations + event_reminders + event_results SOR (migration 93), the DB-trigger compliance enforcement (status 'planning' → 'promoting' requires approved_at), the auto-generated EventPromotionPlan per doc 17 line 996-1048, the 9-window reminder schedule (T-28d/T-21d/T-14d/T-7d/T-2d/T-1d/T-1h/day_of/post_replay) with Pitfall 6 buffer (never dispatch when scheduled_at < now - 1h), and the attribution rollup against P222 opportunities + P225 attribution_touches (SOFT_UPSTREAM graceful degrade).

Purpose: satisfy SG-06 (event marketing as governed workflow), LOOP-06 (revenue modules connect campaign/content to CRM/pipeline evidence), LOOP-08 (measurement updates artifact performance + next-task recommendations), QA-01..03.

Output: 1 SOR migration + 6 events lib files + 3 contracts + flow-registry update + 1 preflight CLI + 7 events/RLS test files + 1 OpenAPI parity test.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-CONTEXT.md
@.planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-RESEARCH.md
@.planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-01-SUMMARY.md
@.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md
@.planning/REQUIREMENTS.md
@obsidian/brain/SaaS Marketing OS Strategy Canon.md
@obsidian/work/incoming/17-SAAS-MARKETING-OS-STRATEGY.md
@CLAUDE.md
@lib/markos/growth/preflight/index.cjs
@lib/markos/crm/agent-actions.ts
@onboarding/backend/runtime-context.cjs
@supabase/migrations/90_growth_referral_programs.sql
</context>

<tasks>

<task type="auto">
  <name>Task 0: Wave 2 entry preflight + slot 93 reservation verify</name>
  <files>scripts/preconditions/220-03-check-upstream.cjs</files>
  <read_first>
    - lib/markos/growth/preflight/upstream-gate.ts (Plan 01 output)
    - .planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md (verify P220-03 owns slot 93)
    - scripts/preconditions/220-01-check-upstream.cjs + 220-02-check-upstream.cjs (replicate pattern)
  </read_first>
  <action>
    Step 1: Verify Plan 01 outputs exist; verify slot 93 unallocated.
    Step 2: Create scripts/preconditions/220-03-check-upstream.cjs replicating prior plans; SOFT_UPSTREAM additions for P222 opportunities + P225 attribution_touches (these enable rollupEventAttribution but are not hard-required — RESEARCH §6 Domain 3 deferred external integrations).
    Step 3: Run CLI; exit 0 on green env.
  </action>
  <acceptance_criteria>
    - `node scripts/preconditions/220-03-check-upstream.cjs` exits 0
    - `grep -E "createApprovalPackage|requireSupabaseAuth|app/(growth)|route.ts|vitest" scripts/preconditions/220-03-check-upstream.cjs | wc -l` returns 0
    - `ls supabase/migrations/93_*.sql` returns empty (slot reserved, not yet claimed)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Events SOR migration 93 + DB-trigger event-promoting-requires-approval</name>
  <files>supabase/migrations/93_growth_events.sql, test/growth-220/events/events.test.js, test/growth-220/events/db-trigger-promoting-requires-approval.test.js, test/growth-220/rls/events-cross-tenant-denial.test.js</files>
  <behavior>
    - Test 1: After migration 93, `\d+ marketing_events` shows 15 cols; event_type CHECK 9 values; format CHECK 3; status CHECK 5; pipeline_target_cents bigint; attribution_window_days INT DEFAULT 90
    - Test 2: After migration 93, `\d+ event_reminders` shows 8 cols; window_kind CHECK 9 values; UNIQUE(event_id, send_at, window_kind)
    - Test 3: After migration 93, all 5 event tables RLS-enabled with tenant_id policy
    - Test 4: db-trigger — service-role UPDATE marketing_events SET status='promoting' WHERE OLD.status='planning' AND NEW.approved_at IS NULL → EXCEPTION 'EVENT_EXTERNAL_DISPATCH_REQUIRES_APPROVAL'
    - Test 5: db-trigger — UPDATE with approved_at set → succeeds
    - Test 6: db-trigger — UPDATE planning → planning (no transition) → no exception
    - Test 7: db-trigger — UPDATE promoting → live (already-approved transition) → no exception
    - Test 8: cross-tenant RLS denial across all 5 tables
  </behavior>
  <read_first>
    - 220-RESEARCH.md §6 Domain 3 (lines 487-617, especially Compliance enforcement boundary + Approval-gate triggers + Test strategy)
    - 220-RESEARCH.md §Pitfall 6 (event reminder dispatched after event ends)
    - obsidian/work/incoming/17-SAAS-MARKETING-OS-STRATEGY.md lines 996-1048 (MarketingEvent + EventPromotionPlan TS interfaces)
    - supabase/migrations/90_growth_referral_programs.sql + 92_growth_community.sql (RLS + comment header pattern)
  </read_first>
  <action>
    Step 1: Write tests 1-8 BEFORE migration (TDD RED).
    Step 2: Write supabase/migrations/93_growth_events.sql:
      - 5 CREATE TABLE statements per RESEARCH §6 Domain 3 SQL block
      - RLS on all 5 + tenant_id policy
      - event_speakers ON DELETE CASCADE FK to marketing_events
      - event_results UNIQUE event_id constraint
      - event_reminders UNIQUE(event_id, send_at, window_kind) prevents duplicate scheduling
      - idx_event_registrations_event ON (tenant_id, event_id)
      - fn_event_promoting_requires_approval trigger function + trg_event_promoting_requires_approval BEFORE UPDATE on marketing_events
      - DO $$ ASSERT 5 tables + trigger present END $$
      - Comment header: `-- Plan 220-03 / Phase 220 / Slot 93 / RESEARCH §6 Domain 3 / DB-trigger compliance + Pitfall 6 buffer note`
      - NO business_mode column (P227 owns ALTER)
    Step 3: Apply + run all 8 tests (TDD GREEN).
  </action>
  <acceptance_criteria>
    - `npm test -- test/growth-220/events/events.test.js test/growth-220/events/db-trigger-promoting-requires-approval.test.js test/growth-220/rls/events-cross-tenant-denial.test.js` all green (8+ assertions)
    - `psql -c "SELECT count(*) FROM information_schema.tables WHERE table_name IN ('marketing_events','event_speakers','event_registrations','event_reminders','event_results')"` returns 5
    - `psql -c "SELECT count(*) FROM pg_trigger WHERE tgname = 'trg_event_promoting_requires_approval'"` returns 1
    - `grep -E "business_mode" supabase/migrations/93_growth_events.sql` returns 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Events lib (promotion-plan + reminders + attribution + approval-gates) + 3 contracts</name>
  <files>lib/markos/events/contracts.ts, lib/markos/events/contracts.cjs, lib/markos/events/events.ts, lib/markos/events/events.cjs, lib/markos/events/promotion-plan.ts, lib/markos/events/reminders.ts, lib/markos/events/attribution.ts, lib/markos/events/approval-gates.ts, lib/markos/events/index.cjs, contracts/F-215-growth-event-v1.yaml, contracts/F-216-growth-event-registration-v1.yaml, contracts/F-217-growth-event-results-v1.yaml, contracts/flow-registry.json, test/growth-220/events/promotion-plan.test.js, test/growth-220/events/reminders.test.js, test/growth-220/events/attribution.test.js, test/growth-220/events/approval-gates.test.js, test/growth-220/openapi/parity-events.test.js</files>
  <behavior>
    - Test 1 (promotion-plan): generatePromotionPlan({ event_type: 'webinar', scheduled_at, channels: ['email','social','community'] }) returns EventPromotionPlan jsonb with announcement_email + reminder_cadence + social_posts + community_post arrays
    - Test 2 (promotion-plan): event_type='product_launch_event' generates extra channels (PR + analyst_brief) per doc 17 line 1010+
    - Test 3 (reminders): scheduleReminders({ event_id, scheduled_at: 2026-05-01 14:00 UTC, window_kinds: ['T-7d','T-1d','T-1h'] }) inserts 3 rows with correct send_at (2026-04-24 14:00 UTC, 2026-04-30 14:00 UTC, 2026-05-01 13:00 UTC)
    - Test 4 (reminders): scheduleReminders idempotent — second call with same args is no-op (UNIQUE(event_id, send_at, window_kind))
    - Test 5 (reminders): dispatchPendingReminders({ now: 2026-05-01 13:30 UTC }) for event scheduled 14:00 UTC dispatches T-1h reminder; updates dispatched_at + status='dispatched'
    - Test 6 (reminders): dispatchPendingReminders({ now: 2026-05-01 14:30 UTC }) for event that ended at 14:00+60min skips dispatch (scheduled_at + duration < now - 1h buffer per Pitfall 6); status stays 'pending' AND post-event monitoring updates status='canceled' if applicable
    - Test 7 (attribution): rollupEventAttribution({ event_id, lookback_days: 90 }) JOINs event_registrations.contact_id → opportunities created within window; sums opportunity.amount_cents → pipeline_sourced_cents; assist via attribution_touches if P225 SOFT_UPSTREAM landed; gracefully returns pipeline_influenced_cents=null if P225 missing
    - Test 8 (approval-gates): requestEventActivation({ event_id, initiated_by }) calls buildApprovalPackage with kind='event_activation' + payload includes promotion_plan_jsonb
    - Test 9 (approval-gates): requestExternalInviteSend({ event_id, recipient_count, initiated_by }) calls buildApprovalPackage with kind='event_external_invite'
    - Test 10 (approval-gates): requestPostEventSequence({ event_id, sequence_kind: 'no_show'|'attendee', initiated_by }) calls buildApprovalPackage with kind='event_post_sequence'
    - Test 11 (openapi-parity): F-215 + F-216 + F-217 YAML schemas match lib/markos/events/contracts.ts zod
    - Test 12 (openapi-parity): flow-registry.json contains F-215 + F-216 + F-217 with no overlap against F-001..F-198 + F-209..F-214
  </behavior>
  <read_first>
    - 220-RESEARCH.md §6 Domain 3 (lines 487-617)
    - 220-RESEARCH.md §6 Domain 6 API surface table (rows for /v1/growth/events/*)
    - 220-RESEARCH.md §Pitfall 6 (event reminder dispatched after event ends — 1-hour buffer)
    - obsidian/work/incoming/17-SAAS-MARKETING-OS-STRATEGY.md lines 996-1048 (TS interfaces)
    - lib/markos/referral/* (Plan 01 lib pattern — same import shape + CJS twin pattern)
    - lib/markos/community/* (Plan 02 lib pattern)
  </read_first>
  <action>
    Step 1: Write tests 1-12 BEFORE implementations (TDD RED).
    Step 2: Implement lib/markos/events/contracts.ts:
      - 5 enum constants per behavior list
      - zod schemas matching migration 93 columns exactly
      - TS types via z.infer
      - CJS twin re-exports constants
    Step 3: Implement lib/markos/events/promotion-plan.ts:
      - generatePromotionPlan({ event_type, scheduled_at, channels[] }) → returns EventPromotionPlan structured per doc 17 line 1010-1048
      - EVENT_TYPE_PROMOTION_TEMPLATES = { 'webinar': [...], 'product_launch_event': [...], ... }
    Step 4: Implement lib/markos/events/reminders.ts:
      - computeReminderTimes(scheduled_at, window_kinds[]) → array of { window_kind, send_at }
      - scheduleReminders inserts rows with INSERT ... ON CONFLICT (event_id, send_at, window_kind) DO NOTHING (idempotent)
      - dispatchPendingReminders WHERE send_at <= now AND status='pending' AND EXISTS (SELECT 1 FROM marketing_events WHERE event_id = event_reminders.event_id AND scheduled_at > now() - INTERVAL '1 hour') (Pitfall 6 buffer)
    Step 5: Implement lib/markos/events/attribution.ts:
      - rollupEventAttribution({ event_id, lookback_days }) — gracefully degrades when P222 opportunities table missing (returns nulls + warning)
      - Same for P225 attribution_touches missing → pipeline_influenced_cents = null
      - UPDATE event_results SET pipeline_sourced_cents/pipeline_influenced_cents/computed_at
    Step 6: Implement lib/markos/events/approval-gates.ts wrapping buildApprovalPackage; 3 functions per behavior list.
    Step 7: Implement lib/markos/events/events.ts CRUD; CJS twins; index.cjs re-exports.
    Step 8: Write F-215 + F-216 + F-217 contract YAMLs.
    Step 9: Update flow-registry.json appending entries.
    Step 10: Run all tests (TDD GREEN). Run npm run openapi:build.
  </action>
  <acceptance_criteria>
    - `npm test -- test/growth-220/events/` all green (12+ assertions)
    - `npm test -- test/growth-220/openapi/parity-events.test.js` green
    - `grep "F-215\|F-216\|F-217" contracts/flow-registry.json | wc -l` returns 3
    - `grep -rn "createApprovalPackage|requireSupabaseAuth|requireTenantContext|app/(growth)|route.ts|vitest|.test.ts" lib/markos/events/ | wc -l` returns 0
    - `node -e "console.log(typeof require('./lib/markos/events/index.cjs').generatePromotionPlan)"` outputs `function`
    - `npm run openapi:build` regenerates openapi.json with /v1/growth/events/* paths
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → POST /v1/growth/events activate | Untrusted; buildApprovalPackage required for status transitions |
| Cron worker → dispatchPendingReminders | Trusted but DB-trigger still enforces approval gate on status transitions |
| Service-role direct UPDATE on marketing_events | DB-trigger blocks promoting/live without approved_at |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-220-03-01 | Tampering | Service-role direct activation without approval | mitigate | DB-trigger fn_event_promoting_requires_approval; tested |
| T-220-03-02 | Tampering | Reminder dispatched after event ends (Pitfall 6) | mitigate | dispatchPendingReminders uses 1-hour buffer + scheduled_at filter; tested |
| T-220-03-03 | Tampering | Duplicate reminder schedule | mitigate | UNIQUE(event_id, send_at, window_kind) + ON CONFLICT DO NOTHING |
| T-220-03-04 | Information Disclosure | Cross-tenant event_registrations leakage (PII in email) | mitigate | RLS on tenant_id; tested |
| T-220-03-05 | Repudiation | Operator denies sending external invites | mitigate | buildApprovalPackage + audit log |
| T-220-03-06 | Tampering | Attribution miscount when P222 missing | accept | Graceful degrade returns null pipeline_sourced_cents; warning logged; not a security threat |
| T-220-03-07 | Information Disclosure | utm_jsonb may contain referrer URLs with tokens | accept | utm fields are tenant-internal; cleartext acceptable per existing tracking pattern |
</threat_model>

<verification>
- `npm test -- test/growth-220/events/ test/growth-220/rls/events-* test/growth-220/openapi/parity-events.test.js` all green
- `node scripts/preconditions/220-03-check-upstream.cjs` exits 0
- `psql -c "SELECT count(*) FROM information_schema.tables WHERE table_name IN ('marketing_events','event_speakers','event_registrations','event_reminders','event_results')"` returns 5
- `grep -rn "createApprovalPackage|requireSupabaseAuth|app/(growth)|route.ts|vitest" lib/markos/events/ scripts/preconditions/220-03-check-upstream.cjs | wc -l` returns 0
- `grep "F-215\|F-216\|F-217" contracts/flow-registry.json | wc -l` returns 3
</verification>

<success_criteria>
- 5 event SOR tables ship with RLS + 1 DB-trigger + UNIQUE indexes
- 9-window reminder taxonomy locked + Pitfall 6 buffer enforced
- Auto-generated EventPromotionPlan per doc 17 doctrine
- Attribution rollup gracefully degrades on missing P222/P225 (SOFT_UPSTREAM)
- 3 F-IDs ship F-215..F-217; flow-registry no overlap; openapi.json regenerated
- Domain 3 architecture-lock PASS — 0 forbidden patterns
</success_criteria>

<output>
After completion, create `.planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-03-SUMMARY.md` documenting:
- Migration slot claimed: 93
- F-IDs claimed: F-215, F-216, F-217
- Tables created: marketing_events, event_speakers, event_registrations, event_reminders, event_results
- Trigger created: trg_event_promoting_requires_approval (BEFORE UPDATE on marketing_events)
- 9-window reminder taxonomy + Pitfall 6 buffer (no late dispatch)
- Auto-generated EventPromotionPlan structure
- Architecture-lock PASS
</output>
