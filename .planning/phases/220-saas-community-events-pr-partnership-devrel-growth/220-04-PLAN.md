---
phase: 220
plan: "04"
type: execute
wave: 3
depends_on:
  - "220-01"
  - "220-02"
  - "220-03"
files_modified:
  - supabase/migrations/94_growth_pr.sql
  - lib/markos/pr/contracts.ts
  - lib/markos/pr/contracts.cjs
  - lib/markos/pr/outreach.ts
  - lib/markos/pr/outreach.cjs
  - lib/markos/pr/reviews.ts
  - lib/markos/pr/coverage-tracking.ts
  - lib/markos/pr/evidence-binding.ts
  - lib/markos/pr/index.cjs
  - contracts/F-218-growth-pr-target-v1.yaml
  - contracts/F-219-growth-pr-outreach-v1.yaml
  - contracts/F-220-growth-pr-review-v1.yaml
  - contracts/F-221-growth-pr-review-request-v1.yaml
  - contracts/flow-registry.json
  - scripts/preconditions/220-04-check-upstream.cjs
  - test/growth-220/pr/outreach.test.js
  - test/growth-220/pr/reviews.test.js
  - test/growth-220/pr/review-requests.test.js
  - test/growth-220/pr/coverage-tracking.test.js
  - test/growth-220/pr/evidence-binding.test.js
  - test/growth-220/pr/db-trigger-pitch-requires-evidence.test.js
  - test/growth-220/pr/db-trigger-pitch-requires-approval.test.js
  - test/growth-220/pr/db-trigger-g2-pricing-context.test.js
  - test/growth-220/rls/pr-cross-tenant-denial.test.js
  - test/growth-220/openapi/parity-pr.test.js
autonomous: true
requirements:
  - SG-06
  - SG-11
  - SG-12
  - EVD-01
  - EVD-02
  - EVD-03
  - EVD-04
  - EVD-05
  - EVD-06
  - QA-01
  - QA-02
  - QA-03
must_haves:
  truths:
    - Wave 3 entry — assertUpstreamReady preflight (scripts/preconditions/220-04-check-upstream.cjs); requires P214/P215/P218 hard; SOFT P209 evidence_map_records (PR pitch evidence_refs validation degrades to cardinality-only when P209 absent per Q-5 resolution)
    - Migration 94 ships per V4.1.0-MIGRATION-SLOT-COORDINATION.md; CONTAINS 5 SOR tables (pr_targets, pr_outreach, review_records, review_requests, coverage_mentions) + 3 DB-trigger functions (pitch-requires-approval, pitch-requires-evidence, g2-pricing-context)
    - DB-trigger compliance per RESEARCH §6 Domain 4 — fn_pr_outreach_requires_approval BEFORE UPDATE on pr_outreach raises EXCEPTION 'PR_OUTREACH_REQUIRES_APPROVAL' when status='sent' AND approved_at IS NULL; fn_pr_pitch_requires_evidence BEFORE INSERT/UPDATE raises 'PR_PITCH_REQUIRES_EVIDENCE' when status IN ('approved','sent') AND cardinality(evidence_refs) = 0 (defense-in-depth EVD-02); fn_review_request_requires_pricing_context on G2/Capterra pricing-sync requests raises when pricing_engine_context_id IS NULL
    - 4 target_type ENUM (journalist/analyst/influencer/review_publication) + 8 review platforms (g2/capterra/trustradius/trustpilot/product_hunt/app_store/play_store/custom) + 5 trigger_kind (nps_high/csat_high/health_score_high/feature_request_shipped/manual) + 6 coverage mention_kinds + 6 relationship_stage values
    - approval-gate wiring — outreach.ts.requestPitchSend, reviews.ts.requestReviewResponse + requestPricingProfileSync, review-requests.ts.requestReviewRequestSend ALL call buildApprovalPackage with kind values pr_pitch_send/pr_review_response/pr_pricing_profile_sync/pr_review_request_send
    - evidence-binding.ts — public claim → EvidenceMap freshness check; soft-FK to P209 evidence_map_records (Q-5 SOFT_UPSTREAM); v1 enforces cardinality-only via DB-trigger; FK validation upgrades to hard once P209 ships (follow-up migration)
    - 4 contracts ship F-218..F-221; flow-registry + openapi regen
    - Architecture-lock PASS on lib/markos/pr/* + scripts/preconditions/220-04-check-upstream.cjs
    - Cross-tenant RLS denial proven across all 5 PR SOR tables
  artifacts:
    - path: supabase/migrations/94_growth_pr.sql
      provides: pr_targets (15 cols — target_id PK + tenant_id FK + target_type CHECK 4 values + name + publication + email + twitter_handle + linkedin_url + beats[] + recent_articles_jsonb + relevance_score + last_contacted_at + relationship_stage CHECK 6 values cold/warming/engaged/responsive/active/do_not_contact + do_not_contact_reason + source_quality_score + created_at) + pr_outreach (16 cols — outreach_id PK + target_id FK + story_angle + pitch_subject + pitch_body + evidence_refs uuid[] (soft-FK to P209) + approval_id + approved_at + approved_by + sent_at + status CHECK 7 values + response_received_at + coverage_url + coverage_kind CHECK 6 values + pipeline_attribution_window_days DEFAULT 90) + review_records (15 cols — review_id PK + platform CHECK 8 values + external_review_id + review_url + reviewer_name + star_rating CHECK 0-5 + sentiment_score + themes[] + response_status CHECK 5 values + response_draft + response_approval_id + responded_at + responded_by) + review_requests (12 cols — request_id PK + contact_id soft-FK + platform + trigger_kind CHECK 5 values + request_template_id + pricing_engine_context_id + approval_id + status CHECK 6 values) + coverage_mentions (10 cols — mention_id PK + source_publication + source_url NOT NULL + mention_kind CHECK 6 values + sentiment_score + brand_awareness_value CHECK 0-100 + related_outreach_id FK to pr_outreach) + RLS on all 5 + 3 DB-trigger functions
    - path: lib/markos/pr/contracts.ts
      provides: 9 enum constants (PR_TARGET_TYPES, PR_RELATIONSHIP_STAGES, PR_OUTREACH_STATUSES, PR_COVERAGE_KINDS, REVIEW_PLATFORMS, REVIEW_RESPONSE_STATUSES, REVIEW_REQUEST_TRIGGER_KINDS, REVIEW_REQUEST_STATUSES, COVERAGE_MENTION_KINDS) + zod schemas
      exports: [...]
    - path: lib/markos/pr/outreach.ts
      provides: PR target CRUD + pitch generation + requestPitchSend (calls buildApprovalPackage with kind='pr_pitch_send'); evidence_refs validation (cardinality > 0 + sliding-freshness check via evidence-binding)
      exports: [createPrTarget, updatePrTarget, listPrTargets, draftPitch, requestPitchSend, sendApprovedPitch]
    - path: lib/markos/pr/reviews.ts
      provides: review_records ingest + response approval flow; requestReviewResponse (kind='pr_review_response'); requestPricingProfileSync (kind='pr_pricing_profile_sync' + Pricing Engine guard SG-11); sentiment classifier integration (LLM via lib/markos/llm/* — RESEARCH §Library Choices option a)
      exports: [ingestReview, draftResponse, requestReviewResponse, requestPricingProfileSync, scoreSentiment]
    - path: lib/markos/pr/coverage-tracking.ts
      provides: coverage_mentions ingest from Google Alerts/NewsAPI (RES-03 substrate); sentiment scoring via LLM; brand_awareness_value rollup; CRM brand-awareness touchpoint emit
      exports: [ingestCoverageMention, rollupBrandAwareness, scoreCoverageSentiment]
    - path: lib/markos/pr/evidence-binding.ts
      provides: validateEvidenceRefs({ evidence_refs[] }) — checks cardinality > 0 + (when P209 SOFT_UPSTREAM landed) FK existence + freshness; returns { valid: bool, reasons: string[] }
      exports: [validateEvidenceRefs, EvidenceValidation]
    - path: contracts/F-218-growth-pr-target-v1.yaml
      provides: GET/POST/PATCH /v1/growth/pr/targets
    - path: contracts/F-219-growth-pr-outreach-v1.yaml
      provides: GET/POST/PATCH /v1/growth/pr/outreach + send-with-approval-gate path
    - path: contracts/F-220-growth-pr-review-v1.yaml
      provides: GET/PATCH /v1/growth/pr/reviews + response-with-approval-gate path
    - path: contracts/F-221-growth-pr-review-request-v1.yaml
      provides: GET/POST /v1/growth/pr/review-requests + send-with-approval-gate path
    - path: scripts/preconditions/220-04-check-upstream.cjs
      provides: CLI preflight; SOFT_UPSTREAM includes P209 evidence_map_records (Q-5 graceful degrade)
    - path: test/growth-220/pr/db-trigger-pitch-requires-evidence.test.js
      provides: service-role direct UPDATE pr_outreach SET status='sent' WHERE evidence_refs='{}' (empty) → expect EXCEPTION 'PR_PITCH_REQUIRES_EVIDENCE'; with non-empty evidence_refs → succeeds (Pitfall 3 fix)
    - path: test/growth-220/pr/db-trigger-pitch-requires-approval.test.js
      provides: service-role direct UPDATE pr_outreach SET status='sent' AND approved_at IS NULL → expect EXCEPTION 'PR_OUTREACH_REQUIRES_APPROVAL'
    - path: test/growth-220/pr/db-trigger-g2-pricing-context.test.js
      provides: INSERT review_requests with platform='g2' AND trigger_kind='manual' AND pricing_engine_context_id IS NULL when this is a pricing-profile-sync request → expect EXCEPTION (Pitfall 4 fix; SG-11 hard floor)
  key_links:
    - from: lib/markos/pr/outreach.ts
      to: lib/markos/crm/agent-actions.ts
      via: const { buildApprovalPackage } = require('../crm/agent-actions') — kinds pr_pitch_send/pr_review_response/pr_pricing_profile_sync/pr_review_request_send
      pattern: "buildApprovalPackage"
    - from: lib/markos/pr/evidence-binding.ts
      to: supabase/migrations/94_growth_pr.sql
      via: validateEvidenceRefs cardinality > 0 mirrors fn_pr_pitch_requires_evidence DB-trigger condition; defense-in-depth pair
      pattern: "evidence_refs"
    - from: lib/markos/pr/reviews.ts
      to: lib/markos/llm
      via: scoreSentiment uses existing LLM adapter (RESEARCH §Library Choices option a — Anthropic/OpenAI/Gemini)
      pattern: "lib/markos/llm"
---

<objective>
Wave 3 plan A (parallel with Plan 05) — translate PR + analyst + review intelligence into governed workflow per RESEARCH §6 Domain 4. Ship pr_targets + pr_outreach + review_records + review_requests + coverage_mentions SOR (migration 94), 3 DB-trigger compliance functions (pitch-requires-approval, pitch-requires-evidence Pitfall 3, g2-pricing-context Pitfall 4), the LLM-backed sentiment classifier (RESEARCH §Library Choices option a — existing adapter), the evidence-binding cardinality check (Q-5 SOFT enforcement; FK upgrade once P209 ships), and the buildApprovalPackage wiring on every external-mutation surface (canon line 144).

Purpose: satisfy SG-06 (PR/analyst/review as governed workflow), SG-11 (G2/Capterra pricing profile sync MUST consume Pricing Engine context), SG-12 (every external mutation approval-gated), EVD-01..06 (evidence linked to factual claims; unsupported claims block external dispatch; freshness; source quality; pricing freshness), QA-01..03.

Output: 1 SOR migration + 5 PR lib files + 4 contracts + flow-registry update + 1 preflight CLI + 8 PR/RLS test files + 1 OpenAPI parity test.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-CONTEXT.md
@.planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-RESEARCH.md
@.planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-01-SUMMARY.md
@.planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-02-SUMMARY.md
@.planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-03-SUMMARY.md
@.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md
@.planning/REQUIREMENTS.md
@obsidian/brain/SaaS Marketing OS Strategy Canon.md
@obsidian/work/incoming/17-SAAS-MARKETING-OS-STRATEGY.md
@CLAUDE.md
@lib/markos/growth/preflight/index.cjs
@lib/markos/crm/agent-actions.ts
@onboarding/backend/runtime-context.cjs
</context>

<tasks>

<task type="auto">
  <name>Task 0: Wave 3 entry preflight + slot 94 reservation verify</name>
  <files>scripts/preconditions/220-04-check-upstream.cjs</files>
  <read_first>
    - lib/markos/growth/preflight/upstream-gate.ts (Plan 01 output)
    - .planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md (verify P220-04 owns slot 94)
    - 220-RESEARCH.md §Q-5 (P209 EvidenceMap availability — SOFT_UPSTREAM degrade)
    - scripts/preconditions/220-01-check-upstream.cjs (replicate pattern)
  </read_first>
  <action>
    Step 1: Verify Plans 01-03 outputs exist; verify slot 94 unallocated.
    Step 2: Create scripts/preconditions/220-04-check-upstream.cjs; SOFT_UPSTREAM additions for P209 evidence_map_records + RES-03 NewsAPI/Google Alerts substrate (graceful degrade — coverage-tracking falls back to manual entry).
    Step 3: Run CLI; exit 0 on green env.
  </action>
  <acceptance_criteria>
    - `node scripts/preconditions/220-04-check-upstream.cjs` exits 0
    - `grep -E "createApprovalPackage|requireSupabaseAuth|app/(growth)|route.ts|vitest" scripts/preconditions/220-04-check-upstream.cjs | wc -l` returns 0
    - `ls supabase/migrations/94_*.sql` returns empty (slot reserved)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 1: PR SOR migration 94 + 3 DB-trigger compliance functions</name>
  <files>supabase/migrations/94_growth_pr.sql, test/growth-220/pr/outreach.test.js, test/growth-220/pr/db-trigger-pitch-requires-approval.test.js, test/growth-220/pr/db-trigger-pitch-requires-evidence.test.js, test/growth-220/pr/db-trigger-g2-pricing-context.test.js, test/growth-220/rls/pr-cross-tenant-denial.test.js</files>
  <behavior>
    - Test 1: After migration 94, all 5 PR tables RLS-enabled with tenant_id policy + correct CHECK constraints (4/8/5/6 ENUM value sets); pr_targets relationship_stage 6 values; pr_outreach status 7 values; review_records platform 8 values; review_requests trigger_kind 5 values; coverage_mentions mention_kind 6 values
    - Test 2: pr_outreach.evidence_refs is uuid[] type (NOT NULL, default ARRAY[]::uuid[]); coverage_mentions.related_outreach_id FK to pr_outreach
    - Test 3: db-trigger pitch-requires-approval — service-role direct UPDATE pr_outreach SET status='sent' WHERE approved_at IS NULL → EXCEPTION 'PR_OUTREACH_REQUIRES_APPROVAL'
    - Test 4: db-trigger pitch-requires-evidence — service-role UPDATE pr_outreach SET status='sent' WHERE cardinality(evidence_refs)=0 → EXCEPTION 'PR_PITCH_REQUIRES_EVIDENCE'; with evidence_refs not empty → succeeds (Pitfall 3 fix; defense-in-depth EVD-02)
    - Test 5: db-trigger pitch-requires-evidence — INSERT pr_outreach with status='approved' AND evidence_refs='{}' → EXCEPTION (covers approval transition, not just send)
    - Test 6: db-trigger g2-pricing-context — INSERT review_requests with trigger_kind='manual' AND platform='g2' AND request kind='pricing_profile_sync' AND pricing_engine_context_id IS NULL → EXCEPTION 'G2_PRICING_PROFILE_REQUIRES_PRICING_CONTEXT' (SG-11 hard floor; Pitfall 4 fix)
    - Test 7: cross-tenant RLS denial across all 5 tables
  </behavior>
  <read_first>
    - 220-RESEARCH.md §6 Domain 4 (lines 620-753, especially Compliance enforcement boundary + Pitfall 3 + Pitfall 4)
    - 220-RESEARCH.md §Q-5 (P209 SOFT_UPSTREAM degrade — cardinality-only validation in v1)
    - obsidian/work/incoming/17-SAAS-MARKETING-OS-STRATEGY.md lines 1075-1132 (G2/PR TS interfaces)
    - supabase/migrations/93_growth_events.sql (Plan 03 pattern)
  </read_first>
  <action>
    Step 1: Write tests 1-7 BEFORE migration (TDD RED).
    Step 2: Write supabase/migrations/94_growth_pr.sql:
      - 5 CREATE TABLE statements per RESEARCH §6 Domain 4 SQL block
      - RLS on all 5 + tenant_id policy
      - pr_outreach.evidence_refs uuid[] NOT NULL DEFAULT ARRAY[]::uuid[] (cardinality check needs non-null)
      - 3 DB-trigger functions:
        - fn_pr_outreach_requires_approval BEFORE UPDATE: when NEW.status='sent' AND NEW.approved_at IS NULL → RAISE EXCEPTION 'PR_OUTREACH_REQUIRES_APPROVAL'
        - fn_pr_pitch_requires_evidence BEFORE INSERT OR UPDATE: when NEW.status IN ('approved','sent') AND cardinality(NEW.evidence_refs) = 0 → RAISE EXCEPTION 'PR_PITCH_REQUIRES_EVIDENCE' (defense-in-depth EVD-02)
        - fn_review_request_g2_pricing_context BEFORE INSERT OR UPDATE on review_requests: when NEW.platform IN ('g2','capterra') AND request_template_id IN (SELECT template_id FROM g2_pricing_sync_templates) AND NEW.pricing_engine_context_id IS NULL → RAISE EXCEPTION 'G2_PRICING_PROFILE_REQUIRES_PRICING_CONTEXT' (SG-11; Pitfall 4) — NOTE: simplified for v1 — checks NEW.platform IN ('g2','capterra') AND NEW.trigger_kind IN ('manual') AND a marker column pricing_sync_required=true → defer template_id lookup to follow-up
      - Indexes on pr_outreach(tenant_id, target_id), pr_outreach(tenant_id, status), review_records(tenant_id, platform, response_status), coverage_mentions(tenant_id, ingested_at)
      - DO $$ ASSERT 5 tables + 3 triggers present END $$
      - Comment header: `-- Plan 220-04 / Phase 220 / Slot 94 / RESEARCH §6 Domain 4 / DB-triggers covering Pitfall 3 + 4 + SG-11/12 + EVD-02`
    Step 3: Apply + run all 7 tests (TDD GREEN).
  </action>
  <acceptance_criteria>
    - `npm test -- test/growth-220/pr/db-trigger-* test/growth-220/rls/pr-cross-tenant-denial.test.js` all green (7+ assertions)
    - `psql -c "SELECT count(*) FROM information_schema.tables WHERE table_name IN ('pr_targets','pr_outreach','review_records','review_requests','coverage_mentions')"` returns 5
    - `psql -c "SELECT count(*) FROM pg_trigger WHERE tgname IN ('trg_pr_outreach_requires_approval','trg_pr_pitch_requires_evidence','trg_review_request_g2_pricing_context')"` returns 3
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PR lib (outreach + reviews + coverage-tracking + evidence-binding) + 4 contracts</name>
  <files>lib/markos/pr/contracts.ts, lib/markos/pr/contracts.cjs, lib/markos/pr/outreach.ts, lib/markos/pr/outreach.cjs, lib/markos/pr/reviews.ts, lib/markos/pr/coverage-tracking.ts, lib/markos/pr/evidence-binding.ts, lib/markos/pr/index.cjs, contracts/F-218-growth-pr-target-v1.yaml, contracts/F-219-growth-pr-outreach-v1.yaml, contracts/F-220-growth-pr-review-v1.yaml, contracts/F-221-growth-pr-review-request-v1.yaml, contracts/flow-registry.json, test/growth-220/pr/reviews.test.js, test/growth-220/pr/review-requests.test.js, test/growth-220/pr/coverage-tracking.test.js, test/growth-220/pr/evidence-binding.test.js, test/growth-220/openapi/parity-pr.test.js</files>
  <behavior>
    - Test 1 (outreach): requestPitchSend({ outreach_id, evidence_refs[], initiated_by }) calls buildApprovalPackage with kind='pr_pitch_send'; evidence-binding pre-validates cardinality > 0
    - Test 2 (outreach): requestPitchSend rejects with reason='evidence_required' when evidence_refs empty (app-layer EVD-02 + DB-trigger defense-in-depth)
    - Test 3 (reviews): scoreSentiment({ review_body }) returns numeric -1.0 to 1.0; uses LLM adapter (lib/markos/llm); cost-aware via existing cost-calculator
    - Test 4 (reviews): requestReviewResponse({ review_id, response_draft, initiated_by }) calls buildApprovalPackage with kind='pr_review_response'
    - Test 5 (reviews): requestPricingProfileSync({ platform: 'g2', pricing_engine_context_id, initiated_by }) calls buildApprovalPackage with kind='pr_pricing_profile_sync'; throws if pricing_engine_context_id missing (SG-11 app-layer guard)
    - Test 6 (review-requests): trigger_kind eligibility — eligibility check 'nps_high' validates contact has nps_score >= 9 within last 7d (P222 SOFT lookup); if P222 missing returns 'manual' fallback
    - Test 7 (review-requests): requestReviewRequestSend({ contact_id, platform, trigger_kind, initiated_by }) calls buildApprovalPackage with kind='pr_review_request_send'
    - Test 8 (coverage-tracking): ingestCoverageMention({ source_url, source_publication, mention_kind, title, snippet }) inserts coverage_mentions row + scores sentiment via LLM; rolls into brand_awareness_value
    - Test 9 (coverage-tracking): rollupBrandAwareness({ tenant_id, period }) sums coverage_mentions.brand_awareness_value WHERE ingested_at in period; emits CRM brand-awareness touchpoint via lib/markos/crm
    - Test 10 (evidence-binding): validateEvidenceRefs({ evidence_refs: [] }) returns { valid: false, reasons: ['cardinality_zero'] }
    - Test 11 (evidence-binding): validateEvidenceRefs({ evidence_refs: ['uuid-1','uuid-2'] }) returns { valid: true } (v1 cardinality-only)
    - Test 12 (evidence-binding): when P209 SOFT_UPSTREAM landed, validateEvidenceRefs cross-checks evidence_map_records existence + freshness_status='fresh' (Q-5 hard upgrade)
    - Test 13 (openapi-parity): F-218..F-221 YAML schemas match lib/markos/pr/contracts.ts zod
    - Test 14 (openapi-parity): flow-registry.json contains F-218..F-221 with no overlap against F-001..F-198 + F-209..F-217
  </behavior>
  <read_first>
    - 220-RESEARCH.md §6 Domain 4 (lines 620-753, full)
    - 220-RESEARCH.md §Library Choices (option a — LLM adapter for sentiment)
    - 220-RESEARCH.md §Pitfall 3 + §Pitfall 4 + §Q-5
    - lib/markos/llm/* (verify existing adapter + cost-calculator pattern)
    - lib/markos/referral/* + lib/markos/community/* + lib/markos/events/* (Plans 01-03 lib pattern)
    - obsidian/work/incoming/17-SAAS-MARKETING-OS-STRATEGY.md lines 1075-1132 (TS interfaces)
  </read_first>
  <action>
    Step 1: Write tests 1-14 BEFORE implementations (TDD RED).
    Step 2: Implement lib/markos/pr/contracts.ts — 9 enum constants + zod schemas + TS types + CJS twin.
    Step 3: Implement lib/markos/pr/evidence-binding.ts:
      - validateEvidenceRefs({ evidence_refs[] }) — v1 cardinality-only check
      - When SOFT_UPSTREAM P209 evidence_map_records present (detected via assertUpstreamReady warnings), upgrade to FK existence + freshness_status='fresh' validation
    Step 4: Implement lib/markos/pr/outreach.ts:
      - createPrTarget, updatePrTarget, listPrTargets CRUD
      - draftPitch({ target_id, story_angle, evidence_refs }) → returns pitch_subject + pitch_body via LLM adapter
      - requestPitchSend({ outreach_id, evidence_refs, initiated_by }):
        - validateEvidenceRefs first → throw if invalid (EVD-02 app-layer)
        - buildApprovalPackage kind='pr_pitch_send' + payload includes evidence_refs
        - return { approval_id }
      - sendApprovedPitch executes after approval → calls outbound (lib/markos/outbound) + UPDATE pr_outreach SET status='sent' + sent_at=now() (DB-trigger validates approved_at + evidence_refs presence)
    Step 5: Implement lib/markos/pr/reviews.ts:
      - ingestReview, scoreSentiment (LLM adapter — cap review_body length to prevent prompt injection T-220-04 mitigation), draftResponse
      - requestReviewResponse, requestPricingProfileSync (with SG-11 guard)
    Step 6: Implement lib/markos/pr/coverage-tracking.ts:
      - ingestCoverageMention with sentiment scoring + brand_awareness_value computation
      - rollupBrandAwareness aggregating brand_awareness_value per period
    Step 7: CJS twins + index.cjs.
    Step 8: Write F-218..F-221 contract YAMLs.
    Step 9: Update flow-registry.json.
    Step 10: Run all tests (TDD GREEN). npm run openapi:build.
  </action>
  <acceptance_criteria>
    - `npm test -- test/growth-220/pr/` all green (14+ assertions)
    - `npm test -- test/growth-220/openapi/parity-pr.test.js` green
    - `grep "F-218\|F-219\|F-220\|F-221" contracts/flow-registry.json | wc -l` returns 4
    - `grep -rn "createApprovalPackage|requireSupabaseAuth|app/(growth)|route.ts|vitest" lib/markos/pr/ | wc -l` returns 0
    - `node -e "console.log(typeof require('./lib/markos/pr/index.cjs').requestPitchSend)"` outputs `function`
    - `npm run openapi:build` regenerates openapi.json with /v1/growth/pr/* paths
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → POST /v1/growth/pr/outreach send | Untrusted; buildApprovalPackage + evidence-binding required |
| Service-role direct UPDATE on pr_outreach | DB-trigger blocks send without approval + evidence (Pitfall 3 fix) |
| External NewsAPI/Google Alerts ingest → coverage_mentions | Untrusted external; sentiment classifier sanitizes review_body before LLM call |
| LLM sentiment classifier prompt | Untrusted review_body input; length capped to prevent prompt injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-220-04-01 | Tampering | Service-role direct send without approval | mitigate | DB-trigger fn_pr_outreach_requires_approval |
| T-220-04-02 | Tampering | PR pitch sent without evidence (EVD-02 bypass) | mitigate | DB-trigger fn_pr_pitch_requires_evidence + app-layer validateEvidenceRefs (defense-in-depth) |
| T-220-04-03 | Tampering | G2 pricing profile drifts from Pricing Engine | mitigate | DB-trigger fn_review_request_g2_pricing_context (SG-11 hard floor); Pitfall 4 fix |
| T-220-04-04 | Information Disclosure | PII in pr_targets.email + review_records.reviewer_name leaked cross-tenant | mitigate | RLS on tenant_id; reviewer_name acceptable as public-facing |
| T-220-04-05 | Spoofing | Forged G2/Capterra ingest claim | accept | v1 manual operator entry only; native integration deferred to v4.3.0 |
| T-220-04-06 | Tampering | Sentiment classifier prompt injection via review_body | mitigate | Sanitize + cap length before LLM call; existing LLM adapter cost guards |
| T-220-04-07 | Repudiation | Operator denies sending PR pitch | mitigate | buildApprovalPackage approval_id + audit log |
| T-220-04-08 | Information Disclosure | evidence_refs leak EvidenceMap structure when P209 absent | accept | v1 cardinality-only; FK upgrade once P209 ships |
</threat_model>

<verification>
- `npm test -- test/growth-220/pr/ test/growth-220/rls/pr-* test/growth-220/openapi/parity-pr.test.js` all green
- `node scripts/preconditions/220-04-check-upstream.cjs` exits 0
- `psql -c "SELECT count(*) FROM information_schema.tables WHERE table_name IN ('pr_targets','pr_outreach','review_records','review_requests','coverage_mentions')"` returns 5
- `psql -c "SELECT count(*) FROM pg_trigger WHERE tgname LIKE 'trg_pr_%' OR tgname LIKE 'trg_review_%'"` returns >= 3
- `grep -rn "createApprovalPackage|requireSupabaseAuth|app/(growth)|route.ts|vitest" lib/markos/pr/ scripts/preconditions/220-04-check-upstream.cjs | wc -l` returns 0
- `grep "F-218\|F-219\|F-220\|F-221" contracts/flow-registry.json | wc -l` returns 4
</verification>

<success_criteria>
- 5 PR SOR tables ship with RLS + 3 DB-triggers (defense-in-depth Pitfall 3 + Pitfall 4 + SG-12)
- LLM-backed sentiment classifier integrated (cost-aware via existing adapter)
- Evidence-binding cardinality check + soft-upgrade path for P209 hard FK
- 4 F-IDs ship F-218..F-221; flow-registry no overlap; openapi.json regenerated
- Domain 4 architecture-lock PASS — 0 forbidden patterns
</success_criteria>

<output>
After completion, create `.planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-04-SUMMARY.md` documenting:
- Migration slot claimed: 94
- F-IDs claimed: F-218, F-219, F-220, F-221
- Tables created: pr_targets, pr_outreach, review_records, review_requests, coverage_mentions
- Triggers created: trg_pr_outreach_requires_approval, trg_pr_pitch_requires_evidence, trg_review_request_g2_pricing_context
- Q-5 resolution: v1 cardinality-only evidence check; FK upgrade once P209 ships
- LLM-backed sentiment classifier integration
- Architecture-lock PASS
</output>
