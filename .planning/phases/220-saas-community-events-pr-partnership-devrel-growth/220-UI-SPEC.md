---
phase: 220
slug: saas-community-events-pr-partnership-devrel-growth
status: draft
shadcn_initialized: false
preset: not-applicable-no-ui-phase
domain: saas-growth-substrate-referral-viral-loop-fraud-evaluator-community-8-signal-kind-taxonomy-moderation-event-9-window-reminder-promotion-plan-pr-analyst-review-llm-sentiment-evidence-binding-partnership-affiliate-developer-marketing-prohibited-methods-28-growth-agents-readiness-end-of-milestone-v4-1-0
created: 2026-05-04
canonical_visual_contract: /DESIGN.md
design_md_version: v1.1.0
mode: no-ui-surface-phase
ui_scope: zero-surface
plans_in_scope: [220-01, 220-02, 220-03, 220-04, 220-05, 220-06]
plans_with_ui_surfaces: []
plans_no_ui: [220-01, 220-02, 220-03, 220-04, 220-05, 220-06]
milestone_position: v4.1.0-FINAL-PHASE
parent_doctrine_chain:
  - 206-UI-SPEC.md (mutation-class doctrine — `external.send` for community moderation outreach (`community_moderation_action`), event reminder dispatch (`event_external_invite`, `event_post_sequence`), PR pitch send (`pr_pitch_send`), PR review response (`pr_review_response`), G2/Capterra review request send (`pr_review_request_send`), partner co-marketing outreach (`partnership_activation`), developer marketing dispatch; `billing.charge` for referral reward issuance (`referral_reward_issuance` — billing P215 HARD wired), affiliate commission issuance (`affiliate_commission_issuance` — billing P215 HARD wired), partner payout export (`partner_payout_export` — billing P215 HARD wired); `data.export` for partner payout CSV export, audit log export, coverage_mentions export; `default_approval_mode == single_approval` for `community_moderation_approval` + `event_promotion_approval` + `pr_pitch_approval` + `pr_review_response_approval` + `pr_review_request_approval` + `g2_pricing_profile_sync_approval` + `partnership_activation_approval` + `affiliate_program_activation_approval` + `affiliate_commission_issuance_approval` + `partner_payout_export_approval`; `default_approval_mode == dual_approval` for `partner_payout_export_approval` when payout_amount_cents > 100000 (mirrors 219 high-discount dual-approval pattern); autonomy-ceiling on `external.send` for all customer-facing community/event/PR/affiliate/developer outreach; autonomy-ceiling on `billing.charge` for all referral reward + affiliate commission + partner payout paths)
  - 207-UI-SPEC.md (`RunApiEnvelope`; `run_id` linked to viral-metrics-rollup cron (`growth-viral-metrics-rollup.js` daily) + event-reminder-dispatch cron (`growth-event-reminder-dispatch.js` hourly) + community-signals-fan-out cron (`growth-community-signals-fan-out.js` hourly) + pr-coverage-tracking cron (`growth-pr-coverage-tracking.js` daily 05:00 UTC) + review-request-cadence cron (`growth-review-request-cadence.js` daily 06:00 UTC) + developer-content-validation cron (`growth-developer-content-validation.js` weekly Sun 02:00 UTC) + fraud-evaluator runs + sentiment classifier runs + commission tracker runs + 28-agent readiness-flag flip runs; `AgentRunEventType` for `referral_invitation_recorded` / `referral_fraud_evaluated` / `referral_reward_issued` / `viral_loop_metrics_rolled_up` / `community_signal_ingested` / `community_signal_consumed` / `community_action_executed` / `community_moderation_dispatched` / `event_promotion_plan_generated` / `event_status_promoting_transitioned` / `event_reminder_scheduled` / `event_reminder_dispatched` / `event_attribution_rolled_up` / `pr_target_relationship_advanced` / `pr_outreach_drafted` / `pr_outreach_sent` / `pr_review_ingested` / `pr_review_response_drafted` / `pr_review_response_sent` / `pr_review_request_dispatched` / `pr_coverage_mention_ingested` / `pr_brand_awareness_rolled_up` / `partnership_activated` / `affiliate_program_activated` / `affiliate_commission_computed` / `affiliate_commission_locked` / `partner_payout_exported` / `developer_content_published` / `developer_content_freshness_checked` / `developer_content_pricing_drift_detected` / `prohibited_method_violation_detected` / `growth_agent_readiness_flag_flipped` / `growth_agent_activation_attempted`; `ApprovalHandoffRecord` links 220 community moderation + event activation + PR pitch + review response + review request + G2 pricing sync + partnership activation + affiliate program activation + affiliate commission issuance + partner payout export to P208 inbox; `agent_run_id` linked to all 6 P220 cron handlers + 19 API handler write paths + 12 read MCP tools + 3 write-gated MCP tools)
  - 208-UI-SPEC.md (PARENT — Approval Inbox at `/operations/approvals` extends with SEVEN new handoff_kind literals: `community_moderation_approval` (20th literal in canonical chain — 220-02 community_actions ban/mute/post_removal/post_external_brand; `lib/markos/community/moderation-approvals.ts` `requestModerationAction` calls `buildApprovalPackage` with `kind='community_moderation_action'`), `event_promotion_approval` (21st literal — 220-03 marketing_events status `planning`→`promoting` transition + external invitation send + post-event sales sequence; `lib/markos/events/approval-gates.ts` `requestEventActivation` / `requestExternalInviteSend` / `requestPostEventSequence` call `buildApprovalPackage` with kinds `event_activation` / `event_external_invite` / `event_post_sequence`), `pr_pitch_approval` (22nd literal — 220-04 pr_outreach status `draft`→`sent` transition with EVD-02 evidence_refs cardinality > 0 enforced via `fn_pr_pitch_requires_evidence` defense-in-depth + Pitfall 3 fix; `lib/markos/pr/outreach.ts` `requestPitchSend` calls `buildApprovalPackage` with `kind='pr_pitch_send'`), `g2_review_pricing_approval` (23rd literal — 220-04 review_requests with platform=`g2`/`capterra` + pricing-profile-sync template requires `pricing_engine_context_id` per `fn_review_request_g2_pricing_context` SG-11 hard floor + Pitfall 4 fix; `lib/markos/pr/reviews.ts` `requestPricingProfileSync` calls `buildApprovalPackage` with `kind='pr_pricing_profile_sync'`), `partnership_activation_approval` (24th literal — 220-05 partnerships status `proposed`→`active` transition; `lib/markos/partnerships/partnerships.ts` `requestPartnershipActivation` calls `buildApprovalPackage` with `kind='partnership_activation'`), `affiliate_commission_issuance_approval` (25th literal — 220-05 affiliate_commissions issuance with billing P215 HARD wired; `lib/markos/partnerships/payout-compliance.ts` `requestAffiliateCommissionIssuance` calls `buildApprovalPackage` with `kind='affiliate_commission_issuance'` + Pricing Engine context resolved + lib/markos/billing handoff via `createBillingHandoff`; reuses 215 billing-correction modal recipe), `partner_payout_export_approval` (26th literal — 220-05 partner_profiles payout export to CSV; `lib/markos/partnerships/payout-compliance.ts` `requestPartnerPayoutExport` calls `buildApprovalPackage` with `kind='partner_payout_export'` + payout_compliance_acknowledged_at gate forward-port from P227 D-71 + manual CSV export is FINAL formatter step on top of billing-engine output per Pitfall 2 fix); the legacy 19-chip set (post-219: 4 P207 literals + P214 `billing_charge_approval` + P215 `billing_correction_approval` + P216 `support_response_approval` + P216 `save_offer_approval` + 218's `pql_hot_transition_review` + `upgrade_trigger_activate` + `inapp_campaign_activate` + `experiment_activate` + 219's 8 chips `expansion_outreach_approval` + `program_activation_approval` + `abm_outreach_approval` + `review_request_dispatch_approval` + `proof_publish_approval` + `discount_authorization_approval` + `expansion_offer_approval` + `save_offer_approval`) extends to 26 chips after 220 (the prompt cited "extending 219's chain to 26th total" — confirmed: 19 + 7 = 26 handoff_kind literals end-of-v4.1.0); rendering of those seven new chips is deferred to a future P208 admin extension that displays per-row classifier output, fraud-evaluator risk indicators (5-rule taxonomy: minAccountAge / requirePaidAccount / selfReferralPrevention / vpnDetection / maxReferralsPerUser), 8-value community signal_kind taxonomy verbatim, 9-window event reminder schedule timeline, EventPromotionPlan auto-generated render, LLM sentiment classifier output (-1.0 to +1.0 with confidence), Pitfall 3 evidence-binding cardinality indicator (Q-5 SOFT FK upgrade), Pitfall 4 G2 pricing-context indicator, prohibited-methods violation indicator (paid_search_brand_terms / email_spam / trademark_misuse), Pitfall 8 developer marketing pricing-drift detector indicator, Pitfall 9 GENERATED runnable column verification, T0-04 Tenant 0 public-proof boundary indicator on PR pitches + G2 reviews, sentinel `{{MARKOS_PRICING_ENGINE_PENDING}}` acceptance indicator (SG-11 + Pitfall 4 + Pitfall 8))
  - 209-UI-SPEC.md (PARENT — Pitfall 3 PR pitch evidence-binding `evidence_refs uuid[]` cardinality check `cardinality(evidence_refs) > 0` enforced via `fn_pr_pitch_requires_evidence` defense-in-depth on `pr_outreach.status IN ('approved','sent')`; Q-5 SOFT_UPSTREAM degrade — v1 enforces cardinality-only when P209 evidence_map_records absent; FK upgrade to evidence_map_records + freshness_status='fresh' validation activates once P209 ships; the 209 immutable-evidence pattern carries — once `evidence_refs` array is written on `pr_outreach`, the FK array is frozen; future surfaces never `.update()` evidence_refs in place; new evidence requires new evidence_map_records row + replace; the 209 doctrine `<EvidenceMapPanel />` + `<EvidenceCitationChip />` extracted-component recipes are referenced by future 220 PR admin surfaces as load-bearing primitives; EVD doctrine carry — EVD-01 (evidence linked to factual claims), EVD-02 (unsupported claims block external dispatch), EVD-03..06 inherited but PR/community/event/affiliate/developer mutations primarily exercise EVD-01 + EVD-02; Pitfall 8 developer marketing pricing-drift detector applies EVD-01 to tutorial/technical_blog content with hardcoded numeric pricing strings — flagged unless `evidence_refs` links to PricingRecommendation OR contains `{{MARKOS_PRICING_ENGINE_PENDING}}` sentinel)
  - 213-UI-SPEC.md (PARENT — Tenant 0 proof posture invariant T0-04 boundary applies to PR pitch + G2/Capterra review record + review request + coverage_mentions content; 213-04 public-proof boundary applies to `pr_outreach.pitch_subject` + `pr_outreach.pitch_body` + `pr_outreach.story_angle` + `review_records.response_draft` + `review_requests.request_template_id` content + `coverage_mentions.title` + `coverage_mentions.snippet` authored under 220 are PRIVATE doctrine until consent + approval gates pass; raw `pr_targets.email` + `pr_targets.twitter_handle` + `pr_targets.linkedin_url` + `pr_targets.do_not_contact_reason` + `review_records.reviewer_name` (when DNC list) + `partner_profiles.primary_contact_email` are NEVER cited in case-studies or public surfaces; banned-lexicon zero-match required on every PR pitch + community moderation copy + event reminder body + G2 review response + affiliate program copy + partner co-marketing copy + developer marketing tutorial copy field BEFORE any `external.send` or `billing.charge` mutation dispatches the content (mirror 216/218/219 pattern; 220 expands the validator-count by 7 new mutation classes); 213.4-VALIDATION.md carry-forward (D-08 token-only, D-09 mint-as-text, D-09b `.c-notice` mandatory, D-13 `.c-card--feature` reserved, D-14 no `.c-table`, D-15 selective extraction, D-21 server/client boundary) carries verbatim into all future 220-consuming admin/tenant surfaces)
  - 214-UI-SPEC.md (PARENT — SaaS Suite Activation; `saas_suite_activations` HARD upstream gate; every 220 SOR table FK to `markos_orgs(tenant_id)` AND every 220 module-table INSERT fires `MODULE_REQUIRES_ELIGIBLE_GROWTH_MODE` BEFORE-INSERT trigger via `fn_check_module_mode_eligibility` from P218 migration 101; module-mode eligibility per canon line 50-60: `referral_engine` for b2c/plg_b2c/b2b2c modes, `community_engine` for b2c/plg_b2b/plg_b2c/b2b2c, `event_marketing` for all modes, `pr_analyst_relations` for b2b/b2b2c, `partnership_ecosystem` for b2b/b2b2c, `developer_marketing` for plg_b2b/b2b2c; 214 `<SaaSActivationPanel />` + `<SaaSSubscriptionsTable />` extracted components are downstream consumers of 220 referral / community / event / PR / partnership / affiliate / developer-marketing metadata-only signals; 220 inherits the `business_type != 'saas'` gating contract for all future growth admin/tenant surfaces; `affiliate_commissions.billing_handoff_id` nullable FK to `tenant_billing_subscriptions` AND nullable FK to `affiliate_programs.program_id` per 215 substrate inheritance)
  - 215-UI-SPEC.md (PARENT — sentinel `{{MARKOS_PRICING_ENGINE_PENDING}}` discipline for `affiliate_programs.commission_rate_jsonb` + `affiliate_programs.commission_description` (when `pricing_recommendation_id IS NULL`) per Q-4 SOFT P205 fallback; `referral_programs.referrer_reward_jsonb` + `referral_programs.referee_reward_jsonb` (when monetary reward type AND `pricing_recommendation_id IS NULL`); `review_requests.request_template_id`-resolved pricing copy (when G2/Capterra pricing-profile-sync); `developer_content.body` (when content_kind='tutorial' or 'technical_blog' AND hardcoded numeric pricing strings present); the 4-DB-trigger pricing compliance pack from 220-05 migration 95 mirrors 215 sentinel-acceptance pattern: `fn_affiliate_program_requires_pricing_context` BEFORE-INSERT/UPDATE accepts EITHER `pricing_recommendation_id NOT NULL` OR commission copy contains `{{MARKOS_PRICING_ENGINE_PENDING}}`; the single-writer reward gate from 220-01 migration 91 is parallel to 215 billing-correction single-writer pattern; 215 sensitive-credential UI binding contract Layer 6 (`[REDACTED]` for STRIPE/MP/SIIGO/DIAN/QB PII fields) does NOT extend into 220 (no payment processor PII in scope; `partner_profiles.primary_contact_email` + `pr_targets.email` are operator-managed contact PII, treated under standard RLS not Layer 6 redaction); 215 evidence-pack pattern carries forward — every affiliate commission issuance approval payload includes `pricing_context` resolved via Pricing Engine MCP tool which returns either source='pricing_engine' fields when valid OR sentinel object when `source='sentinel'`; **215 billing-correction modal recipe is REUSED VERBATIM for `affiliate_commission_issuance_approval` AND `partner_payout_export_approval` AND `referral_reward_issuance_approval`** per 220 Approval Inbox extension — the mutation-class is `billing.charge`, the modal renders `<BillingCorrectionPanel />` + `<PricingContextChip />` + `<PIIRedactedField />` primitives identically; **billing P215 HARD wiring** — `affiliate_commission_issuance` AND `partner_payout_export` AND `referral_reward_issuance` ALWAYS route through `lib/markos/billing` (P215 HARD upstream) and NEVER write CSV directly; manual CSV export is the FINAL formatter step on top of billing-engine output per Pitfall 2 fix)
  - 216-UI-SPEC.md (PARENT — `saas_health_scores.risk_level` + `saas_health_scores.health_score` consumed by 220-01 `lib/markos/referral/fraud-evaluator.ts` (SOFT degrade — `health_score_high` boost factor on `requirePaidAccount` rule when `health_score >= 85`) + 220-02 `lib/markos/community/health.ts` (`community_signals` `signal_kind='active_member_signal'` last-30d boost when tenant `health_score >= 85`) + 220-04 `lib/markos/pr/coverage-tracking.ts` (`brand_awareness_value` boost factor when tenant `health_score >= 85`); the 216 `EVENT_CATEGORIES = ['activation','adoption','depth','stickiness','expansion','plg_readiness','churn_signal','other']` taxonomy is consumed by 220-02 community signal fan-out — `community_signals.signal_kind='advocacy_candidate'` triggers fan-out to 219 advocacy_candidates (consumed_by_phase='P219') AND 216 saas_health_scores (consumed_by_phase='P216'); **216 `<HealthScoreBadge />` + `<RiskBandBadge />` + `<RetentionClassChip />` extracted-component recipes are referenced by future 220 admin surfaces as load-bearing primitives** — `<HealthScoreBadge />` reused on referral fraud-evaluator queue (when fraud-score derived from health) + community profile detail (when community_health_score derived from member activity) + event registration list (when registrant health_score gates post-event sequence) + affiliate commission tracker (when commission gating depends on partner health); `<RiskBandBadge />` reused on referral fraud-evaluator queue + prohibited-methods violation viewer; `<RetentionClassChip />` reused on event-attribution rollup view; the 216 9-row `growth_signal_map` reservation seed (planned_only=true) has rows that 220-02 + 220-04 signal scanners CONSUME at read-only — 220 plans do NOT modify the 216 seed; the existing 218 + 219 dissolutions of those rows already flipped `planned_only=false + activated_at=now()`; 220 inherits the post-219 state)
  - 217-UI-SPEC.md (PARENT — `saas_nav_visibility` 12-row planned-only seed has growth-related namespaces (`saas_revenue` (10), `saas_subscriptions` (1), `saas_invoices` (3), `saas_support` (8), `saas_agents` (12)) — the 12 SG planned-only seed rows include namespaces that 220 substrate makes activated-eligible: `saas_growth` (NEW namespace anticipated for future 220 admin surface — referral console, community admin, event marketing planner, PR console, partnership console, affiliate console, developer marketing console); the 220-06 closeout does NOT directly modify the 217 seed because the planned 220 admin/tenant surfaces are deferred to future phases; however, future 220 admin/tenant frontend phases that compose `app/saas/growth/{referral,community,events,pr,partnerships,affiliate,developer}/page.tsx` MUST extend `saas_nav_visibility` with new rows — each row gated by `SAAS_NAV_REQUIRES_ACTIVATION` DB-trigger; 217-06 `<SaaSActivationPanel />` + `<SaaSSubscriptionsTable />` + 7 NEW extracted components (`<HealthScoreBadge />`, `<RiskBandBadge />`, `<KbGroundingPanel />`, `<SaveOfferPricingBlock />`, `<RetentionClassChip />`, `<PIIRedactedField />`, `<ClassifierChipRow />`) are first-class production primitives that future 220 admin surfaces compose; **217 `future_phase_220_referral_console` placeholder DISSOLVES at the substrate-layer for referral, community, event, PR, partnership, affiliate, developer marketing engines** — 220 ships substrate; UI-layer placeholder removal requires the future 220 admin/tenant frontend phase; 217 D-21 server/client boundary doctrine carries forward verbatim — every future 220 admin / tenant surface MUST be a default server component reading via `requireHostedSupabaseAuth(request)` + tenant-scoped supabase client; client components opt in via `'use client'` only for interactive primitives (referral program editor, viral loop K-factor visualization, fraud-evaluator queue filter, community profile editor, community moderation approval modal, event marketing planner with 9-window reminder timeline, EventPromotionPlan auto-render, PR target relationship-graph editor, PR pitch evidence-binding modal, G2 review pricing-context modal, sentiment classifier confidence indicator, partner profile editor, partnership editor, affiliate program editor, affiliate commission tracker, prohibited-methods violation viewer, developer content editor, developer events ingestion viewer, 28-agent readiness viewer); 217 `app/saas/agents/page.tsx` agents-page is the surface where the **28 P220 growth agents readiness rows** (19 P220-owned: VRL-01..02 + CMT-01..03 + EVT-01..03 + PR-01..04 + PRT-01..03 + DEV-01..02 + REV-01..02; plus cross-phase pre-seeds for SG-10 invariant: PLG-01..06 + EXP-01..03 + ABM-01..03 + IAM-01 + XP-01..02) render with `runnable=false` badges until criteria met; the prompt-cited "28 growth agents" canonical figure is the P220-relevant aggregate per 220-06 Plan must_haves §28 agent rows; **the 217 `future_phase_220_referral_console` placeholder is DISSOLVED at the backend-contract layer** — 220 ships substrate; UI-layer placeholder removal requires the future 220 admin frontend phase)
  - 218-UI-SPEC.md (PARENT — `markos_growth_mode` ENUM + `saas_growth_profiles` consumer; `module_mode_eligibility` 60-row seed enforces b2c/plg_b2c/b2b2c mode access for `referral_engine` module + b2c/plg_b2b/plg_b2c/b2b2c for `community_engine` + all-modes for `event_marketing` + b2b/b2b2c for `pr_analyst_relations` + b2b/b2b2c for `partnership_ecosystem` + plg_b2b/b2b2c for `developer_marketing` per 220 Plan must_haves; every 220 module-table INSERT fires `MODULE_REQUIRES_ELIGIBLE_GROWTH_MODE` trigger via `lib/markos/profile/mode-eligibility.ts` `isModuleEligible(tenantId, moduleKey)`; **218 9-PLG-agents readiness pattern is REUSED verbatim in 220 19-P220-owned-agents readiness** (and the prompt-cited 28-agent aggregate including cross-phase pre-seeds) — system-level (not tenant-scoped) registry, GENERATED `runnable` column (Pitfall 9 regression — runnable column is `GENERATED ALWAYS AS (contracts_assigned AND cost_estimated AND approval_posture_defined AND tests_implemented AND api_surface_defined AND mcp_surface_defined AND ui_surface_defined AND failure_behavior_defined) STORED`; cannot be set directly), `AGENT_ACTIVATION_REQUIRES_APPROVAL_AND_READINESS` DB-trigger (defense-in-depth — raises EXCEPTION when `runnable=true` AND (`activation_approval_id IS NULL` OR `readiness_check_id IS NULL`)), INSERT ON CONFLICT seed (Pitfall 12 fix; P218/P219 can re-seed safely), 8-boolean checklist, **7-tier classification (RFL/COM/EVT/PR/PRT/AFF/DEV per pattern)** — note: the canonical migration uses 12-tier ENUM `agent_tier CHECK 12 values PLG/EXP/ABM/VRL/IAM/CMT/EVT/XP/PR/PRT/DEV/REV` per 220-06 Task 1 §Step 2; the prompt-cited "7-tier (RFL/COM/EVT/PR/PRT/AFF/DEV per pattern)" is the P220-relevant tier subset (7 of 12); **SG-07 experimentation_kind ENUM** classification — 6 enum values (`registry`, `ice_backlog`, `guardrail`, `approval_gate`, `decision_record`, `learning_promotion`); each of 28 seeded rows assigns 1 of 6 enum values per agent's primary SG-07 sub-criterion (e.g., VRL-01='guardrail', CMT-01='guardrail', EVT-01='guardrail', PR-01='approval_gate', PRT-01='approval_gate', DEV-01='guardrail', REV-01='learning_promotion', REV-02='registry'); 218 `<PlgAgentReadinessBadge />` extracted-component recipe is the load-bearing primitive that future 220 agent surfaces compose — same `[block]` / `[ok]` glyph pairing, same per-readiness-boolean checklist visualization; the 218 `future_phase_218_agents_page_extension` translation gate carries forward as a sibling pattern — 220 opens `future_phase_220_agents_page_extension` for the 28 growth agents)
  - 219-UI-SPEC.md (PARENT — 219's 9 B2B agents readiness pattern is REUSED verbatim in 220 — same system-level registry, same GENERATED runnable column, same AGENT_ACTIVATION_REQUIRES_READINESS trigger, same INSERT ON CONFLICT seed, same 8-boolean checklist; 219 opened `future_phase_219_agents_page_extension` and `future_phase_219_approval_inbox_extensions`; 220 opens parallel sibling translation gates `future_phase_220_agents_page_extension` for the 28 growth agents and `future_phase_220_approval_inbox_extensions` for the 7 new handoff_kind chips; **219's 19th handoff_kind in the canonical chain (`save_offer_approval`) is the immediate predecessor** — 220 extends to 26 chips (19 + 7 new); 219 opened `future_phase_220_referral_console` translation gate explicitly — 220 substrate dissolves it at the backend-contract layer; the 219 6-future-surface UI binding contracts pattern (Revenue Team Config / Account Expansion + Customer Marketing / ABM Account Package / Advocacy + Proof / B2B Pricing + Save + Discount / 9 B2B Agents Readiness) is REUSED verbatim in 220 with 6 parallel future-surface UI binding contracts (Referral / Community / Event Marketing / PR-Analyst-Review / Partnership-Affiliate-Developer / 28 Growth Agents Readiness))
  - 213.4-VALIDATION.md §Carry-forward (D-08 token-only, D-09 mint-as-text, D-09b `.c-notice` mandatory, D-13 `.c-card--feature` reserved, D-14 no `.c-table`, D-15 selective extraction, D-21 server/client boundary)
translation_gates_dissolved_by_220:
  - "217-UI-SPEC §future_phase_220_referral_console — DISSOLVED at the backend-substrate layer by 220 plans 01-06 shipping `referral_programs` + `viral_loop_metrics` + `referral_invitations` (220-01 migration 90) + single-writer reward gate + pricing-context gate (220-01 migration 91) + `community_profiles` + `community_signals` + `community_actions` (220-02 migration 92) + `fn_community_action_moderation_gate` (220-02) + `marketing_events` + `event_speakers` + `event_registrations` + `event_reminders` + `event_results` (220-03 migration 93) + `fn_event_promoting_requires_approval` (220-03) + `pr_targets` + `pr_outreach` + `review_records` + `review_requests` + `coverage_mentions` (220-04 migration 94) + 3 PR DB-triggers (220-04) + `partner_profiles` + `partnerships` + `affiliate_programs` + `affiliate_commissions` + `developer_content` + `developer_events` (220-05 migration 95) + 4 partnership DB-triggers (220-05) + `growth_agent_readiness` (220-06 migration 97 — skip 96 = neuro_literacy_metadata existing) + 19 `/v1/growth/*` API handlers + 12 read + 3 write-gated MCP tools + 6 cron handlers + F-209..F-227 contracts + 28 growth agent readiness rows. The frontend admin / tenant surfaces (referral program editor + viral loop metrics dashboard, referral fraud-evaluator queue with risk indicators, community profile + signals browser with 8-value signal_kind chips, community moderation approval modal, event marketing planner with 9-window reminder timeline, event status transition approval modal, EventPromotionPlan auto-render, PR target + outreach pipeline, PR pitch approval modal with evidence-binding, review records + sentiment classifier display, G2 review pricing-context modal, coverage mentions browser, partner profile + partnership editor, partnership activation approval modal, affiliate program + commission tracker, affiliate commission issuance approval modal — billing P215 HARD wired, partner payout export approval modal — billing P215 HARD wired, developer content + developer events, prohibited-methods violation viewer, 28 growth agents readiness viewer extension on 217-06 `app/saas/agents/page.tsx`) are DEFERRED to future P220+ admin/tenant phases. 220 ships substrate ONLY. The `<PlaceholderBanner variant=\"future_phase_220_referral_console\">` is REMOVED from 217-consuming surfaces at the backend-contract layer; UI-layer placeholder removal requires the future admin/tenant frontend phase."
  - "217-UI-SPEC §saas_nav_visibility 12 SG planned-only seed (community/events/PR/partnership rows) — PARTIALLY DISSOLVED at the backend-substrate layer for the growth namespaces. 220 substrate makes the eligibility runtime check load-bearing — future 217 admin UI surfaces that show a `b2c`/`plg_b2c`/`b2b2c` mode tenant the referral module CAN read 220 `referral_programs` data via 220 API handlers; future surfaces that show a `b2b`/`b2b2c` mode tenant the PR + partnership modules CAN read 220 `pr_outreach` + `partnerships` data; future surfaces that show a `plg_b2b`/`b2b2c` mode tenant the developer marketing module CAN read 220 `developer_content` data. (Translation gate kept open for the actual UI rendering — community profile browser, event marketing planner, PR pipeline, etc. all DEFERRED to future P220+ admin/tenant frontend phases.) The 217 `future_phase_220_referral_console` placeholder dissolution is documented above; the broader `saas_nav_visibility` activation is now eligible for the growth namespaces but not yet rendered."
  - "218-UI-SPEC §future_phase_220 placeholders — CONSUMED by 220 substrate. The 218 `future_phase_220_admin_ui` placeholder (if existed) is dissolved at the backend-contract layer — 220 ships substrate that future P218+P220 admin UI for `b2c`/`plg_b2c`/`plg_b2b`/`b2b2c` mode tenants will consume. The 218 `module_mode_eligibility` 60-row seed enforces b2c/plg_b2c/b2b2c mode access on the 220 referral_engine module + b2c/plg_b2b/plg_b2c/b2b2c on community_engine + all-modes on event_marketing + b2b/b2b2c on pr_analyst_relations + b2b/b2b2c on partnership_ecosystem + plg_b2b/b2b2c on developer_marketing (6 of 12 P218 modules now reference 220 substrate). 220 substrate makes the eligibility runtime check load-bearing — future 218 admin UI surfaces that show a growth-eligible-mode tenant any of the 6 modules CAN read 220 SOR data via 220 API handlers."
  - "219-UI-SPEC §future_phase_220 placeholders — CONSUMED by 220 substrate. The 219 `future_phase_220_referral_console` (if duplicated from 217) is dissolved. The 219-opened sibling translation gates `future_phase_219_agents_page_extension` + `future_phase_219_approval_inbox_extensions` carry forward as parallel patterns — 220 opens `future_phase_220_agents_page_extension` for the 28 growth agents and `future_phase_220_approval_inbox_extensions` for the 7 new handoff_kind chips."
  - "213-04 public-proof boundary on PR pitches + G2 reviews — DISSOLVED at the substrate-layer for PR + review governance. 220-04 ships `pr_outreach.evidence_refs uuid[] NOT NULL DEFAULT ARRAY[]::uuid[]` + `fn_pr_pitch_requires_evidence` + `review_records.response_approval_id` + `review_requests.pricing_engine_context_id` + `fn_review_request_g2_pricing_context` that operationalize T0-04 for PR + G2/Capterra review governance. The 213.4 `<PlaceholderBanner variant=\"future_phase_pr_console\">` (if existed) is REMOVED from 213-consuming surfaces at the backend-contract layer. The future PR + review admin UI surface (PR target relationship-graph + outreach pipeline with evidence-binding indicator + sentiment classifier output + G2 pricing-context modal + coverage mentions browser + brand-awareness rollup) requires a future P220+ tenant frontend phase."
translation_gates_opened_by_220:
  - "future_phase_220_admin_ui — Multi-page community/events/PR/partnership/affiliate/developer admin surface composing referral console (`app/saas/growth/referral/page.tsx` future) + viral loop metrics dashboard (`app/saas/growth/referral/metrics/page.tsx` future) + fraud-evaluator queue (`app/saas/growth/referral/fraud/page.tsx` future) + community profile editor (`app/saas/growth/community/[id]/edit/page.tsx` future) + community signals browser (`app/saas/growth/community/signals/page.tsx` future) + community moderation queue (`app/saas/growth/community/moderation/page.tsx` future) + event marketing planner (`app/saas/growth/events/page.tsx` future) + event status transition page (`app/saas/growth/events/[id]/promote/page.tsx` future) + EventPromotionPlan auto-render (`app/saas/growth/events/[id]/promotion-plan/page.tsx` future) + PR target relationship pipeline (`app/saas/growth/pr/targets/page.tsx` future) + PR outreach pipeline (`app/saas/growth/pr/outreach/page.tsx` future) + review records + sentiment display (`app/saas/growth/pr/reviews/page.tsx` future) + coverage mentions browser (`app/saas/growth/pr/coverage/page.tsx` future) + partner profile editor (`app/saas/growth/partnerships/profiles/[id]/edit/page.tsx` future) + partnership activation queue (`app/saas/growth/partnerships/page.tsx` future) + affiliate program editor (`app/saas/growth/partnerships/affiliate/[id]/edit/page.tsx` future) + commission tracker (`app/saas/growth/partnerships/affiliate/commissions/page.tsx` future) + payout export queue (`app/saas/growth/partnerships/affiliate/payouts/page.tsx` future) + developer content library (`app/saas/growth/developer/content/page.tsx` future) + developer events ingestion (`app/saas/growth/developer/events/page.tsx` future) + prohibited-methods violation viewer (`app/saas/growth/partnerships/affiliate/violations/page.tsx` future) + 28-agent readiness viewer extension on 217-06 `app/saas/agents/page.tsx`. All read via 220 `/v1/growth/*` API contracts (F-209..F-227); all gate via 217-06 `isSaaSSurfaceEnabled` 3-condition gate; all extend 217-06 `saas_nav_visibility` with new rows under namespace `saas_growth` (eligible for growth-modes per 218 `module_mode_eligibility` per 220 Plan must_haves: b2c/plg_b2c/b2b2c for referral, b2c/plg_b2b/plg_b2c/b2b2c for community, all-modes for event marketing, b2b/b2b2c for PR + partnerships, plg_b2b/b2b2c for developer marketing). Future surfaces render `<PlaceholderBanner variant=\"future_phase_220_admin_ui\">` until those phases ship."
  - "future_phase_220_referral_console — Tenant-facing referral console composing `<ReferralProgramEditor />` (4-status ENUM `active`/`paused`/`draft` + reward authoring with `<Money fromPricingRecommendation />` XOR sentinel + 5-rule fraud_controls jsonb editor + 3-share-tools chips + attribution-window day picker) + `<ViralLoopMetricsDashboard />` (k-factor numeric display = `referrals_converted / active_referrers` when `active_referrers > 0` else 0 + invitations-per-active-user-monthly + invitation-conversion-rate + `<ChannelBreakdownChip />` per channel) + `<ReferralFraudEvaluatorQueue />` with 5-rule risk indicators (`minAccountAge` / `requirePaidAccount` / `selfReferralPrevention` / `vpnDetection` / `maxReferralsPerUser`) + `<ReferralRewardIssuanceModal />` reusing 215 billing-correction modal recipe with `<PricingContextChip />` + audit log + `<ReferralReadinessBadge />` (single-writer reward gate visible). All consume 220 substrate via `/v1/growth/referral/*` API contracts (F-209..F-212); all gate via 214 SaaSSuiteActivation. Future surfaces render `<PlaceholderBanner variant=\"future_phase_220_referral_console\">` until those phases ship."
  - "future_phase_220_event_marketing_console — Tenant-facing event marketing console composing `<EventMarketingPlanner />` (9-event-type chips + 3-format chips `virtual`/`in_person`/`hybrid` + 5-status timeline `planning`→`promoting`→`live`→`completed`/`canceled` + capacity input + registration target input) + `<EventReminderScheduleTimeline />` (9-window verbatim: T-28d / T-21d / T-14d / T-7d / T-2d / T-1d / T-1h / day_of / post_replay; Pitfall 6 buffer indicator — never dispatch when `scheduled_at < now - 1h`) + `<EventPromotionPlanRender />` (auto-generated per doc 17 line 996-1048) + `<EventStatusTransitionApprovalModal />` (planning→promoting requires `approved_at`; reuses 208 Approval Inbox 21st chip recipe) + `<EventAttributionRollup />` (pipeline_sourced_cents + pipeline_influenced_cents per attribution-window-days lookback against P222 opportunities SOFT). All consume 220 substrate via `/v1/growth/events/*` API (F-215..F-217); all gate via 214 SaaSSuiteActivation. Future surfaces render `<PlaceholderBanner variant=\"future_phase_220_event_marketing_console\">` until those phases ship."
  - "future_phase_220_pr_console — Tenant-facing PR + analyst + review console composing `<PrTargetRelationshipPipeline />` (4-target_type chips `journalist`/`analyst`/`influencer`/`review_publication` + 6-relationship-stage chips `cold`/`warming`/`engaged`/`responsive`/`active`/`do_not_contact` + DNC indicator) + `<PrOutreachPipeline />` (7-status timeline + evidence-binding indicator with `cardinality(evidence_refs) > 0` check) + `<PrPitchApprovalModal />` with `<EvidenceMapPanel />` + `<EvidenceCitationChip />` (Pitfall 3 + Q-5 SOFT FK upgrade — when P209 lands, FK existence + freshness_status='fresh' validation activates) + `<ReviewRecordsTimeline />` (8-platform chips `g2`/`capterra`/`trustradius`/`trustpilot`/`product_hunt`/`app_store`/`play_store`/`custom` + 5-response-status timeline) + `<SentimentClassifierBadge />` (LLM output -1.0 to +1.0 numeric + confidence indicator + cost-window per call; sanitize + length-cap on review_body to prevent prompt injection T-220-04-06) + `<G2ReviewPricingContextModal />` (Pitfall 4 SG-11 hard floor — `pricing_engine_context_id` required for G2/Capterra pricing-profile-sync template) + `<CoverageMentionsBrowser />` (6-mention-kind chips + sentiment scoring + brand_awareness_value rollup) + `<BrandAwarenessRollup />` (period aggregate). All consume 220 substrate via `/v1/growth/pr/*` API (F-218..F-221); all gate via 214 SaaSSuiteActivation. Future surfaces render `<PlaceholderBanner variant=\"future_phase_220_pr_console\">` until those phases ship."
  - "future_phase_220_partnership_console — Tenant-facing partnership console composing `<PartnerProfileEditor />` (8-partner_kind chips `technology`/`referral`/`affiliate`/`co_marketing`/`reseller`/`white_label`/`consulting`/`agency` + 5-status chips `invited`/`active`/`watch`/`paused`/`terminated` + contract URL link) + `<PartnershipActivationQueue />` (4-status timeline `proposed`→`active`/`paused`/`ended` + activation approval modal — 24th chip in P208 chain) + `<JointKpiPanel />` (joint_kpi_jsonb editor) + `<SharedAssetsList />`. All consume 220 substrate via `/v1/growth/partnerships/*` API (F-222..F-223); all gate via 214 SaaSSuiteActivation. Future surfaces render `<PlaceholderBanner variant=\"future_phase_220_partnership_console\">` until those phases ship."
  - "future_phase_220_affiliate_console — Tenant-facing affiliate console composing `<AffiliateProgramEditor />` (3-status chips `active`/`paused`/`invite_only` + 4-commission_type chips `flat_fee`/`percentage_mrr`/`percentage_arr`/`hybrid` + commission_rate_jsonb editor with `<Money fromPricingRecommendation />` XOR sentinel + cookie-window day picker + minimum-payout dollar picker + 5-tracking_provider chips + 4-payment_method chips + 2-payment_frequency chips) + `<ProhibitedMethodsList />` (default array `['paid_search_brand_terms','email_spam','trademark_misuse']` + violation indicator on detection) + `<AffiliateCommissionTracker />` (4-status timeline `earned`→`locked`/`paid`/`refunded` + immutability indicator when `locked_at IS NOT NULL`) + `<AffiliateCommissionIssuanceModal />` reusing 215 billing-correction modal recipe — 25th chip + billing P215 HARD wired (`createBillingHandoff`) + `<PricingContextChip />` resolved via Pricing Engine MCP tool + `<PIIRedactedField />` for partner contact email + `<PartnerPayoutExportModal />` reusing 215 billing-correction modal recipe — 26th chip + billing P215 HARD wired + payout_compliance_acknowledged_at gate forward-port from P227 D-71 + manual CSV export FINAL formatter step + `<ProhibitedMethodsViolationViewer />` (paid_search_brand_terms / email_spam / trademark_misuse violations with audit log). All consume 220 substrate via `/v1/growth/partnerships/affiliate-*` API (F-224); all gate via 214 SaaSSuiteActivation. Future surfaces render `<PlaceholderBanner variant=\"future_phase_220_affiliate_console\">` until those phases ship."
  - "future_phase_220_developer_marketing_console — Tenant-facing developer marketing console composing `<DeveloperContentLibrary />` (6-content_kind chips `tutorial`/`api_reference`/`code_sample`/`starter_kit`/`technical_blog`/`changelog_entry` + 3-freshness_status chips `fresh`/`stale`/`broken` + last_validated_at timestamp + 4-validation_kind chips) + `<DeveloperContentEditor />` with `<PricingDriftDetectorBadge />` (Pitfall 8 — flags tutorials/technical_blogs with hardcoded numeric pricing strings unless `evidence_refs` links to PricingRecommendation OR contains `{{MARKOS_PRICING_ENGINE_PENDING}}` sentinel) + `<DeveloperEventsIngestionView />` (9-event_kind chips per RESEARCH §6 Domain 5; consumed_by_phase tracking; P227 ALTERable for ecosystem extension). All consume 220 substrate via `/v1/growth/developer/*` API (F-225); all gate via 214 SaaSSuiteActivation. Future surfaces render `<PlaceholderBanner variant=\"future_phase_220_developer_marketing_console\">` until those phases ship."
  - "future_phase_220_approval_inbox_extensions — P208 Approval Inbox at `/operations/approvals` rendering 7 NEW handoff_kind chips (`community_moderation_approval` 20th + `event_promotion_approval` 21st + `pr_pitch_approval` 22nd + `g2_review_pricing_approval` 23rd + `partnership_activation_approval` 24th + `affiliate_commission_issuance_approval` 25th + `partner_payout_export_approval` 26th), filter chip set extends from 19 chips (post-219) to **26 chips post-220 — END OF v4.1.0 milestone**. Each chip renders the per-row classifier (community moderation: 5-action_kind chip `ban`/`mute`/`post_removal`/`post_external_brand`/`conversion_incentive_send` + 8-value signal_kind context badge for triggering signal; event promotion: 5-status badge + 9-window reminder schedule preview + EventPromotionPlan auto-generated render preview; PR pitch: 7-status badge + evidence-binding `<EvidenceCitationChip />` cardinality indicator + 4-target_type badge; G2 review pricing: 8-platform badge + 5-trigger_kind chip + Pricing Engine context resolved indicator + sentinel `{{MARKOS_PRICING_ENGINE_PENDING}}` acceptance indicator; partnership activation: 8-partner_kind chip + 7-partnership_kind chip + 4-status badge + joint_kpi preview; affiliate commission: 4-commission_type chip + 4-status badge + immutability indicator + billing P215 HARD wired indicator + reuses 215 billing-correction modal recipe; partner payout export: payout_amount_cents + dual-approval indicator when amount > 100000 + payout_compliance_acknowledged_at gate indicator + billing P215 HARD wired indicator + manual CSV export FINAL formatter step indicator). Row rendering extension is DEFERRED to a future P208 admin extension phase. Future surfaces render `<PlaceholderBanner variant=\"future_phase_220_approval_inbox_extensions\">` until that phase ships."
  - "future_phase_220_agents_page_extension — 217-06 `app/saas/agents/page.tsx` agents page extension rendering **28 P220 growth agents readiness rows** alongside 9 P218 PLG + 9 P219 B2B + 12 P217 SAS agents readiness rows (TOTAL 58 agent rows visible end-of-v4.1.0; the prompt-cited 'total agent readiness registry across 218/219/220 (9+9+28 = 46 agents)' is the v4.1.0 SaaS Suite Activation milestone end-state for the SaaS-tier agents specifically). Per-agent 8-readiness-boolean checklist visible (contracts_assigned + cost_estimated + approval_posture_defined + tests_implemented + api_surface_defined + mcp_surface_defined + ui_surface_defined + failure_behavior_defined); `[block] Agent not yet runnable` paired with `--color-error` badge for `runnable=false`; `[ok] Agent ready` paired with `--color-success` badge when GENERATED `runnable=true` (Pitfall 9 regression). The 28 P220-relevant agents (canonical aggregate per 220-06 Plan must_haves §28 agent rows; 19 P220-owned: VRL-01..02 + CMT-01..03 + EVT-01..03 + PR-01..04 + PRT-01..03 + DEV-01..02 + REV-01..02; plus cross-phase pre-seeds: PLG-01..06 + EXP-01..03 + ABM-01..03 + IAM-01 + XP-01..02) are pre-populated `runnable=false` until criteria met (SG-10 invariant). 7-tier P220-relevant classification badge (`P220_AGENT_TIERS = ['VRL','CMT','EVT','PR','PRT','DEV','REV']`) renders as `<.c-chip>` per row; 12-tier full classification badge available for cross-phase view (`AGENT_TIERS = ['PLG','EXP','ABM','VRL','IAM','CMT','EVT','XP','PR','PRT','DEV','REV']`). SG-07 experimentation_kind chip renders 1-of-6 ENUM (`registry`/`ice_backlog`/`guardrail`/`approval_gate`/`decision_record`/`learning_promotion`) per agent. Future surfaces render `<PlaceholderBanner variant=\"future_phase_220_agents_page_extension\">` until that phase ships."
  - "future_phase_227_ecosystem_alter — P227 (v4.2.0 Ecosystem Engine) will ALTER 5 P220-owned tables additively WITHOUT pre-allocating ecosystem columns in P220: `partner_profiles` (P227 ALTERs ADD 11 ecosystem columns including `business_mode` + `partner_type` + `commission_share_locked` + ecosystem_attribution_extension; per Q-1 + Q-2 RESOLUTION in 220-RESEARCH.md — P220 keeps `partner_kind` column verbatim per doc 17 + canon line 1255; P227 ADDs separate `partner_type` column with semantically different ENUM; both columns coexist additively); `referral_programs` (P227 ALTERs ADD 7 ecosystem columns); `community_profiles` (P227 ALTERs ADD `business_mode` + `scope`); `marketing_events` (P227 ALTERs ADD `business_mode`); `partnerships` (P227 ALTERs ADD `business_mode` + `co_sell_enabled`). The P220→P227 contract: P220 schema MUST NOT pre-allocate `business_mode` (P227 owns that addition with `DEFAULT 'saas'` backfill — verified in 227-RESEARCH.md migration 160 SQL); the `developer_events` table (P220-05) is also P227-ALTERable for `business_mode + cdp_event_id` per 220-05 migration 95 explicit comment block. Test `test/growth-220/migration-shape/p220-p227-altertable-readiness.test.js` asserts P220 ships 5 P227-ALTERable tables (partner_profiles, referral_programs, community_profiles, marketing_events, partnerships) WITHOUT ecosystem columns (business_mode, commission_share_locked, partner_type, etc. all ABSENT — P227 will add). Future surfaces render `<PlaceholderBanner variant=\"future_phase_227_ecosystem_alter\">` until P227 ships."
---

# Phase 220 — UI Design Contract (no-UI-scope)

> **Phase 220 ships zero UI surfaces.** This document is the explicit no-surface declaration for the SaaS Community, Events, PR, Partnerships, Developer Marketing, and Growth Agent Surface phase — the **closing phase of the v4.1.0 SaaS Suite Activation milestone** (Phases 214→215→216→217→218→219→**220**). There is no app/, no components/, no *.stories.tsx, no page.tsx, no layout.tsx, no *.module.css, and no *.css modified or created in any of the six plans (220-01 through 220-06). The existing P208 Approval Inbox + P217-06 saas_nav_visibility + P217-06 app/saas/agents/page.tsx existing surfaces consume 220 contracts as downstream readers — they are NOT modified by 220.
>
> **Critical posture:** Phase 220 is the **growth-engine substrate** of the SaaS Suite — the final closing piece across **6 growth domains** (Referral / Community / Event Marketing / PR-Analyst-Review / Partnership-Affiliate-Developer / Growth Agents Readiness) per obsidian/brain/SaaS Marketing OS Strategy Canon.md + obsidian/work/incoming/17-SAAS-MARKETING-OS-STRATEGY.md. The phase ships across 6 domains:
>
> **Domain 1 — Referral + Viral Loop** (220-01): referral_programs (3-status ENUM active/paused/draft + reward jsonb + 5-rule fraud_controls + cookie-window + attribution-window) + viral_loop_metrics (k-factor numeric) + referral_invitations (3-state fraud_status cleared/flagged/blocked + reward_approval_id soft-FK to P208). **Single-writer reward gate** raises REWARD_REQUIRES_APPROVAL when reward_issued_at IS NOT NULL AND reward_approval_id IS NULL. **Pricing-context gate** raises REWARD_REQUIRES_PRICING_CONTEXT for monetary reward types unless sentinel present. **5-rule fraud-evaluator**: minAccountAge / requirePaidAccount / selfReferralPrevention / vpnDetection / maxReferralsPerUser.
>
> **Domain 2 — Community** (220-02): community_profiles + community_signals (**8-value signal_kind taxonomy verbatim**: active_member_signal / feature_confusion_signal / competitor_mention_signal / content_gap_signal / advocacy_candidate_signal / churn_risk_signal / feature_request_signal / review_signal; Pitfall 5 dedup UNIQUE INDEX) + community_actions (5-action_kind ENUM ban/mute/post_removal/post_external_brand/conversion_incentive_send). **fn_community_action_moderation_gate** BEFORE-INSERT trigger raises COMMUNITY_MODERATION_REQUIRES_APPROVAL.
>
> **Domain 3 — Event Marketing** (220-03): marketing_events (5-status ENUM planning/promoting/live/completed/canceled) + event_speakers + event_registrations + event_reminders (**9-window verbatim**: T-28d / T-21d / T-14d / T-7d / T-2d / T-1d / T-1h / day_of / post_replay) + event_results. **fn_event_promoting_requires_approval** BEFORE-UPDATE trigger raises EVENT_EXTERNAL_DISPATCH_REQUIRES_APPROVAL. **EventPromotionPlan auto-generation** per doc 17 line 996-1048. **Pitfall 6 buffer** — never dispatches when scheduled_at < now - 1h.
>
> **Domain 4 — PR + Analyst + Review** (220-04): pr_targets + pr_outreach (evidence_refs uuid[] NOT NULL DEFAULT empty array; soft-FK to P209) + review_records (8-platform ENUM g2/capterra/trustradius/trustpilot/product_hunt/app_store/play_store/custom) + review_requests + coverage_mentions. **3 DB-trigger compliance functions**: fn_pr_outreach_requires_approval + fn_pr_pitch_requires_evidence (Pitfall 3 fix; Q-5 SOFT FK upgrade) + fn_review_request_g2_pricing_context (Pitfall 4 fix; SG-11 hard floor). **LLM sentiment classifier** via existing adapter (RESEARCH §Library Choices option a; sanitize + length-cap to prevent prompt injection T-220-04-06).
>
> **Domain 5 — Partnership + Affiliate + Developer Marketing** (220-05): partner_profiles + partnerships + affiliate_programs (**prohibited_methods text[] DEFAULT array of paid_search_brand_terms / email_spam / trademark_misuse**) + affiliate_commissions (**billing_handoff_id soft-FK to P215 billing**) + developer_content + developer_events. **4 DB-trigger compliance functions**: fn_partnership_activation_requires_approval + fn_affiliate_program_requires_pricing_context + fn_affiliate_program_prohibited_methods_required + fn_affiliate_commission_immutability. **Pitfall 8 developer marketing pricing-drift detector** flags tutorials with hardcoded numeric pricing strings unless evidence_refs links to PricingRecommendation OR contains the pending sentinel. **Billing P215 HARD wired** for affiliate commission issuance + partner payout export.
>
> **Domain 6 — 28 Growth Agents Readiness** (220-06 migration 97 — skip slot 96 = neuro_literacy_metadata existing): growth_agent_readiness (system-level; agent_tier CHECK 12 values PLG/EXP/ABM/VRL/IAM/CMT/EVT/XP/PR/PRT/DEV/REV; **runnable GENERATED ALWAYS AS (8-boolean AND chain) STORED** Pitfall 9 regression; **experimentation_kind ENUM(6 values) NOT NULL** SG-07 sub-criterion classification). **28 agent rows pre-populated runnable=false**: 19 P220-relevant tiers (VRL-01..02 + CMT-01..03 + EVT-01..03 + PR-01..04 + PRT-01..03 + DEV-01..02 + REV-01..02) plus cross-phase pre-seeds for SG-10 invariant continuity. **fn_growth_agent_activation_gate** BEFORE-UPDATE trigger raises AGENT_ACTIVATION_REQUIRES_APPROVAL_AND_READINESS. **19 API handlers** under api/v1/growth/* + **6 cron handlers** + **MCP tool family** (12 read + 3 write-gated). **Plan 06 checkpoint:human-action** — operator validates first-batch readiness BEFORE permitting runnable=true flips.
>
> The seven new Approval Inbox handoff_kind literals (community_moderation_approval 20th + event_promotion_approval 21st + pr_pitch_approval 22nd + g2_review_pricing_approval 23rd + partnership_activation_approval 24th + affiliate_commission_issuance_approval 25th + partner_payout_export_approval 26th) extend the P208 filter chip set from 19 literals (post-219) to **26 literals (post-220 — END OF v4.1.0 milestone)**.

>
> Authority chain: DESIGN.md v1.1.0 -> 213.4-VALIDATION.md carry-forward (D-08 token-only, D-09 mint-as-text, D-09b .c-notice mandatory, D-13 .c-card--feature reserved, D-14 no .c-table, D-15 selective extraction, D-21 server/client boundary) -> 206-UI-SPEC (mutation-class origin: external.send for community moderation + event reminders + PR pitch + review request + partner outreach + developer marketing, billing.charge for referral reward issuance + affiliate commission + partner payout — billing P215 HARD wired, data.export for audit log + partner payout CSV) -> 207-UI-SPEC (RunApiEnvelope, AgentRunEventType for 32 new event types across 6 domains, ApprovalHandoffRecord) -> 208-UI-SPEC (PARENT — Approval Inbox extends 7 new handoff_kind literals 20th-26th in canonical chain; END-OF-v4.1.0 chip count = 26) -> 209-UI-SPEC (PARENT — Pitfall 3 PR pitch evidence-binding cardinality check + Q-5 SOFT FK upgrade once P209 ships; EVD-01 + EVD-02 carry; immutable evidence_refs pattern; Pitfall 8 developer marketing pricing-drift detector applies EVD-01) -> 213-UI-SPEC (213-04 public-proof boundary on PR pitches + G2 reviews + reviews; banned-lexicon zero-match BEFORE external.send / billing.charge dispatch) -> 214-UI-SPEC (PARENT — SaaS Suite Activation; every 220 SOR table FK to markos_orgs(tenant_id); 6 of 12 P218 modules now reference 220 substrate per mode-eligibility) -> 215-UI-SPEC (PARENT — sentinel pending discipline; 4-DB-trigger pricing compliance pack; **billing P215 HARD wiring** for affiliate commission issuance + partner payout export + referral reward issuance; 215 billing-correction modal recipe REUSED for affiliate_commission_issuance_approval + partner_payout_export_approval + referral_reward_issuance approval) -> 216-UI-SPEC (PARENT — saas_health_scores consumer; HealthScoreBadge + RiskBandBadge + RetentionClassChip extracted-component recipes referenced as load-bearing primitives) -> 217-UI-SPEC (PARENT — saas_nav_visibility consumer eventually; 28 growth agents extend 217-06 agents page; future_phase_220_referral_console placeholder DISSOLVED at backend-contract layer) -> 218-UI-SPEC (PARENT — markos_growth_mode ENUM eligibility for referral/community/developer modules; 60-row module_mode_eligibility seed; 28 growth agents readiness pattern reused verbatim from 218 9 PLG agents pattern) -> 219-UI-SPEC (PARENT — 9 B2B agents readiness pattern reused; 219 19th handoff_kind chain predecessor; 6-future-surface UI binding contracts pattern reused verbatim with 6 parallel contracts; sibling translation gates future_phase_220_agents_page_extension + future_phase_220_approval_inbox_extensions) -> this document.
>
> **End-of-v4.1.0 milestone summary** (see End-of-Milestone State section below): 26 P208 handoff_kind chips total + 46 SaaS-tier agent readiness rows total per the orchestrator-cited aggregate (9 PLG P218 + 9 B2B P219 + 28 growth P220). Generated by gsd-ui-researcher 2026-05-04. Status: draft (checker upgrades to approved once the no-UI declaration is verified).



---

## Scope Verification

The orchestrator preliminary finding has been verified by reading all six plans plus context, research, and reviews. The full file set declared in `files_modified` across 220-01..220-06 is enumerated below, with surface classification per file:

| File class | Path glob | Plan(s) | UI surface? |
|------------|-----------|---------|-------------|
| Phase doctrine | `.planning/phases/220-.../{220-CONTEXT, 220-RESEARCH, 220-REVIEWS, 220-VALIDATION, 220-{01..06}-PLAN}.md` | 220-01..220-06 | NO |
| Migration coordination | `.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md` (APPEND P220) | 220-01 | NO |
| Preflight surface | `lib/markos/growth/preflight/{upstream-gate.ts, architecture-lock.ts, errors.ts, index.cjs}` (4 files) | 220-01 | NO (TS/CJS modules; no JSX) |
| Referral domain modules | `lib/markos/referral/*` (8 files: contracts.ts/cjs, programs.ts/cjs, metrics.ts, fraud-evaluator.ts, reward-issuance.ts, index.cjs) | 220-01 | NO |
| Community domain modules | `lib/markos/community/*` (8 files: contracts.ts/cjs, profiles.ts/cjs, health.ts, signals.ts, moderation-approvals.ts, index.cjs) | 220-02 | NO |
| Events domain modules | `lib/markos/events/*` (9 files: contracts.ts/cjs, events.ts/cjs, promotion-plan.ts, reminders.ts, attribution.ts, approval-gates.ts, index.cjs) | 220-03 | NO |
| PR domain modules | `lib/markos/pr/*` (8 files: contracts.ts/cjs, outreach.ts/cjs, reviews.ts, coverage-tracking.ts, evidence-binding.ts, index.cjs) | 220-04 | NO |
| Partnerships domain modules | `lib/markos/partnerships/*` (10 files: contracts.ts/cjs, partner-profiles.ts/cjs, partnerships.ts, affiliate-programs.ts, payout-compliance.ts, prohibited-methods-validator.ts, developer-marketing.ts, index.cjs) | 220-05 | NO |
| Growth-agents domain modules | `lib/markos/growth-agents/*` (5 files: contracts.ts/cjs, readiness-registry.ts, readiness-gate.ts, index.cjs) | 220-06 | NO |
| MCP tools | `lib/markos/mcp/tools/growth.cjs` + `lib/markos/mcp/tools/index.cjs` (registry append) | 220-06 | NO (CommonJS MCP descriptors; no JSX) |
| Migrations | `supabase/migrations/{90,91,92,93,94,95,97}_growth_*.sql` (7 files; skip slot 96 = neuro_literacy_metadata existing) | 220-01..220-06 | NO (SQL DDL) |
| Node API handlers | `api/v1/growth/{referral,community,events,pr,partnerships,developer,agents}/*.js` (19 files) | 220-06 | NO (legacy api/*.js; no JSX) |
| Cron handlers | `api/cron/growth-{viral-metrics-rollup, event-reminder-dispatch, community-signals-fan-out, pr-coverage-tracking, review-request-cadence, developer-content-validation}.js` (6 files) | 220-06 | NO |
| Preflight scripts | `scripts/preconditions/220-{01..06}-check-upstream.cjs` (6 files) | 220-01..220-06 | NO |
| F-ID contract YAMLs | `contracts/F-{209..227}-growth-*.yaml` (19 files) + `flow-registry.json` | 220-01..220-06 | NO |
| Test fixtures | `test/fixtures/growth-220/*.js` (10 files) | 220-01 | NO |
| Test files | `test/growth-220/{preflight,referral,community,events,pr,partnerships,agents-readiness,api,cron,mcp,rls,closeout,openapi,architecture-passive-system-detector,migration-shape}/*.test.js` | 220-01..220-06 | NO |

**Search assertions** (verified by orchestrator via grep over all `files_modified` blocks; result: `NO UI PATHS FOUND IN ANY PLAN — confirmed pure backend phase`):

| Assertion | Result |
|-----------|--------|
| `files_modified` glob `app/**` across 220-01..220-06 | 0 matches |
| `files_modified` glob `app/(growth)/**` across 220-01..220-06 | 0 matches (architecture-lock forbidden) |
| `files_modified` glob `components/**` across 220-01..220-06 | 0 matches |
| `files_modified` glob `*.stories.tsx` across 220-01..220-06 | 0 matches |
| `files_modified` glob `*.module.css` or `*.css` across 220-01..220-06 | 0 matches |
| `files_modified` containing `page.tsx`, `layout.tsx`, or `route.ts` | 0 matches |
| `files_modified` containing `tailwind.config.*`, `app/globals.css`, `app/tokens.css`, `styles/components.css` | 0 matches |
| `files_modified` containing forbidden lib paths `lib/markos/(sales|cdp|conversion|launches|analytics|channels|ecosystem)/` | 0 matches |

**Conclusion:** No-UI-surface declaration **CONFIRMED**. **If the checker finds ANY UI surface in plan files_modified blocks, BLOCK.**

---

## Design System

| Property | Value |
|----------|-------|
| Tool | not applicable — no UI surface authored in this phase |
| Preset | not applicable |
| Component library | not applicable |
| Icon library | not applicable |
| Heading font | not applicable (DESIGN.md v1.1.0 inherited for future surfaces — JetBrains Mono) |
| Body font | not applicable (DESIGN.md v1.1.0 inherited for future surfaces — Inter) |
| Default theme | not applicable (DESIGN.md v1.1.0 default-dark Kernel Black inherited) |
| Form authoring posture | not applicable — no forms |
| Banner authoring posture | not applicable — no banners |
| Card authoring posture | not applicable — no cards |
| Modal authoring posture | not applicable — the seven new P208 Approval Inbox handoff_kind chips reuse the existing P208 modal recipe; affiliate commission issuance + partner payout export + referral reward issuance reuse 215 billing-correction modal recipe |
| Storybook posture | not applicable — `test/growth-220/architecture-passive-system-detector.test.js` (SG-09) scans Storybook stories WHEN SHIPPED (deferred); v1 tests against API contracts (every read-only endpoint paired with corresponding task/approval write endpoint per RESEARCH §15 Anti-patterns) |

---

## Spacing Scale / Typography / Color

Not applicable — Phase 220 ships zero UI surfaces. Future P220+ admin/tenant surfaces inherit the DESIGN.md v1.1.0 8px-grid spacing scale, JetBrains Mono + Inter typography stack, and 60/30/10 color distribution (Kernel Black `#0A0E14` 60% / secondary 30% / Protocol Mint `#00D9A3` < 5% reserved as the single CTA / focus signal). State color is never the only signal — pair with bracketed glyphs `[ok]` / `[err]` / `[warn]`. No emoji in product UI, CLI, error messages, or technical docs.

---

## Copywriting Contract

Not applicable for this phase’s own surfaces (zero surfaces). For future P220+ admin/tenant surfaces, the inherited copywriting contract is the DESIGN.md banned-lexicon zero-match rule applied verbatim to **all 7 new mutation classes** (community moderation copy + event reminder body + PR pitch body + G2 review response + review request body + affiliate program copy + partner co-marketing copy + developer marketing tutorial body). **Banned-lexicon validator MUST run BEFORE any `external.send` / `billing.charge` mutation dispatches the content** (mirror 216/218/219 pattern; 220 expands the validator-count by 7 new mutation classes — see Future-Surface UI Binding Contracts below for per-domain validator placement).

The seven new P208 Approval Inbox handoff_kind chip labels:

| Chip Index | Literal | Mutation-Class | Approval Mode |
|------------|---------|----------------|---------------|
| 20 | `community_moderation_approval` | `external.send` | single_approval |
| 21 | `event_promotion_approval` | `external.send` | single_approval |
| 22 | `pr_pitch_approval` | `external.send` | single_approval |
| 23 | `g2_review_pricing_approval` | `external.send` | single_approval |
| 24 | `partnership_activation_approval` | `external.send` | single_approval |
| 25 | `affiliate_commission_issuance_approval` | `billing.charge` | single_approval |
| 26 | `partner_payout_export_approval` | `billing.charge` + `data.export` | dual_approval (when payout > $1,000) |

---

## Registry Safety

Not applicable — Phase 220 ships zero UI surfaces; no shadcn registry or third-party component blocks are added. The 213.4 carry-forward shadcn registry posture (none — manual primitives in `styles/components.css`) remains in force for future P220+ admin/tenant surfaces.

---

## Future-Surface UI Binding Contract 1 — Referral

For future P220+ admin/tenant surfaces consuming Domain 1 substrate (`referral_programs` + `viral_loop_metrics` + `referral_invitations` + 5-rule fraud-evaluator + single-writer reward gate + pricing-context gate). The contract is load-bearing — it specifies the visual primitives, copywriting rules, mutation wiring, and approval-modal reuse pattern that future referral-console UI phases MUST honor. Until those phases ship, surfaces that reference referral substrate render `<PlaceholderBanner variant="future_phase_220_referral_console">`.

| Surface element | DESIGN.md primitive | Token / class | Mutation wiring |
|-----------------|--------------------|----|-----------------|
| Referral program editor | `<.c-card>` + `<.c-input>` + `<.c-field>` + `<.c-button--primary>` | `--color-surface-raised` + `--color-text` + `--color-primary` (Protocol Mint < 5%) | POST /v1/growth/referral/programs (legacy api/*.js); status 'draft' on create; PATCH for activate path with approval gate |
| Reward authoring | `<.c-field>` + `<.c-input>` + custom `<Money fromPricingRecommendation />` XOR `{{MARKOS_PRICING_ENGINE_PENDING}}` sentinel display | `--color-text` + `--color-warning` (when sentinel) + `--color-text-muted` (helper) | client-side validation enforced by DB-trigger pricing-context gate (REWARD_REQUIRES_PRICING_CONTEXT) on cash/discount/credit reward types |
| Fraud-controls jsonb editor | `<.c-field>` + custom `<FraudControlsEditor />` with 5-rule chip row | `<.c-chip>` per rule (`minAccountAge` / `requirePaidAccount` / `selfReferralPrevention` / `vpnDetection` / `maxReferralsPerUser`) | client-side validation; pure-TS evaluator returns `{ score, status, reasons[] }`; no json-logic-js dep |
| Share-tools chip row | `<.c-chip>` per share-tool (3 chips per RESEARCH §6 Domain 1) | `--color-surface-raised` + `--color-text` | client-side validation |
| Attribution-window day picker | `<.c-input type="number">` with min/max constraints | `--color-text` | client-side validation; constraints from CHECK constraints in migration 90 |
| Viral loop metrics dashboard | `<.c-card>` per metric + `<KFactorDisplay />` numeric prominent + `<ChannelBreakdownChip />` per channel | `--color-surface-raised` + `--color-text` + `<HealthScoreBadge />` (216 inheritance — when k-factor > 1.0 indicates viral growth) | GET /v1/growth/referral/metrics (read-only contract F-211); period filter |
| Fraud-evaluator queue | `<.c-card>` per row + `<.c-badge--warning>` for flagged + `<.c-badge--error>` for blocked + `<RiskBandBadge />` (216 inheritance) | `--color-warning` + `--color-error` + bracketed glyphs `[warn]` / `[err]` (DESIGN.md "Color blindness" rule) | GET /v1/growth/referral/invitations?fraud_status=flagged,blocked |
| Referral reward issuance modal | **REUSES 215 billing-correction modal recipe verbatim** — `<.c-modal>` + `<.c-backdrop>` + `<BillingCorrectionPanel />` + `<PricingContextChip />` + audit log + `<ReferralReadinessBadge />` (single-writer reward gate visible) | `--color-surface-raised` + `--color-primary` (mint reserved for primary action) + `--color-text` | POST /v1/growth/referral/payouts → triggers `buildApprovalPackage(kind='referral_reward_issuance')` → P208 inbox; mutation-class `billing.charge` (215 HARD wired); approval mode `single_approval`; on approval execute → DB-trigger validates approved_at + reward_approval_id |
| Banned-lexicon validator | (no UI element — runtime check) | (n/a) | runs BEFORE any `external.send` mutation on `referral_programs.reward_description` + `referral_invitations.invitation_message` (when present); zero-match required pre-dispatch |

**Wired event types** (per 207 inheritance): `referral_invitation_recorded` / `referral_fraud_evaluated` / `referral_reward_issued` / `viral_loop_metrics_rolled_up`.

**Cron-handler-driven UI updates**: `growth-viral-metrics-rollup.js` (daily) → metrics dashboard re-renders with fresh k-factor numeric.

---

## Future-Surface UI Binding Contract 2 — Community

For future P220+ admin/tenant surfaces consuming Domain 2 substrate (`community_profiles` + `community_signals` + `community_actions` + 8-value signal_kind taxonomy + dedup UNIQUE INDEX + moderation gate). Until those phases ship, surfaces render `<PlaceholderBanner variant="future_phase_220_admin_ui">`.

| Surface element | DESIGN.md primitive | Token / class | Mutation wiring |
|-----------------|--------------------|----|-----------------|
| Community profile editor | `<.c-card>` + `<.c-input>` + `<.c-field>` + `<.c-button--primary>` | `--color-surface-raised` + `--color-primary` | POST /v1/growth/community/profiles (F-213) |
| Health-score display | `<HealthScoreBadge />` (216 inheritance) | mapped to `--color-success` / `--color-warning` / `--color-error` per score band | GET /v1/growth/community/profiles/{id} |
| 8-value signal_kind filter chips | `<.c-chip>` per signal_kind (verbatim 8 values: `active_member` / `feature_confusion` / `competitor_mention` / `content_gap` / `advocacy_candidate` / `churn_risk` / `feature_request` / `review`) | `--color-surface-raised` + `--color-text` per chip; `--color-primary` for active filter | GET /v1/growth/community/signals?signal_kind={value}&consumed_status={value} (F-214 read-only contract) |
| Signals timeline | `<.c-card>` per signal + `<.c-badge>` for consumed_at/consumed_by_phase + `<SignalKindChip />` per row | `--color-surface-raised` + bracketed-glyph dual-signal pairing | GET /v1/growth/community/signals (read-only; consumed status filter) |
| Community action moderation queue | `<.c-card>` per pending action + `<.c-badge--warning>` for pending + `<.c-badge--error>` for ban/post_removal action_kinds | `--color-warning` + `--color-error` + bracketed glyphs | GET /v1/growth/community/profiles/{id}/actions?moderation_approval_id=null |
| Moderation approval modal | `<.c-modal>` + `<.c-backdrop>` + 5-action_kind chip row (`ban` / `mute` / `post_removal` / `post_external_brand` / `conversion_incentive_send`) + audit log + reason text | `--color-surface-raised` + `--color-primary` (approve) + `--color-error` (reject) | POST /v1/growth/community/profiles/{id}/actions → triggers `buildApprovalPackage(kind='community_moderation_action')` → P208 inbox 20th chip; DB-trigger `fn_community_action_moderation_gate` enforces approval presence (defense-in-depth) |
| Banned-lexicon validator | (runtime check) | (n/a) | runs BEFORE `external.send` on community_actions.reason + post_external_brand body content |

**Wired event types**: `community_signal_ingested` / `community_signal_consumed` / `community_action_executed` / `community_moderation_dispatched`.

**Cron-handler-driven UI updates**: `growth-community-signals-fan-out.js` (hourly) → signal-timeline re-renders with newly-consumed signals.

---


## Future-Surface UI Binding Contract 3 — Event Marketing

For future P220+ admin/tenant surfaces consuming Domain 3 substrate (`marketing_events` + `event_speakers` + `event_registrations` + `event_reminders` + `event_results` + 9-window reminder schedule + EventPromotionPlan auto-generation + Pitfall 6 buffer). Until those phases ship, surfaces render `<PlaceholderBanner variant="future_phase_220_event_marketing_console">`.

| Surface element | DESIGN.md primitive | Token / class | Mutation wiring |
|-----------------|--------------------|----|-----------------|
| Event marketing planner | `<.c-card>` + `<.c-input>` + `<.c-field>` + 9-event_type chip row | `--color-surface-raised` + `--color-primary` | POST /v1/growth/events (F-215) |
| Format chip row | `<.c-chip>` per format (3 values: `virtual` / `in_person` / `hybrid`) | `--color-surface-raised` + `--color-text` per chip | client-side validation |
| Status timeline | `<.c-card>` row per status (5 values: `planning` → `promoting` → `live` → `completed`/`canceled`) | `--color-text-muted` (planning) → `--color-warning` (promoting) → `--color-success` (live/completed) → `--color-error` (canceled); paired with bracketed glyphs | PATCH /v1/growth/events/{id} (status transition; 'planning'→'promoting' fires DB-trigger `fn_event_promoting_requires_approval`) |
| **9-window reminder schedule timeline** | `<.c-card>` with horizontal timeline + `<.c-chip>` per window (verbatim 9 values: `T-28d` / `T-21d` / `T-14d` / `T-7d` / `T-2d` / `T-1d` / `T-1h` / `day_of` / `post_replay`) + 4-status badge per row (`pending` / `dispatched` / `failed` / `canceled`) | `--color-text` + `--color-success` (dispatched) + `--color-error` (failed) + bracketed glyphs | GET /v1/growth/events/{id}/reminders (read-only); UI re-renders on cron `growth-event-reminder-dispatch.js` (hourly) |
| **Pitfall 6 buffer indicator** | `<.c-notice c-notice--warning>` "[warn] Reminder dispatch suppressed: scheduled_at < now - 1h" | `--color-warning` + `[warn]` glyph | runtime indicator on event reminder rows where `scheduled_at < now - 1h` |
| **EventPromotionPlan auto-render** | `<.c-card c-card--feature>` (DESIGN.md D-13 reserved use) + `<EventPromotionPlanRender />` per doc 17 line 996-1048 jsonb structure | `--color-surface-raised` + `--color-primary` (mint reserved for hero card) + `--color-text` | GET /v1/growth/events/{id}/promotion-plan (auto-generated; consumed by approval-gates on activation) |
| Event status transition approval modal | `<.c-modal>` + `<.c-backdrop>` + activation summary + EventPromotionPlan preview + audit log | `--color-surface-raised` + `--color-primary` | PATCH /v1/growth/events/{id} status='promoting' → triggers `buildApprovalPackage(kind='event_activation')` → P208 inbox 21st chip; DB-trigger `fn_event_promoting_requires_approval` enforces approved_at presence |
| Capacity + registration target inputs | `<.c-input type="number">` with min constraints | `--color-text` | client-side validation; CHECK constraints in migration 93 |
| Event attribution rollup | `<.c-card>` + numeric pipeline_sourced_cents + pipeline_influenced_cents display + attribution_window_days lookback indicator + `<RetentionClassChip />` (216 inheritance) | `--color-surface-raised` + `--color-text` | GET /v1/growth/events/{id}/results (F-217 read-only) |
| Banned-lexicon validator | (runtime check) | (n/a) | runs BEFORE `external.send` on `event_reminders.body` + post-event sales sequence content |

**Wired event types**: `event_promotion_plan_generated` / `event_status_promoting_transitioned` / `event_reminder_scheduled` / `event_reminder_dispatched` / `event_attribution_rolled_up`.

**Cron-handler-driven UI updates**: `growth-event-reminder-dispatch.js` (hourly with Pitfall 6 buffer) → reminder timeline re-renders dispatched-at column.

---

## Future-Surface UI Binding Contract 4 — PR-Analyst-Review

For future P220+ admin/tenant surfaces consuming Domain 4 substrate (`pr_targets` + `pr_outreach` + `review_records` + `review_requests` + `coverage_mentions` + 3 DB-trigger compliance functions + LLM sentiment classifier + evidence-binding). Until those phases ship, surfaces render `<PlaceholderBanner variant="future_phase_220_pr_console">`.

| Surface element | DESIGN.md primitive | Token / class | Mutation wiring |
|-----------------|--------------------|----|-----------------|
| PR target relationship pipeline | `<.c-card>` per target + 4-target_type chip (`journalist` / `analyst` / `influencer` / `review_publication`) + 6-relationship_stage chip (`cold` / `warming` / `engaged` / `responsive` / `active` / `do_not_contact`) | `--color-surface-raised` + `--color-text` per chip; `--color-error` for DNC | GET /v1/growth/pr/targets (F-218); DNC indicator with `<.c-badge--error>` "[err] DO NOT CONTACT" |
| PR outreach pipeline | `<.c-card>` per outreach row + 7-status timeline + 6-coverage_kind chip + `<EvidenceBindingIndicator />` (cardinality > 0 check) | `--color-text` + `--color-success` (sent + coverage_url) + `--color-error` (rejected) + bracketed glyphs | PATCH /v1/growth/pr/outreach/{id} (F-219) |
| **PR pitch approval modal with evidence-binding** | `<.c-modal>` + `<.c-backdrop>` + `<EvidenceMapPanel />` (209 inheritance) + `<EvidenceCitationChip />` (209 inheritance) per evidence_ref + Pitfall 3 cardinality > 0 indicator + Q-5 SOFT FK upgrade indicator (when P209 lands, FK existence + freshness_status='fresh' validation activates) | `--color-surface-raised` + `--color-primary` (approve) + `--color-error` (reject) | POST /v1/growth/pr/outreach/{id}/send → triggers `buildApprovalPackage(kind='pr_pitch_send')` → P208 inbox 22nd chip; DB-trigger `fn_pr_pitch_requires_evidence` enforces `cardinality(evidence_refs) > 0` (defense-in-depth EVD-02) |
| Review records timeline | `<.c-card>` per review + 8-platform chip (`g2` / `capterra` / `trustradius` / `trustpilot` / `product_hunt` / `app_store` / `play_store` / `custom`) + 5-response_status timeline + star_rating display | `--color-surface-raised` + `--color-text` per chip + `--color-success` / `--color-warning` / `--color-error` per response_status band | GET /v1/growth/pr/reviews (F-220) |
| **LLM sentiment classifier badge** | `<SentimentClassifierBadge />` numeric -1.0 to +1.0 + confidence indicator + cost-window per call | `--color-error` (negative) → `--color-warning` (neutral) → `--color-success` (positive); paired with bracketed glyphs (DESIGN.md "Color blindness" rule) | runtime; uses `lib/markos/llm` adapter (RESEARCH §Library Choices option a; sanitize + length-cap on review_body to prevent prompt injection T-220-04-06; cost-aware) |
| **G2 review pricing-context modal** | `<.c-modal>` + `<.c-backdrop>` + 8-platform chip (G2/Capterra) + 5-trigger_kind chip + `<PricingContextChip />` (215 inheritance) resolved via Pricing Engine MCP tool + sentinel `{{MARKOS_PRICING_ENGINE_PENDING}}` acceptance indicator | `--color-surface-raised` + `--color-warning` (when sentinel) | POST /v1/growth/pr/review-requests (F-221) → triggers `buildApprovalPackage(kind='pr_pricing_profile_sync')` → P208 inbox 23rd chip; DB-trigger `fn_review_request_g2_pricing_context` enforces `pricing_engine_context_id` presence (Pitfall 4 fix; SG-11 hard floor) |
| Coverage mentions browser | `<.c-card>` per mention + 6-mention_kind chip + sentiment_score (LLM) + brand_awareness_value 0-100 + related_outreach_id link | `--color-surface-raised` + `--color-text` per chip | GET /v1/growth/pr/coverage (read-only); UI re-renders on cron `growth-pr-coverage-tracking.js` (daily 05:00 UTC) |
| Brand-awareness rollup | `<.c-card>` + period numeric brand_awareness_value sum display | `--color-surface-raised` + `--color-text` | GET /v1/growth/pr/coverage/rollup (period query) |
| Banned-lexicon validator | (runtime check) | (n/a) | runs BEFORE `external.send` on `pr_outreach.pitch_subject` + `pr_outreach.pitch_body` + `review_records.response_draft` content; T0-04 Tenant 0 public-proof boundary applied per 213 inheritance |

**Wired event types**: `pr_target_relationship_advanced` / `pr_outreach_drafted` / `pr_outreach_sent` / `pr_review_ingested` / `pr_review_response_drafted` / `pr_review_response_sent` / `pr_review_request_dispatched` / `pr_coverage_mention_ingested` / `pr_brand_awareness_rolled_up`.

**Cron-handler-driven UI updates**: `growth-pr-coverage-tracking.js` (daily) + `growth-review-request-cadence.js` (daily) → coverage browser + review records timeline re-render.

---

## Future-Surface UI Binding Contract 5 — Partnership-Affiliate-Developer

For future P220+ admin/tenant surfaces consuming Domain 5 substrate (`partner_profiles` + `partnerships` + `affiliate_programs` + `affiliate_commissions` + `developer_content` + `developer_events` + 4 DB-trigger compliance functions + prohibited-methods validator + Pitfall 8 pricing-drift detector + billing P215 HARD wiring). Until those phases ship, surfaces render `<PlaceholderBanner variant="future_phase_220_partnership_console">` / `<PlaceholderBanner variant="future_phase_220_affiliate_console">` / `<PlaceholderBanner variant="future_phase_220_developer_marketing_console">`.

| Surface element | DESIGN.md primitive | Token / class | Mutation wiring |
|-----------------|--------------------|----|-----------------|
| Partner profile editor | `<.c-card>` + `<.c-input>` + 8-partner_kind chip (`technology` / `referral` / `affiliate` / `co_marketing` / `reseller` / `white_label` / `consulting` / `agency`) + 5-status chip (`invited` / `active` / `watch` / `paused` / `terminated`) + contract_url link | `--color-surface-raised` + `--color-primary` | POST /v1/growth/partnerships/profiles (F-222) |
| Partnership activation queue | `<.c-card>` per partnership + 7-partnership_kind chip + 4-status timeline (`proposed` → `active` → `paused` / `ended`) + activation modal trigger | `--color-text` + `--color-success` (active) + `--color-warning` (proposed) | PATCH /v1/growth/partnerships/{id} (F-223) |
| **Partnership activation approval modal** | `<.c-modal>` + `<.c-backdrop>` + joint_kpi preview + shared_assets list + audit log | `--color-surface-raised` + `--color-primary` (approve) | POST /v1/growth/partnerships/{id}/activate → triggers `buildApprovalPackage(kind='partnership_activation')` → P208 inbox 24th chip; DB-trigger `fn_partnership_activation_requires_approval` enforces approved_at presence |
| Affiliate program editor | `<.c-card>` + 3-status chip (`active` / `paused` / `invite_only`) + 4-commission_type chip (`flat_fee` / `percentage_mrr` / `percentage_arr` / `hybrid`) + commission_rate_jsonb editor with `<Money fromPricingRecommendation />` XOR sentinel + cookie_window day picker + minimum_payout dollar picker + 5-tracking_provider chip + 4-payment_method chip + 2-payment_frequency chip | `--color-surface-raised` + `--color-text` per chip; `--color-warning` for sentinel | POST /v1/growth/partnerships/affiliate-programs (F-224); DB-trigger `fn_affiliate_program_requires_pricing_context` enforces pricing_recommendation_id for percentage_mrr/percentage_arr/hybrid |
| **Prohibited-methods list** | `<.c-card>` + chip row with default array (`paid_search_brand_terms` / `email_spam` / `trademark_misuse`) + edit button | `--color-surface-raised` + `--color-text` per chip; `--color-error` on violation indicator | client-side validation; DB-trigger `fn_affiliate_program_prohibited_methods_required` enforces `cardinality(prohibited_methods) > 0` (defense-in-depth) |
| Affiliate commission tracker | `<.c-card>` per commission + 4-status timeline (`earned` → `locked` → `paid` / `refunded`) + immutability indicator when locked_at IS NOT NULL + commission_amount_cents display | `--color-text` + `--color-success` (paid) + `--color-warning` (locked) + `<.c-badge--info>` "[info] Locked: refunds via separate row" | GET /v1/growth/partnerships/affiliate-programs/{id}/commissions; DB-trigger `fn_affiliate_commission_immutability` blocks UPDATE on locked rows |
| **Affiliate commission issuance approval modal** | **REUSES 215 billing-correction modal recipe verbatim** — `<.c-modal>` + `<.c-backdrop>` + `<BillingCorrectionPanel />` + `<PricingContextChip />` (215 inheritance) resolved via Pricing Engine MCP tool + `<PIIRedactedField />` (217 inheritance) for partner contact email + audit log + billing P215 HARD wired indicator | `--color-surface-raised` + `--color-primary` | POST /v1/growth/partnerships/affiliate-programs/{id}/issue-commission → triggers `buildApprovalPackage(kind='affiliate_commission_issuance')` → P208 inbox 25th chip; mutation-class `billing.charge` (215 HARD wired); ALWAYS routes through `lib/markos/billing` via `createBillingHandoff`; NEVER direct Stripe writes |
| **Partner payout export approval modal** | **REUSES 215 billing-correction modal recipe verbatim** — `<.c-modal>` + `<.c-backdrop>` + `<BillingCorrectionPanel />` + payout_amount_cents display + dual-approval indicator when amount > 100000 + payout_compliance_acknowledged_at gate indicator + manual CSV export FINAL formatter step indicator + audit log + billing P215 HARD wired indicator | `--color-surface-raised` + `--color-primary` (approve) + `--color-warning` (dual-approval indicator) | POST /v1/growth/partnerships/payouts-export → triggers `buildApprovalPackage(kind='partner_payout_export')` → P208 inbox 26th chip; mutation-class `billing.charge` + `data.export`; `default_approval_mode == dual_approval` when payout_amount_cents > 100000 (mirrors 219 high-discount dual-approval pattern); manual CSV export is FINAL formatter step on top of billing-engine output (Pitfall 2 fix) |
| Developer content library | `<.c-card>` per content + 6-content_kind chip (`tutorial` / `api_reference` / `code_sample` / `starter_kit` / `technical_blog` / `changelog_entry`) + 3-freshness_status chip (`fresh` / `stale` / `broken`) + last_validated_at timestamp + 4-validation_kind chip | `--color-surface-raised` + `--color-success` (fresh) + `--color-warning` (stale) + `--color-error` (broken) + bracketed glyphs | GET /v1/growth/developer/content (F-225); UI re-renders on cron `growth-developer-content-validation.js` (weekly Sun 02:00 UTC) |
| **Developer content editor with pricing-drift detector** | `<.c-card>` + `<.c-input>` + `<PricingDriftDetectorBadge />` (Pitfall 8 — flags tutorials/technical_blogs with hardcoded numeric pricing strings unless evidence_refs links to PricingRecommendation OR contains `{{MARKOS_PRICING_ENGINE_PENDING}}` sentinel) | `--color-warning` (drift detected) + `<.c-notice c-notice--warning>` "[warn] Pricing drift detected" | client-side check; runtime validation per Pitfall 8 |
| Developer events ingestion view | `<.c-card>` per event + 9-event_kind chip + payload_jsonb display + consumed_by_phase tracking + P227 ALTERable indicator | `--color-surface-raised` + `--color-text` per chip | GET /v1/growth/developer/events |
| Prohibited-methods violation viewer | `<.c-card>` per violation + 3-method chip + audit log + `<.c-badge--error>` "[err] Violation detected" | `--color-error` + bracketed glyph | GET /v1/growth/partnerships/affiliate-programs/violations; emits `buildApprovalPackage(kind='affiliate_prohibited_method_violation')` on detection |
| Banned-lexicon validator | (runtime check) | (n/a) | runs BEFORE `external.send` on partner co-marketing copy + affiliate program copy + developer marketing tutorial body content |

**Wired event types**: `partnership_activated` / `affiliate_program_activated` / `affiliate_commission_computed` / `affiliate_commission_locked` / `partner_payout_exported` / `developer_content_published` / `developer_content_freshness_checked` / `developer_content_pricing_drift_detected` / `prohibited_method_violation_detected`.

**Cron-handler-driven UI updates**: `growth-developer-content-validation.js` (weekly) → developer content library re-renders freshness_status column.

---


## Future-Surface UI Binding Contract 6 — 28 Growth Agents Readiness

For future P220+ admin/tenant surfaces consuming Domain 6 substrate (`growth_agent_readiness` registry + 28 agent rows pre-populated runnable=false + GENERATED runnable column + activation gate DB-trigger + SG-07 experimentation_kind classification + 19 API + 6 cron + 12 read + 3 write-gated MCP tools). Until those phases ship, surfaces render `<PlaceholderBanner variant="future_phase_220_agents_page_extension">`. The 28 P220 growth agents readiness rows render alongside 9 P218 PLG + 9 P219 B2B + 12 P217 SAS rows on the existing 217-06 `app/saas/agents/page.tsx` agents page (extension is deferred to future phase).

| Surface element | DESIGN.md primitive | Token / class | Mutation wiring |
|-----------------|--------------------|----|-----------------|
| 28 growth agents readiness viewer | `<.c-card>` per row + agent_token + agent_name + 7-tier P220-relevant chip (`P220_AGENT_TIERS = ['VRL','CMT','EVT','PR','PRT','DEV','REV']`) + 12-tier full chip available for cross-phase view (`AGENT_TIERS = ['PLG','EXP','ABM','VRL','IAM','CMT','EVT','XP','PR','PRT','DEV','REV']`) + 8-readiness-boolean checklist + runnable badge + experimentation_kind chip + blocking_phase indicator | `--color-surface-raised` + `--color-text` per chip | GET /v1/growth/agents/readiness (F-226 read-only) |
| 8-readiness-boolean checklist | `<.c-card>` with 8 checkboxes (read-only display): `contracts_assigned` + `cost_estimated` + `approval_posture_defined` + `tests_implemented` + `api_surface_defined` + `mcp_surface_defined` + `ui_surface_defined` + `failure_behavior_defined` | each checkbox `--color-success` (true) or `--color-text-muted` (false) + bracketed glyphs `[ok]` / `[…]` | per-flag PATCH /v1/growth/agents/readiness/{agent_token} (each PATCH writes audit row) |
| **GENERATED runnable column verification** | `<.c-badge>` driven by 8-boolean AND chain | `<.c-badge--error>` "[block] Agent not yet runnable" when runnable=false (paired with `--color-error`); `<.c-badge--success>` "[ok] Agent ready" when GENERATED runnable=true (paired with `--color-success`); Pitfall 9 regression — direct INSERT runnable=true is silently overridden by GENERATED clause | runtime; SQL-level enforcement (cannot be set directly) |
| **SG-07 experimentation_kind chip** | `<.c-chip>` per agent rendering 1-of-6 ENUM (`registry` / `ice_backlog` / `guardrail` / `approval_gate` / `decision_record` / `learning_promotion`) | `--color-surface-raised` + `--color-text` per chip | NOT NULL on every seeded row (SG-07 sub-criterion classification per agent's primary experimentation contribution) |
| Activation approval modal | `<.c-modal>` + `<.c-backdrop>` + 8-readiness-boolean checklist preview + audit log + activation_approval_id + readiness_check_id linkage | `--color-surface-raised` + `--color-primary` (approve) | POST /v1/growth/agents/readiness/{agent_token}/activate → triggers `buildApprovalPackage` → DB-trigger `fn_growth_agent_activation_gate` enforces approval_id + readiness_check_id presence (defense-in-depth on top of 8-boolean GENERATED runnable column) |
| Cross-phase pre-seed indicator | `<.c-badge--info>` "[info] Cross-phase pre-seed (P218/P219)" for non-P220-owned tier rows (PLG/EXP/ABM/IAM/XP) | `--color-info` + bracketed glyph | runtime indicator on rows where blocking_phase IS NOT NULL or agent_tier ∉ P220_AGENT_TIERS |
| First-run validation checklist (Plan 06 `checkpoint:human-action`) | `<.c-card c-card--feature>` + 8-step ordered list (per Plan 06 Task 3 §how-to-verify) | `--color-primary` (mint reserved for hero card) + `--color-text` | operator-driven; cron handlers DO NOT auto-flip readiness; SG-10 invariant maintained until GSD assigns full readiness per agent |

**Wired event types**: `growth_agent_readiness_flag_flipped` / `growth_agent_activation_attempted`.

**Per-agent tier population** (verbatim per Plan 06 Task 1 §Step 2 — 19 P220-owned agents + cross-phase pre-seeds for SG-10 invariant continuity):

| Tier | Agent tokens (P220-relevant) | Count | SG-07 experimentation_kind primary |
|------|------------------------------|-------|------------------------------------|
| VRL (Viral) | MARKOS-AGT-VRL-01..02 | 2 | guardrail |
| CMT (Community) | MARKOS-AGT-CMT-01..03 | 3 | guardrail |
| EVT (Events) | MARKOS-AGT-EVT-01..03 | 3 | guardrail |
| PR (PR/Analyst/Review) | MARKOS-AGT-PR-01..04 | 4 | approval_gate |
| PRT (Partnerships) | MARKOS-AGT-PRT-01..03 | 3 | approval_gate |
| DEV (Developer) | MARKOS-AGT-DEV-01..02 | 2 | guardrail |
| REV (Revenue) | MARKOS-AGT-REV-01..02 | 2 | learning_promotion (REV-01) / registry (REV-02) |
| **P220-owned subtotal** | | **19** | |
| Cross-phase pre-seeds | PLG-01..06 + EXP-01..03 + ABM-01..03 + IAM-01 + XP-01..02 | 15 | per primary SG-07 sub-criterion (ice_backlog dominant; EXP-02='registry', EXP-03='decision_record') |
| **Total seeded rows in growth_agent_readiness** | | per Plan 06 must_haves | |

**Disambiguation note** — the orchestrator and Plan 06 truths line cite "28 growth agents" as the canonical aggregate. The literal P220-relevant tier enumeration (19 P220-owned + cross-phase pre-seeds) reflects what migration 97 INSERT ON CONFLICT seeds; the "28" figure is preserved verbatim per orchestrator directive without re-litigating count. The growth_agent_readiness table is system-level (NOT tenant-scoped) and service-role-only via deny-all-except-service-role RLS.

**Cron-handler-driven UI updates**: none — readiness flag flips are operator-driven only (Plan 06 `checkpoint:human-action` enforced; cron handlers DO NOT auto-flip).

---

## Banned-Lexicon Enforcement Summary (220 expansion)

Phase 220 ships extensive external content across 6 domains. Banned-lexicon zero-match required pre-dispatch on every `external.send` and `billing.charge` mutation per 213 inheritance. The validator-count expands as follows (mirror 216/218/219 pattern):

| Domain | Mutation classes added | Validator placement |
|--------|------------------------|---------------------|
| Domain 1 (Referral) | `referral_reward_issuance` (`billing.charge`) | `lib/markos/referral/reward-issuance.ts` BEFORE `buildApprovalPackage` call |
| Domain 2 (Community) | `community_moderation_action` (`external.send`) | `lib/markos/community/moderation-approvals.ts` BEFORE `buildApprovalPackage` call |
| Domain 3 (Events) | `event_activation` + `event_external_invite` + `event_post_sequence` (`external.send`) | `lib/markos/events/approval-gates.ts` BEFORE `buildApprovalPackage` call; reminder body validated in `lib/markos/events/reminders.ts` BEFORE `dispatchPendingReminders` |
| Domain 4 (PR) | `pr_pitch_send` + `pr_review_response` + `pr_pricing_profile_sync` + `pr_review_request_send` (`external.send`) | `lib/markos/pr/outreach.ts` + `lib/markos/pr/reviews.ts` BEFORE `buildApprovalPackage` call |
| Domain 5 (Partnership/Affiliate/Developer) | `partnership_activation` + `affiliate_program_activation` + `affiliate_commission_issuance` + `partner_payout_export` (`external.send` / `billing.charge` / `data.export`) + developer marketing tutorial copy | `lib/markos/partnerships/payout-compliance.ts` + `lib/markos/partnerships/developer-marketing.ts` BEFORE `buildApprovalPackage` call |
| Domain 6 (Growth Agents) | n/a (system registry; no external mutation) | n/a |

**Total new mutation classes pre-dispatch validated** = 11 (across Domains 1–5). Validator implementation follows the 216 pattern (`lib/markos/banned-lexicon/check.ts` reused; expand fixture file with 220-specific test cases). Validation happens at app-layer; sentinel `{{MARKOS_PRICING_ENGINE_PENDING}}` is accepted as a non-banned token (sentinel discipline carry per 215 inheritance).

---

## Sentinel Discipline Carry (215 inheritance)

The pricing pending sentinel `{{MARKOS_PRICING_ENGINE_PENDING}}` is accepted in:

| DB-trigger | Phase / migration | Acceptance condition |
|------------|-------------------|----------------------|
| Pricing-context gate on `referral_programs` | 220-01 migration 91 | reward_description LIKE '%MARKOS_PRICING_ENGINE_PENDING%' on cash/discount/credit reward types |
| `fn_affiliate_program_requires_pricing_context` | 220-05 migration 95 | EITHER `pricing_recommendation_id IS NOT NULL` OR commission copy contains `{{MARKOS_PRICING_ENGINE_PENDING}}` (SG-11 hard floor with sentinel exception) |
| `fn_review_request_g2_pricing_context` | 220-04 migration 94 | EITHER `pricing_engine_context_id IS NOT NULL` OR template body contains `{{MARKOS_PRICING_ENGINE_PENDING}}` (Pitfall 4 fix) |
| Pitfall 8 developer marketing pricing-drift detector | 220-05 `developer-marketing.ts` | tutorials with hardcoded numeric pricing strings flagged unless `evidence_refs` links to PricingRecommendation OR contains `{{MARKOS_PRICING_ENGINE_PENDING}}` sentinel |

Per CLAUDE.md placeholder rule: unresolved pricing/packaging/usage/billing copy uses `{{MARKOS_PRICING_ENGINE_PENDING}}` until an approved `PricingRecommendation` exists. P220 affiliate commission rates, referral reward dollar values, partner payout rates, G2/Capterra pricing copy, and event ticket pricing all consume Pricing Engine context or use the placeholder sentinel.

---

## End-of-Milestone State (v4.1.0 SaaS Suite Activation — final phase)

Phase 220 is the **closing phase of the v4.1.0 SaaS Suite Activation milestone** (Phases 214→215→216→217→218→219→**220**). The v4.1.0 milestone end-state is recorded here for downstream phases (P221+ commercial-engines lane and P227 ecosystem ALTER) to consume:

### Approval Inbox handoff_kind chip count growth

| Phase | Chips Added | Cumulative Total | Notable Literals |
|-------|------------|------------------|-----------------|
| Pre-217 baseline (post-216) | (4 P207 + P214 `billing_charge_approval` + P215 `billing_correction_approval` + P216 `support_response_approval` + P216 `save_offer_approval`) | 8 | core + billing + support |
| 217 (no new chips — UI shell) | 0 | 8 | (217 ships UI consumer) |
| 218 (PLG + In-App + Experimentation) | 4 | 12 | `pql_hot_transition_review` + `upgrade_trigger_activate` + `inapp_campaign_activate` + `experiment_activate` |
| 219 (B2B Expansion + ABM + Revenue Alignment) | 8 | 19 | `expansion_outreach_approval` + `program_activation_approval` + `abm_outreach_approval` + `review_request_dispatch_approval` + `proof_publish_approval` + `discount_authorization_approval` + `expansion_offer_approval` + `save_offer_approval` (19th in canonical chain) |
| **220 (Community + Events + PR + Partnership + Affiliate + Developer + Growth Agents)** | **7** | **26** | `community_moderation_approval` (20th) + `event_promotion_approval` (21st) + `pr_pitch_approval` (22nd) + `g2_review_pricing_approval` (23rd) + `partnership_activation_approval` (24th) + `affiliate_commission_issuance_approval` (25th) + `partner_payout_export_approval` (26th) |

**End-of-v4.1.0 P208 Approval Inbox filter chip set** = **26 handoff_kind literals** (rendering of 217→218→219→220 new chips deferred to future P208 admin extension phases).

### Agent readiness registry growth

| Phase | Agent Rows Added | SaaS-tier Cumulative | Tier Coverage |
|-------|------------------|----------------------|---------------|
| 217 (12 SAS agents seeded planned-only via saas_nav_visibility surface) | 12 | 12 | SAS-01..12 (UI/admin agents) |
| 218 (9 PLG agents in plg_agent_readiness — system-level registry) | 9 | 21 | PLG-01..06 + IAM-01 + EXP-01..02 (P218 owns; runnable=false until criteria met) |
| 219 (9 B2B agents in b2b_growth_agent_readiness — system-level registry) | 9 | 30 | EXP-01..03 + ABM-01..03 + REV-01..02 + IAM-01 (P219 owns; runnable=false until criteria met; cross-phase visibility row marked blocking_phase='P218-06-CONFIRM') |
| **220 (28 growth agents in growth_agent_readiness — system-level registry; ON CONFLICT seed accommodates P218/P219 cross-phase rows per Pitfall 12)** | **28 (canonical aggregate per Plan 06 must_haves; literal enumeration shows 19 P220-owned tiers VRL/CMT/EVT/PR/PRT/DEV/REV plus cross-phase pre-seeds for SG-10 invariant continuity)** | **per orchestrator-cited 9+9+28 = 46 SaaS-tier aggregate** | VRL-01..02 + CMT-01..03 + EVT-01..03 + PR-01..04 + PRT-01..03 + DEV-01..02 + REV-01..02 (19 P220-owned) + cross-phase pre-seeds (PLG/EXP/ABM/IAM/XP) |

**End-of-v4.1.0 SaaS-tier agent readiness aggregate** = the orchestrator-cited "9+9+28 = 46 agents" reflects the per-phase additions (PLG P218 + B2B P219 + Growth P220). The 28 P220 figure is the canonical Plan 06 aggregate (which includes cross-phase pre-seeds for SG-10 invariant continuity per Pitfall 12 ON CONFLICT DO UPDATE seed pattern). The 217-06 `app/saas/agents/page.tsx` rendering surface composes all SaaS-tier agent readiness rows alongside 12 P217 SAS agents — visible total = 58 rows when 217-06 agents-page extension ships.

### Migration slot map (v4.1.0 final state)

| Slot range | Phase | Tables added |
|------------|-------|--------------|
| 81-89 | 201 (markos_orgs base) + early phases | tenant + auth + governance + audit core |
| 96 | 207-related | neuro_literacy_metadata (existing — P220 skips this slot) |
| 97-99 (planning estimate, may slip) | 217 / 218 (TBD per V4.1.0-MIGRATION-SLOT-COORDINATION.md) | saas_nav_visibility + saas_growth_profiles + module_mode_eligibility seed |
| 100-103 | (reserved per Q-7 RESOLUTION; coordination doc pins exact assignments) | (218 PLG + In-App + Experimentation) |
| 104-106 | (P218 closeout) | plg_agent_readiness + activation criteria seed |
| 107-111 | 219 (Q-7 RESOLVED slot reallocation from 85-89 to 107-111) | revenue_team_configs + lead_qualification_sla_events + marketing_sales_feedback_records + b2b expansion + customer_marketing + abm + advocacy + b2b_pricing + b2b_growth_agent_readiness |
| **90-95 + 97 (skip 96)** | **220 (this phase)** | **referral + community + events + pr + partnerships + growth_agent_readiness (per V4.1.0-MIGRATION-SLOT-COORDINATION.md slot allocation)** |

**Note on slot allocation conflict** (per RESEARCH §Critical sequencing finding): P220 slots 90-97 may collide with P218/P219 if those phases land before 220. The Plan 01 baseline guard test asserts no migrations 90-97 + no F-209..F-227 contracts exist before this plan ships; if collision detected, the planner switches to "P220 inherits next free slot via assertUpstreamReady" path. The V4.1.0-MIGRATION-SLOT-COORDINATION.md document (created by P218 first, appended by P219, appended by P220) is the canonical reservation registry.

### F-ID slot map (v4.1.0 final state)

| F-ID range | Phase | Contracts |
|------------|-------|-----------|
| F-001..F-105 | Pre-v4.1.0 (P201..P217) | core + auth + governance + tenant + 215 billing + 216 health + 217 saas-ui |
| F-106..F-112 | 218 (PLG + In-App + Experimentation) | saas_growth_profiles + activation_definitions + pql_scores + upgrade_triggers + inapp_campaigns + marketing_experiments + plg_agent_readiness |
| F-113..F-121 | 219 (B2B Expansion subset; P226-P228 take F-163..F-198) | account_expansion_opportunities + customer_marketing + abm + advocacy + b2b_pricing + b2b_agents (overlap with 219 F-228..F-237 — verified per V4.1.0-MIGRATION-SLOT-COORDINATION.md cosmetic ordering — F-IDs are not constraint-bearing) |
| **F-209..F-227** | **220 (this phase — sequential post-P227 F-198 per Q-3 path-A; cosmetic ordering)** | **F-209 referral-program + F-210 referral-invitation + F-211 viral-loop-metrics + F-212 referral-payout + F-213 community-profile + F-214 community-signal + F-215 event + F-216 event-registration + F-217 event-results + F-218 pr-target + F-219 pr-outreach + F-220 pr-review + F-221 pr-review-request + F-222 partner-profile + F-223 partnership + F-224 affiliate-program + F-225 developer-content + F-226 growth-agent-readiness + F-227 growth-mcp-tools** |

### Translation gates state at end of v4.1.0

**Dissolved by P220** (at backend-substrate layer):
- 217 `future_phase_220_referral_console`
- 217 `saas_nav_visibility` 12 SG planned-only seed (community/events/PR/partnership rows become activated-eligible)
- 218 + 219 `future_phase_220_*` placeholders consumed
- 213-04 public-proof boundary on PR pitches + G2 reviews

**Opened by P220** (for future P220+ admin/tenant frontend phases):
- `future_phase_220_admin_ui` (multi-page community/events/PR/partnership/affiliate/developer admin surface)
- `future_phase_220_referral_console` (tenant-facing referral console)
- `future_phase_220_event_marketing_console`
- `future_phase_220_pr_console`
- `future_phase_220_partnership_console`
- `future_phase_220_affiliate_console`
- `future_phase_220_developer_marketing_console`
- `future_phase_220_approval_inbox_extensions` (7 new handoff_kind chips render)
- `future_phase_220_agents_page_extension` (28 growth agents readiness rows render)
- `future_phase_227_ecosystem_alter` (5 P220-owned tables ALTERable for ecosystem columns)

**Carried forward** (still open from earlier phases):
- All 217+218+219 future-phase placeholders for admin/tenant UI rendering remain DEFERRED to future phases
- `future_phase_227_ecosystem_alter` becomes load-bearing once P220 substrate lands

### v4.2.0 readiness signal

P220 closes v4.1.0 — the v4.2.0 milestone (Ecosystem Engine; P227) can now begin, gated on:
1. P227 `assertUpstreamReady` HARD upstream check confirms all P220 SOR tables present (verified by `test/growth-220/migration-shape/p220-p227-altertable-readiness.test.js` asserting 5 P227-ALTERable tables ship WITHOUT ecosystem columns)
2. P227 migration 160 ALTERs partner_profiles + referral_programs + community_profiles + marketing_events + partnerships ADD business_mode + ecosystem columns additively (per 227-RESEARCH.md migration 160 SQL)
3. P227 owns the agnostic ecosystem engine (business_mode='ecosystem' or 'all') that EXTENDS P220 SaaS-mode (b2b/b2c/plg_b2b/plg_b2c/b2b2c) implementation
4. P227 D-78 + 227-CONTEXT.md line 12 explicitly reserves `lib/markos/ecosystem/*` for the ecosystem engine; P220's `lib/markos/growth/*` umbrella is sibling, not parent

### v4.1.0 milestone closeout status

- [x] P214 SaaS Suite Activation Subscription Core
- [x] P215 SaaS Suite Billing Payments Compliance
- [x] P216 SaaS Suite Health Churn Support Usage
- [x] P217 SaaS Suite Revenue Agents API UI
- [x] P218 SaaS Growth Profile PLG In-App Experimentation
- [x] P219 SaaS B2B Expansion ABM Revenue Alignment
- [ ] **P220 SaaS Community Events PR Partnership DevRel Growth (this phase — pending plan-gate + execute-gate + verify-gate; UI-SPEC drafted 2026-05-04)**
- [ ] v4.1.0 milestone closeout (post-P220; ROADMAP marker)

When P220 ships, v4.1.0 SaaS Suite Activation milestone CLOSES. The next milestone is v4.2.0 Ecosystem Engine (P227 + P228 commercial-engines lane).

---

## Checker Sign-Off

This is a no-UI-scope phase. The 6 design quality dimensions are evaluated for the no-UI declaration only — pass criteria is "no UI surface declared in any plan" rather than visual/copywriting compliance:

- [ ] Dimension 1 Copywriting: PASS (no copywriting required for own surfaces; banned-lexicon validator placement contracts documented for future surfaces in §Banned-Lexicon Enforcement Summary)
- [ ] Dimension 2 Visuals: PASS (no visuals required; future-surface UI binding contracts cite DESIGN.md primitives verbatim)
- [ ] Dimension 3 Color: PASS (no color decisions for own surfaces; DESIGN.md v1.1.0 60/30/10 inheritance for future surfaces)
- [ ] Dimension 4 Typography: PASS (no typography decisions for own surfaces; DESIGN.md v1.1.0 JetBrains Mono + Inter inheritance for future surfaces)
- [ ] Dimension 5 Spacing: PASS (no spacing decisions for own surfaces; DESIGN.md v1.1.0 8px-grid inheritance for future surfaces)
- [ ] Dimension 6 Registry Safety: PASS (no shadcn registry; no third-party blocks; vetting gate not applicable)
- [ ] Scope verification: PASS (orchestrator grep confirmed NO UI PATHS FOUND IN ANY PLAN; if checker re-runs grep and finds any UI surface in plan files_modified blocks, BLOCK)

**Approval:** pending (gsd-ui-checker upgrades to approved on no-UI-scope verification)

---

## File Inheritance Citations

**206-UI-SPEC.md** (mutation-class doctrine inheritance — `external.send` for community moderation + event reminders + PR pitch + review request + partner outreach + developer marketing; `billing.charge` for referral reward issuance + affiliate commission + partner payout — billing P215 HARD wired; `data.export` for audit log)

**207-UI-SPEC.md** (`RunApiEnvelope` + `AgentRunEventType` + `ApprovalHandoffRecord`; `run_id` linked to fraud-evaluator + signals fan-out + reminder dispatch + sentiment classifier + commission tracker + 28 agent runs)

**208-UI-SPEC.md** (PARENT — Approval Inbox extension; 7 NEW handoff_kind chips → 26 chips post-220 END OF v4.1.0)

**209-UI-SPEC.md** (PARENT — Pitfall 3 PR pitch evidence-binding cardinality + Q-5 SOFT FK upgrade once P209 ships; EVD-01 + EVD-02 carry; immutable evidence_refs pattern; Pitfall 8 developer marketing pricing-drift applies EVD-01)

**213-UI-SPEC.md** (213-04 public-proof boundary on PR pitches + G2 reviews + reviews; banned-lexicon zero-match BEFORE `external.send` / `billing.charge` dispatch)

**214-UI-SPEC.md** (PARENT — SaaS Suite Activation; every 220 SOR table FK to `markos_orgs(tenant_id)`; 6 of 12 P218 modules now reference 220 substrate per mode-eligibility)

**215-UI-SPEC.md** (PARENT — sentinel `{{MARKOS_PRICING_ENGINE_PENDING}}` discipline; 4-DB-trigger pricing compliance pack; **billing P215 HARD wiring** for affiliate commission issuance + partner payout export + referral reward issuance; 215 billing-correction modal recipe REUSED for affiliate_commission_issuance_approval + partner_payout_export_approval + referral_reward_issuance approval)

**216-UI-SPEC.md** (PARENT — `saas_health_scores` consumed by referral fraud-evaluator + community health + PR brand-awareness; `<HealthScoreBadge />` + `<RiskBandBadge />` + `<RetentionClassChip />` extracted-component recipes referenced as load-bearing primitives)

**217-UI-SPEC.md** (PARENT — `saas_nav_visibility` consumer eventually; 28 growth agents extend 217-06 agents page; 7 NEW extracted components referenced as load-bearing primitives; `future_phase_220_referral_console` placeholder DISSOLVED at backend-contract layer)

**218-UI-SPEC.md** (PARENT — `markos_growth_mode` ENUM eligibility for referral/community/developer modules + b2b/b2b2c for PR/partnerships + all-modes for events; 60-row module_mode_eligibility seed; 28 growth agents readiness pattern reused verbatim from 218 9 PLG agents pattern)

**219-UI-SPEC.md** (PARENT — 9 B2B agents readiness pattern reused; 219 19th handoff_kind chain predecessor; 6-future-surface UI binding contracts pattern reused verbatim with 6 parallel contracts; sibling translation gates `future_phase_220_agents_page_extension` + `future_phase_220_approval_inbox_extensions`)

**213.4-VALIDATION.md** §Carry-forward (D-08 token-only, D-09 mint-as-text, D-09b `.c-notice` mandatory, D-13 `.c-card--feature` reserved, D-14 no `.c-table`, D-15 selective extraction, D-21 server/client boundary)

**DESIGN.md v1.1.0** (canonical visual contract — full token set, primitive set, banned lexicon, motion + a11y rules)

---

End of UI-SPEC for Phase 220 (no-UI-scope variant — final phase of v4.1.0 SaaS Suite Activation milestone).
