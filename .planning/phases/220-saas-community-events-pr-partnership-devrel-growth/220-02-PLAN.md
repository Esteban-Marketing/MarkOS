---
phase: 220
plan: "02"
type: execute
wave: 2
depends_on:
  - "220-01"
files_modified:
  - supabase/migrations/92_growth_community.sql
  - lib/markos/community/contracts.ts
  - lib/markos/community/contracts.cjs
  - lib/markos/community/profiles.ts
  - lib/markos/community/profiles.cjs
  - lib/markos/community/health.ts
  - lib/markos/community/signals.ts
  - lib/markos/community/moderation-approvals.ts
  - lib/markos/community/index.cjs
  - contracts/F-213-growth-community-profile-v1.yaml
  - contracts/F-214-growth-community-signal-v1.yaml
  - contracts/flow-registry.json
  - scripts/preconditions/220-02-check-upstream.cjs
  - test/growth-220/community/profiles.test.js
  - test/growth-220/community/signals-fan-out.test.js
  - test/growth-220/community/health-rollup.test.js
  - test/growth-220/community/moderation-approvals.test.js
  - test/growth-220/community/db-trigger-moderation-gate.test.js
  - test/growth-220/community/dedup-idempotency.test.js
  - test/growth-220/rls/community-cross-tenant-denial.test.js
  - test/growth-220/openapi/parity-community.test.js
autonomous: true
requirements:
  - SG-06
  - SG-09
  - QA-01
  - QA-02
  - QA-03
must_haves:
  truths:
    - Wave 2 entry — assertUpstreamReady preflight (scripts/preconditions/220-02-check-upstream.cjs) consumed from Plan 01 architecture-lock surface; hard-fails if 220-01 helper module trees missing OR P214/P215/P218 tables missing
    - Migration 92 ships per V4.1.0-MIGRATION-SLOT-COORDINATION.md slot allocation; CONTAINS 3 SOR tables (community_profiles, community_signals, community_actions) + 1 DB-trigger function + 1 dedup UNIQUE INDEX (per Pitfall 5)
    - community_profiles RLS-enabled with tenant_id policy; community_signals RLS-enabled; community_actions RLS-enabled with moderation_approval_id FK to approval_packages (soft-FK to P208 — SOFT_UPSTREAM degrade)
    - DB-trigger compliance — fn_community_action_moderation_gate BEFORE INSERT on community_actions raises EXCEPTION 'COMMUNITY_MODERATION_REQUIRES_APPROVAL' when action_kind IN ('ban','mute','post_removal','post_external_brand') AND moderation_approval_id IS NULL AND current_setting('app.community_writer_source', true) IS DISTINCT FROM 'approved_workflow'
    - Dedup pitfall 5 — UNIQUE INDEX on community_signals(tenant_id, community_id, source_post_id) WHERE source_post_id IS NOT NULL; INSERT path uses ON CONFLICT DO NOTHING; tested via dedup-idempotency.test.js
    - 8 signal_kind ENUM values land per RESEARCH §6 Domain 2 (active_member_signal, feature_confusion_signal, competitor_mention_signal, content_gap_signal, advocacy_candidate_signal, churn_risk_signal, feature_request_signal, review_signal)
    - moderation-approvals.ts wires buildApprovalPackage on every external community post + moderation action (ban/mute/post_removal); kind='community_moderation_action'; idempotent via Idempotency-Key on action_id
    - Signals fan-out — community_signals INSERT triggers async enqueue for downstream phases (P216 health_score, P219 advocacy pipeline, support intelligence) via consumed_at + consumed_by_phase tracking columns
    - 2 contracts ship F-213 + F-214 (community-profile + community-signal) with NO collision; flow-registry.json + openapi.json regenerated
    - Architecture-lock test passes against lib/markos/community/* + api/v1/growth/community/* (when those API handlers ship later in Plan 06) — no createApprovalPackage / requireSupabaseAuth / etc.
    - Cross-tenant RLS denial proven across 3 community SOR tables
  artifacts:
    - path: supabase/migrations/92_growth_community.sql
      provides: community_profiles (16 cols — community_id PK + tenant_id FK + community_type/primary_platform/secondary_platforms[] ENUMs + member_count + monthly_active_members + health_score CHECK 0-100 + goals[] + launch_status ENUM + community_url + audit cols) + community_signals (10 cols — signal_id PK + tenant_id + community_id FK + signal_kind ENUM 8 values + source_post_id + source_member_id + source_url + payload_jsonb + consumed_at + consumed_by_phase + UNIQUE(tenant_id, community_id, source_post_id) WHERE source_post_id IS NOT NULL) + community_actions (12 cols — action_id PK + tenant_id + community_id FK + action_kind ENUM (ban/mute/post_removal/post_external_brand/conversion_incentive_send) + target_member_id + target_post_id + reason + moderation_approval_id soft-FK to approval_packages + initiated_by + executed_at + created_at) + RLS on all 3 + idx_community_signals_unconsumed ON (tenant_id, signal_kind, created_at) WHERE consumed_at IS NULL + fn_community_action_moderation_gate trigger + DO $$ ASSERT ... END $$ existence check
    - path: lib/markos/community/contracts.ts
      provides: 6 enum constants (COMMUNITY_TYPES, COMMUNITY_PLATFORMS, COMMUNITY_LAUNCH_STATUSES, COMMUNITY_SIGNAL_KINDS, COMMUNITY_ACTION_KINDS, COMMUNITY_GOALS) + zod schemas (CommunityProfileSchema, CommunitySignalSchema, CommunityActionSchema)
      exports: [COMMUNITY_TYPES, COMMUNITY_PLATFORMS, COMMUNITY_LAUNCH_STATUSES, COMMUNITY_SIGNAL_KINDS, COMMUNITY_ACTION_KINDS, COMMUNITY_GOALS, CommunityProfileSchema, CommunitySignalSchema, CommunityActionSchema]
    - path: lib/markos/community/profiles.ts
      provides: CRUD operations on community_profiles + health_score recompute trigger; consumes runtimeContext.tenant_id from requireHostedSupabaseAuth
      exports: [createCommunityProfile, updateCommunityProfile, getCommunityProfile, listCommunityProfiles]
    - path: lib/markos/community/health.ts
      provides: rollupCommunityHealth — computes health_score from member_count/monthly_active_members ratio + post_velocity (community_signals where signal_kind='active_member_signal' last 30d) + response_time_p50; bounded 0-100
      exports: [rollupCommunityHealth, computeActivityComponent, computeResponseComponent]
    - path: lib/markos/community/signals.ts
      provides: ingestCommunitySignal — INSERT with ON CONFLICT DO NOTHING (dedup); fan-out enqueue to downstream phases via outbox pattern; consumed_at + consumed_by_phase update on dispatch
      exports: [ingestCommunitySignal, consumeCommunitySignal, listUnconsumedSignals]
    - path: lib/markos/community/moderation-approvals.ts
      provides: requestModerationAction — calls buildApprovalPackage with kind='community_moderation_action' on ban/mute/post_removal/post_external_brand; returns { approval_id, action_id }; conversion-incentive-send approval-gated via SG-12
      exports: [requestModerationAction, ModerationActionInput]
    - path: contracts/F-213-growth-community-profile-v1.yaml
      provides: GET /v1/growth/community/profiles, POST /v1/growth/community/profiles, GET /v1/growth/community/profiles/{id}, PATCH /v1/growth/community/profiles/{id}; auth via requireHostedSupabaseAuth
    - path: contracts/F-214-growth-community-signal-v1.yaml
      provides: GET /v1/growth/community/signals (filter by signal_kind, consumed status, community_id, period); read-only contract for operator review surface
    - path: scripts/preconditions/220-02-check-upstream.cjs
      provides: CLI preflight; calls assertUpstreamReady from lib/markos/growth/preflight; verifies P214/P215/P218 tables + 220-01 module tree (lib/markos/growth/preflight/index.cjs exists); exit 1 on hard miss
    - path: test/growth-220/community/dedup-idempotency.test.js
      provides: INSERT same source_post_id twice → second INSERT is no-op (ON CONFLICT DO NOTHING); SELECT returns 1 row not 2; covers Pitfall 5
    - path: test/growth-220/community/db-trigger-moderation-gate.test.js
      provides: service-role direct INSERT on community_actions with action_kind='ban' AND moderation_approval_id=NULL AND no GUC set → expect EXCEPTION 'COMMUNITY_MODERATION_REQUIRES_APPROVAL'; with approval_id set → succeeds; with GUC app.community_writer_source='approved_workflow' → succeeds
  key_links:
    - from: lib/markos/community/moderation-approvals.ts
      to: lib/markos/crm/agent-actions.ts
      via: const { buildApprovalPackage } = require('../crm/agent-actions') — kind='community_moderation_action' on ban/mute/post_removal
      pattern: "buildApprovalPackage"
    - from: supabase/migrations/92_growth_community.sql
      to: lib/markos/community/contracts.ts
      via: COMMUNITY_SIGNAL_KINDS array (8 values) mirrors community_signals.signal_kind CHECK constraint exactly
      pattern: "COMMUNITY_SIGNAL_KINDS"
    - from: lib/markos/community/signals.ts
      to: supabase/migrations/92_growth_community.sql
      via: ingestCommunitySignal uses INSERT ... ON CONFLICT (tenant_id, community_id, source_post_id) DO NOTHING; idempotency tested
      pattern: "ON CONFLICT.*DO NOTHING"
    - from: scripts/preconditions/220-02-check-upstream.cjs
      to: lib/markos/growth/preflight/upstream-gate.ts
      via: const { assertUpstreamReady } = require('../../lib/markos/growth/preflight/index.cjs')
      pattern: "assertUpstreamReady"
---

<objective>
Wave 2 plan A — translate community-led growth into a governed workflow per RESEARCH §6 Domain 2. Ship community_profiles + community_signals + community_actions SOR (migration 92), the moderation-action approval gate via buildApprovalPackage, the DB-trigger compliance enforcement (fn_community_action_moderation_gate per RESEARCH Compliance enforcement boundary), the dedup UNIQUE INDEX (per Pitfall 5), the 8-value signal_kind taxonomy (active_member / feature_confusion / competitor_mention / content_gap / advocacy_candidate / churn_risk / feature_request / review), and the signals fan-out pipeline (consumed_at + consumed_by_phase tracking).

Purpose: satisfy SG-06 (community as governed workflow), SG-09 (community modules emit tasks/approvals/learnings — NO passive dashboards; signals fan out to downstream phases), QA-01..03 (architecture-lock + helper-presence + RLS).

Output: 1 SOR migration + 6 community lib files + 2 contracts + flow-registry update + 1 preflight CLI + 7 community/RLS test files + 1 OpenAPI parity test.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-CONTEXT.md
@.planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-RESEARCH.md
@.planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-01-SUMMARY.md
@.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@obsidian/brain/SaaS Marketing OS Strategy Canon.md
@obsidian/work/incoming/17-SAAS-MARKETING-OS-STRATEGY.md
@CLAUDE.md
@lib/markos/growth/preflight/index.cjs
@lib/markos/growth/preflight/upstream-gate.ts
@lib/markos/crm/agent-actions.ts
@onboarding/backend/runtime-context.cjs
@supabase/migrations/90_growth_referral_programs.sql

<interfaces>
<!-- Imports from Plan 01 — already shipped; consume directly. -->

From lib/markos/growth/preflight/index.cjs:
- assertUpstreamReady({ supabase, hard?, soft? }) → Promise<{ warnings: Array<{ phase, table }> }>
- REQUIRED_UPSTREAM, SOFT_UPSTREAM constants
- scanForbiddenPatterns({ paths }) → Array<{ file, line, pattern, snippet }>
- assertHelpersPresent() → void (throws on missing)

From lib/markos/crm/agent-actions.ts:68:
- buildApprovalPackage({ kind, tenant_id, initiated_by, payload }) → ApprovalPackage with approval_id

From onboarding/backend/runtime-context.cjs:491:
- requireHostedSupabaseAuth({ req, runtimeContext, operation, requiredProjectSlug, env })

Existing contract registry:
- contracts/flow-registry.json contains F-001..F-105 (existing) + F-209..F-212 (Plan 01)
- Plan 02 appends F-213, F-214 with NO overlap
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Wave 2 entry preflight + slot reservation verify</name>
  <files>scripts/preconditions/220-02-check-upstream.cjs</files>
  <read_first>
    - lib/markos/growth/preflight/upstream-gate.ts (Plan 01 output — assertUpstreamReady signature)
    - .planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md (verify P220-02 owns slot 92)
    - scripts/preconditions/220-01-check-upstream.cjs (Plan 01 pattern — replicate)
    - 220-RESEARCH.md §assertUpstreamReady Preflight Design
  </read_first>
  <action>
    Step 1: Verify Plan 01 outputs exist before continuing — if `lib/markos/growth/preflight/index.cjs` not present, hard-fail with message "Plan 220-01 not yet shipped; cannot proceed to 220-02".
    Step 2: Verify migration slot 92 unallocated — `ls supabase/migrations/92_*.sql 2>/dev/null` returns empty.
    Step 3: Create scripts/preconditions/220-02-check-upstream.cjs replicating 220-01 pattern; calls assertUpstreamReady from lib/markos/growth/preflight/index.cjs; adds Plan-02-specific upstream check: P210 connector_installs (SOFT — community signal ingest fallback to manual CSV per RESEARCH §6 Domain 2 External integrations).
    Step 4: Run preflight CLI — must exit 0 on green env (no community tables yet exist; baseline guard).
  </action>
  <acceptance_criteria>
    - `node scripts/preconditions/220-02-check-upstream.cjs` exits 0 with green env (or 1 with proper UpstreamPhaseNotLandedError message)
    - `grep -E "createApprovalPackage|requireSupabaseAuth|app/(growth)|route.ts|vitest" scripts/preconditions/220-02-check-upstream.cjs | wc -l` returns 0
    - `ls supabase/migrations/92_*.sql` returns empty (slot reserved, not yet claimed by this task)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Community SOR migration (92) + DB-trigger moderation gate + dedup UNIQUE INDEX</name>
  <files>supabase/migrations/92_growth_community.sql, test/growth-220/community/profiles.test.js, test/growth-220/community/dedup-idempotency.test.js, test/growth-220/community/db-trigger-moderation-gate.test.js, test/growth-220/rls/community-cross-tenant-denial.test.js</files>
  <behavior>
    - Test 1: After migration 92, `\d+ community_profiles` shows 16 columns; community_type CHECK 5 values (practitioner/user_forum/customer_only/open_education/developer); primary_platform CHECK 8 values (slack/discord/circle/discourse/linkedin_group/reddit/github_discussions/custom); launch_status CHECK 4 values (not_started/soft_launch/active/mature); health_score CHECK BETWEEN 0 AND 100; RLS enabled
    - Test 2: After migration 92, community_signals shows 10 cols; signal_kind CHECK 8 values; UNIQUE INDEX idx_community_signals_dedup ON (tenant_id, community_id, source_post_id) WHERE source_post_id IS NOT NULL
    - Test 3: After migration 92, community_actions shows 12 cols; action_kind CHECK 5 values; FK to community_profiles
    - Test 4: dedup-idempotency — INSERT community_signals with source_post_id='SLK-12345' twice (same tenant + community) → second INSERT is no-op via ON CONFLICT DO NOTHING; SELECT count = 1
    - Test 5: dedup-idempotency — INSERT community_signals with source_post_id=NULL twice → both succeed (NULL not in UNIQUE per WHERE clause); count = 2
    - Test 6: db-trigger-moderation-gate — service-role direct INSERT community_actions with action_kind='ban' + moderation_approval_id=NULL + GUC unset → expect EXCEPTION 'COMMUNITY_MODERATION_REQUIRES_APPROVAL'
    - Test 7: db-trigger — INSERT with moderation_approval_id set → succeeds
    - Test 8: db-trigger — SET LOCAL app.community_writer_source = 'approved_workflow' + INSERT without approval_id → succeeds (workflow-bypass with audit trail)
    - Test 9: db-trigger — INSERT action_kind='conversion_incentive_send' without approval_id (different kind) → expect EXCEPTION (per SG-12 — overlap with referral domain still requires approval)
    - Test 10: cross-tenant RLS denial across all 3 tables
  </behavior>
  <read_first>
    - 220-RESEARCH.md §6 Domain 2 (lines 407-484, especially Compliance enforcement boundary + Approval-gate triggers + Test strategy)
    - 220-RESEARCH.md §Pitfall 5 (community signal duplication — UNIQUE on source_post_id WHERE NOT NULL)
    - 220-RESEARCH.md §6 Domain 2 External integrations (Slack/Discord/Discourse/Circle deferred to v4.3.0; v1 manual CSV)
    - obsidian/work/incoming/17-SAAS-MARKETING-OS-STRATEGY.md lines 864-901 (CommunityProfile TS interface)
    - supabase/migrations/90_growth_referral_programs.sql (Plan 01 pattern — same RLS + tenant_id + comment header style)
    - .planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md (slot 92 reservation)
  </read_first>
  <action>
    Step 1: Write tests 1-10 BEFORE migration (TDD RED). Use shared test helpers from Plan 01.
    Step 2: Write supabase/migrations/92_growth_community.sql per RESEARCH §6 Domain 2:
      - 3 CREATE TABLE statements + RLS enable + tenant_id policy on each
      - community_actions adds 5th action_kind ENUM value 'conversion_incentive_send' (overlaps with referral conversion-incentive-send — both domains require approval per SG-12)
      - UNIQUE INDEX idx_community_signals_dedup ON community_signals(tenant_id, community_id, source_post_id) WHERE source_post_id IS NOT NULL
      - idx_community_signals_unconsumed ON community_signals(tenant_id, signal_kind, created_at) WHERE consumed_at IS NULL
      - CREATE OR REPLACE FUNCTION fn_community_action_moderation_gate() RETURNS trigger; checks NEW.action_kind IN ('ban','mute','post_removal','post_external_brand','conversion_incentive_send') AND NEW.moderation_approval_id IS NULL AND COALESCE(current_setting('app.community_writer_source', true), '') IS DISTINCT FROM 'approved_workflow' → RAISE EXCEPTION 'COMMUNITY_MODERATION_REQUIRES_APPROVAL';
      - CREATE TRIGGER trg_community_action_moderation_gate BEFORE INSERT ON community_actions FOR EACH ROW EXECUTE FUNCTION fn_community_action_moderation_gate();
      - DO $$ ASSERT existence of all 3 tables + trigger present END $$
      - Comment header: `-- Plan 220-02 / Phase 220 / Slot 92 / RESEARCH §6 Domain 2 / DB-trigger compliance + Pitfall 5 dedup`
    Step 3: NO business_mode column on community_profiles (P227 owns ALTER per RESEARCH §P220 → P227 ALTER TABLE).
    Step 4: Apply + run all 10 tests (TDD GREEN).
  </action>
  <acceptance_criteria>
    - `npm test -- test/growth-220/community/profiles.test.js test/growth-220/community/dedup-idempotency.test.js test/growth-220/community/db-trigger-moderation-gate.test.js test/growth-220/rls/community-cross-tenant-denial.test.js` all green (10 assertions)
    - `psql -c "SELECT count(*) FROM information_schema.tables WHERE table_name IN ('community_profiles','community_signals','community_actions')"` returns 3
    - `psql -c "SELECT count(*) FROM pg_indexes WHERE indexname IN ('idx_community_signals_dedup','idx_community_signals_unconsumed')"` returns 2
    - `psql -c "SELECT count(*) FROM pg_trigger WHERE tgname = 'trg_community_action_moderation_gate'"` returns 1
    - `grep -E "business_mode" supabase/migrations/92_growth_community.sql` returns 0 (P227 owns ALTER)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Community lib + 2 contracts + signals fan-out + moderation-approval wiring</name>
  <files>lib/markos/community/contracts.ts, lib/markos/community/contracts.cjs, lib/markos/community/profiles.ts, lib/markos/community/profiles.cjs, lib/markos/community/health.ts, lib/markos/community/signals.ts, lib/markos/community/moderation-approvals.ts, lib/markos/community/index.cjs, contracts/F-213-growth-community-profile-v1.yaml, contracts/F-214-growth-community-signal-v1.yaml, contracts/flow-registry.json, test/growth-220/community/signals-fan-out.test.js, test/growth-220/community/health-rollup.test.js, test/growth-220/community/moderation-approvals.test.js, test/growth-220/openapi/parity-community.test.js</files>
  <behavior>
    - Test 1 (signals-fan-out): each of 8 signal_kind values when ingested correctly enqueues for the right downstream phase: active_member→P216_health_score, feature_confusion→support_intelligence, competitor_mention→competitive_intel, content_gap→content_strategy, advocacy_candidate→P219_advocacy, churn_risk→SAS-07_health, feature_request→product_intelligence, review_signal→PR-04_review_pipeline
    - Test 2 (signals-fan-out): consumeCommunitySignal({ signal_id, consumed_by_phase }) sets consumed_at timestamp + consumed_by_phase; idempotent (second call is no-op)
    - Test 3 (signals-fan-out): listUnconsumedSignals({ tenant_id, signal_kind?, since? }) returns only rows where consumed_at IS NULL; uses idx_community_signals_unconsumed index
    - Test 4 (health-rollup): rollupCommunityHealth computes health_score = 0.4*activity_component + 0.3*post_velocity + 0.3*response_component; bounded 0-100
    - Test 5 (health-rollup): empty community (no signals) → health_score = 0 (not error)
    - Test 6 (health-rollup): full activity (200 active_member_signals last 30d, 50 monthly_active_members) → activity_component near 100
    - Test 7 (moderation-approvals): requestModerationAction({ tenant_id, community_id, action_kind: 'ban', target_member_id, reason, initiated_by }) calls buildApprovalPackage with kind='community_moderation_action'; returns { approval_id, action_id }
    - Test 8 (moderation-approvals): action_kind='post_external_brand' (post on behalf of brand) calls buildApprovalPackage; community_actions row inserted with moderation_approval_id set
    - Test 9 (moderation-approvals): same Idempotency-Key on retry returns same approval_id (no double-charge)
    - Test 10 (openapi-parity): F-213 + F-214 YAML schemas match lib/markos/community/contracts.ts zod
    - Test 11 (openapi-parity): flow-registry.json contains F-213 + F-214 with no overlap against F-001..F-198 + F-209..F-212
  </behavior>
  <read_first>
    - 220-RESEARCH.md §6 Domain 2 (lines 407-484)
    - 220-RESEARCH.md §6 Domain 2 Test strategy (specific test files + per-signal-kind fan-out check)
    - lib/markos/referral/* (Plan 01 pattern — same import shape + CommonJS twin pattern)
    - obsidian/work/incoming/17-SAAS-MARKETING-OS-STRATEGY.md lines 864-901 (CommunityProfile TS interface — column types are source-of-truth)
    - lib/markos/crm/agent-actions.ts:68 (buildApprovalPackage kind values pattern)
    - contracts/F-209-growth-referral-program-v1.yaml (Plan 01 contract pattern — replicate for F-213/F-214)
  </read_first>
  <action>
    Step 1: Write tests 1-11 BEFORE implementations (TDD RED).
    Step 2: Implement lib/markos/community/contracts.ts:
      - 6 enum constants per behavior list
      - zod schemas matching migration 92 columns exactly
      - TS types via z.infer
      - CJS twin re-exports constants
    Step 3: Implement lib/markos/community/health.ts:
      - rollupCommunityHealth({ tenant_id, community_id }) — SELECT activity counts + monthly_active + post_velocity from community_signals last 30d; UPDATE community_profiles SET health_score = computed
      - computeActivityComponent({ active_signals, monthly_active }): number 0-100
      - computeResponseComponent: median response time from community_signals → 100 - normalize(median_minutes)
    Step 4: Implement lib/markos/community/signals.ts:
      - ingestCommunitySignal({ tenant_id, community_id, signal_kind, source_post_id?, source_member_id?, source_url?, payload }): uses INSERT ... ON CONFLICT (tenant_id, community_id, source_post_id) DO NOTHING when source_post_id provided; otherwise plain INSERT
      - listUnconsumedSignals({ tenant_id, signal_kind?, since?, limit? })
      - consumeCommunitySignal({ signal_id, consumed_by_phase }): UPDATE consumed_at = now() + consumed_by_phase; idempotent (only when consumed_at IS NULL)
      - SIGNAL_FAN_OUT_MAP = { 'active_member_signal': 'P216_health_score', 'feature_confusion_signal': 'support_intelligence', ... } (8 entries)
    Step 5: Implement lib/markos/community/moderation-approvals.ts:
      ```typescript
      const { buildApprovalPackage } = require('../crm/agent-actions');
      export async function requestModerationAction({ tenant_id, community_id, action_kind, target_member_id, target_post_id, reason, initiated_by, idempotency_key }) {
        const pkg = buildApprovalPackage({
          kind: 'community_moderation_action',
          tenant_id,
          initiated_by,
          payload: { community_id, action_kind, target_member_id, target_post_id, reason },
          idempotency_key,
        });
        // INSERT community_actions row with moderation_approval_id = pkg.approval_id
        // SET LOCAL app.community_writer_source = 'approved_workflow' to permit DB-trigger pass
        const action = await insertCommunityAction({ ..., moderation_approval_id: pkg.approval_id });
        return { approval_id: pkg.approval_id, action_id: action.action_id };
      }
      ```
    Step 6: Implement lib/markos/community/profiles.ts CRUD; CJS twin; index.cjs re-exports.
    Step 7: Write F-213 + F-214 contract YAMLs per RESEARCH §6 Domain 6 API surface table.
    Step 8: Update contracts/flow-registry.json appending F-213 + F-214 entries.
    Step 9: Run all tests (TDD GREEN). Run npm run openapi:build.
  </action>
  <acceptance_criteria>
    - `npm test -- test/growth-220/community/` all green (11+ assertions)
    - `npm test -- test/growth-220/openapi/parity-community.test.js` green
    - `grep "F-213\|F-214" contracts/flow-registry.json | wc -l` returns 2
    - `grep -rn "createApprovalPackage|requireSupabaseAuth|requireTenantContext|serviceRoleClient|lookupPlugin|app/(growth)|route.ts|vitest|from 'vitest'|.test.ts" lib/markos/community/ | wc -l` returns 0
    - `node -e "console.log(typeof require('./lib/markos/community/index.cjs').requestModerationAction)"` outputs `function`
    - `npm run openapi:build` completes; contracts/openapi.json contains /v1/growth/community/profiles + /v1/growth/community/signals
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Slack/Discord webhook → /v1/growth/community/signals | Untrusted external; signature verification deferred to P227 (v1 manual CSV) |
| Operator → POST /v1/growth/community/moderation-actions | Untrusted operator; buildApprovalPackage required for ban/mute/post_removal |
| Service-role direct INSERT on community_actions | DB-trigger fn_community_action_moderation_gate blocks unless approval_id set OR GUC=approved_workflow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-220-02-01 | Tampering | Service-role direct ban without approval | mitigate | DB-trigger fn_community_action_moderation_gate; tested via db-trigger-moderation-gate.test.js |
| T-220-02-02 | Tampering | Duplicate signal ingest (Slack webhook + nightly poll) | mitigate | UNIQUE INDEX on (tenant_id, community_id, source_post_id) WHERE source_post_id IS NOT NULL + ON CONFLICT DO NOTHING |
| T-220-02-03 | Information Disclosure | Cross-tenant community_signals leakage | mitigate | RLS on tenant_id; tested via cross-tenant denial suite |
| T-220-02-04 | Repudiation | Operator denies authorizing moderation | mitigate | buildApprovalPackage writes audit row + approval_id immutable post-decision |
| T-220-02-05 | Spoofing | Webhook signature forgery (Slack signing-secret, Discord Ed25519) | accept (v1) | Deferred to P227 D-29 implementation; v1 manual CSV operator workflow |
| T-220-02-06 | Information Disclosure | PII in source_member_id (Slack user IDs) leaked to logs | accept | source_member_id is platform-internal; not PII per OAuth-scope contract |
| T-220-02-07 | Tampering | conversion_incentive_send overlaps referral domain — double-approval | mitigate | DB-trigger gates BOTH community_actions + referral_invitations (Plan 01); approval_id required on both paths; idempotency via Idempotency-Key |
</threat_model>

<verification>
- `npm test -- test/growth-220/community/ test/growth-220/rls/community-* test/growth-220/openapi/parity-community.test.js` all green
- `node scripts/preconditions/220-02-check-upstream.cjs` exits 0
- `psql -c "SELECT count(*) FROM information_schema.tables WHERE table_name IN ('community_profiles','community_signals','community_actions')"` returns 3
- `grep -rn "createApprovalPackage|requireSupabaseAuth|app/(growth)|route.ts|vitest" lib/markos/community/ scripts/preconditions/220-02-check-upstream.cjs | wc -l` returns 0
- `grep "F-213\|F-214" contracts/flow-registry.json | wc -l` returns 2
</verification>

<success_criteria>
- 3 community SOR tables ship with RLS + 2 indexes + 1 DB-trigger
- 8 signal_kind taxonomy locked with fan-out map to downstream phases
- Moderation actions approval-gated at app + DB layers (defense-in-depth per P226 D-83/D-84 model)
- Dedup pitfall 5 resolved via UNIQUE INDEX + ON CONFLICT DO NOTHING; tested
- 2 F-IDs ship F-213 + F-214; flow-registry no overlap; openapi.json regenerated
- Domain 2 architecture-lock PASS — 0 forbidden patterns
</success_criteria>

<output>
After completion, create `.planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-02-SUMMARY.md` documenting:
- Migration slot claimed: 92
- F-IDs claimed: F-213, F-214
- Tables created: community_profiles, community_signals, community_actions
- Triggers created: trg_community_action_moderation_gate (BEFORE INSERT on community_actions)
- Indexes: idx_community_signals_dedup, idx_community_signals_unconsumed
- Signals fan-out map: 8 signal_kind → downstream phase routing
- Architecture-lock PASS for lib/markos/community/* + scripts/preconditions/220-02-check-upstream.cjs
</output>
