---
phase: 220
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md
  - test/fixtures/growth-220/index.js
  - test/fixtures/growth-220/referral-program.js
  - test/fixtures/growth-220/viral-loop-metric.js
  - test/fixtures/growth-220/referral-invitation.js
  - test/fixtures/growth-220/community-profile.js
  - test/fixtures/growth-220/marketing-event.js
  - test/fixtures/growth-220/pr-target.js
  - test/fixtures/growth-220/partner-profile.js
  - test/fixtures/growth-220/affiliate-program.js
  - test/fixtures/growth-220/agent-readiness.js
  - lib/markos/growth/preflight/upstream-gate.ts
  - lib/markos/growth/preflight/architecture-lock.ts
  - lib/markos/growth/preflight/errors.ts
  - lib/markos/growth/preflight/index.cjs
  - scripts/preconditions/220-01-check-upstream.cjs
  - test/growth-220/preflight/upstream-gate.test.js
  - test/growth-220/preflight/architecture-lock.test.js
  - test/growth-220/preflight/helper-presence.test.js
  - test/growth-220/preflight/wave-0-baseline.test.js
  - supabase/migrations/90_growth_referral_programs.sql
  - supabase/migrations/91_growth_referral_triggers.sql
  - lib/markos/referral/contracts.ts
  - lib/markos/referral/contracts.cjs
  - lib/markos/referral/programs.ts
  - lib/markos/referral/programs.cjs
  - lib/markos/referral/metrics.ts
  - lib/markos/referral/fraud-evaluator.ts
  - lib/markos/referral/reward-issuance.ts
  - lib/markos/referral/index.cjs
  - contracts/F-209-growth-referral-program-v1.yaml
  - contracts/F-210-growth-referral-invitation-v1.yaml
  - contracts/F-211-growth-viral-loop-metrics-v1.yaml
  - contracts/F-212-growth-referral-payout-v1.yaml
  - contracts/flow-registry.json
  - test/growth-220/referral/programs.test.js
  - test/growth-220/referral/fraud-evaluator.test.js
  - test/growth-220/referral/reward-issuance.test.js
  - test/growth-220/referral/metrics-rollup.test.js
  - test/growth-220/referral/db-trigger-reward-requires-approval.test.js
  - test/growth-220/referral/db-trigger-pricing-context-required.test.js
  - test/growth-220/rls/referral-cross-tenant-denial.test.js
  - test/growth-220/openapi/parity-referral.test.js
  - .planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-VALIDATION.md
autonomous: true
requirements:
  - SG-04
  - SG-11
  - SG-12
  - QA-01
  - QA-02
  - QA-03
  - QA-04
must_haves:
  truths:
    - Wave 0 baseline guard test asserts no migrations 90-97 and no F-209..F-227 contracts exist before this plan ships (executed FIRST in Wave 1; locks slot allocation per Q-3 resolution path-A continuing after P227 F-198)
    - Migration slot allocation document `.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md` ships with explicit table — P220-01 owns slots 90+91, P220-02 owns 92, P220-03 owns 93, P220-04 owns 94, P220-05 owns 95, P220-06 owns 97 (skip 96 — taken by neuro_literacy_metadata) — coordinating with P218/P219 (currently unallocated; this doc reserves the range)
    - F-ID slot allocation P220 = F-209..F-227 (sequential post-P227 F-198 per Q-3 path-A; cosmetic ordering — F-IDs are not constraint-bearing)
    - assertUpstreamReady preflight CLI `scripts/preconditions/220-01-check-upstream.cjs` hard-fails when REQUIRED_UPSTREAM tables missing (P214 saas_suite_activations, P215 billing tables, P218 saas_growth_profiles); soft-warns when SOFT_UPSTREAM tables missing (pricing_recommendations, agent_runs, evidence_map_records); exit 1 on hard miss
    - Architecture-lock test scans grep across `lib/markos/{growth,referral,community,events,pr,partnerships,growth-agents}` + `api/v1/growth/*` + `api/cron/growth-*` and asserts ZERO occurrences of forbidden patterns — createApprovalPackage, requireSupabaseAuth, requireTenantContext, serviceRoleClient, lookupPlugin, public/openapi.json, app/(public), app/(markos), app/(growth), api/v1/.../route.ts, app/api/cron/.../route.ts, vitest run, from 'vitest', .test.ts, "stub if missing", "if exists", openapi-generate
    - Helper-presence test asserts `buildApprovalPackage` exists at `lib/markos/crm/agent-actions.ts:68`, `requireHostedSupabaseAuth` exists at `onboarding/backend/runtime-context.cjs:491`, `resolvePlugin` exists at `lib/markos/plugins/registry.js:102`, `lib/markos/mcp/tools/index.cjs` exists (CommonJS, not .ts)
    - 3 SOR tables ship in migration 90 — `referral_programs`, `viral_loop_metrics`, `referral_invitations` — all RLS-enabled with `tenant_id` policy (`USING (tenant_id = current_setting('app.tenant_id', true)::uuid)`)
    - DB-trigger compliance pack ships in migration 91 — single-writer trigger on referral_invitations (BEFORE UPDATE raises EXCEPTION 'REWARD_REQUIRES_APPROVAL' when reward_issued_at IS NOT NULL AND reward_approval_id IS NULL); pricing-context trigger on referral_programs (BEFORE INSERT/UPDATE raises EXCEPTION 'REWARD_REQUIRES_PRICING_CONTEXT' when reward_type IN cash/discount/credit AND pricing_recommendation_id IS NULL AND reward_description NOT LIKE %MARKOS_PRICING_ENGINE_PENDING%)
    - 4 contracts ship F-209/F-210/F-211/F-212 with NO collision against F-001..F-105 (existing) and NO collision against P221 F-106..F-112 / P222 F-113..F-121 / P223 F-122..F-131 / P226 F-163..F-180 / P227 F-181..F-198 — verified by `flow-registry.json` regen + parity test
    - `lib/markos/referral/fraud-evaluator.ts` implements 5 rule functions — minAccountAge, requirePaidAccount, selfReferralPrevention, vpnDetection, maxReferralsPerUser — each with positive + negative test cases per rule; no json-logic-js dep
    - `lib/markos/referral/reward-issuance.ts` calls `buildApprovalPackage({ kind: 'referral_reward_issuance', ... })` for every reward issuance with reward_value > 0; pricing-engine-context resolver consumes `pricing_recommendation_id` or `{{MARKOS_PRICING_ENGINE_PENDING}}` sentinel
    - Cross-tenant denial test asserts INSERT under tenant A then SELECT under tenant B returns 0 rows for all 3 referral SOR tables (proves RLS not bypassable via service-role-leak)
    - `220-VALIDATION.md` per-task verification map populated with rows for all 6 plans + frontmatter `nyquist_compliant: true` + `wave_0_complete: true` flipped after Plan 01 closeout
  artifacts:
    - path: .planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md
      provides: P220 migration + F-ID slot allocation table; documents collision resolution with P218/P219 (unallocated; this doc reserves 90-97 P220 + leaves earlier slots for P218/P219 if they need to insert below); coordination flag for P227 ALTER readiness
    - path: lib/markos/growth/preflight/upstream-gate.ts
      provides: assertUpstreamReady runtime helper; throws UpstreamPhaseNotLandedError on hard-miss; logs warning on soft-miss; consumed by every Plan 02-06 first task
      exports: [assertUpstreamReady, REQUIRED_UPSTREAM, SOFT_UPSTREAM]
    - path: lib/markos/growth/preflight/architecture-lock.ts
      provides: forbidden-pattern detector + helper-presence asserter; consumed by architecture-lock test
      exports: [scanForbiddenPatterns, assertHelpersPresent, FORBIDDEN_PATTERNS, REQUIRED_HELPERS]
    - path: lib/markos/growth/preflight/errors.ts
      provides: UpstreamPhaseNotLandedError + ArchitectureLockViolationError typed errors
      exports: [UpstreamPhaseNotLandedError, ArchitectureLockViolationError]
    - path: scripts/preconditions/220-01-check-upstream.cjs
      provides: CLI preflight checker invoked before plan execution; matches `api/cron/webhooks-dlq-purge.js` createClient + supabase pattern; exit 1 on hard miss; exit 0 with stderr warning on soft miss
    - path: supabase/migrations/90_growth_referral_programs.sql
      provides: referral_programs (with pricing_recommendation_id soft-FK + approved_at/approved_by) + viral_loop_metrics (period rollup with k_factor/invite_rate/conversion_rate) + referral_invitations (transactional row per invite with fraud_score/fraud_status/reward_approval_id) — all RLS-enabled tenant_id-scoped per markos_orgs FK pattern (migration 81); 3 hot-path indexes on referral_invitations(tenant_id, program_id) + referral_invitations(tenant_id, referrer_user_id) + viral_loop_metrics UNIQUE(tenant_id, period, referral_program_id); CHECK constraints on status/reward_trigger/channel ENUMs + min_account_age_days >= 0
    - path: supabase/migrations/91_growth_referral_triggers.sql
      provides: BEFORE UPDATE trigger on referral_invitations enforcing reward_approval_id presence when reward_issued_at set; BEFORE INSERT/UPDATE trigger on referral_programs enforcing pricing_recommendation_id presence for monetary reward types unless placeholder sentinel present; BEFORE INSERT/UPDATE trigger on referral_invitations enforcing fraud_status='cleared' when reward_issued_at set (defense-in-depth fraud gate)
    - path: lib/markos/referral/contracts.ts
      provides: 8 enum constants + zod schemas for ReferralProgram, ViralLoopMetric, ReferralInvitation, ReferralReward; parity-tested against F-209..F-212 YAML
      exports: [REFERRAL_PROGRAM_STATUSES, REFERRAL_REWARD_TRIGGERS, REFERRAL_INVITATION_CHANNELS, REFERRAL_FRAUD_STATUSES, FRAUD_CONTROL_KINDS, REFERRAL_REWARD_KINDS, ReferralProgramSchema, ReferralInvitationSchema, ViralLoopMetricSchema]
    - path: lib/markos/referral/fraud-evaluator.ts
      provides: 5 rule evaluators (minAccountAge, requirePaidAccount, selfReferralPrevention, vpnDetection, maxReferralsPerUser); pure TypeScript functions; no external deps; returns FraudEvaluation { score, status: 'cleared'|'flagged'|'blocked', reasons: string[] }
      exports: [evaluateFraud, FraudEvaluation, RuleResult]
    - path: lib/markos/referral/reward-issuance.ts
      provides: issueReferralReward orchestrator; calls buildApprovalPackage for monetary rewards (SG-12); resolves pricing-engine-context via pricing_recommendation_id or returns {{MARKOS_PRICING_ENGINE_PENDING}} sentinel (SG-11); writes audit row via lib/markos/audit
      exports: [issueReferralReward, IssueReferralRewardInput]
    - path: lib/markos/referral/metrics.ts
      provides: rollupViralLoopMetrics; computes k_factor (active_referrers > 0 ? referrals_converted / active_referrers : 0), invitations_per_active_user_monthly, invitation_conversion_rate, viral_channels breakdown; period boundary handling (UTC YYYY-MM)
      exports: [rollupViralLoopMetrics, computeKFactor]
    - path: contracts/F-209-growth-referral-program-v1.yaml
      provides: GET /v1/growth/referral/programs (list with pagination), POST /v1/growth/referral/programs (create draft), GET /v1/growth/referral/programs/{id}, PATCH /v1/growth/referral/programs/{id} (update + activate path with approval gate); request/response schemas mirror lib/markos/referral/contracts.ts zod
    - path: contracts/F-210-growth-referral-invitation-v1.yaml
      provides: GET /v1/growth/referral/invitations (filter by program/referrer), POST /v1/growth/referral/invitations (record invitation with fraud-evaluator pre-check); auth via requireHostedSupabaseAuth
    - path: contracts/F-211-growth-viral-loop-metrics-v1.yaml
      provides: GET /v1/growth/referral/metrics (read viral_loop_metrics with optional period filter); read-only contract
    - path: contracts/F-212-growth-referral-payout-v1.yaml
      provides: POST /v1/growth/referral/payouts (issue reward with buildApprovalPackage gate); response includes approval_id; idempotent via Idempotency-Key header
    - path: contracts/flow-registry.json
      provides: 4 entries appended for F-209..F-212 with NO overlap against F-001..F-105 + P221 F-106..F-112 + P222 F-113..F-121 + P223 F-122..F-131 + P226 F-163..F-180 + P227 F-181..F-198
    - path: test/fixtures/growth-220/index.js
      provides: barrel export of 9 P220 fixture factories used across 220-01..220-06 (referralProgramFactory, viralLoopMetricFactory, referralInvitationFactory, communityProfileFactory, marketingEventFactory, prTargetFactory, partnerProfileFactory, affiliateProgramFactory, agentReadinessFactory)
    - path: test/growth-220/architecture-lock.test.js
      provides: scans 220-*-PLAN.md bodies + lib/markos/{growth,referral,community,events,pr,partnerships,growth-agents}/ + api/v1/growth/ + api/cron/growth-*/ for forbidden patterns; asserts grep count == 0 for ALL forbidden patterns
    - path: test/growth-220/preflight/helper-presence.test.js
      provides: imports lib/markos/crm/agent-actions and asserts buildApprovalPackage is exported function; asserts createApprovalPackage IS NOT EXPORTED; same for requireHostedSupabaseAuth (positive) + requireSupabaseAuth (negative); resolvePlugin (positive) + lookupPlugin (negative); index.cjs exists (positive) + index.ts does NOT exist (negative)
    - path: test/growth-220/referral/db-trigger-reward-requires-approval.test.js
      provides: Postgres-backed test using service-role direct UPDATE on referral_invitations (set reward_issued_at without reward_approval_id) → expect EXCEPTION 'REWARD_REQUIRES_APPROVAL'; proves DB-trigger blocks bypass at db layer regardless of app-layer enforcement
    - path: test/growth-220/referral/db-trigger-pricing-context-required.test.js
      provides: service-role direct INSERT on referral_programs with reward_type='cash' + pricing_recommendation_id=NULL + reward_description='Sign up and get $20!' (no MARKOS_PRICING_ENGINE_PENDING sentinel) → expect EXCEPTION 'REWARD_REQUIRES_PRICING_CONTEXT'
  key_links:
    - from: lib/markos/referral/reward-issuance.ts
      to: lib/markos/crm/agent-actions.ts
      via: const { buildApprovalPackage } = require('../crm/agent-actions') — verified function exists at line 68; SG-12 wiring on every reward_value > 0
      pattern: "require.*'\\.\\./crm/agent-actions'"
    - from: lib/markos/referral/reward-issuance.ts
      to: supabase/migrations/91_growth_referral_triggers.sql
      via: app-layer buildApprovalPackage produces approval_id; DB-trigger asserts approval_id NOT NULL when reward_issued_at set — defense-in-depth pair (matches P226 D-83/D-84 model)
      pattern: "reward_approval_id"
    - from: contracts/F-209-growth-referral-program-v1.yaml
      to: lib/markos/referral/contracts.ts
      via: ReferralProgramSchema zod parity-tested against F-209 YAML — column names + ENUM values + required-field set must match exactly
      pattern: "ReferralProgramSchema"
    - from: scripts/preconditions/220-01-check-upstream.cjs
      to: supabase tables saas_suite_activations + saas_growth_profiles
      via: createClient(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY).from(table).select('*').limit(0); on /relation .* does not exist/ → console.error + process.exit(1)
      pattern: "REQUIRED_UPSTREAM"
    - from: test/growth-220/preflight/architecture-lock.test.js
      to: lib/markos/growth/preflight/architecture-lock.ts
      via: scanForbiddenPatterns(searchPaths) returns array of {file, line, pattern, snippet}; assert array.length == 0
      pattern: "scanForbiddenPatterns"
    - from: .planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md
      to: supabase/migrations/90_growth_referral_programs.sql
      via: doc reserves slots 90-95+97 for P220 plans; subsequent migrations reference doc by file path in commit message
      pattern: "MIGRATION-SLOT-COORDINATION"
---

<objective>
Establish the Wave 1 schema foundation for Phase 220: ship the architecture-lock + assertUpstreamReady preflight surface (Plan 01 owns this for ALL 6 plans), the migration slot coordination document (resolves Q-3 collision with P218/P219/P221+), the referral domain SOR (3 tables: referral_programs + viral_loop_metrics + referral_invitations), the DB-trigger compliance pack (single-writer reward gate + pricing-context gate per RESEARCH §6 Domain 1), 4 F-ID contracts (F-209..F-212), the fraud-evaluator + reward-issuance + metrics-rollup logic, the cross-tenant RLS denial suite, and the populated 220-VALIDATION.md per-task map.

Purpose: satisfy SG-04 (referral programs + viral metrics + fraud controls), SG-11 (Pricing Engine context on monetary rewards), SG-12 (every external mutation approval-gated), QA-01..04 (architecture-lock + helper-presence + RLS isolation + F-ID parity). This plan unblocks every later 220-NN slice — the architecture-lock surface (lib/markos/growth/preflight/) is consumed by Plans 02-06 first tasks; the fixture barrel (test/fixtures/growth-220/) is consumed across all later test files; the migration slot doc constrains Plans 02-06 slot allocation.

Output: 1 coordination doc + 4 preflight files + 1 preflight CLI + 4 preflight test files + 9 fixture files + 2 SOR migrations (90+91) + 5 referral lib files + 4 contract YAMLs + flow-registry update + 6 referral test files + 1 RLS test file + 1 OpenAPI parity test + populated VALIDATION.md.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-CONTEXT.md
@.planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-RESEARCH.md
@.planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-REVIEWS.md
@.planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-VALIDATION.md
@.planning/phases/220-saas-community-events-pr-partnership-devrel-growth/DISCUSS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/V4.0.0-TESTING-ENVIRONMENT-PLAN.md
@.planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md
@obsidian/brain/SaaS Marketing OS Strategy Canon.md
@obsidian/brain/Pricing Engine Canon.md
@obsidian/work/incoming/17-SAAS-MARKETING-OS-STRATEGY.md
@CLAUDE.md
@lib/markos/crm/agent-actions.ts
@onboarding/backend/runtime-context.cjs
@lib/markos/plugins/registry.js
@lib/markos/mcp/tools/index.cjs
@api/cron/webhooks-dlq-purge.js
@supabase/migrations/81_markos_orgs.sql
@supabase/migrations/82_markos_audit_log_hash_chain.sql

<interfaces>
<!-- Verified existing symbols from 2026-04-26 codebase enumeration. Plan 01 wires against these directly. -->
<!-- Forbidden symbols (createApprovalPackage, requireSupabaseAuth, etc.) verified absent — see 220-RESEARCH.md §Existing-Code Support. -->

From lib/markos/crm/agent-actions.ts:68:
```typescript
function buildApprovalPackage(input: BuildApprovalPackageInput): ApprovalPackage
// returns { approval_id: string, ... }
// module.exports = { buildApprovalPackage, ... } at line 133
```

From onboarding/backend/runtime-context.cjs:491:
```javascript
function requireHostedSupabaseAuth({ req, runtimeContext, operation, requiredProjectSlug, env })
// returns { tenant_id, user_id, project_slug, ... }
// module.exports = { requireHostedSupabaseAuth, ... } at line 1014
```

From lib/markos/plugins/registry.js:102:
```javascript
function resolvePlugin(registry, pluginId): object | null
```

From lib/markos/mcp/tools/index.cjs (CommonJS):
```javascript
'use strict';
// 30 live tool descriptors — Plan 06 will add growth.cjs to this registry
```

From api/cron/webhooks-dlq-purge.js (cron pattern):
```javascript
// Reference: x-markos-cron-secret header + MARKOS_*_CRON_SECRET env + writeJson + {success, count, duration_ms} response
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0.1: Wave 0 baseline guards + migration slot coordination doc</name>
  <files>.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md, test/growth-220/preflight/wave-0-baseline.test.js, test/fixtures/growth-220/index.js, test/fixtures/growth-220/referral-program.js, test/fixtures/growth-220/viral-loop-metric.js, test/fixtures/growth-220/referral-invitation.js, test/fixtures/growth-220/community-profile.js, test/fixtures/growth-220/marketing-event.js, test/fixtures/growth-220/pr-target.js, test/fixtures/growth-220/partner-profile.js, test/fixtures/growth-220/affiliate-program.js, test/fixtures/growth-220/agent-readiness.js</files>
  <read_first>
    - .planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-RESEARCH.md §F-ID and Migration Slot Allocation (read FIRST — Q-3 resolution path-A)
    - supabase/migrations/ (verify highest existing slot is 96; 90-95 + 97+ are unallocated)
    - contracts/ (verify highest existing F-ID is F-105; F-106..F-227 range unallocated on disk)
    - .planning/phases/226-sales-enablement-deal-execution/226-01-PLAN.md Task 0 (B6 allocation pattern)
    - .planning/phases/227-ecosystem-partner-community-developer-growth/227-RESEARCH.md (P227 reservations: F-181..F-198, migrations 159-171)
  </read_first>
  <action>
    Step 1: Create `.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md` with explicit table:
      | Phase | Plan | Migration slot(s) | F-ID range | Notes |
      | P220 | 01 | 90, 91 | F-209..F-212 | referral SOR + DB-triggers |
      | P220 | 02 | 92 | F-213, F-214 | community SOR + moderation gate |
      | P220 | 03 | 93 | F-215, F-216, F-217 | events SOR + reminders + attribution |
      | P220 | 04 | 94 | F-218..F-221 | PR + reviews + analyst + coverage |
      | P220 | 05 | 95 | F-222..F-225 | partnerships + affiliate + devrel + DB-triggers |
      | P220 | 06 | 97 | F-226, F-227 | growth_agent_readiness + activation gate (skip 96 = neuro_literacy_metadata existing) |
      | P218 | TBD | reserve 88-89 | reserved | unallocated; expand when plans land |
      | P219 | TBD | reserve none below 90 (P218 owns 88-89 reservation) | reserved | unallocated; if needs slots, must coordinate via amendment to this doc |
      | P221 | per P221 plans | 100-105 | F-106..F-112 | per P221 D-30 |
      | P222 | per P222 plans | 106-112 | F-113..F-121 | per P222 D-49 |
      | P223 | per P223 plans | 113-119 | F-122..F-131 | per P223 D-55 |
      | P226 | per P226 plans | 146-158 | F-163..F-180 | per P226 D-91 |
      | P227 | per P227 plans | 159-171 | F-181..F-198 | per P227 D-66/D-67 |
    Document Q-3 resolution path: P220 takes F-IDs F-209..F-227 sequentially POST-P227 F-198 (F-IDs are not constraint-bearing; ordering is cosmetic). P220 takes migrations 90-95 + 97 BELOW P221's 100-105 range to satisfy psql ascending-order constraint. Include explicit assertion: "P220 migration numbers MUST be < P221's first slot (100) to avoid psql apply-order violation when P220 ALTERs are referenced."
    Add P220 → P227 ALTER TABLE coordination block: lists 5 P220-owned tables (partner_profiles, referral_programs, community_profiles, marketing_events, partnerships) that P227 will ALTER additively in migration 161/162; pins Q-1 resolution (P220 owns base affiliate_programs in migration 95, P227 ALTERs additive ecosystem columns commission_share_locked + ecosystem_attribution_extension + partner_kind_v2; both phases agree per RESEARCH §Pitfall 1 + Q-1).
    Step 2: Create test/growth-220/preflight/wave-0-baseline.test.js using node:test + node:assert/strict:
      - assert no `supabase/migrations/9[01234567]_growth_*.sql` files exist before this plan ships (A1 baseline guard)
      - assert no `contracts/F-209*.yaml`..`F-227*.yaml` files exist (A2 baseline guard)
      - assert `.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md` exists with the table headers from Step 1
      - assert highest existing migration is exactly `96_neuro_literacy_metadata.sql`
    Step 3: Create test/fixtures/growth-220/{9 fixture factories}.js as named-export functions producing object shapes matching the upcoming SOR table columns (referralProgramFactory({ tenant_id, name, status, referrer_reward_jsonb, ... }), etc.). Pure JS, no library deps. Each factory returns an object with sensible defaults; consumers override fields per test. Plus index.js as barrel: `module.exports = { referralProgramFactory, viralLoopMetricFactory, ... }`.
    Step 4: Run wave-0-baseline.test.js — must pass (no growth-220 migrations or contracts exist yet).
  </action>
  <acceptance_criteria>
    - `.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md` exists with the full table for P218..P227 + Q-3 resolution + Q-1 resolution + P220 → P227 ALTER coordination block
    - `node --test test/growth-220/preflight/wave-0-baseline.test.js` passes (5 assertions green)
    - `test/fixtures/growth-220/` directory contains exactly 9 factory files + index.js barrel
    - `node -e "console.log(Object.keys(require('./test/fixtures/growth-220/index.js')))"` outputs all 9 factory names
    - `grep -E "F-2(09|1[0-9]|2[0-7])" contracts/flow-registry.json` returns empty (no contracts written yet — only doc reservation)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 0.5: Architecture-lock + assertUpstreamReady preflight surface</name>
  <files>lib/markos/growth/preflight/upstream-gate.ts, lib/markos/growth/preflight/architecture-lock.ts, lib/markos/growth/preflight/errors.ts, lib/markos/growth/preflight/index.cjs, scripts/preconditions/220-01-check-upstream.cjs, test/growth-220/preflight/upstream-gate.test.js, test/growth-220/preflight/architecture-lock.test.js, test/growth-220/preflight/helper-presence.test.js</files>
  <behavior>
    - Test 1: scanForbiddenPatterns({ paths }) scans for createApprovalPackage / requireSupabaseAuth / requireTenantContext / serviceRoleClient / lookupPlugin / public/openapi.json / app/(public) / app/(markos) / app/(growth) / api/v1/.../route.ts / app/api/cron/.../route.ts / vitest run / from 'vitest' / .test.ts / "stub if missing" / "if exists" / openapi-generate; returns array of violations; should return [] when no violations exist
    - Test 2: scanForbiddenPatterns detects createApprovalPackage in a fixture file under test/growth-220/_fixtures/forbidden-fixture.js and returns array of length 1 with file path + line number + pattern matched
    - Test 3: assertHelpersPresent() asserts buildApprovalPackage exported from lib/markos/crm/agent-actions; assert requireHostedSupabaseAuth exported from onboarding/backend/runtime-context.cjs; assert resolvePlugin exported from lib/markos/plugins/registry.js; assert lib/markos/mcp/tools/index.cjs exists (and index.ts does NOT exist)
    - Test 4: assertUpstreamReady({ supabase, requiredTables, softTables }) — when REQUIRED_UPSTREAM table missing → throws UpstreamPhaseNotLandedError with phase identifier; when SOFT_UPSTREAM table missing → returns warning array (no throw)
    - Test 5: scripts/preconditions/220-01-check-upstream.cjs CLI exits 1 + console.error MISSING_UPSTREAM_PHASE on hard miss; exits 0 + console.warn SOFT_MISSING on soft miss only; exits 0 silently on all-green
    - Test 6: helper-presence test asserts createApprovalPackage NOT exported (negative); requireSupabaseAuth NOT exported (negative); lookupPlugin NOT exported (negative); requireTenantContext NOT exported (negative)
  </behavior>
  <read_first>
    - 220-RESEARCH.md §Architecture-Lock + §assertUpstreamReady Preflight Design
    - lib/markos/crm/agent-actions.ts:68 + 133 (verify exports)
    - onboarding/backend/runtime-context.cjs:491 + 1014 (verify exports)
    - lib/markos/plugins/registry.js:102
    - lib/markos/mcp/tools/index.cjs (verify CommonJS, .cjs extension)
    - api/cron/webhooks-dlq-purge.js (cron auth pattern + createClient + writeJson)
    - .planning/phases/226-sales-enablement-deal-execution/226-01-PLAN.md Task 0.5 (D-78 architecture-lock pattern)
  </read_first>
  <action>
    Step 1: Write the 6 tests in test/growth-220/preflight/{upstream-gate.test.js, architecture-lock.test.js, helper-presence.test.js} BEFORE implementation (TDD RED). Each uses node:test + node:assert/strict. Helper-presence imports the actual codebase modules and asserts function references using `typeof === 'function'` checks.
    Step 2: Implement lib/markos/growth/preflight/errors.ts:
      ```typescript
      export class UpstreamPhaseNotLandedError extends Error {
        constructor(public phase: string, public missingTable: string) {
          super(`MISSING_UPSTREAM_PHASE: ${phase} (table ${missingTable} does not exist)`);
          this.name = 'UpstreamPhaseNotLandedError';
        }
      }
      export class ArchitectureLockViolationError extends Error { ... }
      ```
    Step 3: Implement lib/markos/growth/preflight/architecture-lock.ts with FORBIDDEN_PATTERNS array + scanForbiddenPatterns({ paths }) function using Node fs.readdirSync + fs.readFileSync + regex matching. Skips test/_fixtures/ subdirectory only when forbidden patterns are inside an explicit `// architecture-lock-fixture` marker. Pattern list per RESEARCH §Architecture-Lock Forbidden block:
      - createApprovalPackage, requireSupabaseAuth, requireTenantContext, serviceRoleClient, lookupPlugin
      - public/openapi.json, app/\\(public\\), app/\\(markos\\), app/\\(growth\\), route\\.ts, api/v1/.*/route, api/cron/.*/route
      - vitest run, from ['"]vitest['"], \\.test\\.ts (any non-.js test file in growth-220), "stub if missing", "if exists"
      - openapi-generate, lib/markos/(sales|cdp|conversion|launches|analytics|channels|ecosystem)/ (these are sibling-phase reservations; P220 must not import from them directly)
    Step 4: Implement lib/markos/growth/preflight/upstream-gate.ts:
      ```typescript
      export const REQUIRED_UPSTREAM = [
        { phase: 'P214', table: 'saas_suite_activations' },
        { phase: 'P215', table: 'billing_subscriptions' }, // verify exact table name from P215 plans; placeholder
        { phase: 'P218', table: 'saas_growth_profiles' },
      ];
      export const SOFT_UPSTREAM = [
        { phase: 'P205', table: 'pricing_recommendations' },
        { phase: 'P207', table: 'agent_runs' },
        { phase: 'P208', table: 'approval_packages' },
        { phase: 'P209', table: 'evidence_map_records' },
        { phase: 'P211', table: 'pricing_placeholder_audit' },
        { phase: 'P210', table: 'connector_installs' },
        { phase: 'P212', table: 'cross_tenant_learnings' },
        { phase: 'P216', table: 'health_scores' },
        { phase: 'P217', table: 'sas_agents' },
        { phase: 'P219', table: 'revenue_team_configs' },
      ];
      export async function assertUpstreamReady({ supabase, hard = REQUIRED_UPSTREAM, soft = SOFT_UPSTREAM }) {
        const warnings = [];
        for (const { phase, table } of hard) {
          const { error } = await supabase.from(table).select('*').limit(0);
          if (error && /relation .* does not exist/.test(error.message)) {
            throw new UpstreamPhaseNotLandedError(phase, table);
          }
        }
        for (const { phase, table } of soft) {
          const { error } = await supabase.from(table).select('*').limit(0);
          if (error && /relation .* does not exist/.test(error.message)) {
            warnings.push({ phase, table });
          }
        }
        return { warnings };
      }
      ```
    Step 5: Implement lib/markos/growth/preflight/index.cjs as CommonJS twin re-exporting both upstream-gate + architecture-lock surface (for legacy api/*.js consumers).
    Step 6: Implement scripts/preconditions/220-01-check-upstream.cjs matching api/cron/webhooks-dlq-purge.js createClient pattern; calls assertUpstreamReady; on UpstreamPhaseNotLandedError → console.error + process.exit(1); on warnings → forEach console.warn + exit 0; on success → console.log "P220-01 preflight green" + exit 0.
    Step 7: Run all 3 preflight test files — must pass (TDD GREEN).
  </action>
  <acceptance_criteria>
    - `node --test test/growth-220/preflight/architecture-lock.test.js` passes (2 tests green)
    - `node --test test/growth-220/preflight/helper-presence.test.js` passes (4 positive + 4 negative assertions green)
    - `node --test test/growth-220/preflight/upstream-gate.test.js` passes (mock-supabase tests green)
    - `node scripts/preconditions/220-01-check-upstream.cjs` exits 0 on green env or 1 with MISSING_UPSTREAM_PHASE on missing required table
    - `grep -rn "createApprovalPackage\|requireSupabaseAuth\|lookupPlugin\|requireTenantContext\|serviceRoleClient\|app/(public)\|app/(growth)\|route.ts\|vitest\|openapi-generate" lib/markos/growth/ scripts/preconditions/220-01-check-upstream.cjs | wc -l` returns 0 (architecture-lock applied to its own surface)
    - `node -e "console.log(typeof require('./lib/markos/growth/preflight/index.cjs').assertUpstreamReady)"` outputs `function`
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Referral SOR migrations + DB-trigger compliance pack</name>
  <files>supabase/migrations/90_growth_referral_programs.sql, supabase/migrations/91_growth_referral_triggers.sql, test/growth-220/referral/programs.test.js, test/growth-220/referral/db-trigger-reward-requires-approval.test.js, test/growth-220/referral/db-trigger-pricing-context-required.test.js, test/growth-220/rls/referral-cross-tenant-denial.test.js</files>
  <behavior>
    - Test 1: After migration 90 applies, `\d+ referral_programs` shows all 22 columns from RESEARCH §6 Domain 1 schema (program_id PRIMARY KEY DEFAULT gen_random_uuid, tenant_id NOT NULL FK markos_orgs(org_id), name NOT NULL, status CHECK active|paused|draft, referrer_reward_jsonb NOT NULL, referee_reward_jsonb NOT NULL, reward_trigger CHECK signup|activation|first_payment, referral_link_format text DEFAULT, cookie_window_days int CHECK 1-365, multi_use bool, max_referrals_per_user int, min_account_age_days int CHECK >= 0, require_paid_account bool, self_referral_prevention bool, vpn_detection bool, onboarding_email bool, dashboard_visible bool, share_tools text[], pricing_recommendation_id uuid, created_at, created_by NOT NULL, updated_at, approved_at, approved_by); RLS enabled with tenant_id policy
    - Test 2: After migration 90, `\d+ viral_loop_metrics` shows 16 columns; UNIQUE(tenant_id, period, referral_program_id)
    - Test 3: After migration 90, `\d+ referral_invitations` shows 14 columns; INDEX on (tenant_id, program_id) + (tenant_id, referrer_user_id); UNIQUE on invitation_code
    - Test 4: DB-trigger reward-requires-approval — service-role direct UPDATE on referral_invitations SET reward_issued_at=now() WHERE reward_approval_id IS NULL → expect Postgres EXCEPTION 'REWARD_REQUIRES_APPROVAL'; service-role direct UPDATE with both fields populated → succeeds
    - Test 5: DB-trigger pricing-context — INSERT referral_programs WITH reward_type='cash' + pricing_recommendation_id IS NULL + reward_description NOT containing MARKOS_PRICING_ENGINE_PENDING → expect EXCEPTION 'REWARD_REQUIRES_PRICING_CONTEXT'; INSERT with pricing_recommendation_id set → succeeds; INSERT with reward_description containing {{MARKOS_PRICING_ENGINE_PENDING}} → succeeds
    - Test 6: Cross-tenant RLS denial — INSERT referral_program under tenant A; SELECT under tenant B (via SET LOCAL app.tenant_id) returns 0 rows; same for viral_loop_metrics + referral_invitations
  </behavior>
  <read_first>
    - 220-RESEARCH.md §6 Domain 1 (read SQL block lines 293-378 carefully; column types + CHECK constraints + RLS + indexes)
    - 220-RESEARCH.md §Pitfall 7 (referral fraud_controls bypass — DB CHECK constraint on min_account_age_days >= 0)
    - 220-RESEARCH.md §Pitfall 11 (slot-collision blocks P227 execute — confirm migration 90 < P227 159)
    - supabase/migrations/82_markos_audit_log_hash_chain.sql (audit pattern P220 inherits)
    - supabase/migrations/81_markos_orgs.sql (markos_orgs FK pattern verified)
    - .planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md (Task 0.1 output — confirm P220-01 owns slots 90+91)
  </read_first>
  <action>
    Step 1: Write the 6 tests BEFORE migrations land (TDD RED). Tests use a Postgres testcontainer or local supabase shadow database. Use existing test helper if present (look in test/_helpers/); otherwise inline createClient with test SUPABASE_URL.
    Step 2: Write supabase/migrations/90_growth_referral_programs.sql per RESEARCH §6 Domain 1 SQL block. Include:
      - 3 CREATE TABLE statements (referral_programs, viral_loop_metrics, referral_invitations)
      - ALTER TABLE ... ENABLE ROW LEVEL SECURITY on each
      - CREATE POLICY tenant_isolation USING (tenant_id = current_setting('app.tenant_id', true)::uuid) on each
      - 3 hot-path indexes: idx_referral_invitations_program ON referral_invitations(tenant_id, program_id), idx_referral_invitations_referrer ON referral_invitations(tenant_id, referrer_user_id), UNIQUE constraint on referral_invitations.invitation_code, UNIQUE on viral_loop_metrics(tenant_id, period, referral_program_id)
      - DO $$ BEGIN ASSERT (SELECT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'referral_programs')) END $$ assertion at end
      - NO business_mode column (P227 owns the additive ALTER per RESEARCH §P220 → P227 ALTER TABLE)
      - Comment header citing the source: `-- Plan 220-01 / Phase 220 / Slot 90 / RESEARCH §6 Domain 1`
    Step 3: Write supabase/migrations/91_growth_referral_triggers.sql per RESEARCH §6 Domain 1 Compliance enforcement block. Include:
      - CREATE OR REPLACE FUNCTION fn_referral_reward_requires_approval() RETURNS trigger AS $$ BEGIN IF NEW.reward_issued_at IS NOT NULL AND NEW.reward_approval_id IS NULL THEN RAISE EXCEPTION 'REWARD_REQUIRES_APPROVAL'; END IF; RETURN NEW; END $$ LANGUAGE plpgsql;
      - CREATE TRIGGER trg_referral_reward_requires_approval BEFORE UPDATE ON referral_invitations FOR EACH ROW EXECUTE FUNCTION fn_referral_reward_requires_approval();
      - CREATE OR REPLACE FUNCTION fn_referral_program_requires_pricing_context() — checks reward_type IN ('cash','discount','credit') AND pricing_recommendation_id IS NULL AND reward_description NOT LIKE '%MARKOS_PRICING_ENGINE_PENDING%' → raise EXCEPTION 'REWARD_REQUIRES_PRICING_CONTEXT'
      - CREATE TRIGGER trg_referral_program_requires_pricing_context BEFORE INSERT OR UPDATE ON referral_programs FOR EACH ROW EXECUTE FUNCTION fn_referral_program_requires_pricing_context();
      - Comment header: `-- Plan 220-01 / Phase 220 / Slot 91 / RESEARCH §6 Domain 1 Compliance enforcement boundary / DB-trigger defense-in-depth (matches P226 D-83/D-84)`
    Step 4: Apply migrations to test environment; run all 6 tests (TDD GREEN).
    Step 5: Commit migrations with message: `db(220-01): referral SOR + triggers / slots 90+91 (per V4.1.0-MIGRATION-SLOT-COORDINATION.md)`.
  </action>
  <acceptance_criteria>
    - `npm test -- test/growth-220/referral/programs.test.js` passes (3 column-presence + RLS-enabled assertions green)
    - `npm test -- test/growth-220/referral/db-trigger-reward-requires-approval.test.js` passes (positive + negative cases green)
    - `npm test -- test/growth-220/referral/db-trigger-pricing-context-required.test.js` passes (3 cases: missing pricing_recommendation_id throws, with pricing_recommendation_id succeeds, with placeholder succeeds)
    - `npm test -- test/growth-220/rls/referral-cross-tenant-denial.test.js` passes (3 tables × cross-tenant SELECT returns 0 rows)
    - `psql -c "SELECT count(*) FROM information_schema.tables WHERE table_name IN ('referral_programs', 'viral_loop_metrics', 'referral_invitations')"` returns 3
    - `psql -c "SELECT count(*) FROM pg_trigger WHERE tgname IN ('trg_referral_reward_requires_approval', 'trg_referral_program_requires_pricing_context')"` returns 2
    - `grep -E "business_mode" supabase/migrations/90_growth_referral_programs.sql` returns 0 (P227 owns the ALTER per coordination flag)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Referral lib (programs + fraud-evaluator + reward-issuance + metrics) + 4 contracts + flow-registry sync</name>
  <files>lib/markos/referral/contracts.ts, lib/markos/referral/contracts.cjs, lib/markos/referral/programs.ts, lib/markos/referral/programs.cjs, lib/markos/referral/metrics.ts, lib/markos/referral/fraud-evaluator.ts, lib/markos/referral/reward-issuance.ts, lib/markos/referral/index.cjs, contracts/F-209-growth-referral-program-v1.yaml, contracts/F-210-growth-referral-invitation-v1.yaml, contracts/F-211-growth-viral-loop-metrics-v1.yaml, contracts/F-212-growth-referral-payout-v1.yaml, contracts/flow-registry.json, test/growth-220/referral/fraud-evaluator.test.js, test/growth-220/referral/reward-issuance.test.js, test/growth-220/referral/metrics-rollup.test.js, test/growth-220/openapi/parity-referral.test.js</files>
  <behavior>
    - Test 1 (fraud-evaluator): minAccountAge rule blocks invitation when referrer.account_age_days < program.min_account_age_days; passes when >=; status='blocked' on fail with reasons array
    - Test 2 (fraud-evaluator): requirePaidAccount rule blocks when referrer.paid_at IS NULL AND program.require_paid_account=true
    - Test 3 (fraud-evaluator): selfReferralPrevention rule blocks when referrer_user_id == referee_user_id (and email match for unauthenticated)
    - Test 4 (fraud-evaluator): vpnDetection rule flags (not blocks; status='flagged') when referee.signup_ip flagged via existing classifier; reasons include 'vpn_detected'
    - Test 5 (fraud-evaluator): maxReferralsPerUser rule blocks when COUNT(invitations WHERE referrer_user_id=X AND program_id=Y) >= program.max_referrals_per_user
    - Test 6 (fraud-evaluator): all-clear case returns status='cleared' with empty reasons
    - Test 7 (reward-issuance): issueReferralReward({ invitation_id, reward_kind: 'cash', reward_value_cents: 2000, pricing_recommendation_id: 'pr-123', tenant_id, initiated_by }) calls buildApprovalPackage with kind='referral_reward_issuance' + amount_cents=2000 + pricing_context=resolved-from-pr-123; returns { approval_id }
    - Test 8 (reward-issuance): issueReferralReward without pricing_recommendation_id → returns approval_package with pricing_context = '{{MARKOS_PRICING_ENGINE_PENDING}}' sentinel; does NOT throw (app-layer permits sentinel; DB-trigger blocks raw cash without sentinel)
    - Test 9 (reward-issuance): issueReferralReward with reward_value_cents=0 (free credit) → does NOT call buildApprovalPackage; writes audit row only
    - Test 10 (metrics-rollup): rollupViralLoopMetrics({ tenant_id, period: '2026-04', program_id }) computes k_factor = referrals_converted / active_referrers (or 0 when active_referrers = 0); invitations_per_active_user_monthly; viral_channels breakdown by channel
    - Test 11 (metrics-rollup): period boundary handles UTC YYYY-MM correctly (April 2026-04-01 00:00 UTC to 2026-04-30 23:59:59.999 UTC); does not double-count cross-month invitations
    - Test 12 (openapi-parity): F-209 YAML schema for ReferralProgram matches lib/markos/referral/contracts.ts ReferralProgramSchema zod (column names + types + required-field set); same for F-210/F-211/F-212
    - Test 13 (openapi-parity): contracts/flow-registry.json contains entries for F-209..F-212 with no overlap against F-001..F-198
  </behavior>
  <read_first>
    - 220-RESEARCH.md §6 Domain 1 (lines 287-404, especially Compliance enforcement boundary + Approval-gate triggers + Test strategy)
    - 220-RESEARCH.md §Library Choices (no json-logic-js; hand-coded evaluator)
    - 220-RESEARCH.md §Pitfall 7 (fraud_controls bypass via UI prevented by DB CHECK + approval gate)
    - 220-RESEARCH.md §Code Examples (typescript reward-issuance pattern lines 1681+)
    - obsidian/work/incoming/17-SAAS-MARKETING-OS-STRATEGY.md lines 638-712 (TypeScript interface for ReferralProgram + ViralLoopMetrics — column types are source-of-truth)
    - lib/markos/crm/agent-actions.ts:68 (buildApprovalPackage signature)
    - .planning/phases/226-sales-enablement-deal-execution/226-01-PLAN.md (contracts.ts + flow-registry pattern)
  </read_first>
  <action>
    Step 1: Write tests 1-13 BEFORE implementations (TDD RED). fraud-evaluator tests are pure unit tests (no DB). reward-issuance tests use a mock buildApprovalPackage stub. metrics-rollup test uses fixture data. openapi-parity test reads YAML + zod schema and compares.
    Step 2: Implement lib/markos/referral/contracts.ts:
      - Export 8 enum constants: REFERRAL_PROGRAM_STATUSES = ['active','paused','draft'] as const; REFERRAL_REWARD_TRIGGERS = ['signup','activation','first_payment'] as const; REFERRAL_INVITATION_CHANNELS = ['product_share','referral_link','embed','email','social'] as const; REFERRAL_FRAUD_STATUSES = ['pending','cleared','flagged','blocked'] as const; FRAUD_CONTROL_KINDS = ['min_account_age','require_paid_account','self_referral','vpn_detection','max_referrals_per_user'] as const; REFERRAL_REWARD_KINDS = ['cash','discount','credit','swag','feature_unlock'] as const
      - Export zod schemas: ReferralProgramSchema, ReferralInvitationSchema, ViralLoopMetricSchema, ReferralRewardSchema; mirror migration 90 column types exactly
      - Export TS types: type ReferralProgram = z.infer<typeof ReferralProgramSchema>; etc.
      - CommonJS twin contracts.cjs re-exports the constants (zod usage in CJS files is via dynamic import or skipped)
    Step 3: Implement lib/markos/referral/fraud-evaluator.ts as 5 pure functions + evaluateFraud orchestrator:
      ```typescript
      type FraudEvaluation = { score: number; status: 'cleared'|'flagged'|'blocked'; reasons: string[] };
      export function evaluateFraud({ program, referrer, referee, existingInvitationsCount }): FraudEvaluation { ... }
      // Each rule function returns RuleResult = { passed: boolean; severity: 'block'|'flag'|'pass'; reason: string };
      // Aggregation: any 'block' → status='blocked'; any 'flag' (no block) → 'flagged'; all pass → 'cleared'
      ```
    Step 4: Implement lib/markos/referral/reward-issuance.ts:
      ```typescript
      const { buildApprovalPackage } = require('../crm/agent-actions');
      export async function issueReferralReward({ invitation_id, reward_kind, reward_value_cents, pricing_recommendation_id, tenant_id, initiated_by }: IssueReferralRewardInput): Promise<{ approval_id: string | null }> {
        // resolve pricing context
        const pricing_context = pricing_recommendation_id
          ? await resolvePricingContext(pricing_recommendation_id)
          : '{{MARKOS_PRICING_ENGINE_PENDING}}';
        // skip approval for $0 rewards (free credit / feature_unlock no monetary value)
        if (reward_value_cents === 0) {
          await emitAudit({ tenant_id, action: 'referral_reward_zero_value', invitation_id });
          return { approval_id: null };
        }
        // call buildApprovalPackage for monetary
        const pkg = buildApprovalPackage({
          kind: 'referral_reward_issuance',
          tenant_id,
          initiated_by,
          payload: { invitation_id, reward_kind, reward_value_cents, pricing_context },
        });
        await emitAudit({ tenant_id, action: 'referral_reward_approval_created', approval_id: pkg.approval_id, invitation_id });
        return { approval_id: pkg.approval_id };
      }
      ```
    Step 5: Implement lib/markos/referral/metrics.ts rollupViralLoopMetrics — period-bounded SELECT against referral_invitations + computed columns; UPSERT into viral_loop_metrics ON CONFLICT (tenant_id, period, referral_program_id) DO UPDATE.
    Step 6: Implement lib/markos/referral/programs.ts CRUD; calls migration 90 tables via supabase client; honors RLS via runtimeContext.tenant_id passed by caller; activation path ('draft' → 'active') calls buildApprovalPackage with kind='referral_program_activation'.
    Step 7: CommonJS twins for programs.cjs + index.cjs (the latter re-exports surface for legacy api/*.js consumers).
    Step 8: Write 4 contract YAMLs F-209/F-210/F-211/F-212 per RESEARCH §6 Domain 6 API surface table; mirror lib/markos/referral/contracts.ts schema. Include auth scheme: requireHostedSupabaseAuth bearer token. Include error responses: 401 unauthenticated, 403 insufficient scope, 422 validation, 423 approval-required (when activation requires approval).
    Step 9: Update contracts/flow-registry.json adding F-209..F-212 entries with paths + methods + zod-schema-ref. Verify via flow-registry parity test that NO overlap exists with F-001..F-198.
    Step 10: Run all referral tests + openapi-parity test (TDD GREEN). Run `npm run openapi:build` to regenerate contracts/openapi.json.
  </action>
  <acceptance_criteria>
    - `npm test -- test/growth-220/referral/fraud-evaluator.test.js` passes (6 rule tests green)
    - `npm test -- test/growth-220/referral/reward-issuance.test.js` passes (3 wiring tests green; buildApprovalPackage mock invoked with correct kind + amount + pricing_context)
    - `npm test -- test/growth-220/referral/metrics-rollup.test.js` passes (k_factor + period-boundary tests green)
    - `npm test -- test/growth-220/openapi/parity-referral.test.js` passes (4 contracts × zod parity)
    - `grep "F-209\|F-210\|F-211\|F-212" contracts/flow-registry.json | wc -l` returns 4
    - `grep "createApprovalPackage\|requireSupabaseAuth\|lookupPlugin\|requireTenantContext\|app/(growth)\|route.ts\|vitest" lib/markos/referral/ -r | wc -l` returns 0 (architecture-lock applied)
    - `npm run openapi:build` completes successfully and contracts/openapi.json contains paths /v1/growth/referral/programs + /v1/growth/referral/invitations + /v1/growth/referral/metrics + /v1/growth/referral/payouts
    - `node -e "console.log(typeof require('./lib/markos/referral/index.cjs').issueReferralReward)"` outputs `function`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Populate 220-VALIDATION.md per-task verification map + close Plan 01</name>
  <files>.planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-VALIDATION.md</files>
  <read_first>
    - 220-VALIDATION.md (existing draft from validation-strategy step; update Per-Task Verification Map table)
    - 220-RESEARCH.md §Validation Architecture (Phase Requirements → Test Map; copy exact rows with current task IDs)
  </read_first>
  <action>
    Step 1: Update 220-VALIDATION.md frontmatter — `nyquist_compliant: true` + `wave_0_complete: true`.
    Step 2: Replace the Per-Task Verification Map table with full rows for all plans:
      | Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
      | 220-01-00 | 01 | 1 | QA-01,02 | preflight | `npm test -- test/growth-220/preflight/` | ✅ | ✅ green |
      | 220-01-0.5 | 01 | 1 | QA-01,02 | architecture-lock | `npm test -- test/growth-220/preflight/architecture-lock.test.js` | ✅ | ✅ green |
      | 220-01-01 | 01 | 1 | SG-04, QA-02 | schema + RLS | `npm test -- test/growth-220/referral/programs.test.js test/growth-220/rls/referral-cross-tenant-denial.test.js` | ✅ | ✅ green |
      | 220-01-02 | 01 | 1 | SG-04,11,12 | unit + DB-trigger + parity | `npm test -- test/growth-220/referral/ test/growth-220/openapi/parity-referral.test.js` | ✅ | ✅ green |
      | 220-02-01 | 02 | 2 | SG-06,09 | schema + RLS | (placeholder; Plan 02 fills) | ❌ W0 | ⬜ pending |
      ...continue for 220-02..220-06 placeholder rows...
    Step 3: Update Wave 0 Requirements section — flip checkboxes:
      - [x] scripts/preconditions/220-01-check-upstream.cjs ships
      - [x] lib/markos/growth/preflight/upstream-gate.ts
      - [x] lib/markos/growth/preflight/architecture-lock.ts
      - [x] lib/markos/growth/preflight/errors.ts
      - [x] test/growth-220/preflight/architecture-lock.test.js
      - [x] test/growth-220/preflight/upstream-gate.test.js
      - [x] test/growth-220/preflight/helper-presence.test.js
      - [x] test/fixtures/growth-220/* (10 files)
    Step 4: Update Dimensions Coverage table to flip row 8 (Validation strategy) from DRAFT to LOCKED.
    Step 5: Commit VALIDATION.md update with message: `docs(220-01): populate VALIDATION map + close Plan 01 wave 0`.
  </action>
  <acceptance_criteria>
    - Frontmatter `nyquist_compliant: true` + `wave_0_complete: true`
    - Per-Task Verification Map table contains rows for all 6 plans (Plan 01 rows ✅ green, Plans 02-06 placeholder ⬜ pending)
    - Wave 0 Requirements section all P220-01-owned items checked
    - Dimensions Coverage row 8 = LOCKED
    - `git diff HEAD~ -- .planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-VALIDATION.md | wc -l` shows substantive update (>50 lines diff)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser → /v1/growth/referral/* | Untrusted operator input crosses here; mutations approval-gated |
| External invitation form → POST /v1/growth/referral/invitations | Untrusted referee input; fraud-evaluator runs pre-INSERT |
| Cron worker → supabase service-role | High-trust path; protected by MARKOS_GROWTH_CRON_SECRET; DB-triggers still enforce compliance |
| App-layer reward-issuance → buildApprovalPackage | Tenant-isolated; approval_id required for monetary rewards (DB-trigger defense-in-depth) |
| Service-role direct UPDATE on referral_invitations | Bypasses app-layer; DB-trigger blocks reward_issued_at without approval_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-220-01-01 | Tampering | referral_invitations service-role UPDATE | mitigate | DB-trigger fn_referral_reward_requires_approval enforces approval_id when reward_issued_at set; tested via test/growth-220/referral/db-trigger-reward-requires-approval.test.js |
| T-220-01-02 | Tampering | referral_programs INSERT with monetary reward bypassing pricing context | mitigate | DB-trigger fn_referral_program_requires_pricing_context blocks unless pricing_recommendation_id set OR placeholder sentinel; tested |
| T-220-01-03 | Spoofing | Bot accounts farm rewards via fraud_controls bypass | mitigate | fraud-evaluator 5 rules + DB CHECK min_account_age_days >= 0 + buildApprovalPackage every reward + audit trail |
| T-220-01-04 | Information Disclosure | Cross-tenant viral_loop_metrics leakage | mitigate | RLS policy on tenant_id; tested via test/growth-220/rls/referral-cross-tenant-denial.test.js |
| T-220-01-05 | Repudiation | Operator denies approving reward | mitigate | buildApprovalPackage writes approval row with approved_by + approved_at + IP; audit log SHA-256 hash chain |
| T-220-01-06 | Tampering | Affiliate-style double-spend (invitation rewarded twice) | mitigate | UNIQUE on referral_invitations.invitation_code + reward_issued_at idempotency check in app layer + DB-trigger check |
| T-220-01-07 | Spoofing | Cron token leaked → unauthorized metrics rollup | mitigate | crypto.timingSafeEqual on cron token; per-env MARKOS_GROWTH_CRON_SECRET; deferred to Plan 06 cron handlers (Wave 4) |
| T-220-01-08 | Elevation of Privilege | Operator activates referral program without approval | mitigate | activation-path 'draft' → 'active' calls buildApprovalPackage; approved_at column required for status='active' |
| T-220-01-09 | Information Disclosure | Pricing context leaked via metrics endpoint | accept | viral_loop_metrics is per-tenant RLS-isolated; pricing_recommendation_id is tenant-internal; no cross-tenant leak possible |
| T-220-01-10 | Tampering | min_account_age_days set to 0 in launch promo permits bot farming | mitigate | DB CHECK min_account_age_days >= 0 (cannot go negative); app-layer warning when < 7 + reward_trigger='signup' (warning surfaced via approval review) |
</threat_model>

<verification>
**Plan-level verification (run after Task 3 closes):**
- `npm test -- test/growth-220/preflight/ test/growth-220/referral/ test/growth-220/rls/referral-* test/growth-220/openapi/parity-referral.test.js` → all green
- `node scripts/preconditions/220-01-check-upstream.cjs` → exits 0 with green env (or 1 with proper UpstreamPhaseNotLandedError on missing required upstream)
- `psql -c "SELECT count(*) FROM information_schema.tables WHERE table_name IN ('referral_programs','viral_loop_metrics','referral_invitations')"` returns 3
- `psql -c "SELECT count(*) FROM pg_trigger WHERE tgname LIKE 'trg_referral_%'"` returns >= 2
- `grep -E "F-(2(09|1[0-2]))" contracts/flow-registry.json | wc -l` returns 4
- `grep -rn "createApprovalPackage\|requireSupabaseAuth\|requireTenantContext\|serviceRoleClient\|lookupPlugin\|public/openapi.json\|app/(growth)\|app/(public)\|app/(markos)\|api/v1/.*/route.ts\|vitest\|from 'vitest'\|.test.ts" lib/markos/growth/ lib/markos/referral/ scripts/preconditions/220-01-check-upstream.cjs | wc -l` returns 0
</verification>

<success_criteria>
- All 4 tasks complete with passing tests; total ~30+ test assertions green
- Migration slot coordination doc resolves Q-3 with explicit P218/P219 reservation pathway + Q-1 P220-P227 affiliate_programs ALTER TABLE collision resolution + P220 → P227 ALTER readiness coordination block
- 2 SOR migrations + 4 lib files + 4 contracts ship with NO architecture-lock violations
- assertUpstreamReady preflight + architecture-lock + helper-presence test surface ready for consumption by Plans 02-06
- 220-VALIDATION.md per-task map populated; nyquist_compliant + wave_0_complete frontmatter flipped
- Domain 1 (referral/viral) compliance enforcement boundary LOCKED at DB-trigger layer (defense-in-depth) per RESEARCH §Pitfall 11 + §Compliance enforcement
</success_criteria>

<output>
After completion, create `.planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-01-SUMMARY.md` documenting:
- Migration slots claimed: 90, 91 (per V4.1.0-MIGRATION-SLOT-COORDINATION.md)
- F-IDs claimed: F-209..F-212 (per coordination doc)
- Tables created: referral_programs, viral_loop_metrics, referral_invitations
- Triggers created: trg_referral_reward_requires_approval (BEFORE UPDATE on referral_invitations), trg_referral_program_requires_pricing_context (BEFORE INSERT/UPDATE on referral_programs)
- Helper modules: lib/markos/growth/preflight/{upstream-gate, architecture-lock, errors, index.cjs} consumed by all subsequent 220-NN plans
- Architecture-lock surface ready: 0 forbidden patterns across the touched paths
- Q-1 (P220-P227 affiliate_programs collision): RESOLVED — P220 owns base in Plan 05 migration 95; P227 ALTERs additive in migration 162 (documented in coordination doc)
- Q-3 (F-ID + migration slot collision): RESOLVED — P220 takes slots 90-95+97 (below P221's 100-105); F-IDs F-209..F-227 (sequential post-P227 F-198, cosmetic only)
</output>
