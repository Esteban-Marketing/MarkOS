---
phase: 39-pain-points-first-content-corpus
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/literacy-ingest.test.js
  - test/fixtures/literacy/Paid_Media/LIT-PM-001.md
  - .agent/markos/literacy/taxonomy.json
  - supabase/migrations/39_pain_point_tags.sql
  - bin/ingest-literacy.cjs
  - onboarding/backend/vector-store-client.cjs
autonomous: true
requirements:
  - LIT-02
  - LIT-03

must_haves:
  truths:
    - "taxonomy.json exists at .agent/markos/literacy/taxonomy.json with 8 parent tags and 15 sub-tags"
    - "Supabase migration SQL file exists adding pain_point_tags TEXT[] to markos_literacy_chunks"
    - "ingest-literacy.cjs throws MISSING_REQUIRED_FIELD:pain_point_tags when field absent"
    - "ingest-literacy.cjs maps pain_point_tags from metadata into the chunk payload"
    - "upsertLiteracyChunk() writes pain_point_tags to both Upstash metadata and Supabase row"
    - "buildLiteracyFilter() adds pain_point_tags CONTAINS '...' clause when filters.pain_point_tag is provided"
    - "buildLiteracyFilter is exported from vector-store-client.cjs module.exports"
    - "node --test test/literacy-ingest.test.js passes all 7 tests"
  artifacts:
    - path: ".agent/markos/literacy/taxonomy.json"
      provides: "Machine-readable two-tier pain-point taxonomy consumed by Phase 40 discipline router"
      contains: '"version": "1.0"'
    - path: "supabase/migrations/39_pain_point_tags.sql"
      provides: "Non-destructive ALTER TABLE migration for Supabase"
      contains: "ADD COLUMN IF NOT EXISTS pain_point_tags TEXT[]"
    - path: "test/fixtures/literacy/Paid_Media/LIT-PM-001.md"
      provides: "Minimal valid corpus fixture for test assertions"
      contains: 'pain_point_tags: ["high_acquisition_cost", "paid_media:high_cpr"]'
    - path: "test/literacy-ingest.test.js"
      provides: "Unit tests for pain_point_tags parsing, chunk propagation, filter clause, validation rejection"
  key_links:
    - from: "ingest-literacy.cjs"
      to: "literacy-chunker.cjs parseLiteracyFrontmatter()"
      via: "reading pain_point_tags from parsed metadata before chunkLiteracyFile()"
      pattern: "pain_point_tags"
    - from: "ingest-literacy.cjs payload"
      to: "upsertLiteracyChunk()"
      via: "pain_point_tags field on payload object"
      pattern: "pain_point_tags: Array.isArray"
    - from: "vector-store-client.cjs upsertLiteracyChunk"
      to: "Upstash metadata object"
      via: "pain_point_tags: Array.isArray(chunk.pain_point_tags)"
      pattern: "pain_point_tags.*Array.isArray"
    - from: "buildLiteracyFilter"
      to: "module.exports"
      via: "added to exports object"
      pattern: "buildLiteracyFilter,"
---

<objective>
Extend the Phase 32 literacy infrastructure to support the pain_point_tags field: add Wave 0 test scaffolding (4 new unit tests + fixture), create the machine-readable taxonomy artifact, create the Supabase migration file, and extend the ingest pipeline and vector-store client to validate, propagate, store, and filter by pain_point_tags.

Purpose: After this plan, the pipeline rejects documents missing pain_point_tags, stores the field in both Upstash and Supabase, and buildLiteracyFilter supports single-tag filtering — unblocking Plan 02 corpus authoring and Plan 03 ingestion.

Output: taxonomy.json, 39_pain_point_tags.sql, 4 new test cases (RED until Plan 01 Task 3 completes), extended ingest-literacy.cjs, extended vector-store-client.cjs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-pain-points-first-content-corpus/39-CONTEXT.md
@.planning/phases/39-pain-points-first-content-corpus/39-RESEARCH.md
@.planning/phases/39-pain-points-first-content-corpus/39-VALIDATION.md
</context>

<interfaces>
<!-- Key signatures the executor needs. Extracted from codebase. -->

From onboarding/backend/literacy-chunker.cjs:
```javascript
function parseLiteracyFrontmatter(markdown)  // returns object with all YAML keys; already parses pain_point_tags via generic parseSimpleYamlValue()
function chunkLiteracyFile(markdown, metadata) // returns array of {chunk_id, doc_id, content_type, chunk_text, ...}
module.exports = { parseLiteracyFrontmatter, chunkLiteracyFile, ... }
```

From onboarding/backend/vector-store-client.cjs (current state before changes):
```javascript
function buildLiteracyFilter(filters = {}) {
  const parts = ["status = 'canonical'"];
  if (filters.business_model) { parts.push(`business_model CONTAINS '${escapeFilterValue(filters.business_model)}'`); }
  if (filters.funnel_stage)   { parts.push(`funnel_stage = '${escapeFilterValue(filters.funnel_stage)}'`); }
  if (filters.content_type)   { parts.push(`content_type = '${escapeFilterValue(filters.content_type)}'`); }
  return parts.join(' AND ');
}
// NOT currently in module.exports — must be added

async function upsertLiteracyChunk(chunk) {
  // metadata object: category, discipline, sub_discipline, business_model[], funnel_stage, content_type, status, ...
  // Supabase row: chunk_id, doc_id, category, discipline, ..., retrieval_keywords[], chunk_text, ...
  // pain_point_tags is MISSING from both objects — must be added to each
}

module.exports = {
  // ...
  getLiteracyContext,
  upsertLiteracyChunk,
  supersedeLiteracyDoc,
  // buildLiteracyFilter is NOT exported — must be added
};
```

From bin/ingest-literacy.cjs (current state):
```javascript
const metadata = parseLiteracyFrontmatter(markdown);
// NO validation block here — must be added
if (args.discipline && ...) { continue; }
const chunks = chunkLiteracyFile(markdown, metadata);
// payload object: chunk_id, doc_id, discipline, ..., retrieval_keywords[]
// pain_point_tags NOT in payload — must be added
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — Extend test suite with pain_point_tags cases and create fixture</name>
  <files>test/literacy-ingest.test.js, test/fixtures/literacy/Paid_Media/LIT-PM-001.md</files>
  <read_first>
    - test/literacy-ingest.test.js (see current 3 tests — append after the last test block)
    - test/setup.js (understand withMockedModule and available helpers)
    - onboarding/backend/literacy-chunker.cjs (lines 1–50 to confirm parseLiteracyFrontmatter signature)
    - onboarding/backend/vector-store-client.cjs (lines 30–55 to see current buildLiteracyFilter body and lines 680–720 for current module.exports)
    - bin/ingest-literacy.cjs (lines 78–135 to see the current payload object structure)
  </read_first>
  <behavior>
    - Test 4: parseLiteracyFrontmatter parses pain_point_tags as an array from fenced YAML
    - Test 5: chunkLiteracyFile propagates pain_point_tags from the fixture file to all returned chunks
    - Test 6: buildLiteracyFilter (once exported) generates "pain_point_tags CONTAINS 'paid_media:high_cpr'" clause when filters.pain_point_tag = "paid_media:high_cpr"
    - Test 7: ingest-literacy.cjs script source contains the string "MISSING_REQUIRED_FIELD:pain_point_tags" (validates the validation logic is present in the file)
  </behavior>
  <action>
**Step A — Create the test fixture file:**

Create `test/fixtures/literacy/Paid_Media/LIT-PM-001.md` with this exact content (minimal valid corpus document for tests only — does NOT need full production content):

```
```yaml
doc_id: "LIT-PM-001-high-cpr"
discipline: "Paid_Media"
sub_discipline: "Performance_Advertising"
business_model: ["all"]
pain_point_tags: ["high_acquisition_cost", "paid_media:high_cpr"]
funnel_stage: "Acquisition"
category: "STANDARDS"
status: "canonical"
evidence_level: "study"
source_ref: "Meta Ads Benchmark Report 2024"
version: "1.0"
last_validated: "2026-04-01"
ttl_days: 180
retrieval_keywords: ["CPR", "cost per result", "ad efficiency"]
agent_use: ["markos-copy-drafter"]
```

# High Cost Per Result: Diagnosing and Reversing CPR Inflation

CPR inflation is the gradual increase in advertising cost per desired action — click, lead, purchase — caused by audience saturation, creative fatigue, or bid competition.

## EVIDENCE BASE
- 72% of advertisers see CPR increase by more than 30% after 21 days without creative rotation — Meta Performance Insights 2024.
- Median CPR for B2B SaaS lead-gen on Meta is $18–$45; top quartile achieves below $12 — Metadata.io Benchmarks 2024.

## CORE TACTICS
### Creative Rotation Cadence
Rotate ad creative every 14–21 days or when frequency exceeds 3.0 per week.

### Audience Refresh
Exclude recent converters and re-seed lookalikes monthly.

## PERFORMANCE BENCHMARKS
| Metric | Poor | Median | Top Quartile | Source |
|--------|------|--------|--------------|--------|
| CPR (B2B lead-gen, Meta) | > $60 | $18–$45 | < $12 | Metadata.io 2024 |
| Creative frequency before fatigue | > 4.0 | 3.0 | 1.5–2.5 | Meta 2024 |

## COUNTER-INDICATORS
- **Nonprofit / grant-period campaigns:** CPR benchmarks do not apply during grant-restricted spend windows.

## VOCABULARY
- **Creative fatigue**: Declining CTR caused by repeated ad exposure to the same audience.
- **CPR**: Cost Per Result — the amount spent divided by the number of desired actions achieved.
```

**Step B — Append 4 new test cases to test/literacy-ingest.test.js:**

Add AFTER the final closing `});` of the existing 3rd test. Use the same require-style imports already at the top of the file. Add:

```javascript
const vectorClientPath = path.resolve(__dirname, '../onboarding/backend/vector-store-client.cjs');

function loadVectorClient() {
  delete require.cache[require.resolve(vectorClientPath)];
  return require(vectorClientPath);
}

test('Literacy chunker parses pain_point_tags as an array', () => {
  const chunker = loadChunker();
  const fixturePath = path.resolve(__dirname, 'fixtures/literacy/Paid_Media/LIT-PM-001.md');
  const markdown = require('fs').readFileSync(fixturePath, 'utf8');
  const parsed = chunker.parseLiteracyFrontmatter(markdown);
  assert.ok(Array.isArray(parsed.pain_point_tags), 'pain_point_tags should be an array');
  assert.deepEqual(parsed.pain_point_tags, ['high_acquisition_cost', 'paid_media:high_cpr']);
});

test('Literacy chunker propagates pain_point_tags onto each chunk', () => {
  const chunker = loadChunker();
  const fixturePath = path.resolve(__dirname, 'fixtures/literacy/Paid_Media/LIT-PM-001.md');
  const markdown = require('fs').readFileSync(fixturePath, 'utf8');
  const metadata = chunker.parseLiteracyFrontmatter(markdown);
  const chunks = chunker.chunkLiteracyFile(markdown, metadata);
  assert.ok(chunks.length > 0, 'fixture should produce at least 1 chunk');
  for (const chunk of chunks) {
    assert.deepEqual(chunk.pain_point_tags, ['high_acquisition_cost', 'paid_media:high_cpr'],
      `chunk ${chunk.chunk_id} should carry pain_point_tags`);
  }
});

test('buildLiteracyFilter adds pain_point_tags CONTAINS clause', () => {
  const client = loadVectorClient();
  const filter = client.buildLiteracyFilter({ pain_point_tag: 'paid_media:high_cpr' });
  assert.ok(
    filter.includes("pain_point_tags CONTAINS 'paid_media:high_cpr'"),
    `filter should contain pain_point_tags clause, got: ${filter}`
  );
  assert.ok(filter.includes("status = 'canonical'"), 'filter should always include canonical status');
});

test('ingest-literacy script contains pain_point_tags validation error string', () => {
  const script = require('fs').readFileSync(ingestPath, 'utf8');
  assert.match(script, /MISSING_REQUIRED_FIELD:pain_point_tags/,
    'ingest-literacy.cjs must contain validation throw for missing pain_point_tags');
  assert.match(script, /pain_point_tags.*Array\.isArray|Array\.isArray.*pain_point_tags/,
    'ingest-literacy.cjs must map pain_point_tags in payload');
});
```

**Important:** Tests 4 and 5 (parseLiteracyFrontmatter + chunk propagation) will be RED until `chunkLiteracyFile` propagates `pain_point_tags` — this is confirmed in Task 3. Tests 6 and 7 will be RED until Task 3 exports `buildLiteracyFilter` and adds the validation. Run `node --test test/literacy-ingest.test.js` after this task to confirm Tests 1–3 still pass and Tests 4–7 fail (expected RED state).

**Note on chunk propagation (Test 5):** `chunkLiteracyFile` in `literacy-chunker.cjs` may not currently attach `pain_point_tags` to each chunk object. If the chunker does not forward metadata fields to chunks, Task 3 must add `pain_point_tags: metadata.pain_point_tags || []` to the chunk object built inside `chunkLiteracyFile`. Verify by reading the `chunkLiteracyFile` return value structure in `literacy-chunker.cjs`.
  </action>
  <verify>
    <automated>node --test test/literacy-ingest.test.js 2>&1 | head -40</automated>
  </verify>
  <done>
    - test/fixtures/literacy/Paid_Media/LIT-PM-001.md exists and contains `pain_point_tags: ["high_acquisition_cost", "paid_media:high_cpr"]`
    - test/literacy-ingest.test.js contains 7 test() blocks (grep: `^test\(` count = 7)
    - Tests 1–3 still pass; Tests 4–7 fail with assertion errors (not import/parse errors — RED is expected and correct here)
  </done>
</task>

<task type="auto">
  <name>Task 2: Taxonomy artifact and Supabase migration file</name>
  <files>.agent/markos/literacy/taxonomy.json, supabase/migrations/39_pain_point_tags.sql</files>
  <read_first>
    - .planning/phases/39-pain-points-first-content-corpus/39-RESEARCH.md (§Pain-Point Taxonomy Design and §Taxonomy Artifact Format — lines 168–230)
    - supabase/migrations/37_markos_ui_control_plane.sql (lines 1–10 to confirm migration file naming/header convention)
  </read_first>
  <action>
**Step A — Create `.agent/markos/literacy/taxonomy.json`:**

Create the directory `.agent/markos/literacy/` if it does not exist, then write `taxonomy.json` with this exact content (verbatim — per D-39-01, this is the machine-readable artifact Phase 40 will consume):

```json
{
  "version": "1.0",
  "description": "Two-tier pain-point taxonomy for MarkOS literacy corpus. Parent tags drive Phase 40 discipline routing; sub-tags sharpen within-discipline filter precision.",
  "parents": [
    {
      "tag": "high_acquisition_cost",
      "label": "High Acquisition Cost",
      "definition": "CAC / CPL / CPR trending above benchmark; budget efficiency degrading",
      "disciplines": ["Paid_Media", "Content_SEO"]
    },
    {
      "tag": "low_conversions",
      "label": "Low Conversions",
      "definition": "Low CVR at key funnel moments — ad to landing page, landing page to lead, lead to trial",
      "disciplines": ["Paid_Media", "Landing_Pages"]
    },
    {
      "tag": "poor_retention_churn",
      "label": "Poor Retention / Churn",
      "definition": "MRR churn, subscriber loss, repeat purchase rate declining",
      "disciplines": ["Lifecycle_Email"]
    },
    {
      "tag": "low_organic_visibility",
      "label": "Low Organic Visibility",
      "definition": "Search rankings declining or stagnant; organic traffic share shrinking",
      "disciplines": ["Content_SEO"]
    },
    {
      "tag": "attribution_measurement",
      "label": "Attribution & Measurement",
      "definition": "Cannot accurately attribute revenue to channel; data fragmentation across touchpoints",
      "disciplines": ["Paid_Media", "Social"]
    },
    {
      "tag": "audience_mismatch",
      "label": "Audience Mismatch",
      "definition": "Reaching wrong ICP; poor segmentation; high bounce from irrelevant audiences",
      "disciplines": ["Paid_Media", "Content_SEO", "Lifecycle_Email", "Social", "Landing_Pages"]
    },
    {
      "tag": "pipeline_velocity",
      "label": "Pipeline Velocity",
      "definition": "Leads stalling in mid-funnel; follow-up gaps; nurture sequences not converting",
      "disciplines": ["Lifecycle_Email", "Landing_Pages"]
    },
    {
      "tag": "content_engagement",
      "label": "Content Engagement",
      "definition": "Content published but not driving interaction, shares, comments, return visits",
      "disciplines": ["Social", "Content_SEO"]
    }
  ],
  "sub_tags": {
    "Paid_Media": [
      { "tag": "paid_media:high_cpr", "label": "High CPR", "definition": "Cost-per-result above benchmark for ad format / objective" },
      { "tag": "paid_media:low_roas", "label": "Low ROAS", "definition": "Return on ad spend below profitable threshold" },
      { "tag": "paid_media:attribution_gap", "label": "Attribution Gap", "definition": "Multi-touch attribution fragmented across platforms" }
    ],
    "Content_SEO": [
      { "tag": "content_seo:low_rankings", "label": "Low Rankings", "definition": "Target keywords not ranking in top 20" },
      { "tag": "content_seo:thin_content", "label": "Thin Content", "definition": "Pages lacking depth, E-E-A-T signals, or citable evidence" },
      { "tag": "content_seo:low_organic_ctr", "label": "Low Organic CTR", "definition": "Ranking but not driving clicks — title/meta optimization gap" }
    ],
    "Lifecycle_Email": [
      { "tag": "lifecycle_email:low_open_rate", "label": "Low Open Rate", "definition": "Open rates below channel benchmark; deliverability / subject line issues" },
      { "tag": "lifecycle_email:high_unsubscribe", "label": "High Unsubscribe Rate", "definition": "List churn driven by frequency, irrelevance, or list hygiene" },
      { "tag": "lifecycle_email:weak_nurture", "label": "Weak Nurture Sequences", "definition": "Mid-funnel sequences not progressing leads to qualified stage" }
    ],
    "Social": [
      { "tag": "social:low_engagement", "label": "Low Engagement", "definition": "Content receiving near-zero interaction relative to reach" },
      { "tag": "social:audience_growth_plateau", "label": "Audience Growth Plateau", "definition": "Follower growth stalled; organic reach declining" },
      { "tag": "social:poor_brand_awareness", "label": "Poor Brand Awareness", "definition": "Low unaided recall; brand not associating with category problem" }
    ],
    "Landing_Pages": [
      { "tag": "landing_pages:low_cvr", "label": "Low CVR", "definition": "Page visitor to form-fill below benchmark (< 2.5% for B2B)" },
      { "tag": "landing_pages:high_bounce", "label": "High Bounce", "definition": "Visitors leave without scrolling or engaging — message/offer mismatch" },
      { "tag": "landing_pages:poor_form_completion", "label": "Poor Form Completion", "definition": "Form started but abandoned; friction, length, or trust issue" }
    ]
  }
}
```

**Step B — Create `supabase/migrations/39_pain_point_tags.sql`:**

```sql
-- Migration 39: Add pain_point_tags column to markos_literacy_chunks
-- Phase 39: Pain-Points-First Content Corpus
-- Non-destructive: existing rows receive an empty array default.

ALTER TABLE markos_literacy_chunks
  ADD COLUMN IF NOT EXISTS pain_point_tags TEXT[] DEFAULT '{}';

COMMENT ON COLUMN markos_literacy_chunks.pain_point_tags
  IS 'Two-tier pain-point tags: parent category + discipline sub-tag (e.g. ["high_acquisition_cost","paid_media:high_cpr"]). Filterable via Supabase TEXT[] operators.';
```
  </action>
  <verify>
    <automated>node -e "const t = require('./.agent/markos/literacy/taxonomy.json'); console.log(t.parents.length, Object.keys(t.sub_tags).length)" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `.agent/markos/literacy/taxonomy.json` exists: `Test-Path .agent/markos/literacy/taxonomy.json` returns True
    - taxonomy.json is valid JSON: `node -e "require('./.agent/markos/literacy/taxonomy.json')"` exits 0
    - taxonomy.json has exactly 8 parents: the command above prints `8 5`
    - `supabase/migrations/39_pain_point_tags.sql` exists: `Test-Path supabase/migrations/39_pain_point_tags.sql` returns True
    - Migration file contains: `grep "ADD COLUMN IF NOT EXISTS pain_point_tags TEXT\[\]" supabase/migrations/39_pain_point_tags.sql` returns a match
    - Migration file contains: `grep "markos_literacy_chunks" supabase/migrations/39_pain_point_tags.sql` returns a match
  </acceptance_criteria>
  <done>taxonomy.json is valid JSON with 8 parents and 5 discipline sub_tag groups (3 sub-tags each). Migration SQL file is non-destructive and targets markos_literacy_chunks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend ingest-literacy.cjs and vector-store-client.cjs for pain_point_tags</name>
  <files>bin/ingest-literacy.cjs, onboarding/backend/vector-store-client.cjs</files>
  <read_first>
    - bin/ingest-literacy.cjs (full file — especially lines 75–135 for the metadata/payload block)
    - onboarding/backend/vector-store-client.cjs (lines 30–55 for buildLiteracyFilter, lines 387–460 for upsertLiteracyChunk including metadata and row objects, lines 680–720 for module.exports)
    - onboarding/backend/literacy-chunker.cjs (lines 50–120 for chunkLiteracyFile — confirm whether chunk objects currently carry metadata fields like pain_point_tags)
  </read_first>
  <behavior>
    - Validation: ingest-literacy.cjs throws `Error('MISSING_REQUIRED_FIELD:pain_point_tags in {filePath}')` when pain_point_tags is absent or empty
    - Payload: ingest-literacy.cjs chunk payload includes `pain_point_tags: Array.isArray(metadata.pain_point_tags) ? metadata.pain_point_tags : []`
    - Filter: buildLiteracyFilter adds `pain_point_tags CONTAINS '{value}'` clause when `filters.pain_point_tag` is truthy
    - Upsert metadata: upsertLiteracyChunk Upstash metadata object includes `pain_point_tags: Array.isArray(chunk.pain_point_tags) ? chunk.pain_point_tags : []`
    - Upsert row: upsertLiteracyChunk Supabase row object includes `pain_point_tags: Array.isArray(chunk.pain_point_tags) ? chunk.pain_point_tags : []` (after `retrieval_keywords` line)
    - Export: buildLiteracyFilter added to module.exports in vector-store-client.cjs
    - Chunk propagation: if chunkLiteracyFile does NOT currently attach pain_point_tags to each chunk, add `pain_point_tags: metadata.pain_point_tags || []` to each chunk object inside chunkLiteracyFile in literacy-chunker.cjs
  </behavior>
  <action>
**Change 1 — `bin/ingest-literacy.cjs` validation block:**

After the line `const metadata = parseLiteracyFrontmatter(markdown);` (currently line ~83), add this block immediately before the `if (args.discipline &&` check:

```javascript
      // Validate required fields per Phase 39 pain-point taxonomy requirement
      const REQUIRED_FIELDS = ['doc_id', 'discipline', 'business_model', 'pain_point_tags'];
      for (const field of REQUIRED_FIELDS) {
        const val = metadata[field];
        const missing = val === undefined || val === null || val === '' || (Array.isArray(val) && val.length === 0);
        if (missing) {
          throw new Error(`MISSING_REQUIRED_FIELD:${field} in ${filePath}`);
        }
      }
```

**Change 2 — `bin/ingest-literacy.cjs` payload mapping:**

In the `payload` object literal (after `retrieval_keywords: Array.isArray(metadata.retrieval_keywords) ? metadata.retrieval_keywords : [],`), add:

```javascript
          pain_point_tags: Array.isArray(metadata.pain_point_tags) ? metadata.pain_point_tags : [],
```

**Change 3 — `onboarding/backend/vector-store-client.cjs` buildLiteracyFilter extension:**

In the `buildLiteracyFilter` function body, after the `content_type` block (after `parts.push(\`content_type = ...\`)`), add:

```javascript
  if (filters.pain_point_tag) {
    parts.push(`pain_point_tags CONTAINS '${escapeFilterValue(filters.pain_point_tag)}'`);
  }
```

**Change 4 — `onboarding/backend/vector-store-client.cjs` upsertLiteracyChunk Upstash metadata:**

In the `metadata` object literal inside `upsertLiteracyChunk`, after the `ttl_days` line, add:

```javascript
    pain_point_tags: Array.isArray(chunk.pain_point_tags) ? chunk.pain_point_tags : [],
```

**Change 5 — `onboarding/backend/vector-store-client.cjs` upsertLiteracyChunk Supabase row:**

In the `row` object literal inside `upsertLiteracyChunk`, after the `retrieval_keywords` line, add:

```javascript
      pain_point_tags: Array.isArray(chunk.pain_point_tags) ? chunk.pain_point_tags : [],
```

**Change 6 — `onboarding/backend/vector-store-client.cjs` module.exports:**

Add `buildLiteracyFilter,` to the `module.exports` object (place it after `supersedeLiteracyDoc,`):

```javascript
  buildLiteracyFilter,
```

**Change 7 — `onboarding/backend/literacy-chunker.cjs` chunk propagation (conditional):**

Read `chunkLiteracyFile` in `literacy-chunker.cjs`. If the chunk object returned does NOT already include `pain_point_tags`, add it to each chunk object before it is pushed to the result array:

```javascript
chunk.pain_point_tags = metadata.pain_point_tags || [];
```

This ensures Test 5 (chunk propagation) passes.
  </action>
  <verify>
    <automated>node --test test/literacy-ingest.test.js</automated>
  </verify>
  <acceptance_criteria>
    - All 7 tests pass: `node --test test/literacy-ingest.test.js` exits 0 with output showing "7 passing"
    - buildLiteracyFilter is exported: `node -e "const c = require('./onboarding/backend/vector-store-client.cjs'); console.log(typeof c.buildLiteracyFilter)"` prints `function`
    - Validation logic present: `grep "MISSING_REQUIRED_FIELD:pain_point_tags" bin/ingest-literacy.cjs` returns a match
    - Payload mapping present: `grep "pain_point_tags.*Array.isArray" bin/ingest-literacy.cjs` returns a match
    - Upstash metadata present: `grep "pain_point_tags.*Array.isArray" onboarding/backend/vector-store-client.cjs` returns ≥2 matches (one for metadata, one for row)
    - Export present: `grep "buildLiteracyFilter," onboarding/backend/vector-store-client.cjs` returns a match in the module.exports block
    - Full test suite still passes: `npm test` exits 0
  </acceptance_criteria>
  <done>
    All 7 unit tests pass. buildLiteracyFilter exported. Ingest validates pain_point_tags and maps it to payload. upsertLiteracyChunk stores pain_point_tags in both Upstash metadata and Supabase row. npm test exits 0.
  </done>
</task>

</tasks>

<verification>
After Plan 01 completes:
1. `node --test test/literacy-ingest.test.js` — all 7 tests pass
2. `npm test` — full suite exits 0
3. `node -e "const c = require('./onboarding/backend/vector-store-client.cjs'); console.log(typeof c.buildLiteracyFilter)"` — prints `function`
4. `node -e "require('./.agent/markos/literacy/taxonomy.json')"` — exits 0 (valid JSON)
5. `grep "MISSING_REQUIRED_FIELD:pain_point_tags" bin/ingest-literacy.cjs` — returns match
6. `node bin/ingest-literacy.cjs --path test/fixtures/literacy/Paid_Media --dry-run` — exits 0, prints chunks_written > 0
</verification>

<success_criteria>
- taxonomy.json has 8 parent tags and 15 sub-tags across 5 disciplines (LIT-02)
- Supabase migration SQL file is non-destructive and targets pain_point_tags TEXT[]
- ingest-literacy.cjs rejects documents missing pain_point_tags with MISSING_REQUIRED_FIELD:pain_point_tags error (LIT-02, LIT-03)
- pain_point_tags is stored in both Upstash vector metadata and Supabase row via upsertLiteracyChunk (LIT-02)
- buildLiteracyFilter supports pain_point_tag filter and is exported from module (LIT-02)
- All 7 unit tests pass (LIT-02, LIT-03)
</success_criteria>

<output>
After completion, create `.planning/phases/39-pain-points-first-content-corpus/39-01-SUMMARY.md`
</output>
