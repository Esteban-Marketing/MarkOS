---
phase: 214
plan: "03"
type: execute
wave: 3
depends_on:
  - "214-02"
f_ids:
  - F-292
migration_slots:
  - 126
integrates_with:
  - "RUN-01..08 from P207 (every risky transition should emit or link an AgentRun)"
  - "TASK-01..05 from P208 (approval-required transitions surface as unified tasks or approvals)"
translation_gate_for: []
files_modified:
  - supabase/migrations/126_saas_subscription_lifecycle.sql
  - lib/markos/saas/core/lifecycle.ts
  - lib/markos/saas/core/transition-guard.ts
  - lib/markos/saas/core/idempotency.ts
  - lib/markos/saas/core/index.cjs
  - contracts/F-292-saas-lifecycle-transition-v1.yaml
  - contracts/flow-registry.json
  - test/saas-214/domain-3/lifecycle-transition-table.test.js
  - test/saas-214/domain-3/lifecycle-approval-required.test.js
  - test/saas-214/domain-3/lifecycle-idempotency.test.js
  - test/saas-214/domain-3/lifecycle-rollback.test.js
  - .planning/phases/214-saas-suite-activation-subscription-core/214-VALIDATION.md
autonomous: true
requirements:
  - SAAS-03
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - QA-05
  - QA-06
ui_spec_fold:
  source: .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md
  fold_date: 2026-04-29
  scope: backend_doctrine
  acs_folded:
    - BD-6   # idempotency keys (co-owned with 214-02)
    - BD-7   # 8-state machine + 16-transition matrix; SAAS_SUBSCRIPTION_INVALID_TRANSITION + SAAS_SUBSCRIPTION_TRANSITION_REQUIRES_APPROVAL triggers
  lifecycle_event_vocabulary_verbatim:
    - activation_started
    - trial_started
    - trial_converted
    - payment_failed
    - payment_recovered
    - pause_requested
    - pause_applied
    - reactivation_requested
    - reactivated
    - upgrade_requested
    - upgrade_applied
    - downgrade_requested
    - downgrade_applied
    - cancellation_requested
    - canceled
    - rollback_applied
  transition_matrix_canonical:
    - "draft → trialing (event: trial_started; auto)"
    - "draft → active (event: activation_started; auto)"
    - "trialing → active (event: trial_converted; auto on payment)"
    - "trialing → canceled (event: cancellation_requested → canceled; APPROVAL REQUIRED)"
    - "active → past_due (event: payment_failed; auto from billing engine)"
    - "active → paused (event: pause_requested → pause_applied; APPROVAL REQUIRED)"
    - "active → upgrade_pending (event: upgrade_requested; APPROVAL REQUIRED)"
    - "active → downgrade_pending (event: downgrade_requested; APPROVAL REQUIRED)"
    - "active → canceled (event: cancellation_requested → canceled; APPROVAL REQUIRED)"
    - "past_due → active (event: payment_recovered; auto)"
    - "past_due → canceled (event: cancellation_requested → canceled; APPROVAL REQUIRED)"
    - "paused → active (event: reactivation_requested → reactivated; APPROVAL REQUIRED)"
    - "paused → canceled (event: cancellation_requested → canceled; APPROVAL REQUIRED)"
    - "upgrade_pending → active (event: upgrade_applied; auto on approval)"
    - "downgrade_pending → active (event: downgrade_applied; auto on approval)"
    - "any → any (event: rollback_applied; via rollback contract; auto)"
must_haves:
  truths:
    - "Migration 126 creates `saas_subscription_transition_requests` and the trigger/function pair that owns lifecycle-state validation (BD-7)."
    - "Canonical states are exactly 8 verbatim literals from UI-SPEC §Surface A: `draft`, `trialing`, `active`, `past_due`, `paused`, `canceled`, `upgrade_pending`, and `downgrade_pending`."
    - "Canonical event vocabulary is exactly 16 verbatim literals from UI-SPEC §Surface A: `activation_started | trial_started | trial_converted | payment_failed | payment_recovered | pause_requested | pause_applied | reactivation_requested | reactivated | upgrade_requested | upgrade_applied | downgrade_requested | downgrade_applied | cancellation_requested | canceled | rollback_applied`."
    - "Canonical transition matrix is the 16 transitions enumerated above; lifecycle.ts exports `LIFECYCLE_TRANSITION_MATRIX` and `APPROVAL_REQUIRED_TRANSITIONS` so 214-05 mutation-request routing and 214-06 UI consume the same source of truth."
    - "Trigger `SAAS_SUBSCRIPTION_INVALID_TRANSITION` raises when `(prior_state, requested_state)` is not in the canonical matrix; trigger `SAAS_SUBSCRIPTION_TRANSITION_REQUIRES_APPROVAL` raises when an APPROVAL_REQUIRED transition is attempted without a linked `mutation_request_id` (forward-FK to 214-05)."
    - "Every transition request carries `idempotency_key`, and `(tenant_id, subscription_id, idempotency_key)` uniqueness prevents duplicate mutation application (BD-6 co-owned)."
    - "Rollback behavior is explicit: risky transitions record the prior state in `saas_subscription_transition_requests.rollback_to_state` and write a `rollback_applied` event row instead of mutating history in place."
  artifacts:
    - path: supabase/migrations/126_saas_subscription_lifecycle.sql
      provides: lifecycle request table plus transition validation and approval-enforcement triggers
    - path: lib/markos/saas/core/lifecycle.ts
      provides: canonical transition map, event vocabulary, and transition application helpers
      exports: [LIFECYCLE_TRANSITION_MATRIX, APPROVAL_REQUIRED_TRANSITIONS, applyTransition, deriveNextEvent]
    - path: contracts/F-292-saas-lifecycle-transition-v1.yaml
      provides: lifecycle transition request contract and valid-state transition table
  key_links:
    - from: lib/markos/saas/core/lifecycle.ts
      to: supabase/migrations/126_saas_subscription_lifecycle.sql
      via: app-layer and trigger-layer share the same transition matrix
      pattern: "upgrade_pending"
    - from: lib/markos/saas/core/lifecycle.ts
      to: lib/markos/saas/core/contracts.ts
      via: imports SUBSCRIPTION_STATES + SUBSCRIPTION_EVENT_TYPES from 214-02 (no re-derivation)
      pattern: "SUBSCRIPTION_STATES"
---

<objective>
Make subscription lifecycle behavior explicit, idempotent, approval-aware, and rollback-capable so downstream billing and health phases read a stable contract instead of implicit state changes.

Backend doctrine ACs folded from 214-UI-SPEC.md: BD-7 (8-state machine + 16-transition matrix + invalid-transition + approval-required triggers), BD-6 (idempotency, co-owned with 214-02). The 16-event vocabulary and 16-transition matrix are exported as TS literal-union tuples so 214-05 mutation routing and 214-06 UI consume identical literals.
</objective>

<context>
@.planning/phases/214-saas-suite-activation-subscription-core/214-RESEARCH.md
@.planning/phases/214-saas-suite-activation-subscription-core/214-REVIEWS.md
@.planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md
@.planning/phases/214-saas-suite-activation-subscription-core/214-VALIDATION.md
</context>

<tasks>
<task id="214-03-01" tdd="true">
  <name>BD-7 (+ BD-6 co-own): Subscription lifecycle state machine, 16-event vocabulary, 16-transition matrix, idempotent transition requests, rollback contract</name>
  <files>supabase/migrations/126_saas_subscription_lifecycle.sql, lib/markos/saas/core/lifecycle.ts, lib/markos/saas/core/transition-guard.ts, lib/markos/saas/core/idempotency.ts, lib/markos/saas/core/index.cjs, contracts/F-292-saas-lifecycle-transition-v1.yaml, contracts/flow-registry.json, test/saas-214/domain-3/lifecycle-transition-table.test.js, test/saas-214/domain-3/lifecycle-approval-required.test.js, test/saas-214/domain-3/lifecycle-idempotency.test.js, test/saas-214/domain-3/lifecycle-rollback.test.js</files>
  <read_first>
    - .planning/phases/214-saas-suite-activation-subscription-core/214-RESEARCH.md section "Domain 3 - Subscription lifecycle state machine"
    - .planning/phases/214-saas-suite-activation-subscription-core/214-REVIEWS.md notes about the missing explicit transition table
    - .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md §Surface A data dependencies (8 states + 16 event types verbatim)
    - .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md §Surface C state matrix (8 lifecycle states drive the .c-badge--{state} render rule)
    - .planning/phases/207-agentrun-v2-orchestration-substrate/207-UI-SPEC.md §RunApiEnvelope + AgentRunEventType (the 207 event taxonomy is bridged when `agent_run_id` is set on a transition; otherwise the 16 SaaS event-vocabulary literals stand alone)
    - lib/markos/saas/core/contracts.ts (from 214-02; SUBSCRIPTION_STATES + SUBSCRIPTION_EVENT_TYPES exports)
  </read_first>
  <behavior>
    - Test (BD-7 transition table): `LIFECYCLE_TRANSITION_MATRIX` has exactly 16 entries; each entry maps `{from_state, to_state, emit_event, requires_approval}` per the canonical list; the 7 APPROVAL_REQUIRED transitions are exactly: trialing→canceled, active→paused, active→upgrade_pending, active→downgrade_pending, active→canceled, past_due→canceled, paused→active, paused→canceled.
    - Test (BD-7 invalid transition trigger): inserting a transition request with `(prior_state='draft', requested_state='paused')` raises `SAAS_SUBSCRIPTION_INVALID_TRANSITION`.
    - Test (BD-7 approval-required trigger): inserting a transition request with `(prior_state='active', requested_state='canceled', mutation_request_id=NULL)` raises `SAAS_SUBSCRIPTION_TRANSITION_REQUIRES_APPROVAL`; the same transition with a populated `mutation_request_id` succeeds.
    - Test (BD-7 auto transitions): inserting a transition request with `(prior_state='active', requested_state='past_due', mutation_request_id=NULL)` succeeds (auto-transition does not require approval).
    - Test (BD-6 idempotency): inserting two transition requests with identical `(tenant_id, subscription_id, idempotency_key)` raises a unique-constraint violation.
    - Test (rollback contract): when a transition request is marked `rolled_back`, the `rollback_to_state` field is non-null AND a `rollback_applied` event row is inserted into `saas_subscription_events`; the original request's `prior_state` and `requested_state` columns are NOT mutated (history preserved).
    - Test (TS export parity): `LIFECYCLE_TRANSITION_MATRIX.length === 16`; `APPROVAL_REQUIRED_TRANSITIONS.length === 8` (7 unique + the `paused→active` reactivation requirement); all entries reference literals exported from contracts.ts (no string drift).
    - Test (event derivation): `deriveNextEvent('active', 'paused')` returns the 2-step sequence `['pause_requested', 'pause_applied']` matching the 16-event vocabulary; `deriveNextEvent('active', 'past_due')` returns the single-step `['payment_failed']`.
  </behavior>
  <acceptance_criteria>
    Verify: `npm test -- test/saas-214/domain-3/` returns 0 failures.
    Verify: `grep -cE "(SAAS_SUBSCRIPTION_INVALID_TRANSITION|SAAS_SUBSCRIPTION_TRANSITION_REQUIRES_APPROVAL)" supabase/migrations/126_saas_subscription_lifecycle.sql` returns >= 2 (both triggers present).
    Verify: `node -e "const c=require('./lib/markos/saas/core/lifecycle.ts'); console.log(c.LIFECYCLE_TRANSITION_MATRIX.length)"` (or equivalent ts-runner) prints `16`.
    Verify: `grep -E "rollback_applied" supabase/migrations/126_saas_subscription_lifecycle.sql lib/markos/saas/core/lifecycle.ts` returns >= 2 hits across the two files.
  </acceptance_criteria>
  <action>
    Step 1: Create `saas_subscription_transition_requests` (`request_id` uuid pk; `tenant_id` uuid; `subscription_id` uuid FK; `prior_state` text CHECK in the 8-literal list; `requested_state` text CHECK in the 8-literal list; `mutation_request_id` uuid NULL forward-FK to 214-05 `saas_subscription_mutation_requests`; `rollback_to_state` text NULL CHECK in 8-literal list; `idempotency_key` text NOT NULL; `status` text CHECK in `'pending'|'applied'|'rolled_back'|'rejected'`; `created_at` timestamptz; UNIQUE `(tenant_id, subscription_id, idempotency_key)`).
    Step 2: Create the canonical 16-transition matrix as a SQL CTE that the validator function reads. Implement function `saas_validate_transition(prior, requested) RETURNS boolean` returning true IFF the pair is in the matrix.
    Step 3: Create function `saas_transition_requires_approval(prior, requested) RETURNS boolean` returning true for the 8 approval-required transitions.
    Step 4: Create trigger `SAAS_SUBSCRIPTION_INVALID_TRANSITION` on `saas_subscription_transition_requests` BEFORE INSERT raising when the validator returns false (`errcode 'SAAS06'`, hint `'Transition <prior>→<requested> is not in the canonical 16-transition matrix.'`).
    Step 5: Create trigger `SAAS_SUBSCRIPTION_TRANSITION_REQUIRES_APPROVAL` on `saas_subscription_transition_requests` BEFORE INSERT raising when the approval-required function returns true AND `mutation_request_id IS NULL` (`errcode 'SAAS07'`, hint `'Transition <prior>→<requested> requires a linked saas_subscription_mutation_requests.mutation_request_id.'`).
    Step 6: Implement `lib/markos/saas/core/lifecycle.ts`:
      - Import `SUBSCRIPTION_STATES`, `SUBSCRIPTION_EVENT_TYPES` from `./contracts.ts` (no re-derivation).
      - Export `LIFECYCLE_TRANSITION_MATRIX: ReadonlyArray<{from: SubscriptionState; to: SubscriptionState; emit: ReadonlyArray<SubscriptionEventType>; requires_approval: boolean}>` with exactly 16 entries verbatim from the canonical list.
      - Export `APPROVAL_REQUIRED_TRANSITIONS` derived from the matrix.
      - Export `applyTransition(client, {tenant_id, subscription_id, prior_state, requested_state, mutation_request_id?, idempotency_key})` that inserts the request, lets triggers validate, then on success appends the appropriate event(s) to `saas_subscription_events`.
      - Export `deriveNextEvent(prior_state, requested_state): ReadonlyArray<SubscriptionEventType>` returning the canonical event sequence (1 or 2 events) for that transition.
    Step 7: Implement `transition-guard.ts` (app-layer pre-check that mirrors the trigger so callers get typed errors before round-tripping the DB) and `idempotency.ts` (idempotency-key generator + collision check helper).
    Step 8: Create `contracts/F-292-saas-lifecycle-transition-v1.yaml` with the matrix table and approval-required flag column. Update `contracts/flow-registry.json`.
    Step 9: Land the 4 domain-3 test files; each `test()` block cites BD-6 or BD-7 in its name.
  </action>
  <verify>
    <automated>npm test -- test/saas-214/domain-3/</automated>
  </verify>
  <done>The subscription state machine is explicit (8 states), the 16-transition matrix is encoded as both SQL CHECK and TS literal-union, impossible transitions fail closed (BD-7), approval-required transitions cannot bypass the `mutation_request_id` link (BD-7), idempotency uniqueness prevents duplicate application (BD-6), and rollbacks write history-preserving event rows. UI-SPEC ACs BD-6 + BD-7 satisfied at the lifecycle layer.</done>
</task>
</tasks>

<verification>
- npm test -- test/saas-214/domain-3/
</verification>

<success_criteria>
- Migration 126 ships with the transition-request table, the 16-transition validator function, the approval-required function, and both triggers.
- `lib/markos/saas/core/lifecycle.ts` exports `LIFECYCLE_TRANSITION_MATRIX` (16 entries), `APPROVAL_REQUIRED_TRANSITIONS`, `applyTransition`, and `deriveNextEvent` with no string-literal drift from `contracts.ts`.
- F-292 registered without collision.
- Idempotency uniqueness is enforced; rollback contract preserves history via the dedicated `rollback_applied` event.
</success_criteria>
</content>
