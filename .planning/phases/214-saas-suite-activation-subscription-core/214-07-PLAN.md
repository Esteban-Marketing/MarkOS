---
phase: 214
plan: "07"
type: execute
wave: 5
depends_on:
  - "214-06"
f_ids: []
migration_slots: []
integrates_with:
  - "PRC-09 from P205 (Pricing Engine ownership; render via <Money fromPricingRecommendation={pr_id} /> OR verbatim {{MARKOS_PRICING_ENGINE_PENDING}} sentinel)"
  - "208-04 Approval Inbox (this plan extends the handoff_kind filter chip set with `billing_charge_approval` and the family map with the 7 mutation_family literals)"
translation_gate_for: []
files_modified:
  - app/(markos)/settings/saas/page.tsx
  - app/(markos)/settings/saas/page.module.css
  - app/(markos)/settings/saas/page.stories.tsx
  - components/markos/tenant/SaaSActivationPanel.tsx
  - components/markos/tenant/SaaSActivationPanel.module.css
  - components/markos/tenant/SaaSActivationPanel.stories.tsx
  - components/markos/tenant/SaaSSubscriptionsTable.tsx
  - components/markos/tenant/SaaSSubscriptionsTable.module.css
  - components/markos/tenant/SaaSSubscriptionsTable.stories.tsx
  - app/(markos)/layout-shell.tsx
  - test/saas-214/domain-6/saas-ui-gating.test.js
  - test/ui-a11y/214-saas-a11y.test.js
  - .planning/phases/214-saas-suite-activation-subscription-core/214-VALIDATION.md
autonomous: false
requirements:
  - SAAS-01
  - SAAS-02
  - SAAS-03
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - QA-05
  - QA-06
ui_spec_fold:
  source: .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md
  fold_date: 2026-04-29
  scope: ui_render
  parent_plan_split: "Plan 214-06 split into 214-06 (backend gating + API + MCP) and 214-07 (UI render, this plan)."
  acs_folded:
    # Settings page (15 ACs)
    - SS-1   # page.tsx + page.module.css consume only var(--*) tokens (D-08)
    - SS-2   # h1 SaaS Suite renders for business_type='saas' tenants only
    - SS-3   # non-SaaS tenant gating notice verbatim copy via .c-notice c-notice--info
    - SS-4   # pre-activation hides Plans/Subs/Mutations/Lifecycle; only Growth placeholder visible
    - SS-5   # Plans section vanilla <table> (D-14) with .t-label-caps headers + .c-badge--{state}
    - SS-6   # Plan-row Price column <Money /> OR {{MARKOS_PRICING_ENGINE_PENDING}} verbatim
    - SS-7   # Plan-row never hard-codes tier-name strings; renders plan.name from saas_plans
    - SS-8   # Subscriptions section renders <SaaSSubscriptionsTable /> with 8 lifecycle states in MixedStates story
    - SS-9   # Pending mutations vanilla <table> with .c-button--tertiary mint-text "Open approval →" link
    - SS-10  # Lifecycle history with .c-status-dot{,--live,--error} markers per event severity
    - SS-11  # Growth extension renders <PlaceholderBanner variant="future_phase_217"> always; .c-chip when hint set
    - SS-12  # All .c-notice instances use 4 valid state variants; zero local .banner/.alert/.warning/.callout (D-09b)
    - SS-13  # All .c-card use default; zero .c-card--feature (D-13)
    - SS-14  # WCAG 2.1 AA: focus order, 44px touch target via global rule, color+glyph dual-signal
    - SS-15  # page.stories.tsx with ≥8 named state stories registered as Settings/SaaS
    # Activation panel (7 ACs)
    - AP-1   # SaaSActivationPanel.tsx + .module.css consume only var(--*) tokens
    - AP-2   # 5 variants verbatim: pre-activation | wizard-in-progress | activated | activation-failed | tenant-0-gate-closed
    - AP-3   # 6 wizard steps in order; aria-live polite step counter
    - AP-4   # Step 6 [ok] Ready to activate notice; submit disabled when tenant 0 gate closed
    - AP-5   # Activated heading [ok] SaaS Suite active + .c-status-dot--live (kernel-pulse + reduced-motion freeze)
    - AP-6   # .c-modal focus trap; ESC closes wizard with confirmation if draft non-empty
    - AP-7   # SaaSActivationPanel.stories.tsx with ≥7 named state stories
    # Subscriptions table (8 ACs)
    - ST-1   # SaaSSubscriptionsTable.tsx + .module.css consume only var(--*) tokens
    - ST-2   # 8 lifecycle states with verbatim copy from state-badge map; color+glyph dual-signal
    - ST-3   # Action menu exposes 6 lifecycle mutations + audit-log export with verbatim mutation-class chips
    - ST-4   # Each mutation modal verbatim copy; Cancel uses .c-button--destructive; non-destructive .c-button--primary
    - ST-5   # Pending-approval row chip + Open approval → link routes to /operations/approvals?handoff_kind=billing_charge_approval&mutation_request_id={id}
    - ST-6   # Autonomy-ceiling notice [block] glyph + non-export menu items disabled
    - ST-7   # Run chip .c-chip-protocol with agent_run_id link to /operations/tasks?run_id={id}
    - ST-8   # SaaSSubscriptionsTable.stories.tsx with ≥7 named state stories
    # Surface gating (2 client-side ACs from 3-AC group; SG-1 + SG-3 in 214-06)
    - SG-2   # Sidebar nav row hidden via aria-hidden + display:none on isSaaSSurfaceEnabled==false
    # Cross-cutting (10 ACs — all carry-forward to UI files)
    - X-1    # zero hex literals; allowed exception list 1px/2px/4px/44px
    - X-2    # color + bracketed glyph dual-signal everywhere
    - X-3    # zero banned-lexicon words; zero exclamation points in product copy
    - X-4    # ≥3 named state stories per extracted component; ≥7 page stories
    - X-5    # 44px touch target via global (pointer: coarse) rule; modules do not redeclare
    - X-6    # all animated elements freeze under prefers-reduced-motion: reduce
    - X-7    # zero hard-coded $\d literals; zero tier-name strings; pricing via <Money /> OR sentinel
    - X-8    # UI gating test verifies via isSaaSSurfaceEnabled, not by re-implementing the rule
    - X-9    # all stories register under explicit meta.title paths; ≥21 SaaS stories total
    - X-10   # UI gating test + growth-extension non-activation test (UI half — placeholder banner renders)
  storybook_story_register:
    "app/(markos)/settings/saas/page.stories.tsx":
      meta_title: "Settings/SaaS"
      stories: [Default, PreActivation, NotSaaSTenant, WizardInProgress, PendingApproval, ActivationFailed, Tenant0GateClosed, GrowthExtensionPlaceholder]
    "components/markos/tenant/SaaSActivationPanel.stories.tsx":
      meta_title: "Tenant/SaaSActivationPanel"
      stories: [PreActivation, WizardStep1, WizardStep4, WizardStep6Review, Activated, ActivationFailed, Tenant0GateClosed]
    "components/markos/tenant/SaaSSubscriptionsTable.stories.tsx":
      meta_title: "Tenant/SaaSSubscriptionsTable"
      stories: [Default, Empty, AllActive, MixedStates, PendingApproval, AutonomyCeilingReached, MutationModalOpen]
must_haves:
  truths:
    - "`app/(markos)/settings/saas/page.tsx` exists as a server component reading tenant context, activation, subscriptions, pending mutations; `app/(markos)/settings/saas/page.module.css` exists token-only (SS-1)."
    - "Page heading `<h1>SaaS Suite</h1>` renders only for `business_type == 'saas'` tenants (SS-2); non-SaaS tenants see only the gating notice with verbatim copy `[info] SaaS Suite is reserved for SaaS-business tenants. Contact support to change tenant type.` (SS-3 + SG-2)."
    - "`<SaaSActivationPanel />` extracted to `components/markos/tenant/SaaSActivationPanel.tsx` with 5 variants (`pre-activation | wizard-in-progress | activated | activation-failed | tenant-0-gate-closed`) and a 6-step wizard (Confirm business type / Launch countries / Payment processors / Accounting systems / Autonomy posture / Review & activate) (AP-1..AP-7)."
    - "`<SaaSSubscriptionsTable />` extracted to `components/markos/tenant/SaaSSubscriptionsTable.tsx` rendering all 8 lifecycle states with verbatim copy, action menu exposing 6 lifecycle mutations + audit-log export, mutation-class chips bound to `billing.charge` (lifecycle) or `data.export` (audit-log), pending-approval row chip + `Open approval →` deep-link, autonomy-ceiling [block] notice, agent_run_id `.c-chip-protocol` deep-link to `/operations/tasks?run_id={id}` (ST-1..ST-8)."
    - "Pricing rule: every cost-bearing render uses `<Money fromPricingRecommendation={pr_id} />` (when `pricing_recommendation_id` resolves AND `recommendation_status='approved'`) OR the verbatim 35-character literal `{{MARKOS_PRICING_ENGINE_PENDING}}` (X-7 + SS-6). Zero hard-coded `$\\d` literals, zero hard-coded tier-name strings (`Growth Monthly`, `Starter`, `Professional`) anywhere in `app/(markos)/settings/saas/**` or `components/markos/tenant/SaaS*.{tsx,module.css,stories.tsx}`."
    - "Growth-mode metadata-only render: `<PlaceholderBanner variant=\"future_phase_217\">` ALWAYS rendered when activated; `.c-chip` with `[info] Growth extension hint: {hint}` below ONLY when `saas_suite_activations.growth_extension.growth_mode_hint` is set (SS-11 + X-10 UI half)."
    - "Tenant 0 readiness gate UI render: when `getTenant0ReadinessGateStatus()` returns `'closed'`, the activation wizard step 6 renders `.c-notice c-notice--warning` with verbatim copy `[warn] Tenant 0 readiness gate is closed. SaaS Suite activation is paused until the v4.0.0 readiness sign-off completes.` AND submit is disabled (AP-4)."
    - "Sidebar nav row for SaaS settings is hidden via `aria-hidden=\"true\"` + `display: none` when `isSaaSSurfaceEnabled` returns false (SG-2). The nav row is added to `app/(markos)/layout-shell.tsx` after `Billing` and before `Members` per UI-SPEC §From 208-UI-SPEC inheritance."
    - "Approval Inbox extension: `app/(markos)/operations/approvals/page.tsx` filter chip group (208-04) is extended at execute time to add `billing_charge_approval` to the `handoff_kind` filter set AND the 7 mutation_family literals (`subscription_create | subscription_upgrade | subscription_downgrade | subscription_pause | subscription_cancel | subscription_reactivate | audit_log_export`) to the family map (UI-SPEC §From 208-UI-SPEC inheritance + §Reviewer notes 5)."
    - "All visual decisions cite DESIGN.md v1.1.0 token names verbatim: `--color-surface` (page bg), `--color-surface-raised` (.c-card), `--color-surface-overlay` (.c-modal), `--color-border` (1px hairlines), `--color-on-surface` (primary text), `--color-on-surface-muted` (.t-lead, eyebrows, metadata), `--color-on-surface-subtle` (placeholders, dimmed canceled rows), `--color-primary` (single CTA fills + `.c-status-dot--live` kernel pulse), `--color-primary-text` (action-link CTAs + `.c-chip-protocol` text + `[ok]` glyph), `--color-error/--warning/--success/--info` (state-coded notices/badges/glyphs)."
    - "Architecture-lock UI carry-forward (D-08..D-15 from 213.4-VALIDATION): D-08 token-only (zero hex), D-09 mint-as-text (action-link inline + chip-protocol + [ok] glyph), D-09b .c-notice mandatory (zero local banner/alert/warning/callout classes), D-13 .c-card--feature FORBIDDEN this phase, D-14 vanilla <table> for plan catalog + subscriptions + mutations + lifecycle, D-15 selective extraction of SaaSActivationPanel + SaaSSubscriptionsTable to components/markos/tenant/."
    - "Architecture-lock 4 doctrine-violation strings (from 214-01 forbidden-string list) MUST NOT appear in any UI file: `bypass surface gating`, `pricing override`, `growth runnable in 214`, `crm second customer truth`."
  artifacts:
    - path: app/(markos)/settings/saas/page.tsx
      provides: server component reading tenant context, activation, subscriptions, pending mutations, lifecycle events; renders one of 9 documented states (loading | not-saas-tenant-blocked | pre-activation | wizard-in-progress | activated-no-subscriptions | activated-with-subscriptions | pending-approval | activation-failed | tenant-0-gate-closed | growth-extension-pending)
      min_lines: 80
    - path: app/(markos)/settings/saas/page.module.css
      provides: token-only page-level CSS; consumes only var(--*); zero hex literals; ≥30% whitespace per DESIGN.md "Whitespace as primitive"
      min_lines: 40
    - path: app/(markos)/settings/saas/page.stories.tsx
      provides: 8 named state stories registered as `Settings/SaaS` in storybook-static/index.json
      contains: "Default,PreActivation,NotSaaSTenant,WizardInProgress,PendingApproval,ActivationFailed,Tenant0GateClosed,GrowthExtensionPlaceholder"
    - path: components/markos/tenant/SaaSActivationPanel.tsx
      provides: 'use client' subcomponent with 5 variants; 6-step wizard; .c-modal focus-trap; aria-live step counter
      min_lines: 200
    - path: components/markos/tenant/SaaSActivationPanel.stories.tsx
      provides: 7 named state stories registered as `Tenant/SaaSActivationPanel`
    - path: components/markos/tenant/SaaSSubscriptionsTable.tsx
      provides: vanilla <table> with 6 columns; 8 lifecycle state badges; per-row action menu with 7 menu items (6 lifecycle + audit-log export); 7 mutation-confirm modals
      min_lines: 250
    - path: components/markos/tenant/SaaSSubscriptionsTable.stories.tsx
      provides: 7 named state stories registered as `Tenant/SaaSSubscriptionsTable`
    - path: test/saas-214/domain-6/saas-ui-gating.test.js
      provides: UI gating regression — verifies non-SaaS tenants see only the gating notice; verifies isSaaSSurfaceEnabled is consumed (not re-implemented)
    - path: test/ui-a11y/214-saas-a11y.test.js
      provides: ≥30 individual test() blocks; ≥7 AC# mentions; mirrors 213-4-admin-ops-a11y.test.js pattern
  key_links:
    - from: app/(markos)/settings/saas/page.tsx
      to: lib/markos/saas/core/surface-gates.ts
      via: server-side gate read (isSaaSSurfaceEnabled); when false renders only the gating notice
      pattern: "isSaaSSurfaceEnabled"
    - from: components/markos/tenant/SaaSSubscriptionsTable.tsx
      to: /operations/approvals
      via: "Open approval →" deep-link with handoff_kind=billing_charge_approval&mutation_request_id={id}
      pattern: "billing_charge_approval"
    - from: components/markos/tenant/SaaSSubscriptionsTable.tsx
      to: /operations/tasks
      via: agent_run_id .c-chip-protocol deep-link to /operations/tasks?run_id={id}
      pattern: "run_id"
    - from: app/(markos)/layout-shell.tsx
      to: lib/markos/saas/core/surface-gates.ts
      via: nav row visibility driven by isSaaSSurfaceEnabled (SG-2)
      pattern: "aria-hidden"
---

<objective>
Render the SaaS Suite operator surface: one settings page (`app/(markos)/settings/saas/page.tsx`) plus two extracted reusable components (`<SaaSActivationPanel />`, `<SaaSSubscriptionsTable />`) consumed by P215-P217 SaaS subsurfaces. All visual decisions cite DESIGN.md v1.1.0 token names verbatim. All copy strings come from the UI-SPEC §Surface A/B/C copy registers verbatim. All pricing renders go through `<Money fromPricingRecommendation={pr_id} />` or the verbatim `{{MARKOS_PRICING_ENGINE_PENDING}}` sentinel.

This is the UI render half of Plan 214-06. Backend gating + API + MCP are owned by Plan 214-06 (this plan depends on it).

Surface ACs folded from 214-UI-SPEC.md: SS-1..15, AP-1..7, ST-1..8, SG-2, X-1..10.
</objective>

<context>
@.planning/phases/214-saas-suite-activation-subscription-core/214-RESEARCH.md
@.planning/phases/214-saas-suite-activation-subscription-core/214-REVIEWS.md
@.planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md
@.planning/phases/214-saas-suite-activation-subscription-core/214-VALIDATION.md
@DESIGN.md
@CLAUDE.md
@.planning/phases/205-pricing-engine-foundation-billing-readiness/205-UI-SPEC.md
@.planning/phases/206-soc2-type1-foundation/206-UI-SPEC.md
@.planning/phases/207-agentrun-v2-orchestration-substrate/207-UI-SPEC.md
@.planning/phases/208-human-operating-interface/208-UI-SPEC.md
@.planning/phases/213-tenant0-dogfood-compliance-validation/213-UI-SPEC.md
@app/(markos)/settings/billing/page.tsx
@app/(markos)/settings/billing/page.module.css
@app/(markos)/layout-shell.tsx
@styles/components.css
@app/tokens.css
</context>

<tasks>
<task id="214-07-01" tdd="true">
  <name>SS-1..15, SG-2, X-1..10 (page slice): Settings/SaaS page render — server component, token-only CSS, 8 Storybook state stories, gating notice, lifecycle/mutation tables (vanilla &lt;table&gt;), pricing via &lt;Money /&gt; or sentinel, growth placeholder</name>
  <files>app/(markos)/settings/saas/page.tsx, app/(markos)/settings/saas/page.module.css, app/(markos)/settings/saas/page.stories.tsx, app/(markos)/layout-shell.tsx, test/saas-214/domain-6/saas-ui-gating.test.js</files>
  <read_first>
    - .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md §Surface A (entire section: layout grid, copy register, state matrix, data dependencies, accessibility focus order, motion, mobile breakpoint)
    - .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md §Acceptance Criteria SS-1..SS-15 + SG-2 + X-1..X-10
    - .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md §Surface gating contract (verbatim non-SaaS notice copy)
    - .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md §Pricing placeholder rule (verbatim sentinel)
    - .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md §Growth-mode metadata-only contract (PlaceholderBanner variant="future_phase_217")
    - DESIGN.md §Tokens (var(--color-*), var(--space-*), var(--font-*))
    - DESIGN.md §Components (.c-card, .c-button{,--*}, .c-notice c-notice--{state}, .c-badge--{state}, .c-status-dot{,--live,--error}, .c-chip-protocol)
    - DESIGN.md §Composition proportion (60/30/10 surface/text/accent; ≥30% whitespace)
    - CLAUDE.md §Visual Design Canon — non-negotiable rules; banned lexicon
    - app/(markos)/settings/billing/page.tsx (213.3-01 canonical settings shell pattern)
    - app/(markos)/settings/billing/page.module.css (token-only reference)
    - app/(markos)/layout-shell.tsx (NavList nav-row insertion pattern from 213.1-01)
    - lib/markos/saas/core/surface-gates.ts (from 214-06; reads isSaaSSurfaceEnabled)
    - lib/markos/saas/core/contracts.ts (from 214-02; SUBSCRIPTION_STATES + SUBSCRIPTION_EVENT_TYPES + PRICING_PLACEHOLDER_SENTINEL exports)
  </read_first>
  <behavior>
    - Test (SS-1 token-only): `grep -E "#[0-9a-fA-F]{3,8}" app/(markos)/settings/saas/page.module.css` returns 0 hits.
    - Test (SS-1 spacing): every `padding|margin|gap` value in page.module.css cites a `var(--space-*)` token; allowed exceptions are `1px`, `2px`, `4px`, `44px`.
    - Test (SS-2 gated render): `getServerSideProps`-equivalent (Next.js server component) calls `isSaaSSurfaceEnabled(tenantId)` first; when false, returns ONLY the gating notice (no `<h1>SaaS Suite</h1>` rendered).
    - Test (SS-3 verbatim copy): page renders the literal string `[info] SaaS Suite is reserved for SaaS-business tenants. Contact support to change tenant type.` inside a `<div class="c-notice c-notice--info">` for non-SaaS tenants.
    - Test (SS-4 pre-activation): when activation_status='draft', page renders only `<SaaSActivationPanel variant="pre-activation" />` and the Growth extension placeholder; Plans/Subs/Mutations/Lifecycle sections are absent.
    - Test (SS-5 vanilla table): Plans section uses `<table>` (not `.c-table`); `<th>` cells carry the `.t-label-caps` class; row state uses `.c-badge--{success | info}` per the published/draft/archived map.
    - Test (SS-6 + X-7 pricing): page.tsx contains zero `$\d` literals (excluding the verbatim sentinel string). Plan-row Price column renders `<Money fromPricingRecommendation={planVersion.pricing_recommendation_id} />` when set; otherwise renders `<PlaceholderBanner variant="billing_placeholder">{{MARKOS_PRICING_ENGINE_PENDING}}</PlaceholderBanner>` verbatim.
    - Test (SS-7 + X-7 tier names): `grep -E '"Growth Monthly"|"Starter"|"Professional"' app/(markos)/settings/saas/` returns 0 hits. Plan-row title cell renders `{plan.name}` directly from the row.
    - Test (SS-8 subs render): page renders `<SaaSSubscriptionsTable subscriptions={...} pendingMutationsBySubscriptionId={...} onRequestMutation={...} />`.
    - Test (SS-9 mutation table): pending mutations section renders a vanilla `<table>` of `saas_subscription_mutation_requests` with `status='requested'`; each row has a `.c-button c-button--tertiary` link with text `Open approval →` and href matching `/operations/approvals?handoff_kind=billing_charge_approval&mutation_request_id={id}`.
    - Test (SS-10 lifecycle history): last 7 days of events render with timestamps in `.c-field__help`-style markup; each row carries a `.c-status-dot{,--live,--error}` per the event-severity mapping.
    - Test (SS-11 growth placeholder): page renders `<PlaceholderBanner variant="future_phase_217">` ALWAYS when activated; `.c-chip` with literal `[info] Growth extension hint: {hint}` appears below only when `growth_mode_hint` is set.
    - Test (SS-12 .c-notice mandatory + D-09b): `grep -E "\\.banner|\\.alert|\\.warning|\\.callout" app/(markos)/settings/saas/page.module.css` returns 0 hits. All `.c-notice` instances use only `c-notice--{info,warning,success,error}`.
    - Test (SS-13 D-13): `grep -c "c-card--feature" app/(markos)/settings/saas/page.tsx app/(markos)/settings/saas/page.module.css` returns 0.
    - Test (SS-14 a11y): page has programmatic focus on route change; every state badge pairs color with bracketed glyph (`[ok]`/`[warn]`/`[err]`/`[info]`/`[block]`); 44px touch target inherits via global `(pointer: coarse)` rule (no module-level redeclaration).
    - Test (SS-15 stories): `app/(markos)/settings/saas/page.stories.tsx` exports 8 named stories (`Default`, `PreActivation`, `NotSaaSTenant`, `WizardInProgress`, `PendingApproval`, `ActivationFailed`, `Tenant0GateClosed`, `GrowthExtensionPlaceholder`); all register under `meta.title = 'Settings/SaaS'` in `storybook-static/index.json`.
    - Test (SG-2 nav gating): `app/(markos)/layout-shell.tsx` adds a `SaaS` nav entry between `Billing` and `Members`; the nav `<li>` carries `aria-hidden="true"` and `display:none` when `isSaaSSurfaceEnabled` returns false.
    - Test (X-1 hex literals): `grep -E "#[0-9a-fA-F]{3,8}" app/(markos)/settings/saas/page.tsx app/(markos)/settings/saas/page.module.css app/(markos)/settings/saas/page.stories.tsx` returns 0 hits.
    - Test (X-2 dual-signal): every state badge instance contains both a state class (`.c-badge--{state}`) AND a bracketed glyph (`[ok]`/`[warn]`/`[err]`/`[info]`/`[block]`).
    - Test (X-3 banned lexicon): `grep -wiE "synergy|leverage|empower|unlock|transform|revolutionize|supercharge|holistic|seamless|cutting-edge|innovative|game-changer|next-generation|world-class|best-in-class|reimagine|disrupt" app/(markos)/settings/saas/` returns 0 hits. `grep -c "!" app/(markos)/settings/saas/page.tsx app/(markos)/settings/saas/page.stories.tsx` (excluding `!important` CSS and `!=` operators in JS) returns 0 in product copy strings.
    - Test (X-8 gate consumption): `grep -c "isSaaSSurfaceEnabled" test/saas-214/domain-6/saas-ui-gating.test.js` returns >= 1 (test imports the helper, never re-implements the logic).
    - Test (X-10 growth UI): `test/saas-214/domain-6/saas-ui-gating.test.js` includes a case asserting `<PlaceholderBanner variant="future_phase_217">` renders for any non-empty growth_mode_hint, and asserts no `app/(markos)/growth/` or `/api/v1/saas/growth/` route file exists.
    - Test (architecture-lock 4 doctrine strings): `grep -ciE "bypass surface gating|pricing override|growth runnable in 214|crm second customer truth" app/(markos)/settings/saas/` returns 0.
  </behavior>
  <acceptance_criteria>
    Verify: `npm test -- test/saas-214/domain-6/saas-ui-gating.test.js` returns 0 failures.
    Verify: `npx @google/design.md lint DESIGN.md` continues to PASS (no token drift introduced by this plan's CSS).
    Verify: storybook build green: `npm run build-storybook 2>&1 | tail -20` shows zero errors AND `storybook-static/index.json` contains 8 entries under `Settings/SaaS`.
    Verify: `grep -E "#[0-9a-fA-F]{3,8}" app/(markos)/settings/saas/` returns 0 lines (D-08).
    Verify: `grep -E "c-card--feature|\\.banner|\\.alert(\\b|--)|\\.callout" app/(markos)/settings/saas/` returns 0 lines (D-09b + D-13).
    Verify: `grep -E "\\$[0-9]" app/(markos)/settings/saas/` returns 0 lines (X-7); `grep -c "MARKOS_PRICING_ENGINE_PENDING" app/(markos)/settings/saas/page.tsx` returns >= 1.
    Verify: `grep -c "billing_charge_approval" app/(markos)/settings/saas/page.tsx` returns >= 1 (SS-9 deep-link).
    Verify: `grep -wiE "synergy|leverage|empower|unlock|transform|revolutionize|supercharge|holistic|seamless|cutting-edge|innovative|game-changer|next-generation|world-class|best-in-class|reimagine|disrupt" app/(markos)/settings/saas/` returns 0.
  </acceptance_criteria>
  <action>
    Step 1: Create `app/(markos)/settings/saas/page.tsx` as a Next.js server component. Read `tenantId` from `requireHostedSupabaseAuth` (mirror `app/(markos)/settings/billing/page.tsx`). Call `isSaaSSurfaceEnabled(tenantId)`; when false, return the gating notice JSX (single `<div class="c-notice c-notice--info">[info] SaaS Suite is reserved for SaaS-business tenants. Contact support to change tenant type.</div>`) and exit. When true, fetch activation, plans, subscriptions, pending mutations, lifecycle events (last 7 days) via the 214-02..05 server-side helpers; render the 9-state matrix from UI-SPEC §Surface A.
    Step 2: Render the layout grid from UI-SPEC §Surface A verbatim — `<h1>SaaS Suite</h1>`, descriptor `<p class="t-lead">{N} subscription(s)...`, `<SaaSActivationPanel />`, `<h2>Plans</h2>` + vanilla `<table>` with the 5 columns (Plan / Version / Price / Discount / Status), `<h2>Subscriptions</h2>` + `<SaaSSubscriptionsTable />`, `<h2>Pending mutations</h2>` + vanilla `<table>` with deep-link CTA, `<h2>Lifecycle history (last 7 days)</h2>` + vanilla `<table>` with status-dot markers, `<h2>Growth extension</h2>` + `<PlaceholderBanner variant="future_phase_217">` + optional `.c-chip`.
    Step 3: All copy strings come from UI-SPEC §Surface A copy register verbatim — including the 7 empty-state messages (Plans empty / Subscriptions empty / Pending mutations empty / Lifecycle history empty / Growth extension placeholder body / Activation-failed notice / Tenant 0 readiness gate closed).
    Step 4: Pricing rule: import `<Money />` recipe (existing per 208-UI-SPEC) and `<PlaceholderBanner />` recipe. Plan-row Price cell: `{planVersion.pricing_recommendation_id ? <Money fromPricingRecommendation={planVersion.pricing_recommendation_id} /> : <PlaceholderBanner variant="billing_placeholder">{'{{MARKOS_PRICING_ENGINE_PENDING}}'}</PlaceholderBanner>}`. ZERO hard-coded `$` literals; ZERO hard-coded tier-name strings.
    Step 5: Mutation row deep-link: `<a class="c-button c-button--tertiary" href={`/operations/approvals?handoff_kind=billing_charge_approval&mutation_request_id=${row.mutation_request_id}`}>Open approval →</a>`.
    Step 6: Create `app/(markos)/settings/saas/page.module.css` token-only:
      - Page wrapper: `padding: var(--space-xl) var(--space-lg)`; `max-width: var(--w-container)`; `background: var(--color-surface)`.
      - Section gap: `gap: var(--space-lg)`; section vertical rhythm via `var(--space-md)`.
      - Vanilla table recipe: `width: 100%; border-collapse: collapse;` + `<th>`/`<td>` `padding: var(--space-sm); border-bottom: 1px solid var(--color-border); text-align: left;` + `<th>` color `var(--color-on-surface-muted)`.
      - Mobile breakpoint @ `max-width: 640px`: single column; tables collapse to `.c-card` stacks; `padding-inline: var(--space-md)`; horizontal scroll on lifecycle timestamps column.
      - Zero hex literals; zero `.banner`/`.alert`/`.warning`/`.callout` classes; zero `.c-card--feature`; zero `linear-gradient`/`radial-gradient`; zero soft `box-shadow` on cards.
    Step 7: Create `app/(markos)/settings/saas/page.stories.tsx` (CSF3) with 8 named state stories matching UI-SPEC §Storybook story register: `Default` (activated-with-subscriptions fixture), `PreActivation` (draft fixture), `NotSaaSTenant` (gating notice), `WizardInProgress` (modal open at step 4), `PendingApproval` (mutation queue populated), `ActivationFailed` (rare error state), `Tenant0GateClosed` (gate flag closed fixture), `GrowthExtensionPlaceholder` (growth_mode_hint='b2b' set). All register under `meta.title = 'Settings/SaaS'`.
    Step 8: Extend `app/(markos)/layout-shell.tsx` NavList: insert `<li>SaaS</li>` after `Billing` and before `Members`; the `<li>` consumes `aria-hidden={!isSaaSSurfaceEnabled}` and `display: none` via a CSS class toggled on the negation. Server-side `isSaaSSurfaceEnabled` read at the layout level (cached per request).
    Step 9: Create `test/saas-214/domain-6/saas-ui-gating.test.js` with ≥6 individual `test()` blocks: (1) non-SaaS tenant renders only gating notice; (2) gating notice copy matches verbatim; (3) SaaS tenant draft state renders pre-activation; (4) growth placeholder renders for any non-empty growth_mode_hint; (5) no `app/(markos)/growth/` route file exists; (6) `isSaaSSurfaceEnabled` is imported (not re-implemented). Each test cites SS-2/SS-3/SS-4/SS-11/X-8/X-10 in its name.
  </action>
  <verify>
    <automated>npm test -- test/saas-214/domain-6/saas-ui-gating.test.js && npm run build-storybook 2>&1 | tail -5</automated>
  </verify>
  <done>Settings/SaaS page renders the 9-state matrix verbatim, consumes only DESIGN.md tokens (D-08), enforces vanilla `<table>` (D-14), preserves Pricing Engine ownership (X-7 + SS-6), gates non-SaaS tenants (SG-2 + SS-3), renders the growth placeholder ALWAYS when activated (SS-11 + X-10), registers 8 Storybook stories under `Settings/SaaS` (SS-15 + X-4 + X-9). UI-SPEC ACs SS-1..15 + SG-2 + X-1..10 (page slice) closed.</done>
</task>

<task id="214-07-02" tdd="true">
  <name>AP-1..7, X-1..6 (panel slice): SaaSActivationPanel.tsx + .module.css + .stories.tsx — 5 variants, 6-step wizard, focus-trapped .c-modal, kernel-pulse status dot, 7 named state stories</name>
  <files>components/markos/tenant/SaaSActivationPanel.tsx, components/markos/tenant/SaaSActivationPanel.module.css, components/markos/tenant/SaaSActivationPanel.stories.tsx</files>
  <read_first>
    - .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md §Surface B (entire section: prop signature, 3 layout variants, 6-step wizard register, copy register, state matrix, accessibility, motion, mobile)
    - .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md §Acceptance Criteria AP-1..AP-7 + X-1..X-6
    - DESIGN.md §Components .c-card, .c-modal, .c-backdrop, .c-button{--primary,--secondary,--tertiary}, .c-input, .c-field, .c-field__{label,help,error}, .c-notice c-notice--{info,warning,success,error}, .c-status-dot--live
    - DESIGN.md §Motion (modal fade 150ms; reduced-motion freeze)
    - lib/markos/saas/core/contracts.ts (SaaSSuiteActivation + SaaSSuiteActivationDraft types from 214-02)
    - lib/markos/saas/core/surface-gates.ts (getTenant0ReadinessGateStatus stub)
    - app/(markos)/oauth/consent/_components/ConsentCard.tsx (canonical 'use client' presentational subcomponent pattern from 213.2-04)
  </read_first>
  <behavior>
    - Test (AP-1 token-only): `grep -E "#[0-9a-fA-F]{3,8}" components/markos/tenant/SaaSActivationPanel.module.css` returns 0.
    - Test (AP-2 5 variants): TS prop signature `Variant` is exactly the 5-tuple `'pre-activation' | 'wizard-in-progress' | 'activated' | 'activation-failed' | 'tenant-0-gate-closed'`.
    - Test (AP-3 6 steps): wizard renders 6 steps with the verbatim headings (Confirm business type / Launch countries / Payment processors / Accounting systems / Autonomy posture / Review & activate); each has explicit field set; step counter eyebrow `Step {N} of 6` carries `aria-live="polite"`.
    - Test (AP-3 validation): each step's primary CTA is disabled when validation fails; validation error renders via `.c-field__error` with verbatim copy `[err] {field_label} is required.`.
    - Test (AP-4 step 6): when all validations pass AND tenant 0 gate is `'opened'`, step 6 renders `[ok] Ready to activate.` notice and submit is enabled. When gate is `'closed'`, step 6 renders the verbatim warning `[warn] Tenant 0 readiness gate is closed. SaaS Suite activation is paused until the v4.0.0 readiness sign-off completes.` AND submit is disabled.
    - Test (AP-5 activated heading): `activated` variant renders `<h3>[ok] SaaS Suite active <span class="c-status-dot c-status-dot--live"></span></h3>` with kernel-pulse animation that freezes under `@media (prefers-reduced-motion: reduce)`.
    - Test (AP-6 modal a11y): `.c-modal` traps focus (first/last focusable cycle); ESC keypress with empty wizardDraft closes immediately; ESC with non-empty draft renders a confirmation prompt.
    - Test (AP-7 stories): `SaaSActivationPanel.stories.tsx` exports 7 named stories matching UI-SPEC §Storybook story register: `PreActivation`, `WizardStep1`, `WizardStep4`, `WizardStep6Review`, `Activated`, `ActivationFailed`, `Tenant0GateClosed`. All under `meta.title = 'Tenant/SaaSActivationPanel'`.
    - Test (X-1 hex): `grep -E "#[0-9a-fA-F]{3,8}" components/markos/tenant/SaaSActivationPanel.{tsx,module.css,stories.tsx}` returns 0.
    - Test (X-2 dual-signal): every notice/badge/status-dot pairs color with bracketed glyph.
    - Test (X-3 banned lexicon): banned-word grep returns 0; exclamation point in product copy returns 0.
    - Test (X-4 stories ≥3): the file exports ≥3 named stories under `Tenant/SaaSActivationPanel`.
    - Test (X-5 touch target): no module-level `.c-button { min-height: ... }` redeclaration; relies on global `(pointer: coarse)` rule from 213.2.
    - Test (X-6 reduced motion): every animation (modal fade 150ms, step cross-fade 100ms, kernel pulse) has a `@media (prefers-reduced-motion: reduce) { animation: none; transition: none; opacity: 1; }` companion rule.
  </behavior>
  <acceptance_criteria>
    Verify: `npm run build-storybook 2>&1 | tail -5` shows zero errors AND `storybook-static/index.json` contains 7 entries under `Tenant/SaaSActivationPanel`.
    Verify: `grep -E "#[0-9a-fA-F]{3,8}" components/markos/tenant/SaaSActivationPanel.{tsx,module.css,stories.tsx}` returns 0 lines.
    Verify: `grep -c "prefers-reduced-motion" components/markos/tenant/SaaSActivationPanel.module.css` returns >= 1 (X-6).
    Verify: `grep -c "c-status-dot c-status-dot--live" components/markos/tenant/SaaSActivationPanel.tsx` returns >= 1 (AP-5).
    Verify: `grep -wcE "synergy|leverage|empower|unlock|transform|revolutionize|supercharge|holistic|seamless|cutting-edge|innovative|game-changer|next-generation|world-class|best-in-class|reimagine|disrupt" components/markos/tenant/SaaSActivationPanel.{tsx,module.css,stories.tsx}` returns 0 (X-3).
  </acceptance_criteria>
  <action>
    Step 1: Create `components/markos/tenant/SaaSActivationPanel.tsx` as `'use client'`. Implement the prop signature from UI-SPEC §Surface B verbatim (5 variants + WizardStep 1..6 + 4 callbacks + optional errorReason).
    Step 2: Implement the 5-variant render branches: `pre-activation` (hero card with `Activate SaaS Suite` primary CTA), `wizard-in-progress` (`.c-modal` with step indicator + step body), `activated` (hero card with `[ok] SaaS Suite active` + `.c-status-dot--live` + `Edit configuration` secondary CTA), `activation-failed` (hero card with `[err] Activation failed` + `Retry activation` primary CTA), `tenant-0-gate-closed` (pre-activation hero with submit disabled + warning notice).
    Step 3: Implement the 6 wizard steps verbatim from UI-SPEC §Surface B wizard step register. Step 1 reads `business_type_snapshot` from the activation context (read-only display); profile_name input. Step 2-5 are field groups composing `<fieldset><legend>` + `.c-field` + `.c-input`/`.c-button`. Step 6 reviews all 5 prior steps + optional growth_mode_hint select with verbatim notice `[info] Stored as metadata only — Phase 217 enables growth modules.`.
    Step 4: Modal a11y: focus trap (first focusable on open, ESC handler, focus return on close); `aria-live="polite"` on step counter; `<fieldset>` + `<legend>` for checkbox/radio groups; submit explicitly tabbed (no auto-submit on Enter).
    Step 5: Implement copy register verbatim from UI-SPEC §Surface B (29 distinct copy strings). Each `[ok]`/`[warn]`/`[err]`/`[info]` glyph is paired with the corresponding state class.
    Step 6: Create `SaaSActivationPanel.module.css` token-only:
      - `.activationCard { background: var(--color-surface-raised); border: 1px solid var(--color-border); border-radius: var(--radius-md); padding: var(--space-md); }`.
      - `.statusDot` composes `.c-status-dot c-status-dot--live` via `composes:` (CSS Modules) — never redeclares.
      - `.wizardModal` composes `.c-modal` + `.c-backdrop`.
      - `@media (prefers-reduced-motion: reduce) { .wizardModal, .stepTransition, .statusDot { animation: none; transition: none; opacity: 1; } }`.
      - Zero hex literals; zero `.c-card--feature`; zero `.banner`/`.alert`/`.warning`/`.callout`; zero gradients; zero soft shadows.
    Step 7: Create `SaaSActivationPanel.stories.tsx` (CSF3) with 7 named state stories matching UI-SPEC §Storybook story register; meta.title = 'Tenant/SaaSActivationPanel'. Use Storybook controls to demonstrate variant switching.
  </action>
  <verify>
    <automated>npm run build-storybook 2>&1 | tail -5 && grep -c "Tenant/SaaSActivationPanel" storybook-static/index.json</automated>
  </verify>
  <done>SaaSActivationPanel.tsx ships with the 5 variants and 6-step wizard verbatim from UI-SPEC §Surface B; the module.css is token-only with reduced-motion companion rules; 7 Storybook stories register under `Tenant/SaaSActivationPanel`. UI-SPEC ACs AP-1..7 + X-1..6 (panel slice) closed.</done>
</task>

<task id="214-07-03" tdd="true">
  <name>ST-1..8, X-1..6 (table slice): SaaSSubscriptionsTable.tsx + .module.css + .stories.tsx — vanilla &lt;table&gt;, 8 lifecycle state badges, action menu with 7 mutation modals, autonomy-ceiling [block] notice, run chip deep-link, 7 named state stories</name>
  <files>components/markos/tenant/SaaSSubscriptionsTable.tsx, components/markos/tenant/SaaSSubscriptionsTable.module.css, components/markos/tenant/SaaSSubscriptionsTable.stories.tsx, test/ui-a11y/214-saas-a11y.test.js</files>
  <read_first>
    - .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md §Surface C (entire section: prop signature, layout, copy register, mutation-class binding, state matrix, accessibility, motion, mobile)
    - .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md §Acceptance Criteria ST-1..ST-8 + X-1..X-6
    - .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md §Surface A copy register state badges (8 lifecycle state badges with verbatim glyph + color pairing)
    - .planning/phases/206-soc2-type1-foundation/206-UI-SPEC.md §mutation-class-policy.md (default_approval_mode + autonomy_ceiling + evidence_required for billing.charge and data.export)
    - .planning/phases/207-agentrun-v2-orchestration-substrate/207-UI-SPEC.md §AgentRunEventType + AgentFailureClass (7 failure literals)
    - lib/markos/saas/core/contracts.ts (SUBSCRIPTION_STATES; SaaSSubscriptionRow + SaaSSubscriptionMutationRequest types)
    - lib/markos/saas/core/approval-routing.ts (MUTATION_FAMILIES + MUTATION_CLASS_BINDING + HANDOFF_KIND_BILLING_CHARGE_APPROVAL exports from 214-05)
    - test/ui-a11y/213-4-admin-ops-a11y.test.js (canonical a11y test pattern)
  </read_first>
  <behavior>
    - Test (ST-1 token-only): `grep -E "#[0-9a-fA-F]{3,8}" components/markos/tenant/SaaSSubscriptionsTable.module.css` returns 0.
    - Test (ST-2 8 states): every one of 8 lifecycle states (`draft | trialing | active | past_due | paused | canceled | upgrade_pending | downgrade_pending`) has a verbatim badge render: `[info] Draft` (`.c-badge--info`); `[ok] Trialing` / `[ok] Active` (`.c-badge--success`); `[warn] Past due` / `[warn] Paused` / `[warn] Upgrade pending` / `[warn] Downgrade pending` (`.c-badge--warning`); `[err] Canceled` (`.c-badge--error` + row dimmed via `--color-on-surface-subtle`). The `active` row also carries `.c-status-dot--live`.
    - Test (ST-3 action menu 7 items): action menu exposes exactly 7 items: `Pause subscription`, `Cancel subscription`, `Upgrade subscription`, `Downgrade subscription`, `Reactivate subscription`, `Export audit log`, plus implicit `Subscription create` (handled at parent level — table action menu has 6 visible + reactivate gating). Each menu item label corresponds to a `mutation_family` literal; each carries a `.c-chip-protocol` chip in the modal showing `Root: billing.charge` (6 lifecycle) or `Root: data.export` (audit-log export).
    - Test (ST-4 modal copy): every modal heading and body comes from UI-SPEC §Surface C copy register verbatim (9 modal copy entries — 6 mutation-confirm + 1 audit-log-export + 2 default approval/autonomy notices). `Cancel subscription` modal CTA uses `.c-button--destructive`; non-destructive mutations use `.c-button--primary`.
    - Test (ST-5 pending approval): when `pendingMutationsBySubscriptionId[subId].length > 0`, row shows `.c-badge--warning` chip `[warn] {N} pending approval(s)` AND `<a class="c-button c-button--tertiary" href={`/operations/approvals?handoff_kind=billing_charge_approval&mutation_request_id=${id}`}>Open approval →</a>`.
    - Test (ST-6 autonomy ceiling): when `billing.charge` ceiling reached, row renders `<.c-notice c-notice--error>[block] Autonomy ceiling reached for billing.charge. Operator approval is required for every action of this class until ceiling resets.</.c-notice>`; non-export menu items disabled (`aria-disabled="true"` + `is-disabled` class); `Export audit log` (data.export) remains enabled.
    - Test (ST-7 run chip): each row with `agent_run_id` set renders `<a class="c-chip-protocol" href={`/operations/tasks?run_id=${run_id}`}>Run: {run_id}</a>`.
    - Test (ST-8 stories): `SaaSSubscriptionsTable.stories.tsx` exports 7 named stories matching UI-SPEC §Storybook story register: `Default`, `Empty`, `AllActive`, `MixedStates`, `PendingApproval`, `AutonomyCeilingReached`, `MutationModalOpen`. All under `meta.title = 'Tenant/SaaSSubscriptionsTable'`.
    - Test (a11y): action menu uses `<button aria-haspopup="menu" aria-expanded="…">` + `<ul role="menu">` + `<li role="menuitem">`; pending-approval count chip carries `aria-live="polite"`.
    - Test (X-1 + X-2 + X-3 + X-4 + X-5 + X-6 carry-forward): same checks as panel slice apply to table files.
    - Test (a11y suite mirror): `test/ui-a11y/214-saas-a11y.test.js` exists with ≥30 individual `test()` blocks; ≥7 AC# mentions covering SS/AP/ST/SG/X groups; mirrors `test/ui-a11y/213-4-admin-ops-a11y.test.js` 5-line opener pattern + `read()` helper + AC# section comments.
  </behavior>
  <acceptance_criteria>
    Verify: `npm run build-storybook 2>&1 | tail -5` shows zero errors AND `storybook-static/index.json` contains 7 entries under `Tenant/SaaSSubscriptionsTable`.
    Verify: `npm test -- test/ui-a11y/214-saas-a11y.test.js` returns 0 failures with ≥30 tests passing.
    Verify: `grep -c "draft\\|trialing\\|active\\|past_due\\|paused\\|canceled\\|upgrade_pending\\|downgrade_pending" components/markos/tenant/SaaSSubscriptionsTable.tsx` returns >= 8 distinct literal hits (one per state).
    Verify: `grep -c "\\[block\\] Autonomy ceiling reached for billing.charge" components/markos/tenant/SaaSSubscriptionsTable.tsx` returns >= 1 (verbatim copy ST-6).
    Verify: `grep -c "billing_charge_approval" components/markos/tenant/SaaSSubscriptionsTable.tsx` returns >= 1.
    Verify: `grep -c "data.export" components/markos/tenant/SaaSSubscriptionsTable.tsx` returns >= 1 (audit-log export root class).
    Verify: `grep -E "#[0-9a-fA-F]{3,8}" components/markos/tenant/SaaSSubscriptionsTable.{tsx,module.css,stories.tsx}` returns 0.
    Verify: number of SaaS-suite story entries in `storybook-static/index.json` totals ≥22 (8 page + 7 panel + 7 table = 22; X-9 says ≥21 minimum).
  </acceptance_criteria>
  <action>
    Step 1: Create `components/markos/tenant/SaaSSubscriptionsTable.tsx` as `'use client'`. Implement the prop signature from UI-SPEC §Surface C verbatim (variant default-only; subscriptions array; pendingMutationsBySubscriptionId record; onRequestMutation callback typed against the 7 MutationKind literals).
    Step 2: Render the vanilla `<table>` with 6 columns (`Customer | Plan | State | Period | Run | Actions`). Each `<th>` carries `.t-label-caps`; each `<td>` consumes only token-driven CSS. Empty state delegated to parent (component renders header-only table).
    Step 3: Implement the 8 state badges per UI-SPEC §Surface C copy register verbatim. The `active` row also renders adjacent `.c-status-dot c-status-dot--live`. The `canceled` row dims via `color: var(--color-on-surface-subtle)`.
    Step 4: Implement the action menu as `<button aria-haspopup="menu" aria-expanded={isOpen} class="c-button c-button--secondary">Manage ▾</button>` + `<ul role="menu">`. Menu items disable conditionally: `Reactivate` only when `current_state == 'paused'`; non-export items disabled when autonomy ceiling reached.
    Step 5: Implement 7 mutation-confirm modals (one per MutationKind). Each modal:
      - Heading + body from UI-SPEC §Surface C copy register verbatim.
      - `.c-chip-protocol` chip showing `Root: {root_class}` (derived via `MUTATION_CLASS_BINDING` from 214-05).
      - `.c-badge--info` chip showing `Approval: {default_approval_mode}` (e.g. `single_approval` or `dual_approval` per 206 policy).
      - Primary CTA `Request mutation` (`.c-button--primary`) for non-destructive; `Request cancellation` (`.c-button--destructive`) for cancel.
      - Audit-log-export CTA `Request export` (`.c-button--primary`).
      - Cancel CTA `Keep current state` (`.c-button--secondary`).
    Step 6: Implement pending-approval row chip + deep-link `/operations/approvals?handoff_kind=billing_charge_approval&mutation_request_id=${id}` (verbatim ST-5).
    Step 7: Implement autonomy-ceiling notice (verbatim ST-6 copy `[block] Autonomy ceiling reached for billing.charge. Operator approval is required for every action of this class until ceiling resets.`).
    Step 8: Implement run chip `<a class="c-chip-protocol" href={`/operations/tasks?run_id=${agent_run_id}`}>Run: {agent_run_id}</a>` (verbatim ST-7).
    Step 9: Create `SaaSSubscriptionsTable.module.css` token-only:
      - Mobile breakpoint @ `max-width: 640px`: `<table>` collapses to `.c-card` stacks per row; action menu becomes bottom-sheet pattern.
      - `@media (prefers-reduced-motion: reduce)` companion rules for menu open (100ms), modal fade (150ms), live status dot kernel pulse.
      - Zero hex literals; zero `.c-card--feature`; zero `.banner`/`.alert`/`.warning`/`.callout`; zero gradients; zero soft shadows.
    Step 10: Create `SaaSSubscriptionsTable.stories.tsx` (CSF3) with 7 named state stories matching UI-SPEC §Storybook story register; meta.title = 'Tenant/SaaSSubscriptionsTable'.
    Step 11: Create `test/ui-a11y/214-saas-a11y.test.js` mirroring `test/ui-a11y/213-4-admin-ops-a11y.test.js` (≥30 individual `test()` blocks; ≥7 AC# mentions; 5-line `node:test` opener; `read()` helper; AC# section comments). Coverage: 4 file-existence (X-4), 8 primitive composition (state badges + action menu + modal + chips), 8 bracketed-glyph state pairings, 1 multi-file pricing rule, 1 multi-file banned-lexicon, 8 token-consumption checks, 1 stories-meta-title check. Cite UI-SPEC ACs SS/AP/ST/SG/X in test names.
  </action>
  <verify>
    <automated>npm run build-storybook 2>&1 | tail -5 && npm test -- test/ui-a11y/214-saas-a11y.test.js</automated>
  </verify>
  <done>SaaSSubscriptionsTable.tsx ships with the vanilla `<table>` recipe, all 8 lifecycle state badges with verbatim glyph+color pairing, 7 mutation modals with verbatim copy, autonomy-ceiling [block] notice, run chip deep-link, and 7 Storybook stories under `Tenant/SaaSSubscriptionsTable`. The 214-saas-a11y.test.js mirrors the canonical 213.4 a11y pattern with ≥30 tests. UI-SPEC ACs ST-1..8 + X-1..6 (table slice) + a11y suite closed.</done>
</task>

<task id="214-07-04" type="checkpoint:human-verify" gate="blocking">
  <name>Manual verification — mobile breakpoint, Chromatic baselines, axe a11y, growth metadata-only sanity</name>
  <what-built>
    - `app/(markos)/settings/saas/page.tsx` + page.module.css + page.stories.tsx (8 stories)
    - `components/markos/tenant/SaaSActivationPanel.{tsx,module.css,stories.tsx}` (7 stories)
    - `components/markos/tenant/SaaSSubscriptionsTable.{tsx,module.css,stories.tsx}` (7 stories)
    - `app/(markos)/layout-shell.tsx` SaaS nav row insertion
    - `test/saas-214/domain-6/saas-ui-gating.test.js` + `test/ui-a11y/214-saas-a11y.test.js`
  </what-built>
  <how-to-verify>
    1. **Mobile breakpoint (iPhone 14 Pro / 393×852)**: Open `npm run storybook`. Visit `Settings/SaaS` → `Default` story. Use Storybook's viewport addon to switch to "iPhone 14 Pro". Confirm: page padding switches to `--space-md`; tables collapse to vertical card stacks; activation hero remains a single `.c-card`; action menu in subscriptions table opens as a bottom-sheet (full-width buttons stacked, not popover); 44px touch target on every `.c-button` (visual measure or DevTools).
    2. **Chromatic baselines (22 stories)**: Run `npx chromatic --project-token=$CHROMATIC_PROJECT_TOKEN` (skip if token absent — manual visual review acceptable). Confirm 22 SaaS-suite snapshots accept as new baselines: 8 page + 7 panel + 7 table = 22 (X-9 minimum 21 met).
    3. **Axe a11y (page + components)**: With Storybook running, open each of the 22 stories and click "Accessibility" panel. Confirm zero violations on: color contrast (WCAG AA 4.5:1), focus order, ARIA-role validity, form-label association. Pay special attention to: `Tenant0GateClosed` story (verify warning notice is announced), `WizardStep4` story (verify `<fieldset>`/`<legend>` for checkbox group is announced), `MutationModalOpen` story (verify modal focus trap + ESC-to-close).
    4. **Growth metadata-only sanity**: Run `grep -rE "growth/|growth-mode" app/ api/ lib/markos/operator/ lib/markos/mcp/ 2>/dev/null | grep -v "node_modules\\|\\.module\\.css\\|growth_extension\\|growth_mode_hint\\|future_phase_217\\|requires_phase_218\\|placeholder"`. Expected: 0 results (any hit means an unintended growth runtime surface leaked into the diff). Then visit `Settings/SaaS` → `GrowthExtensionPlaceholder` story. Confirm: `<PlaceholderBanner variant="future_phase_217">` renders with literal copy; `.c-chip` shows `[info] Growth extension hint: b2b` (or whichever fixture hint is set); clicking the placeholder does nothing (no navigation, no modal); no nav entry exists for "Growth".
    5. **DESIGN.md token compliance**: Run `npx @google/design.md lint DESIGN.md` and confirm PASS. Run `grep -rE "#[0-9a-fA-F]{3,8}" app/(markos)/settings/saas/ components/markos/tenant/SaaS*.{tsx,module.css}` and confirm 0 hits.
  </how-to-verify>
  <action>Pause execution; present the 5 manual verification steps to the human operator; wait for their explicit "approved" reply (or itemized failure list). This task does NOT modify the working tree — it is a synchronization checkpoint after the 3 autonomous UI tasks (214-07-01..03) finish. If the human reports failures, escalate by routing the failures back into a follow-up task list and re-running the affected 214-07-* task; do not auto-resume.</action>
  <verify>
    <automated>node -e "process.exit(0)"</automated>
  </verify>
  <done>Human operator typed "approved" after confirming all 5 manual checks pass: (1) mobile breakpoint at iPhone 14 Pro renders correctly; (2) 22 Chromatic baselines accepted (or manual visual review passes); (3) axe a11y reports zero violations on all 22 stories; (4) growth metadata-only sanity grep returns 0 hits AND placeholder renders correctly; (5) DESIGN.md lint passes AND zero hex literals in UI files.</done>
  <resume-signal>Type "approved" once all 5 manual checks pass; describe any failures with file/line refs and DESIGN.md token mismatches.</resume-signal>
</task>
</tasks>

<verification>
- npm test -- test/saas-214/domain-6/saas-ui-gating.test.js
- npm test -- test/ui-a11y/214-saas-a11y.test.js
- npm run build-storybook
- npx @google/design.md lint DESIGN.md
- grep -E "#[0-9a-fA-F]{3,8}" app/(markos)/settings/saas/ components/markos/tenant/SaaS*.{tsx,module.css} | wc -l  # must be 0
- grep -wcE "synergy|leverage|empower|unlock|transform|revolutionize|supercharge|holistic|seamless|cutting-edge|innovative|game-changer|next-generation|world-class|best-in-class|reimagine|disrupt" app/(markos)/settings/saas/ components/markos/tenant/SaaS*.{tsx,module.css,stories.tsx}  # must be 0
</verification>

<success_criteria>
- `app/(markos)/settings/saas/page.tsx` renders the 9-state matrix verbatim from UI-SPEC §Surface A (SS-1..15).
- `<SaaSActivationPanel />` extracted with 5 variants + 6-step wizard verbatim from UI-SPEC §Surface B (AP-1..7).
- `<SaaSSubscriptionsTable />` extracted with 8 lifecycle state badges + 7 mutation modals + autonomy-ceiling notice + run chip deep-link verbatim from UI-SPEC §Surface C (ST-1..8).
- 22 Storybook stories registered (8 page + 7 panel + 7 table) under explicit `meta.title` paths (X-4 + X-9).
- Sidebar nav row hidden when `isSaaSSurfaceEnabled` returns false (SG-2).
- DESIGN.md v1.1.0 token-only CSS; zero hex literals (D-08 + X-1).
- Vanilla `<table>` for plan catalog + subscriptions + mutations + lifecycle (D-14).
- Pricing renders via `<Money fromPricingRecommendation={pr_id} />` or verbatim `{{MARKOS_PRICING_ENGINE_PENDING}}` sentinel; zero `$\d` literals; zero tier-name strings (X-7).
- Growth-extension placeholder ALWAYS rendered when activated; growth modules NEVER runnable in 214 (X-10 UI half).
- 4 doctrine-violation strings absent from UI files (architecture-lock carry-forward).
- All animated elements freeze under `prefers-reduced-motion: reduce` (X-6).
- 44px touch target inherits from global `(pointer: coarse)` rule; modules do not redeclare (X-5).
- `test/ui-a11y/214-saas-a11y.test.js` mirrors 213-4 pattern with ≥30 tests + ≥7 AC# mentions.
- Manual verification (mobile breakpoint at iPhone 14 Pro, Chromatic baselines for 22 stories, axe a11y zero-violation on 22 stories, growth metadata-only sanity grep) passes.
</success_criteria>
</content>
