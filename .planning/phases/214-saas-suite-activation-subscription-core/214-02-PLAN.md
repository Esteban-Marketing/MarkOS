---
phase: 214
plan: "02"
type: execute
wave: 2
depends_on:
  - "214-01"
f_ids:
  - F-288
  - F-289
  - F-290
  - F-291
migration_slots:
  - 125
integrates_with:
  - "PRC-09 from P205 (P214 stores `pricing_recommendation_id` or the literal sentinel `{{MARKOS_PRICING_ENGINE_PENDING}}`; Pricing Engine still owns approval and copy policy)"
translation_gate_for: []
files_modified:
  - supabase/migrations/125_saas_subscription_core.sql
  - lib/markos/saas/core/contracts.ts
  - lib/markos/saas/core/contracts.cjs
  - lib/markos/saas/core/profile.ts
  - lib/markos/saas/core/plan.ts
  - lib/markos/saas/core/subscription.ts
  - lib/markos/saas/core/events.ts
  - lib/markos/saas/core/index.cjs
  - contracts/F-288-saas-profile-v1.yaml
  - contracts/F-289-saas-plan-v1.yaml
  - contracts/F-290-saas-subscription-v1.yaml
  - contracts/F-291-saas-subscription-event-v1.yaml
  - contracts/flow-registry.json
  - test/saas-214/domain-2/profile-schema.test.js
  - test/saas-214/domain-2/plan-pricing-sentinel.test.js
  - test/saas-214/domain-2/subscription-schema.test.js
  - test/saas-214/domain-2/subscription-events-append-only.test.js
  - test/saas-214/rls/subscription-cross-tenant-denial.test.js
  - .planning/phases/214-saas-suite-activation-subscription-core/214-VALIDATION.md
autonomous: true
requirements:
  - SAAS-02
  - SAAS-03
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - QA-05
  - QA-06
ui_spec_fold:
  source: .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md
  fold_date: 2026-04-29
  scope: backend_doctrine
  acs_folded:
    - BD-4   # saas_profiles + saas_plans + saas_plan_versions + saas_subscriptions + saas_subscription_events tables with tenant-scoped RLS
    - BD-5   # SAAS_PLAN_REQUIRES_PRICING_RECOMMENDATION_OR_SENTINEL trigger blocks publication without pricing_recommendation_id OR the literal sentinel
    - BD-6   # saas_subscription_events append-only and unique by (tenant_id, subscription_id, idempotency_key) — co-owned with 214-03
  pricing_ownership_rule: |
    P214 stores pricing_recommendation_id FK only (or the verbatim 35-char sentinel `{{MARKOS_PRICING_ENGINE_PENDING}}`).
    P214 NEVER stores local price values, tier name strings, or hard-coded discount envelopes.
    Pricing Engine (P205) is the single source of truth for approved pricing.
  lifecycle_states_verbatim:
    - draft
    - trialing
    - active
    - past_due
    - paused
    - canceled
    - upgrade_pending
    - downgrade_pending
must_haves:
  truths:
    - "Migration 125 creates `saas_profiles`, `saas_plans`, `saas_plan_versions`, `saas_subscriptions`, and append-only `saas_subscription_events` (BD-4 verbatim)."
    - "`saas_plan_versions` stores either `pricing_recommendation_id` (FK into P205 `pricing_recommendations`) or the exact 35-character literal sentinel `{{MARKOS_PRICING_ENGINE_PENDING}}` (UI-SPEC §Pricing placeholder rule). No local price column; no hard-coded tier name string anywhere in this migration or its libs."
    - "Trigger `SAAS_PLAN_REQUIRES_PRICING_RECOMMENDATION_OR_SENTINEL` blocks `status` transitioning to `published` without approved pricing context or the exact placeholder (BD-5)."
    - "`saas_subscriptions.current_state` accepts only the 8 lifecycle literals verbatim from UI-SPEC §Surface A: `draft | trialing | active | past_due | paused | canceled | upgrade_pending | downgrade_pending`."
    - "`saas_subscriptions` stores the 14 fields enumerated in UI-SPEC §Surface A data dependencies: `subscription_id`, `tenant_id`, `customer_bridge_id`, `current_plan_version_id`, `current_state`, `trial_started_at`, `trial_ends_at`, `current_period_start`, `current_period_end`, `last_event_at`, `agent_run_id`, `created_at`, `updated_at`, plus the FK constraints."
    - "`saas_subscription_events.event_type` accepts only the 16 literals verbatim from UI-SPEC §Surface A data dependencies: `activation_started | trial_started | trial_converted | payment_failed | payment_recovered | pause_requested | pause_applied | reactivation_requested | reactivated | upgrade_requested | upgrade_applied | downgrade_requested | downgrade_applied | cancellation_requested | canceled | rollback_applied`."
    - "`saas_subscription_events` is append-only (no UPDATE policy; INSERT only) and uniquely keyed by `(tenant_id, subscription_id, idempotency_key)` for idempotent event insert patterns (BD-6)."
    - "`saas_plans` row.name is read by UI verbatim (UI-SPEC §Surface A copy register: `plan-row title rendered from saas_plans.name; never hard-coded`). The schema enforces `name TEXT NOT NULL CHECK (length(name) BETWEEN 1 AND 80)`."
  artifacts:
    - path: supabase/migrations/125_saas_subscription_core.sql
      provides: the core SaaS profile, plan, version, subscription, and event schema with RLS, the pricing-sentinel trigger, the 8-state CHECK, and the 16-event-type CHECK
    - path: lib/markos/saas/core/contracts.ts
      provides: enum constants and zod schemas for profile, plan, subscription, and event contracts; exports `SUBSCRIPTION_STATES` (8 literals) and `SUBSCRIPTION_EVENT_TYPES` (16 literals) read by 214-03 and consumed by 214-06 UI
      exports: [SaaSProfileSchema, SaaSPlanSchema, SaaSPlanVersionSchema, SaaSSubscriptionSchema, SaaSSubscriptionEventSchema, SUBSCRIPTION_STATES, SUBSCRIPTION_EVENT_TYPES, PRICING_PLACEHOLDER_SENTINEL]
    - path: contracts/F-288-saas-profile-v1.yaml
      provides: SaaS profile contract
    - path: contracts/F-289-saas-plan-v1.yaml
      provides: SaaS plan + plan-version contract; pricing field accepts FK xor sentinel literal
    - path: contracts/F-290-saas-subscription-v1.yaml
      provides: SaaS subscription contract with 8-state enum
    - path: contracts/F-291-saas-subscription-event-v1.yaml
      provides: SaaS subscription event contract with 16-event-type enum
  key_links:
    - from: lib/markos/saas/core/plan.ts
      to: supabase/migrations/125_saas_subscription_core.sql
      via: app-layer validation and DB trigger enforce the same pricing-sentinel rule
      pattern: "MARKOS_PRICING_ENGINE_PENDING"
    - from: contracts/F-289-saas-plan-v1.yaml
      to: lib/markos/saas/core/contracts.ts
      via: zod parity between SaaS plan YAML and runtime schema
      pattern: "SaaSPlanSchema"
    - from: lib/markos/saas/core/contracts.ts
      to: lib/markos/saas/core/lifecycle.ts
      via: 214-03 reads SUBSCRIPTION_STATES and SUBSCRIPTION_EVENT_TYPES exported here; never re-derives the literal lists
      pattern: "SUBSCRIPTION_STATES"
---

<objective>
Create the durable core objects for SaaS activation, profile, plan catalog, subscriptions, and lifecycle events while preserving Pricing Engine ownership of canonical pricing decisions.

Backend doctrine ACs folded from 214-UI-SPEC.md: BD-4 (5 core tables with tenant-scoped RLS), BD-5 (pricing-sentinel trigger), BD-6 (append-only events with idempotency uniqueness, co-owned with 214-03). The 8 lifecycle states and 16 event types from UI-SPEC §Surface A are encoded as CHECK constraints AND exported as TS literal-union types so 214-03 lifecycle code and 214-06 UI consume identical literals.
</objective>

<context>
@.planning/phases/214-saas-suite-activation-subscription-core/214-RESEARCH.md
@.planning/phases/214-saas-suite-activation-subscription-core/214-REVIEWS.md
@.planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md
@.planning/phases/214-saas-suite-activation-subscription-core/214-VALIDATION.md
</context>

<tasks>
<task id="214-02-01" tdd="true">
  <name>BD-4, BD-5, BD-6: Core SaaS profile, plan, subscription, and event schema</name>
  <files>supabase/migrations/125_saas_subscription_core.sql, lib/markos/saas/core/contracts.ts, lib/markos/saas/core/contracts.cjs, lib/markos/saas/core/profile.ts, lib/markos/saas/core/plan.ts, lib/markos/saas/core/subscription.ts, lib/markos/saas/core/events.ts, lib/markos/saas/core/index.cjs, contracts/F-288-saas-profile-v1.yaml, contracts/F-289-saas-plan-v1.yaml, contracts/F-290-saas-subscription-v1.yaml, contracts/F-291-saas-subscription-event-v1.yaml, contracts/flow-registry.json, test/saas-214/domain-2/profile-schema.test.js, test/saas-214/domain-2/plan-pricing-sentinel.test.js, test/saas-214/domain-2/subscription-schema.test.js, test/saas-214/domain-2/subscription-events-append-only.test.js, test/saas-214/rls/subscription-cross-tenant-denial.test.js</files>
  <read_first>
    - .planning/phases/214-saas-suite-activation-subscription-core/214-RESEARCH.md section "Domain 2 - Core profile, plan, subscription, and event schema"
    - .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md §Surface A data dependencies (8 subscription states; 16 event types; 14 saas_subscriptions fields; saas_plans + saas_plan_versions field list)
    - .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md §Pricing placeholder rule (verbatim 35-character sentinel; FK xor sentinel xor reject)
    - .planning/phases/205-pricing-engine-foundation-billing-readiness/205-UI-SPEC.md §PricingRecommendation contract lock (recommendation_status, price_micro_usd, effective_from/to)
    - lib/markos/billing/contracts.ts
    - api/tenant/status.js
  </read_first>
  <behavior>
    - Test (BD-4 schema): the 5 tables exist with tenant_id columns; each has a tenant-scoped RLS policy; service-role insert succeeds; cross-tenant select returns 0 rows; cross-tenant insert raises 42501.
    - Test (BD-5 trigger PASS A): inserting into `saas_plan_versions` with `status = 'published'` and `pricing_recommendation_id = '<valid uuid>'` succeeds.
    - Test (BD-5 trigger PASS B): inserting into `saas_plan_versions` with `status = 'published'` and `pricing_recommendation_id = NULL` and `pricing_pending_sentinel = '{{MARKOS_PRICING_ENGINE_PENDING}}'` succeeds.
    - Test (BD-5 trigger FAIL): inserting into `saas_plan_versions` with `status = 'published'`, `pricing_recommendation_id = NULL`, and `pricing_pending_sentinel != '{{MARKOS_PRICING_ENGINE_PENDING}}'` raises `SAAS_PLAN_REQUIRES_PRICING_RECOMMENDATION_OR_SENTINEL`.
    - Test (BD-5 sentinel literal): the sentinel column accepts ONLY the exact 35-character literal `{{MARKOS_PRICING_ENGINE_PENDING}}` (with the double-brace markers); any variation including localization, lowercasing, or whitespace fails the CHECK.
    - Test (BD-6 append-only): UPDATE on `saas_subscription_events` raises a "no policy" error; only INSERT is allowed.
    - Test (BD-6 idempotency): inserting two events with identical `(tenant_id, subscription_id, idempotency_key)` raises a unique-constraint violation.
    - Test (8-state CHECK): inserting `saas_subscriptions.current_state = 'foo'` raises a CHECK violation; each of the 8 valid literals succeeds.
    - Test (16-event-type CHECK): inserting `saas_subscription_events.event_type = 'foo'` raises a CHECK violation; each of the 16 valid literals succeeds.
    - Test (TS export parity): `SUBSCRIPTION_STATES` from contracts.ts is a tuple of exactly 8 strings matching the SQL CHECK; `SUBSCRIPTION_EVENT_TYPES` is a tuple of exactly 16 strings matching the SQL CHECK.
  </behavior>
  <acceptance_criteria>
    Verify: `npm test -- test/saas-214/domain-2/ test/saas-214/rls/subscription-cross-tenant-denial.test.js` returns 0 failures.
    Verify: `grep -c "MARKOS_PRICING_ENGINE_PENDING" supabase/migrations/125_saas_subscription_core.sql` returns >= 2 (sentinel literal in CHECK + trigger body).
    Verify: `grep -c "MARKOS_PRICING_ENGINE_PENDING" lib/markos/saas/core/contracts.ts` returns >= 1 (PRICING_PLACEHOLDER_SENTINEL export).
    Verify: `grep -E "draft|trialing|active|past_due|paused|canceled|upgrade_pending|downgrade_pending" supabase/migrations/125_saas_subscription_core.sql` returns >= 1 line containing all 8 literals in a CHECK constraint.
    Verify: `grep -E "activation_started|trial_started|trial_converted|payment_failed|payment_recovered|pause_requested|pause_applied|reactivation_requested|reactivated|upgrade_requested|upgrade_applied|downgrade_requested|downgrade_applied|cancellation_requested|canceled|rollback_applied" supabase/migrations/125_saas_subscription_core.sql` returns >= 1 line containing all 16 event-type literals in a CHECK constraint.
    Verify: `grep -cE "(price_micro_usd|tier_name|amount_usd)\\s+(numeric|integer|text)" supabase/migrations/125_saas_subscription_core.sql` returns 0 (no local pricing columns of any kind).
    Verify: `grep -E "\"Growth Monthly\"|\"Starter\"|\"Professional\"" lib/markos/saas/core/` returns 0 hits.
  </acceptance_criteria>
  <action>
    Step 1: Create `saas_profiles` (tenant-scoped 1:1 with markos_orgs), `saas_plans` (FK → tenant; `name` text NOT NULL with length CHECK 1..80; `description` text; `status` text CHECK in `'draft' | 'published' | 'archived'` per UI-SPEC §Surface A copy register), `saas_plan_versions` (FK → saas_plans; `plan_version_id` uuid pk; `pricing_recommendation_id` uuid NULL FK → P205 `pricing_recommendations`; `pricing_pending_sentinel` text NULL CHECK `pricing_pending_sentinel IS NULL OR pricing_pending_sentinel = '{{MARKOS_PRICING_ENGINE_PENDING}}'`; `value_metric` text; `discount_envelope` jsonb), `saas_subscriptions` (14 fields verbatim from UI-SPEC; CHECK on `current_state` IN the 8-literal list), `saas_subscription_events` (FK → saas_subscriptions; `event_type` text CHECK IN the 16-literal list; `idempotency_key` text NOT NULL; `payload` jsonb; `occurred_at` timestamptz default now(); UNIQUE `(tenant_id, subscription_id, idempotency_key)`).
    Step 2: Apply tenant-scoped RLS on all 5 tables: `ENABLE ROW LEVEL SECURITY`; one SELECT/INSERT policy per table with `USING (tenant_id = (auth.jwt()->>'tenant_id')::uuid)`; service-role bypass via `WITH CHECK (true)` on a service-role-only policy. `saas_subscription_events` gets ONLY INSERT and SELECT policies — NO UPDATE policy (append-only enforcement).
    Step 3: Create trigger `SAAS_PLAN_REQUIRES_PRICING_RECOMMENDATION_OR_SENTINEL` on `saas_plan_versions` BEFORE INSERT OR UPDATE. The trigger raises when `status = 'published'` AND `pricing_recommendation_id IS NULL` AND `pricing_pending_sentinel != '{{MARKOS_PRICING_ENGINE_PENDING}}'`. Use `errcode 'SAAS05'` with hint message `'Plan version cannot publish without an approved pricing_recommendation_id OR the verbatim sentinel `{{MARKOS_PRICING_ENGINE_PENDING}}`.`
    Step 4: Implement `lib/markos/saas/core/contracts.ts` exporting:
      - `PRICING_PLACEHOLDER_SENTINEL = '{{MARKOS_PRICING_ENGINE_PENDING}}' as const` (35 characters including double-brace markers).
      - `SUBSCRIPTION_STATES = ['draft','trialing','active','past_due','paused','canceled','upgrade_pending','downgrade_pending'] as const`.
      - `SUBSCRIPTION_EVENT_TYPES = ['activation_started','trial_started','trial_converted','payment_failed','payment_recovered','pause_requested','pause_applied','reactivation_requested','reactivated','upgrade_requested','upgrade_applied','downgrade_requested','downgrade_applied','cancellation_requested','canceled','rollback_applied'] as const`.
      - `SaaSProfileSchema`, `SaaSPlanSchema`, `SaaSPlanVersionSchema` (with `pricing_recommendation_id` xor `pricing_pending_sentinel` refinement matching the trigger), `SaaSSubscriptionSchema`, `SaaSSubscriptionEventSchema`.
    Step 5: Implement `profile.ts`, `plan.ts`, `subscription.ts`, `events.ts` with server-side CRUD helpers. The `plan.ts` `publishPlanVersion` helper must validate the FK-xor-sentinel rule app-side BEFORE submitting to the DB so the error path returns a typed result rather than a raw 23514 CHECK error.
    Step 6: Mirror via `contracts.cjs` and `index.cjs` for legacy callers.
    Step 7: Create `contracts/F-288..F-291` YAML files. Each contract carries the verbatim CHECK literal lists. Update `contracts/flow-registry.json`.
    Step 8: Land the 4 domain-2 test files plus the RLS test, with each `test()` block citing its UI-SPEC AC ID (BD-4, BD-5, BD-6) in the test name.
  </action>
  <verify>
    <automated>npm test -- test/saas-214/domain-2/ test/saas-214/rls/subscription-cross-tenant-denial.test.js</automated>
  </verify>
  <done>SaaS profile, plan, subscription, and event substrate exists with RLS (BD-4), the exact Pricing Engine placeholder rule is enforced at both app and DB layers (BD-5), events are append-only and idempotency-aware (BD-6), and the 8-lifecycle-state + 16-event-type literal lists are exported from contracts.ts for 214-03 and 214-06 to consume verbatim.</done>
</task>
</tasks>

<success_criteria>
- Core SaaS tables exist with tenant-scoped RLS (BD-4).
- `SAAS_PLAN_REQUIRES_PRICING_RECOMMENDATION_OR_SENTINEL` is enforced at the DB layer with the verbatim 35-character sentinel literal (BD-5).
- Events are append-only (no UPDATE policy) and idempotency-keyed (BD-6).
- The 8 lifecycle states and 16 event types are encoded as SQL CHECK constraints AND exported as TS literal-union tuples for cross-plan consumption.
- F-288..F-291 are registered without collision.
- Zero local pricing columns; zero hard-coded tier-name strings anywhere in the migration or libs.
</success_criteria>
</content>
