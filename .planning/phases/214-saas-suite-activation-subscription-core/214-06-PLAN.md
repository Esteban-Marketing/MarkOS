---
phase: 214
plan: "06"
type: execute
wave: 5
depends_on:
  - "214-01"
  - "214-02"
  - "214-03"
  - "214-04"
  - "214-05"
f_ids:
  - F-296
  - F-297
  - F-299
  - F-300
migration_slots:
  - 129
integrates_with:
  - "PRC-09 from P205 (surface copy and pricing fallback still use approved recommendations or `{{MARKOS_PRICING_ENGINE_PENDING}}`)"
translation_gate_for:
  - "P218 (SG-01 growth-mode activation; P214 stores extension metadata only)"
files_modified:
  - supabase/migrations/129_saas_surface_gating.sql
  - lib/markos/saas/core/surface-gates.ts
  - lib/markos/saas/core/growth-extension.ts
  - lib/markos/saas/core/index.cjs
  - lib/markos/mcp/tools/saas-core.cjs
  - lib/markos/mcp/tools/index.cjs
  - lib/markos/operator/shell.ts
  - api/v1/saas/activation.js
  - api/v1/saas/plans.js
  - api/v1/saas/subscriptions.js
  - api/v1/saas/mutations.js
  - contracts/F-296-saas-core-api-v1.yaml
  - contracts/F-297-saas-growth-extension-v1.yaml
  - contracts/F-299-saas-activation-wizard-v1.yaml
  - contracts/F-300-saas-surface-gating-v1.yaml
  - contracts/flow-registry.json
  - test/saas-214/domain-6/saas-api-gating.test.js
  - test/saas-214/domain-6/saas-mcp-gating.test.js
  - test/saas-214/domain-6/growth-extension-non-activation.test.js
  - .planning/phases/214-saas-suite-activation-subscription-core/214-VALIDATION.md
autonomous: true
requirements:
  - SAAS-01
  - SAAS-02
  - SAAS-03
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - QA-05
  - QA-06
ui_spec_fold:
  source: .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md
  fold_date: 2026-04-29
  scope: backend_gating_api_mcp
  parent_plan_split: "Plan 214-06 split into 214-06-01 (backend gating + API + MCP, this plan) and 214-06-02 (UI render)."
  acs_folded:
    - SG-1   # isSaaSSurfaceEnabled(tenant_id) helper exported from lib/markos/saas/core/surface-gates.ts
    - SG-3   # API handlers return 404 + {"error":"saas_surface_disabled"} when gate is closed
    - BD-12  # lib/markos/mcp/tools/saas-core.cjs registers MCP tools that respect isSaaSSurfaceEnabled
    - X-10   # growth-extension non-activation test verifies placeholder renders for any non-empty growth_mode_hint AND no growth surface is reachable
  surface_gates_canonical: |
    isSaaSSurfaceEnabled(tenant_id) returns true IFF:
      markos_orgs.business_type == 'saas'
      AND saas_suite_activations.activation_status == 'active'
      AND tenant_0_readiness_gate.status == 'opened' (per 213-05; stub returns 'opened' if helper not yet shipped)
  growth_extension_metadata_only_rule: |
    Every saas_growth_extension_points row defaults to is_runnable = false AND blocked_reason = 'requires_phase_218'.
    Phase 214 NEVER activates any growth module (PLG, ABM, referral, community, events, PR, partnership, developer marketing, revenue-alignment).
    The rendered placeholder banner uses variant="future_phase_217" (UI-facing copy) but the backend reason literal is "requires_phase_218" verbatim per UI-SPEC §Surface A data dependencies note.
  mobile_priority_secondary_rule: |
    lib/markos/operator/shell.ts SurfaceRouteContract for `settings.saas` declares mobile_priority: 'secondary' (UI-SPEC §Plan Scope Classification).
    'desktop_only' is FORBIDDEN as a mobile_priority value (208-01-01 architecture-lock string).
must_haves:
  truths:
    - "Migration 129 creates `saas_growth_extension_points` plus a helper view/function for SaaS surface gating."
    - "Every growth extension row defaults to `is_runnable = false` and `blocked_reason = 'requires_phase_218'` (verbatim from UI-SPEC §Surface A note: backend literal is `requires_phase_218`; UI placeholder variant is `future_phase_217`)."
    - "`lib/markos/saas/core/surface-gates.ts` exports `isSaaSSurfaceEnabled(tenant_id): Promise<boolean>` returning true IFF `markos_orgs.business_type == 'saas'` AND `saas_suite_activations.activation_status == 'active'` AND `tenant_0_readiness_gate.status == 'opened'` (SG-1; UI-SPEC §Surface gating contract verbatim)."
    - "SaaS API, MCP, and operator settings surfaces appear only when the gate returns true (SG-3 + BD-12)."
    - "Every SaaS API handler (`api/v1/saas/activation`, `plans`, `subscriptions`, `mutations`) returns HTTP 404 with body `{\"error\":\"saas_surface_disabled\"}` when the gate is closed (SG-3 verbatim)."
    - "`lib/markos/mcp/tools/saas-core.cjs` registers SaaS-core MCP tools conditionally per the gate; tools are absent from the `lib/markos/mcp/tools/index.cjs` list when the gate is closed (BD-12)."
    - "API hosted-auth uses `requireHostedSupabaseAuth` from `onboarding/backend/runtime-context.cjs` (architecture-lock helper-presence; never `requireSupabaseAuth`)."
    - "`lib/markos/operator/shell.ts` extends the SurfaceRouteContract list with `settings.saas` carrying `mobile_priority: 'secondary'` (UI-SPEC §Plan Scope Classification mobile-priority literal); `desktop_only` is forbidden."
    - "No Phase 214 surface may activate PLG, ABM, referral, community, events, PR, partnership, developer marketing, or revenue-alignment modules. The growth-extension non-activation test verifies the constraint."
    - "Plan 06 is the translation gate into Phase 218: it stores extension metadata without claiming SG-01 completion."
  artifacts:
    - path: supabase/migrations/129_saas_surface_gating.sql
      provides: growth-extension storage plus a SaaS-surface gating view/function
    - path: lib/markos/saas/core/surface-gates.ts
      provides: canonical `isSaaSSurfaceEnabled(tenant_id)` helper consumed by every Phase 215-220 SaaS surface
      exports: [isSaaSSurfaceEnabled, getTenant0ReadinessGateStatus]
    - path: lib/markos/saas/core/growth-extension.ts
      provides: typed accessors for `saas_growth_extension_points` rows; enforces `is_runnable = false` invariant at app layer
      exports: [GROWTH_MODE_HINTS, BLOCKED_REASON_REQUIRES_PHASE_218, getGrowthExtensionForTenant]
    - path: lib/markos/mcp/tools/saas-core.cjs
      provides: tenant-scoped SaaS-core MCP tools that respect activation gating
    - path: lib/markos/operator/shell.ts
      provides: extension of SurfaceRouteContract list with `settings.saas` carrying `mobile_priority: 'secondary'`
    - path: contracts/F-296-saas-core-api-v1.yaml
      provides: activation, plans, subscriptions, and mutation handler contracts
    - path: contracts/F-300-saas-surface-gating-v1.yaml
      provides: surface-gating contract — `isSaaSSurfaceEnabled` semantics + 404 body literal
  key_links:
    - from: lib/markos/saas/core/growth-extension.ts
      to: supabase/migrations/129_saas_surface_gating.sql
      via: growth mode hints are stored with `is_runnable=false` until P218 lands
      pattern: "requires_phase_218"
    - from: api/v1/saas/activation.js
      to: onboarding/backend/runtime-context.cjs
      via: hosted auth uses `requireHostedSupabaseAuth`
      pattern: "requireHostedSupabaseAuth"
    - from: api/v1/saas/activation.js
      to: lib/markos/saas/core/surface-gates.ts
      via: every handler calls `isSaaSSurfaceEnabled` and returns 404 + saas_surface_disabled literal when false
      pattern: "saas_surface_disabled"
---

<objective>
Expose the SaaS Suite foundation through gated API and MCP, declare the canonical `isSaaSSurfaceEnabled` helper that every downstream SaaS surface (P215-P220) consumes, register the operator settings route as `mobile_priority: 'secondary'`, and store growth-extension metadata without making any growth module runnable.

This is the BACKEND half of Plan 214-06. The UI render half (settings page + 2 extracted components + Storybook stories) is owned by Plan 214-06-02 (separate plan; same wave 5; depends on this plan).

Backend ACs folded from 214-UI-SPEC.md: SG-1 (gate helper), SG-3 (API handlers return 404 when gated), BD-12 (MCP tools respect gate), X-10 backend half (growth-extension non-activation regression test).
</objective>

<context>
@.planning/phases/214-saas-suite-activation-subscription-core/214-RESEARCH.md
@.planning/phases/214-saas-suite-activation-subscription-core/214-REVIEWS.md
@.planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md
@.planning/phases/214-saas-suite-activation-subscription-core/214-VALIDATION.md
</context>

<tasks>
<task id="214-06-01" tdd="true">
  <name>SG-1, SG-3, BD-12, X-10: SaaS surface-gating helper, gated API + MCP, growth-extension metadata-only, mobile_priority='secondary'</name>
  <files>supabase/migrations/129_saas_surface_gating.sql, lib/markos/saas/core/surface-gates.ts, lib/markos/saas/core/growth-extension.ts, lib/markos/saas/core/index.cjs, lib/markos/mcp/tools/saas-core.cjs, lib/markos/mcp/tools/index.cjs, lib/markos/operator/shell.ts, api/v1/saas/activation.js, api/v1/saas/plans.js, api/v1/saas/subscriptions.js, api/v1/saas/mutations.js, contracts/F-296-saas-core-api-v1.yaml, contracts/F-297-saas-growth-extension-v1.yaml, contracts/F-299-saas-activation-wizard-v1.yaml, contracts/F-300-saas-surface-gating-v1.yaml, contracts/flow-registry.json, test/saas-214/domain-6/saas-api-gating.test.js, test/saas-214/domain-6/saas-mcp-gating.test.js, test/saas-214/domain-6/growth-extension-non-activation.test.js</files>
  <read_first>
    - .planning/phases/214-saas-suite-activation-subscription-core/214-RESEARCH.md section "Domain 6 - Surfaces and growth extension point"
    - .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md §Surface gating contract (verbatim `isSaaSSurfaceEnabled` semantics + verbatim 404 body literal)
    - .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md §Growth-mode metadata-only contract (5 growth_mode_hint literals + `requires_phase_218` backend reason)
    - .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md §Plan Scope Classification (mobile_priority='secondary' for settings.saas)
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md §SurfaceRouteContract (3 mobile_priority literals: critical | secondary | desktop_only)
    - .planning/phases/213-tenant0-dogfood-compliance-validation/213-UI-SPEC.md §Tenant 0 readiness gate (213-05 go/no-go opens P214-P217 SaaS Suite gate)
    - onboarding/backend/runtime-context.cjs
    - lib/markos/mcp/tools/index.cjs
  </read_first>
  <behavior>
    - Test (SG-1 helper PASS): `isSaaSSurfaceEnabled('<saas-tenant>')` returns true when `business_type='saas'` AND `activation_status='active'` AND tenant 0 gate `status='opened'`.
    - Test (SG-1 helper FAIL — non-SaaS tenant): `isSaaSSurfaceEnabled('<ecommerce-tenant>')` returns false.
    - Test (SG-1 helper FAIL — not activated): `isSaaSSurfaceEnabled('<saas-tenant-draft>')` returns false when activation_status='draft'.
    - Test (SG-1 helper FAIL — tenant 0 gate closed): `isSaaSSurfaceEnabled('<saas-tenant>')` returns false when tenant 0 gate `status='closed'`.
    - Test (SG-3 API gated): GET `/api/v1/saas/activation` for a non-SaaS tenant returns HTTP 404 with body `{"error":"saas_surface_disabled"}` (verbatim string match including exact JSON shape).
    - Test (SG-3 API ungated): GET `/api/v1/saas/activation` for an activated SaaS tenant returns HTTP 200 with the activation row.
    - Test (SG-3 across all 4 handlers): the 404 + `saas_surface_disabled` body is returned by `activation.js`, `plans.js`, `subscriptions.js`, AND `mutations.js` when the gate is closed.
    - Test (BD-12 MCP gated): `lib/markos/mcp/tools/index.cjs` listing for a non-SaaS tenant excludes the SaaS-core tools; for an activated SaaS tenant, the listing includes them.
    - Test (BD-12 MCP tool calls gated): a tool call against a non-SaaS tenant returns the same `saas_surface_disabled` shape.
    - Test (X-10 growth metadata only): every row in `saas_growth_extension_points` has `is_runnable=false` AND `blocked_reason='requires_phase_218'` (DEFAULT) — even rows with `growth_mode_hint` set to one of `{b2b, b2c, plg_b2b, plg_b2c, b2b2c}`.
    - Test (X-10 growth not reachable): no API route, no MCP tool, no router file matches `app/(markos)/growth/` or `api/v1/saas/growth/`. The architecture-lock self-check greps for these paths and asserts 0 hits.
    - Test (mobile_priority): `lib/markos/operator/shell.ts` SurfaceRouteContract list contains an entry for `settings.saas` with `mobile_priority: 'secondary'`. The string `'desktop_only'` does not appear anywhere in shell.ts (208-01-01 architecture-lock).
    - Test (helper-presence positive): `grep -c "requireHostedSupabaseAuth" api/v1/saas/*.js` returns >= 4 (one per handler). `grep -c "requireSupabaseAuth" api/v1/saas/*.js` returns 0.
  </behavior>
  <acceptance_criteria>
    Verify: `npm test -- test/saas-214/domain-6/saas-api-gating.test.js test/saas-214/domain-6/saas-mcp-gating.test.js test/saas-214/domain-6/growth-extension-non-activation.test.js` returns 0 failures.
    Verify: `grep -c "saas_surface_disabled" api/v1/saas/activation.js api/v1/saas/plans.js api/v1/saas/subscriptions.js api/v1/saas/mutations.js` returns >= 4 (one literal per handler).
    Verify: `grep -c "isSaaSSurfaceEnabled" api/v1/saas/activation.js api/v1/saas/plans.js api/v1/saas/subscriptions.js api/v1/saas/mutations.js lib/markos/mcp/tools/saas-core.cjs` returns >= 5.
    Verify: `grep -c "requires_phase_218" supabase/migrations/129_saas_surface_gating.sql lib/markos/saas/core/growth-extension.ts` returns >= 2.
    Verify: `grep -E "b2b|b2c|plg_b2b|plg_b2c|b2b2c" supabase/migrations/129_saas_surface_gating.sql` returns >= 1 line containing all 5 growth_mode_hint literals in a CHECK constraint.
    Verify: `grep -c "mobile_priority.*secondary" lib/markos/operator/shell.ts` returns >= 1.
    Verify: `grep -c "desktop_only" lib/markos/operator/shell.ts` returns 0.
    Verify: `grep -ciE "requireSupabaseAuth" api/v1/saas/` returns 0; `grep -c "requireHostedSupabaseAuth" api/v1/saas/` returns >= 4.
  </acceptance_criteria>
  <action>
    Step 1: Create migration `129_saas_surface_gating.sql`:
      - Create `saas_growth_extension_points` (`extension_id` uuid pk; `tenant_id`; `growth_mode_hint` text NULL CHECK in `('b2b','b2c','plg_b2b','plg_b2c','b2b2c')`; `is_runnable` boolean NOT NULL DEFAULT false; `blocked_reason` text NOT NULL DEFAULT 'requires_phase_218'; tenant-scoped RLS).
      - Add CHECK constraint `is_runnable = false AND blocked_reason = 'requires_phase_218'` (hard invariant during P214; relaxed in P218 migration).
      - Create view `saas_surface_enabled` returning `tenant_id, enabled boolean` joining `markos_orgs` + `saas_suite_activations` + the tenant 0 readiness flag (stub-table reference; helper falls back to `'opened'` if the source table is absent).
    Step 2: Implement `lib/markos/saas/core/surface-gates.ts`:
      - Export `isSaaSSurfaceEnabled(tenantId: string): Promise<boolean>` querying the view; cache per-request.
      - Export `getTenant0ReadinessGateStatus(): Promise<'opened' | 'closed'>` (stub returns `'opened'` if 213-05 helper not yet shipped per UI-SPEC §Reviewer notes 4).
    Step 3: Implement `lib/markos/saas/core/growth-extension.ts`:
      - Export `GROWTH_MODE_HINTS = ['b2b','b2c','plg_b2b','plg_b2c','b2b2c'] as const` (verbatim from UI-SPEC §Growth-mode metadata-only contract).
      - Export `BLOCKED_REASON_REQUIRES_PHASE_218 = 'requires_phase_218' as const`.
      - Export `getGrowthExtensionForTenant(client, tenantId)` returning the row OR null.
      - Refuse to write any row with `is_runnable=true` (assertion + typed error).
    Step 4: Implement 4 API handlers (`api/v1/saas/{activation,plans,subscriptions,mutations}.js`) — Vercel serverless function shape (NOT `route.ts`, NOT `app/api/.../route.ts` per architecture-lock):
      - Use `requireHostedSupabaseAuth` from `onboarding/backend/runtime-context.cjs` for auth.
      - Call `isSaaSSurfaceEnabled(tenantId)` first; if false, respond `404` + `JSON.stringify({error:"saas_surface_disabled"})` (verbatim).
      - On gate-open, perform the standard tenant-scoped CRUD/list operations against the appropriate 214-02..05 table.
    Step 5: Implement `lib/markos/mcp/tools/saas-core.cjs`:
      - Register tool functions for activation status, plan list, subscription list, mutation list.
      - Each tool's handler calls `isSaaSSurfaceEnabled` first; returns the `saas_surface_disabled` shape when closed.
      - Update `lib/markos/mcp/tools/index.cjs` to conditionally include SaaS tools per the gate.
    Step 6: Extend `lib/markos/operator/shell.ts` SurfaceRouteContract list with `settings.saas` carrying `mobile_priority: 'secondary'`. Confirm zero `desktop_only` literals exist anywhere in this file.
    Step 7: Create the 4 contracts (`F-296`..`F-300` skipping the existing F-298): `F-296-saas-core-api-v1.yaml` (4 handlers), `F-297-saas-growth-extension-v1.yaml` (5-hint enum + `requires_phase_218` invariant), `F-299-saas-activation-wizard-v1.yaml` (6-step wizard contract for the UI plan to consume), `F-300-saas-surface-gating-v1.yaml` (`isSaaSSurfaceEnabled` semantics + the verbatim 404 body literal). Update `contracts/flow-registry.json`.
    Step 8: Land the 3 backend domain-6 test files (`saas-api-gating.test.js`, `saas-mcp-gating.test.js`, `growth-extension-non-activation.test.js`); each `test()` block cites SG-1 / SG-3 / BD-12 / X-10 in its name.
  </action>
  <verify>
    <automated>npm test -- test/saas-214/domain-6/saas-api-gating.test.js test/saas-214/domain-6/saas-mcp-gating.test.js test/saas-214/domain-6/growth-extension-non-activation.test.js</automated>
  </verify>
  <done>SaaS surface-gating helper exists (SG-1), gated API handlers return the verbatim 404 + `saas_surface_disabled` shape (SG-3), MCP tools respect the gate (BD-12), growth-extension is metadata-only with `is_runnable=false` invariant + `requires_phase_218` reason (X-10 backend half), and operator shell registers `settings.saas` as `mobile_priority: 'secondary'`. F-296 + F-297 + F-299 + F-300 registered. UI render is owned by Plan 214-06-02.</done>
</task>
</tasks>

<verification>
- npm test -- test/saas-214/domain-6/saas-api-gating.test.js
- npm test -- test/saas-214/domain-6/saas-mcp-gating.test.js
- npm test -- test/saas-214/domain-6/growth-extension-non-activation.test.js
</verification>

<success_criteria>
- Migration 129 ships with `saas_growth_extension_points` + the `saas_surface_enabled` view + the `is_runnable=false` hard invariant.
- `lib/markos/saas/core/surface-gates.ts` exports the canonical `isSaaSSurfaceEnabled` helper that every downstream SaaS surface (P215-P220) consumes (SG-1).
- 4 API handlers + MCP tools all return `404 + {"error":"saas_surface_disabled"}` when the gate is closed (SG-3 + BD-12).
- `lib/markos/operator/shell.ts` registers `settings.saas` with `mobile_priority: 'secondary'`; zero `desktop_only` literals exist.
- Growth extension is metadata-only; the architecture-lock self-check confirms no growth UI route or API path exists in the diff.
- F-296 + F-297 + F-299 + F-300 registered without collision.
</success_criteria>
</content>
