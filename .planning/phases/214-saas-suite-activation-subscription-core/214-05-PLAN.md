---
phase: 214
plan: "05"
type: execute
wave: 4
depends_on:
  - "214-03"
  - "214-04"
f_ids:
  - F-294
  - F-295
migration_slots:
  - 128
integrates_with:
  - "RUN-01..08 from P207 (mutation requests link back to AgentRun ids rather than owning run substrate)"
  - "TASK-01..05 from P208 (approval and follow-up visibility stays in the Human Operating Interface phase)"
  - "EVD-01..06 from P209 (Phase 214 stores linkage only; evidence substrate stays elsewhere)"
translation_gate_for: []
files_modified:
  - supabase/migrations/128_saas_lifecycle_governance.sql
  - lib/markos/saas/core/mutation-request.ts
  - lib/markos/saas/core/approval-routing.ts
  - lib/markos/saas/core/audit-linkage.ts
  - lib/markos/saas/core/evidence.ts
  - lib/markos/saas/core/index.cjs
  - contracts/F-294-saas-mutation-request-v1.yaml
  - contracts/F-295-saas-lifecycle-audit-v1.yaml
  - contracts/flow-registry.json
  - test/saas-214/domain-5/mutation-request-lifecycle.test.js
  - test/saas-214/domain-5/mutation-request-approval-link.test.js
  - test/saas-214/domain-5/lifecycle-audit-evidence-required.test.js
  - test/saas-214/domain-5/lifecycle-audit-visibility.test.js
  - .planning/phases/214-saas-suite-activation-subscription-core/214-VALIDATION.md
autonomous: true
requirements:
  - SAAS-03
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - QA-05
  - QA-06
ui_spec_fold:
  source: .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md
  fold_date: 2026-04-29
  scope: backend_doctrine
  acs_folded:
    - BD-9    # saas_subscription_mutation_requests + saas_subscription_audit_links + SAAS_LIFECYCLE_AUDIT_REQUIRES_EVIDENCE_PACK trigger
    - BD-10   # buildApprovalPackage reuse from lib/markos/crm/agent-actions.ts (architecture-lock helper-presence)
    - BD-11   # 7 mutation_family literals + billing_charge_approval handoff_kind extension + ApprovalHandoffRecord linkage + data.export mutation-class for audit-log + evidence_refs preserved
  mutation_family_literals_verbatim:
    - subscription_create
    - subscription_upgrade
    - subscription_downgrade
    - subscription_pause
    - subscription_cancel
    - subscription_reactivate
    - audit_log_export
  mutation_class_binding_verbatim:
    subscription_create: billing.charge
    subscription_upgrade: billing.charge
    subscription_downgrade: billing.charge
    subscription_pause: billing.charge
    subscription_cancel: billing.charge
    subscription_reactivate: billing.charge
    audit_log_export: data.export
  handoff_kind_extension: billing_charge_approval
  status_literals:
    - requested
    - approved
    - rejected
    - applied
    - rolled_back
must_haves:
  truths:
    - "Migration 128 creates `saas_subscription_mutation_requests` and `saas_subscription_audit_links` as SaaS-side governance tables that integrate with later run/task/evidence substrate (BD-9)."
    - "`saas_subscription_mutation_requests.mutation_family` accepts only the 7 verbatim literals from UI-SPEC §Surface C mutation-class binding: `subscription_create | subscription_upgrade | subscription_downgrade | subscription_pause | subscription_cancel | subscription_reactivate | audit_log_export`."
    - "`saas_subscription_mutation_requests.root_mutation_class` is derived (and stored) per the mutation_class_binding map: 6 lifecycle families bind to `billing.charge`; `audit_log_export` binds to `data.export` (UI-SPEC §Surface C verbatim)."
    - "`saas_subscription_mutation_requests.status` accepts only the 5 literals from UI-SPEC §Surface A data dependencies: `requested | approved | rejected | applied | rolled_back`."
    - "Risky lifecycle mutations cannot move into `approved`, `applied`, or `rolled_back` states unless approval, audit, and evidence linkage is present (BD-9 trigger)."
    - "Trigger `SAAS_LIFECYCLE_AUDIT_REQUIRES_EVIDENCE_PACK` blocks audit-light mutations from becoming canonical: when `status` transitions to `approved | applied | rolled_back` AND (`approval_package_id IS NULL` OR `evidence_pack_id IS NULL`), the trigger raises."
    - "The implementation uses `buildApprovalPackage` from `lib/markos/crm/agent-actions.ts` rather than introducing a parallel approval helper (BD-10; architecture-lock helper-presence rule from 214-01)."
    - "P214 introduces the new `handoff_kind` literal `billing_charge_approval` (extends the 207 4-literal set: `approval | recovery | follow_up | manual_input`); the `ApprovalHandoffRecord` linkage on each mutation row points at this kind for lifecycle mutations and `data_export` (existing) for audit-log exports (UI-SPEC §From 207 inheritance)."
    - "`evidence_refs` (jsonb array) is preserved on the audit-link row; the trigger compares `evidence_refs` non-empty when `evidence_pack_id` is set; never inlines evidence content."
    - "Audit visibility is explicit for support and compliance readers and remains tenant-scoped via RLS."
  artifacts:
    - path: supabase/migrations/128_saas_lifecycle_governance.sql
      provides: mutation-request and audit-link tables with the 7-mutation-family CHECK + the 5-status CHECK + the `billing_charge_approval` handoff_kind extension + evidence-pack enforcement trigger
    - path: lib/markos/saas/core/approval-routing.ts
      provides: approval-package builder (delegating to `buildApprovalPackage`) and route-selection helpers for pause/cancel/upgrade/downgrade/reactivation/audit-log-export
      exports: [routeApprovalPackage, classifyLifecycleRisk, MUTATION_FAMILIES, MUTATION_CLASS_BINDING, HANDOFF_KIND_BILLING_CHARGE_APPROVAL]
    - path: contracts/F-294-saas-mutation-request-v1.yaml
      provides: SaaS mutation-request contract with 7-literal mutation_family enum + 5-literal status enum + root_mutation_class derivation rule
    - path: contracts/F-295-saas-lifecycle-audit-v1.yaml
      provides: SaaS lifecycle audit-link contract with evidence-refs schema
  key_links:
    - from: lib/markos/saas/core/approval-routing.ts
      to: lib/markos/crm/agent-actions.ts
      via: `buildApprovalPackage` is reused instead of replaced (BD-10)
      pattern: "buildApprovalPackage"
    - from: supabase/migrations/128_saas_lifecycle_governance.sql
      to: supabase/migrations/126_saas_subscription_lifecycle.sql
      via: approval-required transition requests cannot become applied without governance linkage (BD-9 enforcement at audit layer)
      pattern: "evidence_pack_id"
    - from: lib/markos/saas/core/approval-routing.ts
      to: lib/markos/saas/core/lifecycle.ts
      via: imports APPROVAL_REQUIRED_TRANSITIONS from 214-03 to determine which transitions need a mutation_request row
      pattern: "APPROVAL_REQUIRED_TRANSITIONS"
---

<objective>
Route risky subscription lifecycle work through explicit mutation requests, approval packages, audit links, and evidence references without taking ownership away from the later run/task/evidence phases.

Backend doctrine ACs folded from 214-UI-SPEC.md: BD-9 (mutation-request + audit-link tables + evidence-pack trigger), BD-10 (`buildApprovalPackage` reuse, no parallel helper), BD-11 (7 mutation_family literals verbatim + `billing_charge_approval` handoff_kind extension + `ApprovalHandoffRecord` linkage + `data.export` mutation-class for audit-log + evidence_refs preserved).
</objective>

<context>
@.planning/phases/214-saas-suite-activation-subscription-core/214-RESEARCH.md
@.planning/phases/214-saas-suite-activation-subscription-core/214-REVIEWS.md
@.planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md
@.planning/phases/214-saas-suite-activation-subscription-core/214-VALIDATION.md
</context>

<tasks>
<task id="214-05-01" tdd="true">
  <name>BD-9, BD-10, BD-11: Lifecycle governance, approval routing (buildApprovalPackage reuse), 7 mutation families, billing_charge_approval handoff_kind, audit/evidence linkage</name>
  <files>supabase/migrations/128_saas_lifecycle_governance.sql, lib/markos/saas/core/mutation-request.ts, lib/markos/saas/core/approval-routing.ts, lib/markos/saas/core/audit-linkage.ts, lib/markos/saas/core/evidence.ts, lib/markos/saas/core/index.cjs, contracts/F-294-saas-mutation-request-v1.yaml, contracts/F-295-saas-lifecycle-audit-v1.yaml, contracts/flow-registry.json, test/saas-214/domain-5/mutation-request-lifecycle.test.js, test/saas-214/domain-5/mutation-request-approval-link.test.js, test/saas-214/domain-5/lifecycle-audit-evidence-required.test.js, test/saas-214/domain-5/lifecycle-audit-visibility.test.js</files>
  <read_first>
    - .planning/phases/214-saas-suite-activation-subscription-core/214-RESEARCH.md section "Domain 5 - Lifecycle governance, audit, and evidence"
    - .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md §Surface C mutation-class binding (7 mutation_family literals + their root_mutation_class assignments)
    - .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md §From 206-UI-SPEC inheritance (mutation-class doctrine: `billing.charge` for monetary lifecycle, `data.export` for audit-log)
    - .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md §From 207-UI-SPEC inheritance (ApprovalHandoffRecord with new `billing_charge_approval` literal extending 4-literal set)
    - .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md §From 208-UI-SPEC inheritance (Approval Inbox extension: 7 mutation_family literals join 208-04 family map)
    - .planning/phases/206-soc2-type1-foundation/206-UI-SPEC.md §mutation-class-policy.md (root classes; default_approval_mode rules; autonomy_ceiling rules; evidence_required flag)
    - .planning/phases/207-agentrun-v2-orchestration-substrate/207-UI-SPEC.md §ApprovalHandoffRecord (the 4 existing handoff_kind literals to extend)
    - lib/markos/crm/agent-actions.ts (canonical `buildApprovalPackage`)
    - supabase/migrations/53_agent_run_lifecycle.sql
    - supabase/migrations/54_governance_evidence.sql
  </read_first>
  <behavior>
    - Test (BD-9 schema): `saas_subscription_mutation_requests` exists with `mutation_request_id` uuid pk; `tenant_id`; `subscription_id` FK; `mutation_family` text CHECK in the 7-literal list; `root_mutation_class` text CHECK in `('billing.charge','data.export')`; `requested_transition` jsonb; `requested_payload` jsonb; `agent_run_id` uuid NULL FK; `approval_package_id` uuid NULL FK; `task_id` uuid NULL FK; `evidence_pack_id` uuid NULL FK; `rollback_event_id` uuid NULL; `handoff_kind` text CHECK in `('approval','recovery','follow_up','manual_input','billing_charge_approval','data_export')`; `status` text CHECK in `('requested','approved','rejected','applied','rolled_back')`; `created_at`/`updated_at`.
    - Test (BD-9 schema audit): `saas_subscription_audit_links` exists with FK to mutation_request_id; `evidence_refs` jsonb array; `audit_summary` text; tenant-scoped RLS.
    - Test (BD-9 trigger PASS): UPDATE setting `status='approved'` with non-null `approval_package_id` AND `evidence_pack_id` succeeds.
    - Test (BD-9 trigger FAIL — approved without approval): UPDATE setting `status='approved'` with `approval_package_id IS NULL` raises `SAAS_LIFECYCLE_AUDIT_REQUIRES_EVIDENCE_PACK`.
    - Test (BD-9 trigger FAIL — applied without evidence): UPDATE setting `status='applied'` with `evidence_pack_id IS NULL` raises the same trigger.
    - Test (BD-9 trigger FAIL — rolled_back without evidence): UPDATE setting `status='rolled_back'` with `evidence_pack_id IS NULL` raises the same trigger.
    - Test (BD-9 trigger FAIL — empty evidence_refs): when `evidence_pack_id` is set but `evidence_refs` is `'[]'::jsonb` on the audit-link row, the trigger raises.
    - Test (BD-10 helper-presence): `lib/markos/saas/core/approval-routing.ts` imports `buildApprovalPackage` from `lib/markos/crm/agent-actions.ts` (not from a parallel module). The architecture-lock self-check greps for `createApprovalPackage` and `requireSupabaseAuth` in this plan's diff and finds 0 hits.
    - Test (BD-11 mutation_family enum): inserting with `mutation_family='foo'` raises a CHECK violation; each of the 7 valid literals succeeds.
    - Test (BD-11 root_class derivation): when `mutation_family ∈ {subscription_create, subscription_upgrade, subscription_downgrade, subscription_pause, subscription_cancel, subscription_reactivate}`, `root_mutation_class` is auto-set to `'billing.charge'`. When `mutation_family='audit_log_export'`, `root_mutation_class` is auto-set to `'data.export'`. Mismatched manual values raise a trigger error.
    - Test (BD-11 handoff_kind extension): inserting with `handoff_kind='billing_charge_approval'` succeeds; with `handoff_kind='approval'` (existing 207 literal) succeeds; with `handoff_kind='foo'` fails the CHECK.
    - Test (BD-11 ApprovalHandoffRecord linkage): when a mutation row is created with `mutation_family='subscription_pause'`, `approval-routing.ts` calls `buildApprovalPackage` and writes the resulting package_id back into `approval_package_id`; the `handoff_kind` is set to `billing_charge_approval`.
    - Test (TS exports): `MUTATION_FAMILIES` is a 7-tuple matching the SQL CHECK verbatim; `MUTATION_CLASS_BINDING` is a frozen Record<MutationFamily, 'billing.charge' | 'data.export'> with the 7 entries; `HANDOFF_KIND_BILLING_CHARGE_APPROVAL = 'billing_charge_approval' as const`.
    - Test (audit visibility RLS): cross-tenant SELECT on `saas_subscription_audit_links` returns 0 rows; same-tenant authenticated SELECT returns the rows.
  </behavior>
  <acceptance_criteria>
    Verify: `npm test -- test/saas-214/domain-5/` returns 0 failures.
    Verify: `grep -E "subscription_create|subscription_upgrade|subscription_downgrade|subscription_pause|subscription_cancel|subscription_reactivate|audit_log_export" supabase/migrations/128_saas_lifecycle_governance.sql` returns >= 1 line containing all 7 literals in a CHECK constraint.
    Verify: `grep -E "billing\\.charge|data\\.export" supabase/migrations/128_saas_lifecycle_governance.sql` returns >= 1 CHECK constraint enumerating the 2 root classes.
    Verify: `grep -c "billing_charge_approval" supabase/migrations/128_saas_lifecycle_governance.sql lib/markos/saas/core/approval-routing.ts` returns >= 2 (SQL CHECK + TS export).
    Verify: `grep -c "buildApprovalPackage" lib/markos/saas/core/approval-routing.ts` returns >= 1 (reuse, not redefine).
    Verify: `grep -ciE "createApprovalPackage|requireSupabaseAuth" lib/markos/saas/core/` returns 0 (architecture-lock helper-presence).
    Verify: `grep -c "SAAS_LIFECYCLE_AUDIT_REQUIRES_EVIDENCE_PACK" supabase/migrations/128_saas_lifecycle_governance.sql` returns >= 1.
  </acceptance_criteria>
  <action>
    Step 1: Create `saas_subscription_mutation_requests` per the BD-9 schema. The CHECK constraint on `mutation_family` enumerates exactly the 7 verbatim literals from UI-SPEC §Surface C. The CHECK on `root_mutation_class` enumerates `'billing.charge'` and `'data.export'`. The CHECK on `handoff_kind` enumerates the 4 existing 207 literals PLUS the 2 P214 extensions (`billing_charge_approval`, `data_export`). The CHECK on `status` enumerates the 5 literals from UI-SPEC §Surface A. Apply tenant-scoped RLS.
    Step 2: Create `saas_subscription_audit_links` (FK to mutation_request_id; `evidence_refs` jsonb array NOT NULL DEFAULT '[]'; `audit_summary` text; tenant-scoped RLS).
    Step 3: Create trigger `SAAS_LIFECYCLE_AUDIT_REQUIRES_EVIDENCE_PACK` BEFORE UPDATE on `saas_subscription_mutation_requests` raising when `NEW.status IN ('approved','applied','rolled_back')` AND (`NEW.approval_package_id IS NULL` OR `NEW.evidence_pack_id IS NULL`) (`errcode 'SAAS09'`).
    Step 4: Create trigger `SAAS_MUTATION_FAMILY_REQUIRES_ROOT_CLASS_CONSISTENCY` raising when the `(mutation_family, root_mutation_class)` pair contradicts the canonical binding (e.g. `mutation_family='audit_log_export'` with `root_mutation_class='billing.charge'`).
    Step 5: Implement `lib/markos/saas/core/approval-routing.ts`:
      - Import `buildApprovalPackage` from `lib/markos/crm/agent-actions.ts` (NEVER define a parallel helper).
      - Import `APPROVAL_REQUIRED_TRANSITIONS` from `lib/markos/saas/core/lifecycle.ts` (214-03).
      - Export `MUTATION_FAMILIES = ['subscription_create','subscription_upgrade','subscription_downgrade','subscription_pause','subscription_cancel','subscription_reactivate','audit_log_export'] as const` (verbatim from UI-SPEC).
      - Export `MUTATION_CLASS_BINDING: Readonly<Record<MutationFamily, 'billing.charge' | 'data.export'>>` (frozen object).
      - Export `HANDOFF_KIND_BILLING_CHARGE_APPROVAL = 'billing_charge_approval' as const`.
      - Export `routeApprovalPackage(client, {tenant_id, subscription_id, mutation_family, requested_payload, agent_run_id?})` that:
         a. Derives `root_mutation_class` from `MUTATION_CLASS_BINDING[mutation_family]`.
         b. Calls `buildApprovalPackage` with the derived class + the appropriate `handoff_kind` (`billing_charge_approval` for the 6 lifecycle families; `data_export` for `audit_log_export`).
         c. Inserts the mutation_request row with `status='requested'`, the resulting `approval_package_id`, and the chosen `handoff_kind`.
      - Export `classifyLifecycleRisk(prior_state, requested_state): 'auto' | 'requires_approval'` reading APPROVAL_REQUIRED_TRANSITIONS.
    Step 6: Implement `mutation-request.ts` (CRUD + status update helpers that respect the trigger), `audit-linkage.ts` (creates audit-link rows with non-empty `evidence_refs`), `evidence.ts` (typed accessors for `evidence_pack_id` references — never inlines evidence content).
    Step 7: Mirror via `index.cjs` for legacy callers.
    Step 8: Create `contracts/F-294-saas-mutation-request-v1.yaml` (mutation-request schema with 7-family enum + 5-status enum + handoff_kind extension list + class-binding rule). Create `contracts/F-295-saas-lifecycle-audit-v1.yaml` (audit-link schema with evidence_refs jsonb array). Update `contracts/flow-registry.json`.
    Step 9: Land the 4 domain-5 test files; each `test()` block cites BD-9 / BD-10 / BD-11 in its name. Add an architecture-lock self-check that greps for `createApprovalPackage` and `requireSupabaseAuth` in the new files and asserts 0 hits.
  </action>
  <verify>
    <automated>npm test -- test/saas-214/domain-5/</automated>
  </verify>
  <done>Risky lifecycle changes are human-visible (BD-9), auditable, and evidence-backed without creating a second approval or evidence system (BD-10). The 7 `mutation_family` literals + 2-class binding + `billing_charge_approval` handoff_kind extension + `data.export` for audit-log + preserved `evidence_refs` are all encoded as both SQL CHECKs and TS literal-union exports for cross-plan consumption (BD-11). UI-SPEC ACs BD-9 + BD-10 + BD-11 satisfied at the governance layer.</done>
</task>
</tasks>

<verification>
- npm test -- test/saas-214/domain-5/
</verification>

<success_criteria>
- Migration 128 ships with `saas_subscription_mutation_requests` + `saas_subscription_audit_links` + the evidence-pack trigger (BD-9).
- `lib/markos/saas/core/approval-routing.ts` reuses `buildApprovalPackage` from CRM and never defines a parallel helper (BD-10).
- The 7 `mutation_family` literals, the 2-class binding, the `billing_charge_approval` handoff_kind extension, the `data.export` mutation-class for audit-log export, and the preserved `evidence_refs` are encoded as SQL CHECKs and TS literal-union exports (BD-11).
- F-294 + F-295 registered without collision.
- The architecture-lock helper-presence self-check passes (no `createApprovalPackage` or `requireSupabaseAuth` strings in the diff).
</success_criteria>
</content>
