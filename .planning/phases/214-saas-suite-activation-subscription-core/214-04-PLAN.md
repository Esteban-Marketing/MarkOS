---
phase: 214
plan: "04"
type: execute
wave: 3
depends_on:
  - "214-02"
f_ids:
  - F-293
migration_slots:
  - 127
integrates_with:
  - "v3.8 CRM substrate from migration 58 (P214 reuses `crm_customers`, `crm_accounts`, `crm_companies`, and `crm_tasks`; it does not mutate CRM ownership)"
translation_gate_for: []
files_modified:
  - supabase/migrations/127_saas_customer_identity_bridge.sql
  - lib/markos/saas/core/customer-bridge.ts
  - lib/markos/saas/core/bridge-resolver.ts
  - lib/markos/saas/core/merge-review.ts
  - lib/markos/saas/core/index.cjs
  - contracts/F-293-saas-customer-bridge-v1.yaml
  - contracts/flow-registry.json
  - test/saas-214/domain-4/customer-bridge-single-match.test.js
  - test/saas-214/domain-4/customer-bridge-multi-match.test.js
  - test/saas-214/domain-4/customer-bridge-orphan.test.js
  - test/saas-214/domain-4/customer-bridge-merge-review.test.js
  - test/saas-214/rls/customer-bridge-cross-tenant-denial.test.js
  - .planning/phases/214-saas-suite-activation-subscription-core/214-VALIDATION.md
autonomous: true
requirements:
  - SAAS-03
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - QA-05
  - QA-06
ui_spec_fold:
  source: .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md
  fold_date: 2026-04-29
  scope: backend_doctrine
  acs_folded:
    - BD-8   # saas_customer_bridges + SAAS_CUSTOMER_BRIDGE_REQUIRES_CRM_IDENTITY trigger; conflict review semantics; never duplicate customer truth
  doctrine_lock: |
    Architecture-lock string `crm second customer truth` is FORBIDDEN in this plan's diff.
    P214 NEVER creates a parallel SaaS customer table; bridges are the only allowed shape.
    Every bridge row MUST carry `crm_customer_id IS NOT NULL OR crm_account_id IS NOT NULL`.
  resolution_status_literals:
    - linked
    - needs_review
    - orphaned
    - merged
must_haves:
  truths:
    - "Migration 127 creates `saas_customer_bridges` instead of mutating CRM ownership or creating parallel SaaS customer records (BD-8)."
    - "Each bridge row links activation + subscription to CRM customer/account/company references and stores processor customer refs plus a resolution status enumerated as `linked | needs_review | orphaned | merged` (UI-SPEC §Downstream UI Inheritance Map row 'CRM-bridge conflict review')."
    - "Trigger `SAAS_CUSTOMER_BRIDGE_REQUIRES_CRM_IDENTITY` blocks rows that have no usable CRM anchor (`crm_customer_id IS NULL AND crm_account_id IS NULL` raises)."
    - "Conflict states are explicit (`linked`, `needs_review`, `orphaned`, `merged`) so merge-review work is visible instead of hidden — these are the literals a future P217 admin UI will render via `.c-badge--{state}` per UI-SPEC §Downstream UI Inheritance Map."
    - "Unique `(tenant_id, subscription_id)` prevents duplicate bridge rows for the same subscription (forward-FK target from 214-02 `saas_subscriptions`)."
    - "The architecture-lock doctrine string `crm second customer truth` MUST NOT appear anywhere in this plan's diff (enforced by 214-01 forbidden-string list)."
  artifacts:
    - path: supabase/migrations/127_saas_customer_identity_bridge.sql
      provides: the bridge table, RLS, uniqueness constraints, and identity-presence trigger
    - path: lib/markos/saas/core/bridge-resolver.ts
      provides: deterministic CRM match resolution and conflict classification
      exports: [resolveBridgeCandidate, classifyBridgeConflict, BRIDGE_RESOLUTION_STATUSES]
    - path: contracts/F-293-saas-customer-bridge-v1.yaml
      provides: bridge contract and conflict-state schema
  key_links:
    - from: supabase/migrations/127_saas_customer_identity_bridge.sql
      to: supabase/migrations/58_crm_core_entities.sql
      via: bridge rows FK into CRM substrate instead of altering it
      pattern: "crm_customer_id"
---

<objective>
Connect SaaS subscriptions to existing CRM identity safely, using a bridge-table model that supports conflict review without creating a second customer truth.

Backend doctrine AC folded from 214-UI-SPEC.md: BD-8 (saas_customer_bridges table + identity-presence trigger + 4-literal resolution_status). The 4 resolution-status literals are exported as a TS literal-union tuple so a future P217 admin UI consumes the same source of truth via `.c-badge--{state}` per UI-SPEC §Downstream UI Inheritance Map.
</objective>

<context>
@.planning/phases/214-saas-suite-activation-subscription-core/214-RESEARCH.md
@.planning/phases/214-saas-suite-activation-subscription-core/214-REVIEWS.md
@.planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md
@.planning/phases/214-saas-suite-activation-subscription-core/214-VALIDATION.md
</context>

<tasks>
<task id="214-04-01" tdd="true">
  <name>BD-8: CRM identity bridge for SaaS subscriptions (4-literal resolution_status; never duplicate customer truth)</name>
  <files>supabase/migrations/127_saas_customer_identity_bridge.sql, lib/markos/saas/core/customer-bridge.ts, lib/markos/saas/core/bridge-resolver.ts, lib/markos/saas/core/merge-review.ts, lib/markos/saas/core/index.cjs, contracts/F-293-saas-customer-bridge-v1.yaml, contracts/flow-registry.json, test/saas-214/domain-4/customer-bridge-single-match.test.js, test/saas-214/domain-4/customer-bridge-multi-match.test.js, test/saas-214/domain-4/customer-bridge-orphan.test.js, test/saas-214/domain-4/customer-bridge-merge-review.test.js, test/saas-214/rls/customer-bridge-cross-tenant-denial.test.js</files>
  <read_first>
    - .planning/phases/214-saas-suite-activation-subscription-core/214-RESEARCH.md section "Domain 4 - CRM identity bridge"
    - .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md §Downstream UI Inheritance Map row 'CRM-bridge conflict review' (4 literal values for `resolution_status`)
    - supabase/migrations/58_crm_core_entities.sql
    - lib/markos/crm/agent-actions.ts (for `buildApprovalPackage` reuse pattern)
  </read_first>
  <behavior>
    - Test (BD-8 schema): `saas_customer_bridges` exists with `bridge_id` uuid pk; `tenant_id` uuid; `subscription_id` uuid FK to `saas_subscriptions`; `crm_customer_id` uuid NULL FK to `crm_customers`; `crm_account_id` uuid NULL FK to `crm_accounts`; `crm_company_id` uuid NULL FK to `crm_companies`; `processor_customer_refs` jsonb; `resolution_status` text CHECK in `('linked','needs_review','orphaned','merged')`; UNIQUE `(tenant_id, subscription_id)`.
    - Test (BD-8 identity trigger PASS): inserting a row with `crm_customer_id` set (and other CRM cols NULL) succeeds; with `crm_account_id` set (others NULL) succeeds.
    - Test (BD-8 identity trigger FAIL): inserting a row with `crm_customer_id IS NULL AND crm_account_id IS NULL` raises `SAAS_CUSTOMER_BRIDGE_REQUIRES_CRM_IDENTITY`.
    - Test (uniqueness): inserting a second row with the same `(tenant_id, subscription_id)` raises a unique-constraint violation.
    - Test (RLS): cross-tenant SELECT returns 0 rows; cross-tenant INSERT raises 42501.
    - Test (single-match resolver): `resolveBridgeCandidate` returns `{status:'linked', crm_customer_id}` when exactly one customer matches the subscription identifier.
    - Test (multi-match resolver): when 2+ customers match, returns `{status:'needs_review', candidates:[...]}` and the bridge row carries `resolution_status='needs_review'`.
    - Test (orphan resolver): when 0 customers match, returns `{status:'orphaned'}` and the bridge row carries `resolution_status='orphaned'` with both `crm_customer_id` and `crm_account_id` NULL — but the trigger blocks the insert. The resolver therefore creates a `crm_tasks` merge-review row FIRST, then re-tries with `crm_account_id` populated to a placeholder account; the orphan state is recorded on the placeholder rather than the bridge.
    - Test (merge resolver): when 2 bridges are merged via `merge-review.ts`, the surviving bridge has `resolution_status='merged'` and the merged-from row has `merged_into_bridge_id` set.
    - Test (TS export): `BRIDGE_RESOLUTION_STATUSES` is exactly the 4-tuple `['linked','needs_review','orphaned','merged']`.
    - Test (architecture-lock self-check): `grep -i "crm second customer truth" lib/markos/saas/core/customer-bridge.ts lib/markos/saas/core/bridge-resolver.ts lib/markos/saas/core/merge-review.ts supabase/migrations/127_saas_customer_identity_bridge.sql` returns 0 hits.
  </behavior>
  <acceptance_criteria>
    Verify: `npm test -- test/saas-214/domain-4/ test/saas-214/rls/customer-bridge-cross-tenant-denial.test.js` returns 0 failures.
    Verify: `grep -E "linked|needs_review|orphaned|merged" supabase/migrations/127_saas_customer_identity_bridge.sql` returns at least one CHECK constraint enumerating all 4 literals.
    Verify: `grep -c "SAAS_CUSTOMER_BRIDGE_REQUIRES_CRM_IDENTITY" supabase/migrations/127_saas_customer_identity_bridge.sql` returns >= 1.
    Verify: `grep -ciE "crm second customer truth|saas_customers \\(|create table saas_customers" lib/markos/saas/core/ supabase/migrations/127_saas_customer_identity_bridge.sql` returns 0 (architecture-lock doctrine string never appears; no parallel SaaS customer table).
    Verify: `grep -E "crm_customer_id|crm_account_id" supabase/migrations/127_saas_customer_identity_bridge.sql` shows the FKs reference `crm_customers(customer_id)` and `crm_accounts(account_id)` from migration 58 verbatim — bridge holds references, never copies.
  </acceptance_criteria>
  <action>
    Step 1: Create `saas_customer_bridges` per the BD-8 schema. The CHECK constraint on `resolution_status` enumerates exactly the 4 verbatim literals. Foreign keys point at the existing CRM tables from migration 58 — never copy CRM identity into local columns.
    Step 2: Create trigger `SAAS_CUSTOMER_BRIDGE_REQUIRES_CRM_IDENTITY` BEFORE INSERT OR UPDATE raising when `NEW.crm_customer_id IS NULL AND NEW.crm_account_id IS NULL` (`errcode 'SAAS08'`, hint `'Bridge requires at least one CRM identity anchor (crm_customer_id OR crm_account_id).'`).
    Step 3: Apply tenant-scoped RLS on the bridge table. Add UNIQUE `(tenant_id, subscription_id)` to prevent duplicate bridges per subscription.
    Step 4: Implement `lib/markos/saas/core/bridge-resolver.ts` exporting:
      - `BRIDGE_RESOLUTION_STATUSES = ['linked','needs_review','orphaned','merged'] as const`.
      - `resolveBridgeCandidate(client, {tenant_id, subscription_id, processor_customer_refs}): Promise<ResolutionResult>` implementing the deterministic single/multi/orphan/merged classification.
      - `classifyBridgeConflict(candidates): BridgeResolutionStatus`.
    Step 5: Implement `customer-bridge.ts` (CRUD + resolver wiring) and `merge-review.ts` (merge two bridges; mark survivor `resolution_status='merged'`; create a `crm_tasks` row using `buildApprovalPackage` from `lib/markos/crm/agent-actions.ts` — never replace).
    Step 6: Mirror via `index.cjs` for legacy callers.
    Step 7: Create `contracts/F-293-saas-customer-bridge-v1.yaml` with the 4-literal resolution-status enum. Update `contracts/flow-registry.json`.
    Step 8: Land the 4 domain-4 test files plus the RLS test. Add a doctrine self-check test that greps the diff for `crm second customer truth` and `create table saas_customers` and asserts both are 0.
  </action>
  <verify>
    <automated>npm test -- test/saas-214/domain-4/ test/saas-214/rls/customer-bridge-cross-tenant-denial.test.js</automated>
  </verify>
  <done>SaaS subscriptions connect to CRM safely (BD-8), the 4-literal `resolution_status` enum is encoded as both SQL CHECK and TS literal-union, duplicate customer truth is avoided (no parallel SaaS customer table), the architecture-lock doctrine string `crm second customer truth` never appears in the diff, and merge-review reuses `buildApprovalPackage` rather than introducing a parallel approval helper.</done>
</task>
</tasks>

<verification>
- npm test -- test/saas-214/domain-4/
- npm test -- test/saas-214/rls/customer-bridge-cross-tenant-denial.test.js
</verification>

<success_criteria>
- Migration 127 ships with `saas_customer_bridges`, the identity-presence trigger, and tenant-scoped RLS (BD-8).
- `lib/markos/saas/core/bridge-resolver.ts` exports `BRIDGE_RESOLUTION_STATUSES` as a 4-tuple matching the SQL CHECK verbatim.
- The architecture-lock doctrine string `crm second customer truth` is absent from the diff (self-check test).
- No parallel `saas_customers` table exists; bridges hold FKs into the existing CRM substrate.
- F-293 registered without collision.
</success_criteria>
</content>
