---
phase: 214
plan: "01"
type: execute
wave: 1
depends_on: []
f_ids:
  - F-287
  - F-298
migration_slots:
  - 124
integrates_with:
  - "PRC-09 from P205 (Pricing Engine remains owner of approved pricing context; P214 only normalizes business_type and activation eligibility)"
  - "RUN-01..08 from P207 and TASK-01..05 from P208 (preflight hard-gates later lifecycle wiring even though P214 owns only the SaaS-side tables)"
translation_gate_for: []
files_modified:
  - .planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md
  - test/fixtures/saas-214/index.js
  - test/fixtures/saas-214/activation.js
  - test/fixtures/saas-214/subscription.js
  - lib/markos/saas/core/preflight/upstream-gate.ts
  - lib/markos/saas/core/preflight/architecture-lock.ts
  - lib/markos/saas/core/preflight/errors.ts
  - lib/markos/saas/core/preflight/index.cjs
  - scripts/preconditions/214-01-check-upstream.cjs
  - test/saas-214/preflight/wave-0-baseline.test.js
  - test/saas-214/preflight/architecture-lock.test.js
  - test/saas-214/preflight/upstream-gate.test.js
  - test/saas-214/preflight/helper-presence.test.js
  - supabase/migrations/124_saas_activation_foundation.sql
  - lib/markos/saas/core/activation/contracts.ts
  - lib/markos/saas/core/activation/contracts.cjs
  - lib/markos/saas/core/activation/business-type.ts
  - lib/markos/saas/core/activation/activation.ts
  - lib/markos/saas/core/activation/index.cjs
  - contracts/F-287-saas-activation-v1.yaml
  - contracts/F-298-saas-214-architecture-lock-v1.yaml
  - contracts/flow-registry.json
  - test/saas-214/domain-1/activation-gate.test.js
  - test/saas-214/domain-1/business-type-normalization.test.js
  - test/saas-214/domain-1/activation-trigger.test.js
  - test/saas-214/rls/activation-cross-tenant-denial.test.js
  - .planning/phases/214-saas-suite-activation-subscription-core/214-VALIDATION.md
autonomous: true
requirements:
  - SAAS-01
  - SAAS-02
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - QA-05
  - QA-06
ui_spec_fold:
  source: .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md
  fold_date: 2026-04-29
  scope: backend_doctrine
  acs_folded:
    - BD-1   # markos_orgs.business_type column with backfill from onboarding_seed.company.business_model
    - BD-2   # saas_suite_activations table with all required wizard fields
    - BD-3   # SAAS_ACTIVATION_REQUIRES_BUSINESS_TYPE trigger blocks non-SaaS activation attempts
  architecture_lock_forbidden_strings:
    - "bypass surface gating"
    - "pricing override"
    - "growth runnable in 214"
    - "crm second customer truth"
  architecture_lock_forbidden_paths_when_wave_lt_5:
    - "app/(markos)/settings/saas/"
    - "components/markos/tenant/SaaS"
must_haves:
  truths:
    - "Wave 0.5 baseline guard asserts no migration 124 with saas_activation prefix exists, no contracts F-287/F-298 exist, and no P214 reservation row exists yet in `.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md`."
    - "P214 Plan 01 creates or appends `.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md` and documents the execution-first / slot-last rule: P214 executes before P215-P220 while reserving slots 124-129; downstream phases may read P214 tables but P214 may not hold reverse FKs into them."
    - "F-ID reservation P214 = F-287..F-300, with Plan 01 owning F-287 (saas-activation-v1) and F-298 (saas-214-architecture-lock-v1)."
    - "Preflight CLI hard-fails when `markos_orgs`, `pricing_recommendations`, `markos_agent_runs`, `agent_approval_packages`, `crm_customers`, `crm_accounts`, or `crm_tasks` are missing; soft-warns on `evidence_map_records` and `governance_evidence_packs`."
    - "Architecture-lock forbids `createApprovalPackage`, `requireSupabaseAuth`, `requireTenantContext`, `serviceRoleClient`, `lookupPlugin`, `public/openapi.json`, `app/(saas)`, `api/v1/.../route.ts`, `app/api/.../route.ts`, `vitest`, `playwright`, and `.test.ts` across the Phase 214 surface."
    - "Architecture-lock forbids the 4 doctrine-violation strings verbatim: `bypass surface gating`, `pricing override`, `growth runnable in 214`, `crm second customer truth` (UI-SPEC §Reviewer notes 1)."
    - "Architecture-lock forbids any write to `app/(markos)/settings/saas/` or `components/markos/tenant/SaaS*` from waves 1-4 (UI-SPEC §Reviewer notes 1; only 214-06 may write these paths)."
    - "Migration 124 adds durable `markos_orgs.business_type` normalization (literal values: `saas | ecommerce | services | other` per UI-SPEC §Surface A data dependencies) and creates `saas_suite_activations` with tenant-scoped RLS, wizard configuration fields, growth-extension metadata, and trigger `SAAS_ACTIVATION_REQUIRES_BUSINESS_TYPE`."
    - "`saas_suite_activations.activation_status` accepts only the 3 literals from UI-SPEC §Surface A data dependencies: `draft | active | disabled` (a 4th `failed` literal is documented as a rare error state in `<SaaSActivationPanel />` AP-2 but is NOT a DB-stored value; it is a derived UI state computed from trigger errors)."
  artifacts:
    - path: .planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md
      provides: creation or append of the v4.1.0 slot/F-ID history with a Phase 214 reservation row and an explicit note that P214 is first in execution order even though it is last in slot order
    - path: lib/markos/saas/core/preflight/upstream-gate.ts
      provides: `assertUpstreamReady`, `REQUIRED_UPSTREAM`, and `SOFT_UPSTREAM` for SaaS-core planning and later execution
      exports: [assertUpstreamReady, REQUIRED_UPSTREAM, SOFT_UPSTREAM]
    - path: lib/markos/saas/core/preflight/architecture-lock.ts
      provides: `assertArchitectureLock`; forbids 12 helper-mismatch strings + 4 doctrine-violation strings + UI-path prefixes when wave < 5
      exports: [assertArchitectureLock, FORBIDDEN_HELPERS, FORBIDDEN_DOCTRINE_STRINGS, UI_PATH_PREFIXES_LOCKED_UNTIL_WAVE_5]
    - path: supabase/migrations/124_saas_activation_foundation.sql
      provides: `markos_orgs.business_type`, `saas_suite_activations`, tenant-scoped RLS, and trigger `SAAS_ACTIVATION_REQUIRES_BUSINESS_TYPE`
  key_links:
    - from: onboarding/onboarding-seed.schema.json
      to: supabase/migrations/124_saas_activation_foundation.sql
      via: `company.business_model` is the canonical source for backfill into durable tenant business_type
      pattern: "business_model"
    - from: lib/markos/saas/core/activation/business-type.ts
      to: onboarding/onboarding.js
      via: normalizes onboarding seed data into canonical `business_type`
      pattern: "business_model"
    - from: scripts/preconditions/214-01-check-upstream.cjs
      to: supabase tables pricing_recommendations + markos_agent_runs + agent_approval_packages + crm_customers + crm_accounts + crm_tasks
      via: service-role `select('*').limit(0)` table-existence checks with hard/soft gating
      pattern: "REQUIRED_UPSTREAM"
---

<objective>
Ship the activation foundation for the SaaS Suite: Wave 0.5 preflight and architecture lock, the v4.1.0 slot/F-ID coordination doc, durable tenant `business_type`, the `saas_suite_activations` table, and the activation trigger that blocks non-SaaS tenants from entering the suite.

Backend doctrine ACs folded from 214-UI-SPEC.md: BD-1 (business_type column), BD-2 (saas_suite_activations table), BD-3 (activation trigger). Architecture-lock extended with the 4 doctrine-violation strings and the UI-path prefix lock that prevents waves 1-4 from writing into the 214-06 surface area.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/214-saas-suite-activation-subscription-core/214-CONTEXT.md
@.planning/phases/214-saas-suite-activation-subscription-core/214-RESEARCH.md
@.planning/phases/214-saas-suite-activation-subscription-core/214-REVIEWS.md
@.planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md
@.planning/phases/214-saas-suite-activation-subscription-core/214-VALIDATION.md
</context>

<tasks>
<task id="214-01-00">
  <name>Wave 0.5 preflight, architecture lock (with 4 doctrine strings + UI-path prefix lock), and slot coordination</name>
  <files>.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md, lib/markos/saas/core/preflight/upstream-gate.ts, lib/markos/saas/core/preflight/architecture-lock.ts, lib/markos/saas/core/preflight/errors.ts, lib/markos/saas/core/preflight/index.cjs, scripts/preconditions/214-01-check-upstream.cjs, test/saas-214/preflight/wave-0-baseline.test.js, test/saas-214/preflight/architecture-lock.test.js, test/saas-214/preflight/upstream-gate.test.js, test/saas-214/preflight/helper-presence.test.js, .planning/phases/214-saas-suite-activation-subscription-core/214-VALIDATION.md</files>
  <read_first>
    - .planning/phases/214-saas-suite-activation-subscription-core/214-RESEARCH.md section "Domain 0 - Allocation and preflight"
    - .planning/phases/214-saas-suite-activation-subscription-core/214-REVIEWS.md sections HIGH and MEDIUM concerns
    - .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md §Reviewer / executor handoff notes (item 1: forbidden-file-prefix list for waves 1-4)
    - .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md §Surface gating contract (verbatim copy of `isSaaSSurfaceEnabled` semantics)
    - .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md §Pricing placeholder rule (P214 must NOT take pricing ownership)
    - onboarding/backend/runtime-context.cjs
    - lib/markos/crm/agent-actions.ts
    - lib/markos/plugins/registry.js
  </read_first>
  <acceptance_criteria>
    Architecture-lock test fixtures must FAIL when any of the following appear in the planned 214 surface diff:
    - Strings: `createApprovalPackage`, `requireSupabaseAuth`, `requireTenantContext`, `serviceRoleClient`, `lookupPlugin`, `public/openapi.json`, `vitest`, `playwright`, `.test.ts`
    - Path prefixes: `app/(saas)`, `api/v1/.../route.ts`, `app/api/.../route.ts`
    - 4 doctrine-violation strings (verbatim, case-insensitive grep): `bypass surface gating`, `pricing override`, `growth runnable in 214`, `crm second customer truth`
    - When `wave < 5`: any path under `app/(markos)/settings/saas/` or starting with `components/markos/tenant/SaaS`
    Architecture-lock test fixtures must PASS when the planned diff contains only:
    - Helpers: `buildApprovalPackage`, `requireHostedSupabaseAuth`, `resolvePlugin`, `lib/markos/mcp/tools/index.cjs`, `contracts/openapi.json`
    Verify: `npm test -- test/saas-214/preflight/architecture-lock.test.js` exercises both PASS and FAIL fixtures for the 12 helper strings, the 4 doctrine strings, and the UI-path prefix lock at wave=2.
  </acceptance_criteria>
  <action>
    Step 1: Create or append `.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md` with the full reservation history. The Phase 214 row reserves migration slots 124-129 and F-IDs F-287..F-300 with the explicit note "P214 is first in execution order even though it is last in slot order; downstream P215-P220 may read P214 tables but P214 holds no reverse FKs into them."
    Step 2: Implement `lib/markos/saas/core/preflight/upstream-gate.ts` exporting `REQUIRED_UPSTREAM` (the 7 hard upstreams: `markos_orgs`, `pricing_recommendations`, `markos_agent_runs`, `agent_approval_packages`, `crm_customers`, `crm_accounts`, `crm_tasks`) and `SOFT_UPSTREAM` (the 2 soft upstreams: `evidence_map_records`, `governance_evidence_packs`) and `assertUpstreamReady(client)` doing service-role `select('*').limit(0)` table-existence checks. Implement the CommonJS bridge at `lib/markos/saas/core/preflight/index.cjs`. Implement the CLI entrypoint at `scripts/preconditions/214-01-check-upstream.cjs`.
    Step 3: Implement `lib/markos/saas/core/preflight/architecture-lock.ts` exporting:
      - `FORBIDDEN_HELPERS` array of 12 strings (the helper-mismatch list above) PLUS the 3 path prefixes (`app/(saas)`, `api/v1/.../route.ts`, `app/api/.../route.ts`).
      - `FORBIDDEN_DOCTRINE_STRINGS` array of 4 strings verbatim: `'bypass surface gating'`, `'pricing override'`, `'growth runnable in 214'`, `'crm second customer truth'` (case-insensitive match).
      - `UI_PATH_PREFIXES_LOCKED_UNTIL_WAVE_5` array: `['app/(markos)/settings/saas/', 'components/markos/tenant/SaaS']` — when the runtime `wave` parameter is less than 5, any path matching these prefixes triggers a fail.
      - `assertArchitectureLock(diff, { wave })` returning `{ ok: boolean, violations: Array<{ kind: 'helper' | 'doctrine' | 'ui_path_locked', match: string }> }`.
    Step 4: Seed preflight fixtures at `test/fixtures/saas-214/index.js`, `activation.js`, `subscription.js`. Write the 4 preflight test files (`wave-0-baseline.test.js`, `architecture-lock.test.js`, `upstream-gate.test.js`, `helper-presence.test.js`) that exercise PASS and FAIL fixtures for every forbidden string, every doctrine-violation string, and the UI-path prefix lock at wave=2 vs wave=5.
    Step 5: Append pending rows to `214-VALIDATION.md` per-task map for tasks 214-01-00 and 214-01-01 with verbatim verify command literals.
  </action>
  <verify>
    <automated>npm test -- test/saas-214/preflight/</automated>
  </verify>
  <done>Wave 0.5 preflight exists, helper-presence is enforced, the 4 doctrine-violation strings are grep-blocked, the UI-path prefix lock prevents waves 1-4 from writing into the 214-06 surface area, and the v4.1.0 coordination document records Phase 214 as the first execution phase in the lane.</done>
</task>

<task id="214-01-01" tdd="true">
  <name>BD-1, BD-2, BD-3: Activation foundation and SaaS business-type gate</name>
  <files>supabase/migrations/124_saas_activation_foundation.sql, lib/markos/saas/core/activation/contracts.ts, lib/markos/saas/core/activation/contracts.cjs, lib/markos/saas/core/activation/business-type.ts, lib/markos/saas/core/activation/activation.ts, lib/markos/saas/core/activation/index.cjs, contracts/F-287-saas-activation-v1.yaml, contracts/F-298-saas-214-architecture-lock-v1.yaml, contracts/flow-registry.json, test/saas-214/domain-1/activation-gate.test.js, test/saas-214/domain-1/business-type-normalization.test.js, test/saas-214/domain-1/activation-trigger.test.js, test/saas-214/rls/activation-cross-tenant-denial.test.js</files>
  <read_first>
    - .planning/phases/214-saas-suite-activation-subscription-core/214-RESEARCH.md section "Domain 1 - Activation gate and operator wizard"
    - .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md §Surface A data dependencies (literal values for `business_type` and `activation_status`)
    - .planning/phases/214-saas-suite-activation-subscription-core/214-UI-SPEC.md §Surface B (wizard step register: 6 fields the table must hold)
    - supabase/migrations/51_multi_tenant_foundation.sql
    - supabase/migrations/81_markos_orgs.sql
    - onboarding/onboarding-seed.schema.json
    - onboarding/onboarding.js
  </read_first>
  <behavior>
    - Test (BD-1): `markos_orgs.business_type` column exists; CHECK constraint accepts only `'saas' | 'ecommerce' | 'services' | 'other'`; backfill query reads `onboarding_seed.company.business_model` and writes `'saas'` for any tenant whose seed value is in `{'b2b_saas', 'b2c_saas', 'plg_saas', 'saas'}`.
    - Test (BD-2): `saas_suite_activations` table exists with all 13 fields from UI-SPEC §Surface A data dependencies: `activation_id` (uuid pk), `tenant_id` (uuid fk markos_orgs), `business_type_snapshot` (text), `activation_status` (text CHECK in `'draft' | 'active' | 'disabled'`), `profile_name` (text), `enabled_modules` (text[]), `country_codes` (text[]), `processor_codes` (text[]), `accounting_system_codes` (text[]), `legal_billing` (jsonb), `autonomy_posture` (text), `health_score_config` (jsonb), `growth_extension` (jsonb), `activated_at` (timestamptz), `created_at` (timestamptz default now()), `updated_at` (timestamptz default now()).
    - Test (BD-2 RLS): `select` and `insert` from a service-role client succeed; from an authenticated client with a different `tenant_id` claim, a `select` returns zero rows and an `insert` raises `42501`.
    - Test (BD-3): inserting a row with `business_type_snapshot = 'ecommerce'` raises the trigger error `SAAS_ACTIVATION_REQUIRES_BUSINESS_TYPE`; inserting with `business_type_snapshot = 'saas'` succeeds.
    - Test (idempotency): re-running the migration 124 file produces no schema diff (idempotent CREATE IF NOT EXISTS pattern).
  </behavior>
  <acceptance_criteria>
    Verify: `npm test -- test/saas-214/domain-1/activation-gate.test.js test/saas-214/domain-1/business-type-normalization.test.js test/saas-214/domain-1/activation-trigger.test.js test/saas-214/rls/activation-cross-tenant-denial.test.js` returns 0 failures.
    Verify: `grep -E "saas_suite_activations|business_type" supabase/migrations/124_saas_activation_foundation.sql | wc -l` returns >= 10 (table creation + trigger + indexes + RLS policies).
    Verify: `grep -E "'saas'|'ecommerce'|'services'|'other'" supabase/migrations/124_saas_activation_foundation.sql` returns at least one CHECK constraint enumerating all 4 literals verbatim.
    Verify: `grep -E "draft|active|disabled" supabase/migrations/124_saas_activation_foundation.sql` returns at least one CHECK constraint on `activation_status` enumerating the 3 literals verbatim.
  </acceptance_criteria>
  <action>
    Step 1: Add durable `business_type` column to `markos_orgs` with CHECK constraint accepting `'saas' | 'ecommerce' | 'services' | 'other'` (verbatim from UI-SPEC §Surface A data dependencies). Backfill from `onboarding_seed.company.business_model` reading the canonical seed schema; map onboarding values `{b2b_saas, b2c_saas, plg_saas, saas}` to the durable `'saas'` value.
    Step 2: Create `saas_suite_activations` per the UI-SPEC §Surface A data-dependency list (13 fields). Add CHECK constraint `activation_status IN ('draft','active','disabled')` (verbatim from UI-SPEC). Apply tenant-scoped RLS using the canonical Supabase pattern (service-role bypass, authenticated user filter on `tenant_id = auth.jwt()->>'tenant_id'`).
    Step 3: Add trigger `SAAS_ACTIVATION_REQUIRES_BUSINESS_TYPE` raising `errcode 'SAAS01'` with hint message `'Only tenants with business_type = saas may activate the SaaS Suite.'` when `business_type_snapshot != 'saas'`.
    Step 4: Create `contracts/F-287-saas-activation-v1.yaml` (activation contract) and `contracts/F-298-saas-214-architecture-lock-v1.yaml` (architecture-lock metadata contract). Update `contracts/flow-registry.json` to include both F-IDs.
    Step 5: Implement `lib/markos/saas/core/activation/contracts.ts` with zod schemas matching the 13 table fields. Implement `business-type.ts` with the onboarding-seed normalization helper. Implement `activation.ts` with `createActivation` + `updateActivation` + `getActivation` server-side helpers. Mirror via `contracts.cjs` and `index.cjs` for legacy callers.
    Step 6: Land the 4 test files; each `test()` block must cite the AC ID it covers in its name (e.g. `test('BD-2: saas_suite_activations has 13 fields per UI-SPEC §Surface A', ...)`).
  </action>
  <verify>
    <automated>npm test -- test/saas-214/domain-1/ test/saas-214/rls/activation-cross-tenant-denial.test.js</automated>
  </verify>
  <done>Non-SaaS tenants are blocked (BD-3), SaaS activation rows are tenant-scoped and auditable (BD-2), and business-type normalization is durable (BD-1) instead of living only in onboarding seed data. F-287 + F-298 registered. UI-SPEC ACs BD-1, BD-2, BD-3 satisfied at the schema layer.</done>
</task>
</tasks>

<verification>
- npm test -- test/saas-214/preflight/
- npm test -- test/saas-214/domain-1/
- npm test -- test/saas-214/rls/activation-cross-tenant-denial.test.js
</verification>

<success_criteria>
- `.planning/V4.1.0-MIGRATION-SLOT-COORDINATION.md` contains Phase 214 reservation rows and execution-order commentary.
- `markos_orgs.business_type` exists with the 4-literal CHECK and is backfilled where onboarding data exists (BD-1).
- `saas_suite_activations` exists with RLS, the 13 wizard fields, and the 3-literal `activation_status` CHECK (BD-2).
- `SAAS_ACTIVATION_REQUIRES_BUSINESS_TYPE` blocks non-SaaS activation attempts (BD-3).
- F-287 and F-298 are registered without collision.
- Architecture-lock detector grep-blocks the 4 doctrine-violation strings AND any path under `app/(markos)/settings/saas/` or `components/markos/tenant/SaaS*` when wave < 5.
</success_criteria>
</content>
</invoke>