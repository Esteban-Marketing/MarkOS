---
phase: 45-operations-flow-inventory-contract-map
plan: 03
type: execute
wave: 3
depends_on:
  - 45-01
  - 45-02
files_modified:
  - contracts/schema.json
  - contracts/F-*.yaml
  - .planning/FLOW-CONTRACTS.md
  - bin/validate-flow-contracts.cjs
autonomous: true
requirements:
  - API-01
  - API-02

must_haves:
  truths:
    - "Every canonical flow maps to at least one contract artifact."
    - "Contract artifacts validate against a single locked schema.json definition."
    - "Flow-to-contract mapping is explicit and reports zero orphaned flows."
  artifacts:
    - path: "contracts/schema.json"
      provides: "canonical JSON schema for per-flow contract metadata"
      exports: []
    - path: "contracts/F-01-client-intake-submit-v1.yaml"
      provides: "contract stub pattern for all flow contracts"
      exports: []
    - path: ".planning/FLOW-CONTRACTS.md"
      provides: "flow_id to contract file mapping ledger"
      exports: []
    - path: "bin/validate-flow-contracts.cjs"
      provides: "schema and coverage validator"
      exports: []
  key_links:
    - from: ".planning/FLOW-CONTRACTS.md"
      to: "contracts/*.yaml"
      via: "contract_file references"
      pattern: "contracts/F-"
    - from: "bin/validate-flow-contracts.cjs"
      to: "contracts/schema.json"
      via: "schema validation"
      pattern: "schema\.json"
---

<objective>
Create the canonical contract schema foundation and full flow-to-contract mapping artifacts.

Purpose: Deliver partial API-02 by standardizing contract structure before OpenAPI expansion in Phase 47.
Output: contracts/schema.json, 17 contract stubs, FLOW-CONTRACTS.md mapping ledger, and contract validation CLI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-operations-flow-inventory-contract-map/45-CONTEXT.md
@.planning/phases/45-operations-flow-inventory-contract-map/45-RESEARCH.md
@contracts/flow-registry.json
@.planning/FLOW-TAXONOMY.json
@.planning/FLOW-INVENTORY.md

<interfaces>
From onboarding/backend/agents/telemetry.cjs:
- ROLLOUT_ENDPOINT_SLOS keys: /submit, /approve, /linear/sync, /campaign/result
- tiers: critical, standard

Contract metadata requirements:
- flow_id pattern: F-##
- version pattern: v<number>
- openapi fixed: 3.0.3 (per D-07)
- x-markos-meta for actor/auth/handler/path fields (per D-09)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 45-03-01: Implement canonical contracts/schema.json and validator CLI (per D-07, D-08, D-09)</name>
  <read_first>
    .planning/phases/45-operations-flow-inventory-contract-map/45-RESEARCH.md
    contracts/flow-registry.json
    .planning/FLOW-TAXONOMY.json
  </read_first>
  <files>contracts/schema.json, bin/validate-flow-contracts.cjs</files>
  <behavior>
    - Test 1: schema.json enforces required fields flow_id, flow_name, domain, flow_type, version, openapi, info, paths.
    - Test 2: schema.json rejects unknown domain and flow_type values.
    - Test 3: validator exits non-zero if any contract file is missing required schema fields.
  </behavior>
  <action>
Create contracts/schema.json using JSON Schema draft-07 with exact enum sets from .planning/FLOW-TAXONOMY.json, flow_id pattern ^F-[0-9]{2}$, version pattern ^v[0-9]+$, and openapi const 3.0.3. Add x-markos-meta object for actor, auth_local, auth_hosted, handler, local_path, hosted_path, slo_tier. Create bin/validate-flow-contracts.cjs to load contracts/*.yaml, parse YAML, validate required keys and enum membership, and enforce that all flow_id values from contracts/flow-registry.json are present exactly once.
  </action>
  <acceptance_criteria>
    - contracts/schema.json parses as valid JSON and contains required + enum constraints.
    - `node bin/validate-flow-contracts.cjs` fails when a contract has unknown enum labels.
    - Validator prints count summary: total contracts, missing flows, duplicate flow_id.
  </acceptance_criteria>
  <verify>
    <automated>node bin/validate-flow-contracts.cjs --schema contracts/schema.json --registry contracts/flow-registry.json</automated>
  </verify>
  <done>
    Canonical schema and automated validator are available for all Phase 45 contract artifacts.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 45-03-02: Create 17 contract stubs and flow mapping ledger (per API-01 and partial API-02)</name>
  <read_first>
    contracts/flow-registry.json
    contracts/schema.json
    .planning/FLOW-INVENTORY.md
  </read_first>
  <files>contracts/F-01-client-intake-submit-v1.yaml, contracts/F-02-draft-approve-v1.yaml, contracts/F-03-section-regenerate-v1.yaml, contracts/F-04-system-config-read-v1.yaml, contracts/F-05-system-status-health-v1.yaml, contracts/F-06-linear-task-sync-v1.yaml, contracts/F-07-campaign-result-record-v1.yaml, contracts/F-08-markosdb-migrate-v1.yaml, contracts/F-09-literacy-coverage-report-v1.yaml, contracts/F-10-literacy-admin-health-v1.yaml, contracts/F-11-literacy-admin-query-v1.yaml, contracts/F-12-ai-interview-generate-q-v1.yaml, contracts/F-13-ai-interview-parse-answer-v1.yaml, contracts/F-14-source-extraction-v1.yaml, contracts/F-15-extract-and-score-v1.yaml, contracts/F-16-spark-suggestion-v1.yaml, contracts/F-17-competitor-discovery-v1.yaml, .planning/FLOW-CONTRACTS.md</files>
  <behavior>
    - Test 1: All 17 flow IDs resolve to exactly one contract file.
    - Test 2: Each contract includes x-markos-meta fields for actor/auth/handler/path.
    - Test 3: FLOW-CONTRACTS.md reports zero orphaned flows and zero unmapped flows.
  </behavior>
  <action>
Create one YAML contract stub per flow_id F-01..F-17 using filename convention contracts/F-XX-{flow_name}-v1.yaml. Populate each stub with flow_id, flow_name, domain, flow_type, version=v1, openapi=3.0.3, info.title, info.version=1.0.0, x-markos-meta fields, and one minimal paths entry matching local_path. Create .planning/FLOW-CONTRACTS.md with a complete mapping table (flow_id, flow_name, contract_file, contract_version, schema_valid, notes) and explicit totals: total_flows=17, orphaned_flows=0, flows_without_contracts=0.
  </action>
  <acceptance_criteria>
    - contracts directory contains 17 F-XX YAML files matching the flow registry.
    - Every mapping row in .planning/FLOW-CONTRACTS.md references an existing YAML file.
    - `node bin/validate-flow-contracts.cjs --schema contracts/schema.json --registry contracts/flow-registry.json` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node bin/validate-flow-contracts.cjs --schema contracts/schema.json --registry contracts/flow-registry.json</automated>
  </verify>
  <done>
    Flow-to-contract coverage is complete and schema-validated, establishing the Phase 47 OpenAPI starting point.
  </done>
</task>

</tasks>

<verification>
Run contract validator and a directory count check: `node -e "const fs=require('fs');const n=fs.readdirSync('contracts').filter(f=>/^F-\d{2}.*-v1\.yaml$/.test(f)).length; if(n!==17)process.exit(1);"`.
</verification>

<success_criteria>
All active Phase 45 flows are contract-mapped and validated by a canonical schema without introducing a parallel contract format.
</success_criteria>

<output>
After completion, create `.planning/phases/45-operations-flow-inventory-contract-map/45-03-SUMMARY.md`
</output>
