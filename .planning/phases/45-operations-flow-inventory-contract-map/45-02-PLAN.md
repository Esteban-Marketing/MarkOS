---
phase: 45-operations-flow-inventory-contract-map
plan: 02
type: execute
wave: 2
depends_on:
  - 45-01
files_modified:
  - .planning/FLOW-INVENTORY.md
  - .planning/FLOW-INVENTORY-MOCKUP.md
autonomous: true
requirements:
  - API-01

must_haves:
  truths:
    - "Operators can review a canonical inventory view listing at least 10 active flows."
    - "Every listed flow includes method, paths, actor, auth, handler, and journey summary."
    - "Inventory is traceable to extraction output and phase-locked taxonomy decisions."
  artifacts:
    - path: ".planning/FLOW-INVENTORY.md"
      provides: "canonical registry and per-flow detail sections"
      exports: []
    - path: ".planning/FLOW-INVENTORY-MOCKUP.md"
      provides: "UI-oriented mockup view contract for inventory browsing"
      exports: []
  key_links:
    - from: ".planning/FLOW-INVENTORY.md"
      to: "contracts/flow-registry.json"
      via: "flow table rows"
      pattern: "| F-"
    - from: ".planning/FLOW-INVENTORY.md"
      to: ".planning/FLOW-TAXONOMY.json"
      via: "domain and flow_type columns"
      pattern: "domain|flow_type"
---

<objective>
Produce the human-facing canonical flow inventory and inventory mockup artifacts for API-01 coverage.

Purpose: Give operators and engineers a shared, auditable registry before contract generation and testing.
Output: FLOW-INVENTORY.md with 100% active flow rows and FLOW-INVENTORY-MOCKUP.md aligned to the UI spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-operations-flow-inventory-contract-map/45-CONTEXT.md
@.planning/phases/45-operations-flow-inventory-contract-map/45-RESEARCH.md
@.planning/phases/45-operations-flow-inventory-contract-map/45-UI-SPEC.md
@contracts/flow-registry.json
@.planning/FLOW-TAXONOMY.json
@.planning/phases/37-markos-ui-control-plane/37-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 45-02-01: Author canonical FLOW-INVENTORY with full 17-flow registry (per D-04, D-05, D-06)</name>
  <read_first>
    contracts/flow-registry.json
    .planning/FLOW-TAXONOMY.json
    .planning/phases/45-operations-flow-inventory-contract-map/45-RESEARCH.md
  </read_first>
  <files>.planning/FLOW-INVENTORY.md</files>
  <action>
Create .planning/FLOW-INVENTORY.md with sections: metadata header, locked enum reference, flow registry table, and one detail section per flow_id F-01..F-17. Use fixed table columns: flow_id, flow_name, domain, flow_type, method, local_path, hosted_path, actor, auth_local, auth_hosted, handler, slo_tier. For each detail section include journey steps count between 3 and 12, explicit inputs, outputs, and error codes. Add a traceability subsection referencing onboarding/backend/server.cjs route bindings and onboarding/backend/handlers.cjs handler exports.
  </action>
  <acceptance_criteria>
    - `.planning/FLOW-INVENTORY.md` includes exactly 17 table rows matching flow_id F-01..F-17.
    - Every row has non-empty domain and flow_type values from .planning/FLOW-TAXONOMY.json.
    - Every flow detail section includes a journey step count and handler reference.
    - The file includes a registry summary with total active flows >= 10.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const m=fs.readFileSync('.planning/FLOW-INVENTORY.md','utf8').match(/^\| F-\d+/gm)||[];if(m.length!==17)process.exit(1);"</automated>
  </verify>
  <done>
    Canonical inventory is complete, journey-level, and technically traceable to concrete handlers.
  </done>
</task>

<task type="auto">
  <name>Task 45-02-02: Create operator-browsable mockup view contract (per SC1 and UI spec)</name>
  <read_first>
    .planning/phases/45-operations-flow-inventory-contract-map/45-UI-SPEC.md
    .planning/FLOW-INVENTORY.md
    .planning/REQUIREMENTS.md
  </read_first>
  <files>.planning/FLOW-INVENTORY-MOCKUP.md</files>
  <action>
Create .planning/FLOW-INVENTORY-MOCKUP.md as a documentation mockup with five required view states: Loading, Ready, Empty, Validation error, Review pending, Approved baseline. Include an inventory table excerpt showing at least 10 flows, a flow detail panel template, a verification checklist panel placeholder, and a KPI baseline strip placeholder with fixed fields {time_to_first_task, evidence_capture_rate, operator_self_service_rate, flow_coverage}. Keep the artifact read-only and mark any edit controls as deferred to later phases (D-19).
  </action>
  <acceptance_criteria>
    - `.planning/FLOW-INVENTORY-MOCKUP.md` contains all required states and the four-pane view structure from the UI spec.
    - The mockup excerpt displays >=10 flow rows with domain and flow_type tags.
    - Unknown taxonomy handling is explicitly marked as a validation-error state.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/FLOW-INVENTORY-MOCKUP.md','utf8');const req=['Loading','Ready','Empty','Validation error','Review pending','Approved baseline'];if(!req.every(s=>t.includes(s)))process.exit(1);"</automated>
  </verify>
  <done>
    Phase 45 has an operator-browsable inventory mockup contract without pulling scope from Phase 46.
  </done>
</task>

</tasks>

<verification>
Re-run `node bin/extract-flows.cjs --out contracts/flow-registry.json --taxonomy .planning/FLOW-TAXONOMY.json` and confirm FLOW-INVENTORY rows remain in sync.
</verification>

<success_criteria>
The inventory registry and mockup view provide a complete and auditable API-01 foundation for downstream contract work.
</success_criteria>

<output>
After completion, create `.planning/phases/45-operations-flow-inventory-contract-map/45-02-SUMMARY.md`
</output>
