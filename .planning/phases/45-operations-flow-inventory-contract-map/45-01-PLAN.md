---
phase: 45-operations-flow-inventory-contract-map
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/extract-flows.cjs
  - contracts/flow-registry.json
  - .planning/FLOW-TAXONOMY.json
autonomous: true
requirements:
  - API-01

must_haves:
  truths:
    - "A deterministic list of active production flows can be regenerated from source code without mutating runtime state."
    - "Each flow has a locked domain and flow_type classification using the Phase 45 enum set."
    - "Unknown labels fail validation so taxonomy drift cannot silently enter later phases."
  artifacts:
    - path: "bin/extract-flows.cjs"
      provides: "read-only flow extraction CLI and validation"
      exports: []
    - path: "contracts/flow-registry.json"
      provides: "machine-readable registry of discovered flows"
      exports: []
    - path: ".planning/FLOW-TAXONOMY.json"
      provides: "locked enums and flow_id classification map"
      exports: []
  key_links:
    - from: "bin/extract-flows.cjs"
      to: "onboarding/backend/server.cjs"
      via: "route/handler parsing"
      pattern: "GET|POST|handlers\.handle"
    - from: "bin/extract-flows.cjs"
      to: "api/*.js"
      via: "hosted wrapper coverage validation"
      pattern: "module\.exports|/api/"
    - from: "contracts/flow-registry.json"
      to: ".planning/FLOW-TAXONOMY.json"
      via: "flow_id classification"
      pattern: "flow_id|domain|flow_type"
---

<objective>
Build a deterministic extraction foundation for Phase 45 flow inventory and contract mapping.

Purpose: Establish an auditable, read-only source of truth for all active production flows before writing human-facing artifacts.
Output: Extraction CLI, generated flow registry JSON, and locked taxonomy map for downstream plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-operations-flow-inventory-contract-map/45-CONTEXT.md
@.planning/phases/45-operations-flow-inventory-contract-map/45-RESEARCH.md
@onboarding/backend/server.cjs
@onboarding/backend/handlers.cjs
@api/submit.js
@api/approve.js
@api/status.js
@api/regenerate.js
@api/migrate.js
@api/linear/sync.js
@api/campaign/result.js
@api/literacy/coverage.js

<interfaces>
From onboarding/backend/server.cjs route map:
- GET /config -> handlers.handleConfig
- GET /status -> handlers.handleStatus
- GET /api/literacy/coverage -> handlers.handleLiteracyCoverage
- POST /submit -> handlers.handleSubmit
- POST /regenerate -> handlers.handleRegenerate
- POST /approve -> handlers.handleApprove
- POST /migrate/local-to-cloud -> handlers.handleMarkosdbMigration
- POST /linear/sync -> handlers.handleLinearSync
- POST /campaign/result -> handlers.handleCampaignResult
- POST /api/generate-question -> handlers.handleGenerateQuestion
- POST /api/parse-answer -> handlers.handleParseAnswer
- POST /api/extract-sources -> handlers.handleExtractSources
- POST /api/extract-and-score -> handlers.handleExtractAndScore
- POST /api/spark-suggestion -> handlers.handleSparkSuggestion
- POST /api/competitor-discovery -> handlers.handleCompetitorDiscovery
- GET /admin/literacy/health -> handlers.handleLiteracyHealth
- POST /admin/literacy/query -> handlers.handleLiteracyQuery

From onboarding/backend/handlers.cjs exports:
- handleConfig, handleStatus, handleLiteracyCoverage, handleSubmit, handleRegenerate
- handleApprove, handleMarkosdbMigration, handleLinearSync, handleCampaignResult
- handleExtractSources, handleExtractAndScore, handleGenerateQuestion, handleParseAnswer
- handleSparkSuggestion, handleCompetitorDiscovery, handleCorsPreflight
- handleLiteracyHealth, handleLiteracyQuery
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 45-01-01: Build read-only flow extraction CLI and emit registry JSON (per D-10, D-11)</name>
  <read_first>
    onboarding/backend/server.cjs
    onboarding/backend/handlers.cjs
    api/submit.js
    api/approve.js
    api/status.js
    api/regenerate.js
    api/migrate.js
    api/linear/sync.js
    api/campaign/result.js
    api/literacy/coverage.js
  </read_first>
  <files>bin/extract-flows.cjs, contracts/flow-registry.json</files>
  <behavior>
    - Test 1: Extractor returns 17 flow entries with unique flow_id values F-01..F-17.
    - Test 2: Extractor output records method, local_path, hosted_path, and handler for every flow.
    - Test 3: Extractor execution does not require loading runtime modules or environment secrets.
    - Test 4: Every emitted hosted_path is cross-checked against the matching api/* wrapper surface before registry output is accepted.
  </behavior>
  <action>
Create bin/extract-flows.cjs as a read-only parser that uses fs.readFileSync + regex/string scanning only (no require() of runtime handlers). Parse onboarding/backend/server.cjs route bindings and map each route to handler names. Cross-check handler names against module.exports in onboarding/backend/handlers.cjs and fail with non-zero exit code if any mapped handler is missing. For every flow with a hosted_path, scan the corresponding api/* wrapper file(s) and fail if hosted wrapper coverage is missing or points to the wrong handler surface; this explicitly covers submit, approve, status, regenerate, migrate, linear/sync, campaign/result, and literacy/coverage wrappers. Write deterministic output to contracts/flow-registry.json with sorted flow_id order and fields {flow_id, flow_name, method, local_path, hosted_path, handler, actor, auth_local, auth_hosted}. Use fixed flow_id mapping F-01..F-17 from 45-RESEARCH.md.
  </action>
  <acceptance_criteria>
    - Running `node bin/extract-flows.cjs --out contracts/flow-registry.json` exits 0.
    - `contracts/flow-registry.json` exists and contains exactly 17 flow objects.
    - No flow object has blank method, local_path, or handler.
    - Every non-empty hosted_path has validated api/* wrapper coverage before the registry is written.
    - Duplicate flow_id values are rejected with non-zero exit code.
  </acceptance_criteria>
  <verify>
    <automated>node bin/extract-flows.cjs --out contracts/flow-registry.json</automated>
  </verify>
  <done>
    Extraction foundation is deterministic, read-only, and produces a complete machine-readable flow registry.
  </done>
</task>

<task type="auto">
  <name>Task 45-01-02: Lock taxonomy enums and classify every flow (per D-01, D-02, D-03, D-16)</name>
  <read_first>
    .planning/phases/45-operations-flow-inventory-contract-map/45-CONTEXT.md
    .planning/phases/45-operations-flow-inventory-contract-map/45-RESEARCH.md
    contracts/flow-registry.json
  </read_first>
  <files>.planning/FLOW-TAXONOMY.json, bin/extract-flows.cjs</files>
  <action>
Create .planning/FLOW-TAXONOMY.json with locked enums exactly: domain=[onboarding, execution, enrichment, integration, migration, reporting, admin] and flow_type=[submission, approval, regeneration, query, health_check, sync, record, migration, enrichment]. Add a classification map for all 17 flow_id values including both domain and flow_type. Extend bin/extract-flows.cjs to validate each flow classification against these enums and fail on unknown labels.
  </action>
  <acceptance_criteria>
    - .planning/FLOW-TAXONOMY.json exists with the exact 7 domain values and 9 flow_type values above.
    - Every flow_id in contracts/flow-registry.json has a matching classification entry.
    - Unknown domain or flow_type values cause extractor failure.
  </acceptance_criteria>
  <verify>
    <automated>node bin/extract-flows.cjs --out contracts/flow-registry.json --taxonomy .planning/FLOW-TAXONOMY.json</automated>
  </verify>
  <done>
    Taxonomy drift is blocked and all discovered flows are classified with locked enum values.
  </done>
</task>

</tasks>

<verification>
Run `node bin/extract-flows.cjs --out contracts/flow-registry.json --taxonomy .planning/FLOW-TAXONOMY.json` and confirm deterministic output + strict enum enforcement.
</verification>

<success_criteria>
Phase 45 has a reproducible extraction baseline with complete flow discovery and locked taxonomy classification.
</success_criteria>

<output>
After completion, create `.planning/phases/45-operations-flow-inventory-contract-map/45-01-SUMMARY.md`
</output>

