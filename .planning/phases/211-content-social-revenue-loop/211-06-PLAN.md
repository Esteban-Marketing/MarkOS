---
phase: 211
plan: "06"
type: execute
wave: 6
depends_on:
  - "211-01"
  - "211-02"
  - "211-03"
  - "211-04"
  - "211-05"
f_ids: []
migration_slots: []
integrates_with:
  - "TASK-01..05 from P208 (next-task recommendations and weekly narrative references must route into real human work surfaces)"
  - "LRN-01..05 from P212 (measurement handoff is the authoritative downstream learning input, not a separate interpretation layer)"
translation_gate_for:
  - "P212"
  - "P218"
  - "P219"
  - "P220"
ui_scope: NO_UI
ui_spec_block:
  - "211-UI-SPEC.md §Translation Gate Dissolution and Opening — opens future_phase_212 (`MeasurementHandoff.learning_ready == true` consumers) and future_growth_module (8 future_consumer rows in growth-loop-compatibility-map.md)"
  - "211-UI-SPEC.md §Downstream UI Inheritance Map row 11 (Growth-loop compatibility browser) — 8 growth-module rows with `future_consumer` literal status"
  - "211-UI-SPEC.md §Downstream UI Inheritance Map row 14 (Tenant 0 loop-readiness panel) — readiness rollup over `MeasurementHandoff` + `RevenueFeedbackLink` + `DispatchAttempt` + `MarketingArtifact` + `ContentBrief` + `MarketingStrategyRun` counts"
  - "211-UI-SPEC.md §Downstream UI Inheritance Map row 7 (Measurement handoff timeline) — 14-field envelope with six `next_task_kind` literals as task-class chips"
  - "211-UI-SPEC.md §Downstream UI Inheritance Map row 10 (Next-task generation queue) — 208-03 Task Board consumer; six `next_task_kind` literals route via `next_task_ref`"
files_modified:
  - .planning/phases/211-content-social-revenue-loop/211-VALIDATION.md
  - .planning/marketing-loop/measurement-handoff-contract.md
  - .planning/marketing-loop/growth-loop-compatibility-map.md
  - supabase/migrations/measurement_handoffs.sql
  - lib/markos/loop/measurement-handoff.ts
  - lib/markos/loop/next-task-recommendation.ts
  - lib/markos/loop/growth-compatibility.ts
  - api/v1/marketing/measurement-handoff.js
  - test/marketing/phase-211/domain-6/measurement-handoff.test.js
  - test/marketing/phase-211/domain-6/growth-compatibility.test.js
autonomous: true
requirements:
  - LOOP-08
  - QA-15
must_haves:
  truths:
    - "Every dispatched artifact gets expected performance envelopes, actual outcomes, and next-task recommendations."
    - "Measurement output is the authoritative handoff into Phase 212 learning, not a vague later interpretation."
    - "Future growth modules are explicitly marked as consumers, not active scope."
    - "`learning_ready == true` on `MeasurementHandoff` is the doctrine flag that opens the future_phase_212 translation gate per 211-UI-SPEC §Translation Gate Dissolution and Opening (downstream surfaces render `<PlaceholderBanner variant=\"future_phase_212\">` until P212 ships)."
    - "8 growth-module rows (`plg`, `abm`, `referral`, `community`, `events`, `pr`, `partnerships`, `developer_marketing`) all carry the literal status `future_consumer` (per 211-UI-SPEC §Downstream row 11 + §DESIGN.md Compliance Assertions row `Future-consumer doctrine`)."
    - "Doctrine prose for measurement-handoff and growth-compatibility-map carries zero CLAUDE.md banned-lexicon matches and zero exclamation points."
  artifacts:
    - path: .planning/marketing-loop/measurement-handoff-contract.md
      provides: canonical expected-versus-actual outcome handoff from loop execution into narrative and learning (cited by 211-UI-SPEC §Downstream rows 7 and 10 as future-surface authority)
    - path: .planning/marketing-loop/growth-loop-compatibility-map.md
      provides: named future-consumer map for doc 17 growth modules (cited by 211-UI-SPEC §Downstream row 11 + §Translation Gate Opening for future_growth_module placeholder)
  key_links:
    - from: .planning/phases/212-learning-literacy-evolution/212-01-PLAN.md
      to: .planning/marketing-loop/measurement-handoff-contract.md
      via: Phase 212 should consume the exact Phase 211 measurement contract without redefining it
      pattern: "expected performance envelopes"
    - from: lib/markos/crm/copilot.ts
      to: lib/markos/loop/next-task-recommendation.ts
      via: measurement deltas should create grounded next actions rather than passive dashboards
      pattern: "recommendations"
---

<ui_spec_fold>
ui_scope: NO_UI

ui_spec_block:
  - 211-UI-SPEC.md §Translation Gate Dissolution and Opening — this plan OPENS two new translation gates for downstream surfaces. (1) future_phase_212: `MeasurementHandoff.learning_ready == true` consumers render `<PlaceholderBanner variant="future_phase_212">` until P212 ships its learning-ledger surface. (2) future_growth_module: any future surface that attempts to consume `growth-loop-compatibility-map.md` rows whose status is `future_consumer` renders `<PlaceholderBanner variant="future_growth_module">` until the corresponding growth phase (P218/P219/P220) ships.
  - 211-UI-SPEC.md §Downstream UI Inheritance Map row 7 — Measurement handoff timeline (future P208 admin extension or P212 learning ledger). 14-field `MeasurementHandoff` envelope (`handoff_id`, `tenant_id`, `artifact_id`, `expected_outcome`, `actual_outcome`, `outcome_delta`, `attribution_status`, `lesson_candidate`, `next_task_kind`, `next_task_ref`, `weekly_narrative_ref`, `learning_ready`, `created_at`, `updated_at`). The doctrine phrase `expected performance envelopes` rendered verbatim in any explanatory copy. Expected-vs-actual rendered as paired numeric column (token-only, tabular-numerals); `outcome_delta` rendered with directional `.c-badge--{success,warning,error}` based on sign and threshold. Six `next_task_kind` literals (`brief_refresh`, `evidence_refresh`, `pricing_review`, `connector_recovery`, `social_follow_up`, `experiment_candidate`) render as next-task chips deep-linking via `next_task_ref` to the P208 task system. `learning_ready == true` rows compose `<PlaceholderBanner variant="future_phase_212">` until P212 ships.
  - 211-UI-SPEC.md §Downstream UI Inheritance Map row 10 — Next-task generation queue = 208-03 Task Board consumer (existing surface; will be redeployed). Reads `MeasurementHandoff.next_task_kind` + `next_task_ref`. Six `next_task_kind` literals render as task-class chips with the recipe: `brief_refresh` (info), `evidence_refresh` (warning), `pricing_review` (warning), `connector_recovery` (error), `social_follow_up` (info), `experiment_candidate` (success).
  - 211-UI-SPEC.md §Downstream UI Inheritance Map row 11 — Growth-loop compatibility browser (future P217+ growth phases or future enterprise admin). Renders the 8 growth-module rows verbatim (`plg`, `abm`, `referral`, `community`, `events`, `pr`, `partnerships`, `developer_marketing`); every row renders the exact status `future_consumer`. Each row composes `.c-card` with `.c-badge--info` "[info] future_consumer" status. NEVER renders rows as runnable until the corresponding growth phase ships (P218-P220 SaaS Growth phases per ROADMAP / REQUIREMENTS traceability). Until then, all rows render `<PlaceholderBanner variant="future_growth_module">` as a guard.
  - 211-UI-SPEC.md §Downstream UI Inheritance Map row 14 — Tenant 0 loop-readiness panel (future P213 Tenant 0 surface). Rows for `MarketingStrategyRun` count, `ContentBrief` count, `MarketingArtifact` (audited) count, `DispatchAttempt` (dispatched) count, `RevenueFeedbackLink` (ready attribution) count, `MeasurementHandoff` (with `learning_ready == true`) count. Each row's status badge computed from underlying loop-record state. Cites `211-UI-SPEC.md §Downstream UI Inheritance Map` row 13 as authority.
  - 211-UI-SPEC.md §DESIGN.md Compliance Assertions — Future-consumer doctrine row (this plan owns AC for 8 growth-module rows carrying `future_consumer` literal); Banned lexicon row (this plan ships final 2 doctrine docs); Translation-gate dissolution doctrine row (this plan opens future_phase_212 and future_growth_module gates).

cross_cutting_carry_forward:
  - "Banned lexicon (CLAUDE.md): zero matches in `.planning/marketing-loop/measurement-handoff-contract.md` and `.planning/marketing-loop/growth-loop-compatibility-map.md`. No exclamation points. Verified by 211-01-00 architecture-lock CI script. (This plan is the final 2 doctrine docs; the wider enforcement is now complete across all 9 marketing-loop docs.)"
  - "Pricing placeholder rule: not directly applicable to MeasurementHandoff fields (measurement is observed, not priced). However, `next_task_kind == 'pricing_review'` references the upstream pricing context; the pricing review task itself MUST inherit `MarketingArtifact.pricing_context_ref` rules from 211-02 + the `{{MARKOS_PRICING_ENGINE_PENDING}}` placeholder discipline."
  - "Architecture-lock forbidden strings: zero matches against the four 211-doctrine forbidden strings and four test-substrate-mismatch strings across this plan's doctrine, runtime, and test files. Enforced by `scripts/marketing-loop/check-loop-architecture-lock.mjs`."
  - "Future-consumer doctrine (this plan owns the 211-06-02 AC): the 8 growth-module rows in `growth-loop-compatibility-map.md` MUST use the exact status string `future_consumer`. No row may use synonyms like `planned`, `coming_soon`, `future_phase`, etc. (per 211-UI-SPEC §Copywriting Contract future-consumer copy rule)."
  - "Mutation-class doctrine (206-02): `MeasurementHandoff` exports to external learning systems bind to `data.export`. CSV/PDF exports of measurement narrative bind to `data.export`. Override of `lesson_candidate` (operator override) is a `manual_input` `ApprovalHandoffRecord.handoff_kind` per 207."
  - "Node API error envelopes: `error_code` literals for this plan include `measurement-handoff-not-ready`, `growth-module-not-active`. `error_message` is engineering-grade English with no banned lexicon (consumers may render verbatim)."
  - "Doctrine phrase enforcement: `expected performance envelopes` MUST appear verbatim in `.planning/marketing-loop/measurement-handoff-contract.md` AND in `test/marketing/phase-211/domain-6/measurement-handoff.test.js` (per 211-UI-SPEC §Downstream row 7 + 211-VALIDATION existing AC)."

downstream_ui_consumers_read_only:
  - "P208 admin extension or P212 learning ledger — Measurement handoff timeline (211-UI-SPEC §Downstream row 7). Reads 14-field `MeasurementHandoff` envelope. `learning_ready == true` rows render `<PlaceholderBanner variant=\"future_phase_212\">` until P212 ships. NOT modified by this plan."
  - "P208 Task Board (208-03) — Next-task generation queue (211-UI-SPEC §Downstream row 10). Reads `MeasurementHandoff.next_task_kind` + `next_task_ref`. P208 will be redeployed to remove any existing `<PlaceholderBanner variant=\"future_phase_211\">` for loop-originated tasks. NOT modified by this plan."
  - "P217+ growth phases or future enterprise admin — Growth-loop compatibility browser (211-UI-SPEC §Downstream row 11). Reads 8 future-consumer rows. NEVER renders as runnable until P218-P220 ship. NOT modified by this plan."
  - "P213 Tenant 0 surface — Tenant 0 loop-readiness panel (211-UI-SPEC §Downstream row 14). Reads readiness rollup over all 6 loop-record types. NOT modified by this plan."
  - "P208 Weekly Narrative (208-06) — joint dissolution with 211-05 of `phase_211_loop` placeholder; this plan ships `MeasurementHandoff.weekly_narrative_ref` which 208-06 reads. NOT modified by this plan."
</ui_spec_fold>

<objective>
Close the first operating loop by turning measurement into explicit next work, weekly narrative inputs, and a clean downstream handoff for learning and future growth consumers.
</objective>

<context>
@.planning/phases/211-content-social-revenue-loop/211-CONTEXT.md
@.planning/phases/211-content-social-revenue-loop/211-RESEARCH.md
@.planning/phases/211-content-social-revenue-loop/211-VALIDATION.md
@.planning/phases/211-content-social-revenue-loop/211-UI-SPEC.md
</context>

<tasks>
<task id="211-06-01">
  <name>Measurement handoff and next-task generation</name>
  <files>.planning/marketing-loop/measurement-handoff-contract.md, supabase/migrations/measurement_handoffs.sql, lib/markos/loop/measurement-handoff.ts, lib/markos/loop/next-task-recommendation.ts, api/v1/marketing/measurement-handoff.js, test/marketing/phase-211/domain-6/measurement-handoff.test.js</files>
  <read_first>
    - .planning/phases/211-content-social-revenue-loop/211-RESEARCH.md section "Domain 6 - Measurement handoff and growth compatibility"
    - .planning/phases/211-content-social-revenue-loop/211-UI-SPEC.md §Translation Gate Dissolution and Opening (this plan OPENS future_phase_212 gate via `learning_ready == true`)
    - .planning/phases/211-content-social-revenue-loop/211-UI-SPEC.md §Downstream UI Inheritance Map rows 7 (measurement handoff timeline), 10 (next-task generation queue), and 14 (Tenant 0 loop-readiness panel)
    - .planning/phases/212-learning-literacy-evolution/212-RESEARCH.md
    - lib/markos/crm/copilot.ts
    - lib/markos/crm/attribution.ts
  </read_first>
  <action>
    Step 1: Create `.planning/marketing-loop/measurement-handoff-contract.md` with the exact `MeasurementHandoff` fields `handoff_id`, `tenant_id`, `artifact_id`, `expected_outcome`, `actual_outcome`, `outcome_delta`, `attribution_status`, `lesson_candidate`, `next_task_kind`, `next_task_ref`, `weekly_narrative_ref`, `learning_ready`, `created_at`, and `updated_at`.
    Step 2: Make the contract use the exact phrase `expected performance envelopes` for pre-dispatch targets (per 211-UI-SPEC §Downstream row 7).
    Step 3: Constrain `next_task_kind` to the exact literals `brief_refresh | evidence_refresh | pricing_review | connector_recovery | social_follow_up | experiment_candidate`.
    Step 4: Add tests that cover expected-only planning, measured deltas, degraded attribution, and next-task generation. The test asserts that `learning_ready == true` is the doctrine flag opening the future_phase_212 translation gate (per 211-UI-SPEC §Translation Gate Dissolution and Opening).
  </action>
  <acceptance_criteria>
    - `.planning/marketing-loop/measurement-handoff-contract.md` contains the exact field names `expected_outcome`, `actual_outcome`, `outcome_delta`, `next_task_kind`, and `learning_ready`
    - `.planning/marketing-loop/measurement-handoff-contract.md` contains the exact phrase `expected performance envelopes`
    - `lib/markos/loop/next-task-recommendation.ts` contains the exact literals `brief_refresh`, `evidence_refresh`, `pricing_review`, `connector_recovery`, `social_follow_up`, and `experiment_candidate`
    - `test/marketing/phase-211/domain-6/measurement-handoff.test.js` contains the exact string `expected performance envelopes`
    - `.planning/marketing-loop/measurement-handoff-contract.md` contains zero matches against the CLAUDE.md banned lexicon and zero matches against the four architecture-lock forbidden strings (per 211-UI-SPEC §DESIGN.md Compliance Assertions)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/marketing/phase-211/domain-6/measurement-handoff.test.js</automated>
  </verify>
  <done>Measurement now creates explicit next work and a stable downstream contract for weekly narrative and literacy evolution.</done>
</task>

<task id="211-06-02">
  <name>Future growth compatibility map</name>
  <files>.planning/marketing-loop/growth-loop-compatibility-map.md, lib/markos/loop/growth-compatibility.ts, test/marketing/phase-211/domain-6/growth-compatibility.test.js, .planning/phases/211-content-social-revenue-loop/211-VALIDATION.md</files>
  <read_first>
    - .planning/phases/211-content-social-revenue-loop/211-RESEARCH.md section "Domain 6 - Measurement handoff and growth compatibility"
    - .planning/phases/211-content-social-revenue-loop/211-UI-SPEC.md §Downstream UI Inheritance Map row 11 (growth-loop compatibility browser)
    - .planning/phases/211-content-social-revenue-loop/211-UI-SPEC.md §Translation Gate Dissolution and Opening (this task OPENS the future_growth_module gate)
    - .planning/phases/211-content-social-revenue-loop/211-UI-SPEC.md §Copywriting Contract (future-consumer literal exact)
    - obsidian/brain/SaaS Marketing OS Strategy Canon.md
    - .planning/phases/211-content-social-revenue-loop/211-CONTEXT.md
  </read_first>
  <action>
    Step 1: Create `.planning/marketing-loop/growth-loop-compatibility-map.md` with one row each for `plg`, `abm`, `referral`, `community`, `events`, `pr`, `partnerships`, and `developer_marketing`. Each row includes a `future_consumer` literal status column AND a description of which loop primitives the module would consume once activated (per 211-UI-SPEC §Downstream row 11).
    Step 2: Make every row use the exact status `future_consumer`. No synonyms (`planned`, `coming_soon`, `future_phase`) allowed (per 211-UI-SPEC §Copywriting Contract future-consumer copy rule).
    Step 3: Implement `lib/markos/loop/growth-compatibility.ts` and tests that assert the map is descriptive only and does not enable runnable growth execution. Tests assert that the literal `future_consumer` appears for all 8 rows.
    Step 4: Update `211-VALIDATION.md` so Task `211-06-02` is represented in the verification map.
  </action>
  <acceptance_criteria>
    - `.planning/marketing-loop/growth-loop-compatibility-map.md` contains rows for `plg`, `abm`, `referral`, `community`, `events`, `pr`, `partnerships`, and `developer_marketing`
    - `.planning/marketing-loop/growth-loop-compatibility-map.md` contains the exact status `future_consumer` for every row
    - `.planning/marketing-loop/growth-loop-compatibility-map.md` contains zero matches against synonym strings `planned`, `coming_soon`, `future_phase` (per 211-UI-SPEC §Copywriting Contract)
    - `test/marketing/phase-211/domain-6/growth-compatibility.test.js` contains the exact string `future_consumer`
    - `211-VALIDATION.md` contains row `211-06-02`
    - `.planning/marketing-loop/growth-loop-compatibility-map.md` contains zero matches against the CLAUDE.md banned lexicon and zero matches against the four architecture-lock forbidden strings (per 211-UI-SPEC §DESIGN.md Compliance Assertions)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/marketing/phase-211/domain-6/growth-compatibility.test.js</automated>
  </verify>
  <done>Doc 17 growth modules are now explicitly treated as future consumers of the loop rather than accidental in-phase scope.</done>
</task>
</tasks>

<verification>
- npm test -- test/marketing/phase-211/domain-6/measurement-handoff.test.js
- npm test -- test/marketing/phase-211/domain-6/growth-compatibility.test.js
</verification>

<success_criteria>
- MeasurementHandoff has explicit expected-versus-actual and next-task fields
- Phase 212 receives a named, stable learning input contract
- Future growth modules are explicitly parked as consumers, not active scope
- 211-UI-SPEC §Downstream rows 7, 10, 11, 14 inherit the relevant field sets / state literals / future-consumer rows verbatim once their consuming surfaces ship
- Two new translation gates opened: future_phase_212 (via `learning_ready == true`) and future_growth_module (via 8 `future_consumer` rows)
- 208-06 Weekly Narrative `phase_211_loop` placeholder dissolution completed jointly with 211-05 (this plan ships `weekly_narrative_ref`)
- 8 growth-module rows all carry literal `future_consumer` status — no synonyms allowed
</success_criteria>
