---
phase: 211
plan: "03"
type: execute
wave: 3
depends_on:
  - "211-01"
  - "211-02"
f_ids: []
migration_slots: []
integrates_with:
  - "COMP-01 from P206 (dispatch must inherit approval-aware compliance posture)"
  - "RUN-01..08 from P207 (DispatchAttempt should link to real AgentRun lineage and failure visibility)"
  - "TASK-01..05 from P208 (dispatch blockers and approvals route into the human operating interface)"
  - "CONN-01..06 from P210 (connector health determines whether queued work can become real external dispatch)"
translation_gate_for: []
ui_scope: NO_UI
ui_spec_block:
  - "211-UI-SPEC.md §Translation Gate Dissolution and Opening — `DispatchAttempt.dispatch_status == 'blocked'` with `rollback_hint` referencing the unsupported claim dissolves 209 §Downstream row 6 (loop dispatch evidence gate) jointly with 211-02"
  - "211-UI-SPEC.md §Downstream UI Inheritance Map row 4 (DispatchAttempt state board) — 14-field envelope rendered with channel-icon chips + dispatch_status badges + approval_mode badges"
  - "211-UI-SPEC.md §Downstream UI Inheritance Map row 9 (Pre-dispatch approval-token viewer — loop variant) — `MarketingArtifact.draft_body` preview + `ArtifactAudit.recommended_edits` + `EvidenceMap` summary + `DispatchAttempt.channel` + connector health"
  - "211-UI-SPEC.md §Translation Gate Dissolution and Opening — 210 §Surface A `<PlaceholderBanner variant=\"future_phase_211\">` for loop-dispatch-bound connector recommendations dissolves once `DispatchAttempt.queue_status` readiness is implementable"
files_modified:
  - .planning/marketing-loop/dispatch-approval-policy.md
  - supabase/migrations/marketing_dispatch_attempts.sql
  - lib/markos/loop/dispatch-attempt.ts
  - lib/markos/loop/dispatch-gates.ts
  - lib/markos/loop/approval-linkage.ts
  - api/v1/marketing/dispatch.js
  - test/marketing/phase-211/domain-3/dispatch-state-machine.test.js
autonomous: true
requirements:
  - LOOP-04
  - QA-07
  - QA-08
  - QA-09
must_haves:
  truths:
    - "External dispatch never happens without approval or explicit earned-autonomy posture."
    - "Connector health is checked before queued work can become real dispatch."
    - "Provider receipts, queue state, and failures link back to the artifact and AgentRun."
    - "`DispatchAttempt.dispatch_status == 'blocked'` with `rollback_hint` referencing the unsupported claim is the receiving doctrine surface (jointly with 211-02 `MarketingArtifact.artifact_status == 'blocked'`) that dissolves 209 §Downstream row 6 per 211-UI-SPEC §Translation Gate Dissolution and Opening."
    - "Loop dispatch mutations bind to 206 mutation-class doctrine via `external.send` (channel ∈ {`email`, `x`, `linkedin`, `sms`}) and dual-class `public.claim` when artifact targets a public-proof surface (per 211-UI-SPEC §Destructive Actions)."
  artifacts:
    - path: .planning/marketing-loop/dispatch-approval-policy.md
      provides: canonical approval-to-dispatch state machine and connector gate rules (cited by 211-UI-SPEC §Downstream rows 4 and 9 as future-surface authority)
  key_links:
    - from: lib/markos/mcp/tools/execution/schedule-post.cjs
      to: lib/markos/loop/dispatch-gates.ts
      via: the first dispatch proof should reuse the existing approval-token queue path
      pattern: "approval_token"
    - from: lib/markos/crm/agent-actions.ts
      to: lib/markos/loop/approval-linkage.ts
      via: approval package lineage should remain explicit from mutation request through outcome
      pattern: "buildApprovalPackage"
---

<ui_spec_fold>
ui_scope: NO_UI

ui_spec_block:
  - 211-UI-SPEC.md §Translation Gate Dissolution and Opening — 209 §Downstream row 6 (loop dispatch evidence gate `unsupported_blocked` state) dissolves jointly via 211-02 (`MarketingArtifact.artifact_status == 'blocked'` + `blocking_reasons.evidence_blocked`) AND this plan (`DispatchAttempt.dispatch_status == 'blocked'` + `rollback_hint` referencing the unsupported claim). Future pre-dispatch evidence-audit surfaces compose `<EvidenceSummary />` (209) over `MarketingArtifact.evidence_refs` + `ArtifactAudit.evidence_status`, AND record any operator override on `DispatchAttempt.rollback_hint` permanently.
  - 211-UI-SPEC.md §Translation Gate Dissolution and Opening — 210 §Surface A onboarding `<PlaceholderBanner variant="future_phase_211">` for loop-dispatch-bound connector recommendations dissolves once this plan ships. The `dispatch-approval-policy.md` requires connector state for the four channels `email`, `x`, `linkedin`, `sms`; once shipped, P210 onboarding can present connector recommendations as runnable (gated by `DispatchAttempt.queue_status` readiness) instead of placeholder-only.
  - 211-UI-SPEC.md §Downstream UI Inheritance Map row 4 — DispatchAttempt state board (future P208 admin extension or P210 connector recovery extension). Renders 14-field envelope verbatim. Two `approval_mode` literals (`human_approved`, `earned_autonomy`) → `.c-badge--{success,info}`. Five `dispatch_status` literals (`blocked`, `queued`, `dispatched`, `failed`, `retry_pending`) → `.c-badge--{error,info,success,error,warning}`. Four channel literals (`email`, `x`, `linkedin`, `sms`) → channel-icon chips. D-14 vanilla-table recipe required.
  - 211-UI-SPEC.md §Downstream UI Inheritance Map row 9 — Pre-dispatch approval-token viewer loop variant (future P208 Approval Inbox extension). Composes `MarketingArtifact.draft_body` preview + `ArtifactAudit.recommended_edits` + `EvidenceMap` summary (via 209 `<EvidenceSummary />`) + `DispatchAttempt.channel` + `DispatchAttempt.connector_install_id` health (via 210 connector substrate). Mutation-class binding per 206-02: `external.send` for `email`/`x`/`linkedin`/`sms`; ALSO `public.claim` (dual-class) when artifact targets a public-proof surface. `.c-modal` + `.c-button--destructive` confirm gate when default-approval-mode is `dual_approval`.
  - 211-UI-SPEC.md §Destructive Actions — All `DispatchAttempt` queue/dispatch mutations (this plan's API endpoint `POST /api/v1/marketing/dispatch`) emit JSON envelopes only and bind to 206 mutation-class doctrine. Confirm-modal authoring is downstream UI consumers' responsibility, NOT this plan's.

cross_cutting_carry_forward:
  - "Banned lexicon (CLAUDE.md): zero matches in `.planning/marketing-loop/dispatch-approval-policy.md`. No exclamation points in doctrine prose. Verified by 211-01-00 architecture-lock CI script."
  - "Pricing placeholder rule: not directly applicable to DispatchAttempt fields, BUT inherited via `MarketingArtifact.pricing_context_ref` foreign-key linkage. A dispatch attempt for an artifact with `pricing_requirement == 'approved_pricing'` AND placeholder `pricing_context_ref` MUST fail closed with `dispatch_status == 'blocked'` (per 211-UI-SPEC §Copywriting Contract pricing-placeholder rule)."
  - "Architecture-lock forbidden strings: zero matches against the four 211-doctrine forbidden strings (`silent auto-publish`, `dispatch without approval`, `hard-coded public price`, `unsupported claim publish`) and four test-substrate-mismatch strings across this plan's doctrine, runtime, and test files. The `dispatch without approval` string is especially load-bearing for this plan because LOOP-04 is the explicit non-negotiable that earns the architecture-lock entry. Enforced by `scripts/marketing-loop/check-loop-architecture-lock.mjs`."
  - "Mutation-class doctrine (206-02): `DispatchAttempt` queue/dispatch actions bind to `external.send` mutation-class. When `MarketingArtifact.artifact_family` targets a public-proof surface, dispatch is dual-class `external.send` + `public.claim`. The stricter approval-mode applies (typically `dual_approval` from `public.claim`)."
  - "Node API error envelopes (per 211-UI-SPEC §Copywriting Contract): `error_code` literals for this plan include `dispatch-approval-required`, `dispatch-connector-degraded`, `dispatch-channel-not-supported`. `error_message` is engineering-grade English with no banned lexicon (consumers may render verbatim)."

downstream_ui_consumers_read_only:
  - "P208 admin extension or P210 connector recovery extension — DispatchAttempt state board (211-UI-SPEC §Downstream row 4). Reads 14-field `DispatchAttempt` envelope. Cross-binding: 207 `RunApiEnvelope` via `agent_run_id`, 207 `ApprovalHandoffRecord` via `approval_package_id`, 210 connector substrate via `connector_install_id`. NOT modified by this plan."
  - "P208 Approval Inbox extension — Pre-dispatch approval-token viewer loop variant (211-UI-SPEC §Downstream row 9). Reads `DispatchAttempt` + `MarketingArtifact` + `ArtifactAudit` + `EvidenceMap` (209) + connector substrate (210). NOT modified by this plan."
  - "P210 onboarding (`<PlaceholderBanner variant=\"future_phase_211\">`) — once this plan ships, P210 onboarding can dissolve the placeholder and present loop-dispatch-bound connector recommendations as runnable. P210 will be redeployed; NOT modified by this plan."
</ui_spec_fold>

<objective>
Prove one safe path from approved artifact to queued or executed dispatch by reusing the existing approval-token queue substrate and making connector readiness explicit.
</objective>

<context>
@.planning/phases/211-content-social-revenue-loop/211-CONTEXT.md
@.planning/phases/211-content-social-revenue-loop/211-RESEARCH.md
@.planning/phases/211-content-social-revenue-loop/211-VALIDATION.md
@.planning/phases/211-content-social-revenue-loop/211-UI-SPEC.md
</context>

<tasks>
<task id="211-03-01">
  <name>DispatchAttempt state machine and approval linkage</name>
  <files>.planning/marketing-loop/dispatch-approval-policy.md, supabase/migrations/marketing_dispatch_attempts.sql, lib/markos/loop/dispatch-attempt.ts, lib/markos/loop/dispatch-gates.ts, lib/markos/loop/approval-linkage.ts, api/v1/marketing/dispatch.js, test/marketing/phase-211/domain-3/dispatch-state-machine.test.js</files>
  <read_first>
    - .planning/phases/211-content-social-revenue-loop/211-RESEARCH.md section "Domain 3 - Approval-to-dispatch path"
    - .planning/phases/211-content-social-revenue-loop/211-UI-SPEC.md §Translation Gate Dissolution and Opening (this plan jointly dissolves 209 §Downstream row 6 with 211-02; this plan dissolves 210 future_phase_211 placeholder)
    - .planning/phases/211-content-social-revenue-loop/211-UI-SPEC.md §Downstream UI Inheritance Map rows 4 and 9
    - .planning/phases/211-content-social-revenue-loop/211-UI-SPEC.md §Destructive Actions (mutation-class binding for dispatch)
    - lib/markos/mcp/tools/execution/schedule-post.cjs
    - lib/markos/crm/agent-actions.ts
    - .planning/phases/210-connector-wow-loop-and-recovery/210-RESEARCH.md
    - .planning/phases/206-soc2-type1-foundation/206-UI-SPEC.md (mutation-class doctrine for `external.send` and `public.claim`)
  </read_first>
  <action>
    Step 1: Create `.planning/marketing-loop/dispatch-approval-policy.md` with the exact `DispatchAttempt` fields `dispatch_attempt_id`, `tenant_id`, `artifact_id`, `channel`, `connector_install_id`, `approval_package_id`, `approval_mode`, `provider_receipt_ref`, `queue_status`, `dispatch_status`, `rollback_hint`, `agent_run_id`, `created_at`, and `updated_at`.
    Step 2: Constrain `approval_mode` to `human_approved | earned_autonomy` and `dispatch_status` to `blocked | queued | dispatched | failed | retry_pending`.
    Step 3: Make the policy explicitly reuse `schedule_post` and require connector state for the exact channels `email`, `x`, `linkedin`, and `sms`. Document the 206 mutation-class binding: `external.send` for these four channels; dual-class `external.send` + `public.claim` when `MarketingArtifact.artifact_family` targets a public-proof surface (per 211-UI-SPEC §Destructive Actions).
    Step 4: Add tests that cover approved, rejected, connector-blocked, queued, provider-failure, and retry-pending paths. The test asserts that `dispatch_status == 'blocked'` with `rollback_hint` referencing the unsupported claim is the doctrine state that dissolves 209 §Downstream row 6 jointly with 211-02 (per 211-UI-SPEC §Translation Gate Dissolution and Opening).
  </action>
  <acceptance_criteria>
    - `.planning/marketing-loop/dispatch-approval-policy.md` contains the exact field names `approval_package_id`, `provider_receipt_ref`, `dispatch_status`, and `rollback_hint`
    - `lib/markos/loop/dispatch-attempt.ts` contains the exact literals `human_approved`, `earned_autonomy`, `blocked`, `queued`, `dispatched`, `failed`, and `retry_pending`
    - `lib/markos/loop/dispatch-gates.ts` contains the exact strings `email`, `x`, `linkedin`, and `sms`
    - `test/marketing/phase-211/domain-3/dispatch-state-machine.test.js` contains the exact string `schedule_post`
    - `.planning/marketing-loop/dispatch-approval-policy.md` cites the 206 mutation-class literals `external.send` and `public.claim` verbatim (per 211-UI-SPEC §Destructive Actions cross-phase binding)
    - `.planning/marketing-loop/dispatch-approval-policy.md` contains zero matches against the CLAUDE.md banned lexicon and zero matches against the four architecture-lock forbidden strings (per 211-UI-SPEC §DESIGN.md Compliance Assertions)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/marketing/phase-211/domain-3/</automated>
  </verify>
  <done>Phase 211 now has an honest approval-to-dispatch state machine that fails closed on missing approvals or degraded connectors.</done>
</task>
</tasks>

<verification>
- npm test -- test/marketing/phase-211/domain-3/
</verification>

<success_criteria>
- DispatchAttempt has explicit approval and connector fields
- External dispatch cannot bypass approval posture
- Queue, receipt, and failure states remain linked to artifact and run lineage
- 209 §Downstream row 6 (loop dispatch evidence gate) is jointly dissolved with 211-02 via `dispatch_status == 'blocked'` + `rollback_hint`
- 210 §Surface A `future_phase_211` PlaceholderBanner becomes dissolvable once this plan ships
- 211-UI-SPEC §Downstream rows 4 and 9 inherit the `DispatchAttempt` field set + state-literal vocabulary verbatim once their consuming surfaces ship
</success_criteria>
