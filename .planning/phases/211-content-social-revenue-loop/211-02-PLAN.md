---
phase: 211
plan: "02"
type: execute
wave: 2
depends_on:
  - "211-01"
f_ids: []
migration_slots: []
integrates_with:
  - "PRC-01..09 from P205 (pricing-sensitive artifacts must consume pricing context or explicit placeholder posture)"
  - "COMP-01 from P206 (compliance-aware audit posture must exist before approval readiness is granted)"
  - "TASK-01..05 from P208 (blocked artifacts should route into real approval and task surfaces)"
  - "EVD-01..06 from P209 (claim and source-quality rules must drive evidence blocking states)"
translation_gate_for: []
ui_scope: NO_UI
ui_spec_block:
  - "211-UI-SPEC.md §Translation Gate Dissolution and Opening — `MarketingArtifact.artifact_status == 'blocked'` with `blocking_reasons` containing `evidence_blocked` dissolves 209 §Downstream row 6 (loop dispatch evidence gate)"
  - "211-UI-SPEC.md §Downstream UI Inheritance Map row 2 (MarketingArtifact preview / diff viewer) — five `artifact_status` literals + four `blocking_reasons` literals rendered as `.c-badge` chips"
  - "211-UI-SPEC.md §Downstream UI Inheritance Map row 13 (Artifact audit tribunal) — six audit-status fields rendered as per-dimension `.c-card` panels"
files_modified:
  - .planning/marketing-loop/artifact-audit-contract.md
  - supabase/migrations/marketing_artifacts_and_audits.sql
  - lib/markos/loop/draft-pipeline.ts
  - lib/markos/loop/artifact-audit.ts
  - lib/markos/loop/evidence-gates.ts
  - api/v1/marketing/artifacts.js
  - test/marketing/phase-211/domain-2/artifact-audit.test.js
  - test/marketing/phase-211/domain-2/channel-native-draft.test.js
autonomous: true
requirements:
  - LOOP-03
  - QA-04
  - QA-05
  - QA-06
must_haves:
  truths:
    - "Drafts become first-class artifacts with auditable state instead of transient copy blobs."
    - "Evidence, pricing, compliance, and channel-fit blockers are explicit and testable."
    - "Approval readiness is earned only after audit gates pass."
    - "`MarketingArtifact.artifact_status == 'blocked'` with `blocking_reasons` containing `evidence_blocked` is the receiving doctrine surface that dissolves 209 §Downstream row 6 (loop dispatch evidence gate) per 211-UI-SPEC §Translation Gate Dissolution and Opening."
    - "`MarketingArtifact.pricing_context_ref` accepts the `{{MARKOS_PRICING_ENGINE_PENDING}}` placeholder verbatim (per 211-UI-SPEC §Copywriting Contract pricing-placeholder rule + CLAUDE.md Pricing Engine Canon)."
  artifacts:
    - path: .planning/marketing-loop/artifact-audit-contract.md
      provides: canonical MarketingArtifact and ArtifactAudit shapes plus blocking vocabulary (cited by 211-UI-SPEC §Downstream rows 2 and 13 as future-surface authority)
  key_links:
    - from: lib/markos/mcp/tools/marketing/expand-claim-evidence.cjs
      to: lib/markos/loop/artifact-audit.ts
      via: strengthening variants should feed recommended edits instead of disconnected copy suggestions
      pattern: "strengthening_variants"
    - from: lib/markos/mcp/tools/marketing/audit-claim.cjs
      to: lib/markos/loop/evidence-gates.ts
      via: artifact audits should reuse tenant-canon support posture
      pattern: "confidence"
---

<ui_spec_fold>
ui_scope: NO_UI

ui_spec_block:
  - 211-UI-SPEC.md §Translation Gate Dissolution and Opening — 209 §Downstream row 6 (loop dispatch evidence gate `unsupported_blocked` state) dissolves into `MarketingArtifact.artifact_status == 'blocked'` with `blocking_reasons` containing `evidence_blocked`. Override path inherits 209-04 doctrine (`override_path == 'denied'` for autonomy ceiling; `.c-button--destructive` "Continue without evidence" otherwise). Override is recorded permanently on the linked `DispatchAttempt.rollback_hint` field (which 211-03 will define).
  - 211-UI-SPEC.md §Downstream UI Inheritance Map row 2 — MarketingArtifact preview / diff viewer (future P208 admin extension or P217 content authoring). Renders the five `artifact_status` literals (`draft`, `audited`, `blocked`, `approval_ready`, `rejected`) as `.c-badge--{info,warning,error,success,error}` chips. Renders the four `blocking_reasons` literals (`evidence_blocked`, `pricing_blocked`, `compliance_blocked`, `channel_blocked`) as blocker chips. `evidence_refs` chips compose `.c-chip-protocol` (mint-as-text per D-09) deep-linked to 209 `<EvidenceSummary />`.
  - 211-UI-SPEC.md §Downstream UI Inheritance Map row 13 — Artifact audit tribunal (future P208 Approval Inbox extension or P217 content review). Renders every audit-status field as a per-dimension `.c-card` panel: `voice_status`, `claim_status`, `compliance_status`, `channel_fit_status`, `pricing_status`, `evidence_status`. `recommended_edits` array renders as a vanilla `<ul>` with token-only `<li>` recipe per D-14.
  - 211-UI-SPEC.md §DESIGN.md Compliance Assertions — Pricing placeholder rule row (this plan's migration `marketing_artifacts_and_audits.sql` accepts `{{MARKOS_PRICING_ENGINE_PENDING}}` on `MarketingArtifact.pricing_context_ref`).

cross_cutting_carry_forward:
  - "Banned lexicon (CLAUDE.md): zero matches in `.planning/marketing-loop/artifact-audit-contract.md`. No exclamation points in doctrine prose. Verified by 211-01-00 architecture-lock CI script."
  - "Pricing placeholder rule (CLAUDE.md Pricing Engine Canon): `MarketingArtifact.pricing_context_ref` (this plan's migration `marketing_artifacts_and_audits.sql`) accepts the literal `{{MARKOS_PRICING_ENGINE_PENDING}}` verbatim. The `pricing_blocked` blocking reason is raised when `pricing_requirement == 'approved_pricing'` AND `pricing_context_ref` does not point to a Phase-205-approved PricingRecommendation (placeholder is acceptable only when `pricing_requirement == 'placeholder_allowed'`)."
  - "Architecture-lock forbidden strings: zero matches against the four 211-doctrine forbidden strings (`silent auto-publish`, `dispatch without approval`, `hard-coded public price`, `unsupported claim publish`) and four test-substrate-mismatch strings (`vitest`, `playwright`, `.test.ts`, `route.ts`) across this plan's doctrine, runtime, and test files. Enforced by `scripts/marketing-loop/check-loop-architecture-lock.mjs` (211-01-00)."
  - "Engineering-readability prose: short sentences, concrete contract field names. No marketing voice. Each `blocking_reasons` literal asserts a specific failure mode."

downstream_ui_consumers_read_only:
  - "P208 admin extension or P217 content authoring — MarketingArtifact preview / diff viewer (211-UI-SPEC §Downstream row 2). Reads `MarketingArtifact` 12-field envelope + `ArtifactAudit` field set. Renders the five artifact_status literals as `.c-badge--{info,warning,error,success,error}` chips. NOT modified by this plan."
  - "P208 Approval Inbox extension or P217 content review — Artifact audit tribunal (211-UI-SPEC §Downstream row 13). Reads `ArtifactAudit` field set + `recommended_edits` array. NOT modified by this plan."
  - "P209 evidence consumers (existing `<EvidenceSummary />` sub-component) — read-only consumer of `MarketingArtifact.evidence_refs[]`. Phase 211 does not modify any P209 surface; the 209 evidence summary becomes the read substrate for 211 audit-tribunal rows once those tribunals ship."
</ui_spec_fold>

<objective>
Define channel-native artifact and audit records so Phase 211 can block unsafe drafts before they ever reach approval or dispatch.
</objective>

<context>
@.planning/phases/211-content-social-revenue-loop/211-CONTEXT.md
@.planning/phases/211-content-social-revenue-loop/211-RESEARCH.md
@.planning/phases/211-content-social-revenue-loop/211-VALIDATION.md
@.planning/phases/211-content-social-revenue-loop/211-UI-SPEC.md
</context>

<tasks>
<task id="211-02-01">
  <name>MarketingArtifact and ArtifactAudit pipeline</name>
  <files>.planning/marketing-loop/artifact-audit-contract.md, supabase/migrations/marketing_artifacts_and_audits.sql, lib/markos/loop/draft-pipeline.ts, lib/markos/loop/artifact-audit.ts, lib/markos/loop/evidence-gates.ts, api/v1/marketing/artifacts.js, test/marketing/phase-211/domain-2/artifact-audit.test.js, test/marketing/phase-211/domain-2/channel-native-draft.test.js</files>
  <read_first>
    - .planning/phases/211-content-social-revenue-loop/211-RESEARCH.md section "Domain 2 - Draft generation and audit pipeline"
    - .planning/phases/211-content-social-revenue-loop/211-UI-SPEC.md §Translation Gate Dissolution and Opening (this plan dissolves 209 §Downstream row 6)
    - .planning/phases/211-content-social-revenue-loop/211-UI-SPEC.md §Downstream UI Inheritance Map rows 2 and 13
    - lib/markos/mcp/tools/marketing/audit-claim.cjs
    - lib/markos/mcp/tools/marketing/expand-claim-evidence.cjs
    - .planning/phases/209-evidence-research-and-claim-safety/209-RESEARCH.md
  </read_first>
  <action>
    Step 1: Create `.planning/marketing-loop/artifact-audit-contract.md` with the exact `MarketingArtifact` fields `artifact_id`, `brief_id`, `artifact_family`, `channel`, `draft_body`, `variant_count`, `evidence_refs`, `pricing_context_ref`, `approval_status`, `artifact_status`, `created_at`, and `updated_at`.
    Step 2: In the same contract doc and code, add the exact `ArtifactAudit` fields `audit_id`, `artifact_id`, `voice_status`, `claim_status`, `compliance_status`, `channel_fit_status`, `pricing_status`, `evidence_status`, `blocking_reasons`, `recommended_edits`, and `created_at`.
    Step 3: Constrain `artifact_status` to the exact literals `draft | audited | blocked | approval_ready | rejected`. The migration `marketing_artifacts_and_audits.sql` defines `pricing_context_ref` as JSONB accepting the literal placeholder string `{{MARKOS_PRICING_ENGINE_PENDING}}` verbatim.
    Step 4: Use the exact blocking reasons `evidence_blocked`, `pricing_blocked`, `compliance_blocked`, and `channel_blocked`, and add tests that cover passing, pricing-blocked, evidence-blocked, and compliance-blocked cases. The `evidence_blocked` literal is the doctrine surface that dissolves 209 §Downstream row 6 per 211-UI-SPEC §Translation Gate Dissolution and Opening.
  </action>
  <acceptance_criteria>
    - `.planning/marketing-loop/artifact-audit-contract.md` contains the exact field names `draft_body`, `evidence_refs`, `artifact_status`, `blocking_reasons`, and `recommended_edits`
    - `lib/markos/loop/artifact-audit.ts` contains the exact literals `draft`, `audited`, `blocked`, `approval_ready`, and `rejected`
    - `lib/markos/loop/evidence-gates.ts` contains the exact blocking strings `evidence_blocked`, `pricing_blocked`, `compliance_blocked`, and `channel_blocked`
    - `test/marketing/phase-211/domain-2/artifact-audit.test.js` contains the exact string `{{MARKOS_PRICING_ENGINE_PENDING}}`
    - `test/marketing/phase-211/domain-2/artifact-audit.test.js` asserts that `artifact_status == 'blocked'` with `blocking_reasons` containing `evidence_blocked` is the receiving state for 209 unsupported-claim dissolution (per 211-UI-SPEC §Translation Gate Dissolution and Opening)
    - `.planning/marketing-loop/artifact-audit-contract.md` contains zero matches against the CLAUDE.md banned lexicon (per 211-UI-SPEC §DESIGN.md Compliance Assertions row `Banned lexicon`)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/marketing/phase-211/domain-2/</automated>
  </verify>
  <done>Phase 211 drafts now pass through an explicit audit pipeline that makes pricing, evidence, and compliance blockers visible before approval.</done>
</task>
</tasks>

<verification>
- npm test -- test/marketing/phase-211/domain-2/
</verification>

<success_criteria>
- MarketingArtifact and ArtifactAudit have explicit fields and blocking vocabulary
- Channel-native drafts are auditable before approval
- Unsupported claims and pricing-sensitive drafts fail closed instead of drifting into dispatch
- 209 §Downstream row 6 (loop dispatch evidence gate) is implementable in a downstream UI surface via `MarketingArtifact.artifact_status == 'blocked'` + `blocking_reasons.evidence_blocked`
- 211-UI-SPEC §Downstream rows 2 and 13 inherit the `MarketingArtifact` + `ArtifactAudit` field sets verbatim once their consuming surfaces ship
</success_criteria>
