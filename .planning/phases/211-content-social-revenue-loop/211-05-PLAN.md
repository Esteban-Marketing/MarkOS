---
phase: 211
plan: "05"
type: execute
wave: 5
depends_on:
  - "211-01"
  - "211-02"
  - "211-03"
  - "211-04"
f_ids: []
migration_slots: []
integrates_with:
  - "RUN-01..08 from P207 (revenue feedback should remain tied to real run and side-effect lineage)"
  - "TASK-01..05 from P208 (weekly narrative and follow-up work consume the revenue-feedback outputs)"
  - "EVD-01..06 from P209 (revenue claims must remain evidence-linked and source-aware)"
translation_gate_for: []
ui_scope: NO_UI
ui_spec_block:
  - "211-UI-SPEC.md §Downstream UI Inheritance Map row 6 (Revenue feedback narrative consumer = 208-06 Weekly Narrative)"
  - "211-UI-SPEC.md §Translation Gate Dissolution and Opening — 208-06 Weekly Narrative `placeholder_sections` literal `phase_211_loop` dissolves once `RevenueFeedbackLink.narrative_ready` and `MeasurementHandoff.weekly_narrative_ref` are populated"
files_modified:
  - .planning/marketing-loop/revenue-feedback-model.md
  - .planning/marketing-loop/weekly-narrative-source-map.md
  - supabase/migrations/revenue_feedback_links.sql
  - lib/markos/loop/revenue-feedback.ts
  - lib/markos/loop/narrative-input.ts
  - api/v1/marketing/revenue-feedback.js
  - test/marketing/phase-211/domain-5/revenue-feedback.test.js
autonomous: true
requirements:
  - LOOP-06
  - LOOP-07
  - QA-13
  - QA-14
must_haves:
  truths:
    - "Revenue feedback reuses the existing weighted attribution substrate instead of inventing a disconnected model."
    - "Degraded attribution states stay explicit and block unsupported revenue claims."
    - "Weekly Narrative receives evidence-linked source inputs from the loop."
    - "`narrative_ready == true` on `RevenueFeedbackLink` is the receiving doctrine flag that allows 208-06 Weekly Narrative to dissolve its `phase_211_loop` placeholder per 211-UI-SPEC §Translation Gate Dissolution and Opening."
    - "Money rendering doctrine: `revenue_amount` + `currency` MUST be displayed via tabular-numerals JetBrains Mono per DESIGN.md typography rule when consumed by downstream UI surfaces (per 211-UI-SPEC §Downstream row 6)."
  artifacts:
    - path: .planning/marketing-loop/revenue-feedback-model.md
      provides: canonical artifact-to-revenue linkage and degraded-attribution vocabulary (cited by 211-UI-SPEC §Downstream row 6 as future-surface authority)
    - path: .planning/marketing-loop/weekly-narrative-source-map.md
      provides: source-record map from artifacts and revenue feedback into weekly narrative (cited by 211-UI-SPEC §Translation Gate Dissolution for 208-06 placeholder dissolution)
  key_links:
    - from: lib/markos/crm/attribution.ts
      to: lib/markos/loop/revenue-feedback.ts
      via: weighted touch evidence and degraded readiness reasons should be preserved
      pattern: "buildWeightedAttributionModel"
---

<ui_spec_fold>
ui_scope: NO_UI

ui_spec_block:
  - 211-UI-SPEC.md §Downstream UI Inheritance Map row 6 — Revenue feedback narrative consumer = 208-06 Weekly Narrative (existing surface; will be redeployed to dissolve `phase_211_loop` placeholder). 14-field `RevenueFeedbackLink` envelope (`feedback_id`, `tenant_id`, `artifact_id`, `campaign_ref`, `crm_record_kind`, `crm_record_id`, `revenue_amount`, `currency`, `attribution_status`, `weighted_evidence_refs`, `leading_indicator_kind`, `narrative_ready`, `created_at`, `updated_at`) renders as `.c-card` panels per artifact. Four `attribution_status` literals (`ready`, `degraded`, `missing_identity`, `missing_touches`) → `.c-badge--{success,warning,error,error}` mapping. Money rendering: `revenue_amount` + `currency` displayed via tabular-numerals JetBrains Mono per DESIGN.md typography rule. Cross-binding: existing `lib/markos/crm/attribution.ts` `buildWeightedAttributionModel` is the source-of-truth (this plan reuses, never replaces); 209 evidence binding via `weighted_evidence_refs[]`.
  - 211-UI-SPEC.md §Translation Gate Dissolution and Opening — 208-06 Weekly Narrative `placeholder_sections` literal `phase_211_loop` dissolves once this plan ships. `lib/markos/operator/weekly-narrative.ts` removes the `phase_211_loop` placeholder and reads from `MeasurementHandoff.weekly_narrative_ref` (211-06 ships this) + `RevenueFeedbackLink.narrative_ready == true` rows directly. The `narrative_ready` boolean on `RevenueFeedbackLink` is the source-of-truth field for narrative population.
  - 211-UI-SPEC.md §DESIGN.md Compliance Assertions — Money display posture (this plan owns `revenue_amount` + `currency` field shape; rendering is downstream phases' responsibility but tabular-numerals JetBrains Mono is the doctrine rule per DESIGN.md typography).

cross_cutting_carry_forward:
  - "Banned lexicon (CLAUDE.md): zero matches in `.planning/marketing-loop/revenue-feedback-model.md` and `.planning/marketing-loop/weekly-narrative-source-map.md`. No exclamation points. Verified by 211-01-00 architecture-lock CI script."
  - "Pricing placeholder rule: not directly applicable to RevenueFeedbackLink fields (revenue is observed, not priced). However, any revenue claim that asserts a price MUST inherit the artifact-pricing-context-ref rule from 211-02 via the linked `MarketingArtifact`."
  - "Architecture-lock forbidden strings: zero matches against the four 211-doctrine forbidden strings and four test-substrate-mismatch strings across this plan's doctrine, runtime, and test files. Enforced by `scripts/marketing-loop/check-loop-architecture-lock.mjs`."
  - "Mutation-class doctrine (206-02): CRM linkage mutations (this plan's `POST /api/v1/marketing/revenue-feedback`) bind to `data.export` only when the linkage exports outside the tenant boundary; otherwise they are read-only writes inside the tenant scope. CSV/PDF exports of revenue narrative bind to `data.export`."
  - "Node API error envelopes: `error_code` literals for this plan include `revenue-attribution-degraded`, `revenue-attribution-missing-identity`, `revenue-attribution-missing-touches`. `error_message` is engineering-grade English with no banned lexicon (consumers may render verbatim)."
  - "Money display doctrine (DESIGN.md typography): `revenue_amount` rendered with tabular-numerals JetBrains Mono on any downstream surface. This plan defines the data shape; rendering responsibility is downstream."

downstream_ui_consumers_read_only:
  - "P208 Weekly Narrative (208-06) — Revenue feedback narrative consumer (211-UI-SPEC §Downstream row 6). Reads `RevenueFeedbackLink.narrative_ready == true` rows + `MeasurementHandoff.weekly_narrative_ref` (211-06 ships). Renders `RevenueFeedbackLink` as `.c-card` panels with tabular-numerals JetBrains Mono money rendering. Once this plan + 211-06 ship, P208 will be redeployed to dissolve the `phase_211_loop` placeholder. NOT modified by this plan."
  - "Future P217+ marketing dashboards — extended revenue narrative consumers. Read `RevenueFeedbackLink` field set + `attribution_status` literals. NOT modified by this plan."
  - "P209 evidence consumers — `weighted_evidence_refs[]` foreign-key binding to 209 EvidenceMap. P209 evidence summary becomes the read substrate for revenue-claim verification. Phase 211 does NOT modify any P209 surface."
</ui_spec_fold>

<objective>
Tie content and social actions back to revenue or leading indicators using the existing attribution substrate, while keeping degraded states explicit and weekly narrative inputs grounded.
</objective>

<context>
@.planning/phases/211-content-social-revenue-loop/211-CONTEXT.md
@.planning/phases/211-content-social-revenue-loop/211-RESEARCH.md
@.planning/phases/211-content-social-revenue-loop/211-VALIDATION.md
@.planning/phases/211-content-social-revenue-loop/211-UI-SPEC.md
</context>

<tasks>
<task id="211-05-01">
  <name>Revenue feedback links and weekly narrative inputs</name>
  <files>.planning/marketing-loop/revenue-feedback-model.md, .planning/marketing-loop/weekly-narrative-source-map.md, supabase/migrations/revenue_feedback_links.sql, lib/markos/loop/revenue-feedback.ts, lib/markos/loop/narrative-input.ts, api/v1/marketing/revenue-feedback.js, test/marketing/phase-211/domain-5/revenue-feedback.test.js</files>
  <read_first>
    - .planning/phases/211-content-social-revenue-loop/211-RESEARCH.md section "Domain 5 - Revenue feedback and weekly narrative"
    - .planning/phases/211-content-social-revenue-loop/211-UI-SPEC.md §Downstream UI Inheritance Map row 6 (revenue feedback narrative consumer)
    - .planning/phases/211-content-social-revenue-loop/211-UI-SPEC.md §Translation Gate Dissolution and Opening (this plan dissolves 208-06 `phase_211_loop` placeholder jointly with 211-06)
    - lib/markos/crm/attribution.ts
    - .planning/phases/208-human-operating-interface/208-RESEARCH.md
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md (Surface F — Weekly Narrative; placeholder_sections)
  </read_first>
  <action>
    Step 1: Create `.planning/marketing-loop/revenue-feedback-model.md` with the exact `RevenueFeedbackLink` fields `feedback_id`, `tenant_id`, `artifact_id`, `campaign_ref`, `crm_record_kind`, `crm_record_id`, `revenue_amount`, `currency`, `attribution_status`, `weighted_evidence_refs`, `leading_indicator_kind`, `narrative_ready`, `created_at`, and `updated_at`.
    Step 2: Constrain `attribution_status` to the exact literals `ready | degraded | missing_identity | missing_touches`.
    Step 3: Create `.planning/marketing-loop/weekly-narrative-source-map.md` so the narrative layer names which loop records provide the source of truth. Document the dissolution rule: 208-06 `placeholder_sections` literal `phase_211_loop` becomes dissolvable once `narrative_ready == true` rows exist + `MeasurementHandoff.weekly_narrative_ref` (from 211-06) populates.
    Step 4: Add tests that cover direct, assisted, degraded, missing-identity, and missing-touches attribution paths and assert reuse of `buildWeightedAttributionModel`. Doctrine prose cites the DESIGN.md typography rule that downstream surfaces render `revenue_amount` + `currency` via tabular-numerals JetBrains Mono.
  </action>
  <acceptance_criteria>
    - `.planning/marketing-loop/revenue-feedback-model.md` contains the exact field names `revenue_amount`, `attribution_status`, `weighted_evidence_refs`, and `narrative_ready`
    - `lib/markos/loop/revenue-feedback.ts` contains the exact literals `ready`, `degraded`, `missing_identity`, and `missing_touches`
    - `lib/markos/loop/revenue-feedback.ts` contains the exact string `buildWeightedAttributionModel`
    - `test/marketing/phase-211/domain-5/revenue-feedback.test.js` contains the exact strings `missing_identity` and `missing_touches`
    - `.planning/marketing-loop/weekly-narrative-source-map.md` cites the 208-06 `placeholder_sections` literal `phase_211_loop` and declares the dissolution rule (per 211-UI-SPEC §Translation Gate Dissolution and Opening)
    - `.planning/marketing-loop/revenue-feedback-model.md` and `.planning/marketing-loop/weekly-narrative-source-map.md` contain zero matches against the CLAUDE.md banned lexicon and zero matches against the four architecture-lock forbidden strings
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/marketing/phase-211/domain-5/</automated>
  </verify>
  <done>Phase 211 now connects activity to revenue or leading indicators with explicit degraded states and evidence-linked weekly narrative inputs.</done>
</task>
</tasks>

<verification>
- npm test -- test/marketing/phase-211/domain-5/
</verification>

<success_criteria>
- RevenueFeedbackLink has explicit fields and degraded-attribution vocabulary
- Weekly Narrative receives grounded source inputs
- Revenue claims stay evidence-linked instead of optimistic or implied
- 208-06 Weekly Narrative `phase_211_loop` placeholder becomes dissolvable (jointly with 211-06)
- 211-UI-SPEC §Downstream row 6 inherits the `RevenueFeedbackLink` field set + `attribution_status` vocabulary verbatim once the consuming surface ships
</success_criteria>
