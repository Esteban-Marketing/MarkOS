---
phase: 211
plan: "04"
type: execute
wave: 4
depends_on:
  - "211-01"
  - "211-02"
  - "211-03"
f_ids: []
migration_slots: []
integrates_with:
  - "TASK-01..05 from P208 (social signals should create tasks or approval items instead of passive dashboards)"
  - "CONN-01..06 from P210 (signal sources and recovery posture inherit connector-health doctrine)"
translation_gate_for: []
ui_scope: NO_UI
ui_spec_block:
  - "211-UI-SPEC.md §Downstream UI Inheritance Map row 5 (Social signal inbox) — 14-field `SocialSignal` envelope rendered with five `route_kind` literals as `.c-badge` chips"
  - "211-UI-SPEC.md §Translation Gate Dissolution and Opening — 208-04 Approval Inbox handoff-kind filter dissolves placeholder for loop-originated approvals; `route_kind == 'approval_queue'` rows route into 208 ApprovalHandoffRecord with `handoff_kind == 'approval'`"
files_modified:
  - .planning/marketing-loop/social-signal-routing.md
  - supabase/migrations/social_signals.sql
  - lib/markos/loop/social-signal.ts
  - lib/markos/loop/social-routing.ts
  - lib/markos/loop/social-escalation.ts
  - api/v1/marketing/social-signals.js
  - test/marketing/phase-211/domain-4/social-routing.test.js
autonomous: true
requirements:
  - LOOP-05
  - QA-10
  - QA-11
  - QA-12
must_haves:
  truths:
    - "Social signals become actionable routed work rather than dashboards."
    - "Replies, DMs, and moderation actions are approval-gated by default."
    - "Revenue-relevant or urgent signals can attach to CRM or task surfaces."
    - "`route_kind == 'approval_queue'` is the receiving doctrine surface that binds to 208 Approval Inbox via 207 `ApprovalHandoffRecord.handoff_kind == 'approval'` (per 211-UI-SPEC §Downstream row 5)."
    - "Social reply/DM mutations bind to 206 `external.send` mutation-class doctrine; force-route or override actions bind to `connector.mutate` per 211-UI-SPEC §Destructive Actions."
  artifacts:
    - path: .planning/marketing-loop/social-signal-routing.md
      provides: canonical SocialSignal shape, route policy, and escalation defaults (cited by 211-UI-SPEC §Downstream row 5 as future-surface authority)
  key_links:
    - from: lib/markos/outbound/conversations.ts
      to: lib/markos/loop/social-routing.ts
      via: conversation-aware routing should reuse existing thread and provider-event substrate
      pattern: "thread_id"
    - from: lib/markos/crm/copilot.ts
      to: lib/markos/loop/social-escalation.ts
      via: social escalation should remain grounded in CRM context and missing-context visibility
      pattern: "missing_context"
---

<ui_spec_fold>
ui_scope: NO_UI

ui_spec_block:
  - 211-UI-SPEC.md §Downstream UI Inheritance Map row 5 — Social signal inbox (future P208 admin extension or P226 sentiment engine). Renders the 14-field `SocialSignal` envelope verbatim. Five `route_kind` literals (`read_only`, `task_only`, `approval_queue`, `crm_linked`, `spam`) → `.c-badge--{info,warning,error,success,error}`. `approval_needed == true` rows render `.c-notice c-notice--warning` "[warn] Approval required" inline. Sentiment/urgency/revenue_relevance render as paired chip group with token-only color mapping. CRM and task linkage compose `.c-chip-protocol` deep-link chips. D-14 vanilla-table recipe for the feed; per-signal detail composes `.c-card`.
  - 211-UI-SPEC.md §Translation Gate Dissolution and Opening — 208-04 Approval Inbox handoff-kind filter currently renders all 4 `handoff_kind` literals from 207, but loop-originated approvals (including `SocialSignal.route_kind == 'approval_queue'` items) are placeholders. Once this plan ships, approval inbox renders loop-originated approval items linked via `ApprovalHandoffRecord.task_ref`. Mutation-class binding: `external.send` for any reply-mutation row.
  - 211-UI-SPEC.md §Destructive Actions — All `SocialSignal` route-policy mutations (this plan's API endpoint `POST /api/v1/marketing/social-signals`) bind to 206 mutation-class doctrine. `external.send` for any reply or DM mutation. `connector.mutate` for force-route or cancel mid-flight actions. JSON envelopes only; confirm-modal authoring is downstream UI consumers' responsibility.

cross_cutting_carry_forward:
  - "Banned lexicon (CLAUDE.md): zero matches in `.planning/marketing-loop/social-signal-routing.md`. No exclamation points in doctrine prose. Verified by 211-01-00 architecture-lock CI script."
  - "Pricing placeholder rule: not directly applicable to SocialSignal fields (signals are inbound, not pricing-bearing). However, any reply mutation that asserts a price MUST inherit the artifact-pricing-context-ref rule from 211-02 via the linked `MarketingArtifact` (when the reply becomes a public claim)."
  - "Architecture-lock forbidden strings: zero matches against the four 211-doctrine forbidden strings and four test-substrate-mismatch strings across this plan's doctrine, runtime, and test files. Enforced by `scripts/marketing-loop/check-loop-architecture-lock.mjs`."
  - "Mutation-class doctrine (206-02): social reply / DM / moderation actions bind to `external.send`. Force-route or cancel mid-flight `SocialSignal` route mutations bind to `connector.mutate`. Approval-gated by default unless an explicit earned-autonomy policy exists (per 211-CONTEXT non-negotiables)."
  - "Node API error envelopes: `error_code` literals for this plan include `social-signal-route-required`. `error_message` is engineering-grade English with no banned lexicon (consumers may render verbatim)."

downstream_ui_consumers_read_only:
  - "P208 admin extension or P226 sentiment engine — Social signal inbox (211-UI-SPEC §Downstream row 5). Reads 14-field `SocialSignal` envelope. Cross-binding: 206 `external.send` for reply mutations, 208 `ApprovalHandoffRecord` for `approval_queue` route_kind, 222 CRM substrate for `crm_record_ref`. NOT modified by this plan."
  - "P208 Approval Inbox (208-04) — once this plan ships, approval inbox renders `route_kind == 'approval_queue'` items as loop-originated approvals via `ApprovalHandoffRecord.task_ref` linkage. P208 will be redeployed; NOT modified by this plan."
  - "P208 Task Board (208-03) — once this plan ships, task board renders `route_kind == 'task_only'` items as loop-originated tasks via `task_ref` linkage. P208 will be redeployed; NOT modified by this plan."
</ui_spec_fold>

<objective>
Define the social signal layer for the first operating loop so comments, mentions, DMs, and escalations create governed work instead of floating outside the system.
</objective>

<context>
@.planning/phases/211-content-social-revenue-loop/211-CONTEXT.md
@.planning/phases/211-content-social-revenue-loop/211-RESEARCH.md
@.planning/phases/211-content-social-revenue-loop/211-VALIDATION.md
@.planning/phases/211-content-social-revenue-loop/211-UI-SPEC.md
</context>

<tasks>
<task id="211-04-01">
  <name>SocialSignal schema and escalation routing</name>
  <files>.planning/marketing-loop/social-signal-routing.md, supabase/migrations/social_signals.sql, lib/markos/loop/social-signal.ts, lib/markos/loop/social-routing.ts, lib/markos/loop/social-escalation.ts, api/v1/marketing/social-signals.js, test/marketing/phase-211/domain-4/social-routing.test.js</files>
  <read_first>
    - .planning/phases/211-content-social-revenue-loop/211-RESEARCH.md section "Domain 4 - Social signals and escalation"
    - .planning/phases/211-content-social-revenue-loop/211-UI-SPEC.md §Downstream UI Inheritance Map row 5 (social signal inbox)
    - .planning/phases/211-content-social-revenue-loop/211-UI-SPEC.md §Destructive Actions (mutation-class binding for social mutations)
    - lib/markos/outbound/conversations.ts
    - lib/markos/crm/copilot.ts
    - .planning/phases/208-human-operating-interface/208-RESEARCH.md
    - .planning/phases/208-human-operating-interface/208-UI-SPEC.md (Surface D — Approval Inbox; handoff-kind filter)
  </read_first>
  <action>
    Step 1: Create `.planning/marketing-loop/social-signal-routing.md` with the exact `SocialSignal` fields `social_signal_id`, `tenant_id`, `source_platform`, `signal_type`, `signal_text`, `sentiment`, `urgency`, `revenue_relevance`, `approval_needed`, `route_kind`, `crm_record_ref`, `task_ref`, `created_at`, and `updated_at`.
    Step 2: Constrain `route_kind` to the exact literals `read_only | task_only | approval_queue | crm_linked | spam`.
    Step 3: Make the policy define which signal types are read-only, task-only, or approval-gated by default. Document the 206 mutation-class binding: `external.send` for any reply or DM; `connector.mutate` for force-route or cancel actions (per 211-UI-SPEC §Destructive Actions).
    Step 4: Add tests that cover positive, negative, urgent, sales, support, and spam signals plus at least one revenue-relevant escalation.
  </action>
  <acceptance_criteria>
    - `.planning/marketing-loop/social-signal-routing.md` contains the exact field names `signal_type`, `urgency`, `revenue_relevance`, `approval_needed`, and `route_kind`
    - `lib/markos/loop/social-signal.ts` contains the exact literals `read_only`, `task_only`, `approval_queue`, `crm_linked`, and `spam`
    - `lib/markos/loop/social-routing.ts` contains the exact string `approval_queue`
    - `test/marketing/phase-211/domain-4/social-routing.test.js` contains the exact strings `urgent` and `spam`
    - `.planning/marketing-loop/social-signal-routing.md` cites the 206 mutation-class literal `external.send` verbatim for reply/DM mutations (per 211-UI-SPEC §Destructive Actions cross-phase binding)
    - `.planning/marketing-loop/social-signal-routing.md` contains zero matches against the CLAUDE.md banned lexicon and zero matches against the four architecture-lock forbidden strings (per 211-UI-SPEC §DESIGN.md Compliance Assertions)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/marketing/phase-211/domain-4/</automated>
  </verify>
  <done>Social activity now feeds the operating loop as governed work with clear routing and escalation posture.</done>
</task>
</tasks>

<verification>
- npm test -- test/marketing/phase-211/domain-4/
</verification>

<success_criteria>
- SocialSignal has explicit fields and route kinds
- Urgent or revenue-relevant social activity creates actionable work
- Social mutations remain approval-gated by default
- 211-UI-SPEC §Downstream row 5 inherits the `SocialSignal` field set + `route_kind` vocabulary verbatim once the consuming surface ships
- 208-04 Approval Inbox can dissolve loop-originated approval placeholders via `route_kind == 'approval_queue'` linkage to `ApprovalHandoffRecord`
</success_criteria>
