---
phase: 227-ecosystem-partner-community-developer-growth
plan: 05
type: execute
wave: 3
depends_on: ["227-01"]
files_modified:
  - contracts/F-194-ecosystem-community-webhook-v1.json
  - contracts/F-195-ecosystem-signal-route-v1.json
  - supabase/migrations/166_p221_consent_state_community_signal_processing.sql
  - lib/markos/ecosystem/community/signal-service.ts
  - lib/markos/ecosystem/community/signal-routing.ts
  - lib/markos/ecosystem/community/consent-gate.ts
  - lib/markos/ecosystem/community/poll-fallback.ts
  - lib/markos/ecosystem/community/adapters/slack-webhook.ts
  - lib/markos/ecosystem/community/adapters/discord-webhook.ts
  - lib/markos/ecosystem/community/adapters/forum-webhook.ts
  - lib/markos/ecosystem/community/adapters/github-webhook.ts
  - lib/markos/ecosystem/community/adapters/reddit-webhook.ts
  - lib/markos/ecosystem/community/adapters/twitter-webhook.ts
  - lib/markos/ecosystem/community/adapters/signature-verify.ts
  - lib/markos/ecosystem/developer/developer-event-service.ts
  - api/webhooks/community-slack.js
  - api/webhooks/community-discord.js
  - api/webhooks/community-forum.js
  - api/webhooks/community-github.js
  - api/webhooks/community-reddit.js
  - api/webhooks/community-twitter.js
  - test/ecosystem/community/webhook-signature-hmac.test.ts
  - test/ecosystem/community/webhook-signature-discord-ed25519.test.ts
  - test/ecosystem/community/signal-dedupe.test.ts
  - test/ecosystem/community/consent-gate.test.ts
  - test/ecosystem/community/signal-routing.test.ts
  - test/ecosystem/community/learning-candidate-emit.test.ts
  - test/ecosystem/community/poll-fallback.test.ts
  - test/ecosystem/developer/developer-event-fanout.test.ts
autonomous: true
requirements: [ECO-03, SG-06, SG-09, SG-12, QA-01, QA-02, QA-03, QA-04, QA-05, QA-06, QA-07, QA-08, QA-09, QA-10, QA-11, QA-12, QA-13, QA-14, QA-15]
must_haves:
  truths:
    - "Migration 166: P221 cdp_consent_states extended with community_signal_processing field (DEFAULT 'unknown')"
    - "5 sources (Slack, Forum, GitHub, Reddit, Twitter) verify HMAC-SHA256 signature; Discord verifies Ed25519 (Open Question 1)"
    - "Webhook with invalid/missing signature → 401 + markos_audit_log row; per-tenant key absent → 401 (no shared fallback per A24)"
    - "community_signals dedupe via UNIQUE (source, source_message_id) — replay safe"
    - "ConsentState gate: opted_out → write row with consent_state_check_passed=false but DO NOT route"
    - "Signal routing via P225 decision_rules (trigger_kind='community_signal_received') → 6 action_kinds"
    - "DeveloperEvent ingest emits cdp_events with shared source_event_ref UUID (single fan-out, fail-closed transaction)"
    - "Volume spike >2σ from 7-day baseline → operator volume alert task instead of per-signal routing (Pitfall 7)"
  artifacts:
    - path: "supabase/migrations/166_p221_consent_state_community_signal_processing.sql"
      provides: "ALTER TABLE cdp_consent_states ADD community_signal_processing"
      contains: "community_signal_processing"
    - path: "lib/markos/ecosystem/community/signal-service.ts"
      provides: "community_signals upsert with dedupe + ConsentState gate"
      exports: ["upsertCommunitySignal", "scrubSignalForTombstone"]
    - path: "lib/markos/ecosystem/community/signal-routing.ts"
      provides: "Routes signals to decision_rules engine + 6 action_kinds + spike debounce"
      exports: ["routeSignal", "ROUTED_ACTION_KINDS", "shouldDebounceForSpike"]
    - path: "lib/markos/ecosystem/community/consent-gate.ts"
      provides: "ConsentState check for community_signal_processing"
      exports: ["checkCommunitySignalConsent", "COMMUNITY_CONSENT_STATES"]
    - path: "lib/markos/ecosystem/community/adapters/signature-verify.ts"
      provides: "HMAC-SHA256 + Ed25519 (Discord) signature verification"
      exports: ["verifyHmacSignature", "verifyEd25519Signature", "constantTimeCompare"]
    - path: "lib/markos/ecosystem/community/poll-fallback.ts"
      provides: "Polling fallback every 15min when webhook unreachable >1h"
      exports: ["pollFallbackCron", "checkWebhookHealth"]
    - path: "lib/markos/ecosystem/developer/developer-event-service.ts"
      provides: "developer_events ingest + cdp_events fan-out (single transaction)"
      exports: ["ingestDeveloperEvent", "DEVELOPER_EVENT_KINDS"]
  key_links:
    - from: "api/webhooks/community-{source}.js (6 endpoints)"
      to: "lib/markos/ecosystem/community/adapters/signature-verify.ts"
      via: "Per-tenant Edge Config key lookup → HMAC-SHA256 verify (5 sources) OR Ed25519 (Discord); failure → 401 + audit"
      pattern: "verifyHmacSignature\\|verifyEd25519Signature"
    - from: "lib/markos/ecosystem/community/signal-service.ts"
      to: "supabase community_signals UPSERT"
      via: "ON CONFLICT (source, source_message_id) DO NOTHING"
      pattern: "onConflict.*source.*source_message_id"
    - from: "lib/markos/ecosystem/community/signal-routing.ts"
      to: "P225 decision_rules engine"
      via: "trigger_kind='community_signal_received'"
      pattern: "community_signal_received"
    - from: "lib/markos/ecosystem/community/signal-routing.ts"
      to: "P212 LearningCandidate"
      via: "action_kind='create_learning_candidate' → P212 emit"
      pattern: "create_learning_candidate"
    - from: "lib/markos/ecosystem/developer/developer-event-service.ts"
      to: "cdp_events"
      via: "Single transaction fan-out (developer_events + cdp_events) — fail-closed"
      pattern: "source_event_ref"
---

<objective>
Build CommunitySignal webhook ingestion (6 sources) + ConsentState extension + decision_rules routing + DeveloperEvent ingestion + cdp_events fan-out (227-RESEARCH.md slice 227-05). Wave 3 — parallel with 227-04.

Purpose: This slice owns ECO-03 (community + developer signals feed CRM/analytics/launches/learning) + SG-06 (community/event/PR/partnership/devrel motions as governed workflows). Critical security: every webhook signature verified (HMAC-SHA256 for 5 sources, Ed25519 for Discord per Open Question 1). Critical privacy: ConsentState gate per profile; tombstone scrub on related_profile_id. Critical correctness: ON CONFLICT dedupe via UNIQUE (source, source_message_id); fail-closed cdp_events fan-out for developer events.

Output: 2 contracts (F-194/F-195), 1 migration (166), 14 service/adapter modules, 6 webhook API endpoints, 8 vitest test files.
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-CONTEXT.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-RESEARCH.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-VALIDATION.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-01-PLAN.md
@.planning/phases/221-cdp-identity-audience-consent-substrate/221-CONTEXT.md
@.planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md
@.planning/phases/225-analytics-attribution-narrative-intelligence/225-CONTEXT.md
@.planning/phases/212-learning-literacy-evolution/212-CONTEXT.md
@CLAUDE.md

# Incoming docs (CLAUDE.md precedence rule 5): obsidian/work/incoming/{18,22,25}-*.md INFORMATIONAL ONLY.

<interfaces>
From 227-01 (lib/markos/ecosystem/contracts.ts):
```typescript
export const COMMUNITY_SOURCE = ['slack','discord','forum','github','reddit','twitter','community_event','in_app'] as const;
export const COMMUNITY_SENTIMENT = ['positive','neutral','negative'] as const;
export const COMMUNITY_URGENCY = ['low','medium','high','critical'] as const;
export const ROUTED_ACTION_KIND = ['created_task','created_learning_candidate','created_pr_followup','dismissed','escalated','generate_narrative'] as const;
export const DEVELOPER_EVENT_KIND = ['sdk_download','api_first_call','mcp_tool_invocation','plugin_install','docs_view','hackathon_signup','certification_attempt'] as const;
```

From P221 (A3 — must exist for ALTER TABLE):
```sql
-- cdp_consent_states (existing P221 columns: profile_id, email_marketing, sms_marketing, etc.)
-- ALTER ADDS: community_signal_processing text NOT NULL DEFAULT 'unknown' CHECK IN ('opted_in','opted_out','unknown')
```

From P221 (existing get function, extended this slice):
```typescript
// lib/markos/cdp/adapters/crm-projection.ts (P221 existing)
export function getConsentState(profile_id: string): Promise<ConsentState>;
// Extended return shape now includes community_signal_processing
```

From P225 decision_rules engine (A22 soft ref):
```typescript
// lib/markos/analytics/decision/evaluators/* (P225 module)
// Pattern: pluggable evaluator per trigger_kind
// Trigger 'community_signal_received' → action_kind ∈ {create_task, emit_alert, require_approval, generate_narrative, create_learning_candidate, create_pricing_signal, suppress_send}
// 227-05 ADD: also route to 'created_pr_followup' (CONTEXT D-28); maps to action_kind='create_task' with subject='pr_followup'
```

From P212 LearningCandidate (A?):
```typescript
// lib/markos/learning/literacy.ts
export function emitLearningCandidate(input: { source_signal_id, summary, evidence_refs[] }): Promise<string>;
```

From cdp_events (P221):
```sql
-- INSERT INTO cdp_events (event_id, tenant_id, event_name, event_domain, occurred_at, profile_id?, properties, source_event_ref)
-- For developer_events: event_domain='partner', source_event_ref = developer_event.event_id (shared UUID per D-06)
```

From migration 163 (227-01) community_signals + developer_events tables.

From Vercel Edge Config (A15 — fallback Supabase Vault if absent):
```typescript
// Per-tenant per-source HMAC key namespace
// Key naming: webhook_signing_key_{source}_{tenant_id} — 5 HMAC sources
// Discord exception: webhook_discord_public_key_{tenant_id} (Open Question 1)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 166 + signature-verify.ts (HMAC + Ed25519) + 6 webhook adapters + 6 API endpoints + signal-service.ts (dedupe + consent gate)</name>
  <read_first>
    - 227-CONTEXT.md D-25..D-29 (webhook adapters per source, HMAC verify, dedupe, ConsentState gate, poll fallback), D-63..D-64 (signature mandatory; per-tenant HMAC key)
    - 227-RESEARCH.md §Pattern 3 Webhook Ingestion (lines 380-417) — full handleCommunityWebhook implementation
    - 227-RESEARCH.md §Webhook Ingestion Architecture (lines 985-1009) — per-source signature header table + Discord Ed25519 exception
    - 227-RESEARCH.md §Pitfall 11 (lines 971-975) — per-tenant key absent → 401 fail-closed (A24)
    - 227-RESEARCH.md §Schema Sketch 8 (lines 800-807) — migration 166 SQL
    - 227-CONTEXT.md D-05 (community_signals shape), D-27 (community_signal_processing field)
    - 227-VALIDATION.md slice 227-05 row + Coverage Targets "Webhook signature 100% per source"
    - Open Question 1 (RESEARCH lines 1193-1196) — Discord Ed25519 verify
  </read_first>
  <files>
    contracts/F-194-ecosystem-community-webhook-v1.json
    supabase/migrations/166_p221_consent_state_community_signal_processing.sql
    lib/markos/ecosystem/community/signal-service.ts
    lib/markos/ecosystem/community/consent-gate.ts
    lib/markos/ecosystem/community/adapters/signature-verify.ts
    lib/markos/ecosystem/community/adapters/slack-webhook.ts
    lib/markos/ecosystem/community/adapters/discord-webhook.ts
    lib/markos/ecosystem/community/adapters/forum-webhook.ts
    lib/markos/ecosystem/community/adapters/github-webhook.ts
    lib/markos/ecosystem/community/adapters/reddit-webhook.ts
    lib/markos/ecosystem/community/adapters/twitter-webhook.ts
    api/webhooks/community-slack.js
    api/webhooks/community-discord.js
    api/webhooks/community-forum.js
    api/webhooks/community-github.js
    api/webhooks/community-reddit.js
    api/webhooks/community-twitter.js
    test/ecosystem/community/webhook-signature-hmac.test.ts
    test/ecosystem/community/webhook-signature-discord-ed25519.test.ts
    test/ecosystem/community/signal-dedupe.test.ts
    test/ecosystem/community/consent-gate.test.ts
  </files>
  <behavior>
    - **F-194 community-webhook contract:** Schema for inbound webhook envelope per source (covers 6 sources + signal_id, source_message_id, payload, occurred_at). type='write', actor='System/Webhook'.
    - **Migration 166 (D-27):** `ALTER TABLE cdp_consent_states ADD COLUMN IF NOT EXISTS community_signal_processing text NOT NULL DEFAULT 'unknown' CHECK (community_signal_processing IN ('opted_in','opted_out','unknown'))`. A3 dependency: P221 cdp_consent_states must exist; if not, migration logs WARNING and skips (idempotent + future-resumable).
    - **signature-verify.ts:**
      - `verifyHmacSignature(rawBody, signatureHeader, key)`: returns bool. Uses `crypto.createHmac('sha256', key).update(rawBody).digest('hex')`; constant-time compare via `crypto.timingSafeEqual` (prevent timing attacks).
      - `verifyEd25519Signature(message, signature, publicKey)`: returns bool. Uses `crypto.verify('ed25519', Buffer.from(message), { key: publicKey, format: 'der', type: 'spki' }, Buffer.from(signature, 'hex'))`. Per-Open Question 1 recommendation. Discord-specific.
      - `constantTimeCompare(a, b)`: wrapper around `crypto.timingSafeEqual` with length-mismatch guard.
    - **6 adapters in `lib/markos/ecosystem/community/adapters/`:**
      - Each exports `normalize{Source}Payload(rawBody) → CommunitySignalInsert` mapping source-specific JSON shape to `community_signals` row insert object.
      - Each exports `getSignatureHeaderName(): string` returning the source's signature header (per RESEARCH.md table line 1000-1007).
      - Each exports `getEdgeConfigKeyName(tenant_id): string` returning the per-tenant key path.
      - **Slack:** signature header `x-slack-signature`; HMAC-SHA256; key from Edge Config `webhook_signing_key_slack_{tenant_id}`.
      - **Discord:** signature header `x-signature-ed25519` + `x-signature-timestamp`; Ed25519; key from `webhook_discord_public_key_{tenant_id}`.
      - **Forum (Discourse):** `x-discourse-event-signature`; HMAC-SHA256; key `webhook_signing_key_forum_{tenant_id}`.
      - **GitHub:** `x-hub-signature-256`; HMAC-SHA256; key `webhook_signing_key_github_{tenant_id}`.
      - **Reddit:** `x-reddit-signature`; HMAC-SHA256; key `webhook_signing_key_reddit_{tenant_id}`.
      - **Twitter:** `x-twitter-webhooks-signature`; HMAC-SHA256; key `webhook_signing_key_twitter_{tenant_id}`.
    - **6 API endpoints in `api/webhooks/community-{source}.js`:**
      - Each follows the exact pattern from RESEARCH.md §Pattern 3:
        1. Read tenant_id from request (URL path or auth context).
        2. Fetch per-tenant key from Edge Config; if absent → 401 + audit `webhook_signing_key_missing` (A24/Pitfall 11).
        3. Verify signature (HMAC-SHA256 OR Ed25519); if fail → 401 + audit `webhook_signature_failure`.
        4. Normalize payload via adapter.
        5. Call `signal-service.upsertCommunitySignal(signal)`.
        6. Return 200.
      - Endpoints use `req.rawBody` (Vercel API helper); body parser must NOT mutate raw bytes (signature requires exact bytes).
    - **signal-service.ts:**
      - `upsertCommunitySignal(signal)`: (a) check ConsentState if `signal.related_profile_id` set via `consent-gate.checkCommunitySignalConsent()`; (b) `supabase.from('community_signals').upsert(signal, { onConflict: 'source,source_message_id', ignoreDuplicates: true })` — DB-level dedupe (D-26).
      - `scrubSignalForTombstone(profile_id)`: per D-62 — `UPDATE community_signals SET related_profile_id=NULL, raw_payload=jsonb_build_object('scrubbed_at', now()) WHERE related_profile_id=$1`.
    - **consent-gate.ts:**
      - `checkCommunitySignalConsent(profile_id)`: calls P221 `getConsentState(profile_id)`; reads `community_signal_processing`; returns `{ allowed: bool, status: 'opted_in'|'opted_out'|'unknown' }`. If profile_id absent or status='opted_out' → allowed=false. If 'unknown' → allowed=false (fail-closed; per D-27 tenant policy can require explicit consent).
      - `COMMUNITY_CONSENT_STATES` const ENUM.
      - Used by signal-service.upsertCommunitySignal: if not allowed → write row with consent_state_check_passed=false (audit row preserved) but signal-routing.ts (Task 2) skips routing.
  </behavior>
  <action>
    Step 1 — Create `contracts/F-194-ecosystem-community-webhook-v1.json` with schema covering webhook envelope + source enum.
    Step 2 — Create `supabase/migrations/166_p221_consent_state_community_signal_processing.sql` per RESEARCH.md §Schema Sketch 8 verbatim. Use `IF NOT EXISTS` for idempotency. Add comment: `-- A3 dependency: P221 cdp_consent_states must exist; if not, migration is no-op until P221 ships`.
    Step 3 — Create `lib/markos/ecosystem/community/adapters/signature-verify.ts` with HMAC-SHA256 + Ed25519 + constant-time compare.
    Step 4 — Create 6 adapter files. Each ≥40 lines: normalizer + signature header + Edge Config key name. Discord uses Ed25519; others HMAC.
    Step 5 — Create `lib/markos/ecosystem/community/consent-gate.ts`:
      - Import getConsentState from P221 (A3); if module absent, fall back to safe default `{ allowed: false, status: 'unknown' }` (fail-closed).
      - Reads `community_signal_processing`; returns allowed bool.
    Step 6 — Create `lib/markos/ecosystem/community/signal-service.ts`:
      - `upsertCommunitySignal(signal)` calls consent-gate first; sets `consent_state_check_passed` based on result; upserts with onConflict.
      - `scrubSignalForTombstone(profile_id)` SQL.
    Step 7 — Create 6 API endpoint files in `api/webhooks/`. Each ≤80 lines mirroring RESEARCH §Pattern 3:
      ```javascript
      // api/webhooks/community-slack.js (template; same pattern × 6 sources)
      const { upsertCommunitySignal } = require('../../lib/markos/ecosystem/community/signal-service');
      const { normalizeSlackPayload, getSignatureHeaderName, getEdgeConfigKeyName } = require('../../lib/markos/ecosystem/community/adapters/slack-webhook');
      const { verifyHmacSignature } = require('../../lib/markos/ecosystem/community/adapters/signature-verify');
      const { getEdgeConfig } = require('../../lib/markos/auth/edge-config');
      const { writeAuditRow } = require('../../lib/markos/audit/log');
      module.exports = async function handler(req, res) {
        const tenantId = resolveTenantFromRequest(req);
        const key = await getEdgeConfig(getEdgeConfigKeyName(tenantId));
        if (!key) { await writeAuditRow({event:'webhook_signing_key_missing', source:'slack', tenant_id: tenantId}); return res.status(401).end(); }
        const sig = req.headers[getSignatureHeaderName()];
        if (!verifyHmacSignature(req.rawBody, sig, key)) { await writeAuditRow({event:'webhook_signature_failure', source:'slack', tenant_id: tenantId}); return res.status(401).end(); }
        const signal = normalizeSlackPayload(JSON.parse(req.rawBody.toString()));
        signal.tenant_id = tenantId;
        await upsertCommunitySignal(signal);
        return res.status(200).end();
      };
      module.exports.config = { api: { bodyParser: false } }; // raw body required for signature
      ```
      Discord endpoint uses `verifyEd25519Signature` instead of `verifyHmacSignature`.
    Step 8 — Create `test/ecosystem/community/webhook-signature-hmac.test.ts`:
      - Test 5 HMAC sources: valid signature → handler returns 200; invalid signature → 401 + audit row written; missing key → 401 + audit `webhook_signing_key_missing`.
      - Test signature constant-time compare (mock timing — assert `timingSafeEqual` invoked).
    Step 9 — Create `test/ecosystem/community/webhook-signature-discord-ed25519.test.ts`:
      - Discord-specific Ed25519 verify: valid → 200; tampered → 401; missing public key → 401.
      - Use Node `crypto.generateKeyPairSync('ed25519')` to generate test keypair; sign payload; verify.
    Step 10 — Create `test/ecosystem/community/signal-dedupe.test.ts`:
      - Insert 1 signal with (source='slack', source_message_id='msg-1').
      - Insert duplicate same (source, source_message_id) → assert UNIQUE constraint behavior with `ignoreDuplicates: true` returns no error; row count remains 1.
      - Insert different source_message_id same source → 2 rows.
      - Insert same source_message_id different source → 2 rows.
    Step 11 — Create `test/ecosystem/community/consent-gate.test.ts`:
      - Profile with `community_signal_processing='opted_in'` → allowed=true.
      - Profile with `community_signal_processing='opted_out'` → allowed=false.
      - Profile with `community_signal_processing='unknown'` → allowed=false (fail-closed).
      - Profile_id absent → allowed=false.
      - Test full integration: upsertCommunitySignal with opted_out profile → row inserted with consent_state_check_passed=false (audit-preserved per D-27).
    Step 12 — Run vitest.

    **Open Question 1 resolution:** Per RESEARCH recommendation, Discord uses Ed25519 with key in Edge Config namespace `webhook_discord_public_key_{tenant_id}`. Documented in adapter source comment.

    **CLAUDE.md precedence:** obsidian/reference/HTTP Layer.md is canonical for webhook routing patterns; obsidian/work/incoming/25-*.md is informational (rule 5).
  </action>
  <verify>
    <automated>npx vitest run test/ecosystem/community/webhook-signature-hmac.test.ts test/ecosystem/community/webhook-signature-discord-ed25519.test.ts test/ecosystem/community/signal-dedupe.test.ts test/ecosystem/community/consent-gate.test.ts</automated>
    Plus checks:
    - `grep "ALTER TABLE cdp_consent_states" supabase/migrations/166_*.sql` returns ≥1
    - `grep "community_signal_processing" supabase/migrations/166_*.sql` returns ≥1
    - `ls lib/markos/ecosystem/community/adapters/{slack,discord,forum,github,reddit,twitter}-webhook.ts | wc -l` returns 6
    - `ls api/webhooks/community-{slack,discord,forum,github,reddit,twitter}.js | wc -l` returns 6
    - `grep -E "verifyHmacSignature|verifyEd25519Signature" lib/markos/ecosystem/community/adapters/signature-verify.ts` returns ≥2
    - `grep "timingSafeEqual" lib/markos/ecosystem/community/adapters/signature-verify.ts` returns ≥1
    - `grep "verifyEd25519Signature" lib/markos/ecosystem/community/adapters/discord-webhook.ts api/webhooks/community-discord.js` returns ≥1
    - `grep -E "onConflict.*['\"]source.*source_message_id|UNIQUE.*source.*source_message_id" lib/markos/ecosystem/community/signal-service.ts` returns ≥1
    - `grep -E "consent_state_check_passed" lib/markos/ecosystem/community/signal-service.ts` returns ≥1
    - `grep -c "webhook_signing_key_missing\|webhook_signature_failure" api/webhooks/community-*.js` returns ≥10 (audit on both failure modes × 6 endpoints — minimum)
  </verify>
  <done>
    Migration 166 P221 ConsentState extension; 5 HMAC + 1 Ed25519 webhook adapter; 6 API endpoints with signature verify + audit row on failure + per-tenant Edge Config key + fail-closed when key absent; community_signals dedupe via UNIQUE (source, source_message_id) ON CONFLICT; ConsentState gate (opted_in/opted_out/unknown).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build signal-routing.ts (decision_rules + 6 action_kinds + spike debounce) + poll-fallback.ts + developer-event-service.ts (cdp_events fan-out) + 4 tests</name>
  <read_first>
    - 227-CONTEXT.md D-28 (decision_rules routing trigger_kind='community_signal_received' → 6 action_kinds), D-29 (poll fallback), D-55 (volume spike alert >2σ)
    - 227-CONTEXT.md D-06 (developer_events + cdp_events fan-out with shared source_event_ref UUID)
    - 227-RESEARCH.md slice 227-05 spec (lines 852-861) — decision_rules pluggable evaluators + create_task/learning_candidate/pr_followup/dismiss/escalate/generate_narrative
    - 227-RESEARCH.md §Pitfall 7 (lines 947-951) — community signal volume spike → debounce + circuit breaker
    - 227-RESEARCH.md §Integration Contracts cdp_events Fan-Out (lines 1039-1045) — single transaction fail-closed
    - 227-CONTEXT.md D-62 — tombstone scrub on community_signals.related_profile_id + developer_events.profile_id
    - 227-VALIDATION.md slice 227-05 row + Coverage Targets "ConsentState gate 100%; Decision_rules routing per action_kind"
    - migration 163 columns
  </read_first>
  <files>
    contracts/F-195-ecosystem-signal-route-v1.json
    lib/markos/ecosystem/community/signal-routing.ts
    lib/markos/ecosystem/community/poll-fallback.ts
    lib/markos/ecosystem/developer/developer-event-service.ts
    test/ecosystem/community/signal-routing.test.ts
    test/ecosystem/community/learning-candidate-emit.test.ts
    test/ecosystem/community/poll-fallback.test.ts
    test/ecosystem/developer/developer-event-fanout.test.ts
  </files>
  <behavior>
    - **F-195 signal-route contract:** Schema covering route request `{ signal_id, action_kind, action_config }` and response `{ routed_at, routed_by, success: bool }`. type='write/MCP', actor='Operator/Agent'.
    - **signal-routing.ts:**
      - `ROUTED_ACTION_KINDS = ['created_task','created_learning_candidate','created_pr_followup','dismissed','escalated','generate_narrative'] as const` (matches CONTEXT D-05 routed_action_kind enum — 6 values including generate_narrative).
      - `routeSignal(signal_id, action_kind, opts)`:
        1. Read signal from DB; if `consent_state_check_passed=false` → reject route (do not act on opted-out signals).
        2. Check spike via `shouldDebounceForSpike(signal)` — if true (volume >2σ from 7-day baseline), instead emit single volume_alert task per (source+topic+1-hour window) and return without per-signal routing (Pitfall 7).
        3. For action_kind:
           - `created_task` → emit operator task via task system (P208 entry).
           - `created_learning_candidate` → call P212 `emitLearningCandidate({source_signal_id: signal_id, summary, evidence_refs})`.
           - `created_pr_followup` → emit specialized task with subject='pr_followup' + signal_id.
           - `dismissed` → no action; log routing decision.
           - `escalated` → emit operator task with priority='critical' + assignee=tenant_admin.
           - `generate_narrative` → A22 fallback: if P225 narrative module absent, fall back to `created_learning_candidate`.
        4. Update signal: `routed_action_kind = action_kind; routed_at = now(); routed_by = caller`.
      - `shouldDebounceForSpike(signal)`: queries `SELECT count(*) FROM community_signals WHERE source=$1 AND topic=$2 AND occurred_at >= now() - interval '7 days'` → computes mean + stddev → returns true if current 1-hour count > mean + 2*stddev for that (source,topic) window.
    - **poll-fallback.ts:**
      - `pollFallbackCron()`: runs every 15 min. Selects integrations where webhook is configured AND last_received_at < now()-1h.
      - `checkWebhookHealth(integration)`: emits operator task `webhook_unreachable` per stale integration (only once per 1h via idempotency check).
      - For Slack/Discord, would poll via webhook fallback if available; for sources without polling API, just alert operator.
    - **developer-event-service.ts:**
      - `DEVELOPER_EVENT_KINDS` exported.
      - `ingestDeveloperEvent({tenant_id, event_kind, profile_id?, product_area?, version?, evidence_refs?})`: per D-06 fan-out:
        1. Begin Supabase transaction.
        2. Generate `source_event_ref = crypto.randomUUID()`.
        3. INSERT INTO developer_events (event_id, tenant_id, event_kind, profile_id, product_area, version, occurred_at, source_event_ref, evidence_refs).
        4. INSERT INTO cdp_events (event_id, tenant_id, event_name=`developer.${event_kind}`, event_domain='partner', occurred_at, profile_id, properties, source_event_ref) — same source_event_ref UUID.
        5. Commit. **Fail-closed:** if either insert fails, transaction rolls back; both rows absent.
      - Tombstone scrub helper: `scrubDeveloperEventForTombstone(profile_id)` sets profile_id=NULL on developer_events rows.
  </behavior>
  <action>
    Step 1 — Create `contracts/F-195-ecosystem-signal-route-v1.json`.
    Step 2 — Create `lib/markos/ecosystem/community/signal-routing.ts`:
      - Implement `routeSignal` with action_kind dispatch table
      - Implement `shouldDebounceForSpike` SQL with 7-day baseline
      - A22 fallback for generate_narrative
    Step 3 — Create `lib/markos/ecosystem/community/poll-fallback.ts` with pollFallbackCron + webhook health check.
    Step 4 — Create `lib/markos/ecosystem/developer/developer-event-service.ts`:
      - Single Supabase RPC or transaction for both inserts (use `supabase.rpc('ingest_developer_event_with_cdp', {...})` if RPC available, OR perform two inserts inside a transaction with explicit BEGIN/COMMIT/ROLLBACK).
      - Source_event_ref shared between rows.
    Step 5 — Create `test/ecosystem/community/signal-routing.test.ts`:
      - Test all 6 action_kinds (5 ROUTED_ACTION_KINDS + generate_narrative). Each asserts the corresponding side-effect (mock task emit, mock learning candidate, mock PR followup, etc.).
      - Test consent_state_check_passed=false → routeSignal rejects without action.
      - Test signal already routed (routed_action_kind not null) → idempotent reject.
      - Test spike debounce: insert 100 signals same (source,topic) in 1h vs 7-day baseline of 5/h; routeSignal should debounce + emit volume_alert once.
    Step 6 — Create `test/ecosystem/community/learning-candidate-emit.test.ts`:
      - routeSignal with action_kind='created_learning_candidate' for a positive-sentiment signal → mock emitLearningCandidate called with signal_id + summary + evidence_refs.
      - SG-06 traceability: signal source='reddit' + sentiment='positive' + topic='product_idea' → LearningCandidate emitted.
    Step 7 — Create `test/ecosystem/community/poll-fallback.test.ts`:
      - Setup integration with last_received_at = now()-2h (stale).
      - Run `pollFallbackCron()`; assert operator task `webhook_unreachable` emitted.
      - Re-run within 1h; assert NO duplicate task (idempotency).
      - Setup with last_received_at = now()-30min (not stale); assert NO task.
    Step 8 — Create `test/ecosystem/developer/developer-event-fanout.test.ts`:
      - ingestDeveloperEvent({event_kind:'sdk_download', profile_id, ...}) → developer_events row + cdp_events row both inserted with same source_event_ref.
      - Assert source_event_ref UUID matches between two rows (single fan-out).
      - Simulate cdp_events insert failure → assert developer_events row also absent (fail-closed transaction).
      - Test tombstone scrub: scrubDeveloperEventForTombstone(profile_id) → profile_id=NULL on rows; event_id + occurred_at preserved.
    Step 9 — Run vitest.
  </action>
  <verify>
    <automated>npx vitest run test/ecosystem/community/signal-routing.test.ts test/ecosystem/community/learning-candidate-emit.test.ts test/ecosystem/community/poll-fallback.test.ts test/ecosystem/developer/developer-event-fanout.test.ts</automated>
    Plus checks:
    - `grep -E "created_task|created_learning_candidate|created_pr_followup|dismissed|escalated|generate_narrative" lib/markos/ecosystem/community/signal-routing.ts` returns ≥6 distinct
    - `grep "shouldDebounceForSpike\|2\\s*\\*\\s*stddev\|2\\*stddev" lib/markos/ecosystem/community/signal-routing.ts` returns ≥1 (Pitfall 7 spike threshold)
    - `grep "consent_state_check_passed" lib/markos/ecosystem/community/signal-routing.ts` returns ≥1 (consent gate enforced)
    - `grep "source_event_ref" lib/markos/ecosystem/developer/developer-event-service.ts` returns ≥2 (shared between fan-out targets)
    - `grep -E "BEGIN|TRANSACTION|rollback|ROLLBACK|rpc\\(" lib/markos/ecosystem/developer/developer-event-service.ts` returns ≥1 (transaction-bound)
    - `grep -E "emitLearningCandidate|create_learning_candidate" lib/markos/ecosystem/community/signal-routing.ts` returns ≥2 (P212 integration + dispatch case)
    - `grep "webhook_unreachable" lib/markos/ecosystem/community/poll-fallback.ts` returns ≥1
  </verify>
  <done>
    F-195 contract registered; signal-routing covers 6 action_kinds + spike debounce + consent_state_check_passed gate + A22 fallback; poll-fallback emits webhook_unreachable task idempotently; developer-event-service single-transaction fan-out (developer_events + cdp_events with shared source_event_ref); fail-closed on any insert failure; tombstone scrub on profile_id columns.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 6 community webhooks → community_signals | Untrusted external sources; signature verification mandatory (D-63) |
| Per-tenant HMAC key in Edge Config | Key absent → fail-closed (Pitfall 11) |
| ConsentState → routing decision | Opted-out profiles must not be routed (D-27) |
| developer_events → cdp_events | Fan-out must succeed atomically; partial state forbidden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-227-05-01 | Spoofing | Forged community webhook | mitigate | HMAC-SHA256 (5 sources) + Ed25519 (Discord) verify; constant-time compare; failure → 401 + audit |
| T-227-05-02 | Information Disclosure | Per-tenant key fallback to shared key | mitigate | A24/Pitfall 11: key absent → 401; NEVER fallback to shared key |
| T-227-05-03 | Replay | Webhook replay attack | mitigate | UNIQUE (source, source_message_id) DB constraint; ON CONFLICT DO NOTHING; replay yields no new row |
| T-227-05-04 | Information Disclosure | Routing opted-out profile | mitigate | consent_state_check_passed=false → routeSignal rejects; signal stored for audit but not acted on |
| T-227-05-05 | DoS | Community signal storm (viral thread) | mitigate | Pitfall 7: shouldDebounceForSpike — >2σ from 7-day baseline → single volume_alert task per (source,topic,1h window) |
| T-227-05-06 | Tampering | developer_events without cdp_events fan-out | mitigate | Single Supabase transaction; both inserts share source_event_ref UUID; rollback on partial failure |
| T-227-05-07 | Information Disclosure | PII in raw_payload | mitigate | Adapters scrub raw_text + author_handle to hashes; D-05 + Pitfall (raw_payload PII-scrubbed before write) |
| T-227-05-08 | Tampering | Tombstone-affected profile_id retains association | mitigate | scrubSignalForTombstone + scrubDeveloperEventForTombstone — D-62 cascade chain |
</threat_model>

<verification>
- F-194 + F-195 contracts cover webhook envelope + route action.
- Migration 166 P221 ConsentState extended (community_signal_processing).
- 5 HMAC sources + 1 Ed25519 (Discord) signature verification covered; per-tenant key absence → 401 + audit; tampered → 401 + audit.
- community_signals dedupe via UNIQUE (source, source_message_id) ON CONFLICT proven idempotent.
- ConsentState gate: opted_out and unknown → consent_state_check_passed=false; routing rejects.
- 6 action_kinds (5 enum + generate_narrative with A22 fallback) routed correctly.
- Spike debounce >2σ from 7-day baseline (Pitfall 7).
- developer_events + cdp_events fan-out single transaction; shared source_event_ref; fail-closed.
- Poll-fallback emits webhook_unreachable task idempotently.
- Tombstone scrub on community_signals.related_profile_id + developer_events.profile_id.
- Legacy regression `npm test` green.
</verification>

<success_criteria>
- 2 contracts (F-194 + F-195) registered.
- Migration 166 P221 ConsentState extension committed.
- 14 modules under `lib/markos/ecosystem/{community,developer}/`.
- 6 webhook API endpoints under `api/webhooks/`.
- 8 vitest test files green: `npx vitest run test/ecosystem/community/ test/ecosystem/developer/`.
- HMAC + Ed25519 verified; constant-time compare used.
- Per-tenant Edge Config key naming convention documented in adapter source comments.
- ON CONFLICT dedupe + ConsentState gate + spike debounce all enforced.
- 227-VALIDATION.md per-task map updated for slice 227-05.
- Legacy regression green.
</success_criteria>

<output>
After completion, create `.planning/phases/227-ecosystem-partner-community-developer-growth/227-05-SUMMARY.md` with:
- F-194 + F-195 + migration 166 allocation
- 5 HMAC + 1 Ed25519 webhook sources
- Per-tenant Edge Config key naming convention
- 6 routing action_kinds covered + A22 generate_narrative fallback
- Spike debounce threshold + (source,topic,1h) window
- developer_events fan-out with shared source_event_ref UUID
- Tombstone scrub coverage
- Test results (8 files green)
</output>
