---
phase: 227-ecosystem-partner-community-developer-growth
plan: 05
type: execute
wave: 3
depends_on: ["227-01"]
preconditions:
  required_upstream_phases: [221]
  required_tables:
    - cdp_consent_states  # P221 — ALTER TABLE target (migration 166); MUST exist
    - cdp_identity_profiles # P221 — soft FK target
  fail_mode: HARD_FAIL  # RH2 / D-69 — no silent skip; route back to P221
files_modified:
  - contracts/F-194-ecosystem-community-webhook-v1.json
  - contracts/F-195-ecosystem-signal-route-v1.json
  - supabase/migrations/166_p221_consent_state_community_signal_processing.sql
  - supabase/migrations/166_b_webhook_subscription_health.sql
  - lib/markos/ecosystem/community/signal-service.ts
  - lib/markos/ecosystem/community/signal-routing.ts
  - lib/markos/ecosystem/community/consent-gate.ts
  - lib/markos/ecosystem/community/poll-fallback.ts
  - lib/markos/ecosystem/community/webhook-health.ts
  - lib/markos/ecosystem/community/adapters/slack-webhook.ts
  - lib/markos/ecosystem/community/adapters/discord-webhook.ts
  - lib/markos/ecosystem/community/adapters/forum-webhook.ts
  - lib/markos/ecosystem/community/adapters/github-webhook.ts
  - lib/markos/ecosystem/community/adapters/reddit-webhook.ts
  - lib/markos/ecosystem/community/adapters/twitter-webhook.ts
  - lib/markos/ecosystem/community/adapters/signature-verify.ts
  - lib/markos/ecosystem/community/adapters/timestamp-freshness.ts
  - lib/markos/ecosystem/developer/developer-event-service.ts
  - scripts/preconditions/227-05-check-upstream.cjs
  - api/webhooks/community-slack.js
  - api/webhooks/community-discord.js
  - api/webhooks/community-forum.js
  - api/webhooks/community-github.js
  - api/webhooks/community-reddit.js
  - api/webhooks/community-twitter.js
  - test/ecosystem/community/webhook-signature-hmac.test.ts
  - test/ecosystem/community/webhook-signature-discord-ed25519.test.ts
  - test/ecosystem/community/webhook-timestamp-freshness.test.ts
  - test/ecosystem/community/signal-dedupe.test.ts
  - test/ecosystem/community/consent-gate.test.ts
  - test/ecosystem/community/signal-routing.test.ts
  - test/ecosystem/community/learning-candidate-emit.test.ts
  - test/ecosystem/community/poll-fallback.test.ts
  - test/ecosystem/community/webhook-health-table.test.ts
  - test/ecosystem/developer/developer-event-fanout.test.ts
autonomous: true
requirements: [ECO-03, SG-06, SG-09, SG-12, QA-01, QA-02, QA-03, QA-04, QA-05, QA-06, QA-07, QA-08, QA-09, QA-10, QA-11, QA-12, QA-13, QA-14, QA-15]
must_haves:
  truths:
    - "Precondition guard HARD-FAILS if P221 cdp_consent_states is missing (D-69 / RH2) — no silent skip"
    - "Migration 166: P221 cdp_consent_states extended with community_signal_processing field (DEFAULT 'unknown')"
    - "Migration 166_b: webhook_subscription_health table created (D-76 / RM6) — canonical source for poll-fallback eligibility"
    - "5 sources (Slack, Forum, GitHub, Reddit, Twitter) verify HMAC-SHA256 signature; Discord verifies Ed25519"
    - "Every adapter that has timestamp header support enforces 5-min freshness window (D-77 / RM1) — Stripe Discord etc."
    - "Webhook with invalid/missing signature → 401 + markos_audit_log row; per-tenant key absent → 401 (no shared fallback)"
    - "Webhook with stale timestamp → 401 + audit `TIMESTAMP_OUTSIDE_FRESHNESS_WINDOW` (RM1)"
    - "community_signals dedupe via UNIQUE (source, source_message_id) — replay safe"
    - "ConsentState gate: opted_out → write row with consent_state_check_passed=false but DO NOT route"
    - "Signal routing via P225 decision_rules (trigger_kind='community_signal_received') → 6 action_kinds"
    - "DeveloperEvent ingest emits cdp_events with shared source_event_ref UUID (single fan-out, fail-closed transaction)"
    - "Volume spike >2σ from 7-day baseline → operator volume alert (Pitfall 7)"
    - "poll-fallback queries webhook_subscription_health (D-76 / RM6) — concrete source-of-truth, not vague"
  artifacts:
    - path: "scripts/preconditions/227-05-check-upstream.cjs"
      provides: "HARD-FAIL precheck for P221 cdp_consent_states + cdp_identity_profiles (D-69 / RH2)"
      contains: "MISSING_UPSTREAM_PHASE:P221"
    - path: "supabase/migrations/166_p221_consent_state_community_signal_processing.sql"
      provides: "ALTER TABLE cdp_consent_states ADD community_signal_processing — HARD-FAIL if cdp_consent_states absent"
      contains: "community_signal_processing"
    - path: "supabase/migrations/166_b_webhook_subscription_health.sql"
      provides: "webhook_subscription_health table (D-76 / RM6) — last_received_at + polling_enabled per tenant+source"
      contains: "CREATE TABLE webhook_subscription_health"
    - path: "lib/markos/ecosystem/community/signal-service.ts"
      provides: "community_signals upsert with dedupe + ConsentState gate"
      exports: ["upsertCommunitySignal", "scrubSignalForTombstone"]
    - path: "lib/markos/ecosystem/community/signal-routing.ts"
      provides: "Routes signals to decision_rules engine + 6 action_kinds + spike debounce"
      exports: ["routeSignal", "ROUTED_ACTION_KINDS", "shouldDebounceForSpike"]
    - path: "lib/markos/ecosystem/community/consent-gate.ts"
      provides: "ConsentState check for community_signal_processing"
      exports: ["checkCommunitySignalConsent", "COMMUNITY_CONSENT_STATES"]
    - path: "lib/markos/ecosystem/community/adapters/signature-verify.ts"
      provides: "HMAC-SHA256 + Ed25519 (Discord) signature verification"
      exports: ["verifyHmacSignature", "verifyEd25519Signature", "constantTimeCompare"]
    - path: "lib/markos/ecosystem/community/adapters/timestamp-freshness.ts"
      provides: "Webhook timestamp freshness validation (D-77 / RM1) — 5-min skew window"
      exports: ["assertTimestampFresh", "TIMESTAMP_FRESHNESS_WINDOW_SECONDS", "TIMESTAMP_OUTSIDE_FRESHNESS_WINDOW"]
    - path: "lib/markos/ecosystem/community/poll-fallback.ts"
      provides: "Polling fallback using webhook_subscription_health (D-76 / RM6)"
      exports: ["pollFallbackCron", "checkWebhookHealth"]
    - path: "lib/markos/ecosystem/community/webhook-health.ts"
      provides: "webhook_subscription_health write helper — updates last_received_at on every successful webhook receipt"
      exports: ["recordWebhookReceipt", "getWebhookHealth", "markPollingEnabled"]
    - path: "lib/markos/ecosystem/developer/developer-event-service.ts"
      provides: "developer_events ingest + cdp_events fan-out (single transaction)"
      exports: ["ingestDeveloperEvent", "DEVELOPER_EVENT_KINDS"]
  key_links:
    - from: "api/webhooks/community-{source}.js (6 endpoints)"
      to: "lib/markos/ecosystem/community/adapters/signature-verify.ts + timestamp-freshness.ts"
      via: "Per-tenant Edge Config key lookup → HMAC/Ed25519 verify + 5-min freshness check (D-77/RM1); failure → 401 + audit"
      pattern: "verifyHmacSignature\\|verifyEd25519Signature\\|assertTimestampFresh"
    - from: "lib/markos/ecosystem/community/poll-fallback.ts"
      to: "webhook_subscription_health table"
      via: "D-76 / RM6 — canonical source-of-truth for last_received_at + polling_enabled"
      pattern: "webhook_subscription_health"
    - from: "lib/markos/ecosystem/community/signal-service.ts"
      to: "supabase community_signals UPSERT + webhook_subscription_health UPDATE"
      via: "ON CONFLICT (source, source_message_id) DO NOTHING + recordWebhookReceipt"
      pattern: "onConflict.*source.*source_message_id|recordWebhookReceipt"
    - from: "lib/markos/ecosystem/community/signal-routing.ts"
      to: "P225 decision_rules engine"
      via: "trigger_kind='community_signal_received'"
      pattern: "community_signal_received"
    - from: "lib/markos/ecosystem/community/signal-routing.ts"
      to: "P212 LearningCandidate"
      via: "action_kind='create_learning_candidate' → P212 emit"
      pattern: "create_learning_candidate"
    - from: "lib/markos/ecosystem/developer/developer-event-service.ts"
      to: "cdp_events"
      via: "Single transaction fan-out with shared source_event_ref"
      pattern: "source_event_ref"
---

<objective>
Build CommunitySignal webhook ingestion (6 sources) + ConsentState extension + decision_rules routing + DeveloperEvent ingestion + cdp_events fan-out + webhook_subscription_health table (227-RESEARCH.md slice 227-05). Wave 3 — parallel with 227-04.

Purpose: This slice owns ECO-03 (community + developer signals feed CRM/analytics/launches/learning) + SG-06 (community/event/PR/partnership/devrel as governed workflows).

**Critical revisions from 227-REVIEWS.md:**
- **RH2 / D-69 (HARD-FAIL precondition):** Migration 166 was previously "log warning + skip if cdp_consent_states absent". Now HARD-FAILS via precondition guard with MISSING_UPSTREAM_PHASE:P221.
- **RM1 / D-77 (timestamp freshness):** Webhook adapters with timestamp header (Stripe, Discord, etc.) enforce 5-minute freshness window. Stale timestamp → 401 + audit `TIMESTAMP_OUTSIDE_FRESHNESS_WINDOW`.
- **RM6 / D-76 (webhook health source-of-truth):** New table `webhook_subscription_health` (migration 166_b) is the canonical source for last_received_at + polling_enabled. poll-fallback.ts queries this table (no longer vague).

Output: 2 contracts (F-194/F-195), 2 migrations (166 + 166_b — D-76 webhook health table), 1 precondition guard, 16 service/adapter modules (added webhook-health.ts + timestamp-freshness.ts), 6 webhook API endpoints, 10 vitest test files (added webhook-timestamp-freshness + webhook-health-table).
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-CONTEXT.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-RESEARCH.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-VALIDATION.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-01-PLAN.md
@.planning/phases/221-cdp-identity-audience-consent-substrate/221-CONTEXT.md
@.planning/phases/223-native-email-messaging-orchestration/223-CONTEXT.md
@.planning/phases/225-analytics-attribution-narrative-intelligence/225-CONTEXT.md
@.planning/phases/212-learning-literacy-evolution/212-CONTEXT.md
@CLAUDE.md

<interfaces>
From 227-01 (lib/markos/ecosystem/contracts.ts):

```typescript
export const COMMUNITY_SOURCE = ['slack','discord','forum','github','reddit','twitter','community_event','in_app'] as const;
export const COMMUNITY_SENTIMENT = ['positive','neutral','negative'] as const;
export const COMMUNITY_URGENCY = ['low','medium','high','critical'] as const;
export const ROUTED_ACTION_KIND = ['created_task','created_learning_candidate','created_pr_followup','dismissed','escalated','generate_narrative'] as const;
export const DEVELOPER_EVENT_KIND = ['sdk_download','api_first_call','mcp_tool_invocation','plugin_install','docs_view','hackathon_signup','certification_attempt'] as const;
```

From P221 (A3 — MUST exist; HARD-FAIL precondition):

```sql
-- cdp_consent_states (existing P221 columns)
-- ALTER ADDS: community_signal_processing text NOT NULL DEFAULT 'unknown' CHECK IN ('opted_in','opted_out','unknown')
-- HARD-FAIL if absent — no silent skip per RH2 / D-69
```

From P221 getConsentState extended return shape includes community_signal_processing.
From P225 decision_rules engine (A22 soft ref): trigger 'community_signal_received'.
From P212 LearningCandidate: emitLearningCandidate.
From cdp_events (P221): INSERT with source_event_ref shared with developer_events.
From migration 163 (227-01): community_signals + developer_events tables.
From Vercel Edge Config (A15): per-tenant per-source HMAC key namespace.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Precondition guard (D-69) + Migration 166 + 166_b webhook_subscription_health (D-76 / RM6) + signature-verify.ts + timestamp-freshness.ts (D-77 / RM1) + 6 webhook adapters + 6 API endpoints + signal-service.ts</name>
  <read_first>
    - 227-CONTEXT.md D-25..D-29 (webhook adapters, HMAC verify, dedupe, ConsentState gate, poll fallback), D-63..D-64 (signature mandatory; per-tenant HMAC key), **D-69 (HARD-FAIL preconditions / RH2), D-76 (webhook_subscription_health / RM6), D-77 (timestamp freshness / RM1)**
    - 227-RESEARCH.md §Pattern 3 Webhook Ingestion (lines 380-417) — full handler implementation
    - 227-RESEARCH.md §Webhook Ingestion Architecture (lines 985-1009) — per-source signature header table
    - 227-RESEARCH.md §Pitfall 11 (lines 971-975) — per-tenant key absent → 401 fail-closed
    - 227-REVIEWS.md RM1 — timestamp freshness needed even when DB dedupe exists
    - 227-REVIEWS.md RM6 — poll-fallback source-of-truth must be concrete table
    - 227-RESEARCH.md §Schema Sketch 8 (lines 800-807) — migration 166 SQL
    - Open Question 1 — Discord Ed25519 verify
  </read_first>
  <files>
    contracts/F-194-ecosystem-community-webhook-v1.json
    scripts/preconditions/227-05-check-upstream.cjs
    supabase/migrations/166_p221_consent_state_community_signal_processing.sql
    supabase/migrations/166_b_webhook_subscription_health.sql
    lib/markos/ecosystem/community/signal-service.ts
    lib/markos/ecosystem/community/consent-gate.ts
    lib/markos/ecosystem/community/webhook-health.ts
    lib/markos/ecosystem/community/adapters/signature-verify.ts
    lib/markos/ecosystem/community/adapters/timestamp-freshness.ts
    lib/markos/ecosystem/community/adapters/slack-webhook.ts
    lib/markos/ecosystem/community/adapters/discord-webhook.ts
    lib/markos/ecosystem/community/adapters/forum-webhook.ts
    lib/markos/ecosystem/community/adapters/github-webhook.ts
    lib/markos/ecosystem/community/adapters/reddit-webhook.ts
    lib/markos/ecosystem/community/adapters/twitter-webhook.ts
    api/webhooks/community-slack.js
    api/webhooks/community-discord.js
    api/webhooks/community-forum.js
    api/webhooks/community-github.js
    api/webhooks/community-reddit.js
    api/webhooks/community-twitter.js
    test/ecosystem/community/webhook-signature-hmac.test.ts
    test/ecosystem/community/webhook-signature-discord-ed25519.test.ts
    test/ecosystem/community/webhook-timestamp-freshness.test.ts
    test/ecosystem/community/signal-dedupe.test.ts
    test/ecosystem/community/consent-gate.test.ts
    test/ecosystem/community/webhook-health-table.test.ts
  </files>
  <behavior>
    - **Precondition guard (D-69 / RH2):** `scripts/preconditions/227-05-check-upstream.cjs` — same shape as 227-01 guard but checks ONLY `cdp_consent_states` + `cdp_identity_profiles`. Missing → exit 1 with `MISSING_UPSTREAM_PHASE:P221`. NO silent skip.
    - **F-194 community-webhook contract:** Schema for inbound webhook envelope per source. type='write', actor='System/Webhook'.
    - **Migration 166 (D-27 — RH2 HARD-FAIL):** `ALTER TABLE cdp_consent_states ADD COLUMN IF NOT EXISTS community_signal_processing text NOT NULL DEFAULT 'unknown' CHECK (community_signal_processing IN ('opted_in','opted_out','unknown'))`. **Migration depends on precondition guard run before execution.** No "log warning + skip" branch. Header comment: `-- DEPENDS ON: P221 cdp_consent_states (HARD-FAIL via 227-05-check-upstream.cjs precondition guard).`
    - **Migration 166_b (D-76 / RM6 — webhook_subscription_health):**
      ```sql
      CREATE TABLE IF NOT EXISTS webhook_subscription_health (
        health_id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
        tenant_id uuid NOT NULL,
        source text NOT NULL CHECK (source IN ('slack','discord','forum','github','reddit','twitter')),
        last_received_at timestamptz NULL,
        polling_enabled boolean NOT NULL DEFAULT false,
        last_polled_at timestamptz NULL,
        last_alert_at timestamptz NULL,
        consecutive_failures integer NOT NULL DEFAULT 0,
        configured_at timestamptz NOT NULL DEFAULT now(),
        UNIQUE (tenant_id, source)
      );
      ALTER TABLE webhook_subscription_health ENABLE ROW LEVEL SECURITY;
      CREATE POLICY webhook_subscription_health_tenant ON webhook_subscription_health
        USING (tenant_id = current_setting('app.active_tenant_id', true)::uuid)
        WITH CHECK (tenant_id = current_setting('app.active_tenant_id', true)::uuid);
      CREATE INDEX idx_webhook_health_stale ON webhook_subscription_health (tenant_id, last_received_at) WHERE polling_enabled = true;
      ```
    - **signature-verify.ts:** unchanged — HMAC-SHA256 + Ed25519 + constantTimeCompare.
    - **timestamp-freshness.ts (D-77 / RM1 — NEW):**
      - `TIMESTAMP_FRESHNESS_WINDOW_SECONDS = 300` (5 minutes — industry standard).
      - `assertTimestampFresh(timestampHeader: string, now?: Date)`: parses timestampHeader (epoch seconds OR ISO 8601 — adapter passes correct format); computes `Math.abs(now - timestamp_seconds)`; if > TIMESTAMP_FRESHNESS_WINDOW_SECONDS, throws `TIMESTAMP_OUTSIDE_FRESHNESS_WINDOW: skew=<seconds>`.
      - Used by adapters that have timestamp headers — Discord (`X-Signature-Timestamp` epoch seconds), Slack (`X-Slack-Request-Timestamp` epoch seconds), Stripe-style (epoch seconds in signature header). Forum/Reddit/Twitter/GitHub may not have explicit timestamp — adapter documents whether freshness is checked.
    - **6 adapters in `lib/markos/ecosystem/community/adapters/`:**
      - Each exports normalize{Source}Payload + getSignatureHeaderName + getEdgeConfigKeyName + **getTimestampHeaderName (returns null if not supported)** + **timestampFormat (returns 'epoch_seconds' | 'iso8601' | null)**.
      - Slack: signature `x-slack-signature`; HMAC-SHA256; **timestamp `x-slack-request-timestamp` (epoch_seconds) — RM1 freshness checked**.
      - Discord: signature `x-signature-ed25519`; Ed25519; **timestamp `x-signature-timestamp` (epoch_seconds) — RM1 freshness checked**.
      - Forum (Discourse): signature `x-discourse-event-signature`; HMAC-SHA256; no standard timestamp (freshness skipped, document why).
      - GitHub: signature `x-hub-signature-256`; HMAC-SHA256; no standard timestamp (freshness skipped).
      - Reddit: signature `x-reddit-signature`; HMAC-SHA256; no standard timestamp (freshness skipped).
      - Twitter: signature `x-twitter-webhooks-signature`; HMAC-SHA256; no standard timestamp (freshness skipped).
    - **6 API endpoints in `api/webhooks/community-{source}.js`:**
      - Each follows pattern from RESEARCH.md §Pattern 3, with NEW step (D-77 / RM1):
        1. Read tenant_id from request.
        2. Fetch per-tenant key from Edge Config; if absent → 401 + audit `webhook_signing_key_missing`.
        3. **Timestamp freshness (RM1 / D-77):** if adapter.getTimestampHeaderName() returns a header name, call `assertTimestampFresh(req.headers[name])`; on throw → 401 + audit `webhook_timestamp_stale` (TIMESTAMP_OUTSIDE_FRESHNESS_WINDOW).
        4. Verify signature (HMAC OR Ed25519); if fail → 401 + audit `webhook_signature_failure`.
        5. Normalize payload via adapter.
        6. Call `signal-service.upsertCommunitySignal(signal)`.
        7. **Update webhook health (D-76 / RM6):** call `recordWebhookReceipt(tenant_id, source)` to update `webhook_subscription_health.last_received_at = now()` (UPSERT).
        8. Return 200.
    - **webhook-health.ts (D-76 / RM6 — NEW):**
      - `recordWebhookReceipt(tenant_id, source)`: UPSERT into webhook_subscription_health setting last_received_at=now(); resets consecutive_failures=0.
      - `getWebhookHealth(tenant_id, source)`: SELECT row.
      - `markPollingEnabled(tenant_id, source, enabled)`: UPDATE polling_enabled.
    - **signal-service.ts:** unchanged shape — upsert with consent gate + onConflict dedupe + tombstone scrub.
    - **consent-gate.ts:** unchanged.
  </behavior>
  <action>
    Step 0 — Create + run `scripts/preconditions/227-05-check-upstream.cjs` (HARD-FAIL on missing cdp_consent_states / cdp_identity_profiles).
    Step 1 — Create `contracts/F-194-ecosystem-community-webhook-v1.json`.
    Step 2 — Create `supabase/migrations/166_p221_consent_state_community_signal_processing.sql` per RESEARCH.md §Schema Sketch 8 verbatim. Header comment notes HARD-FAIL precondition.
    Step 3 — Create `supabase/migrations/166_b_webhook_subscription_health.sql` per behavior (D-76 / RM6).
    Step 4 — Create `lib/markos/ecosystem/community/adapters/signature-verify.ts` with HMAC-SHA256 + Ed25519 + constant-time compare.
    Step 5 — Create `lib/markos/ecosystem/community/adapters/timestamp-freshness.ts` (D-77 / RM1):
      - TIMESTAMP_FRESHNESS_WINDOW_SECONDS=300
      - assertTimestampFresh throws TIMESTAMP_OUTSIDE_FRESHNESS_WINDOW
    Step 6 — Create 6 adapter files. Each ≥45 lines: normalizer + signature header + Edge Config key name + timestamp header name + format. Slack + Discord enforce freshness; others document why not.
    Step 7 — Create `lib/markos/ecosystem/community/webhook-health.ts` (D-76 / RM6):
      - recordWebhookReceipt + getWebhookHealth + markPollingEnabled.
    Step 8 — Create `lib/markos/ecosystem/community/consent-gate.ts` per prior plan.
    Step 9 — Create `lib/markos/ecosystem/community/signal-service.ts` per prior plan.
    Step 10 — Create 6 API endpoint files in `api/webhooks/`. Each ≤95 lines mirroring revised pattern (timestamp freshness step + recordWebhookReceipt). Discord uses verifyEd25519Signature.
    Step 11 — Create `test/ecosystem/community/webhook-signature-hmac.test.ts` per prior plan.
    Step 12 — Create `test/ecosystem/community/webhook-signature-discord-ed25519.test.ts` per prior plan.
    Step 13 — Create `test/ecosystem/community/webhook-timestamp-freshness.test.ts` (NEW — D-77 / RM1):
      - Slack: timestamp = now → 200; timestamp = now-10min → 401 + audit webhook_timestamp_stale.
      - Discord: timestamp = now → 200; timestamp = now+10min (clock skew or replay) → 401.
      - GitHub (no timestamp header): freshness step skipped (still 200 if signature valid).
      - assertTimestampFresh edge: skew=300 (boundary) → passes; skew=301 → throws.
    Step 14 — Create `test/ecosystem/community/signal-dedupe.test.ts` per prior plan.
    Step 15 — Create `test/ecosystem/community/consent-gate.test.ts` per prior plan.
    Step 16 — Create `test/ecosystem/community/webhook-health-table.test.ts` (NEW — D-76 / RM6):
      - recordWebhookReceipt UPSERT — first call inserts row; second call updates last_received_at.
      - getWebhookHealth returns shape.
      - RLS: tenant B cannot read tenant A's health row.
      - Stale detection: insert row with last_received_at=now()-2h, polling_enabled=true; query stale rows via `idx_webhook_health_stale` index — returns row.
    Step 17 — Run vitest.

    **Open Question 1 resolution:** Discord uses Ed25519 with `webhook_discord_public_key_{tenant_id}`. Discord ALSO has timestamp header → freshness enforced.
  </action>
  <verify>
    <automated>node scripts/preconditions/227-05-check-upstream.cjs && npx vitest run test/ecosystem/community/webhook-signature-hmac.test.ts test/ecosystem/community/webhook-signature-discord-ed25519.test.ts test/ecosystem/community/webhook-timestamp-freshness.test.ts test/ecosystem/community/signal-dedupe.test.ts test/ecosystem/community/consent-gate.test.ts test/ecosystem/community/webhook-health-table.test.ts</automated>
    Plus checks:
    - `grep "ALTER TABLE cdp_consent_states" supabase/migrations/166_*.sql` returns ≥1
    - `grep "community_signal_processing" supabase/migrations/166_*.sql` returns ≥1
    - **`grep "CREATE TABLE webhook_subscription_health" supabase/migrations/166_b_*.sql` returns ≥1 (D-76 / RM6)**
    - **`grep "MISSING_UPSTREAM_PHASE:P221" scripts/preconditions/227-05-check-upstream.cjs` returns ≥1 (D-69 / RH2)**
    - `ls lib/markos/ecosystem/community/adapters/{slack,discord,forum,github,reddit,twitter}-webhook.ts | wc -l` returns 6
    - `ls api/webhooks/community-{slack,discord,forum,github,reddit,twitter}.js | wc -l` returns 6
    - `grep -E "verifyHmacSignature|verifyEd25519Signature" lib/markos/ecosystem/community/adapters/signature-verify.ts` returns ≥2
    - `grep "timingSafeEqual" lib/markos/ecosystem/community/adapters/signature-verify.ts` returns ≥1
    - **`grep "TIMESTAMP_FRESHNESS_WINDOW_SECONDS\s*=\s*300" lib/markos/ecosystem/community/adapters/timestamp-freshness.ts` returns ≥1 (D-77 / RM1 — 5 min)**
    - **`grep "TIMESTAMP_OUTSIDE_FRESHNESS_WINDOW" lib/markos/ecosystem/community/adapters/timestamp-freshness.ts` returns ≥1 (D-77 / RM1)**
    - **`grep "assertTimestampFresh" api/webhooks/community-slack.js api/webhooks/community-discord.js` returns ≥2 (RM1 — adapters with timestamp enforce)**
    - **`grep "recordWebhookReceipt" api/webhooks/community-slack.js api/webhooks/community-discord.js api/webhooks/community-forum.js api/webhooks/community-github.js api/webhooks/community-reddit.js api/webhooks/community-twitter.js` returns ≥6 (RM6 — every adapter updates health)**
    - `grep "verifyEd25519Signature" lib/markos/ecosystem/community/adapters/discord-webhook.ts api/webhooks/community-discord.js` returns ≥1
    - `grep -E "onConflict.*['\"]source.*source_message_id|UNIQUE.*source.*source_message_id" lib/markos/ecosystem/community/signal-service.ts` returns ≥1
    - `grep -E "consent_state_check_passed" lib/markos/ecosystem/community/signal-service.ts` returns ≥1
    - `grep -c "webhook_signing_key_missing\|webhook_signature_failure\|webhook_timestamp_stale" api/webhooks/community-*.js` returns ≥18 (3 audit events × 6 endpoints — minimum)
    - **Behavior-first (RL2):** `node -e "const m=require('./lib/markos/ecosystem/community/adapters/timestamp-freshness.ts'); if(m.TIMESTAMP_FRESHNESS_WINDOW_SECONDS !== 300) process.exit(1); try { m.assertTimestampFresh(String(Math.floor((Date.now()-1000*1000)/1000))); process.exit(2);} catch(e){if(!/TIMESTAMP_OUTSIDE_FRESHNESS_WINDOW/.test(e.message))process.exit(3);} console.log('OK');"` — exits 0
  </verify>
  <done>
    HARD-FAIL precondition guard (D-69 / RH2); migration 166 P221 ConsentState extension + migration 166_b webhook_subscription_health (D-76 / RM6); 5 HMAC + 1 Ed25519 webhook adapter; 6 API endpoints with signature verify + timestamp freshness (D-77 / RM1) + audit row on failure + per-tenant Edge Config key + recordWebhookReceipt updates webhook_subscription_health; community_signals dedupe via UNIQUE (source, source_message_id); ConsentState gate.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build signal-routing.ts (decision_rules + 6 action_kinds + spike debounce) + poll-fallback.ts (D-76 / RM6 — uses webhook_subscription_health) + developer-event-service.ts + 4 tests</name>
  <read_first>
    - 227-CONTEXT.md D-28 (decision_rules routing), D-29 (poll fallback), D-55 (volume spike alert), D-06 (developer_events fan-out), D-62 (tombstone scrub), **D-76 (webhook_subscription_health canonical / RM6)**
    - 227-RESEARCH.md slice 227-05 spec
    - 227-RESEARCH.md §Pitfall 7 — community signal volume spike → debounce
    - 227-RESEARCH.md §Integration Contracts cdp_events Fan-Out
    - 227-REVIEWS.md RM6 — poll-fallback canonical health table
    - migration 163 columns + new migration 166_b webhook_subscription_health
  </read_first>
  <files>
    contracts/F-195-ecosystem-signal-route-v1.json
    lib/markos/ecosystem/community/signal-routing.ts
    lib/markos/ecosystem/community/poll-fallback.ts
    lib/markos/ecosystem/developer/developer-event-service.ts
    test/ecosystem/community/signal-routing.test.ts
    test/ecosystem/community/learning-candidate-emit.test.ts
    test/ecosystem/community/poll-fallback.test.ts
    test/ecosystem/developer/developer-event-fanout.test.ts
  </files>
  <behavior>
    - **F-195 signal-route contract:** Schema covering route request/response.
    - **signal-routing.ts:** unchanged — ROUTED_ACTION_KINDS (6 incl. generate_narrative); routeSignal; shouldDebounceForSpike (>2σ from 7-day baseline).
    - **poll-fallback.ts (D-76 / RM6 — REVISED):**
      - `pollFallbackCron()`: runs every 15 min. **Source of truth (D-76 / RM6):** queries `webhook_subscription_health` table:
        ```sql
        SELECT tenant_id, source, last_received_at, last_alert_at FROM webhook_subscription_health
        WHERE polling_enabled = true
          AND (last_received_at IS NULL OR last_received_at < now() - INTERVAL '1 hour')
          AND (last_alert_at IS NULL OR last_alert_at < now() - INTERVAL '1 hour')
        ```
        — concrete table, not vague "integrations where webhook is configured".
      - `checkWebhookHealth(integration)`: emits operator task `webhook_unreachable` per stale integration; updates `webhook_subscription_health.last_alert_at = now()` for idempotency (only one alert per hour).
    - **developer-event-service.ts:** unchanged — fan-out single transaction; shared source_event_ref.
  </behavior>
  <action>
    Step 1 — Create `contracts/F-195-ecosystem-signal-route-v1.json`.
    Step 2 — Create `lib/markos/ecosystem/community/signal-routing.ts` per prior plan.
    Step 3 — Create `lib/markos/ecosystem/community/poll-fallback.ts` per behavior — queries webhook_subscription_health table (D-76 / RM6).
    Step 4 — Create `lib/markos/ecosystem/developer/developer-event-service.ts` per prior plan.
    Step 5 — Create `test/ecosystem/community/signal-routing.test.ts` per prior plan.
    Step 6 — Create `test/ecosystem/community/learning-candidate-emit.test.ts` per prior plan.
    Step 7 — Create `test/ecosystem/community/poll-fallback.test.ts`:
      - **Setup webhook_subscription_health (D-76 / RM6):** insert row with polling_enabled=true, last_received_at=now()-2h, last_alert_at=NULL.
      - Run `pollFallbackCron()`; assert operator task `webhook_unreachable` emitted; assert last_alert_at updated.
      - Re-run within 1h; assert NO duplicate task (idempotency via last_alert_at).
      - Setup with last_received_at=now()-30min (not stale); assert NO task.
      - Setup with polling_enabled=false; assert NO task.
    Step 8 — Create `test/ecosystem/developer/developer-event-fanout.test.ts` per prior plan.
    Step 9 — Run vitest.
  </action>
  <verify>
    <automated>npx vitest run test/ecosystem/community/signal-routing.test.ts test/ecosystem/community/learning-candidate-emit.test.ts test/ecosystem/community/poll-fallback.test.ts test/ecosystem/developer/developer-event-fanout.test.ts</automated>
    Plus checks:
    - `grep -E "created_task|created_learning_candidate|created_pr_followup|dismissed|escalated|generate_narrative" lib/markos/ecosystem/community/signal-routing.ts` returns ≥6 distinct
    - `grep "shouldDebounceForSpike\|2\\s*\\*\\s*stddev\|2\\*stddev" lib/markos/ecosystem/community/signal-routing.ts` returns ≥1
    - `grep "consent_state_check_passed" lib/markos/ecosystem/community/signal-routing.ts` returns ≥1
    - `grep "source_event_ref" lib/markos/ecosystem/developer/developer-event-service.ts` returns ≥2
    - `grep -E "BEGIN|TRANSACTION|rollback|ROLLBACK|rpc\\(" lib/markos/ecosystem/developer/developer-event-service.ts` returns ≥1
    - `grep -E "emitLearningCandidate|create_learning_candidate" lib/markos/ecosystem/community/signal-routing.ts` returns ≥2
    - **`grep "webhook_subscription_health" lib/markos/ecosystem/community/poll-fallback.ts` returns ≥1 (D-76 / RM6 — concrete table)**
    - `grep "webhook_unreachable" lib/markos/ecosystem/community/poll-fallback.ts` returns ≥1
  </verify>
  <done>
    F-195 contract registered; signal-routing covers 6 action_kinds + spike debounce + consent gate; poll-fallback queries webhook_subscription_health (D-76 / RM6 — concrete source-of-truth); developer-event-service single-transaction fan-out; tombstone scrub.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 6 community webhooks → community_signals | Untrusted external sources; signature verification mandatory + timestamp freshness (D-77 / RM1) |
| Per-tenant HMAC key in Edge Config | Key absent → fail-closed (Pitfall 11) |
| ConsentState → routing decision | Opted-out profiles must not be routed (D-27) |
| developer_events → cdp_events | Fan-out must succeed atomically; partial state forbidden |
| Upstream P221 → P227 schema | HARD-FAIL if cdp_consent_states absent (D-69 / RH2) |
| webhook_subscription_health → poll-fallback | Concrete source-of-truth (D-76 / RM6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-227-05-01 | Spoofing | Forged community webhook | mitigate | HMAC-SHA256 (5 sources) + Ed25519 (Discord) verify; constant-time compare; failure → 401 + audit |
| T-227-05-02 | Information Disclosure | Per-tenant key fallback to shared key | mitigate | Pitfall 11: key absent → 401; NEVER fallback |
| T-227-05-03 | Replay | Webhook replay attack | mitigate | UNIQUE (source, source_message_id) DB constraint + **D-77 / RM1 timestamp freshness 5-min window for adapters with timestamp headers** |
| T-227-05-04 | Information Disclosure | Routing opted-out profile | mitigate | consent_state_check_passed=false → routeSignal rejects |
| T-227-05-05 | DoS | Community signal storm | mitigate | Pitfall 7: shouldDebounceForSpike — >2σ from 7-day baseline |
| T-227-05-06 | Tampering | developer_events without cdp_events fan-out | mitigate | Single Supabase transaction; rollback on partial failure |
| T-227-05-07 | Information Disclosure | PII in raw_payload | mitigate | Adapters scrub raw_text + author_handle to hashes |
| T-227-05-08 | Tampering | Tombstone-affected profile_id retains association | mitigate | scrubSignalForTombstone + scrubDeveloperEventForTombstone |
| T-227-05-09 | Tampering | Migration runs against missing P221 schema | mitigate | D-69 / RH2 — HARD-FAIL precondition guard prevents silent skip |
| T-227-05-10 | DoS | Stale webhook subscription unnoticed | mitigate | D-76 / RM6 — poll-fallback queries webhook_subscription_health for concrete staleness detection |
</threat_model>

<verification>
- HARD-FAIL precondition guard exits 1 with MISSING_UPSTREAM_PHASE:P221 (D-69 / RH2).
- F-194 + F-195 contracts cover webhook envelope + route action.
- Migration 166 P221 ConsentState extended.
- Migration 166_b webhook_subscription_health (D-76 / RM6).
- 5 HMAC sources + 1 Ed25519 (Discord) signature verification.
- **Timestamp freshness 5-min window enforced for Slack + Discord adapters (D-77 / RM1).**
- community_signals dedupe via UNIQUE (source, source_message_id) ON CONFLICT proven idempotent.
- ConsentState gate: opted_out and unknown → consent_state_check_passed=false; routing rejects.
- 6 action_kinds (5 enum + generate_narrative) routed correctly.
- Spike debounce >2σ from 7-day baseline (Pitfall 7).
- developer_events + cdp_events fan-out single transaction.
- **poll-fallback queries webhook_subscription_health (D-76 / RM6 — concrete source-of-truth).**
- recordWebhookReceipt updates last_received_at on every successful webhook (RM6).
- Tombstone scrub on community_signals.related_profile_id + developer_events.profile_id.
- Legacy regression `npm test` green.
</verification>

<success_criteria>
- 1 precondition guard.
- 2 contracts (F-194 + F-195) registered.
- 2 migrations (166 P221 ConsentState + 166_b webhook_subscription_health).
- 16 modules under `lib/markos/ecosystem/{community,developer}/` (added webhook-health.ts + timestamp-freshness.ts).
- 6 webhook API endpoints under `api/webhooks/`.
- 10 vitest test files green.
- HMAC + Ed25519 verified; constant-time compare; **timestamp freshness 5-min window (RM1)**.
- Per-tenant Edge Config key naming convention documented.
- ON CONFLICT dedupe + ConsentState gate + spike debounce + **timestamp freshness** all enforced.
- **webhook_subscription_health canonical for poll-fallback (RM6).**
- 227-VALIDATION.md per-task map updated.
- Legacy regression green.
</success_criteria>

<output>
After completion, create `.planning/phases/227-ecosystem-partner-community-developer-growth/227-05-SUMMARY.md` with:
- Precondition guard outcome (P221 verified or HARD-FAIL — D-69 / RH2)
- F-194 + F-195 + migrations 166 + 166_b allocation
- 5 HMAC + 1 Ed25519 webhook sources
- **Timestamp freshness 5-min window (D-77 / RM1) — Slack + Discord enforced**
- Per-tenant Edge Config key naming convention
- 6 routing action_kinds + A22 generate_narrative fallback
- Spike debounce threshold + (source,topic,1h) window
- developer_events fan-out with shared source_event_ref UUID
- **webhook_subscription_health table (D-76 / RM6) — concrete poll-fallback source**
- Tombstone scrub coverage
- Test results (10 files green)
</output>
</content>
</invoke>