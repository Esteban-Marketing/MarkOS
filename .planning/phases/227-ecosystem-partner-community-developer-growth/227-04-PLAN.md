---
phase: 227-ecosystem-partner-community-developer-growth
plan: 04
type: execute
wave: 3
depends_on: ["227-01", "227-03"]
files_modified:
  - contracts/F-192-ecosystem-fraud-signal-v1.json
  - contracts/F-193-ecosystem-payout-credit-v1.json
  - lib/markos/ecosystem/referrals/referral-service.ts
  - lib/markos/ecosystem/referrals/qualification-rule.ts
  - lib/markos/ecosystem/referrals/fraud-evaluator.ts
  - lib/markos/ecosystem/referrals/fraud-review-queue.ts
  - lib/markos/ecosystem/affiliates/affiliate-service.ts
  - lib/markos/ecosystem/affiliates/commission-evaluator.ts
  - lib/markos/ecosystem/payouts/payout-ledger.ts
  - lib/markos/ecosystem/payouts/payout-export.ts
  - lib/markos/ecosystem/payouts/payout-reconciliation.ts
  - lib/markos/ecosystem/payouts/payout-compliance-gate.ts
  - lib/markos/ecosystem/payouts/pricing-placeholder.ts
  - scripts/payout/reconcile.cjs
  - test/ecosystem/referrals/qualification-rule.test.ts
  - test/ecosystem/fraud/fraud-evaluator.test.ts
  - test/ecosystem/fraud/fraud-cron.test.ts
  - test/ecosystem/fraud/fraud-review-queue.test.ts
  - test/ecosystem/affiliates/commission-evaluator.test.ts
  - test/ecosystem/payouts/payout-ledger.test.ts
  - test/ecosystem/payouts/payout-state-lifecycle.test.ts
  - test/ecosystem/payouts/payout-dispute-flow.test.ts
  - test/ecosystem/payouts/payout-export.test.ts
  - test/ecosystem/payouts/payout-reconciliation.test.ts
  - test/ecosystem/payouts/payout-compliance-gate.test.ts
  - test/ecosystem/payouts/pricing-placeholder.test.ts
autonomous: true
requirements: [ECO-02, ECO-05, SG-04, SG-09, SG-11, SG-12, QA-01, QA-02, QA-03, QA-04, QA-05, QA-06, QA-07, QA-08, QA-09, QA-10, QA-11, QA-12, QA-13, QA-14, QA-15]
must_haves:
  truths:
    - "Referral qualification_rule evaluator returns deterministic pass/fail per program rule"
    - "Fraud rule engine has pluggable evaluators per signal_kind (8 kinds); 24h default window + 7d slow-fraud window (D-72 / RM2)"
    - "Fraud `high` AND `critical` severities BOTH block payout pending human review (D-72 / RM3) — not only `critical`"
    - "Maturation/holding window: first payout for new program held 24h before clearPayout (D-72)"
    - "Affiliate commission evaluator supports flat_rate + tiered + sliding_scale + performance_bonus models"
    - "payout_credits state machine: earned → cleared → exported → paid → {voided, disputed} (D-70 / RH3) — `paid` ONLY after finance reconciliation"
    - "CSV generation marks credits status='exported' (NOT 'paid') — RH3"
    - "`paid` requires explicit finance reconciliation via scripts/payout/reconcile.cjs"
    - "Manual CSV export weekly Sunday 04:00 UTC; gated on tenant payout_compliance_acknowledged_at (D-71 / RH4) — no acknowledgement → no export"
    - "{{MARKOS_PRICING_ENGINE_PENDING}} placeholder enforced on every payout-rate / commission_share copy path"
    - "commission_share never sourced from raw user input — bound to P205 PricingRecommendation OR placeholder (D-65)"
  artifacts:
    - path: "lib/markos/ecosystem/referrals/referral-service.ts"
      provides: "Referral program CRUD + activation gate + qualification eval invocation"
      exports: ["createReferralProgram", "activateReferralProgram", "evaluateReferral"]
    - path: "lib/markos/ecosystem/referrals/qualification-rule.ts"
      provides: "qualification_rule JSONB DSL evaluator"
      exports: ["evaluateQualificationRule", "QualificationRuleSchema"]
    - path: "lib/markos/ecosystem/referrals/fraud-evaluator.ts"
      provides: "Pluggable evaluator per fraud signal_kind + 24h/7d window cron + high+critical severity action (D-72)"
      exports: ["FRAUD_EVALUATORS", "evaluateFraud", "fraudDetectionCron", "FRAUD_SIGNAL_KINDS", "FRAUD_DETECTION_WINDOW_HOURS", "SLOW_FRAUD_WINDOW_DAYS"]
    - path: "lib/markos/ecosystem/referrals/fraud-review-queue.ts"
      provides: "Human review queue for high+critical fraud signals (D-72 / RM3) + maturation window (D-72)"
      exports: ["enqueueFraudReview", "clearFraudReviewItem", "rejectFraudReviewItem", "getMaturationStatus", "MATURATION_WINDOW_HOURS"]
    - path: "lib/markos/ecosystem/affiliates/affiliate-service.ts"
      provides: "Affiliate program CRUD + activation + cookie tracking metadata"
      exports: ["createAffiliateProgram", "activateAffiliateProgram"]
    - path: "lib/markos/ecosystem/affiliates/commission-evaluator.ts"
      provides: "Commission tier evaluator (flat/tiered/sliding/performance)"
      exports: ["evaluateCommission", "COMMISSION_MODELS"]
    - path: "lib/markos/ecosystem/payouts/payout-ledger.ts"
      provides: "payout_credits CRUD + state machine (D-70 — earned→cleared→exported→paid) + dispute flow"
      exports: ["createPayoutCredit", "clearPayout", "voidPayout", "disputePayout", "resolvePayoutDispute", "PAYOUT_TRANSITIONS"]
    - path: "lib/markos/ecosystem/payouts/payout-export.ts"
      provides: "Weekly CSV export cron + payout_export_batches audit + Supabase Storage signed URL — marks 'exported' (D-70 / RH3); compliance-gated (D-71 / RH4)"
      exports: ["payoutExportCron", "generateExportCsv"]
    - path: "lib/markos/ecosystem/payouts/payout-reconciliation.ts"
      provides: "Finance reconciliation — converts 'exported' → 'paid' (D-70 / RH3)"
      exports: ["reconcileBatch", "markCreditsPaid", "RECONCILIATION_STATUSES"]
    - path: "lib/markos/ecosystem/payouts/payout-compliance-gate.ts"
      provides: "Tenant compliance acknowledgement gate (D-71 / RH4); blocks export if missing or stale"
      exports: ["assertPayoutComplianceAcknowledged", "ACKNOWLEDGEMENT_TTL_MONTHS", "COMPLIANCE_NOT_ACKNOWLEDGED"]
    - path: "lib/markos/ecosystem/payouts/pricing-placeholder.ts"
      provides: "{{MARKOS_PRICING_ENGINE_PENDING}} resolver + commission_share guard"
      exports: ["resolveCommissionRate", "PRICING_PENDING_PLACEHOLDER", "guardCommissionInput"]
    - path: "scripts/payout/reconcile.cjs"
      provides: "Finance CLI to flip exported→paid after batch confirmed externally"
      contains: "MARKOS_PRICING_ENGINE_PENDING"
  key_links:
    - from: "lib/markos/ecosystem/payouts/payout-ledger.ts"
      to: "lib/markos/crm/copilot.ts::createApprovalPackage"
      via: "Approval Inbox entry payout_credits_clear (D-45/D-51)"
      pattern: "createApprovalPackage.*payout_credits_clear"
    - from: "lib/markos/ecosystem/referrals/fraud-evaluator.ts"
      to: "fraud_signals + payout_credits + fraud_review_queue"
      via: "high|critical severity → fraud_review_queue + payout_credits.fraud_review_status='blocked' (D-72 / RM2-RM3)"
      pattern: "fraud_review_status\\s*=\\s*'blocked'|severity.*critical|severity.*high"
    - from: "lib/markos/ecosystem/payouts/payout-export.ts"
      to: "lib/markos/ecosystem/payouts/payout-compliance-gate.ts"
      via: "RH4 / D-71 — assertPayoutComplianceAcknowledged before any export"
      pattern: "assertPayoutComplianceAcknowledged"
    - from: "lib/markos/ecosystem/payouts/payout-export.ts"
      to: "payout_credits.status='exported'"
      via: "D-70 / RH3 — CSV generation marks 'exported' NEVER 'paid'"
      pattern: "status.*['\"]exported['\"]"
    - from: "lib/markos/ecosystem/payouts/payout-reconciliation.ts"
      to: "payout_credits.status='paid'"
      via: "D-70 / RH3 — only reconciliation flips to 'paid'"
      pattern: "status.*['\"]paid['\"]"
    - from: "lib/markos/ecosystem/payouts/pricing-placeholder.ts"
      to: "lib/markos/pricing/recommendation.ts (P205) or placeholder"
      via: "resolve via P205 OR {{MARKOS_PRICING_ENGINE_PENDING}}; never raw user input"
      pattern: "MARKOS_PRICING_ENGINE_PENDING"
---

<objective>
Build referral programs + affiliate programs + fraud detection engine + payout credits ledger + manual CSV export + finance reconciliation + compliance acknowledgement gate + pricing placeholder enforcement (227-RESEARCH.md slice 227-04). Wave 3 — parallel with 227-05.

Purpose: This slice owns SG-04 (B2C/PLG growth: viral/referral fraud + payout safety) and ECO-02 (partner/affiliate motions). Critical fidelity to D-23 (manual CSV — NO Stripe Connect v1), D-65 (commission_share NEVER from raw input), {{MARKOS_PRICING_ENGINE_PENDING}} on every payout copy path.

**Critical revisions from 227-REVIEWS.md:**
- **RH3 / D-70 (payout state lifecycle):** payout_credits status now `earned → cleared → exported → paid`. CSV generation marks `exported`. `paid` REQUIRES finance reconciliation via `scripts/payout/reconcile.cjs`. Zero code paths set `paid` from `payout-export.ts`.
- **RH4 / D-71 (compliance gate):** Payout export REJECTS unless tenant has `payout_compliance_acknowledged_at` set within last 12 months. Real gate at API + cron level.
- **RM2 / D-72 (fraud window + severity):** Fraud detection window 24h default + 7d slow-fraud pass. Both `high` AND `critical` block payout. Maturation: first payout for new program held 24h.
- **RM3 / D-72 (human review queue):** `fraud_review_queue` table holds high+critical pending review. Operator clears or rejects.

Output: 2 contracts (F-192/F-193), 11 service modules (added fraud-review-queue, payout-reconciliation, payout-compliance-gate), 1 reconciliation CLI, 12 vitest test files (added fraud-review-queue, payout-state-lifecycle, payout-reconciliation, payout-compliance-gate).
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-CONTEXT.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-RESEARCH.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-VALIDATION.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-01-PLAN.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-03-PLAN.md
@.planning/phases/205-pricing-engine-foundation-billing-readiness/205-CONTEXT.md
@.planning/phases/211-content-social-revenue-loop/211-CONTEXT.md
@.planning/phases/225-analytics-attribution-narrative-intelligence/225-CONTEXT.md
@CLAUDE.md

# Pricing Engine Canon: obsidian/brain/Pricing Engine Canon.md is THE source-of-truth on placeholder rule.

<interfaces>
From 227-01 (lib/markos/ecosystem/contracts.ts):

```typescript
export const FRAUD_SIGNAL_KIND = ['self_referral','ip_pattern_match','velocity_anomaly','ip_overlap','conversion_timing_anomaly','device_fingerprint_match','geo_velocity','account_age_anomaly'] as const;
export const FRAUD_SEVERITY = ['low','medium','high','critical'] as const;
export const FRAUD_STATUS = ['detected','reviewing','confirmed','dismissed'] as const;
// D-70 / RH3 — 'exported' added between 'cleared' and 'paid':
export const PAYOUT_CREDIT_STATUS = ['earned','cleared','exported','paid','voided','disputed'] as const;
// D-72 / RM3 — 'holding' added for maturation window:
export const PAYOUT_FRAUD_REVIEW_STATUS = ['pending','cleared','blocked','holding'] as const;
export const COMMISSION_MODEL = ['flat_rate','tiered','sliding_scale','performance_bonus'] as const;
export const AFFILIATE_PROGRAM_STATUS = ['draft','active','paused','archived'] as const;
```

From 227-03 (lib/markos/ecosystem/partners/partner-service.ts):

```typescript
export function enableReferral(partner_id: string): Promise<void>;
export function enableAffiliate(partner_id: string): Promise<void>;
```

From P205 (A13 — soft ref):

```typescript
export interface PricingRecommendation { id: string; rate: number; currency: string; status: 'approved' | 'draft'; }
export function getApprovedRecommendation(id: string): Promise<PricingRecommendation | null>;
```

From migrations 162 (227-01):

```sql
-- affiliate_programs (commission_model, attribution_window_days, cookie_duration_days, fraud_controls, payout_settings)
-- fraud_signals (program_kind, signal_kind, severity, status)
-- payout_credits — D-70 status enum INCLUDES 'exported'; columns: status, fraud_review_status, currency, amount_cents, exported_at, reconciled_at
-- payout_export_batches — status enum 'pending'/'exported'/'reconciled'/'failed'; reconciled_at, reconciled_by_user_id
-- fraud_review_queue (D-72 / RM3) — queue_id, fraud_signal_id, payout_credit_id, severity, status, queued_at, reviewed_at, reviewed_by_user_id
```

From migration 160 (227-01) partner_profiles + payout_compliance_acknowledged_at column (D-71 / RH4).

From obsidian/brain/Pricing Engine Canon.md: unresolved pricing/payout copy uses `{{MARKOS_PRICING_ENGINE_PENDING}}` until approved P205 PricingRecommendation exists.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build fraud-evaluator.ts (24h+7d windows, high+critical block — D-72/RM2-RM3) + fraud-review-queue.ts + qualification-rule.ts + pricing-placeholder.ts + 5 tests</name>
  <read_first>
    - 227-CONTEXT.md D-20..D-24 (fraud_signals + payout suspension), D-65 (commission_share never from raw input), D-46 (content classifier), **D-72 (fraud window + high+critical block + maturation — RM2/RM3)**
    - 227-RESEARCH.md slice 227-04 spec (lines 843-851) — fraud rule engine + decision_rules pattern
    - 227-RESEARCH.md §Pitfall 5 (lines 935-939) — false positive tuning + per-program fraud_controls override
    - 227-REVIEWS.md RM2 — 1hr window too short; high should block too; maturation needed
    - 227-CONTEXT.md D-23 — manual CSV; placeholder; NO Stripe Connect / KYC / 1099
    - obsidian/brain/Pricing Engine Canon.md placeholder rule
    - 227-VALIDATION.md slice 227-04 row + Coverage Targets
  </read_first>
  <files>
    contracts/F-192-ecosystem-fraud-signal-v1.json
    lib/markos/ecosystem/referrals/qualification-rule.ts
    lib/markos/ecosystem/referrals/fraud-evaluator.ts
    lib/markos/ecosystem/referrals/fraud-review-queue.ts
    lib/markos/ecosystem/payouts/pricing-placeholder.ts
    test/ecosystem/referrals/qualification-rule.test.ts
    test/ecosystem/fraud/fraud-evaluator.test.ts
    test/ecosystem/fraud/fraud-cron.test.ts
    test/ecosystem/fraud/fraud-review-queue.test.ts
    test/ecosystem/payouts/pricing-placeholder.test.ts
  </files>
  <behavior>
    - **F-192 fraud-signal contract:** Schema covering fraud_signals table columns + 8 signal_kinds + 4 severity levels + 4 status values; type='read-write', actor='Operator'.
    - **qualification-rule.ts:** unchanged from prior plan version — QualificationRuleSchema + evaluateQualificationRule (all/any/none groups).
    - **fraud-evaluator.ts (D-72 / RM2):**
      - `FRAUD_DETECTION_WINDOW_HOURS = 24` (default; was 1 — RM2 fix); `SLOW_FRAUD_WINDOW_DAYS = 7` (additional pass for slow patterns).
      - `FRAUD_EVALUATORS: Record<FraudSignalKind, Evaluator>` — 8 evaluators per prior plan (self_referral, ip_pattern_match, velocity_anomaly, ip_overlap, conversion_timing_anomaly, device_fingerprint_match, geo_velocity, account_age_anomaly).
      - `evaluateFraud(referralRecord, programConfig)`: runs ALL evaluators in `programConfig.fraud_controls[]`; returns array of detected fraud signals. Per-program config can disable specific evaluators (Pitfall 5).
      - `fraudDetectionCron()`: hourly handler. Iterates active referral_programs + affiliate_programs; runs evaluateFraud against attribution_touches in **TWO passes** (D-72 / RM2):
        - **Pass A (24h fast pass):** last 24h, all evaluators.
        - **Pass B (7d slow pass):** last 7d, slow-pattern evaluators (velocity_anomaly, account_age_anomaly, conversion_timing_anomaly).
        Inserts fraud_signals rows. **For severity='critical' OR severity='high' (D-72 / RM3 — RM3 fix):**
        1. Insert into fraud_signals.
        2. Update related payout_credits.fraud_review_status='blocked'.
        3. Call `enqueueFraudReview(fraud_signal_id, payout_credit_id?, severity)` from fraud-review-queue.ts.
        For severity='medium': fraud_signals row created but NOT auto-blocked (operator may discretionarily review).
        For severity='low': informational only.
      - Idempotency: composite key (program_kind, program_id, attribution_touch_id, signal_kind) prevents duplicate signals across cron runs.
    - **fraud-review-queue.ts (D-72 / RM3):**
      - `MATURATION_WINDOW_HOURS = 24` — first payout for new program held 24h before clearPayout (D-72 maturation).
      - `enqueueFraudReview(fraud_signal_id, payout_credit_id, severity)`: inserts fraud_review_queue row status='pending_review'; for severity='high'|'critical' updates payout_credits.fraud_review_status='blocked'.
      - `clearFraudReviewItem(queue_id, reviewer_user_id, notes)`: operator action — flips queue status='cleared' AND payout_credits.fraud_review_status='cleared' (allowing payout to proceed).
      - `rejectFraudReviewItem(queue_id, reviewer_user_id, reason)`: flips queue='rejected' AND payout_credits.status='voided'.
      - `getMaturationStatus(program_id, partner_id?)`: returns `{ matured: bool, hours_until_mature: number }` — checks if program has had any prior CLEARED payout for this partner; if not, holds new payouts for MATURATION_WINDOW_HOURS. **Used by payout-ledger.clearPayout** before allowing earned→cleared transition.
      - For first-payout maturation: `payout_credits.fraud_review_status='holding'` (D-72 / new enum value) until window elapses.
    - **pricing-placeholder.ts:** unchanged from prior plan — PRICING_PENDING_PLACEHOLDER + resolveCommissionRate + guardCommissionInput.
  </behavior>
  <action>
    Step 1 — Create `contracts/F-192-ecosystem-fraud-signal-v1.json`.
    Step 2 — Create `lib/markos/ecosystem/referrals/qualification-rule.ts` per prior plan.
    Step 3 — Create `lib/markos/ecosystem/referrals/fraud-evaluator.ts` with:
      - 8 named evaluators
      - FRAUD_DETECTION_WINDOW_HOURS=24 + SLOW_FRAUD_WINDOW_DAYS=7
      - fraudDetectionCron with Pass A (24h) + Pass B (7d slow patterns) — RM2
      - high|critical severity → block + enqueueFraudReview — RM3
    Step 4 — Create `lib/markos/ecosystem/referrals/fraud-review-queue.ts`:
      - enqueueFraudReview / clearFraudReviewItem / rejectFraudReviewItem
      - MATURATION_WINDOW_HOURS=24 + getMaturationStatus
      - holding status for first-payout window
    Step 5 — Create `lib/markos/ecosystem/payouts/pricing-placeholder.ts` per prior plan.
    Step 6 — Create `test/ecosystem/referrals/qualification-rule.test.ts` per prior plan.
    Step 7 — Create `test/ecosystem/fraud/fraud-evaluator.test.ts`:
      - Test all 8 evaluators with positive + negative cases (16+ minimum).
      - **Window test (RM2):** insert fraud signal at now()-2h → caught by 24h window (was missed by 1h window).
      - **Slow-fraud test (RM2):** insert account_age_anomaly at now()-3d → caught by 7d slow pass.
      - ip_overlap → severity='medium' (Pitfall 5 B2B carve-out).
    Step 8 — Create `test/ecosystem/fraud/fraud-cron.test.ts`:
      - Self-referral signal triggered → fraud_signals row + fraud_review_queue row + payout_credits.fraud_review_status='blocked'.
      - **High severity (RM3):** trigger ip_pattern_match >5 in 24h → severity='high' → ALSO blocks payout + enqueues review (was previously only critical).
      - Critical severity → blocks + enqueues.
      - Medium severity → fraud_signals only (no block, no queue).
      - Re-run cron → idempotent (no duplicate signals).
    Step 9 — Create `test/ecosystem/fraud/fraud-review-queue.test.ts` (NEW):
      - enqueueFraudReview creates queue row.
      - clearFraudReviewItem flips queue + payout_credits status.
      - rejectFraudReviewItem flips queue + voids credit.
      - **Maturation (D-72):** new program with no prior cleared payout — getMaturationStatus returns matured=false; first credit gets fraud_review_status='holding'.
      - After 24h window elapsed (mock time) → matured=true; credit can clear.
      - Subsequent payouts on same program/partner skip maturation.
    Step 10 — Create `test/ecosystem/payouts/pricing-placeholder.test.ts` per prior plan.
    Step 11 — Run vitest.

    **CLAUDE.md doctrine:** Pricing Engine Canon is THE source-of-truth on placeholder rule. {{MARKOS_PRICING_ENGINE_PENDING}} is the EXACT string.
  </action>
  <verify>
    <automated>npx vitest run test/ecosystem/referrals/qualification-rule.test.ts test/ecosystem/fraud/fraud-evaluator.test.ts test/ecosystem/fraud/fraud-cron.test.ts test/ecosystem/fraud/fraud-review-queue.test.ts test/ecosystem/payouts/pricing-placeholder.test.ts</automated>
    Plus checks:
    - `grep -c "FRAUD_EVALUATORS\." lib/markos/ecosystem/referrals/fraud-evaluator.ts` returns ≥8
    - `grep -E "self_referral|ip_pattern_match|velocity_anomaly|ip_overlap|conversion_timing_anomaly|device_fingerprint_match|geo_velocity|account_age_anomaly" lib/markos/ecosystem/referrals/fraud-evaluator.ts` returns ≥8 distinct
    - **`grep "FRAUD_DETECTION_WINDOW_HOURS\s*=\s*24" lib/markos/ecosystem/referrals/fraud-evaluator.ts` returns ≥1 (RM2)**
    - **`grep "SLOW_FRAUD_WINDOW_DAYS\s*=\s*7" lib/markos/ecosystem/referrals/fraud-evaluator.ts` returns ≥1 (RM2)**
    - **`grep -E "severity.*===?\s*['\"]high['\"]|severity.*===?\s*['\"]critical['\"]" lib/markos/ecosystem/referrals/fraud-evaluator.ts` returns ≥1 (RM3 — both severities block)**
    - `grep "fraud_review_status\s*=\s*'blocked'" lib/markos/ecosystem/referrals/fraud-evaluator.ts` returns ≥1
    - `grep "enqueueFraudReview" lib/markos/ecosystem/referrals/fraud-evaluator.ts` returns ≥1 (RM3)
    - **`grep "MATURATION_WINDOW_HOURS\s*=\s*24" lib/markos/ecosystem/referrals/fraud-review-queue.ts` returns ≥1 (D-72 maturation)**
    - **`grep "getMaturationStatus" lib/markos/ecosystem/referrals/fraud-review-queue.ts` returns ≥1**
    - **`grep "'holding'" lib/markos/ecosystem/referrals/fraud-review-queue.ts` returns ≥1 (new fraud_review_status enum value)**
    - `grep -E "MARKOS_PRICING_ENGINE_PENDING" lib/markos/ecosystem/payouts/pricing-placeholder.ts` returns ≥2
    - `grep "RAW_COMMISSION_INPUT_FORBIDDEN" lib/markos/ecosystem/payouts/pricing-placeholder.ts` returns ≥1
    - **Behavior-first (RL2):** `node -e "const m=require('./lib/markos/ecosystem/referrals/fraud-review-queue.ts'); if(m.MATURATION_WINDOW_HOURS !== 24) process.exit(1);"` — exits 0
  </verify>
  <done>
    F-192 contract registered; 8 fraud evaluators implemented + tested with 24h+7d windows (D-72 / RM2); fraudDetectionCron blocks payout on BOTH high AND critical severities + enqueues human review (D-72 / RM3); maturation window holds first payout 24h; qualification_rule DSL evaluator; pricing placeholder enforced; A13 P205 fallback returns placeholder.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build payout-ledger.ts (D-70 lifecycle) + payout-export.ts (compliance gated — D-71/RH4) + payout-reconciliation.ts (D-70/RH3) + commission-evaluator.ts + referral/affiliate services + 7 tests</name>
  <read_first>
    - 227-CONTEXT.md D-22..D-24 (payout ledger + manual CSV + dispute), D-04 (affiliate commission models), D-44/D-45 (approval gate), **D-70 (payout state lifecycle / RH3), D-71 (compliance gate / RH4)**
    - 227-RESEARCH.md slice 227-04 spec
    - 227-REVIEWS.md RH3 — `paid` set too early; need `exported` intermediate; finance reconciliation required
    - 227-REVIEWS.md RH4 — payout governance gate; tenant compliance acknowledgement required
    - 227-RESEARCH.md §Pitfall 6 — multi-currency
    - migrations 162 + 160 (D-71 payout_compliance_acknowledged_at)
  </read_first>
  <files>
    contracts/F-193-ecosystem-payout-credit-v1.json
    lib/markos/ecosystem/referrals/referral-service.ts
    lib/markos/ecosystem/affiliates/affiliate-service.ts
    lib/markos/ecosystem/affiliates/commission-evaluator.ts
    lib/markos/ecosystem/payouts/payout-ledger.ts
    lib/markos/ecosystem/payouts/payout-export.ts
    lib/markos/ecosystem/payouts/payout-reconciliation.ts
    lib/markos/ecosystem/payouts/payout-compliance-gate.ts
    scripts/payout/reconcile.cjs
    test/ecosystem/affiliates/commission-evaluator.test.ts
    test/ecosystem/payouts/payout-ledger.test.ts
    test/ecosystem/payouts/payout-state-lifecycle.test.ts
    test/ecosystem/payouts/payout-dispute-flow.test.ts
    test/ecosystem/payouts/payout-export.test.ts
    test/ecosystem/payouts/payout-reconciliation.test.ts
    test/ecosystem/payouts/payout-compliance-gate.test.ts
  </files>
  <behavior>
    - **F-193 payout-credit contract:** Schema covering payout_credits + 6-status enum (D-70).
    - **referral-service.ts:** unchanged in shape; calls `guardCommissionInput`; validates currency in payout_settings.
    - **affiliate-service.ts:** unchanged in shape.
    - **commission-evaluator.ts:** unchanged — 4 models + currency preservation.
    - **payout-ledger.ts (D-70 / RH3 — REVISED):**
      - `PAYOUT_TRANSITIONS` const Record:
        - **earned → cleared (after fraud review pass + maturation) | voided**
        - **cleared → exported (when CSV generated) | disputed**
        - **exported → paid (only after finance reconciliation) | disputed**
        - **paid → disputed**
        - disputed → paid | voided
        - voided: terminal
      - `createPayoutCredit({tenant_id, partner_id?, profile_id?, program_kind, program_id, attribution_touch_id, amount_cents, currency})`: insert payout_credits row status='earned' fraud_review_status='pending'. Currency required.
      - `clearPayout(credit_id)`:
        1. Validates fraud_review_status='cleared' (set by fraud_evaluator).
        2. **Validates maturation:** calls `getMaturationStatus(program_id, partner_id)` — if not matured, sets fraud_review_status='holding' instead and returns without flipping status (D-72 maturation).
        3. Calls `createApprovalPackage('payout_credits_clear', credit_id, ...)`.
        4. After approval, flips status='cleared'. **Does NOT flip to 'paid' or 'exported' — that happens elsewhere.**
      - `voidPayout(credit_id, reason)`: terminal void.
      - `disputePayout(credit_id, dispute_reason)`: status='disputed'; emits operator task.
      - `resolvePayoutDispute(credit_id, resolution)`: 'paid' OR 'voided' (only path back to 'paid' from disputed).
    - **payout-compliance-gate.ts (D-71 / RH4 — NEW):**
      - `ACKNOWLEDGEMENT_TTL_MONTHS = 12`.
      - `assertPayoutComplianceAcknowledged(tenant_id)`: queries tenant-level compliance acknowledgement source. **Source-of-truth resolution:**
        1. Query `partner_profiles WHERE tenant_id=$1 AND payout_compliance_acknowledged_at IS NOT NULL AND payout_compliance_acknowledged_at > now() - INTERVAL '12 months'` — at least one acknowledged partner under tenant satisfies tenant-level gate.
        2. Alternative: if a tenant-level table exists (e.g., `tenants.payout_compliance_acknowledged_at`), prefer that; falls back to partner-level if not.
        3. If NEITHER → throw `COMPLIANCE_NOT_ACKNOWLEDGED:tenant=<id> — tenant admin must acknowledge payout compliance before any export. See /admin/payouts/compliance.`
      - This module is called from: payoutExportCron AND `/v1/ecosystem/payouts/export` API endpoint (227-07).
    - **payout-export.ts (D-70 / RH3 + D-71 / RH4 — REVISED):**
      - `payoutExportCron()`: weekly handler (Vercel Cron Sunday 04:00 UTC).
      - **Step 1 (D-71 / RH4 compliance gate):** For each tenant with cleared payouts pending export, call `assertPayoutComplianceAcknowledged(tenant_id)` — if throws, SKIP that tenant + emit operator task `payout_compliance_required` per skipped tenant. Continue with eligible tenants.
      - **Step 2:** Selects payout_credits WHERE status='cleared' AND payout_export_batch_id IS NULL AND tenant_id IN <compliance-acknowledged tenants>.
      - **Step 3 (D-70 / RH3):** generateExportCsv(credits) builds CSV; uploads to Supabase Storage; obtains signed URL; inserts payout_export_batches row status='exported' with csv_url + credit_count + total_cents + currency. Updates payout_credits.payout_export_batch_id + **status='exported'** (NOT 'paid' — RH3) + exported_at=now().
      - **No code path in payout-export.ts sets status='paid'.** Only payout-reconciliation.ts does.
      - Per-currency batches if multiple currencies (Pitfall 6).
      - csv_url NEVER logged.
    - **payout-reconciliation.ts (D-70 / RH3 — NEW):**
      - `reconcileBatch({batch_id, confirmed_credit_ids, reconciler_user_id, reconciliation_evidence?})`:
        1. Validate batch status='exported'.
        2. For each credit_id in confirmed_credit_ids: validate credit.payout_export_batch_id matches; flip credit.status='paid' + reconciled_at=now().
        3. For credits in batch NOT in confirmed_credit_ids: leave at 'exported' (allows partial reconciliation).
        4. If ALL credits in batch confirmed: flip batch.status='reconciled' + batch.reconciled_at + batch.reconciled_by_user_id.
        5. Emit audit row per credit + per batch.
      - `markCreditsPaid(credit_ids, reconciler_user_id)`: lower-level helper used by reconcileBatch.
    - **scripts/payout/reconcile.cjs (D-70 / RH3 — NEW CLI):**
      - Finance team CLI: `node scripts/payout/reconcile.cjs --batch <batch_id> --confirmed-credits <csv>` OR `--from-csv <reconciled-payouts.csv>`.
      - Parses confirmed credits; calls `reconcileBatch`.
      - Outputs JSON summary `{batch_id, confirmed_count, total_paid_cents, currency}`.
      - Auth: requires `MARKOS_FINANCE_TOKEN` env var (separate from user auth — finance role only).
  </behavior>
  <action>
    Step 1 — Create `contracts/F-193-ecosystem-payout-credit-v1.json` covering all 6-status enum (D-70).
    Step 2 — Create `lib/markos/ecosystem/referrals/referral-service.ts` per prior plan.
    Step 3 — Create `lib/markos/ecosystem/affiliates/affiliate-service.ts` per prior plan.
    Step 4 — Create `lib/markos/ecosystem/affiliates/commission-evaluator.ts` with 4 models.
    Step 5 — Create `lib/markos/ecosystem/payouts/payout-compliance-gate.ts` per behavior (D-71 / RH4).
    Step 6 — Create `lib/markos/ecosystem/payouts/payout-ledger.ts` with D-70 lifecycle + maturation gate (D-72) + clearPayout NEVER flips to 'paid'.
    Step 7 — Create `lib/markos/ecosystem/payouts/payout-export.ts` with:
      - assertPayoutComplianceAcknowledged call FIRST per tenant
      - status='exported' (NOT 'paid') after CSV gen
      - per-currency batching
      - csv_url never logged
    Step 8 — Create `lib/markos/ecosystem/payouts/payout-reconciliation.ts` per behavior.
    Step 9 — Create `scripts/payout/reconcile.cjs` CLI per behavior.
    Step 10 — Create 7 test files per behavior:
      - `test/ecosystem/affiliates/commission-evaluator.test.ts` — 4 models + currency preservation per prior plan.
      - `test/ecosystem/payouts/payout-ledger.test.ts` — createPayoutCredit currency mandatory; clearPayout requires fraud cleared + maturation; void/dispute paths.
      - **`test/ecosystem/payouts/payout-state-lifecycle.test.ts` (NEW — D-70 / RH3):**
        - State machine: earned → cleared → exported → paid (each transition gated correctly).
        - Blocked: earned → exported (skip cleared) rejected.
        - Blocked: cleared → paid (skip exported) rejected — RH3 PROOF.
        - exported → disputed → paid via reconcileDispute.
        - exported → reconciliation → paid (via payout-reconciliation.markCreditsPaid).
      - `test/ecosystem/payouts/payout-dispute-flow.test.ts` per prior plan.
      - **`test/ecosystem/payouts/payout-export.test.ts` (REVISED — RH3):**
        - Setup tenant with payout_compliance_acknowledged_at = now() - 30 days.
        - Insert 5 cleared credits.
        - Run payoutExportCron.
        - Assert: status='exported' on all credits (NOT 'paid' — RH3).
        - Assert: payout_export_batches.status='exported'.
        - **Critical assertion:** `grep -c "status='paid'" lib/markos/ecosystem/payouts/payout-export.ts` returns 0 (RH3 — no path sets paid here).
        - csv_url never logged.
        - Re-run cron → idempotent.
      - **`test/ecosystem/payouts/payout-reconciliation.test.ts` (NEW — D-70 / RH3):**
        - Setup batch in 'exported' state with 5 credits all 'exported'.
        - Call reconcileBatch with all 5 confirmed.
        - Assert: all credits status='paid' + reconciled_at set; batch.status='reconciled'.
        - Partial reconciliation: confirm only 3 of 5 → 3 'paid', 2 stay 'exported', batch stays 'exported'.
        - reconcileBatch on non-exported batch → throws.
      - **`test/ecosystem/payouts/payout-compliance-gate.test.ts` (NEW — D-71 / RH4):**
        - Tenant with payout_compliance_acknowledged_at=NULL → assertPayoutComplianceAcknowledged throws COMPLIANCE_NOT_ACKNOWLEDGED.
        - Tenant with acknowledged_at=now()-13mo → throws (stale beyond TTL).
        - Tenant with acknowledged_at=now()-6mo → passes.
        - payoutExportCron: 2 tenants — one acknowledged, one not. Cron processes acknowledged tenant only; emits operator task `payout_compliance_required` for unacknowledged tenant.
    Step 11 — Run vitest.
  </action>
  <verify>
    <automated>npx vitest run test/ecosystem/affiliates/commission-evaluator.test.ts test/ecosystem/payouts/payout-ledger.test.ts test/ecosystem/payouts/payout-state-lifecycle.test.ts test/ecosystem/payouts/payout-dispute-flow.test.ts test/ecosystem/payouts/payout-export.test.ts test/ecosystem/payouts/payout-reconciliation.test.ts test/ecosystem/payouts/payout-compliance-gate.test.ts</automated>
    Plus checks:
    - `grep -E "flat_rate|tiered|sliding_scale|performance_bonus" lib/markos/ecosystem/affiliates/commission-evaluator.ts` returns ≥4 distinct
    - `grep "PAYOUT_TRANSITIONS" lib/markos/ecosystem/payouts/payout-ledger.ts` returns ≥2
    - **`grep -E "['\"]exported['\"]" lib/markos/ecosystem/payouts/payout-ledger.ts` returns ≥2 (D-70 lifecycle)**
    - `grep "createApprovalPackage.*payout_credits_clear" lib/markos/ecosystem/payouts/payout-ledger.ts` returns ≥1
    - `grep -E "currency" lib/markos/ecosystem/payouts/payout-ledger.ts` returns ≥3
    - **`grep -c "status.*['\"]paid['\"]\|status\s*=\s*'paid'" lib/markos/ecosystem/payouts/payout-export.ts` returns 0 (RH3 — CRITICAL: no path sets paid in export)**
    - **`grep -E "['\"]exported['\"]" lib/markos/ecosystem/payouts/payout-export.ts` returns ≥2 (D-70 — sets exported)**
    - **`grep "assertPayoutComplianceAcknowledged" lib/markos/ecosystem/payouts/payout-export.ts` returns ≥1 (D-71 / RH4 gate)**
    - **`grep -E "status.*['\"]paid['\"]" lib/markos/ecosystem/payouts/payout-reconciliation.ts` returns ≥1 (D-70 — reconciliation IS the path that flips to paid)**
    - **`grep "COMPLIANCE_NOT_ACKNOWLEDGED" lib/markos/ecosystem/payouts/payout-compliance-gate.ts` returns ≥1 (D-71 / RH4)**
    - **`grep "ACKNOWLEDGEMENT_TTL_MONTHS\s*=\s*12" lib/markos/ecosystem/payouts/payout-compliance-gate.ts` returns ≥1 (D-71 — 12mo TTL)**
    - `grep "createSignedUrl\|signed.*url" lib/markos/ecosystem/payouts/payout-export.ts` returns ≥1
    - `grep -c "createApprovalPackage" lib/markos/ecosystem/referrals/referral-service.ts lib/markos/ecosystem/affiliates/affiliate-service.ts lib/markos/ecosystem/payouts/payout-ledger.ts` returns ≥3
    - `ls scripts/payout/reconcile.cjs` returns 1 file (D-70 finance CLI)
    - **Behavior-first (RL2):** `node -e "const m=require('./lib/markos/ecosystem/payouts/payout-compliance-gate.ts'); if(m.ACKNOWLEDGEMENT_TTL_MONTHS !== 12) process.exit(1); if(typeof m.assertPayoutComplianceAcknowledged !== 'function') process.exit(2);"` — exits 0
  </verify>
  <done>
    F-193 contract registered; commission-evaluator covers 4 models with currency preservation; **payout-ledger D-70 state machine (earned→cleared→exported→paid) tested + RH3 proven (paid only via reconciliation)**; payoutExportCron compliance-gated per D-71 / RH4 (skips unacknowledged tenants); payout-reconciliation.ts is the SOLE path to 'paid'; finance CLI script created; per-currency batches; signed URLs not logged; idempotent on re-run; approval gates fire for program activation + payout clear.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| referrer/affiliate user → payout credit | Self-referral and velocity attacks must trigger fraud signals |
| commission_share input → payout amount | Direct user input forbidden — must bind to P205 (D-65) |
| payout CSV signed URL | URL leak exposes payout amounts; never log |
| multi-currency tenant → payout export | USD assumption breaks cross-border tenants (Pitfall 6) |
| CSV generation → payout finalization | RH3 — `paid` is settlement, not export; reconciliation is required between |
| tenant → payout disbursement | RH4 — compliance acknowledgement gate before any export |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-227-04-01 | Tampering | Self-referral fraud | mitigate | FRAUD_EVALUATORS.self_referral → severity='critical'; payout suspension via fraud_review_status='blocked'; tested in fraud-cron.test.ts |
| T-227-04-02 | Tampering | commission_share manipulation | mitigate | guardCommissionInput throws RAW_COMMISSION_INPUT_FORBIDDEN if commission_share without rev_share_terms_id; D-65 |
| T-227-04-03 | Information Disclosure | Payout CSV URL leakage | mitigate | csv_url is signed URL with 1h expiry; NEVER logged |
| T-227-04-04 | Tampering | Payout state bypass (skip fraud review or maturation) | mitigate | clearPayout requires fraud_review_status='cleared' AND maturation passed; createApprovalPackage gates final flip |
| T-227-04-05 | Tampering | Multi-currency conversion error | mitigate | Pitfall 6 — currency mandatory; per-currency batches |
| T-227-04-06 | Spoofing | False positive fraud (B2B NAT) | mitigate | Pitfall 5 — ip_overlap → medium; per-program fraud_controls disable; fraud_review_queue manual review |
| T-227-04-07 | Repudiation | Payout dispute audit gap | mitigate | All dispute state changes append to markos_audit_log |
| T-227-04-08 | Information Disclosure | Pricing Engine drift | mitigate | resolveCommissionRate falls back to PRICING_PENDING_PLACEHOLDER on missing/draft P205 |
| T-227-04-09 | Tampering | Payout marked 'paid' without settlement | mitigate | RH3 / D-70 — payout-export.ts NEVER sets 'paid'; only payout-reconciliation.ts after finance confirms; lifecycle test proves blocked transitions |
| T-227-04-10 | Elevation | Tenant exports payouts without legal/tax compliance | mitigate | RH4 / D-71 — assertPayoutComplianceAcknowledged at every export entry point; unacknowledged tenants skipped + operator task |
| T-227-04-11 | Tampering | Slow-fraud pattern evades 1h window | mitigate | RM2 / D-72 — 24h default + 7d slow pass detection windows |
| T-227-04-12 | Tampering | High-severity fraud clears into payout | mitigate | RM3 / D-72 — both `high` AND `critical` block payout pending fraud_review_queue clearance |
| T-227-04-13 | Tampering | First-payout immediate fraud (no maturation) | mitigate | D-72 maturation — first cleared payout per program/partner held 24h |
</threat_model>

<verification>
- F-192 + F-193 contracts cover all migration 162 columns + D-70 status enum.
- 8 fraud evaluators with positive + negative tests; 24h+7d windows (RM2).
- High AND critical severities block payout (RM3).
- Maturation 24h window for first program payout (D-72).
- 4 commission models tested; currency preserved.
- **Payout state machine: earned → cleared → exported → paid (D-70 / RH3) — `paid` only via reconciliation.**
- **payoutExportCron: compliance-gated per D-71 / RH4.**
- payout-reconciliation.ts is sole path to 'paid'; finance CLI script provided.
- Per-currency batching; signed URLs not logged; idempotent.
- {{MARKOS_PRICING_ENGINE_PENDING}} placeholder enforced; commission_share never raw input.
- Approval gates fire for program activation + payout_credits_clear.
- B2B NAT carve-out (ip_overlap = medium not critical).
- Per-program fraud_controls disable specific evaluators.
- Legacy regression `npm test` green.
</verification>

<success_criteria>
- 2 contracts (F-192 + F-193) registered.
- 11 service modules under `lib/markos/ecosystem/{referrals,affiliates,payouts}/` (added fraud-review-queue + payout-reconciliation + payout-compliance-gate).
- 1 finance CLI script (scripts/payout/reconcile.cjs).
- 12 vitest test files green.
- Pricing placeholder string matches Pricing Engine Canon: `{{MARKOS_PRICING_ENGINE_PENDING}}`.
- D-23: NO Stripe Connect / KYC / 1099 — manual CSV only.
- D-65: commission_share never accepted from raw input.
- D-70 / RH3: payout-export.ts has 0 paths setting `paid`; only payout-reconciliation.ts does.
- D-71 / RH4: payoutExportCron + export API enforces compliance gate.
- D-72 / RM2-RM3: 24h+7d fraud windows; high+critical block; maturation 24h.
- Per-currency batching honored.
- 227-VALIDATION.md per-task map updated for slice 227-04.
- Legacy regression green.
</success_criteria>

<output>
After completion, create `.planning/phases/227-ecosystem-partner-community-developer-growth/227-04-SUMMARY.md` with:
- F-192 + F-193 contract allocation
- 8 fraud evaluators + 24h/7d windows + maturation + high+critical block (RM2/RM3)
- 4 commission models + currency preservation
- **D-70 / RH3 payout state machine — paid only via reconciliation**
- **D-71 / RH4 compliance gate — payoutExportCron skips unacknowledged tenants**
- Finance reconciliation CLI evidence
- Manual CSV export per-currency batching + signed URL boundary
- Pricing placeholder enforcement evidence
- Test results (12 files green)
- A13 P205 fallback path
</output>
</content>
</invoke>