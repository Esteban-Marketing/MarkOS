---
phase: 227-ecosystem-partner-community-developer-growth
plan: 04
type: execute
wave: 3
depends_on: ["227-01", "227-03"]
files_modified:
  - contracts/F-192-ecosystem-fraud-signal-v1.json
  - contracts/F-193-ecosystem-payout-credit-v1.json
  - lib/markos/ecosystem/referrals/referral-service.ts
  - lib/markos/ecosystem/referrals/qualification-rule.ts
  - lib/markos/ecosystem/referrals/fraud-evaluator.ts
  - lib/markos/ecosystem/affiliates/affiliate-service.ts
  - lib/markos/ecosystem/affiliates/commission-evaluator.ts
  - lib/markos/ecosystem/payouts/payout-ledger.ts
  - lib/markos/ecosystem/payouts/payout-export.ts
  - lib/markos/ecosystem/payouts/pricing-placeholder.ts
  - test/ecosystem/referrals/qualification-rule.test.ts
  - test/ecosystem/fraud/fraud-evaluator.test.ts
  - test/ecosystem/fraud/fraud-cron.test.ts
  - test/ecosystem/affiliates/commission-evaluator.test.ts
  - test/ecosystem/payouts/payout-ledger.test.ts
  - test/ecosystem/payouts/payout-dispute-flow.test.ts
  - test/ecosystem/payouts/payout-export.test.ts
  - test/ecosystem/payouts/pricing-placeholder.test.ts
autonomous: true
requirements: [ECO-02, ECO-05, SG-04, SG-09, SG-11, SG-12, QA-01, QA-02, QA-03, QA-04, QA-05, QA-06, QA-07, QA-08, QA-09, QA-10, QA-11, QA-12, QA-13, QA-14, QA-15]
must_haves:
  truths:
    - "Referral qualification_rule evaluator returns deterministic pass/fail per program rule"
    - "Fraud rule engine has pluggable evaluators per signal_kind (8 kinds) — critical severity → operator task + payout suspension"
    - "Affiliate commission evaluator supports flat_rate + tiered + sliding_scale + performance_bonus models"
    - "payout_credits state machine: earned → cleared (via fraud review) → paid | voided | disputed"
    - "Manual CSV export weekly Sunday 04:00 UTC writes payout_export_batches audit row + signed Supabase Storage URL"
    - "{{MARKOS_PRICING_ENGINE_PENDING}} placeholder enforced on every payout-rate / commission_share copy path"
    - "commission_share never sourced from raw user input — bound to P205 PricingRecommendation OR placeholder (D-65)"
  artifacts:
    - path: "lib/markos/ecosystem/referrals/referral-service.ts"
      provides: "Referral program CRUD + activation gate + qualification eval invocation"
      exports: ["createReferralProgram", "activateReferralProgram", "evaluateReferral"]
    - path: "lib/markos/ecosystem/referrals/qualification-rule.ts"
      provides: "qualification_rule JSONB DSL evaluator (mirrors P225 decision_rules pattern)"
      exports: ["evaluateQualificationRule", "QualificationRuleSchema"]
    - path: "lib/markos/ecosystem/referrals/fraud-evaluator.ts"
      provides: "Pluggable evaluator per fraud signal_kind + hourly detection cron + critical-severity action"
      exports: ["FRAUD_EVALUATORS", "evaluateFraud", "fraudDetectionCron", "FRAUD_SIGNAL_KINDS"]
    - path: "lib/markos/ecosystem/affiliates/affiliate-service.ts"
      provides: "Affiliate program CRUD + activation + cookie tracking metadata"
      exports: ["createAffiliateProgram", "activateAffiliateProgram"]
    - path: "lib/markos/ecosystem/affiliates/commission-evaluator.ts"
      provides: "Commission tier evaluator (flat/tiered/sliding/performance)"
      exports: ["evaluateCommission", "COMMISSION_MODELS"]
    - path: "lib/markos/ecosystem/payouts/payout-ledger.ts"
      provides: "payout_credits CRUD + state machine + dispute flow"
      exports: ["createPayoutCredit", "clearPayout", "voidPayout", "disputePayout", "resolvePayoutDispute", "PAYOUT_TRANSITIONS"]
    - path: "lib/markos/ecosystem/payouts/payout-export.ts"
      provides: "Weekly CSV export cron + payout_export_batches audit + Supabase Storage signed URL"
      exports: ["payoutExportCron", "generateExportCsv"]
    - path: "lib/markos/ecosystem/payouts/pricing-placeholder.ts"
      provides: "{{MARKOS_PRICING_ENGINE_PENDING}} resolver + commission_share guard"
      exports: ["resolveCommissionRate", "PRICING_PENDING_PLACEHOLDER", "guardCommissionInput"]
  key_links:
    - from: "lib/markos/ecosystem/payouts/payout-ledger.ts"
      to: "lib/markos/crm/copilot.ts::createApprovalPackage"
      via: "Approval Inbox entry payout_credits_clear (D-45/D-51)"
      pattern: "createApprovalPackage.*payout_credits_clear"
    - from: "lib/markos/ecosystem/referrals/fraud-evaluator.ts"
      to: "fraud_signals + payout_credits"
      via: "Critical severity → fraud_signals row + payout_credits.fraud_review_status='blocked'"
      pattern: "fraud_review_status\\s*=\\s*'blocked'|severity.*critical"
    - from: "lib/markos/ecosystem/payouts/pricing-placeholder.ts"
      to: "lib/markos/pricing/recommendation.ts (P205) or placeholder"
      via: "resolve via P205 OR {{MARKOS_PRICING_ENGINE_PENDING}}; never raw user input"
      pattern: "MARKOS_PRICING_ENGINE_PENDING"
  key_links_extended:
    - from: "lib/markos/ecosystem/referrals/fraud-evaluator.ts"
      to: "P225 decision_rules engine"
      via: "Mirrors decision_rules pluggable evaluator pattern (D-21)"
      pattern: "FRAUD_EVALUATORS\\["
---

<objective>
Build referral programs + affiliate programs + fraud detection engine + payout credits ledger + manual CSV export + pricing placeholder enforcement (227-RESEARCH.md slice 227-04). Wave 3 — parallel with 227-05.

Purpose: This slice owns SG-04 (B2C/PLG growth: viral/referral fraud + payout safety) and ECO-02 (partner/affiliate motions are measurable, fraud-aware, payout-safe, approval-gated). Critical fidelity to D-23 (manual CSV — NO Stripe Connect v1), D-65 (commission_share NEVER from raw input — bound to P205), {{MARKOS_PRICING_ENGINE_PENDING}} on every payout copy path.

Output: 2 contracts (F-192/F-193), 8 service modules, 8 vitest test files covering fraud rule engine (8 signal_kinds), commission evaluator (4 models), payout state machine (5 statuses + dispute), CSV export batch audit, pricing placeholder enforcement.
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-CONTEXT.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-RESEARCH.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-VALIDATION.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-01-PLAN.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-03-PLAN.md
@.planning/phases/205-pricing-engine-foundation-billing-readiness/205-CONTEXT.md
@.planning/phases/211-content-social-revenue-loop/211-CONTEXT.md
@.planning/phases/225-analytics-attribution-narrative-intelligence/225-CONTEXT.md
@CLAUDE.md

# Pricing Engine Canon: obsidian/brain/Pricing Engine Canon.md is THE source-of-truth on placeholder rule.
# Incoming docs (CLAUDE.md precedence rule 5): obsidian/work/incoming/{18,22,25}-*.md INFORMATIONAL ONLY.

<interfaces>
From 227-01 (lib/markos/ecosystem/contracts.ts):
```typescript
export const FRAUD_SIGNAL_KIND = ['self_referral','ip_pattern_match','velocity_anomaly','ip_overlap','conversion_timing_anomaly','device_fingerprint_match','geo_velocity','account_age_anomaly'] as const;
export const FRAUD_SEVERITY = ['low','medium','high','critical'] as const;
export const FRAUD_STATUS = ['detected','reviewing','confirmed','dismissed'] as const;
export const PAYOUT_CREDIT_STATUS = ['earned','cleared','paid','voided','disputed'] as const;
export const PAYOUT_FRAUD_REVIEW_STATUS = ['pending','cleared','blocked'] as const;
export const COMMISSION_MODEL = ['flat_rate','tiered','sliding_scale','performance_bonus'] as const;
export const AFFILIATE_PROGRAM_STATUS = ['draft','active','paused','archived'] as const;
```

From 227-03 (lib/markos/ecosystem/partners/partner-service.ts):
```typescript
export function enableReferral(partner_id: string): Promise<void>; // emits Approval Inbox entry
export function enableAffiliate(partner_id: string): Promise<void>; // emits Approval Inbox entry
```

From P205 (A13 — soft ref; if P205 not yet shipped, fall back to placeholder):
```typescript
// lib/markos/pricing/recommendation.ts (P205) or stub if absent
export interface PricingRecommendation { id: string; rate: number; currency: string; status: 'approved' | 'draft'; }
export function getApprovedRecommendation(id: string): Promise<PricingRecommendation | null>;
```

From P225 decision_rules (A22 — soft ref; reused pattern for fraud evaluators):
```typescript
// Pattern reference only — fraud-evaluator.ts mirrors structure but is independent module
export type Evaluator<TInput, TOutput> = (input: TInput) => TOutput;
```

From migrations 162 (227-01):
```sql
-- affiliate_programs (commission_model, attribution_window_days, cookie_duration_days, fraud_controls, payout_settings)
-- fraud_signals (program_kind, signal_kind, severity, status)
-- payout_credits (status, fraud_review_status, currency, amount_cents)
-- payout_export_batches (csv_url, status)
```

From migration 161 (227-01) — referral_programs extended columns (business_mode, scope, fraud_controls, qualification_rule JSONB, payout_settings JSONB, approval_required, partner_id).

From obsidian/brain/Pricing Engine Canon.md (canonical rule): unresolved pricing/payout copy uses `{{MARKOS_PRICING_ENGINE_PENDING}}` until approved P205 PricingRecommendation exists.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build fraud-evaluator.ts + fraudDetectionCron (8 signal kinds + critical severity action) + payout pricing-placeholder.ts + qualification-rule.ts</name>
  <read_first>
    - 227-CONTEXT.md D-20..D-24 (fraud_signals + payout suspension on critical), D-65 (commission_share never from raw input), D-46 (content classifier reuse for partner-facing materials)
    - 227-RESEARCH.md slice 227-04 spec (lines 843-851) — fraud rule engine + decision_rules pattern + hourly cron + critical → operator task + suspension
    - 227-RESEARCH.md §Pitfall 5 (lines 935-939) — false positive tuning + per-program fraud_controls override + manual review
    - 227-CONTEXT.md D-23 — manual CSV; {{MARKOS_PRICING_ENGINE_PENDING}} placeholder; NO Stripe Connect / KYC / 1099
    - obsidian/brain/Pricing Engine Canon.md placeholder rule
    - 227-RESEARCH.md §Phase Requirements → Test Map: ECO-02 fraud rule engine 8 signal_kinds, SG-09 fraud cron critical → operator task, SG-11 placeholder enforcement
    - 227-VALIDATION.md slice 227-04 row + Coverage Targets "Fraud rule engine 100% per signal_kind"
  </read_first>
  <files>
    contracts/F-192-ecosystem-fraud-signal-v1.json
    lib/markos/ecosystem/referrals/qualification-rule.ts
    lib/markos/ecosystem/referrals/fraud-evaluator.ts
    lib/markos/ecosystem/payouts/pricing-placeholder.ts
    test/ecosystem/referrals/qualification-rule.test.ts
    test/ecosystem/fraud/fraud-evaluator.test.ts
    test/ecosystem/fraud/fraud-cron.test.ts
    test/ecosystem/payouts/pricing-placeholder.test.ts
  </files>
  <behavior>
    - **F-192 fraud-signal contract:** Schema covering fraud_signals table columns + 8 signal_kinds + 4 severity levels + 4 status values; type='read-write', actor='Operator'.
    - **qualification-rule.ts:**
      - `QualificationRuleSchema` Zod schema for the JSONB DSL: `{ kind: 'all'|'any'|'none', conditions: Array<{ field: string, op: 'eq'|'gte'|'lte'|'in'|'between', value: any }> }` + nested groups.
      - `evaluateQualificationRule(rule, context)`: pure function; returns `{ qualified: bool, reasons: string[] }`. Pluggable per-field resolver (e.g., `account_age_days`, `total_spend_cents`, `region`, `customer_360_id_present`). Reuses P225 decision_rules DSL pattern (D-21 + 225-CONTEXT D-18..D-19).
    - **fraud-evaluator.ts:**
      - `FRAUD_EVALUATORS: Record<FraudSignalKind, Evaluator>` — one evaluator per signal_kind (8 evaluators):
        - `self_referral`: referrer_profile_id === referred_profile_id → severity='critical'.
        - `ip_pattern_match`: same ip_hash for >5 referrals in 24h → 'high'; >20 → 'critical'.
        - `velocity_anomaly`: >10 referrals in 1h from same referrer → 'high'.
        - `ip_overlap`: referrer_ip_hash === referred_ip_hash → 'medium' (B2B NAT carve-out per Pitfall 5: only `medium`, not `critical`).
        - `conversion_timing_anomaly`: conversion <60s after referral click → 'medium'.
        - `device_fingerprint_match`: same device_fingerprint hash → 'high'.
        - `geo_velocity`: ip_country_referrer ≠ ip_country_referred AND <5 minutes → 'medium'.
        - `account_age_anomaly`: account created <24h before referral → 'medium'.
      - `evaluateFraud(referralRecord, programConfig)`: runs ALL evaluators in `programConfig.fraud_controls[]`; returns array of detected fraud signals. Per-program config can disable specific evaluators (Pitfall 5 — B2B carve-out).
      - `fraudDetectionCron()`: hourly handler. Iterates active referral_programs + affiliate_programs; runs evaluateFraud against recent attribution_touches (last 1 hour). Inserts fraud_signals rows. **For severity='critical':** (1) emit operator task `fraud_signal_critical_review`; (2) update related payout_credits.fraud_review_status='blocked'.
    - **pricing-placeholder.ts:**
      - `PRICING_PENDING_PLACEHOLDER = '{{MARKOS_PRICING_ENGINE_PENDING}}'` exported const.
      - `resolveCommissionRate(rev_share_terms_id?)`: if rev_share_terms_id present → call `getApprovedRecommendation(rev_share_terms_id)` from P205 (A13 fallback if P205 absent: returns null + placeholder); if null OR status !== 'approved' → return PRICING_PENDING_PLACEHOLDER; else return numeric rate.
      - `guardCommissionInput(input)`: throws `RAW_COMMISSION_INPUT_FORBIDDEN` if commission_share is set without rev_share_terms_id (D-65). Used by every API endpoint that accepts commission input.
  </behavior>
  <action>
    Step 1 — Create `contracts/F-192-ecosystem-fraud-signal-v1.json` covering all migration 162 fraud_signals columns + 8 signal_kinds + 4 severity values + 4 status values.
    Step 2 — Create `lib/markos/ecosystem/referrals/qualification-rule.ts`:
      - Define QualificationRuleSchema Zod
      - `evaluateQualificationRule(rule, context)` recursive evaluator (all/any/none groups + leaf conditions)
      - Pluggable field resolvers
    Step 3 — Create `lib/markos/ecosystem/referrals/fraud-evaluator.ts`:
      - 8 named evaluators in FRAUD_EVALUATORS
      - `evaluateFraud(record, config)` returns array of `{ signal_kind, severity, evidence }`
      - `fraudDetectionCron()` hourly handler
      - On severity='critical': emit operator task + UPDATE payout_credits SET fraud_review_status='blocked' WHERE attribution_touch_id=...
    Step 4 — Create `lib/markos/ecosystem/payouts/pricing-placeholder.ts`:
      - `PRICING_PENDING_PLACEHOLDER` exported const string '{{MARKOS_PRICING_ENGINE_PENDING}}'
      - `resolveCommissionRate(rev_share_terms_id)` returns numeric or placeholder
      - `guardCommissionInput(input)` throws RAW_COMMISSION_INPUT_FORBIDDEN
      - A13 fallback: if `getApprovedRecommendation` import fails or returns null, fall back to PRICING_PENDING_PLACEHOLDER
    Step 5 — Create `test/ecosystem/referrals/qualification-rule.test.ts`:
      - Simple all-condition rule: account_age_days >= 30 AND total_spend_cents >= 10000 → qualified pass/fail cases
      - Any-group rule: region IN ['US','CA'] OR partner_id IS NOT NULL
      - None-group rule: NOT (account_age_days < 7) → block fresh accounts
      - Edge: empty rule → qualified=true (no conditions)
    Step 6 — Create `test/ecosystem/fraud/fraud-evaluator.test.ts`:
      - Test all 8 evaluators with positive case (signal detected) + negative case (no signal). 16 test cases minimum.
      - Self-referral: referrer===referred → severity='critical'.
      - ip_pattern_match: 6 records with same ip_hash → severity='high'; 21 → 'critical'.
      - ip_overlap (Pitfall 5): same ip → severity='medium' NOT critical (B2B carve-out).
      - Per-program config disabled evaluator: even if signal present, NOT evaluated.
    Step 7 — Create `test/ecosystem/fraud/fraud-cron.test.ts`:
      - Insert attribution_touch rows + matching referral records that trigger self_referral.
      - Run `fraudDetectionCron()`.
      - Assert fraud_signals row inserted with signal_kind='self_referral', severity='critical'.
      - Assert operator task emitted (mock task system).
      - Assert payout_credits.fraud_review_status='blocked' for related touch.
      - Re-run cron; assert no duplicate fraud_signal (idempotency via unique key or composite (program_kind, program_id, attribution_touch_id, signal_kind)).
    Step 8 — Create `test/ecosystem/payouts/pricing-placeholder.test.ts`:
      - resolveCommissionRate(undefined) → PRICING_PENDING_PLACEHOLDER.
      - resolveCommissionRate(invalid_id) → PRICING_PENDING_PLACEHOLDER (P205 returns null).
      - resolveCommissionRate(approved_id) (mock returning {rate:0.10, status:'approved'}) → 0.10.
      - resolveCommissionRate(draft_id) (mock returning status:'draft') → PRICING_PENDING_PLACEHOLDER.
      - guardCommissionInput({commission_share: 0.20}) → throws RAW_COMMISSION_INPUT_FORBIDDEN.
      - guardCommissionInput({commission_share: 0.20, rev_share_terms_id: 'xyz'}) → no throw.
      - guardCommissionInput({}) → no throw.
    Step 9 — Run vitest.

    **CLAUDE.md doctrine:** Pricing Engine Canon is THE source-of-truth on the placeholder rule. Per CLAUDE.md precedence rule 1 (product doctrine), placeholder is non-negotiable. {{MARKOS_PRICING_ENGINE_PENDING}} is the EXACT string per Pricing Engine Canon and 211-CONTEXT.md.
  </action>
  <verify>
    <automated>npx vitest run test/ecosystem/referrals/qualification-rule.test.ts test/ecosystem/fraud/fraud-evaluator.test.ts test/ecosystem/fraud/fraud-cron.test.ts test/ecosystem/payouts/pricing-placeholder.test.ts</automated>
    Plus checks:
    - `grep -c "FRAUD_EVALUATORS\." lib/markos/ecosystem/referrals/fraud-evaluator.ts` returns ≥8 (one entry per signal_kind)
    - `grep -E "self_referral|ip_pattern_match|velocity_anomaly|ip_overlap|conversion_timing_anomaly|device_fingerprint_match|geo_velocity|account_age_anomaly" lib/markos/ecosystem/referrals/fraud-evaluator.ts` returns ≥8 distinct matches
    - `grep "fraud_review_status\s*=\s*'blocked'" lib/markos/ecosystem/referrals/fraud-evaluator.ts` returns ≥1 (critical → block payout)
    - `grep -E "MARKOS_PRICING_ENGINE_PENDING" lib/markos/ecosystem/payouts/pricing-placeholder.ts` returns ≥2 (const + return path)
    - `grep "RAW_COMMISSION_INPUT_FORBIDDEN" lib/markos/ecosystem/payouts/pricing-placeholder.ts` returns ≥1
    - `grep "QualificationRuleSchema\|evaluateQualificationRule" lib/markos/ecosystem/referrals/qualification-rule.ts` returns ≥3
  </verify>
  <done>
    F-192 contract registered; 8 fraud evaluators implemented + tested (16+ cases); fraudDetectionCron blocks payout on critical severity + emits operator task + idempotent; qualification_rule DSL evaluator (all/any/none groups); pricing placeholder enforced via PRICING_PENDING_PLACEHOLDER + guardCommissionInput; A13 P205 fallback returns placeholder when recommendation absent.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build payout-ledger.ts + payout-export.ts + commission-evaluator.ts + referral/affiliate services + 4 tests</name>
  <read_first>
    - 227-CONTEXT.md D-22..D-24 (payout ledger + manual CSV + dispute flow), D-04 (affiliate commission models), D-44/D-45 (approval gate payout_credits_clear)
    - 227-RESEARCH.md slice 227-04 spec — referral CRUD + affiliate CRUD + payout state machine + manual CSV cron weekly Sunday 04:00 UTC
    - 227-RESEARCH.md §Pitfall 6 (lines 941-944) — payout currency multi-tenant: payout_credits.currency mandatory, never assume USD
    - 227-RESEARCH.md schema sketch payout_export_batches + signed CSV URL never logged
    - 227-VALIDATION.md slice 227-04 row + Coverage Targets "Payout credits ledger 100% — earned → cleared → paid + dispute"
    - Open Question 4 (RESEARCH line 1208) — Supabase Storage signed URL pattern
    - migrations 162 payout_credits + payout_export_batches + affiliate_programs columns
  </read_first>
  <files>
    contracts/F-193-ecosystem-payout-credit-v1.json
    lib/markos/ecosystem/referrals/referral-service.ts
    lib/markos/ecosystem/affiliates/affiliate-service.ts
    lib/markos/ecosystem/affiliates/commission-evaluator.ts
    lib/markos/ecosystem/payouts/payout-ledger.ts
    lib/markos/ecosystem/payouts/payout-export.ts
    test/ecosystem/affiliates/commission-evaluator.test.ts
    test/ecosystem/payouts/payout-ledger.test.ts
    test/ecosystem/payouts/payout-dispute-flow.test.ts
    test/ecosystem/payouts/payout-export.test.ts
  </files>
  <behavior>
    - **F-193 payout-credit contract:** Schema covering payout_credits columns; type='read-write', actor='Operator'.
    - **referral-service.ts:**
      - `createReferralProgram({tenant_id, partner_id?, business_mode='ecosystem', scope, fraud_controls, payout_settings, qualification_rule, approval_required=true})`: inserts referral_programs row with status='draft'. Validates payout_settings.currency required (Pitfall 6). Calls `guardCommissionInput` if any commission-bearing field present.
      - `activateReferralProgram(program_id)`: D-45 always approval-required → emits createApprovalPackage('partner_referral_program_activate', program_id, evidence_refs). Status flip to 'active' only after approval (227-07 API).
      - `evaluateReferral(referral_record, program)`: runs `evaluateQualificationRule(program.qualification_rule, referral_record)`; if qualified → calls `payout-ledger.createPayoutCredit({...})`; if not qualified → no credit.
    - **affiliate-service.ts:**
      - `createAffiliateProgram({tenant_id, partner_id, commission_model, commission_terms, attribution_window_days=90, cookie_duration_days=30, tier_thresholds, fraud_controls, payout_settings})`: insert affiliate_programs row status='draft'. Validate currency; guard commission input.
      - `activateAffiliateProgram(program_id)`: D-45 approval-required → emit createApprovalPackage('affiliate_program_activate', ...).
    - **commission-evaluator.ts:**
      - `evaluateCommission({commission_model, commission_terms, tier_thresholds, sale_amount_cents, performance_metrics?})`: returns `{ commission_amount_cents, currency, model_applied, tier_applied? }`.
      - `flat_rate`: commission_amount = sale_amount × commission_terms.rate.
      - `tiered`: tier_thresholds={ thresholds:[{min_volume:0, rate:0.05},{min_volume:10000, rate:0.07},...] } — find applicable tier by sale_amount.
      - `sliding_scale`: continuous function; commission_terms specifies start_rate, end_rate, scale_function.
      - `performance_bonus`: base rate + bonus when performance_metrics meet criteria (e.g., conversion_rate >= 0.05).
      - All return amounts honor currency (never default USD); per Pitfall 6, currency comes from payout_settings.
    - **payout-ledger.ts:**
      - `PAYOUT_TRANSITIONS` const Record:
        - earned → cleared (after fraud review pass) | voided
        - cleared → paid (after CSV export confirms) | disputed
        - paid → disputed
        - disputed → paid | voided
        - voided: terminal
      - `createPayoutCredit({tenant_id, partner_id?, profile_id?, program_kind, program_id, attribution_touch_id, amount_cents, currency})`: insert payout_credits row status='earned' fraud_review_status='pending'. Currency required (Pitfall 6).
      - `clearPayout(credit_id)`: validates fraud_review_status='cleared' (set by fraud_evaluator.ts when no critical signals OR by operator manual review); calls `createApprovalPackage('payout_credits_clear', credit_id, ...)`; only flips status='cleared' after approval.
      - `voidPayout(credit_id, reason)`: terminal void; appends audit row.
      - `disputePayout(credit_id, dispute_reason)`: status='disputed'; emits operator task.
      - `resolvePayoutDispute(credit_id, resolution)`: resolution='paid' OR 'voided'.
    - **payout-export.ts:**
      - `payoutExportCron()`: weekly handler (Vercel Cron Sunday 04:00 UTC). Selects payout_credits WHERE status='cleared' AND payout_export_batch_id IS NULL.
      - `generateExportCsv(credits)`: builds CSV string (headers: credit_id, partner_id, profile_id, program_kind, amount_cents, currency, earned_at, attribution_touch_id). Uploads to Supabase Storage; obtains signed URL with 1-hour expiry; inserts payout_export_batches row with csv_url + credit_count + total_cents + currency. Updates payout_credits.payout_export_batch_id + status='paid'. CSV URL NEVER logged.
      - **Per-currency batches:** if multiple currencies in cleared credits, generate separate CSVs per currency (Pitfall 6); separate payout_export_batches rows per currency.
  </behavior>
  <action>
    Step 1 — Create `contracts/F-193-ecosystem-payout-credit-v1.json` covering all payout_credits + payout_export_batches columns.
    Step 2 — Create `lib/markos/ecosystem/referrals/referral-service.ts` per behavior. Each function validates currency present in payout_settings.
    Step 3 — Create `lib/markos/ecosystem/affiliates/affiliate-service.ts` similar pattern.
    Step 4 — Create `lib/markos/ecosystem/affiliates/commission-evaluator.ts` with 4 evaluators.
    Step 5 — Create `lib/markos/ecosystem/payouts/payout-ledger.ts` with state machine + dispute flow + approval gate on `clearPayout`.
    Step 6 — Create `lib/markos/ecosystem/payouts/payout-export.ts` with cron + CSV generation + Supabase Storage signed URL (use `supabase.storage.from('payout-exports').createSignedUrl(path, 3600)`); per-currency batching; never log csv_url.
    Step 7 — Create `test/ecosystem/affiliates/commission-evaluator.test.ts`:
      - flat_rate: 0.10 of $100 → $10. EUR currency preserved.
      - tiered: thresholds 0=5%, 10000=7%, 50000=10%; $25000 sale → 7% = $1750.
      - sliding_scale: linear 5%→10% over $0→$100k; $50k → 7.5%.
      - performance_bonus: base 5% + 2% bonus if conversion_rate >= 0.05.
      - Currency preserved in all returns.
    Step 8 — Create `test/ecosystem/payouts/payout-ledger.test.ts`:
      - createPayoutCredit with currency='EUR' → row stored with EUR.
      - createPayoutCredit without currency → throws (Pitfall 6).
      - State machine: earned → cleared (with approval mock) → paid; assert createApprovalPackage('payout_credits_clear', ...) called.
      - earned → voided (skip cleared) → terminal.
      - cleared → disputed → resolvePayoutDispute('paid') → paid.
      - Blocked transitions: earned → paid (skip cleared) rejected.
    Step 9 — Create `test/ecosystem/payouts/payout-dispute-flow.test.ts`:
      - paid → disputed → paid (resolved in favor of partner)
      - paid → disputed → voided (resolved against partner)
      - disputed → voided directly
    Step 10 — Create `test/ecosystem/payouts/payout-export.test.ts`:
      - Insert 5 payout_credits status='cleared' with currency='USD'; 3 with currency='EUR'.
      - Run `payoutExportCron()`.
      - Assert 2 payout_export_batches rows (one per currency).
      - Assert csv_url is signed URL (mock Supabase Storage; assert createSignedUrl called).
      - Assert payout_credits.status='paid' + payout_export_batch_id set.
      - Assert csv_url NEVER appears in any console.log / audit_log message body (search captured logs).
      - Re-run cron; assert no duplicate batch (idempotency via WHERE payout_export_batch_id IS NULL).
    Step 11 — Run vitest.
  </action>
  <verify>
    <automated>npx vitest run test/ecosystem/affiliates/commission-evaluator.test.ts test/ecosystem/payouts/payout-ledger.test.ts test/ecosystem/payouts/payout-dispute-flow.test.ts test/ecosystem/payouts/payout-export.test.ts</automated>
    Plus checks:
    - `grep -E "flat_rate|tiered|sliding_scale|performance_bonus" lib/markos/ecosystem/affiliates/commission-evaluator.ts` returns ≥4 distinct matches
    - `grep "PAYOUT_TRANSITIONS" lib/markos/ecosystem/payouts/payout-ledger.ts` returns ≥2
    - `grep "createApprovalPackage.*payout_credits_clear" lib/markos/ecosystem/payouts/payout-ledger.ts` returns ≥1
    - `grep -E "currency" lib/markos/ecosystem/payouts/payout-ledger.ts` returns ≥3 (currency mandatory)
    - `grep "createSignedUrl\|signed.*url" lib/markos/ecosystem/payouts/payout-export.ts` returns ≥1
    - `grep -c "createApprovalPackage" lib/markos/ecosystem/referrals/referral-service.ts lib/markos/ecosystem/affiliates/affiliate-service.ts lib/markos/ecosystem/payouts/payout-ledger.ts` returns ≥3 (3 approval gates: program activate × 2 + payout clear)
  </verify>
  <done>
    F-193 contract registered; commission-evaluator covers 4 models with currency preservation; payout-ledger state machine + dispute flow tested; payoutExportCron generates per-currency batches with signed URLs not logged; idempotent on re-run; approval gates fire for program activation + payout clear.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| referrer/affiliate user → payout credit | Self-referral and velocity attacks must trigger fraud signals |
| commission_share input → payout amount | Direct user input forbidden — must bind to P205 (D-65) |
| payout CSV signed URL | URL leak exposes payout amounts; never log |
| multi-currency tenant → payout export | USD assumption breaks cross-border tenants (Pitfall 6) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-227-04-01 | Tampering | Self-referral fraud | mitigate | FRAUD_EVALUATORS.self_referral → severity='critical'; payout suspension via fraud_review_status='blocked'; tested in fraud-cron.test.ts |
| T-227-04-02 | Tampering | commission_share manipulation | mitigate | guardCommissionInput throws RAW_COMMISSION_INPUT_FORBIDDEN if commission_share without rev_share_terms_id; D-65 |
| T-227-04-03 | Information Disclosure | Payout CSV URL leakage | mitigate | csv_url is signed URL with 1h expiry; NEVER logged; payout-export.test.ts verifies no log emission |
| T-227-04-04 | Tampering | Payout state bypass (skip fraud review) | mitigate | clearPayout requires fraud_review_status='cleared'; createApprovalPackage('payout_credits_clear') gates final flip |
| T-227-04-05 | Tampering | Multi-currency conversion error | mitigate | Pitfall 6 — currency mandatory in payout_settings; per-currency batches in CSV export |
| T-227-04-06 | Spoofing | False positive fraud (B2B NAT) | mitigate | Pitfall 5 — ip_overlap → severity='medium' not critical; per-program fraud_controls disable; manual review queue |
| T-227-04-07 | Repudiation | Payout dispute audit gap | mitigate | All dispute state changes append to markos_audit_log + payout_credits state machine row |
| T-227-04-08 | Information Disclosure | Pricing Engine drift | mitigate | resolveCommissionRate falls back to PRICING_PENDING_PLACEHOLDER on missing/draft P205 record |
</threat_model>

<verification>
- F-192 + F-193 contracts cover all migration 162 columns.
- 8 fraud evaluators implemented with positive + negative tests (16+ cases).
- 4 commission models tested; currency preserved.
- Payout state machine: 5 statuses + dispute flow.
- payoutExportCron: per-currency batching; signed URLs not logged; idempotent.
- {{MARKOS_PRICING_ENGINE_PENDING}} placeholder enforced; commission_share never raw input.
- Approval gates fire for program activation + payout_credits_clear.
- B2B NAT carve-out (ip_overlap = medium not critical).
- Per-program fraud_controls disable specific evaluators.
- Legacy regression `npm test` green.
</verification>

<success_criteria>
- 2 contracts (F-192 + F-193) registered.
- 8 service modules under `lib/markos/ecosystem/{referrals,affiliates,payouts}/`.
- 8 vitest test files green.
- Pricing placeholder string matches Pricing Engine Canon exactly: `{{MARKOS_PRICING_ENGINE_PENDING}}`.
- D-23: NO Stripe Connect / KYC / 1099 — manual CSV only (verified by absence of stripe imports in payout-export.ts).
- D-65: commission_share never accepted from raw input — guardCommissionInput throws.
- D-46: content classifier reuse not directly invoked here; partner-facing copy generation lands in 227-07.
- Per-currency batching honored in payoutExportCron.
- 227-VALIDATION.md per-task map updated for slice 227-04.
- Legacy regression green.
</success_criteria>

<output>
After completion, create `.planning/phases/227-ecosystem-partner-community-developer-growth/227-04-SUMMARY.md` with:
- F-192 + F-193 contract allocation
- 8 fraud evaluators + critical-severity payout suspension behavior
- 4 commission models + currency preservation
- Payout state machine + dispute flow
- Manual CSV export per-currency batching + signed URL boundary
- Pricing placeholder enforcement evidence
- Test results (8 files green)
- A13 P205 fallback path (placeholder when P205 absent)
</output>
