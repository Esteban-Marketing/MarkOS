---
phase: 227-ecosystem-partner-community-developer-growth
plan: 01
type: execute
wave: 1
depends_on: []
preconditions:
  required_upstream_phases: [220, 221, 225]
  required_tables:
    - partner_profiles      # P220 — ALTER TABLE target (migration 160)
    - referral_programs     # P220 — ALTER TABLE target (migration 161)
    - community_profiles    # P220 — ALTER TABLE target (migration 161)
    - marketing_events      # P220 — ALTER TABLE target (migration 161)
    - partnerships          # P220 — ALTER TABLE target (migration 161)
  required_files:
    - supabase/migrations/158_*.sql  # P226 last migration baseline
    - contracts/F-180-*.json         # P226 last contract baseline
  fail_mode: HARD_FAIL                # Per RH2 — no silent skip; route back to upstream phase
files_modified:
  - supabase/migrations/159_integration_listings_listing_views_install_requests.sql
  - supabase/migrations/160_p220_partner_profiles_alter_ecosystem.sql
  - supabase/migrations/161_p220_referral_community_events_partnerships_alter.sql
  - supabase/migrations/162_affiliate_programs_fraud_signals_payout_credits.sql
  - supabase/migrations/163_community_signals_developer_events.sql
  - supabase/migrations/164_certification_records.sql
  - supabase/migrations/165_co_sell_opportunities.sql
  - contracts/F-181-ecosystem-integration-listing-v1.json
  - contracts/F-182-ecosystem-partner-profile-v1.json
  - contracts/F-183-ecosystem-certification-record-v1.json
  - contracts/F-184-ecosystem-referral-program-v1.json
  - contracts/F-185-ecosystem-affiliate-program-v1.json
  - contracts/F-186-ecosystem-community-signal-v1.json
  - contracts/F-187-ecosystem-developer-event-v1.json
  - contracts/F-188-ecosystem-co-sell-opportunity-v1.json
  - lib/markos/ecosystem/contracts.ts
  - lib/markos/ecosystem/adapters/plugin-registry.ts
  - scripts/preconditions/227-01-check-upstream.cjs
  - test/fixtures/ecosystem/integration-listing.ts
  - test/fixtures/ecosystem/partner-profile.ts
  - test/fixtures/ecosystem/certification-record.ts
  - test/fixtures/ecosystem/referral-program.ts
  - test/fixtures/ecosystem/affiliate-program.ts
  - test/fixtures/ecosystem/community-signal.ts
  - test/fixtures/ecosystem/developer-event.ts
  - test/fixtures/ecosystem/co-sell-opportunity.ts
  - test/fixtures/ecosystem/fraud-signal.ts
  - test/fixtures/ecosystem/payout-credit.ts
  - test/fixtures/ecosystem/install-request.ts
  - test/fixtures/ecosystem/listing-view.ts
  - test/ecosystem/schema/integration-listings.test.ts
  - test/ecosystem/schema/partner-profiles-extended.test.ts
  - test/ecosystem/schema/certification-records.test.ts
  - test/ecosystem/schema/affiliate-programs.test.ts
  - test/ecosystem/schema/community-signals.test.ts
  - test/ecosystem/schema/developer-events.test.ts
  - test/ecosystem/schema/co-sell-opportunities.test.ts
  - test/ecosystem/schema/fraud-signals.test.ts
  - test/ecosystem/schema/payout-credits.test.ts
  - test/ecosystem/schema/install-requests.test.ts
  - test/ecosystem/schema/listing-views.test.ts
  - test/ecosystem/schema/payout-export-batches.test.ts
  - test/ecosystem/schema/p220-backfill.test.ts
  - test/ecosystem/schema/rls-isolation.test.ts
  - test/ecosystem/schema/rls-policies-correctness.test.ts
  - test/ecosystem/adapters/plugin-registry.test.ts
autonomous: true
requirements: [ECO-01, ECO-02, ECO-03, ECO-04, ECO-05, SG-04, SG-06, SG-09, SG-10, SG-11, SG-12, QA-01, QA-02, QA-03, QA-04, QA-05, QA-06, QA-07, QA-08, QA-09, QA-10, QA-11, QA-12, QA-13, QA-14, QA-15]
must_haves:
  truths:
    - "F-180 + migration 158 baseline verified before any new allocation"
    - "P220/P221/P225 dependency precheck HARD-FAILS if upstream tables missing (route-back to upstream phase) — per D-69"
    - "All 12 new SOR tables exist with RLS enforced AND initial policies are CORRECT (not stub) — per D-73 / RL1"
    - "P220 partner_profiles + referral_programs + community_profiles + marketing_events + partnerships ALTER TABLE additive — every existing P220 row has business_mode='saas' after backfill"
    - "Plugin registry adapter calls resolvePlugin(registry, pluginId) per real registry API at lib/markos/plugins/registry.js:102 — never mutates registry — per D-68"
    - "Adapter accepts a registry instance argument (no global singleton — registry shape verified)"
    - "All 8 base contracts F-181..F-188 registered with required JSON Schema fields"
    - "12 fixture factories importable from test/ecosystem/* and test/api/* for downstream slices"
  artifacts:
    - path: "scripts/preconditions/227-01-check-upstream.cjs"
      provides: "HARD-FAIL precheck for P220/P221/P225 upstream tables (D-69)"
      contains: "MISSING_UPSTREAM_PHASE"
    - path: "supabase/migrations/159_integration_listings_listing_views_install_requests.sql"
      provides: "integration_listings + listing_views + install_requests SOR + RLS + indexes"
      contains: "CREATE TABLE integration_listings"
    - path: "supabase/migrations/160_p220_partner_profiles_alter_ecosystem.sql"
      provides: "P220 partner_profiles ecosystem extension columns + backfill"
      contains: "ALTER TABLE partner_profiles"
    - path: "supabase/migrations/161_p220_referral_community_events_partnerships_alter.sql"
      provides: "P220 referral_programs + community_profiles + marketing_events + partnerships extensions"
      contains: "ALTER TABLE referral_programs"
    - path: "supabase/migrations/162_affiliate_programs_fraud_signals_payout_credits.sql"
      provides: "affiliate_programs + fraud_signals + payout_credits + payout_export_batches (incl. 'exported' status — D-70)"
      contains: "CREATE TABLE affiliate_programs"
    - path: "supabase/migrations/163_community_signals_developer_events.sql"
      provides: "community_signals (UNIQUE source+source_message_id) + developer_events"
      contains: "CREATE TABLE community_signals"
    - path: "supabase/migrations/164_certification_records.sql"
      provides: "certification_records + polymorphic subject + indexes"
      contains: "CREATE TABLE certification_records"
    - path: "supabase/migrations/165_co_sell_opportunities.sql"
      provides: "co_sell_opportunities + indexes"
      contains: "CREATE TABLE co_sell_opportunities"
    - path: "lib/markos/ecosystem/contracts.ts"
      provides: "Ecosystem TypeScript contracts (enum constants + zod schemas)"
      exports: ["LISTING_CATEGORY", "OWNER_TYPE", "CERTIFICATION_STATE", "LISTING_STATUS", "PRICING_MODEL", "PARTNER_TYPE", "CERTIFICATION_LEVEL", "ACTIVE_STATUS", "BUSINESS_MODE", "COMMISSION_MODEL", "AFFILIATE_PROGRAM_STATUS", "COMMUNITY_SOURCE", "COMMUNITY_SENTIMENT", "COMMUNITY_URGENCY", "ROUTED_ACTION_KIND", "DEVELOPER_EVENT_KIND", "FRAUD_SIGNAL_KIND", "FRAUD_SEVERITY", "FRAUD_STATUS", "PAYOUT_FRAUD_REVIEW_STATUS", "PAYOUT_CREDIT_STATUS", "INSTALL_REQUEST_STATUS", "COSELL_STATUS"]
    - path: "lib/markos/ecosystem/adapters/plugin-registry.ts"
      provides: "Read-through adapter wrapping resolvePlugin(registry, pluginId) (D-09..D-11 + D-68)"
      exports: ["createPluginManifestReader", "getPluginManifest", "PluginManifestRef"]
  key_links:
    - from: "lib/markos/ecosystem/adapters/plugin-registry.ts"
      to: "lib/markos/plugins/registry.js"
      via: "resolvePlugin(registry, pluginId) — read-through only — per D-68"
      pattern: "resolvePlugin"
    - from: "supabase/migrations/160_p220_partner_profiles_alter_ecosystem.sql"
      to: "partner_profiles (P220)"
      via: "ALTER TABLE additive with DEFAULT 'saas' backfill"
      pattern: "ALTER TABLE partner_profiles\\s+ADD COLUMN"
    - from: "test/ecosystem/schema/p220-backfill.test.ts"
      to: "partner_profiles + referral_programs + community_profiles + marketing_events + partnerships"
      via: "assert: SELECT count(*) WHERE business_mode IS NULL must be 0"
      pattern: "business_mode IS NULL"
---

<objective>
Lay the schema + contracts + fixtures + plugin-registry adapter foundation for Phase 227 Ecosystem Engine. Wave 1 — no parallel siblings; everything else depends on this.

Purpose: Every downstream slice (227-02..227-07) needs (1) the 12 new SOR tables with INITIAL CORRECT RLS policies (RL1: not deferred to Plan 07 hardening), (2) the 5 P220 ALTER TABLE extensions backfilled to business_mode='saas', (3) base TypeScript contracts F-181..F-188, (4) ecosystem module skeleton + plugin-registry read-through adapter wired to the REAL `resolvePlugin(registry, pluginId)` API (D-68 / RH1), (5) 12 fixture factories. Without this slice, no other slice can compile.

**Hard-fail posture (RH2 / D-69):** This plan HARD-FAILS at preflight if P220 or P221 or P225 upstream tables are missing. There is NO silent skip / no warning-and-continue. Failure → exit 1 with `MISSING_UPSTREAM_PHASE:<phase>` and route-back to that phase.

Output: 1 precondition script (HARD-FAIL guard), 7 migrations (159..165), 8 contract JSON files (F-181..F-188), TypeScript contract module, plugin-registry adapter (registry-instance-aware), 12 fixture factories, 14 schema test files, 1 RLS isolation test file, 1 RLS-policies-correctness test file (RL1).
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-CONTEXT.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-RESEARCH.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-VALIDATION.md
@.planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-CONTEXT.md
@.planning/phases/221-cdp-identity-audience-consent-substrate/221-CONTEXT.md
@.planning/phases/222-crm-timeline-commercial-memory-workspace/222-CONTEXT.md
@.planning/phases/225-analytics-attribution-narrative-intelligence/225-CONTEXT.md
@.planning/phases/226-sales-enablement-deal-execution/226-CONTEXT.md
@.planning/V4.0.0-TESTING-ENVIRONMENT-PLAN.md
@.planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md
@obsidian/reference/Database Schema.md
@obsidian/reference/Contracts Registry.md
@CLAUDE.md

# NOTE on incoming docs (CLAUDE.md source-of-truth precedence rule 5):
# obsidian/work/incoming/{18,22,25}-*.md are INFORMATIONAL ONLY (raw intake).
# They are NOT canonical. Canon = obsidian/brain/* + obsidian/reference/*.

<interfaces>
<!-- Existing code anchors needed by this plan. VERIFIED via Read of lib/markos/plugins/registry.js. -->

From lib/markos/plugins/registry.js (P52/P202; preserved per D-09..D-11; ACTUAL exports verified line-by-line):
```javascript
// File: lib/markos/plugins/registry.js (CommonJS module.exports)
// Map-backed runtime registry; frozen manifests; opaque shape { plugins: Map<string, object> }
function createRegistry() // → { plugins: Map<string, object> }
function registerPlugin(manifest, opts = {}) // throws INVALID_PLUGIN_CONTRACT
function listPlugins(registry) // → frozen object[]
function resolvePlugin(registry, pluginId) // → frozen manifest or null  ← THIS IS THE READ SEAM
module.exports = { createRegistry, registerPlugin, listPlugins, resolvePlugin };
```

From lib/markos/plugins/loader.js (boot-time; consumed by host app):
```javascript
function loadPlugins(manifests) // → { registry, failures }
module.exports = { loadPlugins };
```

**CRITICAL — RH1 / D-68 verification:**
- `lookupPlugin` does NOT exist as an export. Any reference to it is a fictional API.
- `resolvePlugin` REQUIRES TWO arguments: `(registry, pluginId)`. It is NOT a global singleton.
- The registry is a value built by `loadPlugins(manifests)` at boot — there is no module-level singleton in registry.js.
- The ecosystem adapter MUST therefore accept a registry instance (or a getter for it) and pass it through to `resolvePlugin`.

DO NOT modify registry.js — adapter reads through, never writes.

From P220 SaaS-mode tables (must already exist; A25 — now HARD-FAILED via precondition guard):
```sql
-- partner_profiles, referral_programs, community_profiles, marketing_events, partnerships
-- (P220-shape baseline — preserve all rows; ALTER TABLE additive only)
```
If precondition fails (P220 tables missing), Task 1 EXITS with `MISSING_UPSTREAM_PHASE:P220`. No silent skip.

From P221 cdp_identity_profiles (A3 — referenced by community_signals.related_profile_id, developer_events.profile_id, install_requests.requested_by_profile_id):
```sql
-- Referenced as soft FK with ON DELETE SET NULL behavior; D-62 tombstone scrub at app layer
```
Precondition: cdp_identity_profiles must exist (P221). HARD-FAIL otherwise.

From P222 opportunities (A4 — referenced by co_sell_opportunities.opportunity_id):
```sql
-- Soft FK; if P222 not yet shipped, FK constraint omitted with TODO comment in migration 165
-- (P222 is NOT in the hard-required preconditions; co_sell_opportunities can be created without P222 — only the FK is deferred)
```

From P225 attribution_touches (A23 — soft FK from fraud_signals.attribution_touch_id, payout_credits.attribution_touch_id; ALTER lands in 227-06 migration 167):
```sql
-- Soft refs in 227-01 migrations 162; FK formalized in 167 (227-06)
-- attribution_touches MUST exist (P225) — HARD-FAIL precondition
```

From P226 deal_rooms + handoff_records + winloss_records (A27 — soft FKs in co_sell_opportunities columns):
```sql
-- Soft refs only in migration 165; FK constraints omitted if P226 not yet shipped
-- (P226 is NOT in hard-required preconditions; soft refs only)
```

From 226-RESEARCH.md F-ID + migration tables (A1/A2 baseline):
```text
P226 ends at F-180; P227 starts at F-181 (verified)
P226 ends at migration 158; P227 starts at migration 159 (verified)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Precondition HARD-FAIL guard (D-69) + verify F-180 + migration 158 baseline; create migrations 159-165 (7 SQL files) with INITIAL CORRECT RLS policies (RL1)</name>
  <read_first>
    - 227-RESEARCH.md §F-ID + Migration Allocation (lines 134-200) — verified ranges F-181..F-198 + migrations 159..171
    - 227-CONTEXT.md D-01..D-08 (object model) + D-12..D-14 (P220 ALTER TABLE) + D-22..D-23 (payout_export_batches) + D-59 (RLS) + D-66..D-67 (allocation) + **D-68 (resolvePlugin) + D-69 (HARD-FAIL preconditions) + D-70 (payout state lifecycle: 'exported' status)** + D-73 (RLS initial correctness)
    - 227-RESEARCH.md §Schema Sketches (lines 461-808) — exact CREATE TABLE + ALTER TABLE SQL
    - 226-RESEARCH.md §F-ID table — confirm P226 last F-ID = F-180; last migration = 158
    - obsidian/reference/Database Schema.md — RLS pattern + tenant isolation policy
    - V4.0.0-TESTING-ENVIRONMENT-PLAN.md — schema test conventions
    - lib/markos/plugins/registry.js (read first 115 lines) — verify resolvePlugin signature before any adapter work
  </read_first>
  <files>
    scripts/preconditions/227-01-check-upstream.cjs
    supabase/migrations/159_integration_listings_listing_views_install_requests.sql
    supabase/migrations/160_p220_partner_profiles_alter_ecosystem.sql
    supabase/migrations/161_p220_referral_community_events_partnerships_alter.sql
    supabase/migrations/162_affiliate_programs_fraud_signals_payout_credits.sql
    supabase/migrations/163_community_signals_developer_events.sql
    supabase/migrations/164_certification_records.sql
    supabase/migrations/165_co_sell_opportunities.sql
    test/ecosystem/schema/rls-policies-correctness.test.ts
  </files>
  <behavior>
    - **Precondition guard (D-69 / RH2 — HARD-FAIL, no silent skip):**
      - `scripts/preconditions/227-01-check-upstream.cjs` connects via Supabase service-role client; queries `information_schema.tables` for: partner_profiles, referral_programs, community_profiles, marketing_events, partnerships (P220), cdp_identity_profiles, cdp_consent_states (P221), attribution_touches (P225).
      - Missing P220 table → exit 1 with stderr `MISSING_UPSTREAM_PHASE:P220 (table=<name>) — execute /gsd-execute-phase 220 first`.
      - Missing P221 table → exit 1 with `MISSING_UPSTREAM_PHASE:P221`.
      - Missing P225 `attribution_touches` → exit 1 with `MISSING_UPSTREAM_PHASE:P225`.
      - Success → stdout JSON `{ "preconditions": "OK", "verified": [...] }` + exit 0.
      - Script invoked at the START of Task 1 (not in CI) — execution gating, not advisory.
    - **A1/A2 baseline check:** Confirm `supabase/migrations/158_*.sql` is the highest existing P226 migration. Confirm no `contracts/F-181-*.json` or `supabase/migrations/159_*.sql` exists yet. If either fails, STOP and surface the conflict.
    - **Migration 159 (D-01 + D-38 + D-39):** Create `integration_listings` with category/owner_type/certification_state/listing_status/pricing_model CHECKs, listing_url_slug UNIQUE, plugin_manifest_id as text (no DB FK per D-10), tenant_id NULL allowed for markos-global; `listing_views` with ip_hash + user_agent_hash; `install_requests` with status enum + audit_chain JSONB. **RLS (initial correctness — RL1 / D-73):** policies are FINAL, not stub:
      - integration_listings: SELECT/UPDATE/INSERT/DELETE policies — `tenant_id IS NULL OR tenant_id = current_setting('app.active_tenant_id', true)::uuid`. INSERT policy enforces tenant_id matches setting (or NULL only by service-role).
      - listing_views: SELECT/INSERT scoped via JOIN to listing tenant; analytics service-role bypass documented in policy comment.
      - install_requests: tenant_id REQUIRED; RLS `tenant_id = current_setting('app.active_tenant_id', true)::uuid` on all 4 verbs.
      Indexes: `idx_listings_public` partial WHERE status='active' AND certification_state='certified'; `idx_listings_slug`; FTS `idx_listings_fts` USING gin(to_tsvector('english', name||' '||description)).
    - **Migration 160 (D-02 + D-12):** ALTER TABLE partner_profiles ADD COLUMN IF NOT EXISTS for: business_mode (NOT NULL DEFAULT 'saas' CHECK IN saas/commerce/ecosystem/all), partner_type (CHECK 8 values), region, specialization_tags text[] DEFAULT '{}', certification_level (DEFAULT 'registered'), active_status (DEFAULT 'active'), co_sell_enabled bool DEFAULT false, referral_enabled bool DEFAULT false, affiliate_enabled bool DEFAULT false, public_directory_visible bool DEFAULT false, commission_share_default numeric(5,4), revenue_attribution_partner_id uuid (self-FK to partner_profiles.partner_id), **payout_compliance_acknowledged_at timestamptz NULL (D-71 — payout governance gate; tenant admin must explicitly set via 227-04 acknowledgement flow)**. Backfill via DEFAULT 'saas' on ADD COLUMN. RLS already on P220; preserve. Index `idx_partner_profiles_business_mode` on (tenant_id, business_mode); partial `idx_partner_profiles_public` WHERE public_directory_visible=true. **NOTE:** payout_compliance_acknowledged_at lives at PARTNER level (per-partner), not tenant level — supports per-partner compliance attestation; D-71 also adds tenant-level field via separate column on tenants if available, otherwise uses an admin-acknowledgement table created in 227-04.
    - **Migration 161 (D-03 + D-12):** ALTER TABLE referral_programs ADD: business_mode (DEFAULT 'saas'), scope (CHECK tenant/marketplace), fraud_controls text[] DEFAULT '{}', payout_settings JSONB DEFAULT '{}', qualification_rule JSONB DEFAULT '{}', approval_required bool DEFAULT true, partner_id uuid (FK partner_profiles, nullable). Same pattern for community_profiles (+business_mode +scope), marketing_events (+business_mode), partnerships (+business_mode +co_sell_enabled). All DEFAULT 'saas' so existing P220 rows backfill automatically.
    - **Migration 162 (D-04 + D-20 + D-22 + D-23 + D-70 payout state lifecycle + D-72 fraud queue):**
      - Create `affiliate_programs` (commission_model CHECK 4 values, attribution_window_days DEFAULT 90, cookie_duration_days DEFAULT 30, status DEFAULT 'draft').
      - Create `fraud_signals` (program_kind CHECK referral/affiliate, signal_kind CHECK 8 values, severity CHECK low/medium/high/critical, status CHECK detected/reviewing/confirmed/dismissed).
      - Create `payout_credits` with **expanded status lifecycle (D-70 / RH3):** `status CHECK (status IN ('earned','cleared','exported','paid','voided','disputed'))`. Lifecycle: earned → cleared → **exported** → paid; with disputed/voided branches. `fraud_review_status CHECK pending/cleared/blocked/holding` (D-72 adds 'holding' for maturation), `currency NOT NULL DEFAULT 'USD'`, **`exported_at timestamptz NULL`** (set when CSV generated), **`reconciled_at timestamptz NULL`** (set when finance confirms — only after which status='paid').
      - Create `payout_export_batches` (status CHECK 'pending'/'exported'/'reconciled'/'failed' — D-70; csv_url text; `reconciled_at timestamptz NULL`; `reconciled_by_user_id uuid NULL`).
      - Create `fraud_review_queue` (D-72 / RM2-RM3): `queue_id uuid pk, tenant_id, fraud_signal_id uuid (FK), payout_credit_id uuid (FK nullable), severity, status CHECK 'pending_review'/'cleared'/'rejected', queued_at, reviewed_at, reviewed_by_user_id`. Holds high+critical until human review (RM3).
      RLS tenant-scoped on all 5 tables (initial correctness; FINAL policies). Indexes: `idx_fraud_signals_program` (tenant_id, program_kind, program_id, status); `idx_fraud_signals_severity` partial WHERE status IN ('detected','reviewing'); `idx_payout_credits_status` (tenant_id, status, fraud_review_status); `idx_fraud_review_queue_pending` partial WHERE status='pending_review'.
    - **Migration 163 (D-05 + D-06):** Create `community_signals` with `UNIQUE (source, source_message_id)` for dedupe; source CHECK 8 values; sentiment + urgency CHECKs; routed_action_kind CHECK 6 values nullable; `consent_state_check_passed boolean NOT NULL DEFAULT false`; raw_payload JSONB. Create `developer_events` with event_kind CHECK 7 values; profile_id soft FK; source_event_ref UUID. RLS allows tenant_id IS NULL for markos-global. Indexes per behavior section above.
    - **Migration 164 (D-07):** Create `certification_records` with subject_type CHECK listing/partner; subject_id uuid (polymorphic, no DB FK); state CHECK 6 values; criteria_checks JSONB DEFAULT '{}'; expires_at, revoked_at, recertification_due_at, **recertification_warned_at** timestamptz. RLS allows markos-global. Indexes per behavior.
    - **Migration 165 (D-08):** Create `co_sell_opportunities` with opportunity_id (soft FK comment); partner_id (soft FK partner_profiles); commission_share numeric(5,4); rev_share_terms_id (soft FK P205); launch_surface_id (soft FK P224); partner_dealroom_id + handoff_record_id + winloss_record_id (soft FKs P226); status CHECK 6 values; attribution_touch_ids uuid[] DEFAULT '{}'; **commission_locked_at timestamptz NULL**. RLS tenant-scoped. **DB-level commission immutability trigger (D-74 / RM4):** see Plan 06 migration 165 modification — actually trigger is added in Plan 06's migration if a separate file is needed; for this plan we add the column ONLY (trigger creation is a Plan 06 concern in migration 167-companion). Indexes per behavior.
    - **NOTE for Plan 06 (forward reference):** RM4 DB-level commission immutability is enforced via trigger; trigger DDL lives in Plan 06's migration that ALTERs co_sell_opportunities or via a sibling migration. This plan only creates the column.
    - All migrations use `CREATE TABLE IF NOT EXISTS` and `ADD COLUMN IF NOT EXISTS` for re-run safety. Every new table includes `ENABLE ROW LEVEL SECURITY` + tenant policy. No raw IP/user-agent stored — only ip_hash + user_agent_hash columns where applicable (D-39).
    - **No scope simplification:** Every column from CONTEXT.md D-01..D-08 + D-70/D-71/D-72 must appear in migrations.
    - **RLS initial correctness (RL1 / D-73):** Plan 07's migration 170 is for STRICTER policy hardening (e.g., role-scoped policies, defense-in-depth). The policies created here pass cross-tenant denial tests independently of migration 170.
  </behavior>
  <action>
    Step 0 — **HARD-FAIL precondition guard (D-69 / RH2):** Create `scripts/preconditions/227-01-check-upstream.cjs`:
    ```javascript
    #!/usr/bin/env node
    const { createClient } = require('@supabase/supabase-js');
    const REQUIRED = {
      P220: ['partner_profiles','referral_programs','community_profiles','marketing_events','partnerships'],
      P221: ['cdp_identity_profiles','cdp_consent_states'],
      P225: ['attribution_touches']
    };
    (async () => {
      const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY);
      for (const [phase, tables] of Object.entries(REQUIRED)) {
        for (const t of tables) {
          const { data, error } = await supabase.from('information_schema.tables').select('table_name').eq('table_schema','public').eq('table_name', t);
          if (error) { console.error(`PRECHECK_QUERY_FAILED:${error.message}`); process.exit(1); }
          if (!data || data.length === 0) { console.error(`MISSING_UPSTREAM_PHASE:${phase} (table=${t}) — execute /gsd-execute-phase ${phase.replace('P','')} first`); process.exit(1); }
        }
      }
      console.log(JSON.stringify({ preconditions: 'OK', verified: REQUIRED }));
      process.exit(0);
    })().catch(e => { console.error('PRECHECK_FATAL:', e); process.exit(1); });
    ```
    Run `node scripts/preconditions/227-01-check-upstream.cjs` — must exit 0 before proceeding to Step 1.
    Step 1 — Baseline guard: Run `ls supabase/migrations/158_*.sql contracts/F-180-*.json 2>&1 | tail -5` and `ls supabase/migrations/159_* contracts/F-181-* 2>&1 | head -5`. Migration 158 + F-180 must exist; 159 + F-181 must NOT exist. If conflict, abort.
    Step 2 — Create `supabase/migrations/159_integration_listings_listing_views_install_requests.sql` per 227-RESEARCH.md §Schema Sketches 1+2 (lines 462-555). RLS policies are FINAL CORRECT.
    Step 3 — Create `supabase/migrations/160_p220_partner_profiles_alter_ecosystem.sql` — 13 new columns (D-02 12 + D-71 payout_compliance_acknowledged_at); both indexes.
    Step 4 — Create `supabase/migrations/161_p220_referral_community_events_partnerships_alter.sql` per pattern. Add `partner_id uuid REFERENCES partner_profiles(partner_id)` to referral_programs nullable.
    Step 5 — Create `supabase/migrations/162_affiliate_programs_fraud_signals_payout_credits.sql` — 5 tables (affiliate_programs + fraud_signals + payout_credits + payout_export_batches + fraud_review_queue) with D-70 status enum (`'exported'` value) + D-72 fraud_review_queue. RLS final policies.
    Step 6 — Create `supabase/migrations/163_community_signals_developer_events.sql` — UNIQUE (source, source_message_id) constraint critical.
    Step 7 — Create `supabase/migrations/164_certification_records.sql` — polymorphic subject; recertification_warned_at column.
    Step 8 — Create `supabase/migrations/165_co_sell_opportunities.sql` — soft-FK columns documented; commission_locked_at column added.
    Step 9 — Create `test/ecosystem/schema/rls-policies-correctness.test.ts` (RL1): for each of 12 new tables, assert RLS enabled AND at least one SELECT policy exists AND policy uses `app.active_tenant_id` setting (not a stub). This proves Plan 01 RLS is independently correct without Plan 07 migration 170.
    Step 10 — Run all 7 migrations against local Supabase. Verify with `\dt` that all new tables exist; verify status enum on payout_credits includes 'exported' via `\d payout_credits`.

    **Source-of-truth check:** Per CLAUDE.md precedence rule 5 — `obsidian/work/incoming/{18,22,25}-*.md` are INFORMATIONAL ONLY. Canon = 227-CONTEXT.md (locked) + obsidian/reference/Database Schema.md.
  </action>
  <verify>
    <automated>node scripts/preconditions/227-01-check-upstream.cjs && node -e "const fs=require('fs');const m=fs.readdirSync('supabase/migrations').filter(f=>f.startsWith('158_')); if(m.length===0){console.error('Migration 158 baseline missing — P226 not executed');process.exit(1)} const c=fs.readdirSync('contracts').filter(f=>f.startsWith('F-180-')); if(c.length===0){console.error('F-180 contract baseline missing');process.exit(1)} console.log('Baseline OK')" && node -e "const fs=require('fs'); const files=['supabase/migrations/159_integration_listings_listing_views_install_requests.sql','supabase/migrations/160_p220_partner_profiles_alter_ecosystem.sql','supabase/migrations/161_p220_referral_community_events_partnerships_alter.sql','supabase/migrations/162_affiliate_programs_fraud_signals_payout_credits.sql','supabase/migrations/163_community_signals_developer_events.sql','supabase/migrations/164_certification_records.sql','supabase/migrations/165_co_sell_opportunities.sql']; for(const f of files){const c=fs.readFileSync(f,'utf8');if(!c.includes('ENABLE ROW LEVEL SECURITY'))process.exit(1);if(!c.includes('CREATE POLICY')&&!c.includes('ALTER TABLE'))process.exit(2);} console.log('OK')" && npx vitest run test/ecosystem/schema/rls-policies-correctness.test.ts</automated>
    Plus grep checks:
    - `grep -l "CREATE TABLE integration_listings" supabase/migrations/159_*.sql` returns 1 file
    - `grep -l "ALTER TABLE partner_profiles" supabase/migrations/160_*.sql` returns 1 file
    - `grep -l "payout_compliance_acknowledged_at" supabase/migrations/160_*.sql` returns 1 file (RH4 / D-71)
    - `grep -l "UNIQUE (source, source_message_id)" supabase/migrations/163_*.sql` returns 1 file
    - `grep -l "polymorphic\|subject_type IN" supabase/migrations/164_*.sql` returns 1 file
    - `grep -l "DEFAULT 'saas'" supabase/migrations/16{0,1}_*.sql` returns 2 files (backfill present)
    - `grep -l "'exported'" supabase/migrations/162_*.sql` returns 1 file (D-70 status enum / RH3)
    - `grep -l "fraud_review_queue" supabase/migrations/162_*.sql` returns 1 file (D-72 / RM2-RM3)
    - `grep -l "MISSING_UPSTREAM_PHASE" scripts/preconditions/227-01-check-upstream.cjs` returns 1 file (D-69 / RH2)
    - `grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/{159,162,163,164,165}_*.sql` returns 14+ (every new table — 12 + fraud_review_queue + payout_export_batches)
  </verify>
  <done>
    Precondition HARD-FAIL guard exits 1 with MISSING_UPSTREAM_PHASE on missing P220/P221/P225 (RH2 / D-69); 7 migration files exist; 12 new tables creatable + fraud_review_queue (D-72) + payout_credits has 'exported' in status enum (D-70 / RH3) + partner_profiles has payout_compliance_acknowledged_at (D-71 / RH4); 5 P220 ALTER TABLE additive with DEFAULT 'saas' backfill verified; all CHECK constraints + RLS policies present AND CORRECT (RL1 / D-73) — rls-policies-correctness.test.ts passes; UNIQUE (source, source_message_id) on community_signals; soft-FK columns documented for P222/P225/P226 dependents. Baseline check confirms F-180 + migration 158 exist before any new allocation. migration 164 includes recertification_warned_at; migration 165 includes commission_locked_at.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register F-181..F-188 contracts + create lib/markos/ecosystem/contracts.ts + plugin-registry adapter wrapping resolvePlugin (D-68 / RH1)</name>
  <read_first>
    - 227-RESEARCH.md §F-181..F-198 Contract Assignment (lines 162-181) — F-181..F-188 names + types + actors
    - 227-CONTEXT.md D-01..D-08 (object model field lists) + **D-68 (resolvePlugin verified API)**
    - 227-RESEARCH.md §Pattern 1 Plugin Registry Read-Through Adapter (REVISED — see this plan; supersedes RESEARCH lines 320-353)
    - 226-01-PLAN.md (sales contracts.ts) — pattern for enum constants module
    - **lib/markos/plugins/registry.js (read all 115 lines first)** — actual exports: createRegistry, registerPlugin, listPlugins, resolvePlugin
    - lib/markos/plugins/loader.js — `loadPlugins(manifests) → { registry, failures }` boot pattern
    - lib/markos/plugins/contracts.js — existing plugin contract shape (do NOT duplicate; adapter only)
    - obsidian/reference/Contracts Registry.md — F-ID JSON Schema convention
  </read_first>
  <files>
    contracts/F-181-ecosystem-integration-listing-v1.json
    contracts/F-182-ecosystem-partner-profile-v1.json
    contracts/F-183-ecosystem-certification-record-v1.json
    contracts/F-184-ecosystem-referral-program-v1.json
    contracts/F-185-ecosystem-affiliate-program-v1.json
    contracts/F-186-ecosystem-community-signal-v1.json
    contracts/F-187-ecosystem-developer-event-v1.json
    contracts/F-188-ecosystem-co-sell-opportunity-v1.json
    lib/markos/ecosystem/contracts.ts
    lib/markos/ecosystem/adapters/plugin-registry.ts
    test/ecosystem/adapters/plugin-registry.test.ts
  </files>
  <behavior>
    - **F-181..F-188 JSON contracts:** Each is a JSON file at `contracts/F-{nnn}-{name}-v1.json` with shape `{ "id": "F-181", "version": "1.0.0", "name": "ecosystem-integration-listing-v1", "type": "read-write", "actor": "Operator", "schema": { ... JSON Schema object ... } }`. Schema lists every column from CONTEXT.md D-01..D-08 with correct types + enum lists. Required fields per D-01..D-08. Each contract version 1.0.0.
    - **lib/markos/ecosystem/contracts.ts:** Single TypeScript module exporting frozen enum const arrays + zod schemas. Pattern: `export const LISTING_CATEGORY = ['connector','plugin','agent','template','workflow','integration'] as const; export type ListingCategory = (typeof LISTING_CATEGORY)[number]; export const integrationListingSchema = z.object({...});`. Used by 227-02..227-07 for runtime validation.
    - **lib/markos/ecosystem/adapters/plugin-registry.ts (REVISED — D-68 / RH1):**
      - **Real registry API:** `lib/markos/plugins/registry.js` exports `resolvePlugin(registry, pluginId)` — TWO arguments. There is NO `lookupPlugin`. The registry value is built at boot via `loadPlugins(manifests)`; there is NO module-level singleton.
      - **Adapter design:** Factory `createPluginManifestReader(registry)` returns a closure `(plugin_manifest_id: string) => PluginManifestRef | null` that internally calls `resolvePlugin(registry, plugin_manifest_id)`. This lets callers (boot path, test fixtures) inject the appropriate registry instance.
      - **Convenience export:** `getPluginManifest(registry, plugin_manifest_id)` is a thin wrapper that calls `resolvePlugin(registry, plugin_manifest_id)` and shapes the result into `PluginManifestRef`.
      - **Behavior:** Returns null when `resolvePlugin` returns null (D-09 fail-closed). NEVER calls any registry write/mutate function (`registerPlugin`, `createRegistry` are off-limits in this module). NEVER caches outside of registry's internal cache.
      - **TypeScript signature:**
        ```typescript
        // ACTUAL — D-68 verified against lib/markos/plugins/registry.js:102
        import { resolvePlugin } from '../../../plugins/registry.js';

        export interface PluginRegistry { plugins: Map<string, FrozenManifest>; }
        export interface PluginManifestRef {
          id: string; name: string; capabilities: string[];
          iamRoles: string[]; routeOwnership: string[]; version: string;
        }

        export function getPluginManifest(registry: PluginRegistry, plugin_manifest_id: string): PluginManifestRef | null {
          const manifest = resolvePlugin(registry as any, plugin_manifest_id);
          if (!manifest) return null;
          return {
            id: manifest.id, name: manifest.name,
            capabilities: manifest.requiredCapabilities ?? [],
            iamRoles: manifest.requiredIamRoles ?? [],
            routeOwnership: (manifest.routes ?? []).map((r: any) => r.path ?? r),
            version: manifest.version,
          };
        }

        export function createPluginManifestReader(registry: PluginRegistry) {
          return (plugin_manifest_id: string) => getPluginManifest(registry, plugin_manifest_id);
        }
        ```
      - **Adapter consumers (227-02 listing-service.ts) MUST receive the registry via DI** — either via service factory or via a host-app-provided getRegistry() accessor. Adapter does not assume a global.
    - **Adapter tests:**
      - (a) `getPluginManifest(registry, 'nonexistent-id')` returns null when registry is empty.
      - (b) After `registerPlugin(manifest, { registry })` (use real registry helper), `getPluginManifest(registry, manifest.id)` returns shaped PluginManifestRef.
      - (c) Adapter NEVER calls `registerPlugin`, `createRegistry`, or any write — assert via spy on imported module that only `resolvePlugin` is called.
      - (d) `createPluginManifestReader(registry)` returns a closure that produces equivalent results.
      - (e) Verify the registry shape received is `{ plugins: Map }` (RH1 — no global; adapter requires explicit instance).
  </behavior>
  <action>
    Step 1 — Create 8 JSON files under `contracts/` per the F-ID assignment table. Each file contains a JSON Schema covering every column listed in CONTEXT.md D-01..D-08 verbatim. Required fields enforce migration NOT NULL constraints.
    Step 2 — Create `lib/markos/ecosystem/contracts.ts` exporting all 24 enum constants + zod schemas + TypeScript interface types. Each enum list MUST match migration CHECK constraint values exactly. PAYOUT_CREDIT_STATUS now includes `'exported'` (D-70 / RH3).
    Step 3 — Create `lib/markos/ecosystem/adapters/plugin-registry.ts` per the REVISED Pattern 1 (above). Use `import { resolvePlugin } from '../../../plugins/registry.js'`. Add JSDoc: "// D-09..D-11 + D-68: read-through adapter wrapping resolvePlugin(registry, pluginId). NEVER mutates. Verified against lib/markos/plugins/registry.js:102."
    Step 4 — Create `test/ecosystem/adapters/plugin-registry.test.ts` with 5 tests:
      (a) Empty registry → `getPluginManifest(registry, 'x')` returns null.
      (b) After `registerPlugin(manifestFixture, { registry })`, `getPluginManifest(registry, manifestFixture.id)` returns shaped ref.
      (c) Use `vi.spyOn(registryModule, 'registerPlugin')` etc. — assert ONLY `resolvePlugin` was called.
      (d) `createPluginManifestReader(registry)` returns a function; calling it equals direct `getPluginManifest(registry, id)`.
      (e) Negative: passing a non-registry-shaped value (e.g., `{}` without `plugins` Map) → adapter returns null gracefully (resolvePlugin's own null guard kicks in).
    Step 5 — Run `npx vitest run test/ecosystem/adapters/plugin-registry.test.ts` — expect 5 tests pass.

    **Discord Ed25519 note:** Open Question 1 — Discord uses Ed25519. Logic lives in 227-05 webhook adapter; F-186 contract is source-agnostic.
  </action>
  <verify>
    <automated>npx vitest run test/ecosystem/adapters/plugin-registry.test.ts</automated>
    Plus structure checks (RH1 — proves lookupPlugin is fully removed):
    - `ls contracts/F-{181,182,183,184,185,186,187,188}-*.json` returns 8 files
    - `node -e "for (const id of [181,182,183,184,185,186,187,188]) { const c=JSON.parse(require('fs').readFileSync(\`contracts/F-\${id}-ecosystem-\${['integration-listing','partner-profile','certification-record','referral-program','affiliate-program','community-signal','developer-event','co-sell-opportunity'][id-181]}-v1.json\`,'utf8')); if(c.id!==\`F-\${id}\`||c.version!=='1.0.0'||!c.schema)process.exit(id); }"`
    - `grep -c "export const " lib/markos/ecosystem/contracts.ts` returns 24 (enum + schema + type exports)
    - **`grep -c "lookupPlugin" lib/markos/ecosystem/adapters/plugin-registry.ts` returns 0** (RH1 — fictional API removed)
    - `grep -c "resolvePlugin" lib/markos/ecosystem/adapters/plugin-registry.ts` returns ≥2 (import + call site — D-68)
    - `grep -c "createPluginManifestReader" lib/markos/ecosystem/adapters/plugin-registry.ts` returns ≥1 (factory present)
    - `grep -E "registerPlugin|createRegistry" lib/markos/ecosystem/adapters/plugin-registry.ts` returns 0 (no mutation imports)
    - **Behavior-first check (RL2):** `node -e "const m=require('./lib/markos/ecosystem/adapters/plugin-registry.ts'); if(typeof m.getPluginManifest !== 'function') process.exit(1); if(typeof m.createPluginManifestReader !== 'function') process.exit(2); console.log('OK');"` — exits 0
  </verify>
  <done>
    8 contract JSON files registered; lib/markos/ecosystem/contracts.ts exports all 24 named symbols (PAYOUT_CREDIT_STATUS includes 'exported' per D-70); plugin-registry.ts uses **resolvePlugin(registry, pluginId)** per D-68 (NOT lookupPlugin — RH1 fully resolved); 5 adapter tests pass; behavior-first verify confirms exports exist.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create 12 fixture factories + 14 schema test files + RLS isolation suite + P220 backfill verification</name>
  <read_first>
    - 227-VALIDATION.md §Wave 0 Requirements (lines 55-77) — full 12-fixture list with variant requirements
    - 227-VALIDATION.md §Validation Categories per Slice — slice 227-01 row (line 109)
    - 227-RESEARCH.md §Phase Requirements → Test Map (lines 1063-1105) — schema test commands
    - 227-RESEARCH.md §Pitfall 3 (lines 924-928) — P220 ALTER TABLE backfill assertion
    - 226-01-PLAN.md fixture factory pattern (P226 ecosystem precedent) — `makeOpportunity({...overrides})` shape
    - test/fixtures/sales/* (P226) — fixture import path pattern
  </read_first>
  <files>
    test/fixtures/ecosystem/integration-listing.ts
    test/fixtures/ecosystem/partner-profile.ts
    test/fixtures/ecosystem/certification-record.ts
    test/fixtures/ecosystem/referral-program.ts
    test/fixtures/ecosystem/affiliate-program.ts
    test/fixtures/ecosystem/community-signal.ts
    test/fixtures/ecosystem/developer-event.ts
    test/fixtures/ecosystem/co-sell-opportunity.ts
    test/fixtures/ecosystem/fraud-signal.ts
    test/fixtures/ecosystem/payout-credit.ts
    test/fixtures/ecosystem/install-request.ts
    test/fixtures/ecosystem/listing-view.ts
    test/ecosystem/schema/integration-listings.test.ts
    test/ecosystem/schema/partner-profiles-extended.test.ts
    test/ecosystem/schema/certification-records.test.ts
    test/ecosystem/schema/affiliate-programs.test.ts
    test/ecosystem/schema/community-signals.test.ts
    test/ecosystem/schema/developer-events.test.ts
    test/ecosystem/schema/co-sell-opportunities.test.ts
    test/ecosystem/schema/fraud-signals.test.ts
    test/ecosystem/schema/payout-credits.test.ts
    test/ecosystem/schema/install-requests.test.ts
    test/ecosystem/schema/listing-views.test.ts
    test/ecosystem/schema/payout-export-batches.test.ts
    test/ecosystem/schema/p220-backfill.test.ts
    test/ecosystem/schema/rls-isolation.test.ts
  </files>
  <behavior>
    - **Each fixture factory:** Exports a `make{Entity}(overrides)` function returning a plain object matching the migration row shape with valid defaults. Factories produce variants per VALIDATION.md. Factories use `crypto.randomUUID()` for ids. Factories accept partial overrides.
    - **payout-credit.ts factory:** Default `status='earned'`, `fraud_review_status='pending'`. Variants include `'exported'` status (D-70 / RH3).
    - **partner-profile.ts factory:** Includes `payout_compliance_acknowledged_at` field (D-71 / RH4); default null.
    - **Each schema test file (12 + payout_export_batches):** Verifies CRUD insert + select roundtrip; CHECK constraints reject invalid values; UNIQUE constraints enforce; DEFAULT values apply; FK constraints; tombstone-affected columns nullable.
    - **payout-credits.test.ts:** Adds explicit case verifying `status='exported'` accepted (D-70).
    - **p220-backfill.test.ts:** asserts business_mode IS NULL count = 0 across 5 P220 tables; sample rows match 'saas' default; partner_profiles new partner row defaults active_status='active' + co_sell_enabled=false + payout_compliance_acknowledged_at IS NULL (D-71 default).
    - **rls-isolation.test.ts:** Cross-tenant denial test for all 12 new tables + 5 P220 extended tables. Tenant B cannot SELECT/UPDATE/DELETE tenant A's rows. markos-global rows visible cross-tenant.
    - **No fs.existsSync-only verifies** — every test asserts behavior via real DB ops.
  </behavior>
  <action>
    Step 1 — Create `test/fixtures/ecosystem/` directory + 12 factory files. Each follows P226 sales fixture pattern. payout-credit factory includes 'exported' variant; partner-profile factory includes payout_compliance_acknowledged_at field.
    Step 2 — For each fixture, expose `make{Entity}_variants` returning array covering VALIDATION.md fixture requirements.
    Step 3 — Create 12 + 1 schema test files. Each runs against test Supabase. Each test ≥4 cases (CRUD + CHECK + UNIQUE + RLS). payout-credits.test.ts adds explicit 'exported' status acceptance test.
    Step 4 — Create `test/ecosystem/schema/p220-backfill.test.ts` with 5 assertions per behavior + payout_compliance_acknowledged_at default null verification.
    Step 5 — Create `test/ecosystem/schema/rls-isolation.test.ts` with 17 cross-tenant denial assertions + 4 markos-global visibility assertions.
    Step 6 — Run full schema suite: `npx vitest run test/ecosystem/schema/`. All 14+1 test files green (rls-policies-correctness from Task 1 also runs).

    **Validation entry:** Update 227-VALIDATION.md per-task verification map by appending row.
  </action>
  <verify>
    <automated>npx vitest run test/ecosystem/schema/</automated>
    Plus checks:
    - `ls test/fixtures/ecosystem/*.ts | wc -l` returns 12
    - `grep -l "export function make" test/fixtures/ecosystem/integration-listing.ts test/fixtures/ecosystem/partner-profile.ts test/fixtures/ecosystem/certification-record.ts test/fixtures/ecosystem/referral-program.ts test/fixtures/ecosystem/affiliate-program.ts test/fixtures/ecosystem/community-signal.ts test/fixtures/ecosystem/developer-event.ts test/fixtures/ecosystem/co-sell-opportunity.ts test/fixtures/ecosystem/fraud-signal.ts test/fixtures/ecosystem/payout-credit.ts test/fixtures/ecosystem/install-request.ts test/fixtures/ecosystem/listing-view.ts | wc -l` returns 12
    - `grep -l "business_mode IS NULL" test/ecosystem/schema/p220-backfill.test.ts` returns 1
    - `grep -l "payout_compliance_acknowledged_at" test/fixtures/ecosystem/partner-profile.ts test/ecosystem/schema/p220-backfill.test.ts` returns ≥1
    - `grep -l "'exported'" test/fixtures/ecosystem/payout-credit.ts test/ecosystem/schema/payout-credits.test.ts` returns ≥1 (D-70)
    - `grep -c "tenant_id IS NULL" test/ecosystem/schema/rls-isolation.test.ts` returns ≥4
    - All 14+1 schema test files run green via vitest exit code 0
  </verify>
  <done>
    12 fixture factories importable from `test/fixtures/ecosystem/`; 12 + 1 + 1 schema CRUD/CHECK/UNIQUE/RLS test files green; payout-credits accepts 'exported' status (D-70); partner-profile has payout_compliance_acknowledged_at field (D-71); P220 backfill verified business_mode='saas' on all pre-existing rows; RLS isolation across 12+5 tables + 4 markos-global cases verified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upstream phase deps → P227 schema | P220/P221/P225 must exist; HARD-FAIL guard prevents partial schema (D-69 / RH2) |
| migration → DB | Schema changes alter every tenant's data; ALTER TABLE backfill must preserve P220 rows |
| plugin registry → IntegrationListing | Soft FK (no DB enforcement); render-time fail-closed required |
| markos-global rows → all tenants | RLS allows NULL tenant_id rows visible to all; spoofing risk if owner_type misclassified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-227-01-01 | Tampering | P220 ALTER TABLE backfill | mitigate | DEFAULT 'saas' on ADD COLUMN backfills atomically (Postgres 11+ fast path); p220-backfill.test.ts asserts `count WHERE business_mode IS NULL = 0` |
| T-227-01-02 | Information Disclosure | RLS on markos-global rows (tenant_id IS NULL) | mitigate | Policy `tenant_id IS NULL OR tenant_id = current_setting('app.active_tenant_id')` allows global reads but blocks cross-tenant tenant-scoped reads; rls-isolation.test.ts + rls-policies-correctness.test.ts verify INITIAL CORRECTNESS (RL1) |
| T-227-01-03 | Spoofing | plugin_manifest_id soft FK | mitigate | adapter `getPluginManifest(registry, id)` returns null on miss → render-time fail-closed badge (Pitfall 1); never trust plugin_manifest_id without lookup; D-68 verified against actual registry API |
| T-227-01-04 | Tampering | Polymorphic certification_records.subject_id | mitigate | CHECK on subject_type only; app-layer enforces subject_id existence via certification-service (227-03) |
| T-227-01-05 | Information Disclosure | listing_views ip_hash + user_agent_hash | mitigate | Schema enforces ip_hash + user_agent_hash columns only; D-39/D-61 |
| T-227-01-06 | Repudiation | Migration audit trail | accept | Migration 159..165 forward-only with timestamps; markos_audit_log captures state changes in later slices |
| T-227-01-07 | Elevation | RLS bypass via direct DB connection | accept | RLS is auth boundary; service-role queries explicitly bypass; mitigated at app layer via Supabase client |
| T-227-01-08 | Tampering | Partial schema execution if upstream phase missing | mitigate | D-69 / RH2 — preflight HARD-FAILS with MISSING_UPSTREAM_PHASE; no silent skip allowed |
</threat_model>

<verification>
- HARD-FAIL precondition guard exits 1 with MISSING_UPSTREAM_PHASE on missing P220/P221/P225 (D-69 / RH2).
- All 7 migrations idempotent (re-run on clean tree produces same schema).
- 12 fixture factories used by ≥3 downstream slices each.
- F-181..F-188 contracts validate against migration column lists.
- Plugin registry adapter never mutates registry (test asserts no write calls); uses resolvePlugin(registry, pluginId) (D-68).
- `lookupPlugin` is FULLY removed from adapter file (RH1 — `grep "lookupPlugin" lib/markos/ecosystem/adapters/plugin-registry.ts` returns 0).
- P220 ALTER TABLE additive — no existing P220 SAS rows lost.
- payout_credits status enum includes 'exported' (D-70 / RH3).
- partner_profiles has payout_compliance_acknowledged_at column (D-71 / RH4).
- fraud_review_queue table created (D-72 / RM2-RM3).
- RLS suite green across 12 new + 5 P220 extended tables (17 total) — INITIAL CORRECTNESS independently verified (RL1).
- markos-global row visibility verified for integration_listings, community_signals, certification_records, partner_profiles.
- All schema CHECK + UNIQUE constraints fire correctly on invalid input.
- No raw IP/UA stored — only ip_hash + user_agent_hash columns.
- Threat register T-227-01-01..08 mitigations all linked to test files.
</verification>

<success_criteria>
- 1 precondition script (HARD-FAIL guard).
- 7 migration files (159..165) committed; `npm run db:migrate` clean.
- 8 contract JSON files (F-181..F-188) at correct paths.
- `lib/markos/ecosystem/contracts.ts` exports 24 named symbols (PAYOUT_CREDIT_STATUS includes 'exported').
- `lib/markos/ecosystem/adapters/plugin-registry.ts` exists; only imports resolvePlugin from registry.js; never mutates.
- `lookupPlugin` references = 0 in plan adapter file (RH1 resolved).
- 12 fixture factories under `test/fixtures/ecosystem/`; importable.
- 14 + 1 schema test files green: `npx vitest run test/ecosystem/schema/` exit 0.
- P220 backfill: 0 rows with business_mode IS NULL across 5 tables.
- RLS isolation: 17 tables verified.
- RLS initial correctness: rls-policies-correctness.test.ts passes (RL1).
- payout_credits has 'exported' status enum value (D-70 / RH3).
- partner_profiles has payout_compliance_acknowledged_at column (D-71 / RH4).
- fraud_review_queue table exists (D-72 / RM2-RM3).
- 227-VALIDATION.md per-task map updated for slice 227-01.
- Legacy regression `npm test` green.
</success_criteria>

<output>
After completion, create `.planning/phases/227-ecosystem-partner-community-developer-growth/227-01-SUMMARY.md` with:
- Precondition guard outcome (P220/P221/P225 verified or HARD-FAIL)
- F-181..F-188 + migrations 159..165 allocated and committed
- P220 ALTER TABLE results
- 12 fixture factory paths for downstream slice import
- Plugin registry adapter test results (5 tests pass; resolvePlugin path confirmed; lookupPlugin removed)
- RLS isolation results + RLS initial correctness (RL1)
- D-68/D-69/D-70/D-71/D-72/D-73 evidence references
</output>
</content>
</invoke>