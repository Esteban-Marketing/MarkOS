---
phase: 227-ecosystem-partner-community-developer-growth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/159_integration_listings_listing_views_install_requests.sql
  - supabase/migrations/160_p220_partner_profiles_alter_ecosystem.sql
  - supabase/migrations/161_p220_referral_community_events_partnerships_alter.sql
  - supabase/migrations/162_affiliate_programs_fraud_signals_payout_credits.sql
  - supabase/migrations/163_community_signals_developer_events.sql
  - supabase/migrations/164_certification_records.sql
  - supabase/migrations/165_co_sell_opportunities.sql
  - contracts/F-181-ecosystem-integration-listing-v1.json
  - contracts/F-182-ecosystem-partner-profile-v1.json
  - contracts/F-183-ecosystem-certification-record-v1.json
  - contracts/F-184-ecosystem-referral-program-v1.json
  - contracts/F-185-ecosystem-affiliate-program-v1.json
  - contracts/F-186-ecosystem-community-signal-v1.json
  - contracts/F-187-ecosystem-developer-event-v1.json
  - contracts/F-188-ecosystem-co-sell-opportunity-v1.json
  - lib/markos/ecosystem/contracts.ts
  - lib/markos/ecosystem/adapters/plugin-registry.ts
  - test/fixtures/ecosystem/integration-listing.ts
  - test/fixtures/ecosystem/partner-profile.ts
  - test/fixtures/ecosystem/certification-record.ts
  - test/fixtures/ecosystem/referral-program.ts
  - test/fixtures/ecosystem/affiliate-program.ts
  - test/fixtures/ecosystem/community-signal.ts
  - test/fixtures/ecosystem/developer-event.ts
  - test/fixtures/ecosystem/co-sell-opportunity.ts
  - test/fixtures/ecosystem/fraud-signal.ts
  - test/fixtures/ecosystem/payout-credit.ts
  - test/fixtures/ecosystem/install-request.ts
  - test/fixtures/ecosystem/listing-view.ts
  - test/ecosystem/schema/integration-listings.test.ts
  - test/ecosystem/schema/partner-profiles-extended.test.ts
  - test/ecosystem/schema/certification-records.test.ts
  - test/ecosystem/schema/affiliate-programs.test.ts
  - test/ecosystem/schema/community-signals.test.ts
  - test/ecosystem/schema/developer-events.test.ts
  - test/ecosystem/schema/co-sell-opportunities.test.ts
  - test/ecosystem/schema/fraud-signals.test.ts
  - test/ecosystem/schema/payout-credits.test.ts
  - test/ecosystem/schema/install-requests.test.ts
  - test/ecosystem/schema/listing-views.test.ts
  - test/ecosystem/schema/payout-export-batches.test.ts
  - test/ecosystem/schema/p220-backfill.test.ts
  - test/ecosystem/schema/rls-isolation.test.ts
  - test/ecosystem/adapters/plugin-registry.test.ts
autonomous: true
requirements: [ECO-01, ECO-02, ECO-03, ECO-04, ECO-05, SG-04, SG-06, SG-09, SG-10, SG-11, SG-12, QA-01, QA-02, QA-03, QA-04, QA-05, QA-06, QA-07, QA-08, QA-09, QA-10, QA-11, QA-12, QA-13, QA-14, QA-15]
must_haves:
  truths:
    - "F-180 + migration 158 baseline verified before any new allocation"
    - "All 12 new SOR tables exist with RLS enforced"
    - "P220 partner_profiles + referral_programs + community_profiles + marketing_events + partnerships ALTER TABLE additive — every existing P220 row has business_mode='saas' after backfill"
    - "Plugin registry adapter reads via lookupPlugin() — never mutates registry"
    - "All 8 base contracts F-181..F-188 registered with required JSON Schema fields"
    - "12 fixture factories importable from test/ecosystem/* and test/api/* for downstream slices"
  artifacts:
    - path: "supabase/migrations/159_integration_listings_listing_views_install_requests.sql"
      provides: "integration_listings + listing_views + install_requests SOR + RLS + indexes"
      contains: "CREATE TABLE integration_listings"
    - path: "supabase/migrations/160_p220_partner_profiles_alter_ecosystem.sql"
      provides: "P220 partner_profiles ecosystem extension columns + backfill"
      contains: "ALTER TABLE partner_profiles"
    - path: "supabase/migrations/161_p220_referral_community_events_partnerships_alter.sql"
      provides: "P220 referral_programs + community_profiles + marketing_events + partnerships extensions"
      contains: "ALTER TABLE referral_programs"
    - path: "supabase/migrations/162_affiliate_programs_fraud_signals_payout_credits.sql"
      provides: "affiliate_programs + fraud_signals + payout_credits + payout_export_batches"
      contains: "CREATE TABLE affiliate_programs"
    - path: "supabase/migrations/163_community_signals_developer_events.sql"
      provides: "community_signals (UNIQUE source+source_message_id) + developer_events"
      contains: "CREATE TABLE community_signals"
    - path: "supabase/migrations/164_certification_records.sql"
      provides: "certification_records + polymorphic subject + indexes"
      contains: "CREATE TABLE certification_records"
    - path: "supabase/migrations/165_co_sell_opportunities.sql"
      provides: "co_sell_opportunities + indexes"
      contains: "CREATE TABLE co_sell_opportunities"
    - path: "lib/markos/ecosystem/contracts.ts"
      provides: "Ecosystem TypeScript contracts (enum constants + zod schemas)"
      exports: ["LISTING_CATEGORY", "OWNER_TYPE", "CERTIFICATION_STATE", "LISTING_STATUS", "PRICING_MODEL", "PARTNER_TYPE", "CERTIFICATION_LEVEL", "ACTIVE_STATUS", "BUSINESS_MODE", "COMMISSION_MODEL", "AFFILIATE_PROGRAM_STATUS", "COMMUNITY_SOURCE", "COMMUNITY_SENTIMENT", "COMMUNITY_URGENCY", "ROUTED_ACTION_KIND", "DEVELOPER_EVENT_KIND", "FRAUD_SIGNAL_KIND", "FRAUD_SEVERITY", "FRAUD_STATUS", "PAYOUT_FRAUD_REVIEW_STATUS", "PAYOUT_CREDIT_STATUS", "INSTALL_REQUEST_STATUS", "COSELL_STATUS"]
    - path: "lib/markos/ecosystem/adapters/plugin-registry.ts"
      provides: "Read-through adapter to runtime plugin registry (D-09..D-11)"
      exports: ["getPluginManifest", "PluginManifestRef"]
    - path: "test/fixtures/ecosystem/"
      provides: "12 fixture factories (listing, partner, cert, referral, affiliate, community-signal, developer-event, co-sell, fraud, payout, install-request, listing-view)"
      contains: "export function makeIntegrationListing"
  key_links:
    - from: "lib/markos/ecosystem/adapters/plugin-registry.ts"
      to: "lib/markos/plugins/registry.js"
      via: "lookupPlugin import — read-through only"
      pattern: "from.*['\"](\\.\\./)+plugins/registry"
    - from: "supabase/migrations/160_p220_partner_profiles_alter_ecosystem.sql"
      to: "partner_profiles (P220)"
      via: "ALTER TABLE additive with DEFAULT 'saas' backfill"
      pattern: "ALTER TABLE partner_profiles\\s+ADD COLUMN"
    - from: "test/ecosystem/schema/p220-backfill.test.ts"
      to: "partner_profiles + referral_programs + community_profiles + marketing_events + partnerships"
      via: "assert: SELECT count(*) WHERE business_mode IS NULL must be 0"
      pattern: "business_mode IS NULL"
---

<objective>
Lay the schema + contracts + fixtures + plugin-registry adapter foundation for Phase 227 Ecosystem Engine. Wave 1 — no parallel siblings; everything else depends on this.

Purpose: Every downstream slice (227-02..227-07) needs (1) the 12 new SOR tables, (2) the 5 P220 ALTER TABLE extensions backfilled to business_mode='saas', (3) base TypeScript contracts F-181..F-188, (4) ecosystem module skeleton + plugin-registry read-through adapter, (5) 12 fixture factories. Without this slice, no other slice can compile.

Output: 7 migrations (159..165), 8 contract JSON files (F-181..F-188), TypeScript contract module, plugin-registry adapter, 12 fixture factories, 14 schema test files, 1 RLS isolation test file.
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-CONTEXT.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-RESEARCH.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-VALIDATION.md
@.planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-CONTEXT.md
@.planning/phases/221-cdp-identity-audience-consent-substrate/221-CONTEXT.md
@.planning/phases/222-crm-timeline-commercial-memory-workspace/222-CONTEXT.md
@.planning/phases/225-analytics-attribution-narrative-intelligence/225-CONTEXT.md
@.planning/phases/226-sales-enablement-deal-execution/226-CONTEXT.md
@.planning/V4.0.0-TESTING-ENVIRONMENT-PLAN.md
@.planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md
@obsidian/reference/Database Schema.md
@obsidian/reference/Contracts Registry.md
@CLAUDE.md

# NOTE on incoming docs (CLAUDE.md source-of-truth precedence rule 5):
# obsidian/work/incoming/{18,22,25}-*.md are INFORMATIONAL ONLY (raw intake).
# They are NOT canonical. Canon = obsidian/brain/* + obsidian/reference/*.
# Only read these to cross-check decision intent; never cite as doctrine.

<interfaces>
<!-- Existing code anchors needed by this plan. Verified via codebase reads + 227-RESEARCH.md. -->

From lib/markos/plugins/registry.js (P202; preserved per D-09..D-11):
```javascript
// Map-backed runtime registry; frozen manifests
export function lookupPlugin(plugin_manifest_id) // → { id, name, capabilities, iamRoles, routeOwnership, version } | null
export function listPlugins() // → frozen array
```
DO NOT modify registry.js — adapter reads through, never writes.

From P220 SaaS-mode tables (must already exist; A25 assumption):
```sql
-- partner_profiles, referral_programs, community_profiles, marketing_events, partnerships
-- (P220-shape baseline — preserve all rows; ALTER TABLE additive only)
```
If A25 fails (P220 not yet shipped), see <action> in Task 1 for guard logic.

From P221 cdp_identity_profiles (A3 — referenced by community_signals.related_profile_id, developer_events.profile_id, install_requests.requested_by_profile_id):
```sql
-- Referenced as soft FK with ON DELETE SET NULL behavior; D-62 tombstone scrub at app layer
```

From P222 opportunities (A4 — referenced by co_sell_opportunities.opportunity_id):
```sql
-- Soft FK; if P222 not yet shipped, FK constraint omitted with TODO comment in migration 165
```

From P225 attribution_touches (A23 — soft FK from fraud_signals.attribution_touch_id, payout_credits.attribution_touch_id; ALTER lands in 227-06 migration 167):
```sql
-- Soft refs in 227-01 migrations 162; FK formalized in 167 (227-06)
```

From P226 deal_rooms + handoff_records + winloss_records (A27 — soft FKs in co_sell_opportunities columns):
```sql
-- Soft refs only in migration 165; FK constraints omitted if P226 not yet shipped
```

From 226-RESEARCH.md F-ID + migration tables (A1/A2 baseline):
```text
P226 ends at F-180; P227 starts at F-181 (verified)
P226 ends at migration 158; P227 starts at migration 159 (verified)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify F-180 + migration 158 baseline; create migrations 159-165 (7 SQL files)</name>
  <read_first>
    - 227-RESEARCH.md §F-ID + Migration Allocation (lines 134-200) — verified ranges F-181..F-198 + migrations 159..171
    - 227-CONTEXT.md D-01..D-08 (object model) + D-12..D-14 (P220 ALTER TABLE) + D-22..D-23 (payout_export_batches) + D-59 (RLS) + D-66..D-67 (allocation)
    - 227-RESEARCH.md §Schema Sketches (lines 461-808) — exact CREATE TABLE + ALTER TABLE SQL
    - 226-RESEARCH.md §F-ID table — confirm P226 last F-ID = F-180; last migration = 158
    - obsidian/reference/Database Schema.md — RLS pattern + tenant isolation policy
    - V4.0.0-TESTING-ENVIRONMENT-PLAN.md — schema test conventions
  </read_first>
  <files>
    supabase/migrations/159_integration_listings_listing_views_install_requests.sql
    supabase/migrations/160_p220_partner_profiles_alter_ecosystem.sql
    supabase/migrations/161_p220_referral_community_events_partnerships_alter.sql
    supabase/migrations/162_affiliate_programs_fraud_signals_payout_credits.sql
    supabase/migrations/163_community_signals_developer_events.sql
    supabase/migrations/164_certification_records.sql
    supabase/migrations/165_co_sell_opportunities.sql
  </files>
  <behavior>
    - **A1/A2 baseline check (FIRST):** Confirm `supabase/migrations/158_*.sql` is the highest existing P226 migration. Confirm no `contracts/F-181-*.json` or `supabase/migrations/159_*.sql` exists yet. If either fails, STOP and surface the conflict.
    - **Migration 159 (D-01 + D-38 + D-39):** Create `integration_listings` with category/owner_type/certification_state/listing_status/pricing_model CHECKs, listing_url_slug UNIQUE, plugin_manifest_id as text (no DB FK per D-10), tenant_id NULL allowed for markos-global; `listing_views` with ip_hash + user_agent_hash; `install_requests` with status enum + audit_chain JSONB. RLS: `tenant_id IS NULL OR tenant_id = current_setting('app.active_tenant_id')::uuid` for listings; tenant-scoped for install_requests. Indexes: `idx_listings_public` partial WHERE status='active' AND certification_state='certified'; `idx_listings_slug`; FTS `idx_listings_fts` USING gin(to_tsvector('english', name||' '||description)).
    - **Migration 160 (D-02 + D-12):** ALTER TABLE partner_profiles ADD COLUMN IF NOT EXISTS for: business_mode (NOT NULL DEFAULT 'saas' CHECK IN saas/commerce/ecosystem/all), partner_type (CHECK 8 values), region, specialization_tags text[] DEFAULT '{}', certification_level (DEFAULT 'registered'), active_status (DEFAULT 'active'), co_sell_enabled bool DEFAULT false, referral_enabled bool DEFAULT false, affiliate_enabled bool DEFAULT false, public_directory_visible bool DEFAULT false, commission_share_default numeric(5,4), revenue_attribution_partner_id uuid (self-FK to partner_profiles.partner_id). Backfill is automatic via DEFAULT 'saas' on ADD COLUMN. Index `idx_partner_profiles_business_mode` on (tenant_id, business_mode); partial `idx_partner_profiles_public` WHERE public_directory_visible=true.
    - **Migration 161 (D-03 + D-12):** ALTER TABLE referral_programs ADD: business_mode (DEFAULT 'saas'), scope (CHECK tenant/marketplace), fraud_controls text[] DEFAULT '{}', payout_settings JSONB DEFAULT '{}', qualification_rule JSONB DEFAULT '{}', approval_required bool DEFAULT true, partner_id uuid (FK partner_profiles, nullable). Same pattern for community_profiles (+business_mode +scope), marketing_events (+business_mode), partnerships (+business_mode +co_sell_enabled). All DEFAULT 'saas' so existing P220 rows backfill automatically.
    - **Migration 162 (D-04 + D-20 + D-22 + D-23):** Create `affiliate_programs` (commission_model CHECK 4 values, attribution_window_days DEFAULT 90, cookie_duration_days DEFAULT 30, status DEFAULT 'draft'), `fraud_signals` (program_kind CHECK referral/affiliate, signal_kind CHECK 8 values, severity CHECK low/medium/high/critical, status CHECK detected/reviewing/confirmed/dismissed), `payout_credits` (status CHECK earned/cleared/paid/voided/disputed, fraud_review_status CHECK pending/cleared/blocked, currency NOT NULL DEFAULT 'USD'), `payout_export_batches` (status CHECK pending/completed/failed, csv_url text). RLS tenant-scoped. Indexes: `idx_fraud_signals_program` (tenant_id, program_kind, program_id, status); `idx_fraud_signals_severity` partial WHERE status IN ('detected','reviewing'); `idx_payout_credits_status` (tenant_id, status, fraud_review_status).
    - **Migration 163 (D-05 + D-06):** Create `community_signals` with `UNIQUE (source, source_message_id)` for dedupe; source CHECK 8 values (slack/discord/forum/github/reddit/twitter/community_event/in_app); sentiment + urgency CHECKs; routed_action_kind CHECK 6 values nullable (created_task, created_learning_candidate, created_pr_followup, dismissed, escalated, generate_narrative); `consent_state_check_passed boolean NOT NULL DEFAULT false`; raw_payload JSONB. Create `developer_events` with event_kind CHECK 7 values; profile_id soft FK; source_event_ref UUID (shared cdp_events ref). RLS allows tenant_id IS NULL for markos-global. Indexes: `idx_community_signals_inbox` partial WHERE routed_action_kind IS NULL; `idx_community_signals_source` (source, occurred_at DESC); `idx_developer_events_kind` (tenant_id, event_kind, occurred_at DESC).
    - **Migration 164 (D-07):** Create `certification_records` with subject_type CHECK listing/partner; subject_id uuid (polymorphic, no DB FK); state CHECK 6 values (draft/review/certified/suspended/expired/revoked); criteria_checks JSONB DEFAULT '{}'; expires_at, revoked_at, recertification_due_at, recertification_warned_at timestamptz. RLS allows markos-global. Indexes: `idx_cert_records_subject` (subject_type, subject_id, state); `idx_cert_records_expiry` partial WHERE state='certified'; `idx_cert_records_recert` partial WHERE state='certified'.
    - **Migration 165 (D-08):** Create `co_sell_opportunities` with opportunity_id (soft FK; comment "FK → P222 opportunities — formalize when P222 ships"); partner_id (soft FK partner_profiles); commission_share numeric(5,4); rev_share_terms_id (soft FK P205); launch_surface_id (soft FK P224); partner_dealroom_id + handoff_record_id + winloss_record_id (soft FKs P226); status CHECK 6 values (proposed/accepted/active/won/lost/withdrawn); attribution_touch_ids uuid[] DEFAULT '{}'; commission_locked_at timestamptz. RLS tenant-scoped. Indexes: `idx_cosell_opp_partner` (tenant_id, partner_id, status); `idx_cosell_opp_opportunity` (opportunity_id, status).
    - All migrations use `CREATE TABLE IF NOT EXISTS` and `ADD COLUMN IF NOT EXISTS` for re-run safety. Every new table includes `ENABLE ROW LEVEL SECURITY` + tenant policy. No raw IP/user-agent stored — only ip_hash + user_agent_hash columns where applicable (D-39).
    - **No scope simplification:** Every column from CONTEXT.md D-01..D-08 must appear in migrations. Do NOT defer columns to "v2".
  </behavior>
  <action>
    Step 1 — Baseline guard: Run `ls supabase/migrations/158_*.sql contracts/F-180-*.json 2>&1 | tail -5` and `ls supabase/migrations/159_* contracts/F-181-* 2>&1 | head -5`. Migration 158 + F-180 must exist; 159 + F-181 must NOT exist. If conflict, abort and re-verify P226 ship state.
    Step 2 — Create `supabase/migrations/159_integration_listings_listing_views_install_requests.sql` per 227-RESEARCH.md §Schema Sketches 1+2 (lines 462-555). Verbatim CREATE TABLE + ENABLE RLS + CREATE POLICY + 4 CREATE INDEX statements.
    Step 3 — Create `supabase/migrations/160_p220_partner_profiles_alter_ecosystem.sql` per 227-RESEARCH.md §Pattern 4 (lines 423-448). All 12 new columns; both indexes.
    Step 4 — Create `supabase/migrations/161_p220_referral_community_events_partnerships_alter.sql`. Mirror pattern of 160 across 4 P220 tables. Each new column has `IF NOT EXISTS ... NOT NULL DEFAULT 'saas'` for business_mode; other columns nullable or have appropriate defaults. Add `partner_id uuid REFERENCES partner_profiles(partner_id)` to referral_programs (nullable per D-03).
    Step 5 — Create `supabase/migrations/162_affiliate_programs_fraud_signals_payout_credits.sql` per 227-RESEARCH.md §Schema Sketches 4 (lines 596-687). 4 tables with all CHECKs + RLS + 3 indexes.
    Step 6 — Create `supabase/migrations/163_community_signals_developer_events.sql` per §Schema Sketches 5 (lines 691-741). UNIQUE (source, source_message_id) is critical — must be a separate constraint for dedupe.
    Step 7 — Create `supabase/migrations/164_certification_records.sql` per §Schema Sketches 3 (lines 559-591). Polymorphic subject — NO DB FK; CHECK constraint on subject_type values only.
    Step 8 — Create `supabase/migrations/165_co_sell_opportunities.sql` per §Schema Sketches 6 (lines 745-771). Mark soft-FK columns with comments per A4/A23/A27 — no actual REFERENCES until 227-06 migration 167.
    Step 9 — Run all 7 migrations against local Supabase (`npm run db:migrate` if defined, else manually via `supabase db push`). Verify with `\dt` that all 12 new tables exist; verify with `\d partner_profiles` that 12 new columns exist.

    **Source-of-truth check:** Per CLAUDE.md precedence rule 5 — `obsidian/work/incoming/{18,22,25}-*.md` are INFORMATIONAL ONLY. Do NOT cite them for decisions; canonical decisions are in 227-CONTEXT.md (locked) and obsidian/reference/Database Schema.md (canonical schema).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const m=fs.readdirSync('supabase/migrations').filter(f=>f.startsWith('158_')); if(m.length===0){console.error('Migration 158 baseline missing — P226 not executed');process.exit(1)} const c=fs.readdirSync('contracts').filter(f=>f.startsWith('F-180-')); if(c.length===0){console.error('F-180 contract baseline missing');process.exit(1)} console.log('Baseline OK')" && node -e "const fs=require('fs'); const files=['supabase/migrations/159_integration_listings_listing_views_install_requests.sql','supabase/migrations/160_p220_partner_profiles_alter_ecosystem.sql','supabase/migrations/161_p220_referral_community_events_partnerships_alter.sql','supabase/migrations/162_affiliate_programs_fraud_signals_payout_credits.sql','supabase/migrations/163_community_signals_developer_events.sql','supabase/migrations/164_certification_records.sql','supabase/migrations/165_co_sell_opportunities.sql']; for(const f of files){const c=fs.readFileSync(f,'utf8');if(!c.includes('ENABLE ROW LEVEL SECURITY'))process.exit(1);if(!c.includes('CREATE POLICY')&&!c.includes('ALTER TABLE'))process.exit(2);} console.log('OK')"</automated>
    Plus grep checks:
    - `grep -l "CREATE TABLE integration_listings" supabase/migrations/159_*.sql` returns 1 file
    - `grep -l "ALTER TABLE partner_profiles" supabase/migrations/160_*.sql` returns 1 file
    - `grep -l "UNIQUE (source, source_message_id)" supabase/migrations/163_*.sql` returns 1 file
    - `grep -l "polymorphic\|subject_type IN" supabase/migrations/164_*.sql` returns 1 file
    - `grep -l "DEFAULT 'saas'" supabase/migrations/16{0,1}_*.sql` returns 2 files (backfill present)
    - `grep -c "ENABLE ROW LEVEL SECURITY" supabase/migrations/{159,162,163,164,165}_*.sql` returns 12+ (every new table)
  </verify>
  <done>
    7 migration files exist; 12 new tables creatable; 5 P220 ALTER TABLE additive with DEFAULT 'saas' backfill verified; all CHECK constraints + RLS policies present; UNIQUE (source, source_message_id) on community_signals; soft-FK columns documented for P222/P225/P226 dependents. Baseline check confirms F-180 + migration 158 exist before any new allocation. migration 164 certification_records includes `recertification_warned_at timestamptz` column for 227-03 recertification cron idempotency (`grep "recertification_warned_at" supabase/migrations/164_certification_records.sql` returns ≥1).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register F-181..F-188 contracts + create lib/markos/ecosystem/contracts.ts + plugin-registry adapter</name>
  <read_first>
    - 227-RESEARCH.md §F-181..F-198 Contract Assignment (lines 162-181) — F-181..F-188 names + types + actors
    - 227-CONTEXT.md D-01..D-08 (object model field lists) — fully specifies each contract shape
    - 227-RESEARCH.md §Pattern 1 Plugin Registry Read-Through Adapter (lines 320-353) — full adapter source
    - 226-01-PLAN.md (sales contracts.ts) — pattern for enum constants module
    - lib/markos/plugins/registry.js — existing exports to import
    - lib/markos/plugins/contracts.js — existing plugin contract shape (do NOT duplicate; adapter only)
    - obsidian/reference/Contracts Registry.md — F-ID JSON Schema convention
  </read_first>
  <files>
    contracts/F-181-ecosystem-integration-listing-v1.json
    contracts/F-182-ecosystem-partner-profile-v1.json
    contracts/F-183-ecosystem-certification-record-v1.json
    contracts/F-184-ecosystem-referral-program-v1.json
    contracts/F-185-ecosystem-affiliate-program-v1.json
    contracts/F-186-ecosystem-community-signal-v1.json
    contracts/F-187-ecosystem-developer-event-v1.json
    contracts/F-188-ecosystem-co-sell-opportunity-v1.json
    lib/markos/ecosystem/contracts.ts
    lib/markos/ecosystem/adapters/plugin-registry.ts
    test/ecosystem/adapters/plugin-registry.test.ts
  </files>
  <behavior>
    - **F-181..F-188 JSON contracts:** Each is a JSON file at `contracts/F-{nnn}-{name}-v1.json` with shape `{ "id": "F-181", "version": "1.0.0", "name": "ecosystem-integration-listing-v1", "type": "read-write", "actor": "Operator", "schema": { ... JSON Schema object ... } }`. Schema MUST list every column from CONTEXT.md D-01..D-08 with correct types + enum lists. Required fields per D-01..D-08 (e.g., F-181 requires listing_id, tenant_id-or-null, category, name, description, owner_type, certification_state, listing_url_slug, status; optional everything else). Each contract version 1.0.0 (initial allocation).
    - **lib/markos/ecosystem/contracts.ts:** Single TypeScript module exporting frozen enum const arrays (24 enums per files_modified.exports list above) + zod schemas matching JSON schemas. Pattern: `export const LISTING_CATEGORY = ['connector','plugin','agent','template','workflow','integration'] as const; export type ListingCategory = (typeof LISTING_CATEGORY)[number]; export const integrationListingSchema = z.object({...});`. Used by 227-02..227-07 for runtime validation.
    - **lib/markos/ecosystem/adapters/plugin-registry.ts:** Exports `getPluginManifest(plugin_manifest_id)` returning `PluginManifestRef | null`. Internal: `import { lookupPlugin } from '../../../plugins/registry.js'`. Returns null when `lookupPlugin` returns null (D-09 fail-closed). NEVER calls any registry write/mutate function. NEVER caches outside of registry's internal cache.
    - **Adapter tests:** verify (1) returns null for unknown id; (2) returns shaped PluginManifestRef when manifest exists in registry; (3) does NOT call any non-readonly registry function (mock to assert).
    - All contract registration uses fresh F-IDs (verified by Task 1 baseline check). Re-running this task on a clean tree is idempotent.
  </behavior>
  <action>
    Step 1 — Create 8 JSON files under `contracts/` per the F-ID assignment table (per D-66 — F-181..F-198 P227 allocation; this slice covers F-181..F-188 base read-write shells; F-189..F-198 land in slices 227-02..227-07). Each file contains a JSON Schema covering every column listed in CONTEXT.md D-01..D-08 verbatim. Required fields enforce the canonical NOT NULL constraints from migrations 159..165.
    Step 2 — Create `lib/markos/ecosystem/contracts.ts` exporting all 24 enum constants + zod schemas (one per F-181..F-188 contract) + TypeScript interface types. Each enum list MUST match the migration CHECK constraint values exactly. Mirror the pattern from `lib/markos/sales/contracts.ts` (P226 plan 226-01).
    Step 3 — Create `lib/markos/ecosystem/adapters/plugin-registry.ts` per 227-RESEARCH.md §Pattern 1 verbatim. Add JSDoc: "// D-09..D-11: read-through adapter to runtime plugin registry. NEVER mutates."
    Step 4 — Create `test/ecosystem/adapters/plugin-registry.test.ts` with tests:
      (a) `getPluginManifest('nonexistent-id')` returns null (fail-closed per D-10/Pitfall 1).
      (b) When manifest registered (use `registerPlugin` test helper or mock `lookupPlugin`), returns `{ id, name, capabilities, iamRoles, routeOwnership, version }`.
      (c) Adapter never calls `registerPlugin`, `unregisterPlugin`, or any write function (mock the registry module and assert no writes via `expect(registerPluginMock).not.toHaveBeenCalled()`).
      (d) Returned manifest is structurally equivalent to an immutable shape (D-09 fail-closed boundary).
    Step 5 — Run `npx vitest run test/ecosystem/adapters/plugin-registry.test.ts` — expect 4 tests pass.

    **Discord Ed25519 note:** Open Question 1 from RESEARCH — Discord uses Ed25519 (not HMAC). Do NOT add Discord-specific signing logic in this task; it lives in 227-05 webhook adapter. F-186 community-signal contract IS source-agnostic (just enum + payload shape).
  </action>
  <verify>
    <automated>npx vitest run test/ecosystem/adapters/plugin-registry.test.ts</automated>
    Plus structure checks:
    - `ls contracts/F-{181,182,183,184,185,186,187,188}-*.json` returns 8 files
    - `node -e "for (const id of [181,182,183,184,185,186,187,188]) { const c=JSON.parse(require('fs').readFileSync(\`contracts/F-\${id}-ecosystem-\${['integration-listing','partner-profile','certification-record','referral-program','affiliate-program','community-signal','developer-event','co-sell-opportunity'][id-181]}-v1.json\`,'utf8')); if(c.id!==\`F-\${id}\`||c.version!=='1.0.0'||!c.schema)process.exit(id); }"`
    - `grep -c "export const " lib/markos/ecosystem/contracts.ts` returns 24 (enum + schema + type exports)
    - `grep "lookupPlugin" lib/markos/ecosystem/adapters/plugin-registry.ts` returns at least 1 match (import + call site)
    - `grep -E "registerPlugin|unregisterPlugin|set\(|delete\(" lib/markos/ecosystem/adapters/plugin-registry.ts` returns 0 matches (no mutation calls)
  </verify>
  <done>
    8 contract JSON files registered with correct F-IDs + JSON Schemas covering CONTEXT.md D-01..D-08; lib/markos/ecosystem/contracts.ts exports all 24 named symbols (ROUTED_ACTION_KIND now has 6 values including 'generate_narrative'); plugin-registry.ts uses lookupPlugin only; 4 adapter tests pass. `grep "'generate_narrative'" lib/markos/ecosystem/contracts.ts` returns ≥1; migration 163 routed_action_kind CHECK has 6 values.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create 12 fixture factories + 14 schema test files + RLS isolation suite + P220 backfill verification</name>
  <read_first>
    - 227-VALIDATION.md §Wave 0 Requirements (lines 55-77) — full 12-fixture list with variant requirements
    - 227-VALIDATION.md §Validation Categories per Slice — slice 227-01 row (line 109)
    - 227-RESEARCH.md §Phase Requirements → Test Map (lines 1063-1105) — schema test commands
    - 227-RESEARCH.md §Pitfall 3 (lines 924-928) — P220 ALTER TABLE backfill assertion
    - 226-01-PLAN.md fixture factory pattern (P226 ecosystem precedent) — `makeOpportunity({...overrides})` shape
    - test/fixtures/sales/* (P226) — fixture import path pattern
  </read_first>
  <files>
    test/fixtures/ecosystem/integration-listing.ts
    test/fixtures/ecosystem/partner-profile.ts
    test/fixtures/ecosystem/certification-record.ts
    test/fixtures/ecosystem/referral-program.ts
    test/fixtures/ecosystem/affiliate-program.ts
    test/fixtures/ecosystem/community-signal.ts
    test/fixtures/ecosystem/developer-event.ts
    test/fixtures/ecosystem/co-sell-opportunity.ts
    test/fixtures/ecosystem/fraud-signal.ts
    test/fixtures/ecosystem/payout-credit.ts
    test/fixtures/ecosystem/install-request.ts
    test/fixtures/ecosystem/listing-view.ts
    test/ecosystem/schema/integration-listings.test.ts
    test/ecosystem/schema/partner-profiles-extended.test.ts
    test/ecosystem/schema/certification-records.test.ts
    test/ecosystem/schema/affiliate-programs.test.ts
    test/ecosystem/schema/community-signals.test.ts
    test/ecosystem/schema/developer-events.test.ts
    test/ecosystem/schema/co-sell-opportunities.test.ts
    test/ecosystem/schema/fraud-signals.test.ts
    test/ecosystem/schema/payout-credits.test.ts
    test/ecosystem/schema/install-requests.test.ts
    test/ecosystem/schema/listing-views.test.ts
    test/ecosystem/schema/payout-export-batches.test.ts
    test/ecosystem/schema/p220-backfill.test.ts
    test/ecosystem/schema/rls-isolation.test.ts
  </files>
  <behavior>
    - **Each fixture factory:** Exports a `make{Entity}(overrides)` function returning a plain object matching the migration row shape with valid defaults. Factories produce variants per VALIDATION.md (e.g., integration-listing with category × certification_state × owner_type). Factories use `crypto.randomUUID()` for ids. Factories accept partial overrides for specific test cases.
    - **Each schema test file (12 + payout_export_batches):** Verifies (1) CRUD insert + select roundtrip with all columns, (2) all CHECK constraints reject invalid values (e.g., category='invalid' → 23514 error), (3) UNIQUE constraints enforce (community_signals: insert with same (source, source_message_id) → DO NOTHING; install_requests: any unique constraints), (4) DEFAULT values apply for omitted columns, (5) FK constraints (where present in migration), (6) tombstone-affected columns are nullable (community_signals.related_profile_id nullable for D-62).
    - **p220-backfill.test.ts:** After migrations 160+161, asserts (1) `SELECT count(*) FROM partner_profiles WHERE business_mode IS NULL` returns 0; (2) Same for referral_programs/community_profiles/marketing_events/partnerships; (3) All pre-existing P220 rows now have business_mode='saas' (sample 5 rows); (4) New columns have correct DEFAULTs (active_status='active', co_sell_enabled=false, etc).
    - **rls-isolation.test.ts:** Cross-tenant denial test for all 12 new tables + 5 P220 extended tables. Uses two test tenants A and B; sets `app.active_tenant_id` per query; asserts tenant B cannot SELECT/UPDATE/DELETE tenant A's rows. Asserts markos-global rows (tenant_id IS NULL) ARE visible to all tenants where applicable (integration_listings, community_signals, certification_records, partner_profiles per D-59).
    - **No fs.existsSync-only verifies** — every test asserts behavior via real DB ops + ON CONFLICT/CHECK rejections.
  </behavior>
  <action>
    Step 1 — Create `test/fixtures/ecosystem/` directory + 12 factory files. Each follows P226 sales fixture pattern (e.g., `export function makeIntegrationListing(overrides = {}) { return { listing_id: crypto.randomUUID(), tenant_id: null, category: 'connector', name: 'Test Listing', description: 'desc', owner_type: 'markos', certification_state: 'draft', listing_url_slug: \`slug-\${Date.now()}\`, pricing_model: 'free', status: 'hidden', view_count: 0, install_count: 0, ...overrides }; }`).
    Step 2 — For each fixture, expose `make{Entity}_variants` returning array covering VALIDATION.md fixture requirements (e.g., partner-profile: business_mode in saas/commerce/ecosystem/all × partner_type 8 values).
    Step 3 — Create 12 + 1 schema test files. Each runs against test Supabase (use existing test/setup pattern from P226 schema tests). Each test uses fixture factory; each test has ≥4 cases (CRUD + CHECK + UNIQUE + RLS). Use `expect(error.code).toBe('23514')` for CHECK violations; `expect(error.code).toBe('23505')` for UNIQUE violations.
    Step 4 — Create `test/ecosystem/schema/p220-backfill.test.ts` with 5 assertions: (1) 5 P220 tables × business_mode IS NULL count = 0; (2) Sample existing rows match business_mode='saas'; (3) Pre-existing partner_profiles row queries with `business_mode IN ('saas')` return SAS rows; (4) Pre-existing partner_profiles row queries with `business_mode IN ('saas','commerce','ecosystem','all')` return same SAS rows (D-13 single SOR test); (5) New partner_profile row defaults to active_status='active' + co_sell_enabled=false.
    Step 5 — Create `test/ecosystem/schema/rls-isolation.test.ts` with 17 cross-tenant denial assertions (12 new tables + 5 P220 extended tables) + 4 markos-global visibility assertions (integration_listings/community_signals/certification_records/partner_profiles where tenant_id IS NULL → visible to all).
    Step 6 — Run full schema suite: `npx vitest run test/ecosystem/schema/`. All 14 test files green.

    **Validation entry:** Update 227-VALIDATION.md per-task verification map by appending a row for this task: `| 227-01-3 | 01 | 1 | ECO-01 | unit/integration/RLS | npx vitest run test/ecosystem/schema/ | ✅ | pending |`. Planner will sweep all task rows after all 7 plans are written; this task just contributes its row.

    **{{MARKOS_PRICING_ENGINE_PENDING}} placeholder check (D-23/D-46/D-65):** No partner-facing copy is generated in this slice; no commission_share is set from user input. The placeholder rule applies in 227-04 (payouts) and 227-06 (cosell commission). This task does not surface payout/commission copy.
  </action>
  <verify>
    <automated>npx vitest run test/ecosystem/schema/</automated>
    Plus checks:
    - `ls test/fixtures/ecosystem/*.ts | wc -l` returns 12
    - `grep -l "export function make" test/fixtures/ecosystem/integration-listing.ts test/fixtures/ecosystem/partner-profile.ts test/fixtures/ecosystem/certification-record.ts test/fixtures/ecosystem/referral-program.ts test/fixtures/ecosystem/affiliate-program.ts test/fixtures/ecosystem/community-signal.ts test/fixtures/ecosystem/developer-event.ts test/fixtures/ecosystem/co-sell-opportunity.ts test/fixtures/ecosystem/fraud-signal.ts test/fixtures/ecosystem/payout-credit.ts test/fixtures/ecosystem/install-request.ts test/fixtures/ecosystem/listing-view.ts | wc -l` returns 12
    - `grep -l "business_mode IS NULL" test/ecosystem/schema/p220-backfill.test.ts` returns 1
    - `grep -c "tenant_id IS NULL" test/ecosystem/schema/rls-isolation.test.ts` returns ≥4 (markos-global visibility cases)
    - All 14 schema test files run green via vitest exit code 0
  </verify>
  <done>
    12 fixture factories importable from `test/fixtures/ecosystem/`; 12 + 1 schema CRUD/CHECK/UNIQUE/RLS test files green; P220 backfill verified business_mode='saas' on all pre-existing rows across 5 tables; RLS isolation across 12+5 tables + 4 markos-global cases verified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → DB | Schema changes alter every tenant's data; ALTER TABLE backfill must preserve P220 rows |
| plugin registry → IntegrationListing | Soft FK (no DB enforcement); render-time fail-closed required |
| markos-global rows → all tenants | RLS allows NULL tenant_id rows visible to all; spoofing risk if owner_type misclassified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-227-01-01 | Tampering | P220 ALTER TABLE backfill | mitigate | DEFAULT 'saas' on ADD COLUMN backfills atomically (Postgres 11+ fast path); p220-backfill.test.ts asserts `count WHERE business_mode IS NULL = 0` |
| T-227-01-02 | Information Disclosure | RLS on markos-global rows (tenant_id IS NULL) | mitigate | Policy `tenant_id IS NULL OR tenant_id = current_setting('app.active_tenant_id')` allows global reads but blocks cross-tenant tenant-scoped reads; rls-isolation.test.ts verifies 17 tables |
| T-227-01-03 | Spoofing | plugin_manifest_id soft FK | mitigate | adapter `getPluginManifest()` returns null on miss → render-time fail-closed badge (Pitfall 1); never trust plugin_manifest_id without lookup |
| T-227-01-04 | Tampering | Polymorphic certification_records.subject_id | mitigate | CHECK on subject_type only; app-layer enforces subject_id existence via certification-service (227-03); test/ecosystem/schema/certification-records.test.ts asserts orphan subject_id is operator-detectable |
| T-227-01-05 | Information Disclosure | listing_views ip_hash + user_agent_hash | mitigate | Schema enforces ip_hash + user_agent_hash columns only — no raw IP/UA columns; D-39/D-61 |
| T-227-01-06 | Repudiation | Migration audit trail | accept | Migration 159..165 are forward-only with timestamps; markos_audit_log captures every state change in later slices (227-03+) |
| T-227-01-07 | Elevation | RLS bypass via direct DB connection | accept | RLS is the only auth boundary; service-role queries explicitly bypass RLS; mitigated at app layer via Supabase client tenant context |
</threat_model>

<verification>
- All 7 migrations idempotent (re-run on clean tree produces same schema).
- 12 fixture factories used by ≥3 downstream slices each (227-02..227-07).
- F-181..F-188 contracts validate against migration column lists (zod schema in lib/markos/ecosystem/contracts.ts matches DB shape).
- Plugin registry adapter never mutates registry (test asserts no write calls).
- P220 ALTER TABLE additive — no existing P220 SAS rows lost or modified beyond business_mode='saas' backfill.
- RLS suite green across 12 new + 5 P220 extended tables (17 total).
- markos-global row visibility verified for integration_listings, community_signals, certification_records, partner_profiles.
- All schema CHECK + UNIQUE constraints fire correctly on invalid input.
- No raw IP/UA stored — only ip_hash + user_agent_hash columns.
- Threat register T-227-01-01..07 mitigations all linked to test files.
</verification>

<success_criteria>
- 7 migration files (159..165) committed; `npm run db:migrate` clean.
- 8 contract JSON files (F-181..F-188) at correct paths.
- `lib/markos/ecosystem/contracts.ts` exports 24 named symbols.
- `lib/markos/ecosystem/adapters/plugin-registry.ts` exists; only imports lookupPlugin from registry.js; never mutates.
- 12 fixture factories under `test/fixtures/ecosystem/`; importable.
- 14 schema test files green: `npx vitest run test/ecosystem/schema/` exit 0.
- P220 backfill: 0 rows with business_mode IS NULL across 5 tables.
- RLS isolation: 17 tables verified.
- 227-VALIDATION.md per-task map updated for slice 227-01.
- Legacy regression `npm test` green (no break in P100-P105 / P201 / P220 / P221-P226 baseline).
</success_criteria>

<output>
After completion, create `.planning/phases/227-ecosystem-partner-community-developer-growth/227-01-SUMMARY.md` with:
- F-181..F-188 + migrations 159..165 allocated and committed
- P220 ALTER TABLE results (rows backfilled, business_mode distribution)
- 12 fixture factory paths for downstream slice import
- Plugin registry adapter test results (4 tests pass)
- RLS isolation results (17 tables verified)
- Any A1-A27 assumption escalations (e.g., if P220 not yet shipped, document P227 conditional path)
</output>
