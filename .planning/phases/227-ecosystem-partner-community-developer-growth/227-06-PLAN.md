---
phase: 227-ecosystem-partner-community-developer-growth
plan: 06
type: execute
wave: 4
depends_on: ["227-01", "227-03", "227-04", "227-05"]
files_modified:
  - contracts/F-196-ecosystem-cosell-state-v1.json
  - contracts/F-197-ecosystem-attribution-extend-v1.json
  - supabase/migrations/167_p225_attribution_touches_ecosystem_extend.sql
  - lib/markos/ecosystem/co-sell/cosell-service.ts
  - lib/markos/ecosystem/co-sell/cosell-attribution.ts
  - lib/markos/ecosystem/co-sell/p222-lifecycle-hook.ts
  - lib/markos/ecosystem/attribution/ecosystem-attribution.ts
  - test/ecosystem/co-sell/cosell-state-machine.test.ts
  - test/ecosystem/co-sell/cosell-commission-immutable.test.ts
  - test/ecosystem/co-sell/cosell-p222-sync.test.ts
  - test/ecosystem/co-sell/lifecycle-hook.test.ts
  - test/ecosystem/attribution/ecosystem-attribution.test.ts
  - test/ecosystem/attribution/channel-enum.test.ts
  - test/ecosystem/attribution/at-most-one-ecosystem-ref.test.ts
autonomous: true
requirements: [ECO-03, ECO-05, SG-10, SG-12, QA-01, QA-02, QA-03, QA-04, QA-05, QA-06, QA-07, QA-08, QA-09, QA-10, QA-11, QA-12, QA-13, QA-14, QA-15]
must_haves:
  truths:
    - "Migration 167: P225 attribution_touches gains 6 nullable FK columns + channel ENUM extends with 'marketplace' + 'affiliate' + CHECK at-most-one ecosystem ref"
    - "CoSellOpportunity state machine: proposed → accepted → active → {won, lost, withdrawn}"
    - "Commission immutable post-acceptance: commission_locked_at set at status='accepted'; subsequent commission_share PATCH rejected (422)"
    - "P222 Opportunity.source_motion='partner' → auto-create CoSellOpportunity in SAME transaction as lifecycle_transition (Pitfall 8 fail-closed)"
    - "P222 Opportunity.stage 'customer'/'lost' → CoSellOpportunity.status auto-update + winloss_record_id link (in-transaction)"
    - "Ecosystem attribution flows through P225 attribution_touches single ledger (D-34..D-36 / ECO-05); no parallel ledger"
    - "rev_share_terms_id binds to P205 PricingRecommendation OR uses {{MARKOS_PRICING_ENGINE_PENDING}} (D-65)"
  artifacts:
    - path: "supabase/migrations/167_p225_attribution_touches_ecosystem_extend.sql"
      provides: "P225 ALTER TABLE: 6 nullable FKs + channel ENUM extend + at-most-one CHECK"
      contains: "ALTER TABLE attribution_touches"
    - path: "lib/markos/ecosystem/co-sell/cosell-service.ts"
      provides: "CoSellOpportunity state machine + commission immutability + acceptance + close"
      exports: ["createCoSellOpportunity", "acceptCoSell", "withdrawCoSell", "completeCoSell", "transitionCoSellStatus", "COSELL_TRANSITIONS"]
    - path: "lib/markos/ecosystem/co-sell/cosell-attribution.ts"
      provides: "attribution_touch_ids[] management for CoSellOpportunity"
      exports: ["attachAttributionTouch", "computeAttributionForCoSell"]
    - path: "lib/markos/ecosystem/co-sell/p222-lifecycle-hook.ts"
      provides: "P222 Opportunity lifecycle hook → CoSellOpportunity sync (in-transaction)"
      exports: ["syncCoSellFromOpportunity", "registerLifecycleHook"]
    - path: "lib/markos/ecosystem/attribution/ecosystem-attribution.ts"
      provides: "P225 attribution_touches FK column write helper + at-most-one enforcement"
      exports: ["attachEcosystemTouch", "ECOSYSTEM_REF_COLUMNS"]
  key_links:
    - from: "lib/markos/ecosystem/co-sell/cosell-service.ts::acceptCoSell"
      to: "co_sell_opportunities.commission_locked_at"
      via: "set on status='accepted'; subsequent commission_share mutation rejected"
      pattern: "commission_locked_at"
    - from: "lib/markos/ecosystem/co-sell/p222-lifecycle-hook.ts::syncCoSellFromOpportunity"
      to: "P222 lifecycle_transitions write"
      via: "SAME Supabase transaction; fail-closed if cosell sync fails (Pitfall 8)"
      pattern: "transaction|rpc\\("
    - from: "lib/markos/ecosystem/attribution/ecosystem-attribution.ts::attachEcosystemTouch"
      to: "P225 attribution_touches"
      via: "Set exactly one of 6 FK columns; enforce at-most-one constraint"
      pattern: "ECOSYSTEM_REF_COLUMNS"
---

<objective>
Build CoSellOpportunity state machine + P222 lifecycle hook + ecosystem attribution_touches extension (227-RESEARCH.md slice 227-06). Wave 4 — depends on 227-01..05.

Purpose: This slice owns ECO-03 (community/developer signals feed CRM) + ECO-05 (single attribution ledger — no parallel growth ledger). Critical correctness: commission immutability post-acceptance (D-32 — Quote-as-Snapshot pattern from P226 D-21); P222 sync IN-TRANSACTION fail-closed (Pitfall 8); attribution_touches FK columns at-most-one constraint (Pitfall 4).

Output: 2 contracts (F-196/F-197), 1 migration (167 — heaviest of phase), 5 service modules, 7 vitest test files.
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-CONTEXT.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-RESEARCH.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-VALIDATION.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-01-PLAN.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-03-PLAN.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-04-PLAN.md
@.planning/phases/222-crm-timeline-commercial-memory-workspace/222-CONTEXT.md
@.planning/phases/224-conversion-launch-workspace/224-CONTEXT.md
@.planning/phases/225-analytics-attribution-narrative-intelligence/225-CONTEXT.md
@.planning/phases/226-sales-enablement-deal-execution/226-CONTEXT.md
@.planning/phases/205-pricing-engine-foundation-billing-readiness/205-CONTEXT.md
@CLAUDE.md

# Incoming docs (CLAUDE.md precedence rule 5): obsidian/work/incoming/{18,22,25}-*.md INFORMATIONAL ONLY.

<interfaces>
From 227-01 (lib/markos/ecosystem/contracts.ts):
```typescript
export const COSELL_STATUS = ['proposed','accepted','active','won','lost','withdrawn'] as const;
```

From 227-04 (lib/markos/ecosystem/payouts/pricing-placeholder.ts):
```typescript
export function resolveCommissionRate(rev_share_terms_id?: string): number | string;
export const PRICING_PENDING_PLACEHOLDER: '{{MARKOS_PRICING_ENGINE_PENDING}}';
export function guardCommissionInput(input: any): void; // throws RAW_COMMISSION_INPUT_FORBIDDEN
```

From P222 (A4 — must exist for FK + lifecycle hook):
```sql
-- opportunities (opportunity_id, source_motion, stage)
-- lifecycle_transitions (transition_id, opportunity_id, from_stage, to_stage)
-- buying_committee_members (FK reference)
```
P222 lifecycle_transitions hook signature (P222 D-17 ownership tuple change):
```typescript
// lib/markos/crm360/lifecycle-hooks.ts (P222)
export type LifecycleHook = (transition: LifecycleTransition, ctx: TransactionCtx) => Promise<void>;
export function registerLifecycleHook(name: string, hook: LifecycleHook): void;
// Hooks called in-transaction; throw to roll back lifecycle_transition
```

From P225 attribution_touches (A23):
```sql
-- existing columns: touch_id, tenant_id, opportunity_id, profile_id, account_id, campaign_id, channel, occurred_at, touch_type, confidence_score, model_id, model_version, source_event_ref
-- 227-06 ADDS (migration 167): marketplace_listing_id, partner_id, referral_program_id, affiliate_program_id, community_signal_id, co_sell_opportunity_id (all nullable FK)
-- channel ENUM extends with 'marketplace' + 'affiliate'
-- CHECK at-most-one ecosystem ref column non-null
```

From P226 deal_rooms + handoff_records + winloss_records (A27 soft refs in co_sell_opportunities):
```sql
-- co_sell_opportunities.partner_dealroom_id → deal_rooms.deal_room_id
-- co_sell_opportunities.handoff_record_id → handoff_records.handoff_id
-- co_sell_opportunities.winloss_record_id → winloss_records.record_id
```

From migration 165 (227-01) co_sell_opportunities columns.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 167 (P225 attribution_touches extend) + ecosystem-attribution.ts + at-most-one CHECK + 3 attribution tests</name>
  <read_first>
    - 227-CONTEXT.md D-34..D-36 (P225 attribution_touches FK extension + channel ENUM extend + single ledger)
    - 227-RESEARCH.md §Schema Sketch 7 (lines 775-798) — full migration 167 SQL with channel CHECK extension
    - 227-RESEARCH.md §Pitfall 4 (lines 929-933) — at-most-one ecosystem ref CHECK constraint
    - Open Question 3 (RESEARCH lines 1203-1206) — channel ENUM vs CHECK constraint detection
    - 227-VALIDATION.md slice 227-06 row + Coverage Targets "attribution_touches FK columns 100% of at-most-one constraint"
    - P225 D-06 attribution_touches schema (channel column type)
  </read_first>
  <files>
    contracts/F-197-ecosystem-attribution-extend-v1.json
    supabase/migrations/167_p225_attribution_touches_ecosystem_extend.sql
    lib/markos/ecosystem/attribution/ecosystem-attribution.ts
    test/ecosystem/attribution/ecosystem-attribution.test.ts
    test/ecosystem/attribution/channel-enum.test.ts
    test/ecosystem/attribution/at-most-one-ecosystem-ref.test.ts
  </files>
  <behavior>
    - **F-197 attribution-extend contract:** Schema for the 6 ecosystem FK columns + 'marketplace'/'affiliate' channel values; type='read', actor='Operator'.
    - **Migration 167 (D-34 + Pitfall 4):**
      - `ALTER TABLE attribution_touches ADD COLUMN IF NOT EXISTS marketplace_listing_id uuid REFERENCES integration_listings(listing_id), ADD ...partner_id uuid (soft ref — partner_profiles), ADD ...referral_program_id uuid (soft ref), ADD ...affiliate_program_id uuid REFERENCES affiliate_programs(program_id), ADD ...community_signal_id uuid REFERENCES community_signals(signal_id), ADD ...co_sell_opportunity_id uuid REFERENCES co_sell_opportunities(cosell_id)`.
      - **Channel ENUM extend (Open Question 3):** Detect existing constraint type:
        - If CHECK constraint: `ALTER TABLE attribution_touches DROP CONSTRAINT IF EXISTS attribution_touches_channel_check; ADD CONSTRAINT attribution_touches_channel_check CHECK (channel IN (...full list including 'marketplace','affiliate'))`.
        - If Postgres ENUM type: `ALTER TYPE attribution_channel ADD VALUE IF NOT EXISTS 'marketplace'; ADD VALUE IF NOT EXISTS 'affiliate'`.
        - Migration script uses `DO $$ ... pg_typeof(...)` block to detect at runtime.
      - **At-most-one CHECK constraint (Pitfall 4):** `ALTER TABLE attribution_touches ADD CONSTRAINT attribution_touches_at_most_one_ecosystem_ref CHECK ((CASE WHEN marketplace_listing_id IS NOT NULL THEN 1 ELSE 0 END) + (CASE WHEN partner_id IS NOT NULL THEN 1 ELSE 0 END) + (CASE WHEN referral_program_id IS NOT NULL THEN 1 ELSE 0 END) + (CASE WHEN affiliate_program_id IS NOT NULL THEN 1 ELSE 0 END) + (CASE WHEN community_signal_id IS NOT NULL THEN 1 ELSE 0 END) + (CASE WHEN co_sell_opportunity_id IS NOT NULL THEN 1 ELSE 0 END) <= 1)`.
      - Index `idx_attr_touches_ecosystem` (tenant_id, channel) WHERE channel IN ('marketplace','affiliate','partner','referral','community').
    - **ecosystem-attribution.ts:**
      - `ECOSYSTEM_REF_COLUMNS = ['marketplace_listing_id','partner_id','referral_program_id','affiliate_program_id','community_signal_id','co_sell_opportunity_id'] as const`.
      - `attachEcosystemTouch(touch_id, ecosystemRef)`: validates only ONE of the 6 columns is set in `ecosystemRef`; UPDATEs attribution_touches row. Throws `MULTIPLE_ECOSYSTEM_REFS` if more than one column set.
      - Belt-and-suspenders: even though DB CHECK enforces, app-layer guard returns clearer error to API consumers.
  </behavior>
  <action>
    Step 1 — Create `contracts/F-197-ecosystem-attribution-extend-v1.json`.
    Step 2 — Create `supabase/migrations/167_p225_attribution_touches_ecosystem_extend.sql`:
      - 6 ADD COLUMN IF NOT EXISTS statements
      - DO block detecting if `attribution_touches.channel` is CHECK-constraint or ENUM type:
        ```sql
        DO $$
        DECLARE
          col_type text;
        BEGIN
          SELECT pg_typeof(channel)::text INTO col_type FROM attribution_touches LIMIT 1;
          -- Or: SELECT data_type FROM information_schema.columns WHERE table_name='attribution_touches' AND column_name='channel'
          -- If 'text' (CHECK) → DROP+ADD CHECK constraint; if ENUM → ALTER TYPE
          ...
        END $$;
        ```
      - Add `attribution_touches_at_most_one_ecosystem_ref` CHECK constraint
      - Add `idx_attr_touches_ecosystem` index
      - Use IF NOT EXISTS where possible; document non-idempotent ENUM ALTER TYPE caveat
    Step 3 — Create `lib/markos/ecosystem/attribution/ecosystem-attribution.ts`:
      - Export ECOSYSTEM_REF_COLUMNS
      - `attachEcosystemTouch(touch_id, ref)` with at-most-one validation throwing MULTIPLE_ECOSYSTEM_REFS
      - `computeAttributionForCoSell(cosell_id)` — read-only; returns attribution_touch_ids[] linked to cosell.
    Step 4 — Create `test/ecosystem/attribution/ecosystem-attribution.test.ts`:
      - attachEcosystemTouch with single column set → UPDATE succeeds.
      - attachEcosystemTouch with multiple columns set → throws MULTIPLE_ECOSYSTEM_REFS.
      - attachEcosystemTouch with no columns set → no-op (or throws — design choice; test asserts behavior).
    Step 5 — Create `test/ecosystem/attribution/channel-enum.test.ts`:
      - INSERT attribution_touches with channel='marketplace' → success.
      - INSERT with channel='affiliate' → success.
      - INSERT with channel='partner' (existing P225 value) → success (backward compat).
      - INSERT with channel='invalid' → CHECK violation 23514.
    Step 6 — Create `test/ecosystem/attribution/at-most-one-ecosystem-ref.test.ts`:
      - INSERT attribution_touches with marketplace_listing_id only → success.
      - INSERT with partner_id only → success.
      - INSERT with both marketplace_listing_id + partner_id → CHECK violation 23514 (DB-level enforcement).
      - INSERT with all 6 NULL → success (no ecosystem ref required).
      - UPDATE setting marketplace_listing_id when affiliate_program_id already non-null → CHECK violation.
    Step 7 — Run vitest.
  </action>
  <verify>
    <automated>npx vitest run test/ecosystem/attribution/ecosystem-attribution.test.ts test/ecosystem/attribution/channel-enum.test.ts test/ecosystem/attribution/at-most-one-ecosystem-ref.test.ts</automated>
    Plus checks:
    - `grep "ALTER TABLE attribution_touches" supabase/migrations/167_*.sql` returns ≥1
    - `grep -E "marketplace_listing_id|affiliate_program_id|community_signal_id|co_sell_opportunity_id" supabase/migrations/167_*.sql` returns ≥4
    - `grep -E "marketplace.*affiliate|attribution_touches_channel_check.*marketplace" supabase/migrations/167_*.sql` returns ≥1 (channel extend)
    - `grep "attribution_touches_at_most_one_ecosystem_ref" supabase/migrations/167_*.sql` returns ≥1
    - `grep "ECOSYSTEM_REF_COLUMNS" lib/markos/ecosystem/attribution/ecosystem-attribution.ts` returns ≥2
    - `grep "MULTIPLE_ECOSYSTEM_REFS" lib/markos/ecosystem/attribution/ecosystem-attribution.ts` returns ≥1
  </verify>
  <done>
    F-197 contract registered; migration 167 adds 6 nullable FK columns + channel ENUM extension + at-most-one CHECK + index; ecosystem-attribution.ts enforces at-most-one at app layer; 3 attribution tests cover channel + multi-ref violation + correct single-ref usage.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build cosell-service.ts state machine + commission immutability + P222 lifecycle hook (in-transaction) + 4 cosell tests</name>
  <read_first>
    - 227-CONTEXT.md D-08 (CoSellOpportunity SOR), D-30..D-33 (state machine + auto-create from P222 + commission immutability + partner-facing dealroom/handoff)
    - 227-RESEARCH.md slice 227-06 spec (lines 864-873) — full state machine + commission immutability + P222 sync in-transaction
    - 227-RESEARCH.md §Pitfall 8 (lines 953-957) — P222 lifecycle sync in SAME transaction (NOT fire-and-forget); fail-closed
    - 227-RESEARCH.md §Integration Contracts P222 lifecycle hook (lines 1025-1029) — same-RPC transaction scope
    - 227-CONTEXT.md D-65 — commission_share never raw input; bind to P205
    - 226-CONTEXT.md D-21..D-24 — Quote-as-Snapshot immutability pattern (CoSell mirrors)
    - migration 165 columns (commission_locked_at + status state machine)
  </read_first>
  <files>
    contracts/F-196-ecosystem-cosell-state-v1.json
    lib/markos/ecosystem/co-sell/cosell-service.ts
    lib/markos/ecosystem/co-sell/cosell-attribution.ts
    lib/markos/ecosystem/co-sell/p222-lifecycle-hook.ts
    test/ecosystem/co-sell/cosell-state-machine.test.ts
    test/ecosystem/co-sell/cosell-commission-immutable.test.ts
    test/ecosystem/co-sell/cosell-p222-sync.test.ts
    test/ecosystem/co-sell/lifecycle-hook.test.ts
  </files>
  <behavior>
    - **F-196 cosell-state contract:** Schema for state transitions {cosell_id, from_status, to_status, commission_share?, rev_share_terms_id?, evidence_refs}; type='write/MCP', actor='Operator/Partner'.
    - **cosell-service.ts:**
      - `COSELL_TRANSITIONS` const Record:
        - proposed → accepted | withdrawn
        - accepted → active | withdrawn
        - active → won | lost | withdrawn
        - won, lost, withdrawn: terminal
      - `createCoSellOpportunity({tenant_id, opportunity_id, partner_id, ...})`: validates `guardCommissionInput(input)` (D-65); inserts row status='proposed'; commission_share + rev_share_terms_id stored if provided. created_at=now(). NO commission_locked_at yet.
      - `acceptCoSell(cosell_id, opts)`:
        - Reads current row; assertTransitionAllowed(current.status, 'accepted').
        - **Commission lock:** `commission_locked_at = now()`. Subsequent updates to commission_share rejected via `updateCoSell` guard (Pitfall analogous to P226 D-21 Quote-as-Snapshot).
        - Resolve final commission via `resolveCommissionRate(rev_share_terms_id)` from 227-04. If placeholder returned, log warning + emit operator task; do NOT block the acceptance (placeholder is allowed pending P205 approval).
        - Calls `createApprovalPackage('cosell_acceptance', cosell_id, evidence_refs, rev_share_terms_id)` (D-45).
        - Sets accepted_at=now(); status='accepted' (after approval consumed by 227-07 API).
      - `withdrawCoSell(cosell_id, reason)`: from any non-terminal → 'withdrawn'.
      - `completeCoSell(cosell_id, outcome)`: transitions active → won/lost; sets closed_at + winloss_record_id (if from P226 winloss exists).
      - `transitionCoSellStatus(cosell_id, to_status)`: generic transition with assertTransitionAllowed.
      - `updateCoSell(cosell_id, patch)`: rejects mutations to `commission_share` if `commission_locked_at IS NOT NULL` → throws `COMMISSION_IMMUTABLE_POST_ACCEPTANCE` (422 at API layer).
    - **cosell-attribution.ts:**
      - `attachAttributionTouch(cosell_id, touch_id)`: appends touch_id to co_sell_opportunities.attribution_touch_ids[] array.
      - `computeAttributionForCoSell(cosell_id)`: reads all attribution_touches WHERE co_sell_opportunity_id=cosell_id; returns per-model rollup. Reads through P225 attribution_models.
    - **p222-lifecycle-hook.ts:**
      - `syncCoSellFromOpportunity(opportunity_id, transition, ctx)` (lifecycle hook signature):
        - Read Opportunity from P222.
        - If `source_motion='partner'` AND no co_sell_opportunity exists: create one with status='proposed' IN THE SAME ctx (Pitfall 8).
        - If transition.to_stage IN ('customer','lost'):
          - 'customer' → CoSellOpportunity.status='won' + winloss_record_id linked
          - 'lost' → CoSellOpportunity.status='lost' + winloss_record_id linked
        - All UPDATE/INSERT use ctx.transaction (in-transaction; fail throws → P222 lifecycle_transition rolls back).
      - `registerLifecycleHook()`: registers `syncCoSellFromOpportunity` with P222 lifecycle hook system at module load. **A4 fallback:** if P222 module absent, registration is a no-op + warning logged (does not break P227 build).
  </behavior>
  <action>
    Step 1 — Create `contracts/F-196-ecosystem-cosell-state-v1.json`.
    Step 2 — Create `lib/markos/ecosystem/co-sell/cosell-service.ts`:
      - COSELL_TRANSITIONS Record
      - All 5 functions per behavior
      - acceptCoSell: sets commission_locked_at=now() in same UPDATE that flips status='accepted'; calls createApprovalPackage('cosell_acceptance', ...)
      - updateCoSell guard: if `existingRow.commission_locked_at !== null && patch.commission_share !== undefined` throw COMMISSION_IMMUTABLE_POST_ACCEPTANCE
    Step 3 — Create `lib/markos/ecosystem/co-sell/cosell-attribution.ts`:
      - `attachAttributionTouch(cosell_id, touch_id)` — uses Supabase `array_append` or atomic update
      - `computeAttributionForCoSell(cosell_id)` — JOIN attribution_touches with attribution_models for per-model rollup
    Step 4 — Create `lib/markos/ecosystem/co-sell/p222-lifecycle-hook.ts`:
      - `syncCoSellFromOpportunity` reads Opportunity; checks source_motion; auto-creates cosell on first encounter; updates status on subsequent transitions.
      - All operations within ctx.transaction (use Supabase RPC or explicit BEGIN/COMMIT).
      - throw on failure → caller rolls back lifecycle_transition (Pitfall 8 fail-closed).
      - registerLifecycleHook called at module load; A4 fallback if P222 absent.
    Step 5 — Create `test/ecosystem/co-sell/cosell-state-machine.test.ts`:
      - All 6 transitions tested:
        - proposed → accepted (with createApprovalPackage mock); proposed → withdrawn; accepted → active; accepted → withdrawn; active → won; active → lost; active → withdrawn.
      - Blocked transitions: proposed → won (skip accepted/active) rejected; won → active (terminal) rejected.
      - Each transition writes audit row.
    Step 6 — Create `test/ecosystem/co-sell/cosell-commission-immutable.test.ts`:
      - Create cosell with commission_share=0.10; status=proposed → no commission_locked_at.
      - Update commission_share to 0.15 → succeeds (lock not set yet).
      - acceptCoSell → commission_locked_at=now(); status=accepted.
      - Update commission_share to 0.20 → throws COMMISSION_IMMUTABLE_POST_ACCEPTANCE.
      - Update other fields (e.g., partner_dealroom_id) → succeeds (only commission_share is locked).
      - Pricing change scenario: createCoSellOpportunity with new rev_share_terms_id (new cosell row) per D-32 — operator workflow.
    Step 7 — Create `test/ecosystem/co-sell/cosell-p222-sync.test.ts`:
      - Setup Opportunity with source_motion='partner'; trigger lifecycle_transition to_stage='qualified'.
      - syncCoSellFromOpportunity creates co_sell_opportunity status='proposed'.
      - Trigger to_stage='customer'; syncCoSellFromOpportunity flips status='won' + sets winloss_record_id.
      - Pitfall 8: simulate failure inside syncCoSellFromOpportunity (DB error); assert P222 lifecycle_transition rolled back (transaction-level integrity).
    Step 8 — Create `test/ecosystem/co-sell/lifecycle-hook.test.ts`:
      - Mock P222 registerLifecycleHook; assert syncCoSellFromOpportunity registered at import.
      - A4 fallback: simulate P222 module absent (mock import to throw) → registration is no-op + warning; module loads without error.
    Step 9 — Run vitest.

    **{{MARKOS_PRICING_ENGINE_PENDING}} on commission copy (D-46):** When CoSellOpportunity is rendered to a partner-facing surface (227-07 UI), the rev_share_terms_id is resolved via `resolveCommissionRate` (227-04). Service layer here passes the placeholder through without rendering.
  </action>
  <verify>
    <automated>npx vitest run test/ecosystem/co-sell/</automated>
    Plus checks:
    - `grep "COSELL_TRANSITIONS" lib/markos/ecosystem/co-sell/cosell-service.ts` returns ≥2
    - `grep "commission_locked_at" lib/markos/ecosystem/co-sell/cosell-service.ts` returns ≥3 (set at acceptCoSell + guard read in updateCoSell + audit)
    - `grep "COMMISSION_IMMUTABLE_POST_ACCEPTANCE" lib/markos/ecosystem/co-sell/cosell-service.ts` returns ≥1
    - `grep "guardCommissionInput" lib/markos/ecosystem/co-sell/cosell-service.ts` returns ≥1 (D-65 rejection of raw commission)
    - `grep "createApprovalPackage.*cosell_acceptance" lib/markos/ecosystem/co-sell/cosell-service.ts` returns ≥1
    - `grep -E "ctx\.transaction|rpc\(|BEGIN" lib/markos/ecosystem/co-sell/p222-lifecycle-hook.ts` returns ≥1 (in-transaction enforcement)
    - `grep "registerLifecycleHook" lib/markos/ecosystem/co-sell/p222-lifecycle-hook.ts` returns ≥1
    - `grep -E "source_motion\s*===?\s*['\"]partner['\"]" lib/markos/ecosystem/co-sell/p222-lifecycle-hook.ts` returns ≥1
  </verify>
  <done>
    F-196 contract registered; CoSellOpportunity state machine 7 transitions tested + 2 blocked; commission_locked_at set at acceptance; updateCoSell rejects post-acceptance commission_share mutation; createApprovalPackage('cosell_acceptance', ...) emitted; P222 lifecycle hook in-transaction with fail-closed rollback (Pitfall 8); A4 fallback when P222 module absent.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator/partner → commission_share | Direct user input forbidden post-acceptance (D-32 + D-65) |
| P222 lifecycle hook → CoSellOpportunity | Async fire-and-forget would create state divergence (Pitfall 8) |
| attribution_touches → ecosystem ledger | Multiple ecosystem refs would over-count (Pitfall 4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-227-06-01 | Tampering | Commission mutation post-acceptance | mitigate | commission_locked_at set at acceptance; updateCoSell guard throws COMMISSION_IMMUTABLE_POST_ACCEPTANCE; cosell-commission-immutable.test.ts proves rejection |
| T-227-06-02 | Tampering | Raw commission_share input bypassing P205 | mitigate | guardCommissionInput from 227-04 invoked in createCoSellOpportunity + updateCoSell; rejects without rev_share_terms_id |
| T-227-06-03 | Tampering | P222 sync state divergence | mitigate | Pitfall 8 — syncCoSellFromOpportunity runs in-transaction with lifecycle_transition; failure rolls back BOTH writes; cosell-p222-sync.test.ts proves rollback |
| T-227-06-04 | Tampering | Multiple ecosystem refs per attribution_touch | mitigate | DB CHECK constraint at-most-one + app-layer ECOSYSTEM_REF_COLUMNS guard; at-most-one-ecosystem-ref.test.ts proves CHECK violation |
| T-227-06-05 | Spoofing | Parallel ecosystem attribution ledger | mitigate | ECO-05 — single ledger via P225 attribution_touches extension; no new attribution table created |
| T-227-06-06 | Information Disclosure | rev_share_terms_id leak in copy | mitigate | resolveCommissionRate from 227-04 returns PRICING_PENDING_PLACEHOLDER when P205 absent; placeholder rendered in 227-07 partner-facing UI |
| T-227-06-07 | Repudiation | CoSell state transition audit gap | mitigate | Every transition writes markos_audit_log row; commission_locked_at + accepted_at + closed_at preserve timeline |
</threat_model>

<verification>
- F-196 + F-197 contracts registered.
- Migration 167 channel ENUM extension safe across CHECK and Postgres ENUM type variants (Open Question 3 resolution).
- 6 nullable FK columns added; at-most-one CHECK enforced.
- attachEcosystemTouch app-layer guard mirrors DB CHECK.
- CoSell state machine 7 valid transitions + 2 blocked tested.
- Commission immutability proven (acceptCoSell → commission_locked_at; subsequent commission_share PATCH 422).
- P222 lifecycle hook in-transaction; rollback verified.
- A4 fallback (P222 absent) graceful no-op.
- ECO-05 single ledger preserved (no parallel attribution table).
- Legacy regression `npm test` green.
</verification>

<success_criteria>
- 2 contracts (F-196 + F-197).
- Migration 167 with 6 FK columns + channel extend + at-most-one CHECK.
- 5 service modules under `lib/markos/ecosystem/{co-sell,attribution}/`.
- 7 vitest test files green: `npx vitest run test/ecosystem/co-sell/ test/ecosystem/attribution/`.
- COSELL_TRANSITIONS const matches D-30..D-32 state machine.
- commission_locked_at + COMMISSION_IMMUTABLE_POST_ACCEPTANCE error code.
- P222 lifecycle hook in-transaction (Pitfall 8 mitigated).
- ECOSYSTEM_REF_COLUMNS const + at-most-one validation.
- 227-VALIDATION.md per-task map updated for slice 227-06.
- Legacy regression green.
</success_criteria>

<output>
After completion, create `.planning/phases/227-ecosystem-partner-community-developer-growth/227-06-SUMMARY.md` with:
- F-196 + F-197 + migration 167 allocation
- CoSellOpportunity state machine map
- Commission immutability lock pattern (mirror P226 D-21)
- P222 lifecycle hook in-transaction integration
- attribution_touches at-most-one CHECK pattern
- A4 fallback evidence (P222 module absent path)
- Test results (7 files green)
</output>
