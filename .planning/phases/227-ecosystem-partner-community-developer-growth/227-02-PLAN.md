---
phase: 227-ecosystem-partner-community-developer-growth
plan: 02
type: execute
wave: 2
depends_on: ["227-01"]
files_modified:
  - contracts/F-189-ecosystem-install-request-v1.json
  - contracts/F-190-ecosystem-listing-views-v1.json
  - lib/markos/ecosystem/listings/listing-service.ts
  - lib/markos/ecosystem/listings/install-request.ts
  - lib/markos/ecosystem/listings/listing-views.ts
  - lib/markos/ecosystem/listings/isr-cache.ts
  - test/ecosystem/listings/public-filter.test.ts
  - test/ecosystem/listings/install-request.test.ts
  - test/ecosystem/listings/install-bypass-blocked.test.ts
  - test/ecosystem/listings/isr-invalidation.test.ts
  - test/ecosystem/listings/listing-views-aggregation.test.ts
  - test/ecosystem/listings/plugin-manifest-render.test.ts
autonomous: true
requirements: [ECO-01, ECO-04, SG-12, QA-01, QA-02, QA-03, QA-04, QA-05, QA-06, QA-07, QA-08, QA-09, QA-10, QA-11, QA-12, QA-13, QA-14, QA-15]
must_haves:
  truths:
    - "Public marketplace SSR query is double-gated: status='active' AND certification_state='certified'"
    - "Plugin manifest read-through fails closed when manifest missing or stale"
    - "install_request flow: visitor → Approval Inbox → operator approve/reject → install OR rejection (with audit_chain)"
    - "ISR cacheTag fires on certification_state change AND on listing.status change (both directions)"
    - "Direct loader invocation without an approved install_request row is blocked (403)"
    - "listing_views aggregates hourly into integration_listings.view_count via cron"
  artifacts:
    - path: "lib/markos/ecosystem/listings/listing-service.ts"
      provides: "Listing CRUD + getPublicListings (double-gated query) + transitionStatus"
      exports: ["createListing", "updateListing", "getPublicListings", "getListingById", "transitionListingStatus", "PUBLIC_LISTING_FILTER"]
      min_lines: 80
    - path: "lib/markos/ecosystem/listings/install-request.ts"
      provides: "install_request state machine + Approval Inbox emit + audit_chain append"
      exports: ["createInstallRequest", "approveInstallRequest", "rejectInstallRequest", "executeInstall", "INSTALL_REQUEST_TRANSITIONS"]
    - path: "lib/markos/ecosystem/listings/listing-views.ts"
      provides: "listing_views write + hourly aggregation cron + ip_hash/user_agent_hash helpers"
      exports: ["recordListingView", "aggregateViewCounts"]
    - path: "lib/markos/ecosystem/listings/isr-cache.ts"
      provides: "Next.js cacheTag/updateTag wrappers per listing"
      exports: ["listingCacheTag", "invalidateListingCache"]
  key_links:
    - from: "lib/markos/ecosystem/listings/listing-service.ts::getPublicListings"
      to: "integration_listings"
      via: "WHERE status='active' AND certification_state='certified'"
      pattern: "status\\s*=\\s*'active'\\s*AND\\s*certification_state\\s*=\\s*'certified'"
    - from: "lib/markos/ecosystem/listings/install-request.ts::executeInstall"
      to: "lib/markos/plugins/loader.js"
      via: "guard: install_request.status='approved' required before loader call"
      pattern: "status\\s*===\\s*'approved'"
    - from: "lib/markos/ecosystem/listings/install-request.ts::createInstallRequest"
      to: "lib/markos/crm/copilot.ts::createApprovalPackage"
      via: "P208 Approval Inbox emit (entry_kind='ecosystem_install_request')"
      pattern: "createApprovalPackage"
    - from: "lib/markos/ecosystem/listings/isr-cache.ts::invalidateListingCache"
      to: "Next.js updateTag"
      via: "fires on transitionListingStatus + cert state change"
      pattern: "updateTag|revalidateTag"
---

<objective>
Build the IntegrationListing + listing_views + install_requests + plugin-registry adapter operational layer (227-RESEARCH.md slice 227-02). Wave 2 — parallel with 227-03 + 227-05.

Purpose: This slice owns the marketplace listing lifecycle (CRUD + state transitions + public filter), the operator-mediated install workflow (the security boundary per D-37/D-38 and Pitfall 10), and the listing-views analytics with ISR cache invalidation (D-39 + D-40). It does NOT own certification state machine (227-03 owns) — listing_service consumes certification_state read-only.

Output: 2 contracts (F-189/F-190), 4 service modules, 6 vitest test files covering public filter double-gate, install_request state machine, install bypass blocked, ISR invalidation, view aggregation, and plugin manifest render fail-closed.
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-CONTEXT.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-RESEARCH.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-VALIDATION.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-01-PLAN.md
@.planning/phases/202-mcp-server-ga-claude-marketplace/202-CONTEXT.md
@.planning/phases/208-human-operating-interface/208-CONTEXT.md
@.planning/phases/224-conversion-launch-workspace/224-CONTEXT.md
@CLAUDE.md

# Incoming docs (CLAUDE.md precedence rule 5): obsidian/work/incoming/{18,22,25}-*.md INFORMATIONAL ONLY.

<interfaces>
<!-- Outputs from 227-01 this slice consumes. -->

From lib/markos/ecosystem/contracts.ts (227-01):
```typescript
export const LISTING_STATUS = ['active','hidden','archived'] as const;
export const CERTIFICATION_STATE = ['draft','review','certified','suspended','expired','revoked'] as const;
export const INSTALL_REQUEST_STATUS = ['requested','reviewing','approved','installed','failed','rejected','withdrawn'] as const;
export const integrationListingSchema; // F-181 zod
export const installRequestSchema;     // (allocated this slice — F-189)
export const listingViewSchema;        // (allocated this slice — F-190)
```

From lib/markos/ecosystem/adapters/plugin-registry.ts (227-01):
```typescript
export function getPluginManifest(plugin_manifest_id: string): PluginManifestRef | null;
// Returns null on miss. Render fail-closed.
```

From lib/markos/plugins/loader.js (P202; PRESERVED — D-11 — never mutated by ecosystem):
```javascript
// Internal install loader. P227 wraps this with guard logic.
export function activatePlugin(plugin_manifest_id, tenant_id);
```

From lib/markos/crm/copilot.ts (P105):
```typescript
export function createApprovalPackage(packageKind: string, subjectId: string, evidenceRefs: string[], pricingContextId?: string): Promise<ApprovalPackage>;
// New entry kinds for P227 (D-51): 'ecosystem_install_request'
```

From migrations 159 + 164 (227-01):
```sql
-- integration_listings (status, certification_state columns)
-- install_requests (status state machine + audit_chain JSONB)
-- listing_views (ip_hash + user_agent_hash + viewed_at)
-- certification_records (read-only from this slice; 227-03 owns transitions)
```

From Next.js 15 (A16):
```typescript
import { revalidateTag } from 'next/cache'; // Next.js 15 native cache tag invalidation
// Naming note (A16): if using Next.js 14, the API is `revalidateTag` only. Both versions co-exist; use revalidateTag.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build listing-service.ts with double-gated public filter + ISR cache integration</name>
  <read_first>
    - 227-CONTEXT.md D-01 (integration_listings public read filter), D-37 (install workflow), D-40..D-43 (public marketplace + ISR + BotID/rate-limit/honeypot)
    - 227-RESEARCH.md §Pitfall 9 (lines 959-963) — marketplace SEO leak; double-gated public query is mandatory
    - 227-RESEARCH.md §Architecture Patterns + §Pattern 1 (plugin registry adapter render fail-closed)
    - 227-VALIDATION.md ECO-04 row "IntegrationListing public filter (status+cert_state double gate)" + "ISR updateTag fires on certification_state change"
    - lib/markos/ecosystem/contracts.ts (227-01)
    - lib/markos/ecosystem/adapters/plugin-registry.ts (227-01)
  </read_first>
  <files>
    contracts/F-190-ecosystem-listing-views-v1.json
    lib/markos/ecosystem/listings/listing-service.ts
    lib/markos/ecosystem/listings/listing-views.ts
    lib/markos/ecosystem/listings/isr-cache.ts
    test/ecosystem/listings/public-filter.test.ts
    test/ecosystem/listings/listing-views-aggregation.test.ts
    test/ecosystem/listings/isr-invalidation.test.ts
    test/ecosystem/listings/plugin-manifest-render.test.ts
  </files>
  <behavior>
    - **listing-service.ts:**
      - `PUBLIC_LISTING_FILTER` exported as const SQL fragment / typed query: `{ status: 'active', certification_state: 'certified' }` — used in `getPublicListings()`. Per Pitfall 9: NEVER use status filter alone.
      - `createListing(input)`: validates via integrationListingSchema (zod from 227-01); inserts row with `certification_state='draft'` + `status='hidden'` defaults; returns listing.
      - `updateListing(listing_id, patch)`: tenant-scoped update; rejects updates to immutable columns (listing_id, tenant_id, created_at).
      - `getPublicListings(opts)`: Supabase query with PUBLIC_LISTING_FILTER + optional category/owner_type filter + Postgres FTS via `to_tsvector` query plan. Returns rows + plugin manifest joined via `getPluginManifest()` adapter — if manifest missing, returns listing with `manifest_status='unavailable'` flag (D-09 fail-closed; Pitfall 1).
      - `getListingById(listing_id)`: tenant-scoped (or markos-global) fetch; same plugin manifest fail-closed pattern.
      - `transitionListingStatus(listing_id, new_status)`: enforces transitions:
        - `hidden → active`: requires `certification_state='certified'` (D-44/D-45 publish gate); call `createApprovalPackage('listing_publish', listing_id, ...)`; only flips status='active' AFTER approval is granted (consumed via 227-07 API, not directly in service); service-layer write WITH operator+approval audit row.
        - `active → hidden`: operator action; immediate.
        - `* → archived`: terminal; archive cron.
      - On every `transitionListingStatus`, call `invalidateListingCache(listing_id)` to fire ISR `revalidateTag(\`listing-\${listing_id}\`)`.
    - **listing-views.ts:**
      - `recordListingView({listing_id, ip, user_agent, profile_id?, referrer?})`: hashes IP + UA via SHA-256 (NEVER stores raw); inserts listing_views row; returns view_id.
      - `aggregateViewCounts()`: cron handler (call signature for Vercel Cron); for each listing, computes `count(*) WHERE viewed_at >= now() - interval '1 hour'` and updates `integration_listings.view_count = view_count + delta`. Idempotent via timestamp window.
    - **isr-cache.ts:**
      - `listingCacheTag(listing_id) → string`: returns `\`listing-\${listing_id}\``.
      - `invalidateListingCache(listing_id)`: calls `revalidateTag(listingCacheTag(listing_id))`. Wraps Next.js native API (A16). Per D-40 + Pitfall 9 — must be called on ANY change to status OR certification_state.
    - **F-190 listing-views contract** registered.
    - All tests use vitest + 227-01 fixtures. No fs.existsSync-only verifies.
  </behavior>
  <action>
    Step 1 — Create `contracts/F-190-ecosystem-listing-views-v1.json` with JSON Schema covering view_id (uuid), listing_id (uuid required), ip_hash (string required), user_agent_hash (string required), viewed_at (timestamp), referrer (string nullable), time_on_page_seconds (integer nullable), profile_id (uuid nullable). type='read', actor='Operator'.
    Step 2 — Create `lib/markos/ecosystem/listings/listing-service.ts` per behavior above. Export `PUBLIC_LISTING_FILTER` as `{ status: 'active', certification_state: 'certified' } as const`. In `getPublicListings`, build Supabase query: `supabase.from('integration_listings').select('*').match(PUBLIC_LISTING_FILTER)` chained with category/FTS filters. JOIN plugin manifest at app layer (NOT SQL JOIN — soft FK per D-10). Return type includes `manifest_status: 'available' | 'unavailable'` per row.
    Step 3 — Create `lib/markos/ecosystem/listings/listing-views.ts`. Use `crypto.createHash('sha256').update(ip).digest('hex')` for ip_hash; same for user_agent_hash. Truncate to 32 chars to avoid storing full hash if not needed (operator preference; CONTEXT D-39 says "ip_hash" no length specified — use full SHA-256 hex per safety). Aggregation: read latest aggregation timestamp from a small `_meta` table or use deterministic 1-hour window query.
    Step 4 — Create `lib/markos/ecosystem/listings/isr-cache.ts`. Import `revalidateTag` from `next/cache`. Wrap to swallow errors (cache invalidation failure should not break write path; log to markos_audit_log instead).
    Step 5 — Create 4 test files:
      - `public-filter.test.ts`: insert 5 listings (certified+active, certified+hidden, draft+active, suspended+active, expired+active); `getPublicListings()` returns ONLY the certified+active row. Assert SQL query string contains both `status` and `certification_state` filters.
      - `listing-views-aggregation.test.ts`: insert 100 listing_views rows in last hour for one listing; run `aggregateViewCounts()`; assert listing.view_count incremented by 100; re-run; assert no double-count (idempotency).
      - `isr-invalidation.test.ts`: mock `revalidateTag`; call `transitionListingStatus(id, 'active')`; assert `revalidateTag` called with `listing-${id}` tag. Call `transitionListingStatus(id, 'hidden')`; assert call again.
      - `plugin-manifest-render.test.ts`: create listing with plugin_manifest_id; mock `getPluginManifest` returning null (manifest missing); call `getListingById`; assert returned object has `manifest_status='unavailable'` (D-09 fail-closed); mock returning shaped manifest; assert `manifest_status='available'` + manifest fields populated.
    Step 6 — Run `npx vitest run test/ecosystem/listings/public-filter.test.ts test/ecosystem/listings/listing-views-aggregation.test.ts test/ecosystem/listings/isr-invalidation.test.ts test/ecosystem/listings/plugin-manifest-render.test.ts`.

    **{{MARKOS_PRICING_ENGINE_PENDING}} placeholder rule (D-46):** integration_listings.pricing_model is a structural enum, NOT a public-facing rate display. No pricing copy is generated by listing-service.ts. If a partner-facing listing detail page later renders pricing copy (227-07), the content classifier will enforce the placeholder rule there.
  </action>
  <verify>
    <automated>npx vitest run test/ecosystem/listings/public-filter.test.ts test/ecosystem/listings/listing-views-aggregation.test.ts test/ecosystem/listings/isr-invalidation.test.ts test/ecosystem/listings/plugin-manifest-render.test.ts</automated>
    Plus checks:
    - `grep -E "status\s*=\s*'active'\s*AND\s*certification_state\s*=\s*'certified'|status:\s*'active',\s*certification_state:\s*'certified'" lib/markos/ecosystem/listings/listing-service.ts` returns ≥1 match
    - `grep "PUBLIC_LISTING_FILTER" lib/markos/ecosystem/listings/listing-service.ts` returns ≥2 matches (export + usage)
    - `grep "revalidateTag\|updateTag" lib/markos/ecosystem/listings/isr-cache.ts` returns ≥1 match
    - `grep "createHash.*sha256" lib/markos/ecosystem/listings/listing-views.ts` returns ≥2 matches (ip_hash + user_agent_hash)
    - `grep "getPluginManifest" lib/markos/ecosystem/listings/listing-service.ts` returns ≥1 match
    - `grep -E "manifest_status\s*[=:]\s*['\"]unavailable['\"]" lib/markos/ecosystem/listings/listing-service.ts` returns ≥1 match (fail-closed branch)
  </verify>
  <done>
    listing-service.ts double-gated public filter implemented + tested; listing-views aggregation idempotent; ISR invalidation fires on every status/cert transition; plugin manifest render fail-closed when manifest missing.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build install-request.ts with operator approval gate + executeInstall guard + audit_chain</name>
  <read_first>
    - 227-CONTEXT.md D-37, D-38 (install workflow), D-44 (approval gate), D-51 (Approval Inbox entry types)
    - 227-RESEARCH.md §Pitfall 10 (lines 965-969) — install_request bypass blocked
    - 227-RESEARCH.md §Phase Requirements → Test Map: ECO-04 install_request rows
    - 227-VALIDATION.md slice 227-02 row + Manual-Only Verifications row "install_request operator approval"
    - 227-CONTEXT.md D-11 — loader.js NEVER triggers IntegrationListing mutations (separation invariant)
    - lib/markos/crm/copilot.ts createApprovalPackage signature (P105)
    - lib/markos/plugins/loader.js (P202 — read-only reference)
  </read_first>
  <files>
    contracts/F-189-ecosystem-install-request-v1.json
    lib/markos/ecosystem/listings/install-request.ts
    test/ecosystem/listings/install-request.test.ts
    test/ecosystem/listings/install-bypass-blocked.test.ts
  </files>
  <behavior>
    - **F-189 install-request contract**: full schema with status state machine values, audit_chain JSONB shape (`[{event, at, actor_user_id?, evidence_ref?}]`), tombstone-aware columns (requested_by_profile_id + requesting_user_email scrubbed on tombstone per D-62).
    - **install-request.ts:**
      - `INSTALL_REQUEST_TRANSITIONS` const Record exporting allowed transitions:
        - requested → reviewing | rejected | withdrawn
        - reviewing → approved | rejected
        - approved → installed | failed
        - installed: terminal
        - rejected: terminal
        - withdrawn: terminal
        - failed → reviewing (retry)
      - `createInstallRequest({listing_id, requested_by_profile_id, requesting_user_email, request_reason})`: tenant-scoped insert; status='requested'; appends initial audit_chain entry `{event:'created', at:now}`. Calls `createApprovalPackage('ecosystem_install_request', request_id, [], pricing_context_id?)` to emit P208 Approval Inbox entry (D-51).
      - `approveInstallRequest(request_id, reviewer_user_id)`: validates current status='requested' OR 'reviewing'; transitions → 'approved'; appends audit_chain `{event:'approved', at, actor_user_id}`.
      - `rejectInstallRequest(request_id, reviewer_user_id, reason)`: → 'rejected'; appends audit_chain entry with reason.
      - `executeInstall(request_id)`: **GUARD** — first reads install_requests row; if `status !== 'approved'` throws `INSTALL_BYPASS_BLOCKED` error (Pitfall 10). If approved + listing.owner_type='markos' → call `lib/markos/plugins/loader.js::activatePlugin(plugin_manifest_id, tenant_id)`; else (partner-owned) emit operator task `manifest_review_required` (D-37 operator-mediated path). Either path: append audit_chain `{event:'install_executed', at, success:bool}`; flip status → 'installed' on success or 'failed' on error.
    - All transitions write `markos_audit_log` row (D-60); all PII columns (requesting_user_email, requested_by_profile_id) reactive to tombstone scrub (D-62).
  </behavior>
  <action>
    Step 1 — Create `contracts/F-189-ecosystem-install-request-v1.json` with JSON Schema covering all migration 159 install_requests columns + INSTALL_REQUEST_STATUS enum. type='read-write', actor='Operator/Public'.
    Step 2 — Create `lib/markos/ecosystem/listings/install-request.ts` per behavior. State transitions enforced via `assertTransitionAllowed(from, to)` helper analogous to certification state machine pattern in 227-RESEARCH.md §Pattern 2.
    Step 3 — Implement `executeInstall` GUARD as the first statement: `if (request.status !== 'approved') throw new Error('INSTALL_BYPASS_BLOCKED');`. Use shared error code constant exported from contracts.ts (or local export here).
    Step 4 — Audit_chain append: each state transition pushes `{event, at: new Date().toISOString(), actor_user_id?, reason?, evidence_ref?}` to JSONB array via `audit_chain || jsonb_build_array(<entry>)`. Use Supabase RPC for atomic append.
    Step 5 — Create `test/ecosystem/listings/install-request.test.ts`:
      - Test full happy path: `createInstallRequest` → status=requested + Approval Inbox entry created (mock createApprovalPackage; assert called with 'ecosystem_install_request');
      - `approveInstallRequest` → status=approved + audit_chain length=2;
      - `executeInstall` for markos-owned listing → calls loader.activatePlugin (mock); status=installed.
      - State machine blocked transitions: cannot go directly from requested → installed without approve (assert error thrown).
      - Tombstone scrub: insert request with requesting_user_email; simulate tombstone (NULL email + NULL profile_id); assert credit_id + listing_id + audit_chain preserved.
    Step 6 — Create `test/ecosystem/listings/install-bypass-blocked.test.ts`:
      - Insert install_request with status='requested' (NOT approved).
      - Call `executeInstall(request_id)`; assert throws `INSTALL_BYPASS_BLOCKED`.
      - Repeat for status='rejected', 'reviewing', 'withdrawn', 'requested' — all blocked.
      - Assert `lib/markos/plugins/loader.js::activatePlugin` NEVER called (mock + assertNotCalled).
      - Insert with status='approved'; call executeInstall; assert loader.activatePlugin called once.
    Step 7 — Run `npx vitest run test/ecosystem/listings/install-request.test.ts test/ecosystem/listings/install-bypass-blocked.test.ts`.

    **D-11 invariant:** install-request.ts NEVER directly mutates integration_listings via the loader. The install_count increment is a separate operation (in listing-service.ts on `executeInstall` success), preserving the runtime-substrate / business-SOR boundary.
  </action>
  <verify>
    <automated>npx vitest run test/ecosystem/listings/install-request.test.ts test/ecosystem/listings/install-bypass-blocked.test.ts</automated>
    Plus checks:
    - `grep "INSTALL_BYPASS_BLOCKED" lib/markos/ecosystem/listings/install-request.ts` returns ≥1 match
    - `grep "INSTALL_REQUEST_TRANSITIONS\|assertTransitionAllowed" lib/markos/ecosystem/listings/install-request.ts` returns ≥1 match
    - `grep "createApprovalPackage.*ecosystem_install_request" lib/markos/ecosystem/listings/install-request.ts` returns ≥1 match
    - `grep "audit_chain" lib/markos/ecosystem/listings/install-request.ts` returns ≥3 matches (multiple append sites)
    - `node -e "const c=JSON.parse(require('fs').readFileSync('contracts/F-189-ecosystem-install-request-v1.json','utf8')); if(c.id!=='F-189'||!c.schema.properties.status)process.exit(1)"`
  </verify>
  <done>
    install_request state machine enforced; createApprovalPackage emits Approval Inbox entry on creation; executeInstall guard blocks every non-approved status; loader never invoked without approved install_request; audit_chain appended on every transition; tombstone-aware columns nullable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public visitor → marketplace | Untrusted browse; uncertified listings must NEVER appear (Pitfall 9) |
| install_request → plugin loader | Install bypass would let visitors run arbitrary plugins (Pitfall 10) |
| plugin_manifest_id → IntegrationListing render | Manifest drift exposes stale capability claims (Pitfall 1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-227-02-01 | Spoofing | Marketplace SEO leak (uncertified indexed) | mitigate | `getPublicListings` enforces `PUBLIC_LISTING_FILTER` const; tested in public-filter.test.ts; SSR renderer (227-07) consumes only this query |
| T-227-02-02 | Elevation | Direct loader invocation bypasses approval | mitigate | `executeInstall` guard rejects non-approved requests; install-bypass-blocked.test.ts covers 4 non-approved statuses; loader.activatePlugin only called when guard passes |
| T-227-02-03 | Tampering | Plugin manifest drift renders stale claims | mitigate | `getPluginManifest` returns null on miss → `manifest_status='unavailable'`; plugin-manifest-render.test.ts asserts fail-closed branch |
| T-227-02-04 | Information Disclosure | listing_views raw IP storage | mitigate | `recordListingView` SHA-256 hashes ip + user_agent before insert; schema enforces hash columns only |
| T-227-02-05 | Repudiation | install_request audit gap | mitigate | audit_chain JSONB appended on every transition (created/approved/rejected/installed/failed); markos_audit_log emitted in parallel |
| T-227-02-06 | DoS | listing_views aggregation cron storm | accept | Hourly cron with deterministic window; idempotent; spike protection added in 227-07 monitoring (D-56) |
| T-227-02-07 | Tampering | install_request status mutation outside service | accept | RLS isolates tenant rows; service-role bypass possible — addressed via 227-07 cross-tenant denial test |
</threat_model>

<verification>
- public-filter.test.ts proves double-gate query rejects 4 non-active-or-non-certified listings.
- install-bypass-blocked.test.ts proves loader never called without approved request.
- isr-invalidation.test.ts proves revalidateTag fires on every status transition (both directions).
- plugin-manifest-render.test.ts proves manifest_status='unavailable' fail-closed branch.
- listing-views-aggregation.test.ts proves view_count increments idempotently.
- audit_chain JSONB has entries for each state transition.
- tombstone-affected columns (requesting_user_email, requested_by_profile_id) preserve credit_id + listing_id when scrubbed.
- Legacy regression `npm test` green (P201 P208 P220 P221-P226 baseline unaffected).
</verification>

<success_criteria>
- 2 new contracts (F-189, F-190) registered with full schemas.
- 4 service modules under `lib/markos/ecosystem/listings/` exporting documented functions.
- 6 vitest test files green: `npx vitest run test/ecosystem/listings/`.
- PUBLIC_LISTING_FILTER constant referenced in every public read path.
- executeInstall guard blocks all non-approved statuses (4 cases tested).
- ISR `revalidateTag` called on `transitionListingStatus` AND on certification state change (227-03 hooks into invalidateListingCache via shared module).
- 227-VALIDATION.md per-task map updated for slice 227-02.
- Legacy regression green.
</success_criteria>

<output>
After completion, create `.planning/phases/227-ecosystem-partner-community-developer-growth/227-02-SUMMARY.md` with:
- F-189 + F-190 contract registration
- listing-service exports list (with PUBLIC_LISTING_FILTER constant)
- install_request transitions covered
- ISR invalidation tag pattern
- listing-views aggregation cron handler signature
- Plugin manifest render fail-closed branch evidence
- Test results (6 files green)
</output>
