---
phase: 227-ecosystem-partner-community-developer-growth
plan: 07
type: execute
wave: 5
depends_on: ["227-01", "227-02", "227-03", "227-04", "227-05", "227-06"]
files_modified:
  - contracts/F-198-ecosystem-marketplace-public-v1.json
  - supabase/migrations/169_ecosystem_honeypot_botid_policy.sql
  - supabase/migrations/170_ecosystem_rls_hardening.sql
  - supabase/migrations/171_ecosystem_openapi_regen.sql
  - api/v1/ecosystem/listings.js
  - api/v1/ecosystem/partners.js
  - api/v1/ecosystem/certifications.js
  - api/v1/ecosystem/referrals/programs.js
  - api/v1/ecosystem/referrals/credits.js
  - api/v1/ecosystem/affiliates/programs.js
  - api/v1/ecosystem/affiliates/credits.js
  - api/v1/ecosystem/co-sell.js
  - api/v1/ecosystem/community-signals.js
  - api/v1/ecosystem/developer-events.js
  - api/v1/ecosystem/attribution.js
  - api/v1/ecosystem/fraud-signals.js
  - api/v1/public/ecosystem/contact-listing.js
  - api/v1/public/ecosystem/install-request.js
  - lib/markos/mcp/tools/ecosystem-get-listing.ts
  - lib/markos/mcp/tools/ecosystem-list-partners.ts
  - lib/markos/mcp/tools/ecosystem-evaluate-referral.ts
  - lib/markos/mcp/tools/ecosystem-evaluate-affiliate.ts
  - lib/markos/mcp/tools/ecosystem-list-community-signals.ts
  - lib/markos/mcp/tools/ecosystem-route-signal-to-action.ts
  - lib/markos/mcp/tools/ecosystem-get-cosell-opportunity.ts
  - lib/markos/mcp/tools/ecosystem-list-certified-partners.ts
  - app/(markos)/ecosystem/page.tsx
  - app/(markos)/ecosystem/listings/page.tsx
  - app/(markos)/ecosystem/partners/page.tsx
  - app/(markos)/ecosystem/certifications/page.tsx
  - app/(markos)/ecosystem/referrals/page.tsx
  - app/(markos)/ecosystem/affiliates/page.tsx
  - app/(markos)/ecosystem/community/page.tsx
  - app/(markos)/ecosystem/co-sell/page.tsx
  - app/(public)/marketplace/[...slug]/page.tsx
  - app/(public)/developers/[...slug]/page.tsx
  - app/(public)/partners/[...slug]/page.tsx
  - app/sitemap.ts
  - app/robots.ts
  - app/(markos)/_components/approval-inbox/ecosystem-entries.tsx
  - app/(markos)/_components/morning-brief/ecosystem-section.tsx
  - lib/markos/ecosystem/observability/recertification-coverage-cron.ts
  - lib/markos/ecosystem/observability/payout-coverage-cron.ts
  - lib/markos/ecosystem/observability/community-volume-spike-cron.ts
  - lib/markos/ecosystem/observability/marketplace-view-spike-cron.ts
  - public/llms.txt
  - test/ecosystem/api/contract-parity.test.ts
  - test/ecosystem/api/cross-tenant-denial.test.ts
  - test/ecosystem/mcp/mcp-tools.test.ts
  - test/ecosystem/seo/sitemap.test.ts
  - test/ecosystem/seo/structured-data.test.ts
  - test/ecosystem/seo/robots.test.ts
  - test/ecosystem/security/rls-suite.test.ts
  - test/ecosystem/security/tombstone-cascade.test.ts
  - test/ecosystem/security/marketplace-bot-rate-limit.test.ts
  - test/regression/ecosystem-legacy.test.ts
  - tests/ecosystem/certification-review.spec.ts
  - tests/ecosystem/listing-publish.spec.ts
  - tests/ecosystem/fraud-payout.spec.ts
  - tests/ecosystem/community-signal-route.spec.ts
  - tests/ecosystem/cosell-acceptance.spec.ts
  - tests/ecosystem/public-surface-defense.spec.ts
  - tests/ecosystem/approval-gates.spec.ts
  - .storybook/stories/ecosystem/EcosystemWorkspace.stories.tsx
  - .storybook/stories/ecosystem/PartnerDirectory.stories.tsx
  - .storybook/stories/ecosystem/MarketplaceListings.stories.tsx
  - .storybook/stories/ecosystem/CertificationReview.stories.tsx
  - .storybook/stories/ecosystem/ReferralAdmin.stories.tsx
  - .storybook/stories/ecosystem/AffiliateAdmin.stories.tsx
  - .storybook/stories/ecosystem/CommunitySignalInbox.stories.tsx
  - .storybook/stories/ecosystem/CoSellCockpit.stories.tsx
autonomous: false
requirements: [ECO-01, ECO-02, ECO-03, ECO-04, ECO-05, SG-04, SG-06, SG-09, SG-10, SG-11, SG-12, QA-01, QA-02, QA-03, QA-04, QA-05, QA-06, QA-07, QA-08, QA-09, QA-10, QA-11, QA-12, QA-13, QA-14, QA-15]
must_haves:
  truths:
    - "All /v1/ecosystem/* API routes (read-write) implemented + OpenAPI parity test green"
    - "8 MCP tools registered with proper actor + approval posture"
    - "7 operator UI workspaces (P208 single-shell) functional"
    - "3 public surfaces (/marketplace, /developers, /partners) SSR + ISR + BotID + rate-limit + honeypot"
    - "sitemap.xml contains ONLY status='active' AND certification_state='certified' listings"
    - "robots.txt disallows /marketplace/*/draft + /marketplace/*/review"
    - "JSON-LD SoftwareApplication structured data shape verified"
    - "Approval Inbox 8 new entry kinds + Morning Brief 5 new sections wired"
    - "RLS suite green across 12 new + 5 P220 extended tables (17 total)"
    - "Tombstone cascade end-to-end (P221 → P227 across all 6 PII-bearing columns)"
    - "Migration 168/169/170/171 in correct wave order (carry P224 Blocker 5 — wave-execution migration order)"
    - "Cross-tenant API denial tested for every /v1/ecosystem/* route"
    - "Legacy regression: P100-P105 + P201 + P220 + P221-P226 baselines green"
  artifacts:
    - path: "api/v1/ecosystem/"
      provides: "Full /v1/ecosystem/* API surface (D-47)"
      min_lines: 600
    - path: "lib/markos/mcp/tools/ecosystem-*.ts"
      provides: "8 MCP tools (D-48)"
    - path: "app/(markos)/ecosystem/"
      provides: "7 operator UI workspaces (D-49)"
    - path: "app/(public)/marketplace/[...slug]/page.tsx"
      provides: "Public marketplace SSR + ISR cacheTag"
      contains: "cacheTag"
    - path: "app/sitemap.ts"
      provides: "Sitemap with PUBLIC_LISTING_FILTER"
      contains: "status.*active.*certification_state.*certified|PUBLIC_LISTING_FILTER"
    - path: "supabase/migrations/170_ecosystem_rls_hardening.sql"
      provides: "RLS hardening pass on all 12 new + 5 P220 extended tables"
    - path: "supabase/migrations/171_ecosystem_openapi_regen.sql"
      provides: "OpenAPI regen + Supabase types regen + flow-registry sync"
  key_links:
    - from: "app/(public)/marketplace/[...slug]/page.tsx"
      to: "lib/markos/ecosystem/listings/listing-service.ts::getPublicListings"
      via: "double-gated query — ONLY status='active' AND cert_state='certified'"
      pattern: "PUBLIC_LISTING_FILTER\\|getPublicListings"
    - from: "api/v1/public/ecosystem/install-request.js"
      to: "lib/markos/auth/botid.ts + rate-limit + honeypot"
      via: "carry P224 D-25..D-28 + P226 D-29..D-33 patterns"
      pattern: "botid\\|rate.?limit\\|honeypot"
    - from: "app/sitemap.ts"
      to: "integration_listings"
      via: "WHERE status='active' AND certification_state='certified'"
      pattern: "PUBLIC_LISTING_FILTER"
    - from: "test/ecosystem/security/tombstone-cascade.test.ts"
      to: "P221 D-24 cascade chain"
      via: "scrubs across community_signals.related_profile_id, developer_events.profile_id, fraud_signals.referrer_profile_id+referred_profile_id, payout_credits.profile_id, install_requests.requested_by_profile_id+requesting_user_email"
      pattern: "tombstone\\|scrub.*profile_id"
---

<objective>
Phase closeout: Full /v1/ecosystem/* API + 8 MCP tools + 7 UI workspaces + 3 public surfaces (marketplace/developers/partners) + Approval Inbox extensions + Morning Brief extensions + 4 observability crons + RLS hardening + 6 Playwright specs + 8 Chromatic stories + tombstone cascade end-to-end + legacy regression + closeout (227-RESEARCH.md slice 227-07). Wave 5 — depends on all prior slices.

Purpose: This slice ships the operator + public surface, observability posture, and cross-cutting test suites that close out Phase 227. Critical: PUBLIC_LISTING_FILTER must propagate to sitemap.xml + robots.txt + SSR (Pitfall 9 SEO leak); BotID + rate-limit + honeypot on all public endpoints (D-43/D-61); Approval Inbox + Morning Brief integration in P208 single-shell; tombstone cascade across 6 PII-bearing tables; OpenAPI regen + Supabase type regen for SDK consumers.

Marked `autonomous: false` because of the human-verify checkpoint at end (final UI sanity + Approval Inbox flow + sitemap inspection).

Output: 1 contract (F-198), 3 migrations (169-171), 12 API routes, 2 public-API routes, 8 MCP tools, 11 UI workspaces/pages, 4 cron handlers, sitemap+robots, 11 vitest test files, 7 Playwright specs, 8 Chromatic stories, llms.txt update.
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-CONTEXT.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-RESEARCH.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-VALIDATION.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-01-PLAN.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-02-PLAN.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-03-PLAN.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-04-PLAN.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-05-PLAN.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-06-PLAN.md
@.planning/phases/202-mcp-server-ga-claude-marketplace/202-CONTEXT.md
@.planning/phases/208-human-operating-interface/208-CONTEXT.md
@.planning/phases/224-conversion-launch-workspace/224-CONTEXT.md
@.planning/phases/226-sales-enablement-deal-execution/226-CONTEXT.md
@.planning/V4.0.0-TESTING-ENVIRONMENT-PLAN.md
@.planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md
@obsidian/reference/HTTP Layer.md
@obsidian/reference/UI Components.md
@CLAUDE.md

# Incoming docs (CLAUDE.md precedence rule 5): obsidian/work/incoming/{18,22,25}-*.md INFORMATIONAL ONLY.

<interfaces>
From 227-02 (lib/markos/ecosystem/listings/listing-service.ts):
```typescript
export const PUBLIC_LISTING_FILTER: { status: 'active', certification_state: 'certified' };
export function getPublicListings(opts): Promise<Listing[]>;
```

From 227-03 (lib/markos/ecosystem/partners/directory.ts):
```typescript
export const PUBLIC_PARTNER_FILTER: { business_mode: ['ecosystem','all'], active_status: 'active', public_directory_visible: true };
export function getPublicDirectory(filters): Promise<Partner[]>;
```

From P201 (lib/markos/auth/botid.ts + rate-limit.ts + honeypot.ts):
```typescript
export function checkBotId(req): Promise<boolean>;
export function rateLimitByIp(req, key, limit, windowSeconds): Promise<boolean>;
export function checkHoneypot(req, fieldName): boolean;
```

From P208 Approval Inbox + Morning Brief shells (D-49 reuses single shell):
```typescript
// app/(markos)/_components/approval-inbox/* — entry registry pattern
export function registerInboxEntryKinds(kinds: string[]): void;
// 8 new kinds for P227: certification_review, listing_publish, ecosystem_install_request, payout_credits_clear, cosell_acceptance, public_directory_toggle, partner_referral_program_activate, affiliate_program_activate
```

From P200 SDK auto-gen (A?):
```typescript
// dev-portal /developers/[...slug] reads SDK output from P200 substrate
```

From migrations 159..168 (227-01..06) — all SOR tables.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build full /v1/ecosystem/* API + 2 public endpoints + 8 MCP tools + OpenAPI parity test + cross-tenant denial test + RLS suite + tombstone cascade</name>
  <read_first>
    - 227-CONTEXT.md D-47 (full API route set), D-48 (8 MCP tools), D-61 (BotID + rate-limit + honeypot on public), D-62 (tombstone propagation chain)
    - 227-RESEARCH.md slice 227-07 spec (lines 877-889) — full closeout scope
    - 227-VALIDATION.md slice 227-07 row + Coverage Targets RLS 100% + Tombstone 100% + API ≥80% branch
    - obsidian/reference/HTTP Layer.md — REST handler auth pattern
    - 226-06-PLAN.md API route pattern (P226 sales precedent for /v1 namespace)
  </read_first>
  <files>
    contracts/F-198-ecosystem-marketplace-public-v1.json
    supabase/migrations/169_ecosystem_honeypot_botid_policy.sql
    supabase/migrations/170_ecosystem_rls_hardening.sql
    supabase/migrations/171_ecosystem_openapi_regen.sql
    api/v1/ecosystem/listings.js
    api/v1/ecosystem/partners.js
    api/v1/ecosystem/certifications.js
    api/v1/ecosystem/referrals/programs.js
    api/v1/ecosystem/referrals/credits.js
    api/v1/ecosystem/affiliates/programs.js
    api/v1/ecosystem/affiliates/credits.js
    api/v1/ecosystem/co-sell.js
    api/v1/ecosystem/community-signals.js
    api/v1/ecosystem/developer-events.js
    api/v1/ecosystem/attribution.js
    api/v1/ecosystem/fraud-signals.js
    api/v1/public/ecosystem/contact-listing.js
    api/v1/public/ecosystem/install-request.js
    lib/markos/mcp/tools/ecosystem-get-listing.ts
    lib/markos/mcp/tools/ecosystem-list-partners.ts
    lib/markos/mcp/tools/ecosystem-evaluate-referral.ts
    lib/markos/mcp/tools/ecosystem-evaluate-affiliate.ts
    lib/markos/mcp/tools/ecosystem-list-community-signals.ts
    lib/markos/mcp/tools/ecosystem-route-signal-to-action.ts
    lib/markos/mcp/tools/ecosystem-get-cosell-opportunity.ts
    lib/markos/mcp/tools/ecosystem-list-certified-partners.ts
    test/ecosystem/api/contract-parity.test.ts
    test/ecosystem/api/cross-tenant-denial.test.ts
    test/ecosystem/mcp/mcp-tools.test.ts
    test/ecosystem/security/rls-suite.test.ts
    test/ecosystem/security/tombstone-cascade.test.ts
    test/ecosystem/security/marketplace-bot-rate-limit.test.ts
    test/regression/ecosystem-legacy.test.ts
  </files>
  <behavior>
    - **F-198 marketplace-public contract:** Schema for the public marketplace + dev-portal + partners read-only contract; type='public-read', actor='Public'.
    - **Migration 169 (honeypot + BotID policy):** INSERT INTO honeypot_field_registry (mirrors P224/P226 pattern) entries for marketplace contact-form + install-request submit. INSERT BotID policy entries.
    - **Migration 170 (RLS hardening pass):** For each of the 12 new tables + 5 P220 extended tables, verify RLS POLICY exists; ADD/REPLACE if missing. Verify markos-global rows policy `tenant_id IS NULL OR tenant_id = current_setting('app.active_tenant_id')` on integration_listings, community_signals, certification_records, partner_profiles.
    - **Migration 171 (OpenAPI regen + Supabase types regen + flow-registry sync):** Either a SQL migration that triggers a regen (if your project uses pg_notify) OR a no-op SQL file with comment + a sibling `scripts/post-migrate/171-regen.cjs` script that runs npm scripts (openapi:generate, supabase:gen-types, flow-registry:sync). Migration is the marker; script does the work.
    - **API routes (12 routes per D-47):** Each follows existing /v1/* convention from P226. Each route handler:
      - Authenticate via Supabase JWT bearer (existing pattern).
      - Resolve tenant context.
      - GET: read query (uses RLS for tenant isolation).
      - POST/PATCH: write via service module from 227-02..06 (e.g., listings.js → listing-service.ts; co-sell.js → cosell-service.ts).
      - Sub-routes for state actions: POST `/v1/ecosystem/listings/{id}/publish`, `/hide`, `/archive`, `/request-install`; POST `/v1/ecosystem/certifications/{id}/{review|certify|suspend|revoke}`; POST `/v1/ecosystem/referrals/credits/{id}/{clear|void}`; POST `/v1/ecosystem/co-sell/{id}/{accept|withdraw|complete}`; POST `/v1/ecosystem/community-signals/{id}/{route|dismiss|escalate}`; POST `/v1/ecosystem/fraud-signals/{id}/{review|confirm|dismiss}`.
      - All write routes call createApprovalPackage where D-45 requires.
      - All routes return shape consistent with F-181..F-198 contracts.
    - **2 public endpoints under api/v1/public/ecosystem/:**
      - `contact-listing.js`: visitor submits contact form for a listing. Apply BotID + rate-limit (10/IP/min) + honeypot. Captures listing_id + email + message + signed_intent. NEVER stores raw IP/UA — only ip_hash via lib/markos/auth pattern.
      - `install-request.js`: visitor submits install request. Same defense. Calls `createInstallRequest` from 227-02.
    - **8 MCP tools (D-48):** Each tool exports `name`, `description`, `inputSchema`, `handler`, and `actor` per existing P226 MCP tool pattern. Tools:
      - `get_listing` → calls listing-service.getListingById (read).
      - `list_partners` → calls directory.getPublicDirectory or partner-service search (read).
      - `evaluate_referral` → calls referral-service.evaluateReferral (qualification + fraud check) (read).
      - `evaluate_affiliate` → calls commission-evaluator.evaluateCommission (read).
      - `list_community_signals` → calls signal-service query for open signals (read).
      - `route_signal_to_action` → calls signal-routing.routeSignal (write; approval may be required for some action_kinds).
      - `get_cosell_opportunity` → calls cosell-service.getCoSellById (read).
      - `list_certified_partners` → filters partners by certification_level + region + specialization_tags (read).
    - **test/ecosystem/api/contract-parity.test.ts:** For each /v1/ecosystem/* route, verify request/response shape matches the F-181..F-198 contract JSON Schema. Uses zod validation. Verifies OpenAPI spec includes every route.
    - **test/ecosystem/api/cross-tenant-denial.test.ts:** For each /v1/ecosystem/* route, sets `app.active_tenant_id` to tenant B; attempts to read tenant A's row → 404 (not 403, to avoid information leak). Tested for every route + every state action.
    - **test/ecosystem/mcp/mcp-tools.test.ts:** Asserts 8 MCP tools registered; each has correct shape (name + description + inputSchema + handler + actor); approval-required tools (route_signal_to_action) emit createApprovalPackage when action_kind requires.
    - **test/ecosystem/security/rls-suite.test.ts:** Cross-tenant denial across all 12 new + 5 P220 extended tables = 17 tables × CRUD = ≥68 cases. Use existing rls-verifier helper.
    - **test/ecosystem/security/tombstone-cascade.test.ts:** P221 D-24 cascade chain end-to-end:
      1. Create P221 IdentityProfile.
      2. Create related rows in: community_signals.related_profile_id, developer_events.profile_id, fraud_signals.referrer_profile_id+referred_profile_id, payout_credits.profile_id, install_requests.requested_by_profile_id+requesting_user_email.
      3. Trigger P221 tombstone (mock).
      4. Assert: profile_id columns NULL; email columns redacted; non-PII data preserved (signal_id, credit_id+amount, audit_chain).
    - **test/ecosystem/security/marketplace-bot-rate-limit.test.ts:** carry P224/P226 pattern. Submit 11 requests in 1 minute from same IP → 11th rate-limited (429). Submit with honeypot field populated → silently rejected. BotID positive → blocked.
    - **test/regression/ecosystem-legacy.test.ts:** Smoke test imports of P100 + P105 + P201 + P220 + P221 + P222 + P223 + P224 + P225 + P226 modules; assert no breakage. Run sample queries against P220 tables `WHERE business_mode='saas'` returns same rows as before P227.
  </behavior>
  <action>
    Step 1 — Create `contracts/F-198-ecosystem-marketplace-public-v1.json`.
    Step 2 — Create `supabase/migrations/169_ecosystem_honeypot_botid_policy.sql` per D-43/D-61 + P224/P226 pattern. Carry-forward registry entries.
    Step 3 — Create `supabase/migrations/170_ecosystem_rls_hardening.sql` running RLS policy verification + ALTER POLICY where needed across 17 tables. Includes verification block: `DO $$ BEGIN ... assert all 17 tables have RLS enabled ... END $$`.
    Step 4 — Create `supabase/migrations/171_ecosystem_openapi_regen.sql` with `-- triggers post-migration regen via scripts/post-migrate/171-regen.cjs` comment. Wave-execution migration order (carry P224 Blocker 5): 159 → 160 → 161 → 162 → 163 → 164 → 165 → (227-03) 168 → (227-05) 166 → (227-06) 167 → (227-07) 169 → 170 → 171. Documented in migration files headers as `-- Wave: NN; Depends on: <prior migrations>`. Documented serial order is recommended; 166 + 168 may run in parallel since no SQL FK dependency between them (166 ALTERs cdp_consent_states from P221; 168 only touches existing 227-01 tables — no cross-dependency).
    Step 5 — Create 12 /v1/ecosystem/* API routes calling service modules from 227-02..06. Each handler ≤80 lines.
    Step 6 — Create 2 /v1/public/ecosystem/* endpoints with BotID + rate-limit + honeypot.
    Step 7 — Create 8 MCP tool files. Pattern follows P226 MCP tool definitions.
    Step 8 — Create 7 vitest test files per behavior. RLS suite has the most cases (≥68). Tombstone cascade is the most complex (6 PII-bearing tables + 1 P221 trigger).
    Step 9 — Run `npx vitest run test/ecosystem/api/ test/ecosystem/mcp/ test/ecosystem/security/ test/regression/ecosystem-legacy.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run test/ecosystem/api/ test/ecosystem/mcp/ test/ecosystem/security/ test/regression/ecosystem-legacy.test.ts</automated>
    Plus checks:
    - `ls api/v1/ecosystem/*.js api/v1/ecosystem/referrals/*.js api/v1/ecosystem/affiliates/*.js | wc -l` returns ≥12
    - `ls api/v1/public/ecosystem/*.js | wc -l` returns 2
    - `ls lib/markos/mcp/tools/ecosystem-*.ts | wc -l` returns 8
    - `grep -c "ENABLE ROW LEVEL SECURITY\|CREATE POLICY\|ALTER POLICY" supabase/migrations/170_*.sql` returns ≥17
    - `grep -E "botid\|rate.?limit\|honeypot" api/v1/public/ecosystem/install-request.js api/v1/public/ecosystem/contact-listing.js` returns ≥4 (each defense-in-depth across 2 endpoints)
    - `grep "createApprovalPackage" api/v1/ecosystem/co-sell.js api/v1/ecosystem/certifications.js api/v1/ecosystem/listings.js api/v1/ecosystem/referrals/credits.js` returns ≥4
    - `grep -E "tenant_id IS NULL" supabase/migrations/170_*.sql` returns ≥4 (markos-global policy on 4 tables)
  </verify>
  <done>
    F-198 contract registered; 3 migrations (169-171); 12 API routes + 2 public endpoints + 8 MCP tools; cross-tenant denial tested per route; RLS suite covers 17 tables; tombstone cascade covers 6 PII tables; legacy regression green; OpenAPI parity test passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2a: Build 7 operator UI workspaces + 3 public pages + sitemap/robots + Approval Inbox/Morning Brief extensions</name>
  <read_first>
    - 227-CONTEXT.md D-49 (7 operator workspaces), D-50 (3 public surfaces), D-51 (Approval Inbox 8 entry kinds), D-52 (Morning Brief 5 surfaces)
    - 227-RESEARCH.md slice 227-07 spec — sitemap/JSON-LD
    - 227-RESEARCH.md §Pitfall 9 — sitemap + robots.txt mandatory hardening
    - obsidian/reference/UI Components.md — workspace shell pattern
  </read_first>
  <files>
    app/(markos)/ecosystem/page.tsx
    app/(markos)/ecosystem/listings/page.tsx
    app/(markos)/ecosystem/partners/page.tsx
    app/(markos)/ecosystem/certifications/page.tsx
    app/(markos)/ecosystem/referrals/page.tsx
    app/(markos)/ecosystem/affiliates/page.tsx
    app/(markos)/ecosystem/community/page.tsx
    app/(markos)/ecosystem/co-sell/page.tsx
    app/(public)/marketplace/[...slug]/page.tsx
    app/(public)/developers/[...slug]/page.tsx
    app/(public)/partners/[...slug]/page.tsx
    app/sitemap.ts
    app/robots.ts
    app/(markos)/_components/approval-inbox/ecosystem-entries.tsx
    app/(markos)/_components/morning-brief/ecosystem-section.tsx
  </files>
  <behavior>
    - **8 operator UI workspaces under app/(markos)/ecosystem/:** Each is a Next.js page within the P208 single shell. Pattern: server component reads from API, renders shadcn-style table/card UI. Each workspace has list view + detail drill-down.
      - `page.tsx` (EcosystemWorkspace): cockpit overview (listing count + partner count + recent signals + open fraud + co-sell deals).
      - `listings/page.tsx` (MarketplaceListings): listing CRUD; mirror P223 D-25 JSON content editor; certification badge; ISR cache control button.
      - `partners/page.tsx` (PartnerDirectory): table + program toggles + commission_share view (renders {{MARKOS_PRICING_ENGINE_PENDING}} when rev_share_terms_id absent).
      - `certifications/page.tsx` (CertificationReview): pending queue + criteria_checks editor + evidence binding + decision.
      - `referrals/page.tsx` (ReferralAdmin): program CRUD + fraud_signals queue + payout_credits review + CSV export trigger.
      - `affiliates/page.tsx` (AffiliateAdmin): same shape as referrals.
      - `community/page.tsx` (CommunitySignalInbox): signals stream + decision_rules routing controls + sentiment/urgency filters.
      - `co-sell/page.tsx` (CoSellCockpit): per-Opportunity partner co-sell view + commission tracking + LaunchSurface partner_pack ref + DealRoom view.
    - **3 public pages under app/(public)/:**
      - `marketplace/[...slug]/page.tsx`: dynamic route reading from listing-service.getListingById + getPluginManifest. SSR. ISR via `cacheTag(\`listing-\${listing_id}\`)`. JSON-LD SoftwareApplication structured data + Open Graph + Twitter Card metadata. **Render-time 404** if not status='active' AND certification_state='certified' (Pitfall 9 belt-and-suspenders).
      - `developers/[...slug]/page.tsx`: reads SDK auto-gen output (P200 substrate; A?). Read-only docs.
      - `partners/[...slug]/page.tsx`: PartnerDirectory.getPublicDirectory; only public_directory_visible=true partners.
    - **app/sitemap.ts:** Next.js metadata export. Returns array of `{ url, lastModified, changeFrequency, priority }` for each listing where `PUBLIC_LISTING_FILTER` matches AND each partner where `PUBLIC_PARTNER_FILTER` matches. NEVER includes draft/review/suspended/expired listings.
    - **app/robots.ts:** Next.js metadata export. `Disallow: /marketplace/*/draft, /marketplace/*/review`. `Allow: /marketplace, /developers, /partners`.
    - **Approval Inbox extension (D-51):** `ecosystem-entries.tsx` registers 8 entry kinds with rendering components: certification_review, listing_publish, ecosystem_install_request, payout_credits_clear, cosell_acceptance, public_directory_toggle, partner_referral_program_activate, affiliate_program_activate.
    - **Morning Brief extension (D-52):** `ecosystem-section.tsx` adds 5 surface tiles: pending certifications, open install_requests, recent fraud_signals (severity≥medium), at-risk co-sell deals, top community signals (sentiment=negative + urgency≥medium).
  </behavior>
  <action>
    Step 1 — Create 8 operator workspace pages. Each ≤200 lines server component. Use existing shadcn UI primitives + P208 layout shell.
    Step 2 — Create 3 public pages under app/(public)/. Marketplace + partners both use server-side `PUBLIC_LISTING_FILTER` / `PUBLIC_PARTNER_FILTER`. Marketplace renders JSON-LD structured data (SoftwareApplication shape + Organization + breadcrumbs). All 3 pages: revalidate via ISR cacheTag.
    Step 3 — Create `app/sitemap.ts` and `app/robots.ts`:
      - sitemap.ts uses `PUBLIC_LISTING_FILTER` + `PUBLIC_PARTNER_FILTER` (re-imported)
      - robots.ts disallows draft/review paths
    Step 4 — Create Approval Inbox + Morning Brief extension components.
  </action>
  <verify>
    <automated>node scripts/post-migrate/227-07-task2a-verify.cjs || node -e "const fs=require('fs'); const paths=['app/(markos)/ecosystem/page.tsx','app/(markos)/ecosystem/listings/page.tsx','app/(markos)/ecosystem/partners/page.tsx','app/(markos)/ecosystem/certifications/page.tsx','app/(markos)/ecosystem/referrals/page.tsx','app/(markos)/ecosystem/affiliates/page.tsx','app/(markos)/ecosystem/community/page.tsx','app/(markos)/ecosystem/co-sell/page.tsx','app/(public)/marketplace/[...slug]/page.tsx','app/(public)/developers/[...slug]/page.tsx','app/(public)/partners/[...slug]/page.tsx','app/sitemap.ts','app/robots.ts','app/(markos)/_components/approval-inbox/ecosystem-entries.tsx','app/(markos)/_components/morning-brief/ecosystem-section.tsx']; for(const f of paths){if(!fs.existsSync(f)){console.error('Missing:',f);process.exit(1)}} const sitemap=fs.readFileSync('app/sitemap.ts','utf8'); if(!/PUBLIC_LISTING_FILTER|getPublicListings/.test(sitemap)){console.error('sitemap.ts missing PUBLIC_LISTING_FILTER');process.exit(1)} const robots=fs.readFileSync('app/robots.ts','utf8'); if(!/Disallow.*(draft|review)/.test(robots)){console.error('robots.ts missing Disallow draft/review');process.exit(1)} const entries=fs.readFileSync('app/(markos)/_components/approval-inbox/ecosystem-entries.tsx','utf8'); const kinds=['certification_review','listing_publish','ecosystem_install_request','payout_credits_clear','cosell_acceptance','public_directory_toggle','partner_referral_program_activate','affiliate_program_activate']; for(const k of kinds){if(!entries.includes(k)){console.error('Missing entry kind:',k);process.exit(1)}} console.log('Task 2a structure OK')"</automated>
    Plus checks:
    - `ls app/\(markos\)/ecosystem/*.tsx app/\(markos\)/ecosystem/**/*.tsx | wc -l` returns ≥8
    - `ls app/\(public\)/marketplace/*/page.tsx app/\(public\)/developers/*/page.tsx app/\(public\)/partners/*/page.tsx | wc -l` returns 3
    - `grep "PUBLIC_LISTING_FILTER\|getPublicListings" app/sitemap.ts` returns ≥1
    - `grep "Disallow.*draft\|Disallow.*review" app/robots.ts` returns ≥1
    - `grep "SoftwareApplication\|application/ld\\+json" app/\(public\)/marketplace/*/page.tsx` returns ≥1
    - `grep "cacheTag" app/\(public\)/marketplace/*/page.tsx` returns ≥1
    - `grep -E "certification_review|listing_publish|ecosystem_install_request|payout_credits_clear|cosell_acceptance|public_directory_toggle|partner_referral_program_activate|affiliate_program_activate" app/\(markos\)/_components/approval-inbox/ecosystem-entries.tsx` returns ≥8 (entry kinds registered)
  </verify>
  <done>
    8 operator UI workspaces + 3 public pages with SSR + ISR + JSON-LD + sitemap + robots; Approval Inbox + Morning Brief extensions wire 8 + 5 entries respectively. ~22 files committed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2b: Build 4 observability crons + 7 Playwright specs + 8 Chromatic stories + 3 SEO vitest tests + llms.txt + tombstone-cascade-e2e closeout</name>
  <read_first>
    - 227-CONTEXT.md D-53..D-58 (4 observability crons + spike alerts)
    - 227-RESEARCH.md slice 227-07 spec — Playwright + Chromatic + RLS + sitemap/JSON-LD
    - 227-VALIDATION.md slice 227-07 row — Playwright + Chromatic per-component variants + sitemap test + structured data test
    - 227-RESEARCH.md §Pitfall 9 — sitemap + robots.txt mandatory hardening
  </read_first>
  <files>
    lib/markos/ecosystem/observability/recertification-coverage-cron.ts
    lib/markos/ecosystem/observability/payout-coverage-cron.ts
    lib/markos/ecosystem/observability/community-volume-spike-cron.ts
    lib/markos/ecosystem/observability/marketplace-view-spike-cron.ts
    public/llms.txt
    test/ecosystem/seo/sitemap.test.ts
    test/ecosystem/seo/structured-data.test.ts
    test/ecosystem/seo/robots.test.ts
    tests/ecosystem/certification-review.spec.ts
    tests/ecosystem/listing-publish.spec.ts
    tests/ecosystem/fraud-payout.spec.ts
    tests/ecosystem/community-signal-route.spec.ts
    tests/ecosystem/cosell-acceptance.spec.ts
    tests/ecosystem/public-surface-defense.spec.ts
    tests/ecosystem/approval-gates.spec.ts
    .storybook/stories/ecosystem/EcosystemWorkspace.stories.tsx
    .storybook/stories/ecosystem/PartnerDirectory.stories.tsx
    .storybook/stories/ecosystem/MarketplaceListings.stories.tsx
    .storybook/stories/ecosystem/CertificationReview.stories.tsx
    .storybook/stories/ecosystem/ReferralAdmin.stories.tsx
    .storybook/stories/ecosystem/AffiliateAdmin.stories.tsx
    .storybook/stories/ecosystem/CommunitySignalInbox.stories.tsx
    .storybook/stories/ecosystem/CoSellCockpit.stories.tsx
  </files>
  <behavior>
    - **4 observability crons:**
      - `recertification-coverage-cron.ts` (D-58): monthly. Asserts no listing/partner with cert past 12 months in 'active' state. Emits operator task on gap.
      - `payout-coverage-cron.ts` (D-57): monthly. Asserts all earned credits are paid OR voided OR disputed. Operator task on gap.
      - `community-volume-spike-cron.ts` (D-55): per-tenant +per-source 24h check >2σ from 7-day baseline. Operator task on spike.
      - `marketplace-view-spike-cron.ts` (D-56): per-listing 24h check >2σ from 7-day baseline (potential brigading). Operator task on spike.
    - **public/llms.txt update:** Append Phase 227 section: ecosystem listings + marketplace + dev-portal + partners directory descriptions per existing P204 pattern.
    - **3 SEO tests:** sitemap.test.ts (only certified+active listings), structured-data.test.ts (JSON-LD shape), robots.test.ts (disallow non-certified paths).
    - **7 Playwright specs:** End-to-end operator + public flows (incl. cross-cutting tombstone-cascade-e2e harness — primary tombstone-cascade vitest in Task 1).
    - **8 Chromatic stories:** Each component × 4 variants (default, loading, empty, error). 32 stories total.
  </behavior>
  <action>
    Step 1 — Create 4 observability cron handlers per D-53..D-58.
    Step 2 — Append Phase 227 section to public/llms.txt (carry P204 pattern).
    Step 3 — Create 3 SEO test files:
      - sitemap.test.ts: insert 4 listings (certified+active, certified+hidden, draft+active, expired+active); call sitemap export; assert ONLY first appears.
      - structured-data.test.ts: render marketplace listing page; parse JSON-LD; assert SoftwareApplication shape; assert all required fields present (name, description, applicationCategory, operatingSystem, etc.).
      - robots.test.ts: parse robots.ts output; assert Disallow lines for /marketplace/*/draft + /marketplace/*/review.
    Step 4 — Create 7 Playwright specs in tests/ecosystem/:
      - certification-review.spec.ts: operator opens Approval Inbox → certification_review entry → reviews criteria → certifies → asserts integration_listings.certification_state='certified' + listing visible on public marketplace.
      - listing-publish.spec.ts: operator publishes listing → Approval Inbox listing_publish → approves → status='active' → public marketplace shows listing.
      - fraud-payout.spec.ts: trigger fraud signal → operator reviews → confirms → payout_credits.fraud_review_status='blocked'.
      - community-signal-route.spec.ts: webhook → community_signal inbox → operator routes to create_task → P208 task list shows entry.
      - cosell-acceptance.spec.ts: P222 Opportunity source_motion='partner' → cosell_opportunity proposed → operator accepts → commission_locked_at set.
      - public-surface-defense.spec.ts: 11 install-request submissions in 1 min from same IP → 11th 429; honeypot field populated → silent reject; BotID positive → 403.
      - approval-gates.spec.ts: SG-12 cross-cutting — assert listing_publish + certification_review + install_request + payout_credits_clear + cosell_acceptance all gate via Approval Inbox.
    Step 5 — Create 8 Chromatic stories. Each component file with 4 variants (default, loading, empty, error). Storybook config consumes existing project Chromatic setup.
    Step 6 — Run all tests:
      - `npx vitest run test/ecosystem/seo/`
      - `npx playwright test tests/ecosystem/`
      - `npx chromatic --ci` (CI gate; locally `npx storybook build`)
  </action>
  <verify>
    <automated>npx vitest run test/ecosystem/seo/ && npx playwright test tests/ecosystem/ --reporter=line</automated>
    Plus checks:
    - `ls tests/ecosystem/*.spec.ts | wc -l` returns 7
    - `ls .storybook/stories/ecosystem/*.stories.tsx | wc -l` returns 8
    - `grep "Phase 227\|ecosystem" public/llms.txt` returns ≥1
    - `ls lib/markos/ecosystem/observability/*-cron.ts | wc -l` returns 4
  </verify>
  <done>
    4 observability crons; 3 SEO tests + 7 Playwright + 8 Chromatic stories; llms.txt Phase 227 section appended; tombstone-cascade-e2e covered by approval-gates harness; legacy regression unaffected. ~18 files committed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 227 closeout — operator-verify ecosystem cockpit + public marketplace + Approval Inbox + sitemap</name>
  <what-built>
    - 7 operator workspaces under /ecosystem (cockpit + listings + partners + certifications + referrals + affiliates + community + co-sell)
    - 3 public surfaces: /marketplace/[...slug], /developers/[...slug], /partners/[...slug]
    - Approval Inbox with 8 new entry kinds (certification_review, listing_publish, ecosystem_install_request, payout_credits_clear, cosell_acceptance, public_directory_toggle, partner_referral_program_activate, affiliate_program_activate)
    - Morning Brief with 5 new sections (pending certs, open install_requests, fraud signals, at-risk co-sell, top community signals)
    - sitemap.xml + robots.txt with PUBLIC_LISTING_FILTER hardening
    - 4 observability crons (recertification coverage, payout coverage, community volume spike, marketplace view spike)
    - All 7 Playwright specs + 8 Chromatic stories green in CI
  </what-built>
  <how-to-verify>
    1. **Operator cockpit:**
       - Visit `/ecosystem` (logged-in tenant). Confirm overview tiles render: listing count, partner count, recent signals, open fraud, co-sell deals.
       - Click into each of 7 sub-workspaces. Verify list views render with data.
    2. **Public marketplace SSR + SEO:**
       - Visit `/marketplace` in incognito. Confirm only certified+active listings appear.
       - Visit `/marketplace/{certified-listing-slug}`. Confirm SSR-rendered. View page source — confirm JSON-LD `<script type="application/ld+json">` with SoftwareApplication shape.
       - Visit `/sitemap.xml`. Confirm only certified+active listings + visible partners listed.
       - Visit `/robots.txt`. Confirm Disallow lines for /marketplace/*/draft and /marketplace/*/review.
       - Curl a draft listing URL → expect 404.
    3. **Approval Inbox flow:**
       - Submit listing for certification → confirm certification_review entry appears in Approval Inbox.
       - Operator approves → cert state flips → ISR cache invalidated → public listing visible.
       - Submit install request as visitor → confirm ecosystem_install_request entry appears → operator approves → install runs.
    4. **Defense:**
       - Submit 11 install-requests in 1 minute from same IP → 11th rate-limited (429).
       - Submit with honeypot field populated → silently rejected.
    5. **Cron operability:**
       - Manually trigger recertification cron via admin endpoint or Vercel CLI; verify operator tasks emitted for upcoming expiries; verify monthly coverage cron emits gap tasks if any cert past 12 months still active.
    6. **Legacy regression:**
       - Run `npm test` — confirm P100-P105 + P201 + P220 + P221-P226 baselines still green.
    7. **Test suite:**
       - `npx vitest run test/ecosystem/` — all green.
       - `npx playwright test tests/ecosystem/` — all green.
       - `npx chromatic --ci` (or local Storybook build) — visual snapshots reviewed.
    8. **Migration order verified:**
       - Inspect supabase/migrations/ — confirm migrations 159..171 in correct numeric order.
       - Confirm 166 (P221 ConsentState extend) and 167 (P225 attribution_touches extend) referenced their P221/P225 dependencies via comments + IF EXISTS guards.
    9. **{{MARKOS_PRICING_ENGINE_PENDING}} placeholder visible:**
       - Visit a partner profile page where rev_share_terms_id is unbound → confirm the placeholder string `{{MARKOS_PRICING_ENGINE_PENDING}}` is rendered (NOT a hard-coded rate).
  </how-to-verify>
  <resume-signal>Type "approved — phase 227 ships" or describe any issues found (UI bugs, sitemap leaks, approval gate misses, regression breakage, placeholder violations)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public visitor → /marketplace/[...slug] | Untrusted SSR; only certified+active listings; render-time 404 belt-and-suspenders |
| Public visitor → contact-listing + install-request | BotID + rate-limit + honeypot defense-in-depth |
| Operator → Approval Inbox decisions | Class-based approval (D-44/D-45) — all listed entry kinds gate critical state changes |
| Tombstone trigger → 6 PII tables | Cascade must scrub all profile_id + email columns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-227-07-01 | Spoofing | SEO leak via uncertified marketplace listing | mitigate | sitemap.test.ts asserts only certified+active; robots.test.ts asserts disallow draft/review; structured-data.test.ts asserts SoftwareApplication shape only on certified listings |
| T-227-07-02 | DoS | Public install-request flood | mitigate | BotID + rate-limit 10/IP/min + honeypot; public-surface-defense.spec.ts proves 429 on 11th |
| T-227-07-03 | Information Disclosure | Cross-tenant API leak | mitigate | cross-tenant-denial.test.ts covers every /v1/ecosystem/* route; RLS suite covers 17 tables |
| T-227-07-04 | Information Disclosure | Tombstone leak (residual profile_id) | mitigate | tombstone-cascade.test.ts asserts NULL across 6 PII tables; D-62 cascade verified |
| T-227-07-05 | Tampering | Bypassed approval gates | mitigate | approval-gates.spec.ts proves listing_publish + cert + install_request + payout_clear + cosell all gate via Approval Inbox |
| T-227-07-06 | Tampering | Migration order error breaking dependent slice | mitigate | Migration headers document `Wave: NN; Depends on: <prior>`; 166 + 167 use IF EXISTS guards (A3/A23 fallbacks) |
| T-227-07-07 | Information Disclosure | Pricing copy leak (raw rate without P205) | mitigate | UI components render PRICING_PENDING_PLACEHOLDER when resolveCommissionRate returns placeholder; verified visually in checkpoint |
| T-227-07-08 | Repudiation | API audit gap | mitigate | Every write API route invokes markos_audit_log via service modules from 227-02..06; migration 170 RLS hardening preserves audit visibility |
</threat_model>

<verification>
- F-198 contract registered.
- Migrations 169-171 — honeypot/BotID + RLS hardening + OpenAPI regen.
- 12 /v1/ecosystem/* routes + 2 public endpoints.
- 8 MCP tools.
- 8 operator UI pages + 3 public pages.
- sitemap.xml + robots.txt + JSON-LD structured data.
- Approval Inbox 8 entry kinds + Morning Brief 5 surfaces.
- 4 observability crons (recertification + payout + community spike + marketplace spike).
- llms.txt Phase 227 section.
- 11 vitest test files + 7 Playwright specs + 8 Chromatic stories.
- Cross-tenant denial across all routes.
- RLS suite 17 tables.
- Tombstone cascade end-to-end (P221 D-24 → P227 D-62 across 6 PII tables).
- Marketplace BotID + rate-limit + honeypot proven.
- Legacy regression `npm test` green (P100-P105 + P201 + P220 + P221-P226).
- Migration order documented + IF EXISTS guards on cross-phase ALTERs (A3 P221, A23 P225, A4 P222 hooks).
- Human-verify checkpoint completed.
</verification>

<success_criteria>
- F-198 contract + 3 closeout migrations.
- Full /v1/ecosystem/* API + 2 public endpoints + 8 MCP tools.
- 7 operator workspaces + 3 public pages.
- Approval Inbox 8 entries + Morning Brief 5 sections.
- sitemap.xml + robots.txt PUBLIC_LISTING_FILTER + JSON-LD SoftwareApplication.
- 4 observability crons.
- llms.txt updated.
- 11 vitest + 7 Playwright + 8 Chromatic = full test suite green.
- Cross-tenant denial + RLS suite (17 tables) + tombstone cascade green.
- Wave-execution migration order honored (Blocker 5 carry from P224).
- 227-VALIDATION.md per-task map updated; nyquist_compliant=true after all 7 plans complete.
- Legacy regression green.
- Human-verify checkpoint approved.
</success_criteria>

<output>
After completion, create `.planning/phases/227-ecosystem-partner-community-developer-growth/227-07-SUMMARY.md` with:
- F-198 + migrations 169-171 allocation
- Full /v1/ecosystem/* route inventory
- 8 MCP tool registry
- 7 UI workspaces + 3 public surfaces
- Sitemap + robots + JSON-LD evidence
- Approval Inbox + Morning Brief integration
- 4 cron handlers
- Test results (Vitest + Playwright + Chromatic)
- Tombstone cascade trace (P221 → P227)
- Legacy regression result
- Wave-execution migration order audit
- Human-verify checkpoint outcome
- Phase 227 closeout: ECO-01..05 + SG-04/06/09..12 + QA-01..15 traceability matrix
</output>
