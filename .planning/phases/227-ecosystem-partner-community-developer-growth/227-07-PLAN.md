---
phase: 227-ecosystem-partner-community-developer-growth
plan: 07
type: execute
wave: 5
depends_on: ["227-01", "227-02", "227-03", "227-04", "227-05", "227-06"]
files_modified:
  - contracts/F-198-ecosystem-marketplace-public-v1.json
  - supabase/migrations/169_ecosystem_honeypot_botid_policy.sql
  - supabase/migrations/170_ecosystem_rls_hardening.sql
  - supabase/migrations/171_ecosystem_openapi_regen.sql
  - api/v1/ecosystem/listings.js
  - api/v1/ecosystem/partners.js
  - api/v1/ecosystem/certifications.js
  - api/v1/ecosystem/referrals/programs.js
  - api/v1/ecosystem/referrals/credits.js
  - api/v1/ecosystem/affiliates/programs.js
  - api/v1/ecosystem/affiliates/credits.js
  - api/v1/ecosystem/co-sell.js
  - api/v1/ecosystem/community-signals.js
  - api/v1/ecosystem/developer-events.js
  - api/v1/ecosystem/attribution.js
  - api/v1/ecosystem/fraud-signals.js
  - api/v1/ecosystem/payouts/export.js
  - api/v1/ecosystem/payouts/reconcile.js
  - api/v1/ecosystem/payouts/compliance-acknowledge.js
  - api/v1/public/ecosystem/contact-listing.js
  - api/v1/public/ecosystem/install-request.js
  - lib/markos/mcp/tools/ecosystem-get-listing.ts
  - lib/markos/mcp/tools/ecosystem-list-partners.ts
  - lib/markos/mcp/tools/ecosystem-evaluate-referral.ts
  - lib/markos/mcp/tools/ecosystem-evaluate-affiliate.ts
  - lib/markos/mcp/tools/ecosystem-list-community-signals.ts
  - lib/markos/mcp/tools/ecosystem-route-signal-to-action.ts
  - lib/markos/mcp/tools/ecosystem-get-cosell-opportunity.ts
  - lib/markos/mcp/tools/ecosystem-list-certified-partners.ts
  - app/(markos)/ecosystem/page.tsx
  - app/(markos)/ecosystem/listings/page.tsx
  - app/(markos)/ecosystem/partners/page.tsx
  - app/(markos)/ecosystem/certifications/page.tsx
  - app/(markos)/ecosystem/referrals/page.tsx
  - app/(markos)/ecosystem/affiliates/page.tsx
  - app/(markos)/ecosystem/community/page.tsx
  - app/(markos)/ecosystem/co-sell/page.tsx
  - app/(markos)/ecosystem/payouts/compliance/page.tsx
  - app/(public)/marketplace/[...slug]/page.tsx
  - app/(public)/developers/[...slug]/page.tsx
  - app/(public)/partners/[...slug]/page.tsx
  - app/sitemap.ts
  - app/robots.ts
  - app/(markos)/_components/approval-inbox/ecosystem-entries.tsx
  - app/(markos)/_components/morning-brief/ecosystem-section.tsx
  - lib/markos/ecosystem/observability/recertification-coverage-cron.ts
  - lib/markos/ecosystem/observability/payout-coverage-cron.ts
  - lib/markos/ecosystem/observability/community-volume-spike-cron.ts
  - lib/markos/ecosystem/observability/marketplace-view-spike-cron.ts
  - public/llms.txt
  - test/ecosystem/api/contract-parity.test.ts
  - test/ecosystem/api/cross-tenant-denial.test.ts
  - test/ecosystem/api/payout-export-compliance-gate.test.ts
  - test/ecosystem/mcp/mcp-tools.test.ts
  - test/ecosystem/seo/sitemap.test.ts
  - test/ecosystem/seo/sitemap-cert-expiry.test.ts
  - test/ecosystem/seo/structured-data.test.ts
  - test/ecosystem/seo/robots.test.ts
  - test/ecosystem/security/rls-suite.test.ts
  - test/ecosystem/security/rls-hardening-stricter.test.ts
  - test/ecosystem/security/tombstone-cascade.test.ts
  - test/ecosystem/security/marketplace-bot-rate-limit.test.ts
  - test/regression/ecosystem-legacy.test.ts
  - tests/ecosystem/certification-review.spec.ts
  - tests/ecosystem/listing-publish.spec.ts
  - tests/ecosystem/fraud-payout.spec.ts
  - tests/ecosystem/community-signal-route.spec.ts
  - tests/ecosystem/cosell-acceptance.spec.ts
  - tests/ecosystem/public-surface-defense.spec.ts
  - tests/ecosystem/approval-gates.spec.ts
  - tests/ecosystem/payout-compliance-acknowledge.spec.ts
  - .storybook/stories/ecosystem/EcosystemWorkspace.stories.tsx
  - .storybook/stories/ecosystem/PartnerDirectory.stories.tsx
  - .storybook/stories/ecosystem/MarketplaceListings.stories.tsx
  - .storybook/stories/ecosystem/CertificationReview.stories.tsx
  - .storybook/stories/ecosystem/ReferralAdmin.stories.tsx
  - .storybook/stories/ecosystem/AffiliateAdmin.stories.tsx
  - .storybook/stories/ecosystem/CommunitySignalInbox.stories.tsx
  - .storybook/stories/ecosystem/CoSellCockpit.stories.tsx
  - .storybook/stories/ecosystem/PayoutComplianceAck.stories.tsx
autonomous: false
requirements: [ECO-01, ECO-02, ECO-03, ECO-04, ECO-05, SG-04, SG-06, SG-09, SG-10, SG-11, SG-12, QA-01, QA-02, QA-03, QA-04, QA-05, QA-06, QA-07, QA-08, QA-09, QA-10, QA-11, QA-12, QA-13, QA-14, QA-15]
must_haves:
  truths:
    - "All /v1/ecosystem/* API routes (read-write) implemented + OpenAPI parity test green"
    - "POST /v1/ecosystem/payouts/export gated on tenant payout_compliance_acknowledged_at (D-71 / RH4) — 422 if not acknowledged"
    - "POST /v1/ecosystem/payouts/reconcile flips exported→paid (D-70 / RH3) — finance-only auth"
    - "POST /v1/ecosystem/payouts/compliance-acknowledge sets payout_compliance_acknowledged_at (D-71)"
    - "8 MCP tools registered with proper actor + approval posture"
    - "8 operator UI workspaces (P208 single-shell) functional + payout compliance ack workspace"
    - "3 public surfaces (/marketplace, /developers, /partners) SSR + ISR + BotID + rate-limit + honeypot"
    - "sitemap.xml contains ONLY status='active' AND certification_state='certified' AND latest cert.expires_at > now() (D-75 / RH5 — TRIPLE gate)"
    - "robots.txt disallows /marketplace/*/draft + /marketplace/*/review"
    - "JSON-LD SoftwareApplication structured data shape verified"
    - "Approval Inbox 8 new entry kinds + Morning Brief 5 new sections wired"
    - "RLS suite green across 12 new + 5 P220 extended tables (17 total) — STRICTER policies on top of Plan 01 initial correctness (RL1 / D-73)"
    - "Migration 170 ADDS DEFENSE-IN-DEPTH to RLS (role-scoped policies, audit-only views) — NOT initial correctness (already in Plan 01)"
    - "Tombstone cascade end-to-end (P221 → P227 across all 6 PII-bearing columns)"
    - "Migration order verified — every cross-phase ALTER (166, 167) gated by precondition guards (D-69 / RH2)"
    - "Cross-tenant API denial tested for every /v1/ecosystem/* route"
    - "Legacy regression: P100-P105 + P201 + P220 + P221-P226 baselines green"
  artifacts:
    - path: "api/v1/ecosystem/"
      provides: "Full /v1/ecosystem/* API surface (D-47 + payouts export/reconcile/compliance — D-70/D-71)"
      min_lines: 700
    - path: "api/v1/ecosystem/payouts/export.js"
      provides: "Export trigger endpoint — gated on D-71 compliance ack"
    - path: "api/v1/ecosystem/payouts/reconcile.js"
      provides: "Finance reconciliation endpoint — D-70 / RH3 — exported→paid"
    - path: "api/v1/ecosystem/payouts/compliance-acknowledge.js"
      provides: "Tenant admin acknowledges payout compliance — D-71 / RH4"
    - path: "lib/markos/mcp/tools/ecosystem-*.ts"
      provides: "8 MCP tools (D-48)"
    - path: "app/(markos)/ecosystem/"
      provides: "8 operator UI workspaces (D-49 + payout compliance ack)"
    - path: "app/(public)/marketplace/[...slug]/page.tsx"
      provides: "Public marketplace SSR + ISR cacheTag + TRIPLE-gate render-time 404 (D-75 / RH5)"
      contains: "cacheTag"
    - path: "app/sitemap.ts"
      provides: "Sitemap with TRIPLE gate (status + cert_state + cert.expires_at > now — D-75 / RH5)"
      contains: "expires_at"
    - path: "supabase/migrations/170_ecosystem_rls_hardening.sql"
      provides: "RLS HARDENING (stricter policies — DEFENSE IN DEPTH on top of Plan 01 initial correctness — RL1 / D-73)"
    - path: "supabase/migrations/171_ecosystem_openapi_regen.sql"
      provides: "OpenAPI regen + Supabase types regen + flow-registry sync"
  key_links:
    - from: "app/(public)/marketplace/[...slug]/page.tsx"
      to: "lib/markos/ecosystem/listings/listing-service.ts::getPublicListings"
      via: "TRIPLE-gated query — status='active' AND cert_state='certified' AND cert.expires_at > now() (D-75 / RH5)"
      pattern: "PUBLIC_LISTING_FILTER\\|getPublicListings\\|expires_at"
    - from: "api/v1/ecosystem/payouts/export.js"
      to: "lib/markos/ecosystem/payouts/payout-compliance-gate.ts"
      via: "RH4 / D-71 — assertPayoutComplianceAcknowledged before export"
      pattern: "assertPayoutComplianceAcknowledged"
    - from: "api/v1/public/ecosystem/install-request.js"
      to: "lib/markos/auth/botid.ts + rate-limit + honeypot"
      via: "carry P224 D-25..D-28 + P226 D-29..D-33 patterns"
      pattern: "botid\\|rate.?limit\\|honeypot"
    - from: "app/sitemap.ts"
      to: "integration_listings + certification_records"
      via: "JOIN cert.expires_at > now() (D-75 / RH5)"
      pattern: "expires_at"
    - from: "test/ecosystem/security/tombstone-cascade.test.ts"
      to: "P221 D-24 cascade chain"
      via: "scrubs across community_signals, developer_events, fraud_signals, payout_credits, install_requests"
      pattern: "tombstone\\|scrub.*profile_id"
---

<objective>
Phase closeout: Full /v1/ecosystem/* API + 8 MCP tools + 8 UI workspaces + 3 public surfaces + Approval Inbox extensions + Morning Brief extensions + 4 observability crons + RLS hardening + Playwright + Chromatic + tombstone cascade + legacy regression + closeout (227-RESEARCH.md slice 227-07). Wave 5.

Purpose: This slice ships the operator + public surface, observability, cross-cutting test suites that close out Phase 227.

**Critical revisions from 227-REVIEWS.md:**
- **RH3 / D-70 (payout reconcile API):** New endpoints `/v1/ecosystem/payouts/export` + `/payouts/reconcile`. Export marks 'exported'. Reconcile flips to 'paid'.
- **RH4 / D-71 (compliance ack endpoint + UI):** New endpoint `/v1/ecosystem/payouts/compliance-acknowledge` + new operator workspace `/ecosystem/payouts/compliance` for tenant admin to acknowledge.
- **RH5 / D-75 (sitemap + SSR cert expiry):** sitemap.ts JOINs cert.expires_at > now(). Marketplace page render-time 404 includes cert expiry check (carry from 227-02 PUBLIC_LISTING_FILTER).
- **RL1 / D-73 (RLS hardening clarified):** Migration 170 is for STRICTER role-scoped policies + defense-in-depth — NOT for fixing missing policies (those are in Plan 01 with rls-policies-correctness.test.ts).

Marked `autonomous: false` due to human-verify checkpoint at end.

Output: 1 contract (F-198), 3 migrations (169-171), 15 API routes (added 3 payout routes), 2 public-API routes, 8 MCP tools, 12 UI workspaces/pages (added payout compliance), 4 cron handlers, sitemap+robots, 14 vitest test files, 8 Playwright specs, 9 Chromatic stories, llms.txt update.
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-CONTEXT.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-RESEARCH.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-VALIDATION.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-01-PLAN.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-02-PLAN.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-03-PLAN.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-04-PLAN.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-05-PLAN.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-06-PLAN.md
@.planning/phases/202-mcp-server-ga-claude-marketplace/202-CONTEXT.md
@.planning/phases/208-human-operating-interface/208-CONTEXT.md
@.planning/phases/224-conversion-launch-workspace/224-CONTEXT.md
@.planning/phases/226-sales-enablement-deal-execution/226-CONTEXT.md
@.planning/V4.0.0-TESTING-ENVIRONMENT-PLAN.md
@.planning/V4.0.0-PHASE-TEST-REQUIREMENTS-MATRIX.md
@obsidian/reference/HTTP Layer.md
@obsidian/reference/UI Components.md
@CLAUDE.md

<interfaces>
From 227-02 (lib/markos/ecosystem/listings/listing-service.ts):

```typescript
export const PUBLIC_LISTING_FILTER: { status: 'active', certification_state: 'certified', cert_expires_after: 'now()' };
export function getPublicListings(opts): Promise<Listing[]>;
export function buildPublicListingsQuery(supabase): SupabaseQueryBuilder;
```

From 227-03 (lib/markos/ecosystem/partners/directory.ts):

```typescript
export const PUBLIC_PARTNER_FILTER: { business_mode: ['ecosystem','all'], active_status: 'active', public_directory_visible: true, cert_expires_after: 'now()' };
export function getPublicDirectory(filters): Promise<Partner[]>;
```

From 227-04 (lib/markos/ecosystem/payouts/payout-compliance-gate.ts):

```typescript
export function assertPayoutComplianceAcknowledged(tenant_id: string): Promise<void>; // throws COMPLIANCE_NOT_ACKNOWLEDGED
export const ACKNOWLEDGEMENT_TTL_MONTHS = 12;
```

From 227-04 (lib/markos/ecosystem/payouts/payout-reconciliation.ts):

```typescript
export function reconcileBatch(input): Promise<ReconcileResult>;
```

From P201 (lib/markos/auth/botid.ts + rate-limit.ts + honeypot.ts):

```typescript
export function checkBotId(req): Promise<boolean>;
export function rateLimitByIp(req, key, limit, windowSeconds): Promise<boolean>;
export function checkHoneypot(req, fieldName): boolean;
```

From P208 Approval Inbox + Morning Brief:

```typescript
// 8 new kinds for P227: certification_review, listing_publish, ecosystem_install_request, payout_credits_clear, cosell_acceptance, public_directory_toggle, partner_referral_program_activate, affiliate_program_activate
```

From migrations 159..168 (227-01..06) — all SOR tables.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build full /v1/ecosystem/* API (incl. payout export/reconcile/compliance — D-70/D-71) + 2 public endpoints + 8 MCP tools + OpenAPI parity test + cross-tenant denial test + RLS suite + RLS hardening test + tombstone cascade</name>
  <read_first>
    - 227-CONTEXT.md D-47 (full API route set), D-48 (8 MCP tools), D-61 (BotID + rate-limit + honeypot), D-62 (tombstone propagation), **D-70 (payout reconcile / RH3), D-71 (compliance ack / RH4), D-73 (RLS hardening clarification / RL1)**
    - 227-RESEARCH.md slice 227-07 spec (lines 877-889)
    - 227-VALIDATION.md slice 227-07 row + Coverage Targets RLS 100% + Tombstone 100% + API ≥80% branch
    - obsidian/reference/HTTP Layer.md — REST handler auth pattern
    - 226-06-PLAN.md API route pattern (P226 sales precedent)
  </read_first>
  <files>
    contracts/F-198-ecosystem-marketplace-public-v1.json
    supabase/migrations/169_ecosystem_honeypot_botid_policy.sql
    supabase/migrations/170_ecosystem_rls_hardening.sql
    supabase/migrations/171_ecosystem_openapi_regen.sql
    api/v1/ecosystem/listings.js
    api/v1/ecosystem/partners.js
    api/v1/ecosystem/certifications.js
    api/v1/ecosystem/referrals/programs.js
    api/v1/ecosystem/referrals/credits.js
    api/v1/ecosystem/affiliates/programs.js
    api/v1/ecosystem/affiliates/credits.js
    api/v1/ecosystem/co-sell.js
    api/v1/ecosystem/community-signals.js
    api/v1/ecosystem/developer-events.js
    api/v1/ecosystem/attribution.js
    api/v1/ecosystem/fraud-signals.js
    api/v1/ecosystem/payouts/export.js
    api/v1/ecosystem/payouts/reconcile.js
    api/v1/ecosystem/payouts/compliance-acknowledge.js
    api/v1/public/ecosystem/contact-listing.js
    api/v1/public/ecosystem/install-request.js
    lib/markos/mcp/tools/ecosystem-get-listing.ts
    lib/markos/mcp/tools/ecosystem-list-partners.ts
    lib/markos/mcp/tools/ecosystem-evaluate-referral.ts
    lib/markos/mcp/tools/ecosystem-evaluate-affiliate.ts
    lib/markos/mcp/tools/ecosystem-list-community-signals.ts
    lib/markos/mcp/tools/ecosystem-route-signal-to-action.ts
    lib/markos/mcp/tools/ecosystem-get-cosell-opportunity.ts
    lib/markos/mcp/tools/ecosystem-list-certified-partners.ts
    test/ecosystem/api/contract-parity.test.ts
    test/ecosystem/api/cross-tenant-denial.test.ts
    test/ecosystem/api/payout-export-compliance-gate.test.ts
    test/ecosystem/mcp/mcp-tools.test.ts
    test/ecosystem/security/rls-suite.test.ts
    test/ecosystem/security/rls-hardening-stricter.test.ts
    test/ecosystem/security/tombstone-cascade.test.ts
    test/ecosystem/security/marketplace-bot-rate-limit.test.ts
    test/regression/ecosystem-legacy.test.ts
  </files>
  <behavior>
    - **F-198 marketplace-public contract:** Schema for public marketplace + dev-portal + partners read-only contract; type='public-read', actor='Public'.
    - **Migration 169 (honeypot + BotID policy):** INSERT INTO honeypot_field_registry per P224/P226 pattern. INSERT BotID policy entries.
    - **Migration 170 (RLS HARDENING — RL1 / D-73):**
      - Plan 01's migrations created INITIAL CORRECT RLS policies. This migration adds STRICTER role-scoped defense-in-depth:
        - Add role-scoped policies (e.g., `operator_role` vs `partner_role` vs `service_role`) where finer-grained access is needed.
        - Add audit-only views for sensitive columns (e.g., commission_share, fraud_signal evidence) limiting which UI roles can read.
        - Add explicit DENY policies on legacy/legacy-style columns to prevent accidental cross-context exposure.
      - **NOT a substitute for Plan 01 correctness:** rls-policies-correctness.test.ts in Plan 01 must pass without this migration. This migration STRENGTHENS, not enables.
      - Header comment: `-- RL1 / D-73: This is RLS HARDENING (defense-in-depth). Plan 01 already established correct base policies. This adds role-scoping + audit-only views.`
    - **Migration 171 (OpenAPI regen):** Marker SQL + sibling `scripts/post-migrate/171-regen.cjs` script.
    - **API routes (15 routes — 12 prior + 3 new):** Each follows /v1/* convention from P226. Each handler:
      - Authenticate via Supabase JWT bearer.
      - Resolve tenant context.
      - Service module dispatch.
      - All write routes call createApprovalPackage where D-45 requires.
    - **NEW: api/v1/ecosystem/payouts/export.js (D-70 / RH3 + D-71 / RH4):**
      - POST endpoint, operator-role only.
      - Step 1: Call `assertPayoutComplianceAcknowledged(tenant_id)` — if throws COMPLIANCE_NOT_ACKNOWLEDGED → 422 with body `{error: 'COMPLIANCE_NOT_ACKNOWLEDGED', remediation: 'Tenant admin must acknowledge payout compliance via /ecosystem/payouts/compliance', acknowledgement_endpoint: '/v1/ecosystem/payouts/compliance-acknowledge'}`.
      - Step 2: Trigger `payoutExportCron()` (or one-shot version `runExportForTenant(tenant_id)`).
      - Returns 200 with `{batch_id, exported_count, total_cents, currency}`.
    - **NEW: api/v1/ecosystem/payouts/reconcile.js (D-70 / RH3):**
      - POST endpoint, FINANCE-role only (separate from operator role; verify via `req.headers['x-finance-token']` matching `MARKOS_FINANCE_TOKEN` env OR a finance-scoped Supabase JWT claim).
      - Body: `{batch_id, confirmed_credit_ids[], reconciliation_evidence_ref?}`.
      - Calls `reconcileBatch(...)` from 227-04.
      - Returns 200 with `{batch_id, paid_count, batch_status}`.
      - Audit: every call writes markos_audit_log with reconciler_user_id + batch_id.
    - **NEW: api/v1/ecosystem/payouts/compliance-acknowledge.js (D-71 / RH4):**
      - POST endpoint, TENANT-ADMIN-role only.
      - Body: `{partner_id?, scope: 'tenant'|'partner', acknowledgement_text, evidence_ref?}` — partner_id optional if scope='tenant'.
      - Validates acknowledgement_text matches expected legal copy template (read-only string from server config).
      - Updates partner_profiles.payout_compliance_acknowledged_at = now() for the scope.
      - Audit-mandatory: writes markos_audit_log with acknowledger_user_id + acknowledgement_text hash.
      - Returns 200 with `{acknowledged_at, expires_at: now()+12mo}`.
    - **2 public endpoints under api/v1/public/ecosystem/:** unchanged shape — BotID + rate-limit + honeypot + ip_hash; install-request calls createInstallRequest from 227-02.
    - **8 MCP tools:** unchanged from prior plan.
    - **test/ecosystem/api/contract-parity.test.ts:** OpenAPI parity per route.
    - **test/ecosystem/api/cross-tenant-denial.test.ts:** every /v1/ecosystem/* route — tenant B → 404 on tenant A's row.
    - **test/ecosystem/api/payout-export-compliance-gate.test.ts (NEW — D-71 / RH4):**
      - POST /v1/ecosystem/payouts/export with tenant having no acknowledgement → 422 + body `{error: 'COMPLIANCE_NOT_ACKNOWLEDGED', ...}`.
      - POST acknowledgement endpoint with tenant admin → 200; partner_profiles.payout_compliance_acknowledged_at set.
      - POST export again → 200 with batch.
      - 13 months later (mock time): export → 422 (TTL expired).
    - **test/ecosystem/mcp/mcp-tools.test.ts:** 8 MCP tools registered with correct shape.
    - **test/ecosystem/security/rls-suite.test.ts:** cross-tenant denial 12 new + 5 P220 extended + 2 P221/P225 extended = 19+ tables × CRUD = ≥76 cases.
    - **test/ecosystem/security/rls-hardening-stricter.test.ts (NEW — RL1 / D-73):**
      - Verifies migration 170's STRICTER policies (role-scoped) — e.g., operator role can read commission_share but partner role gets a redacted projection.
      - Tests that base policies from Plan 01 are NOT replaced — they are augmented.
    - **test/ecosystem/security/tombstone-cascade.test.ts:** P221 → P227 cascade across 6 PII tables.
    - **test/ecosystem/security/marketplace-bot-rate-limit.test.ts:** carry P224/P226 pattern.
    - **test/regression/ecosystem-legacy.test.ts:** P100-P226 baselines green.
  </behavior>
  <action>
    Step 1 — Create `contracts/F-198-ecosystem-marketplace-public-v1.json`.
    Step 2 — Create `supabase/migrations/169_ecosystem_honeypot_botid_policy.sql` per D-43/D-61.
    Step 3 — Create `supabase/migrations/170_ecosystem_rls_hardening.sql` (RL1 / D-73 — STRICTER role-scoped policies, NOT initial correctness). Header comment clarifies relationship to Plan 01.
    Step 4 — Create `supabase/migrations/171_ecosystem_openapi_regen.sql` (marker) + `scripts/post-migrate/171-regen.cjs`.
    Step 5 — **Migration order documentation:** Update migration headers across all 227 migrations (159..171) to document: `-- Wave: NN; Depends on: <prior migrations>; HARD-FAIL preconditions: <list>`. The actual execution order is enforced by Supabase migration tool (numeric); the doc is for human review. Cross-phase ALTERs (166, 167) are gated by precondition guards (227-05-check-upstream + 227-06-check-upstream — D-69 / RH2).
    Step 6 — Create 12 prior /v1/ecosystem/* API routes calling service modules from 227-02..06.
    Step 7 — Create 3 NEW payout API routes:
      - export.js — assertPayoutComplianceAcknowledged FIRST → 422 on fail; then runExport.
      - reconcile.js — finance-role auth; calls reconcileBatch.
      - compliance-acknowledge.js — tenant-admin auth; updates payout_compliance_acknowledged_at.
    Step 8 — Create 2 /v1/public/ecosystem/* endpoints with BotID + rate-limit + honeypot.
    Step 9 — Create 8 MCP tool files.
    Step 10 — Create 9 vitest test files. RLS suite has the most cases (≥76). Tombstone cascade is the most complex.
    Step 11 — Run `npx vitest run test/ecosystem/api/ test/ecosystem/mcp/ test/ecosystem/security/ test/regression/ecosystem-legacy.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run test/ecosystem/api/ test/ecosystem/mcp/ test/ecosystem/security/ test/regression/ecosystem-legacy.test.ts</automated>
    Plus checks:
    - `ls api/v1/ecosystem/*.js api/v1/ecosystem/referrals/*.js api/v1/ecosystem/affiliates/*.js api/v1/ecosystem/payouts/*.js | wc -l` returns ≥15
    - `ls api/v1/public/ecosystem/*.js | wc -l` returns 2
    - `ls lib/markos/mcp/tools/ecosystem-*.ts | wc -l` returns 8
    - `grep -c "ENABLE ROW LEVEL SECURITY\|CREATE POLICY\|ALTER POLICY" supabase/migrations/170_*.sql` returns ≥10 (hardening additions; not stub policies)
    - `grep -E "botid\|rate.?limit\|honeypot" api/v1/public/ecosystem/install-request.js api/v1/public/ecosystem/contact-listing.js` returns ≥4
    - `grep "createApprovalPackage" api/v1/ecosystem/co-sell.js api/v1/ecosystem/certifications.js api/v1/ecosystem/listings.js api/v1/ecosystem/referrals/credits.js` returns ≥4
    - `grep -E "tenant_id IS NULL" supabase/migrations/170_*.sql` returns ≥4
    - **`grep "assertPayoutComplianceAcknowledged" api/v1/ecosystem/payouts/export.js` returns ≥1 (D-71 / RH4)**
    - **`grep "COMPLIANCE_NOT_ACKNOWLEDGED" api/v1/ecosystem/payouts/export.js` returns ≥1 (D-71)**
    - **`grep "reconcileBatch" api/v1/ecosystem/payouts/reconcile.js` returns ≥1 (D-70 / RH3)**
    - **`grep "payout_compliance_acknowledged_at" api/v1/ecosystem/payouts/compliance-acknowledge.js` returns ≥1 (D-71)**
    - **`grep -i "RL1\|D-73\|hardening.*defense\|stricter" supabase/migrations/170_ecosystem_rls_hardening.sql` returns ≥1 (RL1 doc note)**
  </verify>
  <done>
    F-198 contract registered; 3 migrations (169-171); 15 API routes (incl. 3 NEW payouts: export D-71-gated, reconcile D-70-flips-paid, compliance-acknowledge D-71) + 2 public endpoints + 8 MCP tools; cross-tenant denial tested per route; payout-export-compliance-gate.test.ts proves RH4; RLS suite covers 17 tables; rls-hardening-stricter.test.ts verifies migration 170 adds defense-in-depth (RL1); tombstone cascade covers 6 PII tables; legacy regression green; OpenAPI parity test passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2a: Build 8 operator UI workspaces (incl. payout compliance ack — D-71) + 3 public pages with TRIPLE-gate (D-75 / RH5) + sitemap/robots + Approval Inbox/Morning Brief extensions</name>
  <read_first>
    - 227-CONTEXT.md D-49 (operator workspaces), D-50 (3 public surfaces), D-51 (Approval Inbox 8 entry kinds), D-52 (Morning Brief 5 surfaces), **D-71 (compliance ack workspace), D-75 (sitemap TRIPLE gate / RH5)**
    - 227-RESEARCH.md slice 227-07 spec — sitemap/JSON-LD
    - 227-RESEARCH.md §Pitfall 9 — sitemap + robots.txt mandatory hardening
    - 227-REVIEWS.md RH5 — sitemap belongs in 227-02/03 query path; sitemap.ts honors TRIPLE gate
    - obsidian/reference/UI Components.md
  </read_first>
  <files>
    app/(markos)/ecosystem/page.tsx
    app/(markos)/ecosystem/listings/page.tsx
    app/(markos)/ecosystem/partners/page.tsx
    app/(markos)/ecosystem/certifications/page.tsx
    app/(markos)/ecosystem/referrals/page.tsx
    app/(markos)/ecosystem/affiliates/page.tsx
    app/(markos)/ecosystem/community/page.tsx
    app/(markos)/ecosystem/co-sell/page.tsx
    app/(markos)/ecosystem/payouts/compliance/page.tsx
    app/(public)/marketplace/[...slug]/page.tsx
    app/(public)/developers/[...slug]/page.tsx
    app/(public)/partners/[...slug]/page.tsx
    app/sitemap.ts
    app/robots.ts
    app/(markos)/_components/approval-inbox/ecosystem-entries.tsx
    app/(markos)/_components/morning-brief/ecosystem-section.tsx
  </files>
  <behavior>
    - **8 operator UI workspaces** under app/(markos)/ecosystem/: 7 prior workspaces + NEW `payouts/compliance/page.tsx` (D-71 / RH4 — tenant admin acknowledgement form with legal copy + ack button POSTing to /v1/ecosystem/payouts/compliance-acknowledge).
    - **3 public pages under app/(public)/:**
      - `marketplace/[...slug]/page.tsx`: dynamic route reading from listing-service.getPublicListings (TRIPLE-gated — D-75 / RH5). SSR + ISR via `cacheTag(\`listing-\${listing_id}\`)`. JSON-LD SoftwareApplication. **Render-time 404** if listing fails TRIPLE gate (status='active' AND certification_state='certified' AND latest cert.expires_at > now()).
      - `developers/[...slug]/page.tsx`: SDK auto-gen output.
      - `partners/[...slug]/page.tsx`: PartnerDirectory.getPublicDirectory (TRIPLE-gated via cert JOIN — D-75 / RH5).
    - **app/sitemap.ts (D-75 / RH5 — REVISED):** Returns array of `{url, lastModified, changeFrequency, priority}` for each listing where TRIPLE gate matches AND each partner where partner directory predicate matches. NEVER includes draft/review/suspended/expired/cert-past-now listings.
    - **app/robots.ts:** Disallow draft/review paths.
    - **Approval Inbox extension:** 8 entry kinds.
    - **Morning Brief extension:** 5 surface tiles + NEW: `payout_compliance_status` tile (D-71) showing acknowledgement status per partner.
  </behavior>
  <action>
    Step 1 — Create 8 operator workspace pages (incl. NEW payouts/compliance/page.tsx — D-71 / RH4 acknowledgement form).
    Step 2 — Create 3 public pages. **marketplace/[...slug]/page.tsx**: server-side `buildPublicListingsQuery(supabase)` with TRIPLE gate (D-75 / RH5); JSON-LD; cacheTag.
    Step 3 — Create `app/sitemap.ts` and `app/robots.ts`:
      - sitemap.ts uses `buildPublicListingsQuery` AND `buildPublicDirectoryQuery` (TRIPLE-gated — D-75 / RH5)
      - robots.ts disallows draft/review.
    Step 4 — Create Approval Inbox + Morning Brief extension components.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const paths=['app/(markos)/ecosystem/page.tsx','app/(markos)/ecosystem/listings/page.tsx','app/(markos)/ecosystem/partners/page.tsx','app/(markos)/ecosystem/certifications/page.tsx','app/(markos)/ecosystem/referrals/page.tsx','app/(markos)/ecosystem/affiliates/page.tsx','app/(markos)/ecosystem/community/page.tsx','app/(markos)/ecosystem/co-sell/page.tsx','app/(markos)/ecosystem/payouts/compliance/page.tsx','app/(public)/marketplace/[...slug]/page.tsx','app/(public)/developers/[...slug]/page.tsx','app/(public)/partners/[...slug]/page.tsx','app/sitemap.ts','app/robots.ts','app/(markos)/_components/approval-inbox/ecosystem-entries.tsx','app/(markos)/_components/morning-brief/ecosystem-section.tsx']; for(const f of paths){if(!fs.existsSync(f)){console.error('Missing:',f);process.exit(1)}} const sitemap=fs.readFileSync('app/sitemap.ts','utf8'); if(!/PUBLIC_LISTING_FILTER|getPublicListings|buildPublicListingsQuery/.test(sitemap)){console.error('sitemap.ts missing PUBLIC_LISTING_FILTER');process.exit(1)} if(!/expires_at/.test(sitemap)){console.error('sitemap.ts missing cert expiry filter (D-75 / RH5)');process.exit(1)} const robots=fs.readFileSync('app/robots.ts','utf8'); if(!/Disallow.*(draft|review)/.test(robots)){console.error('robots.ts missing Disallow draft/review');process.exit(1)} const entries=fs.readFileSync('app/(markos)/_components/approval-inbox/ecosystem-entries.tsx','utf8'); const kinds=['certification_review','listing_publish','ecosystem_install_request','payout_credits_clear','cosell_acceptance','public_directory_toggle','partner_referral_program_activate','affiliate_program_activate']; for(const k of kinds){if(!entries.includes(k)){console.error('Missing entry kind:',k);process.exit(1)}} console.log('Task 2a structure OK')"</automated>
    Plus checks:
    - `ls app/\(markos\)/ecosystem/*.tsx app/\(markos\)/ecosystem/**/*.tsx | wc -l` returns ≥9 (8 workspaces + payouts/compliance)
    - `ls app/\(public\)/marketplace/*/page.tsx app/\(public\)/developers/*/page.tsx app/\(public\)/partners/*/page.tsx | wc -l` returns 3
    - `grep "PUBLIC_LISTING_FILTER\|getPublicListings\|buildPublicListingsQuery" app/sitemap.ts` returns ≥1
    - **`grep "expires_at" app/sitemap.ts` returns ≥1 (D-75 / RH5 — sitemap honors cert expiry)**
    - `grep "Disallow.*draft\|Disallow.*review" app/robots.ts` returns ≥1
    - `grep "SoftwareApplication\|application/ld\\+json" app/\(public\)/marketplace/*/page.tsx` returns ≥1
    - `grep "cacheTag" app/\(public\)/marketplace/*/page.tsx` returns ≥1
    - **`grep "expires_at" app/\(public\)/marketplace/*/page.tsx` returns ≥1 (D-75 / RH5 — render-time TRIPLE gate)**
    - `grep -E "certification_review|listing_publish|ecosystem_install_request|payout_credits_clear|cosell_acceptance|public_directory_toggle|partner_referral_program_activate|affiliate_program_activate" app/\(markos\)/_components/approval-inbox/ecosystem-entries.tsx` returns ≥8
    - **`grep "payout_compliance\|payout-compliance\|compliance-acknowledge" app/\(markos\)/ecosystem/payouts/compliance/page.tsx` returns ≥1 (D-71 / RH4 workspace exists)**
  </verify>
  <done>
    8 operator UI workspaces (incl. payout compliance ack — D-71 / RH4) + 3 public pages with SSR + ISR + JSON-LD + sitemap + robots; Approval Inbox + Morning Brief extensions; sitemap and marketplace render-time honor TRIPLE gate (D-75 / RH5).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2b: Build 4 observability crons + 8 Playwright specs (added payout-compliance-acknowledge) + 9 Chromatic stories + 4 SEO vitest tests + llms.txt + tombstone-cascade-e2e closeout</name>
  <read_first>
    - 227-CONTEXT.md D-53..D-58 (4 observability crons + spike alerts)
    - 227-RESEARCH.md slice 227-07 spec — Playwright + Chromatic + RLS + sitemap/JSON-LD
    - 227-VALIDATION.md slice 227-07 row
    - 227-RESEARCH.md §Pitfall 9 — sitemap + robots.txt
  </read_first>
  <files>
    lib/markos/ecosystem/observability/recertification-coverage-cron.ts
    lib/markos/ecosystem/observability/payout-coverage-cron.ts
    lib/markos/ecosystem/observability/community-volume-spike-cron.ts
    lib/markos/ecosystem/observability/marketplace-view-spike-cron.ts
    public/llms.txt
    test/ecosystem/seo/sitemap.test.ts
    test/ecosystem/seo/sitemap-cert-expiry.test.ts
    test/ecosystem/seo/structured-data.test.ts
    test/ecosystem/seo/robots.test.ts
    tests/ecosystem/certification-review.spec.ts
    tests/ecosystem/listing-publish.spec.ts
    tests/ecosystem/fraud-payout.spec.ts
    tests/ecosystem/community-signal-route.spec.ts
    tests/ecosystem/cosell-acceptance.spec.ts
    tests/ecosystem/public-surface-defense.spec.ts
    tests/ecosystem/approval-gates.spec.ts
    tests/ecosystem/payout-compliance-acknowledge.spec.ts
    .storybook/stories/ecosystem/EcosystemWorkspace.stories.tsx
    .storybook/stories/ecosystem/PartnerDirectory.stories.tsx
    .storybook/stories/ecosystem/MarketplaceListings.stories.tsx
    .storybook/stories/ecosystem/CertificationReview.stories.tsx
    .storybook/stories/ecosystem/ReferralAdmin.stories.tsx
    .storybook/stories/ecosystem/AffiliateAdmin.stories.tsx
    .storybook/stories/ecosystem/CommunitySignalInbox.stories.tsx
    .storybook/stories/ecosystem/CoSellCockpit.stories.tsx
    .storybook/stories/ecosystem/PayoutComplianceAck.stories.tsx
  </files>
  <behavior>
    - **4 observability crons:** unchanged from prior plan (recertification + payout + community spike + marketplace spike).
    - **public/llms.txt:** Append Phase 227 section.
    - **4 SEO tests:**
      - `sitemap.test.ts`: only certified+active listings.
      - **`sitemap-cert-expiry.test.ts` (NEW — D-75 / RH5):** insert listing certified+active+expired-cert; sitemap.ts must NOT include it.
      - `structured-data.test.ts`: JSON-LD shape.
      - `robots.test.ts`: Disallow lines.
    - **8 Playwright specs:** 7 prior + NEW `payout-compliance-acknowledge.spec.ts` (D-71 / RH4 e2e):
      - Tenant admin opens /ecosystem/payouts/compliance.
      - Reads legal copy.
      - Clicks acknowledge button.
      - Asserts payout_compliance_acknowledged_at set.
      - Triggers /v1/ecosystem/payouts/export → succeeds (was 422 before).
    - **9 Chromatic stories:** 8 prior + NEW `PayoutComplianceAck.stories.tsx` (D-71).
  </behavior>
  <action>
    Step 1 — Create 4 observability cron handlers per D-53..D-58.
    Step 2 — Append Phase 227 section to public/llms.txt.
    Step 3 — Create 4 SEO test files (incl. NEW sitemap-cert-expiry — D-75 / RH5).
    Step 4 — Create 8 Playwright specs (incl. NEW payout-compliance-acknowledge — D-71 / RH4).
    Step 5 — Create 9 Chromatic stories (incl. NEW PayoutComplianceAck).
    Step 6 — Run all tests:
      - `npx vitest run test/ecosystem/seo/`
      - `npx playwright test tests/ecosystem/`
      - `npx chromatic --ci` (CI gate; locally `npx storybook build`)
  </action>
  <verify>
    <automated>npx vitest run test/ecosystem/seo/ && npx playwright test tests/ecosystem/ --reporter=line</automated>
    Plus checks:
    - `ls tests/ecosystem/*.spec.ts | wc -l` returns 8 (added payout-compliance-acknowledge)
    - `ls .storybook/stories/ecosystem/*.stories.tsx | wc -l` returns 9 (added PayoutComplianceAck)
    - `grep "Phase 227\|ecosystem" public/llms.txt` returns ≥1
    - `ls lib/markos/ecosystem/observability/*-cron.ts | wc -l` returns 4
    - `ls test/ecosystem/seo/sitemap-cert-expiry.test.ts` returns 1 file (D-75 / RH5)
  </verify>
  <done>
    4 observability crons; 4 SEO tests (incl. sitemap-cert-expiry — D-75 / RH5) + 8 Playwright (incl. payout-compliance-acknowledge — D-71) + 9 Chromatic stories; llms.txt Phase 227 section appended; tombstone-cascade-e2e covered by approval-gates harness.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 227 closeout — operator-verify ecosystem cockpit + public marketplace + Approval Inbox + sitemap + payout compliance flow</name>
  <what-built>
    - 8 operator workspaces under /ecosystem (cockpit + listings + partners + certifications + referrals + affiliates + community + co-sell + payouts/compliance NEW for D-71)
    - 3 public surfaces with TRIPLE-gate render (D-75 / RH5)
    - Approval Inbox 8 entry kinds; Morning Brief 5+1 sections (incl. payout compliance status)
    - sitemap.xml + robots.txt with TRIPLE-gate hardening (D-75 / RH5)
    - 3 NEW payout API endpoints (export D-71-gated; reconcile D-70-flips-paid; compliance-acknowledge D-71)
    - 4 observability crons; 8 Playwright; 9 Chromatic stories
  </what-built>
  <how-to-verify>
    1. **Operator cockpit:** Visit `/ecosystem`. Click into 8 sub-workspaces (incl. payouts/compliance — D-71).
    2. **Payout compliance flow (NEW — D-71 / RH4):**
       - Try `POST /v1/ecosystem/payouts/export` for a tenant without acknowledgement → confirm 422 with COMPLIANCE_NOT_ACKNOWLEDGED.
       - Visit `/ecosystem/payouts/compliance` as tenant admin → read legal copy → click acknowledge → confirm payout_compliance_acknowledged_at set.
       - Retry export → confirm 200 + batch created with status='exported'.
       - Run `node scripts/payout/reconcile.cjs --batch <id> --confirmed-credits <csv>` → confirm credits flip to status='paid' (D-70 / RH3).
    3. **Public marketplace SSR + SEO + TRIPLE gate (D-75 / RH5):**
       - Visit `/marketplace` in incognito. Confirm only certified+active+future-expiry listings.
       - Visit certified-listing-slug. Confirm SSR + JSON-LD.
       - Visit `/sitemap.xml`. Confirm only TRIPLE-gate listings.
       - **Manually expire a cert** (`UPDATE certification_records SET expires_at = now() - interval '1 day'`) → curl listing URL → expect 404 (render-time TRIPLE gate hides it BEFORE cron runs — RH5 proof).
       - Visit `/robots.txt`. Confirm Disallow draft/review.
    4. **Approval Inbox flow:** Submit listing for cert → certification_review entry; operator approves → cert state flips → ISR cache invalidated → public listing visible.
    5. **Defense:** 11 install-requests in 1 minute → 11th 429; honeypot → silent reject.
    6. **Cron operability:** Manually trigger recertification cron → operator tasks emitted.
    7. **Legacy regression:** `npm test` → P100-P226 baselines green.
    8. **Test suite:** `npx vitest run test/ecosystem/` + `npx playwright test tests/ecosystem/` + `npx chromatic --ci` all green.
    9. **Migration order verified:** Inspect supabase/migrations/ → 159..171 in numeric order; cross-phase ALTERs (166, 167) gated by precondition guards (D-69 / RH2).
    10. **{{MARKOS_PRICING_ENGINE_PENDING}} placeholder visible:** Visit a partner profile page where rev_share_terms_id is unbound → confirm placeholder rendered (NOT hard-coded rate).
    11. **Plugin registry adapter (D-68 / RH1):** Verify `grep -c "lookupPlugin" lib/markos/ecosystem/` returns 0 — fictional API removed; resolvePlugin used.
  </how-to-verify>
  <resume-signal>Type "approved — phase 227 ships" or describe any issues found (UI bugs, sitemap leaks, approval gate misses, payout compliance flow issues, regression breakage, placeholder violations, lookupPlugin remnants)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public visitor → /marketplace/[...slug] | Untrusted SSR; only TRIPLE-gate (status + cert_state + cert.expires_at) listings; render-time 404 (D-75 / RH5) |
| Public visitor → contact-listing + install-request | BotID + rate-limit + honeypot defense-in-depth |
| Operator → Approval Inbox decisions | Class-based approval (D-44/D-45) |
| Tenant admin → payout compliance acknowledgement | D-71 / RH4 — explicit acknowledgement required before export |
| Finance role → payout reconciliation | D-70 / RH3 — separate finance auth flips exported→paid |
| Tombstone trigger → 6 PII tables | Cascade must scrub all profile_id + email columns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-227-07-01 | Spoofing | SEO leak via uncertified OR expired marketplace listing | mitigate | sitemap.test.ts + sitemap-cert-expiry.test.ts assert TRIPLE-gate (D-75 / RH5); robots.test.ts disallow draft/review; structured-data.test.ts |
| T-227-07-02 | DoS | Public install-request flood | mitigate | BotID + rate-limit 10/IP/min + honeypot |
| T-227-07-03 | Information Disclosure | Cross-tenant API leak | mitigate | cross-tenant-denial.test.ts covers every /v1/ecosystem/* route; RLS suite covers 17 tables |
| T-227-07-04 | Information Disclosure | Tombstone leak | mitigate | tombstone-cascade.test.ts asserts NULL across 6 PII tables |
| T-227-07-05 | Tampering | Bypassed approval gates | mitigate | approval-gates.spec.ts proves listing_publish + cert + install_request + payout_clear + cosell all gate via Approval Inbox |
| T-227-07-06 | Tampering | Migration order error breaking dependent slice | mitigate | Migration headers document Wave + Depends; cross-phase ALTERs (166, 167) HARD-FAIL via precondition guards (D-69 / RH2) |
| T-227-07-07 | Information Disclosure | Pricing copy leak (raw rate without P205) | mitigate | UI components render PRICING_PENDING_PLACEHOLDER when resolveCommissionRate returns placeholder |
| T-227-07-08 | Repudiation | API audit gap | mitigate | Every write API route invokes markos_audit_log via service modules |
| T-227-07-09 | Elevation | Tenant exports payouts without compliance acknowledgement | mitigate | D-71 / RH4 — /v1/ecosystem/payouts/export 422 if not acknowledged; payout-export-compliance-gate.test.ts proves block |
| T-227-07-10 | Tampering | Payout marked 'paid' without finance reconciliation | mitigate | D-70 / RH3 — only /v1/ecosystem/payouts/reconcile flips to 'paid'; finance-only auth |
| T-227-07-11 | Information Disclosure | RLS hardening race (Plan 01 to Plan 07 gap) | mitigate | RL1 / D-73 — Plan 01 establishes initial CORRECT policies (independently tested); Plan 07 migration 170 ADDS defense-in-depth |
</threat_model>

<verification>
- F-198 contract registered.
- Migrations 169-171 — honeypot/BotID + RLS hardening (RL1 / D-73 — defense-in-depth; not initial correctness which is Plan 01) + OpenAPI regen.
- 15 /v1/ecosystem/* routes (incl. 3 NEW payout: export D-71-gated; reconcile D-70 flips paid; compliance-acknowledge D-71) + 2 public endpoints.
- 8 MCP tools.
- 8 operator UI pages (incl. payouts/compliance — D-71) + 3 public pages with TRIPLE gate (D-75 / RH5).
- sitemap.xml + robots.txt + JSON-LD structured data (TRIPLE gate honored — RH5).
- Approval Inbox 8 entry kinds + Morning Brief 5+1 surfaces (added payout_compliance_status — D-71).
- 4 observability crons.
- llms.txt Phase 227 section.
- 14 vitest test files + 8 Playwright specs (added payout-compliance-acknowledge) + 9 Chromatic stories (added PayoutComplianceAck).
- Cross-tenant denial across all routes; payout-export-compliance-gate.test.ts proves RH4.
- RLS suite 17 tables; rls-hardening-stricter.test.ts verifies migration 170 is defense-in-depth not stub policy fix (RL1).
- Tombstone cascade end-to-end.
- Marketplace BotID + rate-limit + honeypot proven.
- Legacy regression `npm test` green.
- Migration order documented + HARD-FAIL precondition guards on cross-phase ALTERs (D-69 / RH2).
- Human-verify checkpoint completed.
</verification>

<success_criteria>
- F-198 contract + 3 closeout migrations.
- Full /v1/ecosystem/* API + 2 public endpoints + 8 MCP tools (incl. 3 NEW payout endpoints — D-70/D-71).
- 8 operator workspaces + 3 public pages.
- Approval Inbox 8 entries + Morning Brief 5+1 sections.
- sitemap.xml + robots.txt TRIPLE gate (D-75 / RH5) + JSON-LD SoftwareApplication.
- 4 observability crons.
- llms.txt updated.
- 14 vitest + 8 Playwright + 9 Chromatic = full test suite green.
- Cross-tenant denial + RLS suite (17 tables) + RLS hardening test (RL1) + tombstone cascade green.
- Migration order honored + HARD-FAIL precondition guards (D-69 / RH2).
- 227-VALIDATION.md per-task map updated; nyquist_compliant=true after all 7 plans complete.
- Legacy regression green.
- Human-verify checkpoint approved.
</success_criteria>

<output>
After completion, create `.planning/phases/227-ecosystem-partner-community-developer-growth/227-07-SUMMARY.md` with:
- F-198 + migrations 169-171 allocation
- Full /v1/ecosystem/* route inventory (incl. 3 NEW payout routes — D-70/D-71)
- 8 MCP tool registry
- 8 UI workspaces + 3 public surfaces (incl. payouts/compliance — D-71)
- Sitemap + robots TRIPLE gate evidence (D-75 / RH5)
- Approval Inbox + Morning Brief integration
- 4 cron handlers
- Test results (Vitest + Playwright + Chromatic)
- Tombstone cascade trace (P221 → P227)
- Legacy regression result
- Migration order audit + HARD-FAIL precondition guard outcomes (D-69 / RH2)
- RL1 / D-73 — RLS hardening clarified (Plan 01 = initial correctness; Plan 07 migration 170 = defense-in-depth)
- Human-verify checkpoint outcome
- Phase 227 closeout: ECO-01..05 + SG-04/06/09..12 + QA-01..15 traceability matrix
- D-68..D-79 evidence references (RH1..RH5 + RM1..RM6 + RL1..RL2 resolutions)
</output>
</content>
</invoke>