---
phase: 227-ecosystem-partner-community-developer-growth
plan: 03
type: execute
wave: 2
depends_on: ["227-01"]
files_modified:
  - contracts/F-191-ecosystem-certification-transition-v1.json
  - supabase/migrations/168_ecosystem_hot_path_indexes.sql
  - lib/markos/ecosystem/partners/partner-service.ts
  - lib/markos/ecosystem/partners/directory.ts
  - lib/markos/ecosystem/certifications/certification-service.ts
  - lib/markos/ecosystem/certifications/criteria-templates.ts
  - lib/markos/ecosystem/certifications/recertification-cron.ts
  - test/ecosystem/partners/partner-service.test.ts
  - test/ecosystem/partners/public-directory-toggle.test.ts
  - test/ecosystem/certifications/state-machine.test.ts
  - test/ecosystem/certifications/criteria-checks.test.ts
  - test/ecosystem/certifications/recert-cron.test.ts
  - test/ecosystem/certifications/listing-hide-on-expire.test.ts
autonomous: true
requirements: [ECO-01, ECO-04, SG-12, QA-01, QA-02, QA-03, QA-04, QA-05, QA-06, QA-07, QA-08, QA-09, QA-10, QA-11, QA-12, QA-13, QA-14, QA-15]
must_haves:
  truths:
    - "PartnerProfile P220 extension supports business_mode + ecosystem fields without breaking SaaS-mode rows"
    - "CertificationRecord state machine enforces draft→review→certified→suspended/expired/revoked transitions"
    - "Recertification cron fires at 11 months (operator task) and 12 months (state=expired + listing hidden)"
    - "criteria_checks JSONB validated against versioned templates (no schema drift)"
    - "Stale certification cannot be added to deal_room_artifacts or any partner-facing materials"
    - "public_directory_visible toggle requires P208 Approval Inbox approval"
  artifacts:
    - path: "lib/markos/ecosystem/partners/partner-service.ts"
      provides: "PartnerProfile extension CRUD + program toggles + co_sell/referral/affiliate gating"
      exports: ["createPartner", "updatePartner", "togglePublicDirectoryVisible", "enableCoSell", "enableReferral", "enableAffiliate", "getPartnerById"]
    - path: "lib/markos/ecosystem/partners/directory.ts"
      provides: "public_directory_visible filter + region/specialization filters"
      exports: ["getPublicDirectory", "PUBLIC_PARTNER_FILTER"]
    - path: "lib/markos/ecosystem/certifications/certification-service.ts"
      provides: "Certification state machine + transition writes + ISR cache hooks"
      exports: ["assertTransitionAllowed", "transitionCertification", "createCertificationDraft", "ALLOWED_TRANSITIONS", "computeListingCertificationState"]
    - path: "lib/markos/ecosystem/certifications/criteria-templates.ts"
      provides: "Versioned criteria templates per category (Pitfall 12 mitigation)"
      exports: ["CRITERIA_TEMPLATES", "validateCriteriaChecks", "CriteriaTemplateVersion"]
    - path: "lib/markos/ecosystem/certifications/recertification-cron.ts"
      provides: "Daily 03:00 UTC cron handler for 11-month warning + 12-month expiry"
      exports: ["recertificationCron", "warnUpcomingExpiry", "expireCertifications"]
  key_links:
    - from: "lib/markos/ecosystem/certifications/certification-service.ts::transitionCertification"
      to: "lib/markos/ecosystem/listings/isr-cache.ts::invalidateListingCache"
      via: "ISR cache invalidate on every cert state change (D-40)"
      pattern: "invalidateListingCache"
    - from: "lib/markos/ecosystem/certifications/recertification-cron.ts::expireCertifications"
      to: "integration_listings.certification_state"
      via: "12-month expiry → flip listing.certification_state='expired' + hide from public"
      pattern: "certification_state\\s*=\\s*'expired'"
    - from: "lib/markos/ecosystem/partners/partner-service.ts::togglePublicDirectoryVisible"
      to: "lib/markos/crm/copilot.ts::createApprovalPackage"
      via: "Approval Inbox entry public_directory_toggle (D-45/D-51)"
      pattern: "createApprovalPackage.*public_directory_toggle"
    - from: "lib/markos/ecosystem/certifications/certification-service.ts::transitionCertification"
      to: "createApprovalPackage"
      via: "certification_review entry kind for → certified | suspended | revoked"
      pattern: "createApprovalPackage.*certification_review"
---

<objective>
Build PartnerProfile P220 extension service + CertificationRecord state machine + recertification cron + criteria_templates module (227-RESEARCH.md slice 227-03). Wave 2 — parallel with 227-02 + 227-05.

Purpose: This slice owns partner extension CRUD (D-02 columns + program toggles), certification state machine (D-15..D-19), criteria template validation (Pitfall 12), and the daily recertification cron that flips expired listings to hidden (D-17 + Pitfall 2). Provides primitives consumed by 227-04 (referral/affiliate program activation) and 227-06 (CoSell partner gating).

Output: 1 contract (F-191), 1 migration (168 hot-path indexes), 5 service modules, 6 vitest test files.
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-CONTEXT.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-RESEARCH.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-VALIDATION.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-01-PLAN.md
@.planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-CONTEXT.md
@.planning/phases/208-human-operating-interface/208-CONTEXT.md
@.planning/phases/209-evidence-research-and-claim-safety/209-CONTEXT.md
@CLAUDE.md

# Incoming docs (CLAUDE.md precedence rule 5): obsidian/work/incoming/{18,22,25}-*.md INFORMATIONAL ONLY.

<interfaces>
From 227-01 (lib/markos/ecosystem/contracts.ts):
```typescript
export const CERTIFICATION_STATE = ['draft','review','certified','suspended','expired','revoked'] as const;
export const PARTNER_TYPE = ['agency','technology','consulting','reseller','affiliate','integrator','devshop','association'] as const;
export const CERTIFICATION_LEVEL = ['registered','certified','elite'] as const;
export const ACTIVE_STATUS = ['active','watch','paused','terminated'] as const;
export const BUSINESS_MODE = ['saas','commerce','ecosystem','all'] as const;
```

From 227-02 (lib/markos/ecosystem/listings/isr-cache.ts):
```typescript
export function invalidateListingCache(listing_id: string): void;
```

From P105 (lib/markos/crm/copilot.ts):
```typescript
export function createApprovalPackage(packageKind: string, subjectId: string, evidenceRefs: string[], pricingContextId?: string): Promise<ApprovalPackage>;
// New entry kinds: certification_review, public_directory_toggle (D-51)
```

From P209 EvidenceMap (A21 — soft ref; if P209 not yet shipped, evidence_refs stays uuid[] without FK):
```typescript
export interface EvidenceClaim { claim_id: string; freshness_status: 'fresh'|'stale'|'expired'; }
```

From migration 164 (227-01) certification_records columns (cert_id, subject_type, subject_id, state, criteria_checks, certified_at, expires_at, recertification_due_at).
From migration 160 (227-01) partner_profiles extension columns.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build certification-service.ts state machine + criteria-templates.ts + F-191 contract + migration 168 hot-path indexes</name>
  <read_first>
    - 227-CONTEXT.md D-15..D-19 (certification state machine + criteria_checks + recertification + revoked terminal + operator review queue)
    - 227-CONTEXT.md D-44..D-46 (class-based approval — certification → certified/suspended/revoked require approval)
    - 227-RESEARCH.md §Pattern 2 Certification State Machine (lines 357-378) — `assertTransitionAllowed` reference impl
    - 227-RESEARCH.md §Pitfall 12 (lines 977-981) — criteria_checks JSONB schema drift; versioned templates required
    - 227-RESEARCH.md migration 168 spec (line 196) — hot-path composite indexes for certification + listing inbox + cosell
    - 227-VALIDATION.md slice 227-03 row + Coverage Targets "Certification state machine 100% — 7 states × 12 transitions × criteria_checks"
    - migration 164 + integration_listings.certification_state column
  </read_first>
  <files>
    contracts/F-191-ecosystem-certification-transition-v1.json
    supabase/migrations/168_ecosystem_hot_path_indexes.sql
    lib/markos/ecosystem/certifications/certification-service.ts
    lib/markos/ecosystem/certifications/criteria-templates.ts
    test/ecosystem/certifications/state-machine.test.ts
    test/ecosystem/certifications/criteria-checks.test.ts
  </files>
  <behavior>
    - **F-191 certification-transition contract:** Schema for state transition request/response: `{ cert_id, from_state, to_state, criteria_checks_patch?, reviewer_user_id, evidence_refs[], reason? }`. type='write/MCP', actor='Operator'.
    - **Migration 168 (hot-path indexes per RESEARCH.md line 196):** 3 composite indexes — (a) `idx_listings_inbox` on integration_listings(tenant_id, status, certification_state) for public marketplace filter; (b) `idx_community_signals_inbox_open` on community_signals(tenant_id, urgency, routed_action_kind) WHERE routed_action_kind IS NULL (used in 227-05 + 227-07 inbox); (c) `idx_cosell_partner_status` on co_sell_opportunities(tenant_id, status, partner_id) for partner cockpit. All indexes use `IF NOT EXISTS`.
    - **certification-service.ts:**
      - `ALLOWED_TRANSITIONS` const Record per RESEARCH.md §Pattern 2:
        - draft → ['review']
        - review → ['certified', 'draft']
        - certified → ['suspended', 'expired']
        - suspended → ['certified']
        - expired → ['review']
        - revoked: [] // terminal
      - `assertTransitionAllowed(from, to)`: throws `INVALID_CERT_TRANSITION` if not allowed; allows `* → revoked` always (per Pattern 2 comment — terminal action).
      - `createCertificationDraft({tenant_id, subject_type, subject_id, criteria_checks, reviewer_user_id?})`: validates criteria_checks via `validateCriteriaChecks` (criteria-templates.ts); inserts row with state='draft'.
      - `transitionCertification(cert_id, to_state, opts)`: reads current row; calls `assertTransitionAllowed(current.state, to_state)`; for transitions to certified/suspended/revoked → calls `createApprovalPackage('certification_review', cert_id, evidence_refs, ...)` (D-45) and only completes the transition AFTER approval (Approval Inbox writes back via 227-07 API; service-layer marks pending_approval); for transitions to draft → review (resubmit) → no approval gate. On state→'certified': sets certified_at=now(); expires_at=now()+12mo; recertification_due_at=now()+11mo. On state→'expired': cron only (see Task 2). Updates integration_listings.certification_state when subject_type='listing' (or partner_profiles.certification_level when subject_type='partner'). Calls `invalidateListingCache(subject_id)` (D-40 ISR).
      - `computeListingCertificationState(listing_id)`: derives current cert state from latest cert row WHERE state='certified' AND expires_at > now(); returns 'expired' if cert past TTL; returns 'draft' if no cert exists.
    - **criteria-templates.ts:**
      - `CRITERIA_TEMPLATES` exports versioned templates per category. Pattern: `CRITERIA_TEMPLATES.connector_v1 = { security_audit: { required: true }, docs_complete: { required: true }, support_sla: { required: true }, sandbox_test: { required: true }, oauth_scopes_minimal: { required: false } }`. Templates per category: connector, plugin, agent, template, workflow, integration (listing categories) + partner_v1 (partner template). Use Zod schema for `criteria_checks` shape: `Record<string, { result: 'pass'|'fail'|'pending', evidence_ref?: string, notes?: string }>`.
      - `validateCriteriaChecks(category_or_template_version, criteria_checks)`: validates that (a) all required criteria are present; (b) every key matches a template criterion; (c) values are valid `result` enums. Returns `{ valid: bool, missing: string[], unknown: string[] }`.
      - **Pitfall 12 mitigation:** template version frozen per cert_id at draft creation; cert row stores `criteria_template_version` — recertification creates new cert_id with current template version (audit-preserved).
  </behavior>
  <action>
    Step 1 — Create `contracts/F-191-ecosystem-certification-transition-v1.json` with JSON Schema covering transition request, response, and CERTIFICATION_STATE enum from CONTEXT.md D-07.
    Step 2 — Create `supabase/migrations/168_ecosystem_hot_path_indexes.sql` with 3 `CREATE INDEX IF NOT EXISTS` statements per RESEARCH.md line 196. Add header comment to the migration file: `-- Depends on: migration 163 (community_signals table — implicit via wave dep on 227-01)`.
    Step 3 — Create `lib/markos/ecosystem/certifications/criteria-templates.ts`:
      - 7 template versions exported (6 listing categories + partner)
      - `validateCriteriaChecks(template_key, criteria_checks)` returns validation result
      - Templates frozen via `as const` to prevent runtime mutation
      - Each criterion: `{ required: bool, description: string, evidence_required: bool }` shape
    Step 4 — Create `lib/markos/ecosystem/certifications/certification-service.ts`:
      - Verbatim ALLOWED_TRANSITIONS per RESEARCH.md §Pattern 2
      - `assertTransitionAllowed(from, to)` exported throwing `INVALID_CERT_TRANSITION:${from}→${to}`
      - `transitionCertification` wraps state change in Supabase transaction; reads current state; assertTransition; for approval-required transitions emits createApprovalPackage('certification_review',...) and writes pending_approval status flag to a side column or audit-only path; for other transitions writes immediately. Calls `invalidateListingCache(subject_id)` after every state change when subject_type='listing'.
      - `computeListingCertificationState(listing_id)` SQL: `SELECT state, expires_at FROM certification_records WHERE subject_type='listing' AND subject_id=$1 ORDER BY certified_at DESC NULLS LAST LIMIT 1` then app-layer expiry check.
    Step 5 — Create `test/ecosystem/certifications/state-machine.test.ts`:
      - Test happy path: draft → review → certified (with approval mock returning approved) → suspended → certified (reinstate) → revoked (terminal).
      - Test blocked transitions: certified → draft (rejected); revoked → anything (rejected); draft → certified (skip review — rejected).
      - Test * → revoked allowed from every state.
      - Test approval gate: transition to certified WITHOUT approval mock → state stays draft/review with `pending_approval` flag; with approval → state flips.
      - Test ISR invalidation: `transitionCertification` for subject_type='listing' calls `invalidateListingCache` (mock + assert).
    Step 6 — Create `test/ecosystem/certifications/criteria-checks.test.ts`:
      - validateCriteriaChecks against template_key='connector_v1' with all required criteria present + valid result → { valid: true, missing: [], unknown: [] }.
      - With one required criterion missing → { valid: false, missing: ['security_audit'], unknown: [] }.
      - With unknown criterion key (drift attempt: `securityAudit` instead of `security_audit`) → { valid: false, unknown: ['securityAudit'] } (Pitfall 12 mitigation).
      - With invalid result value (e.g., 'success' instead of 'pass') → throws zod validation error.
    Step 7 — Run vitest.
  </action>
  <verify>
    <automated>npx vitest run test/ecosystem/certifications/state-machine.test.ts test/ecosystem/certifications/criteria-checks.test.ts</automated>
    Plus checks:
    - `grep "ALLOWED_TRANSITIONS" lib/markos/ecosystem/certifications/certification-service.ts` returns ≥2 matches
    - `grep -E "draft.*\\['review'\\]" lib/markos/ecosystem/certifications/certification-service.ts` returns ≥1 (state machine literal)
    - `grep "INVALID_CERT_TRANSITION" lib/markos/ecosystem/certifications/certification-service.ts` returns ≥1
    - `grep "createApprovalPackage.*certification_review" lib/markos/ecosystem/certifications/certification-service.ts` returns ≥1
    - `grep "invalidateListingCache" lib/markos/ecosystem/certifications/certification-service.ts` returns ≥1
    - `grep -c "CRITERIA_TEMPLATES\." lib/markos/ecosystem/certifications/criteria-templates.ts` returns ≥7 (6 listing categories + partner)
    - `grep -c "CREATE INDEX IF NOT EXISTS" supabase/migrations/168_ecosystem_hot_path_indexes.sql` returns 3
  </verify>
  <done>
    F-191 contract registered; migration 168 with 3 hot-path indexes; certification state machine 12 transitions tested; revoked terminal; criteria_checks template validation rejects drifted keys; ISR invalidation fires on listing cert state change.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build partner-service.ts + directory.ts + recertification-cron.ts + 4 test files</name>
  <read_first>
    - 227-CONTEXT.md D-02 (PartnerProfile extension columns), D-17 (recertification cron daily 03:00 UTC), D-44/D-45 (approval gates including public_directory_toggle + partner_referral_program_activate + affiliate_program_activate)
    - 227-RESEARCH.md §Pitfall 2 (lines 917-921) — certification expiry race; idempotent cron; render-time fail-closed
    - 227-RESEARCH.md slice 227-03 spec (lines 832-840) — public_directory_visible toggle approval, recert cron 11mo+12mo
    - 227-VALIDATION.md slice 227-03 row + Manual-Only Verifications "Certification operator review flow"
  </read_first>
  <files>
    lib/markos/ecosystem/partners/partner-service.ts
    lib/markos/ecosystem/partners/directory.ts
    lib/markos/ecosystem/certifications/recertification-cron.ts
    test/ecosystem/partners/partner-service.test.ts
    test/ecosystem/partners/public-directory-toggle.test.ts
    test/ecosystem/certifications/recert-cron.test.ts
    test/ecosystem/certifications/listing-hide-on-expire.test.ts
  </files>
  <behavior>
    - **partner-service.ts:**
      - `createPartner({tenant_id, partner_type, region, specialization_tags, certification_level, ...})`: validates per partner_profiles extended schema; inserts with business_mode='ecosystem' (D-13 — explicit P227 ecosystem rows); active_status='active'; co_sell_enabled=false; referral_enabled=false; affiliate_enabled=false; public_directory_visible=false (all toggles default OFF — explicit operator action required).
      - `updatePartner(partner_id, patch)`: rejects updates to immutable cols (partner_id, tenant_id, created_at).
      - `togglePublicDirectoryVisible(partner_id, target_value)`: per D-45 → calls `createApprovalPackage('public_directory_toggle', partner_id, evidence_refs, ...)`; only flips public_directory_visible AFTER approval (227-07 API consumes Approval Inbox decision).
      - `enableCoSell(partner_id)`: validates partner has at least one cert in state='certified' (or certification_level >= 'certified'); flips co_sell_enabled=true; emits Approval Inbox entry `partner_referral_program_activate` only when also enabling referral; immediate flip otherwise.
      - `enableReferral(partner_id)`: D-45 always approval-required; emits `partner_referral_program_activate`.
      - `enableAffiliate(partner_id)`: D-45 always approval-required; emits `affiliate_program_activate`.
      - `getPartnerById(partner_id)`: tenant-scoped fetch.
    - **directory.ts:**
      - `PUBLIC_PARTNER_FILTER` const: `{ business_mode: ['ecosystem','all'], active_status: 'active', public_directory_visible: true }`. Note `business_mode IN (...)` — D-13 single SOR.
      - `getPublicDirectory({region?, specialization_tags?, certification_level?})`: returns partners matching PUBLIC_PARTNER_FILTER + optional facets. Used by 227-07 `/partners/[...slug]` SSR.
    - **recertification-cron.ts:**
      - `recertificationCron()`: cron handler entry point. Calls `warnUpcomingExpiry()` then `expireCertifications()`. Designed for Vercel Cron daily 03:00 UTC (A5 — AgentRun bridge stub for now).
      - `warnUpcomingExpiry()`: SQL `SELECT cert_id, subject_type, subject_id FROM certification_records WHERE state='certified' AND recertification_due_at <= now() AND expires_at > now() AND recertification_warned_at IS NULL`; emits operator task `certification_due_in_30_days` per cert; marks recertification_warned_at=now() (idempotent — column from migration 164).
      - `expireCertifications()`: SQL `UPDATE certification_records SET state='expired' WHERE state='certified' AND expires_at <= now()`. For each newly-expired cert WHERE subject_type='listing': `UPDATE integration_listings SET certification_state='expired' WHERE listing_id=$subject_id`. Then call `invalidateListingCache(listing_id)` per affected listing. **Pitfall 2 mitigation:** the cert update and listing update are TWO SEPARATE Supabase RPC calls (not one transaction), so each is individually retryable on cron failure. Render-time belt-and-suspenders: `getPublicListings` (227-02) ALSO checks `certification_records.expires_at > now()` via JOIN — but that hardening lives in 227-07.
  </behavior>
  <action>
    Step 1 — Create `lib/markos/ecosystem/partners/partner-service.ts`:
      - All 6 toggle/CRUD functions per behavior
      - `togglePublicDirectoryVisible` writes `pending_approval` audit + emits createApprovalPackage('public_directory_toggle', ...). Final flip happens via 227-07 API after approval.
      - `enableReferral` + `enableAffiliate` similar approval gating.
    Step 2 — Create `lib/markos/ecosystem/partners/directory.ts`:
      - PUBLIC_PARTNER_FILTER const
      - `getPublicDirectory(filters)` Supabase query with `business_mode in ('ecosystem','all')` AND `active_status='active'` AND `public_directory_visible=true`.
    Step 3 — Create `lib/markos/ecosystem/certifications/recertification-cron.ts`:
      - `recertificationCron()` entry point exporting two functions
      - `warnUpcomingExpiry`: idempotent via `recertification_warned_at` column (added by 227-01 migration 164 per Warning 6 fix) — query `WHERE state='certified' AND recertification_due_at <= now() AND expires_at > now() AND recertification_warned_at IS NULL`; mark `recertification_warned_at = now()` after emitting operator task.
      - `expireCertifications`: 2-step SQL — flip cert state, then flip integration_listings.certification_state. Each step idempotent and retryable.
    Step 4 — Create `test/ecosystem/partners/partner-service.test.ts`:
      - createPartner with required fields → business_mode='ecosystem', toggles all false.
      - updatePartner rejects mutating partner_id/tenant_id/created_at.
      - enableCoSell rejects partner without certified cert.
      - enableReferral always emits createApprovalPackage('partner_referral_program_activate', ...) (mock + assert).
    Step 5 — Create `test/ecosystem/partners/public-directory-toggle.test.ts`:
      - togglePublicDirectoryVisible → mock createApprovalPackage assert called with 'public_directory_toggle' entry kind.
      - Without approval, partner.public_directory_visible stays false; getPublicDirectory excludes partner.
      - With approval (mock approved), partner.public_directory_visible=true; getPublicDirectory includes partner.
    Step 6 — Create `test/ecosystem/certifications/recert-cron.test.ts`:
      - Insert cert with certified_at=now()-330days, expires_at=now()+30days (within 30-day warning window).
      - Call `warnUpcomingExpiry()`; assert operator task emitted (mock task system); assert warned_at marked.
      - Re-run; assert no double-warn (idempotent).
      - Insert cert with expires_at=now()-1day; call `expireCertifications()`; assert state='expired' + integration_listings.certification_state='expired'.
      - Pitfall 2: simulate failure in step 2 (listing update); assert step 1 (cert state) rolled forward; re-run cron; assert step 2 retries successfully.
    Step 7 — Create `test/ecosystem/certifications/listing-hide-on-expire.test.ts`:
      - Setup: listing with status='active' AND certification_state='certified'; cert row with expires_at=now()-1day.
      - Run `expireCertifications()`.
      - Assert listing.certification_state='expired'.
      - Assert `getPublicListings()` (from 227-02) does NOT return this listing (double-gate filter rejects expired).
      - Assert `invalidateListingCache(listing_id)` called.
    Step 8 — Run vitest.

    **{{MARKOS_PRICING_ENGINE_PENDING}} placeholder rule (D-46):** No payout/commission copy in this slice. partner_profiles.commission_share_default IS a numeric field, NOT a copy field; if any partner-facing public directory page renders commission rates (227-07), the placeholder rule will apply there.
  </action>
  <verify>
    <automated>npx vitest run test/ecosystem/partners/ test/ecosystem/certifications/recert-cron.test.ts test/ecosystem/certifications/listing-hide-on-expire.test.ts</automated>
    Plus checks:
    - `grep "createApprovalPackage.*public_directory_toggle" lib/markos/ecosystem/partners/partner-service.ts` returns ≥1
    - `grep "createApprovalPackage.*partner_referral_program_activate\|affiliate_program_activate" lib/markos/ecosystem/partners/partner-service.ts` returns ≥2
    - `grep "PUBLIC_PARTNER_FILTER" lib/markos/ecosystem/partners/directory.ts` returns ≥2
    - `grep -E "business_mode\s*[Ii][Nn]\s*\\(\\s*\\['ecosystem'|business_mode IN \\('ecosystem'" lib/markos/ecosystem/partners/directory.ts` returns ≥1
    - `grep "expireCertifications\|warnUpcomingExpiry" lib/markos/ecosystem/certifications/recertification-cron.ts` returns ≥4
    - `grep "invalidateListingCache" lib/markos/ecosystem/certifications/recertification-cron.ts` returns ≥1
    - `grep -E "certification_state\s*=\s*'expired'" lib/markos/ecosystem/certifications/recertification-cron.ts` returns ≥1
  </verify>
  <done>
    partner-service.ts CRUD + 6 toggles; public_directory_toggle approval gated; getPublicDirectory uses single SOR `business_mode IN ('ecosystem','all')`; recertificationCron 11mo warning + 12mo expiry; listing hide on cert expiry verified; idempotent cron + retry-safe split; ISR invalidation on every cert state change.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator review queue → certification | Operator decision flips trust state; class-based approval enforced |
| recertification cron → public marketplace | Expired cert must hide listing (Pitfall 2 race) |
| criteria_checks JSONB → cert decision | Schema drift risks unreliable cert audit (Pitfall 12) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-227-03-01 | Elevation | Direct certification state mutation bypasses approval | mitigate | `transitionCertification` for → certified/suspended/revoked emits createApprovalPackage; final flip only after approval consumed via 227-07 API; tested in state-machine.test.ts |
| T-227-03-02 | Tampering | Cron failure leaves expired listing visible | mitigate | Pitfall 2 — split cert update + listing update; each retryable; render-time double-gate (227-02 PUBLIC_LISTING_FILTER); D-58 monthly coverage cron |
| T-227-03-03 | Tampering | criteria_checks JSONB drift (key naming) | mitigate | criteria-templates.ts versioned templates; validateCriteriaChecks rejects unknown keys; tested in criteria-checks.test.ts |
| T-227-03-04 | Tampering | Revoked cert reactivation | mitigate | ALLOWED_TRANSITIONS.revoked = []; assertTransitionAllowed rejects all transitions from revoked; new cert = new cert_id (audit-preserved) |
| T-227-03-05 | Spoofing | Partner enables co-sell without certification | mitigate | enableCoSell validates partner has cert in state='certified' OR certification_level >= 'certified'; rejects otherwise |
| T-227-03-06 | Information Disclosure | public_directory_visible toggle leaks partner without consent | mitigate | createApprovalPackage('public_directory_toggle') gates the flip; partner-service test asserts approval required |
</threat_model>

<verification>
- All 12 transitions of certification state machine tested (happy + blocked).
- criteria_checks template drift attempt rejected (Pitfall 12).
- Recert cron 11-month warning emits operator task; 12-month expiry flips state + listing.certification_state + invalidates ISR.
- Pitfall 2 mitigation: cert update + listing update are separate retryable steps.
- Listing hide on cert expiry: `getPublicListings()` from 227-02 returns no expired listings.
- Partner public_directory_visible toggle requires Approval Inbox approval.
- enableCoSell + enableReferral + enableAffiliate emit correct Approval Inbox entry kinds.
- migration 168 has 3 hot-path composite indexes (idx_listings_inbox + idx_community_signals_inbox_open + idx_cosell_partner_status).
- Legacy regression `npm test` green.
</verification>

<success_criteria>
- F-191 contract registered.
- Migration 168 with 3 indexes committed.
- 5 service modules under `lib/markos/ecosystem/{partners,certifications}/`.
- 6 vitest test files green: `npx vitest run test/ecosystem/partners/ test/ecosystem/certifications/`.
- Cert state machine: 12 transitions covered + revoked terminal verified.
- Criteria templates: 7 versions exported (6 listing categories + partner).
- Recert cron tested for 11mo + 12mo windows + idempotency + retry-safe split.
- Listing hide-on-expire: getPublicListings() honors cert expiry.
- Approval gates: public_directory_toggle + certification_review + partner_referral_program_activate + affiliate_program_activate.
- 227-VALIDATION.md per-task map updated for slice 227-03.
- Legacy regression green.
</success_criteria>

<output>
After completion, create `.planning/phases/227-ecosystem-partner-community-developer-growth/227-03-SUMMARY.md` with:
- F-191 + migration 168 allocation
- Cert state machine transition map verified
- Criteria template versions
- Recert cron timing + retry-safe split
- Listing hide-on-expire evidence
- Partner public_directory_toggle approval flow
- Test results (6 files green)
</output>
