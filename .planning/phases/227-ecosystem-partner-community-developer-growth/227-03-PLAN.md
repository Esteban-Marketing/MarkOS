---
phase: 227-ecosystem-partner-community-developer-growth
plan: 03
type: execute
wave: 2
depends_on: ["227-01"]
files_modified:
  - contracts/F-191-ecosystem-certification-transition-v1.json
  - supabase/migrations/168_ecosystem_hot_path_indexes.sql
  - lib/markos/ecosystem/partners/partner-service.ts
  - lib/markos/ecosystem/partners/directory.ts
  - lib/markos/ecosystem/certifications/certification-service.ts
  - lib/markos/ecosystem/certifications/criteria-templates.ts
  - lib/markos/ecosystem/certifications/recertification-cron.ts
  - test/ecosystem/partners/partner-service.test.ts
  - test/ecosystem/partners/public-directory-toggle.test.ts
  - test/ecosystem/certifications/state-machine.test.ts
  - test/ecosystem/certifications/criteria-checks.test.ts
  - test/ecosystem/certifications/recert-cron.test.ts
  - test/ecosystem/certifications/listing-hide-on-expire.test.ts
  - test/ecosystem/certifications/directory-cert-expiry-filter.test.ts
autonomous: true
requirements: [ECO-01, ECO-04, SG-12, QA-01, QA-02, QA-03, QA-04, QA-05, QA-06, QA-07, QA-08, QA-09, QA-10, QA-11, QA-12, QA-13, QA-14, QA-15]
must_haves:
  truths:
    - "PartnerProfile P220 extension supports business_mode + ecosystem fields without breaking SaaS-mode rows"
    - "CertificationRecord state machine enforces draft→review→certified→suspended/expired/revoked transitions"
    - "Recertification cron fires at 11 months (operator task) and 12 months (state=expired + listing hidden)"
    - "criteria_checks JSONB validated against versioned templates (no schema drift)"
    - "Directory + dev-portal queries also enforce cert expiry at query time (D-75 / RH5 belt-and-suspenders) — cron is NOT the only trust barrier"
    - "Stale certification cannot be added to deal_room_artifacts or any partner-facing materials"
    - "public_directory_visible toggle requires P208 Approval Inbox approval"
  artifacts:
    - path: "lib/markos/ecosystem/partners/partner-service.ts"
      provides: "PartnerProfile extension CRUD + program toggles + co_sell/referral/affiliate gating"
      exports: ["createPartner", "updatePartner", "togglePublicDirectoryVisible", "enableCoSell", "enableReferral", "enableAffiliate", "getPartnerById"]
    - path: "lib/markos/ecosystem/partners/directory.ts"
      provides: "public_directory_visible filter + region/specialization filters + cert expiry JOIN (D-75)"
      exports: ["getPublicDirectory", "PUBLIC_PARTNER_FILTER", "buildPublicDirectoryQuery"]
    - path: "lib/markos/ecosystem/certifications/certification-service.ts"
      provides: "Certification state machine + transition writes + ISR cache hooks"
      exports: ["assertTransitionAllowed", "transitionCertification", "createCertificationDraft", "ALLOWED_TRANSITIONS", "computeListingCertificationState"]
    - path: "lib/markos/ecosystem/certifications/criteria-templates.ts"
      provides: "Versioned criteria templates per category (Pitfall 12 mitigation)"
      exports: ["CRITERIA_TEMPLATES", "validateCriteriaChecks", "CriteriaTemplateVersion"]
    - path: "lib/markos/ecosystem/certifications/recertification-cron.ts"
      provides: "Daily 03:00 UTC cron handler for 11-month warning + 12-month expiry (state cleanup, NOT sole trust barrier — RH5)"
      exports: ["recertificationCron", "warnUpcomingExpiry", "expireCertifications"]
  key_links:
    - from: "lib/markos/ecosystem/certifications/certification-service.ts::transitionCertification"
      to: "lib/markos/ecosystem/listings/isr-cache.ts::invalidateListingCache"
      via: "ISR cache invalidate on every cert state change (D-40)"
      pattern: "invalidateListingCache"
    - from: "lib/markos/ecosystem/certifications/recertification-cron.ts::expireCertifications"
      to: "integration_listings.certification_state"
      via: "12-month expiry → flip listing.certification_state='expired' + hide from public (state cleanup)"
      pattern: "certification_state\\s*=\\s*'expired'"
    - from: "lib/markos/ecosystem/partners/directory.ts::getPublicDirectory"
      to: "certification_records (cert.expires_at JOIN)"
      via: "D-75 / RH5 — directory query also gates on cert.expires_at > now() for partner certification_level visibility"
      pattern: "expires_at\\s*>\\s*now\\(\\)"
    - from: "lib/markos/ecosystem/partners/partner-service.ts::togglePublicDirectoryVisible"
      to: "lib/markos/crm/copilot.ts::createApprovalPackage"
      via: "Approval Inbox entry public_directory_toggle (D-45/D-51)"
      pattern: "createApprovalPackage.*public_directory_toggle"
    - from: "lib/markos/ecosystem/certifications/certification-service.ts::transitionCertification"
      to: "createApprovalPackage"
      via: "certification_review entry kind for → certified | suspended | revoked"
      pattern: "createApprovalPackage.*certification_review"
---

<objective>
Build PartnerProfile P220 extension service + CertificationRecord state machine + recertification cron + criteria_templates module (227-RESEARCH.md slice 227-03). Wave 2 — parallel with 227-02 + 227-05.

Purpose: This slice owns partner extension CRUD (D-02 columns + program toggles), certification state machine (D-15..D-19), criteria template validation (Pitfall 12), and the daily recertification cron that flips expired listings to hidden (D-17 + Pitfall 2). **Per RH5 / D-75**, this slice ALSO enforces cert expiry at query time in `getPublicDirectory` (belt-and-suspenders); the cron is for state cleanup but is NOT the only trust barrier. Provides primitives consumed by 227-04 (referral/affiliate program activation) and 227-06 (CoSell partner gating).

Output: 1 contract (F-191), 1 migration (168 hot-path indexes), 5 service modules, 7 vitest test files (added directory-cert-expiry-filter.test.ts).
</objective>

<execution_context>
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User PC/Documents/GitHub/MarkOS/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-CONTEXT.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-RESEARCH.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-VALIDATION.md
@.planning/phases/227-ecosystem-partner-community-developer-growth/227-01-PLAN.md
@.planning/phases/220-saas-community-events-pr-partnership-devrel-growth/220-CONTEXT.md
@.planning/phases/208-human-operating-interface/208-CONTEXT.md
@.planning/phases/209-evidence-research-and-claim-safety/209-CONTEXT.md
@CLAUDE.md

# Incoming docs (CLAUDE.md precedence rule 5): obsidian/work/incoming/{18,22,25}-*.md INFORMATIONAL ONLY.

<interfaces>
From 227-01 (lib/markos/ecosystem/contracts.ts):

```typescript
export const CERTIFICATION_STATE = ['draft','review','certified','suspended','expired','revoked'] as const;
export const PARTNER_TYPE = ['agency','technology','consulting','reseller','affiliate','integrator','devshop','association'] as const;
export const CERTIFICATION_LEVEL = ['registered','certified','elite'] as const;
export const ACTIVE_STATUS = ['active','watch','paused','terminated'] as const;
export const BUSINESS_MODE = ['saas','commerce','ecosystem','all'] as const;
```

From 227-02 (lib/markos/ecosystem/listings/isr-cache.ts):

```typescript
export function invalidateListingCache(listing_id: string): void;
```

From P105 (lib/markos/crm/copilot.ts):

```typescript
export function createApprovalPackage(packageKind: string, subjectId: string, evidenceRefs: string[], pricingContextId?: string): Promise<ApprovalPackage>;
// New entry kinds: certification_review, public_directory_toggle (D-51)
```

From P209 EvidenceMap (A21 — soft ref).
From migration 164 (227-01) certification_records columns.
From migration 160 (227-01) partner_profiles extension columns + payout_compliance_acknowledged_at (D-71).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build certification-service.ts state machine + criteria-templates.ts + F-191 contract + migration 168 hot-path indexes</name>
  <read_first>
    - 227-CONTEXT.md D-15..D-19 (certification state machine + criteria_checks + recertification + revoked terminal + operator review queue), D-44..D-46 (class-based approval), **D-75 (cert expiry at query time / RH5)**
    - 227-RESEARCH.md §Pattern 2 Certification State Machine (lines 357-378) — `assertTransitionAllowed` reference impl
    - 227-RESEARCH.md §Pitfall 12 (lines 977-981) — criteria_checks JSONB schema drift; versioned templates required
    - 227-RESEARCH.md migration 168 spec (line 196) — hot-path composite indexes
    - 227-VALIDATION.md slice 227-03 row + Coverage Targets "Certification state machine 100%"
    - migration 164 + integration_listings.certification_state column
  </read_first>
  <files>
    contracts/F-191-ecosystem-certification-transition-v1.json
    supabase/migrations/168_ecosystem_hot_path_indexes.sql
    lib/markos/ecosystem/certifications/certification-service.ts
    lib/markos/ecosystem/certifications/criteria-templates.ts
    test/ecosystem/certifications/state-machine.test.ts
    test/ecosystem/certifications/criteria-checks.test.ts
  </files>
  <behavior>
    - **F-191 certification-transition contract:** Schema for state transition request/response: `{ cert_id, from_state, to_state, criteria_checks_patch?, reviewer_user_id, evidence_refs[], reason? }`. type='write/MCP', actor='Operator'.
    - **Migration 168 (hot-path indexes per RESEARCH.md line 196):** 4 composite indexes — (a) `idx_listings_inbox` on integration_listings(tenant_id, status, certification_state); (b) `idx_community_signals_inbox_open` on community_signals(tenant_id, urgency, routed_action_kind) WHERE routed_action_kind IS NULL; (c) `idx_cosell_partner_status` on co_sell_opportunities(tenant_id, status, partner_id); (d) **NEW for D-75:** `idx_cert_records_active_by_subject` on certification_records(subject_type, subject_id, expires_at) WHERE state='certified' — accelerates the JOIN in 227-02 + 227-03 public read queries. All indexes use `IF NOT EXISTS`.
    - **certification-service.ts:**
      - `ALLOWED_TRANSITIONS` const Record per RESEARCH.md §Pattern 2:
        - draft → ['review']
        - review → ['certified', 'draft']
        - certified → ['suspended', 'expired']
        - suspended → ['certified']
        - expired → ['review']
        - revoked: [] // terminal
      - `assertTransitionAllowed(from, to)`: throws `INVALID_CERT_TRANSITION` if not allowed; allows `* → revoked` always.
      - `createCertificationDraft({tenant_id, subject_type, subject_id, criteria_checks, reviewer_user_id?})`: validates criteria_checks via `validateCriteriaChecks`; inserts row with state='draft'.
      - `transitionCertification(cert_id, to_state, opts)`: reads current row; calls `assertTransitionAllowed(current.state, to_state)`; for transitions to certified/suspended/revoked → calls `createApprovalPackage('certification_review', cert_id, evidence_refs, ...)` (D-45) and only completes the transition AFTER approval. On state→'certified': sets certified_at=now(); expires_at=now()+12mo; recertification_due_at=now()+11mo. Updates integration_listings.certification_state when subject_type='listing'. Calls `invalidateListingCache(subject_id)`.
      - `computeListingCertificationState(listing_id)`: derives current cert state from latest cert row WHERE state='certified' AND expires_at > now(); returns 'expired' if cert past TTL; returns 'draft' if no cert exists.
    - **criteria-templates.ts:**
      - `CRITERIA_TEMPLATES` exports versioned templates per category (6 listing categories + partner_v1).
      - `validateCriteriaChecks(category_or_template_version, criteria_checks)`: returns `{ valid: bool, missing: string[], unknown: string[] }`.
      - **Pitfall 12 mitigation:** template version frozen per cert_id at draft creation.
  </behavior>
  <action>
    Step 1 — Create `contracts/F-191-ecosystem-certification-transition-v1.json` with JSON Schema covering transition request, response, and CERTIFICATION_STATE enum.
    Step 2 — Create `supabase/migrations/168_ecosystem_hot_path_indexes.sql` with 4 `CREATE INDEX IF NOT EXISTS` statements (added idx_cert_records_active_by_subject for D-75). Add header comment listing migration dependencies (163 community_signals, 164 certification_records, 165 co_sell_opportunities — all from 227-01).
    Step 3 — Create `lib/markos/ecosystem/certifications/criteria-templates.ts` with 7 template versions, frozen via `as const`.
    Step 4 — Create `lib/markos/ecosystem/certifications/certification-service.ts` per behavior.
    Step 5 — Create `test/ecosystem/certifications/state-machine.test.ts` with happy path + blocked transitions + revoked terminal + approval gate + ISR invalidation.
    Step 6 — Create `test/ecosystem/certifications/criteria-checks.test.ts` with valid template + missing required + unknown key drift + invalid result enum.
    Step 7 — Run vitest.
  </action>
  <verify>
    <automated>npx vitest run test/ecosystem/certifications/state-machine.test.ts test/ecosystem/certifications/criteria-checks.test.ts</automated>
    Plus checks:
    - `grep "ALLOWED_TRANSITIONS" lib/markos/ecosystem/certifications/certification-service.ts` returns ≥2 matches
    - `grep -E "draft.*\\['review'\\]" lib/markos/ecosystem/certifications/certification-service.ts` returns ≥1
    - `grep "INVALID_CERT_TRANSITION" lib/markos/ecosystem/certifications/certification-service.ts` returns ≥1
    - `grep "createApprovalPackage.*certification_review" lib/markos/ecosystem/certifications/certification-service.ts` returns ≥1
    - `grep "invalidateListingCache" lib/markos/ecosystem/certifications/certification-service.ts` returns ≥1
    - `grep -c "CRITERIA_TEMPLATES\." lib/markos/ecosystem/certifications/criteria-templates.ts` returns ≥7
    - `grep -c "CREATE INDEX IF NOT EXISTS" supabase/migrations/168_ecosystem_hot_path_indexes.sql` returns 4
    - `grep "idx_cert_records_active_by_subject" supabase/migrations/168_ecosystem_hot_path_indexes.sql` returns ≥1 (D-75 index)
    - **Behavior-first (RL2):** `node -e "const m=require('./lib/markos/ecosystem/certifications/certification-service.ts'); if(typeof m.assertTransitionAllowed !== 'function') process.exit(1); try { m.assertTransitionAllowed('revoked','certified'); process.exit(2);} catch(e){ if(!/INVALID_CERT_TRANSITION/.test(e.message)) process.exit(3);} console.log('OK');"` — exits 0
  </verify>
  <done>
    F-191 contract registered; migration 168 with 4 hot-path indexes (incl. D-75 idx_cert_records_active_by_subject); certification state machine 12 transitions tested; revoked terminal; criteria_checks template validation rejects drifted keys; ISR invalidation fires on listing cert state change.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build partner-service.ts + directory.ts (with cert expiry JOIN — D-75 / RH5) + recertification-cron.ts + 5 test files</name>
  <read_first>
    - 227-CONTEXT.md D-02 (PartnerProfile extension columns), D-17 (recertification cron daily 03:00 UTC), D-44/D-45 (approval gates), **D-75 (cert expiry at query time / RH5)**
    - 227-RESEARCH.md §Pitfall 2 (lines 917-921) — certification expiry race; idempotent cron; render-time fail-closed
    - 227-REVIEWS.md RH5 — cron is for state cleanup, NOT the only trust barrier
    - 227-RESEARCH.md slice 227-03 spec (lines 832-840)
    - 227-VALIDATION.md slice 227-03 row
  </read_first>
  <files>
    lib/markos/ecosystem/partners/partner-service.ts
    lib/markos/ecosystem/partners/directory.ts
    lib/markos/ecosystem/certifications/recertification-cron.ts
    test/ecosystem/partners/partner-service.test.ts
    test/ecosystem/partners/public-directory-toggle.test.ts
    test/ecosystem/certifications/recert-cron.test.ts
    test/ecosystem/certifications/listing-hide-on-expire.test.ts
    test/ecosystem/certifications/directory-cert-expiry-filter.test.ts
  </files>
  <behavior>
    - **partner-service.ts:**
      - `createPartner({tenant_id, partner_type, region, specialization_tags, certification_level, ...})`: validates per partner_profiles extended schema; inserts with business_mode='ecosystem'; active_status='active'; toggles all OFF; payout_compliance_acknowledged_at=NULL by default (D-71 — set via 227-04 acknowledgement flow).
      - `updatePartner(partner_id, patch)`: rejects updates to immutable cols.
      - `togglePublicDirectoryVisible(partner_id, target_value)`: per D-45 → calls `createApprovalPackage('public_directory_toggle', partner_id, ...)`.
      - `enableCoSell(partner_id)`: validates partner has at least one cert in state='certified' AND latest cert.expires_at > now() (D-75 — also gated at activation time, not just at directory render).
      - `enableReferral(partner_id)`: D-45 always approval-required; emits `partner_referral_program_activate`.
      - `enableAffiliate(partner_id)`: D-45 always approval-required; emits `affiliate_program_activate`.
      - `getPartnerById(partner_id)`: tenant-scoped fetch.
    - **directory.ts (D-75 / RH5 belt-and-suspenders):**
      - `PUBLIC_PARTNER_FILTER` const: `{ business_mode: ['ecosystem','all'], active_status: 'active', public_directory_visible: true, cert_expires_after: 'now()' }`. The cert_expires_after predicate is implemented in buildPublicDirectoryQuery via cert JOIN.
      - `buildPublicDirectoryQuery(supabase)`: builds query with cert JOIN — partners only appear if their declared certification_level has a backing cert row in state='certified' AND expires_at > now() OR if certification_level='registered' (which doesn't require a cert). Implementation:
        ```sql
        FROM partner_profiles pp
        LEFT JOIN LATERAL (
          SELECT expires_at FROM certification_records cr
          WHERE cr.subject_type='partner' AND cr.subject_id=pp.partner_id AND cr.state='certified'
          ORDER BY cr.certified_at DESC NULLS LAST LIMIT 1
        ) latest_cert ON true
        WHERE pp.business_mode IN ('ecosystem','all')
          AND pp.active_status='active'
          AND pp.public_directory_visible = true
          AND (
            pp.certification_level = 'registered' -- no cert required for registered tier
            OR (latest_cert.expires_at IS NOT NULL AND latest_cert.expires_at > now())
          )
        ```
      - `getPublicDirectory({region?, specialization_tags?, certification_level?})`: returns partners matching predicate. Used by 227-07 `/partners/[...slug]` SSR.
    - **recertification-cron.ts (state cleanup ONLY — RH5):**
      - `recertificationCron()`: cron handler. Calls `warnUpcomingExpiry()` then `expireCertifications()`. Designed for Vercel Cron daily 03:00 UTC.
      - `warnUpcomingExpiry()`: SQL `SELECT cert_id, subject_type, subject_id FROM certification_records WHERE state='certified' AND recertification_due_at <= now() AND expires_at > now() AND recertification_warned_at IS NULL`; emits operator task `certification_due_in_30_days`; marks recertification_warned_at=now().
      - `expireCertifications()`: SQL `UPDATE certification_records SET state='expired' WHERE state='certified' AND expires_at <= now()`. For each newly-expired cert WHERE subject_type='listing': `UPDATE integration_listings SET certification_state='expired' WHERE listing_id=$subject_id`. Then call `invalidateListingCache(listing_id)`.
      - **RH5 doc:** Add header comment to recertification-cron.ts explaining: "Cron is state cleanup. The PRIMARY trust barrier is the query-time cert.expires_at JOIN in 227-02 listing-service.ts and this slice's directory.ts. A cron miss must NOT expose expired listings/partners — query-time TRIPLE gate handles that."
  </behavior>
  <action>
    Step 1 — Create `lib/markos/ecosystem/partners/partner-service.ts` per behavior. enableCoSell validates cert AND expiry > now (D-75).
    Step 2 — Create `lib/markos/ecosystem/partners/directory.ts` with PUBLIC_PARTNER_FILTER + buildPublicDirectoryQuery (D-75 — cert JOIN).
    Step 3 — Create `lib/markos/ecosystem/certifications/recertification-cron.ts`. Add header comment per RH5 noting cron is NOT sole trust barrier.
    Step 4 — Create `test/ecosystem/partners/partner-service.test.ts`:
      - createPartner with required fields → business_mode='ecosystem', toggles all false, payout_compliance_acknowledged_at IS NULL.
      - updatePartner rejects immutable mutations.
      - enableCoSell rejects partner without certified cert OR cert with expires_at <= now (D-75).
      - enableReferral always emits createApprovalPackage('partner_referral_program_activate', ...).
    Step 5 — Create `test/ecosystem/partners/public-directory-toggle.test.ts`:
      - togglePublicDirectoryVisible → mock createApprovalPackage assert called with 'public_directory_toggle'.
      - Without approval, partner stays hidden; getPublicDirectory excludes.
      - With approval, partner included.
    Step 6 — Create `test/ecosystem/certifications/recert-cron.test.ts`:
      - 30-day warning + idempotency
      - Expire flips listing state + invalidates cache
      - Pitfall 2 retry-safe
    Step 7 — Create `test/ecosystem/certifications/listing-hide-on-expire.test.ts`:
      - Setup: listing certified+active; cert expired.
      - Run expireCertifications.
      - Assert listing.certification_state='expired'.
      - Assert getPublicListings (227-02) does NOT return.
    Step 8 — Create `test/ecosystem/certifications/directory-cert-expiry-filter.test.ts` (NEW — RH5 / D-75):
      - Setup: partner with public_directory_visible=true + certification_level='certified' + cert row state='certified' BUT expires_at = now() - 1 day (cron NOT yet run — race condition simulated).
      - Call `getPublicDirectory()`.
      - Assert: partner is NOT returned (query-time filter excludes despite cron not running).
      - Counter: same setup but cert expires_at = now() + 30 days → IS returned.
      - Counter: partner with certification_level='registered' (no cert required) → IS returned regardless of cert state.
      - **This proves cron-miss-race-mitigation independent of cron** (RH5).
    Step 9 — Run vitest.
  </action>
  <verify>
    <automated>npx vitest run test/ecosystem/partners/ test/ecosystem/certifications/recert-cron.test.ts test/ecosystem/certifications/listing-hide-on-expire.test.ts test/ecosystem/certifications/directory-cert-expiry-filter.test.ts</automated>
    Plus checks:
    - `grep "createApprovalPackage.*public_directory_toggle" lib/markos/ecosystem/partners/partner-service.ts` returns ≥1
    - `grep "createApprovalPackage.*partner_referral_program_activate\|affiliate_program_activate" lib/markos/ecosystem/partners/partner-service.ts` returns ≥2
    - `grep "PUBLIC_PARTNER_FILTER" lib/markos/ecosystem/partners/directory.ts` returns ≥2
    - **`grep -E "expires_at\s*>\s*now\(\)|cert.*expires_at" lib/markos/ecosystem/partners/directory.ts` returns ≥1 (D-75 / RH5)**
    - `grep "buildPublicDirectoryQuery" lib/markos/ecosystem/partners/directory.ts` returns ≥2
    - `grep "expireCertifications\|warnUpcomingExpiry" lib/markos/ecosystem/certifications/recertification-cron.ts` returns ≥4
    - `grep "invalidateListingCache" lib/markos/ecosystem/certifications/recertification-cron.ts` returns ≥1
    - **`grep -i "not the only trust barrier\|state cleanup" lib/markos/ecosystem/certifications/recertification-cron.ts` returns ≥1 (RH5 doc)**
    - **Behavior-first (RL2):** `node -e "const m=require('./lib/markos/ecosystem/partners/directory.ts'); if(typeof m.PUBLIC_PARTNER_FILTER !== 'object') process.exit(1); if(!('cert_expires_after' in m.PUBLIC_PARTNER_FILTER)) process.exit(2);"` — exits 0
  </verify>
  <done>
    partner-service.ts CRUD + 6 toggles; public_directory_toggle approval gated; getPublicDirectory uses cert JOIN with expires_at > now (D-75 / RH5); recertificationCron 11mo + 12mo expiry documented as state cleanup (NOT sole trust barrier); listing hide on cert expiry verified; directory-cert-expiry-filter test proves cron-miss race mitigated at query time.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator review queue → certification | Operator decision flips trust state; class-based approval enforced |
| recertification cron → public marketplace | Expired cert must hide listing (Pitfall 2 race) — query-time TRIPLE gate is primary, cron is secondary (RH5 / D-75) |
| public directory → partner certification | Cert expiry must hide partner cert badge regardless of cron status (D-75) |
| criteria_checks JSONB → cert decision | Schema drift risks unreliable cert audit (Pitfall 12) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-227-03-01 | Elevation | Direct certification state mutation bypasses approval | mitigate | `transitionCertification` for → certified/suspended/revoked emits createApprovalPackage; tested in state-machine.test.ts |
| T-227-03-02 | Tampering | Cron failure leaves expired listing/partner visible | mitigate | RH5 / D-75 — query-time cert.expires_at JOIN in 227-02 + 227-03; cron is state cleanup not sole trust barrier; directory-cert-expiry-filter.test.ts proves race mitigated |
| T-227-03-03 | Tampering | criteria_checks JSONB drift (key naming) | mitigate | criteria-templates.ts versioned templates; validateCriteriaChecks rejects unknown keys |
| T-227-03-04 | Tampering | Revoked cert reactivation | mitigate | ALLOWED_TRANSITIONS.revoked = []; assertTransitionAllowed rejects all transitions from revoked |
| T-227-03-05 | Spoofing | Partner enables co-sell without certification | mitigate | enableCoSell validates cert AND expires_at > now (D-75) |
| T-227-03-06 | Information Disclosure | public_directory_visible toggle leaks partner without consent | mitigate | createApprovalPackage('public_directory_toggle') gates the flip |
</threat_model>

<verification>
- All 12 transitions of certification state machine tested.
- criteria_checks template drift attempt rejected.
- Recert cron 11-month warning emits operator task; 12-month expiry flips state + listing.certification_state + invalidates ISR.
- Cron is state cleanup; query-time filter is primary trust barrier (RH5 / D-75).
- directory-cert-expiry-filter.test.ts proves directory excludes partners with expired certs even when cron has not run.
- listing-hide-on-expire.test.ts: getPublicListings honors cert expiry.
- Partner public_directory_visible toggle requires Approval Inbox approval.
- enableCoSell + enableReferral + enableAffiliate emit correct Approval Inbox entry kinds.
- migration 168 has 4 hot-path composite indexes (incl. D-75 idx_cert_records_active_by_subject).
- Legacy regression `npm test` green.
</verification>

<success_criteria>
- F-191 contract registered.
- Migration 168 with 4 indexes committed (incl. D-75 cert expiry index).
- 5 service modules under `lib/markos/ecosystem/{partners,certifications}/`.
- 7 vitest test files green: `npx vitest run test/ecosystem/partners/ test/ecosystem/certifications/`.
- Cert state machine: 12 transitions covered + revoked terminal verified.
- Criteria templates: 7 versions exported.
- Recert cron tested for 11mo + 12mo windows + idempotency + retry-safe split.
- Directory cert expiry filter at query time (D-75 / RH5) — directory-cert-expiry-filter.test.ts green.
- Listing hide-on-expire: getPublicListings honors cert expiry.
- Approval gates: public_directory_toggle + certification_review + partner_referral_program_activate + affiliate_program_activate.
- 227-VALIDATION.md per-task map updated for slice 227-03.
- Legacy regression green.
</success_criteria>

<output>
After completion, create `.planning/phases/227-ecosystem-partner-community-developer-growth/227-03-SUMMARY.md` with:
- F-191 + migration 168 allocation (incl. D-75 idx_cert_records_active_by_subject)
- Cert state machine transition map verified
- Criteria template versions
- Recert cron timing + retry-safe split + RH5 doc note (state cleanup, not sole trust barrier)
- Directory cert expiry filter at query time evidence (D-75 / RH5)
- Partner public_directory_toggle approval flow
- Test results (7 files green)
</output>
</content>
</invoke>