---
phase: 53-agentic-markos-orchestration-and-mir-msp-intelligence
plan: 02
type: execute
wave: 2
depends_on:
  - 53-01
files_modified:
  - onboarding/backend/agents/approval-gate.cjs
  - onboarding/backend/handlers.cjs
  - onboarding/backend/runtime-context.cjs
  - lib/markos/rbac/iam-v32.js
  - supabase/migrations/53_agent_approval_immutability.sql
  - test/agents/approval-gate.test.js
  - test/rbac/plan-approval-policy.test.js
autonomous: true
requirements:
  - AGT-03
  - IAM-03

must_haves:
  truths:
    - "Every externally visible high-impact action pauses at awaiting_approval before mutation."
    - "Only reviewer-authorized roles can approve/reject, and unauthorized attempts are deny-logged with tenant and correlation context."
    - "Approval decisions are immutable once written and bypass attempts fail deterministically."
  artifacts:
    - path: "onboarding/backend/agents/approval-gate.cjs"
      provides: "Reusable awaiting_approval gate checks and immutable decision write flow."
      exports:
        - "assertAwaitingApproval"
        - "recordApprovalDecision"
    - path: "supabase/migrations/53_agent_approval_immutability.sql"
      provides: "Immutable approval decision table and policy constraints (no update/delete)."
      exports: []
    - path: "test/agents/approval-gate.test.js"
      provides: "Approval bypass, authorization, and immutability negative-path coverage."
      exports: []
  key_links:
    - from: "onboarding/backend/handlers.cjs"
      to: "onboarding/backend/agents/approval-gate.cjs"
      via: "high-impact action gate enforcement"
      pattern: "awaiting_approval|recordApprovalDecision"
    - from: "onboarding/backend/runtime-context.cjs"
      to: "lib/markos/rbac/iam-v32.js"
      via: "deny-log payload with role-based authorization decisions"
      pattern: "approve_task|buildDenyEvent|emitDenyTelemetry"
    - from: "test/rbac/plan-approval-policy.test.js"
      to: "lib/markos/rbac/iam-v32.js"
      via: "plan:approve reviewer matrix assertions"
      pattern: "approve_task|reviewer|403"
---

<objective>
Enforce approval-gated high-impact actions with immutable IAM-03 decisions.

Purpose: Deliver AGT-03 and IAM-03 contracts on top of the lifecycle foundation from 53-01.
Output: Approval gate module, immutable decision persistence, IAM policy alignment, and denial telemetry-backed tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/53-agentic-markos-orchestration-and-mir-msp-intelligence/53-CONTEXT.md
@.planning/phases/53-agentic-markos-orchestration-and-mir-msp-intelligence/53-RESEARCH.md
@.planning/phases/53-agentic-markos-orchestration-and-mir-msp-intelligence/53-01-PLAN.md
@onboarding/backend/handlers.cjs
@onboarding/backend/runtime-context.cjs
@lib/markos/rbac/iam-v32.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 53-02-01: Create approval-gate and IAM reviewer policy RED tests (per D-04, D-05, D-06)</name>
  <read_first>
    - onboarding/backend/handlers.cjs
    - onboarding/backend/runtime-context.cjs
    - lib/markos/rbac/iam-v32.js
  </read_first>
  <files>test/agents/approval-gate.test.js, test/rbac/plan-approval-policy.test.js</files>
  <acceptance_criteria>
    - Tests enforce that high-impact mutations cannot run unless state is `awaiting_approval`.
    - Tests enforce only IAM reviewer-authorized roles can approve/reject decisions.
    - Tests enforce unauthorized approvals emit deny telemetry fields: actor_id, tenant_id, action, correlation_id.
  </acceptance_criteria>
  <action>
Create RED tests that model approval-gated flows for externally visible mutations (publish/approve/schedule class actions) per D-04. Add explicit negative tests for non-reviewer role attempts and required deny telemetry payload per D-06, plus immutable write-once decision behavior per D-05.
  </action>
  <verify>
    <automated>node --test test/agents/approval-gate.test.js test/rbac/plan-approval-policy.test.js</automated>
  </verify>
  <done>
Approval/IAM tests fail before implementation and provide full AGT-03 + IAM-03 behavior envelope.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 53-02-02: Implement immutable approval decision schema and gate helper module (per D-04, D-05)</name>
  <read_first>
    - supabase/migrations/53_agent_run_lifecycle.sql
    - onboarding/backend/runtime-context.cjs
    - .planning/phases/53-agentic-markos-orchestration-and-mir-msp-intelligence/53-RESEARCH.md
  </read_first>
  <files>onboarding/backend/agents/approval-gate.cjs, supabase/migrations/53_agent_approval_immutability.sql</files>
  <acceptance_criteria>
    - Approval helper enforces `awaiting_approval` prerequisite and decision immutability.
    - Migration introduces immutable decision storage keyed by run_id with no update/delete path.
    - Decision writes capture actor, role, tenant, rationale, and correlation metadata.
  </acceptance_criteria>
  <action>
Create `approval-gate.cjs` with guard functions that validate current state, enforce explicit approve/reject transitions, and reject second writes for the same run per D-05. Add SQL migration for append-only decisions with uniqueness per run and policies/triggers that block updates and deletes.
  </action>
  <verify>
    <automated>node --test test/agents/approval-gate.test.js --test-name-pattern="immut|awaiting|decision"</automated>
  </verify>
  <done>
Approval gate and immutable decision persistence are available as reusable runtime primitives.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 53-02-03: Wire handlers/runtime deny logging and reviewer authorization (per D-05, D-06)</name>
  <read_first>
    - onboarding/backend/agents/approval-gate.cjs
    - onboarding/backend/handlers.cjs
    - onboarding/backend/runtime-context.cjs
  </read_first>
  <files>onboarding/backend/handlers.cjs, onboarding/backend/runtime-context.cjs, lib/markos/rbac/iam-v32.js, test/agents/approval-gate.test.js</files>
  <acceptance_criteria>
    - High-impact handler paths call approval gate before mutation side effects.
    - Reviewer authorization is enforced via IAM action policy and fail-closed on unknown roles.
    - Unauthorized approve attempts emit immutable deny telemetry events with required context fields.
  </acceptance_criteria>
  <action>
Integrate approval gate checks into handlers so any externally visible high-impact mutations are blocked until explicit reviewer-authorized approval/rejection per D-04/D-05. Keep policy check centralized through IAM matrix (`approve_task` capability) and emit deny telemetry via existing runtime-context deny helpers for unauthorized attempts per D-06.
  </action>
  <verify>
    <automated>node --test test/agents/approval-gate.test.js test/rbac/plan-approval-policy.test.js</automated>
  </verify>
  <done>
AGT-03 and IAM-03 contracts are enforced in real handler paths with test-backed denial behavior.
  </done>
</task>

</tasks>

<verification>
Run `node --test test/agents/approval-gate.test.js test/rbac/plan-approval-policy.test.js` after each task and at plan completion.
</verification>

<success_criteria>
No high-impact mutation can bypass awaiting_approval or reviewer authorization, and decision logs are immutable.
</success_criteria>

<output>
After completion, create `.planning/phases/53-agentic-markos-orchestration-and-mir-msp-intelligence/53-02-SUMMARY.md`
</output>
