---
phase: 53-agentic-markos-orchestration-and-mir-msp-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - onboarding/backend/agents/run-engine.cjs
  - onboarding/backend/agents/orchestrator.cjs
  - onboarding/backend/runtime-context.cjs
  - supabase/migrations/53_agent_run_lifecycle.sql
  - test/agents/run-lifecycle.test.js
  - test/agents/run-idempotency.test.js
autonomous: true
requirements:
  - AGT-01
  - AGT-02

must_haves:
  truths:
    - "Agent run creation fails closed when tenant context or policy metadata is missing."
    - "All run transitions follow the canonical AGT-02 state machine and illegal edges are denied with audit events."
    - "Duplicate delivery does not produce duplicate run-side effects or duplicate external mutations."
  artifacts:
    - path: "onboarding/backend/agents/run-engine.cjs"
      provides: "Canonical run envelope builder, transition guard, and idempotency ledger helpers."
      exports:
        - "createRunEnvelope"
        - "assertTransitionAllowed"
        - "recordSideEffect"
    - path: "supabase/migrations/53_agent_run_lifecycle.sql"
      provides: "Append-only run + run-event + side-effect schema with tenant-scoped constraints."
      exports: []
    - path: "test/agents/run-lifecycle.test.js"
      provides: "Deterministic transition contract coverage for allowed and denied edges."
      exports: []
  key_links:
    - from: "onboarding/backend/agents/orchestrator.cjs"
      to: "onboarding/backend/agents/run-engine.cjs"
      via: "run bootstrap and transition checks"
      pattern: "createRunEnvelope|assertTransitionAllowed"
    - from: "onboarding/backend/runtime-context.cjs"
      to: "onboarding/backend/agents/run-engine.cjs"
      via: "tenant-bound envelope initialization"
      pattern: "tenant_id|principal|correlation_id"
    - from: "test/agents/run-idempotency.test.js"
      to: "supabase/migrations/53_agent_run_lifecycle.sql"
      via: "duplicate delivery side-effect uniqueness"
      pattern: "idempotency|UNIQUE|side_effect"
---

<objective>
Implement the deterministic tenant-bound run lifecycle foundation for Phase 53.

Purpose: Deliver AGT-01 and AGT-02 as enforceable contracts before approval and MIR lineage wiring.
Output: Run envelope module, lifecycle/idempotency migration, orchestrator integration, and failing-then-passing lifecycle tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/53-agentic-markos-orchestration-and-mir-msp-intelligence/53-CONTEXT.md
@.planning/phases/53-agentic-markos-orchestration-and-mir-msp-intelligence/53-RESEARCH.md
@.planning/phases/52-plugin-runtime-and-digital-agency-plugin-v1/52-01-SUMMARY.md
@onboarding/backend/agents/orchestrator.cjs
@onboarding/backend/runtime-context.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 53-01-01: Scaffold AGT-01/AGT-02 lifecycle and idempotency tests (per D-01, D-02, D-03)</name>
  <read_first>
    - onboarding/backend/agents/orchestrator.cjs
    - onboarding/backend/runtime-context.cjs
    - .planning/phases/53-agentic-markos-orchestration-and-mir-msp-intelligence/53-CONTEXT.md
  </read_first>
  <files>test/agents/run-lifecycle.test.js, test/agents/run-idempotency.test.js</files>
  <acceptance_criteria>
    - Test suite defines the full canonical state machine edges and rejects every non-listed transition.
    - Test suite asserts missing tenant_id, actor_id, or policy metadata blocks run creation.
    - Test suite asserts duplicate side-effect commits are no-op/idempotent under redelivery.
  </acceptance_criteria>
  <action>
Create RED tests for run envelope validation and lifecycle transitions using the exact AGT-02 chain `requested -> accepted -> context_loaded -> executing -> awaiting_approval -> approved/rejected -> completed/failed -> archived` per D-01. Add explicit negative tests for illegal transitions per D-02 and duplicate mutation prevention per D-03.
  </action>
  <verify>
    <automated>node --test test/agents/run-lifecycle.test.js test/agents/run-idempotency.test.js</automated>
  </verify>
  <done>
RED tests fail for missing lifecycle engine implementation and establish Nyquist-ready execution contracts.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 53-01-02: Implement run engine module and lifecycle persistence schema (per D-01, D-02, D-03)</name>
  <read_first>
    - onboarding/backend/agents/orchestrator.cjs
    - onboarding/backend/markosdb-contracts.cjs
    - supabase/migrations/52_plugin_runtime_foundation.sql
  </read_first>
  <files>onboarding/backend/agents/run-engine.cjs, supabase/migrations/53_agent_run_lifecycle.sql</files>
  <acceptance_criteria>
    - `run-engine.cjs` exports envelope, transition, and side-effect idempotency helpers used by orchestrator and handlers.
    - SQL migration creates append-only run events and unique side-effect keys that prevent duplicate mutations.
    - Illegal transition attempts are represented as persisted denied events, not silent drops.
  </acceptance_criteria>
  <action>
Implement deterministic lifecycle primitives in `run-engine.cjs` with a hardcoded allowed transition matrix per D-01 and deny-logged illegal transitions per D-02. Add migration tables for run envelopes, run events, and side-effect idempotency keys; enforce uniqueness on side-effect ledger columns so duplicate redelivery cannot replay visible mutations per D-03.
  </action>
  <verify>
    <automated>node --test test/agents/run-lifecycle.test.js</automated>
  </verify>
  <done>
Lifecycle and idempotency contracts exist as concrete runtime code and persistence constraints.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 53-01-03: Wire orchestrator to tenant-bound run envelopes and transition guards (per D-01, D-03)</name>
  <read_first>
    - onboarding/backend/agents/run-engine.cjs
    - onboarding/backend/agents/orchestrator.cjs
    - onboarding/backend/runtime-context.cjs
  </read_first>
  <files>onboarding/backend/agents/orchestrator.cjs, onboarding/backend/runtime-context.cjs, test/agents/run-idempotency.test.js</files>
  <acceptance_criteria>
    - Orchestrator cannot start a run without tenant-scoped principal and policy metadata.
    - Orchestrator transition writes use run-engine guards and deny invalid transitions.
    - Duplicate queue/event delivery path returns existing run/effect outcome without double mutation.
  </acceptance_criteria>
  <action>
Integrate `run-engine.cjs` into orchestrator entrypoints so run bootstrap requires tenant_id, actor_id, correlation_id, provider policy, and tool policy profile per AGT-01. Ensure transition calls go through the transition guard and side-effect ledger APIs so reruns/redelivery are idempotent per D-03.
  </action>
  <verify>
    <automated>node --test test/agents/run-lifecycle.test.js test/agents/run-idempotency.test.js</automated>
  </verify>
  <done>
AGT-01/AGT-02 run lifecycle behavior is executable end-to-end in orchestrator paths.
  </done>
</task>

</tasks>

<verification>
Run `node --test test/agents/run-lifecycle.test.js test/agents/run-idempotency.test.js` after each task and at plan completion.
</verification>

<success_criteria>
Phase 53 has a fail-closed lifecycle core where run creation, transitions, and idempotency are deterministic and test-backed.
</success_criteria>

<output>
After completion, create `.planning/phases/53-agentic-markos-orchestration-and-mir-msp-intelligence/53-01-SUMMARY.md`
</output>
