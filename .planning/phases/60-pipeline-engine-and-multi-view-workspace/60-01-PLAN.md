---
phase: 60-pipeline-engine-and-multi-view-workspace
plan: 01
type: execute
wave: 1
depends_on:
  - 58-crm-canonical-schema-and-identity-graph
  - 59-behavioral-tracking-and-identity-stitching
  - 51-multi-tenant-foundation-and-authorization
files_modified:
  - contracts/F-60-pipeline-config-v1.yaml
  - contracts/F-60-object-workspace-metadata-v1.yaml
  - supabase/migrations/60_crm_pipeline_workspace.sql
  - lib/markos/crm/contracts.ts
  - lib/markos/crm/api.cjs
  - api/crm/pipelines.js
  - api/crm/object-definitions.js
  - test/crm-schema/crm-pipeline-config.test.js
  - test/crm-api/crm-pipeline-api.test.js
  - test/tenant-auth/crm-pipeline-tenant-isolation.test.js
autonomous: true
requirements:
  - CRM-03
  - REP-01

must_haves:
  truths:
    - "Phase 60 starts by introducing a tenant-owned pipeline and object-metadata layer instead of hardcoding stage semantics into board UI components."
    - "Custom objects participate in pipeline and workspace eligibility through governed configuration rather than becoming admin-only edge artifacts."
    - "Pipeline and object configuration preserve the tenant, IAM, and fail-closed mutation posture established in earlier phases."
  artifacts:
    - path: "supabase/migrations/60_crm_pipeline_workspace.sql"
      provides: "Tenant-owned pipeline, stage, and workspace-object metadata tables with row-level security and validation constraints for multi-view CRM operation."
      exports: []
    - path: "contracts/F-60-pipeline-config-v1.yaml"
      provides: "Contract for pipeline families, stage ordering, simple won or lost semantics, and stage-mutation payloads."
      exports: []
    - path: "contracts/F-60-object-workspace-metadata-v1.yaml"
      provides: "Contract for custom-object workspace eligibility, canonical-object participation rules, and calendar or funnel capability requirements."
      exports: []
    - path: "api/crm/pipelines.js"
      provides: "Tenant-safe API seam for pipeline and stage configuration reads and writes."
      exports: []
    - path: "api/crm/object-definitions.js"
      provides: "Tenant-safe API seam for workspace-eligible object definitions, custom-object capabilities, and view participation rules."
      exports: []
  key_links:
    - from: "api/crm/pipelines.js"
      to: "lib/markos/crm/api.cjs"
      via: "pipeline mutations inherit tenant-context enforcement and role gating"
      pattern: "requireCrmTenantContext|assertCrmMutationAllowed|appendCrmActivity"
    - from: "supabase/migrations/60_crm_pipeline_workspace.sql"
      to: "supabase/migrations/58_crm_core_entities.sql"
      via: "canonical CRM records reference tenant-owned pipeline and stage config rather than UI-local constants"
      pattern: "crm_deals|pipeline_key|stage_key|tenant_id"
    - from: "contracts/F-60-object-workspace-metadata-v1.yaml"
      to: "lib/markos/crm/contracts.ts"
      via: "workspace eligibility stays explicit across canonical and custom object families"
      pattern: "record_kind|custom_object|calendar|funnel|detail"
---

<objective>
Create the tenant-owned pipeline and workspace-object foundation for Phase 60.

Purpose: satisfy the configuration and governance base of CRM-03 and REP-01 by adding explicit pipeline, stage, and object-eligibility contracts before building any workspace views.
Output: pipeline and object metadata contracts, migration-backed schema, tenant-safe APIs, and tests proving multi-family configuration and isolation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/60-pipeline-engine-and-multi-view-workspace/60-CONTEXT.md
@.planning/phases/60-pipeline-engine-and-multi-view-workspace/60-RESEARCH.md
@supabase/migrations/58_crm_core_entities.sql
@lib/markos/crm/contracts.ts
@lib/markos/crm/api.cjs
@api/crm/deals.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 60-01-01: Create failing schema, API, and tenant-isolation tests for pipeline and object configuration</name>
  <read_first>
    - .planning/phases/60-pipeline-engine-and-multi-view-workspace/60-CONTEXT.md
    - .planning/phases/60-pipeline-engine-and-multi-view-workspace/60-RESEARCH.md
    - test/crm-schema/crm-core-entities.test.js
    - test/tenant-auth/crm-tenant-isolation.test.js
  </read_first>
  <files>test/crm-schema/crm-pipeline-config.test.js, test/crm-api/crm-pipeline-api.test.js, test/tenant-auth/crm-pipeline-tenant-isolation.test.js</files>
  <acceptance_criteria>
    - Tests encode multiple pipeline families, tenant-owned stage ordering, and custom-object workspace eligibility as required behavior.
    - Negative-path coverage proves misconfigured objects and cross-tenant access fail closed.
    - API contract tests make stage and object-metadata payload shape explicit before implementation begins.
  </acceptance_criteria>
  <action>
Write the failing regression boundary first. The tests must force a real pipeline configuration layer and object-capability contract rather than allowing the phase to drift into a board-only UI implementation with hidden stage constants.
  </action>
  <verify>
    <automated>node --test test/crm-schema/crm-pipeline-config.test.js test/crm-api/crm-pipeline-api.test.js test/tenant-auth/crm-pipeline-tenant-isolation.test.js</automated>
  </verify>
  <done>
Wave 1 begins with explicit failing evidence for pipeline configuration, object eligibility, and tenant-safe API behavior.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 60-01-02: Implement pipeline, stage, and workspace-object schema plus contracts</name>
  <read_first>
    - supabase/migrations/58_crm_core_entities.sql
    - lib/markos/crm/contracts.ts
    - .planning/phases/60-pipeline-engine-and-multi-view-workspace/60-RESEARCH.md
  </read_first>
  <files>supabase/migrations/60_crm_pipeline_workspace.sql, contracts/F-60-pipeline-config-v1.yaml, contracts/F-60-object-workspace-metadata-v1.yaml, lib/markos/crm/contracts.ts, test/crm-schema/crm-pipeline-config.test.js</files>
  <acceptance_criteria>
    - The schema defines tenant-owned pipelines, ordered stages, and workspace-object metadata with explicit view-capability rules.
    - Canonical objects and user-defined custom objects can participate through the same configuration model without bypassing tenant guards.
    - Contracts name the allowed view types, stage semantics, fallback behavior, and validation rules explicitly.
  </acceptance_criteria>
  <action>
Add the pipeline and object foundation as an additive extension of the Phase 58 CRM model. Keep stage semantics, calendar eligibility, and funnel capability explicit in data and contracts so later workspace views read one governed source of truth.
  </action>
  <verify>
    <automated>node --test test/crm-schema/crm-pipeline-config.test.js --test-name-pattern="pipeline|stage|custom|object|calendar|funnel|tenant"</automated>
  </verify>
  <done>
The repo now has a governed pipeline and object-capability layer for all Phase 60 workspace views.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 60-01-03: Add tenant-safe APIs for pipeline and object-definition management with audit hooks</name>
  <read_first>
    - lib/markos/crm/api.cjs
    - api/crm/deals.js
    - test/crm-api/crm-api-contracts.test.js
  </read_first>
  <files>api/crm/pipelines.js, api/crm/object-definitions.js, lib/markos/crm/api.cjs, test/crm-api/crm-pipeline-api.test.js, test/tenant-auth/crm-pipeline-tenant-isolation.test.js</files>
  <acceptance_criteria>
    - Pipeline and object-definition APIs enforce tenant context and mutation-role gates consistently with existing CRM handlers.
    - Mutations emit CRM activity or equivalent audit evidence for configuration changes.
    - API responses stay deterministic enough for workspace surfaces to consume without route-local inference.
  </acceptance_criteria>
  <action>
Implement tenant-safe pipeline and object-definition handlers on top of the shared CRM API seam. Avoid creating a second auth or mutation path for workspace configuration; the plan depends on one consistent audit-safe boundary.
  </action>
  <verify>
    <automated>node --test test/crm-api/crm-pipeline-api.test.js test/tenant-auth/crm-pipeline-tenant-isolation.test.js --test-name-pattern="pipeline|object|tenant|forbidden|audit|mutation"</automated>
  </verify>
  <done>
Wave 1 ends with governed pipeline and object APIs ready for the shared workspace UI to consume.
  </done>
</task>

</tasks>

<verification>
Run the schema, API, and tenant-isolation suites after each task; Wave 1 is incomplete until pipeline and object capability rules are explicit, tenant-safe, and test-backed.
</verification>

<success_criteria>
Phase 60 has a governed pipeline and workspace-object foundation that supports multiple pipeline families, custom objects, and view eligibility without weakening tenant or audit guarantees.
</success_criteria>

<output>
After completion, create `.planning/phases/60-pipeline-engine-and-multi-view-workspace/60-01-SUMMARY.md`
</output>
