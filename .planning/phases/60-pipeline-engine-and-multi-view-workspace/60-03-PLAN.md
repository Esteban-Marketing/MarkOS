---
phase: 60-pipeline-engine-and-multi-view-workspace
plan: 03
type: execute
wave: 3
depends_on:
  - 60-01
  - 60-02
  - 59-behavioral-tracking-and-identity-stitching
files_modified:
  - contracts/F-60-workspace-rollups-v1.yaml
  - components/markos/crm/calendar-view.tsx
  - components/markos/crm/funnel-view.tsx
  - api/crm/calendar.js
  - api/crm/funnel.js
  - lib/markos/crm/workspace.ts
  - lib/markos/telemetry/events.ts
  - .planning/phases/60-pipeline-engine-and-multi-view-workspace/60-VALIDATION.md
  - test/crm-workspace/crm-calendar-view.test.js
  - test/crm-workspace/crm-funnel-view.test.js
  - test/crm-workspace/crm-cross-view-coherence.test.js
autonomous: true
requirements:
  - CRM-03
  - REP-01

must_haves:
  truths:
    - "Calendar and funnel are delivered as honest first-pass CRM views, not placeholders or pseudo-analytics shells."
    - "Cross-view coherence is verified: pipeline, table, detail, timeline, calendar, and funnel all reflect canonical CRM truth and shared filters."
    - "Phase 60 ends with explicit workspace telemetry and a direct validation ledger for view completeness, audited edits, and simple rollup correctness."
  artifacts:
    - path: "api/crm/calendar.js"
      provides: "Calendar data seam for eligible objects and approved date fields with reschedule-safe mutation support."
      exports: []
    - path: "api/crm/funnel.js"
      provides: "Simple count and value funnel aggregation directly derived from canonical CRM records and stage config."
      exports: []
    - path: "contracts/F-60-workspace-rollups-v1.yaml"
      provides: "Contract for calendar eligibility, funnel count and value totals, and cross-view rollup semantics."
      exports: []
    - path: "lib/markos/telemetry/events.ts"
      provides: "Workspace telemetry vocabulary for meaningful Phase 60 adoption and regression signals."
      exports:
        - "sanitizePayload"
        - "buildEvent"
    - path: ".planning/phases/60-pipeline-engine-and-multi-view-workspace/60-VALIDATION.md"
      provides: "Execution-grade validation ledger for six-view completeness, auditable edits, calendar eligibility, and simple funnel correctness."
      exports: []
  key_links:
    - from: "api/crm/funnel.js"
      to: "supabase/migrations/60_crm_pipeline_workspace.sql"
      via: "funnel count and value totals read live pipeline and stage config rather than hardcoded board columns"
      pattern: "pipeline_key|stage_key|count|amount|value_total"
    - from: "api/crm/calendar.js"
      to: "contracts/F-60-object-workspace-metadata-v1.yaml"
      via: "calendar only exposes object families and date fields explicitly marked eligible"
      pattern: "calendar_enabled|date_field_key|object_kind"
    - from: "test/crm-workspace/crm-cross-view-coherence.test.js"
      to: "lib/markos/crm/workspace.ts"
      via: "view switching preserves filters, pipeline context, and mutation visibility across the workspace"
      pattern: "view_type|filters|selected_record|calendar|funnel"
---

<objective>
Finish the six-view workspace with calendar and funnel, prove cross-view coherence, and finalize validation.

Purpose: close CRM-03 and Phase 60's first-pass REP-01 scope by shipping calendar and simple funnel views that stay grounded in canonical CRM records and shared workspace context.
Output: calendar and funnel APIs and UI, workspace telemetry signals, cross-view coherence tests, and a complete validation ledger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/60-pipeline-engine-and-multi-view-workspace/60-CONTEXT.md
@.planning/phases/60-pipeline-engine-and-multi-view-workspace/60-RESEARCH.md
@.planning/phases/60-pipeline-engine-and-multi-view-workspace/60-01-PLAN.md
@.planning/phases/60-pipeline-engine-and-multi-view-workspace/60-02-PLAN.md
@lib/markos/telemetry/events.ts
@lib/markos/crm/workspace.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 60-03-01: Create failing calendar, funnel, and cross-view coherence tests</name>
  <read_first>
    - .planning/phases/60-pipeline-engine-and-multi-view-workspace/60-RESEARCH.md
    - test/crm-workspace/crm-workspace-views.test.js
    - test/crm-api/crm-workspace-mutations.test.js
  </read_first>
  <files>test/crm-workspace/crm-calendar-view.test.js, test/crm-workspace/crm-funnel-view.test.js, test/crm-workspace/crm-cross-view-coherence.test.js</files>
  <acceptance_criteria>
    - Calendar tests cover eligible object rendering, approved date-field usage, and reschedule persistence.
    - Funnel tests prove stage-count and value totals are computed from canonical records and active stage config.
    - Cross-view tests prove edits made in one view are visible in the others without filter or pipeline drift.
  </acceptance_criteria>
  <action>
Write the failing regression boundary for the final two views and the coherence contract tying all six views together. The tests should block any attempt to satisfy the phase with static funnel snapshots or a fake generalized calendar.
  </action>
  <verify>
    <automated>node --test test/crm-workspace/crm-calendar-view.test.js test/crm-workspace/crm-funnel-view.test.js test/crm-workspace/crm-cross-view-coherence.test.js</automated>
  </verify>
  <done>
Wave 3 begins with explicit failing coverage for calendar, funnel, and workspace coherence.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 60-03-02: Implement honest calendar and simple funnel views plus supporting APIs</name>
  <read_first>
    - contracts/F-60-object-workspace-metadata-v1.yaml
    - supabase/migrations/60_crm_pipeline_workspace.sql
    - .planning/phases/60-pipeline-engine-and-multi-view-workspace/60-CONTEXT.md
  </read_first>
  <files>components/markos/crm/calendar-view.tsx, components/markos/crm/funnel-view.tsx, api/crm/calendar.js, api/crm/funnel.js, contracts/F-60-workspace-rollups-v1.yaml, test/crm-workspace/crm-calendar-view.test.js, test/crm-workspace/crm-funnel-view.test.js</files>
  <acceptance_criteria>
    - Calendar only renders records and date fields explicitly marked eligible in workspace metadata.
    - Funnel exposes simple count and value totals by stage and pipeline family without weighted forecasting logic.
    - Calendar and funnel are fed by the same canonical record and filter context used by other workspace views.
  </acceptance_criteria>
  <action>
Add the final two required views in the narrowest form that still satisfies the phase honestly. Resist forecasting and reporting scope creep; the deliverable is a trustworthy operational calendar and first-pass funnel over canonical records.
  </action>
  <verify>
    <automated>node --test test/crm-workspace/crm-calendar-view.test.js test/crm-workspace/crm-funnel-view.test.js --test-name-pattern="calendar|date|reschedule|funnel|count|value|stage"</automated>
  </verify>
  <done>
The six-view Phase 60 workspace is functionally complete with honest calendar and funnel coverage.
  </done>
</task>

<task type="auto">
  <name>Task 60-03-03: Lock cross-view telemetry, validation, and exit conditions around canonical CRM truth</name>
  <read_first>
    - lib/markos/telemetry/events.ts
    - .planning/phases/60-pipeline-engine-and-multi-view-workspace/60-01-PLAN.md
    - .planning/phases/60-pipeline-engine-and-multi-view-workspace/60-02-PLAN.md
  </read_first>
  <files>lib/markos/telemetry/events.ts, .planning/phases/60-pipeline-engine-and-multi-view-workspace/60-VALIDATION.md, test/crm-workspace/crm-cross-view-coherence.test.js</files>
  <acceptance_criteria>
    - Workspace telemetry vocabulary names meaningful view, record-open, stage-change, field-update, calendar-change, and funnel-open events without leaking sensitive payloads.
    - The validation ledger maps CRM-03 and REP-01 to direct evidence for all six views, auditable edits, and simple rollup correctness.
    - Cross-view coherence proof is defined in terms of canonical-record updates rather than per-view snapshots.
  </acceptance_criteria>
  <action>
Finish Phase 60 with explicit validation and telemetry contracts. The phase only counts as complete if the workspace is measurable, the final two views are real, and cross-view correctness is proven through shared CRM truth.
  </action>
  <verify>
    <manual>Confirm `60-VALIDATION.md` names all six views, maps CRM-03 and REP-01 to direct evidence, and defines exit conditions in terms of canonical-record-backed workspace behavior.</manual>
  </verify>
  <done>
Phase 60 planning ends with a direct validation contract for six-view workspace completeness and cross-view truthfulness.
  </done>
</task>

</tasks>

<verification>
Run the calendar, funnel, and cross-view suites after each task, then confirm the validation ledger and telemetry vocabulary reflect the actual six-view workspace seams.
</verification>

<success_criteria>
Phase 60 is execution-ready with tenant-owned pipelines, first-class custom objects, all six required views, audited mutations, honest count/value funnel rollups, and direct cross-view validation over canonical CRM truth.
</success_criteria>

<output>
After completion, create `.planning/phases/60-pipeline-engine-and-multi-view-workspace/60-03-SUMMARY.md`
</output>
