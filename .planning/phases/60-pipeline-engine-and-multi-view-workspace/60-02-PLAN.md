---
phase: 60-pipeline-engine-and-multi-view-workspace
plan: 02
type: execute
wave: 2
depends_on:
  - 60-01
  - 58-crm-canonical-schema-and-identity-graph
  - 59-behavioral-tracking-and-identity-stitching
files_modified:
  - app/(markos)/crm/page.tsx
  - app/(markos)/crm/[objectKind]/page.tsx
  - app/(markos)/crm/[objectKind]/[recordId]/page.tsx
  - components/markos/crm/workspace-shell.tsx
  - components/markos/crm/kanban-view.tsx
  - components/markos/crm/table-view.tsx
  - components/markos/crm/record-detail.tsx
  - components/markos/crm/timeline-panel.tsx
  - lib/markos/crm/workspace.ts
  - api/crm/records.js
  - test/crm-workspace/crm-workspace-views.test.js
  - test/crm-workspace/crm-record-detail-timeline.test.js
  - test/crm-api/crm-workspace-mutations.test.js
autonomous: true
requirements:
  - CRM-03
  - REP-01

must_haves:
  truths:
    - "The Phase 37 shell gains a real CRM workspace route model rather than expanding placeholder schema-dump pages."
    - "Kanban, table, record detail, and timeline become alternate views over one canonical record layer with shared workspace state."
    - "Editable interactions flow through auditable CRM mutation seams instead of optimistic route-local updates."
  artifacts:
    - path: "app/(markos)/crm/page.tsx"
      provides: "Workspace entry route for pipeline-aware CRM navigation and shared view state."
      exports: []
    - path: "app/(markos)/crm/[objectKind]/page.tsx"
      provides: "Object-family view route that can render Kanban and table perspectives over the same canonical records."
      exports: []
    - path: "app/(markos)/crm/[objectKind]/[recordId]/page.tsx"
      provides: "Record-detail hub where fields, timeline, tasks, notes, and stage state meet."
      exports: []
    - path: "lib/markos/crm/workspace.ts"
      provides: "Shared query, filter, selection, and mutation state for the CRM workspace so view switching remains coherent."
      exports:
        - "createWorkspaceState"
        - "applyWorkspaceMutation"
        - "serializeWorkspaceFilters"
    - path: "api/crm/records.js"
      provides: "Shared record listing and mutation API for stage moves, inline edits, and detail-surface updates."
      exports: []
  key_links:
    - from: "app/(markos)/crm/[objectKind]/page.tsx"
      to: "lib/markos/crm/workspace.ts"
      via: "kanban and table share filter, sort, and pipeline context instead of diverging by route"
      pattern: "view_type|filters|sort|pipeline_key|selected_record"
    - from: "app/(markos)/crm/[objectKind]/[recordId]/page.tsx"
      to: "lib/markos/crm/timeline.ts"
      via: "record detail renders the Phase 58 and 59 timeline as a first-class inspection surface"
      pattern: "buildCrmTimeline|activity_family|stitched_identity"
    - from: "api/crm/records.js"
      to: "lib/markos/crm/api.cjs"
      via: "workspace mutations inherit role gates and append CRM activity"
      pattern: "assertCrmMutationAllowed|appendCrmActivity|updateCrmEntity"
---

<objective>
Build the shared CRM workspace core with editable Kanban, table, record detail, and timeline views.

Purpose: satisfy the primary workspace surface of CRM-03 and the inspection backbone of REP-01 by replacing placeholder routes with canonical-record, audit-safe, multi-view CRM navigation.
Output: shared workspace state, CRM routes, core view components, auditable record mutations, and tests proving cross-view coherence for Kanban, table, detail, and timeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-pipeline-engine-and-multi-view-workspace/60-CONTEXT.md
@.planning/phases/60-pipeline-engine-and-multi-view-workspace/60-RESEARCH.md
@.planning/phases/60-pipeline-engine-and-multi-view-workspace/60-01-PLAN.md
@app/(markos)/layout.tsx
@app/(markos)/company/page.tsx
@app/(markos)/campaigns/page.tsx
@lib/markos/crm/timeline.ts
@api/crm/activities.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 60-02-01: Create failing workspace-view and mutation tests for Kanban, table, detail, and timeline</name>
  <read_first>
    - app/(markos)/layout.tsx
    - test/crm-api/crm-api-contracts.test.js
    - .planning/phases/60-pipeline-engine-and-multi-view-workspace/60-RESEARCH.md
  </read_first>
  <files>test/crm-workspace/crm-workspace-views.test.js, test/crm-workspace/crm-record-detail-timeline.test.js, test/crm-api/crm-workspace-mutations.test.js</files>
  <acceptance_criteria>
    - Tests require Kanban and table to render from the same canonical record dataset and preserve shared filter state.
    - Record-detail tests require timeline ordering, custom-field visibility, and related task or note context.
    - Mutation tests cover stage moves and inline edits with explicit CRM activity evidence rather than silent state updates.
  </acceptance_criteria>
  <action>
Write the failing view and mutation regression boundary before implementing the workspace. The tests should make it impossible to satisfy Phase 60 with disconnected widgets or read-only schema previews.
  </action>
  <verify>
    <automated>node --test test/crm-workspace/crm-workspace-views.test.js test/crm-workspace/crm-record-detail-timeline.test.js test/crm-api/crm-workspace-mutations.test.js</automated>
  </verify>
  <done>
Wave 2 starts from explicit failing coverage for the shared workspace core and auditable edit paths.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 60-02-02: Implement shared CRM workspace state and replace placeholder routes with workspace entry points</name>
  <read_first>
    - app/(markos)/company/page.tsx
    - app/(markos)/campaigns/page.tsx
    - app/(markos)/layout.tsx
  </read_first>
  <files>app/(markos)/crm/page.tsx, app/(markos)/crm/[objectKind]/page.tsx, components/markos/crm/workspace-shell.tsx, lib/markos/crm/workspace.ts, test/crm-workspace/crm-workspace-views.test.js</files>
  <acceptance_criteria>
    - The protected MarkOS shell exposes a real CRM workspace route family instead of placeholder schema dumps.
    - View state tracks active view, pipeline selection, filters, sort, and selected record consistently across Kanban and table.
    - Workspace routing remains tenant-aware and compatible with the existing shell and session model.
  </acceptance_criteria>
  <action>
Add the shared CRM workspace route model and state layer first. Keep the implementation aligned with the existing shell and avoid building isolated page-local state that would fragment the six required views later.
  </action>
  <verify>
    <automated>node --test test/crm-workspace/crm-workspace-views.test.js --test-name-pattern="workspace|route|kanban|table|filter|pipeline|detail"</automated>
  </verify>
  <done>
The placeholder CRM-adjacent routes are now superseded by a coherent, protected, multi-view workspace entry point.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 60-02-03: Implement auditable Kanban, table, record detail, and timeline interactions</name>
  <read_first>
    - lib/markos/crm/timeline.ts
    - api/crm/activities.js
    - api/crm/deals.js
  </read_first>
  <files>app/(markos)/crm/[objectKind]/[recordId]/page.tsx, components/markos/crm/kanban-view.tsx, components/markos/crm/table-view.tsx, components/markos/crm/record-detail.tsx, components/markos/crm/timeline-panel.tsx, api/crm/records.js, test/crm-workspace/crm-record-detail-timeline.test.js, test/crm-api/crm-workspace-mutations.test.js</files>
  <acceptance_criteria>
    - Kanban supports stage movement through governed pipeline config and persists through the shared mutation seam.
    - Table supports actionable inspection plus safe inline edits without bypassing CRM activity logging.
    - Record detail acts as the hub for fields, notes, tasks, stage state, and timeline history.
    - Timeline visibly consumes the canonical CRM activity model and any accepted Phase 59 stitched history.
  </acceptance_criteria>
  <action>
Implement the core editable views on top of the shared state and record APIs. Keep the mutation path deterministic and auditable so Phase 61 and later operator workflows inherit one trustworthy CRM workspace rather than a UI-only shell.
  </action>
  <verify>
    <automated>node --test test/crm-workspace/crm-record-detail-timeline.test.js test/crm-api/crm-workspace-mutations.test.js --test-name-pattern="kanban|table|detail|timeline|stage|inline|activity"</automated>
  </verify>
  <done>
Wave 2 ends with four of the six required views operational over one canonical record layer with audited edits.
  </done>
</task>

</tasks>

<verification>
Run the workspace-view, detail-timeline, and mutation suites after each task; Wave 2 is incomplete until Kanban, table, detail, and timeline all operate on one canonical dataset with audit-safe edits.
</verification>

<success_criteria>
Phase 60 has a real CRM workspace core where operators can inspect and edit canonical records through Kanban, table, record detail, and timeline views without leaving MarkOS.
</success_criteria>

<output>
After completion, create `.planning/phases/60-pipeline-engine-and-multi-view-workspace/60-02-SUMMARY.md`
</output>
