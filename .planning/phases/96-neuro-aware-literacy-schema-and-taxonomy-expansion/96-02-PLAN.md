---
phase: 96-neuro-aware-literacy-schema-and-taxonomy-expansion
plan: "02"
type: execute
wave: 2
depends_on:
  - "96-01"
files_modified:
  - supabase/migrations/96_neuro_literacy_metadata.sql
  - onboarding/backend/vector-store-client.cjs
  - onboarding/backend/literacy-chunker.cjs
  - onboarding/backend/vault/frontmatter-parser.cjs
  - onboarding/backend/vault/audience-schema.cjs
  - bin/ingest-literacy.cjs
  - test/phase-96/literacy-ingest-compat.test.js
  - test/phase-96/vector-metadata-roundtrip.test.js
autonomous: true
requirements:
  - NLI-01
  - NLI-02
must_haves:
  truths:
    - "legacy literacy documents still ingest and retrieve successfully without mandatory backfill"
    - "the new optional neuro-aware and ICP-aware metadata round-trips across relational storage and vector metadata"
    - "schema expansion uses additive migration patterns and preserves current approved-only, preview-safe posture"
    - "validators accept richer metadata while keeping the existing required fields intact"
  artifacts:
    - path: supabase/migrations/96_neuro_literacy_metadata.sql
      provides: non-destructive ADD COLUMN IF NOT EXISTS support for optional tag arrays and structured overlay profiles
    - path: onboarding/backend/vector-store-client.cjs
      provides: relational and vector-store pass-through of the new metadata without breaking the current filter and retrieval baseline
    - path: bin/ingest-literacy.cjs
      provides: backward-compatible ingest handling for legacy docs and enriched neuro-aware docs
    - path: onboarding/backend/vault/audience-schema.cjs
      provides: normalized validation of richer audience and ICP metadata fields
  key_links:
    - from: supabase/migrations/96_neuro_literacy_metadata.sql
      to: supabase/migrations/39_pain_point_tags.sql
      via: use the same additive migration pattern already established in the repo
    - from: onboarding/backend/vector-store-client.cjs
      to: onboarding/backend/literacy-chunker.cjs
      via: chunk metadata emitted during ingest must match the metadata persisted and returned during retrieval
    - from: bin/ingest-literacy.cjs
      to: onboarding/backend/vault/frontmatter-parser.cjs
      via: frontmatter extraction and ingest validation must agree on optional-field normalization and fallback behavior
---

<objective>
Persist the new neuro-aware literacy metadata through the existing storage and ingest pipeline without breaking the current corpus or retrieval foundation.

Purpose: satisfy NLI-01 and the storage half of NLI-02 by making the Phase 96 schema real in the repo's live ingestion and vector-store path while keeping the change non-destructive and backward-compatible.
Output: additive migration, vector-store and relational metadata support, and ingestion or validation updates that preserve legacy document validity.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-neuro-aware-literacy-schema-and-taxonomy-expansion/96-CONTEXT.md
@.planning/phases/96-neuro-aware-literacy-schema-and-taxonomy-expansion/96-RESEARCH.md
@.planning/phases/96-neuro-aware-literacy-schema-and-taxonomy-expansion/96-VALIDATION.md
@.planning/phases/96-neuro-aware-literacy-schema-and-taxonomy-expansion/96-01-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@supabase/migrations/39_pain_point_tags.sql
@onboarding/backend/vector-store-client.cjs
@onboarding/backend/literacy-chunker.cjs
@onboarding/backend/vault/frontmatter-parser.cjs
@onboarding/backend/vault/audience-schema.cjs
@bin/ingest-literacy.cjs

<interfaces>
Current required ingest fields enforced by the repo:
- doc_id
- discipline
- business_model
- pain_point_tags

Current stored literacy metadata in `markos_literacy_chunks`:
- business_model
- funnel_stage
- content_type
- pain_point_tags
- retrieval_keywords
- agent_use
- source_ref
- version
- ttl_days
- chunk_text

Phase 96 storage additions should remain optional:
- desired_outcome_tags, objection_tags, trust_driver_tags, trust_blocker_tags
- emotional_state_tags, neuro_trigger_tags, archetype_tags, naturality_tags, icp_segment_tags
- company_tailoring_profile, icp_tailoring_profile, stage_tailoring_profile, neuro_profile
</interfaces>
</context>

<threat_model>
- T-96-05 (destructive schema change): new columns or metadata blocks could invalidate the existing corpus. Mitigation: use ADD COLUMN IF NOT EXISTS, optional defaults, and explicit legacy-compat tests.
- T-96-06 (storage parity drift): relational rows and vector metadata could carry different values and cause retrieval inconsistency. Mitigation: add round-trip tests and update both write paths together.
- T-96-07 (required-field inflation): making the new fields mandatory too early could break ingestion. Mitigation: preserve the current required field set and treat Phase 96 additions as optional.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create failing tests for legacy ingest compatibility and additive metadata round-trip</name>
  <files>test/phase-96/literacy-ingest-compat.test.js, test/phase-96/vector-metadata-roundtrip.test.js</files>
  <behavior>
    - existing literacy documents with only the current required fields still ingest successfully
    - enriched Phase 96 docs can carry optional desired outcomes, objections, trust, emotion, naturality, ICP, and neuro tags
    - relational rows and vector metadata return the same normalized optional values for a given chunk
  </behavior>
  <action>Write the tests first using one legacy fixture and one enriched fixture. Lock the additive behavior from D-01, D-07, and D-10 by asserting that the current required fields stay unchanged while optional fields round-trip cleanly. Include explicit checks that malformed neuro trigger IDs or malformed overlay objects are rejected or normalized safely without breaking the whole ingest path.</action>
  <verify>
    <automated>node --test test/phase-96/literacy-ingest-compat.test.js test/phase-96/vector-metadata-roundtrip.test.js</automated>
  </verify>
  <done>Wave 0 tests prove that the storage path can expand safely without breaking the existing literacy corpus.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the non-destructive migration and vector-store metadata support for the new optional fields</name>
  <files>supabase/migrations/96_neuro_literacy_metadata.sql, onboarding/backend/vector-store-client.cjs</files>
  <behavior>
    - the Supabase table grows additively using repo-standard migration semantics
    - both relational and vector payloads persist the new optional tags and structured overlay blocks
    - current filters, approval posture, and retrieval defaults remain intact
  </behavior>
  <action>Create a migration that adds optional text-array columns for the filterable tag families and JSONB columns for the structured profiles, all with safe defaults and comments. Update vector-store-client.cjs so upsertLiteracyChunk persists the additive metadata into both the relational row and vector metadata, while keeping the current required metadata and current retrieval behavior fully intact.</action>
  <verify>
    <automated>node --test test/phase-96/vector-metadata-roundtrip.test.js test/vector-store-client.test.js</automated>
  </verify>
  <done>The repo can store and retrieve Phase 96 metadata without breaking the existing literacy write or read paths.</done>
</task>

<task type="auto">
  <name>Task 3: Extend ingest and audience validation to normalize optional neuro-aware fields while preserving current requirements</name>
  <files>onboarding/backend/literacy-chunker.cjs, onboarding/backend/vault/frontmatter-parser.cjs, onboarding/backend/vault/audience-schema.cjs, bin/ingest-literacy.cjs</files>
  <action>Update the markdown parsing, chunking, and audience validation helpers so they accept and normalize the additive Phase 96 fields introduced in 96-01. Keep `doc_id`, `discipline`, `business_model`, and `pain_point_tags` as the only hard requirements in this phase. Ensure legacy docs still parse, enriched docs get normalized consistently, and malformed optional values fail safely with explicit error codes rather than silent corruption.</action>
  <verify>
    <automated>node --test test/phase-96/literacy-ingest-compat.test.js test/phase-96/neuro-literacy-taxonomy.test.js test/vector-store-client.test.js</automated>
  </verify>
  <done>Ingestion and metadata validation support the richer taxonomy without forcing rewrites of the current corpus.</done>
</task>

</tasks>

<verification>
- node --test test/phase-96/literacy-ingest-compat.test.js test/phase-96/vector-metadata-roundtrip.test.js test/vector-store-client.test.js
- node --test test/phase-91/filter-taxonomy-v1.test.js test/phase-95/evaluation-contract.test.js
</verification>

<success_criteria>
- The Phase 96 metadata layer is stored additively in the live literacy pipeline.
- Existing documents remain valid and ingestable with no mandatory backfill.
- Vector and relational metadata stay in parity for the new optional fields.
- The phase still stops at schema-and-taxonomy expansion and does not attempt full corpus or template expansion.
</success_criteria>

<output>
After completion, create .planning/phases/96-neuro-aware-literacy-schema-and-taxonomy-expansion/96-02-SUMMARY.md.
</output>
