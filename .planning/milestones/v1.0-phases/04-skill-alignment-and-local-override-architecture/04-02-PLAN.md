---
plan: "04-02"
wave: 1
depends_on: []
requirements_addressed: ["SKL-02"]
files_modified:
  - .agent/skills/markos-new-project/SKILL.md
  - .agent/markos/templates/MIR/.gitignore
autonomous: true
---

# Plan 04-02: `.markos-local/` Override Convention & Scaffold

## Objective
Define and implement the `.markos-local/` directory convention. Update `markos-new-project` to scaffold `.markos-local/` on every new project init. Document the convention so agents and clients both understand what lives there and how it's protected.

## Tasks

<task id="04-02-T1">
  <title>Create `.markos-local/` scaffold template</title>
  <read_first>
    - `.agent/markos/templates/MIR/README.md`
    - `.agent/markos/templates/MIR/SETUP.md`
    - `.agent/skills/markos-new-project/SKILL.md`
    - `.planning/phases/04-skill-alignment-and-local-override-architecture/04-CONTEXT.md`
  </read_first>
  <action>
    Create the following files that define the `.markos-local/` convention template (stored in the protocol so `markos-new-project` copies them on init):

    **File 1:** `.agent/markos/templates/local-override/README.md`
    ```markdown
    # .markos-local/ — Your Private Customization Space

    This directory is **yours**. Everything in here:
    - Survives MARKOS updates and patches (never overwritten)
    - Survives GSD updates (never overwritten)
    - Overrides the corresponding protocol default when present

    ## What You Can Customize Here

    | What | Override Path | Protocol Default |
    |------|--------------|-----------------|
    | MIR Core Strategy files | `.markos-local/MIR/Core_Strategy/` | `.agent/markos/templates/MIR/Core_Strategy/` |
    | MIR Audience files | `.markos-local/MIR/Market_Audiences/` | `.agent/markos/templates/MIR/Market_Audiences/` |
    | MIR Product files | `.markos-local/MIR/Products/` | `.agent/markos/templates/MIR/Products/` |
    | MIR Campaign files | `.markos-local/MIR/Campaigns_Assets/` | `.agent/markos/templates/MIR/Campaigns_Assets/` |
    | MIR Operations files | `.markos-local/MIR/Operations/` | `.agent/markos/templates/MIR/Operations/` |
    | MSP discipline templates | `.markos-local/MSP/<discipline>/` | `.agent/markos/templates/MSP/<discipline>/` |
    | Project config overrides | `.markos-local/config/config.json` | `.agent/markos/templates/config.json` |

    ## How Overrides Work

    When any MARKOS agent loads a template, it checks `.markos-local/` first.
    If a local override exists → use it.
    If not → fall back to the protocol default.

    You'll see this in agent logs: `[override] Using .markos-local/MIR/Core_Strategy/BRAND-VOICE.md`

    ## What NOT to Put Here

    - Campaign files and RESEARCH/ output → those live in your project root MIR/, MSP/, RESEARCH/
    - Planning files → those live in .planning/
    - This directory is for **template overrides only** — not campaign output

    ## Privacy

    `.markos-local/` is added to `.gitignore` by default. Your client customizations stay private.
    Remove the gitignore entry if you want to version-control your overrides.
    ```

    **File 2:** `.agent/markos/templates/local-override/.gitignore`
    ```
    # This .gitignore is copied to the project root .gitignore by markos-new-project
    # It ensures .markos-local/ client overrides are not accidentally committed
    .markos-local/
    onboarding-seed.json
    .markos-install-manifest.json
    ```

    **File 3:** `.agent/markos/templates/local-override/MIR/.gitkeep`
    (empty file — ensures MIR/ subdirectory exists in the override scaffold)

    **File 4:** `.agent/markos/templates/local-override/MSP/.gitkeep`
    (empty file)

    **File 5:** `.agent/markos/templates/local-override/config/.gitkeep`
    (empty file)
  </action>
  <acceptance_criteria>
    - `.agent/markos/templates/local-override/README.md` exists
    - README.md contains table with "Override Path" and "Protocol Default" columns
    - README.md contains `[override]` log line example
    - `.agent/markos/templates/local-override/.gitignore` exists and contains `.markos-local/`
    - MIR/, MSP/, config/ subdirectories exist inside `templates/local-override/`
  </acceptance_criteria>
</task>

<task id="04-02-T2">
  <title>Update markos-new-project to scaffold .markos-local/ on init</title>
  <read_first>
    - `.agent/skills/markos-new-project/SKILL.md`
    - `.agent/markos/templates/local-override/README.md`
    - `.agent/markos/templates/MIR/SETUP.md`
  </read_first>
  <action>
    Edit `.agent/skills/markos-new-project/SKILL.md` to add the `.markos-local/` scaffold step.

    Find the scaffold/initialization section (where MIR/ and MSP/ are created). After that section, add:

    ```markdown
    ## Step N: Create .markos-local/ Override Directory

    Create the client override space at the project root:

    ```bash
    mkdir -p .markos-local/MIR/Core_Strategy
    mkdir -p .markos-local/MIR/Market_Audiences
    mkdir -p .markos-local/MIR/Products
    mkdir -p .markos-local/MIR/Campaigns_Assets
    mkdir -p .markos-local/MIR/Operations
    mkdir -p .markos-local/MSP
    mkdir -p .markos-local/config
    ```

    Copy the override README:
    ```bash
    cp ".agent/markos/templates/local-override/README.md" ".markos-local/README.md"
    ```

    Add .markos-local/ to .gitignore (append if .gitignore exists, create if not):
    ```bash
    echo "" >> .gitignore
    echo "# MARKOS client overrides (private — do not commit)" >> .gitignore
    echo ".markos-local/" >> .gitignore
    echo "onboarding-seed.json" >> .gitignore
    echo ".markos-install-manifest.json" >> .gitignore
    ```

    Emit confirmation: `✓ .markos-local/ created — your private override space is ready`
    ```

    Also add this block to the `## What Gets Created` summary section of the SKILL.md (if it exists):
    ```
    - `.markos-local/` — Protected client override directory (survives all updates)
    ```
  </action>
  <acceptance_criteria>
    - `.agent/skills/markos-new-project/SKILL.md` contains `mkdir -p .markos-local/MIR`
    - SKILL.md contains `cp ".agent/markos/templates/local-override/README.md" ".markos-local/README.md"`
    - SKILL.md contains `.gitignore` append logic for `.markos-local/`
    - SKILL.md contains `✓ .markos-local/ created` confirmation message
  </acceptance_criteria>
</task>

<task id="04-02-T3">
  <title>Document agent override resolution protocol in MARKOS-INDEX.md</title>
  <read_first>
    - `.agent/markos/MARKOS-INDEX.md`
    - `.agent/markos/templates/local-override/README.md`
  </read_first>
  <action>
    Add an "Override Resolution Protocol" section to `.agent/markos/MARKOS-INDEX.md`.

    Find the appropriate section (after the main file registry, before or after the Gates section). Insert:

    ```markdown
    ## Override Resolution Protocol

    All MARKOS agents that load template files MUST follow this resolution order:

    1. Check `.markos-local/<relative-template-path>` first
    2. If found → use it and log: `[override] Using .markos-local/<path>`
    3. If not found → use `.agent/markos/templates/<relative-path>`
    4. Log fallback: `[override] No .markos-local/<path> — using protocol default`

    ### Protected Paths (never touched by updates or patches)
    - `.markos-local/**/*` — all client overrides
    - `RESEARCH/**/*` — all generated research files
    - `MIR/**/*` — the live project MIR (distinguishable from template MIR by location)
    - `MSP/**/*` — the live project MSP

    ### Overridable Template Paths
    | Protocol Default | Client Override Path |
    |-----------------|---------------------|
    | `.agent/markos/templates/MIR/` | `.markos-local/MIR/` |
    | `.agent/markos/templates/MSP/` | `.markos-local/MSP/` |
    | `.agent/markos/templates/config.json` | `.markos-local/config/config.json` |
    ```
  </action>
  <acceptance_criteria>
    - `.agent/markos/MARKOS-INDEX.md` contains `## Override Resolution Protocol` section
    - Section contains numbered resolution order (1. Check .markos-local first)
    - Section contains `[override] Using .markos-local/` log format string
    - Section contains overridable paths table
  </acceptance_criteria>
</task>

## Verification
- [ ] `.agent/markos/templates/local-override/README.md` exists with full content
- [ ] `markos-new-project` SKILL.md creates `.markos-local/` with all subdirs on init
- [ ] `MARKOS-INDEX.md` documents the override resolution protocol
- [ ] `.gitignore` template excludes `.markos-local/` and `onboarding-seed.json`

## must_haves
- `.markos-local/` is created on every new project init
- Agents know to check `.markos-local/` before falling back to protocol defaults
- Clients have a clear README explaining what they can customize and how
