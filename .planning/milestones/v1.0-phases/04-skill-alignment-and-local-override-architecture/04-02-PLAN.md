---
plan: "04-02"
wave: 1
depends_on: []
requirements_addressed: ["SKL-02"]
files_modified:
  - .agent/skills/mgsd-new-project/SKILL.md
  - .agent/marketing-get-shit-done/templates/MIR/.gitignore
autonomous: true
---

# Plan 04-02: `.mgsd-local/` Override Convention & Scaffold

## Objective
Define and implement the `.mgsd-local/` directory convention. Update `mgsd-new-project` to scaffold `.mgsd-local/` on every new project init. Document the convention so agents and clients both understand what lives there and how it's protected.

## Tasks

<task id="04-02-T1">
  <title>Create `.mgsd-local/` scaffold template</title>
  <read_first>
    - `.agent/marketing-get-shit-done/templates/MIR/README.md`
    - `.agent/marketing-get-shit-done/templates/MIR/SETUP.md`
    - `.agent/skills/mgsd-new-project/SKILL.md`
    - `.planning/phases/04-skill-alignment-and-local-override-architecture/04-CONTEXT.md`
  </read_first>
  <action>
    Create the following files that define the `.mgsd-local/` convention template (stored in the protocol so `mgsd-new-project` copies them on init):

    **File 1:** `.agent/marketing-get-shit-done/templates/local-override/README.md`
    ```markdown
    # .mgsd-local/ — Your Private Customization Space

    This directory is **yours**. Everything in here:
    - Survives MGSD updates and patches (never overwritten)
    - Survives GSD updates (never overwritten)
    - Overrides the corresponding protocol default when present

    ## What You Can Customize Here

    | What | Override Path | Protocol Default |
    |------|--------------|-----------------|
    | MIR Core Strategy files | `.mgsd-local/MIR/Core_Strategy/` | `.agent/marketing-get-shit-done/templates/MIR/Core_Strategy/` |
    | MIR Audience files | `.mgsd-local/MIR/Market_Audiences/` | `.agent/marketing-get-shit-done/templates/MIR/Market_Audiences/` |
    | MIR Product files | `.mgsd-local/MIR/Products/` | `.agent/marketing-get-shit-done/templates/MIR/Products/` |
    | MIR Campaign files | `.mgsd-local/MIR/Campaigns_Assets/` | `.agent/marketing-get-shit-done/templates/MIR/Campaigns_Assets/` |
    | MIR Operations files | `.mgsd-local/MIR/Operations/` | `.agent/marketing-get-shit-done/templates/MIR/Operations/` |
    | MSP discipline templates | `.mgsd-local/MSP/<discipline>/` | `.agent/marketing-get-shit-done/templates/MSP/<discipline>/` |
    | Project config overrides | `.mgsd-local/config/config.json` | `.agent/marketing-get-shit-done/templates/config.json` |

    ## How Overrides Work

    When any MGSD agent loads a template, it checks `.mgsd-local/` first.
    If a local override exists → use it.
    If not → fall back to the protocol default.

    You'll see this in agent logs: `[override] Using .mgsd-local/MIR/Core_Strategy/BRAND-VOICE.md`

    ## What NOT to Put Here

    - Campaign files and RESEARCH/ output → those live in your project root MIR/, MSP/, RESEARCH/
    - Planning files → those live in .planning/
    - This directory is for **template overrides only** — not campaign output

    ## Privacy

    `.mgsd-local/` is added to `.gitignore` by default. Your client customizations stay private.
    Remove the gitignore entry if you want to version-control your overrides.
    ```

    **File 2:** `.agent/marketing-get-shit-done/templates/local-override/.gitignore`
    ```
    # This .gitignore is copied to the project root .gitignore by mgsd-new-project
    # It ensures .mgsd-local/ client overrides are not accidentally committed
    .mgsd-local/
    onboarding-seed.json
    .mgsd-install-manifest.json
    ```

    **File 3:** `.agent/marketing-get-shit-done/templates/local-override/MIR/.gitkeep`
    (empty file — ensures MIR/ subdirectory exists in the override scaffold)

    **File 4:** `.agent/marketing-get-shit-done/templates/local-override/MSP/.gitkeep`
    (empty file)

    **File 5:** `.agent/marketing-get-shit-done/templates/local-override/config/.gitkeep`
    (empty file)
  </action>
  <acceptance_criteria>
    - `.agent/marketing-get-shit-done/templates/local-override/README.md` exists
    - README.md contains table with "Override Path" and "Protocol Default" columns
    - README.md contains `[override]` log line example
    - `.agent/marketing-get-shit-done/templates/local-override/.gitignore` exists and contains `.mgsd-local/`
    - MIR/, MSP/, config/ subdirectories exist inside `templates/local-override/`
  </acceptance_criteria>
</task>

<task id="04-02-T2">
  <title>Update mgsd-new-project to scaffold .mgsd-local/ on init</title>
  <read_first>
    - `.agent/skills/mgsd-new-project/SKILL.md`
    - `.agent/marketing-get-shit-done/templates/local-override/README.md`
    - `.agent/marketing-get-shit-done/templates/MIR/SETUP.md`
  </read_first>
  <action>
    Edit `.agent/skills/mgsd-new-project/SKILL.md` to add the `.mgsd-local/` scaffold step.

    Find the scaffold/initialization section (where MIR/ and MSP/ are created). After that section, add:

    ```markdown
    ## Step N: Create .mgsd-local/ Override Directory

    Create the client override space at the project root:

    ```bash
    mkdir -p .mgsd-local/MIR/Core_Strategy
    mkdir -p .mgsd-local/MIR/Market_Audiences
    mkdir -p .mgsd-local/MIR/Products
    mkdir -p .mgsd-local/MIR/Campaigns_Assets
    mkdir -p .mgsd-local/MIR/Operations
    mkdir -p .mgsd-local/MSP
    mkdir -p .mgsd-local/config
    ```

    Copy the override README:
    ```bash
    cp ".agent/marketing-get-shit-done/templates/local-override/README.md" ".mgsd-local/README.md"
    ```

    Add .mgsd-local/ to .gitignore (append if .gitignore exists, create if not):
    ```bash
    echo "" >> .gitignore
    echo "# MGSD client overrides (private — do not commit)" >> .gitignore
    echo ".mgsd-local/" >> .gitignore
    echo "onboarding-seed.json" >> .gitignore
    echo ".mgsd-install-manifest.json" >> .gitignore
    ```

    Emit confirmation: `✓ .mgsd-local/ created — your private override space is ready`
    ```

    Also add this block to the `## What Gets Created` summary section of the SKILL.md (if it exists):
    ```
    - `.mgsd-local/` — Protected client override directory (survives all updates)
    ```
  </action>
  <acceptance_criteria>
    - `.agent/skills/mgsd-new-project/SKILL.md` contains `mkdir -p .mgsd-local/MIR`
    - SKILL.md contains `cp ".agent/marketing-get-shit-done/templates/local-override/README.md" ".mgsd-local/README.md"`
    - SKILL.md contains `.gitignore` append logic for `.mgsd-local/`
    - SKILL.md contains `✓ .mgsd-local/ created` confirmation message
  </acceptance_criteria>
</task>

<task id="04-02-T3">
  <title>Document agent override resolution protocol in MGSD-INDEX.md</title>
  <read_first>
    - `.agent/marketing-get-shit-done/MGSD-INDEX.md`
    - `.agent/marketing-get-shit-done/templates/local-override/README.md`
  </read_first>
  <action>
    Add an "Override Resolution Protocol" section to `.agent/marketing-get-shit-done/MGSD-INDEX.md`.

    Find the appropriate section (after the main file registry, before or after the Gates section). Insert:

    ```markdown
    ## Override Resolution Protocol

    All MGSD agents that load template files MUST follow this resolution order:

    1. Check `.mgsd-local/<relative-template-path>` first
    2. If found → use it and log: `[override] Using .mgsd-local/<path>`
    3. If not found → use `.agent/marketing-get-shit-done/templates/<relative-path>`
    4. Log fallback: `[override] No .mgsd-local/<path> — using protocol default`

    ### Protected Paths (never touched by updates or patches)
    - `.mgsd-local/**/*` — all client overrides
    - `RESEARCH/**/*` — all generated research files
    - `MIR/**/*` — the live project MIR (distinguishable from template MIR by location)
    - `MSP/**/*` — the live project MSP

    ### Overridable Template Paths
    | Protocol Default | Client Override Path |
    |-----------------|---------------------|
    | `.agent/marketing-get-shit-done/templates/MIR/` | `.mgsd-local/MIR/` |
    | `.agent/marketing-get-shit-done/templates/MSP/` | `.mgsd-local/MSP/` |
    | `.agent/marketing-get-shit-done/templates/config.json` | `.mgsd-local/config/config.json` |
    ```
  </action>
  <acceptance_criteria>
    - `.agent/marketing-get-shit-done/MGSD-INDEX.md` contains `## Override Resolution Protocol` section
    - Section contains numbered resolution order (1. Check .mgsd-local first)
    - Section contains `[override] Using .mgsd-local/` log format string
    - Section contains overridable paths table
  </acceptance_criteria>
</task>

## Verification
- [ ] `.agent/marketing-get-shit-done/templates/local-override/README.md` exists with full content
- [ ] `mgsd-new-project` SKILL.md creates `.mgsd-local/` with all subdirs on init
- [ ] `MGSD-INDEX.md` documents the override resolution protocol
- [ ] `.gitignore` template excludes `.mgsd-local/` and `onboarding-seed.json`

## must_haves
- `.mgsd-local/` is created on every new project init
- Agents know to check `.mgsd-local/` before falling back to protocol defaults
- Clients have a clear README explaining what they can customize and how
