---
plan: "04-03"
wave: 2
depends_on: ["04-01", "04-02"]
requirements_addressed: ["SKL-01", "SKL-02"]
files_modified:
  - .agent/marketing-get-shit-done/MGSD-INDEX.md
autonomous: true
---

# Plan 04-03: Overridable Path Self-Documentation + Phase Verification

## Objective
Complete the self-documentation layer: add overridable path comments to every skill that touches templates, update MGSD-INDEX.md with a unified overridable paths registry, and run final verification that all Phase 4 success criteria are met.

## Tasks

<task id="04-03-T1">
  <title>Add OVERRIDABLE comments to all skills that reference templates</title>
  <read_first>
    - `.planning/phases/04-skill-alignment-and-local-override-architecture/skill-audit.md`
    - All mgsd-* SKILL.md files listed in the audit as touching templates
    - `.agent/marketing-get-shit-done/MGSD-INDEX.md` Override Resolution Protocol section
  </read_first>
  <action>
    For every skill identified in the audit (04-01-T1) as reading or writing template paths, verify that each overridable template path reference has an adjacent `<!-- OVERRIDABLE: path -->` HTML comment.

    Format for the comment:
    ```
    <!-- OVERRIDABLE: .mgsd-local/MIR/Core_Strategy/ overrides .agent/marketing-get-shit-done/templates/MIR/Core_Strategy/ -->
    ```

    The comment must appear either:
    - Directly after the path string in the skill's markdown
    - At the top of the `## Template Paths` section added in Plan 04-01

    For skills that were already updated in 04-01, verify the comments are correctly placed. For any missing ones, add them now.

    Also verify that `mgsd-new-project` SKILL.md has OVERRIDABLE comments for all 5 MIR subdirs and MSP.
  </action>
  <acceptance_criteria>
    - `grep -r "OVERRIDABLE" .agent/skills/mgsd-new-project/SKILL.md` returns at least 6 results (one per MIR subdir + MSP)
    - Every skill in the audit that touches templates has at least 1 `<!-- OVERRIDABLE:` comment
    - `grep -rl "OVERRIDABLE" .agent/skills/` returns at least 3 skill files
  </acceptance_criteria>
</task>

<task id="04-03-T2">
  <title>Add Overridable Paths Registry to MGSD-INDEX.md</title>
  <read_first>
    - `.agent/marketing-get-shit-done/MGSD-INDEX.md`
    - `.planning/phases/04-skill-alignment-and-local-override-architecture/skill-audit.md`
  </read_first>
  <action>
    Add an "Overridable Paths Registry" section to MGSD-INDEX.md (after the Override Resolution Protocol section added in Plan 04-02).

    Content:
    ```markdown
    ## Overridable Paths Registry

    Complete list of files/directories clients can place in `.mgsd-local/` to override protocol defaults.
    Any file placed here **will not be touched** by `mgsd update` or GSD patches.

    | Client Override Path | Overrides Protocol File | Consumed By |
    |---------------------|------------------------|-------------|
    | `.mgsd-local/MIR/Core_Strategy/*.md` | `templates/MIR/Core_Strategy/*.md` | mgsd-new-project, mgsd-plan-phase |
    | `.mgsd-local/MIR/Market_Audiences/*.md` | `templates/MIR/Market_Audiences/*.md` | mgsd-new-project, mgsd-research-phase |
    | `.mgsd-local/MIR/Products/*.md` | `templates/MIR/Products/*.md` | mgsd-new-project, mgsd-plan-phase |
    | `.mgsd-local/MIR/Campaigns_Assets/*.md` | `templates/MIR/Campaigns_Assets/*.md` | mgsd-execute-phase |
    | `.mgsd-local/MIR/Operations/*.md` | `templates/MIR/Operations/*.md` | mgsd-execute-phase, mgsd-linear-sync |
    | `.mgsd-local/MSP/<discipline>/*.md` | `templates/MSP/<discipline>/*.md` | mgsd-plan-phase, mgsd-execute-phase |
    | `.mgsd-local/config/config.json` | `templates/config.json` | All agents (project config) |
    ```
  </action>
  <acceptance_criteria>
    - MGSD-INDEX.md contains `## Overridable Paths Registry` section
    - Table has columns: Client Override Path, Overrides Protocol File, Consumed By
    - Table includes at least 7 rows (one per overridable path group)
  </acceptance_criteria>
</task>

<task id="04-03-T3">
  <title>Run Phase 4 verification checklist</title>
  <read_first>
    - `.planning/ROADMAP.md` — Phase 4 success criteria
    - `.planning/REQUIREMENTS.md` — SKL-01, SKL-02 definitions
    - All updated SKILL.md files
    - `.agent/marketing-get-shit-done/MGSD-INDEX.md`
    - `.agent/marketing-get-shit-done/templates/local-override/README.md`
  </read_first>
  <action>
    Execute these verification checks and report results:

    Check 1: `grep -r "Template Paths" .agent/skills/mgsd-*.*/SKILL.md | wc -l`
    Expected: ≥ 7 (one per major mgsd-* skill)

    Check 2: `grep -r "MIR-TEMPLATE\|MSP-TEMPLATE" .agent/skills/` 
    Expected: 0 results (all stale references eliminated)

    Check 3: `ls .agent/marketing-get-shit-done/templates/local-override/`
    Expected: README.md, .gitignore, MIR/, MSP/, config/

    Check 4: `grep "mgsd-local" .agent/skills/mgsd-new-project/SKILL.md | wc -l`
    Expected: ≥ 5 lines referencing .mgsd-local/

    Check 5: `grep "Override Resolution Protocol" .agent/marketing-get-shit-done/MGSD-INDEX.md`
    Expected: 1 match

    Check 6: `grep "Overridable Paths Registry" .agent/marketing-get-shit-done/MGSD-INDEX.md`
    Expected: 1 match

    Write verification results to `.planning/phases/04-skill-alignment-and-local-override-architecture/VERIFICATION.md`.

    Format:
    ```markdown
    # Phase 4 Verification

    | Check | Command | Expected | Result | Status |
    |-------|---------|----------|--------|--------|
    | Skills have Template Paths | grep count | ≥ 7 | X | ✅/❌ |
    ...

    ## Overall: PASSED / FAILED
    ```
  </action>
  <acceptance_criteria>
    - `VERIFICATION.md` exists in phase directory
    - Overall result is PASSED
    - All 6 checks show ✅
  </acceptance_criteria>
</task>

## Verification
- [ ] All mgsd-* skills have `<!-- OVERRIDABLE:` comments for template paths
- [ ] MGSD-INDEX.md has complete Overridable Paths Registry table
- [ ] VERIFICATION.md exists and shows PASSED
- [ ] Zero stale `MIR-TEMPLATE` or `MSP-TEMPLATE` references in any skill

## must_haves
- Phase 4 verification report written and PASSED
- MGSD-INDEX.md is the single source of truth for overridable paths
