---
id: "24-02-config-environment-centralization"
wave: 1
depends_on: ["24-01-runtime-split-audit"]
discipline: engineering
requirements: ["RTH-01", "RTH-02", "RTH-03"]
files_modified:
  - onboarding/backend/handlers.cjs
  - onboarding/backend/server.cjs
  - onboarding/backend/path-constants.cjs
  - onboarding/backend/utils.cjs
  - onboarding/backend/telemetry.cjs
mir_files_referenced: []
tracking_required: false
requires_human_approval: false
autonomous: true
neuro_dimension: false
objective: "Centralize environment detection, config loading, slug resolution, and persistence guards so local and hosted runtimes follow one explicit precedence model."
must_haves:
  - Environment detection is defined in one place
  - Config precedence is documented and testable
  - Slug and persistence decisions are no longer scattered through handlers
  - Unsupported hosted behaviors fail behind explicit guards
---

# Plan 24-02: Config and Environment Centralization

## Wave 1 â€” Create One Runtime Contract

<task id="1" name="Define the canonical runtime context model">
  <read_first>
    - onboarding/backend/handlers.cjs
    - onboarding/backend/server.cjs
    - onboarding/backend/path-constants.cjs
    - .protocol-lore/CONVENTIONS.md
  </read_first>
  <action>
    Design a shared runtime-context helper or equivalent abstraction that determines:
    - local versus hosted mode
    - config source and precedence
    - canonical project slug source
    - write-capable versus read-only behavior
    The model should be explicit enough that entrypoints can pass context into handlers rather than relying on scattered global checks.
  </action>
  <acceptance_criteria>
    - Runtime mode, config precedence, and slug resolution can be described as one coherent contract
    - Environment-sensitive behavior is no longer encoded as ad hoc branches throughout handler code
  </acceptance_criteria>
</task>

<task id="2" name="Isolate persistence and filesystem guards">
  <read_first>
    - onboarding/backend/write-mir.cjs
    - onboarding/backend/vector-store-client.cjs
    - api/approve.js
  </read_first>
  <action>
    Move write-sensitive and filesystem-sensitive decisions behind explicit helpers or guard functions so approval flows do not silently depend on local disk semantics when running in hosted environments.
  </action>
  <acceptance_criteria>
    - Write-capable behavior is isolated from generic request handling
    - Unsupported hosted paths fail explicitly or are redirected through a defined persistence contract
  </acceptance_criteria>
</task>

<task id="3" name="Preserve current compatibility-sensitive behavior while centralizing">
  <read_first>
    - .planning/phases/23-identity-normalization/23-03-PLAN.md
    - .markos-project.json
  </read_first>
  <action>
    Ensure the centralized runtime model respects the Phase 23 compatibility contract for local state, env vars, and slug naming. If the canonical runtime contract differs from current incidental behavior, document that difference before changing it.
  </action>
  <acceptance_criteria>
    - Runtime hardening does not accidentally invalidate compatibility-sensitive state surfaces
    - Any behavior change is deliberate and documented rather than incidental
  </acceptance_criteria>
</task>

## Verification

Manual review should be able to trace config resolution, environment detection, slug sourcing, and write-guard decisions from one runtime contract instead of reconstructing them from multiple files.

