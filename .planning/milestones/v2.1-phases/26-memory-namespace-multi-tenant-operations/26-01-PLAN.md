---
id: "26-01-namespace-contract"
wave: 1
depends_on: ["23-03-compatibility-contract", "24-02-config-environment-centralization"]
discipline: engineering
requirements: ["MMO-01", "MMO-02"]
files_modified:
  - onboarding/backend/vector-store-client.cjs
  - .protocol-lore/MEMORY.md
  - .protocol-lore/WORKFLOWS.md
  - .planning/PROJECT.md
  - README.md
mir_files_referenced: []
tracking_required: false
requires_human_approval: true
autonomous: false
neuro_dimension: false
objective: "Define the canonical memory contract for project slugs, Vector Store collection names, metadata collections, and compatibility reads so later work has one authoritative namespace model."
must_haves:
  - Namespace naming is documented from persisted slug through collection names
  - Draft, seed, and metadata collection roles are explicit
  - Compatibility-read behavior is defined for legacy and canonical prefixes
  - Operators and future phases can reason about memory without reverse-engineering code comments
---

# Plan 26-01: Namespace Contract

## Wave 1 â€” Define the Memory Model

<task id="1" name="Document canonical namespace construction">
  <read_first>
    - onboarding/backend/vector-store-client.cjs
    - .markos-project.json
    - .protocol-lore/MEMORY.md
  </read_first>
  <action>
    Publish an explicit namespace contract that explains:
    - the canonical source of `project_slug`
    - how section, draft, and meta collections are named
    - which namespace prefixes are canonical now versus compatibility-only
    - which collection patterns are reserved for future use
  </action>
  <acceptance_criteria>
    - The repo has one authoritative answer for how memory namespaces are formed
    - Collection roles and naming semantics are clear enough to support testing and migration work
  </acceptance_criteria>
</task>

<task id="2" name="Define compatibility-read behavior">
  <read_first>
    - .planning/phases/23-identity-normalization/23-03-PLAN.md
    - onboarding/backend/vector-store-client.cjs
  </read_first>
  <action>
    Specify when runtime code should read from legacy `markos-*` collections, when it should prefer canonical namespaces, and how fallback lookup order should behave during the transition period.
  </action>
  <acceptance_criteria>
    - Compatibility-read behavior is explicit instead of implied by whichever collection happens to exist
    - Later migration work can rely on a defined lookup order instead of rediscovering it
  </acceptance_criteria>
</task>

<task id="3" name="Feed the namespace contract into operator docs and planning">
  <read_first>
    - README.md
    - .planning/PROJECT.md
    - .protocol-lore/WORKFLOWS.md
  </read_first>
  <action>
    Update operator-facing and planning-facing docs so memory namespace behavior is treated as a product contract, not an internal implementation detail.
  </action>
  <acceptance_criteria>
    - Namespace rules are visible in the docs operators and planners actually use
    - Phase 26 deliverables are treated as foundational inputs to Phase 27 and later migration work
  </acceptance_criteria>
</task>

## Verification

Manual review should be able to answer where a project's memory lives, how draft and seed data are separated, and how legacy collection prefixes remain discoverable during transition.

