---
id: "26-02-migration-safe-namespace-handling"
wave: 1
depends_on: ["26-01-namespace-contract"]
discipline: engineering
requirements: ["MMO-01", "MMO-03"]
files_modified:
  - onboarding/backend/vector-store-client.cjs
  - onboarding/backend/handlers.cjs
  - bin/update.cjs
  - .planning/ROADMAP.md
mir_files_referenced: []
tracking_required: false
requires_human_approval: false
autonomous: true
neuro_dimension: false
objective: "Add migration-safe namespace handling so legacy `markos-*` collections remain discoverable while the product moves toward a canonical MarkOS-aware namespace strategy."
must_haves:
  - Namespace lookup order is compatible with existing data
  - Migration work can happen incrementally without losing discoverability
  - The code distinguishes read compatibility from destructive migration
  - Phase 26 reduces ambiguity around future prefix transitions
---

# Plan 26-02: Migration-Safe Namespace Handling

## Wave 1 — Preserve Discoverability During Transition

<task id="1" name="Implement compatibility-aware collection lookup">
  <read_first>
    - onboarding/backend/vector-store-client.cjs
    - .planning/phases/26-memory-namespace-multi-tenant-operations/26-01-PLAN.md
  </read_first>
  <action>
    Introduce namespace helpers or equivalent logic that can resolve the appropriate read target across canonical and legacy prefixes without forcing a one-shot migration.
  </action>
  <acceptance_criteria>
    - Existing project memory remains discoverable even if namespace prefixes differ
    - Read compatibility and canonical write behavior are intentionally separated
  </acceptance_criteria>
</task>

<task id="2" name="Separate safe compatibility reads from destructive migration steps">
  <read_first>
    - bin/update.cjs
    - onboarding/backend/handlers.cjs
    - .planning/ROADMAP.md
  </read_first>
  <action>
    Define which namespace operations can happen automatically during normal runtime and which must remain explicit migration work. Avoid deleting, renaming, or overwriting collections implicitly.
  </action>
  <acceptance_criteria>
    - Runtime behavior does not quietly perform destructive namespace migration
    - Later migration phases inherit explicit boundaries between safe reads and explicit upgrades
  </acceptance_criteria>
</task>

<task id="3" name="Lock migration safety into phase sequencing">
  <read_first>
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
  </read_first>
  <action>
    Reflect the compatibility-read and migration boundaries in planning artifacts so namespace transition work is sequenced deliberately rather than folded into unrelated runtime changes.
  </action>
  <acceptance_criteria>
    - Planning docs show that discoverability must be preserved before namespace cleanup
    - Future work can reference explicit migration boundaries instead of relying on context from old discussions
  </acceptance_criteria>
</task>

## Verification

Manual review should be able to explain how existing `markos-*` collections remain readable, what the canonical write target is, and which actions are deferred to an explicit migration path.

