---
id: "25-02-merge-safety-fixtures"
wave: 1
depends_on: ["24-03-runtime-parity-coverage"]
discipline: engineering
requirements: ["ONQ-02"]
files_modified:
  - test/write-mir.test.js
  - onboarding/backend/write-mir.cjs
  - .agent/marketing-get-shit-done/templates/MIR/
mir_files_referenced: []
tracking_required: false
requires_human_approval: false
autonomous: true
neuro_dimension: false
objective: "Protect approved-draft persistence by adding fixture-heavy tests for fuzzy header matching, fallback insertion, and template variance in write-mir.cjs."
must_haves:
  - Merge tests cover realistic template shapes, not only trivial content replacement
  - Header drift and missing-header fallback behavior are explicitly asserted
  - STATE.md update behavior remains coupled to successful write flows
  - Silent content corruption risks are reduced through regression coverage
---

# Plan 25-02: Approval and Merge Safety

## Wave 1 — Protect Client-Owned Content

<task id="1" name="Expand merge fixtures beyond happy-path replacement">
  <read_first>
    - test/write-mir.test.js
    - onboarding/backend/write-mir.cjs
  </read_first>
  <action>
    Add fixture cases for:
    - templates with expected headers
    - templates with slightly drifted headers
    - templates missing the target header entirely
    - multi-section drafts where only some sections should match
    Validate both replacement behavior and fallback append behavior.
  </action>
  <acceptance_criteria>
    - Merge tests reflect the actual fuzzy-header strategy used in production
    - Fallback insertion is observable and intentionally asserted instead of treated as invisible success
  </acceptance_criteria>
</task>

<task id="2" name="Protect STATE and disclaimer behavior during merges">
  <read_first>
    - onboarding/backend/write-mir.cjs
    - test/write-mir.test.js
  </read_first>
  <action>
    Extend tests to cover disclaimer insertion, generated markers, and `STATE.md` updates when drafts are merged into existing local files, newly cloned files, or fallback-written files.
  </action>
  <acceptance_criteria>
    - Generated markers and state transitions stay consistent across merge paths
    - Changes to merge logic cannot silently break completion tracking or output provenance markers
  </acceptance_criteria>
</task>

<task id="3" name="Define acceptable merge degradation boundaries">
  <read_first>
    - .planning/research/PITFALLS.md
    - .planning/phases/25-onboarding-quality-merge-safety/25-CONTEXT.md
  </read_first>
  <action>
    Decide and encode which merge degradations are acceptable for now, which must fail loudly, and which should remain documented fallback behavior. Use those decisions to keep future write-mir changes inside explicit safety boundaries.
  </action>
  <acceptance_criteria>
    - The phase leaves behind an explicit safety contract around fuzzy merge behavior
    - Merge logic can evolve without re-opening silent corruption risk every time templates change
  </acceptance_criteria>
</task>

## Verification

The write-mir test suite should demonstrate correct replacement, explicit fallback insertion, preserved provenance markers, and reliable `STATE.md` updates across representative template variants.
