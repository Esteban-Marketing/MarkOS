---
id: "25-01-extraction-scoring-fixtures"
wave: 1
depends_on: ["24-03-runtime-parity-coverage"]
discipline: engineering
requirements: ["ONQ-01"]
files_modified:
  - test/onboarding-server.test.js
  - test/setup.js
  - onboarding/backend/handlers.cjs
  - onboarding/backend/confidences/confidence-scorer.cjs
  - onboarding/backend/extractors/schema-extractor.cjs
  - onboarding/backend/scrapers/tavily-scraper.cjs
mir_files_referenced: []
tracking_required: false
requires_human_approval: false
autonomous: true
neuro_dimension: false
objective: "Add representative onboarding fixtures that validate extraction quality and confidence scoring across URL, file, and mixed-source input paths."
must_haves:
  - Representative fixtures exist for URL-only, file-only, and mixed-source onboarding inputs
  - Confidence scoring behavior is asserted on meaningful edge cases rather than only filled-or-empty values
  - Tests cover extraction output shape and scoring output shape together where they drive interview behavior
  - Fixture coverage reflects realistic onboarding variability instead of only synthetic happy paths
---

# Plan 25-01: Extraction and Scoring Fixtures

## Wave 1 — Validate Input Quality

<task id="1" name="Add representative onboarding fixtures">
  <read_first>
    - test/onboarding-server.test.js
    - onboarding/backend/handlers.cjs
    - onboarding/backend/extractors/schema-extractor.cjs
  </read_first>
  <action>
    Create reusable fixtures for onboarding inputs that cover:
    - URL-only source extraction
    - file-only extraction across supported file types
    - mixed-source extraction where web, file, and chat input overlap or conflict
    Prefer fixtures that mirror realistic operator input quality and ambiguity instead of idealized complete payloads.
  </action>
  <acceptance_criteria>
    - Fixture coverage spans the main supported input modes used by onboarding
    - Tests can exercise ambiguous and partial-input cases without inlining large payloads into the test body
  </acceptance_criteria>
</task>

<task id="2" name="Assert confidence-routing behavior">
  <read_first>
    - onboarding/backend/confidences/confidence-scorer.cjs
    - onboarding/backend/handlers.cjs
  </read_first>
  <action>
    Add tests that assert how extracted data is scored and how those scores affect follow-up question generation. Cover edge cases like short strings, sparse nested objects, partially populated arrays, and mixed-quality extracted fields.
  </action>
  <acceptance_criteria>
    - Confidence scoring edge cases are explicitly tested
    - Interview-routing behavior is grounded in asserted score outputs rather than assumed heuristics
  </acceptance_criteria>
</task>

<task id="3" name="Protect fixture-backed extraction contracts">
  <read_first>
    - .planning/research/PITFALLS.md
    - .planning/phases/24-runtime-hardening/24-03-PLAN.md
  </read_first>
  <action>
    Define a small but durable set of fixture-backed expectations for extraction and scoring so later onboarding work can improve internals without accidentally degrading output structure or interview quality.
  </action>
  <acceptance_criteria>
    - Extraction and scoring tests act as a stable contract for future changes
    - Phase 25 quality work leaves behind reusable fixtures rather than one-off assertions
  </acceptance_criteria>
</task>

## Verification

Targeted onboarding tests should show that representative inputs produce stable schema shapes and confidence outputs across the main supported input modes.
