---
phase: 45-operations-flow-inventory-contract-map
plan: 06
type: execute
wave: 4
depends_on:
  - 45-03
files_modified:
  - contracts/F-0[9]-*.yaml
  - contracts/F-1[0-7]-*.yaml
  - .planning/FLOW-CONTRACTS.md
autonomous: true
requirements:
  - API-01
  - API-02

must_haves:
  truths:
    - "All 17 canonical flows map to exactly one contract artifact."
    - "Flow-to-contract mapping ledger reports zero orphaned and zero unmapped flows."
    - "D-17 remains enforced: no event/async contract framework introduced in this phase."
  artifacts:
    - path: "contracts/F-10-literacy-admin-health-v1.yaml"
      provides: "second-batch contract stub set for non-initial flows"
      exports: []
    - path: ".planning/FLOW-CONTRACTS.md"
      provides: "flow_id to contract mapping totals and traceability ledger"
      exports: []
  key_links:
    - from: ".planning/FLOW-CONTRACTS.md"
      to: "contracts/F-*.yaml"
      via: "contract_file references"
      pattern: "contracts/F-"
---

<objective>
Complete the second contract batch and close the final flow-to-contract mapping ledger.

Purpose: Finish API-01 mapping completeness and phase-level partial API-02 coverage while keeping scope constrained for Phase 45.
Output: contracts F-10..F-17 and finalized .planning/FLOW-CONTRACTS.md totals.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-operations-flow-inventory-contract-map/45-CONTEXT.md
@.planning/phases/45-operations-flow-inventory-contract-map/45-RESEARCH.md
@contracts/flow-registry.json
@contracts/schema.json
@.planning/FLOW-INVENTORY.md
@contracts/F-01-client-intake-submit-v1.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 45-06-01: Create second batch contract stubs F-09..F-17 (per API-01 and partial API-02)</name>
  <read_first>
    contracts/flow-registry.json
    contracts/schema.json
    contracts/F-01-client-intake-submit-v1.yaml
    .planning/FLOW-INVENTORY.md
  </read_first>
  <files>contracts/F-09-literacy-coverage-report-v1.yaml, contracts/F-10-literacy-admin-health-v1.yaml, contracts/F-11-literacy-admin-query-v1.yaml, contracts/F-12-ai-interview-generate-q-v1.yaml, contracts/F-13-ai-interview-parse-answer-v1.yaml, contracts/F-14-source-extraction-v1.yaml, contracts/F-15-extract-and-score-v1.yaml, contracts/F-16-spark-suggestion-v1.yaml, contracts/F-17-competitor-discovery-v1.yaml</files>
  <behavior>
    - Test 1: Flow IDs F-09 through F-17 resolve to exactly one contract file each.
    - Test 2: Each contract includes x-markos-meta fields for actor/auth/handler/path.
    - Test 3: Contracts validate against contracts/schema.json and no unknown enum labels are introduced.
  </behavior>
  <action>
Create YAML contract stubs for flow_id F-09..F-17 using filename convention contracts/F-XX-{flow_name}-v1.yaml. Populate each stub with flow_id, flow_name, domain, flow_type, version=v1, openapi=3.0.3, info.title, info.version=1.0.0, x-markos-meta fields, and one minimal paths entry matching local_path. Preserve D-17 by excluding event-stream/async contract artifacts.
  </action>
  <acceptance_criteria>
    - contracts directory contains F-09 through F-17 YAML files with required schema keys.
    - No async/event-specific contract artifacts are created in this task (D-17).
    - `node bin/validate-flow-contracts.cjs --schema contracts/schema.json --registry contracts/flow-registry.json` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node bin/validate-flow-contracts.cjs --schema contracts/schema.json --registry contracts/flow-registry.json</automated>
  </verify>
  <done>
    Second contract batch is schema-validated and ready for mapping ledger closure.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 45-06-02: Finalize FLOW-CONTRACTS mapping ledger and totals</name>
  <read_first>
    .planning/FLOW-INVENTORY.md
    contracts/flow-registry.json
    contracts/schema.json
  </read_first>
  <files>.planning/FLOW-CONTRACTS.md</files>
  <behavior>
    - Test 1: All 17 flow IDs resolve to exactly one contract file after this task.
    - Test 2: FLOW-CONTRACTS.md reports zero orphaned flows and zero unmapped flows.
    - Test 3: Mapping table references only existing files under contracts/.
  </behavior>
  <action>
Create .planning/FLOW-CONTRACTS.md with complete mapping table columns (flow_id, flow_name, contract_file, contract_version, schema_valid, notes) and explicit totals: total_flows=17, orphaned_flows=0, flows_without_contracts=0. Include an explicit D-17 note that async/event contracts are out-of-scope and deferred.
  </action>
  <acceptance_criteria>
    - Every mapping row in .planning/FLOW-CONTRACTS.md references an existing YAML file.
    - Mapping totals in FLOW-CONTRACTS.md match the 17-flow registry (`total_flows=17`, `orphaned_flows=0`, `flows_without_contracts=0`).
    - `node bin/validate-flow-contracts.cjs --schema contracts/schema.json --registry contracts/flow-registry.json` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node bin/validate-flow-contracts.cjs --schema contracts/schema.json --registry contracts/flow-registry.json && node -e "const fs=require('fs');const t=fs.readFileSync('.planning/FLOW-CONTRACTS.md','utf8');if(!t.includes('total_flows=17')||!t.includes('orphaned_flows=0')||!t.includes('flows_without_contracts=0'))process.exit(1);"</automated>
  </verify>
  <done>
    Flow-to-contract coverage is complete and schema-validated, establishing the Phase 47 OpenAPI starting point.
  </done>
</task>

</tasks>

<verification>
Run contract validator and a directory count check: `node -e "const fs=require('fs');const n=fs.readdirSync('contracts').filter(f=>/^F-\d{2}.*-v1\.yaml$/.test(f)).length; if(n!==17)process.exit(1);"`.
</verification>

<success_criteria>
All active Phase 45 flows are contract-mapped and validated by a canonical schema without introducing a parallel contract format.
</success_criteria>

<output>
After completion, create `.planning/phases/45-operations-flow-inventory-contract-map/45-06-SUMMARY.md`
</output>
