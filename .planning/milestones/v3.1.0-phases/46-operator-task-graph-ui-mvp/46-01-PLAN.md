---
phase: 46-operator-task-graph-ui-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(markos)/layout.tsx
  - app/(markos)/operations/page.tsx
  - lib/markos/rbac/policies.ts
autonomous: true
requirements:
  - OPS-01

must_haves:
  truths:
    - "Owner and operator roles can reach /markos/operations while non-authorized roles are denied by canAccess()."
    - "The operations route is visible in MarkOS nav and links to the task execution surface."
    - "Task route shell renders without runtime error from the existing (markos) layout."
  artifacts:
    - path: "app/(markos)/operations/page.tsx"
      provides: "operations route shell and handoff to tasks surface"
      exports: []
    - path: "app/(markos)/layout.tsx"
      provides: "operations nav wiring"
      exports: []
    - path: "lib/markos/rbac/policies.ts"
      provides: "operations route permission contract"
      exports: ["RouteKey", "canAccess"]
  key_links:
    - from: "app/(markos)/layout.tsx"
      to: "lib/markos/rbac/policies.ts"
      via: "NAV_ITEMS.route -> canAccess(ACTIVE_ROLE, route)"
      pattern: "operations"
    - from: "app/(markos)/operations/page.tsx"
      to: "app/(markos)/operations/tasks/page.tsx"
      via: "route handoff link"
      pattern: "/markos/operations/tasks"
---

<objective>
Create the Phase 46 route shell and access wiring for the operator task graph surface.

Purpose: Establish a guarded, navigable operations entrypoint before state/store and UI execution work.
Output: Operations route shell, nav integration, and RBAC contract update for owner/operator-only access.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/46-operator-task-graph-ui-mvp/46-CONTEXT.md
@.planning/phases/46-operator-task-graph-ui-mvp/46-RESEARCH.md
@app/(markos)/layout.tsx
@lib/markos/rbac/policies.ts

<interfaces>
From lib/markos/rbac/policies.ts:
```ts
export type MarkOSRole = "owner" | "operator" | "strategist" | "viewer" | "agent";
export type RouteKey =
  | "dashboard"
  | "company"
  | "mir"
  | "msp"
  | "icps"
  | "segments"
  | "campaigns"
  | "settings";

export function canAccess(role: MarkOSRole, route: string): boolean;
```

From app/(markos)/layout.tsx:
```ts
const NAV_ITEMS = [
  { href: "/markos", label: "Dashboard", route: "dashboard" },
  ...
] as const;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 46-01-01: Add operations route guard and nav entry (per D-12, D-13)</name>
  <read_first>
    lib/markos/rbac/policies.ts
    app/(markos)/layout.tsx
    .planning/phases/46-operator-task-graph-ui-mvp/46-CONTEXT.md
  </read_first>
  <files>lib/markos/rbac/policies.ts, app/(markos)/layout.tsx</files>
  <action>
Update RouteKey in lib/markos/rbac/policies.ts by adding "operations" and extend routePermissions with operations: ["owner", "operator"] exactly (no strategist/viewer/agent per D-12). Add NAV_ITEMS entry in app/(markos)/layout.tsx as { href: "/markos/operations", label: "Operations", route: "operations" } so the route is surfaced through existing canAccess() filtering.
  </action>
  <acceptance_criteria>
    - RouteKey includes "operations" and canAccess("owner", "operations") returns true.
    - canAccess("viewer", "operations") and canAccess("agent", "operations") return false.
    - MarkOS nav includes Operations item with href /markos/operations.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const p='lib/markos/rbac/policies.ts';const l='app/(markos)/layout.tsx';const a=fs.readFileSync(p,'utf8');const b=fs.readFileSync(l,'utf8');if(!a.includes('"operations"')||!a.includes('operations: ["owner", "operator"]')||!b.includes('/markos/operations'))process.exit(1);"</automated>
  </verify>
  <done>
    Operations route authorization and navigation are wired to the existing RBAC contract.
  </done>
</task>

<task type="auto">
  <name>Task 46-01-02: Create operations route shell and tasks handoff (per D-01, D-02)</name>
  <read_first>
    app/(markos)/page.tsx
    .planning/phases/46-operator-task-graph-ui-mvp/46-UI-SPEC.md
  </read_first>
  <files>app/(markos)/operations/page.tsx</files>
  <action>
Create app/(markos)/operations/page.tsx as a thin route shell that renders an "Operations" heading, short explanatory copy for sequential task execution, and a direct handoff link to /markos/operations/tasks. Keep this route stateless and UI-only (no graph library, no state machine code in this file).
  </action>
  <acceptance_criteria>
    - /markos/operations route file exists and exports a default React page component.
    - Page includes a link/anchor to /markos/operations/tasks.
    - No external state library import is introduced.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const f='app/(markos)/operations/page.tsx';const t=fs.readFileSync(f,'utf8');if(!t.includes('export default')||!t.includes('/markos/operations/tasks'))process.exit(1);"</automated>
  </verify>
  <done>
    Operations route shell is available as the parent entrypoint for the Phase 46 task graph UI.
  </done>
</task>

</tasks>

<verification>
Run the string-contract check command and confirm operations route shell compiles with existing app/(markos) scaffold.
</verification>

<success_criteria>
Operator-accessible route shell and RBAC/nav wiring are in place for the Phase 46 execution surface.
</success_criteria>

<output>
After completion, create `.planning/phases/46-operator-task-graph-ui-mvp/46-01-SUMMARY.md`
</output>
