---
phase: 46-operator-task-graph-ui-mvp
plan: 07
type: execute
wave: 4
depends_on:
  - 46-03
  - 46-04
  - 46-05
  - 46-06
files_modified:
  - app/(markos)/operations/tasks/tasks.stories.tsx
  - app/(markos)/operations/tasks/story-fixtures.ts
  - .storybook/preview.ts
autonomous: true
requirements:
  - OPS-01
  - OPS-03
  - OPS-04
  - OPS-05

must_haves:
  truths:
    - "Storybook exposes five required task state stories: Queued, Approved, Executing, Completed, Failed."
    - "Stories demonstrate approval gate, retry, and evidence drawer behavior states using typed fixtures."
    - "Operations task stories render from the same reducer/store contracts used by route UI."
  artifacts:
    - path: "app/(markos)/operations/tasks/tasks.stories.tsx"
      provides: "Phase 46 required story set"
      exports: []
    - path: "app/(markos)/operations/tasks/story-fixtures.ts"
      provides: "storybook-specific deterministic task states"
      exports: ["queuedStoryTask", "approvedStoryTask", "executingStoryTask", "completedStoryTask", "failedStoryTask"]
  key_links:
    - from: "app/(markos)/operations/tasks/tasks.stories.tsx"
      to: "app/(markos)/operations/tasks/task-store.tsx"
      via: "provider-backed state rendering"
      pattern: "TaskStoreProvider"
---

<objective>
Create the required Storybook state coverage for the Phase 46 operator task surface.

Purpose: Make all MVP task states and critical interactions reviewable and regression-visible before broader hardening phases.
Output: Five required state stories plus deterministic story fixtures for task UI components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/46-operator-task-graph-ui-mvp/46-CONTEXT.md
@.planning/phases/46-operator-task-graph-ui-mvp/46-UI-SPEC.md
@app/(markos)/operations/tasks/page.tsx
@app/(markos)/operations/tasks/task-fixtures.ts
@.storybook/preview.ts

<interfaces>
Required Phase 46 story names:
```text
Queued
Approved
Executing
Completed
Failed
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 46-07-01: Build deterministic story fixtures for five task states (per D-16)</name>
  <read_first>
    app/(markos)/operations/tasks/task-fixtures.ts
    app/(markos)/operations/tasks/task-types.ts
    .planning/phases/46-operator-task-graph-ui-mvp/46-UI-SPEC.md
  </read_first>
  <files>app/(markos)/operations/tasks/story-fixtures.ts</files>
  <action>
Create app/(markos)/operations/tasks/story-fixtures.ts exporting deterministic fixtures named queuedStoryTask, approvedStoryTask, executingStoryTask, completedStoryTask, failedStoryTask, each typed as TaskExecutionRecord and containing realistic approval/evidence/retry fields consistent with task-types.ts.
  </action>
  <acceptance_criteria>
    - story-fixtures.ts exports exactly five named fixtures for required state stories.
    - Fixture states align to locked enum values.
    - Failed fixture includes retry metadata and evidence payload.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('app/(markos)/operations/tasks/story-fixtures.ts','utf8');const s=['queuedStoryTask','approvedStoryTask','executingStoryTask','completedStoryTask','failedStoryTask'];if(!s.every(x=>t.includes(x)))process.exit(1);"</automated>
  </verify>
  <done>
    Deterministic story fixtures are available for all required task UI states.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 46-07-02: Add Storybook stories for queued/approved/executing/completed/failed states (per D-16, D-17)</name>
  <read_first>
    app/(markos)/operations/tasks/story-fixtures.ts
    app/(markos)/operations/tasks/page.tsx
    .storybook/preview.ts
  </read_first>
  <files>app/(markos)/operations/tasks/tasks.stories.tsx, .storybook/preview.ts</files>
  <behavior>
    - Test 1: Story exports include Queued, Approved, Executing, Completed, Failed.
    - Test 2: Each story renders task shell with matching state chip and evidence context.
    - Test 3: Failed story exposes retry UI and approval-gated stories expose approval status context.
  </behavior>
  <action>
Create app/(markos)/operations/tasks/tasks.stories.tsx with one story per required state, using TaskStoreProvider with per-story fixture injection. Include story descriptions that call out approval blocking, retry attempt visibility, and evidence drawer behavior. Update .storybook/preview.ts only if needed to add deterministic decorators supporting operations task stories; do not alter unrelated route story behavior.
  </action>
  <acceptance_criteria>
    - tasks.stories.tsx exports the five required story names exactly.
    - Stories are typed and use deterministic fixtures from story-fixtures.ts.
    - Build pipeline can include these stories without adding new Storybook dependencies.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('app/(markos)/operations/tasks/tasks.stories.tsx','utf8');const n=['Queued','Approved','Executing','Completed','Failed'];if(!n.every(x=>t.includes('export const '+x)))process.exit(1);"</automated>
  </verify>
  <done>
    Storybook coverage for all five required task states is implemented and ready for CI visual review.
  </done>
</task>

</tasks>

<verification>
Run story export checks and optionally run `npm run build-storybook` for full Storybook compile validation.
</verification>

<success_criteria>
Required five-state Storybook surface is live and maps directly to Phase 46 task state contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/46-operator-task-graph-ui-mvp/46-07-SUMMARY.md`
</output>
