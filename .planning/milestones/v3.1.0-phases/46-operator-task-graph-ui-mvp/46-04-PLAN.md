---
phase: 46-operator-task-graph-ui-mvp
plan: 04
type: execute
wave: 3
depends_on:
  - 46-02
  - 46-03
files_modified:
  - app/(markos)/operations/tasks/approval-gate.tsx
  - app/(markos)/operations/tasks/step-runner.tsx
  - app/(markos)/operations/tasks/task-machine.ts
  - app/(markos)/operations/tasks/task-types.ts
autonomous: true
requirements:
  - OPS-03
  - OPS-04

must_haves:
  truths:
    - "Approval-gated steps cannot execute until an explicit approve/reject decision is recorded."
    - "Rejected decisions are logged with optional rejection reason while step state remains queued."
    - "Failed steps can be retried with input preview/edit and each retry is logged as a separate attempt."
  artifacts:
    - path: "app/(markos)/operations/tasks/approval-gate.tsx"
      provides: "blocking approval modal"
      exports: ["ApprovalGate"]
    - path: "app/(markos)/operations/tasks/task-machine.ts"
      provides: "approval and retry transition logic"
      exports: ["taskReducer"]
  key_links:
    - from: "app/(markos)/operations/tasks/step-runner.tsx"
      to: "app/(markos)/operations/tasks/approval-gate.tsx"
      via: "requires_approval gate before START_STEP"
      pattern: "requires_approval"
    - from: "app/(markos)/operations/tasks/task-machine.ts"
      to: "app/(markos)/operations/tasks/task-types.ts"
      via: "TaskStepRetryAttempt append-only list"
      pattern: "retry_attempts"
---

<objective>
Implement the approval checkpoint and retry flows required for gated and failed task steps.

Purpose: Satisfy blocking human decision behavior and failure recovery behavior in the MVP state machine.
Output: Approval modal component, reducer gating logic, and retry input-mutation path with separate logging.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/46-operator-task-graph-ui-mvp/46-CONTEXT.md
@.planning/phases/46-operator-task-graph-ui-mvp/46-UI-SPEC.md
@app/(markos)/operations/tasks/task-types.ts
@app/(markos)/operations/tasks/task-machine.ts
@app/(markos)/operations/tasks/step-runner.tsx

<interfaces>
Locked approval UX and retry decisions:
```text
blocking modal, dismissible only by Approve/Reject; rejection reason optional
retry attempts logged separately; retry may include edited input snapshot
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 46-04-01: Add blocking approval gate modal and decision capture (per D-07, D-08)</name>
  <read_first>
    .planning/phases/46-operator-task-graph-ui-mvp/46-CONTEXT.md
    app/(markos)/operations/tasks/step-runner.tsx
    app/(markos)/operations/tasks/task-machine.ts
  </read_first>
  <files>app/(markos)/operations/tasks/approval-gate.tsx, app/(markos)/operations/tasks/step-runner.tsx, app/(markos)/operations/tasks/task-machine.ts</files>
  <behavior>
    - Test 1: Running a requires_approval step opens ApprovalGate before START_STEP dispatch.
    - Test 2: Approve dispatch records actor_id, decided_at, decision value and allows execution.
    - Test 3: Reject dispatch records optional rejection_reason and keeps step state queued.
  </behavior>
  <action>
Create app/(markos)/operations/tasks/approval-gate.tsx implementing a blocking modal with two explicit actions only: APPROVE and REJECT. Add optional rejection reason field bound to local state. In step-runner.tsx, intercept Execute Task Step click for requires_approval steps so approval must be completed first. Extend reducer branches APPROVE_STEP and REJECT_STEP in task-machine.ts to persist approval.status, approval.decided_at, approval.decided_by, approval.rejection_reason and append event records.
  </action>
  <acceptance_criteria>
    - ApprovalGate modal cannot be dismissed by passive close path.
    - APPROVE_STEP and REJECT_STEP both persist timestamp + actor metadata.
    - Rejected step remains queued and execution controls stay blocked.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const m=fs.readFileSync('app/(markos)/operations/tasks/task-machine.ts','utf8');const g=fs.readFileSync('app/(markos)/operations/tasks/approval-gate.tsx','utf8');if(!m.includes('APPROVE_STEP')||!m.includes('REJECT_STEP')||!m.includes('rejection_reason')||!g.includes('Approve')||!g.includes('Reject'))process.exit(1);"</automated>
  </verify>
  <done>
    Approval gate is fully blocking and persists decision metadata before execution can continue.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 46-04-02: Implement failed-step retry with input preview/edit and separate attempt logs (per D-17)</name>
  <read_first>
    app/(markos)/operations/tasks/task-types.ts
    app/(markos)/operations/tasks/task-machine.ts
    app/(markos)/operations/tasks/step-runner.tsx
  </read_first>
  <files>app/(markos)/operations/tasks/task-types.ts, app/(markos)/operations/tasks/task-machine.ts, app/(markos)/operations/tasks/step-runner.tsx</files>
  <behavior>
    - Test 1: RETRY_STEP appends a new TaskStepRetryAttempt entry (never overwrites prior attempts).
    - Test 2: Retry UI shows editable input snapshot seeded from last failed inputs.
    - Test 3: If inputs change on approval-gated step, approval resets to pending.
  </behavior>
  <action>
Extend TaskStepRetryAttempt contract in task-types.ts with attempt, requested_at, requested_by, input_snapshot, previous_error, outcome_state. In step-runner.tsx add retry panel visible only for failed state, prefilled with last evidence.inputs and editable JSON/text representation. In task-machine.ts RETRY_STEP branch, increment retry_count, push new retry_attempts record, store input_snapshot, and set step state to queued (or approved for non-gated path only). When requires_approval and input snapshot differs, reset approval.status to pending.
  </action>
  <acceptance_criteria>
    - Retry controls appear only for failed steps.
    - Each retry adds a distinct retry_attempts entry with timestamp and actor.
    - Gated steps require fresh approval when retry inputs are changed.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const m=fs.readFileSync('app/(markos)/operations/tasks/task-machine.ts','utf8');const r=fs.readFileSync('app/(markos)/operations/tasks/step-runner.tsx','utf8');if(!m.includes('RETRY_STEP')||!m.includes('retry_attempts')||!r.includes('Retry')||!r.includes('input'))process.exit(1);"</automated>
  </verify>
  <done>
    Failed-step retry flow supports optional input mutation and append-only retry audit records.
  </done>
</task>

</tasks>

<verification>
Run reducer and component contract checks and confirm approval/retry actions are represented in task-machine branches.
</verification>

<success_criteria>
Approval checkpoints gate execution correctly and failed steps can be retried with auditable attempt history.
</success_criteria>

<output>
After completion, create `.planning/phases/46-operator-task-graph-ui-mvp/46-04-SUMMARY.md`
</output>
