---
phase: 46-operator-task-graph-ui-mvp
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(markos)/operations/tasks/task-types.ts
  - app/(markos)/operations/tasks/task-fixtures.ts
  - app/(markos)/operations/tasks/task-machine.ts
  - app/(markos)/operations/tasks/task-store.tsx
autonomous: true
requirements:
  - OPS-02

must_haves:
  truths:
    - "Task step transitions use a single reducer and explicit enum states queued|approved|executing|completed|failed."
    - "In-memory store holds typed task fixtures and append-only event records shaped for future persistence."
    - "Sequential execution is enforced so only the active step can transition."
  artifacts:
    - path: "app/(markos)/operations/tasks/task-types.ts"
      provides: "TaskStepState enum and typed contracts for steps, approval, retry, evidence, and task events"
      exports: ["TaskStepState", "TaskExecutionRecord", "TaskEventRecord"]
    - path: "app/(markos)/operations/tasks/task-machine.ts"
      provides: "useReducer-compatible transition reducer and action union"
      exports: ["taskReducer", "TaskActionType"]
    - path: "app/(markos)/operations/tasks/task-store.tsx"
      provides: "in-memory context provider + selectors"
      exports: ["TaskStoreProvider", "useTaskStore"]
  key_links:
    - from: "app/(markos)/operations/tasks/task-store.tsx"
      to: "app/(markos)/operations/tasks/task-machine.ts"
      via: "useReducer(taskReducer, initialState)"
      pattern: "taskReducer"
    - from: "app/(markos)/operations/tasks/task-machine.ts"
      to: "app/(markos)/operations/tasks/task-types.ts"
      via: "TaskStepState and event/action contracts"
      pattern: "queued|approved|executing|completed|failed"
---

<objective>
Implement the core reducer/store architecture and typed in-memory contracts for Phase 46 tasks.

Purpose: Build the deterministic state foundation that all task graph UI, approval, retry, and evidence features consume.
Output: Typed contracts, fixtures, reducer transitions, and React context store with append-only event log.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/46-operator-task-graph-ui-mvp/46-CONTEXT.md
@.planning/phases/46-operator-task-graph-ui-mvp/46-RESEARCH.md
@.planning/FLOW-CONTRACTS.md
@contracts/flow-registry.json

<interfaces>
Locked state enum from Phase 46 decisions:
```ts
type TaskStepState = "queued" | "approved" | "executing" | "completed" | "failed";
```

Locked telemetry-relevant events from decisions:
```ts
"step_executed" | "step_approved" | "step_rejected" | "step_retried"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 46-02-01: Define task contracts and fixture schema (per D-03, D-05, D-06, D-11)</name>
  <read_first>
    .planning/phases/46-operator-task-graph-ui-mvp/46-CONTEXT.md
    .planning/phases/46-operator-task-graph-ui-mvp/46-RESEARCH.md
    contracts/flow-registry.json
  </read_first>
  <files>app/(markos)/operations/tasks/task-types.ts, app/(markos)/operations/tasks/task-fixtures.ts</files>
  <behavior>
    - Test 1: TaskStepState enum includes exactly queued, approved, executing, completed, failed.
    - Test 2: TaskStepEvidence type includes inputs, outputs, logs, step_started_at, step_completed_at, actor_id.
    - Test 3: Fixtures include at least one step for each of the five states.
  </behavior>
  <action>
Create app/(markos)/operations/tasks/task-types.ts exporting TaskStepState enum and typed records: TaskApprovalRecord, TaskStepEvidence, TaskStepRetryAttempt, TaskStepRecord, TaskExecutionRecord, TaskEventRecord. Ensure TaskStepEvidence uses Readonly and includes required keys from D-11. Create app/(markos)/operations/tasks/task-fixtures.ts with in-memory typed fixtures seeded from contracts/flow-registry.json metadata (flow_id, flow_name, domain/type labels) and include representative task data that covers all 5 states for Storybook/test scenarios.
  </action>
  <acceptance_criteria>
    - task-types.ts exports TaskStepState enum and all required typed records.
    - task-fixtures.ts exports typed fixture arrays/maps without any `any`.
    - Fixtures include at least one evidence payload with non-empty logs and timestamps.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const a=fs.readFileSync('app/(markos)/operations/tasks/task-types.ts','utf8');const b=fs.readFileSync('app/(markos)/operations/tasks/task-fixtures.ts','utf8');const s=['queued','approved','executing','completed','failed'];if(!s.every(x=>a.includes(x))||!a.includes('TaskStepEvidence')||!b.includes('flow_id'))process.exit(1);"</automated>
  </verify>
  <done>
    Phase 46 has a persistence-ready type contract and fixture set aligned with locked enum/state decisions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 46-02-02: Build reducer and in-memory task store (per D-02, D-03, D-04, D-05)</name>
  <read_first>
    app/(markos)/operations/tasks/task-types.ts
    app/(markos)/operations/tasks/task-fixtures.ts
    .planning/phases/46-operator-task-graph-ui-mvp/46-RESEARCH.md
  </read_first>
  <files>app/(markos)/operations/tasks/task-machine.ts, app/(markos)/operations/tasks/task-store.tsx</files>
  <behavior>
    - Test 1: Reducer only allows sequential current-step transitions.
    - Test 2: Each transition appends a timestamped TaskEventRecord.
    - Test 3: Store exposes selectors for selected task, actionable step, and selected evidence step.
  </behavior>
  <action>
Create app/(markos)/operations/tasks/task-machine.ts with reducer/action union for START_STEP, COMPLETE_STEP, FAIL_STEP, APPROVE_STEP, REJECT_STEP, RETRY_STEP, SELECT_TASK, SELECT_EVIDENCE_STEP. Enforce sequential progression by validating order_index/current_step_id before mutating state. Append TaskEventRecord objects with occurred_at timestamp in every mutating branch. Create app/(markos)/operations/tasks/task-store.tsx exporting TaskStoreProvider and useTaskStore hook; initialize state from task-fixtures and wire useReducer(taskReducer, initialState).
  </action>
  <acceptance_criteria>
    - Reducer branch names include START_STEP, COMPLETE_STEP, FAIL_STEP, APPROVE_STEP, REJECT_STEP, RETRY_STEP.
    - Non-sequential transition attempts are ignored or return unchanged state.
    - Store composes reducer and provides derived selector values for UI components.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const m=fs.readFileSync('app/(markos)/operations/tasks/task-machine.ts','utf8');const s=fs.readFileSync('app/(markos)/operations/tasks/task-store.tsx','utf8');const req=['START_STEP','COMPLETE_STEP','FAIL_STEP','APPROVE_STEP','REJECT_STEP','RETRY_STEP'];if(!req.every(x=>m.includes(x))||!s.includes('useReducer')||!s.includes('TaskStoreProvider'))process.exit(1);"</automated>
  </verify>
  <done>
    Reducer-driven sequential state machine and context store are ready for task graph UI composition.
  </done>
</task>

</tasks>

<verification>
Run file contract checks and confirm reducer/store compile under the existing TypeScript app structure.
</verification>

<success_criteria>
Atomic reducer-based step state transitions exist with typed fixtures and append-only in-memory event logging.
</success_criteria>

<output>
After completion, create `.planning/phases/46-operator-task-graph-ui-mvp/46-02-SUMMARY.md`
</output>
