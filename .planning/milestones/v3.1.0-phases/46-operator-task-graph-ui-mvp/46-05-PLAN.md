---
phase: 46-operator-task-graph-ui-mvp
plan: 05
type: execute
wave: 3
depends_on:
  - 46-02
  - 46-03
files_modified:
  - app/(markos)/operations/tasks/evidence-panel.tsx
  - app/(markos)/operations/tasks/task-graph.tsx
  - app/(markos)/operations/tasks/page.tsx
  - app/(markos)/operations/tasks/task-types.ts
autonomous: true
requirements:
  - OPS-05

must_haves:
  truths:
    - "Selecting a step opens a right-sidebar evidence drawer without route change."
    - "Evidence drawer displays inputs, outputs, logs, timestamps, and actor ID in fixed order."
    - "Evidence content is read-only in UI and marked immutable for MVP."
  artifacts:
    - path: "app/(markos)/operations/tasks/evidence-panel.tsx"
      provides: "immutable evidence drawer component"
      exports: ["EvidencePanel"]
    - path: "app/(markos)/operations/tasks/task-types.ts"
      provides: "readonly evidence type contract"
      exports: ["TaskStepEvidence"]
  key_links:
    - from: "app/(markos)/operations/tasks/task-graph.tsx"
      to: "app/(markos)/operations/tasks/evidence-panel.tsx"
      via: "step click -> SELECT_EVIDENCE_STEP"
      pattern: "SELECT_EVIDENCE_STEP"
---

<objective>
Deliver the evidence drawer and UI-level immutability contract for executed task steps.

Purpose: Provide a clear audit trail surface that satisfies evidence visibility and immutability constraints in MVP scope.
Output: Right-sidebar evidence drawer wired to step selection with fixed evidence schema rendering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/46-operator-task-graph-ui-mvp/46-CONTEXT.md
@.planning/phases/46-operator-task-graph-ui-mvp/46-UI-SPEC.md
@app/(markos)/operations/tasks/task-types.ts
@app/(markos)/operations/tasks/task-store.tsx
@app/(markos)/operations/tasks/task-graph.tsx

<interfaces>
Locked evidence fields from D-11:
```ts
inputs, outputs, logs, step_started_at, step_completed_at, actor_id
```

Locked evidence UX from D-09/D-10:
```text
right sidebar drawer, read-only evidence fields, immutable marker
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 46-05-01: Implement EvidencePanel drawer with fixed field order (per D-09, D-11)</name>
  <read_first>
    .planning/phases/46-operator-task-graph-ui-mvp/46-UI-SPEC.md
    app/(markos)/operations/tasks/task-types.ts
    app/(markos)/operations/tasks/page.tsx
  </read_first>
  <files>app/(markos)/operations/tasks/evidence-panel.tsx, app/(markos)/operations/tasks/page.tsx</files>
  <action>
Create app/(markos)/operations/tasks/evidence-panel.tsx as right sidebar drawer component rendering sections in exact order: Inputs, Outputs, Logs, Timestamps, Actor ID. Timestamps section must include step_started_at and step_completed_at labels explicitly. Wire EvidencePanel into app/(markos)/operations/tasks/page.tsx right region using selectedEvidenceStepId/selected step selectors from store.
  </action>
  <acceptance_criteria>
    - EvidencePanel exports a component and is mounted in tasks page right sidebar region.
    - Drawer contains all required sections with explicit field labels.
    - Selecting no step renders a deterministic empty evidence state message.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const e=fs.readFileSync('app/(markos)/operations/tasks/evidence-panel.tsx','utf8');if(!['Inputs','Outputs','Logs','step_started_at','step_completed_at','Actor ID'].every(x=>e.includes(x)))process.exit(1);"</automated>
  </verify>
  <done>
    Evidence drawer shows structured audit data for selected steps using locked evidence schema.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 46-05-02: Enforce UI-only evidence immutability and step-selection wiring (per D-10)</name>
  <read_first>
    app/(markos)/operations/tasks/task-graph.tsx
    app/(markos)/operations/tasks/task-store.tsx
    app/(markos)/operations/tasks/task-types.ts
  </read_first>
  <files>app/(markos)/operations/tasks/task-types.ts, app/(markos)/operations/tasks/task-graph.tsx, app/(markos)/operations/tasks/evidence-panel.tsx</files>
  <behavior>
    - Test 1: TaskStepEvidence fields are typed as readonly.
    - Test 2: TaskGraph step click dispatches SELECT_EVIDENCE_STEP and opens drawer context.
    - Test 3: EvidencePanel includes immutable marker copy and no editable controls.
  </behavior>
  <action>
Update TaskStepEvidence in task-types.ts to use readonly structure and readonly logs array type. In task-graph.tsx, add click handler dispatching SELECT_EVIDENCE_STEP with step_id for each step card. In evidence-panel.tsx render immutable marker text (for example: "Immutable evidence snapshot (MVP UI-only)") and avoid input/textarea/select controls for evidence values.
  </action>
  <acceptance_criteria>
    - Evidence type contract is readonly at compile-time.
    - Step click selection path is present in task-graph.tsx.
    - EvidencePanel contains immutable marker text and no editable controls.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('app/(markos)/operations/tasks/task-types.ts','utf8');const g=fs.readFileSync('app/(markos)/operations/tasks/task-graph.tsx','utf8');const e=fs.readFileSync('app/(markos)/operations/tasks/evidence-panel.tsx','utf8');if(!t.toLowerCase().includes('readonly')||!g.includes('SELECT_EVIDENCE_STEP')||!e.includes('Immutable evidence'))process.exit(1);"</automated>
  </verify>
  <done>
    Evidence view is immutable in UI and wired to step selection without introducing backend persistence scope.
  </done>
</task>

</tasks>

<verification>
Run evidence contract checks and confirm right-sidebar drawer rendering path exists in tasks page composition.
</verification>

<success_criteria>
Evidence panel is accessible from step selections and provides read-only audit payloads aligned with OPS-05.
</success_criteria>

<output>
After completion, create `.planning/phases/46-operator-task-graph-ui-mvp/46-05-SUMMARY.md`
</output>
