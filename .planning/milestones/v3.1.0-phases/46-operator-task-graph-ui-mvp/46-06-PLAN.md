---
phase: 46-operator-task-graph-ui-mvp
plan: 06
type: execute
wave: 3
depends_on:
  - 46-02
files_modified:
  - lib/markos/telemetry/events.ts
  - app/(markos)/operations/tasks/task-machine.ts
  - app/(markos)/operations/tasks/task-store.tsx
autonomous: true
requirements:
  - OPS-02
  - OPS-05

must_haves:
  truths:
    - "Step execution, approval, rejection, and retry transitions emit typed telemetry events through buildEvent()."
    - "Event payloads are sanitized with sanitizePayload before being exposed for transport."
    - "Transition events include timestamped metadata aligned with reducer atomic updates."
  artifacts:
    - path: "lib/markos/telemetry/events.ts"
      provides: "expanded telemetry event name union for Phase 46"
      exports: ["MarkOSTelemetryEvent", "sanitizePayload", "buildEvent"]
    - path: "app/(markos)/operations/tasks/task-machine.ts"
      provides: "event emission hooks per transition branch"
      exports: ["taskReducer"]
  key_links:
    - from: "app/(markos)/operations/tasks/task-machine.ts"
      to: "lib/markos/telemetry/events.ts"
      via: "buildEvent(sanitizePayload(payload))"
      pattern: "markos_task_step_executed|markos_task_step_approved|markos_task_step_rejected|markos_task_step_retried"
---

<objective>
Integrate Phase 46 telemetry contracts into task state transitions.

Purpose: Ensure all critical task transition events are captured with sanitized payloads and timestamp metadata.
Output: Telemetry event union expansion and reducer/store integration using buildEvent/sanitizePayload.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/46-operator-task-graph-ui-mvp/46-CONTEXT.md
@lib/markos/telemetry/events.ts
@app/(markos)/operations/tasks/task-machine.ts
@app/(markos)/operations/tasks/task-types.ts

<interfaces>
From lib/markos/telemetry/events.ts:
```ts
export function sanitizePayload(payload: Record<string, unknown>): Record<string, unknown>;
export function buildEvent(event: MarkOSTelemetryEvent): MarkOSTelemetryEvent;
```

Locked Phase 46 telemetry names (D-14):
```text
markos_task_step_executed
markos_task_step_approved
markos_task_step_rejected
markos_task_step_retried
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 46-06-01: Extend telemetry event contract for task transitions (per D-14)</name>
  <read_first>
    lib/markos/telemetry/events.ts
    .planning/phases/46-operator-task-graph-ui-mvp/46-CONTEXT.md
  </read_first>
  <files>lib/markos/telemetry/events.ts</files>
  <action>
Extend MarkOSTelemetryEvent.name union in lib/markos/telemetry/events.ts to include markos_task_step_executed, markos_task_step_approved, markos_task_step_rejected, and markos_task_step_retried exactly. Preserve existing buildEvent() and sanitizePayload() behavior; do not add raw emission APIs.
  </action>
  <acceptance_criteria>
    - Event name union includes all four Phase 46 telemetry names.
    - buildEvent and sanitizePayload exports remain intact and unchanged in signature.
    - No new unsanitized telemetry helper is introduced.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('lib/markos/telemetry/events.ts','utf8');const e=['markos_task_step_executed','markos_task_step_approved','markos_task_step_rejected','markos_task_step_retried'];if(!e.every(x=>t.includes(x))||!t.includes('buildEvent')||!t.includes('sanitizePayload'))process.exit(1);"</automated>
  </verify>
  <done>
    Telemetry type contract supports all required Phase 46 task transition events.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 46-06-02: Emit sanitized transition telemetry from reducer/store actions (per D-14, D-15)</name>
  <read_first>
    app/(markos)/operations/tasks/task-machine.ts
    app/(markos)/operations/tasks/task-store.tsx
    lib/markos/telemetry/events.ts
  </read_first>
  <files>app/(markos)/operations/tasks/task-machine.ts, app/(markos)/operations/tasks/task-store.tsx</files>
  <behavior>
    - Test 1: APPROVE_STEP emits markos_task_step_approved via buildEvent().
    - Test 2: REJECT_STEP emits markos_task_step_rejected via buildEvent().
    - Test 3: START/COMPLETE paths emit markos_task_step_executed and RETRY_STEP emits markos_task_step_retried.
    - Test 4: Payload path always passes through sanitizePayload.
  </behavior>
  <action>
Add transition-level telemetry event construction inside task-machine.ts helper branches (or store wrapper dispatch helpers) for APPROVE_STEP, REJECT_STEP, START_STEP/COMPLETE_STEP, and RETRY_STEP. Build payload with task_id, step_id, actor_id, previous_state, next_state, occurred_at, and evidence summary fields. Ensure payload goes through buildEvent() (which sanitizes via sanitizePayload()) before being added to in-memory event queue/exposed callbacks. Apply D-15 by excluding raw sensitive input keys in emitted payload fields.
  </action>
  <acceptance_criteria>
    - Reducer/store path constructs all four event names via buildEvent().
    - Event payload includes step_id and occurred_at for each transition event.
    - No event branch bypasses sanitizePayload.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const m=fs.readFileSync('app/(markos)/operations/tasks/task-machine.ts','utf8');if(!m.includes('buildEvent')||!m.includes('markos_task_step_executed')||!m.includes('markos_task_step_approved')||!m.includes('markos_task_step_rejected')||!m.includes('markos_task_step_retried'))process.exit(1);"</automated>
  </verify>
  <done>
    Task transition telemetry is typed, sanitized, and aligned with atomic reducer transitions.
  </done>
</task>

</tasks>

<verification>
Run telemetry contract checks and confirm no direct raw payload emission path was added.
</verification>

<success_criteria>
Phase 46 transition telemetry is complete and compliant with sanitizePayload/buildEvent policy.
</success_criteria>

<output>
After completion, create `.planning/phases/46-operator-task-graph-ui-mvp/46-06-SUMMARY.md`
</output>
