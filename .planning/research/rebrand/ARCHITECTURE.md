# Architecture Patterns — Rebrand Impact Map

**Domain:** NPM package, distribution, public-facing rebrand  
**Researched:** 2026-03-27

## Rebrand Impact Architecture

### Component Boundaries & MGSD Reference Density

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        REBRAND IMPACT TOPOLOGY                              │
└─────────────────────────────────────────────────────────────────────────────┘

  LAYER 1: PACKAGE IDENTITY (BREAKING)
  ─────────────────────────────────────
  package.json          → name, description, bin, keywords, homepage, files, scripts
  bin/install.cjs       → banner text, detection functions, path constants
  bin/update.cjs        → banner text, path constants, manifest path

  LAYER 2: FILESYSTEM PATHS (MIGRATION REQUIRED)
  ───────────────────────────────────────────────
  .agent/marketing-get-shit-done/   → 317+ files  (rename directory)
  .mgsd-local/                      → user data   (rename + migrate)
  .mgsd-project.json                → project slug (rename)
  .mgsd-install-manifest.json       → install state (rename)
  onboarding-seed.json              → no MGSD ref (OK as-is)

  LAYER 3: RUNTIME CODE REFERENCES
  ─────────────────────────────────
  onboarding/backend/server.cjs     → 7 MGSD refs  (paths, comments, defaults)
  onboarding/backend/handlers.cjs   → 6 MGSD refs  (paths, slug defaults)
  onboarding/backend/write-mir.cjs  → 9 MGSD refs  (paths, "Generated by MGSD AI")
  onboarding/backend/chroma-client.cjs → 9 MGSD refs (collection prefix mgsd-)
  onboarding/backend/agents/telemetry.cjs → 2 refs (MGSD_TELEMETRY, $lib)
  onboarding/backend/agents/orchestrator.cjs → 2 refs (comments, collection)
  onboarding/backend/agents/mir-filler.cjs → 4 refs (path comments)
  onboarding/backend/agents/msp-filler.cjs → 3 refs (path comments)
  onboarding/backend/agents/llm-adapter.cjs → 1 ref (comment)
  onboarding/backend/agents/example-resolver.cjs → 2 refs (template paths)
  onboarding/onboarding.js          → 2 refs (STORAGE_KEY, comment)

  LAYER 4: DOCUMENTATION (COSMETIC)
  ──────────────────────────────────
  README.md             → ~15 MGSD refs (install commands, paths, descriptions)
  CHANGELOG.md          → 5 MGSD refs  (title, version notes)
  ARCH-DIAGRAM.md       → title + topology label "MGSD SYSTEM TOPOLOGY"
  TECH-MAP.md           → ~40+ MGSD refs throughout
  CLAUDE.md             → references .protocol-lore (indirect, no direct MGSD)
  GEMINI.md             → references .protocol-lore (indirect, no direct MGSD)
  .protocol-lore/QUICKSTART.md → many MGSD refs (commands, paths, descriptions)
  .protocol-lore/INDEX.md → no direct MGSD in content (just XML tags)

  LAYER 5: ONBOARDING UI (COSMETIC)
  ──────────────────────────────────
  onboarding/index.html → no direct MGSD (title says "Client Intelligence Onboarding")
  onboarding/onboarding.css → no MGSD classes (uses generic names)
  onboarding/onboarding-config.json → project_slug: "mgsd-client"
  onboarding/onboarding-seed.schema.json → title: "MGSD Onboarding Seed v2.1"

  LAYER 6: EXTERNAL PLATFORMS
  ────────────────────────────
  GitHub repo: currently `mgsd`
  npm registry: `marketing-get-shit-done`
  .gitignore: comments say "MGSD gitignore", paths say ".mgsd-local/"
```

### Data Flow Through Renamed Components

```
npx markos install
  → bin/install.cjs reads VERSION from .agent/markos/VERSION
  → Copies .agent/markos/ → target/.agent/markos/
  → Creates .markos-install-manifest.json
  → Creates .markos-local/ scaffold
  → Writes .markos-project.json

npx markos update
  → Reads .markos-install-manifest.json (falls back to .mgsd-install-manifest.json)
  → Checks .markos-local/ for overrides (falls back to .mgsd-local/)
  → Updates .agent/markos/ (detects old .agent/marketing-get-shit-done/ and migrates)

node onboarding/backend/server.cjs
  → Reads .markos-project.json (falls back to .mgsd-project.json)
  → ChromaDB collections: markos-{slug}-{section}
  → Writes to .markos-local/MIR/
  → Telemetry: $lib = "markos-backend-telemetry"
```

## Patterns to Follow

### Pattern 1: Dual-Path Detection (Backward Compatibility)
**What:** Every file operation that reads/writes MGSD-prefixed paths should check both old and new names  
**When:** During the transition period (at least one major version)  
**Example:**
```javascript
// In install.cjs / update.cjs / handlers.cjs:
const NEW_PATH = '.markos-local';
const LEGACY_PATH = '.mgsd-local';
const localPath = fs.existsSync(NEW_PATH) ? NEW_PATH : 
                  fs.existsSync(LEGACY_PATH) ? LEGACY_PATH : NEW_PATH;
```

### Pattern 2: Auto-Migrate on First Run
**What:** When update.cjs detects old paths, rename them before proceeding  
**When:** First run after upgrade to markos-named version  
**Example:**
```javascript
// Detect and migrate old directory names
if (fs.existsSync('.agent/marketing-get-shit-done') && !fs.existsSync('.agent/markos')) {
  fs.renameSync('.agent/marketing-get-shit-done', '.agent/markos');
  console.log('✓ Migrated .agent/marketing-get-shit-done → .agent/markos');
}
```

### Pattern 3: Bridge Deprecation Package
**What:** Publish final version of `marketing-get-shit-done` that only prints a migration message  
**When:** Same day as `markos` first publish  

## Anti-Patterns to Avoid

### Anti-Pattern 1: Hard Cut Without Migration
**What:** Changing all paths without backward compatibility checks  
**Why bad:** Existing installs break silently — update.cjs can't find manifest, install.cjs can't detect existing install  
**Instead:** Dual-path detection for at least one version

### Anti-Pattern 2: Renaming ChromaDB Collections In-Place
**What:** Trying to rename existing ChromaDB collections  
**Why bad:** ChromaDB has no native "rename collection" operation; requires delete+recreate  
**Instead:** New installs use `markos-` prefix; migration creates new collections and copies data if old ones exist

### Anti-Pattern 3: Mass Find-Replace in Agent Prompts
**What:** Blindly replacing every "MGSD" string in 317 agent files  
**Why bad:** Agent prompt content uses "MGSD" as a token prefix (MGSD-AGT-STR-01); changing these affects the entire token ID system  
**Instead:** Rename file names and directory, but leave internal token IDs for a separate token-system migration phase

## Sources

- Direct audit of all 17 target areas listed in the research brief
