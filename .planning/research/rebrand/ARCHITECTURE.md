# Architecture Patterns â€” Rebrand Impact Map

**Domain:** NPM package, distribution, public-facing rebrand  
**Researched:** 2026-03-27

## Rebrand Impact Architecture

### Component Boundaries & MARKOS Reference Density

```
â”Œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”
â”‚                        REBRAND IMPACT TOPOLOGY                              â”‚
â””â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”˜

  LAYER 1: PACKAGE IDENTITY (BREAKING)
  â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  package.json          â†’ name, description, bin, keywords, homepage, files, scripts
  bin/install.cjs       â†’ banner text, detection functions, path constants
  bin/update.cjs        â†’ banner text, path constants, manifest path

  LAYER 2: FILESYSTEM PATHS (MIGRATION REQUIRED)
  â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  .agent/markos/   â†’ 317+ files  (rename directory)
  .markos-local/                      â†’ user data   (rename + migrate)
  .markos-project.json                â†’ project slug (rename)
  .markos-install-manifest.json       â†’ install state (rename)
  onboarding-seed.json              â†’ no MARKOS ref (OK as-is)

  LAYER 3: RUNTIME CODE REFERENCES
  â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  onboarding/backend/server.cjs     â†’ 7 MARKOS refs  (paths, comments, defaults)
  onboarding/backend/handlers.cjs   â†’ 6 MARKOS refs  (paths, slug defaults)
  onboarding/backend/write-mir.cjs  â†’ 9 MARKOS refs  (paths, "Generated by MARKOS AI")
  onboarding/backend/vector-store-client.cjs â†’ 9 MARKOS refs (collection prefix markos-)
  onboarding/backend/agents/telemetry.cjs â†’ 2 refs (MARKOS_TELEMETRY, $lib)
  onboarding/backend/agents/orchestrator.cjs â†’ 2 refs (comments, collection)
  onboarding/backend/agents/mir-filler.cjs â†’ 4 refs (path comments)
  onboarding/backend/agents/msp-filler.cjs â†’ 3 refs (path comments)
  onboarding/backend/agents/llm-adapter.cjs â†’ 1 ref (comment)
  onboarding/backend/agents/example-resolver.cjs â†’ 2 refs (template paths)
  onboarding/onboarding.js          â†’ 2 refs (STORAGE_KEY, comment)

  LAYER 4: DOCUMENTATION (COSMETIC)
  â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  README.md             â†’ ~15 MARKOS refs (install commands, paths, descriptions)
  CHANGELOG.md          â†’ 5 MARKOS refs  (title, version notes)
  ARCH-DIAGRAM.md       â†’ title + topology label "MARKOS SYSTEM TOPOLOGY"
  TECH-MAP.md           â†’ ~40+ MARKOS refs throughout
  CLAUDE.md             â†’ references .protocol-lore (indirect, no direct MARKOS)
  GEMINI.md             â†’ references .protocol-lore (indirect, no direct MARKOS)
  .protocol-lore/QUICKSTART.md â†’ many MARKOS refs (commands, paths, descriptions)
  .protocol-lore/INDEX.md â†’ no direct MARKOS in content (just XML tags)

  LAYER 5: ONBOARDING UI (COSMETIC)
  â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  onboarding/index.html â†’ no direct MARKOS (title says "Client Intelligence Onboarding")
  onboarding/onboarding.css â†’ no MARKOS classes (uses generic names)
  onboarding/onboarding-config.json â†’ project_slug: "markos-client"
  onboarding/onboarding-seed.schema.json â†’ title: "MARKOS Onboarding Seed v2.1"

  LAYER 6: EXTERNAL PLATFORMS
  â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  GitHub repo: currently `markos`
  npm registry: `markos`
  .gitignore: comments say "MARKOS gitignore", paths say ".markos-local/"
```

### Data Flow Through Renamed Components

```
npx markos install
  â†’ bin/install.cjs reads VERSION from .agent/markos/VERSION
  â†’ Copies .agent/markos/ â†’ target/.agent/markos/
  â†’ Creates .markos-install-manifest.json
  â†’ Creates .markos-local/ scaffold
  â†’ Writes .markos-project.json

npx markos update
  â†’ Reads .markos-install-manifest.json (falls back to .markos-install-manifest.json)
  â†’ Checks .markos-local/ for overrides (falls back to .markos-local/)
  â†’ Updates .agent/markos/ (detects old .agent/markos/ and migrates)

node onboarding/backend/server.cjs
  â†’ Reads .markos-project.json (falls back to .markos-project.json)
  â†’ Supabase + Upstash Vector collections: markos-{slug}-{section}
  â†’ Writes to .markos-local/MIR/
  â†’ Telemetry: $lib = "markos-backend-telemetry"
```

## Patterns to Follow

### Pattern 1: Dual-Path Detection (Backward Compatibility)
**What:** Every file operation that reads/writes MARKOS-prefixed paths should check both old and new names  
**When:** During the transition period (at least one major version)  
**Example:**
```javascript
// In install.cjs / update.cjs / handlers.cjs:
const NEW_PATH = '.markos-local';
const LEGACY_PATH = '.markos-local';
const localPath = fs.existsSync(NEW_PATH) ? NEW_PATH : 
                  fs.existsSync(LEGACY_PATH) ? LEGACY_PATH : NEW_PATH;
```

### Pattern 2: Auto-Migrate on First Run
**What:** When update.cjs detects old paths, rename them before proceeding  
**When:** First run after upgrade to markos-named version  
**Example:**
```javascript
// Detect and migrate old directory names
if (fs.existsSync('.agent/markos') && !fs.existsSync('.agent/markos')) {
  fs.renameSync('.agent/markos', '.agent/markos');
  console.log('âœ“ Migrated .agent/markos â†’ .agent/markos');
}
```

### Pattern 3: Bridge Deprecation Package
**What:** Publish final version of `markos` that only prints a migration message  
**When:** Same day as `markos` first publish  

## Anti-Patterns to Avoid

### Anti-Pattern 1: Hard Cut Without Migration
**What:** Changing all paths without backward compatibility checks  
**Why bad:** Existing installs break silently â€” update.cjs can't find manifest, install.cjs can't detect existing install  
**Instead:** Dual-path detection for at least one version

### Anti-Pattern 2: Renaming Supabase + Upstash Vector Collections In-Place
**What:** Trying to rename existing Supabase + Upstash Vector collections  
**Why bad:** Supabase + Upstash Vector has no native "rename collection" operation; requires delete+recreate  
**Instead:** New installs use `markos-` prefix; migration creates new collections and copies data if old ones exist

### Anti-Pattern 3: Mass Find-Replace in Agent Prompts
**What:** Blindly replacing every "MARKOS" string in 317 agent files  
**Why bad:** Agent prompt content uses "MARKOS" as a token prefix (MARKOS-AGT-STR-01); changing these affects the entire token ID system  
**Instead:** Rename file names and directory, but leave internal token IDs for a separate token-system migration phase

## Sources

- Direct audit of all 17 target areas listed in the research brief

