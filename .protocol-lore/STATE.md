# Protocol-lore note on mission state (routing)

**Canonical source of truth for current milestone, phase, progress, and “what to do next” is:**

**[`.planning/STATE.md`](../.planning/STATE.md)** (repo root: `.planning/STATE.md`).

This file under `.protocol-lore/` exists only so older maps that referenced `STATE.md` in this folder still resolve to a clear rule: **do not duplicate live state here.** Always open `.planning/STATE.md` for GSD/MarkOS planning state.

**Optional historical snapshot (protocol era, not live):** v2.1 product-hardening phases 23–27 completed identity normalization, runtime hardening, merge safety, vector namespaces, and onboarding-to-execution handoff. Milestone sequencing after that is recorded only in `.planning/ROADMAP.md` and `.planning/STATE.md`.
