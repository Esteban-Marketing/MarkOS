# MGSD gitignore

# Environment secrets — never commit these
.env
.env.local
.env.*.local

# Node modules
node_modules/

# OS
.DS_Store
Thumbs.db

# Test artifacts
tmp/

# MGSD client overrides (private — do not commit)
.mgsd-local/
onboarding-seed.json
.mgsd-install-manifest.json
