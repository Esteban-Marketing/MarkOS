# MarkOS gitignore

# Environment secrets — never commit these
.env
.env.local
.env.*.local

# Node modules
node_modules/

# OS
.DS_Store
Thumbs.db

# Test artifacts
tmp/

# MarkOS client overrides (private — do not commit)
.markos-local/
.markos-local/
onboarding-seed.json
.markos-install-manifest.json
.markos-install-manifest.json
