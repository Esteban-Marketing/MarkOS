flow_id: "F-14"
flow_name: "source-extraction"
domain: "enrichment"
flow_type: "enrichment"
version: "v1"
openapi: "3.0.3"

info:
  title: "MarkOS Source Extraction"
  version: "1.0.0"
  description: "Extracts and normalizes intelligence sources from raw scraped content — produces structured source objects for vector ingestion and literacy corpus building."

x-markos-meta:
  actor: "System"
  auth_local: "none"
  auth_hosted: "N/A (local only)"
  handler: "handleExtractSources"
  local_path: "/api/extract-sources"
  hosted_path: ""
  slo_tier: "standard"
  method: "POST"

paths:
  /api/extract-sources:
    post:
      summary: "Extract sources from raw scraped content"
      operationId: "sourceExtraction"
      responses:
        "200":
          description: "Sources extracted and normalized"
        "400":
          description: "Invalid content payload"
        "500":
          description: "Server error"
