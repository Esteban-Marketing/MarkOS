flow_id: "F-11"
flow_name: "literacy-admin-query"
domain: "admin"
flow_type: "query"
version: "v1"
openapi: "3.0.3"

info:
  title: "MarkOS Literacy Admin Query"
  version: "1.0.0"
  description: "Local-only admin endpoint for querying the literacy vector store directly — used for debugging coverage gaps and manual retrieval testing."

x-markos-meta:
  actor: "Admin"
  auth_local: "Admin secret header"
  auth_hosted: "N/A (local only)"
  handler: "handleLiteracyQuery"
  local_path: "/admin/literacy/query"
  hosted_path: ""
  slo_tier: "standard"
  method: "POST"

paths:
  /admin/literacy/query:
    post:
      summary: "Query literacy vector store"
      operationId: "literacyAdminQuery"
      responses:
        "200":
          description: "Query results returned"
        "401":
          description: "Unauthorized — missing admin secret"
        "500":
          description: "Server error"
